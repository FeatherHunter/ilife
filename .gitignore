node_modules/
packages/*/dist/
*.tsbuildinfo
# 工作副本（施工/取证/验收的临时产物）：永不入库，权威在 docs/ 与票面
.scratch/
.tmp-*/
# vision-router 的截图缓存（工具产物，可重跑生成，不入库）
.dsh-vision-router/
# skills deck 插件的工作副本（工具产物，可重跑生成，不入库）
.dsh-mattskillsdeck-cache/
# DSH 自己的会话消息落盘（各包根与仓根的 .dsh-im/）：传输件、非源码，不入库
.dsh-im/
# P0 清账（#624）：ed87c79 曾把根目录施工作业副本误入库；以下全部锚定仓库根（/ 前缀），子目录不受影响
/_r*.txt
/_*.ps1
/.scratch-t*
/.scratch_t*
/.t499-*
/.tmp-394-*
/.tmp-*
/.t512j-*
/900
/undefined
/probe.mjs
/fieldLabel.ts
/_t407_audit/
# Windows 保留名（#707）：PowerShell 的 `2> NUL` 之类重定向写不出真的 NUL 设备时，
# 会在仓根造出一个名叫 NUL 的普通文件，让全仓搜索直接 exit 2。锚定仓库根，子目录不受影响
/NUL
