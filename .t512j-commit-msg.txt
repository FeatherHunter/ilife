fix(512-收尾): 表箱 821–1000 档居中 ＋ 512 档轴注落到实渲 15.05px，.ilife-help-shell 查清不删

复审件 `.scratch/t401j/review/终审.md`（88/100）的三处扣分，写集只有 blocks.ts ＋ charts.ts：

- P0 `blocks.ts`：`block-data-table` 基座规则内加一条 `margin-inline: auto`（表卡在容器余量里居中）。
  不动 `max-width: 680px`（抬到 780 会把本票修掉的「左右两角」退回去，且机检逐值钉死 680），
  不动 #154 的块级外边距（`margin` 仍逐字 `16px 0`；居中另起一条，不占「基座规则恰 1 条」名额）。
  窄档（512：容器 478 < 680）上限不触发 ⇒ `auto` 解成 0，逐值同改前。
- P1 `charts.ts`：`LINE_TEXT_MOBILE` 20 → 19.4 用户单位（512 档实渲 15.52 → **15.05px**）。
  复审那列的 20／12.4／10.8 逐值是**用户单位**常量（`getComputedStyle`），实渲 ＝ 单位 × 盒宽 ÷ 580，
  故 512 档实渲 15.52px ≈ 正文的 1.2 倍，不是 1.6 倍；要的「14–15px」里只有 15.0px 不破断言
  （`charts.test.mjs` H.t512 钉「四档实渲 ≥15px 且单调不降」，压到 15 单位只剩 11.64px）。
  四档实渲 15.05／16.04／17.32／17.32px；`LINE_TICK_TOP_MIN` 等派生常量复核后一字未动。
- P3：`.ilife-help-shell` **不是死码**——它是 HELP 壳的基座规则（`src/style.ts:673` 的 `helpShell` 区，
  由 `renderHelpShell()` 产出的根元素承载，四条用例钉死），主页产物里出现只因整张基座样式表被内联；
  按页裁样式表是装配处的事，不在本票写集 ⇒ 不删，账写在证据 §七-③。
- 证据：`docs/base/base-render/t512-最后一公里-证据.md` §七（含两行变异红／绿：14 单位 → 实渲 10.86px 红；
  `margin: 16px auto` → 「#154 的块级外边距不许被顺手改写」红；还原后 charts 90/90、表那四件 83/83 绿）。
