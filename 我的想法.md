我谈一下我的想法，然后你告诉我当前的进展和我的想法有没有偏离，以及做到了什么步骤了。

我开这个 map 是为了打造出一套 skills，这些 skills 一共应该是六七个技能。它支持用户单纯通过组装 skills，就可以提供相应的能力。

1. 单纯的 skills 场景：
比如说，用户加载“卡路里”技能，说“卡路里 help”，这时候系统返回“卡路里 help”的 HTML 文件。用户在 HTML 文件中复制出 prompt 提示词 500，将其发给 AI，AI 根据对应的规则去执行，执行完成后将结果作为 HTML 文件返还给用户。

2. DSH 插件场景：
在 skills 之上，我希望开发 DSH 插件。DSH 插件包括每个 skill 对应的插件，以及一个总览插件。
(a) 自动安装：用户在 DSH 上安装了这些插件后，这些插件肯定会帮我把对应的技能都装好。
(b) 总览管理：在设置面板里面肯定有一个总览模板，我可以看到我装了哪些插件，以及每个插件对应的环境有没有问题。这个时候，我通过某种方式打开 Better Sidebar 的侧边栏，比如打开一个卡片面板。在里面，我就可以看到根据环境变量或默认路径中数据库所存储的各方面数据状况（如行数据）。我可以对其进行操作，比如添加、删除，以及实现各种各样的功能。

最终，我们将所有的 skills DSH 插件通过 NPM 发布到网上，供别人使用。这是我最终最终的一个设想。