## #499 收口：对比页横幅人话化 ＋ 场景 09 十页基线再落

**① 本票改动（可见文本）——已成**
对比页超预算横幅由「超预算横幅：已嵌入 N 张，还有 M 张未嵌入（单页上限 1 MiB）· 替代操作：…」改成走公共层 `notice()` 的同域人话提示块（与「看身材照」页 #472 口径同族）：**点名哪张没显示 ＋ 为什么**，并给出替代操作；页上不再出现 `1 MiB`／字节数／开发前缀。
- 承载提交：`d5c674a`（`src/photo/compareDoc.ts` ＋ 断言收紧）
- 断言带**负向闸**：旧串 `超预算横幅|未嵌入|单页上限|1 MiB|换小图后重跑|已嵌入` 六串 0 命中才通过；改回旧句必红（实测 `fail 1`），逐字节还原后回绿（`fail 0`）。

**② 受影响样张——已重出**
`09-04-对比两张照片（跨标签·间隔48天）`／`09-05-生成身材照GIF（结果页·内嵌GIF）` 已按最新 dist 重出（真库只读副本），**390／768／1440 三档溢出 0／0／0**（`OVERFLOW-ZERO pages=2 cells=6 failed=0`）。

**③ 十页基线再落——两轮 10/10**
`gen-photo-baseline.mjs --check`（重落前）`BASELINE-DIFF 10 pages` → `--write` `BASELINE-WROTE 10 pages` → 双轮 `--check` **两轮都 `BASELINE-OK 10/10`、exit 0**。
- 负向两行：改 compare 页一句可见文本 → `BASELINE-DIFF 1 pages` **点名 `calorie.photo.compare`** → 逐字节还原 ＋ `tsc -b --force` 重编 → **`BASELINE-OK 10/10`**。（本仓实测：`Copy-Item` 保 mtime 会让 `tsc -b` 跳过重编、dist 残留变异版 → 假红／假绿，故还原后必 `--force`。）
- 回归：照片域 11 件 `tests 77 / pass 77 / fail 0`。
- 钉时钟机制沿用 #493**未改**（进程内 `CALORIE_TODAY=2026-09-06` ＋ #462 四条渲染时钟剥除）。
- 证据件：`docs/skills/skill-calorie/t499-基线再落.md`（逐页 sha 对照／两轮读数／负向两行／GATE-RUN 单行俱在）。
- 提交：`a3c875b`（证据件）＋ `ae63754`（追记）；基线脚本十条 sha 已随保全提交 `1cefc90` 在库、与 `HEAD` 逐字节等同。

**④ 为什么十页会同时变（如实记账，不是本图回归）**
`src/shared/copyArea.ts` ／ `src/shared/docPage.ts` 被**别席在途修改且长时间未提交**（mtime 08:22:25／08:23:03；`git diff --stat` `2 files changed, 37 insertions(+), 7 deletions(-)`），二者是**所有页面的共用装配件**（复制区／整页壳），故十页同时变。前席轮询 30 分钟 25 轮未等到其落定，故按实情重落。隔离复算证明：把这两件逐字节置回旧版 → 重建 → `--check` → **`BASELINE-OK 10/10`**（在册值与之自洽）。

**⑤ 后续接续条（写给编排者）**
- 本票的两件共享件已按**同一 blob** 落库，本票再漂项**已闭合**。
- 但推送后他席又落新改动（`base-render/src/blocks.ts`／`charts.ts` 等共用件在途，整树重建），基线随即再次陈化——已追记第七节。**本仓正处整轮重构波，多席以约半分钟级节奏改共用件＋重建 dist；此期间任何对当刻树取 sha 的基线都会在落库后数分钟内再次陈化**：这不是本票缺陷、不是闸门失灵。
- ⇒ **待整轮重构波收尾（`git status` 共用件全清）后，重跑一次 `node packages/skill-calorie/scripts/gen-photo-baseline.mjs --write` ＋双轮 `--check` 即可**（一条命令的事，属正常收尾）。本席未趁此改闸门口径（属口径变更，不在本票写集）。
