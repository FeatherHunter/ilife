---
"skill-calorie": patch
---

#278 · 场景 02 饮食「唤醒词 → 工作流程」接线：`pnpm help:build` 构建期从单一来源生成 70 条逐词表
（唤醒词与类型取 HELP 资产「饮食」组、命令取路由层 `ALL_ROUTES`，任一侧缺一条即抛、不静默少一行），
速查表「唤醒词」列对饮食场景改取该词自己那条（源＝路由层），示例里的写死日期换成登记过的占位符
（`<日期>`／`<开始日期>`／`<结束日期>`／`<对比开始日期>`／`<对比结束日期>`，按出现次序替换）。
判据：本票单测 `tests 9 / pass 9 / fail 0`；可复跑 `t278-block.mjs` `RESULT: 11/11`（含两行变异自证）；
`packages/skill-calorie/test/*.test.mjs` 基线 `fail 75` → 本席 `fail 74`（不新增红）。
未到位：`pnpm help:examples:check` 124/127，三红均不属本票（`calorie.history` 基线即红；
两条体脂／围度对比的示例需先动 `docs/research/t81-seed.mjs` 的登记值，按硬性条款停手上报，未自行改）。
