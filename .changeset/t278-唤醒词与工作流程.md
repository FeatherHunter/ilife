---
"skill-calorie": patch
---

#278 · 场景 02 饮食「唤醒词 → 工作流程」接线：`pnpm help:build` 构建期从单一来源生成 70 条逐词表
（唤醒词与类型取 HELP 资产「饮食」组、命令取路由层 `ALL_ROUTES`，任一侧缺一条即抛、不静默少一行），
速查表「唤醒词」列对饮食场景改取该词自己那条（源＝路由层），示例里的写死日期换成登记过的占位符
（`<日期>`／`<开始日期>`／`<结束日期>`／`<对比开始日期>`／`<对比结束日期>`，按出现次序替换）。
判据：本票单测 `tests 10 / pass 10 / fail 0`；可复跑 `t278-block.mjs` `RESULT: 11/11`（含两行变异自证）；
`packages/skill-calorie/test/*.test.mjs` 基线 `✔ 1126 / ✖ 155` → 本席 `✔ 1130 / ✖ 149`（新引入的红 0 条）。
占位符改成**按名分组**（单日期／区间／对比，组里的名字与 `docs/research/t81-seed.mjs` 的
`PLACEHOLDER_SUBSTITUTIONS` 的键逐字同）＋ 一个 `placeholderGroupFor` 按日期个数选组（1／2／4，其它个数即抛）；
生成物逐字节不变（本仓唯一有日期的示例落在 1／2／4 三个个数上）。
未到位：`pnpm help:examples:check` 124/127，三红均不属本票（`calorie.history` 基线即红，属 #559；
两条体脂／围度对比的示例卡在种子库 `2026-08-23~08-29` 没有体脂／围度行，属 #559）。
