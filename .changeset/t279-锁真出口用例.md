---
"skill-calorie": patch
---

feat(279): 场景 02 饮食 83 条唤醒词的真出口结构化断言落到一处可重跑的锁（新增 `packages/skill-calorie/test/t279-真出口用例.test.mjs`；跑法与种子同放 `docs/skills/skill-calorie/t279-真跑.mjs`，判据件与可复跑脚本共用一处，唤醒词与命令行从 `dist/triggers/routes.generated.js` 的 `ALL_ROUTES` 取、不手抄）

跑过的判据：① 83 条逐条 exit 0（83/83）＋ `data.output` 绝对路径（83/83）＋ 真落盘（每趟恰 1 件、无两条词落同一份）＋ 字节如实（信封 `delivery.bytes` ＝ 盘上大小，83/83）＋ 完整文档（`<!doctype html>` 在第 0 字节 ＋ charset ＋ 样式段 ＋ `</html>`，83/83），字节区间 68351–167950；② 写类词 19 条——写前确认页 4 条（页上「预检确认」在、库一格未动）／写后回执 15 条（`✅ 操作回执` ＋ `📋 字段变更` 齐，改类出「改前 → 改后对照」，按条删出「删除前的原值（逐条）」，删类回执的「影响行数」与真库落下的行数同值）／真库写入写后回读 15 条对得上，按现值再写一遍内容与计数都不变；③ 空库 64 条取数类读命令 → exit 4 共 61 条 ＋ `ERR 4: 取数失败（缺失阻断）` ＋ 不落盘（例外 3 条具名），空窗 48 条 → 47 条完整页 ＋ 空态句 ＋ 引导句（例外 1 条具名：`看营养分析`，与 `t425` 裁定 4 冲突，`t280` 台账逐格缺陷第 82 格，归属 #275）；④ 断言消息点得出卡在第几步（唤醒词命中／命令实跑／页面）；⑤ 变异自证 98 通过 0 失败 → 18 条红 → 还原 98 通过 0 失败；⑥ `node --test packages/skill-calorie/test/t279-真出口用例.test.mjs` → `tests 98 / pass 98 / fail 0`。
