---
'skill-calorie': patch
---

#464 体成分记录表「备注」列口径订正：该列缺值可见文本由空串改 `—`（基准 `t395-融合基准.md` §四裁定 2；老正本 `body_composition_view.html:211` 的 `esc(s){return String(s==null?'—':s)}` 对 `null` 写 `—`，「空」只来自空串值，写侧统一 `input.note ?? ''`），注释归因由 `:342／:347-352` 改 `:211` 的 `esc` 口径并写明「空」来自空串值而非模板选择。复制 payload 仍保留原始 `null`，两套口径不互染。新增 `test/t464-备注列口径.test.mjs`（3 用例：NULL 格等于 `—`／载荷该字段仍为 `null` 不含 `—`／注释逐字命中），t362 原样不动。变异自证：判据改回空串 → 可见条必红，还原 → 必绿。
