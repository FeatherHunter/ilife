"skill-home": minor
"dsh-home-ilife": minor
---

feat(794): 居家设置页收窄（照记账样板铺开）＋主密钥改从文件读——技能侧回执扩一组解析后绝对路径；页面从6行可改收成1可改＋4只读；删files两键、加key.file；调用参数口令退场。
- **技能侧**：`home.config.read` 的 `data` 新增 `resolved` 一组（数据目录／库文件／HTML产物目录／备份目录／主密钥文件），算式只有一处定义地（`src/fetch/paths.ts` 的 `*Of` 一族，体检与HELP交付都转调它）；`backup.dir` 相对值改「按库目录解」；可改落点 `db.dir` **写**的时候落算出来的绝对路径（首次落文件／保存／重置），**读**仍认空串＝按默认落点；`files.help`／`files.lookup` 出表（文件名回代码常量），过渡按#762（读容忍、写即清）；主密钥改从 `key.file` 指的文件读（内容trim，调用参数给了就响亮失败）；备份zip不装密钥文件，回执说明。
- **插件侧**：数据目录是页面上唯一可改项；库文件名／HTML产物目录／备份目录／主密钥文件**只读**显示技能算好的绝对路径（面板不拼路径、不算默认值）；只读目录行的「浏览」按钮保留但不可点击；只读行不进保存；控件文案不再出现省略号。
- 旧技能（回执没有 `resolved`）仍可用：只读行显示空串，不报错、不编路径。
