---
"skill-calorie": patch
---

test(280): 卡路里场景 02「饮食」83 条唤醒词逐条真跑 ＋ 验收墙记账

验收目录 `docs/skills/skill-calorie/scene02-验收墙/`：83 件产物（发布名 ＝ 唤醒词 ＋ `.html`）＋ `桌面墙-1280.html`（83 格 × 1280）／`手机墙-390.html`（83 格 × 390）／`总索引.html` ＋ `逐格缺陷清单.md` ＋ 清单 `manifest.json` ＋ 生成器 `gen-wall.mjs`。

台账 `docs/skills/skill-calorie/t280-真跑台账.md`／`.json`：逐条唤醒词／命令原文／参数／exit／产物绝对路径／字节／第 0 字节／页内 H1。读数：**exit 0 83/83、落盘 83/83、第 0 字节全为 `<!doctype html>`、字节区间 68,351–168,483 B**；每条跑前把库还原成同一份播种快照（写词会改库）。

**两态分开记**：库为空 → 读命令 65 条里 61 条按设计 `exit 4` 且不落盘；窗口为空 → 10 条里 9 条出完整页 ＋ 空态句 ＋ 引导句（**例外 1 条**：`calorie.view.nutrition-analysis` 空窗报 `exit 4`，已点名）。

自检正反两面都走过（`docs/skills/skill-calorie/t280-断言-自检正反两面.mjs`）：正例 `--check` → `83 格；链接 419 条；缺失 0 -> 可发` exit 0；反例（改坏清单一行）→ exit 1 且点名那份；还原 → exit 0 且清单逐字节相同。清单不带 BOM；墙里未加 `loading="lazy"`。

**本票不改任何包源码**（`packages/**` 一行未动）：只取用当刻 master 编译出的产物，唤醒词与命令从运行期总表 `dist/triggers/routes.generated.js` 的 `ALL_ROUTES` 筛 `scene === '02'` 现读现跑。条数口径：运行期总表 **83 条**；地图设计期老技能词表 `SCENE_02_DIET` **70 条**，其 70 条全含于这 83 条，多出的 13 条是运行期补词。
