---
"skill-calorie": patch
---

test(369): 卡路里场景 08「身体细节」装机 ＋ 13 条逐条真跑 —— 验收目录 `docs/skills/skill-calorie/t369-验收/`（13 件结果型产物 ＋ 5 件写前预检页 ＋ 桌面墙 1100×900／手机墙 390×820 ＋ 总索引 ＋ 链接自检 ＋ `manifest.json`）＋ 清单 `docs/skills/skill-calorie/t369-逐条清单.md`（13 行六项读数：命令／exit／产物绝对路径／sha256／是否整页／关键字段是否在；总门＝exit 全 0 且整页全绿）

**本票不改被测命令**（只装机与跑验收，`packages/skill-calorie/src/**` 一行未动，`routes.ts` sha256 前后一致）：
13 条键取自 `src/body/routes.ts` 的 `list:'wake'` 声明（order 237–249）**现读现跑**，参数照声明 `cli` 逐字
（`<日期>` 占位符按语义填）。判据的期望值全部**手算**（Jackson-Pollock 7 点法重算体脂率、逐项差值/变化率、
对比两段均值与变化率），不吃页面自述；并含「窗口口径」负断言（>90 天的老记录不许混进 90 天窗，
`看体脂`＝全部历史／`看围度` 默认 90 天，出处 `src/body/view.ts:15-23`／`:41`）。

**数据库一律临时库**：`SKILLS_DB_PATH` 钉到 `.scratch/t369/tmpdb`；真库
`D:\2Study\StudyNotes\.db\calorie_data.db` **零写入**（sha256／字节／改动时间／旁文件跑前跑后一致，读数见证据件）。
**负向自证**：声明源变体停用「对比围度」一条 → 该行必红（门 exit≠0）；还原 → 回绿；另补出口级负向
（给一个不存在的键，出口必非 0）。**装机位**：`%USERPROFILE%\.dsh\profiles\web\node_modules\skill-calorie`
经 3 跳 Junction 落到 `D:\ilife\packages\skill-calorie`，两边 `SKILL.md` 与 `dist/cli/cmd_read.js`
sha256 逐字节相同 → 装机位读到的就是本仓当刻版本。
