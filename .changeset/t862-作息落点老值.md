---
'skill-schedule': patch
'dsh-schedule-ilife': patch
---

fix(862): 作息落点认 #843 之前的两级 `html.dir`（设置页不再显示 `schedule_html/help/help`）

#843（「产物落点分家」）把 `html.dir` 从「产物根 ＋ HELP 子目录」两级改成「产物根」一级，HELP 那一级另由
`html.helpDir` 补；老配置文件里的两级值（本机是 `dir: schedule_html/help`）照新算式算就多出一级
——`<库>/schedule_html/help/help`，产物根也被塞进 HELP 目录里，设置页那行只读的「HELP 产物目录」显示的就是它。

改：`src/fetch/paths.ts` 新增 `resolvedHtmlDirs()`——值的末段与生效的 `html.helpDir` 逐字相同、且值多于一段时，
把末段还给 `helpDir`；三个 `resolve*` 与 `health.ts` 的产物根一律改吃它（配置文件一个字不改）。新用例
`test/t862-老值落点.test.mjs` 四条读数，含反向对照（不认账时算出的就是 `…/help/help`）。

本版把插件精确 pin 跟到 `skill-schedule@0.3.3`（装 `dsh-schedule-ilife@0.3.11` 才会拿到这一版技能）。
