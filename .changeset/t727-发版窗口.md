---
'skill-calorie': patch
'skill-chef': patch
'skill-home': patch
'skill-memo-ilife': patch
'skill-schedule': patch
'dsh-calorie': patch
'dsh-chef': patch
'dsh-home-ilife': patch
'dsh-memo-ilife': patch
'dsh-schedule-ilife': patch
'base-link-core': patch
'base-combos': patch
---

chore(727): 发版窗口定版 —— 十家技能／插件升版，另补发公共层两件

本仓的爱生活配置面（技能设置页、配置体检、页签槽注册）早已就绪，但 `package.json` 的版本号与
registry 上那一版**同值**，同号不能重发 ⇒ 用户装到的世界停在改造之前。本票把版本挪开，好让
这些改动有地方落：

- `skill-calorie` 0.2.4 → 0.2.5；`skill-chef`／`skill-home`／`skill-memo-ilife`／`skill-schedule` 0.2.0 → 0.2.1
- `dsh-calorie` 0.2.5 → 0.2.6；`dsh-chef`／`dsh-home-ilife`／`dsh-memo-ilife`／`dsh-schedule-ilife` 0.2.0 → 0.2.1
- 五个插件里精确 pin 技能的版本行随动；各自的 `scripts/wizard-publish.sh` 脚本头随动
- **公共层 `base-link-core` 0.3.3 与 `base-combos` 0.3.3 只补发、不改版本**：registry 停在 0.3.0，
  而六个技能的 `dist/config.js` 都 `import { loadConfig } from 'base-link-core'` —— 不补这一格，
  新技能在装机上 import 就失败（第三道门 G3 的六条契约键红即此事，最小复现已入走查单）。

对外行为零变更（只动版本号与脚本头）；记账那一对（`skill-bill`／`dsh-bill-ilife`）按本票矩阵押后到
#726 落地后再发。
