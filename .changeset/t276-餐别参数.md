---
"skill-calorie": patch
---

feat(276): 餐别 5 条唤醒词的运行时命令行带上餐别参数（`src/home/routes.ts` 的 order 73/74/75/76/77 各加 `meal`：早餐／午餐／晚餐／加餐／all）＋ `src/home/today.ts` 的 `viewDietOverview` 按 `meal` 分流到餐别分布页（取值与解析仍只走 `src/diet/reviewDocs.ts` 的 `mealParamOf`：缺参／未知值 exit 2，不给 `meal` 的调用逐字不变）＋ 可复跑判据 `docs/skills/skill-calorie/t276-餐别修复-run.mjs`（47 条全绿；场景 01 产物 sha256 未变；变异自证 1→0／5→5；`pnpm gen:check` 由红转绿；`npx tsc -b packages/skill-calorie` exit 0）
