---
"skill-calorie": patch
---

feat(440): 围度部位中文名单一来源并统一「肩围」（软尺环绕量口径）

13 部位的中文名收进 `src/fetch/body.ts` 一处（`MEASURE_DEFS` / `MEASUREMENT_ZH` / `measureCamelName`），
`bodyDocs.ts`、`compare.ts`、`wizardPlate.ts`、`analysis/cross.ts`、`body/log.ts` 五处改为引用；
肩部用词由「肩宽」统一为「肩围」。字段名、库表结构、列序、唤醒词与命令名一律未动。
用例：新增 `t440-部位名单一来源.test.mjs` 7/7，`t360`/`t361` 各 8/8，变异改坏必红／还原必绿两行读数见证据。
