---
"skill-calorie": patch
---

feat(534–539): 卡路里场景 08「身体细节」18 件产物视觉整改 —— 双端自适应／HELP 同档手机端／形状化／文案去冗余／独立视觉终审

负责人六条要求（逐字见 `docs/skills/skill-calorie/t534-场景08-视觉整改基准.md` §0）在场景 08 上的落地。
判据面与改前读数：`t534-场景08-视觉整改基准.md`（六条判据 ＋ 形状化规则 ＋ 五维尺）、`t534-改前读数.md`
（分隔符节点级 186 处／`RESULT: 0/18`；30 页 54 格溢出全零＝回归门；`viewport-fit` 0/18；安全区只 bottom）。
收口：`t539-证据.md`、`t539-逐格缺陷清单.md`、验收墙 `docs/skills/skill-calorie/t369-验收/`。

**一轮做完的四件事**

1. **双端自适应与手机端同档**：18 件全部接上 `assembleDocPage({ pageUi: true })`（页面级移动端配方：
   `viewport-fit=cover`／`env(safe-area-inset-*)` 两向以上／820 档 44px 触摸区／640 档字号下限与读数卡两格／
   窄屏表格卡片化／页内导航横滑）；390 档正文类 ≥12px；四张长页补页内定位。
2. **形状化（负责人第 5 条）**：页头三处 `·`、CLI 参数文档副标题、`；`／`、`／`｜`／`~` 硬串、字段清单长串、
   `sqlite:total_changes` 与记录主键上屏等，一律换成仓内既有形状件（徽章列／列表行／键值行／结论条／事实条／
   小表／迷你条／折叠块）；节点级命中 **186 → 0**，`RESULT: 18/18`。
3. **文案去冗余（负责人第 4 条）**：`<title>` 统一 `卡路里 身体细节`（仓内先例＝场景 09 的 `卡路里 身材照片`）；
   窗口只在窗口条说一处；同事实一页多处收敛；`~`／半角标点／英文裸词换人话。
4. **两条本波裁定**：①「看围度」与「看围度趋势」不再出逐字节相同的产物（`BodyMeasureView.windowGiven`
   区分点名／缺省窗口，页面按它换主次）；②围度看页补 `windowLabel`，窗口不再只藏在取数层里。

**结构**：`src/body/**` 内按页族拆件（`compare.ts` → `compareCompositionDoc.ts`／`compareMeasureDoc.ts`／`compareUi.ts`；
`receipt.ts` → `receiptUi.ts`／`receiptData.ts`；新件 `bodyReadUi.ts`），逐件 ≤350 LF，台账逐件对账通过。
