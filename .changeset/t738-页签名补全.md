---
"dsh-schedule-ilife": patch
"dsh-home-ilife": patch
"dsh-chef": patch
"dsh-bill-ilife": patch
"dsh-life-pack": patch
---

feat(738): 面板页签名补全成产品名（作息管家／居家管家／私家大厨／饼干记账）＋ 删掉那行说「有个缺省启用的开关」的静态文本（维护者 2026-09-20 提出）。名字两处产地同改：各家自己的 `SLOT_TITLE` 是主（页签 label／侧边栏页签名／本家设置页标题都从它出），总管 `nav.ts` 那张 `MANAGER_TABS` 是镜像（缺席兜底与更新列表行首用）。判据：`packages/plugin-manager/test/tabs-738.test.mjs` 11 条（真产物跑 apply 读页签名 ＋ 渲面板读页签条字与页头 ＋ 更新列表行首 ＋ 那行静态文本屏上/源码/产物三处清零 ＋ 短名字面量残留）＋ 边界门新增「导航表的页签名与各家契约件逐家一致（#738）」（真 import 两边产物来比）。
