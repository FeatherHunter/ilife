---
"dsh-life-pack": patch
---

fix(737): 面板版本行改走**自动读取**（维护者 2026-09-20 裁定）—— 删掉手写常量 `MANAGER_VERSION`，改由宿主半 `src/manager-version.ts` 运行时读自己这份已安装包的 `package.json`，经电话 `ilife-manager.version` 交给面板渲染（读不到回 `unknown`，不抛）。「屏上那行 ≡ 装机包版本」由此成为构造性事实，定版只剩改 `package.json` 一处；门 `packages/plugin-manager/test/version-737.test.mjs` 改咬「产物里不许再有写死的版本号」与「屏上那行跟着宿主的值跑」。
