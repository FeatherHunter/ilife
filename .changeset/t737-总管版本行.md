---
"dsh-life-pack": patch
---

fix(737): 面板版本行印回包自己的版本 —— `MANAGER_VERSION` 0.2.6 → 0.2.7（定版只 bump `package.json` 时它漂开了一版）；并立一条门 `packages/plugin-manager/test/version-737.test.mjs`，把该常量（从产物 `dist/nav.js` 取）与 `package.json` 的 `version` 钉在一起，产物 `dist/client.js` 里那行屏幕文案也必须带着同一个版本号。
