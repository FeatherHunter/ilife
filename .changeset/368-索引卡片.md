---
"skill-calorie": patch
---

feat(368): 卡路里场景 08 身体细节 · 索引表与 HELP 卡片（框架级）——`SKILL.md` 身体段新增「写词 → 预检页命令」索引表（**7 条**：5 写词 ＋ 2 页面入口行，入口行标明自己是入口）＋ 包内资产 `workflows/08-身体细节.md`（同表 ＋ 入口一览 ＋ 13 条逐条落点）；HELP 卡片详情层在同一条冻结槽位 `SceneEditableField` 上由 1 行改 3 行（**命令**＝注册表命令名／**工作流程**＝子功能名／**可执行命令**＝CLI 全文恒末行），身体 13 条的命令字段值 **13/13 == 注册表命令名**；速查表新列「预检页」由生成器反查 `src/body/wizardPlate.ts` 的 `WIZARD_WRITE_KEYS` 派生（生成器 `gen-cli.mjs` 新增标记块、`build-help.mjs` 加列）；`check-examples.mjs` 的表示例解析改按**列名**定位（插列后位次口径读不到行）。新增门 `test/t368-索引卡片.test.mjs`（`RESULT: 11/11`）＋两处变异自证（删一条映射必红／命令字段改坏必红，逐字节还原必绿）；`help-center-106` 同源断言面同步（条数改由路由层实况派生，未放宽判据）。顺带落地 #367 交棒的速查表陈旧 5 行。
