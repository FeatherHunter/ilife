---
"skill-calorie": patch
---

feat(277): 预检确认页 4 条按老实物重做——导入预检页（预览／校验两形态）＋ 营养表识别确认页，装配落 `src/diet/precheck.ts`／`precheckLabel.ts`，取数与逐行校验落 `precheckPort.ts`（一个定义地：页上判的原因与写命令判得一样）；批量导入预览那一页从 `src/render/trendMiscPort{,Docs}.ts` 就地搬进 `src/diet/`；新增读命令 `calorie.view.label-precheck`；两条过程型词由入口带 `entry:"precheck"` 先出确认页、不写库
