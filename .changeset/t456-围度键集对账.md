---
"skill-calorie": patch
---

feat(456): 围度键集对账（TOP 平局次序表与展示列序表集合相等断言）

`src/analysis/cross.ts` 只加一个导出（`WAIST_DIVERGENCE_ORDER` 加 `export` 一词，次序与用法一字不动）；
新增 `t456-围度键集对账.test.mjs`：两表各 13 项且键集合相等，加第 14 字段／删一字段必红（变异两行读数见证据）。
字段名、库表、列序语义、页面行为文案、唤醒词与命令名一律未动；真库零写入。
