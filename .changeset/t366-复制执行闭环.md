---
"skill-calorie": minor
---

feat(366): 场景 08 预检确认页「复制—执行闭环」——复制区那一段改成**能直接跑的写命令**（填不全时仍是老正本的缺项清单注释串，不给跑不通的命令），老 prose 参数清单搬到「核对」折叠区；同批落 裁定 1（皮褶钳模式体脂率只读＋显示命令将算出的那个数＋文案「体脂率可留空，由命令按 7 点换算」）与 裁定 3（围度逐部位区间／皮褶 0.1·0·100／体脂率 0.01·0·60，提示与校验同源消掉同页两套口径），来源／性别改下拉；`body/wizardPlate.ts`＋`body/wizardDocs.ts`＋`test/t366-复制执行闭环.test.mjs`（新建 14 条）
