---
"skill-calorie": patch
---

fix(274): 食品库两态分开 —— 「整个数据面为空」才 `exit 4`，「库非空但本次零命中」出完整页 ＋ 空态句 ＋ 引导句（`exit 0`、落盘）。

- 取数件 `src/diet/libraryPlate.ts`：`buildProductSearch`／`buildProductLibrary` 先取库内在架总数，只有**总数为 0** 才抛 `missing-data`；库非空而本次零命中返空盘，装配层照老实物 `food_search.html:98-101` 对 `items.length === 0` 出 `emptyState`。
- 装配件 `src/diet/libraryDocs.ts`：来源脚注改报**库内在架条数**（原先取本次命中数，零命中页会印成「在架食品共 0 条」，非零命中页也会把 5 条印成 1 条）。
- 测试件 `test/t274-食品库页.test.mjs`：把原先把两态锁成一条的「空库」拆成两条（整体为空 ⇒ `exit 4` ＋ 0 产物；库非空零命中 ⇒ `exit 0` ＋ 完整文档 ＋ 空态句 ＋ 引导句），另补空态句／引导句逐字与口径说明行三条断言（收口复核点名的探针盲区）。
- 共用件的旧引脚 `test/render-t9.test.mjs`：库非空零命中不再断言抛错，改断言返空盘；「整个库为空」的阻断断言移到空库那一条测试里。
