---
"skill-calorie": patch
---

fix(271): 两处接线缺口落地 —— ①「看饮食总览」那条路由记录带 `entry=overview`，`src/home/today.ts` 的 `viewDietOverview` 见它整页换**总览页**（`buildDietOverviewView` ＋ #275 交的 `buildDietOverviewBlock`；从前与「看最近 7 天饮食」参数一字不差、落的是条目列表页）；②同一条命令**两态分清**：库非空、窗口内零记录 ⇒ 完整页 ＋ 空态句 ＋ 引导句、`exit 0`、落盘（照 `hasAnyDietRow(db)` ＋ #275 的 `buildEmptyWindowDoc`），库为空仍 `exit 4` ＋ `ERR 4: 取数失败（缺失阻断）`、不落盘（不放宽）。顺带把 `hasAnyDietRow` 的两份私有副本（`diet/review.ts`／`diet/rankingPlate.ts`）收敛成取数层那一处。跑过：`t271-总览接线与两态.test.mjs` 9/9 绿；不给 `entry` 的 13 条词 ＋ 场景 01「看今日饮食概览」同库同参数产物逐字节未变（14/14，其中 5 条餐别词仅复制日志第 5 段渲染时刻戳不同）；`tsc -b packages/skill-calorie` exit 0；`pnpm gen` 后 `gen:check` PASS。
