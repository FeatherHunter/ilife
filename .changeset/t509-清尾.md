---
"skill-calorie": patch
---

fix(509): 场景 02 饮食文本清尾 —— 33／81 批量导入预览两页的内部词归零（「精确名匹配／导入后建议补录／热量卡／库热量／✗ 缺库」改读者的话、单元格 `—` 改「食品库中无此食物」、副标题去掉命令名 `diet.batch`）＋ 同形页按唤醒词出标题（两条拍营养表的入口经路由带 `source:"photo"`，05 出「拍照记一餐 · 回执」、06 出「拍照补记一餐 · 回执」，并各带一行「来源：营养表照片」）＋ P0 回查探针升级为按页按类型（`/` 分隔引文逐个查、描述型引文走具名判据）
