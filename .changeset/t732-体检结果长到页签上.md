---
'dsh-life-pack': patch
---

配置体检区重排（票 #732）：体检结果长到页签上。

- 页签圆点从「实心／空心＝装没装」改成「颜色＝体检档位」（灰＝没跑过、绿＝正常、红黄＝要处理）；红黄那几家在家名后带一枚小数字（如 `备忘录 红 2`）。
- 顶部不再是一家一盏灯的按钮排，改成一行总账：`配置体检` ＋ 按档上色的总计数（`红 2 · 黄 4 要处理`／`六家正常`／`还没体检`）＋ 右侧「体检一次」。同屏实测体检区从 153px 收到一行。
- 撤掉两段常显的静态说明（三档图例、「上面那排圆点是体检灯…」）；「只看不改：不建目录、不改配置、不自动重置」挪进「体检一次」的悬停提示。
- 数据面一个字未动：判据仍住技能侧，各家那张 `· 体检` 表照旧。
