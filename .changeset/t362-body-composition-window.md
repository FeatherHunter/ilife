---
'skill-calorie': minor
---

#362 看体脂的窗口不再静默截断：`calorie.view.body-composition`（「看体脂」／「看体成分」）此前**不传窗口参数时默认只给 90 天**，页面不给任何提示——本次改为**不传窗口参数 ＝ 全部历史**，显式传 `days` ＝ 近 N 天、显式传 `dateFrom`+`dateTo` ＝ 闭区间，并把**当前窗口口径写进页面可见文本**（`窗口：全部历史`／`窗口：近 90 天`／`窗口：A → B`）。同批落四块：① 窗口三态在 `body/bodyPlate.ts` 一次裁决（口径句 `compositionWindowLabel` 唯一出处），`limit` **只截显示**——窗口内总条数写进表头（「窗口内另有 N 条，本页只列最近 N 条」），不再静默丢数；② 取数侧新增 `CompositionWindowInput` 与私有 `windowClause()`（`fetch/body.ts`，**点名件**：窗口→日期谓词的唯一定义地），`listCompositions`／`trendComposition`／`trendCompositionBySource`／`compositionSourceCount` 四条取数共用同一口径，第二参数位照旧收 `number`（＝`{days}`）故既有调用逐字不变；③ 基准 §四 **裁定 5**——`source=all` 按来源分组展示（接 `sourceSeries`／`sourceCount`）＋「来源不可直接对比」提示句，**KPI「趋势点」＝图上真画的点数**（逐来源一图，不合并成一条混源线），杜绝「有图＋趋势点 0 天」；④ 基准 §四 **裁定 2** 两条分开落地——可见文本缺值写 `—`（老正本 `:342`／`:347-352`）、复制 payload **原始空值原样透传**（`null` 保留，绝不写 `—`）；另补老正本三项：锚点大数字＝**最新一条**（`:74-84`／`:155`，不是窗口均值）、Δ 与「距上次测量 N 天」（`:339-340`，首条写「暂无对比基线」`:344`）、空态句带「怎么记第一条」（`:355`）。「看体脂趋势」那条词的时间窗语义**不动**（`src/body/routes.ts:17` 的 `{"days":90}` 示例逐字保留，本票零改动）。新增 `test/t362-窗口不截断.test.mjs`（15 用例：冻结种子跨 240 天 9 条 → 无参读数 9／`days:90` 读数 4／口径句随参变／`source=all` 组数 == `COUNT(DISTINCT source)`／KPI 与图同批定／裁定 2 两条分开断言／锚点与 Δ 与空态／`limit` 不静默截断／非法窗口报用法错／`routes.ts:16-17` 逐字复核）。连带：`test/t398-source-group.test.mjs` 一条真出口断言原依赖旧默认窗（读数 5→6 必红），改为**显式传窗口**保住原语义。变异自证三例（默认窗写死 90→9 例必红；payload 写 `—`→载荷断言必红；去掉不可比提示句→裁定 5 必红），还原后 15/15 全绿且四件逐字节复原。`npx tsc -b packages/skill-calorie` exit 0；告警线门 `RESULT: 58/58 PASS`（台账本票零改动）。
