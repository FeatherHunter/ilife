---
"skill-memo-ilife": patch
---

备忘录附件目录：字符串前缀校验换成真目录 ＋ 真包含判定（#712）

- 附件目录解析成绝对路径，必须真的存在且是目录；取不到就给人话原因（点名 `MEMO_MEDIA_DIR` 与它当前指的路径），不静默降级、不替人建目录。
- 传入附件路径与目录都归一成绝对路径算相对路径：不在目录内（含同开头不同目录）、带 `..` 穿出去、就是目录自己 —— 一律拒（exit 2）。
- 落库仍是相对路径（`notes.media_path` 取值形状不变），老数据不用迁。
