---
"skill-bill": minor
"dsh-bill-ilife": minor
---

feat(749): 记账设置页收窄（六家样板）——技能侧回执扩一组解析后绝对路径；页面从「6 行可改」收成「1 可改 ＋ 5 只读」

- **技能侧**：`bill.config.read` 的 `data` 新增 `resolved` 一组（数据目录／库文件／第二份库／HELP 产物目录／
  备份目录／备份文件名示例），算式只有一处定义地（`src/fetch/paths.ts` 的 `*Of` 一族，体检与 HELP 交付都转调它）；
  `backup.dir` 相对值改「按库目录解」（值绝对用它／相对 `<库目录>/<值>`／空串 `<库目录>/backups`）；
  可改落点 `db.dir` **写**的时候落算出来的绝对路径（首次落文件／保存／重置），**读**仍认空串＝按默认落点。
- **插件侧**：数据目录＝页面上唯一可改项；库文件名／预算账户文件名／HELP 产物目录名／备份目录／
  备份文件名前缀**只读**显示技能算好的绝对路径（面板不拼路径、不算默认值）；只读目录行的「浏览」按钮
  保留但不可点击；只读行不进保存；控件文案不再出现省略号。
- 旧技能（回执没有 `resolved`）仍可用：只读行显示空串，不报错、不编路径。
