---
'skill-calorie': patch
---

#361 围度全量表 13 列 ＋ 窄屏卡片：看围度页（`calorie.view.body-measure`，不带 `metric`）的记录表由「日期＋胸／腰／腹／臀＋备注」6 列扩为「日期＋全量 13 项＋备注」15 列，列序**直引** `fetch/body.ts` 的 `MEASUREMENT_FIELDS`（不在本文件另定第二份序），中文名复用既有 `MEASURE_ZH`。同批落三件：① **窄屏卡片**——照老正本 `body_measurements_view.html:128-151`／`:389-399` 移植，页内 CSS 按 640px 断点切宽表／卡片，二者同一份 `v.items`；卡片只列已填项（`filter` 口径），整行无值时写「未填围度」（老 `:397`）；类名前缀 `msr-`，只作用本页，不碰共用层。② **裁定 2 两条判据分开**——表体缺值格仍写「—」（#360 口径沿用），**复制数据 payload 保持原样透传、绝不写「—」**（真库 `null` 原样保留、字段缺位则不出现），两处不互染。③ 表标题改为「围度记录（共 N 条 · 13 项全量 · 单位 cm）」。趋势闸门、KPI 与图区逻辑**保持 #360 原样不动**（本票只改 `metric` 为空的列表分支）。新增 `test/t361-measure-full-table.test.mjs`（8 用例：13 名逐个＋表头列序＋表体「—」＋payload 空值＋窄屏卡同源＋趋势闸门回归＋整页骨架＋空库语义）。实证：删掉一列（肩宽）→ 2 用例必红（`表头缺「肩宽」`）；payload 写「—」→ 载荷断言必红；两次还原均 8/8 绿且源码哈希逐字节复原（`sha256=902b715b…01a1`）。
