"skill-calorie": patch
---

feat(271): 场景 02 条目列表页 11 条照老实物重做 —— `calorie.view.diet` 窗口词那一支按
`t425-融合基准.md` §五 第 ② 类骨架逐行落位（眉标只写中文族名／结论句走副题槽／页内导航
`sec-kpi`+`sec-trend`+`sec-dist`+`sec-daily`+`sec-meals`+`sec-copy`／KPI 四卡／每日摄入折线
单点不成线改说明句／餐别分布零值不画柱身改 `—` 卡／按日汇总无记录日写 `—`／「全部记录」
**九列含备注列**、时间截到分、按日期时间倒序／空窗出完整页＋空态句＋引导句／口径说明行／
复制区双按钮（给了 `command` 才出复制日志，第 4 段＝本次命令原文）／来源脚注），并按派单
**原地**加三个可选入参 `command?`／`mealView?`（调 #273 的 `buildMealDistributionBlock`）／
`overviewView?`（调 #275 的 `buildDietOverviewBlock`）；`buildViewDietDoc` 未搬（仍住
`src/render/dietDocs.ts`，`src/home/today.ts` 的 import 位一行未动）。

`calorie.today` 那三张页（看今日饮食／看有备注的饮食记录／看今日饮食记录）补齐同一套骨架：
页内导航／结论句／口径说明行／来源脚注（老实物 `today_diet.html:173-176` 本来就有页脚来源行）／
复制区双按钮，时间与备注按裁定 4 走 `—`，**备注列改回恒出**（老实物 `today_meals.html:168`
恒有、§五 第 9 行恒出），处理体 `src/diet/today.ts` 的 `viewToday` 传
`command: commandLine('calorie.today', params)`（#275 在 `viewTodayWater` 上的那处改动逐字保留）。

改到的件：`packages/skill-calorie/src/render/dietDocs.ts`（332 LF，线内）、
`packages/skill-calorie/src/diet/todayDocs.ts`、`packages/skill-calorie/src/diet/today.ts`、
`packages/skill-calorie/test/diet-list-t271.test.mjs`（骨架＋备注列结构化断言＋变异自证靶）。

未接线（归 #276 席，`src/home/**`）：`calorie.view.diet` 的处理体不传 `command`／`mealView`／
`overviewView` ⇒ 那 8 条窗口词的复制区当刻只有「复制数据」一颗按钮（不出死按钮），
餐别页／总览页两支已备好、等参数从命令面带进来。
