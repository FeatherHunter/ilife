---
"skill-calorie": patch
---

feat(703): 包对外的公开接口缩到一页 —— 删 19 个能力门上的 `run*`、写命令声明加 `doc` 位、`exports` 缩到三条

包门 `src/index.ts` 的对外名从 **402 个**收到 **63 个**（＝包外真实取名面：全仓 import 解析到 `dist/index.js`
的具名导入，逐名清单见 `docs/skills/skill-calorie/t703-公开面收窄-证据.md`），类型 117 个全撤（包外零引用）；
`package.json` 的 `exports` 从 8 键收到 **3 键**（`.`／`./cli`／`./package.json`——最后一条是清单不是接口，
插件按包名读版本号要用它）。

同时：十个能力门上生产从不走的 **19 个 `run*`** 删除（6 个测试件改打真出口 `cli/cmd_read.ts` 的 `dispatch`）；
写命令的整页回执端口搬进命令声明（`WriteCommandSpec.doc?`，41 条写声明挂各自能力目录的回执件），
`cli/write.ts` 那一行六个 `??` 改成一次查表、删掉 6 条回执件导入——第 7 个带整页回执的能力不再需要碰 `cli/`；
写声明的 `shape: 'receipt'` 随之删掉（它与 `kind: 'write'` 是同一件事实的两种说法，形状的唯一定义地落生成器）。

**CLI 端口与信封形状、页面产物零变化**；对外面收窄按本仓发版口径（技能线锁 `0.2.x`、按技能能力面判）走 `patch`。
