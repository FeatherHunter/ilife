---
"skill-calorie": patch
---

fix(272): 空窗整改 —— 排行榜一族把「窗口内零记录」与「库里零记录」判成两件事（判据＝`t425-融合基准.md` 裁定 4 的 2026-09-15 澄清）

- **窗口为空**（有命令、有窗口，库里有别处的记录、这一段零记录）⇒ 出完整页 ＋ 空态句 ＋ 引导句，`exit 0`、落盘并回绝对路径（单榜页与全榜页两条都改）。整改前这两态在取数层塌成一条 rejection ⇒ 空窗走缺失阻断、0 产物、本票自带的空态文案永远出不来。
- **库为空**（`food_log` 整表零行）⇒ 口径**一字不动**：`exit 4` ＋ `ERR 4: 取数失败（缺失阻断）`、不落盘。窗内有行但被榜的口径筛空（如只播一条 💧水 看低热量榜）也不在这一支里，仍走既有缺失阻断。

改到的件：`packages/skill-calorie/src/diet/rankingPlate.ts`（取数与两态判定：只看库整表行数与窗内行数，不看错误文案）、`packages/skill-calorie/src/diet/rankingDocs.ts`（两页的空窗装配 ＋ 三段条三色的逐色对照改为如实描述）、`packages/skill-calorie/src/diet/ranking.ts`（件头记两态落点，两条出口一行未改）、`packages/skill-calorie/test/t272-空窗两态.test.mjs`（新增：两态出口不同 ＋ 空窗页仍是完整页 ＋ 三段恒好 100%）、`packages/skill-calorie/AGENTS.md`（`rankingDocs.ts` 越告警线的挂号行）。

`dietEngine.ts` 一行未动（它是别的族也共用的取数件），故别的族页面产物逐字节不变；告警线台账当刻 76/83，红点 7 条全是别家件的台账陈化（与本次改动无涉）。
