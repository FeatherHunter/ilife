---
'skill-calorie': minor
---

#86（map #63）wizard 4 页复刻 D1：3 配置型 wizard＋1 GIF 框选器（静态 HTML＋copyText）。

读 60→64、共 99（D2 只许追加）；SoT 436 零改动；M6 正文回引 4 键（#98 G9）。
新键 `calorie.view.measure-wizard`／`view.composition-wizard`／`view.photo-log-wizard`／`view.gif-planner`
（取数 render/wizardPort.ts＋组装 render/wizardPortDocs.ts＋keys/dispatch／NEW_KEY_ROUTES 4 新拟短名／
combos＋present／HELP AUTO＋SKILL M6 回引）。
B7 边界：表单零 JS、无 cropper.js、无 base64 内嵌，复制走 Base P0 双通道。
主证据 `docs/research/t86-wizard-verify-evidence.md`（tmp 种子库真机：4 页 exit 0＋只读 sha 同值＋先看后写 receipt）。
