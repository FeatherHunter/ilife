---
"skill-calorie": patch
---

feat(275): 营养／饮水／总览页 4 条 —— 两态分清 ＋ 两个具名区块函数的调用面冻结 ＋ 探针类名订正

判据与机器读数：`docs/skills/skill-calorie/t275-报告.md`；可复跑 `docs/skills/skill-calorie/t275-真跑.mjs`。

- **两态分清**（`t425` 裁定 4 的 2026-09-15 澄清）：`calorie.view.nutrition-ratio`／
  `calorie.view.nutrition-detail`／`calorie.view.today-water` 在**窗口内零记录**时改出完整页
  （空态句 ＋ 引导句 ＋ 页内导航 ＋ 来源脚注 ＋ 复制区）；**库为空**仍是 `exit 4` ＋
  `ERR 4: 取数失败（缺失阻断）`、不落盘——既有设计行为不动。分辨判据＝`hasAnyDietRow(db)`
  （`diet/nutritionPort.ts` 新增，只看 `food_log` 有没有行，不看错误文案）。
  新增 `buildEmptyWindowDoc(input)`（`diet/nutritionPortDocs.ts`）：窗口为空那一态的整页装配。
- **两个具名区块函数的调用面冻结**：`buildNutritionRatioBlock(v, opts?)`（交 #273，
  `opts = { copy?, command? }`——复制区那一节只在给了 `command` 时出，`copy:false` 可显式关掉）、
  `buildDietOverviewBlock(v, command?)`（交 #271，不给 `command` 则不出复制区）。
  两块都不夹整页壳，宿主页 `assembleDocPage` 的 `charts` 传 `true`。
- **探针按真类名断言**：产物里的区块根类是 `ilife-block-copy-block`／`ilife-block-feedback-block`，
  旧写法 `ilife-block-copyBlock`／`ilife-block-feedbackBlock` 在产物里恒不命中——那样的断言
  红不了也绿不了（本票订正）。`test/t275-营养饮水总览页.test.mjs` 30 条全绿，含四个具名区块函数的
  直测、空库／空窗两态、逐块对老实物与变异自证的原样必绿读数。
