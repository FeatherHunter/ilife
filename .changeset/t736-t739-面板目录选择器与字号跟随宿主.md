---
"dsh-bill-ilife": patch
"dsh-calorie": patch
"dsh-chef": patch
"dsh-home-ilife": patch
"dsh-memo-ilife": patch
"dsh-schedule-ilife": patch
---

feat(736/739): 六家单品设置页两处交付——目录行接系统文件夹选择器；页头文字的字号与字族跟随宿主。

- **#736 目录选择器**：行表新增 `directory` 档（六家 12 行：六家 `db.dir`、记账与居家的 `backup.dir`、
  卡路里的 `photos.dir`／`xunji.stateDir`、备忘录的 `media.dir`／`lark.qrDir`），页面多一枚
  「选择文件夹…」按钮唤起操作系统文件夹对话框：选中回填绝对路径、取消一字不动、这条路供不了时
  出人话并收起入口。命名空间按**可选查找**现取（`ctx.get`），**不写进 `inject`**——
  写进去＝硬依赖，提供方缺席时整包会被停靠、设置页会跟着装不上。
- **#739 面板文字跟随宿主**：六家的文字表统一——不再写 `font:` 简写或 `fontFamily`（字族交给宿主继承链），
  字号一律相对单位（`em`），`card` 上钉死的 13px 拿掉；由仓根用例锁住「六家同值、不许回分叉」。
