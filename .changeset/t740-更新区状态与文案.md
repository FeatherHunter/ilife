---
"dsh-life-pack": patch
---

fix(740): 更新区每个状态都要给得出下一步——① 待重启改按**事实**判（磁盘版本 ≠ 正在跑的版本），不再按原因码（真机那次因此把「该重启」漏成一句看不懂的「装不上」）；② 拦截态各带动作：`installation-changed` →「重新检查」、`recovery-required` →「重试安装」，面板代劳不了的才摊手工命令并写明用途；③ 装成一家之后其余各家的旧快照自动作废（进「重新检查」态），不再让用户「全部点安装」然后连吃几个错；④ 「没东西可装」时不再显示拦截（已是最新的那家不再挂黄条 ＋ 重装命令）。全表用户可见文案按「事实 → 后果 → 动作」重写，清掉自造词与口语（半截任务／电话没接上／回执异常／还出现就重装／查宿主日志），重启动作统一写成「重启 DSH（退出后重新打开）」。

第三轮（朗读表逐条念屏上那句话，逮到的都是「屏上叫你去点一颗够不着的按钮」）：⑤ 结果浮层一开就盖住面板题头的「检查更新」，所以「改完再回来重读一次」这类下一步一律改成画在**那一行上**的「重新检查」——`unknown-profile`／`registry-conflict`／`invalid-installation`／`source-install` 与两张「还没查过」的卡共六态从「一颗按钮都不给」改过来；⑥ 查不到最新版／检查过期／装失败这些**失败的行**（没有快照、原先一颗按钮都不画）一律补一颗「重新检查」，设置页那张缺席卡同样补；⑦ 那句话写着「下面这条命令」的行，命令块必须真出来（原先 `recovery-required` 那句下面什么都没有）；⑧ 待重启横幅退成一行总账（只点名哪几家要重启，理由与做法留在各家卡片上，不再一屏说两遍）。
