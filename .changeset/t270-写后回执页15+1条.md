---
"skill-calorie": patch
---

feat(270): 场景 02 写后回执页 15＋1 条照老实物 `crud_receipt.html`（26279 B，只读）重做内容 —— 四块标题逐字摆上（`✅ 操作回执`／`📋 字段变更`／`📊 今日累计`／`📋 复制明细`）；操作回执按**本次操作**取词（新增成功／修改成功／删除成功），读数卡出状态／影响行数／写入字段，批量补记另出三格计数（新增／已在库跳过／分类失败）；`📋 字段变更` 分三支：改类出「字段｜改前｜改后」对照、删类出「记录编号｜日期｜删除前的原值｜状态」快照、新增类（含批量导入）只摆本次写入的字段名，**不硬套对照**；`📊 今日累计` 出 `getDailySummary` 现值七行并给出「还剩 N／已超 N／未设目标」，**删类不出这一块**（照老实物 `:374-387`）；`📋 复制明细` 出本次逐条时间／内容／状态，**删类不出**（老实物 `:390`）。另按裁定 1／2／3／7 走页题、副题结论句、页内导航（锚点与区块 `id` 同源）、口径说明行与来源脚注，复制区恒双按钮且日志第 4 段＝`commandLine(key, params)` 的本次命令原文（含本次 `--params`，照抄可重跑）。

第 14 条 `calorie.product.import`（批量导入食品）一并收进整页：它原走 `shared/writeParts.ts:83` 的片段回退面（产物里既没有整页也没有复制区），与同族的存食品／改食品／下架食品不同形。

本票实测并改掉一处内容缺陷：装配件原先拿**本次参数**（写进去的新值）当「改前」印，对照表两列同为同一个数，状态卡还照着那一列叫用户「照改前的原值再改一次」。现改为缺值写 `—`（裁定 4）并在表下补一句口径行说明为什么这一列为空，`test/calorie-c43.test.mjs` 的断言同步收窄为「改后＝写进去的那个值；改前要么是写前真值、要么是 `—`，**绝不许等于改后**」。

落到件：`packages/skill-calorie/src/diet/receipt.ts`（装配件，唯一作者）；`packages/skill-calorie/test/calorie-c43.test.mjs`（收窄本票两条读数）；`packages/skill-calorie/test/t270-回执四块.test.mjs`（新增，把票面判据钉成断言）。

未做项（数据面缺口，不在本票声明路径内）：写前真值与「按条件删」的删前逐条原值由 `src/diet/edit.ts`／`src/diet/products.ts` 丢掉，补齐须改那两件；本票只做成「回执带对照／带快照就出，没带就出 `—` 与一句空态说明」，不编数。
