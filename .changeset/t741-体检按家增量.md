---
"dsh-life-pack": patch
---

fix(741): 配置体检**按家增量**出结果——维护者真机反馈「所有插件全部体检好后全量提示出来」。改的是面板侧那一层：`health-panel.ts` 的 `run()` 原先各家里全部置 `running`、等 `loadHealthReports` 整批落定才 `setRows`，所以最慢那家不回话前屏上一直「体检中…」；现在取数口多一个「一家好了就回调一次」的通道（`loadHealthReports(call, tabs, onRow?)`），面板按家 patch 状态（函数式 `setRows`，两家同拍落定也不丢），**哪家先跑完哪家先画**，顶部汇总行随之更新。取数入口仍只有一处（结构门照旧）。

本票**不改**的两处（已开票说明）：① 六家宿主半用的是 `spawnSync`（阻塞同一个宿主线程），六通电话在宿主里排队干——真并发要把六个包的桥改成异步 `spawn`，属另一件；② 一家的报告是一次 `spawnSync` 拿回来的整份 `{ items }`，「每一项出来就更新」需要技能侧改产出协议（逐项／流式回包），按现行契约（判据住技能侧、插件只透传）不做。
