---
"skill-chef": minor
"dsh-chef": minor
---

feat(796): 私家大厨设置页收窄（照记账样板）——技能侧回执扩一组解析后绝对路径；页面收成「1 可改 ＋ 3 只读」

- **技能侧**：`chef.config.read` 的 `data` 新增 `resolved` 一组（数据目录／库文件／HELP 产物目录／
  场景产物根），算式只有一处定义地（`src/fetch/paths.ts` 的 `*Of` 一族，体检转调它）；
  `files.help`／`files.lookup` 出表（文件名回代码常量，产物名逐字不变，老文件走退休过渡）；
  可改落点 `db.dir` **写**的时候落算出来的绝对路径（首次落文件／保存／重置），**读**仍认空串＝按默认落点。
- **插件侧**：数据目录＝页面上唯一可改项；库文件名／HELP 产物目录／场景产物目录**只读**显示技能算好的
  绝对路径（面板不拼路径、不算默认值）；只读行不进保存；控件文案不再出现省略号。
- 旧技能（回执没有 `resolved`）仍可用：只读行显示空串，不报错、不编路径。
- `src/config.ts` 的写盘口径与退休清单随 #766 提交 `f2f22a7e` 先行入仓（其提交信息已具名披露含 796 在途行）。
