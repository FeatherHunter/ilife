---
'skill-calorie': patch
---

#443 看围度全量表判据收紧（只收紧测试口径，不改行为）：① 新增全空行真出口用例（UPDATE 造全空行＋有值行并存，走 PATH `calorie-cmd-read` 真出口断言 exit 0＋该行 13 格「—」＋卡片「未填围度」）；② 载荷断言由全文 `JSON.stringify.includes('—')` 改为 13 字段逐个断言（数值或原始空值、绝不出现「—」），种子 R1 备注改为含「—」（`r1—d`）并断言原样保留。`src/**` 零改动。
