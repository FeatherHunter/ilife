---
"skill-calorie": patch
---

feat(385): 热量缺口页按老实物 `calorie_deficit.html` 逐项核（`src/render/trendDocs.ts` 缺口节一段＋新测试 `test/analysis-deficit-385.test.mjs`）

- 明细表「目标」列改回老侧口径 `target.weekly_deficit_per_day`（改前误用 `target.intake`，拿摄入目标比缺口列）
- 明细表「状态」列按老侧补齐三态 `✓ 达标／⚠ 偏低／✗ 超量`（改前只有两态）
- 补表底合计三行（合计摄入／合计消耗／合计缺口，老侧 `tfoot` 的等价物，走 base-paint B-05 行列表）
- 折线补 `yTicks:3`＋`labels:'select'`＋`format`（老侧自注「Y 轴刻度文字缺失」到此消掉；公共层本支持，属本页漏传）
- 口径不动：`deficit = (TDEE＋运动) − 摄入`、`KCAL_PER_KG=7700`、空窗阻断；不新增命令、不改参数形状
