# #764 证据 · 作息管家设置页收窄 · **技能侧**（回执扩组 ＋ 落点算式收口 ＋ 写盘口径 ＋ 删键 ＋ 体检三条 ＋ 候选表）

判定：**PASS**（技能侧判据全绿：新增 10 条用例 10／10、包级全量 115／115、真出口回执与落点对账一致、
两处变异自证「打掉必红／还原必绿」跑齐；红／黄／绿三档各有一份确定性读数）。

## 一、交付了什么（逐件一句，为什么）

| 件 | 改了什么 | 为什么 |
| --- | --- | --- |
| `src/fetch/paths.ts` | 落点算式收成**纯函数一族**（`dbDirOf`／`dbFileOf`／`htmlDirOf`）＋新增 `resolvedSchedulePaths()` 三格；`DEFAULT_DB_FILENAME`／`DEFAULT_HELP_DIR` 改成对配置表的**具名引用** | 票面要求「落点算式只许有一处定义地」（照 #749 样板）；只读行要显示的那一组值必须由技能侧算（面板不拼路径） |
| `src/help/helpPaths.ts` | `DEFAULT_HELP_DIR` 改成对 `fetch/paths.ts` 的转出，删掉本件那份字面量 | 同一件事不留第二个定义地 |
| `src/config.ts` | 默认值表 5 键 → 3 键（删 `files.help`／`lark.cliPath`）；退休清单填上此二键；加 `writableDefaults()`（可改落点写绝对路径）＋ `withoutBlankEditableDir()`；记忆位改按配置文件路径 | #761 定稿＋#746 总口径回灌（读认空串、写落绝对路径）；记忆按文件是 #749 样板同形（家目录注入下不读脏缓存） |
| `src/cli/config.ts` | `schedule.config.read` 回执 `data` 新增 `resolved` 三格 | #746 处置 8：只读展示要的绝对路径由**技能侧回执**给出 |
| `src/fetch/feishu.ts` | 删配置项分支（`lark.cliPath` 不再是配置）；候选表补 4 条（scoop exe／cmd、homebrew、npm-global、local），保留现有 5 档；抽出纯函数 `larkCliCandidates()`；`where` 多行时优先取 `.cmd` | #761 定稿＋维护者裁决；候选顺序的唯一定义地（体检调它）；`.cmd` 优先是删掉显式路径后暴露的真缺陷（见 §五.1） |
| `src/fetch/index.ts` | 转出新增的算式／候选／回执形状 | 能力门面 |
| `src/health.ts` | 落点改调纯算式（删本地 `splitDirSegments`）；查找改调 `findLarkCli()`（删本地第二份表）；快段超时改 8 秒；第 ④ 条 action 改指配置文件；第 ⑥ 条重写成红／黄／绿三档（与面板同判据、同话术） | 票面补注 2＋#761 定稿（体检三条连带）；`ILIFE_CONFIG_DIR` 本家已无此句（断言其缺席） |
| `src/help/helpFile.ts` | `helpFileStem()` 回代码常量（新增 `HELP_FILE_STEM`），不再读配置 | 删键：文件名回代码常量，产物名逐字不变 |
| `packages/skill-schedule/SKILL.md` | 说明面一句跟删键走 | 文档同步（一句）；**此句已被主干提交 `7776217d` 收录**（并行 #754 席代提交顺手带走，内容即本票所写，见 §五.2） |
| `test/t764-设置页收窄.test.mjs`（新） | 10 条：候选表／首档命中／回执三格＋对账／写盘绝对路径＋读认空串／老文件过渡＋写即清／文件名常量／体检红＋④＋黄＋绿 | 票面《验收命令》第 1、3、5、6 条 ＋ 两条补注的正面验收 |
| 旧锁 6 件 | `config-695`（默认值 3 键＋退休清单＋写盘口径）／`cli`（远端钉红改走 PATH 掐断）／`config-seam`（挡板改走 PATH 首位）／`help-file-202`（主体＝常量）／`plan-ensure-batch-599`＋`plan-feishu-660`（注释） | 写盘口径变了、显式覆盖点没了，旧断言必须跟着走（#749 同例） |

## 二、机器读数（门禁运行，逐条与 `.scratch/locks/gate-runs.log` 对账；导出件见 `t764-门禁运行记录.md`）

GATE-RUN runId=6cad4c1b-1124-4856-bc42-d2a1df643fb9 cmd=node node_modules/typescript/bin/tsc -b packages/skill-schedule
GATE-RUN runId=1f70ab1e-1734-4c42-9d8c-5f86d34feeaa cmd=node node_modules/typescript/bin/tsc -b packages/skill-schedule
GATE-RUN runId=d7c9d580-c876-4b37-928f-650cc1a3a14a cmd=node --test packages/skill-schedule/test/*.test.mjs
GATE-RUN runId=ed22c813-52c6-4e80-b828-96cd65cb40fe cmd=node --test packages/skill-schedule/test/t764-设置页收窄.test.mjs
GATE-RUN runId=4c2f3617-cf22-4031-86c6-f71ffec6dcbc cmd=node .scratch/t764/mutation-skill.mjs

| runId（尾段） | 命令 | exit | 摘要 |
| --- | --- | --- | --- |
| `6cad4c1b` | `tsc -b packages/skill-schedule` | 0 | 技能包编译通过 |
| `1f70ab1e` | 同上（`where` 修后重编） | 0 | 同上 |
| `ed22c813` | 技能侧 t764 单件 | 0 | `tests 10／pass 10／fail 0` |
| `d7c9d580` | 技能侧包级全量 | 0 | `tests 115／pass 115／fail 0` |
| `4c2f3617` | 变异自证（见下） | 0 | 红 `pass=8 fail=2` → 还原后绿 `pass=10 fail=0` |

### 真出口回执（票面《验收命令》第 3 条形状，逐字）

```
{"version":"0.1.0","skill":"schedule","shape":"detail","key":"schedule.config.read","data":{
 "path":"C:\\Users\\辰辰洋洋\\AppData\\Local\\Temp\\t764-read-XlQob1\\.ilife\\schedule.yaml",
 "dataDir":"C:\\Users\\辰辰洋洋\\AppData\\Local\\Temp\\t764-read-XlQob1\\.ilife\\data","created":true,
 "values":{"db":{"dir":"","name":"schedule_data.db"},"html":{"dir":"schedule_html/help"}},
 "resolved":{"dbDir":"C:\\Users\\辰辰洋洋\\AppData\\Local\\Temp\\t764-read-XlQob1\\.ilife\\data",
             "dbFile":"C:\\Users\\辰辰洋洋\\AppData\\Local\\Temp\\t764-read-XlQob1\\.ilife\\data\\schedule_data.db",
             "htmlDir":"C:\\Users\\辰辰洋洋\\AppData\\Local\\Temp\\t764-read-XlQob1\\.ilife\\data\\schedule_html\\help"}}}
```

对账：`db.dir` 空串（＝按默认落点）⇒ 三格全落在 `<家>/.ilife/data` 之下；`values` 里已无 `files`／`lark` 两组。

### 飞书三档读数（各一份确定性读数）

- 红：`{"id":"lark.cli","status":"red","message":"没找到飞书 CLI：面板「飞书 CLI」那一行有安装指引（复制 prompt 照做）。","action":"装好并登录后，在面板上点重新检测。"}`（子进程 PATH 掐掉真 CLI）。
- 黄：`{"id":"lark.cli","status":"yellow","message":"找到了 C:/…/stub/lark-cli.cmd，但还没登录／日历读不到。",…}`（挡板 `mode:unavailable`）。
- 绿：`{"id":"lark.cli","status":"green","message":"已就绪：C:/…/stub/lark-cli.cmd（版本 lark-cli version 1.0.59-stub）。","action":""}`（挡板正常态）。

### 过渡读数（保存一次前后 diff）

```
db:
  dir: "C:\\Users\\…\\.ilife\\data"
  name: old_name.db
html:
  dir: schedule_html/help
```

老文件里的 `files:`／`lark:` 两组消失；`name: old_name.db` 与 `html.dir` 逐字不变。

## 三、变异自证（打掉一处 ⇒ 必红；还原 ⇒ 必绿）

**产物级变异**（不是源码级），原因与 #749 同一件：工作区此刻有并行席在途提交，
源码级变异有被代提交扫描带进主干的风险；`dist/**` 在忽略清单里，还原路径正是「按既定的重新生成流程复原」。
变异窗口全程在加锁状态下（`.scratch/t764/mutation-skill.mjs`）。

| 变异 | 红读数 | 还原读数 |
| --- | --- | --- |
| 把回执 `resolved` 组打掉（`dist/cli/config.js`） | `exit=1 tests=10 pass=8 fail=2` | 逐字节拷回 ＋ `tsc -b` ⇒ `exit=0 tests=10 pass=10 fail=0` |

## 四、结构纪律（必报五步）

- **第一步影响清单**（开工前已报）：`src/{config,health}.ts`、`src/cli/config.ts`、`src/fetch/{paths,feishu,index}.ts`、
  `src/help/{helpFile,helpPaths}.ts`、`SKILL.md`、旧锁 6 件、新 `t764` 1 件——每行一件，零跨件。
- **第二步结构设计**：不新建目录；`fetch/paths.ts` 升为落点算式的唯一定义地（纯算式 ＋ 读配置的薄壳），
  体检与回执都转调它；`feishu.ts` 的 `larkCliCandidates()` 为候选顺序的唯一定义地。对外面没收窄也没放宽。
- **第四步超线报警**：本包告警线数字待维护者定（`AGENTS.md` 明写待定），无可执行的数字门。
  实测行数变化：`src/health.ts` 598 → 526（净减 72，删第二份表与注释）；其余改动件都在原量级内。
  `src/fetch/db.ts`（439）与 `src/help/scenes/help-assets.ts`（2171）是既有大件，本票一行没碰。
- **第五步交付对账**：`git diff --stat -- packages/skill-schedule` 实得改动件与上面清单逐行一致，偏差为零
  （`SKILL.md` 一句已在主干提交 `7776217d` 里，见下）。

## 五、范围内的重要修复与说明（如实记）

1. **`where` 首行缺陷（真缺陷，已修）**：删掉显式路径后，兜底探测在挡板与真机上都可能命中 `where` 吐出的
   无扩展名垫片（`…\lark-cli`，Windows 起不来），回 `LARK_UNAVAILABLE`。此前显式路径盖住了它，
   挡板测试全绿是假象。修法＝win32 优先取 `.cmd` 那一行（真 CLI 本体就是 `.cmd`，#759 实测）。
   根因读数：`.scratch/t764/dbg-seam.mjs`（已删）`run status: 4 … lark-cli 不可用：…\stub\lark-cli` →
   修后 `run status: 0 … 远端已建`。此修只动查找顺序，不改判据。
2. **`SKILL.md` 一句的归属**：本票所写（删键跟进），在工作区里被并行 #754 席的提交 `7776217d`
   （`feat(754)`）顺手收录。内容逐字是本票的，`git show 7776217d -- packages/skill-schedule/SKILL.md` 可查。
   按 §3.2 第 4 条记：这是我的改动、不是无主 diff；提交信息没点名 #764，证据在此补记。
3. **备忘 parity**：备忘录那张表（`skill-memo-ilife`）尚未实施（#760 未动工），故「两张表逐条相同」
   今日不成立；本票只把作息这张写成目标形状并逐条断言（10 条之二），跨包锁进 #758（票面原话即此意）。

## 六、未做项（如实记）

1. **发版与装机**：`skill-schedule` 要抬版本、发布才到维护者的 app 里；那是地图里的 #755，本票只交源码与读数。
2. **真机点屏**：本席无 GUI 点击能力；面板行为已用真产物量过（读数脚本见插件侧证据），真机那一跳归维护者。
