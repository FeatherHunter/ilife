# t844 证据 ——【种子】隔离种子库：锚点日期 ＋ 覆盖五域的可复现数据

判定：**PASS**（票面验收命令 exit 0；17/17 条核验通过；真实 `~/.ilife` 逐件未动，另经仓库独立门复核）

锚点日期：`2026-09-21`（本机今天；跨天复现同一份时用 `--anchor 2026-09-21`）。

## 交付物

| 件 | 路径 | 入仓 |
| --- | --- | --- |
| 种子脚本 | `packages/skill-schedule/scripts/seed.mjs` | 是 |
| 种子库 | `.scratch/t844/home/.ilife/data/schedule_data.db` | 否（临时件） |
| 读数（锚点／窗口／条数／内容摘要／库 sha256） | `.scratch/t844/seed-meta.json` | 否 |
| 门禁运行记录（对账源，协议 §2.3 第 4 条） | `docs/skills/skill-schedule/t844-门禁运行记录.md` | 是 |
| 本证据件 | `docs/skills/skill-schedule/t844-证据.md` | 是 |

票面写面写的是 `.scratch/tE0/`：那是建图脚本里的**票键占位**（`.scratch/map-pages/round4/spec-live.json` 里本票的 `paths` 逐字是 `packages/skill-schedule/scripts/seed.mjs` ＋ `.scratch/t844/`），本件按该规格落到 `.scratch/t844/`。

## 票面口径 ↔ 机器读数

| 票面要求 | 实测 |
| --- | --- |
| 验收命令 exit 0 | `node tooling/run-locked.mjs --ticket 844 -- node packages/skill-schedule/scripts/seed.mjs --check` → exit 0（runId=`eb3d9f48-ba8b-43be-8ac4-af1ed907eaf3`） |
| 打机器摘要行 | `RESULT: 记录 1037／计划 73／摘要 112／一级分类覆盖 8/8` |
| 跑前跑后 `~/.ilife` 下文件 mtime 与 sha256 逐件不变 | 本件自断言：新增=0 删除=0 改动=0（判据件＝`test/helpers/real-home-snapshot.mjs`，同一进程内跑前跑后各快照一次）；另由仓库独立门 `tooling/check-real-home-untouched.mjs --run` 复核：406 件／403 文件／34,531,796 字节，sha=`a7f9867f510bf2bf` 前后一致 |
| 不写 `~/.ilife/**`（真库与真配置） | 写面只在 `.scratch/t844/` 一处；库落点是**隔离家目录**的默认数据目录，进程家目录在 import 编译产物之前就已被接管并当场断言「≠ 真实家目录」 |
| 不改 `src/**` | 本件只读 7 份编译产物与 3 份既有测试／工具件（见下面的 dist 指纹行）；`git status --short -- packages/skill-schedule/src` 里的在途改动全部属于同仓另一席的票（#843 交付面），本票一行未碰 |
| 固定锚点日期 ＋ 可复现 | 锚点钉进库与读数文件；同锚点**两次构建**的内容摘要逐字一致（`629dcaa4acb127fb`），审计时间戳（created_at／updated_at／generated_at）按锚点钉死 |
| 改坏一处必须变红、改回必须变绿 | 见「变异自证」两行读数 |

## 数据形状（覆盖五域）

锚点 `A` ＝ 2026-09-21。

| 表 | 读数 | 形状 |
| --- | --- | --- |
| `schedule_records` | 1037 条／61 天（`A-60`~`A`） | 每天 17 条、`00:00`~`23:59` 首尾相接无洞无叠；三种日型共用同一组时段边界（工作日／周末／低产日），故条数恒定；一级分类覆盖 8/8，锚点日 7 个 |
| `schedule_plans` | 73 条／10 天（`A-6`~`A+3`） | 7 条/天铺在 7 个整点；6 态完成齐 ＋ null（未复盘）；未来 3 天 completion 全为 null；跨天标题重名 2 条；已软删 1 条（`is_active=0`）；飞书已同步 2 条 |
| `daily_summary` | 112 行／14 天（`A-13`~`A`） | 每天写满 8 个一级分类（当天没有的记 0 分钟），且每天合计＝当天记录合计（跨表断言） |

逐域落点：

- **查询与浏览**：单日 17 条铺满 24h（时间轴／四卡摘要／健康分都有输入）；详情页 9 字段在锚点日全非空；修正过的一条（`edit_count=1` 且 `updated_at` 晚于 `created_at`）；区间查询有 61 天可查。
- **写入与同步**：修正样例（`op=amend` 走过的真实路径）；飞书已同步两条（`feishu_event_id` ＋ `last_synced_at` 非空），其余留未同步态。
- **日程与计划**：10 天计划面；6 态完成 ＋ 未复盘；未来三天待办；跨天重名标题（标题搜／三元组查重）；已软删一条。
- **分析与洞察**：跨度盖住「本月」与「上月」整月（最坏情形也够）；`A-3` 是低产日（工作 0 分钟、调整为休息），给异常检测与月度对比造出红行。
- **辅助与管理**：飞书探测有已同步／未同步两态；初始化回执与首次使用向导要的是**空库**，另指一个不存在的库路径即可（种子不预置空库——空库＝没有种子数据）。

## 给下游票的用法

隔离是「把家目录指过来」这一条（不需要测试专用开关，也不需要手写配置文件）：

```powershell
$env:USERPROFILE='D:\ilife\.scratch\t844\home'
$env:HOME='D:\ilife\.scratch\t844\home'
node packages/skill-schedule/dist/cli/cmd_read.js schedule.record.today
```

配置缺省 `db.dir` 正是 `<隔离家目录>/.ilife/data`，所以真 CLI 走生产路径就吃到这份种子；页面产物落在同一目录下的 `schedule_html/`（#843 交付面：页落根、HELP 留 `schedule_html/help/`）。

跨天复现同一份数据：`--anchor 2026-09-21`（不给＝按跑当天）。

## 机器读数

### 门禁运行（与 `t844-门禁运行记录.md` 一对一）

- GATE-RUN runId=eb3d9f48-ba8b-43be-8ac4-af1ed907eaf3 cmd=node packages/skill-schedule/scripts/seed.mjs --check
- GATE-RUN runId=c7eea705-68c7-4ce9-ad1a-2bba227d636f cmd=node packages/skill-schedule/scripts/seed.mjs --check
- GATE-RUN runId=9e29d1e7-ac07-4c9c-a80c-34fa121622aa cmd=node packages/skill-schedule/scripts/seed.mjs --selftest
- GATE-RUN runId=9d207922-b91b-4c7c-8ecf-e23a096bfe25 cmd=node tooling/check-real-home-untouched.mjs --run --baseline .scratch/t844/real-home-before.snapshot --after .scratch/t844/real-home-after.snapshot -- node packages/skill-schedule/scripts/seed.mjs --check
- GATE-RUN runId=550f42e5-8d2c-4732-b325-590816be1a46 cmd=node packages/skill-schedule/dist/cli/cmd_read.js schedule.record.today
- GATE-RUN runId=f7560d34-34d1-4dd4-9109-ffe3f58166eb cmd=node packages/skill-schedule/dist/cli/cmd_read.js schedule.plan.today

（六条都经 `tooling/run-locked.mjs --ticket 844` 持锁跑；后两条是手工冒烟，非门禁——列在这里是为了让它们也在对账源里查得到。）

### 变异自证（红一行／还原一致一行）

刻意红的一次运行（**不声明为 GATE-RUN**，对账只认 exit=0 的条目）：`runId=a72f4991-b414-4854-b45d-723a8d9e208d`，`cmd=node packages/skill-schedule/scripts/seed.mjs --check`，exit=1：

```text
RED 每日条数 :: want 每日 17 条，got 异样日=[{"date":"2026-07-23","n":16},{"date":"2026-07-24","n":16},{"date":"2026-07-27","n":16}]
RED 24h 首尾相接 :: want 每天 00:00~23:59 无洞无叠，got 断点=["2026-09-21 末条 23:00"]
RED 详情 9 字段 :: want 锚点日 17 条三字段齐，got 来源=16 归因=16
RESULT: 记录 995／计划 73／摘要 112／一级分类覆盖 8/8
FAIL 3/17 条核验变红（暂存库已删，落点里仍是上一份）：每日条数、24h 首尾相接、详情 9 字段
```

改了哪一处：把工作日日型里 `['写作', '创作.文字']` 这一槽删掉（一处改动 ⇒ 该日型 16 条、当日末条停在 23:00）。红时**落点没被改**：落库记录数仍 1037、`seed-meta.json` 的 `contentSha` 仍 `629dcaa4acb127fb`（协议 §2.5 第 1 条：断言是前置条件，全绿才落盘）。

还原一致：改前 `seed.mjs` sha256=`220a981ca1061d02ecafa901305e675250dfe2653d094cb6d0a9e4b401727dc2`，还原后 sha256 与改前**逐字相同**（同一命令算出）。

还原后复跑变绿：`runId=c7eea705-68c7-4ce9-ad1a-2bba227d636f`，exit=0，`RESULT: 记录 1037／计划 73／摘要 112／一级分类覆盖 8/8`，`PASS 17/17 条核验通过`。

### 探针自证（真家目录判据会红会绿）

`runId=9e29d1e7-ac07-4c9c-a80c-34fa121622aa`（`--selftest`，一次性沙盒里造一棵假家目录，不碰真家目录）：

```text
SELFTEST PASS 情形=零写 期望变红=false 实测变红=false 差异 新增/删除/改动=0/0/0
SELFTEST PASS 情形=新增件 期望变红=true 实测变红=true 差异 新增/删除/改动=1/0/0
SELFTEST PASS 情形=改内容 期望变红=true 实测变红=true 差异 新增/删除/改动=0/0/1
RESULT: PASS 探针自证 3/3（零写⇒绿／新增件⇒红／改内容⇒红）
```

### dist 指纹绑定（协议 §2.6）

本件读数以编译产物为前提，故把「依赖的 7 份 src ＋ 7 份 dist」的逐件内容 sha256 合成为一个指纹，跑前跑后各算一次并断言相等：

```text
OK  dist 指纹未漂移 :: before=71384dc8e49880b6 after=71384dc8e49880b6
```

### 手工冒烟（非门禁）：真 CLI 吃这份种子出页

| 唤醒词 | 命令键 | 产物 | 字节 |
| --- | --- | --- | --- |
| 今天总结 | `schedule.record.today` | `.scratch/t844/home/.ilife/data/schedule_html/作息管家_今日作息_20260921_125344.html` | 4460 |
| 查日程 | `schedule.plan.today` | `.scratch/t844/home/.ilife/data/schedule_html/作息管家_查日程_20260921_125439.html` | 1868 |

两条都 exit 0，回执里是真数据（今天 17 条记录／当日 7 条计划，第一条计划带 `completion=已完成`、`synced=true`）。

## 未做项与下一手

- **未推远端**：按仓内口径（`docs/subagent-concurrency-protocol.md` §3「推送由编排者统一收口」），本票只落本地提交，未 `git push`。
- **超线报警**：`packages/skill-schedule/scripts/seed.mjs` **462 行，已超线，需要根据规则进行重构**。为什么超：票面写面只许 `packages/skill-schedule/scripts/seed.mjs` 一个源码路径（`spec-live.json` 的 `paths` 逐字如此），所以规格表（时段／三种日型／计划模板）＋核验电池＋真家目录探针全挤在一个文件里。拆法（下次动它时按此拆，不本次私自扩写面）：`scripts/lib/seed-spec.mjs`（规格表）、`scripts/lib/seed-checks.mjs`（核验电池）、`scripts/lib/seed-realhome.mjs`（探针与自证），主件留 CLI 与落库两段——兄弟件 `skill-home` 的 `seed-scenes.mjs`（347 行）＋ `lib/seed-scenes-{data,checks}.mjs` 就是这个形状。
- **空库场景**：初始化回执与首次使用向导要的是「库还不存在」，本件不预置空库（见上文逐域落点）。
- **覆盖面补齐**：某域票若认为种子不够，按票面的遗留出口在该票票面写清要补什么，不自行改本件。
