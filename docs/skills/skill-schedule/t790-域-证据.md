# #790 证据件 · 辅助与管理（初始化数据库回执 ＋ 首次使用向导）出页

> 票：[【辅助与管理】初始化数据库回执 ＋ 首次使用向导 ＋ 飞书探测页](https://github.com/FeatherHunter/ilife/issues/790)（图 [#779](https://github.com/FeatherHunter/ilife/issues/779) 的子票）
> 交付单元＝**场景行**：本域 3 行逐行有下文（出页 2 ＋ 归 #788 的 1），判据按清单逐行驱动。
> 本件是**机器读数的落点**：票面三条验收命令的原样读数、四条断言的逐条对账、变异自证两行、口径自裁与遗留出口。

## 一、交付物

| 件 | 是什么 |
|---|---|
| `packages/skill-schedule/src/admin/adminDocs.ts`（236 行） | 两张页的**页数据与页装配**（口径层）：初始化回执（新建还是沿用已有 ＋ 三张表行数 ＋ 库路径复制位 ＋ 下一步）、首次使用向导（老侧 6 步 ＋ 飞书强引导 ＋ 初始化报告 ＋ 完成） |
| `packages/skill-schedule/src/admin/adminParts.ts`（115 行） | 本域的**族级页内自造件**（自造件与样式常量的唯一住处）：`renderSteps`（序号 ＋ 状态徽章 ＋ 说明）／`renderBadge`（通过／待办／动手）／`renderVerifyList`（可勾选清单）／`adminPartsCss` |
| `packages/skill-schedule/src/admin/handlers.ts`（253 行） | `lookupHelp` 接 preset 两支（`view=init`／`view=firstUse`，分支内自开库，幂等）；HELP 文件与现找两支逐字不动 |
| `packages/skill-schedule/src/admin/routes.ts`（24 行） | 增两条路由（初始化数据库复用 order 10 原槽位、首次使用 order 50，preset 寄宿自家键） |
| `packages/skill-schedule/test/t790-辅助与管理.test.mjs`（8 例） | 页契约：整页／必现块／两档结论互斥／飞书位两面互斥／幂等两档／prompt 单源／路由 preset／HELP 回归 |
| `packages/skill-schedule/test/t780-declarations.test.mjs`（改两行） | 冻结锚 50→51 ＋ 末条锚（【越界报备】，照 #785／#786 同款报备） |
| `.scratch/t790/成品/` | 4 份真出口产物 ＋ 双端截图 8 张 ＋ `t790-清单.json`（墙与收口票共用一份） |
| `.scratch/t790/墙/` | 本域小墙两张（手机 4 格／桌面 4 格，iframe `srcdoc` 自带内容，零外部文件） |
| `docs/skills/skill-schedule/t790-探针.mjs` | 票面验收命令（四道门 ＋ 逐行交代 ＋ 清单 ＋ 截图 ＋ 小墙） |
| `docs/skills/skill-schedule/t790-门禁运行记录.md` | 上面那些 `GATE-RUN` 声明的对账源导出（工具 `tooling/check-gate-audit.mjs` 生成） |

**零改动**：`src/shared/`（共用位）、公共层、policy、render、cli、`src/plan/**`、`src/write/**`、`src/analyze/**`、别域页组件、`src/cli/keys.ts`／`registry.ts`（本票不新造 key，与基线逐字同）。`SKILL.md` 只动 frontmatter 触发词（补「首次使用」）与构建期注入的速查块。

**越界报备三处**（判据都是机器门，照 #785 同款在提交信息里报备）：① `src/query/routes.ts` 删「初始化数据库」一行（order 原槽位由 admin 原样接过去，其余行与页组件一行不动）；② `src/help/index.ts` 门加宽一行转出 `HELP_ASSETS`（向导复制 prompt 与 HELP 单源）；③ `test/t780-declarations.test.mjs` 冻结锚 50→51 ＋ 末条锚改「首次使用」。

## 二、逐场景行裁定（本域 3 行）

清单 `docs/skills/skill-schedule/场景清单.json` 的 `domain=admin` 3 行，逐行交代如下（探针每次跑都逐行断言这 3 行）。

| 场景行 | 老侧家族 | 裁定 | 产物 |
|---|---|---|---|
| `init_default` | （无老家族：建表无产物） | 出页（三档：新建／已就绪／有数据） | `初始化回执（新建）.html` ＋ `初始化回执（已就绪）.html` ＋ `初始化回执（有数据）.html` |
| `first_use` | —外·setup/首次使用（不计 18） | 出页（老侧 6 步 ＋ 飞书强引导 ＋ 初始化报告） | `首次使用向导.html` |
| `feishu_probe` | （状态 JSON 无产物） | **归 #788** | 页随路由住计划域（`schedule.plan.write` 的 `{op:'sync', dryRun:true}`），三档探测页由 #788 出（票面评论两选一之选项一，照 #787 对 #788 九行的记法） |

对账：3 ＝ 2（本票出页） ＋ 1（归 #788）；本域 3 行之外的行本票一行没碰（探针每跑一次都断言这条不相交）。

## 三、机器读数（探针，票面验收命令）

```
node tooling/run-locked.mjs --ticket 790 -- node docs/skills/skill-schedule/t790-探针.mjs
RESULT: PASS pages=4 scenes=3 red=0 | src-n=91 src-sha-前=58faf0be1469d8d6 src-sha-后=58faf0be1469d8d6 dist-n=369 dist-sha-前=ddab614705ccb0b7 dist-sha-后=ddab614705ccb0b7 selftest-dist-变动件=0 tsc-exit=0
GATE-RUN runId=67bb169d-a6a0-4353-95a1-c3d21df3d73e cmd=node docs/skills/skill-schedule/t790-探针.mjs
```

```
node packages/skill-calorie/scripts/audit-separators.mjs .scratch/t790/成品/初始化回执（新建）.html …（四件逐件点名）
FILE 初始化回执（新建）.html  node·0 ；0 ≥3段0 ｜0 、0 ~0 标识符0  命中=0  (行级命中=0)
（其余三件同）
RESULT: 4/4
PASS
```

```
node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t790/成品 --widths 390,768,1440 --label t790-收口
  初始化回执（新建）.html      390档: 0 ✓  768档: 0 ✓  1440档: 0 ✓  img=0(0/0 放得下) clip=0
  初始化回执（已就绪）.html     390档: 0 ✓  768档: 0 ✓  1440档: 0 ✓  img=0(0/0 放得下) clip=0
  初始化回执（有数据）.html     390档: 0 ✓  768档: 0 ✓  1440档: 0 ✓  img=0(0/0 放得下) clip=0
  首次使用向导.html            390档: 0 ✓  768档: 0 ✓  1440档: 0 ✓  img=0(0/0 放得下) clip=0
OVERFLOW-ZERO pages=4 cells=12 failed=0 scopeOutFailed=0 label=t790-收口
```

另两条一并在探针里跑（出页门是地图级的那一条命令，量的是同一件事的三档溢出）：

```
node docs/skills/skill-schedule/map-779-出页门.mjs .scratch/t790/成品
  ✓ 四件三档 390/768/1440 溢出全 0
RESULT: PASS pages=4 red=0
```

四条断言逐条：

1. **逐场景行有交代**：本域 3 行逐行有下文（出页 2 行 → 4 份产物，其中初始化两档是同行另一档；归 #788 的 1 行）＋ 不相交断言（3 ＝ 2 ＋ 1）＋ 边界断言（归 #788 那一行的唤醒词就是「飞书探测」）。
2. **必现块在页上**：4 份产物逐块断言 —— 初始化回执（新建还是沿用已有的结论互斥 ＋ 三张表 ＋ 库路径复制位 ＋ 下一步）、向导（老侧 6 步段名 ＋ 路径确认四枚复制按钮 ＋ 初始化报告 ＋ 飞书强引导 ＋ 完成与复制位）。另量三条反面判据：可见文本零并列分隔符（`· ； ～ ~ 、 ｜`）、零内部标识（命令键／库列名／参数名／票号）、向导飞书位两面互斥（不可用与全通只许出现其中一面，本机这一趟是不可用那一面）、复制 prompt 与 HELP 单源（标记面断言）。
3. **代码层窄判据**：`adminDocs.ts`／`handlers.ts` 零裸色值、零样式字面量；族级件 `adminParts.ts` 里颜色只取冻结 token、长度只取具名常量（串里零 `px` 字面量）。生成物三件：`keys.ts`／`registry.ts` 与基线逐字同（本票不新造 key，基线与 #789 同枚），`routes.generated.ts` 新基线 `784062f712af8df8`（搬路由的两行由生成器写）＋ `gen-cli.mjs --check` 绿。SKILL 触发词：frontmatter 含「首次使用」＋ 速查表有它那一行（构建期注入，随路由来）。
4. **双端**：出页门 `RESULT: PASS pages=4 red=0` ＋ 分隔符门 `RESULT: 4/4` ＋ `measure-responsive` `OVERFLOW-ZERO pages=4 cells=12 failed=0`（另 `clip=0`）。

**变异自证两行**（同一支探针，`--allow-dirty --no-baseline --no-shots`，读的确实是两棵不同的源树）：

```
变异一（`adminDocs.ts` 把新建档结论改短，`本次新建了三张表` 一句没了）：
  runId=0cb07a9d-76c7-4db3-9bc0-b1597c2dd991
  RESULT: FAIL pages=4 scenes=3 red=1 | src-sha-后=085b5e6bf6c139bb dist-sha-后=b595ac505c9b13c0
  红条：② 初始化回执（新建）.html 必现块：初始化结论（新建档）（缺 本次新建了三张表）
变异二（`adminDocs.ts` 把飞书强引导那句改短，`配合飞书效果最好` 一句没了）：
  runId=9b252bdc-5273-4f2a-a895-9da74fc1d63f
  RESULT: FAIL pages=4 scenes=3 red=1 | src-sha-后=8c31662885c1c447 dist-sha-后=c8c4f2da03721c23
  红条：② 首次使用向导.html 必现块：飞书强引导（缺 配合飞书效果最好）
还原（两处各改回一行、重编后重跑收口那一趟，读数见第三节首行；runId 与收口同枚，不另立声明）：绿，`src-sha-后` 回到同一枚。
```

两行怎么读：**收口那一趟**是绿的（`src-sha-后=58faf0be1469d8d6`）⇒ 两个变异各自把该红的块咬红（`red=1`，且红条点的正是被改坏的那一处）⇒ **还原之后** `src-sha-后` 回到**同一枚** `58faf0be1469d8d6` 且读绿 ⇒ 探针不是空跑的。

**其它读数**：

- 本包全量用例：`node --test --test-concurrency=1 packages/skill-schedule/test/*.test.mjs` —— **213 例全绿**（含本票新增 8 例）。
  `GATE-RUN runId=ef5d1911-8403-4fca-b17f-4541d153beb2 cmd=node --test --test-concurrency=1 packages/skill-schedule/test/*.test.mjs`
- 编译（本轮指纹都是这么来的）：`tsc -b packages/skill-schedule --force`（`FINGERPRINT:` 行见上面收口那一趟）。
- 载荷：`init` 档 `data={mode, created, items[3], total:3}`、`firstUse` 档 `data={mode, created, items[6], total:6, pending}`（探针每次跑都断言 `created` 两档互斥与 `total`）。
- 有数据档真读数：种子库上跑出「作息记录 1037 条」（与 #844 隔离库的 1037 对得上）。
- 快照门：`node tooling/skill-html-snapshot.mjs --check` —— **本包 0 件变化**（红条全属 memo 别包在途迁移，与 #789 同一条）。
- 别域产物逐字节不变（探针每跑一次都量，七批）：#783 那 7 件／#784 那 5 件／#785 那 5 件／#786 那 9 件／#787 那 11 件／#788 那 13 件／#789 那 7 件，逐件与各自清单同 sha。
- 视觉：4 份产物 × 双端截图 8 张 ＋ 小墙两张（手机 4 格／桌面 4 格）落在 `.scratch/t790/`；收口票 #792 的墙直接读这份清单。
- 提交：`ad90bcd7`（实现）／`d39a1a9f`（路径走复制载荷修）／`c47206d5`（变异还原＋指纹冻结）。

## 四、本票自己定的口径（都落在一处，别处引用）

1. **键不新造，preset 寄宿自家键**（`admin/routes.ts`）：「初始化数据库」与「首次使用」都进 `schedule.help.lookup`（`view=init`／`view=firstUse`），照 #785「周视图」落 `schedule.record.range` ＋ preset 的先例。新 key 会牵动 `policy` 的 `ScheduleKey` 联合与快照门 keys 件，两处都不在本票写面。
2. **初始化数据库搬家复用原槽位**（`admin/routes.ts` order 10）：query 那边只删一行，其余行与页组件一行不动 —— 比全局顺移少碰三个能力目录的声明（第一版全局顺移撞上 plan 的 order，被生成器门当场拦下，改回槽位复用）。
3. **初始化回执与向导不走 `landing`**（与 #783–#789 同链）：处理函数交真页，出口走缺省落盘（产物根 ＋ 命令标题主体）。每次 fresh 落盘（无复用窗口），故新建／已就绪两档可分两次真跑断言。
4. **路径走复制载荷，不印可见路径**（`adminDocs.ts`）：分隔符门 R7 把真实落点名（`schedule_data.db`／`schedule_html`，配置默认值）判成库表名与蛇形名 —— 可见路径必红。门头自己明示「属性天然不进可见文本」，故四处落点各一枚复制按钮（标题可见，完整路径进 `data-t`，点一下就复制），可见行只留确认语。这是门读数抓出来的，不是目视猜的（第一趟探针 4/4 红在 R7 上，改完 4/4 绿）。
5. **向导飞书位只说三道门的 raw 结果，不复述三档 verdict**（`handlers.ts`）：全通／不完全／没装是 #788 的词（`plan/probe.ts` 的 `TIER_CN`），本域只报装了没／登了没／日历通不通＋强引导句「配合飞书效果最好」＋指针「说「飞书探测」看三档」。探针断言 verdict 词一颗不上屏（用例 V4 同锁）。
6. **复制 prompt 与 HELP 单源**（`help/index.ts` 门加宽一行转出 `HELP_ASSETS`）：向导复制块取 `first_use` 的 `prompt_template`，探针每次跑都在标记面断言相等。
7. **`data` 只装机器读数**：`list` 形（`items`／`total` 必备，`created`／`pending` 是本支的读数位）。页是给人读的那一份，载荷是机器读的那一份，两份在探针里各断各的。

## 五、必报五步（`docs/agents/structure.md`）

**第一步 · 影响清单（开工前报的）**：只碰一个能力目录 —— `packages/skill-schedule/src/admin/`（新增两件：`adminDocs.ts`／`adminParts.ts`；改两件：`handlers.ts` 的 preset 两支、`routes.ts` 的两条路由），另有测试 `packages/skill-schedule/test/t790-*.test.mjs`（新增）与探针证据件落 `docs/skills/skill-schedule/`。**越界三处**（见第一节末）：`src/query/routes.ts` 删一行、`src/help/index.ts` 门加宽一行、`test/t780-declarations.test.mjs` 冻结锚两行 —— 判据全是机器门，提交信息里逐条报备。

**第二步 · 结构设计**：`adminDocs.ts`＝两张页的页数据与装配（对外：`renderInitReceiptPage`／`renderFirstUsePage` ＋ 输入类型 `InitReceiptInput`／`FirstUseInput`（`AdminPaths`／`AdminCounts`／`FirstUseStep`／`FirstUseTodo`／`FirstUseFeishu` 五个辅类型），出口 2 函数）；`adminParts.ts`＝本域族级件（对外：`adminPartsCss`／`renderSectionTitle`／`renderBadge`／`renderSteps`／`renderVerifyList`，五个出口）；`handlers.ts`＝三支分派（只引同目录件与 `fetch`／`help`／`render` 的公开面）。共用件（`src/shared/docPage.ts` 的 `extraCss` 接缝）被本域按公开接口复用，未改一行。

**第三步 · 写代码**：常量与类型只写一处（步骤状态三态、`HELP_ASSETS` 的 prompt、色值＝冻结 token、长度＝`adminParts` 里那组具名常量）。跨目录引用一律走公开接口（`base-paint/blocks`、`base-paint`、`../fetch/index.js`、`../help/index.js`、`../render/index.js`、`../shared/docPage.js`），没有深路径直引（`help/scenes` 只经能力门）。

**第四步 · 超线报警**：本包告警线数字待维护者定（`packages/skill-schedule/AGENTS.md` 明写未定数）；本票三件实测 `adminDocs.ts` 236、`adminParts.ts` 115、`handlers.ts` 253（LF 口径），均未越前票参照线（400）——**未超线，不触发重构报警**。

**第五步 · 交付对账**：与第一步逐行对得上 —— 实碰 `src/admin/` 四件（新增两件、改两件）＋ 越界三处 ＋ `test/t790-辅助与管理.test.mjs`（新增 8 例）＋ `docs/skills/skill-schedule/t790-探针.mjs`（新增）＋ 本证据件 ＋ `t790-门禁运行记录.md`（新增）；`src/shared/`、公共层、policy、render、cli、生成物 `keys.ts`／`registry.ts`、别域页组件 **零改动**（读数见第三节）。偏差：越界三处（已报备）之外为零。

## 六、遗留出口

- **初始化回执的文件名主体**：两支与 HELP 文件共用 `作息管家_作息管家帮助` 主体（同一枚 key 同一个标题，照 #788 十三页共用 `作息管家_写计划` 的先例），靠时间戳与 `_N` 递补区分；探针复制到可读名后收口票读清单，不读落盘名。
- **用户家目录配了带下划线的自定义落点时**：页上可见行已无路径（路径全在复制载荷里），生产环境不触发 R7；探针家目录固定中性名，读数可复现。
- **「首次使用」唤醒词的老侧别名**（初始化／新手）：老侧 SKILL 写「唤醒词：首次使用 ／ 初始化 ／ 新手」——「初始化」是 HELP 全文现找可达的词，不单独立路由；「新手」未立路由（清单 85 行里没有这一行， indifferent 保持现状）。
- **与历史习惯的贴合率、心愿清单等跨技能支线**：仍留在地图的 Not yet specified。
