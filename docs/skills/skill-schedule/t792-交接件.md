# #792 收口进行时：交接件（会话可能断开时先读这一件）

> 归 [#792【收口】双端墙 ＋ 总索引 ＋ 逐格缺陷清单 → 修完复评 ≥90 → 人签字](https://github.com/FeatherHunter/ilife/issues/792)（图 [#779](https://github.com/FeatherHunter/ilife/issues/779)）。
> 本件只写「**现在到哪了、还剩什么、怎么跑**」；读数与论证在 `t792-验收墙-证据.md`，人的结论在 `t792-签字记录.md`。
> 交接时间：2026-09-22 14:30（现场：`#891` 实施席在飞，12 个 src 件已改、未提交）。

## 一、已经交付并验证的（不用重做）

| 项 | 状态 | 证据 |
|---|---|---|
| 三支生成器（清单／墙／索引） | ✅ 入仓 | `t792-清单.mjs`／`t792-墙.mjs`／`t792-索引.mjs` |
| 两张墙（各 61 格，390／1280）＋ 单文件自包含版 | ✅ 出齐 | `.scratch/t792/t792-手机墙.html`／`t792-桌面墙.html`／`t792-手机墙-单文件.html`／`t792-桌面墙-单文件.html` |
| 总索引（61 件 × 8 族 ＋「有意不出」7 条） | ✅ 出齐 | `.scratch/t792/t792-总索引.html` |
| 正反自检 | ✅ 走通 | 正例 `PASS … dropped=0 red=0`；反例 `FAIL … dropped=1 red=1`（exit 1 点名） |
| 出页门 ＋ 三档溢出（墙／索引豁免） | ✅ 绿 | `pages=64 red=0`；`--exclude "手机墙,桌面墙,总索引"` 后 `failed=0`，exit 0 |
| §8 收口自检清单（机器侧） | ✅ 逐条绿 | 见证据件「五之二」 |
| 四件机器读数链 ＋ 判分引擎 | ✅ 跑通（自证差 0） | `t792-读数.mjs`（调度）／`t792-facts.mjs`（本域装配器）；`SCORE 均分 89.02 过线 47/61`（口径修正后） |
| 人核三列（`d1`／`d2`／`english`） | ✅ 判语落定 | 证据件「六之二」：`d1`＝15／`d2`＝20 全 61 页；`english` 除「`HELP` 待裁」外不扣 |
| 手机墙 390（61 格）用户签字 | ✅ 已签（第 1 次） | `t792-签字记录.md`（含「除时间戳外 61/61 相同 ⇒ 本签仍有效」的核验） |

## 二、缺陷四票（这是剩下活的主体）

| 票 | 治什么 | 命中面 | 状态 |
|---|---|---|---|
| [#891](https://github.com/FeatherHunter/ilife/issues/891) | 页内目录 ＋ 定位避让 | 61/61 页（D5 −4/页） | **在飞**：新件 `src/shared/pageNav.ts`（`SECTION_TOC_MIN=3`）＋ 12 个页装配件已改，未提交；实施席报「无硬阻塞，1.5–2.5 h 到 61 件重出 ＋ 七支探针 exit 0」 |
| [#892](https://github.com/FeatherHunter/ilife/issues/892) | 同一句事实在「汇总段 ↔ 逐段表」印两遍（`制定次日计划结果` 两版各 13 处 ⇒ 页分 45）；向导页复选框命中区 16×16（H6=6）；`gap:8pxpx` 拼写 bug | 2 页 ＋ 1 页 | 范围已据实收窄（票面评论），待 #891 落地后派 |
| [#893](https://github.com/FeatherHunter/ilife/issues/893) | 390 档字号 11px（D5 字腿 −5 ⇒ D5=16 < 下限 20） | 11 页（10 页在本包，1 页在公共层） | 修复面已钉到行（票面评论），待派 |
| [#894](https://github.com/FeatherHunter/ilife/issues/894) | 完成状态印成半角括号 `已完成(超时)` | 10 页 | 修复面已钉到行（票面评论），待派 |

**排期**：本包是**单写者资源** ⇒ 四票必须**串行**（一张落地并重出产物，才派下一张）。每票落地后都要：
① `tsc -b packages/skill-schedule --force`；② 按域重跑七支探针（**各支用它自己的票号进锁**，这是探针 `checkQuietWindow()` 的硬要求）；
③ 七支的编译态锚（`FROZEN_FINGERPRINT`）推到当刻 `src`／`dist` 两枚 sha（照 #888 的做法；`t787` 的锚原本是空串，别补新判据）。

## 三、修完之后怎么收（一条一条照着跑）

```sh
# 1) 重出 61 件产物镜像与清单（墙与索引的唯一读出）
node docs/skills/skill-schedule/t792-清单.mjs

# 2) 重出四件读数（三件 reader ＋ 本域 facts 装配器）
node docs/skills/skill-schedule/t792-读数.mjs

# 3) 引擎复算：目标「逐页 ≥90 且每维 ≥ 满权 80%」
node packages/base-render/scripts/判分.mjs --dir .scratch/t792/读数 --json .scratch/t792/读数/判分结果.json

# 4) 重出两张墙（含单文件版）与索引
node docs/skills/skill-schedule/t792-墙.mjs
node docs/skills/skill-schedule/t792-墙.mjs .scratch/t792 t792-桌面墙-单文件.html --only 桌面墙 --embed
node docs/skills/skill-schedule/t792-墙.mjs .scratch/t792 t792-手机墙-单文件.html --only 手机墙 --embed
node docs/skills/skill-schedule/t792-索引.mjs

# 5) 过门：出页门（墙＋索引＋61 件）＋ 三档（墙／索引标范围外）
node docs/skills/skill-schedule/map-779-出页门.mjs .scratch/t792 --widths 390,1440
node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t792 --widths 390,768,1440 --exclude "手机墙,桌面墙,总索引"
```

6) **把新墙交用户滚**（桌面 1280 优先）：给了逐格结论就登记进 `t792-签字记录.md`（第 2 次），
   然后关 #792；再按图规则把 #779 推到 100% 并 close（Decisions 追加最后一行）。

## 四、待人裁的两件（不是阻塞，但会影响分数／口径）

1. **`HELP`**（H7，向导页 3 处）：它是唤醒词「作息管家 HELP」的字面量。若按「唤醒词＝命令字面量」放过（同 `AI` 的理由）⇒ 不扣分；否则该页 −3。
2. **公共层图例字号**：`区间汇总（21 天）` 的 `span.ilife-charts-legend-item` 11.5px 住在
   `packages/base-render/src/charts.ts:2130`（跨域共用位）。本包改不动 ⇒ **该页修完 #891／#893 后仍会扣 5 分**（D5=20 恰好贴线，页分 ≈91）。
   要治它得另开一张**公共层**的票（会同时影响卡路里／备忘录／居家）。

## 五、读数的两处口径坑（已修，写在这里免得后人重踩）

1. **`dupFacts` 不能把短量值当重复**：`23h59m`／`07:30 至 08:00`／`2026-09-16` 这类同一个量值在「总览条 → KPI 卡 → 逐行列表 → 表格」各印一次是**正当的多口径展示**。原口径把它们算成重复 ⇒ 25 页误判；改成「≥14 字且含句读的事实句」后 ⇒ **2 页**（真债）。
2. **`dupFacts` 不能把导航算成重复**：目录项与 `<h2>` 同字是**设计的本意**（导航要点名它指向的那一段）。`t792-facts.mjs` 的可见文本抽取**先把 `ilife-block-toc-block`／`role="navigation"` 整段剥掉**再算（#891 开工前实测：不剥会让 61 件凭空 +50 处）。
