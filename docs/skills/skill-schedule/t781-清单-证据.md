# t781 清单证据（机器读数）

验收：`node docs/skills/skill-schedule/t781-清单.mjs --check`

- GREEN: 85 行=id 集一致；families=18；别名新 48＋老 34 全覆盖；零设计列（白＋黑）；无 BOM；MD 派生一致

变异（改坏一处必须红）：改标题 exit=1 RED-OK；删一行 exit=1 RED-OK；加设计列 exit=1 RED-OK；改家族名 exit=1 RED-OK（后两项是补上去的：首轮改家族名未红，暴露引用完整性缺口，已补 `old_family｜name` 与 families 的双向一致断言；分隔符 `·` 与家族名撞车，已换 `｜`）。

还原：exit=0 GREEN-OK（JSON 还原逐字节，MD 与重算一致，数据行 85）。

`GATE-RUN runId=12e0f8ca-b1c8-4cd7-939c-64372d4a6d8f`

## 决议（过程细节，本票内定；每条均过对抗式审查是否 helps MAP 目标）

- D1 单 JSON 三段（scenarios/families/aliases），MD 纯派生：防双源漂移，判据单源头。
- D2 `old_family` 可空＋理由枚举（理由引 result 原文）：防给无产物场景硬安家族（自造事实）；空行即各域票“有意不出”候选。
- D3 别名双向覆盖（新 48＋老 34，53 行）：`查作息游标`=老 SKILL 表达→`schedule.record.range`；5 老独有词（准备消息/同步作息/增量同步/周视图/首次使用）`new=null`。
- D4 60/25 只做注释不做门禁（R1：25 vs 枚举 32 对不上，源无类型位）。
- D5 行断言白名单（6 键＋条件 `family_null_reason`）＋黑名单（新侧 key/出页/产物名）双保险。
- D6 读域：新仓三件（wakewords/gen-help-assets/help-assets）只读不写——“不碰 packages/**”指写；不读，别名无从验证。

## 待确认（95% 前须裁，未确认不 close）

R1 25/32、R2 模板 19+3 vs 20+3、R3 路由 46 vs 48（本票冻结现行 48）、R4 `review_today_all_done` 是否产页（本票记无产物）。
