# t659 · 作息侧不变量清单（以老代码为准）

- 出件票：#659（横切票，`Part of` 地图 #658；规格 #657）
- 判据与接缝总件：`docs/agents/合成写判据.md`
- 逐条出处（静态读码，含逐字片段）：`docs/skills/skill-schedule/t659-取证-老实现.md`
- 机器读数（老 ↔ 新对拍，三个边界）：`docs/skills/skill-schedule/t659-对拍读数.json`
- 口径：**以代码为准**，不引文档（老技能文档实测与代码不符之处见第四节）。
- 老实现位置：`D:\2Study\StudyNotes\SKILLS\作息管家\`（仓外，只读对照）。

## 一 · 不变量清单

「判据格」六项＝幂等键 · 查重字段 · 失败行为 · 远端标识回写 · 归属标记 · 远端不可用时降级与否。

| 编号 | 不变量 | 判据格 | 老代码出处 | 机器读数 |
|---|---|---|---|---|
| S-01 | 合成写的单位：一条命令同时对齐「本地库 ＋ 飞书日历」两侧，缺哪边建哪边 | 本地键 `(date,time_start,time_end)`；远端键 `(date,time_start,time_end,title)` | `schedule_db.py:1040`、`:1056`、`:1121-1132` | S-A（老：本地落 ＋ 远端建 ＋ 标识回写，9 次调用；新：只落本地，0 次远端调用） |
| S-02 | 本地查重按**三元组**，title 只展示不参与身份 | 查重字段 `date` ＋ `time_start` ＋ `time_end`，且 `is_active = 1` | `schedule_db.py:976`、`:1008`、`:1056` | S-B（重复调用：本地仍 1 行） |
| S-03 | 远端查重按**四元组**（先按 title 取候选，再逐条比起止时刻） | 远端查重字段 `summary` 全等 ＋ 起止时刻相等 | `schedule_db.py:1121-1132` | S-E（老：认出来并回写 `fs_seed_1`；新：0 次远端调用） |
| S-04 | 回执分字段：`action`／`id`／`event`／`feishu`，`feishu ∈ {found_feishu, created_feishu, unavailable}` | 失败行为：`_sync_one_feishu` 返回 `None` → 回执写 `"unavailable"` | `schedule_db.py:1062`、`:1066`、`:1083` | S-A 老回执 `feishu=created_feishu`；S-B 老回执**缺 `feishu` 键**（见 D-14）；新回执只有 `{ok,message}`（缺口） |
| S-05 | 远端标识回写：`feishu_event_id`（同时刷 `last_synced_at`） | 回写函数 `set_feishu_event_id` | `schedule_db.py:1273`、`:1131`、`:1140` | S-A／S-C／S-E 老本地行标识非空；S-A 新本地行标识为 `null`（缺口） |
| S-06 | 归属标记：写进远端事件描述，前缀逐字 `作息管家自动同步`（有备注时 `作息管家自动同步 · {notes}`） | 反查判据＝描述里含该标记 | `schedule_db.py:1137`；另一套拼装见 `feishu_sync.py:1018`（后缀式） | S-A 老建对象描述＝`作息管家自动同步 · 周会`；新不建对象（缺口） |
| S-07 | 远端不可用时**降级**：本地照写，回执标明远端没成，退出码仍 0 | 降级口径：真降级档＝远端可探、写失败 → `feishu: "unavailable"` | `schedule_db.py:1115-1144` | S-G（老：本地落 ＋ `feishu=unavailable`，退出码 0）；S-J（老：远端建成了但拿不到标识 → 同样标 `feishu=unavailable`，不谎报成功） |
| S-08 | 远端**整平台**不可用时（连身份都探不到）**不是降级**：`ensure` 会挂在分片循环里 | —（老实现缺陷，见 D-13） | `feishu_sync.py:546-550` | S-D（老：25s 超时未返回，同一 6 小时窗口重复查 472 次；新：本地落，退出码 0） |
| S-09 | 重同步路径远端不可用时**阻断**（全仓唯一「远端不可用即阻断」的入口） | 阻断点：`is_feishu_available().fully_available` 为假即 `return` | `schedule_cli.py:1495-1499` | S-H（老：打印「❌ 飞书不可用:partial」后返回，退出码 0；新：退出码 4） |
| S-10 | 分片拉取：三阶段合并（议程 ＋ 分片检索 ＋ 逐条补描述），检索按 **6 小时**切片 | 分片＝`timedelta(hours=6)`；单次 20 条上限只写在注释里，代码无截断 | `feishu_sync.py:522`、`:434`、`:439`、`:447`；上限见 `:414`／`:511`（注释） | S-A 老调用留痕：`+agenda` ×1 ＋ `+search-event` ×4（四段 6 小时）＋ `+create` ×1 |
| S-11 | 反向对账（Phase 0）：按 `(start, end, summary)` 索引回填缺失标识；多条候选跳过不猜 | 唯一命中才回填；拉取失败打印警告后 `return 0`（不阻断） | `schedule_cli.py:1405-1470` | S-F 老：回填 `fs_seed_1` 后不再重复建；新：`op=sync` 丢查询结果 → 重复建 1 条（D-12） |
| S-12 | 归属判据：判「这条远端对象是不是我管的」只看**归属标记** —— 孤儿清理与同槽清理共用这一条 | 孤儿＝有标记但本地无对应（该清）；不带标记的**不许动** | `feishu_sync.py:893`、`:900`、`:904`；同槽清理 `schedule_cli.py:947-979` | S-I（老：带标记的 `fs_orphan_mine` 被清掉、不带标记的 `fs_orphan_other` 原样留着；新：两条都留着——孤儿清理未落，属 #660） |
| S-13 | 两种语义分得开：整段覆盖式写入 vs 缺则补 | `upsert-plan-events`＝覆盖（命中 UPDATE、旧活跃软删）；`ensure-plan-event`＝缺则补（命中即 `found`，不覆盖） | `schedule_db.py:1147`、`:1152-1154`、`:1060` | S-B／S-C 老：命中即返回，不改既有字段 |
| S-14 | `create_event` 没有幂等键（重复执行会重复建远端对象） | 签名无 `idempotency`／`client_token` | `feishu_sync.py:288`、`:303` | S-F 新侧（远端已有同四元组仍建）即这条的后果；D-05 要求补 |
| S-15 | 退出码几乎不带信息：除无参数 `sys.exit(1)` 外，失败一律打印后 `return`（退出码 0） | 失败语义：错误只进回执的 `errors` 列表 | `schedule_cli.py:648`、`:488` | S-H 老：远端不可用仍退出码 0；S-G 老：创建失败仍退出码 0 |
| S-16 | 旧坏入口 `upsert-plan` 必抛错被吞（不搬） | 写 `hour_N_planned` 而新表无此列 → `OperationalError` | `schedule_db.py:668` vs `:801`；`schedule_cli.py:488` | `test/synthetic-write-659.test.mjs` T5（新仓 `op=upsert-plan` 非 0 退出） |

## 二 · 老实现自身的缺陷（照抄会坏，已登记偏离）

| 偏离 | 老行为 | 新行为 | 出处 | 两行机器读数 |
|---|---|---|---|---|
| D-11 | `create_event` 拿不到远端标识时本地仍写空**且回执照样报成功** | 显式报错或降级标记（老实现这一档是 `feishu:"unavailable"`） | `skill-schedule/src/cli/cmd_read.ts:302-303` | **改坏必红**：T7 与 S-J 新侧（建了 1 个、本地标识 `null`、回执 `{ok:true,message:'已同步…新建1'}`）；**还原必绿**：S-J 老侧同布景（本地标识同样 `null`，但回执 `feishu:"unavailable"` —— 不谎报）。新侧修好后同一条读数转「显式标记／非 0 退出」 |
| D-12 | `op=sync` 调了 `searchFeishuEvents` 却丢返回值 | 接上返回值做远端判重 | `skill-schedule/src/cli/cmd_read.ts:293` | **改坏必红**：T6 与 S-F 新侧（查了远端＝true、仍重复建 1 条，远端从 1 个变 2 个）；**还原必绿**：S-F **老侧**同布景（Phase 0 回填 `fs_seed_1` 后不再重复建） |
| D-13 | 远端查询失败时分片循环不推进游标 → 挂死 | 失败即上抛／降级 | 老 `feishu_sync.py:546-550` | **改坏必红**：S-D／S-H 老侧「超时未返回（同一窗口重复 472 次）」；**还原必绿**：新侧同布景退出码 0／4，都不挂 |
| D-14 | 回执 `found` 那支缺 `feishu` 键 | 回执一律分字段 | 老 `schedule_db.py:1062` vs `:1066`／`:1083` | **改坏必红**：S-B 老回执 `{action,id,event}` 无 `feishu` 键；**还原必绿**：S-A／S-C／S-E／S-G 老回执都带该键（同档位其它支） |
| D-07 | 降级判据用恒真的 `is_feishu_available()`（那个短路分支永不执行） | 否决权归 `larkReady()` 四门，失败即 throw；不搬恒真判据 | 老 `schedule_db.py:1118-1119`、`feishu_sync.py:75`、`:222` | **改坏必红**：常驻用例 T5 断言包内零命中 `is_feishu_available`——照抄回来（或在 TS 里立等价恒真判据）即红；**还原必绿**：现状 T5 绿 |
| D-08／D-09 | 两份同名顶层定义（`cmd_help`）／同一口径两份副本（`_iso_to_hhmm`） | 合一份；老名字不随移植回来 | 老 `schedule_cli.py:194`／`:3097`、`:938`；`feishu_sync.py:1032` | **改坏必红**：T5 断言包内零命中这两个老名字；**还原必绿**：现状 T5 绿 |

「不搬的四类」（D-01 坏入口、D-02 旧表与迁移脚本、D-04 死接口、D-10 死代码残块）的机器读数＝常驻用例 T5 的四条缺席断言；改坏（搬回来）即红。

## 三 · 新侧当刻缺口（谁欠谁）

> **#660 已落地（2026-09-17）**：本表作息侧的五行全部收口，读数见 `docs/skills/skill-schedule/t658-A-飞书层移植-证据.md`。

| 缺口 | 属主票 | 读数 |
|---|---|---|
| ~~`op=ensure` 只落在本地一侧（不碰远端），回执不分字段、无归属标记~~ → 已落地：缺省合成写，三格永在，描述带锚 | #660 ✅ | 常驻用例 T3／T4（四条全 pass）；`plan-feishu-660.test.mjs` R1／R3 |
| ~~`op=sync` 丢远端查询结果、拿不到远端标识仍写空~~ → 已落地：阶段 0 回填 ＋ 四元组判重；拿不到标识即标 `unavailable` 且非 0 | #660 ✅ | 常驻用例 T6／T7 |
| ~~飞书层完整深度（分片、远端判重、孤儿清理、同槽清理、反向对账）~~ → 已落地：`src/plan/pull.ts`／`remoteDay.ts`／`sync.ts` | #660 ✅ | `plan-feishu-660.test.mjs` R2（议程×1／分片×4）／R4／R5／R6 |
| ~~飞书自检三处改造（默认不跑／点名留痕／能删干净）~~ → 已落地：`op=check`（日历半场；任务域归 #661） | #660（D-03）✅ | 同上 R9 |
| ~~24h 聚合视图与多日查询~~ → 已落地：`src/plan/overview.ts` ＋ 两个唤醒词 preset | #660 ✅ | `plan-overview-660.test.mjs` V1～V4 |

## 四 · 老技能文档与代码不符（本票复核）

| 文档说 | 代码实际 |
|---|---|
| 白皮书式描述「远端不可用即降级」 | 只有**可探但写失败**那一档降级（S-G）；整平台不可用会挂在分片循环里（S-D，D-13） |
| 回执 `feishu` 字段「取 `found_feishu`／`created_feishu`／`unavailable`」 | 三值都对，但 `found` 且本地已有标识那支**根本没有 `feishu` 键**（S-B，D-14） |
| 「单次返回上限 20 条」 | 只写在注释里（`feishu_sync.py:414`／`:511`），代码里没有任何截断或常量 |

## 五 · 复跑

```
node tooling/run-locked.mjs --ticket 659 -- node packages/skill-schedule/scripts/t659-parity-probe.mjs
node tooling/run-locked.mjs --ticket 659 -- node --test test/synthetic-write-659.test.mjs
```

对拍件要跑老实现（仓外 Python），故 `OLD_SKILLS_ROOT` 可换机；挡板与临时库由接缝件现造，跑完即弃，**不碰真飞书、不碰真库**。
