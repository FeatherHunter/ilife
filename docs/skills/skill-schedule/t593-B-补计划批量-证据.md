# t593-B · 补计划多天批量（#599 余量 a/b）证据件

- 本票：[#599](https://github.com/FeatherHunter/ilife/issues/599)（余量两项 a/b；诊断：单天合成写已落 #660，多天批量＋SKILL 示例未动）。
- 前票证据：`docs/skills/skill-schedule/t658-A-飞书层移植-证据.md`（合成写回执三格／退出码 achieved?0:4／降级口径，本票沿用）。
- 参数形决策（二选一自定）：取票面推荐的 **`dates[]` 数组逐条带日期**一支（起止区间那一支不做）。形为
  `{op:'ensure', dates:[{date,time_start,time_end,title,notes?,category?}, …]}`，
  同 `op` 内分流（有 `dates` 走批量，否则单天）；单复数与只读侧同形（`date` 单天／`dates` 多天），
  元素是对象（写侧每天要完整三元组），与只读 `dates:string[]` 按 key 分流、不共用校验。
- 约束遵守：逐天复用既有 `runEnsure`（不另造写路径）；逐天幂等（三元组口径单天侧保证）；
  失败逐条前缀日期点名且 exit 非 0；回执 `items[]` 逐天分字段；`op=sync` 整天口径零改动；
  全程挡板（`tooling/contract-lark-stub.mjs`），禁真飞书。
- 回执顶层 `remoteId` 恒 `null`（批量无单一远端对象，逐天标识只在 `items[]` 内；`null` 表无单一标识、不写空串冒充，沿 D-11 口径）。
- 矛盾入参 `{date＋dates:[]}`：旧实现忽略 `dates` 走单天，现拒收（exit 2），属收紧、可接受，留痕于此。

## 一 · 影响清单与对账（结构必报五步一／五）

| 文件 | 一句话 | 理由 |
| --- | --- | --- |
| `packages/skill-schedule/src/policy/plan.ts` | 新增 `validateEnsureBatchInput`＋`EnsureBatchItem` | 批量口径只住口径层（概念唯一，复用 `normalizeDate`／`validateEvent`） |
| `packages/skill-schedule/src/policy/index.ts` | 转发上条两件 | 口径对外经此门 |
| `packages/skill-schedule/src/plan/ensure.ts` | 新增 `runEnsureBatch`（按天展开逐天调 `runEnsure`，聚合回执） | 能力自治：写编排住 `plan/`，不另造写路径 |
| `packages/skill-schedule/src/plan/index.ts` | `ensure` 分流一行（有 `dates` 走批量） | 能力门分发；`sync` 等余支一字未动 |
| `packages/skill-schedule/src/policy/wakewords.ts` | `补计划` 加 `needs:[date,time_start,time_end,title]` | 速查示例从 bare `{"op":"ensure"}` 变成必填齐全 |
| `packages/skill-schedule/src/help/lookup.ts` | 示例真值加 `time_start/time_end/title` 三档 | 照抄即跑：不再出 `<值>` |
| `packages/skill-schedule/SKILL.md` | 重生成联动块（:72 单条满参）＋手写「批量补计划（多天，#599）」一节 | 生成块不动手改；批量段住生成块之外 |
| `packages/skill-schedule/test/plan-ensure-batch-599.test.mjs` | 新增 B1～B4 | 本票读数（7 天／幂等／失败点名／照抄即跑） |
| `docs/skills/skill-schedule/t593-B-补计划批量-证据.md` | 本件 | 归属件目录证据 |
| `docs/skills/skill-schedule/t593-B-补计划批量-门禁留痕.log` | 门禁运行记录导出（§2.3.4） | 锁目录原文的受版本控制副本 |

对账：事前清单即上表 10 件（其中 src 6 件：policy 3＋plan 2＋help 1），实际落盘 10 件，偏差零。未碰它票路径
（`#600/#601/#607/#608/#610/#612/#613/#614` 一律未动；`src/plan/sync.ts` 等余件一字未改）。
超线报警：本包告警线数字待维护者定（包内 `AGENTS.md` 明说不先编数），无超线可报。

## 二 · 四门读数

`GATE-RUN runId=<运行标识> cmd=<命令>`（标识抄自运行记录对应行；详见门禁留痕导出件）：

- tsc：`GATE-RUN runId=3d140aef-b507-4909-b59f-0c90ff40d33d cmd=node node_modules/typescript/bin/tsc -b --force packages/skill-schedule` → exit 0。
  （初编 `ced00219…` 亦 exit 0；终态以 `--force` 重建为准，见第三节变异教训。）
- 定向：
  `GATE-RUN runId=f0bcdb83-2c75-45cf-99cc-12284c84eb04 cmd=node --test packages/skill-schedule/test/plan-ensure-batch-599.test.mjs`
  → tests 4／pass 4／fail 0（`B1 本地行=7 天=7 逐天=7`／`B2 本地 7→7 远端重复建=0`／
  `B3 exit=4 本地行=2 点名=["2026-09-21","2026-09-22"]`／`B4 单条 → exit 0；批量 2 天 → exit 0 落 2 行`）；
  `GATE-RUN runId=bcd7a68f-b598-417e-abe7-16fd8b8459fc cmd=node --test plan-feishu-660＋plan-overview-660＋cli＋skill＋policy`
  → tests 44／pass 44／fail 0；
  全包 `GATE-RUN runId=13295cf2-f8fd-46c7-af52-025a8c8a7e88 cmd=node --test packages/skill-schedule/test/*.test.mjs`
  → tests 100／pass 100／fail 0。
- gen:check：`GATE-RUN runId=bc001d50-1fe7-4634-87bb-fa0a706b6278 cmd=pnpm gen:check`
  → exit 0（`GEN-CHECK PASS：键 129（写 47 ＋ 读 82）`；本票无生成物可改）。
- 改坏必红还原必绿：`GATE-RUN runId=b4e43977-ef79-47a7-8206-e3c8e7a0b917 cmd=node .scratch/t599/mutate-599.mjs`
  → exit 0，内含 `MUT-RED exit=1`（B3 红）／`MUT-HASH-EQ true`／`MUT-GREEN exit=0 pass 4`。详见三节。
- SKILL 生成链：`GATE-RUN runId=fad0ca17-0f3b-4062-b860-422878f42ba4 cmd=node packages/skill-schedule/scripts/build-help.mjs` → exit 0。

取代关系（结论不回头改，只写取代）：`d6b1b228…`（首版探针，还原用普通 `tsc -b`，增量残留致还原仍红 exit 1）
与 `96242abf…`（残留态下的 batch 重跑 exit 1，非源码问题）均被取代：
取代者＝`3d140aef…`（`--force` 重建）＋ `f0bcdb83…`（重建后 batch 4/4）＋ `b4e43977…`（修正版探针，红绿干净）。
教训：`tsc -b` 增量在内容回到旧值时跳过重发，还原须 `--force`（与 calorie 侧既有结论一致）。

## 三 · 变异两行（源码级，持锁内直调、释放前复原）

变异 M1：批量失败去掉日期前缀（`errors.push(item.date+'：'+e)` → `errors.push(e)`，点名天失效）：

- 改坏必红：`MUT-RED exit=1 line=✖ #599 B3 远端失败 exit 非0且点名天（本地照写）`
- 还原必绿：`MUT-HASH-EQ true`／`MUT-BUILD-GREEN exit=0`／`MUT-GREEN exit=0 pass 4`
- 自证脚本：`.scratch/t599/mutate-599.mjs`（单外层锁，内直调 `tsc -b --force` 与 `node --test`，不嵌套抢锁）

## 四 · (b) SKILL 示例

- 单条：`SKILL.md:72` 已是满参
  ``schedule-cmd-read schedule.plan.write --params '{"op":"ensure","date":"2026-09-01","time_start":"09:00","time_end":"10:00","title":"晨会"}'``
  （由 `WAKE_TABLE`＋`exampleParams` 真值派生，非手写；`skill.test` 钉死速查块与生成器输出逐字相等）。
- 批量：新增「批量补计划（多天，#599）」一节，2 天照抄命令（7 天同形）：
  ``schedule-cmd-read schedule.plan.write --params '{"op":"ensure","dates":[{"date":"2026-09-21","time_start":"09:00","time_end":"10:00","title":"晨会"},{"date":"2026-09-22","time_start":"09:00","time_end":"10:00","title":"晨会"}]}'``
- 守住：`B4` 同 #660 V5 形态——单条从 `buildHelpLookup()` 原样取参喂挡板出口（exit 0），
  批量从 `SKILL.md` 原文正则原样取参喂挡板出口（exit 0，落行数＝天数）。

## 五 · 不动与声明

- `op=sync` 整天口径零改动（`src/plan/sync.ts` 不在 diff 内）；单天 `runEnsure` 行为不变（批量只是逐天调它）。
- 本票无 HTML 页面（回执文本除外），零 HTML → 声明墙不触发。
- 没推过真飞书：全部读数经挡板（`makeSeam('schedule')` → `LARK_CLI_PATH` 指向 stub）。

## 六 · 复跑清单

```
node tooling/run-locked.mjs --ticket 599 -- node node_modules/typescript/bin/tsc -b --force packages/skill-schedule
node tooling/run-locked.mjs --ticket 599 -- node --test packages/skill-schedule/test/plan-ensure-batch-599.test.mjs
node tooling/run-locked.mjs --ticket 599 -- node --test packages/skill-schedule/test/*.test.mjs
node tooling/run-locked.mjs --ticket 599 -- pnpm gen:check
node tooling/run-locked.mjs --ticket 599 -- node .scratch/t599/mutate-599.mjs
```
