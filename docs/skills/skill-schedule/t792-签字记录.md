# #792 签字记录（人滚墙后的逐次结论）

> 归 [#792【收口】双端墙 ＋ 总索引 ＋ 逐格缺陷清单 → 修完复评 ≥90 → 人签字](https://github.com/FeatherHunter/ilife/issues/792)。
> 规矩出处：`docs/agents/视觉验收墙.md`（「没有墙不签字，有墙也不等于签了字」；签字落 `docs/skills/<件名>/`，同票号前缀）。
> 本件只记**人**给的结论（原话照抄）；AI 侧的 vision 打分与缺陷清单另落 #792 的收口证据件，不混进这里。

## 第 1 次 · 2026-09-22 · 手机墙 390（61 格）

**用户原话（一字未改）**：

```
通过验收 D:\ilife\docs\skills\skill-schedule\手机验证墙.html
```

**签字对象的确切字节**（登记这一刻实测；工作区干净，即所签＝已提交那一份）：

| 项 | 值 |
|---|---|
| 文件 | `docs/skills/skill-schedule/手机验证墙.html` |
| 字节 | 7708219 |
| sha256 | `975732C82094774EA587B452FBC09BFAD36C76690B3A04191DB78ED51632FDD7` |
| git | `bd3bed32` |
| 格数 | 61（八份清单件的产物数之和） |
| 当刻产物状态 | 含 #887 的复制区改造（复制数据三选一菜单、每页一处复制区） |

**这一签覆盖**：手机端 390 一格一款的产物样子与信息组织（滚完一整面的结论＝通过）。
**这一签不覆盖**：桌面 1280 那面墙、逐格缺陷清单、vision 逐页打分（那三样属 #792 第一／二段）。
**作废条件**：之后任何**产物内容**改动都要重出墙并重签（#888 只改六支域探针的基线常量、不动产物，故不影响本签）。

### 后续核验（2026-09-22 晚，同一轮里做的）

签名之后发生过一次**时间戳级**（非内容级）的产物重出：#888 收口时复跑 `t787-探针.mjs`／`t790-探针.mjs`，
`scheduleCopyLog` 把「当刻时间」写进每页日志载荷的「时间戳版本」那一段，于是那两族产物的字节变了、**页上可见内容一字未变**。

核验读数（逐格比对签名墙里内嵌的副本 vs 当刻产物）：

```
签名墙格数 = 61
与当刻产物「除时间戳外逐字节相同」 = 61 / 61
```

⇒ **本签仍然有效**（用户看的是内容，不是那一行时间戳），不重签；此条如实登记在案，作为「所签字节 ≠ 当刻字节」的说明。
若日后要做**内容级**改动（形状／文案／数据），照「作废条件」重出墙并请用户重签。

**同一轮里还发现并修掉一处副本陈旧**：`.scratch/t792/` 的发布副本是 13:21 那一代（签字那代），
而域产物在 13:59 被 #888 复跑刷新 ⇒ 墙与索引引的是旧副本。处置＝重跑 `t792-清单.mjs` 刷新发布副本、
重出两张墙与索引，核验「发布副本与域产物逐件同代 = **61/61** ✓」。

## 待签（第 2 次）—— **本票收口时仍未签，如实登记**

签的是**当刻这一代**（五张缺陷票全部落地后重出的那一批；墙与索引的字节／sha 见 `t792-验收墙-证据.md`「第 2 轮复评」末尾那张表）：

| 要滚的件 | 路径 | 怎么开 |
|---|---|---|
| **桌面墙 1280（61 格）** | `.scratch/t792/t792-桌面墙.html` | 要连**同目录**一起（61 件产物是它的兄弟文件） |
| 桌面墙（单文件版，单发一个文件就能开） | `.scratch/t792/t792-桌面墙-单文件.html` | 双击即可（7.39 MB，产物 `srcdoc` 内嵌） |
| 手机墙 390（61 格）· 内容已换代 ⇒ **须重看** | `.scratch/t792/t792-手机墙.html` / 单文件版同名 | 同上 |
| 总索引（8 族 ＋ 「有意不出」7 条） | `.scratch/t792/t792-总索引.html` | 双击即可（细看入口，不替代墙） |

- **第 1 签已按「作废条件」作废**：那五张票改的是页上可见内容（页内目录、字号、半角括号、飞书命令行文案、探测第二道门的读数），
  手机墙 390 那 61 格已**换代**。⇒ 第 1 签不再代表当刻产物，等第 2 次逐格结论。
- **复评读数（AI 侧自检门槛，不是签字依据）**：`逐页 ≥90 且每维 ≥80% ＝ 61/61`（均分 98.93／最低 95）；逐页表在 `t792-打分表.md`。
- **还有扣分的 16 页**（短页不出目录 9 页／公共层图例 11.5px 5 页／向导页 `HELP` 1 页，共 15 页，其中 1 页占两条）
  已在证据件「第 2 轮复评」逐条判级，**都不是待修缺陷**，但那两条口径由用户当场裁了（见上一节）。

**收口时的事实（写清，不混）**：用户给的是**两条口径的裁定 ＋ 收口指令**（原话见上一节），
**不是**「滚完当刻两张墙后的逐格结论」——他滚过并签的是**第 1 次那一代（手机墙 390）**，
而那一代已被缺陷票换代（见上「作废条件」）。故：

| 项 | 状态 |
|---|---|
| 逐页 ≥90 且每维 ≥ 满权 80%（AI 侧自检门槛） | ✅ 61/61（均分 98.93／最低 95），读数见 `t792-打分表.md` |
| 墙 ＋ 逐格缺陷清单 ＋ 自检退出码三样齐 | ✅ 见 `t792-验收墙-证据.md` |
| 人滚当刻两张墙后的逐格结论（签字依据） | ❌ **未做**（按用户收口指令关闭，未再请签） |
| 用户对第 1 代手机墙 390 的签字 | ⚠️ 已被换代作废 |

⇒ 这张表就是「本票按用户指令关了，但签字依据那一格仍是空的」的凭据；**日后若有人问「谁签的当刻这一代」，
答案是没有** —— 要补签就滚 `.scratch/t792/t792-桌面墙-单文件.html` 与 `t792-手机墙-单文件.html`，结论记这里。

## 裁两件口径（第 2 次 · 2026-09-22 · 用户原话）

用户原话（一字未改）：

```
向导页 HELP----这个不算扣分不用管
公共层图例那 5 页照现状收口---已经可以close 792
```

| 件 | 用户裁定 | 落到读数上 |
|---|---|---|
| 向导页 `HELP`（H7 −1） | **不算扣分** | 引擎按机器腿照扣 1 分（99）；判据侧按此裁定**不再追问**，不另立票 |
| 公共层图表图例窄屏 11.5px（D5 −5，5 页） | **照现状收口** | 5 页各 95 分（仍 ≥90）；这条债**不阻塞本票**，由 [#897](https://github.com/FeatherHunter/ilife/issues/897)（跨域共用位）承接 |

⇒ 据此，本票按「复评达标即关」收口（逐页 ≥90 且每维 ≥ 满权 80% ＝ **61/61**，均分 98.93，最低 95）。

## 第 3 代（2026-09-22 晚 · #906 落地后）—— **待你重滚重签**

用户原话（一字未改，就是这一代要做的事）：

```
D:\ilife\.scratch\t792\t792-手机墙.html的HTML中的 "复制初始化结果\复制初始化 prompt\复制与留档\复制给 AI"这些在复制数据按钮之上的文本 都删掉。过于多余了。
```

清点（已量，改前）：**61/61 页**都有一行同名的复制区小标题（`<h2 class="ilife-block-copy-block-title">`，紧贴「复制数据」按钮上方）。
处置写成票 [#906](https://github.com/FeatherHunter/ilife/issues/906)（小标题删、页内导航不动），已落地并关票。

`#906`（[票](https://github.com/FeatherHunter/ilife/issues/906)）把你指的「复制数据按钮之上那行小标题」删掉了
（61/61 页；`复制初始化结果`／`复制初始化 prompt`／`复制与留档`／`复制给 AI` 四种字面），**产物换代** ⇒
照 #792 票面「作废条件」：**以上两签都不再代表当刻产物**，要重滚重签。要滚的件（与第 2 代同一批路径，内容已换代）：

| 要滚的件 | 路径 | 字节 | sha256 |
|---|---|---|---|
| 手机墙 390（61 格） | `.scratch/t792/t792-手机墙.html` | 33524 | `2B9D726841920A3B30812C11CA8E346CDB0FAB6EB4FC006FBFF1BD00CDBDFF7E` |
| 手机墙（单文件，双击即开） | `.scratch/t792/t792-手机墙-单文件.html` | 7748712 | `B6F392C4F87B2F80D4B3FB677331A364AEF370FD7C29E74C41BDBC04741E4B28` |
| 桌面墙 1280（61 格） | `.scratch/t792/t792-桌面墙.html` | 33650 | `D579D22A192CCB503048E45981C6DF8D5E72089F80F351B4D13CDD8B861DFC6F` |
| 桌面墙（单文件） | `.scratch/t792/t792-桌面墙-单文件.html` | 7748838 | `38D7023F9748A3535B099DB706F2395FECCA5746E3F5B1E1F342E232F930BE11` |
| 总索引 | `.scratch/t792/t792-总索引.html` | 26876 | `2B9F38B68FF0A9D1B8656AA4D0F7B1FB8797E7130E87DAB7A01966ACB2F17FCD` |

**这一代换了什么**：只有一处 —— 复制区那行小标题没了（按钮自己写着「复制数据 ▾」／「复制日志」）；别的逐字不变
（复评 61/61、均分 98.93／最低 95，与上一代逐页同分）。读数与逐条改动见 `t906-证据.md`。

**页内导航里那 50 处同名字串仍在**（本票按「页内导航不动」办）：多小节页的目录里仍有一颗「复制与留档」，
指向一个现在没有小标题的复制区。滚墙时留意这一处 —— 觉得别扭就说一声，我另开小票换词或删项。

（沿革：本条起初写成「等人口径 A／B／C」，那是多余的一步 —— 用户的话已经是明确指令，故直接开票做了。）


