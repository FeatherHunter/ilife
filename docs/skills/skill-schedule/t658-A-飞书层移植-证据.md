# t658-A · 作息侧飞书层完整移植 ＋ 写 op 统一成合成写 ＋ 24h 聚合视图（证据件）

- 出件票：[#660](https://github.com/FeatherHunter/ilife/issues/660)（`Part of` 地图 [#658](https://github.com/FeatherHunter/ilife/issues/658)；依据规格 [#657](https://github.com/FeatherHunter/ilife/issues/657)）
- 判据：`docs/agents/合成写判据.md`（接缝 · 断言模板 · 偏离收口）＋ `docs/skills/skill-schedule/t659-不变量清单.md`（S-01～S-16）
- 裁定依据：`docs/skills/skill-calorie/t593-A-跨技能调用-裁定.md`（第四节偏离清单，本票续排 D-15～D-20）
- 老实现位置：`D:\2Study\StudyNotes\SKILLS\作息管家\`（仓外，只读对照）；新实现：`packages/skill-schedule/`
- 一句话：**飞书层按完整深度搬到 `src/plan/`（卡路里式能力目录）＋ 写 op 全部合成写 ＋ 24h 聚合视图接上两个唤醒词**；本条接缝上四条读数全绿，偏离条目逐条两行机器读数。

## 一 · 交付物与结构对账（必报五步第三步／第五步）

| 目录／文件 | 本票处置 | 一句话理由 |
| --- | --- | --- |
| `packages/skill-schedule/src/plan/` | **新增 11 件**（`index.ts` 门 ＋ `iso.ts`／`pull.ts`／`remoteDay.ts`／`receipt.ts`／`context.ts`／`ensure.ts`／`upsert.ts`／`single.ts`／`sync.ts`／`check.ts`／`overview.ts`） | 票面交付物路径点名 `src/plan/**`；「plan」是 HELP 一级分组「日程与计划」；目录与第一件真实代码同时建（#199 §⑦ 判据甲） |
| `packages/skill-schedule/src/fetch/feishu.ts` | 重写（平台层六条子命令 ＋ 归属锚口径） | 平台调用住取数层（卡路里先例：`src/fetch/` 按域分件），三阶段合并拉取等**编排**搬去 `src/plan/` |
| `packages/skill-schedule/src/fetch/{db,index,errors}.ts` | 小改（`getPlanTimestamps` ／ 转发面 ／ 错误码 `LARK_NO_EVENT_ID`） | 聚合视图要「该日首建／末改」，错误码是类型闭集 |
| `packages/skill-schedule/src/policy/{plan,wakewords,index}.ts` | 小改（`op=check`／`view`／`feishu` 三个参数面 ＋ 两词 preset） | 口径只住一处：参数面与唤醒词表都在口径层 |
| `packages/skill-schedule/src/help/lookup.ts` | 小改（槽位示例给真值：`dates` 给数组，不再出 `<值>`） | 速查行要「照抄即能跑」；SKILL.md 那张表就是从它生成的 |
| `packages/skill-schedule/SKILL.md` | 重生成（`node scripts/build-help.mjs`） | 生成物：唤醒词改了，速查块随生成链跟变（不手改） |
| `packages/skill-schedule/src/cli/cmd_read.ts` | 改（分派接线 ＋ 退出码语义） | 出口只有一条：合成写「没达成」时载荷照出、退出码非 0 |
| `packages/skill-schedule/test/plan-feishu-660.test.mjs`、`test/plan-overview-660.test.mjs` | 新增 | 本条接缝的读数（R1～R13／V1～V4） |
| `test/synthetic-write-659.test.mjs` | 改（T3／T4 登记表／T6／T7 收口） | 判据第五节第 6 条：**修好行为而不动登记，用例照样红**——必须同步改登记 |
| `packages/skill-schedule/test/cli.test.mjs` | 改（种子与本地档显式 `feishu:'skip'`；补一条降级退出码读数） | 写 op 成了合成写后，缺省那趟要碰远端；纯本地档由参数讲清 |
| `tooling/contract-lark-stub.mjs` | 改（补 `calendar events get`；时刻粒度窗口；三个可改坏旋钮） | 阶段 3 单条补描述要这条子命令；日期粒度窗口会让「分片规避上限」假绿 |
| `packages/skill-schedule/scripts/t660-mutation-probe.mjs` | 新增（一次性取证脚本） | 两行变异读数要能复跑，不能只贴结论 |

**偏差（第五步对账）**：事前报的影响清单写的是「`fetch`／`policy`／`render`／`cli` 四处 ＋ 新增 `plan/`」；实际**未碰 `render/`**（聚合视图的载荷装配落在能力目录自己的 `overview.ts`——卡路里先例：能力目录自持页装配），**多碰两处**：`src/help/lookup.ts`＋生成的 `SKILL.md`（唤醒词的示例要给真值，否则示例列出 `<值>` 就跑不通——见 V5）。另：票面交付物路径写的 `src/fetch/**` 按你的口径调整为「平台层留 `fetch/`、功能编排进 `src/plan/`」，`fetch` 不再当功能目录用。

## 二 · 四条读数（票面验收第一条）

```
node tooling/run-locked.mjs --ticket 660 -- node --test packages/skill-schedule/test/plan-feishu-660.test.mjs
```

| # | 读数 | 当刻实测 |
| --- | --- | --- |
| R1 | 四条读数模板 · `plan.write op=ensure` | `idempotent=pass{本地位数 1→1，远端重复建 0}`／`receipt=pass{本地侧 created，远端侧 created_feishu，远端标识 fs_evt_1}（3/3 格）`／`degrade=pass{本地照样写 true，回执标明 true，退出码 4}`／`ownership=pass{远端建 1 个，锚「作息管家自动同步 · 周会」}` |
| R2 | 三阶段合并拉取 ＋ 分片规避上限（S-10） | `议程=1 分片=4 补描述=3 重复建=0 本地标识=["fs_a","fs_b","fs_c"]`；分片窗口逐片断言 `00:00~06:00／06:00~12:00／12:00~18:00／18:00~00:00`；单次上限压到 1 条、议程三条都索引不到 —— 仍一条不漏、一条不重复建 |
| R3 | 归属锚写对且可反查（S-06／S-05） | 建对象描述＝`作息管家自动同步 · 周会`（逐字）；远端标识回了本地；远端那条 ↔ 本地这条对得上 |
| R4 | 孤儿清理（S-12） | `远端剩=["fs_orphan_other"]`：带锚的 `fs_orphan_mine` 清掉、不带锚的原样留着；`counts.deleted=1` |
| R5 | 同槽兜底清理（S-12 同槽一侧） | 改时段后 `远端剩=["fs_keep"]`：旧槽那条删了、同槽多余那条兜底清了、本地标识指向留下的那条 |
| R6 | 反向对账（S-11） | `唯一命中={"晨会":"fs_one","撞键":"fs_two"}`；撞键 2 候选只记账不猜（回执说明逐字点名） |
| R7 | 覆盖式写入 `op=upsert`（S-13 覆盖一侧） | `远端=["上午","下午"]`，`counts={created:2,found:0,deleted:1,failed:0,events:2}`（旧日程那条带锚孤儿清掉） |
| R8 | 删计划的远端真删 | 远端删除调用 1 次、远端剩 0 条、本地软删（`is_active=0`）、回执 `remote=deleted_feishu` |
| R9 | 飞书自检 `op=check`（D-03 日历半场） | 普通写路径 0 次自检对象；显式跑：`left=null／cleanup=null` 且远端 0 条残留；`deleteFails` 那一跑 `exit=4`，回执点名 `leftId=fs_evt_3` ＋ 清法含该 id |
| R10 | 只读档不碰远端 | `op=preview` 远端调用 0 次，`remote=none`（连四门都不探） |
| R11 | 远端整平台不可用（D-13 正面） | 一跑 **242ms** 返回（老实现同布景 25s 超时未返回、同窗口重复查 472 次）；`exit=4`／`local=created`／`remote=unavailable`／`achieved=false` |
| R12 | `op=sync` 远端不可用即阻断（S-09） | `exit=4`／`remote=unavailable`／`local=unchanged`／本地 0 行 |
| R13 | 远端一抖（分片检索失败）不挂死（D-13 反面） | 一跑 **737ms**（<12s 阈值），`exit=4`，回执逐条点名「分片检索失败」，本地照写 1 行 |

24h 聚合视图（票面「要做的事」第 7 条）：

```
node tooling/run-locked.mjs --ticket 660 -- node --test packages/skill-schedule/test/plan-overview-660.test.mjs
```

| # | 读数 | 当刻实测 |
| --- | --- | --- |
| V1 | 整点分桶 ＋ 同小时 `+` 合并 ＋ 空桶 | `格 8=早餐+并行事 格 13=午后 已排格数=2`；一天 24 格都在（空桶出「未规划」）；随附该日 `createdAt／updatedAt`；页面上写明这是聚合视图 |
| V2 | 多日查询（#16 独门能力） | `天数=2 日=["2026-09-22","2026-09-23"]`，逐日一段 |
| V3 | 两个唤醒词接上 | `24h 概览={"view":"aggregate"}`／`查多日计划={"view":"aggregate","dates":[…]}`；缺日期抛「缺槽位」；`查日程` 仍是全字段缺省档 |
| V4 | 两条路各走各的 | 全字段档三条都带 `id／synced／completion`，且不是聚合视图 |
| V5 | 速查表那两条唤醒词的示例照抄即能跑 | 逐条从 `buildHelpLookup()` 取示例、原样喂出口：`24h 概览 → --params '{"view":"aggregate"}' ⇒ exit 0`；`查多日计划 → --params '{"view":"aggregate","dates":["2026-09-21","2026-09-22"]}' ⇒ exit 0（天数=2）` |

## 三 · 偏离清单的机器读数（改坏必红／还原必绿，票面验收第二条）

```
node tooling/run-locked.mjs --ticket 660 -- node packages/skill-schedule/scripts/t660-mutation-probe.mjs
```

脚本对源码做**字节级**替换 → 重建 → 跑靶向读数 → 还原 → **比对文件哈希回到原值** → 重建 → 再跑一遍（日志落 `.scratch/t660-mut/`）。

| 变异 | 偏离条目 | 靶向读数 | 改坏必红 | 还原必绿 |
| --- | --- | --- | --- | --- |
| M1 | D-12 远端查询结果被丢弃 | `synth-659 :: T6` | `exit=1 pass=5 fail=2`——`AssertionError: 远端已有同四元组 → 不重复建（D-12 已修；改坏＝回到 1 条即红）`（连带 T4 的幂等读数转 fail） | `exit=0 pass=7 fail=0` |
| M2 | D-11 取不到远端标识仍写空 | `synth-659 :: T7` | `exit=1`——T7 断言 `本地标识=null／remote=unavailable／退出码 4` 三条红 | `exit=0`（T7 全绿） |
| M3 | D-13 分片失败不推进游标 | `plan-feishu-660 :: R13` | `exit=1`，该跑耗时 **15120ms**——被 15s spawn 超时杀掉（＝原地转圈，读数红） | `exit=0`，737ms 返回 |
| M4 | D-14 回执不分字段 | `synth-659 :: T4` | `exit=1`——`回执分字段` 读数只剩 1 格可读，登记表说 pass ⇒ 红 | `exit=0`（3/3 格） |
| M5 | D-03 自检删不干净仍报成功 | `plan-feishu-660 :: R9` | `exit=1`——`exit=4` 与 `leftId／cleanup` 断言红（回执谎报成功） | `exit=0` |
| M6 | 24h 聚合视图的分桶口径 | `plan-overview-660 :: V1` | `exit=1`——`格 8=早餐+并行事` 等分桶断言红 | `exit=0` |

小结：**6/6 条既红又绿**（每条还原后文件哈希与原值一致，脚本自证）。

## 四 · 偏离清单追加（编号续排；四格 ＋ 两行读数）

正本＝裁定件第四节；本票追加 D-15～D-20。两行读数见上表（D-11／D-12／D-13／D-14／D-03 的两行在上表 M1～M5；D-15～D-20 的读数在下表逐一挂）。

| 编号 | 老行为 | 新行为 | 为什么 | 出处 | 两行读数 |
| --- | --- | --- | --- | --- | --- |
| D-15 | 两路并发拉取（`ThreadPoolExecutor`：议程与分片检索同时发） | **顺序发**（议程一次 ＋ 分片四次） | 功能等价、读数相同（S-10 的分片与合并口径不变），并发只影响时延；且老实现的并发正是 D-13 挂死的工地 | 老 `feishu_sync.py:553-565`；新 `src/plan/pull.ts` | 改坏＝把分片改回「单次整日检索」→ R2 的 `分片=4` 断言红；还原即绿（M3 同族读数为证） |
| D-16 | 远端同槽位事件只要**标题＋时刻**对上就认成自己的（不判归属） | 只认**带归属锚**的；撞见别人的对象只记进回执说明，不据为己有 | S-12 把「谁是我管的」唯一化到归属锚上；据为己有会在下一轮把它当孤儿删掉（用户的日历会被误删） | 老 `schedule_db.py:1123-1132`；新 `src/plan/remoteDay.ts` | 改坏＝把 `matchIn` 的 `e.owned &&` 去掉 → R4 的「不带锚的原样留着」转红（它会先被认走再被清）；还原即绿 |
| D-17 | 写命令＝「先写本地，再问一句要不要同步」 | 缺省**合成写**（本地 ＋ 远端一条命令）；只要本地由参数 `feishu:"skip"` 说明 | 裁定 A6③／规格「写命令语义统一」：调用方不必先问「你现在能不能连远端」再拼几条命令 | 老 `schedule_cli.py:850-890`；新 `src/cli/cmd_read.ts` 的 `remoteGate` | 改坏＝把缺省改回只写本地 → R1 的 `ownership`／`receipt` 两条转 fail；还原即绿 |
| D-18 | 失败一律 `print` 后 `return`（退出码 0） | 「这一趟没达成」⇒ 退出码非 0（本地成了远端没成也是 4） | 裁定 A6②／用户故事 6：上层不许误以为事情办完了；降级照老（本地照写、回执如实） | 老 `schedule_cli.py:648`／`:488`；新 `src/plan/context.ts` `receiptResult` | 改坏＝把 `exitCode` 钉成 0 → R11／R12／R13 的退出码断言红；还原即绿 |
| D-19 | 分片部分失败静默 `continue`（读不全也不说） | 逐条记账进回执；「远端读不全」⇒ 这一趟不算达成（非 0） | 读不全会导致重复建（正是 D-13 那条链的后果）；静默等于把「漏读」伪装成「远端没有」 | 老 `feishu_sync.py:546-549`；新 `src/plan/pull.ts` | 改坏＝把 `errors.push('分片检索失败…')` 去掉 → R13 的记账断言红；还原即绿 |
| D-20 | 作息侧**没有**飞书自检（老只有 `_probe_calendar_writable` 只读探测；D-03 的真出处是备忘侧 sentinel） | 新增 `op=check`：默认不跑／回执点名「留下了什么／在哪／怎么清」／日程域删干净、删失败非 0 | 裁定 D-03 三处改造的日历半场；「只看授权证明不了真能写」这个价值要留，代价要可见 | 老 `feishu_sync.py:204-219`（只读探测）；备忘侧 sentinel `备忘录/script/feishu_sync.py:61-62`、`:831-921`；新 `src/plan/check.ts` | 两行见上表 M5（`exit=4` ＋ 点名断言） |

## 五 · 前票挂账的落地（#659 六节那张表里属 #660 的行）

| 缺口 | 落地处 | 读数 |
| --- | --- | --- |
| 回执不分字段（只有一个 `message`） | `src/plan/receipt.ts`（`local／remote／remoteId` 三格永在 ＋ `achieved`） | R1 `receipt`／M4 |
| `op=ensure` 不碰远端 | `src/plan/ensure.ts`（一律查一趟再判） | R1／R3 |
| 远端对象无归属标记 | `src/fetch/feishu.ts` 的 `composeFeishuDescription`（唯一口径处） | R3 |
| `op=sync` 丢远端查询结果（D-12） | `src/plan/sync.ts`（阶段 0 回填 ＋ 主循环判重） | 659 T6／M1 |
| `create_event` 拿不到标识仍写空（D-11） | `larkCreateEvent` 显式 throw ＋ 调用方不写空、标 `unavailable` | 659 T7／M2 |
| 飞书自检三处改造（D-03） | `src/plan/check.ts`（日历半场；任务域归 #661） | R9／M5 |
| 分片死循环（D-13） | `src/plan/pull.ts`（游标一定推进；两路全断即上抛） | R11／R13／M3 |
| 回执 `found` 支缺 `feishu` 键（D-14） | 同「回执不分字段」一行（三格永在，键名统一到 `remote`／`remoteId`） | R1／M4 |
| 飞书层完整深度（分片、远端判重、孤儿清理、同槽清理、反向对账） | `src/plan/pull.ts`／`remoteDay.ts`／`sync.ts` | R2／R4／R5／R6 |
| 24h 聚合视图与多日查询 | `src/plan/overview.ts` ＋ 两个唤醒词 preset ＋ `view` 参数 | V1～V4／M6 |

## 六 · 不动的东西（自查）

- 未改 `packages/base-combos/**`；未改 `tooling/skilllink.mjs` 对外行为（`test/skilllink.test.mjs` 仍绿）。
- 生成物（`keys.ts`／`registry.ts`／`routes.generated.ts`／combos 镜像）**一个字节都没手改**（本包没有这些生成物；`pnpm gen:check` 不在本票触及面上）。
- **没推过真飞书**：全部读数经 `tooling/contract-lark-stub.mjs` 挡板（`LARK_CLI_PATH` 与老侧查找链两种注入），一次真实远端调用都没有。

## 七 · 遗留出口（另开票的候选，不在本票）

1. **唤醒词／命令登记形状**：键与唤醒词今天仍住 `policy/wakewords.ts`（不是卡路里那种「能力目录自持 `commands.ts`／`routes.ts` ＋ 生成器派生共用位」）。本票只把**处理函数**搬进 `src/plan/`，声明面留待整包重排那张票（#199 §①「留给后面裁的那条」）。
2. **老侧对拍件未重跑**：`packages/skill-schedule/scripts/t659-parity-probe.mjs` 是老 ↔ 新对拍的一次性件；本票只改了挡板的窗口粒度与三处旋钮（对老侧全日窗口无影响），未重跑该件。要重跑见 `docs/agents/合成写判据.md` 第四节命令。
3. **任务域自检（D-03 另半场）**：`task +complete` 终态、`task +update` 改期、任务删除（lark-cli 无删除子命令）归 #661。
4. **`npm` 发布链**：本票不动版本与发布（包内 `AGENTS.md` 的 wizard 流程照旧）。

## 八 · 复跑清单

```
node tooling/run-locked.mjs --ticket 660 -- node node_modules/typescript/bin/tsc -b packages/skill-schedule
node tooling/run-locked.mjs --ticket 660 -- node --test "test/synthetic-write-659.test.mjs" "packages/skill-schedule/test/*.test.mjs"
node tooling/run-locked.mjs --ticket 660 -- node packages/skill-schedule/scripts/t660-mutation-probe.mjs
```

当刻汇总：包内 ＋ 判据常驻用例 **103/103 绿**（含本票新增的 R1～R13／V1～V5）；变异取证 **6/6 条既红又绿**；边界门另跑 `test/scaffold.test.mjs`／`test/skills-export-47.test.mjs`／`test/skilllink.test.mjs`／`test/schedule-split.test.mjs`／`tooling/test/skill-html-snapshot.test.mjs` 全绿。
