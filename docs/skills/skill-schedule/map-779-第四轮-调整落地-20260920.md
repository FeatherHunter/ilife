## 第四轮·调整落地（2026-09-20，任务收口）

按第一性原理（并发正本 `docs/subagent-concurrency-protocol.md` §1／§2／§2.6）重排了这张图的阻塞与并发，并落地。**地图与子票的正文已按新口径改写，边已重连，自校验双绿。**

### 这一轮改了什么

| 改动 | 内容 |
|---|---|
| 拆 | `【骨架】`（#780）收成**纯结构**；新增 **#843【交付面】**（唤醒词命令缺省落盘 ＋ 产物落点分家 ＋ 命名一处定义） |
| 增 | 新增 **#844【种子】**（隔离种子库：锚点日期 ＋ 覆盖五域的可复现数据）——这是 Q6 裁定的落地物，原先无票认领 |
| 降 | `【清单】`（#781）降级为**只复述老侧事实**（85 行 ＋ 18 条家族必现块 ＋ 别名表），**不做设计裁定**；设计裁定回到各域票 |
| 串 | 八张域票串成**写者互斥链**（#783→#784→#785→#786→#787→#788→#789→#790） |
| 补 | 每票新增 `## 写面（协议 §1 路径所有权）` 段；域票判据改成**按清单逐场景驱动** ＋ 家族必现块 ＋ 代码层窄判据（字号／间距／色值不许裸字面量）＋ 本域小墙；`【收口】`（#792）改成**两段**（审查开票 → 修完复评 ≥90 → 人签字）；新唤醒词进 `SKILL.md` frontmatter 触发词 |

### 机器读数（本轮实跑）

```
图体检（线上态）  票 15 张，边 38 条
  ✓ 查环 PASS：无环（拓扑序排满全部票）           ← 死锁面：无环
  ✓ 无外部阻塞（没有票被图外的活挡着）
  前沿宽度：2 张可开工  ✓（#780【结构】、#781【清单·取证】）
  ✓ 写面检查 PASS：15 张票的写面，凡有重叠的都已用先后边错开   ← 干扰面：0 撞车
  图体检：PASS（exit 0）

自校验             sub_issues 条数 expected=15 actual=15；15 张票的被阻塞集合逐条 OK → 校验 PASS（exit 0）
反例（工具自证）    抽掉链上任意一条边 → ✗ 写面撞车 21 对，exit 1（这道红能红，不是永远绿）
正文格式契约        地图 102 行、8/8 章节在行首、零字面 \n、无 BOM、表格未塌；15 张子票逐张 5/5 段齐
面板读数            closed/total = 0/15（不是 0/0）
```

### 并发口径（写进地图 Notes 了，别当成遗漏）

`packages/skill-schedule` 是**单写者资源**：`tooling/skill-html.snapshot.json` 是一个覆盖 5 技能 187 件的单文件、三件生成物由**扫全包**的生成器产出、`dist` 的读数必须安静窗口（协议 §2.6）。所以**主干任何时刻只有一张票在写**，并发放在**写面不相交的支线**（清单取证、种子、链路页生成器、证据件撰写）。

**边的性质**（别把锁当依赖读）：语义依赖＝交付面←结构、种子←结构、形状←结构＋交付面＋种子＋清单、八张域票←结构＋形状、链路页←八域、收口←链路页；**写者互斥序**＝八张域票之间的 7 条链边。

### 现在可以开工的

- **#780【结构】**：五域能力目录 ＋ 命令登记进 `src/<能力>/commands.ts` ＋ 接 `pnpm gen`（动共用位，先抢 `gate.lock` 并广播）。
- **#781【清单·取证】**：85 场景老侧事实 ＋ 18 条家族必现块 ＋ 别名表（只读老仓，写面与主干不相交）。

两票可并行；关任何一张票之前先跑 `node docs/skills/skill-schedule/map-chart.mjs <规格> lint`。收口时进度写 95% 必须点名「待用户滚墙肉眼终审并签字」，未签字不得 close 本图。
