# t895 证据 · 飞书探测三页内部命令名上屏

票：[#895](https://github.com/FeatherHunter/ilife/issues/895)（同源 [#875](https://github.com/FeatherHunter/ilife/issues/875) 备忘录家族债）。
口径：上屏正文位一律「飞书命令行」（照 `src/admin/handlers.ts:193/197` 定稿），`lark-cli` 只许留在 agent 面向的参考件与载荷位。
本次推翻三处旧判语：`t792-验收墙-证据.md` 的 `english` 判语表＋「不算的」段、`t788-域-证据.md:119`、
`test/t790-辅助与管理.test.mjs:44` 与 `src/admin/adminDocs.ts:19-20` 的「`lark-cli` 是例外」注释。

## 一、改前改后逐条对照

### 1.1 三页 10 处（正文位）

| 页 | 处 | 改前（逐字） | 改后（逐字） | 落点 |
|---|---|---|---|---|
| 三档全通 | 读数卡 detail | `lark-cli 在场，授权与日历两道门都过了，可以同步` | `飞书命令行在场，授权与日历两道门都过了，可以同步` | `src/plan/probe.ts` 全通 `why` |
| 三档全通 | 结论条 | `飞书能力：三档里的全通。lark-cli 在场，…` | `飞书能力：三档里的全通。飞书命令行在场，…` | 同上经 `feishuDocs.ts:101` |
| 三档全通 | 事实条「命令行版本」 | `version 9.9.9`（`lark-cli` 前缀已 strip，剩英文 `version`） | `9.9.9`（只留数字） | `feishuDocs.ts` 版本格（口径 `fetch` 门 `shortLarkVersion`） |
| 没装 | 读数卡 detail | `本机没找到 lark-cli，同步与探测都跑不了。…` | `本机没找到飞书命令行，同步与探测都跑不了。…` | `probe.ts` 缺装 `why` |
| 没装 | 结论条 | `飞书能力：三档里的没装。本机没找到 lark-cli，…` | `…本机没找到飞书命令行，…` | 同上 |
| 没装 | 下一步第 1 条 | `lark-cli 没装：装与不装由你定，本技能不代装也不代登。` | `飞书命令行没装：装与不装由你定，本技能不代装也不代登。` | `TIER_NEXT.missing[0]` |
| 装了没登录 | 读数卡 detail | `lark-cli 装了但没登录（lark-cli auth status 失败（未登录？）），先在终端里跑一次授权登录` | `飞书命令行装了但没登录，先在终端里跑一次授权登录`（嵌套括号一并消除，不再拼 `e.message` 原文） | `probe.ts` 鉴权 `why` |
| 装了没登录 | 结论条 | 同上 ×2 | 同上 | 同上 |
| 装了没登录 | 下一步第 1 条 | `先在终端里把 lark-cli 的授权补上，再回来说一次「飞书探测」。` | `先在终端里把飞书命令行的授权补上，再回来说一次「飞书探测」。` | `TIER_NEXT.partial[0]` |
| 装了没登录 | 事实条「命令行版本」 | `version 9.9.9` | `9.9.9` | 同上 |

### 1.2 七条抛出文案（`src/fetch/feishu.ts`，code 不变，只换 message）

| # | code | 改前 | 改后 | 门 |
|---|---|---|---|---|
| 1 | `LARK_UNAVAILABLE`（`runLark` ENOENT） | `lark-cli 不可用：<路径原文>`（路径本身含 `lark-cli`） | `飞书命令行不可用：没找到可执行文件`（不再拼路径） | 不可用 |
| 2 | `LARK_TIMEOUT` | `lark-cli 超时：<命令原文>` | `飞书命令行超时：等太久没回应，稍后重试`（不再拼参数） | 超时 |
| 3 | `LARK_NOT_LOGGED_IN`（`auth status` 非 0） | `lark-cli auth status 失败（未登录？）` | `还没登录：登录查询没通过` | 没登录① |
| 4 | `LARK_BAD_RESPONSE`（非 JSON） | `lark-cli auth status 非 JSON` | `登录信息读不懂：重登一次再探` | 返回异常 |
| 5 | `LARK_NOT_LOGGED_IN`（无 openId） | `lark-cli 未登录（无 openId，先 auth login）` | `还没登录：查不到登录身份` | 没登录② |
| 6 | `LARK_UNAVAILABLE`（`larkReady` 无 CLI） | `lark-cli 未找到：缺失阻断同步` | `没找到飞书命令行：缺失阻断同步` | 没装 |
| 7 | `LARK_DENIED`（日历不可达） | `日历不可达（缺授权？先 lark-cli auth login）` | `日历拉不动：多半是缺日历授权，补一次授权再探` | 日历授权 |

### 1.3 同源另两面（任务第 4 条：并进本票）

- 日程管家同步页 `重同步阻断：远端不可用（<why>）`：`why` 即上表 6／7（`larkReady` 经 `remoteGate` 进 `remoteWhy`），修完自动干净，不另改 `sync.ts`。
- 体检／设置面 `lark.cli`（`src/health.ts`）：绿 `已就绪：<裸路径>（版本 <原文>）` → `已就绪：飞书命令行可用（版本 1.0.59，目录 <所在目录>）`；
  黄 `找到了 <裸路径>，但还没登录／日历读不到。` → `在 <所在目录> 找到了飞书命令行，但还没登录／日历读不到。`；
  红（`没找到飞书 CLI…`）本来就不含 `lark-cli`，不动。标题 `飞书 CLI`（面板状态行原名）不动。
  版本与遮蔽走 `fetch` 门同一口径（`shortLarkVersion`／`maskLarkCli`，铁律二唯一定义地）；
  目录取所在目录（可执行文件名已剥掉），scoop 那档目录名本身含工具名，一并经 `maskLarkCli` 遮蔽。

### 1.4 `larkVersion` 的 `'unknown'` 挡板

- 口径唯一定义地：`src/fetch/feishu.ts` 的 `shortLarkVersion`（取首个 `\d+(\.\d+)*`，无则 `读不到`）与 `maskLarkCli`
 （`lark-cli` 字样→中文显示用名，原串不动）——`fetch` 门对外给的显示口径，取用者见定义处注释
  （`feishuDocs` 版本格、`health` 版本＋目录）。本轮 code-review（Standards 轴）指出初版两处各写一份正则违反铁律二，已收敛。
- `feishuDocs.ts`：`report.version === null → 没装`，否则 `shortLarkVersion(report.version)`。
  `unknown` → `读不到`；`lark-cli version 9.9.9` → `9.9.9`；`lark-cli version 1.0.59-stub` → `1.0.59`。
- `health.ts`：同口径（绿档才读版本）。

## 二、三页复量读数（可见文本节点，标签属性已剥离，载荷位不计）

禁词表（大小写不敏感子串）：`lark-cli`／`auth`／`status`／`version`／`openId`／`scope`。

改前（`.scratch/t895/repro.mjs`，dist 重编前）：

- 全通档：`lark-cli, version`
- 不完全档：`lark-cli, auth, status`
- 没装档：`lark-cli`
- `TIER_NEXT.partial[0]`／`missing[0]` 各含 `lark-cli`

改后（`test/t788-复盘与飞书.test.mjs` V11 全绿，三档 6 词＋5 组命令原文位全零；版本格 `9.9.9` 在屏；`version: 'unknown'` 直调印 `读不到` 且零禁词）。

## 三、变异自证两行

- 红：`probe.ts` 全通 `why` 改回 `lark-cli 在场…` → V11 红并点名那一页那一处：
  `AssertionError: 全通档可见文本含内部命令名 + actual ['lark-cli'] - expected []`。
- 绿：改回 `飞书命令行在场…` → V11 绿（含 V8／V10 同跑全绿），判据复位。

## 四、被判据改动清单（同批改，不改必红）

- `test/t788-复盘与飞书.test.mjs`：V8（`把飞书命令行的授权补上`）＋ 新增 V11（零内部命令名可红判据：
 6 词＋`--version`／`+agenda`／`auth login`／`--calendar-id`／`events delete` 命令原文位）＋ 新增 V12
 （七条全文逐字＋`shortLarkVersion`／`maskLarkCli` 直断＋零禁词，挡板扩展 `hang`／`bad-json`／`no-openid`／`no-calendar` 四模式）＋ 头注释 V11／V12 行。
- `test/t764-设置页收窄.test.mjs`：绿档（`已就绪：飞书命令行可用`＋`版本 1.0.59`＋零禁词断言）＋ 黄档（`找到了飞书命令行…`＋零禁词断言）。
- `test/t790-辅助与管理.test.mjs`：`assertNoDebt` 禁词与 V11 同口径（6 词），头注释去「例外」。
- `docs/skills/skill-schedule/t788-探针.mjs:165`：`把 lark-cli 的授权补上` → `把飞书命令行的授权补上`；
 全通（:160）与没装（:169）`needs` 各加 `飞书命令行`（旧文案页无此连续串，钉住新口径；:169 原三词仍在）。
- 旧判语三处：`t792-验收墙-证据.md`（`english` 表行改「推翻（#895）」＋「不算的」段去 `lark-cli` 四词＋ H7 待人裁注记）、
  `t788-域-证据.md:119`（改推翻句）、`src/admin/adminDocs.ts:19-20`（改推翻句）。

## 五、遗留出口（写清，不留缝）

- **指引性用法保留**：`TIER_NEXT` 的「去装／去登／装完回来说飞书探测」三句只换工具名为中文，含义与出口不变。
- **`lark-cli` 在 agent 面参考件照旧**：SKILL.md／HELP 指令文本／安装指引（`LARK_INSTALL_PROMPT`）／复制数据 `message`（`feishuDocs.ts:148-153` 一带）一字不动。
- **`check.ts:59` 清理命令原文不动**：`lark-cli calendar events delete …` 是用户手工清孤儿的可执行指引（载荷性），且 `op=check` 不出 HTML 页，不进三页判据。
- **取数深层错误串**：`mustJson` 的 `r.stderr.slice(0, 200)`（外部进程原文）在极端档（装了也登了但某分片非 0）可经同步页 `errors` 上屏——本次未动（动即把调试信息变哑，且 `plain()` 加英文遮蔽有误伤正常中文词的风险评估未做）。若收口要清，另开一票。
- **体检标题 `飞书 CLI`**：面板状态行原名（`health-contract.ts` 镜像），只含 `CLI` 不含 `lark-cli`，禁词不命中，不动。
- **`runLark` 超时分支**：V12 用 `hang` 挡板＋50ms 覆盖 `ETIMEDOUT`（真机 15/30/60s 三档常量不动）。
- **探针门读数**：见六（t788 探针重跑段）。

## 六、门读数

- 编译：`tsc -b packages/skill-schedule --force` 经 `run-locked --ticket 895` 全绿。
- 用例：t788（12／12，含 V11／V12）＋ t764（10／10）＋ t790（8／8）全绿；
  本包全量 `test/*.test.mjs` 首轮 **248／248 全绿**（24 suites，含 #896 席 4 条，共存）；
  review 返工后第二轮 **247／248**，唯一红是 #894 席在途自改
  （其给自家用例新加 `completionLabelOf('WIP(x)') === 'WIP(x)'` 一行，撞自家映射实现；`reviewDocs.ts`＋t894 用例零交集，非本票回归）。
- t788 探针（`--allow-dirty --no-baseline --no-shots`，ticket 788 跑；第二轮 `--out .scratch/t895/probe788b`，needs 已加固）：
  两轮均为 **RESULT: PASS pages=13 red=0**（逐场景行／不相交／边界／必现块 13 页／反面 13 页／窄判据／生成物 GEN-CHECK PASS／
  探测只读 10 次调用零写／同步真建对照）。
- 分隔符门／三档溢出：随探针 ④ —— 出页门 `RESULT: PASS pages=13 red=0`（390／768／1440 三档零溢出，飞书三页在内）；
  分隔符门 `RESULT: 13/13 PASS`（飞书三页命中 0）。
- 七支域探针：只重跑 t788 一支（本票输入面：plan／fetch／health／admin）。他域产物零改动——探针内
  `别域产物逐字节不变`（#783 7 件／#784 5 件／#785 5 件／#786 9 件／#787 11 件）全过，故他域不重出重跑。

## 七、必报五步

- **第一步 · 影响清单**：见实施前报（12 行：`src/fetch/feishu.ts`＋`src/fetch/index.ts` 转出、
  `src/plan/probe.ts`、`src/plan/feishuDocs.ts`、
  `src/health.ts`、`src/admin/adminDocs.ts` 注释、3 支用例、t788 探针、2 份证据件、t895 证据件＋`.scratch/t895/`）。
  未碰：`src/shared/`、`src/cli/` 生成物、公共层、SKILL.md／HELP、`sync.ts`／`check.ts` 逻辑。
- **第二步 · 结构设计**：零新增目录；`fetch` 门新增 `shortLarkVersion`／`maskLarkCli` 两个显示口径出口
 （取用者：`feishuDocs` 版本格、`health` 版本＋目录，定义处注释写明；包门加名因有明确取用者，见铁律门禁）；
  `why` 不再透传 `e.message`（含义由 fetch 层 code＋message 在 V12 全文断言）；版本号提取只此一处（code-review 返工前为两处）。
- **第三步 · 写代码**：跨能力引用只走公开接口（health 调 `findLarkCli`／`larkVersion`，均为 `fetch` 门内公开函数）；常量与类型无新增复写。
- **第四步 · 超线报警**：`src/health.ts` 超 400 行告警线（系既有，本次 ＋8 行版本号与遮蔽传参，不另拆——拆体检报告会散判据，下次动它时再议）。
  其余四件（`feishu.ts`／`feishuDocs.ts`／`probe.ts`／`adminDocs.ts`）均未超线。
- **第五步 · 交付对账**：实碰与第一步逐行对，偏差零（`git diff --stat` 复核，见八）。

## 八、交付清单（`git diff --cached --name-only` 复核后提交，只含本票路径）

- `packages/skill-schedule/src/fetch/feishu.ts`
- `packages/skill-schedule/src/fetch/index.ts`（转出上条两个显示口径）
- `packages/skill-schedule/src/plan/probe.ts`（只含本票 6 处文案 hunks；同文件 #896 席的 `authenticated` 管线 hunks 留工作区，由该席收口）
- `packages/skill-schedule/src/plan/feishuDocs.ts`（只含本票 import＋版本格 hunks；同上）
- `packages/skill-schedule/src/health.ts`
- `packages/skill-schedule/src/admin/adminDocs.ts`
- `packages/skill-schedule/test/t788-复盘与飞书.test.mjs`
- `packages/skill-schedule/test/t764-设置页收窄.test.mjs`
- `packages/skill-schedule/test/t790-辅助与管理.test.mjs`
- `docs/skills/skill-schedule/t788-探针.mjs`
- `docs/skills/skill-schedule/t788-域-证据.md`
- `docs/skills/skill-schedule/t792-验收墙-证据.md`
- `docs/skills/skill-schedule/t895-证据.md`（本件）
