# #893 证据：正文类字号下限 12px（10 页）

改动只动字号数值，不动形状、口径、布局。

## 1. 改动（两处）

| 文件 | 改前 | 改后 | 影响的选择器 |
|---|---|---|---|
| `packages/skill-schedule/src/analyze/analyzeParts.ts:27` | `const FS_TINY = 11;` | `const FS_TINY = 12;`（附注释说明与 `FS_SMALL` 同值是刻意的） | `.sch-an-diff-tag`、`.sch-an-diff-val`、`.sch-an-radar-label`（SVG 雷达轴名，一起抬大正是想要的）、`.sch-an-frame-badge`（含窄屏覆盖，共 5 处引用，同一常量） |
| `packages/skill-schedule/src/shared/pageParts.ts:170` | 窄屏媒体查询 `'  .heat-day, .heat-sum { font-size: 11px; }'` | 整行删除，继承基线 12px（留 JS 注释说明，两种修法中选了删除这一种） | `.heat-day`（行首日期）、`.heat-sum`（行尾合计）在 820px 以下视口 |

## 2. 改前 / 改后最小字号（样式产出串逐字量）

- 改前（复现脚本 `repro-893.mjs`，已清理）：`FS_TINY = 11`，5 行引用；`pageParts` 基线 12px 但窄屏覆盖压到 11px。结论：390 档 10 页最小字号 11px，小于下限 12px，与票面一致。
- 改后：`src` 内 `11px` 只剩一处 `.heat-ticks`（小时刻度，见第 5 节）；`analyzePartsCss()` 无 `11px`，全部字号 ≥ 12；`pagePartsCss()` 基线 `.heat-day`／`.heat-sum` 为 12px 且无窄屏 11px 覆盖。

## 3. 测试与变异自证

- 新增 `packages/skill-schedule/test/t893-字号下限.test.mjs`（3 条）：
  1. 分析域样式串无 `11px` 且全部字号 ≥ 12；
  2. 热力矩阵基线 12px 且无窄屏 11px 覆盖；
  3. 全串小于 12px 的只许是 `.heat-ticks` 行（含反恒真检查：例外行须存在）。
- 变异自证（只改 git 忽略的 `dist`，不碰源码）：把 `dist` 里 `FS_TINY` 拨回 11 → 第 1 条按预期变红（`11px` 重现）；重编恢复 12 → 3 条全绿。

## 4. 回归

- 本包全量用例：242 / 242 通过（含分析域 t789、形状 t782、单日族 t784，以及同时在途的 t892、t894）。
- 编译：`node node_modules/typescript/bin/tsc -b packages/skill-schedule --force` 通过（中途撞见另一席对 `adminParts.ts` 的半编辑态导致一次失败，等其收尾后重编通过；本票未碰该文件）。

## 5. 如实登记（本票未修的两处）

1. `.heat-ticks`（小时刻度）仍是 11px：判分引擎的 `minFontPxNoSvg` 只看正文类，不在 6 页命中里，属顺带范围，本票不改。测试第 3 条把它显式放行并加了注释。
2. 公共层 `packages/base-render/src/charts.ts:2130` 图例 `span.ilife-charts-legend-item` 11.5px（区间汇总 21 天那 1 页）：跨域共用位，本票不碰；该页仍扣 5 分，需另开公共层的票。

## 6. 未重跑的链条（待确认，见 issue 进度区）

- `fmt.json` 三档重跑与判分引擎复算（`d5.parts.font = 0`、`d5 ≥ 20`）本席未跑：需要真浏览器（headless Chrome）与读数席的产物目录（`.scratch/t792/产物` 属另一席在途的收口工作，本席未碰）。
- 等价依据：10 页的字号只来自上述两处常量（`src` 内 `grep 11px` 已无正文类命中），产物 CSS 是这两个函数的逐字拼接；测试逐字断言了产出串。读数席用新产物重跑 `fmt.json` 确认后，本票即可关闭。
