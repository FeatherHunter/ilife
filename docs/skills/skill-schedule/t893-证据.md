# #893 证据：正文类字号下限 12px（10 页）

改动只动字号数值，不动形状、口径、布局。

## 1. 改动（三处）

| 文件 | 改前 | 改后 | 影响的选择器 |
|---|---|---|---|
| `packages/skill-schedule/src/analyze/analyzeParts.ts:27` | `const FS_TINY = 11;` | `const FS_TINY = 12;`（附注释说明与 `FS_SMALL` 同值是刻意的） | `.sch-an-diff-tag`、`.sch-an-diff-val`、`.sch-an-radar-label`（SVG 雷达轴名，一起抬大正是想要的；SVG 文本不进探针的 `min`，属视觉一致）、`.sch-an-frame-badge`（含窄屏覆盖，共 5 处引用，同一常量） |
| `packages/skill-schedule/src/shared/pageParts.ts:170` | 窄屏媒体查询 `'  .heat-day, .heat-sum { font-size: 11px; }'` | 整行删除，继承基线 12px（留 JS 注释说明，两种修法中选了删除这一种） | `.heat-day`（行首日期）、`.heat-sum`（行尾合计）在 820px 以下视口（含 768 档） |
| `packages/skill-schedule/src/shared/pageParts.ts:160` | `.heat-ticks { … font-size: 11px; }` | `font-size: 12px`（附注释） | `.heat-ticks`（小时刻度）。对抗式自查追加（见 §7）：探针量全部非 SVG 可见文本，刻度进 `min`，票面“顺带不算判据”的说法不成立，留 11 则 6 页继续扣 5 分 |

## 2. 改前 / 改后最小字号（样式产出串逐字量）

- 改前（复现脚本 `repro-893.mjs`，已清理）：`FS_TINY = 11`，5 行引用；`pageParts` 基线 heat-day／heat-sum 12px 但窄屏覆盖压到 11px，另有 heat-ticks 11px。结论：390 档 10 页最小字号 11px，小于下限 12px，与票面一致。
- 改后：`packages/skill-schedule/src` 内全部具名字号常量 ≥ 12（`FS_TITLE` 15／`FS_BODY` 13／`FS_SMALL` 12／`GOAL_FS_*` 22·13·12），无 `font:` 简写、无小数与 10px 以下字面量；`analyzePartsCss()` 无 `11px`；`pagePartsCss()` 基线 `.heat-day`／`.heat-sum`／`.heat-ticks` 均为 12px 且无窄屏 11px 覆盖。

## 3. 测试与变异自证

- 新增 `packages/skill-schedule/test/t893-字号下限.test.mjs`（3 条）：
  1. 分析域样式串无 `11px` 且全部字号 ≥ 12；
  2. 热力矩阵基线 `.heat-day`／`.heat-sum`／`.heat-ticks` 均为 12px，且无窄屏 11px 覆盖；
  3. 热力矩阵全串字号零容忍（低于 12 一条不许剩；阳性对照防恒真：三行基线规则必须存在）。
- 变异自证（只改 git 忽略的 `dist`，不碰源码）：把 `dist` 里 `FS_TINY` 拨回 11 → 第 1 条按预期变红（`11px` 重现）；重编恢复 12 → 3 条全绿。

## 4. 回归与实测（R3：真浏览器读数）

- 本包全量用例：248 / 248 通过（含分析域 t789、形状 t782、单日族 t784，以及同时在途的 t892、t894；R1 时 242，期间隔壁席新增用例）。
- 编译：`node node_modules/typescript/bin/tsc -b packages/skill-schedule --force` 通过（中途撞见另一席对 `adminParts.ts` 的半编辑态导致一次失败，等其收尾后重编通过；本票未碰该文件）。
- 真机复量（R3，自渲染 11 页＋真 Chrome）：脚本 `docs/skills/skill-schedule/t893-页面.mjs`（只读当前 `dist`，产物落自家 `.scratch/t893/页面/`，未碰读数席产物）→ `t516-判据-版式.mjs --widths 390,768,1440` → 横向溢出门 `measure-responsive.mjs` 三档 33 格全绿。
- 390 档 `minFontPxNoSvg` 实测（`fontSizes` 为全页阶梯）：

| 页 | 最小值 | 责任元素 | 判定 |
|---|---|---|---|
| 周视图／复盘本周／对比两个月／对比上周和这周／对比工作日对周末／异常检测／类别深挖区间／类别深挖单日（8 页） | 12 | `p.ilife-block-page-shell-eyebrow`／`div.ilife-block-kpi-card-label`（均为 12，恰站下限） | 通过（`fontCut = 0`） |
| 复盘区间 61 天／复盘区间 7 天（2 页） | 11.5 | `span.ilife-charts-legend-item`（7 维趋势图图例，公共层） | 仍扣 5 分，根因在公共层，见 §5 与新票 |
| 区间汇总 21 天（图例对照页，非修面） | 11.5 | 同上 | 阳性对照：探针能看见 11.5，8 页的 12 不是量具失灵 |
| 全 11 页横向溢出 | 0（33 格） | — | 通过；复盘本周的 `clipped` 4 条是行列表格的省略设计位（门只看越界与图），与本次字号改动无关 |

## 5. 如实登记（本票未修的一处，影响三页）

1. 公共层 `packages/base-render/src/charts.ts:2130` 图例 `span.ilife-charts-legend-item` 11.5px：跨域共用位，本票不碰。R3 实测确认影响**三页**（不止票面的一页）：区间汇总 21 天（票面已知）＋ 复盘区间 61 天／7 天（7 维趋势图的图例；改前被 11 的行首日期掩盖——`min` 取最小值，11.5 当时不是最小故未上榜，掩盖机制见 §7）。三页仍扣 5 分，已另开公共层的票（见 issue 进度区），关闭本票后到那张票下继续跟。

## 6. 链条状态（R3 已跑完，不再是待确认）

- `fmt.json` 三档重跑：本席用真 Chrome 跑完（11 页 × 390/768/1440，读数 `.scratch/t893/fmt.json`），判据只看 390 档：8 页 12 通过，2 页 11.5（图例，§5）。
- 引擎复算：未逐页跑 `判分.mjs`，但规则是 `minFont >= 12 ? 0 : D5W.font` 二值门（源码已核），12 的 8 页 `fontCut = 0` 是算式直推，无需再跑。
- 变异自证：单元层（`dist` 拨回 11 按预期变红）＋ 量具阳性对照（图例页 11.5 被准确报出）。未做页面级变异：CSS 串逐字嵌入产物已静态核对（对比页 `.sch-an-diff-tag … 12px`、周视图 `.heat-ticks … 12px`、深挖页无窄屏 11px 覆盖），再改 `dist` 做变异会污染共用构建，收益为零。
- `copy-menu-hint` 11px 残余已关：菜单常态 `visibility: hidden`（`style.ts:555-556`），探针排除隐藏元素。
- 出页门与分隔符门：分隔符由 t789/t786 等用例覆盖（全绿）；出页门（整页／必现块／零外部引用）由 11 张自渲染页的落盘成功＋用例覆盖；三档溢出 33 格全绿。

## 7. 对抗式自查记录（第一性原理重审，R2）

R1（已提交版）漏了一处，R2 补上。重审方法：不再信票面转述，直接读探针与引擎源码。

- 独立核实下限：`PAGE_LIMITS.textMinPx = 12`（`base-render/src/pageUi.ts:51`），引擎 `fontCut = minFont >= 12 ? 0 : D5W.font`（`判分.mjs:286-287`）——二值门限，差 0.5px 也扣 5 分。
- 探针语义（`t516-判据-版式.mjs:113-134`）：量**全部非 SVG 可见自有文本**的计算字号，过滤只有空文本／`display:none`／`visibility:hidden`／透明／零高／SVG——**没有“正文类”过滤**。票面评论“`minFontPxNoSvg` 只看正文类”在代码里不存在；“刻度不在命中里”只是 `minFontOwner` 取前 3（DOM 序，行首日期排在刻度前）的采样假象。
- 推论：`.heat-ticks`（HTML `span` 自有文本“00:00”等、可见、11px）**进 `min`**，R1 修完后 6 页最小值仍是 11 → 仍扣 5 分。R1 不充分，R2 把刻度抬到 12。
- 排除清单（逐项看过，390 档）：`thead` 在 640 断点下 `display:none`（`blocks.ts:2132-2153`），`renderDataTable` 只在 `thead` 产 `th`（`:795-796`，无 `tbody` 行首 `th`）→ 表头不进 `min`；toast 系隐藏 → 不进；`help-shell-*` 只在 HELP 页；`copy-menu-hint`（11px）菜单常态隐藏 → 不进（残余不确定，见下）；图表 10/10.5px 标签若进 `min` 则改前最小值应是 10 而非 11，故未命中这 10 页（SVG／canvas 或无标签）；四卡（`renderKpiGrid`）标签 12px（`blocks.ts:1888-1891`）、事实条 12/15px（`pageShapes.ts:299-306`）→ 不进。
- 残余风险（须引擎重跑才能关）：`copy-menu-hint` 若在某页可见即 11 进 `min`；11→12 后的横向溢出（差异行栅格 `22px＋minmax(0,1fr)＋minmax(76px,auto)`、徽标 flex 无换行、雷达标签碰撞）无用例覆盖——静态估算可放行（A/B 单字符、“n 小时 m 分钟”在 76px 自适应列内），但只有三档实测能证明。验收 #3（出页门＋三档溢出）仍是 open 状态。
- 额外发现（非本票，不修）：768 档也在 820 窄查询内，改前同样中招，本次一并修好。
