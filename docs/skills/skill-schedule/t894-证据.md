# #894 证据：复盘与查日程完成状态上屏全角（取值不动）

## 改法（一处定义、别处引用）

- 定义：`packages/skill-schedule/src/policy/plan.ts` 新增 `COMPLETION_DISPLAY_LABELS` ＋ `completionLabelOf()`，`VALID_COMPLETIONS` 一字不动。
- 引用：`src/plan/planDocs.ts`（查日程逐条）、`src/plan/reviewDocs.ts`（复盘单页逐条＋分布＋讨论区六种说明）、`src/plan/replaySections.ts`（一体页对照＋分布＋跨域未执行）、`src/plan/receiptDocs.ts`（回执页完成状态同口径收敛）、`src/render/html.ts`（envelope 小块徽章）。
- 不动：`VALID_COMPLETIONS` 取值、校验、DB 载荷（`toPlanItem`）、`runReview` 回执消息、种子与历史数据。

## 改前 / 改后逐页命中数（可见文本节点，正则半角括号含中文）

量法：剥 `style` / `script` / 注释后再剥标签，取可见文本，用 `/\([^)]*[一-鿿][^)]*\)/g` 计数。

| 页 | 改前 | 改后 |
|---|---|---|
| 复盘逐条（`reviewPage`） | 6 | 0 |
| 查日程（`renderPlanDayPage`） | 1 | 0 |
| 复盘今日（`replayPage day`） | 4 | 0 |
| 复盘区间 61 天通用档（`replayPage range`） | 4 | 0 |
| 合计（本证据件最小复现 4 页） | 15 | 0 |

票面 10 页与本复现的对应：复盘三档共用同一张 `reviewPage`（讨论区那句＋逐条＋分布），复盘区间四档共用同一套 `replaySections`（对照＋分布＋跨域），查日程五种查法共用同一支 `eventRowOf`。改的都是这三支共用位，故一处好、十页一起好。

## 渲染面文字门

- 本机无真浏览器条件时，以上逐页复量即同一判据在静态面的读数（半角括号那一条归 0）。
- `node packages/base-render/scripts/check-rendered-text.mjs --dir .scratch/t792/产物` 需在有 Chrome / Edge 的机器上重跑；本批改动只动完成状态文案，不碰版式与英文裸词，预期半角那一条在本批归 0、其余命中不变。

## 变异自证

- `completionLabelOf('已完成(超时)')` → `已完成（超时）`：前者半角命中 1，后者命中 0。
- 改回（把某页的 `）` / `（` 换回 `)` / `(`）→ 复量命中回 ≥1；再改回全角 → 归 0。
- 实跑：`.scratch/t894/mutation.mjs` 输出 `MUTATION OK`。

## 回归

- 编译：`node node_modules/typescript/bin/tsc -b packages/skill-schedule --force` 通过。
- 本包全量：`node --test --test-concurrency=1 packages/skill-schedule/test/*.test.mjs` → 242 / 242 通过（含新增 `t894-完成状态全角.test.mjs` 6 项）。
- 有意保留（不算回归）：`runReview` 回执消息仍用原值拼接（如 `已完成(超时)1`），那是回执载荷不是页上文案；`SKILL.md` 与校验报错里的六态仍是原值，那是协议文档。
