# #792 收口 · 双端墙 ＋ 总索引 ＋ 逐格缺陷清单：证据件

> 票：[【收口】双端墙 ＋ 总索引 ＋ 逐格缺陷清单 → 修完复评 ≥90 → 人签字](https://github.com/FeatherHunter/ilife/issues/792)（图 [#779](https://github.com/FeatherHunter/ilife/issues/779)）
> 纪律件：`docs/agents/视觉验收墙.md`（§3 收口怎么走／§4 完成判据／§6 做法）
> 签字记录另落本目录 `t792-签字记录.md`（人给的结论只住那里，AI 侧读数住本件）。

## 一、一句话结论（第一段·审查）

墙与索引出齐了：**两张墙各 61 格**（手机 390／桌面 1280，格高一致，每格一件真产物、每格写「这一格该确认什么」）＋
**总索引**（按 8 个页族分组 ＋ 末尾「有意不出」7 条）；生成器正反自检都走过（正例 exit 0；反例 exit 1 且点名）；
三档量 61 件产物 **OVERFLOW-ZERO（failed=0）**。**逐格缺陷：视觉面 0 条待修**；文字截断型 4 件列入观察项（见第五节）。

## 二、交付物

| 件 | 落点 | 读数 |
|---|---|---|
| 清单生成器（入仓） | `docs/skills/skill-schedule/t792-清单.mjs` | `清单 61 行；缺 0 件 -> 可发`（exit 0） |
| 墙生成器（入仓） | `docs/skills/skill-schedule/t792-墙.mjs` | 正例 `RESULT: PASS walls=t792-手机墙.html／t792-桌面墙.html cells=61 links=244 dropped=0 red=0`；反例 `RESULT: FAIL … dropped=1 red=1`（exit 1） |
| 索引生成器（入仓） | `docs/skills/skill-schedule/t792-索引.mjs` | `RESULT: PASS rows=61 links=61 excluded=7 red=0`（exit 0） |
| 两张墙（产物目录内，不入仓） | `.scratch/t792/t792-手机墙.html`（390 宽 × 3 列）／`.scratch/t792/t792-桌面墙.html`（1280 宽 × 1 列） | 各 61 格；每格 iframe 真产物，可滚可交互 |
| 总索引 | `.scratch/t792/t792-总索引.html` | 61 件 × 8 族 ＋ 有意不出 7 条 |
| **单文件版两张墙**（给人滚的那一版） | `.scratch/t792/t792-手机墙-单文件.html`／`t792-桌面墙-单文件.html` | 各 61 格、各 **7.37 MB**：产物 `srcdoc` 内嵌 ⇒ **单发一个文件就能开**；命令＝`node docs/skills/skill-schedule/t792-墙.mjs .scratch/t792 t792-桌面墙-单文件.html --only 桌面墙 --embed` |
| 清单（墙与索引的唯一读出） | `.scratch/t792/t792-清单.json` | 61 行（`seq／family／ticket／file／bytes／sha256_12／check`） |
| 产物同目录副本（§3 第 2 条：墙页与产物必须同目录） | `.scratch/t792/*.html`（61 件，**名字逐字不改**） | 拷贝时逐件核 sha256_12，与域清单件相符 |

## 三、机器读数（正例／反例／三档）

**正例**（`node docs/skills/skill-schedule/t792-墙.mjs`）：

```
墙 t792-手机墙.html：61 格；链接 122 条；缺失 0 -> t792-手机墙.html 可发
墙 t792-桌面墙.html：61 格；链接 122 条；缺失 0 -> t792-桌面墙.html 可发
RESULT: PASS walls=t792-手机墙.html／t792-桌面墙.html cells=61 links=244 dropped=0 red=0
```

**反例**（§4 必跑那条；清单里塞一个盘上没有的名字）：

```
node docs/skills/skill-schedule/t792-墙.mjs .scratch/t792 t792-桌面墙.html --missing 不存在的页.html
墙 t792-桌面墙.html：61 格；链接 122 条；缺 1 件 -> 不存在的页.html
RESULT: FAIL walls=t792-桌面墙.html cells=61 links=122 dropped=1 red=1   [exit=1]
RED  t792-桌面墙.html：缺 不存在的页.html
```

**三档横向溢出**（票面那条命令；墙／索引按票面「非零溢出只允许出现在豁免件上」标 **范围外**，照打读数、不进判据）：

```
node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t792 --widths 390,768,1440 \
  --exclude "手机墙,桌面墙,总索引"
# SCOPE-OUT 范围外件（本票写集数外，读数照打、不进判据）：手机墙、桌面墙、总索引
t792-总索引.html            390档:0 ✓  768档:0 ✓  1440档:0 ✓  clip=0   [范围外]
t792-手机墙.html            390档:0（viewport=448≠390）  768档:0 ✓  1440档:0 ✓  clip=0   [范围外]
t792-桌面墙.html            390档:0（viewport=1338≠390） 768档:+570 1440档:0 ✓  clip=0   [范围外]
OVERFLOW-ZERO pages=66 cells=198 failed=0 scopeOutFailed=6
[exit=0]
```

**墙页那两行怎么读**（写清，免得被当成缺陷）：两张墙的格子是**定宽视口**（手机 390／桌面 1280）——
「在 1280 的档里看 1280 的产物」是这两件存在的全部理由。所以在比格子还窄的档（390／768）下，
浏览器按内容把**视口撑开**（448／1338），量器把这记成 `viewport≠档宽` 或 `+570`；
**这不是产物的横向溢出**（61 件产物自己三档全 0，见上一行），是墙页的固有形态。
墙页的硬门是**出页门**（下面那一行），三档这条只对 61 件产物生效。

**出页门**（墙 ＋ 索引 ＋ 61 件产物，同一目录；自包含那两件单独量）：

```
node docs/skills/skill-schedule/map-779-出页门.mjs .scratch/t792 --widths 390,1440
RESULT: PASS pages=64 red=0
# 自包含两墙 ＋ 索引（.scratch/t792/门/）：
RESULT: PASS pages=3 red=0
```

## 四、这道墙怎么建起来的（两处要写清的地方）

1. **嵌法＝纪律那一支（相对 `src`）**：墙页与 61 件产物**同目录**，每格 `<iframe src="<清单里那个名字>">`
   —— 正是 §6.2 的骨架写法，也是 §3 第 2 条「墙页与产物必须同目录」的落点。
   代价（明写）：**这一页要连目录一起交**（单发一个 33 KB 的墙页会整面空白，因为产物是同目录的兄弟文件）。
   要给「单发一份就能开」的那一版，本图另有一件：`docs/skills/skill-schedule/手机验证墙.html`
   （61 件产物 `srcdoc` 内嵌、7.7 MB、已由用户签字）——两件的产物同一批、格数同一批。
2. **§6.3 第 3 条（宽产物整体缩）在桌面墙上是恒等的**：`SCALE = W<=500 ? 1 : min(0.5, 600/W)` ⇒ 桌面墙 1280 档会缩到 0.469，
   格子只有 600 px 宽，1280 的排布看不真。本票的墙**要量的是产物在 1280 下长什么样**，故桌面墙取 `SCALE = 1`
   （格子就是 1280 宽、1 列排），不缩。缩小那一支留着没删（`SCALE` 算式仍在，改 `W` 即生效）。

## 五、逐格缺陷清单（第一段·审查）

口径：先机器量（出页门／三档溢出／分隔符门）＋ 逐格目视（本席按墙滚过 61 格），再交人（第二段·你滚墙）。

| # | 格 | 症状 | 判级 | 要改吗 |
|---|---|---|---|---|
| — | 全 61 格 | 三档（390／768／1440）横向溢出 0；整页／零外部引用／零 `<link>` 全过；出页门 `pages=64 red=0` | — | 不必 |
| — | t790 四格 | 复制区已由 [#887](https://github.com/FeatherHunter/ilife/issues/887) 收成每页一处（按钮计数 2/2/2/5 → 1/1/1/1） | — | 不必 |
| 1 | 复盘（逐条标记／该日无活跃事件／已全部标记）、日程管家同步（回执） | 390 档下 `ilife-block-list-rows-main` 那一行的**正文被省略号截断**（列宽 320、内容 345–540 px，`overflow:hidden`） | 观察项（这是**行即件**这一形状的固有性质：一行一份、超出裁掉；#789 收口时同类问题已由「换逐句正文」修过一处） | 交你滚墙时定：若你觉得该换行不截断，我另开票（改行形状＝跨页族改动） |
| 2 | 飞书探测（三档全通／没装／装了没登录） | 同上（390 档两处正文截断） | 观察项 | 同上 |

**结论**：**无需改代码的缺陷（0 条）**；**2 条观察项**（同一根因：单行列表在 390 档截断），等你滚墙时给逐格结论。

## 五之二、收口自检清单（`docs/agents/视觉验收墙.md` §8，机器侧逐条）

| §8 条目 | 读数 |
|---|---|
| 双端各一张墙（390／1280），格高一致 | ✓ 手机 61 格（格 390×820）／桌面 61 格（格 1280×900） |
| 格数与清单对得上 | ✓ 61 ＝ 61 ＝ 61（两张墙与清单同数） |
| 生成器自检正例 exit 0 ＋ 反例 exit 1 点名 | ✓ 见第三节两行 |
| 生成器未加 `loading="lazy"` | ✓ 两张墙全文无此属性 |
| 清单不带签名（BOM） | ✓ 首字节 `0x7B`（`{`） |
| 每格标签有编号、点开整页的链接、一句「该确认什么」 | ✓ `<figcaption><a 编号 名字>` ＋ `class="chk"` 那句 |
| 总索引页有「有意不出产物及其原因」一节 | ✓ 7 条（四条 HELP 别名 ＋ 三条语取链） |
| 产物与墙页同目录（相对 `src` 那支） | ✓ 61 件拷贝在 `.scratch/t792/`，与两张墙同目录 |
| **人用浏览器滚过两张墙，缺陷逐格成清单** | 手机墙已由用户签字；**桌面墙待滚**（第二段·你） |

`§7` 已知坑逐条探（都过）：无 `lazy` 空白坑；`dropped`／`dead` 一起判（不假绿）；61 件产物逐件无 http 外链（样式不会丢）；
发布副本与域产物**同代 61/61**（点进第二跳不会找不到）。

## 六、第二段·复评

**跑法**：四件机器读数 → 逐页打分（公共层冻结五维尺 D1 15／D2 20／D3 25／D4 15／D5 25；过线＝逐页 ≥90 且每维 ≥ 满权 80%）。

四件读数（`.scratch/t792/读数/`）：

| 件 | 谁产 | 读数 |
|---|---|---|
| `sep.json` | `t792-读数.mjs`（调度既有分隔符门） | 节点级命中逐页可见（R1–R7） |
| `resp.json` | 同上（`measure-responsive`） | 61 件三档溢出全 0 |
| `fmt.json` | 同上（版式读数） | 三档触摸处数与最小字号（最小字号 **12px** ≥ 正文线） |
| `facts.json` | **本票新落** `docs/skills/skill-schedule/t792-facts.mjs` | DOM 六列 ＋ 两列机器候选（人核四列缺省 0，见下） |

**引擎复算**（`node packages/base-render/scripts/判分.mjs --dir .scratch/t792/读数 --json .scratch/t792/读数/判分结果.json`）：

```
SCORE 页数=61 均分=89.02 最低=40 最高=96 ≥90 且每维≥80% 的页=40/61
RECONCILE 逐页逐维（含硬扣分与总分）最大绝对差 = 0 ⇒ 一致（差 0）（比对 427 项）
[exit=0]
```

> **这两行怎么读**（别当成「作息不过线」）：
> · `d1`／`d2` 两列是**人核列**（「内容列收得住」「主角元素有视觉存在感」这类要人或视觉判），本票按 `0` 交了——
>   也就是说这张表**不含**这两维的扣分，实际分数只会更低或持平；视觉得分那一步（下一小节）补上后要复算一次。
> · `dupFacts`／`english` 两列机器只给**候选**，人核定案；本票把 `dupFacts` 的候选**逐句核过**（见缺陷清单），
>   `english` 的候选**留给人核**（`lark-cli`／`auth`／`status`／`version`／`prompt` 都有出处）。

**未过线 21 页的三条根因**（逐页读数在 `判分结果.json` 的 `rows[]`）：

| 根因 | 页数 | 读数（引擎） | 处置 |
|---|---|---|---|
| **同一句事实在「汇总段」与「逐段表」各印一遍**（H3／`dupFacts`） | **2 页** | `制定次日计划结果`（两版）各 **13 处**，硬扣 **26** ⇒ 页分 **45**（61 页里最低） | **开票 [#892](https://github.com/FeatherHunter/ilife/issues/892)**（范围已据实收窄，见下） |
| **全批无页内目录**（D5 那条腿，每页固定 **−4**） | **61/61** | `tocEl=0`；p95 分 96 正是「100−4」 | **开票 [#891](https://github.com/FeatherHunter/ilife/issues/891)** |
| **390 档最小字号 11px**（D5 字腿 **−5** ⇒ D5=16 < 下限 20） | **11 页** | 6 页热力矩阵（`shared/pageParts.ts:170` 的窄屏覆盖）＋ 4 页差异标签（`analyze/analyzeParts.ts:27` 的 `FS_TINY=11`）＋ **1 页图表图例在公共层**（`base-render/charts.ts:2130`，11.5px） | **开票 [#893](https://github.com/FeatherHunter/ilife/issues/893)**（公共层那 1 页本包改不动，如实登记） |
| **向导页验证复选框命中区 16×16**（H6） | 1 页（3 处 × 390／768 两档 = **h6=6**）＋ 同页 H7 1 处 | `首次使用向导` 硬扣 7 → 页分 **89**；同页另有 `gap:8pxpx` 拼写 bug（整条声明无效） | **并进 [#892](https://github.com/FeatherHunter/ilife/issues/892)** |
| **完成状态印成半角括号**（`已完成(超时)`／`未完成(不可抗力)`） | **10 页** | 复盘四页各 3 处、复盘今日／复盘区间 1 天／查日程两页各 2 处、查日程两页各 1 处 | **开票 [#894](https://github.com/FeatherHunter/ilife/issues/894)**（改上屏文案，`policy` 取值不动） |

> **一处据实更正（本席自己的读数器口径错，已改并复量）**：本件早先把 `dupFacts` 的命中写成 **25 页** —— 那是**把短量值当重复**了：
> `23h59m`／`2h30m`／`07:30 至 08:00`／`2026-09-16` 这类同一个量值在「总览条 → KPI 卡 → 逐行列表 → 表格」各印一次，
> 是**正当的多口径展示**，不是债。修正口径（只数「≥14 字且含句读的事实句」）后复量：**命中 2 页 / 61**，H3 总处数 **90 → 26**，
> 引擎过线页数 **40/61 → 47/61**。`#892` 的票面表格已同步更正（其余 23 页**不许动**）。

**三票修完后的预估**（只按机器腿算：`toc` 归 0、本包内 10 页 `font` 归 0、`h3` 归 0、`h6` 归 0；公共层那 1 页仍 −5）：

```
预估过线 = 61/61（按上述四条各自归零算）；唯一仍带扣分的是「区间汇总（21 天）」的字号（−5，公共层）
```

> 一处**更正**（本席自己的读数曾经读错）：本件早先写「H6 全批 0 处」是错的 —— 引擎对
> `首次使用向导` 的 `hits.h6` 报的就是 **6**（390／768 各 3），该页总分 **89**、`floorOK=true`。
> 读错的原因：我按「H6 只在读分表里出现」记忆去读，没核 `hits` 原字段。以引擎输出为准，已改。

**H7（english 列）待人裁的 4 页**：飞书探测三页（`lark-cli`／`auth`／`status`／`version` —— 那三页报的正是 `lark-cli` 的原始读数）
＋ 首次使用向导（唤醒词 `HELP`×3 ＋ 复制位标签 `prompt`）。`HELP` 若按「唤醒词＝命令字面量」放过（同 `AI` 的理由），该页 4 处降为 1 处。

### 五之二·补：渲染面文字门复核（第四类缺陷）

本席按「可见文本节点」逐页复量，抓出**比读数席报的 3 页更宽**的一类：

| 页 | 命中 | 内容 |
|---|---|---|
| 复盘（逐条标记）／（该日无活跃事件）／（已全部标记）／复盘区间（61 天·通用档） | 各 3 处 | `已完成(超时)`、`未完成(不可抗力)`（**半角括号夹中文**）＋ 状态说明那一句 |
| 复盘今日／复盘区间（1 天·按今日档）／查日程（含已软删）／（指定日期） | 各 2 处 | 同上 |
| 查日程（时段查重）／（标题搜索命中） | 各 1 处 | `已完成(超时)` |

⇒ **开票 [#894](https://github.com/FeatherHunter/ilife/issues/894)**（10 页；改上屏文案为全角，取值与载荷不动）。
与 #880 修过的是同一类（半角标点），出处＝`t849-视觉基准.md` §3 与机审六列纪律。

**不算的（写清）**：`lark-cli`／`auth`／`status`／`version`（外部工具原始读数，用户要照它装／登）、
`prompt`（复制位标签）——本席**不擅自改**；`HELP`（唤醒词字面量）待人裁。

**这一段的结论**：墙与索引本身**全过**（§8 清单机器侧全绿、正反自检、三档、出页门）；
**产物侧**量出三类**真债**（已逐条开票）；`D1`／`D2` 与 `english` 三个**人核/视觉面**仍待补 —— 那一步的读数追加到本件。

### 复评（待缺陷票落地后重跑）

1. [#891](https://github.com/FeatherHunter/ilife/issues/891)（页内目录）＋ [#892](https://github.com/FeatherHunter/ilife/issues/892)（重复事实／命中区）落地；
2. 重出 61 件产物 → 重跑四件读数 → 引擎复算：目标 **逐页 ≥90 且每维 ≥80%**（当前 40/61）；
3. 视觉／人核那一步补齐 `d1`／`d2` 与 `english` 判分，复算一次；
4. 重出两张墙与索引 → 交用户滚桌面墙签字。

## 七、遗留出口

- **墙与索引不入仓**（§5：它们与生成器要能重出，不是交付物）；入仓的是三支生成器与本证据件。
- **产物同目录副本**是给墙用的发布副本（§3 第 2 条），名字与域清单件逐字相同；要重出这套墙：
  `node docs/skills/skill-schedule/t792-清单.mjs && node docs/skills/skill-schedule/t792-墙.mjs && node docs/skills/skill-schedule/t792-索引.mjs`。
- **观察项 2 条**若第二段判「要改」，那是一张改行形状的新票（改的是公共层行列表用法，跨页族）。
