# t892 证据（作息页事实重复＋向导复选框命中区＋gap 拼写）

> 范围以 #892 第二条评论为准（收窄后）：真缺陷只有两页结果页（同一产状、同一修复点）＋向导 H6＋`pxpx`。
> 其余 23 页短量值重复是正当多口径展示，一律没动。

## 一、改动（commit 59ca3dc3，3 件）

- `packages/skill-schedule/src/plan/discussDocs.ts`（`plan` 能力）：
  结果页「历史贴合提示」汇总块去掉逐条 `lines`（与下表「说明」列逐字重复的 13 句），
  汇总只留结论（贴合／偏离／无参考计数），偏离时加一句「逐段原因见下表「逐段贴合」」指到明细。
  同一句话只出现在一处；「偏离警示」块（时段＋标题）与逐段表原样保留。
- `packages/skill-schedule/src/admin/adminParts.ts`（`admin` 能力）：
  复选框 `input` 16×16 → 44×44（`HIT=44`，注释写清与 `PAGE_LIMITS.touchMinPx` 同值是刻意的，
  本件不引区块层内部件，沿 `analyzeParts.ts` 的 `FS_TINY`先例），行加 `min-height:44px` 并垂直居中；
  `gap:8pxpx`（`px(GAP)+'px'` 与自带单位的 `px()` 撞车，整条声明被浏览器丢弃）修成 `gap:8px`，删掉失效的 `BOX`。
- `packages/skill-schedule/test/t892-重复与命中.test.mjs`（新增回归）：
  V1 用 `t792-facts.mjs` 同口径（≥14 字＋含句读＋含数字，年月日／冒号）对真出口产物判零重复；
  V2 断言复选框 44×44 与行高 44；V3 断言样式无 `pxpx` 且 `gap:8px`。

公开接口一律没变（`resultPage`／`adminPartsCss`／`renderVerifyList` 签名与形状不变，只改输出串）。

## 二、改前 → 改后读数

### dupFacts（`t792-facts.mjs` 同口径）

| 页 | 改前（`.scratch/t792/产物`） | 改后（`.scratch/t892/产物`） |
|---|---|---|
| 制定次日计划结果 | dupFacts=13 | dupFacts=0 |
| 制定次日计划结果（调整后再生成） | dupFacts=13 | dupFacts=0 |
| 其余 59 页 | 0（没动，复量仍 0） | 0 |
| 候选总和 | 26 | **0** |

### 判分引擎（`packages/base-render/scripts/判分.mjs`，自证差 0）

| 页 | 改前 total／d3／h3 | 改后 total／d3／h3 |
|---|---|---|
| 制定次日计划结果 | 49／0／13（hard 26） | **100／25／0**（hard 0） |
| 制定次日计划结果（调整后再生成） | 49／0／13（hard 26） | **100／25／0**（hard 0） |
| 首次使用向导 | 93／25／h6=6（hard 7） | **99／25／h6=0**（hard 1，见下） |

- 改后全批：均分 98.25，最低 91（周视图，D5 预先存在，与本票无关），`RECONCILE 差 0`（427 项）。
- 向导剩 hard=1 是 H7 `english=1`，属票面「遗留出口」（有出处，留给人核），本票不碰。

### 命中区（`fmt.json`，`首次使用向导`）

| 档 | 改前 `touchSmall`／`touchSmallList` | 改后 |
|---|---|---|
| 390 | 3／`input.@16x16` ×3 | **0／空** |
| 768 | 3／`input.@16x16` ×3 | **0／空** |
| 1440 | 3／`input.@16x16` ×3（引擎不计数） | **0／空** |
| 引擎 `h6` | 6（390＋768） | **0** |

### `pxpx`

- 改前：`.scratch/t792/产物/首次使用向导.html` 有 `gap:8pxpx`（整条间隙声明无效）。
- 改后：`.scratch/t892/产物/` 61 件全文搜 `pxpx` → **0 命中**。

## 三、变异自证（两行）

- 把收掉的那句加回一处（`制定次日计划结果.html` 末尾追加
  `与历史不同：该时段近几天主要是「维持.用餐」（7 次），这一条排的是「健康.冥想」。`）→
  `t792-facts.mjs` 复量 `dupFacts=1`（命中，D3 按 H3=2 掉分）。
- 去掉 → `dupFacts=0`，引擎 `h3=0`、`d3=25`。

## 四、不回归

- 本包全量用例：**242／242 绿**（含新增 `t892` V1–V3 与既有 `t787` V7／`t790` V3，一次跑通）。
- `t787` 探针：**PASS**（11 页；出页门 11／11；分隔符门 11／11；`#783–#786` 别域产物逐字节不变）。
- `t790` 探针：功能全绿（必现块、出页门 4／4、分隔符门 4／4、三档溢出 `OVERFLOW-ZERO`、
  `#783–#789` 别域产物逐字节不变）；两条红都是并发别席的门，不是本票：
  ① 编译态基线（`t790` 收口指纹 vs 本趟 `src=fc960c9b／dist=4700ea11`，本票改动在内，用 `--no-baseline` 重跑后功能全绿）；
  ② 快照门（`src/plan/handlers.ts` 是 #896 未提交的改动，`dist` 自然早于它；本票没碰它，不代编）。
- `t792-读数.mjs`（`.scratch/t892/产物`）：sep `61/61 PASS`、resp `OVERFLOW-ZERO pages=61`、
  fmt `61/61`、facts `dupFacts候选=0`。
- 其余六支域探针（`t783／t784／t785／t786／t788／t789`，`--allow-dirty --no-baseline`）：
  **逐支 PASS 且 red=0**（t783 7页／t784 6页／t785 5页／t786 9页／t788 13页／t789 7页），
  日志见 `.scratch/t892/probes.log`。连同 `t787 PASS` 与 `t790` 功能全绿，八支域探针无一功能红。
- 指纹：`src-sha=fc960c9b0eb898f4 dist-sha=4700ea11e7c27a05`（`t787／t790` 两探针一致）。

## 五、待确认（故本票只到 95%，不 close）

1. 人核看一眼改后两页结果页（汇总结论＋「逐段原因见下表」＋逐段表）信息分工是否顺眼；
2. 向导 44px 复选框视觉是否可接受（满足触摸档的最小改法；要小视觉需另起自定义样式票）；
3. 八支域探针终态：`t783／t784／t785／t786／t787／t788／t789` 逐支 PASS（red=0），
   `t790` 功能全绿、仅两条并发别席门红（已在 §四归因，不属本票）。

## 六、对抗式审查记录（第一性原理自查）

1. **workspace 新鲜度**：`.scratch/t892/产物` ＝ `t792` 基线 61 件＋本票重出的 `t787` 11 件＋`t790` 4 件（同名覆盖，件数保持 61）。
   后跑的六支探针重写了各自成品：`t783` 7 件与 workspace 字节不同——逐字节 diff 证实差的是别席 `#893` 未提交改动
   （`shared/pageParts.ts` 删 `.heat-day／.heat-sum 11px`）＋各跑时间戳／编号，不涉及本票两页。
   八支探针指纹同为 `src=fc960c9b／dist=4700ea11`（单一 dist，前后一致），本票验收结论不受影响；
   `t789` 等热力页的 D5 读数在 `#893` 落定后会更优（11px 债被它清掉），与本票无关。
2. **V1 是否空转**：非空。种子使 11:30 那格与 11 点历史撞小时 → drift≥1（改前 `lines` 非空，改后指引句出现），
   改前必红；旧产物 13 处证实同口径杀伤力。src 级变异（改回 `lines` 跑红）没做——`stash` 被并发纪律禁了，
   如实记录，未冒充。
3. **是否在刷指标**：否。删掉的是逐字相同的 13 句（信息零损失，计数结论保留）；新增指引句无数字，
   按判定口径天然不计数，且它是导航信息、不是事实复述。
4. **44px 视觉**：本席没亲眼看到（视觉后端本轮不可用，截图在 `.scratch/t790/成品/首次使用向导__截图__390.png` 可自看）。
   静态推算：44px 框配 13px 字，框面积 ≈ 行高的 3 倍，**预计偏大但不owa错位**（flex 居中＋行高托住，溢出 0 已证）。
   是否接受仍由人定（待确认 2）。
5. **边界**：drift=0／无历史档输出与改前逐字节同义（`lines=[]` vs 无 `lines` 字段，渲染同空）；
   偏离警示（时段＋标题）与表格单元格粒度不同，复量 dupFacts=0 证实无残留。
6. **git 卫生**：两次提交都只含自己声明的路径（`git diff --cached --name-only` 复核），无切分支／stash／reset；
   编译测试探针全经 `run-locked` 排队（探针按自家票号持锁）。

1. 人核看一眼改后两页结果页（汇总结论＋「逐段原因见下表」＋逐段表）信息分工是否顺眼；
2. 向导 44px 复选框视觉是否可接受（满足触摸档的最小改法；要小视觉需另起自定义样式票）；
3. 八支域探针终态：`t783／t784／t785／t786／t787／t788／t789` 逐支 PASS（red=0），
   `t790` 功能全绿、仅两条并发别席门红（已在 §四归因，不属本票）。
