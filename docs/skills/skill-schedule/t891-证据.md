# t891 证据 · 作息 61 页补页内目录与定位避让（D5「页内定位」那条腿回到 0 扣分）

> 票：[#891](https://github.com/FeatherHunter/ilife/issues/891)（Part of [#792](https://github.com/FeatherHunter/ilife/issues/792) 收口第二段量出的三条真债之一）。
> 口径源：`docs/skills/skill-memo-ilife/t849-视觉基准.md` §2「页内定位」（长页必须有页内目录或锚点；带标识的区块吃定位避让）
> ＋ 判分引擎 `packages/base-render/scripts/判分.mjs` 的 D5 那条腿（`tocCut = (tocEl > 0 && scrollMargin > 0) ? 0 : 4`）。
> 本件里的读数全部可复算：命令逐条写在对应小节里。

---

## 一、做了什么（一句话 ＋ 三处落点）

作息 61 件产物改前**一件都没有页内目录**（`tocEl` 合计 0），D5 每条腿固定扣 4 分。本票给**长页**在页首装上
公共层的页内目录、给每一小节的段名装上**锚点**（避让仍走公共层那份配方），短页照票面允许不出目录。

| 落点 | 件 | 干什么 |
|---|---|---|
| 判据 ＋ 目录装配（唯一一处） | `packages/skill-schedule/src/shared/pageNav.ts`（新件） | `SECTION_TOC_MIN = 3`（小节数 ≥ 3 出目录）；`pageSections()` 逐段包 `<section id="sec-N">` 并从**同一份清单**派生 `renderTocBlock` |
| 页型件（7 张） | `src/shared/{day,plan,week,range,overview,detail}Page.ts` ＋ `docPage.ts`（未动） | 件序列原样，只把每一段标成「有段名／无段名」两态 |
| 各能力目录的页装配处 | `src/{write,plan,query,analyze,admin}/**` 共 25 处 | 同上（每处只加窄口，不动口径与件序列） |

**不自造控件**：目录块走公共层 `renderTocBlock`（`packages/base-render/src/blocks.ts`），与卡路里／备忘录大型页同一件。
**不另写避让**：`scroll-margin-top: 20px` 由公共层页面配方 `pageUiCss()` ③ 给（`packages/base-render/src/pageUi.ts`
的 `.ilife-page-ui .ilife-block-page-shell-body > *[id]`）——本票一行 CSS 都没写，锚点 id 落在**页壳正文的直接子件**上正是为了吃那一条。

## 二、判据（一处定义，逐页可复算）

**小节**＝页内一颗 `<h2>` 段名所领的那一段（与 #891 票面现状表的读数逐字同源：今天总结 3 ／ 复盘本周 9 ／ 首次使用向导 7，
三处都是「页内 `<h2>` 数」）。**长页**＝小节数 ≥ 3（`SECTION_TOC_MIN`）⇒ 页首出目录；**短页**＝小节数 ≤ 2 ⇒ 可以不出。
判据只住 `src/shared/pageNav.ts` 一处；「这一页被谁判为短页」写在**页型件自己的件头**（例：`shared/planPage.ts`
「本页小节恒 2 颗 ⇒ 判为短页」、`src/write/writeDocs.ts`「写作息摘要回执恒 2 小节」、`src/plan/receiptDocs.ts`
「批量补计划回执恒 2 小节」、`shared/weekPage.ts`、`shared/overviewPage.ts`「一天那一档 2 小节»）。

**逐页长短清单**（61 件，出自 `.scratch/t891/交卷读数.json`）：

- **长页 50 件**（出目录，`d5.parts.toc = 0`，逐页列出）：今天总结（指定日期）、今天总结（无记录）、今天总结（昨日）、
  今天总结（满 24h）、今天总结（老侧空日示例）、作息详情（按 ID）、作息详情（按日）、作息详情（推理链）、修正作息回执、
  初始化回执（已就绪）、初始化回执（新建）、初始化回执（有数据）、删计划回执（软删）、制定次日计划结果、
  制定次日计划结果（无历史参考）、制定次日计划结果（调整后再生成）、区间汇总（21 天）、区间汇总（本周）、
  商量计划预览（过程型）、复盘今日、复盘区间（1 天·按今日档）、复盘区间（61 天·通用档）、复盘区间（7 天·按本周档）、
  复盘本周、复盘本月、复盘（已全部标记）、复盘（该日无活跃事件）、复盘（逐条标记）、对比两个月、对比（上周和这周）、
  对比（工作日对周末）、异常检测（30 天）、异常检测（7 天）、批量导入回执（部分成）、改计划回执（只改完成状态）、
  改计划回执（改时段）、日程管家同步（回执）、查多日计划（3 天）、类别深挖（区间）、类别深挖（单日）、
  补计划回执（含备注·远端没成）、补计划回执（幂等命中）、补计划回执（新建）、记作息结果（JSON 一条）、
  记作息结果（单条）、记作息结果（只记一级）、飞书探测（三档全通）、飞书探测（没装）、飞书探测（装了没登录）、首次使用向导。
- **短页 11 件**（小节 ≤ 2，不出目录，D5 那条腿照旧扣 4，逐页列出）：24h 概览（1 天）、写作息摘要回执、周视图、
  批量导入回执、查日程（今日）、查日程（含已软删）、查日程（指定日期）、查日程（时段查重）、查日程（标题搜索命中）、
  查日程（标题搜索零命中）、补计划回执（多天批量）。

## 三、逐页读数（改前／改后 ＋ 判分复算）

四件读数由 `t792-读数.mjs` 现产、facts 由 `t792-facts.mjs` 现产，判分走公共层引擎：

```sh
node docs/skills/skill-schedule/t792-读数.mjs --pages .scratch/t891/产物 --readings .scratch/t891/读数 --no-facts
node docs/skills/skill-schedule/t792-facts.mjs --pages .scratch/t891/产物 --out .scratch/t891/读数/facts.json
node packages/base-render/scripts/判分.mjs --dir .scratch/t891/读数 --json .scratch/t891/判分-后.json
```

`facts 落盘 …：页=61；tocEl=50 scrollMargin=61 … dupFacts候选=26`（改前同一读数器对**同批产物**量的是 `tocEl=0`）。

| 读数 | 改前 | 改后 |
|---|---|---|
| `tocEl > 0` 的页 | **0 / 61** | **50 / 61**（＝长页数，逐页见上） |
| `scrollMargin > 0` 的页 | 61 / 61（只有样式表里那一条声明） | 61 / 61（长页 50 条锚点外壳 ＋ 同一条声明） |
| 判分 D5 那条腿 `toc` | 61 页各 **4** | 长页 **50 页全 0**；短页 11 页照旧 4 |
| 判分均分 ／ 过线页 | 93.20 ／ 47（同批抽掉目录的那一份） | **96.48 ／ 58** |
| 判分最高页分 | 96（＝100−4，正是票面那条观察） | **100** |
| `dupFacts` 候选 | 26 | **26**（增 0：目录项文案与段名同源，不当重复事实） |

> 「改前」这一列是**同一批 61 件产物**先把目录块抽掉再量的（`node .scratch/t891/全批抽掉.mjs` ＋ 同一条 facts／判分命令，
> 落点 `.scratch/t891/判分-改前.json`）——不是拿 #792 那一份旧快照对账，两侧同代、逐页可对。
> #792 记录的原始基线（`.scratch/t792/读数/判分结果.json`，均分 89.02／过线 40）另记一处：那份读的是 `.scratch/t792/产物`
> 快照，其中 15 件比当刻产物旧一代（**我没碰那份目录**，见第六节）。

引擎自证与读数一致：`RECONCILE 逐页逐维（含硬扣分与总分）最大绝对差 = 0 ⇒ 一致（差 0）（比对 427 项）`。

**目录项文案一条如实记**：目录项文本＝该小节段名，**唯一一处例外**是详情页每条记录的段名
（`记录号 919：2026-09-15 00:00 至 06:30 睡眠` —— 它本身就是一句带读数的事实句）：这一类收成 `记录号 919`，
读数留在大段名上。为什么只收这一处：读数器的 `dupFacts` 启发式（当刻口径：≥14 字、含数字、并含 `：` 或 `。`）
会把「目录 ＋ 段名逐字同字」算成「同一句事实印两遍」——按当刻口径量，这类命中**共 +19 处，全部出在三张详情页上**
（26 → 45），其余 58 页逐字同字一处不增。收成记录号之后全批 `dupFacts` 仍是 26，与改前逐页相同。

## 四、变异自证（两行读数，跑的是真判分引擎）

抽掉「今天总结（满 24h）」里那一段页内目录（129 字节，其余一字不动），四件读数只换 facts：

```sh
node .scratch/t891/变异-抽目录.mjs                       # 造变异件：抽掉 / 加回两份
node docs/skills/skill-schedule/t792-facts.mjs --pages ".scratch/t891/变异/抽掉/产物" --out ".scratch/t891/变异/抽掉/读数/facts.json"
node packages/base-render/scripts/判分.mjs --dir ".scratch/t891/变异/抽掉" --json .scratch/t891/判分-变异-抽掉.json
```

```
抽掉（变异那一份）： toc 扣分=4  页分=96（D5 21）
加回（交付那一份）： toc 扣分=0  页分=100（D5 25）
```

同一个锚点件的三份 facts 读数：原产物 `tocEl=1` → 抽掉 `tocEl=0` → 加回 `tocEl=1`（加回那一份与原产物**逐字节同**）。
其余 60 页逐页页分不变（脚本已核）。

## 五、四道门 ＋ 七支域探针

**四道门**（逐支探针内部跑的就是它们，输出逐行见 `.scratch/t891/探针-重出2.log`）：

| 门 | 命令形态 | 读数 |
|---|---|---|
| 出页门（整页／零外部引用／三档零溢出／列宽） | `node docs/skills/skill-schedule/map-779-出页门.mjs <产物目录>` | 八支逐支 `RESULT: PASS pages=… red=0`（7／6／5／9／11／13／7／4） |
| 分隔符门 | `node packages/skill-calorie/scripts/audit-separators.mjs --dir <页群目录>` | 八支逐支 `RESULT: N/N`（7／6／5／9／11／13／7／4），61 件合计 `61/61 PASS` |
| 三档溢出 | `measure-responsive --widths 390,768,1440` | `OVERFLOW-ZERO pages=61 cells=183 failed=0` |
| 版式读数 | `docs/skills/skill-calorie/t516-判据-版式.mjs` | `61/61 件读数完成（四档=390/768/1440）`；三档触摸不足处合计 **3 处**（＝首次使用向导那 3 个验证复选框，改前改后同数）、390 档最小字号 **11px**（11 页，**与改前逐页相同**）、溢出格 0 |

**七支域探针**（各支用它自己的票号进锁，`runId` 逐支点名）：

| 支 | RESULT 行 | GATE-RUN runId |
|---|---|---|
| t783 | `RESULT: PASS pages=7 scenes=14 red=0` | `c7d2126d-79c5-45c9-9556-5e549fc5c5d5` |
| t784 | `RESULT: PASS pages=6 scenes=11 red=0` | `38e57566-948a-4604-b755-0b6df6a58740` |
| t785 | `RESULT: PASS pages=5 scenes=8 red=0` | `7a531e3c-8a8a-4f48-8113-c3b781066dc8` |
| t786 | `RESULT: PASS pages=9 scenes=9 red=0` | `6c4c46a9-ffeb-420e-9204-d10917754f16` |
| t787 | `RESULT: PASS pages=11 scenes=19 red=0` | `0ef0c3bd-d698-4143-b905-bbbe65045ba6` |
| t788 | `RESULT: PASS pages=13 scenes=9 red=0` | `0b3bc706-5bc4-4788-8e95-70522e25bb62` |
| t789 | `RESULT: PASS pages=7 scenes=12 red=0` | `6dac75f7-e3c4-4e36-9b48-ea1ce2ae62e1` |
| t790 | `RESULT: PASS pages=4 scenes=3 red=0` | `0374e15b-d49e-43aa-bbbf-edfe557d1f4a` |

八支逐支 exit 0，命令：`node tooling/run-locked.mjs --ticket <该支票号> -- node docs/skills/skill-schedule/t78x-探针.mjs`。
**编译态锚同批推**（票面验收 5 的原话）：本包 `src`／`dist` 两枚指纹因本票改动而漂，按 #888 的做法推到当刻值
（`node docs/agents/t540-指纹绑定.mjs --scope packages/skill-schedule` ⇒ `src=5f5ed35549bbe1f0 dist=42024e47b554d74f`），
七支里 t784／t785／t786／t788／t789／t790 六支的 `FROZEN_FINGERPRINT` 逐支替换成这两枚；
**t787 的锚原本是空串，按票面要求不补新判据**（它那一支照旧走「首跑未写锚」那一句）。

**本包全量用例**：`node --test --test-concurrency=1 packages/skill-schedule/test/*.test.mjs` ⇒ `tests 230 ／ pass 230 ／ fail 0`
（含本票新落的 `test/t891-页内定位.test.mjs` 12 条）。

## 六、逐条验收自查（票面 1–6）

| 票面 | 自查 |
|---|---|
| 1 · `t792-facts.mjs` 重跑，`tocEl > 0` 的页数 ＝ 长页数（逐页列出），且这些页 `scrollMargin > 0` | ✓ 50 ＝ 50（逐页在 `交卷读数.json` 与第二节）；50/50 页 `scrollMargin > 0`，61/61 页亦然 |
| 2 · 判分复算：长页的 `toc` 那一项为 0（`--json` 落点里逐页可读） | ✓ `判分-后.json` 的 `rows[].detail.d5.parts.toc`：50 个长页全 0；11 个短页为 4（票面允许短页不出） |
| 3 · 出页门 ＋ 分隔符门 ＋ 三档溢出仍全绿（不许新债） | ✓ 见第五节；另：`dupFacts` 26 → 26、`english` 8 → 8、390 档最小字号逐页相同、触摸不足处 3 → 3（都是既有那 3 处） |
| 4 · 变异自证：抽掉某页目录 ⇒ 该页 `toc` 扣分变 4；加回 ⇒ 回 0 | ✓ 见第四节（96 → 100；`tocEl` 1 → 0 → 1） |
| 5 · 本包全量用例全绿；七支探针重跑后逐支 exit 0（基线漂了同批推） | ✓ 230/230；八支 exit 0 ＋ 六支锚已推（见第五节） |
| 6 · 证据件写：改前／改后逐页读数、D5 那一项前后值、变异自证两行 | ✓ 本件第三、四节 |

**两条如实记**：

1. **`.scratch/t792/` 我一件没写**（那是 #792 收口票的墙／索引／读数／证据落点，不是本票写面）：本票的产物镜像与四件读数
   全落 `.scratch/t891/产物` 与 `.scratch/t891/读数`，`t792-facts.mjs` 与 `t792-读数.mjs` 只带 `--pages`／`--readings`／`--out` 跑，
   两份件本身一字未改。附带一条给收口票的实话：`.scratch/t792/产物` 那 61 件快照里**有 15 件比当刻域产物旧一代**
   （t787／t790 在快照之后又重跑过一轮），所以 `.scratch/t792/读数/` 里那四件读数与当刻产物不是同代——本票没去动它，
   只在 `.scratch/t891/` 里按当刻产物重出了同一套。
2. **判据没有为作息域放宽**：`SECTION_TOC_MIN = 3` 是**本域自己的**长短分账（票面允许），判分引擎、
   权重、七条硬扣分、D5 的算式与折算系数一字未动（引擎自证逐页逐维差 0 那行就是证据）。

## 七、怎么复算（一条链）

```sh
# ① 编译（本包单写者：编译／测试／git 写都经 run-locked）
node tooling/run-locked.mjs --ticket 891 -- node node_modules/typescript/bin/tsc -b packages/skill-schedule --force
# ② 重出 61 件产物（各支用**它自己的票号**进锁，这是探针安静窗口那条硬要求）
node tooling/run-locked.mjs --ticket 783 -- node docs/skills/skill-schedule/t783-探针.mjs   # … 逐支到 t790
# ③ 汇总到本票目录（名字与域清单件逐字同、逐件核 sha）
node .scratch/t891/汇总-产物.mjs
# ④ 四件读数 ＋ facts ＋ 判分
node docs/skills/skill-schedule/t792-读数.mjs --pages .scratch/t891/产物 --readings .scratch/t891/读数 --no-facts
node docs/skills/skill-schedule/t792-facts.mjs --pages .scratch/t891/产物 --out .scratch/t891/读数/facts.json
node packages/base-render/scripts/判分.mjs --dir .scratch/t891/读数 --json .scratch/t891/判分-后.json
# ⑤ 交卷读数（长页逐页清单 ＋ 逐页 D5 那条腿）
node .scratch/t891/交卷读数.mjs --json .scratch/t891/交卷读数.json
# ⑥ 变异自证
node .scratch/t891/全批抽掉.mjs          # 同批「改前」
node .scratch/t891/变异-抽目录.mjs       # 单件变异（抽掉／加回）
```

## 八、遗留出口

- **只补页内目录与定位避让**，页面信息组织与件序列一处未动（#782 人裁冻死的形状照旧）。
- **短页 11 件照旧扣 4**：票面写明「短页可以不出目录」，判据与小节数逐页写在第二节；
  若哪天要把「长页」改按页高判（而不是小节数），那是改判据的票，本票不做。
- **重复事实那条腿（H3）不在本票**：`制定次日计划结果` ×2 与 `周视图` 等页的 `dupFacts` 债归
  [#892](https://github.com/FeatherHunter/ilife/issues/892)；本票只保证**没给它加新债**（26 → 26）。
