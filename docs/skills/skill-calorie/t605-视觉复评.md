# t605 场景02饮食·视觉复评：**93/100 — PASS**

> 终审席执行记录（票 #605，属图 #155 收口票）。量尺＝[`t579-场景02视觉整改基准.md`](t579-场景02视觉整改基准.md) §二（J1 门禁 30／跨宽自适应 25／文案 25／vision 抽检 20），未自设口径。
> 当刻源码：`tsc -b packages/skill-calorie --force` 绿（GATE-RUN `b563397b-b29b-4452-a841-af9a4b4b5e0c`，exit 0）；墙按此源码重出（GATE-RUN `f57b6ba8-a769-4616-93a6-ca75b9886da9`，`RESTAGE-OK`）。截图与读数均为重出后当刻产物。
> 机器读数见 [`t605-证据.md`](t605-证据.md)；截图 34 张逐张路径见该件。

## 一 · 四维逐条得分

| 维 | 权重 | 得分 | 依据（读数／落点） |
|---|---|---|---|
| J1 门禁 | 30 | **30** | 83 产物页逐件点名：节点级命中合计 66，**66/66 的归属元素都是 `ilife-block-caliber`（来源脚注行）**，去重后 owner 集合＝{`ilife-block-caliber`}（即无第二种归属）⇒ **非脚注命中 = 0**。命中规则分布 R1 66／R3 52（均落在脚注）。全目录读数 `RESULT: 25/87 FAIL`＝25 件零命中＋66 处脚注（墙 4 件 820 命中，R7 474 全在墙，与 t579 §一 逐数相同）。 |
| 跨宽自适应 | 25 | **22** | `measure-responsive` 四档 348 格：**83 产物页 332 格全 `overflow=0`**，`failed=8` 全部落在墙 4 件（固定展示宽，非产物页）；**无「挤／塌列／被藏起来的横滚」**：83 页 `clippedCount` 命中的是 `ilife-block-dist-row-name`（页内图例条标签，见下二·D1）与 5 页 `ilife-block-list-rows-main`（长值省略号）；横向滚动条只有 37 处 `ilife-block-toc`（页内导航）与 8 处数据表（`overflow-x:auto`，属既定设计）。扣 3 分＝墙 8 格非零。 |
| 文案 | 25 | **22** | 83 产物页**非脚注**可见文本里 `·`／`；`／`｜`／`、` 为 **0**（门禁读数 R1–R6＝0，除脚注行）；V01–V31 逐条销账复核见三。扣 3 分＝本轮抽检新发现 3 处（题名与唤醒词不符／距范围数值自相矛盾／「餐/餐」术语，见二·D2–D4）。 |
| vision 抽检 | 20 | **19** | 5 页族 × 3 档重拍（记一餐含备注／看高热量榜／饮食复盘本周／查食品／看今日营养）＋两墙＋总索引＋链路总表，共 34 张（`.scratch/t605/shots/`）。扣 1 分＝D1（图例标签被裁，2 页 × 3 档可复现）。 |
| **合计** | 100 | **93** | ≥90 判 **PASS**。 |

## 二 · 抽检缺陷清单（症状／量化／归属）

| 编号 | 页码 | 症状 | 量（当刻读数） | 归属 |
|---|---|---|---|---|
| D1 | 看今日营养.html／饮食复盘（本周·本月·今年·最近 90 天·自定义时间）／看营养结构.html／查营养配比.html／看饮食复盘.html | 图例条标签被裁：`蛋白（推荐…`（`ilife-block-dist-row-name` 槽宽 78px，文本实需 131px，`overflow-x:hidden` 静默裁掉）——即 t579 所列「被藏起来的横滚」 | w=390/512/820/1440 **四档全中**（每页 3 处） | 公共层形状件 `packages/base-render/` 分布行；本票写集外 ⇒ 另立票 |
| D2 | 看今日营养.html | 唤醒词「看今日营养」，上屏题名＝`饮食复盘`／眉标＝`饮食复盘·饮食`／副题＝「本窗有记录 1 天…达标 0/1 天」＋页内导航 `本窗读数/每日热量趋势/高频食物/按餐汇总/营养配比`（整页是复盘页内容，与唤醒词不符） | 1440／834／390 三档同 | 路由/取数（场景 02 页归属）；非本票写集 |
| D3 | 饮食复盘（本周）.html（同理「本月／今年／最近 90 天／自定义时间」） | 推荐范围对比表「距范围」列自相矛盾：脂肪实际 44 克（17%）、下限 20%（52 克）⇒ 低于下限 8 克，表内印 **`-8 克`**（应按口径印 `-8 克` 于下限侧／或 `↓ 偏低` 的带符号差），同表蛋白/碳水印 `√` 正确 | 1440 与 390 两档同 | 公共层/页装配的差距算式；非本票写集 |
| D4 | 看高热量榜.html（全榜族同） | 榜单表「餐均」列值印 **`620 卡/餐`**，在 390 档卡片化后键值对读成「餐均 620 卡/餐」，单位「卡/餐」与列名「餐均」语义重复 | 390 档 | 排行族页装配；非本票写集 |
| D5 | 查食品.html | 食品卡片来源行印 `来源: 测试`：那是**种子条目的内部来源名**（`测试`），非用户可读来源 | 1440 与 390 | 种子数据面；非本票写集 |
| D6 | 链路总表.html | 83 行×8 列密表，390 档需横滑（本件是给总工程师看的点查表，非产物品） | 390 档 `scrollers=0`、`overflow=0`，靠列宽自适应 | 豁免件（t579 §〇） |

**反例（看着像缺陷、核过不是）**：① 390 档页内导航胶囊第四枚「复制明细」在右侧被切——那是 `ilife-block-toc` 的 `overflow-x:auto` 横向滚动条（量得 cw 358／sw 423），不是 `hidden` 静默裁切；② 桌面墙在 1440 下缩到 0.469 显示——生成器明文声明的整体缩小（`缩的是显示不是视口，视口仍是 1280`）；③ 两墙＋链路总表在 820 档量到溢出——墙按固定展示宽渲染，非产物页。

## 三 · V01–V31 销账复核（抽检可见面）

- **回执族（V01–V06）**：已销。副题改逗号串（`记一餐·鸡胸 2026-09-16 13:29:35（午餐），备注：加了辣酱，今日剩 1600 卡`）、写入字段 9 项改**徽章列**（`食物名/热量/蛋白质/碳水/脂肪/克数/备注/日期/时间`）、底部来源行改**三段独立 span**（`📊 数据来源：本机饮食库`／`本次影响 1 行`／`时间 2026-09-16 13:29`，段落间由样式出竖线、源里不含并列符）。残留：`成功（编号 306）` 仍带编号（属状态码上屏的弱形式，未列 V 条）。
- **条目列表族（V07–V11）**：表头／面包屑无 `·`；缺值符统一 `—`。
- **排行榜族（V12–V16）**：营养结构行内 `｜` 串**已消**（改三段堆叠条＋`蛋白 N% 碳水 N% 脂肪 N%` 一行）；390 档三卡正常堆叠、徽章基线齐。
- **复盘族（V17–V22）**：顶部 `；` 串 8 语义**已消**（逗号串）；图例「（千卡）」保留但不再重复；表头改「下限/上限」；`#N` 行号两端一致。
- **食品库族（V23–V26）**：眉标无 `·`；两档均无溢出塌列（复算一致）；D5 为数据面。
- **营养总览族（V27–V31）**：副题 `；` 串已消；`[天数]` 占位符残留已消；`✓ 在范围内` 不再拆字。

## 四 · 变异自证（两行机器读数）

```
MUTANT  exit=1 RESULT: 0/1 FAIL bytes=100953
RESTORE exit=1 RESULT: 0/1 FAIL bytes=100942 sha256=df82057bf4d21965 == 变异前 ✓
```

改坏处：向 `看高热量榜.html` 的**非脚注节点**塞入 `·`／`；`／`｜`／`、`（该页本来 0 命中 ⇒ 变异后必须红，实测 exit 1）。还原后逐字节回到变异前（sha256 同）。第二行退出码仍是 1，是因为该页含 1 处**来源脚注**命中（本票不碰，#560）；故另加逐字节校验位 `== 变异前 ✓` 判绿。

## 五 · 结论与返修出口

总分 **93 ≥ 90 ⇒ PASS**。三处新缺陷（D1／D3／D4）与两处数据面缺陷（D2／D5）**均不在本票写集**（本票只重出与评分），逐条写出供总工程师另立票：

1. **D1**（公共层分布行槽宽 78px 固定、标签 `（推荐…` 被 `overflow:hidden` 裁）：建议立「公共层分布行窄槽自适应」票，落点 `packages/base-render/` 分布行 + 调用页 10 页复核。
2. **D3**（距范围带符号差）：建议立「推荐范围差距算式与符号口径」票。
3. **D2**（题名与唤醒词不符）：建议并入场景 02 页归属整理票（与 t579 §五 第 82–83 格「场景归属待裁」同族）。
4. **D4／D5**：低优先级文案／数据面，随所属页族票捎带。

不改任何源码、不碰他人件：本票写集＝`docs/skills/skill-calorie/scene02-验收墙/**` ＋ `t605-*` ＋ `.scratch/t605/**`。
