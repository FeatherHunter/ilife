# #296 复核（单席）

复核对象：`#296` 交付，提交 `6bce6f8` ＋ `9c4f107`（HEAD＝`9c4f107`）。纯文本票，复核席只读、不改被审文档。
复核边界：命令一律走默认锁目录 `node tooling/run-locked.mjs --ticket 296 -- …`，未用 `--lock-dir`；
「搬一条命令」按派单允许的静态口径核到 file:line（未在仓外临时根真搬），并实跑 `gen:check` 与验收探针。

**VERDICT: PASS** ｜ SCORE: 契约=29/30 证据=23/25 可照抄性=17/20 红线=15/15 文档=9/10 → 均分=93

## 一 · 四件事逐条

| # | 要求 | 在/缺 | 落点（我读到的行） |
| --- | --- | --- | --- |
| ① | 新命令怎么写 | **在** | 《照抄说明》`:7-18`（六字段、`example` 必填非空且与 `--params` 形状绑定、新能力「恰好导出一个声明数组」＋`index.ts` 再导出）；《纪律正文》「形状一」`:27-40` |
| ② | 共用位何时能动、要不要抢锁 | **在**（一处错位，见七） | 《照抄说明》`:20-40`（日常加改＝零接触；新增能力／框架变更＝**先抢 `gate.lock` ＋广播**；名单以 `gen:check` 逐件输出为准）；《纪律正文》「形状二」`:42-54` |
| ③ | 撞车怎么办 | **在** | 《照抄说明》`:42-50`（声明写集；跨图撞车停下报告、不自行改别人的行；点明 `legacyCommands.ts`／`routing.ts` 两处全场共用＋动前抢锁）；《纪律正文》`:33`、`:103-108` |
| ④ | 恢复开工前动作 | **在** | 《照抄说明》`:52-59`（`git pull`；`git status --short` **出现不属于你的路径就停下报告**；确认 `.scratch/locks/gate.lock` 不存在）；《广播稿》`:39-48` 同三件 |

④ 三件齐（派单点名的第三件「工作树没有别人的未提交改动」在 `:55-56`，且带亲历来源）。

## 二 · 可照抄性实测（本票核心判据）

我挑 `calorie.view.ranking`（读命令，不属体重）。只读《照抄说明》＋《纪律正文》＋`src/weight/`，我推出的清单与顺序：

1. 定能力目录（新能力则建 `src/<能力>/`）→ 2. `src/<能力>/commands.ts` 加一行声明（六字段全，`example` 照抄即能跑）→
3. 处理函数搬进同一能力目录 → 4. 新能力才要 `src/<能力>/index.ts` 再导出数组 → 5. 新能力／框架变更：**先抢锁＋广播** →
6. **手删 `legacyCommands.ts` 那一行**（抢锁；一个键恰住一处）→ 7. `routing.ts` 只在**加／删键**时动（搬迁键不变＝不动；动则抢锁、只改自己那一行）→
8. 随搬迁**下调棘轮上限**（显式动作）→ 9. 生成链四步 `build→gen→build→help:build` ＋ 自证（`gen:check`、探针）→
10. 写命令另加两面（`cmd-write-40-persist` 落库块＋`t97-probe-receipts.mjs` 场景行）；删命令先动 `RouteKey`（`TS2820`）。

与真实仓核对（file:line）：现有键住在 `packages/skill-calorie/src/cli/legacyCommands.ts:112`（声明）、
`src/cli/cmd_read.ts:705`（`case`＋实现）、`src/cli/keys.ts:123`（生成物）、
`src/triggers/routing.ts:202-222`（手写路由，21 条 `exec` 路由指向同一键）、
`scripts/build-help.mjs:102,210`（生成块）、`SKILL.md:168,185`、`packages/base-combos/combos.yaml:457`（生成镜像），
另加 6 件测试（`cmd-read-t11`／`db-readonly-93`／`diet-homogeneity-108`／`output-naming-119`／`cmd-registry-294`／`legacy-ratchet-295`）。
结论：**清单方向对、顺序对、抢锁面基本对**（生成物零接触、`legacyCommands.ts` 与 `routing.ts` 要抢锁）。差异三处：

- **(a) 两处棘轮 test 文件没点明**：搬走一条会要求手改 `packages/skill-calorie/test/cmd-registry-294.test.mjs:34-54,79`（键从 `FROZEN_READ_CASES` 删、`FROZEN_READ_CASES_MAX` 61→60）
  与 `packages/skill-calorie/test/legacy-ratchet-295.test.mjs:27,53-55`（`FROZEN_LEGACY_KEYS` 删、`FROZEN_LEGACY_MAX` 92→91、`FROZEN_LEGACY_LINES` 119→118）。
  文档只有「下调棘轮上限」（《照抄说明》`:50`、《纪律正文》`:86,117`）一句抽象说法，没写要动哪两个文件、哪个常量。
  同理「搬走后把老 `case` 从分派层删掉」只在《纪律正文》`:101`（「`case` 只许随搬迁减少」）出现一次，《照抄说明》与《广播稿》都没写。
- **(b) 「0 件必改」口径错**：《照抄说明》`:66` 写「实测**加一条**读命令 `test/` 下 0 件必改」，与同段 `:69-74`「命令集一变…`test/calorie-routing-81.test.mjs` 的冻结路由口径…要手跟」互相拆台；
  实测该文件 `:79-80` 是手写字面量 436（exec 344＋non-exec 92），加一条带**新唤醒词**的读命令必然要手改它（`help-center-106.test.mjs:67,68,80` 的 344/92/295 同理）。正确口径应是「**搬迁**（键集不变）才是 0 件」。
- **(c) `SKILL.md` 的门写错**：我自己跑 `pnpm gen:check` 的逐件输出只有四件（`keys.ts`／`registry.ts`／`combos.yaml`／`build-help.mjs`），
  `SKILL.md` 不在其中；它的重写路径是 `pnpm help:build`（`build-help.mjs:251-259`），
  而 `build-help.mjs` 文件头第 9 行明说 `gen:check`「不含 `SKILL.md` 本身」。
  于是《照抄说明》`:22`「哪些在名单里以 `gen:check` 逐件输出为准」＋《广播稿》`:72`「各汇总位…手改会被 `gen:check` 抓红」对 `SKILL.md` 这一件不成立——而票面 ② 恰恰把 `SKILL.md` 点名为共用位之一。

## 三 · 过期清单（铁律二，逐处判定）

| 处 | 数量词 | 判定 |
| --- | --- | --- |
| 《纪律正文》`:44-45`、`:129-130` | 「不列共用位名单」「不抄行数基线、条数上限」，并把口径指给 `pnpm gen:check` 逐件输出与两个棘轮文件 | **合规（本份最正的一处）** |
| 《纪律正文》`:12-13` | 改前 8 个以上位置／6 处共用写点→0 处 | 合规：`改前` 是冻结历史，`:143` 有出处 |
| 《纪律正文》`:91-92` | 棘轮「三条断言」 | 合规（边界）：以 `legacy-ratchet-295.test.mjs:27-82` 为权威源，不抄数字 |
| 《纪律正文》`:104` | 「19 字段 sha 是 #180 重导的锚」 | 合规（边界）：冻结锚的现状描述，带票号出处 |
| 《纪律正文》`:113-114`、《照抄说明》`:66`、《广播稿》`:85` | 读命令 `test/` 下 **0 件**必改 | **边界→实为错**：见二(b)；写成「搬迁」才成立 |
| 《照抄说明》`:18` | 参考实现「（9 条）」 | 合规（边界）：指得出权威源 `src/weight/commands.ts`，是现状描述不是规矩 |
| 《照抄说明》`:36`、《广播稿》`:56` | 生成链「四步」 | 合规：与 `build-help.mjs` 文件头 `:6` 的既有口径一致，是流程本身不是基线 |
| 《照抄说明》`:57`、《广播稿》`:45` | 「锁龄 68 分钟的死锁」 | 合规：事故现状描述（票面 #296 关键事实已给出处） |
| 《照抄说明》`:61`、《广播稿》`:39,81` | 「三条／三件／两件」 | 合规：自指本页条目数 |
| 《照抄说明》`:81`、《广播稿》`:64` | 探针 `UNACCOUNTED` 必须为 0 | 合规：口径指向可复跑探针，非死数字 |
| 《广播稿》`:76` | 「`continue-on-error` 全仓禁用」 | 合规：有机器守（`test/ci-gates-295.test.mjs`，见 `t295-复审-蓝队.md:105`） |

未发现行数基线／命令条数上限／键数写进规矩；共用位名单只在《照抄说明》`:46-48` 以「全场共用、谁都在写」点两处（`legacyCommands.ts`／`routing.ts`），
属撞车面现状描述而非汇总位名单，且 `:22` 已把权威指给 `gen:check`——判**合规（边界：与 ② 的定义同名不同义，见七）**。

## 四 · 三条新判据（#295 关闭后编排者裁）

1. **陈旧守卫边界：在**。《纪律正文》`:68-72`（印记缺失不判陈旧、打 `GEN-STAMP WARN` 放行、不假红优先、CI 不会踩）＋`:64-66`（照医嘱重建、
   **别删文件别改门**、报告）。我核到实现：`gen-cli.mjs:320-325`（`files === null` ⇒ 打 `WARN`）＋`:332`（`if (files === null) continue`）＋`:328-331`（只查编译产物在不在）；
   CI 顺序 `ci.yml:29`（`pnpm build`）先于 `:43`（`gen:check`），根 `package.json` 的 `build` ＝ `tsc -b && node … gen-cli.mjs --stamp` ⇒ 「无条件重打印记」成立。
2. **覆盖面缺口：在**。《纪律正文》`:73-77`（印记里没有某条声明 ⇒ 放行 ⇒ 新增能力目录后改声明报下游 `GEN-CHECK FAIL` 而非 `GEN-STALE FAIL`；「没报陈旧」≠「没问题」；余下留给后续票）。
   实现依据：`gen-cli.mjs:334`（`recorded` 不是字符串 ⇒ `continue`）＋`:423`（`GEN-CHECK FAIL`）。
   附注（不判缺陷）：本地若盘上生成物恰等于「旧 `dist/` 派生结果」，这一刻本地 `gen:check` 也可以绿；红会出现在真 `pnpm build` 之后或 CI——这正是该条要表达的「判定权在下游」。
3. **加／删命令后跑探针、三桶 `UNACCOUNTED` 为空：在**。《纪律正文》`:119-125`（判据第 4 条，含三桶口径）；《照抄说明》`:81`；《广播稿》`:64`。
   我实跑 `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`：`P1: PASS`，`② DISCIPLINED-SHARED` 恰 1 处（`routing.ts`）＋`③ UNACCOUNTED（… 0 处）`＋「⇒ UNACCOUNTED 为空 ⇒ 手写面已全部落在 ①/② 内」。

## 五 · 爆炸半径 / 零代码 / 无重复件 / 更正核对

- `AGENTS.md`：`git show --numstat 6bce6f8` ＝ `4 0`（`AGENTS.md:25-28`＝空行＋`### 命令登记`＋空行＋一行正文）；`9c4f107` 未碰它。**只有指针，无纪律正文、无共用位名单** ✓
- 零代码：`git show --stat` 按 `^(packages/|src/|test/|scripts/|tooling/)` 过滤，`6bce6f8` **0 命中**、`9c4f107` **0 命中** ✓（六个文本件＋两处改名）
- 无重复件：`git ls-files "*纪律正文*"` **空**；`docs/agents/命令登记纪律.md` 恰一份 ✓
- 更正核对（D-1）：我独立跑 `git grep -n "t296-纪律正文\|t296-照抄说明"` ⇒ **3 命中**，全在 `docs/skills/skill-calorie/t296-证据.md:12,13,49`（12/13＝搬家记录，49＝判据文字本身）；
  与它自称「0 命中（排除本件）＋余 3 处全在本文件」**一致**；单跑 `t296-纪律正文` 亦 3 命中。✓

## 六 · 误导性断言逐条

- 「加一条命令要改的就是这一处加它那个子功能文件」（《照抄说明》`:10`、《纪律正文》`:30`）：**例外已标**——`routing.ts` 加／删要动（《照抄说明》`:47`、《纪律正文》`:105-108`）、写命令两面（`:63-65`）、删命令 `TS2820`（`:67-68`）。判**合规（风险：粗体断言处无就地交叉引用，见七-1）**
- 「日常加／改一条命令＝零接触共用位」（《照抄说明》`:26`）：例外在 `:22`（名单以 `gen:check` 为准）＋`:47`（`routing.ts`），判**合规（边界，同上）**
- 「读命令 `test/` 下 0 件必改」（`:66`）：**口径错**，见二(b)。属**量级错**，非漏例（例外在 `:69-74`）
- 未发现无出处的机制细节：三条新判据的点名实现（`staleDeclarations()`／根 `build`）都核到 file:line；`:134` 的行数口径与 `cmd-registry-294.test.mjs:85` 同口径 ✓

## 七 · 未消除 / 新缺陷（归属）

1. **S2（本票，文档）**：`共用位` 一词两名——② 定义它为「汇总位（生成物、零接触）」，③ 又用它指 `legacyCommands.ts`／`routing.ts`（手写、要抢锁），而探针把 `routing.ts` 记作「受纪律约束的共用位」并要求「写进 #296 照抄说明第②项」；实际那句落在 ③ 与 ⑤。建议：② 的表里补一行「手写共用面（`routing.ts` 等）——加／删键必须动，动前抢锁」。
2. **S2（本票，文档）**：二(a)——搬迁要动的两个棘轮文件与常量、以及「删老 `case`」没写成可执行动作。
3. **S2（本票，文档）**：二(b)——「加一条读命令 0 件必改」与同段「命令集一变要手跟」冲突；改「搬迁」即成立。
4. **S2（本票，文档）**：二(c)——`SKILL.md` 不在 `gen:check` 名单里，其门是 `pnpm help:build`；《广播稿》`:72` 的「手改会被 `gen:check` 抓红」对这件不成立。
5. **S3（本票，自述）**：`t296-证据.md:52` 自称「唯一数量词是『参考实现 9 条命令』」不实——同页还有 `0 件`、`68 分钟`、`四步`、`三条实测事实` 等；应写成「数量词逐处判定见复核报告」。
6. **S3（延续，非本票）**：`照抄说明` 对「前后两次 `build` 都要」给的理由（生成器读 `dist/`）只解释了前一次；后一次是为了让 `keys.ts` 重生后 `dist/cli/keys.js` 跟上（`SKILL.md` 互联区读它）。链本身经 `build-help.mjs:6` 印证，只理由不精确。
7. **未做（本席）**：未在仓外临时根真搬一条命令（派单允许静态到 file:line）；未跑 `pnpm test`／`help:examples:check`（本票零代码，且后者票外既有红）。

## 八 · 我跑的读数（可复跑）

```
node tooling/run-locked.mjs --ticket 296 -- pnpm gen:check
  GEN-CHECK ok packages\skill-calorie\src\cli\keys.ts        sha256=4db43569…
  GEN-CHECK ok packages\skill-calorie\src\cli\registry.ts    sha256=9e11f044…
  GEN-CHECK ok packages\base-combos\combos.yaml              sha256=ca17d2c3…
  GEN-CHECK ok packages\skill-calorie\scripts\build-help.mjs sha256=4d44f03f…
  GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 1 个（weight）声明 9 条，未搬迁清单 92 条
node docs/skills/skill-calorie/t293-验收-命令自治.mjs        # P1 PASS；UNACCOUNTED 0 处
git grep -n "t296-纪律正文\|t296-照抄说明"                    # 3 命中（证据件 12/13/49）
git ls-files "*纪律正文*"                                     # 空
```
