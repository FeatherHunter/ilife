# #520 视觉重评 · V4 席（页 24–31）

**席**：#520 视觉重评席 V4（本席只评页 24–31）
**产品目录**：`.scratch/t387/html/`（页 24–31 共 8 页）
**日期**：2026-09-16

## 尺（全批统一，本席不得自定）

| 维度 | 满分 |
| --- | --- |
| 结构纪律 | 20 |
| 信息层级与版式细调 | 25 |
| 分隔符与文字纪律 | 20 |
| 跨宽自适应 | 15 |
| 手机端与 HELP 同档 | 20 |

**加权总分 ＝ 结构纪律×0.20 ＋ 信息层级与版式细调×0.25 ＋ 分隔符与文字纪律×0.20 ＋ 跨宽自适应×0.15 ＋ 手机端与 HELP 同档×0.20**
（各维先按 0–100 记分，再按上式加权；下节「本页总分」即加权值。）

### 用户硬要求（逐条现量，不凭印象）

1. 桌面 1440 与手机 390/768 三档无溢出、无塌列，触摸目标不少于 44px。
2. 手机档与 HELP 同档，参照 `fixtures/help-instances/卡路里_HELP_20260731_201530.html`（断点 `@media (max-width: 600px)`／`@media (min-width: 601px) and (max-width: 900px)`；窄表走 `.container{max-width:720px}` ＋ 流式卡片 `flex-wrap:wrap`，不下横滚）。
3. 设计师审美：间距取 4 或 8 的倍数、字号成档有下限、数值列等宽右对齐、卡/表/图左缘共线、留白有节。
4. 文案零冗余：同一事实一页只讲一次，用词守 `docs/agents/wording.md`（禁黑话、禁英文内部标识符上屏）。
5. 凡用分号/竖线/间隔号把并列信息拼成一串的地方，应改成卡片/分行/徽章/图例等可见结构。
6. 视觉审查用 `vision_html_screenshot` 双档实拍 ＋ `vision_describe` 读图；`dsh visionreview` 已坏，不许再试。

### 每页固定六问

1. 第一眼看到什么、层级清不清。
2. 挤/空/错位/列宽失衡/截断/横滚。
3. 有没有拿分号、间隔号、竖线把并列信息挤成一串。
4. 同一件事有没有写两遍、有没有英文内部标识符/库表名/常量名上屏。
5. 数值列等宽数字吗、缺值写 — 吗、小数位一致吗。
6. 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断。

### 逐页加权总分表

| 页 | 页名 | 结构纪律/20 | 信息层级/25 | 分隔符与文字/20 | 跨宽/15 | 手机端/20 | **加权总分** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 24 | 看BMI报告 | 88 | 72 | 78 | 90 | 74 | **79.5** |
| 25 | 看TDEE报告 | 90 | 82 | 74 | 90 | 86 | **84.0** |
| 26 | 看BMR报告 | 82 | 78 | 68 | 90 | 78 | **78.6** |
| 27 | 看蛋白质摄入报告 | 84 | 66 | 72 | 90 | 72 | **75.6** |
| 28 | 看水分摄入报告 | 84 | 74 | 72 | 90 | 74 | **78.0** |
| 29 | 看综合评分 | 84 | 70 | 74 | 80 | 76 | **76.3** |
| 30 | 看健康趋势 | 86 | 70 | 74 | 90 | 74 | **77.8** |
| 31 | 看健康报告(含对比) | 88 | 72 | 72 | 90 | 78 | **79.1** |

> 本表随逐页评审实时回填；每页评完立即落该页小节，报告文件始终完整。

### 先量到的共同底座（页 24–31 八页同一份字节）

对八页逐个抽出全部 `<style>` 区（先合并再求 SHA256），结果**八页同一份 CSS**：

| 项 | 读数（八页一致） |
| --- | --- |
| CSS 字节数 | 42804 |
| CSS SHA256 前 12 位 | `E41401DD9CEB` |
| 断点 | `820px`、`720px／721–875px／876px`（图表字号三档）、`640px`、`400px`、`prefers-reduced-motion`、`print` |
| `min-height:44px` 出现次数 | 11 |
| `font-variant-numeric` 出现次数 | 8 |

⇒ **本席对八页的「跨宽自适应」「手机端触摸目标」判据是同一条底座**：`min-height:44px` 覆盖 `.ilife-action-btn`／`.ilife-copy-btn`／`.ilife-copy-menu-item`／`.ilife-block-toc a`／页壳 `summary`；窄表堆叠走 `@media (max-width:640px)` 的 `thead{display:none}` ＋ `td{display:flex;justify-content:space-between}` ＋ `td::before{content:attr(data-label)}`。
**与 HELP 锚件对照**：HELP 用 `@media (max-width:600px)` ＋ 601–900px 容器 720px 的**流式卡片／`flex-wrap:wrap`**；本族用 640px 断点把表拆成「标签左／数值右」行——**做法同形（都不横滚、都拆行），断点数值差 40px、窄屏容器内距 16px vs HELP 8px**，此差异在八页里恒定，不逐页重复扣。
**逐页差异只剩正文**（八页 CSS 相同、body 内容不同），故「结构纪律／分隔符与文字纪律」两维的扣分**只来自各页自己的可见文案与表格行数**。

---

## 页 24 · 看BMI报告

**源**：`.scratch/t387/html/24-看BMI报告.html`（2445 行；可见正文全部在 L1824–L1868，L1844 为一行通栏超长行）
**实拍**：桌面 1440×900 fullPage（pageHeight 4403）＋ 手机 390×844 fullPage（pageHeight 7525）；crop 3 张（桌面 560–1400、手机 900–2200、桌面 3900–4403）

### 六问逐条

**① 第一眼／层级**——第一眼是页壳 `<h1 class="ilife-block-page-shell-title">BMI 报告</h1>` 下的结论句 `最近一次称重是 2026-09-16，BMI 24.5，落在正常区间（身高取档案里那一个值，90 天窗口内不变）`（L1844），随后 4 张指标卡（`ilife-block-kpi-card-grid`：当前 BMI／分类／身高／采样点）→ 折线图（`ilife-block-chart-block`）→ 分级表 → 60 行逐日表 → 里程碑 → 数据与日志。递进顺序清楚，读图与源码一致。

**② 挤／空／错位／列宽失衡**——
- 卡／图／表左缘**共线**：三件都是 `margin:16px 0` 的整宽块（`.ilife-block-data-table{L1372}`、`.ilife-block-chart-block{L1500}`），页壳另有 `#573 R-61` 归位规则 `.ilife-block-page-shell .ilife-block-data-table{margin-left:0;margin-right:auto}`（L1843）。读图我说“图表卡片比下表窄”属误读，**源码未证实，不扣分**。
- 读图说“曲线被压在卡片下半、顶部大片空白”：源码 `viewBox="0 0 580.0 260.0"`，网格线在 y=21／120.5／220（刻度 25.8／25.0／24.2），数据 24.5–25.5 映射后上下留白各约 19%，**属对称留白，源码未证实，不扣分**。
- 真实缺陷：**逐日表 60 行全展开、0 个 `<details>`**（实测 `data-label="日期"` 计 60 次、`<details` 计 0 次、`<tr` 计 68 次），明细层没有任何折叠／分组，桌面 4403px、手机 7525px。

**③ 分号／竖线／间隔号拼串**——可见正文里**没有**「；｜·」拼串：页脚三行是三条独立 `<p class="ilife-block-caliber">`，第一行内部也是两个 `<span>` 分块（L1867：`BMI＝体重（kg）÷ 身高（m）的平方` ／ `身高取档案里那一个值`）。**但**「复制日志」浮层待显内容里有一条竖线串，见④。

**④ 写两遍／英文标识符上屏**——
- **同一事实写三遍**：身高来源在 ①结论句括注「（身高取档案里那一个值，90 天窗口内不变）」、②指标卡副标「来自档案（BMI 的分母）」、③页脚 `caliber`「身高取档案里那一个值」各出现一次；「在正常区间」也在结论句与徽章 `<span class="ilife-status-badge ilife-status-badge-ok">在正常区间</span>` 各写一次（L1844）。
- **英文内部标识符**：屏幕可见正文只有 `BMI`／`cm`／`kg`（域内单位，不算泄漏）；但 `data-action-id="ilife-copy-log"` 的 `data-t` 里（L1856–L1867，点开浮层即上屏）含 `calorie.report.bmi（stat）`、`calorie_data.db ｜ calorie_data.db ｜ 报告子形态 BMI 报告`、`calorie-cmd-read calorie.report.bmi --params '{"window":"90d"}'`——**库表名＋「｜」拼串**，命中④⑤。默认不渲染，故按轻档记。

**⑤ 数值列等宽／缺值／小数位**——
- 等宽：`.ilife-block-data-table td.ilife-block-data-table-cell-right{min-width:5.5em;font-family:"SF Mono",monospace;font-variant-numeric:tabular-nums}`（L1419–1423），表头 `cell-right`（L1844，体重／BMI 两列）——**达标**。
- 缺值：分级表非当前档写 `—`（`<td ... data-label="当前落在">—</td>` ×3），当前档写 `✔`——**达标**。
- **小数位不一致（缺陷，源码可抄）**：同一列混排 `78`（L1844 首行 `<td ... data-label="体重（kg）">78</td>`）与 `77.9`，同一列混排 `25.5` 与 `25`——整数形与一位小数混排，列内不对齐成档。

**⑥ 手机档表格／触摸目标／日期折断**——
- 表格形态：≤640px 走 `@media (max-width: 640px)`（L1431）把表拆成「标签左／数值右」堆叠——`thead{display:none}`、`tr{display:block}`、`td{display:flex;justify-content:space-between}`＋`td::before{content:attr(data-label)}`（L1446–1480）。**与 HELP 速查台的窄屏做法同形（同样是拆行不横滚）**；查 `overflow-x:auto`（L1374）是兜底，实拍 390 无横向溢出。
- 触摸目标：页内可点件均 `min-height:44px`——`.ilife-block-toc a`（L1159–1164）、`.ilife-action-btn`（L265–266）、`.ilife-copy-btn`（L327–328）、`.ilife-copy-menu-item`（L376–377）、页壳 `summary`（L1831）——**达标**。
- 日期：实拍未见折断（`2026-07-19` 完整）；源码隐患：堆叠态 `td` 带 `overflow-wrap:anywhere`（L1472），而 `white-space:nowrap` 只在 ≥641px 给首列（L1425–1429），390 档没有 nowrap 护栏——**记为隐患，实拍未发，不扣分**。

### 本页缺陷（可抄源）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 24-1 | 身高来源一页写三遍（结论括注／指标卡副标／页脚） | L1844、L1867 | 中 |
| 24-2 | 逐日表 60 行全展开，无折叠无分组（手机 7525px） | 60×`data-label="日期"`、`<details` 0 个 | 大 |
| 24-3 | 同列小数位不一致：`78` 与 `77.9`；`25.5` 与 `25` | L1844 | 中 |
| 24-4 | 日志浮层含库表名＋「｜」串（`calorie_data.db ｜ calorie_data.db ｜ …`） | L1856–L1867 `data-t` | 中（默认不上屏） |
| 24-5 | 表头 `BMI 分级（中国标准）` 与阈值 `正常 18.5 – 24.9` 自相矛盾（中国标准正常上限 23.9；24.5 按该标准应判超重，页内却判「正常／在正常区间」） | L1844 `caption` ＋ `正常｜18.5 – 24.9` ＋ 结论句 | 大（口径，非版式） |
| 24-6 | 小节标题 `分类里程碑` 带冗余括注，行内又已用箭头表示同一件事 | L1844（`分类里程碑` 段） | 小 |

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 88 | 17.6 |
| 信息层级与版式细调（权重 .25） | 72 | 18.0 |
| 分隔符与文字纪律（权重 .20） | 78 | 15.6 |
| 跨宽自适应（权重 .15） | 90 | 13.5 |
| 手机端与 HELP 同档（权重 .20） | 74 | 14.8 |
| **本页加权总分** | — | **79.5** |

**扣分口径**：结构 88＝页壳/章节/导航完整，扣 24-6 与「数据与日志」整节只为两个复制按钮；信息 72＝扣 24-2（明细层坍平成一条长龙）＋24-3＋24-1 的重复占用首屏；文字 78＝扣 24-4 与 24-5；跨宽 90＝三档无溢出、断点与 HELP 同形，扣 820／640 两档与 HELP 600／601–900 的档位微差与日期 nowrap 护栏缺失；手机 74＝44px 达标、堆叠达标，但 60 行无分组无折叠是 HELP 不会有的密度债（HELP 同类清单可折）。
**中段未细看**：折线图内部（目标线、区间底色、末尾数据点标注）未逐点核对，只核了网格线与刻度。

---

---

## 页 25 · 看TDEE报告

**源**：`.scratch/t387/html/25-看TDEE报告.html`（2445 行；正文全在 L1824–L1868）
**实拍**：桌面 1440×900 fullPage（pageHeight 1559）＋ 手机 390×844 fullPage（pageHeight 1596）；crop 3 张（桌面 150–900、桌面 900–1559、手机 400–1250）

### 六问逐条

**① 第一眼／层级**——结论句 `每日总消耗约 2635 卡，日均摄入 1589 卡，静态缺口 +1046 卡`（L1844）→ 4 张指标卡（每日总消耗／日均摄入／静态缺口／活动量系数）→ `计算口径` 两列表（项｜取值）→ 总消耗曲线 → 复制数据／复制日志 → 页脚 4 条 `ilife-block-caliber`。页短（桌面 1559px／手机 1596px），层级一眼可读完，是这批里最干净的一页。

**② 挤／空／错位**——
- 四卡外框等高对齐（`.ilife-block-kpi-card-grid` 网格拉伸），左缘与 `计算口径` 表、曲线卡片**共线**（同为整宽块）。
- 真实缺陷（源码可抄）：第 3 张卡多一行徽章 `<div class="ilife-block-kpi-card-badge"><span class="ilife-status-badge ilife-status-badge-ok">在缺口</span></div>`，另 3 张卡没有，网格等高拉伸后**另 3 卡底部各空出一行**（`.ilife-block-kpi-card` 只有 `padding:14px`，无内部分配）。
- 曲线留白：读图说“最高点距上网格 30–40px、终点贴底”——源码 `path d="M58.0 49.4 … L566.0 198.7"`，网格线 y=21／120.5／220，曲线占绘图区 **76%**（上留 28px、下留 21px），**属正常上下边距，源码未证实，不扣分**。

**③ 分号／竖线／间隔号拼串**——可见正文无「；｜·」拼串。`计算口径` 表把**算式与说明塞进同一格**：`缺口口径` 行取值 `静态缺口 ＝ 每日总消耗 − 日均摄入（运动消耗另计，不含在内）`（L1844），一条括号夹带第二条事实——按⑤口径应拆行。

**④ 写两遍／英文标识符**——
- **表与页脚系统性重复（本页最大文字债）**：口径表 `公式` 行 `Mifflin-St Jeor：基础代谢 × 活动系数` ↔ 页脚 `总消耗＝基础代谢 × 活动系数`；口径表 `缺口口径` 行 `静态缺口 ＝ 每日总消耗 − 日均摄入（运动消耗另计，不含在内）` ↔ 页脚 `静态缺口＝每日总消耗减日均摄入，运动消耗另计`——**同一事实两处、且两处写法不同（`−` 与 `减`、括注与逗号）**；`活动系数 1.55（中度活动）` 亦在指标卡 `活动量系数 1.55／档位：中度活动` 再写一遍（L1844）。
- 同一张卡内自我重复：副标 `消耗大于摄入（在缺口）` 与徽章 `在缺口`（L1844）三字重复。
- `一样四要素` 类黑话：曲线标题 `总消耗随体重变化（同样四要素下，体重变 → 总消耗变，图上每 4 天一点）`（L1844）——**「四要素」本页无定义**（口径表行是 公式／基础代谢／活动系数／身高／年龄／性别／缺口口径，未列体重、未解释四要素），且括注复述标题前半句。
- 英文：屏幕可见仅 `TDEE`、`Mifflin-St Jeor`（域内专名）；日志浮层 `data-t` 内仍是 `calorie.report.tdee`、`calorie_data.db ｜ calorie_data.db ｜ 报告子形态 TDEE 报告`、`calorie-cmd-read …`（L1856–L1867，默认不上屏，与页 24 同源）。

**⑤ 数值列等宽／缺值／小数位**——
- 口径表两列**都是 `ilife-block-data-table-cell-left`**，故共用的 `cell-right`（`min-width:5.5em`＋`SF Mono`＋`tabular-nums`，L1419–1423）规则**不作用于本页任何数值**：`1700 卡`／`1.55（中度活动）`／`175 cm`／`30 岁` 全左对齐、无等宽数字，取值列长短悬殊（`1700 卡` vs `Mifflin-St Jeor：基础代谢 × 活动系数`）呈参差。
- 符号不一致：结论句 `静态缺口 +1046 卡` 带正号，指标卡 `1046` 不带（L1844）。
- 数值自洽（正面证据）：1700 × 1.55 = 2635 ✓、2635 − 1589 = 1046 ✓，与卡面一致；无缺值单元格，未用到 `—`。

**⑥ 手机档／触摸目标／日期**——
- 390 档：KPI 走 `@media (max-width:820px)` 两列（`grid-template-columns:repeat(2,1fr)`，L1838），口径表走 ≤640px 堆叠（`td::before{content:attr(data-label)}`），实拍无横向溢出、无截断；`缺口口径` 取值换行成两行，属正常。
- 触摸目标：与全批同一套（`min-height:44px` ×11 处，含 `.ilife-block-toc a` L1159–1164、`.ilife-action-btn`、`.ilife-copy-btn`）；读图称“胶囊 36–40px 不足 44px”的对象是 `<span class="ilife-block-chip">`（**纯展示、不可点**），**源码未证实为可点目标，不扣分**。
- 日期 `2026-08-18 至 2026-09-16（共 30 天）` 与图上 9 个 X 轴标签（08-18…09-16）实拍均完整未折断。

### 本页缺陷（可抄源）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 25-1 | 口径表 3 行与页脚 3 条 `caliber` 讲同一件事，且写法不同（`−`／`减`、括注／逗号） | L1844 ↔ L1867 | 大 |
| 25-2 | 卡内三字重复：副标「消耗大于摄入（在缺口）」＋徽章「在缺口」 | L1844 | 小 |
| 25-3 | 结论句与 4 张卡重复印同一组数字（2635／1589／1046） | L1844 | 中 |
| 25-4 | 「同样四要素下」术语本页无定义，括注复述标题 | L1844 | 中 |
| 25-5 | 口径表数值列左对齐、无等宽数字，取值列参差 | L1844 ＋ L1419–1423（`cell-right` 未用） | 中 |
| 25-6 | 第 3 卡多一行徽章，网格等高导致另 3 卡底部空一行 | L1844 ＋ L1320–1326 | 小 |
| 25-7 | `+1046` 与 `1046` 正号不一致 | L1844 | 小 |
| 25-8 | 日志浮层含库表名＋「｜」串（与页 24 同源） | L1856–L1867 | 中（默认不上屏） |

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 90 | 18.0 |
| 信息层级与版式细调（权重 .25） | 82 | 20.5 |
| 分隔符与文字纪律（权重 .20） | 74 | 14.8 |
| 跨宽自适应（权重 .15） | 90 | 13.5 |
| 手机端与 HELP 同档（权重 .20） | 86 | 17.2 |
| **本页加权总分** | — | **84.0** |

**扣分口径**：结构 90＝页壳/章节/导航完整，仅「数据与日志」整节只为两个复制按钮；信息 82＝扣 25-3／25-5／25-6，曲线留白与卡高经源码核对不成立故不计；文字 74＝扣 25-1（表↔页脚系统重复）、25-2、25-4、25-7 与 25-8；跨宽 90＝三档无溢出、同一套断点；手机 86＝1596px 短页、两列卡与堆叠表都读得开，仅余口径表末行换行造成的行高阶梯。
**中段未细看**：曲线 9 个点的数值标注（有无逐点文字）未逐点核对，只核了路径坐标与 X 轴标签。

---

---

## 页 26 · 看BMR报告

**源**：`.scratch/t387/html/26-看BMR报告.html`（2445 行；正文 L1824–L1868，L1844 长 17240 字符）
**实拍**：桌面 1440×900 fullPage（pageHeight 3131）＋ 手机 390×844 fullPage（pageHeight 4660）；crop 3 张（桌面 150–1000、1250–2150、2350–3131）

### 六问逐条

**① 第一眼／层级**——结论句 `窗口里有 30 天摄入低于基础代谢 1700 卡，长期如此会压低基础代谢`（L1844）→ 概览 4 卡（基础代谢 1700／每日总消耗 2635／低于基础代谢 30／达到基础代谢 0）→ 两枚 chip → 告警块 → 每日摄入曲线（含 1700 目标线）→ 30 行日期表 → 计算口径 6 行表 → 复制数据／复制日志 → 页脚 2 条 `caliber`。首屏给结论、图表给形状、表格给明细，顺序合理；**但同一句话在三处出现**（见④）。

**② 挤／空／错位**——
- 4 卡等高对齐、左右缘共线；第三节余下的告警块与曲线/表也是整宽块（`.ilife-block-data-table{margin:16px 0}` L1372、`.ilife-block-chart-block{L1500}`）。
- **告警块被做成表格**（结构错位）：`<section id="sec-danger">` 里是一张 `ilife-block-data-table`，`<th>⚠️ 危险信号</th>` ＋ 单格 `<td>连续多日摄入低于基础代谢，长期会压低基础代谢，建议把摄入提到基础代谢之上。</td>`——一句话占用「表头＋一格」的表格语义，桌面实拍就是一条带表头行的横幅。
- 读图称“1721 刻度被橙色目标线压住”：源码 `markline y1=y2=40.7`、刻度 `1721` 文本基线 `y=24`（`ilife-charts-tick`），相差约 17 用户单位，crop 复核**未见压字**——**源码未证实，不扣分**。
- 无横向溢出、无截断（双档实拍）。

**③ 分号／竖线／间隔号拼串**——可见正文无「；｜·」拼串；chips 承担并列信息（`低于基础代谢 30 天`／`告警线 3 天`）属「徽章」做法，合规。

**④ 写两遍／英文标识符**——
- **同一组事实三写**：卡面标签＋数值 `低于基础代谢`／`30`／`天`、卡内徽章 `<span class="ilife-status-badge ilife-status-badge-danger">低于基础代谢 30 天</span>`、网格后 `<span class="ilife-block-chip">低于基础代谢 30 天</span>`——**同一串字在一屏内印三遍**（L1844）。
- **阈值三写**：chip `告警线 3 天`、告警表 `caption` `低于基础代谢（3 天及以上即告警）`、口径表行 `危险信号判据｜窗口内摄入低于基础代谢的天数达到 3 天即告警`（L1844）。
- **结论复述**：`长期会压低基础代谢` 在结论句与告警正文各一次；`建议把摄入提到基础代谢之上` 只在告警出现（这条不重复）。
- **同一判据两个词**：指标卡副标 `Mifflin-St Jeor（四要素齐备才算）` vs 页脚 `基础代谢按 Mifflin-St Jeor 算式`／`四项齐备才算`——「四要素／四项」混用；**且口径表未列「体重」行**（公式 `10×体重 加 6.25×身高 减 5×年龄 加 5` 用到体重，表里只有 身高／年龄／性别／活动量档位），**「齐备」无法自证**。
- 符号混排（同一行内）：`10×体重 加 6.25×身高 减 5×年龄 加 5`——`×` 与「加／减」汉字混用。
- 英文：屏幕可见 `BMR`、`Mifflin-St Jeor`；日志浮层 `data-t` 内 `calorie.report.bmr`／`calorie_data.db ｜ calorie_data.db ｜ 报告子形态 BMR 报告`／`calorie-cmd-read …`（L1856–L1867，默认不上屏，与页 24／25 同源）。

**⑤ 数值列等宽／缺值／小数位**——
- 长表三列 `cell-left`／`cell-right`／`cell-right`（源码 L1844 表头），数值两列吃 `min-width:5.5em`＋`SF Mono`＋`tabular-nums`（L1419–1423），实拍右对齐 ✓。
- **单位位置不一致（源码可抄）**：第 2 列表头写 `当日摄入（卡）`、单元格只写数字 `1589`；第 3 列表头只写 `距基础代谢`、**每个单元格都带 `卡`**（`-111 卡` … 30 行 30 次「卡」）。
- **负号字形不统一**：本页差值用 ASCII `-`（`-111 卡`），页 25 口径行用 U+2212 `−`（`每日总消耗 − 日均摄入`）——同批两页同一含义两种字形。
- 无缺值单元格，本页未用到 `—`；整数列无小数位问题（1000 级整数 + `1700`／`2635` 一致）。

**⑥ 手机档／触摸目标／日期**——
- 390 档：4 卡按 `@media (max-width:820px)` 走两列（`grid-template-columns:repeat(2,1fr)`，L1838）；30 行表在 ≤640px 拆成「标签左／数值右」行；告警表格顺带拆成「⚠️ 危险信号／正文」两行，形态与 HELP 的卡片行同形。
- 触摸目标：共用底座 11 处 `min-height:44px`（两枚 chip 是 `<span>`，不可点）✓。
- 日期：`2026-08-18` … `2026-09-16` 30 条实拍完整未折断 ✓。
- 手机档整页 4660px（约 5.5 屏），主体是 30 行 ×3 行的堆叠表——比页 24 的 60 行轻，但仍是单一长清单。

### 本页缺陷（可抄源）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 26-1 | 「低于基础代谢 30 天」一屏内印三遍（卡面／徽章／chip） | L1844 | 大 |
| 26-2 | 3 天告警阈值三写（chip／表 caption／口径表行） | L1844 | 中 |
| 26-3 | 告警句用「表头＋单格」表格承载（结构错位） | `sec-danger` 的 `ilife-block-data-table` | 中 |
| 26-4 | 「四要素／四项」混用，且口径表缺「体重」行，齐备无法自证 | L1844 ＋ L1867 | 中 |
| 26-5 | 公式行 `×` 与「加／减」汉字混排 | L1844 | 小 |
| 26-6 | 单位位置不一致：第 2 列单位在表头、第 3 列每行重复「卡」 | L1844 | 中 |
| 26-7 | 负号字形不统一：本页 ASCII `-` vs 页 25 U+2212 `−` | L1844 vs 页 25 L1844 | 小 |
| 26-8 | 日志浮层含库表名＋「｜」串 | L1856–L1867 | 中（默认不上屏） |

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 82 | 16.4 |
| 信息层级与版式细调（权重 .25） | 78 | 19.5 |
| 分隔符与文字纪律（权重 .20） | 68 | 13.6 |
| 跨宽自适应（权重 .15） | 90 | 13.5 |
| 手机端与 HELP 同档（权重 .20） | 78 | 15.6 |
| **本页加权总分** | — | **78.6** |

**扣分口径**：结构 82＝扣 26-3（表格语义被当横幅用）与「数据与日志」整节只有复制按钮；信息 78＝扣 26-6／26-7 与 30 行表的单清单形态；文字 68＝扣 26-1（三写）、26-2（三写）、26-4（术语混用＋口径缺行）、26-5、26-8；跨宽 90＝无溢出、同一底座；手机 78＝堆叠与 44px 达标，但 4660px 全靠一条 30 行清单撑起，触达尾部成本高。
**中段未细看**：30 行日期表只逐条核了首 3 行与末 3 行的取值字形，未逐行核对 30 个差值是否与 1700 一致（抽样 1589−1700=−111 ✓、1531−1700=−169 ✓）。
**手机档读图不可靠声明**：本页手机档 `vision_describe` 输出出现多处源码不存在的文字（如「取件宝已接入」「结算区计算口含」），该次读图**整体作废**，手机档结论只采信「源码判读＋桌面 crop」。

---

---

## 页 27 · 看蛋白质摄入报告

**源**：`.scratch/t387/html/27-看蛋白质摄入报告.html`（2445 行；正文 L1824–L1868）
**实拍**：桌面 1440×900 fullPage（pageHeight 2588）＋ 手机 390×844 fullPage（pageHeight 3942）；crop 3 张（桌面 150–1100、1100–2000、手机 350–1300）

### 六问逐条

**① 第一眼／层级**——结论句 `30 天有蛋白记录，日均 68 g，目标 150 g，达标 0 天`（L1844）→ 概览 4 卡（日均蛋白 68／目标 150／达标天数 0／有记录天数 30）→ 每日蛋白量曲线 → 30 行逐日表 → 两枚 chip（`达标 0 天`／`未达标 30 天`）→ 复制数据／复制日志 → 页脚 3 条 `caliber`。层级顺序同族正常，**但明细层零信息量**（见②④）。

**② 挤／空／错位**——
- **曲线图上半整块空置（源码可算）**：数据 `path d="M46.0 200.5 … L566.0 200.5"`（9 点同值），目标虚线 `markline y=40.5`，绘图区 y∈[21,220]——两线相距 160 用户单位＝绘图区高度的 **80%** 全空；实拍即「底部一条平线＋顶部一条虚线，中间一大片空白」。
- 30 行表行高均匀、四卡等高对齐、块左缘共线；无溢出、无截断。
- 读图称“顶部公告条被截断”：源码结论句是单行 `<p class="ilife-block-conclusion">`，crop 未见截断——**源码未证实，不扣分**。

**③ 分号／竖线／间隔号拼串**——可见正文无「；｜·」拼串；结论句用逗号连四个并列事实（`30 天有蛋白记录，日均 68 g，目标 150 g，达标 0 天`），**未用禁列符号**，但它与紧下方的 4 张卡重复（见④）。

**④ 写两遍／英文标识符**——
- **同一行事实写 30 遍（本批最极端）**：逐日表 30 行取值**唯一值只有 `68`**（`data-label="蛋白（g）">68<` ×30，distinct=68）与**唯一值 `未达标`**（`data-label="是否达标">未达标<` ×30，distinct=未达标）——30 行同值同状态，一页把同一件事印了 30 遍。
- **`30 天` 四写**：结论句 `30 天有蛋白记录`、卡 1 副标 `有记录 30 天`、卡 3 副标 `达标率 0%，共 30 天有记录`、卡 4 `有记录天数 30 天／窗口共 30 天`（L1844）。
- **`达标 0 天` 三写**：结论句、卡 3 数值、表下 chip `达标 0 天`；`未达标 30 天` 亦在 chip 再写一次。
- **页脚样板句**：`目标取目标设置里那一项` 与卡 2 副标 `来自目标设置` 重复；`没有设目标时不判达标` 在本页（已有 150 g 目标）是**永不触发的条件句**。
- 英文：屏幕可见仅 `g`（单位）与日期；日志浮层 `data-t` 内 `calorie.report.protein`／`calorie_data.db ｜ calorie_data.db ｜ 报告子形态 蛋白质摄入报告`／`calorie-cmd-read …`（L1856–L1867，默认不上屏，同族同源）。

**⑤ 数值列等宽／缺值／小数位**——
- 表头 `日期(cell-left)｜蛋白（g）(cell-right)｜是否达标(cell-right)`（源码逐字，L1844）；蛋白列吃 `cell-right` 的 `min-width:5.5em`＋`SF Mono`＋`tabular-nums`（L1419–1423），实拍右对齐 ✓、30 行 `68` 上下对齐 ✓。
- **中文状态列套了数值列的规则**：`<td class="ilife-block-data-table-cell-right" data-label="是否达标">未达标</td>`——中文「未达标」被 `cell-right` 的 `font-family:"SF Mono",monospace` 接管，且三字与「达标」二字等宽右对齐后**左缘必然参差**（页 28 同款，已实拍复核）。
- **状态不用徽章**：`是否达标` 列 30 行都是纯文字 `未达标`，而同页表下却用 chip `未达标 30 天` 表示同一状态——**同页同一状态两种呈现**（表格文字 vs 胶囊），按⑤「应改徽章/图例」计。
- 无缺值单元格，未用到 `—`；`68`／`150`／`0` 均整数，无小数位混排问题。

**⑥ 手机档／触摸目标／日期**——
- 390 档：4 卡两列（`@media (max-width:820px)` `grid-template-columns:repeat(2,1fr)`，L1838），实拍 **2×2**（读图两次给出互斥说法「1 列/2 列」，以源码与 crop 为准＝2 列）；表在 ≤640px 拆成「标签左／数值右」三行一组（日期／蛋白（g）／是否达标），每组约 70–80px，30 组 ≈ 2000–2200px。
- 触摸目标：共用底座 11 处 `min-height:44px` ✓；读图称“Tab 胶囊 36–40px 不足 44px”，对象是 `<span class="ilife-block-chip">`（不可点）或 `.ilife-block-toc a`（源码 L1159–1164 已 `min-height:44px`）——**源码未证实，不扣分**。
- 日期：30 条 `2026-08-18`…`2026-09-16` 实拍完整未折断 ✓。
- 整页 3942px ≈ 4.7 屏，其中约一半是 30 行同值清单。

### 本页缺陷（可抄源）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 27-1 | 逐日表 30 行同值同状态（`68` ×30、`未达标` ×30），一页把同一件事写 30 遍 | L1844 取值 distinct 集合 | 大 |
| 27-2 | 退化输入无降级：9 点同值仍画平线，绘图区 80% 空置 | `path d="M46.0 200.5 …"` vs `markline y=40.5` | 大 |
| 27-3 | `30 天` 四写、`达标 0 天` 三写（结论／卡／chip 互相重复） | L1844 | 中 |
| 27-4 | 状态呈现同页两制：表格纯文字 `未达标` vs 表下 chip `未达标 30 天` | L1844 | 中 |
| 27-5 | 页脚样板句重复卡面（`目标取目标设置里那一项`）＋永不触发的条件句 | L1844 ↔ L1867 | 小 |
| 27-6 | 日志浮层含库表名＋「｜」串 | L1856–L1867 | 中（默认不上屏） |

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 84 | 16.8 |
| 信息层级与版式细调（权重 .25） | 66 | 16.5 |
| 分隔符与文字纪律（权重 .20） | 72 | 14.4 |
| 跨宽自适应（权重 .15） | 90 | 13.5 |
| 手机端与 HELP 同档（权重 .20） | 72 | 14.4 |
| **本页加权总分** | — | **75.6** |

**扣分口径**：结构 84＝四节齐、无错位，扣「数据与日志」整节只有复制按钮与明细表零信息量却立整节；信息 66＝主扣 27-1／27-2（明细占 45% 高度、图表占 25% 高度，两处加起来七成版面不承载新信息）；文字 72＝扣 27-3／27-4／27-5／27-6；跨宽 90＝无溢出、同一底座；手机 72＝堆叠与 44px 达标，但 4.7 屏里一半是同值清单。
**口径说明**：27-1／27-2 有「输入退化（30 天都 68 g）」的客观成分——若认定这是**数据问题而非页面问题**，本页信息维应上调到 71、手机维上调到 75，加权总分为 **78.6**；本席按「页面未为退化输入设计降级呈现」记原分。
**中段未细看**：30 行只核了取值去重集合（`68`／`未达标`）与首末行，未逐行核 9 个图上点与 30 行日期的对应关系。

---

---

## 页 28 · 看水分摄入报告

**源**：`.scratch/t387/html/28-看水分摄入报告.html`（2445 行；正文 L1824–L1868）
**实拍**：桌面 1440×900 fullPage（pageHeight 2588）＋ 手机 390×844 fullPage（pageHeight 3942）；crop 3 张（桌面 550–1250、1250–1900、手机 300–1250）

### 六问逐条

**① 第一眼／层级**——结论句 `30 天有饮水记录，日均 2000 ml，目标 2000 ml，达标 20 天`（L1844）→ 概览 4 卡（日均饮水 2000／目标 2000／达标天数 20／有记录天数 30）→ 每日饮水量曲线 → 30 行逐日表 → 两枚 chip（`达标 20 天`／`未达标 10 天`）→ 复制数据／复制日志 → 页脚 3 条 `caliber`。**与页 27 同一副骨架**（同页高 2588／3942），差别只在数据与文案。

**② 挤／空／错位**——
- 曲线有真实起伏（`path d="M58.0 40.3L121.5 120.5L185.0 120.5L248.5 200.7L312.0 40.3 …"`，9 点取 2200/2000/1800 三档），绘图区利用率优于页 27。
- **目标虚线与中线重合**：`markline y1=y2=120.5`，与中间网格线**同一条 y**（`ilife-charts-grid y=120.5`）——橙虚线叠在灰网格线上，实拍勉强可辨（读图复核确认「两条线并排、并非真正重合」），记为**版式瑕疵（小）**。
- 无溢出、无截断；四卡等高、块左缘共线。

**③ 分号／竖线／间隔号拼串**——可见正文无「；｜·」拼串；结论句逗号连四事（`30 天有饮水记录，日均 2000 ml，目标 2000 ml，达标 20 天`），其中 `2000 ml` 连写两遍（日均＝目标）。

**④ 写两遍／英文标识符**——
- **明细 30 行是 3 值×10 轮循环**（`distinct(饮水)=1800,2000,2200`；序列 `2200 2000 1800` 重复 10 次）——第 4 行起每一行都在复述前三行已给过的事实；`distinct(是否达标)=达标,未达标`，计数 20／10。
- **`30 天有记录` 四写**：结论句、卡 1 副标 `有记录 30 天`、卡 3 副标 `达标率 66.7%，共 30 天有记录`、卡 4 `有记录天数 30 天／窗口共 30 天`（L1844）。
- **`达标 20 天` 三写**：结论句、卡 3 数值 `20 天`、表下 chip `达标 20 天`（`未达标 10 天` 亦在 chip 出现，而表内 10 行已逐条给过）。
- **页脚样板句与卡面重复**：`目标取目标设置里那一项` ↔ 卡 2 副标 `来自目标设置`；`没有设目标时不判达标` 在本页（已有 2000 ml 目标）**永不触发**。
- 英文：屏幕可见 `ml`（单位）与日期；日志浮层 `data-t` 内 `calorie.report.water`／`calorie_data.db ｜ calorie_data.db ｜ 报告子形态 水分摄入报告`／`calorie-cmd-read …`（L1856–L1867，默认不上屏，同族同源）。

**⑤ 数值列等宽／缺值／小数位**——
- 表头 `日期(cell-left)｜饮水（ml）(cell-right)｜是否达标(cell-right)`；饮水列右对齐、`SF Mono`＋`tabular-nums`（L1419–1423）✓；单元格纯数字、单位已在表头 ✓。
- **中文状态列吃数值列规则**：`<td class="ilife-block-data-table-cell-right" data-label="是否达标">达标</td>`——等宽字体＋右对齐，`达标`（2 字）与 `未达标`（3 字）**左缘参差**（crop 实拍可见，右缘齐、左缘不齐），同页也没有用徽章／颜色区分两种状态。
- **跨页小数位不一致**：同一字段「达标率」在页 27 渲染为 `0%`、在本页为 `66.7%`（L1844）——一位小数与整数形混排。
- 无缺值单元格，未用到 `—`；刻度值 `1752／2000／2248` 非成档整数（同族刻度通病，见收口）。

**⑥ 手机档／触摸目标／日期**——
- 390 档：4 卡两列（`@media (max-width:820px)` L1838）、表在 ≤640px 拆成「标签左／数值右」三行一组（crop 复核）；无横向溢出、无截断。
- 触摸目标：共用底座 11 处 `min-height:44px`；chip 为 `<span>` 不可点。读图称“过滤 chip 约 40px 不足 44px”——本页 `<span class="ilife-block-chip">` 共 5 枚（`卡路里`／`水分摄入`／`健康报告`／`达标 20 天`／`未达标 10 天`）**均为纯展示**——**源码未证实，不扣分**。
- 日期：30 条 `2026-08-18`…`2026-09-16` 完整未折断 ✓；整页 3942px ≈ 4.7 屏，约一半是 30 行循环清单。

### 本页缺陷（可抄源）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 28-1 | 明细 30 行＝3 值×10 轮循环，第 4 行起全是复述 | L1844（distinct=1800,2000,2200） | 中 |
| 28-2 | `30 天有记录` 四写、`达标 20 天` 三写 | L1844 | 中 |
| 28-3 | 中文状态列套数值列规则（等宽＋右对齐），`达标／未达标` 左缘参差，且无徽章区分状态 | L1844 ＋ L1419–1423 | 中 |
| 28-4 | 目标虚线与中线网格重合（同 y=120.5） | `markline y=120.5` vs `grid y=120.5` | 小 |
| 28-5 | 「达标率」跨页小数位不一致（页 27 `0%` vs 本页 `66.7%`） | 页 27 L1844 ↔ 本页 L1844 | 小 |
| 28-6 | 页脚样板句重复卡面＋永不触发的条件句 | L1844 ↔ L1867 | 小 |
| 28-7 | 日志浮层含库表名＋「｜」串 | L1856–L1867 | 中（默认不上屏） |

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 84 | 16.8 |
| 信息层级与版式细调（权重 .25） | 74 | 18.5 |
| 分隔符与文字纪律（权重 .20） | 72 | 14.4 |
| 跨宽自适应（权重 .15） | 90 | 13.5 |
| 手机端与 HELP 同档（权重 .20） | 74 | 14.8 |
| **本页加权总分** | — | **78.0** |

**扣分口径**：结构 84＝同页 27（四节齐、扣「数据与日志」只有复制按钮）；信息 74＝扣 28-1／28-3／28-4（比页 27 好一档：图表有起伏、表明细有 3 档值）；文字 72＝扣 28-2／28-5／28-6／28-7；跨宽 90；手机 74＝4.7 屏、一半是循环清单，堆叠与 44px 达标。
**中段未细看**：30 行只核了取值去重集合与前 12 行序列，未逐行核 30 条日期与 9 个图上点的对应。
**手机档读图不可靠声明**：本页手机档 `vision_describe` 输出含源码不存在的文字（「2019-09-15」「碳水化合物:较少」「1000–2200ml」等），该次读图**整体作废**，手机档结论只采信「源码判读＋手机 crop」。

---

---

## 页 29 · 看综合评分

**源**：`.scratch/t387/html/29-看综合评分.html`（2445 行；正文 L1824–L1868）
**实拍**：桌面 1440×900 fullPage（pageHeight 3123）＋ 手机 390×844 fullPage（pageHeight 4010）；crop 2 张（桌面 100–1600、手机 100–1400）

### 六问逐条

**① 第一眼／层级**——结论句 `综合评分 68.4 分（满分 100，六因素等距折算）`（L1844）→ **圆弧仪表盘**（`data-chart-kind="gauge"`，`viewBox="0 0 170.0 105.0"`，`data-pct="68.4"`，弧内 `<text class="ilife-charts-gauge-value">68 分</text>`）→ 3 张小卡（最近一天 67／最低分项 蛋白达标／最高分项 三餐齐备）→ 分项分数表 6 行 → 评分历史 30 行 → 复制数据／复制日志 → 页脚 2 条 `caliber`。本页**唯一用仪表盘的页**，骨架其余同族。

**② 挤／空／错位**——
- **仪表盘在宽屏被等比放大到不成比例（源码可算＋实拍复核）**：CSS 只有 `.ilife-charts-svg{display:block;width:100%;height:auto}`（L518），**`grep -i gauge` 全文件只命中三条**（L552 `.ilife-charts-gauge-value{font-size:26px}`、L553 `.ilife-charts-gauge-label{font-size:11px}`、L1844 用法），**没有任何 max-width／max-height 约束**。1440 档内容宽 1120−40−28＝1052px → SVG 高 ＝1052×105/170 ≈ **650px**，即设计尺寸的约 6.2 倍；而弧线用 `vector-effect:non-scaling-stroke` 把描边钉在 **12px**，粗/半径比从设计值 12/81＝**14.8%** 掉到约 12/500＝**2.4%**（整圈退化成发丝）。读图复核同向：「弧身很细」「弧被横向拉宽」「四周大片空白」。
- 三张小卡等高；卡 2／卡 3 底部的状态徽章把卡片撑高，另卡底部空一行（同页 25 同款）。
- 无横向溢出、无截断。

**③ 分号／竖线／间隔号拼串**——可见正文无「；｜·」拼串；**页脚把两条并列公式塞进同一个 `<p class="ilife-block-caliber">`**（两个 `<span>` 中间一个空格）：`分项命中率＝该项命中天数 ÷ 有记录天数` ＋ `综合评分＝六因素命中数与项数之比折算成 0–100 分，不另算第二套权重`（L1867）——按⑤口径应分行或做成可见结构。

**④ 写两遍／英文标识符**——
- **徽章复述卡面**：卡 2 主值 `蛋白达标` ＋ 副标 `命中率 0%（优先改它）`，徽章又写 `<span class="ilife-status-badge ilife-status-badge-warn">蛋白达标 0%</span>`；卡 3 同款（`三餐齐备` ＋ `命中率 100%` ＋ 徽章 `三餐齐备 100%`）——**同一事实在一张卡里写两遍**。
- **括注逐字重复**：结论句 `（满分 100，六因素等距折算）` 与仪表盘标题 `综合评分（满分 100，六因素等距折算）` 完全相同的一句写两遍。
- **内部口径口吻上屏**：`不另算第二套权重`（L1867）——是解释「为何不加权」的内部说明，不是用户事实。
- 英文：屏幕可见无英文单词（仅日期与百分号）；日志浮层 `data-t` 内 `calorie.report.score`／`calorie_data.db ｜ calorie_data.db ｜ 报告子形态 综合评分`／`calorie-cmd-read …`（L1856–L1867，默认不上屏，同族同源）。

**⑤ 数值列等宽／缺值／小数位**——
- 分项分数表表头 `分项（六因素）(cell-left)｜命中天数(cell-right)｜有记录天数(cell-right)｜命中率(cell-right)`；数值列右对齐 ✓、`SF Mono`＋`tabular-nums` ✓。
- **命中率列列内小数位不一致（源码逐字）**：`0%`／`50%`／`66.7%`／`100%`／`100%`／`100%`——同一列整数形与一位小数混排。
- **主数值三个口径互不相等（本页最重的一处）**：结论句 `68.4`、仪表盘显示 `68 分`（其 `data-pct` 又是 `68.4`）、**页脚自称公式**「综合评分＝六因素命中数与项数之比折算成 0–100 分」→ 表内命中天数 0+15+20+30+30+30＝**125**，有记录天数 30×6＝**180**，125/180＝**69.4%**；30 行评分历史实算均值 2085/30＝**69.5**。**68／68.4／69.4／69.5 四个数在同页互不相等，页内无法自证。**
- 无缺值单元格，未用到 `—`。

**⑥ 手机档／触摸目标／日期**——
- 390 档：3 卡走 `@media (max-width:820px)` 两列（L1838，末位通栏）；两张表在 ≤640px 拆「标签左／数值右」行；仪表盘在 390 档按 330px 内容宽算高约 204px（比例比桌面档接近设计值）。实拍无横向溢出、无截断。
- 触摸目标：共用底座 11 处 `min-height:44px`；chip 为 `<span>` 不可点。
- 日期：30 条 `2026-08-18`…`2026-09-16` 完整未折断 ✓。
- 整页 4010px ≈ 4.8 屏，其中 30 行评分历史约 4 屏里的一半，**无折叠入口**（`<details` 0 个）。

### 本页缺陷（可抄源）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 29-1 | 仪表盘无尺寸约束：宽屏放大到约 1052×650（设计 6.2 倍），描边被 `non-scaling-stroke` 钉死 12px → 粗/半径比 14.8%→2.4%（发丝弧） | L518 ＋ gauge 段（仅 L552/553 两条字号规则） | 大 |
| 29-2 | 主数值四口径互不相等：68／68.4／69.4（页脚公式实算）／69.5（历史均值） | L1844（结论／gauge／表）＋ L1867 | 大 |
| 29-3 | 命中率列列内小数位不一致（`0%`／`50%`／`66.7%`／`100%`） | L1844 | 中 |
| 29-4 | 徽章复述卡面（`蛋白达标`＋`蛋白达标 0%`；`三餐齐备`＋`三餐齐备 100%`） | L1844 | 中 |
| 29-5 | 同一句括注写两遍（结论句与仪表盘标题） | L1844 | 小 |
| 29-6 | 页脚把两条并列公式塞进同一行 `<p>`；含内部口吻「不另算第二套权重」 | L1867 | 中 |
| 29-7 | 评分历史 30 行只在 3 个值（50／67／83）间按 7 周期循环 | L1844（distinct=50,67,83） | 中 |
| 29-8 | 日志浮层含库表名＋「｜」串 | L1856–L1867 | 中（默认不上屏） |

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 84 | 16.8 |
| 信息层级与版式细调（权重 .25） | 70 | 17.5 |
| 分隔符与文字纪律（权重 .20） | 74 | 14.8 |
| 跨宽自适应（权重 .15） | 80 | 12.0 |
| 手机端与 HELP 同档（权重 .20） | 76 | 15.2 |
| **本页加权总分** | — | **76.3** |

**扣分口径**：结构 84＝四节齐、图＋卡＋两表顺序合理，扣「数据与日志」只有复制按钮；信息 70＝扣 29-2／29-3／29-4／29-7；文字 74＝扣 29-5／29-6／29-8；跨宽 80＝主扣 29-1（同一份 CSS 在别的页没问题，坏在 gauge 这一格无约束）；手机 76＝堆叠与 44px 达标，4.8 屏无折叠。
**中段未细看**：分项分数表只逐字核了前 3 行与命中率整列，未核后 3 行的命中天数与命中率是否逐行相乘一致（抽验 0/30=0% ✓、15/30=50% ✓、20/30=66.7% ✓）。

---

---

## 页 30 · 看健康趋势

**源**：`.scratch/t387/html/30-看健康趋势.html`（2445 行；正文 L1824–L1868）
**实拍**：桌面 1440×900 fullPage（pageHeight 4191）＋ 手机 390×844 fullPage（pageHeight 5673）；crop 3 张（桌面 150–1000、3400–4191、手机 150–1100）

### 六问逐条

**① 第一眼／层级**——结论句 `变化方向 平稳，前段均分 70.1 到后段均分 69.3，序列里有 30 个拐点`（L1844）→ 4 卡（变化方向／前段均分／后段均分／拐点数）→ 评分走势折线 → 评分序列 60 行 → 口径说明 4 行表 → 复制数据／复制日志 → 页脚 2 条 `caliber`。顺序合理；**问题在数字自身说不通**（见⑤）。

**② 挤／空／错位**——
- 无溢出、无截断；卡等高，曲线卡片与两表左缘共线。
- 读图称“TOC 五项在 390 折成两行”——`.ilife-block-toc{display:flex;flex-wrap:wrap}`（L1153–1158）本就允许折行，**不构成缺陷**。
- 桌面档 60 行序列占整页约 55%（读图复核）、曲线约 14%——**图表与明细比例倒挂**。

**③ 分号／竖线／间隔号拼串**——可见正文无「；｜·」拼串；口径说明表把「一项判据＋一句解释」放进同一格（`前段与后段｜把窗口按天三等分，取首段与末段的评分均值比较`），属正常键值表。

**④ 写两遍／英文标识符**——
- **卡内徽章复述主值**：卡 1 主值 `平稳` ＋ 徽章 `<span class="ilife-status-badge ilife-status-badge-empty">平稳</span>`（L1844）——同一词写两遍。
- **结论句复述四张卡**：`变化方向 平稳，前段均分 70.1 到后段均分 69.3，序列里有 30 个拐点` 把 4 张卡的主值一次抄完（L1844）。
- **口径表 ↔ 页脚整段重复**：表行 `前段与后段｜把窗口按天三等分，取首段与末段的评分均值比较` ↔ 页脚 `前段与后段＝把窗口按天三等分后取首段与末段`；表行 `上升与下降｜后段减前段大于 1 分即上升…` ↔ 页脚 `变化量超过 1 分才算上升或下降，其余记平稳`（L1844 ↔ L1867）。
- **内部口吻上屏**：口径行 `（不另算一套权重）`（L1844）。
- 英文：屏幕可见无英文单词；日志浮层 `data-t` 内 `calorie.report.trend`／`calorie_data.db ｜ calorie_data.db ｜ 报告子形态 健康趋势`／`calorie-cmd-read …`（L1856–L1867，默认不上屏，同族同源）。

**⑤ 数值列等宽／缺值／小数位**——
- 序列表 `日期(cell-left)｜评分(cell-right)`（右对齐、等宽、`tabular-nums`）✓；口径表两列皆 `cell-left`。
- **本页最重的一处：口径文案、页头窗口、实算三者互不吻合。** 页头写 `2026-06-19 至 2026-09-16（共 90 天）`，序列表实为 **60 行**（`2026-07-19` … `2026-09-16`，实测 60 行、取值仅 `50/67/83`，全序列均值 **69.5**）。卡片副标写「窗口前 1/3 天的评分均值」，而卡片值 `70.1`／`69.3` **只有按 60 天序列取前/后 20 天才能复现**（本席实算：前 20＝**70.05**、后 20＝**69.25**；若按页头 90 天窗口的 30 天三等分，则为 **69.5／69.5**，与卡面不符）。
- **拐点数 30 经复算成立**（按页内定义「高于或低于左右相邻两天」逐点复算＝**30**，与卡面一致）——正面证据。
- 无缺值单元格，未用到 `—`；刻度 `46／67／88` 非成档整数（同族刻度通病）。

**⑥ 手机档／触摸目标／日期**——
- 390 档：4 卡两列（L1838）；60 行表在 ≤640px 拆「标签左／数值右」；TOC 折两行；实拍无横向溢出、无截断，曲线 9 个标签可辨未重叠。
- 触摸目标：共用底座 11 处 `min-height:44px`；chip 为 `<span>` 不可点。
- 日期：60 条 `2026-07-19`…`2026-09-16` 完整未折断 ✓。
- 整页 5673px ≈ **6.7 屏**（本批最长），其中 60 行序列约占一半，**无折叠入口**（`<details` 0 个）。

### 本页缺陷（可抄源）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 30-1 | 口径文案／页头窗口／实算三者不符：卡面 `70.1`／`69.3` 只有按 60 天序列取前后 20 天才能复现；页头却写「共 90 天」、口径写「窗口按天三等分」 | L1844（页头／卡／口径表）＋ 本席实算 | 大 |
| 30-2 | 口径说明表 2 行与页脚 2 条整段重复（同一判据两种写法） | L1844 ↔ L1867 | 中 |
| 30-3 | 卡内徽章与主值同词重复（`平稳` ×2） | L1844 | 小 |
| 30-4 | 结论句把 4 张卡主值整句复述 | L1844 | 中 |
| 30-5 | 60 行序列＝3 值循环（`50/67/83`），占整页约 55%，无折叠 | L1844（distinct=50,67,83） | 中 |
| 30-6 | 「不另算一套权重」内部口吻上屏 | L1844 | 小 |
| 30-7 | 日志浮层含库表名＋「｜」串 | L1856–L1867 | 中（默认不上屏） |

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 86 | 17.2 |
| 信息层级与版式细调（权重 .25） | 70 | 17.5 |
| 分隔符与文字纪律（权重 .20） | 74 | 14.8 |
| 跨宽自适应（权重 .15） | 90 | 13.5 |
| 手机端与 HELP 同档（权重 .20） | 74 | 14.8 |
| **本页加权总分** | — | **77.8** |

**扣分口径**：结构 86＝五节齐、无错位，扣「数据与日志」只有复制按钮与口径表自重复；信息 70＝主扣 30-1（数字对不上）／30-5（比例倒挂）；文字 74＝扣 30-2／30-3／30-4／30-6／30-7；跨宽 90＝无 gauge，同一底座；手机 74＝6.7 屏、60 行无折叠，堆叠与 44px 达标。
**中段未细看**：60 行序列只核了取值集合、前后 20/30 天均值与拐点数，未逐行核 9 个图上点与 60 行的抽样对应。
**手机档读图不可靠声明**：本页手机档 `vision_describe` 输出为**股市行情页内容**（「A 股分析师」「涨停」等），与产物毫无关系，该次读图**整体作废**，手机档结论只采信「源码判读＋手机 crop」。

---

---

## 页 31 · 看健康报告(含对比)

**源**：`.scratch/t387/html/31-看健康报告(含对比).html`（2445 行；正文 L1824–L1868）
**实拍**：桌面 1440×900 fullPage（pageHeight 4375）＋ 手机 390×844 fullPage（pageHeight 3771）；crop 3 张（桌面 150–1050、3600–4375、手机 150–1100）

### 六问逐条

**① 第一眼／层级**——结论句 `本期 2026-09-10 至 2026-09-16，对比期是本窗口之前同样长的 7 天（2026-09-03 至 2026-09-09）`（L1844）→ 4 卡（本期日均摄入 1598.7／本期日均体重 75.2／记录天数 7／前 3 项变化量 -28.6 ml）→ 8 行 Δ 表 → 三枚 chip（`上升 2 项`／`下降 3 项`／`持平 3 项`）→ 前 3 项变化量 3 行表 → **五张整宽折线**（摄入／体重／运动消耗／饮水／缺口）→ 口径说明 4 行表 → 复制数据／复制日志 → 页脚 3 条 `caliber`。两期对比的骨架清楚，`上升 2／下降 3／持平 3` 与 Δ 列逐项复算**完全一致**（正面证据）。

**② 挤／空／错位**——
- **「迷你图」名不副实（源码可算）**：口径表写 `各维各画一张迷你折线（小倍数图）`，实际 5 张图 `viewBox` **全是 `0 0 580.0 260.0`**（与单图页同款），在 1052px 内容宽下每张渲染约 **1052×471**，5 张合计≈**2355px＝整页 4375px 的 54%**——不是小倍数图，是 5 张整宽大图竖排。
- 退化输入照画：`运动消耗` 两期都是 `300.0`（Δ `0.0`），仍占一张约 470px 的整宽平线图。
- 卡 4 徽章把卡片撑高，另 3 卡等高后底部空一行（同族同款）。
- 无横向溢出、无截断。

**③ 分号／竖线／间隔号拼串**——可见正文无「；｜·」拼串；Δ 表四列结构清楚。

**④ 写两遍／英文标识符**——
- **口径表 ↔ 页脚三条整段重复**（本页最重）：表 `对比期怎么取｜紧邻本期之前，与本期的天数一样长的那一段窗口（本期 7 天）` ↔ 页脚 `对比期＝紧邻本期之前，同样长的那一段窗口`；表 `变化量 Δ｜本期值减对比期值（正数＝本期更高）` ↔ 页脚 `变化量＝本期值减对比期值，正数代表本期更高`；表 `日均总消耗怎么算｜两期同源：…体重有变，这一行就会变，不是与体重无关的固定值。` ↔ 页脚 `日均总消耗两期同源：…体重有变这一行就会变`（L1844 ↔ L1867）。
- **同一个数写五遍**：`-28.6` 出现在 Δ 表 `日均饮水` 行、前 3 项变化量表 `-28.6 ml`、卡 4 主值 `-28.6 ml`、卡 4 徽章 `下降 -28.6 ml`、卡 4 副标所指的 `日均饮水`（L1844）。
- **同一组值两种写法**：Δ 表 Δ 列无单位（`-28.6`），前 3 项变化量表带单位（`-28.6 ml`）——同一数一页两种呈现。
- **卡 4 标签与取值不匹配**：标签 `前 3 项变化量`（一个集合），取值 `-28.6 ml`（单项最大变化）。
- **内部口吻／辩解句上屏**：口径表 `…不是与体重无关的固定值。`、`…不把不同单位压到同一条轴上`（L1844）——都是设计取舍自证，不是用户事实。
- `四要素` **第三次出现且本页仍无定义**（口径表 4 行是 对比期怎么取／变化量 Δ／日均总消耗怎么算／五维走势，没有任何一行列出四要素或体重口径）；`Mifflin-St Jeor` 为域内专名；日志浮层 `data-t` 内 `calorie.report.compare`／`calorie_data.db ｜ calorie_data.db ｜ 报告子形态 健康报告（含对比）`／`calorie-cmd-read …`（L1856–L1867，默认不上屏）。

**⑤ 数值列等宽／缺值／小数位**——
- Δ 表 `项(cell-left)｜本期(cell-right)｜对比期(cell-right)｜变化量 Δ(cell-right)`：数值三列右对齐、`SF Mono`＋`tabular-nums` ✓；全部一位小数、Δ 列正负号齐（`+15.8／-4.0／+27.0／-0.4／0.0／-28.6／0.0／0.0`）✓。
- **同页同指标两种精度**：卡面 `记录天数 7 天`（`<span class="ilife-block-kpi-card-value">7</span><span class="ilife-block-kpi-card-unit">天</span>`）vs Δ 表 `记录天数 7.0`（L1844）。
- **同一纵轴三种刻度写法**：体重图刻度 `75`／`75.25`／`75.5`（整数／两位小数／一位小数混排）。
- **缺口列与另两列不能互算（源码数字自证）**：本页 `日均缺口 1207.7` vs `日均总消耗 2635.0 − 日均摄入 1598.7 ＝ 1036.3`（差 **171.4**）；对比期 `1180.7` vs `2639.0 − 1582.9 ＝ 1056.1`（差 **124.6**）——两期差额不同，且**本页口径表与页脚都没有「缺口口径」行**（页 25 同族页有 `静态缺口 ＝ 每日总消耗 − 日均摄入`），故本页无法自证。**差额成因源码未证实，不作扣分依据**，但记为本页观察项，供返修时对账。
- 无缺值单元格，未用到 `—`。

**⑥ 手机档／触摸目标／日期**——
- 390 档：4 卡两列（L1838）；Δ 表在 ≤640px 拆「标签左／数值右」三行一组，实测无溢出；5 张折线各自整宽（约 330×148），合计约 740px。
- 触摸目标：共用底座 11 处 `min-height:44px`（含 `.ilife-block-toc a`）；chip 为 `<span>` 不可点。
- 日期：Δ 表不含日期列；X 轴 7 个日期 `09-10`…`09-16` 实拍完整未折断；页头 `共 7 天` 与结论句的对比期 `09-03 至 09-09` 自洽 ✓。
- 整页 3771px ≈ 4.5 屏；无折叠入口（`<details` 0 个，但本页清单只有 8 行，不构成负担）。

### 本页缺陷（可抄源）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 31-1 | 口径表 3 行与页脚 3 条同义重复（写法还不同） | L1844 ↔ L1867 | 大 |
| 31-2 | 「迷你折线（小倍数图）」实为 5 张整宽大图（各约 1052×471，合计占整页 54%） | L1844（口径行）＋ 5×`viewBox="0 0 580.0 260.0"` | 中 |
| 31-3 | 同一个数 `-28.6` 一页写五遍；Δ 表无单位与前 3 项表带单位两种呈现 | L1844 | 中 |
| 31-4 | 卡 4 标签「前 3 项变化量」与取值（单项最大变化）不匹配 | L1844 | 中 |
| 31-5 | 同页同指标两种精度：`7 天` vs `7.0` | L1844 | 小 |
| 31-6 | 体重图纵轴三种刻度写法：`75`／`75.25`／`75.5` | L1844 | 小 |
| 31-7 | 缺口列与消耗/摄入不能互算，且本页无缺口口径行 | L1844（数字）／口径表 4 行 | 观察项（成因源码未证实，不扣分） |
| 31-8 | 内部口吻上屏 2 处（`不是与体重无关的固定值`／`不把不同单位压到同一条轴上`） | L1844 | 中 |
| 31-9 | 运动消耗两期同为 `300.0`（Δ 0.0）仍占一张整宽平线图 | L1844 | 小 |
| 31-10 | `四要素` 第三次出现仍无定义（页 25／26 已记） | L1844 | 中 |
| 31-11 | 日志浮层含库表名＋「｜」串 | L1856–L1867 | 中（默认不上屏） |

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 88 | 17.6 |
| 信息层级与版式细调（权重 .25） | 72 | 18.0 |
| 分隔符与文字纪律（权重 .20） | 72 | 14.4 |
| 跨宽自适应（权重 .15） | 90 | 13.5 |
| 手机端与 HELP 同档（权重 .20） | 78 | 15.6 |
| **本页加权总分** | — | **79.1** |

**扣分口径**：结构 88＝六节齐、Δ 表与 chip 计数自洽，扣「前 3 项变化量」整节是 Δ 表的子集重排与「数据与日志」整节只有复制按钮；信息 72＝扣 31-2／31-4／31-5／31-6／31-9（31-7 只记不对账）；文字 72＝扣 31-1／31-3／31-8／31-10／31-11；跨宽 90＝无 gauge、同一底座、5 图在 390 可读；手机 78＝4.5 屏、表格可读、无溢出。
**中段未细看**：五张折线只核了摄入/体重/运动消耗三张的刻度与首末点，饮水与缺口两张只核了刻度值；Δ 表只逐行核了 6 行（前 6 行＋运动/记录天数两行的取值格式）。

---

---

## 收口

### 1 · 八页加权总分（固定尺复算）

| 页 | 页名 | 结构纪律 .20 | 信息层级 .25 | 分隔符与文字 .20 | 跨宽自适应 .15 | 手机端同档 .20 | **加权总分** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 24 | 看BMI报告 | 88 | 72 | 78 | 90 | 74 | **79.5** |
| 25 | 看TDEE报告 | 90 | 82 | 74 | 90 | 86 | **84.0** |
| 26 | 看BMR报告 | 82 | 78 | 68 | 90 | 78 | **78.6** |
| 27 | 看蛋白质摄入报告 | 84 | 66 | 72 | 90 | 72 | **75.6** |
| 28 | 看水分摄入报告 | 84 | 74 | 72 | 90 | 74 | **78.0** |
| 29 | 看综合评分 | 84 | 70 | 74 | 80 | 76 | **76.3** |
| 30 | 看健康趋势 | 86 | 70 | 74 | 90 | 74 | **77.8** |
| 31 | 看健康报告(含对比) | 88 | 72 | 72 | 90 | 78 | **79.1** |

**读数（本席 8 页）**：页数 **8**；加权总分 **均值 78.61**；**最低 75.6（页 27 看蛋白质摄入报告）**；**最高 84.0（页 25 看TDEE报告）**；**加权 ≥90 的页数 0**。
**五维均值**：结构纪律 85.75／信息层级与版式 73.00／分隔符与文字 73.00／跨宽自适应 88.75／手机端与 HELP 同档 76.50（用这五个均值按同一权重复算＝78.6125，与逐页加权的均值一致）。
**族内极差**：加权 8.4 分（75.6–84.0）；分维极差最大的是信息层级（66–82，极差 **16**），最小的是跨宽自适应（80–90，极差 **10**，其中 7 页同为 90）。

### 2 · 跨页共性（同一处根因，逐页不重复扣）

| # | 族级发现 | 命中页 | 根因（源） |
| --- | --- | --- | --- |
| F1 | 页脚 `caliber` 与上方口径表/卡面同义重复（同一口径两种写法） | 25／30／31 | 页脚由口径表派生，未去重 |
| F2 | 「复制日志」浮层内上屏库表名与命令链：`calorie_data.db ｜ calorie_data.db ｜ 报告子形态 …`、`calorie-cmd-read calorie.report.* --params '{…}'` | 24–31 全部（八页同源） | 八页共用同一 `data-t` 模板；默认不渲染，点开即见 |
| F3 | 指标卡徽章复述卡面主值（`在缺口`／`低于基础代谢 30 天`／`平稳`／`蛋白达标 0%`／`下降 -28.6 ml`） | 25／26／29／30／31 | 徽章文案由主值拼成 |
| F4 | 结论句把紧下方卡片主值整句复述 | 25／26／27／28／29／30／31 | 结论句与卡面同源取值 |
| F5 | 长清单全展开无折叠（`<details` 八页均为 0） | 24（60 行）／26（30）／27（30）／28（30）／29（30）／30（60） | 表格块无折叠能力 |
| F6 | 退化输入无降级：同值序列照印、平线照画 | 27（30 行同 `68`＋平线）／31（运动 `300.0` 平线） | 图表与表无退化分支 |
| F7 | 数值口径不自证（页内多个数互不相等或与口径文案不符） | 29（68／68.4／69.4／69.5）／30（卡面 70.1／69.3 vs 90 天窗口）／31（缺口列不能互算） | 报表取值与文案口径未对账 |
| F8 | 同列或同轴小数位不一致 | 24（`78` vs `77.9`）／27 vs 28（达标率 `0%` vs `66.7%`）／29（`0%/50%/66.7%/100%`）／31（`7 天` vs `7.0`；`75`/`75.25`/`75.5`） | 各页各自 `toFixed` 不统一 |
| F9 | 图表刻度取「数据极值 ±5%」而非成档整数 | 24（24.2／25.0／25.8）／26（1509／1615／1721）／28（1752／2000／2248）／30（46／67／88）／31（1522／1593／1664） | 图表刻度生成器 |
| F10 | 中文状态列吃数值列规则（`cell-right` → `SF Mono`＋右对齐），且状态无徽章区分 | 27／28（`是否达标`） | `td.cell-right` 规则未区分文本列 |
| F11 | `四要素` 术语三处出现、无一处定义（口径表未列体重） | 25／26／31 | 口径表行集与文案不符 |

### 3 · 正面证据（同样逐条量过，构成 90 分上限之上的「没扣」部分）

- **同一份 CSS 底座**：八页 `<style>` 合并后 SHA256 前 12 位**全为 `E41401DD9CEB`**（42804 字节），断点 820／640／400 ＋ 图表 720-875／876 三档字号；`min-height:44px` **11 处**，覆盖 `.ilife-action-btn`／`.ilife-copy-btn`／`.ilife-copy-menu-item`／`.ilife-block-toc a`／页壳 `summary`——**触摸目标达标**。
- **窄表堆叠**：`@media (max-width:640px)` 的 `thead{display:none}` ＋ `tr{display:block}` ＋ `td{display:flex;justify-content:space-between}` ＋ `td::before{content:attr(data-label)}`（L1446–1480），与 HELP 锚件的卡片化同形；`overflow-x:auto` 只作兜底。
- **数值列**：`cell-right` 列 `min-width:5.5em` ＋ `SF Mono` ＋ `font-variant-numeric:tabular-nums`（L1419–1423），实测右对齐、等宽。
- **缺值**：页 24 分级表非当前档一律 `—`、当前档 `✔`；其余页无空值。
- **无横向溢出／无塌列**：八页 1440 与 390 双档 fullPage 实拍均无溢出、无截断；日期 30/60 条均完整未折断。
- **自洽处（复算通过）**：页 25 `1700×1.55=2635`、`2635−1589=1046`；页 28 `20/30=66.7%`；页 30 拐点数按页内定义复算＝**30**；页 31 `上升 2／下降 3／持平 3` 与 Δ 列一致、前 3 项按 |Δ| 降序成立。

### 4 · 本席的工程量与限制（不掩盖）

- **实拍**：八页各 2 张 fullPage（1440×900、390×844）＝16 张；共 23 张 crop；`vision_describe` 共 17 次。**768 档未实拍**——按本批规定的「一次截图＝桌面＋手机两档」执行，768 落在 641–820 带内（页壳收紧＋指标卡两列＋表仍为 3~4 列真表），本席以**同一份 CSS 判读**替代实拍，不据此报缺陷。
- **读图可靠性**：`vision_describe` 在**页 26／28／30 的 390 档返魂**（分别输出「取件宝已接入」「碳水化合物:较少」「A 股分析师」等源码中不存在的文字），这 3 次读图**整体作废**并在各页小节写明；手机档结论一律以「源码判读＋手机 crop」为准。
- **未细看**：各页「中段未细看」一句已逐页写明（多为长表中间行与副图逐点核对）。
- **尺的口径**：五维均按 0–100 给分，再按 `结构×0.20＋信息×0.25＋分隔符×0.20＋跨宽×0.15＋手机×0.20` 加权；与上一批（#520 终审）同一套权重。
- **7 页同为跨宽 90**：因八页共用同一份 CSS，跨宽分维几乎没有区分度——**这一维在本批实际只区分出页 29（80）**，是本席尺的局限，供汇总席在合并跨席读数时注意。

