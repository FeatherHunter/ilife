# t389 证据 · 运动概览页目标对比位（汇总页给出目标与对比目标位）

> 票面：`gh issue view 389`（卡路里场景01主页转票，`Part of #156`，由 #372 核验发现，证据 `docs/skills/skill-calorie/t372-核验.md`）。
> 验收命令：`calorie-cmd-read calorie.view.exercise --params '{"window":"今日"}'`（固定种子库 `docs/research/t81-seed.mjs`，`CALORIE_TODAY=2026-09-07`）。
> 用词照 `docs/agents/wording.md`（命令、页面交付、结果型 HTML）。

## 一 · 基线与商量一致（开工前只读确认，不碰他人件）

- 基线：`HEAD=4539e91a`（`git rev-parse HEAD` 当刻读数）。
- 开工 `git status --short` 记下他席在途件（本票一字不碰）：
  `packages/skill-calorie/AGENTS.md`、`packages/skill-calorie/src/analysis/multiTrend.ts`、
  `packages/skill-calorie/src/analysis/multiTrendPage.ts`、`packages/skill-calorie/src/analysis/trend.ts`、
  `packages/skill-calorie/src/diet/review.ts`、`packages/skill-calorie/src/diet/reviewDocs.ts`、
  `packages/skill-calorie/src/diet/routes.ts`、`packages/skill-calorie/src/render/trendMiscPort.ts`、
  `packages/skill-calorie/src/render/trendMiscPortDocs.ts`、`packages/skill-calorie/src/triggers/routes.generated.ts`
  ＋散落 `.tmp*.txt`／`cover-err.txt`／他票证据与测试件。
- `src/render/sportDocs.ts` 与 `src/home/exercise.ts` 在开工时 `git status --short -- <两件>` 无输出、
  `git diff -- <两件>` 为空：场景04共用装配无他席在途改动，无需另行商量一致，直接按 `HEAD` 冻基线。

## 二 · 影响清单（第一步：动手前报，逐行一能力）

| # | 目录 | 文件 | 一句话与理由 |
|---|---|---|---|
| 1 | `src/home` | `src/home/exercise.ts` | 取数加目标位：经目标公开接口拿每日目标＋完成度＋差额，不重写目标 SQL（本能力自己的取数） |
| 2 | `src/render` | `src/render/sportDocs.ts` | 汇总页装配加目标对照位 `sec-goal`（本件是运动两页的装配地，目标位落在这里） |
| 3 | 核验 | `.scratch/t389/verify-goal.mjs` | 核验矩阵（票面点名的交付物，新建） |
| 4 | 证据 | `docs/skills/skill-calorie/t389-目标对比位-证据.md` | 本件（票面点名的交付物，新建） |

- 不碰：`diet`／`goal`／`weight` 三家行为、`HOME_COMMANDS` 四条命令声明、`HOME_ROUTES` 路由声明、
  `src/triggers/routes.generated.ts`（生成物，不手改）、`packages/skill-calorie/AGENTS.md` 台账（他席在途件，不碰）。
- 结构标准：能力目录仍是 `src/home`／`src/render` 各归各；跨能力引用只走公开接口
 （`home/exercise.ts` → `render/planPlate.ts::buildExerciseGoalView`，与 `home/today.ts` 已走
  `render/sportDocs.ts` 同向，不反向；`sportDocs.ts` 不查库，只吃调用方备齐的视图）。

## 三 · 结构设计（第二步：目录树＋职责＋对外）

- 不新建目录层级，不碰三个以上能力，不等点头（只动两件各一段）。
- `src/home/exercise.ts`（取数，仍在 350 以内：48→76）：
  `ExerciseView` 加五个可选位 `dailyGoal`／`goalTotal`／`goalPct`／`goalGap`／`goalAchieved`
 （可选＝存量直构造仍逐字节不变）；`buildExerciseView` 在复核＋序列之后试调
  `buildExerciseGoalView(db, start, end)`，成功即填数，`missing-data` 即显式 `null`（未设目标走未设指引）。
  对外仍是一个函数（不多于五个），类型字段 11 个中真正干活的是复核＋序列＋目标三组。
- `src/render/sportDocs.ts`（装配，超线件，见 §四）：
  新增 `goalCard(v)` 一件（目标对照卡，`sec-goal`，题与页内导航同源 `目标对照`）＋
  `summaryCards` 接入两行＋`summaryEnvelope` 加四个度量（`metricsOf` 遇 `null` 即丢，存量信封不变）。
  有目标＝数值四格（目标／实际／完成度／差额）＋判决胶囊（与目标页同词：`已达成目标`／`还差 N 卡`）；
  未设目标＝同一张卡的未设指引（`还没设每日运动消耗目标`＋下一句能说的话）；存量直构造（未带目标字段）＝不加卡。
  口径行与来源脚注一律不加（汇总页口径行仍 0、来源仍无，守 #452／#552）。
- 共用件判定：`sportUi.ts` 的 `exerciseUiCss`／`windowStrip`／`factStrip` 与公共层
  `renderKpiGrid`／`renderTocBlock` 继续共用，本票不新增共用件（第二个用法未出现）。

## 四 · 超线报警（第四步）

- 已超线，需要根据规则进行重构。超因：`src/render/sportDocs.ts` 本票进场已超线
  （台账 434，运动两页融合版式同处一件），本票为落目标对照位再加 `goalCard` 一件＋接入两行＋信封四度量，到 466。
- 本次先不拆：拆分不在 #389 写集（票面只许动目标位相关段）；拆法照台账既有那条
  （按页切 `sportSummaryDocs.ts`／`sportGoalDocs.ts`，两页共用提为 `sportPageParts.ts`，出口经本件薄转出），待收口票认领。
- `src/home/exercise.ts` 76 行，未越线，不触发本步。

## 五 · 改动清单（第三步写代码的落点）

- `packages/skill-calorie/src/home/exercise.ts`：加 `buildExerciseGoalView` 引用＋五个可选位＋试调填数分支（注释三处）。
- `packages/skill-calorie/src/render/sportDocs.ts`：加 `goalCard` 一件（约30行）＋
  `summaryCards` 接入（卡清单注释改目标对照＋两行）＋`summaryEnvelope` 四度量。
- 未改：`src/home/today.ts`（`viewExerciseOverview` 逐字不变，命令级度量不扩）、
  `src/home/commands.ts`、`src/home/routes.ts`、`src/diet/**`、`src/goal/**`、`src/weight/**`。
- 目标口径：每日目标 `daily_goal.exercise_goal`、完成度＝实际÷目标、差额＝实际−目标，
  全经 `render/planPlate.ts::buildExerciseGoalView` 来（与 `calorie.view.exercise-goal` 同一算式），本票无第二处定义。

## 六 · 判据读数（先红后绿）

- 红（改前）：`RESULT: 9/12 FAIL`（`.scratch/t389/logs/run-red.log`）——
  `A档 页上找得到目标位 … 页上无位：目标`、
  `A档 页上找得到对比目标位 … 页上无位：完成度`、
  `A档 对比值 … 页上无位：完成度 106.x%` 三处红，其余 9 项绿（含空库阻断与三家行为）。
- 绿（改后）：`RESULT: 12/12 PASS`（`.scratch/t389/logs/run-green2.log`，还原后重跑；
  用词收口后重编重跑仍 `12/12 PASS`，见 `.scratch/t389/logs/run-final.log`）——
  种子库今日窗（2026-09-07，实际 320 卡，目标 300 卡）页上读到
  `目标对照`＋`目标 300 卡`＋`实际 320 卡`＋`完成度 106.7%`＋`差额 20 卡`＋`已达成目标`；
  空库仍 `exit 4` 不落盘。
- 默认交付：`exit=0`、`key=calorie.view.exercise`、
  `output=…/calorie_html/运动总览_*.html`、`exists=true`、`has-goal=true`、`in-calorie-html=true`
  （`.scratch/t389/logs/default-delivery.log`，`runId=962fcc1b-06b8-49b7-9a7b-705539c63c21`）。
- 回归（旧快照不动的证据）：`render-t8` 7/7 绿、`exercise-summary-goal-fusion-452` 9/9 绿、
  `exercise-text-shape-523` 13/13 绿（存量直构造未带目标字段即不加卡，故旧判据逐字仍绿；
  种子 CLI 那几页多一张 `sec-goal` 卡仍过 #452 的四格／零口径／无来源三条）。

## 七 · 变异自证（两行机器读数）

```
MUTANT  runId=e8b8c4b3-d045-4260-9ce4-ae2df0846604 exit=1 RESULT=11/12 FAIL（完成度标签改坏→对比目标位红）
RESTORE runId=c729b858-af90-42d4-a7fb-2ae0ba01e1ce exit=0 RESULT=12/12 PASS（逐字节还原→绿）
```

- 改坏处：`sportDocs.ts` 目标对照卡 `完成度` 改成 `__MUTATED__` 一行；编译仍绿（`tsc-mut` exit 0），核验必红。
- 还原后重编＋重跑即绿（`tsc-restore` exit 0）。

## 八 · GATE-RUN 对账（持锁运行全清单，`runId` 自 `.scratch/locks/gate-runs.log` 抄录）

| runId | cmd | exit | 备注 |
|---|---|---|---|
| `b05bc505-0763-4cdc-9921-7f0119f0bbb2` | `node .scratch/t389/verify-goal.mjs` | 1 | §六红行（9/12） |
| `1cdb52c8-2607-42c9-9030-54e2a669f2a1` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | 0 | 初编 |
| `05e0d72f-ef1b-4f50-bcf4-bd45e09a458a` | `node .scratch/t389/verify-goal.mjs` | 0 | 初绿（12/12） |
| `d1ab5a1a-e8c8-4d06-8ff7-0f7b7be05cf3` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | 0 | 变异前重编 |
| `e8b8c4b3-d045-4260-9ce4-ae2df0846604` | `node .scratch/t389/verify-goal.mjs` | 1 | §七红行（11/12） |
| `ba2be73a-b12f-4ba1-9b89-ed1464b43d1c` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | 0 | 还原后重编 |
| `c729b858-af90-42d4-a7fb-2ae0ba01e1ce` | `node .scratch/t389/verify-goal.mjs` | 0 | §七绿行（12/12） |
| `e8ce19af-7afe-4726-99ce-6d85354bd10f` | `node .scratch/t389/verify-goal.mjs` | 0 | 用词收口后重跑（12/12，`.scratch/t389/logs/run-final.log`） |
| `85994911-0097-4d68-a3e2-2934684e8cd8` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | 0 | 用词收口后重编 |
| `c1d803b2-5525-474d-b0ef-5d48986135d4` | `node --test packages/skill-calorie/test/render-t8.test.mjs` | 0 | 回归 7/7 |
| `370cddd7-65f1-4dc8-9650-47d96532312f` | `node --test packages/skill-calorie/test/exercise-summary-goal-fusion-452.test.mjs` | 0 | 回归 9/9 |
| `660620f8-8cfe-4498-bcc8-2f7026465747` | `node --test packages/skill-calorie/test/exercise-text-shape-523.test.mjs` | 0 | 回归 13/13 |
| `962fcc1b-06b8-49b7-9a7b-705539c63c21` | 默认交付探针（`calorie.view.exercise` 无 `--html`） | 0 | §六默认落盘 |

## 九 · 并发与越界声明

- 他席在途件（§一）一字不碰；`AGENTS.md` 台账虽在本窗内被他席同步改动（`sportDocs.ts` 行在 434／462 间摆动），
  本票未碰它：本票完工实测 466 与台账当刻值不等即台账陈化红，这是设计行为（#445），待收口票 `--sync` 认领，
  不由本票修（禁碰他人路径），不阻塞本票验收（票面验收只有固定种子＋空库两档）。
- 生成物 `src/triggers/routes.generated.ts` 未动（`gen:check` 无本票增量）；`SKILL.md` 不管（收口票重出）。
- 提交前 `git diff --cached --name-only` 自查只含声明路径；中文信息 `feat(389): …`；不推送。
- `.scratch` 外无散落日志；长输出全落 `.scratch/t389/logs/`，正文只看尾 5 行。

## 十 · 交付对账（第五步：事前清单逐行有着落，偏差为零）

- §二 4 行逐行对上：取数、装配、矩阵、证据各有着落；偏差为零。
- 产物：`.scratch/t389/verify-goal.mjs`、`.scratch/t389/logs/`（红／绿／变异／回归／默认交付）、
  本件；页面交付仍是结果型 HTML，落 `SKILLS_DB_PATH` 下 `calorie_html`。
