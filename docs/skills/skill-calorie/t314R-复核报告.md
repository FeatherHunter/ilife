# #314R · 对抗式复核报告（场景 01／08／09 就地搬进能力目录）

- **被审**：`2979b40`（feat(314) 主页／身体细节／身材照片三场景就地搬进能力目录，36 files，＋1244／−578）；基线 `5020522`
- **复核席**：票 #314 对抗席，标签 `314R`；终端 `314R-复核`（uuid `08fde94e`）
- **开跑固定**：`git status --porcelain` 空、`HEAD=2979b40f8ab52e446cd29f65cc53c29259c4b044`
- **只读＋副本**：仓内交付文件零改动；全部证据快照取自 `git archive`（`.scratch/314R/base`＝5020522、`.scratch/314R/head`＝2979b40）；变异只在仓外副本 `.scratch/314R/mut`（2979b40 全树 archive）
- **并发说明**：复核期间 **#317 正在本窗口落点**，树自 12:58 起长期非空（`cmd_read.ts`／`legacy/scene-05.ts`／`legacy/routes/scene-05.ts`／`registry.ts` ＋ `src/workout/`）。因此「共享树上的门禁」只完成了前半段，详见必做 5。

---

## 一、必做项逐条结论

### 必做 1 · 免费判据（PASS）
`git diff --stat 5020522..2979b40 -- <五件>` ⇒ **只有** `registry.ts`（`1 file changed, 6 insertions(+)`）。自算 sha256（我自己算，不采信它）：

| 件 | 5020522 | 2979b40 | 同否 |
| --- | --- | --- | --- |
| `cli/keys.ts` | `1131e939…bad0` | `1131e939…bad0` | **同** |
| `base-combos/combos.yaml` | `daf07319…fedd` | `daf07319…fedd` | **同** |
| `scripts/build-help.mjs` | `d4b40ffb…cfde` | `d4b40ffb…cfde` | **同** |
| `triggers/routes.generated.ts` | `e6f6296f…9a4f` | `e6f6296f…9a4f` | **同** |
| `cli/registry.ts` | `9d9f2b54…56f8` | `db33e437…854a` | **异**（应然） |

另用 git blob 精确字节复核（`git rev-parse <rev>:<path>`）：keys `09b2d8e6…941c`、combos `e21f3e36…2f5b`、build-help `9695f61c…d5f30`、routes.generated `96e295b0…e85fd` 两版**逐个相同**；registry 由 `c278650f…895e` → `77fbbb46…03c9`。registry 的 ＋6 行内容＝追加 `BODY_COMMANDS`／`HOME_COMMANDS`／`PHOTO_COMMANDS` 三个 import ＋ 三条源码项。**结论：GEN FREEZE 成立。**

### 必做 2 · 不变量（PASS）
`packages/skill-calorie/src/triggers/` 整目录 `diff --name-only 5020522..2979b40` **为空**；逐件 blob sha 对 10 片 `scene-*.ts` ＋ `test/calorie-routing-81.test.mjs` ＋ `packages/skill-calorie/test/legacy-partition-313.test.mjs` 全部 **SAME**。
> 纠错：题面写的 `test/legacy-partition-313.test.mjs` 真实路径在 `packages/skill-calorie/test/` 下（我用真实路径重验，避免空 pathspec 造成的「假绿」）。

### 必做 3 · 别人的 `case` 一个不少（PASS）
集合口径：A＝5020522 的 `cli/cmd_read.ts` ＋ `cli/write.ts` 里全部 `case 'calorie.…':`；B＝2979b40 同；C＝2979b40 `src/home|body|photo/**` 中出现的 `calorie.…` 键 ∪ 证据文档里以反引号引的键。

- `|A| = 92`、`|B| = 70`、`A−B = 22` 键、**`B−A = ∅`**（没有多余消失）；
- `A−B ⊆ C` ＝ **true，越界 0 例**；
- 它自曝吞过的 `calorie.view.exercise-strength` **仍在 B 中**（未被吞）。

⇒ 分派层只少了它声明的 22 键，别人一条没少。

### 必做 4 · 路由记录守恒（PASS）
对 5020522 与 2979b40 的 `cli/legacy/routes/scene-*.ts` 全记录＋新能力 `routes.ts` 做七字段（list／order／wakeWord／scene／kind／key／cli）集合差：

- base 分片 **451** → head 分片 **366** ＋ 能力目录 **85** ＝ **451**（总数守恒）；
- 迁出 **85** 条，**逐条七字段相同**：NOT FOUND=0、FIELD MISMATCH=0、CHANGED=0、ADDED=0、能力目录多余=0；
- 分片去向：01 `8→0`、02 `83→69`、04 `45→26`、06 `29→23`、08 `17→2`、09 `20→0`、10 `187→184`（合计 −85）；能力目录 50／15／20；
- head 全量 `(list, order)` 全局**无重复**（dup=0）；head 中 92 条无 `key` 的 `non-exec` 记录**全部** `kind=non-exec`（非 `non-exec` 而无 key 者＝0）；
- **抽样 4 条手工核对**（home `wake:0`／body `wake:237`／photo `new:0`／home `wake:429`）：整条对象字面量**逐字节相同**（含 `cli` 串的 `\'{\"…\"}\'` 转义）；全量 85 条 `cli` 串亦逐字节相同。

### 必做 5 · 门禁实测（**部分完成**，受并发阻塞）
- **`pnpm build`（共享树）**：跑在树空时刻，`runId=1d8e3378-5c5d-4942-bba8-7d33032a6a6b`，`exit=0`。此后 #317 开始写树。
- **`pnpm gen:check`（共享树）**：`runId=2908f66b-320c-482d-94a7-30e4056df9d4`，`exit=0`——**但此跑被 #317 污染**（输出 `能力 5 个（…／workout）声明 36 条，未搬迁清单 65 条`，与 #314 的 4 个／31 条不符）。**不作为 #314 的证据**，只作「并发记账」。
- **隔离副本复现 `gen:check`（2979b40 全树）**：`GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 4 个（body／home／photo／weight）声明 31 条，未搬迁清单 70 条`，`routes.generated.ts sha256=e9ea9935…d7e9` —— **与证据文档第 165／166 行逐字相同**，`exit=0`。这是比共享树更干净的证据（基线 `exit=0`、变异 `exit=1`，见第二节）。
- **全量 `pnpm test`、`t293`（经包装器）、`t313`：未能在共享树上执行**——树在整个复核窗口内持续非空（#317／#318 在写）。未执行的部分我不冒充已满足。

### 必做 6 · 变异自证（≥2，**完成**）
全部在副本上做，用**官署工具本体**（`packages/skill-calorie/scripts/gen-cli.mjs --check`）：

| 变异 | 副本基线 | 变异后 | 还原后 |
| --- | --- | --- | --- |
| ① 手改生成物 `cli/keys.ts`（追加一行） | PASS(0) | **FAIL(1)**「生成物是派生件：手改无效」 | PASS(0) |
| ② 清空 `home/routes.ts` 的 `HOME_ROUTES`（50 条记录全删） | PASS(0) | **FAIL(1)** `Error: list=wake 的 order 不连续：第 0 位实得 3（weight/routes.ts · "看今日体重概览"）——必须是 0..385 连续无洞` | PASS(0) |
| ③ 删 `home/commands.ts` 一条声明（`calorie.view.home`） | PASS(0) | **FAIL(1)** `GEN-CHECK FAIL：键 100（写 35 ＋ 读 65）；声明 30 条` | PASS(0) |
| ④ 只删 `home/routes.ts` 一条记录 | PASS(0) | **FAIL(1)** `order 不连续：第 0 位实得 1（home/routes.ts）` | PASS(0) |

**新探针（我自己设的，题面未要求）**：发现变异必须**先 `tsc -b` 重编 dist** 才生效——`gen:check` 读的是**编译后的 `dist`**。不重编时，上述②③④（连清空 50 条记录）**全部仍报 `GEN-CHECK PASS`**（我第一轮就撞上了这个假绿）。即：这正是证据文档第七节坑 3「mtime 不刷新＝假绿」的**姊妹坑**——`gen:check` 是**内容印记（dist）**判据，源码改了不重编＝静默绿。这条对**其余五票照抄模板**有直接价值。
另（读向抽查）：`normSource`／`normSex`／`MEASURE_CAMEL`／`measureCliNames`／`helpCenterInfo`／私藏 `photosDirOf`／`daysIn` 等私有件确实已不在分派层被引用（已随件搬进 `body/log.ts` 等）。

### 必做 7 · `t293` 裸跑记账（确认，定为 S3-过程记账）
`.scratch/locks/gate-runs.log` 全量筛：`ticket=314` 共 **28** 行＝`START` 12 ＋ `RUN` 12（另 4 行是我这次 `ticket=314R`）；**12 条 RUN 里没有任何一条 cmd 含 `t293`／`t313`**（正则 `ticket=314.*(t293|t313)` 零命中）。⇒ 它自报①成立：`t293` 确实**未经包装器**跑，无 runId、不入审计日志。定性：**S3-过程记账**（探针本身是只读验收脚本，不写仓；缺的是「持锁＋留 runId」的留痕，不是结论造假）。**不判更重**。

### 必做 8 · 票面格式（PASS）
`gh issue view 314 --json body` 落盘后逐项机器判：首字节 `80,97,114`（`Par`）＝**无 BOM**；`## 进度：95%` **独占一行**（第 42 行）；CRLF 真实换行 **56** 处；**无字面 `\n`**（`-match '\\n'`＝False）；**无 `100%`**（False）。

---

## 二、缺陷清单

### D-A【S1·本票引入】对账件**不能通过它自己写的复核命令**
`docs/skills/skill-calorie/t314-gate-runs.log` 头部自己写明复核用法；照抄执行：

```
node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t314-搬迁-证据.md \
  --log docs/skills/skill-calorie/t314-gate-runs.log --ticket 314 --since 2026-09-13T12:51:43.368Z
=> RESULT: matched=0/12 auditEntries=6 scoped=6 undeclared=6 ; gate-audit: FAIL ; exit=1
```

两个**互相独立**的根因（我都单独隔离复现）：

1. **声明体写法不合语法**：证据文档 12 条 `GATE-RUN` 都写成
   `GATE-RUN runId=… cmd="pnpm build" waitedMs=0 exit=0 pid=17232 at=…`。
   官署解析口径是「`cmd=` 之后到行尾**整段**是命令」，于是 `cmd` 被读成 `"pnpm build" waitedMs=0 …`（首字符是引号、末尾不是引号，无法 unquote），`cmdMatches` 恒假。⇒ **12／12 条声明谁都认领不到**。
   对照：`t313f`／`t313r`／`t313v`／`t322`／`t322r`／`t323` 六件的证据**一律止于 `cmd="…"` 的右引号**，是仓内既有写法。**只有本票多贴了尾巴。**
2. **对账导出窗口漏掉一半**：`t314-gate-runs.log` 的 `since=2026-09-13T12:51:43.368Z` 只导出后 6 条（build／gen／build／gen:check／gen:check／test），**前一批 6 条（12:47–12:48）不在文件里**，而证据文档把 12 条全声明了。

隔离证明（副本，把声明行**只**截到右引号，其余一字不动）：

| 跑法 | 结果 |
| --- | --- |
| 原声明 × 官方日志 | matched=**0/12**，FAIL(1) |
| 截尾声明 × 官方日志（12 条齐全） | **matched=12/12，undeclared=0，PASS(0)** |
| 截尾声明 × 本票导出（只有 6 条） | matched=**6/12**，FAIL(1) |

⇒ 12 次运行**客观存在且 exit=0**（第 2 行证明），栽在**声明写法＋导出窗口**上。但这是受跟踪的交付件、且文档把「对账导出」写进第七节**要其余四票照抄**——模板缺陷会复制五份。**故判 S1-交付缺陷。**

### D-B【S2·本票引入】证据文档「处理函数住处」列 **22／22 个路径都不存在**
第 27–48 行表格「处理函数住处」写 `home/hometoday.ts`／`home/dietoverview.ts`／`home/exerciseoverview.ts`／`home/goalprogress.ts`／`body/compositionadd.ts` … `photo/photologwizard.ts`。在 2979b40 全树上 `Test-Path`：**22／22 = False**。
真实住处：`home/today.ts`；`body/log.ts`（记体脂＋记围度两键同件）／`view.ts`／`remove.ts`／`wizard.ts`；`photo/store.ts`／`gallery.ts`／`compare.ts`／`manage.ts`／`wizard.ts`／`help.ts`（＋`dir.ts`）。
同一文档第 54–60 行**自己**列的就是后一套名字——§二 与 §三 **自相矛盾**。其余机器数字（85 条、50／15／20、101 键、31 条、70 条、sha256）我抽验**全部为真**，所以这是「人写的路径列没跟上落地」，不是数字造假。

### D-C【S3·本票引入】偏离 5 的理由已成化石，且路径写错
§五 偏离 5 称 `test/legacy-partition-313.test.mjs` 的变异样本取自 `SCENES['01'][0]`、「本片搬空后会变 undefined **而红**」、「由 #322 收口席改」。实测：该终态修复**已在基线 `5020522` 里**（`git log`：`7c0e220 test(322): 收口轮…`，文件第 75 行注释即该修复），两版文件 **SAME**、不会红；且真实路径在 `packages/skill-calorie/test/` 下。结论（不碰该文件）与事实相符，**只是理由过期**。

### D-D【S3·过程记账】`t293` 裸跑（见必做 7）。

### D-E【范围外发现】非本票缺陷
- `## 进度：95%` 仍停在被审状态，票面第四段「待确认：① 三片 legacy/scene-01|08|09.ts 均 0 条、且文件仍在」——我复核**成立**（三片各 `13` 行壳、`LEGACY_SCENE_*` 声明为空）。
- `test/legacy-partition-313.test.mjs` 的路径在**多处文档**被写成 `test/…`（少一层 `packages/skill-calorie/`），非本票引入。

---

## 三、五维打分 ＋ verdict

| 维度 | 满分 | 得分 | 依据 |
| --- | --- | --- | --- |
| 契约一致 | 30 | **27** | 22 键／85 记录／分派只减不增／冻结地基零改动／唯一生成物 diff＝registry ＋6 —— 逐条独立成立；扣 §二 契约文档与实际住处不符 |
| 证据真实可复现 | 25 | **12** | 机器数字（101 键／31 条／70 条／sha256／85 条守恒）**可复现且为真**；但受跟踪对账件**按文档自己的命令跑就是 FAIL**（两个独立根因），t293 无 runId 留痕 |
| parity | 20 | **19** | `routes.generated.ts` 逐字节未变；85 条记录七字段＋`cli` 串全量逐字节相同（抽样 4 条另手工核）；`calorie-routing-81` 冻结未动。扣分：路由 81 条 parity 的快照本轮未重跑（文档自认） |
| 工程红线 | 15 | **14** | 新件均 ≤350 行（最大 109）；门对外件数 2／3／3 ≤5；每命令只属一个能力目录；无越界 import。扣：能力目录件数/行数我只按清单核，未逐件跑 boundaries |
| 文档同步 | 10 | **4** | §二 22／22 路径不存在且与 §三自相矛盾；§五 偏离 5 理由过期＋路径错。加分项：机器段数字与实测逐字吻合 |
| **合计** | 100 | **76** | |

## **verdict: FAIL（针对被审提交 `2979b40`；见文末「返修复验」的返修后结论）**

判定依据：存在 **S1-交付缺陷 D-A**（受跟踪对账件不通过官署审计，且被写成其余四票的照抄模板）。按 `任一 S1-交付缺陷 → FAIL` 与 `均分 ≥85` 双条件，**均不满足**（76 分）。
**产品面本身干净**：我在契约／parity／工程红线三个维度上**没有找到任何功能性缺陷**——代码、路由、生成物、冻结地基全部经得起独立复跑。缺陷集中在**证据／对账件与文档**。

## 未确证／风险 top3

1. **全量 `pnpm test`（1492）与 `t313` oracle 本轮未能在共享树跑到**（#317 全程占树）。`t314` 声称的 `1492/1492` 我**未能独立复跑**；`t313` 的 `DEEP-EQUAL=yes`／`surfaceMissing=0` 同理未验。→ **交付判据 ② 与 oracle 一致性属未确证**。
2. **`t293` P1–P5 ＝ 5/5 未独立复跑**：我在**无 `.git` 的副本**里跑得到 `P2=FAIL`（`calorie.view.weight` 等 9 键「仓内零命中」、`冻结事实对=0`），经查是 t293 依赖 `git ls-files` 枚举、副本非 git 仓所致，**不构成对 #314 的反证**；但**共享树上的 5/5 仍未被任何人（含我）在锁内复跑**。
3. **模板传染风险**：D-A 的两种写法（`cmd=` 后贴尾巴、导出窗口只截一半）与文档第七节「照抄七步」绑定，**#315–#319 若照抄会整批对账 FAIL**；D-B 的「处理函数住处」列同理会把假路径抄进四份证据。

---

## 返修复验（HEAD `14f0ecf`，树空）

返修请求写「父 `2979b40`」；**实际父链**为 `2979b40 → 3845718（本席报告提交）→ 535a9b2（refactor(317) 场景 05 搬进 `src/workout/`）→ 14f0ecf`。
`14f0ecf` **自身**只改 2 件：`docs/skills/skill-calorie/t314-搬迁-证据.md`（＋58／−44）与 `docs/skills/skill-calorie/t314-gate-runs.log`；`git diff --name-only 14f0ecf^..14f0ecf` 无 `packages/`／`test/`／`tooling/` 任何一件 ⇒ **源码零改动成立**（先前 12 件的差集来自 `535a9b2`，非本票）。

### 1 · 对账命令（我自己跑，原样输出）

| 跑法 | 原样输出 | exit |
| --- | --- | --- |
| 我的证据 × 官方日志 `.scratch/locks/gate-runs.log`，`--ticket 314`，**不加 `--since`**（最严） | `RESULT: matched=12/12 auditEntries=1634 scoped=12 undeclared=0` ／ `gate-audit: PASS` | **0** |
| 我的证据 × 受跟踪导出 `t314-gate-runs.log`，`--since 2026-09-13T12:45:00.000Z --until 2026-09-13T13:05:00.000Z` | `RESULT: matched=12/12 auditEntries=12 scoped=12 undeclared=0` ／ `gate-audit: PASS` | **0** |

导出件头部现为：`since：2026-09-13T12:45:00.000Z｜until：2026-09-13T13:05:00.000Z｜条目数：12`，文件内 `^RUN ticket=314 ` 实计 **12** 行。
窗口覆盖性另证：`ticket=314` 全部 RUN 的时间跨度是 `12:47:07.638Z → 12:52:41.438Z`，**整段落在 [12:45, 13:05] 内** ⇒ 无窗口外漏网。**S1-a／S1-b 均已消。**

### 2 · 逐行核

**① 12 条 `GATE-RUN` 形态**：正则 `^\s*GATE-RUN runId=\S+ cmd="[^"]*"\s*$` 逐行判 —— **12/12 通过，右引号后含多余字符者 = 0**。12 个 runId 与官方日志中的 12 条 RUN 一一对应（`b212cef4…`／`68545756…`／`95fce68b…`／`84c3d88c…`／`c8549c53…`／`c0c920b9…`／`3bdacce9…`／`735e2636…`／`fe146148…`／`daf7da0a…`／`3ef41814…`／`2ea4bffd…`）。

**② §二「处理函数住处」22 行逐条 `Test-Path`**：表格 22 行，**22/22 命中、缺失 0**。
`home/today.ts`（4 键）／`body/log.ts`（记体脂＋记围度）／`body/remove.ts`（删体脂＋删围度）／`body/view.ts`（体成分＋围度看）／`body/wizard.ts`（两个向导）／`photo/help.ts`／`photo/store.ts`／`photo/compare.ts`（对比＋GIF）／`photo/gallery.ts`（查＋看身材照）／`photo/manage.ts`（删＋改标签）／`photo/wizard.ts`（GIF规划器＋身材照向导）。

**③ 防「路径存在但张冠李戴」交叉抽查**（本席追加）：`body/*`、`photo/store|gallery|compare|manage|help` 各件均实际持有对应键，逐件命中；`photo/compare.ts` 含 `calorie.photo.gif`＋`calorie.photo.compare`、`photo/gallery.ts` 含 `calorie.photo.detail`＋`calorie.photo.list`、`home/index.ts` 含 `runHomeView`＋`HOME_COMMANDS`（与 §三 的门形状一致）。

**④ §五 偏离表**：第 5 行理由已照事实改写（`#322` 的 `7c0e220` **已在基线**、两版同形、本票不许再动）⇒ **S3-D-C 已消**；新增第 7 行如实记 `t293` 裸跑无 `runId` ⇒ **S3-D-D 已记账**；新增第 8 行记本轮返修。

### 3 · verdict per 缺陷 ＋ 是否新引入

- **S1（D-A）已消**：两条路径（官方日志全量／受跟踪导出＋窗口）都 `matched=12/12 undeclared=0 PASS`，导出覆盖全部 12 次运行，声明逐行合法。
- **S2（D-B）已消**：22/22 行指向真实件，交叉抽查确认「件与其命令」对得上（不是「随便一个存在的路径」）。
- **S3（D-C／D-D）已消／已记账**：偏离 5 理由改照事实；`t293` 裸跑补记。
- **新引入缺陷 1 条（S3·措辞过度）**：§五 第 8 行写「S2 §二…照实录改成真实路径（**生成器加存在性断言**）」。实测 `14f0ecf` **未改任何生成器**；`packages/skill-calorie/scripts/gen-cli.mjs:158` 的 `existsSync(join(SRC_DIR, name, 'commands.ts'))` 是**既有的能力目录发现**，与「处理函数住处」路径无关。仓内**不存在**守 §二 路径的存在性断言 ⇒ 这句会让人以为该缺陷类已有机器守卫。**建议改成**「（本票装置脚本加存在性自检，未入仓）」。**只涉措辞，不影响任何机器判据**。
- 残余未确证（非新缺陷，沿用上次）：共享树上的全量 `pnpm test`（1492）与 `t313`（`DEEP-EQUAL`）仍无人复跑；`t293` 的 `5/5` 仍是**裸跑**结果（第 7 行已如实披露）。

### 4 · 本席窗口与本次返修运行

本席对账窗口**闭合**在 `--since 2026-09-13T12:58:31.692Z --until 2026-09-13T13:18:54.065Z`（4 条声明，见「附二」）。**本次返修的提交运行发生在此窗口之后，按定义在窗口外**，故不列入声明行（列入会让它变成 unmatched 声明）；在此如实点名，供全量审计（不加 `--since`）时对照。

### 5 · 返修后总评

| 维度 | 满分 | `2979b40` 原评 | `14f0ecf` 复评 | 变动依据 |
| --- | --- | --- | --- | --- |
| 契约一致 | 30 | 27 | **30** | §二 22 行已指向真实件，文档与实际住处不再矛盾 |
| 证据真实可复现 | 25 | 12 | **23** | 对账两条路径均 `12/12 PASS`；12 次运行客观存在且 exit=0（截尾声明×官方日志 12/12 已证）；残余：`test`／`t313` 仍未复跑 |
| parity | 20 | 19 | **19** | 未变（源码零改动） |
| 工程红线 | 15 | 14 | **14** | 未变 |
| 文档同步 | 10 | 4 | **8** | §二／§五 已修；残余 §五 第 8 行「生成器加存在性断言」措辞过度 |
| **合计** | 100 | 76 | **94** | |

## **返修后 verdict: PASS（94 分；无 S1、无 S2，仅余 1 条 S3 措辞）**

一句话：`2979b40` 的 **S1／S2／S3 全部已消或已如实记账**，`14f0ecf` **自身**只动两件证据、源码零改动；**新引入缺陷 1 条 S3**（§五 第 8 行「生成器加存在性断言」在仓内无对应实现，建议改为「本票装置脚本加存在性自检，未入仓」）。**建议：该 S3 措辞随手改掉后即可收口**；`pnpm test`（1492）与 `t313` 仍属未复跑，若窗口允许应在锁内补跑一次以闭合交付判据 ②。

---

## 附二：本席持锁运行逐条声明（写法按官署语法：`cmd=` 到右引号为止）

共享树（`D:\ilife`，`HEAD=2979b40f8ab52e446cd29f65cc53c29259c4b044`）：第 1 条跑在树空时刻；第 2 条**已被 #317 污染**（其输出为 workout／36 条），只作并发记账，**不作为 #314 的证据**。

**对账窗口（闭合，不随时间漂移）**：`--since 2026-09-13T12:58:31.692Z --until 2026-09-13T13:18:54.065Z`。窗口内本席 `ticket=314R` 的 RUN 条目**恰 4 条**，逐条声明如下（写法止于右引号，避免重蹈 D-A 的坑）：

- GATE-RUN runId=1d8e3378-5c5d-4942-bba8-7d33032a6a6b cmd="pnpm build"
- GATE-RUN runId=2908f66b-320c-482d-94a7-30e4056df9d4 cmd="pnpm gen:check"
- GATE-RUN runId=fe9a3273-22c0-432d-a8ea-0e97ba85a4db cmd="node .scratch/314R/commit.mjs"
- GATE-RUN runId=623e4851-d906-4216-8589-33cffbf07b92 cmd="node .scratch/314R/commit.mjs"

复核命令（应得 `matched=4/4 undeclared=0 PASS`）：

```
node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t314R-复核报告.md \
  --log docs/skills/skill-calorie/t314-gate-runs-314R.log --ticket 314R \
  --since 2026-09-13T12:58:31.692Z --until 2026-09-13T13:18:54.065Z
```

在 2979b40 隔离副本（`.scratch/314R/mut`）内跑的官署工具与探针（不走共享锁，故无 runId，逐条如实声明）：

- `node packages/skill-calorie/scripts/gen-cli.mjs --check`：基线 `exit=0`（101 键／31 条／70 条），四次变异 `exit=1`，还原 `exit=0`
- `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`：**裸跑**，`P1=PASS P2=FAIL P3=PASS P4=PASS P5=PASS 4/5`（P2 失败系副本无 `.git`、t293 靠 `git ls-files` 枚举所致的**假红**，见「未确证」第 2 条）
- `node tooling/check-gate-audit.mjs` 三次对账（原声明／截尾声明 × 官方日志／导出件）：`matched=0/12`、`12/12 PASS`、`6/12`

**未跑**（因 #317／#318 全程占用共享树，且本席不得在树非空时跑门禁）：共享树上的全量 `pnpm test`、经包装器的 `t293`、`t313` routing oracle。

## 附：本席的自证与越界声明
- 仓内**受跟踪交付文件零改动**；本报告与 `t314-gate-runs-314R.log` 为本席新建产物。
- 变异全部在 `.scratch/314R/mut`（2979b40 全树 archive ＋ `node_modules` 目录联结）；每次变异后**逐字节还原**并复验 `byte-identical=true`。
- 未 close #314、未写 100%、未改任何交付件。
