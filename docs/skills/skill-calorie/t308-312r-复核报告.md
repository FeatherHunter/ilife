# 票外 CI 红批次（#308／#309／#311／#312）对抗式复核报告

> 复核席：`308-312r`（对抗者立场：默认交付有问题，去找出来）
> 仓库：`D:\ilife`，分支 `master`，Windows／PowerShell 7。
> 复核窗口 HEAD：`66963aa` 起（开窗）→ `7228148` → `b06bbfd`（门批跑时点）→ 复核期间仍有并行票在落库（见 §6 并发声明）。
> 被复核对象：`4295867`（#309）、`30639ca`（#311）、`0e1fc51`（#312）、`aa7fd61`（#308）。

## 0. 判定总表

| 票 | 提交 | 判定 | 分数 | 一句话 |
|---|---|---|---|---|
| `#309` | `4295867` | **PASS** | **96** | 期望值 10→11 是受跟踪正本，独立复算 11，变异即红（门不是「跟着实现走」）。 |
| `#311` | `30639ca` | **PASS** | **98** | 五道门逐行自带 `if: always()`，机器门入册且变异即红；**真 CI 前后对照**证明第 10／11 步真跑了。 |
| `#312` | `0e1fc51` | **PASS** | **95** | 无 `.scratch` 环境 7 pass／0 skipped，缺源硬红不跳过，产物字节三路同值；残余的是口径提示（S3）。 |
| `#308` | `aa7fd61` | **PASS** | **93** | 种子 100/101→101/101（本机 ＋ 真 CI 4/6 腿），只动种子、未动「本周」语义；Windows 两腿仍红为**票前既有**，非本票引入。 |
| 共同项（5①–④） | —— | **PASS** | — | `pnpm test` 1492/1492；四票文件面零热位；四提交均在 `origin/master` 祖先链上；四票均 OPEN 未关。 |
| **批次合计** | —— | **PASS** | **95** | S1=0、S2=0（分级口径见 §5）。 |

**门槛声明**：本报告按「S1=0 且 S2=0 才 PASS」执行。S1／S2 只登记**本批四票引入、或本批声称已闭合而未闭合**的判定缺陷；票前既有、且不在四票文件面与声称范围内的残余红，登记在 **S3-残余** 并单列「必须整改」（§7），不用于推翻四票各自成立的技术断言——但**明确标注**：若编排者认为「票面验收判据必须全局为真」，则 `#308` 应按 §4.5 降判。

---

## 1. #309 —— 过期期望值 10→11

**结论：PASS（96/100）。改的是受跟踪正本，独立复算一致，且变异即红——不是「跟着实现走」的假绿。**

### 1.1 受跟踪性

```
$ git ls-files tooling/test/skill-html-snapshot.test.mjs
tooling/test/skill-html-snapshot.test.mjs
```
`4295867` 只改一个文件（`git show --name-only --format="" 4295867`）。

### 1.2 独立复算（不抄提交里的数）

从**受跟踪正本** `tooling/skill-html.snapshot.json` 直接数（`ConvertFrom-Json` 后按 id 前缀计数）：

```
total artifacts props = 186
bill      frag=16  tpl=16  other=13
chef      frag=8   tpl=8   other=13
home      frag=21  tpl=21  other=13
schedule  frag=8   tpl=8   other=13
memo      frag=11  tpl=6   other=11
```

`memo/frag/*` 逐条列出（**11 条**，末键 `memo.wish`）：

```
memo/frag/memo.batch      memo/frag/memo.create    memo/frag/memo.detail
memo/frag/memo.help.lookup memo/frag/memo.remind   memo/frag/memo.remove
memo/frag/memo.search     memo/frag/memo.stats     memo/frag/memo.sync
memo/frag/memo.update     memo/frag/memo.wish
```

`artifactCount` 自洽：`5*(4+6+1) + (16+8+21+8+11) + (16+8+21+8+6) + 4*2 = 55+64+59+8 = 186` ＝ 实测 186 ＝ 文件里的 `artifactCount`。`SHAPES` 实测 6 项（`tooling/skill-html-snapshot.mjs:95`），故结构件 = `4+6+1 = 11`，与 `other=11`（memo）／`other=13`（其余四对 = 11 结构件 ＋ 2 共享件）吻合。

注释里的引用也复核了：`1fca032` 存在且标题为 `feat(skill-memo-ilife): #220 备忘录 HELP 首发 ＋ 卡路里线补发 ＋ 作息线发版归位`；`git show 1fca032~1:packages/skill-memo-ilife/src/render/envelope.ts` 里 `'memo.x'` 键 **10** 条，现值 **11** 条 ⇒ 「第 11 键于该提交落地」的注释属实。

### 1.3 门实跑

```
$ pnpm gate:selftest:html            # 该命令自持锁（ticket=96，locks-selftest），窗口内不嵌套我的锁
LOCK-ACQUIRED ticket=96 runId=30ef08e8-…
ℹ tests 10   ℹ pass 10   ℹ fail 0   ℹ skipped 0
LOCK-RELEASED ticket=96 … exit=0
RESULT: ticket=96 … exit=0
```

### 1.4 变异证据（该门读的真源是「活产物」，不是自己）

隔离副本 `git archive HEAD` 派生（`.scratch/t308-312r/iso`，主树零接触），直调 `node`：

```
# 变异：从 packages/skill-memo-ilife/dist/render/envelope.js 的 MEMO_KEY_SHAPES 摘掉 'memo.wish'
$ node --test tooling/test/skill-html-snapshot.test.mjs      # iso 内
  ✖ 真实产物：覆盖集合 == 声称的集合（防脚本自我满足）
       AssertionError [ERR_ASSERTION]: memo key 片段数        ← 就是 EXPECT_KEYS 那条断言
  ✖ 快照文件本身与树一致（收尾前不得留下过期快照）
       actual: [ [ 'memo/keys' ], [], [ 'memo/frag/memo.wish' ], [], [] ]
ℹ tests 10   ℹ pass 8   ℹ fail 2      EXIT=1
```

**判据成立**：期望值是与**活产物**（`collectArtifacts()` 读 `dist/render/index.js`）对撞的写死常量，实现少一个键就红。没有「从素材算」的自校验循环。变异同时触发静态断言与受跟踪快照对比两条独立路径。

### 1.5 扣分项（4）

- 门只钉**条数**不钉**键名**：单键改名（`memo.wish`→`memo.wishlist`）时 `EXPECT_KEYS` 不会红。**覆盖缺口由同文件最后一条用例（受跟踪快照逐 id 对比）兜住**，实测该用例确实在本变异中同时转红，故不构成 S2，仅记录为 S3。
- 期望值清单没有「复算脚本」可一键重跑（复算口径写在注释里，靠人照做）。属可维护性提示，S3。

---

## 2. #311 —— `publish-gates` 每道门自带 `if: always()`

**结论：PASS（98/100）。逐行核实、机器门入册、变异即红，且**真 CI 前后对照**给出了后效证据。**

### 2.1 逐行核 `.github/workflows/ci.yml` 的 `publish-gates` 作业块

作业头（L75）**无 `needs:`**（不会被上游作业整体判 skipped），步骤与缩进逐行：

```
L 94:       - run: pnpm install --frozen-lockfile        （无 if）
L 95:       - run: pnpm build                            （无 if）
L 96:       - run: pnpm publish:pre
L 97:         if: always()                               indent=8
L 98:       - run: pnpm publish:tarball
L 99:         if: always()                               indent=8
L100:       - run: pnpm publish:fresh
L101:         if: always()                               indent=8
L105:       - name: 模板资产门（skill-calorie）
L106:         if: always()                               indent=8
L112:       - name: 打包实证与清理守卫自证（#95 返修）
L113:         if: always()                               indent=8
```

五道门全部命中，缩进 8 空格与机器门正则 `^ {8}if: always\(\)$` 一致。全文 `continue-on-error` 实测 **0 处**：

```
$ Select-String -Path .github/workflows/ci.yml -Pattern "^\s*continue-on-error\s*:"
none
```

### 2.2 机器门名单确实纳入了该作业

`test/ci-gates-295.test.mjs` 的 `GATES` 已从「build-test 七道门」扩为「作业 ＋ 命中串 ＋ 标签」，`publish-gates` 五道门入册（源码 L26–L37 可核）；断言体按作业取步骤（`stepsOf(yaml, job)`），并断言 `matched.length === 1`（命中 0 道或 2 道都红，fail-closed）。

```
$ node --test test/ci-gates-295.test.mjs     # 主树
ℹ tests 2   ℹ pass 2   ℹ fail 0
```

### 2.3 变异自证（隔离副本）

```
# 变异：摘掉 publish:fresh 那一步的 if: always()
$ node --test test/ci-gates-295.test.mjs      # iso 内
ℹ tests 2   ℹ pass 1   ℹ fail 1      EXIT=1
  AssertionError: 「pnpm publish:fresh」（publish-gates）没带 if: always()：
  前一步一红，这门会在 CI 里被判 skipped（#295 返修 A8／#311 的原缺陷）
```

### 2.4 **真 CI 前后对照（本报告最强证据）**

用 `gh run view <run> --json jobs` 取步骤表，对比票前／票后：

| 运行 | headSha | 票前还是票后 | `publish-gates` 第 10／11 步 | `publish:fresh` | 作业结论 |
|---|---|---|---|---|---|
| `34765949207` | `0354944` | **票前** | `skipped, skipped` | failure | failure |
| `34766518187` | `aa7fd61` | **票后**（四票尖端） | **`success, success`** | failure | failure |
| `34766608919` | `6fa0685` | 票后 | **`success, success`** | failure | failure |

即：**票前第 10／11 步在真 CI 里从未跑过；票后两步真跑且通过**。同时 `pnpm publish:fresh` 仍红、作业整体仍 `failure` —— 精确命中票面验收判据「其后步骤仍会真跑 ＋ 不得用 `continue-on-error` 把红改绿」。提交里说的「后效本机测不出来」已由真 run 补上。

### 2.5 扣分项（2）

- 机器门只验证 `if: always()` 的**存在**，不验证「该步确实是门」（把 `run:` 换成 `echo hi` 仍会绿）。属判据强度上限，非本票缺陷，S3。
- 首发/安装步骤（L94–95）故意不带 `if: always()`，故 `pnpm install`／`pnpm build` 一红，五道门仍会被跳过。这是**正确**取舍（没装好不该跑门），但票面未写明这一层「遮蔽面仍在」，S3 口径提示。

---

## 3. #312 —— 事实源入库，4 条用例不再静默跳过

**结论：PASS（95/100）。无 `.scratch` 环境真跑 7 pass／0 skipped；缺源是硬红不是跳过；生成器产物字节不变。**

### 3.1 入库与 ignore

```
$ git ls-files packages/skill-schedule/test/fixtures/
packages/skill-schedule/test/fixtures/t198-old-scenarios.json        ← 确已入库

$ git check-ignore -v packages/skill-schedule/test/fixtures/t198-old-scenarios.json
（无输出 = 不再命中 ignore）
$ git check-ignore -v .scratch/t198/old-scenarios.json
.gitignore:5:.scratch/      .scratch/t198/old-scenarios.json          ← .scratch 仍不入库（票面口径保持）
```

sha256 逐字节对照（fixture vs 落地席所称的 `.scratch` 工作副本）：

```
F0EC8157E63F664626CB32636628189F18E65A02EB8D43CA1C76524C0ED0FB2A  packages/skill-schedule/test/fixtures/t198-old-scenarios.json
F0EC8157E63F664626CB32636628189F18E65A02EB8D43CA1C76524C0ED0FB2A  .scratch/t198/old-scenarios.json
```

### 3.2 「完全没有 `.scratch` 事实源」的环境

自己造：`git archive HEAD` → `Expand-Archive` → `Test-Path <iso>\.scratch` = **False**（`git archive` 只带受跟踪文件）。补齐运行依赖用**目录联接**（junction，不复制、不改主树）：根 `node_modules` ＋ 各 `packages/*/node_modules`，并复制 21 个 `packages/*/dist`。

```
$ node --test packages/skill-schedule/test/help-assets.test.mjs        # iso 内，无 .scratch
▶ #201 作息 HELP 内容资产
  ✔ 三层结构：分组／唤醒词／场景条条数得出，二级组场景非空
  ✔ 字段闭集与两态状态（多一个键即被共享 help 模板拒收）
  ✔ 逐条对账：与源数据 5 个分组／34 条唤醒词／85 条场景逐条对得上      ← 原 skip
  ✔ 85 条 prompt 零差异：逐条逐字节比                                  ← 原 skip
  ✔ 摘要锁：内容摘要与 prompt 摘要都钉死，源侧各算一遍也对得上
  ✔ 待开发标记：源自标项 ＋ 无命令唤醒词下辖项，共 11 条                ← 原 skip
  ✔ 可重跑：生成器 --check 与仓库内资产字节一致                        ← 原 skip
ℹ tests 7   ℹ pass 7   ℹ fail 0   ℹ skipped 0        EXIT=0
```

主树副作用面亦已核（受跟踪清单同一条）：`pnpm test` 全量日志里 `#201 作息 HELP 内容资产` 整组 7 条全 `✔`（1.14s，真跑而非 skip）。

### 3.3 没有「无源就 skip」的假绿

```
$ Select-String -Path packages/skill-schedule/test/help-assets.test.mjs -Pattern "skip|HAS_SOURCE|SKIP"
packages\skill-schedule\test\help-assets.test.mjs:4:// …CI 上恒缺 ⇒ 4 条对账用例**静默 skipped**：
（仅第 4 行注释提到该词，代码里 0 处 skip）
```

`sourcePayload()` 里 `assert.ok(existsSync(SRC), '受跟踪事实源缺失：' + SRC + '（#312：不许静默跳过）')` —— fail-closed。

**变异 A（fixture 挪走）**：

```
✖ 逐条对账…  ✖ 85 条 prompt 零差异…  ✖ 摘要锁…  ✖ 待开发标记…  ✖ 可重跑…
ℹ tests 7   ℹ pass 2   ℹ fail 5   ℹ skipped 0     EXIT≠0（硬红，不是跳过）
```

**变异 B（prompt 里改字：`写了 AI 调优代码` → `写了 AI 调优`）**：

```
✖ 85 条 prompt 零差异：逐条逐字节比
✖ 摘要锁：内容摘要与 prompt 摘要都钉死，源侧各算一遍也对得上          ← 摘要锁真会红
✖ 可重跑：生成器 --check 与仓库内资产字节一致
ℹ tests 7   ℹ pass 4   ℹ fail 3
```

> 口径纠正（S3）：落地席提交信息写「变异对照②（fixture 改一个字节）⇒ 摘要锁转红」。我实测**不是任意一个字节**都成立——先改 `scenario_count` 那个数字（头部元数据，不入 prompt 摘要），只红了「可重跑」，摘要锁仍绿。只有当改的字节落在 `prompt` 里时摘要锁才红。「摘要锁只钉 prompt」是设计如此（`digest(scenes.map(s => s.prompt).join('\n'))`），不构成缺陷，但**证据表述应写准**。

### 3.4 `gen-help-assets.mjs` 三行改动：产物字节不变

三路对照（iso 内直调 `node`，输出到 iso 内临时路径，不覆盖仓内产物）：

```
$ node packages/skill-schedule/scripts/gen-help-assets.mjs \
      --out <tmp>/from-fixture.ts           # 新缺省（受跟踪 fixture 优先）
摘要：85 条 prompt 逐一拼接 sha256=38fc4b4f…b50406565a5
摘要：产物文件 sha256=4260b237beb0fab289a4f41a2d4033d60afd2ca63d6616906a33a112704f13e8（73873 字节）

$ node packages/skill-schedule/scripts/gen-help-assets.mjs \
      --src .scratch/t198/old-scenarios.json --out <tmp>/from-scratch.ts   # 显式工作副本
摘要：产物文件 sha256=4260b237…f13e8（73873 字节）

sha256(from-fixture.ts)      = 4260B237BEB0FAB289A4F41A2D4033D60AFD2CA63D6616906A33A112704F13E8
sha256(from-scratch.ts)      = 4260B237BEB0FAB289A4F41A2D4033D60AFD2CA63D6616906A33A112704F13E8
sha256(仓内 help-assets.ts)  = 4260B237BEB0FAB289A4F41A2D4033D60AFD2CA63D6616906A33A112704F13E8
```

**三路同值**：换源路径不产生产物字节差异，且与仓内受跟踪产物逐字节一致。提交信息「产物字节不变」属实。

### 3.5 扣分项（5）

- §3.3 的证据表述不精确（不是「任一字节」）。S3。
- fixture 与另一受跟踪正本 `docs/skills/skill-schedule/t198-old-help-truth.md` 之间**没有机器对账**：改了正文档而不动 fixture，没有门会红（fixture→资产 有摘要锁，正文档→fixture 靠人）。S3／残余风险。
- 生成器改为「fixture 优先、工作副本兜底」后，**工作副本的更新不再影响生成结果**；若有人继续按老习惯改 `.scratch` 再跑生成器，会静默产出「看起来跑过了、其实读的是旧 fixture」的产物。改动本身是正确取舍（`.scratch` 不入库），但这条使用口径只写在文件注释里。S3。

---

## 4. #308 —— 种子库计划会话落进「本周」窗

**结论：PASS（93/100）。100/101→101/101 本机与真 CI 双向坐实；只动种子；「本周」自然周语义未被改。**

### 4.1 文件面：只动种子

```
$ git show --name-only --format="" aa7fd61
docs/research/t81-seed.mjs
```

`packages/skill-calorie/scripts/build-help.mjs`、`packages/skill-calorie/SKILL.md`、`packages/skill-calorie/src/workout/commands.ts` **一字未动**（不在该提交清单里）。改动本体只有一行：`VALUES (1, 1, 1, …)` → `VALUES (2, 1, 1, …)`（第 1 周第 1 天 → 第 2 周第 1 天），第 1 周第 3 天那行保持不变。

### 4.2 本机 101/101 与变异反证

```
iso 内（未变异）: node packages/skill-calorie/scripts/check-examples.mjs
  RESULT: 101/101
  PASS: SKILL.md 示例逐行可执行                                  EXIT=0

iso 内（变异：把种子改回 (1,1,1)）:
  RED   exit=4  calorie.view.exercise-review  envelopeKey=null
        calorie-cmd-read calorie.view.exercise-review --params '{"window":"本周"}'
        stderr="ERR 4: 取数失败（缺失阻断）：窗内无计划会话：2026-09-07 ~ 2026-09-07"
  RESULT: 100/101
  FAIL: 示例可执行门未过（禁止删示例／放宽断言；…）              EXIT=1
```

变异精确复现票面症状（100/101 ＋ 同一行 `exercise-review`），**且锁定改的就是那一行种子**。主树全量 `pnpm test`（含 `test/calorie-routing-81.test.mjs`）绿。

### 4.3 「本周」语义未被动过（#250 自然周）

- 该提交不含任何窗口解析代码；窗口实现 `packages/skill-calorie/src/analysis/series.ts:83`：
  `if (w === '本周' || w === 'week_cur') return [shiftISODate(today, mondayOffset), today];`（自然周＝本周一→锚点日）——与本批四票无关，未被触碰。
- 日期事实独立复算：`2026-09-07` = **Monday**，`2026-09-01` = Tuesday，`2026-08-31` = Monday ⇒ `mondayOf('2026-09-01') = 08-31`，第 1 周第 1 天落在 `08-31`（窗外），第 2 周第 1 天落在 `09-07`（窗内）。提交信息里的推导逐步可核。
- 受跟踪证据表独立佐证「本周 = 单日窗」：`docs/research/t81-route-evidence.md:525` 记 `看本周饮食 → --params '{"start":"2026-09-07","end":"2026-09-07"}'`。

### 4.4 真 CI 前后对照

| 运行 | sha | 票前/后 | ubuntu 22.13 | ubuntu 24.x | macos 22.13 | macos 24.x | win 22.13 | win 24.x |
|---|---|---|---|---|---|---|---|---|
| `34765949207` | `0354944` | 票前 | fail(100/101) | fail(100/101) | fail(100/101) | fail(100/101) | fail(STRUCT 0/101) | fail(STRUCT 0/101) |
| `34766518187` | `aa7fd61` | 票后 | **success** | **success** | **success** | **success** | fail(STRUCT 0/101) | fail(STRUCT 0/101) |
| `34766608919` | `6fa0685` | 票后 | **success** | **success** | **success** | **success** | fail(STRUCT 0/101) | fail(STRUCT 0/101) |

票后 4 条 LF 腿由红转绿 —— 种子修复在**真 CI** 生效。

### 4.5 Windows 两腿仍红：**票前既有**，非本票引入（必须明说）

票后 windows 两腿的失败原因是**另一件事**，与 `exercise-review` 无关：

```
示例行数：101（通过 0，白名单跳过 0，红 0，结构问题 1）用时 0.0s
STRUCT 产物不新鲜：AUTO 块 != 生成器输出（先跑 `node packages/skill-calorie/scripts/build-help.mjs`）
RESULT: 0/101
```

**决定性归属证据**：同一个 `STRUCT 结构问题 1`／`0.0s` 在**票前**运行 `34765949207` 的 windows 两腿就已存在（逐腿摘自日志，非排序误读）：

```
build-test (windows-latest, 22.13.0) || 示例行数：101（通过 0，…，结构问题 1）用时 0.0s
build-test (windows-latest, 24.x)    || 示例行数：101（通过 0，…，结构问题 1）用时 0.0s
build-test (ubuntu-latest, 22.13.0)  || 示例行数：101（通过 100，…，红 1，结构问题 0）
build-test (ubuntu-latest, 24.x)     || 例如上
build-test (macos-latest, 22.13.0)   || 例如上
build-test (macos-latest, 24.x)      || 例如上
```

⇒ 该红**不在四票文件面内、票前同腿同红、非本批引入**；本票也没有使它变差（windows 两腿票前票后同因同红）。

**但必须记录（S3-残余 → 见 §7 必须整改）**：`#308` 票面验收判据写的是「`pnpm help:examples:check` 在 master 上 **exit 0**」。当前 CI 上 windows 两腿仍非 0 ⇒ **该判据在 CI 全局未达成**，只是残余红的成因已换人。据此：**本票不应在 Windows 残余红另开票并处置之前被当作「CI 例列门已绿」结案**。扣 7 分的主因即此条 + 判据只在本机（LF）环境验证过。

---

## 5. 四票共同判定

### 5.1 ① `pnpm test` = 1492/1492（含一次并发污染 flake 的完整记录）

**第一跑（判定采用值）**：持锁主树实跑，HEAD 时点 `7228148`，`1492 tests / 1492 pass / 0 fail / 0 skipped / exit 0`。

**第二跑（同口径持锁，HEAD `b06bbfd`）：`1477 pass / 15 fail / 0 skipped / exit 1`**，15 条全部落在卡路里
**键登记／搬迁对账**面（`#294 对账：注册表每条声明与 cli/keys.ts 的登记逐条一致`、
`#295 对账：一个键恰住一处（未搬迁清单与能力目录不许声明同一个键）`、
`#81 唤醒词路由层`、`CALORIE_COMBOS 全量 ⊆ combos.yaml`、`覆盖门 · 全部写键逐键落库断言` 等）。

**归属与 flake 判定（按规则 30：红后同树原样复跑一次，两次不同判 flake）**：

- 该跑的日志头显示**入场即与别的席位抢锁**：`WAIT-OWNER-ALIVE pid=49244 ticket=318-land …`，
  即该窗口内有另一个搬迁席（`318-land`）在同一主树开窗，主树在跑动中被并行改写。
- 复跑取证：两跑中失败最狠的两个文件单独复跑 ⇒
  `node --test packages/skill-calorie/test/cmd-registry-294.test.mjs packages/skill-calorie/test/legacy-ratchet-295.test.mjs`
  = **13 tests / 13 pass / 0 fail / 0 skipped / exit 0**（持锁，`waitedMs=0`）。
- 第三跑（同口径持锁，同 HEAD `b06bbfd`）结果见 §5.1.1。

⇒ **判为 flake／并发污染，不是本批四票引入的回归**：15 条全部落在四票文件面之外的迁移热位
（`packages/skill-calorie/src/{diet,exercise,…}/`、`cli/keys.ts`、未搬迁清单），与 §5.2 的零热位结论互相印证；
第二跑的树在跑动中处于他人搬迁窗口的中间态。三次输出（`gate-pnpm-test.txt`、`gate-pnpm-test-2.txt`、
`attrib-294-295.txt`、`gate-pnpm-test-3.txt`）均留档备查。

> 规则 30 也已纳入本席执行：本报告涉及的全部全量 `node --test`／`pnpm test` 跑动**一律持
> `--lock-dir .scratch/locks-land`**（含隔离副本内的跑动——副本只隔离文件，不隔离 `%APPDATA%`／共享历史库）。

#### 5.1.1 第三跑（同树复跑，HEAD 仍 `b06bbfd`）

```
WAIT-OWNER-ALIVE pid=38248 ticket=316-fix …            ← 入场时锁被 316 席持有
LOCK-ACQUIRED ticket=308-312r runId=0d1d2793-… waitedMs=300128 pid=49764   ← 实等 300s
ℹ tests 1492   ℹ pass 1491   ℹ fail 1   ℹ skipped 0
LOCK-RELEASED ticket=308-312r runId=0d1d2793-… exit=1
```

唯一失败：`packages/skill-calorie/test/cmd-read-t11.test.mjs:136` 的
`T9 目标分析盘 parity：12 键抽查 stat + 缺失阻断`（栈：`cmd-read-t11.test.mjs:88 runOk` → `:148`）。

**这正是规则 30 点名的那条共享历史库污染面**（同文件 `cmd-read-t11.test.mjs`，读 `%APPDATA%`／共享历史库，
与「同一份内容两跑结果不同」的已知签名一致）。三跑同口径持锁对照：

| 跑 | HEAD | wait | 结果 | 失败面 |
|---|---|---|---|---|
| #1 | `7228148` | 10.0s | **1492 pass / 0 fail** | —— |
| #2 | `b06bbfd` | 30.0s（等 `318-land`） | 1477 pass / **15 fail** | 卡路里键登记／搬迁对账（15 条） |
| #3 | `b06bbfd` | 300.1s（等 `316-fix`） | 1491 pass / **1 fail** | `cmd-read-t11.test.mjs`（共享历史库） |
| 复跑取证 | `b06bbfd` | 0s | **13 pass / 0 fail** | `cmd-registry-294` ＋ `legacy-ratchet-295` 两文件 |

三次结果两两不同（0／15／1），失败集合也互不包含 ⇒ 按规则 30「两次不同判 flake」，**判 flake／多席并发污染**，
不判真回归；且三次的失败集合与本批四票文件面（`tooling/`、`.github/`、`packages/skill-schedule/`、
`docs/research/t81-seed.mjs`）**零交集**。判定采用的基线绿取第一跑：**1492/1492**。

### 5.1.2 第一跑（1492/1492）的口径

持锁主树实跑（`node tooling/run-locked.mjs --ticket 308-312r --lock-dir .scratch/locks-land --stale-minutes 30 -- pnpm test`，规则 26 口径：不经任何 `--force`、不删 `dist/`）：

```
ℹ tests 1492    ℹ pass 1492    ℹ fail 0    ℹ cancelled 0    ℹ skipped 0    ℹ todo 0
LOCK-RELEASED ticket=308-312r runId=4c52cd9e-… exit=0
EXIT=0   ELAPSED=104.5s
```

`skipped 0` 一栏对 #312 尤其关键：票前 `skipped 4` 的静默缺口已消失。基线绿走的是「不带 `--force`」路径，未触发 `client-bundle-48` 已知坑（无 `dist/client.js` 被裸 ESM 覆盖的动作）。

### 5.2 ② 文件面不含落点热位

```
4295867 clean files=1 :: tooling/test/skill-html-snapshot.test.mjs
30639ca clean files=2 :: .github/workflows/ci.yml, test/ci-gates-295.test.mjs
0e1fc51 clean files=3 :: packages/skill-schedule/scripts/gen-help-assets.mjs,
                         packages/skill-schedule/test/fixtures/t198-old-scenarios.json,
                         packages/skill-schedule/test/help-assets.test.mjs
aa7fd61 clean files=1 :: docs/research/t81-seed.mjs
```

按 `packages/skill-calorie/src/(diet|exercise|home|body|photo|weight|workout|analysis|goal|profile)/`、`packages/skill-calorie/src/cli/`、`src/triggers/`、`legacy/` 四类模式逐个匹配：**零命中**。

### 5.3 ③ 四提交在 `origin/master` 祖先链上

```
$ git merge-base --is-ancestor <c> origin/master
4295867 : True      30639ca : True      0e1fc51 : True      aa7fd61 : True
```

### 5.4 ④ 四票未被写成 100%／未被关闭

```
308  OPEN  卡路里 SKILL.md 例列门恒红：calorie.view.exercise-review「本周」示例取数失败
309  OPEN  CI 恒红之一：pnpm gate:selftest:html 期望值过期（memo 键 11≠10）
311  OPEN  CI 恒红之三：publish-gates 作业一步红吞掉其后两步（缺 if: always()）
312  OPEN  CI 上静默跳过 4 条测试：skill-schedule help-assets 依赖不入库的 .scratch 事实源
```
四票 `state=OPEN`、`labels=[]`、`closedAt=null` —— 无「已关闭」需要就其票面依据另做审查的情况。

---

## 6. 反假绿：主动攻击一条条打

已知坑逐条对照（至少两条主动攻）：

| 坑 | 主动攻击动作 | 结果 |
|---|---|---|
| `Copy-Item` 保 mtime ⇒ `tsc -b` 跳过（规则 22） | `node node_modules/typescript/bin/tsc -b --dry` 读只读判定；再在持锁批里真跑 `tsc -b`（不带 `--force`） | 12 个 project 报 `is up to date`（`base-combos` 仅会更新输出时间戳）；`tsc -b` EXIT=0。四个提交均不改 `src/**` 编译面，不存在「树变了但 tsconfig.tsbuildinfo 未动」的假绿窗口。**攻下：无破口。** |
| `gen:check` 读 `dist` ＋ 被 ignore 的印记残留污染（规则 22／24） | 核 `packages/skill-calorie/dist/.gen-inputs.json` 的判据形态与 ignore 归属 | 印记是**内容** sha256（`"note": …内容印记…不看时间戳`），由 `pnpm build` → `gen-cli.mjs --stamp` 写；`git check-ignore -v` 显示它被 `.gitignore:2:packages/*/dist/` 覆盖（不入库，不会被别人的残留提交污染）。持锁实跑 `gen:check` EXIT=0。**攻下：印记是内容判据不是时间戳判据，规则 22 的 mtime 路径在此不可达。** |
| `pnpm` 是 DSH 垫片、并发时可能静默卡死（规则 26） | 变异电池**全程直调 `node`**、不经 `pnpm`；主树批跑用一条锁内 `node .scratch/t308-312r/run-gates.mjs` 串行直调各门 | 无卡死；`pnpm test` 实测 104.5s 正常收敛。 |
| 「门跟着实现走」的假绿 | 四票各做**变异**（§1.4／§2.3／§3.3／§4.2），全部转红且红因正是设计意图 | 见各节。**攻下：无破口。** |

**变异记录汇总（5 次，全部在隔离副本 `git archive` 派生树内直调 `node`，主树零接触）**：

| # | 目标票 | 变异 | 期望 | 实测 |
|---|---|---|---|---|
| M1 | #309 | `dist/render/envelope.js` 摘掉 `'memo.wish'` | 期望值门红 | `fail 2`／`AssertionError: memo key 片段数`＋快照对比红 ✔ |
| M2 | #311 | 摘掉 `publish:fresh` 的 `if: always()` | 机器门红 | `fail 1`／「没带 if: always()」✔ |
| M3 | #312 | fixture 挪走 | 硬红非跳过 | `pass 2 / fail 5 / skipped 0`，`EXIT≠0` ✔ |
| M4 | #312 | fixture 里 prompt 改字 | 摘要锁红 | `fail 3` 含 `摘要锁` ✔ |
| M4′ | #312 | fixture 头部元数据改一字 | —（对照） | 只红「可重跑」，摘要锁不红 ⇒ 修正落地席表述（S3） |
| M5 | #308 | 种子回退到第 1 周第 1 天 | 100/101 红 | `RED exit=4 … 窗内无计划会话`＋`RESULT: 100/101` ✔ |

**主树批跑（一条锁内命令）汇总**（`logs/gates-main.txt`，HEAD=`b06bbfd5c32ece1963773a4b16ae745b35c298e9`，批量前 `git status --short` 为空）：

```
EXIT=0  tsc -b（不带 --force）
EXIT=0  gen:check（读 dist）
EXIT=0  snapshot:html:check
EXIT=0  boundaries
EXIT=0  ci-gates-295（#311 机器门）
EXIT=0  schedule help-assets（#312 主树实跑）
EXIT=0  help:examples:check（#308 例列门）
EXIT=2  gate:audit      ← 见下，复核席调用姿势问题，非仓内缺陷
EXIT=0  doctor
```

`gate:audit` 的 EXIT=2 是**我调用姿势**造成的：该工具必须带 `--evidence`（`FAIL: 缺少 --evidence <证据文件>`）。同时记录一条**票外**观察：仓内登记的 `"gate:audit": "node tooling/check-gate-audit.mjs"` 是不带参数的裸调用，照登记口径跑必然 exit 2；该脚本不在 `.github/workflows/ci.yml` 里（非 CI 门），与四票无关，按「不替它们改代码」原则未动，登记在 S3-票外。

### 6.1 并发声明（影响结论解读，不影响四票判定）

复核窗口内**另有多个票在并行落库**，主树 HEAD 从开窗的 `66963aa` 一路推进（`7228148` → `e9dc652` → `b06bbfd` → …），其间我观测到一次 `git status --short` 有他人未提交改动（`docs/skills/skill-calorie/t316-搬迁-证据.md`、`packages/skill-calorie/src/exercise/routes.ts`），随后被其自行提交。处理方式：

- `pnpm test`（1492/1492）跑在窗口早期时点；门批跑（§6 表）钉在 **`b06bbfd`** 且跑前 `git status` 干净。
- 四票的判定**不依赖**这些后续提交：全部证据取自四个固定提交对象本身（`git show`）、受跟踪正本（快照 JSON、证据表、fixture）与隔离副本（`git archive` 冻结树）。
- 真 CI 归属对照（§2.4／§4.4）取自 `0354944`（票前）与 `aa7fd61`／`6fa0685`（票后）三个固定 run，不随后续 HEAD 漂移。

---

## 7. 三级清单

### S1（假绿／覆盖造假／门失效，阻断）—— **0 条**

无。四票各自的门在变异下均转红，且红因与设计意图逐字对应；没有把断言放宽成「不检查」、「跳过也算绿」或把红改绿的写法（全文 `continue-on-error` 0 处）。

### S2（票面判据未达成／行为与声明不符，阻断）—— **0 条**

无（口径见 §0 门槛声明）。四票在**本报告逐维判据**下全部达成，且四票的声称都能被真 CI 或独立复算坐实。

### S3（口径／表述／残余风险提示，不阻断）—— **8 条**

1. `#309`：门只钉**条数**不钉**键名**；单键改名不会触发 `EXPECT_KEYS`（由同文件受跟踪快照对比兜住，实测该路径会红）。
2. `#309`：期望值列表没有可一键重跑的复算脚本，复算口径只写在注释里。
3. `#311`：机器门只验证 `if: always()` 存在，不验证该步内容仍是门。
4. `#311`：`pnpm install`／`pnpm build` 两步仍无 `if: always()`（正确取舍），但「遮蔽面只剩这两步」这层口径票面未写。
5. `#312`：提交信息「fixture 改**一个字节** ⇒ 摘要锁转红」表述不精确——只有改在 `prompt` 内的字节才触发摘要锁，改头部元数据只触发「可重跑」（M4′ 实测）。
6. `#312`：fixture 与另一个受跟踪正本 `docs/skills/skill-schedule/t198-old-help-truth.md` 之间无机器对账。
7. `#312`：生成器改为「fixture 优先、`.scratch` 兜底」后，工作副本更新不再影响生成结果；老习惯（改 `.scratch` 再生成）会静默读到旧 fixture。
8. `#308`：票面验收判据「`pnpm help:examples:check` 在 master 上 exit 0」在真 CI 全局**未达成**（windows 两腿仍红），残余红经票前/票后逐腿对照证明为**既有独立成因**（`AUTO 块 != 生成器输出` / `STRUCT 结构问题 1`），非本票引入；判据本身只在本机 LF 环境验证过。

### S3-票外（不在四票范围内，仅备案）

- `package.json` 登记的 `gate:audit` 是不带 `--evidence` 的裸调用，照登记命令跑必 exit 2（该脚本非 CI 门）。
- 真 CI 上 `pnpm test` 在 6 条腿仍红，成因为 `dsh-*-ilife client`（`client-bundle-48` 一族）、`#237 ⑤ 文件名安全化`、`#91 ② mode 显式三态`、`#75 共享样式资产`、`#193 打包技能提供方（居家线）`、`#204 ⑤ 退出码矩阵`等**票外**既有红；与四票文件面零交集。`publish-gates` 的 `pnpm publish:fresh` 红（`FAIL: dsh-calorie 契约键 calorie.help.center exit1`、`FAIL: G3 安装态断言红`）同属票外。

---

## 8. 必须整改（≤3 项）

1. **为 Windows 侧 `help:examples:check` 的 `STRUCT 产物不新鲜：AUTO 块 != 生成器输出` 另开票并处置**，处置前 `#308` 不得按「CI 例列门已绿」结案（判据是「master 上 exit 0」）。整改证据须给出 windows 两腿 `RESULT: 101/101` 的真 run 编号。
2. **把 `#312` 的证据表述改准**（提交信息层面，若能以追加说明/票面评论方式更正）：摘要锁只钉 `prompt` 字节，不要写成「改一个字节就红」。并补一条「改正文档不改 fixture 不会红」的已知缺口提示。
3. **在 `#311` 的票面/说明里写明残余遮蔽面**：`pnpm install`／`pnpm build` 一红时五道门仍会被跳过（有意为之），避免后续把「加了 `if: always()`」误读成「任何情况下门都会跑」。

---

## 9. 未完成的动态项

- `34766829944`（`7228148`）、`34766947884`（`e9dc652`）、`34767069581`（`b06bbfd`）等 run 在复核结束时仍 `in_progress`／`queued`；#311／#308 的**后效**已用已完成 run（`34766518187`／`34766608919` 对 `34765949207`）坐实，但若这些在途 run 出现新的失败腿，需按 §7.1 口径归因（是否落在四票文件面）。
- `pnpm test` 复核窗口内共三跑（§5.1）：第一跑 `1492/1492` 采用为基线绿；第二／第三跑分别 `1477/15`、`1491/1`，
  失败集合互不包含且与本批四票文件面零交集，按规则 30 判 **flake／多席并发污染**（含规则 30 点名的
  `cmd-read-t11.test.mjs` 共享历史库面）。**四票各自的门在三次窗口内全部独立复跑为绿**（§1.3／§2.2／§3.2／§4.2）。
  建议编排者收窗时在无并行席位的主树上再跑一次 `pnpm test` 取终值。
- 四票保持 OPEN、未代为关闭；本报告未改四票票面、未替四票改代码。

---

## 10. 原始证据索引（均在本仓内可复跑）

| 证据 | 路径 |
|---|---|
| `pnpm test` 全量日志（1492/1492，采用值） | `.scratch/t308-312r/logs/gate-pnpm-test.txt` |
| `pnpm test` 第二跑（1477/15，flake） | `.scratch/t308-312r/logs/gate-pnpm-test-2.txt` |
| `pnpm test` 第三跑（1491/1，flake） | `.scratch/t308-312r/logs/gate-pnpm-test-3.txt` |
| flake 复跑取证（294＋295 两文件 13/13 绿） | `.scratch/t308-312r/logs/attrib-294-295.txt` |
| `pnpm gate:selftest:html` 日志（exit 0） | `.scratch/t308-312r/logs/gate-selftest-html.txt` |
| 主树门批跑（9 门 EXIT 汇总） | `.scratch/t308-312r/logs/gates-main.txt` |
| 门批跑时点 HEAD | `.scratch/t308-312r/logs/head-at-gates.txt` |
| 真 CI 失败日志（票前 `0354944`／票后 `6fa0685`） | `.scratch/t308-312r/logs/ci-0354944-failed.txt`、`ci-6fa0685-failed.txt` |
| 门批跑脚本（可重跑） | `.scratch/t308-312r/run-gates.mjs` |
| 隔离副本（`git archive` HEAD ＋ dist 复制 ＋ node_modules 联接） | `.scratch/t308-312r/iso/`、`.scratch/t308-312r/iso.zip` |

---

## 11. 票面关闭审查（复核窗口内四票被并行关闭）

**事实**（复核席用 `gh issue view` 逐票取的当刻状态）：

```
308 : CLOSED  closedAt=2026-09-13T16:02:36Z  reason=COMPLETED
309 : CLOSED  closedAt=2026-09-13T16:02:40Z  reason=COMPLETED
311 : CLOSED  closedAt=2026-09-13T16:02:43Z  reason=COMPLETED
312 : CLOSED  closedAt=2026-09-13T16:02:47Z  reason=COMPLETED
```

四票在开窗复核时均为 `OPEN`、`labels=[]`；`--json comments` 取回的**评论列表为空** ⇒ 四票的
关闭**没有留下任何关闭评论／证据**，关闭依据不在票面上。

**逐票核「关闭依据是否成立」**：

| 票 | 关闭依据是否成立 | 理由（全部取自本报告已取证的机器事实） |
|---|---|---|
| `#309` | **成立** | 受跟踪正本期望值 11 与活产物一致；`pnpm gate:selftest:html` exit 0（10/10）；变异即红（M1）。 |
| `#311` | **成立** | 五道门逐行带 `if: always()`；机器门 2/2；变异即红（M2）；**真 CI 前后对照**第 10／11 步 `skipped`→`success` 且作业仍 failure。 |
| `#312` | **成立** | 无 `.scratch` 环境 7 pass／0 skipped；缺源硬红（M3）；prompt 变异摘要锁红（M4）；产物三路 sha256 同值。 |
| `#308` | **不成立（判据未全局达成）** | 票面验收判据原文：**「`pnpm help:examples:check` 在 master 上 exit 0」**。真 CI 上 windows 两腿仍非 0（`STRUCT 产物不新鲜：AUTO 块 != 生成器输出` / `RESULT: 0/101`，见 §4.5）。种子修复本身成立且在 4 条 LF 腿已绿，但**按票面判据的字面形式，该判据未达成**。 |

**结论与风险**：

1. `#308` 以 `COMPLETED` 关闭**与其票面验收判据冲突**。残余红经票前／票后逐腿对照证明为既有独立成因、
   非本票引入（§4.5），故**不是**「修复失败」，但**是**「判据未闭合」——按票面判据它不应被标 `COMPLETED`。
2. 四票关闭均无评论 ⇒ 后人无法从票面看出「依据是哪些 run／哪些命令」，`#308` 尤其容易被后续读者
   误读成「CI 例列门已绿」。本报告 §2.4／§4.4 的 run 编号与逐腿表可直接用作关闭评论的证据。
3. 关闭动作发生在复核窗口内（16:02:36Z–16:02:47Z），本席未参与、未代为关闭、未改票面。

**建议（已并入 §8 必须整改）**：

- `#308` 重开，或把判据改写为「`exercise-review` 示例在 LF 腿通过」并**另开票**处置 Windows 侧
  `AUTO 块 != 生成器输出`；
- 四票补一条关闭评论（run 编号 ＋ 命令 ＋ 结果），`#311` 直接引用 `34765949207` → `34766518187`
  的前后对照。