# t603 复核证据（复核 #591 D2 残留收口 · 独立重跑 ＋ 裁定1 边界 ＋ 新探针 ＋ 变异）

> 复核对象：#591 的七笔提交 `c6bea9c`／`3322375`／`7c7db05`／`18a62c4`／`f3b73c2`／`d68616c`／`0c105c8`。
> 本席**不采信实施者任何结论**：编译、门禁、族测试、场景10 回归、边界、变异一律自己重跑；
> 被审席证据件 `docs/skills/skill-calorie/t588-证据.md` 只当**被核对的声明**用。
> 运行对账源：`docs/skills/skill-calorie/t603-运行记录.md`（协议 §2.3.4 的导出件）。
> 本席窗口内**只写** `docs/skills/skill-calorie/t603-*` 与 `.scratch/t603/**`；产品代码与他人测试一字未动
> （唯一例外是两次源码级试验——场景10 前后比对与变异确认——全程在同一个持锁窗口内，退出前逐字节还原并自证）。

## 一、回执四段

**第一行：PASS。** 票面五项验收全部由本席独立跑出与实施者一致的读数：编译 exit 0；门禁 83 件**非脚注零命中 83/83**
（本席用 `readOne` 逐条真跑产出的**新页**点名，比被审席审墙内拷贝更硬）；票面 8 件族测试 269 测／267 绿／2 红
（两红＝`t275 ①`／`t275 ⑥`，票面已点名归他席，本席用七笔提交的 diff 复核了「改动面不碰这两条路径」）；
场景10 回归 12 件 82 测／79 绿／3 红（三红全在 `analysis-t5.test.mjs`，陈旧断言，范围外）；裁定1 的
「只改两函数体内」由**静态（逐 hunk 落点 ＋ 函数外逐字节相同）＋动态（前后比对 6 页）**两路证明成立；
自设新探针（字段变更表真值 vs 库内实值）9/9 一致；变异改坏红／还原绿两行读数齐。无 S1，五维 **96/100**。

**第二段：被复核提交（七笔，逐笔经 `git diff --cached --name-only` 观其件）＋ 本席 GATE-RUN 表**
（一律 `node tooling/run-locked.mjs --ticket 603 -- <命令>`；全量条目见 `t603-运行记录.md`）

| 被复核提交 | 内容（本席核对结果） |
|---|---|
| `c6bea9c` | 饮食总览 8 页去分隔符（`DOC_TITLE`／眉标去 `·`、`MEAL_NOTE` 的 `、`→`和`、口径行拆两行）＋两条断言跟改——diff 全在可见文本，未碰脚注与复制区接线 |
| `3322375` | 餐别分布结论句 `；`→`。` ＋两条断言跟改 |
| `7c7db05` | 营养分析／六因素两页页级去分隔符（裁定1）＋两条断言跟改——边界见第三节 |
| `18a62c4` | 回执新增类印库里真值（裁定4）＋预检失败原因字段名换中文标签（裁定5）＋t496 陈旧断言跟改 |
| `f3b73c2` | t271 餐别支页名断言跟改——**是收紧**（旧：子串含窗口的页名；新：`<h1 class="ilife-block-page-shell-title">餐别分布</h1>` ＋新增窗口读法一条） |
| `d68616c` | 场景02 墙重出 83 件＋两墙／总索引／链路总表（85 文件）；`manifest.json` 的 diff 实测**只有 `readings.字节区间` 一行**（`70,974 – 169,101 B` → `70,915 – 169,127 B`，中位不变） |
| `0c105c8` | 证据件 `t588-证据.md`（取代 #587 缺失的证据件） |

| 本席趟次 | GATE-RUN runId | RESULT／退出码 |
|---|---|---|
| 编译 `tsc -b packages/skill-calorie --force` | `3fb54fb1-ae64-454c-bf18-c6c541d328cb` | waitedMs=0 **exit 0** |
| 票面 8 件族测试 | `e66e3982-cc37-497d-9f39-d00db99617db` | waitedMs=0 **exit 1** → 269 测／267 绿／**2 红**（`t275 ①`／`t275 ⑥`） |
| 场景10 回归 `analysis-*.test.mjs`（12 件） | `b6fe01fa-afca-4f33-86b5-8ddecaf0222e` | waitedMs=70063 **exit 1** → 82 测／79 绿／**3 红**（全在 `analysis-t5`） |
| 门禁 83（本席 `readOne` 重跑产物逐件点名） | `affeb998-c20a-4a84-8f72-89f8e2ce066e` | waitedMs=0 **exit 0** → 非脚注零命中 **83/83** |
| 裁定1 前后比对（场景10 六页，窗口内换旧版↔还原） | `9633c023-285c-4864-b3ed-a264052c0c53` | waitedMs=10012 **exit 0** → 变 2 页／未变 4 页 |
| 新探针（页格 vs 库内实值） | `5ad3c996-4b8c-4dc9-b74c-01e26c1d433f` | waitedMs=0 **exit 0** → 9/9 一致 |
| 变异确认（改坏→红→还原→绿） | `c55dacbc-9638-48c6-aaac-ca3224b792b9` | waitedMs=0 **exit 0** → 两行读数见第五节 |
| 告警线台账门（工程红线） | `5a62f6d6-7802-4518-98da-72fd59f5c2b0` | waitedMs=30002 **exit 0** → `RESULT: 91/91 PASS` |
| 同上、对**已提交的**台账版本复核 | `3cff95f7-84d4-4249-9636-cc613420ba1b` | waitedMs=120011 **exit 0** → 91/91 绿（台账由他席 `2eeecb4` 同步过） |

**第三段：逐条路径**

- 本席产出（新件，全部在只写清单内）：`docs/skills/skill-calorie/t603-证据.md`（本件）、
  `docs/skills/skill-calorie/t603-运行记录.md`（运行条目导出件）。
- 本席草稿（不入仓）：`.scratch/t603/**` —— `gate83.mjs`（自写门禁驱动）、`scene10-roundtrip.mjs`（裁定1 前后比对）、
  `probe.mjs`（新探针）、`mut-red.mjs`（变异）、`boundary.mjs`（hunk 落点静态复核）、`lib.mjs`／`runner.mjs`（受锁跑法＋UTF-8 日志）、
  `export-runs.mjs`、`peek-overview.mjs`，与 `tsc.log`／`ticket8.log`／`analysis.log`／`gate83.log`／`roundtrip.log`／
  `probe.log`／`mut-red.log`／`boundary.log`／`warningline*.log` 及各自 JSON 读数。
- 被复核件（本席**只读**，一行未改）：`packages/skill-calorie/src/render/dietDocs.ts`（LF 350）、`src/diet/review.ts`（165）、
  `src/render/trendMiscPortDocs.ts`（608）、`src/diet/receipt.ts`（425）、`src/diet/precheckPort.ts`（253）；
  `test/{t496-文案统一,t511-清尾二,diet-homogeneity-108,diet-list-t271,t273-review-meal}.test.mjs`；
  `docs/skills/skill-calorie/scene02-验收墙/**`。

**第四段：没做完啥 ＋ 下一手缺啥**

- **没做（并写明为什么）**：① **未跑 `scene02-验收墙/restage.mjs`**——它会写 `docs/skills/skill-calorie/scene02-验收墙/**`
  与 `.scratch/restage-83/**`，两者都不在本席只写清单内（跨界即停手）；改用**等价且更硬**的路子：自写门禁
  `readOne` 重跑 83 词、审当趟新产出的页，结果与被审席的墙内读数一致。② 原拟的「空态分支分隔符门」探针**改成**
  了裁定4 的真值探针（见第四节）：前者查过 `manifest.json` 的空库态记录——空库有 61 条读命令按设计 exit 4 不落盘，
  可审的只剩 10 条窗口族，盘子太小、诊断力弱；留作备选，本席只交一个探针。
- **他席红（只点名，不代修）**：`t275 ①`「看饮食总览 缺来源脚注：数据来源 · 饮食记录 · … 共 10 条」归 **#560**；
  `t275 ⑥`「看饮食总览 的复制日志按钮有日志了——接线/装配已变」归 **#399**；`analysis-t5.test.mjs` 三条陈旧断言
  （`:168` 期望 `/startA/`、`:181` 期望 `平台期首日`、`:232` 期望 `forecast.points.length === 13`）——范围外，
  逐条scope 判定见 §2.5。
- **下一手缺啥**：墙若要跟最新公共层需重出一趟（`8920faa`／`09f8f68` 落在被审席重出之后）；终审 vision 打分另立；
  本席不推、不 close、不改产品代码，做完停止等派。

## 二、独立重跑逐条读数

### 2.1 编译

`node tooling/run-locked.mjs --ticket 603 -- node node_modules/typescript/bin/tsc -b packages/skill-calorie --force`
→ **exit 0**（runId `3fb54fb1`）。编译入口按硬规矩写死 `node node_modules/typescript/bin/tsc`，未用 `npx tsc`。

### 2.2 门禁 83：**83/83 非脚注零命中**（本席自写驱动）

本席没有复用被审席的 `.scratch/t588/gate83.mjs`，而是自己写驱动：按运行期总表 `scene === '02'` 取 83 条 →
**逐条 `readOne`（种子快照 ＋ 逐条还原 ＋ 真出口）重跑** → 把**这一趟新产出**的 83 个文件逐件点名
`packages/skill-calorie/scripts/audit-separators.mjs`。

```
GATE83 运行期场景02 = 83 条；有产物 = 83/83
GATE83 非脚注零命中 = 83/83  来源脚注命中(归#560) = 66  全节点命中 = 66
       totals = {"R1":66,"R2":0,"R3":52,"R4":0,"R5":0,"R6":0,"R7":0}
GATE83 被审门脚本自身 exit=1（脚注行按设计仍算债）
GATE83-PASS
```

- **七规则分项**：`R1 66 ／ R2 0 ／ R3 52 ／ R4 0 ／ R5 0 ／ R6 0 ／ R7 0`；66 处全节点命中**全部**是
  `ilife-block-caliber` 的 `📊 数据来源 · …` 来源脚注行（归 #560），扣掉脚注后 R2（`；`）／R4（`｜`）／
  R5（`、`）／R6（`~`）／R7（机器标识）在本批可见文本里**全零**。
- 与被审席读数**逐数相同**（他们的 `gate83-summary.json`：`cleanNonFootnote 83／footnoteHits 66／nodeHitsTotal 66／同 totals`），
  但本席这一趟审的是**当刻真跑出来的页**，不是墙里的拷贝——链条上少一层「拷贝是不是当刻的」的疑点。

### 2.3 票面 8 件族测试（269 测／267 绿／2 红）

```
ℹ tests 269  ℹ pass 267  ℹ fail 2
✖ #275 ① 逐块对得上老实物（diet_overview.html）—— 看饮食总览
    AssertionError: 看饮食总览 缺来源脚注：数据来源 · 饮食记录 · 2026-09-01 → 2026-09-07 · 共 10 条
✖ #275 ⑥ 裁定 7：复制区双按钮 ＋ 日志第 4 段＝本次命令原文 —— 看饮食总览
    AssertionError: 看饮食总览 的复制日志按钮有日志了——接线/装配已变
```

其余 7 件（`t511-清尾二`／`diet-homogeneity-108`／`diet-list-t271`／`t496-文案统一`／`t273-review-meal`／
`t272-排行榜页`／`t279-真出口用例`）全绿，与被审席同趟读数一致（他们 `be5b1962`：同 269/267/2）。

### 2.4 场景10 回归（12 件 82 测／79 绿／3 红）

```
ℹ tests 82  ℹ pass 79  ℹ fail 3
✖ weightCompare：17 场景分发        （analysis-t5.test.mjs:168 期望 /startA/）
✖ weightCompare：平台期/极端月/同期  （:181 期望 '平台期首日'）
✖ simulate：外推+模拟+摄入          （:232 期望 forecast.points.length === 13，实测 14）
```

### 2.5 他席红逐条点名 ＋ 本席的**范围判定**（不看实施者的话，看 diff 与出处）

- **范围判定的机械依据**：七笔提交触及的件并集＝5 个 src（全在 #591 只写清单内）＋5 个测试（同）＋
  `scene02-验收墙/**`＋`t588-证据.md`；`git diff c6bea9c^ 0c105c8 -- <analysis-t5>` **为空**（该件七笔未碰）。
- `t275 ①／⑥`：`c6bea9c` 对 `dietDocs.ts` 的 diff 逐行核过——改了 `DOC_TITLE`／`MEAL_NOTE`／口径行（`；`→两行）／
  eyebrow 四处可见文本，**没有一行**碰来源脚注或复制区（日志按钮）接线；且 #591 票面本身就写明
  「屏上来源脚注行一律不碰（归 #560）」，并预先把这两条红点名为 #560／#399 ⇒ **范围外**。
- `analysis-t5` 三条：三条断言背后的件分别是他席的
  `src/weight/compare.ts`（末次改动 `48aa870`，2026-09-15 13:26——早于 `c6bea9c` 的 09-16 12:54）、
  `src/weight/weightCompare2.ts`（#481 整改）、`src/analysis/simulate.ts`（末次改动 `7831393`，2026-09-16 04:12，
  亦早于本票首笔）；且测试件自 `a39b86a`（2026-09-13）未与源侧同步 ⇒ **范围外（他席陈旧断言）**，只点名不代修。

## 三、裁定1 边界复核（重点；本席自设办法，两路）

### 3.1 静态：逐 hunk 落点 ＋ 函数外逐字节相同

办法（`.scratch/t603/boundary.mjs`，本席自写）：取 `7c7db05^` 与 `7c7db05` 两个 blob；用 TypeScript 编译器 API
解析两份源码、取每个**顶层函数声明**的精确 `[start,end)`；解析 `git show --unified=0` 的补丁，逐个 hunk 记
旧／新行区间；断言每个 hunk **整体**落在两函数之一内部；最后把两份源码里这两个函数的区间**挖空**后逐字节比对。

```
OLD-BLOB 61d418c469d11020e098ff60455c731f0a0705e5 lines=609
NEW-BLOB f6ce9792c58bc5319f47a6101d97a2e20bf8fd48 lines=609
HUNKS 8   →  @335／@337,3／@385／@409,3／@414 → buildNutritionAnalysisDoc（旧新皆 330–418）
             @476／@478／@482,2          → buildSixFactorsDoc（旧新皆 442–487）
             逐 hunk 判＝OK（无一落在别的函数或模块级）
模块常量 DOC_TITLE 旧=新="const DOC_TITLE = '卡路里·趋势';"
OUTSIDE-BYTES-EQUAL true  (oldLen=23643 newLen=23643)
BOUNDARY-PASS
```

⇒ 被审席说的「四个 hunk」是**默认上下文合并**后的分组（`@@ -332,11`／`@@ -382,7`／`@@ -406,12`／`@@ -473,14`），
本席按 0 上下文重解析得 8 个精确 hunk，落点结论**相同且更细**；「模块常量与其余五个函数一字未动」这条
由 `OUTSIDE-BYTES-EQUAL true` 独立成立（连注释与空行都一样）。

### 3.2 动态：场景10 六页「前后比对」（票面点的那个办法）

办法（`.scratch/t603/scene10-roundtrip.mjs`）：该件 6 个场景10 `build*Doc` 的服务键各取总表第一条，
三趟真跑同一组 6 页——**NEW**（当刻源码）→ **OLD**（把该件整份换成 `7c7db05^` 的 git blob → 重编译 → 再渲染）
→ **NEW2**（逐字节还原当刻源码 → 重编译 → 再渲染）。换源码与还原**全程在同一个持锁窗口内**完成，
`finally` 兜底还原，退出前用 sha256 与 `git diff --quiet` 双复核。

```
SRC-BEFORE sha256=74b5a388cafc42ec bytes=30378
PHASE-NEW  ok pages=6        PHASE-OLD  build exit=0 ok pages=6        PHASE-NEW2 build exit=0 ok pages=6
CHANGED   calorie.view.nutrition-analysis  旧页sha=590d07076f6cf1e7 新页sha=d3eab859652e2688  判=允许
CHANGED   calorie.view.six-factors         旧页sha=b14f0765264c44e5 新页sha=b1ea71aa4bc29f37  判=允许
UNCHANGED calorie.view.calorie-trend   sha=30ba8d8ca8b6dddd
UNCHANGED calorie.view.long-trend      sha=9cefa87cae3f6af0
UNCHANGED calorie.view.lint-health     sha=b121036020cf0a9b
UNCHANGED calorie.view.review-template sha=f4aea4b2a543b85a
RESTORE-SELFCHECK 新旧一致=true（NEW vs NEW2 逐页 sha256）
SCENE10-ROUNDTRIP pages=6 changed=2 unchanged=4 → PASS
GIT-DIFF-CLEAN true sha256=74b5a388cafc42ec 进场=74b5a388cafc42ec
```

⇒ **场景10 这 6 页里只有裁定1 点名的两页变了，另外 4 页与改动前逐字节相同**；`NEW2 ≡ NEW` 证明还原干净、
中途那一趟 OLD 没有留下任何残留。该件第 7 个函数 `buildProcessProgressDoc` 属场景05（看落地训练进度），
本席种子里没有训练计划、缺计划即 exit 4 不落盘，故未进动态比对——它由 3.1 的「函数外逐字节相同」覆盖。

## 四、自设新探针（被审脚本覆盖不到的角度）：字段变更表真值 vs 库内实值

**盲区在哪**：裁定4 要求「记一餐那张 9 行表印**库里的真值**」。被审席的自证
（`.scratch/t588/probe-write.log`）比的是**同一个页面**的改前／改后两列——**从头到尾没开过库**。
页比页，脚本自己就能满足自己：万一 `writtenRowOf()` 认错了行（例如按 id 命中别的日期那一行），
页上照样是「一堆真数字」，被审那套读数却读不出来。

**本席换一头量**（`.scratch/t603/probe.mjs`）：真跑「记一餐」→ **直接开库读那一条 `food_log` 行** →
把页上「📋 字段变更」9 行的「本次结果」逐格与库内实值对字符串（缺值口径照 `cellOf`：null／空串印 `—`）。

```
PROBE 真跑「记一餐」 exit=0（库=.scratch/t603/run/probe-write/db/calorie_data.db）
PROBE 库内真值 food_log 行数=76 id=76 {"id":76,...,"food_name":"鸡胸","calories":200,"protein":35,"carbs":0,"fat":0,"grams":100,"note":null,"date":"2026-09-16","time":"13:39:34"}
  格 食物名 页=鸡胸         库=鸡胸         同      格 热量   页=200    库=200     同
  格 蛋白质 页=35           库=35           同      格 碳水   页=0      库=0       同
  格 脂肪   页=0            库=0            同      格 克数   页=100    库=100     同
  格 备注   页=—            库=—            同      格 日期   页=2026-09-16 库=2026-09-16 同
  格 时间   页=13:39:34     库=13:39:34     同
PROBE-READING 页格 vs 库内实值 一致 = 9/9  不符 = 0
PROBE-PASS
```

⇒ 裁定4 不只是「页上不再复读 `—`」，而是**页上那 9 格逐格等于库里那一行**（含写口填的缺省 0／100、
真正的空备注印 `—`、写入日期与时刻）——这条读数被审席那一套量不出来，本席补上了。（本席第一版探针
把「碳水」反查成了 `carbohydrates` 列而不存在，属**本席探针自身**的取列逻辑缺陷，已改成「按域表候选逐个试到
库里命中那一列」，与产品代码无关，如实记在这里。）

## 五、变异确认（源码级，持锁窗口内改坏→红→还原→绿）

改坏的那一处**不是**被审席用的那处（他们改 `dietDocs.ts` 的 `MEAL_NOTE`），本席改的是
`src/render/trendMiscPortDocs.ts` 里 `buildNutritionAnalysisDoc` 页内 KPI 说明：
`（建议 10% 至 20%）` → `（建议 10~20%）`——落在**裁定1 的页级改动**上、探的是 R6（`~`）那条判据。

```
MUT-BASE   auditExit=0  零非脚注命中页=2/2  非脚注命中件=0  totals={"R1":0,"R2":0,"R3":0,"R4":0,"R5":0,"R6":0,"R7":0}
MUT-RED    auditExit=1  零非脚注命中页=1/2  非脚注命中件=1  totals={"R1":0,"R2":0,"R3":0,"R4":0,"R5":0,"R6":1,"R7":0}
           MUT-RED-BAD 营养分析_….html nonFoot=1 :: R6@L1814「18%（建议 10~20%）」
MUT-VERIFY ok（源已逐字节还原 sha256=74b5a388cafc42ec）
MUT-GREEN  auditExit=0  零非脚注命中页=2/2  非脚注命中件=0  totals={"R1":0,"R2":0,"R3":0,"R4":0,"R5":0,"R6":0,"R7":0}
MUT-PASS（改坏必红、还原必绿、源一致）    GIT-DIFF-CLEAN true
```

两行机器读数（票面要的那两行）＝ **`MUT-RED …非脚注命中件=1 R6=1`** 与 **`MUT-GREEN …非脚注命中件=0 R6=0`**；
还原一致性另有 `MUT-VERIFY ok` ＋ `GIT-DIFF-CLEAN true` 两处佐证。

## 六、五维打分（任一 S1→FAIL；均分<85→FAIL）

| 维度 | 分 | 依据（本席自己跑出来的读数） |
|---|---|---|
| 契约一致 | **30**／30 | 票面五项全达：门禁 83 非脚注 83/83（自跑复现）、族测试 267/269 绿＋2 条票面已点名的他席红、场景10 79/82 绿＋3 条范围外陈旧断言、墙重出（manifest 只动字节区间一行，85 件）、证据落盘（`t588-证据.md`）。五条裁定逐条核过：①边界两路证明；②③在 #587 已落、本席复跑 `t511` 全绿；④本席新探针 9/9；⑤墙页实测 `缺少必填字段: 脂肪 (g)`，无裸字段名。 |
| 证据真实 | **24**／25 | 被审席列的 **12 个 runId 逐条在 `gate-runs.log` 里查到**，票号／命令／退出码与声明一致（见 `t603-运行记录.md` §二）；三类核心读数（269/267/2、82/79/3、门禁 totals）本席独立复现**逐数相同**；before/after 表与本席探针的库内真值一致；红的三趟如实标红、没拿红充绿。扣 1：门禁那一步被审席审的是**墙里的拷贝**（票面写的是「`readOne` 跑 83 词」），链条比票面口径弱一档（墙确由 `restage` 的 `readOne` 产出、本席复跑也一致，故只扣方法学一分）。 |
| 新旧对照 | **19**／20 | 5 处测试跟改**逐处核过**：`diet-homogeneity-108`／`t496`（两处）／`diet-list-t271`／`t273-review-meal` 是等价跟改（只把期望串换成现行串，`gone` 项一条未撤）；`f3b73c2` 是**收紧**（旧子串断言 → 精确 H1 ＋新增窗口读法一条）。前后读数齐：门禁 72/83→83/83、回执 9 行 before/after、变异红绿两行。扣 1：`t275`／`t511` 一类「他席红」只做了点名，没有给出「若接线后本席写法是否需要跟改」的下一步判据。 |
| 工程红线 | **14**／15 | 七笔提交的件并集**全在** #591 只写清单内（5 src＋5 test＋墙＋证据件），无 `git add -A`／危险 git 命令可用面（提交均为 `add <自己路径>`＋`commit`）；编译／测试／变异一律经 `run-locked` 并留痕；告警线台账门对**已提交版本**复核 `91/91 PASS`（`receipt.ts` 425 与台账一致；被审席当时报的 87/91 四条陈化行已由他席 `2eeecb4` 同步）；超线件 `receipt.ts`(425)／`trendMiscPortDocs.ts`(608)／`dietDocs.ts`(350 压线) 的第四步说明在台账结论列与证据件里都有。扣 1：被审席未按协议 §2.3.4 把窗口内的运行条目**导出到受版本控制的路径**（只在未受控的 `.scratch/t588/*.log`），本席代为导出到 `t603-运行记录.md`。 |
| 文档同步 | **9**／10 | `t588-证据.md` 覆盖齐：七笔提交逐笔、GATE-RUN 表、逐条裁定落地（含 before/after 表）、门禁三分项读数、变异两行、日志清单、以及「明确没做完的」两项；并写明取代 #587 缺失的证据件。扣 1：证据里没把「门禁那一步审的是墙内拷贝、不是当刻 `readOne` 产物」这条口径差别写明（读者只能从 GATE-RUN 表倒推）。 |

**合计 30＋24＋19＋14＋9 = 96／100 ⇒ PASS**（无 S1：无超越只写清单、无达标缺失、无假绿）。
（若总工按更严口径把「门禁审墙拷贝」计为口径偏离，最低也落在 94，仍在 PASS 线上。）

## 七、其它复核点（顺手核到的）

1. **裁定5 落地实测**（墙页逐字读）：`校验批量导入.html` 与 `看批量导入预览.html` 的可见文本里
   `缺少必填字段: 脂肪 (g)`（原为裸 `fat`），无裸字段名残留。
2. **墙的自证**：`manifest.json` 的 diff 只有 `readings.字节区间` 一行；`d68616c` 共 85 件；
   本席另抽读墙页 `看饮食总览.html` 复核其来源脚注与复制区按钮确实在页上（`📊 数据来源 · 饮食记录 · 本周 …／本月 …（都统计到昨日）`
   ＋ `复制日志` 按钮），与 `t275 ①/⑥` 的失败点对得上——那两条红点在**内容形状**上，不在「页缺块」上。
3. **越界检查**：七笔提交的 `git show --name-only` 并集与本席的 `.scratch/t603/**`、本件、运行记录导出件
   与任何人其它路径**零交集**；本席复跑期间 `git status` 里出现的他人未提交件（`base-combos`／`skill-bill`／`t268`／`t520` 等）
   一件未碰。
4. **缺依赖**：全程无缺件、无安装动作。

## 八、日志与产物清单（`.scratch/t603/`）

`tsc.log`（编译）、`ticket8.log`（族测试 269）、`analysis.log`（场景10 回归 82）、`gate83.log`／`gate83.json`／
`gate83-summary.json`（门禁 83 逐件）、`boundary.log`（hunk 落点静态）、`roundtrip.log`（场景10 前后比对）、
`iso-new/`／`iso-old/`／`iso-new2/`（三趟 6 页产物）、`probe.log`／`probe-summary.json`（新探针）、
`mut-base.json`／`mut-red.json`／`mut-green.json`／`mut-red.log`（变异三趟）、`warningline.log`／
`warningline-head.log`（台账门两趟）、`agents-head.md`（已提交台账的临时副本）、`run/`（快照与产物区）。
