# t572 返修证据（报告族 页24-31，R-36~R-58）

## §0 写集声明（开工前）
- run-all已读：报告族=页24-31=order331-338=`calorie.report.bmi/tdee/bmr/protein/water/score/trend/compare`，生成件仅 `.scratch/t387/html/24-*.html~31-*.html`（+out/logs/results.json），不提交产物。
- 当刻脏件（git status，2026-09-16开工时，一律不碰）：
  `src/diet/libraryDocs.ts, nutritionPortDocs.ts, precheck.ts, precheckLabel.ts, receipt.ts, reviewDocs.ts, sourceStatsDocs.ts, todayDocs.ts`；`src/exercise/receipt.ts, sportUi.ts`；`src/render/sportDocs.ts`；`test/t273-review-meal.test.mjs, t274-食品库页.test.mjs, t275-营养饮水总览页.test.mjs, t277-预检确认页.test.mjs`；未跟踪 `docs/skills/skill-bill/t403-*`、`test/exercise-text-shape-t578.test.mjs`。
- 本票声明写集（只加这些，撞件停手上报；公共层 `packages/base-render/**` 不动；字段口径/唤醒词/路由不动）：
  1. `packages/skill-calorie/src/analysis/reportDocBmi.ts`
  2. `packages/skill-calorie/src/analysis/reportDocTracked.ts`
  3. `packages/skill-calorie/src/analysis/reportDocParts.ts`
  4. `packages/skill-calorie/src/analysis/reportDocScore.ts`
  5. `packages/skill-calorie/src/analysis/reportDocScoreSeries.ts`（未改，R-57销账无需动）
  6. `packages/skill-calorie/src/analysis/reportDoc.ts`
  7. ~~`packages/skill-calorie/src/analysis/reportPlate.ts`~~（未动：阈值唯一判据件，R-38只改标题文案不碰阈值）
  8. `packages/skill-calorie/test/t572-报告族.test.mjs`（新建，仅本票断言）
  9. `docs/skills/skill-calorie/t572-返修-证据.md`（本件）
- 追加（R-39/R-51 既有断言须随R改，都不在脏件内）：
  10. `packages/skill-calorie/test/analysis-shape-report-520.test.mjs`（仅水分页徽章期望 达成→达标一行）
  11. `packages/skill-calorie/test/analysis-report-384.test.mjs`（仅水分字段期望＋对比表方向列断言随R-51）
- 与R冲突以R为准，冲突点单列于各节末尾“冲突”行。用户HTML统一要求执行情况：未新增触摸目标/字号/间距值（纯文案＋结构复用公共层）；HELP fixtures 定向检索：触摸 `touch-action:manipulation`、按钮 `min-height:36px`、间距4/8/12/16、字号10/11/12/13成档——本票无新增样式，仅R-50 KPI换读数走既有网格；数值等宽沿公共层 `tnum`；分号/竖线拼串已清零（audit 31/31）；双档实拍28页（390/1440）＋31/24页（390）＋读图自查通过。
- GATE-RUN 统一格式：`GATE-RUN runId=<标识> cmd=<命令>`，只贴尾5行，产物一次只读一页。

## 基线（开工前重测）
- GATE-RUN runId=24751988-6bb1-4368-8c71-e9ff2b7b62c0 cmd=`node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t387/html` → RESULT 31/31 PASS。
- GATE-RUN runId=7c0d2b51-6126-4943-b3b0-ff23db640fef cmd=`node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t387/html` → OVERFLOW-ZERO pages=31 cells=93 failed=0。
- GATE-RUN runId=ed5f6e3c-645d-427f-8b7b-f889fece2f82 cmd=`node packages/skill-calorie/scripts/gen-cli.mjs --check` → GEN-CHECK PASS（键128）。
- GATE-RUN runId=53395c7b-b0fb-4b02-9c60-4dede5c7e2f6 cmd=`node packages/skill-calorie/scripts/check-warning-line.mjs` → RESULT 90/91 RED（`src/exercise/receipt.ts` 台账439实况468，他席在途陈化；本票件全线内）。收尾时他席已同步，见机器门汇总。
- R-57对账：当刻30页 `ilife-charts-tick`=7、`showValues` 无、柱状图已换折线（`reportDocScoreSeries.ts` #519 W5已改，V4未复述=已修）。**销账**（原文轮次#519 W5 D6）。
- R-58对账：当刻24页口径行 `<p class="ilife-block-caliber"><span>…</span> <span>…</span></p>` 双span（竖线走版式），与25-31同纪律。**销账**（原文轮次#519 W5 D1拆两行）。本票R-43另修表与页脚重复，见该节。

## R-36（页26 老侧口径黑话）
- 改动：`reportDocTracked.ts` 危险表caption去“老侧口径：”、口径表判据行去“（老侧口径）”，改用户直述。
- 验收：全族定向检索「老侧」==0（实测0）。
- GATE-RUN runId=06af741c-16f3-4c97-a4d4-48626c137315 cmd=`node --test packages/skill-calorie/test/t572-报告族.test.mjs` → R-36 PASS。变异：改回即红 runId=70e2bfe9（R-36 fail）→还原绿。

## R-37（页24 from→to形参名上屏）
- 改动：`reportDocBmi.ts` 里程碑caption改「分类里程碑（起 → 止）」。
- 验收：跨档板含中文括注、无`分类里程碑（from`（实测通过）。
- GATE-RUN：同上 runId=06af741c → R-37 PASS。变异 runId=9b70f132 改回即红→还原绿。

## R-38（页24 页面文案口径括注）
- 改动：分级表caption改「BMI 分级（中国标准）」（阈值18.5/24.9/28即中国标准，未动 `reportPlate.ts` 判据）。
- 验收：该页无「页面文案口径」，含中国标准（实测通过）。

## R-39（页28 达标/达成混用）
- 改动：`reportDocTracked.ts` 水分支 `rate/hitWord` 改达标率/达标（与蛋白一致），注释同步。
- 验收：页28「达成」==0、「达标」38（实测）。既有断言随改：384水分字段、520水徽章。

## R-40（页30 无数据胶囊矛盾）
- 改动：`reportDocScore.ts` 趋势方向徽标：平稳挂 `empty＋显式文案「平稳」`（不再落默认「无数据」），上升/下降同理给显式文案。
- 验收：页30无 `status-badge-empty">无数据`，有 `>平稳<`（实测）。胶囊∈结论词汇（平稳）。

## R-41（页31 390档日期折断）
- 改动：随R-50下沉——KPI内不再有长日期串（本期/对比期日期卡已撤，区间只住结论条与页头左格）。
- 验收：对比KPI值无 `\d{4}-` 长日期（测试断言）；390档实拍31页KPI无折断。冲突：无（R-50吸收，结论条日期换行属正常文本流）。

## R-42（五页通用胶囊词）
- 改动：TDEE缺口徽标「在缺口/无缺口」、BMR「低于基础代谢 N 天」、评分最弱/最强「标签 N%」、趋势方向显式词、对比「方向 Δ」——全部显式 `statusText`，默认成功/警告/失败/无数据清零（8页全批0，实测）。
- 冲突：R-42字面“五页均含百分数”——TDEE/BMR/趋势/对比当刻无百分数口径，不造数，改用领域词＋本页真数（天数/方向/读数），通用词清零部分全达；百分数页（评分/蛋白水说明）已含%。

## R-43（页26 口径表与页脚重复）
- 改动：`reportDoc.ts` BMR页脚改「基础代谢按 Mifflin-St Jeor 算式｜四项齐备才算，危险信号见上方判据表」（判据细则只住口径表）。
- 验收：页脚不含表句「窗口内摄入低于基础代谢的天数达到 3 天即告警」（实测False=通过），且保留竖线分段（520-④守卫通过）。

## R-44（页26 KPI3/KPI4同量）
- 改动：BMR KPI4「有摄入记录」→「达到基础代谢」（有记录−低于天数， Chad 30→0）。
- 验收：页26 KPI标签4异、读数1700/2635/30/0四异（实测）。

## R-45（页27 KPI3/KPI4同一事实）
- 改动：蛋白/水分KPI合并——达标天数卡说明带达标率，空出卡给「有记录天数」。
- 验收：页27 KPI标签4异、读数68/150/0/30四异（实测）。

## R-46（页27/28跨页用词）
- 改动：同R-39（两页统一达标；`reportDoc.ts` 结论注释同步）。
- 验收：两页同名指标（达标天数/达标率/是否达标）一致（蛋白水测试双绿）。

## R-47（页28 饮水ml空格）
- 改动：`reportDocTracked.ts` 值头「饮水ml」→「饮水（ml）」（蛋白g同步括号化，见R-56）。
- 验收：页28 `饮水ml`==0（实测）。

## R-48（页28 目标标签与末点相接）
- 改动：无代码改（公共层 `marktext` 已是 `text-anchor=end` 右端收齐；本种子末值与目标远离）。标题「虚线为目标」承担含义。
- 验收：双档实拍读图——390档目标标签与末点相邻不压字，1440档间距明显（vision自查通过）。冲突：无（页面侧一半已齐，图表几何缺口归#567）。

## R-49（页29 仪表与KPI重复+写法）
- 改动：`reportDocScore.ts` 撤评分KPI「综合评分」卡（读数由仪表承载，值文本「N 分」），留最近一天/最弱/最强三卡。
- 验收：页29 KPI无「综合评分」卡（实测0）；仪表值无%、含分（测试通过）；读数出现=仪表＋结论2处写法一致。

## R-50（页31 KPI窗口元信息）
- 改动：对比KPI本期/对比期日期卡→「本期日均摄入」「本期日均体重」真读数（1位小数），区间下沉结论条/页头；留记录天数1个元信息。
- 验收：KPI标签=[本期日均摄入，本期日均体重，记录天数，前3项变化量]，纯元信息仅1（实测）。

## R-51（页31 方向列与Δ重复）
- 改动：逐项Δ表与前3项表撤「方向」列（Δ符号编码方向；徽章/ chips 聚合计数保留）。
- 验收：全族 `>方向<` 表头0（实测）；既有384断言随改（4格＋符号验方向）。

## R-52（页31 日均精度）
- 改动：对比表新增 `fmt1/signed1`（1位小数），本期/对比期/Δ全走它。
- 验收：页31本期格无裸整数（实测[]）；合成板含2635.0。变异 runId=fc2deb53 去toFixed即红→还原绿。

## R-53（页24 分级表缺值—）
- 改动：`reportDocBmi.ts` 非当前档 `''`→`'—'`。
- 验收：页24空td 0、有—（实测）。

## R-54（页24 y轴收敛）
- 改动：`reportDocParts.ts lineOf` 加小跨度收敛（占比<60%时按数据跨度×1.667重定，1位小数）。
- 验收：页24 ticks 24.2/25.0/25.8，占比(25.5−24.5)/1.6=62.5%≥60%（实测）。整数大跨度页不受影响（520-⑩通过）。

## R-55（页24-31 本机日期空格）
- 改动：`reportDocParts.ts sourceFootnoteOf` 改「本机 ＋日期」。
- 验收：全族 `本机\d` 0、`本机 \d` 8（8页各1，实测）。

## R-56（页24-31 单位写法统一）
- 改动：表头「体重kg→体重（kg）」「蛋白g→蛋白（g）」「饮水ml→饮水（ml）」「当日摄入卡→当日摄入（卡）」；值面已有空格（`fmt(,' 卡'/' cm'/' 岁')`＋KPI独立unit槽）。
- 验收：全族四旧串各0（实测）。

## R-57（页30 柱标签压字；对账）
- 对账：当刻tick=7、逐点value标签0、柱状图已换折线——#519 W5 D6已修，**销账**。
- 改动：无（`reportDocScoreSeries.ts` 未动）。
- GATE-RUN runId=06af741c → R-57 PASS。

## R-58（页24 页脚分隔符；对账）
- 对账：当刻24页口径行双span（版式竖线），与25-31同纪律——#519 W5 D1已修，**销账**。
- 改动：无（R-43另修重复问题）。
- GATE-RUN runId=06af741c → R-58 PASS。

## 机器门汇总
- GATE-RUN runId=9377b1da cmd=audit-separators → RESULT 31/31 PASS。
- GATE-RUN runId=8c7e3e97 cmd=measure-responsive → pages=31 cells=93 failed=0。
- GATE-RUN runId=c0660719 cmd=gen-cli --check → GEN-CHECK PASS（键128）。
- GATE-RUN runId=666c9253 cmd=check-warning-line → RESULT 91/91 PASS（开工时90/91系他席陈化，其在途同步后回绿；本票件全线内，最大247行）。
- GATE-RUN runId=984e320f cmd=run-all → 31/31 exit0（PARAM-AUDIT 31/31）。
- 靶向：384 runId=f7ba1101 5/5；520-shape runId=00a99942 10/10；t572 runId=06af741c 21/21（重跑以该runId为准；另两次编译runId=addf1891/0708fe2d exit0）；t573-cross全绿；t401/t401c runId=9c10f7aa 7/7；separator-520 T5前提过期（开工前产物已31/31绿，其断言0/31为旧基线，反为修绿证据；非本票件不动）。

## 变异自证
- 文案类R-36：改回“老侧口径”→ runId=70e2bfe9 R-36必红 →逐文件还原→绿。
- 标题类R-37：改回“from → to”→ runId=9b70f132 R-37必红 →还原→绿。
- 数值类R-52：fmt1去toFixed→ runId=fc2deb53 R-52必红 →还原→绿。

## 产物sha256（重出后，12位）
- 01 35c95f71c812／02 02c09893bcb9／03 b20a113cc2a3／04 d83df84af85d／05 0eafde1f1985／06 1dc7e995f20d／07 7f7da87d2a80／08 d21f7db6046e／09 3052cdd028f7／10 dc9e03ebf0af／11 dbaa5414e55a／12 f66013252eee／13 2ecb317ae5b7／14 aa380707106a／15 e7ab13d96843／16 7c3e0d110fc2／17 10c1d63e61e9／18 8f3986c8e96f／19 ca655a68554d／20 036b8fbfdd0f／21 a7f562797fcc／22 a7f562797fcc／23 35c95f71c812／24 9084a549d7b2／25 233f7aaff908／26 e55bf08d4aa2／27 e6a2badeb0b9／28 2e7d7c4c106b／29 fbb49071946b／30 093dc772beae／31 c4fbdb310782（字节见run-all日志：24页93295／25页77829／26页87864／27页84509／28页84537／29页79884／30页87411／31页97748）
