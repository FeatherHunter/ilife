#495 旧针收窄：#337 整条按 #483 口径重写

## 一、口径判定：收窄（纯测试改动，不碰实现）

`packages/skill-calorie/test/profile-doc-179.test.mjs` 的 `#337` 旧针断言批量删除回执含「批量计数」，
但该表已被 `473dd15 feat(483): 场景03 体重写后回执 9 词文本审查` 有意删掉。不走恢复口径。

依据（提交信息＋件内注释互锁）：

- 提交 `473dd15` 信息行「批量页删整块批量计数表（写入／跳过／失败三数与两句定义在副标题与结论句各一份）」，
  且 `git merge-base --is-ancestor 473dd15 HEAD` 退出码 0（当前 HEAD 的祖先）。
- `packages/skill-calorie/src/weight/logReceipt.ts:11-12` 件头注释「批量计数表整块删（摘要与结论句里已有同样三数）」；
  同件 `:267` 注释「#483 删掉上面那张批量计数表」。
- 双审查 R1／R2 一致确认收窄口径成立；总工裁定以最新评论取代原验收“只动批量段”口径，
  要求重写 `#337` 整条（首格新口径以 #483／#505 产物为准，批量段删批量计数针并保留三数断言）。

## 二、当刻产物探针（先读产物再断言，不抄旧数）

持锁跑探针脚本（与 179 件 `#337` 块同款参数：单条 `kg 70.2／2026-09-06／07:00:00`；
批量 `70.8／70.5／xx` 三条；改删同记录号），临时库，真库零写入。日志 `.scratch/t154/w3-495/probe.log`。

读数（`PROBE exit=0`，真串逐字确认）：

- 单条：`体重 · 写后回执=false`（眉标整族删），`本次体重／70.2／2026-09-06／写入后的现值／较上次差值／距目标差／备注=true`，
  判语 `本次记在 2026-09-06 07:00:00。=true`。
- 批量：`批量计数=false`，`逐条明细（共 3 条）／判语句／两条定义=true`，
  事实条 `写入 2 条／跳过 0 条／失败 1 条=true`，摘要 `批量记体重：写入 2，跳过 0，失败 1`。
- 改：`改前 → 改后／70.2／本次改动 1 条记录。=true`。
- 删：`删除快照=false`，`删除前的原值／硬删除，不可恢复／删后最新体重=true`，
  判语 `本次删掉的记录已经不在库里了，硬删除，不可恢复，要还原请照下表原值重新记一次。=true`。

（首轮探针 `runId=f04ed5ca` 因脚本内路径少一层 `exit=1`，修路径后重跑作废，不计门禁。）

## 三、改动（只动 #337 整条一处）

文件 `packages/skill-calorie/test/profile-doc-179.test.mjs`（块注释＋四格断言，`27+/4-`）：

- 块注释追加 #483／#505／#542 口径变更说明（含本件依据）。
- 单条格：删旧针 `体重 · 写后回执`，改钉 `本次体重／70.2／2026-09-06／写入后的现值／较上次差值／距目标差／备注／判语／对账信息`，
  另加两条 0 命中（已删眉标、副标题元素）。
- 批量格：删旧针 `批量计数`（改 0 命中），改钉表题 `逐条明细（共 3 条）`＋判语句＋两条定义＋事实条三枚＋副标题元素 0 命中＋摘要 `/写入 2，跳过 0，失败 1/`。
- 改格：保留 `改前 → 改后／70.2／70`，加钉判语 `本次改动 1 条记录。`。
- 删格：`删除快照` 改 0 命中，改钉 `删除前的原值／硬删除，不可恢复／删后最新体重／判语全句`。

禁区遵守：未动 #337 其它断言、未动 `test/t365-*`／`test/t364-*`、未动唤醒词名与命令名、
真库零写入（全部临时库）、`src/weight/logReceipt.ts` 一行未碰。

## 四、自证（持锁，红／绿两行读数）

- 改前红：旧针首红在第一格 `weight.log 缺：体重 · 写后回执`，`tests 1／pass 0／fail 1`，日志 `before-337.log`。
- 改后绿：`tests 1／pass 1／fail 0`，日志 `after-337.log`。
- 全量：`tests 10／pass 7／fail 3`（基线 10／6／4，红转绿仅 #337 一条；剩下 #239／#247／#365 三条红与改前一致，
  失败串分别为 `预检确认页按钮不对`／`菜单缺用途提示`／`记体脂缺场景 08 眉标`，属 #239／#247／#365 归属票范围，本票不碰），日志 `after-full.log`。
- 负向对照（变异）：把批量格新针临时改回旧针（断言产物含 `批量计数`）→ `tests 1／pass 0／fail 1` 必红
  （`NEG-CTRL weight.batch 缺：批量计数`，日志 `neg-red.log`）；还原新针 → `tests 1／pass 1／fail 0` 必绿（日志 `restored-337.log`）。
  实现侧冻结（禁区不许碰 `src/weight/logReceipt.ts`），故变异打在测试针一行：改坏一行必红、还原必绿，门禁隔离性成立。

## 五、GATE-RUN 声明（逐条对账源 `.scratch/locks/gate-runs.log`）

GATE-RUN runId=8211e875-74a7-4207-a3d5-3dbd44e3ca78 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=04492afb-b7b1-433f-bbe7-ff145d138cc3 cmd="node --test --test-name-pattern=#337 packages/skill-calorie/test/profile-doc-179.test.mjs"
GATE-RUN runId=60a28f23-6f48-429a-b227-fd29c563d60 cmd="node .scratch/t154/w3-495/probe.mjs"
GATE-RUN runId=8a4f815e-e9c7-423f-b745-93b785f8539d cmd="node --test --test-name-pattern=#337 packages/skill-calorie/test/profile-doc-179.test.mjs"
GATE-RUN runId=873ec93a-4d63-499b-a7df-e8b84118914d cmd="node --test packages/skill-calorie/test/profile-doc-179.test.mjs"
GATE-RUN runId=59696f91-cc45-43be-8897-8ca916120456 cmd="node --test --test-name-pattern=#337 packages/skill-calorie/test/profile-doc-179.test.mjs"
GATE-RUN runId=2984e3d3-7131-46d5-b473-299853da75d3 cmd="node --test --test-name-pattern=#337 packages/skill-calorie/test/profile-doc-179.test.mjs"
