# t581 证据 · 场景02饮食 B3回执预检食品库族文案形状化

## 一、改动清单（只写票面列出的文件）

| 件 | 改动 |
|---|---|
| `src/diet/receipt.ts` | 页题去 `·`（`scene＋回执`）；`<title>` 去 `·`；改类说明拆两句；写入字段顿号串改 `renderChips` 徽章列（卡片 `detail` 改说明句）；批量失败说明拆两句；更新空 `emptyText` 去顿号透出；改前缺值口径拆两条 `renderCaliberLine`；新增类 `本次结果` 改印参数真值（缺值 `—`）；蛋白/碳水/脂肪累计 `g` 改 `克`；今日累计两档口径各拆两条；副题 `；` 改句号、区间 `~` 改 `至`、间隔号改逗号（`subtitleOf` 上屏归一，数据面不动）；来源脚注与 `actionAt` 秒级一律不碰 |
| `src/diet/precheck.ts` | 三处 `metaLeft` 去 `· 饮食`；副题只留结论（计数由读数卡说）；空态引导拆两句；确认后食品名一行一名（逐行，不再顿号串）；校验/预览口径各拆两条（`／` 改 `还是` 列举）；调用点题名改无间隔号（共用件 `precheckParts.ts` 不碰）；来源脚注不碰 |
| `src/diet/precheckLabel.ts` | 口径拆两条；`metaLeft` 去 `· 饮食`；调用点题名改无间隔号；来源脚注不碰 |
| `src/diet/precheckPort.ts` | **零改动**：`fat` 标签已是 `脂肪 (g)`，t277 逐字钉死八串，中文名不动、单位不动 |
| `src/diet/libraryDocs.ts` | 两处 `<title>` 去 `·`；四处 `metaLeft` 去 `· 饮食`；食品/去重口径各拆两条（`finishPage/body` 改收 `readonly string[]`）；空态引导拆两句；来源行拆两行键值；两处占位提示只留一例；分类详情 `·` 改句号；三宏量 `g` 改 `克`；来源脚注不碰 |
| `src/diet/sourceStatsDocs.ts` | `<title>` 去 `·`；`metaLeft` 去 `· 饮食`；表题只留题名＋下架/未知各一条口径行；结论 `（N条 · P%）` 改逗号；来源脚注不碰 |
| `src/diet/nutritionPortDocs.ts` | 只动总览口径行：拆两条 `renderCaliberLine`（统计口径＋今日承接，保留今日日期事实）；配比/深度/饮水主体与四处来源脚注一律不碰 |
| `test/t277/t274/t275` | 断言随文案同改（眉标/标题/口径/占位/来源行/宏量单位/总览口径）；t270 零改动 |

## 二、验收读数

- 编译 `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` → exit 0（终编 `af53ac75`；中途两次被他席在途件挡住 `trendPredictDocs.ts` 语法错、`exercise/receipt.ts` 缺名，均未碰那些文件，等对方修好重编回绿）。
- 四件测试 → `tests 81 / pass 79 / fail 2`，两红均为票面点名不管的 t275 看饮食总览①⑥（当刻 #271 条目列表页，归 #271 接线＋#399/#560）。
- `t279-真出口用例` → `tests 99 / pass 99 / fail 0`。
- 门禁 `audit-separators.mjs --dir .scratch/t581/products --json .scratch/t581/gate.json` → `RESULT: 15/33`；18 个非绿页的剩余命中 23 处＝来源脚注 18 处（票面除外，归 #560）＋票面点名不管 5 处（§三逐页点名），R4/R6/R7 归零。
- 变异自证（`.scratch/t581/mut.mjs`）：`MUT-RED exit=1 眉标断言红=1` → 还原 → `MUT-GREEN exit=0 pass=17`，`MUT-VERIFY ok`。

## 三、门禁剩余命中逐页点名（`.scratch/t581/gate.json`）

- 来源脚注 18 处（不碰，归 #560）：批量导入食品/校验批量导入/看批量导入预览（`· 本次N条`）×3、拍营养表×2 ×2、搜食品/查食品/查食品库/查食品按分类/去重×2/来源统计×2（`· 在架食品共N条`）×8、查营养配比/深度/明细/喝水/饮水 ×5。
- 票面点名不管 5 处：查营养配比 `共 7 天 · 均衡`（配比主体 R1 已完）；看营养素明细 `膳食纤维、钠、糖（…）`＋`这张表…；完成度…`；看营养素深度 `膳食纤维、钠、糖（…）`＋`看的是…；…`（深度/覆盖主体不管，`饮水量ml` 不触发门禁且 t275 钉死列序）。

## 四、跨族断言红（未改那些文件，归 D 轮）

- 本票引入：t496 ③ #3/#3b/#3c（回执页题去 `·`，旧题 `X · 回执` 被钉住）。
- 改前已红/他席在途（改后复跑仍同状，非本票引入）：t496 ③ #7/#8（`goalDetail` 的 `，` 与其 `·` 正则对不上，本票未动该行）；t511 ① 60饮食复盘（`工作日平均`，复盘页非本票）；t511 ④（其变异钩的 `h1Of` 取到眉标行，明细页非本票）；diet-list-t271 餐别支页名；t272 空窗两态营养结构三段（`rankingDocs.ts` 他席在途改动）；514 ③（读 `.scratch/t375` 体重产物，他席重出）。
- t496 ②（状态卡四串）全绿；t496 ③ #4/#6 全绿。

## 五、遗留出口

- 告警线台账：本票三件改后 LF＝台账当场实测（`receipt.ts` 398／`libraryDocs.ts` 407／`nutritionPortDocs.ts` 745），复跑 `check-warning-line.mjs` → `RESULT: 91/91`＋`PASS`（`f0383af1`，exit 0），无需 `--sync`。三件本就超线（改前 384/404/744），台账拆法不变（本次先不拆）。
- `receipt.ts:347-348` 来源脚注与秒级 `actionAt` 不碰（归 #560）；秒取到分与脚注不碰冲突，待编排者裁定（本次副题秒级串随 `，` 归一保留到分秒原样）。
- vision 终审在 D 轮之后，本票不打分。

## 六、GATE-RUN（`.scratch/locks/gate-runs.log` 原文抄录）

- GATE-RUN runId=6bd6498d-84c2-41ac-a19b-f26a5170d368 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force" exit=0（改后首编绿）
- GATE-RUN runId=967ad00b-6def-47c8-83ce-fbc94b6a3f81 cmd="node --test packages/skill-calorie/test/t279-真出口用例.test.mjs" exit=0（ℹ tests 99／pass 99／fail 0）
- GATE-RUN runId=115971c7-e64a-42b8-b7ce-e8558abbd87d cmd="node .scratch/t581/run33.mjs" exit=0（RESULT: 33/33）
- GATE-RUN runId=bf96fd9c-1fb9-4898-a44b-d427e3cb1ad0 cmd="node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t581/products --json .scratch/t581/gate.json" exit=1（RESULT: 15/33，剩余见§三）
- GATE-RUN runId=ca4b6785-ca04-47ae-9f12-1e94ab3e461b cmd="node .scratch/t581/mut.mjs" exit=0（MUT-RED exit=1 眉标断言红=1／MUT-GREEN exit=0 pass=17）
- GATE-RUN runId=af53ac75-57a7-456d-8acc-22b82008363e cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force" exit=0（验收终编）
- GATE-RUN runId=54fe201e-c71b-46dc-8193-ac1bef952567 cmd="node --test packages/skill-calorie/test/t270-回执四块.test.mjs packages/skill-calorie/test/t277-预检确认页.test.mjs packages/skill-calorie/test/t274-食品库页.test.mjs packages/skill-calorie/test/t275-营养饮水总览页.test.mjs" exit=1（ℹ tests 81／pass 79／fail 2，两红见§二）
- GATE-RUN runId=f0383af1-1591-4a8f-9f84-2ffb828d36ae cmd="node packages/skill-calorie/scripts/check-warning-line.mjs" exit=0（RESULT: 91/91＋PASS，本票三行在册一致）
