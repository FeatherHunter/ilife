# #273 交接（复盘／餐别分布页）——**下一手读盘不读对话**

停止点标记行：`#273 收口完成（探针 27/27 ＋ 变异红/绿两行 ＋ 台账门 79/79 ＋ 10 条词 11/11），等编排者指派` ——
**接任者从这一行之后接**：本票交付件已全部提交（`68332d5` 主体；`d7a53f3`／`ee86b1e`／`950f5c9` 文档面三笔）；

## 一、我交出去的（**#271 直接用**）

餐别 5 条（看早餐／看午餐／看晚餐／看加餐／看全部餐别分布）的页住在 `calorie.view.diet`（#271 是那条命令的唯一作者），
本票只交**区块**与**取数**，集成在它那边。**签名逐字如下**（实现住我声明路径内的两件，深路径 import）：

```ts
// packages/skill-calorie/src/diet/reviewDocs.ts（装配件）
export type MealParam = '早餐' | '午餐' | '晚餐' | '加餐' | 'all';
export function mealParamOf(raw: unknown): MealParam;
export function buildMealDistributionBlock(v: MealDistributionView, command?: string): string;
export interface MealDistributionView {
  readonly start: string; readonly end: string; readonly days: number;
  readonly meal: MealParam; readonly mealLabel: string;      // '全部餐别' / '早餐' …
  readonly items: readonly MealDistributionItem[];           // {date,time,meal,food,grams,cal,protein}
  readonly total: number; readonly totalCal: number; readonly avg: number;
  readonly oneLine: string;                                  // 结论句（页头 summary 槽用）
  readonly dist: readonly MealDistributionSlice[];           // {label,count,cal,pct}，仅 'all' 非空
}

// packages/skill-calorie/src/diet/review.ts（处理体／取数）
export function buildMealDistributionView(
  db: DatabaseSync, mealRaw: unknown, start: string, end: string,
): MealDistributionView;
```

集成四步（照 #271 自己的页框）：

1. **只在餐别参数在场时才调**：`const mv = buildMealDistributionView(db, params['meal'], start, end);`
   —— 不给 `meal` 就是条目列表那一支（看今日饮食／看本周饮食…），**别默认 `all`**；
   `meal` 缺失或未知值一律 `CalorieRenderError('bad-input')` ⇒ **exit 2**（不编数、不给默认餐别）。
   认的值：`早餐／午餐／晚餐／加餐／all`（老 CLI 的 `breakfast／lunch／dinner／snack` 与「全部餐别」也认）。
2. `const block = buildMealDistributionBlock(mv, commandLine('calorie.view.diet', params));`
   —— **页面自己已经有复制区就不要传第二个参数**（本区块给了命令原文才出复制区，避免两处复制区）。
3. 宿主页 `assembleDocPage` 的 **`charts` 传 `true`**（「全部餐别」那一支出环图）。
4. 页头（裁定 1，不出命令键）：眉标「看餐别分布 · 饮食」、徽章「复盘 · 餐别」、
   标题「🍽️ 餐别分布 `start ~ end`」、结论句用 `mv.oneLine`。

区块出什么：读数卡两张（餐数／日均热量）、四桶占比环图＋占比条（仅 `all`，零值不画图元）、
明细七列（日期／时间／餐次／食物／克数／热量／蛋白）、来源脚注「📊 数据来源 · 饮食记录 · 餐别时间窗推断 · start → end」；
锚点 `md-kpi`／`md-dist`／`md-table`／`md-copy`（页内导航请指这几个 id）。
**窗口内这一支零记录不抛**：出空态句＋「用「记一餐」把那一顿记上」的引导句（裁定 4）。

## 二、复盘那一侧（我自己集成完的，别人不必动）

`calorie.view.diet-review` 的 8 条词（复盘 6 条 ＋「看营养结构」「看今日营养」）走的都是
`packages/skill-calorie/src/diet/review.ts` 的 `viewDietReview`；营养配比那一段是 **#275 交的
`buildNutritionRatioBlock`**，在本件 `review.ts` 里集成（取数仍走 #275 的 `buildNutritionRatioView`）。

## 三、下一手缺什么（**当刻症状 ＋ 根因行**，2026-09-15）

**症状（探针第 ⑤ 组「当刻症状」实测）**：5 条词的路由记录只带 `{"window":"7d"}`，命令面没有餐别这一位
⇒ 今天跑 `看早餐（最近 7 天）` 出的是**条目列表页**，产物里 `餐别热量占比`／`md-dist` 一个都没有，
即「餐别 5 条到不了餐别页」。

**根因行**（三处，都不在本票声明路径内）：

1. `packages/skill-calorie/src/home/routes.ts:28-32` —— 5 条词的路由记录缺 `"meal"` 位
   （今天一律是 `calorie-cmd-read calorie.view.diet --params '{"window":"7d"}'`）。
   **该文件在本波不在任何人的声明路径内**（本波 #276 的声明路径是 `src/diet/routes.ts` 等）⇒ 要编排者转票或授权。
2. `packages/skill-calorie/src/cli/keys.ts`／`src/cli/cmd_read.ts` —— 餐别这一位的参数透传与登记（#276 的账）。
3. `packages/skill-calorie/src/render/dietDocs.ts` 的 `buildViewDietDoc` —— 拿到 `meal` 之后调本票的两个函数（#271 的账）。

需要的取值：`{"meal":"早餐"}`（或 `午餐／晚餐／加餐／all`；英文键 `breakfast／lunch／dinner／snack` 也认）。

## 四、遗留（不在本票写集，已上报）

- 复盘页上 `buildNutritionRatioBlock` 自带一节「复制数据」（无日志按钮），与本页自己的复制区**并存**
  ⇒ 一张页上两个复制区。要合并得给 #275 的区块加一个「不出复制区」的开关（`nutritionPortDocs.ts` 不在本票声明路径）。
- `docs/skills/skill-calorie/t273-证据.md` 是逐条机器读数与门禁对账的正本。
