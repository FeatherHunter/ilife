# t276 对抗预审报告（静态预审，绿树终验前）

判定：**PASS（静态预审）** —— 均分 87（契约一致 28／证据真实 19／新旧对照 18／工程红线 13／文档同步 9），无 S1。
审的是 `e6c050e`（`feat(276): 命令侧改动暂存…未终验待树绿重跑`）所含 8 件；实施席已挂起，本席未兼实施，未跑任何需持锁动作。

## 机器证据

- 断言文件清单：`git show e6c050e --stat` 恰 8 件（`diet/commands.ts`、`diet/routes.ts`、`diet/today.ts`、`cli/legacy/routes/scene-02.ts`、`diet/productImport.ts`（新建）、`t276-证据.md`、`t276-营养表映射.md`、`.changeset/t276-diet-commands.md`），无 `keys.ts`／`combos`／`routes.generated` 等生成物。
- 自设免构建新探针 `.scratch/t276-review/probe.ps1`（只读 `git show e6c050e:<path>`，不编译不测试不跑 gen）：**20/20 PASS，exit 0**，日志 `.scratch/t276-review/probe.log`。覆盖：声明数组恰一个、无重键（25 键）、新命令七字段齐（`kind/key/shape/title/wakeWord/example/run`）且 `example` 具可跑形、6 个搬迁 order（13,14,33,40,41,78）在 `routes.ts` 各有且仅有一条 exec 且键无孤儿、legacy 分片数组已空零残留、红线文件零触碰、父→子 wake 序列保序（新增恰为 13,14,33,40,41,78）、`today.ts` 备注筛选为查询参数（`hasNote` 主名＋`withNote` 兼容＋非布尔即错）。
- 红线 diff 为空：`git diff e6c050e^ e6c050e -- routing.ts write.ts cmd_read.ts shared/ keys.ts routes.generated.ts` 无输出；分派层零新增 `case`；棘轮两文件未动（计划内未做，见下）。
- 票面：`gh issue view 276` 为 OPEN（未 close 正确）；正文无进度行（无可核项，非缺陷）；三文档无 BOM、无字面 `\n`。
- t166 对账：6 条在 `t166-scene02-reconcile.md`（§四：拍营养表×2、无备注×2、批量导入、校验批量导入）逐条有源，处置与 `t180`（纠正 1／优化 5）一致。

## 路径

- 改动件（e6c050e 内 8 件，见上）。
- 本席证据件：`docs/skills/skill-calorie/t276-review-20260914.md`（本件）、`.scratch/t276-review/probe.ps1`、`.scratch/t276-review/probe.log`。
- 引用但未入仓（忽略清单，可复核性风险 S3-2）：`.scratch/t276/write-dispatch-handover.md`（内容已读：分派零新增结论＋routing.ts:245 不碰 #250 口径，结论与本席独立核验一致）、`gate-runs.log`、`build2.log`、`run-six.mjs`。

## 缺陷清单与下一手

- S3-1（本票引入）：`t276-证据.md` 运行记录行“键 114＝写 46＋读 68”违反用词纪律（应为“114 条命令＝46 条会改数据库的命令＋68 条查询命令”）。改法：一行文字替换，二阶段顺手修。
- S3-2（本票范围·可复核性风险）：GATE-RUN 三行 runId 与“第二阶段 diet 零报错”当前只有忽略区日志可依，第三方不可复核。二阶段须按并发协议 §2.3.4 把运行条目导出到受跟踪路径后再终验；证据“第一阶段 build 仅不含 order:40 的中间态”表述诚实，未夸大，不算造假。
- S3-3（本票范围·观察）：`writeProductImport` 未直调 `importProducts`（书面理由成立：同步 `items` vs 异步 `file`＋人工确认；已复用 `validateRecord`／`checkDuplicate`／`productStore` 同一写路，`DuplicatePolicy` 同名同值）。建议二阶段补一次两者去重口径对账，非阻塞。
- 计划内未做（不是缺陷）：棘轮两文件未下调；生成物（`keys.ts`／`registry.ts`／`combos.yaml`／`routes.generated.ts`／`SKILL.md`／HELP）未重生成——均待树绿后二阶段持锁做。
- 范围外：本次无范围外 S3（工作区另有他人在途脏件 `combos.yaml`／`present.ts`／`build-help.mjs`／`keys.ts`／`routes.generated.ts`，本席未碰、不采信、不评；下一手先确认归属再开窗）。
- 下一手缺（二阶段，待树绿持锁）：`pnpm build`（含 order:40）exit 0 → `pnpm gen`＋`gen:check` → 6 条实跑（隔离库）→ 靶向测试＋`t293` 探针（P1 `UNACCOUNTED` 为空）＋`test/calorie-routing-81.test.mjs` → 变异自证 → 对账导出件入仓 → 终验。接对 4 处中归他能力的（`calorie.view.diet`／`calorie.view.home` 归 `src/home/`、餐别 `meal` 参数归页面票）须转票，不在本票关门条件内。
