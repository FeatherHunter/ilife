# t518 W1 · 搬家证据（`trendDocs.ts` → 姊妹件 `trendPredictDocs.ts`）

> **判据（本窗只有一条）**：**搬迁前后 31 页产物逐字节相同**（票面 W1 段逐字）。
> 本窗**不改任何可见文本、不改任何形状**：搬的是代码，产物必须一个字节都不动。
> 全部命令经加锁包装器 `node tooling/run-locked.mjs --ticket 518 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000 -- …` 跑；
> 本窗的 START／RUN 条目导出件＝[`t518-GATE-RUN.log`](t518-GATE-RUN.log)（协议 §2.3 第 4 条）。

## 一 · 产物基线与重出器（W1 的判据口径）

- **重出器**：`.scratch/t518/gen31.mjs`（本票独占；写法与种子库逐字照抄地图 #161 的原始重跑器 `.scratch/t387/run-all.mjs`）。
  种子库＝**60 天**（体重 78.0→75.1 kg／三餐／隔日运动／水分）＋档案（30／男／175／moderate）＋目标（1800 卡／目标体重 68.0）。
  **必须带 `weight_log`**：#463 的判据要的是「有 75.1 kg 记录时 TDEE 走真实体重」，#517 样板页那份种子库（`.scratch/t517/db`）没有体重记录，故不沿用。
- 产物落 `.scratch/t518/产物/`（**本票独占草稿目录**；`.scratch/t387/html/` 是 #161 收口墙、不在本票写集，一行未写）。
- 断言（协议 §2.5「失败即不落盘」）：31 条 exit 0 ＋完整文档（doctype／charset／`</html>`／零槽位残留）＋**别名逐字节同**（01/23 同命令同参数、21/22 同一产物的两个唤醒词）；任一红即不落盘、非 0 退出。
- 基线读数（W0）：`RESULT: 31/31 页落盘 bytes_total=2339046（别名 01/23、21/22 逐字节同）`。

```
GATE-RUN runId=c9dc4d80-1c15-448e-9370-d6659bcbbce7 cmd="node .scratch/t518/gen31.mjs"          exit=0
```

## 二 · 搬家做了什么（逐件）

`packages/skill-calorie/src/render/trendDocs.ts` **1135 → 528 LF**；新件 `packages/skill-calorie/src/render/trendPredictDocs.ts` **649 LF**。

| 搬走的（＝新件内容） | 留下的（`trendDocs.ts` 现存） |
|---|---|
| **缺口族**：`DEFICIT_TREND_ZH`／`DEFICIT_SECTIONS`／`deficitSection`／`deficitVerdict`／`signed`／`deficitConclusion`／`deficitBurnMix`／`deficitStatusChips` ＋ `buildDeficitDoc`（＝21／22 两页） | `buildCombinedDoc`（组合配对 11 键） |
| **预测族 9 页**：`buildPredictDoc`（01–05／23）／`buildPredictTargetDoc`（06）／`buildSimCutDoc`（07–09）／`buildSimTargetDoc`（10–13）／`buildCalorieForecastDoc`（14–17）／`buildCalorieGoalDoc`（18）／`buildCalorieDeficitDoc`（19）／`buildCalorieStabilityDoc`（20）／`buildGoalPredictDoc`（本图 31 页之外，同族薄页，按票面点名的 10 个一并搬） | `buildAnomalyDoc`（异常诊断）／`contraCopyBlock`／`CONTRA_MOBILE_CSS`／`buildContraDoc`（禁忌扫描） |

- **切分按当刻内容做**（协议 §2.5 第 2 条：不按行号、不按预备记录）：`node .scratch/t518/w1-move.mjs` 以三段段头注释为唯一锚点（`热量缺口（calorie_deficit.html 对照`／`异常诊断（anomaly_report.html 对照`／`体重预测（predict_report 对照`），锚点未命中或不唯一即抛错、不落盘。
- **两族共件留本件并改为导出**：`DOC_VERSION`／`DOC_SKILL`／`DOC_TITLE`／`fmt`／`humanText`／`techNoteHtml`（`export` 关键字是本次唯一对既有代码的改动）；新件 `import { … } from './trendDocs.js'`，方向单向、无环。
- **10 个出口经本件薄转出**：`trendDocs.ts` 末尾 `export { … } from './trendPredictDocs.js'` ⇒ **既有调用面一字不动**——`render/index.ts` 与各测试的 `dist/render/trendDocs.js`（`analysis-deficit-385.test.mjs:62`／`analysis-shape-520.test.mjs:15`）照旧导入得到同一个函数。
- **搬迁命令**：`node .scratch/t518/w1-move.mjs` → `RESULT: 搬出 trendPredictDocs.ts LF=649；trendDocs.ts LF=537`（537 是加导入整理前的数；整理掉 4 条不再使用的渲染件导入与 7 条不再使用的类型导入后为 528，与台账门读数一致）。

## 三 · 判据读数（逐条）

### 3.1 主判据：31 页产物逐字节相同

| 轮次 | 命令 | 读数 |
|---|---|---|
| 搬迁前（W0 基线） | `node .scratch/t518/gen31.mjs` | `31/31 页落盘 bytes_total=2339046` |
| 搬迁后（重编＋重出） | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` → `node .scratch/t518/gen31.mjs --out .scratch/t518/产物-W1 --baseline .scratch/t518/W1-基线.json` | `W1-MOVE byte-identical pages=31/31 DIFF=none`；`totalW0=2339046 totalW1=2339046` |

比对口径＝逐页 `sha256` ＋ `bytes` 双比（脚本对 31 条逐条比，非抽样）；`DIFF=none` 即 31 页全部逐字节相同。

```
GATE-RUN runId=b4b419d8-6584-48e4-9d8a-d56aaba77db6 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"                                        exit=0
GATE-RUN runId=2031ed8a-30ac-4864-8610-a2c99e0bc2cc cmd="node .scratch/t518/gen31.mjs --out .scratch/t518/产物-W1 --baseline .scratch/t518/W1-基线.json"       exit=0
```

### 3.2 靶向测试（四件全绿）

`node --test packages/skill-calorie/test/analysis-predict-383.test.mjs analysis-accept-386.test.mjs analysis-deficit-385.test.mjs analysis-shape-520.test.mjs`
→ **`tests 24 / pass 24 / fail 0`**、exit 0。其中 `analysis-deficit-385.test.mjs:384` 逐字读 `../src/render/trendDocs.ts` 源码查颜色字面量，搬迁后仍绿（该件只剩组合／异常／禁忌三支，颜色字面量同样是 0）。

```
GATE-RUN runId=b2b1a0d1-6367-4036-a7be-7789f7832f39 cmd="node --test packages/skill-calorie/test/analysis-predict-383.test.mjs packages/skill-calorie/test/analysis-accept-386.test.mjs packages/skill-calorie/test/analysis-deficit-385.test.mjs packages/skill-calorie/test/analysis-shape-520.test.mjs"  exit=0
```

### 3.3 变异自证两行（源码级；打的就是本窗的判据）

打 W1 判据本身的识别力：把新件 `trendPredictDocs.ts` 里 `buildPredictDoc` 的一张 KPI 卡标签改坏（`'当前'` → `'当前值'`，命中 01–05／23 共 6 页），重编 → 重出 → 与 W0 基线逐字节比；再**逐文件还原原文**（逐字写回新件，不是整目录还原）→ 重编 → 重出 → 再比。

```
MUT-RED  byte-identical pages=25/31 DIFF=01(b9f68035b8cd->24d20ce55ef4),02(33c051cc59d5->6318cb58349b),03(8b44ed48f105->0eaf077b9c4b),04(3e33012ac85b->9ffa3edbceaf),05(8365cb7ad1ed->3617db1b9c68),23(b9f68035b8cd->24d20ce55ef4)
RESTORE 相等=true sha256_12=96ebb134983e
GREEN    byte-identical pages=31/31 DIFF=none
```

- 变异轮与还原轮共用一次持锁窗口（外层包装器持锁，窗口内 tsc／重出器一律**直调不抢锁**，协议 §2 分锁域）；还原判据＝新件字节 sha256 与改前逐字相同。
- **一次失败运行已如实记账**：`w1-mutate.mjs` 首跑因 `--baseline` 传了绝对路径（重出器内部再 `join(ROOT, …)`）报 `ENOENT`，`finally` 已把新件逐字写回、**未留半成品**；改脚本后重跑得上面两行。

```
GATE-RUN runId=db704ff9-acc0-418a-919f-9903b39d2e7a cmd="node .scratch/t518/w1-mutate.mjs"   exit=0   （变异自证那一轮）
GATE-RUN runId=e1ee9a47-fc6a-419f-89ad-52105dea1364 cmd="node .scratch/t518/w1-mutate.mjs"   exit=1   （首跑失败：argv 路径拼接；已还原、已修脚本、不充作门禁证据）
```

### 3.4 回归底线（改前已绿，只许保持）

`node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t518/产物 --widths 390,512,820,1440 --json .scratch/t518/W0-measure.json`
→ **`OVERFLOW-ZERO pages=31 cells=124 failed=0 scopeOutFailed=0`**（31 页 × 4 档 124 格全 0，与 §二 回归底线同口径）。本窗只搬代码，产物逐字节相同 ⇒ 该读数自动继承。

`node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t518/产物 --json .scratch/t518/W0-sep.json`
→ **`RESULT: 2/31`、exit 1**：**这是「改前读数」，不是本窗的门禁绿条**——31 页里只有 21／22（#517 样板页）已形状化，其余 29 页的 `·`／`；`／`~` 债由 **W2／W3** 销账（票面判据「节点级读数全零」是 W4 收尾的目标值）。

```
GATE-RUN runId=1f2535f8-843a-43e7-ba2f-3de035f7d527 cmd="node -e \"…spawnSync measure-responsive(390,512,820,1440)+audit-separators 两门同一 runId…\""  exit=0（外层；内层 measure exit=0／sep exit=1）
```

## 四 · 包内两道门（W1 后即绿）

| 门 | 命令 | 摘要行 | exit |
|---|---|---|---|
| 告警线 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | `RESULT: 91/91` ＋ `PASS: 告警线台账齐全且与实况一致` | 0 |
| 生成链 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `GEN-CHECK PASS：键 128（写 46 ＋ 读 82）；能力 10 个…未搬迁清单 0 条` | 0 |

```
GATE-RUN runId=0ae93b1d-e1c2-48cd-8cbf-462aef857b19 cmd="node packages/skill-calorie/scripts/check-warning-line.mjs"   exit=0
GATE-RUN runId=a1db0e49-1d1a-4a16-981d-985c6e7026c3 cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"       exit=0
```

**台账同步（`--sync` 先 `--dry`）与本窗必须具名的一件事实**：搬家改变了扫描面（新件超线、本件落回线内），台账必须同步，否则告警线门必红。同步器读数：

```
SYNC-PLAN mode=write 行=44 改=2 增=1 删=0
SYNC-CHANGE src/render/trendDocs.ts 台账=1136 实况=528
SYNC-CHANGE src/exercise/receipt.ts  台账=427  实况=431
SYNC-ADD    src/render/trendPredictDocs.ts LF=649
SYNC-VERIFY ok（回读逐字节等于计划；台账 44 行）
```

- ⚠️ **`src/exercise/receipt.ts` 那一行不是本票的账，是开工前就陈化的**：`git show HEAD:packages/skill-calorie/src/exercise/receipt.ts` 当场数 **430 LF**，而 HEAD 版台账写 **427** ⇒ **本票开窗前该门就是红的**（由更早改动该件的提交留下，未同步台账）。同理 `trendDocs.ts` 在 HEAD 版是 **1135 LF** 而台账写 1136（差 1，文件末尾无换行 ⇒ 「行数 1136／换行数 1135」两种数法之差）。
- 处置：本条门要求 `RESULT: n/n + PASS`，而台账是**一份内容派生的表**（同步器只改「当场实测」列与增删行，块外逐字节不变）。故本票按票面「台账同步」那一条跑了 `--sync`，把两行一并改成实况；**`receipt.ts` 行的数字是被机械带绿的，归属仍在改动该件的票**，本票在提交信息与回执里具名，不据为己有。

```
GATE-RUN runId=91a4e4c9-871b-4968-be2a-da59bfba3519 cmd="node packages/skill-calorie/scripts/check-warning-line.mjs --sync --dry"  exit=1（--dry 只演练不落盘；计划非空即非 0，读 SYNC-PLAN 那一行）
GATE-RUN runId=73b9f08d-7d13-4354-8625-275abde594c1 cmd="node packages/skill-calorie/scripts/check-warning-line.mjs --sync"          exit=0
```

## 五 · 本窗具名未做项（不阻塞 W1，逐条给出下一手）

1. **`trendDocs.ts` 仍 528 LF ＞ 350**、新件 `trendPredictDocs.ts` 649 LF ＞ 350：两行都已在台账里具名（超因＋拆法）。第二半拆法＝`buildCombinedDoc` 一件、异常诊断＋禁忌扫描一件，件头小件提共件。**票面未点名这两支要搬**，故本窗不越界搬。
2. **一处覆盖收窄（S3 记账）**：`analysis-deficit-385.test.mjs:384` 的「技能侧零颜色字面量」扫描面逐字写死了 `['../src/analysis/deficit.ts', '../src/render/trendDocs.ts']`——搬走后缺口族代码住 `trendPredictDocs.ts`，**该新件不在扫描面内**。本窗未改测试（该件不在本票写集），就地记账：要么由收口票把 `../src/render/trendPredictDocs.ts` 补进这一行的数组（一行改动），要么由后续结构票做「扫描面按目录派生」。当前两件颜色字面量都是 0，**不是既成红**。
3. **`.scratch/t387/html/` 收口墙未刷新**：本票判据自用 `.scratch/t518/产物/`（写集只给 `.scratch/t518/**`），#161 收口墙的 29 页仍是 #387 那一批。W4 会把 31 页重出到本票目录并在回执里点名，由编排者决定是否刷墙。

## 六 · 必报五步（本包 AGENTS.md 350 行告警线；编排者裁定 C 要求「两件各报一句」）

**`packages/skill-calorie/src/render/trendDocs.ts`（528 LF）：已超线，需要根据规则进行重构。**
超因：趋势／分析域同质文档装配（数据→区块→填充器）全挤在一件里；W1 已把缺口族与预测族 10 个 `build*Doc` 搬走，本件仍住 `buildCombinedDoc`（组合配对 11 键，约 219 行）＋`buildAnomalyDoc`（55 行）＋禁忌扫描那一支（`contraCopyBlock`／`CONTRA_MOBILE_CSS`／`buildContraDoc`，约 124 行）＋两族共件与件头（约 130 行）。
拆法：按页族再切两件姊妹件——`buildCombinedDoc` 一件、异常诊断＋禁忌扫描一件；三支共用的件头小件（`DOC_TITLE`／`DOC_VERSION`／`DOC_SKILL`／`fmt`／`humanText`／`techNoteHtml`／`techNoteHtml` 已有的两个表＋`SEV_ZH`／`CONTRA_STATUS_ZH`）提为一件共件，出口经本件薄转出。**本次先不拆**：票面 W1 的口径是「原样搬家、一字不改」，W1 窗口内再叠一次结构变更会把「31 页产物逐字节相同」这条判据变成两条变更的合成读数；第二半的接手点＝W4 收尾评估或收口票。

**`packages/skill-calorie/src/render/trendPredictDocs.ts`（649 LF）：已超线，需要根据规则进行重构。**
超因：W1 搬来的 10 个薄页同住一件——缺口族 1 页（含 `DEFICIT_*` 七个小件）＋预测族 9 页；每页的取字段→KPI→表（轨迹／明细）→口径行→复制区都摊在同一文件里，且 W2／W3 还要在同一件上给 20 页铺形状，只会更长。
拆法：按页族再切两件姊妹件（缺口族一件、预测族一件），两族共用的页框（页头胶囊／结论条／页内导航／口径行／来源脚注／复制区接线）提为一件共件，出口经本件薄转出。**本次先不拆**（理由同上：W1 是原样搬家）；接手点＝W4 收尾评估或收口票。

> 台账两行的「当场实测」数值**一律经 `--sync` 生成、不手改**（编排者裁定 A）；本件 §四 记的就是那次生成链的读数。

## 七 · 编排者 2026-09-16 三条裁定的落地（W1 补）

- **裁定 A（台账）＝接受**：AGENTS.md 告警线台账是**内容派生的生成物**，`--sync` 属既定生成链 ⇒ W1 那次三行的改动保留；本票的做法（具名「`receipt.ts` 归 #543 侧」）被认作正确。约束记此：**台账只许经 `--sync` 变更、不许手改**；每窗收尾重跑该门并把读数写进证据。
- **裁定 B（覆盖收窄）＝一次性具名授权**：允许**只改** `packages/skill-calorie/test/analysis-deficit-385.test.mjs` 的扫描列表，把搬迁后的件加进去，语义只许加。
  已落地：该行数组加 `'../src/render/trendPredictDocs.ts'`（原两项一字未动、阈值与滤法一字未动），行内补一段注释引裁定 B。
  变异自证（受控探针，打的就是这次补回覆盖）：

```
MUT-RED-COLOR exit=1  AssertionError [ERR_ASSERTION]: ../src/render/trendPredictDocs.ts 出现颜色字面量（技能侧必须为 0）：#abcdef
RESTORE 相等=true sha256_12=96ebb134983e
GREEN-COLOR   exit=0
```

  探针做法：往新件注入一处 `const W1B_COLOR_PROBE = '#abcdef';` → 只跑该测试文件里的「视图层五列与状态徽章」那一条 → 必红；逐文件写回原文 → 必绿（还原判据＝新件字节 sha256 与改前逐字相同）。产物零改动、无需重编（该断言读 `src` 文本）。

```
GATE-RUN runId=97610443-6743-4229-aa1a-5d88887667d5 cmd="node .scratch/t518/w1b-color-mutate.mjs"  exit=0   （裁定 B 变异自证那一轮）
GATE-RUN runId=9e3d775e-1d4c-46db-bce6-fe88017195e3 cmd="node .scratch/t518/w1b-color-mutate.mjs"  exit=1   （首跑：`--test-name-pattern` 选错测试名 → 0 条测试被选中、假绿；已改选名重跑，不充作门禁证据）
```

- **裁定 C（超线）**：见上「六 · 必报五步」两段，两件各一句「已超线，需要根据规则进行重构。」＋超因＋拆法。
