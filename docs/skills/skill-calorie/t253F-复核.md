# t253F · 独立复核（两处旧断言跟 #253 的新口径）

被审交付：`af0a7cbbc1d6a4a3123d86d9e27073d07bc30af7`
写集：`packages/skill-calorie/src/cli/write.ts`、`docs/skills/skill-calorie/t253-跟随件.md`
复核票号：253-R（本轮自己重跑、自设探针、自己发变异；实施者的一切结论不采信，逐句自己跑）

## 一、圈定范围（机器读数）

- `git show --stat af0a7cb` → 只有两件：`src/cli/write.ts`（1 增 1 删）、`docs/skills/skill-calorie/t253-跟随件.md`（新增 106 行）。**没有第三件。**
- `git show af0a7cb -- packages/skill-calorie/src/cli/write.ts` → 差异只有件头第 16 行那一句注释：
  - 改前：`其余 1 条（35 − 3 − 13 − 4 − 10 − 4）一字不改，仍是原回执片段。`
  - 改后：`其余 6 条（46 − 3 − 14 − 4 − 10 − 4 − 5：calorie.exercise.* 三条、calorie.photo.* 三条）各自由能力目录自己的实现出整页，不经这条链。`
  **代码一行未动**（差异里没有任何语句行）。
- `git diff HEAD -- packages/skill-calorie/src/cli/write.ts docs/skills/skill-calorie/t253-跟随件.md` → 空输出（工作区与这次提交一致）。
- 两处测试文件（`packages/skill-calorie/test/profile-doc-179.test.mjs`、`packages/skill-calorie/test/delivery-83.test.mjs`）都不在 `git status` 的改动名单里 ⇒ 实施者「测试件一行未动」属实。

## 二、独立重跑（一律经 `node tooling/run-locked.mjs --ticket 253-R -- …`）

编译入口按票面写死 `node node_modules/typescript/bin/tsc -b packages/skill-calorie`：

```
GATE-RUN runId=0d29d954-e1ca-4858-af07-d27ffada90c8
        cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
        摘要行 RESULT: ticket=253-R runId=0d29d954-… waitedMs=0 exit=0
        退出码 0；packages/skill-calorie/tsconfig.tsbuildinfo mtime=2026-09-15T21:38:00+08:00（已更新＝编译真的跑过）

GATE-RUN runId=6d80061d-fde7-4f09-b318-aaedb23fe8ad
        cmd=node --test packages/skill-calorie/test/profile-doc-179.test.mjs packages/skill-calorie/test/delivery-83.test.mjs
        摘要行 ℹ tests 23 ／ ℹ pass 17 ／ ℹ fail 6
        退出码 1（日志 .scratch/t253R/accept.log）

GATE-RUN runId=87dbf01f-79fa-4196-980a-45becc37f5a9
        cmd=（同验收命令，受控变异还原后再跑一遍）
        摘要行 ℹ tests 23 ／ ℹ pass 17 ／ ℹ fail 6
        退出码 1（日志 .scratch/t253R/accept-restored.log；6 条红点名与上一遍逐字相同）
```

6 条红逐条点名（两遍一致）。**本票的两条仍然是红的**：

| # | 红在哪条用例 | 报错逐字 | 归谁 |
| --- | --- | --- | --- |
| 1 | `#83 写键同样有 delivery（receipt 产物族）` | `AssertionError: 未切整页的命令仍是片段族`（`delivery-83.test.mjs:327`） | **本票 ②：口径变更所致，当刻无解（见四）** |
| 2 | `#179 其余会改数据库的命令仍是原回执片段（没被一刀切换页）` | `AssertionError: calorie.goal.set 的产物不再是原回执片段（本条钉住的命令尚未切整页）：<!doctype html>`（`profile-doc-179.test.mjs:286`） | **本票 ①：口径变更所致，当刻无解（见四）** |
| 3 | `#337 场景 03 四条体重写命令已是完整文档（三变体字段齐）` | `weight.log 缺：体重 · 写后回执`（`:341`） | 体重域，只记账 |
| 4 | `#365 身体细节四条写命令已是完整文档（七条写词…）` | `记体脂 缺场景 08 眉标`（`:416`） | 身体域，只记账 |
| 5 | `#239 四张页接上「复制日志」：命令原文 ＋ M5 行都在，照抄可重跑` | `预检确认页按钮不对` | 档案域，只记账 |
| 6 | `#247 场景 07 五张页的复制数据是「三格式三选一」菜单：…` | `预检确认页 菜单缺用途提示：粘贴给 AI / 自己看`（`:243`） | 档案域，只记账 |

第 3、4 条红在眉标文字上（产物已经是整页），第 5、6 条红在档案域预检确认页的按钮与菜单上——**都不是这次「只改一行注释」能造成的**（三遍读数逐字相同）。它们与 #253 复核报告里点名的既有红对得上。

**票面验收判据「两个文件里属于本票的那两条从红转绿」当刻不成立。**

## 三、复核者自设探针（两个面，全部自己跑）

探针 `.scratch/t253R/probe.mjs`（本轮自己写的，不抄实施者那一份）：

- **A 面（口径面）**：把命令登记表 `packages/skill-calorie/src/cli/keys.ts` 里 46 条会改数据库的命令逐条喂给六个整页装配口，看谁认。哨兵＝「一碰就抛」的假库对象：不是本族的必须返 `null`，本族的必然往下走去碰库。这一面直接查「40／6 的分法」与「有没有两条命令被两家同时认」。
- **B 面（产物面）**：真 CLI ＋ 临时库（`mkdtempSync` 目录 ＋ `SKILLS_DB_PATH` 指向它 ＋ `docs/research/t81-seed.mjs` 的 `seedFull()` ＋ `CALORIE_TODAY=SEED_TODAY`），六个装配口各取一条代表命令，外加票面点名的链外 6 条，共 13 条，逐条看产物起头是整页（`<!doctype html>`）还是片段（`<section class="ilife-page" …>`）。

基线读数（`GATE-RUN runId=37b060e8-59b8-443a-a82b-37d4c5377126 cmd=node .scratch/t253R/probe.mjs`，摘要 `PROBE-PASS`，退出码 0；日志 `.scratch/t253R/probe-baseline.log`）：

```
A written_keys=46 claimed=40 unclaimed=6
A unclaimed=["calorie.exercise.add","calorie.exercise.remove","calorie.exercise.update",
              "calorie.photo.add","calorie.photo.remove","calorie.photo.tag"]
A multi=[]
B 13 条 status=0 product=doc template=doc-shell
PROBE-PASS 46 条写命令清点：认键 40／链外 6，抽到的 13 条产物逐条整页
```

（探针后来补了一行逐家读数，取还原后那一遍 `GATE-RUN runId=730f5bc9-cb64-4c8b-afff-419a873ef68f`，退出码 0：
`A per_port={"body":4,"diet":14,"goal":5,"profile":3,"weight":4,"workout":10}`——合计 40，与改后注释里「3／14／4／10／4／5」逐家对上。）

（B 面 13 条＝链外 6 条 ＋ 六个装配口各一条代表：`profile.set`／`diet.add`／`weight.log`／`workout.plan-set`／`body.measure-add`／`goal.set`，另加票面 ① 里那条 `goal.water`。）

两件事当场清楚了：

1. **当刻 46 条会改数据库的命令，一条仍是片段的都没有**。链外那 6 条（`calorie.exercise.*` 三条、`calorie.photo.*` 三条）也各自出整页（`template=doc-shell`）。这句独立复现了实施者的核心事实，也说明票面 ①② 那条修法（把抽样换成当刻仍是片段的命令）**没有样本可用**。
2. 改后注释里的计数**对得上两处来源**：`src/cli/keys.ts` 的 `CALORIE_WRITE_COMBOS` 逐行数是 46 条（该件头行也写「写 46 ＋ 读 81 ＝ 127 条」），六个装配口认 40 条，分法 3／14／4／10／4／5 与探针 A 面逐家读数一致；链外 6 条恰好是 exercise 三条 ＋ photo 三条。

## 四、受控变异（红／还原一致，两行读数）

变异落在被审件自己的那一行 `src/cli/write.ts:92`，只换一个实参、随即逐字还原：
`workoutReceiptDoc(key, params, receipt, db)` → `workoutReceiptDoc('calorie.no-such-key', params, receipt, db)`，
效果＝把训练计划那一个装配口的转调打回旧片段（`shared/writeParts.ts` 的 `receiptHtml()` 正是 `delivery-83.test.mjs` 与 `profile-doc-179.test.mjs` 钉住的那种片段形状）。

- **红**：重编 `runId=8ba5c284-81c4-423a-b060-5152af0562a5`（exit 0）→ 探针 `runId=de53c4bb-693c-4ec8-8627-335a38630e95`，摘要行 `PROBE-RED 命中 1 条：["B calorie.workout.plan-set 产物不是整页：fragment"]`，退出码 1。
- **同一变异下拿票面验收命令再跑**：`runId=316cfcee-86d6-47bc-94d7-98c44c5d7424`，摘要行 `tests 23 ／ pass 17 ／ fail 6`，红点名与基线逐字相同，**没有新增一条红**。⇒ 票面这两件用例对「一整个装配口被退回片段」是盲的：两个文件里训练计划那 10 条命令零覆盖。
- **还原一致**：`git diff HEAD -- packages/skill-calorie/src/cli/write.ts` 输出 0 行；重编 `runId=84785fae-fcbf-40e6-b421-6b1098658729`（exit 0，tsbuildinfo mtime 21:43:45）→ 探针重跑 `runId=730f5bc9-cb64-4c8b-afff-419a873ef68f` 摘要 `PROBE-PASS`，退出码 0；验收命令重跑 `runId=87dbf01f-…` 仍是 23／17／6、红点名逐字不变。

两行读数：**变异红（runId=de53c4bb，退出码 1，`calorie.workout.plan-set` 产物变片段）；还原后绿（runId=730f5bc9，退出码 0，13 条逐条整页），验收读数前后逐字一致。**

## 五、逐条缺陷

| # | 归类 | 内容 | 等级 |
| --- | --- | --- | --- |
| 1 | 本票范围（票面前提有错，不是实施者过错） | 票面 ①② 要求「把抽样换成当刻仍是片段的命令」，当刻**无此命令**（第三节 A／B 两面为证）。这条修法出自 #253 复核报告 `docs/skills/skill-calorie/t253-复核.md:120` 那句「链外还有 6 条」，那句把当刻已经出整页的链外 6 条当成了候选作证；本票继承了那个错前提。 | 须编排者裁定（不是 S1） |
| 2 | 本票引入 | **未发现**。改动只有一行注释，零行为影响：改前／变异后／还原后三遍验收读数逐字相同（23／17／6，红点名一致）。 | — |
| 3 | 本票范围（做法欠一步） | 票面「反例必须走：把新换上的取样键改回 `calorie.goal.set`（或把断言放宽成恒真）→ 必须重新变红」**没有真跑**，实施者以「反例恒成立无需改文件」记账。该说法能自圆（基线那两条红行正是该反例的读数），但没给出受控变异读数；而第四节那一发变异正好说明验收用例的判据面有多窄。 | S3 |
| 4 | 范围外发现 | 票面这两个验收文件里，训练计划那 10 条写命令零覆盖：一整口退回片段，两件用例一条都不动（第四节读数为证）。 | S3（建议另开票补断言） |
| 5 | 范围外发现 | 同两个文件里另 4 条红（体重新域／身体域眉标、档案域预检确认页两处）各有别的票在场，三遍读数逐字相同，本票只记账、一行未碰。 | S3 |

## 六、判定与打分

**判定 FAIL（本票未完成）；未发现 S1。**

- 依据：票面验收判据「两个文件里属于本票的那两条从红转绿」当刻**不成立**（两条仍红，报错见第二节）；票面 ③（改准件头那句注释）**做到且做对**（第二、三节机器读数）。
- 无 S1 的理由：**证据真实**——实施者报的 5 个 runId 在 `.scratch/locks/gate-runs.log` 里逐条存在（`7ac21096`／`a04b90f1`／`a994e0cd`／`44436ba0`／`1e64f8b9`，另 `97bbe99f`（git add）／`77554e48`（git commit）也在册），其每一条读数我自己都重跑并得到同一结果；**没有放宽断言**（两处测试件一行未动，工作区干净，`assert` 形状与强度照旧）；**没有用户可见回归**（只改注释）；**数据零风险**（未碰真库，实跑一律临时库＋种子）。

打分 **75／100**：

| 项 | 分 | 依据 |
| --- | --- | --- |
| 契约一致 | 14／30 | ③ 精确达成；①② 未达（票面前提有错），验收判据不成立 |
| 证据真实可复现 | 25／25 | 声称的 runId 全部在册；23／17／6、两条红的报错、46／40／6 与 3／14／4／10／4／5 逐项独立复现 |
| 新旧对照 | 12／20 | 改前／改后读数齐且公开；票面点名的「改回必红」反例未以受控变异真跑 |
| 工程红线 | 15／15 | 只动票面写集两件；编译入口写死 `node node_modules/typescript/bin/tsc -b`；git 只 add／commit 自己那两件；无 push／stash／reset／checkout |
| 文档同步 | 9／10 | 改后那句注释与命令登记表、与探针读数三源一致（46／40／6 ＋ 3／14／4／10／4／5）；证据件如实写明未做项与三条出路 |

## 七、未做项与下一手（给编排者）

1. 两处抽样（`profile-doc-179.test.mjs:276-290`、`delivery-83.test.mjs:324-329`）当刻**无法**按票面修法变绿。三条出路：
   - **(a) 退役**「没被一刀切」那条：它的历史使命是盯住「不许整批换页」，当刻 46 条全整页，这个命题已经没有样本。
   - **(b) 翻成整页断言**：改成「抽样命令已经是整页」，并**补上训练计划那 10 条**（它们当刻零覆盖，正是第四节变异打得到的地方）。改的是断言意图，须票面明写。
   - **(c)** 另开一票把某一族回执退回片段（不建议，与 #253 的口径相反）。
   **本复核建议 (b)**：一句话同时跟上 #253 的口径、又补上第四节那处零覆盖。补一条旁证：`delivery-83.test.mjs:284-301` 那条单元用例已经用合成输入钉住了「片段 → `receipt`／整页 → `doc-shell`」的判定次序（`:296`、`:301`），`:324-329` 那条真机片段断言与它重复，退役它不丢判据面。
2. `src/cli/write.ts` 件头第 3 行 `35 写键一律 receipt 形` 同样陈化（当刻 46 条）：票面只许改第 16 行，本票未动，下一手改它时以命令登记表为唯一来源。
3. 本复核件本该落在 `docs/skills/skill-calorie/t253-复核.md`，但那个名字已被 #253 那次复核的证据件占用（12.6 KB，21:27 落盘，不在本票写集内）；为不动别人的件，本件另起 `t253F-复核.md`。
