# t454 · 趋势／复盘族融合（地图 #156 页面族第 5 票 · 收尾票）

- 票面：#454（地图 #156 卡路里场景 04 运动）；形状照已验样板 #423（`src/exercise/receipt.ts` ＋ `t423-回执族融合.md`）；
  同件第 4 票 #453 先落地（分布／力量／有氧三支），本票**只动** `src/render/sportPortDocs.ts` 的 `buildTrendDoc`／`buildRecapDoc` 两支。
- 一句话判定：**「看运动趋势」＋「运动复盘」五个时间窗（共 6 条词）的产物已换成融合版式；判据件 9/9 绿；五窗同版有机器读数；
  变异两行自证有效（红→绿）；告警线台账已同步回绿。**
- 入库：主体提交见「§八」（提交 sha 由关票补记同步）。证据日期 2026-09-14。读数一律取自 `dist/` 真出口（每步先 `pnpm build`）。
- 窗口钉死：判据件给 CLI 传 `CALORIE_TODAY=2026-06-17`（周三）——本周＝06-15..06-17、本月＝06-01..06-17、90d＝03-20..06-17、今年＝01-01..06-17。
- 参数**不手抄**：判据件运行期读冻结表 `src/triggers/scene-04-exercise.ts` 的 `main_prompt.cli`，按 6 条唤醒词取原文再用（`<开始日期>／<结束日期>` 填真日期），
  并断言取到的 key 与 params 逐字（如 `{ window: '30d' }`／`{ window: 'custom', start: …, end: … }`）。

## 一、逐条交付路径

| 路径 | 状态 | 说明 |
|---|---|---|
| `packages/skill-calorie/src/render/sportPortDocs.ts` | 改（702 → 760 LF） | 趋势／复盘两支换融合版式；对外出口不变（`buildTrendDoc`／`buildRecapDoc`） |
| `packages/skill-calorie/test/exercise-trend-recap-fusion-454.test.mjs` | 新（372 LF） | 9 条判据：6 条真跑＋长窗截断＋空窗（真出口阻断＋装配层构造）＋五窗同版 |
| `packages/skill-calorie/AGENTS.md` | 改（台账「当场实测」列 702 → 760） | 由 `check-warning-line.mjs --sync` 生成（`SYNC-VERIFY ok`），未手抄 |
| `docs/skills/skill-calorie/t454-趋势复盘族融合.md` | 新 | 本证据件 |
| 命令与路由／`sportDocs.ts`／`records.ts`／`receipt.ts`／`packages/base-render/`／#453 三支／生成物／样张 | 未动 | 按票面「不许动」清单 |

## 二、判据先行：先红后绿（同一份判据件、两次编译）

| 步 | 命令（均走 `tooling/run-locked.mjs --ticket 454`） | runId | exit | 读数 |
|---|---|---|---|---|
| — | `pnpm build`（HEAD 源＝换版式之前） | `40250991-9b79-4e4f-8901-6b4bd0080b64` | 0 | 红读数之前的基线编译 |
| 红 | `node packages/skill-calorie/test/exercise-trend-recap-fusion-454.test.mjs` | `e4ad3858-b51f-4ce9-b90d-66ac240546a2` | **1** | **1 pass ／ 8 fail**：8 条的首选失败一律为「缺页内导航」（旧版两页没有页内导航）；唯一绿的是「五窗同版」——旧版五窗本来就同版，这条判据的判别力靠其余 6 条 |
| — | `pnpm build`（本票实现） | `2e373bc1-6810-4c77-b7d2-2168dc67bb4c` | 0 | 绿读数之前的编译 |
| 绿 | 同一条测试 | `dbde79d9-944a-448e-adc1-d0de5f588156` | **0** | **9 tests ／ pass 9 ／ fail 0** |

- 红/绿之间的「换源」手法：`swap.mjs`（草稿件）把源码在「本票实现」与「HEAD 原版」之间**逐字节**互换（Buffer 进／Buffer 出，不做文本转码），
  每次写盘后自证——换到 HEAD 后 `git status` 对本案为空（＝与 HEAD 逐字节相同），换回后源码 sha256 与存档同值。
- 更早还有一次「判据件未定稿」的跑（runId `dacd60af-6516-4b51-a654-708ff8c766d3`，exit=1、0 pass／9 fail）：那次判据件自身有一处 JS 错（单引号字符串里嵌了 `<json>`），
  红读数不作数，**以 `e4ad3858` 为准**（如实记录，免得读成「先绿后红」）。

## 三、6 条词真跑读数（产物落 `.scratch/t454/out/`，绿跑逐件重读，非抽样）

| 唤醒词（冻结参数） | 窗口（解出） | 产物 | 字节 | exit | 页内锚点 | 逐日表（页眉／可见行） | 折线 | 分布条／徽章 |
|---|---|---|---|---|---|---|---|---|
| 看运动趋势 `{window:"30d"}` | 05-19 ~ 06-17 | `01-trend.html` | 80920 | 0 | 6 | 共 30 天／列出 30 天，未截断；31 `<tr>`（1 表头＋30 行） | 2 条序列（实线 1＋虚线 1），主序列点＝4（＝活跃 4 天） | —（趋势页无分布／徽章） |
| 运动复盘（本周）`{window:"本周"}` | 06-15 ~ 06-17 | `02-recap-week.html` | 71366 | 0 | 7 | — | 1 条 | 2 行／2 枚 |
| 运动复盘（本月）`{window:"本月"}` | 06-01 ~ 06-17 | `03-recap-month.html` | 72174 | 0 | 7 | — | 1 条 | 3 行／4 枚 |
| 运动复盘（最近 90 天）`{window:"90d"}` | 03-20 ~ 06-17 | `04-recap-90d.html` | 71391 | 0 | 7 | — | 1 条 | 3 行／4 枚 |
| 运动复盘（今年）`{window:"今年"}` | 01-01 ~ 06-17 | `05-recap-year.html` | 71415 | 0 | 7 | — | 1 条 | 3 行／4 枚 |
| 运动复盘（自定义时间）`{window:"custom",start:"2026-06-01",end:"2026-06-30"}` | 06-01 ~ 06-30 | `06-recap-custom.html` | 72172 | 0 | 7 | — | 1 条 | 3 行／4 枚 |

- 6 条均 `exit=0`、产物存在、`assertDocPage` 过（doctype／charset／样式段／脚本段／版面／无残留标记）；来源脚注、口径行（`ilife-block-caliber` ≥2）、
  三格式 `data-fmt="text|json|csv"`（顺序固定）、`ilife-page-printable` 落版面根＋`@page printable` 逐件断言。
- 三类工程话全文命中 **0**：`calorie.view.`／票号（`t454`／`#454`）／「移植」；页头人话＝`<title>` `卡路里·运动趋势`／`卡路里·运动复盘`，眉标 `运动 · 趋势`／`运动 · 复盘`。
- **截断明示（第 7 件真跑，长窗）**：`07-trend-long.html`（2026-02-18 ~ 06-17，120 天）→ 页眉「共 120 天；本表列出 100 天——已截断，只列前 100 天」，
  `<tr>` 101（1 表头＋100 行）＝页眉条数与可见行数同口径；30 天窗那件则印「未截断」（两方向都真跑）。
- **断点（缺日不断 0）**：06-11 故意空着——逐日表那一行三格全「—」（0 次／0 卡都不是那天的实话），折线主序列点＝4（活跃天数）而不是 30（窗内天数）。
- **空窗两件**：`08-trend-empty-blocked`／`09-recap-empty-blocked` 真跑 `exit=4`（`ERR 4 取数失败（缺失阻断）：无运动记录：2026-05-01 ~ 2026-05-05`）、**不落产物**——故「空态带下一句话」只能**装配层构造**（与 #423 的「空明细」同一处境）：两页空态都印 `本窗还没有运动记录` ＋ `说「记运动」就能记下第一笔／第一条`，且折线／柱图／逐日表／分布条／徽章五张卡**整卡不出现**（不留空壳）。

## 四、五窗同版（五个复盘窗口一套版面，模板零分支）

- 判据把五份产物的**锚点 id 序列**与**页面级类名集合**逐一 deepEqual，并要求五份 h1 各自含本窗起始日（差异只在文本与数据）。
- 读数：`T454-SAME-VIEW anchors=sec-window,sec-conclusion,sec-figures,sec-category,sec-badges,sec-daily,sec-source classes=36`（五窗同值）；五份 h1 各不同：
  `运动复盘 2026-06-15 ~ 2026-06-17`／`…2026-06-01 ~ …`／`…2026-03-20 ~ …`／`…2026-01-01 ~ …`／`…2026-06-01 ~ 2026-06-30`。
- 复盘页四件逐处断言：一句话结论条（页内静态提示 `ilife-block-feedback-block-note`）／类型分布条（`ilife-block-dist-row`）／
  高频运动徽章（`ilife-block-chip`）／每日消耗趋势（`data-chart-kind="line"`）；趋势页四件：`sec-line`（折线）／`sec-weekly`（柱图 `data-chart-kind="bar"`）／
  `sec-daily`（逐日表）／页内导航（`<nav class="ilife-block-toc">`，href 全有落点）。

## 五、零回归与既有红（写集外，如实登记）

| 命令 | runId | exit | 读数 |
|---|---|---|---|
| `node --test packages/skill-calorie/test/doc-page-assert.mjs`（票面指定） | `12868d61-a953-45c9-9919-947b29cb7bf5` | 0 | 1/1 PASS |
| `node --test exercise-port-111 / exercise-routes-265 / exercise-wakewords-266 / db-readonly-93` | `c8a633fb-bcba-4edf-a7fe-5df26eab9e52` | 1 | 27 tests／pass 20／fail 7 |

- 7 条红里 **5 条与本票无关**（#453／#451 域内的既有红：`力量训练总览`／`有氧训练总览`／`运动类型分布` 三条标题针、`看运动记录（按力量筛选）` 错键、
  `看运动类型分布` 缺 `eyebrow key`）；**2 条是本票换版式碰上的旧判据**：
  - `#111 运动复盘：…` 的针 `高频 TOP5`——本票按票面把 TOP5 表换成「高频运动徽章」（新形状）；
  - `#111 复制头与冻结 envelope 版本对齐` 要求页内文本含 `【calorie · calorie.view.exercise-trend】`——**与本票「工程话命中 0」正面冲突**，按票面取后者（#453 三页同样已改成中文键）。
  两条都属「旧判据按新形状在归属票里改判」，本票写集不含那两个测试件（`#265` 路由也不许动），故**未改**，登记在此。

## 六、变异两行（先定向编译再跑判据；写回用定点反向编辑，写回后 sha256 与基线同值）

基线：源码 `sportPortDocs.ts` sha256 `2CEC26DA2028171591BD5DF62A67868EEC96F8C3799F5AD5BE1A90D5C4474DC9`（33053 字节）；
`dist/render/sportPortDocs.js` sha256 `D42BFD514A225E9F2C172DFEC60F7C24E4C70733EAED4BEE776CF23F633FF790`。

| 行 | 变异 | 变异后编译（runId → dist sha256） | 判据读数 | 写回后编译（runId → dist sha256） | 判据读数 |
|---|---|---|---|---|---|
| ① 缺日补 0 | `d.burned`／`d.minutes` → `d.burned ?? 0`／`d.minutes ?? 0`（折线点与逐日表两处） | `40a1e49d-3487-4d8f-be55-d53e541ae804` → `46E1F75344E8C8F55515D97342E0401DD721147370A64E4B5C18B3B53B416AE8` | **红**：`9677472f-1665-4b1c-8e3c-04f6d626e681` exit=1，8 pass／**1 fail**（断言＝「折线点数不等于活跃天数（缺日被当成 0 画点了）」） | `0e38779e-abd8-4114-87d4-0e56ae0de7b4` → `D42BFD51…`（＝基线同值） | **绿**：`8109f825-3cbc-4d04-b0c8-4f48efe89eaf` exit=0，9/9 |
| ② 复盘类别色写死字面量 | `...colorOf(b.category)` → `color: '#ff0000'`（只改复盘那一处，分布页那处一行不碰） | `692fdb9c-c256-4d71-83ce-054575e5d91a` → `8B9DF1A5D8DB3AB1E490E7396D5050436E8A22AF0B32B366BA41367A6FEDE2E3` | **红**：`0a3207d7-cad5-4853-a6ff-89ed8d9094d7` exit=1，5 pass／**4 fail**（断言＝「运动复盘（X）的『有氧』条色不是 categoryColor() 的 hex：#0071e3」×4 窗） | `3dd7b639-bbbd-458d-a723-f47b035018b8` → `D42BFD51…`（＝基线同值） | **绿**：`c3c511a9-eb04-4039-9135-7e6e3574961c` exit=0，9/9 |

- 两行的关键读数：**每次变异后 `dist` 的 sha256 都变了**，写回后**又变回基线值 `D42BFD51…`**——即「编译确实发生、dist 确实回到绿建」。
- 色值口径：两页产物的类别色只来自 `exercise/categoryColors.ts` 的 `categoryColor()`（复盘分布条断言逐类比 hex：有氧 `#0071e3`／力量 `#5856d6`／日常 `#ff9500`），
  件内不写第二份色表。

## 七、门禁 GATE-RUN 对账（各一次，均持锁；`run-locked` 的 `RESULT` 行为准）

| # | 命令 | GATE-RUN runId | exit | 读数 |
|---|---|---|---|---|
| 1 | `pnpm build` | `3dc6739a-14bc-454e-828f-0b6357bf400f` | 0 | 全仓编译＋`gen-cli --stamp`＋各包 client 构建全绿 |
| 2 | `node packages/skill-calorie/test/exercise-trend-recap-fusion-454.test.mjs` | `6c29fc05-9476-4332-ba6d-ce0457f5d479` | 0 | 9 tests ／ pass 9 ／ fail 0 |
| 3 | `node --test packages/skill-calorie/test/doc-page-assert.mjs` | `7ca23079-cdae-4609-8ae1-403915a4214b` | 0 | 1 test ／ pass 1 ／ fail 0 |
| 4 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | `8de359b8-1b48-497e-a243-3ed9714f04df` | 0 | `RESULT: 58/58` ／ `PASS: 告警线台账齐全且与实况一致` |

## 八、告警线（超线第四步：超因＋拆法）

- `sportPortDocs.ts` 在册（702），本票改完 **760 LF**。脚本先红：`RED 台账陈化：src/render/sportPortDocs.ts 台账=702 实况=760`
  （runId `d8054c7e-aa32-4f0a-b37b-ef96406159d3`，exit=1，`RESULT: 57/58`）→ `--sync --dry`（runId `d5df4dda-4196-4e0e-ad9c-ed191e706aa6`，改=1）
  → `--sync`（runId `cbdd7ee9-e095-4915-898f-051df9054533`，`SYNC-WRITE` ＋ `SYNC-VERIFY ok`，58/58）→ 复跑绿（上表第 4 行）。
- **已超线，需要根据规则进行重构。** 超因：本件把 #111 运动移植 6 键的**全文档装配**（数据→区块→填充器）与两族共用件同处一件；本票（#454）给趋势／复盘两支加融合装配（逐日表截断口径／五窗同版锚点／结论条／徽章／双刻度折线）净增 58 行，单件继续变长。**本次先不拆**：拆分不在本票写集（票面只许动趋势／复盘两支，切姊妹件要动出口与调用方）。
  拆法（接 #453 记的那条，落地即执行）：按页族切三件姊妹件——`sportStrengthDistDocs.ts`（分布／力量／有氧）／`sportTrendRecapDocs.ts`（趋势／复盘），
  把两族共用的卡外壳、页内导航、来源脚注、三格式复制区、类别色映射（`shell`／`windowCard`／`sourceCard`／`finishPage`／`colorOf`）提为一件共件 `sportPageParts.ts`，
  出口留本件薄转出；`buildReviewDoc`（计划复盘）与 `sportDocs.ts` 一起归结构票认领。台账「结论」列仍是 #453 的原文（本票按写集只动「当场实测」列），本条即 #454 的第四步报告。

## 九、写盘自证（行尾／改动面）

- `sportPortDocs.ts` 盘上 **CR 计数 = 0**（LF 口径 760）；`git diff --numstat` = `179 121`（非 `N N` 的整文件重写形）。
- 改动 hunk 只落在三段：`@@ -25,3 +25,4 @@`／`@@ -31,0 +33 @@`／`@@ -35,0 +38 @@`（件头注释＋import 两行）、`@@ -461…-521 +464…+540 @@`（`buildRecapDoc`）、
  `@@ -627…-702 +655…+750 @@`（`buildTrendDoc`）——**分布／力量／有氧三支（344–459）与 `buildReviewDoc`（558–625）零 hunk**，一行不碰。

## 十、未做项与下一手

1. 上述「既有红」两条旧判据的改判归各自票（`exercise-port-111.test.mjs`／`exercise-routes-265.test.mjs` 不在本票写集）；#265 那 7 条词的路由归属未动。
2. 空窗两页在真出口按缺失阻断（exit 4）不产产物——「空态带下一句话」目前只有装配层构造的读数（同 #423 的处置），若要真出口产物需另开票改取数层（不在本票写集）。
3. 本件拆姊妹件（§八）未做——结构票认领；视觉终验归 #268；`#267`（锁·真出口用例）可开工。
4. 本票**未**动 `sportDocs.ts`／`records.ts`／`receipt.ts`／`packages/base-render/`／生成物／样张／已关票证据件。

## 十一、样例产物（可双击的绝对路径，6 条词各一份＋长窗＋两份空窗阻断读数）

`D:\ilife\.scratch\t454\out\` 下：`01-trend.html`（看运动趋势）／`02-recap-week.html`（运动复盘·本周）／`03-recap-month.html`（本月）／
`04-recap-90d.html`（最近 90 天）／`05-recap-year.html`（今年）／`06-recap-custom.html`（自定义时间）／`07-trend-long.html`（120 天长窗·截断明示）／
`08-trend-empty-blocked.html`、`09-recap-empty-blocked.html`（空窗：真出口 exit 4，未落产物，路径仅留痕）。

建议肉眼抽查顺序：`01-trend.html`（折线两套刻度→柱图→逐日表页眉与行数→来源脚注→三格式）、`03-recap-month.html`（结论条→核心数字→类型分布条→高频徽章→每日消耗趋势）、
`07-trend-long.html`（截断明示那一句＋100 行表）。
