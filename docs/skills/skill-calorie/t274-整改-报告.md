# t274 · 食品库类页 —— 收口复核整改报告（检出 `D:\ilife-wt\wf155-g3`）

> 判据正本 `docs/skills/skill-calorie/t274-收口复核.md`（`15bd8d8`）判 FAIL（一条 S1）；同类前案 `t272-收口复核.md`；
> 口径正本 `t425-融合基准.md` 裁定 4 ＋ 其 2026-09-15 澄清（`f0decc8`）。老实物只读，未执行其中任何脚本。

## 一、判定

**S1 已消除，三条 S3 已改正，另同步一条本席引入的登记点。**判据读数 `RESULT-ALL: 红=0`。

- **基线 sha** ＝ `D:\ilife` HEAD **`cff2a4dd47c2ac4386cbb252c50bf413f9ec5470`**（`D:\ilife` 未触碰）。进场时该 HEAD 已由 `0d996bc` 前进到 `cff2a4d`（别席「修复(551)」），**工作树创建那一刻的 sha 即 `cff2a4d`**，以它为准。
- **S1**：库非空、本次查询零命中，原先 `exit 4`、不落盘、不出页 ⇒ 现出完整页 ＋ 空态句 ＋ 引导句、`exit 0`、落盘。判别落在**取数层** `src/diet/libraryPlate.ts`：先取库内在架总数，**只有为 0** 才抛 `missing-data`。**「整个数据面为空」的 `exit 4` ＋ `ERR 4: 取数失败（缺失阻断）` ＋ 不落盘逐字保持**，未放宽成别的码，未编造数据。
- 三条 S3 见 §三 第 6~8 条；范围外但必须同步的一处见 §三 第 9 条。

## 二、机器证据

| 读项 | 改前 | 改后 |
|---|---|---|
| 新探针 `t274-收口复核-新探针.mjs` | **`FAIL 红=5 检查项=36`** | **`PASS 红=0 检查项=36`** |
| 复跑 §② 三条 ZERO | 关键词零命中／分类零命中：`exit=4 落盘=no 完整文档=no 空态块=no 空态句=no 引导句=no` | 两条都 `exit=0 落盘=yes 完整文档=yes 块/句/引导句=yes`（69068／69129 B）；去重干净库 68705 B 不变 |
| 复跑 §① 8 词 | `8/8 exit 0`，字节 70354／70427／70033／76639／75821 | **同 8/8、同字节**（未因整改掉格） |
| 复跑 §①b 空库 8 词 | `8/8 exit 4 ＋ 缺失阻断 ＋ 不落盘` | **同 8/8** |
| 复跑 §③ 四张产物两两不同 | `4/4` | `4/4` |
| 复核席变异 | 甲 `必红=true`；**丙 `必红=false`（盲区）** | 甲 `必红=true`；**丙 `必红=true`（盲区补齐）**；两态 `源码回基线=true` |
| 作者件 `test/t274-食品库页.test.mjs` | `tests 11 / pass 11 / fail 0` | `tests 13 / pass 13 / fail 0` |
| 本席变异（把「本次零命中」改回阻断） | — | `作者件 fail=2`、`新探针 红=3`；点名还原后 `fail=0`、`红=0`、`源码回基线=true`；变异 `dist` 指纹 `a1ae3909…` ≠ 还原 `bfea0047…` |
| 别族两条词（基线源码 vs 本席源码，同种子同 `CALORIE_TODAY`） | — | 「看高热量榜」71111 B／「看饮食总览」88390 B：**字节数与去时间戳 sha256 改前＝改后**；两次 `dist` 指纹 `5ccb580a…` ≠ `bfea0047…`，证明确实换过 `dist` |
| 全包跑两遍（基线四件 vs 本席四件） | `tests 1185 / pass 1107 / fail 76`，红件 33 | `tests 1187 / pass 1109 / fail 76`，红件 33；**红件集合双向差集双空** |
| `tsc -b packages/skill-calorie --force` | `exit 0` | `exit 0` |
| `gen:check`／`check-warning-line.mjs` | PASS／81/82（红＝本席件台账陈化） | **PASS**（键 127）／**82/82 PASS** |

两处口径写清楚，免得读数被误读：

- **段③ 的时间戳**：页面按设计带跑那一刻的挂钟时间戳（复制日志第 5 段），两次跑之间隔着一次整包重编，故「看高热量榜」**原始** sha256 必差这一处（同字节数 71111 B，逐字符定位到的唯一差异是 `20:45:15` 与 `20:45:19`）。判据取去时间戳规范化后的 sha256 ＋ 字节数。
- **编译器落点**：本席一开始用 `spawnSync('npx', ['tsc', …])` 驱动编译，中途发现**静默空转** —— `D:\ilife\node_modules\.bin` 会被并发席位改动而消失，`npx` 找不到本地编译器就落到 npm 上的同名占位包（打印一行「This is not the tsc command you are looking for」后 **exit 0**），于是 `dist` 不换、「变异」假绿。已改成**绝对路径调用真编译器**，并把本检出根 `node_modules` 由整目录联结改为「真目录 ＋ 逐条联结 ＋ 本地 `.bin`」；段③④⑤ 各加 `dist` 指纹读数证明每轮真换过产物。`D:\ilife` 未写。

## 三、逐条路径

1. **取数层两态判别** —— `src/diet/libraryPlate.ts`（72 → 83 LF）。两个取数函数各先取 `sourceStats(db).total`，`=== 0` 才抛 `missing-data`（文案统一成「食品库空（先导入食品）」，原先零命中那支的「食品库无命中：<词>」「该分类空库：<分类>」两句取消）；否则返 `total 0` 的空盘。
2. **页脚条数** —— `src/diet/libraryDocs.ts`（396 → 401 LF）。`buildSearchDoc` 的来源脚注改取 `s.libraryTotal`：旧写法取 `s.total`，搜「鸡胸」命中 1 条时把库内 5 条印成「在架食品共 1 条」，**零命中那页更会印成「共 0 条」——那正是本 S1 要出的那张页**，等于在新页面上编一个库里没有的数。`ProductSearch` 加 `libraryTotal`（`data.metrics` 形状不动）；卡片零条分支加注说明它不是死代码。
3. **作者件拆两条 ＋ 补断言** —— `test/t274-食品库页.test.mjs`（400 → 474 LF，11 → 13 条）。原 384-399 那条（名叫「空库」、实际把两态锁成一条）拆成 `整体为空 ⇒ exit 4 ＋ 缺失阻断 ＋ 0 产物`（四条词，逐条核 `stderr` 点名库为空 ＋ `calorie_html/` 里 0 个文件）与 `库非空但本次零命中 ⇒ exit 0 ＋ 完整文档 ＋ 空态句 ＋ 引导句`（走 `assertDelivery` 全套判档①，另钉空态块、空态句、引导句逐字、结论句、页脚条数、日志第 4 段）。新增第 13 条钉**口径说明行文案**（食品四页逐字、去重页逐字、四页构件类名）。
4. **共用件的旧引脚** —— `test/render-t9.test.mjs`（232 → 239 LF）。判别点住在别族也引用的件里，故同步：原 `:213` 断言「库非空零命中 ⇒ 抛 `/无命中/`」改为断言返空盘；空库那条测试补两条「整个库为空仍 `missing-data`」。别族实跑读数见 §二。
5. **判据件进场**（环境事实）：基线 master 上没有复核席那三件探针与裁决件（只在 `15bd8d8`）。用 `git cat-file blob` 取出、`git hash-object` 核对 **blob 完全一致**（复跑 `a7542ff`／新探针 `18f934a`／变异 `7e33b1e`）后放进 `docs/skills/skill-calorie/`，未改一字。
6. **S3-1** `t274-报告.md` 逐块表 `food_search.html:99` 空态那一格：由「对上」重述为「对上（2026-09-15 整改后）」，写明原先登「对上」不成立的理由与整改后的实测。
7. **S3-2** 同件表头：`1 处有意没对上、2 处不适用` → `3 处有意没对上 ＋ 1 处不适用`，与表里实况（`:117`／`:78`／`:7` 三处没对上；`:136-149` 一处不适用）对齐。
8. **S3-3** `t274-证据.md` §一：裁定 1／2／2-补 三条关于来源统计页的过期结论改到 `ca52b5d` 之后的实况。**另多改一行**：同表裁定 4 那行原写「空库＝缺失阻断 exit 4，故三页的空态只能从『去重页干净库』这一态真跑出来」——这句正是本 S1 的成因，整改后已假，一并改写（同一桩过期结论，超出票面点名的三条，如实记此）。
9. **范围外但必须同步**：`packages/skill-calorie/AGENTS.md` 台账里 `src/diet/libraryDocs.ts` 的记数 396 → 401（不同步即台账陈化、`check-warning-line` 转红）。**只改本席那一行**。
10. **脚本与日志**：`docs/skills/skill-calorie/t274-整改-run.mjs`（段①两态／②复核席三件／③别族／④本席变异／⑤不新增红），日志与隔离库在 `.scratch/t274g3/`。段③④⑤会临时与基线对调受改件并 `tsc -b --force` 重编，**逐文件点名备份与还原**、还原后核源码与 `dist` 两处 sha256；编译走绝对路径真编译器；未用 `stash`／`checkout --`／`reset --hard`／`clean`／`restore .`，也未在任何检出跑包管理器。changeset＝`.changeset/t274-空窗整改.md`。

## 四、未做项与下一手缺什么

1. **未做视觉验收墙逐格审版式**（三样齐不在本席范围）。整改后新出的两张零命中页留负责人肉眼：390px／1440px 各看一次空态块与页脚那一行。
2. **未逐件归因全包那 33 个红件**：只做了两次全包跑的红件集合比对（本轮**双向差集双空**）。红名单里别席在途件的红，本席不认领也不改判。
3. **`src/diet/libraryDocs.ts` 401 LF 仍在 350 告警线上**（台账已跟到 401）。拆法照台账原记：切姊妹件 `diet/libraryCard.ts`，**新建文件要走编排者派单**，本席未自开新面。
4. **未动老实物**：`food_search.html` 与 `render_food_search.py` 只读；本次是让新页对齐老实物正本，未反向改老实物。
5. **裁决件不由整改席改**：`t274-收口复核.md` 原样带入、内容一字未改。
6. **需编排者知会（两件，都超出本席写集）**：
   - `packages/skill-calorie/AGENTS.md` 台账那一行：本席为消除自己引入的台账陈化而把 `libraryDocs.ts` 记数改成 401，只此一行。
   - **共享 `node_modules/.bin` 在本席施工途中消失**（`D:\ilife\node_modules\.bin`，进场时还在，里面有 `tsc`／`tsserver`／`changeset` 九个壳）。本席没写 `D:\ilife`，只在**自己检出**里另建本地 `.bin` 兜住；**别的检出若还用 `npx tsc` 驱动编译，会静默空转到 npm 上的同名占位包并 exit 0**，建议编排者回补共享 `.bin` 并在派单书里点明。
