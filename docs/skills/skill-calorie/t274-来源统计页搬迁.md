# t274 · 来源统计页就地搬成姊妹件（编排者裁定 (b) 的落地读数）

> 编排者 2026-09-15 裁定：来源统计页属 ⑤ 食品库类（`t425-融合基准.md` §一 第 ⑤ 类＝`food_search`／
> `dedupe_report`／`source_stats`），**裁 (b)：切姊妹件 `src/diet/sourceStatsDocs.ts`，归 #274**。
> 执行约束照 #393 那一轮：只搬装配件、取数与接线留原地、**产物逐字节不变**、导出面收小、台账同步。

## 一、搬迁范围（只搬装配件）

| 项 | 搬前 | 搬后 |
|---|---|---|
| 装配件 `buildSourceStatsDoc` | `src/diet/nutritionPortDocs.ts:300-344` | `src/diet/sourceStatsDocs.ts` |
| 取数 `buildSourceStatsView` | `src/diet/nutritionPort.ts` | **留原地**（本件 import 它的公开接口） |
| 接线 `viewSourceStats` | `src/diet/library.ts` | **留原地**，只改一行 import 指路 |
| 声明 `calorie.view.source-stats` | `src/diet/commands.ts` | **一行未动** |
| 路由词（看食品来源统计／看食品来源分布） | `src/triggers/routes.generated.ts` | **一行未动**（生成物本票不写不 add） |

`nutritionPortDocs.ts` 只删了那一支函数与其小节注释、改件头说明、把 `SourceStatsView` 从类型 import 里去掉；
其余（配比／营养素深度／饮水／总览四支与两个具名区块）**一行未动**。函数体逐字照抄（含 #496 的两条注释）。

## 二、判据①：搬迁前后产物逐字节相同

同一条命令、同一份播种库（5 条食品、4 个来源）：

```
calorie-cmd-read calorie.view.source-stats
```

| 步 | 产物 sha256 | 字节 | 判定 |
|---|---|---|---|
| 搬前（`nutritionPortDocs.ts` 里那一支） | `2b5964a002b7c5683340aeab1845e3f566e61d6dd35a1420ecf24c2b804e8514` | 71835 | 基线 |
| 纯搬后（新件逐字照抄、只改 import 指路） | `2b5964a002b7c5683340aeab1845e3f566e61d6dd35a1420ecf24c2b804e8514` | 71835 | **`MOVE_IDENTICAL=true`** |

## 三、判据②：补齐 ⑤ 类骨架四行 ＋ 复制区日志（搬完再动内容）

搬完才按 `t425` §五 第 ⑤ 类骨架补缺的几行——这几行是**内容改动**，产物自然不再逐字节相同：

| 行 | 补什么 | 实测（产物可见文本） |
|---|---|---|
| 1 眉标行 | `metaLeft` ＋ `badge` | `看食品来源统计 · 饮食` ＋ 徽章 `食品库` |
| 2 内容标题 | 照老实物 h1 加 emoji | `📦 食品来源统计`（老实物 `source_stats.html:61`） |
| 3 结论句 | 含读数一行 | 「食品库 5 条在架食品来自 4 个来源，最多的是「自制」（2 条 · 40%）。」 |
| 4 页内导航 | `renderTocBlock` | 锚点 `sec-count`（读数）／`sec-chart`（按来源分组）／`sec-table`（来源明细） |
| 13 口径行 | `renderCaliberLine` | 「占比是这一来源的条数 ÷ 在架食品总数，保留一位小数。」 |
| 14 复制区 | 原只有一颗 **disabled 的死按钮**「复制日志」→ 双按钮＋日志第 4 段＝命令原文 | `copyArea({data, log})`；第 4 段逐字 `calorie-cmd-read calorie.view.source-stats` |
| 15 来源脚注 | 原缺 → `renderCaliberLine`（**不走 `notice` 深底块**，裁定 2-补） | 「📊 数据来源：本机食品库 · 在架食品共 5 条」 |

读后读数：产物 71835 → **73461 B**（sha256 `c69054ab570c7311…`）；`#274 来源统计：搬成姊妹件后补齐的 ⑤ 类四行骨架 ＋ 日志第 4 段` 一条测试绿。

## 四、导出面（一处与裁定字面的偏差，已报编排者）

裁定里写「`diet/index.ts` 转出新件出口，调用方 import 跟着改」。本席**只改了调用方 import**
（`diet/library.ts` → `./sourceStatsDocs.js`），**没有往 `diet/index.ts` 加转出**，理由两条：
① 那个件头注写明「对外三件（铁律五「不多于五个」）」，加一个**没有任何调用方**的名字会让三件面变成四件死面；
② #393 那一轮搬四件时走的也是「姊妹件直接 import」这条路（`diet/library.ts` 引 `./libraryDocs.js` 同形）。
若编排者要按字面加，回一句，本席补一行即可（成本一行）。

## 五、台账

`nutritionPortDocs.ts`：**492 → 449 LF**（没掉回 350 以内，故那条台账行照旧挂号，只在结论列追加
「这条拆法的第一步已由 #274 走完」的事实句）。新件 `sourceStatsDocs.ts` **123 LF**，未越线、不挂号。
同步与复跑：`--sync --dry`（`SYNC-PLAN 改=3 增=0 删=0`）→ `--sync`（`SYNC-VERIFY ok`）→ 复跑
`RESULT: 67/67` ＋ `PASS: 告警线台账齐全且与实况一致`。
