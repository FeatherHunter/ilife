# #342 运动记录级明细 · 证据（新命令 `calorie.view.exercise-records`）

> 地图 #156 子票 #342 · 2026-09-14 · 结构落点 `packages/skill-calorie/src/exercise/`。
> 票面与任务描述一致，无偏差声明（已认领 `FeatherHunter`，`gh issue view 342` 为 OPEN）。
> 允许写集合（声明即锁）：`exercise/commands.ts`、`exercise/records.ts`（新）、
> `exercise/routes.ts`、`triggers/scene-04-exercise.ts`、
> `test/exercise-records-342.test.mjs`（新）、`docs/skills/skill-calorie/t342-*`、
> `.changeset/342-*.md`。禁写：`src/home/`、现有 9 条行为、分派层 `case`、生成物手改、
> 场景 05／体重残件、`cmd-write-40`、`t97 SCENARIOS`。`.scratch/t342/` 为草稿（永不入仓）。

## 1. 判据先行（开工前现状，必须红）

- `routesFor('看运动记录（有备注）')` 当刻只有 `non-exec`（`legacy-chain`，理由 `noNoteFilter`），无 `exec`。
- 另 4 条当刻：`看今日运动`／`看昨日运动`→`calorie.view.exercise`（汇总页）；
  `看运动记录（按力量筛选）`→`strength`、`（按有氧筛选）`→`cardio`（总览页，均非记录级明细）。

GATE-RUN runId=fda7962d-a39a-4ced-9e97-5646cb32f787 cmd="node -e routesFor有备注探针" exit=0（探针本身绿，判据红：无 exec）
GATE-RUN runId=f8223f65-b123-449c-b5fd-6b3067e4602f cmd="node -e 其余四词探针" exit=0

## 2. 实现（只写声明集合）

- `src/exercise/records.ts`（新，199 LF，未超 350 线）：取数走 `render/exercisePort.ts` 的
  `listPortRows`（窗内全部行，分类口径库内实填优先、缺失回退推断，与力量／有氧总览同口径，
  未搬迁，只经公开接口引用）；筛选 `category`（非空串）＋ `hasNote`／`withNote`（布尔，
  与饮食 `today.ts` 同形）；筛空即 `missing-data`，不返空表。装配走共用
  `shared/docPage.ts` 的 `assembleDocPage`＋`shared/copyArea.ts` 的 `dataCopyArea`
  （与运动移植 6 页同形：窗口表单＋指标＋逐条表＋复制区）。记录级列表 8 列：
  日期／类型／分类／时长／消耗／距离／心率／备注。对外 2 件：`viewExerciseRecords`、
  `buildRecordsDoc`（另 1 接口 `RecordsView`，共 ≤5）。
- `src/exercise/commands.ts`（35 LF）：+1 条 `calorie.view.exercise-records`
 （`read`／`stat`／标题运动记录／代表唤醒词 `看运动记录（有备注）`／示例
  `calorie-cmd-read calorie.view.exercise-records --params '{"window":"7d"}'`，本地种子库实跑 exit 0）。
  文件头 9 键→10 键，看运动＝`view.exercise-goal`／`view.exercise-records`。
- `src/exercise/routes.ts`（45 LF）：`order 161` 由 `non-exec` 翻为 `exec` 新命令
 （`window 7d＋hasNote true`）；`162`→新命令 `category 力量`；`163`→新命令 `category 有氧`。
  `order` 未动，`wake 20` 条当刻 0 条 `non-exec`。`看今日运动`／`看昨日运动`
 （`order 150／151`，住 `src/home/routes.ts`，场景 01 地盘，本票禁写）未动——
  新命令已支持 `window 今日`／`昨日`直跑（测试第 5 条），路由改指待跨图搬迁（见 §6）。
- `src/triggers/scene-04-exercise.ts`：上 3 条词的 `main_prompt.cli`／`data_source`
  同步到与路由逐字一致；其中有备注由非命令形态 `看带备注的运动记录` 翻为真命令形态。
  `看今日／昨日运动` 的冻结行未动（与 `home` 路由保持逐字一致，D2④ 仍绿）。

行数：三件均未超 350 线，无需重构（`skill-calorie/AGENTS.md` 口径 LF）。

## 3. 验收（票面四命令）

GATE-RUN runId=2eb32633-6d72-44f0-92a3-dc044ee76ed4 cmd="pnpm build" exit=0
GATE-RUN runId=54bb372b-45cf-4169-900b-dfb2a13b2c05 cmd="pnpm gen" exit=0（键 118＝写 46＋读 72，+1 为本票新键）
GATE-RUN runId=78366f4e-bee2-4216-8bee-e64e2539b5e2 cmd="pnpm build" exit=0
GATE-RUN runId=b103179c-614d-44b1-a408-4edd7e53fbf7 cmd="pnpm help:build" exit=0
GATE-RUN runId=7b08aa17-7fc9-4002-957a-4b0ad3745871 cmd="pnpm gen:check" exit=0（GEN-CHECK PASS 键 118）
GATE-RUN runId=8335c4e3-4a39-41c1-b1ed-c6f9443902f0 cmd="node packages/skill-calorie/test/exercise-records-342.test.mjs" exit=0（5/5 绿）
GATE-RUN runId=1c946e10-dab7-4aa5-8356-568fbdcf7e54 cmd="node packages/skill-calorie/test/exercise-records-342.test.mjs（还原后）" exit=0（5/5 绿）

中间态说明（新增命令的类型门 staged）：首建 `b062af2b-efda-4dd5-b55d-c15af49ebb28` exit=1
（`TS2820` 新键未进 `keys.ts`，但 `tsc` 已落盘 `dist` 新声明；随后 `--stamp`＋`gen`＋重建即绿，
见 `stamp 31097095`／`gen 273845f4`／`build b5d1669a`）。生成链最终四步全绿，无假绿。

测试断言（`exercise-records-342.test.mjs` 5 条）：
1. 新命令 `window 7d`→exit 0、五连（`doc-page-assert.mjs`）、8 列＋复制区；
2. 有备注词有 exec（新命令）且实跑是这一页（只含备注行，不含无备注行）；
3. 力量筛选指新命令且实跑是力量子集；4. 有氧筛选指新命令且实跑是有氧子集；
5. 新命令直跑 `今日`／`昨日`窗口即记录级明细（命令侧就绪，路由侧待搬，见 §6）。

## 4. 变异自证两行

- 变异红：把 `src/exercise/records.ts:164` 的 `运动记录明细` 改为 `运动记录XX`，
  重建后 `node test`→exit 1，5 红（`缺记录级列表标题`）。
  GATE-RUN runId=f5d60583-afb7-45ea-afed-46f8d53d6e6b cmd="node packages/skill-calorie/test/exercise-records-342.test.mjs（变异）" exit=1
- 还原一致：改回＋重建后→exit 0，5/5 绿。
  GATE-RUN runId=728db5e6-ac0b-4050-a739-cd0e31143f11 cmd="pnpm --filter skill-calorie build（还原）" exit=0

## 5. 路径对账（第一步清单逐行有着落，偏差见 §6）

- `packages/skill-calorie/src/exercise/commands.ts`（改，+1 声明＋头注 9→10）
- `packages/skill-calorie/src/exercise/records.ts`（新，取数＋装配）
- `packages/skill-calorie/src/exercise/routes.ts`（改，161 翻 exec＋162／163 改指新命令）
- `packages/skill-calorie/src/triggers/scene-04-exercise.ts`（改，上 3 条 cli／data_source 逐字同步）
- `packages/skill-calorie/test/exercise-records-342.test.mjs`（新，5 条＋变异说明）
- `docs/skills/skill-calorie/t342-证据.md`（本件）
- `.changeset/342-exercise-records.md`（新增命令的变更说明）
- `.scratch/t342/`（草稿日志：build／gen／test／mut×2／restore×2，永不入仓）

生成物（`keys.ts`／`registry.ts`／`combos.yaml`／`build-help.mjs`／`routes.generated.ts`／`SKILL.md`）
由 `pnpm gen` 重生成，未手改，不在本票提交内（含他人 #333／#335 的脏改，本票未还原）。

## 6. 未做项与下一手（偏差为零之外的两处，均已留痕）

1. `看今日运动`／`看昨日运动`（`order 150／151`，住 `src/home/routes.ts`）未改指——
   本票禁写 `src/home/`，且该文件当刻有他人脏改（`weight/routes` 等 15 件，见 `handoff`）。
   新命令已支持二者参数形状（测试第 5 条直跑今日／昨日全绿），路由搬迁需与场景 01 图
   （#162）串行＋抢 `gate.lock`＋广播，由编排者协调。票面 5 条中本票交付 3 条路由改指＋2 条命令就绪。
2. Parity 手跟未落盘：`docs/research/t81-exec-smoke.md` 快照（本票 +1 exec，有备注 `hasNote true`
   在标准种子库无备注运动行→预期 exit 4，需按体重 #333 口径在 `routing-81` 登记
   `DATA_DEPENDENT_FAILURES`，但该文件非本票路径且当刻脏）与
   `packages/skill-calorie/test/help-center-106.test.mjs` 场景数（367→368，
   当刻他人脏改 357→367 未提交）均留到最后一步。落盘前已重查：
   `help-center-106` 仍脏→按编排者口径不碰，现场见 `.scratch/t342/handoff.md`。
   `cmd-write-40`、`t97 SCENARIOS` 按票面未动（读命令）。

## 7. 对账导出

本节 GATE-RUN 均可在 `.scratch/locks/gate-runs.log` 按 `runId` 一对一复核；
收尾已把本窗口运行条目导出备查（见 `.scratch/t342/` 日志与本件引用）。
