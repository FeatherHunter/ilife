# t715 · 搬家证据（运动网页单件 `sportPortDocs`：`src/render/` → `src/exercise/`）

> **判据（本票主判据）**：**搬迁前后逐页逐字节相同**（票面「判据」段第一句）。
> 本窗不改任何可见文本、不改任何形状：搬的是代码住在哪个目录，产物必须一个字节都不动。
> 全部编译／测试／门禁经 `node tooling/run-locked.mjs --ticket 715 -- …` 排队；本窗写集见 §五。

## 〇 · 本窗做了什么（一句话）

按**归属律**（`docs/agents/structure.md`「共用件」那一条：写不出哪两个能力在用的，留在那个能力的目录里），
把 `src/render/` 里**只被 `exercise` 一个能力目录引用**的最大单件 `sportPortDocs.ts`（**966 LF**）
**原样迁入** `src/exercise/sportPortDocs.ts`。
`git` 把它识别为重命名 **`R099`**——搬完只剩 2 行差异，就是 §一.2 那两处就地摆正，其余 964 行逐字节相同。

## 一 · 搬了什么（逐件）

| 项 | 读数 |
|---|---|
| 原址 | `packages/skill-calorie/src/render/sportPortDocs.ts` |
| 新址 | `packages/skill-calorie/src/exercise/sportPortDocs.ts` |
| LF（搬前＝搬后） | **966 → 966**（LF 口径＝只数 `\n`：`readFileSync(f,'utf8').split('\n').length - 1`） |
| 字节（搬前＝搬后） | 50,657 → 50,657 |
| 搬走件自身改动面 | **2 行**（见下 §一.2） |

### 1.1 改引用 13 行（`move.mjs` 逐条锚定，命中数不符即停）

- **生产 5 行**：`src/exercise/{cardio,distribution,recap,strength,trend}.ts` 各 1 行
  `'../render/sportPortDocs.js'` → `'./sportPortDocs.js'`。
- **测试 4 行**：`test/{exercise-dist-strength-cardio-fusion-453,exercise-text-shape-544,exercise-trend-recap-fusion-454,t576-n1n8}`
  的 `'../dist/render/sportPortDocs.js'` → `'../dist/exercise/sportPortDocs.js'`（深路径跟着 `dist` 目录走）。
- **散文里的「件住哪」引用 4 处**：`src/exercise/index.ts`（能力门头注点名的域内件清单）、
  `src/render/exercisePort.ts`（件头「组装归 …」）、`test/analysis-deficit-385.test.mjs`、
  `test/exercise-text-shape-544.test.mjs`。老串写的是老址真路径 ⇒ 搬完全部陈化，
  统一改写成**包内规范路径** `src/exercise/sportPortDocs.ts`（不写成「相对谁的文件」，
  免得同一句话在不同件里指不同地方）。

### 1.2 就地摆正 2 行（**与票面「零就地摆正」不一致，据实改**）

票面写「出向引用**全是同能力件与共用位**……搬完**零就地摆正**」。
**实测不成立**：出向里有两件留在 `render/`，跟着走的件离了目录就断。

```
-import { nowStamp } from './receipt.js';
+import { nowStamp } from '../render/receipt.js';
-} from './exercisePort.js';
+} from '../render/exercisePort.js';
```

这一条不是猜的：`move.mjs` 的自检（第四步：`src/**` 每一条相对 specifier 都要解析到盘上真件）
在首跑当场点名 `exercise/sportPortDocs.ts → ./exercisePort.js`，脚本按「失败即不落盘」exit 1。
按当刻内容补上这两行后自检转绿（`fix-outbound.mjs`，同样先断言命中 1 处再写）。

### 1.3 历史事实句 6 处**照旧不动**（具名记账）

`src/exercise/{cardio,distribution,recap,strength,trend}.ts:5` 与 `src/workout/review.ts:5` 里的
`render/sportPortDocs.ts` **原样保留**——那几句记的是**老址的历史事实**
（「#316 搬迁：处理函数逐字取自 `src/cli/cmd_read.ts` 的 `case …`」／「T351-v7 从 `render/sportPortDocs.ts` 搬进复盘族」），
改了等于改历史。收工自检按「代码行 0 处／注释行 6 处」两面分别报（§三.4）。

## 二 · 重出器与产物基线（判据的口径与产物来源）

- **重出器**：`.scratch/t715/regen.mjs`（本票独占）。覆盖口径按「**搬迁面能影响到的页**」定：
  被搬的这一件正是产出这**五键**的那一件
  （`calorie.view.exercise-cardio`／`-distribution`／`-recap`／`-strength`／`-trend`，声明在 `src/exercise/commands.ts:35-41`），
  所以五键各按**路由表上的唤醒词与参数**出全（`src/exercise/routes.ts:42-56`：
  分布／力量／有氧 7d、趋势 30d、复盘 本周／本月／90d），另补窗档（今日＋`offset:-1d`／本月／今年）、
  跨年窗（366d，逼出逐日表 >100 行的截断明示与跨年轴那一支）。
- **种子库**：`.scratch/t715/seed.mjs`（本票独占）——**确定性**：日期由固定窗口
  `[2025-08-01, 2026-09-18]` 派生、数值由记录序号派生，不用随机数、不读 `new Date()`。
  运动记录走**四天循环**（力量·力量·有氧·柔韧，隔日落一条）⇒ 任意 7 天窗都非空且两类都出得到；
  另铺饮食 1227 条（分布页的摄入／TDEE 联动那几格）与体重 110 条（TDEE 折算要真实体重）。
  种子走**写出口**（`calorie.exercise.add` 等批量形态）而不是直接 INSERT：字段口径（分类／难度推断、软删位）
  住写处理函数里，直接 INSERT 等于把口径再抄一遍，抄的那份迟早与正本走散、把判据搅成假红。
- **时钟冻结**：`.scratch/t715/freeze.mjs` 经 `node --import` 预载，复用仓内既有件
  `packages/skill-calorie/test/freeze-clock.cjs`（#326 立，`FAKE_NOW_ISO` 钉 `2026-09-18T12:00:00Z`）。
  两个理由：① 五页的窗口缺省口径 `defaultRange(db, params)` 以「今天」为锚，不冻钟则换一天基线全废；
  ② 只读页的复制日志走 `render/receipt.ts` 的 `nowStamp()`（`new Date()` 直读、无注入口），
  不冻钟则同一份源码连出两遍跨 1 秒就逐字节不同——判据照字面既出假红也出假绿（#704 已具名这条前置）。
- **对照页 2 条**：`calorie.view.weight`（体重盘）与 `calorie.view.diet-review`（饮食复盘），
  产出者都**不在搬迁面**——它们红了就说明抖动来自重出器或种子库本身，不是来自搬家。本窗两条都绿。
- **断言（失败即不落盘）**：每页 exit 0 ＋ 文档完整性四条（doctype／charset／`</html>` 收尾／零槽位残留）。

### 2.1 空窗那一态：CLI 够不到，改走五件直调（`.scratch/t715/empty.mjs`）

票面要求「含窗口档位、**空窗那一态**」出全。实测：五键的取数层在窗内零记录时**一律抛阻断**
（`render/exercisePort.ts:77`／`:416` 与 `analysis/exerciseReview.ts:28` 的 `无运动记录：…`，
CLI 报 `ERR 4 取数失败（缺失阻断）`）⇒ **CLI 出不出空窗页**，够不到
`buildCardioDoc`／`buildDistributionDoc`／`buildStrengthDoc`／`buildTrendDoc`／`buildRecapDoc`
各自的 `hasRows === false` 那一支。故这一态用**五件直调**补：把五个空视图各装配一遍，
返回值（全文档，与 CLI 落盘那份同构）当页产物逐字节比。同时留一条**反证**：
空窗参数在 CLI 上必须仍是 `ERR 4 缺失阻断`（搬迁不许把「取数阻断」变成「出页」或换失败码）。

## 三 · 判据读数（逐条）

### 3.1 主判据：30 页产物逐字节相同

| 轮次 | 命令 | 读数 |
|---|---|---|
| 开窗当刻（HEAD `87ea157f`） | `node .scratch/t715/regen.mjs` | `RESULT: 25/25 页 ＋ 空窗 5/5 直调落盘 bytes_total=2695486`；`STABLE-A-vs-B pages=25/25 DIFF=none` |
| 搬迁后（重编＋重出） | `node .scratch/t715/regen.mjs --tag MOVE --compare .scratch/t715/基线.json` | **`MOVE byte-identical pages=30/30 DIFF=none`**；`total_before=3151588 total_after=3151588` |

比对口径＝逐页 `sha256` ＋ `bytes` 双比，30 条逐条比、不抽样（25 条 CLI 分支路径 ＋ 5 页空窗直调）。

### 3.2 变异自证两轮（打的就是本窗判据本身的识别力）

在**搬迁后的新家** `src/exercise/sportPortDocs.ts` 里改坏一处会落到页面上的可见文案 →
重编 → 重出 → 与基线比；再**逐字节写回原文** → 重编 → 重出 → 再比。两轮共用一次持锁窗口。

```
【力量页来源句】STRENGTH_SOURCE '运动记录（本窗未删除的力量行）' → '…（改坏了的力量行）'
MUT1-RED byte-identical pages=25/30 DIFF=05(力量 7d),06(力量 30d),07(力量 366d),08(力量 本月),E2-力量(空窗)
  total_before=3151588 total_after=3151558          ← 每页恰 −6 B（＝「未删除」→「改坏了」的字节差）
RESTORE-1 相等=true sha256_12=3ae9f65c4050
GREEN1 byte-identical pages=30/30 DIFF=none
【有氧页 km 单位】numUnit(v.totalDistanceKm, 'km') → 'km改坏'
MUT2-RED byte-identical pages=29/30 DIFF=E3-有氧(空窗)
  total_before=3151588 total_after=3151594          ← 恰 +6 B
RESTORE-2 相等=true sha256_12=3ae9f65c4050
GREEN2 byte-identical pages=30/30 DIFF=none
```

两轮的**识别面不同**，合起来证明判据打的是页产物、且五键都在面内：
第一轮只红**力量那一键**的 4 条 CLI 路径 ＋ 它的空窗直调页（别键一字不动）；
第二轮只红**有氧那一键**的空窗直调页——正好指向它唯一读 `km` 的那一处（有 7d／90d／今日／今年
四条 CLI 路径在空窗上被取数层阻断，所以只有直调页能看见这一处）。
还原判据＝文件字节 `sha256` 与改前逐字相同（**逐文件写回**，不是整目录还原）。

### 3.3 收尾三绿（票面「收尾三绿」）

| 门 | 命令 | 摘要行 | exit |
|---|---|---|---|
| 告警线 | `node packages/skill-calorie/scripts/check-warning-line.mjs --sync --dry` → `--sync` | `SYNC-PLAN mode=write 行=45 改=0 增=0 删=0`；`SYNC-VERIFY ok`；`RESULT: 93/93` | 0 |
| 生成链 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `GEN-CHECK PASS：键 134（写 52 ＋ 读 82）；能力 10 个…未搬迁清单 0 条` | 0 |
| 基线 | `node tooling/baseline.mjs --save …` | **红（外部阻塞，见 §四）** | 1 |

**台账那一条按 #704 的口径手改、不让 `--sync` 改名**：`件` 列地址从 `src/render/sportPortDocs.ts`
手改成 `src/exercise/sportPortDocs.ts`，**挂号值（`—`）与结论原样带过去**（另按台账惯例补一段
「**#715 已在场**（纯搬迁，966→966…）」在场记录）。搬完 LF 不变 ⇒ 同步器预期 `改=0`——
实测 `改=0 增=0 删=0`，与预期逐字吻合，也证明本窗没有把别席的数字 baked in。

### 3.4 收工机械自检（`.scratch/t715/verify.mjs`）

```
① 相对 specifier 解析：2491 条，未解析 0 条
② 老址字面：代码行 0 处（要求 0）／注释行 6 处（历史事实句，逐条点名）
③ 台账「件」列：旧址 0 行／新址 1 行
RESULT: 自检全绿
```

## 四 · 收尾三绿的第 3 条：**基线门红，外部阻塞，据实记账**

`node tooling/baseline.mjs --save` 在本窗内**跑了两次**（`runId=…b5cb6416` 与 `…dd0e724b`），两次同一条红：

```
FAIL: ENOENT: no such file or directory, open 'D:\ilife\packages\skill-calorie\src\cli\legacy\index.ts'
```

**根因（实测）**：`baseline.mjs:28` 的源面清单来自 `git ls-files`，而**当刻索引里挂着 25 个
「已删、未提交」的件**（`git ls-files` 逐件 `Test-Path` 数出来：25 件，全部是
`src/cli/legacy/**` 23 件 ＋ `src/shared/pageKit.ts` ＋ `src/shared/conclusionLine.ts`）。
它们**一个都不在本票写集里**（逐条比对：本票相关＝0 件），是**别席在途**的删除
（对应 #705「没有调用方的四处」那张票，与地图 #699 的 Decisions-so-far 里那行一致）。
⇒ 这是**共享工作区里别席未提交状态**造成的**当刻不可复算**，不是本票引起的，也不该由本票
去动那 25 个路径（协议 §1「禁止修改其他票已声明占用的路径」）。

**复现**（任何人可自核）：

```
git ls-files -z | %{ $_ -split "`0" } | ?{ $_ -and -not (Test-Path $_) }   → 25 件，全在 src/cli/legacy/** 与 src/shared/{pageKit,conclusionLine}.ts
node tooling/baseline.mjs --save .scratch/t715/基线-收窗.json              → FAIL: ENOENT（同上）
```

**出口**：那 25 件一旦由它的票提交，本门即可跑（它查的四面里 freezing 面与 dist 面本窗都不受影响）。
⇒ 判据属性＝**本地可判但此刻被外部未提交状态挡着**；按「判据可终止」的写法记成
「改动已落地 ＋ 点名外部确认（谁触发：#705 的提交；看哪个读数：`baseline: PASS（差异 0 项）`；
失败怎么办：若提交后仍红，按 `baseline.mjs` 逐条点名的差异面重查）」。

## 五 · 必报五步第五步：交付对账（偏差为零）

**提交 `682d8df8`**（`git show --name-status` 报 14 个路径，14 files changed／16 insertions／16 deletions）：

```
M  packages/skill-calorie/AGENTS.md                                              ← 台账「件」列地址 ＋ 在场记录
M  packages/skill-calorie/src/exercise/{cardio,distribution,recap,strength,trend}.ts  ← 各 1 行引用 ＋ 头注 1 处
M  packages/skill-calorie/src/exercise/index.ts                                  ← 头注「件住哪」1 处
R099 packages/skill-calorie/src/render/sportPortDocs.ts → src/exercise/sportPortDocs.ts
M  packages/skill-calorie/src/render/exercisePort.ts                             ← 头注「组装归 …」1 处
M  packages/skill-calorie/test/{exercise-dist-strength-cardio-fusion-453,exercise-text-shape-544,exercise-trend-recap-fusion-454,t576-n1n8}.test.mjs
M  packages/skill-calorie/test/analysis-deficit-385.test.mjs                     ← 头注 1 处
```

**暂存前逐件复核过 `git diff --cached --name-status`**：暂存区全是本票声明的路径，无一件他席的在途件
（当刻树上另有一批别席未提交的件：`src/cli/legacy/**` 23 件删除、`src/shared/{pageKit,conclusionLine}.ts` 删除、
`src/render/diet.ts`／`src/diet/review.ts`／`src/fetch/diet.ts`／`src/analysis/anomaly/common.ts` 等改动——
本席一行未碰、也未带进任何提交）。

**第一步清单逐行对账：偏差为零**（13 条锚点 + 1 件搬迁，全部落在清单内的目录）。

## 六 · 票面两个计数器的**可归因**变化

票面要求开工与收工各记一次：外面 → `render/` 深路径 import 行数、`render/` 反向 import 的能力目录数、
`render/` 件数与 LF。本窗期间别席有在途与已提交的变动，故把**本票提交点**的源码面单拿出来算
（`git archive 682d8df8 packages/skill-calorie/src` 解出来算），两边口径同一份 `count.mjs`。

| 读数 | 开窗当刻（HEAD `87ea157f`，别席在途不计入） | **本票提交点 `682d8df8`** | **本票可归因** |
|---|---|---|---|
| ① 外面 → `render/` 深路径 import 行数 | 185 | **181** | **−4** ✅ 降 |
| ② `render/` → 能力目录数 | 11 | **11** | **0**（见下） |
| ③ `render/` 自身 | 29 件／9156 LF | **28 件／8163 LF** | **−1 件／−966 LF** ✅ |
| ④ 被搬件的 LF | `render/sportPortDocs.ts` 966 | `exercise/sportPortDocs.ts` 966 | 966 → 966（不变，与台账口径一致） |

- **① 为什么是 −4 而不是 −5**：五条生产引用里，`src/exercise/index.ts` 那条是**纯注释行**
  （头注点名的域内件清单），另 4 条才是真 `import` 语句。
  票面「生产 5 行」按「提到它的行」数、本读数按「真正的 import 行」数，两个口径都写在这里，别混用。
- **② 本批不降，如实报**：`render/` 里反向引用 `exercise` 的**还有 4 行、且全在 `exercisePort.ts`
  （3 行）与 `sportDocs.ts`（1 行）**，这两件本批不搬 ⇒ 能力目录数从 11 降到 10 要等它们各自的批次。
  票面把它列成「开工与收工各记一次」的读数（不是断言它必降），故**据实报 11 → 11**；
  #704 那批给的是 12 → 11（它的可归因未降项是 `xunji` 随件迁出归零），两张票的降项不同、可并存。
- **③ 反面对照**：当刻树（含别席在途）读到 28 件／**8113 LF**，比本票提交点少 50 LF——
  那 50 行是别席改 `src/render/diet.ts` 的在途成果，**不计入本票的账**。

## 七 · 本窗具名未做项（不阻塞本票，逐条给下一手）

1. **基线门**：见 §四，等 #705 那 25 件提交后即可跑（本票已把它跑红两次并留痕）。
2. **`test/exercise-dist-strength-cardio-fusion-453.test.mjs` 三条红是既有红，不是本窗引入**（如实记账）：
   三条断言都死在 `:185 assert.ok(r.file.includes('ilife-copy-log'), …缺复制日志按钮)`。
   **判定依据（可复核）**：本窗**开窗当刻**（搬前，HEAD `87ea157f`）存下的 25 页基线产物里，
   五键页 `ilife-copy-log` 命中 **0 处**（对照页命中 1 处）——即**搬前就不在**，
   搬后逐字节相同 ⇒ 本窗不可能引入它。溯源：公共层把复制区按钮从 class 改 `data-action-id`
   （`03b31e65` feat(247)，改的是 `src/shared/copyArea.ts` 与 `copy-component-179.test.mjs`，
   **没有动这份 453 测试**），而该测试读的是 class 字面。
   ⇒ 属**范围外发现**（#702 口径第 2 条说的「断言锁在实现上」那一类），转票措辞留给收口；
   本窗按票面「不顺手改」红线**一行未动**。
3. **同一个搬迁面里还有两件同类件没搬**：`src/render/exercisePort.ts`（544 LF，取数）与
   `src/render/sportDocs.ts`（466 LF，运动汇总／目标两页）。它们也是「只被 `exercise` 引」的一族，
   但票面只点了 `sportPortDocs` 一件 ⇒ 留 #704 解决评论 §七 的第六批（跨能力件各自归位）。
4. **台账里本件那一行的拆法原样留着**：结论文写的仍是「按页族切两件姊妹件（分布／力量／有氧一族 与
   趋势／运动复盘一族）」——搬迁完成后地址已改、拆法照旧留给收口票（#704 已裁：先搬、后拆）。
5. **第四步超线报警**（本包告警线 350，LF 口径）：`src/exercise/sportPortDocs.ts` **966 LF，已超线，
   需要根据规则进行重构。** 超因与拆法照台账原文（#111 的六键全文档装配同处一件；
   拆法＝按页族切两件姊妹件，两族共用的卡装配／导航／色映射提为一件共件，出口经本件薄转出）。
   **本窗先不拆**：搬迁判据是「逐字节相同」这条保字节的强不变量，同一窗口里再叠一次结构变更会把
   它变成两条变更的合成读数（#704 已裁的口径）。

## 八 · 门禁运行对账（协议 §2.4）

```
GATE-RUN runId=835e0a14-8715-4bb5-854b-65ad15144d68 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie（搬前）"                    exit=0
GATE-RUN runId=26ba1212-c97e-49d7-8b2f-9c03150afc70 cmd="node .scratch/t715/move.mjs ＋ tsc（搬迁＋编译；move 自检红 exit=1）"                      exit=1
GATE-RUN runId=34c73183-ccde-4002-8f2e-228a26560f5e cmd="node .scratch/t715/mutate.mjs（变异两轮＋还原，持锁窗口）"                                 exit=0
GATE-RUN runId=d8ee26db-3401-4c42-bf86-d7f988330d87 cmd="node packages/skill-calorie/scripts/check-warning-line.mjs --sync --dry"                    exit=0
GATE-RUN runId=c17ec2a0-fa28-431e-85eb-e1a8e1703bef cmd="node packages/skill-calorie/scripts/check-warning-line.mjs --sync"                         exit=0
GATE-RUN runId=de0ef248-b085-446f-8d5b-b89dcf318a22 cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"                                    exit=0
GATE-RUN runId=3e2dfe22-4bdd-4d8c-a97c-b65af9a2d81d cmd="node --test …（4 件靶向；453 三条既有红）"                                                exit=1
GATE-RUN runId=414419e2-9dc7-4f52-bc53-dcee514bc44d cmd="git commit -F .scratch/t715/commit-msg.txt -- <本票写集>"                                    exit=0
GATE-RUN runId=b5cb6416-53e9-4de1-a108-e9f361055f03 cmd="node tooling/baseline.mjs --save .scratch/t715/基线-收窗.json（首跑，ENOENT 红）"           exit=1
GATE-RUN runId=dd0e724b-7b02-444f-b644-abdf22fd4b4b cmd="node tooling/baseline.mjs --save .scratch/t715/基线-收窗.json（复跑，同红）"                exit=1
```

复跑脚本随证据入仓：`docs/skills/skill-calorie/t715-regen.mjs`／`t715-freeze.mjs`／`t715-seed.mjs`／
`t715-empty.mjs`／`t715-move.mjs`／`t715-mutate.mjs`／`t715-count.mjs`／`t715-verify.mjs`。
盘点与留痕类（`fix-outbound.mjs`、`run-*.log`、基线 JSON、产物目录）住 `.scratch/t715/`（`.scratch/` 不入仓）。
