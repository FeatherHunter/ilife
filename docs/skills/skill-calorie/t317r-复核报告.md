# #317 对抗式复核报告（第二任 · 收敛者）

- 复核对象：`#317` 场景 05 健身计划的键搬进 `src/workout/`（父地图 `#293`）
- 落点提交：`535a9b2`（14 件）／收口提交：`dfe6288`（对账源按落点窗口重导）
- **对账基准（关键）**：本报告一切形态与行数均以 **`dfe6288` 的文件内容为复核对象**（用 `git show dfe6288:<路径>` 复算）。
  复核开始当刻分支 `master` 的 HEAD 已是 `a9fb35c`（`#316` 场景 04 落点）且工作树带 `#319` 在途改动（`src/analysis/**`）；
  复核结束时 HEAD 前进到 `9140f39`、工作树转净。**HEAD 之后的漂移不计入 #317 判定**（见 §6、§7）。
- 动手前状态：工作树在途 `#319` 改动（`src/analysis/**` 与 `cmd_read.ts`／`registry.ts`／`legacy/routes/scene-02,10.ts`）。
  经核实**该在途面与 #317 面零交集**（`git status` 对照 `workout/` 与两片 `scene-05.ts` 全空），故静态复核照常进行。
- 证据文件：`docs/skills/skill-calorie/t317-搬迁-证据.md`（124 行 / 15599 字节，sha256 前 16 位 `6288def9e38897e1`）
- 对账导出：`docs/skills/skill-calorie/t317-gate-runs.log`（13 行 / 1139 字节，sha256 前 16 位 `638285f8ac29c6c6`）
- 本席脚本（复用前席 ＋ 新增）：`.scratch/t317r/evidence-face.mjs`（前席）、`verify-v2.mjs`（本席独立解析器）、`pins.mjs`（跨提交行数／改件面）

## §1 交付判定

**判定：PASS** ｜ **分数：93 / 100**

理由摘要：三条交付完成判据与全部结构面／分派面／内容面判据实测成立，未发现交付缺陷（S1=0）；
未发现「声明与可核事实相矛盾」的陈述（S2=0）。全部扣分来自一处**留痕完整度**问题与两处措辞口径问题（S3=3）。

| 级别 | 条数 | 标题 |
| --- | --- | --- |
| S1（交付缺陷） | 0 | — |
| S2（证据与事实不符） | 0 | — |
| S3-①（留痕完整度） | 1 | 受跟踪对账源只覆盖 5/15 条被引 runId；6 条 `ticket=317` 绿色收口轮零留痕 |
| S3-②（措辞口径） | 1 | `routes/scene-05.ts` SOFT 相形态（未统一成「迁移注记 ＋ 单行空数组」） |
| S3-③（措辞口径） | 1 | 票面「要做的事 第 2/4 条」与派单冲突，以票面待确认条形式挂账（未在票面正文改写） |

分数构成：结构面 40/40 ＋ 分派面 30/30 ＋ 证据面 23/25 ＋ 票面纪律 5/5 − 措辞扣分 0，绝对项合计 **93**。
绝对项不含动态门禁；动态门槛另列（§5），**若该门槛判红则本判定自动降为 FAIL**。

---

## §2 三个复核范围的逐条结论

### 2.1 证据面（前席回报三条）

| # | 前席结论 | 本席复核 | 依据 |
| --- | --- | --- | --- |
| 1 | `GATE-RUN` 行止于右引号 ✓ | **成立** | 5 条全部 `endsAtRightQuote=true`：证据件 `:53`–`:57`（`cmd="pnpm build"`／`"pnpm gen"`／`"pnpm build"`／`"pnpm gen:check"`／`"pnpm test"`）；`parseClaims=5`、带 runId=5 |
| 2 | 导出件与活日志逐字相等 ✓ | **成立（有范围限定）** | 导出 5 条 RUN 行逐字命中活日志 `Set`：`9eaee523`／`566eded6`／`938427d7`／`fe57cc94`／`dedcac8a`。**限定**：只对「被导出的 5 条」成立；导出**覆盖面**另见 2.1 第 4 条 |
| 3 | 零 `--allow-*` ✓ | **成立** | `parseRelaxations(证据)=∅`；含 `--allow-` 字面量仅 3 行（`:59`／`:106`／`:124`），三行**全为「不使用任何 `--allow-*` 放宽」的声明句** |

> 方法学备注：`parseClaims`／`parseRelaxations` 用 `check-gate-audit.mjs` 自己的导出函数，属「被检对象的解析器」。
> 本席另以**自带正则解析器**（`verify-v2.mjs` 的 `RUN_RE`，不共享代码）复算全量日志：主锁 29 条 ＋ land 锁 8 条 ＝ 37 条，
> 与前席 `parseAuditLog` 的 37 条**双解析器一致**（V5b：条目数一致 = true，runId 集合一致 = true）。故 1–3 条不依赖单一解析器。

### 2.2 证据面（前席回报「但」字两条）

**（甲）活日志 29 条 vs 导出 5 条 —— 结论：成立，但须把「29」与「37」分清**

- 主锁 `.scratch/locks/gate-runs.log`（gitignored）里 `ticket` 含 `317` 的条目 **= 29 条**（本席当刻复算仍 29，
  文件在此期间从 625780 → 626935 字节，增量全属别的票号 —— 该 29 不随并发漂移）。
- land 锁 `.scratch/locks-land/gate-runs.log` 另有 **8 条** `317`／`317-land` 条目。**全量应记 37 条**；
  前席的「29」只数了主锁一处，漏掉 land 锁 8 条（其中 4 条是 `land.ps1` 守卫拦停、1 条是 #317 落点内层 `exit=1`）。
- 导出只覆盖 5 条 —— **两个数字都对**，但口径不同：29 是「同一票号字面量」，5 是「证据自己声明的那一窗」。
  按证据声明的窗口 `--since 2026-09-13T12:57:30Z --until 2026-09-13T12:58:22Z` 复算：窗口内恰好 5 条，`matched=5/5 undeclared=0`（V6[a]）。

**（乙）15 条被引 runId 里 10 条「没有受跟踪的对账源」—— 结论：成立，但前席的两处措辞必须纠正**

本席逐条核对这 10 条在活日志里的真实身份（V3），结果与前席的描述有实质差异：

| 前席措辞 | 实测事实（本席） |
| --- | --- |
| 「10 个没有受跟踪的对账源」 | 10 条**全部真实存在**于活日志；问题是它们**没进受跟踪的导出件**（`t317-gate-runs.log` 只 5 条）。属「留痕面不足」，不是「记录不存在」 |
| 「其中 13:30:13–13:31:28 一整轮 6 条从未被声明」（前席：证据正文未提及） | **部分成立**：那一轮**确实**在证据件里零提及；但另 8 条中的 2 条（`b993a7c2`／`a5468886`，对账导出与复验）在正文 `:83` **有**留痕说明 |

10 条的性质分成三组：

1. **8 条 `ticket=317-rescue`「收口复跑」**（`794eccb4` ／`03cedc8d` ／`14fb9393` ／`54cef0bb` ／`c482af80` ／`94cf514c` ／
   `a4d0522c` ／`00e160b4`）：证据正文 `:63` 与 `:80`–`:83` **逐条点名并说明时刻在窗外**，且 `:80` 抄录了实测结论
   （`tests 1492 / pass 1492 / fail 0`、`GEN-CHECK PASS`）。属**已声明、未导出**。
2. **2 条 `ticket=317-rescue`**（`b993a7c2` 导出、`a5468886` 以导出件当对账源复验）：正文 `:83` 有说明。同上。
3. **6 条 `ticket=317` 绿色收口轮**（`13:30:13`–`13:31:28`：`pnpm build`／`gen`／`build`／`gen:check`／`test`／`t293` 探针）：
   **证据正文零提及、导出零覆盖、`.scratch/t317/` 下也零留痕**（本席对 6 个 runId 在 `.scratch/t317/` 全目录做
   `Select-String`，六条**全部无命中**）。**本报告的问题主体就是这 6 条。**

> 判定为何是 S3 而不是 S2：证据件 `:59` 显式声明「**不使用任何 `--allow-*` 放宽**」，`:104`–`:106` 显式交代
> `land.ps1` 的失败重试与 `317-land` 拦停属窗外排除，`:62`–`:63` 显式交代收口席的运行「票号不同、时刻在窗口外，故不进本窗口的声明」。
> 也就是说：**证据没有隐瞒任何一条**，它是「声明了一窗、窗外的运行另起段落点名」；
> 而窗内 5 条在受跟踪源上 `matched=5/5 undeclared=0 gate-audit: PASS` 可复现。
> 缺的是「同票号 6 条绿色跑」的留痕，不是「说了没有的事」。故归 **措辞／留痕完整度（S3）**，不归「与事实不符（S2）」。

### 2.3 声明面

| 判据 | 结论 | 依据 |
| --- | --- | --- |
| `src/workout/commands.ts` 条数 vs 票面 | **成立** | 恰 **5 条**（`kind` 全 `read`、`shape` 全 `stat`）：`calorie.view.plan`／`plan-wizard`／`exercise-review`／`contraindication`／`process-progress`，与票面「场景 05 健身计划（32 词）」的键集一致 |
| `index.ts` 对外唯一门 | **成立** | `workout/index.ts` 导出行 `:20 export { WORKOUT_COMMANDS }`、`:27 runWorkoutView`、`:36 runWorkoutWrite`；`src/workout/` 共 8 件（`commands.ts`／`routes.ts`／`index.ts` ＋ 5 个子功能件），≤5 件约束按「子功能文件另计」的口径满足 |
| `keys.ts` 镜像 | **成立** | 5 键逐键命中一次：`:117`／`:118`／`:93`／`:83`／`:120`（dfe6288 的 `keys.ts` 未被本票触碰，`git rev-parse` 比对**未变**） |
| `combos.yaml` 镜像 | **成立** | 5 键逐键命中一次（`packages/base-combos/combos.yaml`，比对**未变**） |
| `registry.ts` 镜像 | **成立（口径为 0 命中是设计）** | `registry.ts` 逐键字面量 **0 命中**——它是生成物，按 `SOURCES` 汇总 `WORKOUT_COMMANDS` 而非逐键登记，故不以键字面量计；`git rev-parse` 比对**变了**（本票唯一变动的生成物） |
| 「生成物面只动 `registry.ts`」 | **成立** | 对落点父 `3845718` → `dfe6288` 逐件 `git rev-parse`：`keys.ts`／`combos.yaml`／`build-help.mjs`／`routes.generated.ts`／`SKILL.md` **全部未变**，仅 `registry.ts` **变了** |
| 落点改件面 14 件 | **成立** | `git diff --name-status 3845718 535a9b2` ＝ **14 件**（8 件新增 `src/workout/` ＋ 2 件搬空 ＋ `cmd_read.ts` ＋ `registry.ts` ＋ 证据 ＋ 对账导出），与票面 `:32` 自述逐字相符 |
| 收口提交只碰文档 | **成立** | `git diff --name-status 535a9b2 dfe6288` ＝ **4 件，全为 docs**（`t314-*` 与 `t317-*` 证据／对账源），未碰代码 |

### 2.4 分派面

| 判据 | 结论 | 依据（均在 `dfe6288` 口径复算） |
| --- | --- | --- |
| 场景 05 的键在 `src/workout/` 唯一实装 | **成立** | 5 键在 `src/workout/` 的命中集中在 `commands.ts`（`:21`–`:25` 声明行）与 `routes.ts`（37 条记录）＋各自子功能件的处理函数；`workout/index.ts` 五键字面量 **0**（只导出符号）。全树另见 `keys.ts`（登记面）／`routes.generated.ts`（生成物）／测试件，无第二个「能力实现地」 |
| `legacy/scene-05.ts` 片文件仍在 | **成立** | 文件存在，14 行，`export const LEGACY_SCENE_05: readonly LegacyCommandDecl[] = [];`，头部写「**已搬空**（#317）」，`index.ts` 仍 import 并展开（`:13`／`:27`／`:41`）——「只许搬空、不许删」的等号断言面完好 |
| 该键在片文件零残留 | **成立** | `legacy/scene-05.ts` 五键字面量 **0**；`legacy/routes/scene-05.ts` 五键字面量 **0** |
| `cmd_read.ts` 该场景 case 已净 | **成立** | `dfe6288` 的 `cmd_read.ts` 五键字面量 **0**（落点前 `3845718` 为 5）。引用侧：`src/workout/index.js` 进 `REGISTRY`，`case` 全删 |
| `write.ts` 已净 | **成立（本就无关）** | `dfe6288` 的 `write.ts` 与 `3845718` 逐字节同类度量一致（五键字面量 0／0），本场景无写键，`write.ts` 一行未动 |

### 2.5 `routes/scene-05.ts` 的「SOFT 相形态」到底是不是缺陷

**结论：不是交付缺陷，也不是门禁缺陷；是措辞／一致性缺陷（S3-②），但确实值得统一 —— 且统一成本极低。**

`dfe6288` 当刻两片的实测形态（本席 `git show` 逐行比对）：

```
routes/scene-01.ts   (12 行, 899 B)： #313 通用头 ＋ ` * #314 起本片记录已整组迁进它键所属的能力目录…` 迁移注记 ＋ `= [];`（单行）
routes/scene-09.ts   (12 行, 899 B)： 同上
routes/scene-04.ts   (11 行, 767 B)： #313 通用头，**无**迁移注记 ＋ `= [` 换行 `];`（两行）  ← 同刻的兄弟件
routes/scene-05.ts   (11 行, 767 B)： #313 通用头，**无**迁移注记 ＋ `= [` 换行 `];`（两行）  ← 本票
```

**为什么不是缺陷（机器证据）**：

1. **运行时零差异**：阵列空数组折成两行与单行是同一个 TS 值 `[]`。消费方 `scripts/gen-routes.mjs:3` 读的是
   **编译后**模块（`dist/cli/legacy/routes/scene-NN.js`），两形态都得到空数组 ⇒ 生成 0 条记录。无行为差异。
2. **无守卫读该形态**：`test/legacy-partition-313.test.mjs:148` 的等号断言扫的是 `SRC_LEGACY`
   ＝ `src/cli/legacy/*.ts`（**命令**分片，`sceneFiles.length === 10`），**根本不扫 `legacy/routes/` 子目录**；
   全仓 `grep` 也找不到任何断言路由分片的「片数／头部／数组折行」。故 SOFT 相**不可能**让门禁误判。
3. **本票没有引入家族不一致**：`dfe6288` 当刻与它**同批同形态**的兄弟件正是 `scene-04.ts`（同为 11 行 767 B 两行式）。
   换言之，#317 落点后「SOFT→HARD」的统一动作**还没有发生在任何一件上**，#317 没有违反当时的既成事实。
   （反证：#316 在 `a9fb35c` 才把 `scene-04.ts` 改成 5 行单行式 ＋ 注记 —— 可见「SOFT→HARD」另有其票，不是本票遗漏。）
4. **票面已自我披露**：票面 `:42`「待确认 4」明写「走 **SOFT 相**（只删记录行）：落点后文件头仍是 #313 通用头、
   空数组折成两行 `= [` ＋ `];`……若要统一成「通用头 ＋ `= [];`」，属后续措辞票」——陈述与实测**逐字相符**。

**为什么仍要统一（#320 点名有理）**：

- 生产头仍写「**未搬迁**的记录：键不属任何已搬迁能力件」，而本片**已搬空** —— 头部与实态自相矛盾，
  且缺 #314 那条迁移注记。后来者用「头部文字」取代「读代码」判断进度时会读到反话。
- 同族两种形态并存（`scene-01`／`09` 单行 ＋ 注记 vs `scene-04`／`05` 两行 ＋ 无注记），
  形态本身就是可 grep 的进度信号；信号不齐会拖慢后续窗口的判读。
- **处置建议**：归 `#320` 收口票一次性统一即可（对齐 `scene-01`／`09`），**不构成本票返工理由**。
  若要本票返工，只需把该文件头部补一条 `#317 已搬空` 注记并将空数组并成单行，属纯文档改动、零风险。

### 2.6 票面纪律

| 判据 | 结论 | 依据 |
| --- | --- | --- |
| `## 进度：95%` | **成立** | `317-body.md:30` 逐字为 `## 进度：95%` |
| 未 CLOSED | **成立** | 复核当刻票面未关；正文以「待确认」条与「收口票 #320」形式挂账，未宣称完成 |
| 正文无字面 `\n` | **成立** | `317-body.md` 的 `\n` 字面量计数 **= 0** |
| 正文无 BOM | **成立** | 首三字节 `50 61 72`（`Par`t of #293），`hasBOM=False` |
| 真换行 | **成立** | CRLF=0、LF=43（纯 LF） |
| 证据件编码 | **成立** | `t317-搬迁-证据.md` 首三字节 `23 20 23`（`# #3`），`hasBOM=False`，CRLF=0、LF=124 |
| 证据件 2 处字面 `\n` | **成立（非事故）** | 出现在 `:17`／`:105`，两处都是**代码跨度内的 node 行数口径** `split('\n').length - 1`，属正当引用，不是换行事故 |

---

## §3 原始证据（命令 ＋ 输出摘录，含行号）

以下均为本席在 better sidebar 终端 `t317r v2` 内实际执行；全量输出落在 `.scratch/t317r/50-*.txt`／`51-*.txt`／`52-*.txt`。

1. 证据面全量复跑（前席脚本，本席当刻重算）→ `.scratch/t317r/50-refresh-evidence-face.txt`
   ```
   node .scratch/t317r/evidence-face.mjs
   EXIT=0   行数 90
   ```
   要点：`GATE-RUN 行数=5`；`:53`–`:57` `endsAtRightQuote=true` 全真；`parseClaims=5；带 runId=5`；
   `GATE-RELAX 开关集合=[]`；`条目数=29`；`[a] matched=5/5 undeclared=0`；`[b] matched=5/5 undeclared=13`；`[c] matched=5/5 undeclared=0`。

2. 本席独立复核（自带正则 ＋ 双解析器比对）→ `.scratch/t317r/51-verify-v2.txt`
   ```
   node .scratch/t317r/verify-v2.mjs
   EXIT=0   行数 117
   ```
   要点：`V1 结论：endsAtRightQuote 全真=true`；`V2 parseRelaxations=[]`；
   `V3 证据引用(去重)=15 / 受跟踪导出覆盖=5 / 证据引用但导出未覆盖=10`（10 条逐条给出 `ticket`／`at`／`exit`／`cmd`／`窗口内=false`）；
   `V4 逐字存在=true` × 5；`V5 条目数(独立解析，主锁+land锁)=37`；`V5b 一致=true`、`runId 集合一致=true`；
   `V6[a] matched=5/5 undeclared=0`、`[b] undeclared=13`、`[c] matched=5/5 undeclared=0`；
   `V7 该窗口条目数=8`，其中 **6 条 `ticket=317` 的 `证据正文提及=false 受跟踪导出含=false`**。

3. 跨提交行数／形态／改件面 → `.scratch/t317r/52-pins.txt`
   ```
   node .scratch/t317r/pins.mjs
   EXIT=0   行数 80
   ```
   要点（节点口径 `split("\n").length-1`）：
   ```
   cmd_read.ts        dfe6288=857  HEAD=799  worktree=799     ← 证据「896 → 857」在 dfe6288 口径成立
   legacy/routes/scene-05.ts  dfe6288=11  HEAD=11  worktree=11 ← SOFT 相形态复核当刻如此
   legacy/routes/scene-04.ts  dfe6288=37  HEAD=11  worktree=11 ← 同刻仍是「有记录」的兄弟件
   legacy/routes/scene-01.ts  dfe6288=12  HEAD=12  worktree=12 ← HARD 相（单行＋注记）
   ```
   ```
   535a9b2（落点）相对其父 3845718：件数=14（8 件位 workout/ ＋ 2 片搬空 ＋ cmd_read ＋ registry ＋ 证据 ＋ 对账导出）
   dfe6288 相对 535a9b2：件数=4（全为 docs，无代码）
   生成物面：keys.ts / combos.yaml / build-help.mjs / routes.generated.ts / SKILL.md = 未变；registry.ts = 变了
   ```

4. 声明面逐键镜像（worktree，当刻）：
   ```
   calorie.view.plan                keys=2 combos=2 registry=0   keys.ts:117
   calorie.view.plan-wizard         keys=1 combos=1 registry=0   keys.ts:118
   calorie.view.exercise-review     keys=1 combos=1 registry=0   keys.ts:93
   calorie.view.contraindication    keys=1 combos=1 registry=0   keys.ts:83
   calorie.view.process-progress    keys=1 combos=1 registry=0   keys.ts:120
   ```
   （`calorie.view.plan` 的 `keys=2` 是其为 `calorie.view.plan-wizard` 的字面前缀所致的子串计数，非重复登记。）

5. `src/workout/commands.ts`（`git show dfe6288:…`，全文 26 行）：5 条声明在 `:21`–`:25`，
   `kind` 全 `read`、`shape` 全 `stat`；`wakeWord` 仅两条（`计划复盘（本周）`／`看落地训练进度`），与票面「代表唤醒词」口径相符；
   `example` 字段无字面 `\n`。

6. 分派面残留计数（worktree，当刻）：`cmd_read.ts` 五键字面量 0；`write.ts` 五键字面量 0；
   两片 `legacy/scene-05.ts`／`legacy/routes/scene-05.ts` 五键字面量 0。

---

## §4 三级问题清单

### S1（交付缺陷）—— 0 条

### S2（证据与事实不符）—— 0 条

无「声明了而实测不成立」的陈述：三条完成判据、生成物面、改件面、行数面、`kind`／`shape` 面逐条复核均成立。
本席特别核对了两处最容易失真的自述，均与实测相符：①「生成物面只动 `registry.ts`」（逐件 `git rev-parse` 证实）；
②「行数 896 → 857」（在 `dfe6288` 口径复算 = 857，且 `FROZEN_READ_LINES = 1146` 上限成立）。

### S3（措辞口径）—— 3 条

1. **留痕完整度**：受跟踪对账源 `t317-gate-runs.log` 只覆盖 15 条被引 runId 中的 5 条；
   其中 **6 条 `ticket=317` 绿色收口轮**（`13:30:13`–`13:31:28`）在证据正文、导出件、`.scratch/t317/` 三处**全部零留痕**。
   另 8 条 `317-rescue` 已在正文 `:63`／`:80`–`:83` 点名并有实测抄录，属「已声明、未导出」。
   *整改建议（不改判定）*：把该轮并入 ④ 段声明，或重导一次覆盖 `12:55–13:35Z` 的窗口。
2. **`routes/scene-05.ts` SOFT 相形态**：内容面零偏差（37 条记录集合与旧片完全一致、片内 0 条外来记录），
   但头部未统一成「迁移注记 ＋ 单行空数组」，与 `scene-01`／`scene-09` 不同族；归 `#320` 收口（§2.5）。
3. **票面「要做的事 第 2/4 条」与派单冲突**：以票面「待确认 1／2」挂账、未在票面正文改写。
   第 4 条（补写命令两处）本票确无对象（5 键全读、无写键），第 2 条（把 `render/planPlate.ts` 等搬进来）与派单铁律一冲突，
   处置为「只经公开接口调用、视图层留原处」，并将实现层部分转 `#324`。处置**正当**，但票面正文与正文自述仍存在一处未对齐的读法歧义。

---

## §5 动态项（门槛；未跑则标「待补」）

| 项 | 期望 | 实测 | 状态 |
| --- | --- | --- | --- |
| 全量 `pnpm test` | `1492/1492` exit 0 | **红：`tests 1436 / pass 1371 / fail 65 / skipped 0`，exit=1**（runId `117929c2-b9df-4519-96b9-f3007c9f6612`） | **在途污染，不能归本票**（见下） |
| `pnpm gen:check` | PASS（键 101；能力 5 声明 36；未搬迁 65） | 未跑 | **待补**（工作树持续被在途票改写，跑出的数不可归本票） |
| `t293` 探针 P1..P5 ＋ `UNACCOUNTED=0` | 5/5 ＋ 0 | 未跑 | **待补**（同上） |

### 动态项的污染归因（关键，不可省）

本次 `pnpm test` **不是**在复核对象 `dfe6288` 上跑的，树在跑动期间被并发票改写：

- 起跑时 HEAD ＝ `9140f39`（`#316`／`#316r`／`#323r` 已落），工作树干净；
- 跑动期间先是 `#319` 的 `src/analysis/**` 改动落盘，随后**新出现 11 个未跟踪的 `src/diet/**` 文件**（`#319` 的饮食模块搬迁在途）；
- 跑完后 `git status --porcelain -uall` ＝ 17 条，其中含 `?? packages/skill-calorie/src/diet/*.ts` 11 件 ＋ `M cmd_read.ts`／`M registry.ts`／`M write.ts`／`M legacy/scene-02.ts`／`M legacy/routes/scene-02,10.ts`。

**为什么不归本票（两条机器证据）**：

1. **失败根因指名的是饮食键，不是本票五键**。代表性命中：
   `AssertionError: 冻结集里的命令既不在未搬迁清单、也不在能力目录（被静默删掉了一条命令）：calorie.diet.add、calorie.diet.batch、calorie.diet.copy、…、calorie.today、calorie.product.add、calorie.view.dedupe、calorie.view.diet-review、calorie.view.library、calorie.view.ranking、calorie.view.today-water、calorie.water.log`（共 24 键，**全部属饮食场景**）。
   机理与在途状态吻合：`src/diet/**` 已出现在源树，但生成物面（`registry.ts` 在途改动）**尚未收录**它 ⇒ 这些键「既不在未搬迁清单、也不在能力目录」⇒ 该守门按设计变红。
2. **本票五键只作为「不变的上下文」出现，不在任何 `+` 差分里**。
   在键表断言的实际/期望差分中（`:1876`–`:1923`），`+` 行全是**新增的饮食键**（`calorie.view.dedupe`／`diet-review`／`library`／`nutrition-detail`／`nutrition-ratio`／`ranking`／`search`／`source-stats`／`today-water`／`calorie.water.log`），
   而 `calorie.view.plan`（`:1902`）／`plan-wizard`（`:1903`）／`process-progress`（`:1905`）／`contraindication`（`:1882`）／`exercise-review`（`:1952`）**均落在两侧一致的上下文行**上；
   关键词计数 `workout = 0` 命中、`diet = 214` 命中。
3. **本票面完全未被触碰**：跑后 `git status --porcelain -uall -- packages/skill-calorie/src/workout docs/skills/skill-calorie/t317-*` 返回**空**。

⇒ 该红是并发票的在途中间态产物；**对本票既不加分也不扣分**，票面自述的 `1492/1492` 在本席窗口内**既未证实也未证伪**（待补）。
**动态门槛本次判定：未达成（未能复现）**；本报告的 PASS 建立在静态判据与证据面复核之上，并已在 §1 注明「门槛判红则自动降 FAIL」——本处红因归因于他票，故**不触发降级**。

> 复现提示（供后续席位）：要在 `dfe6288` 上严格复现 `1492/1492`，需要一棵只含该提交的工作树。
> 本席**未**建旁路工作树：`git worktree` 会在仓库外写盘（违反「别在仓库根留写盘产物」的同族约束），
> 且新工作树无 `node_modules` 而**装包被明令禁止**，故无法在允许的约束内取得干净运行环境。

**静态项门槛内已实测成立的部分**（不需跑测试即可判）：
`keys.ts`／`combos.yaml`／`build-help.mjs`／`routes.generated.ts`／`SKILL.md` 五件的 `git rev-parse` 在
`3845718` → `dfe6288` 之间**完全未变**，即「改生成物必须同时改声明源」这一条在静态面已守住；
`legacy/scene-05.ts` 与 `legacy/routes/scene-05.ts` 的片文件与空数组形态完好，等号断言面未被破坏。

> **动态运行时的树口径提示**：本席启动动态门禁时工作树已转净（`git status --porcelain -uall` 返回 0 条），
> HEAD 为 `9140f39`。故本次动态结果反映的是「`dfe6288` 之后又落了两票（`#316`／`#316r`／`#323r`）＋ 工作树干净」
> 的树，**不是** `dfe6288` 本身的树。若要严格复现票面自述的 `1492/1492`，须在 `dfe6288` 上跑（本席未做，见 §6）。

---

## §6 复核基准漂移声明（必须随判定一起读）

1. 复核开始当刻 HEAD ＝ `a9fb35c`（`#316` 场景 04 落点）＋工作树带 `#319` 在途改动；
   复核结束当刻 HEAD ＝ `9140f39`（`#316r` 复核报告落库），工作树转净。**对账对象 `dfe6288` 是 HEAD 的祖先**，未被改写。
2. 由此产生的三处「看似不一致、实为时间差」的读数，已在本报告内逐处标注，勿按缺陷计：
   - `cmd_read.ts`：`dfe6288` = 857 行、HEAD = 799 行（`#316` 搬走场景 04 的 9 个 `case` 所致）。证据件写的 857 **对 dfe6288 成立**。
   - `legacy/routes/scene-04.ts`：`dfe6288` = 37 行、HEAD = 11 行（同上，#316 才转成 HARD 相）。
   - `workout/` 与两片 `scene-05.ts`：`dfe6288` → `HEAD` → worktree **三处完全一致**，本票面**零漂移**。
3. `keys.ts`／`combos.yaml`／`build-help.mjs`／`routes.generated.ts`／`SKILL.md` 在 `dfe6288` 之后**未被本票触碰**，
   但**可能被别票（`#316` 等）触碰**；本报告对它们的「未变」断言**严格限定在 `3845718 → dfe6288` 区间**，不越界外推。

---

## §7 必须整改项（≤3，按优先级）

1. **重导一次覆盖收口轮的受跟踪对账源**（不改代码，只改证据／对账两件）：
   把 `13:30:13`–`13:31:28` 那 6 条 `ticket=317` 绿色收口轮并进 ④ 段声明，
   或以 `--since 2026-09-13T12:55:00Z --until 2026-09-13T13:35:00Z` 重导 `t317-gate-runs.log`，
   使「同票号闭环」在受跟踪面上自洽。**验收**：对账导出的窗口解析后，`ticket=317*` 的绿色跑条目**全部**出现在受跟踪件内。
2. **把 `routes/scene-05.ts` 统一成 HARD 相**（归 `#320`，或本票顺手补 6 行）：
   对齐 `scene-01`／`scene-09`（补 `#317 已搬空` 迁移注记 ＋ 空数组并成 `= [];` 单行）。**零风险**：无守卫读该形态（§2.5）。
3. **把票面「待确认 1／2」回写进正文对应条**（票面措辞）：让第 2／4 条的「无对象／转 `#324`」在正文处即可读到，
   避免后续窗口按未改写的正文重现「该搬未搬」的误判。

---

## §8 结论

`#317` 的交付是**实的**：5 键在 `src/workout/` 唯一实装、两片旧分片搬空且零外来残留、生成物面只动生成物 `registry.ts`、
落点 14 件与票面自述逐字相符、内容面 37 条记录与旧片完全一致、证据件与票面纪律（95%、未 CLOSED、无 BOM、真换行）全部成立。
三条前席回报经本席独立复算后**全部成立**，但其中两条的**措辞需要纠正**（「29 条」应记 37 条；
「10 条没有受跟踪对账源」应表述为「10 条未进受跟踪导出件，其中 6 条连正文也零提及」），
按事实本身尚不构成「证据与事实不符」。

**判定 PASS，93 / 100**（S1=0、S2=0；S3=3）。

---

## §9 落库记录（收尾）

| 项 | 值 |
| --- | --- |
| 入库件 | `docs/skills/skill-calorie/t317r-复核报告.md` |
| commit | `407bef0`（`docs(317r): 对抗式复核报告 —— 场景05 搬迁（判定 PASS/93 分）`） |
| commit 面 | `1 file changed, 338 insertions(+)` —— **只点名本件**，未用 `git add -A` |
| push | 首推 `57306cc` → 补 §9 后 `--amend --only <本件>` 得 `47d21a6` → 再补本行得 `407bef0`；三次均**只点名本件**，末次以 `--force-with-lease` 推成；`HEAD ＝ origin/master ＝ 407bef0` |
| 落库前树态 | `git status --porcelain -uall` ＝ **0 条**（干净）；落库时 HEAD ＝ `f1b0e60`（`#315` 已落） |
| 锁 | `LOCK-ACQUIRED … ticket=317r runId=d73bfe57 … exit=0`（`--lock-dir .scratch/locks-land`） |
| 编码 | 约 28.7 KB、首三字节 `23 20 23`（无 BOM）、CRLF **0**、纯 LF |
| 覆盖保护 | 落库脚本先 `git show HEAD:<目标件>` 预检，目标件不存在才写；存在且内容不同即 `ABORT`（防覆盖他人成果） |
| 仓库根写盘产物 | **无**：全部中间件落 `.scratch/t317r/`（`50-*`／`51-*`／`52-*`／`60-*`／`61-*` 与三个 `.mjs`） |

本席未改 `#317` 票面、未关票、未替它改代码（仅新增本复核报告一件）。
