# t391 占词证据（场景01-#373遗留help占词）

- 票：#391（地图 #161 子票，母票 #373）；执行席已认领（gh assignee FeatherHunter，加引号复跑 exit 0）。
- 落法终裁：缺词（删 `commands.ts:429` 的 `wakeWord:'看今日主页'`，example q 保留；`routes.ts:178` 零改）。理由见 §0。
- 基线：HEAD `git diff` 前 commands 含占词行；锁等待见各 GATE-RUN waitedMs（build1 等他席 574 号 10s，余 0–20s）。
- 结论先行：本票可关（判据①②③持锁绿＋变异红绿双行；④直接相关绿、三门红归他席漂移零新增，见 §4）。

## §0 选词裁决（编排者复核通过，终裁缺词）

- 候选查唤醒词不可用三条机器事实：
  1. 不在 436 词表（WAKE_ASSETS 436 条无此词；TRIGGERS 同无；`t367-唤醒词门.test.mjs:165` 要求一切已写 wakeWord 必须在表内，必红）；
  2. 现找 0 命中（`searchHelp(TRIGGERS,'查唤醒词')` HITS:0，示例不可执行，`check-examples` 红）；
  3. 若用它须改测试登记表而超写集（写集只许两处源码＋生成物＋证据草稿日志）。
- 备选查档案被编排者实测否决：它是场景07 `calorie.view.profile` 的触发词（`profile/routes.ts:12` order:236，`scene-07-profile.ts:8`，generated:248），用户念查档案跑档案页；若 help.lookup 声明它，速查写 help.lookup、触发跑 profile——与本票要消的同词两去向同类错；且 `profile/commands.ts:19` 的 view.profile 本就没有 wakeWord（缺词退回键名，#323 裁定），速查今日无查档案行，一声明反新造一行错位。
- 终裁缺词（删 wakeWord）：`t367:21` 明写没写代表唤醒词的声明不算错（缺词速查退回键名），删词即消占词行且天然过 `t367:165`；零占词零误导。
- 落法：`commands.ts:429` 删 `wakeWord:'看今日主页'` 一段（仿同件 predict／anomaly 缺词先例），example 的 q 保留 `看今日主页`（能跑的中性演示：查什么回什么，占词的是首列代表词已删，q 列不计占词；备选换中性词不取，最小改动）；`routes.ts:178` 触发词不动、cli 的 q 亦保留 `看今日主页`（两处 q 同一，触发面与声明面两面，编排者指使；故本票源码仅改 commands 一行，routes 零改）。

## §1 全仓验撞（改前机器事实）

- `wakeWord:'查唤醒词'` exact：仅 `analysis/routes.ts:178`（new 声明）＋`routes.generated.ts:471`（生成物）；零 `commands.ts`（故候选初判不撞命令，但 §0 否决其表外＋零命中）。
- `wakeWord:'查档案'` exact：零 `commands.ts`（全仓）；但触发面 `profile/routes.ts:12`＋`scene-07-profile.ts:8`＋generated:248 已有同词去档案页，编排者否决（同词两去向）。
- `wakeWord:'复盘'` exact：零 `commands.ts`（备选未取；`searchHelp` HITS:27 太散，`查档案` HITS:1 更精确，但同被否决逻辑弃）。
- `看今日主页`：改前 `commands.ts` 仅两处——`home/commands.ts:22`（view.home，保留）与 `analysis/commands.ts:429`（help.lookup，占词，删除）；`searchHelp(TRIGGERS,'查唤醒词')` HITS:0，`('查档案')` HITS:1，`('复盘')` HITS:27（无锁检索，仅定选词用，不计门禁）。

## §2 改动（只许写集，实况仅改一行）

- `packages/skill-calorie/src/analysis/commands.ts:429`：删 `wakeWord:'看今日主页'`（example q 保留 `看今日主页`，中性演示，见 §0）。
- `packages/skill-calorie/src/analysis/routes.ts:178`：零改（触发词 `查唤醒词` 不动，cli q `看今日主页` 与 example 同一；编排者指使两面分开）。
- 生成物由 `pnpm gen` 重出，不手改；`pnpm help:build` 重出 SKILL.md 速查块（help.lookup 首列退回键名）。

## §3 生成链（持锁一条一条跑，全 exit 0）

- GATE-RUN runId=`d9c06136-a5ce-4839-b9f4-711c412f1295` cmd=`pnpm build`（初编声明；等他席 574 waitedMs=10001）。
- GATE-RUN runId=`a13aefb3-2df9-4a54-92e9-1ccfeb4b3f4e` cmd=`pnpm gen`（键 129；waitedMs=20001）。
- GATE-RUN runId=`be1b3652-ac61-4167-9485-239fc31056f5` cmd=`pnpm build`（复编生成物进 dist）。
- GATE-RUN runId=`61a909d1-c741-4732-a4da-f42e0cf9b2be` cmd=`pnpm help:build`（速查退回键名；MISSING-MISMATCH 系他席 workout 首词差异，exit 0）。
- GATE-RUN runId=`c59ab400-dfc2-4be5-9989-d9b7b27d9aa4` cmd=`node packages/skill-calorie/scripts/gen-cli.mjs --check`（5×GEN-CHECK ok＋PASS 129 键）。
- 生成物实况：`build-help.mjs` 删 REPR 一行（help.lookup，无他 hunk，可提交）；`SKILL.md` 速查 help.lookup 首列退回键名＋12→13 条说明（另混他席 goal.exercise 行／128→129／diet-review entry 三 hunk，不提交，见 §8）；`routes.generated.ts` 仅他席 diet 看今日营养 hunk（本票零改，不提交）。

## §4 四条判据读数（缺词口径：首列占词消、q 列不计占词）

- ① 速查 AUTO 块（`SKILL.md:82–298`）：GATE-RUN runId=`6dd5c2c5-3682-4808-82bd-864baf59b60f` `RESULT: 1/1 SUKUAI-PASS`（`看今日主页`首列=1、`calorie.help.lookup`首列=1、`查唤醒词`/`查档案`首列=0、HELP-LOOKUP-ROWS=1 键|键）；补 GATE-RUN runId=`29c77caa-39d9-4de3-93cd-9f18d714a496` `RESULT: 1/1 KAN-SCENE01-PASS`（仅剩行 key=`calorie.view.home`）。初版全文件口径红（首列=2，含块外场景对照表，runId=`6b12b195` exit=1，已 supersede，口径收窄后绿）。
- ② gen:check 全绿：runId=`c59ab400` exit=0（见 §3）。
- ③ 真出口（缺词版：无新代表词可查，证示例可跑＋旧词仍归场景01）：GATE-RUN runId=`333aaa5c-1d05-4357-90de-f1e5676a2fe9` `RESULT: 1/1 LOOKUP-PASS`（`dispatch help.lookup q=看今日主页` total=1 firstWake=`看今日主页` firstKey=`home_today_overview`，仓外 tmp DB）。
- ④ 包内相关测试（失真断言同步：无，本票 searchHelp/TRIGGERS 未动，help 断言口径不变；三红＋一红全归他席漂移，本票零新增）：
  - `t367` runId=`c4339fcf` exit=1：唯一违规 `goal calorie.goal.exercise→定运动目标`（`test/t367:211`；goal 写集外；本票 analysis 零违规，help.lookup 缺词未报）。
  - `cmd-registry-294` runId=`95b43a40` exit=1：`#320` 分派层残留 `calorie.view.composition-wizard`、`calorie.view.measure-wizard`（body 写集外；本票 analysis 未动分派层）。
  - `check-examples` runId=`5dea1802` exit=1：`RESULT: 126/129`，红3 `calorie.history`（空库 exit4）＋两 body-compare（占位日期缺数 exit4）；红单无 help.lookup（本票示例通过）；纪律形状一明写该门带着票外既有红。
  - `calorie-c43` runId=`bc0ef4a0` exit=1：红仅 C6 写收据结构化（diet 写集外）；C3 `看今日主页→view.home` 通过（与 §4③同源）。

## §5 变异两行（单锁窗口 `.scratch/t391/mutate.mjs`，直调无嵌套锁）

- GATE-RUN runId=`c8e8f2aa-2c55-4760-ad08-c85c7c0fd59d` cmd=`node .scratch/t391/mutate.mjs` exit=0。
- 红：加回 `wakeWord:'看今日主页'` → 全链后 `COUNT 看今日主页首列=2`、`RESULT: 0/1 SUKUAI-FAIL`、`MUTATE-VERIFY RED-OK 两行复现`。
- 绿：`finally` 还原缺词（改完态，非 HEAD）→ 全链后 `COUNT=1`、`RESULT: 1/1 SUKUAI-PASS`、`RESTORE-VERIFY GREEN-OK`、`RESULT: 2/2 MUTATE-RED-RESTORE-GREEN`。
- 还原口径：变异释放锁前复原到改后缺词态；生成物连带重编＋重签（脚本内 build/gen/build/help:build 两轮），工作区无残留（`git diff` 仅本票行，见 §7）。

## §6 持锁运行记录（GATE-RUN 边跑边补，对账源 `.scratch/locks/gate-runs.log`，导出见 §8）

- GATE-RUN runId=`d9c06136-a5ce-4839-b9f4-711c412f1295` cmd=`pnpm build` exit=0。
- GATE-RUN runId=`a13aefb3-2df9-4a54-92e9-1ccfeb4b3f4e` cmd=`pnpm gen` exit=0。
- GATE-RUN runId=`be1b3652-ac61-4167-9485-239fc31056f5` cmd=`pnpm build` exit=0。
- GATE-RUN runId=`61a909d1-c741-4732-a4da-f42e0cf9b2be` cmd=`pnpm help:build` exit=0。
- GATE-RUN runId=`c59ab400-dfc2-4be5-9989-d9b7b27d9aa4` cmd=`gen-cli.mjs --check` exit=0。
- GATE-RUN runId=`6dd5c2c5-3682-4808-82bd-864baf59b60f` cmd=`verify-sukuai.mjs` exit=0（①主）。
- GATE-RUN runId=`29c77caa-39d9-4de3-93cd-9f18d714a496` cmd=`verify-kan.mjs` exit=0（①补）。
- GATE-RUN runId=`333aaa5c-1d05-4357-90de-f1e5676a2fe9` cmd=`verify-lookup.mjs` exit=0（③）。
- GATE-RUN runId=`c4339fcf-c4e4-4a26-96fe-9950a7f434f9` cmd=`t367` exit=1（他席 goal 行，§4）。
- GATE-RUN runId=`95b43a40-4ed0-4602-807d-9431b44b3f8d` cmd=`cmd-registry-294` exit=1（他席 body 分派，§4）。
- GATE-RUN runId=`5dea1802-f801-47b1-af47-4653df18c3e0` cmd=`check-examples.mjs` exit=1（票外既有 126/129，§4）。
- GATE-RUN runId=`bc0ef4a0-b14a-4947-856c-e0959dd1670f` cmd=`calorie-c43` exit=1（他席 diet C6，C3 通过，§4）。
- GATE-RUN runId=`c8e8f2aa-2c55-4760-ad08-c85c7c0fd59d` cmd=`mutate.mjs` exit=0（§5两行）。
- 初版 sukuai runId=`6b12b195` exit=1 系全文件口径误计块外表，已 supersede（见 §4①），不计门禁。

## §7 提交 sha 清单（只列本票增量提交，主干只许追加，未 push 由编排者收口）

- `f9ce0d53` feat(391): 删analysis help.lookup占词wakeWord看今日主页＋重出REPR速查退回键名（2 件：`src/analysis/commands.ts`＋`scripts/build-help.mjs`；GATE-RUN runId=`05b68d13-a9ab-4ef3-815a-b4750d5bc7ad` exit=0；提交前 `git diff --cached --name-only` 仅本票两件）。
- 本件证据提交待补（下一条增量提交）。

## §8 未做项与下一手（关票由编排者收口；不 comment、不 close）

- 未提交（混他席 hunk，留收口）：`SKILL.md`（本票速查退回键名 hunk＋他席 goal.exercise／128→129／diet-review entry 三 hunk）、`routes.generated.ts`（仅他席 diet 看今日营养 hunk，本票零改）。收口时等他席落点后重跑 `pnpm build→gen→build→help:build` 收敛。
- 未转绿（他席漂移，红归他席记账）：t367 goal 行（`goal/` 登记表待归属票）、294 body 向导分派（`body/` 待归属票）、examples 126/129（history 空库＋两 body-compare 占位日期，种子／归属票）、c43 C6（diet 收据，归属票）。本票 analysis 零新增，落点后复跑即应绿（首选）；实在等不到按 #383 先例具名记账。
- 对账导出：锁目录未受版本控制，本次窗口运行条目已逐条抄录本 §6（`GATE-RUN runId=… cmd=…`），明细日志在仓外 `.scratch/t391/logs/`（build1/gen1/build2/helpbuild/gencheck/sukuai2/kan/lookup/t367/t294/examples/c43/mutate，落盘未入仓，见协议 §2.3.4 复核口径）。
- 用词：命令／跑通／结果型 HTML（本票查询命令＋结果型 HTML；无会改数据库的命令；`wizard` 未出现）。
- 结构第四步：本票源码仅改声明一行，未超告警线；`analysis/commands.ts` 台账 443 行超线系 #376～#378 挂号存量，本次先不拆（拆法见台账）。
