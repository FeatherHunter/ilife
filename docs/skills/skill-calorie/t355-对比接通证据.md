# t355 两条对比词接通 · 证据（MAP #158 第2波）

- 票：#355（父图 #158；设计 t169 票3；派单 t355-对比词接通）
- 目标：对比体脂／对比围度从 non-exec 接成 exec，13 词全可执行。
- 写集（实测）：`src/body/compare.ts`（新建 290 行节点口径）＋`src/body/commands.ts`（＋2 声明）＋`src/body/routes.ts`（＋order246/247 exec）＋`src/fetch/body.ts`（＋`avgCompositionInRange` 21 行）＋`src/cli/legacy/routes/scene-08.ts`（清两条 non-exec 留空片）＋本证据＋`.scratch/t355/`草稿。`src/body/index.ts` 零改（`runBodyView` 经 `BODY_COMMANDS` 自动承接，无需加导出）。
- 禁区：`AGENTS.md`/检查脚本、`SKILL.md`/HELP、生成物手改（未碰）、`bodyDocs`/`wizardPlate`（未碰）、他票件（未碰）。

## §1 基线（开工前后）

- 13 唤醒词名逐字未动（`scene-08-body.ts` 未碰）。
- 11 条已有命令的命令名与示例逐字未动（`git diff` 仅追加 2 行声明，旧 8 行零改）。
- 共用位零手改（`keys.ts`/`registry.ts`/`routes.generated.ts`/`combos.yaml`/`build-help.mjs` 未碰；新键待 `pnpm gen` 派生）。
- 真库未写：本票验证一律 tmp 库（`mkdtemp`），真库无转储变化（无写命令，无落盘真库路径）。

## §2 实现

- 新键：`calorie.view.body-composition-compare`（题 体脂对比，唤醒词 对比体脂）＋`calorie.view.body-measure-compare`（题 围度对比，唤醒词 对比围度），均为 `kind read / shape stat`。
- 示例（照抄可跑，种子日期）：体脂 `--params '{"period1Start":"2026-09-05","period1End":"2026-09-05","period2Start":"2026-09-07","period2End":"2026-09-07"}'`；围度 `--params '{"date1":"2026-09-05","date2":"2026-09-07"}'`。体脂另收 `date1/date2` 单日别名、`source?`；围度日期含相对词（今日／昨日／前天）与 `offset`（与 `view.ts` 同口径）。
- 取数：体脂两段各调一次 `avgCompositionInRange`（与 `compareCompositions` 同表同 `AVG/MIN/COUNT` 口径，闭区间版）；围度复用 `bodyPlate.buildBodyMeasureCompare`（即 `compareMeasurements` 单日快照）。变化率本件算：`rate = delta/before*100` 保留 2 位，`before=0` 即空。
- 整页：`assembleDocPage`（与 `bodyDocs` 同头 `0.1.0/calorie/卡路里·运动身体`）；体脂 KPI＋两行表（均值/条数/差值/变化率）；围度 KPI＋13 行表（部位/前/后/差值/变化率，缺数即 `—`，列序以 `MEASUREMENT_FIELDS` 为准）。
- 行数（节点口径）：`compare.ts` 290、`fetch/body.ts` 259、`commands.ts` 32、`routes.ts` 28——均未超 350 告警线。导出：`compare.ts` 对外 4 函数（2 视图＋2 文档），门（`body/index.ts`）对外仍 3 件。

## §3 手算期望表（来源：冻结种子手算，非新实现输出）

种子（复用 `sport-homogeneity-109` 两日＋围度补齐 13 项）：体脂 `gym 09-05 19.5 / 09-07 19.2`；围度见下表（前＝09-05，后＝09-07）。

体脂：前均值 19.5（n=1），后均值 19.2（n=1），差值 -0.3，变化率 -0.3/19.5*100 = -1.538…% → **-1.54%**。

围度 13 项（差值＝后-前，变化率＝差值/前*100 保留 2 位）：

| 部位 | 前 | 后 | 差值 | 变化率 |
|---|---|---|---|---|
| 胸围 | 95 | 94.5 | -0.5 | -0.53% |
| 腰围 | 80 | 79 | -1 | -1.25% |
| 腹围 | 82 | 81 | -1 | -1.22% |
| 臀围 | 96 | 95.5 | -0.5 | -0.52% |
| 肩宽 | 110 | 109 | -1 | -0.91% |
| 左大腿 | 55 | 54 | -1 | -1.82% |
| 右大腿 | 55 | 54.5 | -0.5 | -0.91% |
| 左小腿 | 36 | 35.5 | -0.5 | -1.39% |
| 右小腿 | 36 | 36 | 0 | 0% |
| 左上臂 | 30 | 29.5 | -0.5 | -1.67% |
| 右上臂 | 30 | 30 | 0 | 0% |
| 左前臂 | 26 | 25.5 | -0.5 | -1.92% |
| 右前臂 | 26 | 26 | 0 | 0% |

可比 13 项，非零 10 项。

- 注（种子分层）：上表 13 项里**只有胸／腰／腹／臀 4 项来自冻结件**（`packages/skill-calorie/test/sport-homogeneity-109.test.mjs:67` 的 `INSERT INTO body_measurements` 两行：`95/80/82/96`、`94.5/79/81/95.5`）；另 9 项（肩宽＋左右大腿／小腿／上臂／前臂）冻结件里没有，是**作者按同表口径自补的种子**（即本节首句的「围度补齐 13 项」）。这与 Issue ⑥ 的「来源＝老技能实现导出的手算表」**不是同一件事**：⑥ 说的是对照基线出自老技能实现，与本表种子的分层无关。
- 注（行序）：本表行序是手算表自己的写法（胸／腰／腹／臀／肩 打头）；**页面行序由 `MEASUREMENT_FIELDS` 决定，`shoulder_cm`（肩宽）在末位**（`src/fetch/body.ts:25-32`），故本表第 5 行「肩宽」在页面里落在最后一行。§2 那句「列序以 `MEASUREMENT_FIELDS` 为准」说的是页面部位序（＝字段序），不是本表行序；§4 的 13 行对账是**逐值**比对（部位名↔值），不受行序影响。

## §4 真跑读数（函数级：直调 dist，绕开红树 registry/CLI）

跑：`node .scratch/t355/verify-compare.mjs`（tmp 库＋上表种子；示例参数逐字同声明）。

- 体脂前均值 19.5 OK／后均值 19.2 OK／差值 -0.3 OK／变化率 -1.54 OK／别名差值 -0.3 OK。
- 围度可比 13 OK／非零 10 OK／13 中文名逐个 OK／含变化率 OK／腰围 -1.25% OK／差值≠0 OK。
- 整页：体脂 `doctype OK／charset OK／style OK／≥2KB(57733) OK／无外链 OK／无src OK`；围度 `同 OK／60207 OK`。落盘 tmp 两件可读。
- 结论：`RESULT: PASS`（`exit 0`；详见 `.scratch/t355/verify-compare.mjs` 输出）。

## §5 13 词映射（源码级，生成物待 `gen`）

- `src/body/routes.ts`：`order 237–249` 13 条全 `exec`（237–245 旧 9 ＋ **246/247 本票 2** ＋ 248–249 旧 2），`cli` 与声明 `example` 逐字同。
- `src/cli/legacy/routes/scene-08.ts`：空数组留片（`ROUTES_SCENE_08 = []`），无残留。
- 待 `pnpm gen` 后复核：`routes.generated.ts` 同 13 条 exec ＋ `keys.ts` 114→116 ＋ `registry.ts` 同步（本票未手改生成物）。

## §6 门禁与负向

- `pnpm build`：本票 5 件自洁（`tsc` 仅报 `body/routes.ts` 两处 `TS2820`——新键待 `gen` 进 `keys.ts` 后自消，属先加命令后派生的 chicken-egg 常态；同票 analysis 多趋势键同理）；整包红在他票中间态（`scene-03-weight.ts:37` 语法错，轮流红，多票并行所致），自家未贡献红点。
- `pnpm gen:check`：待树绿后跑（根脚本 `node packages/skill-calorie/scripts/gen-cli.mjs --check`），判据 `exit 0`。
- 探针 `UNACCOUNTED`：待树绿后跑（本票未加手写面，预期 0）。
- 负向（函数级已跑）：`node .scratch/t355/mutation.mjs` → `MUT-GOOD true／MUT-RED true RED-OK`（改坏一格变化率必红）。声明级负向（删一条声明→对应词回 non-exec 必红，还原必绿）待树绿后补两行。

## §7 进度 95%＋未做项（含待确认，不 close）

- 未做：①树绿后 `pnpm build`＋`pnpm gen`＋`gen:check exit 0` 两行；②示例串经 `calorie-cmd-read` 真出口 `exit 0`＋落盘六项两行；③探针 `UNACCOUNTED=0` 一行；④声明级负向两行；⑤持锁 commit（中文 `feat(355)`，只含自家 5＋1 件）sha 回填；⑥本节进 Issue 95%（`--body-file`，不 close）。
- 待确认：①新键名 `body-composition-compare`／`body-measure-compare`（沿 `body-composition` 前缀＋`weight-compare` 后缀）；②体脂缺省全源口径（`source` 不给即全源，不沿 `latestSource` 暗口径）；③ `index.ts` 零改（门仍 3 件）。

## §8 收口：探针归零，五项全绿（2026-09-14 13:28 起）

- 窗口：持锁 `.scratch/locks/gate.lock`（原子创建，13:28:48 取）；HEAD `c10e5fe`；写入只落两件（本证据件＋豁免登记件）。
- 探针**前**：`RESULT: P1=FAIL P2=PASS P3=PASS P4=PASS P5=PASS 4/5`，`exit 1`，`UNACCOUNTED=3 处`（日志 `.scratch/t355/probe-before.txt`）。
- 探针**后**：`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`，`exit 0`，`UNACCOUNTED=0 处`；② 桶 2 处命中＝本次新增两条登记；④ 主体判不出仍 1 处（只报账、不判红）（日志 `.scratch/t355/probe-after.txt`）。

四项读数：

1. **CLI 真出口（绿）**：`node .scratch/t355/cli-true-exit2.mjs` → `exit 0｜RESULT: PASS (0 FAIL)`（41 条断言全 OK）。两条命令串照抄 `src/body/commands.ts` 的 `example`：`calorie.view.body-composition-compare` `exit 0`＋落盘 59457 字节 HTML；`calorie.view.body-measure-compare` `exit 0`＋落盘 62002 字节 HTML；页面六项判据（doctype／charset／style／≥2KB／无外链／无外 src）全 OK；围度 13 行逐值对手算表 13/13、非零 10。
2. **声明级负向（绿）**：`node .scratch/t355/decl-neg2.mjs` → `exit 0｜RESULT: PASS`。副本删两条声明 ⇒ REGISTRY `[false,false]`、两键 `exit 3`＋`ERR 3: 未知 calorie key…`、不落盘 HTML；还原 ⇒ `exit 0`；真 dist 四件 sha256 前后逐件相同（`body/commands.js=d23c708420d169fe`、`body/routes.js=c1a737ebbe600c31`、`cli/registry.js=0535ffd9bd8a8c3d`、`cli/keys.js=5ce681664ec16b97`）。
3. **自家 6 件相对 `42b770f` 零改动（绿）**：`git diff --quiet 42b770f -- <6 件>` → `exit 0`（`--stat` 0 行；6 件＝`src/body/compare.ts`／`src/body/commands.ts`／`src/body/routes.ts`／`src/fetch/body.ts`／`src/cli/legacy/routes/scene-08.ts`／本证据件）；本段追加前复验同读数。本段追加后，本证据件自身转由本次 commit 承接。
4. **探针（绿）**：前后读数同上。

豁免件三处改动（本票唯一扩权文件 `docs/skills/skill-calorie/t293-纪律豁免.json`；其余写集仍只有 `.scratch/t355/**`＋本证据件＋Issue #355）：

- 改前先验：`git status --short` 该件干净（无他人改动）；`JSON.parse` 通过。
- ① 行号刷新：`[1]` 的 `test/calorie-routing-81.test.mjs` `line` 342 → **350**（当刻该行＝`assert.equal(c.length, 9, …)`，仍把 9 当被比操作数）。342 已指不回当刻代码、登记作废 ⇒ 探针第一处 `UNACCOUNTED`；漂移由 #333／#383 改该文件所致，与本票无关（本票从未碰该文件）。
- ② 新增 `packages/skill-calorie/test/cmd-write-40-persist.test.mjs:497`、`anchor: 72`：`assert.equal(row.start_weight, 72)`；72＝**种子起始体重 72kg**（同文件 :492 入参 `startKg: 72`），另一侧 `row.start_weight` 是库内域值回读，**不是计数**。
- ③ 新增 `packages/skill-calorie/test/goal-upsert-127.test.mjs:109`、`anchor: 72`：`assert.equal(after.start_weight, 72)`；种子见同测试 :95 的 `start_weight = 72.0`，**不是计数**。
- 形状承 #348 席锚 68 成例（`file／line／anchor／status／reason／enforcer／owner／date／evidence` 齐全）；幅度 `git diff --numstat` ＝ `+23/-1`，无重排／无格式化／无加注释；改后 `JSON.parse` 通过（12 条）；首字节 `7B`（`{`）⇒ **无 BOM**。
- 撞锚成因：读键锚 **70→72 由本票推动**（本票 +2 读键；实证当刻 118/46/72/9 ↔ 本票前 116/46/70/9）。探针把这两行判成「主体＝权威总量」，是其 `AUTHORITY_SUBJECT` 关键词表含 `WEIGHT`——标识符带 `weight`（体重）字样即命中，并非它们真是计数；两条 reason 已把该成因写实。

gen／P3 归属：

- 本票**未跑** `pnpm gen`／`gen:check`（红线），门读数取自探针沙箱（`probe-after.txt:61-69`）：沙箱基线 `gen:check exit=0（绿）`＋手改生成物 ⇒ `exit=1`（`GEN-CHECK FAIL`）＋还原 ⇒ `exit=0`。
- 前一手 13:23 读到的 P3 红（沙箱基线 `GEN-STALE FAIL`，点名 `src/exercise/routes.ts` 印记 `48fa8e0e73e7… ≠ 57f4845b52ff…`）系**他人在途件（#342 席）**压红；收口窗口内该红已自消（共享树被他席续改，声明与生成物重新对齐）⇒ P3 记「绿窗已取（沙箱基线 exit 0）＋前红归他席在途件」。本票写集不含 `src/**`、不含生成物手改。
- 本次 commit sha 记在 Issue #355 正文（本段写于 commit 之前，不便自指）；地图 #158 全程未碰。
