# t337 今日盘与写后回执 · 老新融合证据（页面⑤⑥ 11 词）

> 票 `#337`（地图 `#154`）。本件是**融合**证据（上一份 `t337-体重盘与回执-证据.md` 是片段→整页那一轮的，本件不动它）。
> 融合契约：`docs/skills/skill-calorie/t154-体重页面-老新融合规范.md` §2／§3／§4／§5 四节逐条对照。
> 开工差距清单在 `.scratch/t154/gap-receipt.md`（逐格写了文件:行号＋现状＋怎么改）。

## 一、交付一句话

四张写后回执（`calorie.weight.log`／`batch`／`update`／`remove`，对应 9 条写类唤醒词）与今日体重盘（`calorie.view.weight`，2 条读类）
都成了**完整整页**：回执卡四段式＋`status` 徽章、改前 → 改后逐条对照、删除快照与删除后最新体重成对、
空卡守卫（缺的那节只出一句空态）、结论块唯一形态、页脚数据来源行、复制双钮（数据三格式＋日志六段齐）。
真出口 7 份产物全部 `exit 0` 且字节 > 0（§四），产物自检 7/7（§五）。

**本件不含截图**：视觉复核由上级统一另做（本会话按改派不跑截图、不用 read_image）。

## 二、改动的四个文件 ＋ 拆出来的一件

| 件 | 前 → 后（LF） | 一句话 |
|---|---|---|
| `src/weight/receipt.ts` | 316 → 154 | 端口 ＋ 改／删两页；超线（419）已当场拆开，拆法见 §七 |
| `src/weight/logReceipt.ts`（新增） | — → 199 | 单条与批量两页（量体重那两条写命令的产物页） |
| `src/weight/log.ts` | 164 → 285 | 体重盘补徽章／曲线 options／结论折叠区／日志位／来源行；空窗改整页空态 |
| `src/weight/edit.ts` | 78 → 84 | 载荷补改前 → 改后（体重与备注各一对）与快照行原始数值 |
| `src/weight/plate.ts` | 52 → 121 | 新增 `weightCurvePlan`（量程＋目标线判定，取数层算一处） |
| `src/weight/plateDocs.ts` | 14 → 93 | 由「文档头常量」长成「体重各页共用件」（回执页共用件三件起） |
| `test/weight-receipt-337.test.mjs` | — | 批量那条补一行种子（新库什么都不跳过，三态里就少了「跳过」） |

行数口径 LF（只数 `\n`）。**六件全部在 350 线下**（最长的 `log.ts` 285）。

## 三、老实物 → 新仓逐条落点

| 老实物:行号 | 新仓落点 | 落了什么 |
|---|---|---|
| `weight_log_receipt.html:63-70`（回执卡四段：大字／元信息／标签／一句话） | `plateDocs.ts:51`（`stateCard`）＋ `logReceipt.ts:52` 的 KPI 格 | 本次体重＝label＋value＋unit（kg），日期时间与去向进 detail；一句话另起结论块 |
| `weight_log_receipt.html:71-86`（近 30 天趋势小图） | `logReceipt.ts:115-141` | 图表恒出席位＋`options`（量程／刻度／单位／横轴标注／目标线／空态句） |
| `weight_batch_receipt.html:62-66`（写入／跳过／失败三色 KPI） | `logReceipt.ts:167-186` | 三色语义搬到卡上徽章（失败＞跳过＞写入取最重那态）＋计数表 |
| `weight_batch_receipt.html:67-73`（明细表） | `logReceipt.ts:187-201` | 逐条明细（日期／体重／状态／原因），空态句自带口径 |
| `weight_batch_delete.html:117-133`（区间变化＋删除后最新体重） | `receipt.ts:100-131` | 两块成对；无快照行时出「本次没有删除快照行」 |
| `crud_receipt.html:302-327`（update 只画实际变的字段、无变更不出空卡） | `receipt.ts:33-74` | 改前 → 改后逐条对照，空时只出一句空态、不留空表 |
| `crud_receipt.html:328-350`（delete 删除前快照） | `receipt.ts:132-152` | 快照表；老实物「无行整卡隐藏」改成显式空态句 |
| `weight_log_receipt.html:87-89`（空 footer） | `plateDocs.ts:71-84`（`deliveryBlocks`） | 页脚一行 `📊 数据来源:`：哪张库／哪个窗口／多少条 |
| `公共组件/assets/base.js:301-320`（复制双钮） | `plateDocs.ts:62-84` | 数据位恒三格式菜单 ＋ 日志位六段（第 3 段＝渲染命令原文） |
| `weight_dashboard.html` 的 summaryCard | `log.ts:185-236` ＋ `plateDocs.ts:56` | 结论收成唯一形态 `renderDisclosure({title:'结论', open:true})` |

## 四、真出口导出（`.scratch/t154/exports/receipt/`，脚本 `.scratch/t154/export-receipt.mjs`）

种子库走公共件 `docs/research/t81-seed.mjs` 的 `seedFull`（体重 2025-09-07 ~ 2026-09-07 逐日），日期锚 `SEED_TODAY`。

| # | 形态 | 命令 | 落盘（绝对路径） | 字节 | exit |
|---|---|---|---|---|---|
| ① | 记体重回执 | `calorie.weight.log` | `D:\ilife\.scratch\t154\exports\receipt\01-log.html` | 71461 | 0 |
| ② | 批量补录回执 | `calorie.weight.batch` | `D:\ilife\.scratch\t154\exports\receipt\02-batch.html` | 65583 | 0 |
| ③ | 批量删除回执（含快照） | `calorie.weight.remove --start/--end` | `D:\ilife\.scratch\t154\exports\receipt\03-remove.html` | 66255 | 0 |
| ③b | 删除回执·无快照空态（**分支直调**：真出口删空会先报「无体重记录」，这一支只能直调页面端口） | `weightReceiptDoc` | `D:\ilife\.scratch\t154\exports\receipt\03b-remove-nosnapshot.html` | 64521 | 0 |
| ④ | 改记录回执（改前 → 改后） | `calorie.weight.update` | `D:\ilife\.scratch\t154\exports\receipt\04-update.html` | 63578 | 0 |
| ⑤ | 今日体重盘 | `calorie.view.weight --window 30d` | `D:\ilife\.scratch\t154\exports\receipt\05-plate.html` | 79078 | 0 |
| ⑥ | 空窗体重盘（§5.7 肉眼那一格） | `calorie.view.weight --start/--end`（窗内 0 条） | `D:\ilife\.scratch\t154\exports\receipt\06-plate-empty.html` | 61506 | 0 |

读数落 `.scratch/t154/exports/receipt/result.json`（脚本自断言 7/7 `EXPORT-OK`）。

## 五、产物自检（`.scratch/t154/check-receipt.mjs` 的机器读数，替代本轮截图）

逐份断言：`<!doctype html>` 起、`charset`、`ilife-page`、三格式菜单（text／json／csv）、复制日志按钮、
页脚数据来源行、**「结论」恰好一处且 `open`**、卡上徽章种类、无残留标记、老实物区块关键词齐。

| 产物 | 徽章种类（页面上的） | 结论处数 | 数据来源行 | 三格式 | 关键词 |
|---|---|---|---|---|---|
| 01-log | ok（已写入） | 1（展开） | ✓ | ✓ | 5/5 |
| 02-batch | danger（失败 1 条） | 1（展开） | ✓ | ✓ | 5/5 |
| 03-remove | warn（已删除不可恢复）＋empty（无区间时）＋ok（已回读） | 1（展开） | ✓ | ✓ | 4/4 |
| 03b-remove-nosnapshot | warn＋empty＋ok | 1（展开） | ✓ | ✓ | 2/2 |
| 04-update | ok（已改动 1 行） | 1（展开） | ✓ | ✓ | 3/3 |
| 05-plate | ok×5＋warn（距目标还差） | 1（展开） | ✓ | ✓ | 4/4 |
| 06-plate-empty | empty×2（本窗无记录／无对照） | 1（展开） | ✓ | ✓ | 3/3 |

`SELF-CHECK-OK 7/7`。缺口：`02-batch`／`03-*`／`04-*` 无折线图，故 `yTick` 一格不适用（无图表页不勾）。

## 六、测试结果（走锁复跑）

| 用例 | 读数 |
|---|---|
| `test/weight-receipt-337.test.mjs`（本票 11 条真跑：2 读＋9 写，临时库回读对账） | **11/11 绿** |
| `test/profile-doc-179.test.mjs`（含 `#337` 三条钉死断言） | **绿**（除下条） |
| `test/profile-doc-179.test.mjs` 的 `#239 四张页接上「复制日志」` | **红**，断言「预检确认页按钮不对」 |
| `test/m5-receipt-97.test.mjs`／`test/cmd-write-40.test.mjs`／`test/delivery-83.test.mjs` | 绿（本轮早先一轮同跑，101 条里除上述一家全绿） |

**红在哪、为什么不是本票**：那条断言钉的是**基础信息**的预检确认页（`calorie.view.profile-wizard`）的
`data-action-id` 集合，期望 `['ilife-help-copy-prompt','ilife-copy-log']`，实际多出一颗**禁用态的「复制日志」**
（prompt 区旁边，base-render 侧「有数据位无日志位自动补一颗禁用日志」的兜底）。
本票写入集是 `src/weight/` 六个文件，与该页的链路（`src/profile/setup.ts`、`src/shared/copyArea.ts`、
`base-render/src/controls.ts`）零交集；同一现象正是 §5.7④「点不动的按钮不许留」的形态，建议归 base-render 那一侧。
本票四张回执页与体重盘的复制区都同时给了数据位与日志位，产物里 `ilife-copy-log` 只有一颗、无 `disabled`。

## 七、§5 七组勾选表

### 5.1 图表必传 options

- [x] 每张折线都传 `options`（`logReceipt.ts:127-140`、`log.ts:203-222`）
- [x] 纵轴可读数：`yTicks` 显式给（空数据显式 `false`）、`format: v=>v+'kg'`
- [x] 横轴标注 `labels:'select'`（首＋极值＋尾）
- [x] 量程在取数层算：`plate.ts:99 weightCurvePlan`（最小跨度 0.4kg＋上下留白），页面只传 `yMin/yMax`
- [x] `markLine` 只在量程内画；越界改写进「距目标差」的读数（`目标 X kg · 线在量程外，不画`）
- [x] 只走 `renderChartBlock`，无 canvas、无第三方库
- [x] 肉眼验收（机器读数代）：两份含图产物都能读出纵轴 `kg` 数值

### 5.2 徽章 status 映射

- [x] 卡片徽章只传 `ok/warn/danger/empty`（`plateDocs.ts:30-38` 一处定义 ＋ `stateCard`）
- [x] 提示图标走 `notice`（缺省 `info`），本页只有「删除口径」与「数据来源」两条静态提示
- [x] 「无改动／已改动」只由 `statusCard` 出一处（每页 1 次）
- [x] 肉眼验收（机器读数代）：写成功绿、失败红（批量那页是 danger）、删除橙、空态灰，与话里说的状态对得上

### 5.3 结论块唯一形态

- [x] 只用 `renderDisclosure({ title:'结论', contentHtml:'<p>…</p>', open:true })`（`plateDocs.ts:56`）
- [x] 一页最多一块；体重盘原来那张 `caption:'结论'` 的数据表已撤
- [x] 结论句由取数层字段拼（页面无自然语言解析、无正则）
- [x] 肉眼验收（机器读数代）：每份产物「结论」恰好一处且默认展开

### 5.4 复制载荷＝所见

- [x] 复制区按 `copyArea({ data, log })` 组
- [x] 三格式齐（数据位恒出菜单）
- [x] 载荷数字＝页面数字：写后回执的 `message` 取**本页结论句 ＋ 本页表格逐行**；体重盘走 `metricsOf` 冻结投影，缺值不进投影
- [x] 有数据位就有日志位：六段齐，第 3 段＝渲染命令原文（体重盘此前没有日志位，本次补上）
- [x] 空的那一项由共用件出空态句，不出点不动的按钮
- [x] 肉眼验收（机器读数代）：三格式菜单在 7/7 产物里齐；键与页面逐格同源

### 5.5 页脚数据来源行

- [x] 每页一行 `📊 数据来源:`，写清哪张库／哪个窗口／多少条（写法照 `diet/nutritionPortDocs.ts:58-61`）
- [x] 写库页同时进复制日志第 3 段（`calorie_data.db ｜ weight_log (写库回执)`）
- [x] 跳过／失败／删除后无记录这类情况在同一行注明（批量页写「跳过 N 条 · 失败 N 条」）
- [x] 肉眼验收（机器读数代）：7/7 产物都有这一行

### 5.6 空态与单点守卫统一

- [x] 空态句恒有：三张表都传 `emptyText`，两个图表区块恒出席位并传 `options.emptyText`
- [x] 空态分两种且自带口径（数据型「近 30 天无体重记录（窗）」／成功型「删除后库里已无体重记录」）
- [x] 单点守卫同一口径：有记录就出图，读数写「单点无变化」「单点无均值对照」
- [x] 样本不足不拒绝渲染：**空窗改整页空态**（参数非法仍当场报错）
- [x] 无进度条，本格不适用
- [x] 肉眼验收（机器读数代）：`06-plate-empty.html` 标题／空态句／结论／复制区／来源行齐

### 5.7 无静默消失

- [x] 无 `display:none`；图表区块 0 点也出席位（改出空态句）
- [x] 错误面走 `CalorieRenderError`；近 30 天取数失败的原话落进图表空态句（不再吞掉）
- [x] 参数非法当场报错（`assertDate`／`assertRange`）
- [x] 空容器／占位按钮／测试锚点一律不进产物
- [x] 肉眼验收（机器读数代）：空窗仍是一张完整的页（`06-plate-empty.html` 61506 B，机器读数 3/3 关键词齐）

## 八、未做到的点与原因

1. **`shared/receiptParts.ts` 的 `statusCard` 不带 `status`**：该件不在本票写入集，页面侧用展开补徽章
   （`plateDocs.ts:51`），共用件一行未动。
2. **同一口径的第二个定义还在禁区件里**：`history.ts:225-243` 的私有 `niceRange` 与本次 `plate.ts:99`
   的 `weightCurvePlan` 同形；`history.ts` 在本票禁区，本次合不了，**记债**：后续票应让历史页改调 `plate.ts` 的这件。
   （`review.ts`／`volatility.ts` 同族页面已在各自票里补了来源行，同一形状。）
3. **改删的「旧→新」着色**：base 的表格单元格只收文本（`blocks.ts:306-312`），着不了色；
   本页与 `profile/update.ts` 同形——统一列名「改前 → 改后」＋箭头，色落在卡上徽章。
4. **`#239 预检确认页按钮`那条红**：见 §六，红在本票写入集之外。
5. **视觉复核**：按上级改派，本会话不跑截图，交上级统一另做。

## 九、事故与复原（如实记账）

- 18:49 有别的会话在本仓跑 `git stash`，本票 5 个文件被卷走、工作树被还原成 HEAD 形状（stash `d68cc3c`，里头还混着别席一份用例）。
- 上级已从该 stash 里只读抽件并逐字节写回（`.scratch/t154/stash-rescue/`），本票另有一份自备份 `.scratch/t154/backup/`。
- 复原后逐件核过：字节与行数一致（§二），`删除快照（删除前取值…`、`STATUS_KIND`、`📊 数据来源`、批量种子那行都在。
- 已遵守：**不再跑** `git stash`／`checkout`／`reset`／`restore`。

## 十、交付对账（影响清单 vs 实际）

| 事前声明要碰 | 实际 | 偏差 |
|---|---|---|
| `src/weight/receipt.ts` | 碰（改写＋拆出 `logReceipt.ts`） | 拆出一件，已在 §二 写明 |
| `src/weight/log.ts` | 碰 | 0 |
| `src/weight/edit.ts` | 碰 | 0 |
| `src/weight/plate.ts` | 碰（只加量程件） | 0 |
| `src/weight/plateDocs.ts` | 碰（长成共用件） | 0 |
| （未声明）`src/weight/logReceipt.ts` | 新增 | 超线报警的处置，见 §二／§七 |
| （未声明）`test/weight-receipt-337.test.mjs` | 碰 1 行（批量种子） | 本票自己的用例，原期望与新库事实不符 |

未碰：`routes`／`history`／`compare`／`review`／`volatility`／`records`／`figures`／`scene-03`／`triggers`／
`cli/write.ts`／`SKILL.md`／快照与棘轮用例／`src/render/*`／`src/diet/*`／`docs/skills/skill-calorie/t35x-*`。

GATE-RUN 声明：本件 §四／§六 的读数为
`node tooling/run-locked.mjs --ticket 337 -- <命令>`（`node .scratch/t154/export-receipt.mjs`／`npx tsc -p packages/skill-calorie/tsconfig.json --noEmit`／`pnpm build`／`node --test …`）的持锁复跑结果。
