# #521 链路总览页整改 · 证据件

判定：**机器判据全绿**（出页自证 ＋ 三档溢出归零 ＋ 分隔符归零 ＋ 自检 PASS ＋ 变异红绿两行）。视觉判分见 `t521-视觉复评.md`。

## 一、产物身份（本件所有读数都对着这一个版本）

| 项 | 读数 |
|---|---|
| 页面 | `docs/skills/skill-calorie/t268-链路总览.html` |
| 字节 | **89,185** |
| mtime | **2026-09-15 13:03:19** |
| SHA256 | `C14683634CFB36C3844883A0DB55FB3777BF17A8C26911DAE9889FE99F7FAEB2` |
| 生成器 | `docs/skills/skill-calorie/t268-链路总览.build.mjs`（自写入仓） |
| 自检 | `.scratch/t268-link/check-page.mjs`（按新结构重写；`.scratch/` 在 `.gitignore` 里，全仓零个 `.scratch` 文件入过库，故它只作工作件留在盘上） |

生成器读数（落盘自己的文件不占锁，故这一步没有 GATE 存根，读数照抄控制台）：

```
BUILD page=D:\ilife\docs\skills\skill-calorie\t268-链路总览.html bytes=89185 cards=39
BUILD 条数自证 39 = 写 13 ＋ 读 7 ＋ 其余读类 19（与清单 count 段逐字对上）
BUILD 真库实况 念得通 14 ＋ 念不通 25 = 39；阻断四类 时间窗空 20、示例参数 id 在真库不可达 2、未设运动目标 2、昨日无可复制记录 1
BUILD 代实两类 占位符 7 条、示例 id 2 条；样例 06 与 09
BUILD 字节同源 当刻盘上 39 份 65,028 到 121,054 字节，合计 2,977,349；分布三组各加总 39
```

生成器里写死的四条自证（任一条不过就抛，不出页）：条数三口径对不上清单 `count` 段即抛；真库实况对不上 `runs.jia_ok`／`jia_blocked` 即抛；阻断四类加总对不上 25 即抛；三组分布任一加总不是 39 即抛；生成结果不是 39 张卡、或兜底路径不是 39 条、或出现 `http`／`<link`／`@import` 即抛。

## 二、九步验收队列（每条各自持锁，各留 `RUN ticket=521 runId=…`）

队列：`node .scratch\t521\闸门.mjs`，日志 `.scratch\t521\gates2.log`，收尾打 `闸门汇总 steps=9 mismatch=0`。
整条队列跑完**页面 mtime 仍是 13:03:19**（队列里不重跑出页），所以视觉席的当刻截图不作废。

| # | 命令 | 期望 | 实际 | GATE-RUN runId |
|---|---|---|---|---|
| 1 | `node packages/skill-calorie/scripts/measure-responsive.mjs .scratch/t521/改前-页.html --label 改前 --json .scratch/t521/改前-溢出.json` | 1 | **1** | `8b7de635-b436-4ac4-823d-18997d64d42b` |
| 2 | `node packages/skill-calorie/scripts/measure-responsive.mjs docs/skills/skill-calorie/t268-链路总览.html --label 改后 --json .scratch/t521/改后-溢出.json` | 0 | **0** | `b5cc8ee9-9719-45b0-a30a-c54e1fe4034e` |
| 3 | `node .scratch/sep-audit/probe.mjs .scratch/t521/改前-页.html` | 1 | **1** | `2f74200e-d9cc-4bf0-a878-f08a0a107122` |
| 4 | `node .scratch/sep-audit/probe.mjs docs/skills/skill-calorie/t268-链路总览.html` | 0 | **0** | `6bbf8eed-fb52-4496-b77f-f42ae679a88e` |
| 5 | `node .scratch/t268-link/check-page.mjs` | 0 | **0** | `c8d1ff55-e875-4611-9f90-16fd4acd1164` |
| 6 | `node .scratch/t521/变异自证.mjs` | 0 | **0** | `2a90e91e-67b0-4e92-a9aa-9fa473472da4` |
| 7 | `node .scratch/t268-link/check-page.mjs .scratch/t521/坏-中点.html --no-overflow` | 1 | **1** | `4f671d84-369e-4d09-9a08-a9fdc1961e7d` |
| 8 | `node .scratch/t268-link/check-page.mjs .scratch/t521/坏-实体.html --no-overflow` | 1 | **1** | `0889ba52-faa8-43a3-a584-b4c4385fceae` |
| 9 | `node .scratch/t268-link/check-page.mjs` | 0 | **0** | `41f1e390-2b96-4e52-9020-553c8355c0a0` |

第 1、3、7、8 步是**故意红**的对照读数（改前页与改坏件），用来证明判据有鉴别力；退出码 1 是期望值。

## 三、三档溢出读数（判据：`scrollWidth − innerWidth == 0`）

**改前**（旧页副本 `.scratch/t521/改前-页.html`，54202 字节）：

```
390档:      0 ✗ viewport=1169≠390   768档:   +401 ✗ overflow+401   1440档:      0 ✓
OVERFLOW-NONZERO pages=1 cells=3 failed=2
```

旧页没有 `viewport` 声明、768 档还溢出 401px。

**改后**（当刻页面，89185 字节）：

```
t268-链路总览.html   390档: 0 ✓   768档: 0 ✓   1440档: 0 ✓   img=0(0/0 放得下) clip=0
OVERFLOW-ZERO pages=1 cells=3 failed=0
```

三档 `scrollWidth − innerWidth` 都是 **0**，且 `clip=0`（没有元素被裁）。

## 四、分隔符读数（口径见 issue #508）

**改前**：行级 `含 · 44 行 / 含 ；14 行 / ≥3 段并列 63 行 / 命中合计 105 行`；节点级 `含 · 57 处 / 含 ；18 处 / ≥3 段并列 55 处 / 命中合计 126 处` → exit 1（红）。

**改后**：

```
产物: docs\skills\skill-calorie\t268-链路总览.html   可见文本行: 641
【行级】含 · : 0 行   含 ；: 0 行   ≥3 段并列: 0 行   命中合计: 0 行
【节点级】含 · : 0 处   含 ；: 0 处   ≥3 段并列: 0 处   命中合计: 0 处
```

行级与节点级都归零。`<title>` 也在这条判据里（现为「链路总览：卡路里场景 04 运动 39 条唤醒词」，无 `·`／`；`）。

## 五、变异自证两行（判据有鉴别力）

拿**当刻产物**改坏一处唤醒词（`记运动` → `记运动·慢跑`），两个写法各一份：

| 改坏写法 | 自检读数 | probe 读数 |
|---|---|---|
| 真 `·`（U+00B7） | `CHECK-PAGE verdict=FAIL`，`bad=1 · 可见文本出现 · ×1` | 行级 R1 命中 1 行、节点级 R1 命中 1 处 → exit 1 |
| `&middot;` 实体 | `CHECK-PAGE verdict=FAIL`，`bad=2 · 可见文本出现 · ×1 ＋ 页里出现实体 &middot; ×1` | 行级 R1 命中 1 行、节点级 R1 命中 1 处 → exit 1 |

还原（第 9 步再跑当刻页面）：`CHECK-PAGE verdict=PASS`，`ok=11`，exit 0。
——**改坏一处必红、还原必绿**，两行读数都留在上面这张表里。

## 六、自检六组断言（第 5 步的完整读数）

```
CHECK-PAGE verdict=PASS page=D:\ilife\docs\skills\skill-calorie\t268-链路总览.html bytes=89185
CHECK-PAGE ok=11 :: 39 张链路卡齐全 ／ 逐条链接=39 条，无脚本兜底路径=39 条 ／ 零 http ／ 零 <link ／ 零 @import
 ／ 可见文本零 · ／ 可见文本零 ； ／ 实体探针扫过 middot 与 183 两种写法 ／ 390 档 scrollWidth − innerWidth = 0
 ／ 768 档 scrollWidth − innerWidth = 0 ／ 1440 档 scrollWidth − innerWidth = 0
```

逐条对应本票要求：

1. **39 条齐全**：39 张 `<article class="lc">`，每张都有序号、唤醒词、`calorie-cmd-read` 命令块、产物链接、绝对路径兜底。
2. **链接当刻存在且字节一致**：每条 `file:///` 链接解成本机路径后 `existsSync` ＋ `statSync().size` 与页上印的字节逐条相等；链接、`data-copy`、无脚本兜底路径**三者同源**（不同源即红）；39 条去重后仍是 39 条。
3. **自包含**：零 `http`、零 `<link>`、零 `@import`。
4. **可见文本零 `·` 零 `；`**：剥法同 `.scratch/sep-audit/probe.mjs`（整段去掉 `<style>`／`<script>`／注释／标签），并**解实体**（`&middot;`／`&#183;`／`&#xB7;`／`&semi;`／`&#65307;`），实体写法另有一条显式探针。
5. **三档 `scrollWidth − innerWidth == 0`**：自检内部调 `measure-responsive.mjs` 出真浏览器读数（390／768／1440）。
6. **无脚本也有 39 条绝对路径**：39 条路径全在**静态 HTML**（每张卡尾部 `<noscript>` 一行 11px 可选中路径），不靠脚本注入。

## 七、口径出处（判据读数的出处，别自己另立）

- 分隔符：`.scratch/sep-audit/probe.mjs`，口径见 issue #508。注意它的 **R3**（≥3 段并列）把 `、／·；` 的三连串也当命中——本页正文里的三连列表全部拆成独立元素或改用「和／与」，这是改后归零的关键一处。
- 溢出：`packages/skill-calorie/scripts/measure-responsive.mjs`（`--widths 390,768,1440`，读数看 `overflow === 0`）。
- 用词：`docs/agents/wording.md`（禁用词表已逐条过）。
- 手机端同档：`packages/base-render/assets/help-template.html`（断点取 **820／500**，票面第 8 条原写的 900／600 按仓规统一到 help 模板；本页是全宽卡片流，断点差别只剩「单列／双列」与「疏／密」）。
- 条数三口径：`WRITE_KEYS`／`READ7_KEYS` 抄自 `.scratch/t268-link/make-link-page.mjs`（只读了它 3 段共 40 行，没有整体读 27KB）。

## 八、覆盖说明与未做项

- **本票只改链路总览页这一份文件**；它链接的 39 份产物页面另立票（图里 #522 起）。页面上的**逐条数据会过期**：字节数、真库实况都随产物整改变化，本页只管结构与呈现，**重新生成归产物整改收口票**。
- 生成器落盘那一步没有 GATE 存根：按编排者口径，落盘自己的文件不占锁；验收与提交才占锁。队列里因此**不含出页**，页面 mtime 在整个验收期间保持 13:03:19。
- 视觉判分不占锁（口径同上），读数与截图路径见 `t521-视觉复评.md`。
