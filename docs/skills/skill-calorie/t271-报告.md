# #271 条目列表页 · 11 条 —— 实施报告（作者：本票唯一作者）

> 判据正本：`docs/skills/skill-calorie/t155-派单/02-逐票专属节.md` 的 `## #271`（第 45–56 行）。
> 红线与作业面事实：`19-第一波派单附录.md`（逐条生效）、`20-第二波派单附录.md`（交接面）。
> 融合基准：`t425-融合基准.md` §四（裁定 1–11）＋ §五（七类页面骨架）。

## 一、开工当刻量到的盘面（不引用别人给的数）

- `git status --porcelain` 全仓 **159 行**；我声明路径内当刻状态：
  - `src/diet/today.ts`（` M`）＝ #275 的复制日志第 4 段那一处改动（按派单**保留**，本票一行未动）；
  - `src/diet/nutrition.ts`／`src/diet/nutritionPortDocs.ts`（` M`）＝ #275 在途件，本票一行未动；
  - `src/render/dietDocs.ts`／`src/diet/todayDocs.ts`＝**本票改动前干净**（HEAD 态）。
- 行数（LF 口径，当刻实测）：`render/dietDocs.ts` 264 → 改动后见 §四；`diet/todayDocs.ts` 138。
- 11 条词的键面（`src/diet/routes.ts` ＋ `src/home/routes.ts` 回源）：
  - `calorie.today` 3 条：看今日饮食（order 23）／看有备注的饮食记录（order 33，`hasNote:true`）／看今日饮食记录（new order 3）；
  - `calorie.view.diet` 8 条：看昨日（24）／看本周（25）／看上周（26）／看本月（27）／看上月（28）／看最近 7 天（29）／看最近 30 天（30）／看某段时间（31）。
- **`calorie.view.diet` 的处理体不在 `src/diet/`**：声明住 `src/home/commands.ts:21`，处理体住
  `src/home/today.ts` 的 `viewDietOverview`，装配件＝本票的 `src/render/dietDocs.ts` 的 `buildViewDietDoc`。
- 老实物结构（由只读取证子代理从 `.scratch/t271/old-page-facts.md` 摘出，逐条带行号）：
  - `today_meals.html` 明细**九列**＝日期时间／餐次／食物／克数／热量／蛋白／碳水／脂肪／**备注**（`:168`，空备注出空串 `:337`）；
  - 按日汇总**出块条件** `if (meta.days > 7)`（`:240-242`）；明细**倒序** `:322`；时间 `.slice(0,5)`（`:326`）；
    来源行 `:344-345`；KPI 四卡 `:140-145`；迷你柱图量程／灰柱／均线 `:265-279`；筛选下拉 `:228-238`。
  - 样张 `docs/skills/skill-calorie/t425-样张-今日饮食.html`（② 类正本）：DOM 顺序＝页壳（眉标→标题→**结论句走副题槽**）
    →`ilife-block-toc`→`sec-kpi`→`sec-trend`＋口径行→`sec-dist`＋口径行→`sec-daily`→`sec-meals`→总口径行→`sec-copy`→来源脚注；
    明细九列含**备注**、空备注写 `—`。

## 二、改了什么（逐件，全部在我的声明路径内）

### 1. `packages/skill-calorie/src/render/dietDocs.ts` —— `buildViewDietDoc` 原地改内容（不搬）

`calorie.view.diet` 窗口词那一支按 `t425-融合基准.md` §五 第 ② 类骨架逐行落位：

| §五 序 | 区块 | 落法 |
|---|---|---|
| 1／2 | 眉标行／内容标题 | 眉标＝中文族名「卡路里 · 饮食」（裁定 1，源码标识符不上屏）；标题仍「饮食总览 start ~ end」 |
| 3 | 结论句一行 | 走页头副题槽（`t425-融合基准.md:139` 记明公共层没有「结论行」区块，样张做法就是复用副题），句内含本页三个读数；裁定 2-补：普通小字行、不走深底块 |
| 4 | 页内导航 | `renderTocBlock`，六个锚点 `sec-kpi`／`sec-trend`／`sec-dist`／`sec-daily`／`sec-meals`／`sec-copy`（名字照样张），**只列真会出的区块**（空窗时不列趋势／按日汇总，不留死链接） |
| 5 | KPI 读数 | 四张卡（累计／日均／目标／趋势），沿用 |
| 6 | 主体图 | 每日摄入折线：**两点及以上才画**；只有一天有记录时改出说明句（裁定 5「单点不成线」）。餐别有数画柱图；**零值不画柱身**，改四张「无记录」卡且值位写 `—`（裁定 4／5） |
| 9 | 主表／主列表（含备注列） | 按日汇总（无记录日写 `—`，不再留空）＋「全部记录」明细**九列含备注**（日期时间／餐别／食物／克数／热量／蛋白／碳水／脂肪／备注）；时间截断到分、缺值 `—`；**明细按日期＋时间倒序**（移植清单 5 的老口径 `today_meals.html:322`） |
| 12 | 空态块＋引导句 | 本窗零记录 ⇒ 整页仍完整，`emptyGuide()` 出空态句＋一句「怎么说就能记上」 |
| 13 | 口径说明行 | `renderCaliberLine`（餐别口径＋缺值口径，都不带常量名） |
| 14 | 复制区 | `listPageCopy()`：给了 `command` 出**双按钮**、日志第 4 段＝本次命令原文；不给只出「复制数据」，**不留死按钮** |
| 15 | 来源脚注 | `sourceLine()`（`数据来源 · 饮食记录 · start → end · 共 N 条`），裁定 2-补：普通小字行、不走深底块 |

另按派单加三个**可选入参**（不给即老路、不产生死按钮）：

- `command?: string` —— 复制日志第 4 段；
- `mealView?: MealDistributionView` —— 给了即整页换**餐别页**，调 #273 交付的 `buildMealDistributionBlock(v, command)`；
- `overviewView?: DietOverviewView` —— 给了即整页换**总览页**，调 #275 交付的 `buildDietOverviewBlock(v, command)`。

`MealDistributionView`／`DietOverviewView` 两个类型从交付件原样 import，**没有自己重写**。

### 2. `packages/skill-calorie/src/diet/todayDocs.ts` —— ② 类页族共用件 ＋ 今日页补骨架

- 新增并导出四个**族内共用件**：`minuteOf()`（时间截到分、缺值 `—`）、`noteOf()`（备注缺值 `—`）、
  `DIET_LIST_COLUMNS`（老实物九列，两页共用一份列面）、`listPageCopy()`（复制区双按钮与日志第 4 段）。
- 新增 `buildMealDistributionPage()`／`buildDietOverviewPage()`：两个换页支的**页框**（结论句槽／页内导航／`charts: true`），
  区块本体仍由 #273／#275 的具名函数出。
- `buildTodayDietDoc()`（`calorie.today` 三张页）：补页内导航（`td-kpi`／`td-dist`／`td-macro`／`td-table`／`td-copy`）、
  口径说明行、来源脚注（老实物 `today_diet.html:173-176` 本来就有页脚来源行）、复制区双按钮；
  时间与备注按裁定 4 走 `—`；**备注列改回恒出**（老实物 `today_meals.html:168` 恒有、§五 第 9 行恒出；
  件里 #496 当年只在「只看有备注的」那一支加过它，本票按老实物与样张口径改回，旧注释原文已在件里记明）。

### 3. `packages/skill-calorie/src/diet/today.ts` —— 只加一处实参（`viewToday`）

`buildTodayDietDoc({...})` 多传 `command: commandLine('calorie.today', params)`（复制日志第 4 段，裁定 7）。
**#275 在 `viewTodayWater` 上的那处改动逐字保留、一行未动**（`src/diet/today.ts:67`）。

### 4. 没碰的

`src/home/**`（编排者 2026-09-15 撤回授权，改归 #276 席）、`src/diet/index.ts`、`src/diet/routes.ts`、
`src/triggers/**`、`packages/base-render/**`、以及任何别人声明路径。

## 三、机器证据（每次声称跑过的运行都在 `.scratch/locks/gate-runs.log` 里一对一可查）

```
GATE-RUN runId=260a4b50-b401-4b61-aa01-f08af08e0e99 cmd=pwsh locked1.ps1（基线＋编译＋复跑＋12 条词）
GATE-RUN runId=2e94ee45-ab39-434e-a012-c8f30ada88e9 cmd=pwsh locked2.ps1（**最终：BUILD exit=0 ＋ 靶向测试 ＋ 12 条词 → RESULT: 12/12**）
GATE-RUN runId=c7d25043-72ab-4adb-b998-cac2e7a3fe8c cmd=pwsh commit.ps1（git add ＋ git commit → 0957bc8）
GATE-RUN runId=c1dee371-093c-482a-be0a-477d05bb96e8 cmd=pwsh locked3.ps1（变异电池：MUT-BUILD exit=0／MUT-TEST exit=1 红／RESTORE same=True／RES-BUILD exit=0／RES-TEST exit=1）
GATE-RUN runId=a7bfcd85-7ac7-435c-a3a2-b128ee8889ac cmd=pwsh locked3.ps1（变异电池重跑：被他人件挡住，编译红）
GATE-RUN runId=0bb8e22d-66db-49f3-a9e6-e4f07f22fde3 cmd=pwsh locked6.ps1（无编译复跑＋变异电池重跑：仍被同一件挡住）
GATE-RUN runId=00489d58-212d-4e23-8947-bc20fedd3cac cmd=pwsh locked2.ps1（编译红：我的 TS1005，见 §五 5.1）
```

### 11 条词 ＋ 场景 01 回归（最终一次，`BUILD exit=0`，`RESULT: 12/12`）

| # | 词 | 命令 `--params` | exit | 产物字节 | 完整文档读数 |
|---|---|---|---|---|---|
| w01 | 看今日饮食 | `calorie.today` `{"date":"今日"}` | 0 | 84709 | doctype＋charset＋样式＋导航＋来源脚注 |
| w02 | 看昨日饮食 | `calorie.view.diet` `{"window":"昨日"}` | 0 | 80217 | 同上 |
| w03 | 看本周饮食 | 同上 `{"window":"本周"}` | 0 | 86546 | 同上 |
| w04 | 看上周饮食 | 同上 `{"window":"上周"}` | 0 | 98242 | 同上 |
| w05 | 看本月饮食 | 同上 `{"window":"本月"}` | 0 | 123842 | 同上 |
| w06 | 看上月饮食 | 同上 `{"window":"上月"}` | 0 | 160238 | 同上 |
| w07 | 看最近 7 天饮食 | 同上 `{"window":"7d"}` | 0 | 101750 | 同上 |
| w08 | 看最近 30 天饮食 | 同上 `{"window":"30d"}` | 0 | 165288 | 同上 |
| w09 | 看某段时间饮食 | 同上 `{"window":"custom","start":"2026-08-01","end":"2026-08-31"}` | 0 | 160238 | 同上 |
| w10 | 看有备注的饮食记录 | `calorie.today` `{"date":"今日","hasNote":true}` | 0 | 78298 | 同上 |
| w11 | 看今日饮食记录 | `calorie.today` `{"date":"今日"}` | 0 | 84709 | 同上 |
| **r01** | **场景 01 看今日饮食概览（回归）** | `calorie.view.diet` `{"window":"今日"}` | 0 | 80937 | 同上 |

产物绝对路径：`D:\ilife\.scratch\t271\out\w01.html` … `w11.html`、`r01.html`（人眼验收请双击打开）。
逐条读数原文在 `D:\ilife\.scratch\t271\locked2.log` 的 `RUN` 行里。

### 靶向测试（同一窗口）

| 件 | 基线（改动前编译态） | 最终 | 归因 |
|---|---|---|---|
| `test/diet-homogeneity-108.test.mjs` | exit 0 | **exit 0** | 我的主回归 |
| `test/delivery-83.test.mjs` | exit 0 | **exit 0** | 产物字节稳定 |
| `test/diet-review-t273.test.mjs` | exit 0 | **exit 0** | #273 交付件 |
| `test/db-readonly-93.test.mjs` | exit 1 | exit 0 | 环境相关，两次不稳 |
| `test/calorie-c43.test.mjs` | exit 0 | exit 1 | **范围外**：失败句是「改动的分页…单元格」「克数那一行的改前→改后」，落在写后回执页（`src/diet/receipt.ts`，台账显示他人在改）；与条目列表页无关 |
| `test/cmd-read-t11.test.mjs` | exit 1 | exit 1 | **范围外**：基线就红（`今日总览 〈日期〉` 标题断言） |
| `test/t445-告警线门.test.mjs` | exit 1 | exit 1 | **范围外**：台账里他人的件陈化（`nutritionPortDocs.ts` 台账 645／实况 674、`receipt.ts` 台账 456／实况 352 等）；**我改的两件都没进红名单** |

### 台账

`node packages/skill-calorie/scripts/check-warning-line.mjs`：`RESULT: 77/79`，两条红全在他人的件上；
我改的两件 **`src/render/dietDocs.ts` 332 LF**、**`src/diet/todayDocs.ts` 260 LF**（LF 口径当刻实测）
**都在 350 以内、未新增挂号行**。

### 变异自证

```
MUT-BASE  sha256=4976E1ABEEEE8FE588765568AA28DB18FE126242A221ABDAA5B8D6C6CED322DF
MUT-APPLIED sha256=39149E5ACD2268DCDFF02A37B7E72688E077D5D9F9C2C6B79A83154DA18AD0BC   ← 把明细的备注原文换成一句假文案
MUT-BUILD exit=0   MUT-TEST exit=1    ← 红
RESTORE   sha256=4976E1ABEEEE8FE588765568AA28DB18FE126242A221ABDAA5B8D6C6CED322DF same=True
RES-BUILD exit=0   RES-TEST exit=1
```

**红那半边成立**（改坏处 ⇒ 相关断言变红）。**绿那半边没拿到**：还原后那一次 `RES-TEST` 的 `exit=1`
不是「还有红」，而是**测试件自己当时有 4 条写错的断言**（`tableOf` 把 `<thead …>` 当 `<th` 匹配、
`ilife-block-chart-block` 命中的是样式段、`distribution-rows` 实为 `dist-row`、
把公共层的 `disabled` 日志按钮当成了活按钮）。这 4 条**已逐条改对**，并用只读探针
（`.scratch/t271/diag3.mjs`，同一份判定逻辑）重放：`RESULT: 8/8` 全过（退出码 0）。
**真读数（`node --test test/diet-list-t271.test.mjs` 回绿）在最后一次重编时被他人的生成物挡住了**，
见 §六 第 0 条——那是收口前必须补的唯一一格。

## 四、结构必报（`docs/agents/structure.md` 五条铁律／必报五步）

- **这两件当刻都在告警线（350 LF）以内**：`src/render/dietDocs.ts` **332 LF**、`src/diet/todayDocs.ts`
  **260 LF**（口径＝LF 数法；读数用 `File.ReadAllText().ToCharArray()` 数 `\n`）。**没新增挂号行。**
- **中途一度越过 350**：`src/render/dietDocs.ts` 在我第一版重构后到过 **368 LF**，被台账门当场抓到
  （`RED 漏报（台账没有）：src/render/dietDocs.ts LF=368`）。我按规则做的**不是报挂号**而是**把它拆回去**：
  把 ② 类页族真正共用的四件（`minuteOf`／`noteOf`／`DIET_LIST_COLUMNS`／`listPageCopy`）提进
  `src/diet/todayDocs.ts`（今日页与窗口页同族，两页共用一份列面与复制区），`dietDocs.ts` 只留窗口页的编排。
  ⇒ 结论：**已落回线内、不再触发第四步**。
- 新提的四个共用件**不是形状件**：形状全在 `base-paint/blocks`（`renderTocBlock`／`renderCaliberLine`／
  `renderDataTable`／`renderKpiGrid`／`renderChartBlock`／`renderEmptyBlock`）与技能层共用件
  （`shared/sourceLine.ts`／`shared/emptyGuide.ts`／`shared/copyArea.ts`），页面里**一条色值、一个字号都没写**。

## 五、中断与范围外发现（如实记账）

### 5.1 中断记录（编排者要求记一行）

- **现象**：`packages/skill-calorie/src/diet/todayDocs.ts(107,8): error TS1005: ')' expected.`——我在给
  今日页加页内导航时漏了一个括号，属**语法错**（不是类型错）。
- **影响**：语法错会让 `tsc -b` **不产出**整包 ⇒ 本图其他三席一切「以产物为前提」的读数在那一刻全部作废
  （#269 复核席撞上它、一整窗写命令读到 0 字节）。**这是我的过程缺陷，不是判据问题。**
- **处置**：编排者报来后**当场补齐该括号**（`npx tsc --noEmit -p packages/skill-calorie` 回到 0 错，免锁可跑）；
  此后按要求把中间态压短。**后续同类中间态我会先跑免锁 `--noEmit` 再落盘。**

### 5.2 范围外发现（不修、不重跑）

1. `test/cmd-read-t11.test.mjs` exit 1：**基线就红**（`今日总览 〈日期〉` 那条标题断言），与本票页面无关。
2. `test/t445-告警线门.test.mjs` exit 1：**基线就红**——台账里他人的件超线或陈化（`src/diet/receipt.ts`
   台账 456／实况 350、`src/body/compare.ts`／`src/home/homeViewParts.ts`／`src/photo/gifDoc.ts` 等）。
3. `packages/skill-calorie/src/photo/helpDoc.ts` 与 `helpDocContent.ts` 之间的导出名不一致
   （`PHOTO_HELP_ROWS_LEAD`）：他人在途件，**让整包产物在运行期坏掉**（所有读命令 exit 1、0 字节）。
   本票的最终一次 12 条实跑被它挡住，处置见 §六。
4. `packages/skill-calorie/src/body/wizardPlate.ts` 一度有语法错（`TS1128`），同样是他人在途件，随后自愈。

## 六、未做项与下一手缺什么

1. **`calorie.view.diet` 的三处接线——归 #276 席（`src/home/**`）**。编排者 2026-09-15 已把
   `src/home/**` 整块改归 #276，并把我的授权撤回。我这边**入口已备好、类型已定**，一次接三处即可：
   - 处理体：`packages/skill-calorie/src/home/today.ts` 的 `viewDietOverview`（当刻第 94 行起那个
     `buildViewDietDoc({...})` 调用）；
   - 加一行 import：`import { commandLine } from '../shared/writeParts.js';`；
   - 入参对象里加三位（**全部可选，不给即当刻行为**）：
     - `command: commandLine('calorie.view.diet', params)` —— 出「复制日志」第二颗按钮，日志第 4 段＝本次命令原文；
     - `mealView: buildMealDistributionView(db, params['meal'], start, end)`（`../diet/review.js` 导出的签名
       `(db, mealRaw, start, end)`）——**只在命令面带 `meal` 参数时给**；给了即整页换餐别页，
       区块调 #273 的 `buildMealDistributionBlock(v, command)`；`meal` 参数的**参数名与取值是 #276 的账**
       （本票一个字没猜、路由一行没动）；
     - `overviewView: buildDietOverviewView(db, date)`（`../diet/nutritionPort.js`）——**只在入口带标记时给**；
       给了即整页换总览页，区块调 #275 的 `buildDietOverviewBlock(v, command)`。
       「看饮食总览」与「看最近 7 天饮食」**当刻路由参数一字不差**（都是 `{"window":"7d"}`），
       命令这一层分不出进来的是哪条 ⇒ 要照 #509／#511 的先例由入口带一个标记（那要动
       `src/home/routes.ts`，不在本票声明路径内）。
2. **`meal` 参数没到之前，5 条餐别词（看早餐／午餐／晚餐／加餐／全部餐别分布）出的是普通 7 天列表页**
   ——参数缺失按「不编数」处置，餐别页两支已备好、有直测覆盖。
3. **窗口为空 vs 库为空**：`viewDietOverview` 现在直接调 `buildDietOverview`，窗口内零记录会抛
   `missing-data` ⇒ `exit 4`，与裁定 4「窗口为空出完整页＋空态句＋引导句」相冲。**这一支要改的也在
   `src/home/**`**（同一处接线顺手做），本票的页面侧空态支已备好并有直测覆盖（`emptyGuide`）。
4. **按日汇总「窗口天数 > 7 才出」（`t425-融合基准.md` §二 移植清单第 6 条＋裁定 5，老口径
   `today_meals.html:240-242` 的 `if (meta.days > 7)`）本票没有实施**，原因是**与仓内现有断言相冲**：
   `packages/skill-calorie/test/diet-homogeneity-108.test.mjs:149` 用 **3 天窗口**断言产物里必须有
   「按日汇总」，而该测试件**不在本票声明路径内**（红线 1 不许改）。按「红只按我这条路不新增红」判，
   我选择**保留当刻恒出行为**并在此记账。⇒ **下一手缺什么**：请编排者裁定 ① 把该测试件转票给我（收窄那条断言，
   改成「>7 天窗口必须有、≤7 天窗口不出现」）② 或把这条移植项转给收口票。**两条路我都能立刻执行。**
5. **移植清单第 3／5 条的老页交互（点柱联动筛选、日期下拉）未移植**——`t425-融合基准.md` §二-补 已登记
   「老页交互未移植、不作验收项」，本票照此不实施。
6. **视觉由人眼看**：本票不出脚本化视觉判定；产物落 `D:\ilife\.scratch\t271\out\*.html`，
   人眼验收请直接双击打开。

## 七、停止点标记行与下一手缺什么

**STOP-POINT-271：从这一行之后接。**

- **已达成的**：三条声明路径都改完并**提交**（`0957bc8`，6 件）；11 条词＋场景 01 回归逐条真跑
  `12/12 exit 0` 且每条都是完整文档；备注列九列有结构化断言（探针 8/8）；台账两件都在 350 以内。
- **下一手第一件**：等 `packages/skill-calorie/src/triggers/routes.generated.ts` 恢复可编译后，
  在**同一个持锁窗口内**先 `npx tsc -b packages/base-render packages/skill-calorie`（应 exit 0），
  再 `node --test packages/skill-calorie/test/diet-list-t271.test.mjs`（应 exit 0），
  把这两行补进 §三 的变异自证那一格 ⇒ 那次变异自证就从「半格」变成「两行能分辨真假」。
- **下一手第二件**：把 `src/home/**` 的三处接线交给 #276 席（§六 第 1 条已给足入参名与类型）。
- **本票没碰过的**：`src/home/**`（授权已撤回）、`src/diet/index.ts`、`src/diet/routes.ts`、
  `src/triggers/**`（含那个坏掉的生成物）、`packages/base-render/**`。
## 八、接任席现场量测与收尾（同一票第二席，2026-09-15 15:40 起）

### 8.1 开工当刻量到的现场（不采信前任结论，逐项自量）

- `git log --oneline -6`：HEAD＝`edf2469`（他图 #351／#407 的提交），本图两个提交都在祖先链上：
  `git merge-base --is-ancestor 68ee7d2 HEAD` 退出 0；`68ee7d2..HEAD` 之间另有 **9 个**他图提交。
- `git diff --stat HEAD` 对**我声明路径**（`src/diet/todayDocs.ts`、`src/diet/today.ts`、
  `src/render/dietDocs.ts`、`test/diet-list-t271.test.mjs`、`docs/skills/skill-calorie/t271-*`、`.changeset/`）
  **空** ⇒ 前任的源码交付全部已在仓、树上没有无主 diff 留在我的路径里。
- `.scratch/t271` 实有 **48 件**，摘读取到：
  - `locked1.log`：`RESULT: 12/12`，12 条真跑全 `exit=0`（11 条词 ＋ 场景 01 回归）；
  - `commit.log`／`commit2.log`：两次提交 `0957bc8`、`68ee7d2` 均 `exit=0`，提交后 `status` 空；
  - `locked3.log`：**一次完整变异电池**（`MUT-BUILD exit=0`／`MUT-TEST exit=1`／`RESTORE same=True`／
    `RES-BUILD exit=0`／`RES-TEST exit=1`）——绿半边当时没回来；
  - `locked6.log`：同一条电池重跑，**编译红**且红在 `src/triggers/routes.generated.ts`（他人在途的生成物，
    TS1002／TS1005 六行），`BASE` 与还原态一样红 ⇒ 按 `19-第一波派单附录.md` 的 `BASE-NOT-GREEN` 口径，
    那一轮**不构成变异结论**。
- 前任的停止点（`§七 STOP-POINT-271`）＝「补上还原后那次 `RES-TEST` 回绿的两行读数」。
  ⇒ 我判定：**源码交付已完成且已进仓；未完成的只有「绿半边那一格读数」**，本席的活就是把它跑出来并收尾。

### 8.2 前任测试件四条错断言是否真已改对（自行核对，不看它的结论）

`68ee7d2` 的提交信息称改对四条。本席在 **BASE 态**（同窗口先编译、后立刻跑）实测：
`node --test test/diet-list-t271.test.mjs` **exit=0**、主回归 `test/diet-homogeneity-108.test.mjs` **exit=0**。
⇒ 该件当刻是全绿的，四条断言修正确实落在仓里。

### 8.3 最终变异电池（同一持锁窗口内先编译、后立刻跑；两行可分辨读数）

**窗口 A**（`GATE-RUN runId=0a407936-53c9-447b-8085-71d0acd7ae07 cmd=pwsh -NoProfile -File .scratch/t271/my271.ps1`，
`waitedMs=72235`、`LOCK-RELEASED … exit=0`；原始读数在 `.scratch/t271/my271.log`）：

```
BASE-BUILD exit=1 errs=3    ← 3 条红全在范围外：exercise/records.ts、weight/history.ts、weight/log.ts
BASE-TEST file=test/diet-list-t271.test.mjs exit=0
BASE-TEST file=test/diet-homogeneity-108.test.mjs exit=0
… 12 条真跑逐条 exit=0 …
RESULT: 12/12
MUT-BASE   sha256=4976E1ABEEEE8FE588765568AA28DB18FE126242A221ABDAA5B8D6C6CED322DF
MUT-APPLIED sha256=39149E5ACD2268DCDFF02A37B7E72688E077D5D9F9C2C6B79A83154DA18AD0BC
MUT-BUILD exit=1 errs=2 ；MUT-EMIT dist-has-marker=True
MUT-TEST  exit=1（含 AssertionError 1 处）
RESTORE   sha256=4976E1ABEEEE8FE588765568AA28DB18FE126242A221ABDAA5B8D6C6CED322DF same=True
RES-BUILD exit=1 errs=2 ；RES-EMIT dist-has-marker=False
RES-TEST  exit=0
```

**窗口 B**（`GATE-RUN runId=3a4d597a-c4b3-4d7f-9fc7-13a0e5fb697c cmd=pwsh -NoProfile -File .scratch/t271/my271b.ps1`，
`waitedMs=0`、`LOCK-RELEASED … exit=0`；原始读数在 `.scratch/t271/my271b.log`）：

```
BASE-BUILD exit=1 errs=2（范围外：体重域两件）
BASE-TEST-TAP exit=0  # tests 6 | # pass 6 | # fail 0 | # cancelled 0
MUT-APPLIED sha256=39149E5ACD2268DCDFF02A37B7E72688E077D5D9F9C2C6B79A83154DA18AD0BC
MUT-BUILD exit=1 errs=2 ；MUT-EMIT dist-has-marker=True
MUT-TEST-TAP exit=1  # tests 6 | # pass 5 | # fail 1 | # cancelled 0
MUT-NOTOK not ok 1 - #271 骨架：15 行里该出的都出，明细长着备注列
MUT-MSG   error: '备注原文没上屏' ／ name: 'AssertionError'
RESTORE   sha256=4976E1AB…  same=True
RES-BUILD exit=1 errs=2 ；RES-EMIT dist-has-marker=False
RES-TEST-TAP exit=0  # tests 6 | # pass 6 | # fail 0 | # cancelled 0
```

**两行可分辨读数**：
`MUT-TEST-TAP exit=1 # pass 5 | # fail 1`（红）↔ `RES-TEST-TAP exit=0 # pass 6 | # fail 0`（绿）；
还原前后源码哈希一致（`same=True`），产物里的变异标记 `MUT-EMIT=True` → 还原后 `RES-EMIT=False`。

**这个红是脚本约定的红，不是脚本自身失败**：红的那一次测试运行**跑完了并出了完整 TAP 摘要**
（`# tests 6`），失败项是一条具名用例的断言 `AssertionError: 备注原文没上屏`，即我故意把明细表里的备注原文
换成假文案后，探针照约定抓住；不是崩溃、不是导入不到产物、不是环境错。

**编译红（三态都是 `tsc -b exit=1`、`errs=2`）的归属**：这 2 条**全在我声明路径之外**（体重域
`src/weight/history.ts(743,26)`、`src/weight/log.ts(326,26)`，均为 `DocPageInput` 缺 `subtitle` 的 TS2345），
当刻免锁 `npx tsc --noEmit -p packages/skill-calorie` 点名的也正是这 2 条、我的路径 0 条；
且 `tsc -b` 在报错状态下**仍产出**（`MUT-EMIT dist-has-marker=True` 证明变异确实进了产物、测试确实跑在
新产物上）。⇒ 就「我这条路」而言 BASE 是绿的：BASE 靶向两件 exit 0、12 条真跑 12/12 exit 0。
按 19 号附录的 `BASE-NOT-GREEN` 口径，我把**范围外那 2 条**单独记账（见 §8.9），不据此判变异轮作废——
因为本轮的两态读数是在**同一次持锁窗口内**、并且用「产物里有无变异标记」证过编译确已生效。

**一次作废的窗口（如实记账）**：`GATE-RUN runId=4994794c-fcaf-4ec2-b7fe-0f9831308211 cmd=pwsh -NoProfile -File .scratch/t271/my271b.ps1`
`exit=1`，原因是我的脚本第 49 行**自己写错了一个括号**（pwsh 解析期报错，未执行任何一行）⇒ 当次没有改任何文件、
没有编译、没有跑测试，属**空转**；改正后重跑即窗口 B。该条**不作门禁证据**。

### 8.4 11 条词 ＋ 场景 01 回归，逐条真跑（同一窗口 A，产物以刚编的 dist 为前提）

命令一律 `node packages/skill-calorie/dist/cli/cmd_read.js <命令名> --params <入参> --html <产物路径>`。
产物绝对路径：`D:\ilife\.scratch\t271\out\w01.html` … `w11.html`、`D:\ilife\.scratch\t271\out\r01.html`。
完整文档读数＝`doctype`（`<!doctype html>` 开头）／`charset`（`<meta charset="utf-8">`）／样式（共享样式已展开，
无 `<!--SHARED-CSS-->` 占位）／页内导航（`ilife-block-toc`）／来源脚注（含「数据来源」）／备注列（产物含 `>备注<`）。

| # | 词 | 命令名 | 入参 | exit | 字节 | 完整文档读数 | 备注列 |
|---|---|---|---|---|---|---|---|
| w01 | 看今日饮食 | `calorie.today` | `{"date":"今日"}` | 0 | 84725 | doctype1 charset1 样式1 导航1 来源1 | 有 |
| w02 | 看昨日饮食 | `calorie.view.diet` | `{"window":"昨日"}` | 0 | 80233 | 同上 | 有 |
| w03 | 看本周饮食 | `calorie.view.diet` | `{"window":"本周"}` | 0 | 86562 | 同上 | 有 |
| w04 | 看上周饮食 | `calorie.view.diet` | `{"window":"上周"}` | 0 | 98258 | 同上 | 有 |
| w05 | 看本月饮食 | `calorie.view.diet` | `{"window":"本月"}` | 0 | 123858 | 同上 | 有 |
| w06 | 看上月饮食 | `calorie.view.diet` | `{"window":"上月"}` | 0 | 160254 | 同上 | 有 |
| w07 | 看最近 7 天饮食 | `calorie.view.diet` | `{"window":"7d"}` | 0 | 101766 | 同上 | 有 |
| w08 | 看最近 30 天饮食 | `calorie.view.diet` | `{"window":"30d"}` | 0 | 165304 | 同上 | 有 |
| w09 | 看某段时间饮食 | `calorie.view.diet` | `{"window":"custom","start":"2026-08-01","end":"2026-08-31"}` | 0 | 160254 | 同上 | 有 |
| w10 | 看有备注的饮食记录 | `calorie.today` | `{"date":"今日","hasNote":true}` | 0 | 78314 | 同上 | 有 |
| w11 | 看今日饮食记录 | `calorie.today` | `{"date":"今日"}` | 0 | 84725 | 同上 | 有 |
| r01 | **场景 01 · 看今日饮食概览（回归）** | `calorie.view.diet` | `{"window":"今日"}` | 0 | 80953 | doctype1 charset1 样式1 导航1 来源1 | 有 |

`RESULT: 12/12`。空库态由探针直接打（`BUILD_VIEW` 传零记录输入，见 §8.5 第 3 条），两态都能开页。

### 8.5 备注列的结构化断言（不靠肉眼）

- `packages/skill-calorie/test/diet-list-t271.test.mjs`（6 张用例，逐条实跑 exit 0）：
  1. `#271 骨架：15 行里该出的都出，明细长着备注列` —— 明细**九列逐字**断言
     `['日期时间','餐别','食物','克数','热量','蛋白','碳水','脂肪','备注']`（末列＝备注）；
     六个导航锚点与 `id=` 落点逐一对上（不留死链接）；`—` 缺值口径；源码标识符不上屏。
  2. `#271 复制区：给了命令原文即真·日志按钮，第 4 段＝本次命令原文` —— 不给命令原文时按钮是公共层的
     `disabled` 态，不给死按钮。
  3. `#271 裁定 4／5：空窗仍出完整页；单点不成线；零值不画柱身`。
  4. `#271 餐别支／总览支：调用点给了取数才换页，区块走 #273／#275 的具名函数`。
  5. `#271 真跑：calorie.view.diet（窗口词）exit 0＋完整文档＋日志第 4 段＝命令原文` —— 断言
     `tableOf(html,'全部记录')` 末列＝`备注`。
  6. `#271 真跑：calorie.today 带 hasNote 的备注列＋标题写明筛选口径` —— 先经本技能自己的写命令落一条带备注的记录
     （不手改库），再断言标题含「只看有备注的」、备注原文上屏、`今日明细` 与普通今日页两张表的末列都是`备注`。
- 真跑的 12 条产物**逐条**含备注列表头（上表最后一列），与第 1／5／6 条断言互为两道。
### 8.6 场景 01「看今日饮食概览」回归读数（本票不能把它弄坏）

`r01`：`calorie.view.diet --params '{"window":"今日"}'` **exit=0**、产物 **80953 字节**、
`D:\ilife\.scratch\t271\out\r01.html`，完整文档五项读数全 1（doctype／charset／样式已展开／页内导航／来源脚注）。
该条与「看最近 7 天饮食」（w07）当刻在命令面**入参逐字相同**（两者都是 `{"window":"7d"}`／`{"window":"今日"}` 同一支），
命令这一层分不出是哪条词进来的；页面侧无入口标记即按普通窗口页出，**如实记账、不编差异**。

### 8.7 交接签名核对（只 import 调用，不重写——逐行回源）

| 具名函数 | 签名（交付方给的，逐字） | 我这边怎么用的 |
|---|---|---|
| `buildMealDistributionBlock` | `(v: MealDistributionView, command?: string): string`（`src/diet/reviewDocs.ts`） | `src/diet/todayDocs.ts:12` import、`:234` 调用 `buildMealDistributionBlock(v, command)` |
| `buildDietOverviewBlock` | `(v: DietOverviewView, command?: string): string`（`src/diet/nutritionPortDocs.ts`） | `src/diet/todayDocs.ts:13` import、`:258` 调用 `buildDietOverviewBlock(v, command)` |
| `MealDistributionView` | 类型 | `todayDocs.ts:16`／`dietDocs.ts:34` 只 import 类型，**没有自己写一份** |
| `DietOverviewView` | 类型 | `todayDocs.ts:17`／`dietDocs.ts:35` 同上 |

两支的页框（结论句槽／页内导航／`charts: true`）住 `todayDocs.ts` 的 `buildMealDistributionPage()`／
`buildDietOverviewPage()`；`dietDocs.ts:118`／`:120` 只在调用点给了取数时换页（`input.mealView !== undefined`／
`input.overviewView !== undefined`），不给即老路逐字不变。两条默认行为按 20 号附录 §二 落实：
`buildDietOverviewBlock` 不给 `command` 则不出复制区；`charts` 传 `true`。

### 8.8 未做项与下一手缺什么（接任席当刻复核后的更新）

1. **`calorie.view.diet` 的三处接线：餐别那支已由 #276 落在 `src/home/**`（在途、未提交）**。当刻盘上
   `packages/skill-calorie/src/home/today.ts` 已算出 `mealView` 并在给了 `meal` 时随 `command` 一起传进来
   （`...(mealView !== undefined ? { mealView, command: commandLine('calorie.view.diet', params) } : {})`）；
   `src/home/routes.ts` 也已给 5 条餐别词带上 `meal`。⇒ 本票**一个字没碰** `src/home/**`，这半边不再是本票的欠账。
2. **仍然缺的入口标记**：「看饮食总览」与「看最近 7 天饮食」当刻入参逐字相同（都 `{"window":"7d"}`），
   `overviewView` 那支**在命令层分不出是哪条词**；要接就得在 `src/home/routes.ts` 给入口带一个标记，
   那件不在本票声明路径内 ⇒ 转 #276／编排者。本票页面侧那一支已备好并有直测覆盖。
3. **窗口内零记录那一支**：`src/home/today.ts` 的 `viewDietOverview` 直接调 `buildDietOverview`，窗口零记录会抛
   `missing-data` ⇒ `exit 4`，与裁定 4「窗口为空出完整页＋空态句＋引导句」相冲；改点在 `src/home/**`（同属 #276）。
   本票页面侧的空态支已备好（`emptyGuide()`）并有直测覆盖。
4. **按日汇总「窗口天数 > 7 才出」（`t425-融合基准.md` §二 移植清单第 6 条＋裁定 5）本票仍未实施**，原因照旧：
   `packages/skill-calorie/test/diet-homogeneity-108.test.mjs:149` 用 **3 天窗口**断言产物里必须有「按日汇总」，
   而该件不在本票声明路径内（红线：不许改别人的件）。⇒ **请编排者裁定**：把该件转票给我收窄那条断言
   （改成「>7 天必须有、≤7 天不出」），或把这条移植项转给收口票。两条路我都能立刻执行。
5. **移植清单第 3／5 条的老页交互（点柱联动筛选、日期下拉）未移植**——`t425-融合基准.md` §二-补 已登记
   「不作验收项」，本票照此不实施。
6. **视觉由人眼看**：本票不出脚本化视觉判定；产物在 `D:\ilife\.scratch\t271\out\`，双击可开。

### 8.9 范围外发现（不修、不重跑，只记账）

1. `src/weight/history.ts(743,26)` 与 `src/weight/log.ts(326,26)`：TS2345，`DocPageInput` 当刻要求 `subtitle`，
   体重域两件没跟上（他人在途，两个文件当刻均为 ` M`）。本票一行未碰。
2. `src/exercise/records.ts(327,26)`：TS2554（窗口 A 的 BASE 出现过一次，窗口 B 已消失）——他人在途的闪动。
3. `src/triggers/routes.generated.ts` 当刻在树上是 ` M`（他人在途的未提交改动）；本票没动过它，也没跑
   `pnpm gen`／`pnpm build`／`pnpm gen:check`。
4. 前任撞上的那次生成物乱码（`locked6.log`：TS1002／TS1005 六行）当刻已不在——当刻该文件类型与语法都是好的。

### 8.10 提交清单（只这一处给全）

| sha | 提交信息 | 内容 |
|---|---|---|
| `0957bc8` | `feat(271): 条目列表页 11 条融合重做（② 类骨架逐行）＋ 备注列恒出 ＋ 三个可选入参 ＋ 逐条真跑与变异自证` | 三件源码 ＋ 测试件 ＋ 报告 ＋ `.changeset/t271-条目列表页.md`（6 件） |
| `68ee7d2` | `fix(271): 测试件四条写错的断言改对 ＋ 实施报告与交接件（停止点标记行）` | 测试件 ＋ 报告 ＋ 交接件（3 件） |
| `b12286c` | `docs(271): 收尾读数补齐——变异自证绿半边回绿（6/6 过）＋ 12 条真跑 12/12 复跑 ＋ 交接件更新（源码一行未改）` | 只追加本报告与交接件的读数（§八）；源码**一行未改**（2 件、224 行新增） |

声明路径收尾核对：`src/diet/todayDocs.ts`、`src/diet/today.ts`、`src/render/dietDocs.ts`、
`test/diet-list-t271.test.mjs`、`docs/skills/skill-calorie/t271-报告.md`、`t271-交接.md` 之外，
本票**没有**写任何件；`.changeset/t271-*` 已在上面的提交里。`src/home/**`、`src/diet/index.ts` 一行未碰。

**STOP-POINT-271（接任席收尾后更新）**：本节之后没有欠账；本票源码可在 HEAD 态复现——
复现命令＝持锁窗口内先 `npx tsc -b packages/base-render packages/skill-calorie`，
后 `node --test packages/skill-calorie/test/diet-list-t271.test.mjs`（exit 0），
再按 §8.4 表逐条跑真出口。

### 8.11 本次加锁运行的运行标识（与 `.scratch/locks/gate-runs.log` 一对一）

```
GATE-RUN runId=0a407936-53c9-447b-8085-71d0acd7ae07 cmd=pwsh -NoProfile -File .scratch/t271/my271.ps1
         （该次起始 waitedMs=72235、LOCK-RELEASED exit=0；窗口 A：编译＋靶向测试＋12 条真跑＋变异电池两态）
GATE-RUN runId=4994794c-fcaf-4ec2-b7fe-0f9831308211 cmd=pwsh -NoProfile -File .scratch/t271/my271b.ps1
         （exit=1：我脚本第 49 行自己的括号写错，pwsh 解析期即停，未执行任何一行、未改任何文件 ⇒ 空转，不作证据）
GATE-RUN runId=3a4d597a-c4b3-4d7f-9fc7-13a0e5fb697c cmd=pwsh -NoProfile -File .scratch/t271/my271b.ps1
         （waitedMs=0、LOCK-RELEASED exit=0；窗口 B：TAP 摘要版变异电池两态）
GATE-RUN runId=ad84d4cb-23ee-4a14-8f9e-540c92f12623 cmd=pwsh -NoProfile -File .scratch/t271/commit3.ps1
         （waitedMs=186732、LOCK-RELEASED exit=0；提交 `b12286c`，暂存区只含本票两件、`EXTRA-COUNT 0`）
```

**本节所在的那次提交**（只追加本报告）的标识另见 `gate-runs.log` 里 `ticket=271` 的最后一条 `pwsh -NoProfile -File .scratch/t271/commit4.ps1`。
