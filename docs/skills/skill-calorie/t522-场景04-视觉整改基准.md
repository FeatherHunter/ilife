# t522 · 卡路里场景 04 运动：39 页产物视觉整改基准（逐页对账 ＋ 六条判据 ＋ 五维尺）

> 票：[#522](https://github.com/FeatherHunter/ilife/issues/522)（Part of #156）。本件是这批产物页整改的**基准票**：只出基准与判据，**不改产品代码**。
>
> - **当刻 HEAD＝`4be6948`**；快照头写的是 `e84f924`（`.scratch/t522/snapshot-head.txt` 12:26:57 落盘）。两者差一个提交，`e84f924→4be6948` 只动 `docs/skills/skill-calorie/t155-链路导航.html` 一个文件（`git show --stat 4be6948`），diff 里 `packages/**` 与 `src/**` **0 处**，故当刻产物与当刻源码同源，快照口径不破。
> - **产物**：`.scratch/t522/out-baseline/**` 39 件（当刻源码重出，见 `.scratch/t522/run39-baseline.json`：39/39 `exit 0`、字节域 `[66647, 123470]`、全部 `<!doctype html>` 起 `</html>` 收）。
> - **冻结点**：本件落盘后，39 页的判据读数**以本件为准**；改后票（#523／#524／#525）复评一律与本件逐条对照，不看 08:41 旧版。
> - 包名提醒：`base-paint` ＝ `packages/base-render`（`packages/base-render/package.json` 的 `name` 就是 `base-paint`）。实施票找件时认目录 `packages/base-render/`，别去搜一个叫 `base-paint/` 的目录。

## 一、逐页对账表（39 行）

缺陷条目号见 §六 目录；`D1×n` 的 n 是**节点级**命中处数。

| # | 页名 | 族 | 渲染源件:行（build*Doc） | 产物字节 | 缺陷条目 |
|---|---|---|---|---|---|
| 01 | 01-记运动 | 回执·记 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 70201 | D1×9 D2=JSON+exercise_log D3×3 D4×4 D5(11px) D6 D7 D8×3 |
| 02 | 02-记运动（含备注） | 回执·记 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 70535 | D1×9 D2=JSON+exercise_log D3×3 D4×4 D5(11px) D6 D7 D8×3 |
| 03 | 03-记力量训练 | 回执·记 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 70800 | D1×8 D2=JSON+exercise_log D3×2 D4×4 D5(11px) D6 D7 D8×3 |
| 04 | 04-记有氧运动 | 回执·记 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 70876 | D1×9 D2=JSON+exercise_log D3×3 D4×4 D5(11px) D6 D7 D8×3 |
| 05 | 05-记日常活动 | 回执·记 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 70856 | D1×9 D2=JSON+exercise_log D3×3 D4×4 D5(11px) D6 D7 D8×3 |
| 06 | 06-补记运动 | 回执·记 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 70241 | D1×9 D2=JSON+exercise_log D3×3 D4×4 D5(11px) D6 D7 D8×3 |
| 07 | 07-批量补记运动 | 回执·记 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 71301 | D1×8 D2=JSON+exercise_log D3×4 D4×5 D5(11px) D6 D7 D8×3 |
| 08 | 08-复制昨日运动 | 回执·记 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 67772 | D1×6 D2=JSON+exercise_log D3×2 D4×3 D5(11px) D6 D7 D8×2 |
| 09 | 09-改运动记录 | 回执·改删 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 68567 | D1×6 D2=JSON+exercise_log D3×1 D4×4 D5(11px) D6 D7 D8×2 |
| 10 | 10-改某日运动 | 回执·改删 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 69737 | D1×6 D2=JSON+exercise_log D3×1 D4×4 D5(11px) D6 D7 D8×2 |
| 11 | 11-删运动记录 | 回执·改删 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 71329 | D1×8 D2=JSON+exercise_log D3×3 D4×5 D5(11px) D6 D7 D8×2 |
| 12 | 12-删某日运动 | 回执·改删 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 79596 | D1×8 D2=JSON+exercise_log D3×11 D4×4 D5(11px) D6 D7 D8×1 |
| 13 | 13-批量删运动 | 回执·改删 | `exercise/receipt.ts:257 buildExerciseReceiptDoc` | 79678 | D1×8 D2=JSON+exercise_log D3×11 D4×4 D5(11px) D6 D7 D8×2 |
| 14 | 14-看今日运动 | 记录明细 | `exercise/records.ts:250 buildRecordsDoc` | 69081 | D1×6 D2=JSON+exercise_log D3×3 D4×4 D5(11px) D6 D7 D8×1 |
| 15 | 15-看昨日运动 | 记录明细 | `exercise/records.ts:250 buildRecordsDoc` | 69081 | D1×6 D2=JSON+exercise_log D3×3 D4×4 D5(11px) D6 D7 D8×1 |
| 16 | 16-看本周运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 78985 | D1×8 D2=JSON D3×4 D4×6 D5(11px) D6 D7 D8×2 |
| 17 | 17-看上周运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 79812 | D1×8 D2=JSON D3×5 D4×6 D5(11px) D6 D7 D8×2 |
| 18 | 18-看本月运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 81234 | D1×8 D2=JSON D3×5 D4×6 D5(11px) D6 D7 D8×2 |
| 19 | 19-看上月运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 82168 | D1×8 D2=JSON D3×5 D4×6 D5(11px) D6 D7 D8×2 |
| 20 | 20-看最近 7 天运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 80029 | D1×8 D2=JSON D3×5 D4×6 D5(11px) D6 D7 D8×2 |
| 21 | 21-看最近 30 天运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 83289 | D1×8 D2=JSON D3×5 D4×6 D5(11px) D6 D7 D8×2 |
| 22 | 22-看某段时间运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 123277 | D1×8 D2=JSON D3×14 D4×6 D5(11px) D6 D7 D8×2 |
| 23 | 23-看今日运动（vs 目标） | 对照目标 | `render/sportDocs.ts:319 buildExerciseGoalDoc` | 66647 | D1×6 D2=JSON D3×5 D4×2 D6 D7 D8×2 |
| 24 | 24-看本周运动（vs 目标） | 对照目标 | `render/sportDocs.ts:319 buildExerciseGoalDoc` | 66652 | D1×6 D2=JSON D3×6 D4×2 D6 D7 D8×2 |
| 25 | 25-看运动记录（有备注） | 记录明细 | `exercise/records.ts:250 buildRecordsDoc` | 68736 | D1×6 D2=JSON+exercise_log D3×4 D4×4 D5(11px) D6 D7 D8×1 |
| 26 | 26-看运动记录（按力量筛选） | 记录明细 | `exercise/records.ts:250 buildRecordsDoc` | 68710 | D1×6 D2=JSON+exercise_log D3×3 D4×4 D5(11px) D6 D7 D8×1 |
| 27 | 27-看运动记录（按有氧筛选） | 记录明细 | `exercise/records.ts:250 buildRecordsDoc` | 72411 | D1×6 D2=JSON+exercise_log D3×8 D4×4 D5(11px) D6 D7 D8×1 |
| 28 | 28-看最近 60 天运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 96480 | D1×8 D2=JSON D3×8 D4×6 D5(11px) D6 D7 D8×2 |
| 29 | 29-看最近 180 天运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 123470 | D1×8 D2=JSON D3×14 D4×6 D5(11px) D6 D7 D8×2 |
| 30 | 30-看最近 365 天运动 | 运动汇总 | `render/sportDocs.ts:217 buildExerciseDoc` | 122880 | D1×8 D2=JSON D3×14 D4×6 D5(11px) D6 D7 D8×2 |
| 31 | 31-看运动类型分布 | 类型分布 | `render/sportPortDocs.ts:347 buildDistributionDoc` | 76726 | D1×6 D2=TDEE+exercise_log D3×9 D4×7 D5(11px) D6 D7 D8×2 |
| 32 | 32-看力量训练总览 | 力量总览 | `render/sportPortDocs.ts:150 buildStrengthDoc` | 75090 | D1×5 D2=JSON+exercise_log D3×5 D4×6 D5(11px) D6 D7 D8×3 |
| 33 | 33-看有氧训练总览 | 有氧总览 | `render/sportPortDocs.ts:251 buildCardioDoc` | 78153 | D1×6 D2=JSON+exercise_log D3×8 D4×6 D5(11px) D6 D7 D8×2 |
| 34 | 34-看运动趋势 | 趋势 | `render/sportPortDocs.ts:569 buildTrendDoc` | 84122 | D1×8 D2=JSON+exercise_log D3×4 D4×6 D5(11px) D6 D7 D8×2 |
| 35 | 35-运动复盘（本周） | 复盘 | `render/sportPortDocs.ts:476 buildRecapDoc` | 74135 | D1×10 D2=JSON+exercise_log D3×5 D4×7 D6 D7 D8×4 |
| 36 | 36-运动复盘（本月） | 复盘 | `render/sportPortDocs.ts:476 buildRecapDoc` | 74773 | D1×10 D2=JSON+exercise_log D3×5 D4×7 D6 D7 D8×4 |
| 37 | 37-运动复盘（最近 90 天） | 复盘 | `render/sportPortDocs.ts:476 buildRecapDoc` | 76868 | D1×11 D2=JSON+exercise_log D3×4 D4×7 D6 D7 D8×4 |
| 38 | 38-运动复盘（今年） | 复盘 | `render/sportPortDocs.ts:476 buildRecapDoc` | 75465 | D1×11 D2=JSON+exercise_log D3×4 D4×7 D6 D7 D8×4 |
| 39 | 39-运动复盘（自定义时间） | 复盘 | `render/sportPortDocs.ts:476 buildRecapDoc` | 75862 | D1×11 D2=JSON+exercise_log D3×4 D4×7 D6 D7 D8×4 |

**逐页读数**（分隔符／机器话／重复事实／字号／触摸区／溢出）落在 [`t522-改前读数.md`](./t522-改前读数.md)，本表只留条目号。

## 二、族 → 源件归属复核（票面给的初步分族，逐条核对）

| 族 | 页数 | 票面给的源件 | 复核结论 | 依据 |
|---|---|---|---|---|
| 写后回执（记）8 ＋（改／删）5 | 13 | `src/render/receipt.ts`＋`src/exercise/receipt.ts` | **成立**（**两件共同参与**，主装配在后者） | 13 页全部 `templates/crud_receipt.html`；主装配 `src/exercise/receipt.ts:257 buildExerciseReceiptDoc`（`packages/skill-calorie/src/exercise/receipt.ts:257`）；`src/render/receipt.ts` 是**跨场景共用零件**（`buildCrudReceipt:167`／`withM5:137`／`nowStamp:155`／`buildErrorReceipt:212`／`m5LineOf:103`）。**值路径说清**（别写成 `exercise/receipt.ts` 自己 import 了值）：`exercise/receipt.ts:26` 只取**类型** `CrudReceipt`；值来自写路径——`src/exercise/log.ts:13` 与 `src/exercise/edit.ts:15` 取 `withM5`，`src/shared/writeParts.ts:13` 取 `buildCrudReceipt` 并在 `:110` 调用它装配回执 |
| 运动汇总 10 ＋ 记录明细 5 | 15 | `src/render/sportDocs.ts::buildExerciseDoc`＋`src/exercise/records.ts::buildRecordsDoc` | **成立**（按模板分流） | 汇总 10 页＝`calorie.view.exercise`（16–22、28–30）→ `sportDocs.ts:217 buildExerciseDoc`；明细 5 页＝`calorie.view.exercise-records`（14、15、25、26、27）→ `exercise/records.ts:250 buildRecordsDoc`；两族同吃 `templates/exercise_summary.html`（15 页） |
| 对照目标 2 | 2 | `src/render/sportDocs.ts::buildExerciseGoalDoc` | **成立** | 23、24 两页 `calorie.view.exercise-goal` → `sportDocs.ts:319 buildExerciseGoalDoc`，模板 `templates/exercise_goal_view.html` |
| 力量 1／有氧 1／类型分布 1／复盘 5／趋势 1 | 9 | `src/render/sportPortDocs.ts`＋`src/render/exercisePort.ts` | **成立**（五个 `build*Doc` 逐一对上） | `sportPortDocs.ts`：`buildStrengthDoc:150`（32）／`buildCardioDoc:251`（33）／`buildDistributionDoc:347`（31）／`buildRecapDoc:476`（35–39）／`buildTrendDoc:569`（34）；取数走 `src/render/exercisePort.ts`（`buildStrengthView:107`／`buildCardioView:179`／`buildDistributionView:249`／`buildRecapView:316`／`buildTrendView:487`） |
| 票面另记的 `trendDocs.ts` 归场景 10 | — | — | **成立，且本图 39 页不经手** | 39 页的渲染链 9 个 `build*Doc` 全在 `exercise/**` 与 `render/**` 三件里，**无一处** import `render/trendDocs.ts`；#518 票面自己写着「含搬家 trendDocs→姊妹件」 |

**每页真正吃的共用块**（当刻 import 事实，逐族；件名逐条对着 `src/exercise/receipt.ts:20–38`／`src/exercise/records.ts:32–44`／`src/render/sportDocs.ts:27–49`／`src/render/sportPortDocs.ts:30–59` 抄）：

| 族 | 共用块（本族装配件实际 import 的） |
|---|---|
| 回执·记／回执·改删（`exercise/receipt.ts`） | `base-paint/blocks`（`renderCaliberLine`／`renderChangeRows`／`renderDataTable`／`renderDisclosure`／`renderKpiGrid`／`renderPreBlock`／`renderTocBlock`）、`base-paint`（类型）、`src/shared/docPage.ts`（`assembleDocPage`）、`copyArea.ts`（`copyArea`／`copyLog`）、`operationHead.ts`（`operationHead`）、`receiptParts.ts`（`reconcileDisclosure`／`statusCard`）、`fieldLabel.ts`、`sourceLine.ts`、`writeParts.ts`（`SOFT_EXCLUDED`）、`src/render/copy.ts`、`src/analysis/utils.ts`（`EX_ALIVE`）、`src/exercise/fieldLabels.ts`；**值路径另有** `src/render/receipt.ts`（经 `exercise/log.ts:13`／`exercise/edit.ts:15` 的 `withM5`，与 `shared/writeParts.ts:13→:110` 的 `buildCrudReceipt`） |
| 记录级明细（`exercise/records.ts`） | `base-paint/blocks`、`src/render/exercisePort.ts`（`listPortRows`）、`docPage.ts`（`assembleDocPage`／`metricsOf`）、`copyArea.ts`、`emptyGuide.ts`、`params.ts`（`defaultRange`／`fail`／`nums`／`optStr`）、`src/exercise/sportUi.ts`（`capsStrip`／`exerciseUiCss`／`factStrip`／`fmtNum`／`windowStrip`）、`src/render/errors.ts`（`CalorieRenderError`） |
| 运动汇总（`render/sportDocs.ts::buildExerciseDoc`） | `base-paint/blocks`、`base-paint`（`escapeHtml`）、`docPage.ts`、`copyArea.ts`（`copyArea`／`copyLog`）、`emptyGuide.ts`、`writeParts.ts`（`commandLine`）、`src/render/receipt.ts`（`nowStamp`）、`src/exercise/sportUi.ts`、`src/exercise/exerciseStore.ts`（`inferCategory`） |
| 对照目标（`render/sportDocs.ts::buildExerciseGoalDoc`） | 同上（同一件、同一批 import；取数走 `planPlate.ts` 的 `ExerciseGoalView`） |
| 分布／力量／有氧／复盘／趋势（`render/sportPortDocs.ts`） | `base-paint/blocks`、`base-paint`（`buildDataText`／`buildLogText`）、`docPage.ts`、`copyArea.ts`（`copyArea`／`copyLog`／`dataCopyArea`）、`emptyGuide.ts`、`sourceLine.ts`、`src/render/receipt.ts`（`nowStamp`）、`src/exercise/categoryColors.ts`（`categoryColor`）、`src/render/exercisePort.ts`（类型） |

`src/shared/` 当刻 12 件：`commandSpec`／`conclusionLine`／`contentSuffix`／`copyArea`／`docPage`／`emptyGuide`／`fieldLabel`／`operationHead`／`params`／`receiptParts`／`sourceLine`／`writeParts`。

## 三、路径所有权（实施票的写集边界）

**写进实施票票面的四条铁律**：

1. **公共层只许读、不许顺手改**：`packages/base-render/**`（＝包名 `base-paint`，含 `src/blocks.ts` 与 `assets/help-template.html`／`src/helpShell.ts`）归 **#507**（移动端底座：`viewport-fit`／安全区／触摸目标／减动效／`dvh`）与 **#513**（三处加槽：表格行标记／页面级段标题产出器／卡片徽章贴底）；`packages/skill-calorie/src/shared/**` 同属公共层，需要改就在实施票里**停下来报编排者转票**。
2. **分隔符口径**归 **#508**（立规：可见文本不得用 `·`／`；` 硬串并列）——实施票照它的节点级口径复评，不自己发明阈值。
3. **本图自己的件**：`src/exercise/receipt.ts`（回执族）／`src/render/sportDocs.ts`＋`src/exercise/records.ts`（汇总·明细·对照目标族）／`src/render/sportPortDocs.ts`＋`src/render/exercisePort.ts`＋`src/exercise/*`（分布·力量·有氧·复盘·趋势族）。**三族三张票，谁也不许改别人的件**（#523 票面已写死这条）。
4. **不经手**：`src/render/trendDocs.ts`（#518 在飞）／`src/render/reviewDocs.ts`（计划复盘族，不在本图）／其它场景的件与测试／`.scratch/t268-link/out/**`（改前基线）／`.scratch/t522/**`（本基准件，只读）。

超线提醒（本包告警线 350 LF，源 `packages/skill-calorie/AGENTS.md` 台账）：`src/render/sportPortDocs.ts` **666**、`src/render/exercisePort.ts` **544**、`src/render/sportDocs.ts` **351**、`src/render/trendDocs.ts` 1019。动它们即触发必报五步第四步，实施票要在交付里写明「已超线，需要根据规则进行重构」＋拆法。

## 四、六条要求 → 可跑真假的判据（红绿两向）

判据聚合件：`.scratch/t522/redgreen.mjs`（它只聚合，不新造口径）。红绿读数落 `.scratch/t522/redgreen-{products,sample,sample-red}.json`，逐条 `GATE-RUN runId` 见 [`t522-证据.md`](./t522-证据.md)。

| # | 用户要求 | 判据（命令 ＋ 阈值） | 当刻 39 页 | 手工合格样板 | 负向对照样张 |
|---|---|---|---|---|---|
| J1 | ① 需要自适应桌面端和手机端 | `node packages/skill-calorie/scripts/measure-responsive.mjs --dir <页目录> --widths 390,768,1440 --json <out>`；口径＝每页每档 `overflow=0`（`docScrollWidth−innerWidth`）且 `unfitted=0`（图放得下）且 `ok=true` | **绿**（117/117 格 0，红格 0）→ **该项现状已达标** | 绿（3/3 格） | **红**（390／768 两档溢出，`div.wide` 926px） |
| J2 | ⑤ 用「；」「·」分割代表该做 UI 设计 | `node .scratch/sep-audit/probe.mjs <页> --json`（#508 节点级口径，exit 0＝干净）；阈值＝每页 `node.R1=R2=R3=0` 且 `line` 命中 0 | **红**（节点级 **303** 处，干净页 **0/39**） | 绿（0 处，exit 0） | 绿（非该判据的负向面） |
| J3 | ④ 文字不能出现冗余和不合理 | `node .scratch/t522/readings.mjs <页目录> <json> <txtdir>`（`visible-text-probe` 口径）；阈值＝每页 `machineWords` 命中 0 且 `jargon` 0 且 `dupFacts` 0 | **红**（机器话 **39/39** 页、重复事实 **39/39** 页；`wording.md` 硬禁用词 0 命中＝该子项已达标） | 绿（三项全 0） | 绿（非该判据的负向面） |
| J4 | ② 手机端参考 HELP | `node docs/skills/skill-calorie/t516-判据-版式.mjs --dir <页目录> --widths 390,768,1440 --json <out>` ＋ 静态 CSS 扫描；阈值＝390 档字号下限 ≥12px、触摸目标不足 0 处、越界元素 0，且页内有 `viewport-fit=cover` 与 `env(safe-area-inset-*)`，断点集 ⊆ {500,820} | **红**（触摸区不足 **39/39** 页、字号 <12px **32/39** 页、`viewport-fit` **0/39**、安全区 **39/39** 页每页 1 处但只有 `bottom` 方向且缺 `viewport-fit` 故等于 0） | 绿（字号 13px、触摸 0、两项均有、断点 820／500） | **红**（字号 11px、触摸 1 处 `button@84x30`、两项皆无） |
| J5 | ③ UI 代码层符合设计师审美 | 五维尺（§五）＋ 视觉席逐页给分；**改后目标 ≥90** | 当刻 10 页抽样：**族级平均 54 分**（最高 61＝01 记运动，最低 33＝23 vs 目标）；逐页分见 [`t522-改前读数.md`](./t522-改前读数.md) §四 | — | — |
| J6 | ⑥ 用 vision_router 仔细审查 | `dsh --profile visionreview "读取 <brief 绝对路径> 并严格按其中要求执行"`；brief＝`.scratch/t522/vision-brief-baseline.md`，10 页 × 2 档（1440×900／390×844）整页截图＋分段细看＋逐页五维给分 | 已跑完：`runId=b2c77c96-…` `exit=0`（`START 04:40:49Z → RUN 05:04:57Z`），产物 `.scratch/t522/vision-baseline.md`；读数与抽页核对见 [`t522-证据.md`](./t522-证据.md) §五 | — | — |

**HELP 侧出处（J4 的比对基准，逐条给 文件:行）**：

| 条目 | HELP 侧事实 | 出处 |
|---|---|---|
| 断点组 | 只有 **500／820** 两个边界：`@media(min-width:501px)`×1、`@media(max-width:500px)`×1、`@media(max-width:820px)`×4 | `packages/base-render/src/helpShell.ts:42`（前缀 CSS 整段一行）、`:45`（控件库 CSS）；`packages/base-render/assets/help-template.html:119`／`:156`／`:271`／`:1405`／`:1453`／`:1638` |
| 字号下限 | 正文 `14px`；用到的最小字号 `8px`（眉标那类）；常见档 10.5／11／12／12.5／13／13.5／14 | `help-template.html:10`（`font-size:14px`）、`:41`（`8px`）、`:123`（`10px`） |
| 触摸目标 | `min-height:44px` 在册（主按钮／列表项）；另有 40／46／38／36／30／28／26／18 等档 | `help-template.html:58`／`:97`／`:1187`／`:1265`／`:1635`／`:1640`；`helpShell.ts:42`（`min-height:44px` 之一） |
| 安全区 | **HELP 有** `env(safe-area-inset-*)`：`bottom` 方向 **5 处**（模板 4 行）；**没有** `viewport-fit=cover`（`<meta name="viewport" content="width=device-width, initial-scale=1.0">`，全文件 0 处） | `help-template.html:117`／`:133`／`:154`／`:271`；`helpShell.ts:42`（meta 与 CSS 同段）。39 页产物**同样有**：每页 1 处 `bottom: calc(24px + env(safe-area-inset-bottom, 0px))`，`viewport-fit` 0/39 |
| 等宽数字 | `tabular-nums` 1 处（窄屏块里） | `help-template.html:271`；`helpShell.ts:45` |
| 窄屏表格 | 宽表横滚：`overflow-x:auto` ＋ `touch-action:pan-x` | `help-template.html:90`／`:117`；`helpShell.ts:42` |
| 减动效（顺带核对） | 39/39 页**都已带** `@media (prefers-reduced-motion:reduce)`——这一条 #507 的清单里已经在页上，实施票不用再补 | 静态扫描 `.scratch/t522/redgreen-products.json` 的 `J4.css` |

⇒ **J4 的口径对 HELP 自己也不全绿**（`viewport-fit` 一条 HELP 也没有；而没有 `viewport-fit=cover` 时，`env(safe-area-inset-*)` 在 iOS 上解析为 0，安全区实际等于没做）。安全区本身**两侧同档**：HELP 5 处、39 页每页 1 处，都只做 `bottom` 方向。所以 J4 的写法是：**断点组／字号下限／触摸目标／窄屏表格四条照 HELP 比＝判红**，**`viewport-fit` 一条是「比 HELP 更严」的加分项**，兜口归 **#507**，不计入实施票的红线。这条必须写进实施票票面，避免实施票去改公共层。

> **读数订正（收尾席，2026-09-15）**：本行初稿写「安全区 HELP 自己也没有、全文件命中 0」，与机器日志 `J4b … viewport-fit 页=0/39  安全区页=39/39`（`.scratch/t522/logs/redgreen-products.log`）相矛盾；回原文核对后改写为上表（HELP＝`help-template.html` 5 处、产物＝39/39 页每页 1 处）。**判红结论不变**，变的是「缺什么」——缺的是 `viewport-fit=cover`。

## 五、五维尺与权重（100 分制，改后目标 ≥90）

沿用先例（#516 的尺，出处 `docs/skills/skill-calorie/t516-判据-版式.mjs` 件头 ＋ `.scratch/t401d/review-ui/审查报告.md`）：

| 维 | 权重 | 判据（这一维量什么、怎么量） |
|---|---|---|
| 结构纪律（栅格／tnum／缺值／色 token） | 20 | 内容列居中收得住（`t516` 探针的 `shell.w`／`vw`）；数值列 `font-variant-numeric: tabular-nums`（探针 `tableTd`）；缺值统一写「—」；颜色只取少数 token；间距走 4／8 倍数 |
| 信息层级与版式细调 | 25 | 主角数字是否突出（KPI 值字号 ≥ 正文 1.6×）；同级标题字号一致（探针 `fontSizes`＋`minFontOwner`）；卡／表／图同列对齐；无「一片灰小字」（长行 ≥50 字的整段可见文本数＝`longLines50`） |
| 分隔符与文字纪律 | 20 | **J2 ＋ J3 两条机器判据直接判**（本节 §四） |
| 跨宽自适应 | 15 | **J1** 三档零溢出；1440 宽下内容列是「更好」不是「更空」；390 宽下表格／卡片／图读得开、无孤行 |
| 手机端与 HELP 同档 | 20 | **J4** 五条（断点组／字号下限／触摸目标／窄屏表格／`viewport-fit` 加分） |

**改后目标**：加权总分 **≥90**，且**五维里没有一维低于该维的 80%**（避免堆总分掩盖单维塌陷）。逐页分数与族级基线分见 [`t522-改前读数.md`](./t522-改前读数.md)。

## 六、缺陷条目目录（逐页对账表里的编号）

| 号 | 条目 | 口径（怎么判） | 当刻命中 |
|---|---|---|---|
| D1 | 分隔符硬串（`·`／`；`／≥3 段并列） | `.scratch/sep-audit/probe.mjs` 节点级（#508） | 39/39 页，节点级 303 处 |
| D2 | 机器话上屏（库表名／常量名／文件名／命令键） | `visible-text-probe.mjs` 的 `machineWords` 四类正则 | 39/39 页；`exercise_log` 27 页、`JSON` 38 页、`TDEE` 1 页 |
| D3 | 同一事实一页多处 | `readings.mjs` 的 `dupFacts`（数字＋单位型事实同页 ≥2 次） | 39/39 页，合计 209 项 |
| D4 | 触摸目标 <44px | `t516-判据-版式.mjs` 的 `touchSmall` | 39/39 页（390 档 197 处、1440 档 392 处）。390 档**全部是页内锚点 `<a>`**，高度一律 34px；1440 档另有复制控件 `button.ilife-copy-menu-item` 71 处、`button.ilife-copy-btn` 42 处（高 40px） |
| D5 | 字号下限 <12px | 同上 `minFontPxNoSvg` | 32/39 页（下限 11px；责任人 `th.ilife-block-data-table-cell-left` 32 页、`p.ilife-block-page-shell-eyebrow` 7 页） |
| D6 | 缺安全区／`viewport-fit` | 静态 CSS 扫描（`redgreen-products.json` 的 `J4.css`） | `viewport-fit` 缺 **39/39** 页；安全区 39/39 页**各 1 处**但只有 `bottom` 方向、且缺 `viewport-fit=cover`，故 iOS 上实际避让为 **0** |
| D7 | 断点不成组 | 静态 CSS 扫描（断点集 ⊆ {500,820}） | 39/39 页：实测断点集＝**400／640／641／720／721／820／821／876 八个边界**（缺 HELP 的 **500** 那条；820 那条在册） |
| D8 | 长行（整段可见文本 ≥50 字） | `readings.mjs` 的 `longLines50` | 39/39 页，合计 90 行 |

## 七、实施票认领与遗留出口

- **#523**（已开）＝运动汇总 10 ＋ 记录明细 5 ＋ 对照目标 2 ＝ **17 页**；写集 `src/render/sportDocs.ts`／`src/render/exercisePort.ts`／`src/exercise/records.ts`／`src/exercise/*`。
- **#524／#525**（**票面已按此编号分工，当刻仓库里查无此票**）＝#523 票面原话「另两族分票：#524（回执族 13 项），#525（力量·有氧·分布·复盘·趋势族 9 项）」。核对命令：`gh issue view 524`／`gh issue view 525` 当刻返回 `Could not resolve to an issue or pull request`。⇒ **回执族 13 页与分布·力量·有氧·复盘·趋势族 9 页当刻无实施票**，需编排者按此编号开票后回填本基准。
- **#521**（已开）＝`t268-链路总览.html` 那一页本身的整改，与本图 39 页产物页**不是同一批**，别混。
- **公共层**：#507（移动端底座）／#508（分隔符立规）／#513（三处加槽）——实施票不许碰，需要时转票。
- **跨票缺陷**（已在飞的，实施票照它的口径走、别重复开）：#470（来源行口径：`exercise_log` 上屏 27 页）／#477（载荷头口径：汇总与对照目标两页仍印命令键）／#460（对照目标页「共 N 条」实为窗内天数）／#478（示例参数 `id:1` 任何真库不可达）／#486（字段→中文绑定判据）／#506（两条 vs 目标词的预检确认页）。
- **J2／J3 的复评口径**：实施票改完必须跑 §四 的 J2／J3 命令，判据由红转绿，读数落实施票自己的 `.scratch/t5xx/**`。
- **视觉终审票**：**未开**。基准落地后建议由编排者开一张终审票，对本图 39 页三族各抽页做 ≥90 复评。
- **视觉验收墙**（口径件 `docs/agents/视觉验收墙.md`）：**本图自身不命中触发条件**（#522 只出文档、不动 HTML／UI）；但本图给 **#523／#524／#525 切了票**，那三张动 `packages/skill-calorie/src/**` 的页产出，**命中触发条件＝整图带墙**。故编排者在那三张票之后要**留一张收口票**：验收命令＝墙生成器的自检命令（`node <生成器> <产物目录> <输出名>`，**正例 `exit 0` ＋ 反例 `exit 1` 点名缺件**都要走过），交付物＝**墙（手机 390 宽 ／ 桌面 1280 宽 两张）＋ 逐格缺陷清单 ＋ 自检退出码**，三样齐才算验收。**产物册子（份数、名称规则、产物目录）当刻未冻结**——现成底子是 `.scratch/t522/gen/截图清单.md` 与 `.scratch/t522/readings-baseline.json`（39 行逐页）＋当刻 39 件产物 `.scratch/t522/out-baseline/**`，但那是**基准抽样件、不是发布名册**；册子由实施票在票面里定死，生成器照 `docs/skills/skill-calorie/t154-mobile-wall.mjs` 起（照抄时补上 `dropped` 判据，别带那处假绿灯）。
- **范围外缺陷**（不在实施票写集内、但读数里看得见的一条）：`JSON` 机器话 38/39 页来自复制区的格式菜单（`纯文本／JSON／CSV`），**08:41 那版没有**（`.scratch/t522/logs/batch-diff.log`：机器话新增页 38、新增词 `JSON`）。它属公共层复制控件的格式选项面，归 #507／#513 一族判，实施票**不要**自己在页里删这个选项。
