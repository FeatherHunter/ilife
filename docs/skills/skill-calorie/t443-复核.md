# #443 独立复核（复核席，与实现换人；只信提交号与自己复跑）

## 1 判定与票面对照

- 票面（`gh issue view 443` 五段）：目标＝收紧 #361 复核点名的两条 S3（①全空行真出口断言 ②载荷逐字段口径），不改行为；验收＝三条（靶向 8 用例真出口＋逐字段＋备注—保留；M3 全文复位必红/还原必绿；M4 删新用例计数减1/还原复原）；不许动＝`src/**` 行为、13 唤醒词/命令名/示例、真库零写入、排 #440 之后；交付＝测试就地收紧＋新建 `t441-判据收紧证据.md`；遗留＝触发器是否挂 UPDATE 另议、中文名归 #440。
- 判定：**PASS**。两条口径均收紧且独立复跑自证齐；`src/**` 零改动；真库零写入。量刑：无 S1；五维 契约30/证据25/对照20/红线15/文档10 ＝ 29/24/19/14/9，均分约 95（≥85）。
- 结构纪律：复核只新增本件＋`.scratch/t443/review-*`（草稿，已忽略不进仓）；实现文件一行不碰。

## 2 机器证据（独立持锁重跑，不采信实现结论；对账源 `.scratch/locks/gate-runs.log`）

实现提交（只认 sha）：`5d9a6ba`（测试收紧 1 件 `+81/-13`）＋ `e7f7e7d`（证据＋changeset 2 件新建）。`git show --stat` 已验；`git diff 5d9a6ba^ HEAD -- packages/skill-calorie/src/ --stat` 为空。

GATE-RUN（`node tooling/run-locked.mjs --ticket 443 -- …`，长命令落盘＋尾 5 行）：

GATE-RUN runId=fa9a812d-1e31-417e-8473-12393296dde2 cmd="node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs"
GATE-RUN runId=673a6bc0-ef81-460e-aafd-e468b86644eb cmd="node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs"
GATE-RUN runId=6212b786-8ce8-492d-998c-bdad168e3fa8 cmd="node .scratch/t443/mut-battery.mjs"
GATE-RUN runId=8b8d415d-3a35-4123-b615-7a9f1c5cea97 cmd="node .scratch/t443/mut-battery.mjs"
GATE-RUN runId=90aad0bc-194c-475a-9e51-4453405f35ca cmd="node .scratch/t443/review-mut-new.mjs"

| 运行 | runId | exit | 读数 |
|---|---|---|---|
| 靶向重跑① | `fa9a812d` | 0 | `tests 8 / pass 8 / fail 0`，`T443-READOUT empty-row=13/13 via=shim`，`T361-READOUT names=13/13`（日志 `.scratch/t443/review-target.log`） |
| 靶向重跑②（干扰后恢复验证） | `673a6bc0` | 0 | 同上 `tests 8 / pass 8 / fail 0`（日志 `review-target2.log`） |
| 实现电池重跑①（撞并发，见下） | `6212b786` | 1 | `MUT-443 RESULT=FAIL`：M3 `exit=1 fail=7`／还原 `exit=1 pass=1`；M4 `pass=1`／还原 `exit=1 pass=1`；`RESTORE-IDENTICAL=true`（日志 `review-mut-impl.log`） |
| 实现电池重跑②（恢复后） | `8b8d415d` | 0 | `MUT-443 RESULT=4/4`：M3 全文复位 `exit=1 fail=1`／还原 `exit=0 pass=8`；M4 删新用例 `exit=0 pass=7`／还原一致＋`exit=0 pass=8`（日志 `review-mut-impl2.log`） |
| 自设新探针 N1（盲区） | `90aad0bc` | 0 | `REVIEW-443 N1-field-dash-exit=1 N1-field-dash-fail=1 N1-EXPECT=red-OK`／`N1-restore-exit=0 N1-restore-pass=8 N1-note-kept=true RESTORE-IDENTICAL=true RESULT=2/2`（日志 `review-mut-new.log`，脚本 `review-mut-new.mjs`） |

- `6212b786` 说明（范围外干扰，不归本票）：该窗与 `ticket=489` 的 `src/fetch/body.ts` 提交（`27ac615d`，14:25:11，注释删锚点无行为）及 `ticket=253F2` 的 `--force` 重编/变异窗（14:24:24–14:25:10）相邻；电池内还原后仍 `pass=1` 但 `RESTORE-IDENTICAL=true` 且测试文件 `git diff` 为空，靶向②随即回绿，电池②亦回绿 `4/4`。判为并发窗口污染，非本票缺陷（归属见 §4）。
- 新探针 N1（被审脚本覆盖不到）：实现电池未重跑旧 M2（载荷字段写 `—` 回归）。N1 往 `items[0].chest_cm` 注入 `'—'` 模拟源码回归→逐字段必红（备注 `r1—d` 的 `—` 不连带），还原→必绿。红绿两行齐，证逐字段有区分力（误伤/漏报两不犯）。
- 真库零写入：`D:\2Study\StudyNotes\.db\calorie_data.db` 开工前＝完工后 `LEN=3072000 SHA256=D40D58F50E74E294A11E410B4D9E2142FCBCD8993328A4EFB453F1A0D62A1083`（一切运行 `SKILLS_DB_PATH` 指自建 tmp 库）。
- `src/**` 零改动：两实现提交仅含测试＋文档/记账；复核全程 `git diff -- <实现件>` 为空（变异均 `finally` 写回＋sha 校验）。

## 3 逐条路径

- 改动件（实现）：`packages/skill-calorie/test/t361-measure-full-table.test.mjs`（R1 备注 `'r1'`→`'r1—d'`＋载荷全文断言→13 字段循环＋`r1.note` 原样保留＋合成直调→真出口新用例，用例数 8→8；`git diff --numstat` 为 `81/13`）。
- 证据件（实现）：`docs/skills/skill-calorie/t441-判据收紧证据.md`（新建）＋记账 `.changeset/t443-判据收紧.md`（新建唯一，`'skill-calorie': patch`）。
- 复核件（本席）：本件 `docs/skills/skill-calorie/t443-复核.md`（新建，唯一入仓件）；草稿 `.scratch/t443/review-mut-new.mjs`＋`review-target.log`／`review-target2.log`／`review-mut-impl.log`／`review-mut-impl2.log`／`review-mut-new.log`（不进仓，目录已忽略；可复跑脚本为 `review-mut-new.mjs`）。
- 锁：全部编译/测试/提交经官方 `tooling/run-locked.mjs`（RUN 行见 §2，无 `add -A`／stash／amend／push；提交前 `git diff --cached --name-only` 只含本件）。

## 4 未做项与下一手（归属标注）

- 【本票范围·已验】两条收紧口径＋M3/M4＋N1 红绿齐；`6212b786` 一次干扰已恢复并留痕，不阻断。
- 【本票范围·未做】#361 原证反向指针未加：写集不含他人件（`t361-实施证据.md`），跨界停手。请编排者在原证末追加指本件/实现证据的指针，或具名授权补加。
- 【范围外·S3＋转票措辞】并发窗口下靶向/电池读数可被邻票重编短暂污染（本席实测一次，`RESTORE-IDENTICAL=true` 仍红，恢复后绿）。建议编排者知悉即可，不另开票；如下一轮再现，请为判据票配“重跑一次确认”口径。另：触发器是否挂 `BEFORE UPDATE`、中文名归 #440，均按票面遗留出口转交，不在本票。
- 未 push，由编排者统一收口。
