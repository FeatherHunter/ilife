# t167 · 场景 04 运动：边界、页面清单与公共层增量

- 票：**#167**（地图 **#156**）· 2026-09-13 定稿。
- 证据基线：`docs/skills/skill-calorie/t167-老技能对账表.md`（39 条逐词对账 ＋ 8 张老模板实物）＋ 本文件 §四的实跑探针。
- 参照对象：**地图 #152（场景 07 基础信息）**——只引用它的判法与路数，**不处理那张图的账**（用户 2026-09-13 裁定）。
- 本文件**只定边界、归类与接口**，不定长相；长相由各页面票决定。

## 〇、用户 2026-09-13 的三条核心思想（本图与后续各图共用）

1. **`SKILL.md` 的完善与开发要符合 `/writing-for-agents`**：单一来源、渐进披露、去掉 no-op、正面表述、领头词；命令表与工作流程表**不许手工维护两处**。
2. **目标管理相关的新仓代码是 AI 开发的残次品**：按**老技能实物重构**，不在新仓现有实现上打补丁；能复用的既有件不重造。（该条落在**地图 #153**，本图照同一路数办事。）
3. **需要新增唤醒词就新增、需要新增命令就新增、需要改动就改动**：核心目标是 **HELP HTML 里每个场景都能找到它的底层唤醒词，且 AI 知道该从该唤醒词的哪个工作流程执行下去**。

## 一、对账结论（39 条逐词见 `t167-老技能对账表.md`）

| 项 | 结果 |
|---|---|
| 唤醒词数 | **39**（新仓、老表、老 `scene_data` 三份逐字逐序相同，命令编号 0 差异） |
| 老技能做过 | **39 / 39**；「必须多步交互」0 条、「新架构划出去不做」0 条、「老技能也没做过」0 条 |
| 老模板实物 | 8 个**全部存在**（回执 26,279B／汇总 11,856B／复盘 6,652B／对照目标 7,662B／分布 13,875B／力量 6,302B／有氧 5,003B／趋势 7,117B） |
| 老实现脚本 | 8 个：`render_exercise_receipt.py`、`render_exercise_summary.py`、`render_exercise_{goal_view,distribution,strength,cardio,trend,recap}.py` |

**结论：本图不补业务命令、不推翻任何老能力。** 欠账全在「页面产物」与「唤醒词 → 工作流程的接线」两处。

## 二、实跑探针（2026-09-13，临时库，不碰真库）

12 条命令实跑，退出码全 0。脚本 `.scratch/t167/probe-live.mjs`，产物在 `%TEMP%\t167-probe-*`。

| 现况 | 条数 | 实测 |
|---|---|---|
| 写类（写后回执） | **13** | 产物是**片段**：252／282／335 字节，`<section class="ilife-page">` 起，无 doctype、无 charset、无样式、无复制区 |
| 读类 | 22 | 已是**完整文档**：62,937–69,230 字节，doctype＋charset＋样式段＋脚本段＋三格式复制菜单（各 8 处）齐 |
| 读类·**命令指错页** | **7** | 看运动类型分布／看运动趋势／运动复盘×5 → 全指到「运动总览」，而专面页已存在且实跑能出完整文档（67,617／68,577／65,795 字节） |
| **没有命令可执行** | **1** | 看运动记录（有备注） |

## 三、页面清单：39 条唤醒词 → 9 类页面

| 类 | 服务哪些唤醒词 | 老技能实物 | 新仓产出者 | 本图要做的 |
|---|---|---|---|---|
| **A 写后回执页** | 13 条写词（记运动／记运动（含备注）／记力量训练／记有氧运动／记日常活动／补记运动／批量补记运动／复制昨日运动／改运动记录／改某日运动／删运动记录／删某日运动／批量删运动） | `crud_receipt.html` | `src/cli/write.ts` 的 `receiptHtml()` 片段 | **整页化**（走公共出口，见 §五） |
| **B 运动汇总页** | 10 条窗口词（看本周／上周／本月／上月／最近 7／30／60／180／365 天／看某段时间运动） | `exercise_summary.html` 的 `--mode summary` | `calorie.view.exercise` | 已跑通；内容按老规格逐项核（含降采样） |
| **C 记录级明细页** | 5 条（看今日运动／看昨日运动／看运动记录（有备注）／（按力量筛选）／（按有氧筛选）） | `exercise_summary.html` 的 `--mode records` ＋ `--today/--yesterday/--has-note/--category` | **无** | **新建**（含那 1 条没有命令可执行的词） |
| **D 对照目标页** | 2 条（看今日运动（vs 目标）／看本周运动（vs 目标）） | `exercise_goal_view.html` | `calorie.view.exercise-goal` | 已跑通；内容按老规格核 |
| **E 类型分布页** | 1 条（看运动类型分布） | `exercise_distribution.html` | `calorie.view.exercise-distribution`（**已存在，路由没指**） | 改路由 |
| **F 力量总览页** | 1 条（看力量训练总览） | `exercise_strength.html` | `calorie.view.exercise-strength` | 已跑通；内容按老规格核 |
| **G 有氧总览页** | 1 条（看有氧训练总览） | `exercise_cardio.html` | `calorie.view.exercise-cardio` | 已跑通；内容按老规格核 |
| **H 趋势页** | 1 条（看运动趋势） | `exercise_trend.html` | `calorie.view.exercise-trend`（**已存在，路由没指**） | 改路由 |
| **I 复盘页** | 5 条（运动复盘（本周／本月／最近 90 天／今年／自定义时间）） | `exercise_recap.html` | `calorie.view.exercise-recap`（**已存在，路由没指**） | 改路由；内容按老规格核 |

**模板归类（用户 Q4 答 B：各图自己归类，全批最后人工收口）——本图 8 个名字归 3 类：**

| 类 | 模板名（条数） | 归谁管 | 本图态度 |
|---|---|---|---|
| 写后回执页类 | `crud_receipt.html`（13） | **卡路里公共层**（跨 6 场景 49 条，头号共用件） | 本图要动它 |
| 运动汇总与对照目标页类 | `exercise_summary.html`（15）＋ `exercise_goal_view.html`（2） | 卡路里公共层 | 已现成 |
| 按类别分开的列表页类 | `exercise_distribution／_strength／_cardio／_trend／_recap`（5） | 卡路里公共层 | 已现成 |

**三类都不上移 `packages/base-render/src/`**（照 #152 的判法：#163 裁定「今天别的技能都没在用 base-paint，跨技能复用目前只是潜在面」）。

## 四、公共层增量

**① 复用（已有的不重造）**
- `src/shared/docPage.ts` 的 `assembleDocPage` ＋ `metricsOf` —— 整页装配，全部读类页面今天都走它。
- `src/shared/copyArea.ts` 的 `dataCopyArea` —— 三格式复制菜单。
- `base-paint/blocks` 的区块件；`src/render/receipt.ts` 的 `CrudReceipt` 形状；`src/output.ts` 的落盘命名。
- `src/render/exercisePort.ts` 的 `listPortRows`（**记录级取数已经现成**，C 类页面缺的只是页与命令这一段）。

**② 新增**

| 件 | 吃什么数据 | 有哪些位置可以填 |
|---|---|---|
| **写后回执页整页装配**（公共出口，`src/cli/write.ts` 的 `receiptHtml()` 或其替换件） | `CrudReceipt` ＋ 库内现值 | 状态卡／改动字段摘要／逐条明细／改前→改后对照／复制区 |
| **记录级明细页装配**（暂名 `src/render/exerciseRecordsDoc.ts`） | `listPortRows` 的行 ＋ 筛选条件 ＋ 窗口 | 记录级列表（日期／类型／分类／时长／消耗／距离／心率／备注）／窗口累计／筛选说明／数据缺口区／复制区 |
| **新增 1 条只读命令** `calorie.view.exercise-records` | 参数 `{start, end, category?, hasNote?}` | shape＝stat；服务 C 类的 5 条词 |

**③ 登记三处（少一处新命令对 AI 不存在）**：`src/cli/keys.ts` 的 `CALORIE_COMBOS`（title 决定落盘中文名）；`packages/base-combos/combos.yaml`（同值镜像，动了要重跑 `scripts/gen-present.mjs`）；重跑 `scripts/build-help.mjs` 生成 `SKILL.md` 命令表。

## 五、已知的「唤醒词找不到、工作流程不知道」实证（第 4 张票的输入）

`SKILL.md` 的命令表由 `scripts/build-help.mjs:209-221` 用**手工维护的** `REPR` 表（命令 → 代表唤醒词）生成，缺项时**静默退回命令名**（`build-help.mjs:214` 的 `REPR[k] || k`）——漏了没人知道。场景 04 一族的实测后果：

| `SKILL.md` 行 | 首列 | 命令 | 问题 |
|---|---|---|---|
| `:136` | **`calorie.view.exercise-goal`** | 同左 | 首列直接是命令名 → **这个场景找不到底层唤醒词**（路由层有「看今日运动（vs 目标）」「看本周运动（vs 目标）」） |
| `:133` | 看今日运动概览 | `calorie.view.exercise` | 该词属**场景 01 主页**，不是场景 04 的「看今日运动」 |
| `:138` | 计划复盘（本周） | `calorie.view.exercise-review` | 该词属**场景 05 健身计划** |
| `:135` | 看运动分类占比 | `calorie.view.exercise-distribution` | 该词**只存在于路由层**，不在 436 条词表里（见 §六） |
| —— | （无行） | `calorie.view.exercise` | **14 条场景 04 的词共用这一条命令**，命令表按命令列行、只留一个代表词 → **其余 13 条词在表里找不到入口** |

## 六、路由层与词表的错位（第 3 张票的输入）

`routing.ts` 给场景 04 标了 **45** 条，不是 39 条。多出的 6 个词——**看运动目标／看力量总览／看有氧总览／看运动分类占比／看运动复盘／看运动消耗趋势**——**只存在于 `routing.ts`**，不在任何 `scene-*.ts`、不在老技能 436 词表里；它们是 #111 那批为「99 条命令全可达」新拟的入口，现在还进了 `SKILL.md` 与两处测试。

刺眼的反差：**该走专面页的 6 个新词全指对了，该走专面页的 7 个老词全指错**（力量／有氧两条老词已被「促进」到专面页，分布／趋势／复盘这 7 条没跟上）。看着像当年接了一半。

佐证这 6 个词不是唤醒词资产：`test/wake-assets-133.test.mjs:91` 强制断言 `wake-assets.ts` 的 436 条与全部 `scene-*.ts` 的唤醒词**逐字相等**（另 `:36` 钉 436）。

## 七、本票拍定的 8 条（用户 2026-09-13 授权「需要改动就改动」，按推荐执行）

| # | 决定 |
|---|---|
| Q1 | 欠账就 §二 那 4 处；(c) 记录级明细与那 1 条没有命令的词合成一张票 |
| Q2 | 建 5 张票（见 §八）；不单建「锁」以外的重复票 |
| Q3 | **写后回执整页化走公共出口**，不照 #152 的就地绕过路数——那条出口服务全批 35 条写命令，今天已整页化 3 条、**还剩 32 条是片段** |
| Q4 | C 类**合成一张记录级明细页**，**新增 1 条只读命令**（不往 `calorie.view.exercise` 里塞模式参数——命令与唤醒词要一一对得上） |
| Q5 | 读类页面**内容按老规格逐项核**，并进票 2／3；降采样（180／365 两条词）在此一并落地 |
| Q6 | 8 个模板名归 3 类，**全留卡路里公共层**，不上移 base |
| Q7 | 验收**按页面类别抽验约 10 张**，窗口差异走程序断言 |
| Q8 | 6 个只在路由层存在的词**清掉**，并进票 3；命令由老词承担，「每条命令可达」不掉（那 3 条命令正好被 7 条老词接住） |

## 八、本图票图与依赖

| 序 | 票 | 类型 | 阻塞于 |
|---|---|---|---|
| 1 | **#167** 边界、页面清单与公共层（本文件） | grilling ＋ task | — |
| 2 | 写后回执页整页化 · 13 条写词（公共出口） | task | — |
| 3 | 读类页面归位 · 7 条指错页 ＋ 5 条记录明细页 ＋ 6 条幽灵词 | task | — |
| 4 | 唤醒词与工作流程接线（HELP HTML ＋ `SKILL.md`，按 `/writing-for-agents`） | task | 2、3 |
| 5 | 锁 · 真出口用例（结构化断言） | task | 2、3 |
| 6 | 端到端验收 ＋ 肉眼终审（用户亲自在全新空白 session 跑） | task | 5 ＋ 装机 |

## 九、本图明确不做

- **不处理 #152 的账**（用户裁定：只作参照对象）；不替场景 07 做任何返修。
- **不补业务命令**：39 条词的执行命令今天全在、全跑得通；本图只新增 1 条只读页面命令（C 类）。
- **不新增唤醒词**：清掉 6 条只在路由层存在的词之后，本图唤醒词数是 **39**，与词表一致。
- **不改 13 条写命令的行为**；只改它们的产物（片段 → 完整文档）。
- **不发布 npm**（沿用整批裁定）。
- **不动面板／侧栏的 HTML 入口**（属桥／面板那条线）。
