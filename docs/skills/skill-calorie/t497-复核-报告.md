# t497 独立复核报告（复核席，与实施者无交集）

## §0 方式声明

- 只读复核：未改源码、未改他人证据；写集仅本件＋`.scratch/t497r/`（探针＋门禁摘录）。
- 全部复跑经 `node tooling/run-locked.mjs --ticket 497 -- <命令>`；工作区含他席未提交在途件（复跑时原样保留，未碰）。
- 实施者结论一律不采信，下表均为本席实跑。

## §1 独立复跑对照（声称 → 复核）

| 门禁 | 实施者 GATE-RUN（在册 exit 0 已逐条对上） | 本席复跑 runId／exit／摘要 |
|---|---|---|
| 编译 `tsc -b` | `3ac1f101` exit 0 | `561ea308` exit 0 |
| 靶向 `t497-caliber` 6 例 | `a6ddd121` exit 0 | `577b2bfc` exit 0，`pass 6 fail 0`（等锁 50003ms，WAIT-OWNER-ALIVE 在册） |
| 既有 5 件 | `68af6b2b` exit 0 | `8d10c14f` exit 0，`pass 40 fail 0` |
| e2e-check | `7f278b05` exit 0 | `3ac35cee` exit 0，`E2E-CHECK: 6/6 CHECK: PASS`（等锁 60011ms 在册） |
| 台账复跑 | `a29c9790`（fix-ledger）exit 0 | `5adfd804` exit 1，`RESULT: 88/91`——3 条 RED 全非本票行（见 §2⑥） |
| 变异／产物 | `f1526196`／`daf61afd` exit 0 在册 | 未重跑（脚本已审读；变异针法有效，见 §3 对照） |
| t380（披露红） | `80e8de14` exit 1，10/12 | 日志复核：2 红同为「输出未消费 group=weight_calorie」，与披露一致 |

## §2 判据①–⑦逐条结论

- ① 长趋势无 0 点：产物 `t497-long-trend-30d.html` 热量图仅 9 点（data-i＝6,13,20,22,23,24,27,28,29），线段跨空白直连（M178.2→L296.2），图例「没记录的那天不按 0 算」在场。PASS。
- ② 数据天数卡：值 `9`＋副文案「记了吃多少 9 天 · 2026-08-09 ~ 2026-09-07」同口径。PASS。
- ③ 均值分母点名：7 天窗 `avg=668`（3339/5，票面原数复现）；三卡副文案「有记录的 5 天平均」「从有记录的第一天」「只算记了吃多少的天」。PASS。
- ④ 两页同口径：多指标 `complianceRate=1`＋「有记录的 5 天里 5 天达标」，与热量趋势页逐字同口径；裁决统一到有记录的天，理由成立（§1 三条），无需保留两套。PASS。
- ⑤ 零内部标识符：靶向 LEAKS 断言（三页）复跑绿。PASS。
- ⑥ 程序门禁：编译／靶向／既有／e2e 全绿；台账本票 3 行（618/523/414）无 RED，余红 2 项全归他席在途（build-help 744→743 含重复行双报、sportDocs 434→466；reviewDocs 他席已自刷到 380 回绿）。PASS（附条件，见缺陷 D2/D3）。
- ⑦ 肉眼确认：产物在盘，留待收口统一请用户看，本席不代判。未做项。

## §3 自设新探针（实施脚本盲区）

`.scratch/t497r/probe-t497r.mjs`，`73e8113a` exit 0，`RESULT: 3/3`：
- A 全空窗：trend 侧 `avg=0` 无 NaN；长趋势侧觸既有 `missing-data` 守卫（空窗阻断不编数，#497 未动）——初版探针误断言被守卫掷回（`21bb7ea9` exit 1），修正为断言抛错码后绿。实施者只测有记录窗，此分支无覆盖。
- B 纯称重窗：`avgCalorie=null`（印 —）、`loggedDays=0`、体重变化照算 `-0.4`。绿。
- C 部分达标窗（3 记录 2 达标，实施者只测 5/5 全达标）：`complianceRate=0.67`（旧口径 0.29）、页印 `67`＋「有记录的 3 天里 2 天达标」＋注释「达标率分母＝有记录的天3」，取数↔页面同源、无反解。绿。

## §4 §6.1 裸跑核账

- `bb7e9afd` numstat：证据 132/0＋AGENTS 3/3，仅含本票 3 行。工作区 AGENTS 与 HEAD 无 diff（`git diff HEAD --stat` 空），裸跑残留已清。
- `fixledger.log`：`FIX-ROWS numstat="3 3 AGENTS.md"`，`a29c9790` exit 0 在册；当时 3 红全非本票行。裸跑 `sync1.log` 在册、产物已废弃记账。结论：§6.1 记录成立，交付物技术验收结论不受影响。

## §5 缺陷清单（归属标注）

- D1［范围外·S3］t380 两红（group=weight_calorie 未消费）：#497 未碰 group 管道（diff 仅改分母／null／文案），且其期望与判据⑤（可见文本禁内部词）正面冲突。转票措辞：请编排者交 #380 席同步断言或裁决口径优先级。
- D2［范围外·S3］台账 `scripts/build-help.mjs` 重复两行（同值 744），致同一事实双报 RED。转票措辞：请台账 owner 去重一行后自刷 743。
- D3［范围外·S3］台账余红 build-help→743、sportDocs→466 为他席在途。转票措辞：请各自 owner 自刷，本票不追责。
- D4［范围外·S3· concur 实施者§7］`goalProgressDocs.ts:202-203` 注释已陈旧、`html.ts:83/:299` 等同源页副文案未点名分母、`nutritionPort.ts:313` 另套自洽口径。转票措辞：请编排者另开一票定文字／口径。
- 本票引入缺陷：无。本票范围内缺陷：无。

## §6 未做项与下一手

- 判据⑦用户肉眼确认待地图收口统一安排；复核席停止，等编排者指派。

## §7 回执

PASS。
机器证据：编译 `561ea308` exit 0；靶向 `577b2bfc` exit 0 `pass 6 fail 0`；既有 `8d10c14f` exit 0 `pass 40 fail 0`；e2e `3ac35cee` exit 0 `6/6 PASS`；台账 `5adfd804` exit 1 `RESULT: 88/91`（RED 全非本票行）；新探针 `73e8113a` exit 0 `3/3`；对账：7 个声称 runId（`3ac1f101`／`a6ddd121`／`68af6b2b`／`f1526196`／`7f278b05`／`a29c9790`／`daf61afd`）＋披露红 `80e8de14` exit 1 逐条在册。
路径：报告本件；探针 `.scratch/t497r/probe-t497r.mjs`；门禁摘录 `.scratch/t497r/gate-t497r.log`；实施证据 `docs/skills/skill-calorie/t497-口径-证据.md`；提交 `d8fe2dc7`＋`bb7e9afd`。
缺陷：D1 t380 两红先天（转 #380 席）；D2 台账 build-help 重复行（转 owner 去重）；D3 台账余红两项他席在途（转 owner 自刷）；D4 同源文字／另套口径（concur，转编排者另开票）；本票引入／范围内缺陷无。未做项：⑦肉眼确认待收口。下一手：编排者收口安排用户确认后关票。
