# #449 运动域回执「写入字段／字段变更」改中文标签 · 证据

判定：**绿**。#449 只对了一件事——回执页的用户可见文本里不再出现命令行参数名；做法是把字段展示收到
`exercise/fieldLabels.ts` 这一张域表上，走 #422 的查表口径 `shared/fieldLabel.ts`（缺项回退原键名）。
不改 `exercise/commands.ts`／`routes.ts`、不扩 `shared/fieldLabel.ts` 的签名、不碰 `packages/base-render/`。

## 一、本票面的判据（真假由程序判）

| 判据 | 判法 | 今值 |
|---|---|---|
| ① 可见文本零参数名 | 13 条写词逐条真跑，产物剥样式／脚本后按文字节点检索冻结参数名清单（31 个键：CLI 参数名 ＋ 库列名） | 13 条全 0 命中 |
| ② 卡区兜底 | 写入字段卡所在卡区、字段变更卡卡区：参数名 0，且一个 snake_case 形态 token 都没有 | 13 条全 0 |
| ③ 缺项回退原键名 | 查表口径（③a）＋ 往 `buildExerciseReceiptDoc` 喂表外键、页面须原样印（③b） | 两半都绿 |
| ④ 标签单源 | `packages/<包>/src` 面里「运动域列键→中文标签」绑定与 `registerFieldLabels(` 调用（除定义件）各命中 1 个文件 | 两处都＝`exercise/fieldLabels.ts` |
| ⑤ 覆盖面 | 冻结的 26 个字段键在表里全有**纯中文**标签（无沉默回退）；真跑带到的 17 键同样走表 | 绿 |

判据件：`packages/skill-calorie/test/field-labels-449.test.mjs`（17 条）。
「可见文本」口径：剥 `<style>`／`<script>` 段 → 剥全部标签 → 解实体 → 收敛空白。复制区／对账区的机器载荷
（命令原文、M5 行）住在 HTML 属性（`data-t`／`data-fmt`）里、不在文字节点，`#264` 钉着它们的形状，故不进本判据。

## 二、13 条写词逐条真跑（读数＝可见文本参数名命中数）

参数取冻结表 `src/triggers/scene-04-exercise.ts` 的 `main_prompt.cli`（`<日期>` 填真实日期；改／删按 id 的先落种子）。

| # | 写词 | 实现前 | 实现后 | 写入字段卡 |
|---|---|---|---|---|
| 1 | 记运动 | 16 | 0 | 16 项（运动类型、消耗、时长…补录） |
| 2 | 记运动（含备注） | 16 | 0 | 16 项 |
| 3 | 记力量训练 | 16 | 0 | 16 项 |
| 4 | 记有氧运动 | 16 | 0 | 16 项 |
| 5 | 记日常活动 | 16 | 0 | 16 项 |
| 6 | 补记运动 | 16 | 0 | 16 项 |
| 7 | 批量补记运动 | 16 | 0 | 16 项 |
| 8 | 复制昨日运动 | 16 | 0 | 16 项 |
| 9 | 改运动记录 | 2（`minutes`＋副标题 `duration_minutes`） | 0 | 1 项（时长） |
| 10 | 改某日运动 | 2（`note`、`date`） | 0 | 2 项（备注、日期） |
| 11 | 删运动记录 | 1（`is_deleted`） | 0 | 1 项（删除标记） |
| 12 | 删某日运动 | 1（`is_deleted`） | 0 | 1 项（删除标记） |
| 13 | 批量删运动 | 1（`is_deleted`） | 0 | 1 项（删除标记） |

第 9 条的 `duration_minutes` 不在卡上，而在**副标题**：摘要由写命令按库列名给出（`edit.ts`：`已更新运动 #1（duration_minutes）`），
它落在页面 `subtitle` 这一处可见文本，本票在 `receipt.ts` 侧按同一张表改写（改的是页面副标题，复制区机器载荷一字不动）。

## 三、先红后绿（用最终判据件跑一遍实现前的树）

复现脚本 `.scratch/t449/redrun.mjs`：把 `receipt.ts` 换回 `HEAD` 版（336 行）、临时移走 `fieldLabels.ts` → 建本包 → 判据件；
再写回本票实现（逐字节一致、刷 mtime）→ 建本包 → 判据件。（判据只读本包 `dist/`，故这一环用
`pnpm --filter skill-calorie build`，整仓门单独跑在第六节。）

| 段 | 命令 | 退出码 | 读数落盘 |
|---|---|---|---|
| 先红 | `pnpm --filter skill-calorie build` → `node packages/skill-calorie/test/field-labels-449.test.mjs` | 0 → **1** | `.scratch/t449/red-build.log`／`red-test.log`（17 条：15 红 2 绿；读数见第二节「实现前」列） |
| 后绿 | 建本包 → 同上 | 0 → **0** | `.scratch/t449/green-build.log`／`green-test.log`（17/17 绿） |

红段里本就绿的两条：③b（回退口径在实现前就是对的，本票只保它）与 ⑤（管的是表本身，不依赖页面）。
还原自证：`receipt.ts`／`fieldLabels.ts` 与本票实现 sha256 逐字节相同＝true；LF 分别 341／33（告警线 350 以内）。

## 四、变异两行（判据读 `dist/`，每次变异都先建本包再跑判据；还原写回原字节并刷 mtime）

| 变异 | 改哪 | 红读数 | 还原后 |
|---|---|---|---|
| ① 把标签表某键的中文改坏（`duration_minutes: '时长'` → `'duration_minutes'`） | `src/exercise/fieldLabels.ts` | 判据 **exit 1**：17 条里 **13 红**——11 条写词的产物出现 `duration_minutes`（如「记运动 可见文本命中=1…变更卡命中=1」「改运动记录 可见文本命中=1…变更卡命中=1」）＋ ③a 查表口径 ＋ ⑤ 纯中文标签；另 2 条写词（记力量训练、改某日运动）本就印不到「时长」这一行，故仍绿 | 判据 **exit 0**（17/17），mtime 已刷 |
| ② 把回退口径改成回退英文标签（`? label : key;` → `? label : 'Unknown field';`） | `src/shared/fieldLabel.ts`（**临时**，还原后 sha256 回到 `92B91B5D0A81B321C053D8B0FD21AE476C3091FF42F039B74144CD4CA0201A25`，`git status` 无该件） | 判据 **exit 1**：③a 与 ③b 两条同时红（查表口径＋页面端到端各一条） | 判据 **exit 0**（17/17），mtime 已刷 |

日志：`.scratch/t449/mut1-*.log`／`mut2-*.log`；两次变异后都先建本包再跑判据（判据读 `dist/`）。
变异脚本 `.scratch/t449/mutate.mjs`（`apply`／`revert` 两态，还原用原字节副本）。

## 五、单源命中面

- 实现面（`packages/<包>/src/**`）「运动域列键→中文标签」绑定：**命中文件数＝1**＝`packages/skill-calorie/src/exercise/fieldLabels.ts`
  （实现前是 `src/exercise/receipt.ts` 里那份 16 键的表，本票把它搬走，receipt.ts 只留查表调用）。
- 「`registerFieldLabels(` 调用（除定义件 `shared/fieldLabel.ts` 自身）」：**命中文件数＝1**，同一份。
- 域外读数（据实记、非本票判据）：绑定的通用面里还有 3 处**测试夹具**命中——`test/fusion-shared-422.test.mjs`
  的 t422 夹具（`duration_minutes: '时长'`，另一个域）与两条 `exercise_type: '慢跑'`（那是数据值不是标签），都不是标签表。
- 页面可见文本里还剩两个 snake_case token，都不是参数名、本票按范围不碰：`exercise_log`（页头来源行＋来源脚注的表名）
  与 `total_changes`（口径行的 SQLite 口径词）。#452 在运动汇总页把来源名换成了读者看得懂的「运动记录」，同形处理属该族票的地盘。
- 不登记的键（没有中文名就不编）：选择器类 `id`／`from`／`to`／`items`／`copyFrom` 不进 `writtenFields`、也不进变更卡；
  同步列 `xunji_localid`／`xunji_title` 没有任何命令能写，变更卡摆不出来（真出现时判据②的 snake_case 兜底会红）。

## 六、门禁（`node tooling/run-locked.mjs --ticket 449 -- …` 内跑，`.scratch/locks/gate-runs.log` 对账）

- GATE-RUN runId=caf6a6ce-8918-4fef-9c8d-49b847f58e34 cmd="pnpm build"（exit 0）
- GATE-RUN runId=06c62e44-00d6-42c7-90ee-cb944916c344 cmd="node packages/skill-calorie/test/field-labels-449.test.mjs"（exit 0，17/17）
- GATE-RUN runId=da1d35c8-e696-4556-9e08-556b30f4d01b cmd="node --test packages/skill-calorie/test/exercise-receipt-264.test.mjs"（exit 0，13/13）
- GATE-RUN runId=2c6d93b5-5414-4491-9e70-9e5db824f559 cmd="node packages/skill-calorie/test/exercise-receipt-fusion-423.test.mjs"（exit 0，20/20）
- GATE-RUN runId=6adf1e7b-e152-423f-858c-f045fa0b3da0 cmd="node packages/skill-calorie/scripts/check-warning-line.mjs"（exit 0，`RESULT: 58/58`）
- 归因记一笔：本票实施中途整仓 `pnpm build` 曾红一次（`packages/skill-bill/src/record/scene-income.ts`／`scene-photo.ts`
  的 TS2305／TS2459：`shared/photoEscape.js` 尚未导出 `commandsOf`／`pickOf`／`promptOf` 等）——那是当刻他席 skill-bill 的在途
  改动，本票一行未碰；判据只读本包 `dist/`，那一窗口内用 `pnpm --filter skill-calorie build`（exit 0）保证本包产物当刻。
  上表 `caf6a6ce` 是随后他席回稳后的整仓 `pnpm build`，exit 0。

## 七、样例（可双击抽查，实现后产物）

- `D:\ilife\.scratch\t449\out\09-改运动记录.html`——写入字段 1 项「时长」；副标题「已更新运动 #1（时长）」
- `D:\ilife\.scratch\t449\out\12-删某日运动.html`——写入字段 1 项「删除标记」；删除前快照全中文
- `D:\ilife\.scratch\t449\out\01-记运动.html`——写入字段 16 项全中文；`13-批量删运动.html` 同类
- 13 份样例与读数清单一并落在 `D:\ilife\.scratch\t449\out\`（`samples.log` 记生成时的退出码）

## 八、未做项

- 撤销入口真接线（#423 证据件 §八另立）——不在本票。
- 页头来源行／口径行里的 `exercise_log`／`total_changes`：非参数名，按票面范围留给对应族票。
- `exercise/edit.ts` 摘要按库列名报字段这点未在写侧改（本票写集不含该件）；页面侧按同一张表改写，写侧若要顺手改需另开票。
