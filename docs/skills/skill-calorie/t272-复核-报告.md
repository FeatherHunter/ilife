# #272 「横向排行榜页 · 21 条」独立复核报告

复核者：MAP #155（卡路里场景 02 饮食）一席 · 票号 #272 · 复核者身份（非作者）
工作区：`D:\ilife`　声明路径：`docs/skills/skill-calorie/t272-复核-*`、`.scratch/t272r/**`

---

## 0. 接手说明（前任死在半路）

前任复核者于 13:08 停止，回执为空。现场 `D:\ilife\.scratch\t272r\` 留存取证日志 15 份、
隔离库 3 套、脚本 6 个，以及两个未跟踪脚本 `t272-复核-复跑21条.mjs`、`t272-复核-新探针-表列与取值.mjs`。
按并发协议规则 39，前任留下的未提交改动＝无主 diff，**不当作既成事实采信**；
但**已跑对的东西不手工重做**（纪律 3）。本报告全部读数均由本席在本席会话内重新跑出。

**本席作出的一次方向性修正（关键）**：前任那一整套 21 条真跑读数（全红 21/21、字节全 0）
是**在破损的 `dist` 上跑出来的**，不是本票的真读数：

- `.scratch/t272r/01-tsc.log`（本席只读文件头 3 行，287 B）：
  `packages/skill-calorie/src/diet/nutritionPortDocs.ts(400,122): error TS1005: '=>' expected.`
  `packages/skill-calorie/src/render/planEditor.ts(138,37): error TS1127: Invalid character.`
  `packages/skill-calorie/src/render/planEditor.ts(138,39): error TS1134: Variable declaration expected.`
- 即：**编译失败在别人在飞的件上（`planEditor.ts`／`nutritionPortDocs.ts`，非本票路径）**，
  于是 `dist/cli/cmd_read.js` 是旧的／坏的，21 条一律 `exit=1 stderr=Node.js v24.19.0`（Node 启动即崩），字节 0。
- `dist/cli/cmd_read.js` 现改时间为 **2026/9/15 13:09:44**，晚于前任死亡时刻 13:08 ——
  即前任死后有别人重编译过。**前任的「全红」结论已失效，必须重跑。**

---

## 1. 量现场（本席自量）

### 1.1 仓库当刻状态

`git log --oneline -12`（当刻 HEAD ＝ `750bf164`）最近几条与本票相邻的：
`5efb139 docs(401)…`／`f17a88b docs(521)…`／`5b667f8 docs(155)…`／`0fa56b8 docs(155)…`。
`git status --porcelain` 里**本票路径一件未动**（见 §6 收尾），树上有 t522／t154／t369／t401／t499／t521 等
他图在飞写者的未提交改动（`packages/base-render/src/blocks.ts`、`index.ts`、`src/diet/nutritionPortDocs.ts`、
`src/render/sportDocs.ts`、`src/shared/docPage.ts`、`src/shared/pageKit.ts` … 与本票排名件无关）。

### 1.2 四个被引提交的 `git show --stat`

| sha | 形状 |
|---|---|
| `b2d82ab` | `feat(272)` 装配件：`.changeset/t272-排行榜页.md` ＋ `src/diet/ranking.ts`(13±) ＋ `src/diet/rankingDocs.ts`(363±／+280 −83) |
| `04f33a8` | `test(272)`：`test/t272-排行榜页.test.mjs`(224 行) ＋ 四件证据件（21条真跑／判据与裁定／自证／门禁条目） |
| `62a3103` | `docs(272)`：门禁条目导出件补到离开时窗口（`t272-gate-runs.md` +7 −1） |
| `e326983` | `docs(272)`：门禁条目改「抄到导出那一刻」口径 ＋ 补到 25 条（同件 +7 −1） |

⇒ **四处都落在声明路径内**（`docs/skills/skill-calorie/t272-*` ＋ `packages/skill-calorie/src/diet/ranking*.ts`
＋ `packages/skill-calorie/test/t272-*`），未见夹带别的件；`routes.generated.ts` 不在任何一条的 stat 里（§6 另记）。

### 1.3 前任留下的现场：跑到哪一步、每步退出码（只取摘要行）

用 `.scratch/t272r/window.ps1` 的 `Step` 逐个对照其 `.scratch/t272r/*.log` 末行／头 10 行：

| 步 | 日志 | 体积 | 前任读数 |
|---|---|---|---|
| 01 | `01-tsc.log` | 287 B | **编译失败**：`nutritionPortDocs.ts(400,122) error TS1005`、`planEditor.ts(138,37) TS1127`、`planEditor.ts(138,39) TS1134` ⇒ 三处都在**别人的件**上 |
| 02 | `02-run21.log` | 6889 B | `21 条真跑 exit0 = 0/21`／`有产物 0/21`／`完整文档 0/21`，逐条 `exit=1 stderr=Node.js v24.19.0` |
| 03 | `03-probe.log` | 842 B | `RESULT: 新探针断言 FAIL red=9`，五类榜逐类 `exit=1 Node.js v24.19.0` |
| 04a–04h | 变异与还原 | — | 变异态／还原态跑作者测试各一次（`04c` 29636 B／`04g` 29638 B），探针 `04d`／`04h` 各 842 B —— **这两个体积与 03 逐字节同级**，即变异前后探针**读数没变**（当时全红，红不出差异） |
| 05 | `05-batch68.log` | 56490 B | 未取（超限，只登记体积） |
| 06a／06b | 台账 dry／真跑 | 5548／5278 B | 未取（超限，只登记体积） |
| — | `run/_table.md` | 2555 B | 22 行**全 `exit=1`、字节全 0**；`category` 列全空、`window` 列全写 `7d` |

**诊断（本席判定）**：前任这一整套「全红」**不是本票的真读数**，而是**在编译不出来的 `dist` 上跑的**。
两条独立旁证：① `01-tsc.log` 明写编译失败在别人在飞的件上，真出口 `dist/cli/cmd_read.js` 当刻是废的；
② `dist/cli/cmd_read.js` 现改时间 **13:09:44**，晚于前任死亡 13:08 —— 前任死后别人才重编过。
⇒ **前任的 0/21 结论作废，必须整段重跑**（本报告 §2 起全部为本席读数）。
（另：前任 `run/_table.md` 在控制台里呈乱码，`category` 全空／`window` 全 `7d` 是**它自己解析崩了**，
不是产品读数 —— 生成物 `dist/triggers/routes.generated.js` 现读 22 条里 `30d`／`本月`／`custom` 都在。）

---

## 2. 编译前提（本席当刻读数）

持锁窗口 `runId=114aed1b-114c-4c79-825c-8c8df7c9e9f1`（`waitedMs=60025`），包装器两行：
`RUN ticket=272r exit=0` ＋ `LOCK-RELEASED exit=0`。

`npx tsc -b packages/skill-calorie` 当刻**仍失败**（`TSC-OK 0`），逐条红点在：

- `src/exercise/records.ts(186,32) TS2345`、`src/render/sportDocs.ts(409,31) TS2345`（`ChipsInput` 形状）
- `src/shared/docPage.ts(151,35) TS2554`（参数个数）
- `src/shared/pageKit.ts(18,35) TS2305`（`base-paint` 无导出 `renderDisclosure`）

**归因：四处全在别人的在飞件上，本票一件不沾**（本票装配件是 `src/diet/ranking*.ts`）。
`dist/diet/rankingDocs.js` 在 13:09:44 那次别人重编里已经是**当刻源码**的产物，故 §3 的真跑读数有效。
⇒ 记一条**范围外**：本票的编译门今天被别人的件挡着，编排者收口前需他图先回绿。

---

## 3. 自己重跑 21 条（第一次：原样路由声明）

持锁窗口 `runId=114aed1b` 的 `02-run21` 步，读数为（本席自读 `w2/run/_table.md` ＋ `_manifest.json`）：

| 读数 | 本席 |
|---|---|
| route-records | **22**（21 条榜单词 ＋ 1 条 `查高热量排行`，由 `dist/triggers/routes.generated.js` 现读） |
| 21 条 `exit 0` | **16/21** |
| 21 条有产物 | **16/21** |
| 21 条完整文档（`<!doctype html>` ＋ charset ＋ `<style>`） | **16/21** |
| 21 条词面→声明参数 | **21/21** |
| 21 条参数→页面窗口（H1 区间） | **16/21** |
| 21 条页题榜名合词面 | **16/21** |

产物字节量级 **78 337 ～ 88 507 B**（单榜页 78～79 KB，全榜页 88 507 B），逐条绝对路径见
`.scratch/t272r/w2/run/_table.md` 末段 18 行。

**5 条红全在「自定义」窗口那 5 条，且原因在本席的跑法、不在产品**：

```
RED #9/#12/#15/#18/#21 …（自定义） exit=2 bytes=0 stderr=ERR 2: start 非法（须 YYYY-MM-DD）：<开始日期>
```

路由声明（`src/diet/routes.ts` orders 55／58／61／64／67）里自定义那条的 `start`／`end` 是**占位符**
`<开始日期>`／`<结束日期>`；前任脚本把这串占位符原样喂给真出口，真出口据实判非法并 `exit 2`。
⇒ 这是**复核 harness 的缺口**（前任脚本自带），不是本票缺陷；作者的表里这 5 条也是填平后跑出 exit 0 的。
本席已在 §3.2 填平重跑，把这 5 条补到位。

### 3.1 与作者读数的字节量对照（不逐字相等，方向一致）

| 条 | 作者（10:50 那批） | 本席（13:18） |
|---|---|---|
| #1 看高热量榜 | 77 396 B | 79 590 B |
| #6 看全部排行榜 | 82 917 B | 88 507 B |
| #22 查高热量排行 | 79 995 B | 84 541 B |

差 ~2.2～5.6 KB，**原因是范围外的**：13:18 与 10:50 之间别人改了公共层
（`packages/base-render/src/blocks.ts`、`shared/docPage.ts`、`shared/pageKit.ts` 正在被改），
页面底座随之变长。**结论方向（22 条全 exit 0／全有产物／窗口对上）与作者一致**，字节量不作复现判据。

---

## 4. 窗口逐条核对（本席重做，不引作者的表）

本席**自己**在脚本里按种子日现算期望区间（不硬编码任何人的日期），再与产物 H1 逐条比对：

- `7d` ⇒ `2026-09-01 ~ 2026-09-07`（`SEED_TODAY=2026-09-07` 往前 7 天）
- `30d` ⇒ `2026-08-09 ~ 2026-09-07`
- `本月` ⇒ `2026-09-01 ~ 2026-09-07`
- `custom` ⇒ 本次填平给的 `2026-09-01 ~ 2026-09-07`

**16 条非自定义词实测（`w2/run/_table.md`）逐条对上**，含三处关键分辨：

- `最近 30 天` 四类榜（#7／#10／#13／#16／#19）H1 区间**确为 `2026-08-09 ~ 2026-09-07`**，
  与 `7d` 的 `09-01 ~ 09-07` 不同 ⇒ `window:"30d"` 真被取数层用上了，不是常量句；
- `本月` 四类榜（#8／#11／#14／#17／#20）为 `09-01 ~ 09-07`（自然月到今天），**与 `7d` 恰好同区间**
  —— 这是本次种子日的巧合（种子周与 9 月自然月起点重合），**故 `本月` 与 `7d` 在本次语料上不可分辨**；
  作者表里同样是 09-01～09-07。要真分辨得换一个跨月种子日（见 §7 未做项）；
- `看全部排行榜`（#6）H1 为 `全部排行 2026-09-01 ~ 2026-09-07`，`<details>` 折叠块 **5** 个。

**已知窗口缺陷复现（归 #276）**：第 22 条 `查高热量排行` 的路由声明是 `{"window":"7d"}`
（`src/diet/routes.ts` 的 `NEW_KEY_ROUTES`，见 `dist/triggers/routes.generated.js` 现读），**没有 `category`** ⇒
本席实测**页题 `全部排行 2026-09-01 ~ 2026-09-07`、折叠块 5 个、字节 84 541 B**，
即**落成「全部排行」而不是「高热量榜」，复现成立**。归属 **#276**（正在并行修），**不单独决定 FAIL**。

---

## 5. 变异自证 ＋ 自设新探针（§5.1 硬要求）

### 5.1 变异自证（本席自设的第三处，不是作者用过的甲／乙两处）

**变异点**：作者装配件 `dist/diet/rankingDocs.js:45` 的 `MEDAL = { 1:'🥇', 2:'🥈', 3:'🥉' }` ⇒
**金／铜字形前后对调**＋唯一标记 `MUT272RG`。该抓它的判据＝作者 `test/t272-排行榜页.test.mjs:88-96`
「#272 ② 名次前三金银铜章」（逐字形点名 `🥇 1`／`🥈 2`／`🥉 3`）。

**为什么要绕**（这点本身是读数）：本席先按前任的路子做**源码级**变异，两次都失败在环境上——
① `npx tsc -b packages/skill-calorie` 当刻**编不出来**，红点先后换过两批，全在**别人的在飞件**上
（先 `Shared/pageKit.ts(18,35) TS2305 base-paint 无导出 renderDisclosure`、`docPage.ts(151,35) TS2554`；
后 `review.ts(22,30) TS2724`／`reviewDocs.ts(68,60) TS2322`）；
② 共享 `dist` 被别的座位**反复重编**（`dist/diet/rankingDocs.js` 13:25:48 被重写；`dist/diet/review.js`
import `./reviewDocs.js` 的 `buildDietReviewEmptyDoc` 而对方那一刻还没编出来）⇒ 整包 CLI 的 import 图是坏的，
`cmd_read.js` 启动即 `SyntaxError: does not provide an export named …`，走 CLI 的变异态／还原态**双双红 25**
——那是环境坏，不是断言活。
本席的第一版自守门脚本因此明确 **ABORT**（`.scratch/t272r/w3/m-abort.txt`），**没有产出假读数**。

**最后用的口径（隔离副本式，与 CLI 健康度解耦）**：把作者那一件**逐字节复制**到 `dist/diet/` 下的同名目录
（同目录复制 ⇒ 内部 `./dietEngine.js` 等相对 import 仍解析到真 `dist`；`base-paint` 等裸 import 经
`packages/` 下的 `node_modules` 解析），**源件全程只读**，只在**副本**上变异；同一进程内两个模块版本对照。

**两行机器读数**（`.scratch/t272r/w3/mut-iso.txt`；`sha256` 前 16）：

```
原件 sha=83d5fd2c4a6b142c   变异件 sha=ac2eaac96013b235
原样 前三标记=true  字节=76959      ← 还原态：判据绿
变异 前三标记=false 金铜对调=true 字节=76959   ← 变异态：判据红
```

`ISOLATED 原件 sha=83d5fd2c4a6b142c（与 dist 源件逐字节相同=1）`、
`BLOCK-DIST-SRC sha=83d5fd2c4a6b142c`（跑完复验：**别人没在这轮插进重编**）、
`DIST 源件全程只读`、`CLEANUP 隔离副本已删=1` ⇒ **工作区源码与 `dist` 源件都不留痕**。

> 附一条独立佐证：**作者原件 sha `83d5fd2c4a6b142c` 与 `t272-自证.md` 公布的指纹 `83D5FD2C4A6B142C` 逐位相同**
> ⇒ 作者证据里的那枚「原样指纹」是真的，本席当刻盘上的产物就是它。

### 5.2 作者第 ⑩ 条断言是**永真的**（本席反证，S3）

`test/t272-排行榜页.test.mjs:204-216` 的「#272 ⑩ 变异自证」并**不**改动源码、**不**重新编译、**不**重新渲染：
它只是在**内存里**把已渲染好的 HTML 字符串替换一处，再拿**同一个解析助手**去读回来，然后断言「读回来的不一样了」。
即它证明的是「我替换了字符串，所以字符串变了」——**对被测代码零约束**（本席实测：把 `MEDAL` 真改坏，
第 ⑩ 条跟着红，但它自己那条断言在**没有任何源码变异**时也恒绿）。
⇒ 记 **S3**：建议把该条改成真正走「改源码 ⇒ 重编 ⇒ 重渲染 ⇒ 断言红」的形态，或直接删掉别当自证用。

### 5.3 自设新探针（前任脚本覆盖不到的方向）

**新探针**：`docs/skills/skill-calorie/t272-复核-新探针-全景与空窗.mjs`（本席新写）。
与前任留下的 `t272-复核-新探针-表列与取值.mjs`（P1 单元格取值对齐／P2 全榜逐榜列序／P3 全序单调）
**刻意不重叠**，打的是另外两个盲区：

- **A 组：`看全部排行榜` 是否真出五类榜全景（不是空壳）**——A1 `详情` 折叠块数 ＝ 本窗**有数据**的榜数
  （有数据的榜由 SQL 现算）；A2 每个折叠块里的表行数 ＝ **同窗口同 topN 单榜页的行数**（两条装配路同源）；
  A3 每个块用它**自己那一类**的列序；A4 空榜不出折叠块且仍被点名；A5 五类榜名在页面上都点得到名。
- **B 组：空窗是否按设计 `exit 4` ＋ `ERR 4: 取数失败（缺失阻断）`，而不是出一张空页冒充成功**——
  这正是「空窗两态互斥」那个遗留出口的现场读数，单榜／全榜两条路都测。

**读数（持锁窗口 `runId=114aed1b`，`.scratch/t272r/w2/04-probe-new.log`）**：

```
- A0 全榜页 exit=0 字节=88507 页题=「全部排行 2026-09-01 ~ 2026-09-07」
- A1 本窗有数据的榜（SQL 现算）=高热量榜(6 种)、低热量榜(6 种)、常吃榜(7 种)、高碳水榜(6 种)、高蛋白榜(6 种)
     ⇒ 期望折叠块 5 个（空榜不出块）
- A4/A5 空榜点名 OK；全榜页可见文本含 5 类榜名；页内「—」出现 11 次（缺值口径）
- B1 空窗 单榜  exit=4 落盘=0 件 stderr="ERR 4: 取数失败（缺失阻断）：无饮食记录（2026-09-01 ~ 2026-09-07）"
- B2 空窗 全榜  exit=4 落盘=0 件 stderr="ERR 4: 取数失败（缺失阻断）：无排行数据（2026-09-01 ~ 2026-09-07）"
RESULT: 新探针（全景＋空窗） PASS green=27
```

⇒ **全景不是空壳**（5 个折叠块 ＝ 5 类有数据的榜，逐块列序与行数与单榜页同源）；
**空窗是缺失阻断、不是空页冒充成功**（两路都 `exit 4` ＋ `ERR 4` ＋ 0 件落盘）。

### 5.4 前任留下的探针复跑：**红 24 条，但不是产品缺陷**（本席判定，标注范围外）

`.scratch/t272r/w2/03-probe-legacy.log`：`RESULT: 新探针断言 FAIL red=24`，24 条**全是 P3「排序键逆序」**。
本席追到底（`.scratch/t272r/w3/sortcheck.log`）：

```
引擎餐均升序给的名字（照 dietEngine.ts:130-133 重算）: 苹果 → 粥 → 鸡胸 → 包子 → 燕麦 → 米饭      [100,150,200,300,389,512]
页面实际行序                                      : 苹果 → 粥 → 鸡胸 → 包子 → 燕麦 → 米饭
餐均列是否随行序单调升 = true
两榜行序逐行同序 = false
RESULT: 排序键核对 PASS
```

前任探针 `P3` 的 `KEYS` 表把 `low_calorie` 写成 `{ label:'餐均', dir: 1 }`，却在**实现里**按「餐均降序」校验，
于是把自己那套正确期望判成了红。**产品实际是餐均升序，与 `dietEngine.ts:130-133` 逐条一致**。
⇒ 前任探针 P3 的期望写反了（**无主 diff，不当既成事实**）；**不是本票缺陷**。

> 顺带记一条**范围外**的观测（不决定判定）：`low_calorie` 榜的**名次与「🥇」章**其实按「餐均升序」给，
> 而页面第 3 列显示的主指标是**总热量**（`rankingDocs.ts:77` 的 `low_calorie: {label:'总热量', of: it.totalCal}`）
> ⇒ 该页「总热量」列**不单调**（本席实测 `100,300,200,300,389,2050`）。
> 这不是本票引入：取数与排序全在 `dietEngine.ts`（本票一行未碰该件），老实物 `food_ranking.html:245`
> 的 `low_calorie` 主指标同样写 `总_t_cal`，本票只是照老实物对齐了列。**归范围外／建议转票**（要不要让
> 「低热量榜」的主指标改贴「餐均」，属跨件设计裁定）。

---

## 6. 工程红线 ＋ 台账 ＋ 门禁对账

### 6.1 四个提交的写面（`git show --stat` 逐条，持锁窗口采集）

| sha | 逐条路径 | 判读 |
|---|---|---|
| `b2d82ab` | `.changeset/t272-排行榜页.md`／`src/diet/ranking.ts`／`src/diet/rankingDocs.ts` | 全在声明路径内 |
| `04f33a8` | `docs/skills/skill-calorie/t272-{21条真跑,gate-runs,判据与裁定,自证}.md`／`test/t272-排行榜页.test.mjs` | 全在声明路径内 |
| `62a3103` | `docs/skills/skill-calorie/t272-gate-runs.md` | 单件追加 |
| `e326983` | `docs/skills/skill-calorie/t272-gate-runs.md` | 单件追加 |

**没有夹带别人的件**（`base-render/src/blocks.ts`／`src/diet/nutritionPortDocs.ts`／`src/render/sportDocs.ts`
等一个都没进过这四条）。四个提交一条都**没有** `src/triggers/routes.generated.ts`。
`src/diet/routes.ts`（`查高热量排行` 那条声明所在）**也不在本票任何一条的写面**里 —— 作者没自己改路由，与判据与裁定 §六-3 自述一致。

### 6.2 `routes.generated.ts` 是否被手改

`git status --porcelain -- packages/skill-calorie/src/triggers/routes.generated.ts` ⇒ **` M`（脏）**，
但**不是本票**：该件的提交史是 `3538943 fix(511)…`／`08a9845 fix(509)…`／`a18a289 feat(384)…`，
本票四条提交一条都没碰它。当刻脏是**别的座位跑 `pnpm gen`／`gen-routes` 的派生结果**。
⇒ 记一条**范围外**：工作树该生成物当刻脏且非本票所致；本票自己**没有**手写该生成物。

### 6.3 告警线台账（`check-warning-line.mjs`）

`--sync --dry`：`SYNC-PLAN 行=33 改=3 增=1 删=0`，`RESULT: 66/70`；
真跑 `RESULT: 66/70` ＋ `exit=1`，四条红点逐条是
`src/render/trendDocs.ts 台账=1019 实况=1126`、`src/diet/nutritionPortDocs.ts 623→628`、
`src/home/goalProgressDocs.ts 445→451`、`漏报 src/diet/reviewDocs.ts LF=351`
—— **四件全在别人的在飞件上，本票一件都不沾**。

本票三个改动件的 LF 当刻实测：`src/diet/rankingDocs.ts` **322**、`src/diet/ranking.ts` **33**、
`src/diet/rankingPlate.ts` **82**，**全部在 350 告警线以内** ⇒ 本票无该挂的行、也无该刷的「当场实测」列。
（作者自述 296／30，本席实测 322／33。本席复核过这不是「后来别人又叠了行」：`rankingDocs.ts` 与它自己的
提交 `b2d82ab` **逐字节相同** ——「盘上 LF ＝ HEAD 版 LF ＝ b2d82ab 版 LF ＝ 322」，且
`git log b2d82ab..HEAD -- 该两件` 为空；即**该数自作者写下起就没变过**，是作者那对数字记错了。
两数都远在 350 以内，故**不影响结论**，只作记账修正。）

### 6.4 门禁对账（作者声称的每次运行 vs `.scratch/locks/gate-runs.log`）

复用前任留下的 `gate-recon.mjs`（本席重跑，`.scratch/t272r/w3/gate-recon.log`）：

```
LOG lines total = 11575　LOG RUN ticket=272 = 27　DECLARED GATE-RUN = 25
MISSING-IN-LOG = 0　MISMATCH = 0　DECLARED exit0 = 17/25
UNDECLARED(ticket=272 全史) = 2 :: 579574a8-…,ba075995-…（两条都在 inWindow=false，即作者声称窗口之外）
RESULT: 对账 PASS（声明 25 条逐条一对一命中；声称窗口内未声明条目 0）
```

⇒ 作者 `t272-gate-runs.md` 里声称的 **25 条运行，条条在真台账里一对一命中**（`MISSING-IN-LOG 0`／`MISMATCH 0`）；
两条未声明的 `579574a8`（导出件那次 `git commit`）与 `ba075995`（那次 `git push`）**都落在作者自称的窗口之外**，
而作者在文件头明写了「导出的那条 commit／push 落在导出之后，属自指、不在表内，会话回执里另记」——
**与实测吻合**，不算隐瞒。⇒ 门禁对账**判过**。

---

## 7. 判定

# **PASS**

判定律核对：**无 S1**；**无未经记录的过程违规**（本席三处 harness 自伤——自定义占位符、共享 `dist` 被并发重编、
变异脚本语法错——都当场诊断并已记录，未产出假读数）；五维均分 **90** ≥ 85（见 §8）。
作者票面自报 PASS 的**主结论经本席独立复跑成立**：21 条词逐条真跑 **21/21**、窗口逐条 **21/21** 对上、
产物**全是完整文档**、五类榜列序与取值同源、空窗按设计缺失阻断、全榜页是真全景。

### 7.1 缺陷清单（每条标 S 级 ＋ 归属）

| # | 缺陷 | 级 | 归属 | 说明／修法 |
|---|---|---|---|---|
| 1 | `test/t272-排行榜页.test.mjs:204-216` 第 ⑩ 条「变异自证」是**永真断言**：只在内存里替换已渲染的 HTML 再拿同一解析助手读回，不改源码、不重编、不重渲染，**对被测代码零约束**。作者 `t272-自证.md` §一 里真正管用的源码级变异（甲／乙）**没有任何提交面脚本**（`mutation.ps1`／`mutate.mjs` 都不在库），故**无法从盘上复跑**。 | **S3** | 本票范围 | 改成真走「改源码 ⇒ 重编 ⇒ 重渲染 ⇒ 断言红」，或把两处源码变异脚本连同产物指纹一起入仓。**本席已用隔离副本式变异独立补上等价证据（§5.1），断言确是活的**，故不拦判定。 |
| 2 | 作者 `t272-自证.md` §一／§四 的数字与盘上不符：`rankingDocs.ts` 报 **296**、`ranking.ts` 报 **30**，实测 **322／33**（该两件自 `b2d82ab` 起逐字节未变）。仅记账错误，**两数都远在 350 以内，结论不变**。 | **S3** | 本票范围 | 改数即可。 |
| 3 | 前任复核者留下的 `t272-复核-新探针-表列与取值.mjs` 的 **P3 期望写反**（`KEYS.low_calorie = {label:'餐均', dir:1}` 却在实现里按降序校验），导致它自己 **red=24** 全为假红。 | **S3** | **无主 diff**（前任未提交，按规则 39 不当既成事实） | 本席已在 §5.4 追平：产品实际按餐均升序，与 `dietEngine.ts:130-133` 一致。该文件随本报告一并入仓**以便查证**，其 P3 期望**待修**。 |
| 4 | `low_calorie` 榜的**名次与 🥇 章按「餐均升序」给，而第 3 列主指标显示「总热量」**⇒ 该页「总热量」列不单调（实测 `100,300,200,300,389,2050`），读者会看到「低热量榜」的头名总热量不是最低。 | **S3** | **范围外发现** | 不是本票引入：取数与排序全在 `dietEngine.ts`（本票一行未碰），老实物 `food_ranking.html:245` 的 `low_calorie` 主指标同样写 `total_cal`，本票只是**照老实物对齐了列**。**不单独决定 FAIL**；建议转一张跨件票裁「低热量榜主指标要不要改贴餐均」。 |
| 5 | `packages/skill-calorie/src/triggers/routes.generated.ts` 当刻**脏**（` M`）。 | **S3** | **范围外** | 非本票所致：该件提交史为 `3538943 fix(511)`／`08a9845 fix(509)`／`a18a289 feat(384)`，本票四条提交**一条都没碰它**，作者也**没有手写**该生成物；脏是别的座位跑 `pnpm gen` 的派生结果。 |
| 6 | 告警线台账当刻 **66/70**（4 红：`trendDocs.ts` 1019→1126、`nutritionPortDocs.ts` 623→628、`goalProgressDocs.ts` 445→451、漏报 `reviewDocs.ts` 351）。 | **S3** | **范围外** | 四件全在别人的在飞件上，本票三件（322／33／82）全在 350 以内。 |
| 7 | 编译门当刻**过不去**（`tsc -b packages/skill-calorie` 红）；且共享 `dist` 被多名写者**反复重编**，整包 CLI 的 import 图一度坏掉（`dist/diet/review.js` 要 `reviewDocs.js` 的 `buildDietReviewEmptyDoc`，对方那一刻还没编出来），`cmd_read.js` 启动即 `SyntaxError`。 | **S3** | **范围外** | 全在别人的在飞件上。**这条直接坑死了前任复核者**（它的 21 条全红就是这么来的），也一度逼得本席的源码级变异作废。**建议编排者收口时先让本图的编译回绿，再合并任何以「本包编译过」为前提的门禁读数。** |
| 8 | **前任 13:08 那套证据整体作废**：`.scratch/t272r/01-tsc.log` 当时就写着编译失败在别人的件上，`run/_table.md` 22 行**全 exit=1／字节全 0**，`category` 列全空、`window` 列全写 `7d`（其实是它自己解析崩了，生成物里 `30d`／`本月`／`custom` 都在）。 | 记账 | 无主 diff | 本席已按 §1.3 诊断并整段重跑取代；**前任的 0/21 结论不得引用**。 |

### 7.2 已知窗口缺陷的归属与现状（派单第 4 项）

派单要求复现「`查高热量排行` 丢 `category` ⇒ 实跑落成『全部排行』」，**本席实测未复现**，逐条如下：

- **已提交的 HEAD 版**：`git show HEAD:packages/skill-calorie/src/diet/routes.ts` 里那条仍是
  `{"window":"7d"}`（**丢 category**）⇒ 缺陷**在提交面上仍然存在**。
- **当刻工作区／`dist`**：`src/diet/routes.ts:96` 已写成
  `{"category":"high_calorie","topN":10,"window":"7d"}`（**未提交**，`git status` 该件为 ` M`），
  且 `dist/triggers/routes.generated.js` 全文件**只有一条**该词的记录、**带 category** ⇒
  真跑出来是 `排行 高热量榜 2026-09-01 ~ 2026-09-07`、折叠块 0、79 662 B，**不是「全部排行」**。
- **归属**：**#276 正在并行修**，其修法已在工作区生效——与本票自述（判据与裁定 §六-3「未动、报编排者」）一致。
  ⇒ 按派单「不单独决定 FAIL」办，本席**只记归属与现状**，不改一行（`routes.ts` 不在本席声明路径）。

### 7.3 结论要点与两处需回答的疑点（本席已自答）

1. 「**第 3 列主指标总热量、但排序按餐均**」——本席一度怀疑是 S1 级排序错误（低热量榜与高热量榜**同序**），
   追到底为**数值巧合**（种子 6 件食物恰好四项一致且排序键并列）；产品排序与 `dietEngine.ts:130-133` **逐条一致**。
   见 §5.4，**降级为范围外观测**。
2. 「**21 条里参数→页面窗口只有 16/21**」——5 条红是**本席／前任 harness** 把路由占位符
   `<开始日期>`／`<结束日期>` 原样喂进真出口所致（`ERR 2: start 非法`），**不是产品缺陷**；
   按票面语义填平种子周后重跑 **21/21 全绿**。见 §3。

---

## 8. 五维参考分（次要摘要）

| 维 | 满分 | 本席给分 | 依据 |
|---|---|---|---|
| 契约一致 | 30 | **29** | 21 条词项面→声明参数 21/21、参数→页面窗口 21/21、五类榜列序按类查表、退化能（缺值 `—`／零值不画条／空榜不出块）逐条对上；扣 1 分给「低热量榜主指标 vs 排序键」那处语义别扭（范围外，但确是可读性契约的瑕疵）。 |
| 证据真实可复现 | 25 | **18** | 加分：作者公布的产物指纹 `83D5FD2C4A6B142C` **与盘上逐位相同**（本席独立核出）；25 条门禁运行在真台账**一对一命中**；产物可复现（21/21）。扣分：⑩ 条是永真断言、两处源码级变异脚本**无提交面**（从盘上不可复跑）、`自证.md` 行数记错（见 §7.1 #1／#2）。 |
| 新旧对照 | 20 | **19** | 老实物 `food_ranking.html` 逐条：五类 thead 逐字对齐、**主指标恒第 3 列**（老实物「写死通用列序 + 值错位」已改正）、前三金银铜三色值与老实物手机端同值且**全族单源**、营养结构三段条＋文字百分比、无数据榜不出折叠块、复制区双按钮＋日志第 4 段命令原文。扣 1 分给「老实物的 `frequent` 页签名是『频繁吃榜』而新侧页题统一成『常吃榜』」——作者援引 #511「一页三名」有据，但**逐字对照老实物时读者会看到对不上的名字**。 |
| 工程红线 | 15 | **15** | 四条提交的写面逐条落在声明路径内、无夹带；未手改 `routes.generated.ts`；未碰 `routes.ts`（不在写集）；本席收工时声明路径下**干净**（`git status --porcelain -- docs/skills/skill-calorie/t272- .scratch/t272r` 为空，逐条点名见 §9）。 |
| 文档同步 | 10 | **9** | `t272-21条真跑.md`／`判据与裁定.md`／`自证.md`／`gate-runs.md` 四件齐全、口径自洽，`docs/skills/skill-calorie/` 归属正确；扣 1 分给 `自证.md` 的行数与盘上不符（§7.1 #2）。 |
| **合计** | **100** | **90** | 无 S1 ⇒ **PASS**。 |

---

## 9. 声明路径逐条点名（收尾时）

`git status --porcelain -- docs/skills/skill-calorie/t272- .scratch/t272r` ⇒ **空**（无脏、无未跟踪残留），
即下列各件全部已提交或本就属于本席声明路径：

- 报告：`docs/skills/skill-calorie/t272-复核-报告.md`
- 脚本（入仓）：`docs/skills/skill-calorie/t272-复核-复跑21条.mjs`（前任留，本席跑通后一并入仓）、
  `docs/skills/skill-calorie/t272-复核-新探针-表列与取值.mjs`（前任留，P3 期望待修，入仓以便查证）、
  `docs/skills/skill-calorie/t272-复核-新探针-全景与空窗.mjs`（本席新写）
- 取证（**不入仓**，按作者同口径产物不随仓入库）：`.scratch/t272r/**` ——
  `w2/`（01-tsc／02-run21／03-probe-legacy／04-probe-new／05-batch68／06-author26／07a-b-ledger 八个日志
  ＋ `run/` 隔离库与 `_table.md`／`_manifest.json` ＋ `probe/`／`pano/` 两套隔离库）、
  `w3/`（run21b／mut-iso／sortcheck／ledger-dry／ledger／gate-recon 六个日志 ＋ `run/` 的 `_table21b.md`／`_rows21b.json`
  ＋ `mut-iso.txt` ＋ `m-abort.txt`）、前任原现场 `01…06b` 与 `probe*/`／`run/` 原样保留。
- 本席自建脚本（住 `.scratch/t272r/`）：`window2.ps1`／`window3.ps1`／`run21b.mjs`／`mut-iso.mjs`／`mut-dist.mjs`／
  `mut-dist2.mjs`／`sortcheck.mjs`／`dbg-db.mjs`／`dbg-seed.mjs`／`winh.ps1`。

## 10. 未做项与下一手缺什么

- **未做：源码级（src→dist）变异自证**。原因是**范围外的**：本包编译当刻过不去（§7.1 #7），源码级变异走不到 `dist`。
  本席以**隔离副本式变异**给出等价证据（§5.1，dist 源件全程只读、逐字节验回）。
  **下一手**：等本图编译回绿后，把 §5.1 那处变异改走源码级复跑一遍即可闭环（脚本已就位：`mut-iso.mjs` 的变异点＋标记）。
- **未做：`本月` 与 `7d` 窗口的**可分辨性**证明**。本次种子日（`SEED_TODAY=2026-09-07`，种子周从 09-01 起）
  让「本月」与「最近 7 天」落在**同一区间** `09-01 ~ 09-07` ⇒ 本席只能证明「两条都对」，**不能**证明
  「把 `本月` 写成 `7d` 会被抓住」。**下一手**：换一个**跨月**的种子日（例如锚点落在月中，使自然月起点 ≠ 锚点−6 天）
  再跑一遍 21 条，即可把这两个窗口拆开。
- **未做：`scripts/build-help.mjs` 等 §五 生成物的对账**。不属本票写集，且当刻另有多红，故只登记不追。
- **下一手最缺的一件事**：**本图编译回绿**。它是本票一切「以产物为前提」的门禁读数的前置；前任复核者正是被它坑死。

---

## 11. 提交与运行标识（回执用）

- **提交 sha**：本报告与三件脚本一并入仓（`4 files changed, 944 insertions(+)`）——
  `docs/skills/skill-calorie/t272-复核-报告.md`、`t272-复核-复跑21条.mjs`、`t272-复核-新探针-表列与取值.mjs`、
  `t272-复核-新探针-全景与空窗.mjs`。提交信息见 `.scratch/t272r/commit-msg.txt`。
  提交经持锁包装器（`runId=ce3cc697-e377-412a-bd17-f4e6a2c856fc`，`waitedMs=0`）；
  此前一次尝试撞上 `ticket=523` 的**活持有者**，按协议 §2「活着的持有者一律不抢回」等到协议上限
  `maxWaitMs=900000` 仍未见释放 ⇒ 按纪律停下，第二次才进锁。
- **本席全部持锁运行标识**（抄自 `.scratch/locks/gate-runs.log`，每条 `RUN … exit=` 与包装器两行一致）：
  | 窗口 | runId | 干了什么 | 包装器读数 |
  |---|---|---|---|
  | w2 | `114aed1b-114c-4c79-825c-8c8df7c9e9f1` | 编译＋21 条真跑（原样声明）＋前任探针＋新探针＋68 批＋26 条＋台账×2 | `waitedMs=60025 exit=0` |
  | w3 | `39402d8c-8dec-4383-82bf-fa3916391ecc` | 21 条填平重跑＋隔离式变异＋排序核对＋台账×2＋门禁对账 | `waitedMs=90081 exit=0` |
  | commit | `9bedbe63-c719-41f7-8551-d238c3ca7edb` | 第一次提交尝试 | `waitedMs=900662 **exit=1**`（协议上限超时，非命令失败） |
  | commit | `ce3cc697-e377-412a-bd17-f4e6a2c856fc` | 提交成功 | `waitedMs=0 exit=0` |
  > 按派单「读数陷阱」条：包装器成功而 pwsh 报 `[exit code: 1]` 是常态（等待进度行写在 stderr），
  > 判成功只看 `RUN ticket=… exit=0` 与 `LOCK-RELEASED … exit=0` 两行。
