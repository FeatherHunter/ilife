# #320 收口 · 实测席证据（当刻 HEAD 61d784f）

**席位**：#320 收口实测席。本席**只跑不改**：未改任何 `src/`、`packages/`、`test/`，未改任何既有证据件与报告，未删 `dist/`、未删 `legacy/`，未碰票面。
**当刻 HEAD**：`61d784f0cd06b34623489112fe1cd6d6a19bbf6e` —— `git rev-parse HEAD` 与 `git rev-parse origin/master` 两次读数逐字相同；`git status --porcelain` 输出为空 ⇒ 工作树干净。
**判官（验收探针）sha256**：`78AE857426A44D66E81A2489E51D0D2790BD16A0812DB78818B1D2E918BEBE39`。
**运行环境**：node v24.19.0；pwsh 7.6.6；全部门禁命令经 `node tooling/run-locked.mjs --ticket t320 --lock-dir .scratch/locks-land --stale-minutes 30 -- <命令>` 持锁运行（窗口内无嵌套），留痕 `.scratch/locks-land/gate-runs.log`，窗口内条目导出为 `docs/skills/skill-calorie/t320-gate-runs.log`。
**时间**：本机 2026-09-14 05:00–05:05（+08:00）；日志里的 `at=` 为 UTC（2026-09-13T21:0xZ）。

**先给结论（七条）**

| 判据 | 命令 | 当刻读数 | 判定 |
|---|---|---|---|
| ① 生成一致性门 | `tsc -b` ＋ `gen-cli.mjs --check` | exit 0；`GEN-CHECK PASS：键 101（写 35 ＋ 读 66）…未搬迁清单 1 条` | **绿** |
| ② 全量测试 | `node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"` | `tests 1492 / suites 160 / pass 1492 / fail 0` | **绿** |
| ③ 验收探针 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `P1..P5 5/5`、`UNACCOUNTED=0`、④＝3 处、守卫 0 行 | **绿** |
| ⑤ 路由 oracle | `node docs/skills/skill-calorie/t313-routing-oracle.mjs` | `faces=16 diffFaces=0 diffs=0 surfaceMissing=0 surfaceExtra=0 DEEP-EQUAL=yes` | **绿** |
| ⑦ 一次性快照 | `node tooling/skill-html-snapshot.mjs --check` | `artifacts=186 changed=0 added=0 removed=0` | **绿** |
| ⑥ 棘轮终态 | 见 §6 | 终态**未成立**（见下） | **红（未达标）** |
| ④ 门禁对账 | 见 §7 | `matched=6/6 scoped=6 undeclared=0`，exit 0 | **绿** |

**一句话**：**五条绿线 ＋ 一次性快照全绿**；但票面 #320 的达成条件①（`cli/cmd_read.ts`／`write.ts` 里 `case 'calorie.` 计数为 0）与达成条件②（棘轮切终态形态）**在当刻 HEAD 上均不成立**——本席按实测报数，不代改、不代实现（详见 §6）。

---

## §1 生成一致性门（判据 ①）

命令（两次，各自一次持锁运行）：

- `node tooling/run-locked.mjs --ticket t320 --lock-dir .scratch/locks-land --stale-minutes 30 -- node node_modules/typescript/bin/tsc -b`
  - 读数：`LOCK-ACQUIRED ticket=t320 runId=d1d78856-7eb4-40be-a438-c5b6431b9355 waitedMs=0 pid=51344`；`LOCK-RELEASED … exit=0`；`RESULT: ticket=t320 runId=d1d78856… waitedMs=0 exit=0`；子进程**无任何输出**（增量构建，无类型错误、无写盘变更）。
- `node tooling/run-locked.mjs --ticket t320 --lock-dir .scratch/locks-land --stale-minutes 30 -- node packages/skill-calorie/scripts/gen-cli.mjs --check`
  - 读数：`RESULT: ticket=t320 runId=ba0fe4e2-7c1a-4a37-8dbb-3e49bd39f660 waitedMs=1 exit=0`。
  - 摘要行**逐字**（取自本席捕获件 `.scratch/t320/g2-gencheck.log`）：

    `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 10 个（analysis／body／diet／exercise／goal／home／photo／profile／weight／workout）声明 100 条，未搬迁清单 1 条`

  - 其中「键 101（写 35 ＋ 读 66）」逐字命中派单期望（101 ＝ 35 ＋ 66）；「未搬迁清单 1 条」与 §6 的 `legacy/scene-03.ts` 恰 1 条声明互证。
  - 生成物逐条印记（同一次运行输出）：`cli/registry.ts`、`base-combos/combos.yaml`、`scripts/build-help.mjs`、`triggers/routes.generated.ts` 四条 `GEN-CHECK ok … sha256=…` 全 ok。

**判定：绿**（exit 0；`GEN-CHECK PASS`）。

---

## §2 全量测试（判据 ②）

命令：

`node tooling/run-locked.mjs --ticket t320 --lock-dir .scratch/locks-land --stale-minutes 30 -- node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"`

读数（runId `bfae26cf-4e33-4628-9d38-079a0af9d7a0`，exit 0）：

```
ℹ tests 1492
ℹ suites 160
ℹ pass 1492
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 37973.5289
```

**判定：绿**。`tests／suites／pass／fail ＝ 1492／160／1492／0`，与派单期望**逐字相符**；`fail 0`、`cancelled 0`。完整输出落 `.scratch/t320/g3-test.log`。

---

## §3 验收探针（判据 ③）

命令：

`node tooling/run-locked.mjs --ticket t320 --lock-dir .scratch/locks-land --stale-minutes 30 -- node docs/skills/skill-calorie/t293-验收-命令自治.mjs`

读数（runId `cc5a7783-4e2c-4995-a486-1acee7904bbd`，exit 0，输出 96 行落 `.scratch/t320/g4-probe.log`）：

- 逐项：`P1: PASS`、`P2: PASS`、`P3: PASS`、`P4: PASS`、`P5: PASS`；汇总行 `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`。
- 动态口径：`动态（派生桶）证据齐=true；UNACCOUNTED=0 处；④ 主体判不出=3 处（只报账、不判红）；树自相矛盾=0 处`。
- ④ 的 3 处**分组**（脚本自报，逐组）：`[退出码／差值] 0 处`｜`[访问器调用] 0 处`｜`[域内量] 1 处`（`test/calorie-routing-81.test.mjs:342`，另一侧＝`c.length`）｜`[域内 metrics] 0 处`｜`[裸标识符] 0 处（单列）`｜`[其它] 2 处`（`packages/skill-calorie/test/cmd-write-40-persist.test.mjs:144` 另一侧＝`row.protein`；同文件 `:419` 另一侧＝`row.carbohydrates`）。
- 形状守卫：`当刻命中 0 行`（守卫未命中 ⇒ 无 `PENDING`；`④ 只保证看得见，不保证看得住`的保留条款不触发）。
- 旁证读数：P2 `权威声明齐=9/9；未归类的可疑副本=0；未归类的事实对=0；仍留在未搬迁清单=0；冻结事实对=12`；P4 `cmd_read.ts：case ' 共 7 处，calorie. 前缀 1 处，物理行 351`／`write.ts：case ' 共 3 处，calorie. 前缀 3 处，物理行 95`；P5 `守 parity 的既有断言命中 7/7`。

**判定：绿**。`5/5`、`UNACCOUNTED=0`、`④=3`、`守卫 0` 四项与派单期望**逐字相符**。

---

## §4 路由 oracle（判据 ⑤）

命令（脚本头部用法 §1 的默认快照路径，不带 `--dist`／`--snapshot`，即对冻结基线深比对）：

`node tooling/run-locked.mjs --ticket t320 --lock-dir .scratch/locks-land --stale-minutes 30 -- node docs/skills/skill-calorie/t313-routing-oracle.mjs`

读数（runId `cd62d19e-3144-46d2-b7dd-6c58a6b1414d`，exit 0）：

- 汇总：`RESULT: SUMMARY faces=16 diffFaces=0 diffs=0 surfaceMissing=0 surfaceExtra=0 DEEP-EQUAL=yes SNAPSHOT-SHA=d4da337219ea DIST-SHA=4602f79d6927`
- 16 个面**逐面** `DEEP-EQUAL=yes`（before＝now）：`EXPORT_SURFACE 15`、`WAKE_ROUTES 436`、`NEW_KEY_ROUTES 58`、`COVERAGE_REPAIR_ROUTES 1`、`ALL_ROUTES 495`、`EXEC_ROUTES 344`、`HIT_NOT_EXEC_ROUTES 92`、`OUT_OF_SCOPE_ROUTES 10`、`TWIN_WAKE_WORDS 24`、`T71_DIFFS 7`、`NON_EXEC_REASONS 23`、`ROUTES_BY_WAKE_WORD 493`、`EXEC_ROUTE_BY_KEY 101`、`ROUTING_SUMMARY 8`、`DIRECT_API_PROBES 10`、`RECORD_SHAPES 7`。
- 差异数：`diffs=0`（无 `DIFF …` 明细行）。
- 三道门读数：①快照自哈希校验**通过**（`…selfSha256=b1376cba41a5a07e1cc8e92aa3eb5331c38db4c991a4d401cfaf155fbfde9dd2（自哈希校验通过）`）；②导出面清点 `before=15 now=15`，无 `SURFACE-EXTRA` 警告行、`surfaceMissing=0`；③面深比对全绿。
- 溯源：`dist=packages/skill-calorie/dist/triggers/routing.js sha256=4602f79d692769c3c68ddb833efcbf6d30c0a669431197f75a4413574ff466c9`；`src=…/routes.ts sha256=d277c8e510ca1c5c89800d6f82e12f10db5e2da873568908e2ef1506ae04169c`；冻结基件 `t313-routing-exec-face-before.json` sha256 `d4da337219ea98f61ee6d3a42c10898fbedd017004806c90a8b2281eb6eb02da`。当刻那份 `dist` 是本席先跑 `tsc -b`（§1，exit 0）之后的产物，**不是旧 dist**（`DIST-SHA` 与基件 `meta.distSha256=cf9b15a4…` 不同属正常：`meta` 段不入比对，这正是「跑的是新建产物」的痕迹）。

**判定：绿**（`DEEP-EQUAL=yes`，差异 0）。

---

## §5 一次性快照补跑（判据 ⑦）

`package.json:18` 当刻脚本名 `snapshot:html:check` ＝ `node tooling/skill-html-snapshot.mjs --check`，按派单要求**用 node 直调其入口**（不用 `pnpm help:build`）：

`node tooling/run-locked.mjs --ticket t320 --lock-dir .scratch/locks-land --stale-minutes 30 -- node tooling/skill-html-snapshot.mjs --check`

读数（runId `e3000468-f538-475f-9647-31907c4bb61f`，exit 0）：

```
OK: 5 技能 HTML 快照 == 实际（186 件产物，base-* 指纹 ef7cc02969e37e4bfc0eb35751b8dc1c，base-* 文件 27 件）
RESULT: artifacts=186 changed=0 added=0 removed=0 base-* fingerprint=ef7cc02969e37e4bfc0eb35751b8dc1c
```

**判定：绿**（`changed=0 added=0 removed=0`，5 技能一致）。

---

## §6 棘轮终态（判据 ⑥：三项分开报）

### §6.1 全仓 `case 'calorie.` 计数（**不是 0**）

命令（读侧，不改树）：`git grep -n "case 'calorie\."`（＋ `git grep -c "case 'calorie\." -- packages test tooling` 互证）。

读数：**全仓 66 行命中**，按文件逐条列（`：` 后为命中行数）：

- **真实 `case` 标签 25 处**（代码）
  - `packages/skill-calorie/src/cli/cmd_read.ts`：**1**（`:158  case 'calorie.view.goal-weight': {`）
  - `packages/skill-calorie/src/cli/write.ts`：**3**（`:60  case 'calorie.profile.set':`、`:61  case 'calorie.profile.activity':`、`:63  case 'calorie.profile.update':`）
  - `packages/skill-calorie/src/output.ts`：**21**（`:336`–`:411` 一段回执装配 switch：`diet.add/update/batch/copy/update-by-date/remove-by-date/remove-by-range/remove-by-type`、`water.log`、`weight.log/update/remove/batch`、`exercise.add/update/remove`、`photo.add/tag`、`product.add/update`、`photo.gif`）
- **来路注释 13 处**（不是代码，是「#316／#318 搬迁」的出处注释里引用了标签）
  - `src/exercise/` 9 件各 1（`cardio.ts:3`、`distribution.ts:3`、`edit.ts:3-4` 2 处、`goal.ts:3`、`log.ts:3`、`recap.ts:3`、`strength.ts:3`、`trend.ts:3`）＝ 10 行
  - `src/goal/read.ts:3`、`src/goal/write.ts:3`、`src/profile/read.ts:3`、`src/profile/write.ts:3` ＝ 4 行
  - （上两行合计 13 行 ＋ 25 处真实标签 ＝ `packages/` 下 38 行）
- **测试件自述 2 处**：`packages/skill-calorie/test/cmd-registry-294.test.mjs:4`、`:17`（棘轮测试自己的头部注释与口径说明）
- **docs/ 26 行**：历史证据／报告里引用该标签的正文（`t116-assets` 2、`t63` 1、`t91` 2、`t99` 1、`t163` 1、`t164` 1、`t168` 5、`t294` 2、`t314R` 1、`t316r` 2、`t318` 4、`t318r` 4）

对齐票面：票面 #320 达成条件①写的是「**`cli/cmd_read.ts`／`write.ts` 里 `case 'calorie.` 计数为 0**」。**当刻实测 1 ＋ 3 ＝ 4 处，不为 0** ⇒ 该条**未成立**。即使按更宽的「全仓」口径（本席另跑），答案同样是「不为 0」（25 处真实标签里 21 处在 `src/output.ts` 的回执装配 switch、4 处在两个分派文件）。

### §6.2 逐文件行数（预算 ≤200）

`packages/skill-calorie/src/cli/`（`*.ts` ＋ `legacy/` 递归），读数＝行数／字节：

| 文件 | 行数 | 判定（≤200） |
|---|---|---|
| `cli/cmd_read.ts` | **351** | **超线（>200）** |
| `cli/keys.ts` | 149 | ok |
| `cli/write.ts` | 95 | ok |
| `cli/registry.ts` | 50 | ok |
| `cli/legacy/index.ts` | 47 | ok |
| `cli/legacy/routes/index.ts` | 29 | ok |
| `cli/legacy/routes/scene-01..10.ts` | 11／24／30／11／11／12／11／13／12／12 | ok |
| `cli/legacy/scene-01..10.ts` | 13／12／13／12／14／15／15／13／13／13 | ok |
| `cli/legacy/types.ts` | 16 | ok |

**超 200 线的逐条列名**：`packages/skill-calorie/src/cli/cmd_read.ts` ＝ **351 行**（唯一一件）。

### §6.3 棘轮形态（票面达成条件②）

`packages/skill-calorie/test/cmd-registry-294.test.mjs` 当刻仍是**旧形态**：`FROZEN_READ_CASES_MAX = 61`／`FROZEN_WRITE_CASES_MAX = 31`（`:81`、`:82`），断言是「两个分派文件里的 `case 'calorie.…'` 标签集合**只许从冻结清单里减少**」（`:92`–`:102`）＋「行数 ≤ 冻结值 `FROZEN_READ_LINES = 1146`／`FROZEN_WRITE_LINES = 745`」（`:42`、`:43`、`:107`–`:110`）。**未见**「禁止出现 `case 'calorie.`」的终态断言，也**未见** ≤200 行的行数预算。票面达成条件②「棘轮切到终态形态（禁止出现 `case 'calorie.`）＋行数预算（建议 ≤200 行）」⇒ **未成立**。

（本席只报数：全量测试仍 `1492/1492` 全绿，说明旧形态棘轮与当刻源码自洽；终态形态的落地属实施席动作，不在本席范围。）

### §6.4 `legacy/` 与 `legacy/routes/` 残留清单（**只报不删**）

`git ls-files packages/skill-calorie/src/cli/legacy` ＝ **22 件**，全部仍在索引里，**一件未删、一件未改**。

- **命令清单分片 `legacy/scene-NN.ts`（10 件）**：**有内容 1 件** —— `scene-03.ts`（`:13` 起恰 **1 条**声明，与 §1 的「未搬迁清单 1 条」互证）；**空壳 9 件** —— `scene-01/02/04/05/06/07/08/09/10.ts`（均为 `export const LEGACY_SCENE_NN: readonly LegacyCommandDecl[] = [];`，留文件头与 `routing.ts` 字样给出「键 → 场景」的依据出处）。
- **路由声明分片 `legacy/routes/scene-NN.ts`（10 件）**：**有内容 5 件** —— `scene-02.ts`（6 条）、`scene-03.ts`（19 条）、`scene-06.ts`（1 条）、`scene-08.ts`（2 条）、`scene-10.ts`（1 条），合 **29 条** `RouteDecl`；**空壳 5 件** —— `scene-01.ts`、`scene-09.ts`（同行 `= [];`）与 `scene-04/05/07.ts`（多行写法 `= [`…`];`，三件字节数同为 767）。
- **非分片件 3 件（非空壳）**：`legacy/index.ts`（47 行，十片汇总）、`legacy/routes/index.ts`（29 行，路由汇总）、`legacy/types.ts`（16 行，只给声明形状）。
- 分片件里**没有**任何 `case '…'` 标签（`git grep -n "case '" -- packages/skill-calorie/src/cli/legacy` 零命中）。
- 引用关系：生成器 `scripts/gen-cli.mjs`（扫 `src/cli/legacy/*.ts` 按文件名升序）与 `scripts/gen-routes.mjs`（读 `dist/cli/legacy/routes/scene-NN.js`）＋ 测试 `test/declared.mjs` / `legacy-ratchet-295` / `legacy-partition-313` 仍以这些件为声明源 ⇒ **空壳件是有断言的活件（片数＝文件面等号断言），不是可随手删的死件**。

---

## §7 门禁对账（判据 ④）

### §7.1 事先声明（GATE-RUN；runId 逐条取自本席当刻在 `.scratch/locks-land/gate-runs.log` 里的真实记录）

- GATE-RUN runId=d1d78856-7eb4-40be-a438-c5b6431b9355 cmd="node node_modules/typescript/bin/tsc -b"
- GATE-RUN runId=ba0fe4e2-7c1a-4a37-8dbb-3e49bd39f660 cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"
- GATE-RUN runId=bfae26cf-4e33-4628-9d38-079a0af9d7a0 cmd="node --test test/*.test.mjs packages/*/test/*.test.mjs"
- GATE-RUN runId=cc5a7783-4e2c-4995-a486-1acee7904bbd cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
- GATE-RUN runId=cd62d19e-3144-46d2-b7dd-6c58a6b1414d cmd="node docs/skills/skill-calorie/t313-routing-oracle.mjs"
- GATE-RUN runId=e3000468-f538-475f-9647-31907c4bb61f cmd="node tooling/skill-html-snapshot.mjs --check"

（六条对应 §1／§2／§3／§4／§5 的六次持锁运行，全部 `exit=0`。）

### §7.2 对账命令（按 `tooling/check-gate-audit.mjs` 头部用法 §1／§5 的真实参数）

```
node tooling/run-locked.mjs --ticket t320-audit --lock-dir .scratch/locks-land --stale-minutes 30 -- node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t320-收口-证据.md --ticket t320 --log .scratch/locks-land/gate-runs.log --export docs/skills/skill-calorie/t320-gate-runs.log
```

- **票号口径（自指问题的解法，明写不留暗门）**：对账窗口取 `--ticket t320`，即本席被声明的六次运行；**对账自身那次运行**在 `run-locked` 侧的留痕票号写作 `t320-audit`，故不进 `t320` 窗口。「为什么不能声明它」：`--require-claims` 的认领池只含「先于声明存在」的条目，对账不可能在跑之前声明它自己；把它排除在窗口外是本席当刻选的唯一不编造、不隐藏的办法——该条目本身在 `gate-runs.log` 里逐字可查（`ticket=t320-audit`）。
- 未用任何 `--allow-*` 放宽开关，故不需要 `GATE-RELAX` 留痕；`--since`／`--until` 未使用（`t320` 票号下不存在窗口外条目，证据见 §7.3／§7.4 的 `undeclared=0`）。

### §7.3 对账读数（对账 run#1，runId=19bbeda9-bac4-4d91-a5e3-f5f6549172dd）

输出逐字（本席捕获件 `.scratch/t320/g8-audit.log`）：

```
证据：D:\ilife\docs\skills\skill-calorie\t320-收口-证据.md
审计：D:\ilife\.scratch\locks-land\gate-runs.log（RUN 条目 287 条；窗口内 6 条，ticket=t320）
声称运行 6 条
EXPORT: D:\ilife\docs\skills\skill-calorie\t320-gate-runs.log（窗口内 RUN 条目 6 条，受 git 跟踪）
RESULT: matched=6/6 auditEntries=287 scoped=6 undeclared=0
gate-audit: PASS
```

- **matched 6/6｜scoped 6｜undeclared 0｜exit 0（`gate-audit: PASS`）**。
- 逐条认领一对一（证据行 181–186 ↔ 日志 runId，六条全部命中冻结清单里的本次运行），无「同 cmd 历史条目顶替」；未用任何 `--allow-*` 放宽，故无 `GATE-RELAX` 留痕。
- 导出件 `docs/skills/skill-calorie/t320-gate-runs.log`（受 git 跟踪）＝窗口内 6 条 RUN。
- 本节（§7.3）不含任何 `GATE-RUN` 声明行，插入它不改变 §7.1 的六条声明与 `matched／scoped` 口径；含本节的**终稿**复跑读数见 §7.4。
### §7.4 终稿复跑（对账 run#2，runId=d7a7019a-8252-40bd-b919-945c0ca74fac）

含 §7.3 的终稿上，逐字重跑 §7.2 同一条命令（唯一差别：审计日志多了一条上一轮对账自身的 `t320-audit` 留痕，`auditEntries 287 → 288`）：

```
RESULT: matched=6/6 auditEntries=288 scoped=6 undeclared=0
gate-audit: PASS
```

- **判定：PASS**（`matched 6/6｜scoped 6｜undeclared 0｜exit 0`），与 §7.3 读数逐字相同（仅 `auditEntries` 随日志增长）。
- 复核用法（终稿上可复现，**无需任何 `--allow-*`**）：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t320-收口-证据.md --ticket t320 --log .scratch/locks-land/gate-runs.log`。
- 复核口径提示：`--ticket t320` 窗口内恰为 §7.1 的六条；本席另跑的两轮对账自身留痕票号为 `t320-audit`，不落在该窗口内（§7.2 已交代口径）。

---

## §8 未做项与偏差（自报）

1. **票面 #320 的两项终态当刻未成立，本席不代实现**（§6.1／§6.3）：达成条件①「两个分派文件里 `case 'calorie.` 计数为 0」实测 **1 ＋ 3 ＝ 4**；达成条件②「棘轮切终态形态＋行数预算（建议 ≤200 行）」当刻仍是「冻结清单只许变短」的旧形态，且 `cli/cmd_read.ts` **351 行 > 200**。这两条属实施席动作，本席只报数。
2. 未跑 `pnpm build`（按派单只跑 `tsc -b` ＋ `gen-cli.mjs --check`）；未跑 `gen-cli.mjs --stamp`（会改生成物）。
3. 未跑派单七条之外的其它门（`test:types`／`boundaries`／`publish:*`／`help:examples:check`／`gate:selftest`），未擅自扩大范围。
4. 未删任何文件：`dist/` 未动、`legacy/` 只读（22 件全在 git 索引，§6.4）；未 `--amend`／`--force*`／rebase。
5. §6.1「全仓」口径＝`git grep`（**只覆盖受 git 跟踪的文件**）；未跟踪目录（`.scratch/`、`dist/`、`node_modules/`）不计入——含未跟踪件的口径只会更大，不会更小。
6. 未碰票面、未写票面百分比、未 close 任何票（编排者职责）。
### §7.5 受跟踪导出源复核（对账 run#3，runId=5e109478-cd7f-44d3-8aa2-e4dcf55322c7）

再以**受 git 跟踪的导出件**当审计源重跑（`--log docs/skills/skill-calorie/t320-gate-runs.log`，不带 `--export`，以免覆写导出件的来源头）：

```
审计：D:\ilife\docs\skills\skill-calorie\t320-gate-runs.log（RUN 条目 6 条；窗口内 6 条，ticket=t320）
RESULT: matched=6/6 auditEntries=6 scoped=6 undeclared=0
gate-audit: PASS
```

⇒ 第三方**只拿本件 ＋ 导出件**（不必碰 gitignored 的 `.scratch/`）即可复现对账：六条声明与六条 RUN 一对一，`undeclared=0`。三轮对账（§7.3／§7.4／§7.5）读数一致，`matched` 均 `6/6`、`undeclared` 均 `0`、`exit` 均 `0`。