# t519 · W1 证据（场景 10 · 报告族 8 页重排：底座与页框 ＋ 先验 BMI）

> 票：#519（地图 #161 子票，`wayfinder:task`）。体例照 `docs/skills/skill-calorie/t517-W1-共享形状与样板-证据.md`。
> 强制对照基准：`docs/skills/skill-calorie/t516-场景10-视觉整改基准.md`（§一 J1–J9 ＝验收单）。
> 本件只记**事实与机器读数**；五维分归**独立视觉席**（票面要求，本席不自行打分）。
> 全部命令经加锁包装器 `--ticket 519 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000`；
> 长输出落 `.scratch/t519/*.log`，本件只引摘要行与尾行。**不 push、不 close**（编排者裁定）。

## 〇 · 判定与摘要

| 项 | 读数 |
|---|---|
| 主判据 J1（分隔符与内部标识符） | **绿**：8 页 `RESULT: 8/8` ＋ `PASS`、exit 0、节点级 **0**（改前逐页 **5/6/6/5/5/5/4/10**） |
| J2（形状件真的用上） | **绿**：`conclusion=1`／`toc=1`／`caliber=2…3`／`chip=3…6`／`status-badge=0…2`／`data-table=1…5`（改前 `toc=0 caliber=0 conclusion=0 chip=0`） |
| J3（`overflow:hidden` 裁掉） | **绿**：8 页 × 4 档 `OVERFLOW-ZERO pages=8 cells=32 failed=0 scopeOutFailed=0`，逐页 `clip=0`（改前 21／22 各 2 处，本 8 页 0 处） |
| J6（主内容列利用率） | **绿**：1440 档 `shell x=160 w=1120`（内容列 1080）、两侧余量各 **160 对称**；顶层区块（`section`／结论条／导航／口径行）全 **1080**＝铺满 |
| J7（同排卡等高与孤行） | **绿**：四档 `kpi每排=2+2`／`2+2`／`2+2`／`4`，`kpi极差=0`，无 3＋1 孤行 |
| J8（页内导航锚点自洽） | **绿**：8 页导航项与页内 `<section id>` **双向一一对应**（孤儿 `href` 0／孤儿 `id` 0；逐页 6／4／6／4／4／4／5／6 项） |
| J9（三条恒出） | **绿**：8 页「页内导航 1 ＋ 口径说明行 1–2 ＋ 来源脚注（📊 数据来源：）」齐 |
| J4／J5（字号阶梯／断点档数） | **未做**（已由编排者具名改期 [#567](https://github.com/FeatherHunter/ilife/issues/567)）：两项量的是公共层共享样式表整段，落点 `packages/base-render/**`（本票只读）。当刻实测：窄档实际渲染字号 **11px**（7–9 档，含图表档）。**只报读数与归属，不作本票红项**（引 #518 裁定 H 原句的同一处置） |
| 靶向测试 | **绿**：`analysis-report-384` ＋ `analysis-accept-386` → `ℹ tests 13 ｜ pass 13 ｜ fail 0`；新增 `analysis-shape-report-520` → `ℹ tests 7 ｜ pass 7 ｜ fail 0` |
| 包内两道门 | **绿**：`check-warning-line.mjs` → `RESULT: 91/91` ＋ `PASS: 告警线台账齐全且与实况一致`；`gen-cli.mjs --check` → `GEN-CHECK PASS：键 128（写 46 ＋ 读 82）；能力 10 个；未搬迁清单 0 条` |
| 变异自证 | **绿**：四处源码级变异各打一条**不同**判据，全红 → 逐文件还原 → 全绿（两行机器读数见 §五） |
| vision 审查 | **不由本席做**（票面：交独立视觉席给 8 页 1440／390 打分；本席只交产物） |

**主形态**：接上现成件，**零新增形状**（基准件 §4.3 的「新建形状具名清单」本票仍为**空**）。

## 一 · 本窗改了什么（逐文件）

| 文件 | 改了什么 | 行数 |
|---|---|---|
| `packages/skill-calorie/src/analysis/reportDocParts.ts` | **页框公共件**：`ReportSection`／`sec()`／`sectionsHtml()`／`navOf()`／`reportChips()`／`sourceFootnoteOf()` 六件；`metaLeftOf` 区间写「至」、天数进括号；`kvTable` 改「项 ｜ 取值」两列（不再从题名里切列名）；**删掉 `commandNote`**（命令原文改住复制载荷） | 135→167 |
| `packages/skill-calorie/src/analysis/reportDoc.ts` | **多态底座补页框**（仍是**一个**底座）：页头胶囊 → 结论条 → 页内导航 → 锚点区块 → 口径行 → 来源脚注 → 复制区；`KIND_LABELS` 的 `compare` 括号改全角；新增 `TOPIC_OF`／`conclusionOf`／`calibersOf`／`bandSentence`／`avgCalories`／`trackedConclusion` | 98→191 |
| `packages/skill-calorie/src/analysis/reportDocBmi.ts` | 改回 `ReportSection[]`；`当前 BMI` 卡的 `日期 · 分级` 拆槽；分级表下补徽章列；空记录走空态件 | 90→92 |
| `packages/skill-calorie/src/analysis/reportDocTracked.ts` | 四个装配件改回 `ReportSection[]`；身份三件事从斜杠串拆成键值行；缺项清单 `、`→空格；补达标／危险天数徽章列 | 138→180 |
| `packages/skill-calorie/src/analysis/reportDocScore.ts` | 三个装配件改回 `ReportSection[]`；两期区间写「至」；卡片说明里的 `·` 串改徽章列；口径表两处的 `、`／`／` 串改写 | 159→181 |
| `packages/skill-calorie/test/analysis-shape-report-520.test.mjs` | **新增**：报告族形状判据 7 条（三条恒出／锚点双向自洽／形状件接上／可见文本零分隔符／页头三件与页宽／8 形态互不相同／空窗兜底） | 新 |
| `packages/skill-calorie/test/analysis-report-384.test.mjs` | **授权改写 4 处**（见 §二） | — |
| `packages/skill-calorie/test/analysis-accept-386.test.mjs` | **授权改写 1 处**（见 §二） | — |

**未碰**：`packages/base-render/**`（一行未动）、`src/render/trendDocs.ts`／`trendPredictDocs.ts`（预测族地盘）、`src/analysis/reportPlate.ts`（**本窗零改动**——`energyOfWindow` 的真体重口径原样保留）、别席在途件（`src/exercise/**`、`scene06-验收墙/**`、`t543-*`）。

## 二 · 编排者具名授权：4 处／5 条冻结断言的形状改写

**授权原句**（`gh issue view 519 --comments`《编排者授权（2026-09-16 · 报告族 4 条冻结断言的形状改写）》）：

> **范围**（仅这 4 处；执行前按当刻行号逐处核对，行号漂移以「包含的字符串」为准）：
> - `packages/skill-calorie/test/analysis-report-384.test.mjs:166` —— 现要求 `html.includes('卡里路·' + title)`（即 J1-R1 的债）
> - `packages/skill-calorie/test/analysis-report-384.test.mjs:167` —— 现要求 `html.includes('卡路里 · 报告')`
> - `packages/skill-calorie/test/analysis-accept-386.test.mjs:304` —— 同上
> - `packages/skill-calorie/test/analysis-report-384.test.mjs:223`／`227` —— 现要求页面自述两期窗口写成 `(\d{4}-\d{2}-\d{2}) ~ (\d{4}-\d{2}-\d{2})` 且恰好 2 处（即 J1-R6 的债）
>
> **条件（缺一即授权失效，恢复字面口径）**：1. **同语义、新形式、只加强不删减**……2. **每处新增对偶断言**：**可见文本里不得再出现旧字符**（`·`）与该旧字符串（`卡路里 · 报告`），并且页面标题里不得出现 `~`。3. **原文留底**：改写前的原文逐行落进**受跟踪**的证据件……4. **变异两行**……5. **不许顺手改这四件里的其它断言**：`analysis-report-384` 的「拿错页即红」「日均总消耗两期同源真体重」两条**逐字保留**。

**逐处行号核对（执行前实测，未漂移）**：`analysis-report-384.test.mjs` 的 `:166` `:167` `:223` `:227` 与 `analysis-accept-386.test.mjs` 的 `:304` 与授权所列**逐行一致**。
（`report-384` 的 `:226`／`:227` 两行在授权里写作 `:223`／`:227`——`:223` 是那条 `spans` 正则所在行；本件按「包含的字符串」为准逐处核对：`:223` 的 `matchAll(/(\d{4}-\d{2}-\d{2}) ~ (\d{4}-\d{2}-\d{2})/g)` 与 `:226` 的 `spans[1]` 比对都在改写范围内。）

### 2.1 before／after 对照表（原文留底，逐行照抄 `git show HEAD:` 与当刻盘上文件）

| # | 件:行（改前） | before（原文，逐字） | after（原文，逐字） | 同语义的加强 |
|---|---|---|---|---|
| A | `analysis-report-384.test.mjs:166` | `assert.ok(html.includes('卡路里·' + c.title), c.word + ' 缺页面标题（疑似落回 full 健康盘）');` | `assert.ok(html.includes('<title>卡路里 ' + c.title + '</title>'), c.word + ' 缺页面标题（疑似落回 full 健康盘）');` | 语义一件不少（仍逐字钉住「这一页是它自己那个形态」），只把 `·` 换成半角空格；**并加严**：从「整串包含」改成「`<title>` 元素内逐字相等 + 闭合标签」，题名跑到正文里不再算过 |
| B | `analysis-report-384.test.mjs:167` | `assert.ok(html.includes('卡路里 · 报告'), c.word + ' 缺类型徽标');` | `assert.ok(html.includes('<div class="type-badge">报告</div>'), c.word + ' 缺类型徽标');` | 语义一件不少（页型徽章在场）；**并加严**：钉住徽章元素内**恰好一个词「报告」**（#516 §3.2 D04），徽章里再出现 `·` 串即红 |
| C | `analysis-accept-386.test.mjs:304`（同一件事的姊妹件） | `assert.ok(rec.html.includes('卡路里·' + KIND_LABELS[expectKind]), t.wake_word + ' 页面标题不是它自己那个形态（疑似拿错页）：期望「卡路里·' + KIND_LABELS[expectKind] + '」');` ＋ `assert.ok(rec.html.includes('卡路里 · 报告'), t.wake_word + ' 缺报告类型徽标');` | `assert.ok(rec.html.includes('<title>卡路里 ' + KIND_LABELS[expectKind] + '</title>'), …)` ＋ `assert.ok(rec.html.includes('<div class="type-badge">报告</div>'), …)` | 同上（A＋B 两处的同语义加强） |
| D | `analysis-report-384.test.mjs:223` | `const spans = [...new Set([...html.matchAll(/(\d{4}-\d{2}-\d{2}) ~ (\d{4}-\d{2}-\d{2})/g)].map((m) => m[1] + '~' + m[2]))];` | `const spans = [...new Set([...html.matchAll(/(\d{4}-\d{2}-\d{2}) 至 (\d{4}-\d{2}-\d{2})/g)].map((m) => m[1] + '~' + m[2]))];` | 语义一件不少（「页面上恰好自述两期」）；`spans` 的**内部拼法**（用 `~` 拼接）是测试自用中间量、不是产物文本，**一字未动** |
| E | `analysis-report-384.test.mjs:226`／`:227` | `const [curStart, curEnd] = spans[0].split('~');` … `assert.equal(spans[1], prevStart + '~' + prevEnd, win + ' 对比期不是紧邻本期的等长窗口：' + spans[1]);` | **原样保留**（只换上面那条正则） | 「第二期＝紧邻本期的等长窗口」逐字保留 |
| F | `analysis-report-384.test.mjs:165` 之后（**新增对偶**） | —（原来没有） | `const vis = html.replace(/<(style\|script)\b[^>]*>[\s\S]*?<\/\1\s*>/gi, ' ').replace(/<!--[\s\S]*?-->/g, ' ').replace(/<[^>]*>/g, ' ');` ＋ `assert.ok(!vis.includes('·'), …)` ＋ `assert.ok(!html.includes('卡路里 · 报告'), …)` ＋ `assert.ok(!vis.includes('~'), …)` | 条件 ② 要的三条对偶断言；口径与 `scripts/audit-separators.mjs` 同源（同族先例＝#517 在 `analysis-deficit-385.test.mjs` 里加的那条「可见文本零并列分隔符」） |
| G | `analysis-report-384.test.mjs:222` 之后（**新增对偶**） | —（原来没有） | `assert.ok(!vis.includes('~'), win + ' 可见文本里出现 \`~\`（#516 判据 R6 的债）');` | 条件 ② 要的第三条对偶断言（两期窗口那一支） |
| H | `analysis-accept-386.test.mjs:302` 之后（**新增对偶**） | —（原来没有） | `assert.ok(!vis.includes('·'), …)` ＋ `assert.ok(!rec.html.includes('卡路里 · 报告'), …)` | 条件 ② 的同一条，在姊妹件上对称加强 |
| I | `analysis-report-384.test.mjs:81`（`CASES` 的 `title`） | `title: '健康报告(含对比)'` | `title: '健康报告（含对比）'` | 随 `KIND_LABELS.compare` 的括号全角化同步（#518 W5 §A2 的 H1 括号纪律；半角只留在唤醒词／路由里）。这是 A 行断言的**必要一步**——不同步它当场红 |

**条件 ⑤ 的落实（不许顺手改其它断言）**：`analysis-report-384` 的「拿错页即红」（`:172`–`:187` 那条 `8 条产物互不相同`）与「`#384f 日均总消耗两期同源真体重`」（`:189`–`:246`）**逐字未动**，本窗实测仍绿（§四）。`:227`–`:238` 的独立复算（`TO_TDEE`／`lastWeightIn`／`cells[3]`／`cells[4]`）亦**逐字未动**。
**未在授权范围内、故未动的一条（如实登记给复核席）**：`analysis-accept-386.test.mjs:329`／`:350` 的 `assert.ok(!rec.html.includes('卡路里 · 报告'), …)` 不在授权清单里，本窗**一行未改**；它当刻仍绿（该串在产物里已不存在），语义随新写法变弱（原来钉的是「徽章在场」，现在恒真）——**是否收严由编排者另裁，本席不越权**。

### 2.2 改前的红（判据先行，如实记）

改写前跑靶向两件（`runId=02a6bac3-1f0e-4cf4-86e7-598b21093c9a`，`exit=1`）：`ℹ tests 13 ｜ pass 10 ｜ fail 3`，红因逐条：

```
AssertionError [ERR_ASSERTION]: 看BMI报告 页面标题不是它自己那个形态（疑似拿错页）：期望「卡路里·BMI 报告」
AssertionError [ERR_ASSERTION]: 看BMI报告 缺页面标题（疑似落回 full 健康盘）
AssertionError [ERR_ASSERTION]: 7d 页面没给出两期窗口：[]
```

⇒ 红**只**出在这 3 处（`:167`／`:304` 因前一条同测试内先抛而未执行到）——即「冲突面恰好是授权点名的那几处」，没有第四条。

## 三 · 基准件 §一 J1–J9 逐条读数（命令照抄基准件「怎么量」列）

> 判据面＝`.scratch/t519/样板/`（本窗重出的报告族 8 页，生成器 `.scratch/t519/gen8.mjs`：种子与命令参数**逐字照抄** `.scratch/t387/run-all.mjs`，便于 W4 交叉核对）。

| # | 命令（逐字） | 改前（同一批产物重出前的基线） | 改后（本窗） |
|---|---|---|---|
| J1 | `node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t519/样板 --json .scratch/t519/W1-sep.json` | 逐页 **5/6/6/5/5/5/4/10**（节点级） | **`RESULT: 8/8` ＋ `PASS`、exit 0、节点级 0** |
| J2／J6／J7 | `node docs/skills/skill-calorie/t516-判据-版式.mjs --dir .scratch/t519/样板 --widths 390,512,820,1440 --json .scratch/t519/W1-layout.json` | — | 逐页摘要行：`窄档字号=11（7–9档） kpi每排=2+2 kpi极差=0 1440列宽=1120/1440 toc=1 caliber=2 conclusion=1 越界=0` |
| J3 | `node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t519/样板 --widths 390,512,820,1440 --json .scratch/t519/W1-measure.json` | — | **`OVERFLOW-ZERO pages=8 cells=32 failed=0 scopeOutFailed=0`**；逐页 `clip=0` |
| J8／J9 | `node .scratch/t519/probes.mjs --dir .scratch/t519/样板` | — | **`PROBES: 8/8 页三条恒出齐且锚点双向自洽 RESULT: PASS`** |

**回归底线（基准件 §二）复跑**：横向溢出 8×4＝32 格全 0 ✓；`img` 计数 0／`unfitted` 0 ✓；外部引用 0（单文件、样式与脚本全内联）✓；KPI ≤640 二列（实测 390／512／820 皆 2+2）✓；数值列 `min-width:5.5em` ＋ 等宽数字（公共层，未动）✓；窄屏表格紧凑档（公共层，未动）✓。
**J6 的 third 问**：1440 档 `.ilife-block-page-shell-body` 的直接子件＝`style`(w=0)／3 枚 `ilife-block-chip`（行内件，与 #517 样板页同形）／`ilife-block-conclusion`(1080)／`ilife-block-toc`(1080)／6 个 `section`(1080)／2 条 `ilife-block-caliber`(1080) ⇒ **区块全 1080＝铺满**（与 `.scratch/t517/probe-after.json` 的 21 页读数**逐项同形**）。
**J6 的「表宽 680px 上限」不在本票**：该封顶住公共层 `blocks.ts`（#512 的设计），归 [#567](https://github.com/FeatherHunter/ilife/issues/567)；本窗用「区块锚点外壳」让**顶层**区块回到内容列宽（与 #517 同一手法），未动公共层一行。

## 四 · 靶向测试与两门（只领 exit=0）

| 判据 | 命令 | 摘要行 |
|---|---|---|
| 靶向两件 | `node --test …/analysis-report-384.test.mjs …/analysis-accept-386.test.mjs` | **`ℹ tests 13 ｜ pass 13 ｜ fail 0`**（含「拿错页即红」「日均总消耗两期同源真体重」两条逐字保留者，仍绿） |
| 新增断言件 | `node --test …/analysis-shape-report-520.test.mjs` | **`ℹ tests 7 ｜ pass 7 ｜ fail 0`** |
| 包内门 ① | `node packages/skill-calorie/scripts/check-warning-line.mjs` | **`RESULT: 91/91` ＋ `PASS: 告警线台账齐全且与实况一致`**；台账 44 行／扫描面 328 件／超线 40 件／剔出生成物 3 件 ⇒ **本窗未新增超线件，无需 `--sync`**（五件改后 LF：191／167／92／180／181，全在 350 线内） |
| 包内门 ② | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | **`GEN-CHECK PASS：键 128（写 46 ＋ 读 82）；能力 10 个；未搬迁清单 0 条`** |
| 编译 | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | exit 0（零错误行） |

## 五 · 变异自证（四处，各打一条**不同**的判据）

电池：`.scratch/t519/mutate.mjs`（照 #517 `mutate-proof.mjs` 三道闸：跑前自检源码＝pristine 快照／锚点唯一；`process.on('exit')` 崩溃兜底；写回后核 sha256）。**每处变异整段在同一持锁窗口内跑完**（编译 →（必要时）重出产物 → 跑判据 → 逐字节还原 → 复核哈希 → 复跑）。

**两行机器读数**（逐字抄电池输出）：

```
MUT-RED m1=red(exit=1 | RESULT: 0/8)·判据=J1 分隔符（节点级 R1，改回 `卡路里 · 报告` 徽章）  m2=red(exit=1 | ℹ fail 2 | AssertionError: BMI 报告 导航 href 与区块 id 必须一一对应（双向，不许有孤儿）)·判据=J8 锚点自洽  m3=red(exit=1 | ℹ fail 1 | AssertionError: BMI 报告 结论条应恰好一条（renderConclusionBar）)·判据=J9 三条恒出（结论条）  m4=red(exit=1 | ℹ fail 1 | AssertionError: BMI 报告 页面壳宽度没走 pageChromeCss(1120))·判据=J6 页宽
MUT-GREEN restored=4/4 m1=green(exit=0 | RESULT: 8/8)  m2=green(exit=0 | ℹ tests 7 ｜ pass 7 ｜ fail 0)  m3=green(exit=0 | ℹ tests 7 ｜ pass 7 ｜ fail 0)  m4=green(exit=0 | ℹ tests 7 ｜ pass 7 ｜ fail 0)
```

| 变异 | 改哪一处（锚点**唯一命中**，命中 0 或多次即抛错、不落盘） | 判据 | sha256_16（还原前后相等） |
|---|---|---|---|
| `m1` | `reportDoc.ts`：`badge: '报告',` → `badge: '卡路里 · 报告',`（＋重出 8 页产物再量） | J1 分隔符（节点级 R1） | `16b09458b9e6` |
| `m2` | `reportDocParts.ts`：`sectionsHtml` 的 `<section id="' + s.id + '">` → `id="x-' + s.id + '"` | J8 页内导航锚点自洽 | `6fd741e9ed7b` |
| `m3` | `reportDoc.ts`：`renderConclusionBar(conclusionOf(plate))` → `'<p>' + conclusionOf(plate) + '</p>'` | J9 三条恒出（结论条） | `16b09458b9e6` |
| `m4` | `reportDoc.ts`：`content: pageChromeCss(1120)` → `pageChromeCss(960)` | J6 页头三件与宽屏页宽 | `16b09458b9e6` |

**收尾一致性**：四次 `RESTORE 相等=true`；变异件**零残留**（`git status --porcelain` 只含本票 8 件）。

## 六 · 产物（生成物，不入仓）

本窗判据面 `.scratch/t519/样板/`（8 页，逐页 sha256 全长 + bytes）：

| 序 | 文件 | bytes | sha256 |
|---|---|---|---|
| 24 | `24-看BMI报告.html` | 96668 | `24e26ab3f0311b932ca3688c261ce350a647c60da70e8b31b7341dae1824f978` |
| 25 | `25-看TDEE报告.html` | 81474 | `7730786de8114db5738981a2b0c655c1965c0b24755d6db3c36b7690adc1a0f7` |
| 26 | `26-看BMR报告.html` | 91411 | `9f912d72360c92030527595fecb0ea45db93caa477563ba1d0bb0e531cd521c9` |
| 27 | `27-看蛋白质摄入报告.html` | 87955 | `ebf991f09cec5a715d8d4114941885e5a487d3f86538253bf9f697685e443070` |
| 28 | `28-看水分摄入报告.html` | 87973 | `79b2454e3fb56e1886f47b85d318d2ce3de8147843f104219772e06617e2dbf8` |
| 29 | `29-看综合评分.html` | 80106 | `1d53bd3a01d71b0e113ea74cbf77e91417280d1f265aa436a09c1299ee665239` |
| 30 | `30-看健康趋势.html` | 97589 | `4ca06da538cb1ce7f7c42b885d1969291a4ea75de2f54a216e28e3d38f5d986c` |
| 31 | `31-看健康报告(含对比).html` | 96740 | `f903677dbc4bffd85317a2d72065dc554b08d1f1283d93740fb9f654a6db7a39` |

小计 **bytes=719916 ／ 8 页**；登记件 `.scratch/t519/gen8.json`。

## 七 · GATE-RUN 声明（只领 exit=0）

```
GATE-RUN runId=3914167a-3cd7-48c9-8b29-ba3e34fddad3 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=94d7e396-1e39-418b-a0a6-78904d4c1035 cmd="node .scratch/t519/gen8.mjs"
GATE-RUN runId=9753f6ac-7420-4c18-af77-e6a7b1abdf11 cmd="node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t519/样板 --json .scratch/t519/W1-sep.json"
GATE-RUN runId=ef9d8e09-b1ce-4563-a34b-26d341cdc187 cmd="node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t519/样板 --widths 390,512,820,1440 --json .scratch/t519/W1-measure.json"
GATE-RUN runId=aec52991-26cf-4390-bf91-cc00bfb1fa46 cmd="node docs/skills/skill-calorie/t516-判据-版式.mjs --dir .scratch/t519/样板 --widths 390,512,820,1440 --json .scratch/t519/W1-layout.json"
GATE-RUN runId=f5a813d0-4a9c-41bd-a38c-e819427fe104 cmd="node --test packages/skill-calorie/test/analysis-report-384.test.mjs packages/skill-calorie/test/analysis-accept-386.test.mjs"
GATE-RUN runId=798dbe0a-3fab-46a7-a277-30310027a727 cmd="node --test packages/skill-calorie/test/analysis-shape-report-520.test.mjs"
GATE-RUN runId=c69246c5-cd51-4e66-8959-a4adaea480da cmd="node packages/skill-calorie/scripts/check-warning-line.mjs"
GATE-RUN runId=629ebe9e-ef1a-4f6f-a5d7-2238c4006613 cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"
GATE-RUN runId=3e01797e-71b7-4e94-8b89-839df5d48a90 cmd="node .scratch/t519/mutate.mjs m1"
GATE-RUN runId=af96b328-04e9-4801-90c5-5b66ace642c4 cmd="node .scratch/t519/mutate.mjs m2"
GATE-RUN runId=411df20e-e08c-4f30-964d-99fae3b0f93d cmd="node .scratch/t519/mutate.mjs m3"
GATE-RUN runId=7bd7b70b-4881-4b94-b197-599c1e7d4a9e cmd="node .scratch/t519/mutate.mjs m4"
```

**未声称的重复迭代轮次**（按裁定 N 的「类别 ＋ 条数 ＋ 日志指针」具名，不充作门禁证据）：

| 类别 | 条数 | runId | 红因 | 日志指针 |
|---|---|---|---|---|
| 开工基线（**作废读数**：`.scratch/t387/html` 当刻比 `.scratch/t518/产物` 旧） | 1 | `8d8a5881-1206-4b85-b457-f8188ec6cb44` | `RESULT: 2/31`（旧树基线，非验收面） | `.scratch/t519/sep-before.json` |
| 编译口径迭代（首次漏 import） | 1 | `17246b26-e460-4484-8192-03a7cdd74800` | `TS2304: Cannot find name 'fmtInt'` | `.scratch/t519/build-w1.log` |
| 重出器参数核对迭代（回归正则写错） | 1 | `508033c0-176f-493c-86c8-a099f2897aa8` | `PARAM-AUDIT MISMATCH=8`（脚本自身缺陷，非产物问题） | `.scratch/t519/gen8-w1.log` |
| 靶向两件的**判据先行**红（授权改写前，见 §2.2） | 1 | `02a6bac3-1f0e-4cf4-86e7-598b21093c9a` | `tests 13 ｜ pass 10 ｜ fail 3`（正是授权点名的 3 处） | `.scratch/t519/W1-tests-before.log` |
| 变异电池自身的锚点自检拒绝落盘 | 1 | `e4edcf6a-6216-4b77-bec9-52c45956b3ec` | `ABORT：锚点不唯一（from 命中 2 次）`（m4 的锚点撞上注释里的同一串；当场改成含上下文的唯一锚点后重跑） | 见 §五 表 m4 行 |

**杂务运行**（不产判据读数，按类别具名）：`git add`／`git commit`／`git status`／`git diff --cached --name-only`；导出件写入由 W4 窗统一落 `docs/skills/skill-calorie/t519-GATE-RUN.log`（**累积窗口，旧 runId 一条不丢**，照裁定 N）。口径引 **#518 裁定 I 原句**：「**授权（一次性、具名、不类推，仅本票本窗口）**：**不产出判据读数**的杂务运行（`git add`／`git commit`／`--sync`／导出脚本）**只需在证据里按类别具名列出（含 runId）**，不要求逐条 `GATE-RUN` 声明；**判据类**运行（编译／测试／探针／门禁）仍按 §2.4 逐条声明且**只认 exit=0**。证据件须引本句。」

## 八 · 未做项与遗留（逐条具名）

| # | 未做项 | 为什么 | 下一手缺什么 |
|---|---|---|---|
| 1 | **J4 字号阶梯／J5 断点档数** | 两项量的是公共层共享样式表整段（`<style>` 段全部 `font-size`／全部 `@media` 条件），落点 `packages/base-render/**`（本票只读） | 已由编排者具名改期 [#567](https://github.com/FeatherHunter/ilife/issues/567)（落点 `style.test.mjs` 增字号下限守卫）；本票只报读数与归属 |
| 2 | **表宽 680px 上限／`list-rows` 折行／`toc` 触摸区／字体阶梯与断点守卫** | 四条缺口全在公共层，已具名归 [#567](https://github.com/FeatherHunter/ilife/issues/567) | 公共层各一条规则；本票不代做 |
| 3 | **页级「无值位卡」缺口** | 公共层 `KpiCardInput.value` 必填非空，产不出「没有值位的卡」（#518 W6 同款结论） | 归 [#567](https://github.com/FeatherHunter/ilife/issues/567) |
| 4 | **`analysis-accept-386.test.mjs:329`／`:350` 的旧徽章串作废断言** | 不在编排者授权清单内 | 是否收严由编排者另裁（已在本件 §二 具名登记） |
| 5 | **五维视觉分** | 票面明令：8 页 × 1440／390 截图交**独立视觉席**打分，本席不自行打分 | 独立视觉席；报告落 `docs/skills/skill-calorie/t519-视觉审查.md` |
| 6 | **地图产物目录 `.scratch/t387/html/` 的 31 页重出** | 裁定 G：W4 收尾一次性重出（本窗只出 8 页到本票自己的样板目录） | W4 |
| 7 | **`reportPlate.ts` 未拆（406 LF 超线）** | 本窗对**该件零改动**（`energyOfWindow` 的真体重口径原样保留），拆件不属本票写集 | 收口票；台账挂号行不动 |
| 8 | **`analysis-accept-386.test.mjs:304` 的姊妹条已改、`:302` 的 `expectKind` 机制未动** | 授权范围只到 `:304` | ——（`MUT_TPL` 变异钩子仍可用：`expectKind` 换形态即红） |
