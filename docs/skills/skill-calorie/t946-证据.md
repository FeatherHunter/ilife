# #946 写前预览页（`calorie.view.plan-write-preview`）· 证据

Part of #944（报障单，本票覆盖故障 1／2／3／8）。票面＝`gh issue view 946`；派单＝编排者 2026-09-23 窗口。
本件只记**可复核的机器读数**与逐条对账；结论以当刻盘上内容为准。

## 一、判定

**已完成**（三条判据全绿 ＋ 两处源码级变异各自咬住对应判据）。故障 2 的 SKILL.md 那一格**不在本票**（已转 #948，见「未做项」）。

## 二、判据复跑摘要行（当刻交付态）

- 判据命令：`node --test packages/skill-calorie/test/946-写前预览页.test.mjs`
- 收尾窗口（整窗经 `node tooling/run-locked.mjs --ticket 946 -- node .scratch/t946/window-final.mjs` 持锁）：
  `RESULT: ticket=946 runId=730cce33-ece2-4742-87de-e6158f8283b1 waitedMs=0 exit=0`
- 判据件内部读数：`ℹ tests 6`／`ℹ pass 6`／`ℹ fail 0`。
- 本包另两条静态门（同一窗口内跑，均绿）：
  - 页面断言门 `check-page-assert.mjs` → `PASS: spawn 真交付出口并断成功的测试件都断到了信封`（exit 0）；
  - 测试面一条 dist 路径门 `check-one-path.mjs` → `PASS: 测试面每个符号只有一条 dist 路径…`（exit 0）。
- 告警线台账门当刻**红**，但**三条红条逐条都是别席的件**（在途改动导致「当场实测」列陈化），原文：

```
RESULT: 92/95
RED 台账陈化：src/workout/write.ts 台账=643 实况=653；修法：node packages/skill-calorie/scripts/check-warning-line.mjs --sync
RED 台账陈化：src/workout/planEditorRuntime.ts 台账=426 实况=455；修法：node packages/skill-calorie/scripts/check-warning-line.mjs --sync
RED 台账陈化：src/workout/planStore.ts 台账=398 实况=414；修法：node packages/skill-calorie/scripts/check-warning-line.mjs --sync
```

  本票改动件 LF 逐件见 §六，**都在 350 以内、无需挂号**。

### 提交

- `987be75553e1b063d47ba446d2a65e331f98cd96` —— `fix(946): 写前预览页版面回归（本页恢复单列）＋ 换成确认／修改两枚载荷`（7 files changed, 791 insertions(+), 40 deletions(-)）。
- 暂存区复核：`STAGED-ONLY-MINE=true n=7 extra=[]`（`git diff --cached --name-only` 只含本票声明的 7 件）；
  提交后 `git status --short -- <这 7 件>` 为空 ⇒ 本票声明路径下干净（含未跟踪件）。未 push（编排者统一收口）。

### 三条读数（逐条对应票面）

**① 几何（真起 headless Chrome ＋ CDP，`Emulation.setDeviceMetricsOverride` 逐档钉视口）**

```
T946-GEOMETRY @1440 innerWidth=1440 docWidth=1440 改前表卡=880 眉标=880 标题=880 副题=880（caption=改前（1 行））
T946-GEOMETRY @1280 innerWidth=1280 docWidth=1280 改前表卡=880 眉标=880 标题=880 副题=880（caption=改前（1 行））
T946-GEOMETRY @1024 innerWidth=1024 docWidth=1024 改前表卡=880 眉标=880 标题=880 副题=880（caption=改前（1 行））
T946-MUT-GEOMETRY @1440 改前表卡=1240 标题=880
```

- 三档「改前表卡」与页头三级（眉标／标题／副题）**都是 880px**（容差 2px；实测整数、`innerWidth == docWidth` ⇒ 竖直滚动条没吃宽度）。
- 最后一行是**判据不恒真**的对照：把那条单列规则从产物里删掉，表卡立刻回到改前的满铺宽 **1240**、页头仍 880 ⇒ 判据咬得住。

**② 载荷（真跑交付出口 `dist/cli/cmd_read.js`，读确认那颗按钮的 `data-t`）**

```
T946-PAYLOAD confirm="calorie-cmd-read calorie.workout.plan-update-day --params '{\"week\":1,\"dayOfWeek\":3,\"newLabel\":\"下肢＋核心\"}'"
T946-PAYLOAD modify="我现在需要把什么修改为什么，确定录入，可以进行修改了 calorie.workout.plan-update-day"
T946-IDS ilife-help-copy-prompt,ilife-help-copy-params,ilife-copy-data,ilife-copy-log
```

- 确认那枚＝**逐字**手写命令串（去空白后逐字相等 ＋ 连空白也逐字相同，`t366` 口径）：含命令名 `calorie.workout.plan-update-day` 与本次改后值 `下肢＋核心`，**不含** `____`；页面里也不再有 `新值:____` 那一段。
- 修改那枚＝报障人原话那句 ＋ 同一条命令名。
- 页上 `data-action-id` 四枚（改前是 `ilife-help-copy-prompt`／`ilife-copy-data`／`ilife-copy-log` 三枚）；页底不再有 prompt 预览块（`class="ilife-block-pre-block-code"` 命中 0 次）。

**③ 可执行（把该串原样执行）**

```
T946-EXEC exit=0 命令=calorie.workout.plan-update-day 库内 session_label=下肢＋核心 未动行=1
```

分词口径照 `test/t366-复制执行闭环.test.mjs`：首 token `calorie-cmd-read`、带 `--params` 段；exit 0；stdout 解析成 envelope
（`key=calorie.workout.plan-update-day`／`data.ok=true`）；库内 `workout_plans(week=1,day=3).session_label` **＝串里的值**，同一份库里另那一天的行一行未动。

## 三、变异自证（源码级两处，红／还原绿各一行机器读数）

跑法：`node tooling/run-locked.mjs --ticket 946 -- node .scratch/t946/mutate.mjs`
（电池整窗持锁，`try/finally` 逐文件按当刻原文还原并核 sha256）

```
M1-MUTATED file=packages/skill-calorie/src/workout/plan.ts tsc=0 test-exit=1 红用例=["#946 ② 载荷：确认那颗按钮的 data-t＝含命令名与改后值的命令串、不含 `____`","#946 ② 判据不恒真：把载荷换回入口唤醒词的 prompt_template（含 `新值:____`）必红","#946 ③ 可执行：把确认那串原样执行 → exit 0，库内值＝串里的值"] → RED-OK
M1-RESTORED file=packages/skill-calorie/src/workout/plan.ts sha=61fc68ef238d 逐字节一致=true
M1-GREEN tsc=0 test-exit=0 → GREEN-OK
M2-MUTATED file=packages/skill-calorie/src/render/workoutPlanCss.ts tsc=0 test-exit=1 红用例=["#946 ① 几何：真浏览器量「改前表卡」与「页头三级」，三档都 880px（容差 2px）","#946 ① 判据不恒真：把单列规则从产物里删掉，同一条几何判据必红（字串级变异）"] → RED-OK
M2-RESTORED file=packages/skill-calorie/src/render/workoutPlanCss.ts sha=ba3abf6e8845 逐字节一致=true
M2-GREEN tsc=0 test-exit=0 → GREEN-OK
MUT-BATTERY 通过=2/2 → ALL-OK
```

- **M1**（把确认载荷换回入口唤醒词的 `prompt_template`——`#944` 故障 3 的原形态）⇒ ② 载荷必红；
- **M2**（删掉本页那条同权单列规则）⇒ ① 几何必红；
- 两处的还原都是**逐字节**（sha 一致=true），还原后 `tsc` 与判据同时复绿。

## 四、门禁对账声明（协议 §2.4.2）

以下每次运行都在 `.scratch/locks/gate-runs.log` 有一对一条目（标识逐字抄自对应行）：

```
GATE-RUN runId=ac6579d2-0364-4839-8c1e-b0d90299b4a9 cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
GATE-RUN runId=07998a65-57a0-41dc-9072-b9a4f72d26cf cmd="node docs/agents/t540-指纹绑定.mjs --scope packages/skill-calorie --log .scratch/t946/t540-tsc.log"
GATE-RUN runId=07998a65-57a0-41dc-9072-b9a4f72d26cf cmd=node --test packages/skill-calorie/test/946-写前预览页.test.mjs
GATE-RUN runId=cd8be7ff-3bde-4fee-9684-c825ac6046a9 cmd=node .scratch/t946/mutate.mjs
GATE-RUN runId=730cce33-ece2-4742-87de-e6158f8283b1 cmd="node docs/agents/t540-指纹绑定.mjs --scope packages/skill-calorie --log .scratch/t946/t540-tsc.log"
GATE-RUN runId=730cce33-ece2-4742-87de-e6158f8283b1 cmd=node packages/skill-calorie/scripts/check-page-assert.mjs
GATE-RUN runId=730cce33-ece2-4742-87de-e6158f8283b1 cmd=node packages/skill-calorie/scripts/check-one-path.mjs
GATE-RUN runId=730cce33-ece2-4742-87de-e6158f8283b1 cmd=node --test packages/skill-calorie/test/946-写前预览页.test.mjs
GATE-RUN runId=aded04c7-03fe-4ecd-a065-eed222b5e1f6 cmd=node .scratch/t946/commit.mjs
```

- 落点窗口口径（协议 §2）：除第一次单条 `tsc` 外，上面每一步都在**同一个外层锁窗口**里由窗口脚本
  （`.scratch/t946/window.mjs`／`window-final.mjs`／`mutate.mjs`）直接调底层命令，**窗内不再抢锁**；
  窗口的运行标识由外层 `run-locked` 落痕，窗内各步与之共用同一标识（窗口自身的 `RUN` 行命令为
  `node .scratch/t946/window-final.mjs`，退出码 0）。
- 调试期的红（种子夹具缺器材、Chrome 起不来、别席坏 dist、判据件自身一个 `undefined.slice`）**不充门禁证据**，
  只在 §七「现场读数」里留档。

## 五、编译指纹绑定（协议 §2.6）

```
RESULT: src-n=338 dist-n=1550 src-sha=b4dfdf45c98f7b95 dist-sha=226cc200fe54fc7b 一致（dist 与 src 指纹一致）
FINGERPRINT: … src-sha-前=b4dfdf45c98f7b95 src-sha-后=b4dfdf45c98f7b95 dist-sha-前=226cc200fe54fc7b dist-sha-后=226cc200fe54fc7b tsc-exit=0
```

- 窗口内 `src` 无漂移（前＝后），`tsc -b packages/skill-calorie` exit=0 ⇒ §二的三条 CLI 读数绑定在
  `src-sha=b4dfdf45c98f7b95` 这棵树上。
- **本窗真的撞过一次混合态**：23:13 别席（#949）改 `src/workout/planEditorRuntime.ts` 那次 `tsc -b` exit=1
  （`gate-runs.log` ticket=948／runId=0a8948d2）却仍落了产物 ⇒ 当刻 `dist/workout/planEditorRuntime.js` 是语法坏件，
  我的判据件 import 包门即崩（`✖ …test.mjs (89ms)`／`SyntaxError: Missing initializer in const declaration`）。
  那是**别的票的在途件**，本票一行未碰；等它复绿后重跑即得上面的指纹与读数（坏态那次记在 §七）。

## 六、改动件清单（逐件＋当刻 LF）

| 件 | 动作 | LF | 一句话 |
|---|---|---|---|
| `packages/skill-calorie/src/workout/planPreviewPayloads.ts` | 新建 | 68 | op→确认命令的字面量表九条 ＋ 两个薄函数（确认指令／修改指令） |
| `packages/skill-calorie/src/render/workoutPlanDocs.ts` | 改 | 318 | 写前预览页出两枚载荷（`payloadBar`）；`dualCopy` 不再前置 prompt 段；「方式」行改「复制确认指令后执行写命令」 |
| `packages/skill-calorie/src/render/workoutPlanCss.ts` | 改 | 249 | 加 `planPreviewCss()`：页面级段 ＋ **同权单列规则**（本页正文子件收回 880 那一列） |
| `packages/skill-calorie/src/workout/plan.ts` | 改 | 64 | 本页改经 `buildPlanProcessDoc`，按本次 op 与参数算出两枚载荷 |
| `packages/skill-calorie/src/workout/precheckPrompt.ts` | 改 | 23 | 删掉 `previewPrompt()` 与 op→唤醒词那张表（`#944` 故障 3 的出处）；只留构建向导那条词 |
| `packages/skill-calorie/test/946-写前预览页.test.mjs` | 新建 | 454 | 票面验收用例（几何／载荷／可执行 ＋ 两条判据不恒真对照） |

- `packages/base-render/**`、`planCopyBlock.ts`、`scene-05-workout.ts`、`routes.ts`、`write.ts`、`planStore.ts`、
  `commands.ts`、`SKILL.md` 与一切生成物**一行未碰**（`git status` 里那几个 M 是别席的在途件，不进本票提交）。
- 未跑 `pnpm gen`（本票不动对外面）。

## 七、现场读数（另存备查，不进判据）

- **几何改前现场**：删掉单列规则即复现票面故障 1 的读数——`@1440 改前表卡=1240`（页头 880）⇒ 左右各越 180px。
- **Chrome 起不来的真因**（本机实测，值得复用）：`configTestBase()` 把 `USERPROFILE`／`HOME` 指到临时家目录，
  Chrome 继承它起 DevTools 时卡死 —— `.scratch/t946/chrome-probe2.mjs`：`ok=false ms=40156`，Chrome 自己 stderr
  `update_service_dialer_win.cc:75 Failed to open named pipe server process … 拒绝访问。 (0x5)`；
  把家目录还给真实那份、只对 Chrome 摘掉 `NODE_OPTIONS`（技能侧子进程照旧隔离）后
  `.scratch/t946/chrome-probe3.mjs`：`ok=true ms=517`。修法已落进判据件（`CHROME_ENV`，含注释）。
- **种子夹具**：`calorie.workout.plan-set` 对「深蹲」要器材「杠铃」（`planStore.ts` 的 `EQUIPMENT_KEYWORDS`），
  种子计划一开始只给「瑜伽垫」⇒ exit 2（`ERR 2: 计划校验未通过：缺少器材 杠铃`）；补齐器材后种子绿。
- **判据件自身一个 bug**：CDP 的 `evaluate()` 里断言消息参数恒求值，`JSON.stringify(undefined).slice` 抛
  `TypeError`（把真失败盖成了假失败）；已改成先自防 `undefined`。
- 调试红逐条（**不进门禁**）：`runId=3b288285`（种子缺器材）／`450fc3b9`（Chrome 起不来）／
  `c2be510c`（别席坏 dist）／`023304ab`（`undefined.slice`）／`699df386`（台账门因别席陈化而红）。

## 八、锁留痕导出（协议 §2.3.4；本票声明过的运行条目）

```
START ticket=946 runId=ac6579d2-0364-4839-8c1e-b0d90299b4a9 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie" waitedMs=60006 pid=32708 at=2026-09-23T15:06:28.643Z
RUN ticket=946 runId=ac6579d2-0364-4839-8c1e-b0d90299b4a9 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie" waitedMs=60006 exit=0 pid=32708 at=2026-09-23T15:06:30.178Z
START ticket=946 runId=07998a65-57a0-41dc-9072-b9a4f72d26cf cmd="node .scratch/t946/window.mjs" waitedMs=0 pid=4864 at=2026-09-23T15:15:35.678Z
RUN ticket=946 runId=07998a65-57a0-41dc-9072-b9a4f72d26cf cmd="node .scratch/t946/window.mjs" waitedMs=0 exit=0 pid=4864 at=2026-09-23T15:15:40.288Z
START ticket=946 runId=cd8be7ff-3bde-4fee-9684-c825ac6046a9 cmd="node .scratch/t946/mutate.mjs" waitedMs=0 pid=55804 at=2026-09-23T15:16:01.754Z
RUN ticket=946 runId=cd8be7ff-3bde-4fee-9684-c825ac6046a9 cmd="node .scratch/t946/mutate.mjs" waitedMs=0 exit=0 pid=55804 at=2026-09-23T15:16:22.247Z
START ticket=946 runId=730cce33-ece2-4742-87de-e6158f8283b1 cmd="node .scratch/t946/window-final.mjs" waitedMs=0 pid=13224 at=2026-09-23T15:17:18.171Z
RUN ticket=946 runId=730cce33-ece2-4742-87de-e6158f8283b1 cmd="node .scratch/t946/window-final.mjs" waitedMs=0 exit=0 pid=13224 at=2026-09-23T15:17:23.408Z
START ticket=946 runId=aded04c7-03fe-4ecd-a065-eed222b5e1f6 cmd="node .scratch/t946/commit.mjs" waitedMs=0 pid=47100 at=2026-09-23T15:18:40.785Z
RUN ticket=946 runId=aded04c7-03fe-4ecd-a065-eed222b5e1f6 cmd="node .scratch/t946/commit.mjs" waitedMs=0 exit=0 pid=47100 at=2026-09-23T15:18:41.067Z
```

- 等待量可复核：唯一一次等待＝第一条（`waitedMs=60006`，等 #943 的活锁释放，日志里同段有 `WAIT-OWNER-ALIVE pid=… ticket=943 …`）。
- **同名票号的干扰**：`.scratch/locks/gate-runs.log` 里另有几条 `ticket=946` 的条目，命令是
  `tsc -b packages/skill-chef`／`node .scratch\t920\mut-946.mjs`／`packages/skill-chef/test/…`——那是**另一个上下文**的
  #946（只碰 `packages/skill-chef/src/config.ts`），与本票无关；本节的导出只抄本票的命令。
- 本票的调试红条目（§七列的四条 `exit=1`）也在同一份日志里，按 §2.4.3 不充门禁证据。

## 九、必报五步（`docs/agents/structure.md`）

- 第一步（影响清单）／第二步（结构设计）：票面「交付物路径」即已报的这两步，逐行落地见 §六，**偏差为零**：
  改四件（`render/workoutPlanCss.ts`／`render/workoutPlanDocs.ts`／`workout/plan.ts`／`workout/precheckPrompt.ts`）、
  新建一件（`workout/planPreviewPayloads.ts`，导出 3 个名字：一张九条的字面量表 ＋ 两个薄函数）、
  新建判据件一件（`test/946-写前预览页.test.mjs`）。共用件 `shared/planCopyBlock.ts` 未动（两枚按钮骑本页已有的按钮行）。
- 第三步（写代码）：跨能力引用走对方的公开件（`shared/writeParts.ts` 的 `commandLine` 是命令原文的唯一定义地，
  本票只给命令名与参数）；同一条命令名只写一处（新件的字面量表）。
- 第四步（超线报警）：本票改动件 LF 见 §六，**都在 350 以内**，未触发报警。
- 第五步（交付对账）：见本步上一行与 §六 —— 票面每一行都有着落；`SKILL.md` 那一行**已由编排者转 #948**（见 §十），
  这是唯一一处与票面派单的偏差，来源是编排者本窗的转票通知，不是漏做。

## 十、未做项与下一手

- **SKILL.md 那一格（故障 2 的契约那几句）不在本票**：写前预览页出页后「用户回一句确认＝落该 op 的授权」已由编排者
  转给 #948 在本窗代落；本票只交页面载荷与版面两半。（本窗按编排者口径**一步都没写** `SKILL.md`，也没带进提交范围。）
- **告警线台账**：`check-warning-line.mjs` 当刻红的逐条原文见 §二，三件都是别席在途件；本票的件无一条红。
  按编排者口径由其在最后一个窗口落定后统一跑一次 `node packages/skill-calorie/scripts/check-warning-line.mjs --sync` 并单独提交。
- 票面「遗留出口」两条（速查表／HELP 的「预检页」列指向九条确认命令；同族四个调用点仍传 `pageChromeCss(960)`）
  **按票面归另票**，本票不做。
- 下一手：本票提交后由编排者收口（不要 push）；若要复核，判据命令就是票面那一条，变异电池脚本住
  `.scratch/t946/mutate.mjs`（草稿目录，不进交付）。
