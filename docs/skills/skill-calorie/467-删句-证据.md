# #467 切片·目标页删句（用户逐字点名）证据

> 体例照 `t467-重做-证据.md`。本切片只删用户点名的句子，不动图表／达标表／复制区本身，不碰 `today.ts`（在途）、别家目录、公共层。

## 一、开工状态

- `git status --porcelain -- packages/skill-calorie/src/home/goalProgressDocs.ts` 开工时**无输出＝干净未动**（全树另有别席在途脏件，本席未碰）。
- `tooling/run-locked.mjs` 存在，编译／测试／提交／变异重建一律经 `node tooling/run-locked.mjs --ticket 467 -- <命令>`，无裸跑。
- 长输出只看尾 5 行，全文以 `ticket=467` 落 `.scratch/locks/gate-runs.log` 对账（§五）。

## 二、结构纪律必报五步（`docs/agents/structure.md`）

- **第一步·影响清单**（单能力 `home`，一行一能力）：
  - `packages/skill-calorie/src/home/goalProgressDocs.ts` —— 删用户点名 6 处句子＋常量＋注释同步（`home` 能力）。
  - `packages/skill-calorie/test/t467-goal-progress.test.mjs` —— 只改与删除矛盾的断言（测试件，不在结构管辖）。
  - `docs/skills/skill-calorie/467-删句-证据.md` —— 新证据件（文档，不管辖）。
  - 禁区未碰：`src/home/today.ts`、别家目录、公共层、图表／达标表／复制区本身。
- **第二步·结构设计**：无新增目录／文件；公开接口不变（`buildGoalProgressDoc`＋`GoalProgressDocInput`，仍各一句）；无共用件变化。
- **第三步·写代码**：跨能力引用未增；常量只删不增（`NOT_COUNTED_NOTE` 删，`SOURCE_LOGGED` 留供复制日志）；目录内互用不对外。
- **第四步·超线报警**：已超线，需要根据规则进行重构。`goalProgressDocs.ts` LF 452→**446**仍超 350。超因沿台账：本窗四卡＋结论条／走势图＋目标线量程判定／每日达标表／三项目标块／复制区同件。本次先不拆：拆分不在 #467 写集，拆法照台账（量程判定＋走势图＋图下读法另立一件，四卡／结论／达标表／复制区留本件，出口薄转出），待收口票认领。测试件 321 行不在告警扫描面。
- **第五步·交付对账**：见 §八，事前清单与事后对账偏差为零（`today.ts` 一行未碰）。

## 三、源码删除清单（`goalProgressDocs.ts`，LF 452→446）

用户裁决＝本切片派单原文逐字（目标页删句，用户逐字点名；声明写集逐行点名行号与句子）。改动注释逐处引用该裁决。

| # | 派单点名 | 改法 | 用户裁决引用（改动注释原文） |
|---|---|---|---|
| 1 | line360 两句日均摊平／缺口句 | 删两行 `renderCaliberLine` | `#467 切片·用户逐字点名删两句口径：原「日均按窗口天数摊平，没记录的日子也算一天。」＋「缺口是消耗减摄入的差，正数表示摄入比消耗少。」两行已删。` |
| 2 | line277-280 `NOT_COUNTED_NOTE` 用法 | 行列表保留，删总说 `renderCaliberLine` 整段；`emptyText` 不补 | `#467 切片·用户逐字点名删总说 caliber 句`；`emptyText` 签名核查：`base-render/src/blocks.ts:727` 为可选（`emptyText?: string`，缺省 `无数据`），且本块恒 3 行（`notCountedRows` 蛋白／饮水／运动各一）永不走空态分支，故签名不强制、不补中性短句——若后续签名变必填再补并报用户复核。 |
| 3 | line391-394 转场整句 | 删整行 `renderCaliberLine`（两态三句话整句） | `#467 切片·用户逐字点名删转场整句（原「上面那张折线图是窗口…／这个窗口里只有一天…＋下面这张表看的是近…」整行已删）` |
| 4 | line396 达标口径两句 | 删两行 `renderCaliberLine` | `删达标口径两句（原「达标口径：当日摄入占目标的 80% 到 120% 之间算达标。」＋「没记录的日子不列表，既不算达标也不算落空。」已删）。` |
| 5 | line437 来源行 | 删 `renderCaliberLine('数据来源：' + SOURCE_LOGGED + '。')` | `#467 切片·用户逐字点名删来源行（原句已删；来源名仍在复制日志第 4 段 SOURCE_LOGGED 里可重跑，页上不再另起一行）。` |
| 6 | `NOT_COUNTED_NOTE` 常量 line63 | 无引用则删 → 已删（`grep NOT_COUNTED_NOTE` 零命中） | 原总说句逐字留注释备查（不留 `见 NOT_COUNTED_NOTE` 死引用；现 grep 零命中）。 |
| 7 | 相关注释同步 | 件头版式来源／空态两态／`notCountedRows`／函数块序与三恒出注释同步改 | `来源脚注已按 #467 切片删除`、`唤醒词句已按 #467 切片删除`、`三条恒出本页已按 #467 切片改：页内导航恒出，口径只剩图下读法与空态句，来源脚注不再出`。 |

禁区核对：图表函数（`intakeChart`／`chartCaliber`）、达标表（`historyTable`）、复制区（`copyArea`＋`copyLog`，`SOURCE_LOGGED + '（只读）'`保留）、`today.ts` 一行未动。

## 四、测试改动（只改与删除矛盾的断言，旧用例其余不动）

| 断言 | 改法 | 用户裁决引用 |
|---|---|---|
| `assertThreeAlways` 来源／口径下限 | 删 `lines.length >= 1` 下限与 `数据来源：` 正向锁，改来源反向锁 `!includes('数据来源：')`；导航＋锚点＋toast 不动 | `用户裁决（#467 切片）：删 line437 来源行`；`删 line360／391-394／396 → 口径行不设下限（今日窗零口径行是预期）` |
| 30d 用例达标口径句 | `includes('达标口径：')` 正向改反向 `!includes`，加 `!visibleText.includes('不列表')` | `用户裁决（#467 切片）：删 line396 达标口径两句 → 反向锁，旧正向断言已删` |
| 复核用例 ③ 转场 | 两态正向改全窗反向（`上面那张折线图是窗口 30 天…`／`两处不是同一个窗口`／`只有一天有记录…`） | `用户裁决（#467 切片）删 line391-394 转场整句 → 反向锁，旧正向断言已删` |
| 复核用例 ④ 总说＋唤醒词 | `includes('本页的完成度与缺口只算热量')` 改反向；三唤醒词循环改反向；三行值（150 克／2000 毫升／300 卡）＋`还没设`不动 | `用户裁决（#467 切片）删总说句 → 反向锁`；`删唤醒词段 → 反向锁（行列表本身不带唤醒词）` |
| 缺陷⑤用例四行 | 四正向 `lines.includes` 改四反向 `!text.includes`（日均／缺口／达标带／不列表）；合串消失两条保留 | `用户裁决删 line360 两句＋line396 两句 → 反向锁` |
| 复核 ①②⑤ | 一字不动（量程两相／卡四带窗口／标题不带 `·`，与删除无关） | — |

## 五、持锁门禁（`ticket=467`，全文见 `.scratch/locks/gate-runs.log`；长输出此处只贴尾 5 行）

| runId | 命令 | exit | 读数尾 5 行 |
|---|---|---|---|
| `cedd7a96-6b77-41ec-8260-b2d2851ca650` | `pnpm build` | 0 | `LOCK-RELEASED … exit=0`／`RESULT: ticket=467 … exit=0`（`tsc -b` 零报错，`GEN-STAMP ok`） |
| `c5a0bb41-15e8-4961-aa82-8e97c0e4a11c` | `node --test …/t467-goal-progress.test.mjs` | 0 | `tests 6 / pass 6 / fail 0` |
| `b080270e-6414-41cb-97c5-c2e2506702f7` | `node --test …/home-lock-374 …/render-t8 …/render-t9` | 1 | `tests 25 / pass 24 / fail 1`（仅体重概览空库一条，见 §七；目标进度那条 ✔） |
| `b1d3eea9-…` | `pnpm build`（变异前重建） | 0 | `RESULT … exit=0` |
| `6fcd8816-…` | 变异红跑（§六） | 1 | `tests 6 / pass 4 / fail 2` |
| `7acf17e2-…` | `pnpm build`（还原重建，含一次等活锁 150s） | 0 | `RESULT … exit=0` |
| `388aefba-…` | 还原绿跑（§六，含一次等活锁 10s） | 0 | `tests 6 / pass 6 / fail 0` |

## 六、变异自证两行读数（改坏一行必红、还原必绿）

- **变异**：`goalProgressDocs.ts` 历史区临时加回一行 `+ renderCaliberLine('达标口径：当日摄入占目标的 80% 到 120% 之间算达标。')`，持锁重建后跑判据。
- **红读数**（runId `6fcd8816-98fb-470d-a10f-2e93ff697bd1`，exit 1）：`tests 6 / pass 4 / fail 2`，两条红同指加回的那句（30d 用例 `达标口径句已按用户点名删除`＋缺陷⑤用例 `达标带句已按用户点名删除`）。
- **绿读数**（还原后 runId `388aefba-62c8-4b3f-89d1-6a27f0265308`，exit 0）：`tests 6 / pass 6 / fail 0`。
- 还原校验：`goalProgressDocs.ts` SHA256 还原前后同为 `2B00920B686BDB3D221D72CBF89B449C1D53652B4E9EB371249999C1B8C317C3`。

## 七、邻接回归说明

- `home-lock-374` 中与本页相关的 `✔ #374 看今日目标进度` 通过；`render-t8／t9`（旧片段判据，`html.ts` 未动）全绿。
- 唯一红 `✖ #374 看今日体重概览（空库 exit 4，实得 0）` 在**体重能力**（`src/weight`），本切片写集无此目录；当刻工作树另有别席在途脏件（含 `sportPortDocs.ts` 等），判为别家在途影响，非本切片回归。证据：本切片 `git diff --name-only` 仅 §八三件。

## 八、交付对账与提交

- 事前清单（三件）与事后 `git diff --name-only` 逐行对得上，偏差为零；`today.ts` 未动。
- 台账：`packages/skill-calorie/AGENTS.md` 不在本切片声明写集内未同步（`goalProgressDocs.ts` 当场实测 452→446，台账仍 452），需收口票 `--sync` 回绿，特此记账而非隐瞒。
- 提交（持锁，仅声明路径，不推送，由总工程师统一收口）：
  - `packages/skill-calorie/src/home/goalProgressDocs.ts`
  - `packages/skill-calorie/test/t467-goal-progress.test.mjs`
  - `docs/skills/skill-calorie/467-删句-证据.md`（本件）
- 提交信息：`修复(467): 目标页删用户点名口径句 + goalProgressDocs.ts/判据全绿`

## 九、未完事项

- 告警线台账待收口票同步（§八）；`#374 体重概览空库` 红待归属席认领；本票整页重做视觉终审仍开（关票待整页终审）。
