# t165 公共层盘点（场景 03 体重：先复用现成的，再补自己缺的）

> 票 `#165`（卡路里场景 03 体重：公共层提取与边界）的取证件。
> **本文件由编排者直接取证**：原派单的取证席位两次超出上下文、未成文；本文每条都给了 `file:line`，任何人可当场复核。
> 复核脚本：`.scratch/t165/orch-callsites.mjs`（引用计数）、`.scratch/t165/orch-old-templates.mjs`（老模板结构）。
> 范围：只回答「今天有什么、吃什么数据、有哪些位置可以填、谁在用」。长相与设计不在本文。

## 一、base-paint（`packages/base-render`）的导出面

| 出口 | 内容 | 证据 |
|---|---|---|
| `.`（主入口） | 通用件总出口，**受冻结锁** | `packages/base-render/package.json` 的 `exports`；`packages/base-render/test/contract-signatures.test.mjs` |
| `./blocks` | 12 个区块（B-01 壳／B-02 KPI／B-03 表／B-04 图／B-05 列表／B-08 折叠／B-09 参数表单／B-11 复制区…） | `packages/base-render/src/blocks.ts`（50 KB） |
| `./help-shell` | HELP 模板出口 | `packages/base-render/src/helpShell.ts`（117 KB）；`src/helpFile.ts`×1 与 `packages/skill-bill/src/render/helpFile.ts`×1 在用 |
| `./save-html` | 落盘件 | `packages/base-render/src/output/saveHtml.ts`；`packages/skill-bill/src/output.ts` 等在用 |
| 图表 helpers | 折线／柱等 | `packages/base-render/src/charts.ts`（90 KB） |

**今天谁在用**：卡路里包引用 `base-paint/blocks` **13 个文件**、主入口 **14 个文件**；**别的技能只用了 HELP 与落盘两件**（`skill-bill` 的 `/help-shell`×1、`/save-html`×2）。→ 页面级件（曲线／对比／波动）**跨技能复用目前仍是潜在面**，照 #163 的裁定留卡路里包内，不上移 `packages/base-render/src/`。

## 二、卡路里公共层里与场景 03 直接相关的件

| 件 | 对外函数 | 吃什么数据 | 有哪些位置可以填 | 今天谁在用 |
|---|---|---|---|---|
| `src/shared/docPage.ts`（#179） | `assembleDocPage(input)`、`metricsOf(obj)` | 区块 HTML 串 ＋ 标题三件套（`docTitle／title／eyebrow／subtitle／charts`） | 整页骨架（doctype／charset／样式／`CONTENT` 槽）；`metricsOf` 只收确定数字，null／undefined 不进投影 | **10 个文件**（`render/dietDocs.ts`／`nutritionPortDocs.ts`／`sportDocs.ts`／`trendDocs.ts`／`profile/*` …） |
| `src/shared/copyArea.ts`（#239／#247／#248） | `promptCopyArea`／`copyArea`／`dataCopyArea`／`copyLog`／`notice` | prompt 文本、数据（三格式自动派生）、日志六段、提示一句 | `copyArea` 的 6 个可填位（给什么出什么，0–3 颗按钮）；**数据位给了 `data` 就自动出三格式菜单**；指令位（prompt／日志）不给菜单 | **11 个文件**（同上四域 ＋ `profile/*`） |
| `src/render/receipt.ts` | `buildCrudReceipt`／`buildM5`／`withM5`／`CrudReceipt`／`ReceiptItem`／`buildErrorReceipt` | 写入结果：`op／summary／recordId／items[]／tagDiff／distance／noChange／action`＋M5 五要素 | 今天只出**片段**（`src/cli/write.ts:202 receiptHtml` → `<section class="ilife-page" …>`） | **5 个文件**（`cli/write.ts`／`cli/cmd_read.ts`／`profile/setup.ts`／`update.ts`／`view.ts`） |
| `src/render/weightPlate.ts`（#41） | `buildWeightDashboard`／`buildWeightHistoryView`／`buildWeightCompareView`／`buildWeightReviewView`／`buildVolatilityView` | SQLite 连接 ＋ 窗口（`start／end／today／date`） | 5 个视图数据对象（KPI 数值、逐条记录、两段统计、里程碑、波动基线/σ/异常点） | 唯一引用者 `src/cli/cmd_read.ts` |
| `src/render/sportDocs.ts`（#109） | `buildWeightDoc:189`／`buildWeightHistoryDoc:248`／`buildWeightCompareDoc:314`／`buildWeightReviewDoc:361`／`buildVolatilityDoc:410`＋运动/体成分/围度同域页 | `weightPlate.ts` 的视图对象 | 每页 4 块：KPI 网格／图／表／复制区；`assembleDocPage` 出**完整文档** | 唯一引用者 `src/cli/cmd_read.ts` |
| `src/render/html.ts` | `renderGoalWeightHtml:296` → `pageShell:38` | `GoalWeight` | 今天的目标体重页是**片段**（`<section>`，无 doctype） | `src/cli/cmd_read.ts` |
| `src/output.ts` | 单一交付出口（落盘／命名／回执绝对路径） | 文档串 ＋ 命名参数 | 落盘与回执语义（`wx` 独占＋`EEXIST` 递补） | 4 处（含 `test/output-naming-119/87.test.mjs`） |

**本图 18 条缺命令要用的计算零件（都已存在，个别零调用）**：
`src/fetch/weight.ts:12 noteTag`／`:23`（行内含 note）｜`src/analysis/weightCompare2.ts:42 findPlateau／:100 e1／:114 e2／:132 e3／:167 seasonMin／:181 e5／:196 e6`｜`src/analysis/weightCompare3.ts:66 c5／:171 runScenario`（**全仓零调用**）｜`src/analysis/volatility.ts`（rolling／goal 双基线）｜`src/render/insightPlate.ts:86 buildAnomalyView`｜`src/render/trendMiscPort.ts:419 buildReviewTemplateView`（窗内体重变化＋要点，已存在）。

## 三、上一集（场景 07 基础信息）往公共层放了什么

| 件 | 由来 | 今天的状态 |
|---|---|---|
| `src/shared/docPage.ts` | #179 | 已被饮食／营养／运动／趋势四域复用（引用 10 处）——**本图所有读类页面都走它** |
| `src/shared/copyArea.ts` | #239（＋#247／#248 铺开） | 11 个文件在用；数据位自动出三格式菜单 |
| 写前页／结果页落成能力目录件 | #175／#176／#177 | `src/profile/setup.ts`（设置档案／设活动量＋两张写后回执页）／`update.ts`（改档案，改前→改后）／`view.ts`（查档案）。#163 原计划的 `render/profilePort.ts`＋`profilePortDocs.ts` 实施时改成了按能力目录的 `profile/` |
| **「把片段造成完整文档」的先例** | #179 | `profile/setup.ts` 的两张回执页与 `update.ts` 的对照页——**本图写后回执票照此办** |
| 计数同步清单 | #163 | 新增命令要同步改的断言见 #163 收口评论（99→100 等）。**本图 18 条是加参数、不新增命令**，故不触发那批计数断言 |

## 四、缺口候选（场景 03 的 6 类页面：今天已有／今天没有）

| 类 | 今天**已有**可复用 | 今天**没有**、要补 | 依据 |
|---|---|---|---|
| ① 体重曲线／明细页（18 词） | 整页装配（`sportDocs.ts:248`）、曲线图块、5 列明细表、复制区 | 4 个 KPI（均值／极值／区间条数）、明细与曲线**双段随窗口切换**、三种标注层（目标线／里程碑点／异常点）、备注筛选变体（备注列＋备注标签分布） | 老实物 `weight_history.html`：`h2 体重曲线`＋`h2 明细`＋k1–k4＋`modeBadge`；`render_weight_history.py` 的 `--mode／--show-*／--note-only` |
| ② 两段对比页（18 词） | 整页装配（`:314`）、两期对比表、4 个 KPI、复制区 | 两段各自的分段卡（段标签／区间／均值／条数）、差值箭头与速度判语、补充对照行、情景徽章、样本不足警告、结论句 | 老实物 `weight_compare.html`：`segA/segB/segALabel/segARange/segAAvg/segARows/vsArrow/vsDkg/vsSpeed/extraCard/extraRows/summaryCard`＋载荷字段 `seg_a／seg_b／compare／extra_rows／scenario_label／sample_warning` |
| ③ 体重复盘页（7 词） | 整页装配（`:361`）＋「目标复核」形态（当前／目标／差距／预计达成） | **期间复盘**形态（趋势图＋期间 KPI＋结论）与**里程碑回溯**列表 | 老实物 `weight_review.html`：`h2 体重趋势`＋`h2 里程碑`＋载荷 `points／milestones／table_title／summary`；`render_weight_review.py --type week/month/90d/year/range/milestones` |
| ④ 体重波动页（5 词） | 整页装配（`:410`）、基线／σ／阈值／预警／异常点 | **只看异常点**的视图（老脚本 `--view anomalies-only`） | `render_weight_volatility_v2.py` 的 `--view [full\|anomalies-only]` |
| ⑤ 今日体重盘（1 词） | 整页装配（`:189`）、盘面 KPI＋曲线＋记录表 | 今日卡（今日值＋日期＋较上次差值）与近 7 天趋势小图 | 老实物 `weight_dashboard.html`：`todayCard/todayKg/todayDate/todayDelta／trend_points／view=today` |
| ⑥ 写后回执页（9 词，3 变体） | 通用回执数据面（`receipt.ts buildCrudReceipt`）＋M5 五要素 | **整页文档化**（今天只出片段，`write.ts:202`；`test/profile-doc-179.test.mjs:216-231` 把「其余 32 条保持片段」钉住了，改口径要同步改这条断言）＋记体重变体（体重大字／近 30 天趋势／差值／目标差／均值）＋批量变体（写入/跳过/失败三个 KPI＋明细表） | 老实物 `weight_log_receipt.html`（`weightBig／statDeltaLast／statDiff／statAvg／statTrend／trendSvg`）与 `weight_batch_receipt.html`（`kpiWrote／kpiSkipped／kpiFailed／明细表`） |

## 五、方法（可复核）

```pwsh
# 引用计数（本文表格「今天谁在用」一列）
node D:\ilife\.scratch\t165\orch-callsites.mjs

# 老模板结构（区块／id／载荷字段）
node D:\ilife\.scratch\t165\orch-old-templates.mjs

# base-paint 导出面
Get-Content D:\ilife\packages\base-render\package.json -Raw | ConvertFrom-Json | Select-Object -ExpandProperty exports
```

**未做**：未跑 build／test，未实跑任何命令，未动数据库，未改任何源码。今天的 `exit 0` 逐条实跑由后续「锁：真出口用例」票负责。
