# t369 · 验收目录（卡路里场景 08「身体细节」13 条逐条真跑）

票：[#369](https://github.com/FeatherHunter/ilife/issues/369)「卡路里场景 08 身体细节 · 装机 ＋ 13 条逐条真跑」。

## 先开哪一页

| 想干什么 | 打开 |
|---|---|
| **逐条细看**（每份一张卡：唤醒词／命令／文件／状态） | `t369-总索引.html` |
| **一屏扫完双端**（18 件并排，可交互） | `t369-桌面墙.html`（每格 1100×900）／ `t369-手机墙.html`（每格 390×820） |

三页与产物**同目录**、`href`／`src` 全是**相对路径**——整个目录搬到哪儿都能直接打开（双击即可）。

## 目录里有什么

| 文件 | 是什么 |
|---|---|
| `01`–`13-*.html` | **13 件结果型产物**：票面点名的 13 条唤醒词逐条真跑的落盘页（`order` 237–249） |
| `0X-过程-*.html` | **5 件过程型页**：5 条写词（记体脂类／记围度类）的**写前预检确认页**（命令＝`calorie.view.composition-wizard`／`calorie.view.measure-wizard`） |
| `manifest.json` | 机器可读的清单：每件的键／命令／sha256／字节／整页四判据／关键字段命中 ＋ 只读查库回读读数 |
| `t369-墙.mjs` | 生成上面三页的生成器（无参跑：`node t369-墙.mjs`，读同目录 `manifest.json`） |
| `t369-链接自检.mjs` | 链接自检（本目录每个页面的 `href`／`src` 都要落得到真实文件）：`node t369-链接自检.mjs` → 末行 `RESULT: n/n` |

## 口径（读这些页之前先知道的三件事）

1. **跑的是临时库**：`.scratch/t369/tmpdb`（`SKILLS_DB_PATH` 钉死）。真库 `D:\2Study\StudyNotes\.db\calorie_data.db` **零写入**，读数（sha256／字节／改动时间／旁文件）跑前跑后一致——见 `docs/skills/skill-calorie/t369-证据.md` §真库零写入。
2. **这些页是肉眼验收用的展示**，不进机器的判据；机器的判据在 `docs/skills/skill-calorie/t369-逐条清单.md`（13 行六项读数 ＋ 总门）里。
3. **墙只给人看**：别拿它当自动化扫描的输入（整页截图只栅格化靠前的行、靠后的格子空白，那是截图的事；真浏览器里滚动就会画出来）。三页都**没有**用 `loading="lazy"`。

夹具（跑之前的暖库种子）与逐条运行脚本住在草稿区 `.scratch/t369/`（gitignored，不入仓）；本目录是**入仓**的那一份。
