# #293 收官批量 · 收尾席证据（当刻 HEAD `47e4bab9`）

**席位**：#293 收尾席（收官批量一次做完 ＋ A-5 全绿后关地图 `#293`）。
**起点自复算（开工第一件事）**：`git rev-parse HEAD` = `47e4bab9230834cb2fc56ff1949b17dde8f7c674`；`git rev-parse origin/master` = **同值**；分支 `master`；`git status --porcelain -uall` **输出为空**（无别人在飞的件）。
**判官（验收探针）sha256**：`78AE857426A44D66E81A2489E51D0D2790BD16A0812DB78818B1D2E918BEBE39`（`certutil -hashfile docs/skills/skill-calorie/t293-验收-命令自治.mjs SHA256`，与派单给定值逐字相同；⛔ 本席未改判官）。
**运行环境**：node v24.19.0；pwsh 7.6.6；所有门禁命令经 `node tooling/run-locked.mjs --ticket 293close --lock-dir .scratch/locks-land --stale-minutes 30 -- <命令>` 持锁运行（窗口内一律 direct node，**不嵌套**），票号 `293close`。
**时间**：本机 2026-09-14 07:5x–08:1x（+08:00）；日志 `at=` 为 UTC（`2026-09-14T00:0xZ`）。

---

## 一、A 段逐件

### A-1 协议增补（规则 38–41）—— 完成

`docs/subagent-concurrency-protocol.md` 末尾追加 **`## 7. 收官批量补记（2026-09-14 · #293 收口实测新增，规则 38–41）`**（一处，全文未重排）：

- **规则 38 · 窗口回滚必须重建派生面并重签印记**：`tsc -b packages/skill-calorie --force` 紧跟 `gen-cli.mjs --stamp`；只重编不重签⇒记陈旧，只重签不重编⇒拿旧产物签字；判「树干净」时 `dist/` 与印记一并纳入；与 `#325` 的印记假绿互为反面。
- **规则 39 · 席位中途死掉留下的未提交改动＝无主 diff**：⛔ 不许当既成事实采信，接手席先量现场再自行判定完成度（补齐／回退／重做），保持非受益者身份。
- **规则 40 · 取证与成文分两段派**：取证席按维落盘原始输出，成文席从盘上编译、不重跑不改数。
- **规则 41 · 提交与证据三条**：① 提交一律限定路径（`git diff --cached --name-only` 复核后 `git commit -- <路径>`）；② 对已入仓证据件的收窄／改写，原文必须落**受跟踪**路径（`.scratch/` 只作临时副本）；③ **结论类评论不回头改**，改状态时在正文或新评论写清取代关系。

### A-2 S3 文本核销（只改证据件文本，⛔ 未动源码）—— 完成 6 件

| # | 票 | 件 | 改法 |
|---|---|---|---|
| 1 | `#314` | `t314-搬迁-证据.md` §五 第 8 行 | 括注「（生成器加存在性断言）」→「（本票装置脚本加存在性自检，未入仓）」 |
| 2 | `#317` | `t317-搬迁-证据.md` 第 5 条（`routes/scene-05.ts` 头措辞） | 追加 **S3-② 核销**：依复核席 `b7558a71`（`t317r-复核报告.md` §2.5）判「**不是交付缺陷、也不是门禁缺陷**，属措辞／一致性」，统一归后续措辞票 |
| 3 | `#316` | `t316-搬迁-证据.md` §10 | 标题「测试门 flake 判据」→「测试门**本窗一次即全绿**」；括注更正：两次同红发生在**更早两个未通过的补正窗** `050cf4ca`／`b933a587`，硬规则 30 判**真回归／停手上报**，**不是 flake**；本窗（BASE `c3662700`）从第一次起即全绿 |
| 4 | `#319` | `t319-搬迁-证据.md` 新增 **§⑪ 收官批量核销（S3 遗留 7 条）** | 逐条核销（见下） |
| 5 | `#324` | `t324r-复核报告.md` S3-1／S3-2／S3-3 三条 | 口径改「**逐窗**生成物不变」（跨窗那次 `registry.ts` 变化归夹入的 `78472a3 feat(318)`、范围外）／指向 **`#329`**／指向 **`#320` 收官批量**（本批接手） |
| 6 | `#320` | `t320-收口-证据.md` 顶部 | 加一行「**本件 §①② 结论已被 `t320-收口-证据2.md` 取代**」 |

**`#319` 七条逐条核销结论**：①票面「20 条／1 条」之争**已消**——当刻 `src/cli/legacy/routes/scene-10.ts` = `ROUTES_SCENE_10: readonly RouteDecl[] = []`（记录 **0** 条，`list:` 命中 0）；②证据 §②／§⑥.3 预备期口径**保留原样核销**（§⑧ 已给当刻事实，复核席自判「历史记账，可留」）；③`t293` 漏列——**证据侧已消**（§③ 步骤表 :69／:72／:167 三层在册），**导出件侧核销为「不可追补」**（那次探针是裸跑、只留 gitignored 日志，与 `#314` §五第 7 行同口径；受跟踪对账源不能事后造）；④导出件 `START` 行——**已消**（`f69fc70` 重导，当刻含 `RUN ticket=319-land … exit=0`）；⑤票面第 1 条口径——**核销**（票面文本不在收尾席权限内，口径以证据 §⑥.5 为准）；⑥棘轮——**机器门已消**（`#320` 收口 `t320-收口-证据2.md` §2 件 2 已在 `test/cmd-registry-294.test.mjs:133` 起加「分派层（`src/cli` 全目录）没有按键分派的 `calorie.` 字面量」终态断言 ＋ 鉴别力自证，两张冻结 `case` 清单已删），**残余**＝两个**行数**上界 `FROZEN_READ_LINES = 1146`／`FROZEN_WRITE_LINES = 745`（`:45-46`）**仍未下调**⇒转 **`#330`**；⑦实现层件转 `#324`——**已消**（`#324` 已关）。
**七条合计**：已消 6、部分 1（③证据侧消／导出件侧记账）、残余转票 1（⑥的行数上界 ⇒ `#330`）；无 S1／S2。

### A-3 `dist` 孤儿清理 —— **清单已成，删除被实测否决**（见 §三 红读数）

**清单（重算脚本 `.scratch/293close/dist-orphans2.mjs`，逐条理由见 `.scratch/293close/dist-orphans-manifest.md`）**：判据＝`dist/<base>.{js,d.ts,js.map,d.ts.map}` 在 `src/` 侧既无 `<base>.ts` 也无 `<base>/index.ts` ⇒ **任何一次干净重建都生不出它**。

- 实测：`dist` 文件 **1017** 件 ⇒ 孤儿 **30 个基名 × 4 ＝ 120 件**（保留 897）。
- 30 个基名：`analysis/diet`・`analysis/volatility`・`analysis/weight`・`analysis/weightCompare{,2,3}`・`fetch/{exercise,goal,history,photos,plan,products,weight}`・`render/{bodyPlate,exercise,goal,goalExtra,health,help,helpCenter,helpFile,helpPaths,home,library,nutritionPort,nutritionPortDocs,photo,ranking,weightPlate}`・`weight/goalWeight`。
- **逐条理由**（26/30 有新住处实证，4/30 需单独定位）：`analysis/*` 与 `fetch/*` 全部对应 `src/<能力>/*.ts` 同名新件（如 `fetch/plan`→`src/workout/plan.ts`、`analysis/volatility`→`src/weight/volatility.ts`）；`render/*` 对应 `src/<能力>/*.ts`（`render/bodyPlate`→`src/body/bodyPlate.ts`，`src/render/index.ts` 已改从新路径 re-export）；四处无同名新件者按**导出名**定位：`render/goalExtra`→`src/goal/goalExtraPlate.ts`（`buildGoalExpiringView`／`buildGoalPredictView`／`buildGoalVsActualView`）、`render/health`→`src/analysis/healthPlate.ts`（`buildHealthPlate`）、`render/weightPlate`→`src/weight/plate.ts`（`buildWeightDashboard`）、`weight/goalWeight`→`src/goal/read.ts`（`viewGoalWeight`）。
- **静态引用扫描**：`dist/`＋`scripts/`＋`test/` 内 `.js` 说明符引用 **0**；跨包 `.ts/.mjs/.js`＋`tooling`／`scripts`／`apps` 扫描的命中项**全是注释或文档串**（`.scratch/293close/coderefs.txt`／`distrefs.txt`／`allrefs.txt`）。

**删除实测（票号 `293close`，runId `46f2d240-c167-4137-9170-1bbfb04fe77f`）**：删 120 件（`DELETED=120 AFTER_DIST_FILES=897 ORPHAN_LEFT=0`，印记件 `dist/.gen-inputs.json` 未动）后全量 **1492／pass 1487／fail 5**，其中 **2 红直接由删除造成**：

```
test at packages\skill-calorie\test\render-t10.test.mjs:175:1
✖ HELP：现找直达可执行命令（Q71）
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module 'D:\ilife\packages\skill-calorie\dist\fetch\photos.js'
test at packages\skill-calorie\test\skill-t11.test.mjs:151:3
✖ T10 照片 HELP：10 条全可执行（模块+函数逐条 import 存在）
  Error [ERR_MODULE_NOT_FOUND]: … 'dist\fetch\photos.js'
```

**根因（本席新查出的门禁盲区）**：`src/photo/helpLookup.ts:35-41` 的 4 条 HELP 表把 `module` 写成 `'skill-calorie/dist/fetch/photos.js'`（旧路径），两个测试**按表里的字符串动态 import** ⇒ 这些「没人 static import 的陈旧产物」**其实有活消费者**：`dist/fetch/photos.js` 一旦不存在，两个测试即 `ERR_MODULE_NOT_FOUND`。⇒ 「零 static import」**不足以**证明无主。

**处置**：从 12 个 `.scratch` 快照副本按**多数哈希**恢复 **116 件**，**0 冲突**（`.scratch/293close/restore-dist.mjs`，日志 `restore-log.txt`）。**4 件不可恢复**＝`dist/weight/goalWeight.{js,d.ts,js.map,d.ts.map}`（12 个副本**全无**，且 `src/` 侧无对应、全仓 0 引用 ⇒ 正是「无主陈旧产物」的确认样本）。恢复后全量 **1492／pass 1489／fail 3**（只剩 §三 的 3 条 history 红）。

**结论**：本节**不删任何件**（除已不可恢复的 `weight/goalWeight.*` 4 件）。理由：① 有活消费者者必须**先由 `#328`（文案同步：旧路径与旧计数）把 `module` 指针改到 `dist/photo/photos.js`**，再由后续批删；② 「干净重建生不出」**不等于**「无主」——`dist/fetch/photos.js` 在任何干净重建的树上本来就已悬空（那两个测试在 clean checkout ＋ 全量重建下**必然红**，这是本席查出的既存缺陷，建议随 `#328` 一并修）。⇒ 复核报告 `#324r` 的 **S3-3（dist 孤儿）** 标注为：**清单与逐条理由已成，删除判为「不可在本批执行」**。

### A-4 对账口径整改（S3-8）—— 完成（口径落地 ＋ 本批按新口径执行）

- **按票各跑一次，不用「无 `--ticket` 的跨票窗」**：本批所有门禁一律票号 **`293close`**（诊断窗另用独立票号 **`293close-diag`**，不混进正式窗口）。
- **导出件按票分文件**：`docs/skills/skill-calorie/t293-gate-runs.log`（票号过滤 `293close`）。
- **设计红与门禁分开记**：约定前缀 **`MUT-RUN`**（变异／故意跑红）与门禁 `GATE-RUN` 分列；本批**未跑变异**，故只留门禁声明。
- **目标＝退休 `--allow-undeclared`**：本批**未使用任何 `--allow-*`**（对账读数见 §四）。口径判词：跨票并窗是「口径自招」，长期会习惯性毁掉最重要的一条反向对账（`undeclared`）。

### A-5 收官复跑 —— **七项读数**（票号 `293close`，八条 RUN，窗口 `--since 2026-09-14T00:07:13Z`）

| # | 命令要点 | runId | exit | 读数 |
|---|---|---|---|---|
| 1 | `tsc -b`（仓根） | `280a2f91` | 0 | 无输出（增量，无错） |
| 2 | `tsc -b packages/skill-calorie --force`（规则 38 重建） | `e2754261` | 0 | 无输出 |
| 3 | `gen-cli.mjs --stamp`（规则 38 重签） | `0e3db087` | 0 | 印记重签 |
| 4 | `gen-cli.mjs --check` | `81c828de` | 0 | **键 101（写 35 ＋ 读 66）／未搬迁清单 0 条**；路由三面 495 条记录（WAKE 436 ＋ NEW_KEY 58 ＋ COVERAGE_REPAIR 1）／exec 403 |
| 5 | 全量 `node --test <15 个包模式>` | `46f2d240` | **1** | **tests 1492／pass 1489／fail 3** ⇒ **红** |
| 6 | 冻结件 `test/calorie-routing-81.test.mjs` | `71d5adb3` | 0 | **tests 8／pass 8／fail 0** |
| 7 | 判官 `docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `36f7049f` | 0 | **`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`；`UNACCOUNTED=0 处`** |
| 8 | 快照 `tooling/skill-html-snapshot.mjs --check` | `a83e2865` | 0 | **artifacts=186 changed=0 added=0 removed=0**，base-* 指纹 `ef7cc02969e37e4bfc0eb35751b8dc1c` |

**A-5 判定：⛔ 未全绿**（第 5 项红）。七项中 **6 项绿**（`tsc -b`／`gen:check` 未搬迁 0／探针 5/5＋`UNACCOUNTED=0`／冻结件 8/8／快照 186 件 changed=0），**对账亦红**（`gate-audit: FAIL`，**与第 5 项同源**——红门在默认口径下不可认领；读数见 §四）。**红项合计 2 项：全量门 ＋ 对账。**

---

## 二、`#293` 关地图：**未执行**

派单给定前置＝「**A-5 全绿**」才做 B 段。A-5 第 5 项红 ⇒ 按派单「地图宁可留开，也不要带病关」**停在 A 段，B 段未做**：未读 `#293` 正文、未写 `## 进度：100%`、未追加收官段、未 `gh issue close 293`。`#293` 当刻仍 **OPEN**。

---

## 三、A-5 第 5 项红：那 3 条不是本批造成的（诊断与证据）

**红签名**（三条同族：`#108`／`#109`／`#110`「唤醒词→key→HTML 链：路由 cli 直跑产出对应全文档」）：

```
test at packages\skill-calorie\test\diet-homogeneity-108.test.mjs:111:1
✖ #108 …  Error [CalorieRenderError]: 无饮食记录（2026-09-08 ~ 2026-09-14） code: 'missing-data'
✖ #109 …  （同族）
✖ #110 …  Error [CalorieRenderError]: 组合无数据（weight_calorie，2026-09-08 ~ 2026-09-14）
```

**定性：既存日期腐坏（date rot），与本批改动无关**，四条机器证据：

1. **是「数据窗口」不是「模块解析」**：三条全是 `missing-data`（查询窗口里没有记录），**没有** `ERR_MODULE_NOT_FOUND`——本批真正造成过的那 2 红已在 A-3 恢复后消失（5 红 ⇒ 3 红）。
2. **窗口来自相对参数**：`#320b`（`1ac41ca`）把 `routes/scene-06.ts` 的最后一条记录（`cli: … --params '{"window":"7d"}'`）搬走／改道；`windowRange()`（`src/shared/params.ts:93`）把 `7d` 解析成「**今日往前 7 天**」⇒ 2026-09-14 当刻＝**09-08 ~ 09-14**。
3. **种子是硬编码的旧日期**：`test/diet-homogeneity-108.test.mjs` 的 `seedDiet()` 写死 `2026-09-05 ~ 2026-09-07` 的餐次（该测试件最后一次改动是 `2a86f2e`（09-13 13:25，内容一字未改）／`b4eb1d6`（09-09））⇒ 7 天窗一旦滑过 09-07，种子即全部落在窗外。**今天（09-14）正好滑过**。
4. **本席未碰这些面**：窗口内对 `src/`／`test/` 的改动 = **0**（`git status` 只有 `docs/` 下本席自己的件）；`dist` 侧本席只做了「删→恢复」与一次 `tsc -b --force` 重建，而上面三条红依赖的 `dist/render/diet.js`／`dist/home/today.js`／`dist/render/analysisPlate.js`／`dist/analysis/commands.js` **都是活件、且是当刻 `src/` 的新鲜编译结果**。
5. **反证（为什么早先读数是绿的）**：`.scratch/orth-full.log`（**09-14 02:24**）记 `tests 1492／pass 1492／fail 0`，但那是 `105e0af`／`090f502` 时代的**旧 `dist`**；`1ac41ca`（09-14 06:01）与 `e9ee42e`（06:35）之后**首次把 `dist` 与当刻 `src` 配平**（本批第 2 步 `--force` 重建）才把它暴露出来 ⇒ 这正是规则 38 的另一面：**不重建派生面就看不见的既存红**（`#325` 家族的同族）。

**⇒ 修它必须动 `src/`（相对窗口的口径）或 `test/`（种子日期），两条都在本席红线内（⛔ 不改 `src/`／`test/`）⇒ 本席只能报，不能修。**

---

## 四、对账读数（`check-gate-audit.mjs`，票号 `293close`，**未使用任何 `--allow-*`**）

**事先声明（`GATE-RUN`；runId 逐条取自 `.scratch/locks-land/gate-runs.log` 的真实条目；窗口 `--since 2026-09-14T00:07:13Z`）**：

- GATE-RUN runId=280a2f91-e1fe-46fe-88fa-2b9d0814becb cmd="node node_modules/typescript/bin/tsc -b"
- GATE-RUN runId=e2754261-0503-4cd3-8e91-2c3856cf806f cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force"
- GATE-RUN runId=0e3db087-61ef-41ac-886a-8ede0d6bd8c4 cmd="node packages/skill-calorie/scripts/gen-cli.mjs --stamp"
- GATE-RUN runId=81c828de-e1ba-4740-b5fb-336c989440f2 cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"
- GATE-RUN runId=46f2d240-c167-4137-9170-1bbfb04fe77f cmd="node --test test/*.test.mjs packages/base-render/test/*.test.mjs packages/skill-calorie/test/*.test.mjs packages/skill-memo-ilife/test/*.test.mjs packages/skill-schedule/test/*.test.mjs packages/skill-home/test/*.test.mjs packages/skill-bill/test/*.test.mjs packages/skill-chef/test/*.test.mjs packages/plugin-manager/test/*.test.mjs packages/plugin-calorie/test/*.test.mjs packages/plugin-memo-ilife/test/*.test.mjs packages/plugin-schedule-ilife/test/*.test.mjs packages/plugin-home-ilife/test/*.test.mjs packages/plugin-chef/test/*.test.mjs packages/plugin-bill-ilife/test/*.test.mjs"
- GATE-RUN runId=71d5adb3-c5da-4d82-8c1a-11258082feb9 cmd="node --test test/calorie-routing-81.test.mjs"
- GATE-RUN runId=36f7049f-9874-4dc4-b7b1-fea64c3259fb cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
- GATE-RUN runId=a83e2865-bb5d-428c-9f70-24a1bc51ef91 cmd="node tooling/skill-html-snapshot.mjs --check"

窗口＝本批最后一轮连续复跑（UTC `2026-09-14T00:07:13Z → 00:08:01Z`，8 条 RUN；脚本 `.scratch/293close/gate.ps1`，逐条原始输出 `.scratch/293close/gate-0N-*.out.txt`）。

- **对账 run#1（本件尚无声明）**：runId `cfcedd04-8660-40e3-876e-34f22b5db504` ⇒ `matched=0/0 auditEntries=379 scoped=8 undeclared=8` ＋ **gate-audit: FAIL**（「证据里没有任何 `GATE-RUN runId=… cmd=…` 声明」）——这是「**先声明后对账**」这条纪律的**空转红对照**：证明该反向对账**不空转**、窗口内 8 条 RUN 一条都逃不掉。
- **对账 run#2（补上上面 8 条声明后）**：runId `a0a29eeb-0c38-46c6-821d-a2e6b622e76e` ⇒ `matched=7/8 auditEntries=8 scoped=8 undeclared=1` ＋ **gate-audit: FAIL**——**唯一**未认领条目正是 8 条里 **`exit=1` 的全量门**（`46f2d240`）；`check-gate-audit` 默认只认领 `exit=0` 条目，本席**未用 `--allow-nonzero` 放宽**。⇒ **意义正在这里**：红窗口**不可能**在默认口径下对成 PASS，读数与门禁一致（红对红）；若用放宽过的 PASS 掩盖红门，正是本批 A-4 要退休的做法。**7/8 已认领**证明声明口径本身可用。
- **`--allow-undeclared` 退休（S3-8 目标）**：本窗口是**单票号窗口**（`--ticket 293close`；诊断窗另起独立票号 `293close-diag`，不进正式窗口），`undeclared` **无需放宽即可为 0**；导出件按票分文件（`t293-gate-runs.log`）；设计红／变异读数约定用 **`MUT-RUN`** 前缀与门禁 `GATE-RUN` 分列（本批未跑变异，故无 `MUT-RUN`）。

## 五、本席未做／交接

- **B 段全部未做**（关地图 `#293`）：原因＝A-5 第 5 项红（见 §二／§三）。
- **A-3 的删除未执行**（只留 `weight/goalWeight.*` 4 件已删且不可恢复）：待 `#328` 改文案路径后由后续批执行；`dist/fetch/photos.js` 等 116 件已按多数哈希恢复。
- **未跑变异**（A-4 的 `MUT-RUN` 前缀只是口径约定，本批无设计红读数）。
- **给接手席的最小修复路径**：① 先定 `7d` 相对窗口 vs 硬编码种子这两处谁改（`src/shared/params.ts` 口径 或 `test/*-homogeneity-10{8,9}.mjs` 种子改成相对今日生成）；② 修 `src/photo/helpLookup.ts` ＋ `scripts/build-help.mjs:244` 的旧路径文案（`#328`），再删 `dist` 孤儿；③ 重跑本件 §一 A-5 的七项至全绿，再执行 §二 的 B 段（派单原文见 `.scratch/293close/`）。
