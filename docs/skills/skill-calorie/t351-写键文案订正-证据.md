# T351 写键文案订正 · 证据（陈化断言订正：SKILL.md ＋ commands.ts 件头）

- 票／地图：#351 ／ #157
- 角色：收口执行席（纯文案订正；不跑测试、不编译、不改代码逻辑、不重生成）
- 落盘时刻的盘面读数；本仓有并发窗口在落盘，读数只对应本文件成文那一刻
- 写集：`packages/skill-calorie/SKILL.md`、`packages/skill-calorie/src/workout/commands.ts`（只注释）、本件

---

## 一、判定

**PASS** —— 三处陈化断言已按事实订正（SKILL.md 第 63／73 行、`commands.ts` 件头），零代码逻辑改动，
未跑任何重生成与测试，未碰墙目录与他席 M 件。另点名 4 条写集外的同类陈化（下一手清单见 §六）。

---

## 二、只读勘察清单（改前，逐条：file:line ＋ 原文 ＋ 判定）

关键词面（票面指定四个：`无写键`／`不可写`／`不得承诺`／`planWriteMissing`，包内 489 件，剔 `dist`／`node_modules`）：

| # | 位置 | 原文（摘） | 判定 |
|---|---|---|---|
| 1 | `SKILL.md:63` | `\| 定训练计划 \| plan_builder_wizard.html \| **当前不可写**：95 键无训练计划写键（见下） \|` | **陈化 · 改**（第 42 行同文件第 11 行把「定训练计划」列为配置型写词；`calorie.workout.plan-set` 是真写键） |
| 2 | `SKILL.md:73` | 「需多步交互的配置写词在路由层落 `non-exec` 桶…**训练计划 95 键无写键，当前不可写**——`NON_EXEC_REASONS.planWriteMissing` 逐字「…本仓执行层不承接计划写入。」，命中回 `non-exec` 并告知用户属二期，**不得**承诺 verify 后写入。」 | **陈化 · 改**（整句与路由实况相反：`定训练计划` 已是 `exec`；同句里「落 non-exec」与「可落库」不能并存） |
| 3 | `src/workout/commands.ts:13` | 「「定训练计划」「落地训练」「同步到训记」这些词**本场景无写键**（理由逐字住 `routes.ts`）。」 | **陈化 · 改**（同文件第 42 行就是 `kind:'write'` 的 `plan-set`） |
| 4 | `src/workout/index.ts:8-10` ＋ `:35` | 「本场景今天**没有写键**（「定训练计划」在旧链是 `--live-plan-*` 系列，本仓执行层不承接…）」／「写命令入口：同上（本场景今天无写键，任何键都抛）。」 | **陈化 · 写集外**（同一文件的注释与写入口注释；本次不改，见 §六） |
| 5 | `src/triggers/routing.ts:70-71` | `planWriteMissing: '命中但不执行：95 键无训练计划写键（旧链 --live-plan-* 系列），本仓执行层不承接计划写入。'` | **陈化 · 写集外**（实测 `packages/skill-calorie/src/**` 零引用；`routes.generated.ts` 零命中 → 死常量，其词文本与事实相反） |
| 6 | `test/skill-t11.test.mjs:47` | `for (const s of [ …, 'plan_builder_wizard.html', '当前不可写', '不豁免', '协议 fail mode']) { assert.ok(skill.includes(s), …) }` | **陈化 · 写集外（会因本次改动变红）**，见 §六 第 1 条 |
| 7 | `test/skill-t11.test.mjs:42` | `it('M6 Wizard Verify 铁则正文（#98）在 AUTO 块外，含前置/映射/不可写/违规口径', …)` | **陈化 · 写集外**（用例标题文案，钉旧口径；非断言，不影响绿红） |
| 8 | `src/analysis/index.ts:38` | 「写命令入口：同上（本场景今天无写键，任何键都抛）。」 | **正当 · 不改**（实测 `src/analysis/commands.ts` 无一条 `kind:'write'`；分析域 13 键全读） |
| 9 | `src/cli/write.ts:22` | 「#101 → #120 · 删除可恢复性口径（**不得承诺可恢复**——全仓 0 个 restore/undo/recover 入口）」 | **正当 · 不改**（说的是删除的可恢复性，不是写键） |
| 10 | `src/shared/writeParts.ts:10` | 「（**不得承诺可恢复**：软删除＝行保留…；硬删除＝行删除、不可恢复）」 | **正当 · 不改**（同上） |
| 11 | `src/diet/edit.ts:5` | 「（**不得承诺可恢复**的口径只住那一处）」 | **正当 · 不改**（同上） |
| 12 | `src/migrate/migrate.ts:22` | 「dst 不可写」 | **正当 · 不改**（文件系统权限语义，非命令写键） |

关键词之外、同族的陈化（勘察中发现，**本次不动**，逐条登记）：

| # | 位置 | 原文（摘） | 判定 |
|---|---|---|---|
| 13 | `SKILL.md:33` | 「写链（#40）：35 写键（diet/water/weight/exercise/photo/product/profile/goal/body）同出口可执行…；训练计划/训记同步与 mmx vision 两步向导无独立写键（二期，见 triggers 旧链）。」 | **陈化 · 关键词未覆盖**（写作「无独立写键」故不在四词面上；写链枚举九域不含 workout，而 `src/workout/commands.ts:42-51` 已有 10 条写声明） |
| 14 | `docs/skills/skill-calorie/scene-inventory.md:81-90` | 场景 35-44 逐行「❌ 没打通 … 95 键无训练计划写键（旧链 --live-plan-* 系列），本仓执行层不承接计划写入。」 | **陈化 · 写集外（doc）** |
| 15 | `docs/**` 的历史件（`t81-route-evidence.md`／`t98-m6-wizard-verify-evidence.md`／`t164-SKILL与HELP现状.md`／`t313-routing-exec-face-before.json`／`t317-搬迁-证据.md`／`t351-swap-review-报告.md`） | 同口径文本 | **正当 · 不改**（历史快照与前序复核记录：`t313-…-before.json` 就是「改前」面；`t164:33` 早已把 `L63` 记为「陈旧层」） |

---

## 三、逐条「旧原文 → 新原文」对照

### 3.1 `packages/skill-calorie/SKILL.md:63`

- 旧：`| \`定训练计划\` | \`plan_builder_wizard.html\` | **当前不可写**：95 键无训练计划写键（见下） |`
- 新：`| \`定训练计划\` | \`plan_builder_wizard.html\` | **可写**：出计划编辑器（可写页）→ 用户改完确认 → 复制命令 → AI 调 \`calorie.workout.plan-set\` 落库（见下） |`
- 事实出处：`routes.generated.ts:198` `定训练计划 → exec / calorie.view.plan-wizard`；`:510` `确认定训练计划 → exec / calorie.workout.plan-set`；`src/workout/commands.ts:42` `kind:'write'`
- 旁证（本条最能说明「陈化」二字）：**同一份 `SKILL.md` 的 AUTO 块（生成物，块内）第 205 行本来就写着**
  `| 定训练计划 | calorie.workout.plan-set | receipt | … |` —— 生成物早就认这对「唤醒词 ↔ 写键」，
  只有块外那两句手写散文与它相反。改后块外、块内第一次同口径。

### 3.2 `packages/skill-calorie/SKILL.md:73`（整条 bullet 换写）

- 旧：`- 需多步交互的配置写词在路由层落 \`non-exec\` 桶，\`reason\` 逐字 \`NON_EXEC_REASONS.wizard\`（\`triggers/routing.ts\` \`NON_EXEC_REASONS\` 及其词表项）；**训练计划 95 键无写键，当前不可写**——\`NON_EXEC_REASONS.planWriteMissing\` 逐字「命中但不执行：95 键无训练计划写键（旧链 --live-plan-* 系列），本仓执行层不承接计划写入。」，命中回 \`non-exec\` 并告知用户属二期，**不得**承诺 verify 后写入。`
- 新：`- 配置型写词在路由层落 \`exec\` 桶，不再是 \`non-exec\`（路由表里 \`non-exec\` 只剩 9 条，全是「明确不做」与无营养建议形态）：命中即出本表那页——「定训练计划」出的是**计划编辑器（可写页）**，路由键 \`calorie.view.plan-wizard\`；用户改完复制命令，AI 调写键 \`calorie.workout.plan-set\`（唤醒词「确认定训练计划」）落库给 \`receipt\`。**定训练计划的写键确实存在，这条链不缺环**：\`NON_EXEC_REASONS.planWriteMissing\` 在 \`routes.generated.ts\` 里零命中、是死常量，它那句「本仓执行层不承接计划写入」与事实相反，不得据此拒绝出页或拒绝落库。`
- 为什么整条换写（不只切后半句）：同一条 bullet 的前半句「配置写词落 `non-exec` 桶、`reason` 逐字 `NON_EXEC_REASONS.wizard`」实测已不成立（`routes.generated.ts` 里 `NON_EXEC_REASONS` **零命中**；436 条路由只剩 **9 条** `non-exec`＝`out-of-scope` 8 条〔落地 3／训记 2／定时复盘 3〕＋「无营养建议形态」1 条）。只改后半句会留下「前半句说不执行、后半句说可落库」的新自相矛盾——正是 S2 的成因。`planWriteMissing` 这一处保留为**订正后的防呆引用**（明说它是零引用死常量、其词与事实相反、不得据此拒写），不是陈化断言；因为 `routing.ts:70-71` 那句旧词文本仍在仓里，AI 有别的路径读到它。

### 3.3 `packages/skill-calorie/src/workout/commands.ts:13-14`（只注释，代码一行未动）

- 旧：` * 「定训练计划」「落地训练」「同步到训记」这些词**本场景无写键**（理由逐字住 \`routes.ts\`）。`（1 行）
- 新：
  ` * 「定训练计划」有写键（过程页出可写编辑器，用户复制命令后由下方 \`workout.plan-*\` 那一族写声明落库）；`
  ` * 「落地训练」「同步到训记」本场景无写键（理由逐字住 \`routes.ts\`）。`（2 行）
- 「落地训练」「同步到训记」仍无写键属实（`routes.generated.ts:208-212` → `non-exec`／`out-of-scope`，落地与训记是「明确不做」），故这一半原样保留、只把主语拆开。
- `git diff` 该件只有 `@@ -12 +13,2 @@` 这个 hunk 是我的；同文件其余 hunk（`:9-12` 件头「检视半→过程页」、`viewPlanWizard→viewPlanEditor` 等）是**前序席在途的未提交改动**，我未触碰。期间该件被并发窗口写过一次（读后被判 stale，重读确认第 13 行原样，再落盘成功）。

---

## 四、关键词命中数（改前／改后）

包内面（`packages/skill-calorie/**`，剔 `dist`／`node_modules`，行数口径）：

| 关键词 | 改前 | 改后 | 备注 |
|---|---|---|---|
| `无写键` | 4 行 | 3 行 | 减：`SKILL.md:73`；剩：`analysis/index.ts:38`（正当）、`workout/commands.ts:14`（改后仍含，主语已换成真无写键的「落地训练／同步到训记」）、`workout/index.ts:35`（陈化，写集外） |
| `不可写` | 5 行 | 3 行 | 减：`SKILL.md:63`＋`:73`；剩：`migrate.ts:22`（正当「dst 不可写」）、`test/skill-t11.test.mjs:42`＋`:47`（陈化，写集外） |
| `不得承诺` | 3 行 | 3 行 | 未变；三处全是「不得承诺可恢复」（正当）。`SKILL.md` 原文写作 `**不得**承诺`（强调符插入），逐字面本来就是 0 行 |
| `planWriteMissing` | 2 行 | 2 行 | 未变：`routing.ts:70`（死常量定义，写集外）＋`SKILL.md:73`（改后为**订正后的防呆引用**，非陈化断言） |

`SKILL.md` 单件：`无写键` 1 行→**0**；`不可写` 2 行→**0**；`不得承诺` 0→0；`planWriteMissing` 1 行→1 行（见上）。

全仓同一口径的残留（`grep 当前不可写|无训练计划写键`）只剩：`routing.ts:71` 死常量词文本、`test/skill-t11.test.mjs:47` 断言串、`scene-inventory.md:81-90` 十行清单、以及 `docs/**` 历史件（正当）。

---

## 五、`SKILL.md` 是不是生成物 —— 结论

- **是「半生成物」**：`scripts/build-help.mjs` 的 `renderSkillMd()`（`:532-536`）只做
  `text.slice(0, si+START.length) + '\n' + buildHelpBlock() + '\n' + text.slice(ei)` —— **只重写 `<!-- HELP-AUTO-START -->`／`<!-- HELP-AUTO-END -->` 之间那块**，块外逐字节原样；它只写 `SKILL.md` 这一件（`:538-544`），无标记即抛。
- **本次两处在块外**：`SKILL.md:80` 才是 `HELP-AUTO-START`，要改的 `:63`／`:73` 都在它**上面** ⇒ 走**手改**（重跑生成器带不走这两句，复核席的结论复验成立）。生成器我**一次都没跑**（票面禁 `pnpm gen` 之外的重生成，本次连它也没跑：AUTO 块一字未动）。
- 影响面：块内未动 ⇒ `help:examples:check` 的「AUTO 块 == `renderSkillMd` 输出」与 `skill-t11` 的「互联区新鲜」两条对本件的判定不受本次改动影响。
- 行数：`SKILL.md` LF **542→542**（两处都是 1:1 整行替换）；`src/workout/commands.ts` LF **52→53**。

台账（包内规矩）：`node packages/skill-calorie/scripts/check-warning-line.mjs`（只读、无参）→
`RESULT: 87/87` ＋ `PASS: 告警线台账齐全且与实况一致`、`exit 0`（台账 42 行／扫描面 327 件／超线 38 件／剔出生成物 3 件）。
`src/workout/commands.ts` 53 LF 远在 350 线内、且台账里没有它这一行；`SKILL.md` 本就不在扫描面内（包内规矩：「不算 `SKILL.md`（说明面）」）⇒ **没有触发台账陈化，`AGENTS.md` 未改、`--sync`／`--dry` 都没跑**。台账里在陈化的是他席的件，未管。

---

## 六、未做项与下一手（点名到行）

1. **`test/skill-t11.test.mjs:47` 会因本次改动变红（最高优先）**：该断言逐字找 `'当前不可写'`，改后 `SKILL.md` 里已是 0 行。同一数组其余六串实测都在（`记体脂（皮褶钳）` 5 行、`body_composition_wizard.html`／`body_measurements_wizard.html`／`plan_builder_wizard.html`／`不豁免`／`协议 fail mode` 各 1 行）⇒ **只有这一串要重指**。测试件不在本席写集（票面只给 SKILL.md／commands.ts／AGENTS.md／证据件），故留红给持该件的席：把 `'当前不可写'` 换成事实串（如 `'calorie.workout.plan-set'` 或 `'可写编辑器'`），同时把 `:42` 用例标题里的「不可写」一并改口径。我**没有跑测试**（票面禁止），这条是静态推演＋逐串存在性实测的结果。
2. **`src/workout/index.ts:8-10` 与 `:35`（两处同件陈化）**：`「本场景今天**没有写键**（「定训练计划」在旧链是 --live-plan-* 系列，本仓执行层不承接…）」`／`「写命令入口：同上（本场景今天无写键，任何键都抛）」`。`runWorkoutWrite` 的实际行为仍是「查 `kind:'write'` 就派发」，注释与实况相反。写集外 ⇒ 未改；建议随该件归属票一行订正。
3. **`src/triggers/routing.ts:70-71` 死常量**：前序复核 D2 建议「顺手删」，但它住在代码件、票面明令本次「不改代码逻辑」⇒ 未动。它那句旧词文本只要还在仓里，AI 就仍有别的路径读到「执行层不承接计划写入」——本席的处置是在 `SKILL.md:73` 就地写一条防呆（明说该常量零引用、其词与事实相反、不得据此拒写）。
4. **`SKILL.md:33`（关键词未覆盖的同类陈化）**：写链枚举「35 写键（diet/water/weight/exercise/photo/product/profile/goal/body）」不含 workout，末句「训练计划/训记同步与 mmx vision 两步向导无独立写键（二期，见 triggers 旧链）」里的「训练计划」已不成立（`src/workout/commands.ts:42-51` 有 10 条写声明）。票面点名的是 63／73 两处且要求「不要顺手改别的段落」⇒ 未改，**如实登记**。下一手若复量：`SKILL.md` 的「99 键＝64 读＋35 写」是 `CALORIE_COMBOS` 组合键口径，本次**未核**（实测 `src/**/commands.ts` 的声明条数是读 82／写 46，两个口径不可直接比）。
5. **`docs/skills/skill-calorie/scene-inventory.md:81-90`**：场景 35-44 十行仍写「❌ 没打通 … 无训练计划写键」，同属陈化；doc 件、写集外 ⇒ 未改。
6. **没做**：未跑测试／编译／`pnpm gen`／`pnpm gen:check`／`help:build`／`--sync`／装包；未碰 `docs/skills/skill-calorie/scene05-验收墙/**`（本次改动不进产物，未重出墙）；未碰他席 M 件；未做任何 `git add/commit/push/stash/reset/checkout --/clean`（只用了只读的 `git status`／`git diff`）。
