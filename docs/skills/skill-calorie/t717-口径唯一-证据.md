# #717 证据 · 批① 餐别归一（时间窗／归桶／四桶只留一处定义）

票：#717《规格：同一件事只留一处定义（六处口径归位 ＋ 内容三裁 ＋ 防复发门）》，规格 §五 的第 1 批。
本件只记**批①**做完的读数；其余四批的状态在票面「进度」区。

## 一 · 收口结论

同一件事（一条记录属于哪一餐）在全包**只剩一处定义**：`packages/skill-calorie/src/shared/meal.ts`
（零依赖，不出现任何能力目录的名字）。原先的第二套小时边界（`analysis/anomaly/common.ts` 里自写的
10／15／21）已删，回到同一份时间窗（早 6-10／午 10-14／下午茶 14-18／晚 18-22／夜宵 22-6）。

**有用户可见读数变化的一处**（规格 §六 要求预告并留证）：**「诊断饮食结构问题」页的餐次统计**——
收口前它与「餐别分布」页对同一天给出不同的餐次（14:30 那条一边算加餐、另一边算午餐；22:30 那条
既被算进别的桶、又不落进夜宵窗）。改后两页同值。页面文案与版式一行未改。

**连带修掉一处自相矛盾**（规格 §三 已具名）：夜宵窗原先只写 `[0, 6)`，而 22 点后的记录被归成夜宵，
于是「按餐别删夜宵」删不掉它。现在夜宵窗是 `[22, 30)`（跨零点，按 24 取模），按窗删与归桶同一把尺子。

## 二 · 变更前后对照读数（同一脚本、同一份种子库、三个分歧点）

脚本：`.scratch/t717/probe-meal.mjs`（本票工作草稿目录，不入库；下表为它的两轮输出）。
库：`docs/research/t81-seed.mjs` 的 `seedFull` 落系统临时目录，另加三条探针行——
`2026-09-02 14:30`／`2026-09-03 21:30`／`2026-09-04 22:30`（各 300 卡）；窗口 2026-09-01 → 2026-09-07。

| 读数 | 收口前（`runId=91a10092…`） | 收口后（`runId=f5989e50…`） |
| --- | --- | --- |
| 餐别分布页 四桶餐次 | `{"早餐":4,"午餐":4,"晚餐":2,"加餐":3}` | 同左（一值未变） |
| 诊断四桶餐次 | `{"早餐":4,"午餐":5,"晚餐":2,"加餐/夜宵":2}` | `{"早餐":4,"午餐":4,"晚餐":2,"加餐/夜宵":3}` |
| 探针归属（分布页） | `14:30→加餐`、`21:30→晚餐`、`22:30→加餐` | 同左 |
| 加餐（下午茶＋夜宵）次数 | 分布页 3 ／ 诊断 **2**（不一致） | 分布页 3 ／ 诊断 **3**（一致） |

**一句话**：分歧只出在诊断侧那 1 条 14:30 记录（午餐 → 加餐）；分布页三次读数一值未变。

## 二之四 · 批③（时钟 ＋ 谓词）与批④（常数 ＋ 两个日期口径）已交付

**提交** `5098aa4f`（批③＋批④ 主体）＋ `8a1348d3`（批⑤ 门）＋ 本节的补提交。

### 共用位新增两处（都零依赖）

| 件 | 内容 |
| --- | --- |
| `src/shared/time.ts` | `todayISO()`／`timeOfDayISO()`／`shiftISODate()`——**时钟的唯一定义地** |
| `src/shared/kcalPerKg.ts` | `KCAL_PER_KG = 7700`——千卡↔体重的唯一定义地 |
| `src/shared/alive.ts` | `EX_ALIVE`／`BODY_ALIVE`——软删存活谓词的唯一定义地 |

### 时钟归一：**10 份副本收成 3 个函数**（规格只点名了 3 份，实测多出 5 份）

先把三份**规格点名**的副本收口（`fetch/diet.ts:41,42`／`exercise/exerciseStore.ts:11,12`／`weight/records.ts:18,19`），
随后给门加了「本地时钟副本」那一行，**门当场又抓出 5 份规格没点名的**（规格「当刻锚点」表的陈化实证）：

`src/goal/goalHistory.ts:28,32`（`todayISO` ＋ `shiftISODate` 两份）／`src/photo/photos.ts:99,103`（两份）／
`src/photo/photos.ts:161`（`toTimeString`）／`src/weight/review.ts:209`／`src/workout/planStore.ts:169`／`src/goal/goalExtraPlate.ts:42`。

全部改读共用位后：门绿（`定义地闭集账 5 项全在台账内`）。
`analysis/utils.ts` 与 `goal/goalHistory.ts` 都按原名转出 `todayISO`／`shiftISODate`（包对外的名字一个不少）。

**批③ 的账要更正一句**：规格写「把日期固定到某一天时，写入落进真实的那一天、回执印的是被固定的那一天」——
实测（`probe-clock-fork.mjs`）**当刻生产不可达**（#676 摘掉了 `CALORIE_TODAY` 环境钉子，钉钟只剩进程级一种、
读写两侧同时跟走）。故这一批交付的是**结构归一**，不是修活的缺陷；判据 ⑤（落库日＝回执日＝累计读日）**从红转绿**
证明的是「归一之后三者仍同一天」，不是「修好了分叉」。

### 千卡常数归一：两份定义 ＋ 三处裸字面 → 一处正本

- 两份定义：`analysis/deficit.ts:12` 与 `analysis/simulate.ts:12`（同值、各改各的）→ 删，改读共用位（两件都按原名转出 `KCAL_PER_KG`）。
- 三处裸字面：`diet/dietEngine.ts`（`totalDeficit / 7700`）／`goal/set.ts`（`(gapKg / daysLeft) * 7700`）／
  `weight/figures.ts`（`(gap / daysLeft) * 7700`）＋ 一并收口 `analysis/review.ts`（`weeklyDeficit / 7700`）→ 四行都改读常数。
- 判据 ⑥：包门转出的 `KCAL_PER_KG === 7700`，且**源码里再无「算式里的 7700」**（门的那一行盯着；注释与可见文案里的
  「每 7700 卡」照旧，那是给读者的话，不是定义地）。

### 两个日期口径：**分家命名，不合并**（照规格 §三 第三裁）

`daysIn`（`shared/params.ts`，闭区间含首末日）与 `spanDaysOf`（`render/trendPredictDocs.ts`，两点跨度不加 1）
**保持各自的名字与语义**——合并会把 #455 修好的那个缺陷改回去。本批只补判据：
判据 ⑦ 从页头窗口条读「日期差」与「印出的天数」，断言 `印出天数 === 日期差 + 1`（`2026-09-01 → 2026-09-07` 印 `7 天`，日期差 6）。

## 二之五 · 批⑤（防复发门）已交付

**提交** `8a1348d3`。**不新造门引擎**——扩现有那道 `scripts/check-one-path.mjs`（trip 与例外机制同构），
再用**一个测试件把它拉进 `node --test`**：这是本包里唯一「真会跑」的自动执行口（实测 `scripts/check-*.mjs`
**都不在 `package.json` 的 script 里、也没有测试件拉它们**——只靠人记得手工跑）。

| 读法 | 结果 |
| --- | --- |
| 门自证 | `RESULT: 5/5`（原三条 ＋ 新增两条：定义地出账**必红**／薄转出**豁免**） |
| 真跑（假包） | `RED … src/rogue.ts:1 命中 "min: 10, max: 20"`，exit 1——**端到端一条真红**，逐行点名文件与行号 |
| 真跑（本包） | `PASS: 测试面每个符号只有一条 dist 路径` ＋ `PASS: 定义地闭集账 5 项全在台账内` |
| 拉门件 | `test/t717-防复发门.test.mjs`（两条，随包测试自动跑） |

**闭集账当刻 5 项**：夜宵跨零点／营养素推荐区间／孤儿区间（已作废，任何地方都不许出现）／千卡常数／本地时钟副本。
**判据与例外都从源码派生**：文件里写着 `export { X } from './件.js'` 的薄转出即豁免（不认手写清单）；
注释里的旧数不算命中（本门只看代码）。

**门第一天就抓到两处真东西**（这是它「真在守」的证据，不是摆设）：
① 规格点名的 5 处口径之外，另有 **5 份时钟副本**（上节）；
② `test/t271-总览接线与两态.test.mjs:280` 把 `hasAnyDietRow` 的定义地**钉死在行号**上——
批② 的编辑让它漂了 4 行就染红。判据已改成「只看件与条数」（行号漂移不是缺陷，钉死行号会天天假红）。

## 二之六 · 本期**未做**的四项（据实登记，票面不 close）

| # | 项 | 当刻实况 | 为什么这次不做 |
| --- | --- | --- | --- |
| 1 | **谓词字面收口** | 正本已就位（`shared/alive.ts`，`analysis/utils.ts` 按原名转出），但**44 处字面仍散在 10 件的 SQL 串里** | 两处定义地的问题已消失（字面与正本**逐字相同**，故不是正确性问题）；批量替换要把每条 SQL 的拼串重排，风险大于收益。已登记进门的 `PENDING_SINGLE_SOURCE`（机器可读待办） |
| 2 | ~~BMR 算式归正本~~ | **本轮已做**（提交 `fc27cef1`）：算式收成 `analysis/utils.ts` 的 `mifflinStJeorBmr` 一处；三份拷贝（`goal/nutritionGoal.ts`／`analysis/review.ts`／`analysis/reportDocTracked.ts`）改调它，**各件自己的缺项回落原样保留**（`nutritionGoal` 的 身高175／体重70／年龄30／male、`review` 的 `getActivityFactor`、报告件的「四要素不全不出图」分支都不动）⇒ 产物不变 | 已做 |
| 3 | ~~健康速率阈值统一~~ | **本轮已做**（提交 `fc27cef1`）：结论是**两套阈值各有其用，不能合并**——`TARGET_RATE`（0.5–1.0）＝目标速率、`DEFICIT_RATE_TOLERANCE`（0.3–1.2）＝由缺口反推速率的**容差带**（`看热量缺口` 页口径行印的就是后者）。交付＝两处都进 `shared/healthRate.ts`、**各自具名并写清用处**，等于把「阈值到底有几套、各是什么」收成一处答案；合并会改用户可见阈值 | 已做（不合并＝结论） |
| 4 | **逐字抄件合并** | 未定位到对象（规格未具名；普查未见同名单/同值的重复小件） | 无判据即无验收，不猜着合并 |

**结论**：余下的实质缺口只有第 1 项（已登记、非正确性问题）与第 4 项（无对象）；第 2／3 项本轮已给出**可验收的收口**（BMR 一处定义 ＋ 速率阈值一处登记）。

## 二之三 · 批②（营养素推荐区间归一）已交付

**提交** `ef56cb48`；改动两处源码 ＋ 新建一处共用位：

| 件 | 动作 | 一句话 |
| --- | --- | --- |
| `src/shared/nutritionRange.ts` ＋1 | 新建 | 区间正本：`NUTRITION_RANGE`（蛋白 10-20／碳水 45-65／脂肪 20-35）＋ `nutritionRangeNote()`（上屏那句话）。零依赖 |
| `src/analysis/anomaly/diet.ts` | 改 | 删掉自写的那套孤儿数（15-30／40-60／20-35），判决与证据句改读正本；本件不再出现任何字面区间数 |
| `src/diet/nutritionPort.ts` | 改 | 本地 `RATIO_RANGE` 的数值删掉，改指正本（视图字段名与正本键名在此对齐） |

**两处渲染面不用改**：`render/trendMiscPortDocs.ts`（配比页上屏）与 `render/trendMiscPort.ts` 的区间都是从
`buildNutritionRatioView()` 的 `v.range` 读的，而 `v.range` 现在来自共用位——**没有第二处字面值可改**
（当刻实测：全包 `RATIO_RANGE` 只剩 `diet/nutritionPort.ts` 一处引用；两件渲染面零区间字面）。
这与规格「两处印它的渲染件改读它」的**效果**一致，路径更短。

### 用户可见读数变化（规格 §六 预告的那一处）

同一份种子库、同窗口（2026-09-01 ~ 09-07）：

| 读数 | 收口前 | 收口后 |
| --- | --- | --- |
| 配比页印的区间 | 蛋白 10%（83 克 / 7 天）／碳水 45%／脂肪 20% | **同左（一值未变）** |
| 诊断页证据句 | `（参考 蛋白15-30 碳水40-60 脂肪20-35）` | `（参考 蛋白10-20 碳水45-65 脂肪20-35）` |
| 诊断页判决 | `失衡维度：蛋白、碳水、脂肪`（三项全失衡） | `失衡维度：碳水、脂肪`（蛋白 14.7% 落进 10-20，不再判失衡） |

**一句话**：这一批只改诊断侧——配比页一个数未动，诊断页从「按孤儿数判三项失衡」改成「按正本判两项失衡」。
页面文案与版式一行未改。

### 判据与自证（三行机器读数）

- 判据件：`node --test packages/skill-calorie/test/t717-口径唯一.test.mjs` → `ℹ tests 5 / pass 5 / fail 0`
  （第 ④ 条由**红转绿**；`GATE-RUN runId=d4a0f1b2-3e0a-4781-b6e9-372f0f9b46ff`，见附录）
- 靶向回归（配比页／餐别页／分析验收）：`ℹ tests 39 / pass 39 / fail 0`（`GATE-RUN runId=3884bd2b-61e3-4e9b-a387-4b3b8a33cf9b`）
- 变异自证：`NUTRITION_RANGE.protein` 改回孤儿那套 `15-30` ⇒ 判据 ④ **必红**
  （`AssertionError: 配比页印出来的区间不是有出处那一套：{"蛋白":15,...}`）；逐文件取回 ＋ `tsc -b --force` 重编 ⇒ `5/5` **必绿**。
  （`--force` 是必需项：`tsc -b` 会把「内容改回原样」判成无需重出 `dist`，不加就回绿失败——同批①踩到的那条。）

## 二之二 · 判据收口件：`test/t717-口径唯一.test.mjs`（五条，红着的那一条就是下一步的账）

批① 的判据件已改名扩成**六处口径的交付面判据件**，每批一条，全部走真出口。立的当刻读数
（`GATE-RUN runId=8b0fbc75-f459-4255-82b7-b5c70af1c300`，见附录）：`tests 5 / pass 4 / fail 1`。

| 条 | 口径 | 立判据当刻 | 批② 之后 |
| --- | --- | --- | --- |
| ① | 批① 餐别两页同值 | 绿 | 绿 |
| ② | 批① 按窗删与归桶同一把尺子 | 绿 | 绿 |
| ③ | 批① 时间窗对外只有一份 | 绿 | 绿 |
| ④ | **批② 区间与判决同值** | **红（在册）** | **绿** |
| ⑤ | 批③ 落库日 ＝ 回执日 ＝ 累计读日 | 绿 | 绿 |
| ⑥ | 批④ 闭区间天数 | 未写 | 未写 |

**第 ④ 条的当刻读数（这就是批② 的账）**：

- 配比页（`calorie.view.nutrition-ratio`，7d）「推荐范围对比」表印的是 **蛋白 10%（83 克 / 7 天）／碳水 45%／脂肪 20%**；
- 「诊断营养不均衡(含均衡判断)」（`calorie.view.anomaly` ＋ `kind:"diet_unbalanced"`，同窗口、同一份库）证据句印的是
  **`（参考 蛋白15-30 碳水40-60 脂肪20-35）`**，并按这套数判 **`失衡维度：蛋白、碳水、脂肪`**（三项全失衡）。
- 两套数同时在盘上、且**用户可见**：同一份种子库上，一边说 10-45-20、一边说 15-30／40-60／20-35。

**第 ⑤ 条的当刻读数（批③ 的账已量清）**：把当刻钉到 `2026-08-21` 后——
落库 `{"date":"2026-08-21"}`、回执正文「已记一餐：探针-钉钟 **2026-08-21** 20:00:00（晚餐）…」、
`calorie.today`（`date:"今日"`）页上读得到刚记的那一条。**三条读数同一天，当刻没有分叉。**

> 这条读数修正了规格原先的一句判断：「把日期固定到某一天时……落进真实的那一天，而回执上印的是被固定的那一天」
> ——那是 `CALORIE_TODAY` 环境钉子时代的形状，**#676 把那颗钉子摘掉之后生产已不可达**（钉钟只剩进程级一种手法，
> 写入侧与读侧同时跟走；本探针两轮实测：`calorie.view.health` 窗口＝`2026-08-15 ~ 2026-08-21`、
> `calorie.today` 与 `calorie.diet.add` 都落在钉住日）。故批③ 当刻是**结构整改**
> （把 `todayISO`／`shiftISODate` 收进共用位、删三份本地时钟副本、谓词收一处），不是修一个活的缺陷。
> 规格 §六 预告的「三处会改变用户可见读数」核对结果：批① 一处（诊断餐次统计，已交付）、
> 批② 一处（诊断均衡判决，见上）、批③ 一处**当刻不成立**。

## 三 · 批① 的交付面判据（原 `t717-口径归一.test.mjs`，现并入上面的判据收口件）

三条判据都打在**用户真走的那条路**上（spawn 真命令行 `dist/cli/cmd_read.js`，或包对外的门），
不断实现细节、不断内部件名：

1. **两页同一份餐次统计**：同窗口、同库跑 `calorie.view.diet`（`meal:all`）与
   `calorie.view.anomaly`（`kind:diet_structure`），从两张**页产物**里读出四桶次数，逐一相等；
   另把三个分歧点的归属钉在明细表上（`14:30→加餐`／`21:30→晚餐`／`22:30→加餐`）。
2. **按窗删与归桶同一把尺子**：真出口跑 `calorie.diet.remove-by-type`（`mealType:夜宵`），
   断言删掉的那批行 **＝** 归桶判成夜宵的那批行（含 `22:30` 那条）。
3. **时间窗口径对外只有一份**：`dist/fetch/diet.js` 转出的 `MEAL_WINDOWS` 逐格核对
   （夜宵 `[22,30]`、加餐 `[14,18,22,30]`）＋ 五个边界两侧各一个 `inferMealType` 读数。

机器读数：`ℹ tests 3 / ℹ pass 3 / ℹ fail 0`（`runId=2c855064…` 那一次，命令里含本件）。

## 四 · 靶向回归（受影响面全绿）

| 套件 | 为什么点它 |
| --- | --- |
| `test/t273-review-meal.test.mjs` | 餐别分布页与复盘页的产物断言（走真出口） |
| `test/render-t8.test.mjs`／`render-t9.test.mjs` | 老口径断言：`MEAL_WINDOWS` 仍从 `dist/fetch/diet.js` 读得到、15 点＝下午茶 |
| `test/fetch.test.mjs` | 餐别推断的五条读数＋`xx→其他` |
| `test/t496-文案统一.test.mjs`／`test/diet-homogeneity-108.test.mjs` | 页面上不许再出现常量名／口径行的可见文案 |
| `test/t717-口径归一.test.mjs` | 本批新门 |

机器读数：`ℹ tests 108 / ℹ pass 108 / ℹ fail 0`（`runId=2c855064…`）。

## 五 · 变异自证（改坏必红 → 改回必绿）

| 步 | 动作 | 读数 |
| --- | --- | --- |
| 改坏 | `src/shared/meal.ts` 的 `午餐: [10, 14]` → `[10, 15]`（＝旧诊断那一套边界） | 编译 0；本门 `tests 3 / pass 1 / fail 2`（红） |
| 改回 | 逐文件取回原内容并编译 | **首次回绿失败**：`fail 2` |
| 归因 | `tsc -b` 判「内容没变」未重出 `dist/shared/meal.js`（实测产物里仍是 `午餐: [10, 15]`），随后回绿失败是**假红** | `dist` 读数：改回编译后仍为 `午餐: [10, 15]` |
| 回绿 | `tsc -b packages/skill-calorie --force` 重编 ＋ 复跑 | 本门 `tests 3 / pass 3 / fail 0`（绿） |

**这一条要传下去**：变异／回滚的「回绿」必须在**强制重编**下取证——本包 `dist` 是共享可变产物，
`tsc -b` 的增量判定会把「内容改回原样」判成无需重出，于是回绿读的是变异版的产物。
（同源判据见 `docs/subagent-concurrency-protocol.md` §5「回滚必须连生成物与生成记录一起复位」。）

## 六 · 收尾两绿（现成门）

| 门 | 命令 | 读数 |
| --- | --- | --- |
| 告警线台账 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | `RESULT: 93/93` ＋ `PASS: 告警线台账齐全且与实况一致`（`runId=c87cc691…`，exit 0） |
| 生成链 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `GEN-CHECK PASS：键 134（写 52 ＋ 读 82）；能力 10 个…未搬迁清单 0 条`（`runId=97efb845…`，exit 0） |

新建件 `src/shared/meal.ts` 落在线内（未越 350 行告警线），故台账无需 `--sync`；本批也没有改动任何已挂号件。

## 七 · 全包测试的当刻状态（**与本批无关的红，登记在此**）

`node --test "packages/skill-calorie/test/*.test.mjs"`（`runId=218a1a69…`）：`tests 1859 / pass 1819 / fail 40`。

- 40 条红**只落在非饮食域**（运动路由／回执族逐字节／目标进度 metrics／场景表条数等），
  且**没有一条**引用本批碰过的件（`shared/meal.ts`／`fetch/diet.ts`／`render/diet.ts`／
  `render/analysisPlate.ts`／`diet/review.ts`／`analysis/anomaly/common.ts`）。
- 本批碰过的面在同一轮的饮食域断言**全绿**（上一节 108 条＋各页真出口断言）。
- 该红**属当刻共享工作区里别席的在途产出**（工作区同时存在未提交的 `src/render/reviewDocs*.ts` →
  `src/workout/` 搬迁，即 #714 的窗）。独立佐证：`node --test test/calorie-routing-81.test.mjs`
  （`runId=9d866d7f…`）红的读数是「键 503 ≠ 494」与「路由表少两条拍营养表唤醒词」——都在
  触发词／键表侧，与本批六件无引用关系。
- 按 `docs/subagent-concurrency-protocol.md` §3.1「树上可能有别人的在途件」，本票不据此认定本批回归。

## 八 · 本批碰过的件（逐行对账）

| 件 | 动作 | 一句话 |
| --- | --- | --- |
| `src/shared/meal.ts` ＋1 | 新建 | 餐别正本：`MEAL_NAMES`／`MEAL_BUCKETS`／`MEAL_WINDOW`／`MEAL_WINDOWS`／`inferMealType`／`mealBucketOf`／`inMealWindow`，零依赖 |
| `src/fetch/diet.ts` | 改 | 删本地 `MEAL_WINDOWS`／`inferMealType`，按原导出名薄转出；`deleteMealsByType` 读正本（夜宵改跨零点） |
| `src/render/diet.ts` | 改 | 删本地 `assertMealWindows`／`bucketOf`／`MEAL_BUCKETS` 定义，改读共用位；两个对外名字薄转出 |
| `src/render/analysisPlate.ts` | 改 | 同上（复盘页按餐汇总那一支） |
| `src/diet/review.ts` | 改 | 删本地四桶与归桶副本，改读共用位（明细缺值仍写 `—`） |
| `src/analysis/anomaly/common.ts` | 改 | `mealStructure` 删自写小时边界，改走 `mealBucketOf`（对外仍是四桶：加餐 ＋ 夜宵 合为「加餐/夜宵」） |
| `test/t717-口径归一.test.mjs` ＋1 | 新建 | 本批三条判据（见第三节） |

**不碰的**：页面文案与版式、`package.json` 的 `exports`、包门 `src/index.ts` 的名单、公共层
（规格 §四 与「Out of Scope」两条）。

## 附 · GATE-RUN 对账（本票声明过的运行）

```
GATE-RUN runId=91a10092-9377-4596-8ce8-a3a0e467e460 cmd="node .scratch/t717/probe-meal.mjs"          # 收口前对照
GATE-RUN runId=b5ca47b0-a674-44ce-a4dc-d2298a6a90db cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=f5989e50-2483-492f-a7ab-6af65b81c857 cmd="node .scratch/t717/probe-meal.mjs"          # 收口后对照
GATE-RUN runId=bf1b2109-1557-4296-bd49-de0bd2755c0e cmd="node --test packages/skill-calorie/test/t273-review-meal.test.mjs packages/skill-calorie/test/render-t8.test.mjs packages/skill-calorie/test/render-t9.test.mjs packages/skill-calorie/test/t496-文案统一.test.mjs packages/skill-calorie/test/fetch.test.mjs packages/skill-calorie/test/diet-homogeneity-108.test.mjs"
GATE-RUN runId=8a2e7108-b166-45e4-afdd-5b9cdbe28602 cmd="node .scratch/t717/probe-cli.mjs"
GATE-RUN runId=2c855064-4e66-495f-9ee5-3d20dcf481d4 cmd="node --test packages/skill-calorie/test/t717-口径归一.test.mjs packages/skill-calorie/test/t273-review-meal.test.mjs packages/skill-calorie/test/render-t8.test.mjs packages/skill-calorie/test/render-t9.test.mjs packages/skill-calorie/test/fetch.test.mjs packages/skill-calorie/test/t496-文案统一.test.mjs packages/skill-calorie/test/diet-homogeneity-108.test.mjs"
GATE-RUN runId=5a23621c-830a-42e5-937f-3b6f7445c523 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"   # 回绿窗（--force 那一次及其后复跑经手工目录锁，见第五节）
GATE-RUN runId=c87cc691-65b8-4b16-a6a2-0ed995da30eb cmd="node packages/skill-calorie/scripts/check-warning-line.mjs"
GATE-RUN runId=97efb845-f802-4e5c-87a6-c8572aad895b cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"
GATE-RUN runId=218a1a69-83a2-4ccc-83af-2aa82ce2d8fa cmd="node --test packages/skill-calorie/test/*.test.mjs"                # 当刻全包（40 红，见第七节）
GATE-RUN runId=9d866d7f-ed7c-4fe0-accb-025f265fb0de cmd="node --test test/calorie-routing-81.test.mjs"                      # 别席在途产出的佐证
GATE-RUN runId=f9b997ac-2aed-4086-98b6-9c5ad7eb9f65 cmd="node .scratch/t717/probe-criteria.mjs"                                          # 第 0／1 步侦察（批②／批③ 当刻读数）
GATE-RUN runId=a4a70ab4-22f6-484c-a192-23143f4c15a4 cmd="node .scratch/t717/probe-clock-fork.mjs"                                       # 钉钟两态对照
GATE-RUN runId=dca1e203-066a-4ffc-a94b-c1185a070f2e cmd="node .scratch/t717/probe-range.mjs"                                            # 区间两套的逐字取证
GATE-RUN runId=5279b225-64b2-4590-afb7-f463b25aee95 cmd="node --test packages/skill-calorie/test/t717-口径唯一.test.mjs"               # 首跑（两条探针口径之误，见下）
GATE-RUN runId=8b0fbc75-f459-4255-82b7-b5c70af1c300 cmd="node --test packages/skill-calorie/test/t717-口径唯一.test.mjs"               # 收口读数 5/4/1（④ 红在册）
```

**改判据时踩到的两处（记下来免得后人重踩）**：
① 配比页「推荐范围对比」表的上方还有一段说明文字（「总摄入 4239 卡 共 7 天…」，里面同样有数字与「天」），
按固定字符窗口截取会读到说明段而不是那张表——窗口要给足（本件用 900 字符），且判据只看**表行**的形状；
② 写命令回执的**落盘文件名里没有日期**（是命令名 ＋ 当刻时分秒），要读「回执印的是哪一天」必须读**回执正文**
（`data.message`）。两条都是探针自己的错，产品一侧无异常。

对账源＝`.scratch/locks/gate-runs.log`（未受版本控制）里 `ticket=717` 的 START／RUN 行，逐条抄录如上。
未声明、也未列在此处的运行（编译尝试、探针调试等）不计入门禁证据。
