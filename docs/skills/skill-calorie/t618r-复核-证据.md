# #618 复核证据 · 条目列表页趋势卡改中文判语（复核席独立重跑）

票面：GitHub Issue #618（Part of #155）· 场景 02 验收缺陷④。用户原话：「17 看本周饮食、18 看上周饮食 的
趋势这个卡片写的是『up/down』不应该有英文」。

被复核提交：`8f016d5`（改动件 ＋ 回归断言 ＋ 真出口探针）／`ba11140`（证据件 `t618-证据.md`）。
复核席：只读产品代码与他人测试，自设探针 ＋ 变异确认；全部命令经 `node tooling/run-locked.mjs --ticket 618 --` 排队。

## ① 结论

**PASS（96 分）**：票面全部验收命令独立重跑逐条绿；被审证据件的每一行读数都能重现（含 8 个 runId 在
`gate-runs.log` 里逐条对账命中）；自设探针把页数从 8 扩到 15、抽取器换成另一套实现，仍 15/15 绿；
变异确认「改坏→两处红→还原→全绿→文件逐字节还原」成立；新旧产物逐字节对照证明修复足迹**只在趋势卡值位**。

| 维度 | 满分 | 得分 | 扣分理由 |
|---|---|---|---|
| 契约一致 | 30 | 29 | 值改「上升／下降／持平」＝票面点名口径，枚举未动、映射只此一处、定义位置给出、8 张窗口页真出口绿；扣 1：证据件「遗留出口」措辞读起来像还有一页用户看得到英文（见 S3-1），实际那一支在当刻构建里没有运行期调用点。 |
| 证据真实 | 25 | 24 | 探针逐页读数、测试计数、行数口径、8 个 runId 全部重现；扣 1：证据件表格的行序与 `gate-runs.log` 实际时序不一致（S3-2），另「改前」逐页原文只对上了 3/8 直接观测、其余 5 条由种子与窗口规则独立推导对上（推得通，但没直接看到）。 |
| 新旧对照 | 20 | 20 | 15 页产物逐字节对照：9 张列表页差异只落趋势卡值位（旧 `up`／`down`／`flat` → 新 `上升`／`下降`／`持平`），6 张无卡页只差复制载荷里的生成时刻，卡外差异 0。 |
| 工程红线 | 15 | 14 | 提交只含写集内件（3 件／1 件）、无危险 git 动作、测试只增不削、探针只写 tmp 与 `.scratch/`；扣 1：为不越告警线，同票把本件既有 `fmt`／`r1` 两个多行函数压成一行等价写法（写集内、产品出口逐字节可证不变，但属票面没要求过的形状改动）。 |
| 文档同步 | 10 | 9 | 证据件四段齐、映射定义位置与测试尾 5 行都在；扣 1：与 S3-1 同源的措辞精度（同一条不在两处重复扣，此处按文档面记 1）。 |

**S 清单：S1 无 · S2 无 · 3 处 S3（均不翻案）**

- **S3-1｜「遗留出口」把老渲染器说成了活缺陷**：证据件 ④ 点名 `src/render/html.ts:83` 的 `kpi('趋势', o.trend.summary.trend…)`
  「印同一张卡、同一枚举」。代码事实对：该行确实在。但自设探针的静态契约③读数
  （`READING #618r 静态契约③ …在 cmd_read 导入闭包内=是（同件还有 renderPlanHtml／renderErrorHtml 等在用的函数）；
  三个 render*Html 的运行期调用点=闭包内无；测试件直调=render-t8.test.mjs`）说明：`html.ts` 进闭包是因为
  同件的 `renderPlanHtml`／`renderErrorHtml`／`renderHelpLookupHtml` 在用，而 `renderDietHtml`／`renderGoalHtml`／
  `renderExerciseHtml` 在 `src/**` 里**没有调用点**，只有 `test/render-t8.test.mjs` 直调 ⇒ 用户看不到那一支。
  「只点名不代修」的处置与票面一致（票面本就要求点名同类枚举），只是注记该补一句「当前无唤醒词到达」。
- **S3-2｜证据件表格行序与日志时序不一致**：`gate-runs.log` 实际是 06:14 变异三跑 → 06:17 还原后三跑；
  证据件②表把「改后」三行排在变异三行之前。引用到的 runId、命令、退出码逐条对得上，读数不受影响。
- **S3-3｜同票压行换线内**：见上表工程红线扣 1。台账（`packages/skill-calorie/AGENTS.md`）本票未碰；门读数
  `RESULT: 91/91 PASS` 说明台账与实况一致（该件 350 LF，判据是 `LF > 350` 才越线）。**下一位若再往
  `src/render/dietDocs.ts` 加行即越线，需走编排者派单动台账。**

## ② 独立重跑（不采信实施者结论）

| 命令 | GATE-RUN（本席本次） | RESULT | 退出码 |
|---|---|---|---|
| `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` | `ae6cd857-6bf9-4ded-b6c9-a459be3211a3` | 编译通过 | 0 |
| 实施席真出口探针 `docs/skills/skill-calorie/t618-真出口断言.mjs` | `7ff3335a-2e8b-4c57-a079-5c1121d866ce` | `RESULT #618 窗口页 8/8 绿（裸英文 0 且趋势卡是中文判语）` ＋ `PASS` | 0 |
| 三件测试 `node --test t271-总览接线与两态 diet-list-t271 diet-homogeneity-108` | `81b12be6-9e30-4473-91e8-d9682759112f` | `ℹ tests 28`／`ℹ pass 28`／`ℹ fail 0` | 0 |
| 告警线门 `node packages/skill-calorie/scripts/check-warning-line.mjs` | `de937be8-c67b-4578-bfe5-67ed36f6d359` | `RESULT: 91/91` ＋ `PASS: 告警线台账齐全且与实况一致` | 0 |
| 自设探针（首次全绿基线） | `c9132f4d-1a95-4d89-b3f3-5f0a3567b5d5` | `RESULT #618r 真出口页 15/15 绿` ＋ `PASS` | 0 |
| 变异循环（改坏→红→还原→绿→对照） | `67c6a16f-389e-40c5-9e4c-60e13bda628e` | 见 ④ | 0（循环判决 PASS，见下） |
| 自设探针（脚本定稿后复跑） | `3e7e9e9f-d5ce-4811-bfbe-2a3ae3490ca6` | `15/15 绿` ＋ `PASS` | 0 |
| 新旧对照 | `2e50cbbd-9974-4299-a4ea-b4d78c4f9b9e` | `卡值差异=9（列表页） 只载荷差异=6 卡外也有差异=0` ＋ `PASS` | 0 |

**改后绿（本席独立重跑，逐页读数）**：`看昨日饮食=持平`、`看本周饮食=持平`、`看上周饮食=下降`、`看本月饮食=上升`、
`看上月饮食=下降`、`看最近 7 天饮食=上升`、`看最近 30 天饮食=上升`、`看某段时间饮食=上升`，逐页 `裸英文=0`；
`三档判语命中＝上升／下降／持平（共 3 档）` —— 与证据件所载逐字一致。

**改前红（本席变异复现，等价改前那份「值位直印枚举」）**：实施席探针
`RESULT #618 窗口页 0/8 绿（裸英文 0 且趋势卡是中文判语）`，逐页 `趋势卡="up|down|flat"` 且 `裸英文=1`；
证据件所载「8 页原文 `flat / flat / down / up / down / up / up / up`」经本席核对：末三页 `up／up／up` 直接观测一致，
其余五条按证据件的种子（`08-01 500／08-15 600／08-31 400／09-01 600／09-06 300／09-07 800`，`CALORIE_TODAY=2026-09-07`）
与窗口规则（`本月`＝月初..今日、`上周`＝上周一..周日）独立推导：昨日 n=1⇒flat、本周 n=1⇒flat、上周 400→300⇒down、
本月 600→800⇒up、上月 500→400⇒down、7d 600→800⇒up、30d 首日空按 0→800⇒up、自定窗 600→800⇒up —— 八条全对上。

**runId 对账（证据件点名 8 个，全命中 `gate-runs.log` 且命令／退出码一致）**：
`d389a944…` tsc exit 0｜`39c1a3bd…` 探针 exit 1（改前红）｜`d8cb3c03…` tsc exit 0｜`8464c3f8…` 探针 exit 0（改后绿）｜
`360850e0…` 三件测试 exit 0｜`065d2bfb…` tsc exit 0（变异）｜`7556e07c…` 探针 exit 1（变异红）｜`79738c56…` diet-list-t271 exit 1（变异红）。

## ③ 自设探针（`.scratch/t618r/` 起稿、入仓件 `docs/skills/skill-calorie/t618r-独立探针.mjs`）

被审脚本覆盖不到的三件事，本席另起一件（不共用它的词表、抽取器、种子、锚点）：

1. **页数超集**：不写死词表，从 `src/**/routes.ts`（路由**源件**）逐行枚举全部 `key: 'calorie.view.diet'` 的
   唤醒词＝**15 条**（10 条窗口／总览词 ＋ 5 条餐别词），逐条走真 CLI；被审脚本只跑写死的 8 条。
2. **独立抽取**：自带可见文本抽取（剔注释／head／style／script／template → 削标签 → 解实体），不走仓内
   `test/visible-text-probe.mjs` ⇒ 「绿」不是同一个抽取器的自证。
3. **静态契约三查**：① 数据层声明域 `['up','down','flat']` 与页面侧映射键逐档对齐（缺档=无，定义处
   `packages/skill-calorie/src/render/dietDocs.ts:67`）；② 同族导入闭包 57 件里中文判语表**只此一份**；
   ③ 遗留出口的可达性（见 S3-1）。

读数：`RESULT #618r 真出口页 15/15 绿（独立抽取：可见文本裸英文 0 且趋势卡中文）`；`三档判语命中＝上升／下降／持平`；
无卡页 6（总览 1 ＋ 餐别 5，按参数位 `entry`／`meal` 判定，不写死词表）。**第 9 张同族页 `看今日饮食概览`
（窗口＝今日）也在同一处映射下转中文** —— 票面点名的 8 张之外，同族的其余页一并绿。

## ④ 变异确认与新旧对照

- 变异点：`TREND_ZH` 三档值换成英文键（`up:'up', down:'down', flat:'flat'`），等价改前那份
  `value: o.trend.summary.trend`；改坏一处，sha `1c2cd5ac5fe40dd6` → `90a6a8e2b8b5fca6`。
- **改坏（红，两行读数）**：编译 exit 0（静默行为改动，编译拦不住）｜实施席探针 `窗口页 0/8 绿`＋`FAIL` exit 1｜
  三件测试 `tests 28 / pass 27 / fail 1` exit 1｜自设探针 `真出口页 6/15 绿`＋`FAIL #618r 问题 12 条` exit 1。
- **还原（绿，两行读数）**：落盘原文逐字节写回，sha 复为 `1c2cd5ac5fe40dd6`，`git diff` 该件为空｜编译 exit 0｜
  实施席探针 `窗口页 8/8 绿`＋`PASS` exit 0｜三件测试 `tests 28 / pass 28 / fail 0` exit 0｜自设探针 `15/15 绿`＋`PASS` exit 0。
- **新旧对照（字节级）**：`RESULT #618r 新旧对照 页数=15 卡值差异=9（列表页） 只载荷差异=6（无卡页，生成时刻所致）
  逐字节相同=0 卡外也有差异=0`；逐页 `趋势卡值 旧="up"→新="上升"` 等（`看上周／看上月／看最近 7 天`＝up→上升、
  `看本周／看最近 30 天`＝down→下降、`看昨日／看本月／看某段／看今日饮食概览`＝flat→持平），
  「页面其余部分 ＋ 复制载荷归一后逐字节相同」⇒ `fmt`／`r1` 压行在产品出口上**零差异**。

## ⑤ 逐条路径与提交

- 改动件（被审）：`packages/skill-calorie/src/render/dietDocs.ts`（`:67` 映射、`:165` 值位、`:60-63` 压行＋注释）。
- 回归断言（被审）：`packages/skill-calorie/test/diet-list-t271.test.mjs`（`:203-224`，三档枚举逐档钉住，只增不删）。
- 真出口探针（被审）：`docs/skills/skill-calorie/t618-真出口断言.mjs`（写死 8 词，产物只落系统 tmp）。
- 证据件（被审）：`docs/skills/skill-calorie/t618-证据.md`。
- 本席件：`docs/skills/skill-calorie/t618r-独立探针.mjs`（自设探针，`run`／`compare` 两模式）、本文件；
  日志与产物（未入仓，`.scratch/` 已被 `.gitignore:5` 盖住）：`.scratch/t618r/log-r1..r8-*.txt`、`.scratch/t618r/out-old/`、`out-new/`、
  `.scratch/t618r/变异循环.mjs`、`.scratch/t618r/还原.mjs`、`.scratch/t618r/dietDocs.ts.bak`。

## ⑥ 没做完 / 下一手

- **验收墙重出与复拍归 #619**（票面口径），本席只交复核读数，不代出墙。
- **本席过程记事（与验收物无关，如实记）**：第一轮变异循环脚本的读数打印辅助函数写错（把字符串当对象取字段），
  崩在「变异已落盘、还原未执行」之间——源码与 `dist` 被改坏约 1 分钟。处置：立刻用 `git show HEAD:<件>` 取 Buffer
  逐字节写回（sha 复原一致、`git diff` 为空），按同一条命令重编译 exit 0；随后修脚本（异常路径也走还原 ＋ 落盘备份）
  重跑整轮，判决 `RESULT #618r 循环 变异红=是（编译0／我探针1／实施席探针1／测试1） 还原绿=是（编译0／我探针0／
  实施席探针0／测试0／对照0） 文件还原=是`。锁内跑完的规范要求由此得到验证：变异窗口必须与还原同处一个持锁窗口。
- **留给下一手的两条**：① 若要收「同类枚举」的尾，`html.ts` 那三支老渲染器当前无唤醒词到达，动它的收益要先确认
  它是不是还要留（属结构/清账票，不在本票写集）；② `src/render/dietDocs.ts` 已在线上（350 LF），再加 3 行即越线，
  先走编排者派单动 `packages/skill-calorie/AGENTS.md` 台账。
