# #267 · 场景 04 运动：真出口锁（证据）

> 票：MAP #156 · #267「锁 · 真出口用例」。**只加断言**：不改被测实现、不写视觉脚本、不写手写计数、
> 完整文档断言不另起第二份。
> 交付：`packages/skill-calorie/test/exercise-accept-267.test.mjs`（新）＋ 本文（机器读数存档）。
> 结构落点：`packages/skill-calorie/test/` 不在结构纪律管辖范围内（`structure.md` 的「管辖」一节）；
> 本文与本票的判据件都**不在** `check-warning-line.mjs` 的扫描面内（扫描面＝`src/**/*.ts` ＋ `scripts/**/*.mjs`）。

## 一 · 交付物

| 件 | 说明 |
|---|---|
| `packages/skill-calorie/test/exercise-accept-267.test.mjs` | 新。六组断言（G0 范围分母／G1 唤醒词起点／G2 两处定义地／G3 页面归属／G4 阻断／G5 回归灵敏度／G6 产物形态）＋ 真库只读自守；带断言级变异开关 `T267_MUT_PAGE` |
| 本文 | 机器读数：判据先行／六组读数／自证两行／真库对账／告警线与探针／遗留出口 |

**完整文档断言只读复用** `test/doc-page-assert.mjs::assertDocPage`（#264 建，唯一定义地）；
本票不写第二份五连，既有三份（`profile-receipt-175`／`wizard-86`／`profile-doc-179`）一行未动。

范围条数**一律派生**：词＝冻结表 `SCENE_04_EXERCISE` 条数；键＝由各词 `main_prompt.cli` 派生；
读类词＝由**模板件名** `html_template` 派生（分布／趋势／复盘三件）；写类词＝由 `EXERCISE_COMMANDS` 的
`kind === 'write'` 键派生；声明面＝`exercise/routes.ts` ＋ `home/routes.ts` 两件（记录住键所属能力）。
判据里没有一处手写计数。

## 二 · 判据先行（开工前实测）

**票面括号里那句「今天实测 38 条一致、1 条 `non-exec`」是开票日读数**，出处是
`docs/skills/skill-calorie/t167-重新设计-新底座.md:103`——那时唯一例外是 `看运动记录（有备注）`
拿到 `看带备注的运动记录`（非命令形态）。#342 已把它改指真命令
（`calorie.view.exercise-records --params '{"window":"7d","hasNote":true}'`）。**当刻实测：39 条全部逐字相同、0 条 `non-exec`。**
本票的判据不写死这个数——它断言「逐条逐字相同 ＋ 场景 04 无 `non-exec`」，漂了即红。

开工前的形状实测（决定第二组怎么读）：

| 事实 | 读数 |
|---|---|
| 冻结表条数 | 39 |
| `exercise/routes.ts` | 35 条（29 条 `wake` ＋ 6 条 `new` 别名） |
| `home/routes.ts` 里带 `scene: '04'` 的记录 | 10 条（`calorie.view.exercise` 的词——**那个键的场景分区是 01 主页**，依据 `t313a-分区-证据.md`） |
| 场景 04 的声明记录合计（生成物面） | 45 条，`non-exec` **0** 条 |
| 39 条词的 `lookupWake` 命中 | 39/39 命中 1 条，且命令原文＝冻结表原文（#342 之后） |

→ 第二组**不能只盯 `exercise/routes.ts` 一件**：记录按「键属谁」分住两件能力声明，判据两件都读，
并按 `scene === '04' && list === 'wake'` 与冻结表**双向**对账（缺词、孤儿、重复都红）。

## 三 · 验收命令与机器读数

验收串（票面三段 ＋ 真库对账 ＋ 告警线）：

```
node tooling/run-locked.mjs --ticket 267 -- node .scratch/t267/accept.mjs
```

| 步 | 命令 | exit |
|---|---|---|
| ① 真库指纹（跑前） | `node .scratch/t267/fingerprint.mjs before` | 0 |
| ② 构建 | `pnpm build` | 0 |
| ③ 本票判据 | `node packages/skill-calorie/test/exercise-accept-267.test.mjs` | 0 |
| ④ 自治探针 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | 0 |
| ⑤ 告警线门 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | 0 |
| ⑥ 真库指纹（跑后） | `node .scratch/t267/fingerprint.mjs after` | 0 |

**`GATE-RUN runId=676354c3-2b08-4172-87c9-2c28636f44ac`**（`RESULT: ticket=267 runId=676354c3-2b08-4172-87c9-2c28636f44ac waitedMs=0 exit=0`；
六步 `ACCEPT-SUMMARY finger-before=0 pnpm-build=0 test-267=0 t293=0 warning-line=0 finger-after=0 real-db-unchanged=YES`）。
更早一轮同样六步全 0 的串：`runId=99f065f9-5ef5-4b52-ad54-f4b6d5a36318`（在自证变异之前跑的那一轮）。

六组读数（判据自打的 `T267-*` 行，逐字）：

```
T267-G0 词=39 命令键=11 页族针=11 读类词(分布/趋势/复盘)=7 写类词=13 声明面=exercise/routes.ts(35)＋home/routes.ts(41)
T267-G1 词=39 exit0=39 绝对路径=39 字节如实=39 完整文档=39 落盘字节域=[65200,97454]
T267-G2 冻结词=39 逐字相同=39 non-exec=0 生成物一致=39 键数=11 分布=1 趋势=1 复盘=5
T267-G3 读类词=7（分布=1 趋势=1 复盘=5） 各拿自己那页=7 页轴两处对账=7 产物零命令键=7 汇总族对照=10 载荷头仍印命令键=12（见遗留出口）
T267-G4 空库阻断=32 空库新增出账=7 落盘文件数=7 阻断后落盘=0
T267-G5 眉标族长=9 产物逐条对族=39 键→模板件=(crud_receipt.html×3 / exercise_summary.html×2 / exercise_goal_view.html / exercise_distribution.html / exercise_strength.html / exercise_cardio.html / exercise_trend.html / exercise_recap.html) 读类三件模板=3 登记共用件=1
T267-G6 写类词=13 完整文档=13 字节域=[66328,73916]
T267-G7 夹具根=<系统临时根> 真库SKILLS_DB_PATH=D:\2Study\StudyNotes\.db 真库零接触=是
T267-RESULT: 8/8 通过 exit=0
```

逐组要点：

- **第一组（最关键）**：逐条从**用户说的那句话**出发——`lookupWake(buildHelpLookup(TRIGGERS), 词)` 拿命令，
  与冻结表逐字对账、与 `routesFor(词)` 给出的命令对账、与模块级预建 `HELP_LOOKUP` 对账；
  **命令原样跑**（不补 `--html`）→ `exit 0`、`data.output` 绝对路径且落点 `calorie_html/`、
  `delivery.bytes` ＝ `delivery.path` 指向的文件字节 ＝ 读回字节、包络形状与命令声明的 `kind` 一致、
  产物过 `assertDocPage` 五连；满种子库下 **39/39 全绿**（产物 65.2–97.5 KB）。
- **第二组**：39 条冻结词在两件声明面各恰 1 条记录、`kind === 'exec'`、`cli` 与冻结
  `main_prompt.cli`／`data_source` 逐字相同；`scene 04 && list wake` 的声明词集与冻结表词集**相等**；
  场景 04 声明记录**没有 `non-exec`**；生成物 `routes.generated.ts` 对同样 39 条逐字一致、条数相等。
- **第三组**：读类 7 条（由**模板件名**派生：`exercise_distribution.html` 1 条／`exercise_trend.html` 1 条／
  `exercise_recap.html` 5 条）——① **页轴两处定义地对账**：模板件名说的页 ＝ 命令键说的页；
  ② 产物眉标／题面／载荷头三处都指得出**自己那一页**；③ 三处都不是「运动总览」（眉标不是「运动 · 汇总」、
  题面不以「运动汇总」起、载荷头不是 `【calorie · calorie.view.exercise】`）；④ 产物**零命令键**；
  另设对照面：汇总族的 10 条词**确实**拿的是汇总页（证明「不是总览」不是空判）。
- **第四组**：空库 39 条按**派生规则**分两档——`calorie.exercise.add` 且非 `copyFrom` 的 7 条（真新增）
  如实出账（`exit 0`、完整文档、可见文本逐字印出输入的消耗／时长／类型／备注，**不编默认值**）；
  其余 **32 条一律阻断**：`exit ≠ 0`、stderr 点名「取数失败（缺失阻断）／不存在」、stdout 无 `data.output`、
  该词目录**零落盘**。两档之和＝冻结表条数，空库落盘文件数＝7 ＝ 新增条数。
- **第五组**：产物面 39 条逐条对页族（眉标 9 族），且各族的产物数＝同键词数；
  声明面：同键的词必须点名**同一件**模板件名；**一件模板只许服务一个页族**（登记在册的共用件
  `exercise_summary.html` 承载「汇总＋记录级明细」两页，是唯一例外）；读类三件模板都不是汇总页那件。
- **第六组**：写类 13 词（3 个写键）产物全是完整文档：doctype 起、`</html>` 收、`<title>` 非空、
  `ilife-page`、无残留标记（`assertDocPage`）、`复制数据` ＋ 三格式菜单 `text／json／csv` 逐项对账、
  长度 > 10000 字符（当刻 66.3–73.9 KB）——**不得回退成片段**。

## 四 · 自证两行（变异四步序）

四步序：记原字节 → 变异 → `pnpm build` → 跑判据 → **还原原字节并刷 mtime** → `pnpm build` → 跑判据。
驱动在 `.scratch/t267/mutate.mjs`（**不进提交**：`.scratch/` 在 `.gitignore` 里）；结束时逐件比对 sha256。

| 轮 | 变异（件与位置） | 轮次 runId | 变异后 exit | 变异后读数 | 还原后 exit | 还原后读数 | 还原校验 |
|---|---|---|---|---|---|---|---|
| **自证第 1 行 · 任一条词的命令改回总览页** | `看运动趋势` 的命令键改回 `calorie.view.exercise`：冻结表 `main_prompt.cli`＋`data_source` 两处、`exercise/routes.ts` 的 `key`＋`cli` 两处（同批改） | `d4d49406-0172-4b25-936b-40641a18e868` | **1** | `T267-RESULT: 4/8 通过（红：G1；G2；G3；G5）` | **0** | `T267-RESULT: 8/8 通过` | 三件 sha256 与原字节 **SAME**（`scene-04-exercise.ts` `6a947ee11770`／`exercise/routes.ts` `943c1e35d496`／`routes.generated.ts` `bcb16fa36ab8`） |
| **自证第 2 行 · 换掉一处模板件名** | 冻结表 `看运动类型分布` 的 `html_template`：`templates/exercise_distribution.html` → `templates/exercise_recap.html` | `6ec3a24b-baa3-4b27-8d1f-fbeb5ee36c5b` | **1** | `T267-RESULT: 6/8 通过（红：G3；G5）` | **0** | `T267-RESULT: 8/8 通过` | 同上三件 **SAME** |
| **断言级自证** | 不改实现，只把首条读类词的**期望页身份**换成另一页（`T267_MUT_PAGE=1`） | 无需持锁、不开 build | **1** | 红：G3、G5 | **0** | 原样复跑 `8/8 通过` | 三件 **SAME** |

变异后的**断言原话**（红因，逐字）：

- 第 1 行（命令改回总览页）：
  - `AssertionError: 看运动趋势 查找与路由层给的命令不一致` （第一组）
  - `AssertionError: 看运动趋势 生成物 cli 与冻结表不一致` （第二组）
  - `AssertionError: 看运动趋势 两处定义地（模板件名 vs 命令键）对不上页：模板说「运动 · 趋势」，命令键 calorie.view.exercise 说「运动 · 汇总」` （**第三组**）
  - `AssertionError: 同键的词点了两件不同模板：calorie.view.exercise → ["templates/exercise_summary.html","templates/exercise_trend.html"]` （第五组）
- 第 2 行（换掉一处模板件名）：
  - `AssertionError: 看运动类型分布 两处定义地（模板件名 vs 命令键）对不上页：模板说「运动 · 复盘」，命令键 calorie.view.exercise-distribution 说「运动 · 类型分布」` （**第三组**）
  - `AssertionError: 一件模板被两个页族共用（未登记）：templates/exercise_recap.html → ["运动 · 类型分布","运动 · 复盘"]` （第五组）

**一次作废的轮次（归因，不算本票红）**：`runId=65044a24-4cea-4872-bd21-6e76662a6c25`（route）与
`runId=325cdac6-8653-41ff-a035-d1612a640285`（template）在**变异后的 `pnpm build` 上 exit=2**，
编译错是**他席在途件**：`packages/skill-calorie/src/photo/galleryDoc.ts(128,14) TS2304: Cannot find name 'budgetBannerHtml'`
与 `(137,12) TS2554: Expected 4 arguments, but got 3`——与 #267 一行无关（他席随后修好，重跑即通）。
驱动按序**当场中止**并还原到 **SAME**，没有留下半变异的工作区。

## 五 · 真库只读对账

真库＝`SKILLS_DB_PATH` 指的那一份（`D:\2Study\StudyNotes\.db\calorie_data.db`）。
本票全部跑动都在**系统临时根**的夹具目录里（判据的 G7 自守：夹具目录必在 `tmpdir()` 下、且不得是真库或落其内）。

| 读数 | 跑前 | 跑后 | 相同 |
|---|---|---|---|
| 文件 sha256 | `57ce9d87f5f232822332d7057c2ae8411d99ba7fdecea289f2028e19fc3ffc15` | 同左 | **是** |
| 文件字节 | 3072000 | 3072000 | 是 |
| 文件 mtime | `2026-09-14T05:53:37.729Z` | 同左 | 是（早于本席进场时间，本票从未触真库） |
| `PRAGMA integrity_check` | `{"integrity_check":"ok"}` | 同左 | 是 |
| 表数 | 11 | 11 | 是 |
| 逐表行数 | `body_composition=0 body_measurements=1 body_photos=22 daily_goal=1 exercise_log=8297 food_log=562 nutrition_products=1931 user_profile=1 weight_log=129 workout_plan_config=1 workout_plans=100` | 同左 | **是（逐表相同）** |

## 六 · 告警线门与自治探针

- **告警线**（`node packages/skill-calorie/scripts/check-warning-line.mjs`）：`exit 0`、
  `RESULT: 58/58`、`PASS: 告警线台账齐全且与实况一致`。扫描面 285 件、超线 25 件、剔出生成物 3 件、
  挂号台账命中 2/2。**本票没有新增／删除扫描面内的件**（判据件住 `test/`，在扫描面外），故台账不动、无需 `--sync`。
- **自治探针**（`node docs/skills/skill-calorie/t293-验收-命令自治.mjs`）：`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`。
  **P1 的 `UNACCOUNTED` 为空**（③＝0 处）；④ 里当刻只有 1 行（域内量形状），形状守卫命中 0 行（无登记样名、
  无「裸标识符比较裸数字」）⇒ 手写面全部落在 ① ② ④，**没有没被认下的面**。

## 七 · 遗留出口（当场记账，不当判据压红）

1. **两页载荷头仍印命令键**：`calorie.view.exercise`（汇总 10 条）与 `calorie.view.exercise-goal`（目标 2 条）
   的复制载荷头是 `【calorie · calorie.view.exercise】`／`【calorie · calorie.view.exercise-goal】`，
   不是人话页名——#465 的「可见文本零 snake_case」只落到本票那 7 条读类词。这两页是 **#452 地盘**，
   本票只记账、不判红（判据里以 `COPY_KEY_LEAK` 显式登记，并在读数行里点名 12 条）。
2. **`html_template` 无运行时消费者**：冻结表里 39 条的模板件名是**页身份声明**，但 `grep '\.html_template' src → 0 处`
   ——页由命令键选件，模板件名只是 parity 字段。本票把「模板件名 ↔ 命令键」的页轴对账钉在第三／五组；
   要不要给它一个真消费者、或删字段，归底座线（不属本票写集）。
3. **两条目标词产物逐字节相同**（`看今日运动（vs 目标）` 与 `看本周运动（vs 目标）`，65200 B）：
   **不是缺陷**——`CALORIE_TODAY=2026-09-07` 是周一，当天 `本周 ＝ 今日`。记账，免得后人当 bug 修。
4. 既有三份五连（`profile-receipt-175`／`wizard-86`／`profile-doc-179`）仍在各自文件里重复——
   **本票不动**，留给底座那条线收口（票面已划线）。

## 八 · 未做项与下一手（#268 的输入）

- **未做视觉判定**（判定三档只守前两档：文件存在 ＋ 内容字段正确）——视觉是票 6（用户肉眼）。
  可直接喂给 #268 的机器读数：① 39 条词 → 11 命令键 / 9 页族眉标；② 空库 32 阻断 / 7 新增出账；
  ③ 写类 13 词产物 66.3–73.9 KB；④ 读类 7 词产物**零命令键**；⑤ 全 39 条产物 65.2–97.5 KB。
- **未做**：把跑动驱动（`.scratch/t267/{mutate,accept,fingerprint,probe*}.mjs` 与各 `*.log`）入库——
  按「判据进 `test/`、跑动留 `.scratch/`」的口径，**不进提交**（`.scratch/` 已被 `.gitignore` 忽略）。
- **未做视觉**、**未动别席件**：本票提交只含「判据件 ＋ 本文」两条路径（走 pathspec），`pnpm build` 与
  `add`／`commit`／`push` 全在 `tooling/run-locked.mjs` 内。
- **跑法两条都验过**：票面那条 `node <判据件>`（本票全部读数出自这一条）之外，
  本件在仓内全量扫法 `node --test packages/skill-calorie/test/exercise-accept-267.test.mjs` 下同样 `8/8 通过`。
- **一条并发环境的坑（写下来给后人）**：不持锁跑判据时，判据 spawn 的 CLI 子进程可能撞上**他席正在重写**的
  构建产物（实测一次：`dist/photo/pickerDoc.js` 读到 `base-paint/blocks` 半写的导出面 ⇒ CLI 全体 `exit 1`，
  判据当场红 5 组）。**归因不是本票的锅**：同一命令在持锁内与几秒后重跑均 `8/8`。
  ⇒ 验收一律在 `run-locked` 内跑，别拿灯下那一次读数下结论。
- 下一手（#268）：拿本文第三／四节的读数逐页对样张即可；若样张与机器读数冲突，
  先看第三组四条轴（模板件名／命令键／眉标／载荷头）哪一条先断。
