# #453 独立对抗审查报告（分布／力量／有氧三页融合）

**判定：PASS**（无 S1；五维 29／24／19／14／9 ＝ 95／100，均分 95 ≥ 85）

审查对象：`7d4bd7f`（实现 `packages/skill-calorie/src/render/sportPortDocs.ts` 531→702 LF 的三支 builder、判据 `packages/skill-calorie/test/exercise-dist-strength-cardio-fusion-453.test.mjs` 416 行、证据 `docs/skills/skill-calorie/t453-分布力量有氧族融合.md` 101 行、台账 `packages/skill-calorie/AGENTS.md` 1 行）＋ `d7990e2`（证据补记）。
本席只读：被审四件、`gh issue view 453`、`src/triggers/scene-04-exercise.ts`、`test/doc-page-assert.mjs`、`src/exercise/categoryColors.ts`、`AGENTS.md` 台账、`base-paint` 的 `renderDataTable`、实施席会话 `a8b20ad7`、`.scratch/t453/`；未读全仓。自设探针 `docs/skills/skill-calorie/t453-review-probe.mjs`（不 import 判据件，全部断言本席重写）。

## 一、机器证据（全部走 `node tooling/run-locked.mjs --ticket 453 -- …`）

| 门 | runId | 读数 |
|---|---|---|
| 新判据件 | `dd05ceeb-e217-4ddb-ac03-e1ddf8485583`（首跑 `fa940e51…` 同读数） | **exit 0 · 5/5**（tests 5／pass 5／fail 0）；三产物 **75246／71961／73275** 字节；空库三页阻断 exit 4／0 字节 |
| `doc-page-assert.mjs` | `56fa42a5-7a2e-43b0-b198-7232d0319bda`（首跑 `3fb22be3…`） | **exit 0 · 1/1** |
| 告警线台账门 | 首跑 `3b51d5cc-a97c-4895-afd2-9517e4fa8e59` → 复跑 `fea445cb-5ab4-41d8-b5e5-f42308f8f4af` | 首跑 **exit 1 · 56/58**；复跑 **exit 0 · 58/58 · PASS** |
| `pnpm build` | `eacdc6db-19dc-4cfc-badf-70f6452db19c` | **exit 0** |

**告警线两次读数的归因**：首跑两条红为 `sportPortDocs.ts 台账=760 实况=666`、`exercisePort.ts 台账=541 实况=544`。对 `HEAD:<件>` blob 复算 LF → **760／541**，与首跑当刻台账**逐值相等**；两件盘上值当刻均为**他席在途未提交改动**（`git status` 皆 `M`）⇒ **红来自他席在途件，非本票缺陷**。其间他席已把台账「当场实测」列同步到盘上值（666／544），复跑即 **58/58 绿**。两次读数都与本票无关。实施席证据件 §八 记的 `pnpm build` 红（`skill-bill` TS2459）已随他席落地消失，本席进场即绿。

**行尾自证（针对「整文件 LF→CRLF 重写冒充大改动」旧事故）**：`7d4bd7f^` 22313 字节／531 LF／**CR=0**／`sha256=2469C45A…`；`7d4bd7f` 29874 字节／702 LF／**CR=0**／`sha256=5EF5EAE5…`；`HEAD` 33053 字节／760 LF／**CR=0**／`sha256=2CEC26DA…`。另存原字节与 git 对象**逐字节同源**（`git hash-object` 对账）：`.scratch/t453/head_version.bytes` → `ca6f228058c1…` ＝ `7d4bd7f^` blob；`.scratch/t453/orig-sportPortDocs.ts` → `7f4596032c0e…` ＝ `7d4bd7f` blob。`7d4bd7f` 对该件**只有 2 个 hunk**（`@@ -17,20 +17,34 @@`、`@@ -71,219 +85,376 @@`），无整文件行尾重写。

## 二、一号探针：判据件两处改动的独立核

**题面基准取不到**：`git show 7d4bd7f^:<判据件>` → `fatal: exists on disk, but not in '7d4bd7f^'`——该判据件在 `7d4bd7f` 是**新增件**。本席改用两条独立证据链重建「实施前版本」：① 实施席会话 `a8b20ad7` 里两处 `edit` 的**逐字参数**（step=52／step=53）；② 把 new_string 反演回 old_string（各命中**恰好 1 次**）。

**逐条比**：412 行（411 LF）→ 417 行（416 LF），+5 行全是注释（3＋2）。断言条数**逐项同值**：`assert.*` **89→89**、`test()` 5→5、`ok` 44→44、`equal` 30→30、`notEqual` 9→9、`deepEqual` 6→6、`.includes` 26→26 ⇒ **0 删／0 增／0 弱化**。非注释改动**只有 2 行**：

| 处 | 改前 → 改后 | 判定 |
|---|---|---|
| ① 力量页标题读取元素 | `/<p class="ilife-block-data-table-caption">…<\/p>/` → `/<caption class="ilife-block-data-table-caption">…<\/caption>/` | **修缺陷** |
| ② 有氧种子 `室内单车` | 补 `minutes: 15` | **修缺陷，不是迁就实现** |

**① 是修缺陷**：`renderDataTable` 只产 `<caption class="ilife-block-data-table-caption">`，**从不产 `<p>`**；改前读取恒为空串 ⇒ 改前那三条断言（窗口／单侧口径／Σkg×次数）**对任何实现都不可能通过**（是「恒红」，不是「更严」）。改后读的是**同一张表标题的同一处原字**，三条断言与「单侧口径全页恰好 1 次」一条未动。

**② 是修缺陷**：判据自己冻结的合计＝**197 分钟**（未改动区），而改前种子自算＝30＋32＋30＋45＋0＋20＋25 ＝ **182**；判据第 349 行注释又写「本行有 0 以外的分钟数」——**判据自相矛盾，缺陷在种子**。`run-1`（`runId=42fe7877…`）实测实现侧给出 `["合计","7 次","1200 卡","100%","182 分钟"]`：次数／热量／占比全对，**实现的加总没有错**，182 是所给种子的真值。补 `minutes:15` 后 197 与注释同时成立，缺距离／缺步速两条缺值断言原样保留。

**自造反例（改后断言仍抓得住真错）**——见 §三 的四行读数。

## 三、逐条核票面（本席自种子、自复算）

- **3 条命令逐条真跑**：exit 0；产物 75246／71961／73275 字节；信封交付路径＝本次 `--html` 那一份且为绝对路径。
- **三页锚点全有落点**：7／6／6 个、悬空 0；`ilife-page-printable` 落版面根 ＋ `@page printable` ＋ `page: printable`；`data-fmt` 顺序 `text,json,csv`；来源脚注 `数据来源 · `；口径行 ≥2。
- **分布页**：占比迷你条四类 hex（`#5856d6`／`#0071e3`／`#34c759`／`#ff9500`）齐、无 token 名；分类表四类＋合计＝`7 次｜1200 卡｜100%｜197 分钟`；共享图表＋摄入/TDEE 联动。
- **力量页**：口径标题含窗口＋`单侧口径`＋`Σkg×次数`且全页恰好 1 次；动作表 `卧推｜2 组｜1250 kg｜20 次`＝本席自算 `60×10＋65×10`；重量轨迹＋逐条记录表头齐。
- **有氧页**：类型表 5 列；`室内单车`＝`1 次｜15 分钟｜—（距离）｜—（步速）`，全表无空格子；口径行在页上。
- **空态**：空库三页阻断 `exit 4`／0 字节；装配层构造三页＝`ilife-empty` ＋「记运动」／「记力量训练」／「记有氧运动」，零 `<tbody>`、无表卡外壳、来源脚注报 0 条。
- **类别色单源**：色表形状命中文件数 **1** ＝ `packages/skill-calorie/src/exercise/categoryColors.ts`。
- **工程话**：三页全文 `calorie.view.`／`\bt\d{3}\b`／「移植」各 **0**；页头眉标＝人话原字。

**四行反例／自设变异读数**（隔离镜像＝真判据件 × 真 CLI × `dist/` 副本，见 §五 D4）：

| 号 | 改坏 | 红断言（实测原字） | 改回 |
|---|---|---|---|
| C1 | 力量页口径标题**真删掉**（删 `caption`） | `动作表标题没写窗口：力量逐条记录（共 2 条）` | 5/5 绿 |
| C2 | 分布页合计时长**改 196** | `合计时长不是 197 分钟：["合计","7 次","1200 卡","100%","196 分钟"]` | 5/5 绿 |
| M1 | 去掉分布页**合计行** | `分类表行数不是「四类＋合计」：4` | 5/5 绿 |
| M2 | 去掉有氧页**口径行**（`口径：`→`计数：`） | `缺口径行` | 5/5 绿 |

**归属**：`7d4bd7f` 全路径集合（`--name-only`）**恰为声明四件**。五件**零差异**：`sportDocs.ts`／`records.ts`／`receipt.ts`／`commands.ts`／`routes.ts`；命令与路由面（`src/triggers` ＋ `src/cli`）**零差异**。**趋势／复盘两支一行未动**：自 `buildRecapDoc` 头注释起至文末 **243 行逐字节相同**，四个函数落旧行 **292／358／387／458**（均 ≥290，＝两 hunk 之外）。

## 四、污染事故登记（如实记录，不抹）

- **他票在途占件**：`#465` 席在本席进场前后对该件取过原子变异写窗（变异态 `sha256=C3E9C2C5…`）；`T351-v7` 拆件票当刻正把「复盘」整支搬进姊妹件 `./reviewDocs.ts`，该件持续 `M`（盘上 `D85B70D7…`／666 LF）。
- **本席读次与写足迹**：读次 1（`C3E9C2C5…`）**读到的正是 #465 的变异态**——该读次对本件只有 `ReadAllBytes`，同命令唯一写落在 `.scratch/t453-review/` 且源是 git 对象；读次 2（`2CEC26DA…`）亦纯读。**本席从未对该件执行任何 write／edit／还原。** 编排者起初误判为「你把自己读到的变异态写了回去」，核对三个哈希后**已书面更正并致歉**。
- **处置**：丢弃读次 1 的「盘上现行」行；基线**改从 git 对象取**（`git cat-file blob HEAD:<件>`）；**未动真件**。

## 五、缺陷段

- **S1：无**（不改判）。
- **D1（本票范围，S3）**：证据件 §一 的产物字节（75161／71876／73190）与 §二 登记的唯一绿 runId（`1298953e…`）的日志读数 **75152／71859／73184** 不等，且那三份样张**没有对应 runId**——同一份登记里两处不同源，缺一句「样张由哪一次运行产出」的对账。
- **D2（本票范围，S3）**：§一 字节数只对 `7d4bd7f` 当刻 dist 成立；本席复跑当刻（`fa940e51`）为 75246／71961／73275，**各 +85**（种子各 +90），差来自同件后续提交。证据件未写明该读数的时效范围。
- **D3（本票引入／本票范围，S3）**：判据件第 226 行注释写死自指行号「第 344 行」——该指针按**改动前**行号写成，改动把目标行推到 **349**（344 行实为 `const indoor = …`），提交版上**指针已悬空**。记账瑕疵，不影响任何断言成立。
- **D4（本票范围，S3）**：一号探针两处反例与自设变异两处**均已执行**（§三 四行读数），但**口径与题面不同**：题面要求「变异 `src` → `pnpm build` → 判据」，因该件被他席占件、`git status` 不干净，本席改在**隔离镜像**（`dist/` 副本 ＋ 判据件副本 ＋ 同名 junction，全程**仓内共享件零写入**，mirror dist 与仓内逐字节同源）上做端到端变异。差别＝未走「源码→编译」链、sha256 取的是 `dist` 值而非源码值。反例的**鉴别力**结论不受影响（判据读的正是 `dist/`），但四步序仍待该件干净后补做。
- **D5（范围外，注记不判）**：告警线门首跑两条红全归因他席在途未提交件（§一 已逐值对账到 HEAD）；复跑已 58/58 绿。

## 六、五维分（30／25／20／15／10）

| 维 | 分 | 依据 |
|---|---|---|
| 契约一致 | 29／30 | 票面 1–6 条逐条有落点；扣 1＝D4 变异口径偏离题面（隔离镜像而非 `src`＋`pnpm build`） |
| 证据真实可复现 | 24／25 | 四门 runId＋读数齐；两份另存原字节与 git blob 逐字节同源；扣 1＝D1 样张与登记 runId 不同源 |
| 新旧对照 | 19／20 | 判据件两处改动经会话逐字参数＋断言计数证为 **0 删／0 增／0 弱化**；183／384／1250 皆本席自算；扣 1＝D2 字节读数时效未写明 |
| 工程红线 | 14／15 | 只含声明四件；五件＋命令路由面零差异；还原未用 `git checkout --`；他席占件期间**主动不碰真件**；扣 1＝D3 自指行号悬空 |
| 文档同步 | 9／10 | 证据件 101 行覆盖量现场／真跑表／变异表／遗留；扣 1＝D1 读数缺运行来源 |

**无 S1，均分 95 ≥ 85 → PASS。** 遗留：D1／D2／D3 由后续席按记账口径补正；D4 的 `src`＋`pnpm build` 四步序待该件 `git status` 为空后补做。
