# t290 后半 6 条证据（返工第二轮·实施席交付记录）

- 票：#290（结果页后半：体重目标族 2 条＋预测对照族 2 条＋`view.goal-config`／`view.goal-status`）。
- 结论：6 条逐条完整文档 exit0，新测试绿，数值对账绿，与 #254 同一套装配件。
- 卡片归属听 #291 乙结论（查找层别名、不补 HELP 卡）：本票只动页面装配与出口转调，未动命令名／路由／`WAKE_TABLE`／HELP。
- 提交（只提交未推送）：`6771428`（resultDocs.ts）／`051fa43`（read.ts）／`9e10305`（新测试）／本件另提交（见 §五）。
- 前一轮阻塞记录（`cd95487` 版 §一～§三：基线冻结读数与老实物口径分析）已入仓，原文在版本史中可查；本轮复用其冻结值（取数一字未动），不再重抄。

## 一、6 条实跑（种子库 `seedFull`，`CALORIE_TODAY=2026-09-07`，真 CLI）

`GATE-RUN runId=ee867166-aae4-426d-b395-16fc0ee4d814 exit=0`
（`.scratch/t290-后半/rework-probe.mjs` → `rework-probe.log`，探针；断言见 `test/goal-result-290.test.mjs`）：

| 条 | 命令 | exit | 字节 | 完整文档 |
|---|---|---|---|---|
| 看体重目标进度 | `calorie.view.goal-weight {"window":"7d"}` | 0 | 71712 | 是（#390 整页，未动） |
| 看即将到期的目标 | `calorie.view.goal-expiring {"today":"2026-09-07"}` | 0 | 69410 | 是（#254 整页＋本票紧迫度卡） |
| 看目标对比实际 | `calorie.view.goal-vs-actual {"window":"30d"}` | 0 | 80610 | 是（#254 整页＋本票平均偏差行） |
| 看目标预测达成 | `calorie.view.goal-predict {"window":"14d"}` | 0 | 66718 | 是（`trendPredictDocs` 整页，未动） |
| 看目标配置 | `calorie.view.goal-config {}` | 0 | 69173 | 是（本票新建整页，原片段 1503） |
| 看目标状态 | `calorie.view.goal-status {}` | 0 | 68490 | 是（本票新建整页，原片段 960） |

冻结读数（与上一轮 §一逐字段一致，取数一字未动）：

- weight-7d：`{"weightGoal":68,"latestKg":70.6,"deltaKg":-0.1,"loggedDays":7}`
- expiring（显式 today 钉死）：`{"daysLeft":115,"withinDays":14,"expiring":0,"weightGoal":68,"calorieGoal":1800}`
  （上一轮无 today 入参取机器今天、每天漂 1 天；本轮 `today=2026-09-07` ⇒ 115 天可复跑，反向锁另证无参会漂）
- vs-30d：`{"completedCount":0,"incompleteCount":9,"completionPct":0,"trendAvg":158,"calorieGoal":1800}`（与 #254 冻结一致）
  ＋平均偏差行 `-70.81 %`（有记录日达成率偏离 100% 的均值，老技能同式）
- predict-14d：`{"targetKg":68,"current":70.6,"daysLeft":179,"ratePerWeek":-0.1,"feasible":0}` ＋达成日 `2027-03-05`
  ＋徽章 `超范围`（只钉新仓可复跑值；与老家口径差见 §二.3）
- config：`{"calorie_goal":1800,"protein_goal":150,"carbs_goal":200,"fat_goal":50,"water_goal":2000,"diffKcal":50,"consistent":1,"paused":0}`
- status：`{"paused":0,"calorie_goal":1800,"water_goal":2000}`
- 紧迫度三档（同一种子截止 2026-12-31）：`today=2026-12-29` ⇒ `daysLeft:2/expiring:1/高`；
  `today=2026-12-20` ⇒ `daysLeft:11/expiring:1/中`；固定窗 ⇒ `低`。

## 二、老实物口径（沿用上一轮结论，出处精确到行，本轮只补呈现）

1. **紧迫度**（`render_goal_progress.py:353-360`）：`days_left<=3`→高；否则按需算速率分高中低。
   新仓 `buildGoalExpiringView` 无该字段——本票在呈现层按剩余天数分档（已过期／3 天内高／落窗中／窗外低），
   口径差写进口径行（可见文本只留人话，文件名与下划线词一律不上屏，探针见 §四）。
2. **偏差**（同件 `:251-293`）：`pct=实际/目标*100`，平均偏差 `mean(pct-100)`。
   本票在 `vs` 趋势表补一行（有记录日 `pct-100` 均值，`-70.81 %`），取数不动。
3. **预测日与置信度**（同件 `:420-454`）：老家达成日复用 `weight_milestone.est_date`。
   新仓 `weightTarget`（`simulate.ts:114-135`）线性外推，既有注释承认有意偏离——本票只钉新仓 `eta/daysLeft/rate` 可复跑值。
4. **体重进度完成%**（同件 `:481-486`）：起点取 `start_weight` 快照。新仓 `buildGoalWeight` 无该字段——呈现缺口记账，不在本票取数内。

## 三、改动清单（与 #254 同一套装配件，无第二份拷贝）

- `src/goal/resultDocs.ts`（286→333 行，告警线 350 内，不触发第四步，台账无需同步）：
  对外 3→5 个名字（恰顶票面“不超过 5 个”）；新增 `simpleGoalDoc` 共用装配（读数卡＋明细表＋`tailOf` 口径复制尾，
  与 #254 三页同一套）＋`buildGoalConfigDoc`／`buildGoalStatusDoc`；`expiring` 加紧迫度卡与口径行；
  `vs` 趋势表加平均偏差行与口径行。可见新文本经探针（无内部词）。
- `src/goal/read.ts`（167→172 行）：`viewGoalConfig`／`viewGoalStatus` 转调新装配，
  `metrics` 对象原样透传（与冻结值逐字段一致）；旧片段保留（`render/html.ts` 他票在途不碰）；
  `renderGoalRecommendHtml` 照常在用。命令名／路由／`WAKE_TABLE`／HELP 一字未动（红线）。
- `test/goal-result-290.test.mjs`（新建，测试件不计行数门）：主测 6 条（exit0＋绝对路径＋落盘＋四断言＋
  字节>20000＋metrics 冻结＋呈现文本；`resultDocs` 族另加唤醒词／徽章／结论／导航／口径／复制日志／探针），
  反向锁 5 发（片段必红、错配必抛、三档真分档、固定 today 可复跑、无 today 会漂）。
- 结构五步记账：影响＝目标能力目录一件（`src/goal/` 两件）＋测试一件；新建目录无；写码如上；
  超线报警未触发（333/350，余 17 行——后续再碰本件先看余量）；交付对账＝§一§四。

## 四、门禁与对账（运行记录一对一，声明边跑边补）

- `GATE-RUN runId=152261e6-5827-4767-bf62-b87b4bb7e8f0 exit=0`：改前基线 `tsc -b packages/skill-calorie` 绿（调查放行）。
- `GATE-RUN runId=b8e3bf77-f177-408c-abef-85cb7211b6da exit=1`：邻接 254 改前基线（`pass 1/fail 0`→实为 `pass 1/fail 1`），
  唯一红＝goal-7d `weeklyDeficit 10886/avgDeficit 1555` vs 冻结 `10841/1549`（与上一轮邻接红同字段，非本票引入）。
- `GATE-RUN runId=bcf71850-c12a-453e-9703-7e0760917505 exit=0`：改后 `tsc` 绿。
- `GATE-RUN runId=97a09b91-5ab1-4bd6-ad65-daae4f558995 exit=0`：8 条探针全绿（§一读数出处）。
- `GATE-RUN runId=ee867166-aae4-426d-b395-16fc0ee4d814 exit=0`：新测试首绿（`pass 2/fail 0`）。
- `GATE-RUN runId=67b69be0-e7b2-4dc5-a618-160b30cadb08 exit=1`：邻接 254 改后复跑——同红同字段（`pass 1/fail 1`），
  无新增红项（循环止于首红 goal-7d；vs／expiring 由本票新测试全覆盖见证）。范围外漂移，转票措辞，不阻本票关闭。
- 变异自证（源码级，`resultDocs.ts:179` 平均偏差 `-100`→`-90`，逐文件还原）：
  - 红：`GATE-RUN runId=83bbc84f-eb18-40bb-9bc4-830dc54c0033 exit=1`，
    `pass 1/fail 1`，`AssertionError: vs-30d 缺呈现文本：-70.81 %`。
  - 绿：`GATE-RUN runId=6815dfda-2529-493a-bf89-7ae927713b74 exit=0`，`pass 2/fail 0`（与变异前一致）。
  - 现场口径：变异后首次重建（`187006da exit=0`）写回旧 dist（并发构建交错），指纹抽查发现后重建
    （`3ea8bdaf exit=0`，dist 指纹 `-90` 确认）才起红；还原后重建（`1071cb78 exit=0`，指纹 `-100` 确认）回绿。
    教训：热区窗口每次重建后必验 dist 指纹。
- 草稿与日志（`.scratch/t290-后半/`，忽略清单内）：`rework-probe.mjs`／`rework-probe.log`／
  `rework-t254-before.log`／`rework-t254-after.log`／`rework-t290-first.log`／变异四份 log／
  `resultDocs.ts.pristine`（还原源）／`msg*.txt`（提交信息原文）。

## 五、提交与下一手

- 本票提交（只提交未推送）：`6771428`（resultDocs.ts）／`051fa43`（read.ts）／`9e10305`（新测试）／本件提交（证据）。
  提交前 `git diff --cached --name-only` 逐次复核仅含己件；`git show --name-only HEAD` 点名确认。
- 没做完：无（判据四条全绿）。跟进记账（不阻关闭）：① 254 冻结 `weeklyDeficit/avgDeficit/daysLeft` 三处随日期／数据漂移，
  归属 #254 另开票重冻（本票新测试已用显式 today 避开同类陷阱）；② `resultDocs.ts` 现 333/350，后续碰本件先看余量，
  超线即走第四步＋`--sync` 同步台账（本轮线内，未动 `AGENTS.md`）。
