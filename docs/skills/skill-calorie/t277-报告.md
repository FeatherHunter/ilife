# #277 · 预检确认页 · 报告（4 条）

- 票：地图 **#155**（卡路里场景 02 饮食）· #277。工作目录 `D:\ilife-wt\wf155-l`。
- **基线 sha ＝ `d2436196f6ad6aa4618e7cd6729c402e2601a876`**（创建 worktree 当刻 `D:\ilife` 的 HEAD，worktree `wf155-l` 即删它）。`D:\ilife` 与别的 `wf155-*` 一字未碰。
- 提交：`6b69cdb`（20 件，源码／派生件／测试／台账／报告脚本）。
- 可复跑：`node docs/skills/skill-calorie/t277-真跑.mjs`（末行 `RESULT-ALL: PASS`，读数落 `.scratch/t277/真跑-结果.json`）；探针 `node --test packages/skill-calorie/test/t277-预检确认页.test.mjs`（17/17）。

## 一、判定

| 判据 | 判定 | 读数出处 |
|---|---|---|
| 1 四条词真出口 exit 0 ＋ 完整文档 ＋ 绝对路径与字节 | **真** | §二·甲：`bytes=74098／73251／75893／76094`，四条 `doctype0/charset/style/close` 全 true |
| 2 内容与字段按老实物重做（逐块对照） | **真（14 块对上有 12、2 块不适用）** | §三·对照表；§四·3 记两处**没对上** |
| 3 预检确认页「确认 → 执行」链路可走通，写类真写进库 | **真** | §二·乙：`nutrition_products 3→4`、`food_log 21→22` |
| 4 结构化断言且能红 | **真** | `packages/skill-calorie/test/t277-预检确认页.test.mjs` 17 条；§二·丁变异两处 |
| 5 变异自证：改坏必红／还原必绿 ＋ `dist` 指纹 | **真** | §二·丁：`fail=1`／`fail=3` → `fail=0`；指纹 `7f993de54455db16` 前后相同 |
| 6 `tsc -b` 0 ／ `gen-cli --check` 0 ／ 派生件逐件列出 | **真** | §二·戊 |
| 空窗与空库分清 | **部分适用**（见 §三·两态） | 空库不阻断该页是**既有绿断言钉住的设计行为** |

**本票引入的失败＝0**。全包逐名对账**零新增红**，另有 4 条**由红转绿**（§二·己）。

## 二、机器证据

**甲 · 四条词真跑**（`t277-真跑.mjs`；`SKILLS_DB_PATH` 各自一份隔离库，`CALORIE_TODAY=2026-09-07`）

| 词 | 路由命令（原文见 `src/diet/routes.ts`） | exit | 字节 | 产物绝对路径 |
|---|---|---|---|---|
| 批量导入食品 | `calorie.product.import --params '{"items":[{"productName":"测试导入燕麦","calories":389,"protein":13,"fat":7,"carbohydrates":66,"sodium":5}],"entry":"precheck"}'` | 0 | 74098 | `<tmp>/calorie_html/批量导入食品_回执_20260915_223150.html` |
| 校验批量导入 | `calorie.view.batch-import-preview --params '{"items":[{"foodName":"粥","calories":150,"protein":3}],"entry":"validate"}'` | 0 | 73251 | `<tmp>/calorie_html/批量导入预览_20260915_223205.html` |
| 拍营养表记一餐 | `calorie.diet.add --params '{"foodName":"鸡胸","calories":200,"protein":35,"note":"营养表识别","source":"photo","entry":"precheck"}'` | 0 | 75893 | `<tmp>/calorie_html/记一餐_回执_鸡胸_20260915_223220.html` |
| 拍营养表补记一餐 | `calorie.diet.add --params '{"foodName":"米饭","calories":500,"protein":10,"date":"<日期>","time":"12:30:00","note":"营养表补记","source":"photo","entry":"precheck"}'` | 0 | 76094 | `<tmp>/calorie_html/记一餐_回执_米饭_20260915_223239.html` |

后两条的**落盘文件名仍按写键取**（`记一餐_回执_…`）——页本身是确认页，名字沿用交付层的写键口径，属外观遗留，已记 §四·3。

**乙 · 确认 → 执行**：确认页复制日志第 4 段给出**不带入口标记**的那条写命令；原样跑：
`批量导入食品 确认→执行 exit=0 nutrition_products 3 → 4`；`拍营养表记一餐 确认→执行 exit=0 food_log 21 → 22`。

**丙 · 两态**：库为空（`nutrition_products` 零行）⇒ 导入预检页仍 **exit 0**、完整文档、74372 B；一条都进不了库（条目缺必填营养数字）⇒ exit 0、含空态句「这次一条都进不了食品库」＋引导句「…再说一次「批量导入食品」」。

**丁 · 变异自证**（`.scratch/t277/mutate-readings.txt`；每步 `tsc -b --force`）

```
MUTATION-甲（校验形态判别改坏） → tsc=0 node --test exit=1 tests=17 pass=16 fail=1
RESTORE-甲  → tsc=0 node --test exit=0 tests=17 pass=17 fail=0 与进场一致=true
MUTATION-乙（确认后操作块锚点摘掉） → tsc=0 node --test exit=1 tests=17 pass=14 fail=3
RESTORE-乙  → tsc=0 node --test exit=0 tests=17 pass=17 fail=0 与进场一致=true
FINGERPRINT-SAME=true（7f993de54455db16）
```

**戊 · 编译与生成物**：`npx tsc -b packages/skill-calorie` exit 0；`gen-cli.mjs --check` exit 0（`键 128（写 46 ＋ 读 82）`）。跑过 `node scripts/gen-cli.mjs` 与 `node scripts/build-help.mjs`，**派生件逐件**：

| 派生件 | 变化 |
|---|---|
| `src/triggers/routes.generated.ts` | 8 行（4 条记录，各 1 增 1 删） |
| `packages/base-combos/combos.yaml` | ＋5 行（新键 `calorie.view.label-precheck`） |
| `scripts/build-help.mjs` | ＋1 行（示例表添该键一行） |
| `packages/skill-calorie/SKILL.md` | 15 行（速查表添该键一行 ＋ 场景 02 流程表 4 条词的命令列） |
| `packages/skill-calorie/AGENTS.md` | 8 行（告警线台账两格数字 ＋ 两行超因） |

**己 · 全包对账**（干净检出 `D:\ilife-wt\t277-base`，同 sha，独立编译）：基线 `1235／1149 绿／84 红` → 本票 `1252／1169 绿／81 红`（＋17＝新探针）。**新增红＝0**；**由红转绿 4 条**：`① 70/70 都能查到，命令列与路由层逐字一致`、`③ 产物新鲜：AUTO 块 == 生成器输出`、`calorie SKILL 与模板（M6 范式）`、`互联区新鲜（…全量键对齐 combos…）`——前两条是我重生成 SKILL.md，后两条是 `gen` 后 `combos.yaml` 补齐。

## 三、逐条路径

**落点**（新增）`src/diet/precheck.ts`（导入预检页，204 行）／`precheckLabel.ts`（营养表识别确认页，165 行）／`precheckPort.ts`（取数与逐行校验，235 行）／`precheckParts.ts`（两族共用小件，71 行）。**改动**：`diet/nutrition.ts`（两条处理体换装配）／`diet/log.ts`、`diet/productImport.ts`（入口标记门：`entry:"precheck"` ⇒ 只出确认页、不写库）／`diet/receipt.ts`（同标记时回执页让位，2 行）／`diet/routes.ts`（4 条记录）／`diet/commands.ts`＋`cli/keys.ts`（新读命令）／`render/trendMiscPort{,Docs}.ts`（搬走那一支）。

**两态分清的实况**：本类三页的取数底是**参数**（导入条目／识别读数）＋食品库对照，**没有窗口概念** ⇒ 裁定 4 的「空库 ⇒ exit 4」那一支**不适用**：实测空库仍 exit 0 出完整页，**这是既有设计行为且被既有绿断言钉住**（`test/trend-misc-port-113.test.mjs:265-266`「preview 空库应 exit 0」）。「窗口内零记录」那一支**适用**并已断言：条目一条都进不了库 ⇒ 空态句＋引导句。

**老实物逐块对照**（对上了／没对上／不适用）

| 老实物块 | 新侧 | 判定 |
|---|---|---|
| `batch_import_preview` 页头 eyebrow／title／sub／pills | B线页头：眉标行（唤醒词 · 饮食）＋徽章「预检确认」／内容标题／结论句 | 对上了 |
| `导入概览`（5 卡：总数／新增／更新／跳过／失败） | 「读数」＋「这次的处置」两组 KPI（预览形态 7 个读数） | 对上了（`updated` 无策略 ⇒ 不出现，见 §四） |
| `处理进度`（rate ＋ progress-bar ＋ detail） | 口径说明行（会新增／会跳过／会失败怎么算的）＋ 通过率卡（校验形态） | 部分对上：进度条改文字读数（裁定 5：不画无效图元） |
| `明细`（tabs ＋ 行 ＋ 展开详情 ＋ 失败原因 ＋ `无匹配数据`） | 「逐条结果」表 7 列（第几条／食品／热量／食品库热量／是否已匹配／会不会进库／原因） | 对上了（tabs 折叠改一表全出：静态页零 JS，B-09） |
| `确认后操作`（说明 ＋ 三按钮） | 「确认后操作」块（字段与取值表）＋ 复制区双按钮（裁定 7） | 对上了（三颗复制按钮收成双按钮，命令原文落第 4 段） |
| `footer` | 来源脚注小字行（裁定 2-补，不走深底块） | 对上了 |
| `nutrition_label_wizard` 页头 ＋ AI 原文折叠 ＋ 十四个字段 | 页头 ＋ 「照片与日期」＋「识别出的字段」八行（老实物那八个字段逐字） | 对上了（AI 原文折叠**不适用**：本仓页面静态、识别原文由 AI 在对话里给） |
| `diff-tag`（13 处 `.diff-tag.ai`） | 每格 `.precheck-tag.is-ai`「识别得到」；入口点名不确定的格子加 `.is-check`「要你核对」 | 对上了 |
| 表单 `input`／`重置所有修改` | 无（静态页零 JS） | **不适用**（B-09 表单零 JS，改动在对话里说） |
| `FIELD_ORDER` 13 项与 CLI 参数位置 | 写命令参数按 `t276-营养表映射.md` 那张表（接不住的三项不硬塞） | 对上了 |

**位置纪律**：四件新源码全落 `src/diet/`；工种名目录只动登记点 `cli/keys.ts`；`packages/base-combos/combos.yaml` 与 `scripts/build-help.mjs` 只经生成器重跑。**未碰** `src/home/`、`src/cli/{cmd_read,write,keys 之外}`、`src/triggers/{routes.generated 之外}`。`src/triggers/scene-02-diet.ts`／`wake-assets.ts` **一字未改**（理由见 §四·1）。

**告警线（必报五步第四步）**：
- `src/render/wizardPort.ts`——**量现场实测 277 LF（LF 口径），未超 350，不触发第四步**；台账挂号值 `457` 是历史事实、不回改（台账原话「已落回线内、不再触发第四步」）。票面「实测 457 行」是**陈旧读数**，本票按量现场报。
- `src/render/trendMiscPortDocs.ts` 676→**608**、`src/render/trendMiscPort.ts` 456→**404**：**已超线，需要根据规则进行重构。** 超因＝#113 八模板取数／装配同处一件（本票只搬走 `batch_import_preview` 那一支）。拆法（台账已写）＝按页族切姊妹件，本票走完第一步，剩 7 键待收口票。
- **新建四件都在 350 以内**，未在台账新增行。

## 四、未做项与下一手缺什么

1. **prompt 文本未改**（票面 ④ 要求改 `scene-02-diet.ts`／`wake-assets.ts`，我判**不必改**并如实报）：四条词的 prompt **已经**写明两步——`批量导入食品`「先给我看导入预览(导入条数/跳过条数/失败明细),我确认后再真正写入」、`拍营养表记一餐`「给我看识别结果(照片 + 识别出的营养),我确认后记入今天的饮食。识别不确定的地方标注一下」、`拍营养表补记一餐` 同形加「补录日期」、`校验批量导入`「只想先校验能不能导入,不真正写入」。两件自述为**机器生成的 SoT 镜像**（`wake-assets.ts:17`「禁止手工改词；改词即改 SoT」），且属工种名目录非登记点；改即无据。**`entry_sha`／指纹因此一行未动**（`gen-sot-snapshot --check` 不受影响：它只哈希 `TRIGGERS` 的规范字段）。
2. **`updated` 处置缺失**：老实物 `处理进度` 有「更新」一态（重复行覆盖）；本票不提供覆盖策略 ⇒ 只出 `新增／跳过／失败` 三态，`updated` 恒 0 不出。**缺**：覆盖策略（`onDuplicate:"overwrite"`）的入口与在页上如实预告。
3. **两处外观遗留**：① 两条过程型词落盘文件名仍带写键口径（`记一餐_回执_…`），页本身是确认页；② 老实物 `处理进度` 的进度条改成了文字读数。两处都要负责人肉眼定夺。
4. **未做**：视觉逐格审版式（挤／塌列／被藏的横滚）——按票面「视觉不做脚本化判定」留给负责人肉眼；本类三页只到结构级判定。
5. **版本面看 `build-help.mjs:394` 的 `dietFlowOf` 是硬拦**：过程型词的路由键**必须**是会改数据库的键，所以我第一版把两条词改指读命令时被生成器当场拦下（`饮食场景的类型与命令对不上：拍营养表记一餐 类型=过程 命令=calorie.view.label-precheck`）。**下一手要知道**：给「过程型词」加独立预检页**只能**走「入口标记 ＋ 同一条写命令出写前页」这条路，不能把词改指读命令。
6. **范围外**：`src/render/trendDocs.ts` 台账 1135→1136 是进场前就陈化的行，本票 `--sync` 按全表口径顺手刷正（一行数字），未改其结论列。
