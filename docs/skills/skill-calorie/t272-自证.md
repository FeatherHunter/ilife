# #272 · 自证读数（变异／归因／门禁／告警线／生成物）

跑法：一切会动工作区的动作都持锁（`node tooling/run-locked.mjs --ticket 272 --max-wait-ms 900000 -- <命令>`）。运行标识逐条见 `t272-gate-runs.md`。

## 一、变异自证（源码级改坏 ⇒ 同一批断言必红 ⇒ 逐文件点名还原 ⇒ 变绿）

脚本：`.scratch/t272/mutation.ps1` ＋ `.scratch/t272/mutate.mjs`（两处变异各改源码一行，编译后跑 `test/t272-排行榜页.test.mjs`，再按 pathspec 点名还原并 `utimesSync` 逼 `tsc -b` 重编）。

| 步 | 变异 | `dist/diet/rankingDocs.js` 指纹 | 断言读数 |
|---|---|---|---|
| 原样 | — | `83D5FD2C4A6B142C` | `tests 26 / pass 26 / fail 0`；红 0 条 |
| 变异甲 | `rankingDocs.ts` 的 `} else if (cat === 'high_carb' \|\| cat === 'high_protein') {` → `} else if (false) {`（高碳水／高蛋白两榜退回「写死一套」列序——正是老实物那个缺陷） | `7A88B863A4C15862`（变了） | `tests 26 / pass 23 / fail 3`，红的逐条：`#272 ① …高碳水榜`／`#272 ① …高蛋白榜`／`#272 ⑩ 变异自证…` |
| 还原甲 | 逐文件 `git checkout b2d82ab -- <两件>` ＋ 推 mtime | `83D5FD2C4A6B142C`（**回到原值**） | `tests 26 / pass 26 / fail 0`；红 0 条 |
| 变异乙 | `ranking.ts` 的 `const cmd = commandLine('calorie.view.ranking', params);` → `const cmd = '本页由本地 CLI 渲染，无 AI 链';`（日志第 4 段不再是命令原文） | —（变的是 `dist/diet/ranking.js`） | `tests 26 / pass 20 / fail 6`，红的逐条：`#272 ⑦ …`×5 ＋`#272 ⑩ 变异自证…` |
| 还原乙 | 同上 | `83D5FD2C4A6B142C`（回到原值） | `tests 26 / pass 26 / fail 0`；红 0 条 |

⇒ 「红／还原一致」两行机器读数齐：**源码 sha 一致 ＋ dist 重新编译 ＋ 产物指纹回到原值**三件都成立（Windows mtime 陷阱按派单 §十六办：还原后 `utimesSync(now)`）。

## 二、归因自证（不新增红）

### 2.1 本包全量（`node --test packages/skill-calorie/test/*.test.mjs`）

| 窗口 | tests | pass | fail | 红的名字 |
|---|---|---|---|---|
| 改动前（本票进场基线，运行标识 `ff489ea6`） | — | 890 ✔ | 43 个唯一名 | 见 `.scratch/t272/fail-before.txt` |
| 改动后（运行标识 `bbc05d48`） | 1029 | 990 | 40 个唯一名 | 见 `.scratch/t272/fail-after.txt` |

逐条对账（名字级）：**变绿 6 条**（都是别的座位在途件的红：`#274 判档②…`×4、`calorie SKILL 与模板（M6 范式）`、`互联区新鲜…`）；**新增红 3 条**，逐条归因如下：

| 新增的红 | 归因读数 | 与本票的关系 |
|---|---|---|
| `✖ T5 真包门禁：无参运行读真台账 → PASS（台账与当刻实况一致）` | 退回旧写法跑同一批（`.scratch/t272/attr2-*.log`）：**带改动 2 红／退回旧写法 2 红／还原后 2 红**，名字逐条相同（`T5`＋`T9`）。红的原因是告警线台账陈化，红点逐字点名 `scripts/gen-cli.mjs`（台账 754／实况 796）与 `scripts/build-help.mjs`（台账 362／实况 396）——**两件都不在本票写集**（后者还是 §五 列名的生成物，归编排者收口） | 无关（另有座位已在本窗内 `--sync` 回绿，见 §四） |
| `✖ T9 真包台账与当刻实况一致：--sync --dry 零改动且不动文件` | 同上 | 无关 |
| `✖ packages\skill-calorie\test\skill-t11.test.mjs`（文件级） | 该次运行的日志逐字：`file:///D:/ilife/packages/skill-calorie/scripts/build-help.mjs:353 SyntaxError: Unexpected token 'export'`——**另一座位当时正在改 `build-help.mjs`，那一刻文件还不是合法模块**，于是这个 import 它的测试件在装载期就崩 | 无关（本票一行不碰 `build-help.mjs`） |

### 2.2 本票判据批（改动后复跑，运行标识见 `t272-gate-runs.md`）

`node --test test/skill-t11.test.mjs test/t445-告警线门.test.mjs test/t272-排行榜页.test.mjs test/diet-homogeneity-108.test.mjs test/t511-清尾二.test.mjs`
⇒ **`tests 68 / pass 68 / fail 0`**（含别人钉住本页的两条：`diet-homogeneity-108` 的「单榜表＋复制榜单」与 `t511` 的「41 页三个名字统一」）。

### 2.3 靶向归因（本页那两条引脚）

`node --test test/diet-homogeneity-108.test.mjs` 三态跑（`.scratch/t272/attr-*.log`）：带改动红 0／退回旧写法红 0／还原后红 0，且 `dist/diet/rankingDocs.js` 指纹 `83D5FD2C…` 逐态回位——**本票改动没有让这两条引脚变红**。

## 三、门禁运行条目

全部持锁跑，`GATE-RUN` 逐条抄自 `.scratch/locks/gate-runs.log`，导出件＝`t272-gate-runs.md`（编译／测试／`git commit`／`git push` 都在内）。

## 四、告警线台账

- 本票两个改动件的 LF：`src/diet/rankingDocs.ts` **296**／`src/diet/ranking.ts` **30**，都**在 350 以内** ⇒ 本票无该挂的行、也无该刷的「当场实测」列。
- 改动后当刻读数（无参运行）：`RESULT: 67/67` ＋ `PASS: 告警线台账齐全且与实况一致`。
- 本窗内一度读到 `RESULT: 65/67`，两条红是 `scripts/gen-cli.mjs`、`scripts/build-help.mjs` 的台账陈化——两件都不在本票写集，我**不越界 sync**（红线：绝不碰别人声明路径下的文件；`build-help.mjs` 还是 §五 点名的生成物）。该红已由编排者一侧同步后回绿（当刻读数即上一条）。

## 五、生成物

本票**没写、也没 `git add`** §五 那六件生成物（`src/cli/keys.ts`／`src/triggers/routes.generated.ts`／`packages/base-combos/combos.yaml`／`scripts/build-help.mjs`／`SKILL.md`／`packages/base-combos/src/present.ts`）。

`pnpm run gen:check` 当刻读数（运行标识 `a84eccd8`）：**红** —— `Error: 预检页表含注册表里没有的键：composition`（另有座位在途）⇒ **与本票无关**（本票没碰命令声明与生成物），报编排者。
