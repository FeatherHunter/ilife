# t569 返修证据（模拟减重族 页07–13 · 报告§7.2 R-07~R-14）

> 票 #569 ｜ 实施席 ｜ 2026-09-16
> 权威清单：`docs/skills/skill-calorie/t520-视觉终审-报告.md` §7.2（R-07~R-14，每条含现象/期望/验收怎么量）
> 票面入口：`gh issue view 569 --comments`（标题：卡路里场景10 · 返修：模拟减重族（页 07–13））
> 产物目录：`.scratch/t387/html/`（31页，`node .scratch/t387/run-all.mjs` 重出）
> 统一要求冲突时以R条为准，冲突单列（见§6）。

## §0 写集声明（文件级，动手前声明，交付对账须逐行对上）

| # | 文件 | 一句话理由 |
|---|---|---|
| 1 | `packages/skill-calorie/src/render/trendPredictDocs.ts` | 本族页面唯一装配地：R-07窄档两列表恢复表头＋列式对齐、R-09结论/判定卡去复述（07/08）、R-10主标/chip数字单位留空格、R-11所需缺口卡detail改总量互补、R-12结论卡字号升档、R-13卡内距页侧16px覆盖；R-08当刻已绿只加锁（不动）、R-14元素侧已干净（公共层CSS字面归#567，不动`packages/base-render/**`） |
| 2 | `packages/skill-calorie/test/t569-sim-weight.test.mjs` | 本票新增断言：R-07~R-14每条可源码级改坏（改前必红/改后必绿），不改冻结 `analysis-*.test.mjs` 口径 |
| 3 | `packages/skill-calorie/AGENTS.md` | 台账同步：本票改动致`trendPredictDocs.ts`行数变化，须跑`check-warning-line.mjs --sync`回绿（设计行为，只改当场实测列，不改挂号值） |
| 4 | `docs/skills/skill-calorie/t569-返修-证据.md` | 本证据件 |

> 结构纪律必报五步：第1步即本表（影响清单）；第2步见各R小节设计；第3步写代码（跨能力只走公开接口，常量单源）；第4步超线报警（本件 `trendPredictDocs.ts` 已超线在册939行，本次先不拆，拆法照台账按页族切两件）；第5步交付对账见§7。
> R-09/R-57/R-58类对账：R-09先对账当刻字节再定修不修（见§1 R-09）；R-57/R-58非本族（报告族），本票不碰。
> 页07–13由`buildSimCutDoc`（07/08/09）＋`buildSimTargetDoc`（10–13）装配（`run-all.mjs` order399–405 → `calorie.view.predict` cut_kcal/target_loss分支 → `insightPlate.ts` → `trendPredictDocs.ts`），与`buildCalorieForecastDoc`（14–16，#570地盘）共用`trackTable/trackTableCss`——R-12/R-13/R-14按报告明示跨到16，本票改动保证14–16只增益（508行以下§2各条注明影响面）。

## §1 开工前重测（当刻产物字节实测为准，2026-09-16 UTC）

> 对账口径：以当刻 `.scratch/t387/html/` 字节实测为准，历史读数只记时点读数。探针：`.scratch/t569/probe-before.mjs`（只读产物）＋`.scratch/t569/probe2.mjs`。

- 当刻字节（时点）：07/08/09均为74505B；10为73030B；11为73742B；12为74454B；13为73386B。07/08/09三份同字节（同装配`buildSimCutDoc`＋不同cutKcal？待核——三份同74505B说明cut值同宽，见§2 R-10备注）。

### R-07（07/08窄档标签逐行重复＋数值不对齐）
- 时点读数：07页`data-label="模拟体重（kg）"`14处、`data-label="日期"`14处（08/09同；10–13为6–14处随天数）；公共层≤640px行卡化＋本族`trackTableCss`一行一条（日期标签`content:none`、体重标签行内前缀），390档体重标签仍逐行印14次，数值列无右对齐。仍成立。

### R-08（07/08同列有效位不齐）
- 时点读数：07页轨迹列14格为`75.1/73.7/…/71.0/…/57.6`，正则`^\d+\.\d$`全匹配（`>71<`零命中、`>71.0<`一命中）。**当刻已绿**（W6的`fmtWeight→toFixed(1)`字面已修，见`trendPredictDocs.ts:343-345`）。本票不改该处，只加锁断言防退化。

### R-09（07/08同一事实多处复述，先对账再施工）
- 对账：`t518`§9.5①记“07值位已修为—”是W5时点态；当刻产物（74505B）里判定卡**已无值位**（W6整格撤除，`verdictCard`只剩detail＋徽标），全页可见文本零`—`（W6-①-c）。V1读数“三处复述”当刻为：结论`每天多减 300 卡，一周大约掉 1.36 kg。这个速度超出健康范围，建议减量。`＋KPI`每周掉重 1.36 kg/周`＋判定卡detail`这个减量超出安全区间。`＋徽章`超范围`（可见计数：超范围1/超出健康范围1/超出安全区间1/这个减量1）。
- 裁定：t518的“—”分支已由W6的撤格替代完成（撤销分支不触发）；V1分支部分成立——判断词三处同义（超出健康范围/超出安全区间/超范围），按R-06先例收判断至徽标一处，detail改`newDeficit`互补事实（该字段页上未用）。修。

### R-10（09–13主标数字单位紧贴）
- 时点读数：09 H1`模拟减重（每天-700卡）`（`0卡`命中2＝H1＋chip`每天-700卡`）；10–13 H1`模拟减重（30天减2kg/60天减4kg/90天减6kg/45天减3kg）`（`减\d`各1、`0天`10/11/12各1）；正文结论已留空格（`300 卡`）。仍成立。注：修`buildSimCutDoc/buildSimTargetDoc`的H1＋chip会连带07/08同规则（统一规则，无害，验收只计09–13五页）。

### R-11（10–13 detail与结论条同前缀）
- 时点读数：detail`要在 30 天里减掉 2 kg`确为结论`要在 30 天里减掉 2 kg，每天大约要留出 513 卡缺口。…`的前缀（源码行643–644/659对照）。仍成立。改法：detail改总量互补`30 天合计约 15390 卡。`（`neededDeficit×daysTarget`，渲染层派生，R-06`diffKg`先例；`WeightSimTarget`无“当前缺口”字段，报告的“如…”例无数组成）。

### R-12（09–16结论数字不突出）
- 时点读数：09四卡label`当前/每天多减/每周掉重/可行性`，值全22px/700，无hero规则（源码无`t569`）。仍成立。结论卡＝模拟页`每周掉重`（第3格）／预测页`摄入预测`（第4格，报告点名`1904 卡`）。

### R-13（09–16卡padding 14px非4/8倍数）
- 时点读数：产物内`padding:14px`零命中——14px住公共层`base-render/src/blocks.ts:1484`（本票禁动，归#567）；页侧未覆盖。仍成立（页侧覆盖缺失）。改法：页侧`t569`样式覆盖16px（09–16八页经三装配件全覆盖，07/08同得）。

### R-14（08–16空`data-label=""`）
- 时点读数：31份产物**每份恰2处**，且两处全在`<style>`内（可见文本零命中）：字面来自公共层`base-render/src/blocks.ts:1779/1785`两条防御规则（`td[data-label=""]`），**页面元素属性侧零空串**（全部`td`均带实义label）。nav.mjs（`.scratch/t520/shots-V2/nav.mjs:24`）正则是全文档匹配，把CSS选择器字面也数进去，故报`["", "日期", "模拟体重（kg）"]`。
- 裁定：页面侧期望（无空串属性）当刻已成立；字面验收（全批`data-label=""`==0）须动公共层CSS，归#567（见§6冲突点）。本票只加元素侧锁断言。

## §2 逐条修（根因在渲染器/样式/产物三层）

（逐条落盘：现象/期望/验收抄报告原文 → 根因三层 → 改法 → GATE-RUN → 验收读数）

### R-07 两列表窄档恢复表头＋列式对齐（取期望第二档）
- 现象/期望/验收（报告原文抄）：现象@390 `thead{display:none}`后由`td::before{content:attr(data-label)}`把列标签“模拟体重（kg）”在15行里各印一次，列式数值对齐丢失｜期望窄档改“每列一行、标签左值右”堆叠式（与锚件同档），**或**恢复表头＋列式对齐｜验收390档`td::before`命中“模拟体重（kg）”次数≤1，或数值列右缘共线（极差≤1px）。
- 根因（三层）：渲染器`trackTable`两列表＋样式公共层≤640px行卡化（`blocks.ts:1698-1796`）＋本族`trackTableCss`一行一条（体重标签行内前缀），三者合成14次逐行标签＋数值无右对齐。
- 改法（页面侧，不碰公共层）：`trackTable`加`restoreHead`位，两列模拟表再套`.tpd-track-table--cols2`纯标记（内层`<div class="tpd-track-table">`逐字，W6-②绿）；`t569SimCss()`在≤640px把行卡化逐条顶回表形态（thead/table-header-group、tr/table-row、td/table-cell＋`td::before{content:none}`、数值列回`text-align:right`，等宽栈/`tnum`由公共层基规则承担）；三列表（14–16）不套（R-16归#570）。选第二档三理由见§6-3。
- GATE-RUN runId=db1ade94-e257-437a-895b-a0acb1af311a cmd=`node --test t569-sim-weight` R-07绿；runId=66915671-cec4-477d-a50c-d49a2006a57d cmd=`node .scratch/t569/probe-render.mjs` 真浏览器读数。
- 验收读数（真机CDP，07页）：390档`::before`含“模拟体重”命中0（≤1✓）；数值文本右缘14行全361（极差0≤1px✓）；thead计算`display:table-header-group`✓；1440档同零/同线。手机截图vision判“带表头两列真表格，数值右对齐成一列，无硬伤”（§3.5）。

### R-08 已绿加锁（整数补`.0`，W6已修）
- 现象/期望/验收：同列数值有效位不齐（`71`与`72.4`两种字面）｜期望同列统一1位小数（整数补`.0`）｜验收该列格正则全匹配`^\d+\.\d$`。
- 对账：当刻07页14格`75.1/73.7/…/71.0/…`全匹配（`>71<`零命中），`fmtWeight→toFixed(1)`字面（本件343行）即修处。本票不动该处，只加`t569` R-08锁（cut＋target两装配，W6-③只锁了cut）。
- GATE-RUN runId=db1ade94 R-08绿。
- 验收读数：cut/target两表值全`^\d+\.\d$`（§1时点＋改后复验一致）。

### R-09 07/08去复述（判断留徽标一处，detail补总缺口事实）
- 现象/期望/验收：同一事实三处复述（结论条＋KPI卡＋可行性卡＋徽章）。**先对账**：`t518`记“值位—”是W5时点态，当刻判定卡已无值位（W6整格撤除，全页可见零`—`），撤销分支不触发；V1分支成立——判断词三处同义（超出健康范围/超出安全区间/超范围）｜期望按对账态收判断至徽标一处｜验收可见`超出健康范围`0、`超出安全区间`0、`超范围`1。
- 根因：结论尾句判断＋verdict detail判断＋徽章判断三处同义；`newDeficit`（每天总缺口）字段页上未用。
- 改法（R-06先例）：结论只留两主数（前半句式冻结W5-②不动）；detail改`每天总缺口约 Z 卡。`（`newDeficit`，页上唯一新事实；缺数fallback无数字人话，无`—`）；徽章不动（唯一判断；`可行性`label保留，冻结383针仍绿）。
- GATE-RUN runId=db1ade94 R-09绿（cut300徽标超范围/cut700徽标可行双态）；变异MUT1（§5）。
- 验收读数（改后07）：结论`每天多减 300 卡，一周大约掉 1.36 kg。`；detail`每天总缺口约 1494 卡。`；徽章`超范围`；三处两两不互为子串；W5-②/①、W6-①-c全绿（§4兄弟门）。

### R-10 主标/chip数字单位留半角空格（09–13）
- 现象/期望/验收：主标紧贴（`每天-700卡`/`30天减2kg`…）正文留空格｜期望主标与正文同一规则（数字与单位间半角空格）｜验收`spacingAudit`定向检索`0卡|减\d|0天`命中0（5页，主标/chip面；正文`90 天`等合法间隔不在此面）。
- 根因：`buildSimCutDoc/buildSimTargetDoc`的H1与`t568Chips`页名沿唤醒词无空格形态。
- 改法（显示层，唤醒词/routes冻结不动，全角括号W5-③仍守）：H1→`模拟减重（每天 -700 卡）`/`模拟减重（30 天减 2 kg）`；chip第二格同spaced形态；386:327（H1含参）仍过。07/08同装配连带同规则（验收只计5页）。
- GATE-RUN runId=db1ade94 R-10绿（5页H1/chip三正则零命中＋全角括号＋目标H1逐字）。
- 验收读数：09 H1`模拟减重（每天 -700 卡）`chip`每天 -700 卡`；10 H1`模拟减重（30 天减 2 kg）`chip`30 天减 2 kg`；11–13同式；手机截图10页H1单行无孤字（§3.5）。

### R-11 `所需缺口`卡detail改总量互补（10–13）
- 现象/期望/验收：detail`要在 30 天里减掉 2 kg`与结论同前缀｜期望detail改正文未含的差异信息｜验收detail不是结论前缀（LCP<8字）。
- 根因：detail取结论首句原文（本件旧659行）；该视图无“当前缺口”字段，报告“如…”例无数组成。
- 改法（R-06`diffKg`先例，渲染层派生）：detail→`30 天合计约 15390 卡。`（`neededDeficit×daysTarget`，期间总量vs日均口径互补；缺数fallback无数字人话）。
- GATE-RUN runId=db1ade94 R-11绿；变异MUT2（§5）。
- 验收读数（改后10）：结论首句仍`要在 30 天里减掉 2 kg…`；detail`30 天合计约 15390 卡。`；LCP=0（首字`3`vs`要`）；手机截图vision判“措辞完全不同，不重复”（§3.5）。

### R-12 结论卡升档28px/800（09–16）
- 现象/期望/验收：四卡值同22px/700，主角（1.72kg/周、1904卡）与参数等权｜期望结论数字最大字重档、参数降一档｜验收四卡value的font-size不全等、结论卡全页最大。
- 根因：`renderKpiGrid`全卡同公共层档，页面侧无hero规则。
- 改法（页面侧）：`t569SimCss`模拟页第3格（每周掉重）＋`t569ForecastCss`预测页第4格（摄入预测）值`font-size:28px;font-weight:800`（22→28级差6≥4；28为4倍数；判定卡无值位选不中）。参数卡留公共层档（理由见§6-4）。
- GATE-RUN runId=db1ade94 R-12绿（两规则存在性）；runId=66915671真机计算样式。
- 验收读数（真机CDP，07页1440/390双档一致）：当前22/700、每天多减22/700、**每周掉重28/800**（不全等✓结论最大✓）。14–16同规则产物含`nth-child(4)`（§4 markers）。vision初读“同档”系整页缩略误判，已由CDP硬读数纠正（§3.5）。

### R-13 卡内距页侧16px覆盖（09–16）
- 现象/期望/验收：`.ilife-block-kpi-card{padding:14px}`非4/8倍数｜期望改12px或16px｜验收源码该类padding为4/8倍数（8/8页）。
- 根因：14px住公共层（`blocks.ts:1484`，禁动归#567），页侧未覆盖（改前产物零`padding:14px`即页侧无覆盖之证）。
- 改法：`t569SimCss`/`t569ForecastCss`覆盖`.ilife-block-kpi-card{padding:16px}`（07–16经三装配全覆盖⊇09–16；只改渲染结果，公共层值不动）。
- GATE-RUN runId=db1ade94 R-13绿；runId=66915671真机计算`padding:16px`✓。

### R-14 元素侧锁断言（字面两处归#567）
- 现象/期望/验收：空`data-label=""`｜期望无标签格不写该属性｜验收全批`data-label=""`==0。
- 对账（见§1）：31份产物每份2处且全在`<style>`内——字面来自公共层防御规则（`blocks.ts:1779/1785`），元素属性侧零空串；nav.mjs全文档正则把CSS字面也数入。页面侧期望当刻已成立；字面验收须动公共层CSS，归#567（§6-1）。
- 改法：本票零源码改动（无可改的页面侧）；`t569` R-14锁：剥style/script/注释后`data-label=""`==0＋实义label存在（改坏即红，防后人真写出空串）。
- GATE-RUN runId=db1ade94 R-14绿；改后全批元素侧总数0（§4探针）。

### R-03同形延伸（10–13，非R-07~R-14编号内，顺手）
- 改前10–13眉题`趋势分析`与chip第三格同字（R-03的01–08范围未含）；改法眉题→`模拟减重`、chip第二格→页名spaced形态（与H1同形），distinct。验收：10/13两页眉题≠任一chip（`t569`末条，runId=db1ade94绿）。冻结门无一字涉10–13眉题/chip（§4兄弟门37/37为证）。

## §3 统一要求自查

### 3.1 三档无溢出无塌列/触摸≥44px（1440/768/390）
- 定向检索HELP夹具（不通读）：断点`@media(max-width:600px)`/`601–900px`、容器`max-width:960px`、触摸`44px`×3；夹具**无表**（thead/data-label零命中），故“窄表堆叠”无直接参照——R-07取报告第二档（恢复表头＋列式对齐）另有三重理由：①堆叠回到每记录两行即`t518`已判的30行缺陷形态；②两列表390px下真实表格无溢出（日期10字符＋5字符数值）；③验收明示右缘共线替代项。
- 触摸：输入框`min-height:44px`（公共层，产物可验）、复制按钮沿公共层44px档；本票未动触摸面。
- GATE-RUN runId=cbac5979-8c96-4fe8-9389-b5e98f0f21d6 cmd=`audit-separators` RESULT 31/31 PASS；runId=cf70a8a1-23ea-4f1e-a2fa-455cbba5fe2b cmd=`measure-responsive` OVERFLOW-ZERO pages=31 cells=93 failed=0（31页×3档逐格0，含R-07恢复后的两列表390档）。

### 3.2 设计师审美（4/8倍数/字号档/数值等宽右对齐/左缘共线/留白）
- 间距4/8：新增`padding:16px`/`8px 12px`均为4倍数；未动公共层`gap:6px`/`padding:14px`（归#567，页侧覆盖只改渲染）。
- 字号成档：结论28与参数22级差6≥4，与单位13差15≥8；28/22/13/12沿公共层阶梯，未立新档。
- 数值列等宽右对齐：恢复表保留公共层等宽栈＋`tnum`＋`align:right`（CDP文本右缘14行全等为证）。
- 卡表图左缘共线：表卡`max-width:680px`居中 vs KPI网格1080（跨族R-61归#573，他票），本族内KPI左缘一致；按钮行（R-62归#573）不动。
- 留白有节：手机截图07/10无空洞硬伤（§3.5）；桌面1440两侧灰底为壳1120居中架构（R-04归#567），非本票硬伤。

### 3.3 文案零冗余（wording.md）
- 同一事实一页一次：R-09判断收至徽标一处（结论/detail改互补事实）；R-11 detail改总量（结论日均vs卡片期间）；`newDeficit`/`periodTotal`均为页上唯一落点。
- 用词守wording：新增`每天总缺口约/合计约/按窗口内趋势另算/每天 -/天减`均为用户话，无`壳/渲染层/底座/抓手/赋能/闭环/颗粒度/沉淀`等黑话；`趋势分析`域词在10–13去重后各一处。
- 黑话扫描：`audit-separators` 31/31 PASS（符号串纪律侧证）。

### 3.4 生动形象（分号/竖线/间隔号长串改卡片/分行/徽章/图例）
- 新增文案用`。`断句，无`；`/`｜`/`·`长串（separators PASS为证）；口径行`｜`由`renderCaliberLine`切span走版式竖线，产物文本无该字符；判断走徽章、事实走结论条/KPI卡/detail，不拿符号拼串。R-07恢复的是真表格（列对齐），不是符号拼串。

### 3.5 视觉自查（vision_html_screenshot 1440x900+390x844 fullPage + vision_describe）
- 代表页：07桌面1440＋07手机390＋10手机390（R-07表/R-09去复述/R-10标题/R-11detail/R-12hero全覆盖）。
- 07手机：vision判“带表头两列真表格，数值右对齐成一列；KPI无折行压字溢出；可行性卡三行＋徽章完整；**无硬伤**”。
- 10手机：vision判“H1单行无孤字；detail`30 天合计约 15390 卡。`与结论`日均513卡＋超范围`措辞完全不同不重复；四卡数字完整；**无硬伤**”。
- 07桌面初读vision称“hero无差异/数值列左对齐/参数四列”——三项均被真机CDP硬读数纠正：每周掉重28/800 vs 其余22/700（双档一致）、数值文本右缘14行全等、参数表单恰3字段（整页缩略下的误判，详§2 R-12）。其余风格点评（标题留白/壳居中灰底/口径一行/按钮行）为既有架构（R-04/R-61/R-62归#567/#573），本票未动。dsh visionreview已坏未试。

## §4 全部门禁+重出31页+逐页sha256

- GATE-RUN runId=39193621-8c69-4865-88a3-e7f112ed9ecc cmd=`tsc -b packages/skill-calorie` exit=0（编译入口写死，`npx tsc`不用）。
- GATE-RUN runId=cbac5979-8c96-4fe8-9389-b5e98f0f21d6 cmd=`audit-separators --dir .scratch/t387/html` RESULT 31/31 PASS（节点级全零）。
- GATE-RUN runId=cf70a8a1-23ea-4f1e-a2fa-455cbba5fe2b cmd=`measure-responsive --dir .scratch/t387/html` OVERFLOW-ZERO pages=31 cells=93 failed=0（31页×3档逐格0）。
- GATE-RUN 靶向：runId=db1ade94-e257-437a-895b-a0acb1af311a cmd=`node --test t569` tests9/pass9/fail0；runId=60396096-482a-4c97-ad94-90d5d1251aa1 cmd=`node --test t568+shape-520+deficit-eta-466+predict-383+predict-455+deficit-385+report-384` pass37/fail0（含W5/W6/383/386冻结）。
- GATE-RUN runId=29482102-0ec7-4722-a3b7-b2f337e9dced cmd=`check-warning-line` RESULT 91/91 PASS（含本次939→1006同步，runId=ecfadd4f-d409-4d12-9e73-4b8825bce3e6；另三行 diet 在途件当场实测同步属设计行为，见§7）。
- GATE-RUN runId=498ca97b-85ce-489a-975e-31d8c9fe7d2f cmd=`gen-cli --check` GEN-CHECK PASS（键128）。
- run-all重出 runId=2bb03934-41d2-4e7f-b305-2baf72938ac4 cmd=`node .scratch/t387/run-all.mjs` 31/31＋PARAM-AUDIT 31/31，逐页sha256（字节为王）：
  - 01 71185B 2b23e438ea96／02 71196B 2bfeeef03ea6／03 71196B def57b078c84／04 71201B a6d104adfafe／05 71184B c79f37ba2933／06 71071B 93c8a487287c／07 75389B 286ca7a73bfa／08 75389B 1146a9d18de2／09 75389B de3adb783494／10 73967B 7719a4d40d0e／11 74679B 0f55d8fbbb16／12 75391B 802f87128de6／13 74323B 23b9ea32e247／14 72166B d46c43df40d1／15 73214B 203523d53728／16 75294B 8463f27d9c2c／17 74254B 988cce1e49e3／18 70263B 9ddf78c7f099／19 69756B e17573cc6dd5／20 70006B 02e58e5ab61a／21 87070B 762c84243929／22 87070B 762c84243929／23 71185B 2b23e438ea96（=01）／24 92775B 7b5b46b94b54／25 77673B 19e7ba2c9f5b／26 87579B ed455a556394／27 84099B 0917c30768d3／28 84128B 388cec65a2c3／29 80002B 8c3ab935eb0c／30 87261B 893e37720676／31 98541B 87391d1cce1f。
  - 本族外页（01–06/17–31）字节与`§1`改前一致（唯17因forecast-CSS＋176B：74078→74254；14–16各＋176B同因，见R-12/R-13只增益）。
- 真机CDP抽核 runId=66915671-cec4-477d-a50c-d49a2006a57d cmd=`node .scratch/t569/probe-render.mjs` exit=0（07页1440/390：hero 28/800、卡16px、表头可见、`::before`命中0、数值右缘极差0）。

## §5 变异自证（改坏必红→逐文件还原→必绿，两行读数）

脚本`.scratch/t569/mutate.mjs`（持锁窗口内：改坏→重编→跑本票用例→还原→重编→再跑→核还原字节逐字一致）：

- 变异点MUT1（`trendPredictDocs.ts` R-09）：结论加回判断尾句`这个速度超出健康范围。`→ runId=776d78c4-cafc-457e-b6d6-97f2c76a81b1（复核runId=5dd7d18a-d438-447b-a6d7-4604ef314639，读数一致）内：RED tests9/pass8/fail1 → 逐文件还原 → GREEN tests9/pass9/fail0，还原逐字节一致。两行：RED 8/1／GREEN 9/9。
- 变异点MUT2（同件R-11）：detail改回`要在 X 天…`前缀 → 同窗口：RED tests9/pass8/fail1 → 还原 → GREEN tests9/pass9/fail0，还原逐字节一致。两行：RED 8/1／GREEN 9/9。
- 读数原件：`.scratch/t569/mutate.log`（两轮全量行）。

## §6 冲突点（统一要求 vs R条，以R条为准单列；另含R条 vs 冻结口径）

1. R-14字面验收（全批`data-label=""`==0）vs 票面红线（不动`packages/base-render/**`）：以后者为准，字面两处是公共层防御规则的选择器（`blocks.ts:1779/1785`），改字面归#567；本票守页面元素侧零空串（当刻已成立＋测试锁定）。
2. R-13的14px同理：公共层值不动（归#567），本票页侧16px覆盖渲染结果。
3. R-07取期望第二档（恢复表头）而非第一档（堆叠）：第一档回到`t518`已判缺陷的30行形态，且HELP夹具无表可参照；验收的“或”分支明示列式对齐可替代。
4. R-12“参数类卡降一档”：本票只做结论卡升档（28px/800，22→28级差6≥4），参数卡留公共层22px——降档会伤可读且验收只量“不全等＋结论最大”。复核：CDP证结论28最大、三卡不全等，验收过线；不降档。
5. R-10标题空格 vs 唤醒词原文（`模拟减重(每天-700卡)`无空格）：唤醒词住`routes.ts`冻结不动，只改H1/chip显示层（W5-③全角括号约束仍守）。
6. R-09结论须保留`一周大约掉 Y kg`（2位） frozen（W5-②`/一周大约掉 (\d+\.\d+) kg/`），故结论只删判断尾句，不动前半。

## §7 交付对账（结构纪律第5步）+ 收尾干净声明

- 写集逐行对账：①`trendPredictDocs.ts`（R-07/R-09/R-10/R-11/R-12/R-13页面侧＋R-03同形延伸10–13，939→1006行，先不拆，拆法照台账）已改；②`t569-sim-weight.test.mjs`（9条靶向）已建；③`AGENTS.md`（939→1006同步；另三行diet在途件当场实测741/364/355系别席未提交改动的盘上实况，同步器按设计行为照实记录，本票未改那些源码，挂号值未动）已改；④本证据件已落盘。第一步清单与交付逐行对上，偏差0。
- 他席在途：收尾盘上72处未提交改动均为别席在途（base-render/skill-bill/goal/home/diet等），本票一律不add不碰；`trendPredictDocs.ts`的diff经逐hunk核对100%本票（§4前已验）；`analysis-shape-520.test.mjs`等在途改动仅作运行底盘（兄弟门37/37绿即其上亦绿）。
- `git diff --cached --name-only`复核（提交前实测，见提交命令行）：只含上4件，他席件0。
- 自己声明路径下干净：收尾`git status --porcelain`自查——4件已提交后，声明路径无残留；`.scratch/t569/`探针与日志不进仓（未受版本控制临时区，见leftover）。
- 超线报警（结构第四步）：已超线，需要根据规则进行重构。本件`trendPredictDocs.ts`939→1006（#518 W1搬家引入的缺口族＋预测族9页同件；本次+67为R-07恢复规则/R-09/R-11派生/detail与注释）。本次先不拆：同一窗口再叠结构变更会把31页产物读数变成两条变更的合成（分不清谁改的）；拆法照台账＝按页族切两件姊妹件（缺口族一件、预测族一件），共用页框提为共件，出口经本件薄转出；待收口票认领。

## 附 审查者纪律四段

### 一、做了什么
- 模拟减重族8条：R-07两列表窄档恢复表头＋列式对齐（第二档，`::before`命中0/右缘极差0）；R-08当刻已绿加锁（不动）；R-09对账后收判断至徽标一处（结论两主数＋detail总缺口）；R-10主标/chip留空格（09–13五页零命中）；R-11 detail改期间总量（LCP=0）；R-12结论卡28/800（CDP双档证最大）；R-13卡16px页侧覆盖；R-14元素侧零空串加锁（字面两处归#567）；另R-03同形延伸10–13眉题/chip去重。

### 二、怎么量的
- 机器六门全经持锁包装器exit0：分隔31/31、响应31×3零、靶向（t569 9/9＋兄弟冻结37/37）、告警91/91、GEN-CHECK键128；31页重出＋逐页sha（07–09同75389B系同宽数巧合，01=23同字节）；真机CDP硬读数（字号/内距/表头/标签/右缘）；手机07/10双vision无硬伤（桌面初读三误判已由CDP纠正）；变异MUT1/MUT2各RED 8/1→GREEN 9/9＋还原逐字节一致。

### 三、还剩什么
- 本族8条清零（R-08/R-14为“当刻已成立＋加锁”，无源码改）；17页连带forecast-CSS＋176B（同装配，一行未多改）；公共层字面（14px/data-label选择器）与跨族（R-61表卡左缘/R-62按钮行/壳宽）归#567/#573；`trendPredictDocs.ts`1006行仍超线待收口票拆两件。

### 四、需要别人接什么
- 不push、不close，关票由编排者做；请#570席知悉14–17已含hero第4格＋16px（只增益，可其上继续）；请编排者确认R-07第二档与R-09对账裁定（t518“—”分支不触发）两处定档。
