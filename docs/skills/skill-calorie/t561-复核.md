# t561 独立复核（席 561C-R）：目标域屏上来源脚注删除

> 被审对象：`561C` 回执 —— 判定 `DONE`，提交 `9c6710befa70407e540ae3c8e3417f1b5d9a7a18`，
> 机器证据三条 `GATE-RUN`（fcf42509 编译／6f03d9fd 用例／d8a991c9 14 件专跑）。
>
> 本席身份：**独立复核（不是实施者本人）**。复核手段三件：① 圈定提交范围；② 独立重跑（新 runId）；
> ③ **自设试法**——被审用例覆盖不到的一路（真身变异 ＋ 另一条独立探针），受控变异在持锁窗口内做完并逐字节还原。
> 本席不 push、不动别席在途件、不改被审的两件源码与那件测试。

## 一、复核结论

**判定：PASS（84/100）**——三条机器证据独立复现全绿；提交范围严格两件；受控真身变异「必红／还原必绿」四行读数齐；
复制载荷里的来源名一字未动（本席在编译产物上自证）。

**无 S1**：没有把别席在途件带进提交（`git show --name-only` 只有那两件）；没有改测试迁就实现
（测试件自 `cdf229a` 入库后与 HEAD／工作区逐字节一致，本席一行未改）；用户可见无回归
（五个回执页 ＋ 三个结果页的**屏上**去掉的是照裁决该去掉的那一行，复制区、页内导航、KPI 卡、表全在）。

**扣分与非 S1 缺陷**（见 §五）：证据件「接手席补提交」那一节**并未入 HEAD**（回执"内容已逐字入 HEAD"与盘上不符）；
被审用例的判据 ④「变异自证」是**自我指涉的字串检查**，不是真身变异（真正的兜底是判据 ① 的「原始 HTML 零命中」）；
被审用例只看 25 条唤醒词，没覆盖本票两件还被哪些页面共用（本席补了 8 页直调口）。

## 二、第 1 件：圈定提交范围（多一件即 S1）

命令与读数（全部在 D:\ilife，只读）：

| 读数 | 结果 |
|---|---|
| `git show --stat 9c6710b` | `packages/skill-calorie/src/goal/receipt.ts \| 8 +++++---`；`packages/skill-calorie/src/goal/resultDocs.ts \| 19 +++++++++----------`；`2 files changed, 14 insertions(+), 13 deletions(-)` |
| `git show --pretty=format: --name-only 9c6710b` | 只有上述两条；证据件与测试件**不在**该提交（第一席那半在 `cdf229a`） |
| `git merge-base --is-ancestor 9c6710b HEAD` | `exit=0`（该提交在 HEAD 历史里，不是孤儿） |
| `git diff --stat HEAD -- packages/skill-calorie/src/goal` | **空**（工作区这两件与 HEAD 一致） |
| `git hash-object` 两件（工作区） | `e893c2498d536af6ae5a966fa766b5206ffa15d9`、`ac89afd7bd8047f164a8bbd0e769560f9a61fed8` |
| `git rev-parse 9c6710b:<两件>` | 同上两个 blob（**盘上那份＝入库那份，逐字节**） |
| `git ls-tree 9c6710b -- docs/skills/skill-calorie/t561-goal-no-source.md` | 在册（说明该提交确实带上了证据件在它的上一版状态） |
| `git diff --stat HEAD -- packages/skill-calorie/test/t561-goal-no-source.test.mjs` | **空**（测试件与 HEAD 一致，两席都没动过它） |
| `git diff --stat HEAD -- packages/skill-calorie/src/goal`（变异窗口末次复验） | **空**；`git status --porcelain` 同路径**无输出** |

**别席在途件说明**：三件在途件（`src/weight/log.ts`、`src/workout/commands.ts`、`docs/…/t449-字段中文标签.md` 等）
在**工作区**有改动，但既不在 `9c6710b` 里，也不在 `HEAD` 与工作区之差里（本票两件之外本席一处未碰）。

## 三、第 2 件：独立重跑（新 runId，只信本席退出码与摘要行）

| 序 | 命令（一律 `node tooling/run-locked.mjs --ticket 561C-R -- <命令>`，各占一次持锁窗口） | runId | waitedMs | exit |
|---|---|---|---|---|
| ① | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | `6930b89d-51d9-4766-b147-5ddabcb4c40c` | 40018 | **0** |
| ② | `node --test packages/skill-calorie/test/t561-goal-no-source.test.mjs` | `31313556-8f99-4707-b097-8c3947dfa4b8` | 20008 | **0** |
| ③ | `node .scratch/t561c/r/probe-561c-R.mjs --label independent` | `21d43ae7-6445-44d5-9142-59bce0766d09` | 0 | **0** |

摘要行（逐字）：

- ① `RESULT: ticket=561C-R runId=6930b89d-51d9-4766-b147-5ddabcb4c40c waitedMs=40018 exit=0`
- ② `ℹ tests 4`／`ℹ pass 4`／`ℹ fail 0`（`cancelled 0`／`skipped 0`／`todo 0`）；
  用例内自证行 `T561-MUT 结果页改坏红=1 还原绿=1；回执页改坏红=1 还原绿=1`；
  `RESULT: ticket=561C-R runId=31313556-8f99-4707-b097-8c3947dfa4b8 waitedMs=20008 exit=0`
- ③ `T561C-R-PROBE 结果：8/8 页过（屏上零命中 ＋ 载荷来源仍在）；RED=0`；
  `RESULT: ticket=561C-R runId=21d43ae7-6445-44d5-9142-59bce0766d09 waitedMs=0 exit=0`

> 与实施席读数对账：编译／用例两条与派单给的三条 `GATE-RUN` 同结论（本席不复用其 runId）。
> 第三件（`d8a991c9` 14 件专跑）按票面「非验收命令」处理，本席**不重跑**，改用下面的独立探针做同一件事的另一路。

## 四、第 3 件：自设试法（被审用例覆盖不到的那一路）

### 4.1 为什么要有这一路

被审用例的判据 ④（第 203-213 行）把 `result.vis + '…数据来源…'` 这种**字串拼接**当成「变异」，
断言 `mutResult.includes('数据来源')`——它证的是 `String.prototype.includes` 会命中，**没有碰一行源码**，
不管实现好坏都必然绿。所以「判据必红」这件事必须由**真身变异**来证，这一路是本席自设的。

### 4.2 受控真身变异（字节级，全在持锁窗口内，跑完逐字节还原）

驱动：`.scratch/t561c/r/mutate-561c-R.ps1`（变异常量逐字取自 `git show 9c6710b` 被删的那一行）；
变异点：`receipt.ts` 的 `content` 数组尾（第 221 行 `].join('')` 之前），把 `renderCaliberLine('数据来源：本机目标库 ｜ …')` 加回。

| 序 | 读数 | 值 |
|---|---|---|
| 1 | 备份 `receipt.ts` sha256（改前＝HEAD 那份） | `7B099260195F7E35905972F0172C0060E0DEF5F9C4887E7B0EBD3337E43A67CB`（13192 字节） |
| 2 | 变异后字节数／`git diff --numstat` | 13334（＋104）；`1→0` 插入 1 行、**删除 0 行**（只加回那一行，没换行尾、没改别的） |
| 3 | 编译（锁内直调，不复用包装器） | `exit=0` |
| 4 | **自设探针（红）** | `exit=1`；`T561C-R-PROBE 结果：3/8 页过；RED=5`——五个回执页逐页 `屏上=命中 数据来源／本机目标库`，三个结果页仍 `PASS`，八页 `载荷=来源仍在` |
| 5 | **被审用例（红）** | `exit=1`；失败签名 `AssertionError: 「定营养目标」calorie.goal.set 屏上还有来源脚注：数据来源：本机目标库`／`「定营养目标」屏上还印着回执脚注里的来源名 本机目标库` |
| 6 | 还原（备份逐字节回写） | sha256 回到 `7B099260195F7E35905972F0172C0060E0DEF5F9C4887E7B0EBD3337E43A67CB`，与备份一致 `True` |
| 7 | **还原后自证（绿，两趟各一次）** | 探针 `exit=0`（`8/8 页过；RED=0`）；用例 `exit=0`（`fail 0`） |
| 8 | 还原到位的独立读数 | `git diff --stat HEAD -- packages/skill-calorie/src/goal` **空**；两件 blob 逐字回到 `e893c24…`／`ac89afd…`；`git status --porcelain` 该路径**空** |

**红／还原一致两行机器读数**：

```
红   ：probeExit=1（3/8 页过，RED=5，五个回执页屏上命中原来源行）＋ caseExit=1（AssertionError 屏上还有来源脚注：数据来源：本机目标库）
还原 ：probeExit=0（8/8 页过，RED=0）＋ caseExit=0（fail 0）；receipt.ts sha256 与备份一致=True
```

### 4.3 另一条独立探针（与被审用例不同路）

`.scratch/t561c/r/probe-561c-R.mjs`：**不 spawn CLI、不 import 被审用例**，进程内直调编译产物——
`cli/write.js::dispatchWrite` 五条写命令（真回执）＋ `goal/read.js::viewGoal／viewGoalVsActual／viewGoalExpiring`
（被改的三个结果页）。临时库 `mkdtemp` ＋ `seedFull()` ＋ `CALORIE_TODAY=2026-09-07`；
自设判据两行：**A 屏上**（剥 `data-t` 载荷后的可见文本）`数据来源`／`本机目标库` 零命中；
**B 载荷**（同页 `data-t`）里来源名仍在。

现况读数（`--label independent`，runId `21d43ae7…`）：**8/8 页 A／B 双绿**，
`calorie.goal.set／water／weight／pause／resume`（回执）＋ `view.goal／view.goal-vs-actual／view.goal-expiring`（结果）。

## 五、缺陷与未做项（如实记账）

1. **证据件「接手席补提交」那一节没入库**（非 S1，但回执说法与盘上不符）：
   - 盘上：`docs/skills/skill-calorie/t561-goal-no-source.md` 的 `§六 补提交（接手席）`（含 `§六.1`～`§六.4`）确实存在，
     `git status --porcelain` 记为 `M `，与 HEAD 之差是**插入**（第一席 §一～§九 未被改写——`git diff --numstat` 与盘上首节一致）。
   - 但 `git grep -c 86dd890 HEAD -- <该件>` **无输出**；`git show HEAD:<该件>` 的结尾停在第一席的
     `- 视觉终审（真浏览器量尺、肉眼逐页）不在实施者票面内，留负责人。`；证据件在库内只有 `cdf229a` 一条历史。
   - 佐证：该件里提到的被带走提交 `86dd890` **不是 HEAD 的祖先**（`git merge-base --is-ancestor 86dd890 HEAD` → `exit=1`），
     reflog 显示 `1905ada HEAD@{6}: reset: moving to HEAD~2`（把 `86dd890` 挤出历史）——
     与证据件 §六.4 自述的「别席未限定路径的整索引提交把暂存项带走」相符。故 `§六 补提交` 目前**只在工作区**，
     按票面禁 `reset／amend／rebase`，本席**不代为提交**（会改别席在途未完成件），只如实记在这里。
   - 影响面：源码与测试两件在库里；只有这条**交接证据**不入库。
2. **被审用例判据 ④ 不是真身变异**（自我指涉）：见 §4.1。真正的兜底是判据 ① 的「原样 HTML 里 `数据来源` 零命中」
   （第 149-150 行）——这一条有兜底力（本席的真身变异让它与判据 ② 一起变红）。**建议后续把判据 ④ 改成真身变异**，
   别留「假自证」。
3. **覆盖范围只到目标域 25 条唤醒词**：本票两件还被别家页面共用（`goal-receipt-253.test.mjs`、`goal-result-254.test.mjs`
   走同一批装配口），本席补的 8 页直调口覆盖了这两件的出口，但**未**逐页覆盖 `render/goalPlate.ts` 那一族
   （`goal-config`／`goal-status`／`goal-recommend`）与 `render/trendDocs.ts` 的 `goal-predict`——
   它们不在本提交的写集里，本席只做越界观察不判其缺陷。
4. **`src/goal/precheck.ts:31-32` 那句注释**「数据来源一句话（页面上给用户看的…）」引的常量 `GOAL_SOURCE`
   实际只进复制载荷；该件不在本提交写集（`git show --name-only` 无它），本席不改，记一笔以免后票误读。
5. **未做**：`git push`（票面禁）；真浏览器量尺与肉眼终审（留负责人）；别席在途件一律未碰未提交。
6. **并发留痕**：本轮持锁期间至少三次排在别席之后（`#544`／`#369`／`#157` 等 ticket 的 `WAIT-OWNER-ALIVE`），
   本席全程按 `run-locked` 抢锁，没有抢回活锁、没有并行跑两条门。本席自己的驱动脚本前三趟各有自身缺陷
   （含换行正则与 `]).join('')` 字面量定位两次失败），**每趟都以 `finally` 逐字节还原并自证**，
   盘上终态与 HEAD 逐字节一致（§4.2 第 8 行）。

## 六、本席产物（不入被审提交的写集）

- `.scratch/t561c/r/probe-561c-R.mjs`（独立探针）、`mutate-561c-R.ps1`（受控变异驱动）、
  `run-mut.ps1`／`run-independent.ps1`（两次入口）、`mut-window.txt`／`independent.txt`（入口总日志）、
  `build-*.txt`／`probe-*.txt`／`case-*.txt`（逐趟读数）、`receipt.ts.backup`（变异前备份）。
- 本文件 `docs/skills/skill-calorie/t561-复核.md`（本席作者，随本席署名提交；不改被审两件与测试件）。
