# #536 对比族两页重排（对比体脂／对比围度）· 证据件

> 席位＝#536 唯一作者（对抗审查与终审由别的席位做，本件**不含自评**）。
> 开工 HEAD＝`4d2f0341b7dffaad5ee9064c21451f7c84d1c4c3`。基线读数＝`.scratch/m158v2/视觉基线-回收.md`（两页改前 55／—，族均 54/100）。

## 0. 工具与复现口径

| 项 | 值 |
|---|---|
| 出页 | `node .scratch/m158v2/run-pages.mjs --ticket 536 --out .scratch/m158v2/t536/stage --db .scratch/m158v2/t536/tmpdb --words 对比体脂,对比围度`（真库零写入：库路径一律由 `SKILLS_DB_PATH` 指 tmpdb） |
| 分隔符探针 | `packages/skill-calorie/scripts/audit-separators.mjs --dir <页群> --json <落点>`，读数**以节点级为准** |
| 三档溢出 | `packages/skill-calorie/scripts/measure-responsive.mjs --dir <页群> --label t536`（真 Chrome CDP，390／768／1440） |
| 锁 | **本票改用 `--lock-dir .scratch/locks-m158v2`**：共用 `.scratch/locks/gate.lock` 被一个 vision 审查类进程占着（`owner.json` 的 `cmd` 含 `visionreview`，pid 3064／ticket 523），按席位简报 §1 的例外条款偏离，逐次都在证据里记账 |

## 1. 改前地面真值（本席位当场复核，与 #534 清单一致）

### 1.1 「10-对比体脂」改前可见文本（原句）

```
卡路里·运动身体
对比体脂 2026-09-05 ~ 2026-09-05 vs 2026-09-07 ~ 2026-09-07 体脂对比 -1% 变化率 -4.76%
第一段 21% 2026-09-05 ~ 2026-09-05 · 1 条  第二段 20% 2026-09-07 ~ 2026-09-07 · 1 条
变化率 -4.76% 全部来源  体脂两期对比（差值 -1% · 变化率 -4.76%）
期别 区间 均值(%) 条数 差值 变化率  第一段 2026-09-05 ~ 2026-09-05 21 1 — —  第二段 …
```

### 1.2 改前节点级探针读数

| 页 | 节点级命中 | 明细 |
|---|---|---|
| `10-对比体脂.html` | **7** | 页头题名 `卡路里·运动身体`（head）＋副标题 `2026-09-05 ~ 2026-09-05 vs 2026-09-07 ~ 2026-09-07`（R6）＋两条卡明细 `起 ~ 止 · N 条`（R1+R6）＋表题 `差值 -1% · 变化率 -4.76%`（R1）＋两个表格日期格 `起 ~ 止`（R6） |
| `11-对比围度.html` | **2** | 页头题名 `卡路里·运动身体`（head）＋卡明细 `胸/腰/腹/臀/肩/大腿/小腿/手臂/前臂(左+右)`（R3，`/` 切 9 段） |

（`--dir` 扫的是**产物页群目录**；`run-pages.mjs` 的夹具种子页自 2026-09-15 起落 `<OUT>/_seeds/`，不进分母。）

## 2. 「10-对比体脂」（`compare.ts` · `buildBodyCompositionCompareDoc`）

### 2.1 三档读数

| 档 | 改前 | 改后 |
|---|---|---|
| 390 | 溢出 0 | 溢出 0 |
| 768 | 溢出 0 | 溢出 0 |
| 1440 | 溢出 0 | 溢出 0 |

### 2.2 分隔符探针（节点级：7 → 0 行级 2 → 0）

### 2.3 改了什么（逐条）

| # | 债（改前） | 形状（改后） |
|---|---|---|
| ① | `<title>卡路里·运动身体` | `卡路里身体细节`（页签文字里落不下形状，就去掉 `·` 只写一个词） |
| ② | 眉标 `calorie.view.body-composition-compare · 运动身体域`（内部命令名上屏） | 页头走 B 线路：左槽 `体脂率两期对比`＋类型徽章 `身体细节`，内部叫法出页面 |
| ③ | 副标题两个区间硬串 `起 ~ 止 vs 起 ~ 止` | 撤；B 线路结论行改一句人话「把两段的体脂率记录各自求平均，看这一段比上一段变了多少。」 |
| ④ | 两条卡明细 `起 ~ 止 · N 条` | 两张期别卡只留均值＋一句说明；期别区间由表格的 `日期` 列承载 |
| ⑤ | 表题与卡重复同一组数 | 表题改写成读法「第一段是基准，第二段的差值与变化率都以它为准。」；差值／变化率只由结论条说一次 |
| ⑥ | 表格首行差值格整行 `—` | 首行差值格写 `基准`（缺值语义不再靠猜）＋口径行「两段的体脂率按各自那段的记录求平均，来源：…」 |
| ⑦ | 未接手机端配方 | `assembleDocPage({...(4), pageUi: true, metaLeft, badge, summary})` |

## 3. 「11-对比围度」（`compare.ts` · `buildBodyMeasureCompareDoc`）

### 3.1 三档读数

| 档 | 改前 | 改后 |
|---|---|---|
| 390 | 溢出 0 | 溢出 0 |
| 768 | 溢出 0 | 溢出 0 |
| 1440 | 溢出 0 | 溢出 0 |

### 3.2 分隔符探针（节点级：2 → 0 行级 2 → 0）

### 3.3 改了什么（逐条）

| # | 债（改前） | 形状（改后） |
|---|---|---|
| ① | 9 个部位挤一行 `胸/腰/腹/臀/肩/大腿/小腿/手臂/前臂(左+右)` | 卡明细只留口径句；部位名单由表格 `部位` 列逐行承载 |
| ② | 读数卡 `围度对比 2026-09-05 vs 2026-09-07`（英文裸词＋日期作卡值） | 结论条「两个日期之间有 N 个部位的围度变了（前一次记录是 …，后一次是 …）」；两张卡改 `有变化的部位`／`比较的部位` |
| ③ | 表题 `围度两期对比（… vs …，N 项有变化）` | 表题只写读法「前一次记录是基准，差值与变化率都以后一次减前一次算。」 |
| ④ | 页头题名／眉标同上 | 同 §2.3 ①② |
| ⑤ | 未接手机端配方 | `pageUi: true` |

## 4. 构建与在途件

- `tsc -b packages/skill-calorie`：作业期间出现过**别人件**报红（`src/photo/gifDoc.ts` 的 `renderMediaFigure` 坏 import、`src/photo/viewerDoc.ts` 参数数），按编排者口径跑 `.scratch/m158v2/fix-dist.mjs` 补 dist（**只动 dist 产物，不入版本库**）后继续；本席位收尾时 `tsc -b` 已 `exit=0`。
- `src/body/compare.ts.t527bak`（14:10 落盘、未跟踪）**不是本席位建的**（本席位进场时它已在盘上），一行未碰、未删，留给编排者处置。
- 本票写集内件的实测 LF（超线即第四步）：`compare.ts` 收尾见 §5。

## 5. 收口读数

### 5.0 二次收口（编排者 5 处版式复核后的重排）

编排者截真渲染图复核后点出 5 处（1280 档内容只占约 2/3／复制按钮各占半宽／缺「对比」可视化／
`来源：全部来源` 连说两次「来源」与半角减号／390 档结论条括号跨行），外加一条机器读数（390 档
`div.type-badge` 11px 低于本波「390 档正文类 ≥12px」下线）。逐条落法：

| # | 复核意见 | 落法 |
|---|---|---|
| ① | 两张大卡各装三行小字，内容只占视口约 2/3 | 撤两张宽卡，改**事实条**（`renderFactStrip`：第一段均值／第二段均值／差值）＋**对照带**一行；表仍 6 列 |
| ② | 复制按钮各占半宽（约 480×44） | 新立页内样式件 `src/body/compareUi.ts`（`<style>`，仓内既有落法见 `receiptUi.ts:22`）：≥641px 时动作行改 `flex-start` ＋ 按钮 `width:auto`／`min-width:132px` |
| ③ | 缺「对比」可视化 | 对照带改 `renderDistributionRows`（名称栏 `21% → 20%` ＋ 条 ＋ 右栏差值）；围度页同件给「变化最大的 6 个部位」条列 |
| ④ | `来源：全部来源` 重复、数值半角减号 | 口径行改「两段的体脂率按各自那段的记录求平均，全部来源都算在内。」；两页数值负号一律全角 `−`（U+2212），围度表 13 行同步 |
| ⑤ | 390 档结论条括号跨行 | 结论条去括号，改逗号连写「…，相对 −4.76%。」 |
| ⑥ | 390 档 `div.type-badge` 11px | `compareUi.ts` 里补一条 `@media (max-width: 640px){ .type-badge{font-size:12px} }`（只改字号，配色内距仍走公共层） |

> 中途踩到一次：`renderListRows` 的 `right` 槽是**纯文本**（会被转义），第一版把迷你条的 HTML 塞进去，
> 截图上直接印出 `<span class=…>`。改用 `renderDistributionRows`（条与数值**同生一件**）后归位——
> 这条记在这里，供别的席位避坑。

### 5.0.1 拆分（告警线第四步）

二次收口把 `compare.ts` 撑到 **367 LF**（越过 350 告警线）：

> **已超线，需要根据规则进行重构。** 超因：这一件同时住着「两页取数＋参数解析」（`viewBodyCompositionCompare`／
> `viewBodyMeasureCompare`／`resolveCompositionPeriods`／两个 `build*View`）与「两页整页装配」（事实条／对照带／
> 表／口径行／复制区／页头三件），本票给两页加形状时装配面继续变长。

拆法（本票当场执行，不等收口票）＝**按页族切装配面**：
`src/body/compareCompositionDoc.ts`（对比体脂页装配，102 LF）＋
`src/body/compareMeasureDoc.ts`（对比围度页装配，96 LF）；
两页共用的小件（`DOC_TITLE`／`DOC_VERSION`／`DOC_SKILL`／`BADGE`／`PAGE_UI`／`rangeText`／两个视图类型）
留 `compare.ts` 并改导出，`compare.ts` 只留取数与**薄转出**（`buildBodyCompositionCompareDoc`／
`buildBodyMeasureCompareDoc` 出口名与签名一字不变，调用方零改动）。
拆后读数：`compare.ts` **203**／`compareCompositionDoc.ts` **102**／`compareMeasureDoc.ts` **96**／
`compareUi.ts` **27**（四件全在 350 线内）。

> 台账未同步（`check-warning-line.mjs` 仍报红，但报的全是**别人件**：`src/body/bodyDocs.ts 371`／
> `src/home/homeViewParts.ts 367`／`src/photo/gifDoc.ts 351` 漏挂号，`src/photo/receipt.ts`／`src/home/homeDocs.ts`
> 台账陈化）。**跑 `--sync` 会把别人那些行一起改掉**——按「不许动别人写集」的铁律，本席位**不跑**，
> 如实记在这里交编排者统一同步（本票四件都不在需要挂号的名单里）。

**二次收口后读数**：分隔符探针 `RESULT: 2/2` ＋ `PASS`（两页节点级 0）；三档 `OVERFLOW-ZERO pages=2 cells=6 failed=0`；
本族测试 `pass 3／fail 0`。截图：`.scratch/m158v2/shots/536-10-1280.png`（1280×900）、`536-10-390.png`（390×900）、
`536-11-1280.png`（1280×1291）、`536-11-390.png`（390×2405）。

### 5.1 改后可见文本（原样，供终审比对）

```
卡路里 身体细节
体脂率两期对比 身体细节 对比体脂 把两段的体脂率记录各自求平均，看这一段比上一段变了多少。
第一段均值 21%   第二段均值 20%   差值 −1 个百分点
21% → 20%（对照带：条 ＋ 右栏 −1 个百分点）
体脂率均值从 21% 到 20%，下降 1 个百分点，相对 −4.76%。
第一段是基准，第二段的差值与变化率都以它为准。
期别 日期 体脂率均值 记录条数 比基准 变化率
第一段 2026-09-05 21% 1 基准 —   第二段 2026-09-07 20% 1 −1% −4.76%
两段的体脂率按各自那段的记录求平均，全部来源都算在内。
```

```
卡路里 身体细节
围度两期对比 身体细节 对比围度 把两个日期的围度记录摆在一起，逐部位看变了多少。
围度变了 13 个部位   两个日期都比过 13 个部位   前一次记录 2026-09-05   后一次记录 2026-09-07
变化最大的 6 个部位（套条形：肩围 +3cm／胸围 +2cm／左前臂 +2cm／右前臂 −2cm／腹围 +1.5cm／腰围 −1cm）
两个日期之间有 13 个部位的围度变了，后一次记录是 2026-09-07。
前一次记录是基准，差值与变化率都以后一次减前一次算。
部位 前一次(cm) 后一次(cm) 差值(cm) 变化率 …（13 行逐部位）
只有两个日期都有记录的部位才比。差值与变化率都是后一次减前一次，单位厘米。
```

### 5.2 收口读数（原文）

| 项 | 原文 |
|---|---|
| 分隔符探针（两页页群目录） | `FILE 10-对比体脂.html  node·0 ；0 ≥3段0 ｜0 、0 ~0 标识符0  命中=0` ／ `FILE 11-对比围度.html  node·0 ；0 ≥3段0 ｜0 、0 ~0 标识符0  命中=0` → `RESULT: 2/2` ＋ `PASS`（exit 0） |
| 三档溢出 | `10-对比体脂.html 390档: 0 ✓ 768档: 0 ✓ 1440档: 0 ✓` ／ `11-对比围度.html 同上` → `OVERFLOW-ZERO … failed=0` |
| 本族测试 | `node packages/skill-calorie/test/body-compare-536.test.mjs` → `pass 3／fail 0` |
| `compare.ts` 实测 LF | **323**（告警线 350 以内，不触发第四步） |
| 真库 | 全程 `SKILLS_DB_PATH` 指 `.scratch/m158v2/t536/tmpdb`，真库 `D:\2Study\StudyNotes\.db\calorie_data.db` 零写入 |

### 5.1 改后可见文本（原样，供终审比对）

```
卡路里身体细节
体脂率两期对比 身体细节 对比体脂 把两段的体脂率记录各自求平均，看这一段比上一段变了多少。
第一段 21% 这一段的体脂率均值 第二段 20% 这一段的体脂率均值
体脂率均值从 21% 到 20%，下降 1 个百分点（变化率 -4.76%）
第一段是基准，第二段的差值与变化率都以它为准。
期别 日期 体脂率均值 记录条数 比基准 变化率
第一段 2026-09-05 21% 1 基准 —   第二段 2026-09-07 20% 1 -1% -4.76%
两段的体脂率按各自那段的记录求平均，来源：全部来源。
```

```
卡路里身体细节
围度两期对比 身体细节 对比围度 把两个日期的围度记录摆在一起，逐部位看变了多少。
有变化的部位 13 项 差值不为 0 的部位   比较的部位 13 项 两个日期都有记录的部位
两个日期之间有 13 个部位的围度变了（前一次记录是 2026-09-05，后一次是 2026-09-07）
前一次记录是基准，差值与变化率都以后一次减前一次算。
部位 前一次(cm) 后一次(cm) 差值(cm) 变化率 …（13 行逐部位）
只有两个日期都有记录的部位才比。差值与变化率都是后一次减前一次，单位厘米。
```

## 6. 留给别人裁的（本席位不自评）

1. `src/body/compare.ts.t527bak`（14:10:36 落盘、14189 B、未跟踪）**不是本席位建的**（进场时已在盘上），本席位未删、未提交——请编排者裁是否移除。
2. 本席位与 #527 席位**同时改过 `compare.ts`**：14:10:36 那一版把两处 `subtitle:` 行的**值**改成 `null`（本席位当刻的编辑是删掉整行），本席位按类型要求（`DocPageInput.subtitle` 必填）补回 `subtitle: null` 并各留一行注释。**该件是 #536 写集**，#527 席位若还要动它，请走编排者协调。
3. `<title>` 取值：本席位用 `卡路里身体细节`（去 `·`）。同域的 `bodyDocs.ts`／`receipt.ts` 各有自持的一份（`卡路里·运动身体`／`卡路里·身体细节回执`），**不属本票写集**，域内要不要统一请终审裁。

