# #293 收官批量 · 收尾席证据 ②（收官窗口 · 票号 `293close2`）

**本件地位**：`t293-收尾-证据.md`（票号 `293close`，2026-09-14 08:0x 窗口）的对账窗口**已被本件取代**——那一次全量门红（`#108/#109/#110` 既存日期腐坏），本件是修好之后在**当刻 HEAD** 上的**全绿收官窗口**。取代关系照协议 §7 规则 41③ 明写：**旧件原文未改**（已在 `05258ec` 入仓），本件为**新增**而非收窄。

**起点自复算**：`HEAD == origin/master == 85c8f4625435771b392450b0abb66d7a8f4c9ddf`；`git status --porcelain -uall` **空**。
**判官 sha256**：`78AE857426A44D66E81A2489E51D0D2790BD16A0812DB78818B1D2E918BEBE39`（未改）。
**窗口**：本机 2026-09-14 08:29–08:31（+08:00）；UTC `2026-09-14T00:30:02Z → 00:30:54Z`；RUN 条目在 `.scratch/locks-land/gate-runs.log`，导出为 `docs/skills/skill-calorie/t293-gate-runs.log`。
**口径**：每一条各自经 `node tooling/run-locked.mjs --ticket 293close2 --lock-dir .scratch/locks-land --stale-minutes 30 -- <命令>`（窗口内 direct node，不嵌套）；**未使用任何 `--allow-*`**。

## 一、七项读数（全绿）

| # | 项 | runId | exit | 读数 |
|---|---|---|---|---|
| 1 | `tsc -b`（仓根） | `7e5edb90-c94e-485f-a13a-758c2d5339e9` | 0 | 无错 |
| 2 | `tsc -b packages/skill-calorie --force`（规则 38） | `36904195-9e00-426b-9a8e-806bc260ef4d` | 0 | 无错 |
| 3 | `gen-cli.mjs --stamp`（规则 38 重签） | `fbe6b81b-b71c-41f2-9474-39103f18fa4c` | 0 | 印记重签 |
| 4 | `gen-cli.mjs --check` | `0a6c0507-29db-45c1-a185-8504217eac0a` | 0 | `GEN-CHECK PASS：**键 101（写 35 ＋ 读 66）**；能力 10 个声明 101 条，**未搬迁清单 0 条**` |
| 5 | 全量 `node --test <15 个包模式>` | `726aa620-aecb-4a11-9d34-214d5e1d5475` | 0 | `tests 1492 / pass 1492 / fail 0` |
| 6 | 冻结件 `test/calorie-routing-81.test.mjs` | `df9eca95-7f5e-4ab1-b8cc-ebc5825298a1` | 0 | `tests 8 / pass 8 / fail 0` |
| 7 | 判官 `t293-验收-命令自治.mjs` | `15f2528b-fc0b-4fe7-b985-ce7f2e5ef302` | 0 | `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS **5/5**`；`**UNACCOUNTED=0 处**` |
| 8 | 快照 `tooling/skill-html-snapshot.mjs --check` | `f69c7e40-9808-4019-ac80-0163e26c649f` | 0 | `artifacts=**186** changed=**0** added=0 removed=0`，base-* 指纹 `ef7cc02969e37e4bfc0eb35751b8dc1c` |

## 二、事先声明（`GATE-RUN`；runId 与 `cmd` 逐字取自本窗导出件）

- GATE-RUN runId=7e5edb90-c94e-485f-a13a-758c2d5339e9 cmd="node node_modules/typescript/bin/tsc -b"
- GATE-RUN runId=36904195-9e00-426b-9a8e-806bc260ef4d cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force"
- GATE-RUN runId=fbe6b81b-b71c-41f2-9474-39103f18fa4c cmd="node packages/skill-calorie/scripts/gen-cli.mjs --stamp"
- GATE-RUN runId=0a6c0507-29db-45c1-a185-8504217eac0a cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"
- GATE-RUN runId=726aa620-aecb-4a11-9d34-214d5e1d5475 cmd="node --test test/*.test.mjs packages/base-render/test/*.test.mjs packages/skill-calorie/test/*.test.mjs packages/skill-memo-ilife/test/*.test.mjs packages/skill-schedule/test/*.test.mjs packages/skill-home/test/*.test.mjs packages/skill-bill/test/*.test.mjs packages/skill-chef/test/*.test.mjs packages/plugin-manager/test/*.test.mjs packages/plugin-calorie/test/*.test.mjs packages/plugin-memo-ilife/test/*.test.mjs packages/plugin-schedule-ilife/test/*.test.mjs packages/plugin-home-ilife/test/*.test.mjs packages/plugin-chef/test/*.test.mjs packages/plugin-bill-ilife/test/*.test.mjs"
- GATE-RUN runId=df9eca95-7f5e-4ab1-b8cc-ebc5825298a1 cmd="node --test test/calorie-routing-81.test.mjs"
- GATE-RUN runId=15f2528b-fc0b-4fe7-b985-ce7f2e5ef302 cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
- GATE-RUN runId=f69c7e40-9808-4019-ac80-0163e26c649f cmd="node tooling/skill-html-snapshot.mjs --check"

## 三、对账读数（`node tooling/check-gate-audit.mjs`，票号 `293close2`，**未用任何 `--allow-*`**）

- **对账 run#1（本件尚无声明）**：runId `891aacd2-64de-4061-b707-1faad885d8db` ⇒ `matched=0/0 auditEntries=418 scoped=8 undeclared=8` ＋ **gate-audit: FAIL**（「证据里没有任何 `GATE-RUN runId=… cmd=…` 声明」）——「先声明后对账」的**空转红对照**：窗口内 8 条 RUN 一条都逃不掉（`--export` 已随该次写出受跟踪导出件）。
- **对账 run#2（补上 §二 的 8 条声明后）**：runId `7e9abb61-294b-44ba-becd-a72c89d3d215` ⇒ `matched=**8/8** auditEntries=8 scoped=8 undeclared=**0**` ＋ **gate-audit: PASS**（exit 0）。
- **`--allow-undeclared` 已退休（S3-8 目标达成）**：本窗口是**单票号窗口**（`--ticket 293close2`），`undeclared` **不加任何放宽即为 0**；导出件按票分文件（`t293-gate-runs.log`）；设计红／变异读数约定用 `MUT-RUN` 前缀与门禁 `GATE-RUN` 分列（本批未跑变异）。⇒ **对账读数与门禁读数一致（绿对绿）**。

## 四、与上一窗口的差异（为什么这次能全绿）

- 上一窗口（票号 `293close`）唯一的红＝`#108/#109/#110` **既存日期腐坏**：查询窗口 `{"window":"7d"}` 由 `src/shared/params.ts:93` 的 `windowRange()` 按当刻解析，而三件同质测试的种子写死 `2026-09-05~09-07` ⇒ 当刻滑过 09-07 即必红。
- 修复提交 **`85c8f46`**（只改测试面：三件 `packages/skill-calorie/test/*.test.mjs` ＋ 新增 `pin-clock.mjs`，232 增 0 删）把「当刻」**钉到种子锚点**（`#326` 同款手法），并在 **+0／+30／+365 天**三个「现在」下复验全绿。
- 本窗口即该修复后在当刻 HEAD 上的**独立复跑**：七项全绿（见 §一）。

## 五、未完成项（本席记账，不开新票）

- **`src/fetch/body.ts:111` 的 `daysAgo()` 直接读 `new Date()`**，落在 `CALORIE_TODAY` 契约之外 —— 本轮修复席为此改用**钉 `Date` 的手法**才覆盖到它。⇒ 建议后续票（或 `#328`／`#330` 顺带）把 `daysAgo()` 归到 `CALORIE_TODAY` 契约内，使测试可用契约注入时间、不必全局钉 `Date`。
- `#324r` S3-3 的 **dist 孤儿**：清单（30 基名 × 120 件，逐条理由）已成，**本批不删**（`dist/fetch/photos.js` 有活消费者：`test/render-t10.test.mjs`／`test/skill-t11.test.mjs` 经 `src/photo/helpLookup.ts:35-41` 的 `module:` 串动态 import）⇒ 转 `#328` 先改旧路径文案，再由后续批删。详见 `t293-收尾-证据.md` §一 A-3。
- **本题材的变异（`MUT-RUN`）本批未跑**；A-4 只落口径（单票号窗口／导出按票分文件／`MUT-RUN` 前缀与门禁 `GATE-RUN` 分列／退休 `--allow-undeclared`）。
