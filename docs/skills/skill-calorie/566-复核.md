# #566 复核（独立复核席）

> 被审对象：`0cbb0b0`（实现，3 件）＋ `ad4a92d`（证据件补记 GATE-RUN 段，1 件），均未 push。
> 复核席只信自己的退出码与读数：编译、被审用例、邻接回归、自设探针与受控变异全部经
> `node tooling/run-locked.mjs --ticket 566-R -- <命令>` 重跑（runId 见 §二／§三）。
> 复核席写集只有本件（`docs/skills/skill-calorie/566-复核.md`）与 `.scratch/t566-R/` 下的探针与日志；
> 源码只在受控变异里临时改一行，还原后`resultDocs.ts` SHA256 与提交态逐字节相同（§三）。
> 实施者的结论一律不采信——下面每条读数都是复核席自己跑出来的。

## 一、圈定范围

| 提交 | 件 | 在票面写集内 | 说明 |
|---|---|---|---|
| `0cbb0b0` | `packages/skill-calorie/src/goal/resultDocs.ts` | 是 | 唯一动过的源码件；`1 file changed, 21 insertions(+), 4 deletions(-)` |
| `0cbb0b0` | `packages/skill-calorie/test/566-result-title.test.mjs` | 是 | 新建用例（LF=266） |
| `0cbb0b0` | `docs/skills/skill-calorie/566-证据.md` | 是 | 新建证据件（LF=194） |
| `ad4a92d` | `docs/skills/skill-calorie/566-证据.md` | 是 | 只补记 §九 的 GATE-RUN 行与提交 sha |

- **工作区与提交一致**：`git status --porcelain -- <上面三件>` 无输出、`git diff HEAD --stat -- <上面三件>` 无输出。
- **源码 diff 逐行核**（`git show 0cbb0b0 -- packages/skill-calorie/src/goal/resultDocs.ts`）：只有 ①件头注释补一段；
  ②目录内私用小件 `windowLineOf(metaLeft, start, end)`；③`buildGoalDoc` 的 `title:`／`metaLeft:` 各一行；
  ④`buildGoalVsActualDoc` 的同两行。`buildGoalExpiringDoc`、三块读数卡、缺口与摄入、逐日表、复制区、
  唤醒词／命令名／参数名／退出码**一行未动**；`src/goal/read.ts` 与取数三函数不在 diff 里（取数口径不可能被本票动到）。
- **禁区未越**：提交里没有 `src/goal/` 之外的件；既有测试一件未改（两件交付里一件是新用例、一件是证据件）。
- **提交链**：`0cbb0b0` ← `ad4a92d`；其后别席 `b5fc9f9`（565）与 `cdfb939`（565 文档）落在这条链上，
  复核时 HEAD＝`cdfb939`——本票两件在历史里、不在顶端，属同仓多席并发常态。
- 复核期间工作树里别席的件在途（`packages/skill-calorie/src/goal/precheck.ts` 等为 `M`），复核席一行未碰。

## 二、独立重跑（新 runId，只信自己的退出码）

| GATE-RUN | 命令 | exit | 摘要行 |
|---|---|---|---|
| `482a3d03-cd97-4848-93e0-895b5472edd5` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | 0 | 空跑（源码与 dist／tsbuildinfo 都判定为最新，tsbuildinfo mtime 未变）；包当刻无红 |
| `983bf702-302d-454d-bf97-5eba0596f59f` | `node --test packages/skill-calorie/test/566-result-title.test.mjs` | 0 | `tests 5 / pass 5 / fail 0`＋`T566-MUT 字串级：日期加回 H1 红=3、窗口抠出页头 红=3、窗口全挖掉 红=3；还原绿=3` |
| `2fd6f6de-e6bb-4ec4-a5b6-5b1d2b28dabc` | `node .scratch/t566-R/probe.mjs`（复核席探针，§三） | 0 | `T566R-PROBE cases=8 ok=8 red=0` |
| `83bac442-46d0-48af-b7e2-53e453a147ed` | `node .scratch/t566-R/mutate.mjs`（受控变异，§三） | 0 | 场内外来读数：变异后被审用例绿／探针红；还原后两条都绿、SHA 一致；两轮真编译 |
| `af31642a-92aa-48be-99bf-9770f299c7ed` | `node --test …/goal-result-254.test.mjs …/t561-goal-no-source.test.mjs …/goal-weight-b1-390.test.mjs` | 0 | `tests 10 / pass 10 / fail 0`（邻接回归，与实施席报的同一组） |

编译口径说明：票面判定「编译真跑过」＝退出码 0 且 `tsconfig.tsbuildinfo` mtime 更新。本次重跑时
`tsc -b` 判定已是最新、**未重写 tsbuildinfo**（mtime 前后同值 `2026-09-15T17:18:12Z`），所以这一行不作为
「真跑过」的证据；真编译的证据取 §三 变异轮的两行：`17:18:12Z → 17:20:09Z`（变异后重建）与
`17:20:09Z → 17:20:15Z`（还原后重建），两轮 `exit=0`。

## 三、新试法（自设，打被审用例覆盖不到的面）

### 3.1 复核席探针 `.scratch/t566-R/probe.mjs`（8 件，真 CLI ＋ 临时种子库）

被审用例只跑 3 件（`calorie.view.goal` 的 `7d`／`30d` 与 `goal-vs-actual` 的 `30d`），日期判据只有
ISO 一种写法，页头判据钉死实施者选的那串字。复核席换三处口径：

- **换窗口参数路径**：`{"window":"90d"}`（被审用例没跑的窗口，种子数据日 2026-09-07 ⇒ 区间应为 2026-06-10 至 2026-09-07）
  与显式区间 `{"start":"2026-08-25","end":"2026-09-03"}`（走 `defaultRange` 的 start／end 那一支，故意挑一个不在墙上任何窗口里的区间）。
- **日期判据用宽口径**：ISO ＋「2026年9月1日」＋「9月1日」三种写法（同 `.scratch/t153-视觉缺陷/probe.mjs` 负责人肉眼那一条）。
- **不抄实施者的字面串**：从产物可见文本里抓区间原文，再要求页头 `meta-bar` 左行含它、并断言 H1 与请求的窗口
  逐字对上（`window:Nd` 那条路径另算天数距）、可见文本里出现 ≥ 2 次；对照件（无窗口那一页）页头左行必须逐字照旧。

| 件（命令） | `<h1>` | 页头 `meta-bar` 左行 | 可见文本里区间 | 判 |
|---|---|---|---|---|
| `goal-7d` `{"window":"7d"}` | `🎯 目标分析` | `看目标完成度 · 目标管理 · 窗口 2026-09-01 至 2026-09-07` | 1 段 ×3 次 | OK |
| `goal-30d` `{"window":"30d"}` | `🎯 目标分析` | `… · 窗口 2026-08-09 至 2026-09-07` | 1 段 ×3 次 | OK |
| **`goal-90d`** `{"window":"90d"}`（被审用例没跑） | `🎯 目标分析` | `… · 窗口 2026-06-10 至 2026-09-07` | 1 段 ×3 次；天数距＝90 | OK |
| **`goal-range`** `{"start":"2026-08-25","end":"2026-09-03"}`（另一条参数路径） | `🎯 目标分析` | `… · 窗口 2026-08-25 至 2026-09-03` | 1 段 ×3 次 | OK |
| `vs-30d` `{"window":"30d"}` | `🎯 目标对比实际` | `看目标对比实际 · 目标管理 · 窗口 2026-08-09 至 2026-09-07` | 1 段 ×3 次 | OK |
| **`vs-range`** `{"start":"2026-08-25","end":"2026-09-03"}` | `🎯 目标对比实际` | `… · 窗口 2026-08-25 至 2026-09-03` | 1 段 ×3 次 | OK |
| **`goal-30d-range`** 同窗口走显式区间 | `🎯 目标分析` | 与 `goal-30d` **逐字相同**；`metrics` 逐字段相同 | 1 段 ×3 次 | OK |
| `expiring`（对照件，本票未动） | `⏳ 目标到期提醒` | `看即将到期的目标 · 目标管理`（逐字照旧） | 0 次 | OK |

结论：窗口区间在两条参数路径上都跟着**请求的那个窗口**走（90 天窗与显式区间都咬得住），H1 一律只剩页面名，
对照件零变化。

### 3.2 受控变异（真身、整场一个持锁窗口、还原写在 `finally` 并回读比 SHA256）

- **变异点**（一行，`.scratch/t566-R/mutate.mjs`）：`buildGoalDoc` 的 `title: '🎯 目标分析',` 改成
  `title: v.start === '2026-06-10' ? '🎯 目标分析 ' + v.start + ' 至 ' + v.end : '🎯 目标分析',`
  ——即**只把 90 天窗那一个窗口的日期写回 H1**，故意挑被审用例从没跑过的窗口。
- **红读数**：`node --test packages/skill-calorie/test/566-result-title.test.mjs` → `tests 5 / pass 5 / fail 0`，
  `exit 0`（**被审用例看不见这一改**）；`node .scratch/t566-R/probe.mjs` → `exit 1`，
  `T566R-PROBE RED: goal-90d → 「goal-90d」`calorie.view.goal`{"window":"90d"} 的 H1 不是「只留页面名」："🎯 目标分析 2026-06-10 至 2026-09-07"`。
  两轮重建都 `exit 0`，`tsbuildinfo` mtime 随之更新（编译真跑过）。
- **还原一致**：`resultDocs.ts` SHA256 变异前＝还原后＝
  `A54B9C0D4B3764B9EC6B4E5D1CA7CDC968BD576B070908D56EB83A0C8E9FD106`；还原后
  `git status --porcelain --` 该件无输出、`git diff HEAD --stat --` 该件无输出；`dist/goal/resultDocs.js` 里
  变异串 `2026-06-10` 已消失。
- **绿读数**（还原后重建复跑）：被审用例 `tests 5 / pass 5 / fail 0`；探针 `T566R-PROBE cases=8 ok=8 red=0`。

## 四、肉眼看产物（负责人会看的那个面）

产物重出在 `.scratch/t566-R/out/`，用系统 Chrome 截图后由视觉链读数（截图在
`.dsh-vision-router/artifacts/` 下；判据不只看字串断言）：

| 面 | 截图口径 | 视觉链逐字读数 | 判 |
|---|---|---|---|
| `goal-7d` 顶部 | 1280×620 | 小字行＝`看目标完成度 · 目标管理 · 窗口 2026-09-01 至 2026-09-07`；大标题＝`🎯 目标分析`（无日期）；无文字被裁掉或叠住 | 对 |
| `goal-7d` 窄屏 | 390×620 | 小字行整行完整（无省略号、无裁断），右侧「目标分析」徽章独立不叠 | 对 |
| `vs-range` 顶部（显式区间，实施席没看过的件） | 1280×560 | 小字行＝`看目标对比实际 · 目标管理 · 窗口 2026-08-25 至 2026-09-03`；大标题＝`目标对比实际`，无日期 | 对 |
| `goal-90d` 整页 | 1280（整页高 1227） | 大标题＝`目标分析` 无日期；页头小字、卡副说明、表题三处的区间都是 `2026-06-10 至 2026-09-07`，逐处一致 | 对 |

## 五、证据核对（实施席的读数能不能复现、有没有编）

1. **`gate-runs.log` 逐条在册**（`.scratch/locks/gate-runs.log`，`ticket=566` 的 `RUN` 行）：8 条 runId 与
   `cmd`／`exit` 与证据件 §九 逐条对上——`a187d281…`（`before.mjs before`，exit 0）、`44c23a27…`／`76ecaa60…`
   （`tsc -b`，exit 1）、`d0ffd872…`（`tsc -b`，exit 0）、`d3b1d1fa…`（`before.mjs after`，exit 0）、
   `d4b9a9b0…`（用例，exit 0）、`df566ee9…`（`mutate-cycle.mjs`，exit 0）、`35f74e9c…`（邻接回归，exit 0）、
   `4b6830e0…`（`git commit`，exit 0）。**没有编出来的 runId**。
   （顺带一笔：`0cbb0b0` 的提交正文把 `44c23a27` 误写成 `d44c23a27`，证据件 §九 与日志里都是对的——只是提交正文笔误，不影响读数。）
2. **“外来红”是真的外来**：`.scratch/t566/tsc-2.log` 的红行逐字为
   `packages/skill-calorie/src/exercise/sportUi.ts(30,22): error TS2305: Module '"base-paint"' has no exported member 'renderChips'.`
   ——红行指向的件不在本票写集内（别席在途件），与证据件 §六「外来红记录」一致。
3. **改前读数可复算**：`git cat-file blob 564e124:packages/skill-calorie/src/goal/resultDocs.ts` 的
   SHA256＝`7734E81E84E585A6D307F758EED926C775A16613913BE81610F2A1F2F5444BC2`、LF＝269，与证据件 §一 逐字相同
   （改前 4 件读数在 `.scratch/t566/before.json`，与 §三 表逐字段对得上）。
4. **产物可复现**：复核席用真 CLI ＋ 临时种子库重出的 `goal-7d`／`goal-30d`／`vs-30d`／`expiring` 与实施席
   `.scratch/t566/after/*.html` **逐字符相同**（只差页内时间戳 `2026-09-16 01:09:44` → `01:20:18`）。
5. **用例里冻结的 metrics 有出处**：`before.json` 里 3 件的字段名在用例里逐个都能找到（13／13／5 个字段，缺失 0 个）。
6. **数据安全**：实施席与复核席的实跑都用 `mkdtemp` ＋ `SKILLS_DB_PATH` 指临时库 ＋ `CALORIE_TODAY=SEED_TODAY`，
   真库一次未碰；本票没有写库操作（只读页面）。

## 六、缺陷逐条

1. **本票范围（S3，非引入）· 判据宽度不够**：`test/566-result-title.test.mjs` 的日期判据只认 `\d{4}-\d{2}-\d{2}`
   一种写法，且只跑 `7d`／`30d` 两个窗口。复核席的变异实测：把日期写回 H1（只对 90 天窗生效）时，**被审用例 5/5 全绿**，
   是复核席探针才把它咬红。也就是说「日期回到标题」这类改动只要换了写法或换了窗口，就会从这道门下面走过去。
   建议下一轮补两条：宽口径日期正则 ＋ 至少第三个窗口（或显式区间）的实跑。
2. **本票范围（S3，非引入）· 验收墙那 3 件仍是改前形状**：`docs/skills/skill-calorie/scene06-验收墙/` 里
   `看目标完成度.html`／`看目标历史完成.html`／`看目标对比实际.html` 的 `<h1>` 仍逐字带日期（复核席当刻扫描：
   33 件里命中 3 件，就是这 3 件），负责人在墙上看仍见那条被点名的缺陷。原因是票面两句话互斥——
   「只许动 `src/goal/resultDocs.ts`：`src/goal/` 之外一律不碰」与「3 件产物都要改到」不能同时成立，而墙在 `docs/` 下。
   实施席按写集办事、并在证据件「未做项」里如实登记，没有隐瞒；但**交付面需要编排者裁定**：要么另开一票重生成墙，
   要么明确「墙不是本票交付面、负责人看临时产物」。
3. **范围外发现（S3）**：`scene06-验收墙/produce.mjs` 记着一条与本题无关的旧事实（`看目标完成率(按周)` 与
   `(按月)` 都路由到同一条窗口、屏幕上看不出差别）。本票不处理，登记在此备查。

## 七、判定与打分

**判定：PASS**（没有 S1；重跑与自设探针都符合预期）。

| 项 | 分 | 依据 |
|---|---|---|
| 契约一致 | 29／30 | 票面四条验收（H1 只留页面名／窗口仍上屏且住页头控件／metrics 逐字段不变／完整文档四断言）在 3 件产物上逐条真跑通过，另加 90 天窗与显式区间两条路径也通过。扣 1：交付面按「3 件产物都要改到」读，验收墙上那 3 件仍是改前形状（写集与交付面互斥，非实施席失误） |
| 证据真实可复现 | 25／25 | 8 条 GATE-RUN 逐条在册且退出码一致；「外来红」红行逐字复核为真；改前 SHA256／LF 与文档一致；4 件产物可逐字符复现；用例冻结的 metrics 有出处；实跑全在临时库 |
| 新旧对照 | 20／20 | 改前／改后 4 件读数（含对照件零变化）齐全，复核席独立复出同一结果 |
| 工程红线 | 15／15 | 提交只含写集内的件；无 push／切分支／reset／stash／amend；编译、用例、变异、提交全经持锁包装器；真库未碰；无越界件 |
| 文档同步 | 9／10 | 证据件按结构纪律必报五步写齐（影响清单／结构设计／写代码／超线报警 286<350／交付对账），未做项如实登记。扣 1：§五把「判据咬得住」说满了——ISO 一种写法＋两个窗口的覆盖面没如实标注（见 §六 第 1 条） |
| **合计** | **98／100** | |

## 八、复核席未做项与下一手

- 未重生成验收墙（不在复核席写集，且属 §六 第 2 条要编排者裁定的交付面问题）。
- 未跑全仓测试面，只跑了被审用例 ＋ 邻接 3 件（10 条）＋ 自设探针 8 件；本票只动一件只读装配件，面已够。
- 未 push（照票面「不要 push」）。
- 下一手建议（编排者裁）：①补一条宽口径日期 ＋ 窗口没覆盖到的用例判据；②裁定验收墙是否重生成。
