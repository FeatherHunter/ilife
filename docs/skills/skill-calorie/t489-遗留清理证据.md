# t489 残留变异锚点清理证据（身体细节 body.ts）

## 一、目标与基线（以票面五段为准，上游简报只参考）

- 目标：删 `packages/skill-calorie/src/fetch/body.ts:317/392` 两处行尾注释 `// M1-ANCHOR` / `// M2-ANCHOR`（#398 变异电池遗留，全仓无引用，无行为影响）。
- 开工前实测基线：body.ts LF=455；`M1-ANCHOR|M2-ANCHOR` 在 `packages/skill-calorie/**` 命中 2（仅 body.ts:317/392）；AGENTS.md 台账行 `src/fetch/body.ts|369|455` 在位；两靶向测试存在；t489 证据/scratch/changeset 均空。

## 二、结构必报五步

- 第一步·影响清单：`src/fetch/body.ts`（删 2 行尾注释，fetch 能力）＋ `AGENTS.md`（视 --dry 定点，不动他人行）＋ 本证据件 ＋ `.scratch/t489/impl-*` ＋ `.changeset/t489-*`。无新增目录，无跨能力。
- 第二步·结构设计：无新增目录树、无公开接口变化（只删注释，导出数不变），无共用件。
- 第三步·写代码：同能力目录内注释删除，不跨能力引用，常量类型不动；其它注释语义不动。
- 第四步·超线报警：已超线，需要根据规则进行重构。超因沿台账 #398 挂号原文（读侧来源词与按来源分组取数，同既有写侧校验＋围度取数同处一件）；本次先不拆，拆法归结构票（挂号 369：写侧校验／围度取数／体成分取数／删前快照取数四面）。
- 第五步·交付对账：实际改动＝第一步清单之 body.ts（2 行尾注释）＋本证据件＋对账件＋changeset；AGENTS.md 未动（见 §六）；偏差：行尾注释删除不改变 LF（455→455），故台账本行无需同步。

## 三、验收读数

- ① 检索：`M1-ANCHOR|M2-ANCHOR` 在 `packages/skill-calorie/**` 命中 0（终态，grep 无命中；dist 重编后亦 0）。
- ② 靶向测试：t398（pass 9 fail 0 exit 0，GATE-RUN runId=6a85c710-dcb9-4011-81e6-025fe388eea0）＋ t364（pass 7 fail 0 exit 0，GATE-RUN runId=56271808-6323-4ef3-b8db-811fe8085504）；终态联跑 16/16 exit 0（GATE-RUN runId=26723d0a-5566-4e84-af76-b68e951622c1）。
- ③ 告警线门：`--dry` 显示本票行一致（body.ts 实况 455＝台账 455，无 SYNC-CHANGE）；无参门 exit 1 仅因他票两行陈化（sportPortDocs 台账 763 实况 805；goalProgressDocs 台账 452 实况 446），GATE-RUN runId=19db13f5-d079-4ab0-9192-872c74b16892。未跑 `--sync`（会改写他人行，见 §六）。
- ④ 负向对照：加回两注释 → `ANCHOR-HITS M1=1 M2=1 TOTAL=2` 必红（GATE-RUN runId=1b2aa91b-f812-4996-835c-62211eaf1ab2）；删掉 → `TOTAL=0` 必绿（GATE-RUN runId=59ed5489-59f2-460f-81f4-6fbef0a7f489）。

## 四、真库零写入

- 真库 `D:\2Study\StudyNotes\.db\calorie_data.db` 开工前 3072000B mtime=2026-09-15 15:36:54；收尾复核同值（未变）。
- 测试隔离：运行测试时 `SKILLS_DB_PATH` 指 `.scratch/t489/tmpdb`；两靶向测试本身走 mkdtemp 临时库。

## 五、GATE-RUN 对账

- 本票 9 次持锁运行见 `docs/skills/skill-calorie/t489-gate-runs.log`（自 `.scratch/locks/gate-runs.log` 导出 ticket=489 行，共 18 行 START/RUN 对）。
- 构建：`npx tsc -b packages/skill-calorie` exit 0（runId=0ac72700-3166-4b7b-b1a2-4bf6c957c5ad）；变异还原后 `--force` 重编 exit 0（runId=ce73a6fe-9687-4599-be42-54ab27b0949b），dist 无锚点残留。
- 锁等待：开工时锁被 #467/#351/#444/#464/#544 依次持有且 pid 存活，一律未抢回；共等待约 19 分钟后取锁成功。`--dry` 窗等待 #351（runId=t351fr-regress，活进程）60 秒。

## 六、AGENTS.md 未动说明

- `--dry` 的 SYNC-PLAN 只涉及他人在途件（sportPortDocs、goalProgressDocs），按共享台账纪律停手不上报、不跑 `--sync`、不定点改（他人行在窗口内持续变化：作业期间 sportPortDocs 763→805→809）。
- 本票 body.ts 行 LF 不变（行尾注释删除，455→455），台账本行与实况一致，无需同步。故提交不含 AGENTS.md，提交信息不点名它（点名了才是假话）。

## 七、未做项与下一手

- 未做：告警线门全绿（卡在他票两行陈化）；`--sync`（待他票落点、台账静止后由编排者协调）。
- 下一手：他票提交后重跑 `node packages/skill-calorie/scripts/check-warning-line.mjs`，若本票行仍一致则无需动作；若届时门全绿，t489 即补齐验收③。
