# #296 恢复开工清单（下次开工的 session 照这份跑）

> ## 体裁判断：恢复开工清单（依据写在这里，两次实测都留痕）
>
> **判定：受众是「停机冻结、要恢复开工的其他场景图 session」，所以这份是恢复开工清单，不是即时通知。**
>
> 依据分两段，第二段是第一段的订正：
>
> **第一段（2026-09-13 16:07）：停机冻结仍成立。** 四条实测：
> 1. `.scratch/locks/gate.lock` **不存在**，`.scratch/locks/` 下只有 `gate-runs.log`、**没有** `owner.json`；
> 2. 日志末条是 `RUN ticket=295 runId=7ca660b1 … cmd="… git push origin master"` `at=2026-09-13T08:01:26.185Z`
>    （＝北京时间 16:01），**结束动作是 push**，之后无 `START`；
> 3. `git status --porcelain` **空**、`HEAD == origin/master`；
> 4. `.scratch/orchestrator/` 最新过程稿是 `transfer-publish-gates.md`（16:00），此后无新稿。
>
> **第二段（16:10–16:11 实测 ＋ 编排者确认）：确有施工进程在跑，但它是本图自己的返修席，不是场景图 session。**
> 我的 `git status` 露出两件**未提交**改动（`package.json` 的 `build` 加 `gen-cli.mjs --stamp` 一步、
> `gen-cli.mjs` 把新鲜度守卫从按时间戳改成按内容印记），`dist/.gen-inputs.json`／`dist/weight/commands.js`／
> `src/cli/keys.ts`／两个 `tsconfig.tsbuildinfo` 的时间戳从 16:10:57 连续前进到 16:11:01。
> **编排者已确认：那是他派的 `#295` 返修席在修 A4 那条假红自锁，不是场景图开工。**
> 体重图 `#154` 仍被 `#296` 阻塞，其余场景图仍在冻结。
>
> **所以体裁是恢复开工清单**：受众＝停机后要恢复开工的其他场景图；
> 本图返修期确有本图自己的施工进程在跑，那不影响受众是谁。
>
> 下次开工时要不要改成**即时通知**，由编排者按当时实况重判——判据是「**有没有场景图 session 在跑**」，
> 而不是「有没有任何进程在跑」（本图自己的施工进程不算）。
>
> ⚠ **附带一条已由编排者处置的异常（留痕）**：16:10–16:11 有 build／gen 在写 `dist/` 与 `tsconfig.tsbuildinfo`，
> 而 `gate-runs.log` 同期**没有新 `START`**。编排者已按 `docs/subagent-concurrency-protocol.md`
> §2.4.1／§6 处置：令该席停下、自述、并在持锁下重跑全部证据；**无锁那批运行一律不作为证据**。

**新形状一句话**：一条命令的事实只有一个定义地，就是它自己的能力目录 `src/<能力>/commands.ts`；
所有共用位（设计正本里叫「汇总位」，本纪律一律叫「共用位」）都是那个定义地的**派生件**
（`pnpm gen` 重生成、`pnpm gen:check` 守）。
参考实现＝`packages/skill-calorie/src/weight/`（9 条命令，读 5 写 4）。

---

## 一 · 开工前三件事（顺序不要换）

1. **`git pull`**——`#294`／`#295` 的接缝与生成物已入仓，基线不对后面全白做。
2. **看 `git status --short`**：**出现不属于你的路径就停下报告**——别 `git add`、别还原、别问都不问就往下干。
   那是别人的未提交工作（本图起草期亲历过一次：工作树里躺着另一个席位的两件改动）。
3. **确认 `.scratch/locks/gate.lock` 不存在**。
   冻结期出过一次**锁龄 68 分钟的死锁**，它会让抢锁动作无限等待。
   见到锁先按 `docs/subagent-concurrency-protocol.md` §2／§2.4 判活再处置
   （抢回判据是**先探活**，不是只看锁龄；**别照抄旧脚本**）。
   处置完把结论报告编排者。

## 二 · 你要跑的三步

```
# 1 搬一条命令：拷声明进能力目录，手删 legacyCommands.ts 那一行
#   六件事写全：kind／key／shape／title／wakeWord／example（example 必填，照抄即能跑）

# 2 生成链（四步，少一步会静默停在旧键数）
node tooling/run-locked.mjs --ticket <你的票号> -- pnpm build
node tooling/run-locked.mjs --ticket <你的票号> -- pnpm gen
node tooling/run-locked.mjs --ticket <你的票号> -- pnpm build
node tooling/run-locked.mjs --ticket <你的票号> -- pnpm help:build

# 3 自证
node tooling/run-locked.mjs --ticket <你的票号> -- pnpm gen:check
node docs/skills/skill-calorie/t293-验收-命令自治.mjs     # P1 的 UNACCOUNTED 必须为 0
```

## 三 · 你该改哪儿、不该改哪儿

| 面 | 你动它吗 |
| --- | --- |
| `src/<你的能力>/commands.ts` ＋ 该能力目录内的实现 | **只动这里**（加命令的全部动作） |
| 各共用位（生成物） | **不动**，由 `pnpm gen` 写；手改会被 `gen:check` 抓红 |
| `packages/skill-calorie/SKILL.md` | **不动**，由 `pnpm help:build` 重生成（它**不在** `gen:check` 的四件名单里）；「例」列另由 `pnpm help:examples:check` 守 |
| `src/cli/legacyCommands.ts` | 搬迁时**手删你那条**；它全场共用，**动前抢锁** |
| `src/triggers/routing.ts` | 要能被唤醒词命中就得动；手写路由表，**动前抢锁，只加／删自己那一行** |
| 页面内容、命令补齐 | **不归你**：归各自场景的图（体重归 `#154`） |
| 门（`gen:check`、两个棘轮、CI 步骤） | **只许更强不许更松**；`continue-on-error` 全仓禁用 |

**撞车怎么办**：开工前声明自己会写的路径集合；**跨图撞车停下报告，不自行改别人的行**。
两个 session 同搬两个场景 = 抢 `legacyCommands.ts` 这一支笔，这是已知未解的结构问题（归 `#297`）。

## 四 · 两件不看你也会踩的事

- **写命令多一处**：加／删一条**写**命令还要补逐键落库断言
  （`packages/skill-calorie/test/cmd-write-40-persist.test.mjs`）与 `docs/research/t97-probe-receipts.mjs` 的场景行。
  读命令没有这面（实测 `test/` 下 0 件必改）。
- **`GEN-STALE FAIL` 可能是守卫自身的缺陷**：陈旧守卫拦的是「声明变了没重建」这个假绿，**报红先照医嘱重建**
  （重建链要经根 `package.json` 的 `build`，别绕过去直接调 `tsc`）。
  照做后仍红的，是守卫自身的缺陷（`#295` 复审报过一条：判据曾按时间戳比较，正常动作刷新时间戳即误报且解不开；
  见 `t295-复审-红队.md` 的 A4——本清单写稿时这条正在被修，**别把本文件当它的进度**）。
  被它挡住时**不要删文件、不要改门**，报告编排者。

## 五 · 想知道「为什么这么定」时读哪儿

| 想知道 | 读 |
| --- | --- |
| 规矩的来意、判据、违反会坏在哪 | `docs/agents/命令登记纪律.md` |
| 你要照做的动作清单（可整段贴给别的 session） | `docs/agents/命令登记纪律-照抄说明.md` |
| 参考实现 | `packages/skill-calorie/src/weight/` |
| 改前 6 处共用写点 → 改后 0 处的账 | `docs/skills/skill-calorie/命令登记与分派-冲突为何消失.html` |
| 并发／抢锁／提交纪律 | `docs/subagent-concurrency-protocol.md` |
| 结构形状（五条铁律） | `docs/agents/structure.md` |
