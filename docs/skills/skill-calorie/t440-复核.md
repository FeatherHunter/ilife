# #440 独立复核（围度部位中文名单一来源并统一「肩围」）

- 复核对象：`449573fe…`（主体 12 件）＋ `45a069af…`（证据补笔＋对账快照 2 件），已推送到 `origin/master`。
- 复核者现场：`.scratch/t440-review/`（本复核者独占，**未复用**作者 `.scratch/t440/` 的任何脚本／种子／数据）；本件写完即清。
- 复核时点：2026-09-14 21:23–21:33（本地）。复核时 HEAD＝`75b80d8`（#445），两笔被审提交在其祖先链上（`git merge-base --is-ancestor 45a069af HEAD` → true）。
- 被审 9 件（6 实现＋3 测试）在当刻工作区与 `45a069af` 的 blob **逐件同 sha**（`git hash-object` 对比，见 §一）。
- 结论：**PASS**（票面 6 条验收逐条自跑成立；实现与用词无缺陷）。另记 4 处非阻断缺陷（§七），其中 2 处在**本票范围内**且建议关票前收口。

## 一、提交范围核验

`git show --name-status` 逐个核（`A`／`M` 各、无 `D`／无改名／无 mode 变更）：

- `449573f`（12 件）：`.changeset/t440-中文名单一来源并统一肩围.md`、`docs/skills/skill-calorie/t440-gate-runs.log`、`docs/skills/skill-calorie/t440-部位名单一来源证据.md`、`src/fetch/body.ts`、`src/body/bodyDocs.ts`、`src/body/compare.ts`、`src/body/log.ts`、`src/body/wizardPlate.ts`、`src/analysis/cross.ts`、`test/t360-measure-trend.test.mjs`、`test/t361-measure-full-table.test.mjs`、`test/t440-部位名单一来源.test.mjs`。
- `45a069a`（2 件）：`docs/skills/skill-calorie/t440-部位名单一来源证据.md`、`docs/skills/skill-calorie/t440-gate-runs.log`。
- 14 件与票面 §交付物路径 逐条相符，**无越声明件**。
- **0 命中成立**：两笔提交的文件清单里没有 `src/triggers/routes.generated.ts`，也没有 `src/triggers/**` 的任何件。
- 中文信息无乱码：提交信息、新件名、`MEASURE_DEFS` 的 13 个中文名逐字正常（`git show` 与 `Get-Content -Encoding UTF8` 两路各读一遍一致）。

| 件 | 工作区 blob | `45a069af` blob | 同否 |
|---|---|---|---|
| `src/fetch/body.ts` | `d00abeae1f` | `d00abeae1f` | 同 |
| `src/analysis/cross.ts` | `d85755391c` | `d85755391c` | 同 |
| `src/body/bodyDocs.ts` | `a9b1058e26` | `a9b1058e26` | 同 |
| `src/body/compare.ts` | `db7acfa9ea` | `db7acfa9ea` | 同 |
| `src/body/wizardPlate.ts` | `91816df859` | `91816df859` | 同 |
| `src/body/log.ts` | `3b488f9676` | `3b488f9676` | 同 |
| `test/t360-measure-trend.test.mjs` | `efee074887` | `efee074887` | 同 |
| `test/t361-measure-full-table.test.mjs` | `d1cdfe6c97` | `d1cdfe6c97` | 同 |
| `test/t440-部位名单一来源.test.mjs` | `94e6f91be8` | `94e6f91be8` | 同 |

（工作区另有他席在途改动，均在 `src/diet/*`／`src/photo/*`／`src/render/*`／`src/weight/*` 等本票写集之外，与本复核无关。）

## 二、验收命令原样复跑（数据自己播种）

种子＝本复核者手定的两期 13 项（`shoulder_cm` 110 / 109，`date1`=30 天前、`date2`=3 天前），库由被审包 `openDb` 建；
命令**逐字照票面**走 `calorie-cmd-read`（`D:\2Study\nodejs\calorie-cmd-read.ps1`；自核它是**指向本仓 `packages/skill-calorie` 的 junction**，
且其 `dist/fetch/body.js` 与本仓该件 sha256 同为 `39AF1DE6…`）。

| 票面条 | 原样命令 | 自跑读数 |
|---|---|---|
| ① | `calorie-cmd-read calorie.view.body-measure`（不带参数） | `exit=0`　剥标签后**肩围=3**、旧词**=0**、13 名齐备 |
| ② | `calorie-cmd-read calorie.view.body-measure-compare --params '{"date1":"2026-08-15","date2":"2026-09-11"}'`（命令名取自 `src/cli/keys.ts:92`） | `exit=0`　**肩围=1**、旧词**=0**、13 名齐备 |
| ③ | 全树检索旧词（`packages/skill-calorie` 全包） | **0 命中** |
| ④ | 中文名定义地 | 只见 `src/fetch/body.ts:33` 一处（表体 `:29-34`） |
| ⑤ | `node --test …/t360-measure-trend.test.mjs` ／ `…/t361-measure-full-table.test.mjs` | 各 `exit=0`（`tests 8 / pass 8 / fail 0` ／ `tests 8 / pass 8 / fail 0`） |
| ⑤′ | 本票新件 `t440-部位名单一来源.test.mjs` ＋ `tsc -b packages/skill-calorie` | `exit=0`（`tests 7 / pass 7 / fail 0`）／ `exit=0` |

④ 的完整读数（自己持锁重跑，静态探针）：

- 唯一来源：`src/fetch/body.ts:29-34` 的 `MEASURE_DEFS`（13 对「列名＋中文名」），导出 `MEASUREMENT_FIELDS`（列序）／`MEASUREMENT_ZH`（名表）／`measureCamelName`。
- 五个消费件全部 `import … from '../fetch/body.js'`：`bodyDocs.ts:15`、`compare.ts:23`、`wizardPlate.ts:15`、`log.ts:14`、`cross.ts:9`；**没有一件另持 13 名表**（编译产物读数：`MEASUREMENT_FIELDS` 13 键、`MEASUREMENT_ZH` 13 值，逐值＝手算冻结表）。
- 其余「肩围／部位名」命中只有 `src/triggers/scene-08-body.ts`、`src/triggers/wake-assets.ts` —— 唤醒词 prompt 正本资产（票面明令禁改），**不是**定义地。
- 单点标签件（≥1 个部位名但不成表）：`analysis/anomaly/weight2.ts`、`analysis/cross.ts`（既有图例 `腰围(cm)`）、`triggers/scene-10-analysis.ts` 各 1，与唯一来源并列即读作「不成第二份表」。

## 三、自设变异／探针（改坏必红／还原必绿，两行机器读数）

持锁区内做（`--run-id t440-review-mutation-1`），**唯一被改的受版本控制件＝`packages/skill-calorie/src/fetch/body.ts`**，每个变异 `finally` 内逐文件点名写回原文并核 sha256。

| 变异 | 做了什么 | 页面 1（围度看） | 页面 2（对比围度） |
|---|---|---|---|
| **M1**（票面 ⑥ 要求的负向对照） | 唯一来源里 `shoulder_cm` 的中文 → 旧词 | 肩围=0 旧词=**3** → **RED** | 肩围=0 旧词=**1** → **RED** |
| **M2**（本复核者新设） | 唯一来源里**另一项**「腰围」→「腰部围长」 | 腰部围长=3、腰围=0 → **RED** | 腰部围长=1、腰围=0 → **RED** |
| **M4**（本复核者新设） | 唯一来源里 `shoulder_cm` 的中文 → 空串 | `exit=1`：`ERR 4 … renderDataTable: input.columns[13].label 必须是非空字符串` → **RED** | `exit=0` 但肩围=0、且**未回退**到列名（`shoulder_cm` 计数 0）→ **RED** |

- **改坏必红**：`MUTATION-RESULT: 3/3`（三条都「两页同时 RED」）。
- **还原必绿**：三次 `RESTORE` 逐次 `sha前=13a6fb1e9ed89330 sha后=13a6fb1e9ed89330 一致=true`；还原后重建＋复跑基线 `RESULT[restored]: 8/8`、`VERDICT[restored]: GREEN`、`RESTORE-FINAL sha=13a6fb1e… expect=13a6fb1e…`。
- 释放锁前另用 git 口径复核：9 件 `git hash-object` 与 `45a069af` 逐件同 blob（§一表），`git status --porcelain` 对这 9 件为空。

另两条静态探针读数：`measureCamelName` 未知键 `foo_bar_baz → fooBarBaz`、空串 `'' → ''`、`shoulder_cm → shoulderCm`（13 项全含 `Cm` 且无 `_`）；
`MEASUREMENT_ZH['shoulder_cm']` 取空串时 `?? f` **不回退**（`'' ?? 'shoulder_cm'` 仍是 `''`）——即 M4 两条读数的成因。

## 四、设计取舍判断：`analysis/cross.ts` 的英文定序常量

- 事实读数：`WAIST_DIVERGENCE_ORDER`（`cross.ts:15-19`）与 `MEASUREMENT_FIELDS` **同集合**（各 13 键、逐键成对），但**次序不同**：`shoulder_cm` 在来源里是**第 13 位（下标 12）**、在 cross 里是**第 5 位（下标 4）**（探针读数：`PROBE shoulderIndex src=12 cross=4 sameOrder=false`）。`#440` 之前 cross 里是同一份内联 13 对字面量，肩部也在第 5 位。
- **判断：不算「同一事实两份来源」的债务；算「同一键集两份字面表」的耦合隐患（低风险，可开小票）**。理由：两处表达的是**两个不同事实**——来源表管「展示列序／SQL 列序」，cross 的常量管「变化 TOP 并列时取前 3 的确定性次序」；两者在 `#440` 之前本就不同。作者把次序原样抠出、只把**中文名**换成引用，是在票面「不许改行为／不许改列序」约束下的正确取舍；反过来把 cross 改成 `MEASUREMENT_FIELDS` 的次序才是**真的改行为**，应当拒绝。
- 残留风险（一句话）：将来往 `MEASUREMENT_FIELDS` 加第 14 个围度字段时，`WAIST_DIVERGENCE_ORDER` 会**静默漏列**，且当刻没有任何用例把两张表的**键集合**钉在一起。
- **转票措辞（建议开票，不阻断 #440）**：
  - 标题：`围度键集对账：cross.ts 的 WAIST_DIVERGENCE_ORDER 与 MEASUREMENT_FIELDS 防静默漏列`
  - 目标：把「两份字面表同集合」变成机械可查，次序**故意不同**的部分原样保留（不许改行为）。
  - 判据：① 新增用例断言 `new Set(WAIST_DIVERGENCE_ORDER)` 与 `new Set(MEASUREMENT_FIELDS)` 相等且各 13 项（现无任何断言）；② 建议把 `WAIST_DIVERGENCE_ORDER` 导出，或用例直接从 `cross.ts` 源码文本抠出（本复核者用的是后一路）；③ 改坏对照：往 `MEASUREMENT_FIELDS` 加一个字段 → 该用例必红（现为**静默绿**）。
  - 不许动：不得把 cross 的次序改成来源表的次序；不得改 `PAIRS`／TOP 取 3 的口径。

## 五、结构核验

- `src/fetch/body.ts` 当刻行数（节点口径 `readFileSync(f,'utf8').split('\n').length - 1`）＝**382**。
- 包内 `AGENTS.md` 台账在**被审提交当刻**仍写 `src/fetch/body.ts｜369` → **陈化**（本票把该件从 369 撑到 382，未同步台账）。**该缺陷已由 #445（`75b80d8`）当场修掉**：台账改四列口径，该行「当场实测」＝382、「挂号值 369 不回改」，并装上机械陈化门。自跑 `node packages/skill-calorie/scripts/check-warning-line.mjs` → `exit=0`、`RESULT: 49/49`、`PASS: 告警线台账齐全且与实况一致`。
- 新增对外导出数＝**2**：`MEASUREMENT_ZH`、`measureCamelName`（均在 `src/fetch/body.ts`；`MEASUREMENT_FIELDS` 是既有导出、只换了实现）。两件都可从包级出口到达（`src/index.ts:9` → `src/fetch/index.ts` 的 `export * from './body.js'`）。`WIZARD_MEASURE_CAMEL`／`WIZARD_MEASURE_LABELS` 只换实现、名字未动；全仓 diff 无第三个新增导出名。
- 碰到的页面件：`body/bodyDocs.ts` 之外还动了 `body/compare.ts`、`body/wizardPlate.ts`（两个页面件）＋ `body/log.ts`（写侧映射）＋ `analysis/cross.ts`（分析）——**六件全在票面写集内**，没有碰写集外的页面件，也没有碰 `src/triggers/**`。

## 六、真库零写入（自核）

真库 `D:\2Study\StudyNotes\.db\calorie_data.db`，**全部读法**：只对文件取 sha256／size／mtime，完整性检查与逐行读都跑在**副本**上（真库本体不以写模式打开）。

- `sha256 = 57ce9d87f5f232822332d7057c2ae8411d99ba7fdecea289f2028e19fc3ffc15`（与作者报的 `57ce9d87…ffc15` **逐位相符**）；`size = 3072000`。
- `mtime = 2026-09-14T05:53:37.729Z`（本地 13:53:37）——**早于**作者窗口（13:03–13:19Z）约 7 小时，独立佐证窗口内零写。
- 副本上 `PRAGMA integrity_check` → `{"integrity_check":"ok"}`；复核前后 `size／mtime／sha256` 三项全等（`与复核前一致=true`）。

## 七、缺陷清单

| # | 级别 | 归属 | 内容与机器读数 | 建议 |
|---|---|---|---|---|
| 1 | S3 | **本票范围**（已由他票修掉） | 本票把 `src/fetch/body.ts` 从 369 撑到 382 行，但包内 `AGENTS.md` 台账当刻仍写 369 → 台账陈化。 | #445（`75b80d8`）已把该行「当场实测」改为 382 并装上陈化门；#440 侧无需再动，**留痕即可** |
| 2 | S3 | **本票范围** | 证据件 §五 写「`t440-gate-runs.log`（**42 行**：START／RUN 成对）」，随件导出的实况是 **57 行**（START 29／RUN 28）→ 陈化读数。 | 把「42 行」改成当刻读数；「成对」需注明**唯一**未配对的是 `t440-git-commit-2`（它的 RUN 行写于快照之后，属结构必然） |
| 3 | S2 | **本票范围** | 机械门禁对账**不过**。作者证据里的声明行写成 `GATE-RUN runId=… exit=…`，**缺 `cmd=`**；`tooling/check-gate-audit.mjs:112-131` 的 `parseClaim` 对无 `cmd=` 的行 `return null`，整条丢弃。原样自跑：`RESULT: matched=0/0 auditEntries=28 scoped=28 undeclared=28`、`gate-audit: FAIL`，三条 FAIL（无声明／**28 条无人声明**／`t440-test-new`、`t440-attribution` 两个 runId 在窗口内重复出现，后者证据里已披露）。 | ① 13 行补 `cmd=`（命令照 §五 表）；② 28 条 `ticket=440` 的 RUN 要么逐条声明、要么按 §2.4-4 **由编排者具名授权**后写 `GATE-RELAX flag=--allow-undeclared reason=…`（**不得自行放宽**）。不影响实现与用词 |
| 4 | S3 | 范围外（票面理由） | 票面裁定理由里「库里现值 **110cm**」**不可复现**：真库 `body_measurements` 只 1 行且 `shoulder_cm IS NULL`；`calorie_data.db.bak_del_*`、`calorie_data_accident_*`、`calorie_data_before_plan_restore.db`、老技能 `.scratch/380-audit/calorie_data_backup_20260814_232217.db` 四份历史副本**肩围全空**；全仓含 110＋肩两串的只有票面、作者证据与新用例（作者是照票面照抄，不算作者的错）。 | 用词裁定本身另有足够支点（软尺环绕量口径、`CHECK` 界 `[20,200]`、唤醒词 prompt 正本、老技能 94 文件写「肩围」）⇒ 结论不动；建议关票前把那句改成可复现的读数或标出该数字的出处 |
| 5 | 备注 | 范围外（老技能参照件计数） | 票面「89 个文件写『肩围』」实测 **94** 个；「1 个文件写『肩宽』」实测**确为 1**（`templates/body_measurements_wizard.html`）。方向与裁定一致，只是计数不实。 | 无需处理 |
| 6 | 备注 | 范围外（既有） | `?? f` 兜不住**空串**：M4 下围度看页 `exit=4` 硬失败、对比页静默出空格。该写法 `#440` 之前就在（`449573f^` 的 `bodyDocs.ts`／`compare.ts` 同为 `MEASURE_ZH[f] ?? f`）⇒ **非本票引入**。 | 可选小票：把回退改成 `|| f` 或加空串断言；不阻断本票 |
| 7 | 备注 | 范围外（票面划出的边界） | 13 名在 `src/triggers/**` prompt 正本里仍有一份字面表（票面明令禁改）⇒ 将来改唯一来源的名字，prompt 正本不会跟着变。 | 用词已裁定、无当下动作；登记为后续启发项 |

## 八、GATE-RUN 对账（本复核者）

对账窗口＝`2026-09-14T13:24:22Z`（含）→ `2026-09-14T13:32:09Z`（含），日志＝`.scratch/locks/gate-runs.log`（未受版本控制，故声明与读数并列在本受跟踪件内）。
窗口外另有一条 `t440-review-accept-1`（`exit=1`，本复核者第一次跑时的 PowerShell 引号未包住 `--params` 的 JSON，属探路失败，**不作为门禁证据**、不在此声明）。

GATE-RUN runId=t440-review-accept-2 cmd=node .scratch/t440-review/accept.mjs --label baseline2
GATE-RUN runId=t440-review-gate-1 cmd=node .scratch/t440-review/gate.mjs
GATE-RUN runId=t440-review-mutation-1 cmd=node .scratch/t440-review/mutation.mjs
GATE-RUN runId=t440-review-gate-2 cmd=node .scratch/t440-review/gate.mjs

自跑对账（只读、无锁：本工具只读文件，不算编译／测试；本件写完即跑，随后才提交）：

- `node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t440-复核.md --log .scratch/locks/gate-runs.log --ticket 440 --since 2026-09-14T13:24:22Z --until 2026-09-14T13:32:09Z`
  → `声称运行 4 条`（证据 :115–:118）、`RESULT: matched=4/4 auditEntries=3558 scoped=4 undeclared=0`、`gate-audit: PASS`。
- 作者侧对账（同样只读，用**它自己导出的**对账源）：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t440-部位名单一来源证据.md --log docs/skills/skill-calorie/t440-gate-runs.log --ticket 440` → `RESULT: matched=0/0 auditEntries=28 scoped=28 undeclared=28`、`gate-audit: FAIL`（见缺陷 3）。

## 九、现场

- 本复核者只新建了本件（`docs/skills/skill-calorie/t440-复核.md`）与草稿 `.scratch/t440-review/`（gitignored，**收尾已按路径守卫整目录清除**——§八 `cmd=` 里点名的脚本随草稿一并清掉，可核标识以 `runId` 与锁审计日志为准；该日志未受版本控制，故 §八 把声明与读数并列写进本件）。
- 未改任何实现件／测试件／他席件；未跑 `pnpm gen`、未跑 `pnpm --filter build`（编译只用 `tsc -b packages/skill-calorie`）；未 stash／checkout--／reset／clean／restore／rebase／amend；未装包；未碰真库写模式与老技能只读目录的写面。
- 变异期唯一被改的受版本控制件是 `src/fetch/body.ts`，逐次还原并核 sha（§三）；收尾以 9 件 `git hash-object` 复核为零残留。
- 锁：4 次经 `tooling/run-locked.mjs --ticket 440`，最长等待 `waitedMs=30024`（30s，另一票持锁），**未触及 10 分钟上限**，无 `LOCK-STOLEN`。
