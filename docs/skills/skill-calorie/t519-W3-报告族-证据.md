# t519 · W3 证据（报告族 · 评分／趋势／对比三页）

> 票：#519（地图 #161 子票）。判据面＝`.scratch/t519/样板-W3/`（29／30／31 三页，
> 由 `.scratch/t519/gen8.mjs` 从当刻 `dist` 重出后按页号拷入，**产物与 `.scratch/t519/样板/` 逐字节同**）。
> 全部命令经加锁包装器 `--ticket 519 --lock-dir .scratch/locks-m158v2`；长输出落 `.scratch/t519/W3-*.log`。
> **不 push、不 close**。本窗范围与「W1 一次性落 8 页」的口径交代见 `t519-W2-报告族-证据.md` §〇。

## 一 · 判定与读数

| # | 判据 | 命令 | 摘要行 | exit |
|---|---|---|---|---|
| J1 | 分隔符与内部标识符 | `node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t519/样板-W3 --json .scratch/t519/W3-sep.json` | **`RESULT: 3/3` ＋ `PASS`**；逐页 `node·0 ；0 ≥3段0 ｜0 、0 ~0 标识符0 命中=0` | 0 |
| J3 | 溢出与裁切 | `node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t519/样板-W3 --widths 390,512,820,1440 --json .scratch/t519/W3-measure.json` | **`OVERFLOW-ZERO pages=3 cells=12 failed=0 scopeOutFailed=0`**；逐页 `clip=0` | 0 |
| J2／J6／J7 | 形状件／主列／KPI 栅格 | `node docs/skills/skill-calorie/t516-判据-版式.mjs --dir .scratch/t519/样板-W3 --widths 390,512,820,1440 --json .scratch/t519/W3-layout.json` | 逐页 `kpi每排=2+2 kpi极差=0 1440列宽=1120/1440 toc=1 caliber=2–3 conclusion=1 越界=0` | 0 |
| J8／J9 | 锚点自洽／三条恒出 | `node .scratch/t519/probes.mjs --dir .scratch/t519/样板-W3` | **`PROBES: 3/3 页三条恒出齐且锚点双向自洽 RESULT: PASS`** | 0 |

逐页读数：

| 页 | 窄档实际字号 | kpi 每排 | kpi 极差 | 1440 列宽 | toc／caliber／conclusion | 徽章／状态徽章 | 导航项 | 孤儿 | 越界 |
|---|---|---|---|---|---|---|---|---|---|
| 29 看综合评分 | 11px（7 档） | 2+2 | 0 | 1120/1440 | 1／2／1 | 3／2 | 4 | 0／0 | 0 |
| 30 看健康趋势 | 11px（9 档） | 2+2 | 0 | 1120/1440 | 1／2／1 | 3／1 | 5 | 0／0 | 0 |
| 31 看健康报告(含对比) | 11px（8 档） | 2+2 | 0 | 1120/1440 | 1／3／1 | 6／1 | 6 | 0／0 | 0 |

## 二 · 本族销掉的 D 条目（基准件 §3.2 口径）

| 条目 | 改前（本族实测） | 改法 | 销账读数 |
|---|---|---|---|
| **D05／R6**（`~` 顶替「至」） | 31 页 KPI 卡「本期」「对比期」的值位写 `2026-09-09 ~ 2026-09-15`，页头副题也写 `本期 … ~ … 对比期 … ~ …`（R6 ×4） | 一律写「至」；两期口径落**结论条**一句人话（`本期 X 至 Y，对比期是本窗口之前同样长的 N 天（A 至 B）`） | J1 R6=0；`analysis-report-384` 的两期窗口断言（授权改写后）仍逐字钉住「恰好两期」 |
| **D14**（含 `·` ＋ ≥3 段并列） | 31 页「前 3 项变化量」卡说明把三条读数拿 `·` 串成一行（R1＋R3） | 卡片只留**变化最大那一条**（同一事实一页一处，逐条读数住下方「前 3 项变化量」小表）＋ 表下补**徽章列**（上升 N 项／下降 M 项／持平 K 项） | J1 R1/R3=0；J2 chip 6 |
| **R1**（`·`） | 29 页「综合评分」卡说明 `0–100 · N 天有记录` | 改逗号分句 `满分 100 分，N 天有记录` | J1 R1=0 |
| **R3**（斜杠并列） | 31 页口径表 `两期同源：都按档案（身高／年龄／性别／活动量）…`（R3 ×3） | 不列举那四个词——它们已逐行住在「计算口径」表里（同一条 D09 的落点），口径句改成不重复的等价句 | J1 R3=0 |
| **R5**（`、`） | 31 页口径表 `紧邻本期之前、与本期等长的窗口` | 改逗号分句 `紧邻本期之前，与本期的天数一样长的那一段窗口` | J1 R5=0 |
| **D01／D02／D04／D07** | 三页题名 `·`／徽章 `·`／页头左格 `~ ·`／命令原文上屏（R7） | 同 W1／W2 的同一处源头（底座） | J1 R1/R6/R7=0 |

## 三 · 真体重口径不回退（票面硬指标 4）

| 判据 | 读数 |
|---|---|
| `analysis-report-384.test.mjs` 的 **`#384f 日均总消耗两期同源真体重`**（逐字保留、未改动） | **绿**：`ℹ pass`（该测试内 7d／30d 两窗各自用夹具真体重独立复算 `TO_TDEE(lastWeightIn(...))`，并与 `metrics.tdee`／`metrics.deltaTdee`／Δ 表单元格／方向逐个对账；并断言 `deltaTdee !== 79`） |
| `src/analysis/reportPlate.ts` 的 `energyOfWindow`（`d26d6ec` 的修正） | **本票对该件零改动**（`git diff HEAD -- packages/skill-calorie/src/analysis/reportPlate.ts` 空）；对比期仍走 `energyOfWindow(loadProfile(db), prevSeries).tdee`，不读 `prevSeries.tdee`（那是 70 kg parity 常量） |
| 31 页可见文本 | 结论条与口径表各写了一句「两期同源：都按档案四要素加该期窗口内最后一次称重算……体重有变，这一行就会变，不是与体重无关的固定值」；口径说明表另有一行同义（**同一事实一页一处**：结论条那句是读者扫读面，表里那行是口径面，两者不重复叙述算式） |

## 四 · 变异自证（本窗两处，各打本族自己的一条判据）

```
GATE-RUN runId=53277d6e-face-48ba-af27-0bfad03e0735 cmd="node .scratch/t519/mutate.mjs m6"
GATE-RUN runId=faecbfda-42e3-45ca-bb35-79c32d013abc cmd="node .scratch/t519/mutate.mjs m7"
```

**两行机器读数**（逐字抄电池输出）：

```
MUT-RED m6=red(exit=1 | RESULT: 7/8（对比页 R6 节点级命中）)·判据=J1 分隔符（R6）  m7=red(exit=1 | RESULT: 7/8（评分页 R1 节点级命中）)·判据=J1 分隔符（R1）
MUT-GREEN restored=2/2 m6=green(exit=0 | RESULT: 8/8)  m7=green(exit=0 | RESULT: 8/8)  sha256_16=70018897c057（两处还原前后相等）
```

| 变异 | 改哪一处（锚点唯一命中的 `reportDocScore.ts`） | 判据 | 读数 |
|---|---|---|---|
| `m6` | 对比页「本期」卡的值位：`c.cur.start + ' 至 ' + c.cur.end` → `+ ' ~ ' +` | J1 分隔符（节点级 R6） | **红** `exit=1 ｜ RESULT: 7/8` → 还原后 **绿** `exit=0 ｜ RESULT: 8/8` |
| `m7` | 评分页「综合评分」卡说明：`'满分 100 分，' + …` → `'0–100 · ' + …` | J1 分隔符（节点级 R1） | **红** `exit=1 ｜ RESULT: 7/8` → 还原后 **绿** `exit=0 ｜ RESULT: 8/8` |

两处的 `RESTORE 相等=true`；收尾 `git status --porcelain` **空**（零残留）。

## 五 · 未做项

- **J4／J5** 未做（公共层只读，已改期 [#567](https://github.com/FeatherHunter/ilife/issues/567)）。
- 本族**未新建任何形状**（基准件 §4.3 清单仍为空）。
- 页 31 的**五维走势**仍是五张迷你折线（裁决 5 A 方案，本票只给它补了页内导航与口径行，未改图形）。
- **五维视觉分**归独立视觉席。
