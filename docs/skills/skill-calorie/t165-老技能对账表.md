# t165 老技能对账表 · 场景 03 体重

- 老技能权威表：`D:\2Study\StudyNotes\SKILLS\卡路里\scripts\_triggers.py`（621 KB、UTF-8、4467 行；文档字符串自称「已确认分类：……体重 58……」）
- 新仓：`packages\skill-calorie\src\triggers\scene-03-weight.ts`（63 行，58 条触发词条目）
- 本次为只读取证：没有改任何源码，只新增本文件与 `.scratch\t165\` 下的临时脚本与中间结果。

## 结论摘要

- 老表 `TRIGGERS` 全量 436 条；其中 `category == '体重'` 的 **58 条**，与新仓 58 条数量一致。
- 按唤醒词做的**双向差集都是 0 条**：本场景没有「新仓有、老表没有」的幻觉词，也没有漏抄的老词。
- 老表 58 条与新仓 58 条逐字段比较：只有 `main_prompt.cli`（58 条）和 `data_source`（58 条）两处不同，其余 17 个字段（含 `main_prompt.text`、`prompt_template`、`key`、`subfunction`、`output_type`、`html_template`、`data_fields`、`order` 等）**逐字相同**。

## 一、双向差集

| 方向 | 条数 | 词名 |
| ---- | ---- | ---- |
| 老表有、新仓没有 | 0 | —— |
| 新仓有、老表没有 | 0 | —— |

顺带核对的几项（也都是 0）：

- 老表体重 58 条里，`wake_word` 无重复；新仓 58 条里，`wake_word` 也无重复。
- 老表全量 436 条里，没有第二处出现本场景这 58 个词（不存在跨分类的重名字）。
- 新仓 58 条的 `name` 字段与 `wake_word` 完全一致，没有出现「显示名与唤醒词不一致」的情况。

因为差集为空，「新仓独有词的近似词」一节没有内容可列。

## 二、逐条对账表（58 行）

字段取自老表；`老 main_prompt.cli` 截断到 120 字符（截断处标 `…`）。

| # | 唤醒词 | key | subfunction | output_type | html_template | 老 main_prompt.cli（120 字符） | data_fields |
| - | ------ | --- | ----------- | ----------- | -------------- | --------------------------- | ----------- |
| 1 | 记体重 | w_log | 量体重 | receipt | templates/weight_log_receipt.html | `python scripts/render_meal_distribution.py --meal all --days 7 --chain "1.识别→2.读DB→3.渲染"` | `weight_kg`, `bmi`, `delta_last`, `goal_diff`, `date`, `time` |
| 2 | 记体重（含备注） | w_log_note | 量体重 | receipt | templates/weight_log_receipt.html | `python scripts/render_weight_receipt.py --live --kg <kg> --note <备注> --chain "1.解析→2.写库→3.回执"` | `weight_kg`, `bmi`, `note`, `note_tag`, `goal_diff`, `date`, `time` |
| 3 | 补录体重 | w_backfill | 量体重 | receipt | templates/weight_log_receipt.html | `python scripts/render_weight_receipt.py --live --kg <kg> --date <YYYY-MM-DD> --chain "1.解析→2.查冲突→3.写库→4.回执"` | `weight_kg`, `bmi`, `date`, `days_ago`, `backfill_flag`, `conflict` |
| 4 | 批量补录体重 | w_backfill_batch | 量体重 | receipt | templates/weight_batch_receipt.html | `python scripts/render_weight_receipt.py --live-batch --input <jsonl> --chain "1.解析→2.查冲突→3.批量写库→4.回执"` | `wrote`, `skipped`, `failed`, `fail_details`, `items` |
| 5 | 看今日体重 | w_today | 量体重 | result | templates/weight_dashboard.html | `python scripts/render_weight_dashboard.py --view today --chain "1.识别→2.读DB→3.渲染"` | `weight_kg`, `delta_last`, `summary`, `date` |
| 6 | 改体重记录 | w_update | 改体重记录 | receipt | templates/crud_receipt.html | `python scripts/render_crud_receipt.py --live-weight-update --id <ID> --weight <kg> --note <备注> --chain "1.定位→2.写库→3.回执"` | `id`, `weight_kg`, `note`, `old_record`, `new_record`, `bmi` |
| 7 | 改某日体重 | w_update_by_date | 改体重记录 | receipt | templates/crud_receipt.html | `python scripts/render_crud_receipt.py --live-weight-update --date <YYYY-MM-DD> --weight <kg> [--note <备注>] --chain "1.定位…` | `date`, `hit_count`, `old_record`, `new_record`, `weight_kg`, `note` |
| 8 | 删体重记录 | w_delete | 改体重记录 | receipt | templates/crud_receipt.html | `python scripts/render_crud_receipt.py --live-weight-delete --id <ID> --chain "1.定位→2.快照→3.删除→4.回执"` | `id`, `snapshot`, `date`, `weight_kg`, `confirm` |
| 9 | 删某日体重 | w_delete_by_date | 改体重记录 | receipt | templates/crud_receipt.html | `python scripts/render_crud_receipt.py --live-weight-delete --date <YYYY-MM-DD> --chain "1.定位→2.快照→3.删除→4.回执"` | `date`, `deleted_count`, `snapshot`, `confirm` |
| 10 | 批量删体重 | w_delete_batch | 改体重记录 | receipt | templates/crud_receipt.html | `python scripts/render_crud_receipt.py --live-weight-delete --start <S> --end <E> --chain "1.定位→2.快照→3.删除→4.回执"` | `start`, `end`, `deleted_count`, `snapshot`, `confirm` |
| 11 | 看本周体重 | w_detail_week | 看体重明细 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode history --week current` | `week`, `items`, `avg`, `net_change`, `summary` |
| 12 | 看上周体重 | w_detail_last_week | 看体重明细 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode history --week last` | `week`, `items`, `avg`, `net_change`, `vs_this_week` |
| 13 | 看本月体重 | w_detail_month | 看体重明细 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode history --month current` | `month`, `items`, `avg`, `net_change`, `summary` |
| 14 | 看上月体重 | w_detail_last_month | 看体重明细 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode history --month last` | `month`, `items`, `avg`, `net_change`, `vs_this_month` |
| 15 | 看最近 7 天体重 | w_detail_7d | 看体重明细 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode history --days 7` | `days`, `items`, `avg`, `net_change`, `summary` |
| 16 | 看最近 90 天体重 | w_detail_90d | 看体重明细 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode history --days 90` | `days`, `items`, `avg`, `net_change`, `summary` |
| 17 | 看某段时间体重 | w_detail_range | 看体重明细 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode history --start <S> --end <E>` | `start`, `end`, `items`, `avg`, `net_change`, `summary` |
| 18 | 看体重曲线 | w_curve | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --days 30` | `days`, `items`, `current`, `delta`, `daily_rate`, `trend` |
| 19 | 看体重曲线（带目标） | w_curve_target | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --days 30 --show-target` | `days`, `items`, `target`, `goal_diff`, `current`, `delta`, `daily_rate` |
| 20 | 看体重曲线（带里程碑） | w_curve_milestone | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --days 30 --show-milestones` | `days`, `items`, `milestones`, `current`, `delta`, `daily_rate` |
| 21 | 看体重曲线（带异常点） | w_curve_anomaly | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --days 30 --show-anomalies` | `days`, `items`, `anomalies`, `current`, `delta`, `daily_rate` |
| 22 | 看本月体重曲线 | w_curve_month | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --month current` | `month`, `items`, `current`, `delta`, `daily_rate`, `trend` |
| 23 | 看上月体重曲线 | w_curve_last_month | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --month last` | `month`, `items`, `current`, `delta`, `daily_rate`, `trend` |
| 24 | 看最近 90 天体重曲线 | w_curve_90d | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --days 90` | `days`, `items`, `current`, `delta`, `daily_rate`, `trend` |
| 25 | 看最近 180 天体重曲线 | w_curve_180d | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --days 180` | `days`, `items`, `current`, `delta`, `daily_rate`, `trend` |
| 26 | 看最近 365 天体重曲线 | w_curve_365d | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --days 365` | `days`, `items`, `current`, `delta`, `daily_rate`, `trend` |
| 27 | 看某段时间体重曲线 | w_curve_range | 看体重曲线 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode trend --start <S> --end <E>` | `start`, `end`, `items`, `current`, `delta`, `daily_rate`, `trend` |
| 28 | 看体重稳不稳（增强版） | w_vol | 看体重稳不稳 | result | templates/weight_volatility_v2.html | `python scripts/render_weight_volatility_v2.py` | `std`, `avg_daily_delta`, `anomaly_count`, `points`, `anomalies`, `baseline` |
| 29 | 看本月波动 | w_vol_month | 看体重稳不稳 | result | templates/weight_volatility_v2.html | `python scripts/render_weight_volatility_v2.py --start <月初> --end <月末>` | `month`, `std`, `avg_daily_delta`, `anomaly_count`, `points`, `anomalies` |
| 30 | 看最近 90 天波动 | w_vol_90d | 看体重稳不稳 | result | templates/weight_volatility_v2.html | `python scripts/render_weight_volatility_v2.py --days 90` | `days`, `std`, `avg_daily_delta`, `anomaly_count`, `points`, `anomalies` |
| 31 | 看最近 180 天波动 | w_vol_180d | 看体重稳不稳 | result | templates/weight_volatility_v2.html | `python scripts/render_weight_volatility_v2.py --days 180` | `days`, `std`, `avg_daily_delta`, `anomaly_count`, `points`, `anomalies` |
| 32 | 看波动异常点 | w_vol_anomalies | 看体重稳不稳 | result | templates/weight_volatility_v2.html | `python scripts/render_weight_volatility_v2.py --view anomalies-only` | `anomalies`, `reasons`, `summary` |
| 33 | 看「有备注」的体重记录 | w_notes | 看体重备注 | result | templates/weight_history.html | `python scripts/render_weight_history.py --mode notes --days 30` | `items`, `note_tags`, `tag_distribution`, `summary` |
| 34 | 对比体重：最近 30 天 vs 之前 30 天 | w_cmp_30d | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario a1 --chain "1.识别→2.读DB→3.对比→4.渲染"` | `seg1`, `seg2`, `avg`, `delta_kg`, `rate_diff`, `speed_judge`, `volatility` |
| 35 | 对比体重：自定义两段时间 | w_cmp_custom | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario a2 --start-a <S1> --end-a <E1> --start-b <S2> --end-b <E2> --chain "1…` | `seg1`, `seg2`, `avg`, `delta_kg`, `rate_diff`, `speed_judge`, `volatility` |
| 36 | 对比体重：本周 vs 上周 | w_cmp_week | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario a3 --chain "1.识别→2.读DB→3.对比→4.渲染"` | `seg1`, `seg2`, `sample_ok`, `avg`, `delta_kg`, `rate_diff`, `speed_judge` |
| 37 | 对比体重：本月 vs 上月 | w_cmp_month | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario a4 --chain "1.识别→2.读DB→3.对比→4.渲染"` | `seg1`, `seg2`, `avg`, `delta_kg`, `rate_diff`, `speed_judge`, `volatility` |
| 38 | 对比体重：近 N 天 vs 上一个 N 天 | w_cmp_ndays | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario a5 --n <N> --chain "1.识别→2.读DB→3.对比→4.渲染"` | `n_days`, `seg1`, `seg2`, `avg`, `delta_kg`, `rate_diff`, `speed_judge` |
| 39 | 对比体重：今天 vs 一年前今天 | w_cmp_1y | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario a6 --chain "1.识别→2.读DB→3.对比→4.渲染"` | `current`, `year_ago`, `delta_kg`, `direction`, `tolerance_hit`, `period_avg` |
| 40 | 对比体重：今天 vs 半年前今天 | w_cmp_6m | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario a7 --chain "1.识别→2.读DB→3.对比→4.渲染"` | `current`, `past`, `delta_kg`, `direction`, `tolerance_hit`, `period_avg` |
| 41 | 对比体重：今天 vs 三月前今天 | w_cmp_3m | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario a8 --chain "1.识别→2.读DB→3.对比→4.渲染"` | `current`, `past`, `delta_kg`, `direction`, `tolerance_hit`, `period_avg` |
| 42 | 对比体重：当前 vs 目标体重 | w_cmp_target | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario b1 --chain "1.识别→2.读DB→3.对比→4.渲染"` | `current`, `target`, `delta_kg`, `pct_done`, `eta`, `current_bmi`, `target_bmi`, `verdict` |
| 43 | 对比体重：当前 vs 平台期首日 | w_cmp_plateau | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario b8 --chain "1.识别→2.读DB→3.平台期识别→4.渲染"` | `current`, `plateau_start`, `plateau_days`, `delta_after`, `plateau_count`, `avg_break_days` |
| 44 | 对比体重：当前 vs 历史最低 | w_cmp_min | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario e1 --chain "1.识别→2.读DB→3.对比→4.渲染"` | `current`, `min_kg`, `min_date`, `delta_kg`, `days_since`, `summary` |
| 45 | 对比体重：当前 vs 历史最高 | w_cmp_max | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario e2 --chain "1.识别→2.读DB→3.对比→4.渲染"` | `current`, `max_kg`, `max_date`, `delta_kg`, `rate`, `summary` |
| 46 | 对比体重：减重 5kg 那天 vs 今天 | w_cmp_5kg | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario e3 --delta 5 --chain "1.识别→2.读DB→3.反查里程碑→4.渲染"` | `current`, `milestone_kg`, `milestone_date`, `elapsed_days`, `rate`, `trajectory` |
| 47 | 对比体重：减重 10kg 那天 vs 今天 | w_cmp_10kg | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario e3 --delta 10 --chain "1.识别→2.读DB→3.反查里程碑→4.渲染"` | `current`, `milestone_kg`, `milestone_date`, `elapsed_days`, `rate`, `trajectory` |
| 48 | 对比体重：当前 vs 入夏最低 | w_cmp_summer | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario e5 --chain "1.识别→2.读DB→3.季节定位→4.渲染"` | `current`, `season_min_kg`, `season_min_date`, `delta_kg`, `days_since` |
| 49 | 对比体重：当前 vs 入冬最低 | w_cmp_winter | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario e6 --chain "1.识别→2.读DB→3.季节定位→4.渲染"` | `current`, `season_min_kg`, `season_min_date`, `delta_kg`, `days_since` |
| 50 | 对比体重：运动多 vs 运动少的两个月 | w_cmp_exercise | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario c5 --chain "1.识别→2.读DB→3.选极端月→4.渲染"` | `high_month`, `low_month`, `avg`, `delta_kg`, `rate_diff`, `calories`, `sleep`, `exercise_total` |
| 51 | 对比体重：工作日 vs 周末 | w_cmp_weekend | 对比体重 | result | templates/weight_compare.html | `python scripts/render_weight_compare.py --scenario d4 --chain "1.识别→2.读DB→3.周内聚合→4.渲染"` | `weekday_avg`, `weekend_avg`, `delta_kg`, `weekday_vol`, `weekend_vol`, `agreement_rate` |
| 52 | 看体重总览 | w_overview | 体重复盘 | result | templates/weight_dashboard.html | `python scripts/render_weight_dashboard.py --view overview --chain "1.识别→2.读DB→3.渲染"` | `current`, `delta_7d`, `diff_min`, `diff_target`, `vol_level`, `trend_7d`, `summary` |
| 53 | 体重复盘（本周） | w_review_week | 体重复盘 | result | templates/weight_review.html | `python scripts/render_weight_review.py --type week --chain "1.识别→2.读DB→3.复盘→4.渲染"` | `delta_kg`, `avg`, `vs_last_period`, `trend`, `summary` |
| 54 | 体重复盘（本月） | w_review_month | 体重复盘 | result | templates/weight_review.html | `python scripts/render_weight_review.py --type month --chain "1.识别→2.读DB→3.复盘→4.渲染"` | `delta_kg`, `avg`, `vs_last_period`, `trend`, `summary` |
| 55 | 体重复盘（最近 90 天） | w_review_90d | 体重复盘 | result | templates/weight_review.html | `python scripts/render_weight_review.py --type 90d --chain "1.识别→2.读DB→3.复盘→4.渲染"` | `delta_kg`, `avg`, `vs_last_period`, `trend`, `summary` |
| 56 | 体重复盘（今年） | w_review_year | 体重复盘 | result | templates/weight_review.html | `python scripts/render_weight_review.py --type year --chain "1.识别→2.读DB→3.复盘→4.渲染"` | `delta_kg`, `avg`, `monthly_trend`, `summary` |
| 57 | 体重复盘（自定义时间） | w_review_range | 体重复盘 | result | templates/weight_review.html | `python scripts/render_weight_review.py --start <S> --end <E> --chain "1.识别→2.读DB→3.复盘→4.渲染"` | `start`, `end`, `delta_kg`, `avg`, `vs_last_period`, `trend`, `summary` |
| 58 | 看里程碑回溯 | w_milestones | 体重复盘 | result | templates/weight_review.html | `python scripts/render_weight_review.py --type milestones --chain "1.识别→2.读DB→3.回溯→4.渲染"` | `milestones`, `summary` |

## 三、带 variants 的唤醒词

- 老表体重 58 条的 `variants` **全部是空列表**（58／58），所以本场景没有 variants 可以逐条列出。
- 同一批 58 条的 `fill_hints` 也全部是空列表（58／58）。
- 老表体重 58 条里**没有 `aliases` 字段**；整个 `_triggers.py` 436 条里只有 1 条带 `aliases`（不在体重分类）。文件开头的文档字符串写了「aliases: 同义变体」，这批体重条目实际没有填。
- 参照：老表全量 436 条里，`variants` 非空的只有 3 条，`fill_hints` 非空的只有 2 条（都不在体重分类）。

## 四、统计

### output_type 分布（老表体重 58 条）

| output_type | 条数 |
| ----------- | ---- |
| `result` | 49 |
| `receipt` | 9 |
| `process` | 0 |
| 缺失（字段为空或没有该字段） | 0 |

### html_template 分布（老表体重 58 条）

| 模板 | 条数 |
| ---- | ---- |
| `templates/weight_history.html` | 18 |
| `templates/weight_compare.html` | 18 |
| `templates/weight_review.html` | 6 |
| `templates/crud_receipt.html` | 5 |
| `templates/weight_volatility_v2.html` | 5 |
| `templates/weight_log_receipt.html` | 3 |
| `templates/weight_dashboard.html` | 2 |
| `templates/weight_batch_receipt.html` | 1 |
| 合计 | 58 |

8 个模板名都对应老技能 `templates\` 下真实存在的 .html 文件（逐个 `Test-Path` 检查，8／8 存在）。

### 取值为空的条目

- `output_type` 为空的条目：**0 条**。
- `html_template` 为空的条目：**0 条**。

### 附：subfunction 分布（老表体重 58 条）

| subfunction | 条数 |
| ----------- | ---- |
| 对比体重 | 18 |
| 看体重曲线 | 10 |
| 看体重明细 | 7 |
| 体重复盘 | 7 |
| 量体重 | 5 |
| 改体重记录 | 5 |
| 看体重稳不稳 | 5 |
| 看体重备注 | 1 |

`key` 取值 58 个互不相同（无重名）。`order` 在各 subfunction 内从 0 重新计数，取值 0—17。

## 五、字段级差异（补充核对，不属要求项）

老表 58 条与新仓 58 条按 19 个字段逐个比较，差异只出现在两处：

| 字段 | 不同的条数 | 说明 |
| ---- | ---------- | ---- |
| `main_prompt.cli` | 58 | 老表是旧脚本命令 `python scripts/render_*.py …`；新仓 40 条改成 `calorie-cmd-read …`，另 18 条是中文说明文字 |
| `data_source` | 58 | 同上，新仓 40 条是 `calorie-cmd-read …`，18 条是中文说明文字 |
| 其余 17 个字段 | 0 | 包括 `main_prompt.text`、`prompt_template`、`key`、`name`、`desc`、`user_intent`、`subfunction`、`output_type`、`html_template`、`data_fields`、`fill_hints`、`variants`、`order`、`depends_on_external`、`category`、`wake_word` |

**现在没有命令可执行的唤醒词（18 条）**：`main_prompt.cli` 与 `data_source` 都是中文说明文字，不是可执行命令。

- 看体重曲线（带目标）
- 看体重曲线（带里程碑）
- 看体重曲线（带异常点）
- 看「有备注」的体重记录
- 对比体重：当前 vs 平台期首日
- 对比体重：当前 vs 历史最低
- 对比体重：当前 vs 历史最高
- 对比体重：减重 5kg 那天 vs 今天
- 对比体重：减重 10kg 那天 vs 今天
- 对比体重：当前 vs 入夏最低
- 对比体重：当前 vs 入冬最低
- 对比体重：运动多 vs 运动少的两个月
- 体重复盘（本周）
- 体重复盘（本月）
- 体重复盘（最近 90 天）
- 体重复盘（今年）
- 体重复盘（自定义时间）
- 看里程碑回溯

（老表这 18 条都是有命令的：`render_weight_history.py` 4 条、`render_weight_compare.py` 8 条、`render_weight_review.py` 6 条，合计 18 条，逐个覆盖上面的条目。）

**老表里 1 条命令指向了错误的脚本**：

- 「记体重」（`key = w_log`，`html_template = templates/weight_log_receipt.html`）的 `main_prompt.cli` 写的是 `python scripts/render_meal_distribution.py --meal all --days 7 --chain "1.识别→2.读DB→3.渲染"`，这是饮食分布脚本的命令，与记体重无关；同一条目的 `data_source` 与同模板另两条都是 `python scripts/render_weight_receipt.py --live --kg <kg> --chain …`。`_triggers.py` 第 894 行原文可查。新仓这一条已改成 `calorie-cmd-read calorie.weight.log --params '{"kg":70.5}'`。

老表 58 条的 `main_prompt.cli` 指向的脚本共 8 个（都与体重/饮食有关，除上条外搭配一致）：

| 脚本 | 条数 |
| ---- | ---- |
| `render_weight_history` | 18 |
| `render_weight_compare` | 18 |
| `render_weight_review` | 6 |
| `render_crud_receipt` | 5 |
| `render_weight_volatility_v2` | 5 |
| `render_weight_receipt` | 3 |
| `render_weight_dashboard` | 2 |
| `render_meal_distribution` | 1（「记体重」，上述错命令） |

8 个脚本在 `D:\2Study\StudyNotes\SKILLS\卡路里\scripts\` 下都存在（逐个 `Test-Path` 检查，8／8 存在）。

## 六、方法（便于复核）

### 用到的命令

```powershell
python -W ignore D:\ilife\.scratch\t165\a-parse.py
```

脚本（临时件，带 `a-` 前缀）：`D:\ilife\.scratch\t165\a-parse.py`；另有一次性抽查脚本写在命令行里（见下）。

### 怎么读的老表

- 用 `importlib.util.spec_from_file_location('_old_triggers_t165', 老路径)` 把 `_triggers.py` 当独立模块导入（该文件只定义 `_prompt_skeleton`／`CATEGORIES`／`TRIGGERS`／`get_summary`，没有外部 import，可以直接导入），只取 `TRIGGERS` 列表，**没有把 621 KB 原文读进对话上下文**。
- 436 = `len(TRIGGERS)`；58 = `len([r for r in TRIGGERS if r['category'] == '体重'])`。
- 老表体重条目的字段共 17 个：`category`、`wake_word`、`desc`、`main_prompt`、`fill_hints`、`variants`、`key`、`name`、`subfunction`、`output_type`、`html_template`、`data_source`、`prompt_template`、`user_intent`、`data_fields`、`depends_on_external`、`order`。

### 怎么读的新仓

- 逐行读 `scene-03-weight.ts`，去掉行尾逗号后按 JSON 解析（该文件第 5—62 行每行是一个 JSON 对象，共 58 行，正好 58 条）。

### 差集与逐字段比较

- 差集：对两个 `wake_word` 列表做「不在对方集合里」的过滤，得到两个方向的差集（本次都为 0）。
- 逐字段比较：对上述 17 个字段再加上拆开的 `main_prompt.cli`／`main_prompt.text`，共 19 个名字；每个字段值用 `json.dumps(..., sort_keys=True)` 归一后精确比较，按字段名计数差异条数。
- 错命令排查：从每条老 `main_prompt.cli` 里正则取 `scripts/([A-Za-z0-9_]+)\.py`，按 `html_template` 分组看脚本名的多数派，落在少数派上的即为疑似复制粘贴留下的错命令（命中 1 条）。

### 中间结果（可自行核对）

放在 `D:\ilife\.scratch\t165\`：

- `a-old-weight.json`：老表体重 58 条原样。
- `a-new-weight.json`：新仓 58 条原样。
- `a-table.tsv`：逐条对账表（制表符分隔，cli 未截断以外的列同报告）。
- `a-rows.md`：逐条对账表的 Markdown 版（本报告第二节即由它复制而来）。
- `a-diff.txt`：差集、逐字段差异细目、`main_prompt.cli` 全文对照、variants 与统计。
- `a-probe.txt`：抽查前 6 条老条目的 `cli`／`variants`／`fill_hints` 原文。

### 本次判不准的点

- `output_type`／`html_template` 的取值只从老表读出，没有打开这 8 个模板文件核对里面的内容。
- `data_fields` 只是老表与新仓之间的字段名比较，没有核对字段与命令真实返回的字段是否一一对应。
- 老表这 58 条命令今天能否跑得通（脚本存在，但未运行），本次没有验证。
- 新仓那 18 条没有命令的唤醒词，是有意留白待补，还是漏写，本次没有依据可判定。
