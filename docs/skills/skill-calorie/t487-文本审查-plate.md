# t487 · 场景 03 体重「今日盘」两页 文本审查（证据件）：冗余删除＋人话改写

票：**#487**（`wayfinder:task`，`Part of #154`）。本票由 **#483** 切分而来——原票面把「写后回执 9 词 ＋
今日盘 2 词」压在一位执行席上，该席因上下文耗尽阵亡且未落盘；回执 9 词留在 #483，今日盘 2 词另立本票
（两票文件互不共用：本票只动 `log.ts`）。

- **范围**：`calorie.view.weight` 的两条读词 —— 第 8 条「看体重总览」（`window=30d`）、
  第 30 条「看今日体重」（`window=今日`）；连带**空窗整页**（`buildWeightEmptyDoc`，58 页之外的那一支）。
- **判据**（用户 2026-09-15 原话）：① 百分百冗余的必删；② 看不懂的专业话改人话（读者＝不懂技术的项目负责人）；
  ③ 该换展示形式的换形态。
- **写集**：`packages/skill-calorie/src/weight/log.ts`（LF **310 → 327**，未越 350 告警线，台账不必动）。
  **未动**任何测试件的改动由 #483 承接（见本件 §四）。
- 交付样张：`.scratch/t154/text-review/out-plate/`（第 8 条 84,289 B／第 30 条 72,812 B，两条全 `通过`）。

## 一、逐页表

改前／改后逐字取自 `.scratch/t154/text-review/diff-pages.py`（改前基线＝改前交付包导出的整页文本）。

### 第 8 条「看体重总览」（`window=30d`，30 条记录）

| # | 槽位 | 改前（逐字） | 判据 | 判定 | 改后（逐字） | 依据 |
|---|---|---|---|---|---|---|
| 1 | H1 | `体重盘 2026-08-09 ~ 2026-09-07` | 难 | 改 | `体重总览` | 「体重盘」是内部叫法，读者念的词是「看体重总览」；日期与副标题逐字重复 |
| 2 | 副标题 | `趋势上升｜2026-08-09 ~ 2026-09-07｜共 30 条` | 形式 | 改 | `2026-08-09 ~ 2026-09-07 · 共 30 条 · 趋势上升` | 全角竖线在家族里只此一处，改家族通用间隔符；「先看哪段、再看怎么样」的顺序 |
| 3 | 卡 1 标签 | `今日体重`（30 天窗） | 难 | 改 | `最新体重` | 30 天窗里最后一条不一定是今天；一天窗才叫「今日体重」 |
| 4 | 卡 1 徽章 | `已记 30 条` | 冗 | 删 | `已记录` | 条数已在副标题与页脚两处；徽章只说状态 |
| 5 | 卡 2 值槽 | `+0 kg` | 形式 | 改 | `0 kg` | 正零不加号（口径 §3.3）；全族统一 |
| 6 | 卡 3 标签 | `体重盘` | 难 | 改 | `窗口` | 同上第 1 条 |
| 7 | 卡 3 副说明 | `首 70.1 → 末 70.4 kg · 2026-08-09 ~ 2026-09-07` | 冗 | 删后半 | `首 70.1 → 末 70.4 kg` | 窗口已在副标题与页脚 |
| 8 | 卡 3 徽章 | `共 30 条` | 冗 | 删 | （不出徽章） | 同第 4 条 |
| 9 | 卡 4 副说明 | `共 30 条 · 极值 70.1~70.4` | 冗＋难 | 改 | `最低 70.1 kg · 最高 70.4 kg` | 条数第三次出现（删）；「极值」是统计词，改人话 |
| 10 | 卡 4 徽章 | `首末对照` | 难 | 删 | （不出徽章） | 工程词，读者读不出含义 |
| 11 | 卡 5 副说明 | `趋势上升 · 日均 10 g` | 难 | 改 | `趋势上升 · 平均每天约 +10 克` | `g` 与 `kg` 两种单位并存；全族统一「克」 |
| 12 | 卡 6 副说明 | `目标 68 kg · 截止 2026-12-31（线在量程外，不画）` | 难 | 改 | `目标 68 kg · 截止 2026-12-31 · 目标线超出刻度，图上没画` | 「量程」是实现词；口径本身保留（读者会问「目标线呢」） |
| 13 | 卡 6 徽章 | `还差 2.4 kg` | 冗 | 改 | `未达成` | 值槽已有 `+2.4 kg`，徽章说同一件事；徽章改说状态 |
| 14 | 结论句 | `2026-08-09 ~ 2026-09-07 共 30 条，首 70.1 → 70.4 kg（+0.3 kg，趋势上升，日均 10 g）；均值 70.3 kg；目标 68 kg，还差 2.4 kg。` | 冗＋难 | 改 | `这段时间从 70.1 kg 到 70.4 kg，累计 +0.3 kg（趋势上升，平均每天约 +10 克）；平均 70.3 kg；目标 68 kg，还差 2.4 kg。` | 窗口与条数第四次出现（删）；`日均 10 g` 换人话；「均值」→「平均」 |
| 15 | 表注 | `体重记录（2026-08-09 ~ 2026-09-07，共 30 条）` | 冗 | 删 | `体重记录` | 窗口与条数已在副标题与页脚；表头已写清列名 |
| 16 | 页脚来源行 | `📊 数据来源:calorie_data.db · weight_log ｜ 窗口 … ｜ 30 条` | 难 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-08-09 ~ 2026-09-07 ｜ 共 30 条` | 全族统一句式（口径 §3.1）：人话来源在先，库表名退到括号里（§5.5 的「哪张库」不丢），全角冒号 |

### 第 30 条「看今日体重」（`window=今日`，1 条记录）

| # | 槽位 | 改前（逐字） | 判据 | 判定 | 改后（逐字） | 依据 |
|---|---|---|---|---|---|---|
| 17 | H1 | `体重盘 2026-09-07 ~ 2026-09-07` | 难＋冗 | 改 | `今日体重` | 同第 1 条；同一天写两遍日期也不通 |
| 18 | 副标题 | `趋势平稳｜2026-09-07 ~ 2026-09-07｜共 1 条` | 难＋冗 | 改 | `2026-09-07 · 只有一条记录，还看不出趋势` | **「趋势平稳」是错的**：单点读不出趋势；改说清为什么没有趋势 |
| 19 | 卡 2 副说明＋徽章 | `单点无变化`＋`单点无变化`（同屏两次） | 冗＋难 | 改 | （副说明不出）＋`无可比` | 同一句在同一张卡上印两遍；「单点」是数学词 |
| 20 | 卡 3 副说明 | `首 70.4 → 末 70.4 kg · 2026-09-07 ~ 2026-09-07` | 冗 | 改 | `2026-09-07` | 一条记录没有「首末」可言；窗口就那一天 |
| 21 | 卡 3 徽章 | `共 1 条` | 冗 | 删 | （不出徽章） | 同第 4／8 条 |
| 22 | 卡 4 副说明＋徽章 | `单点无均值对照`＋`无对照` | 冗＋难 | 改 | （副说明不出）＋`无对照` | 同第 19 条；且全页三张卡都在说「只有一条记录」——只留副标题那一处 |
| 23 | 卡 5 值槽＋副说明＋徽章 | `+0 kg`＋`趋势平稳 · 日均 0 g`＋`单点无变化` | 冗＋难 | 改 | `0 kg`＋（副说明不出）＋`看不出变化` | 同第 5／19／22 条 |
| 24 | 图表标题 | `近 7 天体重曲线`（今日窗） | 难 | 改 | `今日体重曲线` | 窗内只有当天一个点，却写「近 7 天」——标题与数据不符 |
| 25 | 结论句 | `2026-09-07 ~ 2026-09-07 只有 1 条记录（70.4 kg），单点看不出变化，再记一条就能比较；目标 68 kg，还差 2.4 kg。` | 冗＋难 | 改 | `2026-09-07 只有 1 条记录（70.4 kg），看不出变化，再记一条就能比较；目标 68 kg，还差 2.4 kg。` | 同一天写两遍（删）；去「单点」 |
| 26 | 表注／页脚 | 同第 15／16 条 | 冗／难 | 改 | `体重记录`／`📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-09-07 ｜ 共 1 条` | 同上；窗口为一天时只写那一天 |

### 空窗整页（`buildWeightEmptyDoc`，交付包之外的那一支，一并统一口径）

| # | 槽位 | 改前（逐字） | 判据 | 判定 | 改后（逐字） | 依据 |
|---|---|---|---|---|---|---|
| 27 | H1 | `体重盘 2026-09-07 ~ 2026-09-07` | 难 | 改 | `今日体重`／`体重总览`（按窗口天数） | 同第 1／17 条 |
| 28 | 卡 1 副说明／徽章 | `本窗无记录`／`本窗无记录` | 难 | 改 | `这段时间没有记录`／`没有记录` | 「本窗」是工程话；同一句不在一张卡上印两遍 |
| 29 | 卡 2 副说明／徽章 | `本窗无记录 · 无对照`／`无对照` | 冗＋难 | 改 | `没有记录，也没有上一次可比`／`无可比` | 「无对照」重复出现；改说清「跟什么比」 |
| 30 | 空态块正文／结论句 | `本窗无体重记录（2026-09-07 ~ 2026-09-07）`／`2026-09-07 ~ 2026-09-07 无体重记录，先记一条再看。` | 难 | 改 | `这段时间还没有体重记录`／`这段时间还没有体重记录，先记一条再看。` | 同上；窗口在页脚来源行里仍有（`｜ 窗口 2026-09-07 ｜ 共 0 条`） |

## 二、删掉的信息登记（不许丢）

| 删掉的信息 | 原来住哪 | 去向 |
|---|---|---|
| 「共 30 条」的 3 处重复（卡 3 徽章 / 卡 4 副说明 / 结论句） | 卡片与结论 | **页头副标题 ＋ 页脚来源行**各留一处（§5.5 的「多少条」不丢） |
| 「已记 30 条」 | 卡 1 徽章 | 同上；卡 1 徽章改说状态 |
| 「还差 2.4 kg」 | 卡 6 徽章 | 值槽仍写 `+2.4 kg`，徽章改「未达成」；结论句里仍逐字写「还差 2.4 kg」 |
| 窗口 `2026-08-09 ~ 2026-09-07` 的 2 处重复（卡 3 副说明 / 表注） | 卡片与表注 | 页头副标题 ＋ 页脚来源行 |
| 「首末对照」 | 卡 4 徽章 | 删（工程词）。「首/末」这两个事实仍逐字在卡 3 副说明里 |
| 「极值 70.1~70.4」 | 卡 4 副说明 | **换成**「最低 70.1 kg · 最高 70.4 kg」（同一事实、人话） |
| 「线在量程外，不画」 | 卡 6 副说明 | **换成**「目标线超出刻度，图上没画」（同一事实、人话，未删） |
| 「趋势平稳」 | 第 30 条副标题 | **换成**「只有一条记录，还看不出趋势」——原句与数据不符（单点读不出趋势），属订正 |

## 三、机器读数

| 项 | 读数 |
|---|---|
| 交付（本族 2 条） | `node .scratch/t154/text-review/render-family.mjs --family plate` → `TALLY {"通过":2} TOTAL 2 HTML=2`；第 8 条 **84,289 B**（改前 84,582）、第 30 条 **72,812 B**（改前 73,250），两条全 `通过` |
| 文本差分 | `python .scratch/t154/text-review/diff-pages.py --out …/out-plate` → `CHANGED-PAGES 2`，`DELTA-LINES 65` |
| 编译 | `node tooling/run-locked.mjs --ticket 487 -- npx tsc -b packages/skill-calorie` → exit 0 |
| 告警线 | `log.ts` LF 310 → 327，未越 350，无需同步台账 |
| 靶向用例 | 见 §四（跨票承接） |
| 取值口径 | 富种子库（`.scratch/t154/seed-rich.mjs`，种子日 `2026-09-07`）——与 58 页交付包同一个库 |

## 四、未做项与下一手缺什么

1. **靶向用例的最后一步不在本票手里**：`test/weight-receipt-337.test.mjs` 属 **#483** 的写集，
   本票进场时它正被 #483 改写。本票改了第 8 条的卡 1 标签（`今日体重`→`最新体重`），
   连带该件第 106 行的 needle 过时（实测 `看体重总览 缺：今日体重`）。已按并发协议 §1
   **报给 #483 席转改**（收紧为按页传参：`assertDashboard(r, '看体重总览', '最新体重')`），
   本票**不碰该文件**。合流后需复跑该件确认全绿。
2. 眉标 `calorie.view.weight · 运动身体域` **本轮不动**（等用户裁定；处置见 #154 的裁定记录）。
3. 13 条 `fieldLabel`／`weight_log` 等回执侧口径不在本票范围（归 #483）。

---

## 五、对抗审查整改（2026-09-15·独立审查席判定 PASS 88/100，逐条整改）

审查席（不是作者本人）自跑 5 条探针 ＋ 实看两页全图，判定 **PASS（88/100）**，逐条缺陷与整改如下：

| # | 审查席缺陷 | 严重度 | 整改 |
|---|---|---|---|
| 1 | `test/home-lock-374.test.mjs:39` 的 needle 仍写死 `'体重盘'`——**本票引入的树红**，且当时未登记 | 严重 | **已改**：`['体重总览','最新体重','距目标','变化','体重曲线','体重记录','calorie.view.weight']`（两针替换一针＝收紧）。复跑 `home-lock-374`＋`weight-receipt-337`：20 例 19 过、唯一红是**登记在规范 §9 的空库口径张力**（改前既有，非本票） |
| 2 | 第 30 条均值卡把当天读数又报一遍，同屏三处说「只有一条记录」 | 中 | **已改**：单点页均值卡去掉副说明，徽章改 `只有一天` |
| 3 | 第 8 条「较上次」卡：`0 kg`／`上次 70.4 kg`／徽章 `持平` 三处说同一件事 | 中 | **已改**：零变化**不出徽章**，副说明改 `与上次一样`；升降才出徽章并写 `上次 X kg` |
| 4 | 第 8 条窗口卡「首 X → 末 Y」与均值卡「最低／最高」同报一对值 | 轻 | **已改**：均值卡改说**区间**`这段都在 70.1 ~ 70.4 kg 之间`（首末日那一对只住窗口卡） |
| 5 | 第 30 条结论句复述卡上已有四项事实 | 轻 | **已改**：单点页结论只说 `只有这一天，看不出变化；再记一条就能比较。`（不再复述日期／读数／目标差值） |
| 6 | 卡标签 `均值` 与结论句 `平均` 两套词 | 轻 | **已改**：结论句统一说 `均值`（卡标签是族内通用名，不单页改） |

**整改后机器读数**：`log.ts` LF 327 → 335（未越 350）；出页 `TALLY {"通过":2}`（第 8 条 84,241 B／第 30 条 72,757 B）；
`diff-pages.py` 对本轮起点差分 `CHANGED-PAGES 2`；编译 exit 0。

**审查席自设探针的独立读数（照抄，供复核）**：
- 探针 3「删掉的信息有没有真丢」——**未发现删过头**；真消失只有三类（`体重盘` 内部叫法、`首末对照` 工程词、
  第 30 条的 `趋势平稳／单点／已记 1 条`），其余（`共 30 条`／窗口／首末值／极值→区间／目标／截止／刻度说明／
  `+2.4 kg`／备注串／三格式复制）全部仍在。
- 探针 2 残余工程词：第 8 条 10 行 → 2 行、第 30 条 13 行 → 3 行，余下命中全是口径明文保留（页脚库表名、徽章 `无对照`）。
- 探针 5：第 30 条图轴 x 域恰 `09-07`，新标题 `今日体重曲线` 与原 `近 7 天体重曲线` 相比是**订正**。

**范围外记账（不属本票）**：第 30 条单点图纵轴撑满整块图（`packages/base-render` 图轴自适应）；
`cmd-registry-294.test.mjs:195` 的 `'体重盘未走通'` 只是断言消息串，不是可见文本。

---

## 七、形状化与手机端（#505，2026-09-15）

**票**：#505「场景 03 体重回执＋今日盘 11 页 去分隔符形状化 ＋ 手机端 820 端适配」。
本节只记**今日盘 2 页**（第 8／30 条）；回执 9 页记在 `t483-文本审查-receipt.md` 的同名节。
口径正本 `.scratch/t154/text-review/口径-UI.md`；形状词汇 `src/weight/weightUi.ts`（编排者已入库，本票只用不改）。

### ① 逐页「原来串什么 → 改成什么形状」

| 页 | 原来（`·`／`；` 串） | 改成 |
|---|---|---|
| 08 | 页头副标题 `2026-08-09 ~ 2026-09-07 · 共 30 条 · 趋势上升` | 正文首件：`windowStrip()`（两枚日期块 ＋ 条数胶囊）＋ `chip('趋势上升','warn')`；副标题只剩 `2026-08-09 ~ 2026-09-07（共 30 条）`（`~` 是允许项） |
| 08 | 「变化」卡副说明 `趋势上升 · 平均每天约 +10 克` | 结论块 `factStrip()`：`变化　趋势上升，平均每天约 +10 克` |
| 08 | 「距目标」卡副说明 `目标 68 kg · 截止 2026-12-31 · 目标线超出刻度，图上没画` | `factStrip()`（`目标 68 kg，还差 2.4 kg`／`截止 2026-12-31`）＋ 说明条（`图上没画目标线　目标值超出刻度`） |
| 08 | 结论句 `bits.join('；')`（首末／累计／均值／目标四件一句） | `shapedConclusionBlock(verdict(一句判语) + factStrip(均值／目标))` |
| 30 | 同上（窗口那条在副标题 `2026-09-07 · 只有一条记录，还看不出趋势`） | 同上：窗口条 ＋ `chip('只有一条记录，还看不出趋势','plain')`；副标题 `2026-09-07（共 1 条）` |
| 30 | 「距目标」卡副说明 | 同第 8 条（本页目标线同样超出刻度） |
| 30 | 结论句 `只有这一天，看不出变化；再记一条就能比较。` | `verdict('只有这一天，看不出变化。再记一条就能比较。')`（一句判语，`；` 去） |

**两页的 `detail` 槽为什么改走结论块**：KPI 卡的 `detail` 那一格由共享层 `renderKpiGrid` → `esc(card.detail)`
**转义**，形状词汇的 HTML 进不去——本票第一版把 `factStrip()` 放进那一槽，产物上直接印出
`<div class="wui-strip">…` 字面量（桌面截图肉眼即见）。改法：`plateCards()` 改成回 `{cards, facts}`，
那几枚事实由结论块渲染；`assertReceipt`／`assertDashboard` 同步钉住
`&lt;(p|div|span|ul) class=&quot;wui-` 命中 0（防再次整段转义）。

### ② 删掉的信息登记

**无**。第 8／30 条的窗口、条数、趋势、平均每天、目标、截止、目标线没画、均值、首末、区间 —— 逐条都在上表有去处
（换形状，不是丢）。唯一**措辞**变化：第 8 条目标那句由「还差 2.4 kg」并进事实条（值槽 `+2.4 kg` 仍在）。

### ③ 分隔符回扫读数（逐页）

`python .scratch/t154/text-review/scan-separators.py --dir .scratch/t154/text-review/ui483/out`（交付包口径）
＋ `.scratch/t154/text-review/ui483/scan-zones.py`（本席把命中分成三区，判据＝**正文区必须 0**）：

| 页 | 改前命中 | 改后：页题· | 机器面· | **正文·** |
|---|---|---|---|---|
| 08-看体重总览 | 8 | 1 | 10 | **0** |
| 30-看今日体重 | 5 | 1 | 10 | **0** |
| （同批其余 9 页） | 39 | 1 | 10 | **0** |

11 页合计：改前 52 处 → 改后 **11 处，全部落在允许项**——页题 `卡路里·体重`／`卡路里·体重回执`（全仓同形的文档名，
`<title>` 里那一个）与复制载荷／命令原文（机器面，逐页 10 处）。**正文命中 0/11。**

### ④ 手机端探针读数

- `mobile-frame.mjs shoot`（真 390 iframe 壳）：第 8 条 `mobile/08-看体重总览.png` 107,524 B，已 `read_image` 实看——
  窗口条纵排、四卡两列、结论块判语 ＋ 两条事实、复制按钮两枚各占半宽，无溢出无断字。
- **溢出探针**：共享的 `mobile-frame.mjs probe` 在本机读不到读数（实测 `WRAP-TITLE=等待探针…`：它靠 iframe
  `postMessage` ＋ 父页监听改 `title`，而 `--dump-dom` 抓的是监听落地前那一帧）。本席另写
  `.scratch/t154/text-review/ui483/probe-390.mjs`（口径逐条照搬：真 390 iframe、`scrollWidth` 对 `clientWidth`、
  逐元素 `right > vw+1`，只把读数落点换成 iframe 直接写 `parent.document.title`），读数
  **`SW=390 VW=390 || 无溢出`**（第 1／8／17／23／31 五页同值）。
- 820 段：`weightUi.ts` 的件窄屏塌纵向（事实条一列、判语胶囊行一列）；共享区块自带的 **640** 段不撤（表在窄屏
  由 `th{white-space:normal}` ＋ `td{overflow-wrap:anywhere}` 自行收纳，故 390 无横向滚动）。

### ⑤ 用例与变异读数

- 用例（持锁）：`node tooling/run-locked.mjs --ticket 505 -- node --test packages/skill-calorie/test/weight-receipt-337.test.mjs`
  → `ℹ tests 11 / pass 11 / fail 0`；`runId=690b767e-5328-48fb-80cf-0c6569ab0d31`（exit 0）。
- 同步收紧（旧断言过时即改，不放宽）：`assertDashboard` 加 `visibleBody()` 正文零 `·`／`；` 判据 ＋
  形状件在位判据（`wui-window-block`／`wui-days`／`wui-chip`／`wui-verdict`，多天页另加 `wui-strip`）。
- 变异两行：把 `logReceipt.ts` 的 `距目标差` 那格改回 `未设体重目标 · 说…` → `tsc` exit 0、
  用例 **exit 1**（3 例红：`正文里仍有 ·：… 未设体重目标 · 说「定体重目标」后可看差距`），
  `runId=42f2c12c-5b57-4d4f-8608-91153dc7f334`；改回 → 全绿 `runId=690b767e-…`。

### ⑥ 未做项与下一手缺什么

1. **页题 `<title>` 那个 `·` 未动**（`卡路里·体重`／`卡路里·体重回执`）：它是全仓同形的**文档名**
   （别的域同样写法），改它跨族；口径 §二只把「页脚口径行 `｜`」列为明确允许项，没提页题——
   **请编排者裁定**：要么把「`<title>` 文档名」补进允许项，要么另开一票统一改名（本票不敢单开先例）。
2. **共享探针 `mobile-frame.mjs probe` 在本机读不到读数**：这是工具本身的事（见 ④），本席只旁路未改它；
   建议编排者按 `probe-390.mjs` 的落点改法（直接写 `parent.document.title`）修一处，四席复用。
3. **`log.ts` 越过 350 告警线**（337→382，本票的形状化与手机端改动引入）：已按必报五步第四步挂号
   （`packages/skill-calorie/AGENTS.md` 台账行，「已超线，需要根据规则进行重构。」＋超因＋拆法＝
   读面／写面切姊妹件 `weight/logWrite.ts`）；**本票不拆**（拆分不在写集）。
4. **第 8／30 条单点图的纵轴自适应**仍归 `packages/base-render`（共享层，本批不碰）。
