# T558 锁周结构禁委派层守卫＋清死件 · 证据

判定：已做完（源码 3 行＋探针 1 件＋本证据件）。95% 未确认不关票，待编排者确认两项上报（#556 基线更正、del-train 等第三条写入路径定夺）后再关。

## 一、基线更正（先更正再做）

- 票面验收第 1 条写“与兄弟票 #556 加固后的计数一致”。#556 实为体重空库阻断（`calorie.view.weight` 空库 exit 4，已关），与编辑器无关。
- 正位兄弟是 #557（契约门三处加固：X1 哨兵行、 C3.4 只验齐不验对、 C3.5 口径，目标 19/19）。
- 本票验收第 1 条按“#557 加固后计数”执行。请求编排者把票面 #556 改为 #557（本票不改票面，只在此留痕）。

## 二、结构纪律五步

- 第一步影响清单（均为计划编辑器能力 `src/render`，同一能力）：
  - `packages/skill-calorie/src/render/planEditorRuntime.ts`：set-slot 委派补 `lock()` 守卫 1 行＋删 `isFirstWeek` 死函数 1 行。
  - `packages/skill-calorie/src/render/planEditorCss.ts`：删 `.pe-plain` 死样式 1 条。
- 第二步结构设计：无新增目录层级；无新增公开接口；探针与证据是文档面（不管辖），不进能力门。
- 第三步写代码：跨能力引用无；常量类型无新增；只动本能力目录。
- 第四步超线报警：已超线，需要根据规则进行重构。`planEditorRuntime.ts` LF 425（台账值 425，改前改后同值，净零行）仍超 350，超因与拆法照台账（整台运行时一段 JS 文本；按交互族切 `planEditorPicker.ts`，待收口票认领），本票先不拆。`planEditorCss.ts` LF 140→139，未超线。
- 第五步交付对账：见 §七，偏差为零。

## 三、现状定格（改前）

- `add-train` 委派已有 `if (lock()) return;`（e656ee64 时段复用票顺带所加，本票不冒领）。
- `pick-slot` 委派已有 `if (lock()) return;`（同上 e656ee64，本票不冒领）。
- `set-slot` 委派无守卫（本票目标，已补）。
- `del-train` 约第 360 行无守卫（遗留出口，见 §六，不在本票写集）。
- 另见第三条写入路径（同上报，不扩写集）：`add-move`（开选择层）、`pick`（落子）、`del-move`（删动作）三处委派均无 `lock()` 守卫。锁周删训练／删动作控件虽不渲染（C3.3 判 0 个），合成派发仍可达，属同一类遗留。

## 四、改动（diff 3 行）

```diff
-  function isFirstWeek(){ return week === 0; }
     if (act === 'set-slot'){
+      if (lock()) return;
-.pe-plain{font-size:12.5px;color:var(--fg2);font-variant-numeric:tabular-nums}
```

- sha256（runId t558-sha256，持锁）：`planEditorRuntime.ts 0d8cdcba9971`、`planEditorCss.ts a2fca2a876c7`、探针 `t558-lock-probe.mjs 2b40b065f3ae`。

## 五、票面 5 条验收

1. 契约门：`node tooling/run-locked.mjs --ticket 558 --run-id t558-gate -- node docs/skills/skill-calorie/t554-plan-editor-gate.mjs --phase=gate` → exit 0，RESULT 19/19（#557 加固后计数，含 X3；§1①②④ 9 格全绿，未改红）。盘上门已含 T557 改点（C3.4/C3.5/X1/X3 文案），本票改动后仍全绿。
   - 附带发现（不碰门，只留痕）：门内备注①“委派处理器里没有 lock 守卫”一句已陈化（现 add-train/pick-slot/set-slot 三处皆有守卫），归 #557 或收口票刷文案，本票不动门文件。
2. 结构禁委派层守卫（自写探针 `docs/skills/skill-calorie/t558-lock-probe.mjs`，持锁真出口＋headless Chrome 合成派发）：
   - 还原必绿 runId t558-probe-restore exit 0：L1 段数 2→2 选时段层 0→0；L2 段数 2→2；L3 时段 凌晨→凌晨；M1 选时段层 0→1；M2 段数 2→3；M3 时段 凌晨→上午。RESULT 6/6。
   - 摘守卫必红 runId t558-probe-red exit 1：仅摘 set-slot 那 1 行 `if (lock()) return;`，L3 变红（时段 凌晨→上午，胶囊 disabled=true 照样被改），RESULT 5/6。
   - 变异两行：`RED: L3 set-slot 凌晨→上午 5/6 exit=1` ＋ `RESTORE: 6/6 exit=0`。
3. 母版周不受影响：同探针 M1/M2/M3（上条读数），第 1 周同一套派发照常生效。
4. 死件清理读数（源码面，持锁 `runId t558-counts-before / t558-counts2`）：
   - `isFirstWeek`：改前 1（仅定义，零引用）→ 改后 0。
   - `.pe-plain` 源码面（runtime＋css）：改前 1（runtime 0＋css 1）→ 改后 0。
   - 全仓另有历史引用（不算活引用，不随本票清）：验收墙实物 `scene05-验收墙/定训练计划-编辑器.html:1912` 1 处、`t554-plan-editor-probe.js:104` 1 处、`t554-plan-editor-mutation.mjs:116/118` 2 处、`t554-计划编辑器两处S1修复-证据.md` 3 处、`t554-review-报告.md` 2 处。门探针读 `day3Plain` 的判据是“锁周纯文本参数行＝0”（C3.4），删死样式不影响其通过（本轮门 19/19 为证）。
5. 回归 runId t558-regress exit 0：`t550-复制日志场景标识` ＋场景 05 四件（`test/scene05-read.test.mjs`、`test/scene05-write-create.test.mjs`、`test/scene05-write-mutate.test.mjs`、`test/scene05-write-status-435.test.mjs`）→ tests 10 pass 10 fail 0（日志 `.scratch/t558/regress.log`）。

编译：`node tooling/run-locked.mjs --ticket 558 --run-id t558-restore -- node node_modules/typescript/bin/tsc -b packages/skill-calorie` → exit 0（入口写死 tsc -b；`node_modules/.bin` 不存在、`npx tsc` 是假出口，不用）。

## 六、上报（不停手项之外，需编排者定夺）

1. `#556 基线更正请求`：票面验收第 1 条“#556”改为“#557”。依据：`gh issue view 556` 题为体重空库阻断已关；`gh issue view 557` 题为契约门三处加固（19/19）与门内 T557 文案一致。
2. `del-train 无守卫上报`：`planEditorRuntime.ts:359` `if (act === 'del-train'){ day(d).sessions.splice(s, 1); ... }` 无 `lock()` 守卫。锁周该按钮不渲染，但合成派发可删段，落库表（产物表）会跟着变。另 `add-move`/`pick`/`del-move` 亦无守卫（增删动作两条路）。本票未扩写集，现场另见 `.scratch/t558/handoff.md`。请编排者定夺是否另开一票收 `del-train`＋动作三处（建议与本票同 pattern：一处一守卫＋探针 L4/L5）。

## 七、交付对账（与声明写集逐行对）

- 声明写集：`packages/skill-calorie/src/render/planEditorRuntime.ts`（已改）、`packages/skill-calorie/src/render/planEditorCss.ts`（已改）、新建 `docs/skills/skill-calorie/t558-*`（本证据＋`t558-lock-probe.mjs`）、`.scratch/t558/`（草稿、日志、计数脚本、handoff）、`.changeset/558-*.md`（已建）。偏差为零。
- 未碰：门与探针（`t554-plan-editor-gate.mjs`/`t554-plan-editor-probe.js`/夹具／变异件）、SKILL.md、场景清单、别族件、`packages/base-render/**`、`packages/base-paint/**`。告警线台账未碰（runtime LF 425 前后同值，无陈化）。
- 用词：命令／页面交付／唤醒词／落库／形状，照 `docs/agents/wording.md`；未用禁用黑话。

## 八、未做项/下一手

- 未做：del-train/add-move/pick/del-move 四处守卫（等定夺）；门备注①陈化文案（归 #557）；票面 #556→#557 改字（需编排者改票面）。
- 下一手：编排者确认 §六两项 → 本票可关（当前 95%，不擅自 close）；若另开守卫票，本探针 L1–L3/M1–M3 可直接复用为底盘，另加 L4（del-train）/L5（动作）。
