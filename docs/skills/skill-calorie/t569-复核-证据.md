# t569 复核证据（模拟减重族 页07–13 · 报告§7.2 R-07~R-14）

> 票 #569 ｜ 复核席 ｜ 独立复跑（不采信实施者证据一字，自己的runId见各节）
> 权威清单：`docs/skills/skill-calorie/t520-视觉终审-报告.md` §7.2（R-07~R-14，每条现象/期望/验收原文）
> 约束：只读复核；复跑经 `node tooling/run-locked.mjs --ticket 569 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000 -- <命令>`；草稿只写 `.scratch/t569-R/`；不改源码与产物；不 push、不 close；不通读整份HTML（只正则取片段）；dsh visionreview已坏未试
> 编排裁决：571-R28锁现况S3、573-R59接受同字节由532索引承担、572-T5双向断言（本族复验不退化；21/22当刻仍同字节90096B，见§5）
> 实施者提交：`b1ce1c4`（3件：证据+`trendPredictDocs.ts`+`t569-sim-weight.test.mjs`），`git show --stat` runId=`768d5410-85b2-4805-9c6b-017b854cefc2`

## §0 复核写集与方法声明

- 复核写集：仅 `docs/skills/skill-calorie/t569-复核-证据.md` 一件。源码、测试、产物、台账一律不碰。
- 方法：当刻 `.scratch/t387/html/` 字节为准（历史读数只记时点）；产物只做剥style/script/注释后的正则计数与卡片切分；源码只读片段（`trendPredictDocs.ts:729-848`）；测试只跑不改（dist现况直跑，不重编）。
- 当刻字节漂移声明：复核时07/08/09均为76048B（实施者证据时75389B，+659B）；10为74626B（证据时73967B）；11为75338B（证据时74679B）；12为76050B（证据时75391B）；13为74982B（证据时74323B）。各页+659B左右，源内可见 `t573CrossCss()` 追加（跨票叠加），本族R断言仍全绿（见§2），漂移为加性 benign。
- 草稿：`.scratch/t569-R/probe-r.mjs`、`probe-v2.mjs`、`probe-a.mjs`、`probe-new.mjs`（runId见各节，不进仓）。

## §1 A标准轴（文档标准+气味基线+结构铁律抽查，只读）

- runId=`1d3772da-a90c-4a93-ab0d-0a67b64183a4`（`.scratch/t569-R/probe-a.mjs`）＋分隔符/响应式见§5。
- 文档标准：07–13可见文本黑话0（`赋能/闭环/颗粒度/沉淀/抓手/底座`全零）；分隔符 `audit-separators` 31/31 PASS（本族7页节点级全零）；触摸输入框 `min-height:44px` 沿公共层未动；结论/detail用`。`断句，无`；/｜/·`长串（分隔符PASS侧证）。
- 气味基线12项抽查（文档标准优先）：Mysterious Name无（`totalGap/periodTotal`名实相符）；Duplicated Code无新增（`periodTotal/totalGap`各一处定义；`7700`字面9处均为用户可见口径句/结论句内联数字，非代码常量分支，单源化会伤文案可读，记观察不记缺陷）；Feature Envy无（模拟页只读`WeightSimCut/WeightSimTarget`本族视图）；Data Clumps无新增；Primitive Obsession无（`fmtRate`语义档沿用）；Repeated Switches无；Shotgun Surgery无（改动收敛 `trendPredictDocs.ts` 一件符合页族装配现状）；Divergent Change无；Speculative Generality无（`restoreHead`仅两列表用，三列表显式不套）；Message Chains无（`cardsOf`切分一级）；Middle Man/Refused Bequest无。结论：A轴气味无新坏味道。
- 结构铁律抽查：`any`字面0、`unknown`0；跨能力引用走公开接口（`base-paint/blocks|base-paint`、analysis/simulate2、shared/docPage/copyArea、trendDocs导出件），未见深路径私件直引；文件出口10个（`build*Doc`×10）超铁律五“5个”计数，但为#518 W1搬家既有形状（9页同件），本票diff（+111/-22）未新增出口，拆法（按页族切两件＋共件薄转出）仍有效，本次先不拆理由成立；行数LF=1162超350在册（实施者时1006，+156为他票叠加），超线报警有效，记观察。结论：A轴通过（PASS），附两条观察（出口数/行数均为既有在册，非本票引入）。

## §2 B规格轴（R-07~R-14逐条复验＋R-09对账，自己的runId）

- 主探针 runId=`e2bd19f3-3dff-49d0-bee3-25cceef8b698`（`probe-r.mjs`）；修正探针 runId=`a694f538-003c-4bd3-98e1-bb4d582a12e7`（`probe-v2.mjs`，修chips/detail误取）；靶向测试 runId=`0b0d40f4-7b26-48ea-b29b-afcdf8bcef4f`（`t569-sim-weight` 9/9）。
- R-07（07/08恢复表头＋列式对齐，验收≤1或右缘共线）：产物含`tpd-track-table--cols2`＋`table-header-group`＋`td::before{content:none}`＋`text-align:right`各1；元素侧`data-label="模拟体重（kg）"`14处（标签在属性侧14个，样式`::before`被清零规则压住，验收取后者）。视觉07/08/11三页390档均为带表头真表格、数值右对齐（§4）。判：绿（本票范围）。
- R-08（同列1位，验收`^\d+\.\d$`）：07/08轨迹值各14个全匹配，坏0。判：绿（当刻成立＋加锁，本票未改字面）。
- R-09（先对账再施工，验收按现行产物）：当刻07可见`超出健康范围`0/`超出安全区间`0/`超范围`1、可见`—`0；结论`每天多减 300 卡，一周大约掉 1.36 kg。`；可行性detail`每天总缺口约 1494 卡。`徽章`超范围`；08同式（1.54 kg）。对账：t518 §9.5①记“值位—”是W5时点态，当刻判定卡已无值位（W6整格撤除，全页可见零`—`），t518该分支已由撤格替代完成；V1“三处复述”当刻成立（三处同义判断词），实施者按R-06先例收判断至徽标一处、detail补`newDeficit`互补事实，裁定成立。复核同意该对账结论。判：绿（本票范围）。
- R-10（09–13主标空格，验收`0卡|减\d|0天`==0）：09 H1`模拟减重（每天 -700 卡）`、10`模拟减重（30 天减 2 kg）`、11`模拟减重（60 天减 4 kg）`、12`模拟减重（90 天减 6 kg）`、13`模拟减重（45 天减 3 kg）`，H1三正则各0；chip第二格同spaced形态（`每天 -700 卡/30 天减 2 kg/...`），H1半角括号0（全角守W5-③）。判：绿（本票范围；07/08连带同规则无害）。
- R-11（10–13 detail非结论前缀，验收LCP<8）：10 detail`30 天合计约 15390 卡。`vs结论首句`要在 30 天里减掉 2 kg…`LCP=0；11为`60 天合计约 30780 卡。`LCP=0；12为`90 天合计约 46170 卡。`LCP=0；13为`45 天合计约 23085 卡。`LCP=0。判：绿（本票范围）。
- R-12（09–16结论最大，验收四卡不全等且结论最大）：静态模拟页含`nth-child(3)...28px;800`、预测页含`nth-child(4)...28px;800`（09/10与14/15/16各命中对应一条）；卡结构4卡、第3格为每周掉重/第4格为摄入预测，对位正确。视觉分歧见§4（07称同档、09称最大，前后不一致），按静态硬读数判绿，视觉记存疑不推翻。判：绿（本票范围，视觉存疑备注）。
- R-13（09–16 padding 4/8倍数）：09–16八页均含页侧`.ilife-block-kpi-card{padding:16px}`，产物`padding:14px`字面0（14px仍住公共层`blocks.ts`，本票页侧覆盖，归#567不动公共层正确）。判：绿（本票范围）。
- R-14（08–16空串，验收全批`data-label=""`==0）：元素侧（剥style/script/注释）08/09/10/14/16均为0；style内各2处（公共层防御选择器字面，归#567）。nav.mjs全文档正则把CSS字面计入是误报根因，实施者只锁元素侧正确。判：绿（页面侧已成立＋加锁；字面两处归#567）。
- R-03同形延伸（10/13）：眉题`模拟减重`，chips`卡路里 | 30 天减 2 kg | 趋势分析`（10）/`卡路里 | 45 天减 3 kg | 趋势分析`（13），眉题≠任一chip。判：绿（顺手，冻结门外）。
- 编排裁决回归：21/22当刻同字节90096B（R-59接受同字节现况未破）；R-28缺口族21本族未碰（响应式21三档0）；572-T5双向断言与本族无交集（t569 9/9＋分隔/响应全绿即未退化）。判：无退化。

## §3 自设新探针（被审脚本覆盖不到＋红绿两行，只读）

- 新探针：`.scratch/t569-R/probe-new.mjs`——验`所需缺口`卡算术一致性（`合计总数 == 卡值×天数`）。被审 `t569-sim-weight.test.mjs` R-11只断`合计约+卡`字面与LCP<8，不验乘法；本探针补该空隙。
- 绿行 runId=`9465cdd6-94c0-410a-a898-cd7e59f0a093` exit=0：`GREEN page=10 perDay=513 days=30 total=15390 expect=15390 PASS`；11为30780 PASS；12为46170 PASS；13为23085 PASS；`GREEN RESULT GREEN pass=4/4`。
- 红行 runId=`f69a023a-2278-443f-b480-bfb9de734401` exit=1（预期内红）：期望故意+1，`RED page=10 ... expect=15391 FAIL`；11/12/13同式FAIL；`RED RESULT RED fail=4`。两行证探针敏感且实现算术成立，全程只读产物、零落盘。

## §4 视觉双档（1440x900/390x844 fullPage＋读图，指页指名＋归属）

- 截图14张（本族每页双档，fullPage）：07桌`...07---86096ce0-shot-1440x900-fullpage.png`(1542px)/07手390(1735px)；08桌/08手；09桌/09手；10桌(1405px)/10手(1572px)；11桌(1583px)/11手(1712px)；12桌(1761px)/12手(1852px)；13桌(1494px)/13手(1642px)。路径见本节run记录（vision工具返回件，不进仓）。
- 读图（vision_describe，不通读HTML）：
  - 07桌＋手：轨迹表390档带表头真表格、数值右对齐成列（本票范围✓）；结论/detail/徽章三维度不同义（本票范围✓）；无折行压字溢出（✓）；但hero“每周掉重与75.1/300同档无强调”✗——与静态28/800规则矛盾，记视觉存疑（缩略误判可能，同实施者CDP纠正口径；复核未跑CDP计算样式，故不推翻静态绿）。
  - 09桌＋手：H1单行无孤字（本票范围✓，vision把“每天 -700 卡”说成无空格系OCR丢空格，机器H1hits=0为准）；每周掉重字符最宽视觉最大（本票范围✓）；390真表格无硬伤（✓）。
  - 10桌＋手：vision判“H1无分隔/减字挤压”与“日均vs期间总量本质重复”——系审美口感（spaced指半角空格非断行节奏；R-11验收只量LCP<8，机器LCP=0已过），不构成验收失败；手机detail换行2次属网格内正常换行，溢出门0（§5）为准。记视觉口感分歧，不记缺陷。
  - 08手＋11手：两页真表格＋右对齐✓；H1无孤字溢出✓；卡片无压字✓；范围外硬伤无、无需转票✓。
  - 12手＋13手：H1单行无孤字✓（vision OCR“90天减6kg”丢空格，机器为准）；detail与结论角度不同不重复✓；卡片数字完整✓；S3范围外硬伤无、无转票✓。
- 归属汇总：本票引入缺陷0；本票范围8条机器全绿、视觉无溢出硬伤；范围外发现0（故无S3转票措辞）。

## §5 门禁与只读声明（git diff --cached复核）

- `audit-separators --dir .scratch/t387/html` runId=`b1b95a10-1fb3-48d0-a754-bb3c29da85e8`：`RESULT: 31/31 PASS`（本族7页节点级全零）。
- `measure-responsive --dir .scratch/t387/html` runId=`2a635f07-5c1e-430f-9a16-8635f75978ec`：`OVERFLOW-ZERO pages=31 cells=93 failed=0`（31页×3档逐格0，含R-07恢复后两列表390档）。
- `node --test t569-sim-weight.test.mjs` runId=`0b0d40f4-7b26-48ea-b29b-afcdf8bcef4f`：`tests 9/pass 9/fail 0`（只跑不改，未重编）。
- `gen-cli.mjs --check` runId=`fe94ef6d-82fd-4af2-b104-c22983dbcbfe`：`GEN-CHECK PASS 键128`。
- 只读声明：复核全程未改源码/测试/产物/台账；`.scratch/t569-R/`4个探针与14张截图artifact不进仓；工作区改动仅本证据件一件（提交前 `git diff --cached --name-only` 只含 `docs/skills/skill-calorie/t569-复核-证据.md`，见提交run记录）。
