# #520 视觉重评席 V3 第二轮（页 17–23）

本席只评产品目录 `.scratch/t387/html/` 里的 7 页：17～23。**本文件每评完一页立刻落盘该页小节**，任何时刻都保持「已完成页完整、未完成页留空骨架」。独立重评：实施者结论一律不采信，全部自己重测。

## 一、尺（全批统一，本席不自定，与首轮同一把）

| # | 维度 | 满分 | 折入总分权重 |
|---|---|---|---|
| 1 | 结构纪律 | 20 | ×0.20 |
| 2 | 信息层级与版式细调 | 25 | ×0.25 |
| 3 | 分隔符与文字纪律 | 20 | ×0.20 |
| 4 | 跨宽自适应 | 15 | ×0.15 |
| 5 | 手机端与 HELP 同档 | 20 | ×0.20 |

加权总分 ＝ 结构×0.20 ＋ 信息×0.25 ＋ 分隔符×0.20 ＋ 跨宽×0.15 ＋ 手机端×0.20，**逐页给分**。

## 二、用户硬要求（每页逐条现量，不凭印象）

1. 桌面 1440 与手机 390／768 三档无溢出、无塌列，触摸目标不少于 44px。
2. 手机档与 HELP 同档，参照 `fixtures/help-instances/卡路里_HELP_20260731_201530.html` 的断点与窄表堆叠做法。
3. 设计师审美：间距取 4 或 8 的倍数、字号成档有下限、数值列等宽右对齐、卡／表／图左缘共线、留白有节。
4. 文案零冗余：同一事实一页只讲一次，用词守 `docs/agents/wording.md`（禁黑话、禁英文内部标识符上屏）。
5. 分号／竖线／间隔号拼串一律改成卡片／分行／徽章／图例等可见结构。
6. 视觉审查用手拍（本席用 `vision_html_screenshot` 双档 fullPage ＋ `vision_describe`）；`dsh visionreview` 已坏，未试。

## 三、判读口径与证据

- 视觉模型是提示器不是证据：凡视觉读到的，都在 HTML 源码里找到对应文本／类名才写进结论；找不到的写「源码未证实」，不作扣分依据。
- 尺子量到的东西用本席自建探针复核，读数落在 `.scratch/t520-R2-V3/`。
- 中段未细看的地方，页内明写「中段未细看」。
- 本轮重点复验：18／19／20 三页是否补可见小节标题（与 01–17 同档，导航 `参数／概览／数据与日志` 应有对应 h2）；但必须逐页完整重打分。17 页表格窄档与 21／22 图表字号等旧项照尺重打，不沿用旧分。
- 已知架构（R-59）：21 与 22 逐字节相同、23 与 01 逐字节相同；若仍相同则如实记录、不扣分、不编数。

## 四、页 17 · 摄入预测(自定义)

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/17-摄入预测(自定义).html` |
| 字节 | 76,526（与首轮一致） |
| SHA256 | `55C9DB1C…E2527`（与首轮同一串，**字节未变**） |
| 本席实拍 | `…17---4a1b4ff3-shot-1440x900-fullpage.png`（1440×1636）、`…-390x844-fullpage.png`（390×2247） |

本页字节与首轮逐位相同：几何量（溢出/触摸/字号/表宽）本轮全部重测，逐列宽引用首轮机器读数时明示（字节相同则布局确定）。

### 1 第一眼看到什么、层级清不清

第一眼：页眉「趋势分析」→ 大标题「摄入预测（自定义 60 天）」→ 三枚胶囊 → 蓝边结论条 `<p class="ilife-block-conclusion">按当前速率，60 天后每天大约吃 1692 卡。目标 1800 卡。</p>`（源码逐字）→ 页内导航四项 → 参数／概览／摄入预测轨迹／数据与日志四节（h2＝4，源码逐个确认）。层级清楚的三层。
**但页眉与胶囊重复**：「趋势分析」既是页眉又是第三枚胶囊（`<p class="ilife-block-page-shell-eyebrow">趋势分析</p>` 源码可证），同屏读两遍。

### 2 挤／空／错位／列宽失衡／截断／横滚

- **空（本页最大缺陷，本轮复测成立）**：1440 档表实框 `w334[181..515]`（本席探针），**右缘 515 到内容区右缘 1260 之间约 745px 全空**；768 档同样 `w334[17..351]`。根因源码可查：`.tpd-track-table--cols3 .ilife-block-data-table{display:table;width:auto;margin-inline:auto}` 被同页后一条 `.ilife-block-data-table-table{margin-left:0;margin-right:auto}` 把左居中改成左贴边（两条规则源码逐字，可见上条 grep）。
- **列宽失衡**：首列 `min-width:140px`（`.tpd-track-table--cols3 … td:first-child{min-width:140px}` 源码可证）；三列机器实宽 168／66／100 为首轮读数（本轮字节逐位相同、表总宽 334 复测一致，布局确定，沿用并明示）。
- **不挤不截断不横滚**：三档 `overflowX=0`，越界 0，横滚容器 0（本席探针）。日期格 h45 系 `td{padding:12px 14px}`（源码逐字：上下 24px＋单行约 21px）＝单行未折断，`wrapSuspects` 属探针假阳性（本轮独立复算，与首轮同结论）。
- 视觉称「表格占 7 成、日期列最窄、区间列最宽 3–4 倍」→ 与机器实测（334/1080≈3 成、日期列最宽 168）两处冲突，**判为视觉误读**；但「右侧大片留白」「预计区间首行是单值」两点与机器/源码一致，予以采信（见第 5 维）。
- 中段（参数节、口径行）已看，无错位。

### 3 有没有拿分号、间隔号、竖线把并列信息挤成一串

**没有。** 可见文本分隔符全 0（本席探针 `sep`）；口径行三个独立 `<span>` 靠 `gap` 分列。

### 4 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真）**：① 页眉＋胶囊「趋势分析」；② `<h2>摄入预测轨迹</h2>`＋`<caption>摄入预测轨迹（共 60 天，每周一点）</caption>` 同屏相邻（两处源码逐字）；③ 「60 天」多处（大标题／结论条／表题／参数输入值）；④ `1692` 全页 3 次（结论条／概览卡／表末行，源码计数）；⑤ `1800` 全页 5 次（含复制载荷，可见为结论条＋目标卡）。
- **英文内部标识符上屏：无**（探针 `leaks:{}`；`calorie.view.predict` 只活在不上屏的 `data-t` 载荷）。

### 5 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字＋右对齐：是。** `.tpd-track-table--cols3 … td.ilife-block-data-table-cell-right{…text-align:right;font-variant-numeric:tabular-nums}`（源码逐字）；探针表头 `align[left,right,right]`。
- **小数位一致：是**（整数配整数，「日变化 1.7」一位小数自成一档）。
- **缺值写 —：本页无缺值**（`—` 计数 0），不涉及。
- **写法不一致（真）**：「预计区间」首行单值 `1589`，次行起 `1515 至 1687` 等区间（前两格源码逐字）；首行与同行「预测摄入」1589 重复。

### 6 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **表格读得开**：≤640px 每行变圆角卡（`.tpd-track-table--cols3 … tbody>tr{display:block;…border-radius:12px}` 源码可证），标签在左值在右，`thead{display:none}`。
- **触摸目标**：三档可点件 12 个，高全部 ≥44px，低于 44px 高 **0 处**（本席探针）。
- **日期没被折断**：`td{white-space:nowrap}`（cols3 规则源码可证）＋ h45＝padding＋单行（见第 2 维）。
- **字号：本轮推翻首轮一项**：首轮称移动行标签 `11.5px` 低于 12px 下限；本轮源码复核：`11.5px` 的通用 `td::before` 与 cols3 专用 `td::before{font-size:12px}` 同在 `@media(max-width:640px)` 内、无 `@layer`、无 `!important`（三项计数皆 0/verified），**高特异性规则胜出，实际渲染 12px，不低于下限**。此项本轮不扣分。可见最小字号 12px（探针 `fontMin=12`，本轮探针已剔除不可见元素）。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **17** | 四节 h2＋导航＋真表（thead/tbody/caption/th）＋移动卡片化都在位；扣 3：表在 ≤640 档有新老两套互相覆盖的展示规则 ＋ `#sec-data` 内再嵌无标题 `<section class="ilife-block-copy-block">`（源码可证）＋ `.ilife-block-kpi-card:nth-child(4) .ilife-block-kpi-card-value{font-size:28px}` 按位置单调一张卡（同排余卡 22px，源码可证） |
| 2 | 信息层级与版式细调 | 25 | **19** | 层级清楚、四卡等宽、数值成档；扣 6：表右约 745px 空（复测 w334[181..515]）＋ 列宽 168／66／100 失衡（首列 min-width:140px 源码可证）＋ 间距出 4 倍数（胶囊 `2px 8px`／`margin 6px`、数值行 `gap:6px`×7、表题 `padding:10px`×5，源码计数） |
| 3 | 分隔符与文字纪律 | 20 | **15** | 可见分隔符 0、口径三段独立 `<span>`、无英文标识符上屏；扣 5：页眉与胶囊／标题与表题／60 天／1692／1800 五组复述 ＋ 预计区间首行单值冒充区间 |
| 4 | 跨宽自适应 | 15 | **12** | 三档溢出 0、越界 0、窄表堆叠成立；扣 3：768 档仍收成 334px（复测[17..351]），表格不利用可用宽度 |
| 5 | 手机端与 HELP 同档 | 20 | **18** | 可点件 12 处高全 ≥44px、可见字号 12px（含行标签，级联复核已证）、日期不折、无横滚；扣 2：断点取 640／820 而 HELP 取 600／900（HELP 源文件本轮亲测）＋ 胶囊/数值行间距出 4 倍数 |

**加权总分：17/20×100×0.20 ＋ 19/25×100×0.25 ＋ 15/20×100×0.20 ＋ 12/15×100×0.15 ＋ 18/20×100×0.20 ＝ 85×0.20 ＋ 76×0.25 ＋ 75×0.20 ＋ 80×0.15 ＋ 90×0.20 ＝ 17.0 ＋ 19.0 ＋ 15.0 ＋ 12.0 ＋ 18.0 ＝ 81.0**

**页 17 加权总分 81（未过 90；首轮 80，+1 来自 mobile 行标签 11.5px 经级联复核推翻）。**

## 五、页 18 · 摄入预测(营养目标达成预测)

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/18-摄入预测(营养目标达成预测).html` |
| 字节 | 72,507（首轮 72,387，**变了**） |
| SHA256 | `2DAA5D76…A87D169`（首轮 `1D41E3FC…EF61`，**不同**） |
| 本席实拍 | `…18---a1696c82-shot-1440x900-fullpage.png`（1440×1007，首轮 1440×900一屏；页变高 112px 系新增 h2 所致）、`…-390x844-fullpage.png`（390×1076，首轮 390×964） |

### 1 本轮重点复验：小节标题补上了吗

**补上了，且与导航逐字对齐。** 源码三处 `<h2 class="tpd-sec-title">参数</h2>／概览／数据与日志`，导航三锚点 `sec-params＝参数／sec-overview＝概览／sec-data＝数据与日志`，三节 `section#sec-params／#sec-overview／#sec-data` 各顶一个 h2。样式 `.tpd-sec-title{font-size:16px;line-height:1.5;font-weight:700;margin:0 0 8px}` 无隐藏；本席探针三档 `h2: +参数/16px｜+概览/16px｜+数据与日志/16px`（+＝可见）；视觉桌面截图独立确认「3 个 h2：参数／概览／数据与日志」。首轮「h2/h3 计数 0、三节光节」已不存在。

### 2 第一眼看到什么、层级清不清

第一眼是结论条 `<p class="ilife-block-conclusion">这段的日均摄入 1589 卡，比目标 1800 卡差了 211 卡。</p>`（源码逐字），第二眼是大标题 `摄入预测（营养目标达成预测）` 与三枚胶囊。结论—卡片—复制—口径四层清楚；h2 补上后读者不再只靠胶囊认路。

### 3 挤／空／错位／截断／横滚

- **无溢出无塌列**：三档 `overflowX=0`，越界 0，横滚容器 0（本席探针）。1440 页高 1007、390 页高 1076，均短，无拉白。
- **无表无图**：`<table>` 计数 0。390 档四卡仍 2 列，无塌列。
- 中段（参数表单、按钮行、口径行）已看，无压字。视觉称「左右对称留白、卡片均匀 2×2、无溢出重叠」，与探针一致。

### 4 有没有拿分号、间隔号、竖线把并列信息挤成一串

**没有。** 可见文本 `；｜|·•／` 计数全 0（本席探针 `sep`）；口径行是三个独立 `<span>`：`<p class="ilife-block-caliber"><span>目标＝每天的热量目标</span> <span>达标线＝日均偏离不超过目标的 10%</span> <span>摄入记录不到 14 天不出判定</span></p>`。视觉截图称口径行「用｜分隔」→ 与机器计数冲突，**判为视觉误读**，不采信。

### 5 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真）**：结论条一句复述三卡三数（1589／1800／211）；`1800` 全页出现 6 次含源码载荷，可见三处（结论条、「目标」卡、`相对目标 1800 卡`）；页眉 `趋势分析` 与第三枚胶囊 `趋势分析` 同屏重复（源码两处逐字）。
- **英文内部标识符上屏：无**（探针 `leaks:{}`）。

### 6 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是。** `.ilife-block-kpi-card-value{font-size:22px;font-variant-numeric:tabular-nums}`（源码逐字）。
- **小数位一致：是**（1589／1800／211 全整数）。
- **缺值写 —：本页无缺值**（`—` 计数 0），不涉及。
- **判定卡不成形（真，与首轮同病）**：第四卡源码 `是否在轨＋<div class="ilife-block-kpi-card-detail">日均离目标偏开。</div>＋徽章偏离`，无数值；前三张「标签＋22px 数值＋单位」，它「标签＋不成句小字＋徽章」；判定依据（10% 达标线）住在页脚口径行，离判定块一屏之隔。

### 7 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **本页无表**，窄表堆叠不适用。
- **触摸目标**：三档可点件高全部 ≥44px，低于 44px 高 **0 处**（本席探针 `under44h=0`；`.ilife-block-toc a{min-height:44px}` 源码可证）。
- **日期没被折断**：探针 `wrapSuspects` 空；参数输入单行容纳起止日期。
- **字号**：可见最小 12px（页眉，探针 `fontMin=12`，且本轮探针已剔除不可见元素，首轮的 thead 假阳性不再出现）。

### 8 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **17** | 三节各有可见 h2 且与导航逐字对齐（本轮修复）；扣 3：12 处 `.tpd-track-table--cols3` 规则对应 0 张表（无用规则，源码计数）＋ `#sec-data` 内再嵌无标题 `<section class="ilife-block-copy-block">` |
| 2 | 信息层级与版式细调 | 25 | **19** | 结论—卡片层级清楚、四卡 2×2、h2 补齐认路；扣 6：结论条复述三卡三数 ＋ 判定卡不成形（无数值、判定句不成句）＋ 判定依据远在页脚 ＋ 间距出 4 倍数（`padding:14px`×4、胶囊 `2px 8px`／`margin 6px`、数值行 `gap:6px`，源码计数） |
| 3 | 分隔符与文字纪律 | 20 | **16** | 可见分隔符 0、口径三段独立 `<span>`、无英文标识符上屏；扣 4：结论条复述三数 ＋ `1800` 可见三写 ＋ 页眉与胶囊重复「趋势分析」＋ 「日均离目标偏开。」不成句 |
| 4 | 跨宽自适应 | 15 | **14** | 三档溢出 0、卡片不塌；扣 1：768 与 1440 同一套版式（页内主断点 820，无平板自有处理） |
| 5 | 手机端与 HELP 同档 | 20 | **18** | 可点件高全 ≥44px、可见字号 12px、无横滚、日期不断；扣 2：主断点 820（HELP 取 600／900）＋ 本页无表可比窄表堆叠做法 |

**加权总分：17/20×100×0.20 ＋ 19/25×100×0.25 ＋ 16/20×100×0.20 ＋ 14/15×100×0.15 ＋ 18/20×100×0.20 ＝ 85×0.20 ＋ 76×0.25 ＋ 80×0.20 ＋ 93.33×0.15 ＋ 90×0.20 ＝ 17.0 ＋ 19.0 ＋ 16.0 ＋ 14.0 ＋ 18.0 ＝ 84.0**

**页 18 加权总分 84（未过 90；首轮 81，+3 来自 h2 补齐：结构 +2、信息 +1）。**

## 六、页 19 · 摄入预测(卡路里缺口预测)

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/19-摄入预测(卡路里缺口预测).html` |
| 字节 | 72,039（首轮 71,919，**变了**） |
| SHA256 | `8E9E3B53…92EA529`（首轮 `68627C56…4177`，**不同**） |
| 本席实拍 | `…19---93376f3b-shot-1440x900-fullpage.png`（1440×900一屏内）、`…-390x844-fullpage.png`（390×941） |

### 1 本轮重点复验：小节标题补上了吗

**补上了，且与导航逐字对齐。** 源码 `<h2 class="tpd-sec-title">参数</h2>／概览／数据与日志` 三处；本席探针三档 `h2: +参数/16px｜+概览/16px｜+数据与日志/16px`（+＝可见）；视觉桌面截图独立确认「3 个 h2：参数／概览／数据与日志」。首轮「h2/h3 计数 0、`.tpd-sec-title` 定义了不用、用户把胶囊当节标签」已不存在。

### 2 第一眼看到什么、层级清不清

第一眼是结论条 `<p class="ilife-block-conclusion">这段时间平均每天有 +1196 卡缺口，折算下来一周约 1.09 kg。</p>`（源码逐字），第二眼是大标题与三枚胶囊，再往下两张卡（平均缺口 +1196 卡/天／每周掉重 1.09 kg/周，视觉确认）。h2 补齐后层级清楚。

### 3 挤／空／错位／截断／横滚

- **无溢出**：三档 `overflowX=0`，越界 0，横滚容器 0（本席探针）。
- **口径四段挤在一行（真，本页最重版式问题，复测成立）**：1440 档四个 `<span>` 同行 y=733，`[180..384] [396..540] [552..731] [743..1196]`，跨 1016px，段间隙恰 12px（本席 probe2 实测；末段单段 453px）。390 档折成四行（probe2：y=1433/1455/1481/1503/1525），窄档自动换行。
- **视觉称「内容偏左、右侧 30% 空白、卡片左右不对齐」→ 判为视觉误读**：probe2 实测两卡 `[180..714] [726..1260]`，壳 `160..1280`，左右边距对称（180/180），卡片等宽并排；不采信、不扣分。
- 参数行整行拉满（input 100% 宽只放 10 字符日期）属共享件形状，只记不扣。
- 中段（参数、两卡、按钮行、口径行）已看，无压字、无截断。

### 4 有没有拿分号、间隔号、竖线把并列信息挤成一串

**符号层面：没有。** 可见文本 `；｜|·•／` 全 0（本席探针 `sep`）；`::after content:none` 系 flex `gap` 间隙。视觉两档均称「有竖线分隔」→ 与机器计数冲突，**判为视觉误读**（模型把 12px 间隙读成竖线），不采信、不扣分。但**四段并排挤在一行 1016px 是真的**（见第 3 维）。

### 5 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真）**：结论条复述两卡两数（+1196、1.09）；页眉「趋势分析」与第三枚胶囊「趋势分析」重复（`<p class="ilife-block-page-shell-eyebrow">趋势分析</p>` 源码可证）。
- **用词（真）**：口径第四段源码逐字 `符号＝缺口取正数表缺口，每周掉重取掉量为正，体重速率页另取变化率为负表下降`——「表」代「表示」不成句，且夹带跨页对照（体重速率页）。
- **英文内部标识符上屏：无**（探针 `leaks:{}`）。

### 6 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是**（指标值 `tabular-nums`，模板规则；本页无表）。
- **小数位**：`+1196` 整数带正号、`1.09` 两位小数，各成档。
- **缺值写 —：本页无缺值**（`—` 计数 0），不涉及。

### 7 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **本页无表**，窄表堆叠不适用；两卡 390 档仍并排（首轮机器读数 173＋12＋173；本轮页高 941 一屏多，无塌列）。
- **触摸目标**：三档可点件高全 ≥44px，低于 44px 高 **0 处**（本席探针）。
- **日期没被折断**：探针 `wrapSuspects` 空。
- **字号**：可见最小 12px（页眉，探针 `fontMin=12`）。

### 8 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **17** | 三节各有可见 h2 且与导航对齐（本轮修复）；扣 3：12 处 `.tpd-track-table--cols3` 规则对应 0 张表（源码计数） |
| 2 | 信息层级与版式细调 | 25 | **19** | 两卡等宽居中共线（180..1260，probe2）、h2 补齐；扣 6：口径四段挤一行 1016px（段间隙仅 12px，probe2）＋ 结论条复述两卡两数 ＋ 间距出 4 倍数（`padding:14px`×4、`gap:6px`×7、胶囊 `2px 8px`／`margin 6px`，源码计数） |
| 3 | 分隔符与文字纪律 | 20 | **15** | 可见分隔符 0、口径四段独立 `<span>`、无英文标识符上屏；扣 5：结论条复述两数 ＋ 页眉与胶囊重复「趋势分析」＋ 口径第四段「表」代「表示」不成句并夹跨页对照 |
| 4 | 跨宽自适应 | 15 | **14** | 三档溢出 0、两卡两档都 2 列、口径行窄档自动折行；扣 1：768 与 1440 同一套版式 |
| 5 | 手机端与 HELP 同档 | 20 | **18** | 可点件高全 ≥44px、可见字号 12px、无横滚、日期不断；扣 2：主断点 820（HELP 取 600／900）＋ 本页无表可比窄表堆叠做法 |

**加权总分：17/20×100×0.20 ＋ 19/25×100×0.25 ＋ 15/20×100×0.20 ＋ 14/15×100×0.15 ＋ 18/20×100×0.20 ＝ 85×0.20 ＋ 76×0.25 ＋ 75×0.20 ＋ 93.33×0.15 ＋ 90×0.20 ＝ 17.0 ＋ 19.0 ＋ 15.0 ＋ 14.0 ＋ 18.0 ＝ 83.0**

**页 19 加权总分 83（未过 90；首轮 80，+3 来自 h2 补齐：结构 +2、信息 +1）。**

## 七、页 20 · 摄入预测(摄入稳定性预测)

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/20-摄入预测(摄入稳定性预测).html` |
| 字节 | 72,270（首轮 72,150，**变了**） |
| SHA256 | `D6300A42…76DF0C`（首轮 `24E9E793…0068`，**不同**） |
| 本席实拍 | `…20---43df56f7-shot-1440x900-fullpage.png`（1440×1007，首轮 895）、`…-390x844-fullpage.png`（390×1067，首轮 955） |

### 1 本轮重点复验：小节标题补上了吗

**补上了，且与导航逐字对齐。** 源码 `<h2 class="tpd-sec-title">参数</h2>／概览／数据与日志` 三处；本席探针三档全 `+可见/16px`；视觉桌面截图独立确认「3 个 h2：参数／概览／数据与日志」。首轮「h2/h3 计数 0、`.tpd-sec-title` 定义不用」已不存在。

### 2 第一眼看到什么、层级清不清

第一眼是结论条 `<p class="ilife-block-conclusion">日均摄入 1589 卡，每天上下波动 42 卡，算稳。</p>`（源码逐字），第二眼是大标题与三枚胶囊。h2 补齐后分节可认路；但判定卡「是否稳定」落在第二行左格，答案的视觉权重弱于留白（见第 3 维）。

### 3 挤／空／错位／截断／横滚

- **空（本页最重缺陷，复测成立）**：1440 档三张卡实框 `[180..714] [726..1260] [180..714]`（高 125），**第二行右格 534×125 没有任何元素**（本席 probe2 实测）。视觉独立确认「第 2 行只有左侧一张、右侧位置留白，L 形布局」。
- **根因源码可证**：通栏补齐规则 `.ilife-block-kpi-card-grid>.ilife-block-kpi-card:nth-child(odd):last-child{grid-column:span 2}` **只活在 `@media (max-width:820px)` 里**（页内注释自称「窄屏（820·同 HELP）的页面级部分」）；≥821 档无对应处理。390 档第三卡通栏 `[16..374] w358`（probe2），跨档行为不一致。
- **无溢出**：三档 `overflowX=0`，越界 0（本席探针）。口径三段 1440 同排（probe2 `[180..408][420..613][625..787]`），390 折三行。
- 中段（参数、三卡、按钮行）已看，无压字。

### 4 有没有拿分号、间隔号、竖线把并列信息挤成一串

**没有。** 可见文本分隔符全 0（探针 `sep`）；口径三段是三个独立 `<span>`（`波动＝每天摄入偏离日均的幅度（标准差）`／`判据＝上下波动不超过 300 卡算稳`／`摄入记录不到 14 天不出判定`，源码逐字）。视觉称口径行「用竖线｜分隔」→ 与机器计数冲突，**判为视觉误读**，不采信。

### 5 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真）**：① 结论条「1589 卡／42 卡」与两张数字卡同数据；② 窗口 `2026-08-18 至 2026-09-16` 在源码出现**两次**（均值卡小字＋页脚数据来源，`Matches` 计数 2）；③ 页眉「趋势分析」与第三枚胶囊重复。
- **英文内部标识符上屏：无**（探针 `leaks:{}`）。

### 6 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是**（模板 `tabular-nums`；本页无表）。
- **小数位一致：是**（1589／42 全整数）。
- **缺值写 —：本页无缺值**，不涉及。
- **判定卡不成形（真）**：第三卡源码 `是否稳定＋<div class="ilife-block-kpi-card-detail">每天的摄入比较匀。</div>＋徽章稳定`，无数值；判定依据（300 卡）住在页脚。

### 7 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **本页无表**；390 档末卡通栏（probe2 `w358`），规则出处见第 3 维。
- **触摸目标**：三档可点件高全 ≥44px，低于 44px 高 **0 处**（探针）。
- **日期没被折断**：日期输入单行；390 档一条 `wrapSuspects` 是均值卡小字「日均，窗口 2026-08-18 至 2026-09-16」h36（两行自然换行，非日期格被折断，不记缺陷）。
- **字号**：可见最小 12px（探针 `fontMin=12`）。

### 8 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **17** | 三节各有可见 h2 且与导航对齐（本轮修复）；扣 3：12 处 `.tpd-track-table--cols3` 规则对应 0 张表（源码计数） |
| 2 | 信息层级与版式细调 | 25 | **17** | h2 补齐、前两卡同宽；扣 8：第三卡右侧 **534×125 空格**（桌面档，probe2；补齐规则只在 ≤820 档）＋ 判定卡不成形（无数值，依据在页脚）＋ 结论条复述两数 ＋ 窗口事实两遍 ＋ 间距出 4 倍数 |
| 3 | 分隔符与文字纪律 | 20 | **16** | 可见分隔符 0、口径三段独立 `<span>`、无英文标识符上屏；扣 4：结论条复述 ＋ 页眉与胶囊重复 ＋ 窗口事实两遍 |
| 4 | 跨宽自适应 | 15 | **13** | 三档溢出 0、390 档末卡通栏；扣 2：**同一组件 1440 留空格、390 才补齐**（补齐规则只在 ≤820 档，源码可证），跨档不一致 |
| 5 | 手机端与 HELP 同档 | 20 | **18** | 可点件高全 ≥44px、可见字号 12px、无横滚；扣 2：断点取 820／640 而 HELP 取 600／900（HELP 源文件亲测，本轮复核）＋ 本页无表可比窄表堆叠做法 |

**加权总分：17/20×100×0.20 ＋ 17/25×100×0.25 ＋ 16/20×100×0.20 ＋ 13/15×100×0.15 ＋ 18/20×100×0.20 ＝ 85×0.20 ＋ 68×0.25 ＋ 80×0.20 ＋ 86.67×0.15 ＋ 90×0.20 ＝ 17.0 ＋ 17.0 ＋ 16.0 ＋ 13.0 ＋ 18.0 ＝ 81.0**

**页 20 加权总分 81（未过 90；首轮 78，+3 来自 h2 补齐：结构 +2、信息 +1）。**

## 八、页 21 · 查热量缺口

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/21-查热量缺口.html` |
| 字节 | 90,013（与首轮一致） |
| SHA256 | `6B57286E…3166`（与首轮同一串；与页 22 **逐字节相同**，R-59 架构延续，如实记录、不扣分） |
| 本席实拍 | `…21---e5a69702-shot-1440x900-fullpage.png`（1440×1646）、`…-390x844-fullpage.png`（390×1643） |

### 1 第一眼看到什么、层级清不清

第一眼是结论条 `<p class="ilife-block-conclusion">这段时间平均每天有 +1208 卡缺口，一周合计 +8454 卡，折算下来约 1.1 kg。</p>`（源码逐字），第二眼是大标题与胶囊，再往下四卡一排、占比行、折线图卡、折叠明细、合计行、复制按钮、口径。九节中有 1 个 h2（`<h2 class="ilife-block-chart-block-title">每日摄入与消耗</h2>`，源码确认；视觉把顶部大标题误认成 h2，源码 h2 计数为 1，以源码为准），其余八节无标题，仍靠六枚胶囊导航认路。

### 2 挤／空／错位／列宽失衡／截断／横滚

- **不挤不截断不横滚**：三档 `overflowX=0`，越界 0（本席探针）。四卡一排 `261×4＋12×3＝1080`（probe2），与内容宽严丝合缝。
- **图表字号倒挂（真，本页最重版式问题，本轮复测成立）**：图 `viewBox="0 0 580.0 260.0"`（源码逐字）＋ `width:100%` 缩放。probe2 实测：1440 档图宽 1050（×1.810），轴刻度 CSS 10.8px 渲染框高 **26px（≈19.5px 字）**；390 档图宽 328（×0.566），图内字 CSS 19.4px 渲染框高 **15px（≈11.0px 字）**。桌面刻度比正文 13px 大一档半、手机掉到 12px 下限之下；图例 HTML 12px 不缩放（`.ilife-charts-legend{…font-size:12px}` 源码可证）。视觉本轮确认「坐标轴数字视觉上更大、图例小而浅」，与机器一致，采信。
- **图内标注贴线（真，轻）**：`<text … y="174.0">摄入目标 1800 卡</text>` vs `<line … y1="178.0" …>`（源码逐字）——基线与虚线隔 1.5 用户单位（1440 档渲染约 2.7px）。视觉称「压在虚线上、重叠遮挡」→ **源码证实为贴线非压线**，按留白不足轻记。
- 合计行「合计缺口」无右注释（源码该行只两个 `<span>`，前两行有三段），轻版式不齐。明细表收在 `<details>` 内（390 档 w0 未展开），属长表折叠正确做法。
- 中段（参数／四卡／占比／合计／页脚）已看，无压字。

### 3 有没有拿分号、间隔号、竖线把并列信息挤成一串

- **可见分隔符：0**（本席探针 `sep`）；口径行独立 `<span>`。
- **但有一处符号拼串（真）**：第二卡细字源码逐字 `日常消耗 2635 ＋ 运动 171 卡`——全角加号把两项消耗拼在一行小字里，轻扣（视觉截图逐字确认）。

### 4 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真，可见复述均源码计数）**：`1800` 可见至少 4 处（卡细字「目标 1800 卡/天」／图例／图内标注／页脚「横线是摄入目标 1800 卡」，源码各逐字）；`8454` 可见 3 处（结论条／「周缺口 8454 卡」／合计行「+8454 卡」）；`+1208`、`1.1` 各 2 处（结论条＋卡）。
- **英文内部标识符上屏：无**（探针 `leaks:{}`）。页内样式留带票号类名（`t571` 源码计数 8 处）——不上屏，只记一笔。
- 同一页两种数字写法：图轴带千分位（`1,452／2,276／3,100`，probe2 图内文本）而表写 `1649／2935`，记一笔。

### 5 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是**（表 `tabular-nums`；四卡同；合计行同）。
- **数值列右对齐：是**（探针 `align[left,right,right,right,left]`，数值列带 `cell-right`）。
- **小数位**：整数配整数，`1.1` 一位小数自成一档。
- **缺值写 —：本页无缺值**（`—` 计数 0），不涉及。

### 6 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **明细表默认收起**（`<details>` 未展开时表 w0，probe2）；展开机制为真表＋`data-label`，堆叠规则与页 17 同套（源码类名一致）。390 档 h1 两行（h62，自然换行，非截断）。
- **触摸目标**：三档可点件 14 个，高全 ≥44px，低于 44px 高 **0 处**（探针）。
- **字号**：可见正文最小 12px；**图内文字渲染约 11px，低于 12px 下限**（见第 2 维）。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **16** | 九节有 id、六项导航、`<details>` 折叠、真表 5 列带 `th[scope]`＋`data-label`、图 `role` 语义、有 1 个 h2；扣 4：九节八个无标题（h2＝1，源码计数）＋ 折叠头与表题同句复述 ＋ 样式留带票号类名（`t571`×8） |
| 2 | 信息层级与版式细调 | 25 | **19** | 四卡一排共线、图卡通栏、合计成组；扣 6：图表刻度 1440 渲染 ≈19.5px（probe2 框高 26px）比正文与图例大一档半 ＋ 结论条复述三卡 ＋ 图内标注与虚线只隔 ≈2.7px ＋ 合计第三行右列空 ＋ 间距出 4 倍数 |
| 3 | 分隔符与文字纪律 | 20 | **16** | 可见分隔符 0、无英文标识符上屏；扣 4：`1800`≥4 处／`8454` 3 处／`+1208`、`1.1` 各 2 处复述 ＋ 卡细字全角「＋」拼两项消耗 ＋ 图轴与表两种数字写法 |
| 4 | 跨宽自适应 | 15 | **12** | 三档溢出 0、四卡 4 列→2 列、图随容器缩放、表折叠；扣 3：同一张图两档渲染字号倒挂（≈19.5px／≈11px，probe2 两端实测，档位没把渲染字号钉住） |
| 5 | 手机端与 HELP 同档 | 20 | **16** | 可点件 14 处高全 ≥44px、可见正文 12px、无横滚；扣 4：图内文字渲染 ≈11px 跌破 12px 下限 ＋ 断点与 HELP（600／900）不同值 |

**加权总分：16/20×100×0.20 ＋ 19/25×100×0.25 ＋ 16/20×100×0.20 ＋ 12/15×100×0.15 ＋ 16/20×100×0.20 ＝ 80×0.20 ＋ 76×0.25 ＋ 80×0.20 ＋ 80×0.15 ＋ 80×0.20 ＝ 16.0 ＋ 19.0 ＋ 16.0 ＋ 12.0 ＋ 16.0 ＝ 79.0**

**页 21 加权总分 79（未过 90；与首轮同分，旧项照尺重打：字号倒挂与贴线均本轮复测成立）。**

## 九、页 22 · 看热量缺口

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/22-看热量缺口.html` |
| 字节 | 90,013 |
| SHA256 | `6B57286E…3166`（**与页 21 逐字节相同**，本轮亲测两串相等；R-59 已知架构，如实记录、不扣分、不编数） |
| 本席实拍 | `…22---fd1f1453-shot-1440x900-fullpage.png`（1440×1646）、`…-390x844-fullpage.png`（390×1643，与页 21 同高） |

### 1–6 六问

与页 21 逐项相同。本席探针对页 22 独立实测三档：`overflowX=0`、可点件 14 个高全 ≥44px、可见正文最小 12px（768／390）而 1440 档图内字 10.8px CSS（`fontMin=10.8@text`，即字号倒挂的图内文本）、可见分隔符 0、`leaks:{}`、h2＝1（每日摄入与消耗）、表 5 列 `align[left,right,right,right,left]`——**与页 21 三档读数逐项一致**，与字节相同互证。逐字节相同是比像素对比更强的同一性证据，本轮不再另跑像素差。

产品级现场确认：大标题是「热量缺口 2026-09-10 至 2026-09-16」，页内无一处文字区分「查」与「看」——两个唤醒词下是同一张纸（字节相同逻辑蕴含，仅作事实登记，不计入五维）。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据 |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **16** | 同页 21（逐字节相同，三档探针读数逐项一致） |
| 2 | 信息层级与版式细调 | 25 | **19** | 同页 21 |
| 3 | 分隔符与文字纪律 | 20 | **16** | 同页 21 |
| 4 | 跨宽自适应 | 15 | **12** | 同页 21 |
| 5 | 手机端与 HELP 同档 | 20 | **16** | 同页 21 |

**加权总分：16.0 ＋ 19.0 ＋ 16.0 ＋ 12.0 ＋ 16.0 ＝ 79.0**

**页 22 加权总分 79（未过 90）。**

## 十、页 23 · 看体重预测

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/23-看体重预测.html` |
| 字节 | 71,824（与首轮一致） |
| SHA256 | `C24FF66B…F282`（与首轮同一串；**与页 01 逐字节相同**，本轮亲测与 `01-预测体重(1周后).html` 哈希相等；R-59 架构延续，如实记录、不扣分） |
| 本席实拍 | `…23---b8f69afc-shot-1440x900-fullpage.png`（1440×900一屏内）、`…-390x844-fullpage.png`（390×1070） |

### 1 第一眼看到什么、层级清不清

第一眼是结论条 `<p class="ilife-block-conclusion">按最近的趋势，7 天后体重约 74.8 kg。</p>`（源码逐字，视觉确认为视觉焦点），第二眼是大标题「体重预测（7 天）」。三节都有真标题（h2＝3：参数／概览／数据与日志，源码逐个确认，视觉双档确认），比 18／19／20 修之前的形态齐整。
**词序不一致（真）**：`<p class="ilife-block-page-shell-eyebrow">预测体重</p>` vs `<h1>体重预测（7 天）</h1>`（两处源码逐字），同一件事两个词序并排同屏。

### 2 挤／空／错位／列宽失衡／截断／横滚

- **不挤不错位不横滚**：三档 `overflowX=0`，越界 0（本席探针）。四卡一排 `261×4＋12×3＝1080`（probe2），整页一屏。
- **390 档第四张卡贴边（真，轻，视觉本轮确认）**：视觉手机档确认「右下角这张卡右缘 74.8 紧贴卡片圆角内边距，几乎顶到边缘，比左上卡挤」；机制源码可证——第四卡大数值槽放的是区间字符串 `74.7 至 74.8`（源码逐字，同页前三卡该槽都是单个数字），22px 大字＋`gap:6px`＋单位把卡内宽撑满（首轮机器读数：数值行 143px vs 内容宽 141px；本轮字节逐位相同，布局确定）。1440 档同卡宽松（内容宽 229，probe2）。
- 视觉另称「复制数据按钮手机档截断」→ 探针 `over=0/overflowX=0`，**判为视觉误读**；「中文换行处断行不自然」系普通文本换行，非尺项（尺只保日期不断），不扣分。
- 无表无图，无列宽问题。中段已看，无压字。

### 3 有没有拿分号、间隔号、竖线把并列信息挤成一串

**没有。** 可见文本分隔符全 0（探针 `sep`）；口径三段独立 `<span>`（`体重＝当前值加日速率乘天数`／`预计区间按体重残差向外放宽，取 95% 置信带`／`体重记录不到 14 天不出预测`）。

### 4 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真，源码计数）**：① 结论条「7 天后 74.8 kg」与「预测」卡同数据；② `74.8` 全页 6 次（含载荷，可见三处：结论条／预测卡／`74.7 至 74.8`）；③ `7 天` 4 处（大标题／胶囊 `7 天`／预测卡细字／结论条）；④ `95% 置信带` 两处（区间卡细字＋页脚）；⑤ 门槛句两遍——`体重记录不到 14 天就只说数据不够，不编预测。` vs `体重记录不到 14 天不出预测`（两处源码逐字）；⑥ 页眉与标题词序互换。
- **英文内部标识符上屏：无**（探针 `leaks:{}`）。

### 5 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是**（四卡 `tabular-nums`）。
- **小数位一致：是**（75.1／74.8／74.7 一位小数，-0.34 两位小数自成速率档）。
- **缺值写 —：本页无缺值**，不涉及。
- **记一笔**：「预计区间」卡把区间字符串放进 22px 大数值槽（见第 2 维），正是贴边的成因。

### 6 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **本页无表**；四卡 390 档 2×2（probe2 `[16..189][201..374]`×2 行），无塌列。
- **触摸目标**：三档可点件 11 个，高全 ≥44px，低于 44px 高 **0 处**（探针）。
- **日期没被折断**：起止日期单行（探针 `wrapSuspects` 空）。
- **字号**：可见最小 12px（探针 `fontMin=12`）。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **17** | 三节真 h2＋语义节＋导航＋四卡网格；扣 3：`#sec-data` 内再嵌无标题 `<section class="ilife-block-copy-block">`（源码可证）＋ 1 处 `.tpd-track-table--cols3` 规则而本页表计数 0（源码计数） |
| 2 | 信息层级与版式细调 | 25 | **20** | 层级齐、四卡等宽共线、一屏不拉长；扣 5：结论条复述预测卡 ＋ `74.8` 三处／`7 天` 四处／门槛句两遍 ＋ 390 档第四卡值贴右边（视觉本轮确认，机制源码可证）＋ 间距出 4 倍数 |
| 3 | 分隔符与文字纪律 | 20 | **16** | 可见分隔符 0、口径三段独立 `<span>`、无英文标识符上屏；扣 4：页眉与标题词序互换 ＋ 四组复述（74.8／7 天／95% 置信带／门槛句） |
| 4 | 跨宽自适应 | 15 | **13** | 三档溢出 0、四卡 4 列→2 列不塌；扣 2：同一张卡 390 档贴边、1440 档宽松（长值无跨档退路） |
| 5 | 手机端与 HELP 同档 | 20 | **18** | 可点件 11 处高全 ≥44px、可见字号 12px、无横滚、日期不断；扣 2：第四卡值挤满卡内宽 ＋ 断点与 HELP（600／900）不同值 |

**加权总分：17/20×100×0.20 ＋ 20/25×100×0.25 ＋ 16/20×100×0.20 ＋ 13/15×100×0.15 ＋ 18/20×100×0.20 ＝ 85×0.20 ＋ 80×0.25 ＋ 80×0.20 ＋ 86.67×0.15 ＋ 90×0.20 ＝ 17.0 ＋ 20.0 ＋ 16.0 ＋ 13.0 ＋ 18.0 ＝ 84.0**

**页 23 加权总分 84（未过 90；与首轮同分，旧项照尺重打：贴边视觉本轮确认、机制源码复核）。**

### 另记（跨页事实，不计入本页五维）

`23-看体重预测.html` 与 `01-预测体重(1周后).html` 哈希相等（本轮亲测）。本页大标题「体重预测（7 天）」，唤醒词「看体重预测」在页上无字样区分。

## 十一、汇总

### 1 逐页加权总分（本席七页，第二轮）

| 页 | 标题 | 结构 20 | 信息 25 | 分隔 20 | 跨宽 15 | 手机 20 | 加权总分 | 过 90 | 首轮→本轮 |
|---|---|---|---|---|---|---|---|---|---|
| 17 | 摄入预测(自定义) | 17 | 19 | 15 | 12 | 18 | **81.0** | 否 | 80→81（+1，行标签 11.5px 经级联复核推翻） |
| 18 | 摄入预测(营养目标达成预测) | 17 | 19 | 16 | 14 | 18 | **84.0** | 否 | 81→84（+3，h2 补齐） |
| 19 | 摄入预测(卡路里缺口预测) | 17 | 19 | 15 | 14 | 18 | **83.0** | 否 | 80→83（+3，h2 补齐） |
| 20 | 摄入预测(摄入稳定性预测) | 17 | 17 | 16 | 13 | 18 | **81.0** | 否 | 78→81（+3，h2 补齐） |
| 21 | 查热量缺口 | 16 | 19 | 16 | 12 | 16 | **79.0** | 否 | 79→79（持平，旧项重打分未变） |
| 22 | 看热量缺口 | 16 | 19 | 16 | 12 | 16 | **79.0** | 否 | 79→79（持平，逐字节同 21） |
| 23 | 看体重预测 | 17 | 20 | 16 | 13 | 18 | **84.0** | 否 | 84→84（持平，旧项重打分未变） |

- 七页**没有一页过 90**；最高 84（页 18、23），最低 79（页 21、22），均值 **81.6**（571/7，首轮 80.1）。
- 本轮修复确认：18／19／20 三页 h2＝0 → h2＝3（参数／概览／数据与日志，与导航逐字对齐，三档可见，视觉确认），结构各 +2、信息各 +1。
- R-59 延续：21≡22、23≡01 哈希两两相等（本轮亲测），如实记录、不扣分。

### 2 跨页共性缺陷（按出现页数排序）

| # | 缺陷 | 出现在 | 证据 |
|---|---|---|---|
| 1 | 结论条把卡片数值原样复述一遍 | 17–23 全部七页 | 各页 `<p class="ilife-block-conclusion">` 与卡面值逐字同值（源码） |
| 2 | 页眉与胶囊重复（趋势分析／7 天） | 17、18、19、20、23 | eyebrow 与 chip 同词（源码） |
| 3 | 同一条门槛句参数说明与页脚各说一遍 | 17–23 全部七页 | 如 23 两门槛句源码逐字 |
| 4 | 本页用不到的表格规则 | 18、19、20（各 12 处）、23（1 处） | 源码计数；这些页表计数 0 |
| 5 | 间距出 4 倍数 | 七页 | 胶囊 `2px 8px`／`margin 6px`、数值行 `gap:6px`、`padding:14px/10px`（源码计数） |
| 6 | 判定卡不成形（无数值，只有句子＋徽章） | 18、20 | 卡源码结构 |
| 7 | 判定依据住页脚、离判定块远 | 18、19、20 | 口径行位置 |
| 8 | 断点 820／640 vs HELP 600／900 | 七页 | HELP 源文件本轮亲测 |

### 3 本轮驳掉的视觉读图（不作扣分依据）

| 视觉说法 | 复核 | 结论 |
|---|---|---|
| 18／19／20／21 口径行「有竖线｜分隔」 | 可见文本竖线计数 0、段间隙恰 12px＝flex gap | 视觉误读 |
| 19「内容偏左、右侧 30% 空白、卡片不对齐」 | probe2：卡 180..1260、壳 160..1280，对称居中 | 视觉误读 |
| 17「表格占 7 成、日期列最窄」 | 探针表宽 334/1080≈3 成、日期列最宽 | 视觉误读（留白与首行单值两点采信） |
| 21 图注「压在虚线上」 | 源码 y174 vs y178，隔 2.7px | 贴线非压线，轻记 |
| 23「复制按钮手机档截断」 | over=0／overflowX=0 | 视觉误读 |
| 17／21 h2 个数／标题 OCR（概况/热量缺口为 h2） | 源码 h2 计数 4／1 | 以源码为准 |

### 4 未细看与口径声明

- **中段未细看**：21／22 折线图逐点坐标（只量缩放比、字号、贴线与图例）；17 表中间 8 行按同形处理（首末行已核）。
- 列宽 168／66／100 沿用首轮机器读数（本轮字节逐位相同、表总宽 334 复测一致，布局确定，已明示）。
- 每条扣分结论均回源码核对；「源码未证实」的视觉怀疑一律未扣分（见上表）。
- 三档取 1440×900／768×1024／390×844；截图 `vision_html_screenshot` 双档 fullPage；探针读数落 `.scratch/t520-R2-V3/probe.json`＋`probe2.json`（本席自建脚本 `probe.mjs`／`probe2.mjs`，产物只读）。
- 本文件为唯一写件；产物与 `packages/` 一行未碰；未提交未推送。
