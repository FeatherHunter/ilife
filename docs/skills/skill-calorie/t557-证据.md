# t557 证据 · 计划编辑器契约门三处加固（接管席复跑）

票面：`gh issue view 557`（X1 哨兵行指错／C3.4 只验「齐」不验「对」／C3.5 口径不一致）。
前席中断，留无主改动两件未提交。本席按接管规则处理：无主 diff 不采信，先量现场再判定。

## 一 · 现场量取（不采信，只记录）

- 无主 diff（`git diff`）：`t554-plan-editor-gate.mjs` 约 70 增 22 删，
  `t554-plan-editor-probe.js` 约 24 增。已逐 hunk 通读，见 §二。
- 语法：`node --check` 两件均为 exit 0。
- 哈希（接管时点）：
  - `t554-plan-editor-gate.mjs` sha256
    `4D24E0992FF7EE39E8997EB859C7B1487262E27F16272874D994D10116099DEA`
  - `t554-plan-editor-probe.js` sha256
    `6FACB4DE06DB49EF00DC2B4F60AD5A187D559F2670EDBB2F8407C29D75591CC6`
- 交接线索 `.scratch/t557/`：`final-gate.log`（19/19）、`gate19.log`（19/19）、
  `mut-driver.mjs`（四变异驱动）、`mutation.log`（FAIL）、`mutation2.log`（OK）、
  `check-anchor.cjs`／`check-marker.cjs`／`test-dis.cjs`（变异锚点探测草稿）。
  前席读数一律作废，本席持锁重跑见 §三。

## 二 · 逐条判定（对照票面三条）

### X1 哨兵改当周行 —— 完成

- 门：`X1` 改读探针 `beforeSentinel／afterSentinel`（当周周一凌晨那行），
  第 1 周那行只留 `beforeOrigin／afterOrigin` 对照读数，不计分。
- 探针：`cardioEdit` 新增 `sentinelWeek／beforeSentinel／afterSentinel`，
  取 `txt(q('.pe-week-t'))` 当周（锁周＝第 3 周）。
- 判定理由：回落 `(0,0,0)` 取当前周，自证读数（§三 R-X1）显示变异后
  当周行 30 分钟→77 分钟被写脏、第 1 周行逐字不动——原 `originStill`
  恒真无分辨力，新 `sentinelStill` 为假即红。完成。

### C3.4 补 disabled===false ＋ 对位 ＋ 备注④升 X3（18→19） —— 完成

- 门：`C3.4` 在原「无纯文本＋坐标齐」上追加 `lockedDisabled`
 （`disabled !== false` 即红）与 `lockedMisplaced`
 （`d＝posD 且 s＝posS 且 m＝posM`，任一不等即红）；新增计分条 `X3`
 （锁周改史密斯机深蹲组数 5→9，只有一行动、母版周同名行不动）。
- 探针：`day3Inputs` 每格新增 `disabled`、`posD／posS／posM`
 （日页签 d／段序号／动作序号）。
- 判定理由：R-DIS（全 disabled）18/19 仅 C3.4 红——旧门 18/18 全绿盲区已堵；
  R-MIS（data-d 恒 "1"）15/19、C3.4 红——形似错位已抓。完成。

### C3.5 对齐 structLocked 口径 —— 完成（选边＝纳入时段面）

- 门：新增 `slotLocked`（`slotDis` 全禁用，如 8/8），
  `structLocked` 并进该条件；读数留选边说明。
- 判定理由：R-SLOT（摘时段 disabled）18/19 仅 C3.5 红——旧门全绿盲区已堵；
  与件内「结构禁＝增删训练段／增删动作／换时段」注释口径一致。完成。

### 决议：补齐（保留无主 diff，不还原不重做）

三处实现与票面修法逐字对应，且本席持锁重跑全部验收通过（§三）。
还原会丢已验证的加固，重做无增益。故保留两件改动，补证据＋变更集后提交。

## 三 · 本席验收（持锁重跑，前席读数作废）

- 基线：`node tooling/run-locked.mjs --ticket 557 --run-id take557-gate2 -- node
  docs/skills/skill-calorie/t554-plan-editor-gate.mjs --phase=gate`
 （`PE_OUT=.scratch/t557/gate-takeover-base` 隔离）→ `RESULT: 19/19` exit 0。
- 变异驱动：`node tooling/run-locked.mjs --ticket 557 --run-id take557-mut --
  node .scratch/t557/mut-driver.mjs` → `MUTATION-SUMMARY: OK` exit 0。
  - `R-X1` 摘 X1 那格坐标：`RED: C3.4=RED X1=RED`，门 16/19，九格改红无；
    `RESTORE: IDENTICAL`（源码 3/3、产物 3/3，复跑 19/19 零漂移）。
    读数：当周哨兵行 30 分钟→77 分钟被写脏，第 1 周对照行不动。
  - `R-DIS` 锁周参数格重回 disabled：`RED: C3.4=RED`，门 18/19，九格改红无；
    `RESTORE: IDENTICAL`。
  - `R-MIS` data-d 恒 "1"：`RED: C3.4=RED`（连带 X1／X2／X3 红，门 15/19，
    系坐标全错后写路全乱；九格 §1①②④ 全绿），九格改红无；
    `RESTORE: IDENTICAL`。
  - `R-SLOT` 摘时段 disabled：`RED: C3.5=RED`，门 18/19，九格改红无；
    `RESTORE: IDENTICAL`。
- 九格（§1① C1.1–C1.4／§1② C2.1–C2.5）：基线全绿，四变异下无一改红。
- 编译口径：驱动内 `PREFLIGHT: 编译 exit=0；dist 三件与开工基线全等`；
  入口均为 `node node_modules/typescript/bin/tsc -b packages/skill-calorie`。
- 变异后 src 零残留：`git status` 无 `src/render/planEditor*.ts` 改动。

## 四 · 纪律自查

- 用词：门／探针增量无禁用黑话（茎／写键／读键／闭不了环／打通／执行桶／预检确认页误写「向导」均无命中）。
- 命令：未新增／未搬移命令，不碰 `src/<能力>/commands.ts` 与派生位。
- 页面交付：无新页面，无真出口改动（`packages/skill-calorie/src/**` 零触及）。
- 公共层提取：门／探针住 `docs/skills/skill-calorie/`（归属件目录），
  无可提取到公共层的复用逻辑。
- 范围收敛：写集＝门／探针两件＋`t557-*` 证据＋`.scratch/t557/` 草稿＋
  `.changeset/557-*`；别席件一律未碰。

## 五 · 写集与运行号

- 改动：`docs/skills/skill-calorie/t554-plan-editor-gate.mjs`、
  `docs/skills/skill-calorie/t554-plan-editor-probe.js`（前席无主，本席验证后补齐）。
- 新建：`docs/skills/skill-calorie/t557-证据.md`（本件）、
  `.changeset/557-plan-editor-gate.md`、`.scratch/t557/`（驱动＋本席重跑日志）。
- 本席运行号：`take557-gate2`（基线 19/19）、`take557-mut`（四变异 OK）。
  前席 `t557-final／t557-mut／t557-mut2／t557-gate19` 读数作废，仅作线索。
