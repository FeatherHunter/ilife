# t530 · 场景09 视觉终审（收口报告，2026-09-16 重跑）

票：`#530 卡路里场景09 · 视觉终审`（父图 `#159`）。判据来源：`docs/agents/视觉验收墙.md`（§0 五条验收路线、§3 收口顺序）
与票面四件事。**本报告只记当刻实跑读数**；拿不准的一律写「未做」，不写「应该没问题」。

## 一、判定

- **机器面（链条第 ①／② 条）**：三档横向溢出 **17 件 × 3 档全零**；分隔符节点级探针 **13/17**（4 项逐条定性，见 §三）；
  墙与册子自检 **绿**（含两条反例必红）；HTTP 可达自检 **19/19 全 200、字节零不一致**。
- **交付面（第 ③④ 条的人看物）**：16 件样张**从当刻源码重出**（不只读页 11 件，含本轮修过的 09-05／09-08／09-14），
  墙与总索引按当刻字节重出，预览台在 `http://127.0.0.1:43210/wall/`。
- **仍欠**：① 逐页 vision 打分（本会话无 `vision_*` 工具）；② `gen-photo-baseline.mjs` 两次 `BASELINE-OK 10/10`
  （当刻 `BASELINE-DIFF 10 pages`，且两页有渲染当刻时钟未剥净，见 §四）；③ 导航页 82 处债的去向（P4 待裁，见 §三）。
- **最终签字归负责人**（`视觉验收墙.md` §0 第 ④⑤ 条）：本报告不代签，签字面在 [#287](https://github.com/FeatherHunter/ilife/issues/287)。

## 二、当刻读数（全部本机实跑，命令逐条可复跑）

### 1 三档响应式（`measure-responsive.mjs --dir D:\ilife-验收\scene09`）

```text
OVERFLOW-ZERO pages=17 cells=51 failed=0 scopeOutFailed=0 label=run
```

17 件（16 件样张 ＋ 导航页）× 390／768／1440 三档，`docScrollWidth − innerWidth` 全 0；图片全部放得进容器
（`img=` 逐页给出张数与放得下张数，如 09-01 4/4、09-08 2/2、09-09 3/3）。

### 2 分隔符与内部标识符探针（`audit-separators.mjs --dir … --json`）

```text
RESULT: 13/17      nodeHitsTotal=85  totals={"R1":20,"R2":28,"R3":25,"R4":0,"R5":12,"R6":2,"R7":39}
```

四件非零，逐条定性（都不是页面拿符号偷懒，见 §三）。

### 3 墙与册子自检（仓内件 `docs/skills/skill-calorie/t530-墙生成器.mjs`）

```text
--stage D:\ilife-验收\scene09 .scratch/t351-preview\wall
  → 墙 场景09-手机墙.html、场景09-桌面墙.html、场景09-总索引.html：16 格；链接 80 条；缺失 0 -> 可发   exit=0
--check 同目录 → exit=0
反例A（册子加一行 `09-99-不存在的件.html`）→ exit=1，点名「册子点名而盘上没有：09-99-不存在的件.html」
反例B（手机墙一条链接改成 `09-01-不存在的件.html`）→ exit=1，点名「墙页引用而盘上没有：场景09-手机墙.html -> 09-01-…」
两条反例后重跑 `--stage` 复位 → exit=0
```

### 4 HTTP 可达（`check-http.ps1`，预览台 `http://127.0.0.1:43210/`）

```text
合计 19 条　200 的 19 条　非 200 0 条　字节与本地不一致 0 条
RESULT: HTTP-OK total=19 nonOk=0 mismatch=0      exit=0
```

三条墙页 ＋ 16 件产物同目录（随仓内生成器「墙与产物同目录」布局）。

### 5 当刻样张字节表（交付面 `D:\ilife-验收\scene09\`）

| 样张 | 字节 | 样张 | 字节 |
|---|---|---|---|
| 09-01 看身材照 | 1,036,003 | 09-09 记身材照（回执） | 1,022,744 |
| 09-02 查身材照（超 1MiB） | 90,688 | 09-10 改照片标签 | 85,801 |
| 09-03 查身材照（已内嵌） | 230,920 | 09-11 加照片标签 | 85,878 |
| 09-04 对比两张照片 | 219,825 | 09-12 删照片标签 | 85,863 |
| 09-05 生成身材照GIF | 99,987 | 09-13 删身材照（回执） | 391,078 |
| 09-06 记身材照（预检） | 71,938 | 09-14 删身材照（候选快照） | 361,420 |
| 09-07 GIF 前置规划页 | 84,926 | 09-15 HELP（q 现找） | 94,461 |
| 09-08 删身材照（候选） | 359,978 | 09-16 HELP（全量 10 键） | 114,590 |

（导航页 22,601 B。）**改动过的三件**：09-05 从 83,718 → 99,987（补上公共层媒体件规则 ＋ 舞台挂类名）、
09-08 343,217 → 359,978、09-14 344,659 → 361,420（同因）。三件都验过：`文件大小 0.4 KB`（原「0 KB」）、
备注列不再上屏原始标记、候选卡不再逐张抄文件名。

## 三、四件非零的定性（都不是新债）

| 件 | 命中 | 定性 |
|---|---|---|
| `00-场景09验收导航.html` | 82（R1 20／R2 28／R3 24／R5 12／R6 2／R7 37） | **已知件**：它是 09-15 早安的手工入口页，早于验收墙（票面 J23 就登记过这 82 处）。P4 二选一未裁：删掉由总索引替代，或保留但清债。**本次不动它**（它是负责人入口，改／删要负责人一句话）。 |
| `09-07-生成身材照GIF（前置规划页）.html` | 1（R7，L1938） | **已声明例外**：指令预览块里可见的 `calorie-cmd-read calorie.photo.gif …`（票面 J8，第 21 轮裁定；性质是「给 AI 的指令」复制块，不是拿符号偷懒）。 |
| `09-14-删身材照（候选快照·id19）.html` | 1（R7，L2286） | 同上（`calorie-cmd-read calorie.photo.remove …`），同一条例外。 |
| `09-05-生成身材照GIF（结果页·内嵌GIF）.html` | 1（R3，L2282） | **用户自己写的备注**：`体重待补/对比6/9(93.2kg)→当前更瘦` 落在明细表「备注」单元格里。分段形状在 div 槽位（画廊／对比／详情）已落；表格单元格转义后落不了标记，而改写用户原文更不对。**建议按「用户数据不判债」登记为已声明例外**（需负责人点一句）。 |

## 四、仍欠与下一手

1. **基线门**：`gen-photo-baseline.mjs --check` 当刻 `BASELINE-DIFF 10 pages`；且它自己报
   `UNSTABLE calorie.photo.remove / calorie.photo.tag：渲染当刻时钟未被剥净：2026-09-16 21:35:36`。
   即：这两页新增的时钟位不在 `normalize()` 的四条剥除规则里，**先把剥除规则补齐，再 `--write` 重落十页 sha**，
   然后连续两次 `--check` 必须 `BASELINE-OK 10/10`。这是三步一体的活（同 #493 那一轮的做法）。
2. **逐页 vision 打分**：本会话无 `vision_*` 工具，出不了分。按 `视觉验收墙.md`，最终验收的判者是**人**（第 ④⑤ 条），
   故该项归负责人签字面（#287）；如需机器分，须另派带 vision 的席位。
3. **导航页 82 处债**（P4）与 **09-05 备注例外**：各需负责人一句话。
4. 写类样张（09-09～09-13）沿用 09-15 出的一批：本轮源码改动只碰 `gifDoc`／`pickerDoc`／…，不影响这五页；
   **若要「16 件全部出自同一次重出」**，需连写类一起重出（要建 stage 副本且按 09-09→09-13 的故事线跑）。
