# #295 返修证据（A1–A8 逐条 ＋ 变异自证 ＋ 门禁对账）

票：[#295](https://github.com/FeatherHunter/ilife/issues/295)（地图 [#293](https://github.com/FeatherHunter/ilife/issues/293) 第 2 张）
返修派单：`.scratch/orchestrator/t295-rework-brief.md`（缺陷清单 A1–A8 ＋ D7 裁决）
上一版证据：[`t295-命令汇总位改生成物.md`](t295-命令汇总位改生成物.md)（四件生成物那一版，本文件只记**返修增量**）
两席审查（只读）：[`t295-审查-红队.md`](t295-审查-红队.md)（FAIL 83）／[`t295-审查-蓝队.md`](t295-审查-蓝队.md)（FAIL 75）

---

## ① 提交与改动清单

| commit | 一句话 |
| --- | --- |
| `8f5b1a6` | A1 五处计数改派生对账 ＋ A3 示例搬进声明（生成 EXAMPLE 表）＋ A4 gen 新鲜度守卫 ＋ A5 legacy 棘轮 ＋ A6 `help:build` 入口 ＋ A8 CI 各门 `if: always()` |

改动面（14 件，全部在第一步清单内，逐行对账见 ⑥）：

- **声明与服务**：`src/shared/commandSpec.ts`（`example` 字段）、`src/weight/commands.ts`（9 条带示例）、
  `src/cli/legacyCommands.ts`（92 条带示例 ＋ 文件头说明）。
- **生成器**：`scripts/gen-cli.mjs`（`EXAMPLE` 标记块 ＋ 声明新鲜度守卫 ＋ legacy 的 `example` 校验）。
- **产物**：`scripts/build-help.mjs`（101 条手写 `case` 退役 → 查生成的 `EXAMPLES` 表；文件头写清重生成次序）。
  `SKILL.md` **逐字节未变**（sha256 `DF63151429D5E55C2273593F548E41D6F68271D3535BF4106ADCC643CC5FA3F3`，见 ②A3）。
- **测试**：`test/declared.mjs`（＋`DECLARED_CAPABILITY_KEYS`）、`cmd-registry-294`／`cmd-write-40-persist`／
  `m5-receipt-97`／`output-naming-119`（四处改成派生的对账）、新增 `test/legacy-ratchet-295.test.mjs`、
  新增 `test/ci-gates-295.test.mjs`。
- **配置**：根 `package.json`（＋`help:build`）、`.github/workflows/ci.yml`（各门 `if: always()` ＋ 已知红挪末位）。

---

## ② 逐条验收（A1–A8）

### A1 · 五处钉死命令数 → 从权威声明算出来的对账（判据＝行为）

| 位置 | 改前 | 改后 |
| --- | --- | --- |
| `test/cmd-registry-294.test.mjs:193` | `WEIGHT_COMMANDS.length === 9` | `=== DECLARED_CAPABILITY_KEYS.length`（`cli/registry.ts` 的键集） |
| `test/cmd-write-40-persist.test.mjs:750` | `WRITE_KEYS.length === 35` | `deepEqual([...WRITE_KEYS].sort(), DECLARED_WRITE_KEYS)` |
| 同文件 `:753`／`:755` | `size === 35`／`total >= 35` | `=== WRITE_KEYS.length`（派生） |
| `test/m5-receipt-97.test.mjs:96/97` | `WRITE_KEYS.length === 35`／`SCENARIOS.length === 35` | 前者 `deepEqual(... DECLARED_WRITE_KEYS)`；后者 `=== WRITE_KEYS.length` |
| `test/output-naming-119.test.mjs:67` | `receiptCount === 35` | `=== DECLARED_WRITE_KEYS.length`（编排者补点，探针 P1 抓到的那处） |

**判据证据（仓外临时根，见 ⑤M1）**：给 `src/weight/commands.ts` 加一条假命令 →
靶向六文件里 **`test/` 下改动面 = 0 件**；`pnpm gen` 后 `gen:check` exit 0；SKILL.md 自动长到 102 行。
断言没被删成「没有断言」：五处**都留下了数量断言**，只是分子／分母两边都从声明算
（`test/declared.mjs` 是唯一对账源：`LEGACY_COMMANDS` ＋ `REGISTRY`）。

### A2 · 账目（第五件生成物）

见票面与 ⑧；D7 裁决照抄执行：第五件标「**未做 → 归 #297**」，地图记 **4/5**，
并写明「加／删一条命令仍要手改 `src/triggers/routing.ts`（＋ `test/calorie-routing-81.test.mjs` 的 parity 口径）」。

### A3 · `exampleFor` 的 101 条手写 `case` 搬进声明（判据＝行为 ＋ 逐字节）

- 逐条搬进声明的 `example` 字段：`CommandSpec.example`（能力目录，9 条）＋ `LegacyCommandDecl.example`
  （未搬迁清单，92 条）＝ **101 条**，由脚本 `.scratch/t295-rework/extract-examples.mjs` **机械抽取**、
  字符串字面量原样搬运（`case N 条=101 注入=101 未注入=0`），未手敲。
- 生成器落成 `build-help.mjs` 的 `EXAMPLE` 标记块（与既有 `REPR` 同一机制，第 5 件生成块）；
  `exampleFor(key)` 退化成查表 ＋ 查不到即抛（结构断言保留，声明缺 `example` 时 `tsc` 与生成器各拦一道）。
- **加一条命令不再需要碰 `build-help.mjs`**：M1 里 `build-help.mjs` 是**被 `pnpm gen` 自动改**的，
  人只改了 `commands.ts` 一处。
- **SKILL.md 产物逐字节相同**：重生成前后 sha256 `DF631514…FA3F3` 一致（`help:build` exit 0）；
  `skill-t11` 的「互联区新鲜」与 `check-examples` 的「AUTO 块 == renderSkillMd 输出」两条断言同时绿。

### A4 · `gen:check` 新鲜度守卫（消灭假绿）

`gen-cli.mjs` 读声明前先查「生成器的两个输入（各能力 `commands.ts`、`legacyCommands.ts`）比编译后的
`dist/` 新没有」；陈旧即 `GEN-STALE FAIL` 并退出 1，`gen`／`gen:check` 两条路都拦。
**判据证据（M2）**：只改声明不 build 直接 `pnpm gen:check` → **exit 1 ＋ 逐字 `GEN-STALE FAIL：dist/ 不是这些声明的产物…`**
（改前这一步是 exit 0 的假绿）；先 `pnpm build` 再 `gen:check` → 红在「生成物 ≠ 生成器输出」；`pnpm gen` 后复绿。
不查生成物自己（`src/cli/keys.ts` 刚被 `pnpm gen` 写完，本来就比 `dist/` 新），避免误报。

### A5 · `legacyCommands.ts` 机器棘轮（新件 `test/legacy-ratchet-295.test.mjs`）

形照 `cmd-registry-294.test.mjs` 的棘轮：① 冻结键清单 **92 条**，清单外的新键**进不来**；
② 条数上限 `<= 92`、文件行数上限 `<= 119`（**只许变短**）；③ 一个键恰住一处（未搬迁清单与能力目录不得同一个键，
两边之和 == `DECLARED_KEYS`）。**判据证据（M3）**：往未搬迁清单加一条新键 → 棘轮 exit 1，
红串逐字 `未搬迁清单出现了冻结清单外的新键（新命令要住它自己的能力目录 src/<能力>/commands.ts，这里只进不出）：calorie.view.zzz-new`；
还原后复绿。**没有放宽任何既有棘轮**（`cmd-registry-294` 的 case 清单／行数上限一字未动，探针 P3 亦证「棘轮未被放宽=true」）。

### A6 · SKILL.md 重生成入口

根 `package.json` 加 `"help:build": "node packages/skill-calorie/scripts/build-help.mjs"`；
`build-help.mjs` 头部写清与 `gen`／`gen:check` 的关系与次序：
`pnpm build → pnpm gen → pnpm build → pnpm help:build →（需要时）node packages/base-combos/scripts/gen-present.mjs`，
并说明「`gen:check` 管生成物 == 生成器输出，AUTO 块新鲜度由 `help:examples:check` 的那条断言管」。

### A7 · 证据重写对账命令（第三方照抄可复现）

见 ⑦：写死 `--since`／`--until` 双边界窗口 ＋ `--allow-nonzero`（理由见 GATE-RELAX 行）。
本轮 `GATE-RUN` 声明块见 ④，导出对账源入库：`docs/skills/skill-calorie/t295-返修-gate-runs.log`。

### A8 · CI 一步红不再吞掉其余门（本票修）

`.github/workflows/ci.yml` 的 `build-test`：七道门（`gen:check`／`doctor`／`test`／`boundaries`／
`snapshot:html:check`／`gate:selftest:html`／`help:examples:check`）各带 `if: always()`，
已知恒红的 `help:examples:check` 挪到 job 末尾；**无 `continue-on-error`，job 总体照旧 failure**。
判据由新件 `test/ci-gates-295.test.mjs` 机器盯着（每道门必须 `if: always()`；全作业不许出现 `continue-on-error`）。

**真 CI 实测**（`gh run view 34745203987`，commit `8f5b1a6`，ubuntu-latest/node 24）：

```
Run pnpm install --frozen-lockfile  success      Run pnpm build            success
Run pnpm gen:check                  success      Run pnpm doctor           success
Run pnpm test                       failure      Run pnpm boundaries       success
Run pnpm snapshot:html:check        success      Run pnpm gate:selftest:html failure
取 master 基线                       success      Run pnpm changeset:status  success
Run pnpm help:examples:check        failure      job=build-test 总体=failure
```

**15 个步骤全部真跑，0 个 skipped**（改前 `help:examples:check` 一红，其后 5 步一律 skipped）。
红的三道是**票外既有红**，逐条查清见 ⑨——A8 只是让它们现形，没有把红变绿。

---

## ③ 门禁实测（全部持锁；本地 Windows）

| 门 | runId | exit |
| --- | --- | --- |
| `pnpm build`／`gen:check`／`test`／`doctor`／`boundaries`／`snapshot:html:check`／`help:examples:check`（同一持锁批次） | `4f0dcba6` | **1**（末位 `help:examples:check` 票外红） |
| 同上批次内逐条 | 同 runId | `pnpm test` **1486／1486**、`gen:check` PASS、`doctor` PASS、`boundaries` PASS、`snapshot` changed=0、`help:examples:check` 100/101 |
| `pnpm build; pnpm gen:check; pnpm test`（改 A1 第 5 处后） | `17154fb4` | 0 |
| `pnpm help:build` | `fd67e012` | 0 |
| `pnpm gen` | `471b4c89` | 0 |
| 靶向（棘轮／对账／CI 门 六文件） | `d33d64c4` | 0 |
| `pnpm gate:selftest:html`（CI 同口径，**不套外层锁**，日志在 `.scratch/locks-selftest/`） | `44622697`／`ed0e6ae1` | **1（票外既有红，见 ⑨）** |

`pnpm test` 基线 1481 → 本轮 **1486**（新增 5 条：棘轮 3 ＋ CI 门 2），全绿。

---

## ④ GATE-RUN 声明块（窗口内逐条与 `.scratch/locks/gate-runs.log` 一对一）

GATE-RELAX flag=--allow-nonzero reason=本轮的 exit≠0 条目全是**预期红**：`help:examples:check`（票外既有，转票 #308）、
第一条 pwsh 引号写坏的批次（自证诚实记账）、以及改写断言后第一次靶向（同一处 import 缺陷，已修）。窗口必须容纳它们，否则反向对账会把它们记成「无人声明的运行」。

```
<!-- exit=1 第一条批次：pwsh 引号写坏，三行条件全废（自证，不藏） -->
GATE-RUN ticket=295 runId=f9236b35-3b14-40a4-beca-39afc74a4506 cmd=pwsh -NoProfile -Command
<!-- exit=0 声明带 example 后的 build -->
GATE-RUN ticket=295 runId=0e20526c-83c8-457f-8db3-77ed00a2aeb5 cmd=pnpm build
<!-- exit=0 生成四件＋EXAMPLE 块 -->
GATE-RUN ticket=295 runId=471b4c89-5b9d-4297-9154-e2744b703acf cmd=pnpm gen
<!-- exit=0 SKILL.md 重生成（产物 sha256 与改前逐字相同） -->
GATE-RUN ticket=295 runId=fd67e012-2e41-49e5-a827-8b71b37eaeff cmd=pnpm help:build
<!-- exit=1 靶向第一次：CI 门断言把注释里的 continue-on-error 也算成键（已修窄） -->
GATE-RUN ticket=295 runId=b325105d-a7e9-4c65-9358-c1ead9117d87 cmd=node --test test/ci-gates-295.test.mjs
<!-- exit=0 靶向复跑 -->
GATE-RUN ticket=295 runId=d33d64c4-c497-4935-ae0f-83b4921ff47d cmd=node --test test/ci-gates-295.test.mjs
<!-- exit=0 补第五处（output-naming-119）后的靶向 -->
GATE-RUN ticket=295 runId=09400bd6-c917-41cf-b523-509cf505ddce cmd=node --test packages/skill-calorie/test/output-naming-119.test.mjs
<!-- exit=0 全量 build／gen:check／test 1486/1486 -->
GATE-RUN ticket=295 runId=17154fb4-9f30-46b3-ba4c-1e0b3ea63206 cmd=pwsh -NoProfile -Command
<!-- exit=0 第一步 commit -->
GATE-RUN ticket=295 runId=52a1f14e-44b1-4414-8343-f921fcf68470 cmd=pwsh -NoProfile -Command
<!-- exit=0 推远端（真 CI 因这一次 push 触发） -->
GATE-RUN ticket=295 runId=beab2be1-d0bd-4181-8a71-fe2e9c7690f2 cmd=git push origin master
<!-- exit=1 七道门同一持锁批次；红的是末位 help:examples:check（票外既有） -->
GATE-RUN ticket=295 runId=4f0dcba6-b8a5-4062-a1d7-46f70905c4e8 cmd=pwsh -NoProfile -Command
```

（`cmd=` 取日志里命令的**前缀**——对账工具按「前缀 ＋ 空格」或测试文件集合判定命中，
写全虽可，但 `git add …` 那条 800 字会淹掉本文件；前缀是工具支持的写法，逐条仍与日志一对一。）

窗口：**`2026-09-13T07:19:00Z`（含）～ `2026-09-13T07:30:34Z`（含）**。
`pnpm gate:selftest:html` 两条（`44622697`／`ed0e6ae1`）写在**另一个日志** `.scratch/locks-selftest/gate-runs.log`
（该 script 自身即持锁包装器、用独立锁目录，CI 里也是直接调用，见 `.github/workflows/ci.yml` 的注释），故不在本窗口，
按 CI 同口径单独列出、不混进本对账。

---

## ⑤ 变异自证（全部在**仓外临时根** `D:\_t295rework\repo`＝`git clone --local` 的 `8f5b1a6`，协议 §2.1）

跑法：`node D:\_t295rework\probe.mjs`／`m1b.mjs`（日志 `D:\_t295rework\probe.log`／`m1b-tests.txt`）。跑完即清临时根。

**M1／M1b（A1＋A3 判据：加一条命令要改几个文件）**：往 `src/weight/commands.ts` 加一条假命令
（`calorie.view.zzz-probe`，只动这一处）→ `pnpm build` → `pnpm gen` → `pnpm build` → `pnpm help:build`：

- 自动跟着变的是 5 件：`src/weight/commands.ts`（人手）、`src/cli/keys.ts`、`base-combos/combos.yaml`、
  `scripts/build-help.mjs`（REPR ＋ EXAMPLE 两块）、`SKILL.md`；**`test/` 下 0 件**。
- `pnpm gen:check` exit 0（生成物 == 生成器输出）；SKILL.md 速查表 **102 行**、含探针键。
- 靶向六文件唯一红的是 `cmd-registry-294` 的
  `AssertionError: 代表唤醒词路由到别的键：看今日体重 → calorie.view.weight` ——
  这是 **routing 手改点**（D7／#296 的 DISCIPLINED-SHARED 项），**不是任何计数断言**；探针复用了已有唤醒词，属预期。
- 还原：`git status --porcelain` **空**（声明 ＋ 四件生成物 ＋ SKILL.md 全部回位），靶向复跑 exit 0。

**M2（A4 判据）**：只改声明不 build → `gen:check` **exit 1 ＋ `GEN-STALE FAIL`**（改前假绿 exit 0）；
build 后 → 红在「生成物 ≠ 生成器输出」；`pnpm gen` 后 → exit 0。

**M3（A5 判据）**：往未搬迁清单加一条新键 → 棘轮 **exit 1**，红串逐字见 ②A5；还原后复绿、sha256 一致。

**M4（A1 判据·删命令）**：删 `calorie.view.dedupe` 一条真命令 → `pnpm build` exit 0（这条键不在路由表里，
不撞类型门）→ 对账／棘轮 **exit 1**，红串逐字
`AssertionError: 组合键表 == 权威声明（未搬迁清单 ＋ 各能力），不再手写数字`（**红的是对账，不是哪个手写数字**）；
还原后 sha256 一致、工作区干净。

---

## ⑥ 偏离记账（必报五步第五步：第一步清单 vs 实际）

| 第一步清单 | 实际 | 偏差 |
| --- | --- | --- |
| `packages/skill-calorie/src/`（`shared/commandSpec.ts` 加 `example`；两份声明的 101 条示例） | 同（3 件） | 无 |
| `packages/skill-calorie/scripts/`（`gen-cli.mjs` 出 EXAMPLE 块 ＋ 新鲜度；`build-help.mjs` 手写 case 退役） | 同（2 件） | 无 |
| `packages/skill-calorie/test/`（四处计数改对账；新增 legacy 棘轮） | 同（5 件） | 无 |
| `test/`（新增 CI 门守卫） | `test/ci-gates-295.test.mjs` | 无 |
| 根 `package.json`（`help:build`） | 同 | 无 |
| `.github/workflows/ci.yml`（各门 `if: always()` ＋ 挪位） | 同 | 无 |

**多碰**：0 件。**少碰**：0 件。
`SKILL.md` 与 `base-combos/present.ts` 本轮**未变**（逐字节相同，未出现在 `git status` 里）。
未碰（派单明令）：`src/weight/**` 的页面类文件、`docs/skills/skill-calorie/t293-验收-*`、`t295-审查-*.md`、`src/triggers/routing.ts`。

---

## ⑦ 门禁对账（check-gate-audit，窗口写死、第三方照抄可复现）

```
node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t295-返修证据.md \
  --ticket 295 --since 2026-09-13T07:19:00Z --until 2026-09-13T07:30:34Z --allow-nonzero \
  --export docs/skills/skill-calorie/t295-返修-gate-runs.log
```

结果：**`matched=11/11 auditEntries=1371 scoped=11 undeclared=0`，`gate-audit: PASS`**
（第三方照抄上面这条命令即得同一结果：窗口写死、`--export` 把它导成受跟踪的对账源）。
导出对账源：`docs/skills/skill-calorie/t295-返修-gate-runs.log`（窗口内 11 条 RUN）。
本文件自身的那笔提交发生在窗口**之外**（`--until` 之后），故未列入声明。

AUDIT-RESULT: matched=11/11 scoped=11 undeclared=0 → gate-audit: PASS（runId 见 ④）

---

## ⑧ 风险与未做

1. **`routing.ts` 未做（D7 裁决＝暂缓）**：⑤M1 证明「加一条命令」至今仍要手改 `src/triggers/routing.ts`
   （否则 `calorie-routing-81` 的 D2③ 与 `cmd-registry-294` 的唤醒词路由断言红）。账已按裁决改口径：
   第五件生成物标「未做 → 归 #297」，地图记 4/5。**#296 的纪律文本必须写明这两处手改点**，不许写「共用位零接触」。
2. **`legacyCommands.ts` 仍是手写共用位**（92 条住 `src/cli/`）：本轮给了棘轮（只许变短 ＋ 新键禁入），
   但「两个 session 同搬两个场景 ⇒ 抢同一支笔」这条**结构**没解，属 #297。
3. **生成链次序**：`build → gen → build → help:build` 四步，少一步会静默停在旧键数（A4 的新鲜度守卫只拦第一条）。
   已写进 `build-help.mjs` 文件头；#296 照抄即可。

---

## ⑨ A8 让三道**票外既有红**现形（逐条查明，都不是 #295 引入）

1. **`pnpm help:examples:check`（100/101）**：`calorie.view.exercise-review`，票前既有，已转票 **#308**。
2. **`pnpm gate:selftest:html`**（本地两次 exit 1，`44622697`／`ed0e6ae1`；CI 里同样 exit 1）：
   失败断言 `memo key 片段数 actual: 11 / expected: 10`。**根因**：`packages/skill-memo-ilife/src/render/envelope.ts`
   的 `MEMO_KEY_SHAPES` 在 `1fca032`（2026-09-12，#220 备忘录 HELP 首发）长到 11 条，
   而 `tooling/test/skill-html-snapshot.test.mjs` 的 `EXPECT_KEYS.memo` 仍写 10。
   **证伪「这是 #295 引入的」**：在仓外干净检出 `1396d67`（本票开工基线）跑同一道门 → **同一断言、同一 11/10 红**；
   该门最后一次真跑是 2026-09-09（日志里 exit=0），此后一直因 CI 短路而 skipped。
3. **`pnpm test` 在 CI 里红（全 6 个 matrix 作业都红；本地 Windows 1486／1486 绿）**：
   两条独立成因，都不在 #295 的改动面里——① 各插件 `dist/client.js` 缺失：
   `packages/plugin-*/package.json` 的 `build` ＝ `build:host && build:client`，而根 `pnpm build`（`tsc -b`）**只做 host**，
   CI 从不跑各插件的 `build:client`（本地这份 `dist/client.js` 是 2026-09-12 的旧产物，所以本地绿）；
   ② `#237 ⑤ 文件名安全化`／`#87 ④b 同秒计数大小写不敏感` 两条测试带 Windows 语义假设（`\`／大小写），
   在 Linux／macOS 上红。**这两条要转票**：它们不是本票的改动面，但会让 CI 的 `pnpm test` 长期红。
