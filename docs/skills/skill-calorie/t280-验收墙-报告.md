# t280 · 场景 02 饮食 · 验收墙 报告

- 票：**#280**（地图 **#155**）· 本件交的是**判据②的交付入口**：把该场景的唤醒词全部真跑一遍，把产物整成一张可看、可点、可追溯的验收材料。**不含**票 #279 的结构化断言，也**不含** #280 的另一半（负责人在新空白 session 亲手跑）。
- 检出：`D:\ilife-wt\wf155-m`（新建 worktree）· **基线提交 `5dc096b206ac684d6034fa42436e8371bbc563c6`**。
- 本席**未改 `packages/` 下任何文件**：只取用当刻 master 编译出的产物。改动面见第四段。

## 一、判定

**交付完成**（三样齐：墙 ＋ 逐格缺陷清单 ＋ 自检退出码）。逐条读数如下：

| 项 | 读数 |
|---|---|
| 唤醒词条数（运行期总表 `ALL_ROUTES` 筛 `scene === '02'`） | **83** |
| 真跑 | **83/83**，`exit 0` **83 条**、非 0 **0 条** |
| 落盘件数 | **83/83**（每条词一件产物，发布名 ＝ 唤醒词 ＋ `.html`） |
| 完整文档 | **83/83** 第 0 字节是 `<!doctype html>`；每件含 `charset`、页内 `<style>`、`</html>` |
| 字节区间 | 68,351 – 168,483 B（中位 79,803 B） |
| 自检（正例） | `83 格；链接 419 条（手机墙 167 ＋ 桌面墙 167 ＋ 总索引 85）；缺失 0 -> 可发` **exit 0** |
| 自检（反例） | 把清单第 1 行 `file` 改成盘上没有的名字 → **exit 1** 且点名「1 记一餐 -> __变异-盘上没有这一份__.html」；还原 → exit 0、清单逐字节相同 |
| 清单 | `manifest.json` 83 行，**不带 BOM**（第 0 字节 `0x7b`） |

**「70 条」与「83 条」的口径，先说清**：地图正文说的 70 条是**地图设计期的老技能词表**口径（`packages/skill-calorie/dist/triggers/scene-02-diet.js` 的 `SCENE_02_DIET`，恰好 70 条）；当刻 master 的**运行期总表**筛 `scene === '02'` 是 **83 条**。**70 条一条不漏地全含于这 83 条**，多出的 13 条是运行期补词（`看今日饮食记录`／`看饮食复盘`／`查高热量排行`／`查食品库`／`搜食品`／`看去重报告`／`查营养配比`／`看营养素明细`／`看食品来源分布`／`看今日饮水`／`看批量导入预览`／`看营养分析`／`看每日六因素`）。本席按票面「从运行期总表取、不手抄字面量」的口径跑**全 83 条**。

**两态分开记**（本图刚统一的那条口径）：
- **库为空 → 按设计 `exit 4`、不落盘**（缺失阻断，拒绝编数）：全新空库跑全部读命令 **65 条**，`exit 4` **61 条**；`exit 0` **4 条**（`校验批量导入`／`看批量导入预览`／`看有备注的饮食记录`／`看「有备注」的饮食记录` —— 参数全在命令里，不读库）。
- **窗口为空 → 出完整页 ＋ 空态句 ＋ 引导句**：库不动、窗口取 2000-01-01~2000-01-31 跑 10 条窗口类读命令，`exit 0` **9 条**、出完整文档 **9 件**、**9/9 都带空态句且带引导句**（原文抄进台账）。
- **例外 1 条**：`calorie.view.nutrition-analysis`（看营养分析）窗口为空时报 `ERR 4: 取数失败（缺失阻断）：无饮食记录：2000-01-01 ~ 2000-01-31`，与其余 9 条不同形 —— 见第二段。

## 二、机器证据

- **编译器自检**：`node D:\ilife\node_modules\typescript\bin\tsc --version` → `Version 5.9.2`（**绝对路径调真编译器，未走 `npx`**）。`node D:\ilife\node_modules\typescript\bin\tsc -b --force` → **exit 0**；`node packages/skill-calorie/scripts/gen-cli.mjs --stamp` → **exit 0**（dist 造出）。
- **唤醒词与命令从运行期总表取**：`packages/skill-calorie/dist/triggers/routes.generated.js` 的 `ALL_ROUTES`，筛 `scene === '02'`；未手抄任何字面量。
- **隔离库与种子**：`SKILLS_DB_PATH` 钉到 `.scratch/t280/db`。播种 **103/103 全绿**，快照 **155,648 字节**。种子覆盖：档案 1 条；食品库 10 条并打 6 个分类（蛋白类 3／主食类 3／脂肪类 1／蔬菜类 1／乳品类 1）＋2 组同名近重复（供去重报告取到数）；饮食记录 **75 天 × 每天 4 餐别**（早餐／午餐／下午茶／晚餐，轮换 10 种食物）→ 当日／昨日／本周／上周／本月／上月／最近 7 天／最近 30 天／自定义区间全都取到数；早餐／午餐／晚餐／加餐／全部餐别五支都有数；**至少一条带备注**（当日午餐「煎的，少油」、昨日晚餐「食堂」）；饮水 5 条；体脂 2 条＋围度 2 条。
- **每条跑前把库还原成同一份快照**（写词会改库，一条库从头跑到尾会把后面的读词打成 `exit 4`）—— 台账里 `exit 0` 83/83 就是这么来的，**没有一条是空页充数**。
- **内容完全相同的分组 2 组**：第 21／23 格（`window=7d` 与自定义区间这次正好同窗）；第 33／81 格（同命令同参数）。两组都在逐格缺陷清单里点名。
- **两条记录的所属场景待裁**：`calorie.view.nutrition-analysis` 与 `calorie.view.six-factors` 在 `ALL_ROUTES` 里**同时**挂着场景 10 与场景 02 的记录；`t313a-分区-证据.md` 按「WAKE ＞ NEW_KEY」判属**场景 10**。按票面口径（筛 `scene === '02'`）仍纳入本册，已在清单 `check` 里标注。
- **交付体积**：产物目录 86 个 `.html`（83 件产物 ＋ 两张墙 ＋ 索引），共 **6,951,188 字节**。
- **墙里每一格真渲染**：两张墙各 83 个 `iframe`，**83/83 指向同目录的真实文件**（落不到 0 条）；**未加 `loading="lazy"`**。

## 三、逐条路径

改动件（全部在检出 `D:\ilife-wt\wf155-m`）：

| 件 | 说明 |
|---|---|
| `docs/skills/skill-calorie/scene02-验收墙/manifest.json` | 清单 83 行（`seq`／`family`／`kind`／`wake`／`title`／`file`／`key`／`check`），含 `readings` 与 `notShipped`；族分布 ① 写后回执页 18／② 条目列表页 4／③ 横向排行榜页 22／④ 复盘·餐别分布页 13／⑤ 食品库类页 8／⑥ 营养·饮水·总览页 16／⑦ 预检确认页 2 |
| `docs/skills/skill-calorie/scene02-验收墙/gen-wall.mjs` | 墙生成器（入仓件、无依赖），照 `scene01-验收墙/gen-wall.mjs` 起；位置参数 `[产物目录] [输出名] [宽] [高]`、`--stage <源目录> <产物目录>`、`--check <产物目录>` |
| `docs/skills/skill-calorie/scene02-验收墙/桌面墙-1280.html` | 桌面墙：83 格 × 1280 宽 × 1 列（按 0.469 整体缩显示） |
| `docs/skills/skill-calorie/scene02-验收墙/手机墙-390.html` | 手机墙：83 格 × 390 宽 × 3 列（1:1） |
| `docs/skills/skill-calorie/scene02-验收墙/总索引.html` | 总索引：按 7 类页面分组 83 张卡，前有「本批机器读数」、末有「有意不出产物及其原因」 |
| `docs/skills/skill-calorie/scene02-验收墙/逐格缺陷清单.md` | 83 格逐格一行待负责人判 ＋ 「本席机械读数里已看出的几处」10 条（各带读数／归属票／请你判） |
| `docs/skills/skill-calorie/scene02-验收墙/*.html`（83 件） | 产物，**发布名 ＝ 唤醒词 ＋ `.html`**（全仓只此一处算名字；复制、墙、索引、自检都只读清单的 `file`） |
| `docs/skills/skill-calorie/t280-真跑台账.md` · `t280-真跑台账.json` | 逐条台账：唤醒词／命令原文／参数／exit／产物绝对路径／字节／第 0 字节／页内 H1；含种子、条数口径、两态读数与空态句原文 |
| `docs/skills/skill-calorie/t280-断言-自检正反两面.mjs` | 断言脚本（**能红**）：① 正例 `--check` 必须 exit 0；② 反例（改坏清单一行）必须 exit 1 且点名那份；③ 还原必须回绿且清单逐字节相同 |
| `.changeset/t280-验收墙.md` | changeset 记账（**只记账，不改任何包源码**） |

日志件（不入仓，`.scratch/t280/`）：`build.log`／`gen.log`／`live-run.log`／`live-run.json`／`two-state.log`／`two-state.json`／`empty-state-text.json`／`analyze.txt`／`verify.txt`／`selfcheck.log`／`routes.txt`／`seed-snapshot.db`／`products/`（83 件源副本）／`db-window/`（空窗产物 9 件）。

## 四、未做项与下一手缺什么

**没做的（本席范围内不做）**：
1. **版式判定**（挤、塌列、被藏起来的横滚、层级、可读性、双端差异）—— 一律不做脚本化判定，留给负责人肉眼（那是判据③之外他唯一要看的东西）。
2. **结构化断言**（票 #279）—— 本册只给可跑真跑的台账与可看的墙，锁不在本票。
3. **负责人在新空白 session 亲手跑**（票 #280 的另一半）—— 本册把产物、台账、墙备齐；亲手跑与签字收口不在本席。

**下一手要看的三处（已在逐格缺陷清单里逐条点名归属）**：
1. **第 37 格 `看饮食总览`**：路由参数是 `{"window":"7d","entry":"overview"}`，产物页内 H1 是「饮食总览 2026-09-14 ~ 2026-09-14」（昨日单日），正文是「本周累计／本月累计（统计到昨日）」；同命令的第 16–23 八格 H1 只有「饮食总览」四个字、不带日期。归 **#271**（`calorie.view.diet` 的唯一作者）。
2. **第 82 格 `看营养分析`**：**窗口为空时报 `exit 4`**，与其余 9 条空窗页（完整页＋空态句＋引导句）不同形；且这个命令的场景归属在运行期总表里同时挂场景 10 与 02 —— **场景归属待编排者裁**（若判属场景 10，则归场景 10 那张图，而不是本图 #275）。
3. **第 33／81 格 `校验批量导入`／`看批量导入预览`**：逐字节同一份产物，且产物 `<title>` 与页头品牌行写「卡路里 · 趋势」，页内 H1 是「批量导入预览」——页族名与页内容对不上，归 **#277**。

**下一手缺什么**：本册要变成「判据②达成」，还缺两样 —— ① 负责人拿真浏览器滚一遍两张墙，把缺陷**逐格**填进 `逐格缺陷清单.md` 第五节那张表的最后一列（没缺陷也要写「看过，无」）；② 编排者对上面第 2 条的**场景归属**裁定，以及第 1、3 条要不要作为缺陷开票。
