# t616 证据 · 场景02 验收缺陷①：看营养分析／看每日六因素 标题去日期（日期进正文）

> 票面入口：`gh issue view 616 --json body`（标题：场景02验收缺陷①：看营养分析/看每日六因素 标题去日期（日期进正文））。
> 本席窗口内**只写**：`packages/skill-calorie/src/render/trendMiscPortDocs.ts`（只在 `buildNutritionAnalysisDoc`／
> `buildSixFactorsDoc` 两函数体内 ＋ 一行 import）、`docs/skills/skill-calorie/t616-*`、`.scratch/t616/**`。
> 运行对账源：`docs/skills/skill-calorie/t616-gate-runs.log`（协议 §2.3.4 的导出件；本次窗口 10 趟条目一对一可核）。

## 一、回执四段

**第一行：PASS。** 票面五条验收全部由本席真跑：编译 `exit 0`；两页真出口产物 **H1 无日期 4/4 绿**、
**正文首件含日期 4/4 绿**；`t511-清尾二`＋`render-t9`＋`trend-misc-port-113` → **34 测／34 绿／0 红，exit 0**；
场景10 回归 82 测／79 绿＋**3 条他席陈旧断言**（改前／改后／还原后三趟读数逐字相同，逐条点名不代修）；
同件 6 页前后比对＝**只本票两页字节变、其余 4 页逐页 sha256 相同**；变异自证（H1 日期改回旧写法）**红 2 条 → 还原绿 0 条**。
另跑两条工程门：**门禁 83 件非脚注零命中 83/83**、**告警线门 91/91**（LF **608→608 守恒**，台账不动）。

**第二段：交付提交（逐笔经 `git diff --cached --name-only` 复核只含本票件）＋ GATE-RUN 表**

| 提交 | 内容 |
|---|---|
| `ee4c795` | `修复(616): 看营养分析/看每日六因素标题去日期＋日期进正文窗口条 -- src/render/trendMiscPortDocs.ts`（两页 H1 只留人话短名；窗口区间进正文首件 `dui-window`；原参数表单的换算说明改住口径行；LF 608→608） |
| 本件（文档提交，见第四段） | `docs/skills/skill-calorie/t616-证据.md` ＋ `docs/skills/skill-calorie/t616-gate-runs.log` |

| 本票趟次 | GATE-RUN runId | RESULT／退出码 |
|---|---|---|
| 编译 `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force`（改动前基线） | `t616-build-base` | waitedMs=0 **exit 0** |
| 同上（改动后） | `t616-build-fix` | waitedMs=0 **exit 0** |
| 票面三件测试 `node --test test/{t511-清尾二,render-t9,trend-misc-port-113}.test.mjs` | `t616-test-trio` | waitedMs=0 **exit 0** → 34／34 绿 |
| 场景10 回归 `node --test test/analysis-*.test.mjs` | `t616-run-analysis` | waitedMs=20001 **exit 1** → 82 测／79 绿／**3 红**（全在 `analysis-t5`，他席陈旧断言） |
| 同件 6 页前后比对（窗口内换 `HEAD` 版 ↔ 还原）＋同趟 3 次场景10 回归 | `t616-roundtrip` | waitedMs=0 **exit 0** → 变 2 页／未变 4 页／还原自证真；三趟 82/79/3 同名同数 |
| 变异确认（改坏 → 红 → 还原 → 绿） | `t616-mutation` | waitedMs=10001 **exit 0** → 红 2 条 → 红 4 条 → 绿 0 条 |
| 裁定「只改两函数体内」静态边界复核（逐 hunk 落点） | `t616-boundary` | waitedMs=0 **exit 0** → **BOUNDARY-PASS** |
| 门禁 83（83 条场景02 唤醒词逐条真出口重跑 → 分隔符门点审） | `t616-gate83` | waitedMs=0 **exit 0** → 非脚注零命中 **83/83** |
| 告警线台账门（工程红线） | `t616-warningline` | waitedMs=10001 **exit 0** → `RESULT: 91/91 PASS` |
| 本票源码提交（暂存区复核只含本票 1 件） | `t616-commit-src` | waitedMs=0 **exit 0** → `STAGED-CHECK-GREEN 只含本票 1 件` → `ee4c795` |

**第三段：逐条路径**

- 改动件（本票唯一产品代码，**只动两函数体内 ＋ 一行 import**）：`packages/skill-calorie/src/render/trendMiscPortDocs.ts`
  （LF **608 → 608**，与包内台账「当场实测」同为 608）。
- 本票产出（新件）：`docs/skills/skill-calorie/t616-证据.md`（本件）、`docs/skills/skill-calorie/t616-gate-runs.log`（运行条目导出件）。
- 本票草稿（不入仓，`.scratch/t616/**`）：`lib.mjs`（真出口渲染／编译／sha 小件）、`probe.mjs`（两页判据探针）、
  `dump.mjs`（结构勘察）、`roundtrip.mjs`（6 页前后比对 ＋ 三趟场景10 回归）、`mut.mjs`（变异自证）、
  `boundary.mjs`（静态边界）、`gate83.mjs`（门禁复跑）、`commit.mjs`（受锁提交）、各趟 `*.log`／`gate83.json`。
- **一行未动**（票面「不许动」清单）：`analysis/**`、取数口径、路由（`src/triggers/**`）、`packages/base-render/**`、
  屏上来源脚注行（归 #560）、场景10 的任何断言、两件测试（`t511-清尾二`／`render-t9` **一字未改**——它们本来就没有一条断言钉这两页的 H1，
  见第二节第 3 条）、他席在途件。

**第四段：没做完啥 ＋ 下一手缺啥**

- **没做（并写明为什么）**：① **未改两件测试**——票面只许它们「仅当断言钉住被改标题时」动；实测这两件对本票两页的断言只钉副标题／正文文本／载荷，
  没有一条读 H1（`grep` 逐条核过），故按「不加戏」处置，改为把新判据落成票面点名的真出口探针读数（第二节）。② **未跑 `scene02-验收墙` 重出**——
  它写 `scene02-验收墙/**`，不在本票只写清单内；复验墙与复拍按票面归 **#619**。
- **他席红（只点名，不代修）**：`analysis-t5.test.mjs` 三条陈旧断言——`:168` 期望 `/startA/`、`:181` 期望 `平台期首日`、
  `:232` 期望 `forecast.points.length === 13`（实测 14）；背后三件（`weight/compare.ts`／`weightCompare2.ts`／`analysis/simulate.ts`）均非本票写集，
  且 **#603 复核席 2026-09-16 的独立重跑已记为「范围外（他席陈旧断言）」**（`docs/skills/skill-calorie/t603-证据.md:105-122`：82／79／3、同三条）。
- **下一手缺啥**：复验由 **#619** 统一重出墙＋复拍；本票不推送（推送由总工程师统一收）。

## 二、判据读数（票面五条逐条）

### 2.1 编译（票面第 1 条）

`GATE-RUN runId=t616-build-fix cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force"` → **exit 0**（日志 `.scratch/t616/build-fix.log`）。

### 2.2 两页真出口产物：H1 无日期 ＋ 正文首件有日期（票面第 2、3 条）

真出口＝`calorie-cmd-read <键> --html <我的草稿目录>`（种子 `docs/skills/skill-calorie/t279-真跑.mjs` 的 `makeSeed`，
场景02 参数取运行期总表第一条：营养分析 7 天窗 `2026-09-09 ~ 2026-09-15`；六因素单日 `2026-09-15`）。
**绿两行**（原样产物，`.scratch/t616/probe.log`）：

```
H1-NO-DATE   key=calorie.view.nutrition-analysis h1="营养分析"    命中日期=0 判=PASS
BODY-HAS-DATE key=calorie.view.nutrition-analysis 首件窗口条=dui-window✓ 首件日期数=2 正文日期=["2026-09-09","2026-09-15"] 判=PASS
H1-NO-DATE   key=calorie.view.six-factors      h1="每日六因素"    命中日期=0 判=PASS
BODY-HAS-DATE key=calorie.view.six-factors      首件窗口条=dui-window✓ 首件日期数=1 正文日期=["2026-09-15"] 判=PASS
RESULT: 4/4 PASS
```

**红两行**（同两页把 H1 日期改回旧写法，`.scratch/t616/mut.log` 的 `RED-A` 段）：

```
RED-A H1-NO-DATE   key=calorie.view.nutrition-analysis h1="营养分析 2026-09-09 至 2026-09-15" 判=FAIL
RED-A H1-NO-DATE   key=calorie.view.six-factors       h1="每日六因素 2026-09-15"              判=FAIL
```

⇒ H1 判据会红、会绿，**不是永真**。日期不是被删掉：同两页正文可见文本里读得到同一批日期，且**首件就是窗口条**
（`<div class="dui-window">`：营养分析＝`2026-09-09 → 2026-09-15` ＋「7 天」胶囊；六因素＝单日窗退化成一枚
`2026-09-15（单日）` 日期块，形状与样式接包内现成件 `src/diet/dietUi.ts`，未新造形状）。

### 2.3 票面三件测试（票面第 4 条前半）

`GATE-RUN runId=t616-test-trio` → **exit 0**，`ℹ tests 34 ／ pass 34 ／ fail 0`（日志 `.scratch/t616/trio.log`）。
两件测试**逐字未改**：它们的断言钉的是副标题（`这页有什么`／`这 6 项达标了几项（热量按千卡计）`）、
正文文案与复制载荷，没有一条读这两页的 H1 ⇒ 跨族断言未变红。

### 2.4 场景10 回归与「趋势域页一字未变」（票面第 4 条后半）

① 全量：`GATE-RUN runId=t616-run-analysis` → **exit 1**，82 测／79 绿／**3 红**；三条红逐条点名＝
`weightCompare：17 场景分发`／`weightCompare：平台期/极端月/同期`／`simulate：外推+模拟+摄入`（全在 `analysis-t5.test.mjs`，
见第一段第四段的「他席红」）。

② 前后比对（**同趟窗口**：换 `HEAD` 版 → 重编译 → 重跑 → 逐字节还原 → 重编译 → 重跑；`GATE-RUN runId=t616-roundtrip`，**exit 0**）：

```
ANALYSIS NEW  exit=1 tests=82 pass=79 fail=3 红条=["weightCompare：17 场景分发","weightCompare：平台期/极端月/同期","simulate：外推+模拟+摄入"]
ANALYSIS OLD  exit=1 tests=82 pass=79 fail=3 红条=[同上三条]
ANALYSIS NEW2 exit=1 tests=82 pass=79 fail=3 红条=[同上三条]
CHANGED   calorie.view.nutrition-analysis   旧页sha=d3eab859652e2688 新页sha=dc99bc36b0d39e8c 判=允许(#616 本票改到的页)
CHANGED   calorie.view.six-factors          旧页sha=b1ea71aa4bc29f37 新页sha=03234a56ae67c2e6 判=允许(#616 本票改到的页)
UNCHANGED calorie.view.calorie-trend        sha=30ba8d8ca8b6dddd
UNCHANGED calorie.view.long-trend           sha=9cefa87cae3f6af0
UNCHANGED calorie.view.lint-health          sha=b121036020cf0a9b
UNCHANGED calorie.view.review-template      sha=f4aea4b2a543b85a
RESTORE-SELFCHECK 新旧一致=true（NEW vs NEW2 逐页 sha256）
ANALYSIS-T5-SAME 改前改后同名同数=true 读数=82/79/3
SCENE10-ROUNDTRIP pages=6 changed=2 unchanged=4 → SCENE10-ROUNDTRIP-PASS
```

⇒ 同件 6 页里**只有本票点名的两页**变了；趋势域另 4 页（热量趋势／整体趋势／数据健康／复盘模板）与改动前**逐字节相同**；
场景10 回归读数**改前改后完全一致**（含那 3 条他席红）——本票没往场景10 挪一分脏。

③ 门禁：`GATE-RUN runId=t616-gate83` → **exit 0**，83 条场景02 唤醒词逐条真出口重跑，**非脚注零命中 83/83**
（来源脚注命中 66 处归 #560；`gate83-summary.json` 备查）。两页新产物单审也是 2/2 零命中（`.scratch/t616/audit-sep.log`）。

### 2.5 变异自证（票面第 5 条）

`GATE-RUN runId=t616-mutation` → **exit 0**（`.scratch/t616/mut.log`）：

```
GREEN  红条=0                       （原样产物：H1 无日期 / 正文首件有日期，两条判据各 2 页全绿）
RED-A  红条=2                       （变异A＝两页 H1 日期改回旧写法 ⇒ H1 判据必红、正文判据仍绿——两半互不冒充）
RED-AB 红条=4                       （变异B＝再把两处正文窗口条摘掉 ⇒ 正文判据一并必红）
GREEN2 红条=0                       （逐字节还原 ＋ 重编译后回绿）
RESTORE-SELFCHECK 还原一致=true；GIT-DIFF 15	15  sha256=ace1b191e70d7a5c 进场=ace1b191e70d7a5c
MUTATION-PASS
```

### 2.6 工程红线：告警线台账门（LF 守恒）

`GATE-RUN runId=t616-warningline` → **exit 0**，`RESULT: 91/91` `PASS: 告警线台账齐全且与实况一致`。
本票按**行数守恒**做（`trendMiscPortDocs.ts` **608 → 608**）⇒ 包内台账（`packages/skill-calorie/AGENTS.md` 的
`<!-- warning-line-ledger -->` 表，该件「当场实测」列 ＝ 608）无需改动、不涉他席文件。

> **必报五步·第四步**：`src/render/trendMiscPortDocs.ts` **已超线，需要根据规则进行重构**（608 ＞ 350）。
> 超因：该件一件住着 #113「趋势 2＋其他 6」共 8 键的整页装配；本票只做其中两页的标题落位与窗口条接入，
> 体量上只挪了 3 行注释密度。拆法＝台账那一行已写好的「按页族切姊妹件（营养分析／六因素／其余薄页分开，
> 块层共件提出，出口经本件薄转出）」，**本次不拆**：结构重排不在本票写集（票面只许动两函数体内），归收口票认领。

## 三、边界（裁定「只改两函数体内」的两路证明）

### 3.1 静态：逐 hunk 落点（`GATE-RUN runId=t616-boundary` → **BOUNDARY-PASS**）

把 `ee4c795^` 与 `ee4c795` 两份源码的 `buildNutritionAnalysisDoc`／`buildSixFactorsDoc` 两函数体遮成占位符后逐行对照：

```
BOUNDARY 抹掉两函数体后：只在旧版出现的行=0  只在新版出现的行=1
  NEW-ONLY "import { dietUiCss, windowStrip } from '../diet/dietUi.js';"
PROBE "const DOC_TITLE = '卡路里·趋势';"   旧=1 新=1 判=同
PROBE "    eyebrow: '卡路里 · 趋势',"      旧=5 新=5 判=同
PROBE "    eyebrow: '趋势分析',"          旧=2 新=2 判=同
OTHER-FUNC buildCalorieTrendDoc  体逐字节同=true  bytes=6634
OTHER-FUNC buildLongTrendDoc     体逐字节同=true  bytes=5216
OTHER-FUNC buildLintHealthDoc    体逐字节同=true  bytes=1450
OTHER-FUNC buildProcessProgressDoc 体逐字节同=true bytes=1209
OTHER-FUNC buildReviewTemplateDoc  体逐字节同=true bytes=1760
BOUNDARY-PASS 两函数体外零残留差异（除本票声明的一行 import）
```

⇒ 两函数体之外**只有一行新增 import**（`dietUiCss`／`windowStrip` 的依赖声明，不属「模块常量」也不属「别的函数」，
票面「模块常量 `DOC_TITLE`／`eyebrow` 与其余函数一行不碰」逐条核过）；模块常量 `DOC_TITLE` 与**全部 7 处** `eyebrow`
（`卡路里 · 趋势` 5 处／`趋势分析` 2 处）出现次数与位置一字未动。

### 3.2 动态：同件 6 页前后比对

见 §2.4②：变 2 页（本票两页）／未变 4 页（逐页 sha256 相同）／NEW ≡ NEW2（还原干净）。

### 3.3 两页的改动落点（逐处）

| 页 | 改动前 | 改动后 |
|---|---|---|
| 营养分析 `calorie.view.nutrition-analysis` | H1＝`营养分析 2026-09-09 至 2026-09-15`；正文首件是窗口**参数表单**（两个可编辑 `<input>`，日期只住 `value` 属性，可见文本读不到）＋其 `description` 槽的换算说明 | H1＝`营养分析`；正文首件＝窗口条 `2026-09-09 → 2026-09-15`（＋`7 天`胶囊）；**同一句换算说明原样改住口径行**（一字未删），把「日期只住属性」的老形状一并收掉（同 `#523 R5` 对 `renderParamForm` 的处置先例） |
| 每日六因素 `calorie.view.six-factors` | H1＝`每日六因素 2026-09-15`；正文首件是读数卡格（全页正文里没有该日日期） | H1＝`每日六因素`；正文首件＝单日窗口条 `2026-09-15（单日）`（一枚日期块，现成件退化分支） |

`eyebrow`（`趋势分析`）／副标题／读数卡／明细表／复制载荷／屏上来源脚注**一字未动**。

## 四、备查读数（明细件，均在 `.scratch/t616/`）

`build-base.log`／`build-fix.log`（编译）、`trio.log`（三件测试 34/34）、`analysis.log`（场景10 回归 82/79/3）、
`roundtrip.log`（6 页前后比对 ＋ 三趟回归）、`mut.log`（变异四段）、`boundary.log`（边界）、`gate83.log`＋`gate83.json`＋`gate83-summary.json`（门禁 83）、
`warningline.log`（告警线门 91/91）、`probe.log`（两页判据 4/4）、`audit-sep.log`（两页产物分隔符 2/2）、`dump-after.log`（两页结构勘察）。
