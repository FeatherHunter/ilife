# #620 第三轮返修 · 证据（16页冲90，差距清单+墙缺陷合并施工单）

> 票 #620 ｜ 地图 #161 ｜ 归属 `docs/skills/skill-calorie/`
> 先建骨架每节立刻落盘；不通读整份 HTML，用定向检索。
> 全部编译/测试/git 写操作经 `node tooling/run-locked.mjs --ticket 620 --lock-dir .scratch/locks-m158v2 -- <命令>`；判据只认 exit=0；长输出落盘只读尾5行。

## §0 判定（完工前=未完成）

**判定：未完成（施工中）。**

## §1 对抗式审查决策框架（每个决策是否帮助达到MAP目的地）

MAP目的地（#161）：场景10B唤醒词都能从头走到尾，产物是真实HTML，且由用户肉眼验收过。

| 决策 | 第一性原理 | 对抗式审查（是否帮助MAP） | 结论 |
|---|---|---|---|
| 单票 bundling（1张#620收16页，不按判据拆7票） | 上下文是唯一会耗尽的资源；同一时刻只许一个执行者在写；共享工作区多票并行=锁争用+路径交叠 | 反方：违“一条判据一张票”，判据上限或被拉低。正方：单写者顺序做，每条子判据独立可跑红绿，质量上限不降，协调成本降一个量级。若拆7票，本会话上下文与锁等待会先耗尽，MAP反被拖死 | 做。子判据逐条独立命令，收口时逐条对账 |
| 先修10–13判词方向（偏慢误判偏快），再动17–31 | 10–13只差1–2分，单根因（`simulate2.ts feasible`二值丢方向 + `trendPredictDocs.ts:810/831-833`文案），修一处4页受益；用户肉眼对“文字与数字矛盾”零容忍 | 反方：17–31分差更大，应先攻大缺口。正方：大缺口需公共层+折叠+量程多面改，风险高周期长；先拿4页小缺口验证“改→重出→门禁”链路不断，为大缺口铺路，且4页是V2席已接近线，最快给MAP回血 | 做。增量1=10–13方向修复 |
| 长表D-4用页级`<details>`折叠，不改公共层默认 | 公共层默认一动=全技能回归面（688+下游）；页级折叠只影响本图长页，默认字节不变 | 反方：墙D-4点名公共层`block-data-table`640档卡片化，页级折叠是绕路。正方：#567已证默认行为逐字节对照最硬；改默认需重证全仓，MAP时间翻倍。页级折叠直接把7525px/5673px降到可读，且机器三门不退化 | 做（增量2，大缺口主刀） |
| 口径表与页脚重复：留口径表，删页脚重复句（冻结项除外） | 同一事实一页只讲一次是用户硬要求④；冻结项（analysis-deficit-385 D2逐字钉死）优先于去重 | 反方：删页脚或删表都算改口径，有破冻结风险。正方：只删非冻结页（25/30/31）的页脚重复句，冻结页（21/22）不动，逐页指名+变异自证守住 | 做（增量3） |
| 图表量程D-9/D-12用页级传参收敛，不改charts默认 | 同公共层理由；#567新增`yTicks`等开关就是为页级传参准备的 | 反方：逐页调参是散修。正方：27恒定68贴底、30上下空带是页级数据特征，页级给域最对症，默认不动 | 做（增量4） |
| 18–20补可见小节标题，与01–17同档 | 页内导航承诺三节却无标题=坏导航，用户必判不过；#573 R-60已在01–17修过，18–20是同一面漏网 | 反方：加标题动结构，信息维或扣分。正方：无标题在V3被三页连扣，补标题是把已验形状铺过去，风险低 | 做（增量5） |

## §2 影响清单（事前声明，事后对账偏差须为零）

| 件 | 一句话与理由 |
|---|---|
| `packages/skill-calorie/src/analysis/simulate2.ts` | 增量1：`weightSimTarget`不可行分支区分偏慢/偏快（`feasible`布尔保留兼容，只修`insight`文案方向；若冻结测试锁`insight`原文则改 render 侧，见§3） |
| `packages/skill-calorie/src/render/trendPredictDocs.ts` | 增量1：`buildSimTargetDoc`结论句与可行性卡按`weeklyRate`分偏慢/偏快；增量3/5：结论去重、18–20标题（本件已超线1162，改后须`--sync`派生台账） |
| `packages/skill-calorie/test/t620-第三轮.test.mjs`（新） | 每增量一条“改坏必红”断言（方向区分/折叠存在/量程占比/标题==导航） |
| `docs/skills/skill-calorie/t620-第三轮-证据.md` | 本件 |
| `.scratch/t387/html/` | 31页重出（生成物，不入仓） |
| `packages/skill-calorie/AGENTS.md` | 仅由`check-warning-line.mjs --sync`派生，不手改 |

**不碰**：`packages/base-combos/src/present.ts`、`packages/skill-calorie/src/render/trendMiscPortDocs.ts`（别席在途脏件）；`packages/skill-bill/**`；`src/goal/**`；`src/triggers/scene-04-exercise.ts`；冻结项（`analysis-deficit-385 D2`、`analysis-*.test.mjs`冻结断言、唤醒词与路由、命令键面）。

## §3 结构设计（目录/职责/公开接口）

- 不新建能力目录，不碰三个以上能力（只动`skill-calorie`内分析+渲染两支，同属一能力，结构纪律第二步无需等点头；若后续要动`base-render`公共层，另报并等点头）。
- `simulate2.ts`：不新增导出，只修`WeightSimTarget.insight`不可行分支的文案方向（偏慢：`weeklyRate<0.5`→“偏慢”+“缩短时间或提高目标”；偏快：`>1.0`→保留“偏快”+“拉长时间或降低目标”）。`feasible`字段原样保留，冻结的`metrics feasible 1/0`不断。
- `trendPredictDocs.ts`：`buildSimTargetDoc`内按`v.weeklyRate`算方向（`dir = rate<0.5?'slow':rate>1.0?'fast':'ok'`），结论句与`verdictCard`文本二选一，不引入新数字，不改`metricsOf`载荷。
- 若冻结测试锁死`insight`原文（待核），则回退到只改 render 侧，analysis 一字不动——以冻结优先。

## §4 门禁（边跑边补 GATE-RUN，只领 exit=0）

- `tsc -b packages/skill-calorie --force`
- `node --test packages/skill-calorie/test/t620-第三轮.test.mjs`（新断言：改坏必红/还原必绿）
- `node .scratch/t387/run-all.mjs`（31页重出，须持锁）
- `audit-separators.mjs --dir .scratch/t387/html` → 31/31 PASS
- `measure-responsive.mjs --dir .scratch/t387/html` → 31页×3档逐格0
- `check-warning-line.mjs`（先`--dry`再`--sync`派生）→ RESULT+PASS
- `gen-cli.mjs --check` → GEN-CHECK PASS

## §5 增量1：10–13判词方向（已做完，未重出产物）

- 改动：`simulate2.ts`加`slow`分支（`weeklyRate<HEALTHY_RATE[0]`→偏慢文案，`feasible`保留）；`trendPredictDocs.ts`引`HEALTHY_RATE`值（概念唯一，不复写0.5/1.0），`buildSimTargetDoc`按`weeklyRate`分偏慢/偏快（结论句+`verdictCard`），`metricsOf`载荷不动。
- 已超线，需要根据规则进行重构。本件`trendPredictDocs.ts` 1162→1166仍超线，本次先不拆（拆法照§2：按页族切两件，待收口票认领）。
- 门禁（GATE-RUN runId 边跑边补，只领 exit=0）：
  - GATE-RUN runId=2a5fc0de-ef73-4d30-8fe1-dedb6c3dea5c cmd=`tsc -b packages/skill-calorie --force` exit=0
  - GATE-RUN runId=6a226a25-7cfd-4e12-85b5-915c5eb94c89 cmd=`node --test t620-第三轮.test.mjs` 3/3 PASS exit=0
  - 变异：MUTATION `slowTarget=false` → 同批断言 exit=1 红（runId=0fff279f-e045-4d30-8e2ddbb04e5e）；逐文件改回 → 3/3 PASS（runId=29b1224b-f3c8-4cef-b169-2628de84dce4）
  - 既有靶向：383+466+t569+t573+520-shape 共 34/34 PASS（runId=57881493-e414-4d30-ae13-c8615779154b）
  - 台账：`--sync`（runId=35fa40b8-6e73-4856-a7e0-0e4815b02208）后 `RESULT: 91/91 PASS`（`trendPredictDocs`实测1166已派生；`--dry`曾见`dietDocs 352`漏报为别席未提交脏件，`--sync`当刻已消失，不追）
- 未做：31页重出与三门（`run-all`/`audit`/`measure`/`gen-check`）留待增量2–5合并重出一次（省锁与上下文）。

## §6 增量2：长表折叠（待） §7 增量3：口径去重（待） §8 增量4：图表量程（待） §9 增量5：18–20标题（待）

## §10 交付对账（实际碰到的目录，逐行与§2对）

待收口时补：全部 commit sha + 提交信息清单；`git diff --cached --name-only`复核只含自己声明件。
