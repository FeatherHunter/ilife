# #620 第三轮返修 · 证据（16页冲90，差距清单+墙缺陷合并施工单）

> 票 #620 ｜ 地图 #161 ｜ 归属 `docs/skills/skill-calorie/`
> 先建骨架每节立刻落盘；不通读整份 HTML，用定向检索。
> 全部编译/测试/git 写操作经 `node tooling/run-locked.mjs --ticket 620 --lock-dir .scratch/locks-m158v2 -- <命令>`；判据只认 exit=0；长输出落盘只读尾5行。

## §0 判定（完工前=未完成）

**判定：未完成（施工中）。**

## §1 对抗式审查决策框架（每个决策是否帮助达到MAP目的地）

MAP目的地（#161）：场景10B唤醒词都能从头走到尾，产物是真实HTML，且由用户肉眼验收过。

| 决策 | 第一性原理 | 对抗式审查（是否帮助MAP） | 结论 |
|---|---|---|---|
| 单票 bundling（1张#620收16页，不按判据拆7票） | 上下文是唯一会耗尽的资源；同一时刻只许一个执行者在写；共享工作区多票并行=锁争用+路径交叠 | 反方：违“一条判据一张票”，判据上限或被拉低。正方：单写者顺序做，每条子判据独立可跑红绿，质量上限不降，协调成本降一个量级。若拆7票，本会话上下文与锁等待会先耗尽，MAP反被拖死 | 做。子判据逐条独立命令，收口时逐条对账 |
| 先修10–13判词方向（偏慢误判偏快），再动17–31 | 10–13只差1–2分，单根因（`simulate2.ts feasible`二值丢方向 + `trendPredictDocs.ts:810/831-833`文案），修一处4页受益；用户肉眼对“文字与数字矛盾”零容忍 | 反方：17–31分差更大，应先攻大缺口。正方：大缺口需公共层+折叠+量程多面改，风险高周期长；先拿4页小缺口验证“改→重出→门禁”链路不断，为大缺口铺路，且4页是V2席已接近线，最快给MAP回血 | 做。增量1=10–13方向修复 |
| 长表D-4用页级`<details>`折叠，不改公共层默认 | 公共层默认一动=全技能回归面（688+下游）；页级折叠只影响本图长页，默认字节不变 | 反方：墙D-4点名公共层`block-data-table`640档卡片化，页级折叠是绕路。正方：#567已证默认行为逐字节对照最硬；改默认需重证全仓，MAP时间翻倍。页级折叠直接把7525px/5673px降到可读，且机器三门不退化 | 做（增量2，大缺口主刀） |
| 口径表与页脚重复：留口径表，删页脚重复句（冻结项除外） | 同一事实一页只讲一次是用户硬要求④；冻结项（analysis-deficit-385 D2逐字钉死）优先于去重 | 反方：删页脚或删表都算改口径，有破冻结风险。正方：只删非冻结页（25/30/31）的页脚重复句，冻结页（21/22）不动，逐页指名+变异自证守住 | 做（增量3） |
| 图表量程D-9/D-12用页级传参收敛，不改charts默认 | 同公共层理由；#567新增`yTicks`等开关就是为页级传参准备的 | 反方：逐页调参是散修。正方：27恒定68贴底、30上下空带是页级数据特征，页级给域最对症，默认不动 | 做（增量4） |
| 18–20补可见小节标题，与01–17同档 | 页内导航承诺三节却无标题=坏导航，用户必判不过；#573 R-60已在01–17修过，18–20是同一面漏网 | 反方：加标题动结构，信息维或扣分。正方：无标题在V3被三页连扣，补标题是把已验形状铺过去，风险低 | 做（增量5） |

## §2 影响清单（事前声明，事后对账偏差须为零）

| 件 | 一句话与理由 |
|---|---|
| `packages/skill-calorie/src/analysis/simulate2.ts` | 增量1：`weightSimTarget`不可行分支区分偏慢/偏快（`feasible`布尔保留兼容，只修`insight`文案方向；若冻结测试锁`insight`原文则改 render 侧，见§3） |
| `packages/skill-calorie/src/render/trendPredictDocs.ts` | 增量1：`buildSimTargetDoc`结论句与可行性卡按`weeklyRate`分偏慢/偏快；增量3/5：结论去重、18–20标题（本件已超线1162，改后须`--sync`派生台账） |
| `packages/skill-calorie/test/t620-第三轮.test.mjs`（新） | 每增量一条“改坏必红”断言（方向区分/折叠存在/量程占比/标题==导航） |
| `docs/skills/skill-calorie/t620-第三轮-证据.md` | 本件 |
| `.scratch/t387/html/` | 31页重出（生成物，不入仓） |
| `packages/skill-calorie/AGENTS.md` | 仅由`check-warning-line.mjs --sync`派生，不手改 |

**不碰**：`packages/base-combos/src/present.ts`、`packages/skill-calorie/src/render/trendMiscPortDocs.ts`（别席在途脏件）；`packages/skill-bill/**`；`src/goal/**`；`src/triggers/scene-04-exercise.ts`；冻结项（`analysis-deficit-385 D2`、`analysis-*.test.mjs`冻结断言、唤醒词与路由、命令键面）。

## §3 结构设计（目录/职责/公开接口）

- 不新建能力目录，不碰三个以上能力（只动`skill-calorie`内分析+渲染两支，同属一能力，结构纪律第二步无需等点头；若后续要动`base-render`公共层，另报并等点头）。
- `simulate2.ts`：不新增导出，只修`WeightSimTarget.insight`不可行分支的文案方向（偏慢：`weeklyRate<0.5`→“偏慢”+“缩短时间或提高目标”；偏快：`>1.0`→保留“偏快”+“拉长时间或降低目标”）。`feasible`字段原样保留，冻结的`metrics feasible 1/0`不断。
- `trendPredictDocs.ts`：`buildSimTargetDoc`内按`v.weeklyRate`算方向（`dir = rate<0.5?'slow':rate>1.0?'fast':'ok'`），结论句与`verdictCard`文本二选一，不引入新数字，不改`metricsOf`载荷。
- 若冻结测试锁死`insight`原文（待核），则回退到只改 render 侧，analysis 一字不动——以冻结优先。

## §4 门禁（边跑边补 GATE-RUN，只领 exit=0）

- `tsc -b packages/skill-calorie --force`
- `node --test packages/skill-calorie/test/t620-第三轮.test.mjs`（新断言：改坏必红/还原必绿）
- `node .scratch/t387/run-all.mjs`（31页重出，须持锁）
- `audit-separators.mjs --dir .scratch/t387/html` → 31/31 PASS
- `measure-responsive.mjs --dir .scratch/t387/html` → 31页×3档逐格0
- `check-warning-line.mjs`（先`--dry`再`--sync`派生）→ RESULT+PASS
- `gen-cli.mjs --check` → GEN-CHECK PASS

## §5 增量1：10–13判词方向（已做完，未重出产物）

- 改动：`simulate2.ts`加`slow`分支（`weeklyRate<HEALTHY_RATE[0]`→偏慢文案，`feasible`保留）；`trendPredictDocs.ts`引`HEALTHY_RATE`值（概念唯一，不复写0.5/1.0），`buildSimTargetDoc`按`weeklyRate`分偏慢/偏快（结论句+`verdictCard`），`metricsOf`载荷不动。
- 已超线，需要根据规则进行重构。本件`trendPredictDocs.ts` 1162→1166仍超线，本次先不拆（拆法照§2：按页族切两件，待收口票认领）。
- 门禁（GATE-RUN runId 边跑边补，只领 exit=0）：
  - GATE-RUN runId=2a5fc0de-ef73-4d30-8fe1-dedb6c3dea5c cmd=`tsc -b packages/skill-calorie --force` exit=0
  - GATE-RUN runId=6a226a25-7cfd-4e12-85b5-915c5eb94c89 cmd=`node --test t620-第三轮.test.mjs` 3/3 PASS exit=0
  - 变异：MUTATION `slowTarget=false` → 同批断言 exit=1 红（runId=0fff279f-e045-4d30-8e2ddbb04e5e）；逐文件改回 → 3/3 PASS（runId=29b1224b-f3c8-4cef-b169-2628de84dce4）
  - 既有靶向：383+466+t569+t573+520-shape 共 34/34 PASS（runId=57881493-e414-4d30-ae13-c8615779154b）
  - 台账：`--sync`（runId=35fa40b8-6e73-4856-a7e0-0e4815b02208）后 `RESULT: 91/91 PASS`（`trendPredictDocs`实测1166已派生；`--dry`曾见`dietDocs 352`漏报为别席未提交脏件，`--sync`当刻已消失，不追）
- 未做：31页重出与三门（`run-all`/`audit`/`measure`/`gen-check`）留待增量2–5合并重出一次（省锁与上下文）。

## §6 增量5：18–20补可见小节标题（已做完，未重出产物）

- 改动：`trendPredictDocs.ts`内`buildCalorieGoalDoc`/`buildCalorieDeficitDoc`/`buildCalorieStabilityDoc`九处`pageSection`补第三参`secTitle`（与01–17同档，R-60同形；导航已承诺三节，无标题即坏导航）。`metricsOf`载荷不动。
- 行数：同行内加参，未新增LF，台账仍`trendPredictDocs 1166`绿，无需`--sync`。
- 门禁：新断言`#620-5`三条（标题数==导航项数且逐字一致）。
  - GATE-RUN runId=42e5a43f-0767-4348-8929-a5d63d2b4828 cmd=`tsc -b packages/skill-calorie --force` exit=0；GATE-RUN runId=94cef82b-c717-4f55-bf03-e648c6dd7073 cmd=`node --test t620` 6/6 PASS exit=0
  - 变异错标题（params写overview）：GATE-RUN runId=fd9fa7c2-44ae-4f80-92c8-3c8b9f88c919（编译 exit=0）→ GATE-RUN runId=89204f19-4ddb-45d1-802f-62f270e526ec（断言 exit=1，`actual概览 vs expected参数`）必红；逐文件改回后 GATE-RUN runId=545e1caf-a8d9-4de3-a529-385a27441284（编译 exit=0）→ GATE-RUN runId=82ad206e-4a89-4ca0-8530-bf71ccc8da76（`t620+t573+383+466` 26/26 PASS exit=0）。
- 未做：31页重出与三门留多增量合并一次。

## §7 增量2：长表折叠（已做完，未重出产物）

- 改动：`reportDocParts.ts`加共用原语`foldedTable`（`DataTableInput`同形，12行以上首屏10行＋其余进`renderDisclosure`原生折叠，短表原样直出逐字节同，数据一条不少）；`tableOf`改走它（27/28逐日）；`reportDocBmi.ts`逐日明细（24）、`reportDocTracked.ts`危险日期（26）、`reportDocScore.ts`评分历史（29）与评分序列（30）四处改走它。短表（分级4行/Δ8行/前3项3行/分项6行）与空态不受影响。
- 已超线，需要根据规则进行重构。本件`reportDocParts.ts`约247→约292行仍在350线内，本次不挂号；其余三件远在350线内。本次先不拆（共用原语已归位）。
- 门禁：
  - GATE-RUN runId=5c8e06fb-a3d9-4170-8dc9-9cf4a30ffbb3 cmd=`tsc -b packages/skill-calorie --force` exit=0；GATE-RUN runId=c629781e-eb18-4723-a593-aed403f541cc cmd=`node --test t620` 11/11 PASS exit=0
  - 变异阈值1000：GATE-RUN runId=68c8b1ee-0da9-4c6d-a0c0-2479474aa4e6（编译 exit=0）→ GATE-RUN runId=6743cea7-60dc-4cf0-ad02-66946d6b2f5c（断言 exit=1）必红；逐文件改回后 GATE-RUN runId=d7916a57-44e7-4e58-90e1-5982add33a48（编译 exit=0）→ GATE-RUN runId=d2359a48-beb2-4cea-902e-7f2111b1aa69（`t620+384+386+t573` 33/33 PASS exit=0）
  - 台账`RESULT: 91/91 PASS`（无需`--sync`）；`gen-cli --check` GEN-CHECK PASS（128命令）
- 未做：31页重出与三门留多增量合并一次。

## §8 增量3a：口径层去重（已做完，未重出产物）

- 改动（同一事实只留一个家）：
  - TDEE（25）：`sec-caliber计算口径` kvTable 删通用定义两行（`公式`／`缺口口径`，`Mifflin-St Jeor` 之名由口径行第一段承担，384 钉住它），表里只留带本页取值的五行；底座口径行三段不动（span 结构与 J2/J9 恒出不断）。
  - 趋势（30）：`sec-caliber口径说明` kvTable 删与口径行整段重复的两行（`前段与后段`／`上升与下降`），留独有的 `拐点`／`评分口径` 两行；口径行不动。
  - 对比（31）：底座口径行删与 kvTable `对比期怎么取`／`变化量 Δ` 重复的第一条（D-13 的另一半：kvTable `日均总消耗怎么算` 行一并删除），保留 `日均总消耗两期同源` 这一条并拆成两段（同一事实不断成两处，只为落 span 分段，J1 影子判据要求）。
- 教训（变异自证之外，真回归红过两次才定形）：① 单段口径行不产 span（shape-520 ④后半）；② `Mifflin-St Jeor` 之名只许住一处且必须在（384 针住它）。两次都是冻结测试抓住的，不是复核复跑。
- 门禁：
  - GATE-RUN runId=5d315ac8-9ab0-4f66-86f7-fb56c281511f cmd=`tsc -b packages/skill-calorie --force` exit=0；GATE-RUN runId=746977f0-a34c-41f6-a869-d632eb19d82f cmd=`node --test t620` 14/14 PASS exit=0（初版 3a 断言）
  - 变异口径行加回重复段：GATE-RUN runId=2fa2ef31-b1ba-4983-afeb-79cb2653eb1b（编译 exit=0）→ GATE-RUN runId=59c6df37-ff5e-4f36-908a-2d12587b0342（断言 exit=1）必红；逐文件改回
  - 真红一定形：首版单段 tdee 口径行断 span（shape-520 ④）、`Mifflin-St Jeor` 归零（384）——改走“口径行不动、删表里通用行”后全批 `t620+384+shape-520+t572+t573` 59/59 PASS exit=0（GATE-RUN runId=4786c64e-a917-4e78-89ad-8b97ee5e1309）；定形后源码未再动，重编 GATE-RUN runId=845a37d9-cc8b-44ee-bdff-04cf3087ff47 exit=0
  - 台账`RESULT: 91/91 PASS`（四件远在350线内，无需`--sync`）；`gen-cli --check` GEN-CHECK PASS（128命令）
- 未做：31页重出与三门留多增量合并一次。

## §9 增量3b：卡面徽标去重（已做完，未重出产物）

- 改动（说明不重复徽标读数；读数槽 vs 状态槽同源保留记 S3）：
  - TDEE（25-2）：静态缺口卡说明去 `（在缺口）` 三字（与同卡徽标逐字重复），徽标 `在缺口` 不动。
  - BMR（26-1/26-2）：概览删整行徽标列（首枚与卡徽标逐字相同；次枚 `告警线 3 天` 与表题／口径表行三写规则）；危险信号表题去 `（3 天及以上即告警）`（判据细则只住口径表，R-43），规则单家住口径表行。
  - 评分（29-D11）：最低分项卡说明改 `优先改它`、最高分项改 `继续保持`（原 `命中率 N%` 与徽标逐字重复）；徽标与值位读数不动（R-42 领域词＋百分数）。
- S3（记账，不拦关票；冻结／设计优先，仿 #571 R-28 口径）：
  - S3-1（27/28 D-10）：达标天数卡值与徽标行数字同源——卡片侧 R-45 钉死四卡四值、徽标侧 shape-520 ③钉死 `达标/未达标 N 天`，两边都是冻结，锁现况（读数槽 vs 状态槽，不同槽）。
  - S3-2（29/31 值↔徽标）：读数槽（卡值位）与状态槽（徽标）同源是 R-42 明写设计（领域词＋读数），改徽标即改设计，另开设计票，本轮只清说明 prose。
- 门禁：
  - GATE-RUN runId=49ca7165-eecd-464a-99b2-8396861bc2ca cmd=`tsc -b packages/skill-calorie --force` exit=0
  - GATE-RUN runId=fb8ebd92-5f90-40ef-86a3-8683ab6602ec cmd=`node --test t620+384+shape-520+t572+t573+466` 70/70 PASS exit=0（`t620` 17/17 含新增 3b 三条）
  - 变异最低分项卡说明改回含率版 → `最低分项卡仍在重复徽标读数` exit=1 必红；逐文件改回 → 复绿
  - 共享树插曲：变异轮编译曾报 `src/goal/routes.ts` 类型错（别席在途四件 `goal/{commands,receipt,routes,write}.ts` 未提交改动，非本票写集；本票文件零错误；该错随后由别席消除，终验编译 exit=0）。台账 `--sync`（sportPortDocs 960→966，别席脏件 transient；GATE-RUN runId=b0e10ad8-1a94-4c35-8799-1c522ec3a1d5）后 `RESULT: 91/91 PASS`；`gen-cli --check` GEN-CHECK PASS（128命令）
- 未做：31页重出与三门留多增量合并一次。

## §10 增量4：图表量程（待）

## §10 交付对账（实际碰到的目录，逐行与§2对）

待收口时补：全部 commit sha + 提交信息清单；`git diff --cached --name-only`复核只含自己声明件。
