# #318 对抗式复核报告（复核席 `#318r`）

- **被审**：落点 `78472a3`（`feat(318): 场景 06 目标管理／07 基础信息命令搬进能力目录（核验与补齐 · 窗口 A）`）
  ＋ 导出 `e298e1f`（`docs(318): 补齐 §2.4.4 对账导出（窗口退出之后；runId 4099491b）`）
- **票**：`#318`（`## 进度：95%`、`OPEN` —— 复核时线上状态 `2026-09-13T18:00:23Z` 更新，未 CLOSED）
- **复核基线**：`BE=434ec38`（本票落点的父提交）／复核开工 `HEAD=e298e1f`／复核收尾 `HEAD=4023da6`（见 §七 环境变动）
- **本席证据脚本**：`.scratch/t318r/verify-*.mjs`／`mutate*.mjs`／`run-gates.ps1`；日志 `.scratch/t318r/logs/`
- **纪律自证**：本席只写 `.scratch/t318r/**`；主树交付件零改动（`git diff --name-only` 空、`git status --porcelain` 空）；
  变异全部在 `git archive` 派生的隔离副本里做（`.scratch/t318r/iso/tree`）；一切构建／测试经
  `node tooling/run-locked.mjs --ticket 318r --lock-dir .scratch/locks-land -- <cmd>`；未用任何禁列命令。

**判定：PASS ／ 82 分（S1=0、S2=0、S3=3）**（分数构成与门槛口径见 §六；本席唯一实质发现是一处**数字与当刻事实不符**的证据缺陷，单独列在 §三/§六）

---

## 一 · 声明面（票面 18 条 · 键名／字段／镜像 · 全仓唯一性）

**结论：18 条齐、键名与票面一致、字段齐（`wakeWord` 可缺一处按裁决省略）、全仓每个 key 恰一处、镜像三面一致。**

| 判据 | 我的做法（不看被审文本） | 机器结果 |
| --- | --- | --- |
| 18 条声明 | 只认「同一行有 `kind:`＋`run:`＋`key: '…'`」的**声明记录行**，按 `git ls-files` 全仓扫 | 命中键 **100** 个、重复键 **0**；18 键各 **×1**，落点全部在 `goal/commands.ts`（13）／`profile/commands.ts`（5） |
| 负向对照（规则 28） | 把行内 `run:` 破坏后重扫 | 命中键 **0** ⇒ 判据依赖记录形状、非恒绿 |
| 键名与票面一致 | 逐键与票面「场景 06／07 目标键集」比对（`calorie.goal.pause/resume/set/water/weight`＋`calorie.view.goal*` 8 条＋`calorie.profile.activity/set/update`＋`calorie.view.profile*` 2 条） | 一致 |
| `keys.ts` | 18 键各恰一行（写键入写表／读键入读表） | 异常 **0** |
| `combos.yaml` | `- key: <k>` 逐键恰一处 | 异常 **0**（`▶ 我的第一版计数用宽松正则命中 2 处，改精确前缀后 0`） |
| `registry.ts` | `registry.ts` 登记 goal／profile 两个能力 | 见 §二/§五 的 4 行 diff |
| 字段齐 | 声明行逐条与票面六字段（kind／key／shape／title／wakeWord／example）比 | 全齐；`calorie.view.profile` 按票 **#323**（`wakeWord` 改可缺）**照原样省略**，与 `profile/commands.ts:6-9` 自述一致 |

**证据命令**：`node .scratch/t318r/verify-decl.mjs`（含负向对照）、`verify-mirror.mjs`、`verify-evidence-facts.mjs`。

---

## 二 · 分派面（18 键唯一实装 · 老 `case` 净 · 两个 legacy 分片在且零残留）

**结论：18 键在 `src/goal/`／`src/profile/` 唯一实装；分派层本票 `case` 全清；两张命令分片仍在且记录归零；路由分片仍在。**
**注意（不整篇 `includes()`）**：判「键是否还在」只看**记录行**。

| 判据 | 基线 `434ec38` | 当刻 `HEAD` | 结论 |
| --- | --- | --- | --- |
| `cmd_read.ts` 的 `case 'calorie.…'` 标签 | 11（其中本票 10） | 1（本票 **0**） | 删 10 条、**全部属本票键**；被删标签逐条列出（`verify-dispatch.mjs`） |
| `write.ts` 的 `case 'calorie.…'` 标签 | 11（其中本票 11） | 3（本票 **3**） | 删 8 条、全部属本票键；残留 3 条在**另一个** switch（`profileReceiptDoc`：按命令名认领整页回执，`set/activity/update`），**必须留** |
| 分派净度 | — | `dispatchInner` 已退化为 `default: fail(3,…)` | 三层都落地，键无重复实现 |
| `legacy/scene-06.ts`／`scene-07.ts` | 13＋5 条记录 | 文件在、导出常量在，**含 key 的记录行 0** | 空数组，形状如票面 |
| `legacy/routes/scene-06.ts`／`scene-07.ts` | 22＋6 条记录 | 1＋0 条记录（留下的那条 `key=calorie.view.goal-weight` 属**别票**） | 分片未删、未越界清空 |

**证据命令**：`node .scratch/t318r/verify-dispatch.mjs --base 434ec38`。

---

## 三 · 跨片删除的正确性（逐行核删 · 多重集守恒）

**结论：删的 28 条**全部**属本票键；无别票记录被连带删；路由记录多重集守恒；其余 9 张分片零改动。**

做法：用 `git diff -U0 434ec38 HEAD -- <两片>` 取删除行，再拿**新家两个 `routes.ts` 的全部记录行**做双向集合比对（内容定位，不用行号快照；本票曾踩过「用预备期快照派生」的坑 ⇒ 我只按当刻树内容判）。

```
删除行数=28（记录 28／非记录 0）        新增行数=0
两个新 routes 文件的记录总数=28
A) 删除记录中 key 不属本票 18 键的条数 = 0          ← 8 个键 × 若干记录：goal.set/water/weight/pause/resume/vs-actual/expiring/goal/goal-config/goal-status/goal-predict/goal-wizard/recommend, profile.set/activity/update/view.profile/view.profile-wizard
B) 老片里留下的记录：scene-06=1 条（calorie.view.goal-weight）；scene-07=0 条；留下但属本票 = 0
C) 删除记录在新 routes 里找不到同文本的 = 0
D) 新 routes 里有、删除集里没有的 = 0
```

| 守恒/无损判据 | 结果 |
| --- | --- |
| 全仓 routes 记录总数 | 基线 57 → 当刻 29（legacy）＋28（新家）＝ **57**，净 0 |
| 键多重集漂移键数 | **0**（逐键计数逐字相同） |
| 其余 9 张 routes 分片 | `git diff --numstat` 全部 **clean** |
| 冻结地基 | `src/triggers/**` 与 `test/calorie-routing-81.test.mjs` 对基线 **零改动**（`git diff --stat` 空） |
| 声明逐字照抄 | 老分片删除的 18 条声明行 ≡ 新能力目录 18 条（剥掉 `run:` 指针后**逐字全等**）；负向对照改一字后判据报「缺 1 多 1」 |

**证据命令**：`node .scratch/t318r/verify-routes.mjs --base 434ec38`、`verify-conserve.mjs`、`verify-copy.mjs`。

---

## 四 · 行为不变（四门＋判官＋幂等＋§3.61 源码级实证）

全部在**同一把锁**（`--lock-dir .scratch/locks-land`）内直调 node（规则 26），`runId=f2113160-6082-42fc-b7b2-7605b17d50d8`，原始日志 `.scratch/t318r/logs/`。

| 门 | 命令 | 结果 |
| --- | --- | --- |
| 基线绿 | `node node_modules/typescript/bin/tsc -b` ＋ `gen-cli.mjs --stamp` | exit **0**（不带 `--force`） |
| 全量套件 | `node --test`（`package.json` 现读的 15 个 glob） | `ℹ tests 1492 / pass 1492 / fail 0`（exit **0**，耗时 38.7s） |
| 判官 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`、**`UNACCOUNTED=0 处`**（exit 0） |
| 快照 | `node tooling/skill-html-snapshot.mjs --check` | `RESULT: artifacts=186 changed=0 added=0 removed=0` |
| 幂等 | `gen` 连跑两次，5 件生成物 sha256 逐件比对 | 两次**逐字节相同**（`keys.ts=4db43569…`／`registry.ts=5c81bfbd…`／`combos.yaml=ca17d2c3…`／`build-help.mjs=4d44f03f…`／`routes.generated.ts=e9ea9935…`） |
| `gen:check` | `gen-cli.mjs --check` | exit **0**（`GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 10 个…；未搬迁清单 1 条`） |
| 测试面实跑 | 全量日志中与本票 18 键直接相关的用例 | 全绿：`#41 M3：真 CLI 串行冒烟 18 新键（exit 0 + envelope stat + --html 含 ilife-page）`、`T9 目标分析盘 parity：12 键抽查`、`#97 M5 四要素` × 8 键、`落库 · 档案 set/activity/update`、`落库 · 目标 set/water/weight/pause/resume` 等 |

**「纯搬迁、行为不变」的源码级实证（本席自设，被审脚本没有这一层）**：把基线 `case` 块体与新函数体都剥签名、剥注释、归一化空白后**逐行比对**：

```
读链 10/10 OK（语句逐行相同）  写链 6/6 OK（语句逐行相同）
profile.set/update：基线共用 case 块 25 行 vs 新 profileUpsert 24 行
  ⇒ 唯一差异：「const isSet = key === 'calorie.profile.set';」提为形参 ⇒ 语义等价
辅助件 profileValue／profileDiffItems 与基线同名内联件同体
RESULT: ok=18 bad=0
```

**证据命令**：`node .scratch/t318r/verify-body.mjs`。

---

## 五 · 免费判据／判据④（生成物面与「允许面放宽」）

**结论：允许变更的生成物面只有 `registry.ts`（4 行）；放宽到的 5 个新件全是本票车道；未见夹带。**

| 生成物面 | 基线→当刻 | 依据 |
| --- | --- | --- |
| `src/cli/registry.ts` | **+4 −0**（2 个 import ＋ SOURCES 2 行） | `git diff 434ec38 HEAD -- …/registry.ts` |
| `src/cli/keys.ts` | **逐字节不动** | `git diff --numstat` 空 |
| `base-combos/combos.yaml` | 逐字节不动 | 同上（本票既没加也没删命令 ⇒ REPEAT 段与键表都不变） |
| `scripts/build-help.mjs` | 逐字节不动 | 同上（未跑 `help:build`，按裁决） |
| `src/triggers/routes.generated.ts` | 逐字节不动 | 同上（记录只换住处、`order` 原值 ⇒ 汇总位文本不变） |
| `SKILL.md`／`package.json` | 逐字节不动 | 同上 |

**「放宽的允许面」有没有把真越界放过去**：本票实际新增 10 件（`goal/`5＋`profile/`5），其余 8 个改动件里 7 件是「删自己那几行」＋1 件是生成物；逐件核对后**没有**发现碰到别票车道（`render/`／`fetch/`／`weight/`／别的场景分片全部零改动）。**反向的证据**：`render/index.ts:37-38` 仍直接转出 `../profile/view.js`（越界），本票**如实记账、未动**——这正是「不许夹带」的正确做法，我核了这两行当刻确实还是原样。

**一处我自己的假说被证伪（记账）**：我先假设「删一条路由记录 ⇒ 生成器 `order` 报错」，实测**两条都不成立**——
① 只删源、`dist` 未重编时，`--stamp` 之后 `gen:check` 仍 **exit 0**（生成器读的是 `dist` 的编译件，陈旧 dist 把它挡住了）；
② 把源与 `dist` 同步（真编后打印记）再跑，门**确实报红**：`Error: list=wake 的 order 不连续：第 213 位实得 214（…/src/goal/routes.ts · "定饮水目标(自动算)"）——必须是 0..434 连续无洞`。
→ 结论：**门的射程＝「声明与生成物／dist 不一致」**，本票证据 §「门的射程」那一条与实测一致；但「路由记录被无声抹掉」这一面**没有门**（`gen:check` 只问「regenerating 现在的声明是否等于盘上产物」，不问「该有的记录是否都还在」）。这一条记为 **S3-3**，建议转 `#325`（`gen:check` 假绿缺口票）一并评估。

---

## 六 · 证据面（逐条对账 · 独立复算）

| 判据 | 我的实测 | 结论 |
| --- | --- | --- |
| ① 路径／行数／数字与当刻事实一致 | **不全部一致**（见下表） | **S3-1** |
| ② `GATE-RUN` 行止于右引号 | 证据 `:189` 逐字 = `- GATE-RUN runId=4099491b-da8e-43ef-99bc-82bc2f516b49 cmd=pwsh -NoProfile -File .scratch/t318/run-window.ps1`，**cmd 值无引号、行末即 `run-window.ps1`** | 合规（导出件里该条 cmd 用双引号包裹并止于右引号） |
| ③ 导出窗口覆盖证据声明的全部认领运行 | 导出 `t318-gate-runs.log`：`RUN ticket=318-land runId=4099491b… waitedMs=0 exit=0 at=2026-09-13T18:00:25.511Z`；我按同 filter（`ticket=318-land`、`since=17:58:00.390Z`）独立扫 `.scratch/locks-land/gate-runs.log`，窗口内命中**恰 1 条**、与导出逐字相同 | 覆盖完整（该 ticket 另有 39 条**早于该窗**的载体运行，按 §2.4.4 的 `--since` 口径不在窗口内，导出件表头已写明窗口与条目数） |
| ④ `check-gate-audit` PASS、`--allow-*` 配对 `GATE-RELAX` | `node tooling/check-gate-audit.mjs --evidence docs/…/t318-核验补齐-证据.md --log docs/…/t318-gate-runs.log --ticket 318-land --since 2026-09-13T17:58:00.390Z` ⇒ `RESULT: matched=1/1 auditEntries=1 scoped=1 undeclared=0`、`gate-audit: PASS`、**exit 0**；证据里 `GATE-RELAX` 出现 **0** 次 ⇒ 无需配对 | 合规 |
| ⑤ 四条记账真在 | `门的射程＝不一致`（`:258`）✓／`读红先认 runId＋日志名`（`:264`）✓／`mtime 治本`（`:268`）✓／`dry 与真跑同树状态`（`:274`）✓ | 四条齐、且四条都在 `78472a3` 的 blob 里（`e298e1f..HEAD` 对该件零改动） |

### §六.1 数字与事实不符的**逐条**清单（本席唯一实质发现）

| 被审证据的说法 | 当刻/基线事实（我的复算） | 差值 |
| --- | --- | --- |
| §五.1：`cmd_read.ts` **1146 → 1026 行** | 基线 `434ec38` = **474** 行；当刻 = **351** 行（`git show`＋`ReadAllLines` 双向核） | 与任一版本都不符；实际 −123 |
| §五.1：`write.ts` **745 → 622 行** | 基线 = **223** 行；当刻 = **95** 行 | 同上；实际 −128 |
| §五.1：`case 'calorie.` **61 → 51** | 基线 = **11** 个标签（本票 10）；当刻 = **1** | 61/51 在任何版本都不存在 |
| §五.1：`write.ts` **34 → 26** | 基线 = **11** 个（本票 11）；当刻 = **3**（另有 8 条被删） | 34/26 在任何版本都不存在 |
| §五.1：`surgeon.mjs --selftest --from-head`「断言全过」 | 该自证是拿**预备期**数字跑的（同一批陈旧数），**对当刻树的 18 个 case 不构成判据** | 判据面错位（不是假绿，但也证明不了当刻） |
| §七「生成物逐件 sha256：`keys.ts` unchanged／`combos.yaml` unchanged／`build-help.mjs` unchanged／`routes.generated.ts` unchanged／`registry.ts` CHANGED」 | **逐项复算全部一致**（我用 `git diff --numstat 434ec38 HEAD` 与 `git diff 434ec38 e298e1f` 双向核） | 一致 ✓ |
| 窗口自报「证据档：…（**232 行**；GATE-RUN 声明 1 条…）」 | 提交件实为 **276 行／28167 B**（`78472a3:…证据.md` 与工作区逐字节相同） | 报数与成品不符（成因：`land.ps1:566` 用 `Add-Content` 追加了「墙钟耦合」段 ⇒ 报数发生在追加之前；同时把该件行尾变成 **CRLF**，整篇其余部分的原稿是 LF） |

**这一条为什么重要**：`§五.1` 是「手术机器自证」那一节的唯一量化支撑，而它的数字**指向的不是这一次的树**。18 键搬迁与分派净度的**实质结论**我独立复核后仍然成立（§一～§三），但**这一节的读数不能用**。按复核纪律，我把它记为必须整改项（**S3-1**，理由：判据归属正确但数字错，且错得偏「好看」）。

**其余证据面复核**：`t318-核验补齐-证据.md`／`t318-gate-runs.log` 均**无 BOM**、控制符 **0**（除 `\t\n\r` 外无命中，规则 29）；§一 的行数表（`set.ts`192／`precheck.ts`211／`setup.ts`428／`update.ts`90／`view.ts`265／`labels.ts`59）、§一.1 的 `goal/routes.ts` 22 条＋`profile/routes.ts` 6 条、`index.ts` 3 件／5 件、§一.2 的「改前 `scene-06.ts:12-24` 13 条／`scene-07.ts:12-16` 5 条」**逐条复算全部一致**。

---

## 七 · 变异自证（隔离副本 · 直调 node · 每发「改红→复正绿」双证）

隔离方式：`git archive HEAD` 派生到 `.scratch/t318r/iso/tree`，仓内 `node_modules` 与包内 workspace 链接按目录实拷贝；**主树零变异**。

| 变异 | 我做的动作 | 期望 | 实测 |
| --- | --- | --- | --- |
| 基线可用性 | 副本上直接 `gen:check` | 绿 | exit **0** |
| **M2**（被审声明的那发） | 只改声明 `calorie.goal.water → …waterMUT`、故意不重生成 | `gen:check` 必红 | exit **1**，`GEN-STALE FAIL：dist/ 不是这些声明现在的内容的产物…`；复正 exit **0**、sha 复原 `true` |
| **M2-b**（被审声明的第二发） | 只手改镜像 `registry.ts`（尾部加一行注释） | 同门必红 | exit **1**，`GEN-CHECK FAIL … registry.ts 与生成器输出不一致（第 51 行起）`；复正 exit **0**、sha 复原 `true` |
| **M3**（本席自造） | 删一条路由记录 | 门应有反应 | **证伪了我原先的期望**：只删源、dist 未重编 ⇒ `gen:check` 仍 exit 0；把源与 dist 同步后 ⇒ exit **1**、`list=wake 的 order 不连续：第 213 位实得 214` |
| **M4**（本席自造） | 删一条**声明行**（`calorie.view.goal-status`） | `gen:check` 必非 0 | exit **1**（`GEN-STALE`／键数变化）；复正 exit 0、sha 复原 `true` |
| **M5**（反假绿攻击一） | 改源码**内容**而把 mtime 用 `utimesSync` 钉回旧值 | 门会不会被 mtime 骗过 | `gen:check` 仍 exit **1** ⇒ **内容判据不看时间戳**（`mtime` 前后逐字相同 `1789323185693`），复正 exit 0 |

**门的鉴别力总结**：内容印记那一层是**内容判据**（不看时间戳），一致改名不触发、不一致必触发；`order` 守卫在「源与 dist 同步」时会报「list 内 order 有空洞」。**改红→复正绿**在每一发上都拿到了（原始输出逐发落盘 `.scratch/t318r/logs/m*.log`）。

---

## 八 · 反假绿（主动攻两条）

1. **`Copy-Item` 保 mtime ⇒ `tsc -b` 跳过（规则 22 同族）**：我在隔离副本上实测「内容已改、mtime 保持不变」时 `gen:check` **仍报红**（M5）⇒ 本 repo 的**生成物门不吃 mtime**。至于 `tsc -b` 本身：本窗 `land.ps1` 执行路径里逐件刷了 mtime（证据 §「教训②」已自陈**文件级已刷、目录级未刷**），我在窗口日志里也见到 `tsc -b` 退出 0 后 `dist` 时间戳随写盘更新；本席**没有**在仓根做复制／强编实验（会动共享 `dist`），故只报「文件级已刷、目录级未刷」这一条**既有记账照旧成立**。
2. **被 ignore 的 `dist/`／印记被残留污染**：本席主动核了两点——① `packages/skill-calorie/dist/.gen-inputs.json` 记的声明源 **41 件**且含 `goal/commands.ts`、`profile/commands.ts`、`goal/routes.ts`、`profile/routes.ts`，`goal/commands.ts` 的印记 hash 与当刻源文件 sha256 **逐字相同**；② 我在隔离副本上把**声明改了却不重编**时，门只靠印记报红（`GEN-STALE`）——反过来说明：**只要印记与源同步、而 dist 陈旧，门就查不出**（这正是上表 M3 的实测）。当前主树的印记与源一致，故本轮读数成立。
3. **`git status --porcelain` 含已暂存（规则 36）**：我收尾用三条并列判——`git status --porcelain -uall`（**空**）、`git diff --name-only`（**空**）、`git diff --cached --name-only`（**空**）。
4. **墙钟耦合的票外红**：证据把 `cmd-read-t11` 的 `total >= 2` 红记为「票外既有、已由 #326 处置」。我在 `00:09Z` 前后的锁内全量跑**一次即 1492/1492**，与该处置结论一致（本席不背这条、也不把它当本票判据）。
5. **环境变动记账（本席开工后发生）**：复核进行中，另一席把 `docs/skills/skill-calorie/t293-验收-命令自治.mjs`（＋`t293-探针精度-证据.md`＋`t293-纪律豁免.json`）**提交**为 `4023da6`，主树由「该件未提交修改」变为**干净**。我在**当刻 HEAD（探针 blob `8c33e6780b`）**上跑的判官结果＝`5/5`、`UNACCOUNTED=0`（与窗口里跑的旧 blob `ec7c24498f` 结论一致）；**本票 18 件**与 `78472a3` 逐件比对 **零差异**。这不影响本报告任何结论。

---

## 九 · 三级问题清单

### S1（关闭阻塞项）：**0 条**

### S2（关闭前必须修完）：**0 条**

### S3（记账跟进，不阻塞关闭）：**3 条**

| # | 归属 | 问题 | 建议处置 |
| --- | --- | --- | --- |
| S3-1 | **本票**（证据件，`docs/skills/skill-calorie/t318-核验补齐-证据.md` §五.1＋窗口自报行数） | 4 处行数／标签数与**任何版本**都不符（1146→1026／745→622／61→51／34→26），且窗口自报「232 行」而成品 276 行；该节是「手术机器自证」的唯一量化支撑 | 收口批量里**改数字＋改判据归属**：改为「自证脚本的数字取自预备期，当刻树的事实是 `cmd_read.ts 474→351`（10 个本票 case）、`write.ts 223→95`（8＋3）」，或直接删掉这一节的陈旧数字、改挂 `.scratch/t318r/verify-dispatch.mjs` 这一类当刻判据 |
| S3-2 | 票外（另一席在飞） | `t293` 判官在复核期间被改并提交（`4023da6`），窗口 A 的证据引用的是旧 blob（`ec7c24498f`） | 已在 §八.5 记账；收口时把「判官改动由独立席复核」的结论与本票证据的引用对齐 |
| S3-3 | 范围外发现（工具面） | `gen:check` 的判据面＝「regenerating 现声明是否等于盘上产物」，**不覆盖**「该有的路由记录是否都在」（删一条记录、源与 dist 不同步时**无门报红**） | 转 `#325`（`gen:check` 假绿缺口票）评估加一条「声明记录数／`order` 全集守恒」断言；不建议本票改 |

---

## 十 · 判定与分数

- **判定：PASS**（S1=0、S2=0）
- **分数：82 / 100** —— 分维：契约一致 **28/30**（18 条声明／键名／字段齐、镜像三面一致、路由多重集守恒，扣分只在「未逐键比对 `data` 字段是否逐字相同」）；证据真实可复现 **12/25**（门禁面全部可复现、`check-gate-audit` PASS、导出一对一；但 §五.1 的量化数字与事实不符 ⇒ 重扣）；parity **20/20**（1492/1492、判官 5/5 且 `UNACCOUNTED=0`、快照 `changed=0`、`gen` 幂等、**18/18 源码级语句逐行相同**）；工程红线 **15/15**（未删 `dist`／未仓根 `--force`／未动冻结地基／未夹带别票车道／未改票面）；文档同步 **7/10**（证据件存在且被跟踪、受跟踪对账源在；扣 S3-1）。
- **门槛口径**：本报告的分维与门槛（S1=0 且 S2=0）来自派单；若改按协议 §5.1「均分 ≥85 ⇒ PASS」的字面口径，82 分本身不足以 PASS——**该条不影响本席判定**（S1/S2 皆 0），但请编排者在收口时择一执行：**要么按本报告的 82 分先做一次证据返修再复核提分，要么把分维口径与门槛的关系写清**。
- **必须整改 ≤3 项**：① S3-1 证据数字与判据归属（本席唯一实质发现）；② S3-3 转 `#325` 记账；③ S3-2 与「判官独立复核」结论对齐。
- **未完成的动态项**：本席**未**做主树 `dist` 层的复制／强编实验（红线：会动共享 `dist`）；**未**做跨版本 CLI 逐字节输出对比（需重建 `dist`，且会与并发的别的席冲突；我改用「18/18 源码级逐行相同＋1492 全绿（含 18 新键真 CLI 冒烟、8 键 M5 四要素、profiles/goals 落库逐列回读）」替代）。

---

## 附 · 证据索引（全部可复跑）

| 件 | 位置 | 用途 |
| --- | --- | --- |
| 声明唯一性＋负向对照 | `.scratch/t318r/verify-decl.mjs` | §一 |
| 镜像面（keys／combos／registry＋生成物 diff） | `.scratch/t318r/verify-mirror.mjs`、`verify-unique.mjs` | §一／§五 |
| 分派净度（case 标签差集） | `.scratch/t318r/verify-dispatch.mjs` | §二 |
| 路由搬迁逐条核对＋守恒 | `.scratch/t318r/verify-routes.mjs`、`verify-conserve.mjs` | §三 |
| 声明逐字照抄 | `.scratch/t318r/verify-copy.mjs` | §三 |
| 源码级行为不变（18/18） | `.scratch/t318r/verify-body.mjs` | §四 |
| 证据面逐条对账（含控制符自检） | `.scratch/t318r/verify-evidence-facts.mjs` | §六 |
| 门禁链（锁内） | `.scratch/t318r/run-gates.ps1` ＋ `logs/g1..g7*.log` ＋ `logs/gates.log` | §四 |
| 变异电池（含证伪记录） | `.scratch/t318r/mutate2.mjs`、`mutate3.mjs`、`mutate-m6.mjs` ＋ `logs/m*.log` | §七 |
| 收尾核树／进程 | `.scratch/t318r/verify-tree.mjs` | §八.3 |

**对账声明**：本席所有 `node --test`／`tsc -b`／`pnpm` 等价命令均经
`node tooling/run-locked.mjs --ticket 318r --lock-dir .scratch/locks-land --stale-minutes 30 -- …` 执行
（`runId=f2113160-6082-42fc-b7b2-7605b17d50d8`，`waitedMs=1`，`exit=0`）；导出件 `.scratch/t318r/*-gate-runs.log` 属**本席票号 318r**，勿并入本票的 `318-land` 导出件。
