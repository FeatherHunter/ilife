# #268 验收记录 · 卡路里「运动」（场景 04 · 39 条唤醒词）

> **本件是备料，不是判定。** 第一节与第三节是**机器侧读数**，第二节是用户原话区（**留空，待用户填**）。
> 「运动这一栏都能跑通」这句话由**用户原话**认定。
>
> 备料日期：2026-09-15　｜　票面：[`#268`](https://github.com/FeatherHunter/ilife/issues/268)（地图 #156 的子票）
> 逐条清单（唤醒词／该跑的命令／产物绝对路径／操作步骤）见同目录 `t268-用户验收页.html`（双击打开）。

---

## 一 · 机器侧备料读数

### 1.1 真跑两轮

命令文本逐字取自冻结表 `packages/skill-calorie/src/triggers/scene-04-exercise.ts` 的 `main_prompt.cli`（39 条）。
占位符按 2026-09-15 当天代实：`<日期>`→`2026-09-12`、`<开始日期>`→`2026-02-27`、`<结束日期>`→`2026-09-15`；
`CALORIE_TODAY=2026-09-15` 把「今天」钉死；每条词**各跑一次、各用一份独立库副本**；产物落 `.scratch/t268/out/`。

| 轮次 | 种子 | 条数 | 成功 | 阻断 | 完整文档 | 字节域 |
|---|---|---|---|---|---|---|
| **甲 · 实况** | 真库的字节拷贝（一个字不加） | 39 | **14** | **25** | 14 | 68757–118556 |
| **乙 · 可看** | 真库拷贝 ＋ 当日运动记录与运动目标（**只写临时副本**） | 39 | **39** | **0** | 39 | 65205–121231 |

- 甲轮产物（可开）：`D:\ilife\.scratch\t268\out\甲-实况` —— 14 件 `.html`
- 乙轮产物（可开）：`D:\ilife\.scratch\t268\out\乙-可看` —— 39 件 `.html`
- 交货目录里**只有 `.html`**（零其它文件）；逐条读数在 `.scratch/t268/run39.json`，跑动日志 `.scratch/t268/run39.log`。

### 1.2 甲轮 25 条阻断的成因（照实记账，未伪造产物）

| 条数 | 原话（归一化后） |
|---|---|
| 1 | `ERR N: 取数失败（缺失阻断）：昨日无运动记录可复制` |
| 2 | `ERR N: 取数失败：记录 ID N 不存在` |
| 2 | `ERR N: 取数失败（缺失阻断）：无运动记录（<日期>）` |
| 1 | `ERR N: 取数失败（缺失阻断）：无运动记录（<日期>~<日期>）` |
| 17 | `ERR N: 取数失败（缺失阻断）：无运动记录：<日期> ~ <日期>` |
| 2 | `ERR N: 取数失败（缺失阻断）：未设运动目标（daily_goal.exercise_goal 缺失，先维护运动目标）` |

根因四项，全部读自真库实数（真库全程只读）：

1. **记录窗空**（17 条「无运动记录：…」＋ 2 条「无运动记录（&lt;日期&gt;）」＋ 0 条「无运动记录（&lt;日期&gt;~&lt;日期&gt;）」）：
   真库 `exercise_log` 最后一条记在 `2026-07-31`，而「今日／昨日／本周／上周／本月／上月／最近 7／30 天」这些窗口都落在这之后 ⇒ 窗口里 0 行，命令按设计**拒答**并给缺失阻断。
2. **id 不存在**（2 条）：冻结表里「改运动记录」「删运动记录」的示例参数写的是 `"id":1`，而真库 `exercise_log` 的 id 域是 `3…8305`（8297 行）——`id=1` 从来没有过。
3. **没设运动目标**（2 条）：「看今日运动（vs 目标）」「看本周运动（vs 目标）」要读 `daily_goal.exercise_goal`，真库里这一格是**空值**（`null`）。
4. **当日无记录可复制**（1 条）：「复制昨日运动」要昨天有记录，昨天没有。

**要让这 25 条也能念通，最小前置**（都由用户在会话里念词完成，不需改任何文件）：
① 先念一次「记运动」把当天记上；② 设一个每日运动消耗目标。

### 1.3 乙轮两处参数代实（记账）

- 冻结表里 `"id":1` 是**示例参数**；乙轮把它绑到副本里真实存在的一条记录（`id:8344`），故「改运动记录」「删运动记录」两条在乙轮也 exit 0。
- `<日期>`／`<开始日期>`／`<结束日期>` 三处占位按上表代实；用户念词时由会话按当天相对日算，不必手填。

### 1.4 装机确认（票面遗留出口）

判据：经每条安装链接读 `dist\cli\cmd_read.js` 算 sha256，与仓内那份逐字节比对。

| 安装位 | 链接类型 | 解析到的真目录 | 同源 |
|---|---|---|---|
| `~\.agents\skills\skill-calorie` | Junction | `D:\ilife\packages\skill-calorie` | 是 |
| `~\.dsh\profiles\web\node_modules\skill-calorie` | Junction | `…\.dsh-module-fallback\node_modules\skill-calorie` | 是（下一跳继续） |
| `…\.dsh-module-fallback\node_modules\skill-calorie` | Junction | `D:\ilife\packages\plugin-calorie\node_modules\skill-calorie` | 是（再下一跳继续） |
| `packages\plugin-calorie\node_modules\skill-calorie` | Junction | `D:\ilife\packages\skill-calorie` | **是（终点落回工作树）** |
| `D:\2Study\nodejs\node_modules\skill-calorie` | Junction | `D:\ilife\packages\skill-calorie` | 是（`calorie-cmd-read` shim 走这条） |

（profile 那两跳按既有形状经 `plugin-calorie\node_modules\skill-calorie` 中转，中转位本身也是 Junction、终点同一个目录；
`D:\2Study\nodejs` 下的 `calorie-cmd-read.cmd` 正文写的是 `%dp0%\node_modules\skill-calorie\dist\cli\cmd_read.js`，故 shim 也走同源的那份。）

- 构建：`node tooling/run-locked.mjs --ticket 268 -- pnpm build`
  → 锁等待读数 `WAIT-OWNER-ALIVE pid=6136 ticket=160 runId=286da3b8-9752-442e-bdfa-5767342563b0 ageMinutes=0.84（活进程一律不抢回，等它释放）`
  → `LOCK-ACQUIRED ticket=268 runId=ef804815-a6f8-4d1a-a85a-fabc74e28b16 waitedMs=10005`
  → `RESULT: ticket=268 runId=ef804815-a6f8-4d1a-a85a-fabc74e28b16 waitedMs=10005 exit=0`；日志 `.scratch/t268/build.log`。
  **在本票锁内跑，等 `#160` 的构建段释放后才进场；构建段 `exit=0`，无 `error TS`／`ERR_PNPM`／`ELIFECYCLE`。**
- `dist` 摘要（构建后）：`dist\cli\cmd_read.js` 8006 B、sha256 `833d103cfe5dbb1cc58478f7b1830da3323faf0fcfd96176d38187ae1557c6ce`；`dist` 顶层 38 件／`dist\cli` 25 件。

**唯一一处陈旧拷贝（不在验收链上，本席未改动它）**：`~\.claude\skills\skill-calorie` 是**真实目录、不是 Junction**，
内容是 2026-09-10 的旧快照（`SKILL.md` 30559 B／仓内已 55092 B；`dist\cli\cmd_read.js` 67432 B／仓内 8006 B，
sha256 不同）。它的唤醒词表**没有**本场景这批词；仓内 `tooling/`／`docs/` 里查无该位置的登记项。
⇒ **验收请从 DSH 面板这个会话念词**，不要从 `~\.claude\skills` 那侧起会话。

---

## 二 · 用户原话区（**待用户填**）

> 本席**没有**代写下文任何一句。请负责人照自己的话说。

### 2.1 判定原话（必填）

```
（待用户填）
```

### 2.2 念通了哪些（请逐条打勾或写词）

```
（待用户填）
```

### 2.3 没念通的／哪里不对（请写词 ＋ 你看到的原样）

```
（待用户填）
```

### 2.4 顺口说到的改进（**不占票**，照 `编排纪律` 写进文档）

```
（待用户填）
```

### 2.5 用户原话的采集读数

| 项 | 值 |
|---|---|
| 采集时间 | （待用户填） |
| 会话（哪次念词） | （待用户填） |
| 用户念词的原话（逐字） | （待用户填） |

---

## 三 · 真库只读读数（跑前／跑后对账）

真库：`D:\2Study\StudyNotes\.db\calorie_data.db`

| 项 | 跑前 | 跑后 | 同值 |
|---|---|---|---|
| 文件 sha256 | `57ce9d87f5f232822332d7057c2ae8411d99ba7fdecea289f2028e19fc3ffc15` | `57ce9d87f5f232822332d7057c2ae8411d99ba7fdecea289f2028e19fc3ffc15` | **是** |
| 文件字节 | 3072000 | 3072000 | 是 |
| 修改时间（mtime） | `2026-09-14T05:53:37.729Z` | `2026-09-14T05:53:37.729Z` | 是 |
| `PRAGMA integrity_check` | `{"integrity_check":"ok"}` | `{"integrity_check":"ok"}` | 是 |
| 表数 | 11 | 11 | 是 |
| 行数摘要 digest | `1cd4132be6168ef5` | `1cd4132be6168ef5` | 是 |

逐表行数（跑前＝跑后）：

| 表 | 行数 |
|---|---|
| `body_composition` | 0 |
| `body_measurements` | 1 |
| `body_photos` | 22 |
| `daily_goal` | 1 |
| `exercise_log` | 8297 |
| `food_log` | 562 |
| `nutrition_products` | 1931 |
| `user_profile` | 1 |
| `weight_log` | 129 |
| `workout_plan_config` | 1 |
| `workout_plans` | 100 |

**逐项比对：跑前跑后逐字相同 —— 备料过程（78 次命令跑动 ＋ 一次构建）没有写真库。**

跑前读数 `.scratch/t268/fp-before.log`／跑后读数 `.scratch/t268/fp-after.log`；
跑动期间读出的库内现状（解释阻断成因）在 `.scratch/t268/db-facts.log`。

---

## 四 · 本席未做（留给用户与原票）

1. **判定**：本件不写结论——「运动这一栏都能跑通」由用户原话认定。
2. **乙轮的当日记录与运动目标**：只补在**临时库副本**上；真库里仍没有当日运动记录、`exercise_goal` 仍是空值（是否补，由用户在会话里念词完成）。
3. **`~\.claude\skills\skill-calorie` 那份旧拷贝**：既存状态，本票只验收、未改任何 Junction 与任何安装位。
4. **票面「交货目录只放 `.html`」**：机器侧样例目录 `.scratch/t268/out/`（两轮子目录）内只有 `.html`，已在 1.1 记账。
