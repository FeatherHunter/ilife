# t456 围度键集对账 · 交叉二审（地图#158 · 简单票第二人）

## 1 判定：同意一审 PASS（96 分，无 S1、无过程违规；S3×1 维持 t440 基线转票）

- 票面五段重读结论：目标（两处 13 键字面表防“改一处漏另一处”静默漏列）／验收 4 条／不许动 3 条／交付物 3 路径／遗留出口 2 项，实现逐条可落位，见 §3。
- 五维打分：契约 26/30（验收 1–3 独立复现全绿；验收 4 之 t361 绿、t440 红系范围外基线红扣 4）／证据 25／对照 20／红线 15／文档 10，合计 96，与一审同分。
- S1 漏网 hunt（放宽／造假／越界／丢数据）：均无。一审未漏 S1，见 §3。
- t440 红不证伪本票：`actual=[["body/receipt.ts",2],["fetch/body.ts",13]]` 与一审同因，`receipt.ts:122-124` 的 `GROUP_HEADS` 系 #537（`d2c80a6`）已提交内容，本票 diff 四件之外；跨界停手正确（改组头即改回执行为，属越界），按 §5.1 只转票不判 FAIL。
- 自设新变异（与一审 M1/M2/reorder 均不同）：TOP 侧同数异键 `hip_cm→hip_typo_cm`（长度仍 13）必红且点名双侧差集，还原必绿——证明断言不是“只查长度”，补上了一审 M1（14 项）/M2（12 项）没覆盖的“同数改名”漏列形态。

## 2 机器证据（独立重跑＋新变异红绿两行，日志 `.scratch/t456/review2-*`，凭 `REVIEW2-MARKER` 复核）

- 基线（dist 级 transient 变异之外零写入，受跟踪源码零残留；`.scratch` 被忽略未入仓）：
  - `review2-t456-baseline.log` exit 0（pass 1/fail 0，`T456-READOUT columns=13 top=13 shoulder_pos(columns)=13 shoulder_pos(top)=5 … VERDICT=GREEN`）。
  - `review2-t361-baseline.log` exit 0（pass 8/fail 0）。
  - `review2-t440-baseline.log` exit 1（pass 6/fail 1，红项为判据③④，`actual [[body/receipt.ts,2],[fetch/body.ts,13]] vs expected [[fetch/body.ts,13]]`，与一审同因）。
- 新变异 subst（脚本 `review2-subst-patch.cjs`，只碰被忽略的 dist，字节级 utf8 读写无 BOM；备份 `review2-subst-backup-cross.js`）：
  - 红：`review2-subst-red.log` exit 1（pass 0/fail 1，`两表键集合应相等；只在列序侧多的=["hip_cm"]；只在 TOP 侧多的=["hip_typo_cm"]`，两表长度仍各 13——红来自 `deepEqual` 分支而非长度分支；且该日志无 `T456-READOUT` 行，绿读数非自我满足）。
  - 绿：`review2-subst-green.log` exit 0（pass 1/fail 0，`VERDICT=GREEN` 复原）。
  - 复原自证：`dist-restore-identical=true`（现 dist 与备份逐字节一致）、`has-typo=false`、`has-export=true`。
- 对账（只读命中 12/12，exit 一致）：`f614a48d` build 0、`985a4965` t456 0、`029f66a0` t361 0、`6238f1e2` t440 1、M1 红 `c8b19c57` 1／绿 `5523e14e` 0、M2 红 `42a79c0d` 1／绿 `14353d9a` 0、终态 `8d5e3da3` 0／`10646dcb` 0／`6172688c` 0／`28b136bc` t440 1。
- 度量：`cross.ts` LF=292（线内 350）／t456 测试 LF=40／证据 LF=61／changeset 517B 且严格 UTF-8 解码通过。

## 3 逐条路径（S1 hunt＋等待量对账）

- 验收 1（新断言各 13 且集合相等）：测试三断言（两长度＋排序后 `deepEqual`）＋`T456-READOUT` 打印双集合全量与 `shoulder` 双位置（列序 13／TOP 5），与证据 §1 两集合字面一致；文件行号引用真实（`body.ts:29-34` 的 `MEASURE_DEFS`／`cross.ts:15-19` 的 `WAIST_DIVERGENCE_ORDER`，本席已读核对）。
- 验收 2（列序侧加第 14 键必红／还原必绿）：实现 GATE-RUN `c8b19c57` 红（`展示列序表应有 13 项（实测 14）`）→`5523e14e` 绿；本席基线＋subst 变异旁证断言有效。
- 验收 3（TOP 侧删一键必红／还原必绿）：`42a79c0d` 红（`TOP 平局次序表应有 13 项（实测 12）`）→`14353d9a` 绿；本席 subst 变异另证同数改名亦红（`hip_cm` vs `hip_typo_cm` 双侧点名）。
- 验收 4（t361/t440 仍 exit 0）：t361 本席复跑 exit 0（pass 8）；t440 exit 1 但系基线红（blame 证 `receipt.ts:124` 出自 #537 `d2c80a6`，gate 首跑 `14:06:47Z` 在变异前已红），非本票引入。
- S1-放宽：无。排序后比较是对“集合相等”的正确实现（票面明禁拉齐次序，长度＋排序相等缺一不可）；本席 subst 证长度门过而集合门红，无放宽。
- S1-造假：无。红日志无 `READOUT`（断言抛后才打印，绿读数不可伪造）；M1/M2/subst 三形态均实证红→绿，非脚本自我满足。注：断言系两运行时表互对、无硬编码第三方预言机，但票面目标即“改一处必红”，双侧同改需连库表一起动，已超本票范围，不属造假。
- S1-越界：无。`fd86d1d` 的 `cross.ts` 差分为 `const`→`export const` 一词；测试只读 dist（无 `openDb`/`SKILLS_DB`/`prepare`/`INSERT`，本席已扫）；`body.ts` 本票未碰；`receipt.ts` 停手正确。
- S1-丢数据：无。两集合各 13 全量打印一致；`triggers` 第三份表按票面遗留出口“另议”，不在本票；`GROUP_HEADS` 系分组键（中文标签）非 13 键集，不属本票丢数据。
- 等待量/对账：实现 4 件（src＋test 一提交、证据＋changeset 一提交）与一审复核 1 件分工清晰；changeset 为 patch 且字段名/库表/列序/页面/唤醒词“五不动”声明与代码一致；真库零写入（t456 无库调用，t361/t440 走 tmp 库；本席未碰真库）。
- 文档小瑕疵（不扣分）：changeset 行数“一审记 10 行／实 9 行内容行＋尾换行”，系计数口径差，不影响结论。

## 4 返工意见（无返工；转票措辞维持一审）

- 本票无需返工：不同意推翻，一审 PASS 有效。
- S3-1 转票（t440 基线收口，措辞沿用一审）：「`packages/skill-calorie/src/body/receipt.ts:124` 的 `GROUP_HEADS` 以胸围／左大腿为分组键（#537/#539 已提交），致 t440 判据③④ `heavy` 检出 `[receipt.ts,2]`；请认领（判据豁免分组键单点引用或改写该两处字面）或清退，t440 回绿后关闭。」
- 下一手：编排者统一收口；合并点认本席 `review2-subst-green`（t456 exit 0）与 `review2-t361-baseline`（t361 exit 0），t440 以新票为准；本席只新增 `.scratch/t456/review2-*` 与本件，未碰他人文件，未 push。
