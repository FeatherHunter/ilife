# #339 · 场景 03（体重）58 条唤醒词真出口程序锁 · 证据

- 票号：**#339**（地图 **#154**「卡路里场景 03 体重」第 8 张）。
- 用例：`packages/skill-calorie/test/weight-wake-58-339.test.mjs`（LF 202 行；测试文件不计入 350 行告警线）。
- 跑法：`node tooling/run-locked.mjs --ticket 339 -- node --test packages/skill-calorie/test/weight-wake-58-339.test.mjs`
  （用例不依赖其它用例的文件状态；只读仓，产物一律落系统 tmp）。
- 读数时刻：**2026-09-14 18:36–18:38**。取读数时**别人的工作面仍在进行**（5 个页面融合工作面在改 `src/weight/`，
  另有 1 个共享面收口在跑生成链）：本票三次真跑都排过锁，锁等待 10–60 秒（见 §五 的 `waitedMs`）。
- 用例怎么取材（不手抄、不另造）：
  - 逐条命令只从**冻结表**读：`packages/skill-calorie/dist/triggers/scene-03-weight.js` 的 `SCENE_03_WEIGHT`（58 条，取 `main_prompt.cli`），
    用例里**零条写死命令**（没有 58 行硬编码）。
  - 种子库与装配只复现成件：`docs/research/t81-seed.mjs` 的 `seedFull`／`SEED_TODAY`（＝`2026-09-07`）／
    `PLACEHOLDER_SUBSTITUTIONS`／`createHarness`／`SINGLE_COMMAND_RE`。**没有第二套种子库**。
  - 逐条真跑走 `createHarness` 的装配（模板库逐条复制，`SKILLS_DB_PATH`／`CALORIE_PHOTOS_DIR`／`CALORIE_TODAY` 同款）；
    不用它的 `runCli`，因为那条路不返回 `data.output` 与实测字节，而本锁第 ③④ 格要这两个数。

## 一、判定口径（地图 #154 本轮裁定 4 原文）

> 4. **「跑得通」判在一条写明的数据基线上**（第二轮对抗式审查补的）。58 条里有一批要的是**库里本来就有那段数据**
> （锚点对比 8 条、期间复盘 5 条、里程碑回溯 1 条、备注筛选 1 条）。口径定成：**固定种子库**（照 `docs/research/t81-seed.mjs`）上，
> 有数据那段就出结果页；**没有那段数据时给可读的缺失阻断、不落盘——这也算跑得通**，不是缺陷。真坏掉的样子只有四种：
> 报错／白页／空页／路径不是绝对路径／产物打不开。**#339 与 #340 都按这条判，免得最后白判一轮。**
>
> —— `.scratch/t154/map154-now.md:94`（同文见 `.scratch/t154/bodies/w8.md:66-67`「对齐成果」节）

判定三档第 ① 档（同图 `:55`）：**文件存在**——真出口跑一次，断言**落盘／绝对路径／可打开／字节如实**。

## 二、用例把口径翻成的四格断言

| 格 | 断言 | 不通过时算 |
|---|---|---|
| ① 命令形态合法 | `SINGLE_COMMAND_RE`＝`/^calorie-cmd-read calorie\.[a-z0-9.-]+( --params '\{.*\}')?$/`（复用种子库导出，不另写一份） | 真坏 |
| ② 落盘 | `--html` 产物文件存在且可读 | 真坏 |
| ③ 回执给绝对路径 | `data.output` 过 `isAbsolute` | 真坏 |
| ④ 字节如实 | `statSync().size > 0`，且与 `readFileSync().length` 一致；另判**空页**（剥脚本／样式／注释／标签后无可见文本） | 真坏 |

- **缺失阻断＝通过**：`exit` 恰为 `4` 且 stderr 有非空可读消息 → 记 `缺失阻断`，在日志里逐条打出消息。
- **真坏的失败消息带全四样**：唤醒词、命令原文、exit、stderr 末尾 200 字（§五 有实例）。
- 汇总那条用例自己打一行 `RESULT: n/58`，并断言「非 58 不可、真坏必须为 0」。

## 三、58 条逐条判定表

命令列是冻结表 `main_prompt.cli` 原文；用例真跑时按种子库的 `PLACEHOLDER_SUBSTITUTIONS` 换成真实值再跑：
`<日期>`→`2026-09-06`、`<开始日期>`→`2026-09-01`、`<结束日期>`→`2026-09-07`、`<对比开始日期>`→`2026-08-23`、`<对比结束日期>`→`2026-08-29`。
序＝按冻结表 `order` 排序后的位次（与 `probe58.json` 的 `seq` 同序）。

| # | 唤醒词 | 命令（冻结表原文） | exit | data.output 绝对路径 | 字节 | 判定 |
|---|---|---|---|---|---|---|
| 1 | 记体重 | `calorie-cmd-read calorie.weight.log --params '{"kg":70.5}'` | 0 | 是 | 70788 | OK |
| 2 | 改体重记录 | `calorie-cmd-read calorie.weight.update --params '{"id":1,"kg":70.2}'` | 0 | 是 | 61647 | OK |
| 3 | 看本周体重 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"本周"}'` | 0 | 是 | 69408 | OK |
| 4 | 看体重曲线 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"30d"}'` | 0 | 是 | 103178 | OK |
| 5 | 看体重稳不稳（增强版） | `calorie-cmd-read calorie.view.volatility --params '{"window":"7d"}'` | 0 | 是 | 73496 | OK |
| 6 | 看「有备注」的体重记录 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"30d","noteOnly":true}'` | 4 | — | — | 缺失阻断：`ERR 4: 取数失败（缺失阻断）：本窗无带备注的体重记录` |
| 7 | 对比体重：最近 30 天 vs 之前 30 天 | `calorie-cmd-read calorie.view.weight-compare --params '{"window":"30d","compareWindow":"prev"}'` | 0 | 是 | 64150 | OK |
| 8 | 看体重总览 | `calorie-cmd-read calorie.view.weight --params '{"window":"30d"}'` | 0 | 是 | 79453 | OK |
| 9 | 记体重（含备注） | `calorie-cmd-read calorie.weight.log --params '{"kg":70.5,"note":"晨起空腹"}'` | 0 | 是 | 70911 | OK |
| 10 | 改某日体重 | `calorie-cmd-read calorie.weight.update --params '{"kg":70.2,"date":"<日期>"}'` | 0 | 是 | 61682 | OK |
| 11 | 看上周体重 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"上周"}'` | 0 | 是 | 76405 | OK |
| 12 | 看体重曲线（带目标） | `calorie-cmd-read calorie.view.weight-history --params '{"window":"30d","overlay":"target"}'` | 0 | 是 | 103729 | OK |
| 13 | 看本月波动 | `calorie-cmd-read calorie.view.volatility --params '{"window":"本月"}'` | 0 | 是 | 73500 | OK |
| 14 | 对比体重：自定义两段时间 | `calorie-cmd-read calorie.view.weight-compare --params '{"window":"custom","start":"<开始日期>","end":"<结束日期>","compareWindow":"custom","compareStart":"<对比开始日期>","compareEnd":"<对比结束日期>"}'` | 0 | 是 | 64330 | OK |
| 15 | 体重复盘（本周） | `calorie-cmd-read calorie.view.weight-review --params '{"window":"本周"}'` | 0 | 是 | 63769 | OK |
| 16 | 补录体重 | `calorie-cmd-read calorie.weight.log --params '{"kg":70.5,"date":"<日期>"}'` | 0 | 是 | 69409 | OK |
| 17 | 删体重记录 | `calorie-cmd-read calorie.weight.remove --params '{"id":1}'` | 0 | 是 | 63522 | OK |
| 18 | 看本月体重 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"本月"}'` | 0 | 是 | 76405 | OK |
| 19 | 看体重曲线（带里程碑） | `calorie-cmd-read calorie.view.weight-history --params '{"window":"30d","overlay":"milestone"}'` | 0 | 是 | 104016 | OK |
| 20 | 看最近 90 天波动 | `calorie-cmd-read calorie.view.volatility --params '{"window":"90d"}'` | 0 | 是 | 115398 | OK |
| 21 | 对比体重：本周 vs 上周 | `calorie-cmd-read calorie.view.weight-compare --params '{"window":"本周","compareWindow":"上周"}'` | 0 | 是 | 65007 | OK |
| 22 | 体重复盘（本月） | `calorie-cmd-read calorie.view.weight-review --params '{"window":"本月"}'` | 0 | 是 | 65124 | OK |
| 23 | 批量补录体重 | `calorie-cmd-read calorie.weight.batch --params '{"items":[{"date":"<日期>","kg":70.5}]}'` | 0 | 是 | 62873 | OK |
| 24 | 删某日体重 | `calorie-cmd-read calorie.weight.remove --params '{"date":"<日期>"}'` | 0 | 是 | 63566 | OK |
| 25 | 看上月体重 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"上月"}'` | 0 | 是 | 101928 | OK |
| 26 | 看体重曲线（带异常点） | `calorie-cmd-read calorie.view.weight-history --params '{"window":"30d","overlay":"anomaly"}'` | 0 | 是 | 105788 | OK |
| 27 | 看最近 180 天波动 | `calorie-cmd-read calorie.view.volatility --params '{"window":"180d"}'` | 0 | 是 | 153983 | OK |
| 28 | 对比体重：本月 vs 上月 | `calorie-cmd-read calorie.view.weight-compare --params '{"window":"本月","compareWindow":"上月"}'` | 0 | 是 | 64165 | OK |
| 29 | 体重复盘（最近 90 天） | `calorie-cmd-read calorie.view.weight-review --params '{"window":"90d"}'` | 0 | 是 | 71490 | OK |
| 30 | 看今日体重 | `calorie-cmd-read calorie.view.weight --params '{"window":"今日"}'` | 0 | 是 | 68577 | OK |
| 31 | 批量删体重 | `calorie-cmd-read calorie.weight.remove --params '{"start":"<日期>","end":"<日期>"}'` | 0 | 是 | 63619 | OK |
| 32 | 看最近 7 天体重 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"7d"}'` | 0 | 是 | 76401 | OK |
| 33 | 看本月体重曲线 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"本月"}'` | 0 | 是 | 76405 | OK |
| 34 | 看波动异常点 | `calorie-cmd-read calorie.view.volatility --params '{"window":"7d"}'` | 0 | 是 | 73496 | OK |
| 35 | 对比体重：近 N 天 vs 上一个 N 天 | `calorie-cmd-read calorie.view.weight-compare --params '{"window":"7d","compareWindow":"prev"}'` | 0 | 是 | 64132 | OK |
| 36 | 体重复盘（今年） | `calorie-cmd-read calorie.view.weight-review --params '{"window":"今年"}'` | 0 | 是 | 73566 | OK |
| 37 | 看最近 90 天体重 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"90d"}'` | 0 | 是 | 163034 | OK |
| 38 | 看上月体重曲线 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"上月"}'` | 0 | 是 | 101928 | OK |
| 39 | 对比体重：今天 vs 一年前今天 | `calorie-cmd-read calorie.view.weight-compare --params '{"window":"今日","compareWindow":"今日","compareOffset":"-1y"}'` | 0 | 是 | 64948 | OK |
| 40 | 体重复盘（自定义时间） | `calorie-cmd-read calorie.view.weight-review --params '{"window":"custom","start":"<开始日期>","end":"<结束日期>"}'` | 0 | 是 | 65172 | OK |
| 41 | 看某段时间体重 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"custom","start":"<开始日期>","end":"<结束日期>"}'` | 0 | 是 | 76485 | OK |
| 42 | 看最近 90 天体重曲线 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"90d"}'` | 0 | 是 | 163034 | OK |
| 43 | 对比体重：今天 vs 半年前今天 | `calorie-cmd-read calorie.view.weight-compare --params '{"window":"今日","compareWindow":"今日","compareOffset":"-6m"}'` | 0 | 是 | 64978 | OK |
| 44 | 看里程碑回溯 | `calorie-cmd-read calorie.view.weight-review --params '{"mode":"milestones"}'` | 4 | — | — | 缺失阻断：`ERR 4: 取数失败（缺失阻断）：尚未达成任何减重里程碑` |
| 45 | 看最近 180 天体重曲线 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"180d"}'` | 0 | 是 | 250962 | OK |
| 46 | 对比体重：今天 vs 三月前今天 | `calorie-cmd-read calorie.view.weight-compare --params '{"window":"今日","compareWindow":"今日","compareOffset":"-3m"}'` | 0 | 是 | 64978 | OK |
| 47 | 看最近 365 天体重曲线 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"365d"}'` | 0 | 是 | 431677 | OK |
| 48 | 对比体重：当前 vs 目标体重 | `calorie-cmd-read calorie.view.goal-weight --params '{"window":"30d"}'` | 0 | 是 | 60662 | OK |
| 49 | 看某段时间体重曲线 | `calorie-cmd-read calorie.view.weight-history --params '{"window":"custom","start":"<开始日期>","end":"<结束日期>"}'` | 0 | 是 | 76485 | OK |
| 50 | 对比体重：当前 vs 平台期首日 | `calorie-cmd-read calorie.view.weight-compare --params '{"scenario":"b8"}'` | 0 | 是 | 66603 | OK |
| 51 | 对比体重：当前 vs 历史最低 | `calorie-cmd-read calorie.view.weight-compare --params '{"scenario":"e1"}'` | 0 | 是 | 65861 | OK |
| 52 | 对比体重：当前 vs 历史最高 | `calorie-cmd-read calorie.view.weight-compare --params '{"scenario":"e2"}'` | 0 | 是 | 66011 | OK |
| 53 | 对比体重：减重 5kg 那天 vs 今天 | `calorie-cmd-read calorie.view.weight-compare --params '{"scenario":"e3","delta":5}'` | 4 | — | — | 缺失阻断：`ERR 4: 取数失败（缺失阻断）：未达成减重 5kg 里程碑(当前距历史最高已减 4.4kg)` |
| 54 | 对比体重：减重 10kg 那天 vs 今天 | `calorie-cmd-read calorie.view.weight-compare --params '{"scenario":"e3","delta":10}'` | 4 | — | — | 缺失阻断：`ERR 4: 取数失败（缺失阻断）：未达成减重 10kg 里程碑(当前距历史最高已减 4.4kg)` |
| 55 | 对比体重：当前 vs 入夏最低 | `calorie-cmd-read calorie.view.weight-compare --params '{"scenario":"e5"}'` | 0 | 是 | 65882 | OK |
| 56 | 对比体重：当前 vs 入冬最低 | `calorie-cmd-read calorie.view.weight-compare --params '{"scenario":"e6"}'` | 0 | 是 | 65883 | OK |
| 57 | 对比体重：运动多 vs 运动少的两个月 | `calorie-cmd-read calorie.view.weight-compare --params '{"scenario":"c5"}'` | 0 | 是 | 66680 | OK |
| 58 | 对比体重：工作日 vs 周末 | `calorie-cmd-read calorie.view.weight-compare --params '{"window":"工作日","compareWindow":"周末"}'` | 0 | 是 | 64711 | OK |

- 58 条的 `exit` 只有两个值：`0`（54 条）与 `4`（4 条）；**没有 exit 5／其它非 0 非 4**。
- 54 条 OK 的 `data.output` **全部**过 `isAbsolute`（表中「是」列由用例的 `isAbsolute` 断言得出，不是肉眼看串）。
- 字节区间 **60662（#48 对比体重：当前 vs 目标体重）～ 431677（#47 看最近 365 天体重曲线）**，54 条合计 4761290 字节；无 0 字节、无剥标签后无可见文本的页。

## 四、汇总读数

**`RESULT: 58/58（OK 54 ＋ 缺失阻断 4 ＋ 真坏 0）`**（用例自己打的那一行，逐字）。

- **OK 54 条**：四格全过（形态合法／落盘／绝对路径／字节如实）。
- **缺失阻断 4 条**（照裁定 4 算跑通，逐条消息见上表；用例日志里也逐条打出）：

  | 唤醒词 | 键 | 消息 |
  |---|---|---|
  | 看「有备注」的体重记录 | `w_notes` | `ERR 4: 取数失败（缺失阻断）：本窗无带备注的体重记录` |
  | 看里程碑回溯 | `w_milestones` | `ERR 4: 取数失败（缺失阻断）：尚未达成任何减重里程碑` |
  | 对比体重：减重 5kg 那天 vs 今天 | `w_cmp_5kg` | `ERR 4: 取数失败（缺失阻断）：未达成减重 5kg 里程碑(当前距历史最高已减 4.4kg)` |
  | 对比体重：减重 10kg 那天 vs 今天 | `w_cmp_10kg` | `ERR 4: 取数失败（缺失阻断）：未达成减重 10kg 里程碑(当前距历史最高已减 4.4kg)` |

  **为什么这是正确行为**：种子库的体重史是 2025-09-07→2026-09-07 逐日 75.0→70.6 kg（367 行），
  **距历史最高只减 4.4 kg**，且没有一条带备注的记录 ⇒ 上面三条要的「减重 5／10 kg 里程碑」「带备注的记录」在库里本来就不存在；
  命令给的是**可读的缺失阻断句**且不落盘——正是裁定 4 写的那一半。**没有为了凑绿去编数据。**
- **真坏 0 条**。

## 五、变异验证（证明这条锁真的会红）

对**最终版用例文件**做了绿→红→绿的完整一轮（三次跑都是同一份文件，只多／少一行临时变异行）：

| 轮 | 用例文件 | 读数 | 锁包络 |
|---|---|---|---|
| 改前绿（A） | 最终版 | `RESULT: 58/58（OK 54 ＋ 缺失阻断 4 ＋ 真坏 0）`；`tests 60 / pass 60 / fail 0`（18:36） | `runId=cb5e847a-0e1b-4107-b8ca-6818612a8a0e waitedMs=20002 exit=0` |
| 改坏红（B） | 临时加一行：第 1 条命令改成不存在的键 `calorie.weight.nope-339` | `RESULT: 57/58（OK 53 ＋ 缺失阻断 4 ＋ 真坏 1）`；`tests 60 / pass 58 / **fail 2**`（18:37） | `runId=ad3ab278-8b8e-40a0-95d7-585108a77daa waitedMs=30004 **exit=1**` |
| 还原绿（C） | 变异行已删净（`Select-String 'nope-339'` 无命中） | `RESULT: 58/58（OK 54 ＋ 缺失阻断 4 ＋ 真坏 0）`；`tests 60 / pass 60 / fail 0`（18:38） | `runId=b8f31cef-3ee2-493c-af98-ba5ab9ebfbb9 waitedMs=10001 exit=0` |

- 失败计数 **2 > 0**：一条是「记体重」那条逐条用例，一条是汇总那条（`真坏 1 条：记体重`）。
- 红时那条用例的失败消息（正是票面要求的四样，逐字）：

  ```text
  唤醒词「记体重」(w_log)
  命令原文: calorie-cmd-read calorie.weight.log --params '{"kg":70.5}'
  退出码 3（非 0 非 4＝报错）
  stderr 末尾 200 字: ERR 3: 未知 calorie key：calorie.weight.nope-339
  ```

- 三次跑的完整日志（UTF-16LE）留在 `.scratch/t339/run-A-green.log`／`run-B-mutated.log`／`run-C-restored.log`；
  逐条表由 `.scratch/t339/mk-table.mjs` 从 C 的日志配冻结表生成（中间件住 `.scratch/`，不入仓）。

## 六、门禁读数（本地持锁，2026-09-14 18:33–18:38）

| 步 | 命令 | 读数 |
|---|---|---|
| 类型检查（18:33 第一次） | `node tooling/run-locked.mjs --ticket 339 -- npx tsc -p packages/skill-calorie/tsconfig.json --noEmit` | **红（exit=2），红在别人的文件**：`packages/skill-calorie/src/weight/log.ts(60,3): error TS2741: Property 'curve' is missing … but required in type 'WeightDashboard'`；成因是工作区里 `src/weight/figures.ts` 未提交的一处改动（15:01 写入）把 `curve` 变成必填。两处都不在本票写入集（本票只加用例与证据件）⇒ **按纪律报告并停手，不代改**。 |
| 类型检查（18:39 复跑） | 同上 | **仍红（exit=1），但红点已换成另一处、同样不是本票的文件**：`src/photo/pickerDoc.ts(100,9) error TS2353`（`selectedId` 不在 `data` 的联合类型里）＋ `src/photo/routes.ts(31,74) error TS2820`（`'calorie.view.photo-picker'` 不在 `RouteKey` 里）。第一次的 `src/weight/log.ts` 红点在这两次之间**已被那个工作面自己改掉** ⇒ 印证这是**并发中的工作面**，不是本票引入。见 §七 未决 1。 |
| 构建 | `node tooling/run-locked.mjs --ticket 339 -- pnpm build` | **绿**：`runId=62e89c0b-fb59-4bb8-8a90-907fe179f64a waitedMs=0 exit=0` |
| 本票用例 | `node tooling/run-locked.mjs --ticket 339 -- node --test packages/skill-calorie/test/weight-wake-58-339.test.mjs` | **绿**：`tests 60 / pass 60 / fail 0`，`RESULT: 58/58`，`duration_ms≈16–18k`；三次读数见 §五 |

- 未跑全量 `pnpm test`（会顺手改写其他技能的 `SKILL.md`，本票禁区）。
- 未跑 `pnpm gen:check`（不在本票判据面；本票不动命令汇总位）。

## 七、未决问题

1. **`npx tsc … --noEmit` 当前红，两次读数都红在别人的文件（不是本票的活）。**
   - 18:33 第一次：`src/weight/log.ts:60`（`WeightDashboard` 缺必填 `curve`，成因是 `src/weight/figures.ts` 的未提交改动）。
   - 18:39 复跑：红点换成 `src/photo/pickerDoc.ts:100` 与 `src/photo/routes.ts:31`（第一次那个红点已被原工作面改掉）。
   - 三个文件都在本票禁区／写入集之外（本票只加用例与证据件）⇒ **按纪律报告并停手，不代改**。
     **两个工作面收口后需复跑这一条**，以确认类型门恢复到绿。本票用例是 `.mjs`、真跑吃 `dist`，
     **不依赖这条类型检查**，故本票绿读数不受它影响。
   - 顺带记一个读数差：同一时刻 `pnpm build` 是绿的（`exit=0`），`tsc --noEmit` 是红的。两者口径不同，
     而此刻树里带着两个工作面的半成品；本票只报读数，不判谁该对齐谁。
2. **判定基线是「固定种子库」**：§四 那 4 条缺失阻断是种子库里本来没有那段数据所致。若 #340 的端到端验收换了库
   （例如补进带备注的记录、或把体重史改成减重 ≥10 kg），这 4 条的读数会变——变的是数据基线，不是命令。
3. **本锁只判判定三档第 ① 档**（文件存在：落盘／绝对路径／可打开／字节如实）。第 ② 档（内容与字段正确）与
   第 ③ 档（视觉）不在本文件：前者另有结构化断言用例，后者由用户肉眼看（#340）。
4. **产物落在系统 tmp**（`%TEMP%\t81-seed-*\run-N\calorie_html\*.html`），不入仓、不写库文件到仓内。
   #340 若要交付目录（只放 .html）＋清单页，需要按同样口径再跑一次并收集产物。
5. **耦合点（有意留的）**：用例从 `createHarness()` 的装配目录取模板库（`<workDir>/tpl/<DB_FILENAME>`，另有同名前缀兜底）。
   种子库若改装配布局，用例会以明确消息红——不静默漂移；真要改，改的就是那一个取值函数。
6. **本票只加用例、不改任何源码行为**：58 条全绿／阻断，没有发现需要就地修的缺陷；
   若 #340 肉眼终审看到字段或长相问题，归对应页面票，不回到本锁里改行为。
