# t360 独立复核 · 围度趋势不带部位也出趋势（只复核，不改实现，不关票）

> 复核者独立重跑，不采信作者任何结论。作者实施 95% 停，提交 34d7aa3／3e7f629／1b3eb09／1f554f6，未 push。
> 票面 `gh issue view 360`（目标＋验收＋融合验收）；基准 `t395-融合基准.md` §四裁定4／裁定2表体、§五-2、§二-11／12；作者证据 `t360-实施证据.md` 只读未改。
> 现场 `.scratch/t360-review/` 独占，禁复用作者 `.scratch/t360/`；长命令落盘＋标记行＋只读尾5行；路径守卫，用完即清。
> 红线：只写本件（新建唯一）与草稿；证据持锁提交；禁改实现／测试（变异临时改持锁期做、释放前点名复原，终态零残留），禁 stash／checkout--／reset／clean／restore／rebase／amend／add -A／.，禁装包，禁碰真库与老技能只读目录；不跑 pnpm gen；编译用 `tsc -b packages/skill-calorie`（另一次 `--force` 只为恢复变异污染的 dist，见 §三）。

## 一、提交范围（逐个 git show --name-only）

- `34d7aa3 feat(360)`：`src/body/bodyDocs.ts`、`src/body/bodyPlate.ts`、`src/body/view.ts`、`src/fetch/body.ts`，4 件齐，无 `routes.generated.ts`，中文信息正常。
- `3e7f629 feat(360)`：`test/t360-measure-trend.test.mjs` 唯一件。
- `1b3eb09 docs(360)`：`docs/skills/skill-calorie/t360-实施证据.md`＋`.changeset/t360-measure-trend.md`。
- `1f554f6 docs(360)`：`t360-实施证据.md` §十一补记唯一件。
- 四提交并集＝声明 7 路径，无超范围。`git log --follow --diff-filter=A -- test/t360-measure-trend.test.mjs` 唯一 `3e7f629`；`HEAD:test`、`3e7f629:test`、`578959c:test` 三哈希同 `9502badc532f67d92ee8602f07a656af84e6ff21`，`git diff 578959c 3e7f629 -- test` 空，LF 口径 257 行（`split('\n').length-1=257`，与 numstat 257 一致），收回零损失。
- `git merge-base --is-ancestor 578959c HEAD` exit=1，不在 HEAD 祖先链（reset 类痕迹，记账不扩散）。

## 二、功能重跑（自建 tmp 库，另起种子，不复用作者库）

手算期望（不抄作者输出）。自建种子 R（waist／hip，与作者 chest／waist 不同）：
R4 4天前 waist85／hip空，R3 3天前 waist84／hip96，R2 2天前 waist空／hip95，R1 1天前 waist空／hip94。
最近有数据部位＝hip_cm（臀围，R1）＞ waist 最新 R3；hip 序列 ASC [96,95,94] N=3，avg95，min94，max96，delta-2，latest94；waist 点数 2。

- 跑 `calorie-cmd-read calorie.view.body-measure`（不带 metric）→ exit 0（脚本内断言）。
- 读数① 可见文本含「臀围趋势」且不含「腰围趋势」。
- 读数② 图区非空：html 含 `<svg`、无 `data-chart-empty`、无「该部位暂无趋势数据」。
- 读数③ KPI「趋势点」3 天＝sqlite 窗口内 hip 按日聚合计数 3＝plate `trend.length` 3＝`kpi.count` 3；五项 `[3,95,94,96,-2]`＋序列 `[96,95,94]` 与手算逐值同。
- 机器：`T360-REVIEW-READOUT N=3 sqlite=3 部位=hip_cm KPI=[3,95,94,96,-2]`，`T360-REVIEW-RESULT 4/4`。
- 裁定2表体：caption「围度记录」R4 臀格 `—`、R1 腰格 `—`，逐行无连续空单元，可见含 `—`。`T360-REVIEW-TABLE pass`。
- 样本不足兜底：`metric=left_arm_cm`（库非空、该部位无行）→ exit0，含「左上臂趋势」＋「该部位暂无趋势数据」，无 `<svg`，均值／最小／最大／变化量／趋势点五格全 `—`，单项表「该项目无记录」。`T360-REVIEW-FALLBACK pass`。
- 空库：无参／`hip_cm` 均 exit4 且 stdout 空。`T360-REVIEW-EMPTY pass`。
- GATE-RUN runId=26a4b0d2-8d1c-47c9-8a79-34ffd267c9b9 cmd=node review-run.mjs exit=0；runId=82e3b2fe-78a3-4316-a73a-d8a55ad32d2f probe 基线 exit=0。

## 三、新探针（作者未覆盖：单点样本 n=1，delta null 分支）

作者 8 用例全 N=3／空／兜底，无 n=1。探针种子：仅 1 行 1天前 hip94。手算 N=1，avg94，min94，max94，delta null→可见「—」，latest94。判据 exit0＋「臀围趋势」＋有 svg＋「趋势点1天」＋「均值94cm」＋「变化量—」＋sqlite1＋plate delta null。

- 变异（持锁单窗口做，释放前点名复原 `bodyPlate.ts`）：`measureTrendKpi` 尾 `... : null` 改 `: 0`。
- 改坏必红：`node probe-single.mjs` exit=1，`AssertionError 单点变化量—`，实测 `变化量 +0cm` vs 期望 `/变化量\s*—/`。GATE-RUN runId=b254abd6-d58a-4829-8f6a-6493617eaa37。
- 还原必绿：取回原文件（sha `f0b38f840a8674332d0cf0e0cd9b13000e70e422` 一致，`git status` 该件干净）＋ `tsc -b --force` 重建 dist（含 `delta ... : null`）后同探针 exit=0，`T360-REVIEW-PROBE single N=1 delta=null pass`。GATE-RUN runId=bd6cd057-0342-4488-b73e-d67a885262f1。
- 过程：mut 编译 runId=261b4f9b exit0；restore 编译 runId=93b88f95 exit0 但增量未重建 dist，补 `--force` runId=95cecd02 exit0（dist 删后重建，路径守卫），再验绿。终态 src 零残留、dist 与 src 一致。

## 四、回归抽查与归属

- 作者 8 用例：`node --test t360-measure-trend.test.mjs` → `pass 8 fail 0`，exit0。GATE-RUN runId=7def1c1a-a1bd-43d5-a9c7-15cbd92faef4。
- `tsc -b packages/skill-calorie` → exit0。GATE-RUN runId=f4d446af-8cef-4db4-bef3-34edf50aaa20。
- 相邻门 `render-t41+fetch+deprecated-126` → `pass 20 fail 2`，exit1。GATE-RUN runId=9eabc10d-46fc-46b2-b805-3ca901870d0a。红：① `#41 计划三盘`（`buildPlanWizardView` 文案，workout 在途）② `#41 缺失阻断`（`buildVolatilityView` 空库，weight 在途）。绿：身体 `围度/体成分增查趋势对比` ✔、`#41 身体系两盘` ✔、`fetch/body 7处` ✔、`cross围度分层` ✔。
- `sport-homogeneity-109` → `pass 10 fail 2`，exit1。GATE-RUN runId=57a31d41-6384-4015-8bc6-1d2c168c275c。红：① 唤醒词错键（exercise 路由在途）② 空库卡首例 `calorie.view.weight`（weight 在途）。绿：`#109 围度：单项趋势＋全量表` ✔、`体成分` ✔。四红确非本票路径（body／围度段全绿）。

## 五、结构抽查与卷入记账

- 改动不出 body 能力外：4 源码件全在 `src/body/`＋`src/fetch/body.ts`（身体取数）；`view.ts` 仅注释 1 行；`bodyPlate`＋`bodyDocs`＋`fetch` 增量 122 行；无触发器／路由／他域。
- 图闸门 `trendMetric=metric ?? autoMetric`（`bodyDocs.ts:114`），有值且 `trend` 非空出 `<部位>趋势` 线图，否则空态（仍注部位）；KPI 经 `measureTrendKpi(trend)` 单一来源，渲染不重算。注释明示「全量表不动归 #361，只修缺值格」，#361 串行关系在证据 §九留痕，本票为前置。
- 卷入 `578959c(feat406)` 曾含本票测试件 257 行（numstat 257／0），现不在 HEAD 祖先链；本票已由 `3e7f629` 收回，哈希三方一致、diff 空、当前树可跑（§四 8／8），零损失。不追责他席。
- 缺陷归属按 §5.1（本票引入／范围／范围外，范围外只 S3＋转票）。

## §5.1 回执（限1200词）

PASS。

机器证据：复跑 exit0 RESULT 4/4 `T360-REVIEW-READOUT N=3 sqlite=3 部位=hip_cm KPI=[3,95,94,96,-2]`；GATE-RUN 26a4b0d2(review-run)／7def1c1a(作者8,pass8 fail0)／f4d446af(tsc exit0)／9eabc10d(20/22)／57a31d41(10/12)；新探针改坏必红 runId=b254abd6 exit1（`变化量 +0cm` vs `/变化量\s*—/`）／还原必绿 runId=bd6cd057 exit0（`single N=1 delta=null pass`）。

逐条：提交范围四提交仅含声明 7 路径，无 routes.generated，中文正常，树上测试件唯一来源 3e7f629；功能三读数（臀围趋势／图非空／KPI==sqlite）＋裁定2表体（—无连续空）＋兜底（五—＋空态）＋空库 exit4 全绿；新单点探针红绿对照成立；回归 8／8 绿、四红分属 workout／weight／exercise、身体与围度段绿；结构不出 body、图闸门与 #361 前置留痕；卷入 578959c 不在祖先链、哈希一致零损失。

缺陷清单：本票引入 0；本票范围 0（S1／S2 无）；范围外 4 红全 S3＋转票（workout 计划文案、weight 空库阻断、exercise 唤醒词错键、weight 空库首例），不阻关票。下一手缺：编排者收口关票＋放行 #361（条件：本票关票后，表头与复制区判据归 #361，同一分支 `bodyDocs.ts:123` 之后）；数字动画不验收；显式区间下 trend 仍按 days 取沿旧口径，需区间另开票。
