# t586 B席证据（背靠背独立复算）

- 票：#586（复核 #579 基准双人 CROSS-REVIEW），席位 B，总工程师派单后独立执行。
- 背靠背声明：复算全程未读 `docs/skills/skill-calorie/t586a-*` 内容，未读 `.scratch/t586a/**` 内容；仅在收尾做存在性点名（`t586a-证据.md` 不存在，`.scratch/t586a/` 存在但未读内容），以保结论独立。
- 票面：`gh issue view 586` 已读；验收命令与基准件均按票面原文。
- 基准件：`docs/skills/skill-calorie/t579-场景02视觉整改基准.md`（83 行，唯一基准）。
- 命令一律经 `node tooling/run-locked.mjs --ticket 586 -- <命令>` 排队；长输出落盘、只看最后 5 行； targeted 汇总用 `node -e` 小口径提取，不整文件 dump。
- 本席只写 `docs/skills/skill-calorie/t586b-*` 与 `.scratch/t586b/**`，未碰对方路径，未改产品代码／墙目录／他人测试。

## 一 · 门禁改前读数独立复算（与基准 §一逐数一致）

主 GATE-RUN（JSON 口径，日志与机读 JSON 双落盘）：

- 命令（锁内）：`node packages/skill-calorie/scripts/audit-separators.mjs --dir docs/skills/skill-calorie/scene02-验收墙 --json .scratch/t586b/gate.json --quiet > .scratch/t586b/audit-json-quiet.log 2>&1`
- GATE-RUN：`dedeea2d-142f-48e2-af07-4772ce02e9f7`，`RUN … exit=1`（见 `.scratch/locks/gate-runs.log`），命令退出码 1（改前红，符合预期）。
- 日志尾 5 行含 `RESULT: 0/87`＋`FAIL`（`Get-Content … -Tail 5` 已验）。
- 机读 JSON（`.scratch/t586b/gate.json`）小口径提取：
  - `RESULT: 0/87`，`BROKEN: 0`，`green=0 files=87`
  - `TOTALS: {"R1":802,"R2":178,"R3":838,"R4":475,"R5":251,"R6":172,"R7":474}`
  - `NODES: 1802`
- 另一 quiet 复算（无 JSON，纯日志口径）亦一致：`runId=8314bec9-4436-40b1-8d29-675349fb51d9 exit=1`，`.scratch/t586b/audit-quiet.log` 尾 5 行 `RESULT: 0/87`＋`FAIL`。首次直跑 `9d31fb14… exit=1` 同样 `0/87`，三跑互相印证。

对照基准 §一（本席独立读基准表后比对）：

| 规则 | 基准 | 本席复算 | 一致 |
|---|---|---|---|
| R1 含 `·` | 802 | 802 | 是 |
| R2 含 `；` | 178 | 178 | 是 |
| R3 ≥3 段并列 | 838 | 838 | 是 |
| R4 含 `｜` | 475 | 475 | 是 |
| R5 含 `、` | 251 | 251 | 是 |
| R6 含 `~` | 172 | 172 | 是 |
| R7 内部标识符 | 474 | 474 | 是 |
| 去重节点 | 1802 | 1802 | 是 |
| RESULT | 0/87 FAIL exit 1 | 0/87 FAIL exit 1 | 是 |

R7 切分（基准 §一末“全部在墙 4 件，83 产物页 R7=0”）：本席由 `gate.json rows` 独立加总 `PRODUCT-R7: 0`、`WALL-R7: 474`、`PRODUCT-NODES: 982`、`WALL-NODES: 820`、`SUM: 1802`，与基准“982＋820＝1802”一致。

## 二 · 基准件核对（量尺／问题清单／例外口径）

- 量尺（基准 §二）：`J1 门禁／跨宽自适应／文案／vision` 四维名全在；`| 30 |` 命中；`83 产物页` 口径句命中；权重 30／25／25／20 与终审条件 `83/83 PASS exit 0` 转录无误（本席只核对存在性与数值字面，未重打分，基准 §二已声明改前 vision 25/100 由总工程师抓取）。
- 12 图问题清单（基准 §四）：`V\d{2}` 去重计数 `V-COUNT: 31`，`V-RANGE: V01-V31`，`HAS-V31: true`；A 回执 V01–V06、B 列表 V07–V11、C 排行 V12–V16、D 复盘 V17–V22、E 食品库 V23–V26、F 总览 V27–V31 程序点名 31 个全在。
- 门禁例外口径（基准 §〇／§一末／§六）：墙目录实测 `TOTAL: 87`，豁免 4 件 `总索引.html／桌面墙-1280.html／手机墙-390.html／链路总表.html` `EXEMPT-FOUND: 4/4`，`PRODUCT: 83`，87＝83＋4 与基准“门禁扫 87＝83 产物页＋4，终验只跑 83 文件清单、不扫墙”一致。
- 管辖字节与时刻（基准 §〇转录备查，未作改前重测）：83 份产物页、重出时刻 2026-09-16、提交 `ee6a81b`；本席未改墙目录一字节。
- 回归底线（基准 §五转录）：`measure-responsive 124 格全 0／外部引用 0／<img> 0／83 产物页 R7=0／两态口径／场景归属待裁两格`，本席仅核对 R7=0（上节已验），其余口径未越权重跑。

独立结论：门禁改前读数可复算，量尺与问题清单齐全，本席判 PASS（改前红可复现即基准可信）。

## 三 · 逐条路径（本席写集＋核对对象，只列结果不贴长文）

- `docs/skills/skill-calorie/t579-场景02视觉整改基准.md`：已读 83 行，§一／§二／§四／§〇／§六逐项核对通过（上节）。
- `docs/skills/skill-calorie/scene02-验收墙/`：只读，87 html＝83＋4，豁免 4 件点名通过，未写一字节。
- `packages/skill-calorie/scripts/audit-separators.mjs`：只读 275 行，只为理解 `--quiet` 只打摘要行＋`RESULT green/total`＋`PASS/FAIL`、退出码 0/1/2，未改。
- `.scratch/t586b/audit-quiet.log`：本席锁内落盘，尾 5 行 `RESULT: 0/87`＋`FAIL`，PASS（与基准一致）。
- `.scratch/t586b/audit-json-quiet.log`：本席锁内落盘（主 GATE-RUN stdout），尾 5 行 `RESULT: 0/87`＋`FAIL`，PASS。
- `.scratch/t586b/gate.json`：本席锁内 `--json` 落盘，`0/87＋R1..R7＋1802＋BROKEN 0`，PASS。
- `docs/skills/skill-calorie/t586b-证据.md`：本文件，本席唯一文档交付物。
- `docs/skills/skill-calorie/t586a-*`：未读未写；`.scratch/t586a/**`：仅存在性点名（有目录，内有 3 名），未读内容未写。
- `packages/**`、`docs/skills/skill-calorie/scene02-验收墙/**`、他人测试：未改。

## 四 · 互查结论（背靠背，待交换）

- 本席复算锚点（已冻结供交换）：`RESULT 0/87`、`R1 802／R2 178／R3 838／R4 475／R5 251／R6 172／R7 474`、`NODES 1802`、`PRODUCT-R7 0／WALL-R7 474`、`PRODUCT-NODES 982／WALL-NODES 820`、退出码 1。
- 对方状态：点名时 `t586a-证据.md` 不存在，无法逐行比对；为保背靠背未读对方 `.scratch` 内容，故不臆断对方读数。
- 结论：本席与基准件逐行一致；若 A 席亦为上述锚点则双方一致，否则以本证据锚点为准，差值定位到 `R1–R7／NODES／RESULT／exit` 中的具体一行（票面“不一致即 FAIL 并写清差在哪一行”待交换后由总工程师裁定；本席当前判自身 PASS，互查项记“待交换、暂无行级差异”）。
- 量刑：简单票五维口径（两人≥85 且一致→PASS）由总工程师打分，本席仅供复算数。

## 附 · 可复算留痕

- `gh issue view 586`（`8add1b7e… exit 0`）；主门禁 `dedeea2d… exit 1`；quiet 复核 `8314bec9… exit 1`；汇总提取 `b42fb5b3… exit 0`、`abe1297f… exit 0`、`68f4c670… exit 0`、`3da6ed82… exit 0`、`836b1ee0… exit 0`；日志 `bf860fe2… exit 0`；存在性点名 `adb6e005… exit 0`。`gate-runs.log` 均有 `START／RUN` 留痕。
- 缺依赖：无（`node`、`gh`、`audit-separators.mjs` 均可用）；未派下级 subagent；提交只含自己文件、中文信息、不推。
