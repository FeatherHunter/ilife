# t571 复核证据（复核席独立复跑）

> 复核席声明：实施者证据 `t571-返修-证据.md` 一个字都不采信；本件全部独立复跑。只读复核：不改任何源码与产物；复跑只读；草稿只写 `.scratch/t571-R/`。
> 复核 runId：`R571-20260916-R1`（本复核自己的运行标识；各持锁命令另有独立 runId，见 §0）。
> 范围：缺口族页 21–22，报告 `t520-视觉终审-报告.md` §7.4 R-28~R-35 共 8 条；21 与 22 同字节，一处修两页各验。
> 编排者已发裁决：571-R28 锁现况 S3（不动文案、只锁现况，不按“达标≤2”判 FAIL）；573-R59 接受同字节（由 532 索引承担，本票不验 H1 入口身份）；572-T5 双向断言（基线已清零，按双向识别力验，本票只验不越界）。
> 被审提交：`a33700d`（feat(571)，4 件：证据＋AGENTS 台账＋`trendPredictDocs.ts`＋`t571-deficit-gap.test.mjs`，与票面 §0 写集声明逐件对上）。

## §0 复核方法与对账源（持锁运行）

- 锁：一切复跑命令经 `node tooling/run-locked.mjs --ticket 571 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000 -- <命令>`，对账源为 `.scratch/locks-m158v2/gate-runs.log`。
- 只读边界：未跑 `tsc -b` 重编与 `run-all.mjs` 产物重出（会落盘）；`dist` 经 stat 确认新于源码（§3），直接读 `dist` 复验；草稿只写 `.scratch/t571-R/`（3 件探针）；证据只写本件。
- 等待如实记：R0-3 等 572 席 60s、R3-2 等 573 席 30s、新探针首跑等 573 席 15s（活锁一律不抢回）。

| # | runId | cmd | exit | 读数 |
|---|---|---|---|---|
| R0-1 | `4d5d46b1-9ff4-4339-beb0-a25835cf7053` | html 目录计数 | 0 | 31 件在盘 |
| R0-2 | `55f16f50-7cff-43d5-9b74-00fb80e111a5` | html 名 | 0 | 21=查热量缺口，22=看热量缺口 |
| R0-3 | `97fb6e0b-a900-4069-be16-fd22b0df5a33` | stat src/dist 新鲜度 | 0 | src 81777B 03:39Z，dist 83704B 04:04Z，dist 新于 src（等锁 60s） |
| R1 | `3afe71d4-6109-4b79-bc14-b401b39bb07b` | `node --test t571-deficit-gap` | 0 | 8/8 绿 |
| R2 | `d653d36d-42e5-42da-a94e-8daad0aa3bbc` | `node .scratch/t571-R/probe-product.mjs` | 0 | 21/22 均 87017B：DABI=10，缺口 7/7 纯，图例目标 true，日期 7/7 含星期，折叠有＋默认闭，trs=8，合计短标签 true |
| R3 | `c8617f24-97de-4fd9-af81-f0edd974bb64` | `node .scratch/t571-R/probe-hash-src.mjs` | 0 | sha12 均 `a7f562797fcc`，sameBytes=true，ytextTicks 3/3，源码标记 9/10（第三序列项见 R1 已绿，系本探针转写多一空格所致，非源码缺失） |
| R4废 | `80acda9e-c140-4c78-9bdc-e3663c98fa3a` | 新探针首跑 | 1 | 废跑：探针 import 路径少一段 `packages/`（复核草稿 bug，已修；源码产物未动） |
| R4 | `1cd71a1a-e759-4a9c-a285-a4d856889d98` | `node .scratch/t571-R/probe-new.mjs` | 0 | GREEN＋RED 两行（见 §4） |
| R5 | `eac23009-bed7-4c0f-9822-0e7a65807b39` | audit-separators | 0 | 31/31 PASS（含 21/22 命中 0） |
| R6 | `6b3336b8-f815-4321-bd99-583fa584905b` | measure-responsive | 0 | OVERFLOW-ZERO pages=31 cells=93 failed=0（含 21/22 三档 0） |
| R7 | `1f07de49-3e1d-493e-8ac0-1fd9df0bf33c` | `node --test analysis-deficit-385` | 0 | 6/6 绿（冻结 D1/D2 未破） |
| R8 | `c6ecff06-8edd-4c29-bc49-3987314878e3` | check-warning-line | 0 | 91/91 PASS（等锁 30s；`trendPredictDocs` 台账 1162） |
| R9 | `b68aefb5-7dec-439a-bc51-9fc716ac98e4` | gen-cli --check | 0 | GEN-CHECK PASS 键 128 |

## §1 A 轴—标准轴（文档标准优先＋气味基线＋结构铁律抽查）

- 用词纪律（`wording.md`，只验本票新增可见面）：本票新增可见文案两处——`缺口明细（点击展开）`、`摄入目标 1800 卡`，无黑话、无英文形参、无“壳/键/落地/抓手”类词；`audit-separators` 21/22 命中 0（R5）。源码注释“壳宽”（`t571DeficitCss` 头注“不碰公共层与壳宽”）沿既有 `pageChromeCss` 用词、非用户可见面，不记缺陷。
- 气味基线 12 项（只看 `a33700d` 在 `trendPredictDocs.ts` 的增量：页侧 CSS 5 行＋折叠套壳＋第三序列＋合计套壳）：
  - Mysterious Name：`t571-deficit-table/detail/totals` 票号域前缀、名副其实，无。
  - Duplicated Code：第三序列 `targetLine` 与摄入序列同标签映射 2 行、`markLine` 与第三序列同值双线并存，属 R-33 强制要求（线上标签＋图例各一处），件头已注明同源，轻微 S3 亦不单独记（R 条优先，见报告验收）。
  - Feature Envy／Primitive Obsession／Data Clumps：只读自家 `DeficitData`＋`signed/targetDef` 既有小件，无。
  - Repeated Switches／Shotgun Surgery／Divergent Change：无新增分支，无跨件改（一件），无。
  - Message Chains／Middle Man／Refused Bequest：直调 `base-paint/blocks` 公开接口 `renderDisclosure`，未抄公共层，未造第二份徽章／色表，无。
  - Speculative Generality：CSS 三选择器＋折叠＋第三序列逐项有 R 验收认领，无预留泛化，无。
- 结构纪律五铁律（`docs/agents/structure.md`，管辖＝`packages/` 源码；测试／文档不管）：
  - 一能力自治：源码只动 `skill-calorie/render/trendPredictDocs.ts` 一件，用公共层只经 `renderDisclosure` 公开接口，未抄 `base-render`，PASS。
  - 二概念唯一：未新增色字面量（新增 CSS 零 `#` 命中，橙色走 `palette[2]` 同源）、未新增常量／类型／公式，PASS。
  - 三改动可预告：票面 §0 写集 4 件与 `a33700d` 4 件逐件对上（stat 见首语），偏差 0，PASS。
  - 四名字取自 HELP：未新建源码目录／文件（测试新件不管辖），PASS。
  - 五接口小里面厚：新增 `t571DeficitCss` 为文件内私有函数，对外出口仍 `buildDeficitDoc` 等 10 个薄转出，无新增导出，PASS。
  - 超线报警：`trendPredictDocs.ts` 实测 1162（台账行在，`check-warning-line` 91/91，R8），报警已挂号、本次先不拆口径不变；本复核未改源码，不触发新挂号。

## §2 B 轴—规格轴（R-28~R-35 逐条复验，21/22 双页）

- 当刻产物：21/22 均 87017B、sha12 `a7f562797fcc`、sameBytes=true（R2/R3）。与实施者“终版 89600B/`5ee6e5a1723b`”不同：系 `a33700d` 之后他席跨族改动（`2dedbc9` feat(573) 等）叠加所致的共享树漂移；本节全部读数以当刻盘上产物＋源码为准，8 条 R 验收逐条仍绿（漂移未破缺口族验收）。
- R-59（21=22 同字节）按裁决接受同字节：sameBytes=true 正是接受态，本票不验 H1 入口身份，无越界。
- T5 双向按 572 裁决：本票未碰分隔符基线，R5 31/31（旧基线 0/31 反为修绿证据口径与 572 一致），无越界。

### R-28 同事实重复（锁现况 S3，按裁决不判≤2）
- 验收（裁决后）：“达标”三处锁现况仍在（表 `✓ 达标`＋徽章 `达标 N 天`＋口径 `达标线＝每天 +300 卡缺口`），改任一处即红。
- 复验：R1 R-28 绿＋R7 冻结 6/6＋R2 DABI=10 双页。
- 判定：本票范围绿（锁现况）。计数≤2原文仍不成立（10>2），但裁决已定 S3 锁现况，故不记缺陷、不 FAIL。

### R-29 缺口数值与状态连排
- 验收：缺口列全匹配 `^[+-]\d+$`。
- 复验：R1 R-29 绿（合成 3 行 `+400/+150/-50`）＋R2 产物 7/7 纯双页。
- 判定：本票范围绿。

### R-30 390 数值列不齐
- 验收：390 该列 7 格右缘极差≤1px。
- 复验：R1 R-30 绿（`t571DeficitCss`＋`tnum`＋`.t571-deficit-table` 三标记）；产物列纯符号（R2）为像素对齐前提；本复核无 CDP 像素直读（未做项），以源码标记＋§5 窄档截图数值列视觉齐＋门禁溢出 0 旁证。
- 判定：本票范围绿（像素直读未复，见未做项；不记缺陷）。

### R-31 390 合计行标签折行
- 验收：390 三标签渲染行数==1。
- 复验：R1 R-31 绿（三短标签＋`64px`）＋§5 双档 vision_describe 确认三行单行无折行（21/22 一致）。
- 判定：本票范围绿（本票修法生效）。

### R-32 390 明细表占页高
- 验收：占比≤20% 或默认折叠带展开。
- 复验：R1 R-32 绿＋R2 折叠有＋默认闭双页＋§5 双档见 `▸缺口明细（点击展开）` 闭合＋§4 新探针折叠内 `trs=4`（不减行）。
- 判定：本票范围绿（本票修法生效；桌面亦折叠系验收允许的一致简化）。

### R-33 图例不完备
- 验收：图例含 `摄入目标` 且含 `1800`。
- 复验：R1 R-33 绿＋R2 legendTarget=true 双页＋§4 新探针 markLine 标签保留＋§5 双档三色图例齐（蓝摄入／绿消耗虚线／橙目标）。
- 判定：本票范围绿（本票修法生效）。

### R-34 图表中段空
- 验收：轨迹纵占图高≥60%。
- 复验：R3 ytextTicks=3/3 双页（量程未写死，冻结口径）；R1 R-34 绿。像素占比未直读（未做项），以 ticks==3＋§5 趋势图三刻度完整可读旁证。
- 判定：本票范围绿（开工已绿，保持；本票未改量程）。

### R-35 390 丢失星期
- 验收：390 日期格含星期字。
- 复验：R1 R-35 绿（合成 `2026-09-0X 周X`）＋R2 日期 7/7 含星期双页（窄档 `data-label` 堆叠不丢字）。
- 判定：本票范围绿（开工已绿，保持）。

## §3 机器判据独立复跑（只读，见 §0 对账表）

- 分隔符 `audit-separators` 31/31 PASS（R5）；响应式 `measure-responsive` OVERFLOW-ZERO 31×3（R6）；靶向 `t571` 8/8（R1）＋冻结 `385` 6/6（R7）；告警线 91/91（R8）；生成汇总 `gen-cli --check` 键 128（R9）。
- 未做：`tsc -b` 重编、`run-all.mjs` 31 页重出、CDP 像素探针（`details` 占比／右缘极差直读）——会落盘或需浏览器驱动写入态，故只读复核不跑，见 §6 未做项。

## §4 自设新探针／新变异（被审脚本覆盖不到，红绿两行）

- 探针 `.scratch/t571-R/probe-new.mjs`（草稿，不进 git）：t571 断言只查“有无折叠／图例含词”，不查“折叠不减行”与“线例共存”，本探针补这两处不变量。
- GREEN（R4）：`GREEN trs=4 tableInsideDisclosure=true markLineKept=true legendTarget=true defaultClosed=true`——折叠内 1 表头＋3 行不减、表在折叠体内、markLine 标签与图例第三项共存、默认闭合。
- RED（R4，同次运行内存变异，不写盘）：`RED defaultClosedAssertionFailsOnOpen=true（变异展开即红，探针有识别力）`——给 details 注入 `open` 后默认闭合断言必红。
- 废跑 1 条已如实记（R4废，探针 import 缺 `packages/` 一段，修后重跑绿；源码产物零动）。

## §5 视觉双档复评（21/22 × 1440x900／390x844 fullPage＋读图）

- 截图（`vision_html_screenshot` fullPage，不通读整份 HTML）：21 桌面 `21------.html-4a226025-shot-1440x900-fullpage.png`（1639px 高）、21 窄档 `...-390x844-fullpage.png`（1638px）；22 桌面／窄档同形 4 件（见 `.dsh-vision-router/artifacts/`，草稿态）。
- 读图（`vision_describe`；`dsh visionreview` 已坏未试）：21 双档——合计三行单行无折行、明细折叠闭合带控件、图例三项齐全、无溢出塌列压字；22 双档——与 21 同源同结构、无不一致项（读图原话“未发现与 21 不一致之处”）。
- 归属：R-31 单行／R-32 折叠／R-33 三项图例均为本票修法生效（本票范围绿）；390 统计卡 2×2、H1“至”后换行、“摄入目标”标注近图例边缘未越界三处为正常版式，非缺陷；范围外发现 1 条 S3 见 §6（工作区他席在途改动）。

## §6 判定与提交

- 判定：PASS（R-28~R-35 当刻 8/8 绿，其中 R-28 按锁现况 S3 裁决验；机器门 5 路全绿；视觉 4 档全绿；新探针红绿双行）。
- 缺陷清单：S1 0 条；S2 0 条；S3 2 条——（1）本票范围：R-30/R-34 像素直读（极差／纵占比）本复核未跑 CDP，只验到源码标记＋视觉旁证，建议收口票补一次持锁 CDP 直读（`.scratch/t571-R/` 已留探针位）；（2）范围外发现：工作区当刻他席在途改动（`src/goal/*`、`src/render/sportDocs.ts`、`AGENTS.md` 台账、`t578/t460` 证据草稿等）与本票零交集，本席一行未碰。转票措辞：请对应归属票（目标族／运动族／578）各自收口，本复核不代验。
- 提交：本件 `docs/skills/skill-calorie/t571-复核-证据.md` 一件；`git commit` 带显式 pathspec（只含本件，他席已暂存的 `t578-GATE-RUN.log` 原样留暂存区、一行不动）；持锁提交；不 push、不 close。
