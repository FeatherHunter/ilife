# t518 复核证据（对抗复核席 · 独立复跑与自设探针）

> **判定：PASS**（90 分：契约一致 **27**／证据真实可复现 **23**／新旧对照 **18**／工程红线 **14**／文档同步 **8**）。
> **无 S1**；**S2 一条**（验收主判据的「全尺」里 **J4／J5／J6／J7 四条无读数** —— 本席补跑：J6／J7 绿、**J4／J5 红**且红在公共层底盘）；**S3 五条（本票范围）＋ 2 条范围外发现**（逐条见 §十）。
> 纪律：实施席的结论一律不采信 —— 本件每一个数字都是本席在当刻 HEAD `a967506` 上**自己跑出来的**；
> 实施席给的哈希一律自己重算；四份证据件、产品代码、台账一律未改；未 close、未 push。
> 全部编译／测试／门／变异经 `node tooling/run-locked.mjs --ticket 518 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000 -- <命令>`；
> 只写本席目录 `.scratch/t518-review/**`（该目录与 #518 视觉审查席共用，本席件一律 `r<n>-` 前缀）。
> 实施席的 `.scratch/t518/db/calorie_data.db`（mtime 04:16:21）本席一行未动：重出器我**拷一份到自己目录**跑，种子库落在 `.scratch/t518-review/db`。

---

## 〇 · 复跑汇总（本席读数 ← 实施席声明）

| 判据 | 本席命令（全经加锁包装器） | 本席读数 | 实施席声明 | 合 |
|---|---|---|---|---|
| 可复现性 | `node .scratch/t518-review/gen31.mjs --out …/regen --baseline …` ×2 | `31/31 页落盘 bytes_total=2390076`；与 `.scratch/t518/产物/` **逐页 sha256 31/31 相同**；两遍互比 **31/31 相同** | `31/31 … bytes_total=2390076`；`W4-REPRO 31/31 DIFF=none` | ✓ |
| 分隔符（裁定 D 的 23 页） | `audit-separators.mjs --dir …/p01-23` | **`RESULT: 23/23` ＋ `PASS`**、exit 0；01–23 节点级**全 0** | `RESULT: 23/31`（24–31 范围外） | ✓ |
| 分隔符（归属核对 24–31） | `audit-separators.mjs --dir …/p24-31` | **`RESULT: 0/8`**，逐页 24:6 25:7 26:7 27:7 28:7 29:6 30:5 31:12 | 同上（逐页同值） | ✓ |
| 横向溢出 | `measure-responsive.mjs --dir …/regen --widths 390,512,820,1440` | **`OVERFLOW-ZERO pages=31 cells=124 failed=0 scopeOutFailed=0`**；我自算 `clippedCells=0 scrollerCells=0` | 同 | ✓ |
| 版式探针（3 页抽） | `t516-判据-版式.mjs` 06／12／19 | `RESULT: 3/3`；四档 conclusion=1／toc=1／caliber=2／chip=3／越界=0；字号 5–6 档 | 未报（本席补跑） | 补 |
| 靶向测试六件 | `node --test analysis-predict-383／predict-455／tdee-463／shape-predict-520／accept-386／deficit-385` | **`tests 31 / pass 31 / fail 0`**、exit 0 | 七件 38／38／0 | ✓（补第 7 件＝**7/7/0** ⇒ 31＋7＝**38**） |
| 包内门 ① | `check-warning-line.mjs` | **`RESULT: 91/91` ＋ `PASS`**、exit 0 | 同 | ✓ |
| 包内门 ② | `gen-cli.mjs --check` | **`GEN-CHECK PASS：键 128（写 46 ＋ 读 82）…未搬迁清单 0 条`**、exit 0 | 同 | ✓ |
| GATE-RUN 对账 | 四份证据件声明 × `t518-GATE-RUN.log` | 声明 **45** 条，**逐一命中日志 45/45**；**只认 exit=0 ⇒ matched = 36/45**；**undeclared = 6** | — | 见 §四 |

---

## 一 · 可复现性：31 页重出，逐页 sha256 全同（我自己算的）

- 重出器＝**逐字节拷贝**实施席的重出器（`sha256=bcc969b014cf…` 两份相同），只改落点：
  `node .scratch/t518-review/gen31.mjs --out .scratch/t518-review/regen --baseline .scratch/t518-review/regen-baseline.json`
  → `RESULT: 31/31 页落盘 bytes_total=2390076（别名 01/23、21/22 逐字节同）`；
  同批再出一遍到 `regen2` → 同样 `31/31 bytes_total=2390076`。
- 本席自算汇总：`REPRO-A-vs-产物 pages=31/31 DIFF=none`、`REPRO-B-vs-产物 pages=31/31 DIFF=none`、`STABLE-A-vs-B pages=31/31 DIFF=none`；
  按目录 `Length` 求和：本席 **2390076** ＝ 实施席 **2390076**（`same=True`）。
- 别名对（本席自算）：`01≡23` ✓、`21≡22` ✓。
- **当刻 HEAD 的重出是稳定的**（同一天内两遍逐字节相同）；这条判据的可复现性成立。
  ⚠️ 口径提醒（不是缺陷）：`gen31.mjs` 的种子库按 `new Date()` 生成 60 天日期 ⇒ **跨自然日重出会整批变字节**，本票的「逐字节相同」只在同一自然日内可复现（本席与实施席同在 2026-09-16）。

逐页读数（本席算 ／ 实施席报，31 行全同）：

| 序 | 页 | bytes | sha256(12) | 序 | 页 | bytes | sha256(12) |
|---|---|---|---|---|---|---|---|
| 01 | 预测体重(1周后) | 70743 | `e387c019e3bf` | 17 | 摄入预测(自定义) | 73724 | `4c24208a15db` |
| 02 | 预测体重(1月后) | 70754 | `59337816ae95` | 18 | 摄入预测(营养目标达成) | 70357 | `09d61d6c8bce` |
| 03 | 预测体重(3月后) | 70754 | `be73ee903850` | 19 | 摄入预测(卡路里缺口) | 69842 | `28e428dfdb29` |
| 04 | 预测体重(6月后) | 70757 | `7a26116a2ff5` | 20 | 摄入预测(摄入稳定性) | 70108 | `ef8119a29dd3` |
| 05 | 预测体重(自定义时间) | 70746 | `05a6e0bfd270` | 21 | 查热量缺口 | 87082 | `451ff468680e` |
| 06 | 预测体重(自定义目标) | 70736 | `6b479b0a702a` | 22 | 看热量缺口 | 87082 | `451ff468680e` |
| 07 | 模拟减重(每天-300卡) | 73627 | `6f73680ec2bb` | 23 | 看体重预测 | 70743 | `e387c019e3bf` |
| 08 | 模拟减重(每天-500卡) | 73628 | `aac413dee3a3` | 24 | 看BMI报告 | 94556 | `e1cd4e976c90` |
| 09 | 模拟减重(每天-700卡) | 73628 | `688872dcd4de` | 25 | 看TDEE报告 | 79373 | `a9c87f668467` |
| 10 | 模拟减重(30天减Xkg) | 72660 | `f09f8eda8e78` | 26 | 看BMR报告 | 89026 | `cecc1ae4ce5c` |
| 11 | 模拟减重(60天减Xkg) | 73343 | `329b07f73e82` | 27 | 看蛋白质摄入报告 | 86064 | `6bc541fc0899` |
| 12 | 模拟减重(90天减Xkg) | 74025 | `c0619b24c589` | 28 | 看水分摄入报告 | 86082 | `cd5a4d77c691` |
| 13 | 模拟减重(自定义天数) | 73001 | `4251174a2b01` | 29 | 看综合评分 | 78302 | `d7e0988565a7` |
| 14 | 摄入预测(当前速率1周) | 71568 | `414e895803af` | 30 | 看健康趋势 | 95757 | `55140ec2a7e1` |
| 15 | 摄入预测(当前速率1月) | 72651 | `fd0eb6704b65` | 31 | 看健康报告(含对比) | 94555 | `c16553d8214c` |
| 16 | 摄入预测(当前速率3月) | 74802 | `148c55baa915` | | | | |

> 逐页明细（含本席／实施席两列对照）落 `.scratch/t518-review/r1-table.md`；原始日志 `r1.log`。

---

## 二 · 判据：分隔符按裁定 D 拆两段跑；版式四档自算

- **裁定 D 的验收面（01–23）**：`SEP-IN23 exit=0 :: RESULT: 23/23 :: PASS`（`SEP-IN23-NODES … nonzero=0/23`）。
  本席**不采信**「31 页全零」这种混读：同一目录全跑是 `RESULT: 23/31 :: FAIL`，差的正好是 24–31。
- **归属核对（24–31）**：`SEP-OUT8 exit=1 :: RESULT: 0/8`，逐页节点级 `24:6 25:7 26:7 27:7 28:7 29:6 30:5 31:12`
  —— 与 W3 证据 §一① 的逐页值**逐字相同**，也与我读到的 `reportDoc*.ts`／`reportPlate.ts` 归属一致 ⇒ **8 页红确属 #519 的账**，不是本票漏改。
- **横向溢出**：`OVERFLOW-ZERO pages=31 cells=124 failed=0 scopeOutFailed=0`；我另写了逐格复算（`MEASURE-RECOMPUTE cells=124 nonzero=0 clippedCells=0 scrollerCells=0`）——**逐格 0**成立（31 页 ×4 档覆盖基准件的三档断点 {≤640／641–820／≥821}）。
- **版式探针 3 页**（自己挑，含 01／21 之外）：`06 预测体重(自定义目标)`／`12 模拟减重(90天减Xkg)`／`19 摄入预测(卡路里缺口预测)`，四档读数：
  06＝conclusion 1／toc 1／caliber 2／chip 3／状态徽章 1、越界 0；12＝同上＋状态徽章 1；19＝状态徽章 0（该页无状态）、越界 0。
  `1440列宽=1120/1440`、`kpi每排=2+2`（390／512／820 与 1440 同）、`kpi极差=0`。
  另加两页参照（21 样板页／23 别名页）：21＝conclusion 1／toc 1／**caliber 3**／chip 6／状态徽章 1／越界 0；23＝与 **01** 同形（conclusion 1／toc 1／caliber 2／chip 3、无状态徽章、越界 0）⇒ 预测族与 #517 样板页同档。

---

## 三 · 靶向测试与包内两门（本席复跑）

- 票面点名的六件：`analysis-predict-383`／`analysis-predict-455`／`analysis-tdee-463`／`analysis-shape-predict-520`／`analysis-accept-386`／`analysis-deficit-385`（**六件都存在**）
  → **`tests 31 / pass 31 / fail 0`**、exit 0（`r4-tests.log`）。
- 补跑第 7 件 `analysis-shape-520.test.mjs` → **`tests 7 / pass 7 / fail 0`** ⇒ 实施席「靶向七件 **38/38/0**」这条声明**成立**（31＋7＝38）。
- 包内门① `RESULT: 91/91` ＋ `PASS: 告警线台账齐全且与实况一致`、exit 0；
  门② `GEN-CHECK PASS：键 128（写 46 ＋ 读 82）；能力 10 个…未搬迁清单 0 条`、exit 0。
- 一次失败运行如实记账：本席自设的 #466 探针**首跑红**（脚本导入路径写错：`shiftISODate` 从 `series.js` 取，实住 `utils.js`）⇒ `TypeError`、exit 1；已修脚本重跑，**该次不充作任何判据**。

---

## 四 · 对账（协议 §2.4 第 2／3 条，只认 exit=0）

对账源＝`docs/skills/skill-calorie/t518-GATE-RUN.log`（受跟踪、含 51 条 START ＋ 51 条 RUN，**只含 ticket=518**）。

| 指标 | 读数 |
|---|---|
| 四份证据件里的 `runId=` 声明条数 | **45** |
| 其中在日志里找到**同标识** RUN 行 | **45**（缺条目 **0**） |
| **只认 exit=0** ⇒ 可充门禁证据的 | **matched = 36/45** |
| 声明了但该次 exit≠0 | **9** —— 全部在证据件里**当场具名**为「首跑失败／范围外门」，未充作绿条（W1:75、W1:116、W1:155、W2:27、W2:55、W2:96、W2:124、W3:29、W4:67） |
| 日志里**无人声明**的 RUN 条目 | **undeclared = 6** |

`undeclared` 的 6 条逐条点名（都经加锁包装器、都有 START／RUN 留痕、都是 exit=0）：

1. `282c24a1…` `pwsh -NoProfile -Command "git add packages/skill-calorie/src/render/trendDocs.ts …"`
2. `e55b210d…` `git add …test/analysis-deficit-385.test.mjs…`
3. `6c6b3ce3…` `git add …src/analysis/simulate2.ts…`
4. `e7b8681a…` `git add …src/render/trendPredictDocs.ts…`
5. `bc79911a…` `git add …AGENTS.md docs/skills/skill-calorie/…`
6. `62486edd…` `node packages/skill-calorie/scripts/check-warning-line.mjs --sync`（**唯一一条门再跑**）

判读：1–5 是 §3.1 要求的**持锁 `git add` 暂存**（不是门禁证据）；6 是 W3 窗内的**第二次 `--sync`**（同窗另有 `f8ac1c26`（声明、exit=0）与 `b50edeeb`（声明、`--dry`、exit=1））。
按 §2.4 第 3 条②的**字面**口径，窗口内存在无人声明的运行条目＝对账不通过；按同节第 2 条「声明面＝证据里声称跑过的门禁运行」的口径，前 5 条不属门禁面。⇒ **请编排者裁**（或把这类条目按 §2.3 第 4 条写进导出件的声明口径里），本席按 S3 记账、不据此判 FAIL。

---

## 五 · 对抗点 A：三张吸收票的判据（**全部自己造夹具**，不引实施席任何读数）

夹具：自造 15 天 `DaySeries`（末值 75.1 kg）＋三个自建 SQLite 库（`probe/`、`probe2/`、`probe3/`，真库零触碰）。

### ① #455 轨迹末点口径 —— **9/9 条成立**

| 断言（本席自写） | 读数 |
|---|---|
| `horizon=90`（非整周）第 90 天那一行**存在**且日期＝首发点＋90 天 | 末点 `2026-12-15` ＝ `2026-09-16＋90`，点数 14 |
| 第 90 天那一行的值＝首发值−周速×90/7 | `86.03` ＝ 我自算 `86.03` |
| 90 天点数＝1＋12＋1＝14 | 14 ✓ |
| `weightSimTarget(6 kg, 90 天)` 第 90 天行值＝**69.1** | `69.1` ✓ |
| 第 84 天行的值＝**首发值−目标×84/90 ＝ 69.5**（不再被强写成 69.1） | `69.5` ✓（旧错位值 `69.1` 不在此行） |
| 整周 `horizon=84`：点数＝1＋12＝**13**、日期不重复、末点＝第 84 天、值＝69.1、无第 91 天以后的行 | 13／13 去重／`2026-12-09`／69.1／✓ |
| 摄入预测 30 天页末点＝第 30 天（不是第 28 天） | `2026-10-16` ✓ |
| 体重预测族采样器（`simulate.ts::forecastSeries`）30 天页末点＝第 30 天 | `2026-10-16` ✓ |

### ② #463 档案 TDEE —— **7/7 条成立**

| 断言 | 读数 |
|---|---|
| 档案**无**体重记录 ⇒ 与 `calcTdee(70.0,175,30,male,moderate)` **同一个数** | `loadProfileTdee=2556` ＝ 我自算 `2556` |
| 无体重记录时缺口页 `日均消耗＝静态 TDEE＋运动`（±1 舍入） | `avgBurn=2856 ＝ 2556＋300` ✓ |
| 无体重记录时 `target.tdee === loadProfileTdee` | 2556 ＝ 2556 ✓ |
| 最近体重 **75.1 kg** ⇒ TDEE 落在 **2635±2** | `2635`（差 0）✓ |
| 75.1 kg 时加法式仍成立 | `avgBurn=2935 ＝ 2635＋300` ✓ |
| 「最近」＝按 `date→time` 最后一条（不是第一条／最大值） | 74.4／76.0／75.1 三行 ⇒ 取 **75.1** ✓ |
| 同日后补一条时间更晚的称重 ⇒ TDEE 跟着换 | 补 74.9（23:00）⇒ `2632 ＝ calcTdee(74.9,…)` ✓ |

**「与改前逐字相同」的独立核法**：`git show 7831393 -- src/analysis/series.ts` 显示本票只动 `loadProfileTdee` 里取最近称重那一处、回退值 `70.0` 字面未动；`calcTdee` 本身在本票 diff 之外 ⇒ 回退路径的数**必然是同一个数**（本席另跑 2556 ＝ 2556 复核）。

### ③ #466 降级文案 —— 计数面 **全过**、量纲面 **1 处**不一致

夹具：**空库**（档案＋目标，零记录）与**窗口内正好 13 天**（14 天窗口的 13 天各有体重／三餐／运动）两组，各跑**全部 20 条预测词**（逐条真跑 CLI）：

- 空库：20/20 降级、`当前只有 0 天…记录` ＝ 我自算的窗口内有效记录天数 **0**，**无一等于窗口天数**。
- 窗口内 13 天：体重族 6 条 `当前只有 13 天体重记录`（窗口 14 ⇒ **不是 14**）、30 天窗口的 3 条 `13 天摄入记录`（**不是 30**）、模拟减重族 7 条（门槛＝「至少 1 天」）**不降级、正常出页**——三条断言全过。
- **唯一红条（S3）**：`calorieDeficitEta` 的门槛词与读数**种类词不同** —— 门槛「≥14 天**摄入+运动**记录」、读数「13 天**摄入或运动**记录」（AND vs OR）。其余 5 个调用点逐字相同。
  实施席的 ⑤ 断言件只覆盖体重／摄入／营养目标三条路径，**未覆盖「卡路里缺口」与「摄入稳定性」**两条 ⇒ 这正是「自设探针打脚本盲区」命中的一处。

---

## 六 · 对抗点 B：自设源码级变异（**MUT-RED／RESTORE／GREEN 三行 + 前后 sha256**）

变异点由本席自选（不是照抄实施席的），全程在**同一个持锁窗口**内：备份字节 → 写变异 → `tsc -b` 直调 → 跑红 → **按备份逐字节写回** → 重编 → 跑绿。

**变异 ① `src/analysis/series.ts::loadProfileTdee` 回退常量 70.0**

```
MUTATE  src/analysis/series.ts  sha_before=24c716bf34a4  sha_mutated=59833c7766fd  字节 14683→14673
MUT-RED ① series.ts 回退常量 70.0 :: 实施席断言件(analysis-tdee-463) exit=1 tests=4 pass=2 fail=2 :: 我的夹具 tdee=2556（期望 2635±2）
RESTORE ① 相等=true sha256(before)=24c716bf34a4 sha256(after)=24c716bf34a4
GREEN   ① 还原后 :: 实施席断言件 exit=0 tests=4 pass=4 fail=0 :: 我的夹具 tdee=2635 tdeeOk=true
```

**变异 ② `src/analysis/simulate2.ts::weightSimTarget` 删掉「非整周补第 days 天末点」、回退旧强写**

```
MUTATE  src/analysis/simulate2.ts  sha_before=a7704e1b03e8  sha_mutated=3ce7a3863eca  字节 14090→14044
MUT-RED ② simulate2.ts 回退旧强写 :: 实施席断言件(analysis-predict-455) exit=1 tests=5 pass=2 fail=3 :: 我的夹具 第90天行=缺
RESTORE ② 相等=true sha256(before)=a7704e1b03e8 sha256(after)=a7704e1b03e8
GREEN   ② 还原后 :: 实施席断言件 exit=0 tests=5 pass=5 fail=0 :: 我的夹具 第90天行=69.1
```

- **RESTORE 相等=true 两处**（sha256 前后逐字符相同、字节数相同）；还原后**重编**（`tsc -b` exit 0）⇒ 编译产物同步复位（协议 §5 的两步连做）。
- 收尾复核：`git status --porcelain` 只有**别席**的 `docs/skills/skill-calorie/t518-视觉抽查.md`（未跟踪），本席变异件**零残留**。
- 一次失败运行如实记账：本席变异脚本首版把「75.1 kg 称重」漏在夹具之外 ⇒ 自己的夹具读数恒 2556、**对该变异没有识别力**（`MYPROBE-GREEN` 假红）；已修正夹具重跑，上表是修正后的读数。

---

## 七 · 对抗点 C：W1「逐字节相同」这条判据可信吗

- **改动面（`git show --stat` 自己核）**：`d208e9f` ＝ `trendDocs.ts` 658 行改动（几乎全是删）／`trendPredictDocs.ts` 649 行新增＋`AGENTS.md`（台账，`--sync`）＋W1 证据 ＋ GATE-RUN 导出；`94f59bb` ＝ **只加不减**地改 `analysis-deficit-385.test.mjs` 的扫描列表（`+` 一行数组元素、原两项与阈值滤法一字未动，行内引裁定 B）。
- **源级独立比对（本席自设探针）**：把 `git show d208e9f^:…/trendDocs.ts`（搬前）与 `git show 94f59bb:…/trendPredictDocs.ts`（搬后）里 **10 个 `build*Doc` 的花括号配平块**逐个取出比字：
  → **`RESULT-MOVE 逐函数逐字相同 = 10/10`**（`buildDeficitDoc` 7967B、`buildPredictDoc` 1707B、`buildPredictTargetDoc` 1330B、`buildSimCutDoc` 1949B、`buildSimTargetDoc` 2087B、`buildCalorieForecastDoc` 1803B、`buildCalorieGoalDoc` 1168B、`buildCalorieDeficitDoc` 1110B、`buildCalorieStabilityDoc` 1167B、`buildGoalPredictDoc` 1410B，**两侧字节数逐一相等**）。只有 `export ` 关键字与导入面是本次唯一的形状变化。
- **出口面**：HEAD 的 `trendDocs.ts` 末尾 `export { …10 个名字… } from './trendPredictDocs.js'`（10/10 名字在件里）；真实调用面 `src/analysis/commands.ts:30`／`src/goal/read.ts:13`／`src/workout/contraindication.ts:9` 全部仍从 `../render/trendDocs.js` 导入，**一字未动**。
- **本仓能不能独立复现「搬迁前后 31 页逐字节相同」**：**不能**。该判据需要「搬前源码编译出的产物」与「搬后源码编译出的产物」两份，而**当刻树里只有搬后的源码**（禁止切分支／不许造第二份工作树）⇒ 历史前后一致性**不可复现**。本席能核的是：① **当前态**可复现性（§一：31/31，两遍稳定）；② 搬动的**代码块逐字未变**（上面 10/10）；③ 产物侧 01／23、21／22 别名同字节。⇒ 结论：**只能核当前态的可复现性，历史前后一致性不可复现**；这条在 W1 证据里没有具名，属**交接面缺口**（S3，不阻断）。
- 行数（LF 口径＝只数 `\n`，本席自算）：`trendDocs.ts` **528**、`trendPredictDocs.ts` **747**，与台账「当场实测」两列**逐字相同**。

---

## 八 · 对抗点 D：覆盖有没有被收窄

- **`94f59bb` 只加不减**：`analysis-deficit-385.test.mjs:388` 的数组由 2 项变 3 项（新增 `'../src/render/trendPredictDocs.ts'`），diff 里**没有删除项、没有放宽断言、阈值与滤法未动** ⇒ 语义＝恢复覆盖。(`git show 94f59bb -- …test.mjs` 全 diff 已逐行读过。)
- **其余扫描面普查（本席自跑）**：在 `packages/skill-calorie/test/**` 里搜**写死 `../src/` 路径**的地方 ⇒ **仅此一处**（`grep -n "\.\./src/"` 命中 1 行）。其余测试要么按**目录递归**取件（`readdirSync` 遍历 `src`／能力目录 ⇒ 新件自动进面），要么只出现件名于**注释**中：`doc-page-assert.mjs:30`／`analysis-predict-383.test.mjs:111`（都提 `buildCombinedDoc`，该函数**留在** `trendDocs.ts`）、`trend-homogeneity-110.test.mjs:97,177`、`trend-misc-port-113.test.mjs:88`。
  ⇒ **结论：不需要再补扫描面**（唯一被收窄的一处已由裁定 B 补回，且「只加」可证）。
- **反向验证（这一族的判据还有没有识别力）**：① 实施席的 `w1b-color-mutate.mjs` 往新件注入 `#abcdef` ⇒ 该扫描断言 red（本席核过它对账条目：`runId=97610443`，外层 exit 0、内层 exit 1 已在 W1 证据具名）—— 说明「写死件名」这一族补面后仍能抓；② 本席的**变异 ①②**（§六）另打在**数据面断言件**（`analysis-tdee-463`／`analysis-predict-455`）上，两处都被现成断言件抓住（fail=2／fail=3）。⇒ **不属「收窄变放宽」**。

---

## 九 · 自设静态探针：被漏报的四条判据（J4／J5 详解 ＋ J6／J7 补读数）

基准件 §一 J4／J5 的量法＝「`<style>` 段全部 `font-size`，按 `chartsAt`（`/* charts */`）切出图表段」。本席用基准件自己的取证器 `.scratch/t516/static-facts.mjs` 对 31 页逐页跑（`r5-j4j5.md`）：

| 判据 | 基准件目标值 | **本席实测（01–23 逐页同值）** | 结论 |
|---|---|---|---|
| **J4 字号阶梯** | 页面层 ≤**5 档**、下限 **≥11px**（图表档豁免） | 非图表段 **10 档**＝`10/10.5/11/12/12.5/13/15/17/20/40`，**最小 10px** | **红**（改前＝非图表 10 档、最小 8px） |
| **J4（页面本地）** | 页面本地不许写字号 | 页面本地 **0 处 `font-size`** | 绿（符合票面「一个字号都不许写」） |
| **J5 断点档数** | 页面层只用 ≤640／641–820／≥821 三档；不新增 400／720／875 | `@media` **19 条/页**（21／22 为 22 条），含 `(max-width: 400px)`／`(max-width:720px)`／`(min-width:721px) and (max-width:875px)`／`(min-width:876px)` | 与改前 18–21 条同档、**未新增断点**，但按「只用三档」的目标值仍**红** |

**归属（这段是本席判 PASS 而不是 FAIL 的关键）**：把非图表段的 10 个字号逐条落回选择器，全部落在**公共层内联底盘**上 ——
`.ilife-toast*`／`.ilife-action-btn`／`.ilife-copy-btn`／`.ilife-copy-menu-*`／`.ilife-status-badge`／`.ilife-empty-*`／`.ilife-error-*`（源：`packages/base-render/src/style.ts:205,228,250,281` 与同表其余规则），
31 页（**含已被 #517 验收的样板页 21／22、含 24–31 报告族**）读数**完全一致**。
⇒ **不是本票引入**、也**不在本票写集**（票面「不动共享形状票已定的共享件语义，要回改必须报编排者转票」；基准件 §5.3 已把「字号下限无守卫」登记为公共层缺口 G-2、承接票 #567）。
**但**：本票证据件 §二「全套机器判据（全窗口）」把自己说成「全部判据复跑一遍」，却**没有 J4／J5 的任何读数**；J6／J7（量法＝`t516-判据-版式.mjs`）在四份证据与 GATE-RUN 里**也一次没跑**。本席补跑（5 页 ×4 档，逐格读数）：**J6**＝1440 档页壳盒宽 **1120**、左余量 **160**／右余量 **160（对称）**、逐元素**越界 0**（模板页与别名 23 同值）⇒ 达标（目标「主列 ≥960 且两侧对称」）；**J7**＝KPI 每排 `06／12`：390 `2+2`／512 `2+2`／820 `2+2`／1440 `4`，`19`：四档均 `2`（该页只有两张卡），同排高度极差 **0**、无 3＋1 孤行 ⇒ 达标。

---

## 十 · 缺陷清单（逐条标归属与 S 级）

| # | 缺陷 | 归属 | 级 | 证据 |
|---|---|---|---|---|
| 1 | **主判据「按基准件全尺逐条跑、逐条绿」缺 4 条读数**：J4／J5／J6／J7 在四份证据件与 GATE-RUN 里**零读数**；本席补跑：**J6／J7 绿**、**J4／J5 红**（红在公共层内联底盘：非图表段 10 档 / 最小 10px / 19 条 `@media`，31 页（含 #517 样板页与 24–31）读数一致） | **本票范围**（产品面归公共层 → 转 #567／#519 收口；证据面归本票） | **S2** | §九；`r5-j4j5.md`、`r2-layout.json`、`r4-layout-ref.json` |
| 2 | `#466` 六处调用点里的 `calorieDeficitEta`：门槛「≥14 天**摄入+运动**记录」／读数「N 天**摄入或运动**记录」**种类词不同**（AND vs OR）；实施席 ⑤ 只覆盖三条路径，未覆盖「卡路里缺口」「摄入稳定性」 | **本票范围**（#466 改动内，一行措辞） | **S3** | §五③；`r6c-probeA3.md` |
| 3 | 对账：日志里 **6 条无人声明**的运行条目（5×`git add` 暂存 ＋ 1×`check-warning-line.mjs --sync`），全部有加锁留痕、exit=0、结果未被任何证据件声称 | **本票范围**（过程记账） | **S3** | §四；`r8-gate-run.md` |
| 4 | W1 证据一处措辞与事实不符：「既有调用面（`render/index.ts` 与各测试…）」——`src/render/index.ts` **没有** trendDocs 的导入；真实调用面是 `src/analysis/commands.ts:30`／`src/goal/read.ts:13`／`src/workout/contraindication.ts:9` | **本票范围**（文档精度） | **S3** | §七；`grep trendDocs.js` |
| 5 | 「搬迁前后 31 页逐字节相同」在当刻 HEAD **不可独立复现**（树里只有搬后源码）；W1 证据未具名这条限制，只给了当时读数 | **本票范围**（交接面缺口；判据本身已由源级 10/10 逐字＋当前态 31/31 兜住） | **S3** | §七 |
| 6 | 台账 `packages/skill-calorie/AGENTS.md` 里 `scripts/build-help.mjs` 有**两行完全相同**（554／554）；`scripts/gen-photo-baseline.mjs｜283`、`src/home/homeDocs.ts｜334` 这类「已超线／未越线」与数值相抵的行 | **范围外发现**（经 `git show d208e9f^` 同点比对：**开窗前就在**，计数与 HEAD 一致 ⇒ 生成器 `--sync` 的账，不归本票） | S3 | 逐版本比对 |
| 7 | 报告族 24–31 分隔符 5–12 处/页 + J2 形状三条全 0 | **范围外发现**（裁定 D 已划给 #519；本席复算 `0/8` 与逐页值一致） | S3 | §二 |
| 8 | 票面「交付物路径：产物与截图 `.scratch/t518/样板/`」——该目录**不存在**，实际产物在 `.scratch/t518/产物/`（四份证据件都明写了实路径、且 `.scratch/**` 不入库） | **本票范围**（命名与票面不一致） | S3 | `Test-Path` → False |

**无 S1**：无契约级违约、无用户可见回归（23/23 分隔符、124 格溢出全 0、输出逐字节可复现）、无证据造假（45/45 声明逐一对上、失败运行全部当场具名）、无数据丢失风险、未改他人路径。

---

## 十一 · 打分（协议 §5.1 五维）

| 维度 | 分 | 依据 |
|---|---|---|
| 契约一致 | **27/30** | 写集遵守（报告族一行未碰、逐页读数可核）；三张吸收票判据本席自造夹具**全部成立**；01–23 分隔符 23/23、124 格溢出 0、三条恒出齐。扣 3：J4／J5 未达标（共享底盘，需转票）、#466 一处量纲措辞、四条判据读数缺席 |
| 证据真实可复现 | **23/25** | 31/31 逐页 sha256 我自算全同、两遍稳定、字节总数 2390076 相同；测试／两门复跑一致。扣 2：6 条无人声明运行条目 |
| 新旧对照 | **18/20** | 搬前搬后源级 10/10 逐字＋字节数相等；#463 前 2556／后 2635 我自算。扣 2：「搬迁前后 31 页逐字节相同」不可独立复跑、实施席未具名该限制 |
| 工程红线 | **14/15** | 无切分支／reset／stash／clean；6 个提交的 `--name-only` 只含本票件＋台账；变异在持锁内做且**逐字节**还原后重编；收尾工作区干净（只有别席未跟踪件）；两件超线必报五步齐（裁定 F 接受）。扣 1：6 条无人声明运行（过程面） |
| 文档同步 | **8/10** | 四份逐窗证据＋对账导出件＋台账 `--sync` 齐。扣 2：§二 表把「全部判据」说满但缺 J4–J7 读数；W4 §四 未列 W4 两个提交的 sha（协议 §3.1 要求全部 sha 清单） |
| **合计** | **90** | 无 S1、均分 ≥85 ⇒ **PASS** |

自设探针（§5.1 硬要求，**被审脚本覆盖不到**的至少一件 —— 本席共 4 件）：**① 两处源码级变异**（§六，含前后 sha256 与逐字节还原）；**② J4／J5 静态量法**（§九，打「全套判据」这句自我满足）；**③ W1 搬来源级花括号配平逐字比对**（§七，打「不可复跑的历史声明」）；**④ 写死扫描面普查**（§八，打「收窄变放宽」）。

---

## 十二 · 未做项与下一手缺什么

1. **未做**：真正的「搬迁前后 31 页逐字节相同」复现（需搬前源码可编译的一份工作树；本仓禁止切分支／`git worktree` 未获授权）——下一手若要做，须由编排者给出具名授权与隔离目录。
2. **未做**：31 页截图（票面「产物与截图」的第二半）；本席只用 CDP 探针取版式读数，未落 PNG。
3. **半做**：24–31 报告族的版式读数：本席的 J4／J5 静态量法已**顺带覆盖**这 8 页（读数与 01–23 同值：非图表段 10 档／最小 10px／`@media` 18 条），但**没给它们跑浏览器探针**（那是 #519 关票时要判的面）；本票侧只核了「没被改坏」：分隔符逐页值与 W3 声明逐字相同、产物 sha256 与我重出的全同。
4. **下一手缺什么**：① 编排者对 §四那 6 条无人声明运行条目的裁定口径；② 把 J4／J5 那条 S2 落成「证据补读数＋归属转票（#567 G-2 字号下限守卫／#519 收口）」；③ `#466` 那一行「摄入+运动／摄入或运动」的量纲统一由谁改（本票写集内，可随 #519 窗口一并落）。
5. **本席件清单**（未入库，按票面「只提交一个文件」的限定留在共享工作区）：脚本 `r1-regen`／`r2-criteria`／`r3-static`／`r4-tests-gates`／`r5-j4j5`／`r6c-probeA3`／`r7-mutate`／`r8-gaterun`／`r9-move`（均 `.scratch/t518-review/*.mjs`，sha256 见下），原始日志与 JSON 同目录 `r*.log`／`r*.json`／`r*.md`。
   `r6-probeA.mjs`／`r6b-probeA2.mjs`／`explore-cli.mjs` 是**被取代的中间件**（期望值算错／导入路径写错，读数已在 §三、§五如实记账为失败运行），留在盘上备查、**不充作任何判据**。

| 脚本 | sha256(12) | 脚本 | sha256(12) |
|---|---|---|---|
| `r1-regen.mjs` | `80606f7916d6` | `r6c-probeA3.mjs` | `58077969d4a8` |
| `r2-criteria.mjs` | `609db250c5fa` | `r7-mutate.mjs` | `8a47f6b0019e` |
| `r3-static.mjs` | `8906d941f3f1` | `r8-gaterun.mjs` | `dc45112e7e41` |
| `r4-tests-gates.mjs` | `2ac018d61145` | `r9-move.mjs` | `5ef522b6ef59` |
| `r5-j4j5.mjs` | `301676cc5f12` | `.scratch/t518-review/gen31.mjs`（拷贝，与实施席件同哈希） | `bcc969b014cf` |

> 收尾自检：本件是唯一写集；`git diff --cached --name-only` 只含本文件；未 push、未 close、未改实施席证据件（`t518-W1..W4` 的 sha256 复核前后一致）、未改产品代码（§六 两处变异已逐字节还原并重编）。

---

# 十三 · W5 复审（2026-09-16 · 裁定 J／K ＋ 无回归）

> **复审范围**（编排者派单 2026-09-16）：只复审 ① 裁定 J（#466 量纲）② 裁定 K（三处证据更正）③ 无回归；
> 已复现过的机器面（31 页重出／溢出／分隔符）沿用本席 §一／§二 的读数方法重跑，**不引实施席读数**。
> 变更面（本席自己 `git show --stat` 核）：`fd30caa`（六件：`trendPredictDocs.ts`／`simulate2.ts`／
> `test/analysis-deficit-eta-466.test.mjs`（新）／W1 证据／W5 证据／`AGENTS.md` 台账）＋ `0bda0ed`（W5 证据 ＋ GATE-RUN 导出）。
> 当刻 HEAD `0bda0ed`；本席工作区复核前后干净（`git status --porcelain` 空）。
>
> ## **修订判定：PASS · 94/100**（契约一致 29／证据真实可复现 22／新旧对照 19／工程红线 15／文档同步 9）
> **无 S1**；**S2 一条（W5 窗 23 条「判据类」运行未逐条声明 —— 超出裁定 I 授权的范围，请编排者裁／补声明）**；**S3 四条**。

## 十三·一 · 裁定 J（#466 量纲）—— **成立**（本席自造夹具 ＋ 自打变异）

**读源**：`src/analysis/simulate2.ts:183-190`（`calorieDeficitEta`）＋ 新增件 `test/analysis-deficit-eta-466.test.mjs`（5 条）。
**本席自造夹具**（与实施席 `mkSeries` 不同构：它用「连续 keep 天」，本席用「摄入天／只有运动天／空天」交错）：

| 夹具 | 窗口 | 有摄入 | **只有运动** | 并集（摄入或运动） | `deficit` 非空 |
|---|---|---|---|---|---|
| X | 30 天 | 10 | 12 | 22 | **10** |
| Y | 30 天 | 20（隔日运动） | 0 | 20 | 20 |
| Z | 30 天 | 14（正好门槛） | 0 | 14 | 14 |

本席 10 条断言**全过**（`RESULT-PROBE-J pass=10/10`）：

```
FIXTURE-X 窗口=30 有摄入=10 只有运动=12 并集=22 deficit 非空=10
PASS J-1 13 天有摄入 ⇒ 必须降级 :: degraded=true msg=数据不足:需要 ≥14 天摄入+运动记录,当前只有 10 天摄入+运动记录。
PASS J-2 读数＝`deficit` 非空条数（有摄入的天＝10） :: 读数=10 deficit非空=10 有摄入=10
PASS J-3 读数不许是并集口径（摄入或运动＝22） :: 读数=10 并集=22
PASS J-4 读数不许是窗口天数（30） :: 读数=10
PASS J-5 门槛段与读数段量纲词逐字相等 :: 门槛「≥14 天摄入+运动记录」读数「10 天摄入+运动记录」
PASS J-6 读数段不含「或运动」（旧并集写法） :: 读数「10 天摄入+运动记录」
PASS J-7 20 天有摄入 ⇒ 不降级（「运动」不进分母） :: degraded=false avgDeficit=985
PASS J-8 正好 14 天摄入 ⇒ 边界不降级 :: degraded=false
CLI-P19 exit=4 msg=需要 ≥14 天摄入+运动记录,当前只有 10 天摄入+运动记录。
PASS J-9 页面级（第 19 页命令）降级文案与单位级同形／J-10 读数＝10，非并集 22、非窗口 30
```

- **与算法自洽（本席自己读源码核，不引它的注释）**：`series.ts:239`＝`const deficit = calories === null || calories === undefined ? null : round1(tdee + (exerciseKcal ?? 0) - calories);`
  ⇒ `deficit` **仅由「当天有没有摄入」决定**（运动只进数值、不进分母）⇒ 阈值与读数统一到交集口径「摄入+运动」，与这一支能读到的天数一致 ✓。
- **页面级**：真跑第 19 页那条命令（`calorie.view.predict {"kind":"calorie_deficit","window":"30d"}`，DB 夹具＝10 天摄入＋12 天只有运动），降级文案与单位级**同形同数**（读数 10）✓。
- **本席自打的源码级变异**（锚点命中 1 处；同一持锁窗口内 备份→变异→重编→跑红→**逐字节还原**→重编→跑绿）：

```
MUTATE  src/analysis/simulate2.ts  sha_before=a6af05b56197  sha_mutated=19f8b6310a1d  字节 14855→14857
MUT-RED J :: 实施席断言件 exit=1 tests=5 pass=3 fail=2 :: 本席夹具同量纲=false 读数=10 :: 需要 ≥14 天摄入+运动记录,当前只有 10 天摄入或运动记录。
RESTORE J 相等=true sha256(before)=a6af05b56197 sha256(after)=a6af05b56197
GREEN J :: 实施席断言件 exit=0 tests=5 pass=5 fail=0 :: 本席夹具同量纲=true 读数=10
```

> 旁证：本席自算的 `sha_before=a6af05b56197`／`字节 14855` 与实施席 m2 证据里的 `sha_after=a6af05b56197`／`字节 14855` **逐字相同** ⇒ 两席量的是同一份文件状态。

**残余观察（S3-a，不构成对裁定的违反）**：量纲词「摄入+运动记录」读起来像 AND，而实际计数是「**有摄入的天**」（夹具 Y 证明：20 天有摄入、运动只隔日 ⇒ 不降级；夹具 X 证明：12 天只有运动 ⇒ 不进读数）。这是**门槛段原有的措辞**（本票未改门槛），裁定 J 明示「修法与措辞由实施席定，但**必须与门槛口径一致**」⇒ 对齐到门槛即达标；若要更准，可另开一条把两段都写成「有摄入记录的天」。

## 十三·二 · 裁定 K（三处证据更正）—— **三处全部落地**，另发现一处计数陈旧

| 条 | 要什么 | 本席独立核对（不是读它的自述） | 判定 |
|---|---|---|---|
| K① | 调用面改成真实三处，`render/index.ts` 零命中已具名 | `commands.ts:30`（11 个名字里本件转出的 10 个）／`goal/read.ts:13`（`buildGoalPredictDoc`）／`workout/contraindication.ts:9`（`buildContraDoc`）**逐处行号＋导入串全对**；`render/index.ts` 命中 `trendDocs|trendPredict` ＝ **0**；W1 证据那一行已改并带具名更正块（引裁定 K 第 2 条） | ✓ |
| K② | 「搬迁前后 31 页逐字节相同在当刻 HEAD 不可独立复现」具名 ＋ 兜底三条 | W1 证据 §三 3.1 比对口径行下已有**限制具名块**（写明要两份编译产物、当刻树里只有搬后源码、禁切分支）＋ **兜底三条逐条具名其源**（源级 10/10 ／ 当刻态 31/31 ／ 别名同字节） | ✓ |
| K③ | J4／J5 当刻读数与归属补录，**引裁定 H 原句**（具名改期到 #567） | W5 证据 §三 C1 逐字引了裁定 H（含「**改期**：J4／J5 由 **[#567]** 承接…落点 `style.test.ts` 增字号下限守卫」），归属段逐字落在公共层内联底盘；本席当刻复跑 J4＝非图表 **10 档**／最小 **10px** ⇒ 与补录一致 | ✓（读数一处陈旧，见下） |

- **S3-b（新，记账）**：补录的 J5 行写「`@media` **19 条/页**（21／22 为 22 条；24–31 为 18–22 条）」**（W4 时点旧数；当刻值见 §十四·六）**——本席当刻实测：**01–06／18–20／23＝19；07–17＝20**（W5 新增的页面侧 `trackTableCss()` 多一条 `(max-width:640px)` 规则）；21／22＝22；**24–31 全 8 页均 18**。
  条件集合**未新增断点值**（新规则仍落在「≤640」这一档内）⇒ J5 的结论（红／未新增断点）不变，只是那个「19」是 **W4 时点**读数、且「18–22」应是 18。建议关票前把这一格改成当刻值或标注「W4 时点」。

## 十三·三 · 无回归 —— **逐条全绿**（本席自己跑）

```
STEP1 tsc -b packages/skill-calorie exit=0
STEP2-W5A exit=0 :: RESULT: 31/31 页落盘 bytes_total=2393634（别名 01/23、21/22 逐字节同）
STEP2-W5B exit=0 :: RESULT: 31/31 页落盘 bytes_total=2393634
W5-vs-实施席当刻产物 pages=31/31 DIFF=none
W5-A-vs-B(两遍稳定) pages=31/31 DIFF=none
REPORT-FAMILY(24-31) 逐页与W4相同=8/8 DIFF=none            ← 改动未溢出报告族
IN-SCOPE(01-23) 与W4不同的页=21 ⇒ 01,02,…,20,23            ← 21／22（#517 样板页）一字未动
ALIAS-W5 01/23=true 21/22=true 21/22-vs-W4=true
SEP-IN23 exit=0 :: RESULT: 23/23 :: PASS   （节点级 nonzero = none）
SEP-OUT8 exit=1 :: RESULT: 0/8 :: NONZERO = 24:6 25:7 26:7 27:7 28:7 29:6 30:5 31:12（与 W4 逐字同）
SEP-ALL31 exit=1 :: RESULT: 23/31（差的正是 24–31）
MEASURE exit=0 :: OVERFLOW-ZERO pages=31 cells=124 failed=0 scopeOutFailed=0
MEASURE-RECOMPUTE 档=390/512/820 cells=93 nonzero=0 clippedCells=0 scrollerCells=0
MEASURE-RECOMPUTE 档=390/512/820/1440 cells=124 nonzero=0 clippedCells=0 scrollerCells=0
TESTS8 exit=0 :: tests=43 pass=43 fail=0
GATE-WARN exit=0 :: RESULT: 91/91 :: PASS: 告警线台账齐全且与实况一致
GATE-GEN exit=0 :: GEN-CHECK PASS：键 128（写 46 ＋ 读 82）…未搬迁清单 0 条
J4J5-AT-W5 exit=0 :: 01-23 非图表档数=10 最小字号=10 @media=19/20/22
```

- **31 页逐页明细**（本席 W5 hash ／ 本席 W4 hash ／ 与 W4 同 ／ 与实施席同）落 `.scratch/t518-review/r10-w5-table.md`；原始日志 `r10.log`／`r11.log`。
- **版式不变量（本席加跑，打的是 W5 改动可能碰坏的形状）**：`t516-判据-版式.mjs` 三页 ×4 档（07／01／19）——三条恒出齐（conclusion=1／toc=1／caliber=2）、逐元素**越界 0**、1440 档页壳 **1120**／左右余量各 **160**、KPI 每排 `2+2／2+2／2+2／4`（19 页为 `2`）、同排极差 0；**07 页仍带状态徽章 1**（A1 的「判定词归徽标」没有把徽章删掉）⇒ J2／J6／J7／J8／J9 在 W5 产物上仍绿。

## 十三·四 · W5 窗对账（本席独立重算，只认 exit=0）—— 新增 S2 一条

对账源＝受跟踪导出件 `docs/skills/skill-calorie/t518-GATE-RUN.log`（导出时刻 21:04:02Z，窗口 `since=20:37:00Z`，47 条 START ＋ 47 条 RUN）。
本席的 W5 复审运行**全部发生在导出时刻之后**（第一条第 21:06:30Z）⇒ 下面这 47 条**全部属实施席的 W5 窗**。

| 指标 | 本席读数 |
|---|---|
| W5 窗 RUN 条目 | **47** |
| **matched（声明且 exit=0）** | **10** —— 与证据件声明的 10 条 runId **一一对上**：`fccd62ff`／`f124233b`／`6186d22c`（`w5-mutate.mjs` m1／m2／m3）、`127914f9`（`gen31.mjs`）、`9210ddf7`（`node --test` 八件）、`79320856`（`measure-responsive.mjs`）、`30871e3a`（`tsc -b`）、`06e52114`（`check-warning-line.mjs`）、`d1a32ed1`（`gen-cli.mjs --check`）、`3269801d`（`audit-separators.mjs --dir …产物-in23`） |
| 声明但 exit≠0 | **1**（`f6e8532a` 的 31 页分隔符范围外读数，证据里已具名不入账） |
| **无人声明** | **36**＝**杂务类 13**（`git add`／`git commit`／`--sync`／`--cacheinfo`／`git reset`／`check-gate-audit` 导出与对账）＋ **判据类 23** |
| 声明缺条目 | **0** |

**S2（请编排者裁）**：那 **23 条判据类**无人声明条目是 `tsc -b`（5）／`node --test`（4：其中 2 条 exit=1）／`gen31.mjs`（4）／`audit-separators`（2，含 1 条 exit=1）／`measure-responsive`（1）／`check-warning-line`（3，含 1 条 exit=1）／`gen-cli --check`（2）／`w5-stage-ledger.mjs`（1，**这条已在证据 §五按类别具名**）／`w5-fix-claims.mjs`（1）。
逐条 runId 见 `.scratch/t518-review/r14.log` 与 `r14-w5-audit.md`。
判读：**裁定 I 的放宽只覆盖「不产出判据读数的杂务运行」**，而这 23 条是它自己那一类里的「编译／测试／门禁」——按 §2.4 第 3 条②的字面口径，窗口内存在无人声明的运行条目＝对账不通过，且 `--allow-undeclared` 对这一类**没有具名授权**。
本席不据此判 S1，理由三条（都可复核）：① 47 条全部经加锁包装器、有 START／RUN 留痕（等待量可复核）；② 声明→日志方向完整（10/10，缺条目 0）；③ 实施席把 `undeclared=36` 这个数**写进了证据**（§五）而不是隐去。
⇒ 建议编排者二选一：(a) 把裁定 I 的类别扩一句「**同一窗口内对同一判据命令的重复迭代轮次**（其结果未被任何证据件声称）」；或 (b) 要求 W5 窗把这 23 条按 runId 补列。

**本席自己 W5 复审的运行条目**（同一把尺，一并声明，含 runId）：`8bf7b0bc`（`r10-w5-regen.mjs`：tsc＋两遍重出＋比对，exit 0）／`b80b463f`（`r11-w5-gates.mjs`：分隔符×3＋溢出＋八件测试＋两门＋静态，exit 0）／`e95952aa`（`r12-w5-j.mjs`：裁定 J 夹具＋CLI＋变异，exit 0）／`77f02635`（`t516-判据-版式.mjs` 三页，exit 0）／`9f100357`（`r14-w5-audit.mjs` 对账，exit 0）；
另有一次 `r14-w5-audit.mjs` 的**直调重印**（只读两个受跟踪文件、不编译不测试，故未持锁）——照实具名。

## 十三·五 · W5 复审的缺陷清单与打分

| # | 缺陷 | 归属 | 级 |
|---|---|---|---|
| 1 | W5 窗 **23 条判据类运行未逐条声明**（`--allow-undeclared` 的口径超出裁定 I 的授权范围；数字已如实报出、无隐瞒） | 本票范围（W5 窗过程记账） | **S2** |
| 2 | W5 证据补录的 J5 行「19 条/页」是 W4 时点读数（当刻 07–17 为 20；24–31 应为 18） | 本票范围（文档精度） | S3 |
| 3 | 量纲词「摄入+运动记录」读作 AND、实为「有摄入的天」（门槛段原文，本票未改门槛；裁定 J 只要求与门槛一致 ⇒ 已达标） | 本票范围（观察） | S3 |
| 4 | W5 证据 §五 把只读探针写成「未持锁」，但其中至少 `w5-fix-claims.mjs` 在加锁日志里有 START／RUN 留痕（措辞不准） | 本票范围（文档精度） | S3 |
| 5 | `t518-W4-收尾-证据.md` §四 仍以「（本件）」占位、未列 W4 两个提交 sha（协议 §3.1 要全部 sha 清单） | 本票范围（遗留） | S3 |

| 维度 | 前次 | **本次** | 变动依据 |
|---|---|---|---|
| 契约一致 | 27 | **29/30** | ＋J4／J5 已按裁定 H 具名改期并补录读数与归属、J6／J7 已补读数；＋#466 量纲已修且本席自造夹具 10/10。扣 1：J5 补录计数与当刻不符（S3-b） |
| 证据真实可复现 | 23 | **22/25** | 31 页重出与实施席当刻产物 **31/31 逐字节相同**、两遍稳定、24–31 与 W4 **8/8 相同**，全部判据我复现一致；扣 3：W5 窗 23 条判据类运行未声明（S2-1） |
| 新旧对照 | 18 | **19/20** | ＋「不可独立复现」已具名＋兜底三条具名其源（裁定 K② 落地）；W5 逐页改前→改后表可核。扣 1：那条历史判据在当刻 HEAD 仍不可复跑（属事实限制） |
| 工程红线 | 14 | **15/15** | 无切分支／reset／stash／clean；`fd30caa` 六件＋`0bda0ed` 两件，`--name-only` 逐次复核；变异三处逐字节还原后重编；本席复核前后工作区干净。6 条无人声明那条已由裁定 I 具名授权（不再计） |
| 文档同步 | 8 | **9/10** | ＋J4／J5 补录（引裁定 H 原句）、W1 两处更正落地；扣 1：J5 计数陈旧 ＋ W4 §四 未列 sha |
| **合计** | 90 | **94** | 无 S1、均分 ≥85 ⇒ **PASS** |

> W5 复审自设探针（新增，均为被审脚本覆盖不到）：**① 裁定 J 的自造夹具（含并集≠交集的判别夹具）＋ 页面级 CLI 复核**；**② 打回旧并集写法的源码级变异**（§十三·一）；**③ W5 窗对账重算**（§十三·四，含杂务／判据两类拆分）；**④ W5 产物上的版式不变量探针**（§十三·三）。
> 复审脚本：`r10-w5-regen.mjs`／`r11-w5-gates.mjs`／`r12-w5-j.mjs`／`r14-w5-audit.mjs`（`.scratch/t518-review/`，未入库）。

---

# 十四 · W6 最终核对（2026-09-16 · 裁定 N／O 落地 ＋ 两处定点判 ＋ 四条机器读数）

> **核对范围**（编排者 2026-09-16）：① W6 差异面；② 两处定点判（`verdictCard` 形状判定／21–22 产物变更性质）；③ 变异 m4–m6 与四条机器读数；④ 裁定 N 记账；⑤ 关票建议。
> 变更面（本席自己 `git show --name-only` 核）：`b3895cb` 六件（`trendPredictDocs.ts`／`test/analysis-deficit-eta-466.test.mjs`／`AGENTS.md`／W5 证据／W6 证据（新）／GATE-RUN 导出）＋ `472abfe` 一件（W6 证据）。
> **`packages/base-render/**` 在本票全部窗口（`7ca1f1c..HEAD`）零改动**——`git log --name-only 7ca1f1c..HEAD -- packages/base-render` 零输出。当刻 HEAD `342bf80`。
>
> ## **最终判定：PASS · 95/100**（契约一致 29／证据真实可复现 23／新旧对照 19／工程红线 15／文档同步 9）
> **无 S1、无 S2**；**S3 三条（均为记账级，不阻断关票）**。
> ## **关票结论：可以关票**（并同时关闭 #455／#463／#466）；建议把下列两条 S3 一并转进承接票面（见 §十四·五）。

## 十四·一 · 差异面与定点判①：`verdictCard()` **不算「自造第七种形状」**，属**公共层缺形态时的页面级退让**

**前提本席自己核过（不引它的自述）**：`packages/base-render/src/blocks.ts` 的 `interface KpiCardInput { readonly value: string; … }` ＋ `renderKpiCard` 里 `const value = reqText(card.value, …)`，而 `reqText` 对 `''` 直接 `badInput(…必须是非空字符串)` ⇒ **公共层确实产不出「没有值位的卡」**；红线又不许动 `packages/base-render/**`。

**判定：不成立「自造第七种形状」。** 基准件 §3.1 的六种形状管的是「并列／多值文本该变形成什么」（徽章列／键值行／堆叠条／进度条／小表／结论条）；W6 这一处**没有引入任何第七种可见形态** ——
- 它用的仍是**公共层的卡形态**（类名逐字沿用 `ilife-block ilife-block-kpi-card` 一族：`-label`／`-detail`／`-badge`），网格与**全部样式**仍单源住公共层；
- 判定词仍走公共层 `renderStatusBadge`（本件**没有第二份徽章**）；W6 只是把「值槽那一格」**整格省掉**，并把那一格的内容换成 `detail` 的人话（不进新数字）。
⇒ 形状词表仍是闭集六种；这一处是**公共层 API 缺形态（无值位卡）＋红线禁改公共层**两条约束下的**页面级退让**。

**但它必须按「退让记账」落账，且要点名承接**：本席核对 W6 证据 §一 —— 退让**已具名**（写了前提、改法、同族先例 `sportDocs.ts` 目标环卡），**但没进 §六／§十一 的交接清单**（那里只把「表宽 680」「390 档标签」点名给 #567，把「页 01 载荷／页 19 长句／量纲词」点名给 #520）⇒ **S3-1**：建议补一条——**公共层缺口（`KpiCardInput.value` 必填非空 ⇒ 产不出无值位卡）→ 承接 [#567](https://github.com/FeatherHunter/ilife/issues/567)**；该页框的**视觉结果**由 **[#520](https://github.com/FeatherHunter/ilife/issues/520) 终审**判（与基准件 §5.3 的公共层缺口登记同一路数）。

**本席对这条退让的独立机器读数**（不是读它的表）：真跑 31 页，01–23 **值槽里 `—` ＝ 0／页**、**可见文本 `—` ＝ 0／页**、**可见「公斤」＝ 0／页**、**可见 `KG` ＝ 0／页**；页 07 轨迹列 14 行**全部「整数.一位」**：`75.1／73.7／72.4／71.0／69.7／68.3／66.9／65.6／64.2／62.9／61.5／60.1／58.8／57.6`（§十四·四）。

## 十四·二 · 定点判②：21／22 产物变化＝**页面级文字统一**，**不是形状回退**；#517 读数是**时点读数**

本席把本席 W5 产物与本席 W6 产物逐页比：`21(451ff468680e → 87cfd3dd8da6)`、`22` 同（21≡22 仍成立）。
**差异面逐字刻画**：两页都**只差 2 行**（行数 2468 → 2468 不变），且都是同一件事——单位形态：

| 行 | W5 | W6 |
|---|---|---|
| 结论条 | 「…折算下来约 1.06 **公斤**。」 | 「…折算下来约 1.06 **kg**。」 |
| 口径行 | 「理论减重按每 7700 卡折算约 1 **公斤**估算」 | 「理论减重按每 7700 卡折算约 1 **kg** 估算」 |

**形状面本席另算了逐类命中**（W5 vs W6，两页同值）：`conclusion 1／toc 1／caliber 3／kpi-card 26／status-badge 1／data-table 43／copy-block 1` —— **逐类相同**；分隔符 21／22 仍在 `SEP-IN23 23/23 PASS` 的 23 页里（节点级 0）。
⇒ **结论：页面级文字统一（`公斤`→`kg`，同页单位一形态），不是形状回退、不是结构回退、数值一个未动。**

**对 #517 已验收读数的意义 = 时点读数（不失效，但不再指向当刻产物）**：
- #517 记的 `b76ae035…` 是**它那一刻**的产物指纹；当刻 21／22 的指纹是 `87cfd3dd8da6`（W6 又把它从 W5 的 `451ff468680e` 推到本值）。任何「按 sha 复核当刻产物」的人会看到不匹配 ⇒ 该 sha **已过期**，但作为**历史留痕**仍然有效（协议/规则 41-3「结论类评论不回头改」——不追改 #517 的结论评论，也不拿当刻产物去覆盖它）。
- 等权 **85.4** 是**在那一份产物上**打的分；本窗对 21／22 的改动只有上面 2 处单位词，**三条恒出／形状类命中／分隔符／溢出**全部未变（本席逐条复算）⇒ 分数**不因这 2 处词而失效**；**建议 [#520](https://github.com/FeatherHunter/ilife/issues/520) 终审把验收面绑到当刻指纹 `87cfd3dd8da6`**（并在终审件里写明 21／22 的 sha 沿革：`b76ae035…`（#517 时点）→ `451ff468680e`（W5）→ `87cfd3dd8da6`（W6）），**不要**再去改 #517 的原文。

## 十四·三 · 变异 m4／m5／m6 与「还原后 sha `b8c7435a40a5`」**自洽**（本席另打一遍同向变异）

- **自洽核对**：当刻 `src/render/trendPredictDocs.ts` 的 `sha256(12)=b8c7435a40a5`、`bytes=58196`、`LF=897` —— 与 W6 证据声明的「改后」三元组**逐字相同**；三处 `RESTORE 相等=true`／`sha_after=b8c7435a40a5` 因此**同指一份状态**。声明件 `sha256(12)=d500851a5810`／`21885B`／`311LF` 亦与实际**逐字相同**。
- **三条声明的留痕**：`d9e523ef`（m4）／`a16964ae`（m5）／`54b76f21`（m6）在当刻导出件里都有同 runId 的 `RUN` 行且 `exit=0` ✓（本席逐条查）。
- **本席自打的同向变异**（打的就是 W6-① 那条断言；锚点命中 1 处）：

```
MUTATE  src/render/trendPredictDocs.ts  sha_before=b8c7435a40a5  sha_mutated=efe7fadd06c3  字节 58196→58254
MUT-RED W6-① :: 断言件 exit=1 tests=8 pass=7 fail=1
RESTORE W6-① 相等=true sha256(before)=b8c7435a40a5 sha256(after)=b8c7435a40a5
GREEN   W6-① :: 断言件 exit=0 tests=8 pass=8 fail=0     （当刻件 sha12=b8c7435a40a5，与三处还原落点一致）
```

⇒ m4／m5／m6 的「必红 → 逐字节还原 → 必绿」与还原后 sha **自洽**，且**本席独立复现了同一条方向**（把值槽放回、值写 `—` ⇒ 必红）。

**四条机器读数（本席自己跑，全部经加锁包装器）**：

```
STEP1 tsc -b packages/skill-calorie exit=0
STEP2-W6A exit=0 :: RESULT: 31/31 页落盘 bytes_total=2394021（别名 01/23、21/22 逐字节同）
W6-vs-实施席当刻产物 pages=31/31 DIFF=none      ；W6-A-vs-B(两遍稳定) 31/31 DIFF=none
REPORT-FAMILY(24-31) 与W5相同=8/8 DIFF=none      ；IN-SCOPE(01-23) 变了 19 页（04–22）／未变 4 页（01,02,03,23）
SEP-IN23 exit=0 :: RESULT: 23/23 :: PASS（节点级 nonzero=none）
SEP-OUT8 exit=1 :: RESULT: 0/8（24:6 25:7 26:7 27:7 28:7 29:6 30:5 31:12，与 W5 逐字同 ⇒ 归 #519）
MEASURE exit=0 :: OVERFLOW-ZERO pages=31 cells=124 failed=0 scopeOutFailed=0
MEASURE-RECOMPUTE 档=390/512/820 cells=93 nonzero=0 clippedCells=0 scrollerCells=0（4 档 124 格亦全 0）
TESTS8 exit=0 :: tests=46 pass=46 fail=0
GATE-WARN exit=0 :: RESULT: 91/91 :: PASS: 告警线台账齐全且与实况一致
GATE-GEN exit=0 :: GEN-CHECK PASS：键 128（写 46 ＋ 读 82）…未搬迁清单 0 条
```

## 十四·四 · 自设静态普查（W6 三条缺陷的独立判据）

本席自写剥法（去 `style`／`script`／注释／标签后取可见文本）跑 31 页，01–23 逐页读数：

| 断言 | 本席读数 |
|---|---|
| 可见文本零「公斤」 | **0／0／0…（23 页全 0）** |
| 可见文本零 `KG` | **23 页全 0**（1440 档不再被 `th` 的大写转换顶成 `KG`） |
| 值槽里零 `—`（判定卡值位已撤） | **23 页全 0** |
| 可见文本零 `—`（本夹具数据完整） | **23 页全 0**（24–31 亦 8 页全 0） |
| 页 07 轨迹列 14 行「整数.一位」 | ✓（含 `71.0`——视觉抽查 §9.5 点名那一格） |

## 十四·五 · 裁定 N 记账核对（本席独立重算）与 S3

| 约束 | 本席读数 | 判定 |
|---|---|---|
| ① 被声称的 10 次运行逐条声明且 `exit=0` | 当刻导出件 `RUN` 条目 **83**；10 条声明**命中 10/10**、**其中 exit=0 的 10/10**（`0dab5d2b`／`cab0be03`／`d85e1610`／`a94d1f4e`／`d0d1c8d0`／`b51c1fd2`／`eacf17d2`／`d9e523ef`／`a16964ae`／`54b76f21`） | **满足** |
| ② 未声称的重复迭代 13 条按「类别＋条数＋日志指针」具名、runId 留在导出件里 | 13 条**命中 13/13**；其 exit 分布**与证据逐条一致**（`8e965262`=1／`e26aee0a`=0／`037756e2`=0／`05fae4bd`=1／`f410a270`=1／`4dc12c42`=0／`48daa548`=0／`4a94de41`=0／`6bbae2cd`=0／`115a4003`=0／`48871837`=0／`9ead1591`=0／`aec90580`=1） | **满足**（13/13） |
| ③ 累积导出件不丢旧 runId | W5 导出件 47 条**一条不丢**（47/47）；**但 W1–W4 的 51 条 runId 不在当刻导出件里**——那一步发生在 **W5 窗**（`0bda0ed` 把窗口切成「W5 起点→」；当刻件头写明「累积窗口 since=20:37:00Z」）。可由 `git show a967506:docs/skills/skill-calorie/t518-GATE-RUN.log` **逐条追回**（本席实测 51 条全在历史版本里） | **W6 窗内满足**；**跨窗不完整（S3-2）** |
| 本窗全量对账（本席跑它的工具） | `RESULT: matched=10/10 auditEntries=234 scoped=30 undeclared=20`（until=21:30）／`scoped=35 undeclared=25`（until=21:45）＋ `gate-audit: PASS`；**那 20 条无人声明全部已被证据件按类别具名**（13 迭代 ＋ 2 范围外 ＋ 2 杂务 ＋ 3 提交后自查），我的复跑多出来的 5 条是**本席自己**的复审运行 | **满足（含放宽声明）** |

**S3-2（记账）**：`4a7ab0f9`（提交后台账门复跑）／`e30936b0`／`97948e80`（两次门禁对账，自指）三条的 `RUN` 行**不在受跟踪导出件里**（导出在 21:26:08 落盘、这三条在其后跑），只在未受控 `.scratch/locks-m158v2/gate-runs.log` 里（证据 §九 延伸段表**已具名 runId ＋ 日志指针**，未隐瞒）。同理，导出那一条自身（`09e7f73a`）在导出件里只有 `START` 没有 `RUN`（先跑后记的自指效应，证据亦已具名）。
⇒ 若编排者要求「导出件即完整对账源」，建议关票后由 #519 窗顺手把这三条并进下一次导出；**不影响本窗判定**（都被具名、都有指针、都不是被声称的门禁证据）。

**本席自己的 W6 复审运行**（同一把尺，逐条具名）：`af85bea8`（`r15-w6-regen.mjs`：tsc＋两遍重出＋全判据批，exit 0）／`r16`／`r17` 两个脚本的两次包装器运行（`r16-w6-audit.mjs` 对账、`r17-w6-probes.mjs` 静态普查＋变异，各 exit 0）＋ 若干次**只读直调**（`node -e` 读 JSON／文件比对，不编译不测试、未持锁）。

## 十四·六 · W6 缺陷清单与最终打分

| # | 缺陷 | 归属 | 级 |
|---|---|---|---|
| 1 | `verdictCard()` 退让**已具名但未点名承接票**（应在 #567 记「公共层无值位卡形态」、由 #520 终审视觉） | 本票范围（记账） | S3 |
| 2 | 当刻导出件**不含 W1–W4 的 51 条 runId**（W5 窗切窗口所致，历史版本可追回）＋ 3 条提交后自查的 `RUN` 行不在导出件里 | 本票范围（对账源完整性） | S3 |
| 3 | `t518-W4-收尾-证据.md` §四 仍以「（本件）」占位、未列 W4 两个提交 sha（协议 §3.1 要全部 sha 清单） | 本票范围（遗留） | S3 |

| 维度 | W5 复审 | **W6 最终** | 依据 |
|---|---|---|---|
| 契约一致 | 29 | **29/30** | W6 两条真缺陷修到根（值槽整格不存在／表头真渲染 `kg`）＋一处补齐到列级；01–23 分隔符 23/23、124 格溢出 0、三条恒出与形状类命中在 W5→W6 未变。扣 1：退让未点名承接票（S3-1） |
| 证据真实可复现 | 22 | **23/25** | 重出 `31/31 bytes_total=2394021` 与实施席当刻产物 **31/31 逐字节相同**、两遍稳定、24–31 与 W5 **8/8 相同**；八件 **46/46**；两门 91/91＋GEN-CHECK PASS；裁定 N 的 13 条迭代 13/13 在导出件里且 exit 分布逐条一致。扣 2：4 条 `RUN` 行不在受跟踪导出件里（S3-2）＋ W1–W4 累积缺口 |
| 新旧对照 | 19 | **19/20** | 21／22 的 W5→W6 差异**逐字刻画为 2 处单位词**、形状逐类相同；W6 逐页改前→改后表可核。扣 1：W1 历史判据仍不可复跑（事实限制，已具名） |
| 工程红线 | 15 | **15/15** | `packages/base-render/**` 全窗口**零改动**（本席独立核）；W6 提交面六件＋一件，`--name-only` 逐次复核；三处变异逐字节还原后重编（本席另打一遍同向变异亦还原到 `b8c7435a40a5`）；本席复核前后工作区只余别席在途件（`t541-墙生成器.mjs`，未进本席暂存区） |
| 文档同步 | 9 | **9/10** | 裁定 O 的 J5 计数已改当刻值（本席复算：01–06／18–20／23＝19、07–17＝20、21／22＝22、24–31＝18，与更正逐页一致）、裁定 N 记账齐、裁定 P 只登记。扣 1：S3-1 ＋ W4 §四 遗留 |
| **合计** | 94 | **95** | 无 S1、无 S2、均分 ≥85 ⇒ **PASS** |

**关票建议（一句话）**：**可以关票**——本票交付面（01–23 分隔符节点级全零、31 页 ×3 档溢出 0、八件 46/46、两门 91/91＋GEN-CHECK PASS、31 页逐字节可复现、24–31 无溢出）本席均已独立复现，两条真缺陷修到根且**我自己打了一遍同向变异证明判据有识别力**；关票时一并关闭 #455／#463／#466，并把 §十四·六 的 S3-1（→#567／#520 面）与 S3-2（对账源累积口径）写进承接票面即可，**无必须在关票前落地的项**。
