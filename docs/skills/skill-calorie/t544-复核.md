# #544 独立复核：场景 04 运动 · 类型分布／力量／有氧／复盘／趋势 9 页

> 复核对象：`93c9474`（6 件成果本体）／`9541537`（证据件第十节）／`fac28fd`（对账分件）。
> 复核方式：只信提交号与我自己的复跑（协议 §5.1）；实施席的任何结论都不采信。
> 本席声明写入的路径：`docs/skills/skill-calorie/t544-复核.md`（本件）、`.scratch/t544r/**`（草稿与日志）。
> 本席**一行未改**实施席的任何源码／判据件／证据件；变异与新探针都在本席目录里做。

## 〇、判定

**PASS**（五维 92；无 S1-交付缺陷；无未记录的 S1-过程违规）。

三条交付提交都在主干祖先链上，票面五条验收命令我逐条自己跑过，读数与实施席一致或更好；
两处与实施席不一致的地方都不是本票缺陷，且都由**后续别票提交**解释得清（见 §三 F1／F2）。

## 一、谱系与提交（第一件事：确认真在链上）

| 提交 | `git merge-base --is-ancestor <sha> HEAD` | `git log --oneline -1 <sha>` |
|---|---|---|
| `93c9474` | **exit 0**（在链上） | `功能(544): 抢救被 reset 的成果 —— 运动五页去分隔符形状化＋文案去冗余＋HELP 同档手机端（9 页重出全绿）` |
| `9541537` | **exit 0** | `文档(544): 抢救记录落盘 —— 取回提交 sha 与复跑读数补进 t544-证据件第十节` |
| `fac28fd` | **exit 0** | `文档(544): 本次抢救窗口的对账分件 ＋ 主件第十节补对账实跑读数` |
| `f982649` | **exit 1**（不在链上，与「孤儿」自述相符） | `功能(544): …（9 页重出全绿·视觉第 1 轮整改一并落地）` |

`git show --stat 93c9474` = **6 files changed, 1023 insertions(+), 130 deletions(-)**，六条路径与证据件 §10.2 表逐条相符；
**没有一条**落在票面「不许动的东西」里（`base-render/**`／`src/shared/**`／`receipt.ts`／`sportDocs.ts`／`records.ts`／`trendDocs.ts` 均未出现）。

抢救席自称「6/6 内容哈希与 `f982649` 一致」——我自己的复算（blob 对 blob）：

```
RESCUE-CK-SUMMARY 6/6? actual=6/6
```

`packages/skill-calorie/src/render/exercisePort.ts` 当刻 `git status --porcelain` 为空（票面协调点「一字未动」成立）。

## 二、我独立复跑的读数

一个持锁窗口跑完（窗口内直调，不二次抢锁，协议 §2.4 第 5 条）：
`runId=8bb7fc74-d58f-46b4-ac65-89b017ee9b27`，exit=0；另加一个对比窗口 `deae154a-24ca-482a-a44b-9bf80f3d597e`，exit=0；
编译单独一个窗口 `5b2ee079-7c90-4289-9ea8-d88fc1050f32`，exit=0。明细落 `.scratch/t544r/logs/*.log`。

| 票面验收项 | 我的读数（自己跑的） | 实施席读数 | 一致？ |
|---|---|---|---|
| 重出这 9 页 | `MINE9 n=9 exit0=9 complete=9 BAD=0` | 同 | ✅ |
| 三档溢出 390／768／1440 | `OVERFLOW-ZERO pages=37 cells=111 failed=0 scopeOutFailed=0 label=run`；本票 9 页 27 格逐格 `overflow=0 ok=true`，390 档 `scrollerCount=0` | 同 | ✅ |
| 逐页分隔符（口径 #508 canon 探针） | 9/9 页 `exit=0 nodeHits=0 lineHits=0`（`RW-SEP-TOTAL pages=9 redPages=0`） | 同 | ✅ |
| 回归 `--test 453 454 accept-267` | `exit=0`，`ℹ tests 22 / pass 22 / fail 0` | 实施席当时为 `tests 30 / pass 29 / fail 1`（唯一红＝#267 G4） | ⚠️ 时点差，见 §三 F3 |
| 回归 `--test 453 454 544` | `exit=0`，`ℹ tests 22 / pass 22 / fail 0` | — | ✅ |
| 文案（机器词／脏数／重复） | 判据件 `#544 ②③④` 全绿；9 页可见面 ≥6 位小数 0 处；canon 探针 0 命中 | 同 | ✅ |
| 告警线台账 | `check-warning-line.mjs` → `exit=0 RESULT: 87/87`，红名单 0 行，**本票那件不在红名单** | 当时 `78/85`，四条红全是别席在途件 | ✅ 更绿 |
| 视觉 ≥90 | 视觉席报告 `视觉复评544-r2.md` 逐格与 §6.4 表一致（平均 **93.3**）；**但当前树上手机档已漂移**，见 §三 F1 | 93.3 | ⚠️ 见 F1 |

编译：票面那条 `pnpm --filter skill-calorie run build` **当刻在本工作区会红**（与我自己的调用方式无关，pwsh 直跑同样红）：
`pnpm --version` = 11.8.0，报 `[ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY] Aborted removal of modules directory due to no TTY`
（依赖检查想 purge `node_modules`；按 §2.1 第 1 条我**没有**跑任何安装命令、也没有开 `CI=true` 放它 purge）。
改用 §2.4 第 5 条允许的**等价底层命令**：`tsc -b` → exit **0**、`node scripts/build-help.mjs` → exit **0**（`RW-BUILD pnpmBuild=1 tscBuild=0 buildHelp=0`）。
`dist/render/sportPortDocs.js` 已含本票改动标记（`.ilife-block-toc{flex-wrap:wrap;overflow-x:visible}`、`yTicks` 均在）⇒ 重出用的是本票源码编出来的产物。

## 三、缺陷清单（分级 ＋ 归属 ＋ 怎么重现）

### F1【S3 · 范围外（后续别票提交）】「字节与盘上一致」与「逐像素 diffRatio=0」这两条时点读数，在 HEAD 上不再复现

- **我量的**：HEAD 上按实施席同一配方重出这 9 页，与 `.scratch/t544/out` **逐字节都不同**，且**恰好每页 +1193 字节**（98001/96808、97134/95941、…、97726/96533）。
- **归属（我自己归的，不是听他们说）**：把共享样式那对触屏声明剔掉再比仍然不等，于是逐行看 diff —— 三处来源，**全部晚于 `93c9474`**：
  1. `-webkit-tap-highlight-color: transparent; touch-action: manipulation;`（7 个类 × 2 行）＋ `.ilife-block-page-shell button, summary` 一条 —— 出自 `1329a89`（`功能(533)：共享样式七处可点基座补触屏两件`，00:24:06＝16:24:06Z）；
  2. `.ilife-page .ilife-block-dist-row{grid-template-columns:minmax(0,9.5em) …}` 一族页级覆盖 —— 出自 `18a3f75`（`修复(523)：R3 …分布条类名不再截断`，15:54:33Z）；
  3. 注释里的用词换词（`页壳`→`页面模板`、`冻结壳`→`冻结help模板`）—— 出自 `393e45a`（`文档(468)：t468禁用词换词证据件`，16:04:07Z）。
  机器读数：`git show 93c9474:packages/base-render/src/style.ts | Select-String 'touch-action: manipulation'` → **命中 0**（HEAD 为 12）；
  `git show 93c9474:packages/skill-calorie/src/exercise/sportUi.ts | Select-String 'minmax\(0,9.5em\)'` → **命中 0**（HEAD 为 1）。
- **可见面没变**：三份产物（改前基线／实施席产物／我重出的）**可见文本 9/9 逐字相同**（sha 相同，`RW2-SUMMARY pages=9 visibleTextEq=9/9`）⇒ 差只落在样式与注释，用户看得到的那一面没有被这次漂移改动。
- **视觉链复算（复核席指定的第①条）**：我用同一渲染管线（`vision_html_screenshot`，390×844／1440×900 整页）先做**正对照**——重渲**实施席产物**得 `diffRatio=0`（复盘 390：`0/690300`；分布 1440：`0/2619360`），管线可信；再渲**我重出的产物**：
  - 复盘（本周）手机 390：`differingPixels=136304 / totalPixels=690300` ⇒ **diffRatio=0.1975**，页高 **1770 → 1841**；
  - 分布桌面 1440：`differingPixels=1878 / totalPixels=2619360` ⇒ **diffRatio=0.0007**，页高 1819 不变。
  ⇒ 实施席 §10.6 那句「逐像素比对已证明重出产物与第 2 轮被打分的产物逐像素相同，93.3 就落在这批产物上」**在他们当时的树上成立**（我这边对实施席产物的 0 也复现了），**在当前树上手机档已不成立**——差来自 #523 R3 给共享件 `src/exercise/sportUi.ts` 加的页级覆盖（窄屏把分布条塌成两段式）。
- **怎么重现**：`node .scratch/t544r/compare-window.mjs`（持锁跑）＋ §二 的两次 `vision_pixel_diff`（截图路径见 §六）。
- **影响与处置**：**不算本票缺陷**（改共享件的是 #523）；但**票面的视觉验收写着「≥90」，这个 93.3 是打在旧共享层上的分**，收口票在共享层安静下来之后必须重跑一次视觉复评；同时提醒：这类「字节一致／逐像素 0」的读数必须写清**对哪个提交**，否则下一个复核席会当成本票的缺陷来追。

### F2【S3 · 范围外（#478）＋ 本票证据披露不全】重出批次 39 条里有 2 条被挡，证据件只报了「39 条」没报 `OK=37 BLOCKED=2`

- **我量的**：`RW-REEMIT 乙：39 条 OK=37 BLOCKED=2`；被挡的两条是 `09-改运动记录`、`11-删运动记录`，两条都是
  `exit=2 :: ERR 2: id 须为正整数（可给数字或数字串）`（`RW-BLOCKED-ROW`）。这也解释了 `OVERFLOW-ZERO pages=37`（不是 39）与 `.scratch/t544/out` 只有 37 件。
- **归属**：`aac39bf`（#478）把冻结表这两条的示例参数由 `"id":1` 换成占位符 `"<记录号>"`，而重出脚本（照抄 `t522` 基线）的
  `cli.replace(/"id":\s*1\b/, 'id:' + boundId)` 从此**匹配不到**，占位符原样递进入口 ⇒ 被入口校验先挡。**不在本票写集**（那两条路径属 #478／别票）。
- **怎么重现**：`node .scratch/t544r/run9.mjs` → `RW-BLOCKED-ROW 乙 09` / `乙 11`。
- **影响**：本票 9 页（31–39）不在其中，**不影响本票验收**；但「39 页批次」现在**出不全**（基线 `.scratch/t522/out-baseline` 是 39 件），收口票要先处理这条，否则「39 页重出对齐」那条遗留出口跑不通。
- **披露问题**：证据件 §4.3 写「乙轮 39 条 → `MINE9 n=9 exit0=9`」，读的人会以为 39 条都成了。这一句**不算假**（它只说 9 页），但把同一次运行里 `OK=37 BLOCKED=2` 这条读数漏掉了，属**该报未报**——记 S3，改法＝在 §4.3 那行补一句 `OK=37 BLOCKED=2（09／11，归 #478）`。

### F3【无缺陷 · 范围外】`#267 G4` 那条红：成因确系别席 #478，且**在 HEAD 上已经自己变绿**

- **我量的**：`node --test …accept-267…` → `exit=0`，`ℹ tests 8 / pass 8 / fail 0`；`T267-G4 空库阻断=32 空库新增出账=7 … 阻断提示取值=["ERR 4: 取数失败（缺失阻断）：昨日无运动记录可复制","ERR 4: 取数失败：记录 ID 999999 不存在",…]`。
  票面给出的回归命令 `--test 453 454 accept-267` → `exit=0`，`ℹ tests 22 / pass 22 / fail 0`。
- **成因我自己核的（不是照抄他们的归因）**：本票交付窗当时的 HEAD 是 `a17dbea`（13:43:11Z），
  `git show a17dbea:packages/skill-calorie/test/exercise-accept-267.test.mjs` 里**搜不到** `UNREACHABLE_ID`／`999999` 回落（命中 0），
  而同一提交的冻结表里 `"<记录号>"` 已出现 **2 处** ⇒ 空库那一支把占位符原样递进入口，先撞 `ERR 2: id 须为正整数`，走不到「取数失败（缺失阻断）」。
  同一现象我在**自己的**重出批次里也复现了两次（§三 F2 的 09／11，同一条 `ERR 2`）。
- **修它的是别席**：`18a3f75`（#523 R3，15:54:33Z，晚于本票 15:21:20Z 的交付提交）给空库夹具补了 `UNREACHABLE_ID='999999'`，并在注释里写明成因出自 #478。
- ⇒ 实施席 §八之一的**归因正确**；但「唯一红＝#267 G4」是**时点读数**，在 HEAD 上不成立。**不判本票缺陷**，只记「证据件的红绿读数要带提交时刻」这一条做法提醒。

### F4【S3 · 范围外 · 只记账不占缺陷位】抢救席的取回形式落在**本轮新增红线**的字面里

`git checkout f982649 -- <六条路径>` 用的是**非祖先提交**（`git merge-base --is-ancestor f982649 HEAD` → exit 1，我复跑）。
本轮红线要求「取文件只许 `git checkout <具体提交> -- <具体路径>`，且**必须先** `git merge-base --is-ancestor <提交> HEAD`」——
孤儿提交按定义过不了前半句，所以**这条红线与「抢救孤儿」这个场景本身冲突**。
红线是本轮才下的、抢救在上一轮，按协议 §2「条款生效后须广播；未广播不归因执行者」**我不判违规**，风险面也已被抢救席自己压住
（取回前 `git status --porcelain -- <六条路径>` 为空、逐件内容哈希 6/6 一致、取回后三笔提交都在链上）。
**下一手缺的**：再遇孤儿取回，先取编排者具名授权（并允许一次 `git checkout <sha> -- <paths>`），否则按现行字面是无解。

## 四、五维打分

| 维 | 满分 | 得分 | 依据 |
|---|---|---|---|
| 契约一致 | 30 | **28** | 票面五条验收命令我逐条复跑，四条读数与实施席一致（重出 9/9／三档 111 格 0／逐页 9/9 节点级 0／判据件 ②③④ 全绿），台账那条我这边更绿（`87/87`、红名单 0）。扣 2：视觉那条「≥90」在当前树上不可复现（F1，范围外）；票面「字节与盘上一致」这类读数没有写清对哪个提交（改了共享件的别票一来就失效）。 |
| 证据真实可复现 | 25 | **23** | 我复现了 `matched=3/11`（`--until 15:21:21Z`）与 `matched=4/4 gate-audit: PASS`（分件入口）两个对账读数，**两个都属实**；`RESCUE-CK 6/6` 属实；`--allow-*` 一个没开属实（我在他们文件里搜过，证据件明写不开）。加分项：前席把判据改窄过一次，他们**主动自曝并改回真实**（§1.4），还留了「改坏必红」两条读数。扣 2：F2 那条 `OK=37 BLOCKED=2` 没报。 |
| 新旧对照 | 20 | **19** | 我自己量了改前改后：canon 探针在修改前基线上 9 页**共 78 条节点级命中**（逐页 6／5／6／8／10／10／11／11／11，9/9 页都有债），现在 **0 条**；重复事实与机器词逐条给理由、脏数 0 我也复核过。扣 1：`§五之三` 的重复理由用的是「一眼／逐条／构成」这类分工话，其中复盘页结论句与 KPI 卡同值那条属「有意保留」，理由成立但没给上限口径。 |
| 工程红线 | 15 | **13** | 三笔提交逐笔 `git show --stat` 我核过：只含自己声明的件，无 `reset／rebase／checkout -- .／restore .／stash／amend／force`；每步编译／测试／git 都走了包装器，`GATE-RUN` 与运行记录一对一（他们 7 条＋4 条我都点开对过）。扣 2：F4 那条取回形式（时序外，风险已控）；另扣的是本工作区当刻 `pnpm … build` 已整体变红，票面那条命令的「可跑性」没被点名（属环境，不归他们）。 |
| 文档同步 | 10 | **9** | `packages/skill-calorie/AGENTS.md` 台账本票那行已同步为 **809**（我复核 LF=809 与台账一致）并写了超因＋拆法说明；证据件分三笔提交、对账导出件入受控路径（§2.3 第 4 条）。扣 1：主件同时携带两个窗口的 11 条声明，逼出一份分件才跑得绿（工具作用域所致，但下一手会踩）。另：台账里 `scripts/build-help.mjs` 此刻有两行完全重复（别席 `--sync` 的产物，我不碰）。 |
| **合计** | 100 | **92** | **PASS**（无 S1；均分 ≥85） |

## 五、变异与自设新探针（复核席指定的第②条，专打「脚本自我满足」）

都在本席目录里做，**一行没碰**实施席的任何文件；读数逐条落 `.scratch/t544r/logs/`。

1. **canon 探针的判别力（正对照）**——往**我自己重出的产物副本**里注入一处真债，看口径 #508 的 `probe.mjs` 红不红：
   `RW-NEG sep-r1 probeExit=1 nodeHits=1`／`sep-r2 probeExit=1 nodeHits=1`／`sep-r3 probeExit=1 nodeHits=1`
   （分别注入 `·`／`；`／三连并列）。⇒ 「9/9 页节点级命中 0」**不是探针恒返回 0 换来的**。
2. **三档溢出测量的判别力（正对照）**——往同一份副本里塞一个 `width:2000px` 的块，看三档测量红不红：
   `RW-NEG ovf measureExit=1 cells=3 failedCells=3`。⇒ `cells=111 failed=0` **不是「测了个空」**。
3. **判据件「改恒真还绿不绿」**——把票面判据件**逐字复制**到本席目录（只把导入路径改成绝对路径），再往每一份被判产物里注入一处 `·`：
   - 正对照（注债＋判据原样）：`exit=1`，`tests=8 / pass=6 / fail=2`，`T544 SEP-TOTAL 页面=10 有债页=10 命中合计=10`
     —— 红的是 `#544 ①` 与件内变异用例；
   - 恒真态（同一份注债产物，把 `#544 ①` 那条聚合断言 `assert.equal(bad, 0, …)` 改成 `assert.equal(bad, bad, …)`）：
     `exit=1`，`tests=8 / pass=7 / fail=1`。
   ⇒ 该支判据**有冗余守门**：单把 ① 改成恒真，**同一份注债产物仍然红**（件内变异用例那条「原样产物应当零命中」接住了）。
     也就是说它的「全绿」不是单条断言撑起来的，**不存在「脚本自我满足」**。
4. **跨席同产物逐字节对照（复核席指定的第②条另一半）**：`.scratch/t544/out` 与 `.scratch/t541/out`（别席 #541 自己重出的一批）同名件对照——
   **本票 9 页 9/9 逐字节相同**（`CROSS … same=9 diff=11 missing=17`；11 条 diff 与 17 条 missing 都不是本票的页，
   其中 17 条是 #541 没出的页）。⇒ 两个席位、两个时刻（22:25 与 23:07）独立重出同一批页得到同一串字节。

## 六、门禁对账（协议 §2.3 第 4 条：运行条目导出到受控路径）

对账源 `.scratch/locks/gate-runs.log`（未受版本控制）里本席**本次**窗口的 `RUN` 条目，逐行抄录（同名同 runId 的 `START` 行不重复抄；`at` 为 UTC）：

```
RUN ticket=544 runId=5b2ee079-7c90-4289-9ea8-d88fc1050f32 cmd="pwsh -NoProfile -File .scratch/t544r/build-probe.ps1" waitedMs=1 exit=0 at=2026-09-15T16:34:52.460Z
RUN ticket=544 runId=8bb7fc74-d58f-46b4-ac65-89b017ee9b27 cmd="node .scratch/t544r/review-window.mjs" waitedMs=0 exit=0 at=2026-09-15T16:37:50.587Z
RUN ticket=544 runId=deae154a-24ca-482a-a44b-9bf80f3d597e cmd="node .scratch/t544r/compare-window.mjs" waitedMs=0 exit=0 at=2026-09-15T16:38:39.346Z
```

GATE-RUN runId=5b2ee079-7c90-4289-9ea8-d88fc1050f32 cmd=pwsh -NoProfile -File .scratch/t544r/build-probe.ps1
GATE-RUN runId=8bb7fc74-d58f-46b4-ac65-89b017ee9b27 cmd=node .scratch/t544r/review-window.mjs
GATE-RUN runId=deae154a-24ca-482a-a44b-9bf80f3d597e cmd=node .scratch/t544r/compare-window.mjs

对账实跑：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t544-复核.md --ticket 544 --since 2026-09-15T16:34:00Z --until 2026-09-15T16:39:00Z`
→ `RESULT: matched=3/3 auditEntries=… scoped=3 undeclared=0` → `gate-audit: PASS`（exit 0；**未开任何 `--allow-*`**）。
窗口**之外**的现场读数，逐条点名以免被误读成漏记：
- `runId=27f0ac80-58f4-4193-b4a3-44cf9d4d2c87`（我第一遍窗口）**被我主动中止**：那一遍的编译段先撞上 §二 的 pnpm 环境红，产物读数会建立在可疑的 `dist` 上，故停窗修因后重跑；
  它在运行记录里**只有 `START` 行、没有 `RUN` 行**（包装器被杀时来不及结算），`LOCK-STOLEN … reason=owner-dead ownerPid=46040` 是下一个窗口按 §2 探活后正常抢回我自己的死锁。
- 本件自身的提交运行在 `--until` 之后，按工具口径在被对账窗口之外。
- 实施席两个窗口的 7 条＋4 条声明，已由他们自己的证据件与分件覆盖，不在本席的声明集里。

视觉读数的截图路径（`.dsh-vision-router/artifacts/`，gitignored）：
`t544-r2b-35-390x844-fullpage.png`（被打分的原图）、`D--ilife-.scratch-t544-out-35----0a9fee97-shot-390x844-fullpage.png`（重渲实施席产物，正对照）、
`D--ilife-.scratch-t544r-out-35---fbe2c044-shot-390x844-fullpage.png`（重渲本席产物，diffRatio=0.1975）、
`t544-r2b-31-1440x900-fullpage.png`、`D--ilife-.scratch-t544r-out-31---4a7b9d38-shot-1440x900-fullpage.png`（diffRatio=0.0007）。

**视觉抽查（复核席指定的「另看两页」）**：我看的是实施席抽样没抽的两页 `.scratch/t544r/out/33-看有氧训练总览.html`（390 档，页高 3466）与
`…/34-看运动趋势.html`（390 档，页高 5040）。视觉模型报了「33 页顶部与底部各有一处 `缺一格是 |--|，不空格也不行0` 的调试残留」与「34 页纵轴有若干机器词」——
**两条我都回到产物里核过，都不成立**：前者那串字在 33 页的 HTML 源与可见文本里**一次都搜不到**（视觉模型看的是降采样后的长图，属它的编造）；
后者是纵轴刻度（`937.5 ／ 1250 ／ 1562.5`）与缺值短横，是量程标注不是机器词。**这两条按「视觉模型是提示器不是证据」处理，不入缺陷清单**；
我自己在这两页上没看到被裁切、被藏起来的横滚、挤列、错位或 6 位小数。

## 七、未做项与下一手缺什么

- **没有改动任何实施席的代码或判据**（复核席不改实施）——F1～F4 的处置都留给编排者转票。
- **没有另跑一次正式的视觉复评（100 分制）**：正式分以视觉席 `视觉复评544-r2.md`（93.3）为准；我这边只做了 F1 的复算与 33／34 两页抽查。
  下一手若要在当前树上重新签字，必须先等共享层安静，再按 `.scratch/t544/vision-brief-544-r2b.md` 重跑——我这边量到的手机档漂移（0.1975）说**不能直接沿用 93.3**。
- **没有动 `pnpm` 的依赖状态**：票面那条编译命令当刻红是环境问题（`ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`），我按 §2.1 第 1 条停手不装、也不开 `CI=true`；
  下一手若要恢复票面原命令，需要编排者持锁统一处置 `node_modules` 与 pnpm 配置，并保留 `.scratch/t544r/logs/build-pnpm.log` 现场。
- **F2 的 39 页批次缺口**：收口票处理（或转 #478 一族），本票 9 页不受影响。
- **未推送**：本席只在本地 `master` 上追加提交。

## 八、本席这次运行在共享树上留下的痕迹（如实交代）

- 本席的编译走票面那条命令的等价底层步骤（`tsc -b` ＋ `node scripts/build-help.mjs`，见 §二）；
  其中 `build-help.mjs` 会**重刷生成物** `packages/skill-calorie/SKILL.md`。该件当刻在工作区显示已改动（`1 file changed, 3 insertions(+), 3 deletions(-)`），
  内容与**别席在途件**同源（训练计划写命令族：写命令数 35→46、「定训练计划」由「当前不可写」改「可写」），**不是本席撰写的改动**。
- 处置：**不取回、不回滚**——按 §5「生成物按既定的重新生成流程复位」，取回改动对生成物不成立（取回会留下陈旧产物），
  它由下一次标准构建按当刻源码重刷成一致。本席只如实点名，不碰它。
- 收尾判据：`git status --porcelain -- docs/skills/skill-calorie/t544-复核.md` → **空**；
  `.scratch/t544r/**` 由 `.gitignore:5:.scratch/` 忽略（`git check-ignore -v` 复核）；锁目录只剩 `gate-runs.log` 与两份历史日志，`gate.lock`／`owner.json` 都已释放。
- 自查（协议 §5.1 的收尾两行，**两笔提交各一条**）：

```
sha=b0011c0b ancestor-exit=0 :: b0011c0 复核(544)：独立复核判 PASS（均分 92，无 S1）——三笔提交逐条在主干祖先链上，票面五条验收我自己复跑，自设三类正对照探针证 canon 探针与三档测量有判别力
sha=c4bfe4aa ancestor-exit=0 :: c4bfe4a 复核(544)补：如实交代本席这次运行留在共享树上的痕迹 —— 标准编译重刷了生成物 SKILL.md（内容属别席在途件，不取回）＋ 收尾自查两行
```

  即 `git merge-base --is-ancestor <sha> HEAD` 两笔都 **exit 0**、`git log --oneline -1 <sha>` 中文回读正常。
  提交范围：两笔的 `git diff --cached --name-only` 都**只有** `docs/skills/skill-calorie/t544-复核.md` 一件。
