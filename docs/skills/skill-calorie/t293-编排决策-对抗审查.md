# #293 编排决策 · 对抗式审查（d-review 席）

- **被审**：编排者当刻六条决策（D1–D6），逐条判「帮目标／推远目标／需整改」。
- **判据的锚**：`#293` Destination（一条命令的事实只住 `src/<能力>/commands.ts`；汇总位由 `pnpm gen` 派生、`pnpm gen:check` 守；两道机器门）＋ `#320` 票面的五条绿线。
- **当刻基线**：`HEAD=fb72a31`（工作区除判官外干净）；判官 `docs/skills/skill-calorie/t293-验收-命令自治.mjs` 为**未提交脏件**
  （`git diff --stat HEAD` ＝ `+124/−10`，sha256 `F9162332F068E1166B24E1C72C9152F568C9C61F9C5B8824921D08B2CA700633`）。
- **本席纪律**：全程只读仓内文件；命令只走 `node tooling/run-locked.mjs --ticket d-review --lock-dir .scratch/locks-land --stale-minutes 30 -- …`
  （门禁窗口 `runId=7b09db82-f0ab-40df-ba5f-0c47bcca314a`，`exit=0`）；自造脚本与大输出落 `.scratch/d-review/`；未碰任何票面。

## 一 · 六条判定

| 决策 | 判定 | 一句话理由 | 机器证据 |
| --- | --- | --- | --- |
| D1 形状守卫（否掉 ④≥60 阈值 ＋ 消两处静默出口） | **需整改** | 方向对（消静默是对的、阈值确实是错药），但它守在一个**已经失效的锚集**上，而守卫本身可被"中性命名"绕过 | 判官 `shapeGuard()` L436-453；锚 `totals={"total":10,"writes":4,"reads":6,"weight":9}`（探针自报）vs 真实权威 **101** 条命令（`keys.ts` 101 条、写 35）；`GUARD_pending=false flagged=0`（当刻 ④=81，不误黄） |
| D2 `#318` 证据返修 ＋ 定向复评，不放宽 85 线 | **帮目标** | 首轮 82 分本身违反 §6 量刑律；返修后数字可复现、复评 95 ⇒ 无需动线，动线才是破契约 | `2137e93` 盘实测 `cmd_read 1146 行/61 case`、`write 745 行/34 case`（本席 node 口径复算，逐值一致）；报告 §11.4 82→95 |
| D3 两侧 `reason` 数据串本窗不动、过期散文转跟进小票 | **需整改** | "不动"对（动一侧必红、动两侧破生成物），但**"跟进小票"当刻不存在**，且把**数据串**叫成"散文"，下一个席位可能照"散文"去改 | `src/cli/legacy/routes/scene-08.ts:11-12` 的 `reason:` 与 `src/triggers/routing.ts:75` 同值；冻结测试 `test/calorie-routing-81.test.mjs:93` 要求 `reason` ∈ `Object.values(NON_EXEC_REASONS)`；`gh issue list` 无任何 open 票 body 提及「过期散文／95 键／build-help.mjs:244／SKILL.md:186」 |
| D4 规则 38：回滚重建派生面 ＋ 树干净含 `dist`／印记 | **需整改** | 事故真实、方向对，但只写 `tsc -b … --force` 会**重编不留印记**，回滚后留下 `GEN-STALE` 假红 | `package.json:"build" = "tsc -b && node …/gen-cli.mjs --stamp"`；印记只由 `--stamp` 写、`staleDeclarations()` 逐声明源比内容 ⇒ 源码一旧即 `GEN-STALE FAIL`；规则 38 自证见 `.scratch/t314-319/编排状态.md` L99（`--force` → `git diff --name-only` 0 行 → 冻结文件 8/8） |
| D5 本图不引结构改动，开 `#327` 造四个件 | **帮目标**（补归属） | Destination 不含"分片派生物"；`#327` 有判据与验收命令、且票面自查"不挡 #320 收口"；但 ADR 没有归属票 | `#327` open（四件：编辑集静态校验／基线指纹／证据生成／快门清单，各自 `--self-test` 先红后绿）；`#293` 正文已裁定"#321 与 #325 不阻塞本图收口"；`#321` 正文 **0 处** ADR 字样 |
| D6 收口判据不变 | **需整改** | 判据本身与 `#320` 一致，但缺三处记账：锚集口径、`#327`／ADR、过期散文票 | `#320` 正文五条绿线＋完成判据（gh api 复读）；`#293` 正文已记 `#321`／`#325` 边界，**未记 `#327`** |

## 二 · 三条特别攻击

### (a) D5 是不是"把结构性损耗推给下一张图、带着已知缺陷收口"？

**部分成立，但不构成不达标。** 本图 Destination 要求的是「汇总位＝派生件 ＋ 日常零接触」，**不含**「生成物按能力分片」；
`#293` 正文已具名裁定 `#321` 移出收口判据、`#325` 不阻塞收口，`#327` 也已带判据立票。真正被推迟的是**吞吐**（落点仍需整树 `pnpm gen` ＋ 一把全局写者锁），
这一点在 `docs/agents/编排纪律.md:42` 自己写明了"纪律文件解决不了"。**风险不在少做，而在没人保证它下次会做**：`#327`／`#321` 与 `#325` 一样处于 open 待办态，
而 `#293` 收口后新地图会照旧开工。⇒ 更优解：给 `#327` 加一条**前置关系**（"下一张 wayfinder 地图开窗前 `#327` 必须关"）并写进 `#293` 地图正文的收口段；ADR 的归属补进 `#321` 正文（当刻只有 `#327` 的边界段提到"另需一张 ADR"）。

### (b) D1 的形状守卫能被绕过吗？

**能，且我跑出了躲过守卫仍是权威写死的样例：**

- 躲过：`assert.equal(specs.length, <权威总量>, '声明条数')`
  → 实测 `bucket=undetermined`（④）、`guardPending=false` ⇒ 既不判红也不记 `PENDING`，口径与"没判"无异。
  机理：`shapeGuard` 只吃「另一侧操作数文本 ＋ 整行原文」里的**登记样名**（`REGISTRY_LIKE_CI/CS`，L429-430），`specs` 是中性名 ⇒ 不进名册。
- 被抓住的对照：`assert.equal(n, 10)`（裸标识符）⇒ `PENDING`；`assert.equal(REGISTRY_KEYS.length, 10)` 与 `assert.equal(Object.keys(index).length, 10)` ⇒ ③ `UNACCOUNTED`（P1=FAIL）。
  ⇒ 守卫**有效，但识别的是名字不是事实**：改名、换中性变量即可绕过（复核席自己写的残余风险"④ 不是判过，是没判"）。
- **更要紧的一层（D1 未涉及）**：守卫守的锚集已经陈旧。探针算「权威总量」用的是 `未搬迁清单 ＋ 体重声明`
  （`authorityTotals()`）；当刻残留清单只剩 **1** 条，于是锚＝`10/4/6/9`，而真实权威是 **101 条命令**（写 35／读 66）。
  后果两条：① ④ 的 81 行里有 50 行是 `.status` 退出码一类的**小数字噪声**（`GROUPS=退出码／差值:50 访问器调用:4 域内 metrics:5 域内量:22 裸标识符:0`），
  噪声淹没信号，正是复核席提"≥60 阈值"的来因；② 任何把**真实总量**写死的测试（如 `assert.equal(KEYS.length, 101)`）**连扫都扫不到**——扫描锚里没有 101。
  ⇒ 更优解（比 D1 更省也更强）：把 `authorityTotals()` 改成与生成器同源取权威（读全量 `src/*/commands.ts` 声明，或直接读生成物 `keys.ts`），锚变 `101/35/66/9`；
  ④ 会从 81 行塌到个位数，③ 重新有鉴别力，阈值问题自然消失；`shapeGuard` 作为便宜的追加守卫保留即可（**阈值仍然不要**，这一条 D1 判对了）。

### (c) D2 的"不放宽 85 分线"在 S1／S2 皆 0 时是不是形式主义？

**不是，而且是唯一不破契约的选项。** 三点：

1. 首轮那份报告的**分数与自己的判定不自洽**：`PASS／82` 与 §6 量刑律（均分 <85 ⇒ FAIL）冲突；真实缺陷是"证据里的数字与任何版本都不符"（`docs/agents/编排纪律.md:34` 把"数字与任何版本都不符"列为第三桶成本），属证据面缺陷，不是文风问题。
2. §6.1 只给 **S1-过程违规**开了"具名书面比例处置"的口子，**没有**任何条款允许放宽分数门槛；为 82 分放宽一次，等于把 85 线变成可议价项。
3. 实际走的路径（`5eb41be`／`b35ebfb` 定点修证据 → 原复核席 §十一 定向复评 → 95）**已经是最省的合法路径**：只重开了"证据真实可复现"一维（12→23）与"文档同步"（7→9），没有推翻首轮其他四维结论。
   ⇒ 我按同口径独立复算四个数字（`1146/745/61/34`）逐值一致，复评 95 站得住。**结论：不是形式主义，也没有拖长收口**（当刻 `#318` 已 PASS／95）。

## 三 · 必须整改项（按严重度）

- **S2-1（D1 的同族根因，影响收口绿线④的鉴别力）**：`authorityTotals()` 的"权威总量"仍等于「残留清单＋体重」，与当刻权威（101 条能力声明）脱节 ⇒ `UNACCOUNTED=0` 这条绿线**只在 10/4/6/9 这四个数上有鉴别力**。
  修法：锚集改为全量声明（一条函数），复跑探针，把新锚值写进 `#320` 的绿线证据；**不要**引入 ④ 计数阈值。
- **S2-2（D4）**：规则 38 的回滚路径必须**重签印记**（`node packages/skill-calorie/scripts/gen-cli.mjs --stamp`）或明确写"回滚后下一窗前必须 `pnpm build`"；
  否则回滚完成的树会留下 `GEN-STALE FAIL` 假红，与 `#325`（印记假绿）一起构成"印记既不可全信也不能不用"的夹层。
- **S2-3（D3）**：过期 prose（当刻可见：`routing.ts:75` 的 reason 仍写"95 键"、路径仍写 `render/bodyPlate.ts`）必须**当场开票或写票号**；
  并把决策文本里的"散文"改成"**数据串**（与冻结测试逐字耦合）"，否则下一个席位按"散文"去改就撞红。
- **S3-1（D5）**：ADR 归属补进 `#321` 正文（当刻只在 `#327` 边界段出现）。
- **S3-2（D6）**：地图三处同步的清单里补 `#327`（＋ADR）与上述过期 prose 票；否则 `#293` 关闭后这三项只活在对话里。

## 四 · 不达标判词

**未发现任何一条使 `#293` 不达标（destination 级）。** 六条里两条帮目标（D2／D5）、四条需整改（D1／D3／D4／D6），
整改都是"补一句归属／改一个锚集"，不动已交付的机制与冻结面。唯一"可能变成不达标"的路径是：**带着 S2-1 收口**——
那时五条绿线里的"自治验收 `UNACCOUNTED` 空"会以 10/4/6/9 为口径签出去，而它要守的是 101 条命令的登记面；这属于**证据口径失真**，建议按 S2 在收口前修掉。

## 五 · 复跑命令（本席实测）

```text
node tooling/run-locked.mjs --ticket d-review --lock-dir .scratch/locks-land --stale-minutes 30 -- node docs/skills/skill-calorie/t293-验收-命令自治.mjs
node .scratch/d-review/d1-probe.mjs          # 锚集 / ④ 分组 / 守卫命中 / 绕过样例（原始输出 d1-out.txt）
git show 2137e93:packages/skill-calorie/src/cli/cmd_read.ts > a.ts    # 再按「换行切分计数 − 1」口径复算（node 口径，非 Get-Content）：1146 行 / 61 个 case；write.ts 同法 745 / 34
gh issue list --state open --json number,title,body --jq '.[]|select((.body//"")|test("过期散文|95 键"))'
```

---

# 追加审查 · D7／D8 与四条提问（第二席次 d-review2）

- **被审对象**：D7（不关 `#320`、另开票清残余）／D8（`write.ts:60/61/63` 不计入达成条件①的计数范围），＋ 提问 1–4（D7 的"换干净签收"风险／D8 是澄清还是放宽／`cmd_read.ts` 351 越线／`output.ts` 21 处归属）。
- **基线**：`HEAD=105e0af`（收口实测 `090f502` ＋ 残余定性 `105e0af` 均已入仓；工作区干净）；判官 sha256 `78AE8574…`（＝第五版）。
- **本席门禁窗口**：`node tooling/run-locked.mjs --ticket d-review2 --lock-dir .scratch/locks-land --stale-minutes 30 -- pwsh -NoProfile -File .scratch/d-review2/gates.ps1`
  （`runId=27293a8f-651f-4090-85bf-8709f849c66f`，`exit=0`；原始输出 `.scratch/d-review2/gates.txt`）。
- **复跑读数（本席自己跑的，不采信任一叙述）**：
  `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 10 个声明 100 条，**未搬迁清单 1 条**`，`GEN_EXIT=0`；
  探针 `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`，`UNACCOUNTED=0`，`PROBE_EXIT=0`；
  行数（物理行，node 口径）：`cmd_read.ts=351`／`write.ts=95`／`output.ts=416`；
  `src/body/commands.ts:25`（`calorie.view.body-composition`）与 `src/diet/commands.ts:61`（`calorie.water.log`）**均已声明** ⇒ `cmd-registry-294.test.mjs:147-158` 那条「老路活证」标题已不成立；
  `output.ts:314` ＝ `writeSuffixFor(key, params)` 函数体（21 处 `case 'calorie.` 都在其中）。

## 七 · 四条判定

### Q1 · D7：不关 `#320` ＋ 另开票清残余 —— 「不关」**帮目标（必须）**；「另开票清残余（如草案所写）」**需整改**

- **"不关"必须**：`#320` 票面「达成即完成」两条当刻都不成立 —— ①`cli/cmd_read.ts`／`cli/write.ts` 里 `case 'calorie.` 计数为 **4**（`:158` ＋ `write.ts:60/61/63`）而非 0；②棘轮仍是旧形态（`cmd-registry-294.test.mjs` 的 `FROZEN_READ_CASES_MAX=61`、行数冻结 1146／745，无终态断言）。
  `090f502` 证明的是**五条绿线**（Destination 的机器门），不等于 `#320` 自己的达成条件 ⇒ 此刻写 100% 或"已收口"措辞即**提前签收**（§5.1/L 关闭以合并点安静态复核为准）。
- **"另开票清残余"错在切法**：草案 `.scratch/t330-body.md`「要做的事 1–4」＝搬迁最后一条 ＋ 棘轮切终态 ＋ 修失效断言 ＋ 行数 —— **正是 `#320` 自己的 ①②**。两张票宣布同一判据、同一写集（`cmd_read.ts`／`cmd-registry-294.test.mjs`）而 `#320` 仍 open ⇒ **同一写集两个 owner**（协议 §1 路径所有权；拆票十条 #2 按判据切、#7 一票一作者）。
- **不达标判词**：按草案原样开票且不与 `#320` 建立依赖 ⇒ **不达标**：`#320` 的两条达成条件被搬走而票面未变，收口链上**没有一张票能闭合**（不是慢，是签不了）。
- **替代写法（最省，按判据切）**：
  - **留在 `#320`**：搬 `calorie.view.goal-weight`（判据 `gen:check` 未搬迁 0 条；含三片路由声明）、棘轮切终态（含把 `:147-158` 改成终态断言）、五条绿线复跑、`cmd_read.ts` 落线（见 Q3，顺手）。
  - **新票只收票面之外的三件**：①`cmd_read.ts` 351 → ≤200（结构五步）②`output.ts` 21 处 ＋ `write.ts` 回执端口 3 处（共用位手写键表归零，判据见 Q4）③口径/文档跟进。
  - 若坚持"一张票全收"：`#320` 必须 `blocked_by` 新票，并同时改 `#320` 票面 ①② 为"由 `#NNN` 承接"＋同步地图；否则不得以任何完成措辞签收。
- **归属标注**：残余不是本窗引入，是**六场景票交接处的遗漏**（`calorie.view.goal-weight` 属场景 03／06 边界；`#318` 只收 06／07 的 18 条）⇒ 按「上票遗漏」记账，不追责单席。

### Q2 · D8：`write.ts:60/61/63` 不计入 ① 的计数范围 —— **澄清的前半成立，后半把账抹掉了 ⇒ 需整改为两段裁定**

- **成立的一半（本席复跑）**：三键在 `src/profile/commands.ts:16/17/18` 声明（`kind:'write'`）；`write.ts:79-80` 注册表先行；`write.ts:89-95` 的 `dispatchInner` 只剩 `default: fail(3,…)` ⇒ **老写分支已清空**；三处住 `profileReceiptDoc()`（`write.ts:56-68`，唯一调用点 `:82`），作用＝按命令名把整页回执换成 `src/profile/setup.ts:382`／`update.ts:48`。
  ⇒「不是未搬迁、不是老 case 死代码」**成立**；按字面把它们报红是误判，D8 纠得对。
- **不成立的一半**：`cli/write.ts` 是 Destination **点名的汇总位**，而 `profileReceiptDoc()` 是一张**手写在汇总位里的命令名→装配表**。按 Destination 第 1 条「新增／修改一条命令只写能力目录声明；共用位日常零接触」——新增一条 profile 整页回执类命令**仍须动 `write.ts`** ⇒ 它是**同族欠账**（与 `output.ts` 的 21 处同类），不是"与目标无关"。若把裁定写成"不计入范围"就把它从地图账上抹掉，与 `docs/agents/命令登记纪律.md`「一条命令的事实只有一个定义地」相抵。
- **最省修法（不需框架级变更）**：把 `profileReceiptDoc` 搬进 `src/profile/receipt.ts`，经 `src/profile/index.ts` 导出（`commandLine` 已在 `src/shared/writeParts.ts:101`，可直接 import），`write.ts:82` 改调能力目录出口 ⇒ `write.ts` 里不再出现任何命令名。改动面：新增 1 件（≈15 行）＋ `write.ts` 改 1 行 ＋ 1 处导出；不动 `CommandSpec`／生成器 ⇒ 无需抢锁广播。
- **建议措辞**：①「三处是回执端口，**不计入 `#320` 达成条件①的字面计数**；①的措辞改为『分派分支』」；②「它们与 `output.ts` 21 处同属『共用位手写键表』欠账 ⇒ 转新票（判据见 Q4）」。两段都留痕（`#320` 与新票各一处）。

### Q3 · 越线一行：`cmd_read.ts` 351 vs 告警线 350 —— 判词：**必须收，但不是"必须重构"；用 `#320` 已排的清账顺手收掉**

- **规则面**：`docs/agents/structure.md:70`「文件行数告警线：具体数字由各包自己定……**超线即触发第五步，不是拦路**」；`:97-99`（第四步·超线报警）要求当场报一句「已超线，需要根据规则进行重构。」＋为什么超 ＋ 拆法或"这次为什么先不拆"。本包记账＝**350**（`docs/skills/skill-calorie/t294-登记与分派接缝.md:112`）⇒ 告警线是**记账＋解释义务**，不是硬门。
- **算术面（本席复跑）**：`dispatch()` 的 `switch`（`cmd_read.ts:157-167`）只有一条活 case（`:158` `calorie.view.goal-weight`）＋ `default: fail`；该键搬进能力目录后整口 switch 可删 ⇒ 约 **−11 行 ⇒ ≈340 < 350**。
- **判词**：越线**在本窗内零成本消失** ⇒ 选择"带一行越线收口"就得按第四步写"这次为什么先不拆"，而那个理由在"顺手就没了"面前站不住 ⇒ **必须收**。反过来说：草案把 **≤350** 与 **≤200** 混装进新票是错的 —— `≤350` 属 `#320` 收尾（顺手），`≤200` 才是新票的结构工作。

### Q4 · `output.ts` 21 处是不是"第二处事实" —— **算 `#293` 目标内的欠账，但不算 `#320` 的 ①② 射程**

- **事实**：`output.ts:314` 的 `writeSuffixFor(key, params)`，函数体 `:335-411` 内含 **21 条** `case 'calorie.…'`（21 个键），唯一调用点 `output.ts:257`（`deliverHtml` 内）⇒ 产出**落盘 HTML 文件名里的内容标识段**：不选实现、不取数、不装配回执正文（既有断言 `test/output-naming-119.test.mjs`）。它排除在分派之外，也不在 Destination 点名的汇总位名单（`cli/keys.ts`／`cmd_read.ts`／`write.ts`／`triggers/routing.ts`／`combos.yaml`／`SKILL.md` 的 REPR），更不在 `#320` ① 的字面范围（只点名两个分派件）⇒ **不是 `#320` 的义务**。
- **但**：它是一张**键→文件名后缀**的手写表，住能力目录之外、非派生 ⇒ 属 Destination 第 1 条的同族欠账（与 Q2 那三处同一票可收）。
- **草案判据 1 的射程陷阱（必须修）**：草案写的是 `git grep … -- packages/skill-calorie/src/cli` ⇒ 只到 cli 面，**静默放过** `output.ts` 的 21 处。要么把判据写成 `-- packages/skill-calorie/src`（全域 0），要么在票面点名"`output.ts` 的 21 处留票 `#NNN`"；否则下一个按字面验收的人会以为"全仓归零"。

## 八 · 本节新增／更新的整改项

- **S2-4（Q1／D7）**：新票与 `#320` 的边界与依赖必须先定：留在 `#320` 的是 ①搬迁 ②棘轮终态（含失效断言）；新票只收票面外三件（≤200 瘦身／两处共用位手写键表／口径文档）。若"一张票全收"，则 `#320` `blocked_by` 新票 ＋ 票面 ①② 改写 ＋ 地图同步。
- **S2-5（Q2／D8）**：裁定写成两段（不计入 ① 字面计数 ✓ ／ 仍属地图欠账＋转票），并附最省修法（`profileReceiptDoc` → `src/profile/receipt.ts`）。
- **S3-3（Q4）**：新票判据 1 的射程改成全域或点名留票；`output.ts` 21 处与 `profileReceiptDoc` 同票收。
- **S3-4（Q3）**：`#320` 收尾把 `cmd_read.ts` 落到 ≤350（顺手删死 switch），并在交付对账里写这一行；≤200 留给新票（结构五步）。
- **上一节各项状态**：**S2-1 已修**（判官第五版 `78AE8574…`：锚集＝真实权威 `{"total":101,"writes":35,"reads":66,"weight":9}`，④ 由 81 行降到 **3 行**，`UNACCOUNTED=0`；本席复跑 `node .scratch/d-review/d1-probe.mjs`）✓；**残留边界**：中性命名写的权威总量仍落 ④ 且 `guardPending=false`（3 行规模下可接受，宜写进探针注释）。**S2-2／S2-3／S3-1／S3-2 仍有效**（回滚重签印记／过期数据串票号／ADR 归属／地图记账）。

## 九 · 本节复跑命令

```text
node tooling/run-locked.mjs --ticket d-review2 --lock-dir .scratch/locks-land --stale-minutes 30 -- pwsh -NoProfile -File .scratch/d-review2/gates.ps1
node .scratch/d-review/d1-probe.mjs        # 锚集 / ④ 分组 / 守卫命中 / 绕过样例（跨版本对比）
git grep -n "'calorie.view.body-composition'" -- packages/skill-calorie/src/*/commands.ts   # body/commands.ts:25
git grep -n "'calorie.water.log'" -- packages/skill-calorie/src/*/commands.ts               # diet/commands.ts:61
```
