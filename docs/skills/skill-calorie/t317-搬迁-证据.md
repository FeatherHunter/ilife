# #317 搬迁证据 · 场景 05 健身计划 → `src/workout/`

票面：`gh issue view 317`（父地图 #293，前置 #322 ／ #313）。落点规则正本：`.scratch/t314-319/六票落点规则.md`。

## ① 改前／改后对照

| 面 | 改前 | 改后 |
| --- | --- | --- |
| 命令声明地 | `src/cli/legacy/scene-05.ts`（5 条，`LegacyCommandDecl`） | `src/workout/commands.ts`（5 条，`CommandSpec`，**唯一权威源**） |
| 路由声明地 | `src/cli/legacy/routes/scene-05.ts`（32 条） | `src/workout/routes.ts`（32 条，逐字搬迁） |
| 分派 | `cmd_read.ts` 里 5 个 `case` 内联取数与装配 | 能力目录处理函数，经 `REGISTRY` 直达（5 个 `case` 已删） |
| 写分派 | `write.ts` | **一行未动**（本场景今天无写键） |
| 旧分片 | 5 ＋ 32 条 | 两文件**搬空**（文件保留，只许搬空不许删） |
| 生成物 | — | `src/cli/registry.ts`（`pnpm gen` 派生，`src/workout/index.js` 一行） |
| 棘轮 | 冻结集／上限 | **一行未改**（⊆ 与 ≤ 断言自动绿；终态归 #320 收口票） |

行数（节点口径 `split('\n').length - 1`，落点基线 `2979b40`）：`cmd_read.ts` **896 → 857**（−39 ＝ 删掉的 5 个 `case` 块共 39 行；另 2 行 `import` 就地改写、行数不加不减）。棘轮上限 `FROZEN_READ_LINES = 1146`（`cmd-registry-294.test.mjs`）⇒ 857 ≤ 1146，棘轮绿。（原文写「1146 → 1106」——1146 是**冻结上限**、1106 是预备件行数，都不是本树实测；已按实测订正，见 ⑦ 第 6 条。）

## ② 搬迁键清单（5 条，全读）＋ 路由 `order`

| 键 | 形状 | 标题 | 代表唤醒词 | 处理函数（子功能文件） |
| --- | --- | --- | --- | --- |
| `calorie.view.plan` | stat | 训练计划看 | —（无代表词） | `plan.ts` `viewPlan` |
| `calorie.view.plan-wizard` | stat | 构建向导 | — | `wizard.ts` `viewPlanWizard` |
| `calorie.view.exercise-review` | stat | 计划复盘 | 计划复盘（本周） | `review.ts` `viewExerciseReview` |
| `calorie.view.contraindication` | stat | 禁忌扫描 | — | `contraindication.ts` `viewContraindication` |
| `calorie.view.process-progress` | stat | 落地训练进度 | 看落地训练进度 | `progress.ts` `viewProcessProgress` |

路由记录 32 条 ＝ `wake` 27（`order` 176–207）＋ `new` 5（`order` 26／27／31／38／49）。
其中 **non-exec 23 条**逐字保留（`legacy-chain` 23 条：`view.plan` 粒度 8 ＋ 计划写 15；无法用单命令同形／95 键无训练计划写键；`out-of-scope` 5 条：落地 3 ＋ 训记 2）；exec 9 条。
⇒ 锁住票面要求：**借搬迁改语义一律没做**，理由文本一字未动（见 `src/workout/routes.ts` 与旧片逐条 `diff`）。

## ③ 结构对账（必报五步 · 第一步 vs 第五步）

| 事前清单（第一步） | 实际（第五步） | 偏差 |
| --- | --- | --- |
| `packages/skill-calorie/src/workout/`（新目录，8 件） | 同 | 0 |
| `packages/skill-calorie/src/cli/cmd_read.ts`（删 5 个 `case` ＋ 无用导入） | 同 | 0 |
| `packages/skill-calorie/src/cli/legacy/scene-05.ts`＋`legacy/routes/scene-05.ts`（搬空） | 同 | 0 |
| `packages/skill-calorie/src/cli/registry.ts`（生成物，派生） | 同 | 0 |
| `docs/skills/skill-calorie/t317-*`（证据 ＋ 对账导出） | 同 | 0 |

**没碰**（派单点名）：`src/render/planPlate.ts`／`wizardPort.ts`／`fetch/plan.ts`／`fetch/xunji-catalog.ts`（视图／向导／取数层，不是本票的活）、别的票的能力目录与分片、`src/triggers/scene-*.ts`、`test/calorie-routing-81.test.mjs`、`docs/agents/**`、生成物手改。

## ④ 门禁 `GATE-RUN` 声明（无票外放宽）

**本票这一窗的实况（据实记账，不美化）**：落点脚本 `land.ps1` 在窗口内被反复触发，前 4 次都被**自己的守卫**挡在写树之前（`exit=1`：哈希守卫 `MISSING_CUR` 与 `ABSENT` 的判定 bug、证据件源路径、`index.ts` 类型错各一次），第 5 次（`1fbd13a5`）一路跑到第 11 步，**卡在旧的 `check-gate-audit` 调用缺 `--evidence` 参数**（内层 `exit=2`、脚本整体 `exit=1`，**不是门禁判红**）。

接管记账：**原席窗口停摆 20+ 分钟**（第 11 步 `exit=2` 后无动作、对告警无响应，落点锁随后释放）；**接管席（票号 `317-rescue`）在原窗口内补齐第 11 步之后的动作，并复跑全部门禁**（复跑 runId 见 ⑤、变异自证见 ⑥）。判据：不是「换一棵树重做」，树内那 6 条与预备件逐件 SHA256 相同。

落点窗口（票号 `317`，`--since 2026-09-13T12:57:30Z --until 2026-09-13T12:58:22Z`）内 5 次包装器运行，逐条声明（对账结果 `matched=5/5`）：

GATE-RUN runId=9eaee523-48cb-4be0-a808-8304ea560344 cmd="pnpm build"
GATE-RUN runId=566eded6-4856-4e4d-b4e4-970ce1f98860 cmd="pnpm gen"
GATE-RUN runId=938427d7-e656-4114-a7d8-b1cbc469967a cmd="pnpm build"
GATE-RUN runId=fe57cc94-052c-4d75-be90-b02eb8bac3d6 cmd="pnpm gen:check"
GATE-RUN runId=dedcac8a-448d-481f-9bba-f1cb25694e8d cmd="pnpm test"

窗口界定（协议 §2.4；**不使用任何 `--allow-*` 放宽**）：

- `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`（P1–P5 探针）**不经包装器**、不产生 `RUN` 条目，故不在此声明；其实测见 ⑤。
- 窗口外、非 0 退出、不代表落点成果的条目：落点前 2 次 `pnpm build`（`cfb126e2`／`b33f7349`，`exit=2`，12:56:22Z／12:57:04Z）、落点后那次对账调用（`d8d0a99c`，`exit=2`，12:58:25Z：漏传 `--evidence`）、`317-land` 的 4 次守卫拦停（`41e22eb8`／`18368917`／`5377613d`／`4c345c70`，12:55:17Z–12:57:04Z，另一锁目录、另一票号）。按协议 §2.4 用窗口排除。
- 接管席的收口运行（票号 `317-rescue`，时刻在窗口之后）：`a4d0522c-38dc-42e5-86f8-1e4536c76717`（`pwsh .scratch/t317/finish1.ps1`，13:29:29Z，`exit=0`）＋它内部的门禁 5 跑 `794eccb4`／`03cedc8d`／`14fb9393`／`54cef0bb`／`c482af80` ＋ `t293` 的 `94cf514c` —— 票号不同、时刻在窗口外，故不进本窗口的声明；其成果见 ⑤。

导出命令（可复现）：
`node tooling/run-locked.mjs --ticket 317-rescue --lock-dir .scratch/locks-land --stale-minutes 30 -- node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t317-搬迁-证据.md --log .scratch/locks/gate-runs.log --ticket 317 --since 2026-09-13T12:57:30Z --until 2026-09-13T12:58:22Z --export docs/skills/skill-calorie/t317-gate-runs.log`
- 验收命令：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t317-搬迁-证据.md --log docs/skills/skill-calorie/t317-gate-runs.log --ticket 317` ⇒ `matched=5/5 … undeclared=0`、`gate-audit: PASS`。

## ⑤ 免费判据与实测（落点实测填此段）

**原席落点窗口**（票号 `317`，runId 见 ④）：

- 生成物 diff：**只出现 `src/cli/registry.ts`**（＋2 行：`import { WORKOUT_COMMANDS }` 与 `SOURCES` 末项）；`keys.ts`／`combos.yaml`／`build-help.mjs`／`routes.generated.ts`／`SKILL.md` **无 diff** ⇒ 搬迁是纯的。
- `pnpm test`：exit 0；`tests 1492 / pass 1492 / fail 0 / skipped 0`。
- `pnpm gen:check`：PASS，`GEN-CHECK ok` 逐件；汇总行 `键 101（写 35 ＋ 读 66）；能力 5 个（body／home／photo／weight／workout）声明 36 条，未搬迁清单 65 条`。
- `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`：`P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`、`UNACCOUNTED=0 处`。

**落点接管席复跑**（票号 `317-rescue`：同一棵树、同一份内容；票号不同故不进 ④ 的窗口声明，此处留 runId 备查）：

- `build → gen → build → gen:check → test`（五步均经包装器）：runId `794eccb4-dcae-4ed8-90f3-8aa267da0a3f`／`03cedc8d-75d2-4d23-9c27-20d817555d28`／`14fb9393-af77-4a8e-9875-05275b3a18a0`／`54cef0bb-76ea-4e50-a326-873f1a51d4a4`／`c482af80-b181-43c6-9eb9-77dcccee6ed9`，**5/5 exit=0**；`tests 1492 / pass 1492 / fail 0`；`GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 5 个（body／home／photo／weight／workout）声明 36 条，未搬迁清单 65 条`。
- 免费判据：复跑前／`gen` 后／复跑后三处都实测「生成物面只动 `registry.ts`」。
- `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`（经包装器，runId `94cf514c-a8cf-4f18-890f-2a0f342981fd`）：`P1..P5` 全绿、`UNACCOUNTED=0 处`、树自相矛盾 0 处。
- 对账：导出 `matched=5/5 scoped=5 undeclared=0`（runId `b993a7c2-e42c-44fc-b849-0bfffb532892`）；再以导出的受跟踪文件当对账源复验 `matched=5/5 undeclared=0`（runId `a5468886-19a1-48bd-9eb0-1212edffaa17`）——`gate-audit: PASS` 两次。

## ⑥ 变异自证（门有鉴别力）

落点接管席实测（票号 `317-rescue`，runId `00e160b4-877e-4f1c-ac35-dd18c6866315`）：

- 变异点：`src/workout/commands.ts` 的 `title: '训练计划看'` → `title: '训练计划看（变异）'`（该串在文件内唯一，实测命中 1 次）。
- 变红：`pnpm gen:check` ⇒ `exit=1`，首行是
  `GEN-STALE FAIL：dist/ 不是这些声明**现在的内容**的产物，继续跑会把旧声明当成事实（假绿）。先 \`pnpm build\`（重建 dist 并重打内容印记）再 \`pnpm gen:check\`。`
  次行**指名到件**：`packages\skill-calorie\src\workout\commands.ts 的内容与上次 pnpm build 时不同（印记 72d80d0ee0bd… ≠ 现 a7cda9d36f38…）`。
- 还原：**逐文件点名**（从 `.scratch/t317/tree/` 的同名件复制回该一件）⇒ SHA256 复核 `before == after` ＝ `72D80D0EE0BD045D987B86B0851D311364BB64B05DCA47BCE5FF7DCDF7A21B4C`。未用 `checkout -- .`／`restore`／`stash`／`reset --hard`／`clean`，未切分支。

## ⑦ 偏离记账

1. 票面「要做的事 第 4 条 · 加写命令补 `test/cmd-write-40-persist.test.mjs` ＋ `docs/research/t97-probe-receipts.mjs`」——**本场景 5 条全是读命令、无写键**，故本票无对象，按「别加码」不动那两件（写命令的活属别的场景票）。
2. 票面「要做的事 第 2 条」提到把 `render/planPlate.ts` 等「搬进来」——与派单「那不是你的活／处理逻辑已在别处实现只经公开接口调用（铁律一）」冲突时**以派单为准**：视图与向导层留在 `src/render/`，`src/workout/` 只经它们的公开函数取数与出产物（不抄一份）。
3. 棘轮三处（`cmd-registry-294.test.mjs` 的读 case 清单与行数上限、`legacy-frozen-295.mjs` 的键集与两个上限）**未下调** —— 编排者裁决：纯搬迁靠 ⊆／≤ 自动绿，棘轮终态归 #320 一次到位，避免六席并行各算绝对值反而变成放宽。
4. `pnpm build` 在 `gen` 前后各跑一次（规则书 §三 第 3–5 步的 `#322` 提示口径）：`gen` 只写 `src`，重建一次让 `dist` 与内容印记配平，避免 `gen:check` 报陈旧。

以下三条由**落点接管席**（票号 `317-rescue`，原席在落点第 11 步 `exit=2` 后停摆）勘查后补记：

5. `routes/scene-05.ts` 的形态：它在 **SOFT 相**（`land.ps1` 对全部路由分片只删记录行、不整文件覆盖），故落点后**文件头仍是 #313 的通用头、空数组留成两行**（`= [` ＋ `];`）；预备件里那份整文件版（头改写为「已搬空（#317）」＋ `= [];`）**未被采用**（SOFT 面不复制）。内容面无偏差：我的 37 条记录全删、片内 0 条外来记录（本片记录按 `key` 归属全属本票自己）。形态与 #314 的两片搬空片（`routes/scene-01.ts`／`scene-09.ts` 保留通用头 ＋ `= [];`）同族，差别只在空数组的折行。
6. ① 段行数订正：原写「`cmd_read.ts` 1146 → 1106（−40）」——`1146` 是 #294 的**冻结上限**（`FROZEN_READ_LINES`），`1106` 是 `.scratch/t317/tree/` 里那份按**更早基线**做的预备件行数（56 个 `case`；若照抄会复活 #314 已搬走的键，故 SOFT 相读活文件定点删除才是对的）。实测口径（`split('\n').length - 1`，基线 `2979b40` → 落点后）：**896 → 857（−39）**；上限 1146 ⇒ 棘轮绿。
7. 落点前 2 次 `pnpm build`（runId `cfb126e2`／`b33f7349`）以 `exit=2` 结束、落点后那次对账导出（runId `d8d0a99c`）也 `exit=2`（`land.ps1` 第 171 行漏传 `--evidence`）。三者都不是本票成果、也不代表门禁判红；按 §2.4 用 `--since/--until` 界定对账窗口排除（见 ④），**没有使用任何 `--allow-*` 放宽**。

## ⑧ 未确证与风险 top3

- **未确证**：`example` 列「照抄即能跑」只在本票的自证范围内抽验，`pnpm help:examples:check` 那道门今天带票外既有红（见 `docs/agents/命令登记纪律.md` 形状一）。
- 风险①：`src/workout/index.ts` 同时被 `REGISTRY` 与（日后）别的能力引用时，若别家改从子功能文件直接 import，会绕开这道门 ⇒ 靠 `test/cmd-registry-294` 的「定义地」断言兜。
- 风险②：本场景 23 条 non-exec 的 reason 字面量里写死「95 键／旧链 `--live-plan-*`」——键数若变，这些理由会过期（今天不归本票，留作后续措辞票）。
- 风险③：空数组分片（`scene-05.ts`／`routes/scene-05.ts`）靠「文件仍在」满足片数等号断言；日后有人误删文件才会红，这是规则书 §一「只许搬空、不许删」的直接后果，已按要求保留。

### 落地实测（收口时由 `finish.ps1` 从 gated 日志抄入，非手写）

- `apply-soft-delete.ps1`（SOFT 相）：cmd_read.ts 结果：行数 897 → 858（delta -39）；分派 case 46 → 41（delta -5）；清理导入 0 行 / 路由命中合计 37 条 ＝ 期望 37
- 第 3c 步邻块点名：别人的 `case` 标签 41 条，全部保留（见 `.scratch/t317/logs/03c-case-labels.txt` 逐条名单）
- `pnpm gen:check`：GEN-CHECK ok packages\skill-calorie\src\cli\keys.ts sha256=4db43569fbab400d5664d4ae86f8771c911f3d51351720a1e0182bbc7e96f4ea / GEN-CHECK ok packages\skill-calorie\src\cli\registry.ts sha256=06fd6d3885952c837cd1792e68f0502e5f0cea2fc27cbe3510a9644bfc8ba312 / GEN-CHECK ok packages\base-combos\combos.yaml sha256=ca17d2c3f62ce7527d7747d216426a301c8dde6942f7e63732a965a54d466ec2 / GEN-CHECK ok packages\skill-calorie\scripts\build-help.mjs sha256=4d44f03f6ffb25d7e490ee8d69d39bda5773deb6ed6f9e8b76b7811454765ede / GEN-CHECK ok packages\skill-calorie\src\triggers\routes.generated.ts sha256=e9ea993518ec33e3a69575dac1d8abf4a5e4c12e277b4c2d215ae3e5d339d7e9 / GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 5 个（body／home／photo／weight／workout）声明 36 条，未搬迁清单 65 条
- `pnpm test` 尾：ℹ todo 0 / ℹ duration_ms 44480.6411 / LOCK-RELEASED ticket=317 runId=dedcac8a-448d-481f-9bba-f1cb25694e8d exit=0 / RESULT: ticket=317 runId=dedcac8a-448d-481f-9bba-f1cb25694e8d waitedMs=0 exit=0
- `t293` 探针：P1: PASS / P2: PASS / P3: PASS / P4: PASS / P5: PASS / RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5

- 收口后补跑（在④的对账窗口之后，日志见 `.scratch/t317/logs/resume-*.log`）：`pnpm gen`＝0、`pnpm build`＝0、`pnpm gen:check`＝0（GEN-CHECK PASS）、`pnpm test`＝0、`t293` 探针＝0（P1–P5 全 PASS）；均经 `tooling/run-locked.mjs --ticket 317`。
- 失败的 `317-land` 重试**不在④的窗口内**（12:55–12:59Z，全部 `exit=1`：守卫拦停 ×4 ＋ 一次漏传 `--evidence` 的对账调用），按协议用窗口排除，未使用任何 `--allow-*` 放宽。
