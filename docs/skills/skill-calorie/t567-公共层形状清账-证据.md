# #567 公共层形状清账 · 实施证据

> 票 #567 ｜ 地图 #161 ｜ 仓库 FeatherHunter/ilife ｜ 归属：公共层 `packages/base-render` ＋ 卡路里包缺口页段
> 证据格式照 `docs/subagent-concurrency-protocol.md` §5.1（判定／机器证据／逐条路径／未做项与下一手）。

## §0 判定与交接说明

**判定：实施完成，可交独立复核。**

**交接说明（重要，不许省略）**：本票前一实施席**死于「先干完再写报告」**——代码改完、探针跑绿，但证据件从未建立、未提交、后续门禁未扫。本席（编排者）依 §3.2 第 4 条「无主 diff 先量现场再自行判定完成度」接手，并以**非受益者**身份补齐收尾。接手时实测现场：`probe-before.log`＝PROBE-567 FAIL（exit 1）、`probe-after.log`＝PROBE-567 PASS（7 守卫同绿，exit 0）、`base-test.log`＝688/688；源码与 `.scratch/t567/keep/` 备份逐件 sha256 相同（变异已还原，无残留改坏）。

## §1 写集（文件级，事前声明与事后对账逐行相同）

| 件 | 改动 |
|---|---|
| `packages/base-render/src/blocks.ts` | D1 表卡撤 680 上限；D2 `list-rows` 首列不折行；渲染状态列 `cellHtml` 能力 |
| `packages/base-render/src/charts.ts` | 图表开关四项：柱状 `valueThin` 抽稀／柱状 `yTicks` 刻度／折线只标末值／折线 `dots` 全＋`labels` 每 k 标一（默认行为不变） |
| `packages/base-render/src/pageShapes.ts` | J4 字号收档（区块标题 14→15） |
| `packages/base-render/src/pageUi.ts` | J4 字号收档（次级 11.5→12） |
| `packages/base-render/src/style.ts` | J4 字号阶梯收档 ＋ 10px 抬到 11px 下限 |
| `packages/base-render/src/spec/charts.ts` | 图表开关的类型面 |
| `packages/base-render/test/{blocks,chart-top-table-measure-512,page-viz-421,style}.test.mjs` | 随 D1／D2／D6／J4 跟改的既有断言 |
| `packages/base-render/test/shape-567.test.mjs`（新） | 本票形状靶向断言 |
| `packages/skill-calorie/src/render/trendPredictDocs.ts` | D4 参数说明与口径行去同义；D5 堆叠条值槽只印占比（绝对数留 `detail`） |
| `packages/skill-calorie/test/analysis-shape-567.test.mjs`（新） | 本票 D1／J4 反向守卫 |
| `docs/skills/skill-calorie/t567-公共层形状清账-证据.md` | 本件 |

**不碰**：`packages/skill-calorie/src/goal/**`、`src/triggers/scene-04-exercise.ts`（别席在途件）、D7（已判可接受形态只登记）。

## §2 门禁（全经加锁包装器；只领 exit=0 的条目录入）

| 门 | runId | 读数 |
|---|---|---|
| `tsc -b packages/base-render --force` | `4576078d-6543-4aaf-a77e-f1da95995c4e` | exit 0 |
| `tsc -b packages/skill-calorie --force` | `6a4b6fbb-8c76-4819-8a47-f0e5cffcb97f` | exit 0 |
| `node --test packages/base-render/test/*.test.mjs` | `aba94213-33f6-462e-b166-7230379c924f` | **688 测试 / 688 过 / 0 红** |
| `node .scratch/t387/run-all.mjs`（31 页重出） | `a7f52f87-de8d-4b19-bfd2-7f9753a37106` | 31/31 exit 0；自检三项全过；字节 71710–97771，合计 2425769 |
| `audit-separators.mjs --dir .scratch/t387/html` | `ed335f09-7870-40a8-869c-9222742eb5e9` | RESULT 31/31 PASS，节点级全零 |
| `measure-responsive.mjs --dir .scratch/t387/html` | `9398ab83-93b5-48ec-963d-e7f488f47d5c` | OVERFLOW-ZERO pages=31 cells=93 failed=0 |
| `gen-cli.mjs --check` | `280e0840-96cd-4631-a951-1861c8940429` | GEN-CHECK PASS，128 命令 |
| D1 产物标志位 | —（定向检索） | `21-查热量缺口.html` 内 `max-width:680px` 命中 **0**（D1 已生效） |

### 变异自证（两行机器读数）

- **改坏必红**：`--force` 重编前的陈旧 dist 实测＝`36a5abc3-1d4c-4deb-9c15-18ed2dcd3242` **exit 1，18 红**，且逐条正是本票断言（`#567 D1 表卡吃满内容列`／`D2`／`D6`／`状态列 cellHtml`／`图表开关一~四`／`#512 ②`／`#572-S3-3 表头去大写`／`#421 公共层纪律`／`#75 共享样式资产`）。
- **还原必绿**：逐件还原后重编再跑＝`aba94213` **exit 0，688/688 全绿**。
- 另有一对（前席留档）：`probe-before`＝PROBE-567 FAIL（exit 1，D1／D2／D6／J4／D4／D5／STATUS／S1／S1b／S2／S3／S4 全红）→ `probe-after`＝PROBE-567 PASS（7 守卫同绿，exit 0）。

## §3 基线 A/B 归因（本票最重要的一条读数）

共享树当刻不干净，**必须证明「红不是本票引入」**。做法：把本票改过的 10 个受版本控制件逐件还原到 HEAD ＋ 移开新增测试 → `tsc -b packages/base-render` → 跑全量 → 再逐件还原本票改动 → 重编（`.scratch/t567/baseline.ps1`，整窗持锁，runId `07f536c5-5ffc-4ed0-85c2-df5009bba16f`）。

| 口径 | 测试总数 | 过 | 红 |
|---|---|---|---|
| **HEAD（本票改动全部还原）** | 3004 | 2903 | **101** |
| **当刻（含本票改动）** | 3027 | 2932 | **95** |

文件级对照（失败段 `test at <路径>`）：两边失败件集合**只差一处** —— 新增的 `packages/skill-calorie/test/analysis-shape-567.test.mjs` 由「基线红」变「当刻绿」；其余差异均系本席脚本输出编码差异（同一批件）。

**结论：本票引入 0 条新红，并修好 6 条。** 当刻 95 条红全部在 HEAD 已存在。

### 95 条红的根因（不属本票，登记不修）

1. **全仓 `tsc -b` 被别席在途件打断**：`packages/skill-bill/src/record/scene-collect.ts`（工作树改动，HEAD 有 import）丢了 `renderCaliberLine`／`renderToast` 的引用 ⇒ `tsc -b` **exit 2**（runId `cff99e4e-9ea5-48ce-8ab1-144129cf9335`）⇒ 后续各包 dist 普遍陈旧 ⇒ 大面积假红。按 §1 路径所有权**不碰**。
2. **告警线台账 4 条陈化**（别席已提交未同步）：`src/diet/nutritionPortDocs.ts` 745→750、`src/diet/libraryDocs.ts` 407→408、`src/diet/receipt.ts` 398→425、`src/diet/reviewDocs.ts` 359→362 ⇒ `check-warning-line.mjs` **RESULT 87/91 FAIL**（runId `e4955ee7-76ad-4d7d-b047-c0d168ae81fe`）。本票不在该台账扫描面内（只动 `base-render` 与 `trendPredictDocs.ts`，后者 LF=1162 与台账一致）。**`--sync` 不可行**：`packages/skill-calorie/AGENTS.md` 当刻是别席在途脏件（其 sync 段改的是 exercise/receipt 与 planEditorRuntime 两行），跑同步会把别席在途编辑卷进本票提交。
3. 卡路里包全量实测 84 红（`f21e5d22`／`427a7965`，两次同值），分布与上述既有两类一致，非本票面。

## §4 未做项与下一手

- **D7 未做**（票面已定：手机档明细表 7×5 塌成 15 行键值卡判为可接受形态，只登记）。
- **告警线门未绿**：外部阻塞，见 §3.2；建议开「台账同步」票或在别席 sync 落地后复跑。
- **全仓 `tsc -b` 未绿**：外部阻塞，见 §3.1；归属 `skill-bill` 在途席。
- 下一手：独立复核席按 §5.1 复跑（重点＝探针识别力、公共层回归面、收窄是否变放宽）；复核过即可关票，本票不 push、不 close 由编排者做。
