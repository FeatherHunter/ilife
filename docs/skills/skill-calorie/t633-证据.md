# t633 证据（D5 种子食品来源名用户可读化）

结论：来源行映射分支交付。种子内部名「测试」在食品卡片来源行改印「内置食品」；缺来源仍归「未知」；他源透传；种子值、统计口径、脚注一字未动。

## 一、改动（2 件源码＋测试）

- `packages/skill-calorie/src/diet/libraryDocs.ts:125-127`：来源行 `it.source || UNKNOWN_SOURCE` 改为
  `it.source === '测试' ? '内置食品' : (it.source || UNKNOWN_SOURCE)`。`UNKNOWN_SOURCE='未知'` 语义不变；
  复制载荷（`productCopyRow`）仍带原始值，可复核。
- `packages/skill-calorie/test/t633-种子来源可读.test.mjs`（新建）：① 种子全量扫（`docs/research/t81-seed.mjs`
  的 `seedFull`＋`calorie.view.library`，断言无「来源：测试」、≥3 处「来源：内置食品」）；② 三态直断
  （种子映射 1／缺来源回退未知 1／他源透传 1，全库页＋搜索页双断无「来源：测试」）。
- 行数：`libraryDocs.ts` LF=408，改前改后一致（台账 `当场实测=408` 不变，本票不碰 `AGENTS.md` 台账）。

## 二、种子落点清单（开工 `grep source.*测试` 点名，未改——取映射分支）

- 真种子：`docs/research/t81-seed.mjs:127-129`（3 行，`source='测试'`；历史归档件，他席共用，不动）。
- 各测试自持种子（`cli-smoke-t41`／`cmd-read-t11`／`cmd-write-40*`／`db-readonly-93`／`diet-homogeneity-108`／
  `diet-ranking-route-t276`／`fetch-t6`／`m5-receipt-97`／`render-t9`／`render-t41` 等十余件，同字面 `source='测试'`；
  均为用例夹具，非用户真实数据，不批量改）。
- `src/` 下无 `source.*测试`（`productStore.ts` 写路径不带来源默认值）。

## 三、GATE-RUN 对账（源 `.scratch/locks/gate-runs.log`，`RUN ticket=633 …` 一对一）

| runId | 命令 | exit | 日志 |
|---|---|---|---|
| t633-tsc-01 | `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` | 0 | `.scratch/t633/tsc.log` |
| t633-test-01 | `node --test packages/skill-calorie/test/t633-种子来源可读.test.mjs` | 0（pass 2/fail 0） | `.scratch/t633/test-t633.log` |
| t633-t274-01 | `node --test packages/skill-calorie/test/t274-食品库页.test.mjs` | 0（pass 13/fail 0） | `.scratch/t633/test-t274.log` |
| t633-warn-01 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | 1（他人域，见§五） | `.scratch/t633/warn.log` |
| t633-sot-01 | `node packages/skill-calorie/scripts/gen-sot-snapshot.mjs --check` | 1（他人域，见§五） | `.scratch/t633/sot.log` |
| t633-resp-01 | `node .scratch/t633/gen-resp-page.mjs` | 1（本票驱动 bug，已被 -02 取代） | `.scratch/t633/resp.log`（旧） |
| t633-resp-02 | 同上（修动态 import 的 file-URL 后重跑） | 0，OVERFLOW-ZERO pages=1 cells=3 failed=0 | `.scratch/t633/resp.log`＋`resp.json` |
| t633-mut-red-01 | 变异态重建（同 tsc 命令） | 1（他席 `review.ts` 在途类型错叠加；emit 仍落盘，变异生效） | `.scratch/t633/mut-build.log` |
| t633-mut-red-02 | 变异态靶向测试 | 1（pass 0/fail 2，红成立） | `.scratch/t633/mut-red.log` |
| t633-mut-green-01 | 还原后重建 | 0 | `.scratch/t633/mut-rebuild.log` |
| t633-mut-green-02 | 还原后靶向测试 | 0（pass 2/fail 0，绿回） | `.scratch/t633/test-t633-green.log` |
| t633-t274-02 | 终态 t274 回归 | 0（pass 13/fail 0） | `.scratch/t633/test-t274-final.log` |
| t633-warn-02 | 终态告警线门 | 1（他人域，见§五；本件 `libraryDocs.ts LF=408` 无 RED） | `.scratch/t633/warn-final.log` |

## 四、变异两行（§五 协议自证）

- 红：`t633-mut-red-02` pass 0/fail 2 exit=1（映射改回「测试」即两条全红）。
- 还原一致：`t633-mut-green-02` pass 2/fail 0 exit=0；源码 LF=408、127 行与交付一致。

## 五、他人域异常（非本票引入，未碰）

1. 告警线门红：`-01` 时 `nutritionPortDocs.ts 台账=750 实况=755`；`-02` 时 `reviewDocs.ts 台账=362 实况=380`。
   两次都是他席在途加行，本件无 RED。`--sync` 会改 `AGENTS.md`（本票禁区），留收口票统一 sync。
2. SoT 快照门红：`快照漏了 1 条 entry_sha：goal_set_exercise…`——触发器域（本票零触达），留触发器归属票。
3. 变异重建时他席 `review.ts:92` 在途类型错致 tsc exit=1（后自行恢复，`t633-mut-green-01` exit=0）；emit 未阻塞，
   变异红绿读数有效。
4. 响应式 `t633-resp-01` 失败系本票驱动脚本动态 import 缺 file-URL（已修，重跑 `-02` 绿；旧日志保留备查）。

## 六、未做项

- 食品库族票捎带关闭：本票独立交付，无取代关系可写。
- `docs/research/t81-seed.mjs` 种子值未改（映射分支不需要）；若日后要改种子值，另开票并评估十余件测试夹具。
