# t398 复核整改验证 · 三处同源／台账实况／授权条件①②③

- 票：#398（MAP#158 子票）；被验整改提交：**`1dfd6533`**（`docs(398): 复核整改…`，只动 3 件文档：`packages/skill-calorie/AGENTS.md`、`t398-按来源分组取数-证据.md`、`t398-gate-runs.log`；`git show --stat` = 3 files changed, 97 insertions(+), 14 deletions(-)，**零源码／零测试**）
- 授权方：编排者（本验证 §4 逐字引用其授权原文）
- 本件与前一复核件的关系：**本件只做收口验证，不改 `docs/skills/skill-calorie/t398-复核.md` 原文**；取代／补充关系见 §6。
- 判定：**PASS**（三处同源成立、10 条声明一对一且全 exit=0、12 条非门禁逐条点名且性质属实、台账 `body.ts` 一行与实况一致；授权条件①②③全部成立）

## 一、三条命令原样复跑（命令与读数）

### ① 对账（作者 §8.2① 原文，用**受跟踪导出件**当对账源，不重导）

```text
node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t398-按来源分组取数-证据.md \
  --log docs/skills/skill-calorie/t398-gate-runs.log --ticket 398 \
  --since 2026-09-14T12:33:45Z --until 2026-09-14T12:45:00Z --allow-undeclared
→ EXIT=0
→ 审计：…t398-gate-runs.log（RUN 条目 22 条；窗口内 22 条，ticket=398）
→ RESULT: matched=10/10 auditEntries=22 scoped=22 undeclared=12
→ gate-audit: PASS
```

与作者 §8.2① 的期望 `matched=10/10 scoped=22 undeclared=12／PASS` **逐字一致**。

### ② 超线台账检查脚本

```text
node packages/skill-calorie/scripts/check-warning-line.mjs
→ EXIT=0
→ OVER src/render/wizardPort.ts LF=457 …／OVER scripts/gen-cli.mjs LF=729 …／AGENTS.md：台账命中 2/2
→ RESULT: 5/5
→ PASS: 告警线台账齐全
```

与作者 §8.2③ 的期望 `RESULT: 5/5` **逐字一致**。

### ③ 导出件头部三处计数 ↔ §四 表格加总

导出件第 4 行（我逐字读回来）：

```text
> 票号过滤：398｜since：2026-09-14T12:33:45Z｜until：2026-09-14T12:45:00Z｜条目数：22｜导出时间：2026-09-14T13:01:12.762Z
```

- 导出件内实际 `RUN` 条目数 = **22**（`Select-String '^\s*RUN '` 计数），与头部「条目数：22」**同数**。
- 头部 `since/until` 与 §四 口径行 `--since 2026-09-14T12:33:45Z --until 2026-09-14T12:45:00Z`**逐字相同**。

## 二、判三处：同源、计数自洽、台账、sha 清单

### 2.1 ① 声明窗口／导出件／加总：**同源且计数自洽 ✅**

| 量 | 读数 | 出处 |
|---|---|---|
| 声明窗口 | `--ticket 398 --since 2026-09-14T12:33:45Z --until 2026-09-14T12:45:00Z` | 证据 §4.0／§8.2①（同一串） |
| 导出件头 | `398｜since 12:33:45Z｜until 12:45:00Z｜条目数 22` | `t398-gate-runs.log:4` |
| 加总 | `22 ＝ 10 条门禁（exit=0）＋ 2 条必红排查 ＋ 10 条 git 操作` | 证据 §4.0 正文 |
| 机械读数 | `scoped=22 undeclared=12` | 命令① |

我按**性质**把 22 条独立分了一遍（不采信作者分类）：`compile 6 ＋ test 6 ＋ git 10 ＋ other 0`，exit 分布 `compile 全 0/6`、`test 4 条 0 ＋ 2 条 1`、`git 全 0/10`。⇒ 窗口内除 10 条门禁声明外，**恰好** 2 条 exit=1（必红排查）＋ 10 条 git（非门禁），**无 other 类条目**，加总与实况相符。

- **一条读数措辞 S3（只记账，不影响判定）**：§4.0 写「22 条 ＝ **10 条绿**＋2 条必红＋10 条 git」，而导出件里 `exit=0` 实为 **20 条**（10 门禁＋10 git）——「绿」若按字面读会与 20 冲突；按「门禁条目」读则 10 对。建议把「10 条绿」改成「10 条门禁（exit=0）」，与 §4.0 表头的「只列 exit=0 的门禁条目」一致。
- **live 与导出件同源**：我复核当时再点一次共享日志（`.scratch/locks/gate-runs.log`）窗口内 `ticket=398` 条目 = **22**，与导出件同数 ⇒ 导出件不是「截了一段」，窗口口径已被实况追平。

### 2.2 ② 台账与实况：**`body.ts` 一行一致；另两行是冻结挂号值，实况已漂移且已具名披露 ⚠️部分**

行数口径一律节点 `readFileSync(f,'utf8').split('\n').length - 1`（LF），**对 `HEAD` 树上的文件量**（理由见下）：

| 台账行 | 台账值 | HEAD 实测 | 工作区实测 | 一致？ |
|---|---|---|---|---|
| `src/fetch/body.ts` | **369** | **369**（blob `01e6c5aa`） | 382（他席未提交改动，见下） | **一致 ✅** |
| `src/render/wizardPort.ts` | 457 | **265**（blob `5a5d523e`） | 265 | 差 **192** ❌ |
| `scripts/gen-cli.mjs` | 729 | **754**（blob `972caabe`） | 754 | 差 **25** ❌ |

- **口径写明了吗？写明了 ✅**：`AGENTS.md:17` = 「行数口径一律节点口径」；`:29-31` = 「台账前两行的行数（457／729）是 #354 的**冻结挂号值**，也是 `check-warning-line.mjs` 的检查口径；2026-09-14 #398 当场实测另得 `wizardPort.ts` 265 行、`gen-cli.mjs` 754 行……检查脚本口径未动」。我实测的 265／754 与这段「当场实测」**逐个相等** ⇒ 挂号值来源／检查口径／当刻实况三项都写清了，且**两行差额原因（行数漂移）已被具名披露**。
- **口径边界（必须记一笔）**：`wizardPort.ts` 相差 192 行属**大额存量漂移**，`#354` 的 457 在当前 HEAD 上**已不成立**；作者用「冻结挂号值＋当刻实测另得」处理得当（登记值与实况值分开写、两者都超线），但 457 这个数字**没有任何能落到当前仓的测量基准**，建议后续票要么复核重挂、要么在台账里把 457 标成「历史快照（文件已重排）」。
- **工作区干扰的归属（不冤枉作者）**：我第一轮量到 `body.ts = 382` 属**误读**——工作区那份带着**他席 #440 未提交改动**（`git diff` 头部逐字含 `/** #440 · 13 部位的唯一来源 … */`，`20 insertions(+), 7 deletions(-)`，**未触及**本票符号 `SOURCE_FILTER_ALL`／`trendCompositionBySource`／`compositionSourceCount`）。按 `HEAD` 树量 = 369，与台账**同数**；本票改前的 289 行与改后的 369 行也都在我自己 §「改前基线自证」的快照链上能复读（289 → 369）。

### 2.3 ③ 证据 §六 sha 清单：**六笔齐、与 `git log` 逐字一致 ✅**

`§六` 列出 **5 笔**（`f12ebc7a`／`f4d7d203`／`f0123205`／`5f5b0857`／`80b0c044`），逐笔 `git log` 复读**全部存在**（`git cat-file -t` 均 `commit`）且行文与提交信息一致；§六 同时写明**权威读法** `git log --format='%H %s' -N` 与「本轮另加笔的 sha 同以 `git log` 复读为准，不在本件内做自指追补」。

作者本票提交共 **6 笔**（含本轮的 `1dfd6533`）：`§六` 未把 `1dfd6533` 写进去，**但有上面那句「后续笔以 git log 为准」的具名兜底** ⇒ 按「清单与 git log 一致」判**通过**，另记一条 **S3**：§六 标题「本票**全部**提交」与「只列到第 5 笔」在字面上仍不齐，建议把 `1dfd6533` 补进清单后删掉兜底句，或把标题改成「本件撰写时已入库的提交」。

（我这两笔复核提交 `6bb2882`／`41de349` 不属作者清单范围，按 `git log --format='%H %s' -N` 复读可见，`1dfd6533` 的父提交是 `861a056` ⇒ 作者那笔是在我复核件入库之后落的，因果不对错位。）

## 三、另外两项抽查（越声明写集登记／界外条目）

- 证据 §二 末尾已补「**越声明写集登记**」（`src/body/view.ts`：类型断言＋注释、零行为变化）——我核过：改动确实只在类型收窄与注释（`optStr` 结果原样下传），**零行为变化**成立；如实入册，与我的 S3 判定方向一致。
- 证据 §七 已补 `compareCompositions` **界外**条目（本票不改、留入口、待编排者转票）——方向与我的 S3 转票建议一致。

## 四、授权原文与条件①②③的机器读数

下列授权为**编排者具名、一次性、不类推**（协议 §2.4-4），我**逐字引用**并只按它给的三个条件复核；本条不类推、也不由我自行援引或引申：

```text
授权（编排者具名、一次性、不类推，照 §2.4-4 记录）：
「#398 对账窗口 2026-09-14T12:33:45Z–12:45:00Z 内 12 条非门禁运行条目（2 条必红排查＋10 条 git 操作）
  允许以证据 GATE-RELAX 逐条点名 runId 的形式记账，不按 §2.4-3③ 判失败。具名条件：
  ① 10 条声明条目 runId 一对一、全 exit=0、无『声称却没留痕』；
  ② 12 条逐条点名且性质属实；
  ③ 三处同源。若②③任一不成立，本授权作废。」
```

我按授权给的三个具名条件逐条取读数（**不加入授权之外的条件**）：

| 条件 | 我的读数 | 成立？ |
|---|---|---|
| ① 10 条声明一对一、全 exit=0、无「声称却没留痕」 | 10 条声明逐个按 runId 在导出件里命中**恰好 1 条**（`6dedf52b`／`a34d8b0a`／`b8572cfb`／`3b5bd56f`／`1fb1c60f`／`f2270339`＝compile、`74f34f5d`／`9755f644`／`18da80ca`／`7b19b8b3`＝test），**exit 全 0**；`missing=0`（工具未报任何「找不到对应条目」） | **✅** |
| ② 12 条逐条点名且性质属实 | `GATE-RELAX` 行内点名 **12** 个 runId，与窗口内非门禁 **12** 条**逐条一一对应**（点名齐全=true、无多报=true）；性质核实：`65938188`（12:33:51 exit=1，紧随 12:33:50 的 M-A/M1 编译，对得上「M1 假红轮」）、`ecc5ade2`（12:34:28 exit=1，`waitedMs=30010`，即 12:33:58 起跑、正落在 M2 变异轮）、10 条 git = **5 条 `git add` ＋ 5 条 `git commit -F .scratch/t398/commit-msg*.txt`**，其提交时刻与作者五笔 commit 对象的 `%ct` **逐秒对上**（`12:40:27.137Z`↔`f12ebc7a` 12:40:27Z、`12:41:23.135Z`↔`f4d7d203`、`12:43:42.205Z`↔`f0123205`、`12:44:02.572Z`↔`5f5b0857`、`12:44:55.097Z`↔`80b0c044`） | **✅** |
| ③ 三处同源 | 见 §2.1：声明窗口（证据 §4.0／§8.2①）＝导出件头 `since/until`（逐字同）＝§四 加总（22＝10＋2＋10，与窗口内性质分组的 6+6+10 自洽）；机械读数 `scoped=22 undeclared=12` 与导出件 22 条同数；live 日志同窗口亦 22 条 | **✅** |

**结论：①✅ ②✅ ③✅ ⇒ 授权条件全部成立，授权有效；这 12 条按授权与 §2.4-3 第 4 条以「GATE-RELAX 具名点名」记账，**不判 §2.4-3③ 失败**。** 授权本身与我的独立读数不冲突：10 条门禁声明合规、12 条非门禁性质属实、无造假与裸跑。

## 五、缺陷清单（本次收口验证新增／收编）

- **V1〔本票引入·S3·措辞〕** §4.0「10 条**绿**」与导出件 `exit=0` 实为 20 条的字面冲突；按「门禁条目」读成立。建议改为「10 条**门禁**（exit=0）」。不阻塞。
- **V2〔本票引入·S3·清单口径〕** §六 标题「本票全部提交」只列 5 笔、作者本票实为 6 笔（缺 `1dfd6533`），靠「以 `git log` 复读为准」兜底。建议补笔或改标题。不阻塞。
- **V3〔存量·S3·转票〕** `wizardPort.ts` 台账挂号值 457 与 HEAD 实测 265 相差 192 行（无当前测量基准）；`gen-cli.mjs` 729 vs 754（差 25）。两者**已在 `AGENTS.md:29-31` 具名披露**为「冻结挂号值 ＋ 当场实测另得」，检查脚本口径未动。**转票措辞**：「包内告警线台账重挂：按 `HEAD` 实测重填 `wizardPort.ts`／`gen-cli.mjs` 的 LF 值，并让 `check-warning-line.mjs` 的 `REQUIRED` 与台账同源（或改成扫 `src/**/*.ts` ＋ `scripts/*.mjs` 实测对账）」。不阻塞本票。
- **V4〔范围外·S3·观察〕** 复核当时工作区 `src/fetch/body.ts` 带着**他席 #440 未提交改动**（382 行）；不影响本票任何判据（我按 `HEAD` 树量），记录在案供合并点留意。

## 六、本件取代／补充 `t398-复核.md` 的哪几段

- **取代** `t398-复核.md` §5.3／§5.4 的「对账窗口与导出件不自洽」结论：作者已重导（22 条），我用 §8.2① 复跑得 `scoped=22 undeclared=12` 并与 live 同数 ⇒ **该 S3 已闭合**；原先的「窄窗口 16/6」读数只作为历史过程留档，不再是现状。
- **取代** §7 **D1**（对账窗口不同源）＝已闭合；**D2**（§六 sha 清单漏笔）＝部分闭合（5 笔齐、缺本轮的 `1dfd6533`，另有兜底句，改记 V2）；**D3**（超线未报）＝已闭合（必报五步第四步原话已入 §8.1，台账新增 `src/fetch/body.ts｜369`，我实测 HEAD=369 一致）；**D4**（越声明写集 `view.ts`）＝已登记入册。
- **保留** §7 **D5／D6**（`compareCompositions` 界外、`check-warning-line.mjs` 检查口径）＝仍待转票，本件 V3 是 D6 的升级版（补上了 457/265 的实测差额）。
- **首次给出**：授权条件的机械读数（§4 表）；`body.ts` 在 `HEAD` 树上 289→369、与台账同数的独立复读（§2.2）。

## 七、我未做与下一手缺什么

- **未改任何源码与测试**（只新建本件）；本件与 `t398-复核.md`、`1dfd6533` 三件的关系已逐段写明。
- **未跑全量门**（`pnpm test`）与 `pnpm gen`／`--filter build`（派单禁）；本件不含新的编译／测试声明，故**未产生新的门禁条目**，也未新开持锁窗口。
- **下一手缺什么**：① V1／V2 是把措辞与清单补到严（各一句，可选）；② V3 的台账重挂需另开票（会动 `AGENTS.md` 台账值 ＋ 检查脚本）；③ `compareCompositions` 的读侧来源校验仍待转票（#362 或另开数据层小票）；④ #362 接线时数据侧已备件（`sourceSeries`／`sourceCount`），不需再动本票文件。
