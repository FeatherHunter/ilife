# #273 复盘／餐别分布页 · 证据（机器读数正本）

> 交付物：`packages/skill-calorie/src/diet/reviewDocs.ts`、`packages/skill-calorie/src/diet/review.ts`；
> 探针：`packages/skill-calorie/test/diet-review-t273.test.mjs`；交 #271 的接口见 `t273-交接.md`。
> 老实物（只读，未执行其中任何脚本）：`D:\2Study\StudyNotes\SKILLS\卡路里\templates\diet_review.html`（9041 B）、
> `meal_distribution.html`（11004 B）；取数口径正本 `scripts/render_meal_distribution.py`。

## 一、10 条词（复盘 6 ＋ 餐别 5）

### 1. 复盘 6 条：真出口 exit 0 ＋ 完整文档（`test/diet-review-t273.test.mjs` 第 ① 组）

| 唤醒词 | 参数（照路由记录） | 窗口 | exit | 落盘绝对路径（当次 tmp） | 字节 |
|---|---|---|---|---|---|
| 饮食复盘（本周） | `{"window":"本周"}` | 2026-09-07 ~ 2026-09-07 | 0 | `%TEMP%\t273-S9xwSc\t273-out-tbv4im.html` | 79516 |
| 饮食复盘（本月） | `{"window":"本月"}` | 2026-09-01 ~ 2026-09-07 | 0 | `%TEMP%\t273-S9xwSc\t273-out-ngt3yg.html` | 86857 |
| 饮食复盘（最近 90 天） | `{"window":"90d"}` | 2026-06-10 ~ 2026-09-07 | 0 | `%TEMP%\t273-S9xwSc\t273-out-hx9sti.html` | 88192 |
| 饮食复盘（今年） | `{"window":"今年"}` | 2026-01-01 ~ 2026-09-07 | 0 | `%TEMP%\t273-S9xwSc\t273-out-vgj59b.html` | 88204 |
| 饮食复盘（自定义时间） | `{"window":"custom","start":"2026-09-01","end":"2026-09-07"}` | 2026-09-01 ~ 2026-09-07 | 0 | `%TEMP%\t273-S9xwSc\t273-out-wxpemz.html` | 86937 |
| 看饮食复盘 | `{"window":"今日"}` | 2026-09-07 ~ 2026-09-07 | 0 | `%TEMP%\t273-S9xwSc\t273-out-ccvtry.html` | 79516 |

（`%TEMP%` = `C:\Users\<用户>\AppData\Local\Temp`；产品落 `--html` 指定的绝对路径，探针自证不落仓内件。）

完整文档判据（逐条断过）：`<!doctype html>` 起头 ＋ `<meta charset="utf-8">` ＋ `<style>` 样式段 ＋ `ilife-page` 整页根类 ＋ 无 HTML 注释残留。

### 2. 逐块对得上老实物 `diet_review.html`

| 老实物那一块（行号） | 本仓产物里的落点 | 断言 |
|---|---|---|
| `📝 饮食复盘`（:64） | 标题 `📝 饮食复盘 <起> ~ <止>` | 逐字在 |
| 四张读数卡 `总热量／日均热量／总蛋白／日均蛋白`（:71-74） | `rv-kpi` 的 KPI 四张卡 | 四个标签逐字在 |
| `每日热量趋势`（:78） | `rv-trend` 折线（多点窗）／说明句（单点窗） | 见下「裁定 5」 |
| `高频食物 TOP5`（:84） | `rv-top` 表（列序：排名／食物／总热量／次数／餐均） | 表列序逐个相等 |
| `按餐汇总`（新仓口径行） | `rv-meal` 表（餐别／天数／累计热量）＋口径行 | 表列序逐个相等 |
| `📊 数据来源:food_log · 饮食专属复盘 · <起> → <止>`（:162） | 来源脚注（库表名按裁定 1 改人话「饮食记录」） | `📊 数据来源 · 饮食记录 · 饮食专属复盘 · <起> → <止>` 逐字在 |

`#275` 交过来的营养配比区块（八个唤醒词共出这一页）：「热量来源占比（按营养素折算）／折算合计（千卡）／推荐范围对比／营养配比」四块 + `<section id="sec-chart">` 锚点在位；页内锚点无重号（实测 `<section id=…>` 集合无重复）。

### 3. 餐别 5 条：**当刻症状（取证，不是判据）**

探针第 ⑤ 组实测：5 条词的路由记录只有 `{"window":"7d"}`（`packages/skill-calorie/src/home/routes.ts:28-32`），
命令面没有餐别这一位 ⇒ `calorie.view.diet --params '{"window":"7d"}'` exit 0，产物是**条目列表页**，
`餐别热量占比`／`<section id="md-dist">` 都没有。**根因三处 ＋ 需要的取值**见 `t273-交接.md` 第三节
（① `src/home/routes.ts:28-32` 缺 `"meal"` 位、且该文件不在本波任何人的声明路径；② `src/cli/keys.ts`／`src/cli/cmd_read.ts`；③ `src/render/dietDocs.ts` 的集成）。

### 4. `buildMealDistributionBlock()` ＋ 取数：直测读数（交 #271）

种子库（`docs/research/t81-seed.mjs`，锚点 2026-09-07）窗口 2026-09-01 ~ 2026-09-07：

| 读数 | 实测值 |
|---|---|
| `days` / `total` | 7 / 10（饮水三行不进明细） |
| `totalCal` / `avg` | 3339 / 477（日均＝累计÷**窗口天数**，老脚本口径） |
| `dist`（四桶：标签/餐数/热量/占比） | 早餐 4/989/29.6、午餐 4/1750/52.4、晚餐 1/500/15、加餐 1/100/3 |
| `oneLine`（全部餐别） | `最近 7 天共 10 餐；午餐热量占比最高（52.4%）。` |
| 早餐支 | `total=4 totalCal=989 avg=141.3 dist=[]`；`oneLine=最近 7 天早餐：4 餐，日均 141.3 卡。` |
| 明细首条 | `2026-09-01 08:00 早餐 粥 300 克 150 卡 3 克`（下午茶的苹果归「加餐」，饮水行不在明细里） |
| 区块块位 | `md-kpi`／`md-dist`／`md-table`／`md-copy` 四个锚点；明细列序＝日期／时间／餐次／食物／克数／热量／蛋白；来源行＝`📊 数据来源 · 饮食记录 · 餐别时间窗推断 · <起> → <止>` |
| 区块不是页面 | 无 `<!doctype>`、无 `ilife-block-page-shell`；不给 `command` 不出复制日志，给了则日志第 4 段＝给进来的命令原文 |
| **参数缺失即用法错** | `mealParamOf(undefined/null/''/'brunch'/'早饭')` 逐条抛 `code='bad-input'`（⇒ exit 2）；`早餐`／`snack`／`全部餐别` 各自归一到 `早餐`／`加餐`／`all` |
| 空态支（加餐 09-01~09-04） | `total=0`，值位写 `—`（不写 0）＋空态句「这一段（…）没有加餐的记录」＋引导句「用「记一餐」把那一顿记上」＋来源脚注；不出 `md-dist` |

## 二、融合裁定落点（t425 §四／§五 ④ 类）

| 裁定 | 可判形式 | 实测 |
|---|---|---|
| 1 标识符不上屏 | 可见文本零机器话 | `machineWords()` 四类命中为空；**两个具名放过**：`JSON`（公共层复制菜单格式名，与 t275／t272 同口径）、`TOP5`（老实物 `:84` 自己的可见标题「高频食物 TOP5」逐字，本票判据要求对得上老实物）；命令键只落复制载荷 |
| 2 结论句 | 标题与首个区块之间一行、含本页读数 | `<h1>` 与 `<nav class="ilife-block-toc">` 之间读到 `本窗有记录 N 天：共摄入 … 卡，日均 … 卡；蛋白合计 … 克，日均 … 克；热量目标 … 卡，达标 …/… 天。`（含 5 个读数） |
| 2-补 | 结论句与来源脚注走普通小字行 | 两处都是 `ilife-block-caliber`；页面上 `ilife-block ilife-block-feedbackBlock` 零命中 |
| 3 三条恒出 | 导航 ＋ 口径行 ＋ 来源脚注 | `aria-label="页内导航"` 在位，六个导航项各有 `<section id>` 落点；`按餐汇总…；加餐时段：下午茶、夜宵。`；来源脚注在位 |
| 4 缺值／空窗 | 缺值 `—`；窗口为空仍出完整页；库为空仍 exit 4 | 空窗（2026-09-03~09-04）exit 0、完整文档、空态句「一条饮食记录也没有」＋引导句「先用「记一餐」…」＋来源脚注；空库 exit 4 ＋ `ERR 4: 取数失败（缺失阻断）` |
| 5 图表可读数 | 单点不成线；零值不画图元 | 本周／看饮食复盘（1 天）不出 `每日热量趋势` 图，改出「本窗只有 1 天有记录，连不成趋势线…」；餐别环图只画本窗真有记录的那几餐，占比条四行照出（0 可读） |
| 7 复制区双按钮 | 双按钮 ＋ 日志第 4 段＝命令原文 | `data-fmt-open="1"`＋`data-fmt=text/json/csv`＋`data-action-id="ilife-copy-log"`；日志六段齐，第 4 段＝`calorie-cmd-read calorie.view.diet-review --params '{…本次参数…}'`；无与按钮同名标题 |

## 三、探针与门禁（当刻读数）

| 判据 | 命令（都在加锁窗口内直接调用） | 读数 |
|---|---|---|
| 探针（逐条真跑＋区块直测＋空态＋参数缺失） | `node --test packages/skill-calorie/test/diet-review-t273.test.mjs` | `ℹ tests 27 / pass 27 / fail 0`，exit 0（两次：编译回绿那次 ＋ 变异还原后那次） |
| 10 条词真跑取证（**默认落点**） | `node docs/skills/skill-calorie/t273-真跑.mjs` | `RESULT: 11/11 条 exit 0 且产物是完整文档；复盘 6 条 6/6`，exit 0 |
| **变异自证（源码级）** | 把 `reviewDocs.ts` 的 `{ label: '总蛋白',` 改成 `{ label: '蛋白总量',` → 编译 → 探针；再逐文件还原 → 编译 → 探针 | **红**：`MUT-READING red exit=1 ℹ tests 27 | pass 21 | fail 6`；**还原后绿**：`MUT-READING green exit=0 ℹ tests 27 | pass 27 | fail 0`；`git status --porcelain` 对本件为空 |
| 告警线台账门（真实门禁无参运行） | `node packages/skill-calorie/scripts/check-warning-line.mjs` | `RESULT: 79/79 | PASS: 告警线台账齐全且与实况一致`，exit 0（含本件 351 那一行） |
| 生成物一致门 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | **exit 1**，`GEN-STALE`：`src/home/commands.ts`／`src/home/routes.ts` 内容印迹与上次 `pnpm build` 时不同（**范围外**，别人在飞；须先 `pnpm build`，而当刻树编译不过） |
| 门禁运行条目 | 见 `docs/skills/skill-calorie/t273-gate-runs.md` | 23 条（逐行抄自 `.scratch/locks/gate-runs.log` 的 `ticket=273`） |

复跑取证脚本与环境：`t273-真跑.mjs` 会把 10 条词的产品落到
`%TEMP%\t273-live-<随机>\calorie_html\<中文名>_<YYYYMMDD_HHMMSS>.html`（与共同口径「产物落点」一致；探针那 6 条走 `--html` 指定绝对路径）。

## 四、未成立的对照（如实记，不假装）

**作用面基线对照（「我这条路不新增红」的机器证据）当刻未成立**，两条原因都写清：

1. **脚本自己的错**：基线那一支用 `git show HEAD~1:<路径>` 取「改之前」的版本，但 HEAD 已被别人的提交推进 ⇒ 取回的仍是**我自己那一版**（机器读数：还原后 `git diff --numstat` 为空，即 `F-REVERT-DIFF:` 空行）；
2. **共享 dist 被并发重编**：同一窗口里 `packages/base-render/dist/index.js` 与 `dist/pageShapes.js` 的导出名对不上
   （`renderMediaPlaceholder` 不存在）⇒ 起头那次探针只跑出 `ℹ tests 1 / pass 0 / fail 1`（模块装载失败），
   与同一窗口后段「编译回绿后 27/27」不是同一个 dist 状态，**两态读数不可比**。

⇒ 按编排者 2026-09-15 追加口径：这一条记「**范围外阻塞**」（根因＝跨图共享 dist 被并发写者重编，已立 **#540**）＋
写进「未做项」，**不修别人的件、不为它反复重跑**。可比的那一半证据＝上面那张表里的探针 27/27 ＋ 变异红/绿两行。

## 五、范围外发现（不修、不重跑，按编排者 2026-09-15 追加口径记账）

1. **树当刻编译不过，错在别人的在飞件**（同一个 TypeScript project ⇒ 整包不产出）：
   `src/body/compare.ts(211,26) TS2345`（编排者自测）／`src/photo/galleryDoc.ts(218,69) TS2554`＋`(220,14) TS2304 budgetNoticeHtml`／
   `src/photo/gifDoc.ts(43,42) TS2305 base-paint/blocks 无 renderMediaFigure`（本票自测）。归属＝照片席／公共层席；#540。
2. **锁被别的会话长期持有**：2026-09-15 05:40 起的一次窗口 `--max-wait-ms 900000` 等满 15 分钟未获，
   包装器打 `FAIL: 等待锁超时（waitedMs=900738）`；抄到的持有者＝`ticket=523` 的 `dsh --profile visionreview`
   （长会话，连续持有）。处置＝改后台控制器（每次最多 5 分钟、最多 4 次）继续抢；仍等不到就按协议停手上报，不假装跑过。
3. **告警线台账里本件那一行**：`AGENTS.md` 的台账原有自动补出的
   `| src/diet/reviewDocs.ts | — | 351 | …超因与拆法待补… |`。第四步正文（超因＋拆法）落在**那一行的结论列**，
   而 `AGENTS.md` 不在本票声明路径 ⇒ 已请编排者裁决，获**具名授权（A）**后只改那一行（351 实况不动、不压行数）。
   当刻台账门读数＝`RESULT: 79/79 PASS`（含本行）。
4. **提交面**：控制器重试让同一条提交信息落成**三笔**（`d7a53f3`／`ee86b1e`／`950f5c9`，内容互补、逐笔路径不同：
   ①证据＋交接＋真跑脚本 ②门禁条目 ③台账那行＋门禁条目）；`--amend`／`rebase` 协议禁用 ⇒ 不收口，只在此点名。
