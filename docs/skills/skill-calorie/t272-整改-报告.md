# #272 整改 · 报告（S1 契约违反：空窗未出完整页）

- 工作目录 `D:\ilife-wt\wf155-g`；**基线 sha ＝ `de38a79954c6b9d1028a6bcf214f5b6a0ddba5f2`**（创建 worktree 当刻 `D:\ilife` 的 HEAD）。`D:\ilife` 与别的 `wf155-*` 一字未碰。
- 判据正本 `docs/skills/skill-calorie/t425-融合基准.md` 裁定 4 的 2026-09-15 澄清（`f0decc8`）；被审件＝复核席 `t272-收口复核.md`。
- 可复跑：`node tooling/run-locked.mjs --ticket 272 --max-wait-ms 1800000 -- node docs/skills/skill-calorie/t272-整改-run.mjs`（日志 `.scratch/t272g/run-final2.log`，末行 `RESULT-ALL: PASS`）。

## 一、判定

**S1 已消；三条 S3 已改；`exit 4` 未放宽，空库未改成出页。**

两态在取数层分开，判据只看**行数**，不看错误文案：

| 实况 | 判据 | 出口 |
|---|---|---|
| **窗口为空**（有命令、有窗口，库里有别处的记录、这一段零记录） | 整表 > 0 且窗内 = 0 | `exit 0`、1 件产物、回绝对路径；完整页 ＋ 空态句 ＋ 引导句 |
| **库为空**（`food_log` 整表零行） | 整表 = 0 | `exit 4` ＋ `ERR 4: 取数失败（缺失阻断）`、**0 产物**（逐字保持） |
| 窗内有行、被榜的口径筛空（只播一条 💧水 看低热量榜） | 整表 > 0 且窗内 > 0 | 两支都不是 ⇒ 仍走既有缺失阻断（未放宽） |

单榜（带 `category`）与全榜两条命令逐条都判，两态出口码不同。

## 二、机器证据（当刻读数）

- 编译 `npx tsc -b packages/skill-calorie` → `exit=0`。
- 两态探针：`[two-state] 单榜 看高热量榜 库空: exit=4 产物=0 文案="ERR 4: 取数失败（缺失阻断）…" ｜ 空窗: exit=0 产物=1 完整文档=true 空态句=true 引导句=true`；全榜同形 ⇒ `RESULT-TWOSTATE: PASS`。
- 回归：作者原批 `tests=26 pass=26 fail=0`；新批 `tests=6 pass=6 fail=0`；复核席 `RESULT-RUN22: exit0 22/22 有产物 22/22`；**空库档 `RESULT-EMPTYDB: PASS`**；`RESULT-NEW: PASS 红=0 检查项=31`；复核席变异脚本 `RESTORE-甲／乙 与进场一致=true` ⇒ `RESULT-REGRESS: PASS`。
- 变异自证（本票两处，两行机器读数）：
  - `MUTATION-甲（两态判别改坏：窗口为空照样抛缺失阻断）: tests=6 pass=3 fail=3` → `RESTORE-甲: pass=6 fail=0 与进场一致=true`
  - `MUTATION-乙（三段恒 100% 退回各段四舍五入）: tests=6 pass=5 fail=1` → `RESTORE-乙: pass=6 fail=0 与进场一致=true`
- 全包 `node --test packages/skill-calorie/test/*.test.mjs`：改动前红名 81、后 80，**逐名对账零新增**（唯一差异 `#313 文件级隔离` 由红转绿，与本票无涉）。
- 别的族（改前＝HEAD 版三件／改后＝本票版，冻结时钟同参数）：`看今日饮食（calorie.today）` 与 `饮食复盘（本周）（calorie.view.diet-review）` 产物 sha256 各自逐字节一致（`2FEAD72E…`／`2DFDEFB6…`）⇒ `RESULT-OTHERFAMILY: PASS`。**未走「共用件」让步条款**：`dietEngine.ts` 一行未动。
- 告警线台账 `RESULT: 76/83`；红点 7 条全是别家件的台账陈化，本票写集内无红。

## 三、逐条路径

1. **两态判别落取数层** —— `src/diet/rankingPlate.ts`（82→131）：新增 `hasAnyDietRow()`／`dietRowsInWindow()` 两条读数；单榜命中 `missing-data` 且库非空、窗内零行 ⇒ 回零条目的盘；全榜五榜全空时同判。`bad-input`、`FetchError` 两支一字未改。
2. **完整页 ＋ 空态句 ＋ 引导句** —— `src/diet/rankingDocs.ts`（322→359）：`buildRankingDoc()` 在零条目时、`buildAllRankingsDoc()` 在 `okCount` 0 时各出一段 `emptyGuide` 空态块（空态句「本窗 … 里一条饮食记录都没有」＋引导句「说「记一餐」…补以前的日期就说「补记饮食」」）；读数卡／页内导航／口径行／复制区／来源脚注照旧恒出，表位仍走 `emptyText` 占位。
3. **新断言** —— `packages/skill-calorie/test/t272-空窗两态.test.mjs`（181 行，6 条）：同一命令两份库，出口码不同（4≠0）＋库空 0 产物＋空窗 1 产物且完整文档含两句；空窗页仍是完整页（导航／口径行／来源脚注／结论句含读数）；三段恒好 100%（文字三段和＝100、条宽 `[24,48,28]` 和＝100）。
4. **接线件** —— `src/diet/ranking.ts`（33→37）两条出口**一行未改**（读数照 `items.length`／`okCount` 取），只补件头两句说明。
5. **S3-①** 逐色描述改为如实：老实物 `.protein{background:var(--accent)}`＝`#0071e3`（`food_ranking.html:12`），新侧色板首位 `#007aff`，**不是同一个蓝**；碳水 `#34c759`／脂肪 `#ff9500` 同值（`:13`）；取公共层色板本身对（裁定 8）。落 `t272-判据与裁定.md:69` 与 `rankingDocs.ts` 两处注释。
6. **S3-②** 行数改实数：`t272-自证.md`、`t272-判据与裁定.md` 写成进场 322／33（原 296／30 标明抄错），整改后 359／37／131。
7. **S3-③** 「三段恒好 100%」由新断言第 ③ 条钉住（变异乙下 `fail=1`）。
8. **台账同步**：`t272-判据与裁定.md` 裁定 4 行与 ③ 列第 12 行改写为整改后事实（初版「遗留出口 1」留作历史）。**第四步**：`rankingDocs.ts` 322→359 **已超线，需要根据规则进行重构。** 超因＝一件同时住两页装配与 `RANK_CSS`、整改又补空窗整页；已在 `packages/skill-calorie/AGENTS.md` 挂号（拆法＝样式段另立 `rankingDocsCss.ts`，照 `render/reviewDocsCss.ts` 先例；本次先不拆）。`.changeset/t272-空窗整改.md` 新增。

**碰到**：`src/diet/ranking{,.Docs,Plate}.ts`、`test/`、`packages/skill-calorie/AGENTS.md`（登记行）、`docs/skills/skill-calorie/t272-{判据与裁定,自证,整改-报告,整改-run}`、`.changeset/`。**未碰**：`src/home/`、`src/cli/`、`src/triggers/`、`dietEngine.ts` 及其余件；`git add` 全带 pathspec。

## 四、未做项与下一手缺什么

1. **未开视觉墙逐格审版式**（挤／塌列／被藏的横滚）：空窗两张整页只做结构级判定。
2. **全包 80 条红名未逐条归因**：只做与基线的名字级对账（零新增）；逐条归因不在本票范围。
3. **台账 7 条陈化行未 `--sync`**：全在别家件的行上，不越界改；本票只挂自己那一行。
4. **`rankingDocs.ts` 越线未拆**：拆法已进台账，待收口票认领。
5. **下一手缺**：① 结构票（样式件切分）；② 视觉墙逐格（空窗整页＋22 条正常页）；③ 编排者若要 `#279`／`cmd-read` 系列跟着改期望——本整改**未改任何既有断言的期望**，那些断言当刻全绿，本轮不需再裁；④ `-复跑22条.mjs` 的空窗两条只打印不断言，可升成断言档（本轮新断言已覆盖同一件事）。
