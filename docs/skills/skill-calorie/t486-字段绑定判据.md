# #486 运动域「字段键 → 中文」绑定判据 · 证据

判定：**绿**（本票交付的判据件 28/28；自证红绿两行齐；三处红全是别席在途／已提交件所致，逐条归因在 §四）。

本票是补判据票：不改任何实现件的接口与语义，只新增判据件与证据件，并给
`docs/skills/skill-calorie/t449-字段中文标签.md` 补读数时刻（不改原结论，只加 §九）。

## 一、写集声明与现场判断

派工单第 4 节给本票的写集（只写这些，共 3 件）：

| # | 路径 | 状态 |
|---|---|---|
| 1 | `packages/skill-calorie/test/field-label-bindings-486.test.mjs`（新） | 真实：新建，28 条断言（26 绑定＋计数 guard＋空串回退） |
| 2 | `docs/skills/skill-calorie/t486-字段绑定判据.md`（新，本件） | 真实：在写 |
| 3 | `docs/skills/skill-calorie/t449-字段中文标签.md` | 真实：只追补 §九「读数时刻补记」，原 §一～§八一字未动 |

票面「不许动的东西」逐条现场判断（命令与读数）：

- `shared/fieldLabel.ts` 接口：未动。`git status --porcelain packages/skill-calorie/src/shared/fieldLabel.ts`
  读数＝空（干净）；`git diff --stat` 无该件。
- 实现件语义：未动。自证变异临时改过 `src/exercise/fieldLabels.ts` 一处对调，还原后
  sha256＝`6882ea1bbfd1…`（与变异前逐字节一致，脚本内断言），`git status --porcelain` 该件读数＝空。
- 公共层（`packages/base-render/`）：未动，无该目录下改动。
- 已关票证据件与样张：除 t449 按票面允许补时刻外，其余未碰。
- 老技能目录：只读（仅读 `dist/` 产物跑判据）。

## 二、逐键对照表（26 键，键与中文都在判据件写死）

| 键 | 中文 | 口径 |
|---|---|---|
| `type` | 运动类型 | CLI 参数名 |
| `calories` | 消耗 | CLI 参数名 |
| `minutes` | 时长 | CLI 参数名 |
| `date` | 日期 | CLI 参数名 |
| `time` | 时间 | CLI 参数名 |
| `note` | 备注 | CLI 参数名 |
| `category` | 分类 | CLI 参数名 |
| `difficulty` | 强度 | CLI 参数名 |
| `distance` | 距离 | CLI 参数名 |
| `steps` | 步数 | CLI 参数名 |
| `reps` | 次数 | CLI 参数名 |
| `loadKg` | 重量 | CLI 参数名 |
| `setIndex` | 组号 | CLI 参数名 |
| `backfill` | 补录 | CLI 参数名 |
| `heartRate` | 平均心率 | CLI 参数名 |
| `maxHeartRate` | 最高心率 | CLI 参数名 |
| `exercise_type` | 运动类型 | 库列名 |
| `duration_minutes` | 时长 | 库列名 |
| `calories_burned` | 消耗 | 库列名 |
| `distance_km` | 距离 | 库列名 |
| `avg_heart_rate` | 平均心率 | 库列名 |
| `max_heart_rate` | 最高心率 | 库列名 |
| `load_kg` | 重量 | 库列名 |
| `set_index` | 组号 | 库列名 |
| `is_backfill` | 补录 | 库列名 |
| `is_deleted` | 删除标记 | 库列名 |

判据件不 import 被测表当期望值：对 `dist/exercise/fieldLabels.js` 只有裸副作用 import
（触发登记），从不读 `EXERCISE_FIELD_LABELS`；`grep EXERCISE_FIELD_LABELS` 在判据件命中＝0（除注释提及）。

现状负向读数（要补的那一格今天确实不设防）：旧判据件带具体中文的绑定断言只有 1 条
（`field-labels-449.test.mjs:215`）；内存模拟对调（`duration_minutes→消耗`／`calories_burned→时长`）
下旧两条口径（纯中文、有无参数名）双双放行——`.scratch/t486/swap-blind-probe.mjs`，
读数 `pureChinese=true paramNameHit=false ×2`，结论盲区属实。

## 三、GATE-RUN 对账行（取数时刻 2026-09-15 22:49–22:55＋08:00，即 UTC 14:49–14:55）

| 门 | runId | exit | 读数 | 日志 |
|---|---|---|---|---|
| `pnpm build`（整仓） | `e3246d69-6424-4b9a-b3e2-2b8ad4be5c30` | 2（红，别席，见 §四） | `planEditorPort.ts` TS2304×2 | `.scratch/t486/build.log` |
| 新判据 `field-label-bindings-486.test.mjs` | `0a06e5cf-282f-4a8a-8315-7f946d529a57` | 0 | pass 28 fail 0 | `.scratch/t486/new-test-green.log` |
| 变异自证脚本 `.scratch/t486/selfproof.mjs` | `d06fe992-b85a-4435-be84-ed0527a9f2d7` | 0 | 红绿两行见 §四 | `.scratch/t486/selfproof.log` |
| `field-labels-449.test.mjs`（零回归） | `6eca26ff-2ccb-4e30-9115-11bd4203d142` | 1（红，别席，见 §四） | pass 16 fail 1（仅④单源） | `.scratch/t486/gate-449.log` |
| `exercise-receipt-fusion-423.test.mjs` | `c8ff2444-5c3e-4f11-964e-e1843e9d6295` | 0 | pass 20 fail 0 | `.scratch/t486/gate-423.log` |
| `node --test exercise-receipt-264.test.mjs` | `ca54fcaf-81f3-4382-a94d-c105b19c4085` | 0 | pass 13 fail 0 | `.scratch/t486/gate-264.log` |
| `check-warning-line.mjs` | `49db9367-305e-46fd-b34a-1daff7e32290` | 1（红，别席，见 §四） | 2 处陈化 | `.scratch/t486/gate-warn.log` |

以上 7 条与 `.scratch/locks/gate-runs.log` 里 `ticket=486` 的 RUN 行逐条对得上（START／RUN 成对，7 对）。

## 四、变异自证两行（runId `d06fe992`，exit 0 那次内跑）

| 变异 | 改坏必红 | 还原必绿 |
|---|---|---|
| `duration_minutes↔calories_burned` 中文对调（`src/exercise/fieldLabels.ts` 临时，还原 sha256＝true，原哈希 `6882ea1bbfd1…`） | `SELFPROOF-RED exit=1 fail=2 pass=26`（恰好对调的两条红，其余 26 条仍绿，定位准） | `SELFPROOF-GREEN exit=0 pass=28 fail=0` |

还原验证：源件哈希与变异前一致，还原后重建，dist 含 `duration_minutes: '时长'` 回来；
`git status --porcelain` 该件读数＝空。

## 五、未做项与归因（红全是别席的）

1. 整仓 `pnpm build` exit 2：`src/render/planEditorPort.ts`（别席未跟踪在途件，`git log` 无记录）
   报 `SLOTS` TS2304。本票一行未碰该件；本票判据只读 `dist/shared/fieldLabel.js`／
   `dist/exercise/fieldLabels.js`，两处源件干净且变异前后重建 emit 正常（tsc 无 `noEmitOnError`，红中仍落盘，已验 dist 内容）。
2. `field-labels-449.test.mjs` 16/17：红的是 ④「标签单源」之登记计数——他席已提交的
   diet／weight／workout 三域表（`git log`：#351 `5b086c0`／`8285c15`、#496 `f28229e`）照查表口径
   各自登记，本是设计允许的演进（`shared/fieldLabel.ts` 件头「第二个域有表时照同一口径登记」），
   旧断言「只许一处」已陈化。修它要改 `field-labels-449.test.mjs`，不在本票写集，不碰；转 #449 原席或另开票。
3. `check-warning-line.mjs` 2 处陈化：`src/weight/log.ts`（402→403，别席在途未提交，`git status` M）、
   `src/diet/libraryDocs.ts`（396→401，#274 `f28229e` 提交未同步台账）。本票新增只在 `test/` 与
   `docs/`（扫描面外），结构上不可能致陈化；同步归属各件的席。
4. 未做项：票面要求即全部（逐键绑定 26 条＋空串分支＋t449 时刻＋自证），无缺口；
   ④ 的新断言在复核席独立复跑前仍以本证据件 runId 认口。
