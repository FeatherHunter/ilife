# 场景04（运动 39 页）· 终评重跑席证据（#268 重跑）

**一句话**：上一轮全批 39 页 94.1；此后 `14–30`（#523 · 4a16445）与 `31–39`（#544 · 82b063f）两族
**各重出了自己那几页**。本席把全批 39 页**两档重新读了一遍**、把上一轮那份打回项目录**逐条拿到当前字节上复验**、
重出两墙与索引并走完自检正反例。**判定：全批 ≥90 通过；两族重出版本确实把上一轮点名的缺口清掉了。**

---

## 一、写集声明

**新建或改写（入仓，本次提交的全部内容）**

| 件 | 动作 | 作用 |
|---|---|---|
| `docs/skills/skill-calorie/t268-终评-全批39-重跑.md` | 新建 | 本件 |
| `docs/skills/skill-calorie/t541-证据.md` | **追加一节** | 上一张收口票的证据件，记两墙／索引在产物换版后的重出读数 |
| `docs/skills/skill-calorie/t541-逐格缺陷清单.md` | 不改 | 只引用（模板，缺陷由负责人填） |
| `docs/skills/skill-calorie/t541-墙生成器.mjs`／`t541-索引生成器.mjs` | 一行未改 | 同一目录同一生成器重出 |

**新建（不入仓，`.scratch/t268/`）**：`同步重出版.mjs`／`版本身份核对.mjs`／`出当刻哈希.mjs`／`基线复验.mjs`／
`score-重跑.mjs`／`复验视觉席新发现.mjs`／`gen-重跑brief.mjs`／`追加终审附节.mjs` 与各自的 `.log`／`.json` 读数。

**一行未碰**：`packages/**`（本席不碰源码，故无编译无包测试）、`docs/agents/**`、
`.scratch/t523|t543|t544/out`（三处来源目录一件未改，只读）、别人的票号目录。
`git` 只 `add`／`commit` 上表两件；不切分支、不 `reset`、不 `stash`、不 `push`。

**口径出处**：`docs/agents/视觉验收墙.md` §3–§5（墙与人看、生成器入仓、收口证据落 `docs/skills/<件名>/`）、
§4（正例 exit 0 ＋ 反例 exit 1 点名）、§6.2（`dropped` 与 `dead` 两条一起判）、§7（不加 `loading="lazy"`；清单不带签名）。

---

## 二、任务一：重跑读数与打分

### 2.1 先做的一件事：**批次归位**（上一轮前提与实际盘面不符）

票面前提是「14–30 与 31–39 两族已重出自己那几页」。盘上实情是：

| 目录 | 当刻内容 |
|---|---|
| `.scratch/t541/out`（本批交付目录） | `14–30` 已是 06:29 重出版；**`31–39` 还是 05:13 的旧版** |
| `.scratch/t523/out` | 39 件，`14–30` 与 t541 同版 |
| `.scratch/t543/out` | 39 件，全批 05:06 版（＝上一轮评分那一版的前身） |
| `.scratch/t544/out` | **37 件（缺 09／11）**，写于 **07:53:23–07:53:37**，紧跟 82b063f（07:53:04）——**31–39 的重出版在这里** |

⇒ 判词：**82b063f 的重出版没落到本批交付目录**，t541/out 的 31–39 仍是旧版。
本席把 t544/out 的 31–39 **同步进 `.scratch/t541/out`**（只写本批交付目录；t544/out 只读未动），
并顺带把 t544 有的其余页也取自同一版构建，使「全批 39 页」= 一次构建 + 09／11 两页（t544 无源）。
逐件同步前后 sha256 落 `.scratch/t268/批次归位.json`。

**版本身份**（`.scratch/t268/版本身份核对.mjs`）把四个目录的同名件**剥掉两处构建时刻**（复制日志里的
`时间戳版本 …` 与写后回执页头／改前改后表的**操作时刻**——夹具把操作时刻取成构建时刻）后比 sha256，结论：

- **01–13 四个目录归一化后同渲染 ⇒「记族未改版」属实**（本轮按未改版打分）；
- 有**真实内容差异**的恰是 `14–22／25–30`（#523 R5）与 `31–39`（#544），`23／24` 未改。

### 2.2 三个口径的读数（每个口径都保留一位小数）

| 口径 | 谁算 | 扣什么 | 全批平均 | 与上一轮 94.1 |
|---|---|---|---|---|
| **A 全症状口径** | 视觉席 | 读者看得见的症状**全算扣** | **94.2** | ＋0.1 |
| **B 基线目录口径** | 终审席 | 上一轮那份打回项目录**逐条拿当前字节复验**，已清的不再扣 | **96.0** | ＋1.9 |
| **C 页级口径** | 视觉席 | 只扣「这一页生成件能改掉」的 | **97.7** | （上一轮无此口径） |

**是否 ≥90：是**（三个口径都在 90 以上）。**无族掉到 90 以下，故本轮不触发「不达标」。**

### 2.3 每族平均：与上一轮 94.1 逐族对比

| 族 | 页数 | A 全症状 | 上一轮 | 变化 | B 基线目录 | 变化 | C 页级 |
|---|---|---|---|---|---|---|---|
| 记族（01–13，**未改版**） | 13 | **95.2** | 95.5 | −0.3 | 95.5 | 0.0 | 98.2 |
| 看族（14–30，4a16445） | 17 | **92.6** | 93.2 | −0.6 | 95.8 | **＋2.6** | 97.1 |
| 析族（31–34，82b063f） | 4 | **94.5** | 95.0 | −0.5 | 97.0 | **＋2.0** | 97.0 |
| 盘族（35–39，82b063f） | 5 | **96.8** | 92.8 | **＋4.0** | 97.4 | **＋4.6** | 98.8 |
| **全批** | **39** | **94.2** | **94.1** | **＋0.1** | **96.0** | **＋1.9** | **97.7** |

### 2.4 本轮改动那两组页的分数变化

| 组 | A 全症状（均值变化） | B 基线目录（均值变化） | 怎么读 |
|---|---|---|---|
| **14–30**（看族 17 页 · 4a16445） | **−0.5** | **＋1.7** | 自己那批打回项（窗口节改只读键值行、表题不复读天数、表内数值补单位）**确实清掉了**（B ＋1.7）；A 微负是因为视觉席本轮**对看族多查出一批新页级症状**（读数卡块无题、导航词与卡题不同名、截断说明错位），不是改坏了 |
| **31–39**（析／盘 9 页 · 82b063f） | **＋2.5** | **＋2.9** | 两条口径**同向上涨**：盘族 A ＋4.0／B ＋4.6，析族 A −0.5／B ＋2.0（析族同样被新症状抵掉一部分） |

**记族注**：`01–13` 本轮**按未改版参与打分**（其 P1 返工另走一张票），B 口径下逐页与上一轮完全相同；
A 口径记族 −0.3 来自视觉席本轮**新查出**的几条页级症状（07／08／12／13 明细卡层级、08 徽标与状态打架），
不是记族退步。

### 2.5 上一轮那份打回项：逐条复验（B 口径的分子）

| 上一轮条目 | 复验读数（当前字节） | 判 |
|---|---|---|
| **E1** 参数输入框 <44px（31–39） | 两档「<44px 的可交互元素」＝ **0**；表单实测 434×44（1440）／173×44（390） | **已清** |
| **K1** 一排四卡首卡 28px（31–39） | `font-size:28px` 出现次数 ＝ **0** | **已清** |
| **K2** 折线纵轴负数刻度（37–39） | 刻度文 ＝ `0 / 2000 / 4000`，无负值 | **已清** |
| **K6** 三节无可见标题（35–39） | `#sec-figures`／`#sec-category`／`#sec-badges` 三节**均有 h2** | **已清** |
| **G1a／G1b／G1c** 「窗口」节用参数表单（14／15／25／26／27） | `#sec-window` 已改 `sui-facts` 只读键值行，无 `ilife-block-param-form` | **已清** |
| **G2** 「记录数」同页说两遍（14／15／25／26／27） | 「记录数」出现 **1** 次 | **已清** |
| **I1** 表内数值裸数（16–22／28–30） | 表内纯数字格 ＝ **0** | **已清** |
| **I2** 表题复读「本窗共 N 天」（16–22／28–30） | 16–21／28 已清；**22／29／30 仍各 1 处** | **部分已清** |
| **B4** 单条 fact 组拉满整行（01–13） | 07／08 无单条组、余 11 页仍有；记族未改版故**不加分只留痕** | **沿用** |
| 其余（B1／B2／B3／B5／B6／C1／C2／D1a–D1c／D2／F1–F5／H1–H3／K4／K5） | 机读逐条复验**仍在** | **仍命中** |

脚本：`.scratch/t268/基线复验.mjs`（读数落 `重跑-基线复验.json`）；打分：`.scratch/t268/score-重跑.mjs`。
视觉席正文：`.scratch/t268/视觉终评-全批39-重跑.md`（§1–§8；§9 是终审席追加的三口径对照与逐页变化）。

### 2.6 本轮新查出、上一轮目录里没有的项（已机读复核）

| # | 页 | 症状 | 回哪个源件 |
|---|---|---|---|
| N1 | 14–22、25–30、31–34（**21 页**） | 读数卡块／分布块**没有可见标题**（`#sec-kpi`／`#sec-figures`／`#sec-dist`／`#sec-ratio` 开头 300 字内无 h2／h3／题元素）；35–39 已在同位置补上三节题，**一页两种待遇** | 523 看族／析族页生成件 |
| N2 | 30 | 折线横轴两端标签逐字 `09-16` 与 `09-15`（窗口跨年 2025-09-16 → 2026-09-15，轴只印 `MM-DD`），读者读成「起点比终点晚一天」 | 523 汇总页标签格式化 ＋ 共用层图表件刻度格式 |
| N3 | 19 | 折线卡**没有任何数据圆点**（`<circle>` ＝ 0；同构件 17／21 各 3／6 个） | 523 汇总页图表数据装配 |
| N4 | 16–22、28–30、31、33 | 导航词与卡题不同名（「逐日明细」对「按日消耗」等） | 523 页生成件（导航标签） |
| N5 | 22、28、29、30、37、38、39 | 同列精度混排（「5702 卡」对「1680.8 卡」） | 523 页生成件数值格式化 |
| N6 | 22、29、30 | 「逐日表已截断」说明排在 100 行表之后 | 523 汇总页生成件 |
| N7 | 07、12、13 | 「逐条明细」卡与同级卡不同层 | 523 记族页生成件 |
| N8 | 32、33、34 | 眉标与 H1 用词不一致；距离单位「公里／km」混用；卡题称「两套刻度」而图上只有一套 | 523 析族页生成件 |

复核脚本：`.scratch/t268/复验视觉席新发现.mjs`（三条最要紧的新发现逐条拿机读取证，全部成立）。

---

## 三、任务二：两墙与索引重出 ＋ 自检正反例

**产物字节变了 ⇒ 墙与索引跟着重出**（同一目录 `.scratch/t541/out`、同一生成器）：

| # | 命令 | 读数 | exit |
|---|---|---|---|
| ① 正例（手机墙） | `node docs/skills/skill-calorie/t541-墙生成器.mjs D:\ilife\.scratch\t541\out t541-手机墙.html 390` | `手机墙 t541-手机墙.html：39 格；链接 78 条；缺失 0 -> 可发` | **0** |
| ① 正例（桌面墙） | 同上，`t541-桌面墙.html 1280` | `桌面墙 t541-桌面墙.html：39 格；链接 78 条；缺失 0 -> 可发` | **0** |
| ① 正例（索引） | `node docs/skills/skill-calorie/t541-索引生成器.mjs D:\ilife\.scratch\t541\out` | `索引 t541-索引.html：39 件；链接 41 条；缺失 0 -> 可发` | **0** |
| ② 反例（墙） | 清单加一条指向 `99-不存在的页（反例夹具）.html` 的假条目后跑同一命令 | `手机墙 …：39 格；链接 78 条；缺 1 件 -> 99-不存在的页（反例夹具）.html（清单第 99 行：盘上没有这个文件）` | **1** |
| ② 反例（索引） | 同一假条目下跑索引 | `索引 …：39 件；链接 41 条；缺 1 件 -> 99-不存在的页（反例夹具）.html（清单点名而盘上没有）` | **1** |
| ③ 回绿 | 删掉假条目，两墙与索引各跑一次 | 三条读数都回到「缺失 0 -> 可发」 | **0** |

**另两条自查**：墙页 `iframe` 39 个、`loading="lazy"` **0 处**；清单不带签名（BOM 判据 false）。
墙上看到 31–39 的体积已换成重出版（96–108 KB，旧版 95–107 KB），即**墙里跑的确实是当前字节**。

---

## 四、任务三：负责人怎么看

1. **双击 `.scratch/t541/out/t541-手机墙.html`**（390 宽、3 列）滚一遍 —— 看**塌列／挤／触摸目标**。
2. **双击 `.scratch/t541/out/t541-桌面墙.html`**（1280 宽、1 列、整体缩放）滚一遍 —— 看**排布／层级／留白**。
   （两张墙都不用起服务；格子里跑真 HTML，能点能滚；拿不准的格子点标题开整页。）
3. 逐件查细节开 `.scratch/t541/out/t541-索引.html`（39 件按族分组，每件给族／体积／该确认什么）。
4. **逐格缺陷写进 `docs/skills/skill-calorie/t541-逐格缺陷清单.md`**（模板已备表头）；没问题也要写结论并写清看了哪些格。

**每格该确认什么**（本轮的看点，按族）：

- **写后回执族 01–13**：四条口径各占一行；「本次明细」与「当日累计」分得开；改删页三态表扫得出变了哪条。**本族本轮未改版**。
- **明细／汇总／对照目标族 14–30**：窗口节现在是**只读键值行**（不再是可以改的输入框）；表题不再复述天数、表内数值带单位；**新看两处** —— 读数卡块有没有标题、导航词与卡题同不同名。
- **分布／力量／有氧／复盘／趋势族 31–39**：三节小标题（核心数字／类型分布／高频运动）在不在；折线纵轴有没有 `0／2000／4000` 刻度；占比 <1% 的分布条是不是还读成空行；30 号页跨年轴两端标签是不是 `09-16` 对 `09-15`。

**两条别混**：墙上能验的是**版式**；**墙上看不出错 ≠ 对**（数据对不对、命令键对不对、边界路径走没走到，归机检判据）。

---

## 五、GATE-RUN 声明（本席窗口内经加锁包装器跑过的命令）

**对账窗口**：`--since 2026-09-15T23:57:00Z --until 2026-09-16T00:20:00Z`。
**复核命令**：

```
node tooling/run-locked.mjs --ticket 268 -- node tooling/check-gate-audit.mjs \
  --evidence docs/skills/skill-calorie/t268-终评-全批39-重跑.md \
  --ticket 268 --since 2026-09-15T23:57:00Z --until 2026-09-16T00:20:00Z --allow-nonzero
```

```text
GATE-RUN runId=bc8f4d22-e371-4b73-a3b8-aa537be19bfa cmd="node .scratch/t268/同步重出版.mjs"
GATE-RUN runId=662b563a-9084-4c4d-be64-fdaa05f3f085 cmd="node .scratch/t268/dom-read.mjs"
GATE-RUN runId=0c6d44e8-0aa2-4d40-8b3a-4c24c7a16bd9 cmd="node .scratch/t268/chart-scan.mjs"
GATE-RUN runId=18cbbe56-7956-4111-9b41-a84242b3dbc1 cmd="node .scratch/t268/tick-scan.mjs"
GATE-RUN runId=db524bb4-5986-4ef8-83d4-451c25bd7269 cmd="node .scratch/t268/heading-scan.mjs"
GATE-RUN runId=46aa6ff3-fd0d-4c88-b19b-8e2370c2ddf6 cmd="node .scratch/t268/fact-scan.mjs"
GATE-RUN runId=4991d7ec-9acb-4e91-b428-080b0472b359 cmd="node .scratch/t268/text-shape.mjs"
GATE-RUN runId=fd44792c-3216-4896-88b2-e0aada6a2310 cmd="node .scratch/t268/table-read.mjs"
GATE-RUN runId=bb7dbbc2-d4f4-4bec-a2d2-82402ecafce0 cmd="node .scratch/t268/paramform-scan.mjs"
GATE-RUN runId=c4adb5d0-8f39-4cfa-883a-54487d3f0ba9 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs D:\\ilife\\.scratch\\t541\\out t541-手机墙.html 390"
GATE-RUN runId=28866bfd-2c13-48b5-90ab-0d6244df41e7 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs D:\\ilife\\.scratch\\t541\\out t541-桌面墙.html 1280"
GATE-RUN runId=cc5ffc35-caff-4aff-8763-558de673bf03 cmd="node docs/skills/skill-calorie/t541-索引生成器.mjs D:\\ilife\\.scratch\\t541\\out"
GATE-RUN runId=ef4642a7-4e6b-4acf-95f5-e67cd6a52588 cmd="node .scratch/t541/t541-假条目.mjs add"
GATE-RUN runId=8ddb1df6-de9e-41f8-9a6e-fedb7a2b2753 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs D:\\ilife\\.scratch\\t541\\out t541-手机墙.html 390"
GATE-RUN runId=e634a832-ae4e-4fdd-88c8-26ff81432045 cmd="node docs/skills/skill-calorie/t541-索引生成器.mjs D:\\ilife\\.scratch\\t541\\out"
GATE-RUN runId=13e54546-acd8-442a-ab03-b5b734bd8a29 cmd="node .scratch/t541/t541-假条目.mjs remove"
GATE-RUN runId=458e26d8-caa7-4a57-9ff2-ee084c749b57 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs D:\\ilife\\.scratch\\t541\\out"
GATE-RUN runId=3fe65673-764c-450e-91fa-bde256cf57ca cmd="node docs/skills/skill-calorie/t541-索引生成器.mjs D:\\ilife\\.scratch\\t541\\out"
GATE-RUN runId=t268r-add cmd="git add"
GATE-RUN runId=db3f0f63-49fc-4376-9bd6-35d046b4787d cmd="git diff --cached --name-only"
GATE-RUN runId=t268r-commit cmd="git commit"
```

**放宽留痕**（窗口内有 2 条 `exit=1`，都是**有意跑的红**）：

```text
GATE-RELAX flag=--allow-nonzero reason=窗口内两条红是自检反例：先往清单塞一条指向不存在文件的假条目，
再跑墙与索引生成器，**必须红并点名那份假件**（8ddb1df6＝墙、e634a832＝索引）；删掉假条目后回绿。
```

末两条 git 条目**先定号后跑**（`--run-id t268r-add`／`t268r-commit`），本件落盘时已在上面声明。
**未占锁的只读分析脚本**（只写 `.scratch/t268/` 自己的读数件，不动产物、不动源码）：
`版本身份核对.mjs`／`出当刻哈希.mjs`／`核对来源版.mjs`／`差异定位.mjs`／`基线复验.mjs`／`复验打回项.mjs`／
`复验视觉席新发现.mjs`／`score-重跑.mjs`／`gen-重跑brief.mjs`／`追加终审附节.mjs`／`看节开头.mjs`／`细看两处.mjs`。

---

## 六、未做项 ＋ 下一手缺什么

1. **逐格缺陷清单仍未填** —— 那是负责人的活；本席只把墙重出到位，**人没滚过就等于没验过**（`视觉验收墙.md` §4 红线）。
2. **本席一行源码未改** —— 打分与铺墙都是只读；§2.6 那批新项要修，得回各自的页生成件重出，不在这张重跑票里改。
3. **没跑整包编译与整包测试** —— 不碰 `packages/**`，无可编译物。
4. **`.scratch/t541/out/09` 与 `11` 两页无 07:53 建**（t544/out 缺这两件），保留 05:13 那一版；
   归一化后与其余构建同渲染（见 `版本身份.json`），只是复制日志里的构建时刻与邻页不同。
5. **下一手缺什么**：① 负责人滚两墙填 `t541-逐格缺陷清单.md`；② §2.6 的 N1–N8 按页族各自开票；
   ③ 记族那张 P1 票落盘后再跑一次增量打分（B 口径即可，不必重跑视觉席）。
