# #566 目标类结果页窗口日期出大标题 证据

> 体例照 `t467-删句-证据.md`。本票只动一件源码（`packages/skill-calorie/src/goal/resultDocs.ts`），
> `src/goal/` 之外一行未碰（含 `src/home/**`、`src/render/**`、`src/shared/**`）。
> 来源＝负责人 2026-09-15 肉眼验收（`.scratch/t153-视觉缺陷/probe.mjs` 实测 33 件产物里只有这 3 件的 `<h1>` 带窗口日期）。

## 一、开工状态

- 开工提交：`564e124`（`git rev-parse --short HEAD`）。
- 本票三条交付路径开工时 `git status --porcelain` **无输出＝干净（测试件与证据件尚不存在）**；全树另有别席在途脏件，
  本席一行未碰、不带进提交。
- `packages/skill-calorie/src/goal/resultDocs.ts` 开工态 **LF=269**（口径 `readFileSync(f,'utf8').split('\n').length - 1`），
  SHA256 `7734E81E84E585A6D307F758EED926C775A16613913BE81610F2A1F2F5444BC2`——变异自证还原后与它逐字比。
- 编译／测试／提交／变异重建一律经 `node tooling/run-locked.mjs --ticket 566 -- <命令>`，无裸跑；
  长输出只看尾 5 行，全文落 `.scratch/t566/`，审计条目落 `.scratch/locks/gate-runs.log`（§六逐条抄 runId）。

## 二、结构纪律必报五步（`docs/agents/structure.md`）

### 第一步 · 影响清单

| 路径 | 归属 | 一句话理由 |
|---|---|---|
| `packages/skill-calorie/src/goal/resultDocs.ts` | `goal` 能力 | 3 件产物的整页装配都在这一件里：`title:` 去掉窗口区间（2 处）、同一区间改由页头 `metaLeft:` 左行承载（2 处），跑不动的页面（`goal-expiring` 本来就没有窗口）一行不动。 |
| `packages/skill-calorie/test/566-result-title.test.mjs` | 测试件（不在结构管辖） | 本票验收用例（新建）。 |
| `docs/skills/skill-calorie/566-证据.md` | 文档（不在结构管辖） | 本证据件（新建）。 |

禁区未碰：`src/goal/` 之外全部；`src/goal/read.ts` 的调用面；`buildGoalView`／`buildGoalVsActualView`／
`buildGoalExpiringView`；三块读数卡／缺口与摄入／逐日表的数值与版式。

### 第二步 · 结构设计

- **无新增目录、无新增文件**（源码面）；对外仍是 3 个名字 `buildGoalDoc`／`buildGoalVsActualDoc`／
  `buildGoalExpiringDoc`，签名一字不动。
- 新增的只有一件**目录内私用小件** `windowLineOf(...)`（不出本目录，不进对外面，数不进铁律五的五个名字）。
- 页头仍是 `shared/docPage.ts::assembleDocPage` 的既定字段：`metaLeft`（页头第 1 行左）／`badge`（右）／`title`（H1）。
  `docPage.ts:33` 逐字写着这一行的用途是「参数一行小字（窗口／区间等）」——窗口区间本来就该住这里；
  本票不新增类名、不新增页内样式、不碰 `BLINE_CSS`（那是公共层，本票不许动）。
- 共用件无变化（`docPage.ts`／`copyArea.ts` 一行未动）。

### 第三步 · 写代码

- 跨能力引用未增（本件仍只 import `base-paint`、`base-paint/blocks`、`../paths.js`、`../render/receipt.js`、
  `../shared/docPage.js`、`../shared/copyArea.js` 与同目录类型）。
- 常量的唯一定义地不变；新增小件只服务本目录三处调用。
- 取数口径一行未动：`buildGoalDoc`／`buildGoalVsActualDoc` 仍只吃现算的视图对象与 `metrics`，`metrics` 一进信封
  `data.metrics`、一进复制载荷，两处仍同一份对象。

### 第四步 · 超线报警

- `resultDocs.ts` 开工 269 LF → 收工 **286 LF**（口径同上），**未越本包告警线 350**，不触发第四步，台账无需同步。
- 新建的两件（测试件／证据件）按本包 AGENTS.md 不在告警线扫描面内。

### 第五步 · 交付对账

见 §八（事前清单与事后对账逐行对）。

## 三、改前读数（真 CLI ＋ 临时种子库，改代码之前抄下）

配方照 `test/goal-result-254.test.mjs`：`mkdtemp` ＋ `openDb` ＋ `docs/research/t81-seed.mjs::seedFull()`
＋ `SKILLS_DB_PATH` 指临时目录 ＋ `CALORIE_TODAY=SEED_TODAY`（种子数据日 `2026-09-07`）。
探针 `.scratch/t566/before.mjs`，读数原件 `.scratch/t566/before.json`，逐件产物落 `.scratch/t566/before/*.html`。
持锁实跑：runId `a187d281-5c45-4dfd-b126-25864be72562`，`exit=0`，4 件全 `exit 0`。

| 件（命令） | `<h1>` 原文 | 页头 `meta-bar` 左行原文 | 窗口区间 | 窗口在可见文本出现 | `dateInH1` |
|---|---|---|---|---|---|
| `goal-7d`（`calorie.view.goal --params '{"window":"7d"}'`） | `🎯 目标分析 2026-09-01 至 2026-09-07` | `看目标完成度 · 目标管理` | `2026-09-01 至 2026-09-07` | 3 次 | **true** |
| `goal-30d`（`calorie.view.goal --params '{"window":"30d"}'`） | `🎯 目标分析 2026-08-09 至 2026-09-07` | `看目标完成度 · 目标管理` | `2026-08-09 至 2026-09-07` | 3 次 | **true** |
| `vs-30d`（`calorie.view.goal-vs-actual --params '{"window":"30d"}'`） | `🎯 目标对比实际 2026-08-09 至 2026-09-07` | `看目标对比实际 · 目标管理` | `2026-08-09 至 2026-09-07` | 3 次 | **true** |
| `expiring`（`calorie.view.goal-expiring`，本票不动的对照件） | `⏳ 目标到期提醒` | `看即将到期的目标 · 目标管理` | 无（这一页没有窗口） | 0 次 | false |

改前 3 件的窗口区间在可见文本各出现 3 次：`<h1>`（本次要挪走的那一处）＋ 读数卡副说明（`v.start 至 v.end`）
＋ 表题（「这一段时间的缺口与摄入（…）」／「摄入走向（本窗 …）」）。**改后仍是 3 次**（H1 那一次改住 `meta-bar`），
卡与表一行未动（§四）。

### 改前 `data.metrics` 原文（逐字段冻结，判据③的反向锁拿它比）

```json
goal-7d  {"calorie_goal":1800,"protein_goal":150,"carbs_goal":200,"fat_goal":50,"water_goal":2000,"completionPct":0,"weeklyDeficit":10841,"predictedLossKg":1.41,"avgDeficit":1549,"avgIntake":477,"trendAvg":477,"completedCount":0,"incompleteCount":9}
goal-30d {"calorie_goal":1800,"protein_goal":150,"carbs_goal":200,"fat_goal":50,"water_goal":2000,"completionPct":0,"weeklyDeficit":20276,"predictedLossKg":2.63,"avgDeficit":676,"avgIntake":158,"trendAvg":158,"completedCount":0,"incompleteCount":9}
vs-30d   {"completedCount":0,"incompleteCount":9,"completionPct":0,"trendAvg":158,"calorieGoal":1800}
```

（`goal-7d`／`goal-30d`／`vs-30d` 三份与 `test/goal-result-254.test.mjs` 冻结的改前读数逐字段相同——
两份独立读数互证，说明本探针抄的是改前的真值。）

## 四、改动清单（`resultDocs.ts`，LF 269 → 286，SHA256 `A54B9C0D4B3764B9EC6B4E5D1CA7CDC968BD576B070908D56EB83A0C8E9FD106`）

| # | 位置 | 改前 | 改后 |
|---|---|---|---|
| 1 | 件头注释（#561 那段之后） | — | 补 #566 那一段：为什么区间不进 H1、区间落哪一行、本票只搬这一处 |
| 2 | 目录内私用小件（新） | — | `windowLineOf(metaLeft, start, end)` ＝ `metaLeft + ' · 窗口 ' + start + ' 至 ' + end`（不出本目录） |
| 3 | `buildGoalDoc` 的 `title` | `'🎯 目标分析 ' + v.start + ' 至 ' + v.end` | `'🎯 目标分析'` |
| 4 | `buildGoalDoc` 的 `metaLeft` | `'看目标完成度 · 目标管理'` | `windowLineOf('看目标完成度 · 目标管理', v.start, v.end)` |
| 5 | `buildGoalVsActualDoc` 的 `title` | `'🎯 目标对比实际 ' + v.start + ' 至 ' + v.end` | `'🎯 目标对比实际'` |
| 6 | `buildGoalVsActualDoc` 的 `metaLeft` | `'看目标对比实际 · 目标管理'` | `windowLineOf('看目标对比实际 · 目标管理', v.start, v.end)` |

`buildGoalExpiringDoc` 一行未动（这一页本来就没有窗口）；三块读数卡／缺口与摄入／逐日表／口径行／复制区一行未动。

### 改后读数（同一条探针，`.scratch/t566/after.json`，持锁 runId `d3b1d1fa-7386-4082-adb4-3861473f9e6b`，exit 0）

| 件 | `<h1>`（改后） | 页头 `meta-bar` 左行（改后） | 可见文本里窗口 | `data.metrics` | 字符数 |
|---|---|---|---|---|---|
| `goal-7d` | `🎯 目标分析` | `看目标完成度 · 目标管理 · 窗口 2026-09-01 至 2026-09-07` | `2026-09-01 至 2026-09-07`，3 次 | 逐字段同改前 | 71782 → 71787 |
| `goal-30d` | `🎯 目标分析` | `看目标完成度 · 目标管理 · 窗口 2026-08-09 至 2026-09-07` | `2026-08-09 至 2026-09-07`，3 次 | 逐字段同改前 | 71776 → 71781 |
| `vs-30d` | `🎯 目标对比实际` | `看目标对比实际 · 目标管理 · 窗口 2026-08-09 至 2026-09-07` | `2026-08-09 至 2026-09-07`，3 次 | 逐字段同改前 | 80368 → 80373 |
| `expiring`（对照，本票未动） | `⏳ 目标到期提醒`（照旧） | `看即将到期的目标 · 目标管理`（逐字照旧） | 无 | 逐字段同改前 | 69017 → 69017 |

- `dateInH1`：改前 3 件全 `true` → 改后 3 件全 `false`；`dateInTitle`（head 的 `<title>`）改前改后全 `false`（本来就不带日期）。
- 字符数净 +5：H1 少了 ` 2026-09-01 至 2026-09-07`（24 字符），页头多了 ` · 窗口 2026-09-01 至 2026-09-07`（29 字符）；
  对照件 0 变化＝本票没顺手改别的页。
- 窗口区间在可见文本里仍是 3 次（页头那 1 次 ＋ 读数卡副说明 ＋ 表题），**挪了地方、没丢**。

## 五、验收用例（`test/566-result-title.test.mjs`，新建，LF 266）

| 用例 | 判据（票面逐条） | 反向参照（判据不恒真） |
|---|---|---|
| ① | 3 件产物 `<h1>` 不含日期且含页面名；head 的 `<title>` 同判 | 对照件（无窗口那件）H1 一直是页面名、也过 |
| ② | 窗口区间住页头 `meta-bar` 左行 ＋ 同件可见文本里仍出现 | 对照件页头左行必须逐字不变（本票没顺手加字） |
| ③ | `data.metrics` 与改前逐字段相同（改前读数冻结在用例里，源自 `.scratch/t566/before.json`） | 拿 7 天窗那份冻结表核对 30 天窗读数 ⇒ 必须抛（证明第 ③ 条不是恒真） |
| ④ | 完整文档四断言（doctype／charset／style／ilife-page）＋ `data.output` 绝对路径且真在盘上 | 改前那版片段形状打同一条判据 ⇒ 必须抛（`/缺 doctype/`） |
| ⑤ | 字串级变异：日期加回 H1 ⇒ ①必红；窗口从页头抠掉 ⇒ ②的「住页头」那一半必红；窗口整份挖掉 ⇒ ②的「没丢」那一半必红；还原 ⇒ 必绿 | 变异覆盖表断言三件产物的 ①② 各红一次，漏一件即红 |

判据件（`assertTitleFree`／`assertWindowInHeader`／`assertWindowInVisibleText`）抽成独立函数，变异自证直接拿它们跑，
不另抄一份——变异红了就等于判据红了，不是两套写法。

## 六、持锁门禁（`ticket=566`，全文见 `.scratch/locks/gate-runs.log`；长输出只贴尾 5 行）

| runId | 命令 | exit | 摘要行 |
|---|---|---|---|
| `a187d281-5c45-4dfd-b126-25864be72562` | `node .scratch/t566/before.mjs before` | 0 | 4 件实跑全 `exit 0`；改前读数落 `.scratch/t566/before.json` |
| `44c23a27-d640-4997-9801-d8b985ecc5c8` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | **1** | **外来红**：`packages/skill-calorie/src/exercise/sportUi.ts(30,22): error TS2305: Module '"base-paint"' has no exported member 'renderChips'`（见下「外来红记录」） |
| `76ecaa60-7118-40a0-a51d-63c90f0e6b95` | 同上（重跑一次） | **1** | 同一条外来红（重跑时它仍在途） |
| `d0ffd872-fc31-45d4-9c5f-f701c49eb644` | 同上（第三次，别人的件已改好） | **0** | `tsconfig.tsbuildinfo` mtime 随之更新（编译真跑过）；`exit=0` |
| `d3b1d1fa-7386-4082-adb4-3861473f9e6b` | `node .scratch/t566/before.mjs after` | 0 | 改后读数（§四表） |
| `d4b9a9b0-7040-4cd3-9f5a-5ad58efc086d` | `node --test packages/skill-calorie/test/566-result-title.test.mjs` | **0** | `tests 5 / pass 5 / fail 0`（＋ `T566-MUT 字串级：日期加回 H1 红=3、窗口抠出页头 红=3、窗口全挖掉 红=3；还原绿=3`） |
| `df566ee9-ca71-4f11-89c4-7808953c5a69` | `node .scratch/t566/mutate-cycle.mjs`（真身变异，§七） | 0 | 里面红跑 `exit 1`、绿跑 `exit 0`；还原 SHA 一致 |
| `35f74e9c-517a-4999-875d-8cd62516ffba` | `node --test …/goal-result-254.test.mjs …/t561-goal-no-source.test.mjs …/goal-weight-b1-390.test.mjs` | **0** | `tests 10 / pass 10 / fail 0`（邻接回归，§七末） |

### 外来红记录（并发纪律 §9：红行指向不属于本席的件）

- 红行指的文件是 `packages/skill-calorie/src/exercise/sportUi.ts`——`git status` 里是 `M`（**别席在途未提交件**），
  报的是它 `import { renderChips } from 'base-paint'`（该名字的实际定义地在 `packages/base-render/src/blocks.ts:495`，
  走的子路径应是 `base-paint/blocks`）。本席**一行未碰**该件。
- 记后重跑一次（`76ecaa60`）仍红；隔了一次别人的落盘后第三次（`d0ffd872`）转绿（别席已把导入位改对）。
  本席未改任何别人的件，也未把它的在途改动带进提交。

## 七、变异自证两行读数（真身：改源码 → 重建 → 跑 → 逐字还原 → 重建 → 跑）

- **变异点**：`resultDocs.ts` 的 `title: '🎯 目标分析',` 改回改前形状 `title: '🎯 目标分析 ' + v.start + ' 至 ' + v.end,`
  （＝把窗口日期又写进大标题），持锁窗口内重建再跑用例。
- **红读数**：`node --test packages/skill-calorie/test/566-result-title.test.mjs` → **`tests 5 / pass 3 / fail 2`**，`exit 1`；
  两条红＝用例 ① 与用例 ⑤；用例 ① 的红行原文（`.scratch/t566/mut-red.log`，逐字抄、只把命令键两侧的撇号挪成引号）：

  > AssertionError [ERR_ASSERTION]: 「goal-7d」『calorie.view.goal』的 H1 里还有窗口日期（日期不该住标题）："🎯 目标分析 2026-09-01 至 2026-09-07"

  （用例 ⑤ 红在同一处：它先建「加回日期」的变异产物，再拿原产物当还原对照——源码里 H1 带头日期时，那个对照物自身就红。）
- **绿读数**（还原后重建复跑）：**`tests 5 / pass 5 / fail 0`**，`exit 0`。
- 还原校验：`resultDocs.ts` 还原前后 SHA256 同为 `A54B9C0D4B3764B9EC6B4E5D1CA7CDC968BD576B070908D56EB83A0C8E9FD106`
  （脚本落盘后回读自比，`MUT-SHA … 还原一致=true`）；两场编译都 `exit 0`，`tsconfig.tsbuildinfo` mtime 随之更新。
- **字串级变异**（用例 ⑤ 内，常跑）：日期加回 H1 红=3、窗口抠出页头 红=3、窗口全挖掉 红=3、还原绿=3。
- **邻接回归**（runId `35f74e9c-517a-4999-875d-8cd62516ffba`）：`goal-result-254`（同三件产物的整页化判据 ＋ 换页头那行之后的机器话探针）、
  `t561-goal-no-source`（场景 06 的 25 条唤醒词实跑）与 `goal-weight-b1-390` 一起跑 → `tests 10 / pass 10 / fail 0`。

## 八、交付对账（第五步）

| 第一步清单 | 事后对账 |
|---|---|
| `packages/skill-calorie/src/goal/resultDocs.ts` | 已改（§四 六处：注释＋新小件＋2 处 `title`＋2 处 `metaLeft`）；`git diff --stat`＝`1 file changed, 21 insertions(+), 4 deletions(-)` |
| `packages/skill-calorie/test/566-result-title.test.mjs` | 已新建（5 条用例） |
| `docs/skills/skill-calorie/566-证据.md` | 已新建（本件） |

偏差为零：`src/goal/` 之外**一行未碰**（`git status --porcelain` 里本席只有上面三件：源码 `M`、两件 `??`）；
`read.ts` 调用面、取数三函数、唤醒词／命令名／参数名／退出码、既有用例一字未改。`.scratch/t566/` 下是临时探针与
变异脚本（`.scratch/` 已在 `.gitignore` 里，不进提交）。

## 九、GATE-RUN 逐条（`.scratch/locks/gate-runs.log` 的 `RUN` 行原文，按发生顺序抄；长输出只贴尾 5 行）

| GATE-RUN | 摘要行 | 退出码 |
|---|---|---|
| `runId=a187d281-5c45-4dfd-b126-25864be72562 cmd="node .scratch/t566/before.mjs before"` | 改前读数：4 件实跑，`.scratch/t566/before.json` | 0 |
| `runId=44c23a27-d640-4997-9801-d8b985ecc5c8 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"` | **外来红**：`sportUi.ts(30,22): error TS2305 … 'renderChips'`（别席在途件） | 1 |
| `runId=76ecaa60-7118-40a0-a51d-63c90f0e6b95 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"` | 同上（记后重跑一次，仍红） | 1 |
| `runId=d0ffd872-fc31-45d4-9c5f-f701c49eb644 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"` | **编译真跑过**：`exit=0` ＋ `tsconfig.tsbuildinfo` mtime 更新到 `2026-09-16 01:09:20` | 0 |
| `runId=d3b1d1fa-7386-4082-adb4-3861473f9e6b cmd="node .scratch/t566/before.mjs after"` | 改后读数：`.scratch/t566/after.json` | 0 |
| `runId=d4b9a9b0-7040-4cd3-9f5a-5ad58efc086d cmd="node --test packages/skill-calorie/test/566-result-title.test.mjs"` | `tests 5 / pass 5 / fail 0` | 0 |
| `runId=df566ee9-ca71-4f11-89c4-7808953c5a69 cmd="node .scratch/t566/mutate-cycle.mjs"` | 里面：红 `tests 5 / pass 3 / fail 2` → 绿 `tests 5 / pass 5 / fail 0`；还原 SHA 一致 | 0 |
| `runId=35f74e9c-517a-4999-875d-8cd62516ffba cmd="node --test …/goal-result-254.test.mjs …/t561-goal-no-source.test.mjs …/goal-weight-b1-390.test.mjs"` | `tests 10 / pass 10 / fail 0`（邻接回归） | 0 |
| `runId=2816ac41-9760-41df-b57c-9ff328d1acaa cmd="git add packages/skill-calorie/src/goal/resultDocs.ts packages/skill-calorie/test/566-result-title.test.mjs docs/skills/skill-calorie/566-证据.md"` | 暂存区复核＝只有这三件 | 0 |
| `runId=4b6830e0-9894-474c-926d-dd1149de3c90 cmd="git commit -F .scratch/t566/msg.txt -- <上面三件>"` | 提交 `0cbb0b0`（`3 files changed, 462 insertions(+), 4 deletions(-)`） | 0 |

**提交**：`0cbb0b066d8487a72e7aff81f5ce8c8134803976`（`修复(566): 目标类结果页的窗口区间从大标题挪进页头 meta-bar 左行（H1 只留页面名）…`），
只含上面三件；本席未 push、未 amend、未切分支、未碰别人的未提交件。


