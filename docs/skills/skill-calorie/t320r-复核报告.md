# #320r · 独立对抗式复核报告（收口实施窗 `1ac41ca`）

- 复核对象：`1ac41caf3ed96b72d59606af0431383d7a35075c`（提交信息含「#320」；`feat(320b): 残余清账 —— calorie.view.goal-weight 搬进能力目录、分派层老 switch 删净、棘轮切终态`）
- 复核席：独立对抗式复核席（非作者席、非编排者）；**作者席自报一律不采信，逐条自跑**
- 起点现场（自复算）：`git rev-parse HEAD`＝`git rev-parse origin/master`＝`1ac41caf3ed96b72d59606af0431383d7a35075c`；父提交＝`1ac41ca^`
- 判官指纹（未动，复核前后同值）：`docs/skills/skill-calorie/t293-验收-命令自治.mjs` ＝ `78AE857426A44D66E81A2489E51D0D2790BD16A0812DB78818B1D2E918BEBE39`
- **结论：PASS（有条件）· 90/100 · 无 S1 · 1 条 S2 ＋ 5 条 S3**
- 一句话：搬迁面／声明面／parity／五条绿线／对账／行数／反假绿**七维全部逐条复现为真**；唯一实质缺陷是终态断言的**鉴别力只覆盖单引号拼写**（S2，一行可修），它不违反票面字面契约，故不构成 S1。

---

## §0 现场复算（机器读数）

| 项 | 命令 | 读数 |
|---|---|---|
| 起点 | `git rev-parse HEAD; git rev-parse origin/master` | 两者同为 `1ac41ca` |
| 改动面 | `git show --stat 1ac41ca` | **12 件**：docs 2 ＋ `src/cli` 5 ＋ `src/goal` 3 ＋ `test` 1；**无生成物、无新文件** |
| 判官 | `(Get-FileHash 'docs/skills/skill-calorie/t293-验收-命令自治.mjs' -Algorithm SHA256).Hash` | `78AE8574…BEBE39`（与派单逐字相同） |
| 工作树 | `git status --porcelain -uall` | `0` 条（复核收尾后仍 `0`；含复核期三次变异已逐字复位） |
| 12 件漂移 | 逐件 `git hash-object <p> -eq git rev-parse HEAD:<p>` | `driftFiles=0` |

---

## §1 逐维判定

### 维 1 · 搬迁面 —— **PASS（逐条复现）**

| 判点 | 命令 | 读数 |
|---|---|---|
| 路由记录**并集**不变（防连带删） | 对 `1ac41ca^`／`1ac41ca` 各取 `legacy/routes/scene-*.ts`＋`goal/routes.ts` 全部 `wakeWord:` 行，`Compare-Object` | `old=51 new=51`、**`UNION-IDENTICAL`** |
| 逐文件增减 | 同上逐件计数 | `routes/scene-03.ts 19→18`、`scene-06.ts 1→0`、`scene-10.ts 1→0`、`goal/routes.ts 22→25`（**恰 3 条位移，无第 4 条**） |
| 三条记录逐字 | `git show 1ac41ca -- packages/skill-calorie/src/cli/legacy/routes packages/skill-calorie/src/goal` | 三行 `list/order/wakeWord/scene/kind/key/cli` 与源行逐字相同（`order` 120／219／373 原值） |
| 旧位置不留该件 | `git grep -n "goal-weight" -- packages/skill-calorie/src/cli/legacy` | **0 命中** |
| 只搬空不删文件 | `git ls-tree -r --name-only 1ac41ca^ -- …/cli/legacy` vs `1ac41ca` | `legacy/scene-03.ts` **仍在**（`export const LEGACY_SCENE_03 = [];`），10 件分片件数不变 |
| 别人的声明没被连带 | 10 件 `legacy/scene-NN.ts` 的 `key: 'calorie.` 行并集 `Compare-Object` | `old=1 new=0`，差异**只有** `calorie.view.goal-weight` 那一行 |
| 分派层死 switch 清空 | `git show 1ac41ca -- packages/skill-calorie/src/cli/cmd_read.ts` | `-` 11 行（`case 'calorie.view.goal-weight'` 整口 ＋ 三处 import）／`+` 直接 `fail(3,'未知 calorie key：')` |

### 维 2 · 声明面 —— **PASS**

| 判点 | 命令 | 读数 |
|---|---|---|
| 新键进能力目录声明 | `git show 1ac41ca:packages/skill-calorie/src/goal/commands.ts` | `:44` 新增一条 `kind:'read'`，`run: viewGoalWeight`；`GOAL_COMMANDS` 由 13→**14** 条（写 5 ＋ 读 9） |
| 六字段逐字照抄 | 旧声明行 vs 新声明行去空白后比对（剔除新增的 `run:` 字段） | `sixFieldsIdentical_afterRemovingRun=True`（`kind/key/shape/title/wakeWord/example` 逐字；`run` 是能力声明的必备第 7 字段） |
| `wakeWord` 未擅改 | 新声明 `wakeWord: '定体重目标'`（与旧声明同值）；生成物 `build-help.mjs` blob 同父 | 见维 3 parity 三行 |
| `gen:check` 闭环 101 | 自跑 `node packages/skill-calorie/scripts/gen-cli.mjs --check`（持锁 runId `dbc112fc-ae28-4489-8b6c-1f822262a572`，exit=0） | `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 10 个（analysis／body／diet／exercise／goal／home／photo／profile／weight／workout）声明 101 条，未搬迁清单 0 条` |
| 101 闭环**自算**（不信 tool 出声） | 直接从 git blob 数 `key: 'calorie.`：`1ac41ca^` → `legacy=1 cap=100 total=101`；`1ac41ca` → `legacy=0 cap=101 total=101` | 两侧都恰 101，缺口由 `legacy` 迁入 `goal` 补平 |
| 五件生成物逐字节不变 | 逐件 `git rev-parse 1ac41ca^:<p>` vs `1ac41ca:<p>` ＋ `git hash-object <p>` | `keys.ts`／`registry.ts`／`packages/base-combos/combos.yaml`／`scripts/build-help.mjs`／`triggers/routes.generated.ts` **五件 blobSAME=True、worktree==HEAD=True**；`routes.generated.ts` sha256=`e9ea993518ec33e3a69575dac1d8abf4a5e4c12e277b4c2d215ae3e5d339d7e9`（与作者自报同值） |

### 维 3 · 落点选择（`src/goal/` vs `src/weight/`） —— **PASS（不是迁就判官；但理由次序写反，见 S3-3）**

**自己复现的反证（判官侧，逐字取常量）**：
- `t293-验收-命令自治.mjs:1279`：`const ok = branches.length === 0 && regFirst && wd.reads.length === 5 && wd.writes.length === 4;` —— **硬等号**；`wd` 读的是 `src/weight/commands.ts`。
- 我自算的当刻体重面：`wReads=5 wWrites=4`（`Select-String -Path packages/skill-calorie/src/weight/commands.ts -Pattern "kind: 'read'|kind: 'write'"`）⇒ 把这条读命令落进 `weight/` 即 `6/4`，`=== 5` **必假 ⇒ P4=FAIL**（判官是冻结件，sha256 见 §0）。
- `:274` 把「体重键数」当 P1 锚（`legacy.filter(c => c.from === CAP_SRC_DIR + '/weight/commands.ts').length`），`:594` 把它报进钉死计数扫描 ⇒ 落 weight 会把锚值 9 改成 10，动了 P1 的候选行集。作者自报的 `P1=PENDING P2=FAIL P4=FAIL`（runId `4101606b-…`）与这两处常量**同向**。

**但 `src/goal/` 不是「为迎合判官而挑」——仓内自有更硬的依据（我自算，不引作者措辞）**：
1. **键族同主人**：`calorie.view.goal*` 的九条读命令**全部**同住 `src/goal/commands.ts`（`goal`／`goal-config`／`goal-expiring`／`goal-predict`／`goal-recommend`／`goal-status`／`goal-vs-actual`／`goal-wizard` 八条**在本提交之前就在那里**），本提交只是补齐第九条。
2. **写孪生同目录**：`calorie.goal.weight`（`定体重目标`，同一件事的写侧）同在 `src/goal/commands.ts:35`。
3. **实现模块同族**：该命令的算式 `buildGoalWeight` 住 `src/render/goalPlate.ts`（**goal** plate），页面装配 `renderGoalWeightHtml`；搬迁只承接转调层（`src/goal/read.ts:142-147`）。
4. **对手依据是过渡件**：`src/weight/` 的九条键全是 `view.weight*`／`view.volatility`／`weight.{log,update,remove,batch}`，**一条 `goal*` 也没有**；而把它判给 weight 的唯一依据是 `legacy/scene-03.ts` 这条**过渡期场景分片**（`docs/skills/skill-calorie/t313a-分区-证据.md:89` 的「场景 03（体重，1）」），且该键的三条路由记录本就散在 **03／06／10 三个场景**——用它反推能力归属，本身就自相矛盾。

**判定**：`src/goal/` 是**合法且更自洽**的落点；判官的 5/4 与体重 9 键常量与产品规则**同向**，不是「改判官才能落 weight」以外唯一解，而是「落 weight 需同时改判官＋破键族」。故**不判不达标**，也不需要改判官。缺陷只在**陈述**：作者 §1.1 与提交信息把「判官口径」摆在第一位，把上述 1–3 条产品依据塞进 `commands.ts` 文件头，读起来像判官反推（S3-3）。
- **未做**：weight 版**动态**复跑（见 §5 未做项；红线 ⛔ 禁改 `src/`，且该实验需重生成生成物，会污染本窗已验证字节）。本维结论由「判官常量逐字 ＋ 体重面 5/4 自算」静态闭合。

### 维 4 · 棘轮终态 —— **PASS（字面契约已满足）＋ 1 条 S2（鉴别力只覆盖单引号）**

| 判点 | 命令 | 读数 |
|---|---|---|
| 新终态断言存在且覆盖全目录 | `packages/skill-calorie/test/cmd-registry-294.test.mjs:77-88` | `cliCaseLabels()` 走 `tsFilesUnder(CLI_DIR)`（`:53-59` 递归 `readdirSync`）；**自算覆盖**：`node .scratch/t320r/scan-coverage.mjs` → `scannedTs=27 legacyRoutesTs=11 routesInSet=true liveCalorieLabels=[]`（`legacy/routes/scene-03.ts` **在**被扫集合里，`routes/` 子目录确实递归到） |
| 扫描器鉴别力自证 | 同上探针 `syntheticProbe=["calorie.x"]` | 合成样本被同一扫描器看见（非自述） |
| **变异自证（要求项）** | 塞回 `switch (key) { case 'calorie.x': { fail(3,'x'); } }` 到 `cmd_read.ts:150` 前 → 跑 `packages/skill-calorie/test/cmd-registry-294.test.mjs`（持锁 runId `b24f6df2-a540-4590-af16-b7c05108d9c0`） | 变异前 sha256=`7EF33346AE76FA60`（**与作者自报逐字同值**）、变异后=`51B054F6C138E8DD`；**exit=1**，报错签名「`AssertionError: 分派层仍有按键分派的 case 分支（#320 起老路应无活口）：calorie.x`」；`tests 7 / pass 6 / fail 1`（只这一条转红） |
| 复位自证 | `git checkout -- …/cmd_read.ts` → `git hash-object` | 回到 `b69ea254aed37dc491203d11b1a5a29f8e7cd29a` ＝ HEAD blob；sha256 回到 `7EF33346AE76FA60`；`status=0` |
| 两张 `FROZEN_*` 清单与两个上界真删 | 父 vs 头 `Select-String 'FROZEN_\w+' -AllMatches` | 父：`FROZEN_READ_CASES`／`FROZEN_READ_CASES_MAX`／`FROZEN_READ_LINES`／`FROZEN_WRITE_CASES`／`FROZEN_WRITE_CASES_MAX`／`FROZEN_WRITE_LINES`（6）；头：**只剩 `FROZEN_READ_LINES`／`FROZEN_WRITE_LINES`**（2）⇒ 两张清单 ＋ 两个上界确删，行数棘轮保留（与派单口径一致） |
| 「老路活证」改写 | `:136-157` | 改成「未搬迁清单是空集」＋「注册表键集 == 全量声明（101 键闭环）」＋两个前任老键照旧真跑（体成分读／喝水写） |
| `write.ts` 三处 `case`→键集查表**语义等价** | 读 `git show 1ac41ca -- packages/skill-calorie/src/cli/write.ts` ＋ `:47-67` | 旧：`case 'calorie.profile.set'/'activity' → buildProfileSettingReceiptDoc`、`case 'calorie.profile.update' → buildProfileUpdateReceiptDoc`、`default → null`。新：`!PROFILE_RECEIPT_KEYS.has(key) → null`，否则 `key === 'calorie.profile.update' ? buildProfileUpdateReceiptDoc : buildProfileSettingReceiptDoc`。**三键映射一一对应、其余键全落 `null`，逐字等价**（`has` 命中的集合恰是旧三条 case 标签） |
| `src/cli` 全目录无按键分派 | `git grep -n -- "case .calorie\." HEAD -- packages/skill-calorie/src/cli` | **0 命中**（`git grep` exit=1）；残留 6 处 `case '` 全是**形状**分支（`list/detail/stat/receipt/analysis/fallback`，`cmd_read.ts:104-129`），不按键 |

### 维 5 · 五条绿线 ＋ 冻结件 —— **PASS（全部自跑，全部持锁）**

包装器口径：`node tooling/run-locked.mjs --ticket t320r --lock-dir .scratch/locks-land --stale-minutes 30 -- <命令>`（本窗 6 条串行，故 `tsc -b` 先落、其后各门与探针跑的都是本窗刚编出的 `dist` 字节）。

| 线 | 命令 | exit | 读数 | 我的 runId |
|---|---|---|---|---|
| 1 | `node node_modules/typescript/bin/tsc -b` | 0 | 无输出＝零类型错 | `4f2d0be1-a8d2-458a-bc21-787fce292e38` |
| 2 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | 0 | `未搬迁清单 0 条`；能力 10 个声明 **101** 条；五件逐件 `ok` | `dbc112fc-ae28-4489-8b6c-1f822262a572` |
| 3 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | 0 | **`P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`**；`UNACCOUNTED=0 处`；`体重 9 键仍留在未搬迁清单…0 处`；`权威声明齐=9/9`；④ 主体判不出=3（只报账） | `ab960c77-222b-4871-bab6-6ca734d04aaa` |
| 4 | `node --test test/*.test.mjs packages/*/test/*.test.mjs` | 0 | `tests 1492 / pass 1492 / fail 0` | `88a61b78-930a-4ae5-aafc-f8f6d61c23b0` |
| 5 | `node tooling/skill-html-snapshot.mjs --check` | 0 | `OK: 5 技能 HTML 快照 == 实际（186 件产物，base-* 指纹 ef7cc02969e37e4bfc0eb35751b8dc1c，base-* 文件 27 件）`；`changed=0 added=0 removed=0` | `2d9f60a4-2c93-48ef-96d7-ee1fe517fe89` |
| 附 | `node --test test/calorie-routing-81.test.mjs` | 0 | `tests 8 / pass 8 / fail 0`（冻结件） | `339c02da-75c1-4527-a5aa-c65bf412b070` |

### 维 6 · 对账（`tooling/check-gate-audit.mjs`） —— **PASS（且我另做了一轮更强的「导出保真」核对）**

| 轮 | 命令（均经持锁） | 读数 |
|---|---|---|
| A（派单口径：受跟踪导出件） | `--evidence docs/skills/skill-calorie/t320-收口-证据2.md --log docs/skills/skill-calorie/t320b-gate-runs.log --ticket t320b --since 2026-09-13T21:58:12Z` | **`RESULT: matched=6/6 auditEntries=6 scoped=6 undeclared=0` ＋ `gate-audit: PASS`，exit=0**（runId `87798201-5db3-4e8f-88a7-32a569394c21`） |
| B（**我加的一轮**：回打 gitignored 真源，同窗口加 `--until 2026-09-13T22:00:00Z`） | `--log .scratch/locks-land/gate-runs.log …` | **`matched=6/6 auditEntries=330 scoped=6 undeclared=0` ＋ `PASS`，exit=0**（runId `08936c8e-b8c0-454f-ab74-c22f2abfbb5e`）⇒ 导出件**逐条保真**，不是挑窗口挑出来的；真源日志另有 48 条 `ticket=t320b` 条目全在窗口外，未被「声明认领」 |
| 放宽痕迹 | `Select-String -Path docs/skills/skill-calorie/t320-收口-证据2.md -Pattern 'GATE-RELAX\|--allow-'` | 4 命中，**逐条读全是「本件**没有** GATE-RELAX 行／未用任何 `--allow-*`」的自述**（`:186`、`:239`），**无实际放宽**；两轮 audit 我都**未加**任何 `--allow-*` 且都 PASS |
| 声明与真源 runId 一对一 | 导出件 `:172-177` 六条 `GATE-RUN runId=…` vs 真源日志 | 六条 runId（`20c2cc2d`／`16493d28`／`a19d17a4`／`92aa9da5`／`426ccb56`／`6aa5829f`）在真源逐条存在、`exit=0`、`cmd` 同串 |

### 维 7 · 行数与棘轮 —— **PASS**

| 判点 | 命令 | 读数 |
|---|---|---|
| `cmd_read.ts` 落线 | `(Get-Content …/cmd_read.ts).Count`；判据件口径 `src.split('\n').length - 1` | **340 行**（父 351；**< 350 告警线**，≤349 达成） |
| `write.ts` | 同上 | **99 行**（父 95，＋4 行注释／常量） |
| 入口名与导出未挪 | `git grep -n "export function dispatch" HEAD -- …/src/cli`；`:272 function main` | `cmd_read.ts:146 export function dispatch(key, params, db): DispatchOut`（同名同签名同文件）；`write.ts:77 export function dispatchWrite`；`main` 仍在 |
| 调用面未断（检索证实） | `git grep -l -- "dist/cli/cmd_read" HEAD -- test packages \| Measure \| Count`；`git grep -l -- "import { dispatch" HEAD -- test packages \| Measure \| Count` | **35** 件引用 `dist/cli/cmd_read`、**13** 件 `import { dispatch }`（作者自报「约 28／约 10」是同向的粗估）；再加上线 4 的 1492/1492 ⇒ 出口面未断 |

### 维 8 · 反假绿（含 gitignored 派生面） —— **PASS**

| 判点 | 命令 | 读数 |
|---|---|---|
| 我验的字节＝HEAD 编出的字节 | 本窗串行：`tsc -b`（线 1）**先**跑，线 2–6 与探针**后**跑，全程同一锁窗、一次未改源码 | 顺序即证据（见维 5 表头） |
| 编译产物内容＝搬迁后源码 | `Select-String -Path packages/skill-calorie/dist/cli/cmd_read.js -Pattern 'view.goal-weight'` | **0 命中**（若 `dist` 陈旧，老 switch 的 `case "calorie.view.goal-weight"` 必在） |
| 新处理器进了被 import 的产物 | `Select-String -Path packages/skill-calorie/dist/goal/read.js -Pattern 'viewGoalWeight'` | **1 命中**（测试 `:28-30` 正是 import `dist/cli/cmd_read.js`／`registry.js`） |
| 派生面载体在 | `Test-Path packages/skill-calorie/dist/.gen-inputs.json` | **True**（`dist/` 全 face 为 `!!` gitignored，`git status` 的「干净」不覆盖它，故单独核） |
| 生成物与源码同源 | `git hash-object <五件>` vs `HEAD:<p>` | 五件全 `True`；`routes.generated.ts` sha256 与作者自报同值（见维 2） |
| 变异未污染派生面 | 三次变异**只改 `src`**、只跑 `node --test`（不跑 build），逐次 `git checkout --` 复位 | 收尾 `driftFiles=0`、`porcelain=0` |

---

## §2 S 清单（每条给复现命令）

### S2-1（必须修完或具名改期）· 终态断言的鉴别力只覆盖**单引号拼写**；提交信息与测试头注释的「塞一条当场变红」是过度断言

- 事实：判据件扫描器 `packages/skill-calorie/test/cmd-registry-294.test.mjs:50` ＝ `/case '([^']+)'/g`，**只认单引号 `case`**。
- **我的新探针（本席专打「脚本自我满足」盲区，派单未要求）**：在同一位置塞双引号分支
  `switch (key) { case "calorie.zz": { fail(3,'zz'); } }`
  → 变异 sha256 `6EDD9505F5E431AC`（≠ 无变异 `7EF33346AE76FA60`），**`packages/skill-calorie/test/cmd-registry-294.test.mjs` 仍 `tests 7 / pass 7 / fail 0`、exit=0**（持锁 runId `e2d3f0ac-8418-46e9-9067-5103a3fd4a52`）⇒ 一条真·按键分支进得了分派层而棘轮**不响**。
- 兜底也不存在：`Test-Path eslint.config.js/.eslintrc.cjs/biome.json/.prettierrc` 全 `False`；根 `package.json` **无 `lint` 脚本**。
- 反向对照（要求项，已做）：单引号版本**红**（见维 4 表）。
- 复现：`…cmd_read.ts` 插入上句 → `node tooling/run-locked.mjs --ticket t320r-probe --lock-dir .scratch/locks-land --stale-minutes 30 -- node --test packages/skill-calorie/test/cmd-registry-294.test.mjs` → `git checkout -- packages/skill-calorie/src/cli/cmd_read.ts`。
- 修法（一行级）：判据改**行为口径**，别绑拼写——把正则放宽到 `case\s+['"\`]…`；或更硬：断言两个分派文件里**不出现任何 `calorie.` 字面量键**（`/['"]calorie\.[^'"`]+['"]/`），并把双引号样本加进 `:79` 的鉴别力自证。
- 归属：**本票引入**（新终态断言由本提交写下）。**不构成 S1**：票面字面契约是「`case 'calorie.` 标签集合为空」，该条**已满足**；缺陷在**它自称的保证**（「往分派层加分支已无上限可调」）被 1 个字符的变体证伪。

### S3-2 · 同根因第二形态：`if` 阶梯分派同样逃过断言（建议与 S2-1 一并修）

- 探针：塞 `if (key === 'calorie.yy') fail(3, 'yy');` → 变异 sha256 见 `.scratch/t320r/mutC.log`，**`tests 7 / pass 7 / fail 0`、exit=0**（持锁 runId `72fe95f6-f100-4bb4-b5ed-91179c4901ab`）。
- 复现命令同上（把插入串换成本句）。
- 归属：**本票引入**（判据射程设计）；修法与 S2-1 同一处（行为口径即同时堵住两种写法）。

### S3-3 · 落点理由次序写反：把「判官口径」放第一，把产品依据（键族／写孪生／实现模块）埋在 `commands.ts` 文件头

- 事实：`docs/skills/skill-calorie/t320-收口-证据2.md` §1.1 标题即「落点…**由判官口径反推**的机器证据，不是偏好」；而 `packages/skill-calorie/src/goal/commands.ts:2-8` 才给出键族三依据。落点本身**正确**（维 3 已自算确认），但读者会误以为落点由判官常量决定 ⇒ 一旦判官常量日后变动，接手者会得出「该搬去 weight」的错结论。
- 复现：`GET` 两处原文（`Select-String -Path docs/skills/skill-calorie/t320-收口-证据2.md -Pattern '落点：'`；`Select-String -Path packages/skill-calorie/src/goal/commands.ts -Pattern '键族'`）。
- 修法：把证据 §1.1 的首位依据换成「键族九条同主人 ＋ 写孪生 `calorie.goal.weight` ＋ 实现住 `render/goalPlate.ts`」，判官常量降为**同向旁证**。

### S3-4 · 同一提交内的陈述错误：测试件注释仍写「搬进 `src/weight/`」

- `packages/skill-calorie/test/cmd-registry-294.test.mjs:5`：「#320 把最后一条老键（`calorie.view.goal-weight`）搬进 **`src/weight/`**…」，与实施落点 `src/goal/` 直接矛盾（`:139` 同一件里又写对了）。
- 复现：`Select-String -Path packages/skill-calorie/test/cmd-registry-294.test.mjs -Pattern 'src/weight/'`
- 修法：改一字（`src/goal/`）。归属：**本票引入**。

### S3-5 · 旧证据件无「已被取代」指引，读者可能误判票面状态

- `docs/skills/skill-calorie/t320-收口-证据.md`（当刻 HEAD `61d784f`）§6.1／§6.3 结论是「达成条件①②**未成立**」（`cmd_read.ts` 351 行、4 处 `case`、旧形态棘轮 `FROZEN_*_CASES_MAX=61/31`）；该件未随本提交更新，也没有指向 `t320-收口-证据2.md` 的取代声明。
- 复现：`Select-String -Path docs/skills/skill-calorie/t320-收口-证据.md -Pattern '未成立|61d784f'`
- 修法：该件首行加一行「状态：本条窗口（61d784f）读数；①② 的转绿见 `t320-收口-证据2.md`（1ac41ca）」。归属：**本票范围**（收口票的取证面），不阻塞关闭。

### S3-6（范围外发现，转票）· `src/output.ts` 仍有 21 处按键 `case`，终态断言射程不含它

- 事实：`src/output.ts:336-411` 一段回执装配 switch 里 21 个 `case 'calorie.…'`（`t320-残余定性.md §3` 已记账）；新终态断言的射程是 `src/cli`，**不含 `src/output.ts`** ⇒ 日后有人往那里加键分支，棘轮同样不响。
- 复现：`Select-String -Path packages/skill-calorie/src/output.ts -Pattern "case 'calorie\." | Measure-Object`（21）
- 按 §5.1 归属纪律：**范围外发现，不得单独决定 FAIL**，只作 S3 ＋ 转票措辞：「把 `src/output.ts` 的 21 处按键回执端口也切成键集查表，并把终态断言的射程扩到它能覆盖的回执装配面」。

---

## §3 五维给分与判定（`docs/subagent-concurrency-protocol.md` §5.1）

| 维 | 权重 | 给分 | 依据 |
|---|---|---|---|
| 契约一致 | 30 | **26** | 未搬迁 0／分派层 0 命中／101 闭环／六字段照抄＋必备 `run`／三条记录定点搬入：逐条自跑为真；扣 4 ＝ 终态断言仅在单引号拼写下成立（S2-1、S3-2） |
| 证据真实可复现 | 25 | **22** | 六条 runId 全绿、audit 双向 PASS、变异红绿签名一致、**变异前 sha256 `7EF33346AE76FA60` 与作者自报逐字同值**；扣 3 ＝ 「塞一条当场变红」写成普适（实为拼写相关）＋ 落点理由次序写反（S3-3） |
| parity | 20 | **20** | 五件生成物 blob 同父、`routes.generated.ts` sha256 同值；路由记录并集 **51→51 UNION-IDENTICAL**；`wakeWord` 照抄（REPR／SKILL.md 不动）；快照 186 件 0 变；冻结件 8/8 |
| 工程红线 | 15 | **15** | 未触碰他人路径（并集证明）；只搬空不删文件；判官 sha256 逐字未动；全程经持锁包装器；无 `--allow-*`／`GATE-RELAX`；无 `--amend/--force*/rebase`；树干净（含 gitignored `dist/` 面单独核） |
| 文档同步 | 10 | **7** | 证据台账齐（六条 `GATE-RUN` 一对一＋受跟踪导出件）；扣 3 ＝ `cmd-registry-294:5` 的 `src/weight/` 陈述错误（S3-4）＋ 旧证据件无取代指引（S3-5） |
| **合计** | 100 | **90** | |

**判定：PASS（有条件）**。量刑律口径：**无 S1**（无契约违反／无用户可见回归／无证据造假或私自放宽／无数据丢失风险；隔离核过：`git diff --cached` 面与 12 件 blob 零漂移）且**均分 90 ≥ 85** ⇒ 不判 FAIL。**S2-1 须在关闭前修完**，或由编排者书面改期并给具名票号；S3-2…S3-6 记账跟进、不阻塞收口（S3-6 转票）。

---

## §4 未做的动态项（诚实清单）

1. **`weight/` 落点的动态复跑未做**（作者自报 runId `4101606b-…` 的 `P1=PENDING P2=FAIL P4=FAIL`）。理由：该实验必须把声明搬进 `src/weight/` 并重生成生成物，触碰红线 ⛔（不改 `packages/`）且会用新字节覆盖本窗已验证的生成物字节；本维改用**静态机械闭合**（判官 `:1279` 逐字 `=== 5 && === 4` ＋ 我自算 `wReads=5 wWrites=4`）。若编排者要动态读数，请在**独立工作树**里授权一窗。
2. **未做整仓 `tsc -b --force` 全量重编**：本窗 `tsc -b` exit=0 且增量判定为「无需重建」，改以**内容级**证据核新鲜度（`dist/cli/cmd_read.js` 无搬迁键、`dist/goal/read.js` 有 `viewGoalWeight`、`dist/.gen-inputs.json` 在）。
3. **未复跑 `t320-gate-runs.log`（前一窗口的导出件）**：本维对账按派单口径只钉 `t320b-gate-runs.log`；前一窗口的 `t320-收口-证据.md` 只做文本核对（S3-5）。
4. **未做的动态项中不含「五条绿线」**：六条线全部自跑、全部持锁，runId 见维 5。

## §5 风险 top3

1. **棘轮是拼写门，不是行为门**（S2-1＋S3-2）：终态保证可被双引号或 `if` 阶梯绕过，且仓内无 lint 兜底。修复一行、收益最大。
2. **`src/output.ts` 21 处按键分支不在射程**（S3-6）：分派层「无按键分支」的结论**仅对 `src/cli` 成立**；把它当成全仓结论会误判后续票的返修量。
3. **落点理由写在判官常量上**（S3-3）：短期无害（两者同向），但下次有人「顺手修判官常量」时，会连带把归属结论改错。

---

# §6 追加复核（S2-1 定点复验；被审 sha `e9ee42eb8d533f061afe7813064c7b0f66aa5b60`）

- 被审提交：`e9ee42e`＝`fix(320b2): 棘轮终态判据升到行为口径（S2-1／S3-2）；测试头注释落点改事实（S3-4）`；`HEAD == origin/master == e9ee42e`（自复算）
- 改动面：4 件 `+262 −28`（`test/cmd-registry-294.test.mjs`／`src/cli/write.ts`／`t320-收口-证据2.md`／`t320b-gate-runs.log`）；**生成物 0 件**
- 判官：`78AE857426A44D66E81A2489E51D0D2790BD16A0812DB78818B1D2E918BEBE39`（**未动**，与 §0 同值）
- **判定：PASS · 97/100 · 无 S1／无 S2 · S2-1 与 S3-2 均已关闭 ⇒ 可以关票**（余下全是 S3 记账）；收尾 `porcelain=0`

## §6.1 S2-1／S3-2 是否真消除 —— **已消除（端到端，非自述）**

新判据把口径从**拼写**升到**行为**（`calorieDispatchLiteralsOf`：`case` 任意引号／`===`|`!==` 两侧／就地键集查询；数据位与注释豁免；同一扫描器对文件与合成样本两用，`:67-107`）。

**我的六形态注入（一次注入六条，判据消息逐条点名 ⇒ 一条命令即得六份归因）**：在 `cmd_read.ts:150` 前插入
`case 'calorie.d1'`／`case "calorie.d2"`／`if (key === 'calorie.d3')`／`if ('calorie.d4' === key)`／`if (['calorie.d5'].indexOf(key) >= 0)`／`` case `calorie.d6` ``（模板串标号）。
- 命令：`node tooling/run-locked.mjs --ticket t320r2-probe --lock-dir .scratch/locks-land --stale-minutes 30 -- node --test packages/skill-calorie/test/cmd-registry-294.test.mjs`
- 读数：注入 sha `6229CD13881D997D`；**exit=1**、`tests 7 / pass 6 / fail 1`、消息
  **`分派层仍有按键分派（#320 起老路应无活口）：calorie.d1、calorie.d2、calorie.d3、calorie.d4、calorie.d5、calorie.d6`**
  ⇒ 六种写法**全被同一判据看见**：我上轮的两个活口（d2 双引号＝S2-1、d3 `if` 阶梯＝S3-2）**关闭**，d4 反向比较／d5 就地 `.indexOf`／d6 模板串标号（我自选新形态）也**覆盖**。

**数据位防误伤 ＋ 残留边界（我自设探针，一次注入同时问两件事）**：注入
`const DATA_SET = new Set(['calorie.p1']);`／注释 `// …case 'calorie.p2' …`／`const row = { key: 'calorie.p3', run: 1 };`／`switch (true) { case isCalorieKey('calorie.d7'): … }`
- 命令：同上换 `--ticket t320r2-probe2`；读数：注入 sha `14A98BAB24C2E704`、**exit=0、`tests 7 / pass 7 / fail 0`**
- ⇒ ① **三个数据位（具名键集／注释／对象键值行）都不误报**（判据没有退化成「grep `calorie.`」）；
  ② **残留边界＝「调用位的键字面量」**：`switch (true) { case isCalorieKey('calorie.d7'): }` 这种「字面量当函数实参、判定在函数里」的形态**逃过**判据（作者件内注释已把 `switch (true)` 列入已知边界，故这是**已声明的边界**，不是隐瞒）。同一机制的另一半：**具名键集 ＋ `.has(key)`** 也豁免——而它正是本提交给 `write.ts` 用的合规改法（件内 `:57-60` 亦写明）。⇒ 见 S3-7。
- **复位自证**：`git checkout -- packages/skill-calorie/src/cli/cmd_read.ts` ⇒ sha256 回 **`7EF33346AE76FA60`**、`blobOK=True`、`porcelain=0`。

**反假绿回放（我自设第 3 个探针）**：把新扫描器从判据件里原样取出，回放**搬迁前**与**当刻**的两个分派文件——
`node .scratch/t320r2/replay.mjs`（`.scratch/t320r2/old-*.ts` 由 `git show 1ac41ca^:<path>` 落盘）
- `1ac41ca^ cmd_read.ts: n=1 ["calorie.view.goal-weight"]`；`1ac41ca^ write.ts: n=3 ["calorie.profile.activity","calorie.profile.set","calorie.profile.update"]`；`e9ee42e cmd_read.ts|write.ts: n=0 []`
- ⇒ 新判据**没有变窄**：它当年要守的四处活口（1＋3）在它眼下**照样红**；当刻 0 命中。这条直接堵死「判据放宽换绿」的路。

## §6.2 `write.ts` 改写语义等价 —— **PASS（逐字等价）**

- 当刻（`src/cli/write.ts:61-77`）：`PROFILE_RECEIPT_KEYS = {profile.set, profile.activity, profile.update}`；`:73 !has(key) → null`；`:74 PROFILE_UPDATE_KEYS.has(key) ? buildProfileUpdateReceiptDoc(receipt, commandLine(key,params)) : buildProfileSettingReceiptDoc(db, receipt, commandLine(key,params))`；`PROFILE_UPDATE_KEYS = {calorie.profile.update}`（单元素集，`.has` ≡ `===`）。
- 旧（`1ac41ca:write.ts`）：三 `case` 标签映射到同样两个构造函数、同样实参次序，`default → null`。
- 逐键对拍：`set`／`activity` → 设置页分支、`update` → 改档案页分支、**其余任何键 → `null`** ⇒ 三条映射一一对应、行为逐字等价；且两键集都是**具名数据位**，本文件再无键字面量比较（新判据 n=0 已证）。
- 旁证：`1492/1492` ＋ 判官 `P4=PASS`（薄分派：两个分派文件分支数 0、体重 5 读 4 写）＋ 冻结件 `8/8`。

## §6.3 五条绿线／冻结件／对账（全自跑，持锁 `--ticket t320r2`）

| 线 | 读数 | 我的 runId |
|---|---|---|
| `tsc -b` | exit 0 | `cc1dcf5d-f5af-48e2-95ae-290a56043776` |
| `gen-cli.mjs --check` | exit 0；`键 101（写 35 ＋ 读 66）… 未搬迁清单 0 条` | `61df1306-3927-4ec7-9ae7-34ea8933cb92` |
| 判官 | exit 0；`P1..P5 5/5`；`UNACCOUNTED=0 处`；`权威声明齐=9/9` | `15a5d74f-8e5b-4b08-bf7e-ebc7cbd2d4cb` |
| 全量测试 | exit 0；`tests 1492 / pass 1492 / fail 0` | `1ef4d58c-7b7b-4917-a80e-3e2314cf1718` |
| 快照 | exit 0；`186 件产物 changed=0 added=0 removed=0`、指纹 `ef7cc029…` | `746ba4f4-4c56-43a0-9851-09b72046dc0f` |
| 冻结件 `calorie-routing-81` | exit 0；`tests 8 / pass 8 / fail 0` | `aaca8a8f-c79d-49cf-80e3-2e9357819c3d` |

**对账（我跑了 5 个变体，同一导出件 `docs/skills/skill-calorie/t320b-gate-runs.log`，窗口 `--since 2026-09-13T21:58:12Z`）**

| 变体 | 命令要点 | 读数 | exit |
|---|---|---|---|
| A1 | `--ticket t320b --allow-nonzero`（**不加** `--allow-undeclared`） | `matched=16/16 auditEntries=31 scoped=6 undeclared=0` ＋ PASS | 0 |
| A3 | 严格（无任何 `--allow-*`） | `matched=13/16 … undeclared=0` ＋ `FAIL: 3/16 条声明…找不到对应条目` | 1 |
| A4 | `--ticket t320b2 --allow-nonzero --allow-undeclared` | `matched=10/16 … scoped=10 undeclared=0` ＋ FAIL（另 6 条属 t320b，被票号挡在窗口外） | 1 |
| A5 | 无 `--ticket` ＋ 两个放宽 | `matched=16/16 auditEntries=31 scoped=31 undeclared=15` ＋ PASS | 0 |

**`undeclared=15` 逐条点名**（我自算：导出件 31 行 − 证据件 16 条 `GATE-RUN` 声明 ＝ 15）：

| 票号 | 条数 | 说明 |
|---|---|---|
| `t320r` | 8 | 本复核席上轮：6 条绿线 ＋ 2 轮对账 |
| `t320r-mut`／`t320r-probe`／`t320r-probe2` | 3 | 本复核席上轮三次变异探针（含 1 条设计红） |
| `t320b-audit`／`t320b-audit2`／`t320b-audit3` | 3 | 作者席自己的对账自跑 |
| `t320b2-explore` | 1 | 作者席探索跑 |

⇒ **15 条全部属他票／自检，没有一条是 `t320b`／`t320b2` 的正式门禁**（被审票的 16 条 ⇒ 6＋10 条**全部**被声明一对一认领）。导演这条自报**核实为真**。

**两条 `--allow-*` 的判词与整改建议（S3-8）**：
- 两者都按协议 §2.4 写了 `GATE-RELAX flag=… reason=…`（证据 `:333-334`），**不是私自放宽、不是造假**，本轮**可接受**。
- `--allow-nonzero`：**用法正确**。被认领的 3 条 `exit=1` 是本票**按设计必须红**的变异读数（§5 要求留痕），声明它们比不声明更严（不声明就会落进反向对账）；且它们随导出件长期留档。
- `--allow-undeclared`：**是口径自招、可以退休**。根因＝「一份证据件跨两票（t320b 6 条 ＋ t320b2 10 条）＋ 共享日志 ＋ 单次运行只吃一个 `--ticket`」。**我实测 A1：票号内跑（`--ticket t320b --allow-nonzero`）就 `undeclared=0` 且 PASS**。整改建议（任一）：① 对账按票各跑一次，别用「无 `--ticket` 的跨票窗」；② 导出件按票分文件（`t320b-gate-runs.log`／`t320b2-gate-runs.log`）；③ 若坚持跨票并窗，把「设计红」的变异读数与门禁读数分文件、分措辞（`MUT-RUN` 留痕），声明集只留门禁。长期看：**别把「跨票窗口」当默认口径**，否则 `undeclared` 这条最有力的反向对账会习惯性失效。

## §6.4 S 清单（本轮）

- **S2-1 · 已关闭**（六形态端到端全红；双引号／`if` 阶梯两条原活口被同一判据看见）。**S3-2 · 已关闭**（同一条判据覆盖 `===`／`!==` 两侧）。
- **S3-4 · 已关闭**：测试件头注释现写 `搬进 src/goal/（键族 view.goal* 同主人…`（`Select-String -Pattern 'src/(goal|weight)/'` 命中 `src/goal/`）。
- **S3-7（记账，不阻塞）**：判据仍有两处**已声明**的残留边界——① 调用位字面量（`case isCalorieKey('calorie.x')`／`switch (true)` 形态，我的 R2 探针实证）；② 具名键集 ＋ `.has(key)`（我的 R2 数据位探针实证，且这正是 `write.ts` 的合规改法）。二者都写在件内注释 `:57-60`。要再收紧需换真解析（AST）或加「分派文件只许出现 N 个已登记数据键」的白名单——作者已以「零白名单零豁免」否决后者，属**取舍**而非缺陷。
- **S3-8（记账，不阻塞）**：对账口径建议见 §6.3（退休 `--allow-undeclared`）。
- **S3-3（上轮，未修，仍记账）**：落点理由次序仍以判官口径列首（`t320-收口-证据2.md` §1.1）；`commands.ts` 文件头已有键族依据，属文档次序问题。
- **S3-6（上轮，范围外，转票）**：`src/output.ts:336–411` 21 处按键 `case` 不在断言射程。

## §6.5 五维给分与关票判定

| 维 | 权重 | 上轮 | 本轮 | 依据 |
|---|---|---|---|---|
| 契约一致 | 30 | 26 | **29** | 终态断言升到行为口径并端到端实证（六形态全红、四处历史活口回放全红、当刻 0）；扣 1 ＝ S3-7 两处已声明残留边界 |
| 证据真实可复现 | 25 | 22 | **24** | 五条绿线＋冻结件＋5 个对账变体我全部自跑命中，`GATE-RUN` 16 条一对一、`GATE-RELAX` 2 条在案；扣 1 ＝ 对账需跨票放宽（S3-8） |
| parity | 20 | 20 | **20** | 生成物 0 件改动、五件 blob 未动；快照 186 件 `changed=0`；`gen:check` 101／未搬迁 0；判官 `5/5`；冻结件 `8/8`；`write.ts` 改写逐键等价 |
| 工程红线 | 15 | 15 | **15** | 只读复核未越界；变异只改 `src` 且三次逐字复位（`7EF33346AE76FA60`／`blobOK=True`）；全程持锁；无 `--amend/--force*`；`porcelain=0` |
| 文档同步 | 10 | 7 | **9** | S3-4 已修；证据件 §9 追加 10 条 `GATE-RUN` ＋ 2 条 `GATE-RELAX` ＋ 导出件并窗留痕；扣 1 ＝ S3-3 未改次序 |
| **合计** | 100 | 90 | **97** | |

**量刑**：无 S1、S2-1 已关闭 ⇒ 均分 97 ≥ 85 ⇒ **PASS**。**判词：`#320` 可以关票**——上轮唯一的关闭阻塞项（S2-1）已消除且经端到端反证；余下 S3-3／S3-6／S3-7／S3-8 全部为记账项（S3-6 转票、S3-8 建议退休放宽口径），不阻塞关闭。

## §6.6 本轮未做的动态项

1. **未做 weight 落点的动态复跑**（同 §4-1，红线禁改 `src/`）。
2. **未做整仓 `tsc -b --force`**：本窗 `tsc -b` 增量判定无需重建；仍以内容级证据核派生面（`dist/cli/cmd_read.js` 无搬迁键／`dist/goal/read.js` 有 `viewGoalWeight`）。
3. **未用 AST 解析器核判据完备性**：S3-7 的结论来自我自设的 R2 注入探针（端到端），不是形式化证明；更强的完备性主张需要 AST 判据，属建议不属本轮范围。
