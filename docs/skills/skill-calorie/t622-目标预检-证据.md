# t622 两条目标词预检确认页证据（场景 04 运动 · 地图 #156）

目标：两条唤醒词「看今日运动（vs 目标）」「看本周运动（vs 目标）」在无目标时出预检确认页
（过程型 HTML，exit 0 ＋落盘完整文档：现值缺席说明＋目标值输入指引＋确认后复制指令），
确认后调 #621 的 `calorie.goal.exercise` 写入再出终页；有目标直出终页不变；不画 0% 空环假页面。

前置：#621 写命令已合入（`fa17ba8` 在链上，`git merge-base --is-ancestor fa17ba8 HEAD` exit 0）。
票面：`gh issue view 622` 可读（全文见派单）；`#506` 裁定走甲＋修正（本票即实施票）。

## 开工他席在途件（`git status --short`，本票一字未碰）

- `M packages/base-combos/combos.yaml`
- `M packages/base-combos/src/present.ts`
- `M packages/skill-bill/src/record/receiptBody.ts`
- `M packages/skill-bill/src/record/scene-income.ts`
- `M packages/skill-bill/src/record/scene-photo.ts`
- `M packages/skill-bill/src/record/scene-plain.ts`
- `M packages/skill-bill/src/record/scene-reimburse.ts`
- `M packages/skill-bill/src/shared/duplicateNote.ts`
- `M packages/skill-calorie/scripts/build-help.mjs`
- `M packages/skill-calorie/src/cli/keys.ts`
- `M packages/skill-calorie/src/triggers/routes.generated.ts`
- `M packages/skill-bill/test/record-write.test.mjs`
- `?? docs/base/base-render/t567-公共层形状清账-证据.md`
- `?? docs/skills/skill-calorie/t520-重评2-V1.md`／`V2`／`V3`／`V4.md`
- `?? 工作区审查报告-20260916.html`

## 结构纪律（必报五步）

- 第一步 · 影响清单（每行一个能力目录）：
  - `packages/skill-calorie/src/exercise/`：新 `precheck.ts`（预检确认页装配）＋ 改 `goal.ts`（无目标分支）；
    理由：两条词的命令住运动能力，预检页是该能力的装配。
  - 读目标值走目标能力的公开接口 `src/goal/nutritionGoal.ts::getNutritionGoal`（跨能力引用写明出处，
    不抄目标 SQL 与算式；铁律一）。
  - `packages/skill-calorie/SKILL.md`：仅两条词前置说明一段（HELP 侧 prompt 原文已含先问，本票不碰 HELP 源）。
  - 测试与证据：新 `test/exercise-goal-precheck-622.test.mjs`＋新本证据件＋`.scratch/t622/` 草稿（不管辖）。
- 第二步 · 结构设计：
  - 新 `src/exercise/precheck.ts`（150 行）：对外 2 件（`ExerciseGoalPrecheckView` 7 字段＋
    `buildExerciseGoalPrecheckDoc`／`exerciseGoalWriteExample`），款照抄 `src/goal/precheck.ts`
    与 `src/diet/precheck.ts`（现值 → 要填的一项 → 确认后操作 → 复制区指令／数据／日志三通道）。
  - 改 `src/exercise/goal.ts`（57 行）：有目标直出终页（仍走 `buildExerciseGoalView`＋`buildExerciseGoalDoc`，
    产物与现形状同口径）；无目标且窗内有记录出预检页；无目标且窗内无记录仍走原链抛同一条缺失阻断
    （保 #267 空库档）。
- 第三步 · 写代码：跨能力只调公开接口；常量与类型不另立第二份；目录内互用不对外。
- 第四步 · 超线报警：本票两件 `precheck.ts` 150 行／`goal.ts` 57 行，均未超 350 行告警线，不触发第四步。
- 第五步 · 交付对账：见下「交付路径」，与第一步逐行对得上，偏差为零。

## 判据先行（红 → 绿）

- 红：`node tooling/run-locked.mjs --ticket 622 -- node --test packages/skill-calorie/test/exercise-goal-precheck-622.test.mjs`，
  fail 3（无目标应 exit 0 实测 4，点名本票新断言），见 `.scratch/t622/red-01.log`。
- 绿：同一条靶向命令，pass 3 ／ fail 0（无目标出预检页；未答不给终页；有目标出终页），见 `.scratch/t622/green-02.log`。
- GATE-RUN runId：红 `71ef3489-5c02-4855-9d0d-f72959a481ad`；绿 `f0261fc8-afab-4373-b149-8ce7afce0076`
 （还原后复验绿 `f69d4cad-485d-4e2b-8d06-19f57173ed8b`）。

## 变异（删预检分支 → 必红 → 逐字节还原 → 回绿）

- 变异红：把 `src/exercise/goal.ts` 里 `if (!hasExercise)` 改成 `if (true)` 一处（预检分支走不到），
  重建后同一条靶向命令 fail 3（实测 4，与红同错），见 `.scratch/t622/mut-red.log`
 （runId `3c0cc71b-be46-471c-8d53-741488e973a8`，重建 runId `3f77a049-eab5-4cb0-877f-af77ede69a5a` exit 0）。
- 变异绿：逐字节还原（与 `.scratch/t622/goal.ts.good` `same=True`，sha `83c04027ccf9`）后重建
  （runId `4475ee5c-092d-4dff-82d4-281276fac582` exit 0）回绿 pass 3，见 `.scratch/t622/green-03.log`
 （runId `f69d4cad-485d-4e2b-8d06-19f57173ed8b`）。

## 编译与派生

- 编译：`node tooling/run-locked.mjs --ticket 622 -- node node_modules/typescript/bin/tsc -b packages/skill-calorie`，
  exit 0（runId `529974d0-6897-43ae-8950-efd6db39f03e`，还原后复建 runId `4475ee5c-092d-4dff-82d4-281276fac582` exit 0）。
- 派生：本票不改命令与路由（`commands.ts`／`routes.ts` 一字不动），`gen:check` 无需重跑；
  生成物 `keys.ts`／`routes.generated.ts` 正被他席改动，本票不碰。

## 回归（靶向两条，经持锁包装器）

- `exercise-summary-goal-fusion-452.test.mjs`：pass 9 ／ fail 0（runId `5b9ebe75-9c7a-4720-9361-093181cb72a8`），#452 不回退。
- `exercise-accept-267.test.mjs`：pass 8 ／ fail 0（`T267-RESULT: 8/8`，runId `d5ce9ee2-6837-4cae-a934-ded9ef58f7b6`）。
  - 保住的口径：无目标且窗内无记录仍走原缺失阻断（空库阻断 32 ＋ 新增出账 7，与改前同数），
    有记录无目标才出预检页；有目标终页与现形状同口径。

## 告警线门（只读，未加 `--sync`）

- `node tooling/run-locked.mjs --ticket 622 -- node packages/skill-calorie/scripts/check-warning-line.mjs`：
  `RESULT: 91/91`、`PASS: 告警线台账齐全且与实况一致`（runId `507c9ff8-159b-400c-8421-652cedf05ce0`）。
- 本票两件均未超 350 行（`precheck.ts` 150／`goal.ts` 57），门内无本票件的 OVER 行。

## 交付路径（本票提交只含这些）

- 新：`packages/skill-calorie/src/exercise/precheck.ts`（预检确认页装配，款照抄目标与饮食两款再改）
- 改：`packages/skill-calorie/src/exercise/goal.ts`（无目标分支，读经 `getNutritionGoal` 公开接口）
- 新：`packages/skill-calorie/test/exercise-goal-precheck-622.test.mjs`（判据先行三态）
- 改：`packages/skill-calorie/SKILL.md`（仅两条词前置说明一段，AUTO 块外；HELP 源正被他席改动故不碰，
  两条词 prompt 原文已含先问目标值，本段即工作流程第一步的对齐说明，最小改）
- 新：本证据件
- 草稿：`.scratch/t622/`（红绿、变异、编译、回归、门禁日志＋`goal.ts.good`）

## 取代关系（地图 #156 正文更正，随本票在评论里记，旧字不改）

- 旧句：「39 条逐条无先问要求、0 条需要过程型 HTML」。
- 取代：两条 vs 目标词（`看今日运动（vs 目标）`／`看本周运动（vs 目标）`）经 #506 实测确含「先问我目标值」，
  本票落地后这两条在无目标且窗内有记录时需过程型 HTML（预检确认页），旧句对这两条不再成立；
  其余 37 条仍无先问要求。以本条为准，旧字不改。

## 提交

- `git diff --cached --name-only` 复核只含本票 5 条路径后提交，信息 `feat(622): 两条目标词预检确认页加判据与证据`；不推送。
