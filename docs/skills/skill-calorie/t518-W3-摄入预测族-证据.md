# t518 W3 · 摄入预测族证据（7 页：14–20）

> **本窗范围**：摄入预测族 **7 页**——14–17 摄入预测（按当前速率 1 周／1 月／3 月／自定义）＋ 18 营养目标达成预测 ＋ 19 卡路里缺口预测 ＋ 20 摄入稳定性预测。
> 这一族**没有别名页**（`routes.ts` 只各挂一条唤醒词），故 W3 判据是 **23/23**：本票负责的 01–23 全部节点级为 0。
> 全部命令经加锁包装器 `--ticket 518 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000` 跑；条目导出件＝[`t518-GATE-RUN.log`](t518-GATE-RUN.log)。

## 一 · 判据读数

### 1.1 主判据：本票 23 页分隔符节点级全零

```
RESULT: 23/31（W0 改前 2/31 → W2 16/31 → W3 23/31）
```

逐页核对（`.scratch/t518/W3-sep.json`）：**01–23 全部 `node` 命中 = 0**（W2 已把 01–13＋23 打到 0，本窗补上 14–20 共 7 页）。

剩下的 **8 页红全部是报告族 24–31**（`看BMI报告`／`看TDEE报告`／`看BMR报告`／`看蛋白质摄入报告`／`看水分摄入报告`／`看综合评分`／`看健康趋势`／`看健康报告(含对比)`，逐页 5–12 处）：

| 页 | 节点级 | 页 | 节点级 |
|---|---|---|---|
| 24 BMI | 6（R1=4 R6=1 R7=1） | 28 水分 | 7（R1=4 R3=1 R6=1 R7=1） |
| 25 TDEE | 7（R1=3 R3=2 R6=1 R7=1） | 29 综合评分 | 6（R1=4 R6=1 R7=1） |
| 26 BMR | 7（R1=3 R3=2 R6=1 R7=1） | 30 健康趋势 | 5（R1=3 R6=1 R7=1） |
| 27 蛋白质 | 7（R1=4 R3=1 R6=1 R7=1） | 31 健康报告(含对比) | 12（R1=4 R3=2 R5=1 R6=4 R7=1） |

**这 8 页住 `src/analysis/reportDoc*.ts`／`reportPlate.ts`，是 #519 地盘**，本票红线「一行不碰」⇒ 本票范围内的目标是 **23/23**，31/31 需 #519 落地。这条是**范围外发现**，已写进 W2 证据 §五③ 并在回执里点名。

```
GATE-RUN runId=f2ccffe2-bd98-4d13-826b-7b2747b68a80 cmd="node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t518/产物-W3 --json .scratch/t518/W3-sep.json"  exit=1（门按 31 页判；本票 23 页全零见上）
```

### 1.2 回归底线

```
OVERFLOW-ZERO pages=31 cells=124 failed=0 scopeOutFailed=0
CLIPPED>0 格数=0   scroller>0 格数=0
```

```
GATE-RUN runId=af4084aa-7ff6-4cb7-9322-ab5cb2d5a322 cmd="node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t518/产物-W3 --widths 390,512,820,1440 --json .scratch/t518/W3-measure.json"  exit=0
```

### 1.3 靶向测试（七件 38 条全绿）

`analysis-shape-predict-520.test.mjs` 本窗**扩容到 8 页**（W2 的 4 个装配件 ＋ W3 的 4 个装配件），J2／J8／J9 三条恒出、导航双向自洽、零分隔符这几条现在覆盖**预测族全部 9 个装配件**。

```
tests 38 / pass 38 / fail 0
```

```
GATE-RUN runId=5cede315-936d-437a-8b98-e0de49b407f4 cmd="node --test packages/skill-calorie/test/analysis-shape-predict-520.test.mjs"  exit=0
GATE-RUN runId=e30ce82a-2933-4c3e-864d-b166d4971a22 cmd="node --test 靶向七件合跑"                                                                  exit=0
```

### 1.4 变异自证两行（源码级）

打的是本窗铺开的**口径行**：删掉 `buildCalorieDeficitDoc` 里那条 `renderCaliberLine(...)`（「缺口＝日常消耗加运动消耗减当天摄入｜…」）→ 重编 →（a）形状测试必红；（b）31 页重出后与 W3 基线逐字节比对必有差（第 19 页）。逐文件写回 → 重编 → 两项回绿。

```
MUT-RED shape-test exit=1   AssertionError: 19 摄入预测(卡路里缺口预测) 口径行与来源脚注都走 ilife-block-caliber，至少两条
MUT-RED pages-identical=30/31 DIFF=19
RESTORE 相等=true sha256_12=6636c2c45046
GREEN shape-test exit=0
GREEN pages-identical=31/31 DIFF=none
```

```
GATE-RUN runId=c606b530-a9bd-48e1-8fb0-9776688680fc cmd="node .scratch/t518/w3-mutate.mjs"  exit=0
```

### 1.5 包内两道门 ＋ 台账

| 门 | 摘要行 | exit |
|---|---|---|
| 告警线 | `RESULT: 91/91` ＋ `PASS: 告警线台账齐全且与实况一致` | 0 |
| 生成链 | `GEN-CHECK PASS：键 128（写 46 ＋ 读 82）…未搬迁清单 0 条` | 0 |

```
SYNC-PLAN mode=write 行=44 改=1 增=0 删=0
SYNC-CHANGE src/render/trendPredictDocs.ts 台账=718 实况=747
SYNC-VERIFY ok（回读逐字节等于计划；台账 44 行）
```

```
GATE-RUN runId=f8ac1c26-7cc2-4da4-8741-468181f74d0e cmd="node packages/skill-calorie/scripts/check-warning-line.mjs --sync"  exit=0
GATE-RUN runId=85efc324-2d19-4e40-b684-125be2c7b961 cmd="node packages/skill-calorie/scripts/check-warning-line.mjs"          exit=0
GATE-RUN runId=ef44664c-e541-4c5b-b66b-5a1e831eb2b4 cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"              exit=0
```

## 二 · 铺开做了什么（逐页）

四个装配件都改走 W2 起就位的预测族页框（`predictChips`／`predictNav`／`pageSection`／`sourceFootnote`／`spanDaysOf`），**没有新增任何形状件**：

| 页 | 结论条（只用页里已有的数） | 形状化掉的债 | 保留的断言认领点 |
|---|---|---|---|
| 14–17 | 「按当前速率，N 天后每天大约吃 X 卡。」＋（有目标时）「目标 M 卡。」 | `<title>` 的 `·`（`卡路里·趋势` → `卡路里 摄入预测`）／眉标 `摄入预测 · 趋势分析域` 拆胶囊＋域词／参数说明的 `；` 改两句／`subtitle` 从含 `；` 的 insight 原文改 `null` | H1「摄入预测(按当前速率 N 天)」逐字不动（`analysis-accept-386` 的「H1 含自己参数」与「20 页两两互异」）；卡 label「当前摄入／目标／日变化／摄入预测」不动（needle「摄入」「目标」） |
| 18 | 「这段的日均摄入 X 卡，比目标 Y 卡守在 ±10% 以内／差了 Z 卡。」 | 同上；「是否在轨」改走状态徽章 | 卡 label「均值／目标／缺口／是否在轨」与值「在轨／偏离」不动（needle「目标」「在轨」） |
| 19 | 「这段时间平均每天有 +X 卡缺口，折算下来一周约 Y kg。」 | 同上；「每周掉重」卡的 `detail` 改成健康区间（原来没有） | 卡 label「平均缺口」「每周掉重」不动（needle「缺口」「每周」） |
| 20 | 「日均摄入 X 卡，每天上下波动 Y 卡，算稳／波动偏大。」 | 同上；σ 的统计含义改住口径行 | 卡 label「均值／波动／是否稳定」不动（needle「稳定」「波动」） |

- 新增一列形状：14–17 的轨迹表带**预计区间**（`renderDataTable` 三列：日期／预测摄入／预计区间），表题按 `spanDaysOf` 写**实际覆盖天数**（#455 同口径）。
- 页宽一律 `pageChromeCss(1120)`；页面本地**一行色值、一个字号都没写**。
- 口径行与来源脚注每页各一条（J9 三条恒出的后两条），页头胶囊与结论条补齐前两条。

## 三 · 重出与可复跑

`node .scratch/t518/gen31.mjs --out .scratch/t518/产物-W3 --baseline .scratch/t518/W3-基线.json` → `RESULT: 31/31 页落盘 bytes_total=2390076（别名 01/23、21/22 逐字节同）`；1.4 的 `GREEN pages-identical=31/31` 就是**第二次重出**与基线的比对 ⇒ 可复跑性成立。

```
GATE-RUN runId=04d1999e-892d-4856-8c0f-cbf92522855c cmd="node .scratch/t518/gen31.mjs --out .scratch/t518/产物-W3 --baseline .scratch/t518/W3-基线.json"  exit=0
GATE-RUN runId=69468d24-3c6e-4f40-adb1-375a188607a1 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"  exit=0
```

## 四 · 必报五步（本窗后两件仍超线；数值一律经 `--sync` 生成）

- `src/render/trendDocs.ts`（**528 LF**）：**已超线，需要根据规则进行重构。** 超因与拆法见 W1 证据 §六。
- `src/render/trendPredictDocs.ts`（**747 LF**）：**已超线，需要根据规则进行重构。** 超因＝缺口族 1 页 ＋ 预测族 9 页 ＋ 两族页框小件（`PREDICT_SECTIONS`／`predictNav`／`predictChips`／`sourceFootnote`／`spanDaysOf`／`trackTable`／`pageSection`）同住一件；W3 又把摄入预测族 4 个装配件铺开（718→747）。**拆法＝按页族切两件姊妹件**：①缺口族一件（`buildDeficitDoc` ＋ `DEFICIT_*` 七个小件）②预测族一件（9 个装配件 ＋ 六个页框小件），两族共用的件头小件（`DOC_*`／`fmt`／`humanText`／`techNoteHtml`／`pageSection`）提为一件共件，出口经本件薄转出。**本票不做**：结构重排会让「31 页逐字节相同」这条判据在窗口内失去意义，且 W4 收尾要先把判据全绿；待 W4 评估或收口票认领。

## 五 · 未做项与下一手

1. **报告族 8 页（24–31）**：本票红线不碰（#519 地盘）⇒「31 页节点级全零」这条目标值需要 #519 落地后才成立；本票交付的是 **23/23**。
2. **范围外发现**：`src/exercise/receipt.ts` 台账在 W2 窗被别席推到 439（W3 窗无新变化）；归属别席，按裁定 A 记录并随 `--sync` 带绿。
3. **W4 收尾**（下一窗）：31 页统一重出（含别名逐字节同）、全套机器判据复跑、`trendPredictDocs.ts` 台账结论列补写 W2／W3 的成长事实、`t518-W4-收尾-证据.md` 汇总逐页读数与三张吸收票的兑现点。
