# t453 · 分布／力量／有氧三页融合版式（地图 #156 页面族第 4 票）

**票**：#453｜**地图**：#156｜**形状样板**：#423（已关票，独立对抗审查 PASS 99/100）
**实现**：`packages/skill-calorie/src/render/sportPortDocs.ts` 的三支 builder（`buildDistributionDoc`／`buildStrengthDoc`／`buildCardioDoc`）
**判据**：`packages/skill-calorie/test/exercise-dist-strength-cardio-fusion-453.test.mjs`（5 条）
**样例**：`.scratch/t453/out/01-distribution.html`｜`02-strength.html`｜`03-cardio.html`

## 〇、本席进场时先量的三行读数（续作席，前一席网络中断未提交）

1. **改动清单**：`git diff --numstat -- packages/skill-calorie/src/render/sportPortDocs.ts` = `531 531`，只有一个 hunk（`@@ -1,531 +1,531 @@`）。
2. **逐件统计**：字节比对——HEAD 版 22313 字节／531 LF／0 个 CR；盘上版 22844 字节／531 CRLF。去掉 CR 后两版**逐字节相同**（`CONTENT_IDENTICAL_IGNORING_CR=True`）。
3. **能否对上票面四条**：**对不上**。票面说的「大笔未提交改动」实为**整文件 LF→CRLF 行尾重写**，一条内容改动都没有；前一席的三支实现**不在盘上**（其 `.scratch/t453/` 的草稿与跑次日志留存：`green-run-3.txt` 记「4 红 1 绿」，实现本体已丢）。

**本席据此的动作**：把盘上文件写回**我另存的原字节**（`.scratch/t453/head_version.bytes`，sha256 `2469C45A…`，22313 字节／0 个 CR），先使该件与 HEAD 逐字节一致；此后三支的改动**只落在本票的三个 builder 上**，同文件里趋势（`buildTrendDoc`）与复盘（`buildRecapDoc`／`buildReviewDoc`）三支**一行未碰**。**未用 `git checkout --`。**

## 一、「N 条词各跑一次」＝「N 组冻结参数各跑一次」

三条词的冻结参数取 `src/triggers/scene-04-exercise.ts` 的 `main_prompt.cli`，逐字都是 `calorie-cmd-read <命令> --params '{"window":"7d"}'`，故本票＝**3 组冻结参数各跑一次**（窗口 `7d` 逐字；日锚 `CALORIE_TODAY=2026-09-07` 钉死 7 天窗＝2026-09-01 ~ 2026-09-07）。唤醒词接线归 #266。

| 词 | 命令 | exit | 产物字节 | 落点 |
|---|---|---|---|---|
| 看运动类型分布 | `calorie.view.exercise-distribution` | 0 | 75161 | `.scratch/t453/out/01-distribution.html` |
| 看力量训练总览 | `calorie.view.exercise-strength` | 0 | 71876 | `.scratch/t453/out/02-strength.html` |
| 看有氧训练总览 | `calorie.view.exercise-cardio` | 0 | 73190 | `.scratch/t453/out/03-cardio.html` |

空库三页按缺失阻断 `exit=4`、不落产物（`04-empty-*-blocked exit=4 bytes=0`）；空态页由装配层构造取证。

## 二、门禁对账（runId）

    GATE-RUN runId=13c96d1b-ffa5-4173-b2f9-cb3a5cae8485 先跑一次判据（红基线）：exit=1，tests 5 / pass 1 / fail 4
    GATE-RUN runId=1298953e-c2d7-4559-ba36-c41a41a6fd86 实现后：exit=0，tests 5 / pass 5 / fail 0
    GATE-RUN runId=f76a948f-675a-46d5-a010-5659790b4bfa 变异四步序两行（6 次定向编译 ＋ 6 次判据）：exit=0

## 三、判据两处「读到的不是产物」——本席改正（断言意图不变，未删未弱化任何断言）

1. **动作表标题的读取元素**：原稿 `/<p class="ilife-block-data-table-caption">…<\/p>/`。B-03 表格的标题元素恒是 `<caption class="ilife-block-data-table-caption">`（`renderDataTable` 从不产 `<p>`），原稿读到的恒是空串。改为读 `<caption …>`（同一处标题原字）。
2. **有氧种子缺 `minutes`**：`室内单车` 原稿没给 `minutes`，7 天窗时长合计＝182 分钟；而同一份判据冻结的合计是 `197 分钟`（第 270 行），第 344 行注释又写「本行有 0 以外的分钟数」。按判据自己的冻结合计与注释补齐 `minutes: 15`（182＋15＝197）；缺距离／缺步速两条仍保留（室内单车仍是距离与步速两格缺值）。

两处都在 `test/exercise-dist-strength-cardio-fusion-453.test.mjs` 内，属本票写集。

## 四、结构与口径逐条（票面 1–6）

- **分布页**：`sec-ratio` 占比迷你条（`ilife-block-dist-row` ＋ `-bar` ＋ `-val`，四类色逐类落进条）＋ `sec-table` 分类表（分类／次数／热量／占比／时长；四类行 ＋ **合计行** `7 次`｜`1200 卡`｜`100%`｜`197 分钟`）＋ `sec-chart` 走既有共享图表（`renderChartBlock`）＋ 摄入/TDEE 联动折叠卡。
- **力量页**：`sec-table` 动作表（动作／组数／总重量／总次数）＋ `sec-chart` 重量轨迹；**口径写在表标题里**：`按动作聚合（2026-09-01 ~ 2026-09-07）｜单侧口径 Σkg×次数…`，全页「单侧口径」**恰好 1 次**（不与别处重影）。
- **有氧页**：`sec-table` 类型表（类型／次数／时长／距离／步速），缺距离／缺步速两格显「—」；口径行 `口径：…步速＝分钟÷公里（没有距离就不算步速）；缺一格显「—」…`。
- **三页共同**：`renderTocBlock` 页内导航（href 全有落点）／`ilife-page-printable` ＋ `@page printable`（走 #448 的 `assembleDocPage({printable:true})` 透传位，不做装配后字符串手术）／`data-fmt` 三格式顺序 `text,json,csv`／`sourceLine` 来源脚注／`renderCaliberLine` 口径行（每页 ≥2）／页头人话（`<title>` 与眉标无命令键、票号、工序词「移植」）／空态走 `emptyGuide`（`ilife-empty` ＋ 下一句话：说「记运动」／「记力量训练」／「记有氧运动」，且不留空表外壳）。
- **四态头与变更卡载具不接**（器件归类，不是漏做）：`shared/operationHead.ts` 的三张表是**写操作**四态，只读页硬套会印出与事实不符的态标签（同 #451 裁定）。

## 五、类别色单源

三页的类别色**只从** `src/exercise/categoryColors.ts` 的 `categoryColor()` 取 **hex**；本件不写第二份色表、不写 token 名（`colorOf()` 一处映射「库内分类名 → 四色键」）。全仓检索四组「键→色值」绑定的**色表形状**（8 种写法并查）→ 命中文件数＝**1**：`packages/skill-calorie/src/exercise/categoryColors.ts`。判据第 4 条同读数，绿。

## 六、变异两行（先定向编译再跑判据；判据读 `dist/`）

原字节 sha256＝`5EF5EAE512739A5A97AAF34D48329933FECCA6B7C1F26DD40C76202AFBD3CF54`（29874 字节／702 LF）

| 步 | 动作 | 源码 sha256 | 定向编译 | 判据 | dist sha256 |
|---|---|---|---|---|---|
| 基 | 未变异 | `5EF5EAE5…` | exit=0 | **exit=0 · 5/5 绿** | `ADA64417…` |
| ① | 类别色写成死字面量（不走 `categoryColor()`） | `96FD2194…` | exit=0 | **exit=1 · 4/5 红** | `207E0867…` |
| ①′ | 写回原字节（刷 mtime） | `5EF5EAE5…` 逐字节等于原字节 | exit=0 | **exit=0 · 5/5 绿** | `ADA64417…` |
| ② | 有氧缺值兜底去掉（null 直出空单元格） | `817D9AFC…` | exit=0 | **exit=1 · 4/5 红** | `DF1C48BC…` |
| ②′ | 写回原字节（刷 mtime） | `5EF5EAE5…` 逐字节等于原字节 | exit=0 | **exit=0 · 5/5 绿** | `ADA64417…` |

- ① 红断言：`占比条缺「cardio」的类别色 #0071e3`（死字面量把四类色塌成一色）。
- ② 红断言：`缺距离没有显「—」（不是空单元格）：["室内单车","1 次","15 分钟","","—"]`。
- 还原一律**写回本席另存的原字节**（`.scratch/t453/orig-sportPortDocs.ts`）＋ `utimesSync` 刷 mtime（`tsc -b` 按时间戳判新鲜度，不刷会跳过重编译）；脚本自证 `BYTES-EQUAL-ORIG=true`、末行 `FINAL-SHA256=5EF5EAE5… EQUAL-ORIG=true`。**未用 `git checkout --`／`stash`／`reset`／`clean`。**

## 七、三类工程话（各 0）与零回归

- 三条词产物全文：`calorie.view.` 命中 **0**／`\bt\d{3}\b` 命中 **0**／工序词「移植」命中 **0**（三页逐页读数）。
- 零回归：`node --test packages/skill-calorie/test/doc-page-assert.mjs` **exit=0 · 1/1 绿**；同族兄弟判据同绿：`analysis-deficit-385` 6/6、`exercise-records-fusion-451` 8/8、`exercise-summary-goal-fusion-452` 8/8、`exercise-receipt-fusion-423` 20/20。

## 八、告警线与构建

`pnpm build` 本席当场**红**，**归因他席在途件**（非本票写集、未改一行）：`packages/skill-bill/src/record/scene-restore.ts(24,10): error TS2459: Module '"../shared/recordPicker.js"' declares 'isDeleted' locally, but it is not exported.`（该件 `M`、净增 242 行；`shared/recordPicker.ts` 是同一席的未跟踪新件。）本包定向编译 `node node_modules/typescript/bin/tsc -b packages/skill-calorie/tsconfig.json` **exit=0**。

- 挂号：`src/render/sportPortDocs.ts` 在册（挂号 `—`）。
- 改动**前**门读数：`RED 台账陈化：src/render/sportPortDocs.ts 台账=531 实况=702`（另有 `RED 漏报：src/render/sportDocs.ts LF=351`，属 #452 的在途件，其台账行由该席自行补写）。
- `--dry`：`SYNC-PLAN mode=dry 行=26 改=1 增=1 删=0`。
- `--sync`：`SYNC-VERIFY ok（回读逐字节等于计划；台账 26 行）`。
- 复跑：`RESULT: 58/58`、`PASS: 告警线台账齐全且与实况一致`、**exit=0**。
- 本件当场实测 **702 LF** 已同步进台账「当场实测」列；**挂号值 `—` 未回改**。
- **必报五步第四步**：已超线，需要根据规则进行重构。超因＝#111 运动移植 6 键的全文档装配同处一件，#453 落地融合版式后单件继续变长；本次先不拆：本票只做这三页版式，同件的趋势／复盘两支属第 5 票（#454）；拆法＝第 5 票落地后按页族切姊妹件（分布／力量／有氧一族 与 趋势／复盘一族分住两件，共用的卡装配、导航、色映射提为一件共件），出口留本件薄转出。

## 九、未做项 / 遗留

- 趋势与复盘三支（#454）本票一行未碰；同文件两票串行，本票落地后切。
- 视觉终验归 #268；本席只出结构与口径的机器读数与样例产物。
- `.scratch/t453/` 是样例与读数草稿（不进版本库）。

## 附、判据条数

`exercise-dist-strength-cardio-fusion-453.test.mjs`：`tests 5`（3 条读词逐条 ＋ 类别色单源 ＋ 空态），`pass 5 / fail 0`，`exit=0`。
