# #316 对抗式复核报告 —— 场景 04 运动 9 键搬进 `src/exercise/`

**判定：FAIL ｜ 82 / 100**（S1 = 0 · S2 = 4 · S3 = 4）
> **update**：返修补齐后结论已改 —— 见文末「返修复验（`b06bbfd`）」：8 处逐处已消，**verdict 转 PASS ｜ 96 / 100**。
PASS 门槛是「S1 = 0 且 **S2 = 0**」；本票 S2 = 4，门槛未达。**代码实质面全绿**（声明／分派／行为／生成四面独立复验通过），扣分集中在**证据的数字面与一处交付注释的指针**。

- 复核对象：`a9fb35c`「feat(316): 场景 04 运动 9 键搬进 src/exercise/」（`HEAD` = `origin/master` = `a9fb35c`）
- 票：#316 —— 当刻 `OPEN`，正文含 `## 进度：95%`，不含 `进度：100%`，字面 `\n` 计数 0
- 命令面：better sidebar 终端（uuid `b0b6cb77-34cd-4edc-b464-8f089deb0d6e`）；改树／构建／测试／生成一律经
  `node tooling/run-locked.mjs --ticket 316r --lock-dir .scratch/locks-land --stale-minutes 30 -- <cmd>`
- 全部动态项在**树空窗口**内跑（脚本自印 `TREE-AT-START=[]`、`HEAD=a9fb35c`）；我自己的写件只在 `.scratch/t316r/`
- 本席 runId（`ticket=316r`）：`a5aa33c8-6b7c-437c-8dd0-7cb21041a671`（battery4）／`43595fdb-9fc7-460d-9a7c-bd5a47ac931a`（battery3）

> **并发写者声明**：复核期间 #323r／#319 各有一次落点窗口在跑（我抢锁时分别等过 50 s／100 s）。所有「当刻」数字都取自
> `TREE-AT-START=[]` 的那两个窗口；#319 的在制品（` M src/cli/cmd_read.ts` 等）出现在我下班之后，未进任何判定。

---

## 1 七维逐条结论

### 1.1 声明面 —— **PASS**

**结论**：`src/exercise/commands.ts` 恰 9 条声明，键名与票面 9 键逐字一致，五字段齐（`kind`／`key`／`shape`／`title`／`example`），
`wakeWord` 仅 `calorie.view.exercise-goal` 缺省（#323 放开，且与旧分片原值一致）；与三处镜像逐条对得上。

原始证据：

- `git show --stat a9fb35c` → `packages/skill-calorie/src/exercise/commands.ts | 33 ++++`；读全文 33 行，`export const EXERCISE_COMMANDS = [ … ] satisfies readonly CommandSpec[]`，数组内行 24–32 恰 9 条。
- 9 键（`kind`／`shape`）：`calorie.exercise.add|remove|update` = write/receipt；`calorie.view.exercise-{cardio,distribution,goal,recap,strength,trend}` = read/stat。
- `grep` `packages/skill-calorie/src/cli/keys.ts` → 第 36–38 行（3 写键）＋ 89–95 行（6 读键），`shape`／`title` 与声明逐字同值。
- `grep` `packages/base-combos/combos.yaml` → 第 72–86 行（3 写键）＋ 287–317 行（6 读键），`skill: calorie`／`shape`／`title` 齐。
- `src/cli/registry.ts:11,19` → `import { EXERCISE_COMMANDS } from '../exercise/index.js'`、`SOURCES` 里第二项 `EXERCISE_COMMANDS`。
- 镜像一致性由生成器验真：`pnpm gen:check` → `GEN-CHECK ok` ×5（见 §1.4）。
- `src/exercise/index.ts` 对外三件（`EXERCISE_COMMANDS`／`runExerciseView`／`runExerciseWrite`），未超铁律五「不多于五个」。

### 1.2 分派面 —— **PASS**

**结论**：9 键的执行分支确在 `src/exercise/`；`legacy/scene-04.ts` 仍在但该 9 键零残留；`cmd_read.ts`／`write.ts` 两处的 9 个 `case` 已净；
全仓对这 9 键的**执行实现恰好一处**（`src/output.ts` 的 3 个写键 `case` 是回执摘要渲染，不是分派实现 —— 口径见 S3 备注）。

原始证据：

- 处理函数落点：`log.ts`→`calorie.exercise.add`；`edit.ts`→`calorie.exercise.update`＋`remove`；`goal/strength/cardio/distribution/recap/trend.ts`→6 个读键（`grep -n "export function"` 与声明 `run:` 逐条对上）。
- 私有件随迁：`EX_CAMEL` 只住 `edit.ts`（14／27／42 行），`oneExercise` 只住 `log.ts`（19／67／73 行）；`write.ts` 里两名字 0 命中。
- `read packages/skill-calorie/src/cli/legacy/scene-04.ts` → **12 行**，`export const LEGACY_SCENE_04: readonly LegacyCommandDecl[] = [];`（文件保留、归零）。
- `grep "case 'calorie\.(view\.exercise-(cardio|distribution|goal|recap|strength|trend)|exercise\.(add|remove|update))'"` over `packages/skill-calorie/src/cli` → **No matches found**（两分派文件 9 个 case 全净）。
- 注册表先行：`cmd_read.ts:235-243` `export function dispatch(...) { … const spec = REGISTRY[key]; … switch (key) {` —— 命中即走能力目录，故残留 `case` 是死代码（这条正是 §5 的 M3 实验前提）。
- 全仓 grep（`packages/skill-calorie/src`，9 键合取式）134 命中，逐类归位：权威声明 1 处（`exercise/commands.ts`）／路由声明 1 处（`exercise/routes.ts`）／能力实现 1 处／生成物（`keys.ts`／`routes.generated.ts`）／冻结唤醒词素材（`triggers/scene-04-exercise.ts`）／渲染域（`render/sportDocs.ts`／`render/sportPortDocs.ts`，属本票声明不搬的实现层 ⇒ #324）／回执摘要（`src/output.ts:377/390/392`）。**同一键没有第二处执行实现。**
- 路由分片：`git show a9fb35c -- packages/skill-calorie/src/cli/legacy/routes/scene-04.ts` 的 26 个删除行逐键统计 = `add×8 / update×2 / remove×3 / view-exercise-cardio×3 / -distribution×1 / -goal×3 / -recap×1 / -strength×3 / -trend×1 / non-exec×1`，**全部是本票自己的键**，一行都没误删 `calorie.view.exercise`（别人场景）的记录。
- `src/exercise/routes.ts` 44 行 26 条：wake 20（order 137–149／159–163／168–169，含 1 条 `non-exec`「看运动记录（有备注）」）＋ new 6（28／34／35／36／37／39）——与证据 §2 的 `list/order` 逐格对上。

### 1.3 行为不变 —— **PASS**

**结论**：`pnpm test` 全量 1492/1492；t293 探针 P1..P5 全绿且 `UNACCOUNTED=0`。

原始输出（我锁内复跑）：

```text
$ pnpm test
ℹ tests 1492
ℹ suites 160
ℹ pass 1492
ℹ fail 0
[[04-test]] exit=0
```

```text
$ node docs/skills/skill-calorie/t293-验收-命令自治.mjs
P1: PASS
  ③ UNACCOUNTED（必须手改、既没派生也没纪律覆盖，0 处）：
  ⇒ UNACCOUNTED 为空 ⇒ 手写面已全部落在 ①/② 内
  动态（派生桶）证据齐=true；UNACCOUNTED=0 处；树自相矛盾=0 处
P2: PASS   P3: PASS   P4: PASS   P5: PASS
RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5
```

交叉印证：探针 P4 自己打印 `cmd_read.ts … 物理行 799`、`write.ts … 物理行 466` —— 与证据 §1 的 1090／601 直接冲突（见 S2-②③）。

### 1.4 生成面 —— **PASS**

**结论**：干净树 + `pnpm build` 之后 `pnpm gen` 逐字节幂等（无 diff）；`pnpm gen:check` 五件全 `ok`；无手改生成物痕迹。

原始输出：

```text
$ pnpm build        → GEN-STAMP ok（声明源 33 件）                              exit=0
$ pnpm gen          → gen 完成：键 101（写 35 ＋ 读 66）；能力 6 个声明 45 条   exit=0
$ git status --porcelain -uall   → 空                                          （幂等）
$ pnpm gen:check
GEN-CHECK ok packages\skill-calorie\src\cli\keys.ts
GEN-CHECK ok packages\skill-calorie\src\cli\registry.ts
GEN-CHECK ok packages\base-combos\combos.yaml
GEN-CHECK ok packages\skill-calorie\scripts\build-help.mjs
GEN-CHECK ok packages\skill-calorie\src\triggers\routes.generated.ts
GEN-CHECK PASS：键 101…；能力 6 个…；未搬迁清单 56 条                            exit=0
```

补充实测：`pnpm gen` 的落笔面**不止** `gen:check` 逐件输出那五件 —— `packages/base-combos/src/present.ts` 也会被重写（我在 M2 变异时被它咬到，见 §8）。这条不进 #316 的整改项，只作事实登记。

### 1.5 证据面 —— 覆盖性合格，**数字面不合格**

**覆盖性 ✓（上一票栽的那条这票没栽）**

```text
$ node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t316-搬迁-证据.md \
    --ticket 316 --since 2026-09-13T14:01:29.801Z --until 2026-09-13T14:02:33.324Z --allow-nonzero
审计：.scratch/locks/gate-runs.log（RUN 条目 1668 条；窗口内 15 条，ticket=316…）
声称运行 15 条
RESULT: matched=15/15 auditEntries=1668 scoped=15 undeclared=0
gate-audit: PASS
```

- **导出窗口覆盖全部运行**：窗口内「无人声明的 RUN 条目 = 0」⇒ 证据声明的 15 次与窗口内 15 条一对一，**不是**上一票那种「只导出 6/12」。
- **`GATE-RUN` 行止于右引号**：§3 的 15 行全部以 `cmd="…"` 结束，无 `waitedMs=`／`exit=` 尾巴。
- **零 `--allow-*` 的口径**：实际用了 1 个 `--allow-nonzero`，证据末行有配对的 `GATE-RELAX flag=--allow-nonzero reason=…`；我另跑**不带**该开关的一次：
  `RESULT: matched=12/15 … undeclared=3  gate-audit: FAIL` —— 差的正是三条按设计打红的变异（`12735a13`／`da25d8be`／`439f5850`，均 `exit=1`）。放宽是必要的、理由也写对了。
- **对账源可独立复核**：`--log docs/skills/skill-calorie/t316-gate-runs.log --ticket 316 --allow-nonzero` → `matched=15/15 … gate-audit: PASS`（用受 git 跟踪的那份导出复算，不依赖 gitignored 的共享日志）。
- **路径／行号抽样全中**：`docs/skills/skill-calorie/t313a-分区-证据.md:461`（`pnpm help:examples:check` 带票外既有红）✓；`docs/research/t97-probe-receipts.mjs:100-102`（3 个写键探针）✓；`packages/skill-calorie/test/cmd-write-40-persist.test.mjs:282-350`（`exercise.add` 落库列）／`563-575`（`exercise.remove` 软删）✓。

**数字面 ✗**：见 §7 的 S2-①②③（行数三项全错，根因是把棘轮冻结值当实测值）与 S2-④（交付注释的指针地址当刻已不成立）。

### 1.6 反假绿 —— 一条挡得住，一条被打穿

详见 §3（攻击 A：打穿）与 §4（攻击 B：挡住）。这一维**不判 #316 交付本身**，按编排者 2026-09-13 口径独立成节。

### 1.7 票面与纪律 —— **PASS**

```text
$ gh issue view 316 --json state,title,body --jq '…'
OPEN || 卡路里命令登记与分派：场景 04 运动 搬迁（39 词）
   || HAS-LITERAL-BACKSLASH-N=false || HAS-95=true || HAS-100=false

$ node -e "（逐字节读两件）"
docs/skills/skill-calorie/t316-搬迁-证据.md   BOM=false  bytes=17831  CRLF=false
docs/skills/skill-calorie/t316-gate-runs.log  BOM=false  bytes=2497   CRLF=false
```

- 票面 `OPEN`、`## 进度：95%`、无 `100%`、无字面 `\n` ✓；两件文档 UTF-8 无 BOM、真换行 ✓。
- **无越界声明**：`build-tree.mjs`／`edit-dispatch.mjs`／`run-t293.mjs` 在证据里一律写成 `.scratch/t316/…` 的**本席预备脚本**，没有一处把它们说成「已入仓的生成器断言」。
- 偏离记账齐：实现层不搬转 #324（§7.1）、`wakeWord` 缺省归 #323（§7.2）、路由 26/19 归属（§7.3）、就地摆正 1 行注释（§7.4）、`test/cmd-write-40-persist` 零改动理由（§7.5）、定点删除与守卫口径（§7.6）、过程缺陷自曝（§7.7）。

---

## 2 变异记录（三步，全部在锁内、逐发回正）

| # | 动作 | 预期 | 实测 |
|---|---|---|---|
| M1 | 手改生成物 `src/cli/registry.ts`（`../exercise/index.js`→`…indexMUT.js`） | `gen:check` 红 | **GEN-CHECK FAIL**，逐行指出「盘上/生成」差异，exit=1 ✓ |
| M1 复原 | 源声明未动，靠 `pnpm gen` 重生成回正（**不是** `git checkout`） | 回绿 | `gen` exit=0 → `gen:check` PASS ✓ |
| M2′ | 改掉一个 key：`calorie.view.exercise-trend`→`…-XXX`，先 `pnpm build` 再 `pnpm gen` | 必红 | `gen:check` **PASS**（gen 刚按新声明重写过，两面自洽）；**第二次 `pnpm build` 红**：`routes.ts(43,77): error TS2820: Type '"calorie.view.exercise-trend"' is not assignable to type 'RouteKey'` exit=2 ✓ —— 抓它的是**类型门**（`tsc -b`），不是生成物门 |
| M3 | 把已搬走的 `case 'calorie.view.exercise-trend'`（含所需 import、原始分支体）加回 `cmd_read.ts` | 有没有门红 | **无门变红**（见 §5） |

M2′ 复原：只回正声明文件后 `pnpm build` 会因生成物已被上一次 `gen` 改写而不一致（同一发里我自己踩到，见 §8）——
正确路径是「回正声明 → 逐文件回正被 `gen` 改写的派生件 → `pnpm build` → `pnpm gen` → `gen:check`」，我照这条路走完，收尾 `TREE-AT-END=[]` ✓。

---

## 3 攻击 A（票外缺口，独立成节）：`Copy-Item` 保 mtime ⇒ `pnpm build` 假绿

**结论：打穿。**「改坏声明 + 照常跑一遍门禁链」可以拿到全绿的 `pnpm gen:check`，而当刻树里明明躺着一个内容被改坏的源文件。

### 3.1 精确复现步骤

```powershell
$src = 'packages/skill-calorie/src/exercise/commands.ts'
$T   = (Get-Item $src).LastWriteTime                       # 先记原 mtime
node .scratch/t316r/mut.mjs $src "'有氧训练总览'" "'有氧训练总览AAATTACK'"
Copy-Item $src (Join-Path $bak 'commands.mutmtime.ts')     # 复制出去
(Get-Item (Join-Path $bak 'commands.mutmtime.ts')).LastWriteTime = $T
Copy-Item (Join-Path $bak 'commands.mutmtime.ts') $src -Force   # 拷回：Copy-Item 保留 mtime
pnpm build      # 门禁链第一道
pnpm gen:check  # 免费判据
git status --porcelain -uall
```

### 3.2 观察到什么

```text
ATTACK-A: src mtime=2026-09-13T22:01:30.7715160+08:00  dist/exercise/commands.js sha256=4F16AE6E…
ATTACK-A: 覆盖后 src mtime=2026-09-13T22:01:30.7715160+08:00（与原值逐位相同）
[[08-attackA-build]] exit=0
ATTACK-A: build 后 dist sha256=4F16AE6E…   DIST-CHANGED=False     ← tsc -b 真跳过了，dist 一字未变
$ pnpm gen:check
GEN-CHECK ok …（5/5）
GEN-CHECK PASS：键 101…                                            exit=0   ← 假绿
$ git status --porcelain -uall
 M packages/skill-calorie/src/exercise/commands.ts                  ← 当刻树里内容确实被改坏了
```

### 3.3 为什么现在的机制抓不住

`pnpm build` = `tsc -b && node scripts/gen-cli.mjs --stamp`。两道防线各有职责：

1. **`tsc -b` 是增量编译**，判据是 `tsbuildinfo` 里记的文件版本（mtime＋size）。mtime 被 `Copy-Item` 拉回原值时，`tsc -b` 认定「源没动」⇒ **不重编** ⇒ `dist/<能力>/commands.js` 仍是旧声明。
2. **内容印记 `dist/.gen-inputs.json` 是「源文件的 sha256」**，它在 `tsc -b` **之后被无条件重打** —— 这里记的是「源现在长什么样」，**不记「dist 是不是这份源编出来的」**。攻击下源哈希变了、`dist` 哈希没变，印记照样"一致"。
3. `pnpm gen:check` 比的是**「盘上生成物」vs「生成器此刻的输出」**两面，而生成器读的是 `dist`。两面都源自同一份**陈旧 dist**，自然相等 ⇒ PASS。

所以：`GEN-STALE` 守的是「源改了、**没**跑 build」；它**不守**「源改了、build 跑了、`tsc -b` 却跳过」。

### 3.4 建议修法（对编排者设想的一处独立判断）

编排者的设想是「印记记**源↔`dist` 配对哈希**，`gen:check` 校验配对是否仍成立」——**成立，且是最省的落法**，我认可并把它具体化：

- 把 `.gen-inputs.json` 的每一条从 `{src: sha256(src)}` 改成 `{src: sha256(src), dist: sha256(dist/<对应编译产物>)}`；
- `gen:check` 开头加一步**配对校验**：重算现刻 `sha256(src)`，与印记里的比对（现有行为），**并**重算现刻 `sha256(dist)` 与印记里的 `dist` 比对；两者任一不等 ⇒ 报**新的一类** `GEN-PAIR FAIL`（措辞区别于 `GEN-STALE`：这一次是「dist 与它声称的源不是同一次产物，请重跑 `pnpm build` 并确认 `tsc -b` 真的重编了」）。
- 为什么这条够：攻击下源哈希变了而 `dist` 哈希没变 ⇒ 配对不成立 ⇒ 红。它同时覆盖「`tsc -b` 因 mtime／`tsbuildinfo` 被误判而跳过」的所有变体，不依赖任何时间戳语义（与现有设计「不看时间戳」的取向一致）。
- 更省的一步兜底（可与上面并用）：`--stamp` 那一步在打印记前**自检 `tsc -b` 是否真的产出过新 `dist`**（比对打印记前后 `dist/**` 的哈希集合，未变而源哈希已变即报红）。这条只多一次目录哈希，但把「跳过」这件事从"沉默"变成"出声"。
- **不建议**的修法：把 `pnpm build` 改成强制 `tsc -b --force`。它把每次全量重编的代价摊到所有 ticket 上，且下次有人写个「复制源文件」的脚本仍会重演；病根在**印记的语义**，不在编译开关。

### 3.5 对 MAP Destination「两道机器门」成立性的结论

- 「生成物门」**当前不成立为一道**完整门：在 `Copy-Item` 保 mtime 这条已知手法下它给假绿。**这不是 #316 引入的**（`gen:check` 与印记是 #295 交付的），#316 只是**第一个把「免费判据＝gen:check」写进证据的票**，所以它在这条路上的暴露面被放大了。
- 影响面：所有拿 `gen:check` 当唯一「生成物面」判据的票（本图六票全部）。
- 归口建议：另立一张票（口径/门禁类），修 §3.4；#316 的整改项**不含**这条。

---

## 4 攻击 B：只改源码不重编 ⇒ **挡得住**（真门）

```powershell
node .scratch/t316r/mut.mjs packages/skill-calorie/src/exercise/routes.ts "'看运动分类占比'" "'看运动分类占比BBATTACK'"
pnpm gen:check          # 不跑 build
```

```text
$ node packages/skill-calorie/scripts/gen-cli.mjs --check
GEN-STALE FAIL：dist/ 不是这些声明**现在的内容**的产物，继续跑会把旧声明当成事实（假绿）。先 `pnpm build`…再跑 `pnpm gen:check`。
  packages\skill-calorie\src\exercise\routes.ts 的内容与上次 `pnpm build` 时不同（印记 6114a81e068b… ≠ 现 8c6886a67e7b…）
[ELIFECYCLE] Command failed with exit code 1.
```

⇒ 「本仓已知陷阱二」**没有兑现**：内容印记（按 sha256、不看时间戳）在这一支上是真门。复原走 `pnpm gen` → `gen:check` PASS，`AFTER-ATTACK-B TREE=[]`。

---

## 5 M3（编排者追加）：把搬走的 `case` 加回分派层 ⇒ **抓不住**

**动作**：从 `git show a9fb35c^:…/cmd_read.ts` 取回 `case 'calorie.view.exercise-trend'` 的**原始分支体**，连它需要的两个 import 一起整段加回 `cmd_read.ts`
（文件物理行 **799→808**；探针 P4 自己就打印出了这个变化：`case '` **41→42** 处、`calorie.` 前缀 **35→36** 处 —— 而 P4 不对此断言）。
**先自证是真死代码**：`pnpm build` **exit=0**（能编译；`dispatch()` 里 `REGISTRY` 先行，这条 `case` 永远走不到）。

**四道门全部沉默**：

```text
[[48-mut3-build]]    exit=0      ← 能编译，不是编译错
[[49-mut3-gencheck]] exit=0      ← GEN-CHECK PASS
[[50-mut3-test]]     exit=0      ← ℹ tests 1492 / ℹ pass 1492 / ℹ fail 0
[[51-mut3-t293]]     exit=0      ← RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5，UNACCOUNTED=0
[[52-mut3-status2]]   M packages/skill-calorie/src/cli/cmd_read.ts   ← 树里确实躺着残留分支
```

**为什么抓不住**（三条机制，逐条给当刻事实）：

1. `packages/skill-calorie/test/cmd-registry-294.test.mjs` 的冻结 case 清单**至今仍含这 9 个键**：`FROZEN_READ_CASES` 第 47–48 行有 `…-strength／-cardio／-distribution／-recap／-trend`、第 61 行有 `…-goal`；`FROZEN_WRITE_CASES` 第 72 行有 `exercise.add／update／remove`。断言是 `current ⊆ frozen`（第 99–101 行），**加回已搬走的键不算"清单外的新 case"**。
2. 行数棘轮形同虚设：`FROZEN_READ_LINES = 1146`／`FROZEN_WRITE_LINES = 745`（第 42–43 行），当刻实测 **799／466**，余量 347／279 行 —— 加 9 行碰不到上限。**两个上限自 #294 起一次都没下调过。**
3. `test/legacy-frozen-295.mjs` 同样留着这 9 键（第 35–37、73–79 行），`FROZEN_LEGACY_MAX` 仍是 92（当刻未搬迁清单实测 **56** 条），`FROZEN_LEGACY_LINES` 仍是 119。**自 #295 起一次都没下调过**（不是本票独有的账：`calorie.view.plan*`／`calorie.photo.*` 等更早搬走的键也还留在里面）。
4. 探针 t293 的 P4 只遍历**体重 9 键**，P1 的静态面不看 `case` 条数 —— 也过。

⇒ **这是「分派层残留 case 删净」的机器门缺口**，与 #316 席在证据 §8.1 自曝、票面 `### 待确认` 登记的一致，**终态归 #320**，本报告**不因此判 #316 不合格**，只把可抄的断言留在这里。

**建议 #320 采的断言**（三条，任一条都能独立抓住上面这发变异）：

```js
// ① 「本票搬走的键在分派层零残留」——把冻结清单变成"已搬走的键不许再出现"
//    在 cmd-registry-294.test.mjs 里新增：
const MOVED_TO_CAPABILITY = new Set([...Object.keys(REGISTRY)]);   // 注册表里的键＝已搬迁
for (const [file, frozen] of [['cmd_read.ts', FROZEN_READ_CASES], ['write.ts', FROZEN_WRITE_CASES]]) {
  const { labels } = caseLabels(file);
  const back = [...labels].filter((k) => MOVED_TO_CAPABILITY.has(k));
  assert.deepEqual(back, [], file + ' 里出现了已随搬迁删掉的 case（注册表先行会让它静默变成死代码）：' + back.join('、'));
}
// ② 把 FROZEN_*_LINES 下调到当刻实测值（799／466），并在搬迁票里「随搬迁数下调」成为硬要求
// ③ 把 FROZEN_LEGACY_KEYS 里已搬走的键删掉、FROZEN_LEGACY_MAX 下调到当刻条数（56）
```

（①②合起来正好把「搬走的键加回分派层」和「搬走的键加回未搬迁清单」两头都封住；③ 是纪律书 §三 判据 3 的原话「搬完一条…棘轮上限是不是都变小了」。）

---

## 6 问题清单（三级）

### S1（交付缺陷，必须整改）—— **0 条**

对 **#316 交付本身** 没有发现必须整改的行为缺陷／机器门缺陷。两处疑似缺口按编排者 2026-09-13 口径分流，不计入本票：
- 「`Copy-Item` 保 mtime ⇒ 假绿」＝**票外缺口**（全仓口径，#295 引入）→ 另立票（§3）；
- 「分派层残留 `case` 无机器门」＝**已登记在案**（本票席自曝 ＋ 票面 `### 待确认`）→ 终态 **#320**（§5）。

### S2（证据与事实不符）—— **4 条**

| # | 位置 | 证据怎么写 | 当刻事实 | 判据 |
|---|---|---|---|---|
| S2-① | 证据 §1 末段 | 「新增 11 件**全部 ≤ 60 行**（最长 `edit.ts` **88 行**）」 | `edit.ts` = **96 行**、`log.ts` = **79 行**（两件超 60）；11 件里最长 96 | 读全文行数；且该句**自身矛盾**（「全部 ≤60」与「最长 88」不能同真） |
| S2-② | 证据 §1 末段 | 「`cmd_read.ts` **1146→1090**」 | **857→799**（-58） | `git show a9fb35c^:… \| node -e` = 857；HEAD = 799；**探针 P4 自己也打印 799**。1146 是 `cmd-registry-294.test.mjs:42` 的 **#294 冻结上限**，不是本票改前值；1090 = 1146−56 的**推算**，从未落地（上限当刻仍是 1146） |
| S2-③ | 证据 §1 末段 | 「`write.ts` **745→601**」 | **615→466**（-149） | 同上口径；745 是 `cmd-registry-294.test.mjs:43` 的冻结上限；**探针 P4 自己也打印 466** |
| S2-④ | 交付件 `src/exercise/routes.ts` 第 11–13 行（注释） | 「`calorie.view.exercise` 的 19 条记录…**记录仍住 `src/cli/legacy/routes/scene-04.ts`**」 | 该文件当刻**为空**（`ROUTES_SCENE_04 = []`）；那 19 条住 **`src/home/routes.ts:33-51`**（order 150–158／164–167／170–175，随 #314 搬进主页能力） | 本票 commit 对该文件是 `26 ---`（26 删、0 增）⇒ 落地时文件里只剩 26 行，19 条早已不在；`grep "key: 'calorie\.view\.exercise',"` 在 `home/routes.ts` 命中 19 条、在 `legacy/routes/scene-04.ts` 命中 0 条 |

> S2-①②③ 同源：**把棘轮的冻结上限当成文件的实测行数**，再用自身净删行数推算「改后值」，从未复测。
> 结论句「均未超线，第四步不触发」**结论本身是对的**（最长 96 ≪ 本包 350 告警线），但这不影响它是「证据与事实不符」。

### S3（措辞与口径）—— **4 条**

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| S3-① | 证据 §6 `TEST-TAIL` | 摘录只截了 `cancelled/skipped/todo/duration_ms` 四行，**没有** `ℹ tests 1492 / pass 1492 / fail 0`，声明「基线条数 1492」在摘录里无从核（事实为真：我复跑 1492/1492） | 摘录改成 `-Tail 8`，把三行数字带进去 |
| S3-② | 证据 §6 `T293-TAIL` | 摘录里**没有** `UNACCOUNTED=0 处；树自相矛盾=0 处` 那一行（只有 P1 的 7/7 命中段与 RESULT 行），而本票完成判据③正是「`UNACCOUNTED` 空」（事实为真：我复跑为 0） | 摘录里补那一行（或补 §P1 尾段） |
| S3-③ | 证据 §5 | 用 `tag 08-mut-gencheck`／`09-mutrestore`／`12-mut-build` 等指认 §3 的 runId，但 §3 的 15 行 `GATE-RUN` **没有 tag 字段**；这些标签只存在于 `.scratch/t316/gate-*.out.txt` 的**文件名**里，读者按 §3 核不到 | §5 表格改引 `runId` 前 8 位（当刻 §3 已有的字段），或 §3 每行补 `tag=` |
| S3-④ | 证据 §4 标题 ＋ §5 末段 | §4 标题写「生成物 diff **只应出现** `src/cli/registry.ts`」，下面列的却是 **5 个**文件（另 4 个是手写件）；§5「三条一起覆盖…源码改了没重编」按字面只覆盖「**没跑** `pnpm build`」那一支，读者容易读成「源码与 `dist` 不一致」这一类整体已覆盖（跑过 build 但 `tsc -b` 跳过的那支没覆盖，见 §3） | §4 标题改成「落地窗口内的改动面（生成物部分只有 `registry.ts`；逐字节验真见 `gen:check` 五件 ok）」；§5 补一行边界声明：「本类不含『mtime 未变导致 `tsc -b` 跳过』这一支 —— 该支当刻可绕过 `gen:check`，另立票」 |

> 备注（不计入 S3）：`src/output.ts:377/390/392` 仍有 3 个写键的 `case`（回执摘要尾串渲染）。它是**输出层**、不是分派实现，
> 判据「一 key 一处**执行**实现」在分派口径下成立；但证据通篇没提这一处，若把口径写成「全仓单处」会失真 —— 建议 §2 加半句限定「分派实现单处」。

---

## 7 必须整改（≤3 项）

1. **改证据 §1 的两组行数**（S2-①②③）：按当刻实测写成
   `cmd_read.ts 857→799`、`write.ts 615→466`；新增件写「11 件，最长 `edit.ts` 96 行（`log.ts` 79 行），全部远低于本包 350 告警线」。
   —— 只改文本，不改代码、不改行为。
2. **改 `src/exercise/routes.ts` 第 11–13 行的指针**（S2-④）：那 19 条 `calorie.view.exercise` 记录当刻住 `src/home/routes.ts`（随 #314 已搬进主页能力），
   `src/cli/legacy/routes/scene-04.ts` 已被本票清空 —— 指针按当刻事实重写。
   —— 只改注释文本，不改代码、不改行为。
3. **补证据的可核面**（S3-①②③④）：§6 两处摘录把 `tests/pass/fail` 与 `UNACCOUNTED=0` 带进去；§5 改引 `runId`；§4 标题与 §5 各补一行边界声明（含「mtime 跳过支不在本类覆盖内」）。

---

## 8 过程缺陷自曝与受限项

1. **我自己的第一版电池脚本有缺陷，并因此挡掉过别人的窗口（已处置）**：脚本里自定函数名 `Tee` 与 PowerShell 内置别名 `Tee-Object` 撞名，
   别名优先级高于函数 ⇒ 脚本里全部 `Tee '<name>' '<cmd>'` 调用静默失败，19 个 `pnpm build/gen/test` 步骤一次没跑；
   而夹在中间的 `node -e` 变异照常执行、没走到复原步 ⇒ 工作树留下 ` M src/cli/registry.ts`（内容 `…indexMUT.js`），
   挡掉了 **#318 落点窗口 `131bd876`（14:50:45 exit=1）**。我当刻按编排者指令逐文件回正并复报 `TREE=[]`；
   此后所有电池脚本改为「退出前断言树空，非空即 `ROLLBACK` 并把清单写进 `.scratch/t316r/logs/TREE-DIRTY*.txt`」。
2. **第二轮电池踩到第二层坑（同源，已处置）**：M2 变异走「先 `pnpm build` 再 `pnpm gen`」，而 `pnpm gen` 会把变异**写进四件派生件**
   （`src/cli/keys.ts`／`combos.yaml`／`scripts/build-help.mjs`，以及**没出现在 `gen:check` 逐件输出里的 `packages/base-combos/src/present.ts`**）；
   我的收尾守卫只点名了前几件，于是 `present.ts` 漏网、`tsc -b` 又因派生件与 `routes.generated.ts` 不再自洽而报 `TS2820`，
   形成「越修越脏」。已逐文件回正并复报 `TREE=[]`。教训已写进 §2 的复原路径。
3. **受限项（未完成／无法判定）**：
   - `pnpm help:examples:check` 我**没跑**：政策上它不在本票门禁链内且带票外既有红（证据 §8.3 已如实记账）—— 沿本条，不判缺陷。
   - #324（实现层搬迁）是否成立、#323 的 `wakeWord` 可缺对齐是否完备，属**别的票**的范围，本报告只确认 #316 的偏离记账与派单一致。
   - 复核期间 #319 的在制品（` M src/cli/cmd_read.ts`＝618 行等）出现在我收工之后；本报告一切「当刻」数字均取自我持锁且 `TREE-AT-START=[]` 的两段窗口，**不含**他人未提交改动。

---

# 返修复验（`b06bbfd`）

**复验判定：8 处逐处已消（S2 = 0 · S1 = 0）⇒ 本票由 FAIL 转 PASS ｜ 96 / 100**（新记 2 条 S3，措辞级、不阻塞）

- 复验对象：`b06bbfd`「docs(316): 证据件照当刻实测返修 —— §1 行数／§6 摘录／§5 边界 ＋ routes.ts 注释指针」
  —— `git show --stat`：`docs/skills/skill-calorie/t316-搬迁-证据.md | 76 +++++----`、`packages/skill-calorie/src/exercise/routes.ts | 3 +-`，
  **只有这两处**，与派单声明一致，无夹带改动。
- 判据口径：一切以**当刻**（我复验那一刻）事实为准，不与我原报告里的数字对比。

## 逐处对账

| # | 原判 | 返修后文本（摘） | 当刻实测（本席独立复算） | 结论 |
|---|---|---|---|---|
| S2-① | §1「11 件全部 ≤60 行（最长 `edit.ts` 88）」 | 逐件列 `edit.ts 96／log.ts 79／routes.ts 44／index.ts 38／commands.ts 33／cardio…22／goal 18`，并写「这两件在 60 行以上…不存在「全部 ≤60」这种说法」 | `node` 逐件复算：96／79／**45**／38／33／22×5／18 —— 除 `routes.ts` 一格（见新 S3-①）外**逐位相等** | **已消** |
| S2-② | §1「`cmd_read.ts` 1146→1090」 | 「按本票区间实测：改前（`a9fb35c^`）**857** → 改后（`a9fb35c`）**799**；`1146`／`745` 是 `test/cmd-registry-294.test.mjs:42-43` 的 #294 冻结上限，不是任何一端的实测行数，勿再当起点」 | 857／799 与我原报告独立测得**逐位相同**；`cmd-registry-294.test.mjs` 第 42–43 行确是 `1146`／`745` | **已消** |
| S2-③ | §1「`write.ts` 745→601」 | 「改前 **615** → 改后 **466**」 | 615／466 与我独立测得**逐位相同** | **已消** |
| S2-④ | `src/exercise/routes.ts:11-13` 注释称那 19 条仍住 `legacy/routes/scene-04.ts` | 「那 19 条（`order` 150–158／164–167／170–175）随 #314 已搬进主页能力，当刻住 `src/home/routes.ts:33-51`…本文件里的同名记录 0 条」 | `src/home/routes.ts` 第 **33–51 行**恰为那 19 条、`order` 逐条对上；`exercise/routes.ts` 里 `calorie.view.exercise` **0** 条；`legacy/routes/scene-04.ts` 仍是空数组 | **已消** |
| S3-① | §6 `TEST-TAIL` 缺 `tests/pass/fail` | 改为 `.scratch/t316/gate-06-test.out.txt` **末 10 个非空行逐字摘录** | 按同一定义复算该文件末 10 个非空行 → 与 §6 引文**逐字相同**（含 `duration_ms 39634.2951` 与两条 runId 行） | **已消** |
| S3-② | §6 `T293-TAIL` 缺 `UNACCOUNTED=0` | 改为 `gate-07-t293.out.txt` **第 22–29 行逐字摘录**，注明「`UNACCOUNTED=0 处` 在末行」 | 读该文件 22–29 行 → 与引文**逐字相同**；末行确为「动态（派生桶）证据齐=true；`UNACCOUNTED=0 处`；树自相矛盾=0 处」 | **已消** |
| S3-③ | §5 用 `tag 08-mut-gencheck` 指认 §3 runId，而 §3 无 tag 字段 | §5 改引「36 位 `runId` 的**前 8 位**」，并明写「§3 不另加 `tag` 列」 | 9 个前缀 `12735a13／028d55fb／84b74baa／da25d8be／d06c6108／059ef078／439f5850／8ec7e843／5500da00` 在 §3 的 15 行里逐条命中 | **已消** |
| S3-④ | §4 标题与 5 件列表口径不一；§5 缺边界声明 | §4 标题改「落地窗口内的改动面，共 5 件 —— 生成物部分只有 `src/cli/registry.ts`…」；§5 新增「边界声明（本轮覆盖到哪儿为止）」，明写**不含**「mtime 未变 ⇒ `tsc -b` 跳过」那一支并指 **#325** | 两处文本逐字核对成立；`#325` 与本文 §3 的修法互指 | **已消** |

## 独立门禁复跑（一条锁内命令，`--ticket 316r2`）

按新规则 26 直调 `node`（不用本机 `pnpm` 垫片）、规则 27 逐个断退出码类型（`ALL-INT=True`）、规则 24 用 `tsc -b` 不带 `--force`、未删 `dist`：

```text
runId=2d5e725d-b406-4a37-b731-27960c596ce7      HEAD=b06bbfd
TSC-ENTRY=node_modules/typescript/bin/tsc exists=True
TREE-START=[M packages/skill-calorie/test/cmd-read-t11.test.mjs / ?? …freeze-clock.cjs]   ← 别人的在制品
[[01-tsc]]      exit=0 type=Int32
[[02-stamp]]    exit=0 type=Int32
[[03-gen]]      exit=0 type=Int32
[[04-idem]]     exit=0 type=Int32        （gen 后 git status 与 START 相同 ⇒ 幂等）
[[05-gencheck]] exit=0 type=Int32        （GEN-CHECK 五件 ok / PASS）
[[06-t293]]     exit=0 type=Int32        （P1..P5 5/5、UNACCOUNTED=0）
[[07-test]]     exit=1 type=Int32        ← 4 红，全落在别人的在制品文件里（下节）
```

**关于 `07-test` 的红 —— 规则 30 两跑记录（两次输出都留档）**

| 跑次 | 结果 | 红的落点 |
|---|---|---|
| 第 1 跑（`verify.ps1`） | `ℹ tests 1492 / pass 1488 / **fail 4**` | 4 条全在 `packages/skill-calorie/test/cmd-read-t11.test.mjs`：`Error: no such function: d`（`ERR_SQLITE_ERROR`），落点 `seedFull:54 → mkEnv:73` |
| 第 2 跑（同树原样，`runId=8e5c6346…`） | `ℹ tests 1492 / pass **1491** / **fail 1**` | 仍是**同一件**文件（`T9 目标分析盘 parity`） |

- **两跑不同 ⇒ 按规则 30 判 flake**（不是 #316 的真回归）。
- **归因**：两次的红都只落在**别人未提交**的那一件 —— `git diff` 显示该件当刻 233 行（committed 版 **209** 行）、引入 `d()` 助手 **41** 处、并有新建件 `?? packages/skill-calorie/test/freeze-clock.cjs`（committed 版这两样都是 **0**）。
- **排除该在制品后的归因复跑**：其余 **149** 个测试文件 → `[[09-test-minus-wip]] exit=0`，`ℹ tests 1487 / pass 1487 / fail 0`（1492 − 5 = 1487，恰为该件自身的用例数）。
- ⇒ **该红不属于 `b06bbfd`**：本票返修只改一份文档与一行注释，不碰任何测试件、不改行为；`b06bbfd` 面的基线在排除他人在制品后**全绿**。

## 新记问题（均 S3，措辞级、不阻塞）

1. **新 S3-①（as-of 时差，性质同 #315r2 那条）**：§1 行数段首锚是 `BASE e9dc6523`，其中 `routes.ts` 写 **44** —— 在那锚点为真，但**本次返修自己**给该文件加了 1 行注释，当刻为 **45**；同段其余 12 个数字（含新加的 `cmd_read.ts 474`／`write.ts 223`）与当刻**逐位相同**。建议写成「`routes.ts` 44（BASE）→ 45（返修后）」，或把段首锚改成「逐件各自 as-of」。
2. **新 S3-②**：§6 末段「同文件末段逐字摘录」自 `gate-07-t293.out.txt` **第 67 行**起，恰好漏掉**第 66 行** —— 而 P1 判据点名的「第一条」`sport-homogeneity-109` 正在那一行；引文里写着「判据：7 条**全部命中**」，读起来只见 6 条。建议从第 66 行起摘，或加半句「本段自第 67 行起」。

## 复验结论

- 8 处（4 条 S2 ＋ 4 条 S3）**逐处已消**；返修面与派单声明**完全一致**，无夹带改动。
- **无新 S1、无新 S2** ⇒ 门槛「S1 = 0 且 S2 = 0」达成 ⇒ **verdict 由 FAIL 转 PASS**。
- **分数：96 / 100**（原 82：四条 S2 全消、证据数字面已可按当刻复算；余留 2 条新 S3 各扣 1，其余按实质面全绿 —— 声明／分派／行为／生成四面独立复验通过，门禁 `tsc／stamp／gen／gen:check／t293` 全 0）。
- 未决项照旧不属本票：分派层残留 `case` 的机器门 → **#320**；`gen:check` 假绿缺口 → **#325**；实现层迁移与 `wakeWord` 可缺 → **#324**／**#323**。
