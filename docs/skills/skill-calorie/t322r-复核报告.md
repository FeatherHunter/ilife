# #322r 对抗式复核报告（票 #322 · 六张场景票共用的四处登记断言预热）

- 复核席：票 #322r；被审对象：`47814a7`（四处共享位）＋ `c593777`（对账）。复核基线 HEAD＝`c593777`，开工时 `git status --porcelain` 空。
- 一句话结论：**没有发现 S1 交付缺陷**；四处改动**没有把棘轮净放宽**（assert 25→27，冻结集 92 键与 92／119 两个数字零放宽），
  「迁移无关」在**仓外副本**上被我用**另一条命令**独立复现（四处共享位一行未改即全绿）。
- 复核用品（全部在**仓外**，与主树无关）：副本 `D:\t322r-sim`（`robocopy` ＋ 根/逐包 `node_modules` junction，不安装）；
  脚本与日志 `.scratch/t322r/**`。**主树交付文件零改动**（收尾 `git status --porcelain` 仍为空）。

## ① 必做项逐条结论

**1. 断言没净删 ✅**（`.scratch/t322r/logs/s1.log`、`t293-main.log`）
自己数三档：`47814a7^`＝**25**、`1396d67`（P3 基线）＝**25**、现行＝**27**；`^test(` 块 7→7。
改写那一段 2 条等号 → 4 条（重复键／注册表 ⊇ 体重键／定义地／条数 ≤），净 **+2**，无净删。
自跑 `t293-验收-命令自治.mjs`：`RESULT: P1..P5 = PASS 5/5`，P3 原文
`diff 逐字比较 非空；--stat 36 ++++----；assert 次数 25 → 27；test 块 7 → 7`、`棘轮未被放宽=true`；
且同一次运行里沙箱基线 `gen:check` 绿、手改 `keys.ts` 一行 → exit=1、还原 → 0 ⇒ **P3 真绿，不是绕过**。

**2. 上限与冻结集零放宽 ✅**（`s2.log`、`s5.log`）
`47814a7^` 与现行逐位比较：键数 **92＝92**、`Compare-Object -SyncWindow 0` 顺序**逐位相同**、无重复；
`FROZEN_LEGACY_MAX=92`、`FROZEN_LEGACY_LINES=119` 一字未动；现行件 sha256 前缀 `C6DA06A414E395E0`，与实施者证据 §4.2 **逐字对上**。

**3. 独立复现「迁移无关」 ✅**（`s3-copy.log`／`s4-migrate.log`／`s5.log`／`s8.log`）——**换了一条命令**：`calorie.view.diet`
（实施者用 `calorie.view.goal-weight`；另注：**该键实际住 `scene-01.ts:12`**，不是简报里写的 `scene-02.ts`，我按实际归属片搬）。
搬法＝声明删自分片 → `src/tmpcap/commands.ts`（六字段逐字照抄，只补 `run`）＋ `case` 原样取出到 `src/tmpcap/diet.ts`（分派层删掉该 case）
＋ `routes/scene-01.ts` 里 `order:1`「看今日饮食概览」那条逐字搬进 `src/tmpcap/routes.ts`。
结果：四处共享位全程 sha256 **`E763ADB3…`／`8E5F2C17…`／`C6DA06A41…`／`35249E07…` 三次快照（PRE／GEN／FULL）一字未变**，且与主树逐字节相同。
免费判据（生成物）：`keys.ts 4DB43569…`／`combos.yaml CA17D2C3…`／`build-help.mjs 4D44F03F…`／`routes.generated.ts E9EA9935…`
**逐字节不变**，只有 `registry.ts` 变（`9E11F044…` → **`D18F1173AF74C915`**，＋tmpcap 两行）——**与该票证据 §4.3 的哈希逐字相同**（两席各搬各的，落到同一份 registry 字节）。
四处共享位未改的搬迁树上：`cmd-registry-294／legacy-partition-313／legacy-ratchet-295／calorie-routing-81` →**pass 24／fail 0**；`pnpm gen:check` → `GEN-CHECK PASS：键 101；能力 2 个（tmpcap／weight）声明 10 条，未搬迁清单 91 条`。
**顺序结论**：`build → gen → build → gen:check → test` **部分是硬条件**。实测（`s5.log` STEP A／D／E）：
① `gen` 不先 `build` 直接 **exit=1**（`缺 dist\tmpcap\commands.js` ＋ `GEN-STALE FAIL`）⇒ **第一次 build 硬必要**
（`gen-cli.mjs:67-87`／`:101-106`、`gen-routes.mjs:70-79` 的声明源都是 **dist**，不是 src）；
② `gen` 之后**不**再 build：`gen:check` 仍 **PASS**（STEP D），但测试 **红**（`#313 … actual: ['calorie.view.diet'], expected: []`，即**新写的「零丢失」断言抓住了 stale dist**）⇒ **第二次 build 对 test 硬必要、对 gen:check 不必要**。

**4. 新断言有牙（变异自证 ×2）✅**（`s8.log`）
① **定义地**：删掉 `src/weight/commands.ts` 里 `calorie.view.volatility` 一行（不重 build）⇒
`AssertionError: 注册表的键在各能力目录的声明里找不到定义地：calorie.view.volatility` ✅ 红。
①b（注册表少一个键）：在 `dist/cli/registry.js` 的 `REGISTRY_KEYS` 之前删一条 ⇒ `注册表缺了体重声明的键（生成物落后于权威源？）：calorie.view.volatility` ✅ 红。
② **零丢失（半迁移态）**：从 `scene-02.ts` 删 `calorie.diet.add`，**不加进任何能力目录**、冻结集不动 ⇒
`AssertionError: 冻结集里的命令既不在未搬迁清单、也不在能力目录（被静默删掉了一条命令）：calorie.diet.add` ✅ 红（另有 routing-81 一并红）。

**5. P5 正则修复是真修 ✅（但简报里的证伪方式不成立 → 见 D1）**（`t293-main.log`、`s9.log`）
主树实测该条 `✓ … 命中行 200`，`守 parity 的既有断言命中 7/7`；副本里把该断言（3 行）删掉 ⇒ 该条显示 `✗ 命中行 —`，**但 P5 仍 PASS**（`3/5`，P2／P3 的红是副本无 `.git` 的环境）。原因：`t293-验收:818` 的阈值是 `found ≥ 5`（共 7 条）——删 1 条还剩 6。⇒ 正则是**真修**（0 命中 → 命中真断言行），但 **P5 不能证明四条新断言都在**。

**6. 票面格式 ✅**（`s11.log`）：`state=OPEN`；`## 进度：95%` **独占一行且恰一行**；真实换行（无 CR）；**无 BOM**；**无字面 `\n`**；无 `100%`。未 close、未写 100%。

**7. 自设新探针 ✅（两条，见 ②）**

## ② 变异与新探针（各自红／绿的含义）

- **N1a（纪律面）**：搬迁后**故意不下调**冻结集与上限（`MAX` 仍 92、`calorie.view.diet` 仍在冻结集）⇒ legacy 四件**全绿**（`s8.log` baseline pass 24）。含义：**「搬完必须同步下调」已无断言兜住，只剩文本纪律**（与实施者 §7 风险 2 的披露一致，故非隐瞒）。
- **N1b（把旧等号放回去）**：在副本 `legacy-partition-313` 里插回 `deepEqual([...now], [...expected])` ⇒ **红**（`N1b：复核席放回的旧等号`）。
  红＝旧等号确实**是**那个唯一强制"同步下调"的机制；#322 把它换掉＝**用纪律换掉机械门**。两者合起来说明：这是**被披露的取舍**，不是偷偷放宽。
- **N2（定义地是不是恒真式）**：把 tmpcap 声明改成变量形式（`const DIET_KEY = '…'; key: DIET_KEY`）⇒
  `注册表的键在各能力目录的声明里找不到定义地：calorie.view.diet` **红**。红＝**它不是纯字符串恒真式**（真读源码），
  但**对声明写法过敏**：合法写法也会误红（实施者 §7 风险 3 已披露）。绿才该判缺陷——实测红，故该项不判缺陷。

## ③ 缺陷清单

- **D1【范围外发现｜判据灵敏度】** 本报告任务书「必做 5」预期"删掉新正则指向的断言即 P5 失守"**不成立**（实测 `P5=PASS`，`found 6/7 ≥ 5`，`t293-验收:818`）。归因：#293 的阈值与 7 条清单，**非本票引入**；新正则本身合格。影响：P5 不能当"四条新断言在位"的证明。
- **D2【本票范围｜已披露】** `cmd-registry-294` 的**定义地**断言依赖 `key: '…'` **字面量**（正则扫 `src/*/commands.ts`）。N2 实测：同一条声明改用变量形式即误红。六票若换声明形态会踩。
- **D3【本票范围｜已披露】** 三处 legacy 等号改相对量后，「搬迁必须同步下调冻结集／上限」只剩纪律（N1a 绿／N1b 红）。本票**数字零放宽**，但后续票整段搬空不会报红。
- **D4【本票范围】** 交付物证据 §1① 把"改前"第二条写成 `WEIGHT_COMMANDS.length, DECLARED_CAPABILITY_KEYS.length`；相对 **P3 基线 `1396d67`** 该行原为 `, 9,`（改成 `DECLARED_CAPABILITY_KEYS.length` 是更早的票）。措辞易让重跑者对不上 diff（`s1.log` 逐行公示）。不影响判据。
- 契约面另核：`git show --stat 47814a7`＝**恰 4 件共享位 ＋ 证据文件**，`src/triggers/scene-*.ts`／`test/calorie-routing-81.test.mjs`／五件生成物**零改动** ✅；
  `docs/agents/命令登记纪律-照抄说明.md:78`「搬迁一条既有命令 → `test/` 下 **0 件**必改」**已被本票变成事实**（副本实测），`命令登记纪律.md:97-103／:138` 的"下调上限"纪律文本仍在 ✅；共享日志 `GATE-RELAX` 计数 **0** ✅。

## ④ 分数与 verdict

| 维度 | 权重 | 得分 | 依据 |
| --- | --- | --- | --- |
| 契约一致 | 30 | **28** | 四条判据全部实证；扣 D4（基线措辞） |
| 证据真实可复现 | 25 | **23** | 三档 assert／92 键顺序／92·119／四个 sha／registry 生成物 sha 全部逐字复现；扣：副本未跑**全量** 1492 条 |
| parity | 20 | **19** | 四件生成物迁移下逐字节不变；扣 D1（P5 灵敏度，非本票引入） |
| 工程红线 | 15 | **15** | 主树零改动、只动四处＋证据、无 `--allow-*`、无危险 git、冻结数字零放宽 |
| 文档同步 | 10 | **9** | 证据诚实（§4.5 环境修正、§7 三条披露）；扣 D4 |

**总分 94／100；无 S1 ⇒ verdict：PASS。**

## ⑤ 未确证／风险 top3

1. **副本未跑全量**：我在副本里只跑了 4 个测试文件 ＋ `gen:check`（外加两轮 build）。"其余 1488 条也绿"仍只在实施者日志里，我没有复算。
2. **副本环境差异**：副本无 `.git`、包级 `node_modules` 用 junction ⇒ 副本里 P2／P3 探针的红是环境；副本结论只对"测试件语义"可靠，对 git／CI 断言不可靠。
3. **给六票的硬顺序**：`gen` 的声明源是 **dist**，跳过第一次 `build` 会得到 `GEN-STALE FAIL／缺 dist/*/commands.js`，签名不像本票缺陷 ⇒ 落点席容易误判归属。

## ⑥ 复核席的运行留痕（本票对账窗口 `--since 2026-09-13T20:03:45+08:00 --until 2026-09-13T20:14:00+08:00`）

```
GATE-RUN runId=bbc47334-b53a-4f7d-a88a-051862873d98 cmd="pwsh -NoProfile -File .scratch/t322r/s1-count.ps1"
GATE-RUN runId=2aee2330-a706-4fef-af74-ba16a9ff7e2f cmd="pwsh -NoProfile -File .scratch/t322r/s2-frozen.ps1"
GATE-RUN runId=14d28619-b191-4ed6-9ccf-da25fdbcd2c8 cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
```

窗口的界定理由（**不是放宽**）：`check-gate-audit.mjs` 的声明匹配**只认 `exit=0`** 的运行（实测：同一条声明对 `exit=1` 的条目一律报"未命中"），
故窗口起点取在**第一次成功运行**之后。窗口**之前**还有三次尝试，脚本自身有 PowerShell 解析错、`exit=1`，逐条如实列出、**不冒充成功**：
`ac1cb508-5f55-4b86-8c5b-32aade220904`／`fff3fe3a-c31e-46ac-811a-1db91215d993`／`9a77cce7-bad3-4440-af2e-f8ed2c19669f`
（命令都是 `pwsh -NoProfile -File .scratch/t322r/s1-count.ps1`；错因是我脚本里 `$()` 内的单引号写法，与四处共享位无关，改脚本后第 4 条即 `exit=0`）。
窗口内的三条**exit 都是 0**：第 1 条＝assert 三档计数，第 2 条＝冻结集对比，第 3 条＝自治探针 `5/5`。

说明：**副本内的 build／gen／test（s3–s9）没有走包装器**——它们不读不写主树任何文件（只读主树的 `node_modules` junction），
不占共享 `dist/`／印记，故未进窗口、也未放宽任何开关；本票**未使用任何 `--allow-*`**。
导出面：`docs/skills/skill-calorie/t322-gate-runs-322r.log`。报告与导出**未 git add／未提交**，留给编排者收口。
