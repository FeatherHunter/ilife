# #361 · 围度全量表 13 列 · 独立复核

> 复核席（本文件唯一落点；不改实现、不改测试、不关票、不 push）。
> 票面：`FeatherHunter/ilife#361`（OPEN；2026-09-14 取正文，`gh api …/issues/361`，评论 0 条）。
> 基准：`docs/skills/skill-calorie/t395-融合基准.md` §二-10／§二-12、§四-裁定 2、§六-B-5。
> 被复核提交：`0de70cb`（实现＋测试）／`a69a051`（证据＋changeset）／`014e4cc`（证据更正）。
> 作者证据：`docs/skills/skill-calorie/t361-实施证据.md`（只读）。
> 方法：不采信作者任何结论；每条判据自建现场（`.scratch/t361-review/` 独占草稿，未复用 `.scratch/t361/`）、
> 自选种子、自写解析与探针、自己的运行标识；编译／测试／变异全部经加锁包装器或持锁窗口内直调。

## 0 · 结论先行

| 维 | 复核结论 | 本席读数 |
|---|---|---|
| 票面验收：可见文本 13 部位名逐个出现 | **成立** | `R361-A1-NAMES hit=13/13`；表头 15 列且列序＝`MEASUREMENT_FIELDS` 序 |
| 票面负向：删掉一列 → 必红 | **成立**（作者两轮变异读数不可复跑公开，本席以**自设**变异替代自证） | mut1（列序错位）`test_exit=1 pass=7 fail=1`；mut2（卡片异源）`test_exit=1 pass=7 fail=1` |
| 裁定 2 判据一：表体缺值格 `—` | **成立** | `R361-A2-DASH-CELLS count=30 expect=30 bad=0`；`A2B-CONSEC-EMPTY count=0` |
| 裁定 2 判据二：payload 保留原始空值、不含 `—` | **成立**（两条**分开**实测，含「字段缺位」形态） | `R361-A4B null=30 absent=0 bad=0 dash_in_measure_cells=false`；`R361-A5-SYNTH absent_keys=13/13` |
| §二-10 窄屏卡片与宽表同一份数据 | **成立**（本席另做**逐行逐格**对照，作者只对日期列） | `R361-A3CARD mismatch=0 leak=0 filled=22`；4 张卡＝4 行 |
| §二-12 降序摘要「若本页有对比块则适用」 | **不适用**（前件为假：本页无对比块） | `git grep "最大变化\|降序" -- src` 命中 0；`bodyDocs.ts` 两分支均无对比块 |
| 不许动：#360 趋势闸门 | **未动**（本票删行**全部**落在 `metric` 为空的 else 分支） | 见 §6-2；KPI 在本席种子上按手算复现 |
| 编译 | **绿**（且本席另跑**强制**全量检查，排除增量空转假绿） | `e1383819…` `tsc -b --force` `exit=0`，无 `error TS` |
| 「全空行真库不可达」 | **不成立**（本票引入 · S3） | 见 §5：触发器只挂 `BEFORE INSERT`；`UPDATE` 可造成全空行，且真出口会渲染它 |
| 综合判定 | **PASS**（无 S1；3 条 S3 记账） | 见 §7 |

## 1 · 提交范围核对（逐件点名）

| 提交 | 内容（`git show --name-only`） | 核对 |
|---|---|---|
| `0de70cb` | `packages/skill-calorie/src/body/bodyDocs.ts`（63 增 13 删）＋ `packages/skill-calorie/test/t361-measure-full-table.test.mjs`（325 增） | 与声明一致；**无** `routes.generated.ts`；**无** `body/bodyPlate.ts`／`fetch/body.ts`／`body/compare.ts` |
| `a69a051` | `docs/skills/skill-calorie/t361-实施证据.md`＋`docs/skills/skill-calorie/t361-gate-runs.log`＋`.changeset/t361-measure-full-table.md` | 与声明一致；changeset 全仓唯一的 `*361*`，`'skill-calorie': patch` |
| `014e4cc` | 仅 `docs/skills/skill-calorie/t361-实施证据.md` | 与声明一致（自我更正一笔） |

- 入库态与工作区**逐字节一致**：`git hash-object` ＝ `060742afdf5fb67fb43f08dcc54931e1fe669dde` ＝ `git rev-parse 0de70cb:…bodyDocs.ts`；
  源码 `sha256=902b715b3ea15f59e422657220687ecf0ce873a0832742e38ee9ba8ab88b01a1`，`bytes=12655`；测试件 blob `4328f52f…`＝提交内 blob；
  声明路径 `git status --porcelain` 为空。
- 中文信息：三笔提交信息 `replacement_char=false`（无 U+FFFD），中文 459／364／377 字，主题与正文可读、无乱码。

## 2 · 门禁与独立复跑

本席全部运行经 `tooling/run-locked.mjs --ticket 361`（变异与探针窗口由外层包装器持锁，窗口内**直调**，无嵌套抢锁）。

**本席声明（逐条自跑）：**

- GATE-RUN runId=36fb7446-7767-47ff-a57a-c55d20e8af2f cmd=npx tsc -b packages/skill-calorie
- GATE-RUN runId=cdf99abb-b06c-457c-bbc3-4f51f68fa2a3 cmd=node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs
- GATE-RUN runId=76c56587-fd5f-4858-bb60-b4138acce4e3 cmd=node .scratch/t361-review/probe-truth.mjs
- GATE-RUN runId=6ad85866-da35-47cf-9ea5-c113caf4cf1a cmd=node .scratch/t361-review/probe-emptyrow.mjs
- GATE-RUN runId=981782dd-ecb3-429d-964f-ff0bfb92cb6f cmd=node .scratch/t361-review/probe-emptyrow2.mjs
- GATE-RUN runId=d04a54c2-9bce-4236-9597-8598e6e74864 cmd=node .scratch/t361-review/mut-battery.mjs
- GATE-RUN runId=a1730bdd-de78-4460-88c0-c2bdeace09c7 cmd=node .scratch/t361-review/mut-battery.mjs
- GATE-RUN runId=7070650f-0dbd-409e-ae50-775c30509bbe cmd=node .scratch/t361-review/mut-battery.mjs
- GATE-RUN runId=e1383819-eba2-42ef-ab5f-e2ab6eda0bab cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie --force

对账源（受跟踪·本件窗口 `2026-09-14T12:43:00Z .. 12:51:30Z`，取自 `.scratch/locks/gate-runs.log`）：

```text
RUN ticket=361 runId=36fb7446-7767-47ff-a57a-c55d20e8af2f cmd="npx tsc -b packages/skill-calorie" waitedMs=40033 exit=0 pid=38020 at=2026-09-14T12:43:21.869Z
RUN ticket=361 runId=cdf99abb-b06c-457c-bbc3-4f51f68fa2a3 cmd="node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs" waitedMs=0 exit=0 pid=35004 at=2026-09-14T12:43:30.285Z
RUN ticket=361 runId=76c56587-fd5f-4858-bb60-b4138acce4e3 cmd="node .scratch/t361-review/probe-truth.mjs" waitedMs=1 exit=0 pid=49360 at=2026-09-14T12:45:22.995Z
RUN ticket=361 runId=6ad85866-da35-47cf-9ea5-c113caf4cf1a cmd="node .scratch/t361-review/probe-emptyrow.mjs" waitedMs=0 exit=0 pid=24156 at=2026-09-14T12:45:36.151Z
RUN ticket=361 runId=981782dd-ecb3-429d-964f-ff0bfb92cb6f cmd="node .scratch/t361-review/probe-emptyrow2.mjs" waitedMs=0 exit=0 pid=43212 at=2026-09-14T12:45:54.347Z
RUN ticket=361 runId=d04a54c2-9bce-4236-9597-8598e6e74864 cmd="node .scratch/t361-review/mut-battery.mjs" waitedMs=0 exit=0 pid=45748 at=2026-09-14T12:46:31.647Z
RUN ticket=361 runId=a1730bdd-de78-4460-88c0-c2bdeace09c7 cmd="node .scratch/t361-review/mut-battery.mjs" waitedMs=0 exit=0 pid=52672 at=2026-09-14T12:48:00.948Z
RUN ticket=361 runId=7070650f-0dbd-409e-ae50-775c30509bbe cmd="node .scratch/t361-review/mut-battery.mjs" waitedMs=0 exit=0 pid=13600 at=2026-09-14T12:49:42.635Z
RUN ticket=361 runId=e1383819-eba2-42ef-ab5f-e2ab6eda0bab cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force" waitedMs=0 exit=0 pid=30564 at=2026-09-14T12:50:55.825Z
```

**对账命令与读数**（未用任何 `--allow-*`）：

```text
node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t361-复核.md \
  --log .scratch/locks/gate-runs.log --ticket 361 --since 2026-09-14T12:43:00Z --until 2026-09-14T12:51:30Z
RESULT: matched=9/9 auditEntries=9 scoped=9 undeclared=0
gate-audit: PASS
```

**作者声称的对账（原样复跑）**：`--evidence docs/skills/skill-calorie/t361-实施证据.md --log docs/skills/skill-calorie/t361-gate-runs.log --ticket 361 --since 2026-09-14T12:06:00Z --until 2026-09-14T12:34:00Z`
→ `RESULT: matched=3/3 auditEntries=3 scoped=3 undeclared=0` ＋ `gate-audit: PASS`（`exit=0`）。**声称复现成立。**

**导出件是否忠实**：把 `docs/skills/skill-calorie/t361-gate-runs.log` 的 3 行与实况日志窗口内同票 `RUN` 行逐字比对 →
`live=3 export=3`、`Compare-Object` 无差异 ⇒ **逐字一致**。窗口内实况共 6 行（3 `START`＋3 `RUN`），`RUN` 三条全部被声明；
作者另记的 `exit=1` 条目 `runId=0dae8d1b…` 实况为 `at=2026-09-14T12:05:57Z exit=1`，**确在窗口之外**，未被充作门禁绿 ⇒ 反向对账「记录里没有被漏声明的」成立。

**本席自我更正（留痕，不静默改数）**：首次自设变异电池（`runId=d04a54c2…`）得「变异未红」——
原因是电池内以 `spawnSync('npx', …)` 调编译器，Windows 下 `.cmd` 垫片不可直执行，`compile_exit=null`，
测试跑的是**未重建的旧 dist** ⇒ 假绿。改直调 `node node_modules/typescript/bin/tsc`
（`a1730bdd…`／`7070650f…` 两次有效）后变异如实变红。该条假绿**作废并如实记账**。
同一教训也用于作者的自证面：本席另跑 `tsc -b … --force`（`e1383819…`）排除「增量空转＝假绿」。

## 3 · 票面与融合验收逐条（自建现场）

**现场**：`calorie-cmd-read`（PATH 垫片 `D:\2Study\nodejs\calorie-cmd-read.cmd` → `node_modules/skill-calorie` 连结 →
`D:\ilife\packages\skill-calorie\dist\cli\cmd_read.js`），`SKILLS_DB_PATH` 指自建 `mkdtemp` 临时库；
自选 4 行种子（只填 1 项／13 项全填／只填 1 项＋转义备注／7 填 6 缺）——与本席自写列序表、与作者冻结种子均不同。

| 读数 | 值 | 对应判据 |
|---|---|---|
| `CLI exit=0`（不带 `metric`，`--params '{"days":90}'`） | 0 | 票面「列表页可见文本」前件 |
| `R361-A1-NAMES hit=13/13` | 13/13 | **票面验收** |
| `R361-A1B-HEAD` ＝ `["日期","胸围","腰围","腹围","臀围","左大腿","右大腿","左小腿","右小腿","左上臂","右上臂","左前臂","右前臂","肩宽","备注"]`（15 列） | 与自写序逐字相同 | 票面「13 列」＋列序 |
| `R361-A2-DASH-CELLS count=30 expect=30 bad=0` | 30/30 | 裁定 2 判据一 |
| `R361-A2B-CONSEC-EMPTY count=0` | 0 | 裁定 2「不出现连续两个空单元」 |
| `R361-A3CARD mismatch=0 leak=0 filled=22`；卡片 4 张＝宽表 4 行；卡片值＝宽表同格值 | 全 0 | §二-10「二者同一份数据」 |
| `R361-A4B null=30 absent=0 bad=0 dash_in_measure_cells=false`（13 字段×4 行逐字段核） | 保真 | 裁定 2 判据二（真库 `null` 形态） |
| `R361-A5-SYNTH absent_keys=13/13 dash_in_cells=false table_dash=13 card_empty=true` | 成立 | 裁定 2 判据二（**字段缺位**形态，本席自写复现） |
| `R361-A6-KPI`：`胸围趋势`＋`<svg>`＋`趋势点 2 天`／`均值 89cm`／`最小 88.1cm`／`最大 89.9cm`／`变化量 +1.8cm`／`最新 89.9cm` | 全中 | #360 闸门在本票全量分支下仍按手算渲染 |
| 不带 `--params` | `exit=0` 且 15 列表 | 补测 |
| 空库 | `exit=4`、`stdout` 0 字节 | 空库语义不变 |

**期望值来源**：13 个部位名与本席自写列序表，取自源码 `packages/skill-calorie/src/fetch/body.ts:25-32`
（`MEASUREMENT_FIELDS`，本席逐行抄录后与 dist 导出的同名单比对一致）；中文名取自 `bodyDocs.ts:106-111 MEASURE_ZH`，
并与老正本 `scripts/render_body_measurements_view.py` 的 `METRIC_LABELS` 对照（12／13 同字，唯一差异见 §7-3）。

**裁定 2 两条分开**：同一页同时满足「①可见文本缺值格 `—`」与「②payload 逐字段保留原始空值、13 个围度字段无一被写成 `—`」，
两者不互染（`A2` 与 `A4`／`A5` 分别成立）。

## 4 · 自设新变异（作者未覆盖）

持锁窗口内直调，逐文件点名复原（只改 `src/body/bodyDocs.ts` 一件）：

| 相位 | 变异 | 作者测试 | 本席探针 |
|---|---|---|---|
| P0 baseline | 当刻内容 | `test_exit=0 pass=8 fail=0` | `probe_exit=0 RESULT: PASS` |
| P1 **列序错位**：`MEASURE_FIELDS` 整体倒序 | `test_exit=1 pass=7 fail=1`（`表头列序应与既有列序一致`） | `probe_exit=1 RESULT: FAIL fails=2`（表头倒序；卡片逐格对照 mismatch=2） |
| 还原 | 逐字节写回当刻原文 | `test_exit=0 pass=8 fail=0` | `probe_exit=0 RESULT: PASS` |
| P2 **卡片异源**：卡片只喂 `v.items.slice(0,1)` | `test_exit=1 pass=7 fail=1`（窄屏卡同源用例） | `probe_exit=1 RESULT: FAIL fails=3`（卡片数 1≠4） |
| 还原 | 同上 | `test_exit=0 pass=8 fail=0` | `probe_exit=0 RESULT: PASS` |
| 还原一致性 | `RESTORE-CHECK … sha=902b715b…01a1 expect=902b715b…01a1 equal=true` | 重编 `compile_exit=0` | `RESULT: PASS restore_clean=true` |

两行机器读数：

```
MUT-RED    mut1-order test_exit=1 pass=7 fail=1 ／ probe_exit=1 RESULT: FAIL
MUT-GREEN  restore   test_exit=0 pass=8 fail=0 ／ probe_exit=0 RESULT: PASS ／ source_sha_equal=true
```

释放后独立核：`git hash-object` ＝ `060742af…` ＝ `0de70cb` 内 blob；源码 `sha256=902b715b…01a1`；
声明路径 `git status --porcelain` 为空 ⇒ 窗口内自变更**清零**。

## 5 · 窄屏卡片 ＋「全空行真库可达性」复验（作者声明不成立）

作者声称：全空行「真库不可达（库触发器 `src/schema.ts:144-152`）」，故判据 5 只走合成视图。
本席自造三条路径实测（`probe-emptyrow.mjs`／`probe-emptyrow2.mjs`）：

| 路径 | 读数 |
|---|---|
| ① 直接 `INSERT` 13 项全 `NULL` | `EMPTYROW-INSERT blocked=true`，`msg=body_measurements 需要 date + 至少 1 个围度` ⇒ **触发器栏住 INSERT**（该半句为真） |
| ② 先插一行（单填腰围）再 `UPDATE` 13 项全 `NULL` | `EMPTYROW-UPDATE ok=true stillNonNull=0` ⇒ **UPDATE 无触发器覆盖，全空行真库可构造** |
| ③ 库里**只有**该全空行 → 真出口 | `exit=4`（自动挑部位 `latestMeasurementMetric` 返 `null` → `missing-data`，`bodyPlate.ts:200`） |
| ④ 全空行**与一条有值行共存** → 真出口 | `exit=0`；`整页含「未填围度」=true`；该行表体 15 格中 `—` 格 13；卡片区含「未填围度」 |

⇒ 结论：**「全空行真库不可达」的表述过强**，触发器只封 `INSERT` 一条路；把全空行与任意有值行并存，
判据 5 完全能走真出口。**行为本身正确**（本席正是用真出口验到了「未填围度」＋13 个 `—` 格），故不构成 S1；
属**证据口径不准＋一处真出口覆盖被误判为不可达** ⇒ S3 记账（见 §7-1）。

## 6 · 归属与结构

1. **他席红（`galleryDoc.ts` TS2339）非本票引入**：`0de70cb` 的 `--name-only` 只两件，`photo/` 零改；
   该报错行的类型来源在 `packages/skill-calorie/src/photo/galleryDoc.ts`（工作区 `M`，属 #341 在途：`3526a5f`／`db329e3` 两条 #341 提交）。
   本席另跑**强制**全量编译 `tsc -b packages/skill-calorie --force`（`e1383819…`，`exit=0`、无 `error TS`）
   ⇒ 该红**当刻已收敛**，不是本票带入。
2. **#360 趋势闸门未被本票改动**：本票**删掉的行**只有 2 条旧注释 ＋ 旧「日期＋胸／腰／腹／臀」6 列表的 9 行；
   `if (v.metric)` 单部位分支、KPI 网格、图区／空态分支**一行未删未改**；本席另在自己的种子上重算 KPI（§3 `A6`）逐项吻合。
3. **部位名单一来源**：
   - **列序**：唯一权威 `src/fetch/body.ts:25-32 MEASUREMENT_FIELDS`，本票以 `bodyDocs.ts:115 MEASURE_FIELDS` **直引**（未复制第二份序）⇒ 这一条**是单一来源**。
   - **中文名**：新侧**四份**表并存——`src/body/bodyDocs.ts:106-111`、`src/body/compare.ts:39-44`、
     `src/body/wizardPlate.ts:49-53`（`WIZARD_MEASURE_LABELS`）、`src/analysis/cross.ts:138`；
     另有 `src/body/log.ts:46` 的「CLI 参数名→库列名」第五份映射 ⇒ **这一条不是单一来源**。
   - **两套词**：`肩宽`＝`bodyDocs.ts:110`／`compare.ts:43`／`test/t360-measure-trend.test.mjs:224`；
     `肩围`＝`wizardPlate.ts:50`／`analysis/cross.ts:138`／唤醒词正本 `src/triggers/scene-08-body.ts:7`、`:9`
     与 `src/triggers/wake-assets.ts:2653`、`:2673`（用户可见 prompt 写「肩围(cm)」）。
4. **行数告警线**：`bodyDocs.ts` LF＝**255**（节点口径 `readFileSync(f,'utf8').split('\n').length - 1`）＜350 ⇒ 未超线，无需必报五步第四步。

## 7 · 缺陷清单（分级／归属）

1. **【本票引入 · S3】「全空行真库不可达」不成立**：`src/schema.ts:144-152` 的触发器只挂 `BEFORE INSERT`；
   本席以 `UPDATE` 造出全空行并**用真出口**跑出「未填围度」（§5）。影响：作者证据 §3 判据 5 与测试头注
   （`test/t361-measure-full-table.test.mjs:12-13`）的「不可达」口径不准，且错过一处**可做**的真出口断言。
   不判 FAIL：该分支的行为经本席真出口实测**正确**，票面四项判据与裁定 2 两条均成立。
   建议下一手（不属本票关闭条件）：把判据 5 补成真出口用例（造法见 §5 路径②④），或把注文收窄为
   「仅 `INSERT` 路径不可达」。
2. **【本票范围 · S3】作者测试第 3 例的载荷判据是**全文**代理口径**：`test …:251-252` 用
   `JSON.stringify(items).includes('—') === false`。备注由用户自由填写，备注里出现破折号时该断言**假红**
   （本席种子 `note='d—&'` 即触发；同种子按「字段」口径 `A4B dash_in_measure_cells=false` 为绿）。
   只会假红不会假绿，不影响本票结论；建议收窄为「13 个围度字段逐个不得为 `—`」。
3. **【范围外 · S3／转票】用词与中文名多份表**：新侧 `肩宽`（`bodyDocs.ts:110`、`compare.ts:43`、`t360` 测试）
   与 `肩围`（`wizardPlate.ts:50`、`analysis/cross.ts:138`、唤醒词 `scene-08-body.ts:7/9`、`wake-assets.ts:2653/2673`）
   两套词并存；中文名表四份（§6-3）。
   **编排者已改判：以「肩围」为准**（围度是软尺环绕量；唤醒词 prompt 与预检页已写「肩围」）。
   本票票面判据是「13 个部位名逐个出现」，**用词不在其判据内 ⇒ 不因此 FAIL**。
   转票措辞：请另开一票（建议挂 #366 预检页＋#361/#360 结果页的展示面）统一「肩围」，并把四份中文名表收成一处；
   `fetch/body.ts:25-32` 的列序**已是单一来源，不必动**。
4. **【范围外 · S3】并发在途件**：当刻工作区仍有他席未提交改动（`src/photo/galleryDoc.ts`、`src/photo/photoThumb.ts`、
   `src/fetch/body.ts` 等，属 #341／#398 在途）。合并点安静态复核时须待这些在途件落定或移出。
5. **【记账】本席不作数的读数**：`d04a54c2`（电池首跑，`npx` 垫片未执行致 `compile_exit=null` 的假绿）已作废，
   有效读数为 `a1730bdd`／`7070650f`。

## 8 · 未做项与下一手缺什么

1. **未 push、未关票**（按派单：sha 交编排者统一 push）。票面仍 OPEN。
2. **复核脚本未入仓**：派单红线限定本席只新增 `t361-复核.md` 一件，故可复跑脚本
   （`probe-truth.mjs`／`probe-emptyrow.mjs`／`probe-emptyrow2.mjs`／`mut-battery.mjs`）**只存在于**
   `.scratch/t361-review/`（草稿，不进版本库）——与协议 §5.1「可复跑脚本随证据一起入仓」有出入，
   本席按派单执行并留痕；若要入仓 `docs/skills/skill-calorie/t361-复核-探针.mjs`（有 `t394-review-probe.mjs` 先例），
   请编排者具名授权后另开一笔提交。
3. **#362 可开条件**：#362（看体脂窗口不再静默截断）的硬前置是 **#398**（`source=all` 分组）。
   实况：#398 的实现与证据**已入库**（`f12ebc7`＋`f4d7d20`／`5f5b085`／`f012320`／`80b0c04`，证据件
   `docs/skills/skill-calorie/t398-按来源分组取数-证据.md`＋`t398-gate-runs.log`），但 `gh` 查得 **#398 票面仍 OPEN**。
   ⇒ 建议：等 #398 关闭（或编排者书面按入库态放行）后再开 #362；#360 已 CLOSED、#361 待本席回执后由编排者改判。
