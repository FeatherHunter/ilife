# t588 证据（场景02饮食 · D2 残留收口：三件未进写集 ＋ t511 ＋ 点修 ＋ 门禁 83 清零）

> **本件取代 #587 缺失的 `t587-证据.md`**：#587 落三笔源码提交（`096496b`／`ec7d490`／`e67df73`）＋墙重出
> （`bcbeeb8`）后死在证据段，未留证据件；总工量现场定位的 11 件残留（全部来自三件未进 #587 写集的文件）
> 由本票收完，证据落本件。

## 一、回执四段

**第一行：PASS。** 票面目标全达（门禁 83 件非脚注零命中、票面 8 件族测试除两条点名他席红外全绿、
场景10 回归 12 件里 11 件全绿＋该件 6 页契约件 13/13 绿、墙重出 `RESTAGE-OK`、变异两行红绿）；
余下红均逐条点名在第四段。

**第二段：提交（5 笔，逐笔 `git diff --cached --name-only` 复核只含本席件）＋ GATE-RUN（一律
`node tooling/run-locked.mjs --ticket 591 -- <命令>`）**

| # | commit | 内容 |
|---|---|---|
| 1 | `c6bea9c` | 饮食总览 8 页去分隔符（页头两处＋餐别口径＋末行拆句）＋两条断言跟改 |
| 2 | `3322375` | 餐别分布结论句拆两句（去分号）＋两条断言跟改 |
| 3 | `7c7db05` | 营养分析与六因素两页页级去分隔符（裁定1，模块常量不碰）＋两条断言跟改 |
| 4 | `18a62c4` | 回执新增类印库里真值（裁定4）＋预检失败原因换中文标签（裁定5）＋t496 陈旧断言跟改 |
| 5 | `f3b73c2` | t271 餐别支页名断言跟改现行形状 |
| 6 | `d68616c` | 场景02墙重出 83 件＋两墙／总索引／链路总表（85 文件 161 行；`manifest.json` 只改 `readings.字节区间` 一行） |

| 趟 | runId | RESULT／退出码 |
|---|---|---|
| 编译（终态）`tsc -b packages/skill-calorie --force` | `3d54bd04-08c4-4c5c-b3ea-361fa90954d0` | exit 0 |
| 票面 8 件族测试 | `be5b1962-0a6a-447f-808f-8006456d85e7` | exit 1 → **269 测／267 绿／2 红**（两红＝`t275 ①` 归 #560、`t275 ⑥` 归 #399） |
| 场景10 回归 12 件 `analysis-*.test.mjs` | `4d66f221-46fb-4100-89ad-c09e2f7bfd6b` | exit 1 → **82 测／79 绿／3 红**（三红全在 `analysis-t5.test.mjs`，他席陈旧断言，见第四段） |
| 同上单跑 `analysis-t5.test.mjs`（复现） | `f787b987-0032-480e-8177-854e737859fc` | exit 1 → 13 测／10 绿／3 红（同三条） |
| `trend-misc-port-113.test.mjs`（#113 六页契约） | `0eaf54e4-04d1-4d6b-81c1-8f28ad5b7ea8` | exit 0 → 13/13 绿 |
| `t270`＋`t277`＋`t496` | `69744483-205f-40eb-bb72-ece0cabed951` | exit 0 → 62/62 绿 |
| `diet-list-t271.test.mjs`（跟改后） | `ab272a38-08e4-4e39-862d-781f50da7271` | exit 0 → 6/6 绿 |
| 墙重出 `scene02-验收墙/restage.mjs` | `c28ae7c3-15df-4c28-9d65-fe11082267d7` | exit 0 → `RESTAGE-OK 83 件 · 两墙 · 总索引 · 链路总表 · 正反自检全过` |
| 门禁 83（`.scratch/t588/gate83.mjs`，逐件点名 `audit-separators.mjs`） | `cb01dc1b-522a-47b7-a42f-543b08eb62f6` | 我的门禁脚本 exit 0 → `GATE83-PASS`（83/83 非脚注零命中；被审门脚本自身 exit 1＝脚注行按设计仍算债） |
| 变异自证 `.scratch/t588/mut-red.mjs` | `dc29ab14-82c0-491d-a13c-a3ff97f8b872` | exit 0 → 红／绿两行读数（见第四段） |

**第三段：逐条路径**

- 源码（5 件，全在票面只写清单内）：`packages/skill-calorie/src/render/dietDocs.ts`（LF 350，未越 350 告警线）、
  `src/diet/review.ts`、`src/render/trendMiscPortDocs.ts`（LF **608**，与包台账「当场实测」列一致、模块常量一行未动）、
  `src/diet/receipt.ts`、`src/diet/precheckPort.ts`。
- 测试（5 件，全在票面只写清单内）：`test/t496-文案统一.test.mjs`、`test/t511-清尾二.test.mjs`、
  `test/diet-homogeneity-108.test.mjs`、`test/diet-list-t271.test.mjs`、`test/t273-review-meal.test.mjs`。
- 墙（票面只写清单内）：`docs/skills/skill-calorie/scene02-验收墙/**` —— 83 件产物重出 ＋ 手机墙／桌面墙／总索引／
  链路总表（提交 `d68616c`，85 文件 161 行）；`manifest.json` 的 diff 实测**只动 `readings.字节区间` 一行**
  （`70,974 – 169,101 B` → `70,915 – 169,127 B`，中位不变）。墙面抽查（`.scratch/t588/checkwall.mjs`，逐条 HIT）：
  `看本周饮食.html` 的三处（`<title>卡路里饮食</title>`／眉标 `卡路里饮食`／`加餐时段：下午茶和夜宵`）、
  `看营养分析.html`（`<title>卡路里 趋势</title>`＋眉标 `趋势分析`＋`营养分析 2026-09-09 至 2026-09-15`＋
  `（建议 10% 至 20%）`）、`看每日六因素.html`（`这 6 项达标了几项（热量按千卡计）`）、
  `看全部餐别分布（最近 7 天）.html`（`最近 7 天共 28 餐。加餐热量占比最高…`）、
  `记一餐.html`（`碳水 … 0`／`克数 … 100`）、`看批量导入预览.html`／`校验批量导入.html`（`缺少必填字段: 脂肪 (g)`）。
- 本件：`docs/skills/skill-calorie/t588-证据.md`。
- 草稿（不入仓）：`.scratch/t588/**`（`probe.mjs`／`gate83.mjs`／`mut-red.mjs`／`probe-reason.mjs`／`peek*.mjs`／
  各类日志与 JSON 读数）。

**第四段：没做完啥 ＋ 下一手缺啥（含他席红点名）**

- **他席红 ①（票面点名，不代修）**：`t275 ①`「看饮食总览 缺来源脚注：数据来源 · 饮食记录 · …」归 **#560**；
  `t275 ⑥`「复制日志按钮有日志了——接线／装配已变」归 **#399**。
- **他席红 ②（场景10 那三条）**：`analysis-t5.test.mjs` 三条陈旧断言 —— ①`:168` 期望 `/startA/`，现消息是
  「自定义两段时间需要四个月日期（起／止 × 两段）」（源侧 `14eb13e` #481，2026-09-15 02:10 改词）；
  ②`:181` 期望 `平台期首日`，现产 `平台期第一天`（源侧 `b56771e` #154，2026-09-15 08:33）；
  ③`:232` 期望 `forecast.points.length === 13`，现产 14（模拟域）。测试件自 `a39b86a`（2026-09-13）未同步
  ⇒ 与本票写集无交集（`analysis-t5.test.mjs` 不在本票只写清单内，未动）。
- **他席红 ③（告警线台账，进场前已红）**：`check-warning-line.mjs` 当场 `RESULT: 87/91`、exit 1 —— 四条陈化行
  （`src/diet/nutritionPortDocs.ts` 台账 745／实况 750、`src/diet/libraryDocs.ts` 407／408、
  `src/diet/receipt.ts` 398／399、`src/diet/reviewDocs.ts` 359／362）本席进场前即如此（`bcbeeb8` 那批）。
  本席因裁定4 让 `receipt.ts` 涨到 425（陈化差变大，**未新增红行**）；修法是 `--sync`，而
  `packages/skill-calorie/AGENTS.md` 不在本票只写清单内 ⇒ 未动，请总工收口时一并 `--sync`。
- **没做完（明确不在票面范围、未动）**：多行写入那两页（`复制昨日饮食`／`批量补记饮食`）的「字段变更」表仍是
  9 行 `—` —— 多行写入没有单行 id，逐字段的「本次值」不存在；裁定4 点名的是「记一餐」（单条写入）已印真值。
  若要一并定形状，建议另立票（house 先例：`weight/logReceipt.ts` 的批量回执不出逐字段值表，只出逐条明细）。
- **下一手**：本席不推、不 close；墙由总工统一收。注意本席重出时刻＝runId `c28ae7c3`（2026-09-16 13:1x），
  其后他席又落了 `8920faa`（#567 公共层清账）／`09f8f68`（#589 目标页）；终审 vision 打分前若要墙跟最新公共层，
  请再重出一趟。

## 二、总工程师五条裁定 · 逐条落地

1. **`trendMiscPortDocs.ts` 只改两个函数体内部（页级覆盖）** —— 落法：给 `assembleDocPage` 传**页级**
   `docTitle: '卡路里 趋势'`／`eyebrow: '趋势分析'`；页内「~」改「至」；「（建议 10~20%）」改「（建议 10% 至 20%）」
   （三档同）；R3+R5 副题与 R2+R5 口径行拆句去顿号／分号。
   **边界自证**：`git show 7c7db05 -- <件>` 的四个 hunk 分别是 `@@ -332,11`／`@@ -382,7`／`@@ -406,12`
   （全在 `buildNutritionAnalysisDoc`，当刻 330–418 行）与 `@@ -473,14`（`buildSixFactorsDoc`，442–487 行）
   —— **模块常量 `DOC_TITLE`（:41）与其余五个函数（:123／:226／:491／:537／:569）一行未碰**；件 LF 仍 608。
   **场景10 未脏**：`trend-misc-port-113.test.mjs` 13/13 绿（该件 6 页契约）；场景10 12 件回归里 11 件全绿
   （红 3 条全在体重对比／模拟域的 `analysis-t5.test.mjs`，见第四段）；两页以外页产物无改动来源。
2. **`t511-60（工作日平均）` 期望改现行四卡口径** —— 已由 #587 的 `096496b` 落（`want: ['总热量','日均热量','总蛋白','日均蛋白']`、
   `gone: ['周末/工作日','均值（卡）']`）；本席复跑 `t511-清尾二.test.mjs` 全绿（读票面 8 件那一趟 runId `be5b1962`）。
3. **`t511-④ h1Of` 改从 `<h1 class="ilife-block-page-shell-title">` 取 innerText** —— 已由 `096496b` 落；
   本席复跑读数：`READING #511 营养素深度／明细 默认入口标题「🧪 营养素深度」／带标记入口标题「🧪 营养素明细」`
   （取到的是 H1 而不是 meta 行），同趟的 `#511 ④` 变异自证（把标题改回旧写法 → 该断言必红）通过。
4. **记一餐「字段变更」9 行印真值** —— `receipt.ts` 新增 `writtenRowOf`／`createdValueOf`：单条写入的写口报了
   `recordId` 与写入日期，就按 `listMeals(db, 日期)` 读回那一行逐字段印**库里的真值**（写口填的缺省 0／100 也是
   真写进去的值），读不回那一行才退本次参数。实测 before／after（`.scratch/t588/probe-write.log`，runId `5893b5cd`）：

   | 行 | 改前 | 改后 |
   |---|---|---|
   | 食物名／热量／蛋白质 | 鸡胸／200／35 | 鸡胸／200／35 |
   | 碳水／脂肪／克数 | — ／ — ／ — | **0 ／ 0 ／ 100** |
   | 备注／日期／时间 | — ／ — ／ — | —（真空）／**2026-09-16**／**12:57:59** |

   ⇒ 9 行里 8 行印真值，唯一 `—` 是本次确实没写的备注（缺值口径不乱编）。
5. **预检失败原因里的裸字段名 → 中文标签** —— `precheckPort.ts` 新增 `REASON_LABELS`（八个标签取本件既有的
   `LABEL_FIELDS`，另补 `dietary_fiber`／`product_name`／`source`）＋ `humanReason()`（只换字段名那一处，
   消息本体措辞一字不动）。四形状实测（`.scratch/t588/probe-reason.log`，runId `74d3ca65`）：

   | 校验器原消息 | 页上现读 |
   |---|---|
   | `缺少必填字段: fat` | `缺少必填字段: 脂肪 (g)` |
   | `product_name 必须是非空字符串` | `食品名 必须是非空字符串` |
   | `source 必须是非空字符串(…“未知”)` | `数据来源 必须是非空字符串(…“未知”)` |
   | `carbohydrates 必须是数字` | `碳水 (g) 必须是数字` |

## 三、门禁 83 读数（`.scratch/t588/gate83-summary.json`）

- 83 件产物逐件点名 `audit-separators.mjs`：**非脚注零命中 = 83/83**（`bad: []`）。
- 全节点命中 66 处，**全部**是来源脚注行（`ilife-block-caliber` 的 `📊 数据来源 · …`）⇒ 归 #560，单列计数 **66**。
- `totals`：`R1 66 ／ R2 0 ／ R3 52 ／ R4 0 ／ R5 0 ／ R6 0 ／ R7 0` —— 扣掉脚注后，`；`（R2）／`｜`（R4）／
  `、`（R5）／`~`（R6）／机器标识（R7）在本批可见文本里**全零**。
- 收口前后：#587 读数 **72/83**（11 件残留，`.scratch/t587/gate83-summary.json`）→ 本席 **83/83**（本文件同名 JSON）。
  11 件残留逐条落地：8 张条目列表页（`dietDocs.ts` 4 处）／1 张餐别分布页（`review.ts` 的 `；`）／
  看营养分析 9 命中与看每日六因素 3 命中（`trendMiscPortDocs.ts` 两函数体内部）。

## 四、变异自证（两行机器读数）

改坏的那一处＝本票收掉的债本身（`dietDocs.ts` 的 `MEAL_NOTE` 由「加餐时段：下午茶和夜宵」改回
「加餐时段：下午茶、夜宵」），改坏与还原各真跑一趟 `tsc` ＋ 8 张饮食总览页 ＋ 同一门禁：

```
MUT-RED   auditExit=1 零命中页=0/8  节点命中=16 R5=16 R2=0
MUT-GREEN auditExit=0 零命中页=8/8  节点命中=0  R5=0  R2=0
MUT-VERIFY ok（源已逐字节还原）
```

（16 处＝8 页 × 该常量在图下与页末各一处；脚本 `.scratch/t588/mut-red.mjs`，runId `dc29ab14`。）

## 五、日志与读数落盘（`.scratch/t588/`）

`tsc.log`／`tsc2.log`／`tsc3.log`／`tsc4.log`（编译四趟）、`ticket8.log`（跟改前 269 测 266 绿）、
`ticket8b.log`（跟改后 269 测 267 绿）、`analysis.log`／`analysis-t5.log`、`trend-misc-113.log`、
`t270-t277-t496b.log`、`t271.log`、`probe-diet.log`／`probe-trend.log`／`probe-write.log`／`probe-reason.log`、
`restage.log`、`gate83.log`／`gate83.json`／`gate83-summary.json`、`mut.log`／`mut-gate-red.json`／`mut-gate-green.json`。
