# t626 预检页自定义窗唤醒词回落证据（场景 04 运动 · 地图 #156）

目标：预检确认页在自定义窗（start/end 直调、无 window）下，页内唤醒词一行误标为「看今日运动（vs 目标）」，而页显窗口是自定义日期段。本票二选一后回落，票面两条词行为不动。

票面：`gh issue view 626` 可读（标题「场景04·预检页自定义窗唤醒词回落口径（622复核S3）」，0 条评论，Part of #156，#622 独立复核 S3 转票，探针 P622-REV1）。读不到的情形未发生。

前置点名（`git show --stat 07bf414`，不猜文件名）：
- `packages/skill-calorie/src/exercise/goal.ts`（57 行，改无目标分支）
- `packages/skill-calorie/src/exercise/precheck.ts`（150 行，新预检确认页装配）
- 同提交另有 `packages/skill-calorie/test/exercise-goal-precheck-622.test.mjs`、`docs/skills/skill-calorie/t622-目标预检-证据.md`、`packages/skill-calorie/SKILL.md`，本票只动前两件中的唤醒词行与测试追加。

开工他席在途件（`git status --short`）：
- 开工第一查：空（干净）。
- 中途一查见 `M docs/skills/skill-calorie/t625-第四轮-证据.md`（他席在途，本票一字未碰）。
- 提交前复查：仅本票 3 件（见下「交付路径」），他席件已不在工作区。

## 选型

选：隐去唤醒词行。

理由一句话：自定义窗是 start/end 直调、无 window，没有经过任何一条唤醒词，页上硬写一句就是编造，隐去最诚实。

说明：今日／本周两条词走 window 参数，唤醒词行保留原文；仅无 window（含 `window: custom`）时 `wakeWord` 取空，预检确认页的唤醒词卡与现值表唤醒词行两处一并跳过，窗口、窗口天数、现值缺席说明、目标值输入指引、确认后复制指令、复制区三通道、metrics 口径一字不动。

## 改动（只写声明路径）

- `packages/skill-calorie/src/exercise/goal.ts`：取出 `windowParam`，`windowParam === undefined || windowParam === 'custom'` 时 `wakeWord` 为空，否则按 `windowLabel` 取两条词原文（两条词行为不动）。
- `packages/skill-calorie/src/exercise/precheck.ts`：`ExerciseGoalPrecheckView.wakeWord` 改为 `string | null`（空即自定义窗，隐去该行），现值表与 KPI 卡两处按空跳过该行。
- `packages/skill-calorie/test/exercise-goal-precheck-622.test.mjs`：只追加一条 t626 自定义窗断言，既有三态一字不动。
- 新本证据件；草稿 `.scratch/t626/`（不管辖）。

## 判据先行（红 → 绿）

- 红：`node tooling/run-locked.mjs --ticket 626 -- node --test packages/skill-calorie/test/exercise-goal-precheck-622.test.mjs`，pass 3 ／ fail 1，点名 `t626 自定义窗应隐去唤醒词行，实测仍有唤醒词行`，见 `.scratch/t626/red-01.log`。
  - `RESULT: ticket=626 runId=240b1728-7e74-4bb0-81e6-ab04582e14ea waitedMs=1 exit=1`
- 绿：同一条靶向命令，pass 4 ／ fail 0（两条词三态不回退＋自定义窗隐去），见 `.scratch/t626/green-03.log`。
  - `RESULT: ticket=626 runId=f5f39f2a-441e-4cd4-8862-72a58209d936 waitedMs=0 exit=0`
- GATE-RUN runId：`f5f39f2a-441e-4cd4-8862-72a58209d936`（还原后复验绿；首次改后绿 `d170b750-cb69-4cc4-8843-6cfedb5942db` 同为 pass 4）。

## 变异（改回误标 → 必红 → 逐字节还原 → 回绿）

- 变异红：把 `src/exercise/goal.ts` 唤醒词行改回误标（`windowLabel === '本周' ? … : '看今日运动（vs 目标）'`），重建（runId `64d39378-4e9b-468b-bc45-d3f9320badbc` exit 0）后同一条靶向命令 pass 3 ／ fail 1，同错 `t626 自定义窗应隐去唤醒词行`，见 `.scratch/t626/mut-red.log`。
  - `RESULT: ticket=626 runId=8906a1c5-3318-4c9e-9390-b8717c81596c waitedMs=0 exit=1`
- 变异绿：从 `.scratch/t626/goal.ts.good` 逐字节还原（`same=True`，SHA256 头 `EC972CA705DA`；`precheck.ts` 同 `same=True`）后重建（`--force` 一次 `49004fbf-8aec-4baf-9922-c9a99c5f583d` exit 0，因增量未跟进；再普通重建 `e69e0b28-3940-426f-8632-954484863bdb` exit 0）回绿 pass 4，见 `.scratch/t626/green-03.log`（runId `f5f39f2a-441e-4cd4-8862-72a58209d936`）。

## 编译与回归（经持锁包装器）

- 编译：`node tooling/run-locked.mjs --ticket 626 -- node node_modules/typescript/bin/tsc -b packages/skill-calorie`，exit 0（runId `a50f43d3-086e-4c26-9058-29c31ffd3f9f`；还原后复建 `e69e0b28-3940-426f-8632-954484863bdb` exit 0）。
- 回归 `exercise-summary-goal-fusion-452.test.mjs`：pass 9 ／ fail 0（runId `ba899464-e3bd-4ef9-9b22-a914a9310a74`），见 `.scratch/t626/regress-452.log`。
- 回归 `exercise-accept-267.test.mjs`：pass 8 ／ fail 0（`T267-RESULT: 8/8`，runId `f9443ea9-cd6c-4a86-8f93-e879a10a1ef0`），见 `.scratch/t626/regress-267.log`。

## 告警线门（只读，未加 `--sync`）

- `node tooling/run-locked.mjs --ticket 626 -- node packages/skill-calorie/scripts/check-warning-line.mjs`：`RESULT: 91/91`、`PASS: 告警线台账齐全且与实况一致`（runId `539356f2-9c77-442d-89de-6a8270ff25aa`），见 `.scratch/t626/gate-warning.log`。
- 本票两件 LF 口径 `precheck.ts` 150 ／ `goal.ts` 58，均未超 350 行告警线，门内无本票件的 OVER 行；红在他席路径定界范围外未出现，本票记 S3 无（不修他人件）。

## 交付路径（提交只含这些）

- 改：`packages/skill-calorie/src/exercise/goal.ts`（唤醒词行回落）
- 改：`packages/skill-calorie/src/exercise/precheck.ts`（唤醒词行按空跳过）
- 改：`packages/skill-calorie/test/exercise-goal-precheck-622.test.mjs`（只追加 t626 自定义窗断言）
- 新：本证据件
- 草稿：`.scratch/t626/`（红绿、变异、编译、回归、门禁日志＋`goal.ts.good`／`precheck.ts.good`）

## 提交

- `git diff --cached --name-only` 复核只含本票 4 条路径后提交，信息 `feat(626): 预检页自定义窗唤醒词回落加证据`；不推送。
