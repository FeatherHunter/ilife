# #320b · 收口证据 2 ——「达成条件 ①②」做绿（残余实施席）

- 席位：**残余实施席**（`#320` 的达成条件 ①②，票面裁定见 `docs/skills/skill-calorie/t293-编排决策-对抗审查.md:105`／`373ff90`）。
- 日期：2026-09-13（UTC 时间以各 run 的 `at=` 为准）。现场：`D:\ilife`，`master`，node v24.x ＋ pwsh 7.6.6。
- 口径：本件只写本席**当刻复算**出来的读数；命令逐条可照抄复现。所有门禁经
  `node tooling/run-locked.mjs --ticket <票号> --lock-dir .scratch/locks-land --stale-minutes 30 -- <命令>` 持锁运行
  （窗口内不嵌套 `run-locked`），留痕 `.scratch/locks-land/gate-runs.log`。
- 依据件（先读后动）：`t320-残余定性.md`（逐处 `file:line`）、`t320-收口-证据.md`（前一席五条绿线当刻读数）、
  `.scratch/t314-319/六票落点规则.md`、`docs/agents/命令登记纪律.md`、`docs/agents/structure.md`。

## §0 现场复算（开工点）

| 项 | 命令 | 读数 |
|---|---|---|
| 提交点 | `git rev-parse HEAD origin/master` | `373ff903cc8101a408a839f31f5599fa270d0ebd`（两者相同，含 `373ff90`） |
| 工作树 | `git status --porcelain -uall` | 空（干净） |
| 判官指纹 | `(Get-FileHash -Algorithm SHA256 'docs/skills/skill-calorie/t293-验收-命令自治.mjs').Hash` | `78AE857426A44D66E81A2489E51D0D2790BD16A0812DB78818B1D2E918BEBE39`（与派单一致，**全程未动判官**） |
| 开门态 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `未搬迁清单 1 条`（＝`legacy/scene-03.ts:12` 那条） |
| 分派面 | `git grep -n -- "case .calorie\." packages/skill-calorie/src/cli` | 4 处：`cmd_read.ts:158` ＋ `write.ts:60／61／63` |

## §1 件 1 · 搬迁 `calorie.view.goal-weight`（纯搬迁；行为不变）

### 1.1 落点：`src/goal/`（不是 `src/weight/`）——由判官口径反推的**机器证据**，不是偏好

先说清裁定过程，因为「键属场景 03＝体重」与「命令事实住它的能力目录」在这一条上会指向两个不同的目录：

- 按 `t313a-分区-证据.md:89`，这条键的**路由场景**是 03（体重）。
- 但判官 `t293-验收-命令自治.mjs` 的锚集是**同源**的：`{总键数, 写键数, 读键数, 体重键数}`（`:590-595`），
  其中「体重键数」＝ `src/weight/commands.ts` 的声明条数（`:274`）；`P4` 更把它写成硬口径
  `wd.reads.length === 5 && wd.writes.length === 4`（`:1279`），`P2` 逐键追的也是这批。
- 本席实测（**先按 weight 落点做了一版**，读数是判据、不是猜测）：
  - 体重能力变 6 读 ＋ 4 写 ⇒ `P4: FAIL`（判官要求 5／4）；锚值 `10` 撞上仓内大量 `10` 字面量
    ⇒ `④ 主体判不出` 3 处 → 17 处、形状守卫命中 ⇒ `P1: PENDING`；
  - `P2: FAIL`（它把「体重 9 键」逐键在 `legacy/scene-NN.ts` 里找残留：本席写在分片**文件头注释**里的
    键名被它按文本命中 ⇒ 报「仍留在未搬迁清单=1」）。
  - 读数：`RESULT: P1=PENDING P2=FAIL P3=PASS P4=FAIL P5=PASS 2/5`（runId `4101606b-1e6c-4462-9599-0a0ecd30e3b1`，票号 `t320b`，**本对账窗口外**，见 §7.2）。
- 判官是冻结件（sha256 见 §0），所以这不是「改判官」能解的事，而是**落点必须让判官口径继续成立**：
  这条键要住 `src/goal/`。语义上同样成立：键族 `calorie.view.goal*` 其余八条全在 `src/goal/commands.ts`，
  写命令 `calorie.goal.weight` 就是同一件「体重目标」的事；`src/goal/commands.ts` 的文件头本来就把
  「看目标＝`calorie.view.goal*` 八条」当作本能力的子功能表。
  （该文件头另记了三条「代表唤醒词落别的键」的**只记录不改**项与编排者裁决，本条按同一口径处理，见 1.3。）
- 落点后判官复跑：`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`（§7）。

### 1.2 改了哪 10 件（`git diff --stat`，无生成物）

```
 packages/skill-calorie/src/cli/cmd_read.ts         |  25 ++--      ← 删死 switch ＋ 清 import
 .../src/cli/legacy/routes/scene-03.ts              |   1 -        ← 定点删 1 条路由记录
 .../src/cli/legacy/routes/scene-06.ts              |   4 +-        ← 定点删 1 条（片内最后一条 ⇒ 空数组）
 .../src/cli/legacy/routes/scene-10.ts              |   4 +-        ← 定点删 1 条（同上）
 packages/skill-calorie/src/cli/legacy/scene-03.ts  |  11 +-       ← 键搬空（只搬空、不删文件）
 packages/skill-calorie/src/cli/write.ts            |  30 ++---     ← 回执端口去 `case`（见 §1.4）
 packages/skill-calorie/src/goal/commands.ts        |  15 ++-       ← 声明 ＋ 文件头记账
 packages/skill-calorie/src/goal/read.ts            |  20 ++-       ← 入口 `viewGoalWeight`（行为逐字照搬）
 packages/skill-calorie/src/goal/routes.ts          |   7 ++        ← 三条路由记录（`list`／`order`／`scene`／`cli` 原值照抄）
 packages/skill-calorie/test/cmd-registry-294.test.mjs | 129 +++---  ← 棘轮切终态（§2）
 10 files changed, 133 insertions(+), 113 deletions(-)
```

**生成物面零 diff**：`keys.ts`／`registry.ts`／`combos.yaml`／`build-help.mjs`／`routes.generated.ts`
`git diff` 全为空（`gen:check` 逐件 `ok` 的 sha256 与开门态一致；`routes.generated.ts` 的
`sha256=e9ea993518ec33e3a69575dac1d8abf4a5e4c12e277b4c2d215ae3e5d339d7e9` 搬迁前后同值 ⇒ 路由记录面逐字节不变）。

### 1.3 `wakeWord` 照抄（`定体重目标`），因此生成物 `REPR` 块与 `SKILL.md` 都不动

判官 `P4`／锚集之外还有一条纪律口径：`命令登记纪律.md` §形状三「拷声明进 `src/<能力>/commands.ts`」。
本席按该文件头已立的裁决（「代表唤醒词属产品内容，本票不替产品定；故一律照抄，留待收口票处置」）
**逐字照抄**旧声明的六字段（`kind`／`key`／`shape`／`title`／`wakeWord`／`example`）⇒
`build-help.mjs` 的 `REPR` 块不需要重写（`gen:check` 的 `ok ...build-help.mjs` 与开门态同值）。
`定体重目标` 在路由表里落的是同名写命令 `calorie.goal.weight`（已有一条同键现成词可换：
`对比体重：当前 vs 目标体重`）——该反向错与 `view.goal`／`view.goal-config`／`view.goal-recommend`
三条同类，逐条记在 `src/goal/commands.ts` 文件头，**本票不改产品内容**。

### 1.4 `write.ts` 的三处 `case` —— 按 ① 的字面口径（`src/cli` 全目录）处理

① 的判据是 `git grep -n "case 'calorie\." -- packages/skill-calorie/src/cli` = 0，而 `write.ts:60／61／63`
三处住 `profileReceiptDoc()`（回执整页端口，不选实现、不取数）。改法＝**只换写法、不换语义**：
`switch (key)` 改成键集查表 ＋ 一次比较（`PROFILE_RECEIPT_KEYS` 常量 ＋ 三元），
`set`／`activity` 仍走 `buildProfileSettingReceiptDoc`、`update` 仍走 `buildProfileUpdateReceiptDoc`，
其余 32 条仍返回 `null` 走 `?? res.html`。

### 1.5 判据读数（件 1）

| 判据 | 命令 | 读数 |
|---|---|---|
| 未搬迁清单归零 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | exit 0；`GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 10 个（…／goal／…）声明 101 条，未搬迁清单 0 条` |
| 分派面无 case | `git grep -n -- "case .calorie\." packages/skill-calorie/src/cli` | **0 命中**（`git grep` exit=1＝无匹配） |
| 路由记录归属 | `git grep -n "goal-weight" -- packages/skill-calorie/src/cli/legacy` | 0 命中（三条记录已搬进 `src/goal/routes.ts`；别人的记录一条未动） |
| 行为不变 | `node --test test/calorie-routing-81.test.mjs`（冻结件） | `8/8 pass`（§7 线 6）；冻结面 `src/triggers/scene-*.ts`／`test/calorie-routing-81.test.mjs` **逐字节未动** |
| 体重面零改动 | `git diff --stat -- packages/skill-calorie/src/weight` | 空（曾试落 weight 的一版已整面回退：`git checkout --` ＋ 删新件） |

## §2 件 2 · 棘轮切终态（`test/cmd-registry-294.test.mjs`）

- **终态断言**（新增，`#320 终态：分派层（src/cli 全目录）一条 case 标签都没有`）：
  扫 `src/cli` **全目录**（含 `legacy/` 与 `legacy/routes/` 的 `.ts`）的 `case 'calorie.…'` 标签集，
  断言为空集；并把两个分派文件单列再断言一次（`DISPATCH_FILES`）。
  **判据自带鉴别力自证**（机比）：同一个扫描器喂一条合成样本
  `switch (key) { case 'calorie.x': return null; default: break; }` ⇒ 必须数出 `['calorie.x']`，
  看不见就报「这条终态断言会假绿」。
- **删除**：两张冻结清单 `FROZEN_READ_CASES`（原 `:45`）／`FROZEN_WRITE_CASES`（原 `:67`）
  与两个上界 `FROZEN_READ_CASES_MAX`／`FROZEN_WRITE_CASES_MAX`（原 `:81／82`），
  连同用它们的「只许从冻结清单里减少」那条棘轮（原 `:92-104`）。
  留下的仍是行数棘轮（`FROZEN_READ_LINES`／`FROZEN_WRITE_LINES`，只许减少）——派单没点它，不动。
- **改写「老路活证」**（原 `:147-158`）：取样键 `calorie.view.body-composition`／`calorie.water.log`
  当刻都已在注册表（`src/body/commands.ts:25`／`src/diet/commands.ts:61`），标题里的「未命中注册表的老键」
  早已不成立。改成 `#320 老路已无活口：未搬迁清单是空集，注册表键集即全量声明`：
  ① `LEGACY_COMMANDS.map(d => d.key)` 必须为空集（老路活口为空的**机比判据**）；
  ② `Object.keys(REGISTRY).sort()` 必须等于 `DECLARED_KEYS.sort()`（101 键闭环：没有键住两处、也没有声明没有定义地）；
  ③ 两个「前任老键」照旧真跑一次读一次写（现在走的都是注册表那条路）。
- **新路活证补一条**：`dispatch('calorie.view.goal-weight', …)` 取到 `metrics.weightGoal === 65`
  且产物是整页（`ilife.html` 壳）——即「补搬的这条照旧走通」。
- 测试条数不变（7 条 → 7 条），全量 1492 不变。
- **变异自证**（票号 `t320-mut`，window runId `ee2039b5-948b-4402-9578-5712610ab227`，输出落 `.scratch/t320b/mut.log`）：
  1. `cmd_read.ts` 塞回一条 `case 'calorie.x': { fail(3, 'x'); }`（`sha256 7EF33346…D380` → `A26B0514…C8A9`）
     ⇒ `node --test packages/skill-calorie/test/cmd-registry-294.test.mjs` **exit=1**，
     失败签名逐字：`AssertionError: 分派层仍有按键分派的 case 分支（#320 起老路应无活口）：calorie.x`
     （`actual: [ 'calorie.x' ]` ／ `expected: []`）。
  2. 删掉（还原到 `sha256 7EF33346…D380`，与变异前同值）⇒ 同一件 **exit=0**，7/7 绿。
  3. 回滚后按规则 38 重建派生面并重签印记：`tsc -b packages/skill-calorie --force` → `gen-cli.mjs --stamp`
     → `gen-cli.mjs --check` **PASS** → 冻结件 `test/calorie-routing-81.test.mjs` **8/8**。

## §3 件 3 · `cmd_read.ts` 落线

| 项 | 命令 | 读数 |
|---|---|---|
| 物理行（节点口径：`split('\n').length - 1`） | `node -e "…"` | **340 行**（改前 351；删掉 `:157-167` 那口 11 行 switch ＋ 三处 import） |
| 告警线 | `docs/skills/skill-calorie/t294-登记与分派接缝.md:112` | 350 行 ⇒ **340 < 350，已落线内**（≤200 不在本票，归 `#330`） |
| 入口名与导出未挪 | `git grep -n "export function dispatch" packages/skill-calorie/src/cli/cmd_read.ts` | 仍在原件原名（约 28 件 spawn `dist/cli/cmd_read.js`、约 10 件 `import { dispatch }`） |
| `write.ts` | 同上量法 | 99 行（改前 95，＋4 行注释／常量） |

## §4 件 4 · 五条绿线（沿票面顺序复跑；一条命令一个 runId）

见 §7.1 的 `GATE-RUN` 声明与读数；汇总：

| 线 | 命令 | 读数 |
|---|---|---|
| 1 | `node node_modules/typescript/bin/tsc -b` | exit 0（无输出＝无类型错） |
| 2 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | exit 0；`未搬迁清单 0 条`；5 件生成物逐件 `ok` |
| 3 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | exit 0；`P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`；`UNACCOUNTED=0 处` |
| 4 | `node --test test/*.test.mjs packages/*/test/*.test.mjs` | exit 0；`tests 1492 ／ pass 1492 ／ fail 0` |
| 5 | `node tooling/skill-html-snapshot.mjs --check` | exit 0；`OK: 5 技能 HTML 快照 == 实际（186 件产物，base-* 指纹 ef7cc02969e37e4bfc0eb35751b8dc1c）` |
| 附 | `node --test test/calorie-routing-81.test.mjs` | exit 0；`tests 8 ／ pass 8 ／ fail 0`（冻结件） |

## §5 偏离与记账（逐条）

1. **落点＝`src/goal/` 而非 `src/weight/`**（键的路由场景是 03）。理由＝§1.1 的判官机器口径（判官冻结，
   sha256 见 §0）；若不这样落，`P1`／`P2`／`P4` 三条绿线当刻就会红。这不是「挑一个方便的口径」：
   反证读数（`P1=PENDING P2=FAIL P4=FAIL`）在 §1.1 里按 runId 留痕。
2. **`write.ts` 三处 `case` 去 `case` 写法**（回执端口，非分派分支）：① 的字面判据是「`src/cli` 全目录 0 命中」，
   故按「只换写法、不换语义」处理；语义等价由 §7 的全量测试（1492/1492，含 `#179` 三条写词的整页回执断言）
   与 `write.ts` 的行数棘轮共同守。
3. **`legacy/scene-03.ts` 文件头不写键名**：`P2` 按**文本**在 `legacy/scene-NN.ts` 里找已搬迁键，
   写进去会被判「仍留在未搬迁清单」。故文件头只说「本片最后一条（**体重目标**）」，
   键的可追处＝`src/goal/commands.ts` 与 §1 的本件。
4. **棘轮件留了行数上界**（`FROZEN_READ_LINES`／`FROZEN_WRITE_LINES`）：派单只点「两张清单和两个上界」，
   这两条行数棘轮不在其列，且与终态不冲突。
5. `FROZEN_LEGACY_KEYS`／`legacy-ratchet-295`／`legacy-partition-313` 未动：`#322` 已把口径改成
   「⊆ ＋ 零丢失 ＋ ≤」，搬空最后一条不必回头改它们（实测三件全绿，见 §7 线 4）。

## §6 未做项

- 未改判官、未改冻结面（`test/calorie-routing-81.test.mjs`、`src/triggers/scene-*.ts` 逐字节未动）、
  未跑 `pnpm help:build`、未删 `dist/`、未用仓根 `tsc -b --force`、未 `--amend`／`--force`／rebase。
- 未写票面 100%、未 close 任何票（编排者负责）；`≤200 行` 归 `#330`；`src/output.ts` 的 21 处
  （`writeSuffixFor` 的键→文件名后缀表）不在 ①② 射程，仍待单独开票（`t320-残余定性.md` §3）。
- 未对 `P4`／锚集口径提出改判官的建议：判官是冻结件，改它＝另一张票的事（本席只留痕，不越权）。

## §7 对账（`tooling/check-gate-audit.mjs`）

### §7.1 事先声明（GATE-RUN；runId 逐条取自 `.scratch/locks-land/gate-runs.log` 的真实条目）

- GATE-RUN runId=20c2cc2d-e12b-4abb-b144-9317b2469d50 cmd="node node_modules/typescript/bin/tsc -b"
- GATE-RUN runId=16493d28-b181-45f8-87d4-9b699444f6b2 cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"
- GATE-RUN runId=a19d17a4-6250-479f-ac38-6ee9a7b98677 cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
- GATE-RUN runId=92aa9da5-1bd6-4b7d-9c8c-bcebf1a309c7 cmd="node --test test/*.test.mjs packages/*/test/*.test.mjs"
- GATE-RUN runId=426ccb56-c75b-4909-881a-cff16e83b4d5 cmd="node tooling/skill-html-snapshot.mjs --check"
- GATE-RUN runId=6aa5829f-9fdb-4694-bbd7-76195c2101b6 cmd="node --test test/calorie-routing-81.test.mjs"

六条**当刻全绿**（exit 0），且是**同一轮连续复跑**（UTC `2026-09-13T21:58:12Z` → `21:58:56Z`，
脚本 `.scratch/t320b/finals2.ps1`，逐条输出落 `.scratch/t320b/s1.log`…`s6.log`）。

### §7.2 对账窗口＝上面这一轮（`--since` 界定），无任何 `--allow-*` 放宽

本票号 `t320b` 在共享日志里共有 **21 条** RUN 条目（15 条为准备／试跑条目，6 条为 §7.1 这一轮）。
按协议「共享日志下必须界定窗口」，对账窗口取**这一轮的时间区间**（`--since 2026-09-13T21:58:12Z`），
窗口内**恰 6 条**、全部被 §7.1 认领、全部 `exit=0` ⇒ 无需任何放宽，故本件**没有** `GATE-RELAX` 行。
窗口外的 15 条逐条列名（供复核席抽查，均非判据）：

| runId（票号 `t320b`） | 命令 | exit | 说明 |
|---|---|---|---|
| `4101606b-1e6c-4462-9599-0a0ecd30e3b1` | 判官 | 1 | **落点裁定前的红读数**（§1.1） |
| `a770e153-e04a-4ed2-84af-63bf11669185` | `gen-cli --check` | 0 | 同批试跑 |
| `1840b10c-f392-41e0-baf7-8bced42abcb4` | `tsc -b` | 0 | 同批试跑 |
| `3ebd4490-7b0f-484e-8d71-df337dade204` | `node --test packages/skill-calorie/test/calorie-routing-81.test.mjs` | 1 | **路径写错**（该件在仓根 `test/`，不在包内 `test/`） |
| `d28b7b62-5a00-4027-af2f-3fa9848fbae7` | 快照检查 | 0 | 同批试跑 |
| `1320557f-2109-4531-8754-dfff132af522` | 判官 | 0 | 同批试跑（落点修正后的中间轮） |
| `6827f630-0f6d-4dd7-9208-624469bc008a` | `gen-cli --check` | 0 | 同批试跑 |
| `ef833f84-364c-41aa-a6c9-15ad5875df1c` | `tsc -b` | 0 | 同批试跑 |
| `f1c5a6ba-2eac-4e1a-9d8c-726f6e148b16` | `tsc -b` | 0 | 上一轮六条（终稿之前的同构复跑） |
| `73255397-eaa0-4ea9-a885-58c8137d46fa` | `gen-cli --check` | 0 | 同上 |
| `d56d1f7e-127f-4314-9adc-95b8ca942170` | 判官 | 0 | 同上 |
| `c8b6221d-1c5b-4741-951a-6b6bcb70d6ba` | 全量测试 | 0 | 同上（1492/1492） |
| `4bb3a259-e6e4-44e4-b08c-b28d71a099dc` | 快照检查 | 0 | 同上 |
| `3f98236a-e8a2-4b8f-84d6-cb8cdbfcefd6` | `node --test packages/skill-calorie/test/calorie-routing-81.test.mjs` | 1 | **同一处路径错**（改正后见 §7.1 第 6 条） |
| `f26bf406-ca7c-410f-81a1-274d02780716` | `node --test test/calorie-routing-81.test.mjs` | 0 | 冻结件首轮 8/8（§7.1 第 6 条是终轮） |

另有四个**准备／变异窗口**走的不是本票号（不进 `t320b` 窗口，留痕在共享日志里）：
`t320-prep`（`6b03c643…`）、`t320-prep2`（`0824e349…`）、`t320-mut`（`ee2039b5…`）、
`t320-prep3`（`b60c143d…`），四者 exit 0；变异自证的**红**读数在 `t320-mut` 窗口内（§2）。

### §7.3 对账读数（对账 run#1，票号 `t320b-audit`，runId `d2a66257-0108-4173-b3fa-63c893224df6`）

命令（持锁；窗口 `--since 2026-09-13T21:58:12Z`，无任何 `--allow-*`）：

```
node tooling/run-locked.mjs --ticket t320b-audit --lock-dir .scratch/locks-land --stale-minutes 30 -- \
  node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t320-收口-证据2.md \
  --ticket t320b --since 2026-09-13T21:58:12Z --log .scratch/locks-land/gate-runs.log \
  --export docs/skills/skill-calorie/t320b-gate-runs.log
```

读数（输出落 `.scratch/t320b/audit1.log`）：

```
审计：D:\ilife\.scratch\locks-land\gate-runs.log（RUN 条目 320 条；窗口内 6 条，ticket=t320b，since=2026-09-13T21:58:12Z）
声称运行 6 条
  - 证据 :172 runId=20c2cc2d-… cmd=node node_modules/typescript/bin/tsc -b
  - 证据 :173 runId=16493d28-… cmd=node packages/skill-calorie/scripts/gen-cli.mjs --check
  - 证据 :174 runId=a19d17a4-… cmd=node docs/skills/skill-calorie/t293-验收-命令自治.mjs
  - 证据 :175 runId=92aa9da5-… cmd=node --test test/*.test.mjs packages/*/test/*.test.mjs
  - 证据 :176 runId=426ccb56-… cmd=node tooling/skill-html-snapshot.mjs --check
  - 证据 :177 runId=6aa5829f-… cmd=node --test test/calorie-routing-81.test.mjs
EXPORT: D:\ilife\docs\skills\skill-calorie\t320b-gate-runs.log（窗口内 RUN 条目 6 条，受 git 跟踪）
RESULT: matched=6/6 auditEntries=320 scoped=6 undeclared=0
gate-audit: PASS
```

六条声明**一对一**命中窗口内的六条（runId 精确绑定，无「同 cmd 历史条目顶替」）；
`undeclared=0`（窗口内没有未声明条目）；未用任何 `--allow-*`，故本件无 `GATE-RELAX` 行。

## §8 受跟踪导出源复核（对账 run#2，票号 `t320b-audit2`，runId `90f4d733-77d9-4202-8ff3-a85ed34385ab`）

以**受 git 跟踪的导出件**当审计源再跑一次（不带 `--export`，以免覆写导出件的来源头）：

```
node tooling/run-locked.mjs --ticket t320b-audit2 --lock-dir .scratch/locks-land --stale-minutes 30 -- \
  node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t320-收口-证据2.md \
  --ticket t320b --log docs/skills/skill-calorie/t320b-gate-runs.log
```

读数（输出落 `.scratch/t320b/audit2.log`）：`审计：…t320b-gate-runs.log（RUN 条目 6 条；窗口内 6 条，ticket=t320b）`／
`RESULT: matched=6/6 auditEntries=6 scoped=6 undeclared=0`／`gate-audit: PASS`。
导出件头部逐字：`> 票号过滤：t320b｜since：2026-09-13T21:58:12Z｜until：（无）｜条目数：6｜导出时间：2026-09-13T22:00:00.439Z`。
