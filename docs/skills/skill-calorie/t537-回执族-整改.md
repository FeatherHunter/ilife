# #537 回执族七页重排（记体脂两种／记围度／补记两种／删体脂／删围度）· 读数与证据

> 判据与阈值见 `.scratch/m158v2/席位简报.md`（负责人六条 ＋ 五维尺）与
> `docs/skills/skill-calorie/t534-*`；本件只落**读数**与**逐页逐处对照**。
> 席位：本票唯一作者（窄席位 #537）。README 口径：**「可见文本」按默认态算**——
> `<details>` 折叠区（「对账信息」）里的内容算**行文面**，视觉审查按默认态判（节点级探针仍扫全串）。

## 0. 开工基线（自己复核过一遍的）

- `git rev-parse HEAD` 开工时＝`4d2f0341b7dffaad5ee9064c21451f7c84d1c4c3`；改动只落在写集四类路径，见 §6。
- 三件当刻实测（LF 口径，`\n` 数）：`src/body/receipt.ts` **348 → 250**、`log.ts` 223（**本票未改**）、
  `remove.ts` 104（**本票未改**）；新立两件 `src/body/receiptUi.ts` **48**、`src/body/receiptData.ts` **146**。
- 探针**改前**（七页逐条指名跑，不是 `--dir`）：`RESULT: 0/7 FAIL`，逐页节点级命中
  `01=? 02=? 03=10 04=10 05=11 12=9 13=9`（全量存 `.scratch/m158v2/t537/sep-before.json`）。
  溢出改前：已为 0（回归门）。

## 1. 锁的偏离（要人核）

`.scratch/locks/owner.json` 当刻的持锁者是 **vision 审查类命令**（`ticket=523`，
`cmd` 含 `dsh --profile visionreview …`）⇒ 按票面授权改用 `--lock-dir .scratch/locks-m158v2`。
本票所有门（探针／溢出）都在该目录下跑，`runId` 逐条记在 §4。

## 2. 环境事实（不入版本库的临时处置）

- `tsc -b` 全程报 **他席在途件的红**（先后是 `src/photo/gifDoc.ts:43` 的 `renderMediaFigure` 坏 import、
  以及 `src/render/dietDocs.ts:34` 引用 `diet/todayDocs.js` 两个已不存在的出口）。两条都**不在本票写集**：
  前者按编排者指示每次 `tsc -b` 后跑 `.scratch/m158v2/fix-dist.mjs`（幂等，只改 `dist` 产物），
  后者使整条 CLI 在 import 期 `SyntaxError`、`run-pages.mjs` 的暖库与逐条跑全红——**不是本票改出来的**，
  遇到就重试；本票自己的源件 `tsc` 无红。

## 3. 逐页读数（改前 / 改后）

（逐页追加。）
