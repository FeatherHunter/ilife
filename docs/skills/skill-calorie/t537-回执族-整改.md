# #537 回执族七页重排（记体脂两种／记围度／补记两种／删体脂／删围度）· 读数与证据

> 判据与阈值见 `.scratch/m158v2/席位简报.md`（负责人六条 ＋ 五维尺）。本件只落**读数**与**逐页逐处对照**。
> **「可见文本」口径**：节点级探针扫全串（含折叠区）；**视觉审查按默认态**——`<details>`（「对账信息」）
> 里的内容不算「上屏」，本件所有版式结论都按默认态说。

## 0. 开工基线

- 开工 `HEAD`＝`4d2f0341b7dffaad5ee9064c21451f7c84d1c4c3`；改动只落在票面写集里的路径（见 §6）。
- 探针**改前**（七页逐条指名跑，不是 `--dir`）：`RESULT: 0/7 FAIL`，逐页节点级命中
  `03=10 04=10 05=11 12=9 13=9`（全量存 `.scratch/m158v2/t537/sep-before.json`）。溢出改前：已为 0（回归门）。

## 1. 锁的偏离（要人核）

`.scratch/locks/owner.json` 当刻持锁者是 **vision 审查类命令**（`ticket=523`，`cmd` 含
`dsh --profile visionreview …`）⇒ 按票面授权改用 `--lock-dir .scratch/locks-m158v2`。
本票所有门都在该目录下跑，`runId` 记在 §4。

## 2. 环境事实（不入版本库的临时处置）

- `tsc -b` 全程报**他席在途件的红**，先后是 `src/photo/gifDoc.ts:43` 的 `renderMediaFigure` 坏 import、
  `src/render/dietDocs.ts` 的 `DataTextInput`／`home/today.ts` 的 `commandLine`、以及**整条 CLI 起不来**的
  `dist/diet/receipt.js` 重复声明（`SyntaxError: Identifier 'beforeOf' has already been declared`）。
  三者**都不在本票写集**：第一条按编排者指示每次 `tsc -b` 后跑 `.scratch/m158v2/fix-dist.mjs`
  （幂等，只改 `dist` 产物）；第二、三条使暖库与逐条跑全红，**不是本票改出来的**，遇到就重试。
  本票自己的三件 `tsc` 无红。`packages/base-render/dist/pageShapes.js` 曾与 `src` 不同步
  （缺 `renderMediaPlaceholder`），用 `tsc -b --force packages/base-render` 重建产物即好（未改源码）。
- `t363`／`t364` 一度在 `r.status === 0` 那一步就红（stderr 是 ESM loader 解析失败）：**判非本票范围**
  （dist 与 src 不同步，多席位共用工作区里他席在途件造成），归收口票 #539；本席不改别人的件。

## 3. 七页做了什么（逐页，改前 → 改后）

**七页共用的页头**（票面第 1 条 ＋ 编排者裁定）：`<title>` 由 `卡路里·身体细节回执` 改
**`卡路里 身体细节`**（空格分层，七页同值，照场景 09 已交付先例）；眉标由 `身体细节 · 写后回执` 改
**归属词 `身体细节`**（一页一处）；H1 由 `记体脂 · 回执` 改**这一页是什么**；**副标题整行撤**
（它印的回执摘要与结论条说的是同一件事）。

**页名怎么推**（编排者 2026-09-15 裁定）：分派层只把命令名与参数交给 `bodyReceiptDoc`，
`meta.wakeWord` 恒是命令面的两个字（唤醒词进不来）。故 —
- 体脂按**来源**分：皮褶钳来源＝`记体脂（皮褶钳）`，其余来源＝`记体脂（外部测量）`；
- **是不是补记**按**落库日期早于这一条回执的写入日**判（判据是日期比较，不是猜的）；
- 删类两支＝`删体脂`／`删围度`。七条唤醒词由此各自落到一个页名上，**不动路由、不动冻结的词名与示例**。

| 页 | 改前（原文） | 改后 |
|---|---|---|
| 01 记体脂（皮褶钳） | H1 `记体脂 · 回执`；副标题 `已记体脂：…（Jackson-Pollock 7 点法：7 点合计 82mm ＋ 年龄 30 ＋ 男）`；四张卡（状态／记录编号 `#5`／影响行数／写入字段）；口径行 `记录现值（逐格读自库内 body_composition）：`；页脚 `📊 数据来源：身体细节（calorie_data.db · body_composition） ｜ 单位：…；…` | H1 `记体脂（皮褶钳）`；结论条 `已记下 2026-09-15 的体脂记录，按 Jackson-Pollock 7 点法换算，可照「看体脂趋势」复查。`（**系数与「7 点合计 ＋ 年龄 ＋ 性别」整段撤**）；两张卡（体脂率 `12.02 %` ＋ 操作徽章 `✓ 新增`／日期）；段名 `这次记下的（体脂率读百分比，皮褶读毫米）：`，组头「皮褶读数（毫米）」，值带单位由页内 CSS 补；页脚两条口径行，段内用 `｜` 段位 |
| 02 记体脂（外部测量） | 同上 ＋ 副标题里 `冲突：… 已有 1 条体脂记录 #5（来源 家测皮褶钳；…）` | 同上；同日那条改由 `.brc-alt` 块承载（浅底＋描边，与上表一眼分得开），口径行 `同一天还记过这条`（**主键 `#5` 撤**） |
| 03 记围度 | 结论句 `已记围度：2026-09-15（waistCm 85、hipCm 95）`（**CLI 参数名 ＋ `、`**） | 结论条 `已记下 2026-09-15 的围度记录，可照「看围度趋势」复查。`；卡 `量到的部位 2 个`／日期 |
| 04 补记体脂 | H1 `记体脂 · 回执`（与 02 同字） | H1 `补记体脂`；结论条 `已补记 2026-09-06 的体脂记录，可照「看体脂趋势」复查。`（**日期早于写入日 ⇒ 判补记**） |
| 05 补记围度 | 副标题把 13 个部位硬串一堵墙；两张 13 行同款表（26 行、整页 1649px） | H1 `补记围度`；本次那张**只摆有值的行**（日期＋腰围两行），下面一句 `另有 13 项这次没记，就没摆上来`；同日那条整块浅底＋描边 |
| 12 删体脂 | 副标题 `已删除体脂记录 #1（日期 2025-12-25 · 体脂率 25.5% · 来源 …）（软删除：行保留，已从查询与统计中排除；暂无恢复入口）`；卡里 `记录编号 #1`／`影响行数 1 行`／`写入字段 1 项` | 警示块 `2025-12-25 的这条体脂记录已经删掉。` ＋ `行还留在库里，查询和统计里不再出现它，暂时没有恢复入口。`（**「软删除」这个仓内叫法不上屏**）；卡 `体脂率 25.5 %`（徽章 `✕ 删除`）／日期；段名 `删除前的原值（体脂率读百分比，皮褶读毫米）：` |
| 13 删围度 | 同上那堵副标题墙 ＋ 库表名／库文件名／`sqlite:total_changes` | 警示块同上；卡 `删掉的部位 13 个`／日期；值表**带单位并分组**——组头「躯干（厘米）」「左右成对（厘米）」，行宽收窄（值紧跟标签，不再拉到整行最右） |

**同一事实一页一处**：改前「写成功」在**三处**说（副标题摘要／状态卡 `已改动`／卡内 `✓ 新增`）；
改后只在**结论条一处**（删类在警示块），卡上那枚徽章说的是**这次是什么操作**（新增／删除），两件事不同。

**字段清单一整行**（票面第 4 条）：原 `写入字段 12 项`＋卡明细里一整行 `日期、来源、体脂率、年龄、性别、胸、腹…`
改成**徽章列**（`receiptUi.ts` 的 `labeledChips`，名字槽 ＋ 一排小标签），落进页尾「对账信息」折叠区；
页面上「这一页有哪些列」由那段逐格键值行本身回答（分工：徽章列答**命令写了哪些字段**，逐格行答**这条记录现在是什么值**）。

## 4. 读数

| 门 | 改前 | 改后 |
|---|---|---|
| `audit-separators.mjs`（七页指名跑） | `RESULT: 0/7 FAIL` | **`RESULT: 7/7 PASS`**，七页 `node·0 ；0 ≥3段0 ｜0 、0 ~0 标识符0`（节点级与行级双零） |
| `measure-responsive.mjs`（390／768／1440） | 0（回归门） | **`OVERFLOW-ZERO pages=12 cells=36 failed=0`**（含过程型页；本族 7 页逐档 0） |
| `t365-七条写词整页.test.mjs` | — | 12/12 绿（含新增的 `#537 回执七页` 判据） |
| 视觉自证（编排者裁定第 7 条，`node .scratch/m158v2/截图.mjs …`） | 13-删围度 49 分、01-记体脂-皮褶钳 50 分（编排者改前基线） | `.scratch/m158v2/t537/shot-13-1280.png`（1280×1203）、`shot-05-1280.png`（1280×1335，原 1649） |

## 5. 三件源件与拆法（必报五步第四步）

`src/body/receipt.ts` 进场 348 LF，重排后一度到 **384** —— **已超线，需要根据规则进行重构。**
超因：这一件同时住着七页的整页装配（页头／结论条／读数卡／逐格段／同日块／对账区／复制区／口径行）
与两套数据面（逐格取值 ＋ 复制载荷）。当场拆成三件（均在 350 以内）：

| 件 | LF | 职责 |
|---|---|---|
| `src/body/receipt.ts` | **328** | 页壳与整页装配（页名推导／结论判语／读数卡／分组装配／对账区／口径行） |
| `src/body/receiptUi.ts` | **79** | 页内样式串 ＋ 两个页内形状（`labeledChips` 徽章列／`groupHead` 组头） |
| `src/body/receiptData.ts` | **146** | 数据面（逐格取值、槽位、删前快照、复制载荷）——「这条记录现在是什么值」的唯一出处 |

拆线＝按「这一页长什么样」（装配）／「这条记录是什么值」（数据面）切一刀。三件都在线内，
**台账无需新增超线行**（未跑 `--sync`：当刻台账里他席的 `src/diet/receipt.ts`／`src/home/homeViewParts.ts`／
`src/photo/gifDoc.ts` 三行正是在途件的实况，替它们回写会踩到别人的件）。

## 6. 改了哪些路径

写集内：`packages/skill-calorie/src/body/receipt.ts`（改）、`receiptUi.ts`（新立）、`receiptData.ts`（新立）；
本族测试：`test/t363-补记查冲突.test.mjs`、`test/t364-删除取快照.test.mjs`、`test/t365-七条写词整页.test.mjs`；
证据：本件。**`log.ts`／`remove.ts` 本票未改**——它们出的 `receipt.summary`／`items[].detail` 是
**回执载荷面**（AI 读的那一面），不上页；三条写词测试（`t357`／`t363`／`t364`）逐字钉着它们的形状，
动它等于改冻结口径，而票面点名的债**全部在「上屏」那一侧**。

## 7. 遗留（要人裁）

1. **`t363`／`t364` 我按新页结构改了判据**：它们原先钉的是「整句回执摘要当副标题压在页头」——
   那正是本票要撤的债（主键 `#1`／`#5` ＋ `软删除` 叫法 ＋ 同一事实三处说）。改法是**换锚不换意图**：
   `t363` 改读「同一天还记过这条」那一块并逐格比查库值，`t364` 改为正负两条（逐格段仍在 ＋
   摘要句与「软删除」叫法不许上屏）。这两支不在票面点名的 `*receipt*`／`*log*` 测试名单里，请编排者认一下。
2. **缺值不占位**（编排者裁定第 1 条）改了 `t365` 的 `裁定2-可见` 判据：原判据要求
   未填的 7 点与空备注**逐格写 `—`**；新判据改为「缺值的行整行不摆、上屏的行全为查库真值、
   缺了几项由段下那句计数说清」。`裁定 2` 的另一半（**复制数据里缺值不写 `—`**）一字未动。
   删类**不过这道筛**：删除回执仍摆全字段（`t364` 的逐格同源契约钉的就是这一条）。
3. **值带单位是 CSS 补的**（`::after{content:' 厘米'}`）：与公共层表格卡片化的
   `td::before{content:attr(data-label)}` 同一手法——**值带单位看得见，而逐格比对的值仍是查库原值**
   （三条测试的「页内 == 查库值」契约一位不动）。若终审判「单位必须进可见文本」，改法是把单位并进
   值的字符串，同时要改三条测试的期望。
4. **「删除回执的逐格段仍摆 `—`」**：删类的 13 行里可能有空位（`t364` 的 `裁定2-可见` 钉着），
   与记／补记类的「缺值不占位」不同口径。同一族里两种摆法，请终审判要不要统一。

## 8. 编排者视觉复核后的第三轮收口（提交 `f4ceda5`）

对方点名的三处小口子，按同一族口径一起收（7 页里凡有值表的都适用）：

1. **行里的值不再带单位**——原先由页内 CSS 的 `::after` 补「 厘米」，读起来像 `105-厘米` 一根负号；
   现在行里只留裸值。
2. **单位只留组头一处**（`躯干（厘米）`／`左右成对（厘米）`／`皮褶读数（毫米）`）：段名由
   `删除前的原值（各部位读厘米）：` 收成 `删除前的原值：`，不再与组头说两遍（同一事实一处说）。
3. **身份字段移出部位组**：日期／来源／体脂率／备注走 `rowClass` → `.brc-meta`（整行抬开一档 ＋ 一条细线），
   `备注` 一眼落在「左右成对」组外。

读数：探针 **`RESULT: 7/7 PASS`**（七页节点级与行级双零）；本族四条写词测试 **34/34 绿**
（`t357`／`t363`／`t364`／`t365`）。自证图 `.scratch/m158v2/t537/shot-13-1280.png`（1280×1245）
与 `shot-05-390.png`（390×1541）。三件 LF：`receipt.ts` **327**／`receiptUi.ts` **76**／`receiptData.ts` **146**。

## 9. 提交清单

| sha | 内容 |
|---|---|
| `d6ad686` | 七页重排（页头三处 `·` 清零、库内世界下屏、写成功一处说）＋ 拆 `receiptUi.ts`／`receiptData.ts` |
| `d2c80a6` | 补记判语（日期早于写入日）／缺值不占位／组头与两表分家／按钮收窄 |
| `f4ceda5` | 单位只留组头、行里那根短横去掉、备注移出部位组 |

