# #664 · 四件陈旧断言按当刻实现换对象（证据件）

> 票：**#664**（编排者 2026-09-16 夜开、归本席）；写集＝**四件测试 ＋ 本件**，零源码改动。
> 背景：本席在 #654 里跑相邻门时发现 14 条红，编排者复核后拆成两拨——**12 条归 #664**（本件）、
> 2 条归 [#649]（`wizard-86` 的两条**文本**断言，本席不碰）。
> 时间：2026-09-16 夜（窗口 `2026-09-16T16:17Z` 起；运行条目见 `t654-gate-runs.log`，票号 `ticket=664`）。

## 一、口径（编排者裁定，逐条照做）

> 新口径是「有数据位、无日志位 → **只有一颗真胶囊**」，**不许**改回"必须补占位"，也**不许**放宽成"至少一颗"。
> `photo-helpdoc-488` 按当刻手写命令块那一路数，不是删断言。

根因一句话：公共层 #336 那条「有数据位、无日志位就自动补一颗**禁用态**复制日志」的兜底，已被
`91563b45`（#654）撤掉；这四件测试的最后一次提交都早于它（或早于各自钉的实现变更），于是钉着一个
**已经不存在的形态**。本件一律**换对象**：把断言改到当刻实现上，并**加了红／绿两向的变异**。

## 二、逐件改法与红点（改前 → 改后）

| 件 | 改前（陈旧） | 改后（当刻实现） | 变异（红→绿） |
|---|---|---|---|
| `render-copy-90.test.mjs`（4 条） | HELP 行「每颗数据按钮各带一颗禁用日志占位」（`placeholders==2`／`buttons==4`）；`copyActionHtml` 的逐字期望里带那颗禁用占位；`bindCopyAction` 订阅 2 个 id（数据＋占位）、按 0/1/2 三个索引驱动 | **恰一颗真胶囊**：`buttons==2`、`disabledPills==0`；`copyActionHtml` 逐字期望＝单颗 ＋ `action-row-ghost-single` 修饰类；订阅只剩 1 个 id（两行共用冻结 id），索引 0/1 | 把 #336 那颗禁用占位塞回去 ⇒ 「恰一颗／恰两颗」判据必红；原产物再判必绿（`MUTATION-GREEN #90` 两行） |
| `copy-component-179.test.mjs`（3 条） | ① 菜单用途提示取「老仓原样三项」（`粘贴给 AI / 自己看` 等）；② 日志第 1 段期望 `calorie.calorie.view.profile（stat）`（拿**直调** `buildLogText` 的产物当期望）；③ 提示块期望 `ilife-toast`（深色 toast） | ① 那三句已被作者裁定删除（`MENU_HINTS` 逐项空串）⇒ 判据改「三句 0 命中 ＋ 空 span 0 个 ＋ 三个格式名仍在」；② 以**页面载荷**（`copyArea` 出那颗日志按钮的 `data-t`，过 `sceneEnvelope` 归一）为准，第 1 段＝`calorie.view.profile（stat）`，反证 `calorie.calorie.` 0 命中；③ `notice` 走 **#154 浅色静态形态**（`-note` 一族），不是 toast、也不出「知道了」 | ① 塞回老仓提示 ⇒ 红；② 把键改成拼两遍 ⇒ 红；③ 换成 `renderFeedbackBlock` 不给 `staticNotice` 的产物（真 producer，不是手搓串）⇒ 红；三者还原均绿（`MUTATION-GREEN #179` 两行） |
| `photo-helpdoc-488.test.mjs`（3 条） | ② 数 `renderPreBlock` 的区块 `ilife-block-pre-block`／代码格 `-pre-block-code`（本页 #529 起改**手写复制胶囊**后恒 0）；「同事实一页一处」也数那格提示文本 | ② 数**行内载荷按钮**（`data-action-id="ilife-help-copy-prompt"`，恰＝命中数）＋反证两种 pre-block 类名 0 个；页尾复制区＝双位（行内 hits 颗 ＋ 日志 1 颗 ＝ `hits+1`，并断言日志那颗带载荷、页内 0 禁用态）；②b 清单读法那句**不手抄**，改按当刻实现派生（`photoHelpRowsLead(按钮文案)`） | 八类改坏全红（含把裸 `<pre>` 塞回页里、把原文塞回可见文本、把命令键印成文本节点等）、改回必绿（`MUTATION-RED/GREEN #488` 两行） |
| `t496-文案统一.test.mjs`（2 条） | 复制区「双按钮」写成 `日志按钮数 >= 1`（写页必须有） | 改成**逐页写死期望颗数**：接线的页＝**恰 1 颗**真日志；当刻两页（`#9/#10 看营养分析`、`#11 看每日六因素`）＝**恰 0 颗**，并把这**缺口**写进 `LOG_GAP_IDS` 登记（接线补上则本条即红，届时应摘掉该 id，不许改成「0 或 1 都行」） | 把接线的页里那颗日志摘掉 ⇒ 「按钮数 ≠ 1」必红；原产物再判必绿 |

**缺口登记（范围外、S3 记账）**：`#9/#10 看营养分析`／`#11 看每日六因素` 两页的复制区当刻只有数据那颗
（装配件走 `dataCopyArea` 而不是 `copyArea({data, log})`）——属**饮食族**的接线欠账，不在本票写集
（本票是场景 09 照片族），也不在 #654 的十页名单里；已在 `t496` 里按缺口钉住，供后续票认领。

## 三、读数（改前红、改后绿，逐条可复跑）

| 件 | 改前（本席进场读数） | 改后 |
|---|---|---|
| `photo-helpdoc-488` | `exit=1 pass=1 fail=3`（① ② ④，卡在「命令块数 ≠ 命中数」） | `exit=0 pass=4 fail=0`（含八类变异红名单 ＋ 绿行） |
| `render-copy-90` | `exit=1 pass=9 fail=4` | `exit=0 pass=13 fail=0`（含两行 `MUTATION-GREEN`） |
| `copy-component-179` | `exit=1 pass=3 fail=3` | `exit=0 pass=6 fail=0`（含两行 `MUTATION-GREEN`） |
| `t496-文案统一` | `exit=1 pass=37 fail=2` | `exit=0 pass=39 fail=0` |
| **四件合跑（收尾一轮）** | —— | 见 `t654-读页复制日志.md` §四 的 21 件合跑：`tests 171 / pass 171 / fail 0`，`runId=47ccf3ff-7580-4bc2-9671-8609f12f6ec4` exit=0 |

单件运行标识（均 exit=0，取自 `t654-gate-runs.log`）：`photo-helpdoc-488`＝
`dc771702-ed41-41b1-8ab9-dfec4cddde0f`；`render-copy-90`＝`0542a030-1d5d-45ac-9d49-1c976284c9ec`；
`copy-component-179`＝`d384e72a-adf8-493a-8d64-04f4c0586327`；`t496-文案统一`＝
`20e47c49-9fee-4d5a-b8d1-6800de069580`。

## 四、没做与边界（如实）

1. **`wizard-86` 的两条红本席不碰**（编排者点名归 #649）。并更正本席上一轮的归因：那两条是**文本**断言
   （`empty.html.includes('复制 prompt')`／`bare.html.includes('请选来源')`），真因是文案改成
   「复制指令」／「请**先**选来源。…」没跟断言——**不是** 91563b45；本席按「测试件最后一次提交早于
   91563b45」推的因果不成立（提交时间跟它钉的行为何时变是两件事），此处留痕。
2. 本件**零源码改动**：四件测试 ＋ 本证据件，别的一行不碰（`git diff --name-only` 可核）。
3. 口径只对本件四件生效，**不类推**到别的门：别的门若还钉着 #336 占位形态，按各自归属票处理。
