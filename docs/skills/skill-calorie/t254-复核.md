# #254 独立复核（第二轮）—— 目标类 4 件片段结果页变完整文档

复核者：第三阶段独立复核席。实施者的结论一律不采信，本页每条读数都是本席**自己新跑**的
（runId 全是 `254-R` 的，见 `.scratch/locks/gate-runs.log`）。本席只写两处：
探针与日志在 `.scratch/t254-R/`，结论在本页。

被审对象：`47a52156dc4dffd86282d6d17ffe289e9e1f2781`（功能）＋ `299d094`／`49bd49d`（证据件两次补文）。
复核时刻盘面：`HEAD=49bd49d`；工作区里有别票的在途改动（`packages/skill-calorie/src/home/homeDocs.ts` 等），
本席一字未碰；本席自己动过的件只有本页与 `.scratch/t254-R/**`。

第一轮的判决是 FAIL（报告原文见提交 `848d8ea`，本页末尾附其结论）；第二轮实施者补了交付物，本席重审。

---

## 一、判定

**PASS —— 94 / 100。** 票面验收命令本席新跑 `exit 0`（`ℹ tests 2 ／ pass 2 ／ fail 0`）；
4 件产物逐件实跑都是完整文档，取数口径经**真·改前复现**对照后逐字段未变；无 S1。

| 维度 | 分值 | 得分 | 依据 |
|---|---|---|---|
| 契约一致 | 30 | 30 | 4 件逐件：exit 0／`data.output` 绝对路径且文件在盘／以 `<!doctype html>` 起／含 `charset`（且落在前 1024 字节）／含 `<style`／含 `ilife-page`；参数与退出码读数照旧（缺参 exit 2／缺失阻断 exit 4） |
| 证据真实可复现 | 25 | 20 | 证据件与回执里点名的 12 条 runId 全在台账里、命令原文与退出码逐条对得上；改前读数由本席从父提交复现后与实施者留档逐字节同。**扣 5**：第三段那条废跑的成因挂错了 runId，且漏登另一条真废跑（见缺陷 1）；用例本身有一个覆盖洞（缺陷 2） |
| 新旧对照 | 20 | 20 | 把 `read.ts` 换成父提交那份重跑：改前 1779／1779／1357／1203、三条判据全黑；改后 73411／73405／82733／70421、三条全绿；`data.metrics` 逐字段相同 |
| 工程红线 | 15 | 15 | 提交只动了票面写集里那几件；`git add -A`／`stash`／`reset`／`checkout` 一件未犯（台账里能逐条查）；实跑一律临时库；旧片段渲染件按票面留着 |
| 文档同步 | 10 | 9 | 证据件在、两次补文在；**扣 1**：废跑那一行的机器读数不实（同上） |

---

## 二、圈定范围（多一件即 S1，逐条核过）

`git show --stat` 三发提交动的件：

| 提交 | 动的件 |
|---|---|
| `47a5215` | `packages/skill-calorie/src/goal/resultDocs.ts`（新建，270 LF）／`packages/skill-calorie/src/goal/read.ts`（＋19／−8）／`packages/skill-calorie/test/goal-result-254.test.mjs`（新建，161 LF）／`docs/skills/skill-calorie/t254-证据.md`（新建） |
| `299d094` | `docs/skills/skill-calorie/t254-证据.md` |
| `49bd49d` | `docs/skills/skill-calorie/t254-证据.md` |

- **写集之外一件没动**：`git show --name-only` 逐发看全，就是上表这些；票面点名不许碰的
  `src/render/html.ts`／`src/home/**`／`src/diet/**`／`src/cli/**`／`src/render/sportPortDocs.ts`／
  `src/render/trendDocs.ts`／`src/goal/commands.ts`／`src/goal/receipt.ts` 一件未进提交。
- **工作区与提交一致**：`git diff HEAD -- <上列四件>` 输出为空；两件源码的
  `git hash-object` 与 `HEAD:<路径>` 逐字相同（`read.ts`＝`d7d8ff05…`、`resultDocs.ts`＝`882f12b2…`）。
- **旧片段渲染件按票面留着**：`src/render/html.ts` 最后一次改动仍是 `46fcc4d`（#351），
  `renderGoalHtml:106`／`renderGoalExpiringHtml:562`／`renderGoalVsActualHtml:584` 三件都在，本票一行未删。
- **改动面**：`read.ts` 的 diff 里只有注释、import 表、每条命令新增一行命令原文、以及三个 `html:` 表达式；
  `nums({…})` 的字段映射与三条 `build*View` 的调用**都是 diff 的上下文行**（逐字未动）。

---

## 三、独立重跑（本席自己的 runId、自己的判据）

编译入口写死 `node node_modules/typescript/bin/tsc -b packages/skill-calorie`（`npx tsc` 是假出口，没用）。

| 步 | 机器读数（从 `.scratch/locks/gate-runs.log` 抄） | exit |
|---|---|---|
| 编译 | `GATE-RUN runId=9a2e15f7-2d4c-4b78-92df-d8f38c1ea951 cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie`；`RESULT: ticket=254-R runId=9a2e15f7… waitedMs=0 exit=0`；`tsconfig.tsbuildinfo` mtime `21:49:24.830`→`21:50:39.852` | 0 |
| 票面验收命令 | `GATE-RUN runId=222aa070-6a6e-4c78-8d8c-dad9a609086c cmd=node --test packages/skill-calorie/test/goal-result-254.test.mjs`；`RESULT: ticket=254-R runId=222aa070… waitedMs=0 exit=0`；`ℹ tests 2 ／ ℹ pass 2 ／ ℹ fail 0` | 0 |
| 本席探针（4 件逐件 ＋ 两层读数核对） | `runId=19e07330-459c-4cdb-a113-6e615d058f10 cmd=node .scratch/t254-R/probe.mjs`；末行 `PROBE-RESULT 全绿 件=4/4` | 0 |
| 受控变异两发 | `runId=2dc37d31-78d5-42dc-9cf5-afc78c7c94b8 cmd=node .scratch/t254-R/mutation.mjs`；末行 `MUTATION-RESULT red-then-green=OK problems=0` | 0 |
| 改前真身复现 | `runId=0722d422-dd41-4344-bf79-2bc36a4e9ba7 cmd=node .scratch/t254-R/before-repro.mjs`；末行 `BEFORE-REPRO-RESULT red-then-green=OK problems=0` | 0 |
| 页面八样清单 | `runId=e9d8a765`／`runId=2539c85a cmd=node .scratch/t254-R/inspect.mjs` | 0 |
| 契约（退出码）探针 | `runId=c6feac79-1d94-4cd9-845d-9542af034bb2 cmd=node .scratch/t254-R/contract.mjs`；`CONTRACT-RESULT 不符条数=0` | 0 |
| 聚焦回归（5 件既有用例） | `runId=7e46a5bb-64dd-470d-bd04-6d0eec1429e7`；`ℹ tests 28 ／ pass 27 ／ fail 1`（那 1 条红在别票，见缺陷 4） | 1 |
| 那条红单独重跑 | `runId=4492cf6e-befd-4314-9bb0-e382242be292`；`tests 5 ／ pass 4 ／ fail 1`（同一处） | 1 |
| 告警线台账门 | `runId=203d8c42-7ea2-4993-974c-354a7f30e18e`；`RESULT: 83/87`（红行全在别票，见缺陷 3） | 1 |

### 3.1 4 件逐件读数（临时库：`mkdtemp` ＋ `SKILLS_DB_PATH` ＋ `seedFull()` ＋ `CALORIE_TODAY=2026-09-07`）

| 命令 | 参数 | exit | 字节 | doctype | charset | `<style` | ilife-page | 结论句里的真读数 | 读数对照 |
|---|---|---|---|---|---|---|---|---|---|
| `calorie.view.goal` | `{"window":"7d"}` | 0 | 73411 | ✓ | ✓ | ✓ | ✓ | OK | 与改前同／与现算同 |
| `calorie.view.goal` | `{"window":"30d"}` | 0 | 73405 | ✓ | ✓ | ✓ | ✓ | OK | 与改前同／与现算同 |
| `calorie.view.goal-vs-actual` | `{"window":"30d"}` | 0 | 82733 | ✓ | ✓ | ✓ | ✓ | OK | 与改前同／与现算同 |
| `calorie.view.goal-expiring` | 无参 | 0 | 70421 | ✓ | ✓ | ✓ | ✓ | OK | 与改前同／与现算同 |

「读数对照」两列是两层互相独立的核对：一层对**改前历史值**（第一轮复核报告 §2.3 在实现之前实测的数），
一层对本席**现算**的取数层结果（同一个临时库、同一条 `defaultRange` 窗口，本席自己写的那段取数）。

另外核过：`data.output` 指向的文件与本席用 `--html` 指定的那份产物**内容逐字节同一**（实施者用例只比了长度）；
`<meta charset=` 落在前 1024 字节内（浏览器只认这一段）。

### 3.2 页面八样在位（逐件在盘上数出来，不看实施者的话）

| 件 | 页框 | 唤醒词那一行 | 类型徽章 | 结论句 | 页内导航 | 读数卡 | 表 | 口径行 | 来源脚注 | 复制数据 | 复制日志 | 命令原文 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| goal-7d | ✓ | ✓ | ✓ | ✓ | ✓（三锚点，无悬空） | 6 张 | 2 | 6 | ✓ | ✓ | ✓ | ✓ |
| goal-30d | ✓ | ✓ | ✓ | ✓ | ✓（三锚点，无悬空） | 6 张 | 2 | 6 | ✓ | ✓ | ✓ | ✓ |
| vs-30d | ✓ | ✓ | ✓ | ✓ | ✓（三锚点，无悬空） | 4 张 | 2 | 5 | ✓ | ✓ | ✓ | ✓ |
| expiring | ✓ | ✓ | ✓ | ✓ | ✓（两锚点，无悬空） | 4 张 | 1 | 6 | ✓ | ✓ | ✓ | ✓ |

（读数卡那一列是本席从产物里数出来的卡件数；计数里另含样式段里的 1 处同名规则，已剔除。
页内导航的锚点逐个对 `<section id=…>` 核过，没有指向不存在锚点的项。可见文本零内部词：
走仓内 `test/visible-text-probe.mjs` 的四类机器话判据，4 件全部零命中。）

### 3.3 契约面（不许动的键名／参数名／退出码）

| 场景 | 实测 | 期望 |
|---|---|---|
| `view.goal` 传 `start` 晚于 `end` | exit 2，stderr `ERR 2: start 不得晚于 end` | 2 ✓ |
| `view.goal-vs-actual` 传 `start` 晚于 `end` | exit 2，同上 | 2 ✓ |
| 空库 `view.goal` | exit 4，`ERR 4: 取数失败（缺失阻断）：未设营养目标…` | 4 ✓ |
| 空库 `view.goal-expiring` | exit 4，`ERR 4: 取数失败（缺失阻断）：无到期目标…` | 4 ✓ |
| 空库 `view.goal-vs-actual` | exit 4，同上 | 4 ✓ |

（未知 `window` 值走缺省 7 天窗、不报错——这是 `shared/params.ts::windowRange` 的既有口径，
本票一行未动，本席只记录、不作判据。）

---

## 四、本席自设探针 ＋ 受控变异（票面第四节第 3 条）

### 4.1 探针与实施者用例的四处不同

实施者那份用例本席读完了，它的四条形状断言与 metrics 反向锁都在。本席另写探针
`.scratch/t254-R/probe.mjs`，咬它咬不住的四处：① charset 必须落在前 1024 字节；② `data.output`
指向的文件内容要与实跑产物逐字节同一；③ 结论句里的数必须是信封里的数（它只查「结论：」五个字在不在）；
④ 读数两层独立核对（对改前历史值 ＋ 对本席现算）。

**如实记一条自红**：探针第一版（`runId=6de8bca0`，exit 1）把判据写严了——本席要求「即将到期」那页的
结论文里带体重目标与热量目标，而那两件按版式住在读数卡里（票面要的是「结论句含本页读数」，
不是每个数都塞进那一句）。改判据（结论句查窗口读数、其余读数查全页可见文本）后复跑即绿。
这是本席探针的错，不是产物的缺陷。

### 4.2 两发受控变异（同一个持锁窗口 `runId=2dc37d31` 内做完并逐字节还原）

| 变异 | 改哪一行 | 实施者用例 | 本席探针 | 读数 |
|---|---|---|---|---|
| **A · 屏上读数改坏**（结论句里的日均摄入写死 999，信封 `data.metrics` 一个数不动） | `resultDocs.ts` 的 `goalSummaryOf` | **exit 0（仍绿 —— 它看不见）** | **exit 1（红）** | `PROBE-FAIL goal-7d：结论句里缺了这几个真读数：477（屏上读数与信封读数走散）` |
| 还原 A | 逐字写回 | exit 0 | exit 0 | `RESTORE A byte-identical=true` |
| **B · 出口退回片段** | `read.ts` 的 `goal` 出口返回 `<section class="ilife-page">片段</section>` | exit 1（红） | exit 1（红） | `PROBE-FAIL goal-7d：产物 不是以 <!doctype html> 起` |
| 还原 B | 逐字写回 | exit 0 | exit 0 | `RESTORE B byte-identical=true` |

末行 `MUTATION-RESULT red-then-green=OK problems=0`。
变异 A 是本轮最要紧的一条**反向对照**：它证明实施者那份用例在「版式对、信封对、但用户眼前那个数错了」
这一档上**没有牙**（缺陷 2），同时证明本席的探针咬得住这一档。变异 B 证明形状回退两份判据都咬得住。

### 4.3 改前真身复现（不看实施者自留的读数）

本席把 `47a5215^` 那份 `read.ts` 写回盘上、编译、实跑、再逐字节还原（`runId=0722d422`）：

| 件 | 改前（父提交那份） | 改后（现行树） | metrics |
|---|---|---|---|
| goal-7d | exit 0／1779 B／doctype ✗／charset ✗／style ✗ | exit 0／73411 B／✓／✓／✓ | 逐字段相同 |
| goal-30d | exit 0／1779 B／✗／✗／✗ | exit 0／73405 B／✓／✓／✓ | 逐字段相同 |
| vs-30d | exit 0／1357 B／✗／✗／✗ | exit 0／82733 B／✓／✓／✓ | 逐字段相同 |
| expiring | exit 0／1203 B／✗／✗／✗ | exit 0／70421 B／✓／✓／✓ | 逐字段相同 |

改前那四件与本席无关的旁证：实施者自留的 `.scratch/t254/before.json` 与本席实跑的**字节与 metrics 都逐字对得上**
（他不是事后补的数）。还原后 sha256 自证 `byte-identical=true`，两件源码的 blob 哈希与 `HEAD` 相同（见二）。

### 4.4 「片段 → 整页」有没有丢用户看得见的事实

拿改前那份片段与改后那份整页的**可见文本**对账（`runId=46774810`）：改前有而改后没有的词共 16 个，
逐条看过——都是**换了说法**或**本该消失的内部词**，没有事实丢失：
`30d完成 0/9` → 「达标天数 0 天」＋「近 30 天里有记录 9 天」；`预计 1.41 kg` → 「预计减重 1.41 kg」；
`目标完成率 0%` → 「近 30 天完成率 0%」；`缺口趋势 loss` → 「缺口在变大」（旧页把内部取值 `loss` 印上了屏，
新页换成人话，是改善）；`摄入均值` → 「本窗日均摄入」；`未到期／剩余／窗口` → 「还没进提醒窗口／剩余天数／提醒窗口」。

---

## 五、缺陷清单

| # | 缺陷 | 类别 |
|---|---|---|
| 1 | 证据件第三段那条 `exit=1` 废跑的**成因挂错了 runId**，且漏登一条真废跑：台账里 `runId=5ffbcd72-…`（13:41:59）才是「四个路径误写进 `-m` 引号内」那条（命令原文里四个路径确实都在引号内）；证据件点名的是 `runId=1326f503-…`（13:44:40），它的命令原文里路径已在引号外、失败另有原因（它跑在 `git add` 前 11 秒，`resultDocs.ts` 当时还没进暂存）。两条都是 exit 1、都没产生提交，「未产生提交、改正后重跑成功」的结论仍成立 | **本票范围**（S3：机器读数与陈述不符） |
| 2 | 验收用例对「**屏上读数＝信封读数**」没有牙：它只查「结论：」五个字在不在；变异 A（结论句写死一个错数、信封不动）下它仍绿。本席探针咬得住。这不是实现缺陷（现行实现屏上的数就是真数），是用例强度不够 | **本票范围**（S3：用例覆盖洞，建议收口或后续票补一条断言） |
| 3 | 告警线台账门当刻红（`RESULT: 83/87`，`runId=203d8c42`），红行**全部指向别票在途件**：`src/render/dietDocs.ts` 漏报（LF=351）、`src/render/sportPortDocs.ts`／`src/home/goalProgressDocs.ts`／`src/render/sportDocs.ts` 台账陈化。实施者当时那条（`runId=53e2fc47`，`exit=0`，86/86）在台账里查得到、为真；新增的 `resultDocs.ts`（270 LF）未越线、不挂号，与本票无关 | **范围外发现**（S3） |
| 4 | `packages/skill-calorie/test/cmd-read-t11.test.mjs:143` 对主页 HTML 里「今日总览 ＜日期＞」的断言自 `73f482a`（`feat(401m)` 主页五条词按视图分，2026-09-15 15:59，**早于本票提交 5 小时 45 分**）起就陈化：实跑主页 h1 已是「近 7 天总览」。本席重跑一次仍红，归主页域，不是本票的地盘 | **范围外发现**（S3） |
| 5 | 用户 2026-09-15 的 #560 裁决「所有 HTML 页面底部的『数据来源：xxx』都删掉」与本票这一轮的八样里「来源脚注」相抵（本票 4 页底部的 `📊 数据来源：…` 仍在）。本票照票面做对了，两票口径谁收口请编排者裁 | **范围外发现**（S3） |
| 6 | 页头唤醒词：`calorie.view.goal` 一条命令挂两条词（7 天窗「看目标完成度」／30 天窗「看目标历史完成」），命令这一层收不到唤醒词，故 7 天窗与 30 天窗两页共用「看目标完成度 · 目标管理」那一行。实施者已如实登记，票面这一轮也没要求按窗口分词 | **范围外发现**（S3／下一手） |

**S1 逐条核过，一条不成立**：契约没有被违反（4 件判定全绿，参数与退出码照旧）；没有用户可见的回归
（改前那些事实逐条还在，见 4.4）；没有证据造假或私自放宽（12 条 runId 全在台账里、逐条对得上，
唯一一处是缺陷 1 的挂错，属陈述错、不属把红说成绿）；没有数据丢失风险（本票只改页面装配，
取数表达式是 diff 的上下文行，改前改后 metrics 逐字段相同）。

---

## 六、未做项与下一手缺什么

1. **视觉验收墙**没做（`docs/agents/视觉验收墙.md` §2：墙归收口票——生成器要入仓、产物册子要在地图切票时冻结，
   两样都在本票写集之外）。本席只做了产物层面的机械读数（八样在位、锚点不悬空、可见文本零内部词、
   窄屏样式段来自共享页壳），**挤／塌列／被藏起来的横滚这一类要靠墙与逐格缺陷清单**，请收口票按墙走。
2. **整包回归**没跑（票面不许 `pnpm build`／`pnpm gen`）。本席跑了 5 件与本次改动面相接的既有用例
   （`render-t41`／`cli-smoke-t41`／`cmd-read-t11`／`db-readonly-93`／`goal-weight-deadline-548`）：
   28 条里 27 条绿，那 1 条红归主页域（缺陷 4）。**整包留给收口窗**。
3. 缺陷 3／5 要不要由本票补，请编排者裁（都不在本票写集：一个是别票在途件，一个是用户后续裁决）。
4. **未 push**（票面明令）。

---

## 附：第一轮判决（历史）

第一轮判 FAIL、0 分，三条红（原文见提交 `848d8ea`）：① 验收用例缺席，票面命令直接 exit 1；
② 4 件产物仍是片段（doctype／charset／style 三条全黑，字节 1779／1779／1357／1203）；
③ 无提交、无证据件。本轮这三条逐条复核：① 用例在、本席新跑 exit 0；② 4 件全绿；
③ 提交在（`47a5215` ＋ 两次补文），证据件在。三条全部消解。
