# #254 独立复核 —— 目标类 4 件片段结果页变完整文档

复核者：第三阶段独立复核席（不是实施者；实施者结论一律不采信，本页每条读数都是本席重跑出来的）。
复核时刻盘面：`HEAD=506bdca`（`feat(269)` 那条），工作区里 `src/goal/` 下只有别票的 `goalStore.ts`／`write.ts` 在改与未跟踪的 `receipt.ts`（属 #253／#548 地盘，本席一字未碰）。

---

## 一、判定

**FAIL —— 本票没有交付物。**

票面要求：把 4 个命令的页面装配换成**完整文档**（页框＋页头＋结论句＋页内导航＋读数卡／表＋口径行＋来源脚注＋复制区双按钮），取数口径不动。

实况：这 4 个命令仍走**旧的片段渲染件**，一件都没换。全仓范围内**找不到本票的任何实现痕迹**：

| 找的东西 | 结论 | 依据 |
|---|---|---|
| 验收用例 `packages/skill-calorie/test/goal-result-254.test.mjs` | **不存在** | 直接跑验收命令：`Could not find '…/goal-result-254.test.mjs'` |
| 实现提交 | **不存在** | `git log --all --reflog --grep=254` 只命中 #97／#101 两条无关文档；`git log --oneline -6 -- packages/skill-calorie/src/goal/` 最新是 `ac5cfa4`（#338，2026-09-14） |
| 新增文档装配件 | **不存在** | `git ls-tree -r master` 搜 `goalResult|goalDoc254|t254` 无命中 |
| changeset／交付文档 | **不存在** | `.changeset/` 与 `docs/` 下都没有 #254 的件 |

接线点 `packages/skill-calorie/src/goal/read.ts` 的 4 处仍是旧片段出口（第 38／79／100 行）：

- `viewGoal` → `renderGoalHtml(v)`（`src/render/html.ts:106`）
- `viewGoalExpiring` → `renderGoalExpiringHtml(v)`（`src/render/html.ts:562`）
- `viewGoalVsActual` → `renderGoalVsActualHtml(v)`（`src/render/html.ts:584`）

对照同目录**已整页化**的姊妹键 `viewGoalWeight`（第 148 行）——它走 `buildGoalWeightDoc(g, command)`。同一件里一条整页、三条片段，差的正是本票该做的那一步。

---

## 二、机器证据

### 2.1 验收命令（本席新跑，不是抄实施者的）

```
GATE-RUN runId=b07fc95c-4952-47f9-8d4b-ee7eb2086bd2 cmd=node --test packages/skill-calorie/test/goal-result-254.test.mjs
```

摘要行 `RESULT: ticket=254-R runId=b07fc95c-… waitedMs=30015 exit=1`
—— 退出码 **1**，原因是 `Could not find 'packages/skill-calorie/test/goal-result-254.test.mjs'`（用例件缺席）。
日志：`.scratch/t254/review-accept.log`

### 2.2 本席自设探针：4 个命令逐条真跑（临时种子库 ＋ 真 CLI）

探针 `.scratch/t254/review-probe.mjs`：`mkdtemp` 目录 ＋ `SKILLS_DB_PATH` 指向它 ＋ `seedFull()`
＋ `CALORIE_TODAY=2026-09-07`（`SEED_TODAY`）。判据＝票面「产物＝完整文档」四断言。

```
GATE-RUN runId=0f0b815d-95ca-4961-ae77-6d8e9f815c35 cmd=node .scratch/t254/review-probe.mjs
```

摘要行 `RESULT: ticket=254-R runId=0f0b815d-… waitedMs=530462 exit=1` —— 退出码 **1**。
日志：`.scratch/t254/review-probe.log`

| 命令 | 参数 | exit | 字节 | doctype | charset | style | ilife-page | 结论 |
|---|---|---|---|---|---|---|---|---|
| `calorie.view.goal` | `{"window":"7d"}` | 0 | 1779 | ✗ | ✗ | ✗ | ✓ | 片段 |
| `calorie.view.goal` | `{"window":"30d"}` | 0 | 1779 | ✗ | ✗ | ✗ | ✓ | 片段 |
| `calorie.view.goal-vs-actual` | `{"window":"30d"}` | 0 | 1357 | ✗ | ✗ | ✗ | ✓ | 片段 |
| `calorie.view.goal-expiring` | （无参） | 0 | 1203 | ✗ | ✗ | ✗ | ✓ | 片段 |

`PROBE-RESULT red=4/4 seedToday=2026-09-07` —— 4 个命令全部红在 doctype／charset／style 三条。
字节与票面清单 §二第 3 条登记的数（1779／1779／1357／1204）一致（第 4 条差 1 B，是 `expiring.html` 换行口径的尾差，不影响判读）。

产物头 90 字节仍是那层裸区块，没有文档骨架：

```
head="<section class=\"ilife-page\" data-skill=\"calorie\" data-slot=\"ilife:calorie:goal\" style=\"background:#16181d;color:#e6edf3;"
```

### 2.3 取数口径对照（本席对「一个字不许变」的检查）

把票面登记的**改前** metrics 与本次实跑逐字段对：

| 命令 | 改前（票面／`.scratch/t254/probe.log`） | 本次实跑 | 对照 |
|---|---|---|---|
| `calorie.view.goal` 7d | `weeklyDeficit=10841, predictedLossKg=1.41, avgDeficit=1549, avgIntake=477, trendAvg=477, completedCount=0, incompleteCount=9` | 逐字相同 | **相同** |
| `calorie.view.goal` 30d | `weeklyDeficit=20276, predictedLossKg=2.63, avgDeficit=676, avgIntake=158, trendAvg=158` | 逐字相同 | **相同** |
| `calorie.view.goal-vs-actual` 30d | `completedCount=0, incompleteCount=9, completionPct=0, trendAvg=158, calorieGoal=1800` | 逐字相同 | **相同** |
| `calorie.view.goal-expiring` | `daysLeft=107, withinDays=14, expiring=0, weightGoal=68, calorieGoal=1800` | 逐字相同 | **相同** |

取数口径**没有被改坏**——因为这一票根本没动过这些件。这不是「守住了口径」的功劳，是「没开工」的结果。

### 2.4 探针鉴别力阳性对照（证明 2.2 不是恒红）

同一个判据打**已整页化**的姊妹命令 `calorie.view.goal-weight`（#390 先例）：

```
GATE-RUN runId=50a9e671-46c2-4b06-b1ee-995ca688b141 cmd=node .scratch/t254/review-control.mjs
```

摘要行 `RESULT: ticket=254-R runId=50a9e671-… waitedMs=310220 exit=0` —— 退出码 **0**。
读数：`checks={"exit0":true,"doctype":true,"charset":true,"style":true,"ilifePage":true} RED=[]`
→ `CONTROL-RESULT GREEN(探针有鉴别力)`。日志：`.scratch/t254/review-control.log`

**两条机器读数**（本票要求的「红／还原一致」）：本票没有可还原的变异（无实现件可改），故以**鉴别力对照**替代——
4 个目标命令 **红 4/4**，同一探针打已整页化的对照命令 **绿 1/1**。探针能区分「整页」与「片段」，2.2 的红不是判据写错。

---

## 三、复核三件事的着落

1. **圈定范围**：**无提交可核**。`git show --stat <commit>` 无从执行——本票没有提交，也就没有「改动件超出写集」这回事。`git diff HEAD -- <写集路径>` 同理：`src/goal/read.ts` 与 `src/render/html.ts` 相对 HEAD **零改动**。写集外件被本席改动的数：**0**。
2. **独立重跑**：已做，见 2.1／2.2，都是本席新 runId（`b07fc95c…`／`0f0b815d…`／`50a9e671…`）。
3. **新试法**：本席自设探针见 2.2（4 命令 × 4 断言，含绝对路径与文件存在性）＋ 2.4 鉴别力对照。
   受控变异**没做成**，原因如实记：变异的前提是有一个能改的实现件，本票没有；把变异打在别票的在途件（`goalStore.ts`／`write.ts`／`receipt.ts`）上既越写集又踩别人的窗口，故不做，改用鉴别力对照。

---

## 四、缺陷清单

| # | 缺陷 | 类别 |
|---|---|---|
| 1 | 验收用例 `test/goal-result-254.test.mjs` 缺席，票面给的验收命令直接 `exit 1`，本票无法验收 | **本票引入**（核心交付物缺失，S1） |
| 2 | 4 个命令的装配一行未改，产物仍是片段（doctype／charset／style 三条全红），用户可见结果与票面要求不符 | **本票引入**（S1：用户可见的缺陷未被修） |
| 3 | 无提交、无 changeset、无交付回执，票面「第二段机器证据」要求的东西一样也没有 | **本票引入**（证据缺失） |
| 4 | 段 2.2 表里 `goal-expiring` 实跑 1203 B 而票面清单登记 1204 B（1 B 尾差，应是换行口径） | **范围外发现**（S3，只记不判） |

**打分：0 / 100**

| 维度 | 分值 | 得分 | 依据 |
|---|---|---|---|
| 契约一致 | 30 | 0 | 4 条命令的产物判据全红，契约要求的完整文档一条不满足 |
| 证据真实可复现 | 25 | 0 | 没有交付证据；验收命令指向的用例件不存在 |
| 新旧对照 | 20 | 10 | 取数口径经实跑确认与改前逐字段相同（2.3），但这是没开工的结果，不给满分 |
| 工程红线 | 15 | 15 | 本席核查：无 `git add -A`／无真库直连／无并行抢锁；未发现违反红线的动作 |
| 文档同步 | 10 | 0 | 无交付文档、无 changeset |

---

## 五、下一手缺什么

这 4 个命令的整页化**一行都没做**。接手的实施席需要：

1. 新建文档装配件（照 `src/goal/goalWeightDoc.ts` 与 `src/shared/docPage.ts::assembleDocPage` 的形状），
   把 4 个命令的产物补齐票面那八样：页框／页头（唤醒词 ＋ 类型徽章）／含本页读数的结论句／页内导航／读数卡与表／口径行／来源脚注／复制区双按钮。
2. 改 `src/goal/read.ts` 的 4 处 `html:` 出口（第 38、79、100 行）转调新件；`src/render/html.ts` 里那 3 个旧片段渲染件**留着**（票面明写本票不删）。
3. 取数一行不动：`buildGoalView`／`buildGoalExpiringView`／`buildGoalVsActualView` 与 analysis 层照旧。
4. 按 `test/goal-wizard-251.test.mjs` 的形状补 `test/goal-result-254.test.mjs`，并把「改前 metrics 逐字段不变」写成反向锁（否则只有正向断言，仍是可自我满足的用例）。
5. 页内不得出现内部词（`op=`／`id=1`／函数名那一类）。

复核席建议：新用例至少留一条**反向对照**（像 2.4 那样，用同一探针打一个已知整页的键），否则「四断言全绿」在实现缺失时也可能因用例本身写得松而白过。
