# #520 视觉重评席 V1：页 01–08 逐页判定

本席只评 `.scratch/t387/html/` 里的**页 01–08**（预测体重族 6 页 ＋ 模拟减重族 2 页）。评的是**冻结产物**
本身，分数与结论逐条对着产物源码核过；源码里找不到对应处的怀疑一律写「源码未证实」且**不作扣分依据**。

## 〇、尺（全批统一，不许自定）

| 维 | 满权 | 量什么 |
|---|---|---|
| 结构纪律 | 20 | 页壳／卡片／表格是不是真结构（真 `<table>` 或真卡片），网格是否成列，断点是否成档，有无伪表（纯 `<div>` 拼列） |
| 信息层级与版式细调 | 25 | 第一眼层级、间距取 4/8 倍数、字号成档有下限、数值列等宽右对齐、卡／表／图左缘共线、留白有节 |
| 分隔符与文字纪律 | 20 | 分号／间隔号／竖线拼串处数、同一事实重复处数、英文内部标识符／库表名／常量名上屏处数 |
| 跨宽自适应 | 15 | 1440 ／ 768 ／ 390 三档无溢出、无塌列、无截断、无意外横滚 |
| 手机端与 HELP 同档 | 20 | 对照 `fixtures/help-instances/卡路里_HELP_20260731_201530.html` 的断点与窄表堆叠做法；触摸目标 ≥44px；表格窄档读得开；日期不被折断 |

**加权总分 ＝ 结构×0.20 ＋ 信息×0.25 ＋ 分隔符×0.20 ＋ 跨宽×0.15 ＋ 手机端×0.20**，逐页给分。
五维**各自按百分制打分**（权重×100 就是该维的满权分：结构 20／信息 25／分隔符 20／跨宽 15／手机端 20，
五项满权合计 100），故加权总分与「各维按满权折分后相加」同值，逐页把两条算式都写出来便于复核。

**参照件（HELP 201530）实测口径**：`.container{max-width:960px;padding:0 12px 40px}`；
断点两档 —— `@media (max-width:600px)`（正文降到 `12.5px`，容器 `padding 0 8px 60px`）与
`@media (min-width:601px) and (max-width:900px){.container{max-width:720px}}`；
该件**全页无 `<table>`**，窄表堆叠无从照抄 —— 故本批「与 HELP 同档」按**断点层级 ＋ 手机档字号下限 ＋
容器留白节律**三项比，窄表另按「窄档读得开」独立量。

## 一、冻结产物身份（本席所有读数都对着这一份）

目录 `.scratch/t387/html/`，八件同批冻结（mtime 2026-09-16 05:14:42–43）。本席的机器读数与截图都对着下表这一份：

| 页 | 文件 | 字节 | SHA256（前 16） |
|---|---|---|---|
| 01 | `01-预测体重(1周后).html` | 71,824 | `C24FF66B28B79B09` |
| 02 | `02-预测体重(1月后).html` | 71,835 | `796A39AE7EE50351` |
| 03 | `03-预测体重(3月后).html` | 71,835 | `0B4771C56CC10E09` |
| 04 | `04-预测体重(6月后).html` | 71,840 | `F634220E7470E837` |
| 05 | `05-预测体重(自定义时间).html` | 71,823 | `AC7C85ACA7742EBC` |
| 06 | `06-预测体重(自定义目标).html` | 71,710 | `72C5C34BEF053D0D` |
| 07 | `07-模拟减重(每天-300卡).html` | 76,071 | `0B87AD30889DE82B` |
| 08 | `08-模拟减重(每天-500卡).html` | 76,071 | `4DC63A2E3599F58D` |

**机器读数件（本席自己跑的，不在别席写集内）**：

| 件 | 命令（持锁跑，ticket=520） | 用途 |
|---|---|---|
| `.scratch/t520-V1/sep.json` | `node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t520-V1/pages --json …` | 分隔符／英文标识符／≥3 段并列（节点级） |
| `.scratch/t520-V1/resp.json` | `node packages/skill-calorie/scripts/measure-responsive.mjs --dir … --widths 390,768,1440 --json …` | 横向溢出、被切、横滚容器、图放不下 |
| `.scratch/t520-V1/fmt.json` | `node docs/skills/skill-calorie/t516-判据-版式.mjs --dir … --widths 390,768,1440 --json …` | 逐元素越界、字号阶梯与下限、同排卡等高、参数表单列、触摸小件、表头／单元格计算样式、页壳几何 |
| `.scratch/t520-V1/probes.log` | `node tooling/run-locked.mjs --ticket 520 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000 -- node .scratch/t520-V1/run-probes.mjs` | 三件的持锁跑记录（`runId=bf3e4bff…`，`exit=0`） |

**全批共同读数（八页三档全绿，逐页复述时不再重跑）**：`audit-separators` 八页 `命中=0`
（`·`／`；`／`｜`／`、`／`~` 全 0，`标识符 0`）；`measure-responsive` 8×3=24 格
`ok=true 溢出=0 被切=0 横滚容器=0 放不下的图=0`；`t516` 8×3=24 格 `docScrollW == vw`、`越界=0`、`触摸小件=0`。

**视觉侧驳回记录（视觉模型是提示器、不是证据）**：1440 档整页截图读图两次报「内容只占左侧约 70%、
右半区空白、未居中」，`t516` 读数实测 `shell x=160 w=1120 right=1280 padding=20px/20px` ⇒ 页壳
**是**居中的（1440−1120＝320，左右各 160），该读法**驳回**，不作扣分依据。

**八页逐页核过的共同缺陷（各页结构纪律维已各扣 1 分）**：八页页上**各有一个永远按不动、
且页上一句解释都没有的按钮** —— `<button type="button" class="ilife-copy-btn ilife-copy-btn-ghost"
data-action-id="ilife-copy-log" disabled>复制日志</button>`（八页逐页 `disabled` 按钮计数＝1、
文案「复制日志」；`ilife-empty` 只命中 `<style>` 段、页上元素 0；可见文本里没有任何
「日志为空」的说明）。

**八页逐页核过的共同形状（不另计分，只因它是同一处公共层决策）**：页脚口径行
`:1177-1192` `.ilife-block-caliber:has(>span){display:flex;flex-wrap:wrap;gap:4px 12px}` ＋
`> span + span{padding-left:8px;border-left:1px solid rgba(210,210,215,.6)}` —— 三段并列信息在
1440 档由**两条发丝线**串成一行（读图多处把它读成竖线 `|`），390 档换成换行堆叠。
`#505` 豁免 ＋ `t514-口径行去分隔符.md:47-51` 已裁定「分隔由版式承担」，本席按「读者眼里仍是一条
竖线串」在各页分隔符维**记轻**，不单列重扣。

## 二、逐页判定

### 页 01 · 预测体重(1周后)

**产物**：`.scratch/t387/html/01-预测体重(1周后).html`，71,824 字节，SHA256 前 16 `C24FF66B28B79B09`。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 清楚。眉标「预测体重」`p.ilife-block-page-shell-eyebrow` → H1「体重预测（7 天）」`h1.ilife-block-page-shell-title`（28px）→ 三枚 `span.ilife-block-chip`（卡路里／7 天／趋势分析）→ 结论块 `p.ilife-block-conclusion`（15px／600，蓝左边框 3px）→ 目录 `nav.ilife-block-toc` → 三节 `h2.tpd-sec-title`。转写层级：主标题 > 结论句 > 概览卡数值（22px）> 卡标签（12px）> 小节标题（16px）。**源码未证实**：读图说「底部脚注三段用竖线 `|` 分隔」，源码 `:1177-1192` 是 `span + span{border-left:1px solid rgba(210,210,215,.6)}` —— 角色是 `border-left` 发丝线，不是 `｜` 字符（文本层无竖线，见 ③）。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | 无。`fmt.json`：390／768／1440 三档 `docScrollW=390/768/1440 == vw`、`越界=0`、`bodyScrollW==vw`；`resp.json` 三档 `溢出=0 被切=0 横滚容器=0 放不下的图=0 why=""`。概览卡三档 `极差=0`（390：2卡×173 高100；768：2卡×362 高100；1440：4卡×261 高100）。**留白**：页壳高 390→1070／768→1006／1440→777，正文底 1014／950／710，1440 档一屏放得下（fullPage 实拍即 900＝视口）。读图报「桌面右半区大片空白」**驳回**（页壳实测居中，见 §一）。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中：`audit-separators` 本页 `node·0 ；0 ≥3段0 ｜0 、0 ~0 标识符0 命中=0`。**版式层半命中**：页脚口径行 `:1874` 三枚 `<span>`（体重＝当前值加日速率乘天数 ／ 预计区间按体重残差向外放宽，取 95% 置信带 ／ 体重记录不到 14 天不出预测）由 `:1189-1192` 的 `border-left` 发丝线串成一行 —— 读者眼里仍是一条竖线串（读图两次都把它读成 `|`）。此形按 `#505` 豁免、`t514-口径行去分隔符.md:47-51` 已裁定「分隔由版式承担」，非本批回归，**记轻**。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **有一处真冗余**：`:1847` 参数说明「体重记录不到 14 天就只说数据不够，不编预测。」与 `:1874` 页脚口径「体重记录不到 14 天不出预测」—— 同一条规则一页讲两遍（探针 `probe-text.mjs` 6 字片段 `体重记录不到 14` **×2**）。**半冗余一处**：「95% 置信带」既在「预计区间」卡副文案 `ilife-block-kpi-card-detail` 又在页脚口径第 2 枚 span。**英文上屏零命中**：静态可见文本里只有 `kg`×5（单位）与复制按钮的 `JSON`／`CSV`（通用格式名，非内部标识符），`audit-separators` 标识符读数 0。**未记**：「结论句 74.8 kg／7 天后」与「预测」卡同值，属结论前置的既定形状。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | 等宽数字已达标：`:1116` 页壳 `font-feature-settings:"tnum"`、`:1346` `.ilife-block-kpi-card-value{font-variant-numeric:tabular-nums}`。**本页无 `<table>`**（标签计数 `table:0`；`ilife-block-data-table:0`），故无「数值列右对齐」可量；四张 KPI 值 22px 等档左对齐，单位 `ilife-block-kpi-card-unit` 13px 跟随。**缺值**：本页四张卡全有值，无 `—` 可核 —— 缺值口径留给有缺值的页（见页 06）。小数位：当前 75.1／预测 74.8／速率 −0.34 一位小数，区间 74.7 至 74.8 一位小数，**一致**。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | 本页无表，无可读性风险。触摸：`fmt.json` 390／768／1440 三档 `触摸小件=0`；逐条源码 `:1159-1172` `.ilife-block-toc a{min-height:44px}`、`:1821` `.ilife-block-page-shell summary{min-height:44px}`、`:1826` 窄档页壳内距 `20px 16px 48px`。日期：`probe-text.mjs` 可见文本「2026-09-03」「2026-09-16」单行（`label.ilife-block-param-form-field` 在 390 档宽 358px，`probes` 三档 `被切=0`）；读图 390 档亦报两个日期均单行未折。 |

**五维打分**（每维百分制；「满权」＝权重×100）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里（逐条对源码／读数） |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 真结构齐全（`:1847` `section#sec-params/#sec-overview/#sec-data` ×3 ＋ `h2.tpd-sec-title` ×3 ＋ `nav` ＋ 卡片网格 `grid-auto-rows:1fr` 实测 `极差=0` ＋ 参数表单 1440 三列 `352px×3`／窄档单列）。**扣 5**：节点名 `id="sec-data"`／`h2` 写「数据与日志」，该节唯一内容是 `ilife-block-copy-block`（复制数据／复制日志），**页上零数据行、零日志行**（`ilife-block-data-table:0`）—— 节名与内容不符。**扣 5**：断点不成档 —— 全页 `@media` 21 条吃 `400/640/720/820/821/875/876/641` 八套值，页级主断点 `:1825 @media (max-width:820px)` 单值无配对档。 **扣 1**：页上有一个**永远按不动、页上一句解释都没有**的 `disabled` 按钮（`<button … class="ilife-copy-btn ilife-copy-btn-ghost" data-action-id="ilife-copy-log" disabled>复制日志</button>`；8 页逐页已核，各页 `ilife-empty:0`、可见文本里无「日志为空」的说明）。 |
| 信息层级与版式细调 | 0.25 | 25 | **88** | 22.0 | 层级清、卡等高、左缘实测共线（读图 390 档核：「四张卡最左缘、四个小标题、上方『概览』标题落在同一条竖线」；源码 `:1843` `.ilife-block-page-shell .ilife-block-data-table{margin-left:0}`）。**扣 7**：间距非 4／8 倍数 —— `probe-spacing.mjs` 扫全 `<style>` 段 88 处／26 种，本页**上屏**的即 `:1321` `.ilife-block-kpi-card{padding:14px}`、`:1335` 值行 `gap:6px`、`:1253-1254` `.ilife-block-chip{margin:0 6px 6px 0;padding:2px 8px}`、`:1127` 标题 `margin:6px 0 0`、复制菜单 `gap:14px`／`padding:10px`。**扣 3**：1440 档正文仅 604px、页壳 777px，一屏三段相对 1080px 宽列偏松（观感债，非缺陷）。**扣 2**：概览四张卡里「预计区间」卡的值串最长（`74.7 至 74.8` ＋单位 `kg`），与另三张同宽 261px 同压一行。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 文本层 `命中=0`（机器读数，见 §一）；无 `；`／`·`／`｜`／`、`；无英文内部标识符、库表名、常量名。**扣 5**：真冗余 1 处（体重记录不到 14 天 ×2）＋半冗余 1 处（95% 置信带 ×2）＋口径行三段发丝线串行（版式承担，记轻）—— 三项合扣 5。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档 `docScrollW==vw`、`越界=0`、`溢出=0／被切=0／横滚容器=0`；列数 390→2+2／768→2+2／1440→4，无塌列；页壳 1440 居中（`x=160 w=1120 right=1280`）。无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸三件照 HELP（`:1819` `-webkit-tap-highlight-color:transparent;touch-action:manipulation`）；触摸目标三档 `触摸小件=0`；字号下限 390 档 12px（`fmt` `minFontPxNoSvg=12`，责任元素 `p.ilife-block-page-shell-eyebrow`／`span.ilife-block-chip`）≥ 批内 11px 下限，且高于 HELP 201530 的最小 10px；无横滚、日期不折。**扣 5**：页内注释 `:1824` 自称「窄屏（820 · 同 HELP）」，而参照件 `卡路里_HELP_20260731_201530.html` 实测断点是 `@media (max-width:600px)` 与 `@media (min-width:601px) and (max-width:900px)` 两档，**820 ∉ {600,900}**（实用同档：窄档容器 736px vs HELP 720px，差 16px）。 |

**加权总分 ＝ 89×0.20 ＋ 88×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋22.0＋19.0＋15.0＋19.0 ＝ 92.8**
（折满权同值：17.8／20 ＋ 22.0／25 ＋ 19.0／20 ＋ 15.0／15 ＋ 19.0／20 ＝ 92.8／100）

### 页 02 · 预测体重(1月后)

**产物**：`.scratch/t387/html/02-预测体重(1月后).html`，71,835 字节，SHA256 前 16 `796A39AE7EE50351`。

**本页与页 01 的关系**：同族同模板，`fmt.json` 逐档几何**逐字相同**（`shell x/w`、`kpiRows`、`fontSizes`、`paramFormCols` 全同），差异只在内容：H1「体重预测（30 天）」、徽标「30 天」、结论「30 天后体重约 73.6 kg」、
`horizonDays=30`、KPI 预测 `73.6`／区间 `73.5 至 73.7`。故结构性结论继承页 01（同源码同 CSS），**逐条已按本页源码复核**。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 清楚，同页 01：眉标「预测体重」→ H1「体重预测（30 天）」→ 徽标（卡路里／30 天／趋势分析）→ 结论块「按最近的趋势，30 天后体重约 73.6 kg。」→ 目录 → 三节。数值层级：结论句 > 概览卡 `73.6`（22px）> 卡标签（12px）。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | 无。`fmt.json` 本页 390／768／1440 `docScrollW=390/768/1440`、`越界=0`；`resp.json` 三档 `溢出=0 被切=0 横滚容器=0`。读图（1440＋390 两张）逐条报「没有元素被切、没有横向滚动条、没有文字压字或重叠」。概览卡 `极差=0`（390：2卡×173 高100；768：2卡×362；1440：4卡×261）。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中：`audit-separators` 本页 `命中=0`（`·；｜、~` 全 0）。**版式层半命中**（与页 01 同源码 `:1189-1192`）：1440 档裁剪实拍确认页脚口径行三段之间**有两条细竖线**（读图定位：第 1–2 段之间、第 2–3 段之间各一条）；390 档读图报「分了三行、未见竖线」—— 源码层 `border-left` 仍在（flex 换行后每枚 span 左缘各有一条），**390 档未在实拍中复现**，故只记 1440 档一处，按 `#505`／`t514` 已裁定「分隔由版式承担」记轻。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **一处真冗余**（与页 01 同）：参数说明「…体重记录不到 14 天就只说数据不够，不编预测。」与页脚口径「体重记录不到 14 天不出预测」（`probe-text.mjs` 片段 `体重记录不到 14` **×2**）。**半冗余**：「95% 置信带」在「预计区间」卡副文案与页脚口径第 2 枚 span 各一次。**英文上屏零命中**：静态可见文本只有 `kg`×5、`JSON`×1、`CSV`×1（单位与复制格式名）；`audit-separators` `标识符0`。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | `font-variant-numeric:tabular-nums` 在册（`:1116` 页壳 `font-feature-settings:"tnum"`、`:1346` 卡值）；本页 `ilife-block-data-table:0`（无 `<table>`），无数值列可量。小数位：`75.1`／`73.6`／`-0.34`／`73.5 至 73.7` 全一位小数，**一致**。缺值：四张卡全有值，本页无 `—` 可核。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | 无表。触摸：`触摸小件=0`（三档）；源码 `:1159-1172` `.ilife-block-toc a{min-height:44px}`、`:1291-1294` 触摸三件、`:1821` `summary{min-height:44px}`。读图 390 档核：三颗导航胶囊横排一行、高度一致、未换行。日期：`2026-09-03`／`2026-09-16` 读图报**均单行未折**；`label.ilife-block-param-form-field` 390 档宽 358px，`被切=0`。 |

**五维打分**（每维百分制；「满权」＝权重×100）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 同页 01：`section` ×3 ＋ `h2.tpd-sec-title` ×3 ＋ `nav` ＋ 卡网格 `grid-auto-rows:1fr`（实测 `极差=0`）。**扣 5**：`h2` 写「数据与日志」，节内只有 `ilife-block-copy-block`，零数据行（`ilife-block-data-table:0`）。**扣 5**：`@media` 八套断点值不成档（400/640/720/820/821/875/876/641）。 **扣 1**：页上有一个**永远按不动、页上一句解释都没有**的 `disabled` 按钮（`<button … class="ilife-copy-btn ilife-copy-btn-ghost" data-action-id="ilife-copy-log" disabled>复制日志</button>`；8 页逐页已核，各页 `ilife-empty:0`、可见文本里无「日志为空」的说明）。 |
| 信息层级与版式细调 | 0.25 | 25 | **88** | 22.0 | 层级清、卡等高、左缘共线。**扣 7**：间距非 4／8 倍数（`:1321` 卡 `padding:14px`、`:1335` 值行 `gap:6px`、`:1253-1254` 徽标 `margin:0 6px 6px 0;padding:2px 8px`、`:1127` 标题 `margin:6px 0 0`）。**扣 3**：1440 档正文 604px／页壳 777px，相对 1080px 列偏松。**扣 2**：「预计区间」卡值串最长（`73.5 至 73.7` ＋ `kg`），390 档读图报「`kg` 几乎贴边」（机器读数未越界、未切）。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 文本层 `命中=0`；无英文内部标识符。**扣 5**：真冗余 1 处（体重记录不到 14 天 ×2）＋半冗余 1 处（95% 置信带 ×2）＋口径行发丝线串行（记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档 `docScrollW==vw`、`越界=0`、`溢出=0／被切=0／横滚容器=0`；列数 390→2+2／768→2+2／1440→4；页壳 1440 居中 `x=160 right=1280`。无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸三件照 HELP、`触摸小件=0`、字号下限 390 档 12px（`minFontPxNoSvg=12`）、无横滚、日期单行、三胶囊横排等高。**扣 5**：断点 820 ∉ 参照件实测 {600,900}。 |

**加权总分 ＝ 89×0.20 ＋ 88×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋22.0＋19.0＋15.0＋19.0 ＝ 92.8**

### 页 03 · 预测体重(3月后)

**产物**：`.scratch/t387/html/03-预测体重(3月后).html`，71,835 字节，SHA256 前 16 `0B4771C56CC10E09`。

**本页与页 01／02 的关系**：同族同模板，`fmt.json` 三档几何与页 01／02 **逐字相同**（`shell x=160 w=1120`／`kpiRows 2+2/2+2/4`／`fontSizes [12,13,15,16,22,26|28]`／`paramFormCols none/none/352px×3`）。差异只在内容：H1「体重预测（90 天）」、徽标「90 天」、结论「90 天后体重约 70.7 kg」、`horizonDays=90`、KPI 预测 `70.7`／区间 `70.5 至 70.8`。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 清楚。读图（390 档）逐段抄录：眉标「预测体重」→ 大标题「体重预测（90 天）」（单行未折）→ 胶囊「卡路里｜90 天｜趋势分析」→ 结论句「按最近的趋势，90 天后体重约 70.7 kg。」→ 目录胶囊「参数｜概览｜数据与日志」→ 三节。用户界面层级：结论句与四张卡数值（22px）压过卡标签（12px）与小节标题（16px）。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | 无。`fmt.json` 本页三档 `docScrollW=390/768/1440`、`越界=0`、`触摸小件=0`；`resp.json` 三档 `溢出=0 被切=0 横滚容器=0 放不下的图=0`。读图 390 档逐条报「无元素被切、无横向滚动、无文字压字」；读图 1440 档报「四张卡外框高度完全一致（`速率` 与 `预计区间` 等高）」「眉标／大标题／胶囊／四张卡／『数据与日志』标题左缘同在一条竖线」；`fmt` 实测 `4卡×261 高100-100 极差0` 与两处读图一致。**读图过报**：1440 档读图报「脚注之下空白占整屏 35%–40%」——`fmt` 实测页壳高 777px／视口 900px，底部空档 123px ＝ **13.7%**，读图数按其自身口径**驳回**（留白偏松已计入信息层扣分，不重复计）。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中：`audit-separators` 本页 `命中=0`（`·；｜、~` 全 0，`标识符 0`）。**版式层半命中**：页脚口径行同源码 `:1189-1192` `border-left` 发丝线串三段（读图 1440 档在页 04 同模板页确认两条竖线，本页同 CSS 同标记）。按 `#505`／`t514` 裁定记轻。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **一处真冗余**：参数说明「…体重记录不到 14 天就只说数据不够，不编预测。」与页脚口径「体重记录不到 14 天不出预测」（`probe-text.mjs` 片段 `体重记录不到 14` **×2**）。**半冗余**：「95% 置信带」在「预计区间」卡副文案与页脚口径各一次。**英文上屏零命中**：静态可见文本 `kg`×5、`JSON`×1、`CSV`×1；读图 390 档独立核「未发现任何英文字母串（除 `kg` 外）」—— 与源码一致。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | `tabular-nums` 在册（`:1346` 卡值 `font-variant-numeric:tabular-nums`）；本页 `ilife-block-data-table:0`，无 `<table>`、无数值列可量。小数位：`75.1`／`70.7`／`-0.34`／`70.5 至 70.8` 全一位小数，**一致**。缺值：四张卡全有值，本页无 `—` 可核。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | 无表。触摸：`触摸小件=0`；源码 `:1159-1172` `min-height:44px`、`:1291-1294` 触摸三件、`:1821` `summary{min-height:44px}`。读图 390 档核：三颗导航胶囊横排一行、高度一致；日期「2026-09-03」「2026-09-16」均单行未折；标题「体重预测（90 天）」单行、与胶囊无重叠。 |

**五维打分**（每维百分制；「满权」＝权重×100）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 同页 01（同模板同源码）：`section` ×3 ＋ `h2.tpd-sec-title` ×3 ＋ `nav` ＋ 卡网格实测 `极差=0`。**扣 5**：「数据与日志」节内只有 `ilife-block-copy-block`，零数据行（`ilife-block-data-table:0`）。**扣 5**：`@media` 八套断点值不成档。 **扣 1**：页上有一个**永远按不动、页上一句解释都没有**的 `disabled` 按钮（`<button … class="ilife-copy-btn ilife-copy-btn-ghost" data-action-id="ilife-copy-log" disabled>复制日志</button>`；8 页逐页已核，各页 `ilife-empty:0`、可见文本里无「日志为空」的说明）。 |
| 信息层级与版式细调 | 0.25 | 25 | **88** | 22.0 | 层级清、卡等高、左缘共线（读图 1440 档与 `fmt` 两处独立核过）。**扣 7**：间距非 4／8 倍数（`:1321` 卡 `padding:14px`、`:1335` 值行 `gap:6px`、`:1253-1254` 徽标 `margin:0 6px 6px 0;padding:2px 8px`、`:1127` 标题 `margin:6px 0 0`）。**扣 3**：1440 档正文 604px／页壳 777px（视口 900px，底部空档 123px）。**扣 2**：「预计区间」卡值串最长（`70.5 至 70.8` ＋ `kg`）压同宽 261px 一行。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 文本层 `命中=0`；无英文内部标识符。**扣 5**：真冗余 1 处（体重记录不到 14 天 ×2）＋半冗余 1 处（95% 置信带 ×2）＋口径行发丝线串行（记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档 `docScrollW==vw`、`越界=0`、`溢出=0／被切=0／横滚容器=0`；列数 390→2+2／768→2+2／1440→4；页壳 1440 居中 `x=160 right=1280`。无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸三件照 HELP、`触摸小件=0`、字号下限 390 档 12px、无横滚、日期单行、三胶囊横排等高、标题单行。**扣 5**：断点 820 ∉ 参照件实测 {600,900}。 |

**加权总分 ＝ 89×0.20 ＋ 88×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋22.0＋19.0＋15.0＋19.0 ＝ 92.8**

### 页 04 · 预测体重(6月后)

**产物**：`.scratch/t387/html/04-预测体重(6月后).html`，71,840 字节，SHA256 前 16 `F634220E7470E837`。

**本页与页 01–03 的关系**：同族同模板，`fmt.json` 三档几何逐字相同。差异只在内容：H1「体重预测（180 天）」、徽标「180 天」、结论「180 天后体重约 66.2 kg」、`horizonDays=180`、KPI 预测 `66.2`／区间 `66.0 至 66.5`。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 清楚，同页 01–03。读图 390／1440 两张均报「标题区紧凑、层级清晰、无重叠」。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | 无。`fmt.json` 本页三档 `docScrollW=390/768/1440`、`越界=0`、`触摸小件=0`、`kpiRows 极差=0`；`resp.json` 三档 `溢出=0 被切=0 横滚容器=0`。**两条读图结论驳回（源码／机器读数为准）**：①读图 1440 档报「四张卡不等高，`速率` 卡明显矮一截」——`fmt` 实测 `4卡×261/261/261/261 高100-100 极差0`，**等高**，驳回；②读图 1440 档报「标题区左缘比卡片组左缘更靠左，三者错开」——源码 `.ilife-block-page-shell{padding:32px 20px 60px}`＋`.ilife-block-kpi-card-grid{margin:16px 0}`＋`:1843 .ilife-block-data-table{margin-left:0}`，且 `fmt` 的 `bodyChildren` 实测各直接子元素一律 `x=16`（390 档），**左缘共线**，驳回。**留白**：页壳 777px／视口 900px（1440 档），底部空档 123px；读图 390／1440 两张都报「底部留有较大空白」，390 档实拍整页高 1070px ＝ 页壳高，**无额外空档**，该侧驳回。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中：`audit-separators` 本页 `命中=0`。**版式层半命中（本页是本批该形的实拍证据页）**：读图 1440 档逐字报「三段灰色小字**并排排在同一行**，段与段之间**有细竖线分隔**，一共两条把三段隔开」；源出处 `:1177-1192` `.ilife-block-caliber:has(>span){display:flex;flex-wrap:wrap}` ＋ `> span + span{padding-left:8px;border-left:1px solid rgba(210,210,215,.6)}`。390 档读图报「自动换行成两行／三行，竖线在换行处断开或消失，手机端是纯堆叠」—— 与 `flex-wrap:wrap` 一致。按 `#505`／`t514:47-51`「分隔由版式承担」记轻。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **一处真冗余**：参数说明与页脚口径各讲一次「体重记录不到 14 天」（`probe-text.mjs` 片段 ×2）。**半冗余**：「95% 置信带」×2。**英文上屏零命中**：静态可见文本 `kg`×5、`JSON`×1、`CSV`×1；读图两张独立核「没有出现任何英文单词或字母串（除 `kg` 单位外）」—— 与源码一致。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | `tabular-nums` 在册；本页 `ilife-block-data-table:0`，无数值列。小数位：`75.1`／`66.2`／`-0.34`／`66.0 至 66.5` 全一位小数（`66.0` 保留一位补零，**位数统一**）。缺值：四张卡全有值，本页无 `—` 可核。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | 无表。触摸 `触摸小件=0`；源码 `:1159-1172` `min-height:44px`、`:1291-1294` 触摸三件。读图 390 档核：四张卡 2 列 × 2 行、标题单行、无横向滚动、无文字重叠。日期：本页读图未单独报，`fmt` 390 档 `label.ilife-block-param-form-field w=358`、`resp` `被切=0`，同页 01／02／03 三页读图均报日期单行。**读图过报一条**：390 档报「`预计区间` 卡的 `66.0 至 66.5` 与 `kg` 几乎首尾相连、右侧几无呼吸空间」—— 机器读数 `越界=0`／`被切=0`，不构成缺陷；该「值串最长」已计入信息层扣分，不重复计。 |

**五维打分**（每维百分制；「满权」＝权重×100）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 同页 01–03（同模板同源码）。**扣 5**：「数据与日志」节内只有复制块，零数据行。**扣 5**：`@media` 八套断点值不成档。 **扣 1**：页上有一个**永远按不动、页上一句解释都没有**的 `disabled` 按钮（`<button … class="ilife-copy-btn ilife-copy-btn-ghost" data-action-id="ilife-copy-log" disabled>复制日志</button>`；8 页逐页已核，各页 `ilife-empty:0`、可见文本里无「日志为空」的说明）。 |
| 信息层级与版式细调 | 0.25 | 25 | **88** | 22.0 | 层级清、卡等高（`极差=0`）、左缘共线（`bodyChildren x=16`）。**扣 7**：间距非 4／8 倍数。**扣 3**：1440 档底部空档 123px／整页 777px。**扣 2**：「预计区间」卡值串最长。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 文本层 `命中=0`；无英文内部标识符。**扣 5**：真冗余 1 处（体重记录不到 14 天 ×2）＋半冗余 1 处（95% 置信带 ×2）＋口径行发丝线串行（本页 1440 档实拍见两条竖线，记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档 `docScrollW==vw`、`越界=0`、`溢出=0／被切=0／横滚容器=0`；列数 390→2+2／768→2+2／1440→4；页壳 1440 居中。无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸三件照 HELP、`触摸小件=0`、字号下限 390 档 12px、无横滚、标题单行、卡片 2 列不挤。**扣 5**：断点 820 ∉ 参照件实测 {600,900}。 |

**加权总分 ＝ 89×0.20 ＋ 88×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋22.0＋19.0＋15.0＋19.0 ＝ 92.8**

### 页 05 · 预测体重(自定义时间)

**产物**：`.scratch/t387/html/05-预测体重(自定义时间).html`，71,823 字节，SHA256 前 16 `AC7C85ACA7742EBC`。

**本页与页 01–04 的关系**：同族同模板，`fmt.json` 三档几何逐字相同。内容差异：H1「体重预测（60 天）」、徽标「卡路里／60 天／趋势分析」、结论「60 天后体重约 72.2 kg」、输入项 `start/end/horizonDays=60`、KPI 预测 `72.2`／区间 `72.0 至 72.3`。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 层级清楚（同模板）。**本页独有缺陷：页上看不出它是「自定义时间」算出来的。** 源码逐字：眉标 `p.ilife-block-page-shell-eyebrow`＝「预测体重」，H1 `h1.ilife-block-page-shell-title`＝「体重预测（60 天）」，徽标三枚＝「卡路里」／「60 天」／「趋势分析」。读图 390 档独立核：「整页扫了两遍，大标题与卡片标签里都没有出现『自定义时间』字样」。同族页 06 的徽标却写了「自定义目标」—— 同族两页对「自定义」的表达**不一致**：06 标了、05 没标。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | 无。`fmt.json` 本页三档 `docScrollW=390/768/1440`、`越界=0`、`触摸小件=0`、`kpiRows 极差=0`；`resp.json` 三档 `溢出=0 被切=0 横滚容器=0`。读图两张均报「没有横向滚动条、没有被截断被切的元素、无明显重叠」。页壳高 777px／视口 900px（1440 档），底部空档 123px。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中：`audit-separators` 本页 `命中=0`。**版式层半命中**：页脚口径行 `border-left` 发丝线（`:1189-1192`），与页 01–04 同源码同标记，按 `#505`／`t514` 记轻。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **一处真冗余**：参数说明「…体重记录不到 14 天就只说数据不够，不编预测。」与页脚口径「体重记录不到 14 天不出预测」（`probe-text.mjs` 片段 ×2）。**半冗余**：「95% 置信带」×2。**英文上屏零命中**：静态可见文本 `kg`×5、`JSON`×1、`CSV`×1；读图两张独立核「除单位 `kg` 外没有任何英文字母串（无 JS 字段名、无英文标签、无 key 漏出）」。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | `tabular-nums` 在册；本页 `ilife-block-data-table:0`，无数值列。小数位：`75.1`／`72.2`／`-0.34`／`72.0 至 72.3` 全一位小数（`72.0` 补零，位数统一），**一致**。缺值：四张卡全有值，本页无 `—` 可核。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | 无表。触摸 `触摸小件=0`；源码 `:1159-1172` `min-height:44px`、`:1291-1294` 触摸三件。读图 390 档核：标题与三个胶囊**都单行未换行**；日期「2026-09-03」「2026-09-16」在标签下一行、单行未折、与相邻行不重叠；四张卡 2 列 × 2 行。读图 390 档报「`预计区间` 卡的 `72.0 至 72.3` 与 `kg` 贴得很近」—— 机器读数 `越界=0`／`被切=0`，不构成缺陷，该「值串最长」已计入信息层扣分。 |

**五维打分**（每维百分制；「满权」＝权重×100）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 同页 01–04（同模板同源码）。**扣 5**：「数据与日志」节内只有复制块，零数据行。**扣 5**：`@media` 八套断点值不成档。 **扣 1**：页上有一个**永远按不动、页上一句解释都没有**的 `disabled` 按钮（`<button … class="ilife-copy-btn ilife-copy-btn-ghost" data-action-id="ilife-copy-log" disabled>复制日志</button>`；8 页逐页已核，各页 `ilife-empty:0`、可见文本里无「日志为空」的说明）。 |
| 信息层级与版式细调 | 0.25 | 25 | **86** | 21.5 | 层级清、卡等高（`极差=0`）、左缘共线（读图 1440 档核「四者左缘共线，与最上方标题区同一左基线」）。**扣 7**：间距非 4／8 倍数。**扣 3**：1440 档底部空档 123px／整页 777px。**扣 2**：「预计区间」卡值串最长。**扣 2**：页上**看不出「自定义时间」**—— 徽标写「60 天」，与固定档（7／30／90／180 天）页形状完全相同，而同族页 06 的徽标写了「自定义目标」，同族两页表达不一致。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 文本层 `命中=0`；无英文内部标识符。**扣 5**：真冗余 1 处（体重记录不到 14 天 ×2）＋半冗余 1 处（95% 置信带 ×2）＋口径行发丝线串行（记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档 `docScrollW==vw`、`越界=0`、`溢出=0／被切=0／横滚容器=0`；列数 390→2+2／768→2+2／1440→4；页壳 1440 居中。无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸三件照 HELP、`触摸小件=0`、字号下限 390 档 12px、无横滚、标题与胶囊单行、卡片 2 列不挤、日期不折。**扣 5**：断点 820 ∉ 参照件实测 {600,900}。 |

**加权总分 ＝ 89×0.20 ＋ 86×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋21.5＋19.0＋15.0＋19.0 ＝ 92.3**

### 页 06 · 预测体重(自定义目标)

**产物**：`.scratch/t387/html/06-预测体重(自定义目标).html`，71,710 字节，SHA256 前 16 `72C5C34BEF053D0D`。

**本页独有形状**：输入项换成 `start／end／target=65`（不是 `horizonDays`）；概览四张卡里**第三张是日期值、第四张没有值行**；页上出现本批唯一的 `ilife-status-badge ilife-status-badge-warn`（文字「超范围」）。`fmt.json`：`ilife-status-badge:1`，`kpiRows 390→2卡×173 高111-111 极差0 / 2卡×173 高111-111 极差0`，1440→`4卡×261 高111-111 极差0`。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 层级清楚，但**第四张卡是一处层级破口**。源码 `probe-kpi.mjs` 逐字：卡 1「当前」75.1 kg、卡 2「目标」65 kg、卡 3「预计达成」`2027-04-09`（＋ `-detail` 剩余 205 天）、卡 4「可行性」——**没有 `ilife-block-kpi-card-value-row`**，只有 `-detail` 一行 `当前速率 -0.34 kg/周。` ＋ `-badge`「超范围」。读图两处独立核：1440 档报「第四张卡明显失衡……大片留白，纵向高度虽与其他卡齐平，但视觉密度低、明显单薄」；裁剪实拍（x=150..1290，y=320..560）报「第四张卡没有大号数字，原本留给大数字的那一行高度处留下了一大片明显的空白」，并核「前三张大数字底部在同一水平基线」。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | **「空」有一处实锤**：`grid-auto-rows:1fr` 把四张卡压成等高 `111-111 极差0`，第四张卡少了 22px 值行 → 卡内空出一条带（裁剪实拍已见）。其余无：三档 `docScrollW=390/768/1440`、`越界=0`、`触摸小件=0`；`resp.json` 三档 `溢出=0 被切=0 横滚容器=0`。页壳高 390→1122／1440→777。**读图驳回一条**：1440 档报「四张卡整体居中偏左、右侧留白宽」——`fmt` 实测 `shell x=160 w=1120 right=1280`，左右各 160px **对称**，驳回。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中：`audit-separators` 本页 `命中=0`（`·；｜、~` 全 0，`标识符 0`）。**版式层半命中**：页脚口径行 `border-left` 发丝线（`:1189-1192`）串三段「预计达成日＝…／健康范围＝…／体重记录不到 14 天不出日期」，按 `#505`／`t514` 记轻。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **三处**：①参数说明「…体重记录不到 14 天就只说数据不够，不编一个日期出来。」与页脚口径「体重记录不到 14 天不出日期」（`probe-text.mjs` 片段 ×2）；②**H1 与眉标重复** —— 眉标 `p.ilife-block-page-shell-eyebrow`＝「预测体重」，H1 `h1.ilife-block-page-shell-title`＝「**预测体重**（自定义目标 65 kg）」，同四字在一页出现两次（读图独立核「是，有重复」）；③**同批同一概念两个词** —— 本页口径写「**健康范围**＝每周掉 0.5 到 1.0 kg」，同批页 07／08 的口径写「**安全区间**＝每周掉 0.5 到 1.0 kg」（两处源码逐字，见页 07 一节）。**英文上屏零命中**：静态可见文本 `kg`×7、`JSON`×1、`CSV`×1；读图核「唯一英文字母串就是 `kg`」。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | `tabular-nums` 在册（`:1346` 卡值）；本页 `ilife-block-data-table:0`，无 `<table>`、无数值列可量。**缺值**：本页四张卡全有值（第三张是日期、第四张以文字代替值），页上**没有出现 `—`** —— 缺值口径在 01–08 全批**无用例可核**。小数位：`75.1`／`-0.34`／`0.5 到 1.0` 一位小数，`65` 整数（体重目标本来无小数），**档内一致**。日期值 `2027-04-09` 落在 22px 大数字位（源码 `ilife-block-kpi-card-value`），读图核「未折行、未越出卡片边框，字号与 75.1／65 一致」。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | 无表。触摸 `触摸小件=0`；源码 `:1159-1172` `min-height:44px`、`:1291-1294` 触摸三件、`:1821` `summary{min-height:44px}`；徽章是 `span`（非可点件），不适用 44px。读图 390 档核：「四张卡 2×2」「`2027-04-09` 未折行、未越出卡片边框」「无被切、无横向滚动、徽章与文字未重叠」。**读图报徽章「对比度偏低」——复核驳回**：源码 `:443-446` `.ilife-status-badge-warn{background:#fff5e0;color:#a25b00}`，按 WCAG 2.x 相对亮度公式算得对比度 **4.81:1**（前景 L=0.1516，背景 L=0.9194），字 13px／600 属正文级，AA 门槛 4.5:1，**达标**。 |

**五维打分**（每维百分制；「满权」＝权重×100）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 真结构齐：`section#sec-params/#sec-overview/#sec-data` ×3 ＋ `h2.tpd-sec-title` ×3 ＋ `nav` ＋ 卡网格实测 `极差=0` ＋ 状态徽章用 `ilife-status-badge` 形状（不是拿文字拼一个「超范围」）。**扣 5**：「数据与日志」节内只有 `ilife-block-copy-block`，零数据行（`ilife-block-data-table:0`）。**扣 5**：`@media` 八套断点值不成档。 **扣 1**：页上有一个**永远按不动、页上一句解释都没有**的 `disabled` 按钮（`<button … class="ilife-copy-btn ilife-copy-btn-ghost" data-action-id="ilife-copy-log" disabled>复制日志</button>`；8 页逐页已核，各页 `ilife-empty:0`、可见文本里无「日志为空」的说明）。 |
| 信息层级与版式细调 | 0.25 | 25 | **86** | 21.5 | 左缘共线、三张大数字同基线（裁剪实拍核）。**扣 7**：间距非 4／8 倍数（`:1321` 卡 `padding:14px`、`:1335` 值行 `gap:6px`、`:1253-1254` 徽标 `margin:0 6px 6px 0;padding:2px 8px`、`:430` 状态徽章 `padding:6px 14px`／`gap:6px`、`:1127` 标题 `margin:6px 0 0`）。**扣 4**：第四张「可行性」卡无值行，靠 `grid-auto-rows:1fr` 撑到与前三张等高 111px，卡内空出一条 22px 值行带（裁剪实拍与 `fmt` 两处互证）—— 同排卡视觉重量失衡。**扣 3**：1440 档正文／页壳 777px 对 900px 视口，底部空档 123px。 |
| 分隔符与文字纪律 | 0.20 | 20 | **92** | 18.4 | 文本层 `命中=0`；无英文内部标识符、库表名、常量名。**扣 8**：真冗余 1 处（体重记录不到 14 天 ×2）＋H1 与眉标重复「预测体重」1 处＋同批同一概念两个词（本页「健康范围」vs 页 07／08「安全区间」）1 处＋口径行发丝线串行（记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档 `docScrollW==vw`、`越界=0`、`溢出=0／被切=0／横滚容器=0`；列数 390→2+2／768→2+2／1440→4；页壳 1440 居中 `x=160 right=1280`。无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸三件照 HELP、`触摸小件=0`、字号下限 390 档 12px（`minFontPxNoSvg=12`）、无横滚、日期值不折、卡片 2 列不挤、徽章对比度 4.81:1 达 AA。**扣 5**：断点 820 ∉ 参照件实测 {600,900}。 |

**加权总分 ＝ 89×0.20 ＋ 86×0.25 ＋ 92×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋21.5＋18.4＋15.0＋19.0 ＝ 91.7**

### 页 07 · 模拟减重(每天-300卡)

**产物**：`.scratch/t387/html/07-模拟减重(每天-300卡).html`，76,071 字节，SHA256 前 16 `0B87AD30889DE82B`。

**本页独有形状（源码逐字）**：四节 `sec-params／sec-overview／**sec-track**／sec-data`，页内导航四条 `nav=["sec-params","sec-overview","sec-track","sec-data"]`、**悬空 0**（`probe-nav.mjs`）—— 全批八页逐页核过，无一条锚点落空。真 `<table class="ilife-block-data-table-table">`：`<caption>`「模拟轨迹（共 90 天，每周一点）」＋ `<thead>` ＋ `<th scope="col">`＋ 14 行 `<tr>`，**28 枚 `data-label`（14 行 × 2 格全带）**、`cell-left`×15、`cell-right`×15。页级另有三个 `<style>` 块专门覆盖窄档：`:style#3` 在 `@media (max-width:640px)` 里把 `.tpd-track-table--cols2` 的表**恢复成 `display:table`／`thead{display:table-header-group}`／`td{display:table-cell;white-space:nowrap}`／`td::before{content:none}`**（即**不套用公共层 ≤640 的行卡化**），并钉 `.ilife-block-kpi-card{padding:16px}` 与第三张卡的值 `font-size:28px;font-weight:800`。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 清楚：眉标「模拟减重」→ H1「模拟减重（每天 -300 卡）」→ 徽标（卡路里／每天 -300 卡／趋势分析）→ 结论句「每天多减 300 卡，一周大约掉 1.36 kg。」→ 四节。**一排四张卡的数值有三档**：卡 1「当前 75.1 kg」与卡 2「每天多减 300 卡」是 22px／700，卡 3「每周掉重 1.36 kg/周」被页级样式抬到 **28px／800**（读图 1440 档独立核「第三张『每周掉重 1.36』明显比前两张更大、更粗」），卡 4「可行性」**没有值行**（只有 12px 小字＋徽章）。`fmt` 字号档实测 `[11,12,13,15,16,22,26,28]`（390／768／1440 三档都含 28），**28px 确实渲染出来**——390 档读图报「第三张与前两张字号字重看起来一致」，**驳回**。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | 无溢出、无截断、无横滚：三档 `docScrollW=390/768/1440`、`越界=0`、`触摸小件=0`、`kpiRows 极差=0`；`resp.json` 三档 `溢出=0 被切=0 横滚容器=0 放不下的图=0`。**「空」一处**：卡 4 无值行，靠 `grid-auto-rows:1fr` 与前三张等高（`高115-115 极差0`）。**读图驳回一条**：1440 档报「表格左缘比标题／卡片左缘往右缩进 12–16px」——`fmt` 的 `bodyChildren` 实测 1440 档**每个直接子元素一律 `x=180`**（`body.x=180 w=1080 right=1260`），源码 `:style#4` 也钉了 `.ilife-block-page-shell .ilife-block-data-table{margin-left:0}` ⇒ 盒子左缘共线，驳回（差的是表头格里 14px 内距，非盒偏移）。整页高 1440→1550／390→1747。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中：`audit-separators` 本页 `命中=0`（`·；｜、~` 全 0，`标识符 0`）。**版式层半命中**：页脚口径行三段「每 7700 卡大约折算 1 kg／轨迹从窗口最后一天起算，每周一点，非整周时补最后一天那一行／安全区间＝每周掉 0.5 到 1.0 kg」由 `border-left` 发丝线串成一行 —— 读图 1440 档既抄了这三段，也报「段与段之间用细竖线 `｜` 分隔」（读图把发丝线读成 `｜` 字符，源码层无该字符）。按 `#505`／`t514:47-51`「分隔由版式承担」记轻。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **两处真冗余**（比 01–06 多一处）：①参数说明「…**每 7700 卡大约对应 1 kg**，按这个折算每周掉多少。」与页脚口径「**每 7700 卡大约折算 1 kg**」—— 同一条换算规则一页讲两遍（`probe-text.mjs` 片段 `每 7700 卡大`／`7700 卡大约` 各 ×2）；②参数说明「看每周的掉重落在 **0.5 到 1.0 kg 这个安全区间**里没有。」与页脚口径「**安全区间＝每周掉 0.5 到 1.0 kg**」—— 同一条安全区间讲两遍（片段 `0.5 到 1.0 kg` ×2）。**同批同一概念两个词**：本页口径用「安全区间」，同批页 06 的口径用「健康范围」（页 06 源码逐字，见页 06 一节）。**英文上屏零命中**：静态可见文本 `kg`×8、`JSON`×1、`CSV`×1；读图核「没有用分号 `；` 或中圆点 `·` 拼串的句子」。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | **本批唯一有真数值列的两页之一**。源码 `:1379-1383` `.ilife-block-data-table-table{font-variant-numeric:tabular-nums}`、`:1419-1424` `td.ilife-block-data-table-cell-right{min-width:5.5em;color:var(--fg);font-family:"SF Mono",monospace;font-variant-numeric:tabular-nums}`；`fmt` 实测三档 `tableTh`／`tableTd` 均含 `font-variant-numeric: tabular-nums`，`th` 文本「日期」。读图两处独立核：数值列**右对齐成列**（「小数点基本垂直对齐成一条竖直的列」）。小数位：表体 14 个值全一位小数（`75.1…57.6`），KPI `1.36`／`-0.34`／`300` 与口径 `0.5`／`1.0` 一位小数，**档内一致**。缺值：全表 14 行无缺值，页上**没有出现 `—`** —— 缺值口径在 01–08 全批无用例可核。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | **读得开，但走的是「保持表格」而不是「行卡化」**。源码 `:style#3` 的 ≤640 覆盖把 `thead` 还原、`td{display:table-cell;white-space:nowrap;padding:8px 12px}`、`td::before{content:none}`；读图 390 档独立核三次（整页读图、y=1010..1290 裁剪、y=1036..1150 裁剪实拍）：表头一行「日期｜模拟体重（kg）」，每行「日期 左、数值 右」单行，`2026-09-16`…`2026-12-15` **无折行无截断**，数值右对齐成列，**无横向滚动条**（机器读数 `横滚容器=0`）。这与公共层 ≤640 的行卡化形状不同，属本页**有意覆盖**（`tpd-track-table--cols2` 只两列、每格都短，保持表形更省纵向空间）。触摸：`触摸小件=0`（三档）；字号下限 390 档 **11px**（`minFontPxNoSvg=11`，责任元素 `th.ilife-block-data-table-cell-left`／`-cell-right`）＝ 批内 11px 下限线，**恰好达标**。**扣项**：断点 820 ∉ 参照件实测 {600,900}。 |

**五维打分**（每维百分制；「满权」＝权重×100）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **91** | 18.2 | 真结构是本批最好的一页：真 `<table>` ＋ `<caption>` ＋ `<thead>` ＋ `th[scope=col]` ＋ 28 枚 `data-label`、四节四锚点**零悬空**、卡网格实测 `极差=0`。**扣 3**：`sec-data`（h2「数据与日志」）节内只有 `ilife-block-copy-block`，本页数据其实在 `sec-track` 里，节名与节内容**不符**（比 01–06 轻：本页确有 14 行数据）。**扣 5**：`@media` 九套值不成档（`400/640/641/720/820/821/875/876/style#3 的 640`），页级主断点 `:1825` 与公共层 640 两套并存并互相覆盖。**扣 1**：8 页逐页已核，页上各有一个**永远按不动、且页上一句解释都没有**的 `disabled` 按钮（`<button … class="ilife-copy-btn ilife-copy-btn-ghost" data-action-id="ilife-copy-log" disabled>复制日志</button>`，本页 `ilife-empty:0`，可见文本里无任何「日志为空」的说明）。 |
| 信息层级与版式细调 | 0.25 | 25 | **92** | 23.0 | 层级清、表数值右对齐成列、盒子左缘共线（`bodyChildren x=180`，8 项直接子元素一致）。**扣 5**：间距非 4／8 倍数（本页卡内距已被页级钉成 `16px`，但 `:1253-1254` 徽标 `margin:0 6px 6px 0;padding:2px 8px`、`:1127` 标题 `margin:6px 0 0`、`:430` 状态徽章 `padding:6px 14px;gap:6px`、`:1392-1408` 表 `th{padding:10px 14px}`／`td{padding:12px 14px}` 仍在）。**扣 3**：同一排四张卡三种数值处理（22px／22px／**28px**／无值）—— 卡 3 的 28px 加大是有意强调，但同排同时出现「无值卡」与「加大值卡」，层级读起来像两套规矩。 |
| 分隔符与文字纪律 | 0.20 | 20 | **92** | 18.4 | 文本层 `命中=0`；无英文内部标识符、库表名、常量名。**扣 8**：真冗余 2 处（`每 7700 卡` 与 `0.5 到 1.0 kg` 各在参数说明与页脚口径讲了两遍）＋同批同一概念两个词（本页「安全区间」vs 页 06「健康范围」）1 处＋口径行发丝线串行（记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档 `docScrollW==vw`、`越界=0`、`溢出=0／被切=0／横滚容器=0／放不下的图=0`；列数 390→2+2／768→2+2／1440→4；页壳 1440 居中 `x=160 right=1280`。无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸三件照 HELP、`触摸小件=0`、无横滚、日期不折、数值右对齐成列；窄表虽未套用公共层行卡化，但**本页自立了 ≤640 的等价覆盖**（保留表头 ＋ `nowrap` ＋ `cell-right`），390 档实拍与机器读数两处互证「读得开」。字号下限 390 档 11px ＝ 批内下限线，达标（与批内既有口径「390 档 11px 的 12/16 页」同档）。**扣 5**：断点 820（及页级 640）∉ 参照件实测 {600,900}。 |

**加权总分 ＝ 91×0.20 ＋ 92×0.25 ＋ 92×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 18.2＋23.0＋18.4＋15.0＋19.0 ＝ 93.6**

### 页 08 · 模拟减重(每天-500卡)

**产物**：`.scratch/t387/html/08-模拟减重(每天-500卡).html`，76,071 字节，SHA256 前 16 `4DC63A2E3599F58D`。

**本页与页 07 的关系**：同模板同源码，`fmt.json` 三档几何逐字相同（`shell x=160 w=1120`／`kpiRows 2+2/2+2/4`／`fontSizes [11,12,13,15,16,22,26,28]`／`ilife-block-data-table:1`），页级三块 `<style>` 的窄表覆盖与 `kpi-card{padding:16px}`／第三卡 `28px/800` 两处钉法**逐字相同**；表结构同为 `<tr>`×15、`data-label`×28、`cell-right`×15、`cell-left`×15。差异只在内容：H1／徽标「每天 -500 卡」、结论「每天多减 500 卡，一周大约掉 1.54 kg。」、KPI 每天多减 `500`／每周掉重 `1.54`、可行性「每天总缺口约 1694 卡。」、表体 14 个值（`75.1…55.3`）。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 清楚，同页 07。一排四张卡：卡 1「当前 75.1 kg」（22px）、卡 2「每天多减 500 卡」（22px）、卡 3「每周掉重 1.54 kg/周」（页级 `font-size:28px;font-weight:800`）、卡 4「可行性」（无值行，只有「每天总缺口约 1694 卡。」＋徽章「超范围」）。`fmt` 三档字号档均含 **28** ⇒ 28px 确实渲染。**读图驳回一条**：390 档读图报「第三张 `1.54` 与前两张字号字重一致，并没有明显更大更粗」—— 与源码 `:style#3` 的 `28px／800` 及 `fmt` 的 `28` 实测**冲突**，以源码／机器读数为准，驳回。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | 无。三档 `docScrollW=390/768/1440`、`越界=0`、`触摸小件=0`、`kpiRows 极差=0`；`resp.json` 三档 `溢出=0 被切=0 横滚容器=0`。**「空」一处**：卡 4 无值行，靠 `grid-auto-rows:1fr` 与前三张等高（`高115-115 极差0`）；390 档读图报「第四张整块卡被内容撑满、没有空出一大块」—— 与页 06 桌面档裁剪实拍（同形状卡 4 空出一条值行带）**不一致**，记作**读图分歧**，不作否定依据，以源码（卡 4 无 `ilife-block-kpi-card-value-row`）为准。整页高 1440→1550／390→1747。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中：`audit-separators` 本页 `命中=0`。**版式层半命中**：页脚口径行同上 `border-left` 发丝线串三段（`:1189-1192`）；390 档读图报「三段各自换行、末尾没有竖线」（与 `flex-wrap:wrap` 一致），1440 档同页 07 读图报可见细竖线。按 `#505`／`t514` 记轻。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **两处真冗余**（与页 07 同）：①「每 7700 卡大约对应 1 kg」（参数说明）与「每 7700 卡大约折算 1 kg」（页脚口径）；②「0.5 到 1.0 kg 这个安全区间」（参数说明）与「安全区间＝每周掉 0.5 到 1.0 kg」（页脚口径）。**同批同一概念两个词**：本页「安全区间」vs 页 06「健康范围」。**英文上屏零命中**：静态可见文本 `kg`×8、`JSON`×1、`CSV`×1；读图核「没有除 `kg` 之外的英文字母串」。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | 与页 07 同源码：`:1382` 表 `tabular-nums`、`:1423` 右列 `font-variant-numeric:tabular-nums`；`fmt` 实测三档 `tableTh`／`tableTd` 均含 `tabular-nums`。读图 390 档核「数值右对齐成列，与表头『模拟体重（kg）』对齐」。小数位：表体 14 个值全一位小数（`75.1…55.3`），KPI `1.54`／`300→500`／`-0.34` 一位小数，**档内一致**。缺值：全表无缺值，页上**无 `—`**。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | 同页 07：≤640 保持表形（`thead` 还原、`td{display:table-cell;white-space:nowrap}`、`td::before{content:none}`）。读图 390 档独立核：表头一行「日期｜模拟体重（kg）」，头两行「2026-09-16／75.1」「2026-09-23／73.6」，**表格形态而非堆叠卡片**（读图明确答「不重复出现小标签」），日期串**无折行无截断**，数值右对齐成列，无横向滚动条。触摸 `触摸小件=0`；字号下限 390 档 11px（`th`）＝批内下限线，达标。**扣项**：断点 820／640 ∉ 参照件实测 {600,900}。 |

**五维打分**（每维百分制；「满权」＝权重×100）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **91** | 18.2 | 同页 07（同模板同源码）：真 `<table>` ＋ `<caption>` ＋ `<thead>` ＋ `th[scope=col]` ＋ 28 枚 `data-label`，四节四锚点**零悬空**，卡网格 `极差=0`。**扣 3**：`sec-data` 节内只有复制块，与 h2「数据与日志」不符。**扣 5**：`@media` 九套断点值不成档。**扣 1**：页上那个永远按不动、无说明的 `disabled`「复制日志」按钮（8 页逐页已核）。 |
| 信息层级与版式细调 | 0.25 | 25 | **92** | 23.0 | 层级清、表数值右对齐成列、盒子左缘共线。**扣 5**：间距非 4／8 倍数（徽标 `6px/2px`、标题 `6px`、状态徽章 `6px/14px`、表 `th 10px 14px`／`td 12px 14px`；卡内距本页已钉 16px）。**扣 3**：同排四张卡三种数值处理（22px／22px／28px／无值）。 |
| 分隔符与文字纪律 | 0.20 | 20 | **92** | 18.4 | 文本层 `命中=0`；无英文内部标识符。**扣 8**：真冗余 2 处（`每 7700 卡` ×2、`0.5 到 1.0 kg` ×2）＋同批同一概念两个词（「安全区间」vs 页 06「健康范围」）＋口径行发丝线串行（记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档 `docScrollW==vw`、`越界=0`、`溢出=0／被切=0／横滚容器=0`；列数 390→2+2／768→2+2／1440→4；页壳 1440 居中。无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸三件照 HELP、`触摸小件=0`、无横滚、日期不折、数值右对齐、窄表有本页自立的等价覆盖。字号下限 390 档 11px 达标。**扣 5**：断点 820／640 ∉ 参照件实测 {600,900}。 |

**加权总分 ＝ 91×0.20 ＋ 92×0.25 ＋ 92×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 18.2＋23.0＋18.4＋15.0＋19.0 ＝ 93.6**

## 三、汇总

### 3.1 逐页五维与加权总分（八页全表）

| 页 | 结构（×0.20） | 信息（×0.25） | 分隔符（×0.20） | 跨宽（×0.15） | 手机端（×0.20） | **加权总分** | ≥90 |
|---|---|---|---|---|---|---|---|
| 01 预测体重(1周后) | 89 | 88 | 95 | 100 | 95 | **92.8** | ✅ |
| 02 预测体重(1月后) | 89 | 88 | 95 | 100 | 95 | **92.8** | ✅ |
| 03 预测体重(3月后) | 89 | 88 | 95 | 100 | 95 | **92.8** | ✅ |
| 04 预测体重(6月后) | 89 | 88 | 95 | 100 | 95 | **92.8** | ✅ |
| 05 预测体重(自定义时间) | 89 | 86 | 95 | 100 | 95 | **92.3** | ✅ |
| 06 预测体重(自定义目标) | 89 | 86 | 92 | 100 | 95 | **91.7** | ✅ |
| 07 模拟减重(每天-300卡) | 91 | 92 | 92 | 100 | 95 | **93.6** | ✅ |
| 08 模拟减重(每天-500卡) | 91 | 92 | 92 | 100 | 95 | **93.6** | ✅ |

**本席读数**：`count=8`；`mean=92.8`（742.4÷8）；`min=91.7`（页 06）；`max=93.6`（页 07／08）；
`pass90=8`（八页加权总分全部 ≥90）；`lowest=页 06 · 预测体重(自定义目标) 91.7`。

### 3.2 八页共同结论（逐条都有机器读数兜底）

| 项 | 结论 | 证据 |
|---|---|---|
| ① 桌面 1440／手机 390／平板 768 三档无溢出无塌列 | **达标** | `resp.json` 8×3=24 格 `ok=true 溢出=0 被切=0 横滚容器=0 放不下的图=0`；`fmt.json` 24 格 `docScrollW == vw`、`越界=0`；列数 390→2+2／768→2+2／1440→4 |
| ① 触摸目标 ≥44px | **达标** | `fmt.json` 24 格 `触摸小件=0`；源码 `:1159-1172` `.ilife-block-toc a{min-height:44px}`、`:1821` `summary{min-height:44px}`、`:1189` 附近 `.ilife-block-param-form-input{min-height:44px}` |
| ② 手机档与 HELP 同档 | **近似达标，断点值不符** | 触摸三件照 HELP（`:1819`）、字号下限 390 档 12px（页 01–06）／11px（页 07／08 表头，压批内下限线）、无横滚、日期不折；**但**参照件 `卡路里_HELP_20260731_201530.html` 实测断点为 `@media (max-width:600px)` ＋ `@media (min-width:601px) and (max-width:900px)`，八页页级主断点写的是 **820**（注释自称「同 HELP」），`820 ∉ {600,900}` |
| ③ 间距 4／8 倍数 | **未达标** | `probe-spacing.mjs` 扫页 01 `<style>` 段：**88 处／26 种**取值不是 4 的倍数（`padding:14px`×22、`margin:6px`×9、`padding:10px`×9、`gap:6px`×6…）；八页上屏的即 `.ilife-block-chip{margin:0 6px 6px 0;padding:2px 8px}`、`.ilife-block-kpi-card-value-row{gap:6px}`、`.ilife-block-page-shell-title{margin:6px 0 0}`、`.ilife-status-badge{padding:6px 14px;gap:6px}`（页 07／08 的卡内距已由页级钉成 `16px`） |
| ③ 字号成档有下限 | **达标** | `fmt.json` 390 档 `minFontPxNoSvg`：页 01–06＝12px、页 07／08＝11px（责任元素 `th.ilife-block-data-table-cell-left/right`），均在批内 11px 下限线上或之上；字号取值 6 档（01–06）／8 档（07／08） |
| ③ 数值列等宽右对齐 | **达标** | `:1382` 表 `tabular-nums`、`:1423` 右列 `font-variant-numeric:tabular-nums`；`fmt.json` 三档 `tableTh`／`tableTd` 均含 `tabular-nums`；页 07／08 数值列 `cell-right` ＋ 读图核「小数点垂直对齐成列」；无表的六页用 `font-feature-settings:"tnum"`（`:1116`）＋ 卡值 `tabular-nums`（`:1346`） |
| ③ 卡／表／图左缘共线 | **达标** | `fmt.json` `bodyChildren` 八页各档直接子元素 `x` 全同（390→16／1440→180）；页 07／08 另有证据：1440 档读图报「表格左缘缩进 12–16px」**驳回**（盒子同为 `x=180`，差的只是表头格 14px 内距） |
| ③ 留白有节 | **偏松（已扣分）** | 页 01–06 在 1440 档整页高 777px／视口 900px（底部空档 123px）；页 07／08 整页 1550px，填满一屏以上 |
| ④ 文案零冗余 | **未达标** | 页 01–06：`体重记录不到 14 天` 在参数说明与页脚口径各一次（`probe-text.mjs` 片段 ×2）；页 06 另加 H1 与眉标重复「预测体重」；页 07／08：`每 7700 卡大约…1 kg` 与 `0.5 到 1.0 kg 安全区间` 各讲两遍；**跨页**：页 06 用「健康范围」、页 07／08 用「安全区间」指同一件事 |
| ④ 禁英文内部标识符上屏 | **达标** | `audit-separators` 八页 `标识符0`；静态可见文本只有 `kg`／`JSON`／`CSV`（单位与复制格式名）；读图两处独立核「除 `kg` 外无英文字母串」 |
| ⑤ 缺值写 `—` | **全批无用例可核** | 八页 `probe-fields.mjs` 逐页取字段：八页全部字段与表格 14 行都**没有缺值**，页上零 `—`；此条本批**无法验证**，不作扣分依据 |
| ⑥ 手机档表格读得开 | **达标（走的是「保持表格」而非「行卡化」）** | 页 01–06 无表；页 07／08 页级 `:style#3` 在 ≤640 把 `.tpd-track-table--cols2` 还原成 `display:table`＋`thead{display:table-header-group}`＋`td{display:table-cell;white-space:nowrap}`＋`td::before{content:none}`；390 档三次读图＋390×114 裁剪实拍核：表头一行、每行「日期左／数值右」、日期无折无截、无横滚 |
| ⑥ 日期不被折断 | **达标** | 页 01–06 的 `2026-09-03`／`2026-09-16`：390 档读图三次均报单行；页 06 的 `2027-04-09`（22px 大数字位）读图核「未折行、未越出卡片」；页 07／08 表体 14 个日期读图核「完整 10 字符、单行、无省略号」 |

### 3.3 本席驳回的视觉读图结论（视觉模型是提示器、不是证据）

| # | 读图报的 | 驳回依据 |
|---|---|---|
| 1 | 「内容只占左侧约 70%、右半区空白、未居中」（页 01／03／04／06 多次） | `fmt.json` `shell x=160 w=1120 right=1280`，左右各 160px 对称 |
| 2 | 「四张卡不等高，`速率` 卡明显矮一截」（页 04 桌面） | `fmt.json` `4卡×261/261/261/261 高100-100 极差0` |
| 3 | 「标题区左缘比卡片组左缘更靠左，三者错开」（页 04 桌面） | `fmt.json` `bodyChildren` 各直接子元素一律 `x=180`（1440 档）；源码 `.ilife-block-page-shell{padding:…20px}` ＋ `.ilife-block-kpi-card-grid{margin:16px 0}` |
| 4 | 「表格左缘比标题／卡片左缘往右缩进 12–16px」（页 07 桌面） | 同上，1440 档 `bodyChildren` 全为 `x=180`；`:1843` `.ilife-block-page-shell .ilife-block-data-table{margin-left:0}` |
| 5 | 「脚注之下空白占整屏 35%–40%」（页 03 桌面） | `fmt.json` 页壳 777px／视口 900px，底部空档 123px ＝ 13.7% |
| 6 | 「第三张 `1.54` 与前两张字号字重一致」（页 08 手机） | 页级 `:style#3` 钉 `.ilife-block-kpi-card-grid > .ilife-block-kpi-card:nth-child(3) .ilife-block-kpi-card-value{font-size:28px;font-weight:800}`；`fmt.json` 三档 `fontSizes` 均含 **28** |
| 7 | 「徽章底色与字色对比度偏低」（页 06） | 源码 `:443-446` `.ilife-status-badge-warn{background:#fff5e0;color:#a25b00}`；按 WCAG 2.x 相对亮度算 **4.81:1**（前景 L=0.1516／背景 L=0.9194），字 13px／600 属正文级，AA 门槛 4.5:1，**达标** |

### 3.4 遗留（本席只评页 01–08，未越界改件）

- 本席**只写一件**：`docs/skills/skill-calorie/t520-重评-V1.md`（本报告）；读数件与探针全部落在
  `.scratch/t520-V1/`（工作目录，不入仓）。`packages/skill-calorie/src/goal/**`、
  `src/triggers/scene-04-exercise.ts`、`packages/skill-bill/**`、`AGENTS.md` 台账**一行未碰**。
- 八页产物（`.scratch/t387/html/0[1-8]-*.html`）**只读**，本席未改一字（对比用的是
  `.scratch/t520-V1/pages/` 下的副本，SHA256 与原件逐页一致）。
- 未评项：`dsh visionreview` 已坏，本席未试；页 09–31 不属本席范围。
