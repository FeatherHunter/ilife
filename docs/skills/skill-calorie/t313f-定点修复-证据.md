# #313f · 关票前定点修复 · 证据

- **票**：wayfinder #313 · 本席 `313f`（关票前定点修复：只做 R 的 S3 与本票两处，见末段「范围声明」）。
- **开工**：`2026-09-13T10:47:20Z`（本席第一条命令前取的 UTC；对账窗口 `--since` 取它）。
- **开工基线**：HEAD `d03032a`（复核席报告，本地未推）／`origin/master` = `cb5b4c5`；`git status --porcelain` **空**；`.scratch/locks/gate.lock` 不存在。
- **改动**：3 件受跟踪文件、5 处（4 处文档句 ＋ 1 处生成器注释）；**无逻辑改动**（生成器只加注释，见 ②）。
- **收尾提交**：主改提交 `bacc494`（`docs(313): 关票前定点修复 —— …`）；其后只追加过一次**证据补记**提交（把本节的对账实测与窗口外收尾运行如实记进来），
  两条的 sha 与窗口口径见本席交付报告。

本席全部命令跑在 better sidebar 终端（标题 `313f-定点修复`）：build／test／写 git 一律经
`node tooling/run-locked.mjs --ticket 313f -- <命令>`；只读 git（`status`／`diff`）与对账工具直跑（**不进** `gate-runs.log`）。

---

## ① 修 1 · 纪律文本照事实说记录面落位

事实基础（本席实测复读，非引用）：

- `packages/skill-calorie/scripts/gen-cli.mjs:451-470` 的 `targets` 数组 = `src/cli/keys.ts`、`src/cli/registry.ts`、
  `packages/base-combos/combos.yaml`、`scripts/build-help.mjs`、**`src/triggers/routes.generated.ts`** —— **没有** `routing.ts`；
  `--check` 分支（`gen-cli.mjs:487-492`）逐件比对的正是同一个 `targets`。
- `packages/skill-calorie/src/triggers/routing.ts`：只 `import` 生成物并再导出（`:34-38`、`:123-137`），
  自身只剩 `routesFor`／`routingSummary`／`ROUTE_*` 派生量与常量（`TWIN_WAKE_WORDS`、`NON_EXEC_REASONS`），**不承接记录数据**。
- 独立验收探针把 `routing.ts` 归 **① DERIVED**（`docs/skills/skill-calorie/t293-验收-命令自治.mjs:578`），② 桶实测 **0 处**。

### 四处「改前一句话／改后一句话」（三处＝R 列的三处；第 4 处见下）

| # | 位置（改前 → 改后行号） | 改前 | 改后 |
| --- | --- | --- | --- |
| 1 | `docs/agents/命令登记纪律.md:118-119` → `:118-119` | `src/triggers/routing.ts` 的记录面已是**生成物**：`pnpm gen` 重生成、`pnpm gen:check` 守，日常**零接触**，别再往它里面手写一行（它只剩类型再导出与逻辑）。 | 记录面住**生成物** `src/triggers/routes.generated.ts`：生成器 `scripts/gen-routes.mjs`，进 `pnpm gen` 写盘与 `pnpm gen:check` 逐件比对，日常**零接触**——加／删唤醒词动的是声明，不动这个文件。 |
| 2 | `docs/agents/命令登记纪律.md:119-120` → `:120-122` | （承上，只留「（它只剩类型再导出与逻辑）」这一句括注） | `src/triggers/routing.ts` **不承接记录数据**（只剩类型再导出与 `routesFor`／`routingSummary` 这类逻辑），它**不在生成器 `targets` 里、也不进 `gen:check` 的逐件比对**：手改它那道门不报红，兜住它的只有 `test/calorie-routing-81.test.mjs` 的覆盖对账与独立验收探针「记录面不许回到它身上」的判据；加／删唤醒词别改它。 |
| 3 | `docs/agents/命令登记纪律-照抄说明.md:50` → `:50-52` | `src/triggers/routing.ts` 的记录面**已不是手写位**：`#313` 起它是生成物（`pnpm gen` 重生成、`pnpm gen:check` 守），日常**零接触**； | `src/triggers/routing.ts` 的记录面**已搬进生成物**（`src/triggers/routes.generated.ts`，生成器 `scripts/gen-routes.mjs`：`pnpm gen` 写盘、`pnpm gen:check` 比对），日常**零接触**；`routing.ts` 自己**不承接记录数据**（只剩类型再导出与 `routesFor`／`routingSummary` 这类逻辑，不在生成器 `targets` 里），加／删唤醒词不动它； |
| 4 | `docs/agents/命令登记纪律-照抄说明.md:77` → `:79-80` | `src/triggers/routing.ts` 的记录面是生成物，**不必手改、也别手改**。 | `src/triggers/routing.ts` **不必手改、也别手改**——记录住生成物 `src/triggers/routes.generated.ts`，它自己不承接记录数据（只剩类型再导出与 `routesFor`／`routingSummary` 这类逻辑）。 |
| 5 | `docs/agents/命令登记纪律.md:139-140` → `:142-144`（**R 未列**，见下） | 探针把「加一条命令要碰的面」分三桶：① 已派生（不用手改）／② 受纪律约束的共用位（**`routing.ts`，必须手改但已被纪律认下**）／③ **漏网面**（既没派生也没纪律覆盖）。 | 探针把「加一条命令要碰的面」分三桶：① 已派生（不用手改）／② 受纪律约束的共用位（必须手改但已被纪律认下）／③ **漏网面**（既没派生也没纪律覆盖）。`routing.ts` 落 ① 不落 ②（记录面是生成物，见 §一）：② 空着是实况，不是探针坏了。 |

**第 5 处的来由（如实记，本席主动扩了一处，编排者可决定是否回退）**：R 只列了三处，本席按派单「逐句读一遍」把 §三 判据 4 读到了：
它把 `routing.ts` 举成 **② 桶「必须手改」** 的实例——这与本席刚改掉的第 1-4 处**同一句话的反面**，
且与探针实测（`routing.ts` 归 ①；② = **0 处**，探针自己打出「（0 处：#313 B 段起路由不在这一桶…）」）直接冲突：
不改它，同一个文件里两段话自相矛盾。改动只在同一份受权文件内、不动任何判据与数字（② 的空值由探针输出决定，不写死在文档里）。

三处改名后**逐句复核**并把每句对到实测：见 ④。

---

## ② 修 2 · 生成器注释（**只加注释，判据逻辑一字未动**）

`packages/skill-calorie/scripts/gen-routes.mjs`（守卫 `groupDecls()` 内、跨件重复判定之前，改前 `:120`）:

```diff
     pairOwner.set(pair, d.__src);
+    // **重复词组必须整组同迁**（同一 `wakeWord` 的多条记录留在同一件里）：同一件内的重复是冻结 SoT 的
+    // 既成事实——如 `记身材照` 在 `list=wake` 内合法出现 3 次（order 250/251/252，同住
+    // `src/cli/legacy/routes/scene-09.ts`）——下面的守卫只拦「同一词组被拆到两件」；把一个词组搬散，
+    // 判据不变、按整组搬即可（否则它会在这里假红）。
     const wordKey = d.list + '\u0000' + d.wakeWord;
     const files = wordFiles.get(wordKey) ?? new Set();
     if (files.size && !files.has(d.__src)) { … }
```

实例已核：`packages/skill-calorie/src/cli/legacy/routes/scene-09.ts:11-13` 三条 `记身材照`（`list=wake`，order 250/251/252）同住一件 ⇒ 整组搬不留红、拆成两件必被这条守卫拦下。

---

## ③ 验证（缺一不可 · 全部持锁实跑）

**GATE-RUN 声明**（只认 runId 相同、`exit=0` 的条目；窗口见 ⑤）：

- GATE-RUN runId=86cc4cf0-c247-432b-ac45-0bce60ac4ebf cmd="pnpm gen:check"
- GATE-RUN runId=0817b1e3-089c-4988-a4fc-adba6e38a3e3 cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
- GATE-RUN runId=2c204826-7c34-4e9b-95f5-2e075d81407f cmd="pnpm gen:check"
- GATE-RUN runId=e072fedb-59cc-465c-bee5-f7240064d785 cmd="pnpm test"
- GATE-RUN runId=a316a788-19a5-4c31-a117-33f00e252eda cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"

前两条是**改前基线**（同为持锁运行、同在工作窗口内，一并声明，不留无人认领的条目）。

1. **`pnpm gen:check` 仍 PASS，且生成物输出未变**：改后 runId `2c204826-7c34-4e9b-95f5-2e075d81407f`（exit=0，
   `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 1 个（weight）声明 9 条，未搬迁清单 92 条`）。
   与改前基线 runId `86cc4cf0-…` 逐字比对 5 件生成物 sha256 ⇒ **`SHA-IDENTICAL=True`**：
   `src/cli/keys.ts` `4db43569…`／`src/cli/registry.ts` `9e11f044…`／`combos.yaml` `ca17d2c3…`／
   `scripts/build-help.mjs` `4d44f03f…`／`src/triggers/routes.generated.ts` `e9ea993518ec33e3a69575dac1d8abf4a5e4c12e277b4c2d215ae3e5d339d7e9`（全 5 件一致）。
   ⇒ 修 2 只加注释：**逻辑没被碰**（若 sha 变了就说明动到了逻辑，本席按派单停下报告——没有发生）。
2. **全量 `pnpm test`**：runId `e072fedb-59cc-465c-bee5-f7240064d785`（exit=0）——`ℹ tests 1492`／`ℹ pass 1492`／
   `ℹ fail 0`／`ℹ cancelled 0`／`ℹ duration_ms 63031.4`。**1492/1492 通过、fail 0**。
3. **自治探针** `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`：runId `a316a788-19a5-4c31-a117-33f00e252eda`（exit=0）
   ——`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`，`P1` 段：
   `② DISCIPLINED-SHARED（… 0 处）`／`③ UNACCOUNTED（… 0 处）`／`UNACCOUNTED=0 处；树自相矛盾=0 处`。
4. **`git status --porcelain`（三条门跑完那一刻，`10:54Z`）**：只有本席那 3 个改动件——
   ` M docs/agents/命令登记纪律-照抄说明.md`、` M docs/agents/命令登记纪律.md`、` M packages/skill-calorie/scripts/gen-routes.mjs`；
   工作区其余为**空**（无他人条目）。本证据与 ⑤ 的对账源随后落盘，入库路径＝本席 5 个路径（见 ⑥ 范围声明）。

---

## ④ 「纪律文本确无其它假话」逐句复核结论

复核范围＝两份文档里**与记录面／路由／生成物／门**有关的每一句（其余段落本席未改、只读）。结论：**除上文 5 处外，未发现别的假话**，依据逐条落地：

| 被核的句子 | 实测对到什么 |
| --- | --- |
| `命令登记纪律.md:109` 生成物门在 CI `build-test` 作业真跑 | `.github/workflows/ci.yml:8` 作业 `build-test`、`:43` `- run: pnpm gen:check` |
| `:57`／`:146` `gen:check` 比盘上文件与生成器输出、不等即红 | `gen-cli.mjs:487-492` `--check` 分支逐件比对 `targets` 并报第一处差异（本席两次实跑 PASS） |
| `:120`（新）加／删唤醒词动声明不动生成物文件 | `gen-routes.mjs:48-62` `routeDeclarationSources()`：声明源＝`src/<能力>/routes.ts` ＋ `src/cli/legacy/routes/*.ts` |
| `:121-122`（新）`routing.ts` 不在 `targets`／不进逐件比对 | `gen-cli.mjs:451-470`（targets 五项，无 `routing.ts`）＋ `:487-492` |
| `:126` 漏声明会红在 `test/calorie-routing-81.test.mjs` 的「覆盖数 ≠ 权威声明数」 | `test/calorie-routing-81.test.mjs:105/122-123`（`coveredKeys: DECLARED_KEYS.length`、`键覆盖数 == 权威声明`） |
| `:127` 另一处红在 `cmd-registry-294.test.mjs` 的「代表唤醒词路由回同键」 | `packages/skill-calorie/test/cmd-registry-294.test.mjs:199-206`（`routesFor(spec.wakeWord)` 落回同一 `key`） |
| `:144`（新）`routing.ts` 落 ① 不落 ② | 探针 `t293-验收-命令自治.mjs:578` 归 `DERIVED（路由层：记录面是 routes.generated.ts，本件只再导出）`；实跑 ② = 0 处 |
| `:168` 出处「路由记录面从 `routing.ts` 迁成生成物…」 | `docs/skills/skill-calorie/t313b3-文档与验收-证据.md` 存在；`pnpm gen:check` 逐件输出确实逐件打 `routes.generated.ts`（本席实跑） |
| `照抄说明.md:31` 生成物手改无效／`:37-41` 生成链四步／`:42` 抢锁走 `run-locked` | `package.json:11-13`（`build` = `tsc -b && gen-cli --stamp`、`gen`／`gen:check`）；`gen-cli.mjs` 的 `--stamp`／陈旧守卫；本席全程 `run-locked` |
| `:52`／`:54` 搬迁与新增的改动面、一个键恰住一处 | `gen-routes.mjs` 的 `(list, order)` 重复／跨件同词守卫；`routeDeclarationSources()` 清单 |
| `:79-82`（新）加／删唤醒词不动 `routing.ts`；覆盖断言守生成物 | 同 ① 事实基础＋上表 `calorie-routing-81` 两行 |

**未改的一条相邻不精确（不是假话，报编排者定夺，本席未动）**：`照抄说明.md:39` 说第二次 `pnpm build`
把 `pnpm gen` 刚改写的生成物（`cli/keys.ts`、`cli/registry.ts`）编译进 `dist/`——#313 B 段起 `pnpm gen` 还写
`src/triggers/routes.generated.ts`（也要靠这次 build 进 `dist/` 才被路由层读到），此处**枚举不全**。
它是「不全」不是「说错」，且在 R 列的三处之外，按派单「只做这两件」本席**不动**，只在此记明。

---

## ⑤ 门禁对账（窗口口径 · 零放宽）

对账命令（逐字可复跑）：

```text
node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t313f-定点修复-证据.md --ticket 313f \
  --since 2026-09-13T10:47:20Z --until 2026-09-13T10:55:00Z --export docs/skills/skill-calorie/t313-gate-runs-313f.log
```

- 窗口 `--since` 取本席开工 ISO（等价于本终端 `START-ISO`），`--until` 取**末条门禁运行（`10:53:54.691Z`）之后、
  收尾 git 之前的整分**（与 `t313b4`／`t313v`／`t313r` 三席同口径）：把「证据与对账源入库」的那次 `git add`／`commit`／`push` 留在窗口外
  ——协议 §2.4 写明「窗口外的条目（他人票号、提交后的 git 条目）不受影响」；后移窗口者按同口径声明那三条即可。
- 窗口内 `ticket=313f` 的 `RUN` 条目 = **5 条**，即 ③ 声明的 5 条（基线 2 ＋ 改后 3），全部 `exit=0`；
  **未用任何 `--allow-*`**（故本证据内无 `GATE-RELAX` 行，也没有需要放宽的条目）。
- **窗口内他人票号条目：无**（`.scratch/locks/gate-runs.log` 里本次窗口的相邻条目是 `313r` 的收尾 git 运行，
  时间在 `--since` 之前，不在窗口内）。若第三方后移窗口看到别人条目，按 ⑤ 口径声明，**不加放宽开关**。
- 受跟踪的对账源：`docs/skills/skill-calorie/t313-gate-runs-313f.log`（随本次提交入库）；
  导出件自身写明 `条目数：5｜导出时间：2026-09-13T10:55:36.523Z`，与本节的 `since`／`until` 逐字一致。
- **实测输出**（`exit=0`）：`RESULT: matched=5/5 auditEntries=1538 scoped=5 undeclared=0` ＋ `gate-audit: PASS`；
  `EXPORT: docs/skills/skill-calorie/t313-gate-runs-313f.log（窗口内 RUN 条目 5 条，受 git 跟踪）`。
- **窗口外（收尾动作，如实留痕、**不**作 `GATE-RUN` 声明）**：把本证据与对账源入库的那三步持锁运行为
  `git add -- <本席 5 个路径>`（runId `adb188ff-2af1-4c74-9982-b3ef3f6bfb34`）／
  `git commit -F .scratch/t313f/msg.txt`（runId `b3517acc-3f9f-4be5-a20b-4979ec107b9f`）／
  `git push origin master`（runId `65beb864-a8cf-454d-bded-11105beaf90a`），三条 `at` 均 > `--until 10:55:00Z`；
  按协议 §2.4「窗口外的条目…不受影响」不进本次对账——把它们塞进窗口却又不声明，反而会因「窗口内有无人声明的 `RUN` 条目」误红。

---

## ⑥ 未做／未确证（范围声明）

- **未做**：`close` 票、改票面、碰任何其它文件；未改任何判据／门／棘轮／基线数字；未安装任何东西；未用危险 git 命令
  （只有 `git add <本席 5 个路径>`、`git commit -F`、`git push origin master`）。
- **未确证**：`pnpm help:examples:check` 那道门（派单未要求；`照抄说明.md:13` 自述它带票外既有红，本席未碰）。
- **本席主动扩的一处**：`命令登记纪律.md` 判据 4 的 ② 桶实例（上文 ① 第 5 处）。编排者若判定越范围，回退它一行即可，
  不影响 ①②③ 任何结论（它不参与生成物、不被任何门读）。
- 本席推 `origin/master` 时会把复核席那份**本地未推**的 `d03032a`（对抗式复核报告）一并送上——按派单这是**预期的**，
  其内容一字未动。
