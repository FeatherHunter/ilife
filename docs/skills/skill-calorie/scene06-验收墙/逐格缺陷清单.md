# 场景06 目标管理 · 逐格缺陷清单（34 格）

- 批次：`docs/skills/skill-calorie/scene06-验收墙/`（生产者 `produce.mjs`，墙 `gen-wall.mjs`，清单 `manifest.json`）
- 口径出处：仓规 `docs/agents/视觉验收墙.md`（§3 收口七步／§4 完成判据／§6.2 骨架／§7 坑）
- 墙：`手机墙-390.html`（34 格 × 390 宽 × 3 列，看塌列）、`桌面墙-1280.html`（34 格 × 1280 宽 × 1 列，缩 0.469 显示，看排布）
- 细看入口：`总索引.html`（按页面族分组）／`链路索引.html`（prompt → 唤醒词 → 命令 → 绝对路径，可点开）
- 墙格数 ＝ 清单行数 ＝ 34（生成器自检按清单点名逐件查盘，少一件即点名并 exit 1）。
- 自检读数：正例 `node gen-wall.mjs . 手机墙-390.html 390 820` → **34 格；链接 105 条（手机墙-390.html 69 ＋ 总索引.html 36）；缺失 0 -> 可发，exit 0**；桌面端 `node gen-wall.mjs . 桌面墙-1280.html 1280 860` 同样 **34 格；链接 105 条（桌面墙-1280.html 69 ＋ 总索引.html 36）；缺失 0 -> 可发，exit 0**；
  反例（清单里塞不存在文件：把「看今日目标」的 `file` 改成 `故意写的不存在页.html`，再跑 `node gen-wall.mjs --check .`）→ **33 格；链接 174 条；缺失 1 -> 不可发，exit 1**，并点名 `清单点名却没有文件：14 看今日目标 -> 故意写的不存在页.html`；从备份按字节还原 manifest.json（复原后与变异前逐字节一致）再跑 `--check` → **34 格；链接 174 条；缺失 0 -> 可发，exit 0**。
- 数据来源：一次性临时库（`seedFull` 种子 ＋ `CALORIE_TODAY=2026-09-07`）；写类命令写的是这份临时库，**没碰真库**。
- 唤醒词与命令：主产物 33 件逐条取 `src/triggers/scene-06-goal.ts` 的 25 条冻结词表原文（跑它自己的 `main_prompt.cli`，不另拟命令）；11 件预检页是派生命令（见本目录 produce.mjs 预检分支：自动算三条按冻结原文加 profile cut，其余 8 条只带 wake）；第 34 格是自造词「看目标推荐」，命令取路由层别名表 `src/goal/routes.ts:37` 原文。

## 一、机器读数逐格（生产者出，不靠人眼）

| 格 | 唤醒词 | 类型 | 模板 | 字节 | 完整文档？ | 文件 |
|---|---|---|---|---|---|---|
| 1 | 定营养目标 | 回执型 | `doc-shell` | 88418 | 是 | `定营养目标-回执.html` |
| 2 | 定营养目标 | 过程型 | `doc-shell` | 74766 | 是 | `定营养目标-预检.html` |
| 3 | 定营养目标(自动算) | 过程型 | `doc-shell` | 76797 | 是 | `定营养目标(自动算)-预检.html` |
| 4 | 定体重目标 | 回执型 | `doc-shell` | 86815 | 是 | `定体重目标-回执.html` |
| 5 | 定体重目标 | 过程型 | `doc-shell` | 74302 | 是 | `定体重目标-预检.html` |
| 6 | 定体重目标(自动算截止) | 回执型 | `doc-shell` | 87535 | 是 | `定体重目标(自动算截止)-回执.html` |
| 7 | 定体重目标(自动算截止) | 过程型 | `doc-shell` | 74744 | 是 | `定体重目标(自动算截止)-预检.html` |
| 8 | 定体重目标(含起始日) | 回执型 | `doc-shell` | 88218 | 是 | `定体重目标(含起始日)-回执.html` |
| 9 | 定体重目标(含起始日) | 过程型 | `doc-shell` | 74705 | 是 | `定体重目标(含起始日)-预检.html` |
| 10 | 定饮水目标 | 回执型 | `doc-shell` | 86611 | 是 | `定饮水目标-回执.html` |
| 11 | 定饮水目标 | 过程型 | `doc-shell` | 74293 | 是 | `定饮水目标-预检.html` |
| 12 | 定饮水目标(自动算) | 过程型 | `doc-shell` | 76566 | 是 | `定饮水目标(自动算)-预检.html` |
| 13 | 一键定全套目标 | 过程型 | `doc-shell` | 77138 | 是 | `一键定全套目标-预检.html` |
| 14 | 看今日目标 | 结果型 | `doc-shell` | 73805 | 是 | `看今日目标.html` |
| 15 | 看本周目标 | 结果型 | `doc-shell` | 73805 | 是 | `看本周目标.html` |
| 16 | 看营养目标进度 | 结果型 | `doc-shell` | 81752 | 是 | `看营养目标进度.html` |
| 17 | 看体重目标进度 | 结果型 | `doc-shell` | 73541 | 是 | `看体重目标进度.html` |
| 18 | 看饮水目标进度 | 结果型 | `doc-shell` | 73805 | 是 | `看饮水目标进度.html` |
| 19 | 看目标对比实际 | 结果型 | `doc-shell` | 83770 | 是 | `看目标对比实际.html` |
| 20 | 看目标完成度 | 结果型 | `doc-shell` | 74145 | 是 | `看目标完成度.html` |
| 21 | 看即将到期的目标 | 结果型 | `doc-shell` | 71629 | 是 | `看即将到期的目标.html` |
| 22 | 看目标完成率(按周) | 结果型 | `doc-shell` | 81752 | 是 | `看目标完成率(按周).html` |
| 23 | 看目标完成率(按月) | 结果型 | `doc-shell` | 81752 | 是 | `看目标完成率(按月).html` |
| 24 | 改营养目标 | 回执型 | `doc-shell` | 88146 | 是 | `改营养目标-回执.html` |
| 25 | 改营养目标 | 过程型 | `doc-shell` | 74936 | 是 | `改营养目标-预检.html` |
| 26 | 改体重目标 | 回执型 | `doc-shell` | 87228 | 是 | `改体重目标-回执.html` |
| 27 | 改体重目标 | 过程型 | `doc-shell` | 74551 | 是 | `改体重目标-预检.html` |
| 28 | 改饮水目标 | 回执型 | `doc-shell` | 86661 | 是 | `改饮水目标-回执.html` |
| 29 | 改饮水目标 | 过程型 | `doc-shell` | 74339 | 是 | `改饮水目标-预检.html` |
| 30 | 暂停所有目标 | 回执型 | `doc-shell` | 85720 | 是 | `暂停所有目标-回执.html` |
| 31 | 重启所有目标 | 回执型 | `doc-shell` | 85658 | 是 | `重启所有目标-回执.html` |
| 32 | 看目标历史完成 | 结果型 | `doc-shell` | 74139 | 是 | `看目标历史完成.html` |
| 33 | 看目标预测达成 | 结果型 | `doc-shell` | 68480 | 是 | `看目标预测达成.html` |
| 34 | 看目标推荐 | 结果型 | `doc-shell` | 73285 | 是 | `看目标推荐.html` |

## 二、初筛发现的缺陷（机器可见 ＋ 本席抽查，逐条给证据）

1. **片段件**：本轮实读 **0 件**——34 件产物逐件是完整文档（模板 `doc-shell`，见一段「完整文档？」列全为「是」）。
2. **同一个页面顶着多条词**（屏幕上看不出差别）：
  - 3 条词的产物**逐字节相同**（5 字节量级）：看今日目标、看本周目标、看饮水目标进度
  - 3 条词的产物**逐字节相同**（7 字节量级）：看营养目标进度、看目标完成率(按周)、看目标完成率(按月)
3. **写入缺陷（产品侧行为，留档 ＋ 本轮复跑复核）**：首轮记过一条——`定体重目标`／`改体重目标` 只给 `kg`、不给截止日时会把 `daily_goal.goal_deadline` **抹成 NULL**，紧接着念「看即将到期的目标」→ **exit 4「无到期目标（daily_goal.goal_deadline 缺失）」**。**本轮（#611 重出时）同一套复跑已不复现**：种子库 `goal_deadline=2026-12-31` → `calorie.goal.weight {"kg":68}` exit 0 → 写后 `goal_deadline` 仍 `2026-12-31` → `calorie.view.goal-expiring` **exit 0**；带截止日写回（`{"kg":68,"deadline":"2026-09-19"}`）后也 exit 0。本批产物那一页（格 21）仍按种子表补截止日（见 `produce.mjs` 的 `SUBST_OVERRIDE`），产物本身与这条产品侧行为无关。
4. **历次重出留档**（旧读数，**现状以一段与本轮重出记为准**）：首轮（2026-09-15）有三处当场不达标——10 件写后回执页是 `receipt` 片段（206–275 字节，没有 `<!doctype html>`／charset／样式／页框）；回执正文把内部词 `op=update · id=1` 印上屏；4 件结果页（看目标对比实际 1357 B／看目标完成度 1779 B／看即将到期的目标 1204 B／看目标历史完成 1779 B）是 `fragment` 片段。这三处已由 `056f116`／`af1280d` 两轮修掉，本轮实读 0 片段、回执页均 85 KB 量级。

## 三、待你看（墙上看得出、判据抓不到的那一类）

滚一遍两张墙，逐格记：挤／塌列／被藏起来的横滚／层级乱／文案难读／两端差异。看完没缺陷也算结论，但要写明**看了哪些格**。

| 格 | 该确认什么 | 你的结论 |
|---|---|---|
| 全部 34 格 | 双端各滚一遍，逐格比对 | （待填） |
| 34（看目标推荐，自造词） | 这一页与格 3「定营养目标(自动算)」的预检页内容有重叠（都摆推荐值与依据）：肉眼分不分得出差别；自造词该不该与冻结词同墙并收 | （待填） |
| 1／4／6／8／10／24／26／28／30／31 | 回执页：内部词（`op=`／函数名）还有没有印上屏 | （待填） |
| 14／15／18／22／23 | 判据：同页多词（逐字节相同）该不该按词出不同内容 | （待填） |
| 19／20／21／32 | 曾是片段的 4 件结果页，整页化后还有没有版面问题 | （待填） |

## 四、本批的缺口（清单 `gaps` 段同源）

- （无）

## 五、有意不出产物及其原因（照做法 §3-4）

- **暂停所有目标／重启所有目标 的预检确认页**：这两条词**无空位**（不带参数），按 2026-09-13 裁定不出预检页，只在写后回执页上出现。
- **定营养目标(自动算)／定饮水目标(自动算)／一键定全套目标 的独立回执页**：这三条词的命令本身就是预检确认页（先算给我看）；写由后续三条写命令落地，回执挂在那三条词上——不是漏做，见 `SKILL.md:75`。
- **看目标配置／看目标状态 的产物页（本批不出）**：两条自造词不在场景 06 的 25 条冻结词表里（只在路由层别名表 `src/goal/routes.ts:34-35`），也没有 HELP 卡片（#291 乙：查找层别名查得到跑得通，权威词表与快照不动）；它们的整页装配件随 #290 落地（本席实跑：两条都是 `doc-shell` 完整文档），产物验收挂 #290 自己的件——按 #611 的范围本批只收「看目标推荐」一行；链路索引另附 2 行只给命令不给产物。
- **看目标完成率(按周)／(按月) 的独立页**：两条词今天都路由到 `calorie.view.goal-progress`，与「看本周目标」同页（只是窗口参数相同）——产物在，但**屏幕上看不出差别**，这一条该由用户裁：要不要按词出不同的窗口。

## 六、2026-09-16 重出记（#611 · 看目标推荐接入）

- 起因（#611）：`produce.mjs` 一直把「看目标推荐」记作「无产物」（附 1 行），但它随 #589 已出整页（73285 B）。本批把它收进清单 `rows`，墙格数随之 33 → **34**。
- 只动本目录：`produce.mjs` 自造词段由「另附三行」改成「清单一行（`看目标推荐.html`，命令取路由层别名表 `src/goal/routes.ts:37` 原文 `--params '{"profile":"cut"}'`）＋另附两行（看目标配置／看目标状态，写明本批不出页的原因：不在 25 条冻结词表里、无 HELP 卡片，整页件随 #290 落地，产物验收挂 #290）」，墙／总索引／链路索引／清单随之重出；**没改任何产品代码与测试，没动别图验收墙**。
- 产物读数：**34 件逐件完整文档、0 片段**；`看目标推荐.html` ＝ **73285 字节**（模板 `doc-shell`），与 #589 报的字节一致。
- 自检（同一次重出）：正例 `node gen-wall.mjs . 手机墙-390.html 390 820` → 34 格；链接 105 条；缺失 0 -> 可发，**exit 0**；`node gen-wall.mjs . 桌面墙-1280.html 1280 860` 同；反例（清单塞 `故意写的不存在页.html`）→ 缺失 1 -> 不可发，**exit 1** 并点名 `14 看今日目标`；按字节还原后复跑 exit 0。
- **漂移对账（对 HEAD `13bb4c4`）**：33 件**全部有实质变化，不止时间戳**——逐件删 22–30 行／增 23–31 行。差异主体是并行席已落的公共层改动 `8920faa 形状(567): 公共层清账——表卡撤680上限／列表首列不折行／toc触摸44px／字号收档…`：删行命中 `font-size: 14px` 129×、`font-size: 11.5px` 76×、`font-size: 12.5px` 66×、`font-size: 10.5px` 66×、`padding: 6px 12px` 33×、`max-width: 680px` 33×；增行命中 `font-size: 11px` 132×、`font-size: 15px` 129×、`font-size: 18px` 99×、`font-size: 12px` 76×、`font-size: 13px` 66×、`min-height: 44px` 33×、`display: inline-flex`／`box-sizing: border-box` 各 33×；另有页壳结构（`<section class="ilife-block ilife-block-page-shell">` 20×）与时间戳串 `… · 版本 0.1.0`（64×）。**本席不改数**：逐格字节以 `manifest.json` 与一段表为准，漂移不在本图写集、由公共层改动所致，未改任何产品代码与测试。
- 逐件字节（旧 → 新）：1 86957→88418｜2 74743→74766｜3 74747→74770｜4 85560→86815｜5 74279→74302｜6 85964→87535｜7 74721→74744｜8 86637→88218｜9 74682→74705｜10 85469→86611｜11 74270→74293｜12 74516→74539｜13 75088→75111｜14 73782→73805｜15 73782→73805｜16 81729→81752｜17 73518→73541｜18 73782→73805｜19 83410→83770｜20 74122→74145｜21 71130→71629｜22 81729→81752｜23 81729→81752｜24 86685→88146｜25 74913→74936｜26 85646→87228｜27 74528→74551｜28 85519→86661｜29 74316→74339｜30 85699→85720｜31 85637→85658｜32 74116→74139｜33 68457→68480｜34 **新增 73285**。
- 生产期 dist 指纹（S3 返工已改记可复算的单件 SHA，原合并 SHA 作废留档）：`dist/cli/cmd_read.js` SHA256 `C7FBA3692AB68C3EF2B5064F0541F8A54F8713E61EF5FCBFBD6FBB91BE2C0E3F`、`dist/index.js` SHA256 `E3062238B6667C75312F83839E79E20CD50181DCE3F85B553CAAB16CFE3BD1F0`、`dist/triggers/scene-06-goal.js` SHA256 `B17AAF2C879AB13AA708FAA38DFFB4E440B719A2E05F3D9508246D823D06D1BD`（各 `Get-FileHash -Algorithm SHA256` 可复算，`dist` 文件数 1422；原合并 SHA256 `07693c18b263750f9be54db582c2c142e6888d38fcc3d9b4fb618ef8c917e548` 无配方不可复核，作废）；编译走锁 `ticket=611`（`node node_modules/typescript/bin/tsc -b --force packages/skill-calorie`，exit 0）。
- 回执页内部词那条：`op=update` 本轮在 34 件里 **0 命中**（首轮那条已随重出消失）；现回执行写的是 `calorie-cmd-read … ｜ id=1 | 日期 … | 影响 1 行 | 字段 kg`，`id=1`／`字段 kg` 算不算「内部词印上屏」留给三段人看那一条判。

## 七、2026-09-16 #611 返工记（S1–S3 · 本席）

- S1（红线）：`produce.mjs` 预检分支对 11 条一律跑 `{"wake":…}`，自动算三条（定营养目标(自动算)／定饮水目标(自动算)／一键定全套目标）缺冻结原文的 `"profile":"cut"`，墙上三格出「不推荐／档案不齐，本页不出推荐值」。改法：新增 `AUTO_PROFILE_WAKES` 三词集合，命中即跑 `{"profile":"cut","wake":"<词>"}`，清单 `cli` 同步实跑参数；重出后三格字节 74770→**76797**／74539→**76566**／75111→**77138**，页内均有「推荐方案与依据」＋TDEE，无「不出推荐值」。
- S1 附带：清单口径句「前 33 格逐条取冻结词表原文」改成实况口径（主产物 33 件取冻结原文；11 件预检页是派生命令，自动算三条按冻结原文加 `profile cut`，见 `produce.mjs` 预检分支）。
- S2：`gen-wall.mjs` 的 `selfCheck` 只判清单点名＋页上死链，墙上砍掉一格仍绿。改法：加判「清单 `rows.length` ＝ 两张墙 `<figure>` 数 ＝ 总索引卡数」，读盘计数，不等即点名 exit 1。自证：手机墙砍一格 → `墙格数不对：手机墙-390.html 有 33 格，清单有 34 行`，链接 172 条，缺失 1，exit 1；还原后 34 格 174 条 exit 0。
- S3-1：本文件原第 92 行 dist「合并 SHA256 `07693c18…`」无配方不可复核 → 改记可复算的单件 SHA（见 §六末段，`Get-FileHash` 直算）。
- S3-2：过程型 check 文案「页面是不是静态无脚本」与事实冲突（34 件都带 doc-shell 页内运行时脚本）→ 改成「页面无取数／无AI生成逻辑」（`produce.mjs` 的 `CHECK.过程型`，墙与总索引随之重出，旧文案 0 命中）。
- GATE-RUN（`.scratch/locks/gate-runs.log` 抄 runId）：生产 `76c60204` exit 0（34 件完整文档 0 片段）；手机墙 `70c1d4c4` exit 0（34 格 105 条）；桌面墙 `df45ccf7` exit 0（34 格 105 条）；`--check` `726897ae` exit 0（34 格 174 条）；清单反例变异 `770b51ea` exit 0 → 反例红 `9023d0ea` exit 1（33 格缺失 1，点名 `14 看今日目标`）→ 还原 `14a90541` exit 0 → 复绿 `5af7f86d` exit 0；墙砍格变异 `e0b25bb4` exit 0 → 砍格红 `c46adf95` exit 1（墙格数不对 33 vs 34）→ 墙还原 `56fdeba8` exit 0 → 终绿 `1451236e` exit 0；单件 SHA 读数 `12c113ed` exit 0。
- 只动本目录 `docs/skills/skill-calorie/scene06-验收墙/`；未改产品代码与测试；未动别图验收墙。
- 重出漂移：除上述三格外其余 31 件仅时间戳一行变化（`时间戳版本 13:49:58 → 14:20:14`），`manifest.json` 字节数不变（见一段表与上节对照）；三格为实质变化＋时间戳。
