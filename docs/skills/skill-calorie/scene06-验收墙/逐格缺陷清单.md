# 场景06 目标管理 · 逐格缺陷清单（33 格）

- 批次：`docs/skills/skill-calorie/scene06-验收墙/`（生产者 `produce.mjs`，墙 `gen-wall.mjs`，清单 `manifest.json`）
- 口径出处：仓规 `docs/agents/视觉验收墙.md`（§3 收口七步／§4 完成判据／§6.2 骨架／§7 坑）
- 墙：`手机墙-390.html`（33 格 × 390 宽 × 3 列，看塌列）、`桌面墙-1280.html`（33 格 × 1280 宽 × 1 列，缩 0.469 显示，看排布）
- 细看入口：`总索引.html`（按页面族分组）／`链路索引.html`（prompt → 唤醒词 → 命令 → 绝对路径，可点开）
- 自检读数：正例 `node gen-wall.mjs . 手机墙-390.html 390 820` → **33 格；链接 101 条；缺失 0 -> 可发，exit 0**；
  反例（把清单里「看今日目标」的 file 改成 `故意写的不存在页.html` 再跑 `--check`）→ **缺失 1 -> 不可发，exit 1 并点名那一份**；还原后复跑 exit 0。
- 数据来源：一次性临时库（`seedFull` 种子 ＋ `CALORIE_TODAY=2026-09-07`）；写类命令写的是这份临时库，**没碰真库**。
- 唤醒词与命令：逐条取 `src/triggers/scene-06-goal.ts` 的 25 条冻结词表原文（跑它自己的 `main_prompt.cli`，不另拟命令）。

## 一、机器读数逐格（生产者出，不靠人眼）

| 格 | 唤醒词 | 类型 | 模板 | 字节 | 完整文档？ | 文件 |
|---|---|---|---|---|---|---|
| 1 | 定营养目标 | 回执型 | `doc-shell` | 86957 | 是 | `定营养目标-回执.html` |
| 2 | 定营养目标 | 过程型 | `doc-shell` | 74743 | 是 | `定营养目标-预检.html` |
| 3 | 定营养目标(自动算) | 过程型 | `doc-shell` | 74747 | 是 | `定营养目标(自动算)-预检.html` |
| 4 | 定体重目标 | 回执型 | `doc-shell` | 85560 | 是 | `定体重目标-回执.html` |
| 5 | 定体重目标 | 过程型 | `doc-shell` | 74279 | 是 | `定体重目标-预检.html` |
| 6 | 定体重目标(自动算截止) | 回执型 | `doc-shell` | 85964 | 是 | `定体重目标(自动算截止)-回执.html` |
| 7 | 定体重目标(自动算截止) | 过程型 | `doc-shell` | 74721 | 是 | `定体重目标(自动算截止)-预检.html` |
| 8 | 定体重目标(含起始日) | 回执型 | `doc-shell` | 86637 | 是 | `定体重目标(含起始日)-回执.html` |
| 9 | 定体重目标(含起始日) | 过程型 | `doc-shell` | 74682 | 是 | `定体重目标(含起始日)-预检.html` |
| 10 | 定饮水目标 | 回执型 | `doc-shell` | 85469 | 是 | `定饮水目标-回执.html` |
| 11 | 定饮水目标 | 过程型 | `doc-shell` | 74270 | 是 | `定饮水目标-预检.html` |
| 12 | 定饮水目标(自动算) | 过程型 | `doc-shell` | 74516 | 是 | `定饮水目标(自动算)-预检.html` |
| 13 | 一键定全套目标 | 过程型 | `doc-shell` | 75088 | 是 | `一键定全套目标-预检.html` |
| 14 | 看今日目标 | 结果型 | `doc-shell` | 73782 | 是 | `看今日目标.html` |
| 15 | 看本周目标 | 结果型 | `doc-shell` | 73782 | 是 | `看本周目标.html` |
| 16 | 看营养目标进度 | 结果型 | `doc-shell` | 81729 | 是 | `看营养目标进度.html` |
| 17 | 看体重目标进度 | 结果型 | `doc-shell` | 69527 | 是 | `看体重目标进度.html` |
| 18 | 看饮水目标进度 | 结果型 | `doc-shell` | 73782 | 是 | `看饮水目标进度.html` |
| 19 | 看目标对比实际 | 结果型 | `doc-shell` | 83410 | 是 | `看目标对比实际.html` |
| 20 | 看目标完成度 | 结果型 | `doc-shell` | 74122 | 是 | `看目标完成度.html` |
| 21 | 看即将到期的目标 | 结果型 | `doc-shell` | 71130 | 是 | `看即将到期的目标.html` |
| 22 | 看目标完成率(按周) | 结果型 | `doc-shell` | 81729 | 是 | `看目标完成率(按周).html` |
| 23 | 看目标完成率(按月) | 结果型 | `doc-shell` | 81729 | 是 | `看目标完成率(按月).html` |
| 24 | 改营养目标 | 回执型 | `doc-shell` | 86685 | 是 | `改营养目标-回执.html` |
| 25 | 改营养目标 | 过程型 | `doc-shell` | 74913 | 是 | `改营养目标-预检.html` |
| 26 | 改体重目标 | 回执型 | `doc-shell` | 85646 | 是 | `改体重目标-回执.html` |
| 27 | 改体重目标 | 过程型 | `doc-shell` | 74528 | 是 | `改体重目标-预检.html` |
| 28 | 改饮水目标 | 回执型 | `doc-shell` | 85519 | 是 | `改饮水目标-回执.html` |
| 29 | 改饮水目标 | 过程型 | `doc-shell` | 74316 | 是 | `改饮水目标-预检.html` |
| 30 | 暂停所有目标 | 回执型 | `doc-shell` | 85699 | 是 | `暂停所有目标-回执.html` |
| 31 | 重启所有目标 | 回执型 | `doc-shell` | 85637 | 是 | `重启所有目标-回执.html` |
| 32 | 看目标历史完成 | 结果型 | `doc-shell` | 74116 | 是 | `看目标历史完成.html` |
| 33 | 看目标预测达成 | 结果型 | `doc-shell` | 68392 | 是 | `看目标预测达成.html` |

## 二、初筛发现的缺陷（机器可见 ＋ 本席抽查，逐条给证据）

1. **写后回执页整页缺位**（10 件，格 1／4／6／8／10／24／26／28／30／31）：模板 `receipt`、206–275 字节，双击打开只有一段裸区块——没有 `<!doctype html>`、没有 charset、没有样式、没有页框。地图 #153 的目的地口径是「产物＝完整文档」，这 10 件当场不达标。
2. **回执把内部词印上屏**：回执正文里写着 `op=update · id=1`（`定营养目标-回执.html` 逐字如此）。这正是墙存在的理由那一类缺陷（判据抓不到、人一眼看见）。
3. **结果页还剩 4 件是片段**（模板 `fragment`）：看目标对比实际（1357 B）／看目标完成度（1779 B）／看即将到期的目标（1204 B）／看目标历史完成（1779 B）——走的是旧片段渲染件，不是整页装配。
4. **同一个页面顶着多条词**（屏幕上看不出差别）：
  - 3 条词的产物**逐字节相同**（5 字节量级）：看今日目标、看本周目标、看饮水目标进度
  - 3 条词的产物**逐字节相同**（7 字节量级）：看营养目标进度、看目标完成率(按周)、看目标完成率(按月)
5. **写入缺陷（本席复跑出来的，非本批产物本身的问题）**：`定体重目标`／`改体重目标` 只给 `kg`、不给截止日时，会把 `daily_goal.goal_deadline` **抹成 NULL**；紧接着念「看即将到期的目标」→ **exit 4「无到期目标（daily_goal.goal_deadline 缺失）」**。复跑读数：种子库 `goal_deadline=2026-12-31` → 跑 `calorie.goal.weight {"kg":68}` → `goal_deadline=null` → `calorie.view.goal-expiring` exit 4 → 带截止日写回后 exit 0。本批产物之所以有这一页，是因为生产者按种子表补了截止日（见 `produce.mjs` 的 `SUBST_OVERRIDE`）。

## 三、待你看（墙上看得出、判据抓不到的那一类）

滚一遍两张墙，逐格记：挤／塌列／被藏起来的横滚／层级乱／文案难读／两端差异。看完没缺陷也算结论，但要写明**看了哪些格**。

| 格 | 该确认什么 | 你的结论 |
|---|---|---|
| 全部 33 格 | 双端各滚一遍，逐格比对 | （待填） |
| 1／4／6／8／10／24／26／28／30／31 | 回执片段：裸区块＋`op=update · id=1` 印上屏，能不能接受 | （待填） |
| 14／15／18／22／23 | 判据：同页多词（逐字节相同）该不该按词出不同内容 | （待填） |
| 19／20／21／32 | 4 件片段结果页：要不要现在就整页化 | （待填） |

## 四、本批的缺口（清单 `gaps` 段同源）

- （无）

## 五、有意不出产物及其原因（照做法 §3-4）

- **暂停所有目标／重启所有目标 的预检确认页**：这两条词**无空位**（不带参数），按 2026-09-13 裁定不出预检页，只在写后回执页上出现。
- **定营养目标(自动算)／定饮水目标(自动算)／一键定全套目标 的独立回执页**：这三条词的命令本身就是预检确认页（先算给我看）；写由后续三条写命令落地，回执挂在那三条词上——不是漏做，见 `SKILL.md:75`。
- **三条自造词：看目标推荐／看目标配置／看目标状态**：它们不在场景 06 的 25 条冻结词表里（只在路由层，`src/goal/routes.ts:34-37`），也没有 HELP 卡片；归属与落地是票 #291 在办的事。
- **看目标完成率(按周)／(按月) 的独立页**：两条词今天都路由到 `calorie.view.goal-progress`，与「看本周目标」同页（只是窗口参数相同）——产物在，但**屏幕上看不出差别**，这一条该由用户裁：要不要按词出不同的窗口。
