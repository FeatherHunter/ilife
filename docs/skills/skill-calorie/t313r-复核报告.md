# #313 对抗式复核报告（313r · 对抗式复核席）

**HEAD** `cb5b4c55e5267b1a038e81c4087f91ea104b0683`（会话开工前 HEAD＝`ba1d4dc`）；`git status --porcelain` 空；`.scratch/locks/gate.lock` 不存在。
**纪律自证**：本席**未改仓库任何交付文件**；全部变异只在**副本**上做（仓外 `%TEMP%` 最小子树／`.scratch/t313r/` 副本目录），末次核对 `git status --porcelain` **0 行**。日志全落 `.scratch/t313r/logs/`。

## 一 必跑项逐条结论

| # | 结论 | 怎么验的（证据） |
| --- | --- | --- |
| 1 | 环境干净 | `HEAD=cb5b4c5`；porcelain 0 行；lock 不存在（`logs/00-env.txt`） |
| 补一 | **纯搬迁 diff 为空** | `git diff ba1d4dc..HEAD --numstat --` 那 8 件 ⇒ **0 行**；四件生成物 sha256 与 `ba1d4dc` 全等（`logs/20-run2.log`） |
| 2 | build 0；test **1492／1492**（fail 0） | `pnpm build`／`pnpm test` 持锁（`logs/10-build.log`／`11-test.log`） |
| 3 | `gen:check` PASS；oracle 16 面 `DEEP-EQUAL=yes`、`surfaceMissing=0`、exit 0 | 先 build 再跑；**摘要 `DIST-SHA=4602f79d6927` 与本席自算 `dist/triggers/routing.js` sha256 前 12 位逐字相同**（`logs/12/13`） |
| 4 | 冻结地基**零改动** | 通配 `src/triggers/scene-*.ts`＋`test/calorie-routing-81.test.mjs`＋`cmd_read.ts`／`write.ts` ⇒ diff **0 行**；`src/triggers/` 全树仅 3 件变（routeSpec／routes.generated／routing） |
| 5 | **P-A 有牙** | 副本改 `src/cli/legacy/routes/scene-03.ts` 源码、**不 build** ⇒ `GEN-STALE FAIL` 点名该件（印记 `0dc1f447… ≠ a4f04e5f…`）；还原 ⇒ 回绿；能力件 `src/weight/routes.ts` 同结论（`logs/34-mut2.log`） |
| 6 | P-B `PB-SUMMARY PASS`；自造变异会红 | 真目录 exit 0；副本造"一键住两处" ⇒ `PB-KEYS dupKeys=1`＋`PB-SCENE mismatch=1`＋`PB-SUMMARY FAIL`（`logs/42-mut4.log`） |
| 7 | **变异两连** | 手改生成物 ⇒ `GEN-CHECK FAIL（第 519 行起）`；dist 注入跨件同名 `wakeWord` ⇒ 生成器抛「同一 list 内同 wakeWord 跨件重复（wake · "看今日主页"）：scene-01.ts ／ scene-09.ts」；两者还原后回绿（`logs/32/34`） |
| 8 | t293 `P1..P5=PASS 5/5`、`UNACCOUNTED=0`、动态证据齐 | 持锁真跑（`logs/40-t293.log:76`、`:19` 路由三面、`:29`）；归类判据变异 ⇒ **P1=FAIL（其余 4 项仍 PASS）**＋判据行逐字出现（见三） |
| 9 | 导出面 15 名一个不少；行为对 | `Object.keys(dist/triggers/routing.js)` ＝ **15**（12 值＋3 函数）；`routesFor('记身材照')`＝**3 条 exec**（`["wakeWord","scene","kind","key","cli"]`）、`('查身材照')`1 条、空词／未命中 0 条；总记录 495＝436＋58＋1（`logs/31-exports.log`） |
| 10 | 纪律文本**大体成事实**，一处措辞不实 | `命令登记纪律.md:87,118`／`照抄说明.md:48,50,59,77`：路径 `legacy/scene-NN.ts`、`legacy/routes/scene-NN.ts` 正确；"一条命令的键恰住一片"符合 `gen-cli.mjs:89-122`（只收 `isFile()`，子目录天然跳过）；收口三条判据与棘轮/生成器现状一致。**不实处**见缺陷 1 |
| 11 | 票面 `## 进度：95%` 在（第 65 行）＋ 待确认 7 条齐 | `gh issue view 313`：进度节 **45% 与 95% 各一**（A／B 段各一，95% 在 `:65`）；真实换行、无 BOM、无字面 `\n`；`待确认` 清单 7 条（`logs/30-issue313.txt`） |
| 12 | CI：**HEAD 未终判**；其余红全为票外 | HEAD `cb5b4c5` run `34752146596` **in_progress**（待确认）；其前各次红 = `publish-gates`（`FAIL: dsh-calorie 契约键 calorie.help.center`）＋ `build-test` 24 fail（#237／#91／#87／#48／#96／#75／#80／#83／#133／#188／#193／#201 与 `dsh-*-client` 一批）——**同一批失败名在开工前的非 313 提交 `34748540493` 上逐名复现**，且失败名里**没有任何 #313 口径的测试**（`logs/90-cifail-old.txt`、`91-run9.log`） |
| 13 | 行数属实；"517 是既有超线"**属实** | `routing.ts` 267／`gen-routes.mjs` 241／`gen-cli.mjs` 517／`routes.generated.ts` 518；`git show ba1d4dc:…gen-cli.mjs` ＝ **408 行**（>350 ⇒ 本票前已超线）；棘轮上限 `FROZEN_LEGACY_MAX=92`／`LINES=119` 数字**未动**（只是把定义挪进 `legacy-frozen-295.mjs`），`cmd-registry-294.test.mjs` **0 行改动** |
| 14 | 对账：当事人逐席可复现；本席 5 条 exit 0 | b4 席命令逐字复跑：严格 `matched=11/12 scoped=12 undeclared=1`（唯一未认领＝`ffa7a308`），加 `--allow-nonzero` ⇒ `matched=12/12 undeclared=0 PASS`（`logs/80/81`）；本席运行见下节 |
| 补二③ | 两条口径**属实且无假绿** | 印记 `dist/.gen-inputs.json` 实为 **23 件**、其中路由相关 **12 件**（含 `cli/legacy/routes/index.ts`）；生成器对它 `continue` 跳过是**必要**的——摘掉跳过即抛「同一个 (list, order) 被声明两次：wake@0 ← routes/index.ts ／ scene-01.ts」（`logs/91-run9.log:15`、`logs/32-mut.log`） |

## 二 门禁实测（本席 runId）

`374d50c1` build(0)｜`ae9ded6e` test(0，`tests 1492／pass 1492／fail 0`，48.3 s)｜`2c815fdb` 变异与自设探针(0)｜`08d1e647` P-B(0)｜`505fd83c` t293 五探针(0)。
以上 5 条即窗口内 `ticket=313r` 的全部 `RUN` 条目（`.scratch/locks/gate-runs.log`），逐条声明于本文；`check-gate-audit` 结果见 §四末。

## 三 变异与新探针（打红了什么、意味着什么）

| 变异／探针 | 结果 | 红／绿意味着什么 |
| --- | --- | --- |
| M1 改声明源码不 build（P-A） | **红**：`GEN-STALE FAIL` | 红＝内容印记真盖住 `legacy/routes/*.ts` 与 `weight/routes.ts`——"改声明没重建"不会被假绿放行；绿则意味着该守卫是空壳 |
| M2 手改生成物 | **红**：`GEN-CHECK FAIL（第 519 行起）` | 红＝生成物与生成器输出逐字节绑定 |
| M3 dist 注入跨件同名 `wakeWord`（order 取末位、保持 0..n 连续、不撞 `(list,order)`） | **红**：生成期抛，点名两件 | 红＝"一条记录只许住一件声明"真被执行；合法重复（同件内 `记身材照`×3）不被误杀 |
| **新探针 A·顺序权威是 `order` 而非文件位置** | 搬件不动序 ⇒ 生成物**逐字节不变**（绿）；再把该条 `order` 与邻条对调（集合不变）⇒ 生成物**变**（红对照） | 绿＝生成器不含文件位置知识、换文件搬记录不打乱顺序；红对照＝比较器不空转（否则"逐字节不变"是恒真） |
| **新探针 B·概念唯一＋汇总件跳过必要性** | `wakeWord:'` 字面量 `routing.ts`＝**0**／生成物＝**495**；摘掉对 `index.ts` 的跳过 ⇒ 抛 `(list, order)` 被声明两次 | 绿＝记录面确已唯一落在生成物；红对照＝筛子能认出记录（非空转），且"跳过汇总件"不是可有可无 |
| M-t293：把 `routes.generated.ts` 从 `gen-cli` 的 `targets` 摘掉（探针副本输入被替） | **红**：`P1=FAIL`（P2..P5 仍 PASS）＋`UNACCOUNTED=1`＋判据行「生成物不在 gen-cli 的 `targets` 数组里」逐字出现 | 红＝t293 的"路由归派生侧"是机械判据钉的，不是硬写一句话 |

## 四 补二三处争议点独立判

1. **规格偏离**：① 偏离**属实**（票面 Destination②/要做的事⑤写"生成器产出 `triggers/routing.ts`"，实到 `routes.generated.ts`＋`routing.ts` 只再导出）。② 判决性意图**仍成立**：改/加唤醒词只动声明件（`src/<能力>/routes.ts`、`legacy/routes/scene-NN.ts`）＋`pnpm gen`，`routing.ts` 零接触（t293 ③ 行、M4a 探针）；"生成物≠生成器输出即红"由 `gen:check` 逐件认领 `routes.generated.ts` 保证（M2 实测红）。③ 复核**成立**：`routing.ts` 267 < 350 回到线内；`routes.generated.ts` 属 `docs/agents/structure.md:9` 明列"不管"的**生成的资产**，且真被 `gen:check` 逐件守——豁免不是口头豁免。**唯一保留**：`routing.ts` 自身**不在** `gen:check` 的 targets（见缺陷 1），它的漂移只由 `test/calorie-routing-81.test.mjs` 与 t293 判据 e 兜。
2. **对账放宽**：留痕**逐字存在**（`t313b4-归类与票面-证据.md:164`，理由只针对那一条；`:166-167` 明写未用另外三个开关）；被放宽的**确实是**那一次被打断的运行——严格复跑唯一未认领＝`ffa7a308… exit=3221225786`（`0xC000013A`），且窗口内该票 12 条**只有它** exit≠0 ⇒ 放宽不可能顺带放行别的条目；门禁测试证据取的是重跑干净的 `738005b7`（exit 0／1492/1492），**不是**用被打断那遍顶替。（该文件后段 R-8 轮另有一次 `--allow-undeclared`，同样带 `GATE-RELAX` 留痕，口径一致。）
3. **两处口径校准**：均**属实**（见 §一 补二③）。

## 五 缺陷清单

| # | 级别 | 归属 | 缺陷 |
| --- | --- | --- | --- |
| 1 | S3 | **本票引入** | 纪律文本把 `routing.ts` 说成"记录面已是生成物：`pnpm gen` 重生成、`pnpm gen:check` 守"（`命令登记纪律.md:118`／`照抄说明.md:50,77`）。实测 `pnpm gen` 产的是 `routes.generated.ts`，`gen:check` 的 targets **5 件不含 `routing.ts`** ⇒ 手改 `routing.ts` 本门**不管**。建议改为"记录面住生成物 `routes.generated.ts`；`routing.ts` 只剩类型再导出与逻辑" |
| 2 | S3 | 本票范围（自觉取舍） | 生成物与生成器文本仍指**已删**的 `src/cli/legacyCommands.ts`：`src/cli/keys.ts:5`（生成 banner）、`scripts/gen-cli.mjs:198/318/506`、`scripts/build-help.mjs:117`。成因是 A 段"四件生成物逐字节不变"这条硬约束冻结了 banner ⇒ 属自觉取舍，但读者按 banner 行动会撞空文件；建议 #320 收口时随重生成一并改 |
| 3 | 提示 | 本票引入 | 生成器跨件重复守卫对**同件内**重复放行（冻结 SoT 需要）。副作用：将来把某重复词组（如 `记身材照`×3）拆到两件会**假红**；应在"一条记录只许住一件声明"旁补一句"重复词组必须整组同迁" |
| 4 | S3 | **范围外** | CI 既有红（`publish-gates` 契约键＋`build-test` 一批 #237/#91/#87/#48/#96… ）在开工前的非 313 提交上逐名复现 ⇒ 不记本票账；HEAD run 仍在跑（待确认） |
| 5 | S3 | **范围外** | 卡路里包**无包内告警线**（`packages/skill-calorie/AGENTS.md` 不存在，350 只见 docs），既有债（t179/t180 已登记） |

## 六 五维打分 ＋ verdict

| 维度 | 满分 | 得分 | 依据 |
| --- | --- | --- | --- |
| 契约一致 | 30 | **28** | 完成判据 ①–⑥ 全部满足；落位偏离经裁可接受且判决性意图成立；扣 2＝缺陷 1 的措辞与事实偏差 |
| 证据真实可复现 | 25 | **24** | 14 项必跑全部独立复跑（含 DIST-SHA 自算、变异两连、P-A/P-B、t293、逐席对账）；扣 1＝收口席的变异是**原地改再还原**（逐字节还原＋git 干净已自证），与本席"只在副本上做"的纪律不同口径 |
| parity | 20 | **20** | 16 面 `DEEP-EQUAL=yes`、`surfaceMissing=0`／`surfaceExtra=0`；冻结表＋81 测试＋两分派文件**零 diff**；导出面 15 名一个不少；行为实测一致 |
| 工程红线 | 15 | **14** | 零交付文件改动、持锁跑门、对账源受跟踪、变异全在副本；扣 1＝缺陷 2 的假指引仍在树里 |
| 文档同步 | 10 | **7** | 两份纪律文本已按改后事实改写（含路径与"零接触"口径）；扣 3＝生成物 banner／生成器医嘱／错误提示仍指已删文件 |
| **均分** | | **93** | |

**verdict：PASS**（无 S1 交付缺陷、无 S1 过程违规；均分 93 ≥ 85）。缺陷 1–3 建议由 #320 收口轮一并处置，本席**未修任何东西**。

## 七 未确证／风险 top3

1. **CI 终判缺失**：HEAD run `34752146596` 仍 `in_progress`——本席只能证"此前的红全是票外、且失败名零 313 口径"，不能证 HEAD 绿。
2. **对账窗口边界**：b4 席 `--until=10:30:00Z` 把收尾提交／push 排除在窗口外（其证据已写明口径）；第三方若后移窗口须按同口径声明那两条，否则会误红。
3. **新探针 A 的强度**：搬件是在**编译后 dist**（生成器真实输入）上做的，未做".ts 源搬件＋真 `tsc` 重建"的端到端版；要更强的门需在副本里跑一次 `pnpm build`（本席未做，成本高）。

**本席运行声明（`check-gate-audit` 的证据行；`cmd=` 之后到行尾即命令原文，故 exit 与时刻另记一行）**

- GATE-RUN runId=374d50c1-57d9-440a-b484-aada45dba6f1 cmd="pnpm build"
- GATE-RUN runId=ae9ded6e-e157-4be8-b663-38a2dea616e5 cmd="pnpm test"
- GATE-RUN runId=2c815fdb-2bb3-4bce-9b6c-413f18fa2a08 cmd="node .scratch/t313r/mut.mjs"
- GATE-RUN runId=08d1e647-c0c6-49bf-9121-3f1ef8537a11 cmd="node docs/skills/skill-calorie/t313-pb-分区权威-对照.mjs"
- GATE-RUN runId=505fd83c-3dd2-4990-bf1c-1925f217c411 cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"

逐条 **exit=0**，时刻依次 2026-09-13T10:34:51.289Z／10:35:40.600Z／10:38:37.788Z／10:38:38.190Z／10:40:10.451Z（源：`.scratch/locks/gate-runs.log`）。

对账命令（逐字可复跑）：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t313r-复核报告.md --ticket 313r --since 2026-09-13T10:34:00Z --until 2026-09-13T10:45:00Z --export docs/skills/skill-calorie/t313-gate-runs-313r.log`

实测：`RESULT: matched=5/5 auditEntries=1531 scoped=5 undeclared=0` ＋ `gate-audit: PASS`（exit 0，10:45:13Z 导出）；收尾提交（本报告与对账源入库）发生在 `--until` 之后，按协议 §2.4「窗口外条目不受影响」不进本窗口——第三方要审它，把 `--until` 后移并按同口径声明那两条即可。
