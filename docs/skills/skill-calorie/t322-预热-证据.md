# #322 预热 · 证据（清四处共享位：六票的落点不必再改测试）

- 票：#322（map #293）；票面要求见 issue；基线 sha `4b9e4cf`（`master`，开工时 `git status --porcelain` 为空、`.scratch/locks/gate.lock` 不存在）。
- 开工时刻：2026-09-13T19:25:01+08:00（＝第一道持锁运行的 `at`）。
- 结论：四处**改完**；判据①～⑥逐条见下；判据③的实证在 **§4**。
- **收口轮（复核后）**：`§8` 处置复核席 D1（P5 阈值收紧 ＋ 变异自证）；`§9` 六票共享位**终态核查**与两处补修
  （#314 报回的一处漏网共享位 ＋ 探针撞键候选在清单搬空后的退化）。收口轮的持锁运行逐条列在 `§9.4`（窗口外，未参与 §6 认领）。
- 只动了：四处共享位 ＋ 本证据文件 ＋ `.scratch/t322/**`。`src/triggers/scene-*.ts`、`test/calorie-routing-81.test.mjs`、
  五件生成物（`keys.ts`／`registry.ts`／`combos.yaml`／`build-help.mjs`／`routes.generated.ts`）**零改动**。

---

## §1 四处「改前／改后」

### ① `packages/skill-calorie/test/cmd-registry-294.test.mjs`

| | 内容 |
| --- | --- |
| 改前 `:191-196` | `assert.deepEqual([...REGISTRY_KEYS].sort(), WEIGHT_COMMANDS.map((c) => c.key).sort(), '注册表当前应恰为体重 9 条（别的场景随各自的票搬）')`<br>`assert.equal(WEIGHT_COMMANDS.length, DECLARED_CAPABILITY_KEYS.length, …)`<br>（**措辞更正（复核席 D4）**：第二条相对 P3 基线 `1396d67` 原为 `, 9,`——把它改成 `DECLARED_CAPABILITY_KEYS.length` 是**更早的票**做的事；这里写的是本票**开工时**的现文，与 `t293:49` 的基线 diff 对不上属正常） |
| 改后 `:193-217` | 四条断言（＋口径注释）：<br>① `:198` 体重声明无重复键<br>② `:200-202` **注册表 ⊇ 体重声明的键集**（逐键报出缺哪个）<br>③ `:204-213` **定义地**：注册表每个键都能在某个能力目录的 `commands.ts` 里找到（`readdirSync` 扫 `src/*/commands.ts` ＋ `key: '…'` 字面量；读源码，不留恒真式）<br>④ `:216-217` 体重声明条数 **≤** 各能力目录声明的键数 |
| 为什么不再红 | 原①②钉的是「注册表**恰为**体重 9 条」——第一条命令搬进新能力目录即红，六票都要来改同一处；改成 ⊇／≤ 后，搬家只让集合变大而断言恒真 |

### ② `packages/skill-calorie/test/legacy-partition-313.test.mjs`

| 位置 | 改前 | 改后 |
| --- | --- | --- |
| `:64` → `:68` | `assert.equal(keys.length, FROZEN_LEGACY_MAX, '冻结键集条数与冻结上限不等…')` | `assert.ok(keys.length <= FROZEN_LEGACY_MAX, '冻结键集条数超过了冻结上限…')`（对齐 `legacy-ratchet-295.test.mjs:69-70` 的 `<=` 口径） |
| `:98-99` → `:105-107` | `assert.deepEqual([...now].sort(), [...expected].sort(), '分区后的键集与分区前不一致…')` | `assert.deepEqual(intruders, [], '未搬迁清单出现了冻结集之外的键（这里只进不出）…')`＝**现行键集 ⊆ 冻结集** |
| （新增，补偿） `:109-115` | — | **零丢失**：`清单 ∪ 已搬迁(REGISTRY) ⊇ 冻结集`——搬迁一条时它从清单与冻结集各去掉、同时进 REGISTRY，两种中间态都绿；被静默删掉一条即红（原等号提供的保护不减） |
| `:107` → `:122` | `assert.equal(LEGACY_COMMANDS.length, FROZEN_LEGACY_MAX, '分区后声明条数与冻结上限不等')` | `assert.ok(LEGACY_COMMANDS.length <= FROZEN_LEGACY_MAX, '分区后声明条数超过了冻结上限…')` |
| 文件头 `:11-16` | 「② 键集与分区前一致…将来搬走一条命令时：改 `legacy-frozen-295.mjs`（键 ＋ 上限），再删它那一片里的那一行。」 | 照事实改写：②＝「清单 ⊆ 冻结集 ＋ 零丢失」，并写明搬迁票**不必**再动本件 |

### ③ `packages/skill-calorie/test/legacy-frozen-295.mjs`（只动排版）

| | 内容 |
| --- | --- |
| 改前 `:18-42` | `Set` 字面量 **4 键一行**（24 行；不同场景的键混排在同一物理行） |
| 改后 `:21-113` | **1 键一行**（92 行），文件头加一行 `#322 预热：…**只动排版**：键集内容、顺序与两个上限数字一字未动` |
| 数字 | `FROZEN_LEGACY_MAX = 92`（`:117`）／`FROZEN_LEGACY_LINES = 119`（`:123`）**一字未动** |
| 机器自证 | `.scratch/t322/verify-frozen.mjs`（对 `HEAD` 逐字比）：`before: keys=92 max=92 lines=119`／`after : keys=92 max=92 lines=119`／`VERIFY-OK 键集与两个上限数字零改动（只动排版）` |

### ④ `docs/skills/skill-calorie/t293-验收-命令自治.mjs`（P5 的 PARITY 正则）

| | 内容 |
| --- | --- |
| 改前 `:791` | `/WEIGHT_COMMANDS\.length,\s*9/`——**0 命中**（现文已改成 `WEIGHT_COMMANDS.length, DECLARED_CAPABILITY_KEYS.length`）＝假绿 |
| 改后 `:791` | `/weightKeys\.filter\(\(k\) => !registryKeys\.has\(k\)\)/`——指向改写后的「注册表 ⊇ 体重键集」断言行（`cmd-registry-294.test.mjs:200`），`why` 一栏同步改写 |
| 实测 | 探针 P5：该条 ✓、命中行 `200`，合计 `7/7`（主守 `sport-homogeneity-109` 存在=true） |

### ⑤ 纪律文本（票面第 5 条：「若有相应描述就照事实改」）——读后判断**无需改**

- `docs/agents/命令登记纪律.md`：`:89／92／138` 说的是「随搬迁数**下调**棘轮上限」（本票明确不许放宽数字，该纪律照旧）；
  `:100-103` 的两张棘轮表说的是「搬走一条命令 → 下调 case 冻结清单／行数上限」，与本票改的三处 legacy 等号**不是同一件事**；
  `:153-157` 明写「不抄行数基线、条数上限」，与改动无冲突。
- `docs/agents/命令登记纪律-照抄说明.md:78` 已经写着「**搬迁**一条既有命令 → `test/` 下 **0 件**必改」——
  本票正是把这句从「声称」变成「事实」，无需改字。
- 判据：这两份文本里**没有**对「四处断言用等号还是相对量」的描述（这是实现细节，住测试件里）。

---

## §2 assert 条数前后实测（证明没靠删断言过关）

口径＝`t293-验收-命令自治.mjs:681` 的 `/assert\./g` 计数（P3 用的就是它）。

| 对象 | 值 |
| --- | --- |
| 基线 `1396d67:packages/skill-calorie/test/cmd-registry-294.test.mjs` | **25** |
| 本树同一件 | **27**（净 **+2**，无净删） |
| 被改写那一段内 | 原 **2** 条 → 新 **4** 条（①重复键 ②⊇ ③定义地 ④≤） |
| 探针判据 | `P3: PASS`；`棘轮 … diff 逐字比较 非空；--stat=… | 36 ++++++++++++++++++----`；`assert 次数 25 → 27；test 块 7 → 7`；**`棘轮未被放宽=true`** |

（`legacy-partition-313.test.mjs` 不是 P3 的棘轮文件；那里也是只增不减：`:98-99` 一条等号 → ⊆ 一条 ＋ 零丢失一条。）

---

## §3 上限数字零放宽（判据④）

- `FROZEN_LEGACY_MAX = 92`、`FROZEN_LEGACY_LINES = 119`：值未动（`§1③` 的机器自证）。
- 冻结键集 92 条：内容与顺序未动（同上自证）；`legacy-ratchet-295.test.mjs:69-70` 的
  `FROZEN_LEGACY_KEYS.size <= FROZEN_LEGACY_MAX` 仍成立。
- 主树 `pnpm gen:check`：`GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 1 个（weight）声明 9 条，**未搬迁清单 92 条**`。

---

## §4 判据③ 实证：在**副本**上模拟一次纯搬迁

副本根：**`D:\t322sim2`**（仓外；`robocopy` 排除 `node_modules`／`.git`／`.scratch`／`.dsh-*`，
`node_modules` 用 junction 接回主树 —— 只读主树、绝不安装）。脚本：`.scratch/t322/sim2-all.ps1` ＋ `sim-migrate.mjs` ＋ `sim3.ps1`。

### 4.1 搬的是什么（一次纯搬迁）

| | 动作 |
| --- | --- |
| 键 | `calorie.view.goal-weight`（今天住 `src/cli/legacy/scene-03.ts`，场景 03 唯一一条） |
| 声明 | 删 `scene-03.ts` 那**一行** → `src/tmpcap/commands.ts`（`kind/key/shape/title/wakeWord/example` **逐字照抄**，只补 `run`） |
| 处理函数 | 从 `src/cli/cmd_read.ts` 的 `case` 原样取出 → `src/tmpcap/goalWeight.ts`（语义不动，新增） |
| 路由记录 | 删 `src/cli/legacy/routes/scene-03.ts` 的 `order: 120` 那条 → `src/tmpcap/routes.ts`（`list/order/wakeWord/scene/kind/key/cli` 逐字照抄） |
| 老住处 | 再删 `cmd_read.ts` 的 `case` 与其 import 名（搬迁票自己那一份） |
| 新增 | `src/tmpcap/index.ts`（转出声明） |

### 4.2 四处共享位：副本里**一行未改**，且与主树逐字节相同

| 文件（sha256 前 16） | 副本 开跑前 | 副本 跑完后 | 主树 |
| --- | --- | --- | --- |
| `test/cmd-registry-294.test.mjs` | `E763ADB34979F349` | `E763ADB34979F349` | `E763ADB34979F349` |
| `test/legacy-partition-313.test.mjs` | `8E5F2C17F7A8EF92` | `8E5F2C17F7A8EF92` | `8E5F2C17F7A8EF92` |
| `test/legacy-frozen-295.mjs` | `C6DA06A414E395E0` | `C6DA06A414E395E0` | `C6DA06A414E395E0` |
| `docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `35249E07BB566FF4` | `35249E07BB566FF4` | `35249E07BB566FF4` |

### 4.3 副本门禁（`sim2-all.ps1`）

- `pnpm build` → `gen` → `pnpm build` → `gen:check` → `test`：`SIM2-EXITS build1=0 gen=0 build2=0 gencheck=0 test=1`
  （`test=1` 的三条红＝**环境**：`test/client-bundle-48.test.mjs` 的 `dsh-bill-ilife client` 三连，
  报 `Cannot use import statement outside a module` —— 副本没有 `*.tsbuildinfo` ⇒ 副本里的 `tsc -b` 全量重编，
  把各插件包用 `tsdown` 预打包好的 `dist/client.js` 覆盖成了裸 ESM。见 4.5 的修复与复跑。）
- `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；**能力 2 个（tmpcap／weight）声明 10 条，未搬迁清单 91 条**` ✓
- **免费判据**（协议 §3 ②-5「生成物 diff 只应出现 `registry.ts`」）——副本里逐字节对过：

| 生成物 | 副本 gen 前 | 副本 gen 后 | 主树 | 结论 |
| --- | --- | --- | --- | --- |
| `src/cli/keys.ts` | `4DB43569FBAB400D` | `4DB43569FBAB400D` | `4DB43569FBAB400D` | 逐字节不变 |
| `base-combos/combos.yaml` | `CA17D2C3F62CE752` | `CA17D2C3F62CE752` | `CA17D2C3F62CE752` | 逐字节不变 |
| `scripts/build-help.mjs` | `4D44F03F6FFB25D7` | `4D44F03F6FFB25D7` | `4D44F03F6FFB25D7` | 逐字节不变 |
| `src/triggers/routes.generated.ts` | `E9EA993518EC33E3` | `E9EA993518EC33E3` | `E9EA993518EC33E3` | 逐字节不变 |
| `src/cli/registry.ts` | `…` | `D18F1173AF74C915` | `9E11F0447991DD0D`（1 能力） | **变**（＋tmpcap 一行）＝预期 |

### 4.4 副本全量测试

- `sim3.ps1`（先修 4.5 的环境）：`ℹ tests 1492`／`ℹ pass 1488`／`ℹ fail 0`／`ℹ skipped 4`／`SIM3-FULL-EXIT=0` ✓
- 那 4 条 skip 的原因已查明且与本次改动无关：`packages/skill-schedule/test/help-assets.test.mjs` 的
  `SKIP_NO_SOURCE`＝「事实源不在盘上（`.scratch` 不入库）」，而副本按设计排除了 `.scratch`。
  补上那一份事实源（`.scratch/t198/`）后在副本里真跑：`sim4.ps1` → `ℹ tests 7／pass 7／fail 0／skipped 0`，`SIM4-EXIT=0` ✓
  （即副本全量等价于 1492 跑完、0 红）。

### 4.5 副本环境两处修正（如实记账，都不是四处共享位的事）

1. 第一次（`sim-run.ps1`，`runId=efbc583d…`）：副本里 `pnpm` 先跑「依赖状态检查」并要求 `install`（无 TTY 直接失败），
   且当时漏了 `run` 字段、漏了「gen 之后再 build」⇒ 那次 34 红是**副本环境**。修正：`--config.verify-deps-before-run=false`（不安装）＋ 声明补 `run` ＋ 顺序改成 `build → gen → build → gen:check → test`。
2. `tsdown` 预打包的 `dist/client.js` 被副本的 `tsc -b` 覆盖（主树因 tsbuildinfo 命中而不重编）⇒ `sim3.ps1` 先从主树恢复这些**预打包产物**（`SIM3-RESTORED-CLIENT-BUNDLES=36`）再跑全量。

### 4.6 负对照（证明不是假绿）

同副本、同一棵搬迁过的树，只把三件**换回 `HEAD`（改动前）**版本：

```
✖ #294 对账：注册表每条声明与 cli/keys.ts 的登记逐条一致
✖ #313 纯搬迁：分区后的键集与分区前的冻结键集逐字一致
ℹ tests 16／pass 14／fail 2      NEG-EXIT=1
```

换回本票版本：`ℹ tests 16／pass 16／fail 0`，`POS-EXIT=0` ✓
⇒ 「四处不必改即绿」是**改动带来的**，不是副本本身不校验。

### 4.7 给六票的一条实测提示

副本里实测：`pnpm gen` 写的是 **src** 的 `src/cli/registry.ts`；只跑 `build`（在 gen 之前）会让 `dist/cli/registry.js`
落后于声明，`pnpm test` 会因为「dist 里的 REGISTRY 还没有搬迁后的键」而红一大片。
落点窗口里请照 `build → gen → **build** → gen:check → test` 的顺序走（协议 §3 ② 的第 3–7 步建议补这一步）。

---

## §5 主树门禁（判据①）

命令一律经 `node tooling/run-locked.mjs --ticket 322 -- <命令>`（`.scratch/locks/gate-runs.log` 留痕）。

| 门 | runId | 结果 |
| --- | --- | --- |
| `pnpm build` | `2373311d-259a-47c0-873c-2c2a5e0b2af0` | exit=0（`GEN-STAMP ok … 声明源 25 件`） |
| `pnpm gen:check` | `7a57bb30-edae-439c-a151-c65228430ea0` | exit=0（`GEN-CHECK PASS：键 101；能力 1 个（weight）声明 9 条，未搬迁清单 92 条`） |
| 全量 `pnpm test` | `c19c5c50-9950-4b28-951b-24ccda97854b` | exit=0（`ℹ tests 1492／pass 1492／fail 0`） |
| `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `80b2f3e7-28ea-44e9-b765-86d0d9e574da` | exit=0（`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`；P1 的 `UNACCOUNTED=0 处`） |

关键判据摘录（探针原文）：
- `P3: PASS` … `棘轮 packages/skill-calorie/test/cmd-registry-294.test.mjs vs 1396d67：diff 逐字比较 非空；assert 次数 25 → 27；test 块 7 → 7`；`棘轮未被放宽=true`；`门对"手改生成物"真报红=true`
- `P5: PASS` … `✓ packages/skill-calorie/test/cmd-registry-294.test.mjs（…）命中行 200`；`守 parity 的既有断言命中 7/7`
- `P4: PASS` … 两文件 `registry 先行=true`；`体重 5 读 ＋ 4 写 ＝ 9 键，在分派两个文件里的 case 分支数合计 = 0`

---

## §6 GATE-RUN 对账（本票在窗口内的每一次持锁运行，逐条声明）

```
GATE-RUN runId=2ab52b9c-1071-4fc6-8abd-75f8cc32b13b cmd="pwsh -NoProfile -Command \"git show 1396d67:packages/skill-calorie/test/cmd-registry-294.test.mjs > .scratch/t322/base-cmd-registry-294.test.mjs\""
GATE-RUN runId=2373311d-259a-47c0-873c-2c2a5e0b2af0 cmd="pnpm build"
GATE-RUN runId=7a57bb30-edae-439c-a151-c65228430ea0 cmd="pnpm gen:check"
GATE-RUN runId=c19c5c50-9950-4b28-951b-24ccda97854b cmd="pnpm test"
GATE-RUN runId=80b2f3e7-28ea-44e9-b765-86d0d9e574da cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
GATE-RUN runId=36db01f3-35c9-4bd2-86ef-c38e4ee02ee3 cmd="pwsh -NoProfile -File .scratch/t322/sim-run.ps1"
GATE-RUN runId=efbc583d-7fe9-4677-b046-7e6892876e00 cmd="pwsh -NoProfile -File .scratch/t322/sim-run.ps1"
GATE-RUN runId=7f3def02-df6a-4195-abe2-e78655175ced cmd="pwsh -NoProfile -File .scratch/t322/sim2-all.ps1"
GATE-RUN runId=e5637787-d799-4e78-a004-f866049f67d1 cmd="pwsh -NoProfile -File .scratch/t322/sim3.ps1"
GATE-RUN runId=44df26db-f6a7-4b13-a89b-ac025acedbcb cmd="pwsh -NoProfile -File .scratch/t322/sim4.ps1"
```

说明（不许读成「都成功」）：
- 前五条是主树四道门 ＋ 取基线件；后五条是副本实证（§4）。**十条的 `exit` 都是 0**（包装器记的是脚本退出码）。
- 副本的**内容**红已如实记在 §4.3／4.5（第一次 `sim-run.ps1` 里 `pnpm build`／`test` 因副本环境失败、第二次 3 条 client 包红、
  `sim3` 修正后 0 红）；本票**没有**使用任何 `--allow-*` 放宽，也没有 `GATE-RELAX` 条目。
- 对账窗口（共享日志下必须界定）：`--since 2026-09-13T19:25:01+08:00 --until 2026-09-13T19:59:00+08:00`
  ＝本票从开工到副本实证收尾（最后一条窗口内运行 `44df26db` 在 19:58:07+08:00）。**`--until` 是窗口界定，不是放宽开关**
  （放宽开关只有 `--allow-*` 四个，本票一个没用）。
- 窗口**之外**（本文件自身的提交与推送，属工具文档所说的「提交后的 git 条目」，不参与认领）：
  `ticket=322` `runId=ba9b298d-4a96-485a-ab07-74031f31a220` `git status/git add/git commit -F .scratch/t322/msg.txt`（19:59:08+08:00，exit=0，产出 commit `47814a7`）；
  `ticket=322` `runId=2675b21d-d2aa-4c77-9c9b-824b21857124` `git push`（19:59:17+08:00，exit=0，`4b9e4cf..47814a7 master -> master`）。
  本证据文件自身随这两条之后的那次提交入库（见 §7「偏离」）。
- 导出面：`docs/skills/skill-calorie/t322-gate-runs.log`
  （`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t322-预热-证据.md --ticket 322 --since 2026-09-13T19:25:01+08:00 --until 2026-09-13T19:59:00+08:00 --export docs/skills/skill-calorie/t322-gate-runs.log`）。

---

## §7 偏离／未确证／风险

**偏离**
1. 比票面多写了**两条断言**（`cmd-registry-294` 的「定义地」；`legacy-partition-313` 的「零丢失」）。
   理由：把等号改成 ⊆／≤ 若只删不补，判据就是**净放宽**；这两条正是原等号所保护的性质（键不丢、每个键有定义地），
   且都是「与搬家进度无关」的写法。票面硬约束「不许净删断言」按**只增不减**处理（25 → 27）。
2. 票面第 5 条（纪律文本）**读后判为无需改**，理由见 §1⑤——避免为改而改。
3. 副本实证跑在 `D:\t322sim2`（仓外）＋ `.scratch/t322/**` 脚本；副本与临时脚本在收尾时清理。

**未确证（请复核席独立确认）**
- 「四处不必改即绿」是**副本**上的实测（`pnpm test` 在副本里跑）；主树里我只跑了本票自己的四道门，
  没有在主树里做搬迁（当然不许）。
- 副本排除了 `.scratch` ⇒ 4 条 `help-assets` 用例默认 skip；补事实源后单独跑绿（§4.4），
  但**没有**在补齐后重跑一次 1492 的全量（只补跑了那 7 条文件级用例）。
- 负对照只换了三件测试/判据（④ 是探针文件、不在 `pnpm test` 的 glob 里，故以「文本恒等 ＋ P5 命中」为证）。

**风险（给后面的票）**
1. `pnpm gen` 之后必须再 `pnpm build`（§4.7）——否则 `dist/cli/registry.js` 落后，测试红一片而原因不在共享位。
2. 三处 legacy 断言改成相对量后，「搬完一条要把冻结键集与上限一起下调」只剩**纪律**在管（`legacy-ratchet-295.test.mjs:69`／`:74`／`:80` 仍是 `<=`）：
   不下调不再报红——上限数字本票**零放宽**，但后续票若整段搬空，建议仍按纪律下调并把动作写进评审。
3. `cmd-registry-294.test.mjs` 的「定义地」断言用 `key: '…'` 正则扫各能力目录源码；声明写法若换形态（不是 `key: '…'` 字面量）会误报，
   届时改这一条断言即可（它自己也写着这是「与搬家进度无关」的口径）。

---

## §8 收口轮 · 复核席 D1 的处置（改，附同一个变异自证）

复核席（`t322r-复核报告.md` §③ D1）实测：把 P5 正则指向的那条断言删掉，P5 **仍然 PASS**
（`t293-验收:818` 的阈值是「7 条里命中 ≥ 5」）⇒ P5 不能当「四条新断言都在」的证明。**判定：小且安全，改。**

| | 改前 | 改后 |
| --- | --- | --- |
| `t293-验收-命令自治.mjs:818` | `const ok = primary && found >= 5;` | `const ok = primary && found >= PARITY.length;`（**7 条全命中**） |
| `:816` 判据文字 | 「合计命中 ≥ 5 处」 | 「7 条**全部命中**」＋写明本次收紧的由来（复核席 D1 实测：删掉其中一条仍 PASS） |
| 未动 | — | `primary`（`sport-homogeneity-109` 必须在）与「本探针不自己重算一份 parity」两句原文保留；`PARITY` 四条目标判据的清单与正则不变 |

**同一个变异自证**（`.scratch/t322/d1-mutation.ps1`；主树临时删 3 行后 `finally` 按备份还原并核对 sha）：

```
D1-SHA-BEFORE=E763ADB34979F349 ／ D1-TARGET-OCCURRENCES=1
变异态：P5: FAIL ／ 守 parity 的既有断言命中 6/7 ／ RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=FAIL 4/5
        ／ D1-PROBE-EXIT-MUTATED=1
还原：  D1-SHA-RESTORED=E763ADB34979F349 ／ D1-RESTORE-OK=True
还原态：P1..P5 = PASS 5/5 ／ D1-PROBE-EXIT-RESTORED=0
```

⇒ 收紧后：删掉那条断言 P5 **真的会红**；还原后四处与主树 sha 一致、探针 5/5、`命中 7/7`（§9.3 的主树复跑）。

---

## §9 收口轮 · 六票共享位「终态核查」＋ 两处补修

复核之后 #314 报回**一处漏网共享位**（属本票射程：让六票落点不必碰共享位），核查时又发现同类的第二处。
两处都改成「与清单内容无关」，**断言只增不减**。

### 9.1 补修 A：`legacy-partition-313.test.mjs` 的守卫用例假样本（#314 报回）

| | 内容 |
| --- | --- |
| 病 | 第一个用例「跨场景同键即抛」的变异样本写死 `SCENES['01'][0]`（`:83`／`:92`）。场景 01 整片搬空（**＝#314 的完成判据**）后它是 `undefined` ⇒ 守卫读 `undefined.key` 抛 TypeError（消息不含「命令键重复登记」）⇒ 两条 `assert.throws` 变红；#319 清空 scene-10 后（`LEGACY_COMMANDS` 全空）同理 |
| 修 | 换成**合成假声明** `FAKE_DUP_DECL`（`key: 'calorie.fake.dup-guard'`，永不登记进任何清单）：跨"两个场景文件"重复与同文件重复两个变异都用它构造；并**新增**一条断言钉住「假样本键不与真实清单撞」（`:91-92`） |
| 断言数 | 该用例 3 → **4**；整件 `legacy-partition-313.test.mjs` 的 `assert.` 计数 `c593777`=21 → 现行 **23**（两轮合计 +2，无净删） |

### 9.2 补修 B：`t293-验收-命令自治.mjs` 动态段的撞键候选（核查时发现）

| | 内容 |
| --- | --- |
| 病 | 撞键探针取「未搬迁清单里第一条键」（`t293:538-552` 原口径）。清单**搬空后**它一个键都读不到，探针判红「**探针失能**」（`:542-545` 原文）——那是 #313b3 有意设计的「不许退回字面量键造假绿」，但终态下会让 P1 无条件红 |
| 修 | 清单为空时**退回沙箱里已搬迁能力目录的键**（`src/<能力>/commands.ts` 的 `key: '…'` 字面量，优先真实能力目录、排除本步自造的 `fakecap*`）；一个键都读不到才判红。撞键守卫（`gen-cli.mjs` `merge()`）对「老清单 ↔ 能力目录」与「能力目录 ↔ 能力目录」是同一条判断，撞哪一边都证明守卫还在——**没有造假绿的余地** |
| 现状 | 清单非空时行为**逐字不变**（主树复跑走的仍是老路，见 9.3）；探针文件本身不含 `assert.`（自证口径是它自己的 `set(...)`），本改动不动任何既有判据的阈值 |

### 9.3 终态核查：已逐条核过

副本 `D:\t322term`（仓外；带 `.git`／`dist`）里把 **10 片场景全搬空**（src ＋ dist 同步，文件保留、头里仍写 `routing.ts` 依据）后实测：

| 检查 | 结果 |
| --- | --- |
| ① 守卫用例（终态） | `node --test --test-name-pattern='跨场景同键即抛'` → `tests 1／pass 1／fail 0`，exit=0 ✓（**修好了**） |
| ② 变异：去掉生成器守卫（`if (earlier !== undefined && false)`） | 同一条用例 → `tests 1／pass 0／fail 1`，`AssertionError: Missing expected exception: 跨场景重复声明同一个键没有抛`，exit=1 ✓（**牙齿还在**） |
| ③ 还原守卫 | → `pass 1／fail 0`，exit=0 ✓ |
| ④ 撞键探针（终态、把生成物与「10 片全空」对齐后） | `迁移撞键（假能力声明一条已在能力目录里的键（未搬迁清单已空 ⇒ 走能力目录候选 9 条）：calorie.view.weight）→ gen exit=1，报「命令键重复登记」=true` ✓（**不再失能**）；该副本 P1 仍 FAIL，但原因是人造终态丢了 92 条声明（键 101→9），与撞键无关 |
| ⑤ 主树复跑（清单非空，老路） | `迁移撞键（假能力声明一条仍在未搬迁清单里的键（92 条候选）：calorie.view.diet）→ gen exit=1，报「命令键重复登记」=true` ✓；`P1..P5 = PASS 5/5`；P5 `命中 7/7` |

**逐条核过、无需改**（隐含「清单非空／某场景非空」的其它断言的清点）：

- `legacy-partition-313.test.mjs`：`:91-92`（守卫不改变声明集合：空↔空 ✓）、`:117-119`（⊆ 冻结集）、`:128-130`（零丢失）、`:131-140`（字段存在性循环：空表即无操作）、`:140-141`（条数 ≤ 上限）、`:150-152` 起的源文件面（**文件**仍在、每片文件键 ↔ 片内键，空↔空 ✓、头里含 `routing.ts` 是设计要求）、`:180`（禁 `legacyCommands.ts` 文件：存在性，不是内容）。
- `legacy-ratchet-295.test.mjs`：`:63-70`（冻结集自身只许变短）、`:74-81`（条数／声明行数 ≤ 上限，全空即 0 ✓）、`:85-89`（一键一处 ＋ 与对账源相等）、`:94-101`（分片之和 ↔ 汇总）、`:103-120`（文件面：**文件**在、键对得上、`routing.ts` 字样在）。
- `test/declared.mjs`：只有聚合，无断言 ✓。
- `t293-验收-命令自治.mjs`：`readLegacy()` 的「任一片读不到 ⇒ null ⇒ 判红」是**文件缺失**口径（`:54`／`:88-91`），清单为空但文件在时返回 `''` ✓（本票的退回逻辑正是针对这一态）；`:191`／`:656` 两处用 `readLegacy()` 只做文本检索 ✓。
- 另：`test/no-script-commands-180.test.mjs` 指的是 `src/triggers/scene-NN-*.ts`（另一族、只核**文件**在），与本票路径无关，**未动**。

### 9.4 收口轮的持锁运行（**窗口外**，未参与 §6 认领）

| 命令 | runId | 结果 |
| --- | --- | --- |
| 入库复核席两件（`git add`／`commit`／`push`） | `f82ff668-b866-434c-a037-e8b8a6a0b665` | exit=0，产出 `2137e93` |
| D1 变异自证脚本 | `92ad0a3b-9df9-4cd4-b1d0-081a0e95df39` | exit=0（脚本内先红后绿，见 §8） |
| `pnpm gen:check`（D1 后） | `484f77f9-fdc3-4353-b1ca-189e67558d9d` | exit=0，`GEN-CHECK PASS：键 101…未搬迁清单 92 条` |
| 全量 `pnpm test`（D1 后） | `21b5b3f6-4bc6-474c-8ba7-da1f535e06f1` | exit=0，`tests 1492／pass 1492／fail 0／skipped 0` |
| 终态核查（副本建 ＋ 守卫用例 ＋ 变异） | `fd30eba1-8f0c-482e-a81b-b9d16b2ddb6c` | exit=0（副本内容见 §9.3） |
| 终态核查第二段（副本 gen ＋ 探针） | `fe63b12d-40a1-4a6d-83b1-b6a262a062ea` | exit=0（副本内容见 §9.3） |
| **补修 A／B 后主树复跑**：探针 ＋ `gen:check` ＋ 全量 `test` | `2eb56aa8-1b12-4fb0-bbfb-bc389ded08e2` | 探针 `P1..P5=PASS 5/5`（P5 `命中 7/7`）／`GEN-CHECK PASS`／`tests 1492／pass 1492／fail 0／skipped 0` |

（本节运行的命令与 §6 相同形式：`node tooling/run-locked.mjs --ticket 322 -- …`。它们都在 §6 的对账窗口之外，
**未用任何 `--allow-*`**；若要重跑对账，请用 §6 的两端窗口 `--since 2026-09-13T19:25:01+08:00 --until 2026-09-13T19:59:00+08:00`。）
