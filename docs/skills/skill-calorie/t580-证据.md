# t580 证据（场景02饮食·B2复盘今日族文案形状化）

## GATE-RUN

- GATE-RUN runId=6b97b131-c8e9-42d8-a57a-82e3e15c52ca（`audit-separators.mjs --dir .scratch/t580/products --json .scratch/t580/gate.json`）
- RESULT: 1/18，FAIL。基线 0/18 → 改后 1/18（唯一全绿：12-今日有备注空态）。
- 剩余 34 节点逐条归属（全部落在票面不许动范围，见 §5）；`；`/`｜`/`、`/`~` 四项全零，R7 标识符零命中。

## 变异自证两行（机器读数）

- 变异（`reviewDocs.ts:45 MEAL_NOTE` 改坏为“加餐是早餐和夜宵”）：`ℹ tests 30 / ℹ pass 29 / ℹ fail 1`（红的是 `#273 ⑤ 餐别区块对得上 meal_distribution.html` 那一段）。
- 还原（改回“加餐是下午茶和夜宵”）：`ℹ tests 30 / ℹ pass 30 / ℹ fail 0`（与改前一致）。

## 测试尾 5 行

t273（本族锁，断言随文案同改 3 处）：

```
ℹ tests 30
ℹ pass 30
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
```

t279（跨族安全网）：

```
ℹ tests 99
ℹ pass 99
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
```

编译（`node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` 经排队）：exit 0（runId=39ec2e2a-2370-40ff-a970-bb893c791de9；中途他席 `trendPredictDocs.ts` 破坏编译 2 次，本票一律未碰该件，等其修好后重编转绿）。

## 改动逐条（只动票面只许写内；形状只复用现成件）

`packages/skill-calorie/src/diet/reviewDocs.ts`（LF 356→359，台账当场实测已是 359，无陈化）：

- DOC_TITLE `卡路里·饮食`→`卡路里饮食`（计划外补项：`<title>` 在 head 区同样进门禁节点，不改则门禁恒红；t273 不断言它）。
- EYEBROW `卡路里 · 饮食`→`卡路里饮食`；REVIEW_BADGE `复盘 · 餐别`→`复盘餐别`；REVIEW_META_LEFT `饮食复盘 · 饮食`→`饮食复盘饮食`。
- MEAL_NOTE `加餐时段：下午茶、夜宵`→`加餐是下午茶和夜宵`（与 todayDocs 逐字一致）。
- 结论句 `；`→`。`拆两句（`…日均 Z 卡。蛋白合计 …，目标…。`）。
- 空态 `（起 ~ 止）`→`（起 至 止）`；KPI 餐数 detail `~`→`至`；空态餐别句 `~`→`至`。
- `每个点 = …；…` 拆两条 `renderCaliberLine`；按餐汇总口径 `；`＋MEAL_NOTE 拆两条口径行；占比口径同拆两条。
- KPI 日均 detail `按窗口 X 天算 · Y`→`按窗口 X 天算（Y）`。
- 分布值 `X 餐 · Y 卡 · Z%`→`共 Y 卡，占 Z%`（餐数与占比已由 label 条形 pct 承载）。

`packages/skill-calorie/src/diet/todayDocs.ts`（LF 267→270，线内无须挂号）：

- DOC_TITLE 同改 `卡路里饮食`；MEAL_NOTE 同改逐字一致；眉标 4 处（56/160/229/259）同改。
- `今日饮食 · 只看有备注的`（54/157 两处）→`今日饮食（只看有备注的）`。
- 口径行 `MEAL_NOTE；本页缺值…` 拆两条口径行。
- 副题 `这天记了 X 条、共 Y 卡，目标 G 卡（MEAL_NOTE）。`→剩余额度结论一句（`今日还剩 N 卡可摄入。`／超目标时 `今日已超目标 N 卡。`；条数总量目标已在三张 KPI 卡，副题不再复述）。

`packages/skill-calorie/test/t273-review-meal.test.mjs`（本族锁，随文案同改 3 处）：

- :157 `饮食复盘 · 饮食`→`饮食复盘饮食`；:158 `复盘 · 餐别`→`复盘餐别`；:274 `加餐时段：下午茶、夜宵`→`加餐是下午茶和夜宵`。
- :256（V-01 联动）、:118/:199 等脚注断言一字不动。

未动（票面不许动／调查放过）：`review.ts`（V-01 无编排者授权不动）、`nutritionPortDocs.ts`（B3）、来源脚注 4 行、其它测试一字不动。

## 门禁剩余 34 节点归属（遗留出口，归 D 轮／另票）

- B3 `共 X 天 · 均衡` ×8（`nutritionPortDocs.ts:308`）：B3 件不可写。
- B3 `📊 数据来源 · …（按营养素折算）` ×8（`:381`）：同上。
- B3 总览 `📊 数据来源 · 饮食记录 · 本周 …` ×1（`:733`）：总览宿主调块内，归 B3。
- #560 来源脚注 ×16（`reviewDocs.ts:123/183/352`、`todayDocs.ts:152` 经 `sourceLine`）：票面点名归 #560，一律不碰。
- V-01 `最近 7 天共 28 餐；…` ×1（`review.ts:159-160`）：无编排者授权不动，联动 t273:256 一并挂起，需另票或授权。

## 相邻套件实测（跑可以、改不行；本票未动其中任何一件）

- homogeneity 12/12 绿（调查预测的 D 轮 1 未 materialize：该页 MEAL_NOTE 是跨族另半份，本票未动）。
- t496 38/39：唯一红是 #7/#8 营养配比 `goalDetail`（B3 源件与该测试件均有他席在途改动 `M`，与 580 无关）；:106（D 轮 2 预测项）实测仍绿。
- t271 5/6：唯一红是 :186 `餐别分布 2026-09-01 ~ 2026-09-07`（`ee6a81b` H1 去日期后旧断言未同步，今晨起潜伏；两处文件皆不在 580 写集）。
- t272 31/32：唯一红是排行榜三段条（`rankingDocs.ts` 他席在途 `M`）。
- t275 43/45：两处红全是看饮食总览（`nutritionPortDocs.ts` 与该测试件均有他席在途 `M`）。
- t511 10/12：case60 `工作日平均`（饮食域无此串来源，系超前断言，归属他票功能）＋ ④ homolog 变异（B3 在途改动所致）。

18 词产物落 `.scratch/t580/products/`（18/18 exit 0，清单见 `products-manifest.json`），门禁 JSON 落 `.scratch/t580/gate.json`，日志落同目录（`gate-baseline.txt`／`gate-final.txt`／`t273-final.txt`／`t279-after.txt`／`t273-mut.txt` 等）。
