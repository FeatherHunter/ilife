# #538 过程型五页重排（体成分向导两态／围度向导两态）· 证据件

> 席位＝#538 唯一作者（对抗审查与终审由别的席位做，本件**不含自评**）。
> 开工 HEAD＝`7fde8ad`（#536 收口提交）。基线＝`.scratch/m158v2/视觉基线-回收.md`。

## 0. 工具与复现口径

| 项 | 值 |
|---|---|
| 出页 | `node .scratch/m158v2/run-pages.mjs --ticket 538 --out .scratch/m158v2/t538/stage --db .scratch/m158v2/t538/tmpdb --words 记体脂（皮褶钳）,记体脂（外部测量）,记围度,补记体脂,补记围度`（真库零写入：库路径一律 `SKILLS_DB_PATH`） |
| 分隔符探针 | `packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/m158v2/t538/pages`（**本族 5 件**单独放一份，`stage` 里还有 5 张写后回执页，不属本票） |
| 三档溢出 | `measure-responsive.mjs --dir .scratch/m158v2/t538/pages --label t538` |
| 锁 | 同 #536：本波共用锁被 vision 审查进程占着，改用 `--lock-dir .scratch/locks-m158v2`（席位简报 §1 例外条款） |

## 1. 改前地面真值（节点级探针，本席位当场复核）

| 页 | 节点级命中 | 明细（原句） |
|---|---|---|
| `01/02/04-过程-calorie.view.composition-wizard.html` | **5 ＋ 5 ＋ 5** | 眉标 `配置型 wizard · 填表→复制 prompt→AI 写库`（`wizard`／`prompt`／`AI`／`→`／`·`）＋回显 `体脂率 20% · 来源 gym`（英文枚举名）／`体脂率 12.02% · 来源 home_caliper`（R7 蛇形名）＋`7 处总和:— mm · 公式: BD = 1.112 - 0.00043499×Σ + …`（半角冒号＋公式变量）＋`日期（必填）＋来源（必填）＋年龄／性别（皮褶钳换算要用）＋体脂率＋备注（可选）`（`＋` 串 5 段）＋`每项范围 (0, 100) mm · 总和 7 处建议 40–250`（范围字面量）＋复制区 `// 请选来源（本页按默认「家测皮褶钳」打开；可给：家测皮褶钳／医院测／健身房 InBody）`（`；`＋`／`） |
| `03/05-过程-calorie.view.measure-wizard.html` | **3 ＋ 3** | 眉标同上＋副标题 `13 部位分 3 组，量了哪项填哪项；填好至少 1 项，复制区那一段就是能直接跑的命令`（`；`）＋回显 `胸围 102cm · 腰围 84cm · 腹围 91.5cm · 臀围 94.5cm · …`（`·` 切 13 段）＋表单说明 `测量日期（默认今天；补记历史日期同样适用）＋备注（可选）`（`＋`） |

## 2. 改了什么（逐条，五页同族）

| # | 债（改前） | 形状（改后） |
|---|---|---|
| ① | 眉标 `配置型 wizard · 填表→复制 prompt→AI 写库`；`<title>卡路里·配置向导` | 眉标 `身体细节 · 填表核对`，B 线路左槽 `填好确认一遍，再照复制区那一段把记录写进库里。`；`<title>` 按编排者裁定取 `卡路里 身体细节` |
| ② | 回显印英文枚举名（`来源 gym`／`home_caliper`） | 「上次记的」折叠块里 `来源` 一律中文名（`健身房 InBody`／`家测皮褶钳`），两条事实一格一条（`renderFactStrip`） |
| ③ | `7 处总和:— mm · 公式: BD = 1.112 - 0.00043499×Σ + …` | 整段撤：说明改人话「皮褶钳这一支：体脂率不用手填。把 7 处皮褶填齐，再补上年龄和性别，命令就会按 Jackson-Pollock 7 点法算出体脂率。」**系数与变量不上屏**；缺值不印 |
| ④ | 参数说明 `日期（必填）＋来源（必填）＋…＋备注（可选）` | 一句人话「带星号的必填。皮褶钳那一支的体脂率不用手填。」；必填由每格自己的星号承担 |
| ⑤ | `每项范围 (0, 100) mm · 总和 7 处建议 40–250` | 撤；范围由每一格的 `min`／`max` 约束，说明改「每处按毫米读皮褶值，7 处都量到才换算得出体脂率。」（总和填齐时写成一句人话） |
| ⑥ | 复制区 `；`／`／` 串 | 一条一行：`// 请先选来源。本页按默认的「家测皮褶钳」打开。` ＋ `// 可选的来源：` ＋ 三条 `// - …`；缺项／越界清单同样一条一行 |
| ⑦ | 围度副标题 `13 部位分 3 组，…；…`；回显 `·` 串 13 段 | 副标题撤（B 线路结论行代替）；回显改 `renderDistributionRows` 一条一行 |
| ⑧ | 五页都没接手机端配方 | 五页全部 `pageUi: true` |
| ⑨ | 表单跨全宽（短字段各占一整条约 1000px 的横杠） | 新立页内样式件 `src/body/wizardUi.ts`：≥641px 表单排**两列**（说明句占第一格），窄屏一列 |
| ⑩ | 复制区四处按钮、两颗各占半宽、「复制日志」出现两次 | 收成**一个动作区**：`复制指令` ＋ `复制数据 ▾`（`copyActionHtml` ＋ `dataCopyArea`），「复制日志」在五页上各 0 次 |
| ⑪ | 输入框在白底页面上看不见边（截图上空字段像没有框） | 页内样式给框加一条 1px `--line` 边线（配色与尺寸不动） |
| ⑫ | 备注示例 `如: 早上空腹 / 减脂期第 30 天`（半角冒号＋斜杠并列） | `如：早上空腹，或减脂期第 30 天` |

## 3. 装配拆分（告警线第四步）

`wizardPlate.ts` 改前 **349 LF**（已贴线），本票措辞面继续变长，当场执行拆分：

> **已超线，需要根据规则进行重构。** 超因：这一件同时住着「取数／入参校验」（`build*WizardView`）与
> 「两段文本的措辞」（人眼核对清单、缺项清单、复制区那一段）。

拆法＝**按面切**：`src/body/wizardPrompt.ts`（新，163 LF）住两段文本与它们的构建函数；
`wizardPlate.ts` 留取数与字段口径并向**本件薄转出**（出口名与签名一字不变，`wizard.ts` 与测试零改动）。
拆后读数：`wizardDocs.ts` **288**／`wizardPlate.ts` **325**／`wizardPrompt.ts` **163**／`wizardUi.ts` **27**（全在 350 线内）。

## 4. 本族测试与既有测试的改动（如实记账）

- 新增 `packages/skill-calorie/test/body-wizard-538.test.mjs`（本族门：内部叫法／英文枚举／公式变量／`＋` 串／
  范围字面量／「复制日志」次数／`pageUi` 三件／空参数不抛）：`pass 3／fail 0`。
- **改了 `packages/skill-calorie/test/t366-复制执行闭环.test.mjs`**（不是本票写集，但它的断言与本票要落的形状直接冲突）：
  ① 复制区改成「核对折叠区 ＋ 一个动作区」后，取复制文本的助手改为「先找预览块，找不到退回复制按钮载荷」；
  ② `可见文本应含 JP7 公式原文` → `正文不再印 JP7 公式原文`（负责人第 4 条要求系数与变量不上屏）；
  ③ 缺项／越界三句的期望字面量随新措辞改（仍是同一批判据，只是换了人话）。**这一笔请在收口时报给编排者**。
- `#538 裁定3`／`readonly`／下拉候选值／键表对账等结构性断言**一条未动**。

## 5. 收口读数

| 项 | 原文 |
|---|---|
| 分隔符探针（5 件页群） | `RESULT: 5/5` ＋ `PASS`（五页节点级命中全 0） |
| 三档溢出 | `OVERFLOW-ZERO pages=5 cells=15 failed=0`（390／768／1440 全 0） |
| 本族测试 | `node packages/skill-calorie/test/body-wizard-538.test.mjs` → `pass 3／fail 0` |
| 四件 LF | 288／325／163／27 |
| 真库 | 全程 `SKILLS_DB_PATH` 指 `.scratch/m158v2/t538/tmpdb`，真库零写入 |

### 5.1 阻塞（报编排者）

`packages/skill-calorie/dist/triggers/routes.generated.js:3` 当刻是**语法坏串**
（`--params \'{{"date":"今日",...}}\'` 里出现字面 `\n`），任何 import 它的测试／CLI 在**加载期**就 SyntaxError。
该 dist 件落盘时间 `14:45:48`，**晚于**本席位最后一次出页与探针，且 `src/triggers/routes.generated.ts`（14:45:58）
本身是干净的 ⇒ 像是**别的席位跑生成器只落了一半**。本席位按纪律**不跑 `pnpm gen`**、不碰该件，
报给编排者统一处置。
