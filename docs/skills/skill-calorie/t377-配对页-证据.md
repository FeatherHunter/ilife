# t377 · 配对页证据（combined 系 71 条＋pair／window 接线核验）

> 票：#377（地图 #160）。用词照 `docs/agents/wording.md`：命令／跑得通／过程型／结果型／回执／页面交付。
> 数字以本文件所贴机器读数为准，不另存第二份清单。视觉判定留用户肉眼，本文件只给程序绿。
> 本票零源码改动：routes／冻结表／生成物一律不动，前 13 条命令声明不动，不碰 `calorie_deficit`。

## 0 · 结论先行

- combined 系 71 条（`routes.ts` 内 `calorie.view.combined` 记录数 71，以 #171 复算为准）全部有命令可执行；
  抽样覆盖全部 11 个 pair（含自定义窗口 1 条）：真出口 exit 0，结果型 HTML 落盘、回执绝对路径，
  `RESULT: 15/15`（12 条抽样＋3 条负向探针），`CHECK: PASS`。
- pair／window 真被消费：输出逐字含输入的 pair 与 window／起止（回退缺省即露馅）；
  非法 pair／非法窗口／越界窗口一律大声失败（非 0），无 #154 式静默回退（测试 G4＋#110＋本票负向三重）。
- 门绿：`pnpm build` 绿、抽样绿、靶向测试 combined 面 57／58（唯一红属照片域他人件，§4）。
- 如实红（票外并发累积，非本票写入集）：`pnpm gen:check` 红（scene-02 翻转差＋118 条命令，他票同期条命令所致）；
  t293 P1／P3 FAIL（同源：豁免登记 stale＋两处 72 字面量＋基线红 knock-on）；全量 1568／1548／20（20 红全在他人域）。
- 进度 95%，不 close（待确认见 §6）。

## 1 · 诊断（开工前）

- `Blocked by: #171, #376`：#171 CLOSED（复算 TOTAL 169／A 88／B 81／unassigned＝0，
  当刻复跑 `TOTAL: 169 A: 87 B: 82 UNASSIGNED: 0 INTERSECT: 0 CHECK: PASS`；
  A 87 系 #376 翻转 1 条后启发式计数，唤醒词归属仍 88，以冻结表为准）；
  #376 OPEN 95% 条件交棒：`multi-trend`（key `calorie.view.multi-trend`／shape stat／
  示例 90d＋target／routes order 353／冻结 order 93）只读复用，不改口径。
- `.scratch/t377` 开工前不存在（已建）；`docs/skills/skill-calorie/t377-*` 开工前不存在（本文件为首件）。
- #377 已认领（assignee FeatherHunter，他人未占）。
- combined 71 构成：routes order 260–319（60）＋ 377–386（10）＋ new order 6（1）；
  11 个 pair：weight_calorie 11／weight_deficit 10／weight_exercise 10／weight_protein 10／
  weight_bodyfat 6／weight_waist 6／calorie_exercise 6／protein_carbs 4／protein_fat 3／
  carbs_fat 3／water_weight 2（合计 71，与 classify 复算一致）。
- 页面装配现状：combined 结果页已由 `render/analysisPlate.ts` 取数（`buildCombinedAnalysis`，
  唯一源 `buildSeries`＋`analyzePair`，不自造 SUM）＋ `render/trendDocs.ts` 装配
  （`buildCombinedDoc`：参数表单／KPI／双轴图／散点回归／分层／明细表／复制区，
  图表走 `packages/base-render/src/` 现成块，整页与复制区走卡路里公共层）；
  本票无需新建装配（#171 §5“既有或新建”中取既有），写入集即“核验＋证据”，与 #378 不相交。

## 2 · 真出口抽样（固定种子库）

- 种子：`docs/research/t81-seed.mjs`（`SEED_TODAY=2026-09-07`，`seedFull`；只写系统 tmp）。
  7 天窗（09-01～09-07）下 11 个 pair 全有数据；custom 占位符按 t171 §6 换成 2026-09-01／2026-09-07。
- 脚本：`.scratch/377/pair-sample.mjs`（冻结 cli 逐字照抄，order 见表；断言全在内存跑完才落盘）。
- 链路（每条）：说唤醒词 → 复制 prompt → 冻结 cli 执行 → 结果型 HTML 落盘、回执绝对路径。
- 逐条读数（字节＋sha256 全值；样例固化 `.scratch/377/out/sample-<pair>-<window>.html`）：

| pair／window | order | 唤醒词 | 字节 | sha256 |
|---|---|---|---|---|
| weight_calorie／7d | 260 | 看体重 vs 摄入(最近 7 天) | 74050 | ef0666162beff684d42a6d11be67bbd06c0ebe99c3b274310305ed06f435b212 |
| weight_exercise／7d | 270 | 看体重 vs 运动(最近 7 天) | 74068 | 307eed711628219e4453f46a253be85492adf9a99d868ccd692db4018349f5eb |
| weight_protein／7d | 280 | 看体重 vs 蛋白(最近 7 天) | 74018 | 3df19ef8d6f6197201e02afbc97aca108d74d468efe8656da1a5da59c9933d18 |
| weight_deficit／7d | 290 | 看体重 vs 缺口(最近 7 天) | 73490 | f5e4cc325a5613af90d8eb90571ca730eb8c9e98770ff49470c696ec96f1c169 |
| calorie_exercise／7d | 300 | 看摄入 vs 运动(最近 7 天) | 72238 | c816592ec2c49106c20c7bb1fe3be852eb0bf2b38eac4d7d3458b510f59206c5 |
| weight_bodyfat／7d | 306 | 看体重 vs 体脂(最近 7 天) | 70717 | 66a16d3e269bdeeaa7e60360709a0ff71a9928691aac2ecc2ac2d3fba06e135f |
| weight_waist／7d | 312 | 看体重 vs 围度(最近 7 天) | 70693 | a397c618d2dcec987c6144bf25ee966ab87cc30fead82b04714bce8ed7adf77b |
| water_weight／30d | 318 | 看饮水 vs 体重(最近 30 天) | 81811 | 2d473b5165d91df3b72637b96f64e342a587187569eef30feb682e5797ca8d39 |
| protein_carbs／7d | 377 | 看蛋白 vs 碳水(最近 7 天) | 70956 | 2cb94c43184f83b66ce8e358b7a80b4c6a2c7512c5d28744b829e79b8a7c41c2 |
| protein_fat／30d | 381 | 看蛋白 vs 脂肪(最近 30 天) | 74085 | e7503f357862f66b0f4f73593639d286f408d00fb829dbd987ff69fea27cfdcf |
| carbs_fat／30d | 384 | 看碳水 vs 脂肪(最近 30 天) | 74094 | 4deb56ab876b2c4ece5140ab8cfbc64060e636ac072437721f9d9783a7dc96d7 |
| weight_calorie／custom | 269 | 看体重 vs 摄入(自定义) | 74054 | 663b5fe78212debaf602d2c261a7cd18fae481ba11e005b2ae063826ab082f51 |

- 负向探针（无静默回退）：N1 未知配对 exit 非 0（stderr 含“未知配对”）；
  N2 非法窗口 exit 非 0（stderr 含“window 非法”）；N3 越界 4000d exit 非 0（同上）。
- 汇总：`RESULT: 15/15`，`CHECK: PASS`（日志 `.scratch/377/sample.log`，指纹 `.scratch/377/fingerprints.json`）。

## 3 · 接线核验结论（pair／window 真被消费）

- 每条输出断言：回执绝对路径＋落盘可读非空＋HTML 含该 pair 名＋含该 window（custom 含起止两日）
  ＋含“复制数据”＋复制文本含 correlationR／correlationN＋指标全有限数。任一回退缺省即红，本次 12／12 过。
- 与既有断言互锁：G4（`g2g4-103.test.mjs`：99d／bogus／4000d 走 bad-input exit 2，不静默生效）
  ＋ #110（combined 全链：KPI＋双轴＋散点回归＋延迟＋分层＋明细＋复制；custom 窗；未知窗拒收）本次靶向复跑全绿。
- 缺口模板未碰：`calorie_deficit.html` 系 #161 归属，本票抽样与断言均未触及（`view.deficit` 零调用）。

## 4 · 门绿读数

- `pnpm build`：绿（exit 0）。
  GATE-RUN runId=569fd1cb-d593-440d-b2cd-a952c226cde5 cmd=pnpm build
- 抽样：绿（`RESULT: 15/15`，`CHECK: PASS`）。
  GATE-RUN runId=ccb7b779-dde4-4e1c-911d-d14d4a8d2a3e cmd=node .scratch/377/pair-sample.mjs --all
- `pnpm gen:check`：红（票外并发累积：盘上 scene-02 `product.import` exec vs 生成 non-exec wizard；
  118 条命令＝46 条会改数据库的命令＋72 条查询命令（gen 原文照抄：`键 118＝写 46＋读 72`），他票同期加命令所致；本票零 src 改动，不吸收、不重生成）。
  GATE-RUN runId=d866b727-f70c-4ba0-8531-011263596df9 cmd=pnpm gen:check
- t293 探针：`RESULT: P1=FAIL P2=PASS P3=FAIL P4=PASS P5=PASS 3/5`（同源：
  P1 三处 UNACCOUNTED＝豁免登记 stale 1 处＋两处 72 字面量钉死，均为他票加命令所致；
  P3 系基线红 knock-on；本票新命令面为零，无漏网面）。
  GATE-RUN runId=cfda2c72-a13e-4b36-98ca-5da27d2f8ab8 cmd=node docs/skills/skill-calorie/t293-验收-命令自治.mjs
- 靶向测试（8 件 combined 相关）：`tests 58／pass 57／fail 1`；
  唯一红为 `cmd-read-t11.test.mjs` 照片 parity（`doesNotMatch /base64/`，照片域他人件，与 combined 无关）；
  G4／#110／analysis-t5／render-t9／deprecated-126／softdelete-120／db-readonly-93 全绿。
  GATE-RUN runId=4fd9f2b2-5420-44c3-88d7-1fb1d843416a cmd=node --test 8件combined相关
- 全量套件：`tests 1568／pass 1548／fail 20`（20 红全在他人域：写链收据／照片 parity／
  运动移植命中／HELP 速查台记账／体重计划／路由 parity，无 combined／pair／window 相关红；
  combined 面 G4／#110／analysis-t5／render-t9 等在全量中同样绿）。
  GATE-RUN runId=fe3213db-0d0d-4c2c-a977-bfb2f107e4e4 cmd=node --test 全量（日志 `.scratch/377/full.log`）。
- 入库（本票持锁共 8 次：§4 门禁六条＋入库两条；`.scratch/` 不入仓）：
  GATE-RUN runId=1b8e2c1e-216e-40e7-bf92-03e0f869cdb8 cmd=git add docs/skills/skill-calorie/t377-配对页-证据.md
  GATE-RUN runId=327da339-eba7-4015-910f-5ca9feb1e09f cmd=git commit -F .scratch/377/commit-msg.txt -- docs/skills/skill-calorie/t377-配对页-证据.md

## 5 · 交付对账（#171 §5 写入集）

| #171 切分 | 本票实际 | 偏差 |
|---|---|---|
| combined 页面装配文件（既有或新建，不碰 multi-trend 装配） | 取既有（`render/analysisPlate.ts`＋`render/trendDocs.ts`），src 零改动 | 0（复用即交付，无需新建） |
| 不动 `routes.ts`／冻结表／生成物 | 一律未碰（抽样只读 `routes.ts` 取 cli 底本） | 0 |
| 命令面溶进本票、前 13 条不动 | `commands.ts` 零改动（含 viewCombined 处理函数） | 0 |
| 公共部件只读复用 | base-render 图表块＋`docPage`／`copyArea` 经既有装配复用，未复制第二份 | 0 |
| 不动 B 的模板与词 | `calorie_deficit.html`／诊断／营养／预测零触及 | 0 |
| 抽样脚本 | `.scratch/377/pair-sample.mjs`（＋`extract.mjs` 取数底本、`fingerprints.json`、`out/` 12 样例） | 0 |
| 证据件 | 本文件 | 0 |

- 行数告警线（350，LF 口径）：本票无 src 改动，不触发。
- 结构五铁律：无改动即无违反（复用经公开接口：`buildSeries`／`analyzePair`／`buildCombinedAnalysis`／`buildCombinedDoc`）。

## 6 · 进度与待确认（不 close）

- 进度 95%。
- 待确认（未确认不 close）：① 用户肉眼验收（判定③：抽样产物任选打开，文图一致即过）；
  ② 全量套件 exit 0（已收数：1568／1548／20，20 红全在他人域，留收口，不由本票吸收）；
  ③ 生成物门与 t293 P1／P3 的收口票号（本票红线不动生成物，需编排者指定收口票统一改数）；
  ④ 提交范围：本票提交仅含本证据件（`.scratch/` 不入仓），生成物留收口。
- MAP 同步：本票不改 #160 正文（Decisions 待验收后统一追加，备行见 §7）。

## 7 · MAP Decisions 备行（验收后用，不先行）

- Decisions 备行：#377 combined 系 71 条配对页核验过（11 pair＋custom 真出口 15/15，结果型 HTML 落盘可打开；前 13 条命令与生成物未动），待 #378 铺开剩余趋势页。`

## 8 · 遗留风险

- 生成物多票同期条命令（现 118 条命令）：本票若重跑 `pnpm gen` 会把他票未提交条命令带入，故按红线不跑，留收口统一。
- 靶向 1 红（照片 parity）与 gen:check 红同属并发累积，已逐项归因到他人件；若收口前有人修掉，复跑即绿，无需本票跟进。
- 超窗切分未触发：单窗内 11 pair 全收，无需报备切 #377a／#377b（切法仍有效：体重系／摄入营养系，写入集不相交）。
