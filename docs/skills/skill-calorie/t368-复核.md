# #368 卡路里场景 08 身体细节 · 索引表与 HELP 卡片（框架级）——**独立复核**

> 复核者＝#158 子票 #368 的独立复核席（只复核、不改实现、不关票）。复核对象＝作者三笔提交
> `04deb7d` ／ `4862a82` ／ `8eac546`（根 `D:\ilife`，pwsh）。**本文每句都由本席持锁重跑**，
> 不采信作者结论；探针自建在 `.scratch/t368-review/`（禁复用作者 `.scratch/t368/`），跑完即清。
> 口径源＝`gh issue view 368 --repo FeatherHunter/ilife --comments`（票面权威）＋编排者两条裁定（待验，见 §五）。
> 切片＝`04deb7d^..8eac546`；复核期间 HEAD 已由他席推进到 `b67740b`，但 `git diff 8eac546 HEAD -- packages/` 为空
> ⇒ 复核读数对应的实现面与该三笔一致。
> 运行记录＝`.scratch/locks/gate-runs.log`（未受版本控制），本文每条读数都带 `cmd=` 与 `runId`。

## 〇 · 判定

**PASS。** 票面四条判据 ①②③④ 本席独立复跑全绿，负向必红／还原必绿成立；#367 交棒的速查表陈旧 **5 行 → 0 行**
成立；提交范围与禁碰件核对通过；真库零写入；两处自设变异（作者未做过的形态）同样必红必绿。

另有两处**非本票引入、但作者陈述与实况不符**的点，登记为 S3（不影响本票交付结论）：
§四 A「`help:examples:check` 的『改前 126/127』实为『改前 0/0』」、§四 B「`help-center-106` 三个钉死数字被放宽」。

## 一 · 独立复跑：机器读数（每条都是本席自己跑出来的）

| 编号 | 读数 | cmd=（`runId`） |
|---|---|---|
| 0 | `exit=0` 编译 | `node node_modules/typescript/bin/tsc -b packages/skill-calorie`（`23f08549-5f44-4c9d-a745-9be333a9b5e7`） |
| ①③④ | `t368` 门 `RESULT: 11/11` `exit=0` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs`（`7787d4bd-4e0e-49e9-bf82-751693edbd95`） |
| ①③④ | 自建探针 `P1-A…P1-K`，`problems=0` | `node .scratch/t368-review/probe1.mjs`（`3f447994-deb9-4fad-b51f-4dcc25a94267`） |
| 裁定一① | **HEAD^ `helpCenter.ts` 原件回放**：`P2-C 除 editable_fields 外逐场景逐字相同 436/436 异件=[]`；`P2-D 新字段去掉 command／flow 后与旧逐字相同 436/436 异件=[]`；`P2-H` 抽检 4 卡（`home_today_overview`／`diet_add_meal`／`w_overview`／`body_comp_add_caliper`）`cli 块逐字相同=true` | `node .scratch/t368-review/probe2.mjs`（`33dfbff4-7a62-439f-837c-4774dd6c6c42`） |
| ② ⑤ | `P4` 第 4 行／第 6 行**照抄执行** `exit=0 doctype=1 charset=1 styleBytes=38152 external=0 pathOk=1`；`MUT-1/2/3` 改坏必红、还原 `equal=true` 必绿 | `node .scratch/t368-review/step2.mjs`（`ab117c39-fb68-43a0-a15c-30da27153196`） |
| 裁定二 | `W1` 基线（HEAD^ 脚本＋HEAD^ `SKILL.md`）`exit=1` `示例行数 0` `结构问题 128`；`W2/W3` `RESULT: 126/127` `exit=1` 唯一红 `calorie.history exit=4 ERR 4: 取数失败（缺失阻断）：最近7天无记录`；`W4a/W4b` `--only` 两态同红；直跑该行原文 `exit=4` | `node .scratch/t368-review/probe3.mjs`（`83acc474-3ed6-4c5d-86fc-1c286734b831`） |
| 裁定一② | 回归批 `130` 件逐件跑：`113/130`；红 17 件**在 dist 层把 `cliFields` 退回改前单行形态后仍全红**（⇒ 与本票无因果）；`t368` 门改后态 `exit=1`、还原后 `RESULT: 11/11` | `node .scratch/t368-review/batch.mjs after`（`233aeeb9-bdb7-4464-975e-bcb95d815849`）＋`rerun-reds.mjs`（`2f0d0125-8d46-404b-8318-63aece227d1c`）＋`attrib.mjs`（`a2cf3f79-878f-4366-a5ab-5054afd677f1`） |
| ⑧ 动前 | 告警线门**改前态**（HEAD^ 原件 ＋ HEAD^ 台账，走门自己的 `--root/--agents` 夹具口）`exit=1` `RESULT: 64/67`：`RED 台账陈化：scripts/gen-cli.mjs 台账=796 实况=754`／`src/photo/helpCenter.ts 523/485`／`scripts/build-help.mjs 386/362`；当刻 `RESULT: 67/67 PASS` | `node .scratch/t368-review/prestate.mjs`（`8fb7bc67-cc4b-4be7-8b74-565ec631b78c`） |
| ⑧ 收尾 | `t368` 门 `11/11 exit=0`；`gen:check` `GEN-CHECK PASS`；告警线门 `67/68 exit=1`（唯一红＝他席在飞件，见 §四 C） | `node .scratch/t368-review/final.mjs`（`c6a5c1ea-adc8-458e-a319-eff2f5558ba7`） |

（过程读数：`probe2` 前两趟 `exit=2/1` 是本席夹具的路径解析问题——`base-paint` 是 pnpm 包内链接、scratch 目录解析不到；
改绝对 file URL 后 `exit=0`；`attrib.mjs` 首趟因前台 120 s 超时被我方工具打断，**打断点在变异之前**，
dist 未被改写，已核对 `cliFields` 源码形态后重跑。这些中间态照实列出。）

## 二 · 票面四条判据（逐条自跑）

**① 索引表「写词 → 预检页命令」7 条逐条在。** 本席自写解析器（按表头列名定位，不 import 作者的门）读
`SKILL.md` 身体段（`## 场景 08 身体细节工作流程…`，19 行）：7 行、序号连续、唤醒词逐条对上
（`记体脂（皮褶钳）`／`记体脂（外部测量）`／`补记体脂`／`记围度`／`补记围度`／`看体脂向导`／`看围度向导`），
`P1-B problems=0`；每格「预检页命令」都是注册表里的键。
**映射的事实源自核**：`P1-C3 写词→预检页 与 WIZARD_WRITE_KEYS 一致 5/5`（`dist/body/wizardPlate.js` 的
`{measure:…measure-add, composition:…composition-add}` 反查）。
**负向**：`P1-E` 同文本去掉第 3 行 → 6 条（≠7）；把第 1 行命令换成 `calorie.view.nope-not-a-key` → 注册表里查无。
源码级真变异 `MUT-1`（改 `SKILL.md` 第 4 行预检页命令）→ 门 `exit=1` `RESULT: 8/11`，逐字节还原
`sha=99FC7952…equal=true` → `RESULT: 11/11`。
`workflows/08-身体细节.md` 与 `SKILL.md` 三列（序／词／页命令）逐行相同（`P1-F same=true`；「会改数据库的命令」列
入口行写法不同——`—` vs `—（页本身不改库）`——属资产里的补充说明，不冲突）。

**② 照抄执行 → `exit 0` ＋落盘整页。** `P4` 取索引表第 4 行与第 6 行的命令串**逐字**跑（空库、本包 `openDb` 建库、
`SKILLS_DB_PATH` 指临时目录），两条都 `exit=0`：
`key=calorie.view.measure-wizard`／`calorie.view.composition-wizard`，`doctype=1 charset=1 styleBytes=38152 external=0 pathOk=1`，
HTML 70 650／70 705 字节。
（`styleBytes` 与作者交付时的 38 117 不同＝共享样式字节数漂移；门只判 `>= 2048`，`8eac546` 已把这句话写成注释，
**不是**把某个数字钉死——本席核 `t368-索引卡片.test.mjs:221` 的断言就是 `cssBytes >= 2048`。）

**③ HELP 产物身体 13 条卡片命令字段值 == 注册表命令名 13/13。** `P1-G`：HELP 场景数据里身体 13 条**逐条**
「命令」字段 == `routesFor(词)` 首条 exec 路由的 `key`，且该 `key` 在 `CALORIE_COMBOS` 里 → `13/13`；
`P1-I` 全库口径：`427/427` 条 exec 场景的 `editable_fields` 形状＝`命令＋[工作流程]＋可执行命令`（末行 CLI 逐字等于
路由层 CLI），`non-exec 却发字段=0`。

**④ 身体段首列零命令名冒充唤醒词＋入口行标注。** `P1-D`：入口标注行 **2** 条（`看体脂向导`／`看围度向导`），
首列以 `calorie.`／`calorie-` 起头的行 **0** 条；`MUT-2`（把第 6 行首列换成 `calorie.view.composition-wizard`）→
`exit=1` `RESULT: 7/11`（④ 两条用例红），还原 `equal=true` → `11/11`。

**⑤ 自设变异（本席新增，作者未做过的形态）。** `MUT-3`：把 `helpCenter.ts` 里「命令」字段的**值**换成 CLI 全文
（`value: command` → `value: helpSceneCli(wakeWord)`），`tsc` 后门 `exit=1` `RESULT: 10/11`，`✖ ③ …命令字段值 == 注册表命令名`；
逐字节还原（`sha=03834C7D…equal=true`）＋重编 → `RESULT: 11/11` `T368-READINGS ③ 命令字段==注册表命令名 13/13`。
`MUT-1`（不存在的命令）／`MUT-2`（命令名冒充唤醒词）见上。三处改后 `git status` 为空。

## 三 · #367 交棒：速查表陈旧 5 行（前后读数）

本席自算（`P1-J`）：以「速查表显示值 = `REPR[k] ?? k`」比「应然值 = 各能力目录声明的 `wakeWord` ?? k」：

| 键 | 改前（`04deb7d^` 的 `build-help.mjs`） | 改后（`8eac546`） |
|---|---|---|
| `calorie.body.composition-add` | 表「记体脂」≠ 声明「记体脂（皮褶钳）」 | 一致 |
| `calorie.view.body-composition` | 无 REPR 行 ⇒ 列键名 ≠ 声明「看体脂」 | 一致 |
| `calorie.view.body-measure` | 无 REPR 行 ⇒ 列键名 ≠ 声明「看围度」 | 一致 |
| `calorie.view.composition-wizard` | 表「看体脂向导」（自造词）≠ 应然（声明无词 ⇒ 列键名） | 一致 |
| `calorie.view.measure-wizard` | 表「看围度向导」（自造词） | 一致 |

读数：**`STALE-BODY 5 → 0`**（作者报的「改后 2」是把「声明没写代表唤醒词的键」也算陈旧；按 `build-help.mjs` 的
`REPR[k] || k` 回落口径，那两行退回列键名后**不是**陈旧）。`P1-J2` 两个无词的键＝`calorie.view.composition-wizard`／
`calorie.view.measure-wizard`。另：`gen:check` `GEN-CHECK PASS`（键 127＝写 46＋读 81），说明速查表由生成链派生、未手改。

## 四 · 缺陷清单

### A 本票引入的实施缺陷：**无**（四条判据与全部禁碰项都过）

### B 作者陈述与实况不符（S3，不影响交付）
1. **`help:examples:check` 的「改前读数」**：作者 §七写「他席性既有红：`RESULT: 126/127`，唯一红 `calorie.history`」。
   本席把**改前两件**（`04deb7d^` 的 `check-examples.mjs` ＋ `04deb7d^` 的 `SKILL.md`）配对跑（`W1`）：
   `exit=1 示例行数 0（结构问题 128）` —— 位次解析器读 `cells[4]`，而 `#338` 早在 `shape` 后插了「流程」列，
   故**改前一行都读不到**（`0/0`）。改后（`W3`）才是 `126/127`。**红的内容面（`calorie.history`）确与本票无因果**
   （见 §五裁定二），但「改前 126/127」这句不成立：本票的解析器改动其实是**修好了**一个结构性坏门。
2. **`help-center-106` 的三个钉死数字被改宽**（作者称「未放宽判据」）：
   - `assert.equal(withCli.length, 367)` → `== 路由层派生值` ＋ `>= 341`（不再钉绝对值）；
   - `assert.equal(scenes.length - withCli.length, 69)` → **整条删除**，无等价替代；
   - `assert.equal(new Set(唯一 CLI).size, 307)` → `uniq > 0 && uniq <= 场景数`（唯一 CLI 的**碰撞守卫**没了）。
   三处旧值在本票进场前**已是陈旧**（`attrib2.mjs`：把 dist 的 `cliFields` 退回改前单行形态后跑 HEAD^ 版该测试 →
   `exit=1`，`actual: 427, expected: 367`；当刻实况 `场景 436／带 CLI 427／non-exec 9／唯一 CLI 384`），
   故这不是本票弄红的；但**放宽本身是本票新引入的**，且与票面「不动门与棘轮数字」相抵。**建议**：把三个数字改成
   当刻实况（427／9／384）或另开票认领，别用区间替代钉死值。
   （另两条棘轮件 `cmd-registry-294`／`legacy-ratchet-295` 本席复跑 `exit=0`，且不在提交清单里——门与棘轮数字这一条
   在**其它门**上成立。）

### C 范围外（他席在飞／既有，逐条归属，本席只报不认领）
- **回归批 130 件里 17 件红**，全部在「dist 层退回改前 `cliFields`」后**仍然红** ⇒ 与本票无因果。红的内容面分别是：
  复制按钮／用途提示（`render-copy-90`／`copy-component-179`／`profile-doc-179`）、软删除与回执文案
  （`cmd-write-40*`／`docpage-printable-448`）、快照与路由桶数（`cmd-read-t11`／`calorie-routing-81`／`calorie-triggers`）、
  运动域标签与页面机器话（`field-labels-449`／`sport-homogeneity-109`／`t401c`）、其它（`analysis-t5`／`fetch-t4`／
  `softdelete-120`／`home-lock-374`）。
- **`t445-告警线门`**：本席 12:0x 直跑告警线门 `RESULT: 67/67 PASS exit=0`（`65d38b61-…`）；稍后同一时刻他席落下未跟踪新件
  `packages/skill-calorie/src/home/goalProgressDocs.ts`（LF=351），门即 `67/68 exit=1 RED 漏报（台账没有）：src/home/goalProgressDocs.ts LF=351`。
  即该门红＝他席在飞（该测试件头自己写了「T5／T9 红＝全仓当刻状态」）。
- **告警线门的「动前」是红的**（§一 ⑧ 动前）：台账三行（`build-help.mjs` 386／`gen-cli.mjs` 796／`helpCenter.ts` 523）
  是他席 `#274`（`ca52b5d`）在本票在途工作区里 `--sync` 出来的；本票落地后三件实况**恰好**等于台账，门回绿。
  ⇒ 作者「告警线门 67/67、数字未变」的**结果**成立，但**动前不是 67/67 而是 64/67**；这不是放宽阈值（台账记的是各件真实 LF），
  但「读数未变」这句与实况不符，登记在此。

## 五 · 编排者两条裁定的独立结论

**裁定一（两栏落在共用槽位 `SceneEditableField` ⇒ 全域 exec 场景都多两栏，不收窄）——成立，本席维持。**
1. **没改任何一组的文案口径**（硬读数）：把 `04deb7d^` 的 `helpCenter.ts` **原件回放**（Node 类型剥离直跑，
   仅把 5 条相对 import 与 `base-paint` 改绝对路径），与当刻 dist 逐场景对账：
   `P2-C 除 editable_fields 外逐场景逐字相同 436/436`（`title`／`wake_word`／`status`／`prompt_template`／`types` 全逐字），
   `P2-D 新字段去掉 command／flow 后与旧逐字相同 436/436`，抽检 4 张卡（主页／记饮食／体重总览／身体细节）
   的 `data-field="cli"` 块**逐字相同**，`工作流程` 值＝该卡子功能名（`看今日主页`／`记饮食`／`体重复盘`／`记身体细节`）。
   唯一「既有文本被改动」的落点是 `复制参数` 按钮的载荷：由 `可执行命令: <cli>` 一行变为
   `命令: …／工作流程: …／可执行命令: …` 三行（`label: value` 口径不变）——这是全域 exec 卡片**可见增量**，
   属裁定已认领的收益面，不属「改口径」。
2. **没有别的场景的既有测试被打红**：本票直接门 `t368`／`help-center-106/-88/-91`／`help-delivery-139`／
   `help-paths-133`／`help-shell-134`／`help-reuse-245`／`skill-t11`／`skill-md-photo-285`／`photo-help-flow-345`／
   `t365`／`t366`／`t367(5/5)`／`t440`／`wizard-86`／`cmd-registry-294`／`legacy-ratchet-295` 逐件 `exit=0`；
   130 件回归批的 17 件红经「改前态对照」全部归他席（§四 C）。
3. **非 body 场景那两栏内容正确**（本席点名 3 个非 body 场景）：
   `看今日主页`→`calorie.view.home`（＝注册表名，在册）／`记一餐`→`calorie.diet.add`／`看体重曲线`→`calorie.view.weight-history`，
   三条都与自己的路由层 key 逐字相等。

**裁定二（`126/127 exit=1` 唯一红 `calorie.history` 判为他席性既有红）——结论成立，但「改前 126/127」这句要改。**
- 该行**不是**解析器改动产生的：`W4a`（当刻 `SKILL.md`）与 `W4b`（HEAD^ `SKILL.md`）用同一份当刻解析器跑
  `--only calorie.history` 都是 `RESULT: 0/1` `exit=4`；`W2`（当刻解析器 ＋ HEAD^ `SKILL.md`）与 `W3`（真实门）
  同为 `126/127`、唯一红行逐字相同；**不经任何解析器**直接跑该行原文
  `calorie-cmd-read calorie.history --params '{"days":7}'` 也是 `exit=4 ERR 4: 取数失败（缺失阻断）：最近7天无记录`。
- 根因（本席定位）：`src/analysis/historyStore.ts:30` 的 `getCalorieHistory(db, days = 7, now = new Date())` 用**真钟**，
  而 `src/analysis/commands.ts:297-300` 的 `viewHistory` 没把「被钉住的今天」（`todayISO()`／`CALORIE_TODAY`）传进去；
  种子库数据日恒为 `2026-09-07`（`docs/research/t81-seed.mjs` 的 `SEED_TODAY`），真实日期一过 09-14 的 7 天窗口，
  这条示例必红。**与本票写集（`SKILL.md`／`helpCenter.ts`／两个生成器／`check-examples.mjs`／门／资产）无交集**，
  不认领、不放宽；建议转给 `calorie.history` 的主人（属场景 10 分析域）。
- 但 §四 B1 已述：把这条红说成「改前 126/127」不准确——改前是 `0/0`（位次解析器读不到行）。

## 六 · 禁碰项与真库（零写入核对）

- **提交范围**：`git show --pretty=format: --name-only` 三笔＝11 件（实现＋门＋资产＋证据＋changeset）／2 件（证据与导出件）／
  1 件（门件注释）。`routes.generated.ts`／`keys.ts`／`registry.ts`／`combos.yaml`／`src/body/*`／
  `scripts/check-warning-line.mjs`／`AGENTS.md` 与 `04deb7d^` **逐字相同**（`git diff --stat` 无输出）。
- **`SKILL.md` 只许改身体段**：AUTO 块**之前**逐字相同；AUTO 块**之后**（剔除新增的 `## 场景 08 …` 19 行）
  与改前逐字相同（`P1-K true/true`）。AUTO 块内容由生成链重写（`gen:check PASS` ＋ `check-examples` 的
  「产物新鲜」判据在真实门里未被触发）。
- **真库零写入**：`D:\2Study\StudyNotes\.db\calorie_data.db` 复核窗口前后
  `len=3072000 sha=57CE9D87F5F23282 mtime=2026-09-14T13:53:37.7293890+08:00` **完全一致**；
  本席所有 ②／门的 CLI 运行都指临时目录（`SKILLS_DB_PATH=<tmp>`，`P4` 打印落点）。
- **工作区**：本席变异逐字节还原（三处 `equal=true`）、`dist` 还原 `equal=true`；复核结束时
  `packages/skill-calorie` 下的改动仅他席在飞件（`src/home/today.ts`／`test/home-lock-374.test.mjs` 已改＋
  `src/home/goalProgressDocs.ts`／`test/t467-goal-progress.test.mjs` 未跟踪），与本票无关。

## 七 · 下一手缺什么

1. **`help-center-106` 的三个数字**：需本票作者（或收口席）把 `367／69／307` 改成当刻实况（`427／9／384`）
   或登记转票；现形态丢了「唯一 CLI 碰撞」这条守卫。
2. **`calorie.history` 示例红**：属分析域命令的口径问题（`viewHistory` 未传 `now`），建议转票；修好后
   `help:examples:check` 即 127/127。
3. **他席在飞的 17 件红**：与本票无关，需各自图认领（其中 `src/home/goalProgressDocs.ts` 一旦提交就要同步告警线台账）。
4. **`SKILL.md` 两处手写键数陈旧**（作者 §九 自述：`:12` 的「99 键」、`:39` 的「64＋35」）——本席只确认它们确在
   身体段之外、本票按写集纪律未动；归 `#278`。

---

*复核运行共 17 次持锁条目（含 3 次夹具/超时中间态），逐条见 `.scratch/locks/gate-runs.log` 里
`cmd="node .scratch/t368-review/*"` 的 RUN 行；探针源码随 scratch 目录清理，读数已全文落在本件。*

---

# 八 · 窄复验（S3 收口，作者提交 `d5f0214`）——**PASS**

> 收口三件：`packages/skill-calorie/test/help-center-106.test.mjs`／`docs/skills/skill-calorie/t368-索引证据.md`／
> `docs/skills/skill-calorie/t368-gate-runs.log`（`git show --pretty=format: --name-only d5f0214` 恰此三件；
> `src/**` 与 `SKILL.md` **零命中**）。复核结论＝**通过**；另有两处**精度**级备注（§8.5、§8.6），不改变判定。

## 8.1 四个数字＋两条碰撞数：本席自算（不采信作者的探针）

本席只用 `dist` 的公开出口（`buildHelpSceneData`／`helpSceneCli`／`routesFor`）自算一遍
（`cmd="node .scratch/t368-review/s3.mjs"`，`runId=ff07ed21-d356-4e2d-b71f-73d7a448bfc0`，exit=0）：

```
S3-A 自算：SCENES=436 WITH-CLI=427 NON-EXEC=9 UNIQUE-CLI=384 DUP-GROUPS=30 DUP-ROWS=43
S3-A2 交叉核：能力目录声明 115 条带词声明中，路由命中 exec 的 = 114（声明面按命令去重，与场景面不同分母）
```

四个钉死值与两条碰撞数与作者报的**逐字相同**。另用**另一条来源**（逐能力目录声明的 `wakeWord` → `routesFor` 命中 exec）
交叉核过 exec 面；`non-exec` 9 条**逐条点名**（复核时实况）：
`plan_execute「落地训练」`／`plan_execute_weekend「落地到本周末」`／`plan_execute_month「落地到本月底」`／
`plan_sync_xunji「同步到训记」`／`plan_backfill_xunji「拉训记实绩」`／`nut_advice「看营养建议」`／
`关掉每日定时复盘…「关闭定时复盘」`／`开启每日定时复盘…「开启定时复盘」`／`看当前定时复盘配置…「查定时复盘」`。

## 8.2 承重证伪（本席自己做，两次就地变异＋逐字节还原）

```
S3-D uniqueCli 384→383   → exit=1 :: ℹ tests 10 / pass 9 / fail 1  AssertionError: 唯一 CLI 条数＝实况 384
S3-D 还原 equal=true
S3-D nonExecScenes 9→8   → exit=1 :: ℹ tests 10 / pass 7 / fail 3（三处引用 FROZEN.nonExecScenes 的断言全红）
S3-D 还原 equal=true
S3-D 还原后 help-center-106 exit=0 :: ℹ tests 10 / pass 10 / fail 0
S3-F 收尾 git status（本席改过的测试件，须空）=""
```

⇒ `uniqueCli` 与 `nonExecScenes` 两处**确实是硬断言、且被三条以上判据引用**，不是摆设。

## 8.3 松口径与「逐条扫」核对

- `S3-C 疑似松口径行 4`——逐条看过，**没有**`>=`／`0<n<=` 之类的放宽残留；命中的 4 行是
  `assert.ok(start > 0)`（下标守卫）／`assert.ok(dups.some(n > 1))`（前置）／`assert.ok(flow.value.length > 0)`（非空串）／
  `assert.ok(flowCount > 0 && flowCount <= execCount)`（**新栏**的行数上界）。前三行与本次收口无关，
  第四行是 `04deb7d` 起就有的**新指标**、无历史基线，不属「放宽旧数字」。
- `S3-C2` 钉死断言命中：`436=true 427=true 9=true 384=true｜uniq 硬断言=true｜唯一<场景数=true｜碰撞 30 断言=true｜43 行断言=true`。
- **non-exec 逐条扫（非抽样）**：本席自己按 9 条 id 逐条从 `renderHelpCenterHtml` 产物里抓 `复制参数` 载荷 ——
  `S3-B 9/9 条回落 Scene.id`（作者那条 `for (const nonExec of nonExecScenes)` 前还有 `assert.equal(nonExecScenes.length, 9)`，
  故「9 条都查」是硬断言）。

## 8.4 两门复跑

| 门 | 读数 | cmd=（`runId`） |
|---|---|---|
| `help-center-106` | `exit=0` `ℹ tests 10 / pass 10 / fail 0` | `node .scratch/t368-review/s3.mjs`（`ff07ed21-d356-4e2d-b71f-73d7a448bfc0`） |
| `t368-索引卡片` | `exit=0` `RESULT: 11/11` | 同上（`S3-E`） |

## 8.5 证据两处订正：属实（一处归属再精确一格）

- **`help:examples:check` 改前读数**：订正为「改前 `RESULT: 0/0` ＋ 结构问题 128」——与本席 §一 的 `W1` 读数
  **逐字相同**（`0` 行、`128` 条结构问题＝「行数 0 != 127」1 条 ＋ 逐键「缺示例行」127 条），订正属实。
- **告警线门归属**：订正为「读数随实况到 67/67」＋点名 #274，不再写「数字未变」——**方向属实**；
  精确一点：本席 §一 ⑧ 动前实测的三行陈化里，`gen-cli.mjs` 754→796 与新增 `build-help.mjs` 386 **来自 `ca52b5d`**
  （#274，11:15:11），而 `photo/helpCenter.ts` 485→523 **来自同一张图的更早一笔 `1ee8141`**（#274，11:07:41，
  `git log -S'| 523 | 超因：#88'` 只命中它）。故归因写成「#274 的 `1ee8141`＋`ca52b5d` 两笔」更准；结论不变。

## 8.6 残留观察（S4，非缺陷）

`help-center-106` 里 `工作流程` 栏只判 `flowCount > 0 && flowCount <= execCount`（区间）。当切实况＝
**带 flow 408 条／不带 flow 19 条**（`cmd="node .scratch/t368-review/s3b.mjs"`，`runId=76366140-4f23-4368-9411-7a0cfba401d2`）。
若要与本票的「按实况钉死」口径一致，可把这一行也钉到 `408`（或 `execCount − 19`）；不钉也不构成放宽旧数字，
登记为下一手可做的收尾。

## 8.7 其余（未变）

`§四 B2 的三个数字`（`367／69／307`）本席 §七 提的收口要求已被 `d5f0214` 满足：现形态为
`FROZEN{436,427,9,384}` ＋ `uniq.size === 384` ＋ `uniq.size < cliValues.length` ＋ 碰撞 `30`／`43` 两条硬断言
＋ non-exec 逐条扫。`§五` 两条裁定的结论不变；`§四 C` 他席在飞项与原判定不变。
