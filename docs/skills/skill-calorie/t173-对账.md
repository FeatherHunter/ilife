# #173 对账结论：场景 01 主页边界与公共层

- 基线：`96bbfd1`（2026-09-14 当刻 HEAD）。本票只做诊断与文档回写，`packages/` 源码一行不动。
- 权威表：`D:\2Study\StudyNotes\SKILLS\卡路里\scripts\_triggers.py`，`'wake_word'` 出现 436 次。
- 用词照 `docs/agents/wording.md`：命令就是 `calorie.*`；中文页面只说结果型 HTML；本图 0 条需要预检确认页。

## 一、对账结论：唤醒词 → 老做法 → 去处

老技能 9 条的共同点（逐条核对原文，非只看路由标注）：`output_type` 全 `result`，
`html_template` 全 `templates/home_dashboard.html`（旧仓有实物，38075 字节，以 `<!doctype html>` 起，
内含 `secDiet`／`secExercise`／`secGoals`／`secBudget`／连续记录／`periodView` 等区块），
`depends_on_external` 全 `False`，`order` 0–8。

| # | 唤醒词 | 老做法（`key`／`subfunction`／`data_source`／`data_fields`） | 新仓去处 | 去处分类 |
|---|---|---|---|---|
| 0 | 看今日主页 | `home_today_overview`／看今日主页／`render_home.py` 全量／`today_kpi, goal_progress, weekly_trend, streak_days` | `calorie.view.home`（`src/home/commands.ts`；路由 `src/home/routes.ts` order 0；`WAKE_ROUTES` scene 01 全 `exec`） | 老技能做过、新仓已接命令 |
| 1 | 看今日饮食概览 | `home_today_diet_overview`／看今日主页／`render_home.py --section diet`／`diet_calories, diet_protein, diet_goal, diet_vs_target` | `calorie.view.diet`（路由 order 1） | 老技能做过、新仓已接命令 |
| 2 | 看今日运动概览 | `home_today_exercise_overview`／看今日主页／`render_home.py --section exercise`／`exercise_burn, exercise_duration, exercise_goal, exercise_vs_target` | `calorie.view.exercise`（路由 order 2） | 老技能做过、新仓已接命令 |
| 3 | 看今日体重概览 | `home_today_weight_overview`／看今日主页／`render_home.py --section weight`／`latest_weight, weight_goal_gap, weight_delta_7d` | `calorie.view.weight`（跨家，命令住 `src/weight/`；路由住 `src/weight/routes.ts` order 3，本图只核验、不实现） | 老技能做过、新仓已接命令（跨家核验） |
| 4 | 看今日目标进度 | `home_today_goal_progress`／看今日主页／`render_home.py --section goals`／`goal_calorie_pct, goal_protein_pct, goal_water_pct, goal_exercise_pct` | `calorie.view.goal-progress`（路由 order 4） | 老技能做过、新仓已接命令 |
| 5 | 看本周主页 | `home_week_overview`／看周期主页／`render_home.py --period week`／`week_diet_total, week_exercise_total, week_weight_trend` | `calorie.view.home`（`windowDays:7`，路由 order 5） | 老技能做过、新仓已接命令 |
| 6 | 看本月主页 | `home_month_overview`／看周期主页／`render_home.py --period month`／`month_diet_total, month_exercise_total, month_weight_trend` | `calorie.view.home`（`windowDays:30`，路由 order 6） | 老技能做过、新仓已接命令 |
| 7 | 看连续记录天数 | `home_streak_days`／看今日成就／`render_home.py --section streak`／`streak_current, streak_longest` | `calorie.view.home`（路由 order 7；`streakDays` 经 series 倒数得出） | 老技能做过、新仓已接命令 |
| 8 | 看今日热量预算 | `home_today_budget`／看今日主页／`render_home.py --section budget`／`tdee, exercise_burn, intake_today, remaining_calories, goal_gap` | `calorie.view.home`（路由 order 8；`deficitToday`＋当日摄入覆盖缺口口径） | 老技能做过、新仓已接命令 |

去处统计：老技能做过、新仓已接命令 9 条；必须多步交互 0 条（9 条 `output_type` 全 `result`，
prompt 里没有一条写先问／先确认）；新架构划出去不做 0 条。跨家 1 条（看今日体重概览）只核验不实现。

防幻觉结论：无幻觉词。9 条在权威表里逐字命中且 `order` 0–8 同序；新仓
`src/triggers/scene-01-home.ts` order 0–8 逐字同序逐字段同值（`data_fields` 逐条一致）；
`WAKE_ROUTES` 里 `scene == '01'` 的记录当刻读数 9 条，全 `exec`，命令映射与上表一致
（`view.home`×5、`view.diet`×1、`view.exercise`×1、`view.weight`×1、`view.goal-progress`×1）。

字段差异核验点（已由 #371／#372 票面“字段补齐”覆盖，不另开票）：

- `streak_longest`：新仓 `HomeData` 只有 `streakDays`（当前连续），无最长记录位。归 #371 核验。
- 预算三字段：老 `tdee`／`remaining_calories` 在 `HomeData` 接口无显式位，
  由 `deficitToday`＋`daily.totals`＋`calorieGoal` 同口径推出；`exercise_burn` 在数列层有数。归 #371 核验。
- 周期三字段：老 `week/month_*_total` 与 `weight_trend` 由 `buildHomeData` 的窗口数列
 （`week.series`／`avgIntake`／`avgDeficit`／`loggedDays`，`windowDays` 1..90）同口径推出。归 #371 核验。
- 体重三字段：`latest_weight`／`weight_goal_gap`／`weight_delta_7d` 由 `WeightDashboard`
  的 `trend.lastWeight`／`gapKg`／`trend.changeKg` 覆盖。归 #372 核验。
- 不开新票的理由：以上四点都是“同一命令内字段是否到齐”，#371／#372 票面已明确管字段补齐，
  无超出已有票范围的缺陷，故不开票、不扩大范围。

## 二、页面清单：1 类，就地写

- `home_dashboard.html`×9 → 总览页类 → 就地写 `src/home/`（卡路里公共层候选；
  今天只有本场景用，共用位从第二个用法里长出来，故不上移 `packages/base-render/src/`）。
- 当刻装配实况（只记录、不改）：5 词经 `calorie.view.home` 出 `renderHomeHtml`
 （`pageShell` 片段，`<section>` 形，#370 先搬迁、#371 再整页化）；
  饮食／运动／体重三词经 `buildViewDietDoc`／`buildExerciseDoc`／`buildWeightDoc`
  出完整文档（`assembleDocPage` 包裹）；目标进度 1 词经 `renderGoalProgressHtml`
  出片段（整页化归场景 06，本图只核验那 1 条词跑得通）。
- 地图 `Not yet specified` 第一条的答案：今日总览族 5 条当年 `data_fields` 各不相同
  （今日四件套／本周三件套／本月三件套／连续双字段／预算五字段），老模板是同一张页
  按 `--section`／`--period` 显隐对应区块。结论：沿用同一组装配、按词显隐对应区块；
  连续记录天数与热量预算在总览页内是独立区块（承接老模板连续记录区／`secBudget`），
  与 KPI 组共存，不另设页面。

## 三、公共层增量：只定接口，不定长相

复用（不重复造）：`src/shared/docPage.ts` 的 `assembleDocPage`
（入参 `DocPageInput`：`docTitle`／`title`／`eyebrow`／`subtitle`／`content`／`charts?`）
与 `metricsOf`；`src/shared/copyArea.ts` 的 5 个名字
（`promptCopyArea`／`dataCopyArea`／`copyArea`／`copyLog`／`notice`）。

本图新增候选（今天只有本场景用，标候选、不上移）：总览页装配。

- 吃什么数据：`HomeData`（`src/home/home.ts`），经 `buildHomeData(db, date?, windowDays = 7)` 产出：
  `date`／`daily`（当日汇总）／`calorieGoal`／`waterGoal`／`caloriePct`／`proteinPct`／
  `waterPct`／`deficitToday`／`week{start, end, series, avgIntake, avgDeficit, loggedDays}`／`streakDays`。
- 有哪些位置可填（5 组，对老模板区块的承接）：今日 KPI 组（摄入／蛋白／饮水／缺口）、
  目标进度条组（热量／蛋白／饮水完成度）、周趋势组（周均摄入／窗口起止／有记录天数）、
  连续记录位（当前连续天数）、热量预算位（缺口＋当日摄入＋目标差距）。
- 长相不定：排版与样式在页面票（#371）里定，本票只定接口。
- 不上移 `packages/base-render/src/` 的理由：今天只有本场景用总览页。

## 机器证据（本机复验命令与读数）

- 权威表 `'wake_word'` 出现次数：436；本图 9 条行号 111–197，`order` 0–8 同序。
- `WAKE_ROUTES` 里 `scene == '01'`：9 条，全 `exec`（`view.home`×5、`view.diet`×1、
  `view.exercise`×1、`view.weight`×1、`view.goal-progress`×1）。
- `src/home/commands.ts`：4 条（`view.home`／`view.diet`／`view.exercise`／`view.goal-progress`）；
  跨家 `src/weight/`：`calorie.view.weight` 声明在 `src/weight/commands.ts`，
  路由在 `src/weight/routes.ts`（order 3）。
- 旧模板实物：`D:\2Study\StudyNotes\SKILLS\卡路里\templates\home_dashboard.html`，
  38075 字节，以 `<!doctype html>` 起，含 `secDiet`／`secExercise`／`secGoals`／
  `secBudget`／连续记录／`periodView` 区块。
- 取证脚本：`.scratch/t173/probe_old_template.py`（读模板字节数、区块计数、标题行）。

## 变异自证不适用理由

本票是纯文档活：未改 `packages/` 任何源码，未增删命令与路由，未动生成物。
判据是“地图正文可查到每条去处／页面合并类／公共层部件＋用词合规＋格式合规”，
无可变异的代码行为，故不做“改坏必红”自证。
