# T554 · 计划编辑器两处 S1 修复（证据件）

> 票面＝GitHub issue **#554**（地图 #157 收口期派出；来源＝`docs/skills/skill-calorie/t351-v15-review-报告.md` 第 1／2 条 S1）。
> 需求正件＝`docs/skills/skill-calorie/t351-v14-plan-editor-design.md` §1 第 3 条（**本票一行未改**）。
> 本件每条读数都是本席在**当刻树上**跑出来的；`GATE-RUN` 一行一条命令、带 runId 与退出码。
> 编译／测试／变异一律经 `tooling/run-locked.mjs` 持锁；判「有没有被人动过」一律用 **sha256**（本仓另有席位在刷新 dist 的 mtime）。

## 判定：PASS

两处 S1 都修在**同一处根因**上（「结构禁」与「参数开」曾绑在同一个 `locked` 开关上），修法是**照 §1③ 实现**，不是照口味实现：

| 缺陷 | 原实现 | 修法（本票） |
|---|---|---|
| **S1-①** 锁周力量参数是**一行纯文本**，组数／次数／负重（含 RM↔kg）改不了 | `moveHtml()` 里 `if (lock()) params = '<span class="pe-plain">' + plainParams(mv) + '</span>' …` 整段覆盖参数面 | **删掉这段覆盖**（连同 `plainParams()` 一起删）：锁周与母版周**同形**——`set-sets`／`set-reps`／`set-load`／`toggle-mode`／`set-min` 照样是格 |
| **S1-②** 锁周唯一可填的有氧格**没有坐标**，写值落到**别的行**（数据风险） | `plainParams()` 里 `var at = ''` ⇒ 输入框无 `data-d/s/m`，处理器读 `null` 得 0，回落 `(0,0,0)` 且取**当前周** | 同上：参数面只剩一条路径，坐标 `at`（`data-d`／`data-s`／`data-m`）**两条分支都随格出**；处理器读到的就是被点那一格 |

**结构面一行没动**（§1③ 前两句照旧）：`add-train`／`add-move` 仍 `disabled`、时段胶囊仍禁用、`del-train`／`del-move` 仍走 `lock()` 分支（锁周出 `pe-lockico` 图标，0 个删除钮）。

改动件：**1 件**——`packages/skill-calorie/src/render/planEditorRuntime.ts`
（净行数 **364 → 364**：删 10 行、加 10 行，全是注释与这次改动本身；台账 `src/render/planEditorRuntime.ts｜364` 因此**不陈化**，本席**没碰** `packages/skill-calorie/AGENTS.md`）。
件内还补了一段契约注释（放在 `lock()` 上方），把这次两处 S1 的根因与分工写死：「结构禁／参数开是两件事；凡 `lock()` 分支只许换『增删』那几处控件」。

- 源码 sha256：`b2aac9555632…`（复核席基线）→ **`c1fba00ead48da29a0fff0896a8259754bcba0e889ad65986c9ebe22bdb571cb`**
- 编译产物 sha256：`planEditorRuntime.js` `33db3ad8aa8b…` → **`f40dd2c70ce0029a1dad222c58f56066157937533c175fd0a806086b49b3b1e4`**；
  同族的 `planEditorCss.js` `4e396e2087f4…` 与 `planEditorDocs.js` `069770e0947e…` **逐字节未变**（＝那两件源码一件没碰）

## 一、机器证据（逐条：命令／退出码／摘要行）

| GATE-RUN runId | 命令（均经 `tooling/run-locked.mjs --ticket 554`） | exit | 摘要行 |
|---|---|---|---|
| `t554-gate-base` | `--run-id t554-gate-base -- node .scratch/t351-v15-review/t351-v15-review.mjs --phase=gate`（复核席原件，修前树） | **1** | `RESULT: 16/18`，红＝`C3.4`、`X1`（与票面基线逐字一致） |
| `t554-build` | `--run-id t554-build -- pwsh -NoProfile -File .scratch/t554/build-collect.ps1` | **0** | 内层三件事：`tsc -b` → `BUILD-EXIT=0`（无输出）；契约门 `--phase=collect` → `RESULT: 7/7`；`check-warning-line.mjs` → `RESULT: 79/83`（4 条红**全在他席件**，见 §四） |
| `t554-gate` | `--run-id t554-gate -- node docs/skills/skill-calorie/t554-plan-editor-gate.mjs --phase=gate`（**入仓后的常驻判据**） | **0** | **`RESULT: 18/18`**，0 条红 |
| `t554-mutation` | `--run-id t554-mutation -- node docs/skills/skill-calorie/t554-plan-editor-mutation.mjs --under-lock` | **0** | `MUTATION-SUMMARY: OK`（五处改坏各自必红 ＋ 五处还原逐件 sha256 全等） |
| `t554-gate-final` | `--run-id t554-gate-final -- node docs/skills/skill-calorie/t554-plan-editor-gate.mjs --phase=gate`（**变异套件跑完之后**的复验） | **0** | **`RESULT: 18/18`**，0 条红 |

### 变异红／还原一致（两行机器读数，取自 `logs/mutation.txt`）

```
RED: M1 变异=planEditorCss.ts 「周页签条 flex-wrap:nowrap 改回 wrap」 产物印记=.pe-tabs{display:flex;flex-wrap:wrap; 期望红=[C2.1,C2.5] C2.1=RED C2.5=RED 契约门 16/18 ⇒ OK
RESTORE: IDENTICAL M1 逐件 sha256 一致：源码 3/3、编译产物 3/3；复跑契约门 18/18（基线 18/18）读数漂移=无；源码 planEditorCss.ts=ea11db69756f planEditorDocs.ts=b2e977c868c4 planEditorRuntime.ts=c1fba00ead48
RED: M2 变异=planEditorRuntime.ts 「日页签「只渲染选中的那一天」改回全渲 7 天」 产物印记=dayHtml(0) + dayHtml(1) 期望红=[C1.2,C1.3] C1.2=RED C1.3=RED 契约门 13/18 ⇒ OK
RESTORE: IDENTICAL M2 逐件 sha256 一致：源码 3/3、编译产物 3/3；复跑契约门 18/18（基线 18/18）读数漂移=无
RED: M3 变异=planEditorRuntime.ts 「S1-② 复现：锁周参数格摘掉 data-d/s/m 坐标」 产物印记=var at = lock() ? '' : ' data-d=" 期望红=[C3.4,X1] C3.4=RED X1=RED 契约门 16/18 ⇒ OK
RESTORE: IDENTICAL M3 逐件 sha256 一致：源码 3/3、编译产物 3/3；复跑契约门 18/18（基线 18/18）读数漂移=无
RED: M4 变异=planEditorRuntime.ts 「S1-① 复现：锁周参数改回纯文本（没有可填的格）」 产物印记=function plainOf(x){ return '<span class="pe-plain">' 期望红=[C3.4,C3.5,X1] C3.4=RED C3.5=RED X1=RED 契约门 15/18 ⇒ OK
RESTORE: IDENTICAL M4 逐件 sha256 一致：源码 3/3、编译产物 3/3；复跑契约门 18/18（基线 18/18）读数漂移=无
RED: M5 变异=planEditorRuntime.ts 「S1-① 反侧：锁周「加一次训练」摘掉 disabled（结构禁复现）」 产物印记=(full || false ? ' disabled' 期望红=[C3.1] C3.1=RED 契约门 16/18 ⇒ OK
RESTORE: IDENTICAL M5 逐件 sha256 一致：源码 3/3、编译产物 3/3；复跑契约门 18/18（基线 18/18）读数漂移=无；源码 planEditorRuntime.ts=c1fba00ead48
MUTATION-SUMMARY: OK（五处改坏各自必红；五处还原后逐件 sha256 与基线全等）
```

- **M1／M2 是票面点名的两处种子**（`nowrap`／「只渲一天」），改的是复核已判为真的 §1②／§1①，两处都必红 ⇒ 那几格不是恒绿。
- **M3／M4 是本票两处 S1 的复现株**：M3（锁周格摘掉坐标）⇒ `C3.4`＋`X1` 红；M4（锁周参数回纯文本）⇒ `C3.4`＋`C3.5`＋`X1` 红。⇒ **本票新写的判据对这两处缺陷都有分辨力**（`C3.5` 改点不是放松，见 §三-3）。
- **M5** 摘掉锁周「加一次训练」的 `disabled` ⇒ `C3.1` 红 ⇒ 结构禁那一面仍被测着，「参数开」没把「结构禁」带走。
- 全程 `try/finally` ＋**逐文件按字节**还原（未用 `git checkout --`／未整目录还原）；五处还原后源码 3/3、编译产物 3/3 的 sha256 与基线全等，复跑读数零漂移。

### 落库面读数（票面第 4 条：编辑第 N 周某天某动作的参数，**只有那一行变**）

两条都是**从页面里真写一格、再把页面底部那张计划表逐行对账**（同一个 `tableDiff` 算法：按「周次｜星期｜时段｜动作｜部位｜类型」做键，比「量」与「负重」两列）：

```
X1（计分，改锁周有氧时长）
  输入框=<input type="number" min="1" max="600" value="20" data-act="set-min" data-d="3" data-s="1" data-m="0" aria-label="时长">
  写 77 分钟后表里变动的行=[{"row":"第 3 周 | 周四 | 晚上 | 椭圆机 | 全身 | 有氧","from":"20 分钟","to":"77 分钟"}]
  目标行={"week":"第 3 周","dow":"周四","slot":"晚上","name":"椭圆机","amount":"77 分钟","load":""}
  另一行（第 1 周 周一 凌晨 爬楼机）改前={"amount":"30 分钟"} 改后={"amount":"30 分钟"}   ← 不再被静默改脏
  整表行数 20 → 20

备注④（不计分，S1-① 的另一半：改锁周力量参数）
  第 3 周 周四 的「史密斯机深蹲」把组数 5 改 9 —— 整表变动行=[{"row":"第 3 周 | 周四 | 上午 | 史密斯机深蹲 | 腿 | 主要","from":"5 组乘 8 次 70 kg","to":"9 组乘 8 次 70 kg"}]
  被点那格坐标=<input type="number" min="1" max="30" value="5" data-act="set-sets" data-d="3" data-s="0" data-m="0" aria-label="组数">
  同名动作在母版周的读数（改前／改后，应原样不动）={"week":"第 1 周","dow":"周四",…,"amount":"5 组乘 8 次","load":"70 kg"} → 同上（逐字节相同）
```

⇒ 两条都只动**那一个动作那一行**：改前改后的差异列表长度＝1，另一行（含母版周同名动作）逐字不动。
「备注④」只作附件读数（**不计分**，免得把 18 条判据的计数改掉）；下一次要把锁周力量参数也做成计分条，改 `t554-plan-editor-gate.mjs` 的 `X1` 或另开 `X3`。

### 修后的 §1③ 三条同真（`C3.4`／`C3.5` 读数原文）

```
C3.4 锁周周四动作=["史密斯机深蹲","椭圆机"] 可填格=["set-sets","set-reps","set-load","set-min"] 纯文本参数行=[] RM/kg 切换钮=1 个 缺坐标的格=[]
C3.5 locked:true 态＝结构禁 true／参数开 true（可填格 ["set-sets","set-reps","set-load","set-min"]）；
     locked:false 态＝结构禁 false／参数开 true（尾周可填格同上、加训练 disabled=false、删动作 2 个）
```

对照修前（同一条判据、同一夹具）：可填格只有 `["set-min"]`、纯文本参数行 `["5 组乘 8 次 70 kg","分钟"]`、RM／kg 钮 0 个 ⇒ 正是票面 S1-① 的读数。

## 二、契约门脚本入仓（票面第 2 条）

复核席的脚本原来只活在 `.scratch/t351-v15-review/`。按票面「同一个门只在仓里一份」收进仓，落点＝**技能自己的目录**（`docs/skills/skill-calorie/`，与既有 `t351-v13-editor-wall.mjs`／`t525-样板页生成器.mjs` 同族），带 `554` 前缀：

| 入仓件 | sha256 | 说明 |
|---|---|---|
| `docs/skills/skill-calorie/t554-plan-editor-gate.mjs` | `2b59d0a671fb45f63846042625adbeffcaf671f707b51f41d2211715a1738288` | 契约门 ＋ 取证链（18 条计分 ＋ 7 条取证链）；路径全部相对本件，换工作目录也能跑 |
| `docs/skills/skill-calorie/t554-plan-editor-fixtures.mjs` | `063f918799f7bc5b0a1ea604cc67e4d2d609ea7fa262e945f8e8377cfb90174a` | 复核席夹具（5 状态；**判据的靶子，一行未改**） |
| `docs/skills/skill-calorie/t554-plan-editor-probe.js` | `2dd8a2994fdea6fa1a12f16f5cf4deac5a9ae8967b0b0da30614f10b882255a1` | 页内探针（计算样式／真点击／真填值／整表逐行对账）；本票只**增**两处附件读数（`strengthEdit` 加上 `tableBefore/tableAfter/week`），不改任何计分判据的取值 |
| `docs/skills/skill-calorie/t554-plan-editor-mutation.mjs` | `a9addedafbaa552196c25313aa86c3c145434fe6c147a65439bb0fa248f20bf4` | 变异红／还原一致自证（M1～M5） |

入仓后**跑出来的就是上面那两条 18/18**（`t554-gate`／`t554-gate-final`）——票面第 2 条「入仓后仍能跑出 18/18」成立。

**与复核席原件的三处改点（都是本票修完之后必须改的，逐条交代，改点都写在件头注释里）**：

1. **`C3.4` 加强**（原只数四个 `data-act`）：再加两条——锁周**一个纯文本参数行都不许有**、且**每一格都必须带齐 `data-d`／`data-s`／`data-m`**。这是 S1-①② 的 DOM 面判据。
2. **`C3.5` 改点**：原件自证的是**缺陷机制**（断言「锁周参数开＝假」，因为实现把两件事绑在一个开关上）；修完这句必然为假，故改成自证**需求本身**——`locked:true` 态「结构禁真 **且** 参数开真」、`locked:false` 态「结构禁假 且 参数开真」。**不是放松**：M4（把锁周参数改回纯文本）下这条照样红。
3. **`X1` 读数扩展**：把「被静默改脏的那一行」改前改后写进读数，断言里补一条「该行必须仍是 30 分钟」。

其余 15 条（`C1.1–1.4`／`C2.1–2.5`／`C3.1–3.3`／`C4.1–4.2`／`X2`）与复核席原件**判据同源**：`§1①`／`§1②`／`§1④` 那 11 格在修后全绿，且 M1／M2 证明它们测得动。

## 三、不许动的东西（逐条自证）

| 禁令 | 自证读数 |
|---|---|
| 不改需求正件 | `docs/skills/skill-calorie/t351-v14-plan-editor-design.md` **不在** `git status` 的改动面里（该路径查询输出为空），sha256＝`9208503c506dc5bff81b7505040e99c04aa19abdabe2b0d6254cac25f880d025`；本票一件未写 |
| 不把 §1①／§1②／§1④ 改红 | 契约门那 11 格全绿（`RESULT: 18/18`）；且 M1（`nowrap`→`wrap`）／M2（只渲一天→全渲 7 天）各自必红 ⇒ 非恒绿 |
| 不碰 `src/workout/**`、`packages/base-render/**`、他席 M 件 | 本席**只写 6 个路径**（1 件源码 ＋ 4 件入仓脚本 ＋ 本证据件，`git status` 原文见 §五）；同族 `planEditorCss.ts`／`planEditorDocs.ts` **连编译产物都逐字节未变** |
| 不 `git add`／`commit`／`push`／切分支／`stash`／`reset`／`checkout --`／`clean` | 一次都没跑（改动全留在工作区；`git status` 里我的件仍是 `M`／`??`） |
| 不跑包管理器安装／新增 | 一次都没跑（本票只用仓内已有的 `node`／`tsc`／headless Chrome） |
| 不去改他席件「让门变绿」 | 告警线门如实记账：`RESULT: 79/83`，四条 `RED 台账陈化` **全在他席件**——`src/render/sportPortDocs.ts`（台账 666／实况 757）、`src/diet/nutritionPortDocs.ts`（645／675）、`scripts/build-help.mjs`（386／504）、`scripts/gen-photo-baseline.mjs`（283／429）。**本件 `src/render/planEditorRuntime.ts` 不在红名单**（台账 364＝实况 364，因为本票净行数维持 364） |
| 不替负责人做审美／版式判定 | 本件只判契约层与可复现性；挤／观感／版式留负责人肉眼 |

## 四、重出样张（票面遗留出口）

修完**用真出口重出**（`buildPlanEditorDoc`，编译产物 `dist/render/planEditorDocs.js`），五份在 `.scratch/t554/gate/samples/`：

| 样张 | 字节 | sha256（前 16） |
|---|---|---|
| `01-空态.html` | 97897 | `6f9b59c406d58a67` |
| `02-母版周.html` | 102101 | `486dd6a49da90358` |
| `03-锁住的周.html` | 102114 | `56d63fe8f5df9d36` |
| `04-12周.html` | 110510 | `254c17cc256926d0` |
| `05-尾周全解锁.html` | 101065 | `24b24b02d95f2a4b` |

作者自己的样张在 `.scratch/t351-editor/samples/`（mtime 15:50:54＝V15 提交时刻，**比当刻树旧**），本票**没有**拿它当验收，也没改它。页内实测（真 Chrome）读数在 `.scratch/t554/gate/logs/readings.json`＋`dump-*.txt`。
> 口径如实写：这一页**今天没有命令出口**（`buildPlanEditorDoc` 调用方为零，接线归 **#553**），所以本票能验的是「这个导出函数在当前编译产物里的行为」＋真 Chrome 里的计算样式／点击／填值；**不把「样张能渲染」读作「真出口能跑」**。

## 五、逐条路径

**改动件（1 件）**
- `packages/skill-calorie/src/render/planEditorRuntime.ts`（`c1fba00ead48…`，LF 364→364）

**入仓的契约门脚本（4 件，§二 表）**：`docs/skills/skill-calorie/t554-plan-editor-{gate.mjs,fixtures.mjs,probe.js,mutation.mjs}`

**证据件（本件）**：`docs/skills/skill-calorie/t554-计划编辑器两处S1修复-证据.md`

**本席写集原文（`git status --porcelain -- packages/skill-calorie/src/render/planEditorRuntime.ts 'docs/skills/skill-calorie/t554-*'`）**

```
 M packages/skill-calorie/src/render/planEditorRuntime.ts
?? docs/skills/skill-calorie/t554-plan-editor-fixtures.mjs
?? docs/skills/skill-calorie/t554-plan-editor-gate.mjs
?? docs/skills/skill-calorie/t554-plan-editor-mutation.mjs
?? docs/skills/skill-calorie/t554-plan-editor-probe.js
?? docs/skills/skill-calorie/t554-计划编辑器两处S1修复-证据.md
```

**草稿与日志（`.scratch/t554/`，按票面归本席）**
- `logs/baseline-original-gate.txt`（复核席原件的 16/18）、`logs/run-build-collect.txt`＋`logs/build.txt`＋`logs/check-dist.txt`＋`logs/collect.txt`＋`logs/warnline.txt`、`logs/gate.txt`（18/18）、`logs/gate-final.txt`（18/18）、`logs/mutation.txt`（M1～M5）
- `mut/logs/{gate-base.txt,gate-M1..M5.txt,gate-M1..M5-restored.txt,mut-M1..M5-src.diff.txt}`、`mut/backup/*.bak`
- `gate/{samples/*.html,probe/*.html,logs/{checks.json,readings.json,last-run.txt,dump-*.txt}}`（Run C 的产物；`gate-base/` 是修前基线那次的产物）
- `preview-edits.mjs`／`check-drift.mjs`／`check-dist.mjs`／`build-collect.ps1`（草稿工具）

**加锁审计面**：`.scratch/locks/gate-runs.log` 里本票五次 RUN 行（`t554-gate-base`／`t554-build`／`t554-gate`／`t554-mutation`／`t554-gate-final`），变异套件的内层编译与复跑走隔离锁目录 `.scratch/locks-t554-inner`（不与自己持有的主锁死等）。

## 六、未做项 ＋ 下一手缺什么

**未做项（如实列）**
1. **接线不在本票**：命令出口／唤醒词键位／验收墙那 37 份归 **#553**（票面已裁）。本票只修页面契约。
2. **审美／版式未判**：挤、塌列、观感按纪律归负责人肉眼。
3. **`gen:check`／整树测试未跑**：本票不动命令面与生成物，跑它们只会被他席在途件带红（复核席已记同一现象）。
4. **S3 三项遗留未清**（复核席已记账，本票**故意不动**，动它们就超出「只动 §1③ 两处」的写集）：
   `.pe-plain` 样式规则现在成了死样式（在 `planEditorCss.ts`，本票一行未碰）；`isFirstWeek()` 仍是定义后无人用；锁周「加一次训练」仍只靠 `disabled` 一层挡（委派处理器里没有 `lock()` 守卫，合成派发仍能加段）。
5. **`备注④` 只作附件读数**：锁周力量参数的「只有那一行变」还没进 18 条计分（要进就得改契约门计数或另开 `X3`）。

**下一手缺什么**
1. 编排者收口：本票不提交（改动在工作区），需要一次统一提交（1 件源码 ＋ 4 件入仓脚本 ＋ 本证据件）。
2. 若要「锁周力量参数也只动一行」成为**计分条**：把 `t554-plan-editor-gate.mjs` 的 `备注④` 提升成 `X3`（计数变 19）；这不影响本票已拿到的 18/18。
3. 他席四个台账陈化（§三 最后一行）各自归票，不在本票写集。
