# t576 N1–N8合一票证据（运动39页读者可见的页级症状八条）

> 第一性原理：地图156目的地＝39条唤醒词跑得通＋真HTML＋人已签字通过；本票只修读者看得见的页级症状，不碰口径与数据。
> 用词照 `docs/agents/wording.md`（命令／跑得通／页面交付／help模板／实施）。

## 一、写集声明（9件）与基线

基线：`410d79a`（冻于开工当刻；四源码件 SHA 与 LF 见下，LF 与台账逐字一致）。

| # | 件 | 动作 | LF（改前→改后） |
|---|---|---|---|
| 1 | `packages/skill-calorie/src/render/sportDocs.ts` | 改（N1／N2／N3／N4／N5／N6） | 434→434 |
| 2 | `packages/skill-calorie/src/render/sportPortDocs.ts` | 改（N1／N4／N5／N8） | 944→944 |
| 3 | `packages/skill-calorie/src/exercise/records.ts` | 改（N1） | 381→381 |
| 4 | `packages/skill-calorie/src/exercise/receipt.ts` | 改（N7） | 439→439 |
| 5 | `packages/skill-calorie/test/t576-n1n8.test.mjs` | 新建（N1–N8 八条判据） | — |
| 6 | `packages/skill-calorie/test/exercise-accept-267.test.mjs` | 改（N8 眉标两行同步） | 测试件，不进台账 |
| 7 | `packages/skill-calorie/test/exercise-dist-strength-cardio-fusion-453.test.mjs` | 改（N8 眉标常量同步） | 测试件，不进台账 |
| 8 | `packages/skill-calorie/test/exercise-port-111.test.mjs` | 改（N8 步速单位同步 `4.8 分/km`） | 测试件，不进台账 |
| 9 | `docs/skills/skill-calorie/t576-证据.md` | 新建（本件） | 文档，不进台账 |

草稿（不入仓）：`.scratch/t576/`（`build.log`／`t576.log`／`regress1.log`／`regress2.log`／`mut.mjs`／`mut2.log`／`out/`）。
一行未碰：`packages/base-render/**`（共用层）、`packages/skill-calorie/AGENTS.md`（四件 LF 零漂移，无需同步台账；且该件他席在途 M，本票不争）、别人的票号目录。
`git` 只 `add` 上表 9 件；不切分支、不 `reset`／`stash`／`clean`／`restore .`、不 `--amend`／`rebase`／`force-push`、不 `push`（推送归总工程师）。

## 二、影响清单（必报五步第一步，先报再动）

- `render/sportDocs.ts`（汇总页装配）：N1 读数卡／分布块补可见 `h2`；N2 跨年轴印全年；N3 稀疏窗圆点显式讨要；N4 逐日导航与表题同为「按日消耗」；N5 卡值取整统一；N6 截断说明移到表前。理由：N1–N6 的落点页（14–22／25–30）全走本件装配。
- `render/sportPortDocs.ts`（分布／力量／有氧／趋势页装配）：N1 五处读数卡／分布块补 `head`；N4 两处图表导航与卡题同字；N5 `卡` 进取整集＋图表入参取整；N8 眉标对齐 H1＋距离统一 `km`＋趋势卡题去「两套刻度」。理由：N1／N4／N5／N8 的落点页（31–34／37–39）全走本件装配。
- `exercise/records.ts`（记录级明细页装配）：N1 指标卡补可见 `h2`。理由：N1 落点含 14／15／25–27（本件读数卡 `sec-figures`）。
- `exercise/receipt.ts`（写后回执页装配）：N7 逐条明细卡包进可折叠件，与变更卡同层。理由：N7 落点 07／12／13 全走本件 `detailCard`。
- 三件旧测试＋一件新测试：N8 可见字改了，三处旧钉子同步新字；新测试钉住 N1–N8 八条。

## 三、结构设计（第二步）

不新建目录层级，不碰三个以上能力（只动运动族四件）。每件对外出口不变（`buildExerciseDoc`／`build*Doc`／`buildRecordsDoc`／`buildExerciseReceiptDoc`，均 ≤5 个）；`Card.head` 是 `sportPortDocs.ts` 既有字段（本票只填充，不加字段）；`sportDocs.ts`／`records.ts` 的 `h2` 走行内字符串（不加共用件、不抄第二份常量）；导航项仍由卡清单派生（N4 是把标签字改成与卡题同字，不是另立第二份清单）。共用件：无（`renderDisclosure`／`renderDataTable` 均为公共层既有件，只调用）。

## 四、超线报警（第四步，当场报警但本票不拆）

已超线，需要根据规则进行重构。四件全在 350 线之上（`sportDocs.ts` 434／`sportPortDocs.ts` 944／`records.ts` 381／`receipt.ts` 439，LF 改前改后零漂移，台账仍绿）。超因与拆法照包 `AGENTS.md` 台账既有行（`sportDocs.ts` 按页切 `sportSummaryDocs.ts`／`sportGoalDocs.ts`＋共件 `sportPageParts.ts`；`sportPortDocs.ts` 按页族切两件姊妹件＋共件；`records.ts` 按取数／版式切 `recordsView.ts`／`recordsDocs.ts`；`receipt.ts` 待归属票认领）。本次先不拆：结构重排不在本票写集（票面只许动四件的版面与文本），且四件正在返修窗内；待收口票认领。

## 五、逐条改法（只动可见文本与形状，取数口径一个字不动）

- **N1 读数卡补 h2**：`sportDocs.ts` 的 `sec-kpi`／`sec-dist` 正文首项加行内 `h2`（核心数字／类型消耗分布）；`sportPortDocs.ts` 的分布／力量／有氧／趋势四页 `sec-figures` 加 `head: '核心数字'`、分布 `sec-ratio` 加 `head: '分类占比'`（走既有 `shell()` 的 `section>h2`）；`records.ts` 的 `sec-figures` 加行内 `h2`（指标，本件沿用既有词「指标」，导航同源）。35–39 复盘族不动（已有三节题）。
- **N2 跨年轴印全年**：`sportDocs.ts` 折线入参 `label` 改跨年即全日期（`v.start` 与 `v.end` 年不同即 `d.date` 全字，否则 `slice(5)`）；同年页仍印 `MM-DD`。共用层图表件不动（日期轴复用入参标签，跟着正）。
- **N3 页19图表数据装配查空**：`sportDocs.ts` 折线 `options` 加显式 `showDots: true`（稀疏窗被步长吞掉的点显形；19 类 31 天两点窗 0→2 圆点）。不断 0 口径不动，序列仍全窗（含空）。
- **N4 导航与卡题同源**：`sportDocs.ts` 的 `sec-series` 标签 `逐日明细`→`按日消耗`（与表题同字）；`sportPortDocs.ts` 分布 `sec-chart` 标签 `分类图`→`按分类热量分布`、有氧 `sec-chart` 标签 `类型图`→`按类型次数`（与各自图表题同字）。导航仍由卡清单派生。
- **N5 合计值取整统一**：`sportDocs.ts` 的总消耗／日均／逐日消耗／按类型消耗／分布值／分类汇总全部 `fmtNum(x, 0)`；`sportPortDocs.ts` 的 `INT_UNITS` 加 `卡`（全部 `numUnit(*, '卡')` 取整）＋复盘类目值与三处图表入参取整。图表与表格同取整，不各说各话。
- **N6 截断说明移表题**：`sportDocs.ts` 的 `sec-series` 把 `sui-note` 截断句移到 `table` 之前（表题 `按日消耗` 不变，终审 P2-8 判据仍绿；读者先读到截断再滚 100 行）。
- **N7 逐条明细卡同层可折叠**：`receipt.ts` 的 `detailCard` 包进 `renderDisclosure({ title: '逐条明细', open: true })`（与 `changeCard` 同为可折叠层；空即整卡不出，判据不变）。
- **N8 眉标对齐 H1＋距离统一 km＋34 卡题单刻度说法**：力量眉标 `运动力量总览`→`力量训练总览`、有氧眉标 `运动有氧总览`→`有氧训练总览`（与各自 H1 逐字一致，同分布页先例）；有氧 `分/公里`→`分/km`、口径两行与总距离 `公里`→`km`（记录页早已是 `km`）；趋势卡题去「，两套刻度」（时长虚线走 `ownScale` 独立归一，不另出第二套刻度；两条序列与虚线实线判据不动）。

## 六、判据读数

- 编译：`node node_modules/typescript/bin/tsc -b packages/skill-calorie` exit 0（`build.log` 空错）。
- 靶向：`node --test t576-n1n8` **pass 8 fail 0**（`T576-N1 h2=7/7`／`T576-N3 circles=2`／`T576-N4 nav=3/3`／`T576-N5 int=3/3`／`T576-N6 note<table=1`／`T576-N7 disclosure=1`／`T576-N8 eyebrow=2/2 km=1 nok2=1`；N2 跨年含 `2025-09-16` 与 `2026-09-15`）。
- 回归甲（523／544／453／267／111）：pass 49 fail 3——3 红全在 `exercise-port-111` 的三处**票外既有**旧针（力量来源 `·` 旧式、 cardio 窗口 `~` 旧式、趋势 `＋` 旧式；本票 diff 未碰这三串，见 §八后说明），453／267 随 N8 同步后绿，523／544 全绿。
- 回归乙（452／451／423／454）：**pass 46 fail 0**（收据可折叠、导航改字、截断前移、取整四处无回归）。

## 七、变异自证（源码级，持锁内改坏→红、逐字节还原→绿）

```text
MUT-RED fail=1 exit=1 n1red=true
MUT-GREEN pass=8 fail=0 restored=true
```

变异点：`sportDocs.ts` 汇总读数卡 `h2`核心数字`` 摘掉（重现 N1）→ `t576-N1` 红；逐字节写回（sha 一致）→ 8/8 绿。脚本 `.scratch/t576/mut.mjs`，日志 `.scratch/t576/mut2.log`。

## 八、GATE-RUN 声明（窗口内经加锁包装器跑过的命令）

```text
GATE-RUN runId=05313b9b-37cb-468a-a591-ae589a3846d4 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=f591e6b6-31a3-4e97-99ed-5bd90a8dba71 cmd="node --test packages/skill-calorie/test/t576-n1n8.test.mjs"
GATE-RUN runId=5fd21c95-bbb3-4180-94d9-6b54a417ec81 cmd="node --test packages/skill-calorie/test/exercise-text-shape-523.test.mjs packages/skill-calorie/test/exercise-text-shape-544.test.mjs packages/skill-calorie/test/exercise-dist-strength-cardio-fusion-453.test.mjs packages/skill-calorie/test/exercise-accept-267.test.mjs packages/skill-calorie/test/exercise-port-111.test.mjs"
GATE-RUN runId=8562409c-6142-48fa-b5ab-cae55b545dcb cmd="node --test packages/skill-calorie/test/exercise-summary-goal-fusion-452.test.mjs packages/skill-calorie/test/exercise-records-fusion-451.test.mjs packages/skill-calorie/test/exercise-receipt-fusion-423.test.mjs packages/skill-calorie/test/exercise-trend-recap-fusion-454.test.mjs"
GATE-RUN runId=eb8c8039-6b43-410c-af9f-9bd3064fe234 cmd="node .scratch/t576/mut.mjs"
GATE-RUN runId=t576r-add cmd="git add"
GATE-RUN runId=t576r-diff cmd="git diff --cached --name-only"
GATE-RUN runId=t576r-commit cmd="git commit"
```

复核：`node tooling/run-locked.mjs --ticket 576 -- node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t576-证据.md --ticket 576`（末两条先定号后跑，落盘时已在上表声明）。
放宽：回归甲 exit 1 系三处票外既有旧针（`--allow-nonzero`，理由见 §六；本票不断言 port-111 全绿）。

## 九、交付对账（第五步）与未做项

写集 9 件逐行有落（§一）；第一步清单与交付偏差 0。判据读数见 §六，变异两行见 §七。

未做项：① 共用层图表件刻度格式不动（N2 只在页侧入参标签改跨年全年，不碰 `base-render`）；② `AGENTS.md` 台账不动（四件 LF 零漂移，门仍绿；且他席在途 M）；③ `port-111` 三处旧针未改（来源 `·` 式／窗口 `~` 式／`＋` 式，归属 #111／#453／#475 的旧账，本票不扩面，已当场报警）；④ 推送归总工程师，本票不 `push`。
