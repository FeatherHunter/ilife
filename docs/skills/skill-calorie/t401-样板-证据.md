# #401 样板实施证据 · 「看今日主页」这一页

判定：**完成**。这页按「文本纪律 ＋ 视觉升级」两半改到位，机器读数与实拍都在下面。
产物跑真出口（`dist/cli/cmd_read.js`，固定种子库 `SEED_TODAY=2026-09-07`），落在 `.scratch/t401c/`。

写集（只动这些）：`packages/skill-calorie/src/home/homeDocs.ts`（版面）、`src/home/home.ts`（窗口天数带上页）、
`test/visible-text-probe.mjs`（新，抽词与四类机器话判据）、`test/t401c-页面机器话探针.test.mjs`（新，门禁）、
`test/home-lock-374.test.mjs`（只改一处针位，理由见末节）。**公共层一行没动**（只读）。

---

## 一、文本纪律（照审计工单逐条落）

| 工单条目 | 改前（`.scratch/audit-text/report.md` 的读数） | 改后 |
|---|---|---|
| 删内部标识符上屏 | 眉标 `calorie.view.home · 主页`；表头 `TDEE`；缺口卡说明 `正=缺口（TDEE＋运动－摄入）`；来源段 `food_log＋exercise_log＋daily_goal` | 眉标 `卡路里 · 主页`；表头 `摄入／缺口／目标`（单位写在表格 caption「按日汇总（单位：卡）」）；缺口卡 `摄入比消耗少这么多，正数是缺口`；来源名 `饮食记录 ＋ 运动记录 ＋ 每日目标` |
| 口径句改造 | 一整段「口径：…」压在 caption 里（`按日汇总（2026-09-01 ~ 2026-09-07，无记录日留空，不断 0）`），实现细节与工程话混在句里 | 走仓内已定型的**口径行** `renderCaliberLine`（`blocks.ts:260-262`，样式 `:1222-1227`）：折线一块一行、按日表一块一行；只留「留空不断 0」「缺口＝消耗 − 摄入，正数是缺口」「缺数写 —」这三条读者要的事实 |
| 去冗余 | 窗口区间串一页重复 3 次（页头、周均卡、表 caption）；结论长行与 KPI 卡重复摄入／目标两个数 | 窗口区间**只留页头一处**；结论句只给判定 ＋「还能吃／已超」一个读数，算式不再上屏 |
| 页名与内容对齐 | 四页（今日／本周／本月／连续记录天数）都印「今日总览 2026-09-07」（`named-cases.md` 点名「页名与内容不符」） | 页名按**本唤醒词的窗口**取词：`今日总览`（1 天）／`近 7 天总览`／`近 30 天总览`；窗口是「到今日为止的 N 天」不是日历周月，故不写会与日历口径打架的「本周／本月」 |

## 二、视觉升级（只用已冻结 token 与已支持 API）

| 项 | 做了什么 | 依据 |
|---|---|---|
| KPI 卡状态徽章 | 今日摄入／蛋白／饮水三张卡按完成度给 `good／ok／warn／bad` 四档：≥90 `ok`「达标」、≥60 `warn`「接近目标」、else `danger`「偏少」；缺目标**不给徽章**（摆一枚「无数据」是假信息） | 能力位 `blocks.ts:533-539`；四组实色对 `spec/style.ts:312-321` ＋ `style.ts:548-583`；先例 `render/sportDocs.ts:282` |
| 区块标题带图标 | `🔥 今日速览`／`📈 每日摄入`／`📊 按日汇总（单位：卡）`／`💰 数据与日志`，四个区块各一个，不滥用 | 老实物每块都带（`home_dashboard.html:623-649` 的 🍽️🏃⚖️🎯🔥💰📅） |
| 结论条专属色面 | 结论句从「普通副题灰字」改成专属浅色面：底 `var(--soft)` ＋ 字 `var(--blue2)` ＋ 圆角 14px（闭集中档） | 老实物 `.view-summary`（`home_dashboard.html:568-571` 浅蓝底＋主色字）；色只引 11 个冻结 token，圆角在 `{8,14,20,999,50%}` 里 |
| 页内导航胶囊排 | 从无到有，走公共层 `renderTocBlock`（白底胶囊＋`--line` 描边），锚点清单与正文四个 `id` 同源 | `blocks.ts:242-256` ＋ 样式 `:1200-1216`；`t425` 裁定 3 |
| KPI 值字号 | 页面不再自写任何 `font-size`：整页走公共层产出器，值 22px／`tnum`／`align-items:baseline` 全在 `blocks.ts:1384-1403` | `shared/sourceLine.ts:8-9`「本件不写色、不写字号」；`blocks.ts:1392-1398`（28px→22px 的改小史） |

**没做的两项，以及为什么**：

1. **KPI 卡完成度条**：`t425` 裁定 6 明写条位归 **#418**（公共层增量），「条位与档位色**只准有一处**，
   页面里一行色值都不写」。故**不卡在 #418 上**——本页用徽章 ＋ 完成度百分比顶上，`--soft`／`--blue2`
   那处浅色面是结论条（页面级单件），不是进度条。**本条不构成阻塞**。
2. **表格斑马纹 ／ 数值列等宽栈 ／ 列表行三槽 grid**：这三项的报告落点写的是**公共层**
   （`dataTable` 区／`listRows` 区），改它要动 `packages/base-render/**` —— 与本票「不碰公共层」的纪律、
   与「公共层增量属 #418 一类票」的既有分工都冲突，故**不在本页私改**。本页只做了表能做的两件：
   列数 5→4（去掉 `TDEE` 列）、行序改成最近在上、末行无下边框（公共层既有）。

## 三、机器读数（逐条给数）

跑法：`node .scratch/t401c/run.mjs`（真出口重出）→ `node .scratch/t401c/measure.mjs`（读数）。

### 1. 可见文本零机器话（四类）

| 类别 | 看今日主页 | 看本周主页 | 看本月主页 |
|---|---|---|---|
| A1 全大写常量（`MEAL_WINDOWS`／`TDEE` 一类） | **0**（唯一命中 `AI` 是复制菜单老仓原话「粘贴给 AI / 自己看」里的词，不是源码标识符，判据里按词表放过） | 0 | 0 |
| A2 snake_case（库表名那种写法） | **0** | 0 | 0 |
| A3 内部命令键（`calorie.` 开头） | **0** | 0 | 0 |
| A5 库名／表名／文件名（`*.db`／`*.ts` 一类） | **0** | 0 | 0 |

口径：判据跑在**可见文本**上（剔除 `head`／`style`／`script`／注释与**全部标签属性**），
并**有意**放过复制载荷里的技术原文（复制日志第 3 段是「照抄可重跑」的命令原文，里面就是带命令键的；
`t452` 先例 `exercise-summary-goal-fusion-452.test.mjs:186-190` 同款口径）。复制载荷**确实在场**
（探针正证 `html.includes('calorie.view.home')`）。

### 2. 窗口区间串次数

| 页 | 串 | 次数（**自设上限 1**） |
|---|---|---|
| 看今日主页 | `2026-09-01 ~ 2026-09-07` | **1** |
| 看本周主页 | `2026-09-01 ~ 2026-09-07` | **1** |
| 看本月主页 | `2026-08-09 ~ 2026-09-07` | **1** |

（改前审计读数：同页 3 次。）

### 3. 长行（≥50 字符的整行）

**实得 1 条**（上限 2）：

```
[59] 数据来源 · 饮食记录 ＋ 运动记录 ＋ 每日目标 · 2026-09-01 → 2026-09-07 · 共 5 条
```

这条是**来源脚注**（`t425` 裁定 3 的恒出三条之一，形状逐字照 `shared/sourceLine.ts`，
三个字段一个都省不掉）。改前审计读数：本页 1 条（67 字符的结论句，已拆）；本页口径墙 3 句/152 字符（已撤）。

### 4. 变异自证（两行）

```
红：node --test <探针副本，把「干净产物必绿」那行改成「必红」>  →  ✖ AssertionError: 原始产物不该命中 snake_case（实得 loss_log）
绿：node --test packages/skill-calorie/test/t401c-页面机器话探针.test.mjs  →  ℹ pass 5  ℹ fail 0
```

做法：往**已渲染产物**的可见文本里塞一个内部标识符（表头「缺口」→「缺口loss_log」）→ 探针红；
拿掉（原产物）→ 绿。（走产物不走源码：源码里的 `'…'` 引号会被「丢标签」那一步当属性吃掉，拿源码演示会得到假绿。）

### 5. 探针守什么

新入仓 `packages/skill-calorie/test/visible-text-probe.mjs`（抽词 ＋ 四类判据）与
`test/t401c-页面机器话探针.test.mjs`（门禁，5 例）。它守四条**已成文**的规矩，不是自设美观标准：

1. 可见文本零机器话（`t425-融合基准.md:129-131` 裁定 1；先例 `exercise-summary-goal-fusion-452.test.mjs:196-198`）；
2. 窗口区间串一页一次（审计工单）；
3. 长行 ≤2（审计工单）；
4. 视觉升级四件在场：徽章（且文案不是控件缺省值「成功／警告／失败」）／自产带图标标题／
   胶囊导航（锚点都指得到 `id`）／结论条浅色面（只引冻结 token，零色值字面量）。

## 四、实拍

| 文件 | 宽 | 看了什么 |
|---|---|---|
| `.scratch/t401c/shot-1000.png` | 1000 | 结论条有专属浅蓝面、KPI 三张有档位徽章、导航是四个胶囊、四个区块标题都带图标、表已放得下四列 |
| `.scratch/t401c/shot-390.png` | 512 | 窄版：KPI 落 2 列、导航仍在一行、表与徽章都不溢出（见下方环境限制） |
| `.scratch/t401c/shot-month-top.png` | 1000 | 月窗页头（页名随窗口变） |

**环境限制（如实记）**：本机 Chrome 无头模式下 `window.innerWidth` 有下限——
传 `--window-size=390` 时 `innerWidth` 实为 **512**，`clientWidth=scrollWidth=497` 且**零元素溢出**
（`.scratch/t401c/overflow.mjs` 实测：`OVERN=0`）。故「390」这张实际是 512 宽的窄版，
真正的 390 断点（`≤640px` 那三条既有规则）没有在本机跑到；这条不靠猜，如实记在这里。

## 五、回归

| 命令 | 结果 |
|---|---|
| `node --test packages/skill-calorie/test/render-t8.test.mjs` | **7 / 0**（全绿；7 例逐条 ✔） |
| `node --test packages/skill-calorie/test/home-lock-374.test.mjs` | **8 / 1**（9 例） |
| `node --test packages/skill-calorie/test/t401c-页面机器话探针.test.mjs` | **5 / 0**（本例新加） |
| `node packages/skill-calorie/scripts/check-warning-line.mjs` | `RESULT: 58/58` `PASS` |

`home-lock-374` 的那 1 例红是 **第 4 例「看今日体重概览」**，红在**空库一档**：
`AssertionError: 看今日体重概览 空库 exit 4（stderr：）` —— 空的 stderr 说明这一条根本没跑到本票的写集上
（本票只改 `src/home/**` 与 `src/home/home.ts`，体重页走 `src/weight/**`，两边没有共用改动）。
如实记：这例是**别家在途改动**带来的红，**没有为了让门变绿而改测试**。同一批 5 例「看 X 主页」的红
（`页上无位：今日总览`）是**本票改名的直接后果**，处理见下节。

## 六、为落本票而动的唯一一处测试（有理由、有交代）

`test/home-lock-374.test.mjs` 的针位表首针 `'今日总览'` → `'总览'`（其余针一字未动）。

理由：那根针原本钉的是「h1 里那个整串」，而 `#401c` 照审计工单把页名改成**如实反映本唤醒词的窗口**
（今日／近 7 天／近 30 天）——同一命令键（`calorie.view.home`）带不同 `windowDays` 不再四页共用一个
「今日总览」。「窗」这个字是这五条唤醒词（看今日／本周／本月主页、连续记录天数、今日热量预算）共有的
那一截，故取「**总览**」二字；针的**本意**（h1 位有内容且是这一族的话）一字未损。理由已写进该件件头。

## 七、提交与阻塞

- 阻塞：**无**。KPI 完成度条按 `t425` 裁定 6 归 #418，本页不依赖它也不私自顶它的位（用徽章 ＋ 百分比顶着），
  故本页**不卡 #418**。
- 红线：`packages/skill-calorie/src/home/homeDocs.ts` LF＝**314**（<350，未触发第四步）；
  `src/home/home.ts`＝99；`src/home/today.ts`＝110（本票未动）。
