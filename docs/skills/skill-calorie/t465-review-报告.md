# #465 独立对抗审查报告（运动族既有红清零 · 两张旧判据随融合版式更新）

**判定：PASS**（无 S1；五维 27／24／17／13／8 ＝ **89／100**，均分 89 ≥ 85）

审查对象：`a7872d2`（`test/exercise-port-111.test.mjs` ＋68／−9、`test/exercise-routes-265.test.mjs` ＋38／−11、证据 `docs/skills/skill-calorie/t465-既有红清零.md` 97 行、台账 `packages/skill-calorie/AGENTS.md` 1 行）＋ `e0034c4`（台账 2 行）。
被审三件工作树与 `HEAD` **逐字节同源**（port-111 `sha256=9975B488…`／routes-265 `B091E946…`／证据件 `1E3C948A…`，`git diff --quiet HEAD` 皆 0）——§一 的读数对的就是提交字节。
本席只读：被审三件、`gh issue view 465`、`src/render/sportPortDocs.ts`（**零写入**，该件上有他席在飞的拆件活）、`scene-04-exercise.ts`、台账、`.scratch/t465/`；未读全仓。自设探针 `docs/skills/skill-calorie/t465-review-probe.mjs`（**不 import 被审判据件**，全部断言本席重写、自跑、自数）。

## 一、机器证据（全部走 `node tooling/run-locked.mjs --ticket 465 -- …`）

| 门 | runId | 读数 |
|---|---|---|
| `pnpm build` | `f48250bf-54ef-4e6f-9eb4-130413d84668` | exit 0 |
| 旧判据 port-111 | `343a20eb-abd7-4552-8b5e-b6149fae46f2` | exit 0 · **13/0** |
| 旧判据 routes-265 | `b14ce548-ef78-48a7-a007-4f289731cd41` | exit 0 · **3/0** |
| 族判据 423 | `340fce43-fcfa-4510-bc39-ab583cb47d8d` | exit 0 · **20/0** |
| 族判据 451 | `18e237ea-f3ef-4312-9700-667884121371` | exit 0 · **8/0** |
| 族判据 452 | `e540243b-e36d-4d53-a5f9-3a3e5661b570` | exit 0 · **9/0** |
| 族判据 453 | `d4b71227-5931-4c42-9482-61792f348a67` | exit 0 · **5/0** |
| 族判据 454 | `020d2745-3fdb-4770-92f0-3c3e717e80c9` | exit 0 · **9/0** |
| 告警线脚本 | `6207418f-4fc6-4b3b-8409-c261beae9de7` | exit 0 · `RESULT: 58/58` · `PASS: 告警线台账齐全且与实况一致` |
| 事实面探针（本席自设） | `bfd12043-0601-4034-97e4-23b35195500e` | exit 0，读数见 §二·事实面 |
| 自造反例七行（隔离镜像） | `48187e3f-1964-46b2-af74-a9b53941c522` | exit 0，读数见 §二·反例表 |

**八门读数与证据件 §5 GATE-RUN 表逐值相同**（`13/0`／`3/0`／`20/0`／`8/0`／`9/0`／`5/0`／`9/0`／`58/58`）⇒ 复现成立。

## 二、一号探针：判据跟上新形状，还是放宽把红改绿

**结论：以「判据跟上新形状」为主，局部放宽一处（②③④⑤ 四条版式针丢了邻接性／块名逐字），未发现任何断言被删、被改到恒绿、或把已坏实现判绿。**

### （甲）新旧断言逐条对照

| 处 | 旧断言 | 新断言 | 依据判 | 判 |
|---|---|---|---|---|
| ① 唤醒词两词 | `['看运动记录（按力量筛选）','calorie.view.exercise-strength']` | 同词改认 `…exercise-records` | #342 `5d159c0` 的有意改动 | **跟上**：本席三方同指实测——`routesFor` 两词皆指 `exercise-records`、冻结表 `main_prompt.cli` 同指、`CALORIE_COMBOS` 该键在册（探针 `WAKE` 行） |
| ②③ 力量／有氧题窗 | `'力量训练总览 2026-09-05 ~ 2026-09-07'`（连写） | 拆成 `'力量训练总览'`＋`'2026-09-05 ~ 2026-09-07'` | #453 融合版式 | **跟上但放宽**：旧针当刻**命中 0**（实测），红因属实；但拆针丢掉邻接判据（见 C3） |
| ④ 分布题窗＋块名 | `'运动类型分布 <窗>'`、`'按分类次数占比'` | 两针拆写、`'按分类次数占比'`→`'分类占比'` | #453 | 同上（旧两针命中 0／0，实测） |
| ⑤ 复盘块名 | `'高频 TOP5'` | `'高频运动'` | #454 改名 | **跟上但放宽**：旧针命中 0；新针靠页内副题也能过（见 C4） |
| ⑥ 复制头 | 载荷含 `【calorie · calorie.view.exercise-trend】` | 载荷面 `【calorie · 运动趋势】`＋json `key` 认人话页名＋全份产物零 `calorie.view.` | #465 口径 | **跟上且更严**：本席自数六页产物，五页全文 `calorie.view.` **0** 处、载荷头皆`【calorie · <人话页名>】`——票面「或按你核实的事实写成载荷面断言」的事实成立（C1） |
| ⑦ routes-265 页身份 | `r.file.includes(c.key)`（要产物含命令） | 眉标逐字相等＋题面含页名＋产物零 `calorie.view.`／零「移植」＋载荷头认人话页名 | #465 口径 | **跟上且更严**：不再要命令，改为两处人话＋一条禁用（C1／C2） |

### （乙）自造反例七行（隔离镜像＝真件字节同源 280 件、字节不同源 0；**真件零写入**）

| 号 | 改坏 | 实测 | 判 |
|---|---|---|---|
| C1 | 趋势页载荷页名改回命令（`pay-key`） | port-111 **11/2**：`复制载荷头不是「技能 · 页名」形态："【calorie · calorie.view.exercise-trend】…`／`产物任意一面出现命令键 calorie.view.*`；routes-265 **2/1**：`看运动趋势 产物里出现命令键 calorie.view.*` | **必红**；两条红分属载荷面与全份产物两句，`可见面出现命令键` 那句**未出现**（可见面本身未被这次改坏污染） |
| C2 | 趋势页可见面塞命令（`vis-key`） | port-111 **12/1**，只有一条红：`可见面出现命令键：calorie.view.`；载荷面那条 **PASS** | **必红**；两面真分开、各打各的 |
| C3 | 分布页题面改回「页名＋窗口连写」（`dist-concat`） | port-111 **13/0**、routes-265 **3/0** | **不红** ⇒ 拆针抓不住形状回退（D1） |
| C4 | 复盘页块名改回「高频 TOP5」（`recap-old`） | **13/0 · 3/0** | **不红** ⇒ 新针在页内副题里也命中（D1） |
| C5 | 力量页来源写法换成人话来源名（`src-word`） | **13/0 · 3/0** | **不红** ⇒ 无「来源写法」统一口径断言；与证据件 §6.2 已登记转票**相符**（非隐瞒） |
| C6 | 正面对照：分布页把窗口从全份产物摘掉（`dist-win-off`） | port-111 **12/1**：`distribution 缺：2026-09-05 ~ 2026-09-07` | **必红** ⇒ 拆针不是恒绿，仍抓得住「窗口整块没了」 |
| C7 | 按另存原字节写回＋**刷 mtime** → 重编 | **13/0 · 3/0** | 改回必绿 |

**事实面（探针自数六页）**：五页全文 `calorie.view.`／`calorie.exercise.` 各 0；可见面命令 0、票号 0、「移植」0；`exercise_log` 可见面各 1（§6.2 登记属实）；载荷头与 json `key` 皆人话页名。

复跑法：`node tooling/run-locked.mjs --ticket 465 -- node docs/skills/skill-calorie/t465-review-probe.mjs batch`（自带镜像搭设／逐条变异／按原字节还原，真件零写入；日志落 `.scratch/t465-review/logs/`）。

## 三、另查四件事

1. **复跑门禁**：§一 八门逐值复现证据件读数（runId 齐）。两张旧判据件当刻 `13/0`、`3/0`。
2. **三句口径断言**：三句各一条，且**各自的面写在测试名／注释里**——载荷面（测试名带「（防漂移 · 载荷面）」）／可见面（测试名「#111 可见面零工程话」＋注释「可见面＝剥掉样式／脚本与全部标签后的纯文本」）／全份产物（注释「全份产物（含载荷）也不许有命令键」）。C1／C2 证明三句各打各的面。**但**测试名说「整份产物零命令键／票号／工序词」，整份产物实际只判 `calorie.view.` 一类（另两类只判可见面）→ D4。
3. **提交边界**：`--name-only` 逐笔只四件（证据件／台账／两张判据件），**`sportPortDocs.ts` 零字节**、未进任何一笔；两笔都不含公共层与他席件。台账那行 `src/weight/review.ts 602→600` 属他席在飞件的陈化回绿，已在证据件 §5 登记。
4. **事故登记**：证据件 §4 如实记了（a）真件变异窗与别票写者相撞 → 复算哈希不符 → **中止未覆盖** → 按进场备份逐字节还原（`RESTORED-SHA=2CEC26DA…`＝HEAD 版）；（b）T351-v7 席在同一判据件上的改动被**随本提交带入**（§6.1，写明「非我方改写」）。两件都在、未抹；且（a）有独立旁证——同图 #453 审查报告 §四自述读到「#465 席变异态 `C3E9C2C5…`」，两席所记哈希同值。

## 四、缺陷段

- **S1：无**（不改判）。
- **D1（本票范围，S2）**：②③④⑤ 四条版式针在自造反例 C3／C4 下**不红**——「题面与窗口连写」「块名逐字」两处判据随拆针丢失；证据件 §2 给的是静态「命中 0／1 处」依据，没有本票目标 1 要求的「改坏必红」读数。定性＝**跟上为主、局部放宽**（C6 证明拆针仍抓得住窗口整块消失，故不是把红改绿）。
- **D2（本票范围，S3）**：证据件 §2 的依据读数不可复现——本席自数：力量页 `力量训练总览` 全文 4／可见面 2 处、窗口串 **2** 处（KPI 卡 detail ＋ 动作表 caption）；分布页窗口串 **1** 处，来源是**明细表 caption**。「窗口挪进『窗口』卡」对分布页不成立：窗口卡是 `renderParamForm` 的「开始／结束」两格，不产 `A ~ B` 这个串。
- **D3（本票引入，S3）**：随提交带入的「会话」禁用断言只钉复盘页；**同族分布页**当刻可见面仍有「会话」**1** 处（KPI 标签，实测），与它引用的裁定「页面文案一律不出现」范围不一致。
- **D4（本票范围，S3）**：测试名与断言内容不符（整份产物只判 `calorie.view.`，票号／「移植」只判可见面）。
- **D5（本票范围，S3）**：`e0034c4` 提交信息点名 `fetch/body.ts 406`——该笔未动该行（当刻＝426），406 是 `reportPlate.ts` 的值。
- **D6（范围外，注记不判）**：族第 6 页（计划复盘，他席在飞）产物**载荷面**仍有 `calorie.view.` **2** 处，含疑似拼接瑕疵 `calorie.calorie.view.exercise-review`；本票三句口径只钉 5 页，族级口径未收口。
- **D7（范围外，注记不判）**：可见面零工程话只在趋势页判；routes-265 只覆盖分布／趋势／复盘三页且不含票号一类。

## 五、五维分（30／25／20／15／10）

| 维 | 分 | 依据 |
|---|---|---|
| 契约一致 | 27／30 | 目标 1–5 逐条有落点，两张判据件 `13/0`／`3/0`、五族判据全绿、告警线 58/58；扣 3＝D1（②③④⑤ 无「改坏必红」读数、且 C3／C4 下不红） |
| 证据真实可复现 | 24／25 | 八门 runId＋读数本席逐值复现；载荷事实、旧针 0 命中皆复现；扣 1＝D2 依据读数不可复现 |
| 新旧对照 | 17／20 | 旧→新逐条摆开、旧针当刻 0 命中（非凭空改）；扣 3＝D1 局部放宽＋D2 读数不实 |
| 工程红线 | 13／15 | 只含声明件、`sportPortDocs.ts` 零字节、还原走另存原字节；扣 2＝真件变异窗与他席写者真撞过（已如实登记，但确有覆盖他席未落盘内容的风险） |
| 文档同步 | 8／10 | 证据件 97 行覆盖 7 条定性／对照／变异／GATE-RUN／未做项；扣 2＝D2 读数＋D5 提交信息与改动不符 |

**无 S1，均分 89 ≥ 85 → PASS。** 遗留：D1／D2／D4 建议由 #454 收口票或本图收口票按「每条版式针配一条改坏必红读数」补正；D3／D6 属族级口径收口（分布页「会话」与他席复盘页载荷命令）转票处理。
