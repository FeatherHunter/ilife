# #702 执行状态（第一席，2026-09-18）

票：`测试面改成打在接口上：页面走真正的交付出口、算式走能力门`（wayfinder:task／map #699）。
本件记**做到哪、判据读数、以及为什么有一条做不下去**——数字只住这里，票面不重抄。

## 一 · 交付（两个提交）

| 提交 | 内容 |
| --- | --- |
| `5ee99c94` | 结构门 `scripts/check-one-path.mjs`（同一符号不许从两个 `dist` 路径被取）＋ 修掉当刻唯一红条 ＋ 开工读数件 |
| `079e8faf` | 页面断言门 `scripts/check-page-assert.mjs`（spawn 真出口又断成功的件必须断信封）＋ 包内「测试面口径」三条 |

改动件：`packages/skill-calorie/scripts/check-one-path.mjs`（新，200 LF）、
`packages/skill-calorie/scripts/check-page-assert.mjs`（新，104 LF）、
`packages/skill-calorie/test/t255-单一来源.test.mjs`（一行 import 换缝）、
`packages/skill-calorie/AGENTS.md`（新增「测试面口径」一节）、`docs/skills/skill-calorie/t702-开工读数.md`（新）。

## 二 · 机器读数（真假两态都跑过）

| 门 | 自证（改坏必红／改回必绿） | 对当刻树 | 运行标识 |
| --- | --- | --- | --- |
| `check-one-path.mjs` | `--selftest` **3/3 PASS**（薄转出声明在⇒绿／没有声明⇒红／包门配对⇒绿） | 先红 1 条（`ALL_ROUTES`），修后 **PASS** | `5ee99c94` 那次提交前的本地运行 |
| `check-page-assert.mjs` | `--selftest` **5/5 PASS**（只断 exit 码⇒红／断成功且解析 stdout⇒绿／直调 dispatch⇒不进判／只断阻断⇒不判／spawn 别的脚本⇒不进判） | **PASS**（0 红） | `079e8faf` 同上 |

结构门那条红的实况：`ALL_ROUTES` 被两条测试路径取——`triggers/routes.generated.js`（正本，生成物）与
`triggers/routing.js`（薄转出，`src/triggers/routing.ts:35,137` 逐字）。改法＝把
`test/t255-单一来源.test.mjs:23` 的 import 换到正本件；取值语句一字未动，断言覆盖一条不减。

## 三 · 票面第 1 条为什么不成立（当刻实测，不是没做）

票面第 1 条写「算式断言改走能力门（十个能力门只有 8 件测试碰、6 件真调）」。**那条路今天不存在**：

1. **#703 已 close**（`8a67c572`／`36005389`），把 10 个能力门上的 **19 个 `run*` 全删了**——门今天只剩
   `*_COMMANDS`（＋ weight 门 6 件、workout 门 5 件共 14 个非声明对外名）。没有 `run*` 可 import，
   「8 件 / 6 件」这两个读数随之作废。
2. **门上没有算式**：`src/analysis/index.ts:12-14` 逐字写明算式住 `series.ts`／`trend.ts`／`deficit.ts` 等
   **不出目录**的内部件，「由 `render/` 侧的视图层直接消费，导入面照旧」。
3. **换缝的可达面很小**：深路径语句 **286 条 / 122 件**里，包门（`dist/index.js` 的 63 个值名）**够得着的只有 18 条**；
   剩下的 268 条要么落在实现件上（改不动，改了就是把单测重写成集成测），要么落在 `render/index.js`
   这种**搬迁期 barrel**（#703 已登记待退役）上。
4. **真出口那一半本来就不薄**：201 件里 **132 件**已经在 spawn 真 CLI；「已 spawn 真 CLI 却仍带深路径」的
   只有 **76 件 / 187 条**，而那些深路径多数是**夹具**（造库、造快照），不是断言落点。
   页面断言要补的那一件（第 2 条）已由 `check-page-assert.mjs` 落成机器门，当刻树 0 红。

结论：第 1 条的**可执行量**远小于票面假设。真正的换缝主活（把 `render/*` 那 51 个名字从 barrel 换成
能力门）**要等搬迁票（#704／#714／#715／#716）定下落点**——现在换，等搬迁落地还得再换一次。

## 三之二 · 复核第二轮（2026-09-18 23:15 当刻实测）：症结在门的宽度，不在测试的结构

把「测试台面上还有哪些没打真出口、没打门的缝」量到底之后，结论变了一个形状：

| 读数 | 当刻 | 说明 |
| --- | --- | --- |
| 测试台面上的**独立深路径** | **115 条** | 每条对应一个 `dist/**` 具体件 |
| 其中**整条路径的名字都能从包门拿到** | **5 条** | `analysis/trend`／`exercise/exerciseStore`／`fetch/shapes`／`paths`／`schema` |
| 包门（63 个值名）**部分够得着**的路径 | **4 条**（`fetch/body` 7/22、`analysis/series` 1/8、`fetch/diet` 1/3、`workout/planStore` 1/13） | 换到包门要拆开同名条目，收益 24 条语句 |
| 「只碰内部件、无包门、无能力门、不 spawn」的件 | **29 件 / 87 条** | 这才是票面第 1 条的真正目标面（占深路径语句 30%） |
| 该 29 件的实际内容 | 算法正本单测（`analysis/simulate.js`／`analysis/utils.js`／`analysis/cross.js`…）＋ 棘轮件（`legacy-ratchet-295`／`legacy-partition-313` 本就是钉老件的）＋ 门面单测（`t255-单一来源` 钉 `scripts/build-help.mjs`） | **没有一件是「页面断言打错缝」** |
| 被取最多的路径 | `triggers/routing.js` 20 个取点（16 件已 spawn）／`cli/keys.js` 19（18 已 spawn）／`cli/cmd_read.js` 18（18 全 spawn）／`fetch/body.js` 14 | **取最多的是「声明面与出口面」**，且这些件已经在真出口上 |

**第一性原理的诊断**：深路径读数由**包门面的宽度**决定，不由测试的结构决定。286 条深路径里包门够得着
18 条（6.3%），115 条目标路径里整条够得着 5 条（4.3%）——这不是测试打错缝，是**门太窄**。
而取点最密的几条（`routing`／`cli/keys`／`fetch/body`）**正是生产自己要用的声明面与取数面**，
它们今天没有对外路径可走，于是测试只能走深路径。

**因此第 1 条的正确处置**（不是「不做」，是**换归属**）：

1. 「页面断言走真出口」这一半**已经成立**——132／201 件在 spawn 真 CLI，且第 2 条的机器门
   `check-page-assert.mjs` 当刻 0 红；这一半不要再动。
2. 「算式断言走能力门」这一半**改成源侧票**：把生产自己要用的那几面（`CALORIE_COMBOS`／
   `isCalorieWriteKey`／`ENVELOPE_VERSION`／`routesFor`／`TRIGGERS` 一族声明面）**开出对外路径**，
   测试与被生产消费的件一起改过去——这是**加门**，与 #703 票面「只许收窄」的红线直接冲突，
   必须由那侧裁定，不能由测试票擅自加。
3. 剩下的 29 件算法正本单测**不动**：它们是 `AGENTS.md`「测试面口径」第 3 条那类，件头补一句
   「这是算法正本的单元测试，不是交付面验收」即达标；硬改成走 `dispatch` 是把单测重写成集成测，属降级。

**落地建议（本席不改代码，只报事实）**：给 map 的「Not yet specified」加一条——「生产自己要用的声明面
（`cli/keys` 的 `CALORIE_COMBOS` 一族、`triggers/routing` 的 `routesFor`）要不要开门、开成什么形状」，
它同时挡着 #703 的「只许收窄」与 #702 的「算式走门」两句话。

## 四 · 为什么 `test/` 这一片当刻不能写（并发纪律）

`packages/skill-calorie/test/` 当刻是**别席在途的写集**：124 件未提交（`+649/−634`），内容＝测试隔离从
`SKILLS_DB_PATH` 改到 `ILIFE_CONFIG_DIR`，加新件 `test/helpers/config-test.mjs`；`src` 侧 22 件同批在改。
最近一次写入 22:56:56（写入者持过 `.scratch/locks/gate.lock`，`ticket=676`）。
按 `docs/subagent-concurrency-protocol.md` §1「禁止修改其他票已声明占用的路径」，本票**不在这片上动手**。
（本票唯一改到的 `test/t255-单一来源.test.mjs` 只动 import 一行，且在 22:52 那一批之后；若那边回改到同一行，
以先落地的为准——本条如实记。）

## 五 · 下一手缺什么

1. **第 1 条已按 §三之二 改归属**：不再等 `test/` 静下来做批量换缝——那批换缝的**收益只有 24 条语句**
   （占 286 条的 8.4%），且一半目标要等搬迁票定落点；真正的路是**源侧加门**（与 #703「只许收窄」的冲突
   由那侧裁定）。换缝清单留档备查（`.scratch/t702/seam-plan.txt`、`.scratch/t702/plan-pkgpath.mjs`）。
2. **搬迁票（#704／#714／#715／#716）定下落点后**，把 `render/index.js` 那 51 个名字一次换到能力门——
   若源侧裁定「不加门」，这一条也自然作废（换了也是换到另一个实现件上）。
3. 收尾两绿（`check-warning-line.mjs`、`gen-cli.mjs --check`）：`gen-cli --check` 当刻已 **PASS**；
   告警线门的两条红（`src/health.ts` 于 23:1x 已被别席 `--sync` 登记、`src/photo/gifDoc.ts` 台账 400）
   当刻**已由别席同步完毕**——复跑即绿，见 §六。

## 六 · 收尾两绿复跑（2026-09-18 23:16）

- `node packages/skill-calorie/scripts/gen-cli.mjs --check` → **PASS**（键 134；能力 10 个；未搬迁清单 0 条）。
- `node packages/skill-calorie/scripts/check-warning-line.mjs` → 现为 **PASS**（`src/health.ts` 537 与
  `src/photo/gifDoc.ts` 400 已进台账，本票新增的两个门脚本 200／104 LF 在 350 线内、不产生台账行）。
- 两道新门对当刻树：`check-one-path.mjs` **PASS**、`check-page-assert.mjs` **PASS**。

