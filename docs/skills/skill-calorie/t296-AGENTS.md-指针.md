# #296 · `AGENTS.md` 指针（**已定稿应用**）

> 状态：**已应用**（`#295` 关闭后）。`AGENTS.md` 的 diff ＝ 一行 `### 命令登记`
> ＋ 一行正文 ＋ 一个必要的空行，**无其他改动**。

## 已追加到仓根 `AGENTS.md` 的文本

```markdown
### 命令登记

一条命令的事实只住它自己的能力目录（`src/<能力>/commands.ts`），汇总位一律由 `pnpm gen` 派生、`pnpm gen:check` 守。改命令、搬老命令、碰共用位前读 `docs/agents/命令登记纪律.md`。
```

## 为什么是这两行

- **位置**：仓根 `AGENTS.md` 是唯一能保证每个 session 每轮都读到的位置，所以指针放这儿；
  正文落归属件目录（`docs/agents/doc-homes.md`：跨件共用 → `docs/agents/`）。
- **触发词在行首**：「命令登记」是这份纪律的检索词，放行首做触发工作；
  `src/<能力>/commands.ts` 与 `pnpm gen:check` 是**岔路**——日常加命令的人与碰共用位的人各自被引到同一份正文。
- **词省**：`AGENTS.md` 每轮都加载，指针的每个字都按每轮成本算。
  所以这两行**只留到正文的路**，不复述正文里的规矩（复述就是第二份会走散的副本）。
- **全仓通用**：本段不含任何卡路里专有的东西；正文已归位到 `docs/agents/`，
  其他技能件照同一份形状做时这条指针照样管用。
- **不带清单**：指针里不列「共用位有哪几个文件」——那几个文件正在变成生成物，列进指针必然过期
  （`docs/agents/structure.md` 铁律二）。名单的判据是 `pnpm gen:check` 的逐件输出。

## 本票同时完成的归位（原三条待定项）

1. **`AGENTS.md` 指针**：已追加（见上），本体其余一字未动。
2. **纪律正文归位**：正文从本图目录 `git mv` 到 **`docs/agents/命令登记纪律.md`**
   （一处不留两份，无转发文件）。
3. **照抄说明归位**：从本图目录 `git mv` 到 **`docs/agents/命令登记纪律-照抄说明.md`**
   （独立成件，要能整段贴给别的 session，故不塞进正文当一节）。
4. **广播稿留在原地**：`docs/skills/skill-calorie/t296-广播稿.md`——它是本图恢复开工的产物，属票级证据。
