# #296 · `AGENTS.md` 指针文本（定稿时贴这一行）

> **本文件只是草稿。** 现在**不要**改仓根 `AGENTS.md`——`#295` 还没关，此刻改会把一个还没冻结的形状广播给所有 session。
> 定稿动作（等 `#295` 关闭后做）＝把下面代码块里的两行**原样追加**到 `AGENTS.md` 末尾，其余一字不动。

## 要追加的文本

```markdown
### 命令登记

一条命令的事实只住它自己的能力目录（`src/<能力>/commands.ts`），汇总位一律由 `pnpm gen` 派生、
`pnpm gen:check` 守。改命令、搬老命令、碰共用位前读 `docs/skills/skill-calorie/t296-纪律正文.md`。
```

## 为什么是这两行

- **位置**：仓根 `AGENTS.md` 是唯一能保证每个 session 每轮都读到的位置，所以指针放这儿；
  正文落归属件目录（`docs/agents/doc-homes.md`：跨件纪律 → `docs/agents/`，跟票过程稿 → 归属件目录）。
- **触发词在行首**：「命令登记」是这份纪律的检索词，放行首做触发工作；
  `src/<能力>/commands.ts` 与 `pnpm gen:check` 是**岔路**——日常加命令的人与碰共用位的人各自被引到同一份正文。
- **词省**：`AGENTS.md` 每轮都加载，指针的每个字都按每轮成本算。
  所以这两行**只留到正文的路**，不复述正文里的规矩（复述就是第二份会走散的副本）。
- **全仓通用**：本段不含任何卡路里专有的东西，其他技能包照这份形状做时同一个指针照样管用。
  文件路径里出现 `skill-calorie` 是**过程稿的暂存位置**，定稿时正文按票面归到
  `docs/agents/命令登记纪律.md`，指针里的路径随之改成那一个。
- **不带清单**：指针里不列「共用位有哪几个文件」——那几个文件正在变成生成物，列进指针必然过期
  （`docs/agents/structure.md` 铁律二）。名单的判据是 `pnpm gen:check` 的逐件输出。

## 定稿时另要一起做的两件事

1. **正文改名归位**：`docs/skills/skill-calorie/t296-纪律正文.md` → `docs/agents/命令登记纪律.md`
   （票面原话：跨件纪律住 `docs/agents/`），正文首段那条「其他技能包照此执行」的自述去掉——
   搬进 `docs/agents/` 之后它本来就管全仓，再写反而像只属卡路里。
   **搬完一处**（不留两份，铁律二）。
2. **同步改引用**：`t296-照抄说明.md`／`t296-广播稿.md` 里指向正文的路径一并改成新落点。
