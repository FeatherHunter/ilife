# #320 残余定性（达成条件 ①②）

- 席位：**Residual 定性席（只读 ＋ 出报告）**；产出＝本件；**未改任何源码／测试／票面／既有证据件，未删任何文件**。
- 日期：2026-09-14。现场：`D:\ilife`，`master`。
- 口径：所有读数都是本席当刻复算出来的（命令写在每处），不引用任何一方的自述；票面原文引自 `gh issue view 320`。

## §0 现场复算

| 项 | 命令 | 读数 |
|---|---|---|
| 提交点 | `git rev-parse HEAD origin/master` | `090f502e87f6b79d3688306eef3c89782cca60a4`（两者相同） |
| 工作树 | `git status --porcelain` | 空（干净） |
| 判官指纹 | `(Get-FileHash -Algorithm SHA256 'docs/skills/skill-calorie/t293-验收-命令自治.mjs').Hash` | `78AE857426A44D66E81A2489E51D0D2790BD16A0812DB78818B1D2E918BEBE39`（与派单一致） |
| 生成门 | `node tooling/run-locked.mjs --ticket t320res --lock-dir .scratch/locks-land --stale-minutes 30 -- node packages/skill-calorie/scripts/gen-cli.mjs --check` | exit=0；出声行：`GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 10 个（analysis／body／diet／exercise／goal／home／photo／profile／weight／workout）声明 100 条，未搬迁清单 1 条` |

复算的一个补充读数：`git grep -n "case 'calorie\."` 全仓 **73 行**（`packages/skill-calorie/src` 38 ＋ `packages/skill-calorie/test` 2 ＋ `docs` 33；`.scratch` 0）。
其中 `packages` 侧 40 行＝**真·代码标签 25 处** ＋ 来路注释 13 处 ＋ 测试件自述 2 处；`docs` 侧比收口席当时的 26 行多 7 行，多出来的是那份复核证据自己引用了该标签的正文 —— 与 `docs/skills/skill-calorie/t320-收口-证据.md` §6.1 的两组读数（66＝40＋26）同源、不矛盾。

**四处 case 的读数（逐行复算）**：

| 文件 | 行 | 行原文 |
|---|---|---|
| `packages/skill-calorie/src/cli/cmd_read.ts` | `:158` | `case 'calorie.view.goal-weight': {` |
| `packages/skill-calorie/src/cli/write.ts` | `:60` | `case 'calorie.profile.set':` |
| `packages/skill-calorie/src/cli/write.ts` | `:61` | `case 'calorie.profile.activity':` |
| `packages/skill-calorie/src/cli/write.ts` | `:63` | `case 'calorie.profile.update':` |

（`packages/skill-calorie/src/output.ts:336`–`:411` 另有 21 处，见 §3。）

## §1 四处 case 逐处定性

### 1.1 `cmd_read.ts:158`＝（a）未搬迁清单那 1 条 ＋（c）真·未完成搬迁

证据链（每条可复算）：

1. **未搬迁清单恰 1 条**：`gen-cli --check` 出声「未搬迁清单 1 条」（§0）。
2. **那 1 条就是它**：`packages/skill-calorie/src/cli/legacy/scene-03.ts:12` 唯一一条声明＝`{ kind: 'read', key: 'calorie.view.goal-weight', shape: 'stat', title: '体重目标', wakeWord: '定体重目标' }`。
3. **没有任何能力目录声明它**：`git grep -n "'calorie\.view\.goal-weight'" -- packages/skill-calorie/src` 只命中四处 —— `cli/keys.ts:105`（生成的键表）、`cli/legacy/scene-03.ts:12`（未搬迁声明）、`cli/cmd_read.ts:158`（本处）、`triggers/routes.generated.ts`；十个能力目录的 `commands.ts` 一处也没有（`src/weight/commands.ts:21-29` 的 9 条是 `view.weight`／`view.weight-history`／`view.weight-compare`／`view.weight-review`／`view.volatility`／`weight.log`／`weight.update`／`weight.remove`／`weight.batch`，不含它）。
4. **注册表是聚合件、不落键字面量**：`cli/registry.ts:21-32` 收集十个能力数组、`:48` 合成 `REGISTRY` ⇒ 上面第 3 条成立即「`REGISTRY` 不含该键」。
5. **因此 `:158` 是活代码，不是死代码**：`dispatch()`（`cmd_read.ts:149`）先查注册表（`:152-156`），未命中才落 `switch`（`:157`）；票面「清账」第 1 条的前提「注册表命中优先，未搬迁清单归零后这些分支已不可达」在**清单还剩 1 条**时**不成立**。
6. **分支体只做转调，实现已在库件里**：`:159-163` 六行调用 `defaultRange`／`nums`（`src/shared/params.ts`）、`buildGoalWeight`（`src/render/goalPlate.ts`）、`renderGoalWeightHtml`（`src/render/html.ts`）——缺的是「声明 ＋ 路由归属」这一步，不是缺实现。
7. **路由记录跨三片**：声明面 `cli/legacy/routes/scene-03.ts:15`、`scene-06.ts:11`、`scene-10.ts:11`；记录面 `triggers/routes.generated.ts:132`（scene 03）、`:231`（scene 06）、`:385`（scene 10）⇒ 搬迁这一条要同时动三片路由声明并重出记录面，是一次真改动，不是删一行。
8. **旁证（前一席已记账）**：`docs/skills/skill-calorie/t324r-复核报告.md:53` 把「老分片残留＝`calorie.view.goal-weight`（`scene-03.ts:12`）」记进了「101 键闭环」（能力 100 ＋ 老分片 1 ＝ 101）。

定性：**（a）就是未搬迁清单那一条，且是（c）真·未完成搬迁**（一条命令的声明与路由归属未收尾）。不是「另一种机制」。

### 1.2 `write.ts:60／61／63`＝（b）另一种机制：回执整页端口装配，不是命令分派

证据链：

1. **三键已搬完**：`packages/skill-calorie/src/profile/commands.ts:16`（`calorie.profile.activity`）、`:17`（`calorie.profile.set`）、`:18`（`calorie.profile.update`），三条都是 `kind: 'write'` ＋ `run:` ⇒ 已在 `REGISTRY` 里。
2. **写分派是先查注册表**：`write.ts:79-80` `const spec = REGISTRY[key]; const res = spec && spec.kind === 'write' ? spec.run(params, db) : dispatchInner(key, params, db);`；未命中的兜底 `dispatchInner`（`write.ts:89-94`）函数体只剩 `default: fail(3, '未知 calorie 写键：' + key);` —— **老写分支已清空**，三处不是分派分支。
3. **那三处住在一个回执端口里**：`write.ts:56-68` 的 `profileReceiptDoc()`，`switch (key)` 只认这三个命令名（`:59-64`），作用是把这三条命令的整页回执**换成能力目录 `src/profile/` 的两页装配**；调用点唯一：`write.ts:82` `html: profileReceiptDoc(key, params, receipt, db) ?? res.html`，且在 `withM5`（`:81`）之后 —— 它不选实现、不取数，只按命令名换 HTML。
4. **被换的两页住能力目录**：`src/profile/setup.ts:382`（`buildProfileSettingReceiptDoc`）、`src/profile/update.ts:48`（`buildProfileUpdateReceiptDoc`），对外出口 `src/profile/index.ts:20-21`；来源注释写明是 #179 的接线（`write.ts:7-9`、`:39`）。

定性：**（b）**。这三处既不是「老 case 死代码」（它们当刻可达），也不是「未完成搬迁」（声明与实现都在 `src/profile/`）。票面 ① 与「清账」第 1 条在这三处**前提不成立**：按字面数它们会报红，按机制看它们不是分派分支，而是**端口留在分派件里**这一形状问题。

### 1.3 顺带复算到的一处旧账（只报不改）

棘轮件「两条路各有活证」那一条（`packages/skill-calorie/test/cmd-registry-294.test.mjs:147-158`，标题「#294 老路：未命中注册表的老键照旧落原 switch」）用的两个「老键」当刻**都已在注册表里**：`calorie.view.body-composition`（`src/body/commands.ts:25`）、`calorie.water.log`（`src/diet/commands.ts:61`）⇒ 那条断言证明的已是**新路**，标题里的「未命中注册表的老键」不再成立；老路的活口事实上只剩 `cmd_read.ts:158` 这一条。
（本席只报数，未改测试。它与 §4 判据 7 相关。）

## §2 `cmd_read.ts` 的 351 行构成与「≤200 行」可行性

### 2.1 按段统计（口径＝`\n` 切的物理行；段＝顶层声明切分）

命令：`node .scratch/t320res/segs.mjs packages/skill-calorie/src/cli/cmd_read.ts`（读数落 `.scratch/t320res/segs-read.txt`）。

| 行段 | 行数 | 段 |
|---|---|---|
| 1–58 | 58 | 文件头注释 ＋ imports |
| 59–65 | 7 | `DEFAULT_TIMEOUT_MS` ＋ 两个类型别名 |
| 66–69 | 4 | `toast()` |
| 70–86 | 17 | `preflight()`（DB 路径预检） |
| 87–88 | 2 | `USAGE` |
| 89–104 | 16 | `parseArgs()` |
| 105–141 | 37 | `assertEnvelopeData()` |
| 142–148 | 7 | `buildEnvelope()` |
| 149–172 | 24 | `dispatch()`（含 `:158` 唯一一条 case） |
| 173–179 | 7 | `describeDeliveryTarget()` |
| 180–196 | 17 | `dataTextOf()` |
| 197–239 | 43 | `buildDeliveredEnvelope()`（导出件） |
| 240–278 | 39 | `failWithReceipt()` |
| 279–282 | 4 | `parseReadArgs()`（转调 `parseArgs`） |
| 283–349 | 67 | `async main()`（CLI 入口） |
| 350–351 | 2 | `invokedAsCli` 入口判断 |

合计 **351 行**（空行 24、注释行 56、其余 271）。

### 2.2 触达 ≤200 行要动什么

- **可搬（行为不变，纯物理搬迁）**：`assertEnvelopeData` 37 ＋ `buildEnvelope` 7 ＋ `describeDeliveryTarget` 7 ＋ `dataTextOf` 17 ＋ `buildDeliveredEnvelope` 43 ＋ `failWithReceipt` 39 ＋ CLI 前置段 55（`toast`＋`preflight`＋`USAGE`＋`parseArgs`＋`parseReadArgs`）＝ **约 205 行**。
- **可删（要改行为，且先决于 ① 落地）**：`dispatch` 的那口 `switch`（`:157-167`，约 11 行）—— 只有把 `calorie.view.goal-weight` 声明搬进能力目录之后，它才真成死代码。
- **只能改行为、不建议动**：`main` 的三态交付与失败分支（`:283-349`）、`dispatch` 的注册表先行判定（`:152-156`）—— 属出口语义，搬走只是把复杂度换个位置。
- **不可动的约束（复算过）**：`dist/cli/cmd_read.js` 是约 28 个测试件 spawn 的 BIN（例：`test/cmd-write-40.test.mjs:19`、`test/cli-smoke-t41.test.mjs:17`），约 10 个测试件直接 `import { dispatch } from '../dist/cli/cmd_read.js'`（例：`test/cmd-registry-294.test.mjs:28`）⇒ **入口文件名与 `dispatch` 这个导出不能挪**；能搬的是内部段与「仓内无外部引用」的导出件。
- **一条代价**：`buildDeliveredEnvelope` 是导出件，仓内唯一外部引用在 `docs/research/t83-evidence.mjs:17`（证据脚本）⇒ 搬它要么在新件里留一条再导出，要么动那份证据脚本（本席不建议动既有证据件）。
- **结论：≤200 可达，且不必改行为。** 351 − 205（可搬）＝ **146 行**；即便保守只搬 `assertEnvelopeData`／`buildDeliveredEnvelope`／`failWithReceipt`／CLI 前置四段（约 193 行），余 158 行，仍在线内。代价＝在 `src/cli/` 下新增 1–3 件（每件都在告警线之下）＋ 改本文件的 import。**若红线是「只许删不许搬」，则不可达**：真正可删的只有 `:157-167` 那 11 行，去掉后仍有 340 行。
- **另一条口径事实**：文件行数告警线由各包自定（`docs/agents/structure.md:70`），本包 #294 记账里写的是 **350 行告警线**（`docs/skills/skill-calorie/t294-登记与分派接缝.md:112`）⇒ `cmd_read.ts` 当刻 **351 行，越线 1 行**，按 `docs/agents/structure.md:99` 该报一句「已超线，需要根据规则进行重构」。票面的「≤200 行」是**更严的建议值**，不是告警线。

## §3 `src/output.ts:336–411` 那 21 处

- **读数**：`Select-String -Path packages/skill-calorie/src/output.ts -Pattern "case 'calorie\."` → **21 行**，行号 `336`–`411`（`diet.add`／`diet.update`／`diet.batch`／`diet.copy`／`diet.update-by-date`／`diet.remove-by-date`／`diet.remove-by-range`／`diet.remove-by-type`／`water.log`／`weight.log`／`weight.update`／`weight.remove`／`weight.batch`／`exercise.add`／`exercise.update`／`exercise.remove`／`photo.add`／`photo.tag`／`product.add`／`product.update`／`photo.gif`）。
- **它属谁**：整段是 `writeSuffixFor(key, params)`（定义 `output.ts:314`）的函数体，开关体 `:335` 起、`:411` 是最后一条 case，尾部 `default` 在其后。
- **调用链（谁调它）**：`cmd_read.ts:216／227／258` 调 `deliverHtml()`（`output.ts:228`），`deliverHtml` 内 `:257` 取 `suffix: writeSuffixFor(input.key, input.params)`，再与 `resolveDefaultHtmlPath`（`output.ts:141`）拼成落盘路径 ⇒ 它决定**落盘 HTML 文件名里的内容标识段**（例：`香蕉`／`20260801至20260810`／`500ml`／`68kg`）。
- **与命令分派的关系**：**无分派关系**。它不选实现、不取数、不装配回执正文；回执正文的装配在 `src/render/receipt.ts` 与各能力目录。它是一张「命令键 ＋ 参数 → 文件名片段」的映射表（口径见 `test/output-naming-119.test.mjs:116-141` 的既有断言）。
- **射程判定**：地图 #293 的 Destination 点名要改成「生成 ＋ 薄分派」的汇总位是 `cli/keys.ts`、`cli/cmd_read.ts`、`cli/write.ts`、`triggers/routing.ts`、`packages/base-combos/combos.yaml`、`SKILL.md` 的 `REPR` 表（`gh issue view 293` 正文 Destination 段第 3 行）——**`src/output.ts` 不在名单里**；#320 票面 ① 的字面范围只有 `cli/cmd_read.ts`／`cli/write.ts`，② 的棘轮件也只扫这两个文件（`test/cmd-registry-294.test.mjs:93-95`）。
  ⇒ **不属 ①② 的射程**。
  但它是**同一族**的另一件事：「一条命令的事实的第二处」——21 条键的（键→文件名后缀）表手写在能力目录之外、且非派生（与 `docs/agents/命令登记纪律.md` 的口径相抵，但不是「分派」）。要按「全仓 `case 'calorie.` 归零」的更严口径收口，它得**单独开票**（把后缀派生随命令声明走，或由生成器出表），不能塞进 #320 的收尾动作。

## §4 结论 ＋ 建议后续

**一句话结论：票面 #320 达成条件①②当刻均不成立（①：1＋3＝4 处 ≠ 0；②：棘轮仍是旧形态），但四处不是一件事 —— `cmd_read.ts:158` 是「未完成搬迁」（须另开票），`write.ts:60／61／63` 是「票面口径写得比 Destination 更严」（须明确裁定并留痕），`src/output.ts` 的 21 处不在射程内。**

三条支撑：

1. **未完成搬迁（1 处）**：`cmd_read.ts:158` ＝ `legacy/scene-03.ts:12` 那唯一一条未搬迁声明的**活端口**，实现件已在库件里，缺「声明 ＋ 路由归属」；路线跨三片（§1.1）。
2. **口径比 Destination 严（3 处）**：`write.ts:60／61／63` 在 `profileReceiptDoc()`（回执整页端口，`write.ts:56-68`、调用点 `:82`）里，三键的声明与实现在 `src/profile/`；Destination 只要求「声明与实现住能力目录 ＋ 薄分派」，这三处满足前者、属形状问题（§1.2）。
3. **不在射程（21 处）**：`output.ts:314` 的 `writeSuffixFor`，落盘文件名装配，非分派、也不在 Destination 点名的汇总位名单里（§3）。

### 建议的后续票（本席不开票，只给标题与判据）

- **建议票标题**：「卡路里命令登记与分派：残余 4 处清账（未搬迁 1 条 ＋ 回执端口 3 处）＋ 棘轮切终态」（Part of #293；owner＝本图编排者）
- **判据（逐条可复算，命令直接照抄）**：
  1. `git grep -n "case 'calorie\." -- packages/skill-calorie/src/cli` → **0 命中**（当刻 4）。
  2. `node packages/skill-calorie/scripts/gen-cli.mjs --check` → exit 0，且出声行的「未搬迁清单」为 **0 条**（当刻 1 条）。
  3. `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` → exit 0 且 `UNACCOUNTED` 为空（当刻已绿，作回归线）。
  4. 棘轮件出现**终态断言**：两文件的 `case 'calorie.` 标签集合为空（`assert.equal(labels.size, 0)` 或等价写法），`FROZEN_READ_CASES_MAX`／`FROZEN_WRITE_CASES_MAX`（`:81-82`）与 `FROZEN_READ_CASES`／`FROZEN_WRITE_CASES` 两张冻结清单（`:45`／`:67`）随之删除；**变异自证**：往任一文件塞回一条 `case 'calorie.x':` ⇒ 该断言转红（机比，非自述）。
  5. 行数：`cmd_read.ts` ≤ 200 物理行（当刻 351；搬法见 §2.2）。若不裁 ≤200，则至少落到 ≤350 告警线（`t294-登记与分派接缝.md:112`）并在票面记一句口径。
  6. 裁定留痕：把「回执端口里的 `case 'calorie.`（`write.ts:56-68` 这一类）算不算 ① 的计数范围」写进票面评论，同步改 ① 的措辞（例如限定为「分派分支」）；否则下一个按字面验收的人会继续报红。
  7. `test/cmd-registry-294.test.mjs:147-158`「老路活证」随之处理（两键已在注册表，老路已无活口）：改写成终态断言或删除，并在票面记账。

## §5 复算命令清单

| 用途 | 命令 | 读数 |
|---|---|---|
| 命中面 | `git grep -n "case 'calorie\."` | 73 行（src 38／test 2／docs 33） |
| 分派面 | `git grep -n "case 'calorie\." -- packages src` | 40 行（＝代码 25 ＋ 注释 13 ＋ 测试自述 2） |
| 未搬迁清单 | `node packages/skill-calorie/scripts/gen-cli.mjs --check`（经 `tooling/run-locked.mjs`） | exit 0；未搬迁清单 **1 条** |
| 该键的所有落点 | `git grep -n "'calorie\.view\.goal-weight'" -- packages/skill-calorie/src` | `keys.ts:105`／`legacy/scene-03.ts:12`／`cmd_read.ts:158`／`routes.generated.ts:132,231,385` ＋ 路由声明 `legacy/routes/scene-03.ts:15`／`scene-06.ts:11`／`scene-10.ts:11` |
| 三键的声明 | `Select-String -Path packages/skill-calorie/src/profile/commands.ts -Pattern 'key: '` | `:16 activity`／`:17 set`／`:18 update`（均 `kind: 'write'` ＋ `run:`） |
| 出口行数 | `node .scratch/t320res/segs.mjs packages/skill-calorie/src/cli/cmd_read.ts` | 351 行；分段落 `.scratch/t320res/segs-read.txt` |
| 21 处 | `Select-String -Path packages/skill-calorie/src/output.ts -Pattern "case 'calorie\."` | 21 行，`:336`–`:411` |
| 调用链 | `git grep -n "writeSuffixFor\|htmlFileName" -- packages` | `output.ts:257`（`deliverHtml` 内）唯一调用点 |
| 棘轮形态 | `Select-String -Path packages/skill-calorie/test/cmd-registry-294.test.mjs -Pattern 'FROZEN_|MAX|LINES'` | `:42-43` 行数冻结、`:45`／`:67` 两张清单、`:81-82` 两个上界；无「禁止出现」断言 |

## §6 未做项

- 未改任何源码／测试／票面／既有证据件；未删文件；未开票、未改票面、未 close 任何 issue。
- 未跑全量测试与五条绿线对账（属收口席的口径）；本席只跑了 `gen-cli --check`（经锁）与只读检索。
- 未在 `pnpm build` 后的 `dist` 上复算（全部判据落在源码面；`registry.ts` 的聚合口径由源码 `:21-48` 直接可读）。
- 「≤200 行」的实际落地属实施席动作：本席只给可行性与代价（§2.2），没有动 `cmd_read.ts`。
