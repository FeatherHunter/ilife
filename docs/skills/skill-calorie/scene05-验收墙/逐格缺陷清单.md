# 场景05 验收墙 · 逐格缺陷清单（37 格）

- 口径出处：[`docs/agents/视觉验收墙.md`](../../agents/视觉验收墙.md)（**墙 ＋ 逐格缺陷清单 ＋ 自检退出码，三样齐**；§3 收口七步、§4 完成判据、§7 坑表）。
- **本件是骨架**：前三列（格号／唤醒词／该确认什么）由机器照同目录 `manifest.json` 抄——**清单是发布名与格号的唯一权威**。后两列（**症状**、**要不要改**）**留空：待负责人看图后逐格填**。
- **本轮（票 #157 收口窗 · T351-v19）换了什么**：**第 11 格（order 186「定训练计划」）的发布名换名**——`定训练计划-预检.html` → **`定训练计划-编辑器.html`**（页还是那张**计划编辑器（可写页）**，换的只是名字：它没有「预检确认」这一步，旧名字与页不符）；其余 36 格照本轮重跑批重出。本件重出为**空骨架**——「症状」「要不要改」两列**至今无人填过**（上一版同为空骨架），故重出无损；「该确认什么」列照 `manifest.json` 的 `check` 字段**逐字**抄。
- **人的判定不由机器代填**：本件不写「版式正常／好看」这类结论，也不替负责人判「要不要改」。机器只负责把 27 条可执行唤醒词逐条跑出**真渲染**、把每格「该确认什么」写清、把自检退出码交出来。
- 墙：`手机墙-390.html`（37 格 × 390 宽 × 3 列，1:1，看塌列）／`桌面墙-1280.html`（37 格 × 1280 宽 × 1 列，缩 0.469 显示，看排布）；两墙**格高一致**（820／860）。
- 细看入口：`总索引.html`（按页面族分组，每份一张卡；末尾列清「哪些有意不出产物及其原因」）。
- 产物真身（原批目录）：`.scratch/t157-final/products/`（`orderNNN-<card>.html` ＋ `detail.json` 逐条读数）；本目录里的 `唤醒词.html` 是**同一份的发布名副本**，复制来源逐行写在 `manifest.json` 的 `src` 字段（逐件 sha256 已核对：副本 ≡ 原批）。
- 怎么用：拿真实浏览器双击两张墙滚一遍（不起服务也照看），**逐格**在下面表里填两列；看完没缺陷也算结论，但要写明看了哪些格。

## 一、逐格（37 格）

> 「症状」「要不要改」两列**待负责人看图后逐格填**。填法：症状写「看到什么」（挤／塌列／被藏起来的横滚／层级乱／文案难读／两端差异…），要不要改写「要改／不改／要裁」并附一句归哪张票。

| 格号 | 唤醒词 | 该确认什么（照 `manifest.json` 的 `check` 逐字） | 症状 | 要不要改 |
|---|---|---|---|---|
| 1 | 看本周计划 | 本周（第 1 周）两张场次卡与四列动作表（动作／部位／组数×次数／重量）都在吗、表头有没有挤成两行；周／日两级页签默认落在本周周一吗、点得动吗；两点：休息日标题不重复、节奏落在场次卡头行上。 | —（待填） | —（待填） |
| 2 | 看下周计划 | 第 2 周那五组同重量的行（宽距高位下拉）有没有把表撑出横向滚动；副行小字「背 主」＋「孤立」两颗标签在 390 宽下会不会叠在一起。 | —（待填） | —（待填） |
| 3 | 看上周计划 | 这一条落在第 0 周（空周）：页面是不是**明确说清「没有安排」**而不是留一片空白；空态还撑着表头或空表框吗。 | —（待填） | —（待填） |
| 4 | 看指定周计划 | 页头信息条里计划说明那三段（科学定制／6天·周／270组）是不是按胶囊分开、没有串成一行；第 1 周两张场次卡加休息日一行的次序读得顺吗。 | —（待填） | —（待填） |
| 5 | 看今天练什么 | 单日页是不是只出该日那一场、没把整周铺出来；页上没有周页签时版面会不会空掉一大块。 | —（待填） | —（待填） |
| 6 | 看某动作安排 | 「硬拉」这一条命中只有一场：命中一句是不是写在显眼处、表会不会只占半页就断掉；动作名加粗与副行小字在本页也在吗。 | —（待填） | —（待填） |
| 7 | 看某天练什么 | 指定日（2026-09-09）那一场的内容与日期是否都写在页上、日期只出现一处不打架；表头四列按序吗。 | —（待填） | —（待填） |
| 8 | 看计划概览 | 概览页：周／日／动作三级层次看得出来吗、三处节奏（20-30／18-25／15-20 RPM）在页上各自只落在自己的场次卡头行上吗；390 宽下横滚有没有把右侧列藏起来。 | —（待填） | —（待填） |
| 9 | 看完整计划 | 夹具三条边界同页可验：多组不同次数的紧凑写法（3组×10／8次 ＋ 35／40kg）、空组数的「—」占位、休息日标题＝星期 chip ＋「休息日」两件；外加指标卡「总周数 4 周／其中 2 周有安排」。 | —（待填） | —（待填） |
| 10 | 看计划 vs 实际 | 计划 vs 实际那一张自有表：列序（日期／计划动作／实际完成／缺口）读得顺吗、缺口为空的行有没有占位、全左对齐的四列在 390 宽下挤不挤。 | —（待填） | —（待填） |
| 11 | 定训练计划 | 计划编辑器（可写页）：两排页签（上面选第几周、下面选周几）能不能点、一次只显示一天吗；点加一次训练先选时段（凌晨／上午／下午／晚上，已占的应禁用），一天最多 4 段，建满应有话；每段起止时间两格在段头，锁住的周也改得动吗；动作参数格（组数／每组次数／负重，有氧只填时长）在页上改得动吗，第 2 周起是不是只能改参数、不能增删动作；页底「生成的计划」那张表多出时间列且跟着改动走吗、「复制指令」那颗按钮复制的对不对。 | —（待填） | —（待填） |
| 12 | 复制训练计划 | 写前预览：改前／改后两段快照都在吗、复制来源写清了吗；复制数据按钮的标题是中文吗（不该出现英文命令键）。 | —（待填） | —（待填） |
| 13 | 定休息日 | 写前预览：把第 1 周周三标成休息日的改前→改后一眼看得出吗、原来那天有动作会不会被静悄悄移走（该列出来）。 | —（待填） | —（待填） |
| 14 | 加训练动作 | 写前预览：新动作（硬拉）落在哪一周哪一天说得清吗、原有动作没被吃掉吗；两段快照的对照列宽在 390 宽下挤不挤。 | —（待填） | —（待填） |
| 15 | 定一周计划 | 写前预览：整周替换的改前／改后两段都在吗、被覆盖掉的旧安排有没有明白写出来。 | —（待填） | —（待填） |
| 16 | 改训练计划 | 写前预览：改的是配置里哪几个字段逐项列出来了吗（标题示例改名）、没改的字段不该跟着动。 | —（待填） | —（待填） |
| 17 | 改某天训练 | 写前预览：第 1 周周三改名「下肢＋核心」的改前→改后成对吗、旧标签原文还留着吗（旁有「＋」号的长串会不会把列撑破）。 | —（待填） | —（待填） |
| 18 | 删某天训练 | 写前预览：删的是哪一天说得清吗、改后那一格是不是写成「空」而不是一片空白（空与没写要分得开）。 | —（待填） | —（待填） |
| 19 | 改动作 | 写前预览：把硬拉换成杠铃划船，旧名与新名两处原文都印出来了吗；一句话预览那行长不长到溢出。 | —（待填） | —（待填） |
| 20 | 撤销训练计划 | 写前预览：撤销＝删掉整个计划（所有周次与配置）这个后果写清了吗；确认那一步的口气是不是「要用户自己点头」。 | —（待填） | —（待填） |
| 21 | 定训练计划 | 回执页：本页自有表量得到吗；落库后的计划（标题／起始日／周数）与预检页是不是同一份；有没有把内部字段名（`op=`／`id=`）印上屏。 | —（待填） | —（待填） |
| 22 | 复制训练计划 | 回执页：新副本名（示例副本）印对了吗、来源计划名有没有丢；改前→改后与预检页对不对得上。 | —（待填） | —（待填） |
| 23 | 定休息日 | 回执页：休息日改动后那一周的表／场次跟着变了吗；写后现值与预检页说的是不是一件事。 | —（待填） | —（待填） |
| 24 | 加训练动作 | 回执页：新动作入表后的行位置与预检页说的一致吗、动作名加粗与副行小字有没有跟着出来。 | —（待填） | —（待填） |
| 25 | 定一周计划 | 回执页：整周替换后的周数／场次数与预检页一致吗、被覆盖的旧安排有没有交代。 | —（待填） | —（待填） |
| 26 | 改训练计划 | 回执页：改后的标题印对了吗、没改的字段（起始日／周数）有没有跟着动。 | —（待填） | —（待填） |
| 27 | 改某天训练 | 回执页：改名后的标签（下肢＋核心）印对了吗、改前那一格是不是还留着旧名做对照。 | —（待填） | —（待填） |
| 28 | 删某天训练 | 回执页：删掉那天在改后快照里显示为空吗、空的那一格有没有占位说明（别让人看成漏印）。 | —（待填） | —（待填） |
| 29 | 改动作 | 回执页：动作新旧名（俯卧撑／钻石俯卧撑）成对出现吗、表里那一行确实换过来了吗。 | —（待填） | —（待填） |
| 30 | 撤销训练计划 | 回执页：写后现值是不是逐字写着「计划已撤销（配置与训练安排均为空）」；页上还剩不剩旧计划的行、空态说清了吗。 | —（待填） | —（待填） |
| 31 | 计划复盘（本周） | 复盘页：本页自有统计表量得到吗、计划场次 vs 已完成对不对得上；只有一天实际记录时这页会不会显得空。 | —（待填） | —（待填） |
| 32 | 计划复盘（本月） | 本月窗：四周的完成情况分开摆了吗、标题与副题的窗口口径有没有打架；柱图撤掉后版面留没留下空洞。 | —（待填） | —（待填） |
| 33 | 计划复盘（全部） | 自定义区间窗：区间标识是不是只出现在一处（别标题一个说法、副题另一个说法）、起止日印对吗。 | —（待填） | —（待填） |
| 34 | 看计划完成率 | 完成率页：百分比与分数同源吗、分母（计划场次）写出来了吗；页上的说法是不是人话（没有「会话」这类内部词）。 | —（待填） | —（待填） |
| 35 | 看未完成训练 | 未完成页：没做的场次逐条列出来了吗、一条都没有时是不是明白写「没有未完成」，而不是留空白让人猜。 | —（待填） | —（待填） |
| 36 | 看动作完成率 | 动作完成率页：逐动作的完成比与表里行数对得上吗、动作名过长时列有没有被挤成一字一行。 | —（待填） | —（待填） |
| 37 | 扫禁忌 | 禁忌页：命中的禁忌项与依据分开摆了吗、没有命中时结论说得清不清楚（别只印一个空表）。 | —（待填） | —（待填） |

## 二、有意不出产物及其原因（照 §3-4，与 `总索引.html` 末尾同源）

见同目录 `manifest.json` 的 `notShipped` 段（本段由它逐字生成）：


1. **外部五条：落地训练／落地到本周末／落地到本月底／同步到训记／拉训记实绩（order 196–200）**：`packages/skill-calorie/src/workout/routes.ts:32-36` 把这五条记为 `kind:'non-exec'`、`bucket:'out-of-scope'`，`reason` 逐条写着「明确不做（架构规格 `docs/calorie-architecture.md:60`：落地／训记）；词只保证命中与文案，执行层不承接」。**本轮仍不执行、仍不造产物**：这五条的执行面在外部 App（补计划到日历／记心愿／推送训记／回写训记实绩），卡路里这侧的执行层不承接 ⇒ 既没有「写后」可出回执页、也没有「读」结果可出结果页，硬出一张只能印这几句词本身。本轮实测：五条在路由表／场景词表／HELP 资产三处都命中（5/5），HELP 文案首句逐字在——读数见 `docs/skills/skill-calorie/t351-v19-evidence.md` §四「表二 · 外部五条」。本轮产物原批目录：`.scratch/t157-final/products/`。
2. **撤销后读验证（order 195 的 read-after-delete 一步）**：那一步**不是页面**，是机检读数：撤销后念「看计划」再读一次，得 `exit=4` ＋ stderr 命中「无训练计划」（恢复缺失阻断）——它**没有可上屏的 HTML**，所以本轮照旧只把读数落进批次的 `detail.json`（`steps` 里那条 `撤销后读验证`），不产页面；批脚本一度产出的 `order195-verify.html` 已按陈旧件处理，不进这一册。本轮读数与逐条现场见 `docs/skills/skill-calorie/t351-v19-evidence.md` §三「口径两条」；本轮产物原批目录：`.scratch/t157-final/products/`。
3. **order 176–207 的中间产物：`dbs/`（隔离库）与 `detail.json`（逐条读数）**：它们留在**本轮**的原批目录 `.scratch/t157-final/products/`（`dbs/` 隔离库 ＋ `detail.json` 逐条读数），是**出页依据与判据读数**，不是给人看的页面——墙上没有可看的渲染，故不入这一册。要看机器读数去 `.scratch/t157-final/products/detail.json`（`steps` 43 条 ＋ `checks` 29 条）；要看逐条实跑证据去 `docs/skills/skill-calorie/t351-v19-evidence.md`。

## 三、三样里的第三样：生成器自检退出码

| 面 | 命令（在 `docs/skills/skill-calorie/scene05-验收墙/` 内跑） | 读数 |
|---|---|---|
| **正例** | `node gen-wall.mjs --check .` | `37 格；链接 189 条（手机墙-390.html 75 ＋ 桌面墙-1280.html 75 ＋ 总索引.html 39）；缺失 0 -> 可发` · **exit 0** |
| **反例** | 把 `manifest.json` 第 1 行 `file` 改成盘上不存在的名字再跑 `node gen-wall.mjs --check .` | `36 格；链接 189 条（手机墙-390.html 75 ＋ 桌面墙-1280.html 75 ＋ 总索引.html 39）；缺失 1 -> 不可发` ＋ `清单点名却没有文件：1 看本周计划 -> 看本周计划-反例-盘上没有.html` · **exit 1** |
| **改回** | 从备份逐字节还原 `manifest.json` 后复跑 | 回 `37 格；链接 189 条（手机墙-390.html 75 ＋ 桌面墙-1280.html 75 ＋ 总索引.html 39）；缺失 0 -> 可发` · **exit 0**；清单 sha256 改前改后逐字一致 `4B26952D34342BB4379247AFEE6CBE7C28BAE7F4A0A30DCA0818CA63948F93BB` |

> 反例必须**点名那一份**且 `exit 1`；走不出这条，等于自检永远不会红。自检把 `dropped`（清单点名却没有文件）与 `dead`（页上引用却落不到）**一起判**——只查后者是仓规 §6.2／§7 点名的假绿灯。
> 本轮的逐条读数与运行标识见 `docs/skills/skill-calorie/t351-v19-evidence.md`、`t351-v19-gate-runs.md`。
