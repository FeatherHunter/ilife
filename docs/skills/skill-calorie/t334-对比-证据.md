# #334 证据：页面②两段对比 18 词（9 窗口＋8 锚点＋1 跨场景只核验）

分区：只动 `compare.ts` 归属；未碰 `render/index` 与 `plate` 薄层、`plateDocs`。

## 1. 本页指纹基线（重采）

`node .scratch/332/pages-fingerprint.mjs --check` 改前＋改后各跑一次：

- 改前：FINGERPRINT-CHECK PASS 5/5（compare sha `ae9e74e8…`，59478 字节）。
- 改后：compare SAME（窗口路径源码零改动，只加了情景早退分支）；
  dashboard／history／volatility DIFF＝邻席（#333／#336）在途改动，非本票写入集。
  改后复跑两次一致（日志链 `.scratch/334/fp-check-after.log`，对照 §1 基线）：
  compare SAME／review SAME，dashboard／history／volatility DIFF（邻席在途）。

## 2. 改动（compare.ts，255 行 LF 口径，未超 350 线）

- 参数面：新增 `scenario`（b8／e1／e2／e3／e5／e6／c5／d4，另收 a1–a8／b1 别名）
  ＋ `delta`（e3，缺省 5）＋ `n`（a5，缺省 30），经 `runScenario` 派生锚点日期；
  算式零复制（调同目录 `weightCompare2／3`，铁律二）。
- 页面：新增 `buildScenarioCompareDoc` 对照老实物 `weight_compare.html`：
  情景徽章（eyebrow＋subtitle）／两段卡（段标签／区间／均值／条数）／
  差值箭头与速度判语／补充对照行（extraRows）／容差与样本不足警告（notice）／
  结论句（Disclosure）。
- 缺失口径：算不出锚点 → `missing-data` 可读阻断，不编日期（`e3 delta=50` 验证）。

## 3. 18 词真跑（`.scratch/334/run-compare18.mjs`，固定种子，HTML 落盘 18 件）

RESULT: 17/17（走 `calorie.view.weight-compare` 的 17 条全完整文档）：

- 窗口 9：w30d／wcustom／wweek／wmonth／wndays／w1y／w6m／w3m／wweekend 全 ok。
- 锚点 8（锚点日期逐条印出）：b8 平台期首日 2026-08-01／e1 历史最低 2026-09-06／
  e2 历史最高 2025-09-01／e3 减重 5kg 那天 2025-12-28／e3 减重 10kg 那天 2026-04-26／
  e5 入夏最低 2026-08-31／e6 入冬最低 2026-02-25／c5 运动最少月 2026-07。
- 跨场景 1（b1 当前 vs 目标体重）：命令可执行无抛错，但产物是 section 片段
  （1107 字节）而非完整文档 → 已开票 #390 归 `src/goal/` 侧，本图未动别人目录。

种子要点：平台期须以 >2 天缺口或数据结尾收尾（`findPlateau` 口径与老脚本一致），
本种子用 08-01..08-14 平坦＋08-15..08-17 缺口。

## 4. 用例与门

- `packages/skill-calorie/test/weight-compare-334.test.mjs`：3/3 绿
  （GATE-RUN runId=`2bd47264-22be-4e33-95f2-8e9a5cee2c52` exit=0；
  复跑 runId=`fcef5d10-a1fc-4330-9006-1cdcaf34d31f` exit=0）。
- 构建：GATE-RUN runId=`29eb6a59-ec70-4aaf-b186-84d3ae7e52da` exit=0。
- `pnpm gen:check` 与全量测试：树被邻席半落定改动阻塞（见 §5），待树绿复跑。

## 5. 待办与阻塞（本票未完成项，下一手）

1. 共用 6 处原子改动（8 条翻 exec）：`src/weight/routes.ts`（＋8，order 121–128，
   无 bucket／reason）／`src/cli/legacy/routes/scene-03.ts`（－8）／
   `src/triggers/scene-03-weight.ts`（8 条 cli＋data_source）／SoT 快照重算／
   `test/calorie-routing-81.test.mjs`（exec＋8／non-exec－8／legacyChain－8，
   并在 DATA_DEPENDENT_FAILURES 登记减重 5kg／10kg 那天 exit 4——标准种子库
   总降幅 4.4kg，达不成里程碑，词与参数都对，真用户库减够即跑得通）／
   `docs/research/t81-exec-smoke.md` 重生成。须等 #333 先落子其 4 词再持锁串行，
   当前三件仍 M 在 #333 手里，不碰。
2. `src/triggers/scene-10-analysis.ts:144-157` 被邻席脚本改坏（JSON 被拆成裸标识，
   tsc 红），`pnpm build` 全量红、`gen:check` 与 smoke 重生成须等树绿。非本票引入。
3. 全量 `node --test` 与 `gen:check` 待 1–2 落定后持锁复跑；复跑绿后提交＋推送。

## 6. 持锁链（本票，`.scratch/locks/gate-runs.log` 可对账）

- runId=`29eb6a59-ec70-4aaf-b186-84d3ae7e52da` build exit=0
- runId=`2bd47264-22be-4e33-95f2-8e9a5cee2c52` 本票用例 exit=0
- runId=`fcef5d10-a1fc-4330-9006-1cdcaf34d31f` 本票用例复跑 exit=0
- runId=`51256036-e464-4464-b4d1-06d3fccbc3c1` build exit=1（邻席 scene-10 破坏，留痕备查）
- runId=`820e5504-c10a-4502-8bc3-404b766f2968`／`e0c45f79-dbef-4caf-b577-4097821914c1` build exit=1（本票初期类型错，已修好即绿）
