# T351-A2 独立对抗审查报告（结果页动作明细表改四列行内版式）

> 审查兵＝独立第三方（非实施者）。派单正本 `docs/skills/skill-calorie/t351-a2-review-brief.md`（`6f0dfc1`）
> ＋ 前置正本 `docs/skills/skill-calorie/t351-review-brief.md` §2／§4／§5。
> 被审＝`6c7daca`／`5fa46d6`／`3f282f8` 与产物 `.scratch/t351-fix/final-v4/`（37 份 ＋ `realdata/order184-realdata-result.html`）。
> **不采信实施者任何结论**：本件每条读数都是本席自己跑出来的（脚本 `t351-a2-review-*.mjs` 与产物同轮入仓）。

## 判定：PASS

无 S1／S2；两条 S3 记账（均「本票范围」，不改产品与判据结论）。五维：契约一致 30 ／ 证据真实可复现 24 ／
新旧对照 20 ／ 工程红线 14 ／ 文档同步 10 ＝ **98/100**。

## 一、机器证据

提交 sha：`6c7daca`（三件新增／改）＋ `5fa46d6`（裸词中文化）＋ `3f282f8`（证据件）。当前 HEAD `41de349`；
三件工作区 blob 与 `HEAD:<path>` **全等**（`0c455ec1…`／`9270a460…`／`ed16bcc7…`）⇒ 交付与版本库无漂移。

| GATE-RUN runId | 命令 | exit | 摘要行 |
| --- | --- | --- | --- |
| `t351-a2-review-types` | `pnpm test:types` | 0 | `$ tsc -b` 整树零错（他席在途件本轮未致红） |
| `t351-a2-review-side` | scene05 三支 ＋ 前任三支探针 | 0 | `tests 3／pass 3／fail 0`；`PROBE-INDEPENDENT GREEN 37/37`；`PROBE-PRE-VERBATIM GREEN 10/10`；`PROBE-READ-BLOCK GREEN 6/6` |
| `t351-a2-review-repro` | 被审重跑脚本 ＋ 真数据脚本 ＋ 逐字节对比 | 0 | `RESULT: 58/58`、`PROBE: PASS（21/21）`、`REALDATA_RESULT: 10/10`；重跑 38 份**正文逐字节复现**交付（`仅正文全等=38／正文也不同的份=0`） |
| `t351-a2-review-cf` | 反事实三变体（撤夹具改动） | 0 | `cfA 26/37`、`cfB 21/37`、`cfAB 29/37` 回到 v3 |
| `t351-a2-review-p24` | 零表后门抽／塞表四例 | 0 | 见 §2.4 |

**自设新探针的「变异红／还原一致」两行**（探针＝`t351-a2-review-probe-tables.mjs`：逐页遍历**每一张** data-table）：

- 变异红：`node …probe-tables.mjs .scratch/t351-fix/mut/m1` → `PROBE_TABLES: RED(26)`（26 格副行为空）；
  探针 `-probe-rhythm.mjs` 在 `mut/m2` → `日级带节奏=0`（交付为 14）。
- 还原一致：`…probe-tables.mjs .scratch/t351-fix/final-v4` → `PROBE_TABLES: GREEN`、`RESULT: 1/1`；
  `-probe-rhythm.mjs` → `PROBE_RHYTHM: GREEN`。

**这几支探针打的是哪个盲区**（被审脚本自己覆盖不到的）：① 被审 `c4heads`／`subLines` 都取页内**第一张**
`<thead>`／`<tbody>`，于是 176／177／179／183／184 这 5 页的**第 2…N 张**动作表（共 17 张表、9 张在多表页）
列名与副行**根本没被判**——我的探针逐表判，并额外做**值级**对账（`-probe-values.mjs`：副行／组数×次数／重量
与夹具 `sets` 手写真值逐格比）；② 被审 ⑨ 只查「删减记号不入正文」，我在探针里加了「零动作表页被塞表也须红」；
③ 生成一致性：被审只报自己的 `RESULT`，我另跑一支脚本把**重跑产物与交付产物逐字节**比。

## 二、逐条路径（本席动的件／写的证据件／日志件）

**产物件、源码件、被审脚本、`test/**`、`packages/base-render/**`、`packages/base-paint/**`、他席在途件：
一件未动。** 本席只写自己的草稿与证据件：

| 件 | 说明 |
| --- | --- |
| `docs/skills/skill-calorie/t351-a2-review-报告.md` | 本件（回执正本） |
| `docs/skills/skill-calorie/t351-a2-review-runs.log` | 闸门条目 ＋ 关键读数行（机器读数摘录） |
| `docs/skills/skill-calorie/t351-a2-review-probe-tables.mjs` | 新探针①：**逐张** data-table 列名／动作格／副行逐格核（打「只判首表」盲区） |
| `docs/skills/skill-calorie/t351-a2-review-probe-values.mjs` | 新探针②：值级真值对账（副行／组数×次数／重量 vs 夹具 `sets` 手写真值） |
| `docs/skills/skill-calorie/t351-a2-review-probe-rhythm.mjs` | 新探针③：节奏位置（周级／休息日／日级／表体）＋ 基准 vs 交付列数 |
| `docs/skills/skill-calorie/t351-a2-review-probe-24.mjs` | 新探针④：§2.4 抽／塞表两面（插桩被审脚本，用**它自己的**判据判） |
| `docs/skills/skill-calorie/t351-a2-review-probe-prod.mjs` | §2.1／2.2 生产库独立复算（不 import 被审件） |
| `docs/skills/skill-calorie/t351-a2-review-counterfactual.mjs` ＋ `-cf-compare.mjs` | 反事实三变体（撤夹具改动）与 v3 逐字节对比 |
| `docs/skills/skill-calorie/t351-a2-review-header-stats.mjs`／`-body-diff.mjs`／`-repro-compare.mjs` | 逐表表头统计／正文对比／重跑与交付逐字节对比 |

日志件（草稿，`.gitignore` 内不入仓）：`.scratch/t351-a2/review/` 下 `types.log`／`side.log`／`repro.log`／
`cf.log`／`p24.log`／`probe-prod.log`／`probe-rhythm.log`／`probe-values.log`／`header-stats.log`／
`cf-compare.log`／`body-diff.json` 与反事实产物 `out-cfA|cfB|cfAB`／`out-p24-*`／`out-repro`／`out-realrepro`。
各脚本 cwd ＝ 仓根；读被审脚本的几支按仓内件路径 `docs/skills/skill-calorie/t351-a2-run-176-207-v4.mjs` 取源码。

## 三、逐条证伪

### §1「该变的都变了、不该变的没变」——成立，但归因要改一笔

逐表表头统计（本席自写 `-header-stats.mjs`）：`final-v3` **17 张六列表**（列＝动作／部位／**类型**／组数×次数／
重量／备注），分布在 **8 页**176／177／179／180／181／182／183／184；`final-v4` 六列表 **0**、四列表 **17**、
页集合**逐份相同**；真数据页 96 张表**全部四列**、264 行（＝生产库 96 场次／264 动作，本席独立复算同数）。
`final-v3` 基准**未被覆盖**（mtime 19:01:42–19:01:52，仍 17 张六列表）。

正文口径（剥 `<style>` ＋ 全部 `<script>` ＋ 抹时间戳）复算：37 份里 **20 份逐字节相同、17 份不同**；
不同的 17 份＝**8 份目标页**（应改）＋ **9 份数据面页**（`order185`／`order194`／`order201–207`），
与派单量到的 20／9 一致。**你要我判的那一笔，我给的是反事实实证而不是推断**：把夹具本轮的两处改动
分别／同时撤掉再重跑（`-counterfactual.mjs` ＋ `-cf-compare.mjs`）：

| 变体 | 撤回的夹具改动 | 回到 v3 的份数 |
| --- | --- | --- |
| cfA | 撤掉「补的一条动作」（`宽距高位下拉`） | 26/37（仍差的含 `order185`／`202`／`203`） |
| cfB | 撤掉「改的那条动作名」（第1周周1 `悍马机卧推`→`俯卧撑`） | 21/37（`order185` 回到 v3） |
| cfAB | 两处都撤 | **29/37** —— 只剩 8 份目标页不同 |

⇒ 9 份的差异**恰好且完全**由夹具**两处**改动解释（撤两处即 29 份非目标页逐字节回到 v3），
并且九份的**表形面签名**（表头序列／副行条数／节奏记号／加粗格数）v3＝v4，`SHAPE_BAD=0` ⇒ **零表形层面改动**。

**但派单正本（与实施者证据件）把这 9 份全归给「补了一条动作」，这条不成立**：`order185` 的
`计划 俯卧撑…完成 1/3` → `计划 悍马机卧推…完成 0/3` 是**改名**造成的（cfB 单独撤改名即回 v3；cfA 撤补动作时
`order185` 依旧差），只有 `order194`／`201`／`204–207` 是补动作造成的，`order202`／`203` 两处都占。⇒ **S3-1**。

**「断言有没有跟着夹具一起更新」**：跟着更新了，且可证。`EXPECT_TEMPO` 逐页真值、`PROD_SUB='背 主 · 孤立'`
都是 v4 新加的夹具耦合断言；cfA（撤补动作）实测 `RESULT: 56/58`、⑧ 报
`order177(期望1实测0)／181(期望1实测0)／183(期望3实测2)／184(期望3实测2)` —— 断言会随夹具翻转，
不是「夹具变了断言没变」；也不是「断言变了却不说明」（脚本头 §五 与证据件 §四.3 都写明补了那条夹具）。

### §2.1「两处删减是无损的」——独立复现 264/264（各两条）

本席自写 `-probe-prod.mjs`（只读拷贝生产库 `D:\2Study\StudyNotes\.db\calorie_data.db` 后算，不写它），
**不 import 被审脚本任何件**：动作 **264**；备注有方括号 **264/264**；括号内 `W<n>` 存在 264/264 且
**恒等于该行周次 264/264**（分布 66／66／66／66）。「丢掉那截」的可还原性我不采信被审口径、自己推：
朴素两版（组数×重量原样／去重）都只有 **220/264**，差 44 条全是**自重行**（`unit='自重'`，原文却写 `0kg`、
且 0 不写 `0.0`）；补上「次数去重以／连 ＋ 重量去重（0 写 `0`、非零整数写一位小数）＋ 单位恒 `kg`」这条真口径后
**264/264 命中** ⇒ 被审在证据件 §3.4 主动写出的「口径补正」是真的、且是必需的。产物侧旁证：
`reps×`／`W1 `–`W4 ` 记号在 **37/37 份正文零命中**（`-probe-values.mjs`）。⇒ 两条删减确实无损。

### §2.2「副行小字按规则拼」——成立

按**同一实现口径独立重写**期望副行（细化词中文化 ＋ 去掉与第二段同字词），全库 264 个动作的期望副行
**集合与计数**为 21 种取值、无一条为空、**无 `main`／`iso` 裸词**（`背 主 · 孤立`×12、`背 补充 · 孤立`×12
＝生产库那 24 条 `背 iso 主／补充`，与 `type=iso` 同值 24/24）。产物侧：正文**可见文案**裸词 0、
**连 `data-t` 载荷里也 0**（37 份与真数据页同）；真数据页 96 表 264 行**逐格**副行非空、尾段都收在
「主要／孤立」（`-probe-tables.mjs` 全表口径）。边界两条也在**值级**对过（`-probe-values.mjs`）：
哑铃飞鸟 `note='—'` → 副行只留 `孤立`、两列 `—`；第2周腿深蹲 `note=''` → 副行只留 `孤立`。

### §2.3「节奏只在该在的地方」——成立

生产库：有动作场次 **96**、场内节奏**恒定 96/96**（全库仅 3 种取值，与实施者同数）。
页面侧（真数据页）：标题 104 ＝ 周级 4 ＋ 日级 100（96 个动作场次 ＋ 4 个休息日）；
**周级标题带节奏 0**、**休息日标题带节奏 0**、日级带节奏 **96**、节奏原文恒为夹具／库里那三种；
**表体里零 `节奏`／`RPM`**（节奏没有退回逐行印）。夹具 37 份同口径：日级标题 21、带节奏 14，
无节奏的 7 条＝4 条 `休息日` ＋ 3 条 `周三 · 腿 · 1 组`（该场 `第2周` 深蹲 `note=''`，本就没有节奏，
不是「下移丢了」）。⇒ 无「逐行印节奏」的旧毛病回潮。

### §2.4「零表判绿的后门堵上了」——**主体堵上，残留一个覆盖盲区**

用被审脚本**自己的** `inspect()`／`tableVerdict()` 判（把它的源码定点插桩，产物落盘前动它；被审本体一字未改）：

| 情形 | 判据④ | 结论 |
| --- | --- | --- |
| `order184`（声明 `four`）**抽掉该页全部 4 张表** | **红**（`实测页内零表头（无表）`），`RESULT 54/58`、`PROBE: RED` | 旧后门（零表判绿）**确已堵死** |
| `order178`（本就无表、声明 `none`）抽表 | 绿（`PROBE24_NOTE … 页内本无表，strip 是空操作`） | 「无表」没被误判成「缺表」 |
| `order178` **塞进**一张伪造四列表 | **红**（`声明本页无表，实测 动作／部位／组数×次数／重量`） | `none` 不是免检通道 |
| `order183`（声明 `four`，页内有 4 张表）**只抽掉第 1 张表** | **绿**（`RESULT 58/58`） | **残留盲区** ⇒ **S3-2** |

⇒ §2.4 派单要的两面（应带表页抽表必红／178 抽表不该红）**都给到了，且都符合**；但判据④ 只看页内
**第一张**表，多表页（176／177／179／183／184）的第 2…N 张表无人判。**本席自己的全表探针**在这 17 张表上
逐格核过、全绿，故**交付产物本身无缺陷**；缺陷在判据覆盖面。⇒ **S3-2（本票范围）**：判据④ 建议改成
「逐表皆须命中该页声明」，或多表页至少断「动作表张数 ＝ 该页渲染场次数」。

## 四、老规矩（派单 §3）

- **交付目录未被变异产物污染**（两行读数）：变异轮 `mut/m1` 我的探针 `RED(26)`（26 格副行为空）、
  `mut/m2` 日级带节奏 `0`；交付轮 `PROBE_TABLES: GREEN`、日级带节奏 `14`。
  另：`mut/m1`／`mut/m2` 与 `final-v4` **同名同哈希份数 0/37**；`final-v4` 递归只有 37 份 HTML ＋
  `realdata/` 1 份＋`detail.json`／库文件，**无 `mut` 子目录**；38 份产物**内嵌渲染时间戳 ≡ 各自 mtime**
  且全部落在 `20:41:49–20:42:07` 一个窗口（变异轮各在 20:40:49–20:41:12）⇒ 交付取自**复位后那一次**。
- **产物 mtime 晚于源码**：`workoutMovementTable.ts` 20:40:59、`workoutPlanDocs.ts` 20:41:12、规格 20:39:20；
  37 份产物最早 20:41:49、最晚 20:41:58、真数据页 20:42:07 ⇒ **全部晚于**源码最后改动。
- **生成一致性**：用被审脚本原样重跑到本席草稿目录，得 `RESULT: 58/58`／`PROBE PASS`／真数据页 `10/10`，
  且重跑 38 份与交付**正文逐字节相同**（仅内嵌时间戳不同）⇒ 交付可复现。
- **规格与代码没走散**：`t351-redesign-184-spec.md` §二已是四列 ＋ 副行 ＋ 节奏上移 ＋ 裸词中文化四条口径，
  §五 `movements[].note`／`sets[]` 的出处改成 `planStore:39-42`，「缺字段清单」里那两项已删除。
- 前置派单 §3.2 独立复跑：scene05 三支 `pass 3／fail 0`；§4 三支老探针在新产物上仍全绿。
- **未改** `test/**`／`packages/base-render/**`／`packages/base-paint/**`／他席在途件；`pnpm test:types` 未因
  他席在途件致红（本窗口无「范围外发现·他席在途件」需归因的条目）。

## 五、缺陷清单（含归属与级别）

1. **S3-2 · 本票范围**：判据④ 只判页内第一张表 ⇒ 多表页第 2…N 张表无判。
   证据：`strip:order183` 抽掉首表后 ④ 仍绿（`58/58`）。修法：逐表断言或断张数。
2. **S3-1 · 本票范围**：夹具与基准之间实有**两处**改动（补 `宽距高位下拉` ＋ 首场动作名
   `俯卧撑→悍马机卧推`），实施者证据件与派单正本只记了一处，据此把 `order185` 的差异误归因。
   证据：cfA／cfB／cfAB 三变体读数。修法：证据件补记第二处（属前任无主脚本改动，接任时未量到）。

## 六、未做项与下一手缺什么

- **未跑** `pnpm build`（含 `gen-cli --stamp`）与 `pnpm gen:check`：本票只改渲染件，命令登记与生成物未动；
  走发布链前须按发布口径补跑（与实施者声明一致）。
- **未独立复跑**包内全量测试（`packages/skill-calorie/test/*.test.mjs`）与整树 `pnpm test`；本席只跑了
  根 scene05 三支 ＋ 整树 `tsc -b`。实施者报「包内 24 条红全在他席在途面」**未经本席复核**，
  下一手若要关闭该条需自跑一遍并逐条归因。
- 未跑 `packages/skill-calorie/scripts/check-warning-line.mjs` 台账（两件 126／311 LF 均在线内，
  但本席只做了行数读取，未跑台账脚本）。
- 真数据页只有 `order184` 一份（同本票范围）：其余真数据页在四列口径下的表现**无人证**，
  若要扩大到全 `wake` 词的真数据页，需另开一轮。
- 缺一件「判据④ 逐表口径」的回归测试：本轮只由本席探针在**产物**上代偿；若下一手改判据，
  建议把「多表页抽首表必红」写进机检自证。
