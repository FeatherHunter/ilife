# #498 改判结论：不补冻结表行，转 #471 统一处置

- 票：#498（卡路里场景10-A：3 条新词补冻结表行）；判定：方法改判，原地不落地；结论由编排者在 #498 评论回写。
- 对象词：看组合分析、看热量趋势、看整体趋势（三条均为本轮新造词）。

## §1 对抗式核查（开工后实测，非转述）

- 三条词当刻**已经能跑**：路由层 `packages/skill-calorie/src/analysis/routes.ts` 三条均为 `list:'new'` + `kind:'exec'`，命令 `calorie.view.combined` / `calorie.view.calorie-trend` / `calorie.view.long-trend` 皆可执行；#161 的 Destination（词跑得通 + 产物是真 HTML）**不依赖**这三条冻结表行。
- 三条词不在 #161 的分母里：#382 冻结算的分母是「预测 20 + 报告 8 + 缺口 1 + 别名 2」；这三条属场景 10-A（#160，已关）。
- 唯一的真缺口是 **HELP 现找查不到**（`calorie.help.lookup` exit 4）与链页占位——而这正是 **#471** 的题目：`list:'new'` 全族 **69 条**词不入速查台／lookup，跨 9 个场景，动的是全部场景共用的取数面（`packages/skill-calorie/src/photo/helpCenter.ts:213`）与检索入口（`packages/skill-calorie/src/triggers/help-lookup.ts:108-120`），且 #471 明确写了三条候选（A 纳入速查台数据源／B 加「新词别名」一节／C 维持现状写清口径），**尚未裁定**。
- #391 已独立复核过同一批读数（看热量缺口／看体重预测两条 `list:'new'` 皆 `exit 4`、速查台正文 0 次命中），并据此把「HELP 检索面 69 条」单独立为 #471、明确**不挂** #161 子票。

## §2 为什么「补冻结表行」不是能落地的做法（机器读数）

- 三条词一旦进冻结表 `src/triggers/scene-10-analysis.ts`，该表的计数从 176 变 179，触发**基准重算**连锁：`test/calorie-triggers.test.mjs`（总数 436／十场景计数／wake_multiset／逐条 sha／getSummary）、SoT 快照 `test/calorie-sot.snapshot.json`（total／scene_counts／wake_multiset／summary／entry_sha）、以及散在 **约 20 个测试件**里的硬编码 436（含 HELP 速查台 436 卡／54 子功能／1308 复制按钮、三态同源 436、`wake-assets-133` 多重集、`t291`／`no-script-commands-180` 的 436、`calorie-routing-81` 索引对齐等），还要重出 smoke。
- 实测（15 门批量，`.scratch/t498/logs/redlist.log`）：该批次 exit 1，逐条红点即上述计数族。
- 快照工具 **不支持新增 id**：`packages/skill-calorie/scripts/gen-sot-snapshot.mjs` 头注写明**只重写 `entry_sha` 段**、逐行替换那 16 个十六进制字符（禁整体回写，理由是 `scene_counts` 的 `01…10` 顺序会被 JS 重排）；新增条目没有落点。
- 另有一条**先例债**：`fa17ba8f feat(621): 定运动目标写命令加判据与证据` 往 `scene-06-goal.ts` 加了一行，未同步快照与计数，故**迁移前**该族守卫就已经是 437 vs 436 的红（实测当前树：各场景 head 计数 9/70/58/39/32/**26**/4/13/10/**179**，快照仍 436）。
- 并发面：硬编码 436 的清单里 `packages/skill-calorie/test/cmd-read-t11.test.mjs`、`packages/skill-calorie/test/render-copy-90.test.mjs` **当刻正被他席改着**（未提交），按路径所有权本票不得触碰。
- 结论：这是**一次约 20 件的基准重算**（仓库确有这条流程先例，`f85e169a feat(332): … SoT 快照重算＋路由计数同步＋smoke 重生成`），不是「补 3 行」的小活；而且它**预先替 #471 裁掉了 69 条里 3 条的可见性策略**——若 #471 选候选 A（把 `new` 族纳入速查台数据源），这三条根本不必进镜像表。

## §3 处置（本轮实际动作）

1. **不落地**：`packages/skill-calorie/src/triggers/scene-10-analysis.ts` 已逐文件还原到 HEAD（实测 wake_word 计数回到 176，该路径 `git status` 干净），上一手未提交的三行与未跟踪的证据骨架一并清除（骨架系失效半成品，按无主 diff 处置）。
2. **转 #471**：把这三条作为该族的具体实例补进 #471 评论（附本件路径），由 #471 的裁定统一决定「要不要可见、怎么可见、可见面在哪」。
3. **本票关票理由**：#161 的 Destination 不依赖这三条行；真缺口（HELP 现找）已在 #471 名下待裁；本票按「先判是不是问题、不是就关」的口径收口，不产生大改。

## §4 遗留

- #471 裁定后若选「纳入速查台」，本票要的那三行**仍然不需要**（数据源面即覆盖）；若选「B 加新词别名一节」，同理由 #471 一并处理；若选「C 维持现状」，此三词 HELP 查不到即属既定口径，须在 HELP 文案里说清。
- 链页占位（`.scratch/t381/chain.html`）系场景 10-A 交付期的草稿件，#160 已关，不再作为交付面；如需链页显示 `new` 族的 prompt，随 #471 或新票处理。
