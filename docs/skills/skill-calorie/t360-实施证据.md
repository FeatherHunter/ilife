# t360 实施证据 · 看围度趋势（不带部位也出趋势）

> 实施者只出活、不关票；到 95% 停，等独立复核。票面旧名 `t8-趋势证据.md` 是旧十七票编号残留，本件用 `t360-实施证据.md`。
> 路径所有权（本票只写这些）：`packages/skill-calorie/src/body/view.ts`、
> `packages/skill-calorie/src/body/bodyPlate.ts`、`packages/skill-calorie/src/body/bodyDocs.ts`、
> `packages/skill-calorie/src/fetch/body.ts`；测试 `packages/skill-calorie/test/t360-measure-trend.test.mjs`；
> 证据本件；草稿 `.scratch/t360/`；changeset `.changeset/t360-measure-trend.md`。
> 未碰：`src/triggers/routes.generated.ts`、围度列表结构（归 #361，本票只修缺值格）、他票任何路径。

## 一、冻结种子与手算期望（t169 来源①手算，不拿新输出当期望）

种子（测试内相对日 `dayBefore`，防墙钟滑出 90 天窗）：

| 行 | 日期 | chest_cm | waist_cm | 备注 |
|---|---|---|---|---|
| D4 | 4 天前 | 95 | — | d4 |
| D3 | 3 天前 | 94 | 80 | d3 |
| D2 | 2 天前 | — | 79 | （空） |
| D1（最新） | 1 天前 | — | 78 | d1 |

手算（唯一期望源）：

- 最近有数据部位＝`waist_cm`（腰围最新 D1 ＞ 胸围最新 D3）；固定首项 `chest_cm` 是旧口径（老
  `render_body_measurements_view.py:96-118` 首个有数据即 active，不比日期）。
- 腰围趋势序列（按日 ASC）＝ [80, 79, 78] ⇒ **N＝3**；胸围点数＝2（负向改回固定首项时读到 2）。
- 五项 KPI：点数 3 ／ 均值 (80+79+78)/3＝79 ／ 最小 78 ／ 最大 80 ／ 变化量 78−80＝−2；最新 78。

## 二、实现形状（4 件）

1. `src/fetch/body.ts`：新增 `latestMeasurementMetric(db, {days/dateFrom/dateTo})`——窗口内逐部位取
   最新非空 `(date DESC, id DESC)` 再取最大者；平局按 `MEASUREMENT_FIELDS` 既有次序取首位（确定性）；
   窗口与 `listMeasurements` 同口径（显式区间优先，否则 `days`）；全无数据→`null`（空库语义不变）。
2. `src/body/bodyPlate.ts`：`buildBodyMeasureView` 不带部位时——全量表仍未过滤列表（归 #361，
   本票不动表结构）＋ 用 `latestMeasurementMetric` 挑部位算 `trend`；新增 `autoMetric` 与
   `kpi`（`measureTrendKpi(trend)` 单一来源，口径照老 `:454-462` `kpiFor`，均值 2 位、`n<2` 变化量 `null`；
   `bodyDocs` 只渲染不重算 ⇒ KPI 与图必然一致）。带部位时行为不变，另加该部位无数据时的兜底分支
   （库非空但该部位无行 → 空 KPI＋空趋势＋空表，不抛；真空库仍抛 `missing-data`）。
3. `src/body/bodyDocs.ts`：KPI 网格＝原 3 卡（围度看／最新／趋势点，不动，保 #41／#109 针）＋
   新增均值／最小／最大／变化量 4 卡——五项趋势 KPI（趋势点／均值／最小／最大／变化量）齐；
   图闸门＝`trendMetric (metric ?? autoMetric)` 有值且 `trend` 非空 → `<部位>趋势` 线图，
   否则有部位无趋势 → `renderEmptyBlock({title:'<部位>趋势', text:'该部位暂无趋势数据'})`
   （照老 `:504` 文案，图注仍注部位）；表体缺值格 `''`→`'—'`（老 `:385`），备注沿旧口径。
4. `src/body/view.ts`：注释更新；`metrics` 键不变（`total/latestVal/trendDays`），`trendDays` 现为所选部位点数。

## 三、判据（跑哪条命令 → 看哪个读数 → 等于什么）

测试 `packages/skill-calorie/test/t360-measure-trend.test.mjs`（8 用例），一律走 CLI 真出口
（`SKILLS_DB_PATH` 指 mkdtemp 真库零写入），可见文本走剥标签（去 script／style／注释／标签）判读：

- D1 路由示例：跑 `calorie-cmd-read calorie.view.body-measure --params '{"days":90}'`
  → 读 exit → 等于 0（`readAutoPage` 首断言）。
- D2 自动挑部位：同上跑 → 读可见文本 → 含「腰围趋势」且不含「胸围趋势」；
  读 `latestMeasurementMetric(db,{days:90})` → 等于 `waist_cm`。
- D3 图区非空：同上跑 → 读 html → 含 `<svg` 且无 `data-chart-empty`，可见文本无「该部位暂无趋势数据」。
- D4 恰 N 个趋势点：同上跑 → 读 KPI「趋势点」→ 正则 `趋势点\s*3\s*天`；
  读 sqlite（窗口内腰围按日聚合计数，同 `trendMeasurement` GROUP BY 口径）→ 等于 3；
  读 `buildBodyMeasureView(db,{days:90})` → `trend.length` 等于 3、`kpi.count` 等于 sqlite 计数。
  机器读数：`T360-READOUT N=3 sqlite=3 部位=waist_cm`。
- D5 五项 KPI：同上跑 → 读可见文本 → `均值\s*79\s*cm`、`最小\s*78\s*cm`、`最大\s*80\s*cm`、
  `变化量\s*-2\s*cm`、`最新\s*78\s*cm`；读 plate `kpi` → `[3,79,78,80,-2]`；趋势序列 → `[80,79,78]`。
- D6 裁定 2 表体：同上跑 → 读 caption 含「围度记录」表 → D4 行腰围／腹围格等于 `—`；
  读全表体 → 无连续两个空单元（`!(c[i]===''&&c[i+1]==='')` 逐行成立）。
- D7 样本不足兜底：跑 `--params '{"metric":"shoulder_cm","days":90}'`（库非空、该部位无行）
  → 读 exit → 等于 0；读可见文本 → 含「肩宽趋势」＋「该部位暂无趋势数据」；
  读 KPI → 均值／最小／最大／变化量／趋势点五格全「—」；读 html → 无 `<svg`；读表 → 「该项目无记录」。
- D8 空库语义不变：空库跑（无参／`{"metric":"waist_cm"}`）→ 读 exit → 等于 4，且 stdout 为空。
- D9 整页六项：D1 落盘页 → 读首行／charset／内联样式字节／外链／回执绝对路径 → 等于
  `<!doctype html>`／含 `charset=utf-8`／≥2048B／零外链／绝对路径可读且含「腰围趋势」。

## 四、负向对照（源码级变异两行机器读数）

把 `latestMeasurementMetric` 尾行改回固定首项（`return MEASUREMENT_FIELDS[0]`，即老口径）：

- 改坏必红：`node --test t360-measure-trend.test.mjs` → `ℹ pass 4 ℹ fail 4`
  （红在「自动挑部位／恰 N 个趋势点／五项 KPI／整页落盘图题」4 处；兜底／空库／裁定 2／路由 exit 照旧绿）。
- 还原必绿：逐文件取回原文件（sha 复核见 §六）＋ 重编 → 同命令 → `ℹ pass 8 ℹ fail 0`。

## 五、相邻门回归（红点逐条点名归属，绝不假绿）

- `render-t41 + fetch + deprecated-126`：`ℹ pass 20 ℹ fail 2`——
  ① `#41 计划三盘`（`render-t41:136`，`buildPlanWizardView` 文案 `已检查 1 个会话（N硬止）`，属
  `workout/*` 在途，与本票零交集）；② `#41 缺失阻断`（`render-t41:239`，
  `buildVolatilityView` 空库文案，属 `weight/*` 在途）。身体系两盘 ✔（含 `buildBodyMeasureView`
  空库 `无围度` 阻断 `:241`），围度／体成分取数 ✔。
- `sport-homogeneity-109`：`ℹ pass 10 ℹ fail 2`——① 唤醒词错键「看今日运动」
 （`exercise-records` vs `exercise`，属 `exercise/*` 路由在途）；② 空库阻断卡在首例
  `calorie.view.weight`（属 `weight/*` 在途）。关键：**「#109 围度：单项趋势＋全量表」✔**
  （`全部围度`／全量表值／`腰围趋势`针全过）。
- 初轮 `tsc -b` exit 1 仅 `weight/receipt.ts(113/141)`（`M` 未提交，他席 #337 在途）；
  终轮 `tsc -b` exit 0（树绿）。本票 4 件编译零错误（dist 含 `latestMeasurementMetric`／
  `measureTrendKpi`／`变化量`／`renderEmptyBlock` 已验）。

## 六、门禁对账（GATE-RUN 声明，边跑边补；源 `.scratch/locks/gate-runs.log`）

- GATE-RUN runId=5df370da-4721-4ede-9a37-de23903ab513 cmd=npx tsc -b packages/skill-calorie（exit=1，他席 2 红，§五）
- GATE-RUN runId=1e913663-92dd-4874-b05c-06c74b52f954 cmd=node --test packages/skill-calorie/test/t360-measure-trend.test.mjs（exit=0，8/8）
- GATE-RUN runId=2a43b0e7-7770-4965-b3c8-b13f34e667eb cmd=node --test packages/skill-calorie/test/render-t41.test.mjs packages/skill-calorie/test/fetch.test.mjs packages/skill-calorie/test/deprecated-126.test.mjs（exit=1，20/22，2 红归属 §五）
- GATE-RUN runId=c55a0275-94b7-41b3-9396-c3023576709f cmd=node --test packages/skill-calorie/test/sport-homogeneity-109.test.mjs（exit=1，10/12，2 红归属 §五，围度段 ✔）
- GATE-RUN runId=884f4d34-c2af-4ecc-8160-bc4b8496436e cmd=npx tsc -b packages/skill-calorie（exit=0，变异轮重建）
- GATE-RUN runId=9d37c11b-7e41-4f06-b42d-3f4ed220148e cmd=node --test packages/skill-calorie/test/t360-measure-trend.test.mjs（exit=1，变异红 4/4，§四）
- GATE-RUN runId=5fec4cb9-18ac-4621-9f3e-f17e11c2919d cmd=npx tsc -b packages/skill-calorie（exit=1，他席 `weight/review.ts` 破树中，§七）
- GATE-RUN runId=346c8289-5bc2-49c2-ad5b-a9e6ec493dfd cmd=node --test packages/skill-calorie/test/t360-measure-trend.test.mjs（exit=1，他席破树连带，§七）
- GATE-RUN runId=81625419-2cce-4dc4-ac6c-b5e4a601040a cmd=npx tsc -b packages/skill-calorie（exit=0，终轮树绿重建）
- GATE-RUN runId=55491ff1-99fd-4273-8571-5a48e6329520 cmd=node --test packages/skill-calorie/test/t360-measure-trend.test.mjs（exit=0，还原后 8/8）
- 变异还原一致性：`src/fetch/body.ts` 还原后 sha256
  `97EA902C02F56C1809613A5B37B439A28F75391304916A96E4F03C059E2C0A33` 与变异前逐字相同。

## 七、过程记录（等待量可复核，落盘 `.scratch/t360/`）

- 锁等待：`1e913663` waitedMs=30003、`2a43b0e7` waitedMs=30003（等 ticket 339 活锁）、
  `c55a0275` waitedMs=160011（等活锁，活进程一律不抢回）、`81625419` waitedMs=10002；其余 0。
  均以包装器留痕为准（`gate-runs.log` START／RUN 配对）。
- 他席破树连带一次：`346c8289` 轮 `dist/weight/review.js:428` 含他席半成品语法错
  （源 `src/weight/review.ts` `M` 未提交，非本票路径，未碰；待其修好后重跑 `81625419`＋
  `55491ff1` 复绿）。`dist/` gitignored，重编即复位，无残留。
- 长命令一律落盘（`.scratch/t360/{compile,test,regress,regress109,mut-*,restore-*}.log`）＋标记行＋只读尾行；
  红才查 FAIL 签名。`true-db-check.cjs` 为真库只读探针脚本（见 §八）。

## 八、真库零写入

一切数据走 `SKILLS_DB_PATH`＝mkdtemp（测试源码逐行可查，无真库路径写入）；
真库 `D:\2Study\StudyNotes\.db\calorie_data.db` 只读探针：
`integrity_check=ok`，`body_measurements=1 body_composition=0`，
文件 sha256 `57CE9D87F5F232822332D7057C2AE8411D99BA7FDECEA289F2028E19FC3FFC15`
（mtime 2026-09-14 13:53:37，早于本票测试窗，无写入）。

## 九、融合验收逐条（基准 t395）

- 裁定 4：① 可见「腰围趋势」（D2）② 图区 `<svg` 非空态（D3）③ KPI「趋势点」3＝sqlite 3（D4）——成立；
  反向（无数据部位）图注仍写「肩宽趋势」（D7）——成立。
- 裁定 2 表体：缺值格「—」（D4 行断言）且全表无连续空单元（逐行断言）——成立；
  表头＋复制区判据归 #361（同一分支 `bodyDocs.ts:123` 之后，本票不动）。
- 样本不足兜底（D7）：KPI 五格「—」＋图区「该部位暂无趋势数据」——成立（老 `:491／:496／:507` 对应）。
- 叠加图（老 `:433-448`）与降序摘要（老 `:527-534`)：本页无对比块（对比页在 `compare.ts` 独立成页，
  t395 §三-8 新保留）——本票记 N/A，适用时由对比页承担。
- 数字动画（老 `:326-334／:333-347`）：老页交互未移植登记项，不作验收——未做。
- 排期：`bodyDocs.ts` 图闸门与 KPI 已在本票落地，**#361 必须排在本票之后**（本票是 #361 前置）。

## 十、未做项 ＋ 下一手缺什么（95% 停）

- 未做：独立复核（必须他席做，实施者不得兼任）；#361 全量 13 列表（等本票关票后才开）；
  数字动画；`trend` 在显式 `dateFrom/dateTo` 下仍按 `days` 取（沿旧口径，见 plate 注释；要用区间时另开票）。
- 下一手（复核席）：① 持锁独立复跑 `node --test packages/skill-calorie/test/t360-measure-trend.test.mjs`
  ＋ 自设 1 个新探针（§5.1，如：同日多行 id  tie 或三部位同日）；② 归属复核（本票件外红点 §五）；
  ③ 关票与 #361 放行由编排者收口（本票不 push）。
- 提交清单：代码／测试／证据分步提交（`feat(360)` 中文信息），sha 清单见 §十一。

## 十一、提交-phase 对账补记 ＋ sha 清单 ＋ 他席越界事件

- GATE-RUN runId=d5a5e7a8-3e11-468e-bc73-ffba44626eea cmd=git add -- packages/skill-calorie/src/body/view.ts packages/skill-calorie/src/body/bodyPlate.ts packages/skill-calorie/src/body/bodyDocs.ts packages/skill-calorie/src/fetch/body.ts（exit=0）
- GATE-RUN runId=3c632ab7-19dc-43de-a829-b83dac827c49 cmd=git commit -F .scratch/t360/msg1.txt -- （同上 4 件）（exit=0）
- GATE-RUN runId=8d8dfa95-d01d-4909-9398-69d793c569ca cmd=git add -- packages/skill-calorie/test/t360-measure-trend.test.mjs（exit=0）
- GATE-RUN runId=b6007d5e-ab18-4996-aaf0-d40ccde8dc58 cmd=git commit -F .scratch/t360/msg2.txt -- （测试件）（exit=1：暂存件被他席提交卷走，见下）
- GATE-RUN runId=3ab17cc0-8a9d-48f1-9d65-be860452b9ff cmd=git commit -F .scratch/t360/msg2.txt -- （测试件）（exit=0，重试落地）
- GATE-RUN runId=9e80e3fa-82c4-40ca-b81e-1efbc3be336a cmd=git add -- docs/skills/skill-calorie/t360-实施证据.md .changeset/t360-measure-trend.md（exit=0）
- GATE-RUN runId=074dd40d-743e-4ed9-917c-5f3be3b4b983 cmd=git commit -F .scratch/t360/msg3.txt -- （证据＋changeset）（exit=0）
- sha 清单：`34d7aa3`（4 源码件）／`3e7f629`（测试件）／`1b3eb09`（证据＋changeset）；
  本补记提交后 HEAD 另见回执。不 push（编排者统一收口）。
- 他席越界事件（上报编排者裁决，本票未动他人任何件）：本票 `git add` 暂存测试件后、
  `b6007d5e` 提交前，他席 #406 把本票暂存件卷入其提交 `578959c`（stat 含本票路径）；
  随后 `578959c` 从主线消失（疑似 reset／rebase，禁令动作），暂存区遗留本票 `A` 条目；
  本票核对（暂存＝工作区内容逐字一致、暂存区唯我一件）后以 `3ab17cc0`（`3e7f629`）正常提交收回。
  内容零损失，归属以 `3e7f629` 为准。
