# #527 过程与结果族 整改文档（作者自评）
>
> 票面：`gh issue view 527`；共用简报 `.scratch/scene09fix/seat-brief.md`；基准 `docs/skills/skill-calorie/t524-场景09-视觉整改基准.md`。
> 写集：`src/photo/gifDoc.ts`｜`pickerDoc.ts`｜`picker.ts`｜`src/render/wizardPortDocs.ts`（归属复核见 §8）＋本域新件。
> **一页一提交**：每页做完就把该页读数追加到本文件，再开下一页（§7.5 收口纪律）。

## 0 夹具与复现（改前读数同源）

| 项 | 值 |
|---|---|
| 隔离 stage | `.scratch/t527/stage/`（`calorie_data.db` ＋ `CalorieHub/`，从 `.scratch/t287/backup-real/` 复制） |
| DB | `SKILLS_DB_PATH=.scratch/t527/stage`（22 行 body_photos） |
| 照片 | `CALORIE_PHOTOS_DIR=.scratch/t527/stage/CalorieHub` |
| 今天 | `CALORIE_TODAY=2026-09-06`（交付样张 2026-09-15 08:33 落盘时的相对时间口径） |
| 生成器 | `node .scratch/t527/gen-pages.mjs .scratch/t527/pages`（可 `--deliver` 直接写交付目录） |
| 锁定包装 | 构建／生成／量测一律 `node tooling/run-locked.mjs --ticket 527 -- <cmd>` |

> **夹具漂移（必须写清，否则读数会被误读）**：交付目录 2026-09-15 08:33 那批样张生成时，照片目录里
> `2026-05-30_001.._008`、`_009`、`_010`、`_012`、`2026-06-01_001.._004`、`2026-06-09_001.._004` 等 22 张
> **真图都在**（故 09-08 单页 18.8 MB、09-14 23 MB）。此后这些文件已从照片目录消失（现在只剩 4 张真图：
> `_011`／`2026-06-01_003`／`2026-06-09_001`／`2026-07-17_001`），所以**本票重出的同参数页面字节必然小于交付件**。
> 语义、版式、读数口径不受影响；「改前字节」一栏同时给「当刻夹具」与「交付件」两个数。

## 1 改前读数（当刻夹具，2026-09-15 本席位实测）

生成：`run-locked --ticket 527 -- node .scratch/t527/gen-pages.mjs .scratch/t527/pages`

| 样张 | 当刻夹具字节 | 交付件字节 |
|---|---|---|
| 09-05 GIF 结果页 | 75575 | 71171 |
| 09-06 预检确认页 | 67677 | 64951 |
| 09-07 GIF 规划器 | 76649 | 72459 |
| 09-08 删照候选 | 335980 | 18827839 |
| 09-14 删照候选快照 | 4519968 | 23011477 |

## 2 09-07 GIF 规划器（`src/render/wizardPortDocs.ts` 的 `buildGifPlannerDoc` ＋ 新件 `photoUi.photoPickRows`）

### 2.1 改了什么（逐条对票面六条判据）

| # | 改前 | 改后 | 落到哪 |
|---|---|---|---|
| ① 形状化 | 副标题 `挑照片 → 调快慢与尺寸 → 复制指令`；KPI 明细 `500ms/帧 · 无限循环`；候选区是**六列表**（`ID／日期／标签／文件／照片文件／裁剪`），窄屏靠横滑 | 副标题改一句人话；输出规格撤到卡下**事实条**（`renderFactStrip`：每帧停 0.5 秒／无限循环）；候选区改**一行一张照片**的候选行（编号槽／标签槽／文件名槽／异常徽标槽 ＋ 次要行徽章列），`renderListRows` 形状本域自建件 `photoPickRows` | `wizardPortDocs.ts:116-140`／`photoUi.ts:photoPickRows` |
| ② 文案 | `#31 ✓`（票号式内部写法）、`ID` 列头、`500ms/帧`、`标签` 字段名里的留空说明、`duration/width/height` 的 `50..5000`／`100..2000` 只住在空栏才可见的 hint 里 | `照片 31` ＋「已框选／没框选」徽章；单位与范围进字段名（`每帧多久（毫秒，50 到 5000）`）；`..` 改「到」；`选一个` 占位整体撤掉（当刻值即所选那条） | 同上 |
| ③ 手机端 | 390 档候选表横滑、无 44px 行高 | 候选行窄屏 44px 行高、次要行缩进对齐；页内样式走 `photoUiCss()`（断点 820／640） | `photoUi.ts:phu-pl-*` |
| ④ t524 §3.2 字号下限 | 390 档表头 11px（共享块层 `blocks.ts:1709`） | 本域页面把 `th` 托到 **12px**（`photoUiCss` 里两条，桌面档 11.5→12 一并在内）；本页候选区已无表 | `photoUi.ts` |
| ⑤ t524 §3.2 触摸目标 | 1440 档复制按钮 40px 高 | 本域页面把 `.ilife-copy-btn`／`.ilife-copy-menu-item` 托到 **44px** | `photoUi.ts` |
| ⑥ 过程型「一眼看出这是 AI 已用的值」 | 表单上方那句 `这些是 AI 已经用的值；改了不会自动生效——…` | 拆句为 `…已经用的值。改了不会自动生效——…`（`；` 是并列语义） | `wizardPortDocs.ts:formNoticeHtml` |

### 2.2 读前／读后（机器门）

| 门 | 改前 | 改后 |
|---|---|---|
| `audit-separators.mjs`（节点级命中） | **15**（`·`2／`；`2／标识符11） | **1**（只剩指令预览块那处 `命令键+命令原文`，见 §2.3） |
| `measure-responsive.mjs` 三档溢出 | `0／0／0` | `0／0／0`（回归门保持） |
| 三档最小字号（正文类，自造探针 `.scratch/t527/fmt-probe.mjs`） | 390 档 **11px**（`th`）／768 档 12px／1440 档 12px | 390／768／1440 三档均 **12px**（此刻 `th` 已不在本页） |
| 触摸目标 <44px（三档） | 1440 档 **4 处**（复制按钮 40px） | 三档 **0 处** |
| 页字节 | 76649 | **79739** |

### 2.3 遗留（本页 1 处，跨票性质）

指令预览块（`<pre class="ilife-block-pre-block-code">`，`promptCopyArea` 产物）里的 `calorie-cmd-read calorie.photo.gif`
被探针按 `R7 内部标识符` 计一处。三件事决定它不在本页就地清零：

1. 该块是**给 AI 照抄即跑**的机器内容（`wizard-86.test.mjs` 与 `photo-picker-283.test.mjs` 都把「可见文本里的
   ```bash 块」当**契约**判——抽出来真跑必须 exit 0），删它等于毁掉「照抄即跑」；
2. 该形状由公共共用件 `src/shared/copyArea.ts` 的 `promptCopyArea` 产出，**不在 #527 写集**（归 #525／命令登记面）；
3. 交付基准自身就带这条债：09-14 交付件同一处命中 `命令键+命令原文+票号`；09-05 交付件的复制载荷也含命令键。

**建议**（不动手，留给裁决）：把 prompt 的**可见预览**收成一句人话 ＋ 复制按钮，可执行命令只住 `data-t`
（探针按标签剥离，天然出局）。这样 5 件样张的那一处能一起归零；它同时影响 #528 的五张回执页，宜一次做在共用层。

