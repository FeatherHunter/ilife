# t520 重评4-MA1 · 地图 #161 票 #520 独立微评席 MA1

- 尺：`docs/skills/skill-calorie/t520-重评-V2.md` 五维：结构 20／信息 25／分隔符 20／跨宽 15／手机端 20；加权总分＝结构×0.20＋信息×0.25＋分隔符×0.20＋跨宽×0.15＋手机端×0.20；90 线＝逐页≥90。
- 基线（对照用，不照抄）：14/94.88、15/96.13、16/95.63。
- 改动面（`t625-第四轮-证据.md` §1–§3）：Forecast 结论改综合不断复述数（14–16 结论去数）；其余卡／导航／表／口径不动。
- 前席探针验货：`.scratch/t520-R2-V2/verdict-lines.txt` 14–16 结论仍是旧文案（含 1601/1640/1743＋目标 1800），与本轮产物结论（14「7 天后离目标还差一些」／15「30 天后…」／16「90 天后…」）不一致，结论复述相关行判为过期不复用；结构类探针（nth-child 缺席、tabular-nums、44px、caliber 竖线）经本轮源码复核后再用。
- 旧截图核对：`.dsh-vision-router/artifacts/` 内历史截图文件名均为旧轮次产物编码，无本轮 14–16 标识，判存疑，全部重拍。
- 范围：只读 `D:\ilife\.scratch\t387\html\` 内 `14-`、`15-`、`16-` 开头 3 页；不改产物／源码，不提交。
- 方法：每页 `vision_html_screenshot`（1440×900／390×844 fullPage）＋ `vision_describe`；工具坏照实记；每条扣分回 HTML 源码核对，找不到写「源码未证实」不扣分。

## 身份表

| 页 | 文件 | 结论（本轮源码实测 1858 行） |
| --- | --- | --- |
| 14 | `14-摄入预测(按当前速率1周).html` | 按当前速率，7 天后离目标还差一些。 |
| 15 | `15-摄入预测(按当前速率1月).html` | 按当前速率，30 天后离目标还差一些。 |
| 16 | `16-摄入预测(按当前速率3月).html` | 按当前速率，90 天后离目标还差一些。 |

## 14 摄入预测（按当前速率 1 周）

文件：`.scratch/t387/html/14-摄入预测(按当前速率1周).html`。实拍：1440×900 fullPage（pageHeight 1280）、390×844 fullPage（pageHeight 1392）。本轮重拍（旧截图无本轮标识，判存疑不用）。

### 六问逐条

1. **第一眼／层级**：眉题「趋势分析」→H1「摄入预测（按当前速率 7 天）」→三胶囊→蓝色结论条→页内导航→四节（参数／概览／摄入预测轨迹／数据与日志），层级清楚，双档一致。结论条本轮已改（1858 行）：「按当前速率，7 天后离目标还差一些。」——**不带任何数字**，方向定性句。**残余**：胶囊「摄入预测」不带天数（天数只在标题与参数里），与 09–13 胶囊带参做法不一致（源码 1858 行三 `ilife-block-chip` 确证）。
2. **挤／空／错位／截断／横滚**：双档无横滚、无截断、无压字。桌面表为 `width:auto;margin-left:0` 紧凑左置（1838–1839 与 1854/1856 行），三列本身比例正常，但 2 行小表只占左约 1/3 宽、右侧留空，而下方「复制数据／复制日志」按钮行却是满宽拉开（实拍亲见），表与按钮行宽幅不配，节奏轻扣。手机档两行转纵向卡片后无此感。
3. **符号拼串**：**无**。脚注三 span 间为 CSS 竖分隔线（1177–1192 行规则仍在，桌面实拍亲见浅色竖线）；结论用逗号断句；区间用「1515 至 1687」汉字（1866 行），非符号拼接。`vision_describe` 所称「摄λ」希腊字母，**源码全文无 `λ` 命中**，实拍两档脚注均为「按最近的趋势线往外推」汉字——**源码未证实，不扣分**。
4. **重复与英文上屏**：**本轮改动点，正向**。V2 的「1601 三处」（结论条／卡／表）已消除一处：结论条去数，只剩卡「摄入预测 1601 卡／比目标低 199 卡」＋表末行 `1601` 的常规主从复现（卡为汇总、表为明细，属正常粒度分层，不再记硬重复）。`1589` 仍两处（卡「当前摄入」＋表首行，1866 行）。英文标识符未上屏（`horizonDays: 7`／`calorie.view.predict` 只在 `data-t` 属性，1869–1878 行，源码确证）。
5. **数值列**：通过。`text-align:right＋tabular-nums`（1844 行源码确证；桌面实拍亲见 1589/1601/区间三列右对齐，`vision_describe` 称「左对齐」与源码实拍俱冲突，以源码为准）。整数与一位小数（1.7）各自成档，无缺值样本故 `—` 规则不涉。无 09–13 的 `nth-child(3)` 28px 特例（全文无命中）。**残余（冻结）**：首行「预计区间」仍单值 `1589`（1866 行，R-18 冻结，S3-9）；caption「共 7 天，每周一点」与 2 行数据仍字面不配（1866 行）。
6. **手机档**：通过。`@media (max-width:640px)` 卡片堆叠（1846–1849 行：行变卡、`td:flex＋space-between`、`::before` 取 `data-label`），390 实拍亲见：两张小卡、日期 `2026-09-16/23` **完整未折断**（`white-space:nowrap`，1847 行；`vision_describe` 一处称折断自相矛盾，以亲见＋源码为准）、数值贴右、无横滚。触摸目标 `min-height:44px` 多处（237/279/377/1163/1821 行）。**残余**：2 行内容＋灰色 disabled「复制日志」使窄档偏空（与 V2 同）。

### 得分

结构 19／20；信息 22.5／25（结论去数消除三处硬重复，＋1.0；余下扣首行区间单值、caption 行数不配、1589 两处）；分隔符 20／20；跨宽 14／15（扣表窄左置与满宽按钮行不配）；手机端 18.5／20。

**加权总分 ＝ 19×0.20＋22.5×0.25＋20×0.20＋14×0.15＋18.5×0.20 ＝ 3.80＋5.625＋4.00＋2.10＋3.70 ＝ 19.225/20 ＝ 96.13/100。**

## 15 摄入预测（按当前速率 1 月）

文件：`.scratch/t387/html/15-摄入预测(按当前速率1月).html`。实拍：1440×900 fullPage（pageHeight 1458）、390×844 fullPage（pageHeight 1808）。本轮重拍。

### 六问逐条

1. **第一眼／层级**：眉题「趋势分析」→H1「摄入预测（按当前速率 30 天）」→三胶囊→结论条→四节，清楚。结论条（1858 行）：「按当前速率，30 天后离目标还差一些。」——「30 天」是窗口参数非复述指标数，差额数（1640/160）下沉到概览卡，定性＋定量分层。**残余**：胶囊不带天数（同 14）。
2. **挤／空／错位／截断／横滚**：双档无横滚、无截断、无压字（实拍确认；6 行三列桌面整齐，区间「1515 至 1687」等完整）。桌面沿用 cols3 紧凑约束（1838–1844 行同族规则），6 行体量下观感紧凑，无 14 页的窄表空荡感。
3. **符号拼串**：**无**。区间一律「X 至 Y」汉字（1866 行：1515 至 1687／1491 至 1735／1476 至 1774／1465 至 1809…）；脚注三 span＋竖分隔线（1884 行）；`vision_describe` 把竖线读成 `|` 字符，**以源码为准，不作扣分依据**。
4. **重复与英文上屏**：**本轮正向**。V2「1640 三处」消除一处：结论去数，只剩卡「摄入预测 1640 卡／比目标低 160 卡」＋表末行 `1640` 的主从复现；`1589` 两处（卡＋表首行）。英文标识符未上屏（`horizonDays: 30` 等只在 `data-t`，1884 行）。
5. **数值列**：通过。右对齐＋tabular-nums（1844 行同族规则）；整数与一位小数（1.7）成档统一，无缺值样本。无 `nth-child(3)` 特例。**残余（冻结）**：首行区间单值 `1589`（1866 行，R-18）；caption「共 30 天，每周一点」6 行倒是配得上，但「每周一点」在 30 天窗下仍字面别扭（轻记）。
6. **手机档**：通过且为本批最规范：卡片堆叠源码确证（1846–1847 行 `tr{display:block…}`＋`td{display:flex…}`＋`white-space:nowrap`），390 实拍 6 张小卡、日期 `2026-09-16…2026-10-16` 完整未折断、无横滚。触摸目标同族 44px。**残余**：6 张卡纵向信息量低于桌面（与 V2 同，轻记）。

### 得分

结构 19／20；信息 22.5／25（结论去数＋1.0；余扣首行单值、caption 字面、1589 两处）；分隔符 20／20；跨宽 15／15；手机端 19／20。

**加权总分 ＝ 19×0.20＋22.5×0.25＋20×0.20＋15×0.15＋19×0.20 ＝ 3.80＋5.625＋4.00＋2.25＋3.80 ＝ 19.475/20 ＝ 97.38/100。**

## 16 摄入预测（按当前速率 3 月）

文件：`.scratch/t387/html/16-摄入预测(按当前速率3月).html`。实拍：1440×900 fullPage（pageHeight 1814）、390×844 fullPage（pageHeight 2640）。本轮重拍。

### 六问逐条

1. **第一眼／层级**：标题「摄入预测（按当前速率 90 天）」→三胶囊→结论条→四节，同族。结论条（1858 行）：「按当前速率，90 天后离目标还差一些。」去数，数住卡（1743／比目标低 57 卡）。`vision_describe` 称「结论条信息密度偏低」——此即改动面本意（结论综合不断复述数），**不扣分**。**残余**：胶囊不带天数（同族）。
2. **挤／空／错位／截断／横滚**：双档无横滚、无截断、无压字。14 行三列（源码 1866 行起，末行 `2026-12-15`；`vision_describe` 误数「5 列」，表实为三列，源码确证，属无害误读）。桌面档 14 行数值列贴右、左侧留白（同族紧凑约束）；手机 2640px 纵向 14 张卡（亲见）。
3. **符号拼串**：**无**。区间一律「X 至 X」（如末行 `1435 至 2052`，亲见；V2 同）；脚注三 span＋竖分隔线；📊 为正常 emoji 前缀。校验：末行 1743，`比目标低 57 卡`＝1800−1743，数对。
4. **重复与英文上屏**：**本轮正向**：`1743` 由三处（结论／卡／表）减为两处（卡＋表末行）；`1589` 两处。英文标识符未上屏（`horizonDays: 90` 只在 `data-t`）。`vision_describe` 所称「参数区日期与表日期重复」（2026-09-03／09-16 两现）——两处角色不同（窗口边界 vs 数据点），属正常主从关系，**不扣分**。`95%` 在脚注「取 95% 置信带」为必要口径词（V2 同口径，不扣分）。
5. **数值列**：通过。右对齐＋tabular-nums（同族 1844 行规则）；14 行整数等差＋12（1589…1743，亲见逐行递增）；1.7 一位小数单例；无缺值样本。无 `nth-child(3)` 特例。**残余（冻结）**：首行区间单值 `1589`（R-18）；`日变化 1.7` 三页同值、窗口算法不可见（V2 同记，轻记）。
6. **手机档**：通过。卡片堆叠（同族 1846–1849 行），亲见 14 张卡标签逐行重复（日期／预测摄入／预计区间）、日期全完整无折断、无溢出；触摸件同族 44px。`vision_describe` 所称 KPI「日变化／摄入预测错位」，亲见 2×2 卡规整，属单位字数视觉差，**源码未证实，不扣分**。**残余**：2640px 长滚动、14 卡连续无分组锚点（与 V2 同，手机端扣分点）。

### 得分

结构 19／20；信息 22.5／25（结论去数＋1.0；余扣首行单值、日变化口径不可见、1589 两处）；分隔符 20／20；跨宽 15／15；手机端 18.5／20。

**加权总分 ＝ 19×0.20＋22.5×0.25＋20×0.20＋15×0.15＋18.5×0.20 ＝ 3.80＋5.625＋4.00＋2.25＋3.70 ＝ 19.375/20 ＝ 96.88/100。**

## 汇总

| 页 | 结构20 | 信息25 | 分隔符20 | 跨宽15 | 手机端20 | 加权总分 | 基线 | 回归 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 14 摄入预测（1 周） | 19 | 22.5 | 20 | 14 | 18.5 | **96.13** | 94.88 | ＋1.25 未退化 |
| 15 摄入预测（1 月） | 19 | 22.5 | 20 | 15 | 19 | **97.38** | 96.13 | ＋1.25 未退化 |
| 16 摄入预测（3 月） | 19 | 22.5 | 20 | 15 | 18.5 | **96.88** | 95.63 | ＋1.25 未退化 |

- 90 线：三页逐页≥90，通过。
- 回归结论：改动面（Forecast 结论去复述数）正向消除 V2 信息维首要扣分项（结论／卡／表三处同数→两处主从），其余维度读数不变（卡／导航／表／口径一字未动，源码逐项复核），三页分数相对基线 ＋1.25，未退化。
- 工具记录：`vision_html_screenshot` 6 次全绿（双档×三页）；`vision_describe` 3 次全绿、无需换措辞重试；误读 3 处已用源码／亲见证伪且均未扣分（14「摄λ」无命中、14 数值「左对齐」与 1844 行冲突、16「5 列」实三列）。
- 残余 Top（跨页同源，不拦关票）：①首行「预计区间」单值 `1589`（R-18 冻结，S3-9）；②caption「每周一点」与短窗字面不配（14 最甚）；③胶囊不带天数（三页同）；④手机长表无分组锚点（16 最甚，2640px）；⑤桌面窄表与满宽按钮行宽幅不配（14 最甚）。
