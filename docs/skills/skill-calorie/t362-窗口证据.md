# #362 看体脂的窗口不再静默截断 —— 实施证据（接管席）

> 权威判据＝票面 `gh issue view 362` 评论区「**编排者口径澄清（2026-09-14）**」；基准＝
> `docs/skills/skill-calorie/t395-融合基准.md` §四 裁定 5／裁定 2、§六 B 区。
> 老正本只读参照＝`D:\2Study\StudyNotes\SKILLS\卡路里\templates\body_composition_view.html`。
> 门禁运行对账导出件＝[`t362-门禁对账.md`](t362-门禁对账.md)（§2.3 第 4 条）。

## 一、判定

**完成度：四块全落地，判据全绿；接管席判「补齐（口径待收窄）」。**

前任席（中途死亡那席）留下未提交改动 4 件，接管席按协议 §3.2 第 4 条**先量现场再判定**（§二）。
量的结论：**实现面已完整**（语法过、四处标题逐条对得上任务清单），缺的是**全部判据侧**——测试、证据、changeset
一件不存在，且**从没跑过真出口**（`.scratch/t362/` 里只有 `tsc-*.log`，无 tmpdb）。接管席**不采信前任席任何结论**，
自己重建种子、自己跑真出口、自己做变异电池、自己写测试与证据，保持**非受益者**身份。

## 二、接管量现场（§3.2 第 4 条）

未提交改动 4 件（`git diff --numstat` 当刻读数；`+行数/−行数`）：

| 件 | 改了什么 | HEAD LF | 工作区 LF | sha256(前 16) |
|---|---|---|---|---|
| `src/body/bodyDocs.ts` | 窗口口径句、锚点卡、Δ 卡、不可比提示、分组表、缺值 `—`、payload 原样透传 | 249 | 314 | `44D58D64C7C43E4D` |
| `src/body/bodyPlate.ts` | 窗口三态裁决、`compositionWindowLabel`、锚点／Δ 数据、`windowTotal`、分组序列 | 227 | 302 | `CC11A7913DB2652E` |
| `src/body/view.ts` | `days` 不再兜 90；新增 `dateFrom`／`dateTo` | 40 | 51 | `54A11B5D2309704B` |
| `src/fetch/body.ts`（点名件） | `CompositionWindowInput` ＋ 私有 `windowClause()`（窗口→日期谓词唯一定义地），四条取数共用 | 382 | 406 | `2165117E7B49A7A9` |

- **逐件标题 vs 任务清单**：四件的改动标题（窗口裁决／口径句／锚点／Δ／分组／缺值／payload／窗口谓词）**逐条对得上**票面四块，无夹带、无他票内容。
- **旧新内容哈希**：接管席**一行未改这 4 件**（只读复核）；离开时重取 diff，与进门时逐字节相同：
  `sha256(takeover-4files.diff) = 2261A4E6…02F0B` ＝ `sha256(takeover-4files.after.diff)`（`IDENTICAL=True`）。
- **语法检查**：`npx tsc -b packages/skill-calorie` → `exit 0`（`GATE-RUN runId=8a826228…`）。
- `.scratch/t362/` 前任残留：`plan.md`（5.0KB，含影响清单与窗口语义矩阵，与实现逐条吻合）、`logs/tsc-1.log`（exit 1，红在**他席**`render/sportPortDocs.ts` 三处 `TS1005`，非本票件）、`logs/tsc-2.log`（exit 0）。**无 tmpdb ⇒ 真出口验收前任席一次都没跑过。**

**采纳的取舍**：前任席 implementation 全数保留（补齐路线），接管席补判据侧。以下所有读数**均为接管席自跑**。

## 三、落点（一句）

`calorie.view.body-composition`：**不传窗口参数 ＝ 全部历史**；显式传 `days` ＝ 近 N 天；显式传 `dateFrom`+`dateTo` ＝ 闭区间；
页面可见文本写明当前口径（`窗口：全部历史`／`窗口：近 90 天`／`窗口：A → B`）。
「看体脂趋势」那条词的时间窗语义**不动**（`src/body/routes.ts:17` 的 `{"days":90}` 示例逐字保留，`routes.ts` 本票**零改动**）。

## 四、可跑判据与机器读数（期望值全部手算）

冻结种子：**跨 240 天、每 30 天一条、共 9 条**（日序 0／30／…／240），日基准与 `fetch/body.ts:144 daysAgo` **逐字同式**（避开本地／UTC ±1 天漂移）。
真出口＝`node packages/skill-calorie/dist/cli/cmd_read.js calorie.view.body-composition`，**可见文本从落盘页面 `delivery.path` 剥标签读回**（不是从 stdout 拿 HTML）。

| 判据 | 手算期望 | 当刻读数（真出口） |
|---|---|---|
| ① 不传参＝全部历史 | 条数读数 **9** | `无参=["全部历史"] 读数=[9,9]` ✅（旧行为＝静默 90 天＝4） |
| ② `--params '{"days":90}'` | 条数读数 **4**（第 0／30／60／90 天含界） | `days=90=["近 90 天"] 读数=[4,4]` ✅ |
| ③ 口径句随参变 | 两句不同、各写各口径 | `区间=["2026-05-17 → 2026-08-15"] 读数=[4,4]`；三句两两不同 ✅ |
| ④ `source=all` | `exit 0` ＋ 含「来源不可直接对比」＋ 组数 == `COUNT(DISTINCT source)` | `exit=0 组数(页)=3 组数(SQL)=3 逐组点数=家测皮褶钳:3,健身房 InBody:3,医院测:3` ✅ |
| ⑤ 负向对照 | 默认窗写死 90 → ①读数 4≠9 必红；还原必绿 | 见 §六 变异电池 `M1`（两行读数） ✅ |

同批读数（同一轮测试输出，非另跑）：

- `T362-BOUNDARY 全历史行=9 近90天行=4`（差 5 条，手算一致）
- `T362-LIMIT 表体=20 windowTotal=25 标题=体成分记录（窗口：全部历史 · 共 20 条，窗口内另有 5 条，本页只列最近 20 条）`——`limit` 只截**显示**，窗口内总数**写在标题里**（不静默截断）
- `T362-KPI 趋势点(KPI)=9 图上点数=9 图数=3`——**KPI 与图同批定**（数图上真画的 `<circle class="ilife-charts-dot">`，不是另算一套）⇒ 「有图＋趋势点 0 天」不可能出现
- `T362-SPLIT 图标题=["体脂趋势 · 家测皮褶钳（窗口：全部历史）","体脂趋势 · 健身房 InBody（窗口：全部历史）","体脂趋势 · 医院测（窗口：全部历史）"]`——裁定 5：来源之间**不合并成一条线**
- `T362-SQL total(无参)=9 total(90)=4 SQL(90)=4`——页面读数与**同一 tmp 库的 SQL** 恒等（不靠实现自证）
- `T362-BADINPUT 只给一头 exit=2 非法日期 exit=2`——窗口参数只给一个／非法字面值**报用法错**，不许静默当全历史

### 裁定 2：**两条分开断言**（不用全文 `includes` 代理口径）

| 断言 | 期望 | 读数 |
|---|---|---|
| 可见文本缺值写 `—` | 皮褶 7 点表的缺槽位**逐格** `—` | `T362-DASH-VISIBLE 槽位={"胸":"12","腹":"18","大腿":"15","三头肌":"—","肩胛下":"—","髂上":"—","腋中线":"—"}` ✅（按表行**结构定位**，不靠全文匹配） |
| payload 保留原始空值 | 同一条记录对应槽位在载荷里是 `null` | `T362-PAYLOAD 行数=9 三头肌=null 胸=12 含破折号行=0` ✅（载荷取页面 `data-fmt="json"` 那一路解析） |

### 老正本三项（§六 B 区）

| 项 | 老正本 | 读数 |
|---|---|---|
| 锚点大数字＝最新一条（**不是窗口均值**） | `:74-84`／`:155`／`:327-333` | `T362-ANCHOR 值=20% 注=2026-09-14 · 家测皮褶钳 窗口均值(手算)=24%` ✅（20 ≠ 24 ⇒ 锚点可判） |
| Δ 与间隔天数 | `:335-340` | `T362-DELTA 值=-1% 注=距上次测量 30 天（2026-08-15）· 体脂率 下降 1%` ✅ |
| 首条「暂无对比基线」 | `:344` | `T362-NOBASE 值=— 注=首条记录，暂无对比基线` ✅（Δ 值槽同时落 `—`，照老 `:342`） |
| 空态带「怎么记第一条」 | `:355` | `T362-EMPTY exit=4 句=ERR 4: 取数失败（缺失阻断）：无体成分记录（窗口：全部历史）→ 记体脂：皮褶钳或外部测量，第一条就是基线` ✅ |
| 数字动画／贴底复制条 | `:277-291`／`:173-174` | **不作验收项**（基准 §五-4 只登记为「老页交互未移植」） |

## 五、测试与门禁运行（逐条对账见 `t362-门禁对账.md`）

```
GATE-RUN runId=8a826228-8d34-4b63-ac7a-0e8c5a541dcc cmd=npx tsc -b packages/skill-calorie            # exit 0
GATE-RUN runId=5f1227c3-9b0a-4e9a-97ec-6dc1ad39f28a cmd=node packages/skill-calorie/scripts/check-warning-line.mjs   # exit 0（进场基线）
GATE-RUN runId=9be2a358-c321-4e7f-b8f3-c45b2ccae867 cmd=node .scratch/t362/probe.mjs                  # exit 0
GATE-RUN runId=ce1ac366-233c-49c4-88a7-9f0bee35e6ad cmd=node .scratch/t362/probe.mjs                  # exit 0
GATE-RUN runId=ece5ec4f-97f2-4217-8558-24d61d6eb246 cmd=node .scratch/t362/inspect2.mjs               # exit 0
GATE-RUN runId=7bb61a2c-9be7-4ced-abb8-fa618c1e5251 cmd=node --test packages/skill-calorie/test/t362-窗口不截断.test.mjs   # exit 0，pass 15 / fail 0
GATE-RUN runId=ec8200e9-aee4-44ba-a86d-2e94dbc83410 cmd=node .scratch/t362/mutate.mjs                 # exit 0，3/3 变异识别且还原必绿
GATE-RUN runId=d9aa5ce5-224d-406d-81af-371789705a9d cmd=node --test <11 件体成分／取数面>              # exit 1 —— 见 §七（本票引入的 #398 回归）
GATE-RUN runId=c027d7d4-6d34-4648-925f-7981e179abab cmd=node --test <16 件含体成分／取数面>             # exit 1 —— 4 红全属他席（见 §八）
GATE-RUN runId=700fc58c-1554-4951-9b9d-bead691808d5 cmd=node --test "test/*.test.mjs" "packages/skill-calorie/test/*.test.mjs" "packages/plugin-calorie/test/*.test.mjs"  # exit 1，955 例 39 红全属他席
GATE-RUN runId=30ec14f4-9c94-4e71-81e0-e227d655de91 cmd=node packages/skill-calorie/scripts/check-warning-line.mjs   # exit 1 —— 他席红（见 §八）
GATE-RUN runId=961cb1b9-4de1-4506-b96b-255e24eed455 cmd=node packages/skill-calorie/scripts/check-warning-line.mjs   # exit 0，RESULT: 58/58 PASS
```

本票测试件：`packages/skill-calorie/test/t362-窗口不截断.test.mjs`（**新建，唯一**，15 用例）——
**`RESULT: pass 15 / fail 0`**。测试件不在告警线扫描面内（`test/*.mjs` 明确剔除），本票**没有把任何件撑过 350**：
四件 LF ＝ 314／302／51／406，其中只有 `src/fetch/body.ts` 在台账内（挂号 369、当场实测 **406**，与实况一致）。

## 六、变异电池（源码级；持锁一轮做完，逐文件点名还原）

驱动 `.scratch/t362/mutate.mjs`：**定点字符串替换**（不按行号、不整文件覆盖），命中数必须恰为 1；
每个变异跑完在 `finally` 里**逐文件点名还原并字节自证**，还原后重编再跑（`runId=ec8200e9…`，exit 0）。

| 变异 | 改坏读数（红） | 还原读数（绿） |
|---|---|---|
| **M1** 默认窗写死 90 天（`body/view.ts` ，本票正题的旧行为） | `fail=9 pass=6 首红=#362 判据①②③：不传参＝全部历史 9 条…`（判据①读数由 9 变 4 ⇒ 必红） | `fail=0 pass=15 exit=0` |
| **M2** 复制载荷改写成可见文本那套（`bodyDocs.ts`，缺值写 `—`） | `fail=1 pass=14 首红=#362 裁定2-载荷：复制数据保留原始空值…` | `fail=0 pass=15 exit=0` |
| **M3** 去掉 `source=all` 的「来源不可直接对比」提示句（`bodyDocs.ts`） | `fail=1 pass=14 首红=#362 裁定5：source=all exit 0 ＋ 不可比提示句 ＋ 组数 == SQL COUNT(DISTINCT source)` | `fail=0 pass=15 exit=0` |

收尾自证：`RESULT: 3/3 变异识别且还原必绿`；四件与进门时**逐字节相同**（§二 的 diff 哈希 `IDENTICAL=True`），
生成物（`dist/`）在每次还原后**重编复位**（§5 的两步连做：重编 ＋ 重签由 `tsc -b` 一并完成）。

## 七、跨票改动（本票引入，一行，必须点名）

**`packages/skill-calorie/test/t398-source-group.test.mjs`**：该件第 124 行那条真出口断言依赖**本票改掉的旧默认窗**——
`runRead(dir, { source: 'all' })` 在改前＝「三值筛选 ＋ 隐式 90 天窗」，套本票新口径后＝**全部历史**，
读数由手算 5 变 6 ⇒ 该条红（`GATE-RUN runId=d9aa5ce5…`，`actual 6 / expected 5`）。
修法＝**把窗口显式传上**（`runRead(dir, { source: 'all', days: WINDOW_DAYS })` ＋ 一行注释点名 #362），
该件**原本要测的窗口语义 100% 不变**（断言 `EXPECT.windowRows == EXPECT.allRows - 1` 照旧成立）。
#398 已关；此改动属本票引入的**必然连带修**，已在提交信息里点名。

## 八、他席红逐条归属（不假绿）

1. **`GATE-RUN runId=c027d7d4…` 的 4 红**：`#109 域内唤醒词命中`（`看今日运动` 落 `calorie.view.exercise-records` ≠ 期望 `calorie.view.exercise`）、
   `#109 运动总览`（缺「运动总览 2026-09-05 ~ 2026-09-07」）、`#109 运动目标`（`exercise-goal` 缺「目标 vs 实际」）、
   `#109 无假数据`（`calorie.view.weight` 空库未阻断，status=0）。**全部是运动／体重域**（#452／#453 等他席在途）。
   同一文件里**本票的门**照旧绿：`✔ #109 体成分：来源表单＋趋势＋记录表＋复制`。
2. **`GATE-RUN runId=700fc58c…` 的 39 红**（955 例）：唤醒词／场景登记表、运动域（#109／#110／#111／#113）、
   复制区（#239／#448）、照片（#91）、路由（#265）、体重（#374）——**无一条落在体成分／取数面**。
3. **`GATE-RUN runId=30ec14f4…`（告警线门 exit 1）**：单条 `RED 台账陈化：src/analysis/reportPlate.ts 台账=393 实况=406`
   —— 该件是**#384 在途件**（台账行自己写明的），**不是本票件**；接管席**没有 `--sync`**（理由：① 不是我的件陈化；
   ② `AGENTS.md` 此刻正被他席并发改动，`--sync` 会把他席的行一起提交）。他席随后自行同步，本席复跑 `runId=961cb1b9…` 即 **`58/58` PASS**。

## 九、未做项与边界（如实登记）

1. **`body_fat_pct` 缺值不可达**：`src/schema.ts:94` 是 `REAL NOT NULL`（直插 `NULL` 当场被拒：`rejected: NOT NULL constraint failed`）。
   故裁定 2 的两条断言**落在可达的位**：可见文本＝皮褶 7 点缺槽位 `—`（＋Δ 卡／锚点卡的 `—`），
   payload＝同槽位 `null`。体脂列缺值格仍留 `—`（防御分支，**如实登记为不可达**）。
2. **「备注」列缺值照老正本留空串**：老 `:366` 是 `<td>${esc(r.note)}</td>`（老页对备注不给 `—`，只对 Δ 列 `:365` 与锚点 `:342` 给 `—`）。
   本页沿用老口径 ⇒ 备注空串**不写 `—`**。这是**照老正本**的取舍，不是漏做；如认为该归一，需另开票改基准。
3. **`dateFrom`／`dateTo` 的上界未加 365 天限制**（`days` 仍是 1..365）：区间窗口本票未定上限，维持原 `listCompositions` 口径不动。
4. **告警线台账（`AGENTS.md`）本票零改动**：四件 LF 与台账一致，无需 `--sync`；此文件此刻由他席并发维护。
5. **本票未 push**（按派单口径，报 sha）。

## 十、交付路径

- 实现：`packages/skill-calorie/src/body/bodyDocs.ts`、`src/body/bodyPlate.ts`、`src/body/view.ts`、`src/fetch/body.ts`（点名件）
- 测试：`packages/skill-calorie/test/t362-窗口不截断.test.mjs`（新建，唯一）
- 连带：`packages/skill-calorie/test/t398-source-group.test.mjs`（一行，见 §七）
- 证据：本件 ＋ `docs/skills/skill-calorie/t362-门禁对账.md`
- changeset：`.changeset/t362-体质看窗口不截断.md`
- 草稿：`.scratch/t362/`（`plan.md`、`probe.mjs`、`inspect2.mjs`、`mutate.mjs`、`logs/*`、`takeover-4files*.diff`）
- **禁碰件均已遵守**：`src/body/routes.ts`（零改动，`T362-ROUTES16/17` 逐字复核）、`AGENTS.md`、`scripts/check-warning-line.mjs`、`src/triggers/**`。
