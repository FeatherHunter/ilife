# #520 视觉重评席 V1 第二轮：页 01–08 逐页重判（R2）

本席只评 `.scratch/t387/html/` 里的**页 01–08**（预测体重族 6 页 ＋ 模拟减重族 2 页）。评的是**冻结产物**
本身，与实施者不同席；实施者的任何结论一律不采信，全部自己重测。分数与结论逐条对着产物源码核过；源码里找不到对应处的怀疑一律写「源码未证实」且**不作扣分依据**。
产物只读，**未改产物、未改 `packages/` 下任何源码、未提交未推送**。

本轮改动对照（编排者口径）：10–13 判词方向（01–08 不涉及）；18–20 标题（不涉及）；长表折叠（01–08 无长表，应无折叠）；其余改动在报告族（不涉及）。本席仍逐页完整重打分，不只写 delta；若发现 01–08 退化，逐条点名。

## 〇、尺（与首轮同一把，不自定）

| 维 | 满权 | 量什么 |
|---|---|---|
| 结构纪律 | 20 | 页壳／卡片／表格是不是真结构（真 `<table>` 或真卡片），网格是否成列，断点是否成档，有无伪表（纯 `<div>` 拼列） |
| 信息层级与版式细调 | 25 | 第一眼层级、间距取 4/8 倍数、字号成档有下限、数值列等宽右对齐、卡／表／图左缘共线、留白有节 |
| 分隔符与文字纪律 | 20 | 分号／间隔号／竖线拼串处数、同一事实重复处数、英文内部标识符／库表名／常量名上屏处数 |
| 跨宽自适应 | 15 | 1440 ／ 768 ／ 390 三档无溢出、无塌列、无截断、无意外横滚 |
| 手机端与 HELP 同档 | 20 | 对照 `fixtures/help-instances/卡路里_HELP_20260731_201530.html` 的断点与窄表堆叠做法；触摸目标 ≥44px；表格窄档读得开；日期不被折断 |

**加权总分 ＝ 结构×0.20 ＋ 信息×0.25 ＋ 分隔符×0.20 ＋ 跨宽×0.15 ＋ 手机端×0.20**，逐页给分。
五维**各自按百分制打分**（权重×100 就是该维的满权分：结构 20／信息 25／分隔符 20／跨宽 15／手机端 20，
五项满权合计 100），故加权总分与「各维按满权折分后相加」同值，逐页把两条算式都写出来便于复核。

**参照件（HELP 201530）实测口径**：`.container{max-width:960px;padding:0 12px 40px}`；
断点两档 —— `@media (max-width:600px)`（正文降到 `12.5px`，容器 `padding 0 8px 60px`）与
`@media (min-width:601px) and (max-width:900px){.container{max-width:720px}}`；
该件**全页无 `<table>`**，窄表堆叠无从照抄 —— 故本批「与 HELP 同档」按**断点层级 ＋ 手机档字号下限 ＋
容器留白节律**三项比，窄表另按「窄档读得开」独立量。

## 一、冻结产物身份（本席所有读数都对着这一份）

目录 `.scratch/t387/html/`，八件冻结。本席的机器读数与截图都对着下表这一份（与首轮 V1 同字节，逐页核过）：

| 页 | 文件 | 字节 | SHA256（前 16） |
|---|---|---|---|
| 01 | `01-预测体重(1周后).html` | 71,824 | `C24FF66B28B79B09` |
| 02 | `02-预测体重(1月后).html` | 71,835 | `796A39AE7EE50351` |
| 03 | `03-预测体重(3月后).html` | 71,835 | `0B4771C56CC10E09` |
| 04 | `04-预测体重(6月后).html` | 71,840 | `F634220E7470E837` |
| 05 | `05-预测体重(自定义时间).html` | 71,823 | `AC7C85ACA7742EBC` |
| 06 | `06-预测体重(自定义目标).html` | 71,710 | `72C5C34BEF053D0D` |
| 07 | `07-模拟减重(每天-300卡).html` | 76,071 | `0B87AD30889DE82B` |
| 08 | `08-模拟减重(每天-500卡).html` | 76,071 | `4DC63A2E3599F58D` |

**机器读数件（本席自己跑的，只读产物目录、不加锁）**：

| 件 | 命令（只读，不加锁） | 用途 |
|---|---|---|
| `.scratch/t520-R2-V1/sep.json` | `node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t387/html --json .scratch/t520-R2-V1/sep.json` | 分隔符／英文标识符／≥3 段并列（节点级） |
| `.scratch/t520-R2-V1/resp.json` | `node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t387/html --widths 390,768,1440 --json .scratch/t520-R2-V1/resp.json` | 横向溢出、被切、横滚容器、图放不下 |
| `.scratch/t520-R2-V1/probes.log` | 两探针的合并 stdout 尾行 | 持锁以外的只读跑记录 |

**视觉件**：`vision_html_screenshot`（桌面 1440×900／手机 390×844，fullPage）＋ `vision_describe` 读图；工具坏了照实记录并继续，不换措辞重试。

## 二、逐页判定

（以下逐页六问＋五维打分，每评完一页立刻落盘。）

### 页 01 · 预测体重(1周后)

**产物**：`.scratch/t387/html/01-预测体重(1周后).html`，71,824 字节，SHA256 前 16 `C24FF66B28B79B09`（与首轮同字节）。

**机器**：`sep.json` 本页节点级 `R1–R7 全 0／hits=0`；`resp.json` 390／768／1440 三档 `ok=true`、`docScrollWidth==innerWidth`、`overflow=0／clippedCount=0／scrollerCount=0／unfitted=0`。

**视觉**：桌面 1440×900 整页（页高 900＝视口，一屏放下）＋手机 390×844 整页（页高 1070）均实拍并读图。读图报层级清晰、无横滚、无重叠、日期单行；另报四条存疑，逐条回源码处置见下。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 清楚。眉标 `p.ilife-block-page-shell-eyebrow`「预测体重」→ H1「体重预测（7 天）」→ 三枚 `span.ilife-block-chip`（卡路里／7 天／趋势分析）→ 结论 `p.ilife-block-conclusion`「按最近的趋势，7 天后体重约 74.8 kg。」→ `nav.ilife-block-toc`（参数／概览／数据与日志）→ 三节 `h2.tpd-sec-title`（`:1842` `font-size:16px`）。读图亦报层级递进明确。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | 无。`resp.json` 三档全绿（见上）。**读图两条驳回**：①「桌面四卡不等高、区间卡折两行最高」——源码 `:1316` `.ilife-block-kpi-card-grid{grid-auto-rows:1fr}` 等高构造，驳回；值串最长问题另计入信息层扣分。②「小节标题比参数正文左缩进 16–20px」——源码 `:1842` `.tpd-sec-title{margin:0 0 8px}` 无缩进声明，节内 `h2` 与表单同为 `section` 直接子流，**源码未证实**，不扣分。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中（`sep.json` 节点级全 0）。**版式层半命中**：页脚口径行 `:1874` 三枚 `<span>`（体重＝当前值加日速率乘天数 ／ 预计区间按体重残差向外放宽，取 95% 置信带 ／ 体重记录不到 14 天不出预测）由 `:1191` `border-left:1px solid rgba(210,210,215,.6)` 发丝线串行。本轮读图报「三段之间无可见竖线、仅靠间距分隔」——与首轮读图（见两条竖线）分歧；以源码为准，发丝线存在，按 `#505` 豁免记轻。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **一处真冗余**：`:1847` 参数说明「体重记录不到 14 天就只说数据不够，不编预测。」与 `:1874` 口径「体重记录不到 14 天不出预测」（「不到 14」计数＝2）。**半冗余一处**：「预计区间」计数＝2（概览卡＋口径第 2 枚 span；「95%」计数＝2）。**英文上屏零命中**（`sep.json` R7＝0；可见文本只有 `kg` 单位与复制按钮 `JSON`／`CSV` 格式名）。读图「徽标『卡路里』与体重内容无关」属内容 Judgement，尺内无对应扣项，只登记不扣分。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | 等宽达标：`:1116` 页壳 `font-feature-settings:"tnum"`、多处卡值 `font-variant-numeric:tabular-nums`。**本页无 `<table>`**（`<table` 计数＝0），无数值列可量；四卡值 22px（`font-size:22px` 在册）左对齐。小数位：75.1／74.8／−0.34／74.7 至 74.8 全一位小数，一致。缺值：四卡全有值，无 `—` 可核；`<details` 计数＝0（无长表、无折叠，符合本轮预期）。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | 本页无表。触摸：源码 `:1163` `.ilife-block-toc a{min-height:44px}`、`:1821` `summary{min-height:44px}`；读图「导航直径约 36px、徽标高约 32px」**驳回**（源码保底 44px，截图估算不作数）。日期：读图 390 档报「2026-09-03」「2026-09-16」均单行未折；`resp.json` 390 档 `clippedCount=0／scrollerCount=0`。 |

**五维打分**（每维百分制；「满权」＝权重×100）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里（逐条对源码／读数） |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 真结构齐（`section` ×3＋`h2` ×3＋`nav` ×1＋卡网格 `grid-auto-rows:1fr`）。**扣 5**：`id="sec-data"`（计数 1）节名「数据与日志」，节内只有 `ilife-block-copy-block`（计数 4 命中，皆样式与容器），零数据行（`<table`＝0）。**扣 5**：断点不成档（`@media` 计数 21，页级主断点 `:1825 @media (max-width:820px)` 单值；`max-width:600px`＝0，无 HELP 参照档）。**扣 1**：`<button … data-action-id="ilife-copy-log" disabled>复制日志</button>`（「复制日志」计数 1）永远按不动、页上无一句解释。 |
| 信息层级与版式细调 | 0.25 | 25 | **88** | 22.0 | 层级清、卡等高构造、读图无重叠。**扣 7**：间距非 4／8 倍数（`padding: 14px`＝4、`gap: 6px`＝6、`margin: 6px 0 0`＝6、`padding: 2px 8px`＝3、`margin: 0 6px 6px 0`＝1、`padding: 10px`＝5、`gap: 14px`＝2）。**扣 3**：1440 档页高 900＝视口但正文相对 1080px 级列偏松（观感债）。**扣 2**：「预计区间」卡值串最长（`74.7 至 74.8`＋`kg` 与另三张同宽一行）。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 文本层零命中（`sep.json`）；R7 标识符 0。**扣 5**：真冗余 1 处（不到 14 ×2）＋半冗余 1 处（95%／预计区间 ×2）＋口径行发丝线串行（版式承担，记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档 `docScrollWidth==innerWidth`、`ok=true`、`overflow=0／clipped=0／scrollers=0`。无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸保底 44px 两处在册（`:1163`、`:1821`）；无表；日期单行（读图）；390 档无横滚（`scrollerCount=0`）。**扣 5**：页级主断点 820 ∉ 参照件实测 {600,900}（注释自称同 HELP，`820` 计数 9 处却无 600 档）。 |

**加权总分 ＝ 89×0.20 ＋ 88×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋22.0＋19.0＋15.0＋19.0 ＝ 92.8**
（折满权同值：17.8／20 ＋ 22.0／25 ＋ 19.0／20 ＋ 15.0／15 ＋ 19.0／20 ＝ 92.8／100；首轮基线 92.8，持平，无退化）

### 页 02 · 预测体重(1月后)

**产物**：`.scratch/t387/html/02-预测体重(1月后).html`，71,835 字节，SHA256 前 16 `796A39AE7EE50351`（与首轮同字节）。

**机器**：`sep.json` 本页节点级 `R1–R7 全 0／hits=0`；`resp.json` 390／768／1440 三档 `ok=true`、`docScrollWidth==innerWidth`、`overflow=0／clippedCount=0／scrollerCount=0`。

**视觉**：桌面 1440×900（页高 900）＋手机 390×844（页高 1070）实拍读图。读图：层级干净、标题「体重预测（30 天）」、结论「30 天后约 73.6 kg」、四卡等高上沿对齐、左缘共线、无被切无横滚、日期未折、两档数值一致（75.1／73.6／−0.34／73.5 至 73.7）。

**与页 01 的关系**：同族同模板。源码计数逐项相同：`sec-data`=2（导航锚点＋`section id`，`section id`=3）、`<table`=0、`<details`=0、`grid-auto-rows: 1fr`=1、`min-height:44px` 在册、`max-width:820px`=1、`border-left: 1px`=1（口径行发丝线）。差异只在内容：`horizonDays` value=`30`、结论 `73.6`（计数 5）、区间 `73.5 至 73.7`。本页读图「卡片等高、左缘共线」与页 01 本轮读图（不等高／缩进）直接矛盾——同模板同 CSS 下以源码构造为准，页 01 的两条已按「驳回／源码未证实」处置，本页不再重复计。

**六问**

| # | 问 | 结论（附源码／机器证据） |
|---|---|---|
| ① | 第一眼看到什么、层级清不清 | 清楚，同页 01：眉标「预测体重」→ H1「体重预测（30 天）」（「体重预测」计数 2：眉标＋H1）→ 徽标（卡路里／30 天／趋势分析）→ 结论「30 天后体重约 73.6 kg」→ 导航 → 三节。 |
| ② | 挤／空／错位／列宽失衡／截断／横滚 | 无。`resp.json` 三档全绿；读图两档均报无被切、无横向滚动、无重叠、卡片等高。 |
| ③ | 有没有拿分号／间隔号／竖线把并列信息挤成一串 | 文本层零命中。版式层半命中：口径行发丝线（`border-left: 1px`＝1）串三段；本轮两页读图均报「无可见竖线」——与首轮读图分歧，以源码为准，线存在，按 `#505` 记轻。 |
| ④ | 同一件事写两遍？英文内部标识符／库表名／常量名上屏？ | **一处真冗余**（「不到 14」＝2：参数说明与口径各一次）＋**半冗余一处**（「95%」＝2／「预计区间」＝2）。读图另指两处（窗口起止日期在参数输入与页脚来源行各一次；速率值与口径公式各一次）——输入值与数据来源属不同角色（录入 vs 溯源）、卡面数值与口径公式属不同角色（读数 vs 算法），非散文复述，**不计入**。英文零命中（R7＝0）。**未记**：结论 `73.6` 与预测卡同值，属结论前置既定形状。 |
| ⑤ | 数值列等宽数字？缺值写 —？小数位一致？ | `tabular-nums`／`"tnum"` 在册；无 `<table>`（计数 0），无数值列可量。小数位全一位小数，一致（读图逐值核过）。缺值：四卡全有值，无 `—` 可核；`<details`＝0（无折叠，符合预期）。 |
| ⑥ | 手机档表格读得开吗？触摸目标够大吗？日期有没有被折断 | 无表。触摸保底 44px 在册；读图 390 档：卡片 2×2、`2026-09-03`／`2026-09-16` 单行未折、按钮文字不截断、页脚末段贴近右内边距但未截（机器 `clippedCount=0` 佐证）。 |

**五维打分**（每维百分制）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 同页 01：`section id` ×3＋`h2` ×3＋`nav`＋`1fr` 等高网格。**扣 5**：「数据与日志」节内只有复制块、零数据行。**扣 5**：断点不成档（`@media` 多值、主断点 820 单值、无 600 档）。**扣 1**：`disabled`「复制日志」（计数 1）无解释。 |
| 信息层级与版式细调 | 0.25 | 25 | **88** | 22.0 | 层级清、卡等高（本页读图与源码一致）、左缘共线（本页读图确认）。**扣 7**：间距非 4／8 倍数（`padding: 14px`＝4、`gap: 6px`＝6、`margin: 6px 0 0`＝6，与页 01 同值）。**扣 3**：1440 档页高 900、正文相对宽列偏松。**扣 2**：「预计区间」卡值串最长。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 文本层零命中；R7＝0。**扣 5**：真冗余 1＋半冗余 1＋发丝线串行（记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档全绿，无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸 44px 在册、无表、日期单行、无横滚。**扣 5**：断点 820 ∉ {600,900}。 |

**加权总分 ＝ 89×0.20 ＋ 88×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋22.0＋19.0＋15.0＋19.0 ＝ 92.8**
（首轮基线 92.8，持平，无退化）

### 页 03 · 预测体重(3月后)

**产物**：`.scratch/t387/html/03-预测体重(3月后).html`，71,835 字节，SHA256 前 16 `0B4771C56CC10E09`（与首轮同字节）。

**机器**：`sep.json` 节点级全 0；`resp.json` 三档 `ok=true`、`overflow=0／clippedCount=0／scrollerCount=0`。

**视觉**：桌面 1440×900（页高 900）＋手机 390×844（页高 1070）实拍读图。读图：层级清晰（标题「体重预测（90 天）」、结论「90 天后约 70.7 kg」）、两档无被切无横滚无重叠、四卡等高、左缘贴齐、标题与徽标两档均单行、日期单行、小数位一致（体重 1 位／速率 −0.34 2 位／95% 整数）。

**源码**：`sec-data`=2（锚点＋`section id`）、`<table`=0、`<details`=0、`grid-auto-rows: 1fr`=1、`min-height:44px` 在册、`max-width:820px`=1、「不到 14」=2、「95%」=2、「预计区间」=2、「复制日志」=1（`disabled` 共 5 处含该按钮）、`border-left: 1px`=1。内容差异只在 90 天／70.7／区间 70.5 至 70.8。

**六问**

| # | 问 | 结论 |
|---|---|---|
| ① | 第一眼层级 | 清楚：眉标→H1「体重预测（90 天）」→徽标（卡路里／90 天／趋势分析）→结论→导航→三节。读图与源码一致。 |
| ② | 挤／空／错位／截断／横滚 | 无。机器三档全绿；读图两档确认等高、贴齐、无切无滚。页 01 本轮的「不等高／缩进」读法在本页同模板下未复现，视为读图噪声，维持页 01 的驳回处置。 |
| ③ | 分隔符拼串 | 文本层零命中。版式层半命中：口径行发丝线（`border-left: 1px`＝1）串三段；本轮读图三页一致报「无可见竖线」——以源码为准，线存在但 1px 浅灰（`rgba(210,210,215,.6)` 同源）实拍难辨，记轻。 |
| ④ | 重复／英文标识符 | 真冗余 1（不到 14 ×2）＋半冗余 1（95%／预计区间 ×2）。读图指「结论 70.7 与预测卡 70.7 重复」——结论前置既定形状，不计。英文零命中（R7＝0）。 |
| ⑤ | 等宽／缺值／小数位 | `tabular-nums` 在册；无表无数值列；小数位一致（读图逐值核）；无缺值、无 `—`；`<details`＝0 无折叠。 |
| ⑥ | 手机档／触摸／日期 | 无表。触摸 44px 在册。读图：2×2 网格首屏可读、日期单行、无需横滑。 |

**五维打分**（百分制）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 扣 5（数据与日志节空心、零数据行）＋扣 5（断点不成档）＋扣 1（disabled 复制日志无解释）。 |
| 信息层级与版式细调 | 0.25 | 25 | **88** | 22.0 | 扣 7（间距非 4／8：`padding: 14px`＝4、`gap: 6px`＝6）＋扣 3（1440 档偏松）＋扣 2（区间卡值串最长）。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 扣 5（真冗余 1＋半冗余 1＋发丝线记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档全绿，无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 扣 5（断点 820 ∉ {600,900}）。 |

**加权总分 ＝ 89×0.20 ＋ 88×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋22.0＋19.0＋15.0＋19.0 ＝ 92.8**
（首轮基线 92.8，持平，无退化）

### 页 04 · 预测体重(6月后)

**产物**：`.scratch/t387/html/04-预测体重(6月后).html`，71,840 字节，SHA256 前 16 `F634220E7470E837`（与首轮同字节）。

**机器**：`sep.json` 节点级全 0；`resp.json` 三档 `ok=true`、`overflow=0／clippedCount=0／scrollerCount=0`。

**视觉**：桌面 1440×900（页高 900）＋手机 390×844（页高 1070）实拍读图。读图：层级清晰（标题「体重预测（180 天）」、结论「180 天后约 66.2 kg」）、无横滚无重叠无被切、Tab／结论／四卡／按钮左缘两档均严格对齐、手机 2 列舒适、日期单行、小数位一致（体重 1 位／速率 2 位可接受）。

**源码**：`sec-data`=2、`<table`=0、`<details`=0、`grid-auto-rows: 1fr`=1、`min-height:44px` 在册、`max-width:820px`=1、「不到 14」=2、「95%」=2、「预计区间」=2、「复制日志」=1、`border-left: 1px`=1、「180 天」=5、「66.2」=5。内容差异只在 180 天／66.2／区间 66.0 至 66.5。

**六问**

| # | 问 | 结论 |
|---|---|---|
| ① | 第一眼层级 | 清楚：眉标→H1「体重预测（180 天）」→徽标→结论→导航→三节。读图两档确认。 |
| ② | 挤／空／错位／截断／横滚 | 无。机器三档全绿。**读图一条驳回**：「桌面速率卡因缺副说明明显偏矮」——源码 `1fr` 等高构造（＝1），驳回；卡内密度差已在信息层「值串最长」计过，不重复计。**读图一条采信**：两档左缘严格对齐——与页 01 本轮「缩进」读法矛盾，同模板下以源码（无缩进声明）＋本页读图为准，页 01 维持「源码未证实、不扣分」。 |
| ③ | 分隔符拼串 | 文本层零命中。版式层半命中：发丝线（＝1）串三段；本轮读图四页一致「无可见竖线」，以源码为准记轻。读图建议「加 `|` 或 `·` 分隔」与 `#505` 豁免（分隔由版式承担）冲突，**不采纳**，不扣分。 |
| ④ | 重复／英文标识符 | 真冗余 1（不到 14 ×2）＋半冗余 1（95%／预计区间 ×2）。读图指「66.2 三处／180 天三处（标题／徽标／卡副标）」——标题天数、徽标天数、卡副标天数是三级导航标签（页头／胶囊／卡面），非散文复述，**不计入**（与首轮口径一致）。英文零命中（R7＝0）。 |
| ⑤ | 等宽／缺值／小数位 | `tabular-nums` 在册；无表；小数位一致（66.0 补零统一位数）；无缺值；`<details`＝0 无折叠。 |
| ⑥ | 手机档／触摸／日期 | 无表。触摸 44px 在册。读图：四值未折未溢、日期单行。读图「区间值 `5` 与 `kg` 几乎无间隙」——机器 `clippedCount=0`，不构成缺陷；值串最长已计，不重复计。 |

**五维打分**（百分制）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 扣 5（数据与日志节空心）＋扣 5（断点不成档）＋扣 1（disabled 复制日志无解释）。 |
| 信息层级与版式细调 | 0.25 | 25 | **88** | 22.0 | 扣 7（间距非 4／8）＋扣 3（1440 档偏松）＋扣 2（区间卡值串最长）。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 扣 5（真冗余 1＋半冗余 1＋发丝线记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档全绿，无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 扣 5（断点 820 ∉ {600,900}）。 |

**加权总分 ＝ 89×0.20 ＋ 88×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋22.0＋19.0＋15.0＋19.0 ＝ 92.8**
（首轮基线 92.8，持平，无退化）

### 页 05 · 预测体重(自定义时间)

**产物**：`.scratch/t387/html/05-预测体重(自定义时间).html`，71,823 字节，SHA256 前 16 `AC7C85ACA7742EBC`（与首轮同字节）。

**机器**：`sep.json` 节点级全 0；`resp.json` 三档 `ok=true`、`overflow=0／clippedCount=0／scrollerCount=0`。

**视觉**：桌面 1440×900（页高 900）＋手机 390×844（页高 1070）实拍读图。读图：层级清晰（标题「体重预测（60 天）」、徽标「卡路里／60 天／趋势分析」、结论「60 天后约 72.2 kg」）、两档无横滚无重叠、四卡等高左缘对齐、标题与徽标两档单行、日期单行、页脚长句换行不断义、小数位一致。

**源码**：「自定义」＝0（全文件零出现，含注释与属性）、`sec-data`=2、`<table`=0、`<details`=0、`1fr`=1、`min-height:44px` 在册、`max-width:820px`=1、「不到 14」=2、「95%」=2、「预计区间」=2、「复制日志」=1、`border-left: 1px`=1、「72.2」=2。

**六问**

| # | 问 | 结论 |
|---|---|---|
| ① | 第一眼层级 | 清楚，同模板。但**页上看不出「自定义时间」**：读图通览两图确认「自定义」三字零出现；源码「自定义」＝0——眉标「预测体重」、H1「体重预测（60 天）」、徽标「60 天」，与固定档（7／30／90／180 天）形状完全相同；而同族页 06 徽标写「自定义目标」，同族表达不一致（扣分见信息层）。 |
| ② | 挤／空／错位／截断／横滚 | 无。机器三档全绿；读图两档确认。手机档区间卡「kg 与下行 95% 行距偏紧」——机器 `clippedCount=0`，不构成缺陷；间距债已计。 |
| ③ | 分隔符拼串 | 文本层零命中。版式层半命中：发丝线（＝1）串三段。**本轮首次**：本页读图报「两图均用浅灰竖线 `|` 分隔」——与页 01–04 本轮读图（无竖线）矛盾；源码发丝线五页同值，故可见性属渲染／读图波动，以源码为准，记轻（与首轮一致）。 |
| ④ | 重复／英文标识符 | 真冗余 1（不到 14 ×2）＋半冗余 1（95%／预计区间 ×2）。读图「72.2 一屏出现 3 次（结论＋预测卡＋区间下限）」——源码「72.2」＝2（结论＋预测卡；区间是 72.0／72.3），读图误将区间下限计入，**纠正为 2 次**，属结论前置形状，不计。英文零命中（R7＝0）。 |
| ⑤ | 等宽／缺值／小数位 | `tabular-nums` 在册；无表；小数位一致（72.0 补零）；无缺值；`<details`＝0 无折叠。 |
| ⑥ | 手机档／触摸／日期 | 无表。触摸 44px 在册。读图：标题徽标单行、日期单行、2×2 网格可读。 |

**五维打分**（百分制）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 扣 5（数据与日志节空心）＋扣 5（断点不成档）＋扣 1（disabled 复制日志无解释）。 |
| 信息层级与版式细调 | 0.25 | 25 | **86** | 21.5 | 扣 7（间距非 4／8）＋扣 3（1440 档偏松）＋扣 2（区间卡值串最长）＋**扣 2（页上零「自定义」字样：源码计数 0，与页 06 徽标「自定义目标」不一致，用户无法区分自定义与固定档）**。 |
| 分隔符与文字纪律 | 0.20 | 20 | **95** | 19.0 | 扣 5（真冗余 1＋半冗余 1＋发丝线记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档全绿，无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 扣 5（断点 820 ∉ {600,900}）。 |

**加权总分 ＝ 89×0.20 ＋ 86×0.25 ＋ 95×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋21.5＋19.0＋15.0＋19.0 ＝ 92.3**
（首轮基线 92.3，持平，无退化）

### 页 06 · 预测体重(自定义目标)

**产物**：`.scratch/t387/html/06-预测体重(自定义目标).html`，71,710 字节，SHA256 前 16 `72C5C34BEF053D0D`（与首轮同字节）。

**机器**：`sep.json` 节点级全 0；`resp.json` 三档 `ok=true`、`overflow=0／clippedCount=0／scrollerCount=0`。

**视觉**：桌面 1440×900（页高 900）＋手机 390×844（页高 1122）实拍读图。读图：层级清晰（眉标「预测体重」→H1「预测体重（自定义目标 65 kg）」→徽标「卡路里／自定义目标／趋势分析」→结论→导航→三节）；四卡内容①当前 75.1 ②目标 65 ③预计达成 2027-04-09（剩余 205 天）④可行性（速率 −0.34＋徽标「超范围」，**无大数字**）；四卡等高但第四卡底部约 30–40% 空白带、徽标贴底头重脚轻；徽标对比清晰；手机 2×2、日期与数值完整无折、无横滚；页脚三段有竖线分隔（本轮第三种读法）；选项卡 390 下偏挤。

**源码**：「健康范围」=1／「安全区间」=0（本页用健康范围；页 07／08 用安全区间，跨页同一概念两个词，待页 07 核）、「自定义目标」=2（徽标＋H1，本页标了自定义，与页 05 零「自定义」对照）、「超范围」=1（`ilife-status-badge`＝7 在册）、大数字值位 `kpi-card-value"`＝3（四卡只有三卡有值行，第四卡无值行——空白带的构造证据）＋`kpi-card-detail`＝3、`sec-data`=2、`<table`=0、`<details`=0、`1fr`=1、`min-height:44px` 在册、`max-width:820px`=1、「不到 14」=2、「预测体重」=2（眉标＋H1）、`border-left: 1px`=1。本页「95%」=0（无置信带文案，与 01–05 不同）。

**六问**

| # | 问 | 结论 |
|---|---|---|
| ① | 第一眼层级 | 清楚，但第四卡是层级破口：值位计数 3／4 卡，第四卡无大数字（读图＋源码一致）。读图建议「给第四卡补大数字（如剩余偏差 10.1 kg）」——凭空造数，**不采纳**。 |
| ② | 挤／空／错位／截断／横滚 | **「空」一处实锤**：`1fr` 等高＋第四卡无值行 → 卡内空出一条值行带（读图「底部 30–40% 空白」与值位 3／4 互证）。其余无：机器三档全绿。读图「选项卡 390 下几乎贴边」——源码 `min-height:44px` 保底、机器无横滚无被切，**源码未证实**（未核到 gap 值），不扣分。 |
| ③ | 分隔符拼串 | 文本层零命中。版式层半命中：发丝线（＝1）串三段「预计达成日＝…／健康范围＝…／不到 14 天不出日期」；本页读图见竖线（与页 05 同，页 01–04 未见）——以源码为准，记轻。 |
| ④ | 重复／英文标识符 | **三处**：①不到 14 ×2（参数说明「不编一个日期出来」vs 口径「不出日期」）；②眉标与 H1 重复「预测体重」（「预测体重」＝2，读图独立核「是，有重复」）；③跨页同一概念两个词（本页「健康范围」＝1 vs 页 07／08「安全区间」，页 07 待核）。英文零命中（R7＝0；读图核除 `kg` 外无拉丁字符）。 |
| ⑤ | 等宽／缺值／小数位 | `tabular-nums` 在册；无表；小数位档内一致（75.1／65 整数目标／−0.34／0.5 到 1.0）；日期值 2027-04-09 落 22px 大数字位，读图未折未溢。缺值：全批无 `—` 用例。徽标对比读图报清晰（首轮实算 4.81:1 达 AA），维持达标。 |
| ⑥ | 手机档／触摸／日期 | 无表。触摸 44px 在册；徽章为 `span` 非可点件。读图：2×2、日期两行各一行、标签与选项卡单行、无横滚；口径行换行两行、竖线引导减弱（与 `flex-wrap` 一致，非缺陷）。 |

**五维打分**（百分制）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **89** | 17.8 | 真结构齐（用 `ilife-status-badge` 形状表状态）。扣 5（数据与日志节空心）＋扣 5（断点不成档）＋扣 1（disabled 复制日志无解释）。 |
| 信息层级与版式细调 | 0.25 | 25 | **86** | 21.5 | 扣 7（间距非 4／8）＋**扣 4（第四卡无值行：值位 3／4 卡，`1fr` 撑等高，卡内空一条值行带，读图「底部 30–40% 空白」互证）**＋扣 3（1440 档底部偏松）。 |
| 分隔符与文字纪律 | 0.20 | 20 | **92** | 18.4 | 扣 8（真冗余 1＋眉标H1重复 1＋跨页术语两词 1＋发丝线记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档全绿，无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 触摸在册、日期不折、无横滚。扣 5（断点 820 ∉ {600,900}）。 |

**加权总分 ＝ 89×0.20 ＋ 86×0.25 ＋ 92×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 17.8＋21.5＋18.4＋15.0＋19.0 ＝ 91.7**
（首轮基线 91.7，持平，无退化；仍为八页最低）

### 页 07 · 模拟减重(每天-300卡)

**产物**：`.scratch/t387/html/07-模拟减重(每天-300卡).html`，76,071 字节，SHA256 前 16 `0B87AD30889DE82B`（与首轮同字节）。

**机器**：`sep.json` 节点级全 0；`resp.json` 三档 `ok=true`、`overflow=0／clippedCount=0／scrollerCount=0／unfitted=0`。

**视觉**：桌面 1440×900（页高 1550）＋手机 390×844（页高 1747）实拍读图。读图：层级清晰（标题「模拟减重（每天 −300 卡）」、结论「每天多减 300 卡，一周大约掉 1.36 kg」）；无被切无横滚无重叠；表左缘与标题／卡片左缘完全对齐；表数值右对齐成列、14 值全一位小数（75.1…57.6）；手机保持表形（非堆叠卡）、日期单行、无横滚、表头可读。

**源码**：「安全区间」=2／「健康范围」=0（本页统一安全区间；与页 06「健康范围」=1 跨页两词实锤）、`<table`=1／`<tr`=15（表头＋14 行）／`data-label`=33、`sec-track`=2（四节四锚点）、「7700」=2、「0.5 到 1.0」=2、`nth-child(3)`=1＋`font-size:28px`=1（第三卡 28px 规则在册）、`white-space:nowrap`=4＋`content:none`=2（≤640 保表覆盖在册）、`<details`=0（无折叠；本页表仅 14 行，非长表）、`1fr`=1、`min-height:44px` 在册、「复制日志」=1（`disabled` 共 5）、`border-left: 1px`=1。本页「不到 14」=0（本族无 14 天规则，口径不同）。

**六问**

| # | 问 | 结论 |
|---|---|---|
| ① | 第一眼层级 | 清楚：眉标「模拟减重」→H1→徽标（卡路里／每天 −300 卡／趋势分析）→结论→四节。四卡：当前 75.1／每天多减 300／每周掉重 1.36／可行性（无值行＋「超范围」）。**读图一条驳回**：「第三卡 1.36 与另三张同字号同粗」——源码 `nth-child(3)`＋`font-size:28px` 各＝1，规则在册，驳回（与首轮页 08 同类驳回一致）。 |
| ② | 挤／空／错位／截断／横滚 | 无溢出无截断（机器三档全绿；读图确认表左缘共线）。**「空」一处**：可行性卡无值行、`1fr` 等高（构造同页 06）。读图建议「页脚改竖线分隔或 `<dl>`」——与 `#505` 豁免冲突，不采纳。 |
| ③ | 分隔符拼串 | 文本层零命中。版式层半命中：发丝线（＝1）串三段（7700 折算／轨迹起算／安全区间）；本轮读图「无竖线」——以源码为准记轻。 |
| ④ | 重复／英文标识符 | **两处真冗余**：「7700」=2（参数说明「每 7700 卡大约对应 1 kg」vs 口径「每 7700 卡大约折算 1 kg」）＋「0.5 到 1.0」=2（参数说明安全区间 vs 口径「安全区间＝每周掉 0.5 到 1.0 kg」）。**跨页一词两用**：本页「安全区间」vs 页 06「健康范围」。英文零命中（R7＝0；读图确认除 `kg` 外全中文）。 |
| ⑤ | 等宽／缺值／小数位 | 本批唯二真数值列之一：表 `tabular-nums` 在册（首轮已核 `:1379-1383`／`:1419-1424`，本轮读图「右对齐成列」互证）；表体 14 值全一位小数；KPI 档内一致。无缺值、无 `—`。 |
| ⑥ | 手机档／触摸／日期 | **读得开（走「保持表格」）**：`nowrap`＝4＋`td::before content:none`＝2，读图三重确认（表形非堆叠、日期单行、数值成列、无横滚，机器 `scrollerCount=0`）。触摸 44px 在册。读图「表头约 13–14px 略小」——具体 px **源码未证实**（本轮无字号探针），且首轮实测 11px 达批内下限线，不扣分。**扣项**：断点 820／640 ∉ {600,900}（手机端维记）。 |

**五维打分**（百分制）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **91** | 18.2 | 真表＋caption／thead／th scope／data-label＋四节四锚点。扣 3（`sec-data` 只有复制块，数据在 `sec-track`，节名不符）＋扣 5（断点九套值、页级与公共层 640 并存互覆）＋扣 1（disabled 复制日志无解释）。 |
| 信息层级与版式细调 | 0.25 | 25 | **92** | 23.0 | 层级清、表成列、盒子共线（读图确认）。扣 5（间距非 4／8）＋扣 3（同排四卡三种数值处理：22px／22px／28px／无值）。 |
| 分隔符与文字纪律 | 0.20 | 20 | **92** | 18.4 | 扣 8（真冗余 2＋跨页两词 1＋发丝线记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档全绿，无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 保表覆盖＋读得开两处互证。扣 5（断点 ∉ {600,900}）。 |

**加权总分 ＝ 91×0.20 ＋ 92×0.25 ＋ 92×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 18.2＋23.0＋18.4＋15.0＋19.0 ＝ 93.6**
（首轮基线 93.6，持平，无退化）

### 页 08 · 模拟减重(每天-500卡)

**产物**：`.scratch/t387/html/08-模拟减重(每天-500卡).html`，76,071 字节，SHA256 前 16 `4DC63A2E3599F58D`（与首轮同字节）。

**机器**：`sep.json` 节点级全 0；`resp.json` 三档 `ok=true`、`overflow=0／clippedCount=0／scrollerCount=0`。

**视觉**：桌面 1440×900（页高 1550）＋手机 390×844（页高 1747）实拍读图。读图：层级清晰（标题「模拟减重（每天 −500 卡）」、结论「一周大约掉 1.54 kg」）；两档无被切无横滚无重叠；四卡（75.1／每天多减 500／每周掉重 1.54／可行性 1694＋超范围徽章完整）；表 14 行（2026-09-16…2026-12-15，与「共 90 天」对得上）数值右对齐、全 1 位小数（75.1…55.3）；手机保持表形、日期单行、无横滚。

**源码**：「安全区间」=2／「健康范围」=0、`<table`=1／`<tr`=15／`data-label`=33、`sec-track`=2、「7700」=2、「0.5 到 1.0」=2、`nth-child(3)`=1＋`font-size:28px`=1、「1.54」=5、「1694」=1、`<details`=0、`1fr`=1、`min-height:44px` 在册、「复制日志」=1、`border-left: 1px`=1、`white-space:nowrap`=4。与页 07 同模板同源码，差异只在内容（500／1.54／1694／表体 14 值）。

**六问**

| # | 问 | 结论 |
|---|---|---|
| ① | 第一眼层级 | 清楚，同页 07。 |
| ② | 挤／空／错位／截断／横滚 | 无溢出无截断（机器＋读图）。「空」一处：可行性卡无值行（同页 07）。读图「手机卡内换行紧凑但完整」——`clippedCount=0`，非缺陷。 |
| ③ | 分隔符拼串 | 文本层零命中。版式层半命中：发丝线（＝1）串三段；读图「无竖线、空格断句」——以源码为准记轻。 |
| ④ | 重复／英文标识符 | **两处真冗余**（「7700」=2、「0.5 到 1.0」=2，与页 07 同）＋**跨页一词两用**（本页「安全区间」vs 页 06「健康范围」）。读图另指三组：①「每天 −500 卡」在标题／徽标／结论／卡面多处——三级导航标签＋结论前置，**不计入**；②「1.54」在结论与第三卡——结论前置形状，**不计入**；③「掉／掉重／缺口」动词混用——同一值的不同句式（结论口语 vs 卡面术语），无新增重复事实，**登记不另扣**。英文零命中（R7＝0；读图确认无 key／占位符／图表库标识）。 |
| ⑤ | 等宽／缺值／小数位 | 真数值列：`tabular-nums` 在册，读图「右贴边成列」；14 值全 1 位小数；日期 ISO 左对齐。无缺值。 |
| ⑥ | 手机档／触摸／日期 | 保持表形（`nowrap`＝4）、日期单行、无横滚（读图＋`scrollerCount=0`）。触摸 44px 在册。读图「行高略密、中段定位感弱」——观感建议，无源码缺陷对应，不扣分。**扣项**：断点 ∉ {600,900}（手机端维记）。 |

**五维打分**（百分制）

| 维 | 权重 | 满权 | 百分制得分 | 折满权 | 扣在哪里 |
|---|---|---|---|---|---|
| 结构纪律 | 0.20 | 20 | **91** | 18.2 | 扣 3（`sec-data` 空心）＋扣 5（断点不成档）＋扣 1（disabled 复制日志无解释）。 |
| 信息层级与版式细调 | 0.25 | 25 | **92** | 23.0 | 扣 5（间距非 4／8）＋扣 3（三值处理并存）。 |
| 分隔符与文字纪律 | 0.20 | 20 | **92** | 18.4 | 扣 8（真冗余 2＋跨页两词 1＋发丝线记轻）。 |
| 跨宽自适应 | 0.15 | 15 | **100** | 15.0 | 三档全绿，无扣项。 |
| 手机端与 HELP 同档 | 0.20 | 20 | **95** | 19.0 | 扣 5（断点 ∉ {600,900}）。 |

**加权总分 ＝ 91×0.20 ＋ 92×0.25 ＋ 92×0.20 ＋ 100×0.15 ＋ 95×0.20 ＝ 18.2＋23.0＋18.4＋15.0＋19.0 ＝ 93.6**
（首轮基线 93.6，持平，无退化）

## 三、汇总

### 3.1 逐页五维与加权总分（八页全表）

| 页 | 结构（×0.20） | 信息（×0.25） | 分隔符（×0.20） | 跨宽（×0.15） | 手机端（×0.20） | **加权总分** | ≥90 |
|---|---|---|---|---|---|---|---|
| 01 预测体重(1周后) | 89 | 88 | 95 | 100 | 95 | **92.8** | ✅ |
| 02 预测体重(1月后) | 89 | 88 | 95 | 100 | 95 | **92.8** | ✅ |
| 03 预测体重(3月后) | 89 | 88 | 95 | 100 | 95 | **92.8** | ✅ |
| 04 预测体重(6月后) | 89 | 88 | 95 | 100 | 95 | **92.8** | ✅ |
| 05 预测体重(自定义时间) | 89 | 86 | 95 | 100 | 95 | **92.3** | ✅ |
| 06 预测体重(自定义目标) | 89 | 86 | 92 | 100 | 95 | **91.7** | ✅ |
| 07 模拟减重(每天-300卡) | 91 | 92 | 92 | 100 | 95 | **93.6** | ✅ |
| 08 模拟减重(每天-500卡) | 91 | 92 | 92 | 100 | 95 | **93.6** | ✅ |

**本席读数**：`count=8`；`mean=92.8`（742.4÷8）；`min=91.7`（页 06）；`max=93.6`（页 07／08）；
`pass90=8`（八页加权总分全部 ≥90）；`lowest=页 06 · 预测体重(自定义目标) 91.7`。

### 3.2 八页共同结论（逐条都有机器读数或源码计数兜底）

| 项 | 结论 | 证据 |
|---|---|---|
| ① 三档无溢出无塌列 | **达标** | `resp.json` 8×3=24 格 `ok=true`、`docScrollWidth==innerWidth`、`overflow=0／clippedCount=0／scrollerCount=0／unfitted=0`；读图 16 张一致无横滚无被切 |
| ① 触摸目标 ≥44px | **源码达标** | `:1163` `.ilife-block-toc a{min-height:44px}`（已读源码逐字）、`summary{min-height:44px}`；本轮读图两次估算 32–36px **均驳回**（截图估算不敌源码保底） |
| ② 与 HELP 同档 | **近似达标，断点值不符** | 字号／留白／触摸三件同档；但页级主断点 820 ∉ 参照件 {600,900}（八页逐页 `max-width:820px` 在册、`max-width:600px`＝0），各页手机端维扣 5 |
| ③ 间距 4／8 倍数 | **未达标** | `padding: 14px`＝4、`gap: 6px`＝6、`margin: 6px 0 0`＝6、`padding: 2px 8px`＝3（页 01 实测，余页同模板同值），各页信息层扣 7 |
| ③ 数值列等宽右对齐 | **达标** | 卡值 `tabular-nums`／页壳 `"tnum"` 在册；页 07／08 读图核右对齐成列；无表六页无数值列可量 |
| ③ 卡／表左缘共线 | **达标** | 页 02／04／07 本轮读图确认对齐；页 01 本轮「缩进」读法源码无对应（`tpd-sec-title{margin:0 0 8px}` 无缩进），记「源码未证实」不扣分 |
| ③ 留白有节 | **偏松（已扣分）** | 1440 档页高 900＝视口（01–06）、1550（07／08）；各页信息层扣 3 |
| ④ 文案零冗余 | **未达标** | 01–05：不到 14 ×2；06：另加眉标H1重复＋跨页两词；07／08：7700 ×2＋0.5 到 1.0 ×2＋跨页两词；口径行发丝线八页记轻 |
| ④ 禁英文内部标识符上屏 | **达标** | `sep.json` 八页 R7＝0；读图多页独立核除 `kg` 外无拉丁字符 |
| ⑤ 缺值写 `—` | **全批无用例可核** | 八页无缺值、页上零 `—`，不作扣分依据 |
| ⑥ 手机档表格读得开 | **达标（保持表格）** | 01–06 无表；07／08 `white-space:nowrap`＝4＋`td::before content:none`＝2，读图确认表形／日期单行／无横滚 |
| ⑥ 日期不被折断 | **达标** | 读图 8 页 16 张一致：2026-09-03／2026-09-16／2027-04-09／表体 14 日期全单行 |
| ⑥ 长表折叠 | **符合本轮预期** | 八页 `<details`＝0；01–08 无长表（最长表为 07／08 的 14 行），应无折叠实无折叠 |

### 3.3 本轮退化点名（相对首轮 V1 基线：01–04 各 92.8／05 92.3／06 91.7／07–08 各 93.6）

| 页 | 首轮 | 本轮 | 差值 | 是否退化 |
|---|---|---|---|---|
| 01 | 92.8 | 92.8 | 0 | 否 |
| 02 | 92.8 | 92.8 | 0 | 否 |
| 03 | 92.8 | 92.8 | 0 | 否 |
| 04 | 92.8 | 92.8 | 0 | 否 |
| 05 | 92.3 | 92.3 | 0 | 否 |
| 06 | 91.7 | 91.7 | 0 | 否 |
| 07 | 93.6 | 93.6 | 0 | 否 |
| 08 | 93.6 | 93.6 | 0 | 否 |

**结论：01–08 零退化。** 产物与首轮逐字节相同（字节＋SHA256 前 16 八页全对上）；本轮改动面（10–13 判词、18–20 标题、长表折叠、报告族）确如编排者口径未波及 01–08：跨页术语「健康范围（页 06）／安全区间（页 07／08）」仍在，属**首轮既有残余**，非本轮新退化；`<details`＝0 与「01–08 无长表、应无折叠」一致。

**本轮读图分歧登记（视觉模型是提示器、不是证据；均已按源码处置，不影响分）**：
1. 页 01「四卡不等高」→ 源码 `1fr` 驳回；页 02／03／04／07 同模板读图均报等高，属读图噪声。
2. 页 01「小节头缩进 16–20px」→ 源码无缩进声明，「源码未证实」不扣分；页 04 读图报严格对齐。
3. 口径行发丝线：页 01–04／07／08 读图报「无竖线」，页 05／06 读图报「有竖线」——源码 `border-left: 1px` 八页同值，可见性属渲染／读图波动，以源码为准记轻。
4. 页 07「第三卡与别卡同字号」→ 源码 `nth-child(3)`＋`font-size:28px` 在册，驳回。
5. 页 01／02 触摸 32–36px 估算 → 源码 44px 保底，驳回。
6. 页 05「72.2 出现 3 次」→ 源码计数 2，读图误计，纠正。
7. 页 07／08「页脚加竖线」建议 → 与 `#505` 豁免冲突，不采纳；页 06「给第四卡补大数字」→ 凭空造数，不采纳。

### 3.4 遗留（本席只评页 01–08，未越界改件）

- 本席**只写两处**：本报告 `docs/skills/skill-calorie/t520-重评2-V1.md`；读数件（`sep.json`／`resp.json`）全部落在 `.scratch/t520-R2-V1/`（工作目录，不入仓）。
- 八页产物（`.scratch/t387/html/0[1-8]-*.html`）**只读**，未改一字。`packages/` 下源码一行未碰。未提交未推送。
- 未评项：页 09–31 不属本席范围。`probes.log` 以 `resp.json`／`sep.json` 落盘为准（只读跑，`OVERFLOW-ZERO failed=0`／`RESULT: 31/31 PASS` 尾行已验）。
- 视觉工具链全程可用：`vision_html_screenshot` 16 张（8 页×桌面 1440×900／手机 390×844，fullPage）＋ `vision_describe` 8 次，无坏件、无重试。
