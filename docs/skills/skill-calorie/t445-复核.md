# #445 独立复核（告警线门对新增超线件与台账陈化的识别力）

**判定：PASS**（票面三件必验全绿：漏报必红／陈化必红／同步器在仓内且安全；四处自设新探针全中；
闸门外两条受控变异「改坏必红／逐字节还原必绿」两行读数齐；无 S1-交付缺陷、无未经记录的过程违规）。

- 日期：2026-09-14。复核者＝独立复核席（**只跑不改实现／测试／台账**，不关票）。
- 票面（权威）：`gh issue view 445 --repo FeatherHunter/ilife --comments`（OPEN；**评论 0 条**，
  API 复核 `gh api repos/FeatherHunter/ilife/issues/445/comments --jq length` → `0`）。
- 现场目录：`.scratch/t445-review/`（本席自建；未复用 `.scratch/t445/`；收尾按路径守卫整体清除）。
- **不采信任何作者结论**：本件每条读数都是本席自己造数据、自己重跑得来；作者读数只作对照项。
- 复核期间仓内**多席在飞**（见 §2、§4），故每条读数都带当刻时间戳；凡红条逐条归属，
  既不把「他席在飞」当本票缺陷，也不把「当时绿」当本票功劳。

## 1 提交范围（七笔）

| # | sha | subject（逐字） |
|---|---|---|
| C1 | `75b80d8` | feat(445): 告警线门从 REQUIRED 硬清单扩成「扫描面＋台账逐件对账」＋ 台账改四列口径并对齐实况 |
| C2 | `1afd7b9` | test(445): 新增告警线门用例（漏报必红／陈化必红／还原必绿 ＋ 缩面负向对照 ＋ 真台账绿） |
| C3 | `46f6c9f` | docs(445): 证据件＋changeset＋对账导出（受跟踪的对账源） |
| C4 | `3b9303b` | feat(445): 红门自带仓内修法（`--sync` 同步器，默认只演练）＋ 生成物按生成器自己的输出声明剔出扫描面 |
| C5 | `0c54a2b` | test(445): 补同步器与生成物剔除用例（`--sync` 回绿／`--dry` 不改文件／判据自证必红） |
| C6 | `af8041a` | docs(445): 证据件补记两处结构回改（`--sync` 入仓／生成物按生成器声明剔除）＋ 第二轮对账导出 |
| C7 | `e06fe48` | docs(445): 台账跟当刻实况再同步一行（`src/analysis/reportPlate.ts` 411→393，他席在飞）＋ 门禁复跑 |

七笔合计动 **8 件**（`git show --name-status` 逐笔核过）：

```
M .changeset/t445-warning-line-ledger.md
A docs/skills/skill-calorie/t445-gate-runs-b.log
A docs/skills/skill-calorie/t445-gate-runs.log
A docs/skills/skill-calorie/t445-告警线门回改证据.md
A docs/skills/skill-calorie/t445-告警线门证据.md
M packages/skill-calorie/AGENTS.md
M packages/skill-calorie/scripts/check-warning-line.mjs
A packages/skill-calorie/test/t445-告警线门.test.mjs
```

- **本包源码目录之下命中 0 件**（本票不许碰源码）：把七笔的文件名并起来，按 `packages/skill-calorie/src/`
  过滤 → 无输出。票面「不改各件源码内容」成立。
- **中文无乱码**：自写体检脚本逐件读七笔的提交信息（`git show -s --format=%B`）、改动件全文、
  以及 `git show` 的 diff 本体，按 `U+FFFD` 与 5 类典型乱码正则筛 → **可疑处 0**
  （读数：`编码体检：可疑处=0`）。
- 现场量法两个 blob 的 LF 口径复算（证据件称「旧 blob 80 行」「接管 blob 219 行」）：
  `66175d75` → **LF=80**、`669227e0` → **LF=219**，与声称一致（`git cat-file -t` 均为 blob，在对象库内）。

## 2 靶向门当刻读数（含逐条归属）

命令：`node packages/skill-calorie/scripts/check-warning-line.mjs`；
`node --test packages/skill-calorie/test/t445-告警线门.test.mjs`。

| 时刻（+08:00） | 门 | 靶向测试 | 当刻红条 | 归属 |
|---|---|---|---|---|
| 21:44 首读 | `exit 1`／`RESULT: 50/51` | `pass 7 / fail 2`（T5／T9） | `RED 漏报（台账没有）：src/analysis/commands.ts LF=426` | **他席在飞**：该件 `git status` 为 `M`，`git diff HEAD` `+103/-1`；**HEAD 上它是 LF=324（线内）**，工作区被撑到 426→437 |
| 21:46 复读 | `exit 0`／`RESULT: 52/52`／`PASS` | `pass 9 / fail 0` | 无 | 他席照本票仓内修法补了台账那行（`git diff` 恰为 `+1` 行，`src/analysis/commands.ts` 挂号 437） |
| 21:50 持锁复跑（本席 `runId=t445rev-gate`） | `exit 1`／`RESULT: 51/53` | `runId=t445rev-test`：`tests 9 / pass 7 / fail 2` | `RED 台账陈化：src/render/sportPortDocs.ts 台账=533 实况=539`＋`RED 漏报（台账没有）：scripts/build-help.mjs LF=362` | 两条都是**他席在飞**（两件均 `M`：`+18/-5`／`+300/-107`） |

- **结论**：本票门与测试的红／绿只随**全仓当刻状态**走；本席观察到的每一次红都能一对一落到他席的
  `M` 件上（`commands.ts`、`build-help.mjs`、`sportDocs.ts`、`sportPortDocs.ts`），**没有一条能落到
  七笔里的任何一件**。第一行读数与作者报的「50/50 PASS」并不矛盾——作者的 50/50 是 21:39 的当刻快照，
  我 21:44 读到的 50/51 是**下一条**他席改动造成的漂移（这正是票面要的「陈化/漏报必红」在现场发作）。
- **收尾快照（21:52，同一命令）**：`exit 1`／`RESULT: 48/53`，红条 5 条：
  `src/render/trendDocs.ts` 台账 818／实况 845、`src/render/trendMiscPortDocs.ts` 563／571、
  `src/render/sportPortDocs.ts` 533／657、`src/analysis/multiTrendPage.ts` 485／492，
  以及 `scripts/build-help.mjs` LF=362 未挂号。**5 条全是他席在飞**（8 分钟里第 3 次漂移），
  没有一条是本票件；这也是 §8 第 5 条的现场样本。
- **21:53 再读（同一命令）**：同样的 5 条红（`src/render/trendDocs.ts` 实况 845→850），
  靶向测试 `tests 9 / pass 7 / fail 2`（T5／T9）。**正向实证一条**：复核期间他席以
  `513997d chore(skill-calorie): 告警线台账补挂 src/analysis/commands.ts（437 行）` 把台账行落了盘
  ——本票「红门自带一条仓内修法、别人照着敲就回绿」这条设计目标**在真仓里被别的席实际用过**，
  不是只写在证据件里的话。
- 作者报的基线（改前 `5/5 PASS`）本席另行取证，见 §4。

## 3 自设新探针（4 条；作者用例覆盖不到的）

### P1 漏报：真树、默认无参路径（作者 T2 只跑夹具入口）
自造 `src/t445-review-probe.ts`（落在本包 `packages/skill-calorie/src/` 下、**正好 LF=360**，未挂号）→
`exit 1`、`RESULT: 50/52`、`RED 漏报（台账没有）：src/t445-review-probe.ts LF=360`（**点名件名＋行数**）；
删件（路径守卫只删该绝对路径，`exists=False`）→ 该红条消失、回到当刻唯一那条他席红。
核 `git status` 无 `t445-review*` 残留。

### P2 生成物剔除正确性与手写件仍在面内（用运行证，不看代码）
在**受控副本** `.scratch/t445-review/pkg-copy/`（`src`＋`scripts`＋`AGENTS.md` 复制件，`--root`／`--agents`
指过去）上做：

- 先把副本同步到绿（`--sync` → `SYNC-WRITE`＋`SYNC-VERIFY ok`＋`RESULT: 52/52`）。
- 三件生成物**人为撑过线**：`src/cli/keys.ts` 167→**500**、`src/cli/registry.ts` 50→**410**、
  `src/triggers/routes.generated.ts` 529→**600** → 门 **仍绿**（`exit 0`／`RESULT: 52/52`／`PASS`），
  且逐条打 `GENERATED-SKIP <件> LF=<n>（由 scripts/gen-cli.mjs／gen-routes.mjs 生成，不在扫描面内）`。
- 三件**在台账里没有表行**（对副本与真台账分别按表行正则筛：命中 **0**）；台账里出现的只是
  「范围」一节的散文提及。
- 手写件 `src/triggers/routing.ts`（267 行）人为撑到 **360** → `exit 1`／`RESULT: 52/53`／
  `RED 漏报（台账没有）：src/triggers/routing.ts LF=360`；逐字节还原 → `exit 0`／`52/52`／`PASS`。
  ⇒ 「谁生成才算生成物、谁 import 生成物不算」是**跑出来的**，不是读代码推的。
- 剔除名单的本席独立核对：`scripts/gen-cli.mjs` 的 `targets` 里三条 `path: join(SRC_DIR, …)` 与
  `scripts/gen-routes.mjs` 的 `const OUT = join(SRC_DIR, 'triggers', 'routes.generated.ts')` 就是全部声明；
  `src/**` 下带「勿手改」印记的件**正好三件**（另两个生成器 `gen-photo-baseline.mjs`／`gen-sot-snapshot.mjs`
  不声明 `src` 输出）⇒ **无过度剔除**。

### P3 同步器安全（真台账，逐字节判）
- `--sync --dry`（真台账）：`exit 0`、`SYNC-PLAN mode=dry 行=23 改=0 增=1 删=0`、
  `SYNC-ADD src/analysis/commands.ts LF=426`、`SYNC-DRY ok`；**前后 sha256 相同**：
  `C99636C64D93F6089052C06EC4C77B7BDE86F47CABCADA88E8A8350AC2408CE7`（`IDENTICAL=True`）
  ⇒ 演练**确实不落盘**（且当时的计划非空，即「有东西要改也不写」这一更强情形）。
- 冻结节变异：把台账 `| src/render/wizardPort.ts | 457 |` 按**字节级**改成 `999`（偏移 7264，命中唯一）
  → 跑 `--sync`（**写模式**）：`exit 1`／
  `RED SYNC 断言 挂号值被改写：src/render/wizardPort.ts（台账 999 ≠ 需求原文冻结值 457）；挂号值是历史事实不回改，该改的是「当场实测」列`／
  `FAIL: 同步断言有红，本次不落盘`；**文件 sha256 与变异后完全相同**（`NO-WRITE=True`）
  ⇒ 同步器**拒绝**把冻结值写坏，而不是顺势写坏。
  同一份变异台账上跑无参门：`exit 1`／`RESULT: 49/51`／`RED 挂号值被改写：src/render/wizardPort.ts（台账 999 ≠ 需求原文冻结值 457）`。
- 还原：按备份逐字节取回 → sha256 回到 `C99636…`（`BYTE-EXACT-RESTORE=True`）。
  **过程披露**：这次临时变异只活了本席两次运行之间；期间他席也往同一文件加了一行
  （`git diff HEAD` 现为 `+1` 行 = 他席那条 `src/analysis/commands.ts` 挂号行），**无内容丢失**。

### P4 台账陈化往返（受控副本，全程 node 内做，避免 shell 引号搅乱）
台账 `src/render/trendDocs.ts` 的「当场实测」格 818 改 700（只在该行上做正则替换，命中唯一）→
`exit 1`／`RESULT: 51/52`／`RED 台账陈化：src/render/trendDocs.ts 台账=700 实况=818`；
**逐字节还原** → `exit 0`／`52/52`／`PASS`、`BYTE-EXACT-ROUNDTRIP=true`。

## 4 改前「无感」负向对照（自己从对象库取旧对，不看作者夹具）

- `75b80d8` 的父提交 `45a069afd8faf2d293e352056556d1b5f5ee6914` 里取出**旧脚本**（LF=80）＋**旧台账**（LF=33），
  落在临时目录里跑：`exit 0`／`RESULT: 5/5`／`PASS: 告警线台账齐全`
  ——**同一棵树当刻有超线件 23 件**（本席自算）。⇒ 票面「对新增超线件无感」当场复现。
- 同一棵树上新门：`exit 1`／`RESULT: 51/53`，红条点名陈化件与漏报件。⇒ 识别力确实来自扫描面，
  不是「多打了几行日志」。

## 5 台账与实况（本席自写解析器，不借作者代码）

脚本 `.scratch/t445-review/lf-ledger-check.mjs`：自己 walk `src/**/*.ts` ＋ `scripts/**/*.mjs`、
自己按 `warning-line-ledger:begin/end` 切片、自己按「读全文、以换行符切分、段数减一」数数（LF 口径）。

- 读数：`扫描面件数=282，其中超线=22；台账行数=22；不一致条数=1`（唯一一条＝他席漏报的 `commands.ts`）。
  **22 行台账里 21 行的「当场实测」与本席自算逐件相等，0 条 STALE**。
- 抽 3 行（台账 → 自算）：`src/triggers/wake-assets.ts` 4747→**4747**；
  `scripts/gen-cli.mjs` 754→**754**；`src/render/wizardPort.ts` 265→**265**。
- 冻结挂号值：#354 冻结值 `src/render/wizardPort.ts`=**457**、`scripts/gen-cli.mjs`=**729**，
  两件「挂号值」列逐字等于冻结值而**未被当成实测值**——`wizardPort.ts` 的实测列是 265（≠457，落回线内、
  挂号行保留），`gen-cli.mjs` 的实测列是 754（比挂号值更超）。口径两说在 `packages/skill-calorie/AGENTS.md`
  与脚本注释里同义写明，本席核过一致。

## 6 对账复跑（两窗口，用受跟踪导出件当对账源）

命令形态：`node tooling/check-gate-audit.mjs --evidence <证据件> --log <导出件> --ticket 445 --since <…> --until <…>`。

| 窗口 | 证据件 | 导出件 | 本席复跑读数 | exit |
|---|---|---|---|---|
| A `13:12:00Z–13:23:30Z` | `docs/skills/skill-calorie/t445-告警线门证据.md` | `docs/skills/skill-calorie/t445-gate-runs.log` | `matched=5/5 auditEntries=5 scoped=5 undeclared=0`／`gate-audit: PASS` | 0 |
| B `13:32:20Z–13:34:00Z` | `docs/skills/skill-calorie/t445-告警线门回改证据.md` | `docs/skills/skill-calorie/t445-gate-runs-b.log` | `matched=2/2 auditEntries=2 scoped=2 undeclared=0`／`gate-audit: PASS` | 0 |

- **声明与条目一对一**：A 件第 135–139 行 5 条 `GATE-RUN` ↔ 导出件第 8–12 行 5 条 `RUN`；
  B 件第 86–87 行 2 条 ↔ 导出件第 8–9 行 2 条，runId 逐字对得上。
- **无「用非零退出条目当门禁证据」**：7 条入证条目在源日志里 `exit` 全为 **0**。
- **无 `--allow-*`**：两件证据＋changeset 全文搜 `--allow`／`GATE-RELAX` **0 命中**；
  工具默认严格口径（`--allow-nonzero` 未用）下 7 条非零条目**一条也没被认领**。
- **导出件忠实**：本席把 7 条导出 `RUN` 行与 gitignored 的 `.scratch/locks/gate-runs.log` 原文逐字比对，
  **7/7 完全一致**（含 `waitedMs`／`pid`／`at`）。
- **窗口外条目清点（本席自己数的）**：源日志里 `ticket=445` 的 `RUN` 共 **19 条**；入证 7 条；
  窗口外 12 条。作者报备了其中 6 条（`t445-w5` 20、`t445-w5b` 20、`t445-c3` 0、`t445-final` 0、
  `t445-c6` 34、`t445-sync2` 0），**未报备 6 条**：`t445-audit1`(0)、`t445-c6` **exit=30**、
  `t445-c6c`(0)、`t445-final2`(**exit=1**)、`t445-c7`(0)、`t445-test2`(0)。
- **「无落盘无提交」核**：七笔提交时刻（+08:00）为 21:21:15／21:22:50／21:24:41／21:32:34×2／21:39:08／21:39:48；
  五条非零条目（13:31:42、13:32:16、13:34:51、13:36:36、13:39:34Z）在这张表上**都没有对应提交**
  ⇒ 与作者「断言红即不落盘、不提交」的说法一致（可核的硬证据是提交时刻表＋台账逐字节归还，
  磁盘当时态无法追溯重建，此点按「无相反证据」结论）。
- **C7 那一轮（`e06fe48`）没有第三窗口、没有受跟踪导出**：它的读数只在提交正文里；本席在源日志里
  找到对应条目（`t445-final2` `exit=1` 红 → `t445-c7` `exit=0` 绿 → `t445-test2` `exit=0`），
  **有留痕、无声明**——见 §8 缺陷 3。

## 7 过程瑕疵判级（作者自曝：`af8041a` 提交信息）

事实（本席按 `git show --name-status` ＋ 提交正文逐条对）：

- 该笔动 **6 件**：`M .changeset/t445-warning-line-ledger.md`、`A t445-gate-runs-b.log`、
  `M t445-gate-runs.log`、`A t445-告警线门回改证据.md`、`M t445-告警线门证据.md`、`M packages/skill-calorie/AGENTS.md`。
- 提交正文**只点名 3 件**（证据件、`t445-gate-runs-b.log`、changeset）；**未点名**
  `packages/skill-calorie/AGENTS.md`、`t445-gate-runs.log`、`t445-告警线门回改证据.md`（后者是新件）。
- 更要紧的是：`AGENTS.md` 那一笔**不是格式修补**——它**加了台账一行**
  （`| src/analysis/reportPlate.ts | — | 411 | …`）并把 `src/render/trendDocs.ts` 的实测列 816→818。
  也就是「**改台账**」这件本票的核心动作之一，在提交信息里一个字都没写。

**判级：S3（记账跟进，不阻塞关闭）。** 理由：① 六件全在本票声明路径内，未夹带他席件，
不构成 §3.2 第 1 条；② 不属 §6「违规」清单里任何一条 S1-过程违规；③ 但 §3.1 要求
「提交信息用中文、说清做了什么」且「新文件必须逐条点名」，本条属**信息不精准**，
且缺的正是最有信息量的那一件（台账变更）。若编排者按「§3.1 是硬约束」判 S2 亦无技术返工，
补一句具名补记即可。

**建议措辞（补记，不许 amend／不许回改历史提交）**：

> 补记 `af8041a` 漏点名的三件：该笔同时改了 `packages/skill-calorie/AGENTS.md`
> （台账加 `src/analysis/reportPlate.ts` 一行、`src/render/trendDocs.ts` 实测 816→818，
> 属台账同步不是格式修补）、重导出了 `docs/skills/skill-calorie/t445-gate-runs.log`、
> 新建了 `docs/skills/skill-calorie/t445-告警线门回改证据.md`。
> 原提交正文只写了证据件、窗口 B 导出与 changeset，信息不全，特此补记。

## 8 缺陷清单

**本票引入**

1. **S3 · 靶向测试的真包断言把「他席在飞」变成自己的红，且测试件头没写明这一依赖。**
   `packages/skill-calorie/test/t445-告警线门.test.mjs` 的 T5（真包门禁绿）与 T9（真包台账已与实况一致、
   `--sync --dry` 零改动）断言的是**全仓当刻状态**，不是本票实现。本席 6 分钟内两次实测到它们转红
   （先 `src/analysis/commands.ts`，后 `scripts/build-help.mjs`＋`src/render/sportPortDocs.ts`），
   而其余 7 例（T1–T4、T6–T8，全夹具）**两次全绿** ⇒ 实现本身稳定。
   作者只在证据件 §9-1 提过这层依赖，测试件头（第 12–14 行）只写「真实门禁读两份」，未警示会因此转红。
   建议措辞（改测试件头即可，不改语义）：
   > 注：T5／T9 断言全仓当刻状态。共享工作区里任何他席把 `src/**/*.ts`／`scripts/**/*.mjs` 撑过 350
   > 或改超线件的行数，本两例即红（修法＝跑一次仓内 `--sync`）；这不是本票实现的红。

2. **S3 · 演练口径的退出码与 `RESULT: n/n` 可被误当绿。**
   `--sync --dry` 在台账已陈化时仍 `exit 0`，且末行打的 `RESULT: 54/54` 是**计划落盘后**的读数
   （本席实测：无参门 `exit 1/RESULT 51/53` 的同一当刻，`--sync --dry` 是 `exit 0/RESULT 56/56`）。
   建议措辞（二选一）：
   > `--sync --dry` 在「计划非空（改＋增＋删 > 0）」时以非 0 退出；或把末行改成
   > `SYNC-PLAN-DRY（未落盘；台账尚未与实况一致，别只看退出码）`，并让 `RESULT` 行只在写模式下打。

**本票范围**

3. **S3 · C7 那一轮的对账留痕不全，两窗口的「逐条报备」也自称过头。**
   §6 已列：`e06fe48` 的读数无第三窗口、无受跟踪导出（源日志里 `t445-final2` `exit=1`、
   `t445-c7` `exit=0`、`t445-test2` `exit=0` 三条无声明）；两件证据都写「窗口外、未声明……逐条报备」，
   实际漏 6 条（含 `t445-c6` 首跑 `exit=30`、`t445-final2` `exit=1` 两条非零）。
   均**未被当作门禁证据**（不属 §2.4 第 3 条违规），但「逐条」二字与事实不符。
   建议措辞（新增一小段即可，含一条 `GATE-RUN runId=t445-c7` 声明与第三窗口读数）：
   > 窗口 C（C7 落点）：`--ticket 445 --since 2026-09-14T13:39:00Z --until 2026-09-14T13:40:30Z`
   > → `matched=…／undeclared=0`；并逐条补报 `t445-c6` 首跑 `exit=30`、`t445-final2` `exit=1`
   > （两条都是断言红／门红的首跑，未落盘未提交）。

4. **S3 · 见 §7**（`af8041a` 提交信息漏点名三件，含台账那一笔）。

**范围外发现（只 S3＋转票措辞，不单独决定判定）**

5. **S3 · 他席在飞的超线件把本门连续打红，本票无法、也不该替它们收口。** 本席实测到的有：
   `src/analysis/commands.ts`（HEAD 324 → 工作区 426→437）、`scripts/build-help.mjs`（362）、
   `src/render/sportDocs.ts`（曾 377，后又落回线内）、`src/render/sportPortDocs.ts`（台账 533 → 实况 539→657）、
   `src/render/trendDocs.ts`（818→845）、`src/render/trendMiscPortDocs.ts`（563→571）、
   `src/analysis/multiTrendPage.ts`（485→492）。
   转票措辞：
   > 请 `#376～#381`／`#384` 及 `scripts/build-help.mjs`／`src/render/sportPortDocs.ts` 的归属票各自认领：
   > 要么按件拆分落回 350 以内，要么跑一次
   > `node packages/skill-calorie/scripts/check-warning-line.mjs --sync` 把台账「当场实测」列对齐当刻实况
   > （`--sync` 会自动补行并提示补写超因／拆法）。本门只报警、不替人拆件。

## 9 未做项与下一手缺什么

1. **本席未做**：未跑 `pnpm gen`／`pnpm --filter build`（票面禁止范围外动作，且会动生成物）；
   未跑 `--allow-*`；未 push；未改任何实现／测试／台账（除 §3 P3 的临时变异，已逐字节还原并留 sha256）。
2. **无法追溯的一项**：`t445-w5`／`w5b`／`c6` 三条非零首跑的「当时磁盘态」不可重建；
   本席只能用提交时刻表＋台账逐字节归还做间接核（结论：无相反证据）。
3. **下一手**：① 补 §8 第 3 条的第三窗口对账（含 `t445-c7` 声明）；② 按 §8 第 1、2 条改测试件头与
   `--dry` 的退出码／末行文案；③ `af8041a` 的具名补记（§7 措辞可直接用）；
   ④ 本门的**常态化**：多席并发下台账会持续陈化，建议在编排口径里写明「**改了超线件就顺手跑一次
   `--sync`**」，否则每次合流都要有人来收一次红。
4. 本件为**单票初审**；关闭口径以合并点安静态复核为准（§5.1）。

## 10 附录：本席探针脚本与复现要点

草稿目录按路径守卫**已整体清除**（本席只往版本库留本件一个文件）。要复现 §3／§4／§5，按下面要点重建即可；
脚本都在 `.scratch/t445-review/` 下、都只读或只写该目录与受控副本：

| 草稿脚本 | 用途 | 复现要点 |
|---|---|---|
| `lf-ledger-check.mjs` | §5 自写台账×实况对账 | walk 本包 `src`（`.ts`）与 `scripts`（`.mjs`）→ 逐件数 LF（读全文按换行符切分段数减一）→ 与台账表行逐格比；剔除名单只认本席手读生成器得到的三件 |
| `stale-test.mjs` | §3 P4 陈化往返 | 读副本台账 → 正则只替换 `trendDocs.ts` 那一格 → 跑门 → 原文本逐字节写回 → 跑门 |
| `mutate.mjs` | §3 P2 加行／还原 | 目标件先备份成同名后缀文件，加行后跑门，`restore` 即逐字节取回并删备份 |
| `negative-control.mjs` | §4 改前无感对照 | 从 `45a069af` 取旧脚本与旧台账两个 blob 落临时包根 → 跑旧脚本记 `RESULT: 5/5` → 同树跑新门 |
| `tamper.mjs` | §3 P3 冻结节变异 | 按**字节**定位 `\| 457 \|` 唯一处、替换成 `999`，命中多处即抛错；还原＝整文件字节拷回 |
| `hygiene.mjs` | §1 编码体检＋blob 行数 | `git show` 取提交信息／改动件／diff，按替换字符与 5 类乱码正则筛；blob 行数按 LF 口径数 |

复核期间的工作区快照（供后续复核者对照本席读数）：`git status` 在飞件约 30 个 `M` ＋ 40 余个未跟踪件，
其中与本包告警线相关的在飞件为 `packages/skill-calorie/AGENTS.md`（他席加的一行）、
`packages/skill-calorie/scripts/build-help.mjs`、`src/analysis/commands.ts`、`src/render/sportDocs.ts`、
`src/render/sportPortDocs.ts`。
