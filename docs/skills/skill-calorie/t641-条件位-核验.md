# #641 深底提示条条件位核验（只核验不施工）

来源：母票 #419；本票 #641。工作区 `D:\ilife`，当刻盘上读数。
纪律：全程只读源码，一行源码未改，未提交，未重跑产物。

## 母票 #419 最后两条结论（转述）

- 2026-09-16 10:40 编排者留言：常驻 4 处已在别的票完成（深底实例为零，当天评论已逐处回写），本票不再施工；条件 7–8 处已切出新票 #641（只核验不施工，等用户点头）。
- 2026-09-16 10:41 关闭留言：常驻已在别的票完成，条件位转 #641 核验，本票使命结束。

本票体量：`weight/compare.ts:532`、`weight/history.ts:652`、`weight/review.ts:558`、
`weight/volatilityDoc.ts:312/319/347/363`，加 `diet/nutritionPortDocs.ts:499-519` 的 `warnBox`。

## 共通前提：`notice()` 当刻已是浅色静态提示

- `packages/skill-calorie/src/shared/copyArea.ts:192-201`：`notice()` 固定走
  `renderFeedbackBlock({ staticNotice: true, toast: { icon, msg, detail } })`，
  注释写明“浅色页内提示，不自造提示通道”。
- `packages/base-render/src/blocks.ts:1133-1142,1188-1198`：
  `staticNotice === true` 时不走冻结 `renderToast`（深色毛玻璃卡加关闭按钮），
  改走 `renderStaticNotice`（浅底加细描边，不带关闭按钮，不进提示栈，不自动消失）。
- 因此：凡经 `notice()` 出来的块，当刻都不再是深底条。
  直接调 `renderFeedbackBlock({ staticNotice: true, ... })` 的那一处结论相同。

## 逐处现状

### 1. `weight/compare.ts:532`

- 现状行：`parts.push(notice({ title: '记录与说明', msg: band.msg, icon: 'warn' }));`
  紧随其后 `parts.push(bulletList(band.items));`。
- 还在用深底条吗：否。经 `notice()`，浅色静态提示。
- 命中才出吗：是。守卫 `if (band)`（`compare.ts:531`），
  `band = premiseNotice(a, b, ...)`（`:526`）。
  `premiseNotice`（`:303-333`）只在以下情况回非空：
  任一段条数在 1 到 `SAMPLE_MIN - 1`（`SAMPLE_MIN = 3`，见 `:119`），
  或锚点缺失，或单日段需说明；否则回空。
- 改不改：建议不改。条件提示，命中才出，且已是浅色形态。

### 2. `weight/history.ts:652`

- 现状行：`if (h.rows.length === 1) { parts.push(notice({ icon: 'warn', msg: '本窗记录太少（比较变化要 2 条以上）', detail: '看不出变化，页照常出，表里的数值照读。' })); ... }`
  另带两行逐条列表说明门槛与下一手。
- 还在用深底条吗：否。经 `notice()`，浅色静态提示。
- 命中才出吗：是。只有本窗恰为 1 条记录时出；0 条走空态块（`:675-681`），2 条以上不出。
- 改不改：建议不改。条件提示，与 0 条空态分开处理，形态已是浅色。

### 3. `weight/review.ts:558`

- 现状行：`if (n < 2) { parts.push(notice({ icon: 'warn', title: '记录太少', msg: n === 0 ? '本窗无体重记录' : '这段时间只记了 1 条' })); ... }`
  另带逐条列表说明趋势图与均值变化的处理。
- 还在用深底条吗：否。经 `notice()`，浅色静态提示。
- 命中才出吗：是。只有记录数 0 或 1 时出；2 条以上不出。
- 改不改：建议不改。条件提示，空态与单条各有一句人话，形态已是浅色。

### 4. `weight/volatilityDoc.ts:312`（窗内只有 1 条）

- 现状行：`premiseNotice` 内 `if (o.points.length === 1) return notice({ title: '记录太少', icon: 'warn', msg: '本窗只有 1 条体重记录：...' });`
- 还在用深底条吗：否。经 `notice()`，浅色静态提示。
- 命中才出吗：是。只有 1 条时出；0 条走 `:345-357` 空态分支，多条走下一分支或回空。
- 改不改：建议不改。

### 5. `weight/volatilityDoc.ts:319`（少于对照门槛）

- 现状行：`if (o.sigmaTrend.length === 0) return notice({ title: '记录太少', icon: 'warn', msg: '本窗只有 ... 条记录，少于 ' + SAMPLE_MIN + ' 条：...' });`
  `SAMPLE_MIN = 3`（本件 `:64`）。
- 还在用深底条吗：否。经 `notice()`，浅色静态提示。
- 命中才出吗：是。只有多条但画不出波动曲线时出；记录充足时 `premiseNotice` 回空（`:325`），调用处 `:372-373` 也不挂块。
- 改不改：建议不改。

### 6. `weight/volatilityDoc.ts:347`（本窗无记录空态）

- 现状行：`if (o.points.length === 0) { parts.push(notice({ title: '本窗无体重记录', icon: 'warn', msg: '窗口 ... 内没有体重记录，出不了平均线与波动带。' })); ... }`
- 还在用深底条吗：否。经 `notice()`，浅色静态提示。
- 命中才出吗：是。只有 0 条时出，且该分支直接组装完整一页后返回，不与后面分支同出。
- 改不改：建议不改。数据型空态页需要这一句，否则读者不知为何没有平均线与波动带。

### 7. `weight/volatilityDoc.ts:363`（只看异常点读法）

- 现状行：`if (only) { parts.push(notice({ title: '这一页只看异常点', icon: 'info', msg: '这一页只列超过波动带的点（共 ... 个）。' })); ... }`
  `only = view === 'anomalies-only'`（`:332`），另带两行逐条列表说明不出曲线图与查看整图的方法。
- 还在用深底条吗：否。经 `notice()`，浅色静态提示（`icon: 'info'` 只换图标底盘语义色，不换深底）。
- 命中才出吗：是。只有 `anomalies-only` 读法出；整图读法不出。
- 改不改：建议不改。读法说明，命中才出。

### 8. `diet/nutritionPortDocs.ts:499-519` 的 `warnBox`

- 现状行：`:499 `if (v.missingFoods.length > 0)` 才 `parts.push(warnBox(...))`；
  `:518-520 `warnBox` 本体为 `renderFeedbackBlock({ title, staticNotice: true, toast: { icon: 'warn', msg, detail } })`。
  件内注释（`:513-517`）写明缺数据盒留在浅底提示块里。
- 还在用深底条吗：否。直接 `staticNotice: true`，浅色静态提示，与 `notice()` 同一形态。
- 命中才出吗：是。只有缺数据食物非空时出；为空时不出。
- 改不改：建议不改。提醒读者有东西没算进去，删掉会造成合计口径误读。

## 总结论

- 8 个位置当刻无一处深底条：7 处经 `notice()`，1 处（`warnBox`）直接 `staticNotice: true`，全部浅色静态提示。
- 8 处全部命中才出：各有明确守卫（条数不足、空态、指定读法、缺数据食物非空）。
- 建议：8 处都不改。常驻条的清理已在别的票完成，本批条件位保留原样即可。

## 等用户点头事项清单

1. 条件 8 处是否全部保留原样（本席建议都不改），请用户确认。
2. 若其中某处仍要调整写法（例如把提示块换成标题下小字或口径行），请用户逐处点名，另行派施工票；本票不施工。
3. `warnBox` 是否视同条件位处理（本席已按条件位核验：命中才出、浅色），请用户确认归类无误。
4. 确认后由编排者在本票评论回写结论并关闭，本席不代关票、不代评论。
