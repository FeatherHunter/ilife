# t334-b1 整页证据（关 #390：goal-weight 返回片段而非完整文档）

归属：`src/goal/` 侧（#390 指针）；`compare.ts` 零改动。本票只做 b1 单件整页化。

## 1. 判据（应完整页，五条）

1. #390 题面即定性为缺陷（OPEN）：`calorie.view.goal-weight` 产物是 section 片段而非完整文档。
2. 该命令有三条唤醒词路由（`src/goal/routes.ts` order 120／219／373：对比体重当前 vs 目标体重／看体重目标进度／我距离目标还差什么）——面向用户，非内部嵌入。
3. 全仓 grep：`data-slot="ilife:calorie:goal-weight"` 引用仅生产处（旧片段函数）＋产物 `b1.html`，零嵌入消费。
4. 同图 17 条体重对比页均为完整文档（`<!doctype html>`＋`ilife-page`，约 60KB）；b1 原 1112B，无 doctype／表／复制／结论。
5. `compare.ts` 并无 b1 专属分支：`viewWeightCompareScenario` 全情景共用 `buildScenarioCompareDoc`，
   scenario b1 别名经 `runScenario` 早已是完整页。真源在 goal 侧
   `src/goal/read.ts::viewGoalWeight` → `src/render/html.ts::renderGoalWeightHtml`（`pageShell` 片段）。
   故「只动该分支」落实为 goal 侧 b1 分支（新建装配＋`read.ts` 一处转调），`compare.ts` 不动。

做法：按 `compare.ts` 同形补 KPI／表／复制（含新 base 双钮：`copyArea` 数据＋日志，
与 `src/render/workoutPlanDocs.ts::dualCopy` 同形）／结论；旧片段函数保留（`src/render/html.ts` 他票在途 M，不碰）。

## 2. 结构五步对账

- 第一步影响清单（4 新 1 改）：新建 `src/goal/goalWeightDoc.ts`（整页装配，2 导出）、
  新建 `test/goal-weight-b1-390.test.mjs`（4 断言）、新建本证据件、
  新建 `.changeset/t334-b1-goal-weight-doc.md`；改 `src/goal/read.ts`（`viewGoalWeight` 转调新装配）。
- 第二步结构设计：新文件职责＝体重目标整页装配，对外 2 件（`buildGoalWeightDoc`／`goalWeightSummary`，
  ≤5）；引用走公开口径（`base-paint/blocks` 区块、`shared/docPage::assembleDocPage`、
  `shared/copyArea::copyArea/copyLog`、`shared/params::nums`）；取数仍走 `render/goalPlate::buildGoalWeight`，零新口径。
- 第三步写代码：跨目录引用均为上述共用位；常量（envelope 头）随各 `*Docs.ts` 先例本地定义。
- 第四步超线报警：三件 LF＝109／149／75，均未超线（350 线，LF 口径）——无需重构。
  范围外所见：`src/render/html.ts` 645 行已超线，但系他票在途改动（本树 M），本票不碰、不代拆。
- 第五步交付对账：下述提交件与第一步清单逐行对上，偏差为零；`compare.ts`、他票 M/?? 件零触碰。

## 3. 改动行号

- `packages/skill-calorie/src/goal/goalWeightDoc.ts`（新建，LF 109）：`gapOf`（距目标，缺数即空态）、
  `goalWeightSummary`（结论句，只用入参起止日期）、`buildGoalWeightDoc`（KPI 四格＋数据表＋复制双钮＋结论折叠＋`assembleDocPage`）。
- `packages/skill-calorie/src/goal/read.ts:14-17,139-162`：`viewGoalWeight` 改调 `buildGoalWeightDoc`（metrics 四键与片段时代逐键一致，复制日志命令由起止日期照抄重跑）；`renderGoalWeightHtml` 进口移除（该函数本体不动）。
- `packages/skill-calorie/test/goal-weight-b1-390.test.mjs`（新建，LF 75）：整页四件套／度量零 churn／双空态／缺失阻断。

## 4. 门禁（GATE-RUN，`--ticket 334`，对账源 `.scratch/locks/gate-runs.log`）

- GATE-RUN runId=f0c1b493-a8d3-43ed-9449-a64edd504417 cmd=pnpm build（exit=0，全量绿，`GEN-STAMP ok`）
- GATE-RUN runId=7bcaaf98-458e-45b6-9c52-9bb55030d521 cmd=node .scratch/334/repro-b1-390.mjs（exit=0，见 §6）
- GATE-RUN runId=678167e7-ba15-4417-8f79-e70c4d83e1de cmd=node --test goal-weight-b1-390（exit=0，4/4）
- GATE-RUN runId=f3b79987-2669-42f1-b552-a39be29b47dc cmd=4 件回归（exit=1：18/19，唯一红为 T10 照片 parity，见 §7 范围外）
- GATE-RUN runId=1fc48778-958d-4c80-a23e-3e2caba3950b cmd=filter build（exit=0，变异轮重建）
- GATE-RUN runId=5ad9b0e3-044b-4841-9075-c4b8556d8404 cmd=新用例变异轮（exit=1，3/4，见 §5 红）
- GATE-RUN runId=24157d2c-54ef-440b-b195-213a009d4da9 cmd=filter build（exit=0，还原重建）
- GATE-RUN runId=2b047d3b-5b48-47e3-b76e-25fc4240b820 cmd=新用例＋t334 用例（exit=0，7/7，见 §5 绿）
- GATE-RUN runId=f892afda-a360-4d03-8190-f8b7b097bcd7 cmd=repro 终轮（exit=0，§6 终值）

## 5. 变异自证（源码级，持锁，释放前已复原）

- 红：表 caption `体重目标（目标` → `体重X（目标` ⇒ `MUT-RED tests=4 pass=3 fail=1`（runId=5ad9b0e3，`.scratch/334/mut-test-red3.log`）。
- 绿：逐字还原（src＋dist 双检 `体重X|还差X` 零命中）＋重建 ⇒ `MUT-GREEN tests=7 pass=7 fail=0`（runId=2b047d3b，`.scratch/334/test-390-green.log`）。
- 附：前两轮变异位选错（断言被结论句同值覆盖，未变红）如实记录，不充数。

## 6. 重出与截图对照

- 重出：`.scratch/334/exports/b1.html`（未跟踪草稿，不提交），59854 字节（原 1112B），
  `doctype=true ilife-page=true`，`metrics={"weightGoal":65,"latestKg":69,"deltaKg":-1.4,"loggedDays":27}`（与片段时代逐键一致）。
  同目录 17 件 sibling 均约 59–60KB，同量级。
- 截图：`vision_html_screenshot` 全页渲染＋`vision_describe` 逐项核验——标题「体重目标」、
  副标题「目标 65 kg vs 最新 69 kg · 2026-08-09 ~ 2026-09-07」、
  KPI 四格（65 kg 截止 2026-12-31／69 kg／-1.4 kg 有记录 27 天／还差 4 kg）、
  六列表（本期一行，值与 KPI 一致）、复制数据▾＋复制日志双钮、结论句「还差 4 kg」完整。
  结论：截图对照通过。

## 7. 超线与用词

- 350 线（LF 口径）：109／149／75 三件均未超线——无需重构（本包规矩：超线是报警不是拦路；未触发）。
- 用词：命令／页面交付／整页装配／唤醒词／复制日志（未用黑话；`wizard` 等词未出现）。

## 8. 范围外与下一手

1. 回归唯一红 T10 照片 parity（`cmd-read-t11.test.mjs:219`，base64 断言，身材照片域）：本票文件集
   （`src/goal/*`＋新用例）与该域零代码路径重叠，判范围外既有红（树上有邻席在途 `photo/` 改动），本票不碰。
2. `SKILL.md` 未跟踪改动系 `build-help.mjs` 生成刷新（hunk 全为示例块），本票不提交。
3. `src/render/html.ts`（旧片段＋他票 M 在途）未动；`gen:check` 未跑（命令登记面零触碰：未增删命令／路由／声明）。
4. 下一手：凭本证据关 #390；b1.html 草稿已覆盖， sibling 对照一致。
