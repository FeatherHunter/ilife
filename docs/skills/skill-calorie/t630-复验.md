# t630 复验 · 接手实施席复跑记录（取代无主草稿运行号）

> 取代关系：本件取代 `t630-证据.md` §二门禁读数与 §五 GATE-RUN 表（前席运行号作废）；证据件其余章节（改动清单／裁定纪要／并发声明）经校验沿用。
> 判定：无主 diff 经复验可接管（补齐＋复验，不重做）。升级红线未触发（`calorie.view.diet-review` 归场景 02，t313a §场景02·24 键含 `-diet-review`）。
> 票面冲突声明：票面「只许写」未列 `AGENTS.md`，但票面编排注具名要求源码落地跑 `--sync` 并同批提交台账——本席以定点改一行（`reviewDocs.ts` 行 362→380，他席行一字未动）执行，未跑全局 `--sync`（会 sweeping 他席在途行）；`routes.generated.ts` 零手改（`gen-check` PASS 为证）。

## 一 · 现场量值（接手当刻）

- `git status`：未提交改动 14 件＋未跟踪 8 件；疑似本票 6 件哈希 `routes.ts 48eb43db`／`review.ts c3f78ba3`／`reviewDocs.ts 5e006497`／`routes.generated.ts ea03f0c7`；他席在途（`multiTrend.ts`／`trend.ts`／`trendMiscPort.ts` 等）一律未碰。
- 本票源码 diff 内容＝票面意图（order 44 加 `entry:"today-nutrition"`；`review.ts` 透传；`reviewDocs.ts` 只换页头三件）；LF 实测 `reviewDocs.ts 380`（HEAD 362，＋18）。
- 台账异常：接手后工作区 `AGENTS.md` 的 380 行被他席 `--sync` 回写为 362（本席源码哈希未变，他席文件未动，属共享台账争用）；本席以定点改一行恢复 380。

## 二 · 本席 GATE-RUN（持锁，日志 `.scratch/t630/own-*.log`，只看尾 5 行）

| runId | cmd | exit | 摘要 |
|---|---|---|---|
| `a0b361da` | `tsc -b packages/skill-calorie --force` | 0 | 空输出绿 |
| `ab99e462` | `node --test t630-今日营养题名` | 0 | `pass 6 fail 0` |
| `b5ad6389` | `node --test t273-review-meal`（回归） | 0 | `tests 30 pass 30 fail 0` |
| `3647c6ff` | `gen-cli.mjs --check` | 0 | `GEN-CHECK PASS 键 129` |
| `df07551c` | `render-evidence.mjs`（两证据页重落） | 0 | 今日营养 `h1=📝 今日营养 meta=看今日营养（饮食）`；对照 `h1=📝 饮食复盘`；字节与草稿一致（95548／95478） |
| `f7fcfc75` | `audit-separators.mjs` 两页 | 1 | 每页命中 2，owner 全＝`ilife-block-caliber`（来源脚注，归 #560）⇒ **非脚注 0**（exit 1 系脚注命中，口径内） |
| `c569a3ba` | `measure-responsive.mjs --widths 390,512,820,1440` | 0 | `OVERFLOW-ZERO pages=2 cells=8 failed=0`；clip 均为分布行 78px（#629 地盘） |
| `0ba40123` | `check-warning-line.mjs` | 1 | `RESULT: 88/91`，RED 仅他席两行（见上）；本文件行无 RED |
| `32cb11bd` | 变异 `node --test t630`（题名改坏，两支同值） | 1 | `pass 3 fail 3`（①④⑤红） |
| `68464e4f` | 变异还原后 `tsc -b --force` | 0 | 绿 |
| `b68c41a8` | 变异还原后 `node --test t630` | 0 | `pass 6 fail 0`；`reviewDocs.ts` 哈希回 `5e006497`（逐字节还原） |

## 三 · 变异两行

```
MUTANT  runId=32cb11bd exit=1 tests=6 pass=3 fail=3（题名改坏→①④⑤红）
RESTORE runId=b68c41a8 exit=0 tests=6 pass=6 fail=0（逐字节还原→绿，哈希 5e006497）
```

## 四 · 裁定（书面，沿证据件 §四，本席复核 t313a 后维持）

1. D2 主结论：`calorie.view.diet-review` 归场景 02，「看今日营养」留 02 页、题名／眉标／副题对齐（H1 `📝 今日营养`／眉标 `看今日营养（饮食）`／副题含 `今日营养`＋读数保留）。不触发升级红线。
2. 82–83 去留：`nutrition-analysis`／`six-factors` 归场景 10（t313a：场景10段逐字在列，`six-factors` 双提到取最小 scene＝10）；02 墙暂留（摘除破 t280 真跑完整性，装配超写集），由场景 10 图／收口票摘除。本票零字节改动。
3. t425 回写：不回写（无口径变更；该件不在写集）。

## 五 · 提交清单（本窗一次提交，不推送）

- `packages/skill-calorie/src/diet/routes.ts`（order 44 一行＋注释）
- `packages/skill-calorie/src/diet/review.ts`（`entry` 透传）
- `packages/skill-calorie/src/diet/reviewDocs.ts`（`ENTRY_TODAY_NUTRITION`＋页头三件分支，362→380）
- `packages/skill-calorie/src/triggers/routes.generated.ts`（`pnpm gen` 派生，order 44 一行，零手改）
- `packages/skill-calorie/AGENTS.md`（定点一行 362→380；提交前复核其 diff 仅此一行，他席行已由他席先行入仓）
- `packages/skill-calorie/test/t630-今日营养题名.test.mjs`（新建，6 条）
- `docs/skills/skill-calorie/t630-证据.md`（无主草稿沿用＋两处运行读数订正）
- `docs/skills/skill-calorie/t630-复验.md`（本件，新建）
