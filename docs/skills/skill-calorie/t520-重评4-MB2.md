# #520 重评4 · MB2 席（页 27–28，独立重测）

**席**：地图 #161 票 #520 独立微评席 MB2（与实施者不同席；实施者结论一律不采信）
**范围**：只评 `D:\ilife\.scratch\t387\html\` 里 `27-`、`28-` 开头 2 页（只读，不改产物／源码，不提交推送）
**基线分（对照用，不照抄）**：27/82.2、28/83.7（上一轮 V4）
**日期**：2026-09-16
**状态**：已完成（骨架→27 落盘→28 落盘→收口；未提交推送）

## 尺（V4 五维，加权公式）

- 结构纪律 20／信息层级 25／分隔符与文字 20／跨宽自适应 15／手机端与 HELP 同档 20
- 加权＝结构×0.20＋信息×0.25＋分隔符×0.20＋跨宽×0.15＋手机×0.20
- 90 线＝逐页≥90

## 身份表

| 页 | 文件名 | 行数 | 正文区 | 基线 R2 |
| --- | --- | --- | --- | --- |
| 27 | 27-看蛋白质摄入报告.html | 2445（实测） | L1844–L1867（结论/卡/图/表/折叠/日志同行块） | 82.2 |
| 28 | 28-看水分摄入报告.html | 2445（实测） | L1844–L1867（同式） | 83.7 |

## 改动面（625-第四轮-证据 §6，本席逐条重验）

- 27/28 达标率补位（toFixed(1)，与 29-3 同档）
- 28-3 是否列左对齐（中文状态词不套数值列规则；27 连带）
- 目标卡去来源句（R2 已落实，本轮回验是否仍在）
- 口径条件化（页脚条件化，永不触发句已删，回验是否仍在）

## 冻结/S3 登记（只登记；实证伪验收结论可单页 FAIL 并点名）

- 达标卡与徽标同源 R-45＋shape-520③
- D-9 贴底机制（27 恒定序列贴底）
- 日志浮层（共享 copyArea）

## 探针验货（只读复用，用前先验）

- R2-V4 `probe6.py`：已读源码（DETAIL/CAPTION/TH 定向检索）；本席用等效 pwsh 只读复核，未直接跑旧探针，不照抄结论。
- R3-TB `probe-27-看蛋白质摄入报告.txt`：验货通过——现产物 `len=81788` 与探针 `CHARS=81788` 一致；`0.0%`=1、`来自目标设置`=0、`其余20行`=1、`是否达标cell-left`=30/`cell-right`=0 均复现，复用可信。
- R3-TB `probe-28-看水分摄入报告.txt`：验货通过——现产物 `len=81863` 与探针 `CHARS=81863` 一致；`66.7%`=1、其余同式复现，复用可信。
- R3-TB `probe2728-cell.txt`：验货通过——两页 `是否达标` 单元格样本 `cell-left` 与现产物一致（27/28 各 30 个 `cell-left`，0 个 `cell-right`；`th` 左对齐各 2 处）。
- vision-router artifacts：仅确认历史截图堆积（7652 条），本轮不复用旧截图，全部重拍。

---

## 页 27 · 看蛋白质摄入报告（已落盘）

**源**：`.scratch/t387/html/27-看蛋白质摄入报告.html`（2445 行；L1844–L1867）
**实拍**：桌面 1440×900 fullPage（pageHeight **1756**，与 R2 同值）＋ 手机 390×844 fullPage（pageHeight **2063**，与 R2 同值）；`vision_describe` 双档已读（无返魂，采信）。工具全程可用，无故障。

### 本轮改动面复验（逐条回源码，实施者结论不采信）

- **达标率补位 ✓ 落实**：卡面 `达标率 0.0%，共 30 天有记录`（L1844 逐字；pwsh `0\.0%`=1、`达标率 0%`整数形=0）；读图确认卡面小字为 `0.0%` 非 `0%`。与 29-3 同档（1 位）。
- **是否列左对齐 ✓ 落实**：`cell-left" data-label="是否达标"`=30、`cell-right" data-label="是否达标"`=0、`th 是否达标 cell-left`=2（pwsh UTF8 实测）；读图桌面 `是否达标` 列左对齐、手机堆叠卡值右对齐（堆叠形态允许）。首轮 F10（中文列错套 `cell-right`）已关闭。
- **目标卡去来源句 ✓ 仍在**：`来自目标设置`=0（pwsh）；目标卡仅 `目标 / 150 g` 无小字（读图复核）；来源信息只在页脚 `目标取目标设置里那一项`。
- **口径条件化 ✓ 仍在**：页脚 `<span>达标判定＝当天记录值达到目标值</span> <span>目标取目标设置里那一项</span>`（L1867 逐字）；`没有设目标`=0（永不触发句未回潮）；`达标判定＝…｜` 含 `｜` 形=0（两 span＋CSS 分隔，非字符拼串，读图竖线为分隔线）。

### 六问（残余照尺重打，每条回源码）

① 层级清：结论句→4 卡→曲线→明细表（10＋折叠 20/30）→chips→数据与日志。② 无溢出（双档读图确认；同份 CSS）；**残余**：曲线贴底平线（读图：y 轴 160/109/58，10 点全贴 58 基线，目标虚线 150 高悬；退化输入未降级）。③ 可见正文无 `；｜·` 拼串（读图＋源码 caliber 无 `｜`）。④ **残余**：`30 天`=6（pwsh；结论/卡1/卡3/卡4＋chip）；`达标 0 天`=2（结论句＋chip；另卡值 `0` 单列，实为三写）；状态同页两制（表内 `>未达标</td>`=30 纯文字 vs 表下 chip `达标 0 天`/`未达标 30 天`，L1844＋读图双证）；目标卡无来源句（上节已证，页脚补偿）。⑤ 蛋白列 `<td>68</td>`=30 右对齐等宽 ✓（读图确认）；是否列左对齐已落实（上节）；达标率 `0.0%` ✓（跨页与 28 的 `66.7%` 同为 1 位，28-5R 连带关闭）。⑥ 手机 2×2 卡＋堆叠表 ✓（读图：2×2 网格＋单列堆叠＋`▷ 其余 20 行（共 30 行）` 独立成行）；44px 同底座（CSS 未动）；日期完整；2.4 屏（2063/844）。

### S3/冻结登记（只登记）

- 达标卡与徽标同源（R-45＋shape-520③）：chips（`达标 0 天`/`未达标 30 天`）与卡面同源计数，双档可见；残余分已在 27-3R/27-4R 体现，**不 FAIL**（实证未证伪验收结论）。
- D-9 贴底机制：9 点同值 68 贴底、目标线高悬，读图实证；属退化输入未降级（27-2R 扣分），**不 FAIL**。
- 日志浮层：L1861 `calorie_data.db ｜ calorie_data.db ｜ 报告子形态 蛋白质摄入报告`＋L1863 `calorie-cmd-read calorie.report.protein …` 原样残留；默认不上屏，轻档记，**不 FAIL**。

### 本页缺陷（R4）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 27-1R | 逐日表 30 行同值同状态（`68`×30／`未达标`×30；折叠后首屏 10 行，降档） | `<td>68</td>`=30＋`>未达标</td>`=30 | 中 |
| 27-2R | 退化输入无降级：平线贴底＋绘图区大部空置（数据退化有客观成分，降档） | 读图（58 基线/150 虚线） | 中 |
| 27-3R | `30 天` 六写／`达标 0 天` 三写残留 | `30 天`=6＋`达标 0 天`=2＋读图 | 中 |
| 27-4R | 状态同页两制（表纯文字 vs chip；对齐部分已落实，不重复扣） | L1844＋读图 | 中 |
| 27-6R | 日志浮层库表名＋「｜」串残留 | L1861–L1863 | 中（默认不上屏） |

首轮 F10（是否列错套数值规则）**已关闭**；跨页精度差（27 侧）**已关闭**（转 28-5R 记关闭）。

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 86 | 17.2 |
| 信息层级与版式细调（权重 .25） | 80 | 20.0 |
| 分隔符与文字纪律（权重 .20） | 78 | 15.6 |
| 跨宽自适应（权重 .15） | 90 | 13.5 |
| 手机端与 HELP 同档（权重 .20） | 84 | 16.8 |
| **本页加权总分** | — | **83.1** |

**扣分口径**：结构 86＝折叠成行，扣「数据与日志」整节只为复制按钮＋明细零信息量立节；信息 80＝是否列左对齐落实＋2（R2 78），仍扣 27-1R/27-2R/27-3R/27-4R；文字 78＝达标率补位落实＋2（R2 76），仍扣 27-3R/27-4R/27-6R；跨宽 90＝双档无溢出同底座；手机 84＝2.4 屏折叠独立成行，扣图表空置＋折叠内同值清单。

## 页 28 · 看水分摄入报告（已落盘）

**源**：`.scratch/t387/html/28-看水分摄入报告.html`（2445 行；L1844–L1867）
**实拍**：桌面 1440×900 fullPage（pageHeight **1756**，与 R2 同值）＋ 手机 390×844 fullPage（pageHeight **2063**，与 R2 同值）；`vision_describe` 双档已读（无返魂，采信）。工具全程可用，无故障。

### 本轮改动面复验（逐条回源码）

- **达标率 ✓（本页无变，回验）**：卡面 `达标率 66.7%，共 30 天有记录`（L1844 逐字；pwsh `66\.7%`=1）；读图确认 `66.7%`；与 27 侧 `0.0%` 同为 1 位，跨页精度差关闭（28-5R 关闭）。
- **是否列左对齐 ✓ 落实**：`cell-left" data-label="是否达标"`=30、`cell-right`=0、`th 是否达标 cell-left`=2（pwsh）；读图桌面 `是否达标` 列左对齐（饮水列右对齐等宽对照）。R2 28-3R 对齐半句关闭，残余仅剩无徽章。
- **目标卡去来源句 ✓ 仍在**：`来自目标设置`=0；目标卡仅 `目标 / 2000 ml` 无小字（读图复核）；来源只在页脚。
- **口径条件化 ✓ 仍在**：页脚同 27 式两 `<span>`（L1867）；`没有设目标`=0；`达标判定＝…｜` 含 `｜` 形=0。

### 六问（残余照尺重打）

① 层级清：结论句→4 卡→曲线→明细表（10＋折叠 20/30）→chips→数据与日志（读图 L1–L12 复核，手机同层级）。② 无溢出（双档确认）；曲线有真实起伏（读图：08-18 高位→08-29 低谷→09-05 反弹→09-16 终点下探；y 轴 1752/2000/2248）；**残余**：目标虚线与 2000 网格线重合（读图实证；虚线样式可区分，降档为小）。③ 可见正文无 `；` 误拼；`｜` 仅两处有意分隔（标签链／页脚定义分隔，读图判定符合排版意图；源码 caliber 无 `｜` 字符）。④ **残余**：`30 天`=5（pwsh；结论/卡1副文/卡3副文/卡4副文/卡4值）、`30 天有记录`=1、`达标 20 天`=2（结论句＋chip；另卡值 `20` 单列，实为三写）；明细 30 行＝3 值×10（2200/2000/1800 各 10，`>达标</td>`=20／`>未达标</td>`=10；折叠后降档）；目标卡无来源句（页脚补偿）。⑤ 饮水列右对齐等宽 ✓（读图）；是否列左对齐 ✓（上节）；达标率 `66.7%` ✓（20/30 自洽）。⑥ 手机 2×2 卡＋堆叠表 ✓（读图：2×2 网格＋单列卡片堆叠＋`▸ 其余 20 行（共 30 行）` 独立成行）；无溢出；44px 同底座；日期完整；2.4 屏。

### S3/冻结登记（只登记）

- 达标卡与徽标同源（R-45＋shape-520③）：chips（`达标 20 天`/`未达标 10 天`）与卡面同源计数，双档可见；残余分在 28-2R 体现，**不 FAIL**。
- 日志浮层：L1861 `calorie_data.db ｜ calorie_data.db ｜ 报告子形态 水分摄入报告`＋L1863 `calorie-cmd-read calorie.report.water …` 原样残留；默认不上屏，轻档，**不 FAIL**。
- 本页无 D-9 贴底项（曲线有起伏，与 27 区分）。

### 本页缺陷（R4）

| # | 缺陷 | 源 | 档 |
| --- | --- | --- | --- |
| 28-1R | 明细 30 行＝3 值×10 轮循环（折叠后降档） | 2200/2000/1800 各 10 | 小 |
| 28-2R | `30 天` 多写／`达标 20 天` 三写 | `30 天`=5＋`达标 20 天`=2 | 中 |
| 28-3R | 状态无徽章（纯文本 `达标`/`未达标`；对齐部分已落实，不再扣） | 读图＋`cell-left`=30 | 中 |
| 28-4R | 目标虚线与 2000 网格线重合（样式可区分，降档） | 读图 | 小 |
| 28-7R | 日志浮层残留 | L1861–L1863 | 中（默认不上屏） |

28-5R（跨页精度差：27 `0%` vs 本页 `66.7%`）**已关闭**（27 侧补为 `0.0%`，两页同 1 位）；28-6（页脚样板句）R2 已关闭，本轮确认未回潮（`没有设目标`=0）。

### 本页评分

| 维度 | 得分(0–100) | 加权 |
| --- | --- | --- |
| 结构纪律（权重 .20） | 86 | 17.2 |
| 信息层级与版式细调（权重 .25） | 86 | 21.5 |
| 分隔符与文字纪律（权重 .20） | 78 | 15.6 |
| 跨宽自适应（权重 .15） | 90 | 13.5 |
| 手机端与 HELP 同档（权重 .20） | 84 | 16.8 |
| **本页加权总分** | — | **84.6** |

**扣分口径**：结构 86＝折叠成行，扣「数据与日志」节；信息 86＝是否列左对齐落实＋2（R2 84；图表有起伏优于 27），仍扣 28-1R/28-2R/28-3R/28-4R；文字 78＝28-5R 关闭＋2（R2 76），仍扣 28-2R/28-7R；跨宽 90＝双档无溢出；手机 84＝2.4 屏，扣状态列难扫＋折叠内循环清单。

---

## 汇总

### 逐页加权总分表＋相对基线（R2）变化

| 页 | 页名 | 结构 .20 | 信息 .25 | 分隔符 .20 | 跨宽 .15 | 手机 .20 | **R4 加权** | R2 基线 | Δ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 27 | 看蛋白质摄入报告 | 86 | 80 | 78 | 90 | 84 | **83.1** | 82.2 | +0.9 |
| 28 | 看水分摄入报告 | 86 | 86 | 78 | 90 | 84 | **84.6** | 83.7 | +0.9 |

**判定**：逐页≥90 线——27（83.1）与 28（84.6）**均未达线**。两页均值 83.85；区间 83.1–84.6。

### 改动面关闭清单（本席重验）

| 改动 | 页 | 状态 |
| --- | --- | --- |
| 达标率补位 toFixed(1) | 27（`0%`→`0.0%`，`0\.0%`=1）／28（`66.7%`保持） | ✓ 27 落实、28-5R 关闭 |
| 是否列左对齐 | 27/28（`cell-left`=30、`cell-right`=0、`th-left`=2） | ✓ F10/28-3R 对齐半句关闭 |
| 目标卡去来源句 | 27/28（`来自目标设置`=0，读图卡内无小字） | ✓ 仍在，未回潮 |
| 口径条件化 | 27/28（`达标判定`两 span、永不触发句=0） | ✓ 仍在，未回潮 |

### S3/冻结登记汇总（逐条给了 FAIL 判断，无一 FAIL）

| # | 项 | 实证 | FAIL？ |
| --- | --- | --- | --- |
| S3-1 | 27/28 徽标与卡同源（R-45＋shape-520③） | chips 与卡面同源计数，双档可见；残余分已在 27-3R/27-4R/28-2R 体现 | 否 |
| S3-2 | 27 恒定序列贴底（D-9） | 10 点贴 58 基线、目标线 150 高悬，读图实证；属退化输入，27-2R 扣分 | 否 |
| S3-3 | 日志浮层（共享 copyArea） | 两页 L1861–L1863 原样残留（库表名＋`｜`＋命令链），默认不上屏 | 否 |

### 残余 Top（下一手缺什么）

1. **结论复述卡面（族级，中）**：27-3R（30 天六写／达标 0 天三写）／28-2R（30 天多写／达标 20 天三写）——下一手结论句只给判断＋增量，数字留卡面。
2. **中文状态列无徽章（27-4R／28-3R，中）**：对齐已落实，纯文字难扫未动——下一手徽章化（shape-520③ 允许范围内）。
3. **27 零信息量明细＋贴底空置（27-1R/27-2R，中）**：30 行同值同状态＋平线远低于目标线——下一手退化输入降级（空状态／抽样／区间收敛三选一，D-9 需设计口）。
4. **28 循环清单＋虚线重合（28-1R 小／28-4R 小）**：3 值×10 轮＋目标虚线与 2000 网格重合——下一手错开网格或注一句。
5. **日志浮层模板（轻×2 页）**：与 24/25/26/29/30/31 同源残留——转共享件票（S3-19 候选），本票不动。

### 方法与限制（不掩盖）

- 先建骨架，27/28 各即评即落盘，收口最后落；未攒稿。
- 视觉：4 张 `vision_html_screenshot`（1440×900／390×844 fullPage，2 页×2 档；pageHeight 27/28 均为桌面 1756／手机 2063，与 R2 同值）＋ 2 次 `vision_describe`（每页双档一次）；工具全程可用，**无返魂、无故障**，故无「照实记故障」条目；未拍 768 档（同 R2 口径，以同份 CSS 判读替代）。
- 源码：只读产物（2445 行×2），未碰 `packages/`；定向检索＋pwsh UTF8 计数（`cell-left/right`、`0.0%/66.7%`、`30 天`、`68/2200/2000/1800`、`达标/未达标`、`details/summary/tr`、`｜`），每条扣分均回源码；读图与源码矛盾时以源码为准（本轮无矛盾）。
- 探针只读复用且先验货（R3-TB 两页 `len` 逐字节吻合才复用；R2-V4 只读源码未跑）。
- 产物只读：未改产物、未改源码、未提交未推送。
- 独立性：实施者结论一律不采信；基线分仅对照，评分逐维重打（27 +0.9、28 +0.9 均来自本席实证：精度补位＋是否列左对齐落实）。
