# t255 单一来源证据：命令表首列由路由层派生

> 票：#255（场景 06 目标管理：生成器单一来源与缺失清单）。实施席只做调查席写集内的事；
> 写集：`packages/skill-calorie/scripts/build-help.mjs`、`packages/skill-calorie/SKILL.md`（仅 AUTO 块）、
> `packages/skill-calorie/test/t255-单一来源.test.mjs`、本证据件。红线：不写工作流程文本（归 #292）；
> SKILL 块外一字不动；不动 #152 的账；`src/goal/commands.ts` 等声明件一律不碰（别人的件）。

## 一、必报五步

1. **影响清单（一行一能力目录）**：生成器 `packages/skill-calorie/scripts/build-help.mjs`（派生＋硬失败＋清单）→
   说明面 `packages/skill-calorie/SKILL.md`（仅 AUTO 块重生成）→ 门禁 `packages/skill-calorie/test/t255-单一来源.test.mjs`（新）。
2. **结构设计**：不新建目录层级，不碰三个以上能力；沿用饮食 `#278` 先例（`dietRepresentatives()`），
   加 `goalRepresentatives()`（新词优先否则冻结首词）＋ `checkGoalCoverage()`（双向硬失败）＋
   `buildMissingReport()`（全量软清单）；`wakeOf()` 优先级＝饮食＞目标＞REPR 兜底＞命令名。
3. **写码**：见 §二。
4. **超线报警**：已超线，需要根据规则进行重构。`scripts/build-help.mjs` 本改前已 554 行（LF 口径，告警线 350），
   本次 554→742（＋188：目标派生 9 键、双向检查、软清单、落盘打印）。超因：HELP 注入、饮食逐词表、目标派生、
   示例占位符四组逻辑同住一件。本次先不拆：拆分（按场景族切姊妹件）不在 #255 写集，属结构票地盘；待收口票认领。
   附带：本次改动使告警线台账 `build-help.mjs` 行 `554` 陈化（实况 742），按纪律应由台账票跑 `--sync`，
   本票不碰 `AGENTS.md`（别人的件），见 §六。
5. **交付对账**：偏差为零（§三～§五）。

## 二、改了什么

- `build-help.mjs`：新增 `GOAL_READ_KEYS`（9 读键，场景 06 自管，不含场景 01 代管的 `view.goal-progress`）、
  `GOAL_FAMILY_KEYS`（15 键＝写 5＋读 10，硬失败范围）、`goalNewRoutes()`、`goalRepresentatives()`、
  `checkGoalCoverage()`（29 词人人有路由／15 键个个有词／9 代表词人人路由回同键／9 词互不重复，不过即抛）、
  `buildMissingReport()`（全量无词／错配／重复，只列不抛）；`buildHelpBlock()` 先调硬检查再取数，
  自述由三来源改四来源（目标 9 条单列）；`runMain()` 落盘 `dist/help-missing.json` 并控制台打印
  `MISSING-*` 三组（目标族已硬失败，此处只剩其余各域）。
- `SKILL.md`：仅 AUTO 块重生成（`renderSkillMd` 只重写标记内），块外逐字节不动。
- `test/t255-单一来源.test.mjs`：新（5 用例，真 4 假 1）。

## 三、8 处错配清零（改前→改后，逐行对照）

| # | 命令 | 改前首列 | 改后首列 | 口径 |
|---|---|---|---|---|
| 1 | `calorie.view.goal` | 看今日目标进度（占两行其一） | 看目标完成度 | 路由场景 06 首词（order222） |
| 2 | `calorie.view.goal-config` | 定营养目标（写词指查看） | 看目标配置 | 路由新拟词（`NEW:4`） |
| 3 | `calorie.view.goal-recommend` | 定营养目标(自动算)（写词指查看） | 看目标推荐 | 路由修复词（`REPAIR:0`） |
| 4 | `calorie.view.goal-weight` | 定体重目标（写词指查看） | 看体重目标进度 | 路由场景 06 首词（order219；场景 03 `对比体重…` 非本族词，不取） |
| 5 | `calorie.view.goal-progress` | 看今日目标进度（占两行其二） | 看今日目标进度（不变，现唯一） | 场景 01 代管键，本票不改；上 1 改后重复消除 |
| 6～8 | `goal-expiring`／`goal-predict`／`goal-vs-actual` | 改前已是唤醒词（历史命令名问题此前已由 REPR 修好） | 不变（路由核对一致） | 本次由派生重算逐字确认 |
| 另 | `goal-wizard` | 看目标预检 | 看目标预检（不变） | 新词优先命中，与派生一致 |

去重：`定营养目标`现只领 `goal.set` 一行；`定体重目标`现只领 `goal.weight` 一行；
`看今日目标进度`现只领 `goal-progress` 一行。目标 9 代表词互异（`new Set` 9/9）。

## 四、判据对账

- **构建期缺失清单落盘＋控制台打印**：`runMain` 写 `packages/skill-calorie/dist/help-missing.json`
  （`{at,noWake,mismatched,duplicated}`，不跟踪，随 dist 同生共死）并打印
  `MISSING noWake=1 mismatched=36 duplicated=1 → …/dist/help-missing.json` 起三组 `MISSING-*`。
  当刻读数：`noWake=[view.label-precheck]`（唯一无路由键）；`mismatched` 36 皆非目标族；
  `duplicated=[看今日主页→help.lookup|view.home]`（跨场景遗留，非本族，不管）。
- **目标族缺项即红（真假对照各一）**：真＝`checkGoalCoverage()` 在真实路由上过
  （`{wakes:29,keys:15,reps:9}`）；假＝合成输入删 `看目标配置` 即抛并点名（见 §五）。
- **SKILL 目标段无错配无重复**：`t255` 第 3 用例逐行钉 5 行首列＋3 组去重（上表）。
- **全包靶向测试不新增红**：`skill-t11`＋`skill-md-photo-285`＋`t367` 共 22 用例全绿（§五）；
  钉旧手写表的断言无需同批改——`photo-285⑤` 钉的是非目标 `photo-picker` 退回命令名（本票保留非目标兜底），
  `t367` 的 `ROUTE-UNBOUND` 4 行目标声明旧词仅信息读数（该门明示不判红，声明件本身按 `#318` 裁决留待产品定，
  本票只修展示层派生，不动声明）。

## 五、机器读数

- `t255`（5/5 绿）：`ℹ tests 5`／`ℹ pass 5`／`ℹ fail 0`；关键行
  `GOAL-COVERAGE ok（wakes=29 keys=15 reps=9 dupe=0）`、
  `MUTATION-GOAL-MISSING ok（删看目标配置必红，还原必绿）`。
- 关联门（22/22 绿）：`ℹ tests 22`／`ℹ pass 22`／`ℹ fail 0`（`skill-t11` 9＋`photo-285` 8＋`t367` 5）。
- 变异自证（源码级一行）：`build-help.mjs` 加 `m.set('calorie.view.goal-config','定营养目标')` →
  红 `ℹ tests 5`／`ℹ pass 0`／`ℹ fail 5`；逐文件还原后绿 `ℹ tests 5`／`ℹ pass 5`／`ℹ fail 0`；
  持锁期内还原干净（`git diff` 无 `MUT-` 残留）。
- 生成：`SKILL.md 10 +++++-----`（4 行首列＋1 行自述）；`HELP 已注入`＋`MISSING … → dist/help-missing.json`。

## 六、没做完与下一手缺啥

- `tsc -b packages/skill-calorie` 当刻红于别人的件（`src/render/planEditorPort.ts:232-233 TS2554`），与本票无关，
  本票 JS 只读已编好 `dist/`，未重编；需该件归属票修。
- 告警线门当刻已红 2 行（`trendPredictDocs 1128→1159`、`rankingDocs 367→369`，皆在途他席），
  本票再加 1 行预期陈化（`build-help 554→742`）；三行待台账票 `--sync`，本票按写集纪律不碰 `AGENTS.md`。
- 工作流程文本归 #292；`src/goal/commands.ts` 4 行旧声明词（`t367 ROUTE-UNBOUND` 那 4 行）留待产品裁决，
  本票展示层已由路由覆盖，声明层未动。
