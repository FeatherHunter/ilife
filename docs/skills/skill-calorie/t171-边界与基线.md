# t171 · 边界与基线（场景 10 拆 A：多指标趋势与对比）

> 票：#171（地图 #160）。本票只写文档与 `.scratch/` 脚本，不改 `src/` 行为。
> 数字以 `.scratch/t171/classify.mjs` 输出为准，本文件不存第二份清单。
> 用词照 `docs/agents/wording.md`：命令／跑得通／跑不通／过程型／结果型／回执／预检确认页／页面交付／命令补齐。

## 0 · 复算读数（权威）

复算命令：`node .scratch/t171/classify.mjs --check`（2026-09-14 实跑已绿）：

```
TOTAL: 169
A: 88
B: 81
UNASSIGNED: 0
INTERSECT: 0
DUP: OK
A_BREAKDOWN: combined=71 multi_trend=15 trend_pair=2
CHECK: PASS
```

对账完备性：`node .scratch/t171/check-reconcile.mjs --check`（42 条没有命令可执行的记录逐条在 §1 表中）：

```
NONEXEC_TOTAL: 42
COVERED: 42
MISSING: 0
KINDS_MISSING: 0
CHECK: PASS
```

本票无 `src/` 行为改动：`git status --short -- packages/skill-calorie/src/` 为空（收尾前复核）。

## 1 · A-list 权威定义（A／B 互斥，覆盖 169 条）

规则（MAP #160 Destination）：A ＝ `key` 为 `calorie.view.combined` 的全部
＋ `reason` 含“无多指标趋势键”的全部（15 条整体趋势）
＋ `key` 为 `calorie.view.long-trend`／`calorie.view.calorie-trend` 的对照系；
B ＝ 其余全部。A／B 互斥、无遗漏无重复（`unassigned=0`、`INTERSECT=0`，见 §0）。

### 1.1 A-list（88 条，本图）

| 组 | 条数 | 构成 | 说明 |
|---|---|---|---|
| 两指标配对（`calorie.view.combined`） | 71 | `routes.ts` order 260–319（60）＋ 377–386（10）＋ new order 6（1） | 已有命令可执行；#377 页面交付＋pair／window 接线核验 |
| 多指标整体趋势（`reason` 含“无多指标趋势键”） | 15 | `routes.ts` order 339–353 | 老技能做过、新仓没有命令可执行；新命令面溶进 #376（1 条含目标对比先验）／#378（剩余 14 条） |
| 趋势对照系（`calorie.view.long-trend` 1 ＋ `calorie.view.calorie-trend` 1） | 2 | new order 45（看热量趋势）、new order 47（看整体趋势） | 单指标对照基线，与多指标页共用趋势形态；归本图 |

### 1.2 B-list（81 条，非本图，只定归属不定长相）

| 组 | 条数 | 去向 |
|---|---|---|
| 健康报告（`calorie.view.health` 13：order 320–330、423、new 9） | 13 | #161（预测与报告） |
| 报告子形态（没有命令可执行 8：order 331–338） | 8 | #161 |
| 诊断（`calorie.view.anomaly` 22：order 354–371、374–376、new 30） | 22 | B（诊断／解释类，非趋势对比；具体票号由编排者定，不属本图） |
| 营养（`calorie.view.nutrition-analysis` 3：order 387–388、new 48 ＋没有命令可执行“看营养建议”1） | 4 | B（营养场景归属由编排者定，不属本图） |
| 六因素（`calorie.view.six-factors` 2：order 413、new 51） | 2 | B（单点快照，非多指标趋势；归属由编排者定） |
| 预测（`calorie.view.predict` 6：order 393–397、new 29 ＋没有命令可执行 15：order 398–412） | 21 | #161 |
| 热量缺口（`calorie.view.deficit` 2：order 426、new 7） | 2 | #161（`calorie_deficit.html` 本图不碰） |
| 健康／数据工具（`calorie.view.lint-health` 2、承认 `calorie.history` 2、`calorie.help.lookup` 1、`calorie.view.review-template` 1） | 6 | B（单指标查询／工具／报告模板，非多指标对比） |
| 定时复盘（没有命令可执行 3：order 415、418、425） | 3 | 不做（新架构规格已划出；词只保证命中与文案，见 §2） |

诊断 22 条／营养 3＋1 条／六因素 2 条至此定案：归 B，不属拆 A。本票不定 B 的页面长相，不替 #324 搬视图层。

### 1.3 与 #172（拆 B 边界票）商量一致情况

本票只输出 A／B 归属行，不定 B 的页面长相与命令形状；#172 定 B 侧时若对上表 B-list 内条目提出异议，由编排者裁定，本票停在归属行不改 B 的行。撰写时 #172 未认领、无已落地的 B 定义与本票冲突。

## 2 · 老技能对账（防幻觉词，42 条逐条）

权威源：`D:\2Study\StudyNotes\SKILLS\卡路里\scripts\_triggers.py`（436 条；
头部记“分析 154 场景：A1 组合分析 60 ＋ A2 健康报告 19 ＋ A3 整体趋势 15 ＋
A4 自动分析 23 ＋ A5 营养分析 16 ＋ A6 预测模拟 20 ＋ 单点 1”），
对照 `.scratch/wayfinder-s1/reconcile-old-triggers-20260911.txt`
（新仓判跑不通的 95 条在老表 95／95 全存在，其中 91 条带完整 `output_type` 与模板定义）。

结论先行：本 `routes.ts` 内 42 条没有命令可执行的记录，在老表里 42／42 全存在，
无“新仓凭空造词”（幻觉词）；
反向（老技能有、新仓冻结表丢了的词）本次未发现：A1 组合分析老 60 条对新 `combined` 71 条
为窗口展开演进（7 天／15 天／30 天／60 天／90 天／180 天／365 天／本周／本月／自定义等），
非丢词，不开票。

“哪一种”三类（MAP #160 Notes）：a＝老技能做过、新仓没有命令可执行；
b＝必须多步交互；c＝新架构规格划出去不做。本 42 条中 b＝0（分析 13 条命令全是查询命令，
冻结表 prompt 无“先确认”写法，见 §6）。

### 2.1 报告子形态 8 条 → B（a）

老表：有词，`output_type=result`，模板 `combined_analysis.html`
（当年 `report --kind` 的 BMI／TDEE／BMR／蛋白／水分／评分／趋势／对比形态）；
新仓 95 条命令内无同形报告命令（`view.health` 只有完整健康盘）。归 B（#161）。

| 唤醒词 | 老技能当年怎么做 | 哪一种 | 去处 |
|---|---|---|---|
| 看BMI报告 | 老表有词 ＋ result ＋ combined_analysis.html（report --kind 系） | a：老技能做过、新仓没有命令可执行 | B（#161） |
| 看TDEE报告 | 同上 | a | B（#161） |
| 看BMR报告 | 同上 | a | B（#161） |
| 看蛋白质摄入报告 | 同上 | a | B（#161） |
| 看水分摄入报告 | 同上 | a | B（#161） |
| 看综合评分 | 同上 | a | B（#161） |
| 看健康趋势 | 同上 | a | B（#161） |
| 看健康报告(含对比) | 同上 | a | B（#161） |

### 2.2 整体趋势 15 条 → A（a，老 trend --group g1–g11＋4 对比）

老表：15／15 全存在，`output_type=result`，模板全是 `combined_analysis.html`；
其中 11 条带 `trend --group g1–g11`（当年 `python scripts/render_analysis.py --view trend --group gN --window 90d`），
4 条为对比维度（月度／季度／年度／目标对比，同模板）。
新仓 `view.combined` 只吃两指标配对，无多指标趋势命令（`reason` 原文）。
归 A；其中“看整体趋势(含目标对比)”进 #376 作先验 1 条，其余 14 条进 #378。

| 唤醒词 | 老技能当年怎么做 | 哪一种 | 去处 |
|---|---|---|---|
| 看整体趋势(体重+摄入+运动) | trend --group g1 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(体重+体脂+围度) | trend --group g2 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(饮食+蛋白+纤维) | trend --group g3 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(运动+力量+有氧) | trend --group g4 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(BMI+体脂+肌肉量) | trend --group g5 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(摄入+蛋白+运动) | trend --group g6 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(体重+蛋白+缺口) | trend --group g7 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(体重+摄入+缺口) | trend --group g8 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(体重+摄入+运动+缺口) | trend --group g9 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(蛋白+运动) | trend --group g10 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(综合多指标) | trend --group g11 ＋ result ＋ combined_analysis.html | a | A（#378） |
| 看整体趋势(含月度对比) | 老表有词 ＋ result ＋ combined_analysis.html（对比维度） | a | A（#378） |
| 看整体趋势(含季度对比) | 同上 | a | A（#378） |
| 看整体趋势(含年度对比) | 同上 | a | A（#378） |
| 看整体趋势(含目标对比) | 同上 | a | A（#376 先验 1 条） |

### 2.3 营养建议 1 条 → B（a）

| 唤醒词 | 老技能当年怎么做 | 哪一种 | 去处 |
|---|---|---|---|
| 看营养建议 | 老表有词 ＋ result ＋ combined_analysis.html | a：老技能做过、新仓没有命令可执行（`view.diet-review`／`view.health` 给数据盘，不给建议条目） | B |

### 2.4 预测模拟缺参 15 条 → B（a）

老表：15／15 全存在，`output_type=result`，模板 `combined_analysis.html`；
新仓 `view.predict` 只有开始／结束／`horizonDays`（体重外推），
无目标体重／每日缺口／摄入速率参数（`reason` 原文）。归 B（#161）。

| 唤醒词 | 老技能当年怎么做 | 哪一种 | 去处 |
|---|---|---|---|
| 预测体重(自定义目标) | 老表有词 ＋ result ＋ combined_analysis.html | a | B（#161） |
| 模拟减重(每天-300卡) | 同上 | a | B（#161） |
| 模拟减重(每天-500卡) | 同上 | a | B（#161） |
| 模拟减重(每天-700卡) | 同上 | a | B（#161） |
| 模拟减重(30天减Xkg) | 同上 | a | B（#161） |
| 模拟减重(60天减Xkg) | 同上 | a | B（#161） |
| 模拟减重(90天减Xkg) | 同上 | a | B（#161） |
| 模拟减重(自定义天数减Xkg) | 同上 | a | B（#161） |
| 摄入预测(按当前速率 1 周) | 同上 | a | B（#161） |
| 摄入预测(按当前速率 1 月) | 同上 | a | B（#161） |
| 摄入预测(按当前速率 3 月) | 同上 | a | B（#161） |
| 摄入预测(自定义) | 同上 | a | B（#161） |
| 摄入预测(营养目标达成预测) | 同上 | a | B（#161） |
| 摄入预测(卡路里缺口预测) | 同上 | a | B（#161） |
| 摄入预测(摄入稳定性预测) | 同上 | a | B（#161） |

### 2.5 定时复盘 3 条 → B（c，不做）

老表：3／3 全存在，但无 `output_type` 与模板（当年 `render_cron_setup.py`，
新架构 `docs/calorie-architecture.md` 已明确划出）。词只保证命中与文案，
执行层不承接。归 B（Out of scope）。

| 唤醒词 | 老技能当年怎么做 | 哪一种 | 去处 |
|---|---|---|---|
| 开启定时复盘 | 老表有词，无 output_type／模板（当年 render_cron_setup.py） | c：新架构规格划出去不做 | B（不做） |
| 查定时复盘 | 同上 | c | B（不做） |
| 关闭定时复盘 | 同上 | c | B（不做） |

## 3 · 页面归类（A-list 归 2 类，主力 `combined_analysis.html`）

冻结表（`src/triggers/scene-10-analysis.ts`）实测：分析类 `html_template`
几乎全是 `templates/combined_analysis.html`，唯缺口类是 `templates/calorie_deficit.html`
（`查热量缺口` order 200）。故：

| 类 | 覆盖唤醒词 | 模板 | 归谁管 |
|---|---|---|---|
| ① 两指标配对页 | A 组 combined 71 条 | `combined_analysis.html`（本图主力） | 页面装配就地写（只此一处用）；图／表／KPI／复制区复用公共层（§4） |
| ② 多指标趋势与对照页（新增形态） | A 组 15 条整体趋势 ＋ 对照系 2 条（`long-trend`／`calorie-trend`） | `combined_analysis.html` 同模板、新形态（通用分析页：多指标趋势形态，本图预期贡献） | 形态落卡路里包内公共层（多个场景用得到即提取）；更通用的图／时间窗部件用 `packages/base-render/src/` 现成件，不重复造 |
| （本图不碰） | 缺口 2 条 | `calorie_deficit.html` | 归 #161，本图不碰 |

`goal_config.html` 不属本图（目标管理场景），本票不列。B 侧页面长相本票不定。

## 4 · 公共层复用清单（先复用现成的，再补自己缺的；只定接口，不定长相）

### 4.1 `packages/base-render/src/`（跨技能，通用即用）

| 件 | 吃什么数据 | 有哪些位置可以填 | 本图用法 |
|---|---|---|---|
| `blocks.ts` 区块组件（KPI 卡／网格／数据表／图块／列表／折叠／参数表单／复制区等） | 各区块自持结构 | KPI 数值、表头＋行、图载荷、复制文本 | 两类页的主力装配单元 |
| `template.ts` `fillTemplate` | `{template, assets, data?, strict?, content?}` | 五个占位符 | 文档页装配 |
| `controls.ts`（错误回执／状态徽章／空态／复制按钮行） | 各控件输入 | 文案＋状态 | 空态／缺失阻断／复制区 |
| `text.ts` `buildDataText` | `{envelope, format?, title?, occurredAt?}` | 按形状投影的复制文本 | 复制区文本态 |
| `style.ts`（样式变量＋控件样式） | `{prefix?, extraCss?}` | 主题前缀、额外样式 | 整页样式 |
| `charts.ts`（折线／柱等） | 各图表输入 | 序列、基线、标注层 | 趋势曲线、目标线、异常点标注 |
| `contract.ts`（`escapeHtml` 等） | 待转义串 | — | 全部用户侧文案转义 |

判定：通用（别的技能也用得到的图／表／样式／复制区）→ 用 `packages/base-render/src/` 现成件，不重复造。

### 4.2 卡路里包内公共层（多个场景用得到即提取）

| 件 | 吃什么数据 | 有哪些位置可以填 | 本图用法 |
|---|---|---|---|
| `src/shared/docPage.ts`（整页装配） | 区块 HTML 串 ＋ 标题三件套 | `CONTENT` 槽、标题、图表位 | 两类页整页装配（已在饮食／营养／运动／趋势四域复用） |
| `src/shared/copyArea.ts`（复制区） | prompt 文本、数据、日志、提示 | 0–3 颗按钮；给了数据自动出三格式菜单 | 结果页复制区 |
| `src/output.ts`（交付出口） | 文档串＋命名参数 | 落盘与回执（绝对路径语义） | 结果型 HTML 落盘＋回执给绝对路径 |
| `src/render/receipt.ts`、`src/render/envelope.ts` | 写入结果／交付字段 | 回执字段、交付三态 | 本图无写命令，仅作回执形状参照 |
| `src/analysis/*` 取数（`series.ts` 数列、`cross.ts` 配对、`trend.ts` 趋势） | 库连接＋窗口 | 起止、窗口、配对 | 两类页唯一取数来源，不自造 SUM |
| `src/render/analysisPlate.ts`、`insightPlate.ts`、`trendMiscPort.ts` 等视图层 | 各盘视图 | KPI／图／表／复制区四块 | 复用现成盘面；归属以 #324 裁决为准，本图只新增自己页面的装配 |

判定：只对卡路里多个场景有价值的（通用分析页多指标趋势形态）→ 落卡路里包内公共层；
只此一处用的页面装配 → 就地写。

### 4.3 本图补增（②类页新增形态）

通用分析页（多指标趋势形态）：吃多序列（2–4 指标）＋窗口＋对照基线（目标／同期）；
可填位：序列选择、窗口、对照线、KPI（均值／变化／相关性）、图、表、复制区。
#376 立起最小形态后，后续票只读复用。

## 5 · T2–T4 写入集切分（#376／#377／#378 不撞笔）

原则：命令面溶进页面票（翻转一条没有命令的词是跨文件原子改动）；
公共部件 #376 立起后只读复用；`SKILL.md` 生成块只有 #379 动，谁都不手改；
生成链四步（`pnpm build → pnpm gen → pnpm build → pnpm help:build`）由翻转票走。

| 票 | 写入集（文件级） | 说明 |
|---|---|---|
| #376 先验形状（多指标最小跑通：含目标对比 1 条＋新命令 1 条＋通用分析页最小形态） | `src/analysis/commands.ts`（加 1 条命令声明）＋ 新子功能文件（多指标趋势取数／视图，不碰既有文件）＋ 包内公共层新件（通用分析页骨架）＋ `src/analysis/routes.ts`（仅该 1 条：加 exec 记录）＋ `src/triggers/scene-10-analysis.ts`（仅该 1 条：`main_prompt.cli` 与 `data_source`）＋ 生成物重算（`test/calorie-sot.snapshot.json` 走脚本、`test/calorie-routing-81.test.mjs` 计数、`docs/research/t81-exec-smoke.md` 重生成） | 唯一做翻转＋立公共部件的票；生成链先例在此验证 |
| #377 页面①（combined 71 条：页面交付＋pair／window 接线核验） | combined 页面装配文件（既有或新建的 combined 装配，不碰 multi-trend 装配） | 71 条已有命令，**不动** `routes.ts`／冻结表／生成物；与 #378 写入集不相交，可并行 |
| #378 页面②（整体趋势剩余 14 条＋对照系：翻转＋铺开，命令面溶进本票） | multi-trend 装配文件 ＋ `src/analysis/routes.ts`（仅剩余 14 条记录行）＋ `src/triggers/scene-10-analysis.ts`（仅对应 14 条）＋ 生成物重算 | 不碰 combined 装配；与 #377 文件级不重叠，可并行（都只等 #376） |

持锁命令一律走 `node tooling/run-locked.mjs --ticket <本票号> -- <命令>`；
每票只 `git add` 自己声明的路径。

## 6 · 种子基线＋过程型核验

种子基线（照 `docs/research/t81-seed.mjs`，T6／T7 共用同一口径）：
`SEED_TODAY=2026-09-07`（`CALORIE_TODAY` 钉死）；`seedFull` 含档案＋目标（热量 1800／饮水 2000 等）
＋ 2026-09-01～07 饮食／饮水／运动 ＋ 2025-09-07～2026-09-07 逐日体重（367 天）
＋ 体成分／围度 ＋ 4 周计划种子；占位符替换
`<开始日期>`→2026-09-01、`<结束日期>`→2026-09-07、`<日期>`→2026-09-06 等；
只写系统 tmp，不入仓。

“跑得通”口径（MAP D6）：固定种子库上，无数据时可读缺失阻断、不落盘也算跑得通；
真坏只认报错／白页／空页／非绝对路径／打不开。#380 与 #381 同口径。

过程型核验（“写类流程先出过程型 HTML”逐条结论）：
分析 13 条命令全是查询命令（`src/analysis/commands.ts` 头注释“13 键，全部读命令”）；
A-list 88 条冻结表 prompt 全文搜“先确认／先问／确认后再写” 0 命中
（`Select-String scene-10-analysis.ts` Count 0）。
结论：A-list 0 条适用先出过程型 HTML，无需开票，记账（MAP D5）。

## 7 · 交付对账

- 分类脚本：`.scratch/t171/classify.mjs --check` 绿（§0 读数；总数 169、A 88、B 81、`unassigned=0`、交集 0）。
- 对账完备性：`.scratch/t171/check-reconcile.mjs --check` 绿（42／42 在 §2 表中）。
- 对账件入仓：本文件 `docs/skills/skill-calorie/t171-边界与基线.md`。
- MAP 回写：#160 `Decisions so far` 追加一行 gist＋链接（另行 `gh issue edit 160 --body-file`）。
- 本票无 `src/` 行为改动（`git status --short -- packages/skill-calorie/src/` 为空）。
- 对抗审查 D1–D8 归档 `.scratch/160/`（本轮已有 `对抗审查.md`，本票复用不重复落盘）。

## 8 · 遗留出口记账

- 幻觉词反向（老技能有、新仓冻结表丢了的词）：本次未发现（§2 结论），不开票。
- “先确认”写法的词：逐条核验 0 条（§6），不开票。
- A／B 划分与 #172 冲突：撰写时无冲突；若 #172 侧提出异议，停下报告编排者，不自行改 B 的行。
