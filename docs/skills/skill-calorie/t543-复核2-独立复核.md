# 票 #543 第二轮独立复核（复核席 · 独立复跑）

> 复核对象：`425de9d`／`9160236`（返工两笔主体）＋`2f5d57f`／`35d90e4`（证据补正式分）；上一轮复核 `b05891a` 判 FAIL 的三条 S1 宣称已清。
> 复核席只写本件；**未改**实施席代码与判据。全部编译／重出／变异／提交经 `node tooling/run-locked.mjs --ticket 543 -- …`。
> 日期：2026-09-16；进场 HEAD＝`fd30caa`，复核期间别席提交到 `0bda0ed`（本席读数与之一致，未受影响）。

---

## 一、判定与五维打分

**判定：PASS**（无 S1；均分 88 ≥ 85）。S2×1（本票）＋S2×1（范围外）＋S3×2（本票）。

| 维 | 满分 | 得分 | 依据 |
|---|---|---|---|
| 契约一致 | 30 | 28 | 票面验收命令逐条我自己跑绿（见 §二）；交付物三件齐（判据件／证据件／重出产物）；4 笔提交 `--name-only` 与声明逐笔相符，无越界写入 |
| 证据真实可复现 | 25 | 20 | 声明的 15 个 runId 在门禁源与入仓导出件里**逐条都在**；r10 钉的三页 SHA256 与盘上**逐位相同**；r3 的 281 行／140 行 `83.2` 复算**逐字相符**；扣分＝视觉分同像素两读 89／93.7（S2-1） |
| 新旧对照 | 20 | 18 | 分数轨迹 r1→r10 逐轮有报告与截图可查；13 页重出与盘上被评字节归一化后只差 1 处（别席改动）；缺陷逐条给归因 |
| 工程红线 | 15 | 14 | 无 `reset`／`stash`／`clean`／`amend`／`rebase`；4 笔 sha 全在祖先链上；变异只在持锁期存在、出窗逐文件 SHA256 还原一致；未 push |
| 文档同步 | 10 | 8 | 证据件带具名更正、口径清楚；扣分＝「r5 与 r6 同产物」与 r6 原文相反（S3-1）、台账结论列仍是 `--sync` 占位且正文引了旧行数（S3-2） |

---

## 二、机器证据（我自己跑的）

| 用途 | runId | 命令 | exit | 机器读数 |
|---|---|---|---|---|
| 票面验收全套 | `41fe7bb2-3aed-4a65-aeb2-5de5b2f849f8` | `pwsh -File .scratch/t543/review2/window-rev.ps1` | **0** | `REV-R2 tscPkg=0 tscForce=0 run13=0 byteSame=0 byteDiff=13 resp=0 sepRed=0 scan=0 只数13=0 只算13=11 只数39=2 test543=0 reg=0 warn=0` |
| 自设变异窗 | `59d4d72a-8843-45c7-a22c-5a42f054d183` | `pwsh -File .scratch/t543/review2/window-mut.ps1` | 0 | `MUT-R2 mA(build=0 test=1) …`（详见 §四） |
| 出窗修复＋恒真对照 | `ca7a6956-38fa-4fa5-963c-2fde666b75a0` | `pwsh -File .scratch/t543/review2/window-repair.ps1` | 0 | `REPAIR-R2 修好(build=0 test=0 reg=0) 重出 run13=0 sepRed=0 只数13=0 只算13=11 变异串残留=0 │ mB恒真对照(build=0 test=0 reg=0) │ 出窗(build=0 test=0 reg=0)` |
| 自跑 CDP 量尺 | `0622b418-f1fa-4873-a91b-b5c6ddb22bd8` | `node .scratch/t543/review2/probe-cdp-rev.mjs` | 0 | 3 页 × 2 档 6 组 DOM 读数（见 §四试法③） |

**逐项读数（我自己跑出来的）**

- 重出：`MINE13 n=13 exit0=13 complete=13 BAD=0`；乙轮 39 条 `OK=39 BLOCKED=0`。
- 三档溢出：39 页 × 390／768／1440 ＝ **117 格，failed=0**。
- 逐页分隔符：13 页探针逐个 `exit=0`（`sepRed=0`）；`scan13` 摘要 `节点级命中 0 的页=13/13 行级 0 的页=13/13 exit0 的页=13/13 节点级命中合计=0`（原 `NaN` 已修）、`机器词页=0 脏数页=0`。
- 只数／只算：13 页 `只数`＝**0**（本席 `Select-String` 自跑）、`只算`＝**11 页上屏**；39 页里那 2 处 `只数` 落在 `32-看力量训练总览`／`33-看有氧训练总览`，源文＝`src/render/sportPortDocs.ts:222,339`（#544 的件，票面「不许动」名单内）——**归因成立**。
- 判据／回归／门：主判据 `tests 6 / pass 6 / fail 0`；族回归 4 件 `tests 47 / pass 47 / fail 0`；告警线门 `RESULT: 91/91`＋`PASS 告警线台账齐全且与实况一致`。
- 编译入口：`node node_modules/typescript/bin/tsc -b packages/skill-calorie` ＝ 0，`--force` 整编同 0（排除增量骗绿）。
- 可见面机器词：13 页原始串命中 13 处，**全部在 `data-t` 复制载荷内、载荷外 0**；脏数（≥6 位小数）原始串 0 命中。

**上一轮三条 S1 的当刻状态（逐条复验）**

| 缺陷 | 我的复验 | 判 |
|---|---|---|
| **S1-1** 产物不对版 | ① 13 页 `只数`＝0（自跑）；② 重出后与盘上被评字节**归一化**（抹去 3 处运行时钟）后**逐页只差 1 行**，差的是内联 CSS 里 `.ilife-page .ilife-block-toc a` 的 `padding-inline`（14→16px、窄屏 6→8px）；该处来自 **`fbba22e`（#523 席，04:55:21，晚于本票最后一笔 `9160236` 18 分钟）**，且 `git log 9160236..HEAD -- …/exercise/receipt.ts` **为空** ⇒ 装配源码一字未再动 | **清** |
| **S1-2** 视觉 ≥90 | r10 报告 §0 钉的三页 SHA256 与本席当刻复算**逐位相同**（`7FA1D1EA…E28139`／`E49E69D8…AAD9B`／`00C7B119…FD60D2`）；6 格全填、平均 93.7、P0／P1 空 ⇒ **有正式分**。但同一批像素 r9 记页 89（见 S2-1） | **达成（可复现性存疑）** |
| **S1-3** r3 记录不实 | `视觉复评543-r3.md` **LF＝281**、第 140 行 `平均总分 = … = 499/6 ≈ 83.2`、6 行打分表齐——与证据件的更正**逐字相符**；该文件 mtime 还停在 13:30（未被事后改写） | **已更正（更正本身属实）** |
| **S1-4** 门禁对账 | 证据件声明的 15 个 runId 在 `.scratch/locks/gate-runs.log` 与入仓导出件里**逐条存在**（各 2 行＝START＋RUN）；导出件 210 条 vs 源 214 条，缺的 4 行＝导出器自己的 RUN 行（导出不可能含自己）＋`commit-v8`（`7354fe55`，导出后 2 秒才跑）＋本席这一轮（进场更晚） | **已重导（结构上不可能差 0）** |
| **S3-①** 胶囊守卫 | 见 §四试法①：源码级退回 `join('，')` 判据⑤ **必红**；把守卫改恒真后**整套（含族回归）转绿** ⇒ 这条只有 ⑤ 抓得到 | **清** |
| **S3-②** `scan13` NaN | 我自己跑的摘要行是 `节点级命中合计=0`（不是 `NaN`） | **清** |

**提交链独立核**：`425de9d`／`9160236`／`2f5d57f`／`35d90e4` 逐笔 `git merge-base --is-ancestor <sha> HEAD` ＝ **exit 0**；`git show --name-only` 逐笔＝5／5／2／1 件，与证据件声明一致，无越界件。

**分数轨迹复算**：r2 84.0／r3 83.2（6 格齐）／r4 表空「待填」／r5 91.5／r6 85.8／r7 86.2／r8 86.5（皆 6 格齐）／r9 只 2 格、平均「—」／r10 93.7（6 格齐）——与证据件 §五 的表**逐条相符**。

---

## 三、缺陷清单（分级／归属／怎么重现）

1. **S2（本票）· 视觉 ≥90 的可复现性不足。**
   `t543-r9-*` 与 `t543-r10-*` 的 6 张整页图 **SHA256 逐字节相同**（本席实比），即评的是同一批像素；r9 记页 **89**（①17②19③18④17⑤18），r10 同一页 **93**（①19②19③19④18⑤18）。本票此前以「同一字节两读、从紧」为由弃用 r5 的 91.5，本轮采的是同一字节上的**宽**读（89 那轮因「表没填满」不计分）。另：r10 brief **新增 §8「不改的几条（照实记、别当缺口、不计分）」**（r8 brief 无此节），r10 报告 §4 又把页面最弱的一段（中段 7 行灰字带）列「不扣分」⇒ 93.7 **只覆盖页级还能自己改的活**。
   重现：`Get-FileHash .dsh-vision-router/artifacts/t543-r9-ji-1440x900-fullpage.png` 与 `t543-r10-*` 同项比哈希；两份报告 §1 打分表并读；`.scratch/t543/vision-brief-543-r8.md` 与 `-r10.md` 做 `git diff --no-index`（本席实测 +=28／−=15，新增即 §8）。
   **请编排者裁**：「≥90 是否达成」要不要按这个口径认。
2. **S2（范围外）· 被评产物在当刻 HEAD 上不可原样重出。**
   `fbba22e`（#523 席）改了本票在用的 `src/exercise/sportUi.ts:104,123`（toc 内距 14→16px／窄屏 6→8px）；13 页重出后与 r9／r10 评的那批字节归一化后**逐页只差这一处**（本席实测 `byteSame=0 byteDiff=13`）。后果：r10 评的字节配置 **HEAD 上已不存在**；自跑 CDP 量到导航胶囊 1440／390 档为 **86／70px**，而 r9／r10 记的是 **82／66px**——差 4px 与 −2px×2 逐值吻合。不是本票过错（本票的件被别席合法改动），但「重出后字节与盘上一致」这条**当刻在 HEAD 上跑不成立**。
   重现：`node tooling/run-locked.mjs --ticket 543 -- node .scratch/t543/run13.mjs`，再与本席备份 `.scratch/t543/review2/before-*.html` 做「抹时钟后逐行比」（脚本见 `window-rev.ps1` 第 3 步）。
3. **S3（本票）· 证据件一句话与引用件相反。**
   §五 写「r5 **与 r6 同产物**却差 5.7 分」，而 `视觉复评543-r6.md:149` 原话是「**两轮评的不是同一版**（第 5 轮评的是 01:04 那一批产物）……**分歧在评分尺不在一版式**」。后半句引对、前半句是加戏（同一段自相矛盾）。弃用 r5 的结论不受影响（不同版更该弃）。
4. **S3（本票）· 台账结论列与一行旧数。**
   `packages/skill-calorie/AGENTS.md` 里 `src/exercise/receipt.ts` 的结论列仍是 `--sync` 自动补的「超因与拆法待补」占位（「当场实测」439 与实况一致，门 91/91 绿）；证据件 §六第 6 条引的 `427` 是 `9160236` 之前的旧数（当刻实况 **439**）。证据件自己写明「结论列归收口票」，故只记不判。

---

## 四、自设试法（专打「脚本自我满足」）

**试法①（源码级变异 ＋ 恒真对照，两条读数）**

| 行 | 变异 | build | test | 读数 |
|---|---|---|---|---|
| 改坏必红 | `sportUi.fieldGrid` 上游 `renderChips({items:…})` → `labels.join('，')` | 0 | **1** | 红条 `✖ #543 ⑤ 字段胶囊…`；逐页 `T543 GRID … 容器里没有胶囊件（renderChips）` **9 页全红** |
| 恒真对照 | 同上 ＋ 判据件 `fieldBlockOf()` 改**恒真** ＋ 件内负向断言中和 | 0 | **0** | 主判据 **绿**、族回归 4 件也 **绿** ⇒ 这条退回**没有第二个断言抓得到**，判据⑤ 是唯一载荷（有判别力，非自我满足） |
| 还原必绿 | 逐文件从 `bak-*` 还原 | 0 | 0 | `SHA A/B/C cur==bak` **逐位相同**；出窗后 `git status -- packages/skill-calorie` **空** |

**试法②（真产物 vs 装配源码：页面上的数是不是源码算的那件事）**
抽页 01（记运动，`git show 9160236:packages/skill-calorie/src/exercise/receipt.ts` 与 HEAD 同字节）：
页上「当日累计（写后现值）」＝ `记录日期 2026-09-15／运动条数 6 条／消耗累计 1570 卡／时长累计 180 分钟`，`记录号 #8414`，明细＝id 8414 那行（慢跑 30 分钟 320 卡）。
我在**同一次重出的种子库副本**上按源码口径 `EX_ALIVE`（`COALESCE(is_deleted,0)=0`）现算：`count=6 / sum(duration_minutes)=180 / sum(calories_burned)=1570 / max(id)=8414` ⇒ **逐个相同**（软删除行不计的口径也真在库里生效）。

**试法③（自跑 CDP 量尺复算一页）**
自写 `probe-cdp-rev.mjs`（真 Chrome，headless，1440×900／390×844）量 3 页 6 组：内容列 **960px 宽**（232.5→1192.5，含 15px 滚动条）、`overflow=0`、`under44=[]`（两档都没有 <44px 触摸目标）、底部两键 **476×476×44**（390 档 175／175）、`.sui-facts` 4 列 ×228 ＋16 槽、`.sui-fact-k` 色 `rgb(110,110,115)`。**并量出别席改动的影响**：导航胶囊 86／70px（r9／r10 的 82／66 是改前那版）——这正是 S2-2 的量化证据。

**试法④（可见面反查）**：13 页原始串里机器词命中 13 处**全在 `data-t` 载荷内**、载荷外 0；脏数原始串 0 命中；`只数` 0 命中、`只算` 11 页。

---

## 五、路径

- 我的复核件：`docs/skills/skill-calorie/t543-复核2-独立复核.md`（本件）
- 我的草稿与读数：`.scratch/t543/review2/**`（`window-rev.ps1`／`window-mut.ps1`／`window-repair.ps1`／`patch.mjs`／`probe-cdp-rev.mjs`／各步日志／`before-*.html` 被评字节备份／`hashes-before.txt`）
- 被复核件：`docs/skills/skill-calorie/t543-证据.md`、`docs/skills/skill-calorie/t543-GATE-RUN.log`、`packages/skill-calorie/src/exercise/{receipt,sportUi}.ts`、`packages/skill-calorie/test/exercise-text-shape-543.test.mjs`

---

## 六、未做项 ＋ 下一手缺什么

**未做**：① 视觉席 6 张整页图我只抽看 01 页两档（未逐张独立打分，故 §三 S2-1 只给「可复现性」这一判，不替评审席改分）；② r6／r7／r8 那几轮的 CDP 读数未复跑（只复算当刻产物几何）；③ `.scratch/t543/work/**` 的库副本未逐库核对。

**下一手缺**：① 等别席（#523 等）全部落定后**统一重出一遍**，把 13 页新哈希重新钉进证据件——否则「视觉 93.7 评的那批字节」永远对不上 HEAD；② 若要求视觉分可复现，请给评分尺定一条「同一字节重复评分的容差」并复跑一轮（本轮同一像素两读差 4.7 分）；③ 台账 `receipt.ts` 结论列的「超因与拆法」正文已写在证据件 §六第 6 条，归收口票抄进 `AGENTS.md`。
