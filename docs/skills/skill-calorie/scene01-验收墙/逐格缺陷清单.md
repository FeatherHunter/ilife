# 场景01 验收墙 · 逐格缺陷清单（9 格）

- 口径：[`docs/agents/视觉验收墙.md`](../../agents/视觉验收墙.md)（**墙 ＋ 逐格缺陷清单 ＋ 自检退出码，三样齐**；§3 收口七步、§4 完成判据、§7 坑表）。
- 格与件同源：本清单的格号／唤醒词／文件名逐字取自同目录 `manifest.json`（9 行），墙页的格数与它相等。
- **本件不代签**：仓规 §4 的「人用真实浏览器滚一遍两张墙」是负责人的动作。本件做的是把**现成审查材料翻译成逐格**（每格写清「该看什么／已知什么缺陷／归哪张票」），另附本席自己的机器读数。
- **本批是骨架批**：票面明写「产物先不要正式发布（另一席正在改主页族四词的视图，产物马上会变）」。所以下面凡引上一轮读数的地方都注了**它的版本**；定版后要按当刻产物复跑。

## 0 先说两条本席实测的口径缺口（别当已读过）

1. **票面点名的 `docs/skills/skill-calorie/t351-视觉验收墙-做法.md` 在本仓不存在**：`git ls-files`（`core.quotepath=false`）无此件、全树 `glob **/*做法*` 零命中（2026-09-15 实测）。本批按仓规 §6 的骨架与四条形制落地，并把票面括号里点到的九条（同目录／iframe 真渲染／双端 390·1280／生成器入仓／自检正反两面／不加 `lazy`／索引含「有意不出产物」节／宽产物按比例缩显示／同名只在一处算）逐条落进 `gen-wall.mjs`。**这一条先记在件头**，做法件补齐后按它复核。
2. **上一轮读数与当刻副本不同版**：终审（`.scratch/t401j/review/终审.md`）量的是 `看今日主页.html` **77,034 B**，本目录当刻副本是 **77,081 B**（`.scratch/t375/products/` 2026-09-15 12:40 重出版）；`看本月主页` 的缺数占位终审记 **43 处**、当刻副本 **51 处**。两版不是同一份，下面这几条按「上一轮读数」记，**定版后必须复跑**。

## 1 逐格（9 格）

| 格 | 该看什么 | 已知缺陷（症状 ＋ 出处） | 要不要改 / 归哪张票 |
|---|---|---|---|
| **1 · 看今日主页**（今窗） | 主页族样板格：四张 KPI 卡齐平、图轴刻度取整、表箱在中等宽度居中 | ① **821–1000 档表箱左贴不居中**：`.ilife-block-data-table{max-width:680px;margin:16px 0}`，图表卡 780 宽而表箱 680，右侧空 **100px**（终审 §5-1，扣 5）；② **512 档图轴注 20px**（正文 12–13px 的 1.6 倍），`88` 标签右缘 71.3 与首点屏幕 x 76.8 只隔 **5.4px**（终审 §5-2，扣 4）；③ CSS 522 行起 `.ilife-help-shell` 一组**死码**（DOM 里 `querySelector` 为 null，扣 1）；④ **词面与页面口径不符（本席在墙上新发现，材料里没有）**：念「看今日主页」，格子里渲染出来的 h1 是 **「近 7 天总览」**（2026-09-01 至 2026-09-07，chips「有记录 5/7 天」）——本页缺省窗口是 **7 天**，不是今日 | **要改**。① 落**公共层** `.ilife-block-data-table` 规则、③ 死码归产物 CSS；② 与 **#512**（图件轴标签字号断点跳变）同族；④ 是产品口径问题。**①③ 未见点名票**（#513 是「三处加槽」、#512 只举 820 档），认领请回写 **#401**；④ 归 **#375** 当场裁 |
| **2 · 看本周主页**（7 天窗） | 与格 1 并看：两格**屏幕上是不是同一页**（同键 `calorie.view.home`） | **（换版前读数）本席实测：与格 1 屏幕上完全一致**（同是「近 7 天总览」、同窗口 2026-09-01 至 2026-09-07、同数字）——两页 HTML 长度都是 75,091 字符，**唯一差异是页内「复制日志」载荷里的一处生成时间戳**（`12:40:03` vs `12:40:08`，长度相同、不上屏）。终审的同族核对只点名了 `看本月主页` 与 `看今日体重概览`，**本格没有独立读数** | 不改（同族两条公共层缺陷随格 1）；「两条词出一页」归 **#375** 裁。本格缺读数这一行由定版后的复跑补。**——换版后已可分**：元素级最小差异 138 行、第一屏标题序列五档各异（见 §2） |
| **3 · 看本月主页**（30 天窗） | 缺数占位密度、长表在 1000/1440 档的居中、比值写法 | 终审 §4 同族核对：四档 `relTop +13.1/+13.2`、峰值余量恒 **10.0%**、表宽 680、表列 **116.1/281.9/281.9**、零横向溢出、探针 **0/0/0** ⇒ 同装配成立、**当轮无缺陷**；表内 `/`×1、缺数 `—` 全为占位（合规）。本席实测：本格 h1 是「近 **30** 天总览」（唯一一格窗口换挡看得见） | 不改（除同族公共层的表箱居中）；归 **#401** |
| **4 · 看连续记录天数**（今窗） | 与格 1／2／5 并看：四格是不是同一页 | **（换版前读数）重复产物（本席实测定性）**：本格与格 5 **逐字节相同**（sha `37D143E2B326`，`summary.json` 两行 `src` 都指向 `今日总览_20260915_124010.html`）；与格 1／2 **只差页内时间戳**（长度都是 75,091）。⇒ **1／2／4／5 四格是同一页**，且屏上没有任何字给出「连续记录天数」「今日热量预算」这两个说法（h1 是「近 7 天总览」） | **要裁**（不是渲染缺陷，是**词面与页面的对应关系**）：四条词合成一条，还是每页补该词特有的那段（连击天数／预算口径）。归 **#375** 当场裁；裁「要改」再转页面票（落点 `packages/skill-calorie/src/render/homeDocs.ts`）。**——换版后已可分**：元素级最小差异 138 行、第一屏标题序列五档各异（见 §2） |
| **5 · 看今日热量预算**（今窗） | 同上：与格 4 同 sha、与格 1／2 只差时间戳 | **（换版前读数）**同格 4（四格同页） | 同格 4，归 **#375**。**——换版后已可分**：元素级最小差异 138 行、第一屏标题序列五档各异（见 §2） |
| **6 · 看今日饮食概览**（今窗） | 「趋势」卡的大号粗体数值是不是裸英文 `flat`；512 档明细表有没有逃出视口；KPI 卡档位在不在 | ① **裸英文上屏**：`<span class="ilife-block-kpi-card-value">flat</span>`（源 L1706，列约 1345）——「趋势」卡直接把机器话当大号粗体数值印出（#515 §一①）；② **512 档明细表逃出视口 40px**（表 `l=32 r=552 w=520` vs 视口 512；容器 `overflow-x:auto`，滚到右端能看全**不丢数**，代价是卡片右缘被推出视口）；③ **徽章 0/4、区块标题图标 0/4、缺值 `—` 0 处、空态件 0**（四槽零接入，机器话顶位）；④ 分隔符：节点级 **3 处**（含 R3 1 处）、行级 2 行（首行是 `<title>` 品牌名误报）；⑤ 数值列失衡（512 档 `239.98/238.02`） | **要改** → **#515**（跨域三页对齐主页族）。不要在 #515 之外另开票 |
| **7 · 看今日运动概览**（今窗） | 分隔符债最重的一格（口径注一行里 `；`×4／`＝`×3／括号 5 对）；KPI 卡档位在不在 | ① **分隔符债三页最重**：节点级 **8 处**、其中 R3 **4 处**——口径注 `；`×4 成四段并列、`＝`×3、括号 **5 对**同一行；「数据来源 · 运动记录 · … · 共 1 条」`·`×4 成四段并列；表注 `·`×3 等（#515 §二）；② **徽章 0/4、区块标题图标 0/4**；③ 正文一行 **475 字**（三页最长）；④ 行级首命中同为 `<title>` 误报 | **要改** → **#515** |
| **8 · 看今日体重概览**（7 天窗·7 条记录） | 口径行的 `｜`；1000 档 KPI 卡列数；页头有没有域标签 | ① **口径行 `｜` 硬串并列**：`📊 数据来源：体重记录 ｜ 窗口 … ｜ 共 7 条`（`｜`×2 三段并列；#515 记 L1755、当刻产物已位移到 L1756）→ **独立票 #514**；② **1000 档 6 张 KPI 卡挤一行**（每卡 150px，是页族里**唯一**的 6 列排法；饮食／运动／主页同档都是 4 列）；③ **页头 eyebrow 空**（饮食 `卡路里 · 饮食`／运动 `运动 · 汇总`／主页 `卡路里`，唯本页没写域标签）；④ `wui-chip` 自造壳与 `ilife-status-badge` **两套类名并存**、6 张卡里 **3 张无档**；⑤ 区块标题图标／缺值／空态零接入 | **要改**：① → **#514**；②③④⑤ → **#515**（本页是三页里**最接近达标**的一页：唯一有徽章、折线刻度正常 tick=3、债最轻） |
| **9 · 看今日目标进度**（今窗） | 整页重做 ＋ 返修后的一格：四卡档位、目标虚线的量程判定、页签标题不带 `·` | 按 `.scratch/t467-fix/`（#467 返修）读数：探针 **0/0/0 exit 0**、KPI 徽章 **4/4**、`ilife-charts-markline` 0 且图下是**读数式**、反例正相（目标改 1000 落在量程内 = 1 条、y1=71.6 落在绘图区内）、卡四带窗口、达标表前有窗口转场句、补了「完成度与缺口只算热量」块、页签标题改括号承载。**已知残留**：512 档 **1 处 19px 横向溢出**，根因是**公共层在途的表注** `display:block;width:100%`（摘掉那行即 0，已归因、未碰别家文件） | **残留要改**，落点在**公共层表注**（在途）。其余按 #467 已修。**别把 #541 当这条的根因**：#541 是「≤640 档 caption 被压成一字一行」（竖排），症状不同。本格右下角「定版后复看」见 §3-③ |

## 2 跨格共性（3 条，逐格之外要一起看）

1. **四条词出一页（墙上一并看才发现的那一类）** —— **【已修】**（主页族 5 份产物换 B 方案新版后重出）。**原症状**：格 **1／2／4／5** 的 sha 各不同，但**屏幕内容完全一致**（都是「近 7 天总览」、同一窗口、同一组数字），HTML 唯一差异是页内「复制日志」载荷里的一处**生成时间戳**（不上屏）。**修后依据（定版后读数）**：5 份产物**元素级最小差异 138 行**（不再是「只差时间戳」）；**四档零横向溢出**；探针 **0/0/0**；**第一屏标题序列五档各异**——今日／本周／本月／连续记录天数／今日热量预算各成一页，原记的「四条词的说法在页面上没有任何对应」不再成立。⇒ 仍留用的两点教训：① 用 sha 判「有没有重复产物」在本族**失效**（时间戳把 sha 打散），判之前要先剥时间戳；② 格 **3**（看本月主页）本来就是另一页（「近 30 天总览」）。**本条以此为准**：§1 逐格表里格 1／2／4／5 行那几句换版前的「同一页／逐字节相同」读数，按「其余条目保持原样」留档，不再代表当刻产物（原记「归 **#375** 当场裁」，本条已修）。
2. **墙的格高与产物实高有落差（不是缺陷，是墙的固有口径）**：格高一致 820（桌面墙 860），而本批产物的内页实高 **1,292–2,154px**（390 档读数见 §3-②）。⇒ **格内只见首屏**，下半页要点格标题开整页看。这是做法 §6.3-1「不塞缩略图」的代价，别当成产物被截断。
3. **跨域三页与主页族共用同一批公共层缺陷**：表箱居中（格 1 的 P0）、轴标签字号断点跳变（**#512**）、徽章／段标题槽位（**#513**）、移动端底座（**#507**）、分隔符立规（**#508**）——这些**不是三页自己的事**，别在 #515 里顺手改公共层。

## 3 自检退出码（三样里的第三样）＋ 本席实测读数

### ① 生成器自检 · 正反两面都走过（两行读数就是回执里的那两行）

| 面 | 命令 | 读数 |
|---|---|---|
| **正例** | `node gen-wall.mjs . 手机墙-390.html 390 820`（在产物目录内跑） | `9 格；链接 30 条（手机墙-390.html 19 ＋ 总索引.html 11）；缺失 0 -> 可发` → **exit 0** |
| 正例 （桌面墙） | `node gen-wall.mjs . 桌面墙-1280.html 1280 860` | `9 格；链接 30 条（桌面墙-1280.html 19 ＋ 总索引.html 11）；缺失 0 -> 可发` → **exit 0** |
| **反例** | 把清单第 1 行 `file` 改成 `看今日主页-反例-盘上没有.html` 再跑同一条命令 | `8 格；链接 30 条；缺失 2 -> 不可发` ＋ 点名两行：`清单点名却没有文件：1 看今日主页 -> 看今日主页-反例-盘上没有.html`、`页上引用却落不到：看今日主页-反例-盘上没有.html` → **exit 1 且点名那一份** |
| **改回** | 清单还原后再跑 | 回到 `9 格；链接 30 条；缺失 0 -> 可发` → **exit 0**（已确认第 1 行 `file` 回到 `看今日主页.html`） |

> 反例一次命中 `dropped`（清单点名却没有文件）与 `dead`（页上引用却落不到）**两条**——这正是仓规 §6.2／§7 点名的那处**假绿灯**（只查页上引用、缺件被静默剔掉，自检照样报「缺失 0」）。本件两条一起判。

### ② 双击能不能打开（无头 Chrome 真开页实测，`.scratch/墙-量.mjs`）

| 墙 | 格数 | 格标题链接 | iframe 渲染高度 > 0 | 内页真长出文档 | 空白格 |
|---|---|---|---|---|---|
| `手机墙-390.html` | 9 | 9 | **9/9**（每格 390×820px，视口 390×820） | **9/9** | **0** |
| `桌面墙-1280.html` | 9 | 9 | **9/9**（每格渲染 600.32×403.34px，视口 1280×860，缩放 0.469） | **9/9** | **0** |

- 内页实高（390 档）：格 1／2／4／5 **1395px**、格 3 **2154px**、格 6 **1388px**、格 7 **1560px**、格 8 **1576px**、格 9 **1292px**；文档级横向溢出**九格全 0**。
- **读数校准（重要，别与 #515 打架）**：390 档文档级横向溢出 0 **不等于** #515 的「饮食页 512 档表逃出视口 40px」不存在——那一处是**表本体逃出表容器**，容器 `overflow-x:auto` 把它裁住，故文档级 `scrollWidth` 不动。要看那 40px 得量**表容器自己的 scrollWidth**（512 档），别用「文档级 0」把它判成误报。
- 未加 `loading="lazy"`（九格全渲染、无一格空白，与做法 §7 第一坑相符）；两份墙的 `iframe` 各 9 个。

### ③ 定版后必须复看的三条（本批没条件做）

1. **格 9 的两条 #467 P0 在当刻这一格上无从复看**：本席点检当刻副本 `看今日目标进度.html`（69,566 B）**没有 `<svg>`、没有 `ilife-charts-markline`**（「今日」窗只有一天记录，按 #467 的口径折线本就不出）；当刻档位实例 **5 处**（`ilife-block-kpi-card-badge` 3 ＋ `ilife-status-badge` 2），与 #467 报告那一批样张的「4/4」**不是同源**。要看「目标落在量程外不画虚线／图下改读数式」，得用 `.scratch/t467-fix/` 的 7d／30d 样张，或等定版重出带多天窗的产物。
2. **格 1／2／3 的上一轮读数（88/100 那套）基于 77,034 B 版**，当刻副本 77,081 B；`看本月主页` 缺数占位 43 → 51 处。定版后按当刻产物复跑 `.scratch/t401j/measure.mjs` 与 `.scratch/sep-audit/probe.mjs` 再判。
3. **四档实拍与空窗产物**本批有意不出（原因见 `总索引.html` 末尾那一节）；#515 验收命令第 ⑤ 条要用空窗产物跑，归它的认领者。

## 4 还没做的那一条（本件不代签）

- 仓规 §4 的「人用真实浏览器滚一遍两张墙、缺陷**逐格**记下来」**本批尚未做**——本件给的是**现成材料的逐格翻译 ＋ 本席的机器读数**（格数／链接／真渲染高度／横向溢出）。负责人滚完后，把与上表不符的行**就地改这一件**（哪一格、什么症状、要不要改），别另开一件。
- 墙是**必要条件不是充分条件**（仓规 §4 红线）：本件「缺失 0」只说明**册子里点名的 9 件都在盘上、页上引用都落得到**，不说明任何一页**对不对**。
