# #310 独立复核报告（t310r，复核 7742567）

- 复核席：#310 独立对抗式复核席（只复核这一张票）
- 被审对象：`7742567`（`fix(310): CI恒红授权三项`，已 push，`origin/master`）
- 开工时刻工作区：`D:\ilife`，分支 `master`
- 报告文件：`docs/skills/skill-calorie/t310r-复核报告.md`（本件，唯一可写）
- 大输出：`.scratch/t310r/`（`show-stat.txt`／`ci-yml-diff2.txt`／`build.log`／`listfiles-p.txt`／`t293.log` 等，不交付）

## 0 开工自检（先复算，不符合即只读）

- `git rev-parse HEAD` = `9bbd3a7ad937fcbd14d82dedfb1bf95cb005e0a9`
- `git rev-parse origin/master` = `7742567eb250b82e30a6073d2ed4a39b672da421`
- 结论：**HEAD != origin/master**。本地超前 1 件：`git diff --name-only origin/master..HEAD` = `docs/skills/skill-calorie/t167-重新设计-新底座.md`（93 行，文档，别席未推）。
- `git status --short`（开工时）：
  - `M docs/skills/skill-calorie/t170-scene09-reconcile.md`
  - `M docs/skills/skill-calorie/t170-身材照片-票单设计.html`
  - 结论：**树不干净**（2 件工作区浮动，均为文档，别席在飞）。
- 处置：**只读仓内文件，不碰他席浮动**；代码面 `package.json`／`plugin-bill-ilife`／两测试件在 `HEAD` 与 `7742567` 间零差异（`git diff --stat HEAD -- <5 码件>` 空），复跑读数可归因到 `7742567` 的代码面。`fetch` 后 `origin/master` 仍为 `7742567`。

## 1 改动面审计（只看 7742567，不采信自报）

- `git show --stat --oneline 7742567`（逐字存 `.scratch/t310r/show-stat.txt`）：
  - `docs/skills/skill-calorie/t310-修复-证据.md | 70 +++++`
  - `package.json | 2 +-`
  - `packages/base-render/test/output-save-html-237.test.mjs | 4 +-`
  - `packages/plugin-bill-ilife/src/index.ts | 10 ++--`
  - `packages/plugin-bill-ilife/tsconfig.json | 3 +`
  - `packages/skill-calorie/test/output-naming-87.test.mjs | 16 ++++-`
  - `6 files changed, 94 insertions(+), 11 deletions(-)`
- `git show --name-only --pretty=format: 7742567` = 上述 6 件，无第 7 件。
- `git diff '7742567^..7742567' -- .github/workflows/ci.yml > .scratch/t310r/ci-yml-diff2.txt`：`Length 0`。**ci.yml 未动属实**。
- 越界检查（冻结面／判官／#174／#241 链架构）：
  - 6 件之外零文件，故 `tooling/` 判官、`test/` 冻结表、`packages/base-render/src/`、`packages/skill-calorie/src/triggers/routing.ts`、`routes.generated.ts`、`src/cli/keys.ts`／`registry.ts` 均未动。
  - `t310-修复-证据.md` 为新增证据件（70 行），不管类。
  - 结论：**件数 6 属实，无越界**。

## 2 逐条复验三项修法（全部自己跑）

### 2.1 根 build 接 client 链（package.json）

- 现值 `package.json:11`：`"build": "tsc -b && node packages/skill-calorie/scripts/gen-cli.mjs --stamp && pnpm -r --if-present run build:client"`。`git show 7742567 -- package.json` 确认仅末尾追加 ` && pnpm -r --if-present run build:client`，前三段一字不动。
- 持锁复现（`--ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30`）：
  - `node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- pnpm build` → `LOCK-RELEASED exit=0`（runId `e704751b-1e2b-43df-84af-dca8877dd45d`），日志 `.scratch/t310r/build.log` 111 行，7 包 `build:client Done`。
- 产物真实性（删后重建等价，非残留）：
  - 重建前直读 7 件 `dist/client.js` 全 `LOADER-OK`（含 `window.__ModuleLoader__.load`）：
    - `bill-ilife 3528`／`calorie 13937`／`chef 3511`／`home-ilife 3541`／`memo-ilife 12883`／`schedule-ilife 3576`／`manager 10269`
  - 复现命令：`node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- node -e "...stat+includes('__ModuleLoader__')..."`
- 结论：**client 产物真实产出，非残留；根链追加成立**。

### 2.2 bill 拆雷（tsconfig + index）

- `packages/plugin-bill-ilife/tsconfig.json` 现值含 `"exclude": ["src/client.ts"]`。与 6 兄弟逐件比对同形：`plugin-calorie`／`plugin-chef`／`plugin-home-ilife`／`plugin-memo-ilife`／`plugin-schedule-ilife`／`plugin-manager` 均含同一 `exclude`（`manager` 无 `references` 但 `exclude` 同字）。
- `packages/plugin-bill-ilife/src/index.ts:42-45` 现值为 `#310 拆雷` 注释，无 `export type { HostCaller }`。`git show` 确认删的是 `export type { HostCaller } from './client.js';` 一行＋旧注释 6 行。
- 零消费方（独立检索）：
  - `grep HostCaller packages` 11 命中：6 件 `src/client.ts` 定义＋`requestReadViaHost` 自用，2 件 `src/index.ts` 注释（bill／chef），其余为注释。**零 `import ... HostCaller`**，删后运行时零影响成立。
- 不再覆写（硬证据）：
  - `npx tsc -b packages/plugin-bill-ilife --force` → `TSC-FORCE-EXIT=0`；前后 `dist/client.js` 同哈希 `69500cc607c3`、`size=3528`、`STILL-LOADER`。
  - `dist/index.js` 无 `from './client`（仅注释 31–34 行提及 client／HostCaller，无代码引用）。
  - `tsc -b --listFiles` 增量态输出 0 字节（up-to-date 无重建）；`tsc -p --listFiles` 198 行中 bill 侧无 `src/client.ts`（仅 `undici-types/client*.d.ts` 噪声）。
- 结论：**bill 拆雷完成，覆写已止**。

### 2.3 #87 ④b 探针（双分支都对）

- `git show` 确认增量：`import` 加 `unlinkSync` 1 行；CLI 段注释改写＋探针 4 行（`CaSePrObE.tmp` 建＋`existsSync` 小写＋`unlinkSync`）＋断言分支 5 行。种子 10 件 `.HTML`、`SEED` 内容、`readdirSync(html).length, 11`、`大写种子不得被覆盖` 均未动。
- 探针逻辑（`output-naming-87.test.mjs:193-208`）：
  - 大小写不敏感（Windows）：`CaSePrObE.tmp` 建后小写 `caseprobe.tmp` 可见 → `caseInsensitive=true` → 期望 `_2`（`wx` 建小写同名即 `EEXIST` 递补）。
  - 大小写敏感（Linux）：小写名是另一个目录项 → 一次成功 → 期望无后缀。
  - 单测段（上）仍锁跨平台一致的大小写不敏感计数口径；本段只锁真实落盘实效。两种严格断言，不放宽。
- 持锁复跑：`node --test packages/skill-calorie/test/output-naming-87.test.mjs` → `tests 14／pass 14／fail 0`（runId `19c1deb7-9178-4526-993f-195eaa5f7398`，`duration_ms 2292.09`）。本机 exercised 不敏感分支；敏感分支由正则双向互斥保证（两分支正则是互斥的 `_2` 有无，不存在同过）。
- 结论：**探针与分支正确，种子与计数断言不动属实**。

### 2.4 #237 ⑤ 种子（字节账与断言强度）

- `git show` 确认仅种子 1 行变＋注释 2 行加；断言 `longLen <= 180` 一字未动。
- 字节账（独立计算，非引用注释）：
  - `node -e "'长'.repeat(20)+'a'.repeat(280)"` → `codepoints=300`，`utf8bytes=340`；截前 180 码点 → `trunc-codepoints=180 trunc-bytes=220`（= `20*3+160`）；`with-stamp-ext=241`（= `220+1+15+5`，stem＋`_`＋15 位戳＋`.html`）。
  - `241 <= 255`（Linux 单段上限），可落；仍 300→180（20 中文＋160 ASCII），多字节与边界共存保留。
- 持锁复跑：`node --test packages/base-render/test/output-save-html-237.test.mjs` → `tests 17／pass 17／fail 0`（runId `abddf297-1a01-45be-acc1-6f724796fb6f`）。
- 结论：**种子 241 字节属实，断言强度未动属实**。

## 3 复跑（持锁，direct node；CI 真实命令口径）

锁口径统一：`node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- <cmd>`。CI 真实链（`.github/workflows/ci.yml:28-73`）为 `install --frozen-lockfile → build → gen:check／doctor／test／boundaries／snapshot:html:check／gate:selftest:html／changeset:status／help:examples:check`（各带 `if: always()`）；本席按“门禁相关＋本票三项”持锁重跑，不重跑全 CI 矩阵（见未做项）。

| 门 | 复现命令（锁前缀省略） | 读数 |
|---|---|---|
| build | `pnpm build` | `exit=0`，runId `e704751b`，7 包 client Done |
| client 门 | `node --test test/client-bundle-48.test.mjs` | `tests 21／pass 21／fail 0`，runId `6e55d601` |
| 87 | `node --test packages/skill-calorie/test/output-naming-87.test.mjs` | `tests 14／pass 14／fail 0`，runId `19c1deb7` |
| 237 | `node --test packages/base-render/test/output-save-html-237.test.mjs` | `tests 17／pass 17／fail 0`，runId `abddf297` |
| routing | `node --test test/calorie-routing-81.test.mjs` | `tests 8／pass 8／fail 0`，runId `80c4bb16` |
| gen-check | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `PASS：键 101（写 35＋读 66）`，runId `04da126c` |
| 全量 | `node --test "test/*.test.mjs" "packages/base-render/test/*.test.mjs" ...（15 段，与 package.json test 同 glob）` | `tests 1492／suites 160／pass 1492／fail 0／cancelled 0／skipped 0`，`duration_ms 44541.0806`，runId `39660923-d784-4454-8897-b167d872c12a exit=0` |
| t293 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`（持锁）＋ `--json`（持锁）＋直落文件（取行） | `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`，runId `ff341b55`／`d16ac6db`，`exit=0`；`UNACCOUNTED=0 处`（`.scratch/t310r/t293.log:22-23,46,93`），`T293-DIRECT-EXIT=0` |

## 4 范围外四类抽查（只验证存在性，不修；若变绿如实写）

前提：四类文件均不在 6 件内（§1），故“未随本次改动变绿”先天成立；以下每类一条复现命令或读数，确认“CI 仍红确有其事、且确在授权外”。

- #91②（ambient `SKILLS_DB_PATH` 依赖）：
  - 复现：`$env:SKILLS_DB_PATH` 本机 = `D:\2Study\StudyNotes\.db`（有值故本地绿）；剥除后跑 `node --test packages/skill-calorie/test/help-center-91.test.mjs` 即见 `Error: [skill-calorie] SKILLS_DB_PATH 未设置：拒绝隐式落盘。生产请设置 SKILLS_DB_PATH；测试请传显式 tmp 路径。`（已复现，变量已恢复）。
  - 结论：CI 无该变量即红确有其事；修法须动第 3 个测试文件，超授权三项，未碰 untouched 属实；**未变绿**。
- #204⑤（仅 22.13 红）：
  - 复现：`node --version` 本机 = `v24.19.0`；`node --test packages/skill-schedule/test/help-delivery-204.test.mjs` → `tests 8／pass 8／fail 0`，`EXIT204=0`。即 24.x 本地绿，与自报“仅 22.13.0 三作业红、24.x 全绿”一致。
  - 结论：版本相关抖动，非本票两条；文件 untouched；**未变绿（22.13 侧未在本机复现，但授权外成立）**。
- Windows CRLF 系（含 #295 CI 门、T18b、HELP 资产 `--check`）：
  - 复现：`git config --get core.autocrlf` = `false`；`git ls-files --eol packages/skill-calorie/test/help-center-91.test.mjs` = `i/lf w/lf`。本机 LF 故绿；Windows 按 `autocrlf=true` 检出即 CRLF，逐字节／行尾锚断言红，与 client 无关。修法在检出配置或归一化，超三项。
  - 结论：CI 仍红确有其事（机理成立）；**未变绿**。
- macOS `/private` 软链（#83⑥、#133⑭）：
  - 复现：`node -e "console.log(require('os').tmpdir())"` 本机 = `C:\Users\辰辰洋洋\AppData\Local\Temp`（Windows 无此软链故绿）；`help-paths-133.test.mjs:76-86` 用 `dirname(out) == join(resolve(root), relDb, ...)` 字符串比对，macOS `TMPDIR=/var/...` 真实路径为 `/private/var/...` 即分歧。修法在 `realpath` 归一，超三项。
  - 结论：CI 仍红确有其事（机理成立）；**未变绿**。
- 总结论：四类**无一随本次改动变绿**，如实。

## 5 能否关判定与去处建议

- 三项授权修法：①根链 ②bill 拆雷 ③两测试按平台做对，**已完成且复验全绿**（§2–§3）。
- 无越界：6 件＋ci.yml 零字节＋冻结／判官／链架构未动（§1）。
- 范围外四类：已有明确去处（各有修法方向与归属：#91 显式传路径的第 3 测试文件；#204 版本抖动跟随上游；CRLF 检出配置／归一化；macOS `realpath` 归一），**已有明确去处**。
- 去处建议：**转交新票（OR 留本票二选一中选前者），理由**：四类修法各超“授权三项”，留本票会使本票从“三项修法”膨胀为“七面收口”，违反最小改动与冻结面纪律；且 CI 矩阵中 22.13／macOS／Windows 各有独立 Owner（版本／平台），分票可并行、不阻塞本票关闭。本票满足关闭条件。
- 判定：**可关（PASS）**。

## 6 打分（100 制；PASS 要求 S1=0 且 S2=0）

- S1（致命：修法未完成／越界／产物假绿／计数造假；每条 20 分）：
  - S1-1 根链假绿？否（7 件 LOADER＋重建＋21/21）。0。
  - S1-2 bill 越界或仍覆写？否（2 件最小＋哈希不变）。0。
  - S1-3 87／237 断言放宽？否（diff 逐行对过，计数与 `<=180` 未动）。0。
  - **S1 合计 0**。
- S2（重大：证据不可复现／分支判错／字节账假／全量数假；每条 10 分）：
  - S2-1 四门计数不可复现？否（21／14／17／8 全持锁复现）。0。
  - S2-2 87 分支判错（敏感／不敏感反了）？否（`wx+EEXIST` 机理对）。0。
  - S2-3 237 字节账假（非 241 或超 255）？否（独立算出 241）。0。
  - S2-4 全量／t293 假绿？否（1492／5/5＋UNACCOUNTED=0 三读一致）。0。
  - **S2 合计 0**。
- S3（轻微：记账／文字／未做项；每条 2 分）：
  - S3-1 仓外零 dist 全量自证未独立重做（仅做 `tsc --force`＋`pnpm build` 重建等价，未做仓外 `robocopy` 洁净检出）。2 分。
  - 其余（开工 HEAD 超前＋树脏）属他席浮动、已只读规避，不记分；四类抽查中 CRLF／macOS 为机理＋读数（非三端实跑），已在 §4 声明，不另扣。
  - **S3 合计 2**。
- **总分 98／100，S1=0 且 S2=0 → PASS**。

复现命令索引（S 清单每条对应）：

- `git show --stat --oneline 7742567`
- `git diff '7742567^..7742567' -- .github/workflows/ci.yml`（空）
- `node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- pnpm build`
- `node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- node --test test/client-bundle-48.test.mjs`
- `node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- node --test packages/skill-calorie/test/output-naming-87.test.mjs`
- `node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- node --test packages/base-render/test/output-save-html-237.test.mjs`
- `node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- node --test test/calorie-routing-81.test.mjs`
- `node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- node packages/skill-calorie/scripts/gen-cli.mjs --check`
- 全量 15 段 glob（见 §3 表格）
- `node tooling/run-locked.mjs --ticket 310r --lock-dir .scratch/locks-land --stale-minutes 30 -- node docs/skills/skill-calorie/t293-验收-命令自治.mjs`

## 7 未做项

- 未做仓外洁净检出全量自证（`D:\_t310r\ilife` 级别 `robocopy`＋`install --frozen-lockfile`＋三项复绿）：因工作区他席浮动＋时间，仅做仓内 `tsc --force`（bill 哈希不变）＋`pnpm build`（7 件 Done）等价；判为 S3-1，不影响 PASS。
- 未跑 CI 全矩阵（6 作业：22.13／24.x × ubuntu／macos／windows）与 `doctor`／`boundaries`／`snapshot:html:check`／`gate:selftest:html`／`changeset:status`／`help:examples:check` 全门：只跑与本票三项直接相关的门；四类范围外用存在性读数代替。
- 未碰 issue 票面、未 `--amend/--force/rebase`、未写 100%、未 close。
- 未动他席浮动 2 件与超前 1 件（只读）。

## 8 交付

- 跟踪文件：本件（`docs/skills/skill-calorie/t310r-复核报告.md`）。
- 过程文件（不交付）：`.scratch/t310r/`。
