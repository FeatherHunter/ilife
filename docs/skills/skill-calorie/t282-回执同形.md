# t282 · 身材照片写后回执同形证据（完整文档＋内嵌照片＋复制区）

## 一、目标与装配链

7 条写入类场景（记身材照×3：单张／含备注／批量；删身材照×1；改·加·删照片标签×3）
的回执页＝完整文档＋内嵌照片（快照）＋复制区。内容照老实物
`body_photo_receipt.html`（12500 字节／242 行），长相走新仓共用件
`src/shared/docPage.ts`（`assembleDocPage`）＋`src/shared/copyArea.ts`
（`copyArea`／`copyLog`）＋`src/shared/receiptParts.ts`
（`statusCard`／`reconcileDisclosure`）。

回执装配链（只读侦察确定）：`src/photo/store.ts`（`writePhotoAdd`）／
`src/photo/manage.ts`（`writePhotoRemove`／`writePhotoTag`）先调取数层
`src/photo/photos.ts` 会改数据库，再调 `src/photo/photo.ts` 三回执组装
（`buildAddReceipt`／`buildDeleteReceipt`／`buildTagReceipt`），经
`src/render/receipt.ts`（`withM5`）补自证字段，经 `src/cli/write.ts`
（`dispatchWrite` 注入 `affectedRows`＋选整页）到 `src/output.ts`
（`deliverHtml` 唯一落盘点）。

文件级可写集合（实际只写这些）：新 `packages/skill-calorie/src/photo/receipt.ts`
（回执页渲染件）；改 `packages/skill-calorie/src/photo/store.ts`、
`packages/skill-calorie/src/photo/manage.ts`；新
`packages/skill-calorie/test/photo-receipt-docs-282.test.mjs`；本证据件；
草稿 `.scratch/t282/`。基线脚本声明可写但**未改**（原因见 §八）。
`src/render/html.ts`（645 行，已超线）只读未加行；
`src/photo/commands.ts`／`routes.ts` 命令声明未增删；
`src/triggers/routing.ts`、棘轮文件、共享生成物未碰。

## 二、老→新移植清单比对（t400 §二序 1）

| 老实物 | 本票落点 | 口径 |
|---|---|---|
| 徽章配色 `:155-156`（存入绿／变更橙／删除红） | `receipt.ts:opBadgeOf`（`#34c759`／`#ff9500`／`#ff3b30`） | op 三态随 `receipt.op` 切 |
| sub 徽章 `:158-159`（新增／修改／删除） | `badgeBlock`（动词＋已存入／已更新／已删除） | 同上 |
| id 卡与标题 `:161-165`（✓／✎／✕＋存入成功／修改成功／删除成功） | `badgeBlock`＋`idCardTable` | 同上 |
| scene 换标签词 `:215-219` | `tagLabelsOf`（改前改后／加前加后／删除前删除后） | `html.ts:148` 硬编码改前改后在此终结 |
| 无变化降级 `:227` | `tagDiffTable`（`未产生实际变化`＋标题改 `标签(未变化)`） | `receipt.noChange` 驱动 |
| 空标签灰底 `:195` | `tagText`（空数组即印 `无`） | 可见文本 `无`，复制数据保留原始空数组 |
| 照片明细 `:186-209`（缩略＋标签＋编号日期＋备注＋状态） | `buildPhotoAddDoc`／`buildPhotoRemoveDoc`（`figure`＋`data:image/`＋明细表） | 快照经 `photoThumb.ts` 内嵌，缺失明示原因 |
| 复制区 `:239-240` | `receiptCopyArea`（`copyArea({data, log})`） | 复制数据 ▾ 三选一＋复制日志 |

## 三、新保留清单比对（t400 §三，命中 7 项）

整页装配（`assembleDocPage` 唯一模板）／三格式恒开（数据位恒出纯文本／JSON／CSV
菜单）／复制区分族（数据区＋日志区，空态不出按钮）／体积常量沿用（见 §七）／
缺失不牵连（`embedPhotos` 逐张独立成败）／间隔行（`distanceBlock`）／
删前先取快照再删（`manage.ts` 先 `embedPhoto` 再 `deletePhoto`，t400 §三序 7）。

## 四、裁定验证（t400 §四）

裁定 1：`calorie.photo.tag` 按 op=set/add/remove 各跑一遍→徽章读数
变更回执＋标题修改成功＋图标 ✎ 三组一致，对照行读数分别为改前改后／加前加后／
删除前删除后；无变化用例（重复加同一标签）读数为 `未产生实际变化`＋
`标签(未变化)`；空标签合成用例读数为 `无`。测试 10/10（§八）。

裁定 5：7 条逐条产物首行读数等于 `<!doctype html>`，且每页同时含 `复制数据` 与
`复制日志`（三件位置：状态卡→徽章→id 卡→明细／对照→间隔→对账→复制区，照 §六 C
骨架）。失败分支未改：成功路径抛错（缺参／坏参／缺失阻断）仍走
`delivery.ts:failWithReceipt` 失败回执整页（操作名＋原因＋数据快照＋建议＋
复制修正 prompt），本次只对齐形状（票面遗留出口，缺字段另开票）。

## 五、唤醒词形态三行（t400 §六-补，存／移除／设置）

存身材照（回执：徽章→id 卡→明细→复制区，`create` 即存入）／移除身材照
（回执：删除态徽章→删前快照→复制区，硬删除不可恢复明示）／设置照片标签
（回执：徽章→id 卡→标签对照→复制区，对照词随 scene 换，无变化降级）。
复制区位置均为页尾（老 `:239-240`；新随整页化接入）。

## 六、体积节（t341 沿用，未改值）

`PHOTO_LIST_PAGE_MAX_BYTES = 1048576` 定义地 `src/photo/galleryDoc.ts:25`
未动；本件不另定值；t341 同步清单四处（常量定义＋注释／t341 文档／各页面文档／
形状测试）均未触发改值分支。10 用例实测单页体积全 ≤ 上限（种子为 1x1 小图）。

## 七、超线报告

`src/render/html.ts` 645 行：已超线，需要根据规则进行重构。超因：T8／T9／T10＋
体重／身体／计划等全域模板同处一件；本次先不拆：本票回执装配走能力目录新件
`src/photo/receipt.ts`（225 行），未给它加行（拆法见 t341 超线报告，按域拆分待
后续票确定）。本票三件行数：`receipt.ts` 225、`store.ts` 46、`manage.ts` 66，
新测试 222，均在 350 告警线内。

## 八、机器证据

隔离：`SKILLS_DB_PATH`／`CALORIE_PHOTOS_DIR` 指向 `$env:TEMP\t282\` 下新鲜目录，
真库 `D:\2Study\StudyNotes\.db` 只读零写入（跑写命令前已 `echo` 确认）。

- `GATE-RUN runId=264df2d2-48fa-4776-aa11-5d9250e7a6ff cmd=pnpm build` exit=2：
  他票在途 4 错（`weight/receipt.ts`、`weight/volatilityDoc.ts` 未入库新件、
  `workout/precheckPrompt.ts` 未入库新件），本票三件零报错且 `dist` 已新鲜落盘
 （`tsc -b` 缺省仍落盘，`dist/photo/receipt.js` 18:34:26）。
- `GATE-RUN runId=19c3b1dd-ba55-427a-829b-8c3833883d75
  cmd=node --test packages/skill-calorie/test/photo-receipt-docs-282.test.mjs`
  exit=0：10/10（含 7 场景逐条＋无变化降级＋空标签＋变异）。
- `GATE-RUN runId=c0d2a0df-5bac-4c37-9ae8-c569c3b0f1e0 cmd=pnpm gen:check`
  exit=1：GEN-STALE，系并行票 18:38 在 `src/photo/commands.ts` 加
  `calorie.view.photo-picker` 两行（§九），本票未动命令声明。
- `GATE-RUN runId=920e2fe4-4576-47b8-a321-34d3b86095de
  cmd=node packages/skill-calorie/scripts/gen-photo-baseline.mjs --check`
  exit=1：6 页漂移，其中 3 页系本票整页化（add→`04d0fd45…`、remove→`3e0dd5a3…`、
  tag→`a590922e…`，完整值见脚本输出），另 3 页（list／gif-planner／
  photo-log-wizard）系他票共享脏件（`shared/docPage.ts`、`shared/copyArea.ts`、
  `render/html.ts` 在途改动；读侧路径文件本票零触碰）。基线脚本本次**未回写**
  （当前读数混入他票共享脏内容，回写即把瞬时值固化，待树绿重落，见 §九）。
- `GATE-RUN runId=7ffd33da-34db-4214-92f4-cbf5bd4dac04
  cmd=node --test packages/skill-calorie/test/cmd-write-40-persist.test.mjs`
  exit=0：20/20，会改数据库的命令落库口径无冲突。

变异自证（测试内 `变异自证` 用例，任取记身材照单张文档头断言）：
改坏必红 `assert.throws(/文档头缺失/)` 通过（去 `<!doctype html>` 即红）；
改回必绿 `assertDocTrio(good)` 通过（原文回放即绿）。

## 九、未做项与下一手

1. 基线重落待树绿：树绿后跑 `--write` 重落本票 3 页（以当时脚本输出完整值为准），
   另 3 页（list／两规划器页）归其脏件主人认领；随后 `--check` 连跑确认
   `BASELINE-OK 10/10`（本票单轮读数见 §八，未做双轮稳定复核）。
2. `pnpm build` 与 `pnpm gen:check` 待并行票（#283 的 `photo-picker` 命令、
   共享件改动方）完工重建后复绿；复绿后重跑本票 10 用例确认。
3. 审查席重点看：`receipt.ts:opBadgeOf` 三色与老实物 `:155-156` 是否同值；
   `manage.ts` 删前内嵌与硬删除顺序是否不可调换；`store.ts`／`manage.ts`
   自算 `affectedRows` 与 `dispatchWrite` 复算是否同值（同源 `totalChanges` 差量，
   无他写即一致）；`tagText` 的 `无` 是否只出现在可见文本（复制数据仍为空数组）。
4. 错误回执页缺字段当场开票（票面遗留出口）；`html.ts` 照片段若后续仍超告警线，
   按既定拆法（按域拆 `photoHtml.ts` 等）另票处理。
