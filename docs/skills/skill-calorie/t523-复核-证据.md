# #523 独立复核（第四轮 R4）· 复核席证据件

> 复核对象：`fbba22e`（R4 返修三处）／`bdbac87`／`a41810a`（证据件两次登记）。
> 复核席**不改**实施席的代码；本件只记我自己跑的读数。全部命令经
> `node tooling/run-locked.mjs --ticket 523 -- <命令>`；草稿与探针落 `.scratch/t523-review/`。

## 〇、判定

**PASS**（无 S1／无 S2；三条 S3）。五维：契约一致 **27**／证据真实可复现 **22**／新旧对照 **18**／工程红线 **14**／文档同步 **9** ⇒ **均分 90**。

- 三笔提交逐笔在链上：`git merge-base --is-ancestor fbba22e|bdbac87|a41810a HEAD` ⇒ **exit 0 / 0 / 0**。
- 票面七道机器门**每一条都用我自己的 runId 复现**（下面 §二逐条给读数）。
- **R3 那条「页内导航触摸 −21」作废成立**：我自己写的 CDP 量尺（不复用实施席的探针）在 39 页 × 2 档共 78 格里，`.ilife-block-toc a` **没有一格低于 44px**（最小 46px，本票 17 页逐枚 58px／`min-height:44px`）；且那条 44px 规则的首现提交 `a17dbea`(#543) **是 R3 被评审那次提交 `18a3f75` 的祖先**（exit 0）⇒ 视觉席 R3 记的「32～36px」在被评产物上本来就不成立，不是「先修后撤」。

## 一、三笔提交与写集

| 提交 | 内容 | 在链上 |
|---|---|---|
| `fbba22e` 修(523)：R4 返修三处 | `src/exercise/records.ts`／`src/exercise/sportUi.ts`／`test/exercise-text-shape-523.test.mjs`／`test/exercise-records-fusion-451.test.mjs`／`docs/skills/skill-calorie/t523-证据.md`／`docs/skills/skill-calorie/t523-gate-runs.log`（6 件） | exit 0 |
| `bdbac87` 文档(523)：证据件补两行 | 只含 `t523-证据.md` | exit 0 |
| `a41810a` 文档(523)：登记第二次提交 | 只含 `t523-证据.md` | exit 0 |

- **写集落在票面允许面内**：生产代码三件里的两件 ＋ 本票测试件 ＋ 票面裁定 ① 允许最小改动的本场景测试件 ＋ 交付物证据件／对账源。`src/render/sportDocs.ts` 本轮确实一字未动（`git show --stat` 无此件）。
- **别席的件一行没进**：`git show --stat` 的 6 件与 §十二·一 写集声明逐条一致；`packages/base-render/**`／`src/shared/**`／`sportPortDocs.ts`／`receipt.ts` 均无本票改动。
- **源码与 `a41810a` 逐字节同**：`git diff --stat a41810a HEAD -- <本票写集>` 只剩别席新件 `test/analysis-deficit-eta-466.test.mjs`，本票源件无差异 ⇒ **现在重出即等价于 `a41810a` 重出**。
- 本票 6 条路径 `git status --porcelain` **为空**（无未提交改动）。

## 二、机器证据（我自己跑的 GATE-RUN runId ＋ RESULT 摘要）

| # | 命令（经加锁包装器） | runId | exit | 我看到的摘要行 |
|---|---|---|---|---|
| 1 | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | `00157c98-5348-4700-bd04-d60d9a810660` | **0** | 编译通过（无输出） |
| 2 | `node .scratch/t523/run17.mjs` | `32c5f972-6e6f-4041-ba16-a87c0a8fe200` | **0** | `乙：39 条 OK=39 BLOCKED=0 字节域=[74251,121941]`；`out 落盘件数=39`；`MINE17 n=17 exit0=17 complete=17 BAD=0` |
| 3 | 同 2（第二次，判重出确定性） | `d15ee42c-2241-41f7-a429-161861474655` | **0** | 同 2 |
| 4 | `node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t523/out --json …` | `b7e8f449-1f35-46bf-91a1-4c0cd768925f` | **0** | `OVERFLOW-ZERO pages=39 cells=117 failed=0 scopeOutFailed=0` |
| 5 | `node .scratch/t523/scan17.mjs .scratch/t523/out .scratch/t523-review` | `020b3c84-adf3-48ad-bb96-4d0dd54ea4c0` | **0** | `SEP 节点级命中 0 的页=17/17 行级 0 的页=17/17 exit0 的页=17/17`；`文案 机器词页=0 脏数页=0` |
| 6 | `node --test`（451＋452＋267＋523 四件） | `cfaba978-5000-40fe-ab94-0b594b41e1b4` | **0** | `ℹ tests 35 / ℹ pass 35 / ℹ fail 0`；`T267-RESULT: 8/8 通过` |
| 7 | `node docs/skills/skill-calorie/t523-截断探针.mjs --dir .scratch/t523/out --widths 390,768,1440 --json …` | `5905457a-d77b-4a70-8577-c79f1aaa0696` | **0** | `TRUNC-SCAN pages=39 cells=117 failed=0 scopeOutFailed=8 RESULT: 109/109（本票 17 页）` |
| 8 | 台账门 `check-warning-line.mjs` | `d9db3b13-6b6a-4085-b912-6ff16cb13c49` | **1** | `RESULT: 90/91`；**唯一红＝`src/render/trendPredictDocs.ts 台账=852 实况=897`（#518 在飞的件，范围外）**；`src/exercise/records.ts` 台账 365 ＝ 实况，本票那一格绿 |

**第 5 条的逐页读数**（落 `.scratch/t523-review/read17.json`，17 页 `node=0 R1=0 R2=0 R3=0 line=0 exit=0`）：节点级／行级／退出码逐页归零属实；`机器词页=0 脏数页=0`。每页唯一那条「机器词」命中是复制菜单的 `JSON`／`CSV` 档位名（#508 豁免，票面口径内）。

**17 页完整性自查**：产物目录 39 件 `<!doctype html>` 起、`</html>` 收，**BAD=0**。

### 二之一、`.scratch/t523/out` 是否被别席（#518）写过同名件 —— **结论：本票 17 页没有被污染**

编排者点名要核这条。做法与读数：

1. 重出前后各取一次全目录 SHA256（`out-hashes-before/after.txt`）：**39 件里 25 件字节变了**。
2. 为定位这 25 件到底是什么在变，我另存了重出后的产物（`out-runA/`）再跑一遍 `run17.mjs`（runId #3），逐件比对 ⇒ **同样是这 25 件**。
3. 自写 `.scratch/t523-review/ts-diff.mjs`（把 `YYYY-MM-DD HH:MM:SS` 形态串在两侧掏空后逐字节比，再单独比可见文本）：
   `SUMMARY same=14 tsOnly=18 realDiff=7 visibleTextDiffPages=7`。
   - 变的 25 件＝`01–13`（#524 回执族）＋`16–24、28–30`（本票汇总／对照目标族的**含复制日志**页）；
   - 差异逐字是 `复制日志` 按钮 `data-t` 载荷里那枚**生成时刻**（如 `2026-09-16 05:12:22 · 版本 0.1.0`）——`#523` 这 17 页里它**不在可见文本面**；
   - 7 件「realDiff」的成因是我那条正则只吃 `YYYY-MM-DD HH:MM:SS`、#524 的页另外把 `HH:MM:SS` 单独上屏（`.scratch/t523-review/vdiff.mjs` 实测第 01 页仅第 11／49／78 行是时间戳，无第三条差异）⇒ 仍是同一枚时间戳。
4. **本票 17 页的可见文本两次重出逐字节相同**：`14／15／25／26／27` 连文件字节都相同（这 5 页产物里没有那枚时间戳），`16–24／28–30` 仅时间戳位不同；全 17 页 `body` 可见文本 `same=true`。

⇒ **没有找到 #518（或任何别席）写进 `out/` 的同名件痕迹**；25 件字节变动全部由「生成时刻」这一枚自走变量解释。核对一律以**我这次按 `a41810a` 等价源码重出的产物**为准（§二 表 #2／#3 两次重出的读数用的是同一份盘上产物）。

> **口径提醒（S3）**：票面验收写「字节与盘上一致」。按最严读法（可逐字节复现），本票 17 页里 **12 页**不满足——因为复制载荷里带生成时刻；但该差异**不在可见文本／不在判据面**，且来自公共层 `renderCopyBlock` 的既有行为，非本票引入。请编排者裁：是认「盘上件完整且与本次重出一致」，还是把复制载荷的时间戳排除在「字节一致」之外。

## 三、自设试法（三式，全部我自己写、自己跑）

### 试法① 独立复算「页内导航触摸目标」（R3 最大单项 −21）

**自建量尺 `.scratch/t523-review/measure-rev.mjs`**（不复用实施席 `t523-截断探针.mjs`，自写 CDP 连接与探针表达式），runId `fab23af4-76a6-44b1-932b-bd1c19485726`，exit 0：

```
390档 16-看本周运动.html   toc=6 h=[58..58] mh=44px d=flex pad=8px/8px  不足44的数量=0 触摸面<44的a/button=0 (总 11) 溢出=0
1440档 16-看本周运动.html  toc=6 h=[58..58] mh=44px d=flex pad=16px/16px 不足44的数量=0 触摸面<44的a/button=0 (总 11) 溢出=0
MEASURE-REV cells=78 toc锚点不足44的格数=0 全格最小高度=46px 触摸面<44 的 a/button 合计=0
```

- 78 格＝39 页 × 390／1440 两档；**本票 17 页逐枚 `h=58px / min-height:44px / display:flex`**，#525 族 46px，**无一格 <44px**；顺带把整页 `a`／`button` 也量了，**小于 44px 的合计 0**。
- 两档 `padding-inline` 正好是本次改的两个值：**1440 档 16px、390 档 8px** ⇒ 试法①同时复核了 R4 第 ③ 处返修（14→16／6→8）。
- 历史面（判定「作废」是否属事后追认）：`git log -S "min-height:44px" -- src/exercise/sportUi.ts` ⇒ 首现 `a17dbea`(#543)；`git merge-base --is-ancestor a17dbea 18a3f75` ⇒ **exit 0**，而 `18a3f75` 正是 R3 被评产物对应的提交。
- **独立结论：R3 的「页内导航 6 标签 32～36px 未达 44px」（六页合计约 −21）在产物上不成立，「作废」成立**；且成立的理由不是「改后达标」，而是**改前就已经达标**（规则早于被评提交）。

### 试法② 把新守卫 ⑦／⑧ 改坏看红不红

**自写变异脚本 `.scratch/t523-review/mut-rev.mjs`**（不复用实施席 `mut-r4.mjs`），全程一次持锁内做完、`finally` 逐字节还原，runId `9e5b2a38-ac14-4cca-aaf1-7e5fdda4ba71`，exit 0。两行读数：

```
BASE  原样源码            build=0 测试[523+451] exit=0 pass=18 fail=0
M1 源码=records.ts::sourceCard 塞回「窗口」一格   build=0 测试[523] exit=1 pass=9 fail=1
    红用例=✖ #523 ⑧ 明细族来源脚注：撤「窗口」一格（日期只住页头与窗口卡，脚注留来源与条数）
M2 源码=records.ts::caliberLine 只留第一分句      build=0 测试[523+451] exit=1 pass=10 fail=8
    红用例=✖ #523 ⑦ 明细族口径行：三行收一行… ｜ ✖ #451 看今日运动／看昨日运动／看记录（有备注）／（按力量筛选）…（451 红 7 条）
RESTORE 逐字节还原=true  build=0 测试[523+451] exit=0 pass=18 fail=0
MUT-REV PASS 两步变异各必红＋还原必绿
```

⇒ 两条新守卫**各有一颗真牙**：⑧ 只被「塞回窗口格」打红；⑦ 被「收口时顺手删事实」打红，**并且** 451 的多条冻结断言同时红（「收口不许删事实」是双向钉死的）。还原后读回 `src/exercise/records.ts` 与 HEAD 逐字节同（`git status --porcelain` 空）。

> 前三次跑本试法时脚本自身失败（`324585e2`／`ebea6dcb`／`7adb970b`）：一次是 `tsc -b` 被别席中间态拦下而我只打印了 stderr（tsc 报错走 stdout）；一次是 PowerShell 往返写 UTF-8 把脚本写成花码。两次都是我自己的件的问题、如实留痕，不是本票的件。

### 试法③ 抽两页把产物里的数与 `git show fbba22e:<源件>` 的算式对照

- **第 25 页 `25-看运动记录（有备注）`**（产物可见文本 → 算式）：
  - 算式（`fbba22e:src/exercise/records.ts`）：`sessions: rows.length`／`totalBurned: Math.round(burned*10)/10`／`totalMinutes: mins`／`activeDays: days`；卡值与脚注分别是 `String(v.sessions)`＋`条`、`fmtNum(v.totalBurned)`＋`卡`、`共 ' + v.sessions + ' 条'`。
  - 种子（`.scratch/t523/run17.mjs`）：窗口内只有「慢跑／320 卡／30 分钟／备注 夜跑」带备注，命中 4 天。
  - 手算：**条数 4**、**总消耗 320×4＝1280 卡**、**总时长 30×4＝120 分钟**、**活跃 4 天**。
  - 产物逐字：`记录数 4 条`／`总消耗 1280 卡`／`总时长 120 分钟`／`活跃天数 4 天`／表标题 `共 4 条，本页显示 4 条，每页最多 50 条`／脚注 `记录数 共 4 条` ⇒ **对得上**。
- **第 27 页 `27-看运动记录（按有氧筛选）`**：分类＝有氧 3 行/天 ×4 天 ⇒ **12 条／(320+300+400)×4＝4080 卡／(30+30+45)×4＝420 分钟**，产物逐字 `12 条／4080 卡／420 分钟／4 天` ⇒ 对得上。
- **第 23 页 `23-看今日运动（vs 目标）`**：`fbba22e:src/render/sportDocs.ts` 环取 `Math.max(0, Math.min(100, Math.min(pct,100)))`、百分比由 `fmtNum(v.pct)` 文字承载；种子 `exercise_goal=300`，当日 5 行 320+150+300+80+400＝**1250**。
  产物逐字：`目标 300 卡／实际 1250 卡／完成度 416.7%（环最高画到 100%）／差额 950 卡（超出目标）`；手算 1250÷300＝416.67%≈**416.7**、1250−300＝**950** ⇒ 对得上，且产物自印的公式行「完成度＝实际 ÷ 目标」「差额＝实际 − 目标」与算式同向。

## 四、R4 视觉证据的真实性抽查

- 12 张双档整页截图**在盘上**（`D:\ilife\.dsh-vision-router\artifacts\t523-r4-p{16,22,23,24,25,27}-{1440,390}-fullpage.png`，4:41:12–4:41:16）＋8 张裁切，与 R4 报告点名的路径一一对应。
- 我读了 `t523-r4-p25-390-fullpage.png` 并与产物逐项对：页头「运动记录／运动记录明细」、窗口条 `2026-09-09 → 2026-09-15`＋`4 天有记录`、首页四条统计卡 `4 条／1280 卡／120 分钟／4 天`、8 列 4 行明细、口径区「口径：…／消耗＝…」一行＋「筛选口径：…」一行、脚注**只剩两行**「数据来源／记录数 共 4 条」⇒ **截图与本票 R4 产物一致**，R4 报告点名的三处返修在图上成立。
- 分数账自查：六页分 95／92／95／95／92／92，逐页 ＝ 100 − 报告所列扣分之和；族均 93.5／95.0／92.0；`(93.5+95.0+92.0)÷3 = 93.5 ≥ 90` ⇒ **达标线算术成立**。
- 一处逐页可核的旁证：R4 报告说「汇总族两页没有 `数据来源` 脚注块」——我自己扫 17 页可见文本，**汇总族 10 页全部无**、明细族 5 页与对照目标 2 页有 ⇒ 与该说法同向（该条已被报告列进 `## 待裁` 第 9 条，不扣分）。

## 五、缺陷清单（分级 ＋ 归属 ＋ 复现）

| # | 分级 | 归属 | 缺陷 | 怎么复现 |
|---|---|---|---|---|
| 1 | **S3** | 本票（草稿脚本） | `scan17.mjs` 的汇总行 `节点级命中合计=NaN`（`sum('')` 对不存在的键求和），`read17.json.summary.nodeTotalSum` 落成 `null` | `node tooling/run-locked.mjs --ticket 523 -- node .scratch/t523/scan17.mjs .scratch/t523/out .scratch/t523-review` ⇒ 打完 17 行逐页读数后即见 `节点级命中合计=NaN`。**只坏汇总那一格，逐页读数真实**；`.scratch/**` 是草稿区，不入交付 |
| 2 | **S3** | 视觉席报告（本票证据链上被照抄） | R4 报告的「上一轮 → 本轮」增量叙述与逐项账在**第 5、6 页对不上**：p23 记 `90 → 92`（+2），而逐项为「撤导航 −3＋眉标放宽 +2−替换一条 −4 ⇒ +2」外的其它项对不齐；p27 记 `87 → 95`（+8），而逐项为「撤导航 −3、撤口径 −3、撤长滑 −3、条数 −4→−3 +1、间距 −3→−2 +1」＝**+11**（应落 98） | 对读 `.scratch/t523/视觉复评R3.md` 第 4 页段与 `.scratch/t523/视觉复评R4.md` 第 4 页段；两轮的「100 − 所列扣分」各自自洽，**差的只是跨轮增量叙述**（相当于有 2～3 分未列明的加/扣项）。**不改判定**：两页无论按 92／95 还是 98 计，三族平均都 ≥90 |
| 3 | **S3** | 本票（票面口径歧义） | 「字节与盘上一致」在带复制载荷的 12 页上不成立（生成时刻自走）；见 §二之一 口径提醒 | 连跑两次 `run17.mjs` 比对 39 件 SHA256 ⇒ 25 件变（本票 17 页里 12 件变、5 件不变），差异逐字只有 `data-t` 里的生成时刻 |
| 4 | **S3（范围外）** | #518 | 台账门现为 `RESULT: 90/91`，唯一红＝`src/render/trendPredictDocs.ts 台账=852 实况=897`（该席正在同一棵树上铺 W6，`AGENTS.md` 现显示 897 且该文件有未提交改动） | `node tooling/run-locked.mjs --ticket 523 -- node packages/skill-calorie/scripts/check-warning-line.mjs`。**本票那一格（`records.ts` 365）绿**；不判本票 |
| 5 | **S3（范围外）** | #524／#525 | 截断探针 `scopeOutFailed=8`（`07/11/12/13` 回执族表值列左对齐、`33` 有氧总览同形；另本票 R3 窗口是 5） | 同 §二 表 #7；探针已按「范围外读数照打、不进退出码」口径处理，本票 17 页逐格 0 |

| 6 | **S3** | 本票（提交信息文案） | `fbba22e` 提交信息写「`exercise-text-shape-523`…（共 **9→11** 条用例）」，与盘上实况不符：**8→10** 条 | `git show fbba22e^:…/exercise-text-shape-523.test.mjs` 与 `git show fbba22e:…` 各数 `^test(` ⇒ **8 → 10**；`node --test` 单跑该件 ⇒ `pass 10`。**判据结构未缩水这句是对的**，只有计数错 1 |

**没有 S1、没有 S2**（本票的判据结构未缩水：④ 的 `451` 断言由「≥2 条」改「≥1 条」是票面裁定 ① 授权的「事实换落点」，且我核到它**同时逐字钉死收进去的两条事实**（`test/exercise-records-fusion-451.test.mjs:195` 的 `for (const fact of ['口径：条数＝本窗内未删除的运动记录', '消耗＝记录行上报值合计，不按天摊'])`）；用例数只增不减：523 件 **8→10** 条，四件合跑 `tests 35 / pass 35 / fail 0`（R3 是 33 ⇒ ＋2 ＝ 新增 ⑦⑧ 两条）。

## 六、未做项 ＋ 下一手缺什么

1. **本席未做视觉重评**（不做第二遍 6 页 × 双档打分）：我只核了 R4 视觉件在盘上、与产物一致、分数算术成立。若编排者要「评价者独立于被评者」，需要**另起一位不与实施席同源的视觉席**再判一轮。
2. **R4 余下扣分的处置**（日期／条数／完成度复读 −20、非 4 倍数间距 −12、长窗无折叠 −3）全在 §十三·二 归了去处（冻结断言／页族身份针／公共层票／收口票）。本席同意「不是本票写集内可动」的归类，但**没有**独立验证「改成那样一定会红」——那需要动 `#267`／`#451`／`#452` 的冻结件，属下一张口径票。
3. **公共层两票仍缺**：`.ilife-block-dist-row-name` 的 `6em＋nowrap`（本票用页级覆盖兜住本族，全仓其它调用点仍丢字）与间距网格取值；本票已如实转票。
4. **建议**（给编排者）：`#523` 的机器面已可关；关票前请裁 §五 第 3 条那条口径，并让 #518 把 `trendPredictDocs.ts` 的台账同步做掉（否则任何下游票跑台账门都会看到同一个红）。
