# t253 · 独立复核（第三阶段）

票：#253「实现：目标 5 条写命令的回执页从片段变完整文档」。
被审提交：`488cff56e07579b2fc5c7cdb42644d77b63a7911`（收尾文档提交 `66dd8282ca0ac6ec8fd62435d04fdc49d4fda3bd`）。
复核者＝253-R：**受审的四个件一行未改**（只在一个持锁窗口里临时改过 `src/goal/receipt.ts` 的一行做受控变异，每发立刻还原并比对 sha，读数见 §三）。
实施者的结论一律不采信——§二、§三、§四的每一条都是复核者自己跑出来的；实施者的证据件只当线索用。

探针与日志住 `.scratch/t253-R/`：`probe.mjs`（自设探针，13 条）／`mutate.mjs`（两发受控变异）／`final.mjs`（探针基线复跑＋既有件读红）／`legacy.mjs`（既有件红点逐条点名），同名 `.log` 是全部原始输出，`receipt.ts.orig` 是受审件成文副本。

## 一、圈定范围（机器读数）

`git show --stat 488cff5` ——**四个件，逐件都在票面写集内，没多一件**：

| 件 | 增删 |
| --- | --- |
| `packages/skill-calorie/src/goal/receipt.ts` | 新建，236 行（LF 口径；350 告警线以内） |
| `packages/skill-calorie/src/cli/write.ts` | ＋9 −1（一段件头注释 ＋ 一行 import ＋ 链尾续接一口） |
| `packages/skill-calorie/test/goal-receipt-253.test.mjs` | 新建，267 行 |
| `docs/skills/skill-calorie/t253-证据.md` | 新建，137 行（收尾提交 `66dd8282` 再 ＋20 行） |

- 工作区与提交一致：`git diff HEAD -- <写集四件>` 无输出，`git status --porcelain -- <写集四件>` 无输出。
- 接线点只动两处：`import { goalReceiptDoc } from '../goal/receipt.js';` 与 `src/cli/write.ts:92` 那条 `??` 链**链尾**新加一口（票面记的 `:85` 是改动前的位置，本票在链上方补了一行注释，位置随之下移）。链上既有五家（`profileReceiptDoc`／`dietReceiptDoc`／`weightReceiptDoc`／`workoutReceiptDoc`／`bodyReceiptDoc`）的名字与先后次序一字未动（`git diff 488cff5~1 488cff5 -- packages/skill-calorie/src/cli/write.ts` 逐行看过）。
- 新件对外只有一个名字：`Select-String -Pattern "^export"` 命中 1 行（`export function goalReceiptDoc`）。
- 受审件成文 sha256 前 16 位＝`c417f60bbe580c27`（复核全程不变，见 §三两发变异的 `src_sha` 两行）。

## 二、独立重跑（只信自己的退出码）

```
GATE-RUN runId=75e9084c-679f-4768-98db-823f61b067c9
        cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
        摘要行 RESULT: ticket=253-R runId=75e9084c-… waitedMs=0 exit=0
        退出码 0（日志 .scratch/t253-R/build.log）

GATE-RUN runId=db604dd6-a38f-4d49-ae2f-3cee8f80d51a
        cmd=node --test packages/skill-calorie/test/goal-receipt-253.test.mjs
        摘要行 ℹ tests 5 ／ ℹ pass 5 ／ ℹ fail 0（RESULT: ticket=253-R runId=db604dd6-… waitedMs=50035 exit=0）
        退出码 0（日志 .scratch/t253-R/rerun.log）
```

一条废读数要记：第一次尝试（`runId=3d4f540e-065e-4733-90d3-17048fe0890d`）在等锁 60 秒后被复核者自己的工具超时掐掉，`gate-runs.log` 里只有 `START` 没有 `RUN` ⇒ 那次不算数，上面的读数才是独立重跑的结果。另注：编译一律走 `node node_modules/typescript/bin/tsc`（复核者当场核过 `node_modules/.bin` 不存在，`npx tsc` 在本机解析不到本地编译器；实施者记的那条坑与这一致，故本复核不采信任何走 `npx` 取来的绿读数）。

## 三、新试法（自设探针 13 条 ＋ 两发受控变异）

### 探针凭什么只打实施者用例够不着的地方

| 面 | 实施者用例 | 自设探针 |
| --- | --- | --- |
| A 屏面零内部词 | 从 `<body>` 起截、只扫正文文本层——`<head>` 里的 `<title>`／眉标一律扫不到 | 扫**整份文件**去 script／style／标签后的文本层（含 head），带**反空转哨兵**（取到的文本必须含「字段变更／库里现在的目标／复制数据」，否则零命中不作数） |
| B 「改后」按库列对齐 | 只看过 set／water／pause 三页的字段变更行，**体重页从没查过** | 钉 `calorie.goal.weight` 的两行必须是「体重目标(kg)／截止日期」并读回 68 kg／2026-09-19 |
| C 「改前」真值随库 | 只有种子那个 2000（正好也等于库内原值），分不清真读库与撞种子 | 先手改库里 `water_goal=1500`，再跑 `--water 2600`，要求改前 1500 |
| D 第六个装配口认哪些命令 | 只跑自己那 5 条命令——端口把判定放宽成「全收」它也全绿 | 12 条别的域的命令逐条问端口函数，必须一律返 null 放行 |

基线读数（受审提交、无变异）：`PROBE-SUMMARY total=13 pass=13 fail=0`，两条：

```
GATE-RUN runId=cc0f0b5a-8173-4e71-b13c-8f352cba49b0 cmd=node .scratch/t253-R/probe.mjs waitedMs=310249 exit=0
GATE-RUN runId=12229d19-212c-4c24-8657-06413e4d415e cmd=node .scratch/t253-R/final.mjs waitedMs=140143 exit=0（内含探针复跑 13/13 绿）
```

### 两发受控变异（各在**一个持锁窗口内**做完并立刻还原）

```
GATE-RUN runId=bfd05c23-b7a4-4e46-aa5d-9aced849c0d0 cmd=node .scratch/t253-R/mutate.mjs M1 waitedMs=430371 exit=0
  BASELINE M1 src_sha=c417f60bbe580c27 与提交一致=1
  M1 applied=1 src_sha_mut=6c1e56198b6fccca build_exit=0 dist_changed=1
  M1 IMPL[test/goal-receipt-253.test.mjs] exit=0 pass=5 fail=0        ← 实施者那 5 条用例照样全绿
  M1 PROBE exit=1 red=[D 第六个装配口只认自己那 5 个命令（别的域 12 条一律返 null 放行）——这些命令被目标端口接管了：["calorie.exercise.add"]]
  M1 RESTORED sha_match=1 ／ RESTORE-CHECK build_exit=0 PROBE exit=0 red=[] ／ FINAL clean=1

GATE-RUN runId=74dc1757-8622-4b5e-92de-7aaa42f1eaa0 cmd=node .scratch/t253-R/mutate.mjs M2 waitedMs=50046 exit=0
  BASELINE M2 src_sha=c417f60bbe580c27 与提交一致=1
  M2 applied=1 src_sha_mut=a604410904c7f896 build_exit=0 dist_changed=1
  M2 IMPL[test/goal-receipt-253.test.mjs] exit=0 pass=5 fail=0        ← 实施者那 5 条用例照样全绿
  M2 PROBE exit=1 red=[B 定体重目标页的两行＝体重目标(kg)／截止日期 —— 字段变更行没按库列取中文名：[["起点体重(kg)","—","—"],["截止日期","—","2026-09-19"]]]
  M2 RESTORED sha_match=1 ／ RESTORE-CHECK build_exit=0 PROBE exit=0 red=[] ／ FINAL clean=1
```

两发变异都摆在**受审件自己的那一行**上：M1 ＝放宽端口函数的判定（把 `calorie.exercise.add` 也塞进具名命令集），M2 ＝把「参数名→库列」的 `kg` 指到 `start_weight`。两发的共同读数说明一件事：**实施者那套用例的判据面比实现窄**——端口认哪些命令、体重页那一格读的是哪一列，这两件事它都够不着。M2 那一发的红行里「改后」印成 `—` 而不是 `68 kg`，是把体重目标的格子错读成起点体重的直接后果（真实缺陷面：用户看到的目标值错位）。

还原两行读数（每发窗口末尾）：`RESTORED sha_match=1`、`RESTORE-CHECK build_exit=0 PROBE exit=0 red=[]`，收尾 `FINAL src_sha=c417f60bbe580c27 src_matches_orig=1`；窗口外再核一次 `git status --porcelain -- src/goal/receipt.ts src/cli/write.ts` 无输出 ⇒ 工区干净、受审件与提交一致。

## 四、实施者那句「既有件必红」——复核者自己点名六条

```
GATE-RUN runId=2cb5aa23-0c54-4b3c-8c6a-63fc75cb4ad5 cmd=node .scratch/t253-R/legacy.mjs waitedMs=0 exit=0
        （内含 node --test --test-reporter=tap profile-doc-179 delivery-83）
        摘要行 LEGACY exit=1 ／ # tests 23 ／ # pass 17 ／ # fail 6
        同一件事在 runId=12229d19 那一窗也跑过一遍，读数逐字相同
```

六条红逐条点名（复核者自己跑出来的名字，不是抄实施者的）：

| # | 红在哪条用例 | 归谁 |
| --- | --- | --- |
| 1 | `#179 其余会改数据库的命令仍是原回执片段（没被一刀切换页）` | **本票口径变更所致**（`profile-doc-179.test.mjs:276-290` 正钉 `calorie.goal.set`／`calorie.goal.water` 仍是片段） |
| 2 | `#83 写键同样有 delivery（receipt 产物族）`（用例原名逐字照抄） | **本票口径变更所致**（`delivery-83.test.mjs:324-329` 正钉 `calorie.goal.set` 的产物族＝片段） |
| 3 | `#337 场景 03 四条体重写命令已是完整文档（三变体字段齐）`——`weight.log 缺：体重 · 写后回执` | 体重域（别的票在场） |
| 4 | `#365 身体细节四条写命令已是完整文档（七条写词）`——`记体脂 缺场景 08 眉标` | 身体域（别的票在场） |
| 5 | `#239 四张页接上「复制日志」` | 档案域预检确认页 |
| 6 | `#247 场景 07 五张页的复制数据是「三格式三选一」菜单`——`预检确认页 菜单缺用途提示：粘贴给 AI / 自己看` | 同上 |

第 3～6 条不是本票造成的，有一条反向印证：本票那一口排在 `??` 链**最后**，且对非本域命令一律返 null（探针 D 基线绿），所以链上在前那几家的产物字节不变；M1 那一发把判定放宽之后 `calorie.exercise.add` 立刻被接管（探针 D 变红），正说明这条短路是靠「只认 5 条命令」守住的，不是靠调用次序碰运气。
第 1、2 条是本票**有意**的口径变更（片段→整页），两个件都不在票面写集，票面也明写「不许为了让断言变绿去改既有测试」⇒ 实施者一行未改、只上报，处置正确；须编排者按 #269／#337／#365 先例另裁（把这两处的抽样换成尚未切整页的命令）。

## 五、判定与缺陷清单

**判定：PASS**。打分 99/100：

| 面 | 分 | 依据 |
| --- | --- | --- |
| 契约一致 | 30/30 | 5 条命令经真 CLI 逐条 exit 0；产物以 `<!doctype html>` 起、含 charset／style／`ilife-page`、无片段标记；页签「卡路里 目标回执」；三块标题与锚点对齐；写入语义只读回读逐列不变；缺参 exit 2 不落盘／空库 exit 4；只动链尾一口、既有五家次序未动 |
| 证据真实可复现 | 25/25 | 实施者报的两条主要读数（验收 5/5 绿、既有件 6 红）复核者各自复跑一次，逐字相同；他自己披露的「首轮 npx 假绿」与复核者所见一致 |
| 新旧对照 | 20/20 | 屏幕可见文本（含 head）零内部词，`op=`／`id=1` 不再上屏；「改后」逐格读库（手改库内 1500 后页面跟着变）、「改前」只认回执自带对子 |
| 工程红线 | 15/15 | 写集四件以外零改动；编译／测试／提交全走加锁包装器；未碰真库（临时库＋种子）；未动 `package.json`／`pnpm build`／生成物 |
| 文档同步 | 9/10 | 证据件与实况对得上（§一 两张表、§三 六条红的分组、§四 未做项都实）；扣 1 分见下条缺陷 2 |

缺陷（逐条标来源）：

1. **本票引入：无。**
2. **本票范围（文档不自洽，S3，非本票造错）**：`src/cli/write.ts` 件头那句「其余 1 条（35 − 3 − 13 − 4 − 10 − 4）一字不改，仍是原回执片段」当刻对不上实况——命令登记表 `CALORIE_WRITE_COMBOS`（`src/cli/keys.ts`）里有 **46** 条会改数据库的命令，六个装配口一共认 **40** 条（档案 3／饮食 14／体重 4／训练计划 10／身体 4／目标 5），链外还有 **6** 条（`calorie.exercise.*` 三条、`calorie.photo.*` 三条，其中记运动那一条当刻已由运动域自己的实现出页，读数见 `.scratch/t253-R/probe.log` 的 `info` 行）。这句话在本票之前就已陈化，本票碰了这段件头却没顺手改数。**下一手**：改这句计数时以命令登记表为唯一来源，别再抄旧数。
3. **范围外发现（S3，不单独判 FAIL）**：`#179`／`#83` 两处抽样钉住的命令已不是片段（§四 第 1、2 条），须编排者按先例裁口径变更；同批另 4 条红分属体重域、身体域、档案域，与本票无关。
4. **范围外发现（S3）**：字段中文名在目标域出现第二份字面表（`src/goal/precheck.ts` 那张未导出、`src/goal/receipt.ts:70-81` 另抄一份）；收成一处要另开一票动目标域两件，本票写集不含。
5. **范围外发现（S3）**：「改前」那一列只有饮水一条有真值（写口只回报变更字段名），其余写 `—` ＋表下口径行——票面接受这个口径（不拿新值顶替），要出真值须改 `src/goal/write.ts`（不在写集）。
