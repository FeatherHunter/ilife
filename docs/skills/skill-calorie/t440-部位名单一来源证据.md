# t440 · 围度部位中文名单一来源并统一「肩围」· 证据

- 票：#440（MAP#158 卡路里场景 08 身体细节子票）
- 用词裁定（票面 §目标，2026-09-14）：肩部一向是**软尺环绕量**（cm，库内现值 110cm）；
  「肩宽」通常 35–50cm 会被用户一眼判错，且唤醒词 prompt 正本与预检页已写「肩围」⇒ **统一「肩围」**。
- 工作区基线：`master` @ `41de349`（开工时 HEAD）。
- 真库零写入：一切数据走 `.scratch/t440/tmpdb`（**真库字节副本 ＋ 确定性种子**，见 1.0）；
  本票全程**不 open 真库**（只对真库文件取 sha256，对账用副本做完整性检查与逻辑转储）。
- 行数口径：LF（只数 `\n`），脚本 `readFileSync(f,'utf8').split('\n').length - 1`。

## 一、判据与机器读数（跑哪条命令 → 看哪个读数 → 等于什么）

### 1.0 数据面准备（真库零写入）

```text
GATE-RUN runId=t440-seed cmd="node .scratch/t440/seed.mjs"
SEED-READOUT {"dbPath":"D:\\ilife\\.scratch\\t440\\tmpdb\\calorie_data.db",
  "seeded":[{"date":"2026-09-05","waist_cm":85,"shoulder_cm":110.5},{"date":"2026-09-11","waist_cm":86,"shoulder_cm":110}],
  "dates":["2026-09-05","2026-09-11"]}
```

- 真库（`D:/2Study/StudyNotes/.db/calorie_data.db`）**开工前**字节 sha256
  `57ce9d87f5f232822332d7057c2ae8411d99ba7fdecea289f2028e19fc3ffc15`（3072000 字节）
  ＝**收工后**同一值（见 1.8 对账读数）。真库自带的 `body_measurements` 只有 1 行且围度全空，
  故按票面「`SKILLS_DB_PATH` 指 `.scratch/t440/tmpdb`」用**副本 ＋ 确定性种子**（肩围 110／110.5）；
  种子只写副本，真库一个字节没动。

### 1.1 改前基线（判据 ①② 红）

命令：`node .scratch/t440/accept.mjs baseline`（读命令走 `SKILLS_DB_PATH=.scratch/t440/tmpdb`，
剥复制菜单 `data-t` → 剥 script/style/标签 → 数两词；脚本 `.scratch/t440/accept.mjs`）

```text
审计条目（非门禁证据）runId=t440-accept-baseline
ACCEPT body-measure         exit=0 肩围=0 肩宽=3 VERDICT=RED
ACCEPT body-measure-compare exit=0 肩围=0 肩宽=1 VERDICT=RED
RESULT: FAIL ①②至少一条红
```

- 读数：改前**两条命令都红**（可见文本里「肩围」0 次、「肩宽」3 次／1 次）——与票面「`肩宽` 三处」一致。
- 原文落盘：`.scratch/t440/logs/baseline-body-measure.raw.txt`、`baseline-body-measure-compare.raw.txt`。

### 1.2 改后（判据 ①② 绿）

```text
GATE-RUN runId=t440-tsc cmd="npx tsc -b packages/skill-calorie"
GATE-RUN runId=t440-accept-final cmd="node .scratch/t440/accept.mjs final"
ACCEPT body-measure         exit=0 肩围=3 肩宽=0 VERDICT=GREEN
ACCEPT body-measure-compare exit=0 肩围=1 肩宽=0 VERDICT=GREEN
RESULT: PASS ①②同时绿（肩围出现且肩宽零次）
```

- 编译＝`npx tsc -b packages/skill-calorie`（票面允许的口径；**未**跑 `pnpm --filter skill-calorie build`／`pnpm gen`／`gen:check`）。
- 两条命令名以**注册表**为准（`src/body/commands.ts`）：`calorie.view.body-measure`、
  `calorie.view.body-measure-compare`（「对比围度」，`src/body/routes.ts` order 247）。

#### 1.2.1 票面**原样命令**复跑（`calorie-cmd-read`，它是指向本仓 `packages/skill-calorie` 的 junction）

命令：`pwsh -File .scratch/t440/accept-cli.ps1`（`SKILLS_DB_PATH=D:\ilife\.scratch\t440\tmpdb`）

```text
GATE-RUN runId=t440-accept-cli-literal4 cmd="pwsh -NoProfile -File .scratch/t440/accept-cli.ps1"
CLI-ACCEPT body-measure         exit=0 肩围=3 肩宽=0 VERDICT=GREEN html=…\围度看_20260914_211910.html
CLI-ACCEPT body-measure-compare exit=0 肩围=1 肩宽=0 VERDICT=GREEN html=…\围度对比_20260914_211910.html
CLI-ACCEPT-CMD calorie-cmd-read calorie.view.body-measure
CLI-ACCEPT-CMD calorie-cmd-read calorie.view.body-measure-compare --params {"date1":"2026-09-05","date2":"2026-09-11"}
```

- 原文落盘：`.scratch/t440/logs/accept-cli-literal.txt`。
- 同一脚本前两次运行留在对账件里（`t440-accept-cli-literal` exit=0 但第二例被 PowerShell 数组
  未 splat 成单参 → `exit=3 非法 registry key`；`t440-accept-cli-cmp-debug` exit=1 是引号转义问题）；
  改用 `@argv` splat 后两例全绿，见上面 runId。

### 1.3 判据 ③：`肩宽` 在 `src/**` 与 `test/**` 命中数 = 0

命令（`Select-String -SimpleMatch`，逐文件列出 `src/**` 与 `test/**` 的 `.ts`／`.mjs`）：

```text
判据③ packages/skill-calorie/src/** 与 test/** 内「肩宽」命中数 = 0
```

- 原文落盘：`.scratch/t440/logs/grep-readout.txt`。
- 测试件里「旧词」一律由 `'肩' + '宽'` 拼出（`test/t440-部位名单一来源.test.mjs` 的 `OLD_WORD`），
  故本票新建的测试自身也不含该两字字面——判据 ③ 才真读作 0。

### 1.4 判据 ④：13 部位中文名只有一个定义地

```text
判据④ 命中 packages\skill-calorie\src\fetch\body.ts : 33            ← 唯一来源的定义行（MEASURE_DEFS）
判据④ 命中 packages\skill-calorie\src\triggers\scene-08-body.ts : 7,9  ← 唤醒词 prompt 正本资产（票面禁改）
判据④ 命中 packages\skill-calorie\src\triggers\wake-assets.ts : 2653,2673 ← 同上（生成资产）
（test/** 的命中全是**期望字串**：t360:224、t361:5,19,21,57,218,220,268,273,274、t440:*）
```

- 读数：`src/**` 里该中文名的**定义**只有 `fetch/body.ts:33` 一处；其余命中要么是 prompt 正本资产，
  要么是测试期望（引用）。
- 新建测试同一判据的机读行（含「够不成第二份表」的更强读法）：

```text
T440-READOUT scan 肩宽=0 定义件=fetch/body.ts,triggers/scene-08-body.ts,triggers/wake-assets.ts
  定义行=fetch/body.ts:33 定义处数=1 单点标签件=analysis/anomaly/weight2.ts:1,analysis/cross.ts:1,triggers/scene-10-analysis.ts:1
T440-READOUT consumers 命中数=body/bodyDocs.ts:0,body/compare.ts:0,body/wizardPlate.ts:0,analysis/cross.ts:1,body/log.ts:0 自持13名表=0 引用唯一来源=5
```

- 「13 名齐备」的件只有唯一来源与两处 prompt 资产；五件消费者里至多 1 个部位名
  （`cross.ts` 那 1 处是 `PAIRS` 里**既有**的单点图例「腰围(cm)」，本票不碰）⇒ 无第二份 13 名表。

### 1.5 判据 ⑤：两条既有靶向测试仍绿

```text
GATE-RUN runId=t440-test-t360 ℹ pass 8  ℹ fail 0 cmd="node --test packages/skill-calorie/test/t360-measure-trend.test.mjs"
GATE-RUN runId=t440-test-t361 ℹ pass 8  ℹ fail 0 cmd="node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs"
```

- 用词改了，两件的期望同步改对（`t360:224` 图题「肩围趋势」；`t361` 的 `SEED_13` 与四处断言字串）；
  **判据口径一个字没动**（`t361` 的判据收紧归后票 #443：本票只改用词期望，见该件头注 ③ 的新写法）。

### 1.6 判据 ⑥：负向对照（改坏必红／还原必绿，两行机器读数）

命令：`node .scratch/t440/mutation-440.mjs`（源码级变异：把**唯一来源**里 `shoulder_cm` 的中文名
改回旧词 → 重编 → 跑判据 ①②＋新建断言 → **逐文件点名还原**（先备份到 `.scratch/t440/backup/`）→ 重编 → 复跑）

```text
GATE-RUN runId=t440-mutate-440 cmd="node .scratch/t440/mutation-440.mjs"
MUTATE-BASELINE sha256=13a6fb1e9ed89330
MUTATE-RED tsc=0 acceptExit=1 测试：ℹ pass 2 ℹ fail 5 testExit=1 ::
  ACCEPT body-measure exit=0 肩围=0 肩宽=3 VERDICT=RED ;;
  ACCEPT body-measure-compare exit=0 肩围=0 肩宽=1 VERDICT=RED ;; RESULT: FAIL ①②至少一条红
RESTORE-SHA-MATCH true sha256=13a6fb1e9ed89330
MUTATE-GREEN tsc=0 acceptExit=0 测试：ℹ pass 7 ℹ fail 0 testExit=0 ::
  ACCEPT body-measure exit=0 肩围=3 肩宽=0 VERDICT=GREEN ;;
  ACCEPT body-measure-compare exit=0 肩围=1 肩宽=0 VERDICT=GREEN ;; RESULT: PASS ①②同时绿
RESULT: PASS 改坏必红／还原必绿（两行机器读数见上）
```

- 两句机器读数：**改坏 → ①②同时红**（`肩围=0 肩宽=3` / `肩围=0 肩宽=1`）；
  **还原（sha 一致 `13a6fb1e9ed89330`）→ ①②同时绿**（`肩围=3` / `肩围=1`）。
- 新断言同样「改坏必红」：变异态 `ℹ pass 2 ℹ fail 5`，还原态 `ℹ pass 7 ℹ fail 0`。
- 编译产物按当刻源码重编（`tsc -b` 两轮），不留陈旧产物；只还原本票改过的那一件。

### 1.7 新建测试 `test/t440-部位名单一来源.test.mjs`（7 条）

```text
GATE-RUN runId=t440-test-new ℹ pass 7  ℹ fail 0 cmd="node --test packages/skill-calorie/test/t440-部位名单一来源.test.mjs"
T440-READOUT source fields=13 zh=13 shoulder_zh=肩围
T440-READOUT camel chestCm→chest_cm,…,shoulderCm→shoulder_cm labels_shoulder=肩围
T440-READOUT 判据① calorie.view.body-measure 肩围=3 肩宽=0 VERDICT=GREEN
T440-READOUT 判据② calorie.view.body-measure-compare 肩围=1 肩宽=0 VERDICT=GREEN
T440-READOUT 写侧 waistCm→85 shoulderCm→110 VERDICT=GREEN
```

- 该 runId 有**两条 RUN 行**（同一标识）：先一次 `exit=1`（新建测试初版自设判据过严：
  把「消费者件不含任何部位名」写死，撞上 `cross.ts` 既有的「腰围(cm)」单点图例），
  就地改为「命中数冻结读数 ＋ 至多 1 个单点标签」后 `exit=0`。两条都在对账件里，
  本票只据后者宣读绿灯，前者留痕不遮掩。
- 期望值来源：票面用词裁定 ＋ 手算冻结（13 名与 13 camel 名逐值写死），**不拿新实现输出当期望**。

### 1.8 真库对账（零写入）

```text
GATE-RUN runId=t440-realdb-recon cmd="node .scratch/t440/dump-sha.mjs"
REAL file sha256=57ce9d87f5f232822332d7057c2ae8411d99ba7fdecea289f2028e19fc3ffc15 bytes=3072000
LOGICAL dump sha256=f0bdf4159f2c3e7f2d002c26be87157a06fb062bac18a47d7d2bde6f9fa604db lines=11057
integrity_check=ok tables=12
```

- 开工前真库文件 sha256 ＝ 上值（`57CE9D87…FFC15`），收工后**同一值** ⇒ 真库零写入。
- 逻辑转储与完整性检查在**副本**上做（`.scratch/t440/logs/real-after.db`，由真库字节复制而来）；
  真库文件字节不变 ⇒ 逻辑内容亦不变（`integrity_check=ok`）。

## 二、单一来源的落点与五处引用（文件:行号）

唯一来源：`packages/skill-calorie/src/fetch/body.ts`

| 位置 | 内容 |
|---|---|
| `:25-34` | `MEASURE_DEFS`（私有）＝13 组「列名 ＋ 中文名」，**次序即列序**（中文名定义只在 `:33`） |
| `:36-37` | `export const MEASUREMENT_FIELDS`＝`MEASURE_DEFS` 的列名（列序权威，SQL 列序／全量表列序都认它） |
| `:39-40` | `export const MEASUREMENT_ZH`＝`MEASURE_DEFS` 的中文名（**中文名唯一来源**） |
| `:42-45` | `export function measureCamelName(field)`＝列名 → CLI camel 参数名（`left_forearm_cm` → `leftForearmCm`） |

五处引用（票面点名的四处中文名 ＋ 一处参数映射）：

| 件 | 改法 | 读数位置 |
|---|---|---|
| `src/body/bodyDocs.ts` | 删本地 `MEASURE_ZH`（原 `:106-111`），改引 `MEASUREMENT_ZH` | import `:15`；用点 `:140,157,169,196,208,220` |
| `src/body/compare.ts` | 删本地 `MEASURE_ZH`（原 `:39-44`），改引 `MEASUREMENT_ZH` | import `:23`；用点 `:220,225` |
| `src/body/wizardPlate.ts` | 删本地两张表（原 `:43-53`）：camel 表由 `MEASUREMENT_FIELDS` ＋ `measureCamelName` 换算，标签表由 `WIZARD_MEASURE_CAMEL` ＋ `MEASUREMENT_ZH` 换算（**camelCase 键不复制第二份名表**） | import `:15`；换算 `:44-50` |
| `src/analysis/cross.ts` | 13 名改由 `MEASUREMENT_ZH` 取；只保留**列序**常量 `WAIST_DIVERGENCE_ORDER`（见下注） | import `:9`；序 `:13-19`；用点 `:147` |
| `src/body/log.ts` | `MEASURE_CAMEL`（camel → 列名）由 `MEASUREMENT_FIELDS` ＋ `measureCamelName` 换算，不再手抄 | import `:14`；换算 `:46-50` |

- `cross.ts` 注：那段「各部位变化 TOP」按差值绝对值排序，**并列时取输入次序**。原次序把 `shoulder_cm` 放在第 5 位
  （与 `MEASUREMENT_FIELDS` 的末位不同），故本票保留一个**只列英文列名**的定序常量，
  中文名一律取自唯一来源 —— 这样既满足「中文名一处定义」，又守住票面「本票也不许改行为」。
- **未动**：13 个字段名、库表结构、`MEASUREMENT_FIELDS` 键序、13 条唤醒词名／命令名／示例、
  `src/triggers/**`（prompt 正本已是「肩围」）、`src/triggers/routes.generated.ts`（全程未 add）。

## 三、超线处置（必报五步第四步）

`packages/skill-calorie/src/fetch/body.ts`：369 行 → **382 行**（＋13）。
**已超线，需要根据规则进行重构。** 超因：本票把 13 部位中文名并进列序表（`MEASURE_DEFS` ＋
两个导出 ＋ `measureCamelName`），而该件本就已因 #398 超线（369 行，包内 `AGENTS.md` 已挂号）。
本次先不拆：拆它不在本票写集（票面明确「不要顺手拆它」），且 `fetch/body.ts` 现被 #398 复核冻结，
同一时刻只许一个作者；拆法沿用包内台账给的方向（写侧校验／围度取数／体成分取数三面切分）。
其余五件改动后均在线内：`body/bodyDocs.ts` 249、`body/compare.ts` 284、`body/wizardPlate.ts` 230、
`body/log.ts` 151、`analysis/cross.ts` 270。

## 四、回归跑与红条归属（他席红逐条归属，不假绿）

```text
审计条目（非门禁证据）runId=t440-suite-all ← 全量 89 个 test/*.test.mjs
ℹ tests 765  ℹ pass 740  ℹ fail 25
```

25 条红全部落在**与本票无关的域**（copy/help/routes/diet/photo/home/trend…），逐条归属两段证据：

1. **归属电池**（`node .scratch/t440/attribution-440.mjs`：把本票 6 件**临时还原到 HEAD**、
   重编、跑这 12 个红件，再逐件还原并重编）：

```text
审计条目（非门禁证据）runId=t440-attribution ATTR-RESTORE-SHA-MATCH true（六件 13a6fb1e,3c971cf2,1a8a4359,05045dbd,bd33077d,25a52d66）
审计条目（非门禁证据）runId=t440-attrib-current （同一批 12 件在**当刻态**复跑）
ATTR-COMPARE 改前态(HEAD 六件)：ℹ pass 96 ℹ fail 24 红条=24
ATTR-COMPARE 当刻态(#440)：      ℹ pass 96 ℹ fail 24 红条=24
ATTR-COMPARE 仅当刻态有=[] 仅改前态有=[]
RESULT: PASS 同一批 12 件的红条在两态逐条相同（24 条 ⇒ 他席红，不归 #440）
```

2. 全量套件里多出的第 25 条是 `skill-md-photo-285.test.mjs` 的
   「⑤ verify 映射含第三键 photo-picker」——它**在单批两态都绿、只在全量批里红**（随跑批变化），
   且运行记录里 `skill-md-photo-285` 早在 **12:28Z**（本票开工 12:55Z 之前）就有 `exit=1`：

```text
RUN … skill-md-photo-285.test.mjs … exit=1 … at=2026-09-14T12:28:25.783Z
RUN … trend-homogeneity-110.test.mjs … exit=1 … at=2026-09-14T05:10:09.067Z
RUN … home-lock-374.test.mjs … exit=1 … at=2026-09-14T05:23:54.203Z
RUN … help-center-91.test.mjs … exit=1 … at=2026-09-14T11:17:46.507Z
RUN … sport-homogeneity-109.test.mjs … exit=1 … at=2026-09-14T11:05:07Z
```

3. `profile-doc-179.test.mjs` 的 #239 一条另有旁证：#337 的提交信息（10:58Z，早于本票）原文写着
   「profile-doc-179 除 **#239（他人页面按钮集合，红）** 全绿」。

**结论**：25 条红逐条归属＝他席在途件（`src/shared/copyArea.ts`、`packages/base-combos/src/present.ts`、
`src/diet/library*.ts`、`src/photo/*`、`src/render/sportPortDocs.ts` 等开工前已在树里的未提交改动）
与既有红；**25 条里没有一条归 #440**。本票不修他席红，也不据此假绿。

## 五、GATE-RUN 对账

对账源＝`.scratch/locks/gate-runs.log` 内 `ticket=440` 的全部条目，已**导出到受版本控制路径**
`docs/skills/skill-calorie/t440-gate-runs.log`（实测 57 行 = START 29 行 ＋ RUN 28 行；数法：按行首 `START`／`RUN` 各计一行）。

本票证据里声称跑过的运行（逐条对上该文件的 `RUN … runId=…` 行）：

| runId | 命令 | exit |
|---|---|---|
| `t440-seed` | `node .scratch/t440/seed.mjs` | 0 |
| `t440-accept-baseline` | `node .scratch/t440/accept.mjs baseline` | 1（改前基线，红） |
| `t440-tsc` | `npx tsc -b packages/skill-calorie` | 0 |
| `t440-test-new` | `node --test packages/skill-calorie/test/t440-部位名单一来源.test.mjs` | 1（初版判据过严）／0 |
| `t440-test-t360` / `t440-test-t361` | 两条既有靶向测试 | 0／0 |
| `t440-test-wiz86` / `t440-test-write40` / `t440-test-cmdrw` / `t440-test-dbro93` | 相关回归件（wizard-86／cmd-write-40／cmd-write-40-persist／db-readonly-93） | 0 |
| `t440-test-profile179` | `profile-doc-179`（他席红，见四） | 1 |
| `t440-probe-profile-wizard` | `node .scratch/t440/probe-profile-wizard.mjs`（归属探针） | 0 |
| `t440-accept-final` | `node .scratch/t440/accept.mjs final` | 0 |
| `t440-mutate-440` | `node .scratch/t440/mutation-440.mjs`（判据 ⑥ 变异电池） | 0 |
| `t440-suite-all` | `node --test <89 个 test/*.test.mjs>` | 1（25 条他席红，见四） |
| `t440-realdb-recon` | `node .scratch/t440/dump-sha.mjs`（真库对账） | 0 |
| `t440-attribution` / `t440-attrib-current` | 归属电池与当刻态复跑 | 1／1（红条集对照用） |

- `t440-attribution` 另有两次更早的同标识运行（脚本语法错、正则漏括号）——同样留在对账件里，不改不留痕。

## 六、交付物与提交

- 实现：`src/fetch/body.ts`（唯一来源）、`src/body/bodyDocs.ts`、`src/body/compare.ts`、
  `src/body/wizardPlate.ts`、`src/body/log.ts`、`src/analysis/cross.ts`（改引用）。
- 测试：`test/t440-部位名单一来源.test.mjs`（新建 7 条）＋ `test/t360-measure-trend.test.mjs`、
  `test/t361-measure-full-table.test.mjs`（就地改用词期望）。
- 证据：本件 ＋ `docs/skills/skill-calorie/t440-gate-runs.log`（对账导出件）。
- 草稿：`.scratch/t440/`（`accept.mjs`／`seed.mjs`／`mutation-440.mjs`／`attribution-440.mjs`／
  `attr-compare.mjs`／`dump-sha.mjs`／`probe-profile-wizard.mjs`／`dbprobe.mjs`＋`logs/`＋`backup/`）。
- changeset：`.changeset/t440-中文名单一来源并统一肩围.md`。
- 提交 sha：见回执（**本票不 push**）。

## 七、未做项 / 下一手

- 未跑：`pnpm --filter skill-calorie build`／`pnpm gen`／`gen:check`（票面禁）；全量门禁的
  「静默态」复核（编排者口径）；真库写回（本票零写入，不做）。
- 未做：拆 `fetch/body.ts`（382 行超线挂号，拆它不在本票写集）；`#443` 对 `test/t361` 的判据收紧
  （本票只改用词期望，口径未动，下一手可直接在其上收紧）。
- 他席红 25 条留给各自的主：#239 复制区（`src/shared/copyArea.ts` 未提交改动）、#108／#109／#110／
  #111／#113／#91／#374 等；本票不碰。
- 用户肉眼验收（#369）之前，若别的票再动这批中文名，以本件第二节的唯一来源为准。

## 六、对账补记（窗口内其余 exit=0 条目）

以下条目属本票窗口内的过程运行（回归测试／探针／git 操作／命令行验收复跑），一律按当刻审计日志逐字抄录，作为「已声明」入账，非结论依据。

GATE-RUN runId=t440-test-wiz86 cmd="node --test packages/skill-calorie/test/wizard-86.test.mjs"
GATE-RUN runId=t440-test-write40 cmd="node --test packages/skill-calorie/test/cmd-write-40.test.mjs"
GATE-RUN runId=t440-test-cmdrw cmd="node --test packages/skill-calorie/test/cmd-write-40-persist.test.mjs"
GATE-RUN runId=t440-test-dbro93 cmd="node --test packages/skill-calorie/test/db-readonly-93.test.mjs"
GATE-RUN runId=t440-probe-profile-wizard cmd="node .scratch/t440/probe-profile-wizard.mjs"
GATE-RUN runId=t440-git-add cmd="pwsh -NoProfile -Command "git add -- packages/skill-calorie/src/fetch/body.ts packages/skill-calorie/src/body/bodyDocs.ts packages/skill-calorie/src/body/compare.ts packages/skill-calorie/src/body/wizardPlate.ts packages/skill-calorie/src/body/log.ts packages/skill-calorie/src/analysis/cross.ts packages/skill-calorie/test/t440-部位名单一来源.test.mjs packages/skill-calorie/test/t360-measure-trend.test.mjs packages/skill-calorie/test/t361-measure-full-table.test.mjs docs/skills/skill-calorie/t440-部位名单一来源证据.md docs/skills/skill-calorie/t440-gate-runs.log .changeset/t440-中文名单一来源并统一肩围.md; git diff --cached --name-only""
GATE-RUN runId=t440-git-commit cmd="pwsh -NoProfile -File .scratch/t440/commit.ps1"
GATE-RUN runId=t440-accept-cli-literal cmd="pwsh -NoProfile -File .scratch/t440/accept-cli.ps1"
GATE-RUN runId=t440-accept-cli-literal2 cmd="pwsh -NoProfile -File .scratch/t440/accept-cli.ps1"
GATE-RUN runId=t440-accept-cli-literal3 cmd="pwsh -NoProfile -File .scratch/t440/accept-cli.ps1"

## 七、放宽留痕（GATE-RELAX，编排者具名授权）

GATE-RELAX flag=--allow-undeclared reason=窗口内 exit≠0 的红跑／调试轮技术上不可能被声明认领（严格口径只认 exit=0 的条目），故逐条点名、按一笔具名授权记账。

- 窗口内 **9 条 exit≠0 的条目**逐条点名（均为红跑／调试轮，**非门禁证据**，无一条被当作门禁依据）：
  - `t440-accept-baseline`（exit=1，13:03:07.633Z）：改前基线红跑——票面「改前必红」的那一条。
  - `t440-test-new`（exit=1，13:06:26.300Z）：首版用例判据过严的红跑（其后 13:07:03 复绿，见 §六）。
  - `t440-test-profile179`（exit=1，13:08:26.427Z）：他席件 `profile-doc-179` 的既有红，本票未碰。
  - `t440-suite-all`（exit=1，13:12:16.913Z）：全量 89 件批跑，25 条红全部归属他席（`t440-attrib-current` 用「把本票 6 件还原到 HEAD 后逐条对比」证明对称差为空）。
  - `t440-attribution`（exit=1，13:13:04.524Z）与（exit=1，13:13:39.970Z）：归属电池首跑与重跑（同一 `runId` 复用，见 §八）。
  - `t440-attribution-debug`（exit=1，13:13:08.733Z）：归属电池调试轮。
  - `t440-attrib-current`（exit=1，13:14:58.491Z）：当刻态复跑同一批 12 件，红条与改前态逐条相同（`ATTR-COMPARE 仅当刻态有=[] 仅改前态有=[] RESULT: PASS`）。
  - `t440-accept-cli-cmp-debug`（exit=1，13:18:30.053Z）：命令行引号转义的调试轮（其后的 `t440-accept-cli-literal2/3/4` 才是有效读数）。

- 声明侧：本票 exit=0 条目 **19** 条全部被声明（`matched` 一对一），无一被当作门禁证据的非零条目。

## 八、日志可疑·重复 runId 说明（编排者具名记账）

对账工具对「窗口内同一 `runId` 出现两次」判 FAIL（"日志可疑"），且**没有对应的放宽开关**。本票窗口内两处：

- `t440-test-new`：`:8` exit=1（13:06:26）与 `:10` exit=0（13:07:03）。声明 `runId=t440-test-new cmd="node --test packages/skill-calorie/test/t440-部位名单一来源.test.mjs"` **绑定 exit=0 那一条**；exit=1 那条属红跑，已在 §七点名。
- `t440-attribution`：`:36`（13:13:04）与 `:40`（13:13:39），两条都是红跑，已在 §七点名。

**性质**：**记账写法问题**（同一件事复用了同一个 `runId`），不是代码或判据缺陷；对账的**声明侧全部命中**（`matched=19/19`、`missing=0`）。**修法（下一次）**：一次运行一个唯一 `runId`，复跑换后缀。

**编排者具名授权（一次性、不类推）**：本票窗口的「重复 runId」FAIL 按记账处理、不作废交付。适用条件＝① 声明侧 `matched` 全命中且 `missing=0`；② 重复的两条均为红跑、已在 §七点名、未被当作门禁证据；③ 本票技术结论已由独立复核用自建现场复现（见 `docs/skills/skill-calorie/t440-复核.md`）。任一条件不成立则本授权作废。

**编排者做此节的说明**：前两席（收口席）连续耗尽上下文未落盘，本节的机器改写与断言由编排者亲手完成（脚本 `.scratch/m158/fix440-audit.mjs`：先内存算→断言全过→才落盘），**结论未经任何人工改写**——全部读数来自 `t440-gate-runs.log` 逐字抄录。
