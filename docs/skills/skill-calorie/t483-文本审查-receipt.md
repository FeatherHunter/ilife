# #483 · 卡路里场景 03 体重「写后回执」9 词文本审查（B4 族证据件）

## 一、票面与范围

- 票号 **#483**（地图 #154 · 场景 03 体重 58 词 · B4「回执与今日盘」族）。**今日盘 2 页归 #487**，本票不碰 `log.ts`。
- 本票 9 条词（HELP 序）：**1 记体重／9 记体重（含备注）／16 补录体重／23 批量补录体重／2 改体重记录／10 改某日体重／17 删体重记录／24 删某日体重／31 批量删体重**。
- 读者画像＝不懂技术的项目负责人。三问照票面：① 删**百分百冗余**的；② 专业话**改人话**；③ 该换展示形式的换形态（只用已有公共件）。
- 改前基线＝`.scratch/t154/text-review/pages/<页>.html.txt`（改前交付包导出）；改后＝本席 `render-family --seqs 1,9,16,23,2,10,17,24,31`。
  逐页差分读数见 `.scratch/t154/text-review/b483/diff9.log`：**9 页全变、273 行变化**（改前 45/34/45/34/45/47/44/47/47 行 → 改后 39/29/39/27/39/36/26/34/34 行）。
  页内 `2026-09-14 00:08:23 → 01:51:17` 这类时间差是渲染时刻（`receipt.meta.actionAt`），不是文案改动，下表不列。
- 截图（真的看过图，未见省略号截断／压字／断行）：`.scratch/t154/text-review/shots-receipt/`（9 张；本席逐图核过 01／17／23 三张改动最大的）。

## 二、写集清单（件 ｜ 改前 LF → 改后 LF ｜ 干了什么）

| 件 | 改前 | 改后 | 干了什么 |
|---|---|---|---|
| `src/weight/fieldLabels.ts`（新建） | — | 25 | 体重域字段→中文标签表（`kg` 体重／`date` 日期／`time` 时间／`note` 备注／`id` 记录编号），登记一次、缺项回退原键名；形态照 `exercise/fieldLabels.ts` |
| `src/weight/plateDocs.ts` | 103 | 152 | 状态卡改三槽各说一件事；新增 `reconcileBlock`（替换共用件 `reconcileDisclosure`）＋ `fieldLabelList`；页脚来源行统一句式（全角冒号＋说人话「体重记录」）；`writtenDetailOf` 四句改写成「接下来怎么办」 |
| `src/weight/logReceipt.ts` | 209 | 206 | 单条页：删「影响行数」卡与「本次体重」卡副说明、字段名换标签、砍掉「量程」与两句定义、图表刻度 `70kg`→`70 kg`、页脚统一；批量页：删整块「批量计数」表、结论句改人话、页脚统一 |
| `src/weight/receipt.ts` | 165 | 171 | 改类页：状态卡合并「影响行数」、结论句去重复、对照表 caption 改人话；删类页：状态卡合并两张计数卡、删整块「删除口径」提示块、「快照」改「删除前的原值」、卡片阵由 2 行并 1 行 |
| `test/weight-receipt-337.test.mjs` | 244 | 257 | 旧断言同步收紧（旧句 0 命中）＋编排者插单的两条读页针（见 §五-7） |
| `docs/skills/skill-calorie/t483-文本审查-receipt.md`（本件） | — | — | 证据件 |

四件源码 LF 全部 < 350（告警线），**台账 `packages/skill-calorie/AGENTS.md` 本票不动**（编排者统一同步）。

## 三、逐页表（改前逐字 → 改后逐字）

### 1 记体重（`01-记体重.html`，28 行变化）

| 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|
| `状态` ／ `已改动` ／ `已写入 weight_log 体重记录` ／ 徽章 `已写入` | `本次写入` ／ `1` `条` ／ `已存入体重记录，可照「看今日体重」复查` ／ 徽章 `已写入` | 冗（同一件事说三遍）＋难（库表名上屏） | 口径 §3.5「同一事实只留信息量最大的一处」；§3.2 `weight_log`→「体重记录」 |
| `本次体重` 卡副说明 `2026-09-14 00:08:23 · 已写入 weight_log` | 删（卡片只剩值槽 `70.5 kg`） | 冗（副标题已有 `已记体重 70.5 kg（BMI 23 · 2026-09-14 00:08:23）`） | 票面已知候选②；口径 §3.5 |
| `影响行数` ／ `1 行` ／ `本次写入的行数` | 并入状态卡：`本次写入` ／ `1` `条` | 冗（标签与说明说同一件事，且与状态卡同一个数） | 口径 §3.2 `影响行数`→「改了 1 条记录」；§3.5 值槽只放短数字＋单位 |
| `写入字段` ／ `4 项` ／ `kg、note、date、time` | `写入字段` ／ `4 项` ／ `体重、备注、日期、时间` | 难（参数名上屏） | 口径 §3.4（走 `shared/fieldLabel.ts` ＋ 本域标签表） |
| `写后现值（较上次＝本次减上一次记录；距目标＝本次减体重目标；窗口 2026-08-16 ~ 2026-09-14）` | `写入后的现值` | 冗（两句定义解释的是自明行名；窗口在页脚来源行） | 口径 §3.2「口径→说明／直接删」；§3.1 窗口归来源行 |
| `+2.5 kg（目标 68 kg · 线在量程外，不画）` | `+2.5 kg（目标 68 kg）` | 难（量程／线在量程外） | 口径 §3.2「量程」条：更好是只留「目标 68 kg，还差 2.4 kg」 |
| 图上刻度 `70kg 70.2kg …` | `70 kg 70.2 kg …` | 形式（数字与单位之间留空格） | 口径 §3.3 |
| `对账信息`：`记录编号 497` ／ `写入时间 …` ／ `回执格式 v1（写库回执）` | 前两行留，`回执格式 v1（写库回执）` 删 | 冗（机器面契约号，读者零收益） | 口径 §3.2 `回执格式 v1（写库回执）`→删（去向登记见 §四） |
| `📊 数据来源:calorie_data.db · weight_log ｜ 本次记录 2026-09-14 ｜ 写入 1 条（影响 1 行）｜ 近 30 天窗 2026-08-16 ~ 2026-09-14` | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 本次记录 2026-09-14 ｜ 写入 1 条 ｜ 近 30 天 2026-08-16 ~ 2026-09-14` | 难（全角冒号／说人话）＋冗（掉重复的「影响 1 行」） | 口径 §3.1 统一句式（58 页同一行） |

### 9 记体重（含备注）（`09-记体重(含备注).html`，28 行变化）

与第 1 条**同一支装配**（`buildLogReceiptDoc`），逐字改动**与上表逐条相同**；本页仅取值不同——`写入字段 4 项` = `体重、备注、日期、时间`、`备注` 行有值 `晨起空腹`、`本次体重` 卡副说明同上删除。无本页独有落点。

### 16 补录体重（`16-补录体重.html`，28 行变化）

与第 1 条**同一支装配**，逐字改动**与第 1 条逐条相同**；本页独有之处只有两点，均已核实无问题：
`本次体重` 卡的值槽照旧 `70.5 kg`（补录不额外标「补录」——场景名与副标题 `记体重 · 回执`／`已记体重 …（2026-09-06 …）` 已说清是哪一天）；窗口 `2026-08-08 ~ 2026-09-06` 照旧随日期滚动（页脚同一行照上表句式统一）。

### 23 批量补录体重（`23-批量补录体重.html`，30 行变化）

| 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|
| `状态` ／ `无改动` ／ `值与改前一致` ／ 徽章 `跳过 1 条` | `本次批量` ／ `1` `条` ／ `这次没有写出任何改动` ／ 徽章 `无改动` | 冗＋形式（值槽只放短数字；徽章只说状态） | 口径 §3.5 |
| `影响行数` ／ `0 行` ／ `本次写入的行数` | 并入状态卡（`本次批量` `1 条`：提交 1 条） | 冗 | 口径 §3.2 `影响行数` 条；§3.5 |
| `写入字段` ／ `0 项` ／ `—` | 删这一格（0 个字段就不出这张卡） | 冗（0 项／— 零信息） | 运动族同口径（`exercise-receipt-fusion-423`：写入字段为空即不出该卡） |
| 整块 `批量计数：写入／跳过／失败` 表（`写入 0 条`／`跳过 1 条（该日已有记录，不覆盖）`／`失败 0 条（日期格式或体重非法）`） | 删整表 | 冗（三数与两句定义在副标题与结论句里已各有一份） | 口径 §3.5；信息去处见 §四 |
| `本次批量 1 条，写入 0 条、跳过 1 条、失败 0 条；跳过＝该日已有记录（不覆盖），失败＝日期格式或体重非法（原因见逐条明细）。` | `本次批量 1 条：写入 0 条、跳过 1 条、失败 0 条。跳过＝那天已经记过（不覆盖旧记录）；失败＝日期格式或体重值不对（原因见下表）。` | 难（`＝` 电报体／「非法」） | 口径 §3.2 人话化 |
| `本次没有逐条明细（命令未带 items）` | `这次没有逐条明细` | 难（实现细节） | 口径 §3.2 |
| 页脚 `… 本次批量 1 条 ｜ 写入 0 条 · 跳过 1 条 · 失败 0 条（影响 0 行）` | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 本次批量 1 条 ｜ 写入 0 条 · 跳过 1 条 · 失败 0 条` | 冗＋难 | 口径 §3.1 |

（`逐条明细（共 1 条）` 表与列头 `日期／体重／状态／原因` 保留、无问题。）

### 2 改体重记录（`02-改体重记录.html`，21 行变化）

| 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|
| `状态` ／ `已改动` ／ `旧值已按新值写回库，可照上表原值再改一次` ／ 徽章 `已改动 1 行` | `本次改动` ／ `1` `条` ／ `已按新值写回体重记录，上表左列就是改前的原值` ／ 徽章 `已改动` | 冗（撤销指引在结论句里已有一遍）＋难（「写回库」） | 口径 §3.5；§3.2「库」类词换人话 |
| `影响行数` ／ `1 行` ／ `本次写入的行数` | 并入状态卡（`本次改动` `1 条`） | 冗 | 口径 §3.2／§3.5 |
| `改动字段` ／ `1 项` ／ `kg` | `改动字段` ／ `1 项` ／ `体重` | 难（参数名上屏） | 口径 §3.4 |
| `改前 → 改后对照（改前取值在写入前回读；体重那一侧写 \`—\` 即本次没改体重）` | `改前 → 改后对照（— 表示这一次没动它）` | 难（「回读」＋反引号字面） | 口径 §3.2 |
| `本次改动 1 条记录、写入 1 个字段；改错可按上表「改前」原值再改一次。` | `本次改动 1 条记录。改错了，照上表「改前」那一列的原值再改一次就行。` | 冗（字段数在卡上）＋难（句读） | 口径 §3.2／§3.5 |
| `对账信息` 第三行 `回执格式 v1（写库回执）` | 删（同上 §1 页） | 冗 | 口径 §3.2 |
| 页脚 `… 本次改动 1 条 ｜ 影响 1 行 ｜ 字段 kg` | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 本次改动 1 条 ｜ 字段 体重` | 冗＋难 | 口径 §3.1／§3.4 |

### 10 改某日体重（`10-改某日体重.html`，23 行变化）

与第 2 条**同一支装配**（`buildUpdateReceiptDoc`），逐字改动**与上表逐条相同**；本页独有一处已核实并处理：**对账信息里的 `记录编号` ／ `未设置` 两行**（按日期定位没有单条编号）→ **删**（机器面的缺值不摆给读者看；编号在对照表 `#494` 里另有）。页脚同样删掉重复的「影响 1 行」。

### 17 删体重记录（`17-删体重记录.html`，37 行变化）

| 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|
| `状态` ／ `已改动` ／ `快照即删除前的取值，行已从库中移除` ／ 徽章 `已删除 1 条 · 不可恢复` | `本次删除` ／ `1` `条` ／ `删除的行已从体重记录里移除，本页没有撤销按钮` ／ 徽章 `已删除` | 冗（值槽「已改动」与徽章「已删除」互相矛盾；「不可恢复」本页说 4 遍）＋难 | 票面候选「删族措辞一页说好几遍」；口径 §3.5 |
| `影响行数` ／ `1 行` ／ `本次删除的行数` ＋ `删除条数` ／ `1 条` ／ `删除前快照的行数` | 两张卡删，条数并入状态卡（`本次删除` `1 条`） | 冗（同一屏同一个「1」说两遍，且各自标签＝说明） | 口径 §3.2／§3.5 |
| `区间变化` 空态 `删除不足两条，区间变化无从计算` ＋ 徽章 `无区间` | `只删了 1 条，看不出区间变化` ＋ 徽章 `没法比` | 难（无从计算／无区间） | 口径 §3.2「无从对照」条的人话口径 |
| `2026-09-08 · 删除后回读（与快照成对）` ＋ 徽章 `已回读` | `2026-09-08 · 删除后重新读到的值` ＋ 徽章 `库里还有记录` | 难（回读／成对） | 口径 §3.2 |
| 表 caption `删除快照（删除前取值，共 1 条）`、列头 `快照`、单元格 `已删除（硬，不可恢复）` | `删除前的原值（共 1 条）`、列头 `状态`、单元格 `已删除` | 冗（整页说 5 遍）＋难（硬删除） | 票面候选；**机器面载荷里的 `已删除（硬，不可恢复）` 一字不动** |
| 整块 `删除口径`／`💡 硬删除，不可恢复`／`删除的行已从库中移除，页面不留撤销按钮（全仓无恢复入口）` | 删整块（`notice`） | 冗（结论句已有一遍）＋难（「全仓」＝实现细节） | 口径 §3.2「口径→说明／直接删」；去处见 §四 |
| 结论 `… 。删除不可恢复，要还原请照上表快照原值重新记。` | `… 。删除不可恢复，要还原请照上表原值重新记一次。` | 难（快照） | 口径 §3.2 |
| 页脚 `… 本次删除 1 条 ｜ 影响 1 行 ｜ 删除后最新 70.4 kg（2026-09-08）` | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 本次删除 1 条 ｜ 删除后最新 70.4 kg（2026-09-08）` | 冗＋难 | 口径 §3.1 |
| 卡片阵：第一行 3 张卡 ＋ 第二行 2 张卡 | 并成一行 3 张卡（`本次删除`／`区间变化`／`删除后最新体重`） | 形式（计数卡删到 1 张后，孤零零一行卡阵不成形） | 票面③「换展示形式」，只用既有 `renderKpiGrid` |

### 24 删某日体重（`24-删某日体重.html`，39 行变化）

与第 17 条**同一支装配**（`buildRemoveReceiptDoc`），逐字改动**与上表逐条相同**；本页独有一处：对账信息 `记录编号`／`未设置` 删（同第 10 条口径）。

### 31 批量删体重（`31-批量删体重.html`，39 行变化）

与第 17 条**同一支装配**，逐字改动**与上表逐条相同**；本页独有一处已核实：快照表里 `体重` 空值渲染为 `—`（`kgOf` 解析不出数值时），本次不动（属数值缺值写法，口径 §3.3 未涉）。对账信息 `记录编号`／`未设置` 同样删。

## 四、删掉的信息登记（不许丢）

| 删掉的东西 | 原位置 | 为什么删 | 去向（还在哪） |
|---|---|---|---|
| `回执格式 v1（写库回执）` | 对账信息第三行（9 页都有，源在 `shared/receiptParts.ts:39`） | 机器面契约号，读者零收益 | 复制载荷／复制日志的版本段（`版本 0.1.0`）与 CLI 信封仍在；体重域现走 `plateDocs.reconcileBlock`（别的域仍走共用件，不属本票写集） |
| `记录编号：未设置` | 对账信息第一行（10／23／24／31 四页） | 机器面缺值，不是给读者看的结论 | 按 id 定位的页（2／17）照旧出 `记录编号`；编号本身也在页内表格（`#494`）里 |
| `影响行数 N 行`（含说明 `本次写入的行数`） | 卡片阵（9 页） | 与状态卡同一个数，说明＝复述标签 | 并入状态卡值槽 `N 条`（页脚也各有一份口径读数） |
| `删除条数 N 条`（含说明 `删除前快照的行数`） | 卡片阵（17／24／31） | 与 `影响行数` 是同一个数，同屏说两遍 | 状态卡值槽 `N 条` ＋ 表 caption `删除前的原值（共 N 条）` ＋ 结论句 `本次删除 N 条` |
| `批量计数` 表（写入／跳过／失败三行及两句定义） | 第 23 页表格块 | 三数与两句定义在副标题与结论句里各有一份，整块冗余 | 副标题（`log.ts` 机器面摘要）＋结论句 `本次批量 N 条：写入 …`；逐条原因仍在 `逐条明细` 表 |
| `删除口径` 提示块（`硬删除，不可恢复` ＋ `删除的行已从库中移除，页面不留撤销按钮（全仓无恢复入口）`） | 17／24／31 页（`notice`） | 结论句已说一遍「删除不可恢复」；「全仓」是实现细节 | ① 行已移除 → 状态卡副说明 `删除的行已从体重记录里移除，本页没有撤销按钮`；② 不可恢复 → 结论句；③ 撤销／还原指引 → 结论句 `要还原请照上表原值重新记一次` |
| `快照` 一词（表 caption／列头／状态卡副说明／结论句） | 17／24／31 | 「快照」是技术话，读者看不懂 | 改说「删除前的原值」；机器面载荷仍写 `已删除（硬，不可恢复）`（一字不动） |
| `线在量程外，不画` | 第 1／9／16 页「距目标差」行 | 说的是画图实现，读者要的是差距 | 目标值仍在括号里（`（目标 68 kg）`）；结论句仍给 `距目标 +2.5 kg` |
| `单点无均值对照`／`单点无变化` | 第 1／9／16 页表格值槽 | 「单点／对照」是统计话 | 改 `（只有一条记录）`；口径 §3.2「n=1→只有一天」同一族 |
| 参数名 `kg、note、date、time`／`kg` | 写入字段卡／改动字段卡／页脚字段位／对照表空态 | 参数名上屏（口径 §3.4：命中必须为 0） | `weight/fieldLabels.ts` 中文标签 |
| `本次体重` 卡副说明 `时间 · 已写入 weight_log` | 第 1／9／16 页 | 副标题已有同一时间与同一结果 | 时间仍在副标题；去向在状态卡副说明 |

## 五、机器读数

**出页**（`node .scratch/t154/text-review/render-family.mjs --seqs 1,9,16,23,2,10,17,24,31 --out .scratch/t154/text-review/out-receipt`）：
`TALLY {"通过":9} TOTAL 9 HTML=9` ＋ 末行 `OK-RENDER D:\ilife\.scratch\t154\text-review\out-receipt`；
导出 9 条，字节：01=74729、02=65354、09=74852、10=65254、16=72803、17=66291、23=65490、24=66205、31=66258（合计 **617236**）。
差分：`SAME-PAGES 0/9 ；CHANGED-PAGES 9 ；DELTA-LINES 273`（`.scratch/t154/text-review/b483/diff9.log`）。
截图：9 张落 `.scratch/t154/text-review/shots-receipt/`（`SHOT 9 files`），本席看图的 3 张见 §一。

**用例**（`node --test packages/skill-calorie/test/weight-receipt-337.test.mjs`）：
`ℹ tests 11 ／ ℹ pass 11 ／ ℹ fail 0`；`RESULT: ticket=483 runId=7f8cf5af-aadc-4e56-9541-7254317f86b0 waitedMs=30019 exit=0`。

**变异自证两行**（改坏一处＝第 1 页「写入字段」卡回到 `receipt.writtenFields.join('、')`）：
- 红：`AssertionError [ERR_ASSERTION]: 记体重 仍含 #483 已删文本：kg、note、date、time`；`ℹ pass 0 ／ ℹ fail 1`；`RESULT: ticket=483 runId=7ab2920d-3794-4fd1-b232-30531831a3ae waitedMs=0 exit=1`
- 还原绿：`ℹ pass 1 ／ ℹ fail 0`；`RESULT: ticket=483 runId=e9fb8031-379d-4c14-8120-3d1b41b7e872 waitedMs=0 exit=0`

**编译（持锁）**：`npx tsc -b packages/skill-calorie` → `runId=53b2c8d2-e8cc-4944-a4aa-250f41dc4ede exit=0`（首轮）、`runId=f0cbf134-d65f-425a-a078-22f2fa07911d exit=0`（变异轮）、`runId=2355bd74-535d-45e8-815b-564758fcdf84 exit=0`（还原轮）。

**GATE-RUN（逐条对账源＝`.scratch/locks/gate-runs.log`，本票 9 条 RUN 全声明）**

- GATE-RUN runId=53b2c8d2-e8cc-4944-a4aa-250f41dc4ede cmd="npx tsc -b packages/skill-calorie"
- GATE-RUN runId=f2429c68-84bc-4f02-b956-4c65edb99376 cmd="node --test packages/skill-calorie/test/weight-receipt-337.test.mjs"
- GATE-RUN runId=9a3aa08e-9497-4362-84c5-fcfd99a58759 cmd="node --test packages/skill-calorie/test/weight-receipt-337.test.mjs"
- GATE-RUN runId=95c474da-437b-494d-b26c-8f738b7ef4b9 cmd="node --test packages/skill-calorie/test/weight-receipt-337.test.mjs"
- GATE-RUN runId=7f8cf5af-aadc-4e56-9541-7254317f86b0 cmd="node --test packages/skill-calorie/test/weight-receipt-337.test.mjs"
- GATE-RUN runId=f0cbf134-d65f-425a-a078-22f2fa07911d cmd="npx tsc -b packages/skill-calorie"
- GATE-RUN runId=7ab2920d-3794-4fd1-b232-30531831a3ae cmd="node --test --test-name-pattern=记体重$ packages/skill-calorie/test/weight-receipt-337.test.mjs"
- GATE-RUN runId=2355bd74-535d-45e8-815b-564758fcdf84 cmd="npx tsc -b packages/skill-calorie"
- GATE-RUN runId=e9fb8031-379d-4c14-8120-3d1b41b7e872 cmd="node --test --test-name-pattern=记体重$ packages/skill-calorie/test/weight-receipt-337.test.mjs"

GATE-RELAX flag=--allow-nonzero reason=前三条测试运行（f2429c68／9a3aa08e／95c474da）是「断言收紧前」的红读数、7ab2920d 是票面要求的变异红读数，都是本票交付过程的一部分，必须逐条声明，故对账时放宽 exit 口径。

**门禁对账自跑**：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t483-文本审查-receipt.md --ticket 483 --allow-nonzero`
→ `RESULT: matched=9/9 auditEntries=4700 scoped=9 undeclared=0` ＋ `gate-audit: PASS`。

**提交与推送**：提交 `473dd15a23240483ed26c9a0fe788974b8ee2c88`（`feat(483): …`，按路径 6 件：4 源码 ＋ 1 测试 ＋ 本证据件）；
推送 `a150df9..473dd15  master -> master`（`git push` 持锁 runId=ed35c152-a5d6-4e6f-b0f0-d8ef5cb4d316 exit=0`）。
落盘卫生：6 件均 UTF-8 无 BOM、无 CRLF、无字面 `\n`（`.scratch/t154/text-review/b483/check-clean.py` 读数）。

## 六、未做项与下一手缺什么

1. **`edit.ts` 的 5 处摘要串未改**（`R(...)` 第 3 参）：`已删除体重 #1（… · 硬删除，不可恢复）`／`已删除 2026-09-06 体重 1 条（硬删除，不可恢复）`／`批量删体重 …（硬删除，不可恢复）`。它们**同时是 CLI 信封 `data.message`（机器面）与页面副标题（可见文本）**——票面「机器面 payload 一字不动」，故本票未动。结果：可见文本里该措辞由原来 5 处收到 **2 处**（副标题＋结论句）。要再收需要跨族裁定（要么副标题与机器消息解耦，要么口径改写「摘要一律不算可见文本」）。
2. **共用件 `shared/receiptParts.ts` 的 `reconcileDisclosure` 仍带 `回执格式 vN（写库回执）`**：本票只在体重域用 `plateDocs.reconcileBlock` 换掉它，别的域（照片／基础信息／训练／饮食）页面照旧。要全局删得动共用件，那不在本票写集。
3. **`edit.ts` 的 `fail()` 错误文案**（`kg 须为 0..500`／`缺参数 id/date/start+end（三选一）`／`start 不得晚于 end`）：它们是**失败回执页**的可见文本且含参数名，但不在这 9 条成功回执的页面里，本票未动（下一手：失败回执族一并过，或单独一票）。
4. **眉标 `体重 · 写后回执`**：口径 §四本轮明确不动（等用户裁定），保持原样。
5. **第 23 页副标题**（`批量记体重：写入 0，跳过 1，失败 0`）住 `log.ts`（#487 地盘）未动；本票删掉「批量计数」表后，三数在副标题与结论句各一份。
6. **差分脚本 `.scratch/t154/text-review/diff-pages.py` 的 delta 统计有 bug**：`d[:3] in ('-', '+')` 恒为 False（大 3 字符切片的成员判断），所以它**永远**打 `SAME-PAGES 9/9 ；DELTA-LINES 0`——虽然逐行仍会打印。本席改用自持 `.scratch/t154/text-review/b483/diff9.py`（同口径，只把那一行改成 `d[:1]`）。建议编排者修它（或让别席照改），否则按 `SAME-PAGES` 会误判「无变化」。
7. **编排者插单（#487 连带的两条读页针，已并入本票提交）**：`assertDashboard(r, what)` 的 `'今日体重'` 改成第三参 `firstCard`（`看今日体重` 传 `'今日体重'`、`看体重总览` 传 `'最新体重'`）；同页另一条过时针 `'体重盘'` 同步改成 `'体重总览'`（盘名已从可见文本撤掉）。**收紧不是放宽**：两条都还钉一个真实存在的页面字面。
8. 未跑全仓门（`pnpm test`／全包测试）：本票写集只有体重域 4 件 ＋ 1 个测试件，按工作流只跑靶向用例；别席在途件（`log.ts`／`history.ts`／`compare.ts`／`volatility*.ts` 等）与我的改动**同处一个包**，全仓门此刻的信号不干净（本席实测：`看体重总览` 的 `'体重盘'` 针就是被 #487 的在途改动打红的）。


---

## 六、对抗审查整改（2026-09-15）

独立审查席判 **FAIL 78.5 分（含两条 S1）**。整改席完成源码改动后因后端故障中断，**由编排者接手提交并复核**
（另派过一名收尾席，同样因后端故障中断）。

| 缺陷 | 严重度 | 整改后 |
|---|---|---|
| 卡副说明「上表左列就是改前的原值」——该表左列是「记录」，`改前 → 改后` 在第二列 | **S1** | 改指「改前」那一列 |
| 删除快照表用 `—` 顶替被删记录的体重（同页别处写着 `70.4 kg`，结论还教读者照表重记） | **S1** | 把真实体重补回去 |
| 对账信息只剩 `写入时间`，唯一标识消失（与复制日志 `id=n/a` 呼应） | S2 | 恢复「记录编号」行；批量页写「本次不适用」 |
| `写入时间` 与记录日期同屏易读错 | S2 | `落库时间` |
| 标题 `2026-09-06~2026-09-06` 同一天写两遍 | S2 | `已删除 2026-09-06 当天体重 1 条` |
| 原因列 `已有记录 70.4kg` 缺空格 | S2 | `70.4 kg` |
| `删除后最新体重 …` 同屏四处 | S2 | 副说明压成 `2026-09-08（删除后重新读到的值）`；删「库里还有记录」徽章 |
| 值槽 `本次批量｜1 条` 与「写入 0」并列易误读 | S2 | 值槽 `投入 1 条` |
| 同页并存 `#1` 与列头 `编号` | S3 | 统一「记录编号」（跨族裁定 G） |

**未做**：整改单要求的两针（对账信息键集 ≥2／复制载荷数字 ⊇ 页面数字）**未落成用例**——两任执行席都因后端故障中断。
登记为遗留：见本件末。**裁定 F**：页脚去库表名（本族 `plateDocs.ts` 已改）。

**读数**：`weight-receipt-337` 全绿；六族关键词回扫 32 词全 0（含 `上表左列`／`回执格式`／`库里还有记录`）；
出页 `TALLY {"通过":9}`。提交：`682ac1e`。

---

## 七、形状化与手机端（#505，2026-09-15）

**票**：#505「场景 03 体重回执＋今日盘 11 页 去分隔符形状化 ＋ 手机端 820 端适配」。
本节只记**回执 9 页**（第 1／9／16／23／2／10／17／24／31 条）；今日盘 2 页记在 `t487-文本审查-plate.md` 的同名节。
口径正本 `.scratch/t154/text-review/口径-UI.md`；形状词汇 `src/weight/weightUi.ts`（编排者已入库 `992aa9e`，本票只用不改）。

### ① 逐页「原来串什么 → 改成什么形状」

| 页 | 原来（`·`／`；` 串） | 改成 |
|---|---|---|
| 01／09／16（记体重 3 条） | 副标题 `已记体重 70.5 kg（BMI 23 · 2026-09-15 08:43:23）` | 副标题只写 `已记体重 70.5 kg`；BMI 进结论块事实条 `BMI　23`；时刻进判语 `本次记 70.5 kg，2026-09-15 08:43:23。` |
| 01／09／16 | 结论句四件 `；` 串（本次／较上次／距目标／近 30 天） | `shapedConclusionBlock(verdict(一句判语) + factStrip([较上次／距目标／近 30 天]) + BMI 一枚)` |
| 01／09／16 | 「写入后的现值」表 `距目标差` 格 `未设体重目标 · 说「定体重目标」后可看差距` | 拆两句人话 `未设体重目标，说「定体重目标」后可看差距`（表格单元格吃纯文本，形状进不来） |
| 23（批量补录） | 结论 `本次批量 3 条：写入 1 条、跳过 1 条、失败 1 条。跳过＝…；失败＝…` | `verdict(三数一句判语)` ＋ `bulletList([跳过＝…, 失败＝…])`（两条定义各成一行） |
| 23 | 页脚来源句 `… ｜ 写入 0 条 · 跳过 1 条 · 失败 0 条` | 同族同形：`… ｜ 写入 0 条 ｜ 跳过 1 条 ｜ 失败 0 条`（`｜` 是允许项） |
| 02／10（改体重 2 条） | 状态卡副说明 `已按新值写回体重记录；上表「改前」那一列就是原值` | 改两句人话＋指列改名：`已按新值写回体重记录。要改回去，照表里「改前」那一列的原值再改一次。`（本页有两张表，「上表」要靠猜） |
| 02／10 | 结论句 `本次改动 1 条记录；有 N 条只改了备注…。改错了…` | `verdict('本次改动 1 条记录。')` ＋（只改备注的行存在时）`factStrip([只改备注的　N 条（体重没动，下表写 —）])`；「改错了怎么办」住状态卡副说明，不再复述 |
| 17／24／31（删 3 条） | 结论句 `本次删除 1 条；删除后最新体重 70.4 kg（2026-09-08）。…` | `verdict('本次删除 1 条。删除不可恢复，要还原请照上表原值重新记一次。')` ＋ `factStrip([删后最新体重　70.4 kg])` |
| 17／24／31 | 状态卡副说明 `删除的行已从体重记录里移除，本页没有撤销按钮` | 两句人话：`删除的行已从体重记录里移除。本页没有撤销按钮。` |
| 17 | 卡副说明 `2026-09-08 · 删除后重新读到的值` | `2026-09-08（重新读到的值）`（日期 ＋ 括注，一个分隔符也不留） |
| 17／24／31 | 副标题照抄回执摘要（含 `· 硬删除，不可恢复`） | 可见副标题把那个 `·` 落掉（`plateDocs.visibleSubtitleOf` 只删这一个字符）；**摘要本体（信封 `message`／复制载荷）一字不动** |
| 全部 9 页 | 页题 `记体重 · 回执` | `记体重回执`（`·` 是把「做什么 ＋ 这是一张回执」串成一句符号；眉标已整族删，页题不必再挂一次） |

**形状入口分家（本票实测出来的坑）**：`plateDocs.conclusionBlock(sentence)` 的契约是「正文只转义」
（#483 定稿，锁死一句话结论）。本票第一版把 `verdict()`＋`factStrip()` 的 HTML 交给它 ⇒ 整段被转义、
页面上原样印出 `<p class="wui-verdict">…`（桌面截图肉眼即见）。故另立同形态入口
`shapedConclusionBlock(bodyHtml)`（同一个折叠区形态、同一个默认展开，收的才是已成形 HTML），
并在用例里钉住 `&lt;(p|div|span|ul) class=&quot;wui-` 命中 0。另一个同类坑：KPI 卡的 `detail` 槽
由 `renderKpiGrid` → `esc()` 转义，形状进不去（今日盘那两卡因此把事实改走结论块，见 t487 §七）。

### ② 删掉的信息登记

**零**（逐条登记在上面那张表的右列）。三处**措辞**收紧（事实未丢）：
① 改类「上表左列」→「表里「改前」那一列」（指列更准）；② 批量页脚 `·` 串改 `｜` 串（同一组数）；
③ 副标题删掉的那个 `·` 只是分类符（两半各是完整的一句）。

### ③ 分隔符回扫读数（逐页）

| 页 | 改前 | 改后：页题· | 机器面· | **正文·** |
|---|---|---|---|---|
| 01-记体重 | 6 | 1 | 10 | **0** |
| 09-记体重(含备注) | 6 | 1 | 10 | **0** |
| 16-补录体重 | 6 | 1 | 10 | **0** |
| 23-批量补录体重 | 5 | 1 | 10 | **0** |
| 02-改体重记录 | 3 | 1 | 10 | **0** |
| 10-改某日体重 | 3 | 1 | 10 | **0** |
| 17-删体重记录 | 4 | 1 | 10 | **0** |
| 24-删某日体重 | 3 | 1 | 10 | **0** |
| 31-批量删体重 | 3 | 1 | 10 | **0** |
| （＋今日盘 2 页） | 13 | 1 | 10 | **0** |

11 页合计 52 → **11 处全部落允许项**（页题 1 ＋ 机器面 10/页），**正文 0/11**。工具：
`python .scratch/t154/text-review/scan-separators.py --dir .scratch/t154/text-review/ui483/out`
（交付包口径）＋ 本席 `.scratch/t154/text-review/ui483/scan-zones.py`（三区分解，判据＝正文区必须 0）。

### ④ 手机端探针读数

- `mobile-frame.mjs shoot`（真 390 iframe 壳）：第 1 条 `mobile/01-记体重.png` 63,276 B，
  已 `read_image` 实看——KPI 两列、表在窄屏收字号不断字、结论块判语 ＋ 两条事实（`较上次 +0.1 kg`／`距目标 +2.5 kg`／
  `近 30 天 均值 70.3 kg，趋势平稳`／`BMI 23`）、复制按钮两枚各占半宽、页脚来源句两行折行，无溢出。
- **溢出探针**：共享的 `mobile-frame.mjs probe` 在本机读不到读数（实测 `WRAP-TITLE=等待探针…`）；
  本席另写 `.scratch/t154/text-review/ui483/probe-390.mjs`（口径逐条照搬，读数落点换成 iframe 直接写
  `parent.document.title`），读数 **`SW=390 VW=390 || 无溢出`**（第 1／8／17／23／31 五页同值）。
- 820 段：`weightUi.ts` 的件窄屏塌纵向（事实条一列、判语胶囊行一列）；共享 640 段管共享件（表在窄屏
  由 `th{white-space:normal}` ＋ `td{overflow-wrap:anywhere}` 自行收纳）。表形态**保留表格**（两列表＝项／值），
  390 实测不压扁、不横滚。

### ⑤ 用例与变异读数

- 用例（持锁）：`node tooling/run-locked.mjs --ticket 505 -- node --test packages/skill-calorie/test/weight-receipt-337.test.mjs`
  → `ℹ tests 11 / pass 11 / fail 0`；`runId=690b767e-5328-48fb-80cf-0c6569ab0d31`（exit 0）。
- 同步收紧（旧句过时即改，不放宽）：`assertReceipt` 加 `visibleBody()` 正文零 `·`／`；` 判据（削 `<title>`／
  样式／脚本／复制区／`data-*` 属性后再扫）＋ 页题无 `·` ＋ `wui-verdict` 在位 ＋ 形状未被转义；
  批量页那条过时断言 `本次批量 3 条：写入 1 条、跳过 1 条、失败 1 条` 改成新句并加钉 `wui-bullets` 两个 `<li>`。
- 变异两行：`logReceipt.ts` 的 `距目标差` 那格改回 `未设体重目标 · 说…` → `tsc` exit 0、用例 **exit 1**
  （3 例红，`正文里仍有 ·：…`），`runId=42f2c12c-5b57-4d4f-8608-91153dc7f334`；改回 → 全绿 `runId=690b767e-…`。

### ⑥ 未做项与下一手缺什么

1. **页题 `<title>` 的 `·` 未动**（见 t487 §七.⑥.1，同一件事）：它是全仓同形的文档名，改它跨族；
   请编排者裁定（补进允许项／另开一票），本票不单开先例。
2. **共享探针 `mobile-frame.mjs probe` 在本机读不到读数**：本席旁路未改它；建议按 `probe-390.mjs` 的落点
   （直接写 `parent.document.title`）修一处，四席复用。
3. **`log.ts` 越过 350 告警线**（337→382）：已按必报五步第四步挂进本包台账（超因 ＋ 拆法＝读面／写面切
   `weight/logWrite.ts`）；本票不拆（不在写集）。
4. **第 17／24／31 条的副标题仍照抄回执摘要**（只落掉 `·`）：摘要与可见副标题**同源**是 #483 的裁定
   （缺陷 5 那页也照它做），彻底解耦要动 `edit.ts` 的摘要串（跨族、`t483` 已登记为遗留）。


## 八、设计视角审查整改（#510，2026-09-16）

**本族整改完成**（逐条「改了／为什么没改」见下）。依据：整改单 `.scratch/t154/text-review/fix-brief-uireview.md`
（八节）＋审查席回执与四份读数 `.scratch/t154/text-review/rev-ui/`。本席出页与读数在
`.scratch/t154/text-review/rev-fix/`（`out-<族>`／`out-all`／`*.log`／`probe-fix-readings.py`／`readings-fix.txt`）。

### 八.1 第二节四类硬读数（复跑）

| 类 | 整改前（审查席实测） | 整改后（本席复跑） | 判据 |
|---|---|---|---|
| ① 硬编码色值 | 页内件面 6 处自造：`#e7f8ee/#1a7a3a`、`#fff4e5/#a05a00`、`#ffeceb/#c0392b` | 页内件面只剩 `#fff5e0`／`#a25b00`——**逐值等于冻结正本** `base-render/src/style.ts` 的 `statusBadge` warn；`ok`／`danger` 两档连类带值删（58 页里 0 页用过＝死 CSS，`chip()` 类型同步收到 `warn`／`plain`） | PASS |
| ② 字号阶梯 | 5 档 `{12, 12.5, 13, 14.5, 15}px` | **3 档**：`12px:456`／`13px:228`／`15px:171`（`.wui-bullets li` 12.5→12、手机档 `.wui-verdict` 14.5→15） | PASS |
| ③ 死 CSS | `wui-strip-note`／`wui-strip-v`／`wui-chip-ok`／`wui-chip-danger` 各 0 页 | 声明 22 个 `wui-*` 类、**0 页用到 0 个**：`-note` 2 页、`-v` 17 页真用上，`-ok`／`-danger` 删 | PASS |
| ④ 同屏事实 ≥3 | **51/58 页** | **7/57 页**（第 48 条归 #436 未纳入本批；残留 7 页全在写集外的 `logReceipt.ts`／`receipt.ts`） | 本族 0 页 |

复跑命令（口径与审查席四脚本同源，源目录换 `PROBE_DIR`）：

```
python .scratch/t154/text-review/rev-fix/probe-redundancy.py     # → .scratch/t154/text-review/rev-fix/redundancy-after.txt（等价于 rev-ui/probe-redundancy.py）
python .scratch/t154/text-review/rev-fix/probe-css.py            # → .scratch/t154/text-review/rev-fix/css-after.txt
python .scratch/t154/text-review/rev-fix/probe-fix-readings.py   # → .scratch/t154/text-review/rev-fix/readings-fix.txt（色值／字号／死 CSS／形状槽顿号四类一次出）
```

其余三面读数（审查席已判达标、本轮未推翻）：间距页内件面 12 档无孤值｜圆角 2 档＋零 box-shadow｜
手机 390 真视口 **57/57 页 `SW=390 VW=390`**（`.scratch/t154/text-review/rev-fix/mobile-probe.log`）。

### 八.2 本族（回执族 9 页：01/09/16/23/02/10/17/24/31）逐条 —— **未改，写集外**

整改单第五节要求「照回执表逐条改」，但本族这 9 页的正文出自 **`weight/logReceipt.ts`**（01／09／16／23）
与 **`weight/receipt.ts`**（02／10／17／24／31）——**两件都不在本票（#510）写集**（写集＝`weightUi.ts` ＋
`history.ts`／`compare.ts`／`review.ts`／`volatilityDoc.ts`／`log.ts`／`plateDocs.ts` 六件），本席不越界改它们，
逐条如实记账，供编排者派下一手（两件同属 #505 的落点）：

| 缺陷 | 页 | 现状（本席复跑） | 修法（下一手照抄） |
|---|---|---|---|
| S3 顿号漏网 | 01／09／16 | 卡副行仍印「体重、备注、日期、时间」（3 处／3 页，`probe-fix-readings.py` ④ 段点名） | 该槽吃纯文本 ⇒ 改写成一句话（或把四个字段名逐条落 `bulletList()`） |
| S2 关键数字三处 | 01／09／16 | `70.5` ×3（副标题 ＋ KPI 卡·值 ＋ 判语块） | 判语块撤掉那个数（判语只说「记下了／什么时候」） |
| S2 条数三～四处 | 17／24／31 | `1 条` ×3～4（KPI 卡·副行 ＋ 表题 ＋ 判语块，24／31 另有副标题） | 卡副行与判语块各撤一次，条数收到「值槽 ＋ 表题」两处 |
| S2 条数三处 | 23（批量补录） | `1 条` ×3（表题 ＋ 判语块 ×2） | 判语块只说跳过／失败那两条规则（`bulletList()` 已在），条数收表题一处 |
| S2「标签 ＋ 说明」同字号 | 01 | 参上（同一型：标签那半与说明那半同字号同色） | 走 `factStrip(…, asNote=true)`（形状已有，改一处调用即可） |

本票已把**形状与样式层**备齐（`weightUi.ts` 的 `asNote` 两半分两档、`wui-strip-note` 已实载），
本族只差这两件里的调用点改写——**预估一处一行**。

### 八.2b 已落地（收尾席，同票 #510 第二手）

上表五条**已逐条落地**（写集＝`weight/logReceipt.ts`＋`weight/receipt.ts`＋`test/weight-receipt-337.test.mjs`）：
改了 **① 01／09／16 判语撤掉本次体重那个数 ② 01／09／16 卡副行「体重、备注、日期、时间」→「体重 与 备注
与 日期 与 时间」（副行吃纯文本，照审查单第二种改法改写成一句话） ③ 17／24／31 判语撤条数／撤方向词／
撤首末对＋区间变化卡副行改说「只有一份原值行」 ④ 23 判语只说这张表怎么看（三数归副标题与表题）
⑤ 24／31 表题撤条数改「（逐条）」**——第 ⑤ 条与上表写的「条数收值槽＋表题」差一处理由：那两页副标题
本身就是机器面摘要 `…体重 1 条…`，副标题＋值槽＋表题＝3 处到不了 ≤2，故表题让位（17 那页副标题不带
条数，表题照上表保留条数）。删掉的每一条事实的去处逐条登记在证据件
`.scratch/t154/text-review/rev-fix2/证据-收敛.md`；读数：7 页「同一事实摘要面次数」最高值
**4 → 2**（逐页 2／0／2／2／2／2／0／2／2，超 2 次页数 0/9），出页 `TALLY {"通过":9}`。

### 八.3 自证

- 持锁编译：`node tooling/run-locked.mjs --ticket 510 -- npx tsc -b packages/skill-calorie` → **exit=0**
  （`.scratch/t154/text-review/rev-fix/tsc-2.log`）。
- 出页六族全 `OK`：history 18／compare 17／review 6／volatility 5／plate 2／receipt 11（含 08／30 复用）
  ——`TALLY {"通过":n}` 且 `OK-RENDER`，日志 `.scratch/t154/text-review/rev-fix/render-*.log`。
- 用例（持锁）：五件体重族测试 **43/43 通过**（`tests 43 / pass 43 / fail 0`，`.scratch/t154/text-review/rev-fix/tests-final.log`；
  其中 `weight-history-333` 自打 `RESULT: 18/18`）。旧断言随新文案同步收紧（h1、徽章、里程碑副说明、
  单日窗副标题、期间事实条、卡②徽章与指路牌、今日盘形状件与卡网格）。
- **变异两行**：把 `history.ts` 的节奏／首末事实条改回 `·` 串 ⇒ 必红
  （`runId=0270d68d-6937-4f6d-8de4-cc1aaa0b579a`，`fail 1`：`看上周体重 可见面 · 命中数不对`）；
  改回 ⇒ 必绿（`runId=800175d3-1eec-43a3-8484-38a5ec5dca23`，`RESULT: 18/18`／`pass 3`）。
  日志 `.scratch/t154/text-review/rev-fix/mut-red.log`／`.scratch/t154/text-review/rev-fix/mut-green.log`。
- 手机 390 真视口：`node .scratch/t154/text-review/mobile-frame.mjs probe 390 2400 .scratch/t154/text-review/rev-fix/out-all` →
  **57/57 页 `SW=390 VW=390`**；截图两档 `.scratch/t154/text-review/rev-fix/shots-desktop/*.png`（1200）与
  `.scratch/t154/text-review/mobile/*.png`（390），审查席点名的 04／08／22／30／45／47／53 逐张看过。
- **告警线台账**：`packages/skill-calorie/AGENTS.md` 台账里本票六件的「当场实测」列已随实况（别席同步器在场）；
  本席复跑 `node packages/skill-calorie/scripts/check-warning-line.mjs` 的残余红只剩
  `src/render/trendDocs.ts 台账=994 实况=996`（**别席在途件**，不在本票写集，不代改）。
- 未动（照整改单第八节）：命令与路由／`shared/*`／`packages/base-render/*`（共享层一行未碰——间距 16 档、
  字号 18 档、数据表 1200 下两列相距 ~1000px、图表末点标签压均值虚线／`70.4kg` 少空格，均归 #424）／眉标／
  复制区形态与深色瞬时 toast／第 48 条整页（归 #436）／`.scratch/t154/delivery/`（只有编排者重出交付包）。
- **写集外残留（本票不改，供编排者派下一手）**：`weight/logReceipt.ts` 与 `weight/receipt.ts` 两件里的
  7 页 ★（01／09／16／17／23／24／31）与 3 处顿号——见 `t483-文本审查-receipt.md` 八.2 的逐条表。
