# t475 · 版式针补硬（承接 #465 审查 D1／D2／D4）

日期 2026-09-14 · 承接票 #475（来源：`docs/skills/skill-calorie/t465-review-报告.md` 缺陷段 D1／D2／D4）。
写集＝两件判据件（`packages/skill-calorie/test/exercise-port-111.test.mjs`、`.../exercise-routes-265.test.mjs`）＋本件；
本席 `git status` 只有这三件——**零实现件写入**（`sportPortDocs.ts`／`reviewDocs.ts` 一行未碰）。

## 一、隔离镜像与字节读数（变异一律在镜像上做，真件零写入）

镜像＝`.scratch/t475/mirror/packages/skill-calorie/`：`dist/` 真包副本 ＋ `test/` 真判据件副本 ＋
`node_modules` junction（只借解析路径，不复制依赖）。

- **整包字节同源**：真 `dist` vs 镜像 `dist` 逐字节同值 **1241／1241**（不同值 0／镜像多出 0／缺 0）；
- 两份判据件与仓内同字节（sha256 前 16 位：111 `e241748154bed7ec`／265 `2514eb67fd688821`）；
- **基线从 git 对象取字节**：`git cat-file blob HEAD:<路径>` 与工作区**逐字节同源**（111 `9975b488…`／265 `b091e946…`）；
- 变异件 `dist/render/sportPortDocs.js` 开工与收尾同值 `8194f7b804afe17d…`；收尾 `FINAL-PARITY` 仍 **1241／1241**
  （开工后他席重编漂走 `photo/photo.js` 与其 `.map` 两件，非本席写入）；
- 每条变异都**按另存的原字节写回并刷 mtime**，写回后 sha256 逐条自证＝变异前（报告逐条打「逐字节同源=true」）。

复算：`node .scratch/t475/mutate.mjs` → `.scratch/t475/mutate-report.txt`（基线两行 ＋ 12 条变异）。

## 二、逐条「旧断言 → 新断言 → 依据 → 改坏必红读数」

「旧」＝#465 版当刻字面；「新」＝本票加在旧条**之后**的第二条（旧条一条不删、不弱化，故不存在判据放宽）。
「依据」一律当刻可复算：写清读产物里的**哪个节点／属性**，并给复算命令。

| # | 针 | 旧断言 | 新断言（本票加） | 依据（当刻可复算） | 改坏必红读数（镜像实测原字） |
|---|---|---|---|---|---|
| ② | 力量页题面 | `out.html.includes('力量训练总览')` | `assert.equal(pageTitle(out.html), '力量训练总览')` | 页题节点 `<h1 class="ilife-block-page-shell-title">` 当刻逐字＝`力量训练总览`（`node .scratch/t475/probe3.mjs`） | **M5** 12/1：`strength 页题不是逐字页名（连写了窗口？）："力量训练总览 2026-09-05 ~ 2026-09-07"`（旧条同档仍绿） |
| ② | 力量页窗口 | `out.html.includes('2026-09-05 ~ 2026-09-07')` | `assert.ok(窗口卡两格 ＋ sec-table caption 各逐字)` | 窗口串全文 **2** 处：KPI 卡 detail ＋ `sec-table` caption；窗口卡是 `renderParamForm` 两格 `name="start"/"end"` | **M7** 12/1：`strength 窗口未写在自己的落点（窗口卡两格／聚合表 caption）："按动作聚合｜单侧口径 Σkg×次数…"`（KPI 那份不代跑） |
| ③ | 有氧页题面 | `includes('有氧训练总览')` | 同 ② 口径 | 同上（有氧页落点同形） | **M6** 12/1：`cardio 页题不是逐字页名（连写了窗口？）："有氧训练总览 2026-09-05 ~ 2026-09-07"` |
| ③ | 有氧页窗口 | `includes('2026-09-05 ~ 2026-09-07')` | 同 ② 口径 | 窗口串 **2** 处：KPI 卡 detail ＋ `sec-table` caption（`按类型聚合（…）`） | **M8** 12/1：`cardio 窗口未写在自己的落点（窗口卡两格／聚合表 caption）："按类型聚合；缺一格显「—」不空着"` |
| ④ | 分布页题面 | `includes('运动类型分布')` | `assert.equal(pageTitle(out.html), '运动类型分布')` | 页题节点当刻逐字＝`运动类型分布`（**不**连写窗口） | **M1**：111 `12/1` `distribution 页题不是逐字页名（连写了窗口？）："运动类型分布 2026-09-05 ~ 2026-09-07"`；265 `2/1` `看运动类型分布 页题节点不是本页题面："运动类型分布 2026-09-08 ~ 2026-09-14"` |
| ④ | 分布页窗口 | `includes('2026-09-05 ~ 2026-09-07')` | 同 ② 口径 | 窗口串全文**仅 1 处**、住明细表 caption；窗口卡两格**不产** `A ~ B` | **M2** 12/1：`distribution 窗口未写在自己的落点（窗口卡两格／明细表 caption）："分类明细（2026-09-05 ~ 2026-09-07，末行为合计）"`（旧条在这条**仍绿**——摘窗口卡它抓不住） |
| ⑤ | 复盘页块名 | `includes('高频运动')` | `assert.equal(<a href="#sec-badges"> 那一条的文本, '高频运动')` | 块名＝页内导航里 `#sec-badges` 那一条；页内副题同字，故旧条换字样不红 | **M3** 12/1：`recap 块名不是逐字「高频运动」："高频 TOP5"`（副题那份仍命中，只有新条红） |
| — | 力量页来源 | 无（#465 没有这一针） | `assert.equal(sourceText(out.html, 'sec-source'), '数据来源 · exercise_log（本窗未删除的力量行） · 2026-09-05 → 2026-09-07 · 共 2 条')` | `sec-source` 段里 `ilife-block-caliber` 的逐字文本；`exercise_log` 属 #465 §6.2 已登记的可见面例外 | **M4** 12/1：`strength 来源句不是登记写法："数据来源 · 运动记录 · 2026-09-05 → 2026-09-07 · 共 2 条"` |
| ④ | 整块消失（回归读数） | — | 同 ④ 旧条 | — | **M2b** 12/1：`distribution 缺：2026-09-05 ~ 2026-09-07`（窗口卡与 caption 一起摘掉时旧条抓得住；新条不作唯一防线） |

## 三、D4：测试名与内容对齐（改名或补齐，本票取「补齐」）

测试名 `#111 可见面零工程话：整份产物零命令键／票号／工序词` 旧版在整份面只判了 `calorie.view.` 一类（另三类只判可见面）。
本票**补齐三类各一条到整份产物面**（可见面那三条原样留着），名字与内容对齐：

| 新断言（整份产物面） | 改坏必红读数（污染只落在载荷属性里，可见面那条仍绿） |
|---|---|
| `!out.html.includes('calorie.exercise.')` | **M11** 11/2：`产物任意一面出现命令键：calorie.exercise.` |
| `!out.html.includes('移植')` | **M10** 11/2：`产物任意一面出现工序词「移植」：移植` |
| `!/\bt\d{3}\b/i.test(out.html)` | **M9** 11/2：`产物任意一面出现票号样式` |

依据（当刻可复算）：五页**整份产物**这四个词面当刻**各 0**（逐页自数，`node .scratch/t475/probe2.mjs` 的 D4 段）。

## 四、门禁对账（各一次，`node tooling/run-locked.mjs --ticket 475 -- <命令>`）

| 门禁 | runId | 读数 |
|---|---|---|
| `pnpm build` | `541dbc81-f069-4c36-bc0e-4207704981fd` | exit=0 |
| 111 件 | `87fd706c-eda2-413b-bcf2-a03e9d8a49b9` | 13/0 |
| 265 件 | `3def9359-b406-4e90-833f-c833d3763e7c` | 3/0 |
| 423 族 | `ccf18aef-2898-4ec4-9026-2291d0b92bab` | 20/0 |
| 451 族 | `70222976-b4d2-4cd3-a9c9-a5c3f19dfc88` | 8/0 |
| 452 族 | `2f5dcf26-afab-4ffa-b47f-c1b72199bb2e` | 9/0 |
| 453 族 | `85eb5062-d49e-445f-a02f-62de04cd0ee2` | 5/0 |
| 454 族 | `1cabf4aa-5c51-4a05-8a14-a431eeb66747` | 9/0 |
| 告警线（首跑） | `51cc60bb-9690-475b-baf6-4654467c856d` | **exit=1**：`RESULT: 56/60`、4 条陈化 |
| （归因读数）告警线 `--sync --dry` | `1da61fee-f1b4-4aeb-a8ba-ebe99d891329` | `SYNC-PLAN 改=4 增=0 删=0`（未落盘） |
| 告警线（归因复跑） | `4e1f76c6-a7fa-4ac1-b42f-bf311b611b7c` | **exit=1**：`RESULT: 59/60`、1 条陈化 |

**告警线红归因（两跑都不算本席）**：本席零源码写入——写集只有两件判据件，而 `test/*.mjs` 在告警线**扫描面之外**。

- 首跑 4 条陈化：`src/render/trendDocs.ts`（826／889）、`src/render/trendMiscPortDocs.ts`（571／592）、
  `src/analysis/multiTrendPage.ts`（490／501）、`src/render/wizardPort.ts`（265／266）；四件当刻 `git status` 均 ` M`（他席在改）。
  这四条随后由**归属票自己**同步（`AGENTS.md` 台账改在 892／592／501／266 —— 与本席 `--sync --dry` 读出的实况同值）。
- 归因复跑 1 条陈化：`src/fetch/body.ts`（台账 426／复跑当刻实况 443，当刻 ` M`；写本行时该件 LF 已到 455，仍在被他席改写）。
- 故本席**不**跑 `--sync` 改 `packages/skill-calorie/AGENTS.md`（不在本票写集，红线禁）；告警线红全部落在他席在途件上。

## 五、未做项与边界

- **未走「改源码 → `pnpm build` → 判据」四步序**：`sportPortDocs.ts`／`reviewDocs.ts` 当刻有别票在写，本票红线不碰实现件，
  故变异一律在隔离镜像的 `dist/` 副本上做（判据读的正是 `dist/`，鉴别力结论不受影响）；差别＝未过编译链、sha 取的是 `dist` 值。
- **D3／D6／D7（范围外）**：分布页可见面「会话」内部词、计划复盘页载荷面的 `calorie.view.` 2 处——票面指明属 #351 线，本票不做。
- **265 件只加「页题节点」一针**（分布用例逐字、趋势／复盘认「页名起头」）：窗口与块名两针只留在 111 件，免得同一判据两处走散。
- **未改测试名**：D4 走「补齐断言」这一支，名字原样（名字与内容已对齐）。
