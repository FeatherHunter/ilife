# t500 · 身体域失败面走失败回执整页证据

- 票面：卡路里 · 身体域失败面走失败回执整页（融合基准 §二 序 16 未落地），框架级。
- 基准：`docs/skills/skill-calorie/t395-融合基准.md` §二 序 16（`error_receipt.html:67`／`:80`／`:101-107`）＋ §六-C 第 7 行（新落点 `render/receipt.ts:212`）；#365 实测口径（`t365-复核.md` §2.5：缺数据 exit 4／坏参数 exit 2 只给 stderr，渲染失败 exit 5 出 RECEIPT，本票三件对该支 0 命中）。

## 一、改前基线（锁内实测，未改前）

- `GATE-RUN runId=5276d36a-bfbb-459a-ba03-92d5c335a229 cmd=node .scratch/t154/w1-500/baseline-500.mjs`
- 缺参数身体读命令 `calorie.view.body-measure-compare {}` → `exit=2 stdoutBytes=0 hasRECEIPT=false files=0`，`stderr=ERR 2: 参数失败：缺参数 date1…`（无整页）。
- 缺数据空库 `calorie.view.body-composition {}` → `exit=4 stdoutBytes=0 hasRECEIPT=false files=0`，`stderr=ERR 4: 取数失败（缺失阻断）：无体成分记录…`（无整页）。
- 结论：失败回执整页根本没接在这条路径上，与票面一致。

## 二、实现（影响清单先行，结构纪律第一步）

| 文件 | 一句话 | 理由 |
|---|---|---|
| `packages/skill-calorie/src/cli/readArgs.ts` | 新增 `bodySceneFor`（身体 10 键→`身体细节`，其余 null） | 场景判定的唯一定义地（权威源 `src/body/commands.ts` 10 键，前缀判定＋注释指权威，测试同步守漂移） |
| `packages/skill-calorie/src/cli/delivery.ts` | 新增 `failWithBodyReceipt(reason,key,code)`（复用 `buildErrorReceipt`＋`renderErrorHtml`，外层 `assembleDocPage` 包整页，exit 保持 2／4） | 失败分支产物怎么造（整页装配位），复用 `receipt.ts:212`，不新造形状 |
| `packages/skill-calorie/src/cli/cmd_read.ts` | catch 接线 6 行：身体键的 `missing-data`／`bad-input`／`FetchError` 改走 `failWithBodyReceipt`（同文案同 exit），非身体键走原 `fail` | 框架调用方最小 wiring（声明外，见 §六跨界说明；禁区未碰：唤醒词／命令名／示例、真库零写入） |
| `packages/skill-calorie/test/t500-失败整页.test.mjs` | 新建靶向 4 条（缺参数整页／缺数据整页／非身体仍纯文本／`bodySceneFor` 单元，自带负向对照） | 本票判据 |
| `docs/skills/skill-calorie/t500-失败整页证据.md` | 本件 | 证据 |

- 结构对账：`readArgs` 对外 5 件（`parseReadArgs`／`preflight`／`toast`／`USAGE`／`bodySceneFor`，≤5）；`delivery` 对外 4 件（`buildDeliveredEnvelope`／`failWithReceipt`／`failWithBodyReceipt`／`describeDeliveryTarget`，≤5）；跨能力引用只走公开接口（`shared/docPage.assembleDocPage` 共用位、`render/receipt`＋`render/html` 既有模板）；概念唯一（身体键判定只此一处，注释指 `body/commands.ts`，测试钉 10 键）。
- 行数（LF 口径，当场实测）：`readArgs.ts=69`／`delivery.ts=235`／`cmd_read.ts=146`，均＜350，未触发告警线第四步。
- exit 映射（按 #365 实测，保持）：缺参数→2、缺数据→4（含 `FetchError` 身体键→4）、渲染→5（原 `failWithReceipt` 片段不动，其它域 2／4 仍纯文本）。

## 三、改后读数（锁内）

- 构建 `GATE-RUN runId=7de18833-1c90-4756-9b7d-5ec52fff620c cmd=pnpm --filter skill-calorie build` exit 0。
- 靶向 `GATE-RUN runId=513a3860-c371-472b-848a-1d6be4cede34 cmd=node --test packages/skill-calorie/test/t500-失败整页.test.mjs` exit 0，`tests 4 pass 4 fail 0`。
  - ① 缺参数 `body-measure-compare {}` → exit 2、stdout 空、`ERR 2`＋`RECEIPT`、落盘整页首行 `<!doctype html>`、含`身体细节`／`失败`／`重试指令`＋命令串／`建议下一步`、路径绝对可读、bytes 相符。
  - ② 缺数据空库 `body-composition {}` → exit 4，同样四件齐。
  - ③ 非身体 `view.diet` 空库 → 仍 exit 4 纯文本（无 RECEIPT、无落盘），其它域不动。
  - ④ `bodySceneFor` 10 键→`身体细节`，非身体／空→null。
- 回归（cli＋body 系 9 件）`GATE-RUN runId=ffb648a9-97f1-43ea-90a8-3fe9ae57a08d cmd=node --test delivery-83 cmd-read-t11 body-compare-536 body-wizard-538 t360 t361 t362 t365 t398` → `tests 79 pass 78 fail 1`，唯一红 `T8 四主视图 parity`（`cmd-read-t11:143 HTML 快照须带基准平移后的当日标题`，T8 家族成功路径，与本票身体失败分支无因果；同期树上有他席 `M src/goal/*`、`M src/render/sportPortDocs.ts`、`M src/cli/write.ts` 在途，见 §六）。

## 四、源码级变异（改坏必红→还原必绿）

- 变异：将 `readArgs.ts` 身体前缀改 `calorie.body.`→`calorie.nope.`、`calorie.view.body-`→`calorie.view.nope-`（编译仍过，身体键全判 null＝摘掉失败分支）。
- 红 `GATE-RUN runId=b59340e8-4f59-420d-be96-0db355cf6d8d cmd=node --test packages/skill-calorie/test/t500-失败整页.test.mjs` → `tests 4 pass 1 fail 3`（①②④红，③绿），exit 1。
- 绿（逐文件还原 `readArgs.ts` sha 回 `563F39414…`，dist 自证含 `failWithBodyReceipt`＋身体前缀）`GATE-RUN runId=5a961682-5dcc-47ca-a979-087f8c073a1f cmd=node --test packages/skill-calorie/test/t500-失败整页.test.mjs` → `tests 4 pass 4 fail 0`，exit 0。
- 注：还原后全量 `tsc -b` 报 `src/render/planEditorPort.ts SLOTS` 4 处（该件为盘上 `??` 他席在途新件，本票一行未碰；虽 exit 1，`tsc` 仍落盘本票三件，dist 已验含新分支，靶向回绿即证；现场日志 `.scratch/t154/w1-500/mut-build-green.log` 保留，恢复由编排者持锁统一做）。

## 五、用法错误例外（属用法错误，不走整页，不在本票硬判）

- CLI 前置：未知参数／`--timeout` 非正数／`--params` 非 JSON／缺 key／未知 key（exit 2／3）／预检（node＜22.13／`SKILLS_DB_PATH` 未设，exit 1）——无场景可归，仍纯文本。
- 写命令直调 `fail(2)`（`src/body/log.ts`：缺 `source`／`sex` 非法／缺 `bodyFatPct`／7 点不齐等 6 处）：写前校验直接退出，未经框架 catch，改动需碰 `body/log`（非本票写集），留后续票；读验收不受影响。
- 未知失败 `fail(4)` 与非身体 `FetchError`：仍纯文本（其它域不动）。

## 六、跨界与在途说明（过程透明）

- `cmd_read.ts` 接线 6 行属声明外最小 wiring：它是 `delivery.failWithBodyReceipt` 的唯一调用方，不接则 CLI 验收跑不通；改前核对该件无他席 `M`（`git status` 仅本票三件 `M`），禁区（13 条唤醒词名／命令名／示例、真库零写入）一行未碰，真库全程 tmp 隔离。请编排者追认该 6 行（若不认，本票 delivery＋测试仍可独立复用，CLI 接线另票认领）。
- 他席在途：`?? src/render/planEditorPort.ts`（新件，`SLOTS` 未定义，挡全量 `tsc -b`）与 `M src/goal/*`、`M src/render/sportPortDocs.ts`、`M src/cli/write.ts` 等，本票一律未碰；回归唯一红 T8 归他席快照漂移（成功路径），身体旧针 `t360/t361/t362` 空库断言（仅 exit＋stdout，未钉 0 产物）仍全绿，故本票整页升级未打红身体旧针。
- 全仓统一遗留：其它域 2／4 仍纯文本；若要全仓统一换整页，与 #367／#368 同批并先抢锁广播（票面遗留出口）。

## 七、门禁声明

- `GATE-RUN runId=5276d36a-bfbb-459a-ba03-92d5c335a229 cmd=node .scratch/t154/w1-500/baseline-500.mjs`
- `GATE-RUN runId=7de18833-1c90-4756-9b7d-5ec52fff620c cmd=pnpm --filter skill-calorie build`
- `GATE-RUN runId=513a3860-c371-472b-848a-1d6be4cede34 cmd=node --test packages/skill-calorie/test/t500-失败整页.test.mjs`
- `GATE-RUN runId=9eb30c2e-9777-4b38-a8c0-0f5dab7c8baf cmd=pnpm --filter skill-calorie build`
- `GATE-RUN runId=b59340e8-4f59-420d-be96-0db355cf6d8d cmd=node --test packages/skill-calorie/test/t500-失败整页.test.mjs`
- `GATE-RUN runId=5a961682-5dcc-47ca-a979-087f8c073a1f cmd=node --test packages/skill-calorie/test/t500-失败整页.test.mjs`
- `GATE-RUN runId=ffb648a9-97f1-43ea-90a8-3fe9ae57a08d cmd=node --test delivery-83 cmd-read-t11 body-compare-536 body-wizard-538 t360 t361 t362 t365 t398`
