# t371 整页证据（今日总览族 5 词片段切完整文档）

- 票：#371（地图 #162 子票）；执行席已认领（assignee 已置）；基线 HEAD `1be7858`。
- 结论先行：今日总览族 5 件产物逐件 sha256（`5/5 ALL-COMPLETE`），件件 `<!doctype` 真，双击能开；
  `render-t8` 7/7 绿；`pnpm build` 与 `gen:check` 当刻红，红因均在伙伴在途写集内（见 §5），本票零新增红。

## ① 基线与写集

- 开工：`git pull` 已是最新；`.scratch/locks` 下无 `gate.lock`（锁空，持锁记录见各命令 `gate-runs.log`）。
- 票面广播（#371 评论 5659060941）：文件级写集
  `packages/skill-calorie/src/home/homeDocs.ts`（改）、
  `packages/skill-calorie/src/home/today.ts`（改 3 行）、
  `.scratch/t371/`（草稿）、`docs/skills/skill-calorie/t371-*.md`（证据）。
- 老行为不许动清单：`src/home/routes.ts`、`src/home/commands.ts` 一行未改；
  `src/weight/`、`src/diet/`、`src/exercise/`、`src/goal/` 未碰（`git diff --name-only` 本票仅 2 件 src；
  工作区其余在途改动分属伙伴，并发实况见 §5）；`src/render/html.ts` 等共用位未碰。
- 前置已就绪：`src/home/homeDocs.ts` 归位件（#370 纯搬迁，仍出片段）、
  基线 `docs/skills/skill-calorie/t370-fingerprint-before.json`。

## ② 做了什么（影响清单→交付对账，偏差零）

| 件 | 动作 | LF（节点口径） |
|---|---|---|
| `src/home/homeDocs.ts` | 新增 `buildHomeDoc`：内容 = `base-paint/blocks` 的区块（B-02 KPI 六卡／B-03 按日表／每日摄入线图／提示块），文档 = `assembleDocPage` 包裹，复制区走 `dataCopyArea`；旧 `renderHomeHtml` 片段保留（`render-t8` 锁定旧片段形状，不走 live 出口） | 148（≤350，不报警） |
| `src/home/today.ts` | 改线：`renderHomeHtml` 改从 `./homeDocs.js` 取 `buildHomeDoc`（导入 1 行＋调用 1 行＋注释 1 行）；`calorie.view.home` 的 metrics 口径不动 | 110（≤350） |
| `.scratch/t371/fingerprint-home5.mjs` | 新建：票内一次性指纹脚本（照抄 t370 真出口径＋t81 种子库；行为变更只记前后，不要求相同） | 草稿，不计数 |
| 本证据件 | `docs/skills/skill-calorie/t371-整页-证据.md` | 证据，不计数 |

- 5 词同一组装配共存：今日 KPI（今日摄入／蛋白／饮水／今日缺口）／目标进度（完成度＋提示块）／
  周趋势（每日摄入线图＋按日汇总表）／连续记录／热量预算（目标＋缺口＋TDEE 列），连续与预算为总览页内独立区块。
- 结构形状：本文件对外给 2 个（`renderHomeHtml`、`buildHomeDoc`，≤5）；新增类型 0；无 `any`。

## ③ 指纹判据：5 件逐件 sha256，真出口命令＋固定种子库

- 跑法：固定种子库（照 `docs/research/t81-seed.mjs`，`CALORIE_TODAY` 钉住）上经
  `src/home/routes.ts` 逐字 cli 跑 5 条（其中连续天数／热量预算两条与第一条同命令，经唤醒词的 `main_prompt.cli` 跑）。

| 唤醒词 | sha256 | bytes | doctype |
|---|---|---|---|
| 看今日主页 | `ec58555ddcf7764d09237480715474771b471957a672e58a9838a30853061ae2` | 68502 | true |
| 看本周主页 | `ec58555ddcf7764d09237480715474771b471957a672e58a9838a30853061ae2` | 68502 | true |
| 看本月主页 | `5273defe4800c09a20f5fc4b5cbebc3f2ec9acd1831048ed99e63e4bd21459c1` | 75844 | true |
| 看连续记录天数 | `ec58555ddcf7764d09237480715474771b471957a672e58a9838a30853061ae2` | 68502 | true |
| 看今日热量预算 | `ec58555ddcf7764d09237480715474771b471957a672e58a9838a30853061ae2` | 68502 | true |

- `RESULT: 5/5 ALL-COMPLETE`（runId=`23e1167b-b258-4347-9b4c-ade0ccb40201` exit=0）。
- 对照 t370 基线：5 件全 `CHANGED`（片段 2499B → 完整文档 68–75KB），行为变更只记前后，不要求相同；
  自检合理性：本周（windowDays 7）与今日缺省（7）同哈希，月（30）不同——窗口语义正确。
- 完整文档五连＋无残留逐件绿：`charset="utf-8"`／`<style>`／`<script>`／`ilife-page`／无 `<!--`；
  字段 8/8 在页上找得到（今日总览／今日摄入／蛋白／饮水／今日缺口／连续记录／周均摄入／复制数据）。
- 回执同值同源（抽查今日主页）：`data.output` 为绝对路径且存在，磁盘 68502B；
  `delivery.bytes`=68502 与磁盘相等，`delivery.path` 与 `data.output` 同值。
- 5 份产物落 `.scratch/t371/products/calorie_html/`（绝对路径，双击能开）：
  `今日总览_20260914_124059.html`（68502）、`…_2.html`（68502）、`…_3.html`（75844）、
  `…_4.html`（68502）、`…_5.html`（68502）。
- 缺失阻断沿既有口径：空库（未播种）跑 `calorie.view.home` 得 exit=4 `missing-data`，不落盘——这也算跑得通。

## ④ 变异自证（改坏必红，还原必绿）

- 改坏：`homeDocs.ts` 内 `今日摄入` 改 `当天摄入` →
  `RESULT: 0/5 MISMATCH`（5 件 `fields=MISS`，runId=`07666d79-1677-4ccb-abd2-05ef16b8f247` exit=1）。
- 还原：逐字改回（残留 `当天摄入` 命中 0）→ `RESULT: 5/5 ALL-COMPLETE`，
  与变异前哈希逐字一致（今日 `ec58555d…`／本月 `5273defe…`）。

## ⑤ 门禁（持锁，运行记录一对一）

- `node --test packages/skill-calorie/test/render-t8.test.mjs`：pass 7，fail 0
 （runId=`c961f89b-b52d-4dce-a53d-d4dfd3efcfaa` exit=0；片段保留故旧形状仍绿）。
- `tsc -p packages/skill-calorie`（包级，强制发射 `--noEmitOnError false`，因伙伴半截改动挡住整包）：
  报的错全在伙伴写集（当刻 `src/analysis/routes.ts`、`src/body/routes.ts` 等），
  本票两件零差异（`build.log` 内 `src/home/homeDocs|src/home/today` 命中 0，见 `.scratch/t371/build.log`）。
- `pnpm build`（根）：红，但红因是伙伴在途写集（如 `src/triggers/scene-03-weight.ts` 半截中文引号、
  `analysis/routes.ts`、`body/routes.ts` 的 `TS2820`）；本票锁内文件零差异，待伙伴落定后复绿。
- `gen-cli.mjs --check`：红，但唯一原因是 `GEN-STALE`（`body/commands.ts`、`body/routes.ts`、
  `scene-08.ts` 内容与上次 `pnpm build` 印记不同——全是伙伴在途能力目录）；
  本票未改任何生成器输入（commands／routes／legacy），10 个能力目录中 `home` 干净，零新增红。
- `pnpm snapshot:html:check`：依票面禁用（不覆盖卡路里），未跑。

## ⑥ 提交范围

- 本票 3 件：`packages/skill-calorie/src/home/homeDocs.ts`、
  `packages/skill-calorie/src/home/today.ts`、本证据件。
- `git diff --name-only` 核对：禁区（home routes／commands、weight、diet、exercise、goal、
  `src/render/html.ts`）无本票改动；`SKILL.md` 等其余在途改动分属伙伴，本票提交时只 `git add` 自声明 3 件。

## ⑦ 遗留与交接

- 未新开票：复核无可复现缺陷。两处记账（想法写文档，不占票）：
  ① `renderHomeHtml` 片段因 `render-t8` 锁定而保留，待整批页面收口时随 fragments 清理；
  ② `calorie.view.goal-progress` 仍是片段，归场景 06 那一侧，本票只记录、不改。
- 下一手缺什么：本票提交后，#371 整页化可直接收口；#372（其余 4 词）与本票并行不重叠；
  #373 接线待 #371、#372 落定；肉眼终审归 #375（用户亲手看结果型完整文档）。
