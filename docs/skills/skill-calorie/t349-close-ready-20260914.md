# #349 关票建议包（实施兵补证据，2026-09-14）

> 只加证据，不改认领人代码。`packages/` 源码与认领人测试文件均只读；新增断言只在 `.scratch/t349/` 探针里验证。
> 规范词：命令／帮助模板／页面交付／跑得通／回执／过程型／结果型。

## 1. 测试口径结论：以根路径为准

- 票面验收命令写 `packages/skill-calorie/test/scene05-write-mutate.test.mjs`，该路径**不存在**（与 #348 同样是旧路径笔误，#348 的创建类测试实际也在根 `test/`）。
- 为准的测试：`test/scene05-write-mutate.test.mjs`（93 行，5 条整链：过程页 → 写命令 → 结果页落盘＋回执绝对路径 → 读验证）。
- 建议：认领人关票时在票下回一声“口径以根 `test/` 为准”，或把票面路径改成根路径（只建议，不代改票面）。

## 2. 判据逐条：判据项／证据／命令／结果

| # | 判据项 | 命令 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | 主测试跑得通，exit 0 | `node --test test/scene05-write-mutate.test.mjs` | exit 0，pass 1／fail 0（2955ms） | `.scratch/t349/mutate-run.log`；门禁行 `RUN ticket=349 …t349-mutate-01… exit=0`（`.scratch/locks/gate-runs.log`） |
| 2 | 5 条逐条有过程页→写命令→结果页落盘＋回执绝对路径 | 同上（CHAINS 5 条） | 5/5：每条断言过程页落盘含 `ilife-page`、写 `data.ok=true`、回执绝对路径存在；撤销后读验证 exit 4 报“无训练计划” | 同上日志；独立复跑 `.scratch/t349/mutate-probe.log` GREEN 5/5 |
| 3 | 落库断言证据链（落库块） | `node --test packages/skill-calorie/test/cmd-write-40-persist.test.mjs --test-name-pattern="训练计划 update"` | 目标块 ✔ `落库 · 训练计划 update/update-day/delete-day/update-movement/delete：回执与库内行`（2375ms；逐条回读：改标题／改时段名／删整天仅删当天行／动作改名无残留／撤销删配置＋会话行，裸调无确认 exit 2 不写库） | `.scratch/t349/persist-block.log:25`；门禁行 runId `t349-persist-01` |
| 4 | t97 场景行 | `node docs/research/t97-probe-receipts.mjs --json .scratch/t349/t97-rows.json` | exit 0，`RESULT: 45/45`；其中 5 条变更类 `PASS … id/ts/rows/fields/line 全 ✓` | `.scratch/t349/t97.log:48-52`、`.scratch/t349/t97-rows.json`；门禁行 runId `t349-t97-01` exit=0 |
| 5 | 生成链＋探针 | `pnpm gen:check` | exit 0，`GEN-CHECK PASS：键 114（写 46＋读 68），未搬迁 0`，5 件生成物逐件 ok | `.scratch/t349/gen-check.log`；门禁行 runId `t349-gencheck-01` exit=0 |
| 6 | 变异红／还原一致两行 | `node .scratch/t349/t349-mutate-probe.mjs`（照抄认领人 CHAINS／seed 再改，新探针，不碰认领人文件） | `GREEN: 5/5`；`RED: RED-AS-EXPECTED`（第 1 条错期望触发断言失败）；`RESTORE: IDENTICAL`（4 件监视文件前后 sha256 全 SAME）；exit 0 | `.scratch/t349/mutate-probe.log`、`t349-mutate-probe.mjs` |

### 5 条变更逐条路径（过程页 op → 写命令 → 落库断言）

| 唤醒词 | 过程页 op（`calorie.view.plan-write-preview`） | 写命令 | 落库断言（`cmd-write-40-persist` :835 起） |
|---|---|---|---|
| 改训练计划 | `update` | `calorie.workout.plan-update` | 改后 `workout_plan_config.title=新标题`；带 `totalWeeks` 改 exit 2 |
| 改某天训练 | `update-day` | `calorie.workout.plan-update-day` | `workout_plans` 对应天 `session_label=腿部日` |
| 删某天训练 | `delete-day` | `calorie.workout.plan-delete-day` | 当天行数 0、全表剩 1 行（只删当天） |
| 改动作 | `update-movement` | `calorie.workout.plan-update-movement` | 动作 JSON 含新名、无旧名残留 |
| 撤销训练计划 | `delete` | `calorie.workout.plan-delete`（须 `confirm:true`，裸调 exit 2 不写库） | 配置行 0、会话行 0；读验证 exit 4 |

## 3. 是否可关判定：建议可关（附 2 个关前确认项）

- 技术证据链齐：主测试／落库块／t97 行／生成检查／变异探针全绿，5 条每条有过程页→写命令→结果页落盘＋回执绝对路径＋落库断言。
- 关前确认项（需认领人在票下回一声，不需改代码）：
  1. **撤销语义**：票面目标写“撤销语义以可逆、有回执为准”，实际实现为硬删除（删当天／撤销整份均不可恢复，见 `write.ts` :504／:558 注释与测试 exit 4 断言）。若“硬删除＋确认口径”即被接受，引用本包关闭；若坚持可逆，另开票，不在本票返工。
  2. **测试口径**：验收命令路径以根 `test/` 为准（票面 `packages/...` 路径不存在），关票时备注一句即可。
- 未在本次跑的（留给 #351 真机端到端或认领人收口）：包全量／smoke／t293 自治探针全量——当刻工作区有他人在途件（饮食 `productImport`、运动改动），全量门禁必然掺红（本次 `覆盖门` 已见 `calorie.product.import` 一条他人红，见 `.scratch/t349/persist-block.log:26`），不等干净树不跑全量。

## 4. 未做项＋下一手（含撞车与认领人待办）

- **是否撞车**：未撞。认领人文件（`src/workout/write.ts` 等 11:47、根测试 11:48）早于本席开工；本席只写 `.scratch/t349/`＋本建议包。树上饮食／运动在途件属其他席，不碰。
- **本席一次无锁触碰生成物**：12:23 查 `gen-cli.mjs --help` 时该脚本无视 `--help` 直接执行了一次生成，5 件生成物 mtime 被刷新；核对前后 `git status` 的 M 集合一致（`registry.ts` 内容逐字节无变化、仍干净），随后 `pnpm gen:check` 全绿，无内容伤害。如实留痕，判据引用以持锁的三条 runId 为准。
- **认领人还需做什么**：① 在 #349 下回复关前确认项 2 条；② 若要全绿收口，等树干净后在 #351 复跑包全量＋smoke；③ 若必须改认领人测试文件（如票面路径），本席未提交，diff 建议：把验收命令改成 `node --test test/scene05-write-mutate.test.mjs` 一行即可。
