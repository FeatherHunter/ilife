# T640 锁周委派守卫补齐 del-train／del-move／picker 确认路径 · 证据

判定：已做完（源码 3 行守卫＋探针 1 件＋本证据件＋changeset 1 件）。100% 待关（正文进度＋决议见票面，本席关票）。

## 一、读码定落库点（只在真正落库处加守卫，参数面禁碰）

| 分支 | 盘上行 | 落库操作 | 结论 |
|---|---|---|---|
| `del-train` | 约 359 行 | `day(d).sessions.splice(s, 1)` 直写 | 落库点，已加 `if (lock()) return;` |
| `del-move` | 约 384 行 | `day(d).sessions[s].moves.splice(m, 1)` 直写 | 落库点，已加 `if (lock()) return;` |
| `pick` | 约 373–382 行 | `se.moves.push({...})` 落子 | picker 确认路径真正落库处，已加 `if (lock()) return;`（紧随 `if (!picker) return;` 之后） |
| `add-move` | 约 365 行 | 只置 `picker／query／filt`＋`render()`，无 splice/push | 只开选择层不落库，不加守卫；机器依据＝探针 L7/M7（开层前后段数/动作数不变，改前改后皆绿） |
| `set-tstart`/`set-tend`/`set-sets`/`set-reps`/`set-load`/`set-min`/`toggle-mode` | input 委派约 399–417 行 | 参数面写回 | 负责人已裁不跟锁走，禁碰，本票一行未动 |

界面层现状（与票面一致，不复述为功劳）：`del-train` 锁下不渲染（`:178`）、`del-move` 锁下换锁图标（`:215–216`）、`add-move` 锁下禁用（`:175–177`）。本票补的是合成派发绕过界面直调委派的最后一道。

## 二、结构纪律五步

- 第一步影响清单（均为计划编辑器能力 `src/render`，同一能力）：
  - `packages/skill-calorie/src/render/planEditorRuntime.ts`：del-train／pick／del-move 三处各补 `if (lock()) return;` 一行（两处同行内嵌，一处独立新行）。
  - 新建文档面：`docs/skills/skill-calorie/t640-lock-probe.mjs`（探针不管辖）、本证据件、`.changeset/640-锁周守卫.md`、`.scratch/t640/`（草稿，不管辖）。
- 第二步结构设计：无新增目录层级；无新增公开接口；探针与证据是文档面，不进能力门。
- 第三步写代码：跨能力引用无；常量类型无新增；只动本能力目录一件源码。
- 第四步超线报警：已超线，需要根据规则进行重构。`planEditorRuntime.ts` LF 425→426（＋1 行＝pick 守卫独立新行；del-train／del-move 两处为同行内嵌不占新行），超因与拆法照台账（整台运行时一段 JS 文本；按交互族切 `planEditorPicker.ts`，待收口票认领），本次先不拆。台账「当场实测」列差 1 已如实记入 §五，未手改台账（先例：HEAD 已有两处他人陈化在案，全仓 `--sync` 会把别席在途数 baked in，故不动）。
- 第五步交付对账：见 §五，偏差为零（runtime 仅 3 行守卫，无逻辑改动；其余新增皆为文档/草稿面）。

## 三、改动（diff 3 行守卫）

```diff
-    if (act === 'del-train'){ day(d).sessions.splice(s, 1); render(); return; }
+    if (act === 'del-train'){ if (lock()) return; day(d).sessions.splice(s, 1); render(); return; }
     if (act === 'pick'){
       if (!picker) return;
+      if (lock()) return;
-    if (act === 'del-move'){ day(d).sessions[s].moves.splice(m, 1); render(); return; }
+    if (act === 'del-move'){ if (lock()) return; day(d).sessions[s].moves.splice(m, 1); render(); return; }
```

- sha256（持锁外只读机检）：`planEditorRuntime.ts 35bef0850210`、`t640-lock-probe.mjs d3b69a3856a8`。
- 未动：`add-move`、`set-*` 参数面 7 处、`SKILL.md`、门与门探针／夹具／变异件（`t554-*`）、门备注文案、别族件。

## 四、票面验收（4 条逐行对）

1. 自写探针 `docs/skills/skill-calorie/t640-lock-probe.mjs`（T558 底盘 L1–L3/M1–M3 判据一字不动；新增 L4/L5/L6 锁周不变、M4/M5/M6 母版照常、L7/M7 add-move 不动库依据；真出口＋headless Chrome 合成派发，一律持锁）：
   - 改前基线 runId t640-probe-base2 exit 1：RESULT 11/14，红恰为 L4（段数 2→1）／L5（动作数 1→0）／L6（动作数 0→1，层 0→1）；L7/M7 绿（add-move 开层前后段数/动作数不变）——探针灵敏且不加守卫结论有机器依据。
   - 还原必绿 runId t640-probe-final exit 0：RESULT 14/14。锁周 L1 段数 2→2 选时段层 0→0；L2 段数 2→2；L3 时段 凌晨→凌晨；L4 段数 2→2；L5 动作数 2→2；L6 动作数 2→2（层 0→1，开层照旧、落子被拦）；L7 段数/动作数不动。母版周 M1 选时段层 0→1；M2 段数 2→3；M3 时段 凌晨→上午；M4 段数 3→2；M5 动作数 1→0；M6 动作数 0→1；M7 不动。
   - 摘守卫必红（逐守卫隔离变异，各自单红 13/14 exit 1，还原即绿）：
     - `RED: mut1 摘 del-train 13/14 exit=1`（单红 L4 段数 2→1；L5/L6 因各自守卫仍在而绿）＋`RESTORE: 14/14 exit=0`。
     - `RED: mut2 摘 del-move 13/14 exit=1`（单红 L5 动作数 2→1）＋还原绿（同上）。
     - `RED: mut3 摘 pick 13/14 exit=1`（单红 L6 动作数 2→3）＋还原绿（runId t640-probe-final）。
   - 变异多行（机器读数）：`RED-L4 13/14` ＋ `RED-L5 13/14` ＋ `RED-L6 13/14` ＋ `RESTORE 14/14`。
2. 契约门 runId t640-gate exit 0：`node docs/skills/skill-calorie/t554-plan-editor-gate.mjs --phase=gate` → RESULT 19/19（#557 口径含 X3；§1①②④全绿）。附带留痕不碰门：备注①“委派处理器里没有 lock 守卫”一句更陈化了（现 6 处有守卫），归 #557 或收口票刷文案。
3. 回归 runId t640-regress exit 0：`t550-复制日志场景标识`＋场景 05 四件（`test/scene05-read.test.mjs`、`test/scene05-write-create.test.mjs`、`test/scene05-write-mutate.test.mjs`、`test/scene05-write-status-435.test.mjs`）→ tests 10 pass 10 fail 0。
4. 编译（入口写死 `node node_modules/typescript/bin/tsc -b packages/skill-calorie`，`npx tsc` 假出口不用）：t640-build／t640-restore-build 等 exit 0。

## 五、交付对账（与声明写集逐行对）＋台账说明

- 声明写集：`packages/skill-calorie/src/render/planEditorRuntime.ts`（已改 3 行）、新建 `docs/skills/skill-calorie/t640-lock-probe.mjs`＋本证据件、`.changeset/640-锁周守卫.md`（已建）、`.scratch/t640/`（RUNS.md、probe 产物 readings.json＋locked/master.html）。偏差为零。
- 未碰：门与探针（`t554-plan-editor-gate.mjs`／`t554-plan-editor-probe.js`／夹具／变异件）、`SKILL.md`、场景清单、参数面、别族件、`packages/base-render/**`、`packages/base-paint/**`。
- 告警线台账（runId t640-warnline，87/91 exit 1）：红 4 行中本票仅 `planEditorRuntime.ts 台账=425 实况=426` 一行（＋1＝pick 守卫新行，§二第四步已报）；另 `build-help.mjs 744→743` 与 `sportDocs.ts 434→466` 系 HEAD 已提交的他人陈化（工作区干净时即存在，非本票引入），全仓 `--sync` 会连别席数一起落盘故本票不动，留收口票。

## 六、遗留出口（票面两条销账）

- `pick` 语义判定：选择层（add-move）只开层，真正落库是 `pick` 的 `push`——结论已落库点加守卫，L6/M6 为证。本条销账。
- 锁周委派守卫齐：`add-train`／`pick-slot` 既有（e656ee64，不冒领）、`set-slot` 归 #558、`del-train`／`del-move`／`pick` 归本票。本票关后，锁周结构禁委派层再无敞开的增删改段／动作／换时段路径（参数面按裁定本就不跟锁）。本条销账。

## 七、未做项/下一手

- 未做：无（票面三处落库点全收；add-move 经机器读数定为不加；参数面禁碰一条未动）。
- 下一手：编排者核对本证据件 §四四条读数后，本票可关（本席已把票面进度推 100% 并关票；门备注①陈化与台账收口归 #557／收口票）。
