# t265 证据 · 7 条读类词归位（分布 1＋趋势 1＋复盘 5）

认领：`gh issue view 265` 示 assignees 已为 FeatherHunter，state OPEN，无需再 edit；票面与派工描述一致，无差异。

路径集合（声明即锁，开始前复述）：
- `packages/skill-calorie/src/exercise/routes.ts`（搬入 7 条：order 167→distribution，170→trend，171–175→recap，order 不动）
- `packages/skill-calorie/src/home/routes.ts`（删去被搬走的 7 条，其余 12 条窗口词 150–158／164–166 不动）
- `packages/skill-calorie/src/triggers/scene-04-exercise.ts`（7 条 `main_prompt.cli`／`data_source` 逐字同步）
- `packages/skill-calorie/test/exercise-routes-265.test.mjs`（新，3 用例，含变异说明）
- 证据 `docs/skills/skill-calorie/t265-*`、草稿 `.scratch/t265/`、changeset `.changeset/265-*.md`
- 生成物 `src/triggers/routes.generated.ts` 由 `pnpm gen` 派生（手未改）；禁区未碰：commands 键集、`routing.ts`、order、分派层。

前置：`git log df2e07f..HEAD -- <3 件>` 无输出；`git status --short` 脏树 30＋ 件均不在声明路径；`.scratch/locks` 无 owner、无 gate.lock，`gate-runs.log` 无 ticket=162 记录 → 与 #162 无同文件窗口，可动。

## 机器读数

判据先行（现状红，`.scratch/t265/red-before.log`）：
- `node packages/skill-calorie/test/exercise-routes-265.test.mjs` → exit 1，3 用例均红于「缺期望 key 的 exec 记录」（7 条仍指 `calorie.view.exercise`）。

实施后（`.scratch/t265/final-green.log`）：
- `pnpm build && pnpm gen && pnpm build && pnpm help:build` → 均 exit 0
- `pnpm gen:check` → exit 0（键 118＝写 46＋读 72；能力 10 个；声明 118 条；未搬迁 0）
- `node packages/skill-calorie/test/exercise-routes-265.test.mjs` → exit 0，tests 3／pass 3（routesFor 7 条各指其页；逐条按路由 cli 实跑含 eyebrow key＋题面且无「运动总览 」；冻结 7 条逐字一致）

变异自证（`.scratch/t265/mut-red-final.log`）：
- 把 order 167 声明改回 `calorie.view.exercise` → `pnpm build && pnpm gen && pnpm build` 后测试 exit 1（红）
- 改回 distribution → 全绿（上段）。注：只改声明不 `gen` 会假绿（生成物未派生），变异必须走 build→gen→build。

回归（`.scratch/t265/related.log`）：
- `exercise-records-342` 5／5、`cmd-registry-294`、`legacy-ratchet-295`（棘轮未动）→ 共 18／18 绿
- `test/calorie-routing-81.test.mjs`（`.scratch/t265/routing81b.log`）→ 8 中 6 红，但全部在别域（A7 营养表×2、FX-81-5 product.import、exec 数 479≠459 含他席在途约 7 条、D2③／D2④ 拍营养表记一餐），无一条涉本票 7 词；脏树他席改动所致，本票未碰其行，按票面「停下报告，不代改」。

## 结构对账（必报第五步）

第一步清单 4 件＋证据 3 类，实际触达：exercise/routes.ts（头注 33 条＋搬入 7 行）、home/routes.ts（删 7 行）、scene-04-exercise.ts（14 处键名改指，params 逐字沿旧）、新测试 1 件、证据／changeset／.scratch 落盘；生成物 routes.generated.ts 由 gen 派生但含他席在途声明（如饮食／体重侧翻转）故本次不随件提交（沿 #342 先例：声明先行，快照统一重导），工作区内 gen:check 为绿。偏差为零。行数均远低于 350，无超线。
