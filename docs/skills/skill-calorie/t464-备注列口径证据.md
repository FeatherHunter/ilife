# t464 备注列口径证据（地图#158 · 票 #464）

## 一、目标与改动（票面五段为准，`gh issue view 464`）

`packages/skill-calorie/src/body/bodyDocs.ts` 体成分记录表「备注」列两处订正：

1. 可见文本：缺值由空串改 `—`（`note: typeof x.note === 'string' ? x.note : ''` →
   `note: typeof x.note === 'string' ? x.note : '—'`）。空串值仍为空（「空」只来自空串值，
   写侧统一 `input.note ?? ''`，实测 `fetch/body.ts:289`，票面 252 已漂移）。
2. 注释：由老 `:342／:347-352` 改老正本 `:211` 的 `esc` 口径
   （`esc(s){return String(s==null?'—':s)}`，当场重验 `D:\2Study\StudyNotes\SKILLS\卡路里\templates\body_composition_view.html`
   第 211 行；`:366` 行正是 `esc(r.note)`，故老页对 `null` 同样写 `—`）。

复制 payload 不动（`{ ...r }` 原样透传，`null` 保留）。其它列（date/pct/source）、围度 `:278`、
唤醒词与命令名、`body_fat_pct`、真库一律不动。

## 二、验收（跑哪条命令 → 看哪个读数 → 等于什么）

| # | 命令 | 读数 | 期望 | 实测 |
|---|---|---|---|---|
| 1 | tmp 库直插 `note=NULL` → `calorie-cmd-read calorie.view.body-composition`，剥标签后按格取备注格 | NULL 行该格 | `—` | `T464-VISIBLE null格="—"` ✅ |
| 1b | 同上（空串／文本对照行） | 空串行该格／文本行该格 | `` ／ `晚餐后测` | `空串格="" 文本格="晚餐后测"` ✅ |
| 2 | 同一页复制数据 JSON 载荷（与第 1 条分开断言） | 该字段／全行扫描 | `null`、不含 `—` | `T464-PAYLOAD null=null 空串="" 含破折号行=0` ✅ |
| 3 | 读 `src/body/bodyDocs.ts` 源码 | 注释含 `:211`＋`esc(`＋`空串值`＋`input.note ?? ''`，判据为非串走 `—` | 逐字命中 | `T464-COMMENT 211/esc/空串值/input.note逐字命中` ✅ |
| 4a | 变异：判据改回空串 → 重编 → 跑 t464 测试 | 必红 | exit 非 0 | `tests 3 pass 1 fail 2`，exit=1 ✅ |
| 4b | 还原 `—` → 重编 → 跑 t464 测试 | 必绿 | exit 0 | `tests 3 pass 3 fail 0`，exit=0 ✅ |
| 回归 | `node --test test/t362-窗口不截断.test.mjs`（t362 原样未碰） | 15/15 | exit 0 | `tests 15 pass 15 fail 0`，exit=0 ✅ |

测试：`packages/skill-calorie/test/t464-备注列口径.test.mjs`（新建 3 用例，不碰 t362）。
编译：`npx tsc -b packages/skill-calorie`，exit=0（日志空）。

## 三、门禁对账（GATE-RUN，边跑边补）

- `GATE-RUN runId=t464-5481714f cmd="npx tsc -b packages/skill-calorie"`（初编，exit=0）
- `GATE-RUN runId=t464-5481714f cmd="node --test packages/skill-calorie/test/t464-备注列口径.test.mjs"`（初绿 3/3，exit=0）
- `GATE-RUN runId=t464-5481714f cmd="node --test packages/skill-calorie/test/t362-窗口不截断.test.mjs"`（回归 15/15，exit=0）
- 变异红：`cmd="node --test packages/skill-calorie/test/t464-备注列口径.test.mjs" exit=1 fail=2`（负向证据，非门禁引用）
- `GATE-RUN runId=t464-5481714f cmd="npx tsc -b packages/skill-calorie [restore]"`（还原重编，exit=0）
- `GATE-RUN runId=t464-5481714f cmd="node --test packages/skill-calorie/test/t464-备注列口径.test.mjs [restore]"`（还原绿 3/3，exit=0）
- 运行条目原文见本目录 `t464-GATE-RUN.log`（持锁 `RUN ticket=464` 行导出件）；长命令日志在 `.scratch/t464/`（`impl-build.log`／`impl-test-green.log`／`impl-test-t362.log`／`impl-test-red.log`／`impl-test-restored.log`，均只看尾行，明细备查）。
- 持锁：`gate.lock` 抢前探活（467 活进程不抢、560 活进程不抢），空窗即抢、留痕 `RUN ticket=464`，持锁区只做编译／测试／变异／提交，释放前复原（源码逐字节还原＋重编＋复绿）。

## 四、真库零写入对账

- shell 本机 `SKILLS_DB_PATH=D:\2Study\StudyNotes\.db`；本票一切运行显式传 mkdtemp tmp 目录覆盖（`SKILLS_DB_PATH: dir`），从未裸跑。
- 真库 `D:\2Study\StudyNotes\.db\calorie_data.db` 跑前：mtime=2026/9/15 15:36:54，size=3072000，
  sha256=`D40D58F50E74E294A11E410B4D9E2142FCBCD8993328A4EFB453F1A0D62A1083`；跑后复核三项全等（见 §六）。

## 五、遗留出口（只登记，不改）

- `src/body/view.ts:37` 的 `metrics.trendDays` 取 `v.trend.length`，与页面 KPI（`chartPoints()` 过滤后点数）
  仅在 `avgPct` 非有限时分叉；写侧校验使该状态不可达 ⇒ 本票不改，要收窄另开票。
- 老技能参照件不回改（只读重验行号）。

## 六、结构对账（`docs/agents/structure.md` 必报五步）

1. 影响清单：只碰身体能力一行（`src/body/bodyDocs.ts` 备注列判据与注释）；测试／证据／草稿／changeset 在管辖外随附，无跨能力引用。
2. 结构设计：无新目录、无新公开接口（改一处表达式＋一处注释）。
3. 写代码：跨能力引用无；常量与类型无新增；目录内互用不对外。
4. 超线报警：本件 318 行，未超本包 350 行告警线，不触发第四步。
5. 交付对账：本节即对账——清单逐行有着落，偏差为零。

## 七、提交清单

- `feat(464)` 增量提交（信息经 UTF-8 文件提交，`diff --cached --name-only` 复核仅自有 5 件，不 push）：
  `packages/skill-calorie/src/body/bodyDocs.ts`、`packages/skill-calorie/test/t464-备注列口径.test.mjs`、
  `docs/skills/skill-calorie/t464-备注列口径证据.md`、`docs/skills/skill-calorie/t464-GATE-RUN.log`、
  `.changeset/t464-备注列口径.md`。commit sha 见回执。
