# #313c · 收尾席 · 证据（关票 ＋ 地图同步）

- **票**：wayfinder [#313](https://github.com/FeatherHunter/ilife/issues/313) 的**收尾席**（标签 `313c`）；只做关票与地图同步，**不改任何交付代码**。
- **开工基线**：HEAD `2fcb6b255f6d8e26708f8b1e6b40797a7fd47228`（＝ `origin/master`）；`git status --porcelain` **0 行**；`.scratch/locks/gate.lock` **不存在**。
- **本席唯一改的受跟踪文件**：`docs/agents/命令登记纪律-照抄说明.md`（一处「生成物枚举」补齐，见 §②）；其余为本文件与随它入库的对账源。
- **终端纪律**：全部命令跑在 better sidebar 终端（标题 `313c-收尾`）；门禁与写 git 一律经 `node tooling/run-locked.mjs --ticket 313c -- <命令>`；
  只读 `gh`／`git status`／`git diff` 与对账工具直跑（**不进** `gate-runs.log`）。

---

## ① 开票前核三件（不核不许写 100%）

1. **工作树与环境**：`git status --porcelain` 空、`HEAD=2fcb6b2`、`Test-Path .scratch\locks\gate.lock` = `False`（本席第一条命令实测）。
2. **定点修复席提交在链上且门自跑为绿**（本席**自己重跑**，非引用）：
   - `pnpm gen:check`：runId `28245ebb-6959-47e5-a360-bc605ab3e037`，`exit=0`，
     `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 1 个（weight）声明 9 条，未搬迁清单 92 条`，五件生成物逐件 `GEN-CHECK ok`。
   - 全量 `pnpm test`：runId `d385a506-8a4d-410f-b913-2c212490e9ec`，`exit=0`，`ℹ tests 1492`／`ℹ pass 1492`／`ℹ fail 0`／`ℹ cancelled 0`（**1492／1492**）。
   - 链上提交：`bacc494`（关票前定点修复，3 件 5 处）→ `2fcb6b2`（证据补记）→ 即 HEAD；复核席报告 `d03032a` 亦在链上。
3. **CI 逐名对照（本席独立做，非引用复核席结论）**：
   - **HEAD `2fcb6b2` 的 run `34753153085` 在本席关票时仍 `in_progress`** ⇒ 按派单**如实记「待确认」**（不许猜绿）。
   - 已完成的前一次 `cb5b4c5` run `34752146596`（`conclusion=failure`）与**开工前的非 313 提交** `34748540493` 逐名对照：
     - 失败**作业集完全相同**：`build-test`（macos／ubuntu／windows × 22.13.0／24.x 五格）＋ `publish-gates`，`Compare-Object` **差 0 项**；
     - 失败**测试名 45 名完全相同**：只 HEAD 有 = **0**、只基线有 = **0**；
     - 45 个失败名里对 `313|routing|legacy|gen-check` 的命中数 = **0**（**零 #313 口径测试**）。
   - **结论**：本次引入的红 = **0**；这些红在开工前的非 313 提交上**逐名复现** ⇒ 全为**票外既有红**（`publish-gates` 的 `dsh-calorie 契约键 calorie.help.center`、一批 `dsh-*-client`、`#237/#91/#87/#48/#96/#75/#80/#83/#133/#188/#193/#201` 等）。
   - 留痕（未入库，`.scratch/t313c/logs/`）：`ci-head-jobs.txt`／`ci-base-jobs.txt`／`ci-head-logfailed.txt`／`ci-base-logfailed.txt`／`ci-head-failed-tests.txt`／`ci-only-head.txt`（空）。

---

## ② 折进两件小事（派单「补 1」）

1. **定点修复席主动多改的第 4 处（编排者已接受，本席不回退）**：`docs/agents/命令登记纪律.md` 判据 4 的 ② 桶实例
   （原把 `routing.ts` 举成「必须手改」，与探针实测「② 桶 0 处、`routing.ts` 落 ①」冲突）。已如实写进票面决议评论
   「定点修复」一句：**含一处同类扩改（判据 4），编排者接受**。
2. **补齐相邻不精确的那一处枚举（本席唯一改动）**：`docs/agents/命令登记纪律-照抄说明.md`（原 `:39`，「生成链四步」条）——
   原文说「**第二次**把 `pnpm gen` 刚改写的生成物（`cli/keys.ts`、`cli/registry.ts`）编译进 `dist/`」，
   `#313` 起 `pnpm gen` 还写 `src/triggers/routes.generated.ts`（`gen-cli.mjs` 的 `targets` 五件之一，`--check` 逐件比对同一数组），
   该件也要靠这次 build 进 `dist/` 才被路由层读到 ⇒ **枚举不全（不假但漏）**。改后：

   ```
   **第二次**把 `pnpm gen` 刚改写的生成物里那三件 TS
   （`cli/keys.ts`、`cli/registry.ts`、`triggers/routes.generated.ts`——`#313` 起最后一件才进 `pnpm gen`／`gen:check`）
   编译进 `dist/`，好让分派层读到新的键表、路由层读到新的路由表（另两件产物 `combos.yaml` 镜像与
   `scripts/build-help.mjs` 不经 `tsc`，故不在此列）。少任何一次都会按旧内容走。
   ```

   - **「另两件不经 `tsc`」的依据**：`packages/skill-calorie/tsconfig.json` 的 `include` 只有 `["src"]`（`scripts/` 不在编译面，且它是 `.mjs`）；`packages/base-combos/combos.yaml` 非 TS。
   - **两份纪律文本里其余「生成物枚举」处逐处核过（本席 grep 后逐条判）**：`命令登记纪律.md` 的 `:36`（单指 `cli/registry.ts` 运行期查表）、
     `:51`（生成的 `registry.ts` 取 `../<能力>/index.js`）、`:109`（生成物门）、`:118-122`（记录面住 `routes.generated.ts`、`routing.ts` 不在 `targets`）、
     `:146`／`:151`（不列名单，以 `gen:check` 逐件输出为准）、`:133`／`:136`（判据 1／2）；
     `照抄说明.md` 的 `:27`／`:31`（共用位零接触、手改无效）、`:50-52`（记录面落位）、`:61`（收口判据③）、`:79-82`（新增命令改动面）、`:98-99`（自证三件）——
     **除本席改的这一处外，无第二处把生成物集合写窄的地方**（`命令登记纪律.md:46,151` 两处**刻意不列名单**，是纪律自设，不是漏）。
   - **改后复跑（派单 §一.2 顺序）**：`pnpm gen:check` runId `8e9feb12-737c-4cb5-addf-c3950123bb1f` `exit=0`（五件 sha256 与改前**同值**，含 `routes.generated.ts` `e9ea9935…`）；
     全量 `pnpm test` runId `5499d6c6-9c92-47d1-b9f2-5028baa867af` `exit=0`、`ℹ tests 1492`／`ℹ pass 1492`／`ℹ fail 0`。doc 改动**未影响任何门**，无红可报。

---

## ③ 票面、决议评论、关票、地图（四步都做了回读核对）

| 步骤 | 命令 | 结果 | 回读核对 |
| --- | --- | --- | --- |
| 票面写 100% | `gh issue edit 313 --body-file …` | `exit=0` | `## 进度：100%` **独占一行**（1 处）＋ `## 进度：45%` 保留；字面 `\n` = **0**；前 3 字节 `80,97,114`（**无 BOM**）；行数 87 → 100 |
| 决议评论 | `gh issue comment 313 --body-file …` | [issuecomment-5652865097](https://github.com/FeatherHunter/ilife/issues/313#issuecomment-5652865097) | 回读 38 行、字面 `\n` = **0**、① 与 ⑤ 段在位（各 1 处） |
| 关票 | `gh issue close 313 --comment "<一句话>"` | `exit=0`，`state=CLOSED`、`closedAt=2026-09-13T11:04:12Z` | — |
| 地图 #293 | `gh issue edit 293 --body-file …` | `exit=0` | 逐行 `Compare-Object`：与改前**只差 3 处**（表行／新增一行 Decisions／「下一步」重写），**其余行列零改动**；字面 `\n` = 0；无 BOM |

- **票面「待确认」7 条 → 已确认**：逐条注明确认方式（复核席独立复跑／定点修复席复绿／编排者裁定），CI 那条**一半已确认、一半待确认**（HEAD run 未终判）。
- **评论按派单 §三 五节写**：① 结论（含定点修复一句）② 关键数字 ③ 偏离与裁定（5 条：声明落位／生成物落位被接受／棘轮分母／铁律四命名／场景归属口径）④ 记账（S3 七条 ＋ **定点修复席对账口径** ＋ **复核席未确证项两条**）⑤ 证据链。
- **地图三处**：任务清单 #313 行 → `— ✅ 已关（100%）`；`## Decisions so far` 追加 [313](https://github.com/FeatherHunter/ilife/issues/313) 一行（含两条待核约束）；
  `## Not yet specified` 的「下一步」写成 #314–#319 六票已解阻、彼此不阻塞、可并发开工（只指票号，不列会过期的清单）。

---

## ④ 门禁对账（窗口口径）

**GATE-RUN 声明**（只认 `exit=0` 的 `RUN` 条目）：

- GATE-RUN runId=28245ebb-6959-47e5-a360-bc605ab3e037 cmd="pnpm gen:check"
- GATE-RUN runId=d385a506-8a4d-410f-b913-2c212490e9ec cmd="pnpm test"
- GATE-RUN runId=8e9feb12-737c-4cb5-addf-c3950123bb1f cmd="pnpm gen:check"
- GATE-RUN runId=5499d6c6-9c92-47d1-b9f2-5028baa867af cmd="pnpm test"

对账命令（逐字可复跑）：

```text
node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t313c-收尾-证据.md --ticket 313c \
  --since 2026-09-13T10:58:45Z --until 2026-09-13T11:07:00Z --export docs/skills/skill-calorie/t313-gate-runs-313c.log
```

- 窗口起点取本席**首条门禁 `START`**（`10:58:45.363Z`），窗口终点取**末条门禁 `RUN`（`11:01:36.000Z`）之后、收尾 `git` 之前**的整分
  （与 `t313b4`／`t313v`／`t313r`／`t313f` 四席同口径）：把「本证据与对账源入库」的那次 `git add`／`commit`／`push` 留在窗口外
  ——并发协议 §2.4 写明「窗口外的条目（他人票号、提交后的 git 条目）不受影响」。
- 窗口内 `ticket=313c` 的 `RUN` 条目 = **4 条**，即上面声明的四条，**全部 `exit=0`**；**未用任何 `--allow-*`**（无 `GATE-RELAX` 行，也没有需要放宽的条目）。
- **窗口内他人票号条目**：本席首条门禁之前最近的是 `313f` 的收尾 `git` 运行（在 `--since` 之前，不在窗口内）；窗口内无他人条目。

**实测**（`exit=0`，导出时刻 `2026-09-13T11:06:16Z`）：`RESULT: matched=4/4 auditEntries=1548 scoped=4 undeclared=0` ＋ `gate-audit: PASS`；
`EXPORT: docs/skills/skill-calorie/t313-gate-runs-313c.log（窗口内 RUN 条目 4 条，受 git 跟踪）`。

**窗口外的收尾动作（如实留痕，不作 `GATE-RUN` 声明）**：把本证据、纪律文本枚举修正与对账源入库的三步持锁运行
（`git add -- <本席 3 个路径>`／`git commit -F …`／`git push origin master`）在本文件写作时**尚未发生**，
按 §④ 口径落在 `--until` 之后 ⇒ 不进本窗口（第三方后移窗口审它时，按同口径声明那三条即可，**不加放宽开关**）。

---

## ⑤ 未做／未确证（范围声明）

- **未做**：改任何交付代码、改任何已入库证据文件、碰 `packages/**` 下任何文件；未装任何东西；未用危险 git 命令
  （只有 `git add <本席路径>`、`git commit -F`、`git push origin master`）。
- **未确证（如实记）**：HEAD `2fcb6b2` 的 CI run `34753153085` 在**关票时仍 `in_progress`** ⇒ 本席未能证 HEAD 绿，只证「本次引入的红 = 0、其余红逐名票外复现」。
  关票后该 run 若出结论，**不由本席追认**（本席已停），后续席位按同一逐名对账口径处置即可。
- **未确证（承复核席）**：复核席新探针之一（顺序权威是 `order` 而非文件位置）只在**编译后 dist** 上做搬件，
  **未做「`.ts` 源搬件 ＋ 真 `tsc` 重建」的端到端版**；这是已知强度缺口，非失败。
