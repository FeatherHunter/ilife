# #268 对抗式审查（代码层）· 卡路里「运动」场景 04 · 39 条唤醒词

> 票：[`#268`](https://github.com/FeatherHunter/ilife/issues/268)（地图 #156 子票）｜席＝对抗式审查席（代码层），**只审不实现**。
> 负责人 2026-09-16 裁定：本轮验收不再要求负责人亲手复制 prompt 跑到打通，**以代码层对抗式审查为准**。
> 审查日：2026-09-16　｜　被测形态：`packages/skill-calorie/dist`（当刻源码经 `node node_modules/typescript/bin/tsc -b packages/skill-calorie` 编译，runId `a5ec6a56-afb2-405f-9076-faaa9dfed157` exit=0）
>
> **本席自造探针**（不复用 #267 的跑动驱动），本席的件＝`.scratch/t268r/` 下的 `lib.mjs`／`fp.mjs`／`p1-parity.mjs`／`p234.mjs`／`p4-cross.mjs`／`p4b.mjs`／`p4c.mjs`／`p5-mutate.mjs`／`p506-nogoal.mjs`／`p6-realdb.mjs`／`p7-helpcli.mjs` 与同名 `*.log`（`.scratch/` 已被 `.gitignore` 忽略，不入提交）。
> 同目录另有**别席** 12:57–13:15 写下的件（`audit.mjs`／`probe-*.mjs`／`mutate.mjs`／`real-db-facts.mjs` 等），本席**未读、未用**，本文任何读数都不出自它们。
> 真库＝`D:\2Study\StudyNotes\.db\calorie_data.db`，**全程只读**；13 条写词只在临时库副本上跑。

## 〇 · 判定与逐面结论（一句话表）

| 面 | 内容 | 结论 | 机器读数（首行） |
|---|---|---|---|
| 1 | 词→命令：39 条 `main_prompt.cli` ↔ 路由声明逐字一致（两处定义地不走散） | **成立** | `P1-TOTAL 断言=746 通过=746 不符=0` |
| 2 | 真出口逐条跑：exit 0 ＋ 绝对路径 ＋ 完整文档；真库 sha256 跑前跑后不变 | **成立** | `T267-RESULT: 8/8 通过 exit=0`；`G1 词=39 exit0=39 绝对路径=39 字节如实=39 完整文档=39` |
| 3 | 页面归属：7 条读类词各拿自己那一页 | **成立** | 分布 1／趋势 1／复盘 5，7/7 产物零命令键 |
| 4 | 边界：空窗／无备注筛选／长窗截断／跨年轴 | **三档成立，跨年轴一处不成立** | 见 §四；不成立项＝趋势族／复盘族的折线卡跨年不印年份 |
| 5 | 变异探针：破坏一处→必红→逐字节还原→必绿 | **成立** | 红 `7/8（红：G2）`～`6/8（红：G3；G5）` → 绿 `8/8`，还原 `SAME` |
| 特别 | 「无目标」时两条 vs 目标词的真出口输出原件（#506 裁决用） | **证据已成，见 §六（含「无写路径」实测）** | `exit=4`、`stdout=""`、stderr 不点名任何命令 |

**判定：地图 #156 场景 04 的三句目的地（39 条都能从头走到尾 ／ 产物是真 HTML ／ 由真出口产出）在代码层成立**（隔离库 39/39）。
附一条面 4 不成立项与七条记账，逐条在 §七，未覆盖面在 §八。

---

## 一 · 面 1 · 词→命令（39 条逐条）

**四条轴各读一遍再互相咬合**（都取原文、不做归一化；任一条轴漂了即计数）：

| 轴 | 读取面 | 读数 |
|---|---|---|
| ① 冻结表 | 源码 `src/triggers/scene-04-exercise.ts`（每行一个 JSON 字面量，本席自行解析） | 39 条 |
| ② 路由声明 | 源码 `src/exercise/routes.ts` ＋ `src/home/routes.ts`（记录住「键属谁」，场景 04 分住两件） | 45 条（39 `wake` ＋ 6 `new` 别名） |
| ③ 生成物 | 源码 `src/triggers/routes.generated.ts`（`pnpm gen` 派生） | 45 条，与 ② 逐条同源 |
| ④ 运行时 | `dist/**` 的 `SCENE_04_EXERCISE`／`WAKE_ROUTES`／`routesFor`／`lookupWake`／预建 `HELP_LOOKUP` | 39 条 |

机器读数（`node .scratch/t268r/p1-parity.mjs`，逐字）：

```
== 面1 词→命令 · 四轴对账（39 条逐字） ==
冻结表条数(源码解析)=39 运行时 SCENE_04_EXERCISE=39
声明件记录(scene04)=45（exercise/routes.ts ＋ home/routes.ts） 生成物记录(scene04)=45
逐轴读数（分子＝通过条数）：
  声明记录恰 1 条：39/39
  声明 kind=exec：39/39
  声明 cli＝冻结 main_prompt.cli：39/39
  声明 cli＝冻结 data_source：39/39
  声明 key＝命令键：39/39
  声明 scene=04：39/39
  冻结 data_source＝main_prompt.cli：39/39
  生成物有这条词：39/39
  生成物 cli＝冻结 cli：39/39
  生成物 kind=exec：39/39
  生成物 key＝命令键：39/39
  运行时冻结表有这条词且 cli 同源：39/39
  AI 路 lookupWake 命中恰 1 条：39/39
  AI 路给的命令＝冻结 cli：39/39
  预建 HELP_LOOKUP 同源：39/39
  运行时路由层 exec 恰 1 条：39/39
  运行时路由层 cli＝冻结 cli：39/39
  生成物运行时面 cli＝冻结 cli：39/39
  声明件 wake 词集＝冻结表词集：1/1
  生成物多出来的词＝声明件 new 别名：1/1
  冻结表词集 ⊆ 生成物词集：1/1
  冻结表无重复词：1/1
  声明件场景04 无 non-exec：1/1
P1-TOTAL 断言=746 通过=746 不符=0
P1-FROZEN 39 条命令键计数：11 键
```

反向项也读了两条：生成物里场景 04 的词比冻结表**多 6 条**，逐条对上声明件 `list === 'new'` 的别名（`看运动目标／看力量总览／看有氧总览／看运动分类占比／看运动复盘／看运动消耗趋势`）——不是孤儿，是自造词；`scene 04 && list wake` 的声明词集与冻结表词集**相等**（缺词、孤儿、重复皆 0）。

### 1.1 补一条更贴用户的那条路：走真出口的 HELP 查找（`p7-helpcli.mjs`）

`lookupWake` 是库函数；AI 真正念到的路径是**先查帮助拿到命令**。本席按这条真路逐条跑了一遍：
`node dist/cli/cmd_read.js calorie.help.lookup --params '{"q":"<词>"}'`（库＝空夹具），取回 `data.items[].cli` 与冻结表原文逐字比。

```
P7 命中恰 1 条 ＋ 就是这条词 ＋ cli 与冻结表逐字相同 = 33/39
P7 命中的那条里，是本词自己的命令（cli 逐字相同）= 39/39
P7 精确命中排在第 1 位 = 38/39
P7 命中多于一条（要靠排序挑）= 6 条：
  记运动 → 4 条，序=["记运动","记运动（含备注）","补记运动","批量补记运动"] ｜ 本词 cli 逐字相同=true
  补记运动 → 2 条，序=["补记运动","批量补记运动"] ｜ 本词 cli 逐字相同=true
  改运动记录 → 2 条，序=["改运动记录","改某日运动"] ｜ 本词 cli 逐字相同=true
  看今日运动 → 3 条，序=["看今日运动概览","看今日运动","看今日运动（vs 目标）"] ｜ 本词 cli 逐字相同=true
  看本周运动 → 3 条，序=["看本周运动","看本周运动（vs 目标）","运动复盘（本周）"] ｜ 本词 cli 逐字相同=true
  看本月运动 → 2 条，序=["看本月运动","运动复盘（本月）"] ｜ 本词 cli 逐字相同=true
```

读数：**39/39 这条词自己的命令都在命中集里且逐字相同**；6/39 命中多于一条要靠排序挑；**38/39 精确命中在首位**——唯一例外是 `看今日运动`，首位是主页族的 `看今日运动概览`（`calorie.view.exercise`），该词自己的 `calorie.view.exercise-records` 排第 2（见 §七 记账 2）。

---

## 二 · 面 2 · 真出口逐条跑（隔离库）＋ 真库 sha256 对账

### 2.1 现成真出口用例（票面点名的那件）

```
node tooling/run-locked.mjs --ticket 268 -- node packages/skill-calorie/test/exercise-accept-267.test.mjs
```

读数（逐字，`SKILLS_DB_PATH=D:\2Study\StudyNotes\.db` 已设，故 G7 的零接触判据按真库路径实判）：

```
WAIT-OWNER-ALIVE pid=47408 ticket=603 …（活进程一律不抢回，等它释放）
LOCK-ACQUIRED ticket=268 runId=3a77defd-323a-42d1-8a81-6fcb63ce6ccd waitedMs=10001
T267-G0 词=39 命令键=11 页族针=11 读类词(分布/趋势/复盘)=7 写类词=13 声明面=exercise/routes.ts(35)＋home/routes.ts(41)
T267-G1 词=39 exit0=39 绝对路径=39 字节如实=39 完整文档=39 落盘字节域=[74308,176913]
T267-G2 冻结词=39 逐字相同=39 non-exec=0 生成物一致=39 键数=11 分布=1 趋势=1 复盘=5
T267-G3 读类词=7（分布=1 趋势=1 复盘=5） 各拿自己那页=7 页轴两处对账=7 产物零命令键=7 汇总族对照=10 载荷头仍印命令键=12（见遗留出口）
T267-G4 空库阻断=32 空库新增出账=7 落盘文件数=7 阻断后落盘=0
T267-G5 眉标族长=9 产物逐条对族=39 键→模板件=(crud_receipt.html×3 / exercise_summary.html×2 / exercise_goal_view.html / exercise_distribution.html / exercise_strength.html / exercise_cardio.html / exercise_trend.html / exercise_recap.html) 读类三件模板=3 登记共用件=1
T267-G6 写类词=13 完整文档=13 字节域=[75070,83182]
T267-G7 夹具根=C:\Users\辰辰洋洋\AppData\Local\Temp 真库SKILLS_DB_PATH=D:\2Study\StudyNotes\.db 真库零接触=是
ℹ tests 8 ℹ pass 8 ℹ fail 0
T267-RESULT: 8/8 通过 exit=0
RESULT: ticket=268 runId=3a77defd-323a-42d1-8a81-6fcb63ce6ccd waitedMs=10001 exit=0
```

### 2.2 本席独立复跑（自造驱动，起点＝唤醒词）

`node .scratch/t268r/p234.mjs`：逐条 `lookupWake(buildHelpLookup(TRIGGERS), 词)` 取命令 → **命令原样跑**（不补 `--html`）→ 每词一份独立库副本（满种子库 400 天 × 3 条，`daily_goal.exercise_goal=300`，`CALORIE_TODAY=2026-09-07`）。

```
SEED 满种子库=C:\Users\辰辰洋洋\AppData\Local\Temp\t268r-full-1gqIXs 记录号=1200 条数=39
G1 exit0=39/39 绝对路径=39 delivery.path=output=39 delivery.bytes=落盘字节=39 完整文档=39 落盘恰1件=39 起点命令＝冻结表=39
G1 字节域=[74308,176913]
```

完整文档判据逐条过（本席自写：doctype／`charset="utf-8"`／`<style>`／`<script>`／`ilife-page`／无五个未填槽位残留；并另调唯一定义地的 `test/doc-page-assert.mjs::assertDocPage` 交叉核对，两处同为 39/39）。写类 13 词与读类 26 词产物都落 `…\calorie_html\` 下、每词目录恰 1 件 `.html`，`delivery.bytes` ＝ 盘上字节 ＝ 读回字节。

### 2.3 真库 sha256 跑前／跑后（证明没碰真库）

| 读数 | 跑前（13:40:27） | 跑后（13:41:37；收尾复读 13:50:20） | 同值 |
|---|---|---|---|
| 文件 sha256 | `d40d58f50e74e294a11e410b4d9e2142fcbcd8993328a4efb453f1a0d62a1083` | 同左 | **是** |
| 字节 | 3072000 | 3072000 | 是 |
| mtime | `2026-09-15T07:36:54.706Z` | 同左 | 是 |
| `PRAGMA integrity_check` | `{"integrity_check":"ok"}` | 同左 | 是 |
| 表数／逐表行数 | 12 表；`exercise_log=8297 food_log=564 daily_goal=1 …` | 同左 | 是 |

> 记账：`docs/skills/skill-calorie/t267-锁-真出口.md` 与 `t268-验收记录.md` 里记的真库指纹 `57ce9d87…` **已陈化**（真库 9/15 被动过：`food_log` 562→564）。本席以当刻跑前／跑后对为证，不引旧值。

### 2.4 真库现状实况（隔离拷贝，日历＝真实当天 2026-09-16）

只读读数：`daily_goal#1 = {"exercise_goal":null,"calorie_goal":1850}`；`exercise_log` 未删除 8297 行、**最后一条 2026-07-31**；窗口行数 `{今日:0, 昨日:0, 最近7天:0, 最近30天:0, 最近90天:534, 最近365天:564}`。

把它整份拷进临时目录当隔离库，39 条从唤醒词起跑：

```
exit0=16 阻断=23 完整文档=16 字节域=[75714,127597]
阻断成因分组：
  17 × ERR 4: 取数失败（缺失阻断）：无运动记录：<日期> ~ <日期>
  2 × ERR 4: 取数失败（缺失阻断）：无运动记录（<日期>）
  2 × ERR 4: 取数失败（缺失阻断）：未设运动目标（daily_goal.exercise_goal 缺失，先维护运动目标）
  1 × ERR 4: 取数失败（缺失阻断）：昨日无运动记录可复制
  1 × ERR 4: 取数失败（缺失阻断）：无运动记录（<日期>~<日期>）
```

→ 「39 条都能从头走到尾」是**有前提的**：隔离满种子库 39/39；真库现状 16/39（21 条是「窗口里没有记录」，2 条是「没设运动目标」，1 条是「昨天没记录可复制」）。前 21 条＋1 条念一次「记运动」即可满足；**那 2 条的「设目标」当刻没有命令可跑**——见 §六与 §七 记账 4。

---

## 三 · 面 3 · 页面归属（7 条读类词各拿自己那一页）

派生口径＝**模板件名**（`html_template`）：`exercise_distribution.html` 1 条／`exercise_trend.html` 1 条／`exercise_recap.html` 5 条，共 7 条。

| 词 | 模板件名 | 产物眉标 | 产物题面（H1） | 载荷头 | 产物内含 `calorie.view.` |
|---|---|---|---|---|---|
| 看运动类型分布 | `exercise_distribution.html` | 运动类型分布 | 运动类型分布 | `【calorie · 运动类型分布】` | 否 |
| 看运动趋势 | `exercise_trend.html` | 运动趋势 | 运动趋势 2026-08-09 ~ 2026-09-07 | `【calorie · 运动趋势】` | 否 |
| 运动复盘（本周） | `exercise_recap.html` | 运动复盘 | 运动复盘 2026-09-07 ~ 2026-09-07 | `【calorie · 运动复盘】` | 否 |
| 运动复盘（本月） | `exercise_recap.html` | 运动复盘 | 运动复盘 2026-09-01 ~ 2026-09-07 | `【calorie · 运动复盘】` | 否 |
| 运动复盘（最近 90 天） | `exercise_recap.html` | 运动复盘 | 运动复盘 2026-06-10 ~ 2026-09-07 | `【calorie · 运动复盘】` | 否 |
| 运动复盘（今年） | `exercise_recap.html` | 运动复盘 | 运动复盘 2026-01-01 ~ 2026-09-07 | `【calorie · 运动复盘】` | 否 |
| 运动复盘（自定义时间） | `exercise_recap.html` | 运动复盘 | 运动复盘 2026-02-19 ~ 2026-09-07 | `【calorie · 运动复盘】` | 否 |

读数：**7/7 三处（眉标／题面／载荷头）都指自己那一页**，`7/7 产物零命令键`（`calorie.view.` 在产物里 0 次）；对照面：汇总族 10 条词确实拿的是 `运动汇总`（证明「不是总览」不是空判）。

> 面 3 附带订正一条**陈旧登记**：`exercise-accept-267.test.mjs` 的 G3 仍打 `载荷头仍印命令键=12`，那 12 条（汇总 10＋目标 2）走的是判据里的 `COPY_KEY_LEAK` 显式豁免；本席实读这两页载荷头**已是人话**（`【calorie · 运动汇总】`／`【calorie · 运动目标】`，#460 已修）。⇒ 那行读数与豁免面**比实况旧**（见 §七 记账 5）。

---

## 四 · 面 4 · 边界（四档，每档给读数）

### 4.1 空窗（种子只有最近 3 天）

| 词 | exit | 落盘 | 原话 |
|---|---|---|---|
| 看上月运动 | **4** | 0 | `ERR 4: 取数失败（缺失阻断）：无运动记录：2026-08-01 ~ 2026-08-31` |
| 看上周运动 | 0 | 1 | （该周内有记录，正常出页） |
| 看本月运动／看最近 30 天运动／看今日运动／看本周运动 | 0 | 1 | 正常出页 |

空库（只有 schema）下 39 条的分档：`空库阻断=32 空库新增出账=7 落盘文件数=7 阻断后落盘=0`——阻断侧 `exit≠0`、stdout 无 `data.output`、该词目录**零落盘**；新增 7 条如实出账且只消费输入。

### 4.2 无备注筛选（种子 400 天全无备注）

| 词 | exit | 落盘 | 原话 |
|---|---|---|---|
| 看运动记录（有备注） | **4** | 0 | `ERR 4: 取数失败（缺失阻断）：本窗无带备注的运动记录：2026-09-01 ~ 2026-09-07` |
| 看运动记录（按力量筛选） | **4** | 0 | `ERR 4: 取数失败（缺失阻断）：无力量记录：2026-09-01 ~ 2026-09-07` |
| 看今日运动 | 0 | 1 | 正常出页（`1 天有记录`） |

### 4.3 长窗截断（满种子库 400 天）

| 词 | exit | `<tr>` 数 | 截断句（逐字） |
|---|---|---|---|
| 看最近 60 天运动 | 0 | 65 | （无——60 ≤ 100 上限） |
| 看最近 180 天运动 | 0 | 105 | `逐日表已截断：本窗共 180 天，显示最近 100 天，其余 80 天见上方折线` |
| 看最近 365 天运动 | 0 | 105 | `逐日表已截断：本窗共 365 天，显示最近 100 天，其余 265 天见上方折线` |
| 看运动趋势（custom 365 天） | 0 | — | `逐日明细（共 365 天，本表列出 100 天，已截断，只列前 100 天，空缺日「—」不断 0）` |

三数同句（本窗共 N 天／显示最近 100 天／其余 M 天），被截窗的页眉天数不再与表题复读。

### 4.4 跨年轴（A／B 对照：同一条命令只换窗口）

读数取**卡片内**的轴标签（剥标签后的可见文本，`p4c.mjs` 按 `section id` 逐卡取）：

| 页族 | 卡 | 跨年窗 2025-09-08 ~ 2026-09-07 | 同年窗 2026-01-01 ~ 2026-09-07 |
|---|---|---|---|
| 汇总族（看某段时间运动） | `sec-trend`（折线） | `["2025-09-08","2026-09-07"]` ← **印全年** | `["01-01","09-07"]` |
| 趋势族（看运动趋势） | `sec-line`（折线） | `[500,1000,"09-08","09-07","11-20","02-01","04-14","06-26"]` ← **只月日** | `[500,1000,"01-01","09-07","02-20","04-11","05-30","07-19"]` |
| 复盘族（运动复盘（自定义时间）） | `sec-daily`（折线） | `[500,1000,"09-08","09-07","11-20","02-01","04-14","06-26"]` ← **只月日** | `[500,1000,"01-01","09-07","02-20","04-11","05-30","07-19"]` |

- **成立的半边**：汇总族在跨年窗下轴标印全年（`2025-09-08` 对 `2026-09-07`），同年窗退回月日——这正是 `t576-N2`（判据名「跨年轴印全年（09-16 对 09-15 不再同形）」）要守的形状，本席复跑 `t576-n1n8.test.mjs` 亦 8/8 通过（runId `02ba4c4e-289c-40d5-b52b-241d9af301da`）。
- **不成立的半边**：**趋势族与复盘族的折线卡在跨年窗下不印年份**，两枚端点刻度落到 `09-08` 与 `09-07`——正是 N2 在汇总族明令禁止的「同形」症状；`t576-N2` 的页枚举只盖了汇总页（`buildExerciseDoc`），没盖这两族。可达路径是**冻结词自己的参数**：`运动复盘（自定义时间）`（`window:"custom"` ＋ 起止日期）与 `看运动趋势`（同族可传 custom）。

---

## 五 · 面 5 · 变异探针（红绿两行）

驱动 `.scratch/t268r/p5-mutate.mjs`：记原字节 → 变异 → 经 `run-locked` 编译 → 经 `run-locked` 跑判据 → **逐字节还原** → 编译 → 复跑判据 → 比 sha256。还原前先断言盘上还是本席写下的变异字节（被别席动过就中止、不覆盖）。

```
▸ 第 1 行 · 路由声明走散（一处）
  件=packages/skill-calorie/src/exercise/routes.ts ｜ 变异=「看运动趋势」的路由声明 cli 从 calorie.view.exercise-trend 改指 calorie.view.exercise（冻结表一字不动）
  sha256[:12] 原=81d0608f6169 → 变异=2b9aaf66d38c
  变异后：编译 exit=0 ｜ 判据 exit=1 ｜ T267-RESULT: 7/8 通过（红：G2） ｜ ℹ pass 7 ℹ fail 1
    红因：AssertionError [ERR_ASSERTION]: 看运动趋势 两处定义地不一致（冻结表 vs 路由声明）
  还原后：编译 exit=0 ｜ 判据 exit=0 ｜ T267-RESULT: 8/8 通过 ｜ ℹ pass 8 ℹ fail 0 ｜ 逐字节还原=SAME（sha256[:12]=81d0608f6169）

▸ 第 2 行 · 页面装配走散（一处）
  件=packages/skill-calorie/src/render/sportPortDocs.ts ｜ 变异=「运动趋势」页的眉标（页身份）从 运动趋势 改成 运动复盘（页件与命令都不动）
  sha256[:12] 原=5f99f8cb0c76 → 变异=b13447a1b58b
  变异后：编译 exit=0 ｜ 判据 exit=1 ｜ T267-RESULT: 6/8 通过（红：G3；G5） ｜ ℹ pass 6 ℹ fail 2
    红因：AssertionError [ERR_ASSERTION]: 看运动趋势 的页眉不是它自己那一页：实得「运动复盘」，期望「运动趋势」
    红因：AssertionError [ERR_ASSERTION]: 看运动趋势 眉标不是它自己那一族：运动复盘
  还原后：编译 exit=0 ｜ 判据 exit=0 ｜ T267-RESULT: 8/8 通过 ｜ ℹ pass 8 ℹ fail 0 ｜ 逐字节还原=SAME（sha256[:12]=5f99f8cb0c76）
```

**红绿两行**：`红 7/8（G2）`→`绿 8/8`（路由）；`红 6/8（G3；G5）`→`绿 8/8`（页装配）。
处置：两件都已**逐字节还原并复核**（`git status --short -- packages/` 只剩他席在途的 `packages/base-combos/src/present.ts`；两件 sha256 前 12 位 `81d0608f6169`／`5f99f8cb0c76` 与变异前同值），dist 已按还原后的源码重编译（还原后判据 8/8 绿）。

---

## 六 · 特别 · 决策证据（#506：无目标实测原文）

### 6.1 两条词给 AI 的提示原文（冻结表 `prompt_template` 逐字，换行以 `\n` 显现）

```
▸ 看今日运动（vs 目标）
  命令：calorie-cmd-read calorie.view.exercise-goal --params '{"window":"今日"}'
  提示原文："请你加载技能 卡路里,执行唤醒词「看今日运动（vs 目标）」。\n\n我想看今天的运动目标达成情况。如果还没设过每日运动消耗目标,先问我目标值。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。"

▸ 看本周运动（vs 目标）
  命令：calorie-cmd-read calorie.view.exercise-goal --params '{"window":"本周"}'
  提示原文："请你加载技能 卡路里,执行唤醒词「看本周运动（vs 目标）」。\n\n我想看本周的运动目标达成情况。如果还没设过每日运动消耗目标,先问我目标值。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。"
```

### 6.2 无目标（`daily_goal.exercise_goal` 为 NULL、窗内有记录）时的真出口输出原件

```
① 无目标 ▸ 看今日运动（vs 目标） ｜ exit=4 ｜ 落盘=0
  stdout 原件（逐字，长度 0）=""
  stderr 原件（逐字）="ERR 4: 取数失败（缺失阻断）：未设运动目标（daily_goal.exercise_goal 缺失，先维护运动目标）\n"
  输出里有没有点名下一步命令（/calorie\./ 或 /calorie-cmd-read/）＝没有

① 无目标 ▸ 看本周运动（vs 目标） ｜ exit=4 ｜ 落盘=0
  stdout 原件（逐字，长度 0）=""
  stderr 原件（逐字）="ERR 4: 取数失败（缺失阻断）：未设运动目标（daily_goal.exercise_goal 缺失，先维护运动目标）\n"
  输出里有没有点名下一步命令（/calorie\./ 或 /calorie-cmd-read/）＝没有

② 有目标（对照） ▸ 看今日运动（vs 目标） ｜ exit=0 ｜ 字节=74308 ｜ 产物含目标字样
② 有目标（对照） ▸ 看本周运动（vs 目标） ｜ exit=0 ｜ 字节=74308 ｜ 产物含目标字样

③ 连 daily_goal 行都没有 ▸ 看今日运动（vs 目标） ｜ exit=4 ｜ 落盘=0
  stderr 原件="ERR 4: 取数失败（缺失阻断）：未设运动目标（daily_goal.exercise_goal 缺失，先维护运动目标）\n"
```

**给 AI 看的提示只有这一句 `ERR 4: …先维护运动目标`：没有命令名、没有 `calorie-cmd-read`、没有键名。**（`stdout` 是**空串**：失败态不给 envelope，AI 拿不到任何「下一步」字段。）

### 6.3 决定性读数：当刻**没有任何命令能把目标写进去**

```
起手库内 daily_goal = {"exercise_goal":null,"calorie_goal":1800,"water_goal":2000,"weight_goal":null}
▸ goal.set 塞 exerciseGoal ｜ exit=0 ｜ 跑后库内 daily_goal = {"exercise_goal":null,…}   ← 没写进去
▸ goal.water 塞 exerciseGoal ｜ exit=0 ｜ 跑后库内 daily_goal = {"exercise_goal":null,…}   ← 没写进去
▸ goal.weight 塞 exerciseGoal ｜ exit=0 ｜ 跑后库内 daily_goal = {…,"weight_goal":68}      ← 只写了体重目标
全仓命令登记面里提到 exercise／运动目标 的只有： [{"key":"calorie.view.exercise-goal","title":"运动目标视图"}]（读视图）
```

源码侧同向：全 `packages/**/*.ts` 里 `exercise_goal` 命中 **42 处**（其中一半是 `packages\plugin-calorie\node_modules\skill-calorie` 这个 **Junction 同一件的第二次数**），分类读数＝`SELECT` 4 处／`SET … exercise_goal =` **0 处**／`INSERT` **0 处**，其余是注释、类型字段与唤醒词数据；`src/goal/nutritionGoal.ts:70` 与 `src/goal/write.ts:20` 都写明「`exercise_goal` 按原值保持」。

⇒ **实测结论（供 #506 裁决）**：提示词让 AI「先问我目标值」，但那之后**没有任何命令能把用户给的值写进库**；阻断文案也不点名下一步命令。所以「问出目标值」这一步在当刻是**死路**——甲（补预检确认页）与乙（改措辞＋更正地图句）都要面对这一格：单靠改提示或改页面装配都不足以让「设目标」这一步落地，还缺一条写命令（或一处界面写入口）。判定权归负责人，本席只交原件。

---

## 七 · 记账（不压红，但必须点名）

1. **真库现状 16/39**（§2.4）：隔离满种子库 39/39，真库只剩 16 条出页——21 条缺窗内记录、2 条缺运动目标、1 条缺昨日记录。前 21＋1 条念一次「记运动」即可满足；那 2 条见记账 4。
2. **HELP 查找的精确命中不总在首位**（§1.1）：`calorie.help.lookup --params '{"q":"看今日运动"}'` 首条是主页族的 `看今日运动概览`（`calorie.view.exercise`），`看今日运动` 自己那条排第 2。6/39 条命中多于一条要靠排序挑；本词命令都在命中集里（39/39 逐字相同）。
3. **跨年轴只在汇总族成立**（§4.4）：趋势族／复盘族的折线卡跨年不印年份（`09-08` 对 `09-07`），`t576-N2` 的页枚举漏了这两族。可达路径是冻结词自己的 custom 参数。
4. **「每日运动消耗目标」当刻没有写路径**（§6.3）：两条目标词在真库上因此**不可达**（不是缺数据，是缺命令）。
5. **判据里一处陈旧豁免**（§三末）：`exercise-accept-267.test.mjs` 的 `COPY_KEY_LEAK` 显式放行 12 条（汇总 10＋目标 2），而这两页载荷头 #460 之后已是人话；那 12 条现在**本可以通过**更严的判据，却被豁免掉了——覆盖面比实况窄。
6. **一处 CLI 不可达的空态分支**：`src/render/sportDocs.ts:296-306` 给「零记录」备了一整页空态指引（`本段时间没有运动记录` ＋ `先说一句「记运动」…`），但取数层 `home/exercise.ts:31-36 → analysis/exerciseReview.ts:28` 在窗内 0 行时先抛 `missing-data`（实测 `exit 4`，不落盘），故用户从唤醒词走进不到这一页。要么让空窗走空态页（§4.1 的两态口径需要重定），要么撤掉这条分支——记一笔待裁。
7. **两页产物逐字节相同**（沿用 #267 记账）：`看今日运动（vs 目标）` 与 `看本周运动（vs 目标）` 同为 74308 B——`CALORIE_TODAY=2026-09-07` 是周一，当天「本周 ＝ 今日」。**不是缺陷。**

## 八 · 未覆盖面（照实记）

1. **视觉判定未做**：本席只判「文件存在 ＋ 内容字段正确 ＋ 完整文档」，页面的好看／版式归用户肉眼（判定三档的第三档）。
2. **浏览器实开未做**：产物未在真浏览器里渲染过（DOM 级读数由 `t267`／`t576` 两件各自覆盖，本席只读 HTML 文本面）。
3. **会话侧未做**：本席没有从 DSH 会话念词（#268 备料件 `t268-验收记录.md` 第二节的用户原话区仍留空）；「用户念词能打通」这一档本轮的既定口径已改由代码层审查承担。
4. **装机位未复核**：Junction 链（`~\.agents\skills\skill-calorie` 等）沿用 `t268-验收记录.md §1.4` 的读数，本席本轮未重走。
5. **别家技能未碰**：本席只审 `packages/skill-calorie` 场景 04；`packages/base-combos/src/present.ts` 是他席在途件，本席未读未改。
6. **真库未跑任何写词**：39 条里的 13 条写词只在临时库副本上跑；真库全程只读（§2.3 前后同值）。
7. **生成链未重跑 `pnpm gen`**：生成物只做了「与声明逐条对账」，未跑 `pnpm gen:check`（变异第 1 行恰好证明：改声明件而不重生成，判据 G2 会红）。

## 九 · 复跑命令与读数存档

| 步 | 命令 | 读数件（`.scratch/t268r/`，不入提交） |
|---|---|---|
| 编译 | `node tooling/run-locked.mjs --ticket 268 -- node node_modules/typescript/bin/tsc -b packages/skill-calorie` | 输出 `RESULT: … exit=0` |
| 面 1 四轴 | `node .scratch/t268r/p1-parity.mjs` | `p1.log`（`P1-TOTAL 断言=746 通过=746 不符=0`） |
| 面 1 补（真出口 HELP 查找） | `node .scratch/t268r/p7-helpcli.mjs` | `p7.log` |
| 面 2／3／4 | `node .scratch/t268r/p234.mjs` | `p234.log`、`p234-rows.json` |
| 面 4 跨年轴 | `node .scratch/t268r/p4-cross.mjs`／`p4b.mjs`／`p4c.mjs` | `p4-cross.log`／`p4b.log`／`p4c.log` |
| 面 2 官方用例 | `node tooling/run-locked.mjs --ticket 268 -- node packages/skill-calorie/test/exercise-accept-267.test.mjs` | `t267-test.log` |
| N1／N8 页症状（#602 在飞项） | `node tooling/run-locked.mjs --ticket 268 -- node packages/skill-calorie/test/t576-n1n8.test.mjs` | `t576.log`（8/8 通过，`T576-N1 h2=9/9（含#602目标两页2/2）`） |
| 面 5 变异 | `node .scratch/t268r/p5-mutate.mjs`（路由行）；`$env:ONLY="第 1 行"` 选择性复跑 | `p5-mutate.log`／`p5-mutate-route.log` |
| 特别证据 | `node .scratch/t268r/p506-nogoal.mjs` | `p506.log` |
| 真库实况 | `node .scratch/t268r/p6-realdb.mjs`（只读＋副本跑） | `p6.log` |
| 真库指纹 | `node .scratch/t268r/fp.mjs` | `fp-before.log`／`fp-after.log`／`fp-after2.log` |

**#602 在飞项复核（票面点名）**：`看今日运动（vs 目标）`／`看本周运动（vs 目标）` 两页（墙序号 23／24）的读数卡 `sec-figures` **首个子节点＝`数值对照`**，全页 `<h2>` 清单 `["数值对照"]`——**有题**；`t576-n1n8.test.mjs` 亦 8/8 通过。⇒ 本席**没有**看到「无题」那一态。
