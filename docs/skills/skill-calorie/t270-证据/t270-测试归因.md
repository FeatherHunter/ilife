# #270 · 全包测试「不新增红」逐条归因（两态同环境各跑一次）

- BEFORE＝工作树只含 `645aa37` 原样（`src/diet/receipt.ts` 239 行、`test/calorie-c43.test.mjs` 原样）＋ `tsc -b packages/skill-calorie --force`
- AFTER ＝本票交付态（`src/diet/receipt.ts` 379 行、`test/calorie-c43.test.mjs` 收窄、`test/t270-回执四块.test.mjs` 新增）
- 命令：`pnpm test`（同一份 `node_modules`、同一台机、同一批生成物）

BEFORE tests=2381 suites=209 pass=2280 fail=99 cancelled=0 skipped=2
AFTER  tests=2389 suites=209 pass=2289 fail=98 cancelled=0 skipped=2

## 新增红（BEFORE 绿、AFTER 红）
（0 条）

## 由红转绿（BEFORE 红、AFTER 绿）
- #269／#270 饮食写命令已是完整文档，且四块标题（老实物逐字）在产物里

（两态各自的 ✖ 全量清单：`.scratch/t270/fails-before.txt` / `fails-after2.txt`）
