# t479 · 证据件陈化对齐（文本改造后旧口径描述与盘上实现对齐）

票：#479（MAP #159 子票，label `wayfinder:task`）；本件是**纯文档票**的证据件：
把 #472／#473／#474／#476 四票改造**之前**写成的证据件里、已被改造淘汰的**旧口径描述**
改成与当刻盘上实现一致；**一份文件都没删、任何历史读数一字未回改、源码与测试零触碰**。

- 判据（本件 §二）：逐件旧串命中数（改前 → 改后）＋「改后断言式＝0」；脚本
  `.scratch/t479/count-docs.mjs`，读数 `.scratch/t479/counts.md`。
- 交叉验证（本件 §三）：用**盘上真跑产物**核对新写的描述；脚本 `.scratch/t479/run-pages.mjs`，
  产物 `.scratch/t479/pages/*.html`（17 页），原始读数 `.scratch/t479/readings.json`。
- 全部命令跑在隔离库（`mkdtemp` 的临时 `SKILLS_DB_PATH`／`CALORIE_PHOTOS_DIR`，`CALORIE_TODAY=2026-07-10`），
  真库只读零写入；未做任何 `git add`／`commit`／`push`／`stash`／`reset`／`checkout`。
- **读数时刻**：页面真跑产物 `.scratch/t479/readings.json` 落在 `2026-09-15 01:39`（17 页，
  `.scratch/t479/pages/`）；判据读数 `counts.md` 落在 `01:54`。工作树里他席在途改动与本票不相干
  （本票写集只有 9 件文档 ＋ `t287-验收-runbook.md` ＋ 本件 ＋ `.scratch/t479/`）。

## 一、判定与逐件清单（改了哪几处、改前 → 改后）

| 件 | 改动处 | 改前（旧口径） → 改后（当刻实现） |
|---|---|---|
| `t341-页面形状.md` | 3 | ① 形状节：只讲「KPI＋内嵌照片＋缺失明示」→ 加一段**现状注**（眉标整行删、KPI 四格、`notice()` 提示块、消失的旧说法逐串 0 命中），并声明下面十页 sha 与读数是历史事实不回改<br>② 体积节：超限策略写「分页或落盘缩略图，二选一或并用」→ **逐张试嵌**（`embedPhotosWithinBudget`，超额那张去掉内嵌字节），并写明 #472 起那段走公共层 `notice()` 而**不再是自造 `budget-banner` 裸 `div`**、体积上限不上屏<br>③ 缺失口径节：「缺失块写 `缺失照片 N 张：#id 文件名（原因）`／图注带 `文件缺失`」→ 人话四态＋KPI 明细「M 张找不到文件／K 张太大未显示」＋缺失清单逐张一行挂徽标；示例句按 #472 后的用例重写 |
| `t438-体积退让.md` | 2 | ① §一 落点表两行：`galleryDoc.ts:56-61` 体积横幅（N／M 两数）→ `budgetNoticeHtml` 走 `notice()`、句面「本页只显示 N 张／还有 M 张没显示」；占位句「照片未内嵌（原因）」→ `notShownReason` 的人话四态（机器串 `体积预算未内嵌` 不再上屏）。同处加**现状注**：**#438 行为一条未变**（触发与 M 只按预算跳过计数），变的只是呈现；并注明 §二／§三／§八／§十 是当刻历史读数<br>② §十 第 2 条（「已嵌／已嵌入」差一个字、留审查席定）→ 补记**已由 #472 换句收口**，本条不再是待办 |
| `t281-读侧同形.md` | 1（整节，6 条） | 「融合验收对照」节整体重写：眉标整行删；对比页 KPI 收两格（`角度一致吗／照片`）、提醒改人话整句；详情页首尾改「已是第一张／已是最后一张」（不再写「翻页禁用」）；删除入口改「删掉这张照片」＋安全感句＋两条可复制命令块；体积口径写明对比页横幅原样、详情页改一句人话；**并记明详情页 KPI「内嵌」二字仍在**（#473 登记的同族遗留，不是已改过） |
| `t282-回执同形.md` | 6 | ① 顶部现状注（删掉的三件／人话／版面三块）＋§一装配链：页尾那块不再复用共用件 `reconcileDisclosure`，改本域自渲染 `aiCheckDisclosure`<br>② §二 老→新移植清单 8 行按 #476 落地重写（三态对照表、无变化走提示块、空标签写 `无标签`、失败张逐张上页、明细表 ≥3 张或有失败才出）<br>③ §三 保留清单：「间隔行（`distanceBlock`）」→ 状态卡旁的独立小卡「拍照节奏」（并记下它原先恒为死支）<br>④ §四 加读数提示；裁定 5 的三件位置改为现状（记录标识／快照／节奏，两处并进页尾折叠）<br>⑤ §五 唤醒词形态三行改成现文案<br>⑥ §七 超线补记（当场实测 LF：`receipt.ts` 324／`photo.ts` 355，后者已挂号）；§九 审查项 `tagText` 的 `无`→`无标签` |
| `t283-候选与快照.md` | 6 | ① 顶部现状注（眉标改「删照片 · 只看不删」、H1 只留页名、KPI 三格、候选行精简、异常才出声、表注改人话、「内嵌」0 命中）＋目标节的「代表唤醒词」改为「已在 #159 词表订正里去掉」<br>② 裁定 4 的快照表注改现句；体积口径那条改成 KPI 一格的现句并写明该页**不受 1 MiB 预算管**（#461 在办）<br>③ 未做项里的「#285／#345 提示」改成「没有代表唤醒词」<br>④⑤⑥ 逐条路径／提交门／smoke 快照行里出现的 `选身材照` 字样，逐条注明是 #283 当刻记录、现盘上已无此词 |
| `t345-入口与流程.md` | 2 | ① §二 加现状注：#450 词表订正把两个自造页面名改回页面本来的叫法（「预检确认页（`calorie-cmd-read calorie.view.photo-log-wizard`）」／「候选页（`…view.photo-picker`）」），并说明该页命令本来就没有代表唤醒词<br>② §二 表格里那三格 prompt 引文按当刻 `scene-09-photo.ts` 原文改掉「看身材照向导」「选身材照」 |
| `t285-工作流落点.md` | 2 | ① t400 基准对照节加**核对注**：本件用词与当刻一致（`wake-assets.ts`／`SKILL.md`／`workflows/09-身材照片.md` 现无那两个自造词）；并区分「披露文件的口径措辞」与「#472／#473 换掉的页面句面」，披露文件属包内说明面、本票不动<br>② 逐条路径节加说明：段内 `选身材照` 是 #285 当刻写集记录 |
| `t287-验收-runbook.md` | 5 | ① 顶部现状注（四票改了什么／§一与§三已按当刻重写／其余各节是彩排当刻读数，拿新产物比对时字节必然不同）<br>② §一「期望页形态」10 行按现文案逐句重写（含各页句面、KPI 格数、提示块原句、状态列、失败张上页、标签三态、「硬删除，不可恢复」已不上屏等）<br>③ §二 追加打勾项 10 行同步（提示块原句／首尾人话／三态列／异常才出声／节奏小卡等）<br>④ §三「已知正常异常」5 条重写（8 张找不到文件的三处归位、体积让位三处归位、候选页体积与 #461、两张假图已被 #341 整改删除、GIF 产物小）<br>⑤ §四 节头与 ① 两条提示：本节是彩排当刻读数、判据是「页 ≤ 上限＋两数报得出」 |
| `t400-融合基准.md` | 5 | ① 裁定 3：结论句的「已嵌入 N 张／还有 M 张未嵌入」＋「网格三态」按 #472 换句改写（提示块原句、三类去向、对比页那句横幅仍在），可判形式同步<br>② §二序 3：移植项与落点按当刻改（GIF 结果页落 `gifDoc.ts`，`html.ts` 的 `renderGifHtml` 已不被本命令调用）<br>③ §六 A 表：第 1 行「页眉＝场景名＋命令名＋唤醒词」改成现状（眉标改人话／整行删，唤醒词不进页面），第 2 行候选表列名改当刻<br>④ §六 B 表 3 图区行：网格三态 → 三类去向<br>⑤ §六-补 表前加现状注（眉标／KPI／口径词三处），命令名与唤醒词改按文件（不钉会漂的行号） |
| `t352-结果页内嵌.md` | 3 | ① 顶部现状注：眉标删、KPI 十格→三格、`cs` 与上限下屏、文件只显名＋「复制路径」块、状态列改人话、表注改帧序句；**并明确「页内嵌字节与盘上文件同源」这句已从页面删掉、但测试仍断言同源**<br>② 能力边界那条同源句加括注（是测试断言，页上不印）<br>③ 改法行：`gifDoc.ts` 271 行 → 补当场实测 304 行（仍在 350 内） |

机器读数（`git diff --numstat`，9 件为 git 跟踪件；`t287-验收-runbook.md` 未被 git 跟踪，故只报处数）：

```
t341-页面形状.md  +27/-6    t282-回执同形.md    +50/-17   t285-工作流落点.md  +10/-0
t438-体积退让.md  +16/-4    t283-候选与快照.md  +24/-7    t400-融合基准.md    +31/-9
t281-读侧同形.md  +25/-10   t345-入口与流程.md  +12/-3    t352-结果页内嵌.md  +14/-2
```

## 二、判据读数：逐件旧串命中数（改前 → 改后）

完整表见 `.scratch/t479/counts.md`（**44 行 = 10 件 × 各自旧串，逐行带「改后断言式」列**）。
下表照抄其中与本轮淘汰说法直接相关的行（读数与 counts.md 逐字一致）：

| 件 | 旧串 | 改前 | 改后 | 改后「断言式」 |
|---|---|---|---|---|
| t341 | `缺失照片 N 张` | 1 | 2 | 0 |
| t341 | `文件缺失` | 1 | 1 | 0 |
| t341 | `分页` | 1 | 1 | 0 |
| t341 | `budget-banner` | 0 | 1 | 0 |
| t438 | `已嵌 N 张` | 4 | 4 | 0 |
| t438 | `已嵌入 N 张` | 1 | 0 | 0 |
| t438 | `网格三态` | 1 | 1 | 0 |
| t281 | `可比性`／`按日期正序`／`翻页禁用` | 0／0／0 | 1／1／1 | 0／0／0 |
| t282 | `distanceBlock`／`间隔行` | 1／1 | 0／0 | 0／0 |
| t282 | `影响行数`／`写入字段`／`回执格式` | 0／0／0 | 2／1／1 | 0／0／0 |
| t282 | `徽章：`／`reconcileDisclosure`／`空标签灰底` | 0／1／1 | 2／1／1 | 0／0／0 |
| t282 | `未产生实际变化`／`标签(未变化)`／`无变化降级` | 2／2／3 | 3／3／3 | 0／0／0 |
| t283 | `选身材照` | 5 | 4 | 0 |
| t283 | `只读，删除走写命令` | 1 | 1 | 0 |
| t345 | `选身材照`／`看身材照向导` | 2／1 | 1／1 | 0／0 |
| t285 | `选身材照`／`看身材照向导` | 1／0 | 3／1 | 0／0 |
| t287 | `照片未内嵌（文件缺失：` | 2 | 1 | 0 |
| t287 | `体积横幅`／`内嵌 N/总数 张` | 1／0 | 1／0 | 0／0 |
| t287 | `可比性`／`翻页禁用` | 6／2 | 6／3 | 0／0 |
| t287 | `硬删除，不可恢复`／`已嵌 3 张／还有 1 张未嵌入` | 2／2 | 2／2 | 0／0 |
| t400 | `已嵌入 N 张` | 1 | 2 | 0 |
| t400 | `网格三态`／`场景名＋命令名＋唤醒词` | 5／1 | 4／1 | 0／0 |
| t352 | `十格`／`页内嵌字节与盘上文件同源` | 0／0 | 1／1 | 0／0 |

判据跑法（可复核）：`node .scratch/t479/count-docs.mjs` → `WROTE counts.md rows=44 assertiveAfter=0`。

- 「改后」列非零不等于冲突：**旧串被留在文档里的，都是「这条已换掉／这是历史读数」的行**
  （本票新加的现状注、当刻测试 stdout、老侧引用），脚本按行判「解释式」并把每一条理由登记在
  `counts.md` 的「允许残留」表（14 条，逐条给理由）。
- 「断言式」＝仍在把旧说法当「当前页面长这样」讲的行；**44 行全 0**。
- `t287` 的改前读数取自本票开工当刻跑出的 `old-strings-before.json`（该件未被 git 跟踪，无 HEAD 版本可比）；
  其余各件的改前一律取 `git show HEAD:<件>`。

## 三、抽样交叉验证（3 处要求，实做 4 处；全部用盘上真跑产物）

跑法：`node .scratch/t479/run-pages.mjs`（隔离库：4 张照片＝正面 3 张含 2 张 700 KB 大图、侧面 1 张；
其中正面第一张的本体删掉造「找不到文件」；`CALORIE_TODAY=2026-07-10`）。

1. **看身材照的提示块与 KPI 明细（对应 t341②③、t438①、t287 §一第 1 行）**
   - 新描述写的是：提示块「照片较多，本页只显示 N 张；还有 M 张没显示」＋「可按「标签」或日期分批看」；
     KPI「本页」格 `N 张已显示`、明细「M 张找不到文件 · K 张太大未显示」。
   - 真跑读数（`.scratch/t479/pages/看身材照.html`，1,024,583 字节）：可见文本逐行是
     「照片较多，本页只显示 1 张；还有 1 张没显示」「可按「标签」或日期分批看」「本页」「1 张已显示」
     「1 张找不到文件 · 1 张太大未显示」；旧串 `内嵌`／`缺失`／`单页上限`／`1048576`／`超预算未嵌`／
     `体积预算`／`照片未内嵌` **逐串 0 命中**。
2. **详情页首尾位与 KPI「内嵌」遗留（对应 t281 那条「#473 登记的同源遗留」）**
   - 新描述写的是：首／尾位印「已是第一张」「已是最后一张」（不再写「翻页禁用」）；
     「内嵌」二字**仍在** KPI 里。
   - 真跑读数：`查身材照-首张.html`（65,740 B）「已是第一张」=1、「下一张 #2 →」=1、
     「照片没显示」=1；`查身材照-尾张.html`（1,021,481 B）「已是最后一张」=1、「← 上一张 #2」=1；
     两页「内嵌」=1、「翻页禁用」=0。**若照票面转述写成「KPI 已去内嵌」就是错的，故此处按实现写。**
3. **GIF 结果页三格 KPI 与「同源句不上屏」（对应 t352 现状注）**
   - 新描述写的是：KPI 三格＝`合成（M 张／M 帧 · 每帧停 0.5 秒）／尺寸（W×H／大小 x KB）／文件（<标签>_<起>_<止>.gif）`；
     文件只显名＋「复制路径」块；「页内嵌字节与盘上文件同源」不上屏但测试仍断言同源。
   - 真跑读数（`生成身材照GIF.html`，66,907 B，标签 正面、`days=3650`）：可见文本逐行是
     「标签「正面」 · 2026-05-30 ~ 2026-06-20 · 3 张里挑出 2 张能用的合成（1 张找不到文件）」
     「合成」「2 张」「2 帧 · 每帧停 0.5 秒」「尺寸」「64×64」「大小 0.2 KB」「文件」
     「正面_2026-05-30_2026-06-20.gif」「重跑同标签同跨度会覆盖这个文件」「复制路径」；
     旧串 `页内嵌字节与盘上文件同源`／`50cs`／`上限 512000B`／`画布边上限`／`入片`／`时间跨度` **逐串 0 命中**。
   - 同源（测试侧口径）未受影响：`photo-gif-page-352.test.mjs` 用例③仍按长度＋sha256 断言页内与盘上同源。
4. **（加做）删身材照回执的「永久删除」口径（对应 t282 现状注、t287 §一第 10 行）**
   - 新描述写的是：页面不再印「硬删除，不可恢复」，「永久删除，无法恢复」全页只 1 处（删除快照块标题）。
   - 真跑读数（`回执-删身材照.html`，65,921 B）：「硬删除，不可恢复」=0、「永久删除，无法恢复」=1、
     「照片和记录都已经删掉了」=1、「给 AI 核对的信息」=1、「数据库改了 1 行（给 AI 核对用）」=1；
     `影响行数`／`写入字段`／`对账信息`／`body_photos`／`徽章：` 逐串 0 命中。

## 四、遗留出口与建议（不删文件）

1. **建议标注为历史件（本票未动，交项目负责人裁定）**：`docs/skills/skill-calorie/t156-新仓HTML盘点-身材照片.md`
   是 #341 施工**之前**的新仓盘点快照（件头写明「工作树 HEAD = `bee1451`」），其 `:160` 记的
   「`galleryDoc.ts` 的眉标仍写 `calorie.photo.list · 身材照片域`」等问题**已由 #472 解决**，
   该件描述的页面形状整份都已不是当刻实现。它不是本轮点名件、且性质与 `t313-*` 同属施工前快照，
   故本票**只登记不删、也不改一个字**；建议在件头加一句「本件是 #341 施工前盘点，页面形状以 t341／t472 起为准」。
2. **#461（删照候选页体积退让）仍在办**：`t287` §三 与 `t283` 体积那条已写明该页不受 1 MiB 预算管，
   #461 落地后这两处的描述需要再跟一次（本票只按当刻实现写）。
3. **同源遗留（不是本票写集）**：详情页 KPI 的「内嵌」二字仍是专业话（#473 当场登记，票面只点了对比页那一格）。
   本票只把这个事实写准，**没有替 #473 改口**；要不要改词归后续文本票。
4. **披露文件与 SKILL.md 的句面**：`packages/skill-calorie/workflows/09-身材照片.md`／`SKILL.md` 里仍有
   「网格三态」「过大降级」「间隔行」这类**口径词**（不是页面句面）。它们属包内说明面（源码面），
   本票一行未碰，只在 `t285` 里登记了「口径词 vs 页面句面」的关系。

## 五、未做项与下一手

- 未删任何文件；未改任何历史读数（sha／字节／行数／GATE-RUN／mtime）；未动 `t313-*` 等施工前快照。
- 未动源码与测试：`git status` 里除本件与 9 件文档外，本票零新增改动（工作树里原有的源码改动都是他席在途件）。
- 未做 git 写操作（无 `add`／`commit`／`push`／`stash`／`reset`／`checkout`）。
- 下一手：① 要不要给 `t156-新仓HTML盘点-身材照片.md` 加「历史件」标注，归项目负责人定；
  ② #461 落地后回跟 `t283`／`t287` 两处体积描述；③ 详情页 KPI「内嵌」改词另开文本票。
- **发现但未动的既有格式瑕疵（点到即止）**：`t400-融合基准.md` §二 移植清单表的表头是 5 列、数据行却是
  6 格（多一格「归属票」）——**HEAD 版本逐行同形**（本票跑表格体检脚本 `.scratch/t479/table-check.mjs`
  对 HEAD 与工作树各测一次，读数一致），属落笔时就有的写法，不属本轮陈化描述，故本票只登记不改。
  体检结论：其余 10 件（含本件）表块列数一致、`## `／`### ` 标题均独占一行（`TABLE-CHECK` 只报这一处）。
- 草稿与读数：`.scratch/t479/`（`run-pages.mjs`／`summarize.mjs`／`count-docs.mjs`／`old-count.mjs`／
  `readings.json`／`summary.txt`／`counts.md`／`old-strings-before|after.json`／`pages/` 17 页）。
  票面草稿 `issue-body.md` 同目录。

本件格式自检：无字节顺序标记；`## ` 章节标题独占一行；正文不含字面转义换行两字符；
用词按仓规 `docs/agents/wording.md`（全文用**命令／跑通／过程型／结果型／回执／预检确认页／页面交付**，
未用黑话词）。
