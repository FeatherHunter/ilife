# #342 收口（150/151 搬迁）·证据（docs/skills/skill-calorie/t342-收口-证据.md）

> 地图 #156 子票 #342 收口席 · assignee FeatherHunter · state OPEN · 进度 85% → 尾巴。
> 以票面 + `.scratch/t342/handoff.md` 为准，只做本票尾巴，不开新票。

## 0. 路径集合复述（开始前声明即锁）

允许写：`packages/skill-calorie/src/exercise/routes.ts`、`packages/skill-calorie/src/home/routes.ts`（仅搬 2 条）、
`packages/skill-calorie/src/triggers/scene-04-exercise.ts`（仅该 2 条 `cli`/`data_source` 逐字同步）、
`packages/skill-calorie/test/exercise-records-342.test.mjs`（今日/昨日升级为路由断言）、
parity 三件（`help-center-106`、`routing-81`、`t81 snapshot`，待树干净手跟，本次若树脏则不动）、
证据 `docs/skills/skill-calorie/t342-收口-*`、草稿 `.scratch/t342-收口/`、changeset `.changeset/342-收口-*.md`。
禁写：他票件、`commands` 行为、生成物手改、分派层。

开工前核验：`git status --short -- home/routes.ts exercise/routes.ts scene-04-exercise.ts exercise-records-342.test.mjs` 为空；
`.scratch/locks` 当刻无 `gate.lock` 独占（若有持锁者则经 `run-locked` 排队，撞同文件窗口即停报）。
跨场景 01 #162 地盘：150/151 原属 `home`（`calorie.view.exercise` 键分区归 01），本次经 `run-locked --ticket 342` 抢锁+广播后搬迁。

## 1. 搬迁（order 不动，余窗口词不动）

- `src/home/routes.ts`：删 `order 150 看今日运动`、`151 看昨日运动` 两行（其余 152-158/164-166 等一字不动）。
- `src/exercise/routes.ts`：在 149 与 159 之间插入同 `order` 两条，改指
  `calorie.view.exercise-records --params '{"window":"今日"}'` / `'{"window":"昨日"}'`；
  头注释 33→35 条、12→10 条余量同步更新。
- `src/triggers/scene-04-exercise.ts`：`看今日运动`/`看昨日运动` 两行 `main_prompt.cli` + `data_source`
  由 `calorie.view.exercise` 改为 `calorie.view.exercise-records`（窗口不变），与路由逐字一致。
- `test/exercise-records-342.test.mjs`：第 5 条由“新命令直跑”升级为
  `routesFor('看今日运动')` / `routesFor('看昨日运动')` 有 exec 且 `key === calorie.view.exercise-records`，
  再实跑今日/昨日窗口是记录页。

## 2. 验收（持锁跑，长命令落盘尾 5 行，GATE-RUN 声明导出）

日志在 `.scratch/t342-收口/`（每份尾 5 行已验），对账导出见 `t342-收口-gate-runs.md`。

GATE-RUN runId=t342-shoukou-build1 cmd="pnpm build" exit=0
GATE-RUN runId=t342-shoukou-gen1 cmd="pnpm gen" exit=0（键 118=写 46+读 72）
GATE-RUN runId=t342-shoukou-build2 cmd="pnpm build" exit=0
GATE-RUN runId=t342-shoukou-helpbuild cmd="pnpm help:build" exit=0
GATE-RUN runId=t342-shoukou-gencheck cmd="pnpm gen:check" exit=0（GEN-CHECK PASS 键 118）
GATE-RUN runId=t342-shoukou-t342 cmd="node packages/skill-calorie/test/exercise-records-342.test.mjs" exit=0（pass 5/fail 0）
GATE-RUN runId=t342-shoukou-mutred2 cmd="node packages/skill-calorie/test/exercise-records-342.test.mjs" exit=1（变异红：看今日运动应指新命令，实测旧键）
GATE-RUN runId=t342-shoukou-restore-green cmd="node packages/skill-calorie/test/exercise-records-342.test.mjs" exit=0（还原绿 pass 5/fail 0）
GATE-RUN runId=t342-shoukou-final-help cmd="pnpm help:build" exit=0
GATE-RUN runId=t342-shoukou-final-gencheck cmd="pnpm gen:check" exit=0（终态 GEN-CHECK PASS 键 118）
- 自证：改回旧指（150/151 指回 `calorie.view.exercise`）→ 342 测试变红；改回新指 → 全绿。

## 3. parity（待树干净手跟，本次若脏则列未做）

- `help-center-106` 场景数、`routing-81`（exec 数/有备注 exit4 登记）、`t81 snapshot` 重跑（含本票新 cli）。
  当刻树上有他人 28 件脏（含上述两测试），若仍脏则本次不动，列入未做项交编排串行收口。
- `cmd-write-40` / `t97` 不动（本票是读命令）。

## 4. 结构对账（structure.md 必报五步）

- 影响清单：见 §0（4 件源码+测试+证据+changeset）；交付对账逐行对，无偏差。
- 铁律：能力自治（运动记录随键归位，不抄 01 实现）；概念唯一（cli 事实住路由声明，冻结逐字跟）；改动可预告（清单即锁）；
  名字取自 HELP（看今日/昨日运动现成说法）；接口小里面厚（本次未加接口）。
- 行数：`records.ts` 199LF、`commands.ts` 35LF、`routes.ts` 57LF，均未超 350，无需重构。
- 用词：命令/跑通/页面交付/help模板，无禁词。
