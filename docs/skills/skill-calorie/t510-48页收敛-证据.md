# #510 48页收敛证据（接管席，2026-09-16）

> 写集（本席声明三处内两处落盘，第三处为预留空位不扩散）：`packages/skill-calorie/src/goal/goalWeightDoc.ts`（跟进席遗留1行，收成 `caption: '体重目标'`）、`docs/skills/skill-calorie/t510-48页收敛-证据.md`（本件）。不碰 `weightUi.ts`（已交 `c090d301`）、`shared/*`、`base-render/*`、命令路由、眉标、复制区、别席件。

## 1. 现场量（该行 diff＋语法自证＋哈希）

- diff（工作区未提交，`git diff -- packages/skill-calorie/src/goal/goalWeightDoc.ts`）：
  - `-    caption: '体重目标（目标 ' + fmtKg(g.weightGoal) + ' · 最新 ' + fmtKg(g.latestKg) + '）',`
  - `+    caption: '体重目标',`
  - 位置 `packages/skill-calorie/src/goal/goalWeightDoc.ts:92`，1行1删1增，布局行数 LF=119。
- 哈希（`python hashlib.sha256`）：`92e884f462bb7e14b87fab30278e80fea22f7feb396670d1ca36a658940bdf0f`，字节 6157。
- 语法自证（持锁）：
  - `node node_modules/typescript/bin/tsc -b packages/skill-calorie` → `GATE-RUN runId=fd9316ca-7fa0-4b8b-a5be-fe6e0aa1770a waitedMs=0 exit=0`
  - `node --check packages/skill-calorie/dist/goal/goalWeightDoc.js` → `GATE-RUN runId=73f8518d-6fd5-4c96-aed9-481aa4c5a411 waitedMs=0 exit=0`
  - `dist` 已含 `caption: '体重目标',`（与源码一致，非 stale）。
- 线索：`.scratch/t510f/`（`probe-48/render-48/scan-48` 日志、`out-48`、`final-out`）；前席 `c090d301` 后终扫 `1/58`，超标页仅 48（`两端值·值 + KPI卡·值 + 表题 摘要面3次`，落点 `goalWeightDoc.ts:62/71/92`）。

## 2. 独立判定（caption 去数后事实仍由两端值＋KPI 卡承载）

- 去数前表题 `体重目标（目标 68 kg · 最新 70.4 kg）` 与两端值、KPI 卡值同事实三处 → 摘要面 3 次（冗余）。
- 去数后表题仅 `体重目标`（纯标题，无数字无 `·`），事实落点：
  - 正文首件 `pairStrip('目标 68 kg','最新 70.4 kg','vs')`（`goalWeightDoc.ts:62`）：两端值各一处；
  - `renderKpiGrid` 中 `体重目标 68kg`（`:66`）＋ `最新体重 70.4kg`（`:71`）：KPI 卡值各一处；
  - 数据表体 `68 kg / 70.4 kg` 为数据面（判据只计摘要面，不进阈值）。
- 当刻出页自证（`.scratch/t510f/out-48/48-对比体重-当前-vs-目标体重.html`）：`<caption class="ilife-block-data-table-caption">体重目标</caption>`；`wui-pair-v` 仍为 `目标 68 kg / 最新 70.4 kg`；`kpi-card-value` 仍为 `68 / 70.4`。结论：去数去 `·` 不丢事实，符合“表题／两端值／卡值三处去一处”。

## 3. 持锁重跑（48 页探针摘要面≤2＋全量终扫目标 0/58）

- 48 页出页：`node .scratch/t154/text-review/render-family.mjs --seqs 48 --out .scratch/t510f/out-48` → `GATE-RUN runId=6ea81c00-4fb3-46f7-a7df-b01cf488f43a exit=0`，`TALLY {"通过":1} TOTAL 1 HTML=1`，`bytes=73452`。
- 48 页探针：`PROBE_DIR=.scratch/t510f/out-48 python .scratch/t154/text-review/rev-fix/probe-redundancy.py` → `GATE-RUN runId=bfdba7f7-66af-4571-be50-9c3c6147ee65 exit=0`：
  - `48-对比体重-当前-vs-目标体重 关键数字 ｜ 70.4 ｜ 两端值·值 + KPI卡·值 ｜ 摘要面 2 次（数据面 1／元信息面 0）`
  - `冗余页数 = 0 / 58（摘要面 ≥3 次事实的页）`（探针分母写死 58，实扫 `##`=1）。
- 48 页分隔符扫描：`python .scratch/t154/text-review/scan-separators.py --dir .scratch/t510f/out-48` → `GATE-RUN runId=72546a5a-896e-4b80-8601-0dd295c6e19d exit=0`：`含分隔符串的页：1 页；合计 1 处 48-对比体重-当前-vs-目标体重 1 {'·': 1}`，该 1 处为 `docTitle 卡路里·目标管理`（全 58 页基线，见下），表题 `·` 已清零（旧页该目录计数为 2：`卡路里·目标管理`＋`体重目标（目标 68 kg · 最新 70.4 kg）`）。
- 全量出页：`--seqs 1..58 --out .scratch/t510f/final-out` → `GATE-RUN runId=66d73677-6741-4d35-b6ca-c6192b6dd8a5 exit=0`，`TALLY {"通过":58} TOTAL 58 HTML=58`。
- 全量终扫：`PROBE_DIR=.scratch/t510f/final-out python .../probe-redundancy.py` → `GATE-RUN runId=f492bee6-6558-48d5-aeb1-5947e79e176d exit=0`：`冗余页数 = 0 / 58`，48 页行同上为 `摘要面 2 次`，其余 57 页均 ≤2（`无 ≥2 次事实` 或 `2 次`，无 `★`）。
- 全量分隔符扫描（基线说明）：`--dir .scratch/t510f/final-out` → `GATE-RUN runId=72550e86-59bc-49a4-a544-16b50dc16abf exit=0`：`58 页；合计 58 处`，每页各 1 处 `{'·': 1}` 均为 `docTitle`（如 `卡路里·目标管理`），票面不许动眉标／标题，该基线非本票判据；本票判据（探针冗余）已 `0/58` 全绿。

## 4. 变异红绿两行（持锁）

- `MUT-RED old-caption含·=1 (预期红, 回退则探针摘要面3次)` → `GATE-RUN runId=1415f2fb-bee4-47ce-948a-e34849a29725 exit=0`（旧串 `体重目标（目标 68 kg · 最新 70.4 kg）` 含 `·`，对应回退态探针 `★ 摘要面 3 次`，见 `.scratch/t510/probe-final.log`）。
- `MUT-GREEN caption=[体重目标]` ＋ `MUT-GREEN caption无·=1 (绿, 探针摘要面2次)` → `GATE-RUN runId=f94269a7-223d-49cc-9707-9374a1451787 exit=0`（直调 `dist/goal/goalWeightDoc.js::buildGoalWeightDoc`  mock `68/70.4/30天`，断言 `cap==='体重目标'` 且不含 `·`）。

## 5. 提交（持锁，暂存区仅本席两件）

- `git add packages/skill-calorie/src/goal/goalWeightDoc.ts docs/skills/skill-calorie/t510-48页收敛-证据.md`（持锁，`git diff --cached --name-only` 复核仅此两件）
- `git commit -m "feat(510): …" -- <两路径>`（中文提交，`--` 限路径，不碰别席件）
- 本件落盘后回读自证通过再提交。

## 6. 结论

- 全绿：编译 0、48 页探针 2 次（≤2）、全量探针 0/58、变异红绿各 1 行、扫描表题 `·` 清零（残留 1 处为全页基线标题，非判据）。
- 下一步：贴票面 `##进度：100%` 正文＋决议评论，关 `#510`。
