# #552 运动页去文 · 证据（Part of #162，用户点名运动页标题带日期/副题程序员腔/口径文字墙）

- 基线 HEAD：`9b27549`（开工 `git status` 见 §一）。
- 写集（只这些）：`packages/skill-calorie/src/render/sportDocs.ts`（buildExerciseDoc 一支）、
  `packages/skill-calorie/test/exercise-text-shape-523.test.mjs`、
  `packages/skill-calorie/test/exercise-summary-goal-fusion-452.test.mjs`（只改矛盾断言，逐条引裁决）、本票 `552-*`。
- 禁区（一行未碰，`git status` 自证）：`sportPortDocs.ts`、`exercise/`、`today.ts`、`routes.ts`、公共层、对照目标页分支。
- 锁协议：编译/测试/提交/变异重建一律 `node tooling/run-locked.mjs --ticket 552 -- <命令>`。
- 长输出落本票日志（`552-*.log`），正文只看尾 5 行。

## 一、让路检查（开工第一条）

开工 `git status --porcelain` 未见 `packages/skill-calorie/src/render/sportDocs.ts` 改动——
无他人在途，可开工。`sportPortDocs.ts` 盘上有他人 `M`，本票一行不碰。

## 二、必报五步

### 第一步 · 影响清单（动手前）

| 文件 | 一句话 | 理由 |
|---|---|---|
| `packages/skill-calorie/src/render/sportDocs.ts` | 运动汇总去文：标题去日期/副题删/口径来源整块删 | 票面点名的唯一源码件（buildExerciseDoc 一支） |
| `packages/skill-calorie/test/exercise-text-shape-523.test.mjs` | 变异②换锚点（原口径锚点已删） | 只改与本支矛盾的断言（裁决见 §三） |
| `packages/skill-calorie/test/exercise-summary-goal-fusion-452.test.mjs` | H1 去日期/口径来源改断消失 | 只改与本支矛盾的断言（裁决见 §三） |
| `docs/skills/skill-calorie/552-*` | 本证据组 | 票面证据落点 |

### 第二步 · 结构设计（无新目录）

- 不新增目录，不碰三名以上能力：只动运动域自己一件。
- 对外接口不变：`buildExerciseDoc`/`buildExerciseGoalDoc` 签名零增删；删 `summaryCalibers`（仅本支用）、删 `SOURCE_LOGGED`（汇总来源块删后零引用；目标页 `footFacts`/`SOURCE_GOAL` 保留）。
- 窗口由 `windowStrip` 承载（既有形状，不重造）；`subtitle:null` 即副题元素消失（`assembleDocPage` 空即不出）。
- 超线：第 4 步见 §五。

### 第三步 · 写代码（diff 见 §三）

### 第四步 · 超线报警（见 §五）

### 第五步 · 交付对账

事前清单（上表）与事后 `git show --stat` 逐行对，偏差为零；禁区零改动。

## 三、改了什么

### 源码 `sportDocs.ts`（buildExerciseDoc 一支）

1. 标题 `line286`：`'运动汇总 ' + rangeText(v.start, v.end)` → `'运动汇总'`。
   用户裁决“标题无日期”（票面判据；issue #552“标题去时间”）。
2. 副题 `line302` 整行删：`subtitle: '这段时间的运动量，按消耗、时长与类型分布同窗直出'` → `subtitle: null`。
   用户裁决“副题元素消失”（票面判据；issue #552“无价值的解释句删掉”）。窗口由 `windowStrip` 承载。
3. `line305`：`pageBody(cards, summaryCalibers(v), source, copy)` → `pageBody(cards, [], '', copy)`；
   `summaryCalibers` 整函数删（仅此处用）；`const source = footFacts(SOURCE_LOGGED, …)` 整行删；
   `SOURCE_LOGGED` 一并清（删后本件零引用）；`footFacts` 保留（目标页 `line413` 在用）。
   用户裁决“口径/来源块消失”（票面判据；issue #552“口径文字去冗余”）。

### 测试（只改矛盾断言，逐条引裁决）

- `452.test.mjs:255`：`startsWith('运动汇总 ')` → `equal('运动汇总')`。
  用户裁决“标题无日期”（票面判据）。
- `452.test.mjs:273-277`：`sourceFootnote`/`calibers>=2`/`按 N 天画` → `calibers==0`/`!includes('数据来源')`/`!includes('按 N 天画')`。
  用户裁决“口径/来源块消失”（票面判据）。
- `523.test.mjs:232`：变异②锚点 `消耗＝运动记录上报值合计` → `按日消耗`。
  用户裁决“口径/来源块消失”（票面判据；原锚点已随口径块删，只换锚点，`；`必红口径不变）。

## 四、判据读数

- 标题无日期：`T552 TITLE h1=运动汇总 hasDate=0`（`552-probe.log`）。
- 副题元素消失：`T552 SUBTITLE hasSub=0`（同上；`assembleDocPage` 传 `subtitle:null`）。
- 口径/来源块消失：`T552 CALIBER n=0`、`T552 SOURCE hasSource=0`（同上）。
- 390 无横滑：`OVERFLOW-ZERO pages=1 cells=1 failed=0`（`552-390.log`，`measure-responsive --widths 390`）。
- 变异红绿：见 §六（两行读数）。
- 用户肉眼终审：样张 `552-exercise.html`（双击即看），待用户终审。

## 五、超线报警

已超线，需要根据规则进行重构。`sportDocs.ts` LF（`split('\n').length − 1` 口径）423 → 417，
本次净 −6 行，未新增超线（已超线在册；拆法照台账既有项＝按页切 `sportSummaryDocs.ts`/`sportGoalDocs.ts`，
共件提 `sportPageParts.ts`，待收口票；本次先不拆，拆分不在 #552 写集）。
测试文件与证据件不在扫描面内。
附带门禁说明（非本票红）：`check-warning-line.mjs` 只读跑 `RESULT: 84/86`，
两条 `RED 台账陈化`——`src/render/sportDocs.ts 台账=423 实况=417` 系本票净 −6 行所致、
`src/home/goalProgressDocs.ts 台账=452 实况=446` 系他席在途所致；台账文件不在本票写集，不归本票。

## 六、变异自证（改坏一行必红、还原必绿）

- 改坏：标题行塞回日期（`'运动汇总'` → `'运动汇总 ' + v.start`）→ 重建 → `552-mut-red.log` exit=1，`T552 TITLE hasDate=1` 红在标题判据。
- 还原 → 重建 → `552-mut-green.log` exit=0，`T552 TITLE h1=运动汇总 hasDate=0` 绿。
- 两行读数见 `552-mutation.log` 尾（`MUT-RED exit=1` / `MUT-GREEN exit=0`）。

## 七、提交（不推送）

- `修复(552): 运动页去标题日期副题口径 + sportDocs.ts/判据全绿`
- 只提交声明路径；推送一律不推，由总工程师统一收口。

## 八、未做项

- `sportPortDocs.ts`、`exercise/`、`today.ts`、`routes.ts`、公共层、对照目标页分支：一行未碰。
- 390 之外档（768/1440）未量（票面只要求 390）。
- 用户肉眼终审待收（样张见上）。
