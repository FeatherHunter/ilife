# t467 重做 · 证据

票：[#467 卡路里场景 01：看今日目标进度 整页重做（用户判定质量特别差）](https://github.com/FeatherHunter/ilife/issues/467)

一句话：这一页从 **1,666 字节的片段**（无 `<!doctype html>`）换成 **69,566 字节的完整文档**，
装配另立新件 `src/home/goalProgressDocs.ts`，出口在 `src/home/today.ts` 的 `viewGoalProgress` 薄转出；
`src/render/html.ts`（别家 T351 的在途文件）**一行未动**。

## 一、判定与落点

### 判定：做了（不是停手待裁）

父席给的那道门是「重做会不会改到那 7 条唤醒词的产物」。核实结果与处置：

| 唤醒词 | 场景 | 命令参数 | 与「看今日目标进度」的产物 |
|---|---|---|---|
| 看今日目标进度（order 4） | 01 | `{"window":"今日"}` | 本票验收面 |
| 看今日目标（order 216） | 06 | `{"window":"今日"}` | **逐字节同一份调用**（无另一份产物可保） |
| 看饮水目标进度（order 220） | 06 | `{"window":"今日"}` | **逐字节同一份调用** |
| 看本周目标（order 217） | 06 | `{"window":"本周"}` | 同一命令、另一窗口 |
| 看营养目标进度（order 218） | 06 | `{"window":"7d"}` | 同一命令、另一窗口 |
| 看目标完成率(按周)（order 224） | 06 | `{"window":"7d"}` | 与 order 218 同参数 |
| 看目标完成率(按月)（order 225） | 06 | `{"window":"7d"}` | 与 order 218 同参数 |
| 我的减肥策略对吗（order 372） | 10 | `{"window":"30d"}` | 同一命令、另一窗口 |

- 8 条唤醒词共用一条命令 `calorie.view.goal-progress`，共 **4 份互不相同**的调用（今日／本周／7d／30d）。
  来源：`packages/skill-calorie/src/home/routes.ts:14` 与 `:43-49`。
- ⇒ **重做会改到那 7 条的产物**，且 order 216／220 与本票那条是同一份调用，**没有「别家不动」这条路可走**。
- 父席列的路线①（只在场景 01 这条词上换装配）**在当刻结构下做不到**：路由声明里这条命令**没有** `entry`
  标记（营养族有、这条没有），命令收不到唤醒词；要按词分叉就得改 `src/home/routes.ts`，而票面
  「不许动的东西」第一段与「唤醒词与命令声明」两处都明令**一行不改**。且一命令两形状＝两处口径，
  与 `t401-融合设计.md` §三冲突 3 的「同源视图同装配」相冲。
- **票面已预裁定**：`## 已知约束` 逐字写「形状提升会同时落到那 7 条上——**记账，不扩验收面**」；
  `## 遗留出口` 逐字写「那 7 条的可读性如要单独收口，另开票，不在本票扩面」。
  故本票按「做了」执行，验收面**只到「看今日目标进度」这一条**，不替那 7 条改验收判据。
- 相邻先例：#452 已把同样被多词复用的 `calorie.view.exercise` 族换成融合版式（见包内台账 `sportPortDocs.ts` 行）。

### 落点

| 件 | 动作 | 理由 |
|---|---|---|
| `packages/skill-calorie/src/home/goalProgressDocs.ts` | **新建**（LF 350） | 整页装配的归位件。照 #370 的先例另立（不是搬迁），落点与 #370 的 `homeDocs.ts` 同目录同层级 |
| `packages/skill-calorie/src/home/today.ts` | 改出口（＋1 空窗分支） | `viewGoalProgress` 由 `renderGoalProgressHtml`（片段）改走 `buildGoalProgressDoc`（整页） |
| `packages/skill-calorie/src/render/html.ts` | **一行未动** | 别家 T351 的在途文件；旧片段函数 `renderGoalProgressHtml` 原样留着，`render-t9.test.mjs:119-121` 那条片段判据因此不受影响（票面允许「留薄转出」） |
| `packages/skill-calorie/test/home-lock-374.test.mjs` | 只改**第 40 行**那一行 | 该行把「片段」钉成了要求（`shape: 'fragment'`）；本票把这条词变成完整文档，行必须跟着改。其余 8 行一字未动 |
| `packages/skill-calorie/test/t467-goal-progress.test.mjs` | **新建** | 本页判据（完整文档／文本纪律／三条恒出／双按钮／空窗／形状） |

### 照的哪六条标准

形状标杆是同一族的整页装配 `src/home/homeDocs.ts`（#401／#401c／#401g／#507 逐轮定稿），不是凭空设计：

1. **完整文档**：`<!doctype html>` 起、`charset="utf-8"`、视口 meta、样式段与脚本段全内联、单文件、双击即开；
   实测 `ext=false`（无任何 `http(s)://` 外部引用）；
2. **四档自适应**：512／820／1000／1440 四档横向溢出均为 0（见 §三）；
3. **可见文本零内部标识符**：剥掉样式段、脚本段与复制载荷后，可见面零 snake_case、零裸英文枚举、零命令键、零票号；
4. **分隔符＝设计债**：三份产物分隔符探针均 0／0／0（见 §三）；
5. **形状化**：全部取现成件——KPI 卡带状态徽章档位、区块标题带图标、缺值写 `—`、空态带引导句、
   页内导航用胶囊（`renderTocBlock`）、结论用结论条（`renderConclusionBar`）、复制区双按钮；
   页面本地**一行色值、一个字号都没写**（版面单源住 `packages/base-render/`）；
6. **口径句走口径行**：四处口径全走 `renderCaliberLine`，页上无整段纯文本墙。

### 块序（两态一致，只有中段不同）

页头（眉标 ＋ 标题 ＋ 副题区间）→ 页头胶囊 → 结论条〔空窗时换成空态块〕→ 页内导航 →
🎯 目标完成（KPI 四张 ＋ 口径行）→〔📈 摄入走势（＋线）｜📝 先记一笔（空窗）〕→ 📊 每日达标（表 ＋ 口径行）→
💰 数据与日志（双按钮复制区）→ 数据来源行（浅色口径行，不走 `notice()`）。

三条恒出（`t425-融合基准.md` 裁定 3）：**页内导航 ＋ 口径说明行 ＋ 来源脚注**，两态都在。

### 三处实况订正（重做时改掉的）

1. **枚举原文上屏**（票面点名）。重做前 `src/render/html.ts:297-298` 把 `g.deficit.summary.trend`
   （`'loss' | 'gain' | 'flat'`）与 `g.trend.summary.trend`（`'up' | 'down' | 'flat'`）直接当**值位**印。
   本件换成中文（缺口方向走徽章「摄入比消耗少／多／持平」，摄入趋势走图下那句
   「跟窗口里第一个有记录的日子比，摄入下降了 N 卡」）。
2. **窗口为空时整页不落盘**（票面「空窗也是完整页」那条判据）。重做前 `buildGoalProgress` 对
   「有目标但这一段零记录」也抛缺失阻断 ⇒ 当天还没记东西时「看今日目标进度」直接报错。
   现按 `t425-融合基准.md:156-159` 裁定 4 分两态：**窗口为空**出完整空态页（读数一律 `—` ＋ 引导句）；
   **库为空**（连营养目标都没有）仍走缺失阻断、exit 4、不落盘。分辨两种态的判据是**营养目标行在不在**，
   不看错误文案。
3. **「周缺口」这块牌子挂错了**。取数层的 `deficit.summary.weeklyDeficit` 是**本窗累计缺口**
   （`analysis/deficit.ts` 的 `weeklyDeficit: totalD`，totalD 是全窗求和），重做前却写成「周缺口」——
   在 30 天窗上会被读成「一周的缺口」。本件按本窗如实写「本窗累计缺口」。
   **取数一行未动**（键名 `weeklyDeficit` 也原样留在复制载荷里），只改上屏的字；
   键名与算法的收口归 #396 那条线。
4. **「达标账」的表不再列没记录的日子**。这一族默认看近 30 天（`historyDays` 缺省 30），
   稀疏记录会让 30 行里二十多行是三格 `—`——那也是「拿符号顶 UI」的一种形态。
   现在只列有记录的日子，caption 写明「近 30 天里有记录 N 天」，口径句写明「没记录的日子不列表，
   既不算达标也不算落空」。件数与 caption 对得上。

### 一处有意偏离（记账）

图下那句摄入趋势**不走数据层的 `trend.summary.trendValue`**：它拿**没记录的首日当 0**
（`analysis/trend.ts` 的 `startAvg = series[0].calorie ?? 0`，是 #160 明写「本票之外的口径」），
本窗稀疏时会算出「摄入上涨了 1339 卡」这种假涨跌。本件只用**图上也画得出来的那些点**比首末两个有记录日
（阈值仍取数据层那 ±50 卡），句子在页上如实写「跟窗口里第一个有记录的日子比」，读者能自己对着图核。

## 二、影响面（那 7 条唤醒词）

**会被波及，且是按票面预裁定记账、不扩验收面**（依据见 §一）。逐条说清被改到的是什么：

- **order 216「看今日目标」／order 220「看饮水目标进度」**：与本票那条**同一份调用** ⇒ 产物逐字节同变。
  这两条从「片段」变成完整文档，属于票面说的「形状提升同时落到那 7 条上」。
- **order 217／218／224／225／372**：同一命令换窗口（本周／7d／30d），装配同一份 ⇒ 同样变成完整文档，
  且同样吃到文本纪律（枚举不人话的那两处）与三条恒出的好处。
- **没有扩验收面**：本票只跑「看今日目标进度」这条词的真出口判据，没有替那 7 条改任何验收文件，
  也没有给它们新建判据。
- **一处必须跟着改的别家判据已如实记账**：`packages/skill-calorie/test/home-lock-374.test.mjs:40`
  把这条词的形状钉成 `fragment`，本票改成 `doc`——它是**场景 01 那九条词**的交付出口锁，
  而被改的这一行正是本票验收的那条词，属本票写集范围；同文件其余 8 行（含场景 01 其余 8 条）一字未动。

## 三、三组读数

### ① 分隔符探针（`.scratch/sep-audit/probe.mjs`）

| 产物 | 行级 含·／含；／≥3 段并列 | 节点级 含·／含；／≥3 段并列 | 退出码 |
|---|---|---|---|
| `看今日目标进度.html`（今日窗） | 0／0／0（命中合计 0 行） | 0／0／0（命中合计 0 处） | 0 |
| `看今日目标进度-近30天.html`（出图档） | 0／0／0（命中合计 0 行） | 0／0／0（命中合计 0 处） | 0 |
| `看今日目标进度-空窗.html`（空窗档） | 0／0／0（命中合计 0 行） | 0／0／0（命中合计 0 处） | 0 |

### ② 四档溢出（真 Chrome ＋ CDP `Emulation.setDeviceMetricsOverride`，量法 `.scratch/t467/browser.mjs`）

判据：`documentElement.scrollWidth > clientWidth+1` 为假，且**逐元素**扫一遍，越界元素数＝0。
KPI 列数**按卡的实际位置**数（同一个 `top` 上有几张卡）——`getComputedStyle(...).gridTemplateColumns`
的字符串切分不可靠（本票实测被尾随空格骗过：切出 5／6，实际是 4）。

| 档 | 视口 | 文档 scrollWidth | 判据 | 越界元素 | 页身 | KPI 卡（每行／卡宽） | 表格 | 复制两颗按钮 |
|---|---|---|---|---|---|---|---|---|
| 512 | 497 | 497 | 不溢出 | **0** | 满宽 497 | 2＋2（每张 227） | 465（内 463） | 229／229，两列平分 |
| 820 | 805 | 805 | 不溢出 | **0** | 满宽 805 | 4（每张 182） | 682（内 680） | 256／256（动作条 520 靠左） |
| 1000 | 985 | 985 | 不溢出 | **0** | 满宽 985 | 4（每张 227） | 682（内 680） | 469／469，平分 |
| 1440 | 1425 | 1425 | 不溢出 | **0** | 1425 里 1000 宽居中（x=213） | 4（每张 231） | 682（内 680） | 476／476，平分 |

有图那一档（近 30 天窗）同样四档 `overflowing(0)`，折线图 435×195（512）／735×329（820）／915×410（1000）／930×417（1440）。

**与形状标杆同形**：拿同一量法量 `.scratch/t375/products/看今日主页.html`（主页族现状），四档读数与上表逐项一致
（KPI 列数、复制两颗按钮 229／256／469／476、页身 1440 档 x=213 宽 1000 居中）——本页不是孤例。
**两处已知公共层缺口照实记账、本票不修**：820 档动作条钉 520 靠左、窄档（≤640）「ghost 行一列」没有规则，
两条都记在 `t401-融合设计.md` §「适配」的记账 3（票 #427）；主页族今天就是这个样子。

### ③ 变异自证（三处源码级变异，每处打一条不同的判据）

| 变异 | 编译 | 判据读数 | 首条红 |
|---|---|---|---|
| ① 值位回印**枚举原文**（图下那句接 `g.trend.summary.trend`） | 绿 | pass 3／**fail 1**／断言红 1 | 近 30 天目标进度 可见面出现裸英文枚举：`up` |
| ② 去掉**来源脚注** | 绿 | pass 1／**fail 3**／断言红 3 | 今日目标进度 缺来源脚注（口径行里读不到「数据来源：」） |
| ③ 缺值口径折成 **0**（`DASH = '0'`） | 绿 | pass 3／**fail 1**／断言红 1 | 空窗页把缺值折成了 0 |
| 还原（三处全部回原样） | 绿 | pass **4**／fail **0**／断言红 0 | — |

脚本 `.scratch/t467/mutate.mjs`：每个变异锚点**必须唯一命中**（命中 0 或多次即当场抛，免得改错地方还报绿）；
`finally` 里一律还原 ＋ 重新编译 ＋ 再跑一遍。**两行读数**＝上面「三处各不相同」的红色档 ＋ 最后一行还原绿。

### 交付产物

| 产物 | 字节 | `<!doctype html>` | 外部引用 | 页内锚点 |
|---|---|---|---|---|
| `.scratch/t467/看今日目标进度.html` | 69,566 | 是 | 无 | 3 |
| `.scratch/t467/看今日目标进度-近30天.html` | 78,519 | 是 | 无 | 4 |
| `.scratch/t467/看今日目标进度-空窗.html` | 66,449 | 是 | 无 | 3 |

对比：重做前同一命令的产物是 **1,666 字节的片段**（`.scratch/t375/products/看今日目标进度.html`）。

### 判据测试读数

| 用例文件 | 读数 |
|---|---|
| `packages/skill-calorie/test/t467-goal-progress.test.mjs` | **tests 4／pass 4／fail 0** |
| `home-lock-374.test.mjs` ＋ `render-t8.test.mjs` ＋ `render-t9.test.mjs` | tests 25／pass 24／**fail 1** |

**那 1 条红是别家引入的，不是本票**（归属判定见下）：

```
✖ #374 看今日体重概览（有段出页／无段阻断算通）
  AssertionError [ERR_ASSERTION]: 看今日体重概览 空库 exit 4（stderr：）   0 !== 4
```

- 该行是 `home-lock-374.test.mjs:39`，断言「`calorie.view.weight` 空库必须 exit 4、不落盘」。
- 实况：空库下它 **exit 0 且落盘**——现场复跑同一条命令得
  `{"key":"calorie.view.weight","data":{"metrics":{"recordCount":0},…"bytes":67868}`。
- 来源件是**别家的**：`packages/skill-calorie/src/weight/log.ts:11` 逐字写着
  「窗口内没有记录时**不再阻断，改出整页空态**」、`:337` 是 `buildWeightEmptyDoc`。
  本票写集只有 `home/today.ts`、`home/goalProgressDocs.ts`、两个测试件、证据件——
  `git status` 里 `src/weight/` 一件未动，**结构上不可能由本票造成**。
- 处置：按协议 §5.1 标「范围外发现」，**只记账不代改**（改它等于动场景 03 的验收面，属别人的写集）。
  转票建议：`home-lock-374.test.mjs:39` 的 `checkEmpty` 期望值随 #337／#505 的空态页一并收口。

**`render-t8` 与 `render-t9` 全绿**：前者根本不测 `renderGoalProgressHtml`（它测 `renderGoalHtml`，另一支）；
后者第 119-121 行测的是留在 `html.ts` 里的旧片段函数，本票一行未动，故原样通过。

### 包内两道门

| 门 | 读数 |
|---|---|
| `node packages/skill-calorie/scripts/check-warning-line.mjs` | **RESULT: 67/67**／`PASS: 告警线台账齐全且与实况一致` |
| `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `GEN-CHECK PASS`：命令 127（写 46 ＋ 读 81）；能力 10 个；未搬迁清单 0 条 |

新件 `src/home/goalProgressDocs.ts` **LF 350**，停在告警线上（未越线），故台账不需新增行
（判据是 `LF > 350`，见 `check-warning-line.mjs:13`）。**票面流程没碰命令声明与路由**，命令名、参数、落盘路径零变。

## 四、门禁运行记录（对账源）

本票全部经 `node tooling/run-locked.mjs --ticket 467` 执行；导出件 `.scratch/locks/gate-runs.log` 里
`ticket=467` 的条目**一条不漏**列在下面（20 条 RUN）。按 §2.4 第 3 条，**只领 exit=0 的条目当门禁证据**，
exit≠0 的是过程迭代（改判据、修锚点），声明在此备查。

| 运行标识 | 命令 | 等待毫秒 | 退出码 |
|---|---|---|---|
| `af92010b-6285-4823-8073-b47582f5ab49` | `pnpm build` | 130075 | 2（首次编译报错，修两处类型） |
| `01d053bd-9c2a-4a17-a661-b8c699195399` | `pnpm build` | 0 | **0** |
| `3c210958-6231-4f30-a053-dc0a236330ab` | 内嵌 pwsh 串（探针＋测试） | 80069 | 1（here-string 截断，未跑到探针） |
| `b5c5b9c7-0152-4570-a456-2ea94b62a247` | 内嵌 pwsh 串（2／2 同因） | 0 | 0（只跑了重出产物） |
| `2f5589f3-6217-4ba4-b684-6dc1cc7db5d9` | `node .scratch/t467/run-all.mjs all` | 110085 | 1（测试里 `--params` 多套了单引号） |
| `2297e1d1-a7b3-4645-8c28-9cf76c1f961a` | 同上 | 70052 | 1（同上，已修） |
| `1efc601e-749e-4f55-b6b8-db5a9a648391` | `… run-all.mjs test` | 70068 | 1（断言把样式段里的类名也算进去了，已修） |
| `7929c5d3-0c53-4a94-a319-68da48827068` | `… run-all.mjs test` | 0 | 1（复制载荷里的命令原文被 `&quot;` 转义，已修） |
| `8098fba7-6638-4440-99d8-b5720b0892a7` | `… run-all.mjs all` | 0 | 1（空窗档选了 8 月，种子库那头有记录） |
| `539b3646-1ab1-4b44-97e2-ab1364e7907d` | `… run-all.mjs test` | 0 | 1（**仅剩别家那条红**：看今日体重概览） |
| `9908d3ce-ea02-4189-a84b-509d45c4772c` | `node .scratch/t467/shots.mjs` | 0 | **0** |
| `407d2a45-a2a1-4ab0-b4f5-a71abeb627ff` | `… shots.mjs 看今日目标进度-近30天.html measure` | 0 | **0** |
| `7bfe6f91-50e7-4e89-b750-abb3a5828690` | `… shots.mjs ../../../.scratch/t375/…` | 0 | 0（相对路径算错，页面没加载，读数全空） |
| `ef3cf5ef-0b8b-4976-a5e0-bd4ecf51c760` | `… shots.mjs ../t375/products/看今日主页.html measure` | 0 | **0**（标杆对照四档） |
| `9628051b-83d3-4dcb-8dc7-5315b24fb39a` | `… run-all.mjs all` | 1 | 1（`pnpm` 没带 shell，编译静默没跑） |
| `5268a2fa-0a42-449e-a30b-a247f5b5cd20` | `… run-all.mjs all` | 0 | 1（同上已修；**探针 0／0／0 ＋ t467 全绿**） |
| `21662f05-3238-4a2f-bdb4-dad9ced5d621` | `… shots.mjs 看今日目标进度.html both` | 0 | **0** |
| `6dfc936d-378d-40ad-a0e7-d9bc1f81c934` | `node .scratch/t467/mutate.mjs` | 0 | **0** |
| `2e26bb06-8ccd-46d7-bae8-e6991687f100` | `node .scratch/t467/final.mjs` | 0 | 1（**仅「族内」那一条别家红**；其余全绿） |

导入对账前又补了一次 KPI 卡位实测，形成第 20 条：`7c4b54ec-bac2-40bb-8da7-e9db55254b9a`
（`node .scratch/t467/shots.mjs 看今日目标进度.html measure`，等待 0，exit **0**）。

对账窗口内**每一条** `RUN` 条目都声明在下面（§2.4 第 3 条②：无人声明的条目即失败）；
其中 **14 条 exit≠0** 是本票的过程迭代读数，按同条第④款**不得充作门禁证据**，
故另附一条放宽留痕（第 4 条：放宽必须写进证据）。

```
GATE-RELAX flag=--allow-nonzero reason=对账窗口内 14 条 RUN 的 exit≠0 全是本票过程迭代读数（首版类型错、测试锚点与转义写法错、空窗档选错月份、pnpm 未带 shell、别家那条红），按 §2.4 第 3 条②须逐条声明入账，按④不得充作门禁证据；真正的门禁证据只领 exit=0 的那几条。
```

```
GATE-RUN runId=af92010b-6285-4823-8073-b47582f5ab49 cmd="pnpm build"
GATE-RUN runId=01d053bd-9c2a-4a17-a661-b8c699195399 cmd="pnpm build"
GATE-RUN runId=3c210958-6231-4f30-a053-dc0a236330ab cmd="pwsh -NoProfile -Command"
GATE-RUN runId=b5c5b9c7-0152-4570-a456-2ea94b62a247 cmd="pwsh -NoProfile -Command"
GATE-RUN runId=2f5589f3-6217-4ba4-b684-6dc1cc7db5d9 cmd="node .scratch/t467/run-all.mjs all"
GATE-RUN runId=2297e1d1-a7b3-4645-8c28-9cf76c1f961a cmd="node .scratch/t467/run-all.mjs all"
GATE-RUN runId=1efc601e-749e-4f55-b6b8-db5a9a648391 cmd="node .scratch/t467/run-all.mjs test"
GATE-RUN runId=7929c5d3-0c53-4a94-a319-68da48827068 cmd="node .scratch/t467/run-all.mjs test"
GATE-RUN runId=8098fba7-6638-4440-99d8-b5720b0892a7 cmd="node .scratch/t467/run-all.mjs all"
GATE-RUN runId=539b3646-1ab1-4b44-97e2-ab1364e7907d cmd="node .scratch/t467/run-all.mjs test"
GATE-RUN runId=9908d3ce-ea02-4189-a84b-509d45c4772c cmd="node .scratch/t467/shots.mjs"
GATE-RUN runId=407d2a45-a2a1-4ab0-b4f5-a71abeb627ff cmd="node .scratch/t467/shots.mjs 看今日目标进度-近30天.html measure"
GATE-RUN runId=7bfe6f91-50e7-4e89-b750-abb3a5828690 cmd="node .scratch/t467/shots.mjs ../../../.scratch/t375/products/看今日主页.html measure"
GATE-RUN runId=ef3cf5ef-0b8b-4976-a5e0-bd4ecf51c760 cmd="node .scratch/t467/shots.mjs ../t375/products/看今日主页.html measure"
GATE-RUN runId=9628051b-83d3-4dcb-8dc7-5315b24fb39a cmd="node .scratch/t467/run-all.mjs all"
GATE-RUN runId=5268a2fa-0a42-449e-a30b-a247f5b5cd20 cmd="node .scratch/t467/run-all.mjs all"
GATE-RUN runId=21662f05-3238-4a2f-bdb4-dad9ced5d621 cmd="node .scratch/t467/shots.mjs 看今日目标进度.html both"
GATE-RUN runId=6dfc936d-378d-40ad-a0e7-d9bc1f81c934 cmd="node .scratch/t467/mutate.mjs"
GATE-RUN runId=2e26bb06-8ccd-46d7-bae8-e6991687f100 cmd="node .scratch/t467/final.mjs"
GATE-RUN runId=7c4b54ec-bac2-40bb-8da7-e9db55254b9a cmd="node .scratch\t467\shots.mjs 看今日目标进度.html measure"
```

对账源自证（窗口封到上面这一条为止）：`node tooling/check-gate-audit.mjs --evidence
docs/skills/skill-calorie/t467-重做-证据.md --ticket 467 --until 2026-09-15T04:23:15.332Z --allow-nonzero`，
读数 **`RESULT: matched=20/20 auditEntries=5594 scoped=20 undeclared=0`／`gate-audit: PASS`**
（`GATE-RUN runId=99cb69dd-937c-46c2-a59a-915848c8a2f9`；对账窗口内 20 条 RUN 全部一对一命中，反向无一条无人声明）。
对账源导出件＝本目录 `t467-gate-runs.log`（受 git 跟踪，20 条 RUN 原文）。

**提交之后的四条运行**（同属本票，一并声明；前三条是对账工具的两次返修与一次通过，第四条是 add／commit／push 那条）：

```
GATE-RUN runId=eeba5a08-5c5a-404b-bafe-2f0139c96ef6 cmd="node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t467-重做-证据.md --ticket 467 --until 2026-09-15T04:23:15.332Z --export docs/skills/skill-calorie/t467-gate-runs.log"
GATE-RUN runId=2c7df160-b367-41dd-a1c9-a50d80cc593e cmd="node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t467-重做-证据.md --ticket 467 --until 2026-09-15T04:23:15.332Z --allow-nonzero --export docs/skills/skill-calorie/t467-gate-runs.log"
GATE-RUN runId=99cb69dd-937c-46c2-a59a-915848c8a2f9 cmd="node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t467-重做-证据.md --ticket 467 --until 2026-09-15T04:23:15.332Z --allow-nonzero --export docs/skills/skill-calorie/t467-gate-runs.log"
GATE-RUN runId=377d2018-ffea-40b1-9e72-3c9b5ea5ed90 cmd="pwsh -NoProfile -Command"
```

（前两条 exit≠0：第一条漏了 `--allow-nonzero`，第二条有三条声明的 `cmd=` 写法没与审计条目对齐；
第三条 exit=0，是本件引用的通过读数。第四条 exit=1，但**红的只是脚本末尾那句 `git log --format` 的引号**——
`git add`／`git commit`／`git push` 三步都成功，见下节。）

**对账窗口的尾巴**：本补记提交自身那条 `RUN` 在 `--until` 之后，不在本件声明的窗口内；
窗口由复核方按 `--since/--until` 界定，若把它纳入，按它自己的 runId 另行声明即可。

### 提交

| 提交 | 内容 |
|---|---|
| `71cd5e5` | `feat(467): 「看今日目标进度」整页重做 —— 片段改完整文档 ＋ 文本纪律与形状化 ＋ 判据与证据`（6 件：新装配件、出口、两条测试件、证据件、对账源导出件） |
| 已推送 | `e830740..71cd5e5  master -> master`（`git push` 成功，与其他席的在途改动无交错） |

暂存区自查：`git diff --cached --name-only` 只列本票声明的 6 条路径，无一条别家的件
（同工作区里别家改过的 `test/help-center-106.test.mjs` **没有被本票带进提交**）。

**一次锁被抢回，如实记账**：`LOCK-STOLEN ticket=467 runId=2f5589f3… reason=owner-dead ownerPid=34740
ownerAlive=0 ownerBelongsToLock=1 ageMinutes=1.99`——包装器按 §2 的「先探活」判定前持有者已死、
且记录属于当前锁，才抢回；不是按锁龄硬抢。

## 五、实拍（肉眼看）

| 图 | 档 | 文档尺寸 |
|---|---|---|
| `.scratch/t467/实拍-512.png` | 512 | 512×1271 |
| `.scratch/t467/实拍-1000.png` | 1000 | 1000×1235 |

两张都亲眼看过了。看下来的结论与一处返修：

- **返修一处**：第一版「每日达标」表按近 30 天逐日列全，1000 档量到表高 1055px、整页 2169px，
  30 行里二十多行是三格 `—`——正是一堵「拿符号顶」的墙。改成只列有记录的日子后，
  1000 档表高降到 479px、整页降到 1235px，实拍图上每行都有真读数。（§一 订正 4）
- 512 档：KPI 两列 2×2 顺读，表格 465px 铺满内容列不滚动，两颗复制按钮各 229px 并排，结论条与三段口径行都在。
- 1000 档：KPI 四张一行铺满，表格 682px 居中，折线那一档图 915×410。
- 两档都读不到任何英文内部标识符、任何 `·`／`；` 串。

**视觉最终验收归用户**：本票只做机器可判的那几条，好不好看由用户原话为准。

## 六、未做项与遗留出口

1. **那 7 条唤醒词的验收没收口**（票面明文「不扩验收面」）。它们的产物随本票一起变成完整文档，
   若各自要单独验收，另开票。
2. **`home-lock-374.test.mjs:39`（看今日体重概览 空库 exit 4）是别家引入的红**，本票只记账不代改（§三）。
3. **内容面疑似缺项转 #396**：这条唤醒词的意图写的是「看今天 4 项目标(热量／蛋白／饮水／运动)的完成度」
   （`src/triggers/scene-01-home.ts`），而当刻取数盘 `GoalProgress` 只给热量口径 ——
   `nutrition` 有 `protein_goal`／`water_goal` 的目标值但**没有蛋白／饮水的实际摄入**，
   运动目标（`daily_goal.exercise_goal`）连字段都没进 `NutritionGoalRow`。
   上屏蛋白／饮水／运动三项目标进度＝**改取数面**，按票面「别悄悄改口径，判成口径或字段问题转 #396」办，
   本票**只做呈现重做**，不动取数。
4. **取数层的两个键名与算法的收口**（不在本票写集）：`deficit.summary.weeklyDeficit` 实为**本窗累计**
   （已在页上如实改名，键名未动）；`trend.summary.trendValue` 拿没记录的首日当 0（本件绕开，见 §一 有意偏离）。
   两条都建议并入 #396 那条线。
5. **单日窗下两张缺口卡同值**（`日均缺口` 与 `本窗累计缺口` 都是 1687）：单日窗里「日均」与「累计」本就恒等，
   两块牌子各自都没写错。为 1 天窗特判合并卡片会让页面形状随窗口漂移，本票先不做——想法只写文档，不占票。
6. **公共层两处缺口（#427）**：820 档动作条钉 520 靠左、窄档 ghost 行没有「一列」规则。主页族今天就是这个样子，
   本票照同一套类名，不本地补规则。

## 七、可复跑件

| 脚本 | 作用 |
|---|---|
| `.scratch/t467/gen.mjs` | 固定种子库重出三份产物到 `.scratch/t467/` |
| `.scratch/t467/run-all.mjs [build\|gen\|probe\|test\|final\|all]` | 持锁窗口内的串行驱动（编译／产物／探针／测试／包内两道门） |
| `.scratch/t467/browser.mjs measure\|shot` | 真 Chrome ＋ CDP 量一档、出一张图 |
| `.scratch/t467/shots.mjs [产物] [both\|measure]` | 起 Chrome、四档量测、按票面出两张实拍 |
| `.scratch/t467/mutate.mjs` | 三处源码级变异 ＋ 还原复绿 |
| `.scratch/t467/final.mjs` | 收尾一条龙 |

---

# 复核整改（第二席收尾 · 2026-09-15）

复核报告 `.scratch/t467-review/审查报告.md` §七「必改清单」给了五条。上一席的改动落在工作区但没验证、没提交（网络中断）；
本席逐条对照后**只补缺、只纠错**，已在工作区的那两份源码改动里，五条**都做了**，但其中两条夹着**新债**，本席当场改掉。

## 八、五条逐条

| # | 必改条目 | 上一席 | 本席 |
|---|---|---|---|
| 1 | 【P0】目标虚线加量程判定（量程外不画，改写进读数） | 已做（`targetInRange()` ＋ `markLine` 条件传） | 不动 |
| 2 | 【P0】图下那句在「线不画」时改读数式 | 已做（`chartCaliber()`） | 不动 |
| 3 | 【P1】卡四牌子带窗口 | 已做（`'近 ' + historyDays + ' 天达标天数'`） | 不动 |
| 4 | 【P1】达标表前补窗口转场句 | **做错**：句里无条件提「上面那张折线图」，而折线只在窗口有 ≥2 个有记录日时才出 ⇒ 单日／同周单日档的页面上**说了一句假话**（同一页上 P0 的病根） | **改**：`loggedDays >= 2` 走折线那句，否则走「这个窗口里只有一天有记录，折线图要两天以上才出」 |
| 5 | 【P1】补三项目标说明 ＋ 蛋白／饮水目标上屏 | 已做（行列表 ＋ 唤醒词句） | 不动（唤醒词逐字核过：`定营养目标`／`定饮水目标`／`看今日运动（vs 目标）` 三条都在 `wake-assets.ts`，第三条的 `prompt_template` 原文就是「没设过就先问我目标值」） |
| — | 【P2】四卡补档位徽章 | 已做 | 不动（空窗页仍不摆徽章：`rateStatus(null) === null`） |
| — | 【P2】页签标题带窗口 | **做错**：`DOC_TITLE + ' · ' + title` ⇒ 可见文本出现 `·`，分隔符探针**红 2 处**，且与 h1 的「近 N 天目标进度」重复 | **改**：`DOC_TITLE + '（近 ' + windowDays + ' 天）'`（括号承载，零重复零 `·`） |

**另修一处同源债**：上一席新增的两句口径（「本期只算热量」那句与窗口转场那句）都用了 `；` —— 可见文本里的
`；` 与 `·` 同罪（分隔符懒政）。改 `。` 后**五份产物探针全 0/0/0**。

## 九、读数（本席当场）

**分隔符探针**（`node .scratch/sep-audit/probe.mjs <产物>`，退出码 0 ＝ 行级／节点级／段落级均 0）：

| 产物 | 种子今天 | 窗口 | 探针 | 字节 |
|---|---|---|---|---|
| `本周-09-09.html` | 2026-09-09（周三） | 本周→3 天 | **0/0/0 exit 0** | 73,546 |
| `本周-09-07.html` | 2026-09-07（周一） | 本周→1 天（夹具事实） | **0/0/0 exit 0** | 73,543 |
| `7d-09-09.html` | 2026-09-09 | 7d | **0/0/0 exit 0** | 81,042 |
| `30d-09-09.html` | 2026-09-09 | 30d | **0/0/0 exit 0** | 82,307 |
| `30d-目标1000-在量程.html` | 2026-09-09 | 30d（目标改 1000） | **0/0/0 exit 0** | 82,500 |

> **夹具事实**：`SEED_TODAY=2026-09-09` 下「本周」＝09-07 至 09-09 共 3 天，但**只有 1 天有记录** ⇒ 这一页**不出折线**，
> 所以「虚线实测」只能在出图的那两档（7d／30d）上量；周三那两份量的是**无图态**的转场句。

**虚线实测**（`node .scratch/t467-fix/measure.mjs <产物>`：刻度定标 → 反解目标位的 y → 数 `ilife-charts-markline` 的元素数）：

| 产物 | y 量程（刻度） | 绘图区 | 目标 | 判定 | 图上线 | 图下读法 |
|---|---|---|---|---|---|---|
| `7d-09-09.html` | 618／935／1252 | y ∈ [21,220] | 1800 | **量程外**（若画 y=-144.5） | **0 条** | 读数式：「最低 650 卡、最高 1189 卡，热量目标 1800 卡在这段量程之外，所以图上没画那条虚线。」 |
| `30d-09-09.html` | 88／700／1311 | y ∈ [21,220] | 1800 | **量程外**（若画 y=-54.0） | **0 条** | 读数式（「最低 150 卡、最高 1189 卡」） |
| `30d-目标1000-在量程.html` | 88／700／1311 | y ∈ [21,220] | 1000 | **量程内**（算得 y=73.6） | **1 条，y1=71.6**（落在绘图区内） | 承诺式：「虚线是热量目标 1000 卡，点是有记录的每一天。」 |

**改前对照**（复核报告那份产物）：`<line class="ilife-charts-markline" … y1="-58.5">` —— 绘图区顶是 y=21，
它跑到框外 58.5px，靠 `.ilife-charts-svg{overflow:visible}` 飘到卡区。改后同档 **`dasharray` 命中 0**。

**徽章与页名**：五份产物 KPI 徽章均 **4/4**；`<title>` 分别 `卡路里 目标进度（近 3 天）`／`卡路里 目标进度`／
`（近 7 天）`／`（近 30 天）`，`·` 命中 **0**；h1 同步带窗口。

**四档横向溢出**（真 Chrome，`node .scratch/t467-fix/shots.mjs <产物>`）：

| 档 | `本周-09-09` | `7d-09-09` |
|---|---|---|
| 512 | 1 处（见下） | 1 处（见下） |
| 820／1000／1440 | **0／0／0**，`docXOverflow=false` | **0／0／0**，`docXOverflow=false` |

> **512 档那 1 处是别家在途债，不是本页内容**：溢出元素是 `caption.ilife-block-data-table-caption@17+483`（越卡 19px）。
> 决定性对照：把产物里**公共层那一行** `display:block; width:100%` 摘掉、别处一字不改再量 →
> `inner` 483→463、**溢出 0**（`.scratch/t467-fix/strip-caption-width.mjs`）。
> 那行是 `packages/base-render/src/blocks.ts` 的**在途**改动（`#154` 表注转块级），本席不碰别家文件，只记账。

**测试**：`node --test packages/skill-calorie/test/t467-goal-progress.test.mjs` → **5/5 pass**（新增第 5 条用例
「复核整改五条」把本席五条全钉住）；`node --test packages/skill-calorie/test/render-t8.test.mjs` → **7/7 pass**。两条都不是别家红。

**变异自证两行**（改动 `intakeChart` 的 `markLine`：量程判定 → 无条件画）：

```
变异：markLine: { value: f.goal ?? undefined, label: '热量目标' }   ⇒ tsc exit 0；测试 ✖ fail 1
      AssertionError: 目标落在量程外时仍画了那条虚线（会飘出绘图区）
还原：markLine: f.targetInRange && f.goal !== null ? … : undefined  ⇒ 测试 ✔ pass 5 / fail 0，exit 0
```

## 十、实拍（本席亲眼看过的）

- `.scratch/t467-fix/实拍-1000-7d-09-09.png`（1000×2600）：**卡与口径行之间干干净净——那条橙虚线没有了**；
  图上只 618／935／1252 三条刻度与蓝折线；图下那句话是读数式；卡四是「近 30 天达标天数 0 天」；
  蛋白／饮水／运动三行与「本页的完成度与缺口只算热量…」那段说明在位；四张卡各一枚徽章。
- `.scratch/t467-fix/实拍-1000-本周-09-09.png`：无图态；转场句如实写「这个窗口里只有一天有记录，折线图要两天以上才出」。
- 512 档实拍各一张（`.scratch/t467-fix/实拍-512-*.png`）：2×2 卡、表格铺满、两颗复制按钮并排。

## 十一、结构账与别家红

- **告警线（必报五步第四步）**：`packages/skill-calorie/src/home/goalProgressDocs.ts` **LF 451 ＞ 350**
  ⇒ **「已超线，需要根据规则进行重构。」** 超因＝这一件同时住着本窗四卡与结论条、走势图与目标线量程判定、
  达标表与两档窗口转场、本期之外三项目标那一块、复制区与页框接线；#467 建件时 350，复核整场涨到 451。
  **拆法**＝把「目标线量程判定 ＋ 走势图 ＋ 图下读法」按页族切成姊妹件，四卡／结论条／达标表／复制区留本件，
  出口经本件薄转出；本次先不拆（拆分不在 #467 写集），台账行已按实况登记 451（`packages/skill-calorie/AGENTS.md`）。
- **别家在途红**（本席只记账，不代改）：① 512 档表注溢出（上文）；② `check-warning-line.mjs`
  另有 5 条红全是别家在途件（`trendDocs.ts`／`nutritionPortDocs.ts`／`sportDocs.ts`／`diet/reviewDocs.ts`／`photo/receipt.ts`）；
  ③ 编译期别家报错（`diet/reviewDocs.ts` 的 `version` 字面量类型、`sportDocs.ts`／`exercise/records.ts` 的 `ChipsInput`）
  与本件无关，本件的两个文件**零 TS 报错**。

## 十二、可复跑件（本席）

| 脚本 | 作用 |
|---|---|
| `.scratch/t467-fix/gen.mjs` | 两个种子日 × 五个产物（含「目标 1000 在量程」反例正相） |
| `.scratch/t467-fix/measure.mjs` | 静态读数：量程／markline／图下读法／徽章数／title／三项目标行／转场句 |
| `.scratch/sep-audit/probe.mjs <产物>` | 分隔符探针（仓内既有件，本席未改） |
| `.scratch/t467-fix/shots.mjs <产物>` | 起真 Chrome：四档溢出读数 ＋ 1000／512 两张实拍 |
| `.scratch/t467-fix/strip-caption-width.mjs` | 512 档溢出的归因对照（摘掉公共层表注那一行） |
| `.scratch/t467-fix/lf.mjs` | 量 LF（台账口径：只数 `\n`） |
