# t716 · 搬家证据（照片族四件：`src/render/` → `src/photo/`）

> **判据（本票主判据）**：**搬迁前后逐页逐字节相同**（票面「判据」段第一句）。
> 本窗不改任何可见文本、不改任何形状：搬的是代码住在哪个目录，产物必须一个字节都不动。
> 全部编译／测试／门禁经 `node tooling/run-locked.mjs --ticket 716 -- …` 排队。

## 〇 · 本窗做了什么（一句话）

按**归属律**（`docs/agents/structure.md`「共用件」那一条：写不出哪两个能力在用的，留在那个能力的目录里），
把 `src/render/` 里**只被 `photo` 一个能力目录引用**的四件**原样迁入** `src/photo/`：
`wizardPort`（277→275）、`wizardPortDocs`（346）、`helpShell`（31）、`templates`（28）。
`git` 把四件全判为**重命名**：`R097`／`R098`／`R093`／`R091`。

- 开窗提交：`2c04918d`（存产物基线当刻的 HEAD）
- 本票提交：`252456eb`（19 个路径：4 重命名 ＋ 15 改动）

## 一 · 搬了什么（逐件）

| 件 | 开窗 `src/render/` | 本票提交 `src/photo/` | `git` 判定 |
|---|---|---|---|
| `wizardPort.ts` | 277 LF／14291 B | **275 LF**／14037 B | `R097` |
| `wizardPortDocs.ts` | 346 LF／20602 B | 346 LF／20595 B | `R098` |
| `helpShell.ts` | 31 LF／1594 B | 31 LF／1595 B | `R093` |
| `templates.ts` | 28 LF／1309 B | 28 LF／1316 B | `R091` |

LF 口径＝只数 `\n`：`readFileSync(f,'utf8').split('\n').length - 1`。

### 1.1 改引用（生产 9 行 ＋ 测试 15 行 ＋ 旧 barrel 收窄 2 行）

- **生产 9 行**：`photo/wizard.ts` 3 行（两条 import ＋ 1 行注释）、`photo/helpFile.ts` 1 行、
  `photo/helpCenter.ts` 1 行、`body/wizardPlate.ts` 1 行、`body/wizardDocs.ts` 1 行、`shared/docPage.ts` 1 行、
  `profile/setup.ts` 1 行——**后四行是散文里的「件住哪」引用**（注释，不是 import），照 `#715` §1.1 的口径
  统一改写为**包内规范路径**；其中 `body/{wizardPlate,wizardDocs}.ts:3` 原文写的是
  「身材照／GIF 两页**仍住** `render/wizardPort*.ts`」——搬完当场变假话，本窗据实改写。
- **测试 15 行**（5 件）：`photo-help-flow-345` 1 行（→ `dist/photo/wizardPort.js`）、`help-shell-134` 1 行
  （→ `dist/photo/helpShell.js`）、`wizard-86` 1 行（→ `dist/body/wizardPlate.js`，见 §1.3）、
  `skill-t11` 8 行（import 改径 ＋ 装载器夹具重排成两层布局）、`t445-告警线门` 4 行（`FROZEN` 路径 ＋ 件头一句 ＋
  `T8` 里那句硬编码期望串 ＋ 夹具补建 `src/photo/` 目录）。
- **旧 barrel 收窄 2 行**：`render/index.ts`（118→116 LF）去掉 `templates` 的两行转出——旧 barrel 只许收窄；
  包门 `src/index.ts` 本就不转出 `templates`（当刻实测），故对外面零变化。唯一消费者
  `photo/helpCenter.ts` 改走同目录。

### 1.2 票面数据的一处订正：冻结身份是**六处**，不是三处

票面（继承 `docs/skills/skill-calorie/t704-搬家-证据.md:131` 的读数）写「路径被**三处**写死」。
当刻全仓实测 `src/render/wizardPort.ts` 命中 61 处，其中**活引用六处**：`scripts/check-warning-line.mjs:46`（`REQUIRED`）、
`test/t445-告警线门.test.mjs:54`（`FROZEN`）、同件 `:10`（件头复述）、同件 `:282`（`T8` 的硬编码期望串）、
`packages/skill-calorie/AGENTS.md`（台账「件」列 ＋ 「台账两种口径」段各一处）、
`docs/skills/skill-calorie/t169-设计定稿.md:92`（票 2 验收命令）。其余 55 处是历史证据件，照旧不动。
⇒ 照票面字面只改「需求原文」而漏掉其余五处，会在**同一份 `AGENTS.md` 里**留下两个地址打架。

### 1.3 就地摆正 2 行（**搬迁自己做出来的违规，本窗消掉**）

`wizardPort.ts` 迁进能力目录后，它原来那两行**零生产调用方**的身体域转出会从
「工种名目录 → 能力内部件」变成「**能力 → 能力内部件**」——正是铁律一「两个能力的文件互相引用内部件」的违反样子。
两行唯一消费者是 `test/wizard-86.test.mjs`，而同一个符号 `test/t440-部位名单一来源.test.mjs:31` **早已从定义地**
`../dist/body/wizardPlate.js` 取。处置＝**删掉两行转出 ＋ 测试改指定义地**：

```
-export { WIZARD_CALIPER_LABELS, WIZARD_MEASURE_CAMEL, WIZARD_MEASURE_LABELS } from '../body/wizardPlate.js';
-export type { CompositionWizardView, MeasureWizardView } from '../body/wizardPlate.js';
-import { WIZARD_MEASURE_CAMEL } from '../dist/render/wizardPort.js';   // test/wizard-86
+import { WIZARD_MEASURE_CAMEL } from '../dist/body/wizardPlate.js';
```

两条测试因此收敛到**同一条 `dist` 路径**，`check-one-path` 由「薄转出声明豁免」转为「本来就一条路径」。
**这一条与票面「原样搬家」不同，据实记账**：`wizardPort.ts` 的 LF 由 277 降到 **275**，
台账「当场实测」列随之 `--sync` 派生（`改=1`）。其余三件 LF 不变。

### 1.4 出向引用的就地摆正（留在 `render/` 的两件是真共件）

`photo/wizardPort.ts` → `../render/errors.js`、`photo/wizardPortDocs.ts` → `../render/receipt.js`、
`photo/helpShell.ts` → `../render/errors.js`、`photo/templates.ts` → `../render/errors.js`。
`errors.ts`（14 LF／10 个能力在用）与 `receipt.ts`（243 LF／9 个能力在用）都过得了「写得出哪两个能力在用」，
属最后一批的真共件，**本窗不搬**（先例同 `#715` 对 `receipt.ts` 的处理）。

### 1.5 历史事实句照旧不动（具名 4 处）

- `src/body/wizardPlate.ts:2`／`src/body/wizardDocs.ts:2`／`src/body/wizard.ts:6`：记的是 **#353 当期**「自 `render/…` 原样迁入」
  ——改了等于改历史（#715 §1.3 同款口径）。
- `src/photo/helpShell.ts:4`：记的是 **#136 当期**的裁定「既有导入路径 `render/helpShell.js` 是 #134 八用例回归锁的锚点…
  故保留路径」——**那句「保留路径」在本窗之后已不成立**（路径改了），但它是当期裁定的记录，照旧不动；
  具名记在本节，垫片本身该不该退役见 §七.3。

收工自检按「代码行 0 处（要求 0）／注释行 4 处」两面分别报（§三.4）。

## 二 · 重出器与产物基线（判据的口径与产物来源）

- **重出器**：`.scratch/t716/regen.mjs`（入仓副本 `docs/skills/skill-calorie/t716-regen.mjs`）。覆盖口径按
  「搬迁面能影响到的页」定，不按页数好看定：
  - `wizardPort` ＋ `wizardPortDocs` 产出 `calorie.view.photo-log-wizard`（身材照向导）与
    `calorie.view.gif-planner`（GIF 规划器）两键（声明在 `src/photo/commands.ts:40-41`）
    ⇒ 两键**全分支**各出（空态／只给路径／路径＋标签／＋备注／路径含引号反斜杠／满 20 张；
    缺省 365 天窗／按标签／标签无候选／框选／框选含缺失 id／裁剪／全参数／日期窗／`days=1`／无标签），共 16 条；
  - `helpShell` 被 `photo/helpFile.ts` 用，出 `calorie.help.center` **缺省那一支**（V4 HELP 文件）；
  - `templates` 被 `photo/helpCenter.ts` 用（看板页入口那一节）⇒ 速查台三态（`inline`／`file`／`text`）也进面；
  - 另出**两条对照页**（`calorie.photo.list`／`calorie.photo.detail`，产出者都**不在搬迁面**）：
    它们必须也逐字节不变——它们变了就说明抖动来自重出器或种子库本身，不是来自搬家。本窗两条都绿。
  - 共 **22 条**：20 条 CLI 分支路径 ＋ 2 条对照页。
  - **票面键名订正**：票面写「`calorie.photo.gif-planner` 一类」——**该键不存在**，实测是 `calorie.view.gif-planner`。
- **种子库**：`.scratch/t716/seed.mjs`。确定性：日期靠时钟钉子（钉 `2026-09-18T12:00:00Z`）、文件内容用固定
  1×1 PNG 字节，不用随机数、不读 `new Date()`。**走写出口**（`calorie.photo.add`）而不是直接 INSERT——
  照片行的字段口径（标签规范化、同日序号编名、拷进照片目录）住在写处理函数里，直接 INSERT 等于把口径再抄一遍。
  另**特地逼出「缺失位」**：种完把其中一张从照片目录删掉，`toCard()` 的 `fileExists=false` 那一支才有页可看。
  库与配置都住本票草稿目录（`ILIFE_CONFIG_DIR` 指过去），**真库零触碰**。
- **时钟冻结**：`.scratch/t716/freeze.mjs` 经 `node --import` 预载，**复用仓内既有件**
  `packages/skill-calorie/test/freeze-clock.cjs`（#326 立，`FAKE_NOW_ISO` 钉钟）——不另写一份同类钉钟件。
  两个理由：① `photo/help.ts:62` 的 `new Date()` → `formatHelpMinute(now)`（HELP 文件副标题的分钟）；
  ② 只读页复制日志第 5 段走 `render/receipt.ts` 的 `nowStamp()`（`new Date()` 直读、无注入口）。
  不冻钟则同一份源码连出两遍跨 1 秒就逐字节不同——判据照字面既出假红也出假绿（#704 具名过这条前置）。
  实测 `STABLE-A-vs-B pages=22/22 DIFF=none`。
- **断言（失败即不落盘）**：每页 exit 0 ＋ 文档完整性四条（doctype／charset／`</html>` 收尾／零槽位残留）。
  `速查台(inline)` 与 `速查台(text)` 出的是**片段**与**纯文本索引**（设计如此，不是完整文档），
  故这两条标 `fragment` 只查非空与零槽位残留——**不放宽其余 20 条的判据**。

## 三 · 判据读数（逐条）

### 3.1 主判据：22 页产物逐字节相同

| 轮次 | 命令 | 读数 |
|---|---|---|
| 开窗当刻（`2c04918d`） | `node .scratch/t716/regen.mjs --head 2c04918d` | `RESULT: 22/22 页落盘 bytes_total=4947614`；`STABLE-A-vs-B pages=22/22 DIFF=none` |
| 搬迁后（重编＋重出） | `node .scratch/t716/regen.mjs --tag MOVE --compare .scratch/t716/基线.json` | **`MOVE byte-identical pages=22/22 DIFF=none`**；`total_before=4947614 total_after=4947614` |

比对口径＝逐页 `sha256` ＋ `bytes` 双比，22 条逐条比、不抽样。

### 3.2 变异自证三轮（打三块互不重叠的页族）

在**搬迁后的新家**里改坏一处会落到页面上的内容 → 重编 → 重出 → 与基线比；再**逐字节写回原文**
→ 重编 → 重出 → 再比。三轮共用一次持锁窗口（`.scratch/t716/mutate.mjs`，runId `c940a3b0`）。

```
【MUT1-向导页副标题】src/photo/wizardPortDocs.ts  subtitle 加「（改坏）」
MUT1-RED   byte-identical pages=16/22 DIFF=01,02,03,04,05,06        ← 恰是身材照向导那一族 6 条，每页恰 +12 B
  total_before=4947614 total_after=4947686
RESTORE-1  相等=true sha256_12=1f1e8b6af8ed
GREEN1     byte-identical pages=22/22 DIFF=none

【MUT2-模板清单】src/photo/templates.ts  从 CALORIE_TEMPLATES 去掉一个成员
MUT2-RED   byte-identical pages=19/22 DIFF=18,19,20                ← 恰是速查台三态，别的一字不动
  total_before=4947614 total_after=4946634
RESTORE-2  相等=true sha256_12=4cd66b417bb0
GREEN2     byte-identical pages=22/22 DIFF=none

【MUT3-HELP壳载荷】src/photo/helpShell.ts  透传给公共层壳的 data.title 改坏
MUT3-RED   byte-identical pages=21/22 DIFF=17                      ← 恰是 HELP 文件那一页
  total_before=4947614 total_after=4947638
RESTORE-3  相等=true sha256_12=61a4203e0e5a
GREEN3     byte-identical pages=22/22 DIFF=none
```

三轮的**识别面互不重叠**，合起来覆盖本窗四个搬走件的产出面：向导两页那一族（01–06）、速查台那一族（18–20）、
HELP 文件那一页（17）。**面外页一条都没红**——这就是「判据打的是页产物、且归因分得开」的证据。
还原判据＝文件字节 `sha256` 与改前逐字相同（**逐文件写回**，不是整目录还原）。

### 3.3 收尾三绿（票面「收尾三绿」）

| 门 | 命令 | 摘要行 | exit |
|---|---|---|---|
| 告警线 | `check-warning-line.mjs --sync --dry` → `--sync` → 无参 | `SYNC-PLAN mode=dry 改=1 增=0 删=0` → `SYNC-VERIFY ok` → `RESULT: 93/93` ＋ `PASS` | 0 |
| 生成链 | `gen-cli.mjs --check` | `GEN-CHECK PASS：键 134（写 52 ＋ 读 82）；能力 10 个` | 0 |
| 基线 | `baseline.mjs --save` → `--compare` | `SAVED`；`--compare` **红（外部阻塞，见 §四）** | 0 ／ 1 |

**台账那一条按 #704 的口径手改、不让 `--sync` 改名**：`件` 列地址从 `src/render/wizardPort.ts` 手改成
`src/photo/wizardPort.ts`，**挂号值 457 与「#354 挂号原文」那句结论原样带过去**（另按台账惯例补一段
「**#716 已在场**」记录）；`AGENTS.md` 的「台账两种口径」段同时补上现址。

**顺序纪律（本票实测的坑，具名留给后来者）**：手改台账**必须**排在 `--sync` 之前。若指望 `--sync` 顺手改名，
`planSync` 会把旧址那行 **DROP**（`check-warning-line.mjs:161`），再因为 `REQUIRED` 指向的件在扫描面内、
台账没有行而**自动补一行「挂号值 457 ＋ 待补模板结论」**（`:176-180`）——「#354 挂号原文」那句结论当场烧掉。
本窗实测计划恰为 `改=1 增=0 删=0`（**没有 DROP、没有 ADD**），说明手改生效、也没把别席的数字 baked in。

另跑两条结构门：`check-one-path.mjs` = `PASS`；`check-page-assert.mjs` = `PASS`。

### 3.4 收工机械自检（`.scratch/t716/move.mjs` 的第五步）

```
① 相对 specifier 解析：1651 条，未解析 0 条
② 旧址字面：代码行 0 处（要求 0）／注释行 4 处（历史事实句，逐条点名，见 §1.5）
RESULT: 搬迁与自检全绿
```

另跑靶向测试 5 件（`node --test`，持锁）：**`tests 40 / pass 40 / fail 0`**
（`photo-help-flow-345`／`wizard-86`／`help-shell-134`／`t445-告警线门`／`skill-t11`）。

## 四 · 收尾三绿的第 3 条（基线门）：**红，且红在窗外；具名它能证什么、不能证什么**

- 读数：`baseline: SAVED`（源码 3624 件／派生 1510 件／冻结 11 件，exit 0）；
  收窗比对 `baseline: FAIL（差异 1 项）`——`dist：派生面一致` ＋ `.gen-inputs.json：一致` ＋ `frozen：冻结文件一致`，
  只有 `source` 面变了：`（文件数 3624→3628）`。
- **根因可归因（不是本窗）**：`git log --oneline 252456eb..HEAD` = 一笔别席提交 `b2ba0b0e fix(678)…`（4 个件）。
  #715 §4.2 已经把这条具名过：`--compare` 证的从来不是「搬迁前后一致」，而是「**存基线那一刻之后这棵树没再被动过**」；
  在共享工作区里，这条结构性不可满足（碰运气），**不作为交付条件**。
- **取代它的判据（本窗采用，与 #715 §4.3 同一条）：**「本窗只碰了申报的路径」——
  ```
  git show --name-status 252456eb   → 恰 19 个路径（4 重命名 ＋ 15 改动），无一件他席的件
  逐路径 git log --oneline 2c04918d..252456eb~1 -- <路径>  → 19/19 全空
  ⇒ 我提交之前，这 19 个路径一件都没被别席动过；被别席动过的 = 0
  ```
  ⇒ 「搬迁前后一致」由 §三.1 的**页产物逐字节判据**承担（22 页 sha256 ＋ bytes，另有变异三轮证明它有识别力）；
  「窗口干净」由上面这条**范围判据**承担。两条分工不同、不能互相顶替。

## 五 · 必报五步：影响清单与交付对账（偏差为零）

- **第一步·影响清单（事前申报）**：搬 4 件（`render/` → `photo/`）；改引用 7 件生产
  （`photo/{wizard,helpFile,helpCenter}`／`shared/docPage`／`profile/setup`／`body/{wizardPlate,wizardDocs}`）；
  `render/index.ts` 收窄两行；改 5 件测试；门 1 件（`check-warning-line.mjs`）；台账 1 件（`AGENTS.md`）；
  交付物 1 件（本证据 ＋ 复跑脚本）。每行只指一个能力目录；`body`／`shared`／`profile`／`photo` 四行的理由都写在 §1.1。
- **第二步·结构设计**：搬完 `src/photo/` 是照片能力的家；四件的对外面不变（`wizardPort` 少两个死转出、
  `templates` 少一个 barrel 转出，都只许收窄）；留在 `render/` 的 `errors`／`receipt` 是真共件（10／9 个能力在用）。
- **第三步·写代码**：跨能力引用只改「跟着走的件离了目录就断」的那些行；常量与类型定义地不变。
- **第四步·超线报警**：四件均在线内（275／346／31／28，告警线 350）。三条超线件不在本窗写集：
  `render/trendPredictDocs.ts`（1190）、`src/photo/helpCenter.ts`（590）、`render/html.ts`（586）、
  `src/photo/photo.ts`（355）——**已超线，需要根据规则进行重构**，拆法见台账各行，本窗先不拆。
- **第五步·交付对账**：`git show --name-status 252456eb` 恰 19 个路径，与事前清单**逐行对得上，偏差为零**
  （`src/photo/helpShell.ts`／`templates.ts`／`wizardPort.ts`／`wizardPortDocs.ts` 四件以重命名计入）。
  暂存前复核过：索引里另有一批**别席的暂存删除**（`docs/skills/skill-calorie/t717-口径归一-证据.md`、
  `test/t717-口径归一.test.mjs`）——本席一行未碰、也未带进任何提交（提交用 `-- <路径>` 限定）。

## 六 · 票面计数器的**可归因**变化

开窗与收工各算一次（口径同一份 `.scratch/t716/scan.mjs`，`--pkg` 指到 `git archive` 解出来的另一棵树，
免得把别席的在途算进本票）：

| 读数 | 开窗 `2c04918d` | **本票提交点 `252456eb`** | **本票可归因** |
|---|---|---|---|
| ① 外面(`src/**` 非 `render/`) → `render/` 深路径 import 行数 | 184 | **184** | **0**（见下分解） |
| ② `render/` 反向 import 的能力目录数 | 11 | **11** | **0**（见下） |
| ③ `render/` 自身 | 28 件／8113 LF | **24 件／7429 LF** | **−4 件／−684 LF** ✅ |
| 附 测试面 → `dist/render/` 引用行数 | 39 | **36** | −3 ✅ |

- **① 为什么是 0（逐行分解，不是没降）**：`--dump` 逐行差集恰 8 行——
  **−4**：三个调用方改同目录（`photo/wizard.ts` 2 行、`photo/helpFile.ts` 1 行、`photo/helpCenter.ts` 1 行）；
  **+4**：搬走件对**留在 `render/` 的真共件**的就地摆正（`photo/wizardPort.ts`→`../render/errors.js`、
  `photo/wizardPortDocs.ts`→`../render/receipt.js`、`photo/helpShell.ts`→`../render/errors.js`、
  `photo/templates.ts`→`../render/errors.js`）。净 0。
  票面把这条列成「开工与收工各记一次」的读数（**不是断言它必降**），故据实报 0 并给出分解。
- **② 本批不降，如实报**：搬迁后 `render/` 里反向引 `photo` 的还有 6 行、引 `body` 的还有 3 行，
  全在 `render/html.ts` 与 `render/index.ts`——这两件本批不搬 ⇒ 目录数从 11 降到 10 要等它们各自的批次。
- **③ −684 LF 的分解**：四件 682 LF（`wizardPort` 277 ＋ `wizardPortDocs` 346 ＋ `helpShell` 31 ＋ `templates` 28）
  ＋ `render/index.ts` 收窄的 2 行 ＝ 684。

## 七 · 本窗具名未做项（不阻塞本票，逐条给下一手）

1. **全量测试面未跑**：本窗只跑靶向 5 件（40/40 绿）＋ 两条结构门。不跑全量的两个理由：全量约 40 分钟，
   且当刻工作区有他席在途改动（`t717-*` 的暂存删除与新增件）。**搬前没跑过一遍全量**，故不做「红都是既有红」
   这类全量断言——本窗只对**落在搬迁面内的**那几条给出归因（见 §7.2）。
2. **`test/t440-部位名单一来源.test.mjs` 的「判据③④」是既有红，不是本窗引入**（如实记账）：
   `src/body/receipt.ts` 含 2 个部位中文名（`:124` 的 `measure: { 胸围: …, 左大腿: … }` 一格映射），
   被该测试「别处不许再出现第二份 13 名表」那条判为重型命中。**判定依据（可复核）**：该件
   `git status` 干净、`git diff --stat HEAD` 为空（字节等于 HEAD `8b7d172a`，2026-09-15 提交），
   且**不在本票写集**——本窗搬的是 `render/`→`photo/` 的四件，不可能给 `body/receipt.ts` 添命中。
   溯源＝该格是回执页的「部位 → 说明」表，本就不是第二份 13 名表（属 #702 口径第 2 条「断言锁在实现上」那一类）；
   转票措辞留给收口，本窗按红线**一行未动**。
3. **`helpShell.ts` 这枚兼容垫片的退役条件已变**（具名，不在本窗做）：件头自述的存在理由是
   「既有导入路径 `render/helpShell.js` 是 #134 回归锁的锚点……故保留路径」——**本窗把路径改了**，那条理由随之作废。
   该件今天仍是 `base-paint/help-shell` 的转发＋错误类映射（铁律五说的「一层只做转发的包装」）。
   本窗不顺手退役：一窗一变更（搬迁判据是保字节的强不变量），退役要连同 #134 测试的锚点一起动，属下一票。
4. **`dist` 里有旧产物残留**：`tsc -b` 是增量构建，**不删已搬走源码的旧产物**。
   当刻 `packages/skill-calorie/dist/render/` 下有 **28 件孤儿**（源已不在此目录，含本窗四件与 #704／#714／#715
   搬走的那些）。`dist/` 在 `.gitignore` 里、不入仓，且判据走真交付出口（`dist/cli/cmd_read.js` 按声明取 `run`），
   故本窗判据不受影响；**但下一手要知道**：按 `dist/<旧址>` 直取的断言会读到陈旧代码而**响亮地红**，不会静默绿。
5. **旧 barrel 的退役**：`render/index.ts` 本窗只收窄了 `templates` 两行（118→116 LF），整体退役仍归
   「包对外的公开接口缩到一页」那条线（#703 已关；本件当刻 116 LF）。
6. **台账里本件那一行的拆法原样留着**：结论仍写「拆法待后续票确定」——搬迁完成后地址已改、拆法照旧
   （#704 已裁：先搬、后拆）。

## 八 · 门禁运行对账（协议 §2.4）

```
GATE-RUN runId=efb74014 cmd="tsc -b packages/skill-calorie（搬前，确认 dist 与源码同步）"                        exit=0
GATE-RUN runId=3e30a2ef cmd="tsc -b packages/skill-calorie（搬迁后；等别席 715 释放锁 waitedMs=20012）"          exit=0
GATE-RUN runId=c940a3b0 cmd="node .scratch/t716/mutate.mjs（变异三轮＋还原，持锁窗口）"                          exit=0
GATE-RUN runId=3f624973 cmd="check-warning-line.mjs --sync（改=1 增=0 删=0；SYNC-VERIFY ok）"                     exit=0
GATE-RUN runId=a90e2f92 cmd="node --test …（5 件靶向；T8 一条本席漏改的硬编码期望串 → 红）"                       exit=1
GATE-RUN runId=b68d9ae3 cmd="node --test …（同上 5 件，修好锚点后 40/40 绿）"                                     exit=0
GATE-RUN runId=7a3438fc cmd="baseline.mjs --save .scratch/t716/基线-收窗.json（SAVED）"                          exit=0
GATE-RUN runId=fc1f4191 cmd="git add ＋ git commit（19 路径）"                                                    exit=0
GATE-RUN runId=e9235a5d cmd="baseline.mjs --compare .scratch/t716/基线-收窗.json（别席提交致 source 面差异）"      exit=1
```

复跑脚本入仓副本：`docs/skills/skill-calorie/t716-{regen,freeze,seed,move,mutate,scan}.mjs`。
（副本按 `.scratch/t716/` 的布局写：`HERE` 的上一级两级当仓根、库与临时产物取同目录下的 `db/`／`tmp-out/`——
复跑时拷回 `.scratch/t716/` 即为原样。）盘点与留痕类（`基线.json`／`基线-收窗.json`／`产物/`／`trees/`／
`target-tests.log`／`backup/`）住 `.scratch/t716/`（`.scratch/` 不入仓）。

## 九 · 开工前四件待裁的处置（用户 2026-09-19 逐条认可）

| 待裁 | 处置 | 依据 |
|---|---|---|
| 冻结挂号身份（`wizardPort.ts` 的 457） | **机器两处改路径、活复述两处跟着改；需求原文 `t169:92` 逐字不动**，台账结论列与本节具名记账 | 台账自己的口径「挂号值＝历史事实永不回改／『件』列地址随实况走」；门**不需要**新机制——忘了改 `REQUIRED` 会当场红（`冻结件不在扫描面内`／`台账缺行`）；#715 §1.3「历史事实句照旧不动，改了等于改历史」 |
| 边界件 `templates.ts` 落点 | **一并搬进 `src/photo/`** | 两类准入都不成立（写不出两家 ⇒ 不是共用位；不是「出整页必经」⇒ 不是页面交付面）⇒ 只能回它唯一消费者的域。#704 把它排进「最后一批」的读数是错的：`cli/delivery.ts` 与 `output.ts` 一次都没引它 |
| `wizardPort.ts:49-50` 身体域死转出 | **删掉两行 ＋ 测试改指定义地** | 搬迁把这条边从「工种名目录→能力内部件」变成「能力→能力内部件」，是铁律一的违反样子；违规是搬迁做出来的，本窗摆正 |
| 重出器／证据件入仓 | **照 #715／#708 入仓** | 票面「重出器住 `.scratch/<票号>/`」的字面已被后来两票超越；`.scratch/` 不入仓 ⇒ 只入仓才可复跑 |
