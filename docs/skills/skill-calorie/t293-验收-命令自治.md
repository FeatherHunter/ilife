# #293 验收 · 命令自治（独立探针）

**这份是什么**：一份**独立验收探针**，不引用任何一方的自述，只从仓内可观测事实与命令输出判定五条承诺
（单一权威源／两道机器门／薄分派／parity 有主）。它**不进** `pnpm test` 的 glob——根 `package.json` 的测试
glob 只覆盖 `test/**` 与 `packages/*/test/**`，本文件住在 `docs/skills/skill-calorie/`。

## 怎么跑

```pwsh
node "docs/skills/skill-calorie/t293-验收-命令自治.mjs"          # 人读
node "docs/skills/skill-calorie/t293-验收-命令自治.mjs" --json    # 机器读（末尾一行完整 JSON）
```

- **输出怎么读**：每条先打 `P1: PASS|FAIL|PENDING`，缩进行是证据（计数／file:line／命令 exit）。
  最后一行是机读摘要：`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`。
- **退出码**：有 FAIL → `1`；PENDING 不算红，但摘要行会点名。
- **缺件不崩**：少文件／少生成器缺脚本 → 该条报 `PENDING` 并写明缺什么，其余条照跑。
- **不写仓内文件**：动态实验一律在**仓外临时根**（`%TEMP%\t293-accept\sandbox-<pid>-<ts>`）做，用完即清；
  删除前做路径守卫（目标必须在自己临时基底内、不在仓内、路径不含 `node_modules`／`packages`／`docs`／
  `test`／`tooling`／`.git` 任何一段）。唯一落盘：`.scratch/t293-accept/probe-report.json`（过程稿）。

## 逐条：验的是哪句承诺 · 怎么判 · 现在结果

### P1 自治 —— 「加一条命令，除了能力目录里的声明与生成物，还有哪些手写文件必须跟着改」

承诺：地图 #293 第二级（`c993035`）「把一条命令的事实从汇总位挪到权威声明，汇总位一律派生」。
判据：静态读生成器输入／输出面（源码里数，不靠自述）＋ **仓外沙箱真跑**（复制最小子树，加一条假命令）
＋ **手写面三桶**。

⚠ 只数生成器输出面会报**假绿**：同一个 `build-help.mjs` 里还有第二处手写的「一条命令的事实」，
另有若干钉死计数的断言也不在派生面上——#295 红队审查（`5af4dd1`）实测点破，本探针据此补全三桶，
并把 **UNACCOUNTED 非空 ⇒ `P1=FAIL`** 写死。

**① DERIVED（不用手改）**

| 面 | 凭什么算派生 |
| --- | --- |
| `src/cli/keys.ts`／`src/cli/registry.ts`／`combos.yaml` 镜像段／`build-help.mjs` 的 REPR 块 | 生成器输出：`pnpm gen` 重生成、`pnpm gen:check` 守；沙箱实测加假能力后一次补齐 |
| `build-help.mjs` 的 EXAMPLE 表 ＋ 读它的 `exampleFor`（返修 A3 后：`:226` 起读生成表 `EXAMPLES`） | 示例住声明的 `example` 字段（`CommandSpec` 必填）→ 新键不必手改本文件；沙箱实测假键的示例自动落表 |
| `src/cli/cmd_read.ts`／`write.ts` | registry 先行（`cmd_read.ts:268-272`、`write.ts:213-214`）；沙箱实测两文件**逐字节未动** |
| 已改成与权威声明对账的计数断言（`test/declared.mjs` 的消费者，`467cf64`；返修后又并入若干） | 数字从声明现算，加命令不必手改 |
| 能力目录的声明 `src/<能力>/commands.ts`（＋`index.ts` 再导出那条数组） | 权威源本身：gen-cli.mjs 扫 `src/*/commands.ts`；生成的 registry 从 `../<能力>/index.js` 取 |

另：`src/cli/legacyCommands.ts` 只在**搬迁**一条老键时必删一行（生成期同键两处声明即抛；沙箱实测 exit 1 报
「命令键重复登记」）；加**新**键不用碰它——故不在③里。

**② DISCIPLINED-SHARED（必须手改，但已被认下 → 写进 #296「照抄说明」第②项）**

| 面 | 为什么必须手改 | 机器守 |
| --- | --- | --- |
| `packages/skill-calorie/src/triggers/routing.ts`（手写 exec 路由，现覆盖 101 键） | 新键要能被唤醒词命中就得加一条路由；冻结的 `scene-*.ts` 不许往进加 | `test/calorie-routing-81.test.mjs:105` `coveredKeys = DECLARED_KEYS.length` ⇒ 新键不加路由即「覆盖数 ≠ 声明数」（红队实测 exit 1） |

**③ UNACCOUNTED（必须手改，既没派生也没纪律覆盖 ⇒ 有它就 P1=FAIL）**

| 面 | 为什么必须手改 |
| --- | --- |
| `packages/skill-calorie/test/output-naming-119.test.mjs:67` | `assert.equal(receiptCount, 35, 'receipt 形写键须为 35 个')`——写键数钉死在断言行里 |

（探针**以当刻树实测为准**：桶面归属逐次实跑重数，不沿用旧快照。历史（返修前）：返修席曾把 `exampleFor`
与另 6 条钉死计数移进 ①，旧快照读作 `exampleFor:108-219` ＋ 7 条钉死计数共 8 处——不再与现读数混读。）

**历史（返修前）；现以探针实时输出为准**：最近一次实跑快照 **FAIL**（`UNACCOUNTED` 1 处：`output-naming-119.test.mjs:67`）。派生桶与纪律桶的
动态证据全绿：沙箱假能力被生成器扫到、假事实自动流进四件生成物＋EXAMPLE 表、两个分派文件与 `legacyCommands.ts`
逐字节未动、撞键被生成期机器拦下。**这是本次验收的真实结论**——本探针**不去改实现把它翻绿**。

**分工建议**：剩下这 1 条钉死计数 → **#295 返修**（改走 `declared.mjs` 对账）；`routing.ts` → **#296 写成纪律**
（照抄说明第②项：加命令必须同步加路由，动前抢锁）。

### P2 单一权威源 —— 「同一件事实还有几份手写副本」

判据：对体重 9 键（#294 已搬迁的那批）逐键 `git grep`，把每一处命中按归属分类
（AUTHORITY／DERIVED／FROZEN-PARITY／ROUTE／SWITCH／TEST／DOC），并单独找「键＋代表唤醒词／标题」同行的**事实对**。
出现 `UNCLASSIFIED` 即「还有手写副本没清」。

**现在：PASS** —— 权威声明 9/9（每键恰 1 处，在 `src/weight/commands.ts`）；未归类命中 0；
未归类事实对 0；9 键在 `legacyCommands.ts` 里剩 0 处（搬走即删行）；事实对只落在冻结词表（12 处，P5 有断言守）／
路由表／测试／文档／派生件上。四条派生件自证：`keys.ts`／`registry.ts` 有「本文件由 gen-cli.mjs 生成」横幅，
`combos.yaml`（:6／:512）与 `build-help.mjs`（:11／:106）有 GEN-CLI 标记块。

### P3 两道机器门

| 子判据 | 现在 |
| --- | --- |
| ① `gen:check` 存在？（根 `package.json`） | PASS：`node packages/skill-calorie/scripts/gen-cli.mjs --check` |
| ② CI 真调用？ | PASS：`.github/workflows/ci.yml` 里 `pnpm gen:check` 那一步，同 job 先跑 `pnpm build`（生成器读 dist，缺则必挂）；行号不冻在文档里，以当刻该文件为准 |
| ③ 门是真门？（手改生成物→非 0；还原→0） | PASS：仓外沙箱 `keys.ts` 加一行注释 → exit 1 报 `GEN-CHECK FAIL`；还原 → exit 0 |
| ④ 棘轮 `cmd-registry-294.test.mjs` 是否被放宽？ | PASS：`git diff 1396d67 -- …` **逐字为空**；assert 25→25、test 块 7→7 |

### P4 薄分派

**现在：PASS**
- `src/cli/cmd_read.ts`：`case '` 共 67 处，其中 `calorie.` 前缀 **61** 处（唯一 61）；物理行不以数字冻在文档里，以当刻实测为准（读数见探针实时输出）。
- `src/cli/write.ts`：`case '` 共 34 处，其中 `calorie.` 前缀 **34** 处（唯一 31＝3 处 fall-through 重复标签）；物理行同上，以当刻实测为准。
- 两文件都 registry 先行（各有 `const spec = REGISTRY[key]`）。
- 体重（场景 03）**5 读 ＋ 4 写 ＝ 9 键，在分派两个文件里的 `case` 分支数合计 0**——已无分支。

### P5 parity —— 「哪几条既有断言在守体重产物逐条不变」

本探针**只点名、不重算**（重算一份就是又抄一份）。现在：PASS，7/7 命中：

| 断言 | 守什么 |
| --- | --- |
| `packages/skill-calorie/test/sport-homogeneity-109.test.mjs`（:3、:98-99、:186-191、:264） | **主守**：运动/身体域 9 键（含 view.weight／weight-history／weight-compare／weight-review／volatility）唤醒词命中＋HTML 同质＋无假数据 |
| `packages/skill-calorie/test/cmd-registry-294.test.mjs:191-193` | 注册表恰为体重 9 条；每条声明的 shape／title／kind 与写键表逐条对账 |
| `packages/skill-calorie/test/cmd-write-40-persist.test.mjs:233-240` | `calorie.weight.log` 落库列逐列（bmi 等） |
| `packages/skill-calorie/test/cmd-write-40.test.mjs:135` | 写链 parity 抽查（体重写键） |
| `packages/skill-calorie/test/render-t41.test.mjs:98、:285` | 渲染锚点 `ilife:calorie:weight-history` |
| `test/calorie-routing-81.test.mjs` | 唤醒词路由 parity（冻结词表逐条恰一次，键必在键表内） |
| `packages/skill-calorie/test/wake-assets-133.test.mjs` | 唤醒词素材与冻结场景表 parity |

## 本次实跑结论（快照）

```
RESULT: P1=FAIL P2=PASS P3=PASS P4=PASS P5=PASS 4/5
```

**历史（返修前）；现以探针实时输出为准**：两种模式都跑过（默认人读／`--json` 一行 JSON），退出码都是 `1`
（有 FAIL 即红），结论一致。**P1=FAIL 是那次实跑的真实结论，不是探针故障**：`UNACCOUNTED` 读得 1 处
（`packages/skill-calorie/test/output-naming-119.test.mjs:67` 的 `receiptCount, 35`）。⚠ 返修席在同一棵树上施工，
本探针**以当刻树实测为准**——`exampleFor` 与另 6 条钉死计数已被它移进 ① 派生桶；返修前那份快照是 8 处。

探针的判定口径写死在代码里且**能红**：生成器输出面变窄（keys／registry／combos 镜像／build-help 少一件）、
四件生成物缺派生标记、分派不再 registry 先行、生成期不再拦同键两处声明、沙箱里假能力不被扫到／假事实不进生成物／
撞键不被拦、`exampleFor` 形态不认识、以及 `UNACCOUNTED` 非空——任一条 ⇒ `P1=FAIL`；树自相矛盾（生成的
`EXAMPLES` 表条数 < 权威键数）⇒ `P1=PENDING`。

修过的口径（过程留痕）：① 假绿——生成器输出面正则只认 `join(...)`，漏掉 `COMBO_YAML`／`BUILD_HELP` 两处；
② 假绿——`git grep` 没加右边界，`calorie.view.weight` 前缀吃掉了 `weight-history`；
③ 假红——小数点里的数字被当成计数（`22.9` 里的 `9`）；
④ 假红——沙箱里假声明漏了返修后新增的必填字段 `example`，`gen` 因别的原因红，被误记成"撞键被拦"。
