# t650 · 写门三条红：修法与证据

票：#650（`cmd-write-40-persist` 三例红）。结论：测试侧修，源码零改动；`node --test packages/skill-calorie/test/cmd-write-40-persist.test.mjs` 22/22 exit 0。

## 一、复现读数（2026-09-17，修前）

`node --test packages/skill-calorie/test/cmd-write-40-persist.test.mjs` ＝ 17 pass / 3 fail：

1. 口径 · 删除命令数据驱动：`calorie.product.deprecate 文案缺「软删除：行保留，已从查询与统计中排除；暂无恢复入口」：已下架：燕麦片（不再出现在搜索和统计里；暂时无法恢复）`
2. 口径 · 删除回执可恢复性：`product.deprecate 文案缺 软删除：行保留，已从查询与统计中排除；暂无恢复入口：已下架：鸡胸肉（不再出现在搜索和统计里；暂时无法恢复）`
3. 覆盖门：`以下写键没有「写后 SELECT 回读」断言：calorie.goal.exercise、calorie.workout.land、calorie.workout.land-monthend、calorie.workout.land-weekend、calorie.workout.xunji-backfill、calorie.workout.xunji-push`（实测 6 条，票面记 1 条）

## 二、归因

- H1（前两条）：产品下架回执摘要在饮食线文本波次中改成人话（`src/diet/products.ts` 注释点名此次改动，共享常量 `SOFT_EXCLUDED_INNER` 一字未动），断言仍钉旧词条。运动／体脂／围度三域仍用旧词条，故同文件其余口径断言为绿——不是业务坏，是断言过期。
- H2（第三条）：定运动目标命令（#621）与落地训练族 5 条命令（#612／#613／#614）注册为会改数据库的命令后，落库断言里没有对应的写后回查块。
- 范围修正：票后新增的落地训练族把缺口从 1 条扩到 6 条，本票按 6 条修（名单不许缩）。

## 三、改法（只动 `packages/skill-calorie/test/cmd-write-40-persist.test.mjs`）

1. 新增 `PRODUCT_DEPRECATE_TAG ＝ '（不再出现在搜索和统计里；暂时无法恢复）'`（摘要里的稳定括号段，品名逐条变故不断言它），两处旧词条期望（数据驱动 `DELETE_CASES` 产品条、可恢复性鸡胸肉条）改钉它。`items[].status` 期望不动（`已下架（软，不可恢复）` 本就与实现同源）。
2. 新增「定运动目标 exercise」落库块：空库写 300 → 回读 300；改写 500 → 回读 500（`daily_goal#1.exercise_goal` 单列，INSERT 与 ON CONFLICT 两路都走到）。
3. 新增「落地训练族 5 条命令」落库块：四路挡板全绿跑非预演，跑前直读快照、跑后经 `withRead` 重算比对＋回执成功形断言。快照是整库 JSON 定序（`workout_plan_config／workout_plans／exercise_log` 逐行＋其余 8 表行数），改值／增删相抵／表外写都看得见。为「零本地写」而非「逐列回读」的原因：挡板短路后四条腿（作息合成写／备忘合成写／训记推送／训记回写）都不进子进程，真写发生在外部进程与远端；断言注释里写明了这一前提，日后加本地写即红。`land-monthend` 取月末锚点只跑 1 天（`t613` 同形，避开整月子进程；多天批量路径由 `land-weekend` 7 天覆盖）。跑前快照故意不经 `withRead`（经它会计入覆盖门读数，掏空跑后块即不红）。

牙齿验证：新标签与新摘要互含、与旧句双向互斥（脚本实测 `TEETH-OK: true`）；修前基线即证明旧期望在新摘要下红、覆盖门在缺回查下红；反向对照（副本掏空 `xunji-push` 跑后块）：该块与覆盖门双红，`withRead` 自检与缺命令名单两道都响，副本已删。

## 六、code-review 意见处置（双轴）

标准轴：硬违规 0。采纳 1 条（推送／回写两对字面相同的挡板桩提为 `PUSH_OK_DATA／BACKFILL_OK_DATA` 共用）；其余判断性意见保留现状——逐命令显式块合本文件惯例（失败点名到命令，数据驱动收表反而藏行号）。

规格轴：
- (a1)证据件缺失→不成立：本件即证据件，随提交落盘（评审时未提交故 diff 外不可见）。
- (a2)「第二条文案无改动」→不成立：票面「两条」指两处同文案断言（数据驱动燕麦片条、可恢复性鸡胸肉条），两处都已改钉，22/22 为证。
- (a3)monthend 只跑 1 天→维持：月末锚点按命令语义即 1 天，多天批量路径由 weekend 7 天覆盖，与 `t613` 同形。
- (c1)跑前快照计入覆盖门→成立，已修（跑前改直读）＋反向对照验证双红。
- (c2)COUNT 看不住改值／相抵／表外→半成立，已加强为整库 JSON 快照（相关表逐行＋其余 8 表行数）。
- (c3)「暂时无法恢复」与 0 恢复入口矛盾→转交：该句是 #496 产品文案，本票按「当刻口径」只钉不断言可恢复（`promisesRecovery` 仍过）；改文案归口径 owner，不在本票动。

## 七、当场解决邻居红（维护者指令：不开票，2026-09-17）

第一性原理审查结论：7 处全属同一类——**源码侧 deliberate 改完、测试钉没跟上**（#340 补单位空格、#481 对比族换人话、#455 补 90 天末点、#503 形状化改名），用户面文案本身都是改进；红的是钉死旧文案的断言。套件红会掩盖真回归，所以是真问题，当场修测试侧；回滚源码等于撤销三票 deliberate 决策，一致否决；放宽断言等于丢牙齿，一致否决。

| # | 文件：行 | 改法 | 口径依据 |
|---|---|---|---|
| 1 | cmd-write-40:139 | `/70.2→70 kg/` ⇒ `/70\.2 kg → 70 kg/`（点转义附带） | #340＋全仓空格惯例 |
| 2 | softdelete-120:169 | C5 期望改新人话（含 `1 个月`），指针 `:68` ⇒ `:85` | #481 |
| 3 | analysis-t5:168 | `/startA/` ⇒ `/自定义两段时间需要四个月日期/` | #481 同行 |
| 4 | analysis-t5:181 | `平台期首日` ⇒ `平台期第一天` | #481 缺陷 12（weight-compare-334 已绿佐证） |
| 5 | analysis-t5:232 | `13` ⇒ `14`（1＋12＋1，#455 末点） | #455（表头 90 天不再说谎） |
| 6 | analysis-t5:170/172 | `减重 5kg 那天` ⇒ `减重 5 kg 那天`；`体重轨迹` ⇒ `体重变化曲线`（spark 照查）；`/未达成/` ⇒ `/还没减到 50 kg/` | #340 系＋#503＋#481 |
| 7 | analysis-t5:190 | `睡眠数据`＋`缺失` ⇒ `睡眠`＋`没有记录`（精确相等） | #481（默认分支确定） |

读数：5 文件 59/59 exit 0（persist／cmd-write-40／softdelete-120／analysis-t5／weight-compare-334），另 weight-history-333 对照 3/3。

刻意不碰：`docs/research` 历史探针与审计文档（当时证据，不改写历史）；唤醒词输入面紧凑形（`减重 5kg…`、`平台期首日` 输入别名表仍有效，输出标签才换人话）；`weightCompare2.ts:69` 那条 14 天 generic（另一检查，无测试钉它）。

## 四、读数（修后）

- 目标文件：22/22，exit 0（含新增 2 块）。
- 邻居回归（`cmd-write-40`／`softdelete-120`／`exercise-goal-write-621`／`exercise-goal-precheck-622`／`deprecated-126`）：26/28。2 红与本票无关且文件均未动——体重回执空格波次（`70.2→70 kg` 期望 vs `70.2 kg → 70 kg` 实现）、`softdelete-120` 比对文案波次；属别票口径漂移，已报维护者定夺是否另开票，本票不碰。

## 五、自查打勾

- [x] 边界：非法参数路径由 `t621` 已钉（本票只加合法值回读，不重复）。
- [x] 异常分支：空天路径被 `land-monthend`（月末空天）与 `land-weekend`（5 个空天）走到；`dryRun` 零写是契约（别票覆盖），覆盖门按命令计数不按参数变体计数。
- [x] 回归影响：源码零改动；钉旧词条的其余测试（运动回执族）实现未变，继续绿。
- [x] 并发与旧数据：每块独立临时库；只读句柄串行；固定日期无 `today()` 依赖；`plan-set` 种子复用本文件既有 helper，无新耦合。
- [x] 可能漏处：子进程耗时（`land-weekend` 7 子进程约数秒，当前整文件 44 秒可接受）；挡板 JSON 形状须与 `asPushSummary／asBackfillSummary` 守卫一致（已照 `t612／t614` 同形抄，`verify_note` 必填已带）。
