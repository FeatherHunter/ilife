#456 围度键集对账证据

## 判定

- 本票交付：`cross.ts` 只加 `export` 一词；新建对账测试 1/1 绿；`t361` 8/8 绿。
- `t440` 6/7：唯一红项系基线已红（`body/receipt.ts` 含 2 个部位名，#537/#539 已提交内容），与本票无关，本票未碰该件（跨界停手，见 §5）。

## 1. 两集合（文件:行号，各自列出）

- 展示列序：`packages/skill-calorie/src/fetch/body.ts:29-34`（`MEASURE_DEFS`），键序即列序：
  `chest_cm,waist_cm,abdomen_cm,hip_cm,left_thigh_cm,right_thigh_cm,left_calf_cm,right_calf_cm,left_arm_cm,right_arm_cm,left_forearm_cm,right_forearm_cm,shoulder_cm`（13 项，`shoulder_cm` 第 13 位）。
- TOP 平局次序：`packages/skill-calorie/src/analysis/cross.ts:15-19`（`WAIST_DIVERGENCE_ORDER`，本票加 `export`）：
  `chest_cm,waist_cm,abdomen_cm,hip_cm,shoulder_cm,left_thigh_cm,right_thigh_cm,left_calf_cm,right_calf_cm,left_arm_cm,right_arm_cm,left_forearm_cm,right_forearm_cm`（13 项，`shoulder_cm` 第 5 位）。
- 次序不同是行为（TOP 并列取前 3 vs 展示列序），不断言次序相等，只断言集合相等＋各 13 项。
- 绿读数（`final-t456.log:1`）：`T456-READOUT columns=13 top=13 shoulder_pos(columns)=13 shoulder_pos(top)=5 … VERDICT=GREEN`。

## 2. 门禁运行（GATE-RUN 对账源：`.scratch/locks/gate-runs.log` 对应行）

- GATE-RUN runId=f614a48d-fa41-416b-bde6-b248b85531b6 cmd="npx tsc -b packages/skill-calorie" exit=0
- GATE-RUN runId=985a4965-e8c2-4230-af8f-b29e7bf665d9 cmd="node --test packages/skill-calorie/test/t456-围度键集对账.test.mjs" exit=0（pass 1/fail 0）
- GATE-RUN runId=029f66a0-1d55-47f9-98df-e4ff528bb512 cmd="node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs" exit=0（pass 8/fail 0）
- GATE-RUN runId=6238f1e2-b826-4a64-bf36-18a2425d2797 cmd="node --test packages/skill-calorie/test/t440-部位名单一来源.test.mjs" exit=1（pass 6/fail 1，见 §5）
- 最终态复验（BOM 清理后重建）：GATE-RUN runId=8d5e3da3-f613-40b0-91d1-cf00b14d678e build exit=0；runId=10646dcb-0fd1-4802-a973-79d74aa8bf89 t456 exit=0；runId=6172688c-7e77-47dc-a1ee-f1519e78b99c t361 exit=0；runId=28b136bc-4db1-45ef-999a-8b9d20748010 t440 exit=1（同 §5）。
- 日志：`.scratch/t456/{build,t456,t440,t361,final-build,final-t456,final-t361,final-t440,mut1-*,mut2-*}.log`。

## 3. 变异自证（改坏必红＋还原必绿，两行机器读数）

- M1（列序侧加假第 14 键 `fake14_cm`，源码级 transient，还原已验 `git diff` 无残留）：
  - 红：GATE-RUN runId=c8b19c57-d6bf-4e3a-883e-8beba3f46365 exit=1，`AssertionError: 展示列序表应有 13 项（实测 14）`（`mut1-red.log:15`，pass 0/fail 1）。
  - 绿：GATE-RUN runId=5523e14e-86b6-419b-ac37-455fb0a63cd2 exit=0（`mut1-green.log` pass 1/fail 0）。
- M2（TOP 侧删 `hip_cm`，源码级 transient，还原已验）：
  - 红：GATE-RUN runId=42a79c0d-5a32-4e47-a7b2-4c366e061e18 exit=1，`AssertionError: TOP 平局次序表应有 13 项（实测 12）`（`mut2-red.log:15`）。
  - 绿：GATE-RUN runId=14353d9a-aa7c-4b0f-81e9-1f67b4e19940 exit=0（`mut2-green.log` pass 1/fail 0）。

## 4. 真库零写入

- 真库 `D:\2Study\StudyNotes\.db\calorie_data.db` 跑前跑后同一指纹：Length=3072000，mtime=2026-09-15T15:36:54.7063302+08:00，SHA256=D40D58F50E74E294A11E410B4D9E2142FCBCD8993328A4EFB453F1A0D62A1083。
- 本票测试：t456 纯内存断言不开库；t440/t361 走 `mkdtemp` 临时库（`SKILLS_DB_PATH` 指临时目录）。

## 5. t440 红项归因（基线已红，非本票引入）

- 红断言：`#440 判据③④`，实测 `[["body/receipt.ts",2],["fetch/body.ts",13]]`，期望只有 `fetch/body.ts`。
- `body/receipt.ts` 在本票工作区未被本票改动（本票 `git diff` 仅 4 件：见 §6；该件 2 处部位名来自已提交的 #537/#539）。
- 本票无害证明：t440 第三项「展示层四件」仍 ✔（`cross.ts` 部位名命中数仍为 1 且仍引用 `MEASUREMENT_ZH`），即本票一词导出未改变任何部位名表面。
- 处置：跨界停手，不碰该件，上报编排者（另开票收口 t440 基线）。

## 6. 提交（只含本票 4 件，不含 `body.ts`）

- COMMIT1_SHA（src＋test）：见回执。
- COMMIT2_SHA（证据＋changeset）：见回执。
- `git diff --cached --name-only` 复核：仅 `packages/skill-calorie/src/analysis/cross.ts`、`packages/skill-calorie/test/t456-围度键集对账.test.mjs`、`docs/skills/skill-calorie/t456-围度键集对账证据.md`、`.changeset/t456-围度键集对账.md`。

## 7. 结构对账（第五步）与超线

- 影响清单四件逐行有着落，偏差零；`cross.ts` 改后仍 292 行（线内 350），`body.ts` 只读未改：第四步不触发，无需重构。
- 并发事件：变异窗内观测到他席活写 `body.ts`（`M1-ANCHOR`／`M2-ANCHOR` 两处注释摘除）；本票变异脚本曾以 `Set-Content utf8` 误带 BOM，还原后已按字节剥离（`cross.ts` 回到单行差，`body.ts` 零字节残留，他席改动原样保留）。

## 8. 未做项＋下一手缺什么

- 未做：t440 基线回绿（需另票处理 `body/receipt.ts` 2 处部位名）；遗留出口两项（两处合一次序／`triggers` 第三份表收编）均不在本票。
- 下一手：复核者持锁独立复跑 `final-t456`／`final-t361`（t440 以基线另票为准），认 `git diff HEAD --stat` 仅本票 4 件。
