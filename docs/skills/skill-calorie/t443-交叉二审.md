# #443 交叉二审（地图 #158 简单票第二人；只认 sha 与自己复跑）

## 段1｜判定：同意一审 PASS（本席独立复核：PASS，约 94 分）

- 重读票面五段（`gh issue view 443`，OPEN）：目标＝收紧 #361 复核点名的两条 S3（①全空行真出口断言 ②载荷逐字段口径），不改行为；验收＝三条（靶向 8 用例真出口＋逐字段＋备注—保留；M3 全文复位必红／还原必绿；M4 删新用例计数减 1／还原复原）；不许动＝`src/**` 行为、13 唤醒词／命令名／示例、真库零写入、排 #440 之后；交付＝测试就地收紧＋新建 `t441-判据收紧证据.md`；遗留＝触发器是否挂 UPDATE 另议、中文名归 #440。
- 结论：**同意一审 PASS**。两条口径均收紧且本席独立复跑自证齐；`src/**` 零改动（443 自有提交范围）；真库零写入。量刑：无 S1 交付／过程违规；五维契约 29／证据 23／对照 19／红线 14／文档 9，约 94 分（≥85）。证据分扣 1：见段 4 ①（一审复核 §2 的 diff 范围措辞 slip，实体无损）。

## 段2｜机器证据（只认 sha 与自己复跑；对账源 `.scratch/locks/gate-runs.log`）

- 实现提交（只认 sha，已验 `--stat`）：`5d9a6ba`（测试 1 件 `+81/-13`）＋`e7f7e7d`（证据＋记账 2 件新建）；复核 `bad8492`（本件 1 件）。`git show` 逐件已验：`5d9a6ba` 仅测试、`e7f7e7d` 仅文档＋记账、`bad8492` 仅复核件；`git diff 5d9a6ba~1 5d9a6ba -- src/` 为空。
- 本席独立重跑（`tooling/run-locked.mjs --ticket 443`，全部持锁）：
  - 靶向关键断言 `bd260ec8`：`node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs` → exit 0，`tests 8 / pass 8 / fail 0`，`T443-READOUT empty-row=13/13 via=shim`，`T361-READOUT names=13/13`（日志 `.scratch/t443/review2-target.log`）。
  - 自设新变异 N2（与一审 N1 不同轴；一审 N1 测载荷字段写—，本探针测全空行断言是否空心：`UPDATE …SET shoulder_cm=NULL` 中性化为 `=44`，该行不再全空）`b957e463`：`REVIEW2-443 N2-noempty-exit=1 fail=1 red-OK`／`N2-restore-exit=0 pass=8`／`RESTORE-IDENTICAL=true RESULT=2/2`（脚本 `.scratch/t443/review2-mut-n2.mjs`，日志 `.scratch/t443/review2-mut-n2.log`，`waitedMs=30025` 系等 ticket=561 活锁释放，已披露）。
- 一审读数本席逐条对账（gate 日志原文在，exit 相符）：`c63f463b` 基线 exit 0／`8702267c` 靶向 exit 0／`4c4d79d3` 电池 exit 0／`fa9a812d` exit 0／`6212b786` exit 1（废跑，已披露）／`673a6bc0` exit 0／`8b8d415d` exit 0／`90aad0bc` exit 0。本席两行 `bd260ec8`／`b957e463` 均已落 gate 日志（START＋RUN 双行，exit 0）。
- 真库零写入：`D:\2Study\StudyNotes\.db\calorie_data.db` 本席跑前＝跑后 `LEN=3072000 SHA256=D40D58F50E74E294A11E410B4D9E2142FCBCD8993328A4EFB453F1A0D62A1083`，与一审开工＝完工值逐字一致；新用例走 `mkdtemp` 自建库（`SKILLS_DB_PATH` 指过去），真库只读对账。
- 变异后复原：本席跑后 `git diff --stat -- <测试件>` 为空（N2 `finally` 写回＋sha 校验＋`RESTORE-IDENTICAL=true`）。

## 段3｜逐条路径（S1 重点查：放宽／造假／越界／丢数据＋等待量／对账）

- 改动 `packages/skill-calorie/test/t361-measure-full-table.test.mjs`（R1 备注 `r1`→`r1—d`＋载荷全文→13 字段循环＋`r1.note` 原样保留＋合成直调→真出口新用例，8→8；`numstat 81/13` 已验；diff 存 `.scratch/t443/review2-diff.txt`）：旧合成用例被同口径真出口用例替换（渲染口径仍认老 `:397`，落点换真出口），属收紧非放宽；M1（删列）锚点仍活（13 名断言未动），M2 由一审 N1 补证红绿。
- 证据 `docs/skills/skill-calorie/t441-判据收紧证据.md`＋记账 `.changeset/t443-判据收紧.md`（`'skill-calorie': patch` 单行，内容已读无误；pwsh 下的乱码仅控制台解码，文件 UTF-8 正常）；复核 `docs/skills/skill-calorie/t443-复核.md`（本次唯一入仓的前手件）。
- S1 排查（均未见）：放宽——逐字段循环＋备注保留比基线多不断少（基线从不断备注）；`undefined` 容忍与基线等价（基线全文口径同样抓不到缺键），见段 4 ②；造假——全空行 13×—／13 名／KPI 均有老原文或手算来源，非新输出当期望；越界——443 自有三提交写集干净（上见段 2），无唤醒词／命令／示例改动，无 `src/**` 行为改动；丢数据——真库哈希三方一致，测试件还原一致。
- 对账：#440 已 CLOSED，测试零命中 `肩宽`、全 `肩围`（排 440 之后遵守）；#361 原证反向指针确实未加（`t361-实施证据.md` 内零命中 `443`／`441`），一审作未做项转交属实非漏报；`6212b786` 废跑归因可信（窗内邻接 `253F2` 提交行与 #489 锚点清理提交 `ea0a7e0`，`RESTORE-IDENTICAL=true` 后随即回绿 `673a6bc0`＋`8b8d415d`），一审未将其充作门禁证据且重跑回绿，处置正确；草稿仅 `.scratch/t443/review2-*`（已忽略，不进仓）。

## 段4｜返工意见（不阻断 PASS；未 push，由编排者统一收口）

- ①【措辞订正·证据分扣 1 的唯一扣分】一审 §2“`git diff 5d9a6ba^ HEAD -- src/ --stat` 为空”按字面在 `bad8492` 时点不成立（该区间含他票 `src` 改动：`7984924 today.ts`、`ea0a7e0 body.ts`，本席已验）。实体无损（443 自有提交逐件干净，见段 2）。请编排者知悉即可，或改一处措辞为“443 自有提交（`5d9a6ba`／`e7f7e7d`）`-- src/` 为空”；不另开票，不推翻判定。
- ②【范围外 S3· hardening 建议，不属 S1】载荷逐字段循环放行 `undefined`（`v === null || v === undefined || typeof v === 'number'`）。与本票基线等价（旧全文口径同样抓不到缺键），不构成放宽交付。但后票若愿收紧，可把该行改为仅 `null`／`number` 并配“删键必红”变异；本票不做（动即超写集）。
- ③【本票范围·未做，沿用一审转交】#361 原证反向指针未加（写集不含他人件，跨界停手正确）。请编排者在 `t361-实施证据.md` 末追加指本件／`t441` 证据的指针，或具名授权补加。
- ④【遗留出口原样转交】触发器是否挂 `BEFORE UPDATE`、中文名归 #440（已关），均按票面不入本票。
