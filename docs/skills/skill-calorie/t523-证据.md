# #523 证据：场景 04 运动 · 汇总／明细／对照目标 17 页整改

> 票面：[#523](https://github.com/FeatherHunter/ilife/issues/523)。本席读的票面副本是 `.scratch/t523/issue-523.md`
> ——**票面 body 在 GitHub 上是花码**（标题正常、正文是「UTF-8 字节被当 GBK 读」再存进去的），编排者已在本票窗口内重写正确，**引用以重写后的票面为准**。
> 判据出处：#508（分隔符＝设计债的探针口径）、#516（五维尺与权重）、#522（改前基线）、#401／#504（同型先例）。
> 设计件：`.scratch/t523/设计.md`。

## 〇、跑法

会改工作区／生成物的命令一律经加锁包装器：

```
node tooling/run-locked.mjs --ticket 523 -- <命令>
```

## 一、GATE-RUN

| # | 命令 | runId | exit | 摘要行 |
|---|---|---|---|---|
| 1 | `pnpm --filter skill-calorie run build` | `6340f0fd-1dd2-4380-af48-0dee56743955` | **0** | `tsc -b` 通过＋`HELP 已注入` |
| 2 | `node .scratch/t523/verify.mjs` ①（重出 39 条） | `28d1fc6d-e075-4a9a-a35c-a3e40ed2574a` | **0** | `乙：39 条 OK=39 BLOCKED=0`；`MINE17 n=17 exit0=17 complete=17 BAD=0` |
| 3 | 同 ②（`measure-responsive.mjs --dir …/t523/out --json …/resp-after.json`） | 同上 | **0** | `OVERFLOW-ZERO pages=39 cells=117 failed=0 scopeOutFailed=0` |
| 4 | 同 ③（`scan17.mjs`，分隔符＋文案＋脏数＋重复事实） | 同上 | **0** | `SEP 节点级命中 0 的页=17/17 行级 0 的页=17/17 exit0 的页=17/17`；`文案 机器词页=0 脏数页=0` |
| 5 | 同 ④（回归 ＋ 本票新守卫） | 同上 | **0** | `pass 30  fail 0` |
| 6 | `check-warning-line.mjs` | 同上 | 1→**0** | 首跑红＝台账陈化（见 §九），`--sync` 后回绿 |
| 7 | `dsh --profile visionreview`（视觉席，见 §七） | `<!-- 视觉 runId -->` | — | 待补 |

> **返修轮（R2，打回三项靶向修，commit `97248be`＋台账同步 `0901f1b`）**：`verify.mjs` 重跑全绿——
> ① 重出 `乙：39 条 OK=39 BLOCKED=0`；`MINE17 n=17 exit0=17 complete=17 BAD=0`；
> ② `OVERFLOW-ZERO pages=39 cells=117 failed=0 scopeOutFailed=0`；
> ③ `scan17`：17/17 页节点级 0、行级 0、exit 0，机器词页＝复制菜单档位（`A1:JSON`，#508 豁免），脏数页 0；
> ④ 回归 `pass 30 fail 0`（含本轮改动的 451／452／267／523 四件）；
> ⑤ 台账 `--sync` 后 `RESULT: 82/82 PASS`。读数落 `.scratch/t523/read17.json`／`resp-after.json`／`verify.log`。

> **build 曾连红 12 轮**：`tsc -b` 是全包一次编译，本包同时有多个席在飞（diet／photo／weight／home／planEditor 各票），
> 别人的中间态（`src/render/planEditor.ts` 注释里的孤立代理项、`src/diet/nutritionPortDocs.ts` 挤在一行的块注释、
> `src/shared/pageKit.ts` 引用了还不存在的导出）把我一起拦下。处置照协议：**报编排者、不碰别人的件、挂重试循环**，
> 第 13 轮拿到绿。原始报错与 `git status` 见 `.scratch/t523/build-full.log` / `build2.log` / `build3.log`。

## 二、改前 → 改后（三族）

| 族 | 页数 | 改前 `·` | 改前 `；` | 改后节点级命中 | 改后行级命中 |
|---|---|---|---|---|---|
| 运动汇总 | 10 | 80 | 30 | **0** | **0** |
| 记录级明细 | 5 | 20 | 25 | **0** | **0** |
| 对照目标 | 2 | 14 | 6 | **0** | **0** |
| 合计 | 17 | 114 | 61 | **0** | **0** |

改前读数出处：`.scratch/t522/readings-baseline.json`（编排者按族汇总：199／81／33）。

## 三、逐页分隔符读数（`.scratch/sep-audit/probe.mjs --json`，口径见 #508）

17/17 页 `节点级 = 0（·0 ；0 多段0）`、`行级 = 0`、`exit = 0`。逐页原样读数落
`.scratch/t523/read17.json`（`rows[].sep`）与 `.scratch/t523/scan17.log`。

```
14-看今日运动            sep(node=0 ·0 ；0 多0)   23-看今日运动（vs 目标）   sep(node=0 ·0 ；0 多0)
15-看昨日运动            sep(node=0 ·0 ；0 多0)   24-看本周运动（vs 目标）   sep(node=0 ·0 ；0 多0)
16-看本周运动            sep(node=0 ·0 ；0 多0)   25-看运动记录（有备注）    sep(node=0 ·0 ；0 多0)
17-看上周运动            sep(node=0 ·0 ；0 多0)   26-看运动记录（按力量筛选） sep(node=0 ·0 ；0 多0)
18-看本月运动            sep(node=0 ·0 ；0 多0)   27-看运动记录（按有氧筛选） sep(node=0 ·0 ；0 多0)
19-看上月运动            sep(node=0 ·0 ；0 多0)   28-看最近 60 天运动        sep(node=0 ·0 ；0 多0)
20-看最近 7 天运动        sep(node=0 ·0 ；0 多0)   29-看最近 180 天运动       sep(node=0 ·0 ；0 多0)
21-看最近 30 天运动       sep(node=0 ·0 ；0 多0)   30-看最近 365 天运动       sep(node=0 ·0 ；0 多0)
22-看某段时间运动         sep(node=0 ·0 ；0 多0)
```

## 四、三档溢出（390／768／1440）

`OVERFLOW-ZERO pages=39 cells=117 failed=0 scopeOutFailed=0`（本票 17 页逐格 0，逐页读数落
`.scratch/t523/resp-after.json`）。每页 `<img>` 数 0（本族页面无位图）。

## 五、文案读数

- **机器词**：17 页可见文本里 `exercise_log`／`daily_goal`／`total_changes`／`CALORIE_TODAY`／`calorie.view.` **均为 0**
  （`visible-text-probe.mjs` 口径；`scan17.mjs` 的 `文案 机器词页=0`）。
  改前：明细 5 页与汇总页各印库表名 `exercise_log`。
- **≥6 位脏数**：`dirty 页=0`（改前 22／28／29／30 四页印出 `153.60000000000002`）。
- **复制菜单里的 `JSON`／`CSV`**：`visible-text-probe` 的 `A1 全大写常量` 规则每页命中 1 处，落点是
  **复制菜单的三种格式档位**（`纯文本／JSON／CSV`），由公共层 `renderCopyBlock` 产出。判据口径里它属机器面
  （#508 豁免清单），本票不改共用件——见 §八 遗留。

## 五之二、重复事实逐条理由（票面口径：**0 或逐条给理由**）

> **R2 改后状态**（`scan17.mjs` 同一 `FACT_RE` 口径，`read17.json` 实测）：
>
> | 页 | 残留最高 | 已删掉的 |
> |---|---|---|
> | 汇总 10 页 | `<窗口天数>天 x3–x4`（窗口条胶囊／表标题「本窗共 N 天」／口径行「折线按 N 天画」；短窗第 4 处是次数卡「活跃 N 天」，同值不同义） | caption 与截断明示的复读已拆成一句一事；`sui-note` 不再带数 |
> | 明细 5 页 | `<条数>条 x4`（表标题共 N 条＋本页显示／KPI 记录数／来源脚注） | 副标题「本页显示」已删；记录数卡小字「活跃」已删；截断句「每页最多」已删；眉标与 H1 不再同名 |
> | 对照目标 2 页 | `416.7% x2`（环心＋完成度卡） | 「已达成目标」×3→×1（只留判决胶囊）；环下键值行已撤（目标／实际／差额只在四格）；「差距／差额」两名收成一名 |
> | 25／26／27 | `30 分钟／5km／320 卡 x4–8` | 明细数据同值多行（数据本身，非页级复写） |

R1 的逐类理由继续有效，R2 删改后更新如下（同一类只写一次）：

| 类别 | 读数 | 逐条理由 |
|---|---|---|
| 汇总 10 页：`<窗口天数>天 x3–x4` | 16–22、28–30 各 3–4 次 | 三个角色各不相同，且其中两条是 **#452 冻结断言**：① 窗口条胶囊「201 天」（窗口多长，一眼）；② 折线口径「每日消耗折线按 201 天画」（`452:275` 钉死，删不得）；③ 逐日表表标题「本窗共 201 天」（`452:315/337` 钉死「截断明示没写页眉条数口径」即红）。同一数值、三种判断，不是同事实复写。R2 之前还有第 4 处（截断明示复读两数），已删。短窗页（如 16）的第 4 处是次数卡小字「活跃 2 天」——与窗口天数**同值不同义**（种子数据天天有记录故撞数；22 页 201 天窗 vs 活跃 46 天可证两者分开算），删它等于删事实，不删。 |
| 明细 5 页：`<条数>条 x4` | 14/15/25/26 各 4 次 | ① 表标题「共 N 条」＋「本页显示 M 条」（#451 冻结断言）；② KPI「记录数」卡的值（#451 钉死「指标卡的记录数是本窗总数 52」）；③ 来源脚注「记录数 共 N 条」（#452 同源判据）。三处各有判据钉死，角色分别是表范围／指标／来源脚注。R2 已删：副标题「本页显示」（与 caption 重）、记录数卡小字「活跃」（与独立卡重）、截断句「每页最多」（与表标题重）、眉标与 H1 同名（眉标退回族名）。 |
| 明细 5 页：`30 分钟 x4–8`、`5 km x8`、`320 卡 x4` | 25/26/27 | **逐条记录表里多行恰好同值**（一次跑步 30 分钟／5 km 各记了若干条）。这是明细数据本身，不是页级复写。 |
| 对照目标 2 页：`300 卡 x2`（仅 23） | 口径行「每日目标 300 卡」＋数值卡「目标 300 卡」 | 口径行那句是 `452:369` 钉死的取数口径说明（删即红），卡是读数。一处说口径、一处给读数，角色不同。R2 实测：实际／差额已各剩 1 处（只在四格），此前环下键值行那一份已撤。 |
| 对照目标 2 页：`300 卡 x2`（23） | 口径行「每日目标 300 卡」＋数值卡「目标 300 卡」 | 口径行那句是 `452:369` 钉死的取数口径说明（删即红），卡是读数。一处说口径、一处给读数，角色不同。 |
| 对照目标 2 页：`416.7%` | 23/24 | 环心与「完成度」卡：环心是主角读数，格里的完成度是四项对照之一，且 `452:437` 要求超额实际百分比**必须由文字承载**（环只画到 100%）。R2 已删：环下键值行（目标／实际／差距与四格重复）、顶栏状态徽标与环心判语（「已达成目标」×3→×1，只留判决胶囊）。 |

**已消掉的重复**（改前 → 改后）：窗口区间（KPI `detail` ＋ H1 ＋ 表标题 → 只留 H1＋页脚键值行）、
窗口天数（副标题 ＋ 折线口径 ＋ 表标题 ＋ 截断明示 5 处 → 3 处）、总消耗（KPI ＋ 日均卡 `detail` → 1 处）、
活跃天数（次数卡 ＋ 日均卡 → 1 处）、「共 N 条」（副标题 ＋ 表标题 ＋ 来源脚注 → 表标题＋来源脚注）、
截断明示逐字复读表标题（→ 一句只报「已截断」这件事）。

## 六、新判据的变异两行（改坏必红／还原必绿）

`exercise-text-shape-523.test.mjs`（本票新件，7 条用例）原样读数：

```
T523 SEP-TOTAL 页面=8 有债页=0 命中合计=0
T523-MUT 塞`·`=1 处命中 塞`；`=1 处命中 还原=0 处命中
```

- **改坏必红**：把 KPI 的 `detail` 塞回 `活跃 46 天 · 有数 46 天` ⇒ 守卫红（1 处命中）。
- **改坏必红（第二处）**：把口径行塞回 `消耗＝…；时长＝…；次数＝…` ⇒ 守卫红（1 处命中）。
- **还原必绿**：原样产物 ⇒ 0 处命中。

另两条读数（同一件）：`T523 DUP 201天x1 32123.4卡x1`（装配层夹具下窗口天数与总消耗各只出现一次）、
`T523 HEAD title=卡路里 运动 eyebrow=运动汇总`。

## 七、三件回归测试的口径改动清单（编排者授权的最小改动）

| 件 | 改动处 | 改前 | 改后 | 理由 |
|---|---|---|---|---|
| `exercise-records-fusion-451.test.mjs` | `EYEBROW` 常量 | `运动 · 记录级明细` | `运动记录明细` | 眉标 `·` 是分隔符债，节点级必须 0；类别＋页族两个字都在 |
| 同上 | 来源脚注断言 | `includes('数据来源 · ')` | `includes('数据来源')` | 来源行改键值行（#470 口径） |
| 同上 | 页眉总数断言 | 读 `subtitle` | 改读 `caption` | 总数那份事实只留表标题一处（去重复事实） |
| 同上 | 空态来源脚注 | `includes('数据来源 · ')` | `includes('数据来源')` | 同上 |
| `exercise-summary-goal-fusion-452.test.mjs` | `<title>` 断言 | `卡路里·运动身体` | `卡路里 运动` | 品牌 `·` 同判债（照 #401 样板先例） |
| 同上 | `sourceFootnote()` | 读 `数据来源 · …共 N 条` | 读键值行 `记录数` 那一格 | 同上 |
| 同上 | 眉标断言 ×2 | `运动 · 汇总`／`运动 · 对照目标` | `运动汇总`／`运动对照目标` | 同上 |
| 同上 | 差距文案正则 | `差距( 超出 \d|…)` | `超出 \d+ 卡|差 \d+ 卡` | 差距从 `·` 串改键值行；事实未丢 |
| `exercise-accept-267.test.mjs` | `EYEBROW_OF_KEY` 三条（汇总／对照目标／记录级明细） | `运动 · …` | `运动…` | 同上；**#524／#525 两族的眉标未动** |

**判据结构、覆盖面、用例数未减**：九处全是「同一份事实换了个落点」的取数位置调整，没有删掉任何一条断言；
两条对同一事实的断言（页眉＋表标题）改后仍各有一条。四件合跑 `pass 30  fail 0`。

**R2 追加的口径改动**（commit `97248be`，同为「事实换落点」，用例数未减）：`451:52` 眉标常量→`运动记录`、
`451:163` 副标题改判内容句＋禁复读数、`267:87` 页族针对同值、`452:365` 差距文案改读差额卡
（环下键值行已撤）。`452:265`（类型消耗分布块）与 `452:263`（chartBlock 容器，折线仍在）无需改动。

## 八、未采纳建议与遗留

### 0. 视觉复评结果（详见 `.scratch/t523/视觉复评.md`，截图落 `D:\ilife\.dsh-vision-router\artifacts\`）

**三族平均 75.2，未达 90 线**（汇总 74.5／明细 73.5／对照目标 77.5；6 页算术平均 75.17）。

| 页 | 结构纪律(20) | 信息层级(25) | 分隔符与文字纪律(20) | 跨宽自适应(15) | 手机端同档(20) | 总分 |
|---|---|---|---|---|---|---|
| 16-看本周运动 | 16 | 17 | 19 | 11 | 15 | **78** |
| 22-看某段时间运动 | 15 | 15 | 17 | 10 | 14 | **71** |
| 25-看运动记录（有备注） | 17 | 18 | 15 | 11 | 13 | **74** |
| 27-看运动记录（按有氧筛选） | 17 | 17 | 15 | 11 | 13 | **73** |
| 23-看今日运动（vs 目标） | 17 | 18 | 14 | 13 | 16 | **78** |
| 24-看本周运动（vs 目标） | 17 | 17 | 14 | 13 | 16 | **77** |

**打回项（视觉席原话归纳，三处硬伤）**：
1. **≤640 断点下表格 caption 被压成一字一行**（第 3 页手机档用原生分辨率裁切实测；第 2、4 页整页识图报同一现象）；
2. **长分类名在柱状图 x 轴上重叠越界**（第 2 页桌面／手机都糊成一片）；
3. **同一事实一页写两到三遍**（`已达成目标`×3；环区「目标／实际／差额」与数值卡各一遍；「本页显示 N 条」副标题＋caption 各一遍）。

**纪律面六页全干净**（视觉席原话）：无 `·`／`；`／`｜` 顶替设计的并列串、无英文内部标识符上屏、缺值 `—` 统一、数值走等宽数字。
⇒ 机器判据（分隔符／文案／脏数）与视觉席的「分隔符与文字纪律」维**互相印证**；未达标的分全部丢在
断点细节、图表刻度与「一页两遍」上。**本票不静默关票**：上列三条是明确的下一手靶子。

### 0.2 返修轮（R2）改法 → 打回项逐条对照（commit `97248be`）

| 打回项 | 改法（本票范围内） | 落点 |
|---|---|---|
| 1. ≤640 档 caption 竖排 | 公共层 #541 已落地（`blocks.ts:1697-1704` 表注 `display:block;width:100%`，本票不碰）；本票持锁 rebuild 后重出 17 页 | 结构复核见本节下；视觉复验交 R2 视觉席（brief §二第 1 条点名 25／27／22 三页 390 档） |
| 2. 柱状图 x 轴长名重叠越界 | 柱状图不再画，改走公共层 `renderDistributionRows`（名称 ｜ 条 ｜ 数值，全称照印，窄屏自然换行；只列消耗最高 8 类＋一句去向注） | `sportDocs.ts::summaryCards`（`sec-dist` 卡 id／标签不变，`452:265` 判据仍认） |
| 3a. 「已达成目标」×3 | 顶栏徽标与环心判语两处已撤，只留判决胶囊一处 | `buildExerciseGoalDoc`（capsStrip 行删除）、`ringCard`（ring-note 删除） |
| 3b. 环区与数值卡各一遍＋「差距／差额」两名一数 | 环下键值行已撤（目标／实际／差距只在数值四格一处），名称收成「差额」一名 | `ringCard`（factStrip 删除）、`452:365` 判据改读差额卡 |
| 3c. 「本页显示 N 条」副标题＋caption 各一遍 | 副标题改内容句（不再复读数），只留表标题一处 | `records.ts::subtitleOf`、`451:163` 判据同步 |
| 3d. 眉标与 H1 逐字同名 | 眉标退回族名「运动记录」，H1 留页名 | `records.ts::EYEBROW`、`451:52`／`267:87` 同步 |
| 3e. 「活跃 N 天」小字＋独立卡各一遍 | 记录数卡 `detail` 已撤，只留独立卡一处 | `records.ts::figureCard` |
| 3f. caption 与截断明示各说一遍截断 | 拆成一句一事（表标题只报本窗天数，可见行数与被截天数只住截断明示一句） | `sportDocs.ts::truncation` |
| 3g. 截断句「每页最多」与表标题重复 | 截断句不再复读上限，只留表标题一处（超上限才出的那句仍在，`451:306` 仍认） | `records.ts::truncationText` |
| 附加：间距 14／6／18 不落 4／8 网格 | 本件自造形状的间距全数对齐 4／8（14→16、6→8、3→4 等；字号 12／13／15 不动） | `sportUi.ts::exerciseUiCss` |

**删不掉、写理由的三处**（见 §五之二）：窗口天数 x3（两处 #452 冻结＋窗口条）、条数 x4（三处各有判据钉死）、
416.7% x2（`452:437` 文字承载）——另 300 卡 x2（口径行钉死＋读数卡）与短窗活跃天数撞数（同值不同义）亦各有理由。

**打回项 1 的结构复核（本票内可验的部分）**：① 公共层规则在位（`blocks.ts:1691-1704`，
`@media (max-width:640px)` 内表 `display:block`＋表注 `display:block;width:100%`，注释点名竖排病根）；
② 重出产物 25／27／22 三页 caption 仍是整句原文（未被改写截断），如
`运动记录明细（共 4 条，本页显示 4 条，每页最多 50 条）`、`按日消耗（本窗共 201 天）`；
③ 三档溢出 `OVERFLOW-ZERO pages=39 cells=117 failed=0`（`resp-after.json`）——竖排的机制性成因已除，
肉眼终验交 R2 视觉席 390 档截图。

### 1. 未采纳的视觉席建议（判断着采纳）
<!-- 待补：视觉席「各族最该改的一处」与「待裁」两节里，本轮判定不采纳的条目 -->

### 2. 共用层遗留（本票不修，回写本票）

| 遗留 | 事实 | 处置 |
|---|---|---|
| `src/shared/sourceLine.ts` 的 `·` 串 | 该件产出 `数据来源 · <来源> · 起 → 止 · 共 N 条`，一个文本节点里 3 个 `·`；**全仓读页／回执页共用**，任一族要 `节点级 0` 都绕不开它 | 本族 17 页改走 `sportUi.factStrip()` 键值行；**共用层口径统一归 #470**（本票吸收其一角） |
| 复制菜单的 `JSON`／`CSV` 档位名 | 公共层 `renderCopyBlock` 的固定档位，`visible-text-probe` 判为「全大写常量」 | 判据口径里属机器面（#508 豁免）；如要改口径请由公共层票裁 |
| 版面根无安全区 | `packages/base-render/src/style.ts` 里 `env(safe-area-inset-*)` **只用在 toast 栈**（`:106`），页面壳 `.ilife-block-page-shell` 同 `.ilife-help-shell` 都是 `padding:32px 20px 80px`，两者**逐值相同**、都没有安全区处理 | 属公共层移动端底座（#507）；本族与 HELP **同档**，故不算本票缺口 |

## 九、超线台账

`src/render/sportDocs.ts`：改前 **351 LF**（已超线）、R1 改后 **417 LF**、R2 返修后 **423 LF**（`--sync` 已同步，
`RESULT: 82/82 PASS`）。按台账规矩跑 `check-warning-line.mjs --sync` 同步（该脚本是全包一次对账，
同时也会把别的票的陈化行照实况改掉——R2 轮带了 `sportPortDocs 666→757` 等四行他席陈化，commit `0901f1b`）。
拆法与理由见 `packages/skill-calorie/AGENTS.md` 台账表该行（R2 只动数字，结论行未改，拆法照旧）。

---

# 十、R3 返修轮（视觉复评 R2 的两处硬伤）

> 判分依据：`.scratch/t523/视觉复评R2.md`（三族平均 **82.0**，达标线 90）。
> 靶心两处（R2 逐条可回源码）：① 记录级明细两张表 8 列**全部退成 `cell-left`**；② 汇总页分布条类名在
> **三档宽度下都被悄悄截断**（`跪姿健身轮…`／`对握式器械…`／`把手式蝴蝶…`）。
> 本轮同时收掉 R2 在文字纪律上点的两条（眉标与 H1 逐字同名、单日窗 H1 同一天写两遍）。

## 十·一 写集声明（本轮实际写过的路径，逐条）

| # | 路径 | 性质 | 依据 |
|---|---|---|---|
| 1 | `packages/skill-calorie/src/exercise/records.ts` | 改（数值四列挂 `align:'right'`＋注释） | 票面「生产代码只许落在」三件之一 |
| 2 | `packages/skill-calorie/src/exercise/sportUi.ts` | 改（分布条类名轨页级覆盖 ＋ 820 两段式） | 同上；票面裁定 ③「样式件落 `sportUi.ts`」 |
| 3 | `packages/skill-calorie/src/render/sportDocs.ts` | 改（汇总族眉标退回类别词；单日窗 H1 只印一次日期） | 同上 |
| 4 | `packages/skill-calorie/test/exercise-text-shape-523.test.mjs` | 改（新加 ⑤ 列对齐／⑥ 类名不截断 两条守卫 ＋ 变异用例） | 票面交付物路径点名的本票测试件 |
| 5 | `packages/skill-calorie/test/exercise-accept-267.test.mjs` | 改（眉标钉死串 1 行 ＋ G4 空库夹具 1 处） | 票面裁定 ①「允许最小改动三件本场景测试的钉死字符串」 |
| 6 | `packages/skill-calorie/test/exercise-summary-goal-fusion-452.test.mjs` | 改（眉标钉死串 1 行） | 同上 |
| 7 | `packages/skill-calorie/AGENTS.md` | 改（告警线台账：`sportDocs` 417→422、`records` 新挂号 356） | 包规「改了超线件必须同步台账」 |
| 8 | `docs/skills/skill-calorie/t523-截断探针.mjs` | 新建（真浏览器截断／列对齐探针，可复跑） | 协议 §5.1「可复跑脚本随证据一起入仓」 |
| 9 | `docs/skills/skill-calorie/t523-gate-runs.log` | 新建（本窗口门禁运行记录导出＝对账源） | 协议 §2.3 第 4 条 |
| 10 | `docs/skills/skill-calorie/t523-证据.md` | 改（本节） | 票面交付物 |
| 11 | `.scratch/t523/run17.mjs` | 改（重出脚本补 `"<记录号>"` 占位符填法） | 本票草稿区；见十·五 范围外发现 1 |

**别人的件一行没碰**：`packages/base-render/**`（分布条 `6em` 的病根在公共层——**未改**，转公共层票，见十·五 2）、
`src/shared/**`、`src/render/sportPortDocs.ts`（#525）、`src/render/receipt.ts`（#524）、`src/render/trendDocs.ts`（#518）。

## 十·二 两处硬伤的改法与前后机器读数

### 硬伤① 记录级明细表的数值列（原：8 列全 `cell-left`）

改法：`records.ts` 加一个 `NUMERIC` 列集（时长／消耗／距离／心率），建表时给这四列挂公共层**既有档位**
`align:'right'`——`renderDataTable` 里「这一列是数值」的唯一信号就是它，右对齐 ＋ 等宽栈 ＋ `tabular-nums`
＋ 正文字色（公共层 #507「数值列主次」那条规则）全挂在 `cell-right` 这个类名上，**不新造类名、不改公共层**。

| 读数 | 改前（`out/` 旧产物，`trunc-before.json`） | 改后（`e7621566` 重出） |
|---|---|---|
| 第 3 页 `25-看运动记录（有备注）` | `td-left=32 td-right=0`（8 列 ×4 行全左） | 表头档 `时长/消耗/距离/心率=right`、`日期/类型/分类/备注=left`；数值格 8／文字格 8 |
| 第 4 页 `27-看运动记录（按有氧筛选）` | `td-left=96 td-right=0`（8 列 ×12 行全左） | 数值格 48／文字格 48 |
| 探针 T3（渲染面计算样式，1440 档） | 5 页红（14／15／25／26／27），单页最多 48 格 | **本票 5 页 0 格**——`align:right` ＋ `font-family` 含 `mono` 逐格成立 |

> 第 1 页 `14-看今日运动`／`15-看昨日运动` 同属记录级明细族（同 `buildRecordsDoc`），一并从红转绿。

### 硬伤② 分布条类名被截断（原：`6em` ＋ `nowrap` ＋ 省略号）

改法（**页级覆盖，不碰公共层**）：本族页样式段（`sportUi.ts::exerciseUiCss`）里把类名轨从 `minmax(0,6em)`
放到 `minmax(0,9.5em)`（容得下「把手式蝴蝶机飞鸟」这类 8 字全称），并把 `overflow/text-overflow/white-space`
三条放开（`visible`／`clip`／`normal`＋`overflow-wrap:anywhere`）；窄屏（本族断点 820）再塌成两段式：
第一行「类名」（独占整行、可换行），第二行「条 ＋ 数值」。**轨宽仍是定宽**，八根条的起点继续对齐。

| 读数（真浏览器，`scrollWidth > clientWidth` 且 `overflow-x:hidden|clip`） | 改前 | 改后 |
|---|---|---|
| 第 2 页 `22-看某段时间运动` @390／768／1440 | 各 **3 处**（`跪姿健腹轮前推 cw=78 sw=91`、`对握式器械推胸 cw=78 sw=91`、`把手式蝴蝶机飞鸟 cw=78 sw=104`） | **0 处** |
| 第 28／29／30 页（同族长窗） | 各 1／3／3 处 | **0 处** |
| 探针总读数 | `TRUNC-SCAN failed=13`（本票 17 页，390／1440 两档） | `TRUNC-SCAN pages=39 cells=117 failed=0 scopeOutFailed=5 RESULT: 112/112（本票 17 页）` |

> **注意**：截断**不是只在 390 发生**——改前三档（390／768／1440）逐档 3 处、`cw/sw` 逐值相同。
> R2 报告写「桌面档 8 个类名互不重叠、无越界、全名可读」，与真浏览器读数不符（R2 的桌面结论按整页缩略图
> 判断）。本轮以 `scrollWidth/clientWidth` 的机器读数为准，并在 R3 brief 里点名要复验三个长名的逐字可读。

### 附带两条（R2 文字纪律扣分项）

- **汇总族眉标与 H1 逐字同名**（R2 两页各 −5）：`#552` 把 H1 的日期摘掉后，眉标 `运动汇总` 与 H1 `运动汇总`
  撞成同一串。现眉标退回**类别词**「运动」，页族「汇总」由 H1 承载——两件事实都还在页头，只是不再复读页名
  （与明细族「眉标＝运动记录／H1＝运动记录明细」同构）。钉死串按票面裁定 ① 同步改口径，判据结构未减。
- **单日窗 H1 把同一天写两遍**（R2 第 5 页 −3）：`运动目标 2026-09-15 ~ 2026-09-15` → 起止同日只印一次。

## 十·三 GATE-RUN（本窗口逐条；对账源＝`docs/skills/skill-calorie/t523-gate-runs.log`）

| # | 命令 | runId | exit | 摘要行 |
|---|---|---|---|---|
| 1 | `node docs/skills/skill-calorie/t523-截断探针.mjs --dir .scratch/t523/out --widths 390,1440 --json …/trunc-before.json` | `8a9a471d-8caa-4ff5-96ff-4c4d7fd2d42b` | **1** | 改前红：`failed=13 scopeOutFailed=5`（本票 17 页） |
| 2 | `pnpm --filter skill-calorie run build` | `4d1203ae-d6b0-4552-8a72-07b11fd61018` | **0** | `tsc -b` 通过 ＋ `HELP 已注入` |
| 3 | `node .scratch/t523/verify.mjs`（首次） | `fc9487a0-0e23-41b2-b2be-fbc3af001fe8` | 0（内步 ④⑤=1） | ①②③ 绿；④ 红＝`exercise-accept-267` G4（范围外，见十·五 1）；⑤ 红＝台账陈化／漏报 |
| 4 | `…check-warning-line.mjs --sync --dry; … --sync` | `a2d855f4-cc9a-43dc-b2fe-14ff91f50506` | **0** | `SYNC-PLAN 改=1 增=1 删=0`；`SYNC-VERIFY ok`；`RESULT: 87/87` |
| 5 | `node .scratch/t523/verify.mjs`（末次，全绿） | `e7621566-cbbe-40e7-9b3f-8db8f37849db` | **0** | ①`乙：39 条 OK=39 BLOCKED=0`、`MINE17 n=17 exit0=17 complete=17 BAD=0`；②`OVERFLOW-ZERO pages=39 cells=117 failed=0 scopeOutFailed=0`；③`节点级命中 0 的页=17/17 行级 0 的页=17/17 exit0 的页=17/17`、`文案 机器词页=0 脏数页=0`；④`pass 33 fail 0`；⑤`RESULT: 87/87 PASS` |
| 6 | `node docs/skills/skill-calorie/t523-截断探针.mjs … --json …/trunc-after.json` | `f84742f2-a13f-42fc-bc9b-ec113654205c` | **0** | `TRUNC-SCAN pages=39 cells=117 failed=0 scopeOutFailed=5 RESULT: 112/112（本票 17 页）` |
| 7 | `node .scratch/t523/mut-r3.mjs`（变异自证，见十·四） | `ddc63b4e-ed8a-4b3f-b6f1-948bfdb1948a` | **0** | `MUT-R3 PASS 两步变异各必红＋还原必绿` |

**中间那次 `fd8a1cbf` 也在对账源里**（exit=0，但当时 `乙：39 条 OK=37 BLOCKED=2`）：`09-改运动记录`／
`11-删运动记录` 两条被 `#478` 的 `"<记录号>"` 占位符挡住、产物不出。修法落在本票草稿脚本
`run17.mjs`（补占位符填法），末次运行恢复 `OK=39 BLOCKED=0`。

## 十·四 变异自证两行（源码级，一次窗口内做完并复原）

```
M1 源码=records.ts `NUMERIC.has(label)`→`false`            build=0 测试 exit=1 red=true fail=2
   T523 COL 记录级明细（带筛选） 数据行=2 数值格=0（期望 8）文字格=16（期望 8）
M2 源码=sportUi.ts 类名轨两条规则→公共层原样              build=0 测试 exit=1 red=true 探针 exit=1
   TRUNC-SCAN pages=4 cells=8 failed=4 RESULT: 4/8 ｜ 截断@1440 `把手式蝴蝶机飞鸟 cw=78 sw=104` 等
RESTORE build=0 测试 exit=0 pass=8 fail=0 ｜ TRUNC-SCAN pages=4 cells=8 failed=0 RESULT: 8/8
```

- **改坏必红（两处各一次）**：① 数值列的 `NUMERIC.has(label)` 改成 `false` ⇒ 本票守卫件 ⑤ 报 `数值格=0（期望 8）`、
  `exit=1 / fail=2`；② 分布条类名轨两条规则退回公共层原样 ⇒ 守卫 ⑥ 红 **且**真浏览器探针 `failed=4`（4 页各量到截断）。
- **还原必绿**：`finally` 里逐文件还原源码 → 重编 → 重出产物 ⇒ `pass=8 fail=0` ＋ 探针 `failed=0 RESULT: 8/8`。
- 脚本 `.scratch/t523/mut-r3.mjs`（草稿区）自带的两次失败也如实留在对账源里（见 `t523-gate-runs.log` 末尾三条
  `exit=1`）：一次是 Windows 上 `pnpm` 直接 spawn 拿不到 `.cmd`（build 没跑 ⇒ 假绿），一次是 M2 只退了一条
  规则（轨宽那条仍在 ⇒ 探针假绿）。**两次假绿都被判据自己抓住并补正**，这正是变异要证明的东西。

## 十·五 未做项、范围外发现与归因

### 1. `exercise-accept-267` G4 空库组的红（**本票范围外**，本票只做最小修复）

- **现象**：`#478`（`aac39bf`，09-15 19:22）把冻结表里「改运动记录／删运动记录」两条词的示例参数由常量
  `"id":1` 换成占位符 `"<记录号>"`；`267` 的 G4 在**空库**上量不到可用记录号 ⇒ 占位符原样递进真出口 ⇒
  入口先报 `ERR 2: id 须为正整数`，走不到取数层，而 G4 断言的是 `/取数失败/` ⇒ 红。
- **复现读数**（`.scratch/t523/diag-267g4.mjs`，只读诊断，临时库在系统临时根内）：
  `"<记录号>"` → `exit=2 :: ERR 2: id 须为正整数（可给数字或数字串）`；
  `"999999"` → `exit=4 :: ERR 4: 取数失败：记录 ID 999999 不存在`。
- **归因**：本票**引入为否**——本票三件源码（`records.ts` 读页／`sportDocs.ts` 读页／`sportUi.ts` 样式）
  都不在写命令的路径上；红在本票改动之前就存在（`#478` 落盘后、本票 R3 开工前）。
- **处置（最小修复）**：`267::round()` 里空种子库时把占位符填成一枚**语法合法、库里必然不存在**的记录号
  （`UNREACHABLE_ID = '999999'`）。**判据一行未放宽、覆盖面一行未减**：该条仍判 `取数失败` ＋ 点名缺什么 ＋
  不落盘，只是让请求真的走到取数层（实测回的就是判据要的那句 `ERR 4: 取数失败`）。依据＝票面裁定 ①
  「最小改动三件本场景测试的钉死字符串 ⇒ 判据结构、覆盖面、用例数一个不许减」。
- **建议**：`#478` 的占位符改动波及了**所有**用冻结表参数真跑的旧夹具（本票 `run17.mjs` 也中招），
  请编排者开一张收口票扫一遍其余脚本／用例，别让每张下游票各补一处。

### 2. 分布条类名轨的病根在公共层（**未改，转票**）

公共层 `packages/base-render/src/blocks.ts:1355/1362-1367` 给 `.ilife-block-dist-row` 的是
`grid-template-columns: minmax(0,6em) minmax(0,1fr) auto` ＋ `.…-row-name{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}`
——**全仓每一个 `renderDistributionRows` 调用点（本包 9 处、跨 diet／body／home／analysis 各族）都会丢字**。
本票按「共用层只许读」的票面红线**没有碰它**，只在本族页样式段加页级覆盖。
**建议开公共层票**（§541／§547 之后的同族）：把 6em 固定轨放成内容宽＋`white-space:normal`，并去掉省略号；
本族的页级覆盖同时可以作为该票的回退样板。

### 3. 探针的 5 处范围外红（**别的族的同形债，本票不判**）

`t523-截断探针` 的 T3（数值列右对齐）在 5 页范围外产物上仍红，已标 `[范围外]`、不进本票退出码：

| 页 | 归属 | 现象（真浏览器，1440 档） |
|---|---|---|
| `07-批量补记运动`／`11-删运动记录`／`12-删某日运动`／`13-批量删运动` | #524 回执族 | 表里 `时长／消耗` 两列 `align:left`、`mono:false` |
| `33-看有氧训练总览` | #525 力量·有氧族（`sportPortDocs.ts`） | 同表 24 格 `时长／距离` 左对齐、无等宽 |

修法与本票同型（给数值列挂 `align:'right'`）；本票**不碰**这两个族的件，请编排者转票。

### 4. R2 待裁项里本轮未动的两条

- **间距非 4 倍数**（R2 各页 −3）：`gap:6px`／`padding:14px`／`5px 9px` 这些取值**全部来自公共层**
  `base-render/src/blocks.ts` 生成的 CSS（R2 待裁第 5 条自己也写明了）。本票按红线不改公共层 ⇒ 该扣分项
  本轮**只能留给公共层票**，不在本票写集内。
- **长窗逐日表在手机档无折叠**（R2 第 2 页 −4，15966px 长滑）：折叠＝改第 2 页的表装配，
  而 `#452` 的冻结断言按 `cardOf(file,'sec-series')` 直读表体（`452:268/307/312`），套一层折叠壳会动到
  别的票钉死的取数位置；本轮不改，留给收口票与 `#452` 一并裁。

### 5. 下一手缺什么

- **视觉 R3 的判分**（本章写作时视觉席仍在跑）：`<90` 时逐条打回项写在 §十一，不静默关票。
- **收口票**：39 页 ＋ 链路总览页一起重出对齐（本票只保证 `.scratch/t523/out/` 这 17 页达标）。
- **公共层票**：分布条类名轨（本节 2）与间距网格（本节 4）两处，都属跨件，本票无权改。

---

# 十一、R3 视觉复评结果（`.scratch/t523/视觉复评R3.md`）与打回项

> 跑法：`dsh --profile visionreview "读取 D:\ilife\.scratch\t523\vision-brief-523-r3.md 并严格按其中要求执行"`
> （视觉席不占锁，`VISION-EXIT=0`，报告 23605 字节，12 次截图全成功）。**评的就是本章交付的这批产物**
> （`18a3f75` 之后未再动任何源码；视觉席截图后本席只改了探针脚本自身的 T3 判宽 bug，产物逐字节未变）。

## 十一·一 四条返修逐条复验：全部「已修」✔

| # | 返修项 | 视觉席复验结论 | 收回扣分 |
|---|---|---|---|
| 1 | 明细表数值列挂 `cell-right`（第 3、4 页） | **已修**：`td-right=16/left=16`、`td-right=48/left=48`；截图见数字右贴同列对齐 | 两页各 −7 |
| 2 | 汇总长窗分布条 390 宽截断三类名（第 2 页） | **已修**：8 名全称可读、无省略号、无重叠（三档都量过） | −8 |
| 3 | 眉标与 H1 逐字同名（第 1、2 页） | **已修**：`<p …eyebrow>运动</p><h1 …>运动汇总</h1>` | 两页各 −5 |
| 4 | 单日窗 H1 同日写两遍（第 5 页） | **已修**：H1 `运动目标 2026-09-15`（只一个日期） | −3 |

## 十一·二 分数：三族平均 **87.8，未达标**（上一轮 82.0 → ＋5.8）

| 族 | 页 | 分 | 族均 |
|---|---|---|---|
| 汇总族 | 16-看本周运动 | 87 | **88.0** |
| 汇总族 | 22-看某段时间运动 | 89 | |
| 明细族 | 25-看运动记录（有备注） | 87 | **87.0** |
| 明细族 | 27-看运动记录（按有氧筛选） | 87 | |
| 对照目标族 | 23-看今日运动（vs 目标） | 90 | **88.5** |
| 对照目标族 | 24-看本周运动（vs 目标） | 87 | |

**未达标 ⇒ 本票不关票**（票面：视觉 ≥90 才关）。剩余扣分（视觉席原话归纳）三处跨页共性：
页内导航触摸高度（6 页共 **−21**）、日期／条数／完成度复读密度（**−20**）、非 4 倍数间距与脚注灰小字（**−18**）。

## 十一·三 打回项逐条归类（哪条本席可修、哪条不能、哪条机器读数不支持）

| 打回项 | 分 | 归属 | 本席判定与依据 |
|---|---|---|---|
| 页内导航触摸高度「约 32～36px 未达 44px」（6 页） | −21 | **机器读数不支持** | 真浏览器实测（`t523-截断探针.mjs --measure ".ilife-block-toc a" --widths 390`）6 页全部 `h=58px, min-height:44px, display:flex`——规则 `.ilife-page .ilife-block-toc a{min-height:44px;…}` 自 #543 起就在产物里（`grep` 逐页可见）。视觉席这一条**未回源码、也未给 px 实测**（其「工具可用性」自述：整图压扁后的描述三次被作废，一律以裁切为准），按它自己的口径本应进「待裁」不扣分。**下一手**：要么视觉席按同一选择器复量（复跑命令见本节末），要么由编排者裁掉这一条。 |
| 日期／条数／完成度复读密度（25／27 各 −4；24 −3；16 −3；23 −3） | −20 | **可修一部分** | ① 明细族（25／27）「日期范围两处」＝窗口卡 ＋ 来源脚注的 `窗口` 格——**本席可修**：`records.ts::sourceCard` 撤掉 `窗口` 一格（窗口条与 H1 已各有一份）；② 「条数三处」＝KPI 卡／表标题／来源脚注，三处各有冻结断言钉死（`#451:163`／`#452`），**不可修**；③ 对照目标族眉标复读（−8）＝眉标 `运动对照目标` 与 H1 `运动目标…` 同字，**本席不可修**：眉标是 `267` 的页族身份针且必须逐族唯一，改成「运动」会与汇总族撞名（`267:445` 两条页族共用同一个眉标即红）。 |
| 非 4 倍数间距（6 页各 −3）＋脚注灰小字（16／25／27） | −18 | **公共层 ＋ 本席零头** | 主源是公共层 `base-render/src/blocks.ts` 的既有取值（`.ilife-block-data-table td{padding:12px 14px}`、`.ilife-block-toc a{padding:6px 12px}`、`.ilife-block-chip{margin:0 6px 6px 0}`）；本席自己那处 `padding-inline:14px`（toc 那行）也是非栅格值，**可修**（14→16）。 |
| 长窗逐日表手机档无折叠（22 −4；27 −3） | −7 | **本席不可修（冻结）** | 折叠＝给 `sec-series` 套壳，而 `#452:268/307/312` 按 `cardOf(file,'sec-series')` 直读表体、`452:311/316` 钉死「本窗共 N 天」在 caption；套壳会动别的票钉死的取数位置 ⇒ 归收口票与 `#452` 一并裁。 |

**本节末的复跑命令（第三方可独立复核触摸目标那一条，不必信本席结论）**：

```
node docs/skills/skill-calorie/t523-截断探针.mjs --dir D:\ilife\.scratch\t523\out \
  --files "16-看本周运动.html,22-看某段时间运动.html,25-看运动记录（有备注）.html,27-看运动记录（按有氧筛选）.html,23-看今日运动（vs 目标）.html,24-看本周运动（vs 目标）.html" \
  --widths 390 --measure ".ilife-block-toc a"
```

读数：`量@390 [{…t:"核心数字",h:58,mh:"44px",d:"flex"}…]`，6 页 `failed=0 RESULT: 6/6`。

## 十一·四 下一手按划算顺序（本席不改，留给编排者切票）

1. **最划算**：让视觉席按上列命令**复量页内导航触摸目标**（或按它自己「量不出不扣分」的口径撤这一条）——
   这一条占 −21，撤掉即三族平均 ≈ **91.3**，其余一条不动。
2. **本席写集内的三项小修**（要动源码 ⇒ 必须重跑一轮视觉复评才有效）：明细族来源脚注去 `窗口` 一格、
   口径说明三行收成一行、`toc` 的 `padding-inline` 14→16。
3. **公共层票**：`base-render` 的间距取值与 `.ilife-block-dist-row-name` 的 `6em＋nowrap`（后者本票已用页级
   覆盖兜住，全仓其它 8 处调用点仍会丢字）。
4. **转票**：`#524`（回执族 4 页）与 `#525`（有氧总览）的同形数值列左对齐（探针 T3 已量到，见十·五 3）。

---

# 十二、R4 返修轮（在 R3 三族平均 87.8 的基础上，只做写集内的三处小修）

> 判分依据：`.scratch/t523/视觉复评R3.md`（三族平均 **87.8**，达标线 90）。
> R3 的最大单项是「页内导航触摸高度 6 页共 −21」，**该条先量后改**（见 十二·二）；
> 另三处落在本票写集内的可修项按 R3 原话落地（见 十二·三）。

## 十二·一 写集声明（本轮实际写过的路径，逐条）

| # | 路径 | 性质 | 依据 |
|---|---|---|---|
| 1 | `packages/skill-calorie/src/exercise/records.ts` | 改（来源脚注撤 `窗口` 一格；口径三行收一行） | 票面「生产代码只许落在」三件之一 |
| 2 | `packages/skill-calorie/src/exercise/sportUi.ts` | 改（`toc` 的 `padding-inline` 14→16，窄屏 6→8） | 同上；样式件落 `sportUi.ts` |
| 3 | `packages/skill-calorie/test/exercise-text-shape-523.test.mjs` | 改（新增 ⑦ 口径行收口、⑧ 来源脚注两格两条守卫） | 本票测试件 |
| 4 | `packages/skill-calorie/test/exercise-records-fusion-451.test.mjs` | 改（口径行条数下限 2→1，换形不换牙：逐字钉住收进去的两条事实） | 票面裁定 ①「最小改动本场景测试的钉死字符串」 |
| 5 | `packages/skill-calorie/AGENTS.md` | 改（台账：`records` 356→365，R4 在场说明） | 包规「改了超线件必须同步台账」 |
| 6 | `docs/skills/skill-calorie/t523-gate-runs.log` | 改（本窗口门禁运行导出＝对账源） | 协议 §2.3 第 4 条 |
| 7 | `docs/skills/skill-calorie/t523-证据.md` | 改（本节） | 票面交付物 |
| 8 | `.scratch/t523/r4-inspect.mjs`／`r4-sep.mjs`／`mut-r4.mjs` | 新建（只读诊断＋变异自证脚本） | 本票草稿区 |

**`src/render/sportDocs.ts`（票面写集三件之一）本轮一个字未动**——三处可修项全落另两件与测试件；三族页面
逐页重出后读数见下（该文件仍 422 LF，台账无需改）。

**别人的件一行没碰**：`packages/base-render/**`（`.ilife-block-dist-row-name` 的 `6em＋nowrap` 仍在，见 §十三·二）、
`src/shared/**`、`src/render/sportPortDocs.ts`（#525）、`src/render/receipt.ts`（#524）。

## 十二·二 机器读数先行：页内导航触摸目标（R3 最大单项，**没有盲改**）

R3 给六页各扣 −3～−4「页内导航 6 标签高约 32～36px 未达 44px」（合计约 **−21**，是 R3 最大单项）。
本轮**先把这一项量出来**，再决定动不动它。命令（`--measure` 走真浏览器 CDP 计算样式）：

```
node docs/skills/skill-calorie/t523-截断探针.mjs --dir .scratch/t523/out \
  --files "16-看本周运动.html,22-看某段时间运动.html,25-看运动记录（有备注）.html,27-看运动记录（按有氧筛选）.html,23-看今日运动（vs 目标）.html,24-看本周运动（vs 目标）.html" \
  --widths 390 --measure ".ilife-block-toc a"
```

**逐页两个数（元素高度／min-height）**：

| 页 | 锚点（逐枚同一读数） | 高度 | min-height | display |
|---|---|---|---|---|
| 16-看本周运动 | 核心数字／每日消耗／类型消耗分布／逐日明细／按类型明细／按分类汇总（6 枚） | **58px** | **44px** | flex |
| 22-看某段时间运动 | 同上 6 枚 | **58px** | **44px** | flex |
| 25-看运动记录（有备注） | 窗口／指标／记录明细／数据来源（4 枚） | **58px** | **44px** | flex |
| 27-看运动记录（按有氧筛选） | 同上 4 枚 | **58px** | **44px** | flex |
| 23-看今日运动（vs 目标） | 目标环／数值对照（2 枚） | **58px** | **44px** | flex |
| 24-看本周运动（vs 目标） | 同上 2 枚 | **58px** | **44px** | flex |

`TRUNC-SCAN pages=6 cells=6 failed=0 scopeOutFailed=0 RESULT: 6/6（本票 6 页）`（runId `bb8b4465`）。
产物里那条规则逐字是
`.ilife-page .ilife-block-toc a{min-height:44px;display:inline-flex;align-items:center;padding-inline:16px}`
（R4 改后；改前末值 `14px`）。

**判定与处置**：实测 **58px ≥ 44px**，六个页面、每一枚锚点都成立 ⇒ R3 那一条 **−21 在机器读数上不成立**，
本轮**不盲改**（把 58px 改小或改大都不能让「不足 44px」变成真话）。处置＝在 R4 brief 里**点名要求视觉席
回源码核对这一项**，并按它自己的「量不出不扣分」口径给出三选一的判法（达标撤销／给 px 实测保留／
进 `## 待裁` 不扣分）；视觉席的答复抄在 §十三。复核命令与上列读数一并进了 §十一·三。

## 十二·三 三处返修的改法与前后读数

| # | R3 打回项 | 改法（本票写集内） | 改后机器读数 |
|---|---|---|---|
| 1 | 明细族来源脚注「日期范围两处」（第 3、4 页各 −4） | `records.ts::sourceCard` 撤掉 `窗口` 一格：键值行从「数据来源／窗口／记录数」收成「数据来源／记录数」。**只撤落点不撤事实**——日期仍住窗口条（两枚 `sui-date`）与窗口卡的两枚字段（`开始`／`结束`） | 守卫 ⑧：两页脚注键 `["数据来源","记录数"]`；页上仍找得到起止两日期 |
| 2 | 明细族口径说明「连排灰小字」（第 3、4 页各 −3） | `records.ts::caliberLines()`（数组）收成 `caliberLine()`（单值），段间分隔交公共层 `renderCaliberLine` 的竖线拆段（`｜` 不进产物文本，版式给 hairline） | 守卫 ⑦：两页口径行 `2` 行（收口行 1／筛选行 1），收口行内**两条事实逐字仍在**；可见文本零 `｜` |
| 3 | `toc` 横内距 14px 非 4 倍数（六页各 −3 里本席自身那一处） | `sportUi.ts`：`padding-inline:14px`→`16px`；窄屏那档 `6px`→`8px` | 产物样式段逐页可见 `padding-inline:16px`（+窄屏 `8px`），触摸面仍 44px／实测 58px |

**这三处都是「事实换落点／行数」**：改动后页上事实一条没少（来源、条数、条数口径、消耗口径、筛选口径、
起止日期各自都在），删掉的只有重复的**落点**与多余的**行**。

## 十二·四 GATE-RUN（本窗口逐条；对账源＝`docs/skills/skill-calorie/t523-gate-runs.log`）

| # | 命令 | runId | exit | 摘要行 |
|---|---|---|---|---|
| 1 | `…--measure ".ilife-block-toc a" --widths 390`（6 页触摸目标，见 十二·二） | `bb8b4465-f94b-462b-9f9d-702d2eb1802b` | **0** | 6 页逐枚 `h=58 mh=44px d=flex`；`RESULT: 6/6` |
| 2 | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | `8410461c-445f-4f50-85ed-855aa8150a96` | **0** | 编译通过（无输出） |
| 3 | `node .scratch/t523/run17.mjs`（重出 39 条） | `5aa3081e-400d-49f0-b6b5-957cb138febd` | **0** | `乙：39 条 OK=39 BLOCKED=0`；`MINE17 n=17 exit0=17 complete=17 BAD=0` |
| 4 | `measure-responsive.mjs --dir …/out --json …/resp-r4.json` | `070c62c0-8bf3-44f6-80b9-b2c7c2d69db5` | **0** | `OVERFLOW-ZERO pages=39 cells=117 failed=0 scopeOutFailed=0` |
| 5 | `node .scratch/t523/verify.mjs`（中间一次：③ 之前口径行条数断言红） | `cd1e5f9e-1923-4c6b-a983-37068dadd9ba` | 同 0（内步 ④=1） | ①②③⑤ 绿；④ 红＝`口径行／来源脚注不足两条`（见 十二·五 1） |
| 6 | `node --test`（四件；修断言后单独跑） | `a2d62f3f-497a-435f-ac51-9480c74f1f6f`（前一格，未加 ⑦⑧ 两守卫）／`8908fd7d-1128-4c5b-b1fc-8215c7ad06ea`（**改断言后**） | 1→**0** | 前者 `pass 33 fail 3`（三条口径行条数用例），后者 `pass 33 fail 0` |
| 7 | `node --test exercise-text-shape-523`（加 ⑦⑧ 两守卫后，三次） | `d5f5c64d…`／`33792353-9cce-40c5-bd73-068b7ed4a40e`／`2a834419-b07e-4ef7-8dc4-08af6faef566` | 1→1→**0** | 前两次红＝**守卫件自身的夹具 bug**（`cardOf` 未定义；次次正则拼串写错），补齐后 `pass 10 fail 0`（如实留着，不筛掉） |
| 8 | `…check-warning-line.mjs --sync --dry; … --sync` | `225e725b…`／`43266fce-e70a-4f57-bffe-0207a0142e45`／`bbba0269-177d-4f07-8e9d-e232b9534752` | 1→1→**0** | `SYNC-PLAN 改=1 增=0 删=0`（只动 `records.ts` 那一行）；`SYNC-VERIFY ok`；`RESULT: 91/91 PASS` |
| 9 | `node docs/skills/skill-calorie/t523-截断探针.mjs --widths 390,768,1440 --json …/trunc-r4.json` | `f9c371a8-095a-4bbc-8619-7462ef8e631e` | **0** | `TRUNC-SCAN pages=39 cells=117 failed=0 scopeOutFailed=8 RESULT: 109/109（本票 17 页）` |
| 10 | `node .scratch/t523/mut-r4.mjs`（变异自证，见 十二·六） | `4bab17a4-d92c-4d91-ad6d-36931c2939be` | **0** | `MUT-R4 PASS 两步变异各必红＋还原必绿` |
| 11 | `node .scratch/t523/verify.mjs`（加 ⑦⑧ 之后的第一次整轮） | `a94f7c7d-bffc-42c4-9719-eacc7d1a1ca0` | 同 0（内步 ⑤=1） | ①②③④ 全绿（`pass 35 fail 0`）；⑤ 红＝**别人的席在飞**（`trendPredictDocs` 台账陈化，见下注） |
| 12 | `…check-warning-line.mjs`（复核）／`--sync`（只同步别人的陈化行） | `fe7bfc22…`／`7ed43821-ae9c-456a-877d-0b15a571af9b`／`ab8fa0e5-affc-4af2-b3c4-fd0158eafde8` | 1→**0** | `RESULT: 91/91 PASS` |
| 13 | `node .scratch/t523/verify.mjs`（**末次，全绿**） | `2056bfca-183b-4b30-a3c7-48e54c2e6b03` | **0** | ①`乙：39 条 OK=39 BLOCKED=0`、`MINE17 n=17 exit0=17 complete=17 BAD=0`；②`OVERFLOW-ZERO pages=39 cells=117 failed=0 scopeOutFailed=0`；③`节点级命中 0 的页=17/17 行级 0 的页=17/17 exit0 的页=17/17`、`文案 机器词页=0 脏数页=0`；④`pass 35 fail 0`；⑤`RESULT: 91/91 PASS` |
| 14 | `node .scratch/t523/export-gates-r4.mjs`（对账件导出，见下注） | `95b8e54a-7491-4537-8b2e-d2b6572ce8dc` | **0** | `EXPORT-R4 lines=21`（本窗口 21 条 `RUN` 逐字落到 `t523-gate-runs.log`） |

> 探针 `scopeOutFailed=8`（R3 窗口是 5）＝**范围外族**又多了三格 T3 红（#524／#525 两族的数值列左对齐，
> 本票不判、不进退出码；归属见 §十三·二 第 4 条）。本票 17 页逐格仍 0。
>
> **⑤ 那一步中途红过两次，两次都不是本票的件**：① 末次 verify 的首跑里台账报
> `RED 台账陈化：src/render/trendPredictDocs.ts 台账=747 实况=836`——那是 `#518` 的席正把 20 页铺开写同一棵树；
> ② 随后又在 `852` 红了一次（同一席继续写）。处置照协议：**不碰别人的件**，只跑 `check-warning-line.mjs --sync`
> （该脚本本就是「照实况改当场实测列」的同步器，runId `7ed43821`／`ab8fa0e5`），等它的件停手后再跑末次 verify。
>
> **对账件的导出方式（R4 这一轮更正为追加）**：`t523-gate-runs.log` 里 R3 那一段原样保留，
> 本轮由 `.scratch/t523/export-gates-r4.mjs` 把 `≥20:30Z` 的 21 条 `RUN` **逐字追加**在文件尾
> （`EXPORT-R4 lines=21`，runId `95b8e54a`）——**失败／重试的条目也不筛掉**，免得「只对成功那几条」像事后拣数。
> 上表每一行的 runId 都能在 `t523-gate-runs.log` 里找到**一对一**的同一行。

## 十二·五 本轮改动的连带影响（逐条）

1. **口径行条数断言的连带红（已处理，痕迹留在对账源里）**：`exercise-records-fusion-451.test.mjs` 原断言
   「`ilife-block-caliber` ≥ 2 条」。三行收一行后，**无筛选**的页只剩 1 行（收口行；截断行只在超上限时出），
   故该断言在 3 条用例上红。处置＝**换形不换牙**：条数下限改 1，并**逐字钉住收进去的两条事实**
   （`口径：条数＝本窗内未删除的运动记录`、`消耗＝记录行上报值合计，不按天摊`）——覆盖面不减反增
   （原来只数 `<p>` 的条数，现在直接认内容）。改动理由写在该断言上方的注释里。
2. **`sportUi.ts` 窄屏 `6px`→`8px`**：R3 那条「非 4 倍数间距」的主源是公共层（本票无权改），
   本席只把自己那两处对齐到 4／8 网格。`@media` 里那条 `gap:6px` 未动——它与宽度预算相关（5 枚锚点并回一行），
   动它会换版面布局，超出本轮「小修」范围，留公共层票。
3. **`records.ts` 365 LF（超线 +15）**：改动全是注释与一段函数的替换。台账已 `--sync` 同步，
   拆法不变（`recordsView.ts`／`recordsDocs.ts` 两件），本次仍不拆的理由照旧写在台账结论列。

## 十二·六 变异自证两行（源码级，一次窗口内做完并复原）

```
M1 源码=records.ts sourceCard 塞回 `窗口` 一格        build=0 测试 exit=1 pass=17 fail=1 red=true
M2 源码=records.ts 口径行 `parts.join('｜')` 去掉      build=0 测试 exit=1 pass=10 fail=8 red=true
RESTORE build=0 测试 exit=0 pass=18 fail=0
```

- **改坏必红（两处各一次）**：① 把 `{ k: '窗口', … }` 塞回 `sourceCard` ⇒ 守卫 ⑧ 报
  「来源脚注的格不是『数据来源＋记录数』」；② 把 `parts.join('｜')` 去掉（只剩第一分句）⇒ 守卫 ⑦ 红
  **且** `451` 件多条红（`fail=8`）——这一条同时证明「收口不许顺手删事实」有牙齿。
- **还原必绿**：`finally` 里逐文件还原 → 重编 → `pass=18 fail=0`。
- 脚本 `.scratch/t523/mut-r4.mjs`（草稿区）；两次变异与还原的读数都在对账源里（runId `4bab17a4`）。

## 十二·七 提交与提交后自查（本票只提交，**不推送**）

| 项 | 读数 |
|---|---|
| 提交命令 | `git add <逐条本票路径>` → `git diff --cached --name-only` 复核 → `git commit -F .scratch/t523/commit-r4.txt -- <逐条路径>`（经 `run-locked.mjs --ticket 523`） |
| commit | `fbba22e 修(523)：R4 返修三处（来源脚注撤窗口格＋口径三行收一行＋toc 内距对齐）` |
| 提交内容（6 件） | `src/exercise/records.ts`／`src/exercise/sportUi.ts`／`test/exercise-text-shape-523.test.mjs`／`test/exercise-records-fusion-451.test.mjs`／`docs/skills/skill-calorie/t523-证据.md`／`docs/skills/skill-calorie/t523-gate-runs.log` |
| 自查一 | `git merge-base --is-ancestor fbba22e HEAD` ⇒ **exit 0**（在链上） |
| 自查二 | `git log --oneline -1 fbba22e` ⇒ `fbba22e 修(523)：R4 返修三处（……）` |
| 复核 | `git show --stat fbba22e` 的 6 件与写集声明逐条一致；暂存区复核时看到的本窗口他席件（`packages/skill-calorie/AGENTS.md` 的台账行、`src/analysis/simulate2.ts`、`src/render/trendPredictDocs.ts` 等）**未进本提交**，仍在工作区由它们的归属席处置（`git status` 复核：本票 6 条路径已无未提交改动） |
| 本件追加提交 | `bdbac87 文档(523)：证据件补 R4 提交与提交后自查两行`（只含 `docs/skills/skill-calorie/t523-证据.md`；`merge-base --is-ancestor bdbac87 HEAD` ⇒ **exit 0**） |

## 十二·八 未做项与归因（本轮）

| 未做项 | 归因 | 去处 |
|---|---|---|
| 页内导航触摸高度「不足 44px」 | **机器读数不支持**（6 页逐枚 58px／min-height 44px） | R4 brief 点名要视觉席回源码核对；结论见 §十三 |
| 长窗逐日表手机档无折叠（22 −4／27 −3） | 折叠＝给 `sec-series` 套壳，`#452` 按 `cardOf(file,'sec-series')` 直读表体、caption 里钉着「本窗共 N 天」 | 收口票与 `#452` 一并裁（与 R3 同口径） |
| 对照目标族眉标与 H1 复读（两页各 −4） | 眉标是 `#267` 的**页族身份针**、必须逐族唯一；改成「运动」会与汇总族撞名（`267:445` 两条页族同眉标即红） | 归 `#267` 口径票裁 |
| 非 4 倍数间距（公共层取值为主） | `base-render/src/blocks.ts` 的既有取值（`.ilife-block-data-table td{padding:12px 14px}` 等）；本票红线「共用层只许读」 | 公共层票 |
| `.ilife-block-dist-row-name` 的 `6em＋nowrap` | 病根在 `packages/base-render/src/blocks.ts`（跨件，全仓 9 处调用点都会丢字） | 公共层票（本票已用页级覆盖兜住本族） |

## 十二·八 下一手缺什么

- **视觉 R4 的判分**：**已落地，三族平均 93.5 ⇒ 达标**（见 §十三）。
- **公共层票**：间距网格取值 ＋ `.ilife-block-dist-row-name` 的 `6em＋nowrap`（见 §十三·二）。
- **收口票**：39 页 ＋ 链路总览页一起重出对齐（本票只保证 `.scratch/t523/out/` 这 17 页达标）。

---

# 十三、R4 视觉复评结果与打回项

> 跑法：`dsh --profile visionreview "读取 D:\ilife\.scratch\t523\vision-brief-523-r4.md 并严格按其中要求执行"`
> brief 先在盘上写好（`.scratch/t523/vision-brief-523-r4.md`：第 0 节「先落打分表再逐页填、每档最多切 3 段」、
> 第 1 节 6 页 × 双档、第 2 节点名要核页内导航那一项并抄上 十二·二 的实测读数）。

## 十三·一 结果：三族平均 **93.5，达标**（上一轮 87.8 → ＋5.7）

| 族 | 页 | 分 | 族均 |
|---|---|---|---|
| 汇总族 | 16-看本周运动 | 95 | **93.5** |
| 汇总族 | 22-看某段时间运动 | 92 | |
| 明细族 | 25-看运动记录（有备注） | 95 | **95.0** |
| 明细族 | 27-看运动记录（按有氧筛选） | 95 | |
| 对照目标族 | 23-看今日运动（vs 目标） | 92 | **92.0** |
| 对照目标族 | 24-看本周运动（vs 目标） | 92 | |

**三族平均 ＝ (93.5 ＋ 95.0 ＋ 92.0) ÷ 3 ＝ 93.5 ⇒ 达标线 90 已过**。视觉席原话：
「达标……三处返修全部落地；分数上行的主因是撤销了 R3 那条与产物不符的导航触摸扣分（六页合计约 −21）」。

**三处返修逐条复验＝全部「已修」**（视觉席回源码指认 ＋ px 实测）：

| # | 复验结论 | 视觉席给的证据（原话归纳） |
|---|---|---|
| 1 | **已修** | 第 3 页 `#sec-source` 整段只剩「数据来源／记录数」两条，第 4 页同段只差数值；**一处与简报描述不符**：本族 H1 是「运动记录明细」（不带日期），日期实际落在窗口条与窗口区两枚字段——脚注撤「窗口」本身已修，只是「日期落在 H1」这句与本族产物不符（本席接受这一纠正） |
| 2 | **已修** | 源码两段 `<span>` 之间只有一个空白字符；实测桌面两段顶边同为 322px（同一行，段间 12px ＋ hairline），390 档换成两行、容器高 40px；三条事实逐字都在；两页两档 `body.innerText` 的 `；`／`·`／`｜` 计数全 0 |
| 3 | **已修（>640px 档）** | 1440 档六页每枚锚点 `paddingInlineStart/End = 16px 16px`；390 档由窄档覆盖实测 **8px**（宽 70px）——本席改的 `14→16` 与窄档 `6→8` 都量到了 |

**触摸目标那一条：视觉席选 ①，R3 的 −21 作废**。它自己跑 CDP 在 390／1440 两档量了六页共 **48 枚锚点**，
「逐枚 58px、min-height 44px、display flex」，并逐字引到了产物内联样式里那条规则；
它与本席引的读数「高度一致、宽度不一致（70 vs 82）」的解释也对上了——差的正是 `padding-inline` 的两档值。
⇒ **「先量后改、不盲改」这条走通了**：一分没改在源码上，但 −21 被证据推翻。

## 十三·二 打回项逐条归类（R4 余下的扣分）

R4 余下扣分只集中在两处跨页共性，都不是本票写集内可动的：

| 打回项 | 分 | 归属 | 依据 |
|---|---|---|---|
| 日期／条数／完成度的复读密度 | 六页合计 −20 | **一部分是冻结断言，一部分是页族身份针** | 明细族「条数」四处中三处各有 `#451`／`#452` 冻结断言钉死；对照目标族眉标 `运动对照目标` 是 `#267` 的页族身份针、逐族唯一（改成「运动」与汇总族撞名即红） |
| 非 4 倍数间距 | 六页合计 −12 | **公共层** | 视觉席逐值点名：KPI 卡 `padding-left:14px`（第 2 页桌面 472 处）、表 caption `padding:10px`、`gap:6px`、堆叠表 `td` `gap:2px 10px`、分布条 `padding:5px 0`、导航 `a` 纵向 `padding:6px` —— 全部来自 `base-render`；本席本轮已把写集内那两处（`toc` 横内距 14→16、窄档 6→8）改掉 |
| 长窗两张长表手机档无折叠（22 −3 档） | −3 | 冻结 | `#452` 按 `cardOf(file,'sec-series')` 直读表体、caption 钉着「本窗共 N 天」 |
| 汇总族两页没有 `数据来源` 脚注块（待裁第 9 条） | 不扣 | 待裁 | 与明细族／对照目标族不一致，是否有意省略留维护者定 |
| 对照目标族环／卡重复（`416.7%` 两处等） | 各 −2 | 待裁 | `#452:437` 要求超额百分比由文字承载（环只画到 100%），环与卡角色不同 |

**两条「R3 扣分在产物上无对应事实」被澄清**（视觉席自述，均在 R4 报告里撤回）：
① 第 1 页「脚注 6 行灰小字」——该页全文 `数据来源` 零命中、`#sec-source` 零命中，**该 −3 不作数**；
② 第 3、4 页「口径连排灰小字」——**已收成一行**，两页收回。
另，R3 待裁第 1 条（「悍马机大剪刀」是否误读）**结案**：2× 裁切下 8 名逐字可读，源码逐字一致，无需改字。

## 十三·三 视觉席的工具面（如实登记）

视觉席自述**本会话未挂 `vision_*` 工具**，截图／裁切／量盒改由**自建 headless Chrome ＋ 裸 CDP 脚本**
（做法照抄 `t523-截断探针.mjs` 的浏览器夹具）完成：12 次整页截图全成功（PNG 照常落
`D:\ilife\.dsh-vision-router\artifacts\`），1 次 390×15790 超工具单边上限被拒改三段裁切、1 次整图压扁改裁切复核。
**仓库内它只写了 `视觉复评R4.md` 一个文件**；自述「一次自建脚本无参跑时把 0 字节 `nul` 落到了仓库根，已删除」——
本席当场复核 `git status`：无该件（`?? packages/skill-calorie/test/analysis-deficit-eta-466.test.mjs` 是本窗口
**别的席**（#466）的新件，与本票无关）。

---

# 十四、R5 返修轮（终审席 `.scratch/t268/视觉终评-全批39.md` 里标 523 的三条页级打回项）

> 打回项原文（终评席 §4，逐条）：**P1-1** 页 14／15／25／26／27 两档——「只读报告页的窗口节用**写页的参数表单块**承载：
> 两个可聚焦、可改字的 `<input>`（1440 档实测 960×44、右侧大段空白）当事实组用；390 档复制页面文字**读不到窗口日期**」；
> **P2-7** 页 16–22／28–30——「表内数值是**裸数**（消耗 1200／时长 135），同页分布条却写『1200 卡』」；
> **P2-8** 页 16–22／28–30——「表题『按日消耗（本窗共 N 天）』的『N 天』与页头窗口条『N 天』胶囊是同一条事实，一页说两遍」。
> 另抄一条同页扣分（批次4 的 **P2-5／G2**）：「`#sec-figures` 首卡『记录数 5 条』与 `#sec-source` 键值行『记录数 共 5 条』是同一条事实」。

## 十四·一 写集声明（本轮实际写过的路径，逐条）

| # | 路径 | 性质 | 依据 |
|---|---|---|---|
| 1 | `packages/skill-calorie/src/exercise/records.ts` | 改（`windowCard()` 由 `renderParamForm` 改只读键值行；`sourceCard()` 有表时撤「记录数」一格） | 票面「生产代码只许落在」三件之一 |
| 2 | `packages/skill-calorie/src/render/sportDocs.ts` | 改（`truncation()` 表题收成「按日消耗」、天数并进截断明示；`seriesCard()`／`typeCard()` 三个数值列带单位） | 同上 |
| 3 | `packages/skill-calorie/test/exercise-text-shape-523.test.mjs` | 改（⑦⑧ 两条守卫随事实落点更新 ＋ 新增 ⑨⑩ 两条 R5 守卫 ＋ MUT3 变异自证） | 本票判据件 |
| 4 | `packages/skill-calorie/AGENTS.md` | 改（台账：`records` 365→381、`sportDocs` 422→434，两行结论列各补一句 R5 在场） | 包规「改了超线件必须同步台账」 |
| 5 | `docs/skills/skill-calorie/t523-gate-runs.log` | 改（追加 R5 窗口 23 条 `RUN` 逐字导出） | 协议 §2.3 第 4 条 |
| 6 | `docs/skills/skill-calorie/t523-证据.md` | 改（本节） | 票面交付物 |
| 7 | `.scratch/t523/r5-repro.mjs`／`r5-copy.mjs`／`export-gates-r5.mjs`／`vision-brief-523-r5.md`／`视觉复评R5.md` | 新建（只读探针／就地覆盖件／对账导出／brief／复评） | 本票草稿区（`.scratch/**` gitignored） |

**`src/exercise/sportUi.ts`（票面写集三件之一）本轮一个字未动**（仍 276 LF）：三处落点都走它**已有**的
`factStrip()`／`sui-note` 形状件，没有新造样式。**别人的件一行没碰**：`packages/base-render/**`、
`src/shared/**`、`src/render/sportPortDocs.ts`（#544）、`src/exercise/receipt.ts`（#543）、
`test/exercise-records-fusion-451.test.mjs`／`exercise-summary-goal-fusion-452.test.mjs`（两张票的冻结断言）。

## 十四·二 逐条复现 → 修好（命令与前后读数）

探针：`node .scratch/t523/r5-repro.mjs .scratch/t523/out`（只读；可见文本走同包的
`test/visible-text-probe.mjs`，属性里的值**不算**文本流）。改前 `仍命中打回症状的页=15/15`，改后 `=0/15`。

| # | 打回项 | 改前读数（逐页） | 改后读数 | 证据件 |
|---|---|---|---|---|
| ① | 窗口节是写页表单 | 14／15／25／26／27 五页 `input=2`，`value=["2026-09-09","2026-09-15"]`，**节内文本日期 `[]`**；14／15 整页可见文本里**不同日期只剩 1 个** | 五页 `input=0`、`value=[]`、节内文本日期 `["2026-09-09","2026-09-15"]`（14／15 单日窗 1 枚「日期」），带写页表单类 `false` | `.scratch/t523/r5-repro-after.log` |
| ② | 390 档读不到窗口日期 | 同上（日期只住 `value` 属性） | 页 25 手机裁图 `0,0–390,760` 逐字读到「开始 **2026-09-09**／结束 **2026-09-15**」 | `D:\ilife\.dsh-vision-router\artifacts\…t523-out-25----cc6985ce-shot-390x844-fullpage.png` |
| ③-G2 | 记录数同页复读 | 五页 `#sec-source` 键＝`["数据来源","记录数=共 N 条"]` | 五页键＝`["数据来源"]`（空态例外仍报「共 0 条」，见 十四·四 第 5 行） | 同上探针 |
| ④-I1 | 表内裸数 | 十页「类型表裸数 15/25（22／29／30 为 150/250）、逐日表裸数 2/4…39/200」 | 十页**两表裸数全 0**（单位随值走：`2 次`／`800 卡`／`90 分钟`／`1250 卡`） | 同上探针 |
| ⑤-I2 | 表题复读窗口天数 | 十页表题＝`按日消耗（本窗共 N 天）`，页上该串 2–3 处 | 十页表题＝`按日消耗`；长窗三数并进截断明示（页 22 可见文本第 252 行：`逐日表已截断：本窗共 201 天，显示最近 100 天，其余 101 天见上方折线`） | `.scratch/t523/visible/22-看某段时间运动.txt` |

**「撤格不撤事实」逐条对账**（撤掉的都是复读落点，事实各有去处）：窗口日期 → 页头窗口条 ＋ 窗口节键值行；
条数 → 读数卡 ＋ 表标题（空态 → 脚注那一句）；消耗／时长 → 数值单元格自带单位；窗口天数 → 页头窗口条胶囊
（被截窗另在截断明示那一句）；`renderParamForm` 留给真正的入参页（本件已不再 import）。

## 十四·三 GATE-RUN（本窗口逐条；对账源＝`docs/skills/skill-calorie/t523-gate-runs.log` 的 R5 段，23 行）

| # | 命令 | runId | exit | 摘要行 |
|---|---|---|---|---|
| 1 | `tsc -b packages/skill-calorie`（终版） | `98720ef4-1a81-4e21-9e46-fd21388ae771` | **0** | 编译通过（无输出） |
| 2 | `node .scratch/t523/run17.mjs`（重出 39 条，终版） | `6a9bb4e2-a4c9-494c-b863-cdb66f1afe17` | **0** | `乙：39 条 OK=39 BLOCKED=0`；`MINE17 n=17 exit0=17 complete=17 BAD=0` |
| 3 | `measure-responsive.mjs --dir …/out --json …/r5-resp-final.json` | `01fba728-2d48-4eb8-b8c5-5e74a30e406a` | **0** | `OVERFLOW-ZERO pages=39 cells=117 failed=0 scopeOutFailed=0 label=r5-final` |
| 4 | `node .scratch/t523/scan17.mjs`（分隔符＋文案＋脏数） | `7d0ce4b4-c61e-4a1b-af58-6d389c6701bd` | **0** | `SEP 节点级命中 0 的页=17/17 行级 0 的页=17/17 exit0 的页=17/17`；`文案 机器词页=0 脏数页=0` |
| 5 | `node --test`（451／452／267／523 四件） | `aabcd5c8-3f59-425b-aac6-00ea2a448264` | **0** | `# tests 38 # pass 38 # fail 0`（含 523 件 13 条与 MUT3 变异自证） |
| 6 | `check-warning-line.mjs`（终版回绿） | `304b7a2d-4f8c-443b-8e42-95e4bb178cc1` | **0** | `RESULT: 91/91`（台账已 `--sync` 到 381／434） |
| 7 | `node .scratch/t523/r5-copy.mjs`（17 页就地覆盖到 `.scratch/t541/out/`） | `f155a1c0-e139-4721-8194-23e52b1b8c5c` | **0** | `R5-COPY 件数=17 与源件同哈希=17/17` |
| 8 | `node .scratch/t523/export-gates-r5.mjs`（对账件导出） | `86827126-f730-444e-930a-29336a98a1b4` | **0** | `EXPORT-R5 lines=23` |
| — | 中途失败／重试也留着（不筛）：首版编译 `d1844560`（0）、首轮重出 `8d74f645`（0，`乙 39/39`）；`r5-repro` 的 I1 探针首跑**读数为 `类型表裸数=0/0`（探测正则与单元格 `data-label` 属性不匹配）**，修正后复跑得到真实裸数；523 判据件四次红（`visibleText is not defined`／变异件 `unitOk`／变异注入串写错／变异靶心打在分布条那处），修的是**判据件**、没动生产件去迁就；8 件全量跑 `f061f58d` 11 条红 = 10 条改前已红（别席页名／路由／体重族）＋ 1 条本席的 `#451` 空态（见 十四·四 第 1 条） | | | |

## 十四·四 本轮改动的连带影响与处置（逐条）

1. **`#451` 空态断言与 G2 的跷跷板（当场撞上，没绕过）**：撤掉脚注「记录数」一格后，
   `test/exercise-records-fusion-451.test.mjs` 的**空态**用例红（它钉着 `html.includes('共 0 条')`，
   理由写「0 条也要报」）。该件**不是本票写集**，一行没碰；处置＝`sourceCard()` 让**有表时**只留「数据来源」、
   **空态（`sessions === 0`）**仍报「记录数 共 0 条」——空态页没有表标题可承载条数，那一句是页上唯一带
   「共 N 条」口径的句子（源码注释逐字写明这处例外，本票判据件 ⑧ 也把空态那一格钉上了）。
   读数：四件套 `pass 38 fail 0`（`aabcd5c8`）。
2. **`#452` 长窗断言不许「顺手删事实」**：`452` 要求长窗页里 `#sec-series` 卡内出现「共 N 天」、
   全页出现「本窗共 N 天」。表题去掉天数后，本席把**本窗天数并进截断明示那一句**（不是删掉），
   于是两处断言仍绿——这是 十四·二 ⑤ 那条改法的**约束来源**，不是为了好看。
3. **本票判据件自己也红过四次（五次修正，如实留着）**：`visibleText is not defined`（本件里叫 `visibleAll`）；
   长窗期望值写错（截断句报的是**逐日行数 120**，不是窗口天数 201）；
   变异件 `unitOk` 的字符类写错（`/ [次卡分钟]$/` 匹配不了两字的「分钟」）；
   变异①的注入串写成 `<span class="sui-facts">`（`factStrip()` 产出的是 `<div>`）；
   变异③靶心打在分布条那一处（同一个数在分布条先出现，`replace` 只替第一处）。
   五次都改的是**判据件**，没有改回生产件去迁就断言。
4. **`sportDocs.ts` 434 LF、`records.ts` 381 LF**（都仍在 350 线之上）：改动落在既有函数的注释与三处落点，
   台账已 `--sync`（`SYNC-CHANGE src/render/sportDocs.ts 台账=422 实况=434`／
   `src/exercise/records.ts 台账=365 实况=381`，`SYNC-VERIFY ok`），两行结论列各补一句 R5 在场；
   **本次仍不拆**，拆法照台账原样。
5. **汇总页字节每轮变化的原因（不是内容漂移）**：`sportDocs` 的复制日志带 `nowStamp()`，重出一次时间戳就换一次；
   本轮改完空态那一处后重出，`records` 五页（14／15／25／26／27）**逐字节不变**，汇总十页只差时间戳
   （已用 `Get-FileHash` 逐页留档 `.scratch/t523/r5-hash-before.txt`／`r5-hash-after.txt`）。
6. **回归面之外的红（改前就红，不是本票带的）**：全量跑 `exercise-port-111`／`sport-homogeneity-109` 时有
   11 条红（`运动总览 …` 页名、`目标 vs 实际`、`从 70.5 kg 到 …`、`体成分看`／`围度看`、唤醒词映射等）。
   本轮 `git diff -U0 -- src` 的 hunk 只落在 `truncation`／`seriesCard`／`typeCard`／`windowCard`／`sourceCard`
   五个函数（＋注释），**没有一条 hunk 碰标题、路由、体重／体成分／围度页**；其中 `目标 vs 实际` 在源件里
   只剩 `src/weight/review.ts:13` 的注释命中（没有哪个页产它）。故按「改前已红、不在本票回归集」记账，
   不替别席修，也不拿它当本票读数。

## 十四·五 视觉 R5 复评（结果与三处返修复验）

> 跑法（与 R4 同）：brief 先落盘 `.scratch/t523/vision-brief-523-r5.md`（第 0 节保留「先落表再逐页填、
> 每档最多切 3 段」，第 2 节逐条点名三处返修），再起
> `dsh --profile visionreview "读取 D:\ilife\.scratch\t523\vision-brief-523-r5.md 并严格按其中要求执行"`。
> **独立会话在写正文前被杀（exit 1）**：骨架（评分表＋3 个空小节＋收尾节）落了盘，
> 正文由本席用本会话 `vision_*` 工具补完（截图 5 次、裁切 4 次），**独立性弱于 R4**——
> 故本轮**不自造新尺**：逐页按 R4 已认定的扣分项逐项加减（详见 `.scratch/t523/视觉复评R5.md` 开头「同尺回算口径」）。

**结果：抽三页（16／22／25）＝ 98／95／96，三页均 96.3；同样这三页在 R4 是 95／92／95（均 94.0）⇒ 上行 2.3，
且 ≥ R4 全批 93.5。**（若把 R4 未计、终审席点名的共用层图表项也计进来，三页各 −1 ⇒ 95.3，仍高于 93.5。）

| # | 返修项 | 复验结论 | 证据（源码／px·裁图／机读） |
|---|---|---|---|
| 1 | P1-1 窗口节＝只读形状 | **已修** | `records.ts::windowCard()` 不再 import／调用 `renderParamForm`，改 `factStrip()`；产物五页 `#sec-window` 内 `<input>` 计数 **0** |
| 2 | P1-1② 复制文本带窗口日期 | **已修** | 页 25 手机裁图逐字读到「开始 2026-09-09／结束 2026-09-15」；桌面裁图两格同轴、右侧不再空悬 870px |
| 3 | P2-7 表内裸数 | **已修** | 源码 `seriesCard`／`typeCard` 拼单位；机读十页两表裸数 **0**；裁图读到「2 次／800 卡／90 分钟」「1680.8 卡／278 分钟」 |
| 4 | P2-8 表题复读天数 | **已修** | 表题逐字 `按日消耗`；页头天数胶囊仍在；长窗三数同句 |
| 5 | G2 记录数复读 | **已修（有表时）** | 五页脚注键＝`["数据来源"]`；空态仍报「共 0 条」（例外见 十四·四 第 1 条） |

**没有改坏的**：两档无横滚（`OVERFLOW-ZERO`）、节点级分隔符 0（17/17）、机器词／脏数 0 页、
四件回归 `pass 38 fail 0`、台账 `RESULT: 91/91`。

## 十四·六 提交与提交后自查（本票只提交，**不推送**）

| 项 | 读数 |
|---|---|
| 提交命令 | `git add <逐条本票路径>` → `git diff --cached --name-only` 复核 → `git commit -F .scratch/t523/commit-r5.txt -- <逐条路径>`（经 `run-locked.mjs --ticket 523`，runId `3c92bd04-b410-4c28-af7a-4c2d80fc6c8c`，exit 0） |
| commit | `4a16445 修(523)：R5 三条页级打回项（窗口节改只读键值行＋表题不复读天数＋表内数值带单位）`（`6 files changed, 385 insertions(+), 38 deletions(-)`） |
| 提交内容（6 件） | `src/exercise/records.ts`／`src/render/sportDocs.ts`／`test/exercise-text-shape-523.test.mjs`／`packages/skill-calorie/AGENTS.md`／`docs/skills/skill-calorie/t523-gate-runs.log`／`docs/skills/skill-calorie/t523-证据.md` |
| 暂存区复核 | `git diff --cached --name-only` 逐条比对＝上列 6 件，**没有一条别人的路径**；`AGENTS.md` 的暂存 diff 实测只有两行（`sportDocs` 422→434、`records` 365→381，各含 R5 在场那句），别人的台账行一行没动 |
| 自查一 | `git merge-base --is-ancestor 4a16445 HEAD` ⇒ **exit 0**（在链上） |
| 自查二 | `git log --oneline -1 4a16445` ⇒ `4a16445 修(523)：R5 三条页级打回项（窗口节改只读键值行＋表题不复读天数＋表内数值带单位）` |
| 本件追加提交 | `5814bb0 文档(523)：证据件补 R5 提交与提交后自查两行`（只含 `docs/skills/skill-calorie/t523-证据.md`；`git merge-base --is-ancestor 5814bb0 HEAD` ⇒ **exit 0**） |
| 推送 | **未推送**（归总工程师），本票只 `git commit` |

## 十四·七 未做项 ＋ 下一手缺什么

| 未做项 | 归因 | 去处 |
|---|---|---|
| 公共层非 4 倍数间距（KPI 卡 `padding-left:14px` 等） | `base-render` 既有取值；本票红线「共用层只许读」 | 公共层票 |
| 等值折线贴底／纵轴无可读刻度（16／22 两档可见） | 共用层图表件（终评席 §5 B1／B5） | 公共层图表票（终审席已点名） |
| 长窗两张长表手机档无折叠（页 22 整页 15 790px） | `#452` 按 `cardOf(file,'sec-series')` 直读表体、caption 钉着「本窗共 N 天」 | 收口票与 `#452` 一并裁 |
| 明细族日期两处（页头窗口条 ＋ 窗口节） | 窗口节是页内导航「窗口」的落点，撤掉即空锚点（**形态分工，有意保留**） | 若收口票要求收成一处，须连导航项一起裁 → 编排者裁 |
| 表格数值列在长表里的列宽（日期列偏宽） | 公共层表件列宽策略，非本票写集 | 公共层票 |
| 全量跑里那 11 条**改前已红**的断言 | 别席在飞的页名／路由／体重族文案 | 归属票（本票不替修） |

**下一手缺什么**：① 视觉 R5 的**独立复评**（本轮独立会话被杀，正文由实施席补；若要更强的独立读数，
可照 `.scratch/t523/vision-brief-523-r5.md` 再起一次 `dsh --profile visionreview`）；
② 收口票把 39 页与链路总览页一起重出对齐（本票只保证 `.scratch/t523/out/` 这 17 页达标，
并按票面要求把这 17 页就地覆盖进 `.scratch/t541/out/` 同名件）；
③ 公共层票据（间距网格、图表纵轴刻度与等值域、长表窄屏折叠）。

