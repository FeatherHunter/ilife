# #316 证据：场景 04 运动 9 键搬进 `src/exercise/`（命令声明与分派）

票号标签 `316`。地图 #293 下票，工作区 `D:\ilife`。
范围＝把**场景 04（运动）9 键**的命令声明与分派搬进新能力目录 `packages/skill-calorie/src/exercise/`，
**纯搬迁**：处理函数逐字搬出、字段原值照抄、行为不变。

## 0. 落点窗口

- 开门条件：`git status --porcelain` 空（`#322` CLOSED ＋ `#323` 已落点之后）；审计窗口 `--since`＝`2026-09-13T14:01:29.801Z`、
  `--until`＝`2026-09-13T14:02:33.324Z`（收口在 `git add` 之前：其后的 git 条目按 `check-gate-audit` 的用法属「提交后条目」，不进窗口）。
- 本票实测改动面（`git status --porcelain`，脚本落盘，非手抄）：

```text
     M packages/skill-calorie/src/cli/cmd_read.ts
     M packages/skill-calorie/src/cli/legacy/routes/scene-04.ts
     M packages/skill-calorie/src/cli/legacy/scene-04.ts
     M packages/skill-calorie/src/cli/registry.ts
     M packages/skill-calorie/src/cli/write.ts
    ?? packages/skill-calorie/src/exercise/cardio.ts
    ?? packages/skill-calorie/src/exercise/commands.ts
    ?? packages/skill-calorie/src/exercise/distribution.ts
    ?? packages/skill-calorie/src/exercise/edit.ts
    ?? packages/skill-calorie/src/exercise/goal.ts
    ?? packages/skill-calorie/src/exercise/index.ts
    ?? packages/skill-calorie/src/exercise/log.ts
    ?? packages/skill-calorie/src/exercise/recap.ts
    ?? packages/skill-calorie/src/exercise/routes.ts
    ?? packages/skill-calorie/src/exercise/strength.ts
    ?? packages/skill-calorie/src/exercise/trend.ts
```
- 持锁：`node tooling/run-locked.mjs --ticket 316-land --lock-dir .scratch/locks-land --stale-minutes 30 -- pwsh -NoProfile -File .scratch/t316/land.ps1`
- 全部门禁与 git 写操作经 `node tooling/run-locked.mjs --ticket 316 -- <命令>`（逐条 runId 见 §3）。
- 结束条件：`git status --porcelain` 空（窗口外零未提交改动）。
- 写集分三类（编排者裁决：`.scratch/.../tree/` 是预备期快照，别人可能已改过同一文件）：
  **独占件整文件复制**（我新建的 `src/exercise/**` 11 件 ＋ 我独有的旧命令分片 `legacy/scene-04.ts`，
  复制前后者要过 blob 基线守卫）；**其余一律定点删除**——`src/cli/cmd_read.ts`／`src/cli/write.ts`
  （六票共用分派文件）与 `src/cli/legacy/routes/scene-04.ts`（#314 要搬的 `calorie.view.exercise`
  那 19 条也住这个文件）由 `.scratch/t316/edit-dispatch.mjs` 读**当前**文件、只删属于本票的行再写回；
  **生成物**由 `pnpm gen` 出，不手改。
  写树之前还有一道**防回滚守卫**：`.scratch/t316/prep-hashes.txt`（`write-hashes.mjs` 机械生成）
  ＋ `check-hashes.mjs` 逐条重算比对，不等即 `TRIPPED` 退 1（留痕 `.scratch/t316/logs/hash-guard.out.txt`）。
  另核：本票 9 键的 25 条路由记录**只住** `routes/scene-04.ts`（`grep` 全量扫过 `legacy/routes/*.ts`），
  故别票的分片里没有本票的点删对象。

## 1. 改前／改后对照

| 面 | 改前 | 改后 |
|---|---|---|
| 命令声明（权威源） | `src/cli/legacy/scene-04.ts` 的 9 条 `LegacyCommandDecl` | `src/exercise/commands.ts` 的 9 条 `CommandSpec`（六字段逐字相同；机械自检见 `.scratch/t316/build-tree.mjs` 的「六字段逐条相等」断言） |
| 处理函数 | `src/cli/write.ts` 的 3 个 `case`（`exercise.add`／`update`／`remove`）＋ `src/cli/cmd_read.ts` 的 6 个 `case` | `src/exercise/log.ts`／`edit.ts`／`goal.ts`／`strength.ts`／`cardio.ts`／`distribution.ts`／`trend.ts`／`recap.ts`（逐字搬出） |
| 路由记录 | `src/cli/legacy/routes/scene-04.ts` 45 条 | `src/exercise/routes.ts` **26** 条（19 wake ＋ 6 new ＋ 1 条 non-exec）；原文件**定点删除**本票这 26 条（余下多少取决于 #314 是否已先搬走它那 19 条 `calorie.view.exercise`——两种情况都对：全局 `(list, order)` 仍逐条恰好一次） |
| 分派层 | `cmd_read.ts`／`write.ts` 的 switch 里有这 9 个 `case` | 两处 `case` 删净（**定点删除**，不整文件覆盖）；`REGISTRY`（注册表先行）命中后走能力目录的 `run` |
| 文件内私有件 | `write.ts` 的 `EX_CAMEL`／`oneExercise` | 随处理函数搬进 `edit.ts`／`log.ts`（各自唯一用户，铁律二） |
| 生成物 | — | 只 `src/cli/registry.ts` 多一行 `EXERCISE_COMMANDS` |

行数（本包告警线 350；物理行＝按 `\n` 切分计数）。**返修当刻实测**（BASE `e9dc6523`，2026-09-13T23:54:46+08:00）：
新增 11 件逐件为 edit.ts 96、log.ts 79、routes.ts 44（**BASE 锚点 as-of**；补正窗前的返修窗给它加 1 行注释 ⇒ 该件在提交后为 **45**）、index.ts 38、commands.ts 33、cardio.ts 22、distribution.ts 22、recap.ts 22、strength.ts 22、trend.ts 22、goal.ts 18 —— **最长 `edit.ts` 96 行**，次长 `log.ts` 79 行
（这两件在 60 行以上，其余各件 ≤ 44；不存在「全部 ≤ 60」这种说法）。
分派两件按本票区间实测：`cmd_read.ts` 改前（`a9fb35c^`）**857** → 改后（`a9fb35c`）**799**；
`write.ts` 改前 **615** → 改后 **466**（`1146`／`745` 是 `test/cmd-registry-294.test.mjs:42-43` 的 #294 冻结上限，
不是任何一端的实测行数，勿再当起点）。同一时刻的工作区（BASE）为 `cmd_read.ts` 474 行、`write.ts` 223 行 —— 两件自本票改后只被两张后续票动过（按 `git log a9fb35c..c3662700` 实测）：#315 场景 02 饮食 24 键 `86786e8` ⇒ `cmd_read.ts` 799→653、`write.ts` 466→223；#319 场景 10「分析」13 键 `cbb5da8` ⇒ `cmd_read.ts` 653→474（`write.ts` 未动） —— 均未超线，第四步不触发。

## 2. 搬迁键清单（9 键；`order` 为原列表内原值）

| 键 | 形状 | 标题 | 代表唤醒词 | 处理函数（新住处） | 路由记录 `list/order` |
|---|---|---|---|---|---|
| `calorie.exercise.add` | receipt | 记运动 | 记运动 | `log.ts` `writeExerciseLog` | wake 137–144（8 条） |
| `calorie.exercise.update` | receipt | 改运动 | 改运动记录 | `edit.ts` `writeExerciseUpdate` | wake 145–146 |
| `calorie.exercise.remove` | receipt | 删运动 | 删运动记录 | `edit.ts` `writeExerciseRemove` | wake 147–149 |
| `calorie.view.exercise-goal` | stat | 运动目标视图 | （原值即缺，见 §7 偏离 2） | `goal.ts` `viewExerciseGoal` | wake 159–160 ＋ new 28 |
| `calorie.view.exercise-strength` | stat | 力量训练总览 | 看力量训练总览 | `strength.ts` `viewExerciseStrength` | wake 162／168 ＋ new 34 |
| `calorie.view.exercise-cardio` | stat | 有氧训练总览 | 看有氧训练总览 | `cardio.ts` `viewExerciseCardio` | wake 163／169 ＋ new 35 |
| `calorie.view.exercise-distribution` | stat | 运动类型分布 | 看运动分类占比 | `distribution.ts` `viewExerciseDistribution` | new 36 |
| `calorie.view.exercise-recap` | stat | 运动复盘 | 看运动复盘 | `recap.ts` `viewExerciseRecap` | new 37 |
| `calorie.view.exercise-trend` | stat | 运动趋势 | 看运动消耗趋势 | `trend.ts` `viewExerciseTrend` | new 39 |

non-exec 记录 1 条：`{ list: 'wake', order: 161, wakeWord: '看运动记录（有备注）', scene: '04', kind: 'non-exec', bucket: 'legacy-chain', reason: … }`
——**形态与理由逐字未改**（缺「备注」筛选参数，参数补齐归场景 04 自己的图 #156）。

路由整组同迁自检（`.scratch/t316/build-tree.mjs`）：`搬走 26 ＋ 剩下 19` 逐条**逐字等于**原 45 条；
9 键在搬迁后的记录面里全部有落点。

## 3. 门禁运行声明（`GATE-RUN`，与 `.scratch/locks/gate-runs.log` 一对一）

# GATE-RUN 声明（窗口 since=2026-09-13T14:01:29.801Z）
GATE-RUN runId=286bbcda-f698-48dc-9ddc-6523527e054e cmd="pnpm build"
GATE-RUN runId=93e00525-2d0e-4f49-a5a0-f708d51d0070 cmd="pnpm gen"
GATE-RUN runId=5d91a1dd-40e6-41f6-81fc-4aa9848d6c89 cmd="pnpm build"
GATE-RUN runId=d74ab7fd-f844-4875-a1c4-9d0c055ab9e2 cmd="pnpm gen:check"
GATE-RUN runId=3e19b5c8-8f6f-4ba6-9374-303642738d15 cmd="pnpm test"
GATE-RUN runId=4846eab1-e3de-456b-89d4-7ec037fa936b cmd="node .scratch/t316/run-t293.mjs"
GATE-RUN runId=12735a13-d078-4b08-933a-72ed4c35cba9 cmd="pnpm gen:check"
GATE-RUN runId=028d55fb-e56f-4882-b98f-4baae55ce8ef cmd="pnpm gen"
GATE-RUN runId=84b74baa-1b2a-40ec-94b6-1f6f4d881a8e cmd="pnpm gen:check"
GATE-RUN runId=da25d8be-9b79-40d5-8466-fceeb3348f5d cmd="pnpm gen:check"
GATE-RUN runId=d06c6108-9d51-4c4b-a090-3b7a0b056874 cmd="pnpm gen:check"
GATE-RUN runId=059ef078-47db-4fba-a598-f41e403d4e55 cmd="pnpm build"
GATE-RUN runId=439f5850-93a7-4b87-af75-9cdc2ce588fc cmd="pnpm gen"
GATE-RUN runId=8ec7e843-e15c-444b-b897-e252b5007ac3 cmd="pnpm build"
GATE-RUN runId=5500da00-3f20-440b-9b15-4044d2045e15 cmd="pnpm gen:check"

## 4. 免费判据实测（落地窗口内的改动面，共 5 件 —— 生成物部分只有 `src/cli/registry.ts`，其余 4 件是搬迁／定点删除写下的手写件；生成物逐字节验真见 `pnpm gen:check` 五件 ok）

`pnpm gen` 之后 `git diff --name-only`（逐字节比对由 `pnpm gen:check` 兜底）：

    packages/skill-calorie/src/cli/cmd_read.ts
    packages/skill-calorie/src/cli/legacy/routes/scene-04.ts
    packages/skill-calorie/src/cli/legacy/scene-04.ts
    packages/skill-calorie/src/cli/registry.ts
    packages/skill-calorie/src/cli/write.ts

## 5. 变异自证（两发，各证明一条机器门真会红）

下表「实测」列直接写 §3 `GATE-RUN` 行里 36 位 `runId` 的**前 8 位**（§3 不另加 `tag` 列）——按前缀在 §3 逐条对得上；`exit` 值取自同一次运行的 `RESULT` 行。

| 变异 | 动作 | 预期 | 实测 |
|---|---|---|---|
| M1（生成物面） | 手改生成物 `src/cli/registry.ts`（`src/exercise/index.js`→`…indexMUT.js`） | `pnpm gen:check` 报 **GEN-CHECK FAIL**、exit≠0 | 见 §3 里 `runId` 前 8 位 `12735a13` 那一行（exit=1，按设计） |
| M1 复原 | `git checkout -- <该件>` → 重跑 `gen:check` | PASS | §3 `runId` `028d55fb`（`pnpm gen`，exit=0）→ `84b74baa`（`pnpm gen:check`，exit=0） |
| M2（防假绿面） | 改声明（`routes.ts` 的 `wakeWord: '记运动'`→`'记运动MUT'`）**不重编** | `pnpm gen:check` 报 **GEN-STALE FAIL**、exit≠0 | §3 `runId` `da25d8be`（exit=1） |
| M2 复原 | 从 tree 拷回 → 重跑 `gen:check` | PASS | §3 `runId` `d06c6108`（exit=0） |
| M3（内容面） | 删本票一条路由记录（order 137）**先重编**（`pnpm build`）再 `pnpm gen` | `gen` 报「`list=wake` 的 `order` 不连续」、exit≠0（build 本身该绿） | §3 `runId` `059ef078`（`pnpm build`，绿，exit=0）→ `439f5850`（`pnpm gen`，红，exit=1） |
| M3 复原 | 从 tree 拷回 → `pnpm build` → `pnpm gen:check` | 全绿 | §3 `runId` `8ec7e843`（`pnpm build`，exit=0）→ `5500da00`（`pnpm gen:check`，exit=0） |

三条一起覆盖：生成物手改（免费判据的门）、源码改了没重编（`GEN-STALE` 型假绿）、**内容错**（先重编也能被记录面的连续性守卫抓住）。
按广播提醒：`gen:check` 读的是**编译后的 `dist`**，所以内容面变异（M3）必须先 `pnpm build` 再 `pnpm gen`，否则「清空 50 条也照样 PASS」。
**边界声明（本轮覆盖到哪儿为止）**：上面三条只覆盖「生成物被手改」「源码改了**没跑** `pnpm build`（`GEN-STALE` 型）」「记录面内容错（先重编再 `gen`）」；
**不含**「源码内容被改坏、但 mtime 未变 ⇒ `tsc -b` 增量跳过重编 ⇒ 内容印记记的是新源、`dist` 仍是旧产物 ⇒ `gen:check` 两面自洽」这一支 ——
该支当刻能整体绕过免费判据（**假绿**），**不在本票覆盖声明内**，已独立立票 **#325**（`gen:check` 假绿缺口）跟进，修法与复现见复核报告 §3。
**没覆盖**的：分派层残留 `case`（见 §8）。

## 6. 测试与验收探针尾部

`pnpm test`（须全绿，基线条数 1492；下面为 `.scratch/t316/gate-06-test.out.txt` 末 10 个非空行**逐字**摘录）：

```text
    ℹ tests 1492
    ℹ suites 160
    ℹ pass 1492
    ℹ fail 0
    ℹ cancelled 0
    ℹ skipped 0
    ℹ todo 0
    ℹ duration_ms 39634.2951
    LOCK-RELEASED ticket=316 runId=3e19b5c8-8f6f-4ba6-9374-303642738d15 exit=0
    RESULT: ticket=316 runId=3e19b5c8-8f6f-4ba6-9374-303642738d15 waitedMs=0 exit=0
```

`node docs/skills/skill-calorie/t293-验收-命令自治.mjs`（须 P1..P5 全绿、`UNACCOUNTED=0`）：

同一次运行（`runId=4846eab1-e3de-456b-89d4-7ec037fa936b`，见 §3）的 **P1 尾段** —— `.scratch/t316/gate-07-t293.out.txt` 第 22–29 行**逐字**摘录（`UNACCOUNTED=0 处` 在末行）：

```text
    ③ UNACCOUNTED（必须手改、既没派生也没纪律覆盖，0 处）：
    ⇒ UNACCOUNTED 为空 ⇒ 手写面已全部落在 ①/② 内
  动态：加假能力后 `gen` exit=0；生成器扫到=true（输出含 fakecap=true）
    假事实自动流进：registry=true keys(写)=true keys(读)=true combos 镜像=true REPR=true EXAMPLE 表=true
    分派两文件 + 未搬迁清单十片（legacy/scene-NN.ts）逐字节未动={"read":true,"write":true,"legacy":true}
    迁移撞键（假能力声明一条仍在未搬迁清单里的键（56 条候选）：calorie.diet.add）→ gen exit=1，报「命令键重复登记」=true
  已清理临时根：临时根已清理：C:\Users\辰辰洋洋\AppData\Local\Temp\t293-accept\sandbox-42716-1789308137930
  动态（派生桶）证据齐=true；UNACCOUNTED=0 处；树自相矛盾=0 处
```

该运行的尾部（同文件**第 66 行起**的末段**逐字**摘录；第 66 行正是判据点名的主守断言 `sport-homogeneity-109`）：

```text
    ✓ packages/skill-calorie/test/sport-homogeneity-109.test.mjs（运动/身体域 9 键同质：唤醒词命中 ＋ HTML 同质 ＋ 无假数据（含 view.weight／weight-history／weight-compare／weight-review／volatility））命中行 3,98,99,186,264
        ✓ packages/skill-calorie/test/cmd-registry-294.test.mjs（注册表 ⊇ 体重声明的键集（#322 预热：搬迁无关口径，原 /WEIGHT_COMMANDS\.length,\s*9/ 已 0 命中）＋ 每条声明形状/标题/写键表与 registry 对账）命中行 200
        ✓ packages/skill-calorie/test/cmd-write-40-persist.test.mjs（写链落库列逐列（weight.log 的 bmi 等））命中行 234,236,238,241,595,721
        ✓ packages/skill-calorie/test/cmd-write-40.test.mjs（写链 parity 抽查（体重写键））命中行 135,153,277,280,290,292
        ✓ packages/skill-calorie/test/render-t41.test.mjs（渲染锚点（ilife:calorie:weight-history））命中行 73,98,285
        ✓ test/calorie-routing-81.test.mjs（唤醒词路由 parity（冻结词表逐条恰一次，键必在键表内））命中行 25,29,78,79,80,98
        ✓ packages/skill-calorie/test/wake-assets-133.test.mjs（唤醒词素材与冻结场景表 parity）命中行 23,44
      判据：7 条**全部命中**，且守"体重产物逐条不变"的第一条（sport-homogeneity-109）必须在；本探针不自己重算一份 parity。（#322 起收紧：原口径是「合计 ≥ 5 处」，复核席 #322r 实测「删掉其中一条（含新正则指向的那条）仍然 PASS」⇒ 太松，不能当「四条新断言都在」的证明，故提到全命中。）
      守 parity 的既有断言命中 7/7（主守 sport-homogeneity-109 存在=true）
    
    RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5
    LOCK-RELEASED ticket=316 runId=4846eab1-e3de-456b-89d4-7ec037fa936b exit=0
    RESULT: ticket=316 runId=4846eab1-e3de-456b-89d4-7ec037fa936b waitedMs=0 exit=0
```

## 7. 偏离记账

1. **票面「要做的事」第 2 条的实现层部分转专票 #324**（编排者裁决 · 窗口风险控制）：本票只做**窗口 A**
   ——声明 ＋ 9 个 `case` ＋ 26 条路由记录；`render/exercise.ts`／`exercisePort.ts`／`fetch/exercise.ts`／
   `analysis/exercise*.ts` 这些**实现件**不动（它们别的场景也在用，搬会连带改调用方）。
   与本席派单一致（派单原话：只搬命令声明与分派，处理逻辑经已公开的接口调用）。→ 待复核席确认。
2. **`calorie.view.exercise-goal` 的代表唤醒词照旧缺省**：能力声明原先强制六字段全有字符串
   （`gen-cli.mjs:82-84`），而该键在旧分片里没有 `wakeWord` ⇒ 搬进能力目录会在生成期抛错，
   或被迫补一个唤醒词（那会让 `build-help.mjs` 的 REPR 多一行、还要连带重生成 `SKILL.md`，
   免费判据当场失效）。由**独立票 #323** 把「可缺」对齐到 `commandSpec.ts` 与生成器后再落点；
   本票不替产品事实决定唤醒词（六票同因，影响面由 #323 承担）。
3. **`src/cli/legacy/routes/scene-04.ts` 只搬 26 条**：记录归属＝其 `key` 的所有者
   （`docs/skills/skill-calorie/t313a-分区-证据.md`：`calorie.view.exercise` 属场景 01 主页），
   故那 19 条留给 #314。该文件在本票窗口内被编辑（属别人场景的残留记录），已在影响清单里点名，
   编排者 2026-09-13 裁决「窗口内你是唯一写者，允许动」。`non-exec` 无 `key`，按 `scene` 归属＝本票。
4. **就摆正 1 行注释**：`cmd_read.ts` 的 `// #111 · 运动移植 6 键（t71 需移植 exercise_*…）` 在 5 键
   搬走后已不准确，就地改成「其中 5 键已随 #316 搬进 `src/exercise/`，本处只剩计划复盘那一条」。
5. **票面第 4 条（`test/cmd-write-40-persist.test.mjs` ＋ `docs/research/t97-probe-receipts.mjs` 各补一处）
   不需要动**：那两处是「新增写命令」时补落库断言／回执探针的口径；本票三个写键**既有**且两处
   已有逐键覆盖（`test/cmd-write-40-persist.test.mjs:282-350, 563-575`；`t97-probe-receipts.mjs:100-102`），
   键集由 `test/declared.mjs` 派生（`assert.deepEqual(WRITE_KEYS, DECLARED_WRITE_KEYS)`），
   纯搬迁不改键集 ⇒ 两处零改动即绿。
6. **两个共用分派文件改「定点删除」**（编排者裁决 §3）：预备期 tree 快照里对 `cmd_read.ts`／`write.ts`
   的整文件成品已作废并删除；落点窗口改用 `.scratch/t316/edit-dispatch.mjs` 对**活文件**做同一组手术
   （删 6 读 ＋ 3 写 `case`、搬走 `EX_CAMEL`／`oneExercise`、收窄 4 处 import、摆正 1 行注释），
   每条动作落 `land.log`；命中 0 次记 `ALREADY-GONE`（别人票若先动过不误伤），>1 次即抛。
   路由分片 `legacy/routes/scene-04.ts` **同样是共享件**（#314 要搬的 19 条也住它）⇒ 一并走定点删除：
   以「刚复制进树的 `src/exercise/routes.ts` 那 26 条记录」为名单逐行删，别人键的记录一行不动。
   守卫口径照精修后的规则：**hard**（`src/exercise/**` ＋ `legacy/scene-04.ts`）不等即 `TRIPPED` 退 1；
   **soft**（`cmd_read.ts`／`write.ts`／`routes/scene-04.ts`）不等只记 `SHARD-DRIFT`。
   落点后另加一条自检：两个分派文件**行数只减不增**（不减反增即 `ROLLBACK` ＋停手报告，挡「整段复活」）。
7. **过程缺陷自曝（S3 记账，编排者已独立核实）**：预备段我把 `node .scratch/t316/edit-dispatch.mjs --help`
   当成「测参数」跑了，而那时脚本**没有 dry-run 开关**，于是它按设计改了**活树**的
   `packages/skill-calorie/src/cli/cmd_read.ts`／`write.ts`（违反「预备段不碰树」）。当刻立刻**逐文件**
   `git checkout -- packages/skill-calorie/src/cli/cmd_read.ts packages/skill-calorie/src/cli/write.ts`
   回退（协议禁的只是 `-- .`／`restore .`／`stash`／`reset --hard`／`clean`／切分支），并复核两件 blob
   指纹＝HEAD：`cmd_read.ts` `a0244eaf…`、`write.ts` `e3c355ea…`（编排者独立核实同值 ⇒ 零损坏）。
   当刻树里只有 #323 的两件未提交改动（`scripts/gen-cli.mjs`／`src/shared/commandSpec.ts`），
   与本票这两件无关。**补偿控制**：`edit-dispatch.mjs` 改为**默认 dry-run**（不带 `--write` 时只打印
   将要删的行与改后行数，绝不写盘），`land.ps1` 里显式传 `--write`；本票不再有「随手跑一下就改树」的入口。

## 8. 未确证／风险 top3

1. **「分派层残留 case 删净」目前只有机械清点与行为等价支撑，没有一道门会为它单独变红**：
   `.scratch/t316/build-tree.mjs` 断言产出里 `case '<键>'` 命中 0 次；`t293` 的 P4 只遍历体重 9 键，
   P1 的静态面不看 `case` 条数 ⇒ 若有人把某个 `case` 手工加回来，本地门禁未必红（注册表先行会让它
   静默成为死代码）。建议复核席用「加回一条 case → 跑 `pnpm test` ＋ t293 看是否变红」独立验一遍。
2. **实现层（`render/exercise*.ts`／`exercisePort.ts`／`fetch/exercise.ts`／`analysis/exercise*.ts`）本票不搬**
   ——编排者已把票面第 2 条那部分转出成专票 **#324**。待复核席确认这条转出成立（若不成立，本票范围要重开）。
3. **`pnpm help:examples:check` 未跑**：规则书 §三 的门禁链里没有它，且它今天带票外既有红
   （`docs/skills/skill-calorie/t313a-分区-证据.md:461`）。本票的 9 条 `example` 逐字照抄自旧声明，
   未新增示例。

GATE-RELAX flag=--allow-nonzero reason=§5 的 M1／M2 是**按设计故意打红**的变异自证（exit≠0 是探针预期结果，不是交付门禁失败），故对账时必须允许非 0 退出码的条目被 GATE-RUN 声明认领。

## 9. 返修窗基线绿与 dist 印记（编排者硬规则 24／26）

- 返修窗 BASE `e9dc6523`（测量时刻见 §1 段首）。**动本票文件之前**先做基线绿：
  `node node_modules/typescript/bin/tsc -b` → `node packages/skill-calorie/scripts/gen-cli.mjs --stamp` →
  `node packages/skill-calorie/scripts/gen-cli.mjs` → 同件 `--check`，四步全绿 —— `dist/` 被 `.gitignore` 挡住，
  别人半迁移的残留（含 `dist/.gen-inputs.json` 印记）在 `git status` 里看不见，故先重编再拿判据。
- **窗口内一律不用 `pnpm <script>`（硬规则 26）**，也**不用仓根 `tsc -b --force`、不删 `dist/`**（硬规则 24 终版：插件包的
  `tsconfig` 与 `tsdown.config.ts` 同 `outDir`、产物同名 `dist/client.js`，靠 tsdown 后跑取胜；仓根 `--force` 会用裸 ESM 原地覆盖它）。
- **过程记账（如实）**：前一轮返修空跑（`runId` `f2c39719`，2026-09-13）在测试门出现过 **4 条票外红** ——
  `dsh-bill-ilife client` 的 3 条（`classic 执行并注册自身 id`／`factory 可物化`／`产物无 ESM 语法`）＋ `#204 ①` 墙钟秒边界 1 条；
  根因是当轮用了仓根 `tsc -b --force` 覆盖 `dist/client.js`。补回 client 产物、改用「不带 `--force` 的重编」后，本窗回到 **1492/1492**（见 `.scratch/t316fix/gate-05-test.out.txt`）。
- `packages/skill-calorie/dist/.gen-inputs.json` 的 sha256：**开门** `92f7d45b1beb98a1cb05dbf40f5264dc46f9c714ce0647fafe107ee174ce78ea` → **收尾** `74f22c2886d56cddf9c3e742edb2f8335e06e4b4ee2cad65cdded4aec0df1ae1`（2026-09-13T23:54:46+08:00）。
- 本窗另跑 `node tooling/skill-html-snapshot.mjs --check` 并留档（`.scratch/t316fix/gate-07-snaphtml.out.txt`）；该门票外既有红只记账，不判本票不合格。
- 本窗全部写集只有两件：本证据件 ＋ `packages/skill-calorie/src/exercise/routes.ts` 的注释指针（无其它改动）。

## 10. 补正窗记账（§1 净减归因补正 ＋ 测试门本窗一次即全绿）

- 补正窗 BASE `c3662700`（2026-09-14T00:24:50+08:00）：只改**本证据件文本**三处，不动源码、不动行为 ——
  ① **§1 净减归因**（本席自查所得）：474 行不是 #315 一票所致，按 `git log a9fb35c..<BASE>` 实测是 **#315 `86786e8`（`cmd_read.ts` 799→653、`write.ts` 466→223）＋ #319 `cbb5da8`（`cmd_read.ts` 653→474，`write.ts` 未动）**两票叠出来的；
  ② **复核报告「新 S3-①」**（as-of 时差）：§1 的 `routes.ts` 值标明它是 **BASE 锚点 as-of**（44），并写明返修窗给它加了 1 行注释 ⇒ 提交后为 **45**；
  ③ **复核报告「新 S3-②」**（末段摘录漏行）：§6 的 t293 末段摘录改从 `gate-07-t293.out.txt` **第 66 行**起，把判据点名的主守断言 `sport-homogeneity-109` 那一行补进引文。
- 测试门按编排者**硬规则 30**（红后同树原样复跑）执业，**本窗一次即全绿**：本轮实际只跑 `05-test`（exit=0，无红点），采用 `gate-05-test.out.txt` 的汇总：
  `tests=1492 pass=1492 fail=0`。⚠️ **更正（收口批量核销）**：本件原写「两次输出都留档在 `.scratch/t316fix/`，按规则 30 判为 flake，不作真回归」**与事实不符** —— **两次同红**发生在**更早两个未通过的补正窗** `050cf4ca`／`b933a587`，且硬规则 30 对它们的判定是**真回归／停手上报**，**不是 flake**；本补正窗（BASE `c3662700`）的测试门**从第一次起就是全绿**，不存在需要按规则 30 处置的 flake。
- `node tooling/skill-html-snapshot.mjs --check` exit=0（票外既有红只记账）。
- 印记 sha256：开门 `cb41eccd4f8946761644defd4dfaa23b70a318c6272640e00fcadcb901f1f2f5` → 收尾 `cb41eccd4f8946761644defd4dfaa23b70a318c6272640e00fcadcb901f1f2f5`。
- 补正窗同样守：窗口内不用 `pnpm <script>`、不用仓根 `--force`、不删 `dist/`；收尾 `git status --porcelain -uall` 为空。
