# T351 对抗审查派单（编排者预置稿）

> 用途：实施兵交付后，由**独立审查兵**执行。审查兵与本单的实施者不得是同一人；作者自评不算复核。
> 把本文件当派单正本读，不要只看摘要。

## 0. 你是谁、审什么

你是**独立对抗审查兵**，工作目录 `D:\ilife`。审的是 wayfinder 地图 `#157`（卡路里场景 05 健身计划）里 T351 这一轮的三件交付：

1. **复制区收口**：186–195 段的写前预览页／回执页，底部从「`复制数据 ▾` ＋ text/json/csv 三格式菜单」改成负责人要求的「复制数据／复制日志」冻结双按钮（`id="ilife-copy-data"`／`id="ilife-copy-log"`，中文单语、无格式菜单）。
2. **读命令缺失阻断回归修复**：`workout/plan.ts` 的 `viewPlan` 曾用 catch 把 `missing-data` 吞成 exit 0 空态页（提交 `5255b01` 引入），现已恢复照旧抛（exit 4），`render/planDeleteVerifyDocs.ts` 随之删除。
3. **全量重跑 176–207** 到 `.scratch/t351-fix/final-v3/`，逐份机检四条。

## 1. 先读（别整仓乱找）
- `docs/agents/编排纪律.md`（地图执行者十一条）
- `docs/subagent-concurrency-protocol.md` §3.2、§5、§5.1、§6（尤其 §5.1 审查者纪律：独立复跑／自设新探针／归属标注／报告形状）
- `docs/agents/wording.md`（报告一律用规范词：命令／跑得通／缺失阻断／结果型／过程型／回执／预检确认页／公共层；**禁**「键／打通／闭环／落地(作实施讲)／壳／渲染层」）
- `docs/skills/skill-calorie/t351-recon-20260914.md`（编排者的勘查记录：缺口实证与回归归因）
- `docs/skills/skill-calorie/t351-redesign-184-spec.md`（176–185 版式规格）

## 2. 硬纪律（违反即判 S1-过程违规）
- **不采信实施者的任何结论**。每句「已绿／已满足」你都要自己跑一遍。
- **必须自设 ≥1 个被审脚本覆盖不到的新探针**（专打「脚本自我满足」的盲区）。没有这条，你的结论不予采信。请说明你的新探针打的是哪个盲区。
- **不许改** `test/**`（冻结判据）、`packages/base-render/**`、`packages/base-paint/**`、他席在途件。
- 编译／测试／`git add`／`commit` 一律持锁：`node tooling/run-locked.mjs --ticket 351 --run-id t351-review-<你能认出的短名> -- <命令>`。
  **当前门禁锁拥塞严重**（十几个席抢同一把锁，实测一条命令等过 130 秒），所以：长命令一律落盘＋等标记行＋只读尾 5 行；一次等不到就按包装器自己的重试口径再等，**不要因为慢就改成裸跑**。
- 长命令输出落盘到**你自己的目录**（建议 `.scratch/t351-review/`），只读尾 5 行与退出码，**不要把大日志读进上下文**。
- 危险 git 命令禁用（`stash`／`reset --hard`／`clean`／`amend`／`rebase`／`switch`）。
- 提交只 `git add` 你自己声明的件，提交前 `git diff --cached --name-only` 自查；若用 `git commit`，**必须带路径限定** `git commit -F <msg> -- <你的路径>`（不带限定会把暂存区里他席的件一起提交——本轮编排者已犯过一次并已纠正）。

## 3. 要你证伪的具体断言（逐个给机器读数）

### 3.1 「186–195 段的复制区已经对了」
- **正**：每份 186–195 产物的正文里 `id="ilife-copy-data"` 与 `id="ilife-copy-log"` 各**恰好一次**（注意 `id="x"` 是 `data-action-id="x"` 的子串，计数要排除后者）。
- **反**：正文里**不出现** `class="ilife-copy-menu-wrap"`、`复制数据 ▾`、`data-fmt-open`、`data-fmt="text/json/csv"`、`text`／`json`／`csv` 三个菜单标签。
  （注意：共享样式表与页面运行脚本里**恒有** `.ilife-copy-menu*` 类名，那是 `base-paint` 自带、未启用，**不算违规**。判据只针对**正文**。）
- 请你自己造一份**反例**证明你的探针有识别力：把某份产物正文里的冻结 `id` 删掉、或塞回一个 `ilife-copy-menu-wrap`，你的断言必须变红；还原后必须变绿。给「变异红／还原一致」两行读数。

### 3.2 「读命令的缺失阻断真的恢复了」
- 独立复跑：`node --test test/scene05-write-mutate.test.mjs test/scene05-write-create.test.mjs test/scene05-read.test.mjs` 三支全绿（持锁）。
- 独立取证：空库上调 `calorie.view.plan` 应 **exit 4** 且 stderr 命中「无训练计划」；**有**计划的库上应 exit 0。
- **新探针建议打这个盲区**：实施者的脚本只断言了「撤销后 exit 4」。你去打**「有计划时不能也 exit 4」**这个反向面——若恢复缺失阻断时误伤了正常路径（例如把有计划的库也判成缺失），实施者的断言**抓不到**。请给真例假例各一。
- 另验：`render/planDeleteVerifyDocs.ts` 已删除且**无任何悬空引用**（源码、测试、生成物、脚本全查）。删件后若还有地方 import 它，`pnpm test:types` 会红——自己跑一遍 `pnpm test:types`。

### 3.3 「机检四条逐份都过了」
- 四条的**口径本身**是否被偷偷放宽？逐条读实施者脚本（`.scratch/t351-fix/final-v3/run-176-207-v3.mjs`）里的断言实现，指出任何「自己给自己开后门」的写法。
- **口径是最该审的地方**（合并点安静态复核的核心维度＝收窄有没有变成放宽）。特别看：
  - 「无三格式菜单」是否只在正文判、且 token 列表够不够（漏一个就形同没判）；
  - 「六列表头」在缺列时会不会被判成 NA 从而永远绿；
  - 冻结 id 的计数排除逻辑是否真的排除了子串。
- 自造一份**权威写死的假产物**（真值你手写，不由被审脚本生成）验证机检会红。

### 3.4 两个已知陷阱（编排者在 r1 重跑里已判明，你要独立复验）

**(a) 机检②「冻结 id 各恰好一次」曾报假红，别被它带跑。**
真相：`data-action-id="ilife-copy-data"` 里含子串 `id="ilife-copy-data"`，朴素计数得 2，产物其实是对的（真 id 恰好 1 个）。要用负向后顾 `(?<!data-action-)id="..."` 或先抹掉 `data-action-id="..."` 再数。
**重点审这里**：实施者有没有为了让计数变成 1 而**删掉 `data-action-id`**？那是复制按钮的动作钩子，删了就成真回归。逐份确认两个属性都在。

**(b) 10 份过程页的复制数据标题曾是英文命令键。**
r1 实测：`order186-process.html` 的标题是 `calorie.view.plan-wizard`，`order187–195-process.html` 是 `calorie.view.plan-write-preview`，其余 27 份都是中文（训练计划查看／计划对比实际／定训练计划／…／撤销训练计划／计划复盘／禁忌扫描）。负责人明令中文单语、不许出现英文命令键。
**你要独立验**：37 份逐份抽 `【calorie · …】` 的标题，**任何一份出现 `calorie.` 开头的英文串即判缺陷**。另加一条自己写的机检：正文命中 `/【calorie · calorie\./` 即红。
（注意：复制**日志**里的 `call_chain` 本该是命令行原文，那里出现 `calorie-cmd-read …` 是**正常**的，不算违规。判据只针对载荷**标题**与页面可见文案。）

### 3.5 还要你判的一条
实施者的 `check ④`（六列表头）在「页内没有『组数×次数』」时记 **NA**。请判定 NA 是否成了**永远绿的通道**：如果一份本该有动作明细的页因为渲染坏了而没有表头，它会不会被记成 NA 从而蒙混过关？给出你的判定与证据。

### 3.6 产物与源码的**生成顺序**（本轮已出现过的失效模式，重点查）
r2 重跑（18:24:28）**早于**标题中文化的源码改动，于是 r2 的 37 份产物里 10 份过程页仍带英文命令键标题，而实施者脚本报 `50/50 全绿`——**绿灯是虚的，因为脚本里没有这条检查**。

你要独立确认：
- 交付的 37 份产物，**每一份的 mtime 都晚于**这几件的最后一次改动：`src/render/workoutPlanDocs.ts`、`src/workout/receipt.ts`、`src/render/planPlate.ts`、`src/shared/copyArea.ts`、以及任何被改过且参与装配的件。给出「产物最早 mtime vs 相关源码最晚 mtime」两行读数。
- 若产物早于源码，**这份交付不成立**（生成一致性不为绿），判 S1 或至少 S2，并在回执里点名。
- 顺带查实施者的判据表里**有没有覆盖「载荷标题不许出现英文命令键」**。若没有，这本身就是交付缺陷（负责人明令过的口径，机检里必须有）。

### 3.7 过程型页面必须带可复制的 prompt（本轮新增的功能缺口）
依据：`SKILL.md` 明写「出页后**由用户复制 prompt** 回给 AI」；`shared/copyArea.ts` 的 `promptCopyArea` 标签逐字「复制 prompt（**必走**）」；本仓其余全部预检确认页都摆了它（`goal/precheck.ts:192`、`profile/setup.ts:328`、`body/wizardDocs.ts:87,152`、`render/wizardPortDocs.ts:58,125`）。唯场景 05 的 `dualCopy()` 原本没传。

**你要独立验**：
- 10 份过程页（`order186-process` ＋ `order187–195-process`）正文里**都**含该词 prompt 的逐字原文（至少断言含 `执行唤醒词「<该词>」` 与该词独有的一句，如「复制训练计划」含 `我想把现有训练计划复制一份作为模板`）。
- 结果页（176–185、201–207）与回执页（186–195-receipt）**不得**出现 prompt 块——它们不是预检确认页。多出来的也要判缺陷。
- prompt 的原文必须与 `src/triggers/scene-05-workout.ts` 里该词的 `prompt_template` **逐字相同**（自己从触发器数据取原文比对，不要采信实施者脚本里的字符串常量）。
- 另确认**层级没倒置**：`src/render/**` 不得出现对 `src/triggers/**` 的 import（照 `goal/read.ts:116` 的先例，取 prompt_template 的活应在命令／取数层）。用 `git grep` 给出读数。

### 3.8 用词瑕疵：撤销回执的状态卡副行（编排者发现，请你独立判级）
`order195-receipt.html` 正文实测（去标签后的可见文案）：

> … 状态 **已改动** **已写入训练计划** 影响行数 5 行 …

出处 `src/workout/receipt.ts`：`statusCard(receipt, '已写入训练计划')` 的第二参是个**恒定的兜底文案**。对 10 条写命令里那 9 条增改类是贴切的，但 `calorie.workout.plan-delete`（撤销整份计划）下面同一页又写着「写后现值：计划已撤销」「改动对照：已删除（硬，不可恢复）」——同一页对同一动作一边说「已写入」一边说「已删除」。

**请你判**：这是不是负责人会挑的用词毛病（他对用词有专门纪律文档）？若判是，给出建议修法与级别（我倾向 S3 记账跟进或 S2 关闭前修，你定）。判据：兜底文案是否该随 op 变（撤销类给「已删除训练计划」，其余保持）。
另请顺带核：**回执页不许出现 `已写入` 与 `已删除` 并列的语义冲突**这一条，值不值得进机检。
### 3.9 交付目录有没有被变异产物污染（本轮实测存在的风险）
实施兵的变异自证是**在交付目录 `.scratch/t351-fix/final-v3/` 原地跑**的：变异期间（源码被故意改坏，实测摘掉了给数据按钮补冻结 id 的那行 `.replace(...)`）它照样重跑了一遍，**把变异产物写进了 `final-v3`**。实测读数：那一轮 30 份的 `id="ilife-copy-data"` 缺失、6 份正文带三格式菜单。

**你要独立确认交付的 37 份是「还原之后」跑出来的**：
- 逐份：`(?<!data-action-)id="ilife-copy-data"` 恰好 1、`id="ilife-copy-log"` 恰好 1；正文无 `data-fmt-open`／`class="ilife-copy-menu-wrap"`／`复制数据 ▾`。
- 时间线：产物 mtime **晚于**「源码最后一次还原」的时刻，且**晚于**变异轮（变异轮日志见 `logs/351-rerun-MUTATED.log`）。给出两行读数（变异轮产物读数 vs 交付轮产物读数）。
- 若交付目录里混着变异轮产物、或证据件引用的读数取自变异轮，判 **S1**（证据不实）。这条是本轮最该抓的地方。

## 4. 五个维度打分（供参考，判定＋缺陷清单才是正文）
契约一致 30 ／ 证据真实可复现 25 ／ 新旧对照 20 ／ 工程红线 15 ／ 文档同步 10。
量刑：任一 S1-交付缺陷 → **FAIL**（分数再高也否决）；均分 <85 → FAIL；无 S1 且无未记录的过程违规且均分 ≥85 → PASS。

每个缺陷必须标注归属：**本票引入／本票范围／范围外发现**。范围外发现不得单独决定 FAIL，除非它直接证伪本票的验收结论。

## 5. 回执形状（上限 1200 词，缺格与超长都不算交付）
1. 第一行判定：PASS／FAIL／未完成
2. 机器证据段：提交 sha、运行标识（`GATE-RUN runId=`）、退出码、摘要行（`RESULT: n/n`）、你新探针的「变异红／还原一致」两行
3. 逐条路径：你动的件、你写的证据件、日志件
4. 未做项与下一手缺什么

证据件落 `docs/skills/skill-calorie/t351-<你的角色>-review.md` 并进版本库。完成后停止，等编排者指派。
