# t380 · 锁证据（A-list 88 条真出口全量程序锁）

> 票：#380（地图 #160）。用词照 `docs/agents/wording.md`：命令／跑得通／过程型／结果型／回执／页面交付。
> 数字以本文件所贴机器读数为准，不另存第二份清单。视觉判定（判定③）留 #381 用户肉眼，本文件只给程序绿（判定①②）。

## 0 · 结论先行

- A-list 全量 88 条（combined 71＋整体趋势 15＋对照 2）逐条有读数：持锁真出口 `TRUE-OUTLET: 95/95`（88 用例＋6 负向探针＋1 固定种子复算），`CHECK: PASS`。
- 判定①（落盘／绝对路径／可打开／字节如实，照 139）与判定②（包络 stat／指标全有限数／输出逐字消费输入，照 139＋134）全绿；真坏＝0。
- 变异自证 `MUTATE-SELF-PROOF: PASS`（改坏断言针必红 exit 1／还原必绿 exit 0／逐字一致）。
- 本票只加 2 件（用例＋本证据件），`src/` 零改动；用例 174 行（LF、无 BOM、无 CR），未超 350 线。
- 进度 95%，不 close（待确认见 §8）。

## 1 · 诊断（开工前）

- `Blocked by: #377, #378`：两票均 OPEN 95% 条件开工——#377（combined 抽样 15/15，证据 `t377-配对页-证据.md`）＋#378（翻转 14/14、真出口 17/17，证据 `t378-趋势铺开-证据.md`）；开工前复核 `flip-all.mjs --check` 仍 `FLIP: 14/14`、`A-FIXED: 88`、`CHECK: PASS`。
- `.scratch/t380` 开工前不存在（已建，内有 `dump-envelope.mjs`／`probe-neg.mjs`／`probe-contrast.mjs`／`wrap.mjs`（一次性）／`mutate-self.mjs`／`full-all.log`，不入仓）；`docs/skills/skill-calorie/t380-*` 开工前不存在（本文件为首件）；`packages/skill-calorie/test/t380-*` 开工前不存在（用例为首件）。
- #380 认领：开工前 assignees 为空，`gh issue edit 380 --add-assignee "@me"` 后为 FeatherHunter，他人未占，不停手。
- #171 CLOSED（复算 TOTAL 169／A 88／B 81／unassigned＝0）；#376 OPEN 95%（先验形状只读复用，不改口径）。

## 2 · 用例设计（判定①②，不加码）

- 入仓用例：`packages/skill-calorie/test/t380-真出口.mjs`（174 行；独立脚本，非 `*.test.mjs`，不进全量 `--test` glob）。
- A-list 口径（A-FIXED）：`src/analysis/routes.ts` 中 key 为 `calorie.view.combined`／`calorie.view.multi-trend`／`calorie.view.long-trend`／`calorie.view.calorie-trend` 的全部；断言 `TOTAL＝169`、`A-FIXED＝88`、A-list 全 exec——任一漂移即红。
- 判定①（照 139）：exit 0／stdout 一行 JSON（P9）／version 0.1.0＋shape stat＋key 一致／`data.output` 绝对路径／落盘可读非空／落盘字节＝`delivery.bytes`／`delivery.path` 与 output 同源／完整文档（doctype）／复制区在。
- 判定②（照 139＋134结构面）：`metrics` 全有限 number；输出逐字消费输入——combined 须含 pair＋window（custom 含起止两日）；multi-trend 须含 window＋group（非 comprehensive）＋compare；long-trend 须含 group＋key；calorie-trend 须含 key＋窗口表单（对照页不回显原始窗口字样，见 §6）。
- 固定种子：`docs/research/t81-seed.mjs`（`seedFull`）＋`CALORIE_TODAY=2026-09-07`＋每用例隔离 tmp 库；`--all` 加跑确定性复算（首用例同一种子跑两次同 sha）。
- 负向 6 条（大声非 0，防 #154 式静默回退）：未知配对／非法窗口／越界 4000d／非法 group／非法 compare／非法趋势窗，针分别为“未知配对”／“window 非法”／“group 仅支持”／“compare 仅支持”。
- 字节／sha 只记录不钉死（理由见 §6）；用法：`--all` 全量 88（验收用），缺省冒烟 6 条。

## 3 · 真出口读数（固定种子库，持锁 `--ticket 380`）

- 命令：`node tooling/run-locked.mjs --ticket 380 -- node packages/skill-calorie/test/t380-真出口.mjs --all`（runId `94aa90c3-79b3-4e21-8dbf-552b03047d4a`，锁等待 250018ms，exit 0；全日志 `.scratch/t380/full-all.log`）。
- 汇总：`TRUE-OUTLET: 95/95`（88 用例 OK＋6 负向大声＋1 确定性复算 OK），`A-FIXED: 88 (TOTAL 169)`，`CHECK: PASS`。
- 代表行（全量 88 行见日志；字节系当刻工作区形状，跨票漂移见 §6）：

| 唤醒词 | 字节 | sha256 前 16 |
|---|---|---|
| 看体重 vs 摄入(最近 7 天) | 74120 | a77bb9582937d769 |
| 看体重 vs 摄入(最近 15 天) | 77992 | d186ba36d9fae207 |
| 看组合分析（同 cli 复用） | 74120 | a77bb9582937d769 |
| 看整体趋势(含月度对比) | 115228 | 52072e7039c75a21 |
| 看整体趋势(含季度对比) | 212714 | c2b0331f9b540912 |
| 看整体趋势(含年度对比) | 583679 | 416a2b92181a4902 |
| 看整体趋势(含目标对比) | 139508 | bb38848efc0b038f |
| 看热量趋势 | 65059 | 0aa0209e33a7eed6 |
| 看整体趋势 | 81809 | fc79a78aec2d7559 |
| determinism 首用例双跑 | 同 sha | a77bb9582937d769 |

## 4 · 变异自证（新断言改坏必红）

- 脚本：`.scratch/t380/mutate-self.mjs`（只碰本票用例文件，不碰 `src/`；try/finally 全程复原）。
- 读数：`MUTATE-RED: exit=1`（断言针“复制数据”改坏后冒烟必红）／`MUTATE-GREEN: exit=0, restore-identical=true`／`MUTATE-SELF-PROOF: PASS`。
- 落盘①改坏必红：`MUTATE-RED: exit=1`（断言针“复制数据”改坏后冒烟必红，`MUTATE-RED-CHECK: RED-OK`）；持锁上下文 runId `94aa90c3-79b3-4e21-8dbf-552b03047d4a`，全日志 `.scratch/t380/full-all.log`。
- 落盘②还原必绿逐字一致：`MUTATE-GREEN: exit=0, restore-identical=true`（try/finally 还原后冒烟必绿，还原前后逐字一致）／`MUTATE-SELF-PROOF: PASS`。

## 5 · 门绿读数

- t380 全量（持锁）：`CHECK: PASS`（§3）。
- 本票区行数／编码：用例 174 行（LF 口径 `split('\n').length-1`），LF、无 BOM、无 CR；`src/` 无改动故五铁律无新增违反面，350 线未触发不报警。
- 全量套件（`node --test`）：本票未重跑——工作区多票并发累积红，留收口复跑（沿 #378 §5 口径；待确认①）。

## 6 · 如实记录（漂移与口径诚实项，非本票红）

- 先验字节漂移：#376 证据钉 139438（sha `8cbfaea2…`），本票当刻同命令 139380～139519 系（§3 目标对比 139508）。归因：工作区多票并发改动渲染件（`trendDocs.ts`／`copyArea.ts` 等，他人件，本票不动）；结构断言全绿，页面语义未坏。故本锁钉结构（①②）不钉字节——字节逐次记录备查。
- 渲染件哈希（一行）：`git log --oneline -1 -- packages/skill-calorie/src/analysis/multiTrendPage.ts` 无输出（该件未入仓）；就近已入仓渲染件 `c9c836f feat(351): 201-207双按钮补id并落证据`（`src/render/trendDocs.ts`）／`6636201 feat(370): 主页装配与视图模型按子功能归位（纯搬迁，指纹5/5全等）＋ homeDocs新建/today改线/html删段/t370证据`（`src/render/html.ts`）／`2e6bf81 fix(skill-calorie): #238 场景 07 五张页文案与信息摆法返修（技术词／英文枚举／同名复制标题出页）`（`src/shared/copyArea.ts`）。
- 对照页口径：`calorie-trend`／`long-trend` 结果页不回显原始窗口字样（`7d`／`30d` 无命中），但含 key＋窗口表单／group；用例按实际回显断言（§2），不虚构不存在的针。
- 分类器启发式：`classify.mjs --check` 仍 `CHECK: PASS`（TOTAL 169／A 73／B 96系启发式不认 multi-trend，见 #378 §7）；权威计数以本用例 `A-FIXED: 88` 为准。

## 7 · 交付对账（#380 票面）

| 票面声明 | 本票实际 | 偏差 |
|---|---|---|
| 用例 `packages/skill-calorie/test/t380-真出口.mjs` | 新建 174 行 | 0 |
| 证据件 `docs/skills/skill-calorie/t380-锁-证据.md` | 本文件 | 0 |
| 不改命令与页面（坏了转票不修） | `src/` 零改动（`git status --short -- packages/skill-calorie/src/` 内本票无行） | 0 |
| 不写视觉判断脚本；不加码字段断言 | 只有①②程序断言，无视觉脚本；字段以 #171 为准 | 0 |
| 不动 B 的词；不动生成物 | 未碰 B／未跑 `pnpm gen` | 0 |
| 只 `git add` 自己件；持锁 `--ticket 380` | 见 §8 | 0 |

## 8 · 进度与待确认（不 close）

- 进度 95%。
- 待确认（未确认不 close）：① 全量套件 exit 0（收口复跑；本票区门已绿）；② 提交范围裁定（本票拟只交用例＋本证据件两件，已持锁提交见门日志；生成物未动）；③ 与 #377／#378／#379 的 close 顺序（本票被 377＋378 阻塞，两票 close 后本票再 close）；④ 用户肉眼（判定③）归 #381。
- MAP 同步：本票不改 #160 正文；MAP Decisions 备一行见 §9（验收过后再回写）。

## 9 · MAP Decisions 备一行（验收过后再回写 #160）

`#380 95%：A-list 88 条真出口程序锁全绿（持锁 95/95，判定①②过，变异自证 PASS，src 零改动）；待收口复跑全量套件与 377/378 close 后再 close。`
