# t341 · 看身材照页面形状（完整文档＋内嵌照片＋复制区）

## 形状

- 出口：`calorie.photo.list`（看身材照）的 `html` 产物＝完整文档（`<!doctype html>` 起、
  `<meta charset>`、版面、复制区），不再是 `<section>` 片段。
- 取数：仍是 `src/photo/photo.ts` 的 `buildGalleryData`（筛选／计数／距上次，口径不变）；
  呈现改走本能力内 `src/photo/galleryDoc.ts` 的 `buildPhotoListDoc`（KPI＋内嵌照片＋缺失明示＋
  明细表＋`dataCopyArea('复制数据', …)`），经共用件 `assembleDocPage` 包成整页。
- 内嵌：`src/photo/photoThumb.ts`（`embedPhoto`／`embedPhotos`）：按扩展名定 MIME、
  读文件、拼 `data:<mime>;base64,…`；单张超 4 MiB 即不嵌（回缺失句）。住本能力目录，
  第二个用法出现才上移共用位（票面口径）。
- `data` 信封不变（`{items[{id,date,photoPath,tagList,fileExists}], total}`，`list` 形守卫照旧）。

## 体积上限（本票首定，供 281／282／352 复用）

- `PHOTO_LIST_PAGE_MAX_BYTES = 1048576`（1 MiB，住 `galleryDoc.ts` 并导出，测试直接引用）。
- 依据：种子 3 张 1x1 PNG 的实测页约 2 KB；1 MiB 容得下几十张实拍缩略图，
  又拦得住“原图无脑全嵌”的体积爆炸。超限即 `photo-shape-341.test.mjs` 红，不静默放宽。
- 复用：281／282／352 的页面直接引用同一常量（或在各自文档注明沿用本值），上限变更须同步改三票文档。

## 缺失口径

- 单张失败（无照片目录／文件不在／过大）一律回缺失句、不抛（`embedPhoto` 契约）。
- 页面明示：缺失块写 `缺失照片 N 张：#id 文件名（原因）`；每张 `figcaption` 另带 `文件缺失` 位；
  正常照片不受牵连（仍内嵌 `data:image/`）。
- 示例：删掉 `2026-09-04_001.png` 本体后页面含 `2026-09-04_001.png`＋`缺失`（用例已钉）。

## 基线数字（十页，规范化正文 sha256，改前落、改后目标页更新＋其余 9 页不变）

改前（`gen-photo-baseline.mjs --write`，种子库＋种子照片＋固定时钟，`--check` 10/10）：

- `calorie.help.center`（q 现找）：`0a0132d335b129a57c9fc15d2bcdcc998de7cbfb93784308ecd9c849f81a070f`
- `calorie.photo.add`：`fcc55990964664e10433824cc5a1fd723cb4827d4dea6dedabe0e5eae1fd5a61`
- `calorie.photo.compare`：`deb413fde06aed08900081356108d9c51c185db7b945e5ce3699cf7eb970432b`
- `calorie.photo.detail`：`dfc49b927eb7c6b7b7abeeaff2fa1096864c2c6efbfcaf64c14f7bd2789310d6`
- `calorie.photo.gif`：`c7869012198940e69a365aba79b8349ea48302a4379947ad412d7116dbca1827`
- `calorie.photo.list`（目标页，改前片段）：`920da2e54f06b75900330961865539d589a650a82a13da09a3a1deee02248dab`
- `calorie.photo.remove`：`31508df6dd87213729e0a9c9f477b098f37e4542ec793fa685a7c498ffef7397`
- `calorie.photo.tag`：`c87ebf61d83424b8df10ba27b56b9148d0318249274e2025dfa893ffb464ee8a`
- `calorie.view.gif-planner`：`ccfaf619c44b92f3bbbcd3d0696631b0302c0ecb1eb5c014e82f5dd13669172e`
- `calorie.view.photo-log-wizard`：`84315cd16883b0fcc8b8e9bf2634c4ba76080b768d989c1e0dbb88e5c2b86f9b`

改后：目标页 `calorie.photo.list` → `856e37c368cd268c7ecc817b705b8153fc5e96c5361800e27bff914c3307df77`
（完整文档＋内嵌），其余 9 页逐字不变（`--check` 证据见回执）。
完整 64 位值以 `gen-photo-baseline.mjs` 体内 `BASELINE` 常量为准（清单住脚本体内，不另落文件）。

## 不稳定单列

- 本轮十页两次连跑全绿（`--check` BASELINE-OK 10/10），无本就不稳定的页，无单列。
- 若后续某页出现时间戳／随机序不稳定，当场记进本节并把它从基线单列，不静默放宽。

## 超线报告（当场报拆法，不悄拆）

- `packages/skill-calorie/src/render/html.ts` 实测 680 行（LF 口径，票面 589 行已过期；
  包内告警线 350 行）：已超线，需要根据规则进行重构。
- 超因：T8／T9／T10＋体重／身体／计划等全域模板同处一件。
- 拆法（本票不拆）：按域拆成 `photoHtml.ts`／`weightHtml.ts`／`goalHtml.ts` 等，
  `html.ts` 只留 `pageShell`／`kpi`／`bar` 底座并转出；待后续票确定。
- 本票避让：`html.ts` 只读；看身材照呈现走本能力内 `galleryDoc.ts`，不给它再加行。
