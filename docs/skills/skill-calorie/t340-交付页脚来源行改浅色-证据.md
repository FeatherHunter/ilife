# t340 · 体重页脚「📊 数据来源」改浅色口径行 · 证据

- 票：#340（体重六类页面页脚来源行，由深色 toast 卡改浅色口径行）
- 用户原话（转述自派单）：页脚那行「📊 数据来源:calorie_data.db · weight_log · 窗口 …」被渲染成**深色毛玻璃卡 ＋
  「✓ 知道了」按钮**，「像弹窗、有些奇怪」。
- 基准件：`docs/skills/skill-calorie/t422-融合共用件.md`（#422 浅色来源脚注）、`docs/skills/skill-calorie/t400-融合基准.md`（#420 口径行）
- 工作区基线：`master`，开工时 `449573f`（提交 sha 见本件末节）
- 写集：`packages/skill-calorie/src/weight/` 下六件 ＋ 本证据件（不碰 `notice()` 本身、不碰别的技能、不碰 `git stash/checkout/reset/restore`）

## 一、改了哪几件（逐处：改前产出器 → 改后产出器）

| 件 | 行 | 改前产出器 | 改后产出器 |
|---|---|---|---|
| `src/weight/history.ts` | `sourceLine()`（原 462 行） | `notice({ msg: '📊 数据来源:' ＋ 库 · weight_log, detail: 窗口 ＋ 条数 ＋ 缺口 })` ⇒ `ilife-block-feedback-block` ＋ `ilife-toast` | `renderCaliberLine('📊 数据来源:' ＋ 库 · weight_log · 窗口 · 共 N 条 …')` ⇒ `ilife-block-caliber` |
| `src/weight/compare.ts` | 原 256 行 `sourceText()` ＋ 原 431–432 行渲染处 | 同上（`msg` ＋ `detail` 两行） | `sourceText()` 改回整句字符串，渲染处 `renderCaliberLine(sourceText(a, b))` |
| `src/weight/log.ts` | 原 167 行（`deliveryBlocks()` 内） | `notice({ icon: 'info', msg: '📊 数据来源:' ＋ sourceText })` | `renderCaliberLine('📊 数据来源:' ＋ sourceText)` |
| `src/weight/plateDocs.ts` | 原 83 行（`deliveryBlocks()` 内，写后回执族共用） | 同上 | 同上 |
| `src/weight/review.ts` | 原 365／495／560 行（三形态：目标复核／窗口／回溯） | `notice({ icon: 'info', msg: '📊 数据来源:weight_log ｜ …' })` ×3 | `renderCaliberLine('📊 数据来源:weight_log ｜ …')` ×3 |
| `src/weight/volatilityDoc.ts` | 原 206 行 `sourceLine()` | `notice({ msg: '📊 数据来源:… · 窗口 … （M 天 · N 条记录 …）' })` | `renderCaliberLine(同一句)` |

- **用的是 `renderCaliberLine`（公共层 #420 的浅色口径行），不是 #422 的 `sourceLine()`**：六处文案各不相同
  （两段窗口／缺口句／「复核日 ｜ 当前 vs 目标」／「全量至 … ｜ N 条」），#422 的固定句式
  「数据来源 · 来源 · 起 → 止 · 共 N 条」装不下；派单要求「内容文案尽量保持原样」，故逐处保留原句、只换形态。
  #422 那件（`src/shared/sourceLine.ts`，运动回执族 `src/exercise/receipt.ts:220` 在用）本轮不动。
- 文案原样保留：`📊 数据来源:` 前缀不变（`weight-volatility-336.test.mjs:86／:89` 逐字断言它仍在）。
- 原 toast 的「标题 ＋ `detail`」两行折成口径行一句（` · ` 连接），`msg`／`detail` 的用词逐字未改。
- 注释同步改口径：六处「页脚数据来源行」注释里「toast 标题行有『知道了』按钮」那段解释删掉，换成
  「走 #420 浅色口径行」（`review.ts` 件头的 ⑤ 条也同步）。
- 清掉的死导入：`log.ts`／`plateDocs.ts` 两件的 `notice` 导入（该件再无其它 `notice` 调用）；
  `history.ts`／`compare.ts`／`review.ts`／`volatilityDoc.ts` 仍用 `notice`（页顶软横幅／空态说明／删除口径），导入保留。

## 二、58 页统计读数（判据）

量法：交付目录逐页扫 HTML（跳过 `index.html`），把每个反馈区块切出来，看「数据来源」这句话落在哪类块里。
脚本：`.scratch/t154/t340-measure.py`（本票新写）＋沿用 `.scratch/t154/measure-notices.py`。

| 读数 | 改前 | 改后 |
|---|---|---|
| 页面总数 | 58 | 58 |
| **「数据来源块是 `class="ilife-toast"`」的页数** | **57** | **0** |
| 「数据来源块是浅色口径行（`ilife-block-caliber`）」的页数 | 0 | **57** |
| 「数据来源」是别的形态的页数 | 0 | 0 |
| `measure-notices.py`：「📊 数据来源」这类块 | 57 | **0** |
| `measure-notices.py`：toast 块总数（一句话／带 detail） | 74（22／52） | 17（0／17） |

- 改前 57 页的 toast 形态原文（`08-看体重总览.html`）：
  `<div class="ilife-toast" …><span class="ilife-toast-icon">💡</span>…<span class="ilife-toast-title">📊 数据来源:…</span>…<button class="ilife-toast-close">✓ 知道了</button>`
- 改后同一页原文：`<p class="ilife-block-caliber">📊 数据来源:calorie_data.db · weight_log ｜ 窗口 2026-08-09 ~ 2026-09-07 ｜ 30 条</p>`
  ——深色卡面与「知道了」按钮一并消失（那颗按钮的委派监听挂在 `.ilife-toast-close` 上，块没了就不会再把页脚点掉）。
- 余下 17 个 toast 块**全是真正的提示**，逐条点数：`硬删除，不可恢复` ×3、
  `本窗只有 1 条记录` 系 ×4、`…低于门槛 3 条` 系 ×10（页顶「样本与口径」软横幅与删除口径）——`notice()` 形态未动。
- 差额 1 页＝`48-对比体重-当前-vs-目标体重.html`：它的真出口是 `calorie.view.goal-weight`（**体重目标页**，属目标域，
  渲染在 `src/goal/`），本族本来就不带页脚来源行；改前改后一致（不在本票写集）。

## 三、靶向用例读数

命令（持锁跑）：

```text
node tooling/run-locked.mjs --ticket 340 -- node --test \
  packages/skill-calorie/test/weight-history-333.test.mjs \
  packages/skill-calorie/test/weight-compare-334.test.mjs \
  packages/skill-calorie/test/weight-review-335.test.mjs \
  packages/skill-calorie/test/weight-volatility-336.test.mjs \
  packages/skill-calorie/test/weight-receipt-337.test.mjs \
  packages/skill-calorie/test/render-t41.test.mjs
```

```text
ℹ tests 43
ℹ pass 43
ℹ fail 0
RESULT: ticket=340 exit=0
```

- 逐条绿，无红。其中直接盯着改动面的三条：`#336 融合（§5 七组）…页脚来源行`、
  `#333 页面① 18 词逐条真跑`、`#335 判别式② 五窗口逐条走窗口形态`。
- 原文落盘：`.scratch/t154/t340-tests.log`。

## 四、构建与交付包读数

```text
node tooling/run-locked.mjs --ticket 340 -- pnpm build
  → tsc -b ＋ gen-cli --stamp ＋ 16 个 build:client 全绿；RESULT: ticket=340 exit=0（日志 .scratch/t154/t340-build.log）
node .scratch/t154/deliver58.mjs
  → TALLY {"通过":58} TOTAL 58；交付目录 .html 文件数 58；非 .html 混入 []
  → 58/58 通过，无「缺失阻断」、无「真坏」（日志 .scratch/t154/t340-deliver.log）
node .scratch/t154/gen-index.mjs
  → 写出 D:\ilife\.scratch\t154\delivery\index.html；通过 58 / 数据缺失 0 / 运行失败 0，共 58 条；OK-INDEX
```

- `deliver58.mjs` 会清空交付目录 ⇒ 索引页按派单要求由 `gen-index.mjs` 重生成（清单页在，58 条逐条可点）。
- 读数落盘：`.scratch/t154/t340-measure-before.txt`、`.scratch/t154/t340-measure-after.txt`、
  `.scratch/t154/t340-notices-after.txt`、`.scratch/t154/t340-toasts-after.txt`。

## 五、本票不动的（边界）

- `notice()` 本身（`src/shared/copyArea.ts:177`）不改：它仍服务页顶软横幅、`删除口径`、空态说明。
- 复制成功／失败的瞬时 toast 不动（用户 2026-09-12 裁定的深色卡面）。
- 别的技能（bill／chef／home／schedule／memo）与 `src/diet/`／`src/body/`／`src/exercise/`／`src/analysis/` 一律未碰。
- `src/shared/sourceLine.ts`（#422）未改。

## 六、告警线（350 行，LF 口径）

当场实测（`readFileSync(f,'utf8').split('\n').length - 1`）：

| 件 | 开工（HEAD `449573f`） | 本票改后 | 结论 |
|---|---|---|---|
| `src/weight/history.ts` | 671 | **670** | 已超线，需要根据规则进行重构。超因见包内 `AGENTS.md` 台账行（明细／曲线两条子功能共用一件）；本票让该件短 1 行，超线是既有事实、本票不引入。 |
| `src/weight/compare.ts` | 548 | **547** | 已超线，需要根据规则进行重构。超因见台账行（主窗口／对比窗口两套参数与对比算式同处一件）；本票让它短 1 行。 |
| `src/weight/review.ts` | 597 | **590** | 已超线，需要根据规则进行重构。超因见台账行（复盘三形态判别式与页面装配同处一件）；本票让它短 7 行。 |

- 其余三件改后均在线内：`log.ts` 299、`plateDocs.ts` 103、`volatilityDoc.ts` 301。
- 三件超线件在 `packages/skill-calorie/AGENTS.md` 台账里各有挂号行，「当场实测」列与上表**逐件相等**；
  `node packages/skill-calorie/scripts/check-warning-line.mjs` 当场复跑：`RESULT: 49/49`、
  `PASS: 告警线台账齐全且与实况一致`、`exit 0`。
- 拆法不在本票写集（本票只换页脚来源行的形态），超线件逐件结论待归属票认领。

## 七、未决

- 其余域（diet／body／exercise／analysis）页面里若还有「来源行走 `notice()`」的写法，本轮**未核未动**（派单限定体重六类页）。
- 第 48 页（`calorie.view.goal-weight`，体重目标页，渲染在 `src/goal/`）不带页脚来源行——是否要补一行来源口径，
  属目**标域**的事，本票不裁。
- 提交与推送读数见第八节。

## 八、提交与推送

```text
node tooling/run-locked.mjs --ticket 340 -- git commit -F .scratch/t154/t340-commit-msg.txt -- <逐条列出的 7 条路径>
  → [master 14d2664] fix(340): 体重六类页面页脚「数据来源」由深色 toast 卡改浅色口径行
     7 files changed, 170 insertions(+), 49 deletions(-)
git push
  → To https://github.com/FeatherHunter/ilife.git  5f1441e..14d2664  master -> master
```

- 按路径提交（索引多席共用）：六件源码 ＋ 本证据件，逐条列在命令里；`git show --stat` 复核＝7 条路径、
  无他人文件混入。
- 推送成功，未强推。日志落盘：`.scratch/t154/t340-commit.log`、`.scratch/t154/t340-push.log`。
