# #702 开工读数与范围复核（2026-09-18，认领后第一席）

票：`测试面改成打在接口上：页面走真正的交付出口、算式走能力门`（wayfinder:task，map #699）。
本件只记**当刻实测**与**票面陈化处**，不改任何源码；决定待用户裁定后再动。

## 一 · 当刻实测（口径可复现；脚本 `.scratch/t702/*.mjs`）

| 读数 | 票面写 | 当刻实测 |
| --- | --- | --- |
| `test/*.test.mjs` 件数 | 201 | **201** |
| import `dist/**` 具体件的件数 | 127 | **122**（`dist/index.js` 与 `dist/<能力>/index.js` 之外的所有 dist 路径） |
| 深路径 import 语句数 | 345（静态 314＋动态 31） | **286** |
| 只碰内部件、一个门都不碰 | 34 | **50**（深路径且无 CLI、无包门、无能力 barrel） |
| import 包门 `../dist/index.js` | 108 件／109 条 | **108 件／109 条**（其中取 `openDb` 的 100 件仍在） |
| import 能力 barrel `../dist/<能力>/index.js` | 32 件 | **27 件／34 条**（render 13、triggers 12、xunji 6、weight/exercise/photo/body 各 1） |
| spawn 真 CLI（`cmd_read.js`／`cmd_write.js`） | 124 件 | **132 件**（按语句口径：36 件 import 过 `dist/cli/*`） |
| 深路径里包门够得着的 | — | **18 条**（够不着 **268 条**） |
| 已 spawn 真 CLI、却仍带深路径的件 | — | **73 件／173 条** |

分区自洽：201 件里，能数出 dist 面语句的 155 件；纯无 dist（只 spawn 或纯读盘）其余。

## 二 · 票面里已被别的票改掉的两处（口径须重写）

1. **「import 十道 `run*` 门（当刻 8 件）」这一条已作废**：#703（已 close，`8a67c572`／`36005389`）把 10 个能力门上的 19 个 `run*` 全删了，门今天只剩
   `*_COMMANDS`（＋ weight 门 6 件、workout 门 5 件共 14 个非声明对外名）。既没有 `run*` 可 import，「8 件」这个数不再存在；同一张票还把包门从 402 值名收到 **63 值名**、
   `exports` 从 8 键收到 **3 键**（`.`／`./cli`／`./package.json`）。#703 的关闭评论自己点名：「公开面棘轮（冻结对外名清单、只许变短）……是 #702 第 3 条的射程」。
2. **「双路径」那一例当刻仍成立，但两处来源都要重新点名**：#703 之后，`test/analysis-shape-520.test.mjs` 与 `test/analysis-shape-567.test.mjs` 取 `buildDeficitDoc` 的路径须开工时逐行复核（本件未核，
   动手前先读那两行）。「同一符号两路径」这件事本身没被 #703 消掉。

## 三 · 生产分派真身（三条，测试要换的缝就到这一层）

- 真出口＝`dist/cli/cmd_read.js`（`src/cli/cmd_read.ts`）：argv → 预检 → 查 `REGISTRY`（生成物 `src/cli/registry.ts`）→ `spec.run(params, db)` → envelope ＋ 三态交付（`delivery.ts`：`file`／`inline`／`text`，`data.output` 与 `delivery.path` 同源）。
- `cmd_read.ts` 自己就转出一个**测试直调出口**（件头逐字：「#41 · 测试直调出口（纯 CLI 同逻辑，不经过 argv/spawn；CLI 唯一出口仍为 main）」）：
  `dispatch(key, params, db): DispatchOut`。它是**同一件**里的缝，换缝后断言的对象就是生产分派那一次调用。
- 能力门上今天**没有**算式门：算式住在 `analysis/*.ts`／`weight/*.ts` 这些**不出目录**的内部件里，由 `render/` 侧视图层直接消费（`src/analysis/index.ts:12-14` 逐字）。

## 四 · 工作区现场（并发纪律）

- `packages/skill-calorie/test/` 当刻有**别席在途的未提交改动**：124 件（`649 insertions / 634 deletions`），内容＝测试隔离从 `SKILLS_DB_PATH` 改到 `ILIFE_CONFIG_DIR` ＋ 新增 `test/helpers/config-test.mjs`；
  `src` 侧 22 件同批未提交（`src/config.ts`／`src/cli/config.ts` 为新件，`paths.ts`／`output.ts`／`readArgs.ts`／`xunji/*` 等在改）。最近一次写入 22:49:26，写入者持过 `gate.lock`（`ticket=676`）。
- 结论：**test 目录是别席的在途写集**，本票开工前须等它落地提交，否则同一批件会被两席互相覆盖（`docs/subagent-concurrency-protocol.md` §1）。

## 五 · 票面口径与新规矩的关系（给裁定用）

- `docs/agents/structure.md:9` 逐字：「不管：测试文件、页面模板、构建产物、生成的资产、文档。」⇒「测试面 ＝ 接口」是**新立的规矩**，要立得改那份规矩件（或只在包内 `AGENTS.md` 立一条）。
- 「同一符号不许从两个 `dist` 路径导入」与台账 `packages/skill-calorie/AGENTS.md:49`（`trendDocs.ts` 的 10 条薄转出）＋ 同表 `trendPredictDocs` 行（#518 W1 的拆法）正面打架：**搬迁期薄转出是台账已登记、且 #518 W1 刚按它做出来的合法形状**，门要立就必须同时给出例外口径。
