# #335 复盘证据：页面③ 体重复盘 6 词（期间复盘 ＋ 里程碑回溯）

> 分区：只动 review.ts 归属；禁碰 render/index 与 plate 薄层（plate.ts／plateDocs.ts 零改动）。
> 三形态判别式（冻结）：`mode==='milestones'`→回溯形态；有 `window` 或 `start`＋`end`→窗口形态；
> 其余（`today`／`date`／无参）→目标复核形态（既有行为一字不动，另有「看体重复核」在用）。

## 0. 基线与对照

- 本页指纹基线（改前，GATE-RUN runId=`1b818df7` exit=0）：target 58681 字节
  sha256=`f78ec5dcf034c3d95c7b262b7f2f305b06aa8857096351fd991eb3ac165a959d`，
  与 #332 证据逐字节一致；其余 6 格当时全返回目标复核形（窗口／回溯参数是死输入）。
- 老技能对照：`render_weight_review.py --type week|month|90d|year|range|milestones`；
  里程碑算法（历史最高起 5／10／15／20kg 首达）与窗口算法（自然周／自然月／90天／自然年／自定义，
  上一段等长区间均值差）逐条照抄，`_d` 符号口径（0 值不带号）保留。
- 超线报警：「已超线，需要根据规则进行重构。」`review.ts` LF=360（节点口径），超 10 行。
  超因：三形态视图模型与整页装配同处一处；本次先不拆：拆成 `review-period.ts`／
  `review-milestones.ts` 会扩大写入集并改命令声明引用路径，脏树并行期风险更大（编排者已裁定记债）。

## 1. 改动件（实际提交＝本节清单）

（待落地后填：commit sha＋文件列表。事前声明见 #335 开工广播评论。）

- `packages/skill-calorie/src/weight/review.ts`：加窗口形态＋回溯形态（目标复核一字不动）
- `packages/skill-calorie/src/weight/routes.ts`：加 6 条 exec（order 131–136 原号，`bucket`／`reason` 不留）
- `packages/skill-calorie/src/cli/legacy/routes/scene-03.ts`：删同样 6 条（一记录恰住一处）
- `packages/skill-calorie/src/triggers/scene-03-weight.ts`：只改 6 条的 `main_prompt.cli` 与 `data_source`
- `test/calorie-sot.snapshot.json`：脚本重算
- `test/calorie-routing-81.test.mjs`：计数＋登记册（看里程碑回溯 exit 4）
- `packages/skill-calorie/test/help-center-106.test.mjs`：67–68／110–129 行计数
  （同一数字另一落点；盘上值 exec 367／non-exec 69／legacy-chain 59＝已提交 361（含 #333 的 4 条）
  ＋本票 6 条；分析席落定后以终值为基一次重算，见 §5）
- `docs/research/t81-exec-smoke.md`：重生成
- 新建：`packages/skill-calorie/test/weight-review-335.test.mjs`、本证据件
- scene-03 未搬声明剩 121–128 共 8 条（`对比体重：当前 vs 平台期首日`等 8 锚点词，归 #334 设计保留，
  其两段对比 8 锚点走情景面runScenario、路由记录保持没有命令可执行——本票票面“剩 0 条”一句过时，以 8 为准；
  本票 6 条原子移动完整，一条记录恰住一处成立）

## 2. 持锁运行记录（GATE-RUN 对账）

- 改前基线构建：runId=`5abd739a` exit=1——红在别席冻结文件（后由 #333 提交解掉），非本票。
- 指纹基线 `--write`：runId=`1b818df7` exit=0（waitedMs=0）。
- 冻结转义自修后重建（日志 `.scratch/335/build-fix1.log`）：scene-03-weight.ts 错误清零，
  余红唯一为 `src/exercise/routes.ts` 运动席路由声明引用了未登记的命令键（不代修）。
- 本票用例：初跑 8／9（本周 vsLast 手算误 -0.4，实测 -0.1 系等长 1 天窗、老脚本同义，已修正 pin 值），
  复跑 PASS 9／9（日志 `.scratch/335/mytest2.log` exit=0）。
- 指纹 `--check`（`.scratch/335/fp-check.log`）：target SAME＋#332-SAME；其余 7 格 DIFF
  （5 窗口＋回溯由死输入变为真页，milestones-empty 由目标形变为可读缺失阻断）——均为预期形态跃迁。
- 6 词真跑（`.scratch/335/true-runs2.log`，runId=`b5464742` exit=0）：RESULT A=6/6 B=6/6，见 §4。
- 待双广播后：生成链四步＋gen:check＋全量测试＋smoke 重生成＋变异自证（红／还原一致两行）。

## 3. 两道门（待双广播后跑终值；本节先记范围与预期）

- `pnpm gen:check`：待跑。本票改动中影响生成物的只有路由声明（weight routes ＋6、场景分片 −6；
  冻结表改的是命令字段字面，不改声明结构）；生成物由 `pnpm gen` 重算、`gen:check` 逐件比对，
  weight 段预期与声明一致。 diet／analysis 席半落定段若红，逐件归属、不代修。
- 棘轮 `cmd-registry-294`／`legacy-ratchet-295`：本票不碰命令清单（`src/cli/legacy/scene-03.ts`
  命令清单本来就是 0 条）与分派层，只动路由声明——两棘轮的钉住对象都不在本票写集内，预期绿，
  终值以全量测试为准。
- 已跑的门：本票用例 9／9（`weight-review-335`）、指纹 target SAME、SoT 心脏逐字断言（用例内 6 条
  cli／data_source 与路由声明交叉核对：order 131–136、kind exec、无 bucket／reason）。

## 4. 真出口（6 词逐条，标准种子库 frozen cli 原样＋富种子回溯）

A（实跑 exit／envelope）：本周 0／本月 0／90天 0／今年 0／自定义 0（占位符替换）／
回溯 exit 4 envelope 空（`尚未达成任何减重里程碑`，已登记数据依赖）。
B（HTML 落盘 `.scratch/335/exports/`，逐件字节）：week.html 62909／month.html 64262／
90d.html 82363／year.html 116436／custom.html 64310／milestones.html（富种子）59141，
均为完整文档（57–116KB 档）。
C（标准种子读数）：本周 rows=1 delta=0 avg=70.6；本月 rows=7 delta=-0.1 avg=70.7；
90天 rows=90 delta=-1.1 avg=71.2 months=4；今年 rows=250 delta=-3 avg=72.1 months=9；
自定义 rows=7；回溯（富种子）milestones=1 maxWeight=75.5 currentWeight=69.6 totalLoss=5.9。
6 条 100% 没有命令可执行 → 全部有命令可执行（5 条 exit 0＋1 条可读缺失阻断 exit 4 登记）。

## 5. 交付对账（事前清单 vs 实际，偏差逐条）

事前清单见 #335 开工广播评论（comment 5659095945）；实际提交待双广播后一次落地，偏差预告如下：

- 增：`help-center-106` 110–129 行计数（第二处 357 落点；断言保留、数字随事实 bump，
  开工广播只点了 67–68 行，此处补记）。
- 改：scene-03 落完剩 8 条（121–128，#334 锚点设计保留），票面“剩 0 条”过时，以 8 为准。
- 改：`review.ts` LF 360（超线 10 行，记债不拆，编排者已裁定）。
- 改：全部计数以分析席落定后终值为基一次重算（盘上 367 系＝361 已提交＋本票 6，终值＝361＋分析席数＋6）。
- 其余逐行对上，无偏差。

## 6. 落地清单（双广播后一次执行，单次持锁窗口内串完即 commit＋push）

生成链四步（`pnpm build → pnpm gen → pnpm build → pnpm help:build`，SKILL.md 若附带改写则还原归 #338）
→ SoT 快照重算 → smoke 重生成 → `pnpm gen:check` → 全量测试 → 本票用例 → 变异自证
（review.ts 改坏一处必红、还原变绿两行机器读数）→ 指纹 --check → 外观抽查 → 对账提交推送。
