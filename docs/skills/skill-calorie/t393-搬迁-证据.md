# t393 · 卡路里场景 02 饮食：页面装配件按页面类就地摆正（证据）

**判定：PASS。** 决定性证据＝同批输入下七个装配函数的产物 `sha256` **拆前＝拆后 22/22 一致**；
`pnpm build`／`pnpm gen:check` 各 exit 0；变异自证「改坏 18/22／还原 22/22」两行读数齐；
引用被搬符号的测试文件 95 例 94 绿，唯一的红是基线即红的照片域用例。

**提交**：`ba73a68cfd4cc182840c7c4e82319797fe1e2421`（21 件，已推送）。
回填 sha 的这次提交见文末「八、未做项与遗留」第 5 条。

## 一、做了什么（逐条路径）

| 件 | 动作 | LF 行数（告警线 350） |
| --- | --- | --- |
| `packages/skill-calorie/src/diet/todayDocs.ts` | 新建：`buildTodayDietDoc` ＋ 它的输入形 `TodayDietDocInput` | 104 |
| `packages/skill-calorie/src/diet/reviewDocs.ts` | 新建：`buildDietReviewDoc` | 111 |
| `packages/skill-calorie/src/diet/rankingDocs.ts` | 新建：`buildRankingDoc`／`buildAllRankingsDoc` ＋ 私有辅助 `RANK_ZH`／`RANK_COLUMNS` | 118 |
| `packages/skill-calorie/src/diet/libraryDocs.ts` | 新建：`buildSearchDoc`／`buildLibraryDoc`／`buildDedupeDoc` ＋ 私有辅助 `PRODUCT_COLUMNS` | 152 |
| `packages/skill-calorie/src/render/dietDocs.ts` | 定点删除（五段）：663 → 247 | 247 |
| `packages/skill-calorie/src/diet/today.ts`／`review.ts`／`ranking.ts`／`library.ts` | 只改 import 指路 | 54／29／28／50 |
| `.changeset/t393-diet-docs-split.md` | 新增 | — |

四件新件都远在 350 以内，**未触发超线报警**。`render/dietDocs.ts` 留原地的两件：
`buildViewDietDoc`（`src/home/today.ts:12` 在用）、`buildHealthDoc`（`src/analysis/commands.ts:25` 在用）。

删除集只由**当刻内容**的锚点派生（`t393-split.mjs`：小节注释命中恰一次，命中 0 次记「已被别人做掉」，
多次即抛错），不按行号、不按预备快照；先在内存里算出产物、跑完断言才落盘。
删除区间（1 基）：`[[70,152],[259,350],[351,450],[451,533],[613,663]]`。

## 二、判据① 每一页产物逐字节不变（决定性证据）

做法：`t393-probe.mjs` 对**同一批 22 例输入**跑两态——搬迁前全取自 `dist/render/dietDocs.js`
（`t393-map-before.json`），搬迁后分别取自 `dist/diet/{today,review,ranking,library}Docs.js`
（`t393-map-after.json`）；每例把返回值取 `sha256` 写进 `t393-sha-before.json`／`t393-sha-after.json`，
再逐条比。用例覆盖每个函数的空值／满值／边界分支（含 `buildDedupeDoc` 的 55 组「仅列前 50 组」分支）。

```
COMPARE .scratch/t393/before.json ↔ .scratch/t393/after.json
RESULT: 22/22 一致
```

用例分布：`buildTodayDietDoc` ×3／`buildDietReviewDoc` ×3／`buildRankingDoc` ×3／
`buildAllRankingsDoc` ×3／`buildSearchDoc` ×3／`buildLibraryDoc` ×4／`buildDedupeDoc` ×3。

> 一行如实说明：22 例里有 1 例两态**同样**抛错（`buildLibraryDoc` 传空串分类时
> `renderKpiCard: input.value 必须是非空字符串`，`lib.category ?? '全量'` 对空串不回落，是既有的老写法，
> 本票不修）。探针把异常也按「构造名＋消息」逐字记进摘要，两态相等，故仍计入 22/22。

## 三、判据② 编译与生成一致性

同一持锁窗口内顺序执行（运行标识 `b3252cca-8270-4bea-a8e2-0cf0f260fc64`）：

```
STEP split exit=0
STEP build exit=0
STEP probe-after exit=0
STEP compare exit=0
STEP gen-check exit=0
GEN-CHECK PASS：命令 118（会改数据库 46 ＋ 查询 72）；能力 10 个（analysis／body／diet／exercise／goal／home／photo／profile／weight／workout）声明 118 条，未搬迁清单 0 条
```

本票**未** `git add` 任何生成物（`src/cli/keys.ts`／`src/triggers/routes.generated.ts`／
`packages/base-combos/combos.yaml`／`scripts/build-help.mjs`／`SKILL.md`／`packages/base-combos/src/present.ts`），
归编排者统一收口（#392）。

## 四、判据③ 不新增红

**全量差集不采用**：基线（13:52 落下）与收工之间，别席至少落了 `bf274e6`(#276)、`b646d6f`(#342 收口)、
`3e33048`／`17d09ba`(#276) 等提交，差集被并发提交污染。实测口径：

| | 基线（window1） | 搬迁后（window2b） |
| --- | --- | --- |
| 全量 `ℹ tests` | 1608 | 1616 |
| `ℹ pass` | 1580 | 1588 |
| `ℹ fail` | **28** | **28** |

28 对 28；两态唯一红名的差集是 5 换 5，**一条都不落在饮食域**：
新增的四条全在 `trend-homogeneity-110.test.mjs`（#110 自己的票），一条是
`db-readonly-93.test.mjs`，它那条断言当刻的 `actual` 是 `[ 'calorie.view.profile-wizard' ]`
（档案预检页，`src/profile/*` 装配，本票一个字节没碰）；消失的五条是 #269／#342 自己修好的
（`#83`／`#179`／`C6`／`#342`）。

**改用靶向读数**（运行标识 `ab11468f-f2a1-4c70-ac3c-bf6d9597bde4`，见第七节）：只跑「引用被搬符号（饮食七页命令）」的 11 个测试文件——

```
ℹ tests 95
ℹ pass 94
ℹ fail 1
✖ T10 照片 parity：画廊/单图/对比/动图/HELP + 二进制不内嵌
```

唯一的红是照片域（`/base64/` 断言），**基线窗口里同样红**，不是本票带来。
`diet-homogeneity-108.test.mjs`（饮食八条命令逐条真跑）、`db-readonly-93.test.mjs`（8/8）、
`cmd-read-t11.test.mjs`、`delivery-83.test.mjs`、`calorie-c43.test.mjs` 等全绿。

## 五、判据④ 变异自证

改坏点：`src/diet/rankingDocs.ts` 的 `RANK_ZH.high_calorie`（`'高热量'` → `'高热量!'`）。
选它的理由：它同时进 `buildRankingDoc` 与 `buildAllRankingsDoc` 两件的产物，
因此应当「部分用例变、其余不变」——既能证明探针抓得住，也能证明它不是「一律全变」的假红。

```
MUTATE applied sha 4db8bc92ea4aa69ea2615d866b001098bf26dad82676e8016dcaedbc468098c3 → d56990fac3effcb0fe5f5c385d085f991bd38aacd497d115caa7ac3c7dbb7356
STEP build-mutant exit=0
有差异: buildRankingDoc#case7, buildAllRankingsDoc#case10, buildAllRankingsDoc#case11, buildAllRankingsDoc#case12
RESULT: 18/22 一致
```

逐文件还原（`t393-mutate.mjs --restore`，还原后回读 sha 与备份逐字节相等）＋按当刻源码重新编译＋重签：

```
MUTATE restored sha 4db8bc92ea4aa69ea2615d866b001098bf26dad82676e8016dcaedbc468098c3 与备份一致 mtime=2026-09-14T06:19:21.365Z
STEP dist-pollution-check polluted=[False]
RESULT: 22/22 一致
STEP gen-check exit=0
```

**两行机器读数：改坏 `RESULT: 18/22 一致`（4 条 sha 变）／还原 `RESULT: 22/22 一致`。**

> 窗口三踩到一个真坑，记在这里备查：改坏轮的 `pnpm build` 之后，还原用的是 `copyFileSync`，
> **Windows 上它把备份的 mtime 一并带过来**，mtime 往回走 ⇒ `tsc -b` 认为「没变」而跳过重编 ⇒
> `dist/diet/rankingDocs.js` 里留着改坏后的产物，于是「还原」读数一度显示 18/22（假红）。
> 已在 `t393-mutate.mjs` 的还原路径补 `utimesSync(..., now)`，并在窗口四加了
> `STEP dist-pollution-check polluted=[False]` 这条断言。这正是 `gen-cli.mjs` 头注释里 #325 记的那条假绿链。

## 六、符号级核对（旧路径再无引用）

```
=== dietDocs.ts 还导出什么 ===
  31: export interface DietMealRow {
  66: export interface ViewDietDocInput {
  88: export function buildViewDietDoc(input: ViewDietDocInput): string {
 179: export function buildHealthDoc(h: HealthPlate): string {
=== 7 个被搬函数的老家还有没有 ===
  buildTodayDietDoc in render/dietDocs.ts = False      （其余六名同：False）
=== 4 个调用点的指路 ===
  today.ts:13:   import { buildTodayDietDoc } from './todayDocs.js';
  review.ts:11:  import { buildDietReviewDoc } from './reviewDocs.js';
  ranking.ts:8:  import { buildAllRankingsDoc, buildRankingDoc } from './rankingDocs.js';
  library.ts:9:  import { buildDedupeDoc, buildLibraryDoc, buildSearchDoc } from './libraryDocs.js';
=== 仍指旧路径的 import（恰两处，就是留原地的两件）===
  src/home/today.ts:12:       import { buildViewDietDoc } from '../render/dietDocs.js';
  src/analysis/commands.ts:25: import { buildHealthDoc } from '../render/dietDocs.js';
```

一处需要说明的类型引用：`src/diet/todayDocs.ts:10` 用 `import type { DietMealRow } from '../render/dietDocs.js'`。
该接口同时被搬走的 `buildTodayDietDoc` 与留在原地的 `buildViewDietDoc` 用到，**定义只留一处**（铁律二），
搬走它会新造一条「留下的那个函数反过来依赖新件」的边；本票按「只换住处」的最小改动把它留在原地。

## 七、门禁对账（GATE-RUN）

运行记录导出件：`docs/skills/skill-calorie/t393-门禁运行记录.txt` ——覆盖本票在闸门日志里的**全部** 20 行
（9 次运行，含两次被自家断言拦下的删除试验），逐笔与下表一对一。

| GATE-RUN runId | cmd | 退出码 |
| --- | --- | --- |
| `GATE-RUN runId=73c7960e-fe7b-459d-b070-ea83e49ff6f8 cmd=pwsh -NoProfile -File .scratch/t393/window1.ps1` | 基线：build ＋ 搬迁前探针 ＋ 全量测试 | 0（窗内 `build=0 probe=0 test=1`，测试红数即基线 28） |
| `GATE-RUN runId=bd7f3707-1fa1-4536-a831-ab7ec08ac223 cmd=pwsh -NoProfile -Command "node .scratch/t393/probe.mjs --map .scratch/t393/map-before.json …"` | 补第 22 例后的搬迁前探针 | 0 |
| `GATE-RUN runId=3f7414d6-351d-458e-b427-41298a28451d cmd=node .scratch/t393/split.mjs` | 定点删除（第一试） | 1（自家断言红：保留项检查误判，**未落盘**） |
| `GATE-RUN runId=821e757f-1aa1-4937-9f74-17f66abb09d4 cmd=node .scratch/t393/split.mjs` | 定点删除（第二试） | 1（自家断言红：结尾空元素计数，**未落盘**） |
| `GATE-RUN runId=b3252cca-8270-4bea-a8e2-0cf0f260fc64 cmd=pwsh -NoProfile -File .scratch/t393/window2b.ps1` | 落点窗：删除 ＋ build ＋ 探针 ＋ 比对 ＋ gen:check ＋ 全量测试 | 0 |
| `GATE-RUN runId=3bf3b750-da9c-47fc-abd2-70d56f0f5262 cmd=pwsh -NoProfile -File .scratch/t393/window3.ps1` | 变异自证窗（改坏／还原＋两次编译两次探针） | 0 |
| `GATE-RUN runId=745790aa-082d-4cc5-8e73-8e7a60731335 cmd=pwsh -NoProfile -File .scratch/t393/window4.ps1` | 还原复位窗（重编＋污染检查＋比对＋gen:check） | 0 |
| `GATE-RUN runId=ab11468f-f2a1-4c70-ac3c-bf6d9597bde4 cmd=pwsh -NoProfile -File .scratch/t393/window5.ps1` | 靶向：引用被搬符号的 11 个测试文件 | 0（窗内 `targeted=1`，唯一红是基线即红的照片域用例） |
| `GATE-RUN runId=25952612-989f-4a20-8b0a-d06b494ce388 cmd=pwsh -NoProfile -File .scratch/t393/commit.ps1` | 提交窗：点名加件 ＋ 复核暂存区 ＋ 提交 ＋ 推送（不含编译／测试） | 0（提交 `ba73a68`，推送 `442c780..ba73a68`） |
| `GATE-RUN runId=e5d678e1-7f7f-47eb-ad79-aec756241f4d cmd=pwsh -NoProfile -Command "git add -- … commit … push"` | 回填提交 sha 的那一笔（不含编译／测试） | 0（提交 `c725c20`，推送 `ba73a68..c725c20`） |

**本文件的发布提交**（写下这行的那一笔）自身的运行条目也在导出件里，运行标识见票面评论——
它只是一次 `git add／commit／push`，不含编译与测试，不再循环声明。

两次删除试验（3f7414d6／821e757f）都是**自家前置断言**拦下的，符合 §2.5 第 1 条
「失败即不落盘」：工作区在两次失败后都干净（`git status` 逐件核对，`dietDocs.ts` 未改动）。
其中 821e757f 那笔等待了 550 秒才拿到锁——当刻全仓锁饥饿（#276 变异电池整窗持锁）。

## 八、未做项与遗留

1. **说明文字未同步（有意留在本票范围外）**：派单写明 `render/dietDocs.ts`「只许删掉搬走的那 7 个函数与
   它们的私有辅助」、四个调用点文件「只许改 import 指路」，故下列**只描述位置、不影响行为**的文字未动，
   搬走后它们与事实不符，建议并入 #270–#275 任一票或另开一行小票：
   - `src/render/dietDocs.ts:3-7` 文件头「范围」段仍列着已搬走的那六页；
   - `src/diet/today.ts:4`／`review.ts:4`／`ranking.ts:4`／`library.ts:4` 的说明句仍写「装配走 `render/dietDocs.ts`」。
2. **全量测试差集被并发提交污染**，故判据③改用靶向读数（第四节）；若收口时要一份干净的对照，
   需要在安静窗口重跑一次基线。
3. **生成物**：`pnpm gen:check` 为绿，但共享工作区里 `src/cli/keys.ts` 等仍带别席在途改动，本票未碰、未提交。
4. **未碰别席声明路径**：`src/diet/receipt.ts`／`src/diet/routes.ts`／`src/cli/legacy/routes/scene-02.ts`／
   `test/{profile-doc-179,calorie-c43,delivery-83,m5-receipt-97,cmd-write-40-persist}.test.mjs` 一律未写。
   派单里「`src/cli/cmd_read.ts` 也引这批」与当刻事实不符——该件已无对这些函数的引用（#315 已把处理体搬进 `src/diet/`），
   故**没有需要改的调用点**，本票未动 `cmd-read`。
5. **本文件的提交 sha 回填**：搬迁本体与证据同批交在 `ba73a68`；本行所属的那次提交只追加本节第 1、5 条与顶部
   「提交」那一行，不动任何源码。

## 附：可复跑脚本与读数件

- `docs/skills/skill-calorie/t393-probe.mjs` —— 两态探针（`--map/--state/--out` 与 `--compare`）
- `docs/skills/skill-calorie/t393-split.mjs` —— 定点删除（前置断言，全过才落盘）
- `docs/skills/skill-calorie/t393-mutate.mjs` —— 变异改坏／还原
- `docs/skills/skill-calorie/t393-map-before.json`／`t393-map-after.json` —— 两态的「函数名 → 模块」
- `docs/skills/skill-calorie/t393-sha-{before,after,mutant,restored}.json` —— 四份逐例 sha256 读数
- `docs/skills/skill-calorie/t393-门禁运行记录.txt` —— 本票运行记录导出
