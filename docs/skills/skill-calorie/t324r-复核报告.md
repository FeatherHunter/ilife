# #324 实现层六窗搬迁 · 对抗式复核报告（接手席）

- 被审对象：`feat(324)` 六窗落点提交 —— W2 `a39b86a`（试点窗，已由 `docs/skills/skill-calorie/t324-W2r-复核报告.md` 判 **PASS／93**，本报告只引用其口径、不重做）／W6 `c366270`／W5 `f297a8a`／W3 `4b69ff5`／W4 `3cda469`／W1 `b59d4bb`，＋ W1 对账导出 `7c295d4`。
- 复核构成：**前席**跑完取证（原始输出落 `.scratch/t324r/`）后、在成文前耗尽上下文；**接手席（本席）**读前席落盘证据 ＋ 自己复跑便宜且决定性的判据，收成本报告。
- 归属标注：`【自跑】`＝本席当刻实跑的读数；`【继承】`＝前席落盘的原始输出，本席只引用其读数、并已在可能处交叉验算。
- 现场偏差（如实记录）：派单记 `HEAD == origin/master == fb72a31`——那是派单当时；本席**开工时** HEAD 已前进到 `10938ad`（`docs(agents): 编排纪律 —— 取证与成文分两段派`，其父提交正是 `fb72a31`），**成文当刻**又前进到 `8a721fb`（他席 push 的 `review(293): 编排六决策对抗式审查 —— 判定 D2／D5 帮目标、D1／D3／D4／D6 需整改`）。三次核对 `git rev-list --left-right --count HEAD...origin/master` 均为 `0 0`（本地与远端同步，只是派单记载的 sha 陈旧）。
- 他席在飞改动（钉住不动）：开工时工作树为 ` M docs/skills/skill-calorie/t293-验收-命令自治.mjs` ＋ `?? docs/skills/skill-calorie/t293-编排决策-对抗审查.md`（派单只提到前一处）；成文当刻那份 md 已由他席在 `8a721fb` 提交，只剩 ` M …/t293-验收-命令自治.mjs` 仍未提交。本席对这两处**未碰、未提交、未回退**；本报告只追加一个提交，`git add` 按路径点名（不用 `-A`）。

## 判定与分数

| 项 | 值 |
|---|---|
| S1（交付缺陷） | **0** |
| S2 | **1**（对账面：六窗证据件 `GATE-RUN` 声明 0 条、`check-gate-audit` 六窗 FAIL） |
| S3 | **4** |
| **判定** | **FAIL** |
| **分数** | **87 / 100** |

- 门槛沿用本批已生效口径（`t324-W2r-复核报告.md` 第 9 行）：**「S1 = 0 且 S2 = 0」未过 ⇒ FAIL**；协议 §6 量刑律（均分 ≥85）已过，故 FAIL 的唯一来源是那条 S2，不是均分。
- 一句话：**「搬」的那一半六窗全部为真**——逐窗生成物五件逐字节不变、声明面 100＋1 键闭环零重复、两个分派文件的老 `case` 面 0 行改动、全量 1492／1492、冻结面 8／8、自治验收 P1..P5 5/5、`UNACCOUNTED=0`，这些本席都自己跑过；**扣分与 FAIL 全在「记账」**：除已返修的 W2 外，六窗证据件**一条 `GATE-RUN` 声明都没有**，协议下这些运行**不计入对账、也不得当作门禁证据引用**。
- 五维（§5.1 参考口径）：契约一致 **26**／30、证据真实可复现 **20**／25、parity **18**／20、工程红线 **14**／15、文档同步 **9**／10。

## 逐维结论

### 维1 · 免费判据（生成物五件 ＋ 改动账）—— PASS【自跑】

判据④要求「生成物五件 sha256 逐字节不变」。**本席不看结论，直接从 git 对象侧复算**（脚本 `.scratch/t324r2/free-evidence.txt`）：

- 逐窗 `git diff --numstat <窗>~1 <窗> -- <五件>` **全空**（每件 `diff=0`），且五件的 blob 在**同一窗的父提交与自身之间逐一相等**：
  `registry.ts`（W2／W6／W5／W3／W4 各窗 parent == self）、`keys.ts`、`combos.yaml`、`routes.generated.ts`、`build-help.mjs` 六窗全部 parent == self。
- 当刻工作树五件 sha256【自跑 `Get-FileHash`】：`registry.ts 5c81bfbd…`、`keys.ts 4db43569…`、`combos.yaml ca17d2c3…`、`routes.generated.ts e9ea9935…`、`build-help.mjs 4d44f03f…` —— 与前席落盘的门禁原始输出（`_g-gate-summary.txt` L8–L12、L2035–L2039）**逐字相同**，即「当刻文件 == 门禁当时认下的那五件」。
- 改动账【自跑 `git diff --shortstat <窗>~1 <窗>`】：W2 `23 files, 2326+/41-`、W6 `9 files, 2285+/11-`、W5 `13 files, 2381+/17-`、W3 `12 files, 2346+/17-`、W4 `6 files, 2332+/8-`、W1 `42 files, 2689+/80-`；`--name-status` 显示搬件为 **R094–R100 重命名** ＋ 少量定点修改，插入行绝大部分是各窗自己的证据件（A）。
- **范围外发现（S3-1）**：`registry.ts` 的 blob 在**跨窗**比较时有**一次**变化（W2／W6／W5／W3／W4 = `759155ef83` → W1 = `d62ad6ddef`）。本席用 `git log 3cda469..b59d4bb -- <registry.ts>` 定位到变因是**夹进这批六窗之间的 `78472a3 feat(318): 场景 06 目标管理／07 基础信息命令搬进能力目录（核验与补齐 · 窗口 A）`**——不是六窗中的任何一窗（逐窗 parent == self 已证）。⇒ 六窗自己的「生成物五件不变」成立；把口径写成「六窗全程不变」则不成立。

复现：

```powershell
foreach($c in @('a39b86a','c366270','f297a8a','4b69ff5','3cda469','b59d4bb')){ git diff --numstat "$c~1" "$c" -- packages/skill-calorie/src/cli/registry.ts packages/skill-calorie/src/cli/keys.ts packages/base-combos/combos.yaml packages/skill-calorie/src/triggers/routes.generated.ts packages/skill-calorie/scripts/build-help.mjs }
foreach($a in @('packages/skill-calorie/src/cli/registry.ts','packages/skill-calorie/src/cli/keys.ts','packages/base-combos/combos.yaml','packages/skill-calorie/src/triggers/routes.generated.ts','packages/skill-calorie/scripts/build-help.mjs')){ (Get-FileHash -Algorithm SHA256 $a).Hash.ToLower() }
git log --oneline 3cda469..b59d4bb -- packages/skill-calorie/src/cli/registry.ts
```

### 维2 · 搬迁面（件是否恰好搬走、引用点是否同步）—— PASS【继承＋自跑交叉】

- 六窗搬件清单与新家落点（每窗证据件 §1「件 → 新家 → 依据」【继承】，本席由 `--name-status` 的 R 记录交叉确认）：W2 六件 → `src/diet/`；W6 两件（`render/health.ts`→`analysis/healthPlate.ts`、`fetch/history.ts`→`analysis/historyStore.ts`）；W5 三件（`render/goal.ts`→`goal/goalPlate.ts`、`render/goalExtra.ts`→`goal/goalExtraPlate.ts`、`fetch/goal.ts`→`goal/goalStore.ts`）；W3 一件（`fetch/exercise.ts`→`exercise/exerciseStore.ts`）；W4 一件（`fetch/plan.ts`→`workout/planStore.ts`）；W1 九件 → `src/home/`、`src/body/`、`src/photo/`。
- 旧路径删除与共用位定点删改：`--name-status` 中旧路径全部以 `R0xx` 出现（无「新增副本 ＋ 旧件留着」型半搬）【自跑】；共享位只动说明符【继承 `_d-refs.txt`：引用点同步清单 ＋ `_u-dispatch.txt`】。
- 共用层残留判据②（「不再有只属一个场景的件」）：【继承 `_v-contract.txt` ②】逐件列出 `src/render` 22 件／`src/fetch` 15 件的消费方清单并按「单能力消费」标记。⇒ 剩余共用件当刻**都有 ≥2 消费方或属真共用**的账；被标「单能力消费」的行是**报账项**，不影响本票判据（且本报告不据此判任何方向）。
- 铁律五（一个文件对外给的东西 ≤5）：【继承 `_v-contract.txt` ①】新家目录里**22 件对外导出名 >5**（如 `analysis/anomaly/index.ts` 25 个、`workout/planStore.ts` 26 个）。本席判其**越不出本票「别加码」的边界**（判据④要求生成物逐字节不变、判据是纯搬迁、行为不变），故记 **S3-2** 转票，不据此判 FAIL。

### 维3 · 声明面与分派面 —— PASS【自跑】

- 声明面（**源码侧**，本席不采信前席的 dist 侧读数，另算一遍）：`Select-String -Path packages\skill-calorie\src\*\commands.ts -Pattern "key:\s*'([^']+)'"` ⇒ **commands.ts 10 件、声明记录 100 行、唯一键 100、重复键 0**；逐能力 `analysis=13 body=8 diet=24 exercise=9 goal=13 home=4 photo=10 profile=5 weight=9 workout=5`（合计 100）。
- 老分片残留：`src/cli/legacy/*.ts` 声明键 **1 条** = `calorie.view.goal-weight`（`scene-03.ts:12`）；`scene-01.ts`／`scene-02.ts` 为空数组。⇒ **101 键闭环**，与前席 dist 侧计数（`_c-decl.txt`：能力 100 ＋ 老分片 1 ＝ 101）及门禁「键 101（写 35 ＋ 读 66）」**三面一致**。
- 分派面（判据⑤「两个分派文件的老 `case` 面零改动」）：本席逐窗分类 `cli/write.ts` ＋ `cli/cmd_read.ts` 的 `+/-` 行 ⇒ 改动行数 `0/0/6/2/0/4`，其中**非导入、非注释行 = 0**（W5 只换两处 `import` 说明符、W1 只改两处注释，与 `_u-dispatch.txt` 的逐行原文一致）。⇒ 老 `case` 面**逐字节未动**，成立。
- 路由记录面【继承 `_g-gate-summary.txt` L36】：记录面 `routes.generated.ts` 共 495 条（`WAKE_ROUTES 436 ＋ NEW_KEY_ROUTES 58 ＋ COVERAGE_REPAIR_ROUTES 1`）／exec 403 条，带生成 banner 且在 `gen-cli` 的 targets 数组里。
- gen:check：本席**未复跑 `gen:check` 本身**（成本），但三面旁证（五件 sha256 与门禁记录逐字相同【自跑】＋ 探针 P3 的「手改生成物 ⇒ exit=1」反假绿【继承】＋ 声明面源码侧计数与门禁读数一致【自跑】）共同支持前席落盘的 `GEN-CHECK PASS`【继承】。

复现：

```powershell
Select-String -Path packages\skill-calorie\src\*\commands.ts -Pattern "key:\s*'([^']+)'" -AllMatches
Select-String -Path packages\skill-calorie\src\cli\legacy\*.ts -Pattern "key:\s*'([^']+)'" -AllMatches
foreach($c in @('a39b86a','c366270','f297a8a','4b69ff5','3cda469','b59d4bb')){ git diff "$c~1" "$c" -- packages/skill-calorie/src/cli/write.ts packages/skill-calorie/src/cli/cmd_read.ts }
```

### 维4 · 行为不变 —— PASS【自跑】

- 全量（**本席持锁一次**，`--ticket t324r2`）：`node tooling/run-locked.mjs --ticket t324r2 --lock-dir .scratch/locks-land --stale-minutes 30 -- node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"` ⇒
  `ℹ tests 1492 ; ℹ suites 160 ; ℹ pass 1492 ; ℹ fail 0 ; ℹ cancelled 0 ; ℹ skipped 0 ; ℹ todo 0 ; ℹ duration_ms 37701.29`（日志 `.scratch/t324r2/full-test.log`；与前席落盘读数逐字一致）。
- 冻结面（判据⑤）：`node --test test/calorie-routing-81.test.mjs`（持锁）⇒ `ℹ tests 8 ; ℹ pass 8 ; ℹ fail 0`。
- 自治验收 `P1..P5`（持锁）：`node docs/skills/skill-calorie/t293-验收-命令自治.mjs` ⇒ `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`，`③ UNACCOUNTED = 0 处`，`④ 主体判不出 = 81 处（只报账、不判红）`，`树自相矛盾 = 0 处`；wrapper 行 `RESULT: ticket=t324r2 runId=9977c045-1fd2-4e3c-bcaa-f86b48968b1a waitedMs=0 exit=0`（日志 `.scratch/t324r2/probe.log`）。
- **在飞改动的影响研判**：当刻盘上该探针带着**他席未提交的改动**，本席跑的正是这份工作树版本，结果**全绿** ⇒ 不触发「若红则先判是否在飞改动所致」那一条，**无归因分支需要启动**（若后续该改动把探针改红，那是后续时点的事，与本票无关）。
- 冻结地基三面（`src/triggers/scene-*.ts`、`test/calorie-routing-81.test.mjs`、两分派文件老 `case` 面）：【继承 `_t-home-frozen.txt` B／B2】六窗均未碰 `scene-01/08/09` 与冻结测试；另有 `scene-06`／`scene-10` 亦未被碰。本席另自跑了分派面（见维3）——判据⑤三面**全部成立**。

### 维5 · 证据面 —— **S2（唯一 FAIL 源）**

- 证据件在仓且被 git 跟踪：六窗各 `A docs/skills/skill-calorie/t324-W{1,3,4,5,6}-证据.md`（本席由各窗 `--name-status` 交叉确认）；W1 证据件标题结构完整（§1 件→新家→依据 … §13 注释维）【继承 `_r-w1-headings.txt`】；控制符 0（BEL／ESC／FF／NUL 全空）、占位符 0 残留（唯命中行是模板说明与测试名）【继承 `_w-final.txt`／`_m-placeholder.txt`】。
- **S2-1【自跑复现】**：除已返修的 W2 外，**六窗证据件的 `GATE-RUN` 声明 = 0 条**（本席 `Select-String`：W1／W3／W4／W5／W6 = `0`，W2 = `11`）；本席实跑
  `node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t324-W1-证据.md --log docs/skills/skill-calorie/t324-W1-gate-runs.log --ticket 324-land-W1`
  ⇒ `RESULT: matched=0/0 auditEntries=8 scoped=8 undeclared=8`、`gate-audit: FAIL`、exit=1。
  【继承 `_h-audit-summary.txt`】其余窗同型：W3 `undeclared=2`、W4 `1`、W5 `1`、W5fix `4`、W6 `3`、W6b `2`、W6c `1`，**全部 `gate-audit: FAIL`**；对照 W2（已返修）`gate-audit: PASS`。
- 这条的**硬依据**是协议 §2.4.4 第 3 条（`docs/subagent-concurrency-protocol.md` 第 93 行）：没有 `GATE-RUN` 声明的运行**不计入对账，也不得当作门禁证据引用**。⇒ 本票判据⑥「逐票留证据与**对账导出**」当刻**不成立**；且本报告里【继承】两字的门禁读数因此**只能当「前席落盘的原始输出」引用，不能当对账通过的证据**。

### 维6 · 变异自证 —— PASS【继承】

- 前席落盘四次真变异（改坏变红／改回变绿）【继承 `_l-mut-summary.txt`／`_p-mut-summary.txt`／`_q-m2b.txt`】：

| 变异 | 打的哪一面 | 改坏 | 改回 |
|---|---|---|---|
| M1 `src/photo/photo.ts` 指回旧家 `../fetch/photos.js` | 搬迁面 | `tsc -b` exit=**2** | exit=0 |
| M2 `src/goal/commands.ts` 重复登记老分片键 `calorie.view.goal-weight` | 声明面 | `gen:check` exit=**1**（报「命令键重复登记」） | exit=0 |
| M3 `src/cli/registry.ts` 改一个字节 | 免费判据 | `gen:check` exit=**1** | exit=0 |
| M4 `src/goal/goalStore.ts` `resumeState` 文案改坏 | 语义面 | goal 用例 exit=**1** | exit=0（另 M2B 同型复读一次：1／0） |

- 本席**未新增 src 级变异**：派单红线要求本席不改 `src/`／`packages/`／`test/`，变异自证与红线冲突，故本席改为补两条**只读型新探针**（§5.1「须自设至少一个被审脚本覆盖不到的探针」的部分满足）：①**分派面逐行分类探针**（六窗对两分派文件的 `+/-` 行按「导入／注释／其他」分面计数，前席只给了 W1／W5 两窗的逐行原文，未做六窗全量分类）；②**跨窗生成物 blob 对照探针**（把「生成物五件不变」从单窗断言升级为跨窗对象级对照，正是它逮到了 `registry.ts` 的跨窗变化并定位到 #318 夹入提交）。两条都打在「脚本自我满足」的盲区上：前者的判据来自 git 对象，不来自任何窗口脚本的输出；后者是前席口径里**没有**的跨窗比较。**未做新的 src 级变异，如实记账**（见「未做的动态项」）。

## S1／S2／S3 清单

- **S1：0 条。**
- **S2-1（唯一 FAIL 源）· 六窗门禁对账面整体缺失**：W1／W3／W4／W5／W6 证据件 `GATE-RUN` 声明 0 条，反向对账存在无人声明的 `RUN` 条目（W1 = 8 条起），`check-gate-audit` 六窗全 FAIL ⇒ 判据⑥「证据 ＋ 对账导出」不成立。
  复现：`Select-String -Path docs/skills/skill-calorie/t324-W*-证据.md -Pattern 'GATE-RUN'`（W1/W3/W4/W5/W6 各 0 命中）＋
  `node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t324-W1-证据.md --log docs/skills/skill-calorie/t324-W1-gate-runs.log --ticket 324-land-W1` ⇒ `gate-audit: FAIL`／exit 1。
  修法（照 W2 返修先例，不由本席执行）：每窗把当刻门禁运行的 `GATE-RUN runId=… cmd="…"`（右引号处断行）写进自己的证据件，窗口退出后 `--export` 导出该窗 `gate-runs.log`，再重跑同一条审计至 `PASS`。
- **S3-1 · 跨窗生成物口径**：`registry.ts` 有一次跨窗变化，归属**范围外**（夹入的 `78472a3 feat(318)`，见维1）；建议把口径写实为「逐窗不变」而不是「六窗全程不变」，或在 #320 收口时点名这次变化。
- **S3-2 · 铁律五导出数**：新家目录 22 件对外导出名 >5（`analysis/anomaly/index.ts` 25、`workout/planStore.ts` 26 …）【继承 `_v-contract.txt`】。纯搬迁不许在票内改接口，故**不阻断关闭**；转票措辞：请开「实现件接口瘦身」票（归 #320 收口或新票），逐件把对外面收到 ≤5。
- **S3-3 · dist 孤儿产物 58 件**：主树 `dist/` 比隔离对照副本多 58 件陈旧产物（`onlyIso=0`、`diff=0`）【继承 `_k-b0-summary.txt`】。建议 #320 收口前 `pnpm build` 重建并纳入快照对账，避免后来者把陈旧产物当事实。
- **S3-4 · 教训回灌**：六窗的对账缺失与 W2 同根因（窗口收尾脚本不写 `GATE-RUN`、不导出对账件）。建议把 `t324-W2r-复核报告.md` 第 169 行的教训（「把对账做成窗口脚本的收尾步骤，不要事后补」）回灌到 `.scratch/t314-319/六票落点规则.md`，否则 #320 之后的每一批都会重犯。

## 证据来源与归属

**本席当刻自跑（原始输出落盘 `.scratch/t324r2/`）**

| 项 | 命令（要点） | 读数 |
|---|---|---|
| 免费判据 · 逐窗生成物 diff | `git diff --numstat <窗>~1 <窗> -- <五件>` ×6 | 全空（`diff=0`），逐窗 parent == self |
| 免费判据 · 当刻 sha256 | `Get-FileHash -Algorithm SHA256` ×5 | `5c81bfbd…`／`4db43569…`／`ca17d2c3…`／`e9ea9935…`／`4d44f03f…` |
| 免费判据 · 跨窗变因定位 | `git log --oneline 3cda469..b59d4bb -- <registry.ts>` | `78472a3 feat(318)…（核验与补齐 · 窗口 A）` |
| 改动账 | `git diff --shortstat/--name-status <窗>~1 <窗>` ×6 | 见维1 |
| 声明面（源码侧） | `Select-String … src\*\commands.ts -Pattern "key:\s*'…'"` | 10 件／100 行／100 唯一／0 重复 |
| 老分片残留 | `Select-String … src\cli\legacy\*.ts` | 1 条：`calorie.view.goal-weight`（`scene-03.ts:12`） |
| 分派面 | `git diff <窗>~1 <窗> -- cli/write.ts cli/cmd_read.ts` ×6＋行分类 | `0/0/6/2/0/4`，**非导入非注释 = 0** |
| 全量（持锁） | `run-locked --ticket t324r2 -- node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"` | `tests 1492 / pass 1492 / fail 0` |
| 冻结面（持锁） | `run-locked --ticket t324r2 -- node --test test/calorie-routing-81.test.mjs` | `tests 8 / pass 8 / fail 0` |
| 自治验收（持锁） | `run-locked --ticket t324r2 -- node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `P1..P5 5/5`、`UNACCOUNTED=0`；runId `9977c045…` |
| 对账复现 | `node tooling/check-gate-audit.mjs --evidence t324-W1-证据.md --log t324-W1-gate-runs.log --ticket 324-land-W1` | `matched=0/0 scoped=8 undeclared=8`、FAIL、exit 1 |
| 证据件 `GATE-RUN` 计数 | `Select-String -Pattern 'GATE-RUN'` ×6 件 | W1/W3/W4/W5/W6 = 0；W2 = 11 |

**前席落盘、本席只引用（`.scratch/t324r/`）**：`_a/_b-facts`、`_c-decl`（dist 侧声明计数 100＋1）、`_c2-legacy-remain`、`_d-refs`（引用点同步）、`_e*`、`_f-audit-raw`、`_g-gate-summary`（门禁链原始输出：`gen:check` PASS、`tests 1492`、`P1..P5`、`snapshot-check artifacts=186 changed=0`、`gate-audit exit=2` 因缺 `--evidence`）、`_h-audit-summary`（六窗审计 FAIL 逐窗原始输出）、`_i-ticket`（#324 票面）、`_j-iso-baseline`／`_k-b0-summary`（隔离副本与 dist 对照：`onlyMain=58 onlyIso=0 diff=0`）、`_l-mut-summary`／`_p-mut-summary`／`_q-m2b`（变异自证）、`_m-placeholder`／`_w-final`（占位符与控制符自检 0）、`_n-claims`（逐窗门禁读数）、`_o-caps`（各窗 `commands.ts` 能力数）、`_r-w1-headings`、`_s-w1-residue`、`_t-home-frozen`（入边计数与冻结地基）、`_u-dispatch`（W1／W5 分派面逐行原文）、`_v-contract`（铁律五导出数与共用层消费方）。
**本席未复算前席的这两项**：`check-gate-audit` 其余七次运行的完整原始输出（只引用 `_h` 的汇总读数）、隔离副本 `iso/` 与 `dist/` 的 58 件孤儿逐件清单（只引用 `_k`）。

## 本席自跑的门禁声明（§2.4.4 · GATE-RUN）

本席三条持锁运行（`--ticket t324r2`）逐条声明如下（每条 `cmd="…"` 的右引号处即行尾，不用表格包住）：

GATE-RUN runId=a3cb4df6-254e-4a2b-8ac9-4f858b245bbe cmd="node --test test/*.test.mjs packages/*/test/*.test.mjs"
GATE-RUN runId=a48edad3-a91c-41d0-a9f1-d5987530c60a cmd="node --test test/calorie-routing-81.test.mjs"
GATE-RUN runId=9977c045-1fd2-4e3c-bcaa-f86b48968b1a cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"

自对账（**本席实跑**）：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t324r-复核报告.md --log .scratch/locks-land/gate-runs.log --ticket t324r2` ⇒ `RESULT: matched=3/3 auditEntries=219 scoped=3 undeclared=0`、`gate-audit: PASS`、exit=0。这份自对账同时是 **S2-1 的修法样板**：把 `GATE-RUN` 声明写进证据件后，反向对账当场闭合（W2 返修走的正是这条路）。本报告一旦再跑任何持锁命令，此读数即失效；本席之后不再跑持锁命令。

## 未做的动态项

1. **未新增 src 级变异**：派单红线禁止本席改 `src/`／`packages/`／`test/`，而 src 级变异必须改这些路径（前席的 M1–M4 已在红线生效前做完）。⇒ §5.1「自设新变异／新探针」这一条本席只以**两条只读型新探针**（分派面逐行分类、跨窗生成物 blob 对照）部分满足；若编排者要求一条**新的 src 级变异**，需另派一席在获准改 `src/` 的前提下做。
2. **未复跑 `pnpm gen:check`／`tsc -b`／`snapshot-check` 本身**（成本取舍）：这三项以「前席落盘原始输出【继承】＋ 五件 sha256 与当刻文件逐字相同【自跑】＋ 探针 P3 反假绿【继承】」三重旁证支持，**未由本席独立复跑**。
3. **未重做 W2**：按派单只引用 `t324-W2r-复核报告.md` 的判定（PASS／93）与口径。
4. **未验 W2 返修件之外的历史运行**：`check-gate-audit` 的宽窗口径下还会报出更多未声明 `RUN`（W2r 报告 §S3-5 已点名 3 条），本次未展开。
5. **未做隔离副本复算**：`iso/` 侧读数（`onlyIso=0 diff=0`）只引用前席落盘；本席未进 `iso/`（派单明令不递归该目录）。
