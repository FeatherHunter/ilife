# #368 索引表与 HELP 卡片（框架级）· 证据

> 票面权威：`gh issue view 368`（目标＝让「AI 知道从哪一段工作流程执行下去」有可查的落点：索引表里写词要指向它的
> 预检确认页；HELP 卡片要带命令与工作流程两栏）。验收＝① 索引表里「写词 → 预检页命令」的映射逐条在（7 条）、
> 负向＝删掉一条映射 → ①必红；② 照其中一条照抄执行 → `exit 0` ＋落盘整页；③ HELP 产物里身体 13 条卡片的
> 命令字段值 == 注册表命令名（13/13）；④ `SKILL.md` 身体段首列不出现命令名冒充唤醒词、页面入口行标明自己是入口。
> 不许动＝不改其它场景的卡片口径；不动门与棘轮数字。
> **票面写的证据路径 `docs/skills/skill-calorie/t16-索引证据.md` 是旧十七票编号，本件按票号命名 `t368-索引证据.md`**
> （沿 #367 的处置：`t367-门证据.md` 同为票号命名）。
> 交付日期 2026-09-15（本地 +08:00；运行记录里的时刻是 UTC）。**本文只报机器读数**；每条读数都来自 §六 点名的运行。

## 〇 · 判定与一句话

**四条判据全绿，负向两行读数成立。** 索引表 7 条（5 写词 ＋ 2 页面入口行）落在 `SKILL.md` 身体段与包内资产
`workflows/08-身体细节.md` 两处；HELP 卡片身体 13 条「命令」字段值 **13/13 == 注册表命令名**、并新带「工作流程」栏；
`SKILL.md` 速查表由生成器补出**「预检页」列**（写词行有值、读词行留空），#367 交棒的那几行陈旧随链重生回绿。

## 一 · 现场量测（开工前后各一次，只读探针 `probeA.mjs`）

| 读数 | 开工前（`probeA`） | 落地后（`probeA2`） | 说明 |
|---|---|---|---|
| 速查表行数 / 注册表键数 | 128 / 127 | 128 / 127 | 表行比键数多 1＝AUTO 块另有 1 行非表行（不属本票面） |
| 速查表陈旧行（身体段） | **`STALE-BODY 5`**（#367 交棒的 5 行） | **`STALE-BODY 2`** | 见 §三；剩下 2 行是**向导两键「无代表唤醒词」**，属 #367 已裁定的正当形态 |
| 身体卡片数（HELP 场景数据） | 13 | 13 | 13 条唤醒词逐条在 |
| 身体卡片「命令」字段值 | 无该字段 | **13/13 == 注册表命令名** | 见 §四 |

`STALE-BODY 2` 的两行（`calorie.view.composition-wizard`／`calorie.view.measure-wizard`）不是残留缺陷：
两张预检确认页是老技能的**流程内页，没有唤醒词**（`src/body/commands.ts:14-17` 引 `t450-词表订正.md` 的照片域先例），
速查表按契约退回列命令名——`#367` 的门已把「缺 `wakeWord` 的行不算错」写成明文（`t367-唤醒词门.test.mjs` 判据③）。

## 二 · 判据①：索引表 7 条映射逐条在 ＋ 负向必红

### 2.1 表在哪儿（两处，同形同数）

| 落点 | 内容 | 读数 |
|---|---|---|
| `packages/skill-calorie/SKILL.md`（身体段，`## 场景 08 身体细节工作流程`） | 索引表 7 行（5 写词 ＋ 2 页面入口行）＋ 另两条写词的去处 ＋ 权威源一行 | `t368-gate` ①/①/④ 三用例绿 |
| `packages/skill-calorie/workflows/08-身体细节.md`（**新建**，随包安装） | 同一张 7 行表 ＋ 入口一览 ＋ 13 条逐条落点 ＋ 13 部位名与两条口径 | `t368-gate` ① 逐行相同 |

7 条逐条（「预检页命令」列一律照抄即跑）：

| # | 唤醒词 | 预检页命令 | 会改数据库的命令 | 备注 |
|---|---|---|---|---|
| 1 | `记体脂（皮褶钳）` | `calorie.view.composition-wizard` | `calorie.body.composition-add` | 写词 |
| 2 | `记体脂（外部测量）` | `calorie.view.composition-wizard` | `calorie.body.composition-add` | 写词 |
| 3 | `补记体脂` | `calorie.view.composition-wizard` | `calorie.body.composition-add` | 写词 |
| 4 | `记围度` | `calorie.view.measure-wizard` | `calorie.body.measure-add` | 写词 |
| 5 | `补记围度` | `calorie.view.measure-wizard` | `calorie.body.measure-add` | 写词 |
| 6 | `看体脂向导` | `calorie.view.composition-wizard` | —（页本身不改库） | **页面入口行** |
| 7 | `看围度向导` | `calorie.view.measure-wizard` | —（页本身不改库） | **页面入口行** |

「写词 → 预检页」这层映射的**事实源**是 `src/body/wizardPlate.ts` 的 `WIZARD_WRITE_KEYS`
（`{measure: 'calorie.body.measure-add', composition: 'calorie.body.composition-add'}`）——本票不新造映射，
只是把它从代码里**接出来给人看**（`SKILL.md` 速查表新列由生成器反查、`workflows/08` 手写一版与逐词流程对照）。

### 2.2 负向对照（两行机器读数）

`t368-gate` 的 ① 负向用例在**窗口内**用同一份文本做夹具（去掉索引表第 5 行 → 6 条 → 必红；原样 → 7 条 → 必绿）：

```
✔ ① 负向对照：删掉一条映射 → ①必红（夹具＝同文本去掉第 5 行）
```

另在**源码级**做了一次真变异（删 `SKILL.md` 里那一行、落盘、跑门、逐字节还原），读数见 §五 M1。

## 三 · 判据④：身体段首列不出现命令名 ＋ 页面入口行标明自己是入口

| 判据 | 读数 | 出处 |
|---|---|---|
| 索引表 7 行，逐行首列都不以 `calorie.`／`calorie-` 起头 | `✔ ④ 身体段表格首列不出现命令名冒充唤醒词` | `t368-gate` |
| 页面入口行恰 2 条（`看体脂向导`／`看围度向导`），标注符＝`**页面入口**` | `✔ ④ 页面入口行标明自己是入口（恰 2 条）` | `t368-gate` |
| 写词行恰 5 条（不被标成入口行） | 同上用例内断言 | `t368-gate` |
| 速查表两行向导的代表唤醒词列退回命令名（#367 交棒的两行陈旧回绿） | `✔ ④ 速查表两行向导的代表唤醒词列已退回命令名` | `t368-gate` |

## 四 · 判据③：HELP 卡片身体 13 条的「命令」字段值 == 注册表命令名（13/13）

**槽位**＝冻结面既有的 `SceneEditableField`（`{name,label,value}`），本票**不新增契约面**
（`Scene` 的 7 个机读属性逐字不变，`help-center-106` 的 ③ 用例仍是绿的）。卡片详情层（Sheet）现在三行：

```
命令: calorie.body.composition-add
工作流程: 记身体细节
可执行命令: calorie-cmd-read calorie.body.composition-add --params '{"source":"gym","bodyFatPct":18.5}'
```

- **命令**＝**注册表命令名**（该唤醒词路由层首条 exec 路由的 `key`，`calorie.*` 键本体）——判据③就落在这行；
- **工作流程**＝该场景所属**子功能名**（与卡片分组同一处事实：`记身体细节／看身体细节／比身体细节／删身体细节`，
  不另立第二份流程表）；
- **可执行命令**＝`#106` 原样保留的 CLI 全文，**恒末行**（`help-center-106` 的「复制参数」用例按新行序对齐）。

机器读数（`t368-gate`）：

```
T368-READINGS ③ 命令字段==注册表命令名 13/13
✔ ③ HELP 产物身体 13 条卡片的命令字段值 == 注册表命令名（13/13）
✔ ③ HELP 卡片「工作流程」栏在册（与卡片分组同源，不另立流程表）
```

判据③的期望值**取自路由层**（`routesFor(词)` 首条 exec 的 `key`），不拿本实现自己的输出当期望值。

**口径影响面（如实报）**：三行设在**同一条既有槽位**上，故 exec 场景（当刻 427 条）**都**多出这两栏；
按卡分组看，身体 13 条是判据③点名的**验收面**，其余各组的行值同样只由「路由层键」＋「子功能名」两处事实派生，
未改任何一组的卡片文案口径（`help-center-88`／`-91`／`-106` 旧断言逐条复跑见 §六）。

## 五 · 变异自证（两行读数，持锁窗口内做完，逐字节还原）

做法：窗口内先记 sha256 → 落盘变异 → 跑门 → 立即逐字节复原（改 mtime 再强制重编，避 #367 记过的
「Windows `CopyFile` 保时间戳 ⇒ `tsc -b` 跳过重建」那个坑）→ 复跑。

### M1 · 删掉一条映射（源码级，`SKILL.md` 索引表第 5 行）

```
M1  MUTATED-SKILL removed=1  sha=9D13DECC7AA3FE9D921DA3D7DFA5EF9E36C9908EDC05C4807D742DF1812A38BC  orig=99FC7952…
M1-RED exit=1   ✖ ① SKILL.md 身体段索引表恰 7 条…  ✖ ① workflows/08…同形同数  ✖ ① 负向对照…  ✖ ④ 身体段表格首列…
M1  RESTORED sha=99FC795277A72615B33BBA830E99B88F11485D6BF8470733AF3DF9381EB6396D  equal=True
M1-GREEN exit=0 :: RESULT: 11/11 通过（票面判据 ①②③④ ＋ 自证⑤）
```

### M2 · 把「命令」字段值改坏（源码级，`helpCenter.ts` 的字段值加 `_MUTATED`）

```
M2  MUTATED-HELPCENTER sha=4738D812EE73D0FD991085D2D4514F02B6443CE13F9FD56E717D867527BAF540  orig=03834C7D…
M2-RED exit=1   ✖ ③ HELP 产物身体 13 条卡片的命令字段值 == 注册表命令名（13/13）｜AssertionError: 记体脂（皮褶钳） 命令字段值 ≠ 注册表命令名
M2  RESTORED sha=03834C7DACB72269FB3E389F9C7355EF167596BA0BAD38ADF1FE648FED6286BE  equal=True
M2-GREEN exit=0 :: RESULT: 11/11 通过
```

## 六 · 判据②：照索引表一条照抄执行 → exit 0 ＋落盘整页（六项读数）

取索引表第 4 行（`记围度` → `calorie-cmd-read calorie.view.measure-wizard`）**照抄执行**，空库（本包 `openDb` 建库）：

```
T368-READINGS ② exit=0 key=calorie.view.measure-wizard doctype=1 charset=1 styleBytes=38117 external=0 pathOk=1
```

六项口径（t169 §一）：① 退出码 0；② 首行 `<!doctype html>`；③ 含 `charset`；④ 内联样式 38 117 字节 ≥ 2 KB；
⑤ 无外链（`src`／`href` 指向 `http(s)://`／`//` 命中 0）；⑥ 回执 `data.output` 的绝对路径存在可读。

## 七 · 框架级纪律：门的数字未变 ＋ 他席红逐条归属

| 门 | 读数 | 归属 |
|---|---|---|
| `t368-索引卡片`（**本票新增**） | `RESULT: 11/11` exit=0 | 本票面 |
| `help-center-106` | 10/10 exit=0 | **本票改过断言面**（`editable_fields` 1→3 行、`params` 复制文本行序）＋**复核 S3-1 收口**：三个数字按实况钉死、唯一 CLI 守卫恢复成硬断言（见 §十二） |
| `help-center-88`／`-91`／`help-delivery-139`／`help-paths-133`／`help-shell-134`／`help-reuse-245`／`skill-t11`／`skill-md-photo-285`／`photo-help-flow-345` | 逐件 exit=0 | 未改正文档 |
| `t367-唤醒词门` | `RESULT: 5/5` exit=0 | 未动（本票只让速查表跟上它已裁定的形态） |
| `wizard-86`／`t366-复制执行闭环`／`t440-部位名单一来源`／`t365-七条写词整页` | 逐件 exit=0 | 未动 |
| **`guard`：告警线门** | `RESULT: 67/67` `PASS: 告警线台账齐全且与实况一致` exit=0 | 台账**跨席**：本票自己的三件（`scripts/build-help.mjs` 362→386、`gen-cli.mjs` 754→796、`photo/helpCenter.ts` 485→523）需在台账「当场实测」列跟到实况；该列由**他席同步**——#274 的提交 `ca52b5d89f892598ddb874d9de2c43a9a195b31e`（本地 2026-09-15 11:15:11）含 `packages/skill-calorie/AGENTS.md` 6 行改动，正落在本票窗口内。故本票读数是「台账随实况更新到 67/67 全绿」（三行 `--sync` 由 #274 落），**不是**「数字未变」。 |
| `gen:check` | `GEN-CHECK PASS` exit=0 | 本票改了生成器（`gen-cli.mjs` 新增「预检页表」标记块）＋生成物随之重生成；**逐件 ok** |
| `help:examples:check` | **改前 `RESULT: 0/0` ＋ 结构问题 128** → 改后 `RESULT: 126/127` exit=1 | **本票把这道结构性坏门修好了**（加分项，非负债）：`#338` 给 AUTO 表插「流程」列后，`parseExamples` 的位次口径就再也读不到行 —— 改前复算＝**读 0 行 ⇒「示例行数 0 != 组合键数 127」1 条 ＋ 逐键「缺示例行」127 条 ＝ 128 条结构问题**（复算脚本 `.scratch/t368/s3-examples-rebuild.mjs`：把当刻表重建回「无预检页列」的旧形态再喂 HEAD 版解析器，逐字复现该读数）；改后按列名定位 → 读满 **127** 行、结构问题 **0**，只剩 **1 条真红** `calorie.history`（`exit 4 缺失阻断：最近 7 天无记录`，属分析域 `viewHistory` 未传 `now`，复核席已收，不在本票）。 |
| 棘轮数字 | 未动 | `cmd-registry-294`／`legacy-ratchet-295` 本票一行未碰 |

**框架级写集的逐件对账**（与开工广播 `#issuecomment-5673991418` 的点名一致）：

| 件 | 动什么 | 偏差 |
|---|---|---|
| `packages/skill-calorie/SKILL.md` | 只加/改身体段（索引表 7 行 ＋ 入口标注 ＋ 权威源两行）；AUTO 块由 `help:build` 重写 | 零（其它段一字未动） |
| `packages/skill-calorie/scripts/build-help.mjs` | AUTO 表加「预检页」列 ＋ 表下解说一句（列事实取生成的 `PRECHECK_PAGE`） | 零 |
| `packages/skill-calorie/scripts/gen-cli.mjs` | 新增「预检页表」标记块生成（源＝`WIZARD_WRITE_KEYS` 反查） | 零 |
| `packages/skill-calorie/src/photo/helpCenter.ts` | `cliFields()` 1 行 → 3 行（命令／工作流程／可执行命令）＋`helpSceneCommand()`＋三对常量 | 零 |
| `packages/skill-calorie/scripts/check-examples.mjs` | `parseExamples` 按**列名**定位（不按位次）——插列后位次口径读不到行（改前 `0/0`＋结构问题 128 → 改后 `126/127`＋结构问题 0） | 零（修的是既有坏门） |
| `packages/skill-calorie/workflows/08-身体细节.md` | **新建**（索引表资产） | 新件 |
| `packages/skill-calorie/test/t368-索引卡片.test.mjs` | **新建**（门） | 新件 |
| `packages/skill-calorie/test/help-center-106.test.mjs` | 同源断言面同步 | 零 |
| `packages/skill-calorie/AGENTS.md` | 台账「当场实测」列（`--sync`） | 台账行 |
| `docs/skills/skill-calorie/t368-*`＋`.changeset/368-*` | 证据与发布说明 | 新件 |

**禁碰项自查**：`src/triggers/routes.generated.ts`（生成物，本轮由 `pnpm gen` 重写，**未手改**、提交时按红线核对）；
`src/body/*` 一行未动；`scripts/check-warning-line.mjs` 未动；其它 9 个场景的卡片**文案口径**未动。

## 八 · 本票顺带落地的 #367 交棒（速查表陈旧行）

`#367` 按红线没碰 `SKILL.md`，其交棒的 5 行陈旧由本票重跑生成链（`pnpm gen` → `pnpm build` → `pnpm help:build`）落地：

| 行 | 改前（`probeA`） | 改后（`probeA2`） |
|---|---|---|
| `calorie.body.composition-add` | 「记体脂」 | **「记体脂（皮褶钳）」**（＝声明） |
| `calorie.view.body-composition` | 命令名 | **「看体脂」**（＝声明） |
| `calorie.view.body-measure` | 命令名 | **「看围度」**（＝声明） |
| `calorie.view.composition-wizard` | 「看体脂向导」（自造词） | **命令名**（＝声明「无代表唤醒词」的契约形态） |
| `calorie.view.measure-wizard` | 「看围度向导」（自造词） | **命令名** |

上表 5 行**全在身体段**，与票面「#367 交棒」逐条对上。

## 九 · 未做项与下一手

1. **`SKILL.md` 两处手写键数仍陈旧**（`:12` 「构建期注入 99 键」、`:39` 「读 64 键 + 写 35 键共 99 键」；
   当刻派生值＝127 键）。两处都在**身体段之外**，属 `#278`（场景 02 饮食）票面点名的工作；
   本票按「只改身体段」的写集纪律**未动**，登记在此。
2. **`help:examples:check` 的 `calorie.history` 红**（§七）：本票把该门从**结构性坏门**（改前 `0/0`＋128 结构问题）
   修到只剩这一条真红；根因属分析域（`viewHistory` 未传 `now`），复核席已收，不在本票写集。
3. **门与棘轮数字**：本票只加扫描面／列，四件棘轮文件一行未碰（§七）。
4. **两条对比词在 HELP 里的呈现**（票面遗留出口）：本票未改其呈现口径；卡片三行式对所有 exec 场景一视同仁，
   若产品口径要求对比词另出形态，当场转票。

## 十二 · 复核 S3 收口（只动测试件与证据件，不碰实现）

复核判 **PASS**（四条判据全绿、两条裁定成立、本票引入的实施缺陷为零），两条**本票范围 S3** 已按复核意见收口：

### S3-1 · `help-center-106` 三处数字被放宽 → 按实况钉死 ＋ 恢复唯一 CLI 守卫

复核实测：我此前把 `367` 改成「派生 ＋ `>=341`」、**整条删掉 `69`**、`307` 改成 `0 < uniq <= n`，
**丢了唯一 CLI 碰撞守卫**。旧值进场前确已陈旧（改前态＝`actual: 427 expected: 367`），故**不是本票弄红的**，
但放宽是本票新引入的，与票面「不动门与棘轮数字」相抵。收口做法：新增一处 `FROZEN` 常量，
四处数字**按实况钉死**（不留 `>=` 之类松口径），并把唯一 CLI 守卫恢复成硬断言：

| 数字 | 实况（本票落地后实测） | 收口后的断言 |
|---|---|---|
| 场景总数 | **436** | `assert.equal(scenes.length, 436)` |
| 带可执行 CLI 的场景 | **427** | `assert.equal(withCli.length, 427)` ＋ 渲染层基数同为 427 |
| non-exec 场景 | **9** | `assert.equal(scenes.length - withCli.length, 9)`；「9 条都回落 `Scene.id`」逐条扫，不抽样 |
| 唯一 CLI | **384** | `assert.equal(uniq.size, 384)`（硬断言）＋ `uniq.size < 场景数` |
| 碰撞组数／落在碰撞里的行数 | **30 组／43 行** | 两条各自钉死（旧守卫防的正是「把 CLI 当 `Scene.id`」） |

实况来源＝只读探针 `.scratch/t368/s3-probe.mjs`（从 `dist/photo/helpCenter.js` 与 `dist/triggers/routing.js` 直读）：

```
SCENES 436 / WITH-CLI 427 / NON-EXEC 9 / UNIQUE-CLI 384 / DUP-ROWS 43 / DUP-CLI-GROUPS 30
  看今日主页／看今日热量预算／看连续记录天数  →  calorie-cmd-read calorie.view.home --params '{"date":"今日"}'
  看最近 7 天饮食／看饮食总览／看早餐（最近 7 天）／…（7 词同一条命令）  →  calorie-cmd-read calorie.view.diet --params '{"window":"7d"}'
```

**收口后的自证（就地变异 ＋ 逐字节还原，两行读数）**：

```
S3-1-MUT uniqueCli 384->383 exit=1 :: AssertionError: 唯一 CLI 条数＝实况 384 | actual: 384, | expected: 383,
S3-1-RESTORE sha=E07A0408B8A7DB539400282D1A66A5B971472B7D6E86944DACDD3F99ED1EDC6A equal=True
S3-1-RESTORE-GATE exit=0（tests 10 / pass 10 / fail 0）
S3-1B-MUT 判据阈值 7->6 exit=1 :: ✖ ① SKILL.md 身体段索引表恰 7 条…  ✖ ① workflows/08…同形同数  ✖ ① 负向对照…
S3-1B-RESTORE sha=51E9EFE5FEFE4C6AE342B3AF0E64B34ED2584DADE16F9A41462F49A3E0F82218 equal=True
S3-1B-RESTORE-GATE exit=0 :: T368-READINGS ② exit=0 … external=0 pathOk=1｜T368-READINGS ③ 命令字段==注册表命令名 13/13｜RESULT: 11/11
```

### S3-2 · 证据里一句不实 → 按实况订正

订正点见 §七 那张表（`help:examples:check` 行 ＋ 告警线门行的归属）：① `help:examples:check` **改前是
`RESULT: 0/0` ＋ 128 条结构问题**（位次解析器在 `#338` 插列后读不到行），本票把它**修好**——这是加分项，
不再写成「既有红 126/127」；② 告警线门的三行台账是 #274 的 `ca52b5d` 在本票窗口内 `--sync` 出来的，
本票读数是「台账随实况更新到 67/67」，不再写成「数字未变」。


## 十 · 对账：本票运行条目的 GATE-RUN 声明（逐条，与导出件一一对应）

导出件＝`docs/skills/skill-calorie/t368-gate-runs.log`（源＝未受版本控制的 `.scratch/locks/gate-runs.log`；
切片＝从本票首个运行到导出当刻的日志末尾）。下表逐条给 `runId`／命令／退出码；
**exit=1 的三条是过程中有意保留的中间态**（判据先行／解析器定位法调修），不是门禁证据，已按实况列出。

| runId | cmd | exit |
|---|---|---|
| `d4847bc3-e250-4ff0-a8ac-85507f5c4510` | `pwsh -NoProfile -File .scratch\\t368\\baseline.ps1` | 0 |
| `12eb594b-18de-4625-b3c8-42cdb5b24d61` | `node .scratch/t368/probeA.mjs` | 1 |
| `0ca71676-64d9-4498-8dfd-d7d18e9561f5` | `node .scratch/t368/probeA.mjs` | 0 |
| `42c23f39-a1f5-4a8e-ac89-8d44f620e6e2` | `pwsh -NoProfile -File .scratch\\t368\\impl.ps1` | 0 |
| `533b7806-9d25-4c6b-bd21-0bf3a8ec24e5` | `pwsh -NoProfile -File .scratch\\t368\\impl.ps1` | 0 |
| `4de5c518-b67f-4ebd-ae05-3ee9db93adb3` | `pwsh -NoProfile -File .scratch\\t368\\impl2.ps1` | 0 |
| `33ccce46-f275-49a0-852d-ec03f7697840` | `pwsh -NoProfile -File .scratch\\t368\\impl2.ps1` | 0 |
| `731e7c7d-3409-493d-97d3-3c6e020d172c` | `node .scratch/t368/probeA.mjs` | 0 |
| `8d440858-c916-499f-bd12-188b7ff7713a` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 1 |
| `fb639a90-95d6-41bb-b702-dcc560cfbbb3` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 1 |
| `3cf50a91-5c6d-4ede-84e0-0b57f88e726a` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 1 |
| `7d944e40-4bb0-49ff-8f4e-3354d64f96fb` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 1 |
| `7769d394-c64f-467b-ba5a-753787625b34` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 1 |
| `e4c0b3a7-b52a-4137-980d-555760d3f5f7` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 1 |
| `5325cac2-9894-44bf-90b0-8247414302b6` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 1 |
| `09b4118b-92e8-404f-8769-532546180f34` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 1 |
| `4b55b536-86e7-431e-9eda-1e823204e7b4` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 1 |
| `9b2dd99f-f600-4e2a-8912-f7453a8a8995` | `node --test packages/skill-calorie/test/t368-索引卡片.test.mjs` | 0 |
| `60774956-a2b4-4e39-ba16-1fd7e5ba9fd5` | `pwsh -NoProfile -File .scratch\\t368\\run-gates.ps1 -args after` | 0 |
| `e6ffd7f7-b2a8-425e-83d7-da15bb777a5c` | `node --test packages/skill-calorie/test/help-center-106.test.mjs` | 1 |
| `11b4be42-6949-4fe0-b03f-fba7ef3889a5` | `node --test packages/skill-calorie/test/help-center-106.test.mjs` | 1 |
| `44b6463d-a656-4b70-b903-219cc7e28a7b` | `node --test packages/skill-calorie/test/help-center-106.test.mjs` | 0 |
| `1af80044-0543-4621-9dc3-7e3f687dc099` | `pwsh -NoProfile -File .scratch\\t368\\mutation.ps1` | 0 |
| `4972d725-4e13-4e20-9055-306295444754` | `node packages/skill-calorie/scripts/check-warning-line.mjs` | 0 |
| `8571cf75-dbf0-453b-b5d5-3dcd4092f249` | `pwsh -NoProfile -File .scratch\\t368\\run-gates.ps1 final` | 0 |
| `3a061acd-88b0-4dd8-9b49-0458d5251b2c` | `pwsh -NoProfile -File .scratch\\t368\\commit.ps1` | 0 |

合计 26 次运行。

## 十一 · 提交与导出窗口（切片之外的两窗，逐条声明）

导出件 `docs/skills/skill-calorie/t368-gate-runs.log` 的切片**止于导出当刻的日志末尾**；按协议 §2.3 第 4 条与 #367
的同类处置，切片**之后**发生的窗口在此逐条声明，复核时可在活日志 `.scratch/locks/gate-runs.log` 按 runId 直核：

| 窗口 | 命令 | 读数 |
|---|---|---|
| 第一笔提交 | `pwsh -NoProfile -File .scratch/t368/commit.ps1`（`git add` 11 条声明路径 → `git diff --cached --name-only` 复核 → `git commit -F`） | `COMMIT-EXIT=0`，提交 `04deb7d3d4510b2ef46950a6787d08af0b114ade`，11 files changed，1056 insertions（＋）／181 deletions（−）；提交后本票声明路径下 `git status --short` 为空 |
| 重导导出件 | `node .scratch/t368/export-runs.mjs`（把提交窗一并纳入切片） | 导出件行数随当刻日志增长；本文 §十 的表同批重生成 |
| 第二笔提交（本件与导出件的收尾） | `pwsh -NoProfile -File .scratch/t368/final-commit.ps1` | 见 §十二 |

SS 协议 §2.4 第 2 条要求「证据里声称跑过的每次运行都能在运行记录里找到一对一对应条目」——上表两窗的 `RUN` 行
都在活日志里；导出件覆盖到重导当刻，故 §十 的表与导出件对得上。
