# #438 · 场景 09 看身材照页体积退让（证据）

一句话判定：`calorie.photo.list` 的页面交付已真按 `PHOTO_LIST_PAGE_MAX_BYTES`（1048576，未改值）逐张试嵌、超限退让 — 隔离超预算库产物 830,849 字节、真库同尺寸库 1,009,946 字节，均 ≤ 上限；横幅两数、占位原因、复制数据全量行、产物绝对路径在盘六条判据逐条有机器读数（下表）；变异自证「改坏必红、改回必绿」两行见 §三。票面判据全绿，唯 §五 第 1 条为**他人已提交件**的旧断言（非本票所致），按纪律未碰。

## 一、口径（老正本 → 本仓实施）

| 老正本 | 老正本做法 | 本仓落点 |
| --- | --- | --- |
| `scripts/render_body_photo_gallery.py:62-71` | 预算常量＋注释：全量内嵌会让页面超出可发送上限，「能发出去」优先于「一次看全」 | `galleryDoc.ts:27-31` 的 `PHOTO_LIST_PAGE_MAX_BYTES`（值 1048576 沿用，**未改**） |
| 同件 `:86-105` | `embed_budget` 逐张递减，耗尽即标 `embed_skipped`（前端显示「未嵌入」而非空白） | `photoThumb.ts:79-105` 的 `embedPhotosWithinBudget`：逐张试嵌、超剩余预算即记为预算未嵌 |
| 同件 `:132-133` | `embed_budget_bytes`／`embed_skipped_count` 进载荷 | 本仓按票面判据 4 **不进复制数据**：复制数据仍是全量行，退让只在页面内嵌这一处 |
| `templates/body_photo_gallery.html:143-151` | 横幅：已按预算嵌入前 N 张＋还有 M 张未嵌入＋用「标签」或时间筛选分次查看 | `galleryDoc.ts` 的 `budgetNoticeHtml`（**#472 起**走公共层 `notice()` 提示块；两数＝「本页只显示 N 张／还有 M 张没显示」＋「可按「标签」或日期分批看」） |
| 同件 `:156-163` | 网格三态：未嵌入（体积超限）／照片数据不存在／无法读取 | `galleryDoc.ts` 的 `notShownReason`：**#472 起**上屏的是人话四态——`照片太大，本页没显示`／`找不到文件：<文件名>`／`没配照片目录，读不到文件`／`读不出这张照片：<文件名>`（逐张原因表 `skipReason` 里的机器串仍是 `体积预算未内嵌`，它不再直接上屏） |

**现状注（#472 起，本件改后口径）**：**#438 的行为一条未变**——触发与 M 仍只按**预算跳过计数**
（文件缺失不进提示块）、复制数据仍是全量行、逐张试嵌与兜底收回轮照旧；变的只有**呈现**：
那段横幅由 #438 当时自造的裸 `div` 改成公共层静态提示块 `notice()`，句面换成「照片较多，本页只显示
N 张；还有 M 张没显示」＋「可按「标签」或日期分批看」，体积上限这类技术细节下屏。逐条对照见
`docs/skills/skill-calorie/t472-读侧A-文本.md`。

**下面 §二／§三／§八／§十 的读数与断言句是 #438 当时的机器读数（历史事实，本票一字未回改）**：
它们写的「已嵌 N 张／还有 M 张未嵌入」「体积预算未内嵌」是那时页面上的原句；测试侧 #472 已把断言
换成新句（`photo-budget-438.test.mjs` 现断言「本页只显示 N 张；还有 M 张没显示」）。
§一 那处「差一个『入』字」的口径差异已由 #472 的换句收口（现行句不再出现「已嵌入」这个写法）。

## 二、逐判据读数

读数脚本：`.scratch/t438/readings.mjs`（真跑 `calorie-cmd-read calorie.photo.list`），原始读数 `.scratch/t438/readings.json`。
场景 A＝隔离超预算库（4 张 560KB 正面照）；场景 B＝真库同尺寸库（14 张＝4 张 2.8–3.1MB＋10 张 105–251KB，真库只读搬尺寸）。

| 判据 | 场景 A 读数 | 场景 B 读数 |
| --- | --- | --- |
| ① 产物 ≤ 1048576 | **830,849** ≤ 1,048,576（同一批照片全量内嵌的字节合计 3,058,440 已超限，证用例真在超限区） | **1,009,946** ≤ 1,048,576（退让前同尺寸全量内嵌约 17.9 MiB） |
| ② 横幅两数＋替代操作 | 「已嵌 1 张／还有 3 张未嵌入」＋「替代操作：用「标签」或日期筛选分次查看」 | 「已嵌 4 张／还有 10 张未嵌入」＋同一替代操作句 |
| ③ 超出项走占位并写明原因 | 占位 3 张，原因逐张「体积预算未内嵌」（用例二另证文件缺失态写「文件缺失：2026-09-01_001.png」） | 占位 10 张，原因逐张「体积预算未内嵌」 |
| ④ 复制数据仍是全量行 | `data.items` 4 行＝库内 4 行（不是内嵌 1 行）；`data.total` 4；明细表 5 行＝4＋表头 | `data.items` 14 行＝库内 14 行；`data.total` 14；明细表 15 行 |
| ⑤ 变异自证 | 见 §三 | 同左 |
| ⑥ `data.output` 绝对路径在盘 | 绝对路径、`在盘=true`（隔离库 `…\Temp\t438a-*\db\calorie_html\看身材照_*.html`） | 绝对路径、`在盘=true`（隔离库 `…\Temp\t438b-*\db\calorie_html\看身材照_*.html`） |

判据 ③ 的三种归属（预算／文件不在／读不出）在用例二里各有断言：删掉一张已入库照片的本体后，页里该张写「文件缺失：2026-09-01_001.png」，正常张仍按预算内嵌，页仍 ≤ 上限。

## 三、变异自证两行

```
MUTATION-RED 变异一/二（删横幅／占位原因换空话）＝横幅句缺失（须含「已嵌 N 张／还有 M 张未嵌入」） | 占位原因须是体积预算：原因见别处
MUTATION-GREEN 改回必绿 bytes=830849 limit=1048576
```

## 四、GATE-RUN 单行

```
RUN ticket=438 runId=86a03d15-4305-464f-b0c1-fa579bece7bf cmd="node --test packages/skill-calorie/test/photo-budget-438.test.mjs" waitedMs=0 exit=0 pid=30504 at=2026-09-14T12:34:54.927Z
```

（编译：同票 `pnpm --filter skill-calorie exec tsc -b`，runId `5cbc8c69-1b8d-4717-929f-c50e69251951`，exit=0。回归批：`f392753d-90e8-4370-8acd-143e2e9f3bbf`，7 件 44 例，43 绿 1 红，红的那件见 §五第 1 条。）

## 五、写集清单

改动（3 件）：

- `packages/skill-calorie/src/photo/photoThumb.ts`（改）：新增 `embedPhotosWithinBudget`／`EMBED_BUDGET_REASON`／`withoutDataUri`；117 行。
- `packages/skill-calorie/src/photo/galleryDoc.ts`（改）：横幅＋逐张试嵌＋兜底收回轮；163 行。
- `packages/skill-calorie/test/photo-budget-438.test.mjs`（新增）：三例（超预算真跑／缺失与预算各归各位／变异自证），本票六条判据逐条断言。

新增文档与草稿：

- `docs/skills/skill-calorie/t438-体积退让.md`（本件）。
- `.scratch/t438/`：`readings.mjs`／`readings.json`（判据读数）、`real-sim.mjs`、`probe.mjs`、`regress.log`、`baseline*.json`、`real-page.html`。

未碰（逐条复核过 mtime 或 diff）：

- `src/render/html.ts` 零加行（`git diff` 空）。
- `src/photo/commands.ts`、`src/cli/keys.ts` 本票未动（`keys.ts` 在 20:27 被别的票重生成，photo 面只多一条 `calorie.view.photo-picker`，`calorie.photo.list` 一字未改）。
- `compare.ts`、`viewerDoc.ts`、`receipt.ts`、`picker*` 一行未碰。
- `compare.ts`／`gifDoc.ts`（#352 席在改）一行未碰（mtime 19:46／19:50，早于本席开工）。
- 老技能目录 `D:\2Study\StudyNotes\SKILLS\卡路里` 只读（只读 §一 引用的三处行，未读 `.个人笔记不允许参考/`）。

收口席补的两处半成品（前席未回执处，均已修并复测）：

1. `photoThumb.ts` 预算跳过的张只在 `skipReason` 记原因、**没去掉 `dataUri`** → 页面照样全量内嵌（实测 3,124,376 字节），横幅也不出；改为跳过项入集前去掉内嵌字节。
2. `galleryDoc.ts` 横幅判据写反（`contentCount >= embeddedCount` 即返回空）→ 真退让时反而不出横幅；改为 `embeddedCount >= contentCount` 才返回空。
3. 用例五的变异二原先靠全页匹配 `/体积预算/`，会被横幅句满足而不判别；原因断言收到**占位句**上后变异二才真红。

## 六、终审影响（一句话）

真库 14 张（含 4 张 2.8–3.1MB 原图，原始字节合计 14,058,620＝13.41 MiB）时，看身材照页从约 17.9 MiB（base64 全量内嵌）降到 **1,009,946 字节＝0.963 MiB**（约降 18 倍），页里 4 张内嵌、10 张占位并写明「体积预算未内嵌」，4 张大原图**恒在占位里**（单张 base64 3.8–4.2MB 已超剩余预算，无论排序如何都嵌不进），复制数据仍是 14 行全量。

## 七、未做项与审查席重点

1. `packages/skill-calorie/test/cmd-read-t11.test.mjs:219` 断言 `doesNotMatch(html, /base64/)` 与**已提交件** `photo-shape-341.test.mjs`（要求画廊页含 `data:image/`）互斥：HEAD 的 `viewPhotoList` 就把照片目录传给了 `buildPhotoListDoc`，页内必含 base64，故该断言在 HEAD 上已是红的，非本票所致（同批 `render-t10.test.mjs:108` 走老渲染路径 `renderGalleryHtml`，不内嵌，仍绿）。本席按「他人已提交件一行不碰」未动它，请审查席定归属票。
2. `pnpm --filter skill-calorie build` 当前被 `scripts/build-help.mjs:139`／`:155` 重复声明 `const FLOW`（SyntaxError）挡住 —— 另一席在途改件，本席未碰；本票编译改走 `pnpm --filter skill-calorie exec tsc -b`。根 `pnpm build` 会跑 `gen-cli.mjs` 重写 `cli/keys.ts`（他人件），本席未跑。
3. 真库照片目录 `D:\2Study\StudyNotes\.db\CalorieHub` 现有 16 个文件（含 2 个 5 字节占位），票面样本取其中 14 张；本席对真库只读（列目录＋读文件字节），未跑任何对真库的命令。
4. 前席对真库跑过 `calorie.photo.list`（`.scratch/t438/baseline*.json`，20:04），产物落进真库 `calorie_html/`；本席未再对真库跑命令，建议审查席确认那两件产物是否需要清理。
5. 三态目前靠**占位句里的原因文本**区分（未嵌入／文件缺失／读不出），不是三个不同的样式标记；票面判据 3 要的是「该张位置明写原因」，已满足，若审查席要三态各有独立标记位，属加样式，另开小票。

## 八、整改席：#438 横幅两态口径（整改前后对照）

判定一句话：横幅的**触发**与 **M** 已改用**预算跳过计数**（`galleryDoc.ts:59-63` 的 `budgetSkippedCount`，实参取 `skipReason.size` 活表——预算挑选与兜底收回轮都记在该表）；文件缺失态仍只由缺失明示行承担、不进横幅。整改前「3 张正常照＋1 张缺文件、页仅 82,789 字节」也会弹预算横幅，现不再弹。

读数脚本 `.scratch/t438/recheck-banner.mjs`；原始读数 `.scratch/t438/recheck-before.json`／`recheck-after.json`（同一脚本、同一批场景，只差源码口径，逐字节可比）。

「整改前」是**真编出来再跑的**，不是推演：把 `galleryDoc.ts` 的横幅触发还原成旧口径（`if (embeddedCount >= contentCount) return '';` ＋ 实参 `embeds.length`）→ `tsc -b` → 跑读数与测试；跑完当场改回并重编译（改正后 `dist` 口径为 `if (budgetSkippedCount <= 0)`，两次编译 exit 均 0）。

| 自查场景 | 整改前读数 | 整改后读数 | 合口径 |
| --- | --- | --- | --- |
| ① 仅缺失、无预算跳过（4 张 4KB 小图，删第 1 张本体） | **出横幅**「已嵌 3 张／还有 1 张未嵌入」；页 82,789 字节 | **不出横幅**；页 82,591 字节（差 198 字节＝横幅那一段）；缺失明示句「缺失照片 1 张：」＋「文件缺失：2026-09-01_001.png」仍在 | 缺失不进横幅 |
| ② 纯预算超限（4 张 560KB，无缺失） | 出横幅「已嵌 1 张／还有 3 张未嵌入」；页 830,973 字节 | 同左，一字未改 | N／M 正确 |
| ③ 混合（4 张 560KB，删第 1 张本体） | 出横幅「已嵌 1 张／还有 **3** 张未嵌入」（把缺失那张算进 M） | 出横幅「已嵌 1 张／还有 **2** 张未嵌入」 | M＝预算跳过数，不含缺失数 |
| ④ 票面六判据 | — | 加锁真跑 4 例全绿（见下 GATE-RUN）；六条判据一字未松，整改只加断言不加宽 | 全绿 |

③ 的两种归属另逐张核过：2 张占位写「体积预算未内嵌」、1 张写「文件缺失：2026-09-01_001.png」；明细表 5 行、复制数据 4 行＝库内 4 行（只截断内嵌，不截断数据）。

测试侧改动（`packages/skill-calorie/test/photo-budget-438.test.mjs`）：新增一例「#438 整改自查」（① 与 ③ 的机器断言）＋断言件 `assertNoBudgetBanner`；变异自证补**变异三**——把横幅**触发条件**还原成旧口径（在「仅缺失」页上注入旧口径那一段横幅）必红。原有变异一／二与票面六条判据一字未动。

### GATE-RUN（本席两条）

```
改坏（源码级变异：横幅触发还原旧口径）
RUN ticket=438 runId=7a9de29a-8366-4ffd-ac25-0ac4ed244de9 cmd="node --test packages/skill-calorie/test/photo-budget-438.test.mjs" waitedMs=1 exit=1
  → tests 4 / pass 2 / fail 2；红的是「整改自查」与「变异自证」两例，理由「仅文件缺失时不该出体积横幅（缺失不进横幅）」
改回
RUN ticket=438 runId=0ac74bc4-4246-4084-9058-96400e232521 cmd="node --test packages/skill-calorie/test/photo-budget-438.test.mjs" waitedMs=0 exit=0
  → tests 4 / pass 4 / fail 0
MUTATION-RED 变异一/二/三（删横幅／占位原因换空话／触发条件还原旧口径）＝横幅句缺失（须含「已嵌 N 张／还有 M 张未嵌入」） | 占位原因须是体积预算：原因见别处 | 仅文件缺失时不该出体积横幅（缺失不进横幅）
MUTATION-GREEN 改回必绿 bytes=830973 limit=1048576
同票形状：node --test packages/skill-calorie/test/photo-shape-341.test.mjs → tests 4 / pass 4 / fail 0（exit=0，未被整改改坏）
最终态复跑（同一代码态，整改席收工前）
RUN ticket=438 runId=3d157edb-c803-41c1-98a7-0b094d001ee0 cmd="node --test packages/skill-calorie/test/photo-budget-438.test.mjs" waitedMs=0 exit=0 → tests 4 / pass 4 / fail 0
```

## 九、整改席：真库残件清理（备份 → 删除 → 复查，三态）

背景：前席 20:04／20:07 对真库跑过三次 `calorie.photo.list`，三件产物落在 `D:\2Study\StudyNotes\.db\calorie_html\`，都是改前的超限产物。本席只做「备份 → 删这三个文件」这一种写操作：未对真库跑任何命令、未碰 `CalorieHub`、未用通配符、未删目录。

原件读数（删除前当场重算，不采信转述）：

| 文件 | 字节 | mtime | sha256 |
| --- | --- | --- | --- |
| `看身材照_20260914_200402.html` | 202,563 | 2026-09-14 20:04:02.044 | `1749B3D336211DA5A505C316AAD2E7FF5E47FE0B5AA5A78368021554E1F74768` |
| `看身材照_20260914_200454.html` | 5,030,957 | 2026-09-14 20:04:54.377 | `13A79682C778942EDEDD6F0ED64EF5063D10F316BDFBF739C99017F39A9F4A0D` |
| `看身材照_20260914_200712.html` | 5,031,319 | 2026-09-14 20:07:12.046 | `06A34431238F329B6760B5DA84DEB2213F55E88ED1BFE3635C601CC1459AAB60` |

三态读数：**备份**到 `.scratch/t438/real-db-backup/`，三件逐件复算 sha256 与原件一致（字节亦一致）；**删除**只按这三个精确文件名 `Remove-Item -LiteralPath` 逐件办，删后逐件 `Test-Path=False`；**复查**——`calorie_html` 文件数 366 → 363（差恰为 3），`calorie_data.db` 删前删后字节 3,072,000、mtime `2026-09-14 13:53:37.729` 均未变，同前缀残留 0 件。

另：`.scratch/t438/real-page.html` 与已删的 20:04:54 那件**逐字节同源**（sha256 `13A79682…9F4A0D`，5,030,957 字节），佐证两件是同一个超限产物。

## 十、整改席：审查席指出的两处口径瑕疵（如实登记）

1. **变异的自证属产物字符串级，不是源码级。** 测试里三条变异都做在**产出的 HTML 字符串**上（删横幅／换占位原因／注入旧口径横幅），不在源码上改完再编译。本席另补了一次**真源码级**变异循环（还原横幅触发 → `tsc -b` → 测试 exit=1 → 改回 → `tsc -b` → 测试 exit=0，runId 见 §八）；但**测试文件里**那两行机器读数仍是产物字符串级——要让测试自己改源＋编译＋跑，属另一张小票，本席未做。
2. **（#479 陈化对齐：本条已收口，见下补记）横幅文案用票面「已嵌 N 张」，t400 基准写「已嵌**入** N 张」，差一个「入」字。** 本席按票面逐字保留（改文案会让票面判据 ② 的断言句与产物句脱开）；**择机对齐 t400**——对齐动作归 t400 那侧或另开小票，**本席未动 t400**。
   **（#479 陈化对齐补记）**：这一处已由 #472 的**换句**收口——页上现在是「照片较多，本页只显示 N 张；
   还有 M 张没显示」＋「可按「标签」或日期分批看」，**「已嵌／已嵌入」之争随旧句一起消失**；
   t400 裁定 3 的措辞已同步改写（见该件裁定 3）。本条的登记性质＝**已完成**，不再是待办。

### 整改席写集（本席）

- `packages/skill-calorie/src/photo/galleryDoc.ts`（改）：横幅触发＋M 两处口径（函数签名参数名／触发条件／M 取值／调用点实参）。
- `packages/skill-calorie/test/photo-budget-438.test.mjs`（改）：`seedIso(size)` 可调尺寸、`assertNoBudgetBanner`、整改自查一例、变异三。
- `docs/skills/skill-calorie/t438-体积退让.md`（本件）：追加 §八／§九／§十。
- `.scratch/t438/`：`recheck-banner.mjs`、`recheck-before.{json,out.txt}`、`recheck-after.{json,out.txt}`、`mutation-red.log`、`green.log`、`shape341.log`、`real-db-backup/`（三件备份）。
- 真库 `calorie_html/`：删上表三件（已备份）。

未碰：`photoThumb.ts`（20:25:15）、`test/cmd-read-t11.test.mjs`（20:40:51）、`scripts/build-help.mjs`（20:39:14）、`scripts/gen-cli.mjs`（20:32:17）、`SKILL.md`（20:39:17）、`CalorieHub` 目录、t400 相关件。本席未做任何 `git add`／`commit`／`push`／`stash`／`reset`／`rebase`／`checkout`。
