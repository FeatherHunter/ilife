# t630 证据 · 终审D2「看今日营养」题名对齐 ＋ 82–83 格去留裁定

> 票面：`gh issue view 630`（D2 场景02页归属整理，终审D2）。派单与票面冲突以票面为准——本件声明两处机械派生（§七）。
> 认领：`gh issue edit 630 --add-assignee "@me"`（开工第一动作）。
> 接手复验声明（本席，接手实施席）：本件为前席无主草稿，经本席逐项复跑校验后**沿用技术结论**；原 §五 GATE-RUN 运行号一律作废，以 `t630-复验.md` 本席运行号为准（取代关系：复验件取代本件 §二／§五的运行读数，本件其余章节保留）。

## 一 · 改动清单（手改 4＋1，派生 2）

| 件 | 改法 |
|---|---|
| `packages/skill-calorie/src/diet/routes.ts` | 只动 order 44「看今日营养」一行：cli 加 `,"entry":"today-nutrition"` ＋ #630 注释（沿 #511 标记做法） |
| `packages/skill-calorie/src/diet/review.ts` | 取数层：`optStr(params,'entry')` 透传给装配层；取数口径一字不动 |
| `packages/skill-calorie/src/diet/reviewDocs.ts` | 页装配：新增 `ENTRY_TODAY_NUTRITION` 常量；`DietReviewExtra` 加可选 `entry`；`buildDietReviewDoc` 里**只换页头三件**（题名／眉标／副题），徽章／导航／正文／复制区／脚注不动；362→380 行（+18，仍超线，不拆，见 §七） |
| `packages/skill-calorie/test/t630-今日营养题名.test.mjs` | 新建靶向探针 6 条（①默认分支一字不差／①今日营养页头／②体不动／③entry 三硬要求／④路由链照抄／⑤ in-test 变异） |
| `packages/skill-calorie/src/triggers/routes.generated.ts` | **非手改**：`pnpm gen` 派生，差分仅 order 44 一行（`git diff` 已验）；`GEN-CHECK PASS 键 129` |
| `packages/skill-calorie/AGENTS.md` | **非手改**：`check-warning-line.mjs --sync` 派生，仅 `reviewDocs.ts` 行 362→380；`RESULT: 91/91` |

分支后页头（真出口实测，`.scratch/t630/看今日营养-证据.html`）：H1＝`📝 今日营养`／眉标＝`看今日营养（饮食）`／副题＝`今日营养：本窗有记录 1 天：共摄入 … 卡…`（读数全保留，裁定 2）；默认分支（其余 7 条词）H1／眉标／副题与改前逐字相同（t273 全绿为证）。

## 二 · 门禁读数

- 编译：`node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` → exit 0（GATE-RUN `405986d0`；派生后重编 `c9a7c1c4`、`4d9dea61` 均为 exit 0）。
- 生成门：`gen-cli.mjs --check` → `GEN-CHECK PASS：键 129（写 47 ＋ 读 82）`（GATE-RUN `96629d99`，exit 0）。
- 台账门：本席复跑 `check-warning-line.mjs` → `RESULT: 88/91`，RED 仅他席在途两行（`build-help.mjs 744→743`、`sportDocs.ts 434→466`，本票零触及）；本文件行（`reviewDocs.ts` 台账 380＝实况 380）无 RED（GATE-RUN `0ba40123`，exit 1 系他席行所致；本席未跑 `--sync`，不代他席 sweeping 台账）。
- 靶向：t630 6/6 绿（GATE-RUN `f07385d3`，exit 0）；t273（本页归属 #273）全绿 30＋ 项；t275 中本页两格（看营养结构／看今日营养 host 框）全绿。
- 范围外 2 红（t275 看饮食总览，`calorie.view.diet` #271 页）：缺来源脚注 `· 共 10 条` ＋ 复制日志按钮已接日志（断言原文「接线/装配已变」）。归因：该页装配（`today.ts`／`todayDocs.ts`／`render/dietDocs.ts`）近期经 #588／#618 改动、本票 diff 零触及（§七），且该页只 import 本件未动的 `buildMealDistributionBlock`（类型 import 除外）。**范围外在途项，转 #271／收口，不由本票修（禁碰他人路径），不阻塞本票验收。**
- 目标页分隔符门：两证据页节点级命中各 2，归属 owner 集合＝{`ilife-block-caliber`}（来源脚注行，归 #560）⇒ **非脚注命中 0**（`.scratch/t630/audit.json`，`nodeHits=2 owners={ilife-block-caliber} nonFoot=0` ×2）。
- 目标页响应式（终审四档 390/512/820/1440）：`OVERFLOW-ZERO pages=2 cells=8 failed=0`；`clip=3` 均为 `ilife-block-dist-row-name` 78px（终审D1，`base-render` 分布行，#629 地盘，本票不碰）。

## 三 · 变异自证（两行机器读数）

```
MUTANT  runId=bb043f60-7067-4f43-8a26-04e4d7be2525 exit=1 tests=6 pass=3 fail=3（题名改坏→①④⑤红）
RESTORE runId=f07385d3-a538-43d8-b763-ed97bc64dcfd exit=0 tests=6 pass=6 fail=0（逐字节还原→绿）
```

改坏处：`reviewDocs.ts` 页头分支 `today ? '📝 今日营养'` 改成 `'📝 饮食复盘'`（单行）；还原后 `dist` 重编、`headTitle` 回原值（已验 `dist/diet/reviewDocs.js:198`）。

## 四 · 裁定纪要（D2 主结论 ＋ 82–83 去留）

1. **D2 主结论：`calorie.view.diet-review` 键归属场景 02（t313a §③场景02·24 键逐字在列），「看今日营养」留 02 页、题名对齐——不触发升级红线。** 落法沿 #511 同源入口页（作者裁定一类比）：8 词共页体不动，只换页头三件。备选「维持复盘题名＋书面口径」未取：唤醒词与题名一字对不上是终审实测扣分项（t605 文案维扣 3 分之一），有 #511 先例可抄，改动面可控。
2. **老实物注记（非改动依据）**：`scene-02-diet.ts:40` 记「看今日营养」老模板 `today_diet.html`（② 类今日页）、用户意图「看今日 4 项营养完成度」。现行 8 词共页把营养段嵌进复盘页（`buildNutritionRatioBlock`），今日营养读者在同页下半段读到配比环＋对比表；**迁回今日页族＝换键换页，超本票写集**（`todayDocs.ts` 不在写集），留待收口票评估，本票只做题名对齐。
3. **82–83 去留（书面裁定，不改字节）**：`calorie.view.nutrition-analysis`／`calorie.view.six-factors` 两键归属**场景 10**——复述 t313a §③（场景10·13 键逐字在列；`six-factors` 双提到取最小 scene＝10），非本票新擅断。**去留＝02 墙暂留**：摘除会破坏 83 条真跑完整性（t280 口径）且两页装配（`trendMiscPortDocs.ts`）超本票写集；场景 10 图认领后由收口票摘除／跳转，本票只出裁定（合票面遗留出口「只出裁定与跳转，不硬改 02 页」）。
4. **t425 回写判定：不回写。** 本票无口径变更（题名落位是标题执行、82–83 是执行 t313a 既有判定），`t425-融合基准.md` 文字无需改；且该件不在本票写集。收口票若迁页再议。

## 五 · GATE-RUN 对账（持锁运行全清单）

| runId | cmd | exit | 备注 |
|---|---|---|---|
| `405986d0-8e6c-4135-a3ef-55b33b0b67d8` | `tsc -b packages/skill-calorie --force` | 0 | 初编 |
| `117a6e86-61e4-4ebc-a834-092f2a4821be` | `node --test t630` | 1 | 派生前负对照（④ 红，预期内） |
| `b4950437-ccdc-4226-8545-e097ed0fdf80` | `gen-cli.mjs`（写盘） | 1 | GEN-STALE 拒写（门行为正常），转 stamp |
| `f8df41bd-cb2f-43ce-8632-454492e5c569` | `tsc -b`（全仓） | 0 |  |
| `c42271ce-b334-43a2-80cf-25e3dd348886` | `gen-cli.mjs --stamp` | 0 | 重打内容印记（41 件） |
| `9c5a6fed-e337-4f0d-a789-cc32ea50d3d3` | `gen-cli.mjs`（写盘） | 0 | 生成物仅 order 44 一行 |
| `c9a7c1c4-7ae3-42af-ad3e-3f4767114114` | `tsc -b packages/skill-calorie --force` | 0 | 派生后重编 |
| `96629d99-5500-4fa4-8d90-174bc546a55e` | `gen-cli.mjs --check` | 0 | GEN-CHECK PASS 键 129 |
| `1dd099a5-b41b-441d-bf7c-e3045faa5a50` | `node --test t630+t273+t275` | 1 | t630 6/6、t273 全绿；t275 2 红＝§二范围外项 |
| `bb16e836-7ac0-451e-a19e-727c57c0588e` | `check-warning-line.mjs --sync` | 0 | RESULT 91/91 |
| `00e841db-572a-4622-a864-307e21d17627` | `render-evidence.mjs` | 0 | 两证据页落盘 |
| `bb043f60-7067-4f43-8a26-04e4d7be2525` | `node --test t630`（变异） | 1 | §三红行 |
| `4d9dea61-194e-49fc-8a0a-b1e061bfaa15` | `tsc -b packages/skill-calorie --force` | 0 | 还原后重编 |
| `f07385d3-a538-43d8-b763-ed97bc64dcfd` | `node --test t630`（还原） | 1→0 | §三绿行（exit 0） |

`audit-separators.mjs` 与 `measure-responsive.mjs` 为纯读操作（未持锁，见 `.scratch/t630/audit.log`／`resp4.log`）：前者 exit 1 系脚注行命中（口径内，§二），后者 exit 0。

## 六 · 并发与越界声明

- 派生两件（`routes.generated.ts`、`AGENTS.md` 台账行）**零手改**：前者 `pnpm gen`（`git diff` 一行），后者 `--sync`（`SYNC-VERIFY ok`，后者由票面编排注具名要求同批提交）。票面「不许改」列表本意为禁手改（括号已写「走 pnpm gen 派生不手改」），#511 同 pattern 已收口；回执首段外另在此声明。
- 未碰：`base-render/**`、`routes.generated.ts` 手改、种子数据、他席证据件、来源脚注行；`.scratch` 外无散落日志；`SKILL.md` 例行 stale（`help:build` 在收口票重出，本票不管）。
- 他席在途件（`nutritionPortDocs.ts` 未提交改动、`libraryDocs.ts` 暂存改动、t631/t633 件）一律未动；提交前 `git diff --cached --name-only` 自查（见复验件）。
- 不推送（总工收口统一推）。
