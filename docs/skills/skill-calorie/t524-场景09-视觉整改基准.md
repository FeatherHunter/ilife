# t524 · 卡路里场景 09「身材照片」视觉整改基准（16 页对账 ＋ 六条判据 ＋ 形状化规则 ＋ 五维尺）

> 票：[#524](https://github.com/FeatherHunter/ilife/issues/524)（Part of [#159](https://github.com/FeatherHunter/ilife/issues/159)）。
> 本件是场景 09 整改波次的**强制对照基准**：只出判据与整改项，**不改产品代码**。
> **本版为订正版**（2026-09-15，按 [`t524-对抗审查.md`](./t524-对抗审查.md) §6 的必改项逐条订正）：权重唯一化、判分脚本入仓（[`t524-判分.mjs`](./t524-判分.mjs)）、H6 只在触摸档扣、D4 去隐藏 −5、五处不实与五处行锚逐条订正。
> 判据读数一律以 [`t524-改前读数.md`](./t524-改前读数.md) 为准，逐条复现命令与读数见 [`t524-证据.md`](./t524-证据.md)。
> 后续四票（#525 共享形状／#526 读侧族／#527 过程与结果族／#528 回执族／#529 HELP）与终审 #530 **逐条照本件对照**。

用户裁定逐字（2026-09-15）见票面 §用户裁定与 `D:\ilife\.scratch\scene09fix\seat-brief.md` §0：六条要求＝①双端自适应 ②手机端参考 HELP ③代码层审美 ④文字不冗余 ⑤`；`／`·` 分割代表该做 UI 设计 ⑥vision 审查，最终目标 100 分制 ≥90 分。

---

## §1 场景与交付面

### §1.1 交付面（16 件样张 × 命令 × 族 × 渲染源件:行）

命令的**唯一出口**是 `calorie-cmd-read`；样张 ↔ 命令 ↔ 参数的权威对照在 `.scratch/t351-preview/gen-scene09-wall.mjs`（本件 §1.2 只记渲染源件归属，不重复抄参数）。
「渲染源件:行」＝**产这一页的那条装配链**（数据面 → 页面装配件 → `build*Doc` 函数）；同族共用件单列在 §1.3，不逐页重复。

| # | 样张（`D:\ilife-验收\scene09\`） | 命令键 | 族 | 渲染源件:行（精确到 `build*Doc`） |
|---|---|---|---|---|
| 09-01 | 看身材照（画廊·全窗22张·本页4张） | `calorie.photo.list` | 读侧A | `src/photo/photo.ts:80` `buildGalleryData` → `src/photo/galleryDoc.ts:236` `buildPhotoListDoc` |
| 09-02 | 查身材照（单张·大图超1MiB） | `calorie.photo.detail` | 读侧A | `src/photo/photo.ts:160` `buildViewerData` → `src/photo/viewerDoc.ts:211` `buildPhotoViewerDoc` |
| 09-03 | 查身材照（单张·大图已内嵌） | `calorie.photo.detail` | 读侧A | 同上两件（内嵌分支由 `src/photo/photoThumb.ts` 出图） |
| 09-04 | 对比两张照片（跨标签·间隔48天） | `calorie.photo.compare` | 读侧B | `src/photo/photo.ts:131` `buildCompareData` → `src/photo/compareDoc.ts:187` `buildPhotoCompareDoc` |
| 09-05 | 生成身材照GIF（结果页·内嵌GIF） | `calorie.photo.gif` | 结果型 | `src/photo/photo.ts:192` `buildGifTask` → `src/photo/gifDoc.ts:284` `buildPhotoGifPage` |
| 09-06 | 记身材照（预检确认页） | `calorie.view.photo-log-wizard` | 过程型 | `src/render/wizardPort.ts:88` `buildPhotoLogWizardView` → `src/render/wizardPortDocs.ts:60` `buildPhotoLogWizardDoc` |
| 09-07 | 生成身材照GIF（前置规划页） | `calorie.view.gif-planner` | 过程型 | `src/render/wizardPort.ts:214` `buildGifPlannerView` → `src/render/wizardPortDocs.ts:165` `buildGifPlannerDoc` |
| 09-08 | 删身材照（候选页·全窗） | `calorie.view.photo-picker` | 过程型 | `src/photo/picker.ts:80` `buildPhotoPickerPrompt` → `src/photo/pickerDoc.ts:105` `buildPhotoPickerDoc` |
| 09-09 | 记身材照（写入回执） | `calorie.photo.add` | 回执 | `src/photo/photo.ts:265` `buildAddReceipt` → `src/photo/receipt.ts:270` `buildPhotoAddDoc` |
| 09-10 | 改照片标签（回执） | `calorie.photo.tag`（`op=set`） | 回执 | `src/photo/photo.ts:324` `buildTagReceipt` → `src/photo/receipt.ts:322` `buildPhotoTagDoc` |
| 09-11 | 加照片标签（回执） | `calorie.photo.tag`（`op=add`） | 回执 | 同 09-10（同一条装配链，`scene` 换对照词） |
| 09-12 | 删照片标签（回执） | `calorie.photo.tag`（`op=remove`） | 回执 | 同 09-10 |
| 09-13 | 删身材照（写入回执） | `calorie.photo.remove` | 回执 | `src/photo/photo.ts:300` `buildDeleteReceipt` → `src/photo/receipt.ts:300` `buildPhotoRemoveDoc` |
| 09-14 | 删身材照（候选快照·id19） | `calorie.view.photo-picker`（选中态） | 过程型 | 同 09-08（同一条装配链，选中态走另一支） |
| 09-15 | 看身材照HELP（q现找·记身材照） | `calorie.help.center` | HELP | `src/photo/help.ts:58` `viewPhotoHelpCenter` → `src/photo/helpDoc.ts:134` `buildPhotoHelpDoc` |
| 09-16 | 看身材照HELP（全量10键） | `calorie.help.center` | HELP | 同 09-15（`q` 缺省＝全量） |

### §1.2 渲染源件归属复核（结论：**成立**；行锚为**当刻实测**，会漂，引用带函数名）

复核命令（本件现场重跑，逐条对行号；行号会随源码改动漂移——**引用一律带函数名，行号只作辅证**）：

```
grep -n "export function build" packages/skill-calorie/src/photo/*.ts packages/skill-calorie/src/render/wizardPort*.ts
```

| 票面「已实测的源件归属」 | 复核结论 | 复核到的行（源码锚点） |
|---|---|---|
| 读侧A：`galleryDoc.ts`（`buildGalleryListDoc`）／`viewerDoc.ts`／`photoThumb.ts` | **成立**，函数真名是 `buildPhotoListDoc`（票面把名字写成了 `buildGalleryListDoc`，件与族都对） | `galleryDoc.ts:236`（`buildPhotoListDoc`；旧稿写 `:219`，审查曰漂 15 行，**订正为当刻实测 236**）／`viewerDoc.ts:211`（`buildPhotoViewerDoc`；旧稿 `:198`）／`photo.ts:80`·`:160` |
| 读侧B：`compareDoc.ts` | **成立** | `compareDoc.ts:187`／`photo.ts:131` |
| 结果型：`gifDoc.ts`；过程型：`pickerDoc.ts` ＋ `picker.ts` | **成立** | `gifDoc.ts:284`／`photo.ts:192`；`pickerDoc.ts:105`／`picker.ts:80` |
| 预检确认页：`wizard.ts`／`render/wizardPortDocs.ts` | **成立**（两页都在 `src/render/`，不在 `src/photo/wizard.ts`） | `wizardPortDocs.ts:60`·`:165`／`wizardPort.ts:88`·`:214` |
| 回执：`receipt.ts`／`store.ts`／`manage.ts`／`photo.ts` | **成立** | `receipt.ts:270`·`:300`·`:322`／`photo.ts:265`·`:300`·`:324` |
| HELP：`helpCenter.ts`（**524 LF**，订正波实测；旧稿 523） | **成立** | `help.ts:58`／`helpDoc.ts:134` |
| 整页装配共用件：`shared/docPage.ts`（`assembleDocPage`）＋`shared/copyArea.ts`；块层唯一定义地 `base-render/src/blocks.ts` | **成立** | `docPage.ts:117` `assembleDocPage`／`docPage.ts:101` `docShell` |

> **纪律（写进后续五票的作业须知）**：**行号会漂，引用要带函数名**。
> 一条行锚必须写成「`文件` 的 `函数名`／`选择器`（当刻 `:行`）」——只写 `:行` 的引用在下一个 session 改码后必然引错。
> 漂移的两个现成例子：`galleryDoc.ts` 的 `buildPhotoListDoc` 从 `:219` 漂到 `:236`；
> `packages/base-render/src/style.ts` 的 `@media (max-width: 640px)` 从旧稿 `:1167` 漂到 **`:1183`**、`(max-width: 400px)` 从 `:1178` 漂到 **`:1194`**。
> 比对行号前先比 §9 的件哈希（或 `git log -1 --format=%H -- <文件>`）。

### §1.3 同族共用件（四票与终审比对行号前先读这一节）

| 共用件 | 作用 | 行锚（当刻实测；**引用带函数名／选择器**） |
|---|---|---|
| `packages/base-render/src/blocks.ts` | 块层与样式唯一定义地（16 页的每个区块都从它出） | **2169 LF**（旧稿 2158，已漂）；`@media (max-width: 640px)` 六处 `:1254`·`:1542`·`:1547`·`:1669`·`:1689`·`:1691`；`overflow-x` 五处 `:1560`·`:1678`·`:1862`·`:2114`·`:2126`；`block-toc` 规则 `:1264`·`:1270`·`:1281`·`:1442` |
| `packages/base-render/src/style.ts` | 页面壳样式与 token | **1327 LF**（旧稿 1310，已漂）；`@media (max-width: 640px)` **`:1183`**、`(max-width: 400px)` **`:1194`**、`prefers-reduced-motion` `:1199`（旧稿写 `:1167`／`:1178`／`:1183`——**逐条引错，已订正**） |
| `packages/base-render/src/charts.ts` | 图表断点资产（`CHART_BREAKPOINTS.mobileMaxPx = 720`，`charts.ts:228`） | `charts.ts:2000` 出 `@media (max-width:720px)` |
| `packages/base-render/src/spec/controls.ts` | `TOAST_DEFAULTS.mobileMaxPx = 820`（`:194`） | 820 是 toast 栈与页面壳共用的移动档分界；另有 `ACTION_BAR_DEFAULTS.minHeightPx = 44`（宽档 44px 的定义地） |
| `packages/skill-calorie/src/photo/photoThumb.ts` | 照片与 GIF 的内嵌出图（`embedPhoto`／`embedPhotos`） | 09-01／03／04／05／08／09／13／14 的图都从它出 |
| `packages/skill-calorie/src/shared/copyArea.ts` | 页尾复制区（`copyArea`／`copyLog`／`notice`／`dataCopyArea`／`promptCopyArea`） | 16 页页尾同款；三格式标签（纯文本／JSON／CSV）是公共层 `#247` 定案，露英文处 `:22`·`:48`（标签）·`:66` |
| `packages/skill-calorie/src/shared/receiptParts.ts` | 回执族共用的状态卡（`statusCard`） | 09-09～09-13 五页共用 |
| `packages/base-render/src/pageUi.ts` · `pageShapes.ts` | #525 已落盘的**页面级移动端配方**与**形状件**（`PAGE_UI_CLASS`／`pageUiCss()`／`pageShapeCss()`） | `pageUi.ts`：`PAGE_UI_CLASS` `:31`、`PAGE_UI_VIEWPORT` **`:35`**、`pageUiCss()` `:43`；`@media (max-width: 820px)` **`:71`**、`min-height:44px` **`:78`**、`scroll-margin-top:20px` **`:62`**、安全区 `max(16px, env(safe-area-inset-*))` **`:92`·`:93`**；`docPage.ts:50` `pageUi?: boolean` 位、`:124` `const pageUi = input.pageUi === true;` |

> **旧稿五处行锚引错，逐条订正**（审查件 §5）：`pageUi.ts` 的 `:99`→**`:92`**、`:100`→**`:93`**（`max(16px, env(...))` 的真身行；旧稿指的 `:99` 是 `font-size: 12px`）、`:68`→**`:62`**（`scroll-margin-top`；旧稿指的 `:68` 是 `}`）、`:60`（「复制日志置灰规则已备」）→ **该件查无此规则**：`pageUi.ts:57`–`:58` 写明规则**已上移到 `style.ts` 的 `copyButton` 区**，本件只留说明；引用请写「`style.ts` `copyButton` 区（当刻 `:520` 附近）」。
> 记法：**行号会漂，引用要带函数名／选择器**（§1.2 末的纪律同条）。

**当刻实测（本票现场复核，写进基线以免后续票误判「已生效」）**：

- 16 页产物里 `ilife-page-ui` 命中 **0**、`ilife-helpdoc-` 命中 **0**（09-02／09-15 逐页查过）⇒ **#525 的移动端配方与 HELP 页内样式当刻都还没接进产出链**；`docPage.ts:124` `const pageUi = input.pageUi === true;` 是缺省关闭位。故 §3 的手机端数值表是**要达的线**，不是现状写照。
- 16 页产物 `@media` 各 **14 条**、`env(safe-area-inset-*)` 各 **1 处**（只 `bottom` 方向）、`viewport-fit` **0 处** ⇒ 无 `viewport-fit=cover` 时 `env()` 在 iOS 上取 0，安全区实际等于没做（与兄弟场景 04 基准 §四 的 J4 结论同源）。

**读数件与源件的时刻差（后续票必读，否则会误判「本该有的形状怎么没有」）**：被检的 16 件产物落在 `.scratch/t524/pages16/`，mtime **2026-09-15 08:14:48**；而 `packages/skill-calorie/src/photo/` 已在 **13:01～13:28** 被改到（`galleryDoc.ts`／`viewerDoc.ts`／`helpDoc.ts`／`helpDocContent.ts`／`helpDocCss.ts`／`photoUi.ts`，`git status` 全部为 `M`）。故：

- 本件的 16 页读数＝**改前基线**，读数有效、可复现（页群文件冻结在 `.scratch/t524/pages16/`）。
- 但**拿行号回去核源码时，源码已经比产物新**：例如 `helpDoc.ts:94`·`:120` 已经输出 `ilife-helpdoc-section`／`ilife-helpdoc-nav`，而 16 页产物里 `ilife-helpdoc-*` 命中 **0**（改前产物里页内导航是 `ilife-block-toc`×4）。⇒ 后续票比对行号前**先比 §9 的件哈希**，行号漂移时按选择器锚点重定位。

---

## §2 六条判据

### §2.0 判据面总览

| 判据 | 用户要求 | 出口（工具／读数件） | 当刻读数 | 改后要达的线 |
|---|---|---|---|---|
| P1 双端自适应（三档零溢出） | ① | `measure-responsive.mjs` | 16 页 48 格全零 **（回归门，不是成绩）** | 仍全零；破一格即红 |
| P2 手机端与 HELP 同档 | ② | `t516-判据-版式.mjs`读数 ＋ §3.2 数值表 | 触摸区：**触摸档（390／768）逐页 0 处不足**，1440 档 5～49 处（见 §4.2 H6 口径）；字号下限 **390 档 12/16 页 11px、4/16 页 12px**（§3.2、读数件 §6、`t524-判据-版式` 逐值同）；`viewport-fit` 0/16；页内定位**元素** 0/16 | §3.2 数值表逐条达标 |
| P3 分隔符与文字纪律 | ④⑤ | `audit-separators.mjs`（节点级） ＋ 文本探针 | **`RESULT: 0/16` FAIL，231 处** | `RESULT: 16/16` PASS，0 处 |
| P4 手机端观感与可用性 | ①② | `vision_html_screenshot` ＋ `vision_describe` | 逐页 51～77.6 分（`.scratch/t524/out/shots/` 32 张） | VLM ≥92（⇔ 用户 ≥90） |
| P5 五维尺（代码层审美） | ③ | 本件 §4 的算式 ＋ **[`t524-判分.mjs`](./t524-判分.mjs)（入仓的唯一判分口径）** | 均分 **12.69**（订正口径；旧口径 61 见 §4.1 注） | 逐页 ≥90 |
| P6 vision 最终审查 | ⑥ | `vision_*` 三件套 ＋ 逐条复核 | 已出 32 张图与复核表（读数件 §7.1） | 终审 #530 两档 390／1280 ≥90 |

### §2.1 逐条判据（判据／命令／当刻读数／改后要达的线）

**P1 双端自适应（三档零溢出）——回归门**

- 判据：390／768／1440 三档，每页 `docScrollWidth − innerWidth == 0` 且每张 `<img>`／`<svg>` 的 `scrollWidth ≤ clientWidth`（`img-not-fit=0`）。
- 命令：`node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/measure-responsive.mjs --dir <页群目录> --widths 390,768,1440 --json <落点>`
- 当刻读数：本票 16 件页群——`OVERFLOW-ZERO pages=16 cells=48 failed=0`；编排者的 17 页交付目录（含 `00-场景09验收导航.html`）——**`OVERFLOW-ZERO pages=17 cells=51 failed=0`**。
- **这条是 #484 的成果，不是本波成绩**：它是**回归门**——改后仍须为 0，弄成非零即红；**谁拿它报成绩＝虚报**（审查席位要当面点出）。
- 识别力**不靠现状读数**（现状已全绿），靠**变异自证**（读数件 §8，本票现场重跑）：
  - 往合格样板塞一条 `width:1200px` 的元素 → 768 档 `+432` 溢出、390 档视口守卫破 → exit=1；还原 → 48 格全零 exit=0。
  - 往合格样板塞一张不带 `max-width` 兜底的 2000px 宽图 → 390 `+440`／768 `+1232`／1440 `+560` 三格全破且 `img-not-fit=1` → exit=1；还原 → 全零 exit=0。
- 改后要达的线：48 格仍全零 ＋ 两条变异仍成立（判据有牙齿）。

**P2 手机端与 HELP 同档**

- 判据：§3.2 的数值表**逐条**达标——断点组／字号下限／行高／触摸区 ≥44px／`env(safe-area-inset-*)` 与 `viewport-fit=cover`／窄屏表格行为／图片与 GIF 容器／页内定位。
- 命令：`node <HEAD 版读数脚本> --dir <页群目录> --widths 390,768,1440 --json <落点>`（真 Chrome ＋ CDP 覆写设备度量）＋ 静态 CSS 扫描 ＋ 逐格人工／vision 抽查。
  **HEAD 版读数脚本的取法见 §2.7**：`git show HEAD:docs/skills/skill-calorie/t516-判据-版式.mjs > .scratch/t524/t516-head.mjs`，再 `node .scratch/t524/t516-head.mjs …`
  （工作树那份正被别人 session 改、当刻字段全空，**读它的数字不算数**）。
- 当刻读数：见 §2.5 的机审六列（**当刻无专用工具的列，本票明确记「无工具」**）。三条当刻真读数：**触摸档（390／768）逐页 0 处不足**（1440 档 5～49 处只记观感）；**390 档字号下限 11px 的 12/16 页、12px 的 4/16 页**；`viewport-fit` 0/16、安全区 1 处／页（只 `bottom`）。
- 改后要达的线：§3.2 每一条都能指出产出物里的 `文件:行`（不是「看着像」）。

**P3 分隔符与文字纪律——当刻真正判红的一条**

- 判据（节点级，命中 0 才绿）：R1 含 `·`／R2 含 `；`／R3 同一分隔符切出 ≥3 段并列（每段 ≤40 字）／R4 含 `｜`或`|`／R5 含 `、`／R6 含 `~`／R7 印出内部标识符（命令键／命令原文／库表名／蛇形名／驼峰参数名／常量名／票号）。
- 命令：`node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/audit-separators.mjs --dir <页群目录> --json <落点>`（工具是**另一 session 的在途件**，本票只读跑、不改、不提交；当刻 sha256 `A8FEA10F5E92C3810D7D0404A7EE6CF59D44A81FDBB11AFC728401C72F9481CE`／275 行；退出码 0＝全绿、1＝有值、2＝用法错）。
- 当刻读数：**`RESULT: 0/16` FAIL，节点命中 231 处**（R1 `·` 154／R2 `；` 6／R3 ≥3 段 83／R5 `、` 2／R6 `~` 1／R7 内部标识符 121；交付目录 17 页版＝**`RESULT: 0/17`**）。逐页六列读数与逐处命中表**直接抄**读数件 §2.1／§2.2（本件不重复抄数字，免得两处走散）。
- 识别力**有牙齿**（变异两行，本票现场跑）：往一份合格样板塞一处 `；` 并列 → `RESULT: 0/1` FAIL（exit=1，节点级命中 1）；还原 → `RESULT: 1/1` PASS（exit=0）。原文见 `t524-证据.md` §2（**订正波重跑的那两行**；旧稿引的 `probe-mut-sep.html:119 节点 #48` 那份文件后来被还原/改动，已不可复现，改记重跑读数）。
- 改后要达的线：**`RESULT: 16/16` PASS，节点命中 0**；R7 为 0 意味着页上不再出现 `#19` 这类编号（编号要换成读者看得懂的叫法或落进形状）。

**P2 的窄屏表格行为（订正）**

- 当刻事实：**16 页每页都有 5 处 `overflow-x: auto`**（含 `/auto` 变体，与 `overflow-x:auto` 无空格写法同一个声明）——其中 `.ilife-block-data-table` 这条**表格容器本身就是横滚容器**（`blocks.ts` 的表格区）。旧稿写「静态命中 0」是**只数了无空格变体**，**不实**。
- 真缺陷不是「没有横滚」，而是「**有横滚容器但表不撑开**」：表 `width:100%` ⇒ 窄屏把六列**压成一串**而不是让它滚（读数件 §7 逐页扣分理由、§2.2 的 R3 落在 `data-table-cell` 的命中）。
- 卡片化的那半条**当刻没生效**：`:has(td[data-label])` 塌列规则在 `pageUi.ts` 里**已备**，但 16 页产物 `td[data-label]` 命中 **0**、`ilife-page-ui` 命中 **0** ⇒ **「已备」≠「已生效」**（本件两词分开写）。
- 改后要达的线：**卡片化**（首选，让 `renderDataTable` 写入 `data-label`，再把 `pageUi` 接进产出链）或**横滚 ＋ 提示**；两条都能判（`facts.tdDataLabel > 0` vs `overflow-x` 容器 + 表 `min-width`），但都**不许「挤成串」**。

**P4 手机端观感与可用性（用户第 1 条的真实缺口）**

- 判据：两档（390×844／1280×900）截图逐页看**版式与阅读节奏**——字号层级／留白节奏／主角元素是否有视觉存在感／窄屏表格是否读得开／图片与图注是否成组／有无「一片灰小字」。
- 命令：`vision_html_screenshot`（只吃工作区内路径；长页**不要**用 `fullPage`，按固定视口分段）× 每页两档 → `vision_describe` 逐页审 → 要证据时 `vision_crop`／`vision_ground`。
- 当刻读数：逐页 51～77.6 分（读数件 §7；32 张图在 `.scratch/t524/out/shots/`）。**VLM 是提示器不是证据**：它的每条结论都要能用 DOM／CSS 读数或 `文件:行` 复核，复核不了的写「未证」（读数件 §7.1 已列 6 条复核，其中 2 条判「不成立」、1 条「未证」、1 条判「模型误读」）。
- 改后要达的线：VLM ≥92（⇔ 用户 ≥90），且 09-02 一类「用户判 30 分」的页要能被逐条硬扣分条款解释清楚为什么低。

**P5 五维尺（代码层审美）**

- 判据与权重：见 §4.1；改后**逐页 ≥90**，且**没有一维低于该维的 80%**（防堆总分掩盖单维塌陷）。
- 命令：`node .scratch/t524/score.mjs`（本票草稿脚本，算式在本件 §4.2；不进库）。
- 当刻读数：均分 **61**（读数件 §6，逐页 51～77.6，五维均分 D1 11.6/15・D2 13.4/20・D3 7.7/25・D4 15/20・D5 13.3/20）。
- 改后要达的线：逐页 ≥90；D3（分隔符与文字纪律）与 D5（手机端与 HELP 同档）必须是**结构性**达标，不许靠 D1／D2 补分。

**P6 vision 最终审查（用户第 6 条）**

- 判据：全部整改落地后，用 `vision_*` 对 16 页两档重跑一遍并**打分**，100 分制下要能到 90 分（VLM 侧 ≥92）。
- 命令：`vision_html_screenshot` 两档出图 → `vision_describe` 逐页给分与扣分理由 → 复核表（提示器不是证据）。
- 当刻读数：已出 32 张图（390×844／1280×900 各 16 张，另 3 张 390×3600 长页图）＋逐页扣分理由（读数件 §7）。
- 改后要达的线：**终审 #530 独立席位**按同一尺判，两档 ≥90；本票不给自己打分（作者禁自审）。

### §2.2 三档零溢出是回归门（口径，逐字照 `review-brief.md` §2.2 抄）

> 「三档零溢出」是**回归门**：改后必须仍为 0，弄成非零即红；**作者拿它报成绩＝虚报**，审查时要当面点出。
> 这条判据的**识别力靠变异**证明：塞一张 2000px 宽的图或去掉 `img{max-width:100%}` 兜底 → 必红。

⇒ 本基准件里 P1 一律写作「回归门」。任何票的报告里把「零溢出」列为**成果／成绩／加分项**的，按虚报处理。

### §2.3 机审六列（口径照仓规 `docs/agents/视觉验收墙.md` §0 第 ② 条；判据与工具抽自兄弟场景 04 基准 `docs/skills/skill-calorie/t522-场景04-视觉整改基准.md` §四）

仓规那一行写的是六列：**双端自适应／触摸目标／触屏三件／分隔符懒政／英文裸词／重复句**。逐列的判据、工具、命令、绿线、当刻读数与**证据强度**如下（`工具` 列写清了才敢说「有工具」——本仓多把「口径」当「工具」用，本件不这么记）：

| 机审列 | 判据 | 工具 | 命令 | 什么算绿 | 当刻读数 | 证据强度 |
|---|---|---|---|---|---|---|
| ① 双端自适应 | 每页每档 `docScrollWidth − innerWidth == 0` 且图放得进容器 | **有工具**：`packages/skill-calorie/scripts/measure-responsive.mjs` | `node tooling/run-locked.mjs --ticket <票> -- node packages/skill-calorie/scripts/measure-responsive.mjs --dir <页群> --widths 390,768,1440 --json <落点>` | 摘要行 `OVERFLOW-ZERO … failed=0` 且 exit 0 | 16 页／48 格全零（交付目录 17 页／51 格全零） | **强**（变异自证：超宽元素与无兜底大图各一条，见 P1） |
| ② 触摸目标 | **触摸档（390／768）**可点元素命中区 ≥44×44px，不足者 0 处；1440 档不判定（只记观感） | **有工具（读数）**：`docs/skills/skill-calorie/t516-判据-版式.mjs` 的 `touchSmall`（**当刻工作树版坏、读数取 HEAD 版**，见 §2.7） | 同上，`--widths 390,768,1440`（**判分只数 390／768**） | 390／768 两档 `touchSmall` 逐页 0 | 见 §2.5（本票现场读数：390 档 0／页、768 档 0／页；1440 档 5～49 处记观感） | **中**（工具只出读数、**不判红绿**；阈值写在基准里，靠人核） |
| ③ 触屏三件 | ① `viewport-fit=cover` 在页内 ② `env(safe-area-inset-*)` 四向都做 ③ 滚动／惯性／触摸行为不缺 | **半有工具**：`t516` 的静态 CSS 扫描出 `css` 段（`viewport-fit`／安全区计数）；触摸行为仍需人工 | `t516-判据-版式 … --json <落点>` ＋ `grep -c "viewport-fit\|safe-area-inset" <页>` | 三个子项在页内都能指出 `文件:行` | 当刻 `viewport-fit` 0／16 页、安全区 1 处／页且只 `bottom` | **弱**（一个子项靠机器，两个靠人工；见下方「无工具的四列」） |
| ④ 分隔符懒政 | 可见文本里 `·`／`；`／`｜`／`、`／`~`／≥3 段并列（节点级）命中 0 | **有工具**：`packages/skill-calorie/scripts/audit-separators.mjs` | `node tooling/run-locked.mjs --ticket <票> -- node packages/skill-calorie/scripts/audit-separators.mjs --dir <页群> --json <落点>` | 摘要行 `RESULT: n/n` ＋ `PASS`，exit 0（节点级命中 0） | **`RESULT: 0/16` FAIL，231 处** | **强**（变异自证：一处 `；` 必红、还原必绿，见 P3） |
| ⑤ 英文裸词 | 可见文本里不出现未解释的英文标签／半角标点（`JSON`／`CSV`／裸 `:`／裸 `,` 那类） | **当刻无工具**（`audit-separators.mjs` 的 R7 只认**内部标识符**七类；常见英文词与半角标点不在它的面内） | — | 逐页人工清点 ＋ vision 抽查，命中 0 | 16 页人工清点：**每页 9 处以内，全页群已知共 9 处**（页尾复制区三格式标签 `纯文本／JSON／CSV`，公共层 `#247` 定案，落 `packages/skill-calorie/src/shared/copyArea.ts:22`·`:48`·`:66`；归 #525 的公共层）。该页处数落 `facts.json` 的 `english`，`t524-判分.mjs` 据此扣 H7 | **无**（本件写清「无工具」，见 §2.6 遗留出口） |
| ⑥ 重复句 | 同一可见文本节点（≥4 字）同页出现 ≥2 次；人核后按「同事实一页一处」判 | **半有工具**：本票草稿 `.scratch/t524/probe-text.mjs`（机器计数）；`t522` 的 `.scratch/t522/readings.mjs` 有 `dupFacts`（数字＋单位型事实） | `node .scratch/t524/probe-text.mjs <页群> <落点>`（**不进库**）＋ 逐条人核 | 机器计数可 >0（逐条如实报不算冗余），**人核后的真冗余处 = 0** | 机器重复节点 235 处；**人核后真冗余 29 处**（读数件 §3） | **中**（机器只出候选；判红绿靠人核口径，见读数件 §3 的「逐条如实报」例外） |

**证据强度三档的说法**（供审查席位用同一套词）：

- **强**：① 有入库脚本 ② 摘要行可被判红绿 ③ 有变异自证两行。当刻只有两列（① 双端自适应、④ 分隔符懒政）。
- **中**：脚本只出读数、不判红绿（阈值写在基准里靠人核），或判红绿要接一次人核。
- **无／弱**：当刻没有能覆盖该列的脚本；只能「逐格人工 ＋ vision 抽查顶上」，并在遗留出口里开票建工具。

### §2.4 三档断点复核结论（票面要求先复核再照用）

票面写「仓内既有口径是三层断点并存（页面 640／400、图表 720、toast 栈 820）——先复核是否成立，成立就照用，不新造断点值」。**复核结论：三条边界值都是真的，但「三层」这个说法不成立**，实际是**四套断点值、分住三件，且 HELP 自己另用 500／820**：

| 边界值 | 出处（`文件:行`） | 管什么 | 16 页产物命中 |
|---|---|---|---|
| **820** | `packages/base-render/src/spec/controls.ts:194` `TOAST_DEFAULTS.mobileMaxPx = 820`；`packages/base-render/src/style.ts:286`·`:336`·`:371`·`:426`·`:520`·`:660`（`max-width`）＋`:317`（`min-width: 821`） | toast 栈移动档 ＋ 页面壳移动档 | 820×6 ＋ 821×1 |
| **720** | `packages/base-render/src/spec/charts.ts:228` `CHART_BREAKPOINTS.mobileMaxPx = 720`；`charts.ts:2000` 出串 | **只管图表** | 720×1 |
| **640／400** | `packages/base-render/src/style.ts:1167`／`:1178`（页面档）；`blocks.ts:1254`·`:1547`·`:1691` 三处 `@media (max-width: 640px)` | 页面档 ＋ 块层紧凑档 | 640×3 ＋ 400×1 |
| **500／820** | `packages/base-render/src/helpShell.ts:42`（HELP 壳前缀 CSS 整段一行；`max-width:500px`×1、`min-width:501px`×1、`max-width:820px`×1）；`packages/base-render/assets/help-template.html:119`／`:156`／`:1405`／`:1453`／`:1638` | **HELP 页自己那一套** | 不在 16 页产物里（HELP 产物走 `helpShell` 另一条链） |

⇒ **裁定**：①640／400／720／820 四个值**照用，不新造**；②但**「手机端基准＝HELP」这句话在断点这一项上不能简单照抄**——HELP 壳自己用的是 **500／820**，与本技能页面用的 **640／400／820** 不是同一组；③要让「手机端与 HELP 同档」**可判**，判据面只认**两条**：**（a）触摸区 ≥44px 与安全区**（HELP 有 `min-height:44px`、有 `env(safe-area-inset-bottom,*)`，两侧同档，可逐值比）；**（b）窄屏表格行为**（HELP 走横滚 `overflow-x:auto`＋触屏惯性，页面要么横滚要么卡片化，不许「挤成串」）。断点值本身按「页面用页面那套、HELP 用 HELP 那套」记，**不写成同档**（否则判据自己就先错了）。

### §2.5 当刻机审读数（本票现场跑）

- 分隔符与内部标识符探针：`node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t524/pages16 --json .scratch/t524/out/sep-before-16.json`
  → 摘要行 **`RESULT: 0/16`** ＋ `FAIL`，`exit=1`；GATE-RUN `runId=ad93f5e3-d72f-471a-a1d9-111ca53171b7`。
- 三档响应式（回归门）：`… measure-responsive.mjs --dir .scratch/t524/pages16 --widths 390,768,1440`
  → 摘要行 **`OVERFLOW-ZERO pages=16 cells=48 failed=0 label=t524-before-16pages`**，`exit=0`。
- 版式与形状读数（触摸目标／字号／触屏三件／断点集）：`node tooling/run-locked.mjs --ticket 524 -- node docs/skills/skill-calorie/t516-判据-版式.mjs --dir .scratch/t524/pages16 --widths 390,768,1440 --json .scratch/t524/out/fmt-16.json`（读数落 `.scratch/t524/out/fmt-16.json`；**该工具不判红绿**，阈值按 §3.2 与本节的线人核）。
- 文本探针（重复句候选）：`node .scratch/t524/probe-text.mjs`（草稿，不进库）→ 机器重复节点 235 处、人核后真冗余 29 处（读数件 §3）。
- **回归门工具当刻不可用（范围外）**：`gen-photo-baseline.mjs --check` 当刻 `exit=1`，**实况**是摘要行 `BASELINE-DIFF 10 pages` 加两行不稳定：
  `UNSTABLE calorie.photo.add：渲染当刻时钟未被剥净：<当刻时刻>` 与 `UNSTABLE calorie.photo.remove：渲染当刻时钟未被剥净：<当刻时刻>`（2026-09-15 两次实跑都复现；逐字读数见 `t524-证据.md` §3.1）。
  （旧稿把病因写成「`dist/diet/review.js` 导入 `buildDietReviewEmptyDoc` 失败」——那是**更早一次的过时病因**，当刻已不是它；**现象在、病因已换**。）
  ⇒ 这是构建产物／渲染时钟与基线不一致，**不在本票写集**（本票不许跑构建、不许改 `packages/**`）。写进 `t524-证据.md` §环境异常，**归 #530 收口**。

### §2.6 遗留出口：无工具的两列（本件主动开票）

| 列 | 当刻状态 | 建议 |
|---|---|---|
| ⑤ 英文裸词 | 无工具；本票只能用 `grep` ＋ vision 抽查 | **另开一票建工具**：节点级可见文本探针加两类规则——「已知英文标签白名单」与「半角标点出现在中文串中」（可挂在 `audit-separators.mjs` 的姊妹件上，别改它） |
| ③ 触屏三件 的「触摸行为」子项 | 半有工具（静态扫描出计数，行为靠人） | **并入上面那张票**：真 Chrome 里跑 `touch-action`／`-webkit-overflow-scrolling`／点击命中区三项，出摘要行 |

### §2.7 读数工具的可复现性（写清「读数取自哪个 sha」）

**不许只说「t516 的读数」**——那个文件正被另一 session 在途改，工作树版和 HEAD 版读数**不是一个东西**。三种跑法都要点名：

| 跑法 | 命令 | 当刻状态 |
|---|---|---|
| **工作树版** | `node docs/skills/skill-calorie/t516-判据-版式.mjs --dir … --json …` | **当刻坏**：`RESULT: -2/1 件读数完成`、`minFontPxNoSvg`／`touchSmall`／`shellMaxWidth` 字段全空、exit 1（本席位 2026-09-15 实测；审查件 §6-8 记的 `-32/16` 是同现象） |
| **HEAD 版** | `git show HEAD:docs/skills/skill-calorie/t516-判据-版式.mjs > .scratch/t524/t516-head.mjs` 再跑它 | **可跑**，出与读数件 §1.3 逐值同的读数（本席位本次读数即取此版） |
| 冻结副本 | `.scratch/t524/t516-head.mjs`（16812 字节，不随工作树变） | 本波复跑一律用它，并在证据里记「取 HEAD 版」 |

⇒ **本件与 `t524-判分.mjs` 的 `fmt.json` 一律由「HEAD 版」产出**；t516 工作树件归它自己的 session，本票只读不碰。
`facts.json` 的 `source` 位记这一条，后续票复跑前先核 `git diff --stat docs/skills/skill-calorie/t516-判据-版式.mjs` 是否已收敛。

---

## §3 形状化规则 ＋ HELP 手机端数值表

### §3.1 形状化规则：分隔符串该变成什么形状

用户第 5 条逐字：「当一个内容需要通过「；」和「·」分割时代表需要进行UI上的设计，该问题是用这些符号简化了UI展示的设计」。
⇒ **判目**：可见文本里出现并列分隔符 ＝ 那一处**少了一次设计**，必须换形状。判据出口＝`audit-separators.mjs` 节点级命中 **0**（P3）。

形状件**只用仓内既有出口**（`packages/base-render/src/blocks.ts`；`pageShapes.ts` 是 #525 新落盘的形状件），**不许自造**：

| 碰上什么（分隔符串的语义） | 换成什么形状 | 用哪个出口 | 当刻该改的页（读数件 §2.2 的实例） |
|---|---|---|---|
| `·` 串**并列标签／状态词**（角度、标签、状态） | **徽章列**（胶囊行） | `renderChips` | 09-01 图注「2026-07-17 15:19 · 侧面 · 60 天前」×20 处、09-08／09-14 每张候选卡 |
| `·` 串**多张实体并列**（候选照片、帧、条目） | **卡片格**（一格一实体） | `renderListRows`（左／中／右三槽）＋ `photoThumb` 出图 | 09-08「#32 · 2026-… · 正面」22 处、09-09「正面 · 495shots 重出样张」 |
| `·` 串**单实体的多个字段**（「这张照片的信息」） | **键值行**（标签 ＋ 值，一行一件） | `renderDetailSection` | 09-02 图注四件事串一行、09-13 快照行 |
| `；` 串**两句并列**（判语 ＋ 补充） | **结论条**（一句判语＋事实条）／**事实条** | `renderConclusionBar`／`renderCaliberLine` | 09-06 预检确认页 2 处、09-07 规划页 2 处 |
| `、` 串**枚举**（三件以上同类） | **事实条**／**小表** | `pageShapes` 的事实条／`renderDataTable` | 09-09 1 处、09-11 1 处 |
| `~`／`～` **范围**（一律写「至」或换成条） | **进度条**（范围读成占比）或**时间轴条** | `renderMiniBar`／`renderDistributionRows`；时间序列用 `pageShapes` 的时间轴条 | 09-05 1 处（`~` 顶替「至」，硬扣分条款里另有 −1） |
| `｜` 串**多列并列** | **小表**（真二维数据）或**卡片化**（窄屏） | `renderDataTable`（窄屏行为见 §3.2） | 当刻 0 处（**改后也不许出现**，R4 是 0） |
| **占比／进度**（「4 帧里 1 帧」） | **进度条**（迷你条） | `renderDistributionRows` | 09-07 KPI 明细、09-01「本页 4 张已显示」 |
| **一串时间点**（`·` 串日期） | **时间轴条** | `pageShapes` 时间轴条 | 09-01 画廊时间序 |

两条硬约束（写进后续票的交付面）：

1. **卡槽吃纯文本**：`renderKpiCard`／`renderKpiGrid` 的值槽**放不进形状**（出处 `base-render/src/blocks.ts` 的卡槽实现）。要形状就落到**卡外的事实条／行**里——#504／#505 已走过的路（`docs/skills/skill-calorie` 的兄弟票记录）。
2. **形状件不许在族票里自造**：跨域共用形状归 **#525**（`base-render/src/pageUi.ts`／`pageShapes.ts`／`skill-calorie/src/shared/pageKit.ts`）；族票只许加**自己页内**的件。

### §3.2 HELP 侧手机端数值表（逐条给 HELP 的 `文件:行`，并给 16 页当刻读数）

**断点组（HELP 自己那套）**：

| 边界 | HELP 侧事实 | 出处 |
|---|---|---|
| **500／501** | `@media(max-width:500px){…}` 把窗口铺满（`.phone{width:100%;height:100vh}`、`.sheet{bottom:0}`）；`@media(min-width:501px)` 是桌面那支 | `packages/base-render/src/helpShell.ts:31`（前缀 CSS 整段一行，命中 `max-width:500px`×1、`min-width:501px`×1）；`packages/base-render/assets/help-template.html:119`·`:156` |
| **820** | 控件级移动档（`max-width:820px`×4：错误卡内距、按钮栅格与 46px 高、批量条竖排…） | `help-template.html:1405`·`:1453`·`:1638`；`helpShell.ts:34`（控件库 CSS 整段一行，命中 `max-width:820px`×1）；同值另有 `packages/base-render/src/spec/controls.ts:194` `TOAST_DEFAULTS.mobileMaxPx = 820` |

⇒ **HELP 用 500／820，本技能页面用 640／400／820**（§2.4）——**两套不是同一组**，判据面只认「触摸区／安全区／窄屏表格」三条可逐值比的项。

**数值表（改后要达的线；每行给 HELP 出处与 16 页当刻实测）**：

| 项 | HELP 侧当刻事实（出处） | 16 页当刻实测 | 改后要达的线 |
|---|---|---|---|
| **触摸区 ≥44px** | `min-height:44px` 在册：HELP 壳与模板共 **12 处**（`helpShell.ts:42`；`help-template.html:58`·`:97`·`:1187`·`:1265`·`:1635`·`:1640` 等）；820 档按钮 46px（`help-template.html:1453`） | **触摸档（390／768）逐页 `touchSmall=0`——达标**；1440 档 5～49 处不足（`button.ilife-copy-btn@78x40`·`@476x40`·`button.ilife-copy-menu-item@186x40`，高 **40px**），**只记观感、不进 H6**（口径见 §4.2） | 触摸档保持 0 处不足；1440 档的 40px 复制按钮／菜单项按「更好」处理（#525 `pageUi.ts` 的 `pageUiCss()` 820 档配方 `min-height:44px`，当刻 `:78`） |
| **字号下限** | HELP 正文 `14px`；最小 `8px`（眉标类，`helpShell.ts` 命中 `font-size:8px`×1）；常见档 10.5／11／12／12.5／13／13.5／14 | 390 档 `minFontPxNoSvg` ＝ **11px（12 页）／12px（4 页）**；11px 的责任元素是 `th.ilife-block-data-table-cell-left`·`-right`（表头） | 390 档正文类（表头／口径行／列表行／卡明细）**≥12px**；表头那 11px 必须抬到 12 |
| **行高** | HELP 正文 `line-height:1.5`（`body`） | 正文 22.5px／15px ＝ 1.5（同档）；11px 表头行高 16.5px ＝ **1.5**（同档） | 保持 1.5；不许出现 <1.4 的挤行 |
| **页脚留白／安全区** | 壳 `padding:24px 16px 60px`；`env(safe-area-inset-bottom)` 在册 **5 处**（`helpShell.ts:42`）＋模板 4 处（`:117`·`:133`·`:154`·`:271`）。**HELP 自己没有 `viewport-fit=cover`**（`helpShell.ts` 命中 `viewport-fit` **0**） | 16 页**每页 1 处** `env(safe-area-inset-bottom)`、**只 `bottom` 一个方向**、`viewport-fit` **0 处**；页脚下留白 60px（与 HELP 同值） | ① 页脚 60px 保持；② `viewport-fit=cover` 必须加（#525 `pageUi.ts` 的 `PAGE_UI_VIEWPORT`，当刻 `:35`，已有该串）；③ 至少 left／right 也避让（#525 `pageUi.ts` 的 `pageUiCss()` 窄屏段 `max(16px, env(...))`，当刻 **`:92`·`:93`**；旧稿写 `:99`·`:100` 引错） |
| **窄屏表格行为** | HELP 走**横滚**：`overflow-x:auto` ＋ `touch-action:pan-x`（`helpShell.ts` 命中 `overflow-x:auto`×3；`help-template.html:90`·`:117`） | **16 页每页 5 处 `overflow-x: auto`**（含加空格写法；旧稿写「没有任何 `overflow-x:auto`／静态命中 0」**不实**——只数了无空格变体），其中 `.ilife-block-data-table` 就是横滚容器；但表 `width:100%` ⇒ 窄屏把六列**压成一行长串**而不是滚（09-02／03／04／07／09～13；读数件 §7 逐页扣分理由） | 二选一：**卡片化**（首选，#525 `pageUi.ts` 的 `:has(td[data-label])` 塌列**已备未生效**——当刻 `td[data-label]` 与 `ilife-page-ui` 命中都是 0）或**横滚 ＋ 提示**（让表撑开）；不许「挤成串」 |
| **等宽数字** | `tabular-nums` 在册（`helpShell.ts`×1；`help-template.html:271`） | 表头与单元格都已是 `tabular-nums`（`t516` 读数 `tableTh`／`tableTd` 均 `tabular-nums`）→ **该项当刻已达标** | 保持 |
| **页内定位（锚点／目录）** | HELP 有 `.toc`／tab 条（500 档 `tab-bar` 吸底）；本技能 HELP 页的页内目录见 `helpDocCss.ts:26`·`:27`（`min-height:44px` 胶囊）＋ `:31`（节头 `scroll-margin-top:16px`）＋ `:69`～`:76`（400 档目录转横滑胶囊行） | 16 页 `ilife-block-toc` 的**元素**命中 **0/16**（当刻 `class="…ilife-block-toc` 逐页 0；旧稿写「改前产物里 HELP 页有 4 个」**不实**——那 4 个是**同名 CSS 规则**，不是元素；且与 §2.0 的「页内定位 0/16」互斥，已订正）；`scroll-margin-top` **0 处** | 长页（09-01／08／09／13／14／16）**必须有页内目录或锚点**；所有带 `id` 的区块吃 `scroll-margin-top`（#525 `pageUi.ts` 的 `pageUiCss()` 已备 20px，当刻 **`:62`**；旧稿写 `:68` 引错） |
| **图片／GIF 容器** | HELP 无图；本技能的口径是「容器有明确宽高比 ＋ `max-width:100%` ＋ `object-fit` 说清」 | 48 格全零、图放得下（回归门已绿）；但容器无圆角兜底、GIF 画布 64×64（09-05） | 容器形状走 `pageShapes.ts`（比例集合 `:97`、容器函数 `:120`）；**不许裸 `<img>` 撑破容器** |

> **两条最容易看错的**：①「手机端与 HELP 同档」**不是**「断点值一样」（§2.4）；②HELP 自己也没有 `viewport-fit=cover`，所以这一条是**比 HELP 更严**的线，按 #525 已落盘的配方加，不算「改公共层」。

---

## §4 五维尺与判分口径

### §4.1 五维与权重（100 分制，**唯一口径**）

维名与先例一致（`.scratch/t401d/review-ui/审查报告.md` 的五维）。**权重以本表为准，且只有这一套**：
**D1 15 ／ D2 20 ／ D3 25 ／ D4 15 ／ D5 25**（合计 100）——
手机端与 HELP 同档是用户第 2 条**明写**的要求，故在手机端这一支取高的一支（D5 25）。
本波此前出现过两套权重（本表旧写 D4 15／D5 25，而读数件 §6 与判分草稿写 D4 20／D5 20），
**已按本条全部对齐**：本表、同节散文、读数件 §6、[`t524-判分.mjs`](./t524-判分.mjs) 四处逐值同。

| 维 | 维名 | 权重 | 这一维量什么（判据出口） |
|---|---|---|---|
| D1 | **结构纪律**（栅格／tnum／缺值／色 token） | **15** | 内容列收得住（1440 档 shell 1000／max 960）；同排卡等高（`t516` 的 `kpiMaxSpread`）；数值列 `tabular-nums`；缺值统一 `—`；同页不出现同质卡片堆；色只用 token |
| D2 | **信息层级与版式细调** | **20** | 主角元素有视觉存在感（KPI 值 ≥ 正文 1.6×；09-05 的 GIF 画布不许 64×64）；同级标题字号一致；卡／表／图同列对齐；无「一片灰小字」（长行 ≥50 字的整段）；间距节奏成 4／8 倍数 |
| D3 | **分隔符与文字纪律** | **25** | **P3 的机器读数直接判**（`audit-separators.mjs` 节点级）＋ §3.1 的形状化落地 |
| D4 | **跨宽自适应** | **15** | **P1 三档零溢出（回归门）** ＋ 1440 档「更好不是更空」（人核落 `d4cut`，见 §4.2 ③）＋ 390 档读得开、无孤行 |
| D5 | **手机端与 HELP 同档** | **25** | **§3.2 数值表逐条**（触摸区／字号下限／行高／安全区与 `viewport-fit`／窄屏表格行为／页内定位／图片容器） |

**当刻基线（订正口径，[`t524-判分.mjs`](./t524-判分.mjs) 可复算；逐页读数落 `.scratch/t524/out/score-before-corrected.json`）**：
D1 **11.56**／D2 **13.44**／D3 **2.00**／D4 **15**／D5 **11.25**，**均分 18.75**（逐页 0～63），
每维 ≥ 满权 80% 的页 **0/16**，逐页 ≥90 且每维 ≥80% 的页 **0/16**。
复算命令：`node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-before`。

> **旧口径 61 的来历（写清，免得两处再走散）**：读数件 §6 的 61 分是**另一套算式**算的——
> 权重 D4 20／D5 20、D3 用比例式（25 −(1.2×分隔符 ＋ 1.5×标识符 ＋ 重复节点÷5)）、且**不扣** H1～H4 硬扣分。
> 两套口径对同一页差 43 分（09-02：旧 63.9 ／ 订正 10），这就是审查件 §4 判「不可比」的根因。
> 本件起**只认** §4.1 ＋ §4.2 ＋ `t524-判分.mjs` 这一套；读数件 §6 那一列标注为「旧口径读数，仅存档」。
> ⇒ **D4 满分是「回归门已绿」造成的假象**（三档早已零溢出）——**不许**拿 D4 补别的维；改后 D4 仍须 ≥满权 80%（≥12），而 D3／D5 必须结构性达标。

### §4.2 维度分与硬扣分（两条腿，别混）

**① 逐维分（加权）**：五维各自 0～满权（**权重＝§4.1 的表：15／20／25／15／25**）。
D3 由机器读数折算；D1／D2／D4 的人核扣分位由人核扣分表算出（逐条带理由，落 `.scratch/t524/out/vision-deductions.json`）；
D5 由四条机器／静态读数折算。**算式本体入仓**：[`t524-判分.mjs`](./t524-判分.mjs)（不再是草稿）。

**② 硬扣分条款（从逐维合计里直接扣，扣到底，别客气）**——照 `review-brief.md` §3 逐字口径：

| 号 | 触发 | 扣分 | 判据／读数出处 |
|---|---|---|---|
| H1 | 可视区里**一处分隔符串并列语义** | **−2** | `audit-separators.mjs` 节点级 R1～R6 逐处（09-01 一组 34 处 ⇒ 该页 H1＝−68；这也是它总分 0 的来源） |
| H2 | **一处内部标识符或裸代码**（命令键／库表名／蛇形名／驼峰参数名／常量名／票号 `#19`） | **−3** | 同工具 R7 逐处（09-02 起 7 处、09-16 20 处） |
| H3 | **同一事实一页重复说**（副标题／读数卡／表头三处说同一件事） | **−2** | 读数件 §3「人核后的真冗余处」＋ `facts.json` 的 `dupFacts`（「逐条如实报」不算） |
| H4 | **符号顶替文字**（`~` 写「至」、`→` 写「到」那类） | **−1** | `audit-separators.mjs` 节点级 R6（09-05 1 处） |
| H5 | **窄屏溢出**（`docScrollWidth − innerWidth ≠ 0` 或图放不下） | **−5** | `measure-responsive.mjs` 逐格（当刻全零） |
| H6 | **点击区 <44px**——**只在触摸档扣**：390／768 档，或 CDP 显式打开 `pointer: coarse` | **−1** | `t516-判据-版式` 的 `touchSmall` 逐处，**只数 390／768 两档**（见下方裁定） |
| H7 | **露英文或半角标点** | **−1** | 人工清点＋vision 抽查（`纯文本／JSON／CSV` 三格式标签等）；`facts.json` 的 `english` 逐页记处数 |

**H6 口径（编排者裁定 2026-09-15，取代原「按 1440 档逐处扣」）**：

- **44px 触摸区是触屏要求**：只在触摸档扣。仓内既有口径本来就是「≤820px 才放大」
  （`packages/base-render/src/pageUi.ts` 的 `pageUiCss()`：`@media (max-width: 820px)` 段把
  `copy-btn`／`copy-menu-item`／`action-btn`／`block-toc a`／`block-disclosure-summary`／`block-param-form-input`
  的 `min-height` 抬到 44px）——**桌面 1440 档不硬扣、只记观感**（落 D1 人核，不落 H6）。
- 由此 09-01 的 49 处（全是 1440 档的 40px 复制按钮／菜单项）**不再进 H6**，H6＝0；
  该页的线因此**可达**（验算见 §4.3）。
- **读口诀**：H6 的真实读数在 `390`／`768` 两档。若哪天要连宽屏也判 44px，**先改仓库规则**
  （把 `min-height:44px` 从 820 档提出来），别只改判分脚本——判据不能比仓库规则更严。

**③ 算式（唯一）**：`页分 = max(0, Σ(逐维分) − Σ(硬扣分))`，逐页算、逐页判；族票报**逐页分 ＋ 均分**两行。
每条硬扣分**同时**在对应维度里落一次账（H1～H4→D3／H5→D4／H6→D5／H7 无维度），
再由本条从逐维合计里扣一次——**这是有意的两层**（先压维度、再压总分），不是重复计价：
`t524-判分.mjs --dir …` 的 `SCORE` 与 `RECONCILE` 两行就是这两层的逐页复算，
**「用脚本算 ＝ 用本节字面公式算」差 0**（自证见 §4.6）。
`facts.d4cut` 是人核位（1440 档只多留空的观感债，正例 0／下限页 3），**不是算式里的隐藏 −5**——
审查件点的「D4 自带 −5」已删除。

### §4.3 换算关系（本件为准）：VLM ≥92 ⇔ 用户 ≥90

**换算**：`用户分 ≈ VLM 分 − 2`。理由（实测，不是估计）：同一个 VLM 对 09-02 给 **70／78**，而用户判 **30**——差距来自 VLM 看不见「符号顶替设计／内部叫法上屏／同事实三遍」这类**纪律债**；把这些债按 §4.2 的硬扣分单独扣掉之后，VLM 的宽松分与用户口径的差收敛到 **2 分**左右。

⇒ 坐实三档线：**族票自测 ≥92**（给终审留余量）／**#525 样板页 ≥92**／**终审线 ≥90**（#530 独立席位判）。同时**每一维不得低于该维满权的 80%**（按 §4.1 的权重：D1≥12／D2≥16／D3≥20／D4≥12／D5≥20），防堆总分掩盖单维塌陷。

### §4.3.1 可达性验算（「逐页 ≥90 且每维 ≥80%」自己走一遍能走通）

拿 09-01 当刻产物当**反例**、手工合格样板当**正例**，两向读数落盘
（`.scratch/t524/out/readings-09-01/`／`readings-draft/`／`readings-floor-draft/`，汇总 `readings-two-way.json`；
三条都由 `node docs/skills/skill-calorie/t524-判分.mjs --dir <目录>` 现场复算）：

| 线 | 逐维（D1／D2／D3／D4／D5） | 逐维合计 | 硬扣分 | **页分** | 每维 ≥80% | 判定 |
|---|---|---:|---:|---:|---|---|
| **反例**·09-01 当刻产物 | 12／13／**0**／15／**7** | 47 | −90（H1×34 H2×6 H3×2） | **0** | 否（D2／D3／D5 塌陷） | **判红** ✓ |
| **正例**·手工合格样板 | 15／20／25／25／25 | 100 | −0 | **100** | 全部满足 | **判绿** ✓ |
| **下限页**·每维贴 80% | 12／16／25／**12**／25 | 90 | −0 | **90** | 全部满足 | **判绿**（恰压线）✓ |

读数来源与构造：

- 反例＝真产物（与 `D:\ilife-验收\scene09` 逐字节同哈希），三份机器读数原样取用。
- 正例／下限页＝手工构造的合格样板块（页内目录 ＋ `scroll-margin-top` ＋ 每格列头 `data-label` ＋ 图片固定宽高比 ＋
  文字无分隔符串／无内部标识符／无符号顶替）。静态形状事实（`tocEl=1`／`tdDataLabel=9`／`aspectRatio=1`／`scrollMargin=1`）实测；
  两条自证：分隔符探针 `RESULT: 1/1 PASS`（exit 0）、三档读数 `OVERFLOW-ZERO pages=1 cells=3 failed=0`（exit 0）。
  正例的 D1／D2 取 0 扣分（＝改后无人核可扣项）；**下限页**把三项压到 80% 线本身（D1 扣 3／D2 扣 4／D4 扣 3），页分正好 90。

⇒ **裁定：线可达，且余量很小**。D3 与 D5 结构性达标后（分隔符／标识符／符号顶替全 0 ＋
HELP 四条全做：触摸档 ≥44px、390 档字号 ≥12px、有横滚或卡片化、页内目录 ＋ `scroll-margin-top`），
`D3=25`、`D5=25`、`D4≥12` 已锁死 **62 分**，**剩下 28 分全看 D1 ＋ D2**；
即 **D1 ＋ D2 ≥ 28**（下限页）是过线的硬条件，而 09-01 当刻的 D1＋D2＝25.
⇒ **D1／D2 只修「文字／形状」是不够的**：卡片栅格、层级节奏、主角元素这几项必须真动，否则 09-01 卡在 87～89。这条写进 #526 的交付面。

### §4.4 判分协议（谁审、审几页、几档、怎么算）

| 项 | 口径 |
|---|---|
| **谁审** | **独立对抗审查席位**（作者禁自审；审查者按 `D:\ilife\.scratch\scene09fix\review-brief.md` 走，**必须自己重跑**机器门，不许抄作者数字） |
| **审几页** | **16 页全审**（不许抽样；族票报自己族的那几页 ＋ 抽 1 页跨族对照） |
| **几档** | **两档：390／1280**（390×844 与 1280×900 各 16 张；长页**不许**用 `fullPage`，按固定视口分段截） |
| **怎么算分** | `页分 = max(0, Σ(逐维分，权重见 §4.1) − Σ(硬扣分，§4.2))`；**逐页算、逐页判**，且**每维 ≥ 满权 80%**；族分＝族内页均分 ＋ 最低页分（两个数都要报）。**命令**：`node docs/skills/skill-calorie/t524-判分.mjs --dir <读数目录> [--json <落点>]`（入仓脚本；grep 不到 `.scratch` 的私有算式就不算数） |
| **vision 的效力** | **提示器不是证据**：VLM 的每条结论都要能用 DOM／CSS 读数或 `文件:行` 复核；复核不了的写「未证」（读数件 §7.1 已给 6 条复核先例，其中「09-05 没显示 GIF」判**不成立**、「09-16 拼写」判**模型误读**） |
| **红绿两向** | 每条机器判据都要有**变异自证两行**（见 §2.1 P1／P3 与 `t524-证据.md`）；只有「改前红、改后绿」还不够，必须「**塞回缺陷必红、还原必绿**」 |
| **不许自评** | 族票的报告里只能写「读数 ＋ 逐处清单 ＋ 自查结论」，**分数由审查席位给**（作者给的分一律作废） |

### §4.5 缺陷条目目录（对账表与逐页清单用这一套号）

| 号 | 条目 | 机器出口 | 当刻命中 |
|---|---|---|---|
| D1 | 分隔符硬串（`·`／`；`／≥3 段并列／`｜`／`、`／`~`） | `audit-separators.mjs` 节点级 R1～R6 | 16/16 页，节点级 231 处（其中 R1 154／R2 6／R3 83／R5 2／R6 1） |
| D2 | 内部标识符上屏（命令键／命令原文／库表名／蛇形名／驼峰参数名／常量名／票号） | 同工具 R7 | 16/16 页，121 处（09-16 最多 20 处） |
| D3 | 同事实一页多处 | 读数件 §3（机器计数 ＋ 人核） | 人核后 29 处 |
| D4 | 触摸目标 <44px | `t516-判据-版式` 的 `touchSmall` | **触摸档（390／768）当刻已绿**（逐页 0 处——**回归门，不得记成缺陷**，见 §4.8）；1440 档 5～49 处＝**只记观感、不扣分**（H6 口径见 §4.2） |
| D5 | 字号下限 <12px | `t516` 的 `minFontPxNoSvg` | 390 档 12/16 页下限 11px（表头） |
| D6 | 缺 `viewport-fit`／安全区只做 `bottom` | 静态扫描 | `viewport-fit` **0/16 页**；安全区 16/16 页各 1 处 |
| D7 | 断点不成组／页面与 HELP 不同档 | 静态扫描（§2.4） | 640×3／400×1／720×1／820×6／821×1，每页 14 条 |
| D8 | 长行（整段可见文本 ≥50 字） | `t516` 读数 ＋ vision | 逐页见读数件 §7 的扣分理由 |
| D9 | 页内无定位（无目录／无 `scroll-margin-top`） | 静态扫描 ＋ `t516-判据-版式` 的 `toc` 计数 | **元素** `class="…ilife-block-toc` 16/16 页 **0**（每页 4 条同名 CSS 规则不算元素）；`scroll-margin-top` 16/16 页 **0** |
| D10 | 图片／GIF 容器形状缺失（无圆角／无比例／画布过小） | vision ＋ DOM 读数 | 09-05 GIF 画布 64×64；多页容器无圆角兜底 |

> **对账表（逐页一行：样张／命令／族／渲染源件:行／手机端现状／缺陷条目号／冗余逐处／分隔符债逐处／改前 vision 分）** 的逐页数字一律**引读数件**（`t524-改前读数.md` §1／§2.2／§3／§6／§7），本件不重复抄，免得两处走散。

### §4.6 判分脚本的一致性自证（同一页：用脚本算 ＝ 用字面公式算，差 0）

复现命令（两条腿写在同一个入仓脚本里，**不共享实现**：一条走函数，一条把 §4.2 的字面公式逐字重打一遍）：

```
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-before       # 16 页基线
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-draft        # 正例
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-09-01        # 反例
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-floor-draft  # 下限页
```

读数（exit 0 即两腿一致；exit 1 表示有页不一致）：

| 读数目录 | 比对项数 | `RECONCILE` 最大绝对差 | 结论 |
|---|---:|---:|---|
| `readings-before`（16 页基线） | 112（16 页 × 7 项：五维 ＋ 硬扣分 ＋ 总分） | **0** | 差 0 |
| `readings-draft`（正例） | 7 | **0** | 差 0 |
| `readings-09-01`（反例） | 7 | **0** | 差 0 |
| `readings-floor-draft`（下限页） | 7 | **0** | 差 0 |

**两句读数的对话**（审查件 §4 原话 `20.9 vs 63.9`）：

- **旧口径**：09-02 一页，口径 A（字面公式，权重取 `§4.1 表`）＝ **20.9**，口径 B（作者草稿 `score.mjs`，另一套权重）＝ **63.9** ⇒ 差 **43**。
- **订正后**：同一个 09-02，脚本腿与字面公式腿**差 0**；基线均分从「旧口径 61」变为**订正口径 18.75**
  （差不是「修好了」，是**旧口径根本不扣 H1～H4**，见 §4.1 注）。

### §4.7 H7 的折叠态口径（审查件 §7-2 点的「没写清」）

页尾「复制数据 ▾」那颗菜单里有 **3 项**（`data-fmt=text/json/csv`，16 页 × 3 ＝ 48 项），
菜单**折叠态是 `opacity:0; visibility:hidden`**（`style.ts` 的 `copy-menu` 规则）——两档截图里都不出现。

⇒ **本件口径**：H7 只数**可视区**——折叠态记 **0 处**（`facts.json` 的 `english` 逐页 0）；
**展开后**那三项该不该扣 −1×3，是**开放项**：要扣就得先把 `copyArea.ts` 的标签改中文（归 #525），
**不许**在基准里偷偷把折叠内容算成命中（会与「VLM 提示器不是证据」自相矛盾）。
写进 `t524-证据.md` §4 的开放项，终审 #530 判前定。

### §4.8 「改前必判红」的例外登记（订正波新增；别把已绿的当缺陷报）

票面的验收口径有一条：**基准里每条机器判据对当刻 16 页必须判红**（不许有一条「改前就已经是绿」）。
订正波按 H6 新口径 recheck 一遍，**必须登记两条当刻已绿的**，并写清为什么它们**不能**当缺陷报：

| 已绿的门 | 当刻读数 | 为什么不能报成缺陷 | 该怎么用 |
|---|---|---|---|
| **H6 触摸档（390／768）** | 16 页逐页 `touchSmall = 0` | 44px 在本技能早已按 `pageUi.ts` 的 `@media (max-width: 820px)` 段做在 390／768 档（H6 改口径见 §4.2）；**它不是本波欠的债** | 当**回归门**用：改后仍须逐页 0；弄成非零即红（识别力靠变异：把 `min-height:44px` 去掉即红） |
| **`env(safe-area-inset-bottom)` 的存在** | 16 页每页 **1 处** | 存在性已绿；**真缺陷是它取了 0**（无 `viewport-fit=cover`，iOS 上 `env()` 恒 0）⇒ 该报的是 `viewport-fit` 那条（0/16，当刻红） | 报 `viewport-fit`（D6），不报「安全区没做」 |

其它机器门当刻**都判红**（可复核）：①分隔符／内部标识符 `RESULT: 0/16` FAIL（231 处）；
③`viewport-fit` 0/16；⑤英文裸词（人工清点，见 §4.7 的折叠态）；⑥重复句人核 29 处；D4 的 1440 档 5～49 处（观感）。
⇒ **本波可当缺陷报的，是上面这些；H6 触摸档与安全区存在性这两条，改后只能当「没退化」报。**

---

## §5 遗留出口

1. **基准已冻**：本件落盘后，16 页的判据读数以本件 ＋ 读数件的哈希为准；后续五票（#525／#526／#527／#528／#529）与终审 #530 复评一律逐条对照本件，**不看更早的旧读数**。
2. **范围外缺陷当场开票**：
   - **无工具的两列**（机审第 ⑤ 英文裸词、第 ③ 触屏三件的「触摸行为」子项）→ **另开票建工具**（建议挂在 `audit-separators.mjs` 的姊妹件上，**别改它**）。
   - `gen-photo-baseline.mjs --check` 当刻 `exit=1`，实况摘要行 **`BASELINE-DIFF 10 pages`** ＋ `UNSTABLE calorie.photo.add`／`calorie.photo.remove`（都是「渲染当刻时钟未被剥净：<当刻时刻>」）⇒ 基线捕获与当刻渲染不一致（旧稿的 `dist/diet/review.js` 导入失败是**更早一次的过时病因**），**不在本票写集**，写进 `t524-证据.md` §3.1，归 **#530 收口**（收口前必须先重建基线）。
   - 09-08／09-14 两页字节 18 MB／23 MB（合计 45 MB 里的 42 MB）⇒ 减重归 **#461**，不在本图判据面。
3. **公共层两处债归 #525**（不许族票自己改）：①读页面页尾「复制日志」置灰禁用——**规则已在库**：`packages/base-render/src/style.ts` 的 `copyButton` 区、当刻 `:430`·`:431` 两条 `[disabled]`／`:disabled` 选择器给 `opacity: .45` ＋ `cursor: not-allowed`（`pageUi.ts` 当刻 `:57`–`:58` 的历史注释已作废，勿再引 `pageUi.ts` 的 `:60`）；②「复制数据 ▾」三格式标签露英文 `JSON／CSV`（`shared/copyArea.ts`，当刻 `:22`·`:48`·`:66`）。
4. **超线件**：`src/photo/helpCenter.ts` **524 LF**（>350 告警线，**旧稿写 523，已漂 1 行**；#529 动它即触发必报五步第四步）；`blocks.ts` **2169 LF**、`style.ts` **1327 LF** 在 #525 写集内，同样要报。动它们前先比 §1.3 的行锚与读数件 §9 的哈希。
5. **#530 收口要交三样**（仓规 `docs/agents/视觉验收墙.md`）：墙（390 ＋ 1280 两张）＋ 逐格缺陷清单 ＋ 墙生成器自检退出码（正例 `exit 0`、反例 `exit 1` 都要走过）。
