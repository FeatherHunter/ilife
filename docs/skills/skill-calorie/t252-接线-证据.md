# t252 · 预检确认页 11 条写词执行接线证据（实施席/BLOCKED）

票：#252「预检确认页 · 11 条写词的执行接线」。调查席写集未在仓内找到（`.scratch/t252*` 不存在、`docs/skills/skill-calorie/t252*` 不存在、issue 252 评论 0 条），本席按票面「要做什么 1–7＋判据＋红线」作写集，跳过清单未见即不动。

结论先行：**BLOCKED—— worktree 全包编译被他席在途 T573 的 `trendPredictDocs.ts` 语法红挡住，本票源码改动无法持锁验证，故源码一行未动，只落本证据（docs 件，不影响构建）**。11 条基线与 3 条 exec 的只读实测已取，缺口已定位到两处守卫（改类 exit4、暂停/重启不出页），下一手在 T573 落定后按 §五照抄即合。

## 一、影响清单（一行一个能力目录）

| 文件 | 动作 | 理由 |
|---|---|---|
| `packages/skill-calorie/src/goal/read.ts` | 拟改两处（未动，见 §五） | `viewGoalWizard` 加守卫：改类三词空库 exit4；暂停/重启两词一律 exit2 不出页。只碰目标管理能力目录。 |
| `packages/skill-calorie/test/t252-goal-wizard.test.mjs` | 拟新建（未建） | 本票断言：11 条逐条出完整预检页；3 条桶为 exec；空库 exit0 无假数字；改类空库 exit4 不落盘。待构建恢复后落地。 |
| `docs/skills/skill-calorie/t252-接线-证据.md` | 新建（本件，已落） | 本票证据：基线读数＋缺口＋下一手接线。归属件自己的目录。 |

结构五步：① 本表即影响清单；② 无新建目录层级，只动 `src/goal/` 一个能力目录，不碰三能力线；③ 写码待下一手（§五 diff 照抄）；④ 超线报警：本包线 350 LF——`src/goal/read.ts` 160、`precheck.ts` 250、`set.ts` 192，均在线内；`src/triggers/wake-assets.ts` 4747 已超线但本票一行不碰，不触发新报警；⑤ 交付对账：本轮只交付本证据一件，偏差零（源码零碰）。

红线遵守：未新增写命令；25 条唤醒词名字零改；两份提示词（`scene-06-goal.ts`＋`wake-assets.ts`）本轮零改，指纹测试未碰。

## 二、基线自证（持锁，只读，不编译源码）

```
GATE-RUN runId=22cf1d8c-84f5-473d-8845-8fd7428b425b cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie exit=1
  红因：packages/skill-calorie/src/render/trendPredictDocs.ts(774,28)/(844,28)/(943,28) error TS1005: ')' expected（他席 T573 在途，见 §六）
GATE-RUN runId=1d20eba7-ff8c-46e7-8657-5d951edf15dc cmd=node --test packages/skill-calorie/test/goal-wizard-251.test.mjs exit=0 tests 4/pass 4/fail 0
GATE-RUN runId=762ed29f-805e-4da8-b188-e10748b36f78 cmd=node .scratch/t252/probe11.mjs exit=0
GATE-RUN runId=bd5de65b-f6bb-4d58-8bc7-9f86b3247150 cmd=node --test packages/skill-calorie/test/wake-assets-133.test.mjs exit=0 tests 8/pass 8/fail 0
```

11 条逐条（`calorie.view.goal-wizard --params '{"wake":"…"}'`，空库/有库各一遍，`CALORIE_TODAY=2026-09-07`，日志 `.scratch/t252/probe11.log`）：

| 唤醒词 | 空库 exit/字节/doctype | 有库 exit/字节/doctype |
|---|---|---|
| 定营养目标 | 0/71666/有 | 0/71711/有 |
| 定营养目标(自动算)+profile cut | 0/71655/有 | 0/73423/有 |
| 定体重目标 | 0/71394/有 | 0/71439/有 |
| 定体重目标(自动算截止) | 0/71596/有 | 0/73364/有 |
| 定体重目标(含起始日) | 0/71584/有 | 0/71629/有 |
| 定饮水目标 | 0/71332/有 | 0/71377/有 |
| 定饮水目标(自动算)+profile cut | 0/71519/有 | 0/73287/有 |
| 一键定全套目标+profile cut | 0/71751/有 | 0/73519/有 |
| 改营养目标 | 0/71655/有（应为 exit4，缺口） | 0/71700/有 |
| 改体重目标 | 0/71426/有（应为 exit4，缺口） | 0/71471/有 |
| 改饮水目标 | 0/71342/有（应为 exit4，缺口） | 0/71387/有 |

3 条桶状态（`src/goal/routes.ts:15,20,21`＋生成物 `routes.generated.ts:221,226,227` 均为 `kind:'exec', key:'calorie.view.goal-wizard'`）：已是 exec，无需改判。`routing.ts:128-133` 那段“保留 non-exec”注释是 stale 文案，逻辑以生成物为准。

空库无假数字：空库+profile cut 出“不出推荐数字”、无“推荐方案与依据”段、有“未设置”（`.scratch/t252/probe11.log---EMPTY-NO-FAKE---`）。`calorie.view.goal-recommend` 空库仍 exit4 不落盘（strict，与 `render-t9.test.mjs:229-230` 钉住一致，本票不动它）。

暂停/重启经 wizard 当前各 exit0 有页（应不出页，缺口，见下一节）。

## 三、逐条路径

- `packages/skill-calorie/src/triggers/scene-06-goal.ts`：只读。11 条 prompt_template 均已有“请先出预检确认页给我看…我确认后再写入”；3 条自动算词 `main_prompt.cli` 已是 `goal-wizard`；暂停/重启两条无预检句、cli 直走写命令。已合票面 §2 后半与“暂停不出页”的 prompt 侧，无需再改。
- `packages/skill-calorie/src/triggers/wake-assets.ts`：只读。对应三行 prompt 与上一件逐字同（含预检句），指纹 8/8 绿。零改。
- `packages/skill-calorie/src/goal/routes.ts`＋`src/triggers/routes.generated.ts`：只读。3 条已 exec，入口即预检页命令。零改。
- `packages/skill-calorie/src/goal/precheck.ts`（250 LF）、`set.ts`（192 LF）：只读。统一页已含营养 5 项表、体重 8 行块、推荐段（有档才出）、三通道复制区；缺项走 `energyOf` 写“缺×，不算”。按词过滤与改类 diff 表不在本票最小口径内，未动（统一页即判据“完整预检页”）。
- `packages/skill-calorie/src/goal/read.ts`（160 LF）：只读。`viewGoalWizard` 对 11 条均 exit0 出页；`command` 经 `routeWakeword`（别名表，对 11 条恒空）回落 `main_prompt.cli`，结果与路由层逐字同（8 条写命令／3 条 wizard），行为对但 ` :128-129` 注释 stale（“三条今天还没有可执行路由”已错）。改动见 §五。
- `packages/skill-calorie/src/goal/commands.ts`：只读。`calorie.view.goal-recommend` 已在目标目录（#318 搬入），代表唤醒词与路由错位（`定营养目标(自动算)` 落 wizard）是 #318 明文留待收口票的事项，本票不动。所谓“认领”即确认不另造第二张推荐页：3 条的预检形态走统一 wizard 页内的推荐段，recommend 命令保持 strict 供“看目标推荐”别名直调，两页职责不并。
- `packages/skill-calorie/src/goal/write.ts`／`nutritionGoal.ts`／`goalStore.ts`：只读。写命令不分定/改（同键），故“改类空库 exit4”只能落 wizard 层（按 `wake` 区分），不碰写命令。

## 四、没做完＋下一手缺啥

没做完：§五两处守卫＋配套断言＋变异自证（源码零碰，故变异未跑）。判据对照：11 条出完整页 PASS；3 条 exec PASS；空库 exit0 无假数字 PASS；改类空库 exit4 FAIL（当前 exit0 有盘）；靶向全绿 PASS（既有 4+8），新断言待补。

下一手缺：T573 落定后的干净构建窗（`tsc -b packages/skill-calorie` exit0）。拿到后按 §五改两处，重跑 `goal-wizard-251`＋新建 `t252`＋`wake-assets-133`，做单行变异（改坏守卫条件致红、还原致绿，给两行读数），再提交。

## 五、下一手照抄（`src/goal/read.ts::viewGoalWizard` 内，未应用）

```diff
   const wakeWord = optStr(params, 'wake') ?? null;
+  // #252 · 暂停/重启不出预检页（没有可确认的内容），直接用法阻断，不落盘。
+  if (wakeWord === '暂停所有目标' || wakeWord === '重启所有目标') {
+    throw new CalorieRenderError('bad-input', '暂停／重启不出预检页（没有可确认的内容，直接跑写命令）');
+  }
   const draft = buildGoalDraft(db, { profile: profileRaw === undefined ? null : profileRaw });
+  // #252 · 改类词空库阻断：无目标行时 exit4 不落盘；定类空库 exit0 照常出页。
+  if ((wakeWord === '改营养目标' || wakeWord === '改体重目标' || wakeWord === '改饮水目标') && draft.current === null) {
+    throw new CalorieRenderError('missing-data', '尚无目标（先定目标，再改目标）');
+  }
```

另将 `:128-129` 注释“今天还没有可执行路由…归 #252”改为“3 条已有 exec 路由（`src/goal/routes.ts`），此处回落值与路由层逐字同”。仅此一件三行级改动，不碰提示词、路由、写命令、推荐 strict。

## 六、阻塞实据（跨界，一律不碰）

worktree 他席在途：`M packages/skill-calorie/src/render/trendPredictDocs.ts`（T573 R-60，`pageSection` 加 `heading` 第三参，9 处调用点改半，774/844/943 括号未闭）、`M packages/skill-calorie/src/analysis/reportDoc.ts`、`M packages/skill-calorie/src/diet/rankingDocs.ts`。本席未读其内容 beyond 编译报错行，未 `add` 其任一件。需 T573 闭合括号并自证后，本票 §五方可进场。
