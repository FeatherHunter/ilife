# #313 收口席（票号 `313b4`）· 验收探针归类归位 ＋ 票面 95% —— 证据

- **席**：wayfinder #293 → 票 #313 的**收口席**（B 段最后一块）。票号标签 `313b4`；写集只有 `docs/**`。
- **开工**：`2026-09-13T10:06:03Z`（本席第一条持锁命令；对账窗口 `--since` 取它前 3 秒）。
- **提交**：`69c8e68`（首件：探针＋变异自证驱动）；`e680d64`（本文件 ＋ 对账源导出）。
- **落盘日志**（仓内未跟踪 `.scratch/t313b4/`）：`t293-before.log`／`t293-after.log`／`t293-after2.log`／
  `mut-proof.log`／`mut-proof2.log`／`probe-report-M2.json`／`probe-report-M2-rerun.json`／
  `gate-build.log`／`gate-gencheck.log`／`gate-test.log`（被打断的那遍）／`gate-test2.log`／`gate-oracle.log`／
  `my-runs.txt`（本席 12 条持锁条目）。

---

## ① 改了哪几处（改前 → 改后）

只改两件（都在 `docs/**`）：`docs/skills/skill-calorie/t293-验收-命令自治.mjs`（769 → 884 行）与新增
`docs/skills/skill-calorie/t313b4-变异自证.mjs`（142 行）。提交 `69c8e68`：2 files changed，288 insertions(+)，31 deletions(-)。

行号一栏的「改前」按 B 段文档席证据 `t313b3-文档与验收-证据.md` §④ 给的行号逐条对上（实读有一处错位，见末行）。

| 改前（`t293-验收-命令自治.mjs`） | 改后 |
| --- | --- |
| `:40` `ROUTING: '…/src/triggers/routing.ts'` | **保留**（文件仍在，只是不再含记录）；另加 `ROUTES_GEN`／`ROUTE_DECL_DIR`／`GEN_ROUTES` 三个路径常量 |
| `:165` 桶注释：「② DISCIPLINED-SHARED 必须手改，但已被认下并写进 #296「照抄说明」第②项（routing.ts）」 | 「② …（**#313 B 段起路由已不在这一桶**：声明住能力目录／`legacy/routes/scene-NN.ts`，汇总位 `src/triggers/routes.generated.ts` 是生成物——归类判据见 `routeSurfaces()`）」；① 那条也补了「＋路由声明面」 |
| `:252` 注释：「`routing.ts`：手写路由表（新键要能被唤醒词命中就得加一条 exec 路由），有机器守」 | 换成三件：`genTargetsBlock()`（从 `gen-cli.mjs` 切出 `targets` 数组）／`capabilityRouteDecls()`（按目录枚举 `src/<能力>/routes.ts`）／`routeSurfaces()`（记录面＋五条判据），注释写明「记录面＝生成物，不是手写表」 |
| `:253-268`（实读在 `:256`）`routingSurface()`：直接读 `routing.ts` 数 `kind:'exec'` 的键——今天恒为 **0**（记录已搬走），却仍以「手写表」名义进 ② 桶 | `routeSurfaces()`：读**生成物**数记录（本次实测 `WAKE_ROUTES 436 条 ＋ NEW_KEY_ROUTES 58 条 ＋ COVERAGE_REPAIR_ROUTES 1 条 ＝ 495 条／exec 403 条`），并同时判 banner／是否在 gen-cli 的 targets 里／gen-routes 是否靠 `routeDeclarationSources()` 出声／声明源是否齐／`routing.ts` 自身是否已不带记录 |
| `:310-314` `out.disciplined.push({ where: P.ROUTING（手写 exec 路由覆盖 0 键）… })` | **删**。改成：判据任一不成立 ⇒ 每条 `out.unaccounted.push({ where, what: '路由归类判据不成立 ⇒ 路由整面按未认下记：…' })`（⇒ P1=FAIL）；全都成立 ⇒ `out.derived.push('路由三面：…')`（见 §② 末的实测原文） |
| `:461`／实读 `:469` CLASS 表 `routing.ts ⇒ 'ROUTE（唤醒词路由表，手写）'` | `routing.ts ⇒ 'DERIVED（路由层：记录面是 routes.generated.ts，本件只再导出）'`；**另补四条**：`routes.generated.ts ⇒ 'DERIVED（路由记录面，gen-routes 生成）'`、`src/cli/legacy/routes/scene-NN.ts ⇒ 'ROUTE/AUTHORITY②（路由声明，键恰住一处）'`、`src/cli/legacy/routes/index.ts ⇒ 'ROUTE/AUTHORITY②（路由声明汇总件，十片拼接）'`、`src/<能力>/routes.ts ⇒ 'ROUTE/AUTHORITY②（能力路由声明，键恰住一处）'` |
| `:535` 结论口径「事实对只许落在 FROZEN-PARITY／ROUTE／TEST／DOC／DERIVED」 | 「…只许落在 FROZEN-PARITY／**ROUTE/AUTHORITY②**／DERIVED／CAPABILITY／TEST／DOC」（名字随分类改；`CAPABILITY` 是照 P2 实测输出的实际取值补的） |
| 文件头纪律「缺文件／缺脚本不抛栈：该条报 PENDING」 | 补一条**例外**：沙箱基线 `gen:check` 非 0 **不记 PENDING 而显式判红**（见 §⑤） |
| （新增） | `t313b4-变异自证.mjs`：两个变异 ＋ 无条件还原 ＋ 逐字节/`git status` 核对（见 §③） |

## ② 归类判据：为什么不硬写「它已生成」

`routeSurfaces()` 里五条**机械判据**，每条各有 FAIL 分支（任一条不成立 ⇒ 路由整面落 ③ UNACCOUNTED ⇒ **P1=FAIL**）：

- **a）生成物在，且带生成 banner**：`/本文件由\s*`scripts\/gen-routes\.mjs`\s*生成/`。
- **b）生成物在 `scripts/gen-cli.mjs` 的 `targets` 数组里**（`pnpm gen:check` 逐件比对这个数组 ⇒ 这门真覆盖它）；
  另查 `gen-cli.mjs` 引了 `./gen-routes.mjs`（出声道）。
- **c）`gen-routes.mjs` 出声靠 `routeDeclarationSources()`**（声明源清单，不是内置表）。
- **d）声明源齐**：`src/cli/legacy/routes/scene-01..10.ts` 十片全在 ＋ 汇总件 `index.ts` ＋ 至少一件能力
  `src/<能力>/routes.ts`（按目录枚举，不写死名单）。
- **e）记录面真落在这里**：生成物里记录 > 0 且 exec > 0（空壳生成物即红），且 `routing.ts` 去注释后
  已不带 `kind:'exec'` 记录（两个记录面并存即红）。

判据全部成立时，① 派生侧那一行的实测原文（`.scratch/t313b4/t293-after2.log:19`）：

> 路由三面：① 记录面＝生成物 packages/skill-calorie/src/triggers/routes.generated.ts（WAKE_ROUTES 436 条 ＋
> NEW_KEY_ROUTES 58 条 ＋ COVERAGE_REPAIR_ROUTES 1 条 ＝ 495 条记录／exec 403 条；带生成 banner=true，
> 在 gen-cli 的 targets 数组里=true ⇒ `pnpm gen:check` 逐件比对它）；② 声明权威源＝12 件
> （…/cli/legacy/routes/scene-01..10.ts ＋ index.ts ＋ …/weight/routes.ts），由 …/scripts/gen-routes.mjs 的
> `routeDeclarationSources()` 出声，声明源另进 build 的内容印记（改声明不 build ⇒ GEN-STALE FAIL）；
> ③ …/src/triggers/routing.ts 只再导出记录与类型（自身带 exec 记录=false）——加一条命令＝在能力目录补一条
> 路由声明，汇总位与路由层都不必手改；唤醒词覆盖仍由 test/calorie-routing-81.test.mjs:105 的
> `coveredKeys = DECLARED_KEYS.length` 对账（新键不加声明 ⇒ 覆盖数 ≠ 声明数）

另有一条**独立佐证**：门禁 `pnpm gen:check` 的输出里逐件点名生成物——
`GEN-CHECK ok packages\skill-calorie\src\triggers\routes.generated.ts sha256=e9eaa993518ec3…`（见 §⑦ 的 gen:check 条目）。

## ③ 变异自证（判据真能被打红）

驱动：`docs/skills/skill-calorie/t313b4-变异自证.mjs`（仓内**临时**改一处 → 跑探针 `--json` → **无条件还原** →
核 sha256 与原字节一致 ＋ `git status --short` 为空）。日志：`.scratch/t313b4/mut-proof2.log`。

| 变异 | 判据行出现 | 沙箱基线同时变脏 | 探针 P1 | 还原核对 |
| --- | --- | --- | --- | --- |
| **M1** 把 `routes.generated.ts` 从 `gen-cli.mjs` 的 `targets` 数组里摘掉 | ✓（`不在 gen-cli 的 targets 数组里`） | **否**（基线仍绿） | FAIL（exit=1） | 逐字节一致 ✓／`git status` 干净 ✓ |
| **M2** 改掉生成物的生成 banner（生成 → 产出） | ✓（`生成物没有生成 banner`） | 是（`gen:check` 同时不一致） | FAIL（exit=1） | 逐字节一致 ✓／`git status` 干净 ✓ |

M1 的意义：**只有归类判据这一条被触发**（沙箱基线仍绿），所以 P1=FAIL 不是靠别的红顺带——判据自己有牙。
M2 的意义：除了 banner 判据，还顺带证明 §⑤ 那条「基线脏 ⇒ 显式判红」真的会亮（`.scratch/t313b4/probe-report-M2.json`
的 P1 两行都写着）。

第一次跑（`mut-proof.log`，runId `d07286de…`）与加打印后的第二次跑（`mut-proof2.log`，runId `3eacf66e…`）结论一致；
两次都在 `finally` 里还原并核对。

## ④ 五探针实测（改前／改后都 5/5）

| 时点 | 命令 | runId | 结果 |
| --- | --- | --- | --- |
| 改前（769 行版本） | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `18b694a1-efe7-40ef-967b-9fcb418a964b` | `P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5` |
| 改后（+分类/判据，未加逐列表条数） | 同上 | `cf22ad05-e769-4c84-a234-4837f87b4281` | `5/5` |
| 改后（终稿） | 同上 | `fc69672e-2a42-4e47-a479-b521e28f50ab` | `5/5` |

改前 ② 桶那一行（假话的原状，`t293-before.log:20`）：

> · packages/skill-calorie/src/triggers/routing.ts（手写 exec 路由覆盖 **0** 键）｜新键要能被唤醒词命中就必须往路由表加一条
> exec 路由（手写表，键必在键表内）…

改后（`t293-after2.log:19-23`）：② 桶 **0 处**（附一行说明「#313 B 段起路由不在这一桶」）、③ UNACCOUNTED **0 处**、
路由三面进 ① 派生侧（原文见 §②）。

## ⑤ 顺手修的一处假绿：沙箱基线脏改显式判红（B-3 证据 §⑥1 的建议）

- `p1()`：沙箱里 `gen:check` 基线非 0 时，原先是 `set('P1', unacct ? 'FAIL' : 'PENDING', …)`——并发席在途时
  **静默降级为「未判」**。现改成**恒 FAIL**，并把原因写清楚：临时根是仓内现树的副本，基线红只可能来自仓内事实
  （生成物与生成器不一致／声明改了没 build ⇒ 仓内 `pnpm gen:check` 同样红）。
- `p3()`：同一处由 `{ ok:false, pending:true }` 改成 `{ ok:false, pending:false, selfRed:true }` ⇒ P3=FAIL。
- 缺文件／缺前置（dist 没 build 等）**仍是 PENDING**（口径未动：那是「缺前提、还没法判」）。
- 实测：M2 的报告里两行都在（`.scratch/t313b4/probe-report-M2.json`）。

## ⑥ 票面 95%（`gh issue edit 313 --body-file`）

- 正文 47 行 → 86 行（7321 字节，LF、无 BOM、无字面 `\n`）。提交件与 `gh issue view 313 --json body` 回读
  **逐字节相等**（`.scratch/t313b4/issue-verify.txt`）。
- 增改**只有三处**（逐行比对见 `.scratch/t313b4/body-diff.txt`）：
  1. **新增一节** `## 进度：95%` ＋ 空行 ＋ `下一步：待对抗式复核（见下）`（38 行）——内含「本段落地了什么」
     与 7 条**待确认清单**：① P-A 印记守卫（改声明不 build ⇒ `GEN-STALE FAIL`）② P-B 分区权威（跨件同键 ⇒ 抛）
     ③ 对照物**须先 `pnpm build`** 再跑并核 `DIST-SHA`（期望 `DEEP-EQUAL=yes faces=16`）④ t293 五探针 5/5
     ⑤ 全量测试 ④ CI 结论须区分「票外红」与「本次红」 ⑦ 一处措辞待判（生成物落位与 Destination 措辞的差异）。
  2. **过期数字实测更正**（第 27 行，原文保留，加括号更正）：`814 行／498 条记录` →
     **改造前 816 行、改造后 267 行**；记录 **495 条** ＝ `WAKE_ROUTES` 436 ＋ `NEW_KEY_ROUTES` 58 ＋
     `COVERAGE_REPAIR_ROUTES` 1 ＝ `ALL_ROUTES` 495（照对照物口径）。
     实测法：`git show 4e55157:…/routing.ts` 数行（817 个 `\n` 分段＝816 行；记录行 495）与现树 `routing.ts`（268 段＝267 行）；
     对照物 `t313-routing-exec-face-before.json` 的 `WAKE_ROUTES`/`NEW_KEY_ROUTES`/`COVERAGE_REPAIR_ROUTES`/`ALL_ROUTES`。
  3. 45% 那一节末行加尾注：`下一步：B 段…接手` → 同句 ＋ `—— **已完成**，见下「进度：95%」。`（免得与新增节自相矛盾）。
- **未做**：未 close 票、未写 100%、未动 Destination／完成判据／45% 节其它文字。

## ⑦ 门禁（全部持锁 `--ticket 313b4`，逐条 runId 可对账）

下面每行一条声明（`cmd` 与审计日志逐字对应，`runId` 一对一绑定）：

- GATE-RUN runId=b9ab7c60-b06c-4757-86f7-af8346c7577e cmd="pnpm build"
- GATE-RUN runId=5f0539cd-9fe8-4cce-8102-eba91b3abf85 cmd="pnpm gen:check"
- GATE-RUN runId=738005b7-a70d-46ca-8977-aa75e348cec7 cmd="pnpm test"
- GATE-RUN runId=ffa7a308-6252-4704-9d4f-becc73afe0a0 cmd="pnpm test"
- GATE-RUN runId=75511086-8cc2-4f99-89a8-3a7d0e2d0c19 cmd="node docs/skills/skill-calorie/t313-routing-oracle.mjs"
- GATE-RUN runId=18b694a1-efe7-40ef-967b-9fcb418a964b cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
- GATE-RUN runId=cf22ad05-e769-4c84-a234-4837f87b4281 cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
- GATE-RUN runId=fc69672e-2a42-4e47-a479-b521e28f50ab cmd="node docs/skills/skill-calorie/t293-验收-命令自治.mjs"
- GATE-RUN runId=d07286de-3bd0-4431-8e15-d5619677ff95 cmd="node docs/skills/skill-calorie/t313b4-变异自证.mjs"
- GATE-RUN runId=3eacf66e-987e-4662-970e-d43a1cc06483 cmd="node docs/skills/skill-calorie/t313b4-变异自证.mjs"
- GATE-RUN runId=7ff4cd35-14e6-460b-8002-8da65d390e44 cmd="git show 4e55157:packages/skill-calorie/src/triggers/routing.ts"
- GATE-RUN runId=6814919b-7549-4ae6-bb5a-2ef2ebc17041 cmd="git commit -F .scratch/t313b4/commit-msg-1.txt"

逐条结果：

- **build**（`b9ab7c60…`）exit 0：`GEN-STAMP ok …：声明源 23 件`（十片场景分片 ＋ `weight/commands.ts` ＋
  `legacy/routes/index.ts` ＋ 十片路由声明分片 ＋ `weight/routes.ts`）。
- **gen:check**（`5f0539cd…`）exit 0：逐件 `GEN-CHECK ok`，含 `…/src/triggers/routes.generated.ts sha256=e9ea…d7e9`；
  `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）`。
- **test 干净那遍**（`738005b7…`）exit 0：`tests 1492／suites 160／pass 1492／fail 0`（`.scratch/t313b4/gate-test2.log`）。
- **test 被打断那遍**（`ffa7a308…`）**exit=3221225786**（`0xC000013A`＝STATUS_CONTROL_C_EXIT）：本席按键打断，
  如实声明并重跑（见 §⑨1）——**这一条正是严格对账唯一过不去的地方**，见 §⑧。
- **对照物（build 之后跑）**（`75511086…`）exit 0：`RESULT: SUMMARY faces=16 diffFaces=0 diffs=0 surfaceMissing=0
  surfaceExtra=0 DEEP-EQUAL=yes SNAPSHOT-SHA=d4da337219ea DIST-SHA=4602f79d6927`。
- **探针三遍**：改前 `18b694a1…` 5/5、改后 `cf22ad05…` 5/5、终稿 `fc69672e…` 5/5（原文见 §④）。
- **变异自证两遍**：`d07286de…`、`3eacf66e…` 都 exit 0 且 `VERDICT: OK`（还原核对见 §③）。
- **`git show`**（`7ff4cd35…`）exit 0：票面数字实测（见 §⑥）。
- **提交**（`6814919b…`）exit 0：提交 `69c8e68`。

## ⑧ 机械门禁对账（`tooling/check-gate-audit.mjs`）

- **窗口**：`--ticket 313b4 --since 2026-09-13T10:06:00Z --until 2026-09-13T10:30:00Z`（起＝本席第一条持锁命令
  10:06:03Z 前 3 秒；止＝对照物 10:29:49.957Z 之后、**收尾提交之前**）。窗口内的 `ticket=313b4` 条目**恰为本席这 12 条**；
  同一时段他人条目是 `ticket=313b2` 的 3 条，被票号过滤排除（本席**没有**动任何放宽开关去绕它们）。
- **日志**：`.scratch/locks/gate-runs.log`（本机共享、gitignored）。**导出（受跟踪的对账源）物**：
  `docs/skills/skill-calorie/t313-gate-runs-313b4.log`。
- **严格口径（不加任何放宽开关）**：`gate-audit: FAIL`，`RESULT: matched=11/12 auditEntries=1523 scoped=12 undeclared=1`——
  **唯一原因**是 `ffa7a308…` 那条 `exit=3221225786`（被本席按键打断的那遍 `pnpm test`）按严格口径认领不到
  （`缺失 … runId=ffa7a308-… 未命中（或 exit≠0）`）。本席**不隐藏它**（隐藏＝`undeclared` 也是 FAIL）：
  如实声明、如实标 exit，然后重跑干净。
- **纳入该条（`--allow-nonzero`）**：`gate-audit: PASS`，`RESULT: matched=12/12 … undeclared=0`。
  按协议 §2.4 的口径，放宽**写明在本证据里**（下面这行），理由只针对那一条：
- GATE-RELAX flag=--allow-nonzero reason=票号 313b4 本席自己的一次 pnpm test 被终端按键打断（exit=3221225786，STATUS_CONTROL_C_EXIT），随后已重跑干净（runId 738005b7-a70d-46ca-8977-aa75e348cec7，exit=0，1492/1492）；该条为如实留痕而声明，不放宽其它任何口径。

原始输出落 `.scratch/t313b4/gate-audit-strict.log` 与 `gate-audit-allow-nonzero.log`；本席**只**用了这一个放宽开关，未用
`--allow-no-claims`／`--allow-undeclared`／`--allow-no-runid` 任何一个。

**窗口边界的一条口径**：把证据与对账源落地的那次提交（`git commit -F .scratch/t313b4/commit-msg-2.txt`，
runId `5c8fc128-dbc7-4128-b3b9-484d5afd98d6`）与随后的 `git push` 都发生在 `--until=2026-09-13T10:30:00Z`
**之后**，因此**不进**本窗口的对账——协议 §2.4 写明「窗口外的条目（他人票号、提交后的 git 条目）不受影响」；
若把它们塞进窗口却又不声明，反而会因「窗口内有无人声明的 RUN 条目」误红。复核席若要审计收尾那几步，
用同一个日志把 `--until` 往后放再跑一次即可（那时按本席同样口径声明 `5c8fc128…` 与 push 两条）。

## ⑨ 未做／未确证 ＋ 风险

1. **打断与重跑（自伤，已如实记账）**：第一遍 `pnpm test` 跑到 41 s 时我向终端发了下一条命令，被 `cmd.exe` 读成
   「Terminate batch job?」⇒ 该进程以 `0xC000013A`（3221225786）结束（它已打印 `tests 1492 / pass 1492 / fail 0`，
   但那不是可采信的干净绿）；已重跑一遍干净（runId `738005b7…`，exit=0，1492/1492）。**教训**：长命令期间不许向该终端写字节。
2. **未做**：未 close 票、未写 100%（按派单，留给复核）；未独立复跑 P-A／P-B 变异（那是复核席的面）；未核 CI
   （本席没跑 CI；复核席看 CI 时须区分「票外红」与「本次红」）；变异自证只做 M1／M2 两条，**没做**「摘掉一件声明源
   ⇒ 判据 d 变红」这第三种（未确证）。
3. **判据 b 的解析口径**（可被变异打红的代价）：它从 `gen-cli.mjs` 里切 `const targets = [` … `\n  ];` 这段文本。
   若生成器换写法（例如 `targets` 不叫这名、或数组字面量拆成两处拼接），判据会**报红**而不是静默放过——**报红方向是安全的**，
   但那时要跟着改探针（已写进注释）。
4. **新风险（显式判红带来的）**：并发席**正在**写 `packages/**` 时跑本探针，沙箱副本可能自相矛盾 ⇒ 现在会判红
   （原先记 PENDING＝未判）。口径上这是「树不一致就是红」，但仍有一条**脆弱面**：若别的席只是改了声明还没 rebuild，
   这条红与 P-A 的 `GEN-STALE FAIL` 是同一件事，复核席重跑一次即可分辨。**本席未为此加任何放宽开关。**
5. **票面落位差异**：票面「Destination」写「生成器产出 `triggers/routing.ts`」，实际是记录面住
   `src/triggers/routes.generated.ts`、`routing.ts` 保留为再导出＋查找层（导出面 15 个一个不少，对照物
   `EXPORT_SURFACE before=15 now=15`）。**是否接受**由复核席判（已写进票面待确认第 7 条），本席未改 Destination 原文。
