# #264 独立对抗审查报告（2026-09-14，不采信实施者结论，全部持锁自跑）

- 路径集合复述：只写 `docs/skills/skill-calorie/t264-review-*`（本件＋探针＋运行记录）、`.scratch/t264-review/`（草稿日志）、`.changeset/264-review-*.md`（如需，本次无）；禁碰 `src/exercise/` 下实施件、两被审测试、冻结表、routes/commands/分派层/生成物。复跑只读＋持锁；实施件需改不动手，只记缺陷。
- 被审对象：`f7d5d90`（7 件）＋`df57752`（1 件），`merge-base --is-ancestor` 对 HEAD 均为 exit 0；票面 `gh issue view 264`（OPEN，13 条写词出完整文档）与交付目标一致。

## 判定：PASS（均分 93，无 S1，无未记录过程违规）

## 机器证据

| # | 持锁命令 | runId | exit | 摘要行 |
|---|---|---|---|---|
| 1 | `node --test exercise-receipt-264.test.mjs` | 2be21949 | 0 | pass 13 fail 0 |
| 2 | `npx tsc -p packages/skill-calorie/tsconfig.json --noEmit` | 3a131bfc | 2 | 唯一错误 `analysis/routes.ts:109` multi-trend 未登记；`src/exercise/` 0 错误 |
| 3 | `pnpm build` | e2054bbb | 2 | 同上单错，`tsc -b` 失败；无 exercise 错误 |
| 4 | 回归 5 件（cmd-write-40/m5-97/softdelete-125/profile-179/cmd-registry-294） | 9669d602 | 1 | tests 86 pass 82 fail 4（见归属） |
| 5 | 新探针 `.scratch` 版 | 92bd8267 | 0 | P1_EX_ALIVE=GREEN／P2=GREEN／P3=GREEN／FROZEN3=GREEN／DEEP_ALL=GREEN |
| 6 | 新探针取反 `probe-deep.mjs neg` | 6f49b5be | 1 | `AssertionError: NEG 取反：错值 450 卡应不存在`（鉴别力为真） |
| 7 | 新探针版本库版 `t264-review-probe-deep.mjs` | d2ca5b79 | 0 | 全 GREEN 行复现（含 FROZEN3／DEEP_ALL），版本库可复跑 |

- 变异红/还原一致：实施者 `.scratch/t264/test-mut.log`（眉标改坏→单条红 `记运动 缺运动眉标`）与 `test-full.log`（13/13 绿）存在；审查者禁碰实施件未独立复跑变异，以 #6 取反红作鉴别力旁证（未做项）。
- 新探针盲区行：`SHALLOW_ON_WRONG_COUNT=GREEN(盲区实锤)`（把 350 卡篡改为 999 卡，被审同款浅检查仍绿）／`DEEP_ON_WRONG_COUNT=RED(能抓错数)`。

## 逐条路径

1. 独立复跑：#1 主测试 13/13 绿，与交付 `0231c399` 一致；#2/#3 他人红归属逐条复核——今日树红是 `analysis/routes.ts:109 multi-trend`（工作区未提交多头，在途票产物），非票面所记 `diet/routes.ts:36`／`photo/gif.ts:263`（树已前移），但“他人红、非本票”结论不变，exercise 零错误成立。
2. 新探针（`t264-review-probe-deep.mjs`，版本库内可复跑，#7 已复现全绿）：P1 删后当日累计须排除软删行（350 卡/35 分钟/2 条，`EX_ALIVE` 口径）；P2 旧值 30 须在 → 左、新值 40 在右；P3 批量 3 条写入计数与 caption；P4 盲区演示；P5 冻结 verbatim 三样例（卧推 60kg／改某日补记／范围删）。红绿两行见上表 #5/#6。注：中途 `dist/triggers/scene-10-analysis.js` 被他人重建写坏（SyntaxError，他人工种），探针遂与 `dist/index.js` 解耦（改走 `node:sqlite` 建空库，CLI 直跑不受影响），#7 为解耦后版本库版绿证。
3. 收窄变放宽：P5 用冻结表逐字参数（仅 `<日期>` 填真日）三形态全绿；P1 证明“应抓（软删污染累计）仍被抓”——朴素和 450 卡不在页内。
4. 对账：交付 GATE-RUN runId 抽查三条（921d663d／87ad099d／0231c399）在 `gate-runs.log` 中 START/RUN 一对一、exit 一致；`.scratch/t264/` 24 件日志在盘；`f7d5d90` 7 件＋`df57752` 1 件均为本票件（此前 11 件统计是审查者取错区间 `f7d5d90^..df57752` 横跨他票，已纠正，非实施者问题）；等待量（waitedMs 0/1/30001，末笔系等 ticket 333 活锁释放）均落盘。
5. 禁区：7 件清单外无分派层/路由/命令声明/冻结表/生成物/读命令改动；`receipt.ts` 266 行／`log.ts` 110 行／`edit.ts` 134 行均 <350；`receipt.ts` 无 `any`；changeset 在位。

## 缺陷（均非 S1）

- D1（本票范围·S3 建议）：`batchTable` 跳过/失败硬编码 `0 条`，被审测试只断言字符串、P4 实锤计数值错仍绿。今日行为一致（批量无跳过失败路径），不判 FAIL；建议后票补数值断言。转不转票由下一手定。
- D2（本票范围·S3 轻微）：交付 GATE-RUN 两条 `npx tsc（变异后/还原后重出）` 命令文本省参数，可复核性扣分，日志本身在盘。
- D3（范围外发现·S3 转票，不判 FAIL）：回归 4 红——3×`calorie.view.multi-trend` 未登记（工作区 `analysis/multiTrend*.ts`＋`combos.yaml` 未提交多头）＋1×`profile-doc-179` 反面 `water.log 已是整页`（在途饮食票产出）。均不涉运动三键，转对应票。
- D4（范围外发现·记录）：`pnpm build` 红点由票面 `diet/routes.ts:36` 漂为 `analysis/routes.ts:109`，树前移所致，结论“他人红”不变。

## 五维打分：93

契约一致 28/30（扣 D1）／证据真实可复现 23/25（扣 D2＋变异未独立复跑）／新旧对照 18/20（同前）／工程红线 15/15／文档同步 9/10（扣 D2）。无 S1、≥85 → PASS。

## 未做项与下一手缺什么

1. 未独立复跑变异红/还原一致（需改实施件，审查者禁碰）；缺：有改码权的人重跑 `receipt.ts eyebrow` 改坏→单条红→改回 13 绿，或采信本报告 #6＋实施者日志。
2. 未重跑全量回归（树脏，他人工种在途；且 `dist/triggers/scene-10-analysis.js` 现被他人写坏 SyntaxError）；缺：待树绿＋重建 dist 后重跑 #4 五件＋`pnpm build`＋#7 探针。
3. D1 数值断言、D3 转票（multi-trend 登记／water.log 反面归属）留给下一手指派。
