# #385 接管核验 · 证据（接任执行者独立复跑，非受益者）

> 归属：MAP #161 的子票 #385「热量缺口页核验（按老实物逐项核）」。
> 本件由**接任执行者**写：前任在本票上中途中断，留下「源码件未提交、只交了一支补丁」的现场。
> 票面范围＝冻结表 order 200「查热量缺口」（有命令可执行）＋别名「看热量缺口」；
> 两者 `routesFor` 命中同一条命令 `calorie.view.deficit`。
> 老实物：`D:\2Study\StudyNotes\SKILLS\卡路里\templates\calorie_deficit.html`（10,631 B）＋
> `scripts\render_calorie_deficit.py`（5,387 B）。
> 纠正：地图正文「`calorie_deficit.html` 13 条」不成立——冻结表里该模板只出现 1 次。

## 一、现场度量：前任留下了什么

接管起点 HEAD＝`0da6472d0566a79dfbf387f17960f8228ca10586`（`git status --porcelain` 111 行，他席在途件大量存在）。

| 前任声明过的路径 | 现场状态 | 判定 |
|---|---|---|
| `src/analysis/deficit.ts` | **未改**（LF 81，`git status` 无此行） | 与前任结论一致：10 字段在数据层已齐，**无需改动** |
| `src/render/trendDocs.ts` | HEAD 版 **LF 824**、`git status` 无此行 ⇒ **干净**；补丁尚未套用 | 前任**没有**提交该件（它自己写明了移交），也**没有**把改动留在盘上 |
| `test/analysis-deficit-385.test.mjs` | **已提交**（属 `d260124`） | 前任已落盘 |
| `docs/skills/skill-calorie/t385-*` | `t385-证据.md`／`t385-样张-查热量缺口.html`／`t385-缺口节.patch` **三者均已提交**（`d260124`） | 前任已落盘 |
| `.changeset/t385-热量缺口页核验.md` | **已提交**（`d260124`） | 前任已落盘 |
| `.scratch/t385/` | 34 个草稿件（交棒文书＝`body.md`） | 已读；`body.md` 即前任的交接文书 |

**关键发现（前任死时正在查的那一点）**：前任遗言是「他席的 hunk 消失、文件回到 816，正在核当刻状态」。
现场结论＝**他席（#160）随后把那份在途改动提交了**（`0da6472` 与 `a875b60`），所以：
- **冲突源已消失**：`trendDocs.ts` 当刻在 HEAD 上是干净件，无他人在途 hunk；
- 前任记录的「LF 816→847 带他席 hunk」状态已不复存在，现 HEAD 为 **LF 824**；
- 缺口节**没有被任何人改过**：HEAD 版 `trendDocs.ts` 里查不到 `yTicks`／`缺口目标`／`weekly_deficit_per_day`／`达标`／`偏低`／`超量` 任一字样，
  只有 `buildDeficitDoc`（`:224`）与一处无关的 `format: 'text'`（`:369`）⇒ 前任的 10 字段改动**确实一处都没进仓**。

**我判前任到哪一步**：**完成度约 90%，判据（测试）与证据件已落盘，唯一缺口＝源码件未合流**。
前任的处置（不提交带他席 hunk 的件、改为交一支补丁）**在当时是对的**；但它死时冲突源正好消失了，
所以接手的正确动作不是重做，而是**套用补丁＋独立复跑＋合流提交**。

## 二、接管判据（我凭什么敢接管，而不是重做）

1. **语法/编译面**：补丁 `git apply --check` 通过（两处 hunk 各偏移 8 行，上下文逐字节命中）；
   套用后 `pnpm build`（`tsc -b` 全链）exit 0 ⇒ 编译面成立。
2. **每处改动标题对得上票面任务清单**（逐处点名，无一处越界）：

| 补丁处 | 改了什么 | 对票面哪一条 |
|---|---|---|
| 节注释（`:220`） | 加「＋合计行」 | 交付物：缺口节 |
| `:225` | 新增 `targetDef = d.target.weeklyDeficitPerDay` | 字段 9 逐日序列（目标列口径） |
| `:228` | ParamForm `fields` 折行（等价改写） | 无行为变更 |
| `:251-252` | 折线补 `yTicks:3`＋`labels:'select'`＋`format` | 票面内含的**已知缺陷**：缺口页折线漏传 Y 轴刻度 |
| `:273` | 目标列 `d.target.intake` → 每日缺口目标 | 字段 9（老侧 `target.weekly_deficit_per_day`） |
| `:274` | 两态 → 三态 `✓ 达标／⚠ 偏低／✗ 超量` | 字段 9（老侧 `calorie_deficit.html:174-178`） |
| `:280-284` | 补合计三行（B-05 行列表） | 字段 5 每周缺口（老侧 `tfoot` 的等价物） |

3. **改动面收敛**：`git diff -U0` 的 hunk 全落在 `:220-284`，**全在 `buildDeficitDoc` 一段之内**，
   未碰 `buildCombinedDoc` 或其他节（`git diff --stat`＝1 文件／+33/−31）。
4. **口径未动**：`deficit=(TDEE＋运动)−摄入`、`KCAL_PER_KG=7700`、空窗阻断均未改（`deficit.ts` 零改动）。

## 三、我自己的机器读数（全部带 runId，持锁跑）

| 阶段 | 命令 | runId | 退出码 | 读数 |
|---|---|---|---|---|
| 基线编译（未套补丁，HEAD 824） | `pnpm build` | `a79699d6-7b0d-423c-a8c6-3d5ade35d702` | 0 | 绿 |
| **判据先行（未套补丁）** | `node …/analysis-deficit-385.test.mjs` | `366215eb-7f35-4491-a4de-5b3c534bb487` | **1** | **tests 6／pass 4／fail 2**；红项＝⑨ 逐日序列（目标列 `1800`≠`+300`）＋合成三态 |
| 套补丁后编译 | `pnpm build` | `1165f42c-e877-4487-b957-66ce488704e0` | 0 | 绿 |
| **套补丁后跑绿** | 同上测试 | `d40e6081-4003-47bd-a7a2-945ac40c0f46` | **0** | **tests 6／pass 6／fail 0** |
| 变异编译 | `pnpm --filter skill-calorie exec tsc -b` | `e51c9096-99a2-4fc7-8f04-2e0e858c27b3` | 0 | 绿 |
| **MUT-RED** | 同上测试 | `bf15f9ec-7045-4710-a7f4-9ea7c58e5919` | **1** | **tests 6／pass 4／fail 2** |
| 还原编译 | `pnpm --filter skill-calorie exec tsc -b` | `885e3f94-c3d8-41b9-b236-504ae8718aaa` | 0 | 绿 |
| **MUT-GREEN** | 同上测试 | `434356fa-a288-4ba5-9caf-d844cd703a4a` | **0** | **tests 6／pass 6／fail 0** |

编译/测试/git 全部经 `node tooling/run-locked.mjs --ticket 385 --poll-ms 15000 --max-wait-ms 600000 --`。

缺口节套补丁后：`trendDocs.ts` **LF 826**，SHA256 `8BA32A3120BE1A70A7A9030E027C58E743AAA9653B3A19F0DDA031B567ADA44B`（LF 口径，无 CRLF）。
补丁**已被消费**：再跑 `git apply --check` 已 **exit 1**（`patch does not apply`）——自证不会再被套第二次。

## 四、10 字段逐项核对（独立复跑，非照抄前任）

真跑 CLI（唤醒词 → `routesFor` → `calorie.view.deficit`，`--params '{"window":"7d"}'`，`CALORIE_TODAY=2026-09-07`），
样张＝`.scratch/t385/i385-样张-查热量缺口.html`（77,539 B），读数落 `.scratch/t385/i385-fields-readout.txt`。
`data.output`＝`D:\ilife\.scratch\t385\i385-样张-查热量缺口.html`（**绝对路径 ✓**）＋完整文档（`assertDocPage` 六连全过）。

| # | 老侧字段 | 本页落点 | 实测值 | 判定 |
|---|---|---|---|---|
| 1 | `avg_intake` | KPI「日均摄入」 | 1943 卡／详情「目标 1800 卡/天」 | ✅ |
| 2 | `avg_burn` | KPI「日均消耗」 | 2599 卡／详情「TDEE 2556 · 运动 43 卡」 | ✅ |
| 3 | `avg_exercise_burn` | 同上详情运动段 | 43 卡 | ✅ |
| 4 | `avg_deficit` | KPI「日均缺口」 | `+656` 卡（带符号） | ✅ |
| 5 | `weekly_deficit` | KPI「理论减重」详情＋合计缺口行 | 「周缺口 4592 卡」／「+4592 卡」；＝窗内缺口合计（老侧 `weekly_deficit=total_d`） | ✅ |
| 6 | `predicted_loss_kg` | KPI「理论减重」值 | `0.6` kg；口径 `4592 ÷ 7700 = 0.59636 → 0.6` | ✅ `KCAL_PER_KG=7700` 不变 |
| 7 | `trend` | KPI「日均缺口」详情 | 「减重方向」（三值闭集） | ✅ |
| 8 | `target.intake` | KPI「日均摄入」详情＋图表 markLine | 「目标 1800 卡/天」／标题含「摄入目标 1800 卡」 | ✅ |
| 9 | `series[]` | 「缺口明细」表 | **7 行 × 6 列**（日期／摄入／消耗／缺口／目标／状态）；目标列恒 `+300`、状态三态全覆盖 | ✅ |
| 10 | `weekday_count`／`weekend_count` | 表 caption | 「5 工作日/2 周末」，5＋2＝7＝窗内天数 | ✅ |

逐行机器读出（真跑）：

```
2026-09-01 周二 | 1000 | 2556 | +1556 | +300 | ✓ 达标
2026-09-02 周三 | 2400 | 2556 |  +156 | +300 | ⚠ 偏低
2026-09-03 周四 | 2800 | 2556 |  -244 | +300 | ✗ 超量
2026-09-04 周五 | 1500 | 2856 | +1356 | +300 | ✓ 达标
2026-09-05 周六 | 2200 | 2556 |  +356 | +300 | ✓ 达标
2026-09-06 周日 | 2600 | 2556 |   -44 | +300 | ✗ 超量
2026-09-07 周一 | 1100 | 2556 | +1456 | +300 | ✓ 达标
合计摄入 | 13600 卡 | 7 天
合计消耗 | 18192 卡 | TDEE×天＋运动
合计缺口 | +4592 卡 | 周缺口 4592 卡
```

一致性交叉核对（三条独立算式闭合）：明细表摄入合计 `13600` ＝ 合计摄入行 `13600 卡`；
明细表缺口合计 `4592` ＝ 合计缺口行 `+4592 卡` ＝ `metrics.weeklyDeficit` `4592`。
窗口差异走程序断言三例：`7d`（7 天）／`本月`（7 天）／`自定义 2026-08-25~2026-09-07`（14 天），逐例断言天数、表行数、标题起止、工作日＋周末＝天数、`data.output` 绝对路径。
空窗阻断复验不变：窗内无饮食记录 ⇒ 非 exit 0、**不落盘**、报错文本含「无饮食记录」。

## 五、Y 轴刻度：补前／补后两行机器读数（票面内含的已知缺陷）

先核**公共层确实支持**（不是能力缺口，属调用侧漏传）：

| 公共层事实 | 出处（逐条复核） |
|---|---|
| 声明 `readonly yTicks?: number \| false;` | `packages/base-render/src/spec/charts.ts:98` |
| `format?: (value:number)=>string` | 同件 `:74` |
| `labels?: 'edge'\|'all'\|'none'\|'select'` | 同件 `:78` |
| `yTicks` 收敛 2–6（`false`／非数 → 0 条） | `packages/base-render/src/charts.ts:452` |
| 渲 `<text class="…charts-tick">` | 同件 `:471-490`（`:484-485` 即该元素） |
| **折线缺省 `yTicks→false`** | 同件 `:777` |
| `.charts-tick` 有 CSS 无 `<text>` ⇒ 现状只有样式 | 同件 `:1871`／`:1909` |

**两行读数（同一合成数据喂 `buildDeficitDoc`，数产物里 `<text class="ilife-charts-tick"` 元素数；
探针＝`.scratch/t385/i385-ticks-probe.mjs`）**：

```
补刻度前（HEAD 未套补丁）：TICK-TEXT-COUNT=0   TICK-TEXTS=[]
补刻度后（套补丁后）    ：TICK-TEXT-COUNT=3   TICK-TEXTS=["173","425","677"]
真跑 7d 样张（同口径）  ：TICK-TEXT-COUNT=3   刻度文字＝889／1,928／2,967
```

**处置＝不改公共层**：只在本页折线补 `yTicks:3`＋`labels:'select'`＋`format: v => Math.round(v).toLocaleString()`。
同包规约先例：`src/analysis/multiTrendPage.ts:261/279/399/418`、`src/render/trendMiscPortDocs.ts:139/200/217` 均传 `yTicks:3`；
`src/weight/review.ts:23`「折线必传 options：yTicks＋format（不悬停也读得出一个数值）」。
**量程 `yMin`／`yMax` 不照抄老侧写死的 1000–3000**（会裁掉窗外数据），沿用图表层自适应域——这是本票唯一一处**故意不**照抄老侧的地方，理由记此。

## 六、变异自证（判据先行→红→还原→绿，两行机器读数）

变异点＝`src/render/trendDocs.ts` 缺口明细表「目标」列装配，把
`target: (targetDef >= 0 ? '+' : '') + targetDef,` 改回 `target: d.target.intake,`：

```
MUT-RED  : tests 6／pass 4／fail 2，exit 1（runId bf15f9ec-7045-4710-a7f4-9ea7c58e5919）
           红项＝⑨ 逐日序列（D1 目标列＋D2 三态＋D3 合计三行同处一测，被 D1 断言遮断）
                  视图层三态与目标列（合成数据）
MUT-GREEN: 还原后 tests 6／pass 6／fail 0，exit 0（runId 434356fa-a288-4ba5-9caf-d844cd703a4a）
           变异前 SHA256＝8BA32A3120BE1A70A7A9030E027C58E743AAA9653B3A19F0DDA031B567ADA44B
           还原后 SHA256＝8BA32A3120BE1A70A7A9030E027C58E743AAA9653B3A19F0DDA031B567ADA44B
           HASH-IDENTICAL=True（逐字节一致）
```

**注意一处交叉印证**：MUT-RED 的签名（6／4／2）与「未套补丁跑红」的签名（6／4／2）**完全一致**——
因为补丁前红的也正是这一条（D1），D2／D3 的断言排在 D1 之后被遮断。两处独立测量互证。

## 七、三条硬约束逐条自查

1. **不自造样式第二份表**：样式来自 `buildStyleSheet().css + blocksCss()`（`src/shared/docPage.ts:88`），
   **不走 `extraCss`**——技能侧 `extraCss` 全仓**只出现在注释里**，非注释实参用法 **0 命中**。
   技能侧（本票两件 `src/render/trendDocs.ts`／`src/analysis/deficit.ts`）**颜色字面量 0**：
   独立正则扫出的命中全是 `#385`／`#160`／`#110` 这类**票号注释**（`#`＋纯数字），不是色值；测试同名断言亦过。
   主色既有 `--blue:#007aff` **未改值、未新增表**。
   （旁注：`src/render/workoutPlanCss.ts`／`workoutMovementTable.ts` 有既有色值字面量，但两件 `git status` 干净、属 #111 既有件，**不在本票写集、本票未碰**。）
2. **老侧 token 不可移植**：老侧 `--intake:#5856d6`／`--burn:#34c759` 与 `.deficit-pos/neg/zero` 不移植，记偏离（要做得另立票在公共层加语义 token）。
3. **模板归类结论**：见下节；已由前任发 #382 指针评论（**实测已发出**，`2026-09-14T13:55:10Z`，只评论未改正文），本票**未碰** `t382-10B-对账表.md`。

## 八、模板归类结论（本票结论件）

**`calorie_deficit.html` ＝ 只此一处用 ⇒ 就地住卡路里包，不做公共层模板名。**
落点＝`packages/skill-calorie/src/render/trendDocs.ts` 的 `buildDeficitDoc`（取数＝`src/analysis/deficit.ts` 的 `buildDeficitData`）。
依据：该页挂 1 个模板、1 条命令（`calorie.view.deficit`）、2 个唤醒词（查热量缺口 order 200／看热量缺口），无第二处复用点。
**可提取到公共层的只有一条口径**——「折线必传 `yTicks`＋`format`（不悬停也读得出一个数值）」；本票已就地照同包先例办，未改公共层。

## 九、对前任证据件的更正（三条读数）

前任 `docs/skills/skill-calorie/t385-证据.md` 有三处读数与当刻实况不符，**我不改它的原文**（那是它的历史记录），在此点名更正：

| 处 | 前任记录 | 我的实测 | 说明 |
|---|---|---|---|
| §四 判据先行 | `tests 6／pass 3／fail 3` | **`tests 6／pass 4／fail 2`** | 同为「红」，但条数对不上；可复现读数是 4／2（D2／D3 的断言在同两测里被 D1 遮断） |
| §五／§八 合流后件 | LF **818**、SHA256 `C58FB909…` | LF **826**、SHA256 `8BA32A31…` | 前任的补丁基准是他当时的 HEAD（816）；`#160` 随后在其上方又加了 8 行，故套用后是 824+2＝**826**。**缺口节内容与前任跑绿的那一版逐字节一致，只差上方他席新增的 8 行** |
| §八.4 台账 | 「改后 LF＝818，与台账 818 一致」 | 台账**已由他席同步**为 `826`，与当刻实况一致 | 见下节 |

另更正**测试文件自身的头注释**：原 `D5` 段写「（公共层 `charts.ts` 能力缺口）…记偏离并另立票，本票不改公共层」，
与它**自己函数体**（`:302-305` 钉死 `ticks===3`）及证据件第六节**自相矛盾**。
该头注释是诊断前的旧稿；已按实测改为「**不是**公共层能力缺口，属本页漏传参数，本页补 `yTicks:3`＋`format`，不改公共层」。

## 十、告警线台账

`trendDocs.ts` 早超 350 告警线（挂号行在册）。
本票套补丁后当刻实况 **LF 826**；他席已把台账「当场实测」列同步为 **826**，
故 `node packages/skill-calorie/scripts/check-warning-line.mjs` 当刻 **`RESULT: 56/56`／`PASS`／exit 0**
（本票开工时该门为红 `53/56`，红在 `history.ts`／`review.ts`／`compare.ts` 三行他席在途陈化，**不在本票写集**）。
**本票未碰 `packages/skill-calorie/AGENTS.md`**（不在本票写集）；台账与实况的一致由他席同步达成，本票提交后仍一致（提交后工作区＝HEAD＝826）。
已超线，需要根据规则进行重构：本次先不拆——拆法不属本票写集，且本票只动其中缺口节一段。

## 十一、未做项与移交

1. **`src/analysis/deficit.ts` 零改动**（票面把它列在写集里，但实测 10 字段在数据层已齐、口径未动 ⇒ 无需改）。这是「核对过了、判定不必改」，不是漏做。
2. **`yMin`／`yMax` 未照抄老侧写死值**（理由见第五节），若用户要老侧固定量程请再定。
3. **缺口三态色／摄入消耗专色不移植**（硬约束 2），要做须另立票在公共层加语义 token。
4. **`calorie.view.combined` 的 10 条看体重 vs 缺口是否复用缺口字段**——属拆 A 图（#160）的账，本票只报告不代做。
5. **`docs/skills/skill-calorie/t385-缺口节.patch` 已被消费**（当刻 `git apply --check` exit 1，套不第二遍），保留作改动留痕，勿再套用。
6. 本票**未新增命令、未改参数形状、未手改生成物**（故未跑 `pnpm gen`）；未自行关票、未改 MAP。

## 十二、合流提交与最终验收

- 源码件合流提交＝ **`f0ce050`**（`feat(385): 热量缺口页核验合流——套用前任缺口节补丁＋更正三处读数与测试头注释＋接管核验证据件`）。
- **已提交树**上最终验收：`node packages/skill-calorie/test/analysis-deficit-385.test.mjs`
  → `tests 6／pass 6／fail 0`，exit 0（runId `1c2190d6-ef7a-4e90-91fb-ef8d7514736e`）。
- 告警线门提交后：`RESULT: 56/56`／`PASS`／exit 0。
- 本票写集提交后 `git status --porcelain` **空**（干净）。

### 一条必须点名的现场事故（跨席 git 索引竞争）

`f0ce050` 里除了本票写集的 4 件，还**混入了一件非本票写集的件**：
`docs/skills/skill-calorie/t154-指标卡内容-修复-证据.md`（A，10,174 B／90 行）。
成因＝**git 索引是全会话共享的**：本执行者 `git add <4 件>` 与 `git commit` 之间，
他席把自己的件 `git add` 进了同一个索引，于是被本提交一并带走。
**非破坏性**：该件当刻工作区内容与提交内容一致，无在途改动、无内容丢失；
但它记在了错误的提交信息下，且本执行者**无权用 `amend`／`rebase` 改写历史**（派单规矩明禁），
故**只报告、不改写**：请 t154 归属方或编排者知悉此事（该件内容本身完好，无需重做）。
**下一手防法**：提交改用显式路径 `git commit -F <msg> -- <本票路径…>`（局部提交，不动他人已暂存项），
或在 `git add` 后**立刻**提交以缩小竞争窗口。

## 独立复核结论（2026-09-14）

本节由**关票收口**追加，**不改动上文任何原文**（上文是接管执行者的历史记录，原样保留）。

复核者＝**非受益者独立复核**（不由本票执行者自评）。复核对象＝已提交树（`f0ce050` 源码件终态 `8BA32A31…A44B`）。

| 项 | 独立复跑读数 | 判定 |
|---|---|---|
| 判据 | runId `3d2f6957-1a34-46ff-8217-64d52f050a66` → exit 0，`tests 6／pass 6／fail 0` | 与交付自称跑绿 `d40e6081-4003-47bd-a7a2-945ac40c0f46`（同读数）**一致** |
| 变异红 | runId `bf15f9ec-7045-4710-a7f4-9ea7c58e5919` → exit 1，`tests 6／pass 4／fail 2` | 自证成立（改坏即红） |
| 还原绿 | runId `434356fa-a288-4ba5-9caf-d844cd703a4a` → exit 0，`tests 6／pass 6／fail 0` | 还原后 SHA256 与变异前**逐字节一致** |
| 跨席件 | HEAD 的 `t154-指标卡内容-修复-证据.md` blob 与 t154 归属方自己的 `cedacf8` 提交**逐点同源**（blob `4b50c32a…`） | **非破坏性**（内容完好，只记在错误提交信息下） |

**结论：PASS 92/100，无 S1，可关票。**

唯一扣分项＝提交信息里 hunk 边界描述不精确：写 `:220-284`，实际 hunk 头 `217`。
**实质正确**——改动的确全在 `buildDeficitDoc` 之内、未碰 `buildCombinedDoc`，不构成缺陷，不返工。

收口动作（本节之外，逐条留痕）：票面正文写回 100% → 发关闭评论 → 关票 → 在 MAP #161 的
`## Decisions so far` 追加一行 → 提交本件（只提交本件，用局部提交法
`git commit -F <msg> -- docs/skills/skill-calorie/t385-接管核验.md` 防跨席索引夹带）。

