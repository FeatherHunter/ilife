# #581 收尾修复 · 证据（t509 ②／④ 两条孤儿红跟改）

> 票：#581（场景02饮食·B3回执预检食品库族文案形状化）的**收尾窗**。
> 本窗只做一件事：把 `t509-清尾.test.mjs` ②／④ 两条仍按旧题名口径取页题的断言，改成与**当刻实现**
> 一致；不动任何源码。#617／#617r 两席独立复核都点名「需 #581 归属票跟改」，而 #587／#591 的写集
> 都不含该测试件 ⇒ 这条红无主，由本窗清掉。

## 一、判定

**PASS。** 改前红（②／④，`fail 2`）→ 改后绿（`fail 0`）；源码级变异自证成立（改坏必红、逐字节还原
必绿、源码净改动为零）；编译门 exit 0。断言只换取法／期望，**未放宽**（仍是逐字相等，不是「非空即过」）。

## 二、根因（当刻实现口径）

- 页题构造：`packages/skill-calorie/src/diet/receipt.ts:388` 的 `title: receipt.scene + '回执'`。
  `scene` 由 `src/diet/log.ts:69-71` 按本次写入自己的事实取（`记一餐`／`记一餐（含备注）`／`补记饮食`／
  `拍照记一餐`／`拍照补记一餐`）。
- #581 去分隔符之后，`scene` 与 `回执` 之间**不再有 ` · `**；而 t509 的 `titleOf()` 是
  「哪行以 `· 回执` 收尾就取哪行」⇒ 当刻产物里一条都扫不到，取回空串 ⇒ ②／④ 必红。
- 落地位置：`assembleDocPage({ title })` → `src/shared/docPage.ts` → `packages/base-render/src/blocks.ts`
  的 `pageShell`，页题独占 `<h1>` 这一格（只读探针实测 `H1COUNT=1`）。

## 三、改前红 → 改后绿（两行读数）

| 读数 | GATE-RUN | 退出码 | 摘要 |
|---|---|---|---|
| 改前红 | `GATE-RUN runId=t581fin-before cmd="node --test packages/skill-calorie/test/t509-清尾.test.mjs"` | 1 | `ℹ tests 5`／`ℹ pass 3`／`ℹ fail 2`；红条＝② ④ |
| 改后绿 | `GATE-RUN runId=t581fin-after cmd="node --test packages/skill-calorie/test/t509-清尾.test.mjs"` | 0 | `ℹ tests 5`／`ℹ pass 5`／`ℹ fail 0` |

改前红的两条断言原文（`.scratch/t581/t581fin-t509-before.log`）：

```
READING #509 标题 01 唤醒词「记一餐」→ 标题「」
AssertionError [ERR_ASSERTION]: 01 的标题与唤醒词对不上        ← ②
AssertionError [ERR_ASSERTION]: 原样产物该读到新标题            ← ④
```

改后绿的五条 READING（`.scratch/t581/t581fin-t509-after.log`，页题逐字）：

```
01 记一餐回执 ｜ 02 记一餐（含备注）回执 ｜ 03 补记饮食回执 ｜ 05 拍照记一餐回执 ｜ 06 拍照补记一餐回执
```

## 四、机器证据（本窗全部运行，逐条）

| GATE-RUN runId | 命令 | 退出码 | 摘要行 |
|---|---|---|---|
| `t581fin-probe` | `node .scratch/t581/t581-finish-title-probe.mjs` | 0 | 五页 `H1COUNT=1`，H1＝`记一餐回执`／`记一餐（含备注）回执`／`补记饮食回执`／`拍照记一餐回执`／`拍照补记一餐回执`；旧取法（`· 回执` 收尾）命中 `[]`；以「回执」收尾可见行＝`[页题, ✅ 操作回执, ✅ 操作回执]` |
| `t581fin-before` | `node --test packages/skill-calorie/test/t509-清尾.test.mjs` | 1 | `tests 5`／`pass 3`／`fail 2`（② ④） |
| `t581fin-after` | 同上 | 0 | `tests 5`／`pass 5`／`fail 0` |
| `t581fin-tsc` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` | 0 | 无诊断输出（写死显式入口，未用 `npx tsc`） |
| `t581fin-mut` | `node .scratch/t581/t581-fin-mut-battery.mjs` | 1 | **作废窗**（见第六节）：驱动 argv 写错 ⇒ tsc 一次没跑 ⇒ 变异后测试照旧全绿＝假绿。本席自查抓住，未采信任何读数 |
| `t581fin-mut2` | 同上（驱动已修） | 0 | `MUT-RESULT PASS：变异红=yes 还原绿=yes 还原逐字节一致=yes 源码净改动=0` |

### 逐条对账（`.scratch/locks/gate-runs.log` 导出，受版本控制的导出件＝本节）

```
START ticket=581 runId=t581fin-probe cmd="node .scratch/t581/t581-finish-title-probe.mjs" waitedMs=50005 pid=63292 at=2026-09-16T12:17:31.617Z
RUN ticket=581 runId=t581fin-probe cmd="node .scratch/t581/t581-finish-title-probe.mjs" waitedMs=50005 exit=0 pid=63292 at=2026-09-16T12:17:37.145Z
START ticket=581 runId=t581fin-before cmd="node --test packages/skill-calorie/test/t509-清尾.test.mjs" waitedMs=0 pid=50856 at=2026-09-16T12:17:41.720Z
RUN ticket=581 runId=t581fin-before cmd="node --test packages/skill-calorie/test/t509-清尾.test.mjs" waitedMs=0 exit=1 pid=50856 at=2026-09-16T12:17:50.565Z
START ticket=581 runId=t581fin-after cmd="node --test packages/skill-calorie/test/t509-清尾.test.mjs" waitedMs=0 pid=53292 at=2026-09-16T12:18:41.493Z
RUN ticket=581 runId=t581fin-after cmd="node --test packages/skill-calorie/test/t509-清尾.test.mjs" waitedMs=0 exit=0 pid=53292 at=2026-09-16T12:18:50.480Z
START ticket=581 runId=t581fin-tsc cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force" waitedMs=0 pid=25672 at=2026-09-16T12:18:57.863Z
RUN ticket=581 runId=t581fin-tsc cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force" waitedMs=0 exit=0 pid=25672 at=2026-09-16T12:19:01.383Z
START ticket=581 runId=t581fin-mut cmd="node .scratch/t581/t581-fin-mut-battery.mjs" waitedMs=0 pid=7204 at=2026-09-16T12:19:30.446Z
RUN ticket=581 runId=t581fin-mut cmd="node .scratch/t581/t581-fin-mut-battery.mjs" waitedMs=0 exit=1 pid=7204 at=2026-09-16T12:19:49.101Z
START ticket=581 runId=t581fin-mut2 cmd="node .scratch/t581/t581-fin-mut-battery.mjs" waitedMs=20001 pid=62212 at=2026-09-16T12:20:43.387Z
RUN ticket=581 runId=t581fin-mut2 cmd="node .scratch/t581/t581-fin-mut-battery.mjs" waitedMs=20001 exit=0 pid=62212 at=2026-09-16T12:21:08.613Z
```

上表 6 条运行与上面「机器证据」表**一对一**（含作废窗 `t581fin-mut`：它退出码非 0，不领作门禁证据，
只作过程留痕）。`t581fin-mut`／`t581fin-mut2` 两窗内的 tsc 与 t509 是**窗口内直调**（外层包装器持锁，
§2.4.5），其留痕由外层包装器与窗口日志（`.scratch/t581/t581fin-mut[-2]-window.log` ＋ 窗口内各步日志）共同承担。

## 五、改了什么（逐条对账：每条断言 ↔ 当刻实现）

只改 `packages/skill-calorie/test/t509-清尾.test.mjs`（+23 −13 行）：

| # | 位置 | 旧写法（#581 之前的口径） | 新写法（当刻实现） | 依据 |
|---|---|---|---|---|
| 1 | `titleOf()` | `text.split('\n')…find((l) => l.endsWith('· 回执')) ?? ''` | 取页框 `<h1>` 那一格：`/<h1[^>]*>([\s\S]*?)<\/h1>/` → `visibleText(...).trim()` | 页题落点＝`assembleDocPage` 的 `renderPageShell({ title })`（`src/shared/docPage.ts:144`）→ `packages/base-render/src/blocks.ts:229` 的 `<h1 class="…pageShell…title">`；**不能**退化成「以 `回执` 收尾就取」——本页 `✅ 操作回执`（区块标题＋页内导航项各一次）恒有两条命中，先命中的不是页题（探针实测） |
| 2 | `RECEIPT_CASES[01].title` | `记一餐 · 回执` | `记一餐回执` | `receipt.scene + '回执'`（`receipt.ts:388`）＋ `log.ts:71` |
| 3 | `RECEIPT_CASES[02].title` | `记一餐（含备注） · 回执` | `记一餐（含备注）回执` | 同上（`log.ts:71` 的 `hasNote` 支） |
| 4 | `RECEIPT_CASES[03].title` | `补记饮食 · 回执` | `补记饮食回执` | 同上（`log.ts:69-70` 的 `backfill` 支） |
| 5 | `RECEIPT_CASES[05].title` | `拍照记一餐 · 回执` | `拍照记一餐回执` | 同上（`log.ts:69-71` 的 `photo` 支） |
| 6 | `RECEIPT_CASES[06].title` | `拍照补记一餐 · 回执` | `拍照补记一餐回执` | 同上（`backfill`＋`photo` 支） |
| 7 | ② 取数 | `titleOf(r.text)` | `titleOf(r.html)` | `titleOf` 换成认 `<h1>`，须喂整页 HTML（可见文本已被剥掉标签） |
| 8 | ④ 取数 | `titleOf(badReceipt.text)`／`titleOf(receipt.text)` | `titleOf(badReceipt.html)`／`titleOf(receipt.html)` | 同上 |
| 9 | ④ 期望 | `assert.equal(goodFirst, '拍照记一餐 · 回执')` | `assert.equal(goodFirst, '拍照记一餐回执')` | 同 #5 |
| 10 | 件头 ② 说明 | （无） | 补一行「页题逐字＝`receipt.scene + '回执'`（#581 去分隔符后不再以 ` · 回执` 收尾）」 | 让后来人先读到口径再读断言 |

**口径未被放宽**：② 仍是 `assert.equal(first, c.title)` 逐字相等（外加 `must()` 那条「看得出唤醒词」），
④ 仍是「改坏后读数必须变」；`titleOf` 无模糊兜底，取不到 `<h1>` 即返回空串（空串必不等于期望值 ⇒ 仍红）。

### 已复核、未改（逐条点名，免得下一手再来扫一遍）

- `PREVIEW_GONE`／`PREVIEW_WANT`／单独成行的 `匹配`／`—`／`kpiRegion` 不含 `未匹配`／机器话四类：① 两条
  改前改后都绿，当刻实现里逐条对得上（`trendMiscPortDocs.ts` 的 #509 在场段与 `precheck.ts`）。
- ③ 的四条：`来源：营养表照片` 出自 `src/diet/log.ts:85` 的 `sourceText`（经 `subtitleOf` 的 ` · `→`，`
  归一后仍在屏上）；`calorie-cmd-read calorie.diet.add` 与 `source":"photo` 出自复制日志段
  （`receipt.ts:377-380` 的 `copyLog`）——改前改后都绿。
- `scrub()` 的时间戳正则**仍是活的**（不是陈旧口径）：本仓唯一时间戳口径＝`YYYY-MM-DD HH:MM:SS`
  （`src/render/receipt.ts:152`），复制载荷的第 5 段仍按秒级印（`receipt.ts:384` 注释点名「复制载荷机器
  时间保持秒级精确」）；故 ② 的「两两不再逐字节相同」比较的是抹平写库时刻之后的产物，不是永真式。

## 六、变异自证（源码级，两行机器读数）

窗口：`GATE-RUN runId=t581fin-mut2 cmd="node .scratch/t581/t581-fin-mut-battery.mjs"`
（外层包装器持锁，窗口内直调 tsc／t509；驱动 `.scratch/t581/t581-fin-mut-battery.mjs`）。

变异点：`src/diet/receipt.ts` 的 `title:` 一位 `receipt.scene + '回执'` → `receipt.scene + ' · 回执'`（改回旧写法）。

```
变异红   MUT-RED   exit=1 tests=5 pass=3 fail=2 断言=["#509 ② 五页回执标题按唤醒词对上 ＋ 两两不再逐字节相同",
                                                    "#509 ④ 变异自证：新句改回旧写法 / 标题改回旧写法，同一段断言必红"]
还原一致 MUT-BACK  sha256_before=4842777e75c1109e25921292824522966b2fd57eede67ac203ab0d34f045b7f9
                   sha256_after =4842777e75c1109e25921292824522966b2fd57eede67ac203ab0d34f045b7f9
                   逐字节一致=yes；MUT-GREEN exit=0 tests=5 pass=5 fail=0 断言=[]
```

红掉的正是本窗改过的 ②／④ 两条（证明它们**不是永真**），①③ 仍绿。
（驱动原样打印的 `断言=` 里同一条列了两次——TAP 树与失败明细各一次，上表已去重，未改任何读数。）

**回滚连产物一起复位**（§5 的两步连做，逐条读数）：

```
MUT-STEP 2 重编（变异版）exit=0 dist 带上变异写法=yes     ← 变异真的进了跑的代码（测试从 dist/ 导入）
MUT-STEP 4 重编（还原版）exit=0 dist 已回到原写法=yes     ← 还原后重编，不留陈旧产物
MUT-STEP 5 git diff（该件）=（空，净改动为零）
```

`receipt.ts` 源码 sha256 前后同值（`4842…b7f9`，26044 字节），`git diff` 该件为空 ⇒ **净改动为零**。

### 作废窗自陈（`t581fin-mut`，exit=1）

第一次变异窗的驱动把 `node` 前缀也塞进了 `spawn(process.execPath)` 的 argv ⇒ 子进程报
`MODULE_NOT_FOUND`、tsc 一次没跑，`dist/` 留旧产物，测试于是照旧全绿（`MUT-RED exit=0 pass=5`）＝**假绿**。
本席在窗口内自查发现（还原已生效、sha256 一致、git diff 为空），判本窗作废，**未采信其任何读数**，
驱动改为「编译退出码为 0 **且**产物里真带上变异写法」双前置后才重跑（`t581fin-mut2`）。这条留在证据里，
因为「编译没真跑 ⇒ 测试绿」正是本仓点名过的假绿陷阱。

## 七、逐条路径

- 改动件：`packages/skill-calorie/test/t509-清尾.test.mjs`（唯一入仓改动，+23 −13）
- 证据件：`docs/skills/skill-calorie/t581-收尾-证据.md`（本件）
- 日志件（未跟踪，`.scratch/` 在忽略清单内）：
  - `.scratch/t581/t581fin-probe.log`（题名探针）
  - `.scratch/t581/t581fin-t509-before.log`（改前红尾 5 行）
  - `.scratch/t581/t581fin-t509-after.log`（改后绿尾 5 行）
  - `.scratch/t581/t581fin-tsc.log`（编译门）
  - `.scratch/t581/t581fin-mut-window.log`／`t581fin-mut2-window.log`（两个变异窗的整窗留痕）
  - `.scratch/t581/t581fin-mut-{red,restore}-tsc.log`／`t581fin-mut-{red,restore}-t509.log`（窗口内各步）
- 可复跑脚本：`.scratch/t581/t581-finish-title-probe.mjs`（只读探针）、`.scratch/t581/t581-fin-mut-battery.mjs`（变异电池）
- 未动（**净改动为零**）：`packages/skill-calorie/src/**`（含 `src/diet/receipt.ts`）、他席文件、其它测试

## 八、未做项与下一手

1. **本窗不跑全量门禁**（票面只要求 t509 靶向 ＋ 该包编译门）：`t279`／`t270`／`t277`／`t274`／`t275`
   与 `audit-separators` 门禁的当刻状态未经本窗复核，回执只报本窗读到的。
2. **页题口径若再变**（例如有人把 `scene + '回执'` 改回带分隔符），`t509` ②／④ 会再红：
   那属于**新票的口径问题**，跟改点就是本文件第五节 #1／#2-#6 那两处，不要放宽成「非空即过」。
3. `t496-文案统一.test.mjs:44/49/54` 已按同一口径（`补记饮食回执`／`记一餐（含备注）回执`）跟改过
   （见提交 `18a62c4f`），本窗未动它、也未复跑它。
4. 本窗**不推送**（推送由编排者统一收口）。
