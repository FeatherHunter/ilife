# t422 融合共用件（卡路里场景 04 运动 · 技能层）

- 票：`gh #422`（地图 #156 的子票；消费 #420／#421 已落地的公共层共用件）。
- 编排者前置要求（#422 首条评论）：先查 `src/weight/`／`src/photo/` 有没有第二份「结论块／徽章／空态」，
  并给三个概念各一条命中面读数 → 见 **§四**。
- **判定：交付完成**——六件全部落地、判据 9/9 绿、变异两行红→绿、三条门禁绿。
  两处**未落**且已记账：①两条页面级类名的样式不在本票（公共层，另开 `#434`）；②别域（weight／photo）
  的同形件合并不在本票写路径内（§四、§八）。
- 判据运行面：Windows ＋ node v24.19.0；凡会改工作区的命令一律走
  `node tooling/run-locked.mjs --ticket 422 -- <命令>`，退出码取进程退出码。

## 一、交付物（逐条路径）

| # | 件 | 路径（新增） | 对外给什么（数得出） | 分工 |
|---|---|---|---|---|
| 1 | 回执四态头 | `packages/skill-calorie/src/shared/operationHead.ts`（92 LF） | `OPERATION_LABELS`／`OPERATION_TONES`／`OPERATION_ICONS`＋`operationHead(...)`＝4 | 三张表唯一定义地；产操作卡头部（图标＋状标签徽章＋标题＋记录号＋时刻行） |
| 2 | 字段标签查表口径 | `packages/skill-calorie/src/shared/fieldLabel.ts`（43 LF） | `fieldLabel(domain,key)`／`registerFieldLabels(domain,table)`＝2 | 只给口径：缺项回退原键名、同域只许登记一次；**表本身不住共用位** |
| 3 | 来源脚注 | `packages/skill-calorie/src/shared/sourceLine.ts`（45 LF） | `sourceLine({source,start,end,count})`＝1 | 读页与回执页共用；版面走公共层 `renderCaliberLine` |
| 4 | 一句话结论条 | `packages/skill-calorie/src/shared/conclusionLine.ts`（21 LF） | `conclusionLine(text)`＝1 | 只定形状与类名 `ilife-block-conclusion`；文案全由调用方给 |
| 5 | 空态指引 | `packages/skill-calorie/src/shared/emptyGuide.ts`（39 LF） | `emptyGuide({icon,text,hint})`＝1 | 三件套必填；结构仍走公共层 `renderEmptyBlock`（不复制结构） |
| 6 | 运动类别色表 | `packages/skill-calorie/src/exercise/categoryColors.ts`（32 LF） | `EXERCISE_CATEGORY_COLORS`／`categoryColor(cat)`＝2 | 四类色（strength／cardio／flex／daily）本仓唯一定义地 |

判据：`packages/skill-calorie/test/fusion-shared-422.test.mjs`（247 LF，9 条）。证据件：本文件。

**运动域字段标签表**（票面第 2 件的前半）：当刻唯一落点是 `src/exercise/receipt.ts:42-59`
（16 键的 `FIELD_LABELS`）＋ `:61` 的回退 `FIELD_LABELS[col] ?? col`——与本票口径逐字同形。
**本票不搬**它：①票面「遗留出口」——共用位从第二个用法里长出来，当刻只有一处用法（回执页族），
搬它要动回执页族实施票的件；②本票写路径在 `src/exercise/` 只许**加**数据件。
第二个用法出现时（读页族实施票）把表搬到 `src/exercise/fieldLabels.ts` 并按 §一-2 的
`registerFieldLabels('exercise', 表)` 登记即可。

## 二、判据先行：先红后绿（machine 读数）

| 序 | 命令（均在 `--ticket 422` 锁内） | runId | exit | 读数 |
|---|---|---|---|---|
| 1 | `node --test packages/skill-calorie/test/fusion-shared-422.test.mjs`（六件不存在时） | `dbefd4e4-cb0e-4c07-9593-ef95e9e3fb09` | **1** | `ERR_MODULE_NOT_FOUND …/dist/shared/operationHead.js`；tests 1／fail 1 → **红** |
| 2 | `pnpm build` | `d7bc4120-c3f6-4d24-ae87-87d69a0741f5` | 0 | 六件编译通过（tsc strict，无 `error TS`） |
| 3 | 同 1（检据器两处保真问题回落） | `eec38216-dff4-485c-8d40-24442be3f699` | 1 | 7 pass／2 fail（扫描面误收 test；空态探测器写错）→ 当场修判据 |
| 4 | 同 1 | `8346309f-fc16-4572-a34c-7341969b0648` | 1 | 8 pass／1 fail（空态构件正则漏 `renderEmptyState:` 的箭头写法）→ 修 |
| 5 | 同 1（首次全绿） | `0e3686f7-2715-4a01-86c0-d770d8d51ac2` | **0** | tests 9／pass 9／fail 0 |

## 三、单源读数（命中文件数）

判据内的扫描面＝**全仓源码树** `packages/<件>/src/**`（不含 `test`／`dist`／`node_modules`；
`docs/` 侧的老盘点与两张样张是记录件、票面明令不碰，另在下方单列）。

绿跑（runId `0e3686f7`）逐行 stdout：

```
RESULT 单源-类别色-表形绑定命中文件数=1 files=skill-calorie/src/exercise/categoryColors.ts
RESULT 单源-类别色-四值同现文件数=1 files=skill-calorie/src/exercise/categoryColors.ts
RESULT 读数-四类色-裸值命中文件数（含公共层 token／图表缺省色／禁色表）=11
   base-render/src/charts.ts, base-render/src/helpShell.ts, base-render/src/spec/charts.ts,
   base-render/src/spec/index.ts, base-render/src/spec/style.ts, base-render/src/style.ts,
   skill-calorie/src/analysis/multiTrendPage.ts, skill-calorie/src/exercise/categoryColors.ts,
   skill-calorie/src/photo/helpCenter.ts, skill-calorie/src/photo/receipt.ts, skill-calorie/src/shared/docPage.ts
RESULT 单源-四态标签表-键值绑定命中文件数=1 files=skill-calorie/src/shared/operationHead.ts
RESULT 单源-四态图标表-键值绑定命中文件数=1 files=skill-calorie/src/shared/operationHead.ts
RESULT 单源-运动域字段标签表-命中文件数=1 files=skill-calorie/src/exercise/receipt.ts
RESULT 概念-结论条-结论helper定义文件数=8
   shared/conclusionLine.ts, weight/compare.ts, weight/history.ts, weight/log.ts,
   weight/logReceipt.ts, weight/plateDocs.ts, weight/receipt.ts, weight/volatilityDoc.ts
RESULT 概念-结论条-conclusionLine定义地数=1 files=skill-calorie/src/shared/conclusionLine.ts
RESULT 概念-徽章-技能层本地badge定义文件数=5
   photo/helpCenter.ts, photo/receipt.ts, render/html.ts, shared/docPage.ts, weight/history.ts
RESULT 概念-徽章-技能层回执徽章文案绑定文件数=1 files=skill-calorie/src/photo/receipt.ts
RESULT 概念-空态-技能层自造空态HTML结构文件数=0
RESULT 读数-空态-技能层空态同名符号文件数=5
   body/bodyDocs.ts, body/bodyPlate.ts, shared/emptyGuide.ts, weight/log.ts, weight/weightCompare.ts
RESULT 概念-空态-技能层调公共层空态的件数=11
RESULT 概念-空态-公共层构件定义文件数=2 files=base-render/src/blocks.ts, base-render/src/controls.ts
```

**「全仓」四个字要说清**：票面/编排者那句「全仓检索该四类色的色值字面量」若按**裸字面量**扫，
全仓会命中 60 件——因为四值里三支同时是公共层冻结 token（`--ok:#34c759`）、图表缺省色
（`charts.ts` 的 `#ff9500`）与 H-01 禁色表引用（`#0071e3`），它们**不是类别色取值**。
所以单源判据锚在**色表形状**（键→值绑定、四值同现），裸值只作读数用。
含 `docs/`＋`fixtures/`＋`test/` 的全仓读数（`.scratch/t422/scan.mjs`，只读）：
表形绑定 2 件（色表本件 ＋ 本票判据文件自身）、四值同现 9 件（色表本件 ＋ 判据文件 ＋ 7 件 docs
记录件：两张 `t156-样张-*.html`、`t156-老HTML盘点-读页.md`、`t156-老HTML盘点-写与汇总.md`、
`t156-老HTML索引-写与汇总.json`、`t161-老技能HTML清单*.md`）、四态标签表绑定 2 件
（`shared/operationHead.ts` ＋ 样张 `docs/skills/skill-calorie/t156-样张-写后回执.html:154` 的
`opLabels`；样张写的是 `update:'已更新'`／`delete:'已删除'`，与老表逐字不同源）。这 7 件 docs
记录件**票面「不许动」**，故它们不进判据断言，仅作残留登记（老技能目录 `D:\2Study\…\卡路里`
的四类别 CSS 变量在 `templates/exercise_summary.html:14`，仓外只读件）。

## 四、第二份定义检索结论（编排者前置要求）

1. **结论条** —— 有别域实现，但**形状不同**：weight 域 8 件里有 7 件各揣一份「结论」渲染
   （`weight/plateDocs.ts:44` 的 `conclusionBlock`、`weight/compare.ts:445` 同名件、
   `history.ts:549`／`review.ts` 直接内联），形状是 `renderDisclosure({title:'结论', open:true})`；
   本票的「一句话结论条」是页面级 `<p class="ilife-block-conclusion">`（老实物 `one_line` 那块、
   样张 `t156-样张-运动复盘.html:102` 的 `#sec-oneline`）。两者是**条 vs 块**两种形状。
   合并代价：先要有人裁定留哪一种（属 weight 域/设计席），再把一边改掉；改 weight 侧＝4 处替换、
   改本件＝把条换成折叠区（1 行）。**本票不改别域行**；建议编排者开一张「结论：条／块择一」的收口票。
2. **徽章** —— **同形件确有**：`packages/skill-calorie/src/photo/receipt.ts:43-47` 的 `opBadgeOf(op)`
   与 `:50` 的 `badgeBlock`，逐 op 返回 `{badge,title,icon,verb,done,color}`，色值是字面量
   （`#ff3b30`／`#ff9500`／`#34c759`），文案是照片域措辞（存入回执／变更回执／删除回执）。
   形状差异（对账逐条）：①它是 if-链不是三张表，且只三态（`create/update/delete`，无 `add`）；
   ②返回值带 `verb/done` 两段域词、色值落在域内（本票色档只落公共层档名，不含色值）；
   ③它只出行 `<div>徽章：…</div>`，不是操作卡头部（无记录号／时刻行）。
   合并代价：photo 侧改调 `operationHead` 要（a）把 `badge/title/verb/done` 抬成调用方参数——
   6 条写命令的 scene 文案分散在 `photo.ts`／`manage.ts`；（b）`badgeBlock` 的两行结构改成头部。
   约 40~60 行 diff，风险面＝`photo-receipt-docs-282.test.mjs` 的形状断言与 `t282-回执同形.md` 的对照表。
   **按编排者要求停手不单边改**：本票只交付共用件，photo 域并入与否请编排者裁定（并票或另开收口票）。
3. **空态** —— **无第二份构件**：技能层自造空态 HTML 结构＝0 件；结构唯一定义地在公共层
   （`base-render/src/controls.ts` 的 `renderEmptyState` ＋ `blocks.ts` 的 `renderEmptyBlock`，
   同一层两件：控件产出器＋区块包装）；技能层 11 件只是**调用**，本票新增的 `emptyGuide` 是其中
   第 11 件（调用面封装，不含结构、不含默认文案）。weight 域 4 处 `empty*` 同名符号是局部文案／
   整页空页装配（`bodyDocs.ts:172` 的 `strOrEmpty`、`plate.ts` 的 `emptyMeasureKpi`、
   `weight/log.ts:237` 的 `buildWeightEmptyDoc`、`weightCompare.ts:105` 的 `emptySeg`），不是构件。

## 五、变异自证两行（红→绿，逐行机器读数）

| 变异 | 动作 | 读数 |
|---|---|---|
| ①四态标签表 | `shared/operationHead.ts` 的 `delete: '删除'` → `delete: '删掉'` | `pnpm build` runId `0cf35af9-9f38-4677-ae72-fbb09cfde76a` exit 0；判据 runId `fad9d0a2-34a2-4538-bc0c-02a1e712bd3d` **exit 1**（pass 8／fail 1，断言 `OPERATION_LABELS.delete === '删除'`）→ **红** |
| ①还原 | 改回 `delete: '删除'` | build runId `c267ab97-20c4-40aa-b5cc-45a6970f822d` exit 0；判据 runId `9926901d-8dc1-471e-a7c7-a3b94c5b2912` **exit 0**（9/9）→ 绿 |
| ②类别色表 | `exercise/categoryColors.ts` 删掉 `flex: '#34c759',` 一行 | `pnpm build` runId `a26b4f9d-0c78-4247-8d39-c52f4c4c299d` **exit 2**（`error TS2741: Property 'flex' is missing …`——类型面先红）；判据 runId `b00963e6-a1d3-412e-a44d-aa6d37a0a4c7` **exit 1**（pass 8／fail 1，红的是「运动类别色：…四类只有这一处表」那条 deepEqual）→ **红** |
| ②还原 | 补回 `flex: '#34c759',` | build runId `0a3cf172-ec7b-44fb-a18f-73bca3173f64` exit 0；判据 runId `859b1419-c3df-4a91-a41e-6e08879afdff` **exit 0**（9/9）→ 绿 |

行数口径（LF，`ReadFileSync.split(\n).length-1`）：新件最大 92 LF（`operationHead.ts`），判据 247 LF，
**都在包内 350 行告警线内**，无超线。

## 六、门禁 GATE-RUN 对账

| 命令 | runId | exit | 读数 |
|---|---|---|---|
| `pnpm build`（终态） | `0a3cf172-ec7b-44fb-a18f-73bca3173f64` | 0 | 全仓构建通过，无 `error TS` |
| `node --test packages/skill-calorie/test/exercise-receipt-264.test.mjs` | `e905b2c3-0a09-42e3-ad6e-c86f4b979b88` | 0 | tests 13／pass 13／fail 0 |
| `node --test packages/skill-calorie/test/doc-page-assert.mjs` | `97aed28e-0652-42c3-944f-9bac9d9576a3` | 0 | tests 1／pass 1／fail 0 |
| 本票判据 `test/fusion-shared-422.test.mjs`（终态） | `859b1419-c3df-4a91-a41e-6e08879afdff` | 0 | tests 9／pass 9／fail 0 |

`pnpm build` 全程未出现过落在我写范围内的错行（唯一一次非零是变异②自己造的 `TS2741`，
已还原）。故无「他席在途件」归因争议。

## 七、结构纪律（必报五步 · 逐条）

- **第一步 影响清单**（事前）：新增 6 件源码（`src/shared/` 5 件 ＋ `src/exercise/` 1 件数据表）、
  1 件判据、1 件证据；`src/exercise/commands.ts`／`routes.ts` 与 `packages/base-render/` 一字不改。
- **第二步 结构设计**（事前）：共用位 `src/shared/` 五件＝「回执页族 ＋ 六个读页族」两族在用的形态
  （票面点名的消费方）；数据件 `src/exercise/categoryColors.ts` 住运动域自己的目录——共用位里
  不出现任何能力名（铁律一/依赖方向）。每件对外 ≤4 个名字（铁律五）。
- **第三步 写代码**：常量与口径各写一处（三张 op 表、四类色表各一处）；
  跨能力引用只走公开面（`base-paint`／`base-paint/blocks`）。
- **第四步 超线报警**：无超线件（§五末行读数）。
- **第五步 交付对账**：实际碰到的路径＝第一步那 8 条，**偏差为零**（未新增未声明的路径；
  `.scratch/t422/` 下两个草稿件与 `#434` 是开票与草稿，不进提交）。

## 八、未做项与下一手

1. **样式未落**：`ilife-block-op-head*` 与 `ilife-block-conclusion` 两条类名的规则属公共层
   `pageShell` 区（同 #420／#421 处置），本票不许改 `packages/base-render/` → 当场开票
   **`#434`**「卡路里场景04运动·公共层增量：操作卡头部／结论条两条页面级类的样式」，
   口径逐条引用 #420／#421。
2. **运动域字段标签表未搬**（§一末段）：等第二个用法（读页族实施票）出现再搬，今天的唯一落点是
   `src/exercise/receipt.ts:42-59`。
3. **别域同形件未合并**（§四-1／-2）：weight 的「结论块」7 处、photo 的 `opBadgeOf`＋`badgeBlock`
   各留原处；合并与否请编排者裁定（形状差异与合并代价已逐条写在上方）。
4. **消费方不在本票**：回执页族（13 条会改数据库的命令）、读页族（分布／趋势／复盘／汇总／
   记录级明细）实施票接这六件；接的时候 `operationHead` 的标题／来源句、`conclusionLine` 的整句、
   `emptyGuide` 的三件套都要由调用方给，共用件不给默认文案。
5. 留存读数脚本：`.scratch/t422/scan.mjs`（只读；含 docs 的全仓读数复跑用）、
   `.scratch/t422/issue-body.md`（`#434` 正文草稿）。

## 九、提交与推送对账（含一次「被并席提交卷走」的记账）

本票声明的提交路径共 8 条（§一 的六件 ＋ 判据 ＋ 本证据件），全部由自己在锁外按路径提交，
**未用** `git add -A`／`stash`／`reset`／`amend`／`rebase`。

- **事件**：19:19 我按 `git add <8 条路径>`（只加自己的件）后立即执行
  `git commit -F <msg> -- <8 条路径>`；但同一窗口里另一席先一步在 19:19:30 做了**不带 pathspec** 的提交
  ——暂存区里我那 8 条件被一并带走，落进 `b8520ed6 feat(285): SKILL.md常驻规则D2-D7＋09工作流披露（审查98分放行）`。
  我的 pathspec 提交因此报 `no changes added to commit`（exit 1），**没有**产生本票自己的提交。
- **事后核对**（逐条机器读数）：`git show --name-only b8520ed6 -- <8 条路径>` → 八条全在；
  `git diff HEAD -- <8 条路径>` → 空（工作区与 HEAD 逐字一致，入库内容＝本票终态）；
  `git rev-list --count origin/master..master` → 0（**已推送**，origin/master＝本地 master）。
- **处置**：禁改历史（`amend`／`rebase`／`reset --hard` 都在禁止清单里），故**不重写**，
  改为用本文件补一次记账提交（`feat(422): …`，pathspec 模式，只含本文件），把「本票的件由哪个提交承载」写明。
- **账目（终态）**：本票的件承载于两个提交——`b8520ed6`（六件 ＋ 判据 ＋ 本证据件）
  ＋ 本次 `feat(422)` 记账提交（本文件本节）。
- **给并席窗口的提醒**：提交一律走 pathspec（`git commit -F <msg> -- <路径>`）；否则会把别席
  已 `git add` 的件卷进自己的提交，两边的提交信息都对不上账（本次就是这一例）。

## 十、S1 复核节点（判据件入库面：两半改法 ＋ 四条自证读数）

派单方（编排者）裁定：工作区那处判据件更正**不能照原字节直接收**，两半分开处理。本节记
S1 是什么、两半各是什么、改法与四条自证读数。门禁命令一律
`node tooling/run-locked.mjs --ticket 422 -- node --test packages/skill-calorie/test/fusion-shared-422.test.mjs`。

1. **S1 是什么**（审查报告 §三-S1）：**入库状态不可复现验收**。工作区判据件与 `HEAD` 差
   **21 增 4 删**、未入库；对提交态复跑即红。红因触发点是别席在途件 `src/body/bodyDocs.ts`
   的一处 `class="…empty"`（当刻未入库，本席实测其在 `HEAD` 里不命中），不是本票六件。
2. **两半各是什么**（按 `git diff` 逐条量）：
   - 甲半（**17 增 2 删**）：新增 `MY_PATHS`（本票六件）＋ 把「技能层不许自造空态结构」由
     **全仓断言** `deepEqual(hits, [])` 收窄成**逐本票件断言**，全仓命中数只作 `RESULT` 读数。
     收窄的代价＝层内别件不再被本票判据拦住 → **配自证①**（第 5 条）。
   - 乙半（**4 增 2 删**，**真放宽**）：公共层那条由 `deepEqual(emptyDefs, 两件闭集)` 改成
     `includes` 两支，把闭集降成「至少含这两件」——第三处定义地冒出来也不会红，正是本票
     要防的「第二份定义」。**已改成闭集＋具名允许清单**（第 3 条）。
3. **判据件终态改法**（`packages/skill-calorie/test/fusion-shared-422.test.mjs`）：
   - 甲半照收：逐本票件断言（含逐件存在性）＋ 全仓命中只读数；
   - 乙半还原成**闭集**：`EMPTY_DEF_ALLOWLIST` 两行**具名登记**（`base-render/src/blocks.ts`
     ＝区块包装 `renderEmptyBlock` 唯一定义地，来源 `#104` B1 区块落盘；`base-render/src/controls.ts`
     ＝控件产出器 `renderEmptyState` 唯一定义地，来源 `#76` 公共层控件层），检索面由「公共层」
     放大到**全源码树**（`walkSources()`），落点写成**一条深比较**：多一处红、少一处红、
     跑到公共层之外（技能层／别家包）同样红。要新增定义地必须在清单里补一行并写明理由与票号。
4. **当刻门禁读数**（工作区）：`runId=ba4e83ca-3ed7-43ec-b17d-8ab9afad2d40`，**exit=0**，
   tests 9／pass 9／fail 0。其中
   `RESULT 概念-空态-全源码树构件定义地（闭集比对面）=2 files=base-render/src/blocks.ts,base-render/src/controls.ts`
   （公共层命中数同为 2）；`RESULT 读数-空态-技能层自造空态HTML结构文件数=1 files=skill-calorie/src/body/bodyDocs.ts`
   （他席在途件，只读数不断言）。
5. **自证①「收窄不是放宽」**（驱动 `.scratch/t422-s1/proof1-inject.mjs`：给六件之一
   `src/shared/emptyGuide.ts` 塞一段 `class="t422-probe-empty"` 的结构，随即写回原字节）：
   - 注入态：`runId=84e4e2cb-de96-4314-bd84-31b58bf2bab9`，**exit=1**，tests 9／pass 8／fail 1，
     断言 `skill-calorie/src/shared/emptyGuide.ts 不许自造空态结构（只许调公共层）`；
     该件 sha256 `d9fa02bd217933716ee5c7cc4fc7a98208b59ac97fc7c186a2ee4aecb8d66b87`（基线）
     → `9fca3605d7368423c21247feab8f32d56e89afdea133c2b10c2ea8feb456c014`（注入）。
   - 写回态：写回后 sha256 与基线**逐值相同**（脚本自核 `与基线同上=true`，本席另核一次同值）；
     `runId=6d6c1286-d244-4b9d-8391-4ee8ad917e99`，**exit=0**，tests 9／pass 9／fail 0。
6. **自证②「闭集」**（驱动 `.scratch/t422-s1/proof2-probe.mjs`：临时新增一处定义地，随即删除。
   探针写成 `.mjs`——`base-render` 构建是 `tsc -b`、不编译 `.mjs`，故不留 `dist` 产物）：
   - 公共层**内**新增第四处（`packages/base-render/src/t422-probe-empty.mjs`）：
     `runId=5dff3375-0ff3-476b-89ff-41915566e922`，**exit=1**，tests 9／pass 8／fail 1，
     闭集比对面由 2 件变 3 件。
   - 公共层**之外**新增（`packages/skill-calorie/src/t422-probe-empty.mjs`）：
     `runId=cc8b437b-912b-4a35-b395-0c961f8784a2`，**exit=1**，tests 9／pass 8／fail 1。
   - 删除后：`runId=5c146663-60fc-4de6-814f-b11b57e57f04`，**exit=0**，tests 9／pass 9／fail 0；
     两条探针路径与两包 `dist` 均无残留（`git status` 逐条核过）。
7. **待接线清单**（审查第 ② 条）：六件当刻消费方**各只有判据文件 1 处**（本席按 import 逐件复核，
   与审查探针读数一致）。将来按下面的族接线：

   | 件 | 接线族票 |
   |---|---|
   | `shared/operationHead.ts` | `#423`（写后回执页族，13 条写词）——操作卡头部 |
   | `shared/fieldLabel.ts` | `#423` ＋ 记录级明细页族实施票（**票未切**）——按 `registerFieldLabels('exercise', 表)` 登记 |
   | `shared/sourceLine.ts` | `#423`（回执页融合补来源脚注）＋ 运动汇总／对照目标／趋势／复盘各族读页票（**票未切**） |
   | `shared/conclusionLine.ts` | 复盘族读页票（**票未切**）——整句由调用方给 |
   | `shared/emptyGuide.ts` | 类型分布／力量总览／有氧总览／复盘各族读页票（**票未切**）——三件套由调用方给 |
   | `exercise/categoryColors.ts` | 分布／趋势／复盘各族读页票（**票未切**）——色表与图例同源 |

   读页各族实施票当刻**票未切**（切分建议见 `docs/skills/skill-calorie/t156-融合设计.md` §六），
   故本表只写族名、不预支票号；实施时按族票号回填本表。
8. **补记两处漏记**（审查第 ③ 条；行号为本席当刻读数）：
   - `fieldLabel` 同形件另有 2 处：`src/exercise/receipt.ts:42`（16 键表，`:62` 回退
     `FIELD_LABELS[col] ?? col`）、`src/profile/labels.ts:19`（表，`:29` 回退，自带 5 键）。
   - `sourceLine` 私有同形件另有 3 处：`src/weight/history.ts:462`、
     `src/weight/volatilityDoc.ts:206`、`src/diet/nutritionPortDocs.ts:59`。
9. **转票候选（范围外，本席未动）**：`src/body/bodyDocs.ts:153` 自造空态结构 1 处
   （`class="msr-empty"`，别席在途、未入库）→ 归该域票收口；别域同形件合并仍按 §四 记账待裁定。
