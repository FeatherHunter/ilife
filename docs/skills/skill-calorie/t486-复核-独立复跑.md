# #486 独立复核（被复核件：孤立提交 962e35d）

判定：**FAIL**（S1-交付缺陷：交付物不在 HEAD 祖先链上，三件里的两件在仓库里根本不存在）。

被复核方声称的提交 `962e35d4b471cde0a8f0b5f00ca17727876c839b` 内容完好可读，但**不在 HEAD
祖先链上**（`git merge-base --is-ancestor 962e35d HEAD` → exit=1；`git for-each-ref --contains`
无任何引用；`git rev-list --all` 不含它）。故本票的核心交付物在仓库当刻状态里**不存在**。

## 一、写集声明（复核席，只写这两处）

| # | 路径 | 用途 |
|---|---|---|
| 1 | `docs/skills/skill-calorie/t486-复核-独立复跑.md`（本件） | 复核证据件 |
| 2 | `.scratch/t486/**` | 复核席独占草稿：日志、隔离副本、自设探针 |

**未写**：`packages/skill-calorie/**` 一行未动；`962e35d` 的三件一件未取回、未改写（复核席不改实施席代码）。
`docs/skills/skill-calorie/t449-字段中文标签.md` 未碰。

## 二、链上状态与成因（机器读数）

- `git merge-base --is-ancestor 962e35d HEAD` → **exit=1**（不在链上）。
- `git show --name-status --format='' 962e35d` → `M docs/skills/skill-calorie/t449-字段中文标签.md`／
  `A docs/skills/skill-calorie/t486-字段绑定判据.md`／`A packages/skill-calorie/test/field-label-bindings-486.test.mjs`。
- `git ls-tree -r --name-only HEAD` 里搜 `field-label-bindings-486`／`t486-字段绑定判据` → **0 命中**；
  盘上同样**不存在**这两件。故 `node packages/skill-calorie/test/field-label-bindings-486.test.mjs`
  报 `ERR_MODULE_NOT_FOUND`、exit=1。
- 成因（`git reflog --date=iso` 原文）：`1905ada HEAD@{2026-09-15 23:01:28 +0800}: reset: moving to HEAD~2`
  —— 这一手把 `aeb7a90`（23:01:01 提交，560 席）与本票 `962e35d`（23:01:07 提交）**一起移出分支**。
  23:01:51 以 `f9f3fed` 重提了 560 的成果，**本票的成果再没有被重提**。
  违反的是**他席**（`reset --soft`，仓规禁止的危险 git 命令）；本票实施席自身流程未见违规。
  孤立对象仍在 reflog：`962e35d refs/heads/master@{28}`。
- 抢救口径（下一手可直接照做，**复核席不代做**）：`git cherry-pick 962e35d`，或
  `git checkout 962e35d -- <三件路径>` 后按本票流程重提。内容完好，无需重写。

## 三、独立重跑：票面「验收命令」逐条（我自己跑的，不采信实施席「已绿」）

```
GATE-RUN runId=e382a02e-f3ff-4bb0-a260-08dc4693cde7 cmd=pnpm build
GATE-RUN runId=b42f910f-58ff-4318-93e0-75a2cc8803db cmd=node node_modules/typescript/bin/tsc -b
GATE-RUN runId=c65fb8db-5206-4be0-bf90-0d91d7b149b9 cmd=node --test packages/skill-calorie/test/field-labels-449.test.mjs
GATE-RUN runId=05e82c55-e9f3-478e-a98a-7fcef94f2c8f cmd=node packages/skill-calorie/test/field-label-bindings-486.test.mjs
GATE-RUN runId=c5c9fd51-0d8e-40e6-a3ca-4b70c0a01e02 cmd=node packages/skill-calorie/test/exercise-receipt-fusion-423.test.mjs
GATE-RUN runId=80dbe814-310f-4bf0-807d-5deb721dd770 cmd=node --test packages/skill-calorie/test/exercise-receipt-264.test.mjs
GATE-RUN runId=9d24c53d-b8ac-4e64-9513-04d465d341ce cmd=node packages/skill-calorie/scripts/check-warning-line.mjs
GATE-RUN runId=2a3dda35-3b83-4d09-8354-dccc297b6a00 cmd=node tooling/check-gate-audit.mjs --evidence .scratch/t486/orphan-t486-doc.md --log .scratch/locks/gate-runs.log --ticket 486 --since 2026-09-15T14:49:00Z --until 2026-09-15T15:02:00Z
```

| 验收命令 | 我的 exit | 我的读数 | 判定 |
|---|---|---|---|
| `pnpm build` | **1** | `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` | **环境阻断，非本票** |
| 其第一步等价直调 `tsc -b` | 0 | 零报错 | 编译面绿（§2.4.5 直接调用等价底层命令） |
| `field-labels-449.test.mjs` | **1** | `tests 17 pass 16 fail 1`，红的只有 ④「标签单源」 | **范围外**（见 §六） |
| `field-label-bindings-486.test.mjs` | **1** | `ERR_MODULE_NOT_FOUND` | **S1：交付物不存在** |
| `exercise-receipt-fusion-423.test.mjs` | 0 | `tests 20 pass 20 fail 0` | 绿 |
| `exercise-receipt-264.test.mjs` | 0 | `tests 13 pass 13 fail 0` | 绿 |
| `check-warning-line.mjs` | 0 | `RESULT: 87/87`／`PASS: 告警线台账齐全且与实况一致` | 绿 |
| `check-gate-audit.mjs`（对孤立证据件原文） | **1** | `matched=0/0 undeclared=11`／`FAIL: 证据里没有任何 GATE-RUN runId=… cmd=… 声明` | **S2：声明格式不合规**（见 §六） |

`pnpm build` 说明（§2.1 第 5 条，当场停手上报）：pnpm 认为 `node_modules` 需先清除重装，
无 TTY 即 `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` 中止；**装依赖不在本席权限，未装、未绕过**。
`node_modules/.modules.yaml` mtime＝2026-09-15 23:58:12，晚于 `pnpm-lock.yaml`（09-12 23:21），疑似他席装过。
编译面以其第一步（`tsc -b`）单独取读数，绿。

## 四、自设探针（实施席脚本覆盖不到的新试法）

脚本随证据入仓：`.scratch/t486/probe-486.mjs`（隔离副本 `.scratch/t486/iso/`，
`node_modules` 以 junction 指回真件；**不碰**仓库任何共享路径）。

```
GATE-RUN runId=499aa533-2c0e-416b-9c7d-72d3bd3ff70c cmd=node .scratch/t486/probe-486.mjs
GATE-RUN runId=5f371803-acf1-4bc5-a829-0b4ea216c0dd cmd=node .scratch/t486/probe-486.mjs
```

（`499aa533` 是**作废的第一次**：隔离副本漏了 workspace 依赖链接，三份老判据在**加载期**就死
（`ERR_MODULE_NOT_FOUND: base-paint`），那一轮 `fail=1` 全是假红。已补 junction 后重跑，
`LOAD_ALL_OK=YES`；两次都留痕在案。）

**变异自证两行（`5f371803` 那次内跑）**：

- 改坏必红：`MUTANT-M1` 把 CLI 参数名两键中文对调（`calories: '消耗'` ↔ `minutes: '时长'`，
  正是票面举例的「消耗印在时长行上」）→ **449:fail 1→1、423:fail 0→0、264:fail 0→0，三份全不动**。
  `PROBE-VERDICT S2_STILL_OPEN=YES`。
- 还原必绿：探针件 sha256 复原为 `7bb94ac2ee56…`（与变异前逐字节一致），
  三份老判据读数回到基线 `mut1=[1,0,0]` → `restored=[1,0,0]`；`PROBE-FINAL-RESTORE sha-equal=OK`。
  真仓库 `packages/skill-calorie/src/exercise/fieldLabels.ts` 未动，其 sha256＝
  `6882ea1bbfd1013894b63f5244dc747607b3295cd4f65c012b869e0a426ea8a8`。

**判别力正对照（防「探针恒绿」）**：`PROBE-CTRL-M2` 换成对调库列名两键
（`duration_minutes: '时长'` ↔ `calories_burned: '消耗'`）→ **449:fail 1→2**（多红一条，正是
`field-labels-449.test.mjs:215` 那条唯一写死中文的断言）。故 M1 的「三份全不动」是**真阴性**，
不是装置失明。

**结论（本票要治的那一格，在 HEAD 上仍敞）**：HEAD 当刻的产物里把两个 CLI 键的中文对调，
`449`／`#423`／`#264` 三份**一份都不红**——#449 审查判 S2 的场景**原样复现**。本票要补的正是这一格。

**顺带核了孤立判据件本身值不值得抢救**（隔离副本内跑 `962e35d` 的
`field-label-bindings-486.test.mjs`）：基线 `exit=0 fail=0`（28 条全绿）→ 上 M1 后
`exit=1 fail=2` → 还原后 `exit=0 fail=0`；上 M2 亦 `fail=2`。
即该件**确实有判别力**、两套键都钉住，是**可抢救的合格件**。

## 五、证据真实性核验（对实施席 §三那 7 条逐条认口）

7 条 `runId` 我逐条回查 `.scratch/locks/gate-runs.log`：**全部命中**，START／RUN 成对，
exit 与其证据件表格**逐格一致**（2／0／0／1／0／0／1），时刻落在其自述的
「取数时刻 22:49–22:55＋08:00」（UTC 14:49–14:55）之内。**无编造 runId。**
另核其两条可复算断言，**皆真**：

- 「旧判据件带具体中文的绑定断言只有 1 条（`field-labels-449.test.mjs:215`）」→ 逐字属实（已读原文）。
- 「还原 sha256＝`6882ea1bbfd1…`」→ 我独立算 `src/exercise/fieldLabels.ts` 得 `6882ea1bbfd101…`，**逐字相符**。
- 其「对调必红 fail 2／还原必绿」→ 我用自己的 M2 在隔离副本上独立复现 **fail=2**，与之一致。

## 六、缺陷清单（分级＋归属＋怎么重现）

1. **S1-交付缺陷｜本票范围（成因他席）｜交付物不在链上。**
   重现：`git merge-base --is-ancestor 962e35d HEAD; echo $?` → 1；
   `node packages/skill-calorie/test/field-label-bindings-486.test.mjs` → `ERR_MODULE_NOT_FOUND`。
   后果：本票判据件与证据件在仓库里**不存在**，票面验收命令有两条根本无法开跑；#449 的 S2 **仍未清**。
   归因：直接成因是**他席** `1905ada reset --soft HEAD~2`（危险 git 命令，仓规禁止）；
   本票实施席在 23:01:07 的提交动作本身合规（提交范围只含自己的三件，可与 §五 runId `7b35ca9e`／`44b8e70b` 对上）。

2. **S2｜本票范围｜`GATE-RUN` 对账用了表格，不是规范声明行。**
   重现：`node tooling/check-gate-audit.mjs --evidence <其证据件原文> --ticket 486
   --since 2026-09-15T14:49:00Z --until 2026-09-15T15:02:00Z` →
   `FAIL: 证据里没有任何 GATE-RUN runId=… cmd=… 声明`／`matched=0/0 undeclared=11`／exit=1。
   协议 §2.4.2 要求「单独一行 `GATE-RUN runId=<标识> cmd=<命令>`」；仓内已有 74 份证据件用该形态
   （含 `git add`／`git commit` 条目，如 `t180-scenes-01-05.md`）。表格形态使官方对账门直接红。
   同一因致其窗口内 4 条**无人声明**的条目（`git add`／`git restore --staged`／`commit.ps1`／`commit.mjs`，
   runId `7b35ca9e`／`96591e09`／`e55a553d`／`44b8e70b`）。

3. **S3｜范围外｜`field-labels-449.test.mjs` ④「标签单源」已陈化。**
   重现：跑该件 → `tests 17 pass 16 fail 1`；断言 `actual=[diet, exercise, weight, workout 四张表]`、
   `expected=[exercise/fieldLabels.ts]`。成因＝#483／#351／#496 后来各自提交了本域标签表，
   而 ④ 用全仓扫描＋写死「只许一处」。**与本票无关**：本票三件里一件源码都没碰；
   该红在实施席自己的基线 `aeb7a90`（23:01）上已存在（当时四张表俱在，且 449 判据件与 HEAD 逐字节同）。
   实施席已在 `t449-字段中文标签.md` §九**如实记下「16/17，④单源红，系他席已提交的三域表所致，与本票无关」**，
   归因正确、无瞒报。修法：转 #449 原席或另开票收窄该断言。

4. **S3｜范围外（环境）｜`pnpm build` 当刻不可跑。**
   重现：`node tooling/run-locked.mjs --ticket 486 -- pnpm build` →
   `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`／exit=1。属共享依赖目录异常，
   按 §2.1 第 5 条**停手上报**，恢复由编排者持锁统一做（本席未装依赖、未绕过）。
   佐证：实施席 22:49 那次同命令 exit=2，是**另一种**红（`planEditorPort.ts` TS2304，他席在途件）；
   我当刻直调 `tsc -b` 已 exit=0，那条 TS 错已消失。

## 七、五维打分（供参考，不作判定主据）

| 维 | 分 | 依据 |
|---|---|---|
| 契约一致 | **13**/30 | 判据内容与票面逐条对得上（26 键写死、空串分支、t449 §九、交付路径正确），但**交付物不在仓库**＝票面交付未达成；对账形态不合规 |
| 证据真实可复现 | **18**/25 | 7 条 runId 全部命中且 exit 逐格一致、时刻自洽；还原哈希与「fail 2」我独立复现；扣分因声明非规范行、4 条窗口条目未声明 |
| 新旧对照 | **16**/20 | 我独立证实「三份老判据对两键对调都不红」（S2 属实）与「新判据对调即红 fail=2」；老件只加时刻未改结论 |
| 工程红线 | **13**/15 | 实施席自身：提交限定三件、走包装器、未用危险 git、未裸跑；扣分因窗口内 4 条未声明；`reset` 系他席所为，不记本票 |
| 文档同步 | **6**/10 | 证据件详实、缺陷逐条归因且归因正确；扣分因 `GATE-RUN` 未用规范声明行（官方门 FAIL） |
| **合计** | **66**/100 | 有 S1 ⇒ FAIL；均分 <85 ⇒ FAIL |

若把 `962e35d` 原样重提（cherry-pick）并把 §三 改成规范 `GATE-RUN` 行，契约一致可回到 ~27/30、
文档同步 ~9/10，总分约 **87–89**——本件是**抢救件**，不是重做件。

## 八、未做项与归因

1. **未做：取回/重提孤立件**。复核席不改实施席代码，`962e35d` 一件未取回；抢救归属实施席或编排者。
2. **未做：整仓全量测试**。票面验收命令未要求，且当刻 `pnpm build` 被环境阻断（§六-4）。
3. **未做：`pnpm build` 的整条链路**。只取到第一步 `tsc -b`（绿）；后两步（`gen-cli.mjs --stamp`、
   `pnpm -r run build:client`）因 pnpm 依赖检查阻断未跑——未跑 `--stamp` 也是为免在多席在飞时改生成物。
4. **未做：视觉验收墙**（`docs/agents/视觉验收墙.md` 那一套）。本票是判据票，交付物里无页面产物。
5. **下一手缺什么**：① 一条干净的 `cherry-pick 962e35d` 窗口（重提后把证据件 §三 改成规范
   `GATE-RUN runId=… cmd=…` 行）；② 恢复 `node_modules`／`pnpm build` 的编排者持锁作业（现被
   `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` 阻断）；③ #449 ④ 断言的收窄归属（另票或 #449 原席）。

## 九、复核席本窗 git 步骤对账行（规范声明行，逐条与 `gate-runs.log` 对得上）

```
GATE-RUN runId=bfb137f5-a2c3-4ffb-86a7-b86678dfc1a7 cmd=git add -- docs/skills/skill-calorie/t486-复核-独立复跑.md
GATE-RUN runId=d96033a6-7c41-425e-814f-2ced44288801 cmd=git commit -F .scratch/t486/msg-1.txt -- docs/skills/skill-calorie/t486-复核-独立复跑.md
GATE-RUN runId=<本补记提交自身的 runId，见下一次补行> cmd=git commit -F .scratch/t486/msg-2.txt -- docs/skills/skill-calorie/t486-复核-独立复跑.md
```

提交范围自核：`git diff --cached --name-only` 当刻读数＝**1 件**（本件），未夹带他席在途件。
本席**未 push**（派单明令禁止）。
