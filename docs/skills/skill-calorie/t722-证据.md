# #722 执行与核验证据 · 五家技能 SKILL.md：路径口径改读配置文件

> 落点：本件住归属件目录 `docs/skills/skill-calorie/`（件名＝`packages/` 下目录名逐字；跨件票按 §1 的「同一目录、票号前缀分家」办）。
> 写集：`packages/{skill-chef,skill-home,skill-memo-ilife,skill-schedule,skill-calorie}/SKILL.md`、`packages/skill-calorie/test/skill-t11.test.mjs`、`docs/env.md`、`docs/skills/skill-calorie/t722-*`。
> **`packages/*/src/**` 一行未改；生成器脚本（`scripts/build-help.mjs`、`scripts/gen-*.mjs`）一行未改，只重跑；`templates/*.html` 一行未改。**
> `docs/env.md` 不在票面点名的交付物路径里，但**六家 `SKILL.md` 的「环境」节全都写「见 docs/env.md」**⇒ 它是这些行的**被指向物**，不改就等于把同一批假话留在读者被指去的下一页。处置与理由见 §七。

## 一、判定

**PASS** —— 票面四条验收逐条有本席自跑的机器读数；另加两组归因实验，把两条「门红」判到别人头上（不采信「反正门本来就红」这种说法）。

开工现状：本票 2026-09-18 建档、无 assignee、无评论、无阻塞边、`wayfinder:task` 标签、无 `needs-triage` —— **未动工**，故走「正常诊断 → 实施」。本席先认领（`assignees=FeatherHunter`，会话第一笔写）再动。

## 二、票面验收逐条读数

| # | 票面判据 | 本席读数 |
|---|---|---|
| 1 | 五家 `SKILL.md` **零命中**已删环境变量名 | `git grep -n -E "SKILLS_DB_PATH\|_FORCE_PROD\|_PHOTOS_DIR\|CALORIE_TODAY" -- <五家 SKILL.md>` → **零命中（exit 1）**；扩到 `LARK_CLI_PATH\|MEMO_MEDIA_DIR\|process\.env` 仍 **零命中（exit 1）**（读数见 §三） |
| 1b | 落点口径一律改写成「配置文件里的哪一项 ＋ 空值回落到哪」 | 五家逐处改写，配置项名逐字取自各包 `src/config.ts` 的默认值表（见 §四对照表）；新增的 `skill-t11` 正向断言把 `~/.ilife/calorie.yaml` 与 `db.dir`／`html.dir`／`photos.dir`／`photos.gifs` 四个配置项名钉进常驻看守 |
| 2 | 钉着旧变量名的断言改成钉新口径；改坏一处必红、改回必绿 | `packages/skill-calorie/test/skill-t11.test.mjs:42` 的 `assert.match(skill, /SKILLS_DB_PATH/)` 换成「正向钉配置口径 ＋ **负向禁已删变量名**」两组断言；**两行机器读数**＝`MUT exit=1 pass=7 fail=1` ／ `RESTORE grep-exit=1 pass=8 fail=0`（§五） |
| 3 | 生成物链不破：AUTO 块由生成器重写、只动块外正文；`gen:check` 与各包 `build-help`／`check-examples` 门不红 | 五家 `scripts/build-help.mjs` 重跑后 **SKILL.md 字节不变**（`skill-chef=e2a52830 skill-home=928b61cd skill-memo-ilife=bbcb3189 skill-schedule=00811d0f skill-calorie=5b62601f`，`ab-A`／`ab-B` 两轮同值）；`gen:check` 两包 exit 0；四家 `gen-help-assets --check` exit 0。**`check-examples` 当刻为红**——经归因实验判定**与本票无关**（§六），据实登记 |
| 4 | 两条边界门不红 | `check-boundaries` exit 0（`boundaries: PASS`，`runId=0ff2b33e`）；`plugin-p10-boundaries` 13/13（`runId=405fc29b`） |

## 三、判据 1 的机器读数（可复跑）

```
$ git grep -n -E "SKILLS_DB_PATH|_FORCE_PROD|_PHOTOS_DIR|CALORIE_TODAY" -- \
    packages/skill-chef/SKILL.md packages/skill-home/SKILL.md \
    packages/skill-memo-ilife/SKILL.md packages/skill-schedule/SKILL.md \
    packages/skill-calorie/SKILL.md
[无输出]  exit=1        ← 零命中

$ git grep -n -E "SKILLS_DB_PATH|_FORCE_PROD|[_A-Z]*PHOTOS_DIR|CALORIE_TODAY|LARK_CLI_PATH|MEMO_MEDIA_DIR|process\.env" \
    -- 'packages/skill-{chef,home,memo-ilife,schedule,calorie}/SKILL.md'
[无输出]  exit=1        ← 零命中（加宽到全部已删变量名与 process.env）
```

**开工前同一命令的读数**（`git show HEAD:<件>` 实测，加宽到六个变量名）：五家**各有命中、合计 28 处** ——

```
skill-calorie     : 10 处 / 6 行  :31 :32 :320 :545 :549 :556
skill-home        :  8 处 / 5 行  :23 :24 :33 :153 :160
skill-chef        :  5 处 / 4 行  :24 :36 :56 :107
skill-memo-ilife  :  3 处 / 3 行  :64 :70 :76
skill-schedule    :  2 处 / 2 行  :93 :104
```

票面那条正则（只 `SKILLS_DB_PATH`／`_FORCE_PROD`／`_PHOTOS_DIR`／`CALORIE_TODAY` 四个名）在提交版上同样是 **28 处**（这回多出来的 3 处 `LARK_CLI_PATH` 恰好都在含其它变量名的行里，故四个名的正则一处不漏）。

## 四、逐处改动对照（口径逐字取自各包 `src/config.ts`）

| 件:原行 | 原样（已删机制） | 改成 |
|---|---|---|
| `skill-chef/SKILL.md:24` | `<SKILLS_DB_PATH>/cook_html/help/…` | `<库目录>/<产物目录>/…`，并写明 `db.dir`（空＝数据目录 `~/.ilife/data/`）与 `html.dir`（默认 `cook_html/help`） |
| `skill-chef/SKILL.md:36` | 「落文件仍然要求 `SKILLS_DB_PATH` 已设置」 | 「库目录定得下来：`db.dir`，空串＝数据目录，故从不因『没设什么』而失败」 |
| `skill-chef/SKILL.md:56` | 「`SKILLS_DB_PATH` 指向 mkdtemp」 | 「测试把配置目录经 `ILIFE_CONFIG_DIR` 指到 mkdtemp」 |
| `skill-chef/SKILL.md:107` | `SKILLS_DB_PATH`（必设）＋ `CHEF_FORCE_PROD` 哨兵 | 五个配置项逐项点名（`db.dir`／`db.name`／`html.dir`／`files.help`／`files.lookup`）＋ `ILIFE_CONFIG_DIR` |
| `skill-home/SKILL.md:23,24` | `<SKILLS_DB_PATH>/home_manager_html/…` | `<库目录>/<产物目录>/…`（`db.dir`／`html.dir`＝`home_manager_html`）；速查表改为「同一产物目录下」 |
| `skill-home/SKILL.md:33` | 「要求 `SKILLS_DB_PATH` 已设」 | 同大厨那条 |
| `skill-home/SKILL.md:153` | 三个变量（含 `HOME_PHOTOS_DIR`） | 六个配置项点名；**并写明「没有照片目录这项配置」**（本包 src 零读该变量） |
| `skill-home/SKILL.md:160` | `export SKILLS_DB_PATH=…` / `$env:SKILLS_DB_PATH=…` | 换成**可照抄即跑**的隔离配方：`ILIFE_CONFIG_DIR` 指临时目录 ＋ 现场写一份 `home.yaml`，并写明落点 |
| `skill-memo-ilife/SKILL.md:64` | `<SKILLS_DB_PATH>/memo_html/…` | `<库目录>/<产物目录>/…`（`db.dir`／`html.dir`＝`memo_html`，扁平一段） |
| `skill-memo-ilife/SKILL.md:70` | 「跑完 `<SKILLS_DB_PATH>/memo` 仍不存在」 | 「跑完数据目录里仍没有 `memo.db`」 |
| `skill-memo-ilife/SKILL.md:76` | `SKILLS_DB_PATH`（必设）＋ lark-cli | 八个配置项点名（含 `lark.cliPath` **空＝走本机自动探测**、`lark.qrDir`）＋ lark-cli 四门 |
| `skill-schedule/SKILL.md:93` | `<SKILLS_DB_PATH>/schedule_html/help/…` | `<库目录>/<产物目录>/…`（`db.dir`／`html.dir`＝`schedule_html/help`） |
| `skill-schedule/SKILL.md:104` | 两个变量 | 六个配置项点名 ＋ lark-cli 四门 |
| `skill-calorie/SKILL.md:31` | `<SKILLS_DB_PATH>/calorie_html/…`、相对 `SKILLS_DB_PATH` | `<库目录>/<产物目录>/…`（`db.dir`／`html.dir`＝`calorie_html`）、相对库目录 |
| `skill-calorie/SKILL.md:32` | 「`SKILLS_DB_PATH` 必设（无默认值）；照片存在位需 `CALORIE_PHOTOS_DIR`」 | 「库目录与照片目录都从配置文件取（`db.dir`／`photos.dir`）；**没有一个环境变量是必设的**；照片目录没配时存在位记 null 不断言」 |
| `skill-calorie/SKILL.md:320` | 「读 `SKILLS_DB_PATH` 库」 | 「读配置里的库目录」 |
| `skill-calorie/SKILL.md:545` | 「`CALORIE_TODAY=2026-09-07`」 | 「『今天』由测试把整只钟钉到种子日（`test/freeze-clock.cjs`）」 |
| `skill-calorie/SKILL.md:549` | 两个变量 | 九个配置项点名（`db.dir`／`db.name`／`html.dir`／`photos.dir`／`photos.gifs`／`xunji.*`／`land.*`） |
| `skill-calorie/SKILL.md:556` | `export SKILLS_DB_PATH=…` / `$env:SKILLS_DB_PATH=…` | 同居家那条（`ILIFE_CONFIG_DIR` ＋ 现场写 `calorie.yaml`），并把落点写进下文 |

**一处正文订正（超出「删变量名」的字面要求，但属同一假话）**：`skill-memo-ilife/SKILL.md` 原说「HELP 页 `exec` 读 `SKILLS_DB_PATH` 库」「落 `<SKILLS_DB_PATH>/memo`」——第二句在改动前就已不成立（库文件是 `<库目录>/memo.db`，不是目录 `<库目录>/memo`），一并按当刻实现改准。

## 五、判据 2 与自证（两行机器读数）

`packages/skill-calorie/test/skill-t11.test.mjs` 的 `SKILL 含说明/唯一出口/envelope/HELP/环境` 这条，原断言 `assert.match(skill, /SKILLS_DB_PATH/)` 只要求「说明面提到过这个变量名」——**它对改后的口径恒为红，对改前的假话恒为绿**，是这次要换的钉子。换成：

- **正向**：`~/.ilife/calorie.yaml` 必须出现（配置文件落点），且 `db.dir`／`html.dir`／`photos.dir`／`photos.gifs` 四个配置项名逐条必须在场；
- **负向**：`SKILLS_DB_PATH|CALORIE_PHOTOS_DIR|CALORIE_FORCE_PROD|CALORIE_TODAY` 一个都不许再出现 —— 把票面判据 1 变成**常驻看守**，不靠人每次手跑 grep。

**说明面变异自证**（靶点＝同一件里块外的正文一行，`AUTO` 块不受影响）：

```
MUT     把「存在位记 null、不断言。」改成「存在位由 SKILLS_DB_PATH 与 CALORIE_PHOTOS_DIR 决定。」
        → git grep … exit=0  命中 packages/skill-calorie/SKILL.md（零命中那条当场报命中）
        → node --test …/skill-t11.test.mjs   exit=1  pass=7  fail=1   ← 红
RESTORE 逐字节写回（sha=5b62601f0e19b03f 与原件相同）
        → git grep … exit=1  ← 回到零命中
        → node --test …/skill-t11.test.mjs   exit=0  pass=8  fail=0   ← 绿
```

**源码级变异自证**（复跑 `#718` 的同一靶点，证本票改动没把它弄钝）：

```
MUT      packages/skill-calorie/src/config.ts 的 CALORIE_CONFIG_STEM 由 'calorie' 改 'calorie_t722'
        → tsc -b exit=0
        → node --test …/photo-gif-page-352.test.mjs  exit=1  pass=2  fail=6
RESTORE  逐字节写回（sha=3feba2356cfede12）→ tsc -b exit=0
        → node --test …/photo-gif-page-352.test.mjs  exit=0  pass=8  fail=0
```

## 六、两条门红：归因实验（都不落在本票头上）

判据电池 `RESULT: 32/34 FAIL`，两条红如下。**不采信「门本来就红」这种口说**，各出一组实验。

### 6.1 `calorie-c43.test.mjs` 的 `C6 写收据HTML结构化分项`（fail=1）

这条断的是 `calorie.diet.add` 回执页的**结构化表格单元格**（`data-label="字段" … 已写入饮食记录`），与 `SKILL.md` 一字无关。实验＝**HEAD 基线 A/B**（同一加锁窗口内，把 `SKILL.md` 与 `skill-t11.test.mjs` 逐字节换成 `git show HEAD:` 版本，重编再跑，最后逐字节还原并自证 sha）：

```
HEAD 基线 · calorie-c43    exit=1  pass=8  fail=1     ← 提交版也是红
我的版本   · calorie-c43    exit=1  pass=8  fail=1     ← 同一条
还原自证：SKILL.md sha=5b62601f0e19b03f、skill-t11.test.mjs sha=58261b47d21f2c1b（与原件相同）
```

⇒ **与他席在途的卡路里回执改动同源，非本票引入**（`packages/skill-calorie/src/diet/receipt.ts` 当刻正被另一席改，工作树里 10:13 前后还有写入）。

### 6.2 `check-examples`（红 34/134）

红的是 `calorie.diet.*`／`photo`／`view` 等**真跑示例**（`ERR 4 取数失败：… 无饮食记录`／`Entry ID 1 不存在`），不是 AUTO 块新鲜度那条 —— 结构问题 `0`，AUTO 块 == 生成器输出那条**过**。实验＝**同 dist 换说明面 A/B**（同一加锁窗口、同一份编译产物上，只把 `SKILL.md` 在我的版本与 HEAD 版本之间来回换，A→B→A→B 各跑一轮）：

```
窗口内 skill-calorie/dist 指纹：before=ba9df0bf8a470eb3/1530  after=ba9df0bf8a470eb3/1530   ← 无漂移
A 轮（#722 改写版）红数：r1=34/134  r2=34/134
B 轮（HEAD 版）   红数：r1=34/134  r2=34/134
⇒ 差 0：说明面换进换出，红数不变
```

⇒ **这 34 条红与本票的 `SKILL.md` 改写无关**；根因在他席当刻在改的命令声明／示例数据（`gen-cli` 的 `EXAMPLE` 表由声明派生，声明一改示例即变）。

### 6.3 处置

票面判据 3 写的是「各包 `build-help.mjs`／`check-examples.mjs` 门不红」。本票改动**没有**让这两道门变红（6.1／6.2 两组实验各出机器读数），但 `check-examples` 当刻**确为红**，主因在别席在途。按「不越界改别人的件」的口径：**本席只登记、不改**（见 §八遗留 1），并把这条如实写进票面进度区，不当作本票绿。

## 七、本票落点之外的发现与处置（`git grep` 扫出的同一类假话）

票面「遗留出口」要求：发现别的已退役机制，登记在评论里另裁。本席扫全仓后分两类处置：

**写进本票的**（都在交付物路径的延长线上，不写就等于把假话留在读者被指向的下一页）：

| 件 | 当刻内容 | 处置 |
|---|---|---|
| `docs/env.md` | 全文 16 行仍在登记「`SKILLS_DB_PATH` 全局必设 ＋ 五个 `*_FORCE_PROD` 哨兵 ＋ `CALORIE_PHOTOS_DIR`／`LARK_CLI_PATH`」，而 **六家 SKILL.md 的「环境」节全都写「见 docs/env.md」** —— 它正是这些行的被指向物 | 按当刻实况重写：一、六家的配置面（配置文件是唯一真相＋逐家键表）；二、记账尚未迁移的那一套（**当刻仍是实况**，单列）；三、其它环境项；四、doctor 与测试隔离；五、遗留 |

`docs/env.md` 无程序化消费者（`tooling/skilllink.mjs` 只在**注释**里引它），改写不影响任何门。

**只登记、不改的**（各有归属，见 §八）：

1. `packages/skill-bill/SKILL.md:125`（`SKILLS_DB_PATH` 必设 ＋ `BILL_FORCE_PROD`）与 `:114` —— **目前与实况相符**，因为记账的**取值面还没迁移**（`packages/skill-bill/src` 当刻仍读 `SKILLS_DB_PATH`／`BILL_FORCE_PROD`／`BILL_TODAY`）。它该由记账的取值面迁移票一并改，**不是本票这种「说明面跑在取值面前面」的假话**。
2. `tooling/skilllink.mjs` 的 doctor（`checkDb()` 判 `SKILLS_DB_PATH`；`SKILL_CHECKS` 逐包判五个哨兵与三个目录变量）—— 两个 artefact 类（可执行工具＋自带测试），不是说明面。
3. `packages/skill-calorie/MIGRATE_ROLLBACK.md:5` 与 `packages/skill-calorie/scripts/migrate-calorie.mjs:7` —— 两处**用户可见文案**仍教人设已删的 `CALORIE_FORCE_PROD=1`，而该 opt-in 已删、守卫改成「非 tmp 一律拒」。它们是 #718 写集之外的同一条尾巴（#718 修的是 `src/` 与 `test/` 里的四处，这两件一件是包内文档、一件是脚本）。

## 八、遗留（登记去处）

1. **`check-examples` 当刻为红**（34/134，示例行真跑取数失败）：归因指向他席在途的命令声明／示例数据改动，非本票。本票关闭后该门是否回绿，跟着那一席走。
2. **`calorie-c43.test.mjs` 的 `C6`** 同上：他席在改 `src/diet/receipt.ts`，改完需重跑该件。
3. **`packages/skill-bill/SKILL.md` 的「环境」节**：等记账取值面迁移票落地后一并改写（口径与本票五家同形）。
4. **`tooling/skilllink.mjs` 的 doctor**：等六家取值面迁移齐了再重写（全局项＋逐包项两张表）。
5. **`MIGRATE_ROLLBACK.md`／`migrate-calorie.mjs`** 两处 `CALORIE_FORCE_PROD` 文案：按 `docs/agents/编排纪律.md` 第十条三问问责去留。

## 九、机器读数与路径

- 提交：见票面评论（本件与改动同轮提交）
- 判据脚本（可复跑，住草稿目录 `.scratch/t722/`）：`judge.mjs`（主电池）／`baseline.mjs`（HEAD 基线归因）／`ab-examples.mjs`（check-examples A/B 归因）
- 运行记录导出：`docs/skills/skill-calorie/t722-门禁运行记录.md`
- 明细日志：`.scratch/t722/*.log`（含 `tsc.log`／`inject-*.log`／`test-*.log`／`mut-*.log`／`restore-*.log`／`ab-*.log`）
- `GATE-RUN runId=150b85c2-e2f2-4ce6-b0c7-c2e57c0de710 cmd="node .scratch/t722/judge.mjs --wait-quiet-ms 180000"` → `exit=1`，`RESULT: 32/34 FAIL src-sha=67cb654bedf7ddc6`（两条红见 §六）
- `GATE-RUN runId=f06ca6d1-9c95-4eca-ad2b-e79bbd8cc906 cmd="node .scratch/t722/baseline.mjs"` → `exit=0`，`RESULT: 5/5 PASS`
- `GATE-RUN runId=0d844e41-94bb-4fe1-8ed8-5a005f36d44d cmd="node .scratch/t722/ab-examples.mjs"` → `exit=0`，`RESULT: 4/4 PASS`
- `GATE-RUN runId=405fc29b-11c3-457b-be0e-841612f4d51b cmd="node --test test/plugin-p10-boundaries.test.mjs"` → `exit=0`（13/13 pass）
- `GATE-RUN runId=0ff2b33e-f894-4be2-933d-e06ad258ae2f cmd="node tooling/check-boundaries.mjs"` → `exit=0`（`boundaries: PASS`）
- 窗口指纹：`src-sha=67cb654bedf7ddc6`（五家包 `src` 合计 487 件；窗口内 before==after，无漂移）
- 顺带跑的散件门（非票面点名，避免留盲区）：`check-one-path --selftest` exit 0／`check-one-path`（真实门）exit 0／`check-page-assert` exit 0／`check-warning-line` exit 0
