# #526 读侧族整改（看身材照／查身材照两态／对比两张照片 · 四页）

Part of #159（卡路里场景 09「身材照片」）。**被整改的四件样张**（交付目录 `D:\ilife-验收\scene09\`）：

| 样张 | 命令键 | 渲染源件 |
|---|---|---|
| 09-01 看身材照（画廊·全窗22张·本页4张） | `calorie.photo.list` | `packages/skill-calorie/src/photo/galleryDoc.ts` |
| 09-02 查身材照（单张·大图超1MiB） | `calorie.photo.detail` | `packages/skill-calorie/src/photo/viewerDoc.ts` |
| 09-03 查身材照（单张·大图已内嵌） | `calorie.photo.detail` | `packages/skill-calorie/src/photo/viewerDoc.ts` |
| 09-04 对比两张照片（跨标签·间隔48天） | `calorie.photo.compare` | `packages/skill-calorie/src/photo/compareDoc.ts` |

本票另立一件页内形状件 `packages/skill-calorie/src/photo/photoUi.ts`（三页共用的徽章列／键值行／备注分段／
间隔条与 `phu-` 页内样式），`photoThumb.ts` 只读未改（体积退让口径不动）。

## 0. 读法与纪律（先读这一节，免得把回归门当成绩）

- **回归门**：**三档横向溢出 0 → 0（保持）**。`#484` 早已把它做成零（编排者 2026-09-15 实测
  `OVERFLOW-ZERO pages=17 cells=51`），本票**不许弄成非零**，但**它本身不是成绩**——作者拿它报成绩即虚报。
  本节每页仍照录改前／改后两行，只为证明「没弄坏」。
- **实际改好的是**：① 分隔符与内部标识符**节点级归零**（本页各自的「改前 → 改后」）；② 图注／导航／
  表格等**并列语义换成形状**；③ 同一事实一页一处、内部叫法（`内嵌`／`存在`／命令原文）出页面；
  ④ 手机端口径（触摸区 ≥44px／窄屏表格行为／页内定位）。
- 判分口径（`seat-brief` §6）：VLM 宽松分 ≥92 才等价用户口径 ≥90；硬扣分条款＝一处分隔符串 −2／
  一处内部标识符 −3／一处同事实重复 −2／一处符号顶替文字 −1／一处窄屏溢出 −5／一处点击区 <44px −1／
  一处露英文标签或半角标点 −1。
- 探针读数**以节点级为准**（`audit-separators.mjs` 的 `node` 行），改前读数取自编排者与上一席位在
  **旧交付样张**上的实测（`.scratch/t526/before-sep.json`）。

### 0.1 工具与夹具（复现口径）

| 项 | 值 |
|---|---|
| 夹具 | `.scratch/t287/backup-real/`（真库逐字节备份；本票用隔离副本 `.scratch/t526/iso/{db,photos}`，sha256 与备份件一致 `57ce9d87…ffc15`） |
| 今天 | `CALORIE_TODAY=2026-09-15`（与 `00-场景09验收导航.html` 的现场条件段同口径） |
| 四页 `--params` | `{"days":180}`／`{"id":19}`／`{"id":31}`／`{"id1":19,"id2":32}`（逐字照 `gen-scene09-wall.mjs` 的 ITEMS） |
| 生成 | `node .scratch/t526/gen-pages.mjs [09-0n]`（产物落 `.scratch/t526/pages/`，同名交付件） |
| 量测 | `node packages/skill-calorie/scripts/measure-responsive.mjs`（真 Chrome CDP 三档） |
| 探针 | `node packages/skill-calorie/scripts/audit-separators.mjs`（**另一 session 在途件**：只读跑，sha256 `a8fea10f…481ce`） |
| DOM 读数 | `node .scratch/t526/dom.mjs <宽> <页…>`（本席位自造的只读探针：caption 盒／触摸区／提示条） |

### 0.2 构建与在途件（如实记账）

`npx tsc -b packages/base-render packages/skill-calorie` 在本票作业期间**报红，但报错都不在本票写集**
（别的 session 正在改道）：13:19 那次是 `src/exercise/records.ts(186,32)`、`src/render/sportDocs.ts(409,31)`、
`src/shared/docPage.ts(151,35)`、`src/shared/pageKit.ts(18,35)`／`(18,53)`；13:28 那次是
`src/render/wizardPortDocs.ts(115,10)`／`(129,12)`、`src/shared/docPage.ts(151,35)`、
`src/shared/pageKit.ts(18,35)`／`(18,53)`。**这三件都不是本票写集**（`docPage.ts`／`pageKit.ts` 归 #525，
`render/*` 与 `exercise/*` 归别的票），故照 `seat-brief` §7.5 第 4 条：记证据、继续做本票的四页，
**一处别人的件都没碰**。`tsc` 有错仍照常产出 `dist`，四页的生成与量测都用当刻 `dist`（本票写集的
`dist/photo/*.js` 与源件逐条对应，见每页的「产物字节」行）。

## 1. 09-01 看身材照（`galleryDoc.ts`）

### 1.1 三档读数（回归门：0 → 0，保持）

| 档 | 改前（旧交付样张） | 改后（重出） |
|---|---|---|
| 390 | 溢出 0 · img 4/4 放得下 | 溢出 0 · img 4/4 放得下 |
| 768 | 溢出 0 · img 4/4 放得下 | 溢出 0 · img 4/4 放得下 |
| 1440 | 溢出 0 · img 4/4 放得下 | 溢出 0 · img 4/4 放得下 |

读数来源：`.scratch/t526/measure-before.json`（`OVERFLOW-ZERO pages=4 cells=12 failed=0 label=t526-before`）
与 `.scratch/t526/measure-after.json`（`label=t526-after`）。

### 1.2 分隔符与内部标识符探针（节点级：34 → 0）

| 口径 | 改前 | 改后 |
|---|---|---|
| 节点级命中 | **34**（`·`33／`；`1／≥3段22／标识符6；R7 明细＝票号 `#16` 一类 6 处） | **0** |
| 行级命中 | 2 行 | 0 行 |

改前那 34 处的形状（逐处抄自 `.scratch/t526/before-sep.json`）：页头题名 `卡路里·身材照片`（R1）、
副标题 `看身材照 · 22 张`、提示块 `照片较多，本页只显示 4 张；还有 10 张没显示`（R1+R2）、
读数卡明细 `8 张找不到文件 · 10 张太大未显示`、**22 条图注** `2026-07-17 15:19 · 侧面 · 60 天前`（R1+R3）、
缺失清单 `#16 · 2026-05-30_008.jpg · 找不到文件` 一排（R1+R7 票号）。

### 1.3 改了什么（逐条）

| # | 债（改前） | 形状（改后） |
|---|---|---|
| ① | 页头副标题串 `看身材照 · 22 张` | 副标题撤；身份徽章列 `全部标签`／`近 180 天`（`chipRow`＋`renderChips`） |
| ② | 读数卡明细 `8 张找不到文件 · 10 张太大未显示` 两件事串一格 | 两张卡各说一件事：`找不到文件 8 张`＋卡下 `文件不在照片目录里`；张数只由 `本页显示 4 张／共找到 22 张` 一处说 |
| ③ | 图注一长串 `时刻 · 标签 · 相对时间 · 文件名` | `phu-when` 时刻一行（粗体）＋**徽章列**（编号／标签／相对时间）＋ `phu-file` 文件名小字块 |
| ④ | 缺失清单 `#16 · 文件名 · 徽标` 三串一行（与网格里的占位说同一件事） | 整块删；**网格里那张自己的占位**承担——状态徽标（`原图太大`／`找不到文件`）＋文件名＋「为什么」＋「下一步」三行 |
| ⑤ | 内部叫法上屏：`内嵌 0/1 张`、`太大放不下`、`存在` | 出页面：改「本页显示 N 张」／「原图太大，没进这一页」；正常行状态列留空（零信息值不印） |
| ⑥ | 六列表（含备注列）窄屏被撑爆 | 收成四列（编号／日期／标签／状态），窄屏由公共层行卡化＋本族一行「表格宽，手机上按住左右滑可以看全」 |
| ⑦ | 照片是裸 `<img>`（不等高、参差） | 等高卡片网格 `aspect-ratio:4/5` ＋ `object-fit:cover`（宽屏多列、390 宽两列），占位也占同一格位 |
| ⑧ | **（收口新增）** 逐卡 `复制文件名`＋公共层自动补的**禁用 `复制日志`**：一页 **49 颗按钮** | 撤掉逐卡按钮（文件名仍是可选中小字块）；页上按钮 **49 → 5** |
| ⑨ | **（收口新增）** 表题槽 `caption` 在公共层窄屏行卡化里被挤成 **32×106 px 竖排**（6 行一句话） | 表题槽不用：小节标题 `照片清单` ＋ 表下一行口径 `按时间倒序`（`renderCaliberLine`） |

### 1.4 收口两处的硬读数（`dom.mjs`，390 宽，改前＝旧交付样张，改后＝重出件）

| 量 | 改前 | 改后 |
|---|---|---|
| 按钮总数 | 49（`复制文件名`22＋`复制日志`23＋`复制数据▾`1＋三格式 3） | **5**（`复制数据▾`1＋`纯文本`／`JSON`／`CSV` 3＋页尾禁用 `复制日志`1） |
| 表格图注盒 | 356×34（满宽一行） | 无此槽（改小节标题＋口径行） |
| 窄屏滑行提示 | 无 | `display:block`（≥641 不出） |
| 点击区 <44px | 0 处 | 0 处 |
| 卡片盒（390） | 无网格 | 174×353 |

### 1.5 产物

- 重出件：`.scratch/t526/pages/09-01-看身材照（画廊·全窗22张·本页4张）.html`，1,039,033 字节
  （旧交付样张 1,031,961 字节）。
- 交付样张重出与字节表见本文 §5；截图与 vision 分见 §6。
