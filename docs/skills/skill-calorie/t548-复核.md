# #548 独立复核 · 定体重目标只给 kg 时不许抹掉 goal_deadline

复核者席位（第三阶段）。**不采信实施者的任何「已绿」**：下面每一条读数都是本轮自己跑的，
每条都带 `runId`（可从 `.scratch/locks/gate-runs.log` 逐行核对）。复核窗口内的探针与窗口脚本落
`.scratch/t548-R/`（`.scratch/` 在 `.gitignore` 里，不入库）。

## 一、判定

**PASS**，打分 **97/100**（契约一致 30／证据真实可复现 24／新旧对照 19／工程红线 15／文档同步 9）。
无 S1：改动件全在票面写集内、三条验收都自己跑绿、受控变异两场都证明「实施者四条用例发现不了、复核者探针能发现」、
还原后逐文件哈希与基线一致、产物是完整文档且屏幕上没有内部词、真库没被碰过。

## 二、圈定范围（提交件 ↔ 票面写集）

- `git show --stat 7d5c589` 只有四件，逐件都在票面写集内：
  `packages/skill-calorie/src/goal/goalStore.ts`（＋55／−8）、`packages/skill-calorie/src/goal/write.ts`（＋23／−8）、
  `packages/skill-calorie/test/goal-weight-deadline-548.test.mjs`（新建 136 行）、`docs/skills/skill-calorie/t548-证据.md`（新建）。
- 票面禁止清单逐条比对：`src/triggers/**`、`SKILL.md`、`src/cli/keys.ts`、`src/cli/registry.ts`、
  `src/triggers/routes.generated.ts` —— **一件未碰**；唤醒词名与四个参数名（`kg`／`deadline`／`startKg`／`startDate`）一字未动。
- `git diff HEAD -- <上面四件>` **空**：工作区这四件与提交逐字节一致。
- 读码确认没动的：`setWeightGoal` 的 ISO 校验与 `0..500` 边界、`pauseAllGoals`／`resumeAllGoals`、
  `writeGoalSet`／`writeGoalWater`（`write.ts` 只改了体重那一条与一行 import）。
- 提交信息里没有「键／写键／闭环／打通／收敛／沉淀」这类黑话（对新增行逐行扫过，零命中）。

## 三、独立重跑（自己跑，不看实施者的退出码）

1. 重建（持锁窗口内）：`GATE-RUN runId=765b22c8-207b-4c95-8fe0-24bd8123fc55 cmd="pwsh -NoProfile -File .scratch\t548-R\build-548r.ps1"`
   —— 删本包 `tsconfig.tsbuildinfo` 后重编，`BUILD-EXIT=0`，`DIST-SHAPE 修复版`（dist 里挑列那一行＝
   `['startKg', 'start_weight', startKg],`，摘要那一行＝`r.written.includes('deadline') && r.deadline !== null`）。
2. 验收命令原样重跑（票面那条，一字不加）：
   `GATE-RUN runId=47530a19-9d5c-4358-b512-07534270d8f8 cmd="node --test packages/skill-calorie/test/goal-weight-deadline-548.test.mjs"`
   —— 摘要行 `tests 4`／`pass 4`／`fail 0`，**退出码 0**。四条用例逐条：① 只给 kg → 截止日仍 `2026-12-31`
   且 `weight_goal` 换成 68；② `{kg,deadline}` → 写进 `2026-09-19`；③ 再念「看即将到期的目标」→ exit 0 且有 `daysLeft`；
   ④ `deadline:""` → 写 NULL 且 `writtenFields` 含 `deadline`。
3. 复核者探针（见第四节）在同一份 dist 上：`runId=052d936a-e74e-4afb-a815-f4e8714591b3`
   —— 摘要行 `tests 5`／`pass 5`／`fail 0`，**退出码 0**。
4. 症状独立复现（不靠实施者读数）：把「未传的列不进 SET」那一行改成恒进（＝改前写法）重编后，
   只给 kg 跑一次体重目标命令（exit 0），紧接着念「看即将到期的目标」→ **exit 4**，
   屏幕原文 `ERR 4: 取数失败（缺失阻断）：无到期目标（daily_goal.goal_deadline 缺失）`；
   还原重编后同一对命令 → **exit 0 ／ exit 0**（`runId=32abc7cb-7187-4460-9e5d-d592604a9ab6`）。

## 四、新试法（复核者自设，专打「实施者四条用例的盲区」）

探针 `.scratch/t548-R/probe-548r.mjs`（五条，全部是我自己写的，实施者看不到）：

- **P1／P1b**：`startKg`／`startDate` 两列各自独立挑列 —— 给 `{kg, startKg, startDate}` 后回读
  `start_weight`／`start_date`，再只给 `kg` 看三列是否保持。**实施者四条用例一条都没传过这两个参数**。
- **P2**：产物是完整文档（`<!doctype html>` 起＋`<meta charset=`＋`<style`＋`ilife-page`）＋
  envelope 的 `data.output` 是本次 `--html` 的绝对路径且文件真在 ＋ **只给 kg 那次的摘要与屏幕都不许出现「截止」**
  ＋ 屏幕（**可见文本**，口径取仓内 `test/visible-text-probe.mjs`：剔 head／style／script／注释与全部标签属性、
  并剔复制载荷）里没有 `op=`／`id=1`。
- **P3**：边界与 ISO 校验一字未动（`kg` 0／700 → exit 2；`deadline` `2026-9-1`、`startKg` 501、
  `startDate` `09/01/2026` → exit 4），且非法值跑完整行一字未动。
- **P4**：只给 kg 之后念「看即将到期的目标」exit 0（自己跑一遍，不抄）。

**受控变异两场**（都在持锁窗口内做完并立刻逐文件还原；每场都先读 dist 里那两处的原文，证明变异真编进去了）：

| 变异 | 改法 | dist 自证 | 实施者四条用例 | 复核者探针 |
|---|---|---|---|---|
| MUT-1 | `goalStore.ts` 挑列表 `startKg` 那一行的列名写错（`start_weight`→`start_date`） | `DIST1 pick=['startKg', 'start_date', startKg],` | `pass 4 / fail 0`（**全绿，发现不了**） | `pass 3 / fail 2`：`startKg 应落进 start_weight 列` 实测 `null !== 72.5` |
| MUT-2 | `write.ts` 摘要那一行改成 `const wroteDeadline = r.deadline !== null;`（无条件提截止） | `DIST2 summary=const wroteDeadline = r.deadline !== null;` | `pass 4 / fail 0`（**全绿，发现不了**） | `pass 4 / fail 1`：`本次只写 kg，摘要不许报「（截止 …）」：已定体重目标 66 kg（截止 2026-12-31）` |

两场红行点名的是**用户看得见的两件事**：一列写错地方、以及「没传截止却报「（截止 2026-12-31）」」——
后者尤其要紧：改前的写法正是这样把旧截止日谎报成刚写进去的。

**还原一致两行机器读数**（同一窗口内，跑完立刻逐文件覆盖回来并重编）：
`RESTORE1 ok=True`／`RESTORE2 ok=True`／`FINALLY-RESTORED goalStore=True write=True`、
`SRC-EQUAL-BASELINE goalStore=True write=True`（SHA256 与备份/提交件同值）、
`MARK-LEFT=False`（崩溃留痕文件已清）、`DIST-GREEN` 形态＝修复版、回绿后 `IMPL-GREEN pass=4 fail=0`、
`PROBE-GREEN pass=5 fail=0`。

## 五、缺陷逐条

**本票引入**：无。没有发现契约违反、用户可见回归、证据造假或私自放宽、数据丢失风险。

**本票范围**（已记，不判 FAIL）：
1. `affectedRows` 走 `total_changes()` 增量，只算行不算列（只改 `weight_goal` 也报 1 行）——旧口径，本票没动它，
   实施者已在证据件与用例里写明「不拿它当字段数判据」。
2. `deadline:""` 的口径按旧 `iso()` 钉成「显式空串＝清空该列」。若编排者要的是「空串等于没传」，
   这条判据与 `iso()` 要一起反过来——**需裁决**，本票两种口径都写在证据件里。

**范围外发现**（只记 S3）：
3. 只给 kg 之后本次会写进 `startKg`/`startDate` 的地方按同一原则办到了，但老库上「起点体重→当前」那类页面
   会继续沿用旧起点值——是否另开票未定（实施者已记）。
4. `calorie.view.goal-expiring` 的「今天」取机器时钟、不读 `CALORIE_TODAY`，判据不能钉死 `daysLeft`。
5. **本机编译入口今天已坏**：`D:\ilife\node_modules\.bin` 不存在，`npx tsc -b …` 会去网上抓一个同名假包，
   打印「This is not the tsc command you are looking for」**并以退出码 0 结束、根本不编译**。
   凡是以 `npx tsc` 为编译步的验收脚本，今天都可能「拿旧 dist 跑出假绿」。本轮两个变异窗口我改用
   `node node_modules/typescript/bin/tsc`（5.9.2），并每次都读 dist 原文自证编译真发生过。
   建议后续票把编译步换成显式路径，或在脚本里查假包横幅＋验 dist 形态。
   （实施者当时那条 `npx tsc` 确实编进了 dist——他们的 `BODY`／`check-dist` 读数显示 dist 形态随每场变异改变，
   所以他们的红绿读数不作废；这是今天的机器状态变化，不是他们的错。）
6. 同族旧用例有 **3 条红**（两次重跑仍红）：`cmd-write-40-persist` 的删除回执文案 2 条、
   `cmd-write-40` 的体重记录摘要空格 1 条。红行都不指向本票写集，且这两件测试文件本身没被改过
   （属并行会话在途改动或既存陈化）。**本票相关的旧断言全绿**：`cmd-write-40-persist:518-524` 四列落库、
   `cmd-write-40:240-258`（含 `kg:-1` 仍 exit 2）、`goal-receipt-253:247-251` 四列落库、`weight-receipt-337:204`。
7. `packages/skill-calorie/src/goal/receipt.ts` 正被并行会话改着（工作区有未提交改动）——我复核的页面装配含他人在途改动；
   摘要那一格本身来自本票的 `write.ts`，判据不受影响。
8. 复制载荷（按钮 `data-t`）里带 `id=1`／命令原文：那是全仓写命令同款的 M5 日志原文，仓内 452／401c 口径
   **有意**放过（不是屏幕上可见文本）。要收的话归 M5 日志格式那张票，不归本票。

## 六、机器读数清单（runId 均可从 `.scratch/locks/gate-runs.log` 核对）

| runId | 命令 | 退出码 |
|---|---|---|
| `765b22c8-207b-4c95-8fe0-24bd8123fc55` | `pwsh -NoProfile -File .scratch\t548-R\build-548r.ps1` | 0 |
| `47530a19-9d5c-4358-b512-07534270d8f8` | `node --test packages/skill-calorie/test/goal-weight-deadline-548.test.mjs` | 0（pass 4／fail 0） |
| `f89c16f2-0535-41ee-acab-7adc28cba5d1` | `node --test --test-concurrency=1 .scratch/t548-R/probe-548r.mjs` | 1（**我自己的断言写宽了**：拿原始 HTML 查内部词，把复制载荷算进去了；见下条改为按可见文本判） |
| `052d936a-e74e-4afb-a815-f4e8714591b3` | 同上（改按仓内可见文本口径） | 0（pass 5／fail 0） |
| `cf18d1bb-…`／`96245657-…` | `pwsh -NoProfile -File .scratch\t548-R\regress-548r.ps1` | 2／2（同族旧用例 3 条红，范围外，见 §5.6） |
| `e81837d9-cd16-424b-8276-32f6e79916f1` | `pwsh -NoProfile -File .scratch\t548-R\mutate-548r.ps1` | 1（窗口脚本在 MUT-1 之后被我自己的函数名与 `Get-History` 别名撞车打断；MUT-1 与还原读数有效） |
| `32abc7cb-7187-4460-9e5d-d592604a9ab6` | `pwsh -NoProfile -File .scratch\t548-R\mutate2-548r.ps1` | 1（我脚本里 dist 行匹配写错，两个自证标志误报；行为读数有效） |

两处「退出码 1」都是**复核者自己的窗口脚本毛病的自报**，不是被测代码的问题：第一窗是我把辅助函数命名成 `H`，
撞上 PowerShell 的 `Get-History` 别名；第二窗是我用 `continue;` 去匹配 dist 里那一行、匹配到了别的循环。
两处都只影响我脚本的自证标志，测试与命令的退出码读数另在日志里（`.scratch/t548-R/mut-*.log`、`sym2-*.log`）。
另外必须记一句：第二窗的 MUT-3 我最终是靠「改前 exit 4／还原后 exit 0」这对行为读数确认变异生效的，
不是我脚本里那个坏掉的行匹配。

## 七、未做项与下一手缺什么

1. `deadline:""` 两种口径要编排者裁一句（清空／等于没传）；裁「等于没传」的话，`iso()` 与第 ④ 条用例一起翻。
2. `kg` 只给一次时回执里的「写入字段」卡只列「体重目标(kg)」——本次不动，若产品上想把「保留未动」的那几列也说出来，
   是另一张票（会动到 `goal/receipt.ts`，本票写集外）。
3. 编译入口（§5.5）建议单独开一条小票，把全仓验收脚本的 `npx tsc` 换成显式路径——这是**下一手最该先补的**，
   否则后面的复核绿都可能绿在旧 dist 上。
4. 范围外那 3 条旧用例红、以及并行会话在 `goal/receipt.ts` 的在途改动，归各自归属票收口。
