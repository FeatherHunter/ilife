# t256 复核席证据（复核席 #256）

> 复核对象：提交 `abfe158`（新测试件 `packages/skill-calorie/test/goal-lock-256.test.mjs`，489 行）
> ＋ `303bab2`（证据件 `docs/skills/skill-calorie/t256-锁票-证据.md` 第二轮段）。
> 本席**不改源码、不改实施件**：只独立重跑判据、自设新探针、打分。全部门禁经 `node tooling/run-locked.mjs --ticket 256`。

## 一、判定：PASS（93 分；无 S1，1 处 S3 记账）

| 判据 | 独立重跑读数 | 与实施席对账 |
|---|---|---|
| ① 28 条真出口（exit0＋绝对路径＋字节如实＋完整文档四断言） | **28/28**（`T256-①`，含第 26 条 看目标推荐 70811 B 完整文档） | 一致 |
| ② 12 条看类不同形 | **8 档／同形对 6**（今日目标｜本周目标、今日目标｜饮水进度、本周目标｜饮水进度、营养进度｜完成率周、营养进度｜完成率月、完成率周｜完成率月）＋加宽 15 条 **11 档** | 一致 |
| ②-正对照 「本周」真被采纳 | 锚点 2026-09-09 → 「近 3 天目标进度」，与看今日目标**不同形** | 一致 |
| ③ 数值三组 | 宏量 `合计 1850 卡，宏量换算 1850 卡`／`diff ＋50 卡，一致`；速率 `2.6kg ÷ 115天 × 7 = 0.16 kg/周 ≈ 0.7 kg/月`；饮水 `推荐 2471 ml`（均本席自跑 CLI 复现） | 一致（三组本席另从源码算式独立重算：`receipt.ts:207`、`:242-248`、`nutritionGoal.ts:214-215`） |
| ④ 阻断四态 | D1 exit0 不编数／D2 exit4 不落盘／D3 exit0 缺项不编数／D4 exit2 不落盘 | 一致 |
| ⑤ 灵敏度 10 处 | `红 10 处`（键名 5＋宏量 2＋速率 1＋饮水 1＋形状 1），原样全绿 | 一致 |
| ⑥ 真库零接触 | `calorie_data.db` sha256 跑前跑后同值 `d40d58f5…a1083`（3072000 B） | 一致 |
| 件级 | `node --test goal-lock-256.test.mjs` → **tests 7／pass 7／fail 0**（两次，`8ab60138`、终跑） | 一致 |
| 邻接 | 14 件靶向 → **tests 62／pass 62／fail 0**（`22113bcb`，含 251／252／253／254／289／290／566／589／127／390／548／561／467） | 实施席记 57/57（其批未含两件），无红 |

## 二、独立重跑的四层自证

1. **只读先行**：`git status` 确认 `goal-lock-256.test.mjs` 与 `HEAD` 逐字相同（`git diff` 空），提交物未被在途改动污染；
   `packages/base-combos/src/present.ts`／`src/render/sportPortDocs.ts`／`src/render/trendMiscPortDocs.ts` 的在途改动**属别席**，与本票无关。
2. **真源重建**：本席不从件内表抄，直接从 `src/triggers/scene-06-goal.ts`（25 条）＋ `help-lookup.ts` 的 `WAKE_TABLE`（3 别名）
   重建 28 条命令与参数，再与件内 `WAKE_TABLE` 逐条对账（`.scratch/256-review/probe-contract.mjs`）。
   文档面 `docs/skills/skill-calorie/06-目标管理-工作流程.md` 28 行命令字串对账 `DOC-CHECK PASS`。
3. **重建可复现**：`tsc -b packages/skill-calorie --force`（`d9345b75`／`de52627e`／`0d4a8186`／`80be29c7`）→ **goal 族 dist 产物（280 行指纹）与实施席构建时逐字节相同**（`dist-after-rebuild` vs `dist-final`：`goal|cli|index` 行完全相同）；
   全量 1422 个 dist 件只有 `trendMiscPortDocs.*` 6 行差异，来源＝别席在途改那件后我的再次 `--force`，与本票判据面零交叉。
4. **冷门断言**（绕开实施件照抄的窄串）：本席按「完整文档形状」独立断言（doctype＋charset＋style＋ilife-page），
   拿真产物与「截成片段」两态比：原样 `all=true`、片段四项全 false → `COLD PASS`（片段必红）。

## 三、自设新探针（改坏一行看红立刻还原 ＋ 抽冷门断言）

源码级变异（本席自选行，`mutate-review.mjs`；每轮＝改一行 → `--force` 重建 → 跑件 → **立即逐文件还原**）：

| 变异 | 位置 | 红读数 | 还原 |
|---|---|---|---|
| A 季节档边界 `6..9 → 6..8` | `src/goal/nutritionGoal.ts:214` | `test-exit=1 fail=2`，hit＝`数值③「改饮水目标」 缺「推荐 2471 ml」`／`饮水「原样」缺「推荐 2471 ml」` | `sha-identical=true`、`git diff` 空 |
| B 宏量系数 `f*9 → f*8` | `src/goal/receipt.ts:207` | `test-exit=1 fail=3`，hit＝`数值①「改营养目标」 缺宏量合计「合计 1850 卡，宏量换算 1850 卡」`（连带 ⑥ 因 ① 中断而少跑临时库） | `sha-identical=true`、`git diff` 空 |
| C 测件自证 `28 → 27` | 件内 `assert.equal(r.size, 28,` | `test-exit=1 fail=1`，hit＝`28 条都要真跑（判据不许空跑），实测 28` | `sha-identical=true`、`git diff` 空 |

收尾 `FINAL-BUILD exit=0`；`git status` 只剩别席在途件，本席改动的两件源码与测件**零残留**。

## 四、缺陷清单（1 处 S3，均不代改）

- **S3-1（测试数据偏离声明真源，建议一手返修一行）**：#5「定体重目标(含起始日)」件内参数
  `startDate: '2026-09-01'`，而票面指定真源 `06-目标管理-工作流程.md` 第 23 行与 `scene-06-goal.ts` 的 `data_source`
  都是 `"startDate":"<日期>"`——按其口径 `<日期>`＝种子日/截止日口径系 `2026-12-31`（同件 `deadline` 用的正是该值）。
  实测差异**有观测后果**：两种取值产物逐字节不同（82330 B，页上读数分别印 `2026-09-01`／`2026-12-31`），
  故不是「参数被吞」。判据面不受影响（exit0＋完整文档＋四断言两值都成立，无假绿），但件头「逐字照抄该表」的声明与实际不符。
  修法：把该值改成 `DEADLINE` 或票面口径值，其余不动。
- 非缺陷记账：件内 #27／#28 写 `params: {}` 而真源不带 `--params` —— 实测两条命令带 `--params '{}'` 与不带
  **产物逐字节相同**（`config`／`status` 各 true），等价写法，不影响判据。

## 五、打分（100 分制）

判据覆盖 40（五组全在、逐条可跑、出处带行号）／独立复现 25（28 条真跑＋件 7/7＋邻接 62/62＋真源与文档双对账）／
自证强度 20（源码级变异两轮本席自选＋冷门形状断言＋还原 sha 逐字）／证据与边界 10（真库 sha 同值、临时库守卫、四态阻断）／
声明一致性 5（S3-1 扣 5）＝ **93**。无 S1（无假绿、无假红、无红线动作），S2 零，S3 一处 → **PASS**。

## 六、机器证据台账（持锁 `ticket=256`，`--` 后单命令，无嵌套）

| runId | 命令 | exit | 读数 |
|---|---|---|---|
| `36c593d7` | `tsc -b packages/skill-calorie --force` | 0 | 编译干净，dist 指纹落盘 |
| `8ab60138` | `node --test test/goal-lock-256.test.mjs` | 0 | 7/7；`T256-①…⑥` 全绿；真库 sha 同值 |
| `22113bcb` | 邻接 14 件靶向 | 0 | 62/62，0 红 |
| `d9345b75`／`de52627e`／`0d4a8186`／`80be29c7` | 变异 A／B／C 的 `--force` 重建 | 0 | 三次红后还原全部 sha 一致 |
| `054ed5af`／`97297a90`／`83b1217f` | 变异 A／B／C 的跑件 | 1／1／1 | 「改坏即红」三行读数（§三） |
| （终跑）`node --test …goal-lock-256…` | 0 | 7/7；`T256-① 28/28`、`T256-⑥` 真库 sha 同值 |

真库 `D:\2Study\StudyNotes\.db\calorie_data.db`：跑前跑后 sha256 同为 `d40d58f50e74e294a11e410b4d9e2142fcbcd8993328a4efb453f1a0d62a1083`（3072000 B），
本席全部实跑落 `mkdtemp` 临时库（探针 7 个＋件内 12 个），真库只读哈希。

## 七、没做完 ＋ 下一手

1. 不代改：S3-1 由**实施席**一手返修一行（`startDate` 取 `DEADLINE`），返修后重跑件 7/7 与 `--force` 建；
   本席复核结论对判据面**不因该一行而变**（等价路径，已实测两值都绿）。
2. 本席未跑全量测试（纪律禁 `pnpm test`），未碰 `docs/skills/skill-calorie/t256-锁票-证据.md`（实施席写集）。
3. 别席在途（`present.ts`／`sportPortDocs.ts`／`trendMiscPortDocs.ts`／四件测试）与本票判据面零交叉，未复核其内容。
