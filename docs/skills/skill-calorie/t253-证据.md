# t253 · 「5 条目标写命令的回执页从片段变完整文档」证据

票：#253「实现：目标 5 条写命令的回执页从片段变完整文档」。
做题顺序照票面：先写本清单，再动代码，再跑验收，再自证，最后持锁提交。

## 一、影响清单（一行一个文件 ＋ 一句话理由）

| 文件 | 动作 | 理由 |
| --- | --- | --- |
| `packages/skill-calorie/src/goal/receipt.ts` | 新建 | 目标管理这一族（5 条会改数据库的命令）的整页回执装配 ＋ 唯一端口 `goalReceiptDoc()`；形状照 `diet/receipt.ts`／`body/receipt.ts` 抄。 |
| `packages/skill-calorie/src/cli/write.ts` | 改两处 | 加一行 import（直引 `../goal/receipt.js`——`goal/index.ts` 明文不转出域内件）＋ 在 `:85` 那条 `??` 链尾续接一口 `goalReceiptDoc(...)`；链上既有五家的名字与顺序一字不动。 |
| `packages/skill-calorie/test/goal-receipt-253.test.mjs` | 新建 | 本票用例：5 条命令逐条真跑（临时种子库 ＋ 真 CLI）＋ 完整文档判据 ＋ 可见文本零内部词 ＋ 写入语义只读回读 ＋ 改前不许顶替。 |
| `docs/skills/skill-calorie/t253-证据.md` | 新建 | 本文件：影响清单 ＋ 自证读数（GATE-RUN／变异电池）。 |

### 不在写集、但会因本票口径变更而变红的既有件（须编排者另裁）

| 文件 | 行 | 当刻钉住的旧口径 |
| --- | --- | --- |
| `packages/skill-calorie/test/profile-doc-179.test.mjs` | 276–290 | 抽样正钉 `calorie.goal.set`／`calorie.goal.water`「仍是原回执片段」「不得以 `<!doctype html>` 起」。 |
| `packages/skill-calorie/test/delivery-83.test.mjs` | 324–329 | 正钉 `calorie.goal.set` 的产物族＝`receipt` 片段。 |

仓内先例 #269／#337／#365 都是照「口径变更」改这两处（把抽样换成尚未切整页的命令）；本票写集不含这两件，故**一行不改**，读数见 §三。

### 既有红（非本票所造，新件因此绕开）

`packages/skill-calorie/test/field-labels-449.test.mjs:233-253` ④ 今天已红（盘上四个域登记，断言只认一个）；
读数落 `.scratch/t253/t449-04-baseline.log`，runId=`d3a92bbc-b163-4ef8-947d-d4722c515307`，exit=1。
故新件的中文名走 `src/goal/precheck.ts` 那几张表，**不调** `registerFieldLabels`。

## 二、自证读数

### 1. 编译 ＋ 验收（持锁包装器）

```
GATE-RUN runId=115e90ef-6643-4390-b1c3-007c8bf988be
        cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
        摘要行 RESULT: ticket=253 runId=115e90ef-… waitedMs=0 exit=0
        退出码 0（日志 .scratch/t253/build2.log）

GATE-RUN runId=b50ed504-9ca2-42af-bdca-33d2d0ce2fa6
        cmd=node --test packages/skill-calorie/test/goal-receipt-253.test.mjs
        摘要行 ℹ tests 5 ／ ℹ pass 5 ／ ℹ fail 0（duration_ms 29219）
        退出码 0（日志 .scratch/t253/accept3.log）
```

本轮之前另有一条同命令的绿读数（runId=36ba197a-dee5-4ab4-86f3-a455689d6c81，exit=0，日志 `accept2.log`）；
两条之间只有 §四 第 3 条那次「源码被外部覆盖」的还原动作，故重跑一遍取干净读数。

**编译器不走 `npx`（本机踩坑，记下来）**：本机没有 `node_modules/.bin`，`npx tsc` 会落到 npm 上那个
同名占位包——它只打一行「This is not the tsc command you are looking for」并以 **0** 退出（静默不编译）。
故一律 `node node_modules/typescript/bin/tsc`（同 5.9.2）。

首轮验收（runId=b44dc724-a7de-4e5c-a99a-068b947804a9，exit=1）红的两条，是本票用例自己把口径写错了，
不是实现缺陷；改法如下，改完复跑即绿：

| 首轮红 | 复现读数 | 修法 |
| --- | --- | --- |
| ③「日志缺本次参数原文」 | 载荷是 HTML 属性值，命令原文里的单引号在 `data-t` 里是 `&#39;` | 断言只钉到 `--params`（不再要求那对单引号） |
| ④「现值块没读到…饮水」 | 本次写的就是饮水 2400 ⇒ 现值块该读 2400，断言误写成种子的 2000 | 断言改成 `2400 ml`（并保留反向对照：未被本次命令碰过的 1800 卡／68 kg 仍在） |

### 2. 变异自证（持锁窗口内做完并复原）

```
GATE-RUN runId=95a15fea-c570-48c7-96a2-b220e65e7f2a cmd=node .scratch/t253/mutate.mjs
        MUT M1 applied=1 dist_changed=1 fail=4 red_hit=1
              red=① 五条命令逐条真跑 ／ ② 可见文本零内部词 ／ ③ 段落在场 ／ ④ 改前不许拿新值顶替
        MUT M2 applied=1 dist_changed=1 fail=4 red_hit=1
              red=① ／ ② ／ ③ ／ ④（暂停那一条退回裸片段，四个用例都读到它）
        MUT M3 applied=1 dist_changed=1 fail=1 red_hit=1
              red=④ 改前不许拿新值顶替
        RESTORED src_sha_match=1 dist_clean=1 final_fail=0 red=(无)
        退出码 0（日志 .scratch/t253/mutate2.log；逐次日志 mut-M1.log／mut-M2.log／mut-M3.log／mut-restored.log）
```

三处按票面逐条落：

| 变异 | 改哪一行 | 期望红 | 实得红 |
| --- | --- | --- | --- |
| M1 | `src/cli/write.ts` 摘掉 `?? goalReceiptDoc(key, params, receipt, db)` | 用例 ①／② | ① ② ③ ④（四条红行见日志） |
| M2 | `src/goal/receipt.ts` 具名集里删掉 `'calorie.goal.pause',` | 用例 ① | ① ② ③ ④ |
| M3 | `src/goal/receipt.ts` 的「改前」那一格改成 `after`（拿新值顶替） | 用例 ④ | ④（只此一条） |

收尾：两件源码按 `*.orig.bak` 逐字节还原、重编、复跑全绿（`RESTORED … final_fail=0`）。
改前／改后各一份备件落 `.scratch/t253/`（读之前先看同目录 `README.md`：`*.orig.bak` 才是原版）。

**注意（首轮变异电池的假绿）**：第一次跑时三条变异全报 `dist_changed=0 fail=0`——原因是脚本里的编译走
`npx tsc`（占位包，静默不编译），于是用例跑的是没变过的产物。改成 `node node_modules/typescript/bin/tsc`
之后三条变异各自按预期变红（上表）。这一条也写进 `README.md`，免得下一手再踩。

### 3. 提交（持锁包装器；提交前先跑 `.scratch/t253/precommit.mjs`，9/9 绿）

```
GATE-RUN runId=e81089cc-977d-405a-a208-58101b610e24
        cmd=git add packages/skill-calorie/src/goal/receipt.ts packages/skill-calorie/src/cli/write.ts
            packages/skill-calorie/test/goal-receipt-253.test.mjs docs/skills/skill-calorie/t253-证据.md
        摘要行 RESULT: ticket=253 runId=e81089cc-… waitedMs=20021 exit=0
        退出码 0（日志 .scratch/t253/gitadd.log）

GATE-RUN runId=8940389e-3422-4933-b10a-ec05724fe65f
        cmd=git commit -m "功能(253): 目标 5 条写命令的回执页接整页装配（cli/write.ts 链上第六口）＋ goal/receipt.ts 整页 ＋ 用例 5 条全绿" -- <上列四件>
        摘要行 4 files changed, 648 insertions(+), 1 deletion(-)（三条 create mode ＋ write.ts 一行改动）
        退出码 0（日志 .scratch/t253/gitcommit.log）
        提交 sha=488cff56e07579b2fc5c7cdb42644d77b63a7911
```

提交前自证 9 条（`.scratch/t253/precommit.mjs`）：import 在／链尾那一口在／链上既有五家顺序未动／
`goal/receipt.ts` 对外只有 `goalReceiptDoc` 一个名字／页签名与眉标在／具名集五条齐／用例在／证据件在／
行数 236 ≤ 350 告警线。

## 三、既有件读数（不在写集、须编排者另裁）

```
GATE-RUN runId=299ef384-8517-4619-a38b-28664080b21d
        cmd=node --test packages/skill-calorie/test/profile-doc-179.test.mjs packages/skill-calorie/test/delivery-83.test.mjs
        摘要行 ℹ tests 23 ／ ℹ pass 17 ／ ℹ fail 6
        退出码 1（日志 .scratch/t253/kouchang.log）
```

六条红分成两组：

**本票口径变更所致（两条，正是 §一 预判的那两处）**

| 红点 | 失败行 |
| --- | --- |
| `#179 其余会改数据库的命令仍是原回执片段（没被一刀切换页）` | `calorie.goal.set 的产物不再是原回执片段（本条钉住的命令尚未切整页）：<!doctype html>` |
| `#83 写键同样有 delivery（receipt 产物族）` | `未切整页的命令仍是片段族` |

**与目标域无关的既有红（四条，各有别的在场改动指着，非本票所造）**

| 红点 | 失败行 | 指到谁 |
| --- | --- | --- |
| `#337 场景 03 四条体重写命令已是完整文档（三变体字段齐）` | `weight.log 缺：体重 · 写后回执` | 体重域（`src/weight/receipt.ts` 件头已被并行的在场改动改写，`DOC_TITLE` 已不在该件） |
| `#365 身体细节四条写命令已是完整文档` | `记体脂 缺场景 08 眉标` | 身体域（本票链上第五口在目标域之前，「`??` 短路」保证它的产物不变） |
| `#239 四张页接上「复制日志」` | `预检确认页按钮不对` | 档案域预检确认页 |
| `#247 场景 07 五张页的复制数据是「三格式三选一」菜单` | `预检确认页 菜单缺用途提示：粘贴给 AI / 自己看` | 同上 |

判据：本票那一口排在 `??` 链**最后一个**，且只对 5 个目标键返回字符串——链上在前的那几家的产物，
字节与调用次数都不变（用例①里体重／身体／档案域不在场，另由上面四条红点反向印证：它们的红行
说的都是各自域里的东西）。

## 四、未做项与下一手接哪里

1. **`profile-doc-179.test.mjs:276-290` 与 `delivery-83.test.mjs:324-329` 两处口径变更**（§三那两条红）：
   仓内先例 #269／#337／#365 都是照「口径变更」把抽样换成尚未切整页的命令；这两件不在本票写集，
   **本票一行未改**。下一手：把 179 的抽样换成还没切整页的命令（或按 #269 的写法把目标域挪出样本），
   把 83 的 `calorie.goal.set` 换成还没切整页的命令，两处各补一句「口径变更」注释。
2. **「改前」有值的只有饮水那一条**（写口只回报变更字段名）：本票按 `diet/receipt.ts:139-149` 的先例
   接受「`—` ＋ 表下口径行」，**不拿新值顶替**。要它出真值，须改 `src/goal/write.ts`（不在本票写集）
   让写口回报旧值，再开一票。
3. **工作区事件（不是本票所造，但要报）**：20:19 变异窗口里 `src/cli/write.ts` 被外部按
   `.scratch/t253/M1.mut.bak` 的内容覆盖回去（连 mtime 一起，读数 5641 字符版 / sha `0348314bdd89`），
   编译产物随之变成「链尾那一口没了」的版本；已按 `.scratch/t253/write.ts.orig.bak` 还原并复跑取绿
   （§二 第 1 条的 runId=b50ed504）。**下一手提交前请再核一次这两件的 sha**。本目录已加 `README.md` 说明
   哪一份是原版、哪一份是故意改坏的。
4. 字段中文名在 `src/goal/` 内出现第二份字面表（`precheck.ts:35-41,60-74` 那张表没导出，且本票写集
   不含该件）⇒ 若要把中文名收成一个定义地，须另开一票把名字表提为目标域两件共用的域内件。

