# #460 · 对照目标页脚注口径（天数冒充条数）实施证据

> 一票一事：只动三处（`sportDocs.ts` 三段等行＋`452.test.mjs` 两处钉子同步＋本件）。
> 总工程师书面裁定（#460 评论 5691991833）：走乙（脚注改称窗口天数）＋联动 452 测试同步（一次性跨界授权）。
> 草稿（`.scratch/t460/`）不入仓；`AGENTS.md`／`resultDocs.ts` 等他席件一字未碰。

## 一、现象与选型

对照目标页来源脚注第三格印「记录数／共 N 条」，N 传的是 `v.days`（天数），而取数面
`ExerciseGoalView`（`planPlate.ts:299-309`）根本没有条数字段。同日 2 条（慢跑 320 卡＋卧推
150 卡，2026-09-14）→ 脚注印「共 1 条」，病状坐实（基线 `GOAL count=1` 实有 2 条）。

- **乙（本票）**：第三格改诚实措辞「窗口天数／共 N 天」，全页不再出现「条」；两页复制载荷头换人话页名。
- **甲（未走，需拆票）**：取数面增条数字段要动 `planPlate.ts` 形状，超出写集且无库可查，编数即违规。

## 二、改动（三段等行，LF 434→434）

1. `footFacts(source,start,end,days)`：`{k:'记录数',v:'共 N 条'}` → `{k:'窗口天数',v:'共 N 天'}`，
   形参 `count` 改名 `days`（调用处 `v.days` 不动），注释记 `#460 诚实口径`。
2. `summaryEnvelope.key`：`'calorie.view.exercise'` → `'运动汇总'`。
3. `goalEnvelope.key`：`'calorie.view.exercise-goal'` → `'运动目标'`。
   复制日志机器面照旧；stdout 信封键走命令层交付件，不影响 #267-G1。

## 三、452 同步说明（跨界一次性）

`packages/skill-calorie/test/exercise-summary-goal-fusion-452.test.mjs` 仅两处：

- `:171-176` `sourceFootnote()` 正则 `记录数／共 (\d+) 条` → `窗口天数／共 (\d+) 天`，
  断言文案同步改「窗口天数」，返回 `{text,count}` 形状不动（`count` 现承载天数，调用 `r.count` 不动）。
- `:383` `r.file.includes('共 '+r.count+' 条')` → `('共 '+r.count+' 天')`，
  文案 `条数口径／记录数` → `天数口径／天数`，仍是同一 `assert.ok(includes)` 结构。

判据结构、覆盖面、冻结参数一字未动；注释 `:168-170` 旧措辞留原样（写集外不碰）。

## 四、读数（终态，改后重编）

- `node node_modules/typescript/bin/tsc -b packages/skill-calorie`：exit 0。
- T460（`.scratch/t460/t460-footnote-check.mjs`）：`T460-RESULT PASS 全过` 12/12
 （今日／本周各 5＋汇总头 1＋装配层 7 天窗 1；新跑 `check-new.log`）。
- #452：`pass 9 / fail 0` 9/9（`after-452-new.log`；`GOAL count=1` 现为天数，与脚注 `共 1 天` 对齐）。
- #267：`T267-RESULT: 8/8 通过`（`after-267-new.log`）。
- #523：`pass 13 / fail 0` 13/13（`after-523-new.log`）。
- 变异：`mutA-red.log` 把脚注改回「记录数／共 N 条」→ `T460-RESULT FAIL` 红 5 项；
  还原重编 → `check-new.log PASS`。载荷头改回命令键同理（基线 `mutB-red.log` 红 2 项）。

## 五、超线与遗留

已超线，需要根据规则进行重构。`sportDocs.ts` LF=434（改前改后同值，台账 `434` 对得上）。
超因照台账：两页装配同处一件。本次先不拆：写集只许措辞与载荷头；
拆法照旧（按页切 `sportSummaryDocs.ts`／`sportGoalDocs.ts`＋共件 `sportPageParts.ts`），待收口票。

- 运动域 `footFacts(` 六处调用：移植五页传真条数、记录族传 `sessions`，无第二处天数当条数。
- 别族同形嫌疑未动（`weight/volatilityDoc.ts` 把 `warnDays` 印成「共 N 条」，归 #505，另开票＋回写 #156）。
- 提交：`33498e9`（sportDocs）＋`adad159`（452 同步）＋本件；未 push。
