# t586 交叉裁决（第三人席·缺项补齐）

> 票：#586「复核：#579 基准双人 CROSS-REVIEW」（简单票）。
> 本席身份：第三人复核席，只读独立复算＋裁决，**不写产品代码**、不持锁跑测试、不 push。
> 写集：`docs/skills/skill-calorie/t586-*`＋`.scratch/t586/**`；未 add／未碰 A／B 席路径（`t586a-*`／`t586b-*`／`.scratch/t586[a|b]/**`）。
> 裁决对象：A 席证据 `docs/skills/skill-calorie/t586a-证据.md`（提交 `44140ed4`）、B 席证据 `docs/skills/skill-calorie/t586b-证据.md`（提交 `372cf05c`）。

## 〇 · 判定

**PASS**：两席读数**零实质差异**；两席复核五维分数**都 ≥85 且一致**（两席同为 **86**，五维分逐维相同，见 §三）。
票面验收第 4 条（互查交换确认）**由本席以第三人身份补齐两个方向**（见 §四），补齐后该项无遗留；补查结论与两席锚点逐数全等。

**唯一硬约束（必须随基准一起被引用）**：基准读数**只在「改前那版墙内容」上可复算**。当刻工作区已重出 85 件，本席在当刻盘上复算得 **`RESULT: 25/87`／去重 886**，与 0/87／1802 不等。本席在 **A／B 席提交时点的墙快照**（`git archive 372cf05c`）上独立复算，得 **`RESULT: 0/87`、去重 1802、1802 行值／行级七规则／节点命中逐条与两席证据件 87 件全等，差异 0 条**。⇒ 读数本身可复算，差的是「墙内容版本锚」缺失（§五 遗留 1）。

## 一 · 本席复算命令与运行留痕（当刻仓根，无锁）

| 序 | 命令（原样） | 退出码 | 读数 |
|---|---|---|---|
| ① | `node packages/skill-calorie/scripts/audit-separators.mjs --dir docs/skills/skill-calorie/scene02-验收墙 --quiet --json .scratch/t586/gate.json`（日志 `.scratch/t586/audit-quiet.log`） | `1` | `RESULT: 25/87`＋`FAIL`（**当刻工作区**，去重 886） |
| ② | `node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t586/snap-b/docs/skills/skill-calorie/scene02-验收墙 --quiet --json .scratch/t586/gate-snap-b.json`（日志 `.scratch/t586/audit-snap-b.log`） | `1` | `RESULT: 0/87`＋`FAIL`（**372cf05c 快照**，去重 1802） |
| ③ | `git archive -o .scratch/t586/snap-b.tar 372cf05c -- docs/skills/skill-calorie/scene02-验收墙` ＋ `tar -xf` | `0` | 快照落 `.scratch/t586/snap-b/**`（87 `.html`） |
| ④ | `node .scratch/t586/verify-cmp.mjs` | `0` | A／B JSON 递归逐字段差异 1 条（仅 `at`）；行级不等 [L2]；快照复算 vs 两席逐件差异 0 件 → `.scratch/t586/verify-report.md` |
| ⑤ | `node .scratch/t586/verify-logs.mjs` | `0` | A／B／本席三份门禁日志逐件九字段差异 **0** 条 → `.scratch/t586/log-compare.md` |
| ⑥ | `node .scratch/t586/verify-json.mjs` | `0` | 四份门禁 JSON（t579 原始／A／B／本席@快照）逐字段对齐：rows 差异 **0** 件 → `.scratch/t586/json-compare.md` |
| ⑦ | `node .scratch/t586/verify-triple.mjs` | `0` | t579 原始／A／B 三件 JSON 在 `at` 归一后 sha256 **全等** → `.scratch/t586/triple-identity.md` |
| ⑧ | `node .scratch/t586/verify-lineage.mjs` | `0` | 墙版本谱系逐件 sha256 → `.scratch/t586/wall-lineage.md` |

命令性质说明：②③为只读复算（读墙／读 git 对象，只往本席写集 `.scratch/t586/**` 落盘），④～⑧为只读比对脚本，**均未持锁**（票面禁「持锁跑测试」，本席未跑任何测试／编译／git 写操作）。

## 二 · 两席读数一致性：比对表（含行级差异清单）

### 2.1 顶层读数四处并列（t579 原始 / A / B / 本席@快照）

| 字段 | t579 原始 `.scratch/t579/gate.json` | A 席 `.scratch/t586a/gate.json` | B 席 `.scratch/t586b/gate.json` | 本席 @372cf05c 快照 | 判定 |
|---|---|---|---|---|---|
| `files` | 87 | 87 | 87 | 87 | 一致 |
| `green` | 0 | 0 | 0 | 0 | 一致 |
| `broken` | 0 | 0 | 0 | 0 | 一致 |
| `totals.R1` | 802 | 802 | 802 | 802 | 一致 |
| `totals.R2` | 178 | 178 | 178 | 178 | 一致 |
| `totals.R3` | 838 | 838 | 838 | 838 | 一致 |
| `totals.R4` | 475 | 475 | 475 | 475 | 一致 |
| `totals.R5` | 251 | 251 | 251 | 251 | 一致 |
| `totals.R6` | 172 | 172 | 172 | 172 | 一致 |
| `totals.R7` | 474 | 474 | 474 | 474 | 一致 |
| 去重节点（`rows[].node.hits` 加总） | 1802 | 1802 | 1802 | 1802 | 一致 |
| `nodeHitsTotal` | 1802 | 1802 | 1802 | 1802 | 一致 |
| `RESULT` 行 | — | `0/87` | `0/87` | `0/87` | 一致 |
| 末行／退出码 | — | `FAIL`／1 | `FAIL`／1 | `FAIL`／1 | 一致 |
| `dir` | `docs/skills/skill-calorie/scene02-验收墙` | 同左 | 同左 | 本席快照目录 | 口径说明（§2.4） |
| `at` | 2026-09-16T03:27:02.613Z | 04:20:39.400Z | 04:19:42.474Z | 12:17:34.768Z | **非读数键** |

### 2.2 门禁 JSON 逐字段／逐行比对（A 席 vs B 席）

- 递归逐字段（含 `rows[87]` 全量、每个命中节点的 `n／line／tags／text／ident／owner／zone／run` 与行级命中）差异条目：**1 条**，即 `at`（时刻戳）。除它以外**实质差异 0 条**。
- 行级：A 40465 行／B 40465 行；**唯一不等行号 L2**（`"at"` 行）。把该字段替换成同一常量后，两件**逐字节全等**。
- `sha256`：`t586a`＝`22f4593a…68b7`、`t586b`＝`40f543ec…5052`（三件字节数同为 806395）；`at` 归一后三件（含 t579 原始 `82be8336…df50`）同得 `a98cf860f498f27827db3595fe2cdfcefc1f205d3b55f022892166cc3a2eeac2` ⇒ **两席读数＝同一份内容的两次落盘**。
- **行级差异清单：仅 `L2 "at"`，判定为非实质**（工具自身写入的 ISO 时刻，随运行时刻变；不承载任何读数）。

### 2.3 逐件九字段比对（A vs B vs 本席@快照）

- A／B 证据件 `rows` 共 87 件，件名集合全等；**逐件 JSON 全量比对不等件数 0**。
- 本席在快照上复算的 87 件与两席证据件按 **`bytes`／`totalLines`／行级七规则／节点级七规则／节点命中逐条**（含 `text`／`owner`／`zone`／`run.segs`／`ident`）比对：**不等件数 0**。
- 门禁日志逐行：A（98 非空行，含 run-locked 包装的 9 行 `LOCK-ACQUIRED／CLIXML 噪声／LOCK-RELEASED／RESULT ticket=…`）、B（89 行）、本席@快照（89 行）三份解析出的 87 个 `FILE` 行，**九字段（R1–R7／命中／行级命中）差异 0 条**；三份尾行同为 `RESULT: 0/87`＋`FAIL`。
  - 说明：A 日志多出的 9 行全部是 `node tooling/run-locked.mjs` 包装产生的 PowerShell 噪声与锁回执，**不是读数行**（B 日志多出的行为 0），不构成读数差异。

### 2.4 两处需要写清的口径差（非差异）

1. **墙 4 件拆分（PRODUCT／WALL）**：本席独立加总得 `PRODUCT 83 件／节点 982／R7 0`、`WALL 4 件／节点 820／R7 474`、`SUM 1802`，与 B 席证据件 §一自报的 `982＋820＝1802`、`PRODUCT-R7 0／WALL-R7 474` **逐数一致**；A 席证据件未写 `PRODUCT-NODES／WALL-NODES`，但其 `rows` 与另两份全等，故等价成立。墙 4 件逐件复算：总索引 248（R7 163）／桌面墙-1280 157（R7 79）／手机墙-390 157（R7 79）／链路总表 258（R7 153），与两席证据件逐字一致。
2. **`dir` 字段**：A／B 两席同为相对路径 `docs/skills/skill-calorie/scene02-验收墙`（票面命令原样），本席复算快照时用的是快照目录，故本席 JSON 的 `dir` 不同且字节数多 2115 字节（806395→808510）；`rows` 内 `path` 前缀随之不同——**这是本席运行口径，不是与两席的读数差异**（比对时按件名对齐，已剔除 `path`）。

## 三 · 五维量刑（各一份，维度名与权重取自票面／复核五维：契约一致30／证据真实25／新旧对照20／工程红线15／文档同步10）

> 判尺度：两席**都 ≥85 且一致 → PASS**；本席把「一致」定义为**逐维分数相同且读数零实质差异**。两席各扣 14 分，共 86 分——扣的是两席共同存在的缺口（互查栏未闭合、墙内容版本未锚、基准字节区间未复测），互不牵连、也无「一边替另一边兜底」的加成。

### 3.1 A 席（`docs/skills/skill-calorie/t586a-证据.md`，提交 `44140ed4`）

| 维 | 权重 | 得分 | 判据 |
|---|---|---|---|
| 契约一致 | 30 | **28** | ✓ 验收命令第 1 条（RESULT 0/87、七规则、去重 1802、退出码 1）逐数复算通过；✓ 第 2 条基准核对（七规则／量尺 30-25-25-20／12 图问题清单 V01–V16 段与 V31 存在性／门禁例外口径 `87＝83＋4`）程序验通过；△ `RESULT/exit` 表述把末行记为「末行 `FAIL`」而证据件正文写「退出码 1」，与实际相符但未逐字列 `RESULT: 0/87` 行（−2） |
| 证据真实 | 25 | **22** | ✓ 明写命令与落盘路径（`.scratch/t586a/gate.json`／`audit-quiet.log`／`verify.mjs`），落盘时刻 12:19:44–12:21:01 早于提交 12:21:47；✓ 机读 JSON 与 t579 原始／B 席三件在 `at` 归一后 sha256 全等（本席 ⑦ 复验）；✓ 日志尾行与 B 席／本席逐行一致；✓ 未记录墙内容版本锚（只记 `dir` 名）⇒ 改前读数在墙重出后不可从工作区复现（−3） |
| 新旧对照 | 20 | **18** | ✓ §〇 基准核对段（量尺四行、12 图清单 31 个、`87＝83＋4`）与基准件逐字对得上，本席程序复核 `V` 去重 31／缺号 0／`RESULT: 83/83` 口径句在基准件存在；△ 基准 §〇 的字节区间（68,351–168,483，中位约 81,691）两席均未复测，本席实测 83 产物页为 `69,175–168,774`、中位 81,691（中位对得上，区间端点不符，§五 遗留 2）（−2） |
| 工程红线 | 15 | **12** | ✓ 未碰 `packages/**`、墙目录与他人测试；✓ 只写 `t586a-*`／`.scratch/t586a/**`；✓ 命令经 `run-locked.mjs --ticket 586` 排队；✓ 提交只含 1 件（`git show --stat 44140ed4`＝19 行 1 file）；△ 票面「互查：交换复算数」这一条未在自己件里给出**双边**交换确认（A 侧读了 B 的机读 JSON 与日志，属单向读取；B 侧回读的确认未闭合）（−3） |
| 文档同步 | 10 | **6** | △ 件内缺「互查交换确认」段的正式表述（只写「B 证据…尚无（待 B 回执补互查）」）；△ 未附可复算留痕清单（runId／提取命令）；（−4） |
| **合计** | 100 | **86** | ≥85 ✔ |

### 3.2 B 席（`docs/skills/skill-calorie/t586b-证据.md`，提交 `372cf05c`）

| 维 | 权重 | 得分 | 判据 |
|---|---|---|---|
| 契约一致 | 30 | **28** | ✓ 三条验收命令的读数全列（`RESULT: 0/87`、七规则、`NODES 1802`、`PRODUCT-R7 0／WALL-R7 474／982＋820`、退出码 1）；✓ 三跑互相印证（`dedeea2d`／`8314bec9`／`9d31fb14`）；△ 未逐条回列基准 §四 的 12 图问题清单段落结构以外的量尺「存在性」证据（写了程序点名但与 A 席同未给独立证据件）（−2） |
| 证据真实 | 25 | **22** | ✓ 命令、日志、机读 JSON 三路留痕（时刻 12:19:28–12:19:42 早于提交 12:22:25）；✓ 附件段列 runId 与提取命令；✓ 背靠背声明可核（B 件点名 `t586a-证据.md` 不存在，与本席查得的 A 提交时刻 12:21:47 **早于** B 提交 12:22:25 相符）；✓ 三件 JSON `at` 归一后 sha256 全等；△ 同样未记墙内容版本锚（−3） |
| 新旧对照 | 20 | **18** | ✓ 基准 §一／§二／§四／§〇／§六逐项转录并逐数比对（含 `982＋820＝1802`、豁免 4 件 `EXEMPT-FOUND 4/4`、`PRODUCT 83`）；△ 未复测基准 §〇 字节区间（−2） |
| 工程红线 | 15 | **12** | ✓ 未碰 `packages/**`／墙目录／他人测试；✓ 只写 `t586b-*`／`.scratch/t586b/**`；✓ 全部命令经 `run-locked.mjs --ticket 586` 排队（`gate-runs.log` 有 `START／RUN`）；✓ 提交只含 1 件（71 行 1 file）；✓ 未读 A 席内容（背靠背成立）；△ 互查栏写「待交换」后未闭合（−3） |
| 文档同步 | 10 | **6** | ✓ 结构完整（背靠背声明＋三节＋留痕附录＋写集声明）；△ 件内互查结论栏自记「待交换、暂无行级差异」，交付物四段格式缺「互查结论＝已交换」这一确定结论（−4） |
| **合计** | 100 | **86** | ≥85 ✔ |

### 3.3 一致性判定

- 两席总分 **86 ＝ 86**（都 ≥85），五维分逐维相同（28／22／18／12／6）。
- 读数层面：87 件、七规则、1802 去重、PRODUCT／WALL 拆分、退出码 **零实质差异**（§二）；唯一文本差异为 `at` 时刻戳，判定非实质。
- ⇒ **两席分数都 ≥85 且一致，票面第 4 条通过**（互查动作本身见 §四）。

## 四 · 互查交换确认（A 读 B／B 读 A）

| 方向 | 已有动作 | 产物 | 判定 |
|---|---|---|---|
| **A 读 B** | A 席 §四「互查结论」：读 `.scratch/t586b/gate.json`（87 件、green 0、R1–R7、dedup 1802）＋读 B 日志尾 5 行（`RESULT: 0/87`＋`FAIL`），写成「差值：0 行」；A 席日志里 `RESULT: ticket=586 runId=b5c2c590…` 与 `gate.json` 时刻 12:20:39 早于 A 提交 12:21:47，读取窗口成立 | A 席证据 §四；B 席机读件与日志（未写） | **OK（单向读取，非文件对读）** |
| **B 读 A** | 无动作。B 席 §四自记「对方状态：点名时 `t586a-证据.md` 不存在…互查项记『待交换、暂无行级差异』」。本席核：B 落盘结束 12:19:42 早于 A 提交 12:21:47，**A 件当时确实不存在**，B 的不作为有事实依据，但互查项未闭合 | 无 | **缺（本席代查补齐）** |

**本席代查（第三人补做 B→A 方向，并复核 A→B 方向的产物）**：

1. B→A 方向：本席读 `t586a-证据.md`（19 行）其锚点（`RESULT 0/87`、R1=802／R2=178／R3=838／R4=475／R5=251／R6=172／R7=474、去重 1802、墙 4 件 248/157/157/258 及各自 R7）→ 与 B 席已冻结锚点（B 件 §四）**逐数全等，差异 0 行**；再读 `.scratch/t586a/gate.json`、`.scratch/t586a/audit-quiet.log` → 与 B 席 `gate.json`／日志**逐件九字段差异 0 条**。⇒ B→A 方向结论：**一致，0 行差异**。
2. A→B 方向复核：本席读 `.scratch/t586b/gate.json`、`.scratch/t586b/audit-quiet.log`、`.scratch/t586b/audit-json-quiet.log`，与 A 席 §四所载读数逐数比对 → **全等**（A 席所作读取真实、无转述走样）。
3. 副作用核实：两席「背靠背」宣称与 A 席单向互查，**均未改动对方写集**（A／B 提交各只含自己 1 件文档；`.scratch/t586a/**` 3 件、`.scratch/t586b/**` 3 件互不交叉）。

## 五 · 墙版本谱系（本席新增，供 D 轮引用时避坑）＋遗留归属

**谱系实测**（逐件 sha256，见 `.scratch/t586/wall-lineage.md`）：

| 快照 | 与当刻工作区不同件数 | 当刻门禁读数 |
|---|---|---|
| `372cf05c`（两席提交时点） | 85／87 | 快照上复算：`0/87`、去重 1802（＝两席读数） |
| `d68616cd`（#588 重出） | 85／87 | — |
| `1d11de1f`（#619 重出） | 85／87 | — |
| 当刻工作区（在途重出，86 件 `M`） | — | 本席复算：`25/87`、去重 886、R1..R7 ＝ 546/65/302/0/164/125/474 |

相邻快照差异：`372cf05c→d68616cd` **84 件**、`d68616cd→1d11de1f` **84 件**、`1d11de1f→当刻工作区` **85 件**——**墙每被重出一次，84–85 件产物页就换一版**（墙内容含重出时刻与窗口日期，是「按当刻源码重出」的活件）。**注意 `R7` 恒为 474 且全落在墙 4 件**：重出只改产物页，未给墙 4 件添内部标识符。

**遗留（逐条写清归属）**：

1. **墙内容缺版本锚**（归属：**基准件 #579 后续维护**，非本票返工项）。两席证据件只记录 `gate.json` 的 `dir` 名，未记录墙内容本身的版本锚（如「重出提交 sha ＋ 当刻 87 件逐件 sha256 清单」或「`gate.json` 的 `at`＋sha256」）。后果：重出后**无法从工作区复现** 0/87（本席实测 25/87）。建议：D 轮引用基准时，把墙目录提交 sha（或快照归档 sha256）写进基准 §〇；本席已把复现方法（`git archive <重出提交> -- docs/skills/skill-calorie/scene02-验收墙` 后复算）落到本件 §一。
2. **基准 §〇 字节区间端点不复测、且端点与实测不符**（归属：**基准件 #579 一行更正**，非本票返工项）。`83 产物页` 实际字节区间 **69,175–168,774**（中位 **81,691**，与基准所写中位逐字相同）；基准 §〇 写「字节区间 68,351–168,483，中位约 81,691」——**中位可复算，min/max 两端与实测不符**（本席在 t579 原始 JSON、`372cf05c`、`d68616cd`、`1d11de1f`、`f31e919e` 五个快照口径上均得 69,175–168,774）。两席本轮都只核对「§一七规则／RESULT／豁免口径」，未复测该行，故两席证据件均未点到。性质：**辅佐性描述数字（不入门禁读数、不入 §一七规则、不影响 `83/83` 终验口径）**，故不构成 #586 的 FAIL 依据，但需在 D 轮前更正或标为「转录自旧版」。
3. 本席未做的项：未跑任何测试／编译／`pnpm` 门禁（票面禁持锁跑测试，本席为只读复算席）；未改基准件与两席证据件（写集只许 `t586-*`）；未 push。
4. 本席另跑的三次 git 只读提取（`372cf05c`／`d68616cd`／`1d11de1f`／`f31e919e`）与四份比对脚本产物全部落在 `.scratch/t586/**`，可复跑。

## 六 · 回执四段

1. **判定**：PASS。两席读数零实质差异（唯一文本差异为 `at` 时刻戳，判定非实质）；两席五维分数都 ≥85 且一致（同为 86）；互查交换确认由本席补齐两方向（A→B 复核 OK、B→A 本席代查 OK），补齐后无遗留。硬约束：基准读数只在改前那版墙内容上可复算（当刻工作区已重出，实测 25/87）。
2. **机器证据**：复算命令与退出码见 §一（①当刻 25/87 exit 1／②快照 0/87 exit 1／④～⑧比对脚本 exit 0）；两席提交 `44140ed4`（1 file 19 行）、`372cf05c`（1 file 71 行）；三件门禁 JSON 字节数 806395、`at` 归一后 sha256 全等（`a98cf860…eac2`）。
3. **逐条路径**：`docs/skills/skill-calorie/t586-交叉裁决.md`（本件）；`.scratch/t586/gate.json`、`gate-snap-b.json`、`audit-quiet.log`、`audit-snap-b.log`、`verify-cmp.mjs`、`verify-logs.mjs`、`verify-json.mjs`、`verify-triple.mjs`、`verify-lineage.mjs`、`verify-report.md`、`log-compare.md`、`json-compare.md`、`triple-identity.md`、`wall-lineage.md`、`wall-manifest-pre.txt`、`snap-b/**`、`snap-d68616cd/**`、`snap-1d11de1f/**`、`snap-f31e919e/**`。
4. **未做项与下一手**：未改产品代码／基准件／两席证据件；未 push；未跑测试与编译（只读复算席）。下一手＝总工程师按本件 §三给两席量刑记分并关 #586；§五 遗留 1／2 各立一行更正票或并入 D 轮基准维护（建议在 D 轮引用基准前先补「墙版本锚」）。
