# t370 搬迁证据（纯搬迁：主页装配与视图模型按子功能归位）

- 票：#370（地图 #162 子票）；执行席认领（assignee 已置）；基线 HEAD `96bbfd1`。
- 结论先行：今日总览族 5 件产物搬迁前后逐件 sha256 全等（5/5 ALL-MATCH），无 `<!doctype`，
  `render-t8` 7/7 绿。`pnpm build` 与 `gen:check` 当刻红，红因均在伙伴在途写集内（见 §5），本票零新增红。

## ① 基线与写集

- 开工：`git pull` 已是最新；`git status` 干净；`.scratch/locks/gate.lock` 不存在（锁空）。
- 票面广播（#370 评论 5658792194）：文件级写集
  `src/home/homeDocs.ts`（新建）、`src/home/today.ts`、`src/render/html.ts`、
  `src/render/index.ts`（转出口径一行，编译必须）、`.scratch/t370/`（草稿）、`docs/skills/skill-calorie/t370-*.md`。
- 老行为不许动清单：`src/home/routes.ts`、`src/home/commands.ts` 一行未改；
  `src/weight/`、`src/diet/`、`src/exercise/`、`src/goal/` 未碰（§⑥实测）；
  不做整页化（指纹锁 `doctype=false`，见 §3）。
- 并发实况：窗内 #332（weight）、场景 02 饮食席、#354（告警线台账）、#284/#341 等并行在飞；
  `src/home` 依赖锥（home/render/shared/fetch/analysis/triggers/cli-keys）在 `--before` 前与 HEAD 零差异；
  `renderHomeHtml` 全仓仅三处引用（`home/today.ts`、`render/html.ts`、`render/index.ts`），无第二个调用方，无需转票。

## ② 做了什么（影响清单→交付对账，偏差零）

| 件 | 动作 | LF（节点口径） |
|---|---|---|
| `src/home/homeDocs.ts` | 新建：`renderHomeHtml` 自 `render/html.ts` 逐字迁入（5 段与原文逐字节一致，脚本实测 IDENTICAL×5）；仍出片段 | 63（≤350，不报警） |
| `src/home/today.ts` | 改线：`renderHomeHtml` 改从 `./homeDocs.js` 取；`renderGoalProgressHtml` 仍走 `../render/html.js` | 110（≤350） |
| `src/render/html.ts` | 只删主页段：删 `renderHomeHtml`（18 行）＋ `HomeData` 导入 1 行＋头注释 1 行，共 −19 行；其余 30 余处装配一行未动 | 680→661（超线，见第四步） |
| `src/render/index.ts` | 转出口径：`renderHomeHtml` 改从 `../home/homeDocs.js` 转出（沿用本文件 `buildHomeData` 转出先例）；调用方导入面不变 | 116（≤350） |
| `.scratch/t370/fingerprint-home.mjs` | 新建：指纹脚本（照抄 139 真出口口径＋t81 种子库＋252 摘要行），另有单窗运行器 `run-window.mjs`（发射→烟雾→指纹，防夹写） | 草稿，不计数 |
| 本证据件 | `docs/skills/skill-calorie/t370-搬迁-证据.md` | 证据，不计数 |

- `HomeData` 定义唯一处仍是 `src/home/home.ts:19`（本票前已在能力目录内，无需再搬）。
- 第四步 · 超线报警：`src/render/html.ts` **已超线，需要根据规则进行重构。**
  理由：661 行（>350），超因是 30 余个场景装配同处一处（存量，HEAD 即 680 行）。
  本次先不拆：本票只删主页段（−19 行）；整件拆分归 #371 整页化及后续页面票，另台账漏挂号已单独立票 #388。

## ③ 指纹判据（5 件逐件 sha256，真出口 CLI＋固定种子库）

- 基线 `.scratch/t370/fingerprint-home.json`（`--before`，GATE-RUN runId=`4c1a28a4-721b-4d08-986e-6cb90e4963b1` exit=0）：

| 唤醒词 | sha256 | bytes | doctype |
|---|---|---|---|
| 看今日主页 | `d7c7366526afff1a0ecaf679cc3736fcd0a7ee4eb74295371b926e8b3ae327b7` | 2499 | false |
| 看本周主页 | `d7c7366526afff1a0ecaf679cc3736fcd0a7ee4eb74295371b926e8b3ae327b7` | 2499 | false |
| 看本月主页 | `3bfb2783f6332e41af564ae59a6f7f3e637fcd29f13aa84fe6b263d6df36bce6` | 2499 | false |
| 看连续记录天数 | `d7c7366526afff1a0ecaf679cc3736fcd0a7ee4eb74295371b926e8b3ae327b7` | 2499 | false |
| 看今日热量预算 | `d7c7366526afff1a0ecaf679cc3736fcd0a7ee4eb74295371b926e8b3ae327b7` | 2499 | false |

- 搬迁后 `--after`（GATE-RUN runId=`87d2d261-a091-41d0-a17a-9aad-ac61d712aab6` exit=0）：`RESULT: 5/5 ALL-MATCH`。
  提交前终验（当刻树，runId=`c8e3c074-76a8-448d-acc7-f96b55a88c8c` exit=0，TSC exit=0）：同 `5/5 ALL-MATCH`。
- 自检合理性：本周（windowDays 7）与今日缺省（7）同哈希，月（30）不同——窗口语义正确。

## ④ 变异自证（改坏必红，还原必绿）

- 改坏：`homeDocs.ts` 内 `kpi('今日摄入'…)` 加一字 → `--after`（runId=`288538de-6c94-46d8-a07c-f7b97ede227f` exit=1）：`RESULT: 0/5 MISMATCH`。
- 还原：逐字改回（残留 `今日摄入X` 命中 0）→ `--after`（runId=`2dd1d26e-4be9-471d-b9b9-3dff74d50002` exit=0）：`RESULT: 5/5 ALL-MATCH`，与基线哈希逐字一致。

## ⑤ 门禁（持锁，运行记录一对一）

- `tsc -p packages/skill-calorie`（包级编译）：绿（`--after` 窗内 TSC exit=0；`render-t8` 窗 runId=`82f12331-94f1-44bf-815e-47df4230a726` exit=0，pass 7／fail 0）。
- `node --test render-t8.test.mjs`：pass 7／fail 0（上行同窗）。
- `pnpm build`（根）：红，但红因是伙伴在途写集——`src/diet/routes.ts` 未提交新增行（order 40 `calorie.product.import`）无对应生成键，`TS2820`（runId=`cad8ff4b-e8d3-424c-8647-52ac3109435d` exit=2）。本票锥内文件零差异，待伙伴落定后复绿。
- `gen-cli.mjs --check`：红，但唯一 FAIL 是 `routes.generated.ts:52`（盘上 `批量导入食品 non-exec` vs 生成 `exec calorie.product.import`）——同一处伙伴未提交路由声明；`keys.ts`／`registry.ts`／`combos.yaml`／`build-help.mjs` 四件 ok（runId=`0a0f4efd-fff9-4f3b-b62f-2c74593092b8` exit=1）。本票未改任何生成器输入（commands／routes／legacy），零新增红。
- `calorie-routing-81`＋`cmd-registry-294`：红（`436 !== 421` 等计数漂移），同因伙伴新增路由未重生成；非本票范围。
- `check-warning-line.mjs`：`PASS: 告警线台账齐全`（5/5，台账 2/2）——本票未动台账两件。
- `pnpm snapshot:html:check`：依票面禁用（不覆盖卡路里），未跑。

## ⑥ 提交范围

- 本票 5 件：`src/home/homeDocs.ts`（新）、`src/home/today.ts`、`src/render/html.ts`、
  `src/render/index.ts`（4 行转出口径）＋本证据件。
- **记账（并发实况）**：`src/render/index.ts` 的本票 4 行已被 #332 会话的提交 `de2f935`
  连带入仓（逐字一致，含 `#370` 注释；其提交信息未提 #370）。
  内容正确、归属记在此处；本票剩余 3＋1 件独立提交，不触碰该提交。
- `git diff --name-only` 核对：禁区（home routes／commands、weight／diet／exercise／goal）无本票改动。

## ⑦ 遗留与交棒

- 已开票：#388（告警线台账漏挂号 `html.ts` 680 行，needs-triage）。
- needs-triage 判断：#370 本体工作完成（判据全绿）。建议编排者：① 通知饮食席跑生成链（`pnpm build → pnpm gen → pnpm build → pnpm help:build`）解掉根 build 与 gen:check；② #388 派给台账主人；③ de2f935 连带入仓属 #332 会话的范围口径问题，内容无误，记账即可。
- 下一手缺什么：本票提交后，在 #370 评论贴提交 sha；#371 整页化可直接改 `src/home/homeDocs.ts`（归位件已就绪）。
