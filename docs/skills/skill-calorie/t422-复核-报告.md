# t422 复核报告（复核席）—— 清障提交 f12aa2b 的入库面与两半改法

**判定：FAIL** —— 技术面四查全清（入库态可复现、收窄未变放宽、闭集真闭、两处补记到位）；唯一 FAIL 依据在**过程面**：票 422 三笔提交（`f12aa2b`／`d8c2af4`／`f901e03`）的 git 动作在**全仓所有** `gate-runs.log` 中**零持锁留痕**。按 §2（`git commit` 必须持锁）＋§2.3-3（看持锁行判是否走包装器）＋§6（绕过锁跑 git＝S1-过程违规）。§6.1④ 限定「记录是编排者的具名行为」，实施者／审查者无权自行援引降级；①–③的机器读数已备齐（§一），④请编排者裁定。

## 一、机器证据（本席独立复跑，一律经 `node tooling/run-locked.mjs --ticket 422 --`）

- **① 入库态复跑**：`git show HEAD:<判据件>` 与工作区逐字节相同——同为 sha256 `53119287d80783240b1f8fb452bb6cad0b16d27ddbc608e0ab384353e6759d14`（17449 B，`git diff` 空）。
  `GATE-RUN runId=f59f5369-7d15-44c8-aa9e-0aa485f67122 cmd="node --test packages/skill-calorie/test/fusion-shared-422.test.mjs"` → **exit=0**，tests 9／pass 9／fail 0。⇒ 入库态可复现这条 S1 已清。
- **② 自证①复做**（换件，不用清障席选的 `emptyGuide.ts`）：给 `skill-calorie/src/shared/sourceLine.ts` 塞一段 `class="ilife-recheck-empty"` 结构 →
  `GATE-RUN runId=2b775c6a-48dc-44bb-a4a7-31d2f46625bb`：注入态 **exit=1**、8/9、断言点名该件；写回**本席另存的原字节**（sha256 `d36a672afe9002d97ade961deb61e505597fcf450bf80dbfce0d44a19a2c8a28`，与基线逐值同，未用 `git checkout --`）→ 还原态 exit=0、9/9。⇒ **收窄未变成放宽**：六件里任一自造空态结构仍被抓。
- **③ 自证②复做**（公共层加**清单外新件名** `base-render/src/t422-recheck-empty.ts`）：
  `GATE-RUN runId=216f2045-8461-4558-a734-c724ec39cdc8`：红——闭集比对面 2→3 件（`blocks.ts,controls.ts,t422-recheck-empty.ts`）、exit=1、8/9；逐条点名删除后回到 2 件、exit=0、9/9。当刻落点是**真闭集** `assert.deepEqual(emptyDefsAll, EMPTY_DEF_ALLOWLIST.map(e=>e.path))`，非「至少含两件」；清单两行**具名**（`blocks.ts`＝`renderEmptyBlock` 唯一定义地／来源 `#104`；`controls.ts`＝`renderEmptyState` 唯一定义地／来源 `#76`），检索面＝全源码树（本席按同一条正则独立列过 `base-render/src`，确为这两件；全源码树那条读数见判据 `RESULT` 行，本席复跑一致）。
- **④ 本席自设新探针**（§5.1 第 2 条，打脚本自我满足盲区）：在**已登记件内**再落一处定义地（`blocks.ts` 内加函数局部 `const renderEmptyState`）→
  `GATE-RUN runId=3f77022f-b155-48d2-ae66-b482dc4b2d6a`：**红＝0**（9/9 绿），还原 sha256 `dba73fb9fd75c11e3f9dea571e994a08631ceb6f61047323eb596779a48f1fb1` 同值、还原后绿。⇒ 闭集口径是**文件级**（见 §三-2）。
- **⑤ 对账**：证据件 §十 引的 6 条 runId 在运行记录里**一比一**命中、命令与 exit 相符；本席四次运行亦逐条落痕（`waitedMs=10007／10015／50048／1`，见文末导出块）。

## 二、逐条路径

| 件 | 状态 | 读数 |
|---|---|---|
| `test/fusion-shared-422.test.mjs` | 入库、与工作区逐字节同 | 280 行／17449 B（本席读数）；blob `5ca978a6…` |
| `docs/skills/skill-calorie/t422-融合共用件.md` | 入库（`f12aa2b`＋`d8c2af4`＋`f901e03`，三笔均已推送） | §十.7 **待接线清单**（六件→接线族票，票未切）；§十.8 补记两处漏记 |
| 两处补记行号（本席逐条核） | 相符 | `profile/labels.ts:19`（5 键表）／`:29` 回退；`exercise/receipt.ts:42`（16 键表）／`:62` 回退；三处私有 `sourceLine`＝`weight/history.ts:462`、`weight/volatilityDoc.ts:206`、`diet/nutritionPortDocs.ts:59` |
| 六件源件／`base-render` | 探针后无改动、无残留 | 本票六件＋判据件＋证据件 `git status` 空；两包 `src` 下无 `t422*` 残留 |

## 三、缺陷清单（标注归属）

1. **S1-过程违规 · 本票范围（请编排者按 §6.1 裁定）**：三笔提交的 `git commit` 无 START／RUN 行（提交秒 `12:36:22Z`／`12:37:04Z`／`12:37:20Z` 的留痕行各为 0；`f12aa2b` 提交秒目录锁正被 `ticket=424` 的测试窗口占着，12:36:21.384–12:36:28.735），而同期 398／338／428／361 的 git 动作都有持锁行。未见任何放行授权句。
2. **S3 · 本票范围，非本次改法引入**：闭集按**件**比，同一已登记件内再落一处定义地不红（§一-④）。若要按「处」判，需在比对面带上件内命中数；基线同形，故不改判本次收窄/还原。
3. **范围外（登记＋转票）**：`src/body/bodyDocs.ts:153` 的 `class="msr-empty"` 已随 `0de70cb`（#361）入库；「技能层不许自造空态结构」当刻只由本票六件承接，余下归该域票收口（证据件 §十.4／§十.9 已明写，不构成隐瞒）。
4. **未发现**：证据造假（四查读数与清障席声称一致）、私自放宽（②③红绿证明六件与公共层闭集都仍被抓）、用户可见回归。

## 四、五维分摘要（30／25／20／15／10）

| 维 | 分 | 依据 |
|---|---|---|
| 契约一致 | 29 | 六件＋判据＋证据齐；甲半收窄有派单方裁定的「照收」句 |
| 证据真实可复现 | 24 | 读数可复跑、对账一比一；扣 1＝探针脚本落 `.scratch` 未入仓 |
| 新旧对照 | 19 | 两半逐条量、闭集由 `includes` 还原成 `deepEqual`；扣 1＝按件还是按处比对没写进判据 |
| 工程红线 | 5 | 三笔提交绕过锁 |
| 文档同步 | 9 | 待接线清单＋两处漏记补齐 |
| **合计** | **86** | ≥85 线；但 §6 的 PASS 条件含「不存在未经记录的过程违规」→ **FAIL** |

## 五、取径（供编排者裁决）

若三笔提交系编排者／用户在本协议之外手工执行，请按 §6.1 出**具名**书面记录（引条号、一次性、不类推）⇒ 本项降 S3、判定改判 **PASS**，技术面四查无需重跑；若无此记录，按 §6 字面作废重做，重做只需在提交动作面（内容面可原样沿用）。

## 六、本席运行记录导出（对账源；全量在 `.scratch/locks/gate-runs.log`）

```
RUN ticket=422 runId=f59f5369-7d15-44c8-aa9e-0aa485f67122 cmd="node --test packages/skill-calorie/test/fusion-shared-422.test.mjs" waitedMs=10007 exit=0 at=2026-09-14T12:40:05.628Z
RUN ticket=422 runId=2b775c6a-48dc-44bb-a4a7-31d2f46625bb cmd="node .scratch/t422-recheck/probe-a-fix-six.mjs" waitedMs=10015 exit=0 at=2026-09-14T12:40:28.411Z
RUN ticket=422 runId=216f2045-8461-4558-a734-c724ec39cdc8 cmd="node .scratch/t422-recheck/probe-b-newfile.mjs" waitedMs=50048 exit=0 at=2026-09-14T12:41:30.246Z
RUN ticket=422 runId=3f77022f-b155-48d2-ae66-b482dc4b2d6a cmd="node .scratch/t422-recheck/probe-c-same-file.mjs" waitedMs=1 exit=0 at=2026-09-14T12:41:37.261Z
```

探针驱动（可复跑）在 `.scratch/t422-recheck/`：`probe-a-fix-six.mjs`／`probe-b-newfile.mjs`／`probe-c-same-file.mjs`（各自运行期另存原字节、`finally` 内逐字节写回并核 sha256）。按派单写路径限制，脚本未入仓；若需随证据入仓，请授权归属件路径。本席仅在 `.scratch/t422-recheck/` 与本报道路径内写作。
