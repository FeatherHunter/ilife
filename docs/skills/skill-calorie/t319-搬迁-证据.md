# #319 场景 10「分析」搬迁 · 证据

票号标签 `319`。范围：把 HELP 场景 10「分析」的 13 条读命令从 `src/cli/legacy/scene-10.ts`
（未搬迁清单）搬进能力目录 `src/analysis/`，并按**记录归属＝它 `key` 的所有者**（无 `key` 的
`non-exec` 记录＝它 `scene` 所属票）把 169 条路由记录整组同迁。

- 正本：`.scratch/t314-319/六票落点规则.md`；`docs/agents/命令登记纪律.md` §二 形状三。
- 本席由编排者裁决改过两处口径（见「偏离记账」）：**不下调棘轮**、**给缺 `wakeWord` 的老键不补词**（等 #323）。
- 本窗口 BASE：`130eacbbd340a895df2abf25ade176a3c73bd3c5`（开窗前 `git log -1 --format=%H`；短 sha `130eacb`）；抢锁留痕（外层落点锁包装器持锁后**立即**写的 `START` 行）：`START ticket=319-land runId=614d7913-df65-4508-a1ca-0ca6f813e3ca cmd="pwsh -NoProfile -File .scratch/t319/land.ps1" waitedMs=0 pid=29844 at=2026-09-13T15:27:21.468Z`

## ① 改前 / 改后对照

| 面 | 改前 | 改后 |
| --- | --- | --- |
| 命令声明住处 | `src/cli/legacy/scene-10.ts`（13 条） | `src/analysis/commands.ts`（13 条），分片搬空 |
| 能力对外的门 | 无（`src/analysis/` 只有算式与取数件，无 `index.ts`） | `src/analysis/index.ts`：`ANALYSIS_COMMANDS`／`runAnalysisView`／`runAnalysisWrite` |
| 路由记录住处 | `legacy/routes/scene-10.ts`（167 条属本票）＋ `legacy/routes/scene-02.ts`（2 条属本票） | `src/analysis/routes.ts`（169 条），两个分片定点删掉这 169 行 |
| 分派层 | `cli/cmd_read.ts` 里 13 个 `case` | 删掉这 13 个块（−73 行）＋ 随之未使用的 25 个 import 绑定 |
| 生成物 | — | 只有 `src/cli/registry.ts` 变（多一行 `ANALYSIS_COMMANDS`＋一行 import） |

## ② 搬迁键清单（键 → 路由 `order` → 处理 `case`）

三张清单由 `.scratch/t319/mk-roster.mjs` **机械取自基准**（不靠记忆），全文见 `.scratch/t319/名册.md`。摘要：

| 键 | 路由记录数 | `list:order` | 原处理 `case` 行 |
| --- | --- | --- | --- |
| `calorie.help.lookup` | 1 | `new:18` | 837 |
| `calorie.history` | 2 | `wake:427`、`new:19` | 852 |
| `calorie.view.anomaly` | 22 | `wake:354-371`、`wake:374-376`、`new:30` | 931 |
| `calorie.view.calorie-trend` | 1 | `new:45` | 450 |
| `calorie.view.combined` | 71 | `wake:260-319`、`wake:377-386`、`new:6` | 659 |
| `calorie.view.deficit` | 2 | `wake:426`、`new:7` | 674 |
| `calorie.view.health` | 13 | `wake:320-330`、`wake:423`、`new:9` | 699 |
| `calorie.view.lint-health` | 2 | `wake:424`、`new:46` | 563 |
| `calorie.view.long-trend` | 1 | `new:47` | 462 |
| `calorie.view.nutrition-analysis` | 2 | `wake:387`、`wake:388` | 471 |
| `calorie.view.predict` | 6 | `wake:393-397`、`new:29` | 923 |
| `calorie.view.review-template` | 1 | `new:50` | 591 |
| `calorie.view.six-factors` | 1 | `wake:413` | 483 |

小计：**127 条**带 `key` 记录（`scene-10.ts` 125 ＋ `scene-02.ts` 2）＋ **42 条**无 `key` 的
`non-exec`（`scene: '10'`，全在 `scene-10.ts`）＝ **169 条**。`non-exec` 的 `reason` 文本逐字照抄，
不借搬迁改语义。

**留在原片、不属本票的 20 条**（键不属分析能力，由所有者在自己那票里定点删）：
`view.diet-review`×10（`wake:390-392,414,416,417,419-421,428`）、`view.ranking`×6（`wake:422,431-435`）、
`view.exercise`×2（`wake:429,430`）、`view.goal-progress`×1（`wake:372`）、`view.goal-weight`×1（`wake:373`）。

## ③ 门禁实测

命令一律**在落点窗口内直接调用**（编排者 2026-09-13 新硬规矩：窗口内门禁不得嵌套
`tooling/run-locked.mjs`——外层包装器已持有 `.scratch/locks-land`，再抢默认闸锁会锁反转／自堵；
本席 22:58 那次窗口正是这么僵住的，被编排者判为「不是真红」并代为解锁）。留痕＝外层落点锁这一次
窗口的 `START/RUN ticket=319-land` 条目 ＋ 下表逐步 exit ＋ 逐文件日志：

- GATE-RUN runId=614d7913-df65-4508-a1ca-0ca6f813e3ca cmd="pwsh -NoProfile -File .scratch/t319/land.ps1"    ← 外层落点锁这一次窗口（`START` 行同 runId；`RUN` 行在窗口退出时由包装器补写进 `.scratch/locks-land/gate-runs.log`）  | 步 | 命令（**窗口内直接调用**） | exit | 日志 | |---|---|---|---|
| 4 build | `pnpm build` | 0 | `.scratch/t319/logs/04-build.log` |
| 5 gen | `pnpm gen` | 0 | `.scratch/t319/logs/05-gen.log` |
| 7 build（gen 只写 src ⇒ 重建配平 dist 与内容印记） | `pnpm build` | 0 | `.scratch/t319/logs/07-build.log` |
| 8 gen:check | `pnpm gen:check` | 0 | `.scratch/t319/logs/08-gencheck.log` |
| 9 test | `pnpm test` | 0 | `.scratch/t319/logs/09-test.log` |
| 9b 快照 | `pnpm snapshot:html:check` | 0 | `.scratch/t319/logs/09b-snapshot.log` |
| 10b-1 变异 | `pnpm gen:check` | 1 | `.scratch/t319/logs/10b-mutation.log` |
| 10b-2 变异复原 | `pnpm gen:check` | 0 | `.scratch/t319/logs/10b-mutation-restore.log` |

- 顺序：`build → gen → 免费判据 → build → gen:check → test → 快照 → t293 → 变异自证`。
- `pnpm gen:check`：PASS。`pnpm test`：全绿（条数见日志尾）。
- `pnpm snapshot:html:check`（票面判据 ② 的 `changed=0` 口径；`pnpm test` 的 glob **不含**它）：PASS（changed=0）· 末两行：`OK: 5 技能 HTML 快照 == 实际（186 件产物，base-* 指纹 ef7cc02969e37e4bfc0eb35751b8dc1c，base-* 文件 27 件） ／ RESULT: artifacts=186 changed=0 added=0 removed=0 base-* fingerprint=ef7cc02969e37e4bfc0eb35751b8dc1c`
- `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`：`P1..P5` 全绿、`UNACCOUNTED=0`。

## ④ 免费判据实测（生成物 diff 只应出现 `src/cli/registry.ts`）

`git status --short` 的完整名单（落点脚本自检，见 `.scratch/t319/logs/03d-status.txt` 与 `06-diffstat.txt`）：

- 变：`src/cli/registry.ts`（生成器按能力目录扫出来的一能力一行）。
- 变（预期内，声明面）：`src/analysis/commands.ts`／`index.ts`／`routes.ts`（新）、
  `src/cli/legacy/scene-10.ts`／`legacy/routes/scene-10.ts`／`legacy/routes/scene-02.ts`、`src/cli/cmd_read.ts`。
- **逐字节不变**（脚本硬断言，任一有 diff 即 `LAND-ABORT`）：`src/cli/keys.ts`、
  `packages/base-combos/combos.yaml`、`packages/skill-calorie/scripts/build-help.mjs`、
  `src/triggers/routes.generated.ts`、`packages/skill-calorie/SKILL.md`。

## ⑤ 变异自证

落点脚本第 10b 步：给声明源 `src/analysis/routes.ts` 尾末加一个空格 ⇒
`pnpm gen:check` 必须报 `GEN-STALE FAIL`（内容印记不符）；复原该文件后 `pnpm gen:check` 必须回到 PASS。
两次实跑输出见 `.scratch/t319/logs/10b-mutation.log` 与 `10b-mutation-restore.log`。

另有一条**前置自证**：`node .scratch/t319/apply-routes.mjs --check` 拿**当前仓内**算出的成品与预备期
快照逐字节比（`ROUTES-CHECK OK mine=169`）——记录集合与预备期不一致即停手，防「别人动过之后我按旧快照覆盖」。

## ⑥ 偏离记账（与票面／派单不一致处，逐条说明）

1. **不下调棘轮上限／冻结集**（票面「② 既有断言全绿」照旧成立：两处判据都是 `⊆ ＋ ≤`，纯搬迁自动绿）。
   依据：编排者裁决（2026-09-13）——六票并行预备各自预算的绝对数字会因别人先落点而失真，终态由 #320 一次到位。
2. **不给缺 `wakeWord` 的两条键补词**（`view.anomaly`／`view.predict`）。票面没写这条；若按
   `gen-cli.mjs` 原先的必填口径补词，`build-help.mjs` 的 `REPR` 段会多两行、`SKILL.md` 跟着变，
   免费判据失效，且属替产品定内容。依据：编排者裁决 ＋ 新票 **#323**（把 `wakeWord` 改成可缺）已落点。
3. **路由记录不整片同迁**：派单原字面把 `routes/scene-10.ts`（187 条）整片点名为本席独有分片；
   编排者裁决改为「记录归属＝它 `key` 的所有者」，故 20 条外来键记录留在原片。本席已按裁决执行，
   并把整片搬到 `src/analysis/routes.ts` 的初版方案作废（该方案会让 #314／#315 随后的定点删除丢数据）。
4. **`src/analysis/` 不按票面第 2 条搬 `render/*`**：票面第 2 条要求「搬 `render/analysisPlate.ts`／
   `insightPlate.ts`／`trendMiscPort.ts`／`trendDocs.ts`／`analysis/*` 里属场景 10 的部分」，
   派单明写「视图层不搬不改；你搬的是命令声明与分派」——**以派单为准**：视图层一行未动，
   `commands.ts` 只经它们的公开接口调用（铁律一：不许抄实现）。
   后期编排者裁决：**第 2 条的实现层部分整体转新票 #324**（窗口风险控制），本票只做窗口 A
   （声明 ＋ 13 个 `case` ＋ 169 条路由记录）。本席预备件里从未含实现件成品，落点无需排除。
5. **票面第 1 条的「子功能文件」未新建**：派单限定「只加 `commands.ts`／`routes.ts`／扩 `index.ts`」，
   故处理函数住 `commands.ts`（同目录），按 HELP 下一级分段（组合分析／缺口分析／营养分析／
   单点分析／健康报告／预测模拟／自动分析 ＋ 无下一级的那 6 键按移植来源分段），不新开子功能文件。
   另：派单说「动手前先读它的 `index.ts`」——`src/analysis/index.ts` 此前**不存在**，故是新建而非扩写。
6. **`cmd_read.ts` 走定点删除而非整文件覆盖**（编排者裁决「规则精修」）：整文件覆盖可能把别人先落点的
   case 删除回滚成复活，而 `#294` 的 case 棘轮是「⊆ 冻结清单 ＋ size ≤ max」，复活一条仍在冻结清单里的
   `case` 不报红 ⇒ 静默回归。预备期的整文件成品仍放在 `.scratch/t319/tree/` 作对照物。
7. **票面第 4 条「加写命令补两处」**：本场景 13 键**全是读命令**，无写键 ⇒
   `test/cmd-write-40-persist.test.mjs` 与 `docs/research/t97-probe-receipts.mjs` 无补项。
8. 票面第 3 条的 42 条 `non-exec`（`multiTrendMissing`／`predictParamMissing`／`reportKindMissing`／
   `oosCron` 那些理由码的展开文本）逐字照抄进 `src/analysis/routes.ts`，未改一个字符。

## ⑦ 并发纪律自述（编排者 2026-09-13 增补四条 · 逐条对应）

1. **派生式删除，非快照式**：`.scratch/t319/apply-routes.mjs` 的删除集只由「本票拥有的 key 集合 ＋
   `scene: '10'` 的 `non-exec`」这个谓词在**当刻活树内容**上现算；行号与字节比对**不作为手术依据**
   （`--check` 只是一道「有人动过本票记录就停手」的前置守卫，见下）。`cmd_read.ts` 走**标签驱动**删除
   （`case '<键>':` 整块），同样与行号无关。
2. **fail-closed**：两个脚本都是「① 派生（只读）→ ② 断言 → ③ 写盘」三段，断言全在写盘之前；
   路由侧断言 A 整组同迁／B 计数 169·127·42／C 残留零引用（被删过的件里本票 13 键出现 0 次、
   `scene: '10'` 的 `non-exec` 出现 0 次）／D 只减不增（残留记录逐行仍是改前的行）；
   分派层断言 E 改后 case 集合**逐条等于**「改前 − 本票 13 个」（不是只数条数）／F 剩下每条 import
   绑定都仍有使用处（防误删仍在用的符号）。任一红 ⇒ `ROUTES-ABORT`／抛异常，**一个字节都不写**。
   实测日志：`logs/03c-routes-apply.txt`、`logs/03d-readcmd.txt`（各自打出 `assertions=…-OK`）。
3. **窗口自带回滚**：`land.ps1` 的 `Fail` 在「本席写过盘」之后会逐文件点名回退
   （`git checkout -- <单个文件>`，只限本票 4 个 tracked 件 ＋ 生成物 `registry.ts`）＋ 点删本票新件
   （`analysis/{commands,index,routes}.ts` ＋ 本票两份收尾件），随后打印 `git status --porcelain -uall`；
   非空即报 `ROLLBACK-INCOMPLETE` 并停手（不擅自再动）。开门／守卫阶段的红**不回退任何文件**
   （那时树里可能有别人的改动）。**留脏树＝交付不合格**，故脚本末步自检关门：`-uall` 必须为空。
4. **BASE 与抢锁留痕**：见文首两行（BASE sha 由 `git log -1 --format=%H` 取；`START ticket=319-land …`
   由外层包装器持锁后立即写入 `.scratch/locks-land/gate-runs.log`，本脚本在开门时读它）。
   门禁各自的 `runId` 见 ③ 的 `GATE-RUN` 声明。

## ⑧ 未确证 / 风险

- **未确证**：本票只做了「声明与分派」的搬迁；`src/analysis/routes.ts` 里 5 个外来键的记录
  （20 条留原片之外的视野）最终能否被各自所有者全部认领，要到 #320 收口时才能独立复核。
- 风险 1：`routes/scene-02.ts` 的 2 条是本票在**别人分片**上的定点删除，若 #315 同窗口内也删同一行
  ⇒ `(list, order)` 重复被生成器当场抛（可发现，不会静默）。
- 风险 2：本票最后落点，前五票在 `cmd_read.ts` 上的删除若与本席标签驱动手术交叉，本席已用
  「行数不减反增即停手 ＋ case 数必须恰好 −13」两道护栏兜住。


