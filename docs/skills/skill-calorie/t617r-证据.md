# #617 复核席（第二席·复核#617）证据 · 预检确认页表格印出HTML标签字面量

> 被复核提交：`7085b8e`（改动三件，首轮席落盘）／`a10e9a6`（首复验席：`t617-证据.md` 第七节）／
> `9b02ef2`（首复验席：`t617-运行记录.md` 补入复验窗口）。
> 本席独立重跑票面全部验收命令（不采信实施者结论），自设 1 个被审脚本覆盖不到的新探针，再做变异确认
> （改坏→红→逐文件还原→绿两行读数）。命令一律经 `node tooling/run-locked.mjs --ticket 617 -- …` 排队。
> 本席运行记录：`docs/skills/skill-calorie/t617r-运行记录.md`；探针入仓：`docs/skills/skill-calorie/t617r-探针-格子.mjs`。

## 〇 · 判定

**PASS**。票面五段逐条成立，且**独立复现**：编译 exit 0／t277 `19 pass 0 fail`／真出口那一列
逐格只剩读者话与公共层徽章结构（8 格 8 枚，点名两格那页 2 枚橙档）／改前红与还原绿两行读数齐。
五维：契约一致 **30/30**、证据真实 **25/25**、新旧对照 **19/20**、工程红线 **15/15**、
文档同步 **8/10** ⇒ **97**。**无 S1、无 S2**；新增 S3 两条（见第六节）＋复现既有 S3 一条（第五节）。

## 一 · 盘上 sha256 与 blob 逐字节相同（先核盘，再重跑）

| 件 | 盘上 sha256（前 16） | `git hash-object` | `7085b8e:<路径>` | 同 |
|---|---|---|---|---|
| `src/diet/precheckLabel.ts` | `173A3EE5D18E6037` | `4daaefdc9aa95c07…` | `4daaefdc9aa95c07…` | ✅ |
| `src/diet/precheckParts.ts` | `0CDDF1D4B2F10FA5` | `729ba0cc396a419b…` | `729ba0cc396a419b…` | ✅ |
| `test/t277-预检确认页.test.mjs` | `D0E2B3BDB204795E` | `5c183c5f527d98ad…` | `5c183c5f527d98ad…` | ✅ |

提交号全值 `7085b8e5e7c3427b563ca51c01d0f69eb47157d7`。`git diff 7085b8e --` 上述三件**无输出**（提交后一字未动）。
`precheckLabel.ts` 盘上 sha 与首轮席在 §四 记的两个还原判据之一逐字相同（该值独立复算命中）。

## 二 · 票面验收命令逐条独立重跑

| # | 票面命令 | 本席 runId | exit | 读数 |
|---|---|---|---|---|
| ① | `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` | `2a169132-30ae-406a-83c0-ffaf90cbabfb` | 0 | 编译绿 |
| ② | `node --test packages/skill-calorie/test/t277-预检确认页.test.mjs` | `303e4bf4-39ed-40e3-a920-eb15d0f0ca4d` | 0 | `tests 19 / pass 19 / fail 0`（自跑读数 8624.5ms） |
| ③ | `node docs/skills/skill-calorie/t617-探针-tag.mjs --out .scratch/t617r/probe-after` | `a4c847ab-7d8f-4d93-af03-951814d5e5f1` | 0 | `RESULT: 3/3 PASS` |
| ④ | 自设探针 `t617r-探针-格子.mjs` | `30d2e18f-a6b3-4eee-82da-076584fe1417` | 0 | `RESULT: 4/4 PASS` |
| ⑤ | 邻接六件 `node --test t277 t509 t496 trend-misc-port-113 t270 t279` | `5e92fbd9-2380-44d5-9b7c-46049a347e66` | 1 | `tests 181 / pass 179 / fail 2` |
| ⑥ | 本包告警线门 `node packages/skill-calorie/scripts/check-warning-line.mjs` | `19c79648-7799-4299-bca5-714dfda7ad7a` | 0 | `RESULT: 91/91`／`PASS: 告警线台账齐全且与实况一致` |

③ 的三行读数与被审证据 §三「改后」三条**逐字节相同**（同一条探针、同一段判据、同一份源码）：

```
T617 拍营养表记一餐    exit=0 bytes=84429 可见文本：&lt;=0 裸<span=0 precheck-tag=0 ｜ 结构：徽章=8  橙徽章=0 旧类标记=0 VERDICT=GREEN
T617 拍营养表补记一餐  exit=0 bytes=84640 可见文本：&lt;=0 裸<span=0 precheck-tag=0 ｜ 结构：徽章=8  橙徽章=0 旧类标记=0 VERDICT=GREEN
T617 预检确认页（点名两格不确定） exit=0 bytes=85251 可见文本：&lt;=0 裸<span=0 precheck-tag=0 ｜ 结构：徽章=10 橙徽章=2 旧类标记=0 VERDICT=GREEN
RESULT: 3/3 PASS
```

⑤ 的两条红**点名到件**：`✖ #509 ② 五页回执标题按唤醒词对上…`／`✖ #509 ④ 变异自证…`，
断言原文为 `AssertionError: 原样产物该读到新标题`（`t509-清尾.test.mjs:174`，期望 `拍照记一餐 · 回执`、
实得 `''`）。这两条读的是**回执页题名**（`src/diet/receipt.ts` 那一支），与本票两件无交集
⇒ 与本票无关，**只点名不代修**，归属 #581 的判断成立（与首轮席 §五 一致，本席独立复核过断言原文）。

## 三 · 自设探针（被审脚本覆盖不到的那个面）

被审探针只判「可见文本里**不含** `&lt;`／裸 `<span`／`precheck-tag`」——**缺字也算过**：把那一列
整列撤掉、或旧形状换个类名重现，它照判 `VERDICT=GREEN`（本席已实测，见第四节 mutation「own=RED/accepted=GREEN」）。
本席探针（`t617r-探针-格子.mjs`，163 LF）改判**按格钉死的渲出文本**：

1. 按列头 `data-label="这一格怎么来的"` 取那一列的**逐格 HTML**，逐格读出徽章段（class ＋ 文本），
   要求：段数＝徽章枚数（普通格 1／点名两格那两格 2）、class 只许
   `ilife-status-badge ilife-status-badge-empty`／`…-warn` 两档、**整格渲出文本逐字等于**「识别得到」
   或「识别得到要你核对」、格内**不许夹裸文字**；
2. 旧类名残留三类（`precheck-tag`／`is-ai`／`is-check`）逐页数、上限全 0（被审探针一个都不判）；
3. 那一列**逐格**另数转义壳 `&lt;`（同页别的槽有元字符时也不许漏进这一列）；
4. 另加一例**自由文本槽带元字符**的真出口跑（`productName`／`note` 塞 `<span class="q">`、`a&b`），
   验纯文本面没被这次改动放松。

四行读数（runId `30d2e18f…`；`CALIB` 附落点切片供核对）：

```
T617R 拍营养表记一餐 exit=0 bytes=84429 列格数=8 首格="识别得到" 旧类名=0/0/0 转义壳=0 VERDICT=GREEN
T617R 拍营养表补记一餐 exit=0 bytes=84640 列格数=8 首格="识别得到" 旧类名=0/0/0 转义壳=0 VERDICT=GREEN
T617R 预检确认页（点名两格不确定） exit=0 bytes=85251 列格数=8 首格="识别得到" 旧类名=0/0/0 转义壳=0 VERDICT=GREEN
T617R 预检确认页（字段值带元字符） exit=0 bytes=84400 列格数=8 首格="识别得到" 旧类名=0/0/0 转义壳=10 VERDICT=GREEN
RESULT: 4/4 PASS
```

元字符例的 `CALIB` 落点（**五处全部是转义后的文本**，无一处成真标签）：页头摘要行、
「会写进去的字段」表的 `data-label="这一条写什么"` 格、复制数据／复制日志载荷（JSON 内层再转义）、
来源脚注行；`&lt;` 合计 10 处＝`<span class="q">酸奶</span>` 一文三次出现（各含 2 个 `&lt;`）＋
载荷里 4 处。被审证据 §七 记的「真产物那一列首格
`<span class="ilife-status-badge ilife-status-badge-empty">识别得到</span>`」本席**逐字复现**。

## 四 · 变异确认（两轮，一次持锁窗口内：备份→改坏→两条探针→逐字节还原→复跑）

变异打在 `dist/`（被测探针与真出口读的就是 `dist/`，正好落在观测量上；本席不动源码、也避开在共享
工作区里 `tsc --force` 重编与他席互相踩）。窗口 runId `3fd58a90-a311-4627-97e0-14b783be9c3e`，
`.scratch/t617r/mut-run.mjs` 驱动，逐段读数落 `.scratch/t617r/logs/`。变异字面量**逐字取自
`git show 7085b8e^:…/precheckLabel.ts`**（读回，不手抄）：

```
mut1＝那一列退回纯文本面（tagCellHtml 一律返 undefined）        mut2＝把 7085b8e^ 的原样字面量写回那一列的值
MUT-READING tag=mut1-celltext-off      own=RED(exit=1)        accepted=GREEN(exit=0)   ← 被审探针在「那一列整列无货」时仍判绿
MUT-RESTORE  tag=mut1-celltext-off      sha256 同原值=YES
MUT-READING tag=mut1-celltext-off-restored own=GREEN(exit=0)  accepted=GREEN(exit=0)
MUT-READING tag=mut2-defect-literal-back own=RED(exit=1)      accepted=RED(exit=1)     ← 缺陷逐字同形，两条探针都红
MUT-RESTORE  tag=mut2-defect-literal-back sha256 同原值=YES
MUT-READING tag=mut2-defect-literal-back-restored own=GREEN   accepted=GREEN
```

mut2 的两条读数与**被审证据 §三「改前」**同形，是本席对首轮席那次变异的独立复现：

```
本席 mut2：T617 拍营养表记一餐 exit=0 bytes=84405 可见文本：&lt;=0 裸<span=8 precheck-tag=8 ｜ 结构：徽章=0 橙徽章=0 旧类标记=0 VERDICT=RED
被审 §三改前：T617 拍营养表记一餐 exit=0 bytes=84617 可见文本：&lt;=0 裸<span=8 precheck-tag=8 ｜ 结构：徽章=5 橙徽章=1 旧类标记=0 VERDICT=RED
```

字节数不同（84405 vs 84617）是**页面壳的别处差异**（首轮席那一轮是他在途件共存时的产物），
判据行（`裸<span`／`precheck-tag`／VERDICT）逐项相同；`徽章=5 橙徽章=1` 那两格是**该探针量的是整页**
（含样式段里 5 条 `status-badge` 规则，其中 `-warn` 1 条），**与是否改坏无关**——本席已单独量过：
改动后页面的**样式段** `status-badge-warn` 命中恰 1、**体**里该页 `status-badge` 命中 16、`warn` 命中 0。
⇒ 首复验席 §七 那句「`橙徽章=1` 那处是样式段里的规则，票面口径不计」**属实**。
还原判据：`dist/diet/precheckLabel.js` sha256 `15B81C9D9C448C1C`／10136 B，两轮前后**逐字节相同**；
本票三件源码全程 `git diff` 无输出。

## 五 · 新旧对照与工程红线（独立复核，不采信结论）

- **行为面**：模板自出，`precheckLabel.ts` 里已**无 HTML 字面量**（体里 `<span` 只出现在注释；
  `grep -F '<span' src` 命中全在别件、且都是区块容器不是单元格值）。真产物那一列由公共层
  `renderStatusBadge` 产出：灰档 `-empty`／橙档 `-warn`（`base-render/src/style.ts`：
  `-warn{background:#fff5e0;color:#a25b00}`＝老实物 `.diff-tag` 橙；`-empty` 中性灰）。
  档位映射（不确定→`warn`）与具名先例（`photo/viewerDoc.ts:152` 的中性档）均在件头写明。
- **事实不变**：每格仍是「这一格怎么来的」，八个字段、单位、读数与「要你核对」格数（`uncertainCount`）一条不减；
  `sec-check` 区块与口径行口径未动。
- **不越写集**：`git show --stat 7085b8e` 恰三件；`precheck.ts`／`precheckPort.ts` 对
  `<span`／`class="`／`cellHtml`／`renderStatusBadge` **零命中**；`base-render/**` 未动。
- **死规则清账**：`PRECHECK_CSS` 的两条 `.precheck-tag` 已撤，**全仓无一处再引用该类的样式**（HEAD 全仓
  `precheck-tag` 命中只剩注释与测试断言文字；dist 两件各 1／2 命中，本席逐条打印确认**全在注释**）。
- **告警线**：`precheckLabel.ts` 201 LF／`precheckParts.ts` 84 LF，均在 350 线内；
  **不触发必报五步第四步**，台账无需为它同步；本包无参跑门 `91/91 PASS`（表已由他席同步到 `build-help.mjs 744`）。
- **一条既有 S3（复现，不新增）**：`t509-清尾` 两条断言读旧题名串（`t509-清尾.test.mjs:174` 期望
  `拍照记一餐 · 回执`），源头是 `973b897` 去分隔符那一笔；与本票无关，只点名。

## 六 · 本席新发现（S3 两条，不翻案；判级依据见下）

1. **被审探针的判据是「措辞合规」而不是「内容存在」**（`t617-探针-tag.mjs:71`：
   `ok = exit 0 && html !== '' && escaped === 0 && naked === 0 && cls === 0`）。本席实测：把那一列
   整列改回纯文本面（`徽章=0`、可见文本里也没有任何禁词）时，它仍打 `VERDICT=GREEN`／`RESULT: 3/3 PASS`。
   票面要的是「改前红／改后绿」，该红读数**确实**由首轮席那次变异给出（其 t277 变异轮断言原文里
   逐字带着 `&lt;span class=&quot;precheck-tag is-ai&quot;&gt;…`，本席已从 `.scratch/t617/logs/mut1-defect-back-t277.log` 读到），
   故**这是判据强度问题、不是证据造假**；建议后续把「那一列格数 ≥ 1 且逐格含徽章结构」并进该探针的
   `ok`（本席探针已按此写）。
2. **相邻两枚徽章之间没有分隔符**：那一列渲出文本同格内是 `识别得到要你核对`（两枚独立胶囊相邻，
   无空格无逗号；老实物也是两个相邻 `<span>`）。样式在场时两枚胶囊各自成块、可辨；但**脱离样式**
   （纯文本抽取、窄屏读屏顺序）会读成一句连写。非阻断（与老实物及公共层既有用法一致），
   记在案供后续文案／结构票决定是否补分隔。

## 七 · 写集与本次提交

- 本席只写：`docs/skills/skill-calorie/t617r-证据.md`（本件）、`docs/skills/skill-calorie/t617r-运行记录.md`、
  `docs/skills/skill-calorie/t617r-探针-格子.mjs`、`.scratch/t617r/**`。
- 变异窗口只碰 `packages/skill-calorie/dist/diet/precheckLabel.js` 一个构建产物（`.gitignore` 已忽略
  `packages/*/dist/`），两轮**都逐字节还原**（sha256 `15B81C9D9C448C1C` 同原值＝YES）。
- 产品代码与他人测试**只读**；未动 `packages/skill-calorie/AGENTS.md`（该件在本席窗口内被他席改过
  `build-help.mjs 742→744`，与本票无关，本席不带进提交）。
