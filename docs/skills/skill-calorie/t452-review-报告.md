# #452 独立对抗审查报告（运动汇总族与对照目标族融合）

**判定：PASS**（无 S1；五维 29／24／19／14／9 ＝ 95／100）

审查对象：`4e7b223` 四件——实现 `packages/skill-calorie/src/render/sportDocs.ts` 351 LF／18852 字节、判据 `packages/skill-calorie/test/exercise-summary-goal-fusion-452.test.mjs` 491 行、证据 `docs/skills/skill-calorie/t452-汇总与目标族融合.md` 127 行、台账 `packages/skill-calorie/AGENTS.md` 1 行。
本席只读：被审四件 ＋ `src/triggers/scene-04-exercise.ts`（冻结表）＋ `test/doc-page-assert.mjs` ＋ `docs/agents/wording.md` ＋ 交付物目录 ＋ 本席自己跑出来的 15 份产物；未读全仓。

## 一、机器证据（均走 `node tooling/run-locked.mjs --ticket 452 -- …`）

1. **复跑门禁**：
   - `pnpm build` → `9b2e7cc7` exit **0**（首报 exit 1 是 pwsh 把包装器 stderr 当错的管道行为，落盘日志实测 exit 0）。
   - 新判据 → `69969860` exit **0**、**9/9**、35 条读数；12 条各 exit 0；12 份产物字节（73414／76377／79160／80266／77228／82194／86005／89435／92134／92134／65115／65115）与证据件表**逐格同值**，`TRUNC 窗内天数=365 可见行数=101 上限=100`、`CALIBER-SRC 命中文件=1` 同值。
   - `node --test …/doc-page-assert.mjs` → `21dfe4f7` exit **0**、1/1。
   - 告警线：首跑 `0a8cc58b` **红**（`RESULT: 57/58`、`RED 台账陈化：src/render/sportPortDocs.ts 台账=702 实况=760`）——该件不在被审提交里、当刻是别席工作区在途改动；别席同步台账后复跑 `b1a2b1a6` exit **0／58/58／PASS**。本票件 351＝台账 351。
2. **自设变异两处**（均非实施席那两处；四步序各走一遍，判据读 `dist/`）：
   - M1「判决胶囊两态删成一态」：源 `7F573B72…`（18852→18830 字节）→ 编译 `24ab6a42` exit 0、dist `152A5051…` → 判据 `d3cc5e60` **exit 1**（tests 9／pass 8／fail 1，红在 `未达成页胶囊不是 no 档`）→ 写回另存原字节（逐字节相等、sha 回 `3F9F67BB…`、mtime 刷到 15:46:10）→ 编译 `a85f3a2d` exit 0、dist 回 `A43C7501…` → 判据 `de844c06` **exit 0**（9/9）。
   - M2「环度去掉 `min(pct,100)`」：源 `76C10C6D…` → 编译 `a74714c6` exit 0、dist `019ED08E…` → 判据 `e855832e` **exit 1**（红在 `环不是 min(pct,100)：实测 420%`）→ 写回原字节（sha 回 `3F9F67BB…`）→ 编译 `c71fd122` exit 0、dist 回 `A43C7501…` → 判据 `c4375085` **exit 0**（9/9）。
   - 还原只写回本席另存的原字节（`.scratch/t452-review/sportDocs.ts.pristine`），未用 `git checkout --`；最终源 `3F9F67BB…`＝证据件同值，dist 回变异前读数。M1 首轮两次编译 exit 2 的报错 4 条**全在别席在途件** `src/exercise/receipt.ts`，与变异无关，换干净窗口重做后 exit 0。
3. **自设探针** `docs/skills/skill-calorie/t452-review-probe.mjs`（`1cd4ae2c` exit **0／13 项全过**）：
   - P1 冻结表逐条对账：自解得汇总 10 条＋目标 2 条与票面逐条同名，12 条参数与判据件写死的参数**逐条同值**（非手挑子集），真跑各 exit 0。
   - P2 十窗同版：短窗 7／长窗 3 各一枚结构指纹，唯一结构增量＝截断明示那处标记；可见行＝min(自算天数,100)，页眉「本窗共 N 天」「其余 N 天」逐窗对得上。
   - P3 15 页锚点零悬空（汇总页各 6 个）、`ilife-page-printable` 恰 1 处且在版面根、`@page printable` 在位、`data-fmt` 逐份 `["text","json","csv"]`。
   - P4 周口径那句：整周页**恰 1 次**、日窗 0 次；各包 `src` 含实现语句命中 **1** 文件＝本票主件（另一处是公共层注释举例）；全仓 8 文件命中，与证据件登记同值。
   - P5 目标缺席：装配层走专门空态（无环、无胶囊、带下一句话）；命令面同数据 exit 4（取数层缺失阻断），接缝读数、写集外。
   - P6 按被审提交的字节逐件对台账：扫描 285 件、超线 23 件、**漏报 0**，本票行 351＝提交字节 351。P7 15 页全文断言：命令字面 0、票号 0、工序词 0、可见面 `snake_case` 0、页头人话。

## 二、逐条核票面（独立复算，不复用实施席判据函数）

KPI 四格／每日消耗折线／类型消耗分布／逐日与按类型明细／按分类汇总 ✔；长窗截断（180／365／365 三窗）页眉条数＝窗内天数、可见行＝100、明示「其余 N 天」✔；环 `min(pct,100)` 实测 100%、判决胶囊两态（命令面 ok／no 都跑到）、差距文案、口径行 ✔；无目标走专门空态、不画空环 ✔；空态带下一句话 ✔；三格式顺序 ✔；12 条＝12 组冻结参数各跑一次 ✔。
**归属**：`git show --name-only 4e7b223` ＝声明四件、无第五件；`git diff 4e7b223^ 4e7b223` 对包内 30 个 `commands.ts`／`routes.ts`／`records.ts`／`receipt.ts`／`sportPortDocs.ts` 读数**空**。

## 三、缺陷段

- **S1：无。**
- **S3（本票范围）①**：证据件变异两行的「判据（绿）8/8」出自**八条版**判据件（第九条「整周才印周口径」是其后补的），与交付的九条版不同版次且未写明；本席按交付字节重做两处自设变异，九条版下红／绿读数齐（§一.2）。
- **S3（本票范围）②**：告警线会被别席工作区在途件顶红——本席复跑首读该门红（702／760），别席同步后回绿；证据件只记一次绿读数，缺该门浮动的对账语。
- **注记（本票范围，口径分歧，不判）**：空态页无页内导航／口径行／来源脚注（单块页没有可导航的卡）；票面只钉「空态带下一句话」，已满足。
- **注记（本票范围，口径分歧，不判）**：环卡写 `var(--blue)`／`var(--line)`（CSS 变量名），与 `t156-样张-运动复盘.html` 同法；本票两页未自写分类色表，`categoryColor()` 的落点在 #453 件里。按族票「不写 token 名」字面套到环卡，是一处 1 行的口径返工。
- **注记（范围外）**：全仓「每日目标 × 7」命中 8 文件 ≠ 票面字面「＝1」；生产代码面**定义**确为 1 处，其余是公共层注释、判据件、历史文档（都在票面「不许动」名单内）——票面口径自相冲突，不作缺陷。
- **注记（范围外，引用 #460）**：目标页来源脚注「共 N 条」实为窗内天数——本席以自造的当日 3 条记录复现 `数据来源 · 每日目标 ＋ 运动记录 · 2026-09-14 → 2026-09-14 · 共 1 条`。**已裁定接受、另立 #460，本票不作缺陷。**
- **已裁定口径复核**：① 可见标签直写中文、未走 `shared/fieldLabel.ts`——可见面 `snake_case` 实测 0，**已裁定接受，不作缺陷**；② 上述 #460 那条——**已裁定接受，不作缺陷**。

## 四、五维分

| 维 | 分 | 依据 |
|---|---|---|
| 票面符合度 | 29／30 | 六条落点齐；扣 1：全仓那句命中 8 文件 ≠ 票面字面「＝1」（口径自相冲突） |
| 机器证据强度 | 24／25 | 9/9＋12 条真跑＋两门回归；两处自设变异在交付字节上红→绿、源 sha256 同值、dist 逐次可追；扣 1：变异两行是八条版读数 |
| 独立对抗性 | 19／20 | 两处变异均非实施席那两处；探针 13 项含逐条对账、十窗同版、周口径出现次数、台账按提交字节对账；扣 1：未发现真实 S1 |
| 文档与证据记录 | 14／15 | 证据件读数可复算（12 份产物字节逐格同值）；扣 1：版次与门读数浮动未写（S3①／②） |
| 纪律合规 | 9／10 | 只含声明四件、五件零差异、还原只写回另存原字节＋刷 mtime、判据读 `dist/` 且变异后重编译、台账挂号（第四步）；扣 1：窗口内仓级编译被别席在途件打断一次 |

**均分 95／100，无 S1 → PASS。**
