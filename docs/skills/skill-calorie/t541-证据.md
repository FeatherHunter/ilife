# 场景04（运动 39 页）整改批 · 收口证据（#541）

**一句话**：三个整改目录里的 39 页合并进同一个交付目录（**合并是复制，不是重出**，产物一字节未改），
出两张验收墙与一张总索引，生成器入仓且**正反例都走过**。墙与索引给负责人滚动验收用 —— 它们是**证据**，
不是判据（`docs/agents/视觉验收墙.md` §0／§4 那条红线：别把「墙出得来」当成「验收过了」）。

---

## 一、写集声明

**新建（入仓，本次提交的全部内容）**

| 件 | 作用 |
|---|---|
| `docs/skills/skill-calorie/t541-墙生成器.mjs` | 两墙生成器；起点照抄 `t154-mobile-wall.mjs`，**补上 §6.2 的 `dropped` 判据** |
| `docs/skills/skill-calorie/t541-索引生成器.mjs` | 总索引页生成器；末尾固定「有意不出产物及其原因」一节 |
| `docs/skills/skill-calorie/t541-逐格缺陷清单.md` | 逐格缺陷清单**模板**（表头与判据；本票不预填缺陷） |
| `docs/skills/skill-calorie/t541-证据.md` | 本件 |

**新建（不入仓，`.scratch/t541/`）**：`out/`（39 件产物 ＋ `t541-清单.json` ＋ 两墙 ＋ 索引）、
`t541-合并.mjs`（合并／断言／造清单）、`t541-假条目.mjs`（反例夹具）、`t541-核验.mjs`（只读核验读数）、
`t541-对账行.mjs`（对账声明导出）＋ 各步日志。

**一行未碰**：`packages/**`（本票不碰源码，故没有编译与包测试；产物是**复制**来的，不是本席重出的）、
`docs/agents/**`、三处来源目录（`t523/out`／`t543/out`／`t544/out` 一件未改）、别人的票号目录。
git 只 `add`／`commit` 上表四件，不切分支、不 reset、不 stash、不 push。

**口径出处**：`docs/agents/视觉验收墙.md` §5（墙页与产物同目录、生成器入仓、收口证据落 `docs/skills/<件名>/`）、
§6.2（`dropped` 与 `dead` 两条一起判）、§6.3（每格一件真产物／成对出墙／宽产物整体缩／每格标签三件）、
§7（不加 `loading="lazy"`；清单不带签名）、§8（收口自检清单）。

---

## 二、合并读数（任务一）

**来源三处的当刻读数**（与票面「17／13／9」的对照）：票面那三个数是**三个页族的页数**——
`汇总／明细／对照目标族` 17 页（第 14–30 号）、`写后回执族` 13 页（第 01–13 号）、
`分布／力量／有氧／复盘／趋势族` 9 页（第 31–39 号），三族相加恰 39。
但**三个目录各自都被上一席铺成了全批副本**，盘上件数是：

| 来源 | 盘上件数 | 与取用那份的关系 |
|---|---|---|
| `.scratch/t523/out`（票面点名「为准」） | 39 件 | **39 件全部取自这里** |
| `.scratch/t543/out` | 39 件 | 14 件与 t523 逐字节相同、25 件是另一版渲染 |
| `.scratch/t544/out` | 37 件（**缺 09、11**） | 37 件全部是另一版渲染 |

⇒ 按票面「以 `t523/out` 为准，其余两目录补它缺的」执行的结果是 **t523 命中 39、t543 补缺 0、t544 补缺 0**；
另两处那 39 份同名件**没有被取用**（它们仍在原目录，一件未动），每一份的去向都逐条写进了合并日志
（`t543`：14 逐字节相同／25 内容不同；`t544`：37 内容不同），**没有静默丢弃**。

**逐件三条断言**（票面第一段）：① 以 `<!doctype html>` 起；② 含 charset；③ 含样式段 —— 39/39 全过。
**齐 ＋ 不多**：目录内 42 个 `.html` ＝ 39 件产物 ＋ 两张墙 ＋ 一张索引；此三者之外的 `.html` 一件都没有。

**摘要行**：`合并：39 件、缺 0`（`来源命中：t523（优先）=39　t543（补缺）=0　t544（补缺）=0`）。

**清单** `D:\ilife\.scratch\t541\out\t541-清单.json`：39 行，字段
`seq／wake／kind／family／check／file／bytes／from`；**不带签名**（BOM 判据：`false`）；
`family` 三族分布 **13／17／9**，与票面的族划分逐族对上。

---

## 三、两墙与索引的格数与链接数（任务二、三）

| 文件（与产物同目录） | 宽 | 格 | iframe | 页面链接 | `loading="lazy"` | 字节 |
|---|---|---|---|---|---|---|
| `t541-手机墙.html` | 390 | 39 | 39 | 39（＋39 条 iframe src ＝ 读数行里的 78 条） | 无 | 15,833 |
| `t541-桌面墙.html` | 1280 | 39 | 39 | 39（同上，78 条） | 无 | 15,905 |
| `t541-索引.html` | — | — | — | 41 ＝ 39 产物 ＋ 两张墙 | 无 | 16,308 |

- 两张墙**格高一致**（820），格数＝清单条目数＝39；窄的 3 列排（390），宽的 1 列排且整体缩到 600 可视宽
  （`SCALE=600/1280≈0.469`，**视口仍是 1280**，所以墙上看到的就是 1280 档真版式）。
- 每格标签三件齐：**编号 ＋ 点开整页的链接（新标签打开）＋ 一句「这一格该确认什么」**（在格子下面那条）。
- 墙按**页族分组**（10 组），`flex-wrap` 换行铺开 —— 不用横滚，堵掉 §7「右边的格子看不见」。
- **手机端**（用户六条第 ① 条）：墙页与索引页在 390 宽的浏览器里都不横滚 —— 索引页把五列表格转成
  「一行一件」的卡片（各列带小标签），手机墙在 ≤414px 时让出页边距、让 390 的格子正好落满一屏
  （文字块自己留 14px 内边距，不贴边）。**桌面墙不适用**：格子宽 1280 是它的量具，窄窗口放不下，
  页内那句话已直接写明「请在桌面浏览器上滚」。
- 索引末尾有「**有意不出产物及其原因**」一节，本批逐字写明「本批 39 页全出，无有意缺项」。
- 核验读数出处：`t541-核验.mjs`（只读），逐项打 `清单 BOM？false`／`39/39 逐字节相同`／
  `check 里带反引号的行数：0`／`目录 html 件数 42` 与上表三行。

---

## 四、正例／反例三条读数（任务四）

三条读数都取自**最终版**脚本的持锁运行（正例的验收命令与票面逐字相同）：

| # | 命令 | 读数 | exit |
|---|---|---|---|
| ① 正例 | `node docs/skills/skill-calorie/t541-墙生成器.mjs .scratch/t541/out t541-手机墙.html 390` | `手机墙 t541-手机墙.html：39 格；链接 78 条；缺失 0 -> 可发` | **0** |
| ② 反例 | 先在清单里加一条指向 `99-不存在的页（反例夹具）.html` 的假条目，再跑同一命令 | `手机墙 t541-手机墙.html：39 格；链接 78 条；缺 1 件 -> 99-不存在的页（反例夹具）.html（清单第 99 行：盘上没有这个文件）` ＋ 逐件点名行 `缺失 99-…（清单第 99 行：盘上没有这个文件）` | **1** |
| ③ 回绿 | 删掉假条目，再跑 `… t541-墙生成器.mjs .scratch/t541/out`（两墙都出） | `手机墙 …：39 格；链接 78 条；缺失 0 -> 可发` ／ `桌面墙 …：39 格；链接 78 条；缺失 0 -> 可发` | **0** |

③ 之后索引复出：`索引 t541-索引.html：39 件；链接 41 条；缺失 0 -> 可发`，exit 0。

**这条反例为什么要跑**：`t154-mobile-wall.mjs` 有个**假绿灯** —— 它先按「盘上有没有」过滤清单、再只查页面引用，
于是缺件被静默剔掉、自检照样报「缺失 0」（§6.4 点名）。本件把 §6.2 的 `dropped` 判据补上，
②就是这条判据的**活体对照**：清单点名而盘上没有的件，必须红、必须点名。走不出这条，说明自检永远不会红，等于没有。

**过程读数（一并留痕，不进门禁）**：合并脚本第一版把本批自己的两张墙＋索引当成了「盘上多出的残留」，
`t541-合并.mjs` 因此红过一次（`合并：39 件、缺 1`）；当场改成「三件本批验收件不算多」后回绿。
这正是「齐 ＋ 不多」这条判据想抓的东西，只是第一版抓错了对象。

---

## 五、GATE-RUN 对账

对账源：`.scratch/locks/gate-runs.log`（本席窗口 `ticket=541` ＋ `at ≥ 2026-09-15T21:20:00Z`）。
窗口内本席的 `RUN` 条目**逐条**声明如下（26 条 ＝ 24 条实测运行 ＋ 2 条先定号后跑的 git 条目）：

GATE-RUN runId=9dc3fad0-0e95-4f97-95ad-72f30031b09f cmd="node .scratch/t541/t541-合并.mjs"
GATE-RUN runId=edd9f534-00fb-4a98-a009-ad01f61f406d cmd="node .scratch/t541/t541-合并.mjs"
GATE-RUN runId=be921f3b-7fb9-404c-9a8c-6c3725cd72f2 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=67378def-a47b-41c9-8ce7-b0bce26899a4 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=f7611a07-03fc-4e12-a374-f4acc9680256 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=63c35f55-34ba-49b4-a10f-8b018d5c2dd2 cmd="node docs/skills/skill-calorie/t541-索引生成器.mjs"
GATE-RUN runId=4b4710cb-96dd-4703-a35b-7585acaedfcc cmd="node .scratch/t541/t541-假条目.mjs"
GATE-RUN runId=f1b28f9e-ba29-4738-a943-8578b677bda4 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=ee802be7-9db0-4d77-84e5-7e30dc45b5fb cmd="node .scratch/t541/t541-假条目.mjs"
GATE-RUN runId=36d108ec-f1a1-46c9-bd06-74b67b6e127e cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=fb219765-7b3e-426a-acfa-f22a3c318ae1 cmd="node docs/skills/skill-calorie/t541-索引生成器.mjs"
GATE-RUN runId=6953632a-b577-48fb-9bb4-1a73997ee7e9 cmd="node .scratch/t541/t541-合并.mjs"
GATE-RUN runId=6901fc20-9a1e-4433-9302-5a7e00b08f04 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=0e4ee532-1f24-41f7-8136-babb785f4f89 cmd="node docs/skills/skill-calorie/t541-索引生成器.mjs"
GATE-RUN runId=690b283b-1769-424d-a9be-102ac845b3af cmd="node .scratch/t541/t541-合并.mjs"
GATE-RUN runId=2a847921-aa52-4a40-8977-a3f25563ed07 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=22229aba-aa40-4959-ba07-56c02f86ddaa cmd="node docs/skills/skill-calorie/t541-索引生成器.mjs"
GATE-RUN runId=f91dd303-9c67-40cb-a68f-80d92c17649e cmd="node .scratch/t541/t541-核验.mjs"
GATE-RUN runId=85fd232f-3df2-4b7d-bd09-d289cfb11f72 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=f7691ef4-810b-40bf-91fd-c70883608325 cmd="node .scratch/t541/t541-假条目.mjs"
GATE-RUN runId=3acbcee5-724b-4e35-9850-253a186cb081 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=6e28c01d-5593-4661-b6e0-feea4ee857c2 cmd="node .scratch/t541/t541-假条目.mjs"
GATE-RUN runId=83e6f054-a0d7-4472-af1e-2d8ce361aff0 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=b7a2856d-7a23-4777-b1c8-daeed6903615 cmd="node docs/skills/skill-calorie/t541-索引生成器.mjs"
GATE-RUN runId=t541-add cmd="git add"
GATE-RUN runId=t541-commit cmd="git commit"
GATE-RUN runId=95f972ec-cb30-47e1-abcc-cd08d919e77e cmd="node tooling/check-gate-audit.mjs"
GATE-RUN runId=220f446d-6d96-4328-b95d-47a4693b6804 cmd="node docs/skills/skill-calorie/t541-墙生成器.mjs"
GATE-RUN runId=9fa5d411-c98b-4658-aa53-5920f7232fa5 cmd="node docs/skills/skill-calorie/t541-索引生成器.mjs"
GATE-RUN runId=t541-add2 cmd="git add"
GATE-RUN runId=t541-commit2 cmd="git commit"

（`cmd=` 只写命令前缀——对账工具的 `cmdMatches` 认前缀，日志里的 Windows 绝对路径带反斜杠，
逐字抄进来反而容易和转义打架；runId 是一对一绑定的，不会认错条目。）

**放宽留痕**（窗口内有 3 条 `exit=1`，都是**有意跑的红**）：

GATE-RELAX flag=--allow-nonzero reason=窗口内三条红是本票的有意动作：两次自检反例（`f1b28f9e`／`3acbcee5`，
自检必须红并点名那份假件）、一次合并脚本第一版 extras 判据自踩（`6953632a`，当场修好，回绿读数见 §四）。

**复核命令**（本席跑完把读数写进回执第二段）：

```
node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t541-证据.md \
  --ticket 541 --since 2026-09-15T21:20:00Z --until <本批提交落盘时刻> --allow-nonzero
```

**本席实测读数**（第一次对账，窗口 `until=2026-09-15T21:31:00.753Z`）：`RESULT: matched=26/26
auditEntries=7589 scoped=26 undeclared=0` ＋ `gate-audit: PASS`，exit 0。第二次（终态）读数见回执第二段。

末两条 git 条目**先定号后跑**（`--run-id t541-add`／`t541-commit`），故本件落盘时它们已在上面声明。
本件随本批共两次提交：第一次落四件（本件＋两件生成器＋缺陷清单模板），第二次只改本件
（补手机端那条口径与终态读数）；两次都只 `add`／`commit` 自己的件，`--run-id t541-add2`／`t541-commit2` 同样是先定号后跑。
`--until` 取本批**末次**提交落盘时刻：对账工具自身的运行条目在窗口之外，不进反向对账（协议 §2.4-3 的窗口口径）。

---

## 六、负责人怎么看（双击哪两个文件、每格该确认什么）

1. **双击 `.scratch/t541/out/t541-手机墙.html`** —— 390 宽、3 列，滚一遍看**塌列／挤／触摸目标**。
2. **双击 `.scratch/t541/out/t541-桌面墙.html`** —— 1280 宽、1 列、整体缩放，滚一遍看**排布／层级／留白**。
   （两张墙都不需要起服务，双击文件就照看；iframe 里跑的是真 HTML，格子里能点、能滚。）
3. 拿不准的那一格：**点格子标题**在新标签打开整页细看。
4. 逐件查细节时开 `.scratch/t541/out/t541-索引.html`（39 件按页族分组，每件给族／体积／该确认什么）。

**每格该确认什么**：不看墙页上那句话，看**格子里的产物**。三个族的看点不同 ——

- **写后回执族（01–13）**：四条口径是不是各占一行（不再串成一句）；「本次明细」与「当日累计」两块分不分得开；
  改删页的「改前 → 改后」三态表扫不扫得出变了哪条；快照与警示成不成块。
- **汇总／明细／对照目标族（14–30）**：四张读数卡有没有撤掉与窗口条重复的事实；逐日表缺的日子有没有断点口径；
  长窗（60／180／365 天）折线补没补纵轴刻度；对照目标页的环与判决胶囊两态在不在；明细页八列在窄屏塌不塌。
- **分布／力量／有氧／复盘／趋势族（31–39）**：分布条有没有长度而不是只有百分比；窗口卡两格并成一行没有；
  双轴图注明两套刻度没有；窄屏的页内导航是换行铺开还是被横滑藏起来。

**缺陷记法**：逐格写进 `docs/skills/skill-calorie/t541-逐格缺陷清单.md`（模板已备好表头：在哪张墙／症状／要不要改／判定）。
**没看出缺陷也算结论**，但要写清看了哪些格 —— 空表不算看过（§4「人看过」那一行）。

**两条别混**：墙上能验的是**版式**；**墙上看不出错 ≠ 对**（数据对不对、命令键对不对、边界路径走没走到，归机检判据）。

---

## 七、未做项 ＋ 下一手缺什么

1. **逐格缺陷清单本身没填** —— 按票面，那是负责人的活；本票只备模板与判据。
2. **39 页产物内容一行未改** —— 本票是合并与铺墙，不是重出；要改产物回各自的整改票，不在这张收口票里改。
3. **没跑整包编译与整包测试** —— 本票不碰 `packages/**`，一字节源码未改；没有可编译物。三处来源的产物质量
   归各自整改票的机检读数，本票只保证「合起来是 39 件、件件能打开、件件在墙里有格」。
4. **另两处的同名件没有取用** —— t543／t544 那 39 份（含 25 件与 t523 不同版）留在原目录。
   若负责人认为某一族该用另一版，要重出一张票（本票的合并口径是票面给定的「以 t523 为准」）。
5. **下一手缺什么**：负责人的**逐格读数**（缺陷清单）。拿到之后若要点名返修，按页族回各自的整改票；
   若缺陷集中在某一族，不排除要再切一张小票收口。
