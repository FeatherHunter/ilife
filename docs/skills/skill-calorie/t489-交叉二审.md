# t489 交叉二审（地图#158 · 简单票第二人，与一审换人，不派下级）

票面五段已重读（`gh issue view 489`：目标／验收①②③④／不许动／交付物路径／遗留出口）。
一审结论（`t489-复核.md`）：**未完成**（实现静态正确，但复核窗内主干两次 reset 孤立实现提交，当刻干验收①红，独立持锁复跑未执行；非技术 FAIL；五维 70）。

## 一、判定：同意一审“未完成”（80/100，未达 PASS 线）

1. 主依据 intact（二审全部独立复验通过）：实现提交 `ea0a7e0`／`d597390` 仍孤立（`merge-base --is-ancestor` exit＝1 ×2；reflog 两次 `reset` 可见）；当刻干验收①红（跟踪面 `body.ts:317/392` 2 处＋在盘 `dist` 1 处）；独立持锁复跑缺——树已非实现树，二审亦故意停手（零测试执行、零真库写、零跟踪件改）。
2. 二审补强了动态链（GATE 14/14＋stdout 全合，见 §二），但复核的本质动作“在落定树上独立重跑①②③④”仍无法执行；且验收③客观未闭（门红系他票行、`--sync` 未跑、提交信息未点名 `AGENTS.md`）。故上调分数但维持判定。
3. 五维（参考口径，不跨票比较；PASS 线参同地图 PASS 例 t409-96／t254-94，约≥90）：契约一致 19/30（①内容对＋对账补强；②③未独立复跑、③未闭）＋证据真实 20/25（静态全验＋动态 14 对 stdout 全合；扣等待量差额与非落定树）＋新旧对照 17/20（父/子/红绿对照全＋M3/语义双新变异；扣独立执行缺）＋工程红线 15/15（零违规实证，另有守界 unstage 行）＋文档同步 9/10（链完整，唯等待量差额）＝**80**。

## 二、机器证据

- 基线（探针执行时刻 HEAD＝`38c6d0b`；一审时 `bd08a2d`，树继续前移但孤立依旧）：`ea0a7e0`／`d597390` ancestor exit＝1；reflog `reset→e350865`（22:42:49）、`reset→89d9250`（22:43:00）；`git ls-files|grep t489` 仅 `t489-复核.md`（`d597390` 三件不在树）。
- 关键断言独立重跑（票面精确串，跟踪面 `git grep`）：当刻干 `M1=1 M2=1 TOTAL=2` **红**（`body.ts:317` M1／`:392` M2）；孤立 blob `ea0a7e0` hits＝0 **绿**；父版 `ea0a7e0~1` hits＝2（红绿对照成立）；全文件 diff 仅 ±2 行且逐对去尾相等（`ONLY-COMMENT-CHANGE=yes`，无行为改）。
- dist 盲区实证：`git ls-files -- packages/skill-calorie/dist` 为空（跟踪面无 dist，`git grep` 对其隐身）；在盘 `dist/fetch/body.js:271` 有 M1 残留 ×1——被审 `anchor-probe.mjs`（`git grep`＋精确串＋恒 exit 0）看不见它；与一审 `RED_HITS=3（src2＋dist1）` 吻合。
- 新变异 MUT-X（与一审不同，新形状；`review2-mut-m3.ts` 在盘）：`// M3-ANCHOR` 尾注 → 票面精确串 hits＝0（**假绿**）／宽串 `M\d+-ANCHOR` hits＝1（抓住）。小写 `// m1-anchor`（`review2-mut-lower.ts`）→ 精确串 0＋一审宽串 0**双盲**：残留类判据应大小写不敏感（一审探针此节漏过）。
- 新变异 MUT-Y（补一审未做语义变异 B；`review2-mut-sem.ts` 在盘，**未在工作树执行**——禁改他人件＋树非实现树）：`COALESCE(is_deprecated,0)=0`→`=1` 翻转下检索仍绿（证验收①行为盲）；`t398`／`t364` 含 deprecated 钉住行 24 处（如 `t398:91/93/95` 的 `COALESCE…=0` 断言 SQL、`t364:215` 的 `writtenFields is_deprecated`），真翻转必红、非空断言。
- GATE 对账 14/14（共享日志**未轮转**：28 行 14 对俱在，订正一审“已轮转不可复核”）：`eadb97fd:1`（dry）／`0ac72700:0`（tsc）／`6a85c710:0`（t398）／`56271808:0`（t364）／`19db13f5:1`（无参门）／`1b2aa91b:0`／`59ed5489:0`（探针两发）／`ce73a6fe:0`（force 重编）／`26723d0a:0`（联跑）／`6c2a0589:0`（add）／`d003239b:0`（unstage t464 两件，守界）／`27ac615d:0`（commit1）／`b80bb99a:0`（add 三件）／`1ab71c4a:0`（commit2）。stdout 交叉全合：`log-mut-red TOTAL=2/MUT-RED`、`log-mut-green TOTAL=0/MUT-GREEN`、`log-t398 pass9fail0`、`log-t364 pass7fail0`、`log-retest pass16fail0`、`log-commit1 [master ea0a7e0] 1件2±`、`log-commit2 [master d597390] 3件75+`、`log-dry/log-gate` 当时 RED 确系他票两行（`sportPortDocs 763/805`、`goalProgressDocs 452/446`）。
- 等待量缺口（记局限，非造假指控）：有行等待合计 380320ms≈6.3min，自述约 19min，差额约 13min 无行可对。另：`anchor-probe.mjs` 无非零退出分支，两发负向对照的红绿只活在 stdout，gate verdict 本身不载红绿（一审未点明）。
- 台账：当时与当刻 `body.ts` 行均与台账一致（无 body RED 行）；二审只读 `--dry` exit＝1，RED 行已漂为 4 行他票行（`build-help 521/554`、`goalProgressDocs 452/446`、`sportDocs 423/417`、`libraryDocs 396/401`）→ `--sync` 停手正确，但票面③客观未闭。另票面③前提“删注释行数变小”事实有误：行尾注释删除 LF 455→455（已实证），见 §四。
- S3 复核（同意一审归属）：`T362-ANCHOR`×1（`t362-窗口不截断.test.mjs:319` 日志标签，`747ab73` 在）／`CLOCK_ANCHOR`×3（`cmd-read-t11.test.mjs:41/44/238` 时钟常量，`6e34766` 在）——意图明确，不阻塞。
- 零越界零丢数据：`diff --name-only` 仅 `body.ts`（唤醒词／命令面结构未碰）；真库 3072000B、mtime 2026-09-15 15:36:54（证据／一审／二审三方一致，零写入）；测试走 `.scratch/t489/tmpdb`＋mkdtemp（`SKILLS_DB_PATH` 行＋T364 落盘路径为证）。
- 探针：`node .scratch/t489/review2-probe.mjs`（只读：git 读对象／读文件／读共享日志；写仅 `review2-*`；不跑测试、不写真库、无需持锁），26/26 自洽，exit＝0。方法注脚：pwsh `Measure-Object -Line` 对本仓 `body.ts` 给出 360／407 两个错数（真值 LF＝455，node／门脚本一致）；Windows cmd 吞 `^`（`git show ea0a7e0^:…` 须写 `~1`），探针内已避坑。

## 三、路径

- 改动（孤立提交内）：`ea0a7e0:packages/skill-calorie/src/fetch/body.ts`（1 件 2＋2－）。
- 证据（孤立提交内）：`d597390` 三件（`t489-遗留清理证据.md`／`t489-gate-runs.log`／`.changeset/t489-anchor-cleanup.md`；`AGENTS.md` 未动）。
- 一审：`docs/skills/skill-calorie/t489-复核.md`＋`.scratch/t489/review-probe.mjs`＋`review-run.log`。
- 二审新增（本席唯一入仓件＋忽略区草稿，他人文件零改，不 push）：本件 `docs/skills/skill-calorie/t489-交叉二审.md`；`.scratch/t489/review2-probe.mjs`（全文自含逻辑，拷贝即复跑）＋`review2-run.log`（26 行 CHECK 行）＋`review2-mut-m3.ts`／`review2-mut-lower.ts`／`review2-mut-sem.ts`（三个物化变异）。

## 四、返工意见（转票措辞＋未做项）

- 转票①（给编排者，沿用一审并补③改写）：「实现提交 `ea0a7e0`／`d597390` 已被主干改写孤立（reflog：`reset→e350865`、`reset→89d9250`；现 HEAD 已前移，`merge-base --is-ancestor` exit＝1 依旧），当刻干验收①红（src 2 处＋dist 在盘 1 处）。请明确重落方式（cherry-pick 或重做，含 dist 重编与台账行确认）后重派复核，须在落定树上重跑①②③④。另票面③请改写：行尾注释删除不改变 LF（455→455 已实证），“行数变小⇒--sync”前提不成立；重派时③改为“确认台账本行一致（一致则不跑 --sync、不点名 AGENTS.md）”，或“待台账静止后 --sync 全绿”。」
- 转票②（S3，沿用一审）：「`T362-ANCHOR`（#362 日志标签）与 `CLOCK_ANCHOR`（#326 时钟常量）意图明确无需改；嫌形似易混由归属票自行改名，不阻塞 t489。」
- 二审未做项（故意停手）：t398／t364／tsc／告警线门的独立持锁复跑（树非实现树，跑亦不对实现作证，与一审同）；工作树语义翻转的执行（禁改他人件，以静态钉住审计 24 行代替）；13 唤醒词逐条点名（`diff` 单件已结构证明未碰，未逐条列清单）。
