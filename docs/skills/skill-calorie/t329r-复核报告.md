# #329r 独立复核报告（复核 ab12f27，不采信被审文本，全部自跑）

- 基线：`HEAD == origin/master == ab12f27`（`git rev-parse HEAD`／`origin/master` 同值），分支 `master`。
- 树状态：开工即脏（他席在飞，只读不碰）。`git status --porcelain` 见 9 个 M＋6 个 ??（`packages/skill-calorie/SKILL.md`、`package.json`、`scripts/build-help.mjs`、`src/cli/cmd_read.ts`、`src/cli/write.ts`、`src/output.ts`、`src/photo/helpLookup.ts`、`src/profile/index.ts`、`templates/help.html`；`?? docs/agents/命令事实边界.md`、`docs/skills/skill-calorie/t328-动态加载路径-证据.md`、`src/cli/delivery.ts`、`src/cli/readArgs.ts`、`src/profile/receipt.ts`、`src/shared/contentSuffix.ts`）。本席唯一写 `docs/skills/skill-calorie/t329r-复核报告.md`，大输出落 `.scratch/t329r/`（gitignored，不入提交）。
- 被审对象：`ab12f27 docs(329): 铁律五补计数口径＋结构导出核验-证据`，已 push。

## 1 导出计数（HEAD 快照取数，避工作树污染）

### 1.1 10 能力门（`src/<能力>/index.ts`，HEAD）

复现：`git show HEAD:packages/skill-calorie/src/<能力>/index.ts | Select-String '^export'`

| 门 | HEAD raw 行 | 运行名（值） | 类型名 | 对外判定 |
|---|---|---|---|---|
| analysis | 3 | ANALYSIS_COMMANDS、runAnalysisView、runAnalysisWrite (3) | 0 | ≤5 合规 |
| body | 3 | BODY_COMMANDS、runBodyView、runBodyWrite (3) | 0 | ≤5 合规 |
| diet | 3 | DIET_COMMANDS、runDietView、runDietWrite (3) | 0 | ≤5 合规 |
| exercise | 3 | EXERCISE_COMMANDS、runExerciseView、runExerciseWrite (3) | 0 | ≤5 合规 |
| goal | 3 | GOAL_COMMANDS、runGoalView、runGoalWrite (3) | 0 | ≤5 合规 |
| home | 2 | HOME_COMMANDS、runHomeView (2) | 0 | ≤5 合规 |
| photo | 3 | PHOTO_COMMANDS、runPhotoView、runPhotoWrite (3) | 0 | ≤5 合规 |
| profile | 5 | PROFILE_COMMANDS、buildProfileSettingReceiptDoc、buildProfileUpdateReceiptDoc、runProfileView、runProfileWrite (5) | 0 | ≤5 合规 |
| weight | 4 行 | WEIGHT_COMMANDS、getWeightGoalInfo、weightTrend、runWeightView、runWeightWrite (5) | WeightGoalInfo、WeightTrend (2，随值) | 运行面 5，≤5 合规 |
| workout | 3 | WORKOUT_COMMANDS、runWorkoutView、runWorkoutWrite (3) | 0 | ≤5 合规 |

结论：“10 门全部≤5”属实。注意工作树 `profile/index.ts` 因他席 #328 在飞已变为 4（PROFILE_COMMANDS、profileReceiptDoc、runProfileView、runProfileWrite），HEAD 为 5，被审证据表记 4 系脏树快照（见 S3-1），结论不变。

非门不验收：`fetch/index.ts`（旧取数 `export *`）、`render/index.ts`（旧渲染约 80 名）、`triggers/index.ts`（冻结）、`src/index.ts`（包级），与被审口径一致。

### 1.2 强反例抽查（4 件，HEAD）

- `goal/read.ts`：`git show HEAD:packages/skill-calorie/src/goal/read.ts | Select-String '^export' | Measure`＝9（viewGoal×9）；`git grep -n "goal/read" ab12f27 -- packages/skill-calorie/src` 空（exit 1），同目录唯一引用 `goal/commands.ts:27 from './read.js'`。raw9→真对外 0，属实。
- `photo/photos.ts`：raw21（`Select-String '^export' Count`＝21）；`git grep -n "photo/photos.js" ab12f27 -- packages/skill-calorie/src` 唯一 `render/wizardPort.ts:24 import { listPhotos }`。raw21→1，属实。
- `workout/planStore.ts`：raw26（Count＝26）；`git grep -n "workout/planStore" ab12f27 -- packages/skill-calorie/src` 仅 `render/exercisePort.ts:12 getPlan`、`render/planPlate.ts:10 getPlan,validatePlan`＋`:11 type PlanInput,PlanSessionRow`（另 `workout/wizard.ts:6` 注释非 import）。raw26→4 且调用方全 `render/*`，属实。
- `analysis/review.ts`：raw14（RangeType、parseRange、DayIntake、DayBurn、WeightLogPoint、FitnessSession、TopFood、FiveDims、query5dims、NutritionMatch、nutritionMatchRate、AnomalyDay、DerivedReview、deriveReview）；`git grep -n "analysis/review" ab12f27 -- packages/skill-calorie/src` 空（exit 1，测试 `dist` 引用不计，管辖不管测试）。raw14→0，属实。

### 1.3 8 件残留超数调用方核验

复现示例：`git grep -n "body/bodyPlate" ab12f27 -- packages/skill-calorie/src` 等（§1.2 同款）。

| 件 | raw | 真对外 | 调用方实测 | 是否旧 barrel |
|---|---|---|---|---|
| body/bodyPlate.ts | 6 | 6 | render/html.ts、render/index.ts、render/sportDocs.ts | 是 |
| photo/helpCenter.ts | 25 | 15（10 名零引用） | render/index.ts | 是 |
| photo/photo.ts | 16 | 16 | render/html.ts、render/index.ts、render/wizardPort.ts | 是 |
| exercise/exerciseStore.ts | 22 | 22 | fetch/index.ts:5 `export *`、render/exercisePort.ts、render/sportDocs.ts、analysis/exerciseReview.ts（3 名） | 主旧＋1 新（已披露） |
| goal/goalExtraPlate.ts | 6 | 6 | render/html.ts、render/index.ts、render/trendDocs.ts | 是 |
| analysis/deficit.ts | 7 | 7 | goal/goalPlate.ts、home/home.ts（新）、src/index.ts（包级）、render/*（旧） | 混合（已披露） |
| analysis/series.ts | 15 | 10 | goal/home/weight（新）、render/*（旧）、shared/params.ts | 混合，新占大头（已披露） |
| analysis/utils.ts | 13 | 10 | body/diet/exercise/goal/home/photo/profile/weight/workout 8 能力（新）＋render/*＋shared/params.ts | 新占大头（已披露） |

结论：8 件真超数属实；“调用方几乎全是旧 barrel”总述不精确（见 S3-2），但详表对新能力直调如实披露，未隐瞒。

## 2 文档改动（`docs/agents/structure.md`）

`git show ab12f27 -- docs/agents/structure.md`：仅铁律五判据一句由“数得出有几个”补为“「对外给」只数出了本能力目录的名字（目录内自用不算、raw 不算、零引用不算）；验收以能力门为准，分发器非门；过渡期旧 barrel 深引记债务”。

判定：自洽、无放宽。线“不多于五个／字段八个”未动；计数法与同份写代码第三步“只有出这个目录才算对外”一致；债务是记账（后续搬调用方）非豁免；`docs/` 全仓 25 处旧口径引用皆“≤5 这条线本身”，无第二种对外定义，无冲突。series/utils 新能力直调用途在证据记为“事实共用核、远期归共用位”，由结构标准共用件条覆盖，不属旧 barrel 句的扩大解释（见 S3-2）。

## 3 复跑（持锁 `run-locked --ticket 329r --lock-dir .scratch/locks-land --stale-minutes 30`，direct node）

| 门 | 读数 | runId／exit |
|---|---|---|
| `node node_modules/typescript/bin/tsc -b` | exit 0 | 953b08c2-ae4d-4779-9acb-8b5c83a94308 exit0 waitedMs1 |
| `node packages/skill-calorie/scripts/gen-cli.mjs --check` | GEN-STAMP FAIL（`dist/.gen-inputs.json version 不是 2`，印记不可信不判绿） exit 1 | c0772faa-a711-4a1c-993e-0dc4269eea28 exit1 |
| 全量 `node --test test/*.test.mjs packages/*/test/*.test.mjs` | tests1492 pass1491 fail1（`cmd-registry-294.test.mjs:133 #320 calorie.zz-mut`） exit 1 | 498aa9e9-02e8-4b6c-ab55-77d2253b0d56 exit1 |
| `node --test test/calorie-routing-81.test.mjs` | tests8 pass8 fail0 exit0 | 456da2ea-aeff-4953-8c30-d5089d2caf9e exit0 |
| `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5，UNACCOUNTED=0，树矛盾0 exit0 | b9566d35-ba0a-4a18-8637-84c16bcf8f89 exit0 |

日志：`.scratch/t329r/tsc.log`、`gen-check.log`、`full.log`、`routing.log`、`probe.log`；留痕 `.scratch/locks-land/gate-runs.log` 一一对应。

归因：两红皆他席在飞所致，与被审无关。被审提交源码零改动（§4），不可能引入 `zz-mut` 或印记版本；`zz-mut` 在 HEAD（`git grep -n "zz-mut" ab12f27` exit1）与当刻工作树（`grep zz-mut` 0 命中）皆无，系并发窗口残留／变异探针瞬态；印记 FAIL 系 9 个 M（`package.json`、`build-help.mjs`、`cli/*` 等恰为 gen 输入）未重打印记。本席按纪律未 `build`/未清树，未复现被审当时 1492/1492＋gen 101 键全绿，记环境差异，不记被审缺陷。

## 4 反假绿

`git show --stat --oneline ab12f27`：2 files（`docs/agents/structure.md` 1 增1 删；`docs/skills/skill-calorie/t329-结构导出核验-证据.md` 120 增），`git show --name-status`：M 前者、A 后者，无 `packages/` 源码。源码零改动属实。

## 5 打分（100 制，PASS 要求 S1=0 且 S2=0）

- S1（交付缺陷）：0 条。
- S2（过程违规）：0 条（未见 amend/force/rebase、票面、100%、close；持锁直调合规）。
- S3（记账问题）：
  - S3-1（-2）：证据§1 门数快照点未注明。HEAD 门为 5，表记 4（脏树值）。复现：`git show HEAD:packages/skill-calorie/src/profile/index.ts | Select-String '^export'`。结论不变，纯记账。
  - S3-2（-2）：总述“8 件几乎全是旧 barrel”以偏概全。series/utils/deficit/exerciseStore 含新能力直调（utils 8 能力、series goal/home/weight 等），详表已披露但总述未限缩。复现：`git grep -n "analysis/utils" ab12f27 -- packages/skill-calorie/src`、`git grep -n "analysis/series" ab12f27 -- packages/skill-calorie/src`、`git grep -n "analysis/deficit" ab12f27 -- packages/skill-calorie/src`。

总分 96，判定 PASS（S1=0 且 S2=0）。

## 6 提交与未做项

- 本报告限定路径提交 push（仅 `docs/skills/skill-calorie/t329r-复核报告.md`，`git add` 限定单件，`--amend/--force/rebase` 未用）。
- 未做项：干净树复跑（并发窗内禁 stash/clean，无法复现被审当时全绿，已按当刻脏树如实记账）；`snapshot-check`（派单§3 五项外，被审已记账）；源码收口（8 债务留后续搬迁票，本票不动）。
