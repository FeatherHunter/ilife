# #565 复核（二席）：目标预检页复制区回旧例三通道

**被审对象**：提交 `e93c9211799f79d5a96486002f5b0598f092cb3e`（「修复(565): 预检页复制区回旧例三通道…」，3 files changed）＋紧接一笔 `23a2b292ddc4e0eaab69522c9f3204f9e68bae2d`（只补证据件 §九）。
**复核席**：ISSUE #565 二席（独立复核，不采信实施者结论；只读被审件，不实施）。
**复核时的工作树**：`HEAD = 23a2b29`；本票写集三件无未提交改动。
**被审源件**：`packages/skill-calorie/src/goal/precheck.ts`，sha256 = `e1d1ddff94d3e185b01db22896ed9c5778cde6b0ab06aff8d3b0a3d8246f1f45`（前 12 位 `e1d1ddff94d3`，与实施者自报一致）。

---

## 一、圈定范围（读 git，不看实施者的说法）

| 判据 | 读数 | 结论 |
| --- | --- | --- |
| 被审提交改了哪些件 | `git show --name-only e93c921`：`docs/skills/skill-calorie/565-证据.md`／`packages/skill-calorie/src/goal/precheck.ts`／`packages/skill-calorie/test/565-precheck-copy.test.mjs` | 3 件，与票面写集逐行相同，**多一件即 S1 这一条不成立** |
| 紧接那一笔 | `23a2b29` 只改 `docs/skills/skill-calorie/565-证据.md`（1 file changed） | 同上 |
| 工作区与提交是否一致 | `git hash-object` 对三条路径：源件 `78eb7828…`＝提交内 blob；用例 `c029dcb5…`＝提交内；证据件 `1c4fd9f6…`＝提交内 | 三件逐字一致，工作区没有在途改动 |
| 写集之外的件 | `git status --porcelain -- <写集三件>` 空；`src/shared/**`／`src/render/**`／`packages/base-render/**` 一件未动（被审提交的 `--stat` 里也没有） | 共用件与冻结表一行未改，与票面「只许动 src/goal/precheck.ts」相符 |

---

## 二、独立重跑（我自己的 runId，全部经加锁包装器 `node tooling/run-locked.mjs --ticket 565B-R -- …`）

```
GATE-RUN runId=1520252a-a160-47fd-8967-3c3860ef91c9 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"            exit=0
GATE-RUN runId=966164b5-9e5c-4c82-8230-eafb2f26a211 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force"    exit=0
GATE-RUN runId=56841fa0-c380-4fdc-b83e-53cbb2a9cc16 cmd="node --test packages/skill-calorie/test/565-precheck-copy.test.mjs"        exit=0  ℹ tests 5／pass 5／fail 0
GATE-RUN runId=45beafe3-91c4-46eb-a008-4a1f110394c4 cmd="node --test …/goal-wizard-251 …/t561-goal-no-source …/goal-weight-deadline-548 …/goal-receipt-253 …/goal-result-254 …/goal-upsert-127 …/goal-weight-b1-390 …/t467-goal-progress"  exit=0  ℹ tests 32／pass 32／fail 0
GATE-RUN runId=177f6a87-deea-4553-ab15-52fe837bed6b cmd="node .scratch/t565B-R/probe.mjs"                                            exit=0  PROBE: 11 条 → 红行 0 条
GATE-RUN runId=15d220da-9c1c-4f82-bf7b-86f4f741f7ec cmd="node .scratch/t565B-R/mutate.mjs"                                          exit=0  （内中：两条变异各自改坏 probe=1／还原 probe=0）
GATE-RUN runId=1ea74682-f02a-469a-8bf3-aacae0cb7274 cmd="node .scratch/t565B-R/mutate-with-ticket-test.mjs"                          exit=0  （改坏：被审用例=1、断言红 3 行；还原：用例=0）
GATE-RUN runId=8eee7fd5-2dfa-48ea-ba13-71c6bebcf3bd cmd="node .scratch/t565B-R/probe-nowake.mjs"                                     exit=0
GATE-RUN runId=7216b72b-1c1b-43f7-b992-bfce12c90164 cmd="node .scratch/t565B-R/checks2.mjs"                                         exit=0
GATE-RUN runId=e6679f06-db1d-4a33-aa88-02aeed37576a cmd="node packages/skill-calorie/scripts/check-warning-line.mjs"                 exit=1  （红行指向别席在途件：src/exercise/receipt.ts LF=430）
GATE-RUN runId=f8c2e4ff-7fc4-4180-b55c-0f8f6a7ac73d cmd="node packages/skill-calorie/scripts/check-warning-line.mjs"                 exit=1  （同上，按并发纪律重跑一次仍红 → 记录后上报，不代改）
```

**编译那一条我另加了一道证明**（怕「增量空跑出的假绿」）：`--force` 全量重编 exit=0，且重编前后 `packages/skill-calorie/dist/goal/precheck.js` 的 sha256 都是 `C695368B931B447DD7C4676DD00344280D323D5333A2B78F8D73625ADEE0093D`——即**盘上那份产物就是提交里的源件编出来的**，后面所有真 CLI 读数跑的确实是这一版实现，不是旧产物顶班。

**兄弟用例我跑得比实施者宽**：不只他点名的 5 件，把目标域 8 件全跑了（另加 `goal-upsert-127`／`goal-weight-b1-390`／`t467-goal-progress`），32/32 绿，无回归。

---

## 三、新试法 ＋ 看真产物

### 3.1 我自己写了两条变异（不照抄实施者那份脚本）

在定稿源件（sha `e1d1ddff94d3`）上改一行、编译、跑**我自己的探针**（`.scratch/t565B-R/probe.mjs`：手写标签分词器，元素级数按钮，不复用被审用例的任何断言）：

```
MUT-READING MUT-A 删一条通道（数据位整行拿掉）   compile=0 probe=1 红行数=110
  RED-LINE [定营养目标] copy-btn 元素应 3 个，实测 2：["复制指令","复制日志"]
  RED-LINE [定营养目标] 「复制数据」按钮应恰 1 颗，实测 0
  RED-LINE [定营养目标] 三格式菜单项 data-fmt="text" 应恰 1 处，实测 0（数据通道没了？）
  RESTORE OK（逐字还原 sha=e1d1ddff94d3）→ 还原态 compile=0 probe=0 红行数=0
MUT-READING MUT-B 指令那颗改成与日志同名（复制日志）compile=0 probe=1 红行数=55
  RED-LINE [定营养目标] 「复制指令」按钮应恰 1 颗，实测 0
  RED-LINE [定营养目标] 「复制日志」按钮应恰 1 颗，实测 2
  RESTORE OK（逐字还原 sha=e1d1ddff94d3）→ 还原态 compile=0 probe=0 红行数=0
```

改坏在编译层永远 exit=0（是行为缺陷、不是类型错误），判据红在用例层——这正是本票要的两面。

### 3.2 再拿**被审的那份用例**当探针走一遍（证明交付件的判据真有牙）

我自己的变异（删数据位那条通道）＋跑被审用例，复现出实施者 `MUT-READING A` 的三条断言**逐字**：

```
改坏态：compile=0 被审用例=1 断言红行=3
  「定营养目标」预检页 copy-btn 应恰 3 个（指令／数据／日志各一颗），实测 2 个
  「定营养目标」预检页 复制数据的纯文本载荷空了
  「定营养目标」预检页 整页「复制数据」应恰 1 次（只有复制区在用这个名字），实测 0
RESTORE OK（sha=e1d1ddff94d3）→ 还原态：compile=0 被审用例=0（绿）
```

### 3.3 11 件预检页我自己重出到临时目录（临时库＋真 CLI，不碰真库，也不碰验收墙）

配方：`mkdtempSync` ＋ `SKILLS_DB_PATH` ＋ `docs/research/t81-seed.mjs` 的 `seedFull()` ＋ `CALORIE_TODAY=SEED_TODAY`，逐条跑
`calorie.view.goal-wizard --params '{"wake":"<词>"}' --html <临时目录>`；产物落 `.scratch/t565B-R/out/`。我自己的判据（元素级）：

| 写词 | exit | copy-btn | 指令／数据／日志（按钮标签计数） | 三格式菜单项 | 载荷字数 指令／数据／日志 |
| --- | --- | --- | --- | --- | --- |
| 定营养目标 | 0 | 3 | 1／1／1 | 3 | 255／74／280 |
| 定营养目标(自动算) | 0 | 3 | 1／1／1 | 3 | 232／74／261 |
| 定体重目标 | 0 | 3 | 1／1／1 | 3 | 165／74／228 |
| 定体重目标(自动算截止) | 0 | 3 | 1／1／1 | 3 | 211／74／246 |
| 定体重目标(含起始日) | 0 | 3 | 1／1／1 | 3 | 201／74／278 |
| 定饮水目标 | 0 | 3 | 1／1／1 | 3 | 132／74／232 |
| 定饮水目标(自动算) | 0 | 3 | 1／1／1 | 3 | 164／74／261 |
| 一键定全套目标 | 0 | 3 | 1／1／1 | 3 | 283／74／258 |
| 改营养目标 | 0 | 3 | 1／1／1 | 3 | 261／74／267 |
| 改体重目标 | 0 | 3 | 1／1／1 | 3 | 180／74／230 |
| 改饮水目标 | 0 | 3 | 1／1／1 | 3 | 137／74／232 |

另外每条都过了：无一颗禁用态复制钮、`ilife-help-copy-prompt` 与 `ilife-copy-log` 各恰 1 处、菜单开合器恰 1 个、三条载荷互不相同、**指令载荷逐字等于** `src/triggers/scene-06-goal.ts` 里该条词的 `prompt_template`、页内 `<pre>` 预览块同字、三格式载荷互不相同。

**「数据通道真的还在」这一条我单独断过**（不只数按钮）：每条产物里 `data-fmt="text"`／`"json"`／`"csv"` 各恰 1 处且载荷非空、`ilife-copy-menu-wrap` 菜单容器在场、开合器带 `aria-haspopup`——即三格式菜单这一段确实在页面上，不是被一颗空壳按钮顶替。

### 3.4 肉眼排布（截图后按像素级读数，不只信字串）

`vision_html_screenshot` 把 `.scratch/t565B-R/out/定营养目标.html` 整页拍成 1200×1882 的图（`D:\ilife\.dsh-vision-router\artifacts\…-shot-1200x900-fullpage.png`），再据图读数：

- 底部复制区**两行**：第一行只一颗「复制指令」；第二行「复制数据」＋「复制日志」（左→右）。
- 「复制数据」那颗带下拉三角（菜单标记）在场；三颗**没有一个**是灰底禁用态外观；无重叠、无裁切、无挤压。
- 三颗按钮的可见名字就是三个名字各一次，页面上没有第二颗同名按钮。

（第一行只有一颗按钮＝指令那一排自成一行的旧例形状：旧例同样是「prompt 那一排 ＋ 复制区那一排」两行；本票只是把那排里那颗兜底占位撤了。）

### 3.5 旧例对照：我自己跑了仓内旧例那一页

`calorie.view.profile-wizard`（档案预检页，`src/profile/setup.ts`）在临时种子库上真跑：

```
按钮 4 颗：复制指令(ilife-help-copy-prompt) ／ 复制日志(ilife-copy-log, disabled) ／ 复制数据(菜单开合器) ／ 复制日志(ilife-copy-log, 真)
data-fmt 菜单项=3 ｜ 可见名字 复制指令=1 复制数据=1 复制日志=2
「复制数据·纯文本」载荷 97 字＝【calorie · 档案预检】＋ 五项读数
```

即实施者 §9.1 那条判据来源**属实**：旧例逐字写 `copyArea({ prompt, data, log })` 也到不了「三颗、名字各一次」——公共层 #336 兜底会补一颗**禁用态**「复制日志」（实测就是那一颗 disabled）。所以本票不照抄那一句实参、改成「指令那颗挂日志槽位（单独一颗，不触发兜底）＋数据位与日志位合成一个 `copyArea`」，是为达成票面判据的必要做法，不是无谓偏离。

### 3.6 无 wake 进来那一档（页面上没有 prompt 的那次）

`--params '{"profile":"cut"}'`：exit=0，2 颗按钮`复制数据`／`复制日志`，无同名、无禁用态、三格式菜单仍在、页上无 prompt 预览块——与共用件「prompt 空串不出预览与按钮」的口径同，页面上不会留一颗点了没反应的死按钮。

### 3.7 实施者的机器读数复核

- 数值：`255／74／235／101／280` 五个字数**全部复现**（指令、纯文本、JSON、CSV、日志）。
- 运行留痕：证据件 §9.4／§9.6 点名的 **16 条 runId 逐条**在 `.scratch/locks/gate-runs.log` 里找得到，cmd 文本与 `exit=0` 与自报一致（含两条 git 写操作）。
- 变异读数：他 `MUT-READING A` 的三条断言我在 3.2 里逐字复现；`MUT-READING B` 的「copy-btn 实测 5 个」与其脚本做法（把 prompt 位那条老路补回、同时保留新的指令那颗）自洽可核（`.scratch/t565B/mutate.mjs:28-29`）。

---

## 四、缺陷清单

### 本票引入：无

四条 S1 逐一排除：① 用户可见的能力删除（「复制数据」三格式通道）**不存在**——11 件各恰一颗、菜单与三份载荷都在（§3.3）；② 同名重复**不存在**——按钮标签口径三个名字各 1，无禁用态占位（§3.3／§3.4）；③ 禁区文件**未动**——被审提交只碰写集三件，共用件与冻结表一行未改（§一）；④ 证据**无造假**——16 条 runId 可查、五个字数读数复现、源件 sha 对得上、两条变异读数可复现（§3.7）。

### 本票范围（备注，非缺陷）

- **指令那颗按钮借的是日志槽位**：`copyZone` 里那句是 `renderActionBar({ copyLog: { actionId: CALORIE_COPY_ACTION.actionId, label: CALORIE_COPY_ACTION.label, text: v.prompt } })`——实参名是日志槽位，但货真价实的是冻结表里的指令 actionId（`ilife-help-copy-prompt`）与文案（`复制指令`），按下的也是 prompt 原文。**页面语义与行为都对**（我按 `data-action-id` 与 `data-t` 两处独立核过）。这一处偏离的来路（公共层 #336 兜底在本票写集之外、不许改）已逐段写进源码注释与证据件，属可接受的取巧；若日后有人给这个槽位加「日志专用」的形态，需要连带看这一处。

### 范围外发现（S3，只记不判）

1. **「复制数据」这条通道的载荷是页面信封的读数投影，不是本页那几张表的逐行数据**。实测纯文本 74 字＝页名 ＋ `hasGoal／recommendReady／missingCount／fillCount` 四项读数，JSON 是同一个信封、CSV 四行。旧例档案预检页**同形**（97 字＝页名 ＋ 五项读数），这是共用件 `copyArea` 的数据位口径（信封投影），不在本票写集。若编排者要「那几张表的逐行数据」进这条通道，那是另票（要动页面信封与取数口径，票面明确划在本票之外）。
2. **验收墙那 11 件预检产物仍是改前那版**（工作区现存件实测：copy-btn 2 颗、标签只有`复制指令`／`复制日志`、`data-fmt` 0 处）——墙是编排者的验收面，本票写集不含它的件，实施者已在未做项里点名。**给编排者两条口径提醒**：① 重出要**先确认 dist 是 e93c921 这版**再跑 `produce.mjs`，重出后应读到 copy-btn＝3、三个名字各 1 颗（按钮口径）；② `produce.mjs` 全程共用一份临时库、且每条的写类命令先于该条的预检页跑，所以墙上的预检页数值是「写后」的，跟「每条词一份干净种子库」的复核读数不会逐字相同（我实测 `改饮水目标`：墙上 `2200 ml／67.5 kg／2026-09-19`，我这边 `2000 ml／68 kg／2026-12-31`——这是库状态口径差，不是缺陷）。
3. **告警线门当前是红的，但红行不指向本票**：`RED 漏报（台账没有）：src/exercise/receipt.ts LF=430`。该件不在被审提交里（它最后一笔提交是 `ed87c79`）、不在本票写集里，且工作区里它是**在途改动**（`git status` 显示 ` M`，mtime 2026-09-16 03:22:16，正好落在我这轮复核窗口内，是别席在编辑）。按并发纪律我记录后重跑一次（仍是同一条红），停手上报，不代改、不动别人的在途件。另附一条读数备查：该件在 `HEAD` 里的内容是 354 LF，台账里也没有它的行——这一条与本票无关，归其归属席处理。

---

## 五、判定

**PASS，96／100。**

- 票面三条硬判据（三颗按钮且名字各一次／三条通道一条不少／三份载荷非空）我用自己的元素级判据在**自己重出的 11 件真产物**上全部复现，并另加了肉眼排布读数与「三格式菜单真在场」的独立断面。
- 正反两面都走过：我自己的两条变异各自改坏必红、逐字还原必绿；拿被审用例当探针也复现出同样的三条红行，交付的判据确有牙。
- 范围、禁区、证据三件事都干净：提交只有写集三件、共用件一行未改、16 条 runId 与五个读数可查。
- 扣分只在两处轻微项：指令那颗按钮借日志槽位（可接受取巧，已入注释）；「复制数据」载荷只有四项读数（属旧例同形的范围外口径，见 §四）。

---

## 六、未做项

1. **验收墙 11 件未重出**：`docs/skills/skill-calorie/scene06-验收墙/**` 是编排者的验收面，本票写集与我的复核写集都不含它——重出归编排者（口径提醒见 §四.2）。
2. **告警线门未替别席回绿**：红的件不是本票的（§四.3），按并发纪律不代改；`--sync` 能把台账补齐，但那是该件归属席的动作。
3. **未 push**：按票面口径。
4. 本复核只写了一件：`docs/skills/skill-calorie/565-复核-二席.md`；探针与长输出落 `.scratch/t565B-R/`（未受版本控制的临时区），未进任何提交。
