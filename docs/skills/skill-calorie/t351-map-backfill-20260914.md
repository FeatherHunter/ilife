## Destination

场景 05 健身计划 的 **32 条唤醒词**在 DSH 里**都能从头走到尾**：说唤醒词 →（写类流程先出过程型 HTML）→ 复制 prompt → AI 用命令执行 → 拿到**结果型 HTML 落盘、回执给绝对路径**。
两条同时达成即本图完成：**① 本图 32 条唤醒词都跑得通（其中 23 条现在跑不通）**；**② 产物是真实 HTML 文件、能打开、由用户肉眼验收过**。

## 进度：90%

下一步：**收口**。#346／#347／#348 已关；32 条唤醒词里 27 条已 `exec` 且全量重跑出产物（机检逐份全绿），196–200 外部 5 条按本图 Out of scope 记 `non-exec`；剩 #349／#350 待用户签字、#351 端到端与肉眼终审。视觉重做已完成待审（见 Decisions so far 末条）。

## Notes

- **本图不是第一张。** 前一张（场景 04 运动）已经立起公共层的一部分，本图**先复用现成的，再补自己缺的**（用户裁定：公共层跟着场景一集一集长）。
- **本图要交货**（用户 Q6／照 #131 与 #143 的惯例）：默认 plan-don't-do 在此不适用——必须交出能跑的产物，不只是决定。
- **第一件事：跟老技能对账，防「幻觉词」**。开工前必须读 `D:\2Study\StudyNotes\SKILLS\卡路里\scripts\_triggers.py`（436 条唤醒词的权威表，621 KB），核对本图每条唤醒词**当年到底是怎么做的**，再判断它属于哪一种：**「老技能做过、新仓没接命令」**（绝大多数）／**「必须多步交互」**／**「新架构规格划出去不做」**。
  已查证的事实：新仓判「跑不通」的 95 条，老技能里 **95/95 全都存在**，其中 **91 条**带完整的 `output_type` 与模板定义——它们是「老技能有、新版本没接通」，不是「从来没做过」。**不许只看新仓 `routing.ts` 的标注就下结论。**
- **公共层怎么长**（用户 2026-09-11 裁定）：**只要分析下来有可能被多个场景用到，就提到公共层**，不等「已经有两个场景在用」。开发时**优先去公共层找**，找到了就用，不重复造。更通用的（别的技能也用得到的）上移到 `packages/base-render/src/`；只对卡路里多个场景有价值的留在卡路里包内。
- **模板怎么归**（用户 Q4 答 B）：**本图自己把用到的模板名归好类**（合并成哪几类、每类归谁管：给所有技能用／只给卡路里用／只此一处用），写进本图正文的 Decisions so far；**全批最后人工收口**。不预先建一张全批的模板总表。本图要归的模板名见下。
- **模板归类**：本图页面用的模板名**由本图自己归类**（合并成哪几类、每类归谁管：给所有技能用 → `packages/base-render/src/`；只给卡路里多个场景用 → 卡路里公共层；只此一处用 → 就地写），写进本图正文的 `Decisions so far`；**全批最后人工收口**（用户 Q4 答 B）。本图要归的模板名见下。
- **判定三档（用户已裁定，别加码）**：① 文件存在（程序：真出口跑一次，断言落盘／绝对路径／可打开／字节如实）；② 内容与字段正确（程序：结构化断言，照 `packages/skill-calorie/test/help-delivery-139.test.mjs` ＋ `help-shell-134.test.mjs`）；③ **视觉由用户肉眼看，不写大量脚本做视觉判断**。
- **用词纪律（硬性）**：一律照 `docs/agents/wording.md`。**禁止**再出现「键／写键／补写键」「执行层轨／渲染轨」「壳」「闭环／打通／收敛／沉淀」等词；「命令」就是 `calorie.*` 那些东西，「会改数据库的命令」就是写操作。
- **会话纪律**：不弹窗问，问题写在对话正文里；用户只负责扫码（2FA）。
- **并发纪律**：只 `git add` 自己的文件；`pnpm test` 会顺手改写其他技能的 `SKILL.md`（跑完还原）。
- **本图预期贡献给公共层的部件：** 计划视图页、进度型中间反馈页
- **本图要归类的模板名（共 6 个）**：workout_plan_view.html×13、crud_receipt.html×11、process_progress.html×3、exercise_review.html×3、plan_builder_wizard.html×1、contraindication_report.html×1。旧技能 `templates\` 目录里这些名字**绝大多数有实物可照**（#131 的成功路子：照老实物分析，不凭空设计）。注意 `goal_config.html` 在旧目录里**找不到**（只有它没被引用的两个分身 `goal_config_nutrition`／`goal_config_water`），这一类要新设计。

## 任务清单

<!-- 原生子议题边与原生阻塞边才是准；本清单只作索引 -->

| 序 | 票 | 类型 | 被谁阻塞 |
|---|---|---|---|
| 0 | #168 本图「公共层提取与边界」（对账证据；其原定第 2–5 步规划已由下表取代，不另建重复票） | grilling ＋ task | — |
| 1 | #346 读筛选形状验证（看今天练什么） | task | — |
| 2 | #347 读筛选铺开（其余 7 条） | task | #346 |
| 3 | #348 写命令·创建类（5 条） | task | #347 |
| 4 | #349 写命令·变更类（5 条） | task | #348 |
| 5 | #350 外部 5 条有结局（落地×3／训记×2，条件票） | task | —（调研可并行，落码等用户裁定） |
| 6 | #351 真机端到端 ＋ 肉眼终审 | task | #347 ＋ #348 ＋ #349 ＋ #350 |

**此刻的 frontier**：#349 与 #350 的**签字**（两票的活都已完成，只差用户回那几句）；#351 的端到端与肉眼终审同批走，不再等前两票关。#168 保持打开，只作对账证据。

## Decisions so far

<!-- 索引：一行一条＝已关的子票 gist ＋ 链接；细节在票里，这里不复述 -->

- （暂无。本图的边界由整批 Q1–Q6 裁定给出，见文末。）
- 2026-09-14 · 按命令登记纪律底座（#293）重切：旧“页面交付／命令补齐 23 条”因写不出单一条判据、三种性质混装而作废，切成 #346–#351（本轮设计，见会话）。计数口径＝`list:'wake'` order 176–207 共 32 条，5 条 `new` 别名不计入 ①；复盘窗口语义归 #250，本图只消费。
- 2026-09-14 · #346 已关（提交 33ff950）：`view.plan` 加 `date` 筛选，「看今天练什么」转 exec；冻结数 344→345／92→91 随票重导；产物为 `ilife-page` 片段（满足判定①，doctype 化另起票）。[#346](https://github.com/FeatherHunter/ilife/issues/346)
- 2026-09-14 · #347 已关（提交 de748c0）：周／动作筛选＋新读命令 `plan-vs-actual`，7 条转 exec；冻结数 345→352／91→84，唯一 CLI 295→302；包全量 473/473。[#347](https://github.com/FeatherHunter/ilife/issues/347)
- 2026-09-14 · #348 已关（提交 a97b59c）：5 写命令＋写前预览，5 条转 exec＋5 确认执行新拟入口；冻结数 352→357／84→79，新拟 58→63；探针 5/5（含 6 条 68kg 豁免）；包全量 479/479。[#348](https://github.com/FeatherHunter/ilife/issues/348)
- 2026-09-14 · Destination“有结局”限定句（外部 5 条以“做到可跑，或经用户裁定改规格＋改 HELP”为准）**待用户签字**，Destination 正文暂不动；#350／#351 带裁定材料请签。
- 2026-09-14 · **#349 已完成待签**：变更类 5 条逐条走「过程页 → 写命令 → 回执＋落库断言」，测试以**根 `test/`** 为准（票面写的 `packages/skill-calorie/test/...` 路径不存在，已实测确认）；对抗审查 80/100 有条件放行。
- 2026-09-14 · **#350 裁定材料已出**（提交 0621392＋7441e29）：外部 5 条（落地×3／训记×2，order 196–200）**维持不做**，只保命中与文案，HELP 文案改法见 `docs/skills/skill-calorie/t350-external-five-verdict.md` §4.2。**待用户签字。**
- 2026-09-14 · **视觉重做（#351 范围）定案口径**：底部一律「复制数据／复制日志」冻结双按钮（`id="ilife-copy-data"`／`id="ilife-copy-log"`）、中文单语、**禁 text/json/csv 英文格式菜单**；零内联脚本，老页 JS 页签用原生两级 `<details open>` 替代；视觉沿用已获接受的共享区块卡片语言，不自造样式、不新增公共层件；结果页动作明细按老页行内排版（类型中文化并降为副行小字、备注去掉与组数×次数／重量逐字重复的段、节奏上移会话标题行）；不印「休息」列（库无该字段）；部位色块本轮降级纯文本（要复原另开公共层票）。**预检确认页必须带可复制的 prompt**（`SKILL.md` 口径「出页后由用户复制 prompt 回给 AI」，本仓其余预检页均如此）。证据 `docs/skills/skill-calorie/t351-redesign-184-spec.md`、`t351-recon-20260914.md`。
- 2026-09-14 · **读命令缺失阻断回归已修**：提交 `5255b01` 曾把 `calorie.view.plan` 的 `missing-data` 吞成 exit 0 空态页，违反 `SKILL.md`「缺失阻断不返空…不静默空页」并使 #349 判据转红；已恢复照旧抛（exit 4），死件 `render/planDeleteVerifyDocs.ts` 删除。归因与读数见 `t351-recon-20260914.md` §二。

## Not yet specified

- ~~本图 32 条唤醒词里，哪些页面其实是**同一类**、可以合并成一张~~ → **已定**：按形态分三类，各自共用同一装配件——结果型（计划查看 176–185／复盘与扫描 201–207）、过程型＝预检确认页（构建向导 186／写前预览 187–195）、回执型（186–195）。6 张老模板的归类结论见下 `## 模板归类`。
- 跨图的重复件（如 `crud_receipt.html` 被 6 个场景共用）最终归到哪一层、上不上移 base，等公共层攒到第三、四张图时再定。
- 旧技能 `templates\` 里有 **24 张模板没被任何场景引用**（本仓也丢了），与本场景相关的要不要救回来，等工作面走到时再决定。——**本场景相关的 6 张已全部定好处置**（见下），其余 18 张仍留。
- **留档一条已查结论（免得后手再查一遍）**：老 `crud_receipt.html` 的「今日累计」上下文卡是喂给饮食／饮水的计量回执的（模板正则写死 `卡|ml`），训练计划回执不是量类操作，**不算本场景缺口**，不补。

## 模板归类（本图那 6 张）

| 老模板 | 本图处置 | 归谁管 |
|---|---|---|
| `workout_plan_view.html`（13 条） | 结果型页面装配，住 `render/workoutPlanDocs.ts` | 只此一处用，就地写 |
| `crud_receipt.html`（11 条） | 回执型页面装配，住 `workout/receipt.ts` | 形状与饮食／体重回执同源，公共层已有对应区块 |
| `process_progress.html`（3 条） | 走读侧命令 `calorie.view.process-progress` 看进度（那 3 条词本身是外部 3 条，按 Out of scope 不执行） | 只此一处用，就地写 |
| `exercise_review.html`（3 条） | 走 `calorie.view.exercise-review`（复盘族已有件） | 卡路里包内 |
| `plan_builder_wizard.html`（1 条） | 预检确认页，住 `render/workoutPlanDocs.ts` 的构建向导装配 | 只此一处用，就地写 |
| `contraindication_report.html`（1 条） | 走 `calorie.view.contraindication`（已有件） | 卡路里包内 |

复制区三段（prompt／数据／日志）本图收成一份住 `render/planCopyBlock.ts`，不替换共用件 `shared/copyArea.ts` 的既有行为。

## Out of scope

- **其余 9 个场景**：各归自己的图（本批共 11 张，顺序 07→06→03→02→04→05→08→09→10→01，场景 10 拆 2）。
- **新架构规格明确不做的 10 条**（拍营养表 2、定时复盘 3、训记 2、落地 3）：只保证唤醒词命中与文案，执行层不做。
  **但注意：这 10 条老技能当年全都实现了**（`render_nutrition_label.py`／`render_cron_setup.py`／`xunji_bridge/__main__.py`／`render_plan_builder.py`），所以这是**新架构主动划出去的，你可以推翻**——要推翻请另开票，不在本图内悄悄做。
- **发布到 npm**：本批不做，单列成票。npm 上 `base-paint@0.3.0` 的 exports 还没有 `./help-shell`，发布态会 `ERR_PACKAGE_PATH_NOT_EXPORTED`。
- **面板／侧栏的 HTML 入口**：属桥／面板那条线，不在本图。

### 本图收口时另开的票（建议，不在本图做）

- 《训记推送公开接口立项（推送＋回写＋KEY 管理＋限频）》——老 `xunji_bridge` 有完整契约（8 子命令／45s 限频／幂等键）但新仓零移植（只有只读校验）；是否做、密钥放哪、限频谁守需先立项。见 `docs/skills/skill-calorie/t350-external-five-verdict.md` §五。
- 《落地本地部分先行（补计划＋记心愿走公开命令）》——路由为 `schedule.plan.write(op=ensure)`＋`memo.create`，训记两步仍裁定不做。
- 《部位色块复原（提公共层 `base-paint`）》——本轮按负责人裁定降级为纯文本，要复原须走公共层。
- 老 `goal_config.html` 那一类（旧目录里找不到实物，只有两个没被引用的分身）要新设计。

### 超线件台账（本包告警线＝350 行 LF 口径，见 `packages/skill-calorie/AGENTS.md`）

本图勘查实测，逐件挂号（**均为既有已提交件，非本图引入**）：

| 件 | LF | 说明 |
|---|---:|---|
| `src/workout/write.ts` | 570 | 交接文档的另票清单里漏了这一件，本次补挂 |
| `src/render/trendDocs.ts` | 816 | 已在册 |
| `src/render/sportPortDocs.ts` | 533 | 已在册 |
| `src/render/wizardPort.ts` | 457 | 本包 `AGENTS.md` 已挂（#354 登记） |
| `src/workout/planStore.ts` | 369 | 已在册 |
| `scripts/gen-cli.mjs` | 729 | 本包 `AGENTS.md` 已挂（#354 登记） |

本图动过的件全部在 350 以内：`render/workoutPlanDocs.ts` 348、`render/planCopyBlock.ts` 61、`workout/precheckPrompt.ts` 38、`workout/plan.ts` 54、`workout/receipt.ts` 120。

## 整批共同裁定（用户 2026-09-11 拍板，本图继承）

- **Q1 怎么切图**：按 **10 个场景**切；场景 10 拆成 2 张 → 本批共 **11 张图**。
- **Q2 公共层怎么长**：**跟着场景一集一集长**；分析下来可能被多个场景用到就提到公共层；开发时**优先去公共层找**。
- **Q3 95 条跑不通的唤醒词**：**按性质分三种去处** —— 5 条必须多步交互（跟着场景图做，当预检确认页的第一批样本）／80 条缺命令（在各场景图里**单独列票**，不混进页面改版）／10 条新架构划出去不做（写进对应图的 Out of scope）。
- **Q4 51 个页面模板名**：**各图自己归类，全批最后人工收口**（本图第 1 张票里做本图那一份）。
- **Q5 场景顺序**：**07 → 06 → 03 → 02 → 04 → 05 → 08 → 09 → 10 → 01**（场景 10 再拆 2）。
- **Q6 本次止点**：**只开图** —— 建齐 map ＋ 各自首发票 ＋ 原生边，不开工。



