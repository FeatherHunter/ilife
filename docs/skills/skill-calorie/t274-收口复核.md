# #274 收口复核（食品库类页 · 4 条）

## 一、裁决

**FAIL（需整改）** —— 一条 **S1（契约违反 ＋ 用户可见回归 · 本票范围）**，由我独立复跑取证，不是听作者席自报。

**FAIL 点**：库非空、但本次查询零命中（搜索词 0 条／分类 0 条）时 `exit 4`、不落盘、不出页；应当出完整页 ＋ 空态句 ＋ 引导句。老实物正本 `food_search.html:98-101` 对 `items.length === 0` 恰恰是出 `emptyState`（含引导句）；老技能 `render_food_search.py:132-153` 无论命中几条都 `status:'ok'`、写页、`return 0`。⇒ 读者从「一张写着『无匹配食物』并教怎么加第一条的页」退成「一条错误」。这与同图 **#272 刚判 FAIL 的是同一类**（空窗未出完整页），不是新规矩。

五维（供参考）：契约一致 18／证据真实可复现 20／新旧对照 16／工程红线 15／文档同步 8 ＝ **77**（低于 85）；S1 单独即可否决。

**站得住的半边（照实记）**：8 条词真出口 8/8 `exit 0` 且产物是完整文档，字节与作者席的表逐个相同；四张产物两两不同；空库 8/8 `exit 4` ＋ 缺失阻断 ＋ 不落盘；干净去重库出完整页 ＋ 空态块 ＋ 空态句 ＋ 引导句（68705 B）；转义、卡片格、去重两态、来源统计骨架、复制区双按钮与日志第 4 段，我的硬写死样例 31/36 项全对。

## 二、机器证据

检出 `D:\ilife-wt\wf155-g2`，基线 sha **`506bdcaba3d51dd257856841c1adb9096966f355`**（`D:\ilife` 未触碰）。作者席点名的 5 个提交（`1ee8141`／`ca52b5d`／`2c582b8`／`e426114`／`5539f8a`）逐条 `merge-base --is-ancestor` = 真；`t274-gate-runs.md:49-50` 写的 `de5e91b`／`cd4578f` **不在 HEAD 历史里**（实际落成 `2c582b8`／`e426114`）。`npx tsc -b packages/skill-calorie` `exit 0`。

| 读项 | 我的读数 |
|---|---|
| 8 条词（命令行取自 `dist/triggers/routes.generated.js` 的 `ALL_ROUTES`） | **8/8 `exit 0` ＋ 完整文档**；字节 70354／70427／70033／75821（与作者表逐个同） |
| 四张产物两两 | 规范化去时间戳后 **4/4 互不相同** |
| 空库（8 条词） | **8/8 `exit 4` ＋ `ERR 4: 取数失败（缺失阻断）` ＋ stdout 空 ＋ 不落盘** |
| **库非空 ＋ 本次零命中** | **`exit 4`、不落盘、空态块／句／引导句三样全无**（分类零命中同）；干净去重库 `exit 0` ＋ 68705 B ＋ 三样齐 |
| 我的新探针（36 项硬写死样例） | `RESULT-NEW: FAIL 红=5`，5 红全在上面那一格 |
| 变异-甲（取消卡片名转义） | 作者件 `tests 11 / pass 10 / fail 1`（**必红=true**），我的探针红 5→6；**还原**：源码回基线=true、`fail 0`、红回 5 |
| 变异-丙（口径行文案换一句） | 作者件 `11 / pass 11 / fail 0`，我的探针也没抓 ⇒ **探针盲区** |
| `libraryDocs.ts` | 396 LF／19151 B／sha256 前 16 `7bae0a8054a49db2`（与作者自报同） |
| 可见文本（8 件产物独立抽文本） | 源码标识符命中 `[]`、无禁用按钮 ⇒ 裁定 1 独立成立 |
| 全包基线（含本票件） | `tests 1163 / pass 1080 / fail 81`，红件 **35**，本票件**不在**其中 |
| 全包基线（本票件移出） | `tests 1152 / pass 1070 / fail 80`，红件 **34** |
| 两跑差集 | 非空 1 条 `db-readonly-93.test.mjs`（单跑 8/8 绿、与本票件合跑 19/19 绿、本票件单跑 11/11 绿）⇒ **别席抖动，不归本票** |

## 三、逐条路径

- 复核脚本（随本报告入仓）：`docs/skills/skill-calorie/t274-收口复核-复跑.mjs`、`t274-收口复核-新探针.mjs`、`t274-收口复核-变异.mjs`；日志与隔离库在 `.scratch/t274g2/`（`复跑.log`、`新探针.log`、`变异.log`、`pkg-test-with-t274.log`、`pkg-test-without-t274.log`）。隔离库＝`.scratch/t274g2/<态>/calorie_data.db`，HTML 落同目录 `calorie_html/`；三条脚本都从运行期总表取命令行，不抄字面量。
- 被审交付件：`src/diet/libraryDocs.ts`（396 行）、`src/diet/library.ts`（65 行）、`src/diet/sourceStatsDocs.ts`（123 行，新建）、`test/t274-食品库页.test.mjs`（11 条）、`.changeset/t274-{食品库类页,来源统计页搬迁}.md`。
- 老实物（只读，未执行其中任何脚本）：三张 HTML ＋ `scripts/render_food_search.py`（定行为基线）。
- **逐块对账**：`food_search.html` 的 head 标题／h1／占位提示／卡片格七格／四宏量（`165 卡`／`31.0 g`／`3.6 g`／`0.0 g`）**对上**；匹配数换槽位、结论句走裁定 2 **对上**；页脚去库表名、缺品牌不出那一行 **没对上（有意）**；`:99` 空态登「对上」**不成立**（缺陷 1）；客户端实时过滤 **不适用**。`dedupe_report.html` 的 h1／两态条幅／三读数／重复组表／处理建议 **对上**（中格与列名按 #496 改人话）。`source_stats.html` 的 h1／两读数／按来源分组（换图元：公共层柱图 ＋ 明细表）**对上**；head 标题改族名 **没对上（有意）**。
- **两处有意偏离我独立判：站得住。** ① 缺品牌不出那一行 ⇒ `t511-清尾二-证据.md:88` 第 74 条原文即「那一行整行不出」，且 `t425:211` 有反面记录禁止把卡片格改表；② 页脚与标题换人话 ⇒ #496 裁定 1（标识符不上屏），且 `t511-清尾二.test.mjs:120` 等别票件把族名 `卡路里·饮食` 钉住，改标题会踩别人引脚（裁定 11）。
- 专属约束① 独立核过：8 条词→命令取自运行期总表，`看食品来源统计`／`看食品来源分布` 都落 `calorie.view.source-stats`，无互调。约束② 的搬迁：`ca52b5d` 对 `nutritionPortDocs.ts` 只删那一支 ＋ 改件头与 import，其余行未动。

## 四、缺陷清单

1. **【S1·契约违反＋用户可见回归·本票范围】** 库非空但本次零命中不出完整页。落点＝`diet/libraryPlate.ts:33`／`:56-58`（本票声明路径内）；`libraryDocs.ts:109-115` 的 `emptyGuide` 支因此是死代码。作者席**未引入**该行为（本体 2026-09-13 由 #324 纯搬迁带进），但该支明确压在本票四页上且被登成「对上」。
2. **【S3·证据不实·本票范围】** 逐块表把 `food_search.html:99` 空态登成「对上」（报告 §一、`t274-证据.md:75`）；依据是我两处独立复跑都读不到那一支——它只在两个取数函数抛错之后才可能被调，而两者先抛。
3. **【S3·计数不符·本票范围】** 报告 §一 表头写「1 处有意没对上、2 处不适用」，同报告的表里实为 **3 处没对上（有意）＋ 1 处不适用**。
4. **【S3·探针盲区·本票范围】** 口径行文案无断言：变异-丙下作者件全绿、我的探针也全绿。
5. **【S3·文档未同步·本票范围】** `t274-证据.md` §一 三条仍写「来源统计页没对上」，已被 `ca52b5d` 与在手测试推翻，只有报告改了。
6. **【登记·本票范围】** `fixed0`／`fixed1` 的「缺值写 `—`」在真库落不到（`schema.ts` 把 `calories／protein／fat／carbohydrates／sodium` 定成 NOT NULL）⇒ 该口径无法由真出口证伪，登「对上」缺可跑依据。
7. **【S3·范围外】** 两跑红件集合双向差集**非空**（`db-readonly-93.test.mjs`，报的是 `calorie.view.goal-weight` 只读／可写句柄不等，属目标线）；单跑绿、与本票件合跑也绿 ⇒ 别席抖动，**不决定本票裁决**，但「33=33 双向差集为空」这句读数在当前 master 上复现不出来。

## 五、未做项与下一手缺什么

**未做**：未开视觉验收墙逐格审版式（本次是收口复核，三样齐不在范围）；未做全仓 `pnpm test`（只做包级）；未与 #278／#279 对账（两票未开工）。

**下一手缺什么**：① 在取数层把「本次零命中」与「库为空」分开（落点 `diet/libraryPlate.ts`），守住 `exit 4` **只留给库为空**（`t425` 裁定 4 澄清明写不许改空库那条）；② 作者件 `test/t274-食品库页.test.mjs:384-399` 现在把两态锁成同一条（测试名叫「空库」而实际测的是「零命中」），改判据须同步改它；③ 补空态句／引导句断言 ＋ 口径行断言；④ 改完重跑我这三件 ＋ 作者 11 条。**需编排者裁**：`查食品` 的「零命中」算不算裁定 4 的空窗那一支——若裁「设计就这样」，须把澄清件的边界写清，并同时改老实物正本与 `render_food_search.py` 的行为基线。
