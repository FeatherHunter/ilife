# t593-S2 · 训记推送链（#607）· 证据件

> 票：[#607「训记推送链：push-plan／upsert（接口形状＋限频＋重试＋退出码映射）」](https://github.com/FeatherHunter/ilife/issues/607)（Part of #593 Step3 推送）
> 判定：**PASS**——编译 0／定向 30/30／全量 72 红与基线同因（§七）／`gen:check` 绿／变异两轮红→绿（§六）。
> 门禁留痕导出：`docs/skills/skill-calorie/t593-S2-推送链-门禁留痕.md`（本件每条 `GATE-RUN` 在它里面都有同标识 `RUN` 条目）。
> **本票无 HTML**：交付物全是模块源码＋测试＋本文档，无任何页面产物——视觉验收墙不触发（地图上下文点名条款）。

## 一 · 交付物（实际碰到的件）

| 件 | 动作 | 一句话 |
|---|---|---|
| `packages/skill-calorie/src/xunji/request.ts` | 新增 | 请求形状：session→`res[]` 转换 ＋ upsert 包体组装（纯函数） |
| `packages/skill-calorie/src/xunji/rateLimit.ts` | 新增 | 45 秒常量唯一定义地 ＋ 窗口判定 ＋ 跨进程节流器 |
| `packages/skill-calorie/src/xunji/retry.ts` | 新增 | 7 类错误分类 ＋ `retry_with_backoff` 移植（2 次／5 秒退避／三类不重试） |
| `packages/skill-calorie/src/xunji/upsert.ts` | 新增 | `upsertTrains` 原子调用（KEY 预检→组包→POST＋超时→分类→重试） |
| `packages/skill-calorie/src/xunji/push.ts` | 新增 | `pushDayPlan` 编排（逐段限频＋逐个 upsert＋汇总） |
| `packages/skill-calorie/src/xunji/planSource.ts` | 新增 | `--date`→sessions 取数口（只读开库＋周定位＋存在性预检） |
| `packages/skill-calorie/src/xunji/exitMap.ts` | 新增 | 老 0–4→新仓退出码逐位映射表（唯一定义地） |
| `packages/skill-calorie/src/xunji/run.ts` | 改 | 接线 `upsert`／`push-plan` 真实现；分派改异步＋可注入 `deps`（向后兼容） |
| `packages/skill-calorie/src/xunji/subcommands.ts` | 改 | **仅翻两条**的 `state`→`implemented`、`ownerTicket`→`null`（票面点名） |
| `packages/skill-calorie/src/xunji/cli.ts` | 改 | 一行：`await` 分派（分派改异步的强制随动） |
| `packages/skill-calorie/src/xunji/index.ts` | 不改 | 能力门仍恰五件（`t606` 钉死；推送链经 `runXunjiCommand`／CLI 对外） |
| `packages/skill-calorie/test/t607-推送链.test.mjs` | 新增 | 17 条挡板用例（§六验收四点） |
| `packages/skill-calorie/test/t606-训记模块骨架.test.mjs` | 改 | 随动 6 行：两条移出“未实现”＋分派 await（§八·1） |
| 本件 ＋ `t593-S2-推送链-门禁留痕.md` | 新增 | 证据件 ＋ 运行记录导出 |

## 二 · 必报五步（`docs/agents/structure.md`）

### 第一步 · 影响清单（动手前定的）

| 归属 | 件 | 碰它的理由 |
|---|---|---|
| `xunji`（内） | `src/xunji/{request,rateLimit,retry,upsert,push,planSource,exitMap}.ts`（新） | 按变化频率分层的新件（票面首选名）：形状／限频／重试／原子调用／编排／取数／码表各住一件 |
| `xunji`（内） | `src/xunji/run.ts`（改） | 分派接线两条真实现 |
| `xunji`（内） | `src/xunji/subcommands.ts`（改） | 仅翻两条的 `state`／`ownerTicket` |
| `xunji`（内） | `src/xunji/cli.ts`（改） | 分派改异步的强制随动（一行） |
| 测试 | `test/t607-…`（新）＋`test/t606-…`（随动） | 票面四点挡板 ＋ 与本票目标直接矛盾的旧断言随动 |
| 证据 | `docs/skills/skill-calorie/t593-S2-推送链-证据.md`（＋留痕导出） | 票面点名证据件 |

### 第二步 · 结构设计

```
src/xunji/
  request.ts    纯函数：session→res[]（datestr 原样／localid=0／start=end=0／name 原样）＋包体组装
  rateLimit.ts  45 秒唯一定义 ＋ rateLimitWaitMs 纯函数 ＋ createRateLimiter（时钟／睡眠／状态文件全注入）
  retry.ts      7 类分类（classifyStatusBody／classifyThrown／softFailureOf）＋ retryWithBackoff（2 次／5 秒退避）
  upsert.ts     upsertTrains：dry-run 摘要 → KEY 预检 → POST＋30 秒超时 → 分类 → 重试（传输／KEY／睡眠全注入）
  push.ts       pushDayPlan：逐段限频 → 逐个 upsert（幂等键 {date}_{label}_{uuid8}）→ 汇总（ok/fail/verified）
  planSource.ts resolveDayPlan：只读开库 → getPlan → weekOfDate 定位 → sessions（读不出回 found:false，不抛）
  exitMap.ts    PUSH_EXIT_TABLE（11 行）＋ exitForErrorKind（auth→2，其余→3）
  run.ts        runXunjiCommand(argv, deps?)：verify 同前；upsert／push-plan 走上列件；其余五条仍拒绝
```

每件对外 ≤5 个（`index.ts` 能力门仍恰五件，本票不扩门——`t607` 首条用例钉住）；
目录内互用不算对外（`run.ts` 用六件内部件走相对引用，不经门）。

### 第三步 · 写代码

- 常量类型只一处：`RATE_LIMIT_SECONDS`（`rateLimit.ts`）、`XUNJI_UPSERT_ENDPOINT`＋`XUNJI_KEY_ENV`（`upsert.ts`）、`PUSH_EXIT_TABLE`（`exitMap.ts`）、`XUNJI_EXIT_CODES`（沿 `subcommands.ts`，不另起）；
- 类型无 `any`、无说不清形状的 `unknown`（`JSON.parse` 结果先收 `unknown` 再收窄／断言到定形类型）；
- 失败不吞：每条失败都带结构化读数进 `data`，`fail_count`／`err` 进退出码；
- 跨能力引用走仓内先例（见 §八·3），不抄别家逻辑。

### 第四步 · 超线报警（本包告警线 ＝ 350 LF，节点口径）

当场实测：`request.ts` 101／`rateLimit.ts` 116／`retry.ts` 135／`upsert.ts` 146／`push.ts` 113／`planSource.ts` 89／`exitMap.ts` 53／`run.ts` 213；`subcommands.ts` 236／`cli.ts` 14（未超）。
**全部在 350 以内，未触发第四步**，台账无需增行（台账只记超线件）。

### 第五步 · 交付对账（第一步逐行 → 实际）

| 第一步的行 | 实际 | 偏差 |
|---|---|---|
| 七件新源码 | 七件都在 | 无 |
| `run.ts`／`subcommands.ts`／`cli.ts` 改 | 三件都改（`cli.ts` 为分派异步的强制随动，第一步漏列此处补记） | ＋一行，随动一行 |
| `index.ts` 不改 | 未改 | 无 |
| 测试新＋随动 | 两件 | 无 |
| 证据件 | 本件 ＋ 留痕导出（协议 §2.3 第 4 条要求） | 加一件留痕导出 |
| 计划外触碰 | 无 | **零实质偏差** |

## 三 · 票内定的口径

### 1. 请求形状（票面逐字落实）

- 接口 `POST https://trains.xunjiapp.cn/api_upsert_trains_for_llm_v2`（老 `upsert.py:53-54`）；
  包体五键 `schema_version=train_open_api_v2／client_request_id／dry_run／include_full_data／res`（老 `:90-96`）；
- `res[].datestr` 用传入 `YYYY-MM-DD` 原样（**不转时间戳**，老 `push.py:104`），同串进幂等键前缀（老 `:153`）；
- `localid=0`（新建；更新语义归 #610 的 `overlay-plan`）、`start=end=0`（老 `:105-108`）；
- `movements[].name` **原样上报**（老 `push.py` 不 import `catalog`；`t607` 故意送`不存在动作XYZ`并断言原样发出）；
- set 只留 `done=false＋weight＋unit＋reps`（老 `:95-100`），`weight` 缺时回退 `load`，缺省 `"0"／"kg"／"0"`；
- `client_request_id` 缺省自动生成（老 `upsert.py:77-79`）；`push` 按 session 现生成 `{date}_{label}_{uuid8}`（老 `push.py:153`）。

### 2. 限频档位决议（票面点名“本票内定”）

- **按段逐个守**：每次 upsert 调用前都看“距上次过去了多久”，窗内睡够再调（老“睡够再调”保留，`push.py:157-158`；`--dry-run` 不守不记，老 `:156` 同述）；
- **跨天／跨进程也守**（把“跨天不守”当缺陷修掉）：上次时刻落盘 `~/.mavis/xunji_push_rate.json`
  （与读侧 `xunji_bridge_rate.json` 分开，老 `auth.py:40` 同目录先例），不同进程的两次 `push-plan` 照样排开；
- 老明确不守的三处保留：`--dry-run`（老 `:156`）、`overlay-plan`（单次调用，归 #610）、读侧 15s/30s 缺省关（归 #608）；
- 代价诚实写明：N 天×多段的串行推送总等待变长（正确性优先；`run-sync` 归 #610，届时照此口径排队）。

### 3. 重试表（老 `retry_with_backoff(max_retries=2, sleep_base=5)`，`upsert.py:134-135`）

| 失败 | 重试 | 退避 | 备注 |
|---|---|---|---|
| `auth`（本地缺 KEY／服务端 401-403） | 否 | — | 老 `errors.py:270-271`；本地缺 KEY 连调都不调 |
| `vip_required`（仅VIP） | 否 | — | 老 `:270-271` |
| `validation`（400／响应 JSON 解析失败） | 否 | — | 老 `:273-274`（解析失败归 validation 是老 `:113-115` 同例） |
| `rate_limit`（429／too frequent） | 是（≤3 次调用） | 服务端 `retry_after` 优先，否则 5s、10s | 老 `:276-282` |
| `server`（5xx） | 是 | 5s、10s | 同上 |
| `network`（超时 30s／连接错） | 是 | 5s、10s | 老 `:109-111`；超时由 `AbortController` 触发 |
| `unknown` | 是 | 5s、10s | 老 `:275`“其余重试” |
| 调用抛错（传输层异常） | 按分类进上表 | 同上 | 老 `:283-293` |

软错误同判：200 但 `success:false`／含 `error`／`res` 为 too frequent 串，走同一套分类（老 `:79-103`）；
`attempts>1` 才回写（老 `:136-137`）；睡眠全注入（测试零真睡）。
退出分流见 §四：`auth` 且无 HTTP 状态码（本地缺 KEY，没调网）→2；`auth` 且有状态码（服务端 401／403）→3。

### 4. 取数口（`push-plan --date` 的 sessions 从哪来）

- 只读开库（`openDbReadOnly`，不建表不迁移；`cli/cmd_read.ts:97` 同法），库＝`SKILLS_DB_PATH/calorie_data.db`（`paths.ts`；不用会 `mkdir` 的 `resolveDbPath`）；
- 日期→（周，日）走 `weekOfDate`（`render/planPlate.ts:38-45`），会话过滤走 `getPlan`（`workout/planStore.ts:231`）；
- 老 `calc_plan_week` 的“超总周数循环”**不照抄**——新仓 `buildPlanView`（`planPlate.ts:61-76`）同样不循环，对不上即“这天没排练”（`session_count: 0`＋note，退出 0，老 `push.py:135-143` 同形）；
- 无任何计划／缺开始日期／库不在／`SKILLS_DB_PATH` 未设 → `found:false` 带人话原因 → 退出 1（“失败不许静默吞”）。

### 5. KEY 缝（只调接口，不实现）

- 缺省读环境变量（`XUNJI_TRAINS_KEY` 优先→`XUNJI_API_KEY` 回退，老 `auth.py:47-55` 同序）；
- 读写实现（`set／clear／status`）与持久化归 #610 的 `key.ts`，本件只留 `readKey` 注入缝（测试给挡板）；
- 测试与证据一律 `REDACTED`，真 KEY 永不进仓。

## 四 · 新旧退出码逐位映射表

唯一定义地 `src/xunji/exitMap.ts` 的 `PUSH_EXIT_TABLE`（11 行；下表与该表逐行对应，备注栏为证据件裁剪引用，原文以代码为准）：

| 老码 | 事件 | 新码 | 备注 |
|---|---|---|---|
| 0 | 全部 session 成功（含无 session 空天） | 0 | 老 `__main__.py:121-123` |
| 0 | `--dry-run` 转换成功 | 0 | 老 `:213` |
| 1 | 用法错：`--json` 二选一违例／解析失败／非数组 | 1 | 老 `:166-181` 同位 |
| 2 | 用法错：argparse 缺参／未知参 | 1 | ★老 argparse 自身退 2（`:246`）；与鉴权码撞车，新仓一律 1（#606 同例） |
| — | 入参／取数失败：坏日期／计划读不出／库不在 | 1 | 新增明确失败（老直接抛穿，`t350` §三·2） |
| 2 | 本地没配 KEY | 2 | 老 `:117-119／:207-209` 同位；自述 2 只覆盖此支（#595 §八·2） |
| 3 | 服务端 401／403 | 3 | 老实况退 3（#595 §四·4.4／§六：调用侧只看 `err` 即 `EXIT_API`，`__main__.py:211-212`）；行为以老代码为准 |
| 3 | `vip_required` | 3 | 老不重试→退 3，同位 |
| 3 | `validation` | 3 | 老不重试→退 3，同位 |
| 3 | 限频／5xx／超时网络／未分类（耗尽） | 3 | 老重试→带 err 回→退 3，同位 |
| — | 校验位 4 | 4 | 本链不用（`verify` 专位）；推送前不校验 |

与卡路里业务面码（`cli/cmd_read.ts:5` 的 0／1／2／3／4／5）**不混**：模块对外面与老实现一模一样是 #597 裁定，
业务面那套的对照（老 2↔新 3 key、老 3↔新 4 取数）见 #595 §六；跨面调用时由 #614 的薄命令做翻译，不在本票。

## 五 · 与老代码 file:line 对照（根＝`D:\2Study\StudyNotes\SKILLS\卡路里\scripts\`）

| 新仓件 | 老出处 | 对什么 |
|---|---|---|
| `request.ts`（`sessionToResItem`） | `xunji_bridge/push.py:32-43`（`_safe_str`）、`:81-110`（`_session_to_res`） | 标量化／六键形状／`done:false` |
| `request.ts`（`buildUpsertPayload`） | `xunji_bridge/upsert.py:90-96` | 包体五键 |
| `rateLimit.ts`（常量＋守点） | `xunji_bridge/push.py:29`、`:156-158`；状态文件先例 `auth.py:40`、`218-222` | 45 秒／睡够再调／原子落盘 |
| `retry.ts` | `xunji_bridge/errors.py:39-46`（7 类）、`:128-180`（分类）、`:242-295`（退避） | 分类序／不重试三类／5s-10s |
| `upsert.ts` | `xunji_bridge/upsert.py:53-54`（地址）、`:62`（30s）、`:77-89`（幂等键＋dry-run）、`:108-138`（调用＋重试） | 全流程 |
| `upsert.ts`（KEY 缺省读法） | `xunji_bridge/auth.py:35-55` | 两名同名同序 |
| `push.ts` | `xunji_bridge/push.py:113-197` | 天汇总六键／幂等键格式／`verified`／`verify_note` |
| `planSource.ts` | `workout_plan.py:44-62`（周换算思路）、新仓 `render/planPlate.ts:38-76`（实际口径） | 日期定位（取新仓口径，见 §三·4） |
| `run.ts`（`runUpsert`） | `xunji_bridge/__main__.py:163-213` | 二选一／解析失败／`err→3` |
| `run.ts`（`runPushPlan`） | `xunji_bridge/__main__.py:114-123` | `fail_count>0→3` |
| `exitMap.ts` | `xunji_bridge/__main__.py:29-49` | 码表（一处修正式映射见 §四：argparse 用法错 2→1；其余按老自述＋老实况） |

## 六 · 判据读数（票面验收命令，逐条）

| 门 | 命令 | 运行标识 | 退出码 | 摘要行 |
|---|---|---|---|---|
| 编译 | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | `050beb3f-c124-4377-96d4-e81edf1f8c62` | 0 | 无错（tsc 无输出即无错；P0-1 整改后重跑） |
| 定向 | `node --test t607-… t606-…` | `e39794ad-a206-4c43-9fdb-e08e10a39579` | 0 | `tests 30 / pass 30 / fail 0`（P0-1 整改后重跑） |
| 全量 | `node --test packages/skill-calorie/test/*.test.mjs` | `e381ff43-5cb8-45bb-adef-1e022626a8f7` | 1 | `tests 1781 / pass 1709 / fail 72`（§七：与基线同因，无一沾训记） |
| 生成物门 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `3add20a5-3ed0-48bc-a1c8-48cf04f702f4` | 0 | `GEN-CHECK PASS：键 129（写 47 ＋ 读 82）；能力 10 个`（与改前同；P0-1 整改后重跑） |
| 告警线门 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | `31288a4f-b6be-4ea0-99a6-0cf4cf915b15` | 0 | `RESULT: 91/91`、`PASS: 告警线台账齐全且与实况一致`（P0-1 整改后重跑） |
| 变异 | `node .scratch/t607/mutation.mjs` | `73a17b56-efda-40f7-91c0-d83106440051` | 0 | 两轮红→绿＋还原 `bit-identical`（下） |

`GATE-RUN runId=050beb3f-c124-4377-96d4-e81edf1f8c62 cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie`
`GATE-RUN runId=e39794ad-a206-4c43-9fdb-e08e10a39579 cmd=node --test packages/skill-calorie/test/t607-推送链.test.mjs packages/skill-calorie/test/t606-训记模块骨架.test.mjs`
`GATE-RUN runId=e381ff43-5cb8-45bb-adef-1e022626a8f7 cmd=node --test packages/skill-calorie/test/*.test.mjs`
`GATE-RUN runId=3add20a5-3ed0-48bc-a1c8-48cf04f702f4 cmd=node packages/skill-calorie/scripts/gen-cli.mjs --check`
`GATE-RUN runId=31288a4f-b6be-4ea0-99a6-0cf4cf915b15 cmd=node packages/skill-calorie/scripts/check-warning-line.mjs`
`GATE-RUN runId=73a17b56-efda-40f7-91c0-d83106440051 cmd=node .scratch/t607/mutation.mjs`

变异两轮（`.scratch/t607-mutation.log`）：

```
MUTATION-A-auth重试 build-exit=0 test-exit=1 red=yes
RESTORE-A-auth重试 build-exit=0 test-exit=0 green=yes
RESTORE-一致 packages/skill-calorie/src/xunji/retry.ts=bit-identical
MUTATION-B-localid改1 build-exit=0 test-exit=1 red=yes
RESTORE-B-localid改1 build-exit=0 test-exit=0 green=yes
RESTORE-一致 packages/skill-calorie/src/xunji/request.ts=bit-identical
```

- 变异 A（重试面）：`auth` 移出不重试集 ⇒ “服务端 401 不重试”用例变红（调 3 次）；
- 变异 B（形状面）：`localid: 0` 改 `1` ⇒ “请求形状逐字段”用例变红；
- 还原＝按原字节写回，两件 SHA256 与变异前逐字节相同；还原后重编译＋复跑全绿。
- 变异电池命令（持锁窗内直调，不嵌套 `run-locked`；每轮：改坏→编译→定向测试→原字节写回→编译→定向测试→SHA 比对）：
  `node .scratch/t607/mutation.mjs`（内调 `node node_modules/typescript/bin/tsc -b packages/skill-calorie`
  与 `node --test packages/skill-calorie/test/t607-推送链.test.mjs` 各两次；脚本见 `.scratch/t607/mutation.mjs`，未进仓）。

注：定向首跑（`de2eaaaf-…`）红在三处**测试侧 bug**（`reps` 期望笔误、`tmp()` 未建子目录×2），修测试后复跑全绿；
三处皆非源码问题，见 `.scratch/t607定向.log`。

## 七 · 既有红（票外）说明

- 全量 `1781 = 1764（#606 基线）+ 17（本票新增，全部绿）`；`pass 1709 = 1692 + 17`；`fail 72` 与基线计数相同；
- 72 条逐名如下（P1-2 整改：逐名落盘；来源全量日志 `failing tests:` 段，`✖` 后为用例名，已去重计数 72）：
  域核对结论先行：**无一条沾训记域**（无 `t606／t607／推送链／骨架／verify／push／upsert／xunji` 字样；
  唯一的“upsert”类字样是 `goal/profile.write.ts` 注释里的“单例 upsert”，与本票无关）；

```
✖ #514 对齐③ 产物口径行无分隔符且三段事实俱全（公共层结构槽）
✖ #566 ③ data.metrics 与当刻口径逐字段相同，且这条件不是恒真
✖ weightCompare：17 场景分发
✖ weightCompare：平台期/极端月/同期
✖ simulate：外推+模拟+摄入
✖ C6 写收据HTML结构化分项
✖ 口径 · 删除键数据驱动：prose／items[].status／库内行三源一致（回执 id 定位被删行）
✖ 口径 · 删除回执可恢复性：文案与库内语义一致（软删行留／硬删行无）
✖ 覆盖门 · 全部写键逐键落库断言（每键 ≥1 次真实只读查询，缺键/空转即红）
✖ 体重记/改/删/批量 + 缺身高仍记（C5 #43）
✖ #448 判据①：不给该位／给假 → 结构 sha 与改动前同值（三件样本）
✖ #448 判据①：改前留档在场 → 三件样本逐字节相同
✖ #448 判据③：新透传位的产物 ≡ 老字符串手术后的产物（逐字节）
✖ #453 看运动类型分布（3 条之 1，先验形状第一条）
✖ #453 看力量训练总览
✖ #453 看有氧训练总览
✖ #111 力量总览：按动作聚合＋重量轨迹＋逐条记录
✖ #111 有氧总览：按类型聚合＋配速＋逐条记录
✖ #111 运动趋势：日序列＋周频次＋峰值（空日断点）
✖ #265 逐条按路由 cli 实跑是各自那一页、不是运动总览
✖ plan 校验：三硬止+软提示
✖ #449 ④ 标签单源：运动域标签表与域登记都只有一处
✖ 单源：四类色的色表绑定全仓只在一处
✖ #256 ② 12 条看类两两不同形：区块集相等 ⟺ 同槽（同命令＋同段窗口）＋ 不同形档数 ≥ 8
✖ #256 ⑤ 灵敏度：摘模板键名／挖数值／两条命令塌成同一份，对应断言必须变红（原样必绿）
✖ #254 四件产物＝完整文档：绝对路径 ＋ 落盘 ＋ 四断言 ＋ metrics 与改前逐字段相同
✖ #290 六条产物＝完整文档：exit0 ＋ 落盘 ＋ 四断言 ＋ metrics 冻结 ＋ 新呈现三处
✖ b1 整页：完整文档＋KPI/表/复制双钮/结论四件套
✖ ① exec 场景带可执行 CLI，逐条逐字 = 路由层 exec 路由（红点：helpSceneCli 改回 main_prompt.cli 原文）
✖ ② 数据层：exec 场景的 `editable_fields` 形状固定（命令＋工作流程＋可执行命令；non-exec 不发）（红点：给全部场景发空值行）
✖ ② 渲染层：exec 场景各一条 `data-field="cli"`（file／inline），落在场景卡内的 Sheet 里（红点：字段发到卡外／不发）
✖ ② 卡面 `cliText` 不动：436 条 `<code class="…-cli">` 逐字 = Scene.id（红点：把 CLI 塞进 id）
✖ ③ 冻结面恒 130 条／`Scene` 属性集不变（红点：往 Scene 上加 cli 字段）
✖ ④ text 态不含命令行（纯文本索引；#88 D-3 锁尖括号集恒 {<N>}）（红点：把 CLI 写进 text）
✖ A1 10 分组／54 子功能／436 场景（红点：分组或子功能聚合逻辑改坏）
✖ A1 types 直方图 329/79/6/22 ＋ 恒发 SceneTypeBadge{text,bg,fg}（红点：types 发字符串＝丢三档色）
✖ A1 顶层键 = F3 键集（skill_name/title/subtitle/contact/groups）（红点：多塞一个顶层字段）
✖ P-2 updatedAt 显式参数：缺省无时间戳且两次调用逐字相同（红点：改用 Date.now()）
✖ D-1 三档徽章色**逐条**映射 = F3 TYPE_DEFAULT（红点：互换 receipt／process 配色——集合口径零鉴别力）
✖ A2 复用冻结面 130 条 implemented／0 pending（红点：base-render 新增契约面）
✖ A2 file 态：完整文档 ＋ 436 卡／54 子功能／1308 复制按钮（红点：壳结构改动）
✖ 三态同源：file／inline 的 436 个 data-scene-id 逐字同序；text 覆盖同一 436 个 id（红点：三态各派生一份数据）
✖ 守卫② id 唯一：数据层 436/436 ＋ 子功能 54/54（红点：id 派生规则碰撞）
✖ 守卫② id 唯一：HTML 层 data-scene-id 436/436 ＋ 元素 id 全唯一（红点：壳内 id 派生用数据里的重复值）
✖ #91 ① 缺省（无参）＝HELP 文件：envelope 索引不变 ＋ 落 卡路里_HELP_<TS>.html
✖ #91 ①b 速查台＝显式 mode file：完整文档落盘 ＋ 独立命名 ＋ #88 结构不动
✖ #91 ② mode 显式三态：file／inline／text 同源 436 场景，inline 片段与 text 文本不入 envelope
✖ #139 ① 缺省＝「卡路里help」HELP 文件：卡路里_HELP_<TS>.html 落 calorie_html／回执绝对路径
✖ #97 · 场景表覆盖全部写键（与 CALORIE_WRITE_COMBOS 一一对应）
✖ #180 场景数据的三个字段里不得再出现脚本命令（今天必红：待清账目）
✖ #247 场景 07 五张页的复制数据是「三格式三选一」菜单：三种格式各一份文本 ＋ 用途提示 ＋ 选中即报所选格式
✖ #365 身体细节四条写命令已是完整文档（七条写词；从「其余仍是片段」那张表里拿出来）
✖ 120 · analysis 11 处查询逐处排除软删行（每处唯一可观测面）
✖ #109 域内唤醒词命中：代表词→运动/身体域 9 键（路由不断）
✖ #109 运动总览：汇总＋双图＋按日表＋类型明细＋分类折叠＋复制
✖ #109 运动目标：目标 vs 实际＋完成度＋达成判定
✖ #109 体重盘：首末＋均值＋变化＋曲线＋记录表
✖ #109 体成分：来源表单＋趋势＋记录表＋复制
✖ #109 围度：单项趋势＋全量表（胸腰腹臀）＋复制
✖ #109 无假数据：9 键空库一律 exit 4 且 stdout 纯净
✖ #109 复制头与冻结 envelope 版本对齐（防漂移）
✖ #255 真：目标 29 词 15 键双向覆盖必过
✖ #275 ① 逐块对得上老实物（diet_overview.html）—— 看饮食总览
✖ #275 ⑥ 裁定 7：复制区双按钮 ＋ 日志第 4 段＝本次命令原文 —— 看饮食总览
✖ #291 乙⑤ SoT 零改动（436）＋饮食三别名回归（仍首命中 diet.add）
✖ #359 看体脂页：7 点逐个出现、逐位等于查库值（冻结种子 3 条）
✖ #367 ③ 代表唤醒词必须是唤醒词表（436 条）里真有的词
✖ #440 判据③④：src 里旧词零命中；13 名的定义只在 fetch/body.ts 一处
✖ #561 ① 场景 06 的 25 条词实跑 exit 0，屏上可见文本 `数据来源` 零命中
✖ wake 多重集 ＝ TRIGGERS（含记身材照×3，一词三 id）
✖ #86 围度 wizard：场景1空页＋场景2预填＋recent＋白名单
✖ #86 体脂 wizard：来源＋体脂率＋皮褶7点＋换算未移植口径
```

- 限定语（明示未做项）：本票**未做** worktree 基线逐条比对；“同集”结论的依据＝计数一致（72＝72）＋上表逐名域核对（无一沾训记）＋
  本票触碰面全绿＋外域消费者只用未改动函数。如审查要求基线逐条对账，另跑 `git worktree` 基线全量（本票未跑，特此明示）。
- 本票触碰面（`src/xunji/*`＋`t606` 测试）全绿；外域消费者（`fetch/audit.ts`、`fetch/cli.ts`、`render/planEditorPort.ts`）
  只用未改动的同步函数（`verifyMovements`／`readMovementCatalog`），`runXunjiCommand` 的唯一仓内调用方是 `cli.ts`（已随动）；
- 结论：72 条是 HEAD 既有红（#606 §六同集：master CI 亦红），本票**零新增红**。

## 八 · 有意偏离清单（与老实现／设计件不同，逐条给理由）

1. **分派改异步**：老 CLI 全同步（urllib 阻塞）。新仓调网走全局 fetch（Promise），`runXunjiCommand` 改 `async`，
   `cli.ts` 加 `await`（顶层 await，ESM 合法），`t606` 一处断言改 await。不同步 HTTP（`spawnSync curl` 之类）——平台相关且不可注入。
2. **限频跨天／跨进程守**（§三·2）：老只守同天第 2 段起。按票面建议把跨天不守当缺陷修掉（状态文件＋每次调用都看）。
3. **跨能力直引是过渡债务（P1-1 整改定性）**：`planSource.ts` 直引 `db/readonly.ts`、
   `workout/planStore.ts#getPlan`、`render/planPlate.ts#weekOfDate`，按 `structure.md` 铁律一字面属违反。
   所引三处恰是既有同类债务（`cli/cmd_read.ts:40`、`render/planPlate.ts:10`、`render/exercisePort.ts:12`、
   `workout/write.ts:12`），不是合规先例——本票沿债务是为不把“计划读取”再造第二处（铁律二），
   收口项见 §九（#610／#614 前认领，本票只立项不实施）。类型复用（`PlanSessionRow`）同理。
4. **周换算取新仓口径**（§三·4）：老循环周不照抄，与 `buildPlanView` 一致。
5. **退出码一处 ★ 修正**（§四）：argparse 用法错 2→1（与鉴权码撞车，#606 同例）。
   服务端 401／403 按 P0-1 整改**改回退 3**（行为以老代码为准；初版误写成两支归 2，披露≠授权，见 §四）。
6. **KEY 缺省读法暂住 `upsert.ts`**：#610 的 `key.ts` 未落，缺省读法（两名同名同序）先住本件、行为与老 `get_key` 一致；
   #610 落门后改走能力门（§三·5）。
7. **`verify` 的动作库校验不进推送**：推送前不校验（老 `push.py` 同样不 import `catalog`）；“先审计”是链的第一步，由调用方（#614）编排。
8. **响应 `gzip` 不手工解**：全局 fetch 缺省解压；urllib 才要手工解（老 `upsert.py:112-114` 的环境相关分支）。

## 九 · 未做项与下一手

- 回写链（#608）、密钥实现与编排（#610）、薄命令与路由（#614）——本票只留声明与拒绝路径，不碰；
- 真机联调（要 KEY、要用户在场）另开票：本票零真网（测试挡板＋`REDACTED`）；
- **收口项 R1（P1-1 立项，本票不实施）**：消 `planSource.ts` 的跨能力直引债务。三选一（#610／#614 前认领）：
  (a) `workout` 门增只读 `getPlan`＋`PlanSessionRow` 类型出口；(b) `render` 门增 `weekOfDate` 出口；
  (c) 计划读取下沉共用位。认领票实施后 `planSource.ts` 改走门，本证据件 §八·3 同步销项；
- 附带发现：无（未撞见新缺陷）。

## 十 · 无 HTML 声明

本票交付物＝模块源码 7 新 3 改＋测试 1 新 1 改＋证据 2 件，**零 HTML 产物**；
`docs/agents/视觉验收墙.md` 与用户 HTML 六条**不触发**。
