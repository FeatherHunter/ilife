# t256 锁票证据（实施席·调查席写集内）

> 票：#256（地图 #153 子票）。本席只做调查席写集内的事。
> 写集核查（开工前只读）：`.scratch/t256*` 不存在（开工时无本票草稿目录）；
> `docs/skills/skill-calorie/t256*` 不存在；issue 256 评论 0 条。
> 故本席按票面「要做什么 1–6 ＋ 判据 ＋ 红线」作写集：**源码零改，只落本证据件**；
> 28 条锁断言入仓（新测试件）不在本席写集，留下一手（§七）。
> 若编排者另有调查席写集，以其为准，本证据照单可并。

## 一、判定：FAIL（锁未闭合，真缺口 1 处，已定位到单件，转票后可收）

- 28 条逐条真跑（临时库，`CALORIE_TODAY=2026-09-07`，种子 `seedFull`）：**27/28** exit0＋绝对路径＋字节如实＋完整文档。
- 唯一真缺口：第 26 条「看目标推荐」（`calorie.view.goal-recommend`）exit0 但产物是
  `<section class="ilife-page">` **片段**（1790 B），不是完整文档——与 #290 留档一致
  （t290-后半 §三：`renderGoalRecommendHtml` 照常在用，原片段 1503 级）。
  既有断言只钉 metrics 与空库 exit4（`cmd-read-t11:151,182`／`render-t9:108,226`），从未要求完整文档，
  故本缺口是**前票已知状态**，不是本席引入。红线禁顺手改产品代码，只记账转票（§七）。
- 五组各至少一条可跑判据：真出口（27/28 实跑）／不同形／数值 3 组／阻断四态／灵敏度（摘键名＋源码变异）——**全部可跑，见 §三**。
- 真库 `calorie_data.db` sha 前后相同（§五）；除既有红（254 同 t290 记账）外不新增红（§六）。

## 二、28 条逐条读数（`GATE-RUN runId=39da1723-fb1c-4183-9bc0-98368e6194c3 exit=1`，探针 `.scratch/t256/probe-28.mjs`）

28 条清单来源：`06-目标管理-工作流程.md` §28 条逐条表（25 场景＋3 自造）。
每条独立临时库（模板库拷贝隔离，写类互不串味），判据＝exit0＋`data.output` 绝对路径在盘＋落盘字节一致＋四断言。

| # | 唤醒词 | exit/字节/doc | # | 唤醒词 | exit/字节/doc |
|---|---|---|---|---|---|
| 1 | 定营养目标 | 0/82713/是 | 15 | 看目标完成度 | 0/71787/是 |
| 2 | 定营养目标(自动算) | 0/73422/是 | 16 | 看即将到期的目标 | 0/69369/是 |
| 3 | 定体重目标 | 0/81360/是 | 17 | 看目标完成率(按周) | 0/79281/是 |
| 4 | 定体重目标(自动算截止) | 0/81702/是 | 18 | 看目标完成率(按月) | 0/79281/是 |
| 5 | 定体重目标(含起始日) | 0/82311/是 | 19 | 改营养目标 | 0/82456/是 |
| 6 | 定饮水目标 | 0/81317/是 | 20 | 改体重目标 | 0/81376/是 |
| 7 | 定饮水目标(自动算) | 0/73286/是 | 21 | 改饮水目标 | 0/81333/是 |
| 8 | 一键定全套目标 | 0/73518/是 | 22 | 暂停所有目标 | 0/80334/是 |
| 9 | 看今日目标 | 0/71508/是 | 23 | 重启所有目标 | 0/80314/是 |
| 10 | 看本周目标 | 0/71508/是 | 24 | 看目标历史完成 | 0/71781/是 |
| 11 | 看营养目标进度 | 0/79281/是 | 25 | 看目标预测达成 | 0/66718/是 |
| 12 | 看体重目标进度 | 0/71712/是 | 26 | 看目标推荐 | **0/1790/否（片段，缺口）** |
| 13 | 看饮水目标进度 | 0/71508/是 | 27 | 看目标配置 | 0/69173/是 |
| 14 | 看目标对比实际 | 0/80610/是 | 28 | 看目标状态 | 0/68490/是 |

注记（非回归，如实）：9/13 同命令（`goal-progress` 今日）产物同形，11/17/18 同命令（`goal-progress` 7d）产物同形——
同命令别名预期如此（t292 ALIGN-NOTE 同款）；不同命令间见 §三不同形判据。

## 三、五组可跑判据（同一次探针运行内）

- **不同形 PASS**：看今日目标区块集 `[sec-copy,sec-goal,sec-history]` vs 看目标完成度
  `[sec-gap,sec-goal,sec-readings]`，不全等（`DIFF-SHAPE-VERDICT: PASS`）。
- **数值 3 组 PASS**（种子：30/男/175/moderate，最新体重 70.6kg，今日 2026-09-07）：
  - 宏量合计：1800/150/200/50 → 合计 1850（`150×4＋200×4＋50×9`），页含 1850——
    出处：老 `nutrition_goal.py:84-85`（`calculated/diff`）、`:111-112`（`|diff|≤50` 合规句）；
    `t251-老技能算式.md` §1.1；`t289-补齐-证据.md` §3.1。
  - 体重速率：70.6→68、截止 2026-12-31（115 天）→ `2.6kg ÷ 115天 × 7 = 0.16 kg/周` 在页——
    出处：老 `weight_goal.py:116-118`（gap/required_daily/7700）、`t251-老技能体重与状态页.md:143-146`
    （per_week/per_month/≥1.0 极端）；新仓 `src/goal/set.ts:112-120`。
  - 饮水推荐：`70.6 kg × 35 ml/kg` → 推荐 2471ml（9 月夏季档）在页——
    出处：老 `nutrition_goal.py:252-271`（显式体重→最新记录→70.0；6/7/8/9 月为夏；35/30）；`t289` §3.3。
  - 另：推荐页片段本身亦印 `TDEE 2564`／`2064`／`141/57/246`／`2471`（cut 模板），与上三组同源。
- **阻断四态 PASS**：
  - D1 空库 wizard（定营养目标）exit0、无假数字（未设置＋不算推荐／不出推荐值／不推荐，且无 2064/2471）——沿 #251 裁定（空库可打开、不编默认值）。
  - D2 改类空库（改营养目标）exit4、不落盘——沿 #252。
  - D3 删档案表后自动算 exit0、页写「缺×，不算」——沿 #251 缺项不编数。
  - D4 方向非法 exit2、不落盘——沿 #251。
- **灵敏度 PASS（两层）**：
  - 摘键名（scratch 级）：完成度页含 `ilife-block-toc`；摘除该键后同一检查必不通过
   （`SENSI-KEY: hasToc=true afterRemovePass=false verdict=PASS`）——形状断言咬得住键。
  - 源码变异（§四两行读数）：`resultDocs.ts:179` `-100 → -90` 必红，还原必绿。

## 四、机器证据台账（持锁 `ticket=256`，`--` 后单命令，无嵌套）

| runId | 命令 | exit | 摘要行 |
|---|---|---|---|
| `6222ef43` | `tsc -b packages/skill-calorie` | 0 | 编译干净 |
| `985342f8` | `goal-wizard-251` | 0 | 4/4（空库完整文档／缺项不编数／三档 1075/1535/1994／方向非法 exit2） |
| `e30a2a16` | `t252-goal-wizard` | 0 | 6/6（11 条有库／定类空库／3 条 exec 双源／无假数字／改类 exit4／暂停重启 exit2） |
| `1e7bca51` | `goal-receipt-253` | 0 | 5/5（骨架完整文档／零内部词／三段在场／改前对照／写语义＋空库 exit4） |
| `20799582` | `goal-result-254` | 1 | **既有红** 1/2：`weeklyDeficit 10886 vs 10841`、`avgDeficit 1555 vs 1549`（与 t290 §四同字段同值，非本票引入，见 §六） |
| `810fa816` | `goal-receipt-289` | 0 | 6/6（三族＋自动算两段＋他域无串段） |
| `56a41882` | `goal-result-290` | 0 | 2/2（六条完整文档＋反向五发） |
| `baea2b52` | `goal-h1-date-566` | 0 | 4/4（三窗 H1 无日期＋窗口住页头＋完整文档＋字串变异） |
| `39da1723` | `.scratch/t256/probe-28.mjs` | 1 | 27/28（§二；红即第 26 条片段缺口） |
| `e4cdb7f6` | `.scratch/t256/mutate-cycle.mjs` | 0 | 变异自证红后绿（下） |

变异自证两行读数（`src/goal/resultDocs.ts:179` 一行改坏致红→逐文件还原致绿，`--force` 重建＋dist 指纹）：
- 红：`MUT-RED: build-exit=0 test-exit=1 hit="AssertionError [ERR_ASSERTION]: vs-30d 缺呈现文本：-70.81 %"`（dist 指纹 `-90` 确认，非旧产物写回）
- 绿：`RESTORE-GREEN: test-exit=0 pass 2 / fail 0`（还原 SHA 与变异前一致 `identical=true`，dist 指纹回 `-100`，`git diff` 无残留）

## 五、真库零接触

- `D:\2Study\StudyNotes\.db\calorie_data.db`（3072000 B）：跑前
  `D40D58F50E74E294A11E410B4D9E2142FCBCD8993328A4EFB453F1A0D62A1083`，
  跑后同值。全部实跑经 `SKILLS_DB_PATH=系统 tmp 临时目录`（251 先例），真库只做读哈希。
- 数值若系回归只记账不顺手改：254 读数漂移（§六）与 26 条片段缺口（§一）均只记账转票，本席源码 diff 为零（除本证据件外无新增件）。

## 六、既有红说明（不新增红）

- `goal-result-254` 1/2 红：`goal-7d` 的 `weeklyDeficit/avgDeficit` 随日期与数据漂移，
  与 `t290-后半-证据.md` §四登记的邻接红**同字段同值**（改前基线 `b8e3bf77` 即红，非本票引入）。
  本席附带靶向三件（289/290/566）全绿；未跑全量测试（纪律禁 `pnpm test`）。
- 本席未建新测试件，故无「有意先红」条目；探针红（26 条）落在草稿盘，不进仓。

## 七、没做完＋下一手

1. **转票（需编排者新开一票）**：把 `calorie.view.goal-recommend` 由片段渲染换整页装配
   （复用 `src/goal/resultDocs.ts` 同一套，不另起第二套装配；命令名、路由、`WAKE_TABLE`、HELP 卡片归属一字不动），
   预期 `看目标推荐` 出完整文档且复用 cut 推荐数（2064/141/57/246/2471）不断言第二遍数值。
2. **锁入仓（待写集扩大到测试件后）**：新建 `packages/skill-calorie/test/goal-lock-256.test.mjs`，
   把本证据 §二（28 条 exit0＋绝对路径＋四断言，26 条届时应转绿）与 §三（不同形一对／数值三组带出处／阻断四态／摘键名灵敏度）写成可重跑断言；
   入仓后重跑本探针（期望 28/28 exit0）＋ §四靶向表＋变异自证。
3. 254 冻结值漂移（`weeklyDeficit/avgDeficit/daysLeft` 随日期）归 #254 另开票重冻，本票不碰。

## 八、结构五步与红线对账

- 一影响清单：本轮只新增本证据件（技能文档归属件自己的目录）；测试与源码零碰。
- 二结构设计：无新目录层级（草稿住 `.scratch/t256/`，忽略清单内，不进提交）。
- 三写码：不适用（无产品代码变更）。
- 四超线报警：未改 `src/**/*.ts`，本包 350 线台账无需同步，不触发。
- 五交付对账：提交只含本证据一件（`git diff --cached --name-only` 复核），偏差零。
- 红线：无 `pnpm build/gen/test` 全量；无切分支/reset/stash/clean/restore 整树/amend/rebase/force push/`add -A`；
  编译入口写死 `node node_modules/typescript/bin/tsc -b <包>`；重建一律 `--force`＋dist 指纹；
  提交全中文、只提交不推送；禁再派下级（全程本席直做）。
