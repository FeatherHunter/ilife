# #270 · 写后回执页 · 15＋1 条 —— 交付报告（实施席＝本席，唯一作者）

## 一、判定

**PASS（本票范围内的判据全绿）**；一条判据**在范围内做不到**，已归因并留读数。

**量现场（先量后判，不采信在飞成果的自述）**：`D:\ilife` 与 `645aa37` 同头，在飞 diff 只有 `src/diet/receipt.ts`（239→350 行，`267＋／156−`）＋ `test/calorie-c43.test.mjs`（`64＋／3−`）＋ 未跟踪的 `.changeset`／`docs/…/t270-报告.md`／`t270-证据/`。逐件读过之后本席的判定是**沿用骨架＋补齐＋修缺陷**，不是照抄：

1. **保留**它摆上的四块标题、改类三列对照、删类四列快照、新增类不硬套对照、今日累计七行与目标对照、复制区双出口与日志第 4 段命令原文、以及「缺值写 `—` 且不拿新值顶替」这条（它已修掉原先把本次参数当改前值的缺陷）。
2. **补齐一处它会打红的地方**：它把 `batchCountsCard`（批量补记三格计数）整块删了，`test/t496-文案统一.test.mjs` 第 ③ 条第 4 条逐字钉着的 `已在库跳过`／`同名同餐的记录不会重复添加`／`分类失败` 因此归零。它自己的报告把这写成「范围外发现」——**归因错了**：那三串是它删掉的。本席把这一块搬进「✅ 操作回执」区块内（不是撤回四块结构）。
3. **核实它的三条自述**：`test/profile-doc-179.test.mjs` 在本检出**未被改动**（它报告 §5 说改了，与事实不符；那件的内容 `645aa37` 就在），本席按票面要求对它一行未动；`docs/agents/wording.md` 那张禁用字表在它的版本里已是 0 命中，本席沿用并保持 0（本报告不复述那张表）；它未提交（**无主 diff**），本席按协议 §3.2 第 4 条／规则 39 自行判定完成度后落盘。

**做不到的那一条**：`pnpm build` **不是 exit 0**——`645aa37` 基线即红，只有一行错：`packages/base-render/test-d/contract-signatures.ts(390,20)` TS2344（#525 把 `ACTION_BAR_DEFAULTS.minHeightPx` 由 40 改成 44，那条编译期断言仍写 40）。红点在 `packages/base-render/`，**不在本票声明路径内**，本席未碰（`git status -- packages/base-render` 为空）。基线读数（本席改动前的同一次 `pnpm build`）与终态读数逐字相同，都是这一行。

## 二、机器证据

| 判据 | 机器读数 | 出处 |
|---|---|---|
| 15＋1 条逐条真出口 | `RESULT: 16/16`（exit 0；`<!doctype html>` 在第 0 字节＋charset＋内联样式；字节 68351～74079；绝对路径逐条在 JSON 里） | `t270-证据/t270-真跑.log`、`t270-真跑-结果.json` |
| 其余写命令产物逐字节未变 | `RESULT: 16/16 逐字节一致`（32 条非饮食写命令逐 sha256 比对，其中 16 条真有产物）＋ `VERIFY-PRODUCTS: 16/16 归一时刻后逐字相同`（两态产物原件逐字复核，差异只有当刻时钟） | `t270-证据/t270-快照比对.log`、`t270-快照复核.log` |
| 变异自证 | `MUT-VERIFY-RED RESULT: 0/16` ⇒ 逐文件点名还原 ⇒ `MUT-VERIFY-GREEN RESULT: 16/16`；`DIST-POLLUTION-CHECK … polluted=false` | `t270-证据/t270-变异自证.log` |
| 全包测试不新增红 | BEFORE `tests=2381 pass=2280 fail=99` → AFTER `tests=2389 pass=2289 fail=98`；**新增红 0 条**；由红转绿 1 条（`#269／#270 饮食写命令已是完整文档，且四块标题（老实物逐字）在产物里`） | `t270-证据/t270-测试归因.md` |
| 本票新测试 | `test/t270-回执四块.test.mjs` 6 条全绿 | 同上 |
| 告警线台账 | `RESULT: 82/82`＋`PASS` | `t270-证据/t270-告警线台账.log` |
| 生成物 | `pnpm gen:check` PASS（127 条命令：写 46＋读 81） | `t270-证据/t270-生成物检查.log` |
| 编译 | 基线 `pnpm build` 与终态 `pnpm build` 同为一行 TS2344（见 §一） | `t270-证据/t270-pnpm-build.log` |
| 提交 | `1e3f1a9`（增量提交，逐条 pathspec 点名，未推送） | `git log -1` |

**必报五步第四步（本席那一行）**：`src/diet/receipt.ts` **LF=379，已超线，需要根据规则进行重构。** 超因＝一件同时住着 15＋1 条写命令的整页回执（四区块各自的取词与分支／两套对照表／今日累计七行／页框与复制区／件头的老实物对照与数据面缺口登记）；拆法＝按**区块**切姊妹件 `diet/receiptChange.ts`（`snapshotTable`／`changeRowsOf`／`beforeOf`／`beforeMissing`），**本次先不拆**（结构重排不在本票写集，四块边界还要过肉眼验收）。已写进台账该行「结论」列。

## 三、逐条路径

- 源码：`packages/skill-calorie/src/diet/receipt.ts`（唯一作者，239→379 行）
- 测试：`packages/skill-calorie/test/t270-回执四块.test.mjs`（新增）；`packages/skill-calorie/test/calorie-c43.test.mjs`（只收窄本票两条读数：C6 与「改类对照」，「删类快照」为新增）
- 台账：`packages/skill-calorie/AGENTS.md`（`--sync` 全表口径：改 7 行；其中 6 行是 `645aa37` 上就有的他席陈旧值：`src/weight/history.ts` 862→878、`src/render/sportPortDocs.ts` 757→666、`src/diet/nutritionPortDocs.ts` 675→645、`scripts/build-help.mjs` 504→386、`scripts/gen-photo-baseline.mjs` 429→283、`src/home/homeDocs.ts` 328→334；**只有 `src/diet/receipt.ts` 350→379 是本票的**），本席另把手写的那一行「结论」列补成超因＋拆法
- 可复跑脚本：`docs/skills/skill-calorie/t270-真跑.mjs`、`docs/skills/skill-calorie/t270-快照比对.mjs`（`--record`／`--verify-products` 两个开关）
- 证据件：`docs/skills/skill-calorie/t270-证据/`（10 件，含两态测试红清单与逐条归因）
- changeset：`.changeset/t270-写后回执页15+1条.md`
- 日志：`.scratch/t270/`（build／tsc／tests／台账／快照／变异／两态换件脚本）

## 四、未做项与下一手缺什么

1. **「改前」真值与「按条件删」的删前逐条原值取不到**：读数只在写执行那一层（`fetch/diet.ts` 回 `before`），`src/diet/edit.ts`／`src/diet/products.ts` 把它丢了——两件都不在本票声明路径内，本席未碰。本页现在＝**带了就出、没带就出 `—` 与一句空态说明**，不编数。下一手要拿到真值须改那两件。
2. **`pnpm build` exit 0 做不到**（§一）：红点 `packages/base-render/test-d/contract-signatures.ts:390`，归属 #525 那条线（错在断言未跟值走，不在 #525 的写集里也是当刻事实）。要本票绿，须由那一线把断言改成 44。
3. **视觉验收**：16 张产物在盘（路径见 `t270-真跑-结果.json`），按判据由项目负责人肉眼看，本席不做脚本化视觉判定。
4. **`packages/skill-calorie/src/triggers/routes.generated.ts` 被 `pnpm gen` 重生成**（1 行，`查高热量排行` 的窗口参数补齐，属他票声明）、**本席未提交**（派单 §五：生成物不许 `git add`）。工作树里留着它；`gen:check` 因此 PASS，收口时按生成物流程处置。
5. **环境补课（本席实测到的派单不符）**：`D:\ilife-wt\wf155-e` 只有根 `node_modules` 是指向 `D:\ilife\node_modules` 的 junction，**每个包自己的 `packages/<件>/node_modules` 全缺** ⇒ `tsc -b` 在 `import … from 'base-paint/blocks'` 上全数 TS2307，`pnpm build`／`pnpm test` 都跑不起来。本席用 `.scratch/t270/link-node-modules.mjs` 把主工作区那份链接图照抄过来（仓内包指向本检出、pnpm 存储指向共享只读目录），并清掉随之产生的废 dist 后重编。**这是依赖脚手架、未进版本控制**；同批检出的其他窗口若也编译不动，缺的是同一样东西。
6. **`tsc -b` 的跳过陷阱（本席实测）**：换源码内容后 `tsc -b` 仍可能判「没变」而跳过，`dist/` 留旧产物 ⇒ 判据读的是旧编译态（本席因此白跑一轮全量测试）。凡「换件→比前后」的窗口一律加 `--force`，并在换件后核 `dist` 指纹。
7. **未用共享闸门锁**：本检出隔离、无并发写者，`tooling/run-locked.mjs` 未跑，故没有 `GATE-RUN` 留痕导出件；编译／测试／`git add`／`git commit` 都是本窗内顺序执行、逐条落盘。
