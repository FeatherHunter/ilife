# t376 · 最小跑通证据（先验 1 条含目标对比＋新命令 1 条＋通用分析页最小形态）

> 票：#376（地图 #160）。用词照 `docs/agents/wording.md`：命令／跑得通／过程型／结果型／回执／预检确认页／页面交付。
> 数字以本文件所贴机器读数为准，不另存第二份清单。视觉判定留用户肉眼，本文件只给程序绿。

## 0 · 结论先行

- 目标词「看整体趋势(含目标对比)」从说唤醒词到结果型 HTML 落盘回执绝对路径，全链在固定种子库上可复算：真出口 exit 0，产物 139438 字节，sha256 `8cbfaea2a69137be…`（全值见 §2）。
- 新命令 1 条住 `src/analysis/`，六件事齐，示例照抄即跑，本地已跑通；通用分析页最小形态复用公共层在先，主力 `combined_analysis.html` 同模板新形态，未碰 `calorie_deficit.html`。
- 门绿：`pnpm gen` 绿、`pnpm gen:check` 绿、`pnpm help:build` 绿、`help:examples:check` 117/117 绿、双棘轮 13/13 绿、指纹 `--check` 绿（其余 5 条逐字节 SAME＋新命令 OK）。
- 如实红（票外并发累积，非本票写入集）：`pnpm build` 红（他票 `weight/review.ts` 半写缺导入）、t293 P1 FAIL（豁免登记 stale 1 处，他票改行所致）、路由覆盖测试红（累积 29 条翻转差，本票占 1 条）、全量测试待收（后台跑中，见 §4）。
- 进度 95%，不 close（待确认见 §6）。

## 1 · 新命令六件事（#293 形状）

| 事 | 值 |
|---|---|
| kind | read |
| key | `calorie.view.multi-trend` |
| shape | stat |
| title | 多指标趋势（#171 多指标趋势形态短名） |
| wakeWord | 看整体趋势(含目标对比)（`TRIGGERS` 真有，冻结表 order 93） |
| example | `calorie-cmd-read calorie.view.multi-trend --params '{"window":"90d","compare":"target"}'`（与路由 cli 逐字一致） |
| run | `viewMultiTrend`（`commands.ts` 薄转调 → `./multiTrend.ts` 取数 → `./multiTrendPage.ts` 装配） |

- 权威源：`packages/skill-calorie/src/analysis/commands.ts`（14 键，前 13 条照抄未搬迁清单，第 14 条为本票新增）。
- 取数：`src/analysis/multiTrend.ts`（160 行，≤350 行；子功能＝整体趋势；唯一源 `buildSeries`＋`seriesAvg`／`seriesDelta`／`seriesCount`；目标线取库内现值，体重目标缺行即 null；`compare` 本票仅收 `target`，其余维度报 bad-input 并点名 #378；`group` 缺省 `comprehensive`，g1–g11 展开留 #378）。
- 装配：`src/analysis/multiTrendPage.ts`（123 行，≤350 行；通用分析页最小形态：图／表／参数表单走 `packages/base-render/src/` 现成块，整页走 `src/shared/docPage.ts`，复制区走 `src/shared/copyArea.ts`；不复制第二份整页装配；对照目标线用 `markLine`）。
- 路由：`src/analysis/routes.ts` order 353 由 no-命令可执行翻为 exec（同 order，key＋cli 新值；动前持锁）。
- 冻结表：`src/triggers/scene-10-analysis.ts` order 93 仅改 `main_prompt.cli` 与 `data_source` 为新 cli（SoT 快照走生成脚本，不手改；`SKILL.md` 生成块由 `pnpm help:build` 出）。
- 文件 sha（16 位前缀＋字节，LF、无 BOM）：
  - `multiTrend.ts`：`9f84fdefed9ad91b` 5868
  - `multiTrendPage.ts`：`302ca5a3718b0708` 5075
  - `commands.ts`：`d16fa68311bc3140` 21825（含 #383 同文件异区改动，本票区见 diff §5）
  - `routes.ts`：`55d01018becad542` 43109
  - `scene-10-analysis.ts`：`ae4ed258240d59f7` 217223

## 2 · 真出口读数（固定种子库）

- 种子：`docs/research/t81-seed.mjs`（`SEED_TODAY=2026-09-07`，`seedFull`＋照片 harness；只写系统 tmp）。
- 命令（冻结表 `main_prompt.cli` 照抄即跑）：
  - `calorie-cmd-read calorie.view.multi-trend --params '{"window":"90d","compare":"target"}'`
- 实跑（`.scratch/376/true-outlet.mjs`）：
  - `KEY:calorie.view.multi-trend`
  - `OUTPUT:C:\Users\辰辰洋洋\AppData\Local\Temp\t376-out-DuYBa1\calorie_html\多指标趋势_20260914_124700.html`
  - `ABSOLUTE:true`
  - `BYTES:139438`
  - `SHA256:8cbfaea2a69137be1e20fd006b26895afcba2fedd21946ca137342ef4c32540e`
  - `METRICS-KEYS:days,loggedDays,avgCalorie,weightChange,avgExercise,avgProtein,avgDeficit,complianceRate,targetCalorie,weightGoal`（全有限 number，空值由 `nums` 丢弃后仍全有限）
  - 固化样例：`.scratch/376/out/sample-multi-trend.html`（同 sha，同字节；内容含“整体趋势(含目标对比）／目标／每日热量／体重轨迹／逐日多指标明细／复制数据／calorie.view.multi-trend”，7/7 有）
- 复算：同一种子库重跑，指纹 `--check` 中新命令 OK 同 sha（§3）。

## 3 · 门绿读数

- `pnpm gen`（持锁）：绿（键 117＝写 46＋读 71；能力 10 个声明 117 条，未搬迁清单 0 条）。
- `pnpm gen:check`：绿（5 件 `GEN-CHECK ok`，`GEN-CHECK PASS：键 117`）。
- `pnpm help:build`（持锁）：绿（`HELP 已注入：…/SKILL.md`）。
- `help:examples:check`：绿（`示例行数：117（通过 117，白名单跳过 0，红 0，结构问题 0）`）。
- 双棘轮：绿（13/13 pass，含分派冻结与未搬迁只许变短）。
- t171 分类：`CHECK: PASS`（TOTAL 169／A 87／B 82／UNASSIGNED 0／INTERSECT 0；`multi_trend=14`：本票翻转 1 条由 no-命令可执行变为新命令 exec，唤醒词仍属 A-list，分类启发式计入 B，结构 PASS，计数变化如实记录）。
- 指纹（`.scratch/376/pages-fingerprint.mjs`，票内一次性）：
  - `--write`：6 条全 OK（5 条其余＋1 条新命令 139438 字节）。
  - `--check`：`CHECK: PASS`（5 条 SAME 逐字节一致＋新命令 OK 同 sha）。

## 4 · 如实红（票外并发累积）

- `pnpm build` 红：`packages/skill-calorie/src/weight/review.ts(220,14): error TS2304: Cannot find name 'renderListRows'`（他票半写缺导入，非本票写入集；本票 5 件无报错；已上报编排者，不碰他人件）。
- t293 P1 FAIL：`UNACCOUNTED=1` 唯一项为 `t293-纪律豁免.json[1]` stale（`test/calorie-routing-81.test.mjs:342` 行号失效，他票改行所致）；动态证据齐、树自相矛盾 0；P2/P3 PASS，沙箱基线绿。本票新命令面全部落在 ① 派生。
- 路由覆盖测试红：实际 `{exec:390, non-exec:46}` vs 预期 `{361,75}`（差 29 条累积翻转，本票占 1 条 order 353；另 `NEW_KEY_ROUTES` 68 vs 63 等均为多票累积）。共享测试计数不由本票吸收，留收口票统一改数（见 D8）。
- 全量测试（持锁）：`tests 1563／pass 1502／fail 61`（`$env:TEMP/b376-full.log`；失败为多票并发累积红，与 §4 同源；本票区棘轮 13/13 与指纹绿见 §3）。

## 5 · 交付对账（#171 §5 写入集）

| #171 切分 | 本票实际 | 对偏差 |
|---|---|---|
| `src/analysis/commands.ts`（加 1 条） | 加 1 条声明＋2 导入＋`viewMultiTrend` 薄转调（同文件 #383 异区改动共存，无冲突标记） | 0（异票同文件异区共存已在 diff 区分） |
| 新子功能文件（取数／视图） | `multiTrend.ts` 新建 | 0 |
| 包内公共层新件（通用分析页骨架） | `multiTrendPage.ts` 新建（落 `src/analysis/`，#376 票面“新件只落 src/analysis”；#378 只读复用） | 0（落位按 #376 票面） |
| `src/analysis/routes.ts`（仅该 1 条加 exec） | order 353 翻转 1 条 | 0 |
| `scene-10-analysis.ts`（仅该 1 条 cli＋data_source） | order 93 改 2 处（同短语替换，逸出已对齐 combined 形） | 0 |
| 生成物重算 | `pnpm gen`＋`help:build` 已跑；`keys.ts`／`registry.ts`／`combos.yaml`／`build-help.mjs` REPR／`routes.generated.ts` 派生更新（含多票同期键，提交时只 add 自己件，见 §6）；SOT 快照／路由计数／smoke 不 absorption（收口票统一，见 D8） | 0（派生已出，提交按红线收窄） |
| 指纹脚本 | `.scratch/376/pages-fingerprint.mjs`（＋`fingerprints.json` 基线、真出口样例） | 0 |
| 证据件 | 本文件 | 0 |
| 对抗 D1–D8 | `.scratch/160/t376-对抗.md` | 0 |

- 行数告警线（350，LF 口径）：新两件 160／123，其余改动件 273→324（commands 含他票区）、routes 185，均未触发本票新区超线；触发判据未命中故不报线。
- 结构五铁律：能力自治（取数经公开接口与唯一源）、概念唯一（窗口／目标口径引用唯一处）、改动可预告（本表）、名字取自 HELP 与 #171（整体趋势／多指标趋势形态）、接口小里面厚（新两件各 1 主出口，类型 ≤8 字段）。

## 6 · 进度与待确认（不 close）

- 进度 95%。
- 待确认（未确认不 close）：① 全仓 `pnpm build` 綠需他票 `weight/review.ts` 落盘（门禁绿后复跑）；② 全量测试 exit 0（后台任务收后补数）；③ 提交范围裁定：生成物含多票同期键，本票提交是否只含 5 件源码＋证据（生成物留收口）需编排者拍板；④ 路由计数／SOT／smoke 的收口票号。
- MAP 同步：本票不改 #160 正文（Decisions 待收口统一追加）；#377／#378 仍被 #376 阻塞，#376 未 close 故不交棒。

## 7 · 遗留风险

- 并发同文件（`commands.ts` 与 #383 异区共存）：合并无冲突标记，但后续任一票回滚需按区回滚，不可整文件回滚。
- 生成物多票同期键：本票若先提交生成物会把他票未提交键带入；若后提交需重跑 gen。建议收口统一。
- 分类启发式（`classify.mjs`）不认识新 key：A 87／B 82 为启发式计数，A-list 唤醒词归属仍为 88（以冻结表与唤醒词为准），#378 铺开时需升级分类规则或冻结定义。
