# t456 围度键集对账 · 独立复核（地图#158）

## 1 判定：PASS（96 分，无 S1、无过程违规；S3×1 另票收口 t440 基线）

- 五维：契约一致 26/30（验收 1–3 独立复现全绿；验收 4 之 t361 绿、t440 红系范围外基线红，扣 4）／证据真实可复现 25／新旧对照 20／工程红线 15／文档同步 10，合计 96。
- 缺陷清单：S1 无；S2 无；S3-1（范围外）：t440 判据③④红 `heavy=[["body/receipt.ts",2],["fetch/body.ts",13]]`，`receipt.ts:124` 的 `GROUP_HEADS` 含胸围／左大腿 2 名系 HEAD 已提交内容（#537/#539），本票 diff 四件之外，不证伪本票结论，按 §5.1 只转票不判 FAIL。
- 自设新探针结论：把 TOP 表 `shoulder_cm` 移到表尾（集合不变）→ t456 仍绿：t456 按设计对次序盲（票面明禁拉齐次序），属口径边界非缺陷，只记录。

## 2 机器证据（复跑 sha＋GATE-RUN＋新探针红绿两行）

- 复核对象：`fd86d1d14d4b9c5e5ae39564acd2ceb30b705087`（src＋test）、`d18a23edb083b10a4f0f4c5da85a9becd56f46ea`（证据＋changeset），均为 HEAD 已含提交。
- 实现 GATE-RUN 对账（`.scratch/locks/gate-runs.log` 逐条命中、exit 一致）：`f614a48d` build exit 0；`985a4965` t456 exit 0；`029f66a0` t361 exit 0；`6238f1e2` t440 exit 1；M1 红 `c8b19c57` exit 1／绿 `5523e14e` exit 0；M2 红 `42a79c0d` exit 1／绿 `14353d9a` exit 0；终态 `8d5e3da3`／`10646dcb`／`6172688c` exit 0、`28b136bc` t440 exit 1（同因）。
- 独立复跑（持目录锁两窗，dist 级 transient 变异、受跟踪源码零写入，日志 `.scratch/t456/review-*.log` 凭 `REVIEW-MARKER` 行复核；`.scratch` 被忽略未入仓）：
  - `review-build.log` exit 0；`review-t456.log` exit 0（pass 1/fail 0，`T456-READOUT columns=13 top=13 shoulder_pos(columns)=13 shoulder_pos(top)=5 … VERDICT=GREEN`）；`review-t361-bis.log` exit 0（pass 8/fail 0）；`review-t440.log` exit 1（pass 6/fail 1，`别处不许再出现第二份 13 名表（实测 [["body/receipt.ts",2],["fetch/body.ts",13]]）`，与实现同因）。
  - M1（列序侧加 `fake14_cm`）：`review-mut1-red.log` exit 1（pass 0/fail 1，`展示列序表应有 13 项（实测 14）`）；`--force` 重建后 `review-mut1-green2.log` exit 0（pass 1）。
  - M2（TOP 侧删 `hip_cm`）：`review-mut2-red2.log` exit 1（pass 0/fail 1，`TOP 平局次序表应有 13 项（实测 12）`）；`--force` 重建后 `review-mut2-green2.log` exit 0（pass 1）。
  - 新探针 reorder（TOP 表 `shoulder_cm` 移表尾，集合仍 13 相等）：`review-probe-reorder2.log` exit 0（pass 1，`shoulder_pos(top)=13 … VERDICT=GREEN`）；`--force` 重建后 `review-final-green2.log` exit 0（`shoulder_pos(top)=5` 复原）。
- 过程披露：首窗用增量 `tsc -b` 恢复 dist 撞 stale-dist（还原后假红），已改 `--force` 重建纠正并完整复现红→绿；变异脚本与 runner 为 `.scratch/t456/review-*.cjs`（自有路径）；终态受跟踪件零残留（`review-srcstat.txt` 空）。

## 3 逐条路径（改动件、证据件、日志件）

- 改动件：`packages/skill-calorie/src/analysis/cross.ts` 仅 `const`→`export const` 一词（292 行，线内，次序与用法未动：复跑两集合字面与实现证据一致）；新建 `packages/skill-calorie/test/t456-围度键集对账.test.mjs`（40 行，只读 dist，不开库，tmp 库仅 t361/t440 用）。
- 证据件：`docs/skills/skill-calorie/t456-围度键集对账证据.md`（GATE-RUN 引用、变异读数、t440 归因均与本席复核一致）；`.changeset/t456-围度键集对账.md`（517 字节／10 行，UTF-8 严格解码通过，中文正常，先前花屏系控制台代码页所致）。
- 日志件：`.scratch/t456/review-*.log`（本席复跑）＋`review-{run,patch-m1,patch-m2,patch-probe,extract}.cjs`（可复跑脚本）；实现草稿 `.scratch/t456/` 原样未动。
- 结构与并发：`cross.ts` 292 行＜350，第四步不触发；`body.ts` 本席只读（工作区 `M1/M2-ANCHOR` 注释摘除两行是他席在途改动，未提交，本席未碰）；`AGENTS.md` 台账两次同步是他席改动，与本票无关。

## 4 未做项＋下一手缺什么

- 未做：t440 基线回绿（`body/receipt.ts` 不在本票写集，跨界停手）；遗留出口（两处合一次序另开票、`triggers` 第三份表收编另议）不在本票。
- 转票措辞（S3-1）：「t440 基线收口：`packages/skill-calorie/src/body/receipt.ts:124` 的 `GROUP_HEADS` 以胸围／左大腿为分组键（#537/#539 已提交），致 t440 判据③④ `heavy` 检出 `[receipt.ts,2]`；请认领（判据豁免分组键单点引用或改写该两处字面）或清退，t440 回绿后关闭。」
- 下一手：本席只提交复核报告一件（中文信息、无 push，收口由编排者统一推）；合并点认 `review-final-green2`（t456 exit 0）与 `review-t361-bis`（t361 exit 0），t440 以新票为准。
