# #757 证据 · 卡路里设置页收窄（照记账样板铺开）· **技能侧**（回执扩组 ＋ 落点算式收口 ＋ 写盘口径 ＋ 删键 ＋ 体检连带）

判定：**PASS**（技能侧判据全绿：新增 12 条用例 12／12、包级全量 1867 条里 1827 绿、
告警线台账 93/93、`boundaries: PASS`、真出口回执与 `~/.ilife/calorie.yaml` 取值逐字对账一致、
两处变异自证「打掉必红／还原必绿」跑齐）。**40 处红是既有／别席在途（见末节「范围外发现」），与本票无关**，
判定口径照记账 #749：红在改之前或改之外，且本票未重录任何指纹账本。

## 一、交付了什么（逐件一句，为什么）

| 件 | 改了什么 | 为什么 |
| --- | --- | --- |
| `src/config.ts` | 默认值表删 `land.scheduleCli`／`land.memoCli`（14→12 键）；`CALORIE_CONFIG_RETIRED` 填上这两键；加 `writableDefaults()`（可改落点 `db.dir`／`photos.dir` 写绝对路径）＋ `withoutBlankEditableDirs()`（读仍认空串） | #748 定稿的删键 ＋ #746 总口径回灌（读认空串、写落绝对路径）＋ #762 过渡（读容忍、写即清） |
| `src/paths.ts` | 落点纯算式收成一族（`dbDirOf`／`dbFileOf`／`htmlDirOf`／`photosDirOf`／`gifsDirOf`／`stateDirOf`）＋ `PHOTOS_DIR_NAME`／`XUNJI_STATE_DIR_NAME`／`HTML_DIR_NAME` 具名引用 | 票面「回执扩解析后绝对路径组」＋ 样板「落点算式只许有一处定义地」；面板不拼路径 |
| `src/cli/config.ts` | `calorie.config.read` 回执 `data` 新增 `resolved` 七格（生效数据目录／库文件／产物目录／照片目录／GIF 子目录／训记状态目录／动作库）＋ `resolvedCaloriePaths()`（只算路径、不建目录） | #746 处置：只读展示要的绝对路径由**技能侧回执**给出 |
| `src/photo/photos.ts` | `resolvePhotosDir` 空串不再抛，解到默认落点 `<库目录>/photos`（调 `photosDirOf`）；注释同步 | #748 定稿：`photos.dir` 默认改 `<数据目录>/photos`，「未配置」状态消失（行为反转已接受） |
| `src/photo/dir.ts` | 写侧注释改「缺省落默认目录」（读侧容忍缺省不变，视图照常出） | 同上，读写分流口径说清 |
| `src/xunji/catalog.ts` | 删 `XUNJI_CATALOG.machine`（`~/.minimax`）＋ `homedir` 导入＋两档注释；只剩 `preset` | 票面「删掉老机器路径的读取点」；转出面仍五件（铁律五，`t606` 锁死） |
| `src/xunji/rateLimit.ts` | `xunjiStateDir()` 空串改解 `stateDirOf(dbDir)`（`<库目录>/xunji`，不再 `~/.mavis`）；调 `paths.ts` 纯算式 | 同上；三份状态文件（限频／同步游标）从零重算，不丢训练数据（#748 连带） |
| `src/xunji/run.ts`／`run-sync.ts`／`index.ts` | 缺省注释改新落点（`~/.mavis` → 默认落点）；能力门注释改单档 | 注释随实况，不留第二套口径的文字 |
| `src/fetch/audit.ts` | 件头改「老机器路径已退场」（#748 清理点那处注释） | 同上 |
| `src/output.ts` | `htmlDir()` 改调 `htmlDirOf` 纯算式；`HTML_DIR_NAME` 改薄转出 `paths.ts`（删掉本件那份定义） | 同一件事不留第二个定义地（照记账 `helpPaths` 改调 `fetch/paths` 的先例） |
| `src/workout/landRunner.ts` | `crossSkillCliPath` 不再读配置（删 `scheduleCli`／`memoCli` 取值），只按包布局算；指路文案改「出口按包布局推断（设置页已不再提供这一项，见 #748 定稿）」 | #748 定稿删键 ＋ 连带二「按包布局推断 ＋ 先构建」；补注二「页面已只读化，做不到的指引改掉」 |
| `src/workout/landBatch.ts`／`xunjiRunner.ts` | 挡板缝注释改新通道（跨技能走文件缝、训记走 `xunji.cli` 页外键） | #762 遗留出口要的**测试缝替代通道**（见下「测试缝」一节） |
| `src/health.ts` | `photos.dir` 判据改新口径（空串按默认落点，还没建＝绿，不再有「未配置」黄）；`xunji.stateDir` 默认改 `stateDirOf`；删 `XUNJI_STATE_DIR`（`~/.mavis`）常量；`land.cli` 判据不再读配置（恒推断，来源恒默认值）；`xunji.catalog` 改调 `XUNJI_CATALOG.preset`；三处只读项指引改指配置文件（`html.dir`／`stateDir`／`catalog` 的「改到别处」→「编辑配置文件改…」） | 票面「配置体检判据改新口径」＋ 补注 2 体检连带（删 `ILIFE_CONFIG_DIR` 那句——本家本就没有，只改后半） |
| `test/t757-设置页收窄.test.mjs`（新） | 12 条：回执七格齐＋逐字对账／读认空串＋读不写回／保存与重置落绝对路径／空串默认落点／删键容忍＋写即清＋护栏／老机器退场／体检同源＋无未配置无环境变量无页面指引 | 票面《验收命令》 ＋ 两条补注的正面验收 |
| `test/helpers/land-inferred-stub.mjs`（新） | 文件缝：在 `before` 把 `land-fixture.mjs` 暂放到推断位置（位置由生产函数回认），`after` 原样还原 | #762 遗留出口：「卡路里那家必须先定测试缝替代通道」（0 环境变量、子进程测试接不了函数参数，故用文件缝，见本节下） |
| `test/t612`／`t613`／`t614`／`cmd-write-40-persist`／`land-fixture.mjs` | 接文件缝（`before/after` 两行）＋ 配置里删 `land` 两键（写了也被退休清单忽略）＋ 件头隔离口径改文件缝 | 删键后旧缝失效，不接新缝即全红（实测：真调兄弟包撞飞书未登录，见 §四） |
| `test/fetch-t4.test.mjs` | 照片断言改新口径（空串解默认落点＋建目录，不再抛） | 行为反转的正面断言 |
| `test/t606-训记模块骨架.test.mjs` | `machine` 断言改退场断言（`undefined`＋只剩 `preset`） | 老机器路径退场的正面断言；能力门仍五件、零深引仍绿（`cli/config` 与 `health` 改走 `xunji/index.js` 门） |
| `packages/skill-calorie/AGENTS.md` | 告警线台账 `src/health.ts` 当场实测 537 → 539 | `--sync` 同步（本门就是这么设计的） |
| `test/plugin-calorie/test/health-706.test.mjs`（插件侧，顺手） | F 组件头改新口径（黄只留 GIF／状态目录还没建） | 口径随实况（断言本身 7／7 绿，一行未改） |

键表两行没动之外：`xunji.cli` 页外键不动（测试缝保留）；`land.xunjiSeconds`／`land.landSeconds` 只读化但键不动。
本轮配置键 14 → 12（`land.scheduleCli`／`land.memoCli` 出表，进 `CALORIE_CONFIG_RETIRED`）。

### 测试缝替代通道（#762 遗留出口的交接）

- 为什么是文件缝：删键后跨技能两步（作息／备忘）在测试里绝不能真调（写真实库、要飞书登录）；
  0 环境变量不许加开关；子进程测试（`execFileSync` 走真 CLI）接不了函数参数。⇒ 同一份 `land-fixture.mjs`
  改由文件缝交付（暂放到推断位置），`xunji.cli` 页外键照旧走配置。生产代码零测试特判（`landRunner` 无测试分支）。
- 位置永不同步：helper 不手写路径，装完后调生产 `scheduleCliPath()`／`memoCliPath()` 回认，走散当场红。
- 并发与崩溃：测试经 `run-locked` 排队（同一时刻一进程）；`after` 还原（断言失败也走）；进程崩溃残留按件头办
  （重编兄弟包两份 `dist` 即可，`dist/**` 在忽略清单里，打不到主干）。实测 `t612 11／11`、`t613 19／19`、
  `cmd-write-40-persist 22／22` 全绿，还原后兄弟包真 CLI 照常（见 §二）。

## 二、机器读数（门禁运行，逐条与 `.scratch/locks/gate-runs.log` 对账）

| 命令 | exit | 摘要 |
| --- | --- | --- |
| `tsc -b packages/skill-calorie packages/plugin-calorie` | 0 | 宿主侧两包编译通过 |
| `pnpm --filter dsh-calorie run build:client` | 0 | client 束重建（`dist/client.js` 77.87 kB） |
| 技能侧 t757 单件 `test/t757-设置页收窄.test.mjs` | 0 | `tests 12／pass 12／fail 0` |
| 技能侧包级全量 `test/*.test.mjs` | 1 | `tests 1867／pass 1827／fail 40`——40 条红见「范围外发现」，均非本票引起 |
| 插件侧包级全量 `test/*.test.mjs` | 0 | `tests 85／pass 85／fail 0`（含 t757 新 17 条） |
| 告警线台账 `--sync` 后复跑 | 0 | `RESULT: 93/93`（`health.ts` 537→539） |
| `tooling/check-boundaries.mjs` | 0 | `boundaries: PASS` |
| 跨包锁 `test/panel-copy-743.test.mjs` | 0 | 5 条全绿（③ `prefillFrom` 仍只有 `db.dir` 标——本票新加的是 `prefillResolved`，锁不破） |
| 变异自证（技能侧，见下） | 0 | 红 `pass=10 fail=2` → 还原后绿 `pass=12 fail=0` |
| 变异自证（插件侧，见插件证据件） | 0 | 红 → 还原后绿（见 `docs/plugins/plugin-calorie/t757-证据.md`） |

### 真出口回执（票面《验收命令》第 3 条，逐字）

```
{"version":"0.1.0","skill":"calorie","shape":"detail","key":"calorie.config.read","data":{
 "path":"C:\\Users\\辰辰洋洋\\.ilife\\calorie.yaml","dataDir":"C:\\Users\\辰辰洋洋\\.ilife\\data","created":false,
 "values":{"db":{"dir":"","name":"calorie_data.db"},"html":{"dir":"calorie_html"},
           "photos":{"dir":"","gifs":"gifs"},
           "xunji":{"key":"","cli":"","stateDir":"","catalog":"","backfillDays":1},
           "land":{"xunjiSeconds":300,"landSeconds":60}},
 "resolved":{"dbDir":"C:\\Users\\辰辰洋洋\\.ilife\\data",
             "dbFile":"C:\\Users\\辰辰洋洋\\.ilife\\data\\calorie_data.db",
             "htmlDir":"C:\\Users\\辰辰洋洋\\.ilife\\data\\calorie_html",
             "photosDir":"C:\\Users\\辰辰洋洋\\.ilife\\data\\photos",
             "gifsDir":"C:\\Users\\辰辰洋洋\\.ilife\\data\\photos\\gifs",
             "stateDir":"C:\\Users\\辰辰洋洋\\.ilife\\data\\xunji",
             "catalog":"D:\\ilife\\packages\\skill-calorie\\src\\xunji\\data\\训记官方动作.json"}}}
```

对账：本机那份配置里 `db.dir`／`photos.dir`／`xunji.stateDir` 都是空串（＝按默认落点），故七格全落在
`<家>/.ilife/data` 之下（照片 `…/photos`、状态 `…/xunji`）；`catalog` 空串＝包内预置那份（31,536 字节）。
七格与那份文件**逐字一致**（`land` 组已无 `scheduleCli`／`memoCli`——出表，进退休清单）。

### 老配置行为（票面第 4 条，按 #762 过渡方案逐字）

沙盒复演（维护者真配置全程不碰）：临时家目录里放一份**含已删键**的 `calorie.yaml`
（`land.scheduleCli: ""`／`land.memoCli: ""`，余同本机那份）⇒ `calorie.config.read` **exit 0**，
`values.land` 里没有这两键（跳过校验、不进取值）；再调一次 `calorie.config.write`（与面板「保存」同一条代码路径）
⇒ 文件里那两个键消失、其余取值逐字不变（前后 diff 进本件测试 ⑤ 那两条，12 条里在册）。
反例：加一个集合外的键（`db.dirr`）⇒ 照旧硬失败、`ERR 1: 不认识的配置项「db.dirr」…（第 4 行）`（护栏没拆）。

### 落点默认值写绝对路径（补注 1，两条读数）

沙盒：删掉 `<家>/calorie.yaml` 再跑一条只读命令 ⇒ 落下来那份文件里 `db.dir` 与 `photos.dir` 是**绝对路径**
（`…/.ilife/data` 与 `…/.ilife/data/photos`，那两格直接用 `configPaths(stem).dataDir` 派生，不自己拼）；
再把那两格改回 `""` ⇒ 仍按默认落点工作（回执 `resolved` 照旧，两条读数都在测试 ①②③ 里）。

### 体检口径（票面第 5 条）

`calorie.config.check` 回执（本机真跑，见上）：`photos.dir` 那条绿「还没建：`…/data/photos`（首次记身材照时自动建）」——
**不再出现「未配置」**（全文 `未配置` 只剩训记 KEY 那条凭据提示，与落点无关）；全文无 `ILIFE_CONFIG_DIR`；
无「页面上去改」类指引（只读三项的 action 已改「编辑配置文件改…」；可改两项与 KEY 的「配置页填」保留——页面给改）。

## 三、变异自证（打掉一处 ⇒ 必红；还原 ⇒ 必绿）

**产物级变异**（不是源码级），原因照记账 #749：本工作区此刻有多席按锁排队提交，
源码级变异有被扫描提交进主干的风险；`dist/**` 在 `.gitignore` 的忽略清单里，
改它打不到主干，还原路径也正是协议要的「按既定的重新生成流程复原」（`tsc -b`）。

| 变异 | 红读数 | 还原读数 |
| --- | --- | --- |
| 把回执 `resolved` 组里的 `catalog` 那一格打掉（`dist/cli/config.js`） | `exit=1 tests=12 pass=10 fail=2`（①「七格齐」与 ⑦「同源」两条） | 逐字节还原 ＋ `tsc -b packages/skill-calorie` ⇒ `exit=0 tests=12 pass=12 fail=0` |

（插件侧那条在 `docs/plugins/plugin-calorie/t757-证据.md`。）

## 四、结构纪律（必报五步）

- **第一步影响清单**（开工前已报）：`packages/skill-calorie/src/{config,paths,health,output}.ts`、
  `src/cli/config.ts`、`src/photo/{photos,dir}.ts`、`src/xunji/{catalog,rateLimit,run,run-sync,index}.ts`、
  `src/workout/{landRunner,landBatch,xunjiRunner}.ts`、`src/fetch/audit.ts`、
  `packages/plugin-calorie/src/{settings,contract,client,index}.ts`、
  新测试 `test/t757-设置页收窄.test.mjs` ×2、新 helper `test/helpers/land-inferred-stub.mjs`、
  旧测试 `t612`／`t613`／`t614`／`cmd-write-40-persist`／`fetch-t4`／`t606`／`health-706`（件头）、
  `packages/skill-calorie/AGENTS.md`（台账 `--sync`）——每行一件，跨件处均走公开接口
  （`paths` 纯算式、`xunji/index.js` 能力门、`cli/config` 回执组）。
- **第二步结构设计**：不新建目录（helper 住既有 `test/helpers/`）；`paths.ts` 升为落点纯算式的唯一定义地，
  照片／状态／产物三处薄壳调它；`cli/config.ts` 的 `resolved` 组装三处纯算式（目录系）＋门内预置（动作库）。
  对外面没收窄也没放宽：`src/index.ts` 与 `package.json` 的 `exports` 一字未动；
  `xunji/index.ts` 仍五件（`defaultCatalogPath` 不进门，调用方用门内已有的 `XUNJI_CATALOG.preset`）。
- **第四步超线报警**：**已超线，需要根据规则进行重构。** `src/health.ts` 539 行（LF 口径，台账 537→539 已 `--sync`）——
  超因与拆法沿用台账里那一行（自动补出那句「超因与拆法待补」仍待归属票认领；本票只动它的算式调用与判据文案，
  拆件会把「判据住技能侧」这条口径搅成两次改动的合成读数）。**本次先不拆**，台账已同步。
  其余改动件（`config`／`paths`／`cli/config`／`photos`／`catalog`／`rateLimit`／`landRunner`／`output`）均在线内。
- **第五步交付对账**：`git status --porcelain` 实得改动件与上面清单逐行一致，偏差为零（见收尾提交）。

## 五、范围外发现（如实记，均**非本票引起**，附读数）

1. **包级全量 40 红**（`tests 1867／pass 1827／fail 40`，完整清单见 `.scratch/t757-skill-full.log`）。
   逐文件归属（`test at …` 去重）：`514`／`566`／`c43`／`448`×3／`453`×3／`111`×4／`265`／`fetch-t4:125`／
   `449`／`422`／`256`×2／`254`／`290`／`b1-390`／`m5-97`（场景表 46≠52）／`179`×2／`t11`（SKILL 模板）／
   `109`×8／`t255`（目标覆盖）／`t275`×2（总览页）／`359`／`440`（13 名表 `body/receipt.ts` 多 2 处）／`561`（场景 06 词表 26≠25）。
   无一是本票改动件：本票改的源码（config／paths／photos／xunji／land／health／output）与测试
   （t757／t612／t613／t614／persist／fetch-t4 照片段／t606／config-surface／health-706）**全绿**；
   红的全在运动／身体／目标／营养／场景表／复制区等未动区（多席在途，今日锁等待里见 #754／#796 两席活着）。
   其中 `fetch-t4:125`（`validatePlan({weeks:[]})`）与本票改的照片段（`:210` 起，已改新口径并绿）是同一文件的两段，
   前者与落点算式无关。**未重录任何指纹账本**（重录会把别席在途的红盖成假绿）。
2. **`t445 告警线门` 的台账有一行重复**（`scripts/build-help.mjs` 两行逐字相同）——`--sync` 未动它（非本票引入），
   留给归属票。

## 六、未做项（如实记）

1. **发版与装机**：`skill-calorie`／`dsh-calorie` 要抬版本、发布、装进 profile 才到维护者的 app 里；
   那是地图里的 #755（本图末段收口），本票只交源码与读数。
2. **真机点屏**：本席无 GUI 点击能力。面板行为已在本机用**真产物**量过（t757 插件侧 17 条组件级用例 ＋
   `config-surface-676` 28 条），真机那一跳按 #736／#743 的先例归维护者。
3. **跨技能联动的正式通道**：地图 Out of scope（本票只删键＋面板不显示，见 #745）。测试缝的文件缝是测试内手段，
   不是联动通道——通道另起一次努力（维护者定）。
