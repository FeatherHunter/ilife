# t545 目标对照证据（48 页副标题撤行入形状＋结论去前缀）

## 一、票面与范围

- 派单 #545：48 页（`calorie.view.goal-weight`，`src/goal/goalWeightDoc.ts`）页头副标题
  `目标 68 kg vs 最新 70.4 kg · 2026-08-09 ~ 2026-09-07` 是纯文本三件事串行（#154 #340 打回批第 ⑧ 项，
  #542 移交）；追加 #436「结论块去「结论：」前缀」（`goalWeightSummary`）。
- 写集三件（禁区一字未动：机器面摘要/信封/复制载荷、命令路由冻结表、weight 目录、表宽居中）：
  1. `packages/skill-calorie/src/goal/goalWeightDoc.ts`
  2. `packages/skill-calorie/test/goal-weight-b1-390.test.mjs`（补 #545 断言）
  3. 本证据件。
- 前置（开工前已确认）：`9c6710b`（#561 落点）在 HEAD 祖先链（`merge-base --is-ancestor` exit 0）；
  盘上 48 页屏上「数据来源」零命中（#561 范围干净）；自有路径无在途改动、无暂存。

## 二、改了什么（实现）

1. 副标题整行撤：`assembleDocPage` 改传 `subtitle: ''`（空串即整段省略，与 `weight/log.ts:332` 同口径）。
2. 三件事进形状，放正文首件（只读引用 `../weight/weightUi.js`，不搬家；`weightUiCss()` 按口径放 parts 第一项）：
   - 目标与最新走两端值 `pairStrip('目标 68 kg', '最新 70.4 kg', 'vs')`（中缝沿原串写 `vs`）；
   - 窗口走窗口条 `windowStrip(g.start, g.end)`（两枚日期块＋箭头，不带条数胶囊——「有记录 X 天」
     已住「净变化」卡，不复读）。
3. `最新体重` 卡 `detail` 的裸窗口串 `g.start + ' ~ ' + g.end` 整条撤（窄卡会从日期中间断开，
   窗口只住窗口条）。
4. 结论去前缀：`goalWeightSummary` 四个分支句首 `结论：` 全撤（块标题已是「结论」）。
5. 保留项（票面观察未定，按保守口径不动）：数据表题 `体重目标（目标 68 kg · 最新 70.4 kg）` 原样保留；
   表卡 `max-width: 680px`＋居中原样保留（用户裁定 C）。

## 三、前后读数（48 页）

- 改前（盘上 `.scratch/t154/delivery/48-对比体重-当前-vs-目标体重.html`）：
  `HAS-SOURCE=False`，`HAS-SUBTITLE=True`（含 `<p class="ilife-block-page-shell-subtitle">…</p>`）。
- 改后（本票草稿重出 `.scratch/t154/w3-545/48-对比体重-当前-vs-目标体重.html`，种子贴票面 68/70.4）：
  文件级探针 11/11 通过（`RESULT: 11/11 PROBE-PASS`）——无副标题元素、两端值/窗口条形状在、
  目标/最新/窗口日期事实在、卡片无裸区间、结论无前缀、屏上无来源、完整文档。
- 双档截图真看：桌面 1280 四卡一排无塌列；手机 390 两端值按词换行、窗口条日期块整体换行不断散、
  卡片 2×2、表格走窄屏卡片行、无溢出。截图见 `.dsh-vision-router/artifacts/` 下
  `…-48-a64401ff-shot-1280x900.png` 与 `…-shot-390x900.png`。

## 四、门禁与自证（GATE-RUN 声明，逐条对账运行记录）

GATE-RUN runId=e4a30132-d8cc-4451-a551-36443c75cb9b cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
GATE-RUN runId=2c056929-c9ec-4b2a-84bd-e67b1b3a106d cmd=node --test packages/skill-calorie/test/goal-weight-b1-390.test.mjs
GATE-RUN runId=70361fcc-607a-44c5-a5a2-bdb96e0b1c17 cmd=node --test packages/skill-calorie/test/goal-weight-deadline-548.test.mjs
GATE-RUN runId=3772248b-59b9-4b37-96f9-ebbe45563d59 cmd=node --test packages/skill-calorie/test/goal-weight-deadline-548.test.mjs
GATE-RUN runId=b8b0f9c1-ba63-4e6e-aa50-0eaf368650c3 cmd=node --test packages/skill-calorie/test/t561-goal-no-source.test.mjs
GATE-RUN runId=74784fb9-3514-400c-a3e6-161e4b05d649 cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
GATE-RUN runId=347492c6-1d0f-4e3c-a8af-bad8e5fcd7c5 cmd=node --test packages/skill-calorie/test/goal-weight-b1-390.test.mjs
GATE-RUN runId=b3dd0fd8-1b03-4a14-b78a-2c92dd8a8ceb cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
GATE-RUN runId=9af7a25d-4150-4a37-a3a0-234402ddb0b6 cmd=node --test packages/skill-calorie/test/goal-weight-b1-390.test.mjs
GATE-RUN runId=d576b5ad-462b-4d16-b56c-22513218f857 cmd=node .scratch/t154/w3-545/regen-48.mjs

- 编译（真 tsc）：`e4a30132` exit 0；变异轮重建 `74784fb9` exit 0、还原重建 `b3dd0fd8` exit 0。
- 用例：b1（含新增 #545 断言）`2c056929` tests 5/pass 5/fail 0；548 `70361fcc` 4/4/0
  （`3772248b` 为同命令重复跑，exit 0，无独立日志文件）；t561 `b8b0f9c1` 4/4/0。
- 源码级变异：改坏副标题行（传回旧副标题串）→ `347492c6` tests 5/pass 4/fail 1，
  红的正是 `#545 副标题撤行入形状…`（`副标题元素应整行撤掉`）；逐文件还原 → `9af7a25d` 5/5/0；
  还原后确认盘上无 `MUT` 残留。
- 48 重出＋探针：`d576b5ad` exit 0，11/11 PROBE-PASS（BYTES=73518）。
- 另有两次非门禁运行如实记：认领评论 `77379bd6`（gh issue comment，exit 0）；
  重出脚本首跑 `d853a997` exit 1（种子脚本 import 用了盘符绝对路径，修成相对路径后重跑即绿，
  不充门禁）；254 首跑 `7d1dcee7` exit 1 见 §五。

## 五、范围外发现（S3，不阻断，拟转票）

- `goal-result-254.test.mjs` 首跑红一处：`goal-7d` 的 `data.metrics` 与冻结值不一致
  （avgDeficit 1555 vs 1549、weeklyDeficit 10886 vs 10841，均为 `calorie.view.goal` 缺口侧）。
  与本票无关的三条证据：① 该用例跑 `resultDocs.ts` 链，从不装载本票改的 `goalWeightDoc.ts`
 （全仓仅 `read.ts` import 本件，且只服务 `calorie.view.goal-weight` 键）；
  ② 本票工作区改动仅两件源码＋测试（§一写集），`resultDocs`/`deficit`/`trend` 一行未碰；
  ③ 冻结值写于 `47a5215`（#254 当时），其后已合并的 `ea7cdc9`（#160，2026-09-15）改了
  `src/analysis/trend.ts`（＋23/−8，含口径修正），冻结值未同步。
  本票 254 循环在首个 `goal-7d` 即抛，未及跑到本页对照项；本页对照形状由 §三探针与 b1 用例覆盖。
  建议另开一票同步 254 冻结值（或钉死该用例的取数口径），本票不碰（非写集）。
- 本票会话中途，工作区出现别席在途改动 `M packages/skill-calorie/test/profile-doc-179.test.mjs`
 （开工时无，开工后有；非本票声明路径）。本票提交一律限定自有路径，该件不暂存、不提交、不还原。

## 六、未做项与下一手

- 未做：表题 `·` 串保留与否（等编排者定，见票面观察）；254 冻结值同步（§五转票后由归属席做）；
  `git push`（按规矩总工统一收口）。
- 下一手：复核席独立复跑 §四各条（b1/548/t561 任一条）＋自设新探针；收口时回写 #542 与 #340。
