# t283 · 删照候选与快照（只读过程型命令）证据

## 目标与做法（一句话）

说「删身材照」时先出只读过程型页：列候选（缩略图＋日期＋标签）→ 选中一张看快照 → 复制 prompt → 才走写命令（老技能 D6 三步）。
新增一条只读页面命令 `calorie.view.photo-picker`（代表唤醒词「选身材照」），服务「删身材照」与标签三条。

新命令的键名与 example 原文（登记在 `packages/skill-calorie/src/photo/commands.ts`）：

- 键名：`calorie.view.photo-picker`
- example 原文：`calorie-cmd-read calorie.view.photo-picker`

## t400 基准对照（逐条）

- 裁定 2（prompt 即 CLI）：prompt 预览区有已选 ID 时等于 bash 可执行命令（含 `calorie.photo.remove`／`calorie.photo.tag` 全形与 `--params` 及选中 id）；无 id 时等于指步骤占位句（记下 #ID 再带 `{"id": <ID>}` 重跑）；tag 缺 op/newTag 时等于指步骤占位句。复制按钮文案只称复制（「复制指令」），不称发送；页内无「发送」字样（测试断言）。
- 裁定 4（快照只读必读）：快照区只做选中与复制，不含删除动作；删除走写命令；快照明细 caption 明示「只读，删除走写命令」。
- §二-补（复制文案不夸大）：按钮按实际动作写，不写「发送」。
- 体积节口径沿用 t341：上限仍是 `galleryDoc.ts` 的 `PHOTO_LIST_PAGE_MAX_BYTES` 同一个值，本票不另定；测试直接引用该常量断言；候选缺省 20 张、上限 100 张，截断时明示还有 N 张未列出。
- 十条唤醒词形态表：新代表唤醒词「选身材照」为过程型页新拟词（不住冻结表，与「看身材照向导」／「看GIF规划器」同形）。

## 验收命令 exit 码＋读数（持锁窗口实测）

窗口 2（分阶段登记：先命令声明 build→gen→build，再路由声明 build→gen→build→help:build）：

- `pnpm build` exit=0；`pnpm gen` exit=0；`pnpm build` exit=0；路由追加后 `pnpm build` exit=0；`pnpm gen` exit=0；`pnpm build` exit=0（他席半写 weight 文件时曾 exit=2，见下）；`pnpm help:build` exit=0；`pnpm gen:check` exit=0（GEN-CHECK 全绿）。
- 窗口 4（树收敛时点 18:47）：`pnpm build` exit=0；`pnpm gen:check` exit=0；`node --test packages/skill-calorie/test/photo-picker-283.test.mjs`：tests 5／pass 5／fail 0；`node packages/skill-calorie/scripts/check-examples.mjs --only calorie.view.photo-picker` exit=0。
- 窗口 6（收口 18:53–18:57）：`pnpm build` exit=0；`pnpm gen:check` exit=0；本票测试 tests 5／pass 5／fail 0；`check-examples.mjs` 全量 exit=0（含本票 example）；`t81-exec-smoke.mjs --out` 写盘 489 行（本票行 exit=0，见下），脚本 exit=1（14 条他席 exec 非零）；r81／t293 红在共享收口债务（见下）。
- 窗口 7（#332 提交后 18:59）：本票测试 tests 5／pass 5／fail 0（树动后复绿）；r81 余三红（D2④ 拍营养表 intent、FX-81-5「看有备注的饮食记录」未登记、A7 名单，均为他席翻转）；t293 P1=FAIL（4 处与窗口 4 逐字相同的豁免件历史条目）。
- 用例②（价值核心）真跑读数：从页内 prompt 预览块抽出 `calorie-cmd-read calorie.photo.remove --params '{"id":1}'`，隔离库实跑 exit=0 且回执 ok；再跑标签流 `calorie-cmd-read calorie.photo.tag --params '{"id":2,"op":"add","tag":"晨起"}'`，隔离库实跑 exit=0 且回执 ok。
- 用例③：空库 `pnpm` 外实跑 exit=4，显式 `--html` 落点无文件建成。
- 用例⑤：`data.output` 为绝对路径且在盘上；单页字节 ≤ 上限（测试直接引用常量）。

变异自证两行机器读数（测试内打印）：

- `MUTATION-RED ok（摘候选内嵌必红）`
- `MUTATION-GREEN ok（改回必绿）`

GATE-RUN 单行（运行记录 `.scratch/locks/gate-runs.log` 可复核，等待量落盘）：

- `GATE-RUN runId=98a88238-b7cc-460d-a66e-4e7a8fc869ba cmd="pwsh -NoProfile -File .scratch/t283/window4.ps1" waitedMs=140012 exit=1`（窗口内本票 4 步全绿；窗口 exit=1 系同窗 r81／t293 两步红在共享收口债务，见下）
- `GATE-RUN runId=d66d6d9f-3948-4f3f-8dbf-f04fc5c0bfbe cmd="pwsh -NoProfile -File .scratch/t283/window6.ps1" waitedMs=20002 exit=1`（窗口内 build／gen:check／本票测试／示例全量绿；exit=1 系 t293 P1／smoke／r81 三步红在共享收口债务）
- `GATE-RUN runId=4c49d6d3-77bc-4b79-90c4-5c7e9220ab44 cmd="pwsh -NoProfile -File .scratch/t283/window7.ps1" waitedMs=30008 exit=1`（窗口内本票测试绿；exit=1 系 r81 三红与 t293 P1 均为他席收口债务，行号级归属见「未做项」节）

## 逐条路径（改了哪些文件各一句话）

- `packages/skill-calorie/src/photo/picker.ts`（新）：只读取数与 prompt 组装（候选窗／已选快照／三态 prompt），空库抛缺失阻断，参数非法抛可用性错误。
- `packages/skill-calorie/src/photo/pickerDoc.ts`（新）：整页文档组装（KPI＋候选＋快照＋prompt 复制区＋复制数据），完整文档落盘。
- `packages/skill-calorie/src/photo/commands.ts`（＋1 行）：新命令声明行（键／list 形／标题删照候选／代表唤醒词选身材照／可执行 example／处理函数），既有 10 行一字未动。
- `packages/skill-calorie/src/photo/routes.ts`（＋1 行）：新路由声明（new 表 order=68，末尾追加保连续），既有 21 行一字未动。
- `packages/skill-calorie/test/photo-picker-283.test.mjs`（新）：票面 5 用例（①候选快照真跑 ②prompt 抽出即跑删与标签 ③空库 exit 4 不落盘 ④变异 ⑤落盘与体积）。
- `test/calorie-routing-81.test.mjs`（跟进）：新拟入口 68→69、路由总数 505→506、新拟词数 69，均为同行改数（零行移位，不扰动豁免行号）。
- 生成物（`pnpm gen`＋`pnpm help:build` 派生，人未手改）：`src/cli/keys.ts`、`src/triggers/routes.generated.ts`、`packages/base-combos/combos.yaml`、`scripts/build-help.mjs`（REPR／EXAMPLE 各＋1 行）、`SKILL.md`（速查表＋1 行）；`src/cli/registry.ts` 逐字节不变。其中 `routes.generated.ts`、`SKILL.md`、`test/calorie-routing-81.test.mjs`、`docs/research/t81-exec-smoke.md` 已随 #332 收口提交 f85e169 入库（含本票增量，提交说明逐条认领在途声明）；仍未提交的生成物只剩 `keys.ts`、`combos.yaml`、`build-help.mjs`（混入清单见「提交门」节）。
- `.changeset/t283-photo-picker.md`（新）：skill-calorie patch 件。
- 草稿与日志：`.scratch/t283/`（窗口脚本 w1／w2／w4／w5、apply-route.mjs、各步日志与 summary）。

## 提交门（生成物混入清单，提交专人按此剥离）

`pnpm gen` 从当刻脏树派生。#332 收口提交 f85e169 已把 `routes.generated.ts`、`SKILL.md`、`routing-81`、`smoke` 快照连同在途增量一并入库（提交说明点名含 `calorie.view.photo-picker`）。
仍未提交的生成物只剩三件，本票认领且仅认领含以下字样的行：`photo-picker`／`选身材照`／`删照候选`（`keys.ts` 键行、`combos.yaml` 镜像块、`build-help.mjs` REPR／EXAMPLE 行）。
三件内其余增量均属他席在途（`calorie.product.import`、`calorie.view.body-composition-compare`、`calorie.view.body-measure-compare`、`calorie.view.exercise-records`、`calorie.view.multi-trend`、运动代表唤醒词换词行），不得随本票提交，剥离或等树绿。

## 未做项与下一手

- 已入库（#332 f85e169）：smoke 快照 489 行（本票行 `| 488 | 新拟 | 09 | 选身材照 |` exit=0、envelope 对齐）、路由冻结数（69／506 与本票增量合流）、SKILL.md 速查表行。快照内 14 条非零中 4 条已由 #332 登记，余 10 条归各翻转席（营养表／预测）。
- 未做：r81 剩余三红——D2④「#13 拍营养表记一餐」intent、「看有备注的饮食记录」exit 4 未登记、A7 不做桶名单。归属：营养表席与饮食备注筛选席；本票行全绿。
- 未做：t293 P1 的 4 处 UNACCOUNTED（窗口 4／6／7 三轮逐字相同，均为豁免登记件历史条目指不回当刻行；本票文件在③④桶零出现）。归属：t293 探针维护席与各断言行主人。
- 审查席重点：用例②的抽出即跑链（prompt 文本 → CLI → 隔离库 exit=0）是否覆盖「复制的就是写命令照抄即跑形状」；快照页全文无删除动作；envelope 仍为 list 形（items＋total）。
- #285／#345 提示：依赖本票新键名时引用 `calorie.view.photo-picker`（代表唤醒词「选身材照」，example 见上）；标签三条「先看原标签」若不与本页同形，按票面另开票，不在本票夹带（本票未夹带）。

## 并行阻塞时间线（mtime 实测，只读探针）

- 18:39–18:41 weight/compare.ts 半写致本票 B-build2 exit=2（他席文件，本票未碰）。
- 18:41–18:42 weight/review.ts 多次半写，共享 dist 出现语法 broken， poison 所有 barrel 进口测试；18:44 他席收敛（#154 只读类型检查 exit=0），本票窗口 4 全绿。
- 18:50 analysis/multiTrendPage.ts（他席新建未跟踪件）半写致窗口 5 build exit=2；等待收敛中。
- 18:50 analysis/multiTrendPage.ts（他席新建未跟踪件）半写致窗口 5 build exit=2；审查席确认树绿后窗口 6 全链：本票四步绿，余红皆他席收口债务。
- 真库只读：全程 `SKILLS_DB_PATH=D:\2Study\StudyNotes\.db` 仅回显确认；一切写命令与测试均走 `$env:TEMP` 下隔离库与隔离照片目录，真库零写入。

## 结构纪律五步对账

影响清单（第一步）即「逐条路径」节；本票只碰身材照片能力目录（`src/photo/`）、根测试两件（本票测试＋路由冻结跟进）、归属文档与草稿、changeset；路由记录面走生成物，未手改 `src/triggers/routing.ts`，未碰 `src/cli/legacy/**`，棘轮两件未加行，`src/render/html.ts` 只读。
新增目录树：无（只新增文件，不建目录）。公开接口：`picker.ts` 对外 2 件（`viewPhotoPicker`／`buildPhotoPickerPrompt`，目录内互用不算对外），`pickerDoc.ts` 对外 1 件（`buildPhotoPickerDoc`）；两件均远低于 350 行告警线（约 150／130 行），无超线。
交付对账（第五步）：本清单即账，偏差为零；生成物混入另见「提交门」节（树脏所致，非本票多碰）。
