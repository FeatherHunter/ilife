# #267 独立对抗审查席 · 报告（交付件 `59ecf548`＋`a08d4888`）

**判定：PASS**（六项走完；无 S1／S2；五维 91/100）

## 一 · 机器证据

| 路径 | runId | 读数 |
|---|---|---|
| 复跑三段 | build `0ec3219f-46b9-4cf6-9748-5882eaad6a3d`／判据 `fa7ad21d-948b-4fd6-a98c-b57aea8e7a9e`／t293 `a4512521-36fd-492e-b887-5853bb7285b7` | 三段 exit=0；`T267-RESULT: 8/8 通过`；`P1..P5 5/5`、`UNACCOUNTED=0 处` |
| 自设探针＋收尾 | `4c604a83-0239-49b5-9ede-e068a95696ad`／`f39249e6-e858-4940-bcd9-c995e42e377a`／`bbb2b044-7799-4b1e-ba8a-71c4b26c08b0` | 探针 5/5；收尾判据 8/8、t293 5/5；真库跑前跑后同值 |
| 变异甲（族眉标） | `6c7ef9c1-ab6e-4af6-84fe-5b2fb899fbd8` | 变异段 exit=1、还原段 exit=0 |
| 变异乙（窗口参数） | `57991e54-c207-4955-b95d-0dd8d1ca21bd`／`85f2d5b4-7171-433c-bf03-8915b05baa7f` | 见二·3 |

读数逐字复现实施席：G1 `词=39 exit0=39 绝对路径=39 字节如实=39 完整文档=39 字节域=[65200,97454]`；G3 `读类=7 各拿自己那页=7 零命令键=7`；G4 `空库阻断=32 出账=7`；G5 `眉标族长=9`；G6 `写类=13 域=[66328,73916]`。我这两件挂树上时三段仍全绿。

## 二 · 逐条路径

**1 复跑三段**　见表。判据跑动一律在 `node tooling/run-locked.mjs --ticket 267 -- …` 内跑；即便持锁，我也撞到过一次 `dist/fetch/body.js` 取不到（他席在重写构建产物），故读数只取完整构建之后那几轮。

**2 G1 起点真是唤醒词**　`src/triggers/help-lookup.ts:34-57`：速查表以 `t.wake_word`（＋aliases）为键、值取该行自己的 `main_prompt.cli`，`lookupWake` 就是 `map[word] ?? []`——不是从命令表反查。我的探针反证：键数=440、拿 `calorie.view.exercise` 当查询词**命中 0 条**、汇总键下 10 条词命令文本互不相同（反查则必相同）。自选 3 词各走一遍独立脚本（自建索引 → `lookupWake` → 路由层 → 真跑）：**看运动类型分布** 74338 B／**记力量训练** 69356 B／**看某段时间运动** 95485 B——三条 exit 0、绝对路径、`delivery.bytes`＝盘上字节＝读回字节、完整文档、自定义起止逐字在产物里。

**3 自设变异两处**（另存原字节→变异→build→判据→写回原字节→build→判据）
- **甲 · 族眉标**：`src/render/sportDocs.ts:51`「运动 · 汇总」→「运动 · 记录级明细」⇒ build=0、判据 **exit=1**（G3 `看本周运动 汇总页眉标不对：运动 · 记录级明细` ＋ G5 同族）⇒ 写回（`sportDocs.ts` 3f9f67bbdc48、`routes.generated.ts` bcb16fa36ab8 双 **SAME**，`git status` 两件空）⇒ build=0、判据 **exit=0**。
- **乙 · 窗口参数**：看本周运动 `window` 本周→上月。**只改冻结表＋声明**：exit=1，但 diag 证明红因是**生成物滞后**（冻结表／lookup 已「上月」，`routesFor`／生成物仍「本周」；`pnpm build` 的 `gen-cli.mjs --stamp` 只签印记、不重生成，连跑两次 build 亦然）。**只拧声明侧一行**（上月→上周）：exit=1，红因 `两处定义地不一致（冻结表 vs 路由声明）`——真的轴。**四处同改**（冻结表 `cli`＋`data_source`＋声明＋生成物记录，build 两次后四面一致）：判据 **exit=0 全绿** ⇒ 这一类回归抓不住（见 D2）。还原：三件 **SAME**、`git status` 三件空。中途一次锚点不唯一，驱动当场还原，无半变异工作区。

**4 自设新探针**（`docs/skills/skill-calorie/t267-review-probe.mjs`，与实施席判据件不共用一行）
- **双向对账**：直读源码文本，`39 ＝ exercise/routes.ts(29) ＋ home/routes.ts(10)`，双向无孤儿无缺词、逐条 cli 与冻结表逐字相同 ⇒ 没有漏掉分区。
- **读类 7 条**：零命令键=7、完整文档=7、字节域=[71468,93218]（我自己的种子库）；全 39 条=[65200,97454] 与其一致。
- **它没覆盖的幂等**：同一词两跑 sha256 相同；**本票范围内没有两条词共用同一条命令文本**（重复组=0，如实记账）；同键异窗口产物分布 `add:8→8／records:5→5／recap:5→5／exercise:10→9／exercise-goal:2→1`——`exercise-goal` 那 1 处与实施席遗留③「周一 本周＝今日」读数一致，汇总键也塌 1 处（我未逐对定位，只记账）。窗口参数确实进了产物。

**5 真库只读复核**（我自己算）　跑前／跑后（其间含两次变异与全部 build／判据跑动）`sha256=57ce9d87f5f232822332d7057c2ae8411d99ba7fdecea289f2028e19fc3ffc15`、3072000 B、mtime `2026-09-14T05:53:37.729Z` 全同（只读打开前后也全同）；`integrity_check=ok`、表数=11、逐表行数 `body_composition=0 body_measurements=1 body_photos=22 daily_goal=1 exercise_log=8297 food_log=562 nutrition_products=1931 user_profile=1 weight_log=129 workout_plan_config=1 workout_plans=100`——与实施席声明**逐字相同**。

**6 归属**　`git show --name-only 59ecf548 a08d4888` 只含 `docs/skills/skill-calorie/t267-锁-真出口.md` 与 `packages/skill-calorie/test/exercise-accept-267.test.mjs`，两提交都在 `origin/master`；`base-render`／`commands.ts`／`routes.ts`／`render/*.ts` 零差异；`doc-page-assert.mjs` 只被只读引用，没有第二份五连。

## 三 · 缺陷

- **D1（本票范围 · S4）**　证据件「自证两行」的红因清单含构建序成分：改声明不重生成时 G1／G2 的红来自生成物滞后，真页身份轴是 G3／G5。修法：四步序补 `pnpm gen`，或红因逐条注明是哪条轴。
- **D2（本票范围 · S3）**　**非自定义窗口的词，产物与词义无断言**（四处同改全绿实证）。第三组只管 7 条读类词的页归属，G1 只对 `window==='custom'` 校起止——「看本周运动」可以出上月页而判据全绿；建议 #268 肉眼前补一条「窗口参数解出的区间 ↔ 产物区间串」逐词对账。
- **D3（范围外 · 记账）**　汇总／对照目标两页载荷头仍印命令键（实测 `看某段时间运动` 产物内出现 4 次）。票面已裁定另立票；判据以 `COPY_KEY_LEAK` 显式登记并在读数行点名 12 条，处置正确。
- **D4（范围外 · 记账，结构可读）**　条数分母全派生自冻结表：把一条词从冻结表＋两件声明同删，六组仍绿（#266 只钉 `WAKE_ASSETS=436` 与代表词，不钉场景 04 的 39 行）。票面禁手写计数，故不算本票违规；若要外部锚，可用 `WAKE_ASSETS` 运动分组词集对账。

## 四 · 五维分（30/25/20/15/10）

交付与票面覆盖 **28**／鉴别力 **21**（D2、D1）／证据可复现 **18**（读数逐字对上，红因口径扣 2）／纪律与归属 **15**／文档与可读 **9** ⇒ **91/100，PASS**。
