# #944 四票（#946／#947／#948／#949）· 复核报告（复核席 §5.1）

复核席读盘时刻：2026-09-23T16:0x–16:2x Z（本机 9/24 00:0x）；派单＝`.scratch/bug-plan-preview/dispatch-review-wave1.md`。
**本件只记机器读数与逐条出处**；结论以当刻盘上内容为准，不采信任何实施席的结论句。

## 〇 · 树状态绑定（读数一律绑这个 sha）

```
HEAD   = b9d98a79a03e309fb3d4afd52905173d59aaab86
git status --porcelain -- packages/skill-calorie/src packages/skill-calorie/test docs/skills/skill-calorie packages/skill-calorie/scripts
                                              → （空）四票的源码／测试／证据／脚本面无未提交改动
git status --porcelain（全仓，复核开始时）
   M packages/base-render/assets/help-template.html
   M packages/base-render/src/helpShell.ts
   M packages/base-render/test/help-shell-136.test.mjs
  ?? .dsh-im/ …
  ?? packages/skill-calorie/.dsh-im/inbound/2026…（6 个目录）
  ?? packages/skill-calorie/tsconfig.t947.json
git diff --stat -- packages/skill-calorie/src → （空）
```

- 三个 `M` 在 `packages/base-render/**`（**别包**，与四票的声明路径零交集）；`git diff` 对 `packages/skill-calorie/src` 为空 ⇒ 本报告一切以 `dist` 为前提的读数绑定在**四票提交后的干净 src**（协议 §2.6：前后同值，无漂移；不做「重跑碰运气」）。
- 复核全程只写 `docs/skills/skill-calorie/t944-复核-报告.md` 与本件同名目录 `.scratch/t944-review/**`；四票的代码件／测试件／证据件**一个字节未改**（变异电池的还原逐字节＝1，见 §三）。

## 一 · 逐票判定

| 票 | 判定 | 一句话 |
|---|---|---|
| **#946** | **PASS** | 三条判据独立复绿；自设探针换 op（set-week／add-movement）＋九 op 全表里 8/9 合契约；源码级变异咬住单列规则。留 1 条 S3 边界态（见 §五 D1） |
| **#947** | **PASS** | 六条判据独立复绿；自设探针把夹具改成**三周有课**，「周区块数／其中 N 周有安排」随实况走；既有 `test/scene05-*` 三件冻结用例同跑全绿（反向锁对照）。留 1 条 S3 卫生项（见 §五 D4） |
| **#948** | **PASS** | 八条判据、`gen:check` 6/6、示例门 137/137 独立复绿；自设探针给**两周两天的部分 plan**，载荷周数／天数＝页上那份且原样执行落库。留 2 条 S3 陈述／字节项（见 §五 D2／D3） |
| **#949** | **PASS** | 五条判据独立复绿；自设探针加**多字段原子性**（坏日期那一笔不得把别的字段写进去）＋三个新坏串＋跨票串联落库；源码级变异咬住日期体检调用点 |

**四票都不是「未完成」**：票面点名的交付物逐条在树上，判据命令在我自己的锁窗里各自 exit 0（§二）。

## 二 · 独立复跑（命令 ＋ RESULT 摘要行 ＋ runId ＋ 我的退出码）

全部经 `node tooling/run-locked.mjs --ticket 944 -- <命令>`；明细日志落 `.scratch/t944-review/<名>.log`（§2.2）。

| 环节 | runId（抄自 `.scratch/locks/gate-runs.log`） | exit | 摘要行 |
|---|---|---|---|
| 编译 | `24ae74ab-f343-4eda-8a86-7bb08124b5a3` | 0 | `tsc -b packages/skill-calorie --force`（先删 `tsbuildinfo`，runbook §8 口径） |
| #946 判据 | `50150e31-c1e7-4a56-b320-58b45a3ffb4c` | 0 | `ℹ tests 6 / pass 6 / fail 0`；`T946-GEOMETRY @1440/1280/1024 改前表卡=880 眉标=880 标题=880 副题=880`；`T946-MUT-GEOMETRY @1440 改前表卡=1240`；`T946-EXEC exit=0 命令=plan-update-day 库内 session_label=下肢＋核心 未动行=1` |
| #947 判据 | `4e66540b-3cb2-4631-8cd1-7a1b7eb90b67` | 0 | `ℹ tests 6 / pass 6 / fail 0`；`T947-RESULT 七条读数：越界出页／未开始出页／无参页三读逐槽不动／带参与不带参同口径／本日无课出页／真无计划出页／范围判定未判宽（越界 data-wk=2、未开始 data-wk=2、无参 data-wk=2、无参总场次=3、全量场次=3）` |
| #948 判据 | `b924fe7d-478c-478a-90f9-f0a2ccb77fd9` | 0 | `ℹ tests 8 / pass 8 / fail 0`；`T948-EX1 示例=冻结表="calorie-cmd-read calorie.view.plan"`；`T948-W1/W2/W3 exit=0 weeks=0/0/1`；`T948-CMD 逐字相等=true len=353`；`T948-P2 原样执行 exit=0 周=1 行=1` |
| #949 判据 | `8d902362-c6ef-4156-97ea-0a43e045a379` | 0 | `ℹ tests 5 / pass 5 / fail 0`；`T949-RESULT 坏日期挡住／改标题话术对／改日期话术对／plan-set 收口／02-30 仍放行／合法日期仍写得进（坏日期 exit=2、合法日期 exit=0、plan-set 坏日期 exit=2）` |
| 示例门（全量） | `258619cd-d386-4a83-a13f-d89be200e915` | 0 | `示例行数：137（通过 137，白名单跳过 0，红 0，结构问题 0）`＋`RESULT: 137/137`＋`PASS: SKILL.md 示例逐行可执行` |
| 示例门 `--list` | `41efd726-5367-4a00-b45c-4757d2e985c7` | 0 | `calorie.view.plan → calorie-cmd-read calorie.view.plan`（**不带 `--params`**，与冻结表逐字同）＋`RESULT: 137/137` |
| 一条 dist 路径门 | `ffcaf9bf-4a9f-4dd8-a321-e6ab4bf77fe5` | 0 | `PASS: 测试面每个符号只有一条 dist 路径` |
| 页面断言门 | `49e765e4-4713-441a-bc63-5d1e40ec6429` | 0 | `PASS: spawn 真交付出口并断成功的测试件都断到了信封` |
| 告警线台账门 | `324e0a02-c4df-41bd-ad92-7725a0924f48` | 0 | `RESULT: 99/99`＋`PASS: 告警线台账齐全且与实况一致`（台账 48 行；扫描面 347 件，超线 44 件，剔出生成物 3 件） |
| 生成物门 | `de1b184c-36f6-4aca-bc5e-e3e9eeefdc26` | 0 | `pnpm gen:check`：`RESULT: 6/6`＋`PASS: 六份说明面与唯一来源逐字同源，块外无旧写法` |
| 场景 05 冻结三件（**反向锁对照**，非本波写集） | `d2c47142-86e5-4e0f-87ba-5e6d436f9239` | 0 | `node --test test/scene05-read.test.mjs test/scene05-write-create.test.mjs test/scene05-write-mutate.test.mjs` → `tests 3 / pass 3 / fail 0` |

**与实施席证据件对账**：四票证据件里声称跑过的 28 条 runId，在 `gate-runs.log` 里**逐条都能找到 START＋RUN 一对**（缺对＝0；样本 `RUN ticket=949 runId=691f795e… exit=0`）。读数与我的复跑一致，无一条需要按 §2.4 判非 0。

## 三 · 自设探针（每票 ≥1 条，全部由复核席自己写、只走真交付出口）

脚本：`.scratch/t944-review/probes.mjs`（A/B/C/D 四条）＋`.scratch/t944-review/probes-946-nine.mjs`（九 op 全表）。读数原文：同目录 `probes.out.txt`／`probes-946-nine.out.txt`；锁窗 runId `825c0d61-e359-4e61-a118-b402796fe1cc`（修正后）与 `6943ca05-a14b-4cc6-84be-464c6209625a`。

```
PROBE946-OP1 op=set-week cmd=calorie-cmd-read calorie.workout.plan-set-week… exec=0 db=复核推日
PROBE946-OP2 op=add-movement cmd=…plan-add-movement… exec=0 库内含新动作=1
PROBE947-W3 周区块 无参=3 越界=3 远期越界=3 第3周当天=1（收成第 3 周） 全量场次=4（夹具＝种子 3 场 ＋ 补第 3 周 1 场）
PROBE948-PARTIAL weeks=2 载荷周数=2 原样执行 exit=0 库内=["1/1/凌晨","2/3/凌晨"] 载荷同名=[[1,1,"凌晨"],[2,3,"凌晨"]]
PROBE948-LABEL 所给时段名=晚课 → 载荷时段名="凌晨"（编辑器四档时段模型所致；既有行为，范围外发现）
PROBE949-ATOMIC 多字段(title＋坏 start_date) exit=2 库内 title="seed计划" start_date="2026-09-01"（原样）
PROBE949-BAD3 2026-00-10:exit=2,db=2026-09-01 ｜ 2026-12-32:exit=2,db=2026-09-01 ｜ 2026-9-7:exit=2,db=2026-09-01
PROBE949-IMPACT 两字段(version+description)="只改版本号，不影响周次与训练内容；只改描述，不影响周次与训练内容" ／ 多字段(title+start_date)="只改标题，不影响周次与训练内容；改开始日期会影响周次计算：按周次推出的日期整体平移，已经记过的运动记录不动"
PROBE949-CHAIN 确认串=calorie-cmd-read calorie.workout.plan-update --params '{"title":"复核X","start_date":"2026-09-07"}' → exec=0 db="复核X"/2026-09-07
T944REVIEW-PROBES A#946=PASS ｜ B#947=PASS ｜ C#948=PASS ｜ D#949=PASS
```

**每条探针各打哪个盲区**

- **A（#946）换 op**：判据件只覆盖 `update-day` 一条。探针换 `set-week`（并自带 `days`）与 `add-movement`，载荷命令名**跟着 op 变**、串去空白后＝手写期望、原样执行 exit 0、库内值＝串里的值 ⇒ 「只对一条写死」证伪不成立。再往前一步，**九 op 全表**（参数逐字抄 `src/workout/routes.ts` 九条唤醒词登记的 `--params`，期望命令名手写）：
  ```
  PROBE946-NINE copy/set-rest/add-movement/update/update-day/delete-day/update-movement/delete 八条：载荷命令＝期望=1 且 原样执行 exit=0
  PROBE946-NINE set-week 期望=plan-set-week 载荷命令＝期望=1 原样执行 exit=2 stderr="ERR 2: 缺参数 days（非空数组）"
  PROBE946-NINE-RESULT 九条里不合契约的条数=1
  ```
- **B（#947）夹具加第 3 周有课**：判据件把「有课的周数」冻结成 2。探针走真出口给种子库补第 3 周一场课，再跑四页 ⇒ 无参页 `data-wk=3`、`「其中 3 周有安排」`、越界页 `data-wk=3` 且看得到第 3 周的课、远期越界（2028-01-05）同值；范围内那一天（2026-09-16）页面收成**第 3 周**（1 块）并打出那场课 ⇒ 周次编号与「有几个周区块」都随实况走，不是按 2 写死。
- **C（#948）给两周两天的部分 plan**：判据件只试 1 周 1 天。探针给 2 周（周 1 第 1 天、周 2 第 3 天）⇒ `metrics.weeks=2`、载荷 `plan.weeks` 两条且 `day_of_week=[1,3]`、载荷原样执行 exit 0、库内两周两天与载荷逐字段同名 ⇒ 「只对空模板写死」证伪不成立。（时段名归一见 §五 D5，属既有模型。）
- **D（#949）多字段同改＋原子性**：判据件只试单字段坏日期。探针把 `title`（合法）与 `start_date`（坏）**一起**给 ⇒ exit 2 且**库里 title 也一个字节没动**（不是「先写好的、后报坏的」）；再补三个判据件没有的坏串 `2026-00-10`／`2026-12-32`／`2026-9-7` 全 exit 2 且库不动；两个字段（`version`＋`description`）的影响逐字＝一条一句并排、不含周次那句；最后把该页的「确认指令」原样执行 ⇒ 多字段真落库（跨 #946×#949 的串联读数）。

**探针首轮的两条自证（如实留档）**：首跑（runId `3fa29859-8525-4c04-8702-508e4bbab15a`）里有两条是我方期望写错，不是实现的黑洞——
① B3 我按「越界页那样铺开」期望了 3 块，实测 1 块：给了**范围内**的日子时本页收成该日那一周（判据件 `noclass` 档的 `data-wk=1` 同口径）；② C1 我拿「所给 plan 的时段名」当期望，实测被编辑器四档模型归到 `凌晨`（见 D5）。两条更正后期望重跑即 `A/B/C/D=PASS`。

## 四 · 变异复核（复核席自己动手，红／绿各一行机器读数）

脚本 `.scratch/t944-review/mutate.mjs`（锁窗 runId `3db88756-06f3-46e1-ac37-4f59c882ad99`，exit 0）。每处都是「改坏 → **删 `tsconfig.tsbuildinfo` ＋ `tsc -b --force`** → 跑该票判据 ⇒ 必红 → 按**原字节**还原 → 再删 tsbuildinfo ＋ `--force` 重编 → 再跑 ⇒ 必绿」（runbook §8 那条陈旧 tsbuildinfo 的坑按机械做法绕开）：

```
MUT-M1-946-单列规则 RED: tsc-exit=0 test-exit=1 pass=4 fail=2 红条=#946 ① 几何…三档都 880px ／ #946 ① 判据不恒真…
MUT-M1-946-单列规则 GREEN: tsc-exit=0 test-exit=0 pass=6 fail=0 还原逐字节=1
MUT-M2-949-日期体检调用 RED: tsc-exit=0 test-exit=1 pass=3 fail=2 红条=#949 ① 坏 start_date 挡在写前… ／ #949 ④ 变异自证…
MUT-M2-949-日期体检调用 GREEN: tsc-exit=0 test-exit=0 pass=5 fail=0 还原逐字节=1
MUT-TREE packages/skill-calorie 未提交改动="…（只有 .dsh-im/ 的 6 个未跟踪目录 ＋ tsconfig.t947.json）"
```

- M1＝把 `src/render/workoutPlanCss.ts` 的 `PREVIEW_COLUMN_CSS` 置空（#946 的版面回归那一处）；M2＝删掉 `src/workout/write.ts` 的 `if (k === 'start_date') assertStartDate(v);`（#949 的日期体检调用点）。两处都**只**咬住对应票的判据，还原后 `git status` 对 `packages/skill-calorie/src` 为空 ⇒ 复核没有在树上留痕。

## 五 · 缺陷清单（归属 ＋ 严重度 ＋ 证据）

范围外发现一律不单独决定 FAIL（本报告里四票的 PASS 也不依赖它们）。

| # | 归属 | 严重度 | 事实与读数 |
|---|---|---|---|
| **D1** | **本波范围**（#946 的载荷契约） | **S3** | `op=set-week` 且 `days` 缺席时，页底那枚「复制确认指令」仍给出 `calorie-cmd-read calorie.workout.plan-set-week --params '{"week":1}'`，原样执行 `exit=2`（`ERR 2: 缺参数 days（非空数组）`）。触发面＝`src/workout/routes.ts:28` 登记的「定一周计划」参数（`{"op":"set-week","week":1}`）——页面自己在 `previewSetWeek` 里已印「这一页还没填要定哪几天，补上后再确认」（`write.ts:274`），但载荷按钮没跟着收起来。无数据损失、报错清晰 ⇒ S3；建议另票（或收口席就地裁定）把「本次没有改后值」这一态也纳入载荷契约。其余八条 op 全部合契约（§三） |
| **D2** | **本波引入**（#948 的提交 `4a1122e8`） | **S3** | `packages/skill-calorie/SKILL.md` 首字节＝`ef bb bf`（UTF-8 BOM），头一行成了 `﻿---`；同仓另五个 `packages/*/SKILL.md` **都没有** BOM（逐件实测）。BOM 是 `4a1122e8` 的 diff 第一行 `----` → `-﻿---` 带进来的。**仓内已有明文先例**：`packages/skill-chef/scripts/add-frontmatter-218.mjs:24` 就写着 `if (raw.charCodeAt(0) === 0xfeff) throw new Error('SKILL.md 以 BOM 开头，先清 BOM')` —— 即「SKILL.md 不许以 BOM 开头」是本仓认过的口径。`gen:check`（6/6）与示例门都不拦这一格，故没有任何机器门发现它；另一面风险是：下一次**整份重写** SKILL.md 的生成器（如 `tooling/skill-call-form.mjs:176` 的 `writeFileSync`）会**静默把 BOM 抹掉**、凭空多出一次字节 diff。建议收口时按字节摘掉 |
| **D3** | **本波范围**（#948 新写的三处陈述） | **S3** | 说明面与产物不符：`SKILL.md:80`（#948 新写那条）写「速查表「例」列给的是第 ① 种（照抄即跑的那一行）」，`t948-证据.md` §七.2 的标题写「`SKILL.md` 速查表「例」列只出空模板那一条」，`src/workout/commands.ts:59`（#948 新写注释）写「本格**只留第 1 例**」（按它自己第 56-57 行的编号，第 1 例＝① 不给 plan）；**实测该列出的是第 ② 种**——`SKILL.md:187`（表头在 `:88` 的 `| 唤醒词 | key | shape | 预检页 | 流程 | 例 |`）的例列＝`calorie-cmd-read calorie.view.plan-wizard --params '{"plan":{"config":{"title":"减脂4周",…}}}'`，`--list` 同。**该列本身没被 #948 改过**（`4a1122e8` 只动了 `calorie.view.plan` 那一行，plan-wizard 那行是 diff 的上下文）⇒ 错的是**新写的三处陈述**，不是表。功能不受影响（两种形态都真能跑），但陈述会误导下一手（例如照「只出空模板」去动 `gen-cli.mjs`）。编排者那句「部分达成」的裁定本身不受影响 |
| **D4** | **本波引入**（#947） | **S3** | 包根多一件**未跟踪**件 `packages/skill-calorie/tsconfig.t947.json`（内容：`extends ./tsconfig.json`、`outDir ./.scratch/t947/dist`，即 #947 席的临时编译配置），既不在 `.scratch/`（那条被 `.gitignore` 覆盖）也没进提交 ⇒ 违反收口令 §0「每一席的声明路径下无未提交改动、无未跟踪件」。它在扫描面外（`src/**/*.ts`、`scripts/**/*.mjs`），故台账门照绿。建议收口前删掉或移进 `.scratch/t947/` |
| **D5** | **范围外发现**（编辑器时段模型，`918a9706` #704 搬来的既有件） | **S3** | `calorie.view.plan-wizard` 的部分预填会把所给 `plan` 的时段名归一：探针给 `session_label:"晚课"`，载荷与库内都成 `凌晨`（`planEditorPort.ts:81-83` 的 `slotOf`：含四档字样才命中，否则落第一档 `SLOTS[0]`）。页面、载荷、库三者自洽（都显示 `凌晨`），丢的是「所给值 → 状态」这一跳。不在 #948 写集（它只给 `planEditorPort.ts` 加了一行接线），非本波引入；建议转票 |
| **D6** | **范围外发现**（工具作用域，答派单 §二 item 7） | **S3** | `docs/agents/t540-指纹绑定.mjs` 的 `--scope` 是**整包**：`fingerprint(<scope>/src)`／`<scope>/dist` 全树逐件 sha256，**不按票的声明路径隔离**。故 #946 与 #949 报同一个 `src-sha=b4dfdf45c98f7b95` 是**正常的**（两席读数时点 15:13–15:17Z 同在一棵树上，#948 的 src 改动到 15:38Z 才落）——这条巧合**不是**「作用域没按票隔离」的证据。另一面：同一 `src-sha` 下两票的 `dist-sha` 不同（`226cc200fe54fc7b` vs `b4a1c23ce6054c7d`）也可解释——dist 里住着生成器印记 `dist/.gen-inputs.json`（`pnpm build` 调 `gen-cli.mjs --stamp` 写，4300 字节），它**不是 src 的纯函数**，两票之间正好夹着 #948 的 `pnpm build`／`pnpm gen`。⇒ 结论：指纹读数的两侧都只对该包当时的现场成立，**不能**当「某票自己的绑定」用；登记，不据此判 FAIL |
| **D7** | 范围外发现（台账） | **S3** | `packages/skill-calorie/AGENTS.md` 里 `| scripts/build-help.mjs | — | 758 |` 有**两条完全相同的行**；`09e0cca0^` 里就已经是 2 条 ⇒ 不是本波 `--sync` 引入。台账门照绿（99/99） |
| **D8** | 范围外发现（dist 卫生） | **S3** | `packages/skill-calorie/dist/render/planEditor*.{js,d.ts}`（mtime 2026/9/18）等 #704 搬迁**之前**的陈旧产物仍在 dist（tsc 不清 outDir）。#946 证据件 §五 记的「别席坏 dist 把判据件带崩」与 #947 证据件 §三 记的编译受阻，风险面都长在这堆残留上。非本波引入；建议收口时评估一次 dist 清理 |

- 逐票**没有** S1／S2：四票的判据命令与实施席证据件的读数**逐条一致**（§二），自设探针与变异复核也都咬得住；D1／D3 是同一票契约面上的边界与陈述问题，不证伪任何一条票面验收判据。

## 六 · 派单点名要核的几条（逐条答复）

1. **#949 的指纹两行与「02-30 仍放行」**：`t949-证据.md` §三的 `src-sha=b4dfdf45c98f7b95`／`dist-sha=b4a1c23ce6054c7d` 与 §四的 `CASE update-02-30 exit=0 db.start_date="2026-02-30"` 在判据件里都是**真断言**（我复跑 §二 第 5 行绿，`T949-RESULT` 里「02-30 仍放行」在列）。`rg "startDateInvalid" packages/skill-calorie/src` 命中 **4 行／2 件**（`planStore.ts:88` 定义、`planStore.ts:107` 校验器自用、`write.ts:25` import、`write.ts:473` 调用）⇒ 与「概念唯一、只在 planStore 定义 ＋ write 引用」的**件**口径自洽。
2. **#947 的两处口径与夹具不符**：核过，都在判据件里如实留痕且与实况一致——越界档取末日次日 `2026-09-29`（票面给的 `2026-09-23` 单列成「范围内无课」反面参照档，实测不报「计划已结束」）；「有课的周数」冻结成 2（我的三周夹具探针当场把这一格推到 3，口径跟着走）。
3. **#946 代落的故障 2 那一格**：`SKILL.md:79`（写前预览页之后回「确认」＝落该 `op` 的授权，附九条 op→确认命令表）在场，`SKILL.md:85` 的 #943「落地训练族」那一段**未被这一笔碰到**（`4a1122e8` 的 `SKILL.md` diff 只加了三段、改了 `calorie.view.plan` 那一行例列）⇒ 两段不打架。
4. **#948 提交面**：`4a1122e8` 的 11 件里没有 #946／#947／#949 的任何件；四件生成物（`src/cli/keys.ts`／`src/cli/registry.ts`／`src/triggers/routes.generated.ts`／`packages/base-combos/combos.yaml`）在该提交里零 diff（与本席 `gen:check` 6/6 一致）。
5. **#948 的 LF 账**：`4a1122e8^` 的 `planEditorRuntime.ts`＝**426** LF、`4a1122e8` 与 HEAD 都＝**412** LF（逐字节数 `\n`）⇒ 证据件「改动前 426 → 改动后 412、净减 14」属实；`planEditorPort.ts` 257／`planPayload.ts` 149／`planEditorDocs.ts` 73 也逐件对上。

## 七 · 未做项（逐条交代）

1. **全量套件（`packages/skill-calorie/test/*.test.mjs`，208 件）未跑**：按收口令 §3 它属**合并点安静态**复核（编排者收口时跑），派单 §一D 也明写「不许拿全量门给两席定罪，也不许它充作两席的绿」。本席改为跑**与该波直接相关**的四票判据件 ＋ 既有 `test/scene05-*` 三件冻结用例（全绿）。
2. **没做的写操作**：未改四票任何代码件／测试件／证据件；未写 `packages/skill-calorie/AGENTS.md`；未 `push`；未用 `gh` 动票据状态；未新建源码件。
3. **未复核别席在途件的实体**：`packages/base-render/**` 三个 `M`（help-shell 一族）与 `packages/skill-calorie/.dsh-im/**` 六个 inbound 目录与本波无关，只登记在 §〇 的树状态里。
4. **D1／D3／D4 三条要不要就地收**：本席按红线只报不改；D4（未跟踪件）与 D3（一句说明面）成本极低、可在收口窗口顺手处理；D1 建议另票。

## 八 · 逐条路径

- 报告件：`docs/skills/skill-calorie/t944-复核-报告.md`（本件）
- 探针／变异件：`.scratch/t944-review/probes.mjs`、`.scratch/t944-review/probes-946-nine.mjs`、`.scratch/t944-review/mutate.mjs`、`.scratch/t944-review/run1.ps1`、`.scratch/t944-review/drive1.ps1`、`.scratch/t944-review/drive3.ps1`
- 机器日志（§2.2：整份落盘；终端只回尾部）：`.scratch/t944-review/{gate-examples,gate-onepath,gate-pageassert,gate-warnline,gen-check,tsc-force,t946,t947,t948,t949,examples-list,scene05-frozen,probes,probes2,mutate,probes-nine}.log` ＋ `probes.out.txt`／`probes-946-nine.out.txt`／`mutate.out.txt`／`drive1.out.txt`／`drive3.out.txt`
- 对账源：`.scratch/locks/gate-runs.log`（本席 runId 逐条见 §二）
