# t375 肉眼清单（9 份产物双击验收）

- 归属：地图 #162 场景 01，#375《端到端验收＋肉眼终审》前置清单。本件只备清单，不改源码，不关任何票。
- #375 状态为 OPEN，待用户肉眼终审；本件不代判通过与否。

## 一、9 行绝对路径（逐行双击能开）

双击说明：在文件管理器地址栏粘贴下面每一行绝对路径后回车，或双击该文件，均可用浏览器打开查看版面。

| 唤醒词 | 绝对路径 | 字节 | 头 30 行 |
|---|---|---:|---|
| 看今日主页 | `D:\ilife\.scratch\t371\products\calorie_html\今日总览_20260914_124059.html` | 68502 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看本周主页 | `D:\ilife\.scratch\t371\products\calorie_html\今日总览_20260914_124059_2.html` | 68502 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看本月主页 | `D:\ilife\.scratch\t371\products\calorie_html\今日总览_20260914_124059_3.html` | 75844 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看连续记录天数 | `D:\ilife\.scratch\t371\products\calorie_html\今日总览_20260914_124059_4.html` | 68502 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看今日热量预算 | `D:\ilife\.scratch\t371\products\calorie_html\今日总览_20260914_124059_5.html` | 68502 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看今日饮食概览 | `D:\ilife\.scratch\t375\看今日饮食概览.html` | 69111 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看今日运动概览 | `D:\ilife\.scratch\t375\看今日运动概览.html` | 66238 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看今日目标进度 | `D:\ilife\.scratch\t375\看今日目标进度.html` | 1666 | 片段，以 `<section` 起，无 `<!doctype html>`，归场景 06 整页化，本票只验跑通 |
| 看今日体重概览 | `D:\ilife\.scratch\t375\看今日体重概览.html` | 67132 | 含 `<!doctype html>`＋`charset="utf-8"` |

## 二、种子口径

- 固定种子库 `SEED_TODAY=2026-09-07`（`docs/research/t81-seed.mjs::seedFull`），真命令落盘。
- 判定口径与锁票一致：有当天段出结果页；无段给 `exit 4` 可读缺失阻断且不落盘，也算跑通。
- 前 5 份为 #371 产物（`.scratch/t371/products/calorie_html/`，落盘时同一cmd 真跑，与证据哈希字节一致）。
- 后 4 份因 #372 矩阵只写系统临时目录、落盘已失效，按同一固定种子库重跑对应 4 条命令生成到 `.scratch/t375/`，复制为顶层稳定名供双击；生成脚本 `.scratch/t375/gen4.mjs`（草稿，不入库）。

## 三、肉眼三看（用户逐份判通过或不通过）

1. 版面不白页：双击打开后版面正常显示，非白页非空页。
2. 字段与 HELP 一致：页上关键字段与 HELP 说明一致（如总览含今日摄入蛋白饮水缺口连续记录，饮食含累计日均目标趋势，运动含总消耗总时长，体重含体重盘距目标变化，目标进度含完成与目标完成率）。
3. 路径绝对：回执路径与上表绝对路径一致，文件真实存在且能打开。

## 四、用户动作

- 请双击上表 9 个绝对路径逐份查看，每条记录：唤醒词 → 路径 → 能开 → 通过或不通过（以用户原话为准）。
- 未通过项转成有编号的票并回写 #375；本清单不预填结论。
