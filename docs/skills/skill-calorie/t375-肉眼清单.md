# t375 肉眼清单（9 份产物双击验收）

- 归属：地图 #162 场景 01，#375《端到端验收＋肉眼终审》前置清单。
- 本清单只备路径与读数，不做视觉判断，不预填结论（用户说了算）。
- #375 状态为 OPEN，待用户肉眼终审。

## 一、9 行绝对路径（逐行双击能开）

双击说明：在文件管理器地址栏粘贴下面每一行绝对路径后回车，或双击该文件，均可用浏览器打开查看版面。

| 唤醒词 | 绝对路径 | 字节 | 头 |
|---|---|---:|---|
| 看今日主页 | `D:\ilife\.scratch\t375\products\看今日主页.html` | 69225 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看今日饮食概览 | `D:\ilife\.scratch\t375\products\看今日饮食概览.html` | 69543 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看今日运动概览 | `D:\ilife\.scratch\t375\products\看今日运动概览.html` | 66670 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看今日体重概览 | `D:\ilife\.scratch\t375\products\看今日体重概览.html` | 67553 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看今日目标进度 | `D:\ilife\.scratch\t375\products\看今日目标进度.html` | 1666 | 片段，以 `<section` 起，无 `<!doctype html>`，整页化归场景 06，本票只验跑得通 |
| 看本周主页 | `D:\ilife\.scratch\t375\products\看本周主页.html` | 69225 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看本月主页 | `D:\ilife\.scratch\t375\products\看本月主页.html` | 76593 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看连续记录天数 | `D:\ilife\.scratch\t375\products\看连续记录天数.html` | 69225 | 含 `<!doctype html>`＋`charset="utf-8"` |
| 看今日热量预算 | `D:\ilife\.scratch\t375\products\看今日热量预算.html` | 69225 | 含 `<!doctype html>`＋`charset="utf-8"` |

## 二、种子口径（本批 9 份一律重跑，不再混用旧产物）

- 固定种子库 `SEED_TODAY=2026-09-07`（`docs/research/t81-seed.mjs::seedFull`），9 条唤醒词对应命令逐条真跑，产物落 `.scratch\t375\products\`。
- 顶层 9 个稳定名由脚本从原始落盘复制而来（原始落盘在 `products\work-0`～`work-8\calorie_html\`，命令与唤醒词的对应就是脚本里的 9 条），生成脚本 `.scratch\t375\gen9.mjs`（草稿，不入库）。
- 判定口径与锁票一致：有当天段出结果页；无段给 `exit 4` 可读缺失阻断且不落盘，也算跑得通。
- 前几轮的旧产物（`.scratch\t371\products\` 与 `.scratch\t375\` 顶层那 4 份）留着不动，本清单只认上面 9 行。

## 三、肉眼三看（用户逐份判通过或不通过）

1. 版面不白页：双击打开后版面正常显示，非白页非空页。
2. 字段与 HELP 一致：页上关键字段与 HELP 说明一致（如总览含今日摄入蛋白饮水缺口连续记录，饮食含累计日均目标趋势，运动含总消耗总时长，体重含体重盘距目标变化，目标进度含完成与目标完成率）。
3. 路径绝对：回执路径与上表绝对路径一致，文件真实存在且能打开。

## 四、用户动作

- 请双击上表 9 个绝对路径逐份查看，每条记录：唤醒词 → 路径 → 能开 → 通过或不通过（以用户原话为准）。
- 未通过项转成有编号的票并回写 #375；本清单不预填结论。
