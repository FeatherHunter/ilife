# t281 · 读侧铺开：对比两张照片＋查身材照（完整文档＋内嵌照片＋复制区）

## 目标

`calorie.photo.compare`（对比两张照片）与 `calorie.photo.detail`（查身材照，单张页）
两条读命令的产物与票 1（#341 看身材照）同形：完整文档 ＋ 内嵌照片 ＋ 复制区。
单张页同时充当删身材照流程的快照。

## 改法（一句话一件）

- 取数不动：对比仍走 `buildCompareData`，单张仍走 `buildViewerData`，`data` 形状逐字节不变。
- 呈现改走本能力内两新件（`src/render/html.ts` 只读，不加行；`galleryDoc.ts` 只复用，不改）：
  `compareDoc.ts`（间隔横幅＋跨标签警告＋双卡内嵌＋复制区）与 `viewerDoc.ts`
  （翻页链＋黑底大图＋快照＋删改链＋复制区），经共用件 `assembleDocPage` 包成整页。
- 缩略体内嵌复用 `photoThumb.ts`，上限复用 `PHOTO_LIST_PAGE_MAX_BYTES`（只引用，不另定）。

## 融合验收对照（t400＠9a53c20）

- 对比页裁定 4：跨标签警告行文案保留（老页 `:75`＋本仓句合并），间隔横幅大数字 N
  经测试断言等于两张照片日期差（1 vs 2→1 天；1 vs 4→3 天）；不存在的 id 走既有
  缺失阻断（exit 4，不回半页）。
- 详情页裁定 4：同标签前后翻页可点（中张双链），首尾禁用（首张只剩下一张链、
  尾张只剩上一张链、孤张双禁用），无 `href="#"` 坏链；返回带筛选上下文
  （返回画廊＋标签＋`calorie.photo.list` 命令）。
- 详情页裁定 6：大图黑底 75vh contain；删记录说明按硬删除口径（不可恢复），
  全文无「文件保留」。
- 两页裁定 5：完整文档（doctype 起、charset）＋复制区分族（复制数据，三格式菜单随
  共用件恒开），位置照基准骨架（页尾）。
- 体积口径复用 t341：超预算即横幅「已嵌入 N 张／还有 M 张未嵌入」＋替代操作
  （2×400KB 对比页实测：弃一张后回绿，正常照片不受牵连）。
- 已知局限（落盘静态页无稳定单图路由）：翻页链锚点给可点形态，`data-command`
  给照抄重跑命令；首尾禁用无空 href。见本件「未做项」。

## 验收证据

- `pnpm build` exit 0；`pnpm gen:check` exit 0（PASS，119 键）。
- `node --test packages/skill-calorie/test/photo-shape-read-281.test.mjs`：7/7 绿。
- 变异两行（测试 stdout 原文）：
  - `281-MUT-RED 去内嵌必红 OK`
  - `281-MUT-GREEN 改回必绿 OK`
- GATE-RUN：`RUN ticket=281 … cmd="node --test packages/skill-calorie/test/photo-shape-read-281.test.mjs" waitedMs=10002 exit=0 … at=2026-09-14T11:14:56.215Z`
- 每用例均断言 `data.output` 绝对路径且在盘上（失败用例断言无半页：stdout 空）。
- 票 1 回归：`photo-shape-341.test.mjs` 4/4 绿（本票零回归）。

## 基线 `--check` 归因（红项附归属）

- 本轮 `--check` 报 `BASELINE-DIFF 8 pages`，做还原对照实验（本票 4 件移出＋两改件
  `git checkout` 回 HEAD，重建后重跑）：
  - 无本票文件时：`compare`／`detail` 回到 `ok`（旧 sha），另 6 页照红 → 6 页与本票无关。
  - 有本票文件时：`compare`（`…9d85df71…`）／`detail`（`…65cc9179…`）为本票有据变更。
- 他席 6 页（工作树脏文件归属他席，本票 `git diff` 文件清单不含它们）：
  - `list`／`gif-planner`／`photo-log-wizard`：确定性漂移（两次跑 got 值逐字相同，
    如 list `43d3c6c9…`），来自他席脏树（`shared/docPage.ts`／`shared/copyArea.ts`／
    `render/*Docs.ts` 等）；t341 形状测试仍 4/4 绿，属良性。
  - `add`／`remove`／`tag`：两次跑 got 值不同（如 add `7fdaeeec…` vs `c98ba3f8…`），
    含非确定性内容（疑似他席在建页，时钟或随机序未钉），请审查席转交相关席位钉死；
    本票文件不喂这三页（还原实验已证无本票时照红）。

## 未做项与下一手

- 基线脚本未 `--write`（棘轮文件不碰；compare／detail 新 sha 待提交席与审查席确认后统一重落）。
- 回执侧铺开归票 3；缩略图件暂只有本能力在用（`galleryDoc`／`compareDoc`／`viewerDoc`／
  `receipt`／`pickerDoc` 五处调用仍在同一能力内），「上移共用位」条件未触发，不搬。
- 翻页链稳定单图路由（落盘页直达链接）超出本票口径，留待后续票确定。
- `git add` 未做（留给提交席）；工作树仅本票 5 处（2 改＋3 新）＋证据件，见写集声明
  `.scratch/t281/WRITESET.md`。
