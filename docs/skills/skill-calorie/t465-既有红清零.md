# t465 · 运动族既有红清零（7 条逐条定性＋「可见面 vs 载荷面」口径落地）

票：#465（地图 #156）。写集：两张旧测试件＋本证据件；**实现件一行未改**。
进场树：`packages/skill-calorie/src/render/sportPortDocs.ts` sha `2CEC26DA…`（＝`git show HEAD:` 同值，LF 760 行）；
`pnpm build` exit 0（runId `59074bdf-6553-49dd-b28b-3a27e448dfdc`）。

## 0. 现场读数

复跑两张旧测试件（runId `79ec3796-9a97-4730-8eca-0f24bcc59a97`，票据原 runId `c8a633fb-bcba-4edf-a7fe-5df26eab9e52`）：
port-111 **6 红**／routes-265 **1 红**，共 7 条，与票面数一致。

## 1. 七条逐条定性

| # | 位置 | 红条原文（截断） | 定性 | 归属 | 处置 |
|---|---|---|---|---|---|
| ① | port-111:113 | `唤醒词错键：看运动记录（按力量筛选）` actual `…exercise-records` expected `…exercise-strength`（按有氧同条同因） | 乙 | **#342**（`5d159c0` 运动记录 3 词改指记录级明细） | 判据跟上（钥匙＋注释标归属） |
| ② | port-111:163 | `strength 缺：力量训练总览 2026-09-05 ~ 2026-09-07` | 甲 | #453 融合版式 | 旧针拆成页题＋窗口两针 |
| ③ | port-111:181 | `cardio 缺：有氧训练总览 2026-09-05 ~ 2026-09-07` | 甲 | #453 | 同上 |
| ④ | port-111:198 | `distribution 缺：运动类型分布 2026-09-05 ~ 2026-09-07`（下一针 `按分类次数占比` 亦已改名） | 甲 | #453 | 页题＋窗口两针；`按分类次数占比`→`分类占比` |
| ⑤ | port-111:214 | `recap 缺：高频 TOP5` | 甲 | #454 | 改 `高频运动`（前 5 名以徽章列） |
| ⑥ | port-111:305 | `复制头与 envelope key 不一致`（要 `【calorie · calorie.view.exercise-trend】`） | 甲 | #465 口径 | 拆「载荷面／可见面」两句＋全文一句 |
| ⑦ | routes-265:130 | `看运动类型分布 缺 eyebrow key calorie.view.exercise-distribution`（要产物含命令键） | 甲 | #465 口径（票面未点名、同族，已获编排者授权） | 页身份改认眉标逐字＋载荷头 |

甲类 5 条（②③④⑤⑥⑦）＝旧版式／旧口径字样；乙类 1 条（①，含力量／有氧两处同因）。

## 2. 甲类：旧断言 → 新断言 → 依据

### ②③④ 页题与窗口
- 旧：`['力量训练总览 2026-09-05 ~ 2026-09-07', …]`（页名＋空格＋窗口连写）。
- 新：`['力量训练总览', '2026-09-05 ~ 2026-09-07', …]`（有氧／分布同形）。
- 依据：融合版式把窗口挪进「窗口」卡；当刻产物 `力量训练总览` 命中 1 处、`2026-09-05 ~ 2026-09-07` 命中 1 处，连写命中 0（探针 `.scratch/t465/probe.mjs`）。
- 分布另针：旧 `按分类次数占比`（命中 0）→ 新 `分类占比`（命中 3：页内导航／图卡／明细表）；`按分类热量分布` 仍命中 1，保留。

### ⑤ 复盘针
- 旧：`'高频 TOP5'`（命中 0）；新：`'高频运动'`（命中 2：页内导航＋区块题）。依据同上探针；`运动复盘 2026-09-05 ~ 2026-09-07`／`每日消耗趋势`／`累计消耗 1320 卡` 等针**仍在**，保留。

### ⑥ 复制头：两面分开判（票面第 2 条）
- 旧：`assert.ok(out.html.includes('【calorie · calorie.view.exercise-trend】'))`。
- **核实到的事实**（与票面推测不同）：五页载荷里**也没有命令键**——复制 text 载荷头＝`【calorie · 运动趋势】`（技能 · **人话页名**），json 载荷＝`{"version":"0.1.0","skill":"calorie","shape":"stat","key":"运动趋势",…}`；整份产物 `calorie.view.` 命中 **0**（含载荷）。票面第 2 条原话留了「或按你核实的事实写成载荷面断言」的口子，据实落地。
- 新（三句，各自判哪一面写在断言旁）：
  1. **载荷面**（`data-fmt="text"` 的 `data-t`）：头行得是 `【calorie · 运动趋势】`；json 载荷 `version`／`skill`／`key` 三处与冻结 envelope 对齐（`key` 是人话页名）。
  2. **可见面**（剥掉 `<style>`／`<script>`／全部标签后的纯文本）：`calorie.view.`／`calorie.exercise.`／票号／`移植` 零命中。
  3. **全份产物**（含载荷）：`calorie.view.` 零命中——本页入口是唤醒词，命令键一次都不该印出来。
- 不改 `可见面零 snake_case` 的理由：本页来源脚注照 #451／#453／#454 版式仍写库表名 `exercise_log`（可见面），族内 #452 两页才换成人话来源名；本图口径只管命令键／票号／工序词三类，故只按三类判（事实登记，见「未做项」）。

### ⑦ routes-265:130
- 旧：`assert.ok(r.file.includes(c.key), …缺 eyebrow key…)`——要产物（可见面或载荷）含命令键。
- 新：眉标逐字相等（`运动 · 类型分布`／`运动 · 趋势`／`运动 · 复盘`，读 `ilife-block-page-shell-eyebrow`）＋题面含页名＋全份产物零 `calorie.view.`／零 `移植`＋**载荷面**复制头得是 `【calorie · <页名>】`。页身份不再靠命令键，改靠「可见面眉标＋载荷面页名」两处。

## 3. 乙类 ①：唤醒词错键（归属 #342）

- 旧断言：`['看运动记录（按力量筛选）', 'calorie.view.exercise-strength']`（有氧同理）。旧原文自 #111 `ee8a1f0c` 起未动过。
- 当刻事实（机器读数）：`routesFor` 两词均指 `calorie.view.exercise-records`（带 `category=力量／有氧`），`CALORIE_COMBOS` 已登记该键；即 #342「运动记录 3 词改指新命令并同步冻结表」的有意改动，本件判据没跟上。
- 新断言：两词期望键改 `calorie.view.exercise-records`，并就地标归属注释；**未改路由／命令声明**。

## 4. 变异自证（改坏必红 → 改回必绿）

改坏两处（都在趋势页同一处实现位）：`title: '运动趋势 ' + …` 里塞命令键（打**可见面**判据）＋`finishPage(…, '运动趋势', …)` 的载荷页名改 `运动趋势（改坏对照）`（打**载荷面**判据）。

**（甲）真件窗**（runId `6a7e91e1-2dce-435d-89f2-1bc4e2aaad7b`；日志 `.scratch/t465/mutation.txt`）
进场 `2CEC26DA…` → 改坏 `C3E9C2C5…` → `pnpm build` exit 0 → 红条原文：
`trend 缺：运动趋势 2026-09-05 ~ 2026-09-07`／`复制载荷头不是「技能 · 页名」形态："【calorie · 运动趋势（改坏对照）】…"`／`可见面出现命令键：calorie.view.`（port-111 pass=10 fail=3）；routes-265 `看运动趋势 产物里出现命令键 calorie.view.*`（pass=2 fail=1）。

**事故登记（如实记）**：该窗与**别票写者**相撞——还原前复算哈希 `now=5DD17AF3…≠` 我的变异态，按自锁协议**中止未覆盖**；随即复看该件已回到 `C3E9C2C5…`（＝**我的变异字节**，mtime 00:10:10），即对方把「它读到的原版」写回时读到的是我的变异态。我随即开**还原窗**（runId `bba83603-01cc-4ab7-b234-1c8d684e42cc`，日志 `.scratch/t465/restore.txt`）：写回进场备份 → `RESTORED-SHA=2CEC26DA…`（与进场**逐字节相同**）＋刷 mtime 00:10:46。**该还原写回的是 HEAD 版字节**（`git show HEAD:` 同值，收尾前复核 SHA 仍为 `2CEC26DA…`）；若那一刻别票（T351-v7 拆件席）有未落盘内容，则被这一写覆盖——按协议如实登记，供其复核。

**（乙）镜像法**（编排者指定；真件自此只读）
- 镜像＝整包字节同源镜像 `.scratch/t465/mirror/`（`src` 280 件逐字节同源 diff=0；`node_modules` 接 junction）。
- **artifact parity**（runId `a498e586-42a3-4adc-9c0d-bac5e93c1be6`）：镜像产物 vs 真 `dist` **1120 个文件逐字节同值、0 差异**，仅镜像独有 0；真 `dist` 另有 **121 个陈旧产物**（源已不在 `src` 面内：30 个已删源的 `.js` ＋其 `.d.ts`／`.map`，另 1 个 `.gen-inputs.json` 非 tsc 产出）——两侧构建都不产它。
- 变异自证（runId `722107fb-1842-49b2-8c4a-77d68ccb0cfd`，日志 `.scratch/t465/mirror-mut.txt`）：镜像源面先回 `2CEC26DA…`（＝HEAD 版）→ 清 `dist`／`tsbuildinfo` **新鲜编译** → 三段读数：
  - 基线（未改坏）port111 `pass=12 fail=1`、routes265 `pass=3 fail=0`；
  - 改坏 `C3E9C2C5…` → port111 `pass=9 fail=4`、routes265 `pass=2 fail=1`，红条＝差分正是设计那三条（`复制载荷头不是「技能 · 页名」形态："【calorie · 运动趋势（改坏对照）】…"`／`可见面出现命令键：calorie.view.`／routes-265 `看运动趋势 产物里出现命令键 calorie.view.*`）＋一条级联（`trend 缺：运动趋势 …`，题面被改坏所致）；
  - 还原 `2CEC26DA…` → 读数**回到基线逐条相同**（`12/1`、`3/0`）＝改回必绿。
  - 基线那 1 条常驻红 `review 缺：计划 vs 实做` 属**在飞别票 T351-v7**（整页搬进 `render/reviewDocs.ts`、眉标改 `健身计划 · 计划复盘`、`计划 vs 实做` 字样不再有），非我的变异、也不是本票 7 条红之一。

## 5. 门禁对账（GATE-RUN）

**收尾当刻**（00:19，runId `899feaff-c01e-468c-a415-6da6afab3566`，真件当刻字节 `sportPortDocs.ts` sha `D85B70D7…`＝别票 T351-v7 在写版）——七张判据全绿：

| 命令 | 读数 |
|---|---|
| `node --test …/exercise-port-111.test.mjs` | `pass 13 / fail 0` |
| `node --test …/exercise-routes-265.test.mjs` | `pass 3 / fail 0` |
| `node --test …/exercise-receipt-fusion-423.test.mjs` | `pass 20 / fail 0` |
| `node …/exercise-records-fusion-451.test.mjs` | `pass 8 / fail 0` |
| `node …/exercise-summary-goal-fusion-452.test.mjs` | `pass 9 / fail 0` |
| `node …/exercise-dist-strength-cardio-fusion-453.test.mjs` | `pass 5 / fail 0` |
| `node …/exercise-trend-recap-fusion-454.test.mjs` | `pass 9 / fail 0` |
| `node packages/skill-calorie/scripts/check-warning-line.mjs` | 先红（`台账陈化：src/weight/review.ts 台账=602 实况=600`，别票在飞件）→ `--sync` 后 `RESULT: 58/58` `PASS`（runId `b96e2a4a-920c-459b-a780-dd029488d804`）；台账该行随本提交入库 |

同一组判据在**我进场版字节**（`sportPortDocs.ts` = `2CEC26DA…`＋`pnpm build`）下亦全绿（runId `bba83603-01cc-4ab7-b234-1c8d684e42cc`，00:10:46）：port111 `13/0`／routes265 `3/0`／423 `20/0`／451 `8/0`／452 `9/0`／453 `5/0`／454 `9/0`。

## 6. 未做项与转票

1. **别票在飞 · T351-v7（拆件席）**：复盘一整支正搬出本件（`render/reviewDocs.ts` 新增，`sportPortDocs.ts` 当刻 M）。该席 00:13:43 **就地更新了本件复盘针**（`计划 vs 实做`→`每日完成情况`，另加周次列／热力图／禁「会话」三句）——与我的改动同处 `exercise-port-111.test.mjs`，本提交**一并带入库**（如实登记，非我方改写）。我的镜像快照取在 00:13:21（早于该针改动），故镜像基线读到 `review 缺：计划 vs 实做`——那是**两侧暂态错位**，不是我的变异（同快照的变异／还原两段差分仍全由我的改坏造成）。
2. **转票 · 族内口径不一致（#451／#453／#454 面）**：本件五页**可见面**的来源脚注仍写库表名 `exercise_log`（#452 两页已换人话来源名，其测试自述「族票共同要求：可见文本零 snake_case」）。本票只判三类工程话，不动 `sportPortDocs.ts`；建议立票收口。
3. 实现件（`exercise/receipt.ts`／`records.ts`／`render/sportDocs.ts`／`render/sportPortDocs.ts`）**一行未改**——两条旧判据的更新全在测试面完成。
4. 载荷面「命令键」未落成断言：核实事实是**两面都无命令键**，故按事实写成「载荷面认技能 · 页名＋全文零命令键」（见 2.⑥）。
