# t524 · 场景 09 视觉整改基准 · 独立对抗审查

> 被审件：`89e2140`（`docs/skills/skill-calorie/t524-场景09-视觉整改基准.md`／`t524-改前读数.md`／`t524-证据.md`）。
> 审查席位：独立对抗审查（作者禁自审）；**不改被审件、不改产品代码**。我重跑的命令与原始读数见 `.scratch/review-524/evidence.md`。
> **总分 63／100　结论：退回（逐条必改项）**——基准的**核心读数全对**（可复现），但 §4 这把尺**自相矛盾且局部不可达**，会把后续五票与终审一起带偏。

## §1 逐条判据表（作者声称／我实测／差）

| # | 作者声称 | 我实测 | 差 |
|---|---|---|---|
| 1 | 探针 `RESULT: 0/16` FAIL，节点 231 处（R1 154／R2 6／R3 83／R4 0／R5 2／R6 1／R7 121） | 自跑：`RESULT: 0/16` FAIL，`nodeHitsTotal=231`，七条规则计数逐条同 | 一致 ✓ |
| 2 | 16 页逐页命中 34/10/10/5/4/5/15/24/17/8/8/8/10/26/13/34 | 自跑逐页同值（合计 231） | 一致 ✓ |
| 3 | `OVERFLOW-ZERO pages=16 cells=48 failed=0` | 自跑同一摘要行、exit 0；基准把它写作**回归门**、未报成绩 | 一致 ✓ |
| 4 | 1440 档触摸不足 5～49 处／页、390 档 0 | 自跑 49/7/7/5/7/7/7/7/5/5/5/5/5/7/8/15，390 档全 0 | 一致 ✓ |
| 5 | 390 档字号下限 11px（12 页）／12px（4 页） | 自跑同值；**但 §2.0 P2 行写「字号 <12px 16/16 页」**，与 §3.2、读数件、我实测（12 页 11px＋4 页 12px）互斥 | **不实**（应 12/16 页） |
| 6 | 基线均分 61 | 按读数件 §6（权重 D1 15／D2 20／D3 25／**D4 20／D5 20**）算 60.96≈61 ✓；基准 §4.1 表却写 **D4 15／D5 25**，而同节句子又说「D3＋D5 合计 45」（＝D5 20） | **走散**：两个权重口径 |
| 7 | 源件名订正为 `buildPhotoListDoc` | 源码确为 `buildPhotoListDoc` ✓，当刻在 `galleryDoc.ts:234`（基准写 `:219`） | 名对、行号已漂 15（基准自曝漂移风险） |
| 8 | 三层断点复核「值都对、三层说法不成立」，HELP 另用 500/820 | 逐条复核成立：`style.ts` 640/400（现 `:1183`/`:1194`）、`spec/charts.ts:228`＝720、`spec/controls.ts:194`＝820、`helpShell.ts:42`（500/501 各 1）、`assets/help-template.html:119`·`:156`·`:1405`·`:1453`·`:1638` | 成立 ✓（可采） |
| 9 | 每页断点构成 820×6／821×1／720×1／640×3／400×1，`@media` 14 条 | 09-02 逐值计数同 ✓ | 一致 ✓ |
| 10 | 「16 页**没有任何** `overflow-x:auto`（静态命中 0）」 | **每页 5 处 `overflow-x: auto`**，其中 `.ilife-block-data-table { … overflow-x: auto; … }`——**表格容器本身就是横滚容器** | **不实**（「0」只数了无空格变体） |
| 11 | 「改前产物里 HELP 页有 4 个 `ilife-block-toc`」 | 09-15／09-16 静态 `class="…ilife-block-toc`＝**0**；作者自己的 t516 表 390 档 `toc` 全 0；`score.mjs:54` 注释也写「全 16 页 DOM 里 block-toc 命中 0」 | **不实**（且与 §2.0「页内定位 0/16」互斥） |
| 12 | 读数件 §6 表「分隔符节点」＝34/10/10/5/4/5/15/24/17/8/8/8/10/26/13/34，合计 167 | 我直算纯分隔符节点＝34/4/5/5/4/5/4/22/16/5/6/5/8/23/7/14（合 167）；§6 的**逐行值其实是 R1–R7 节点合计**（合 231），12/16 行与合计自相矛盾 | **不实**（列错、合计对） |
| 13 | 每条机器判据对当刻 16 页判红；已绿者写「回归门＋变异自证」 | ①溢出＝回归门＋作者两变异 ✓ ②触摸 1440 红 ✓ ③触屏三件 `viewport-fit` 0/16 红 ✓ ④分隔符红＋变异 ✓ ⑤英文裸词**自认无工具** ⑥重复句半工具（机器 235／人核 29） | 大体做到；缺口见 §3 |
| 14 | 回归门 `gen-photo-baseline --check` 当刻 `exit=1`，病因＝`dist/diet/review.js` 导入缺名 | 自跑确实 `exit=1`，但病因是 **`BASELINE-DIFF 10 pages`**（`CHANGED calorie.photo.*` ×6 ＋ `UNSTABLE calorie.photo.remove：渲染当刻时钟未被剥净：2026-09-15 13:37:37`） | 现象一致、**病因写成过时的一条** |

## §2 变异两行（我自己造的探针，独立于作者的、结果同向）

探针＝`.scratch/review-524/probe/sep-probe.html`（作者 `good-sample.html` 的副本，未改原件）；变异＝`</body>` 前插 `<p class="fault">第一张已选；第二张待选。</p>`。

```
红向：FILE sep-probe.html  node·0 ；1 ≥3段0 ｜0 、0 ~0 标识符0  命中=1  (行级命中=1)
         L  121 # 49 R2  第一张已选；第二张待选。   <fault> zone=body
      RESULT: 0/1   FAIL   exit=1
绿向：RESULT: 1/1   PASS   exit=0
```

另加做一条作者没做的**真页变异**：09-02 产物副本塞同一处 `；` → `RESULT: 0/1` `FAIL` exit=1（命中 `L 2279 # 47 R2`）。
⇒ 分隔符探针**有牙**，且对真产物同样有牙。

## §3 判据识别力复核（作者自称「每条红或回归门＋变异」）

| 判据 | 识别力 | 判定 |
|---|---|---|
| P1 三档零溢出 | 回归门＋两条变异（超宽元素／无兜底大图） | 够 ✓ |
| P3 分隔符与内部标识符 | 当刻判红＋变异两行 | 够 ✓ |
| P2/D4/D5（触摸区／字号／断点／安全区） | 读数工具**不判红绿**，靠人核阈值 | 中（基准已如实标注） |
| 机审 ⑤ 英文裸词 | **当刻无工具**，H7 因此没有出口、也无法变异自证 | **识别力不足（哪条：H7；为什么：无出口、无变异）** |
| D9 页内无定位 | 「静态扫描＋t516 的 toc 计数」，但静态扫描把 **CSS 规则**当命中（作者自己的「4 个 toc」就是这么来的），工具又不判红绿 | **识别力不足（哪条：D9；为什么：把声明数当元素数）** |
| H3 同事实重复 | 机器只出候选、人核判红绿，「逐条如实报不算」无操作边界 | 中偏软 |
| H6 点击区 <44px | 按 **1440 档**读数逐处 −1，而基准给的修法只写在 `@media (max-width: 820px)`（`pageUi.ts:71`–`:87`）里 | **识别力反向（见 §4）** |

## §4 可判性复核（重点：我拿 09-02 当刻产物自己走一遍尺）

**两向之一：尺松不松？——不松。** 09-02 与我算出的分（输入全是我实测）：
纯分隔符节点 4／R7 节点 7／重复节点 4／390 触摸不足 0／1440 触摸不足 7／三档溢出 0／无页内定位／页壳 960。

- **口径 A（基准 §4.2 ③ 的字面公式：Σ逐维分 − Σ硬扣分，权重取 §4.1 表）**
  D1 11（三卡底边不齐、窄屏碎成 2+1、黑条与白卡撞色）＋D2 14（主角缺席、一片灰小字、同事实两遍）＋D3 8.9（25 −(1.2×4＋1.5×7＋4÷5)）＋D4 10（15−5）＋D5 18（25−5−2）＝ **61.9**
  硬扣 H1 8＋H2 21＋H3 4＋H6 7＋H7 1 ＝ **41** ⇒ **页分 ≈ 20.9**
- **口径 B（作者实跑的 `.scratch/t524/score.mjs`：只有维度一条腿、D4 20／D5 20）** ＝ **63.9**

**两向之二：改好的页面够不够 90？——当前不可达。** 两条硬扣分**结构性清不掉**：

1. **H6 与修法不同档**：H6 按 1440 档逐处 −1（09-01 49 处＝−49、09-16 15 处），而基准给的修法（`pageUi.ts:78` `min-height:44px`）在 `@media (max-width: 820px)` 里 ⇒ 1440 档的 40px 复制按钮／菜单项**永远修不到**（09-01 的 49 处不是笔误，是口径错位）。
2. **D4 的「改后必须满分」与算式互斥**：`score.mjs:50` 的 D4 自带 −5（「页壳 max-width 固定 960，1440 档左右各留空」）⇒ D4 上限是 15/20；基准 §4.1 又写「改后 D4 仍必须满分」，§4.3 的「每维 ≥80%」在 D4 上更是**当刻就不成立**（基准自己的读数 15/20＝75%）。

⇒ 结论：**判据不是太松，是自相矛盾＋局部不可达**；同一页两套口径差 **43 分**（20.9 vs 63.9），后续族票与终审的分数因此不可比。

## §5 越界与纪律

- `git show --stat 89e2140`：**恰好三件**，全部在票面交付面内；无 `packages/**`、无别人在途件、无 `git add -A`；`audit-separators.mjs` 未被本票改动（我按件头 sha 只读跑）✓
- 「未证／引错」当结论用的四处（`packages/base-render/src/pageUi.ts`）：§3.2 的 `:99`·`:100`（实为 `font-size:12px` 与 `}`，`max(16px, env(...))` 在 `:92`·`:93`）、§3.2 的 `:68`（实为 `}`，20px 在 `:62`）、§5.3 ①「复制日志置灰（`:60` 已备规则）」（该件查无此规则，`:57` 注释说规则已上移到 `style.ts`）。
- 被检页群出自作者、我只读；我的探针页与副本全部落 `.scratch/review-524/`（仓外与别人目录未动）。

## §6 必改项（逐条，改完可再送审）

1. **权重唯一化**：§4.1 表改成 **D4 20／D5 20**（与同节「D3＋D5 合计 45」、读数件 §6、`score.mjs` 一致），§4.3 的每维下限同步（D4≥16／D5≥16）；或反过来保留 15／25 一套并把读数件与 `score.mjs` 全量重算。现值：同一基准两个口径。
2. **算式落盘并二选一**：把 D3 折算式（`1.2×分隔符节点＋1.5×标识符节点＋0.2×重复节点`）与「硬扣分是否参与总分」写进基准件（现在只在草稿 `.scratch/t524/score.mjs`）——当刻两套口径差 **43 分／页**。
3. **H6 口径修正**：或按 390 档判（当刻 0 处，与 §2.3 ② 一致），或把 44px 修法扩到全宽；否则「逐页 ≥90」在 09-01（49 处）上不可达。
4. **D4 的满分／80% 门槛与 −5 冲突**：改口径或删「改后 D4 仍必须满分」，并校准 §4.3。
5. **事实订正四处**：①`overflow-x:auto`：删「静态命中 0」，改成每页实测 5 处（含 `.ilife-block-data-table` 自身即横滚容器），窄屏表格另给证据；②删「HELP 页有 4 个 `ilife-block-toc`」（实测元素 0）；③读数件 §6「分隔符节点」逐行改 34/4/5/5/4/5/4/22/16/5/6/5/8/23/7/14；④§2.0 P2 的「字号 <12px 16/16 页」改 12/16 页。
6. **行锚订正或标「已漂移」**：`galleryDoc.ts:219→234`；`pageUi.ts:99·:100→:92·:93`、`:68→:62`、`:60`（规则在 `style.ts`）；`blocks.ts` LF 2158→2164、`style.ts` 1310→1326。
7. **回归门病因改写**：当刻不是 `dist/diet/review.js` 导入缺名，而是 `BASELINE-DIFF 10 pages`（含 `UNSTABLE…渲染当刻时钟未被剥净`）；仍归 #530，但别把过时病因写成「当刻」。
8. **读数工具可复现性写清**：`t516-判据-版式.mjs` 工作树版当刻 `RESULT: -32/16`、字段全空、exit=1（另一 session 在途改件），HEAD 版可跑出与证据表逐值同的读数 ⇒ 基准件里写明「读数取自哪个 sha」。

## §7 我另找到的缺陷（作者清单里没有）

1. **09-02 卡与表重复同一事实**：卡「编号 #19／标签 正面」与表行「19／正面／存在」重复（H3 面），基准的人核表只记了「黑条／提示条／正文三处」这一条。
2. **H7 没区分折叠态**：09-02 两档截图里只见「复制数据 ▾」，三格式标签（`纯文本／JSON／CSV`）不展开不出现 ⇒ 「已知一处」的扣分口径不唯一（折叠态该不该扣，没写）。
3. **窄屏表格的真缺陷被 10 号错报盖住**：容器有 `overflow-x:auto`、表 `width:100%` ⇒ 表被压而不是滚动；基准该判的是「有横滚容器但表不撑开」，不是「没有任何横滚」。
4. **`@media` 14 条里没有一条是「表格卡片化」**：`pageUi.ts:130` 的 `:has(td[data-label])` 塌列规则当刻未接进产出链（16 页 `ilife-page-ui` 命中 0），而 §3.2 把它写成「已备」——「已备」与「已生效」在基准里没分开写。

## §8 遗留

- `gen-photo-baseline --check` 当刻红（`BASELINE-DIFF 10 pages`）——**不算作者的错**，但 #530 收口前必须重建，且基准件 §2.5 的病因需按 §6 第 7 条改写。
- 09-08／09-14 两页 42 MB（合计 45 MB）减重归 #461；09-16 的 20 处标识符与 #529 的重叠面待族票收口时再核。
