# t520 勘察 · 卡路里场景 10 的 31 页：页源归属表 ＋ 符号债／文案债逐处清单

> 席位：只读勘察席（无 git 写、无产品代码改动；全程只新建本文件一件）。
> 产物口径：`D:\ilife\.scratch\t387\html\*.html`（31 件，2026-09-15 00:15 由当时 dist 重跑；见 `.scratch/t387/验收清单.md` 附二）。
> 权威清单：`.scratch/t387/验收清单.md`、`.scratch/t387/sample-paths.txt`（31/31 存在、字节一致）。
> 纪律件已读：`docs/agents/编排纪律.md`、`docs/agents/structure.md`、`docs/agents/wording.md`。

## 0 · 一条先行发现（影响第 3、4 节的读法）

**这 31 份产物比当前源码旧一代。** 证据三条（都可复跑）：

1. `git --no-pager log --oneline -3 -S "体重外推（horizonDays" -- packages/skill-calorie/src/render/trendDocs.ts`
   → `ea7cdc9 fix(skill-calorie): 分析页可见文本返工——删冗余、换人话、修两处口径错（#160）`（**2026-09-15 02:30:21**）；
   产物跑批时刻是 **2026-09-15 00:15**（`验收清单.md:150`）⇒ #160 的可见文本返工**不在产物里**。
2. `(Get-Content -Raw .scratch\t387\html\29-查热量缺口.html) -match '<!--'` → `False`（31 件 `<!--` 全为 0），
   而 `#160` 引入的「技术口径改住 HTML 注释」（`src/render/trendDocs.ts:109` 的 `techNoteHtml`）在当前源码里到处在用。
3. 产物 01 页头眉标逐字为 `calorie.view.predict · 趋势分析域`（命令键上屏），
   当前源码 `src/render/trendDocs.ts:660` 已改成 `体重预测 · 趋势分析域`，
   且 `src/shared/docPage.ts:70` 的 `SOURCE_IDENTIFIER_RE` 会**整行挡掉**含命令键的眉标。

**推论（读法）**：
- **报告族（21–28）与缺口明细的骨架同代**——抽查的产物串在当前源码里都命中（`分项分数表`＝`reportDocScore.ts:55`、
  `连续多日低于基础代谢`＝`reportDocTracked.ts:117`、`按 |Δ| 降序`＝`reportDocScore.ts:133`、`目标设置`＝`reportDocTracked.ts:34`）。
- **预测族（01–20、31）与缺口页（29、30）的可见文本已被 #160 整族换掉**——产物串在当前源码里 **0 命中**
  （`体重外推（horizonDays`、`按当前速率预测未来日均摄入；需≥`、`模拟每天多减`、`预测摄入稳定性（σ ≤`、`达成判定＝日均偏离`、
  `缺口 = 消耗` 六串在 `packages/skill-calorie/src` ＋ `packages/base-render/src` 里逐条 `命中=0`）。
  ⇒ 第 3、4 节里这些页的「现文」是**产物原话**（判据仍是产物），但它们的**源码行号**只能给「该页当前源码里的对应行」，
  不能装作是产物那串的出处——凡属这一类的，逐处都写明「当前源码 0 命中」。

---

## 第 1 节 · 31 条产物 → 命令键 → 渲染源件 对照表

### 1.0 派生方式（怎么从命令键追到渲染件）

链路固定四段，逐段都有可复跑命令：

1. **唤醒词 → 命令键**：`src/analysis/routes.ts` 的声明表（`{ list:'wake'|'new', order, wakeWord, key, cli }`）。
   ```
   Select-String -Path packages\skill-calorie\src\analysis\routes.ts -Pattern 'order: 393|order: 331|order: 426|order: 7|order: 29'
   ```
2. **命令键 → 处理函数**：`src/analysis/commands.ts` 的声明表（`run:` 裸函数名；报告族 8 条是 8 个薄壳，见该件 `:314-316` 的注释）。
   ```
   Select-String -Path packages\skill-calorie\src\analysis\commands.ts -Pattern "key: 'calorie.view.predict'|key: 'calorie.view.deficit'|key: 'calorie.report."
   ```
3. **处理函数 → 取数件 ＋ 整页装配件**（本表「源文件:行」列）：
   - 预测族：`viewPredict`（`commands.ts:197`）按参数存在性分发到 `render/insightPlate.ts` 的 8 个 `build*View` ＋ `render/trendDocs.ts` 的 8 个 `build*Doc`；数据层 `analysis/simulate.ts`／`analysis/simulate2.ts`。
   - 缺口族：`viewDeficit`（`commands.ts:133`）→ `render/analysisPlate.ts:89` ＋ `render/trendDocs.ts:340`；数据层 `analysis/deficit.ts`。
   - 报告族：`viewReport`（`commands.ts:318`）→ `analysis/reportPlate.ts:263`（取数）＋ `analysis/reportDoc.ts:28`（整页装配）＋ 三个分片。
   ```
   Select-String -Path packages\skill-calorie\src\render\trendDocs.ts -Pattern '^export function build.*Doc'
   Select-String -Path packages\skill-calorie\src\render\insightPlate.ts -Pattern '^export function build'
   Select-String -Path packages\skill-calorie\src\analysis\reportPlate.ts -Pattern '^export function buildReportPlate'
   ```
4. **所有 31 页共用同一段页壳**（无一页例外）：`src/shared/docPage.ts:106` `assembleDocPage`
   → `base-paint`（＝`packages/base-render`，`package.json:2` 名字 `base-paint`）的
   `src/template.ts`（`fillTemplate`）＋ `src/blocks.ts:218`（`renderPageShell`，A线）／`src/blocks.ts` 的 B线老A壳（`docPage.ts:120-127`）
   ＋ `src/style.ts`（`buildStyleSheet`，含 `chartsCss` 于 `:668`）。
   两条线：**报告族 8 页走 B 线**（页头 meta-bar ＋ 右徽章「卡路里 · 报告」，`docPage.ts:109` 起）；**预测族与缺口族走 A 线**（`page-shell-eyebrow`＋H1）。

### 1.1 对照表（31 行）

族：预测＝预测权威族 20 条 ＋ 别名 1；缺口＝缺口族 2；报告＝报告族 8。

| 序 | 文件名 | 命令键（routes 逐字参数） | 产出该页 HTML 的源文件:行 | 族 |
|---|---|---|---|---|
| 01 | 01-预测体重(1周后).html | `calorie.view.predict --params {"horizonDays":7,"window":"14d"}` | 分发 `src/analysis/commands.ts:197`；取数 `src/render/insightPlate.ts:47`；整页装配 `src/render/trendDocs.ts:624`；算式 `src/analysis/simulate.ts:80,97`；页壳 `src/shared/docPage.ts:106` | 预测 |
| 02 | 02-预测体重(1月后).html | 同上，`"horizonDays":30` | 同 01（`horizonDays` 只进参数与文案） | 预测 |
| 03 | 03-预测体重(3月后).html | 同上，`"horizonDays":90` | 同 01 | 预测 |
| 04 | 04-预测体重(6月后).html | 同上，`"horizonDays":180` | 同 01 | 预测 |
| 05 | 05-预测体重(自定义时间).html | 同上，`"horizonDays":60` | 同 01 | 预测 |
| 06 | 06-预测体重(自定义目标).html | `--params {"target":65,"window":"14d"}` | 分发 `commands.ts:197`（target 分支 `:202`）；取数 `insightPlate.ts:79`；装配 `trendDocs.ts:669`；算式 `simulate.ts:103,123` | 预测 |
| 07 | 07-模拟减重(每天-300卡).html | `--params {"cut_kcal":300,"window":"14d"}` | 分发 `commands.ts:211`；取数 `insightPlate.ts:100`；装配 `trendDocs.ts:709`；算式 `simulate2.ts:21,40-42` | 预测 |
| 08 | 08-模拟减重(每天-500卡).html | 同上 `"cut_kcal":500` | 同 07 | 预测 |
| 09 | 09-模拟减重(每天-700卡).html | 同上 `"cut_kcal":700` | 同 07 | 预测 |
| 10 | 10-模拟减重(30天减Xkg).html | `--params {"target_loss":2,"days_target":30,"window":"14d"}` | 分发 `commands.ts:221`；取数 `insightPlate.ts:121`；装配 `trendDocs.ts:763`；算式 `simulate2.ts:50,68-69` | 预测 |
| 11 | 11-模拟减重(60天减Xkg).html | 同上 `4`／`60` | 同 10 | 预测 |
| 12 | 12-模拟减重(90天减Xkg).html | 同上 `6`／`90` | 同 10 | 预测 |
| 13 | 13-模拟减重(自定义天数减Xkg).html | 同上 `3`／`45` | 同 10 | 预测 |
| 14 | 14-摄入预测(按当前速率1周).html | `--params {"kind":"calorie_forecast","horizonDays":7,"window":"14d"}` | 分发 `commands.ts:234`；取数 `insightPlate.ts:146`；装配 `trendDocs.ts:817`；算式 `simulate2.ts:110,118,132` | 预测 |
| 15 | 15-摄入预测(按当前速率1月).html | 同上 `"horizonDays":30` | 同 14 | 预测 |
| 16 | 16-摄入预测(按当前速率3月).html | 同上 `"horizonDays":90` | 同 14 | 预测 |
| 17 | 17-摄入预测(自定义).html | 同上 `"horizonDays":60` | 同 14 | 预测 |
| 18 | 18-摄入预测(营养目标达成预测).html | `--params {"kind":"calorie_goal","window":"30d"}` | 分发 `commands.ts:243`；取数 `insightPlate.ts:167`；装配 `trendDocs.ts:869`；算式 `simulate2.ts:140` | 预测 |
| 19 | 19-摄入预测(卡路里缺口预测).html | `--params {"kind":"calorie_deficit","window":"30d"}` | 分发 `commands.ts:249`；取数 `insightPlate.ts:184`；装配 `trendDocs.ts:907`；算式 `simulate2.ts:158,162,168` | 预测 |
| 20 | 20-摄入预测(摄入稳定性预测).html | `--params {"kind":"calorie_stability","window":"30d"}` | 分发 `commands.ts:255`；取数 `insightPlate.ts:201`；装配 `trendDocs.ts:943`；算式 `simulate2.ts:175,186` | 预测 |
| 21 | 21-看BMI报告.html | `calorie.report.bmi --params {"window":"90d"}` | 分发 `commands.ts:318`（薄壳 `:371`）；取数 `src/analysis/reportPlate.ts:263`；整页装配 `src/analysis/reportDoc.ts:28`；区块 `src/analysis/reportDocBmi.ts:28`；共用 `reportDocParts.ts` | 报告 |
| 22 | 22-看TDEE报告.html | `calorie.report.tdee --params {"window":"30d"}` | 同上 + 区块 `src/analysis/reportDocTracked.ts:48` | 报告 |
| 23 | 23-看BMR报告.html | `calorie.report.bmr --params {"window":"30d"}` | 同上 + 区块 `src/analysis/reportDocTracked.ts:98` | 报告 |
| 24 | 24-看蛋白质摄入报告.html | `calorie.report.protein --params {"window":"30d"}` | 同上 + 区块 `src/analysis/reportDocTracked.ts:22` | 报告 |
| 25 | 25-看水分摄入报告.html | `calorie.report.water --params {"window":"30d"}` | 同上 + 区块 `src/analysis/reportDocTracked.ts:22`（与 24 同一函数，单位随目标切） | 报告 |
| 26 | 26-看综合评分.html | `calorie.report.score --params {"window":"30d"}` | 同上 + 区块 `src/analysis/reportDocScore.ts:19` | 报告 |
| 27 | 27-看健康趋势.html | `calorie.report.trend --params {"window":"90d"}` | 同上 + 区块 `src/analysis/reportDocScore.ts:66` | 报告 |
| 28 | 28-看健康报告(含对比).html | `calorie.report.compare --params {"window":"7d"}` | 同上 + 区块 `src/analysis/reportDocScore.ts:95` | 报告 |
| 29 | 29-查热量缺口.html | `calorie.view.deficit --params {"window":"7d"}` | 分发 `src/analysis/commands.ts:133`；取数 `src/render/analysisPlate.ts:89`；整页装配 `src/render/trendDocs.ts:340`；算式 `src/analysis/deficit.ts:12,70` | 缺口 |
| 30 | 30-看热量缺口.html | 同 29（`routes.ts:176` 别名 new7，同命令同参数） | 同 29（产物与 29 逐字节同、sha256 同，见 `验收清单.md:81`） | 缺口 |
| 31 | 31-看体重预测.html | `calorie.view.predict --params {"horizonDays":7,"window":"14d"}` | 同 01（`routes.ts:180` 别名 new29，参数与 01 逐字同） | 预测（别名） |

**族分布**：预测族 20 条（01–20）＋ 预测别名 1 条（31）＝ 21；报告族 8 条（21–28）；缺口族 1 条（29）＋ 缺口别名 1 条（30）＝ 2。合计 31。

**独立整页装配件只有 3 个**：`render/trendDocs.ts`（预测 8 个 `build*Doc` ＋ 缺口 `buildDeficitDoc`，共 21＋2 页）、
`analysis/reportDoc.ts`（报告 8 页）、外加它们共同调用的 `shared/docPage.ts:106`。

---

## 第 2 节 · 每页响应式现状（实测）

### 2.0 测法与可复跑命令

产物是 LF 单行流（`.scratch\t387\html\01-预测体重(1周后).html`：`len=62606`、`lines=2275`、`CRLF=0`、`<style>` 1 个、`<script>` 1 个），
故按「`<style>` 区 / `<script>` 区」分区计数，用的是引号感知的标签剥法（属性值里出现 `>` 不会截断）。

```powershell
# 逐页数 @media 并列出条件原文
$dir='D:\ilife\.scratch\t387\html'
Get-ChildItem $dir -Filter *.html | Sort-Object Name | ForEach-Object {
  $t=[IO.File]::ReadAllText($_.FullName)
  $m=[regex]::Matches($t,'@media[^{]*\{')
  "{0}`t{1}`t{2}" -f $_.Name, $m.Count, (($m | ForEach-Object { ($_.Value -replace '[{]$','').Trim() }) -join ' ; ') }
}
# 分区（style / script）归属
#   取 <style>/<script>/</style>/</script> 四个标记的位置，按 @media 的偏移判区
```

### 2.1 产物里实际出现的 @media 条件（按产物内出现次序，公共 14 条）

| # | 条件原文（产物逐字） | 产物区 | 源文件:行（该条件在源码里的唯一定义地） |
|---|---|---|---|
| 1 | `@media (max-width: 820px)` | `<style>` | `packages/base-render/src/style.ts:286`（`.ilife-toast-stack` 触屏档） |
| 2 | `@media (min-width: 821px)` | `<style>` | `packages/base-render/src/style.ts:317`（`.ilife-action-bar{max-width:none}`） |
| 3 | `@media (max-width: 820px)` | `<style>` | `packages/base-render/src/style.ts:336`（`.ilife-action-row-ghost`） |
| 4 | `@media (max-width: 820px)` | `<style>` | `packages/base-render/src/style.ts:371`（`.ilife-action-btn{min-height:44px}`） |
| 5 | `@media (max-width: 820px)` | `<style>` | `packages/base-render/src/style.ts:426`（`.ilife-copy-btn`） |
| 6 | `@media (max-width: 820px)` | `<style>` | `packages/base-render/src/style.ts:520`（`.ilife-copy-menu-item`） |
| 7 | `@media (max-width: 820px)` | `<style>` | `packages/base-render/src/style.ts:660`（`.ilife-error:has(> .ilife-error-title)`） |
| 8 | `@media (max-width:720px)`（注意：无空格） | `<style>` | `packages/base-render/src/charts.ts:2000`（`chartsCss`，函数头 `charts.ts:1942`；`mobile=720` 见 `charts.ts:228,1944`） |
| 9 | `@media (max-width: 640px)` | `<style>` | `packages/base-render/src/style.ts:1167`（`.ilife-help-shell` 收内距） |
| 10 | `@media (max-width: 400px)` | `<style>` | `packages/base-render/src/style.ts:1178`（`.ilife-help-shell-tab-bar`） |
| 11 | `@media (prefers-reduced-motion: reduce)` | `<style>` | `packages/base-render/src/style.ts:1183` |
| 12 | `@media (max-width: 640px)` | `<style>` | `packages/base-render/src/blocks.ts:1227`（`.ilife-block-page-shell` ＋ `-title`） |
| 13 | `@media print` | `<style>` | `packages/base-render/src/blocks.ts:1389`（`.ilife-page-printable` ＋ `@page printable`） |
| 14 | `@media (max-width: 640px)` | `<style>` | `packages/base-render/src/blocks.ts:1638`（`.ilife-block-data-table-*` 收字号内距） |
| 15（仅 29、30） | `@media (max-width:720px)` | `<script>`（图表 helpers 文本内） | 同 8 的同一函数 `charts.ts:2000`，经 `charts.ts:2053 buildChartsHelpersJs` 落进产物；页面侧由 `src/render/trendDocs.ts:363,387`（`charts=true`）与 `src/shared/docPage.ts:116` 触发 |

### 2.2 逐页实测表（31 行）

| 序 | 文件 | 产物 `@media` 条数 | 条件文本 | 与公共 14 条的差异 |
|---|---|---|---|---|
| 01 | 01-预测体重(1周后).html | 14（style 14／script 0） | 公共 14 条（次序 1–14） | 无 |
| 02 | 02-预测体重(1月后).html | 14／0 | 公共 14 条 | 无 |
| 03 | 03-预测体重(3月后).html | 14／0 | 公共 14 条 | 无 |
| 04 | 04-预测体重(6月后).html | 14／0 | 公共 14 条 | 无 |
| 05 | 05-预测体重(自定义时间).html | 14／0 | 公共 14 条 | 无 |
| 06 | 06-预测体重(自定义目标).html | 14／0 | 公共 14 条 | 无 |
| 07 | 07-模拟减重(每天-300卡).html | 14／0 | 公共 14 条 | 无 |
| 08 | 08-模拟减重(每天-500卡).html | 14／0 | 公共 14 条 | 无 |
| 09 | 09-模拟减重(每天-700卡).html | 14／0 | 公共 14 条 | 无 |
| 10 | 10-模拟减重(30天减Xkg).html | 14／0 | 公共 14 条 | 无 |
| 11 | 11-模拟减重(60天减Xkg).html | 14／0 | 公共 14 条 | 无 |
| 12 | 12-模拟减重(90天减Xkg).html | 14／0 | 公共 14 条 | 无 |
| 13 | 13-模拟减重(自定义天数减Xkg).html | 14／0 | 公共 14 条 | 无 |
| 14 | 14-摄入预测(按当前速率1周).html | 14／0 | 公共 14 条 | 无 |
| 15 | 15-摄入预测(按当前速率1月).html | 14／0 | 公共 14 条 | 无 |
| 16 | 16-摄入预测(按当前速率3月).html | 14／0 | 公共 14 条 | 无 |
| 17 | 17-摄入预测(自定义).html | 14／0 | 公共 14 条 | 无 |
| 18 | 18-摄入预测(营养目标达成预测).html | 14／0 | 公共 14 条 | 无 |
| 19 | 19-摄入预测(卡路里缺口预测).html | 14／0 | 公共 14 条 | 无 |
| 20 | 20-摄入预测(摄入稳定性预测).html | 14／0 | 公共 14 条 | 无 |
| 21 | 21-看BMI报告.html | 14／0 | 公共 14 条 | 无 |
| 22 | 22-看TDEE报告.html | 14／0 | 公共 14 条 | 无 |
| 23 | 23-看BMR报告.html | 14／0 | 公共 14 条 | 无 |
| 24 | 24-看蛋白质摄入报告.html | 14／0 | 公共 14 条 | 无 |
| 25 | 25-看水分摄入报告.html | 14／0 | 公共 14 条 | 无 |
| 26 | 26-看综合评分.html | 14／0 | 公共 14 条 | 无 |
| 27 | 27-看健康趋势.html | 14／0 | 公共 14 条 | 无 |
| 28 | 28-看健康报告(含对比).html | 14／0 | 公共 14 条 | 无 |
| 29 | 29-查热量缺口.html | **15**（style 14／script 1） | 公共 14 条 ＋ `<script>` 内 `@media (max-width:720px)` | ＋1 条（图表 helpers 文本，见 2.1 #15） |
| 30 | 30-看热量缺口.html | **15**／1 | 同 29 | 同 29（与 29 逐字节同） |
| 31 | 31-看体重预测.html | 14／0 | 公共 14 条 | 无 |

### 2.3 对派单给定线索的复核

派单线索原文：「整个 `packages/skill-calorie/src/render/` 里带 `@media` 的只有
`trendDocs.ts`(1)／`pageChromeCss.ts`(1)／`reviewDocsCss.ts`(1)／`planWizardCss.ts`(1)／`workoutPlanCss.ts`(4)／`workoutPlanLook.ts`(1)」。

**复核结论：文件面成立，计数口径不成立（要订正两处）。** 复跑命令与读数：

```powershell
Get-ChildItem -Recurse D:\ilife\packages\skill-calorie\src -File -Include *.ts |
  ForEach-Object { Select-String -LiteralPath $_.FullName -Pattern '@media' }
```

- **成立**：`packages/skill-calorie/src/render/` 下出现 `@media` 字样的文件**恰好这 6 件**，没有第 7 件（该目录共 30 个 `.ts`）。
- **订正一（按代码算，不按字样算）**：`workoutPlanCss.ts` 的 4 处里，`:20` 与 `:163` 是**注释**，
  真正的规则只有 2 处——`:134`（`@media print`）与 `:165`（`@media (max-width:820px)`）。
- **订正二**：`workoutPlanLook.ts` 的 1 处（`:22`）也是**注释**，该件**没有** `@media` 规则（代码 0 处）。
- 另记（不在 `render/` 但在本包）：`weight/weightUi.ts:111`、`diet/rankingDocs.ts:172`、`diet/libraryDocs.ts:86`、`body/bodyDocs.ts:191` 也各带 `@media`。

### 2.4 与本 31 页直接相关的两条实测事实

1. **`render/` 里那 6 件的 `@media` 一条都没进这 31 页**。三条判据串在 31 件里全部为 0 命中：
   `ilw-hit`（`trendDocs.ts:538` 的 `CONTRA_MOBILE_CSS` 钩子类）= 0／31；
   `ilife-block-page-shell{box-sizing`（`pageChromeCss.ts:28` 的头一条规则）= 0／31；
   `ilw-wkr`（`workoutPlanCss.ts` 的件类）= 0／31。
   ⇒ 这 31 页的响应式**全部来自 `packages/base-render`**（10 条 style.ts ＋ 1 条 charts.ts ＋ 3 条 blocks.ts），
   页面自己**没有一段页内 CSS**（31 件各只有 1 个 `<style>`，且它就是 `buildStyleSheet().css + blocksCss()`）。
2. **每页都白带了一份图表 CSS**：`buildStyleSheet()` 无条件拼进 `chartsCss`（`packages/base-render/src/style.ts:668`），
   所以连**一页图表都没有的预测族**（`<svg>` 数 = 0／21 件）也各带 3092 字节的 `/* charts */` 段（含那条 `≤720px` 媒体查询）。
   实测：`ilife-charts` 串在 21 件预测页各出现 69 次、`<svg>` 0 次；报告族 21–28 是**静态 SVG**（`<svg>` 1–5 个，**无**图表 helpers `<script>`）；
   只有 29／30 另带运行时 helpers（第 15 条 `@media` 就在它里面）。
