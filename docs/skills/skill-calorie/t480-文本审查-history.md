# t480 · 场景 03 体重 · 明细／曲线族（18 词）文本审查：删冗余·改人话·换形态（证据件）

票：**#480**（场景 03 体重 × 文本审查，第 3／4／6／11／12／18／19／25／26／32／33／37／38／41／42／45／47／49 条）。
任务三条＝① 百分百冗余的必删；② 看不懂的专业话改人话（读者＝不懂技术的项目负责人）；③ 该换展示形式的换形态（只用已有公共件）。
统一口径正本：`.scratch/t154/text-review/口径.md`（§3.1 页脚句式、§3.2 术语替换表、§3.3 数字写法、§二 五条红线）。

**写集（实际碰到的件，第五步对账）**

| 件 | 动作 | 说明 |
|---|---|---|
| `packages/skill-calorie/src/weight/history.ts` | 改（683 → **742** LF，无 BOM／LF） | 18 页共用的取数＋装配一件：删冗余、改人话、换形态全落在这里 |
| `packages/skill-calorie/test/weight-history-333.test.mjs` | 改（147 → **225** LF） | 旧断言随文本收紧（旧句命中数必须为 0）＋新增八条判据（见 §四） |
| `docs/skills/skill-calorie/t480-文本审查-history.md` | 新增 | 本证据件 |
| `.scratch/t154/text-review/b480/**` | 新增 | 本地出页脚本、逐页对照、变异电池、探针与日志 |

只读未碰：`plate.ts`／`figures.ts`／`records.ts`／`shared/*`／`packages/base-render/*`／`packages/skill-calorie/AGENTS.md`／
`test/weight-wake-58-339.test.mjs`／`test/render-t41.test.mjs`／`.scratch/t154/delivery/`／眉标／第 48 条整页／复制区形态与深色 toast。
**必报五步第四步**：`src/weight/history.ts` **已超线（742 行 > 350），需要根据规则进行重构。** 超因＝「看体重明细」与「看体重曲线」在命令面上是同一个命令，
两条子功能（取数、曲线计划、整页装配、复制载荷）共用一件；本票只做可见文本，不拆（拆分不在本票写集）。拆法照 `docs/skills/skill-calorie/t154-体重页面-老新融合规范.md` §9 第 1 条：
`history.ts` → `historyCurve.ts`（曲线计划与量程）／`historyRows.ts`（明细分段与标签分布）／`historyPage.ts`（整页装配与复制区），台账行由编排者统一同步。

---

## 一、逐页表（18 页逐条过）

**本族 18 页共用同一批槽位**，因此先列「逐页都改的六处」的逐字对照，再逐页给该页特有项与结论。
判定用的三个判据代号：**冗**＝百分百冗余（同一屏同一条事实两处，删一处读者不丢信息）；**难**＝看不懂的专业话；**形式**＝该换展示形式。

### 1.0 逐页共有的六处（逐字）

| # | 槽位 | 现文本（改前，逐字） | 判据 | 判定 | 改后文本（逐字） |
|---|---|---|---|---|---|
| A | 副标题 | `模式：明细／曲线｜2026-08-09 ~ 2026-09-07（共 30 条）` | 冗 | 删 | `模式：明细／曲线`（窗口 ＋ 条数在页题、KPI 卡说明、页脚各仍有一份；原先同一屏印 4 遍） |
| B | 变化卡说明 | `30 天 · 日均 0.01 kg` | 难 | 改 | `30 天 · 每天 +10 克`（口径 §3.2：全族统一「克」，不出现 `g/天`，不两种单位并存） |
| C | 均值卡说明 ＋ 徽章 | `首 70.1 → 末 70.4 kg` ／ 徽章 `首末对照` | 难 | 改 | `首日 70.1 kg → 末日 70.4 kg` ／ 徽章 `首日 → 末日`（§3.2「首日与末日对比」） |
| D | 曲线图例 | `– – 7 天均线 图上那条灰色虚线（按窗口 7 点现算）` | 难＋冗 | 改＋删 | `– – 均线（最近 7 天） 图上那条灰色虚线`（「按 7 点现算」是实现细节上屏，删） |
| E | 表注 | `体重历史 2026-08-09 ~ 2026-09-07（共 30 条）` | 冗 | 删＋改 | `体重明细（共 30 条）`（窗口串在页题／卡说明／页脚已有；长表仍是 `体重明细（最近 30 条）`／`其余 N 条`） |
| F | 结论块 | 一长句：`2026-08-09 ~ 2026-09-07 共 30 条，首 70.1 → 末 70.4 kg（+0.3 kg，日均 0.01 kg，30 天）；均值 70.3 kg。` | 形式 | 换 | 一句话 ＋ 并列小标签（公共件 `renderChips`）：`这段累计涨了 0.3 kg。` ＋ 小标签 `每天 +10 克`、`首日 70.1 kg → 末日 70.4 kg`、`均值 70.3 kg`（结论块仍恰一处，标题仍只有「结论」两个字） |
| G | 页脚来源行 | `📊 数据来源:calorie_data.db · weight_log · 2026-08-09 ~ 2026-09-07 · 共 30 条` | 难＋冗 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-08-09 ~ 2026-09-07 ｜ 共 30 条`（口径 §3.1：全角冒号、三段 `｜`；`weight_log` 只留在复制日志第 3 段，不上屏） |
| H | 图表刻度格式 | `format: v => round1(v) + 'kg'` → 图上刻度 `70.4kg` | 难 | 改 | `70.4 kg`（§3.3：数字与单位之间留一个空格，值槽里也一样） |

### 1.1 逐页（第几条／唤醒词）

| 页（第几条／唤醒词） | 现文本（逐字，该页特有项） | 判据 | 判定 | 改后文本（逐字） | 依据 |
|---|---|---|---|---|---|
| **03 看本周体重**（单点窗） | 图例首行 `— 体重曲线 2026-09-07`；结论 `2026-09-07 只有 1 条记录（70.4 kg），单点看不出变化，再记一条就能比较；均值 70.4 kg。` | 冗／形式 | 改 | 图例首行 `— 体重曲线`（窗口串不再重印）；结论 `本窗只有 1 条记录，单点看不出变化，再记一条就能比较。` ＋ 小标签 `只有 1 天有记录`、`均值 70.4 kg` | 窗口串在本页原本出现 6 处；结论里那串「（70.4 kg）」是 4 张卡之外的第 3 遍 |
| **04 看体重曲线**（30 天） | 上述 A–H 全中 | 冗／难／形式 | 删／改／换形态 | 见 1.0 | 页头副标题、图例、表注、页脚四处在说同一件事 |
| **06 看「有备注」的体重记录** | 副标题 `模式：备注筛选｜窗口（共 5 条）`；第四卡徽章 `已筛备注`；图例行 `✎ 只看有备注的记录 共 5 条`；表注尾 `·有备注`；结论尾 `只看有备注的 5 条` | 冗 | 删／改 | 副标题只留 `模式：备注筛选`；第四卡徽章改 `标签 4 类`；图例行整行删；表注去 `·有备注`；结论里那句删 | 「只看有备注的 5 条」原先在副标题、次卡、图例、表注、结论说了 5 遍；现在卡片说明与页脚各一处（表注的窗口串与页脚重复，见 1.0-E） |
| **11 看上周体重**（7 天） | 上述 A–H 全中 | 冗／难／形式 | 删／改／换形态 | 见 1.0 | 与 04 同形 |
| **12 看体重曲线（带目标）** | 图例 `- - 目标线 目标 68 kg（量程外，改画文字）`＋`→ 距目标还差 2.4 kg 目标 68 kg`；距目标卡徽章 `还差 2.4 kg`；图上画线标签 `目标 68kg` | 难＋冗 | 删／改 | 图例一行 `- - 目标线 目标 68 kg（超过图的取值范围，没画）`；`→` 那行整行删（差数已在 KPI 卡值槽与结论小标签）；距目标卡徽章改 `未达目标`（值槽已写 `+2.4 kg`）；图上标签 `目标 68 kg` | 「量程」是读者看不懂的实现词；「距目标还差」原先说 3 遍；徽章复述值槽＝同一事实第三处 |
| **18 看本月体重**（7 天） | 上述 A–H 全中 | 冗／难／形式 | 删／改／换形态 | 见 1.0 | 与 04 同形 |
| **19 看体重曲线（带里程碑）** | 里程碑卡说明 `门槛 减重 5kg／10kg · 减重 5kg 那天 2025-11-27；减重 10kg 那天 2026-06-20`；图例 `◆ 减重 5kg 那天 2025-11-27 75kg（窗口外）`；图上竖线标签 `减重 5kg 那天 75kg`；结论尾 `里程碑达成 2／2：减重 5kg 那天 2025-11-27；…` | 冗＋难 | 删／改 | 里程碑卡说明 `门槛 减重 5 kg／10 kg · 达成 2 个`（日期交给图例与结论小标签）；图例 `◆ 减重 5 kg 那天 2025-11-27 75 kg（窗口外）`；图上标签同；结论小标签 `里程碑达成 2／2` | 日期原先在卡片、图例、结论三处逐字重复；`5kg` → `5 kg`（§3.3）；图例里「窗内已标竖线」是渲染动作，在窗内时不写 |
| **25 看上月体重**（31 天，长表） | 表注 `体重明细（最近 30 条）`／`体重明细（其余 1 条）` | 冗 | 改 | `体重明细（最近 30 条）`／`体重明细（其余 1 条）` 保留（分段说明不是重复）；其余同 1.0 | 分段表注自带口径，保留 |
| **26 看体重曲线（带异常点）** | 异常点卡说明 `2026-09-03 70.4kg；2026-09-04 70.4kg；…（5 条全列）`；图例 `▲ 异常点 2026-09-03 70.4kg（偏 0.13，图上已染红）`×5；结论尾 `异常点 5 个：…（偏 0.13）`×5；图例首行 `— 体重曲线 窗口` | 冗＋难＋形式 | 删／改／换形态 | 卡说明 `2026-09-03 70.4 kg；2026-09-04 70.4 kg；2026-09-05 70.4 kg 等 5 个`（前 3 条 ＋ 计数）；图例 `▲ 异常点 2026-09-03 70.4 kg（比平均线高 0.1 kg）`（最多 5 行，另见下方明细表；无符号位重印）；结论小标签 `异常点 5 个`；图例首行 `— 体重曲线 偏红的点＝异常点` | 同一份清单原先印 3 遍（卡说明／图例／结论）；`偏 0.13` 没说相对谁、也没单位；「图上已染红」是渲染动作 |
| **32 看最近 7 天体重** | 上述 A–H 全中 | 冗／难／形式 | 删／改／换形态 | 见 1.0 | 与 04 同形 |
| **33 看本月体重曲线** | 上述 A–H 全中 | 冗／难／形式 | 删／改／换形态 | 见 1.0 | 与 04 同形 |
| **37 看最近 90 天体重**（90 天，长表） | 变化卡 `90 天 · 日均 0 kg` | 难 | 改 | `90 天 · 首日到末日没变化`（持平不编速率句） | 原句「日均 0 kg」既看不出方向也没有可读性 |
| **38 看上月体重曲线**（31 天，长表） | 上述 A–H 全中 | 冗／难／形式 | 删／改／换形态 | 见 1.0 | 与 04 同形 |
| **41 看某段时间体重** | 上述 A–H 全中 | 冗／难／形式 | 删／改／换形态 | 见 1.0 | 与 04 同形 |
| **42 看最近 90 天体重曲线**（90 天，长表） | 变化卡 `90 天 · 日均 0 kg` | 难 | 改 | `90 天 · 首日到末日没变化` | 同 37 |
| **45 看最近 180 天体重曲线** | 变化卡值 `-2 kg`（一位小数与其它读数槽不一致） | 形式 | 改 | `-2.0 kg`（差值恒一位小数，与 `±0.1` 级读数同形） | §3.3 百分数与读数位数统一；看图发现「-2 kg」与「-0.1 kg」两种写法并存 |
| **47 看最近 365 天体重曲线** | 上述 A–H 全中 | 冗／难／形式 | 删／改／换形态 | 见 1.0 | 与 04 同形 |
| **49 看某段时间体重曲线** | 上述 A–H 全中 | 冗／难／形式 | 删／改／换形态 | 见 1.0 | 与 04 同形 |

**逐页判定**：18 页全部「改」（无「本页无问题」页）——本族是同一件装配出的 18 张读法，页头／卡片／图例／表注／结论／页脚六个槽位逐页同形，六处冗余与专业话逐页都在。

---

## 二、删掉的信息登记（不许丢）

| # | 删掉的文本（逐字） | 原住处 | 这条事实现在还在哪 | 为什么删 |
|---|---|---|---|---|
| 1 | `｜2026-08-09 ~ 2026-09-07（共 30 条）` | 副标题（18 页） | 页题 `<h1>体重历史 …`、KPI 卡说明 `本窗 …`、页脚来源行 `｜ 窗口 … ｜ 共 N 条` | 同一屏第 4 遍 |
| 2 | `体重历史 2026-08-09 ~ 2026-09-07` 里的窗口串 | 单段表注（18 页） | 页题、KPI 卡说明、页脚 | 同上；表注保留「共 N 条」这一个真信息 |
| 3 | `·有备注` | 表注（06 页） | 副标题 `模式：备注筛选`、KPI 卡说明 `（只取有备注的）`、页脚 `（只取有备注的）` | 第 4 遍 |
| 4 | `✎ 只看有备注的记录 共 5 条` 整行 | 曲线图例（06 页） | KPI 卡值槽 `5 条` ＋ 副标题模式词 | 图例是「图上有什么线」的地方，不是筛选条件的第 3 处 |
| 5 | `（按窗口 7 点现算）` | 曲线图例（16 页） | 图例主栏已写 `均线（最近 7 天）` | 实现细节（怎么算的），读者零收益 |
| 6 | `（量程外，改画文字）` ＋ `→ 距目标还差 2.4 kg 目标 68 kg` 整行 | 曲线图例（12 页） | KPI 卡值槽 `+2.4 kg`、结论小标签 `目标 68 kg，还差 2.4 kg` | 「量程」是专业话；差数原先说 3 遍 |
| 7 | `（窗内已标竖线）` | 里程碑图例（19 页） | 图上那条竖线本身 | 渲染动作，读者看图即知 |
| 8 | `（偏 0.13，图上已染红）` | 异常点图例（26 页） | 改后写 `（比平均线高 0.1 kg）`；「已染红」由红点本身承担 | 没说相对谁 ＋ 渲染动作 |
| 9 | 结局小标签里逐字列 5 个异常点日期 | 结论（26 页） | 图例 5 行 ＋ 下方逐日明细表 | 同一清单第 3 遍 |
| 10 | `日均 0.01 kg` 这一整类写法 | 变化卡（18 页） | 改后 `每天 +10 克`（信息量更大：带方向与克） | 口径 §3.2 要求换人话，且原写法方向感弱 |
| 11 | `首 70.1 → 末 70.4 kg` / `首末对照` | 均值卡（16 页） | 改后 `首日 70.1 kg → 末日 70.4 kg` / `首日 → 末日` | 术语替换，不是丢信息 |
| 12 | 页脚 `calorie_data.db · weight_log`（裸库表名） | 页脚（18 页） | 改后仍在括号里 `体重记录（calorie_data.db · weight_log）`；复制日志第 3 段逐字保留原句 | 加读者词，不删技术原文 |
| 13 | 结论正文里的 `共 N 条` ＋ `均值 X kg`（合并进小标签） | 结论（18 页） | 同一结论块里的小标签 `均值 70.3 kg`；条数在卡值槽 | 一长句拆成一句 ＋ 小标签，事实一条不少 |

**没删的地基（红线自检）**：口径句（空态句自带口径）、结论块一页恰一处（标题仍只有「结论」两个字，小标签与它同块）、
页脚来源行（库／窗口／条数三段齐全，缺口仍有话）、复制三格式载荷（`text`／`json`／`csv` 与页面数字逐个对得上）、
复制区形态与深色 toast、眉标（`CALORIE.VIEW.* · 运动身体域` 原样，等用户裁定）——**六条一条未动**。

---

## 三、机器读数

**提交**：`ddcaf0d2d20b7db4f45082a6341a138c0fd82081`（`feat(480): 场景03 体重 明细／曲线族 18 页 文本精简·人话改写·展示升级（删四处窗口/条数重印）`，
按路径点名提交：`history.ts`／`weight-history-333.test.mjs`／本证据件；已 `git push`，`549f4bc..ddcaf0d master`）。
四席共用一个索引：本次 `git add` 后暂存区里出现别席在途件 `docs/skills/skill-calorie/t483-文本审查-receipt.md`，
按 §3.2 第 1 条**把别席件从暂存区摘出**再提交（`git restore --staged` 只动索引，那份工作区内容一字未动、未丢）。

**导出（`node .scratch/t154/text-review/render-family.mjs --family history --out .scratch/t154/text-review/out-history`）**：
`TALLY {"通过":18} TOTAL 18 HTML=18`、末行 `OK-RENDER D:\ilife\.scratch\t154\text-review\out-history`；
18 页字节合计 **2,329,025**（单页 74,240 ~ 437,425，逐页读数见 `out-history/result.json`）。

**门禁与用例（每条都经 `node tooling/run-locked.mjs --ticket 480 -- …`；本窗运行条目导出件 = `.scratch/t154/text-review/b480/gate-runs-480.txt`，45 条）**

```
RESULT: ticket=480 runId=fedb8304-9da7-4777-bd86-157d3256e27f waitedMs=40041 exit=0     ← 靶向用例
  RESULT: 18/18（三只用例：18 词逐条真跑／备注空窗 exit 4 不落盘／旧口径 days 与显式起止）
RESULT: ticket=480 runId=20ef66d8-8db6-45a7-ab8c-3eecf5f010b3 waitedMs=1 exit=0         ← 变异电池（持锁）
RESULT: ticket=480 runId=f7c46a4f-954b-487a-a93a-6c5b06806243 waitedMs=0 exit=0         ← tsc -b packages/skill-calorie
RESULT: ticket=480 runId=e94f8595-c9e1-4071-af56-5d9865850c3f waitedMs=20021 exit=0     ← tsc（异常点口径那轮）
RESULT: ticket=480 runId=d4757902-81fa-4e7a-8519-80fc8ec041f2 waitedMs=0 exit=0         ← tsc（首轮）
```

**变异两行（`.scratch/t154/text-review/b480/mutations.mjs`，一次锁里跑完 5 条 ＋ 逐条还原；日志 `b480/log-mutations.log`，
runId=`20ef66d8-8db6-45a7-ab8c-3eecf5f010b3`）**

```
BASELINE 改前（改动已在树里） exit=0 RESULT=18/18
MUTATION M1 副标题重印窗口与条数   改坏 exit=1 RESULT=(无 RESULT 行)｜改回 exit=0 RESULT=18/18｜还原逐字节一致=true
MUTATION M2 日均改回 kg 说法       改坏 exit=1 RESULT=(无 RESULT 行)｜改回 exit=0 RESULT=18/18｜还原逐字节一致=true
MUTATION M3 图例重印现算细节       改坏 exit=1 RESULT=(无 RESULT 行)｜改回 exit=0 RESULT=18/18｜还原逐字节一致=true
MUTATION M4 异常点偏离改回旧写法   改坏 exit=1 RESULT=(无 RESULT 行)｜改回 exit=0 RESULT=18/18｜还原逐字节一致=true
MUTATION M5 表格标题重印窗口       改坏 exit=1 RESULT=(无 RESULT 行)｜改回 exit=0 RESULT=18/18｜还原逐字节一致=true
MUTATION-READING 红=5 条（改坏必红）｜绿=6 次（含基线＋逐条还原必绿）
RESTORE-OK 逐字节比对 true（packages/skill-calorie/src/weight/history.ts）
FINAL-GREEN exit=0
```

**别席在途件的类型错（不阻断本票，如实记账）**：变异电池一轮里，`tsc -b packages/skill-calorie` 报
`src/photo/helpDoc.ts(65,7) TS2322`（照片域，别席在途件）。变异电池只判**本件**（`weight/history.ts`）的类型错，
用例吃已落盘的 `dist`，故该错不影响本票读数；**全包编译回绿要等别席落完**（本票的 `tsc` 三次绿读数发生在那件改动之前）。

**新判据（`test/weight-history-333.test.mjs` 里 18 页循环内，逐页过）**：
① 页脚来源行统一句式（全角冒号 ＋ 三段 `｜`）恰 1 处；② 全窗不编缺口句；③ 旧句零命中
（`📊 数据来源:`／`日均 0.01 kg`／`首末对照`／`量程外`／`基线 `／`黄±`／`红±`／`weight_log ·`／`窗内 `／`本窗一条记录都没有`／`按窗口 7 点现算`）；
④ 窗口区间串可见文本里 ≤3 处（单点窗 ≤6，那天本身还出现在数据行与页顶提示）；⑤ 不印「日均 N kg」；
⑥「结论」恰 1 处且结论块内有并列小标签；⑦ 数字与单位之间留空格；⑧ 结论正文恰一句。

---

## 四、未做项与下一手缺什么

1. **名册口径**：`calorie.view.weight-history` 的四个 overlay 读法（新增／替换）与「看体重曲线」命名收口**不在本票**（本票只动可见文本）。
2. **生成物重落**：`pnpm gen`／`gen:check` 与告警线台账（`packages/skill-calorie/AGENTS.md`）由**编排者统一同步**；本票只报一句（`history.ts` 683→742 LF，已超线，拆法见文件头）。
3. **同一口径的邻居**：里程碑标签 `减重 Nkg 那天` 也由 `src/weight/weightCompare2.ts:154` 生成（对比族页面在用），本票按 §3.3 只在 `history.ts:178` 侧改成 `减重 N kg 那天`；**对比族那一份与 `routes.ts` 的唤醒词原句**（`对比体重：减重 5kg 那天 vs 今天`）要不要一起加空格，请编排者裁定（唤醒词是触发器原文，本票不动）。
4. **波动页的「基线／黄±／红±」**：`src/weight/volatility.ts` 的口径串（`基线 … 黄± … 红± …`）属 B4 波动族，本票只改本文件里引用它时自己拼的那句（改后 `平均线 … 注意线 ±… kg，警戒线 ±… kg`）；**同一屏两句口径要不要同形**归 B4。
5. **看图未覆盖的部分**：长表（90／180／365 天）只抽看了 45 号页（两张分段表、无压字）；其余长页以机器读数（表注逐字对照）代替眼睛。
6. **回执（给编排者）**：见本次消息，格式按 `docs/subagent-concurrency-protocol.md` §5.1（判定／机器证据／路径／未做项）。
7. **草稿件在忽略清单里**：`.scratch/` 全目录被 `.gitignore:5` 忽略 ⇒ 本节引用的脚本与日志（`b480/*.mjs`／`log-*.log`／
   `pages-对照.md`／`gate-runs-480.txt`／`out-history/`／`shots-history/`）**不入版本库**，只在工作区备查；入版本库的可复跑件是本证据件引到的三条命令本身。

---

## 五、对抗审查整改（第二轮：按 `fix-brief-history.md` 的 15 条缺陷 ＋ 裁定 F 逐条改）

独立审查席判 **FAIL（83 分）**，缺陷单：① 一条跨族统一裁定（页脚删库表名）＋ ② 15 条逐缺陷。本轮**只按单改**（不扩大范围），改前逐字一律取自 `.scratch/t154/text-review/after-history/*.txt`（18 页本轮起点）。

### 5.1 裁定 F · 页脚只留人话来源，库表名退出可见面

| 面 | 改前 | 改后 |
|---|---|---|
| 页脚（人面） | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 … ｜ 共 N 条` | `📊 数据来源：体重记录 ｜ 窗口 … ｜ 共 N 条` |
| 复制日志第 3 段（机器面） | 与页脚**共用**同一句（`sourceTextOf` 一份措辞两处用） | 照旧写库文件名 ｜ 来源（§5.5） |

改法照裁定原文：把共用的那句**拆成两句**——`sourceTextOf`（日志，含 `DB_FILENAME · weight_log`）与 `sourceLine`（页脚，只说「体重记录」）。

### 5.2 逐缺陷（15 条，改前 → 改后逐字）

| # | 严重度 | 页（实测命中） | 改前逐字 | 改后逐字 |
|---|---|---|---|---|
| 1 | S2 | 04／12／19／26 | 结论块小标签 `每天 +10 克`／`首日 70.1 kg → 末日 70.4 kg`／`均值 70.3 kg`（与变化卡／均值卡逐字重复） | **三枚全删**；结论块只留正文那一句 ＋ 标注类小标签（目标／里程碑／异常点） |
| 2 | S2 | 备注卡（14 页） | 副说明 `标签 4 类` ＋ 徽章 `标签 4 类` | 副说明删；徽章 `标签 4 类` |
| 3 | S2 | 03 | 变化卡副说明 `单点无变化`／均值卡副说明 `单点无均值对照`／备注卡副说明 `标签 1 类`；页顶提示 `…再记一条就能比首末。` | 变化卡副说明 `只有 1 天记录，没法算变化`（徽章仍 `单点无变化`）／均值卡副说明删（徽章 `无对照`）／备注卡副说明删（徽章 `标签 1 类`）／提示 `…再记一条就能比首日和末日。` |
| 4 | S2 | 19 | 里程碑卡副说明 `减重 5 kg／10 kg 两档 · 已达成 2 个`（末字 `kg／` 悬空）／徽章 `达成 2 个` | 副说明 `减重 5 kg、10 kg 两个里程碑都达到了`／徽章 `已达成`／值槽仍 `2 个` |
| 5 | S2 | 26 | 异常点卡副说明＝整张清单 ＋ `等 5 个`（换行截断）／徽章 `异常 5 个` | 副说明 `2026-09-03 起连续 3 天偏高`（按当刻数据取头三天）／徽章 `偏高`／值槽仍 `5 个` |
| 6 | S3 | 26（本族只这一页有异常点图例） | `▲ 异常点 2026-09-03 70.4 kg（比平均线高 0.1 kg）` | `（比平均线高 0.13 kg）`——两位小数，与旧句「偏 0.13」同精度 |
| 7 | S3 | 12 | 距目标卡值槽 `+2.4 kg` | `还差 2.4 kg`（负差 `已低于目标 N kg`、零差 `已达目标`） |
| 8 | S3 | 25／37／38／42／45／47（分段头表） | `体重明细（最近 30 条）` | `体重明细（只列最近 30 条）` |
| 9 | S3 | 25／37／38／42／45／47（续表） | `体重明细（其余 335 条）` | `体重明细（更早的 335 条）` |
| 10 | S3 | 19 | `◆ 减重 5 kg 那天 2025-11-27 75 kg（窗口外）` | `◆ 减重 5 kg 那天 2025-11-27，75 kg（不在这段时间里）` |
| 11 | S3 | 12（本族只这一页带目标线） | `- - 目标线 目标 68 kg（超过图的取值范围，没画）` | `- - 目标线 目标 68 kg，还差 2.4 kg（图上画不下，没画）`（画得下时不重复「还差」） |
| 12 | S3 | 26 | 图例首行 `— 体重曲线 偏红的点＝异常点` | `- - 异常点 比平均线高的那几天` |
| 13 | S3 | 单表页（03／06／11／18／32／33／41／49／04／12／19／26） | `体重明细（共 N 条）` | `明细记录（共 N 条）` |
| 14 | S3 | 全 18 页 | 徽章 `样本 30 条`／`样本 180 条`；单点页 `单点数据` | 徽章 `共 30 条`／`共 180 条`；单点页 `只有一条` |
| 15 | S3 | 19 | 副说明末 `kg／` 悬空 | 随 #4 一并消除（新句无悬空单位） |

**实现要点（三条不是改字符串、是真的改了算法）**：
① #5「按当刻数据取头三天，不要写死」⇒ 新函数 `anomalyHeadHuman(list)`：头三天**连号**才说 `某日起连续 3 天偏高`，不连号只点到首日（本窗实测 09-03…09-07 连号 ⇒ 走连续那一支）。
② #6「只有一位有效位就写略高」⇒ `deviationHuman` 取两位（新 `round2`）；`偏差 < 1 kg` 且次位为 0 时说 `比平均线略高／略低`（本窗实测 0.13 ⇒ 走数值那一支）。
③ #1／#2／#3／#4／#5 的「删副说明」⇒ 本件 `KpiCard.detail` 改**可缺省**（`base-paint` 的 `KpiCardInput.detail` 本就可选、`optText` 不给就不出这一格），删掉的那五处在 DOM 里**整格不渲染**，不是留一行空白。

**没按单改的两处（口径冲突，取更具体的那条，如实记账）**：
① **#8 与 #13 对同一串冲突**：#8 说「其余页统一 `体重明细（共 N 条）`」，#13 说这 8 页的 `体重明细（共 30 条）` 改 `明细记录（共 N 条）`（理由：主标题已有「体重历史」）。两条都是逐字改法 ⇒ 取 #13（对同一串更具体、且是后一条），单表页落 `明细记录（共 N 条）`；#8 的页列里 37／42 实测是**分段表**（有头表 ＋ 续表），它们的头／尾按 #8／#9 逐字落 `体重明细（只列最近 30 条）`／`体重明细（更早的 N 条）`。
② **#9／#11 的页列与实测不符**：#9 点 25／38／47，实测有续表的另有 37／42／45（`其余 N 条` 六页都有）⇒ 规则照做、六页全改；#11 点 04／12／19／26／37／42／45／47，实测**只有 12** 带目标线（其余七页没有 target overlay，页上根本没有目标线）⇒ 规则照做，命中面＝12。

### 5.3 自证（本轮）

**补断言（`test/weight-history-333.test.mjs`，18 页循环内逐页过）**：审查席点名上一轮的自证「旧句命中 0」漏了 `样本`／`门槛` ⇒ 本轮 `OLD_TEXTS` 增补 `样本`／`门槛`／`calorie_data.db`／`weight_log`／`单点数据`／`单点无均值对照`／`偏红的点`／`超过图的取值范围`／`窗口外`／`体重明细（共 `／`体重明细（最近 30 条）`／`体重明细（其余 `，并新增四条：
⑨ **同卡跨槽重复 0**（缺陷单 §三.1 点名要钉的五处）：把四张卡的四槽从 HTML 里切出来逐卡比——副说明与徽章不许逐字相同、别的槽不许重复值槽、除「体重历史」卡外徽章不许复述值槽（`达成 2 个`／`异常 5 个` 那种；体重历史卡的 `共 N 条` 是缺陷 14 点名要的写法，单列豁免）；
⑩ 徽章 `共 N 条`／单点页 `只有一条`；⑪ `标签 N 类` 全页最多一次（只在徽章）；⑫ 表标题三种形态逐字。
另：⑥ 原断言「结论块里必须有并列小标签」在缺陷 1 落地后**必须改**（无标注的页面结论块本就该没有小标签）⇒ 改成「结论块不重复变化卡／均值卡的说明槽 ＋ 不含速率小标签」；① 页脚断言改人话句，并**加一条机器面断言**（复制日志第 3 段仍含 `体重记录（calorie_data.db · weight_log） ｜ 窗口 …`，库表名是退出可见面、不是删除）。

| 读数 | 值 |
|---|---|
| 编译（持锁） | `runId=ed291cab-8648-4d31-a935-d3c01b1aa2f3` exit=0（改完首编）；改回变异后重编 `runId=e68f4b28-614b-4629-af79-3c82fc6df778` exit=0 |
| 靶向用例（持锁） | `runId=d368782b-3b89-4f0e-98cd-8b71bc564f5f` exit=0，`RESULT: 18/18`（三只用例全绿）；回绿复跑 `runId=2845b1ed-2902-4802-b7ea-e6a9e9c174bd` exit=0，`RESULT: 18/18` |
| 变异「`样本` 放回」（持锁） | `runId=fba51302-d82a-4d6c-aceb-be1f0b7646ed` exit=1，`AssertionError: 看上周体重 旧句泄漏：样本` ⇒ 必红；改回并重编后 `2845b1ed` 必绿 |
| 出页 | `node .scratch/t154/text-review/render-family.mjs --family history --out .scratch/t154/text-review/fix480/out` ⇒ `TALLY {"通过":18} TOTAL 18 HTML=18`、末行 `OK-RENDER …/fix480/out` |
| 差分 | `python .scratch/t154/text-review/diff-pages.py --out …/fix480/out` ⇒ `SAME-PAGES 0/18 ；CHANGED-PAGES 18 ；DELTA-LINES 455`（基线 `pages/` 是本族上一轮改动**之前**的交付包，故行数含上一轮的改动） |
| 逐条自证脚本 | `python .scratch/t154/text-review/fix480/verify480.py`（本轮草稿，18 页 × 10 组判据）⇒ `PAGES 18；FAILS 0`、`RESULT: 18/18` |
| 看图 | `node .scratch/t154/shoot.mjs …/fix480/out …/fix480/shots` ⇒ `SHOT 18 files`；**实看 04／19／26 三张**（卡槽、图例、表标题、页脚、结论块逐处对过；26 的异常点卡副说明 `2026-09-03 起连续 3 天偏高` 一行不被截断） |
| 行数 | `history.ts` 本轮整改后 **LF=775**（上轮 742；超 350 线，超因与拆法见文件头，台账由编排者 `--sync`） |

**一处过程失误也如实记**：变异改回后第一次复跑**忘了重编译**，用例吃的是 `dist` 里的变异版 ⇒ `runId=646866c1-2e5a-47eb-b6f0-6c64484f36d9` 仍红；`tsc -b` 重编后回绿（`2845b1ed`）。判红／判绿的两次读数都发生在**编译过的那份 dist** 上，这条口径与上一轮一致。

**未做项**：眉标（`eyebrow`）那一行按令**一行未动**（等用户裁定）；第 48 条整页归 #436；告警线台账与 `pnpm gen` 生成物由编排者统一 `--sync`；`fix480/` 下的脚本与日志同 `.scratch/` 全目录在忽略清单里，不入版本库。
