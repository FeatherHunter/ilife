# t398 独立复核 · 体成分按来源分组取数（数据层）

- 票：#398（MAP#158 子票；前置挡 #362 的裁定 5 部分）
- 基准：`docs/skills/skill-calorie/t395-融合基准.md` §四 裁定 5、§八；票面编排者口径澄清（2026-09-14）
- 被复核提交：`f12ebc7a`（实现＋测试＋探针）／`f4d7d203`（证据＋对账＋changeset）／`f0123205`／`5f5b0857`／`80b0c044`（证据收口三笔）；未 push
- 复核现场：**自建** `.scratch/t398-review/`（独占，不复用作者 `.scratch/t398/`）；自建种子库 `.scratch/t398-review/db1`
- 复核者纪律：不采信作者任何结论；每条读数自跑，红绿两行均落盘
- **判定：PASS**（判据全绿；缺陷清单见 §7，无 S1-交付缺陷、无 S1-过程违规）

## 一、提交范围

五笔逐个 `git show --name-only` 与窗口 `git diff --name-only f12ebc7a^..80b0c044` 合计**变更件 8 条**（窗口内含他席 `ac5cfa4` 一笔，不属本票）：

```text
.changeset/t398-source-group.md
docs/skills/skill-calorie/t398-gate-runs.log
docs/skills/skill-calorie/t398-按来源分组取数-证据.md
packages/skill-calorie/src/body/bodyPlate.ts
packages/skill-calorie/src/body/view.ts
packages/skill-calorie/src/fetch/body.ts
packages/skill-calorie/test/t398-source-group.test.mjs
packages/skill-calorie/test/t398-source-probe.mjs
```

均为票面写集（`src/fetch/body.ts`／`src/body/bodyPlate.ts`／`test/t398-*`／证据件／`gate-runs.log`／changeset）。
**必须为 0 命中，实测全 0**：

| 文件 | 五笔命中 | 工作区脏 |
|---|---|---|
| `src/body/bodyDocs.ts` | 0 | 无 |
| `src/triggers/routes.generated.ts` | 0 | 无 |
| `packages/skill-calorie/src/kcal.ts` | 0 | 无 |

中文信息逐笔回读无乱码（`git log --format=%s` 与 `commit-msg*.txt` 逐字比对，`（）「」` 与全角标点均正常）。
`src/body/view.ts` 也进了写集——票面写集未点名该件，但它是 `calorie.view.body-composition` 的读侧接线口（编排者口径澄清里「确需读侧接线时改 `bodyPlate.ts`」的同类），归属**本票范围**、不算越界；见 §7 缺陷 D4。

## 二、改前基线自证（不采信作者日志）

作者证据 §1.1 只给了一段文本读数。我**自己造了改前模块**：`git archive f12ebc7a^ packages/skill-calorie/src` 落到 `.scratch/t398-review/before/`，
逐文件核 blob（`git hash-object` == `git rev-parse f12ebc7a^:…`，**五件全 match**），再用独立 tsconfig 编出 `before/out/`，`before-probe.mjs` 直调改前产物：

```text
BEFORE rows(source=all)=0 rows(no source)=7 rows(gym)=2
BEFORE trend(all).days=0 trend(gym).days=2
BEFORE exports: trendCompositionBySource=false compositionSourceCount=false assertSourceFilter=false
BEFORE 未知来源抛错=false（改前无校验：静默当来源名用）
BEFORE-RESULT 改前待证：all 取不到任何记录（必红），具体来源仍绿
```

- 改前 `listCompositions(source='all')` ⇒ **0 行**（`if (opts.source)` 把 `all` 当字面来源名过滤）；同库不带 source ⇒ 7 行、`gym` ⇒ 2 行 ⇒ 库里确有数据，红在**读侧**，与票面现状三条一致。
- 改后（`SKILLS_DB_PATH=.scratch/t398-review/db1`，我的库、我的种子，同一命令）：

```text
{"source":"all"}          exit=0 :: total=7 latestPct=20.5 trendDays=5
{"source":"all","days":90}exit=0 :: total=7 latestPct=20.5 trendDays=5
{"source":"home_caliper"} exit=0 :: total=3 latestPct=20.5 trendDays=3
{"source":"gym"}          exit=0 :: total=2 latestPct=19.6 trendDays=2
{"source":"hospital"}     exit=0 :: total=2 latestPct=20.0 trendDays=2
{}                        exit=0 :: total=7 latestPct=20.5 trendDays=3
{"source":"nope"}         exit=4 :: ERR 4: 取数失败（缺失阻断）：未知来源: nope（合法值: home_caliper / hospital / gym 或 all）
```

窗口内真实行数 7（我手算 8 条 − 窗外 1 − 废弃 1）、3 个来源、5 个不同日期，读数逐项对上。

## 三、核心判据（我自己写的探针，不复用作者 `t398-source-probe.mjs`）

`probe-mine.mjs`（`DatabaseSync readOnly` 自己数 SQL 当期望，不取实现输出）＋ `probe-mine2.mjs`（作者 9 条未覆盖的 5 个新场景）：

```text
MINE-RESULT PASS 组数(实现)=3 组数(SQL)=3 组=home_caliper(3点,3条),hospital(2点,2条),gym(2点,2条)
MINE2-RESULT PASS（21 条，红 0）
CLI-RESULT PASS（CLI total==SQL 窗口行数、trendDays==SQL 不同日期数）
```

四条票面判据逐条对上：① **组数 == 我自己 sqlite 数出的 `COUNT(DISTINCT source)`**（窗口版与全库版都验）；② **每组序列条数 == 该来源窗口内点数**（逐来源 `sql2/impl2`、`sql3/impl3`）；③ **`all` 跳过来源过滤**（`all` 行数 == 不带 source 行数 == SQL 窗口行数，行形状不变）；④ **未知来源报错**（`listCompositions` 与 `trendComposition` 两条主路都抛，不静默返空）。
另加作者未覆盖的场景：**同日三源**（3 条序列各含该日点，不合并）、**窗外记录**（91 天前、`is_deprecated=1` 两条均不入序列与行数）、**单一来源库**（`all` == 不带 source == 显式来源，组数 1）、**`all` 与显式来源逐来源一致**、**空来源库 `all` 返空不抛**、**组序＝最近日期降序、并列按来源名升序**、**换窗口（30 天）读数独立变化**（组数 3、行数 5、日期 3 —— 不是把窗口也放开）。

## 四、靶向门复跑与新变异（三行机器读数）

```
GATE-BASE  tsc exit=0   node --test t398-source-group.test.mjs exit=0 pass=9 fail=0
```

自设 **3 个**作者未覆盖的变异（`.scratch/t398-review/mutate-mine.mjs`；每条都核 dist 真被重编）：

| 变异 | dist 重编 | 我的探针1 | 我的探针2 | 作者靶向 |
|---|---|---|---|---|
| M-A 同日两源合并（`GROUP BY source, date`→`GROUP BY date`） | true `748FDC…`→`11374C…` | **exit=1**（每组条数红） | **exit=1**（红 2） | **exit=1** |
| M-B 窗外混入（`trendCompositionBySource` 窗口下界→全历史） | true `11374C…`→`98BBB7…` | exit=0（该库无窗外行，**不假红也不假绿**） | **exit=1**（红 2：窗外/废弃不入序列） | **exit=1** |
| M-C `all` 不再跳过来源过滤（退回改前口径） | true `98BBB7…`→`185421…` | **exit=1**（all 行数/形状红） | **exit=1**（红 7） | **exit=1** |

还原逐文件点名（**还原前一律先核 sha 再写回**，写回后顶 mtime 防 tsc 增量假红）：

```text
ORIG         sha256=E8811F4834E33A71 blob=01e6c5aa 与 HEAD 同内容=true
RESTORE-M-A  sha256=E8811F4834E33A71 match=true blob=01e6c5aa
RESTORE-M-B  sha256=E8811F4834E33A71 match=true blob=01e6c5aa
RESTORE-M-C  sha256=E8811F4834E33A71 match=true blob=01e6c5aa
FINAL        sha256=E8811F4834E33A71 equals-orig=true blob=01e6c5aa
FINAL 三实现件 git status（空＝干净）: ""
FINAL tsc exit=0 / node --test exit=0 pass=9 fail=0 / 三条探针 exit=0
dist 终检：`npx tsc -b` 后 `dist/fetch/body.js` hash 前后同为 `748FDC1483723DB3`（无残留变异）
```

**改坏必红＝三行（M-A/M-B/M-C 各自 exit=1）；还原必绿＝三行（三条探针 exit=0）。**
第一轮变异电池我自己的运行器有 bug（Windows 上 `spawnSync('node'/'npx')` 找不到可执行文件、`status=null`），三条变异全是**空跑**；已修（`shell:true`、记录 `SPAWN-ERROR`、加 dist 重编核对）后重跑，上表是修复后的读数。**这条自曝记在案**，避免把空跑当红。

## 五、门禁对账复核（重点）——裁定建议

### 5.1 我自己跑的对账（两份对账源各一次）

```text
$ node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t398-按来源分组取数-证据.md \
    --ticket 398 --since 2026-09-14T12:33:45Z --until 2026-09-14T12:45:00Z --allow-undeclared
（A）对 live `.scratch/locks/gate-runs.log`：EXIT=0  RESULT: matched=10/10 auditEntries=3342 scoped=22 undeclared=12
（B）对导出件 `docs/skills/skill-calorie/t398-gate-runs.log`：EXIT=0  RESULT: matched=10/10 auditEntries=16 scoped=16 undeclared=6
```

### 5.2 那 10 条是真门禁、exit=0？（是）

逐条在 live 日志里一对一找到，runId 相同、`exit=0`、命令为编译或靶向测试：

```text
6dedf52b / a34d8b0a / b8572cfb / 3b5bd56f / 1fb1c60f / f2270339   npx tsc -b packages/skill-calorie  exit=0
74f34f5d / 9755f644 / 18da80ca / 7b19b8b3                        node --test …/t398-source-group.test.mjs exit=0
```

**无「声称跑过却查不到留痕」**（`missing=0`）；**无「用非零退出条目充门禁证据」**（`--allow-nonzero` 未开也不报，10 条全 exit=0；两条 exit=1 的 `65938188`／`ecc5ade2` 没被任何声明认领）。

### 5.3 那 6 条确为非门禁条目？（是，但窗口漏了 6 条 git 条目）

- `65938188-7aae-416c-83ab-ce0ba3ef07e4`（12:33:51，exit=1）＝M2 变异轮靶向测试红；`ecc5ade2-e7e4-4fa9-96c7-cbbf7d076001`（12:34:28，exit=1，waitedMs=30010）＝M1 假红轮靶向测试红。两条**都承载「必红」过程结论、不承载门禁证据**，未声明由「只认 exit=0 条目」覆盖 ⇒ 正当。
- 4 条 git（`1c338248`／`5595d6ee`／`23e7930b`／`5eab4381`，全 exit=0）＝`git add`／`git commit`，受 §3.1 提交纪律管，不属门禁口径 ⇒ 正当。
- **但**：live 日志在**同一声明窗口**（12:33:45Z–12:45:00Z）内还有 **6 条 git 条目**（`ea961749`／`5cd18a2b`／`5f2f7430`／`0e7d6e32`／`2a32d3c0`／`79525583`，12:43:26–12:44:55），导出件里**一条都没有**——导出件头里写死的「until：2026-09-14T12:45:00Z」与实际内容（最末 `7b19b8b3` 12:41:44Z）**不自洽**。
- 根因可复现：导出时间 `12:42:26.479Z` 早于 `f0123205`／`5f5b0857`／`80b0c044` 的 `git add`／`git commit`（12:43:26–12:44:55）；f0123205 那一笔导出后，**后面三笔没再重导**。作者 §四「16 条＝10 绿＋2 必红＋4 git」的加总，以及 `GATE-RELAX` 里「另有 6 条非门禁条目」的计数，**只对「到 12:41:44 为止」这个窄窗口成立**；按声明窗口的真实条目是 22 条（10 绿＋2 红＋**10** git），非门禁 12 条。

### 5.4 裁定建议（引条号）

- **不构成 §2.4-3 违规**：细则③「声明必须引运行标识、只认 runId 相同条目」、④「只领 exit=0 条目」两条都满足；§2.4-3 末句「违规只落在**有人声称跑过的运行**上：声称了却没有对应留痕才判违规」——本票 `missing=0`，**没有任何裸跑被追究**。
- **不构成 §2.4-4 的「私自放宽」**：放宽开关（`--allow-undeclared`）**写进了证据**（`GATE-RELAX flag=--allow-undeclared reason=…`），且放宽对象（必红排查＋git 操作）确为非门禁条目，方向正确、无假绿。
- **建议按 §2.3 第 4 条记一条 S3-过程缺陷（不阻塞关闭）**：对账源必须「可直接当对账源复核」，而当前导出件**只能复核窄窗口**；第三方用声明窗口复核 live 日志会得到 `scoped=22 undeclared=12`，与证据里写的 `16/6` **不同数**，且 `--until 12:45:00Z` 这个口径**无法从入仓件验证**。修法二选一：① 把导出重做到 12:45:00Z（22 条）、读数改 `matched=10/10 scoped=22 undeclared=12`；② 或把窗口收窄到 `--until 2026-09-14T12:41:45Z`（16 条）并让 §四 的加总与 `GATE-RELAX` 计数与之对齐。**两处数字必须同源**。
- 结论一句话给编排者：**没有造假、没有裸跑、没有用红条目充门禁；只有「声明窗口与导出件不自洽」这一条对账口径缺陷**，按 §2.3-4 记账即可，不必按 §6 作废重做。

## 六、真库零写入

```text
REAL-DB file=D:\2Study\StudyNotes\.db\calorie_data.db
        size=3072000 mtime=2026-09-14T13:53:37+08:00（=13:53:37 旧值，本票窗口内未动）
        sha256=57CE9D87F5F232822332D7057C2AE8411D99BA7FDECEA289F2028E19FC3FFC15
integrity=[{"integrity_check":"ok"}]  page_count=750 page_size=4096
body_composition 行数=0   来源列=[]
```

与作者报的 `sha256=57CE9D87…FC15`、`size=3072000` **逐字相同**。五笔提交里出现 `calorie_data.db`／`StudyNotes`／`integrity_check` 的地方逐条看过：全是**证据文本与测试里的路径字符串**（`mkdtempSync` 临时库与只读 DOM 对账），**无一句写侧 SQL 指向真库**；本票实现三件没有真库路径。本复核自己的读数一律走 `.scratch/t398-review/db1` 与 `readOnly:true`。

## 七、结构与缺陷清单

结构读数：

```text
src/fetch/body.ts：改前 289 行 → 改后 369 行（节点口径 readFileSync(f,'utf8').split('\n').length - 1，LF）
本票在 body.ts 新增对外导出 6 个：SOURCE_FILTER_ALL／SourceFilter／assertSourceFilter／SourceSeries／
                              trendCompositionBySource／compositionSourceCount
（其中 SourceFilter／SourceSeries 为纯类型导出；口径「每个文件对外给的东西不超过五个」）
bodyDocs.ts：未碰（五笔命中 0、工作区不脏）
页面链未消费新字段：`sourceSeries`／`sourceCount` 全仓引用只在 bodyPlate.ts 内（bodyDocs.ts 仍只读原有字段）
⇒ 「页面渲染链一行为变」成立，页面文本归 #362 的切分站得住
```

**缺陷清单（本票引入／本票范围／范围外发现）：**

- **D1〔本票引入·S3-过程〕对账窗口与导出件不自洽**：见 §5.3／§5.4。10 条门禁声明本身合规。
- **D2〔本票引入·S2（文档面）〕§六 提交 sha 清单只列三笔**：本票实际五笔（`f0123205`／`5f5b0857`／`80b0c044` 三笔证据收口未入清单）。`80b0c044` 那笔已加「权威清单以 `git log` 复读为准」的自指尾说明，属**已部分自认**，但清单仍是三笔、与「本票全部提交」的标题不符；编排者按清单统一 push 时会漏两笔。
- **D3〔本票引入·S2（结构·必报五步第四步）〕`src/fetch/body.ts` 369 行超本包 350 告警线**：`packages/skill-calorie/AGENTS.md` 写死「告警线＝350 行（LF 口径）」且「超线即触发必报五步第四步：当场报一句『已超线，需要根据规则进行重构。』」。本票把它从 **289 → 369**（+80），**作者任何一处（§二／§七／提交信息）都没报这句**。同文件台账「其余件均在 350 以内」也随之变假。建议：证据件补这一句（超因：体成分读侧的取数、分组、计数三件事挤在 `fetch/body.ts`），并把该件挂进 `packages/skill-calorie/AGENTS.md` 台账；拆法留给后续票（本票不拆）。
- **D4〔本票范围·S3〕写集外多改一件**：`src/body/view.ts` 在第五笔 `git show --name-only` 里，票面写集未点名（写集含 `bodyPlate.ts`）。改动只是类型收窄＋注释，**无行为变化**（`optStr` 结果原样下传），判为范围外扩但正当；只为台账精确记一笔。
- **D5〔范围外发现·S3＋转票措辞〕**：`compareCompositions`（`body/compare.ts` 那条「对比」链）未收未知来源读侧校验，`source='all'` 在对比命令上仍按字面过滤（原名行为）。作者已在 §七「本票未做」自认；**转票措辞**：「体成分对比命令的读侧来源词与未知来源校验对齐 `assertSourceFilter`（含 `all` 的口径）」，建议并入 #362 或另开数据层小票。
- **D6〔范围外发现·S3＋转票措辞〕**：`packages/skill-calorie/scripts/check-warning-line.mjs` 的 `REQUIRED` 只钉两件（`wizardPort.ts` 457／`gen-cli.mjs` 729），对第三件超线件（本票的 `fetch/body.ts` 369）**无感**——检查脚本绿不代表台账齐。**转票措辞**：「告警线台账/检查脚本改为扫 `src/**/*.ts` 实测行数并与台账对账」。

## 八、我未做与下一手缺什么

- **未跑全量门**（`pnpm test`）：按派单，全量门由编排者统一跑；我只报靶向门与自设探针。**下一手**：编排者在合并点跑一次全量门（含 `t398-source-group.test.mjs`），并跑 `node packages/skill-calorie/scripts/check-warning-line.mjs` 看台账是否仍绿。
- **未跑 `pnpm gen`／`pnpm --filter build`**（派单禁止）；编译只用 `npx tsc -b packages/skill-calorie`（经 `tooling/run-locked.mjs --ticket 398-review` 持锁，留痕在 `.scratch/locks/gate-runs.log`）。
- **未改实现与测试**：三条变异在持锁窗口内做完，逐文件点名还原并核 sha（`E8811F48…`）、`git status` 干净；`.scratch/t398-review/` 是我的独占现场，复核结束按派单清理。
- **下一手缺什么**：① §5.4 的对账窗口二选一修法（谁改由编排者定，会再产出一笔证据提交）；② D2 的提交 sha 清单补两笔（**在 push 前补**，否则清单漏笔）；③ D3 的「已超线」报句与台账挂件；④ #362 接线时只剩数据已备件（`sourceSeries`／`sourceCount`）＋「来源不可直接对比」提示句落 `bodyDocs.ts`，不需再动本票文件。
