# t330 共用位收敛-证据

席位：#330 实施席（只做这一张票）。工作区 `D:\ilife`，分支 `master`。
开工：`HEAD == origin/master == 61caf45`，`git status --porcelain` 空（干净）。

## 一 · 归位对照（行为逐字不变，调用点只改引用）

| # | 旧住处 | 新住处 | 手法 | 同值证据 |
| --- | --- | --- | --- | --- |
| 1 | `src/cli/write.ts` 的 `profileReceiptDoc`（两具名键集＋三键→整页，调用点 `:63`） | `src/profile/receipt.ts`（30 行），经 `src/profile/index.ts` 转出；`write.ts` 改调门（76 行） | 纯搬：键集、分支、传参（`commandLine`、`db`、`withM5` 后调用）逐字照旧 | 全量 1492/1492（含三条档案写词整页回执断言）；294 终态 7/7 |
| 2 | 根 `src/output.ts` 的 `writeSuffixFor`（21 处 `case`，调用点 `deliverHtml`） | `src/shared/contentSuffix.ts`（118 行：12 行头＋107 行原文）；`output.ts` 改 import＋再导出（313 行） | 整搬：函数体逐字节一致，`sha256 = 21402c26757a8b2e70c5013357209b2f7dd33c9815887821f53495ada4761a79`（搬前＝搬后，见 `.scratch/t330/`） | `output-naming-119` 逐键断言全绿（测试仍从 `dist/output.js` 取用，再导出面不变） |
| 3 | `src/cli/cmd_read.ts` 340 行 | `cmd_read.ts` 135 行 ＋ 新 `readArgs.ts` 53 行 ＋ 新 `delivery.ts` 173 行 | 纯搬：CLI 前置（参数＋预检）与交付装配（envelope＋三态＋失败回执）按变化频率分件；原文块顺序不变逐块照搬 | 入口名与 `export function dispatch` 未挪；`buildDeliveredEnvelope` 经原路径再导出（`t83-evidence.mjs` 不动） |

两处与纯搬等价的整理（零运行时影响）：`delivery.ts` 内把原 `cmd_read` 本地别名 `DispatchOut` 写作同形的 `ViewOut`（`DispatchOut = ViewOut`，类型同一）；`describeDeliveryTarget` 随交付装配走并转出（主入口失败分支直用它）；`profile/index.ts` 门 5→4（去两装配转出，加 `profileReceiptDoc`，仍≤5）；`cmd_read.ts` 去一条已失效的注释（老 switch）与一个未用导入（`dayField`）。

## 二 · 行数（节点口径 `split('\n').length - 1`）

| 文件 | 改前 | 改后 |
| --- | --- | --- |
| `src/cli/cmd_read.ts` | 340 | **135**（≤200 达成，无需改行为） |
| `src/cli/write.ts` | 104 | 76 |
| `src/output.ts` | 416 | 313 |
| `src/cli/readArgs.ts` | — | 53（新） |
| `src/cli/delivery.ts` | — | 173（新） |
| `src/profile/receipt.ts` | — | 30（新） |
| `src/shared/contentSuffix.ts` | — | 118（新） |
| `src/cli`＋`src/output.ts` 的 `case 'calorie.` | 21（全在 output） | **0**（`git grep` 无匹配） |

包告警线 350：新建改动件全部落线内。

## 三 · 口径段位置

`docs/agents/命令事实边界.md`（新）：一句话口径＋边界三问＋两处归属裁定（回执端口→能力目录／标识段→共享件）＋非目标＋出处。

## 四 · 复跑（持锁 `run-locked --ticket 330 --lock-dir .scratch/locks-land --stale-minutes 30`，direct node）

| 门 | 命令 | 读数 |
| --- | --- | --- |
| 类型 | `node node_modules/typescript/bin/tsc -b` | `exit=0`（runId `795f7c3e`；中间一次 `exit=2` 仅为本席漏搬 `describeDeliveryTarget`／`DispatchOut` 别名两处，已修后复绿） |
| 生成物 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `exit=0`；`GEN-CHECK PASS：键 101（写 35＋读 66）；能力 10 个声明 101 条，未搬迁清单 0 条`（runId `a4aa2b94`） |
| 全量 | `node --test test/*.test.mjs packages/*/test/*.test.mjs` | `tests 1492／pass 1492／fail 0／cancelled 0／skipped 0／todo 0`，`exit=0`（见 `.scratch/t330/full-test.log`） |
| 冻结 | `node --test test/calorie-routing-81.test.mjs` | `tests 8／pass 8／fail 0`，`exit=0`（runId `59aed338`） |
| 探针 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `P1..P5 5/5`，`UNACCOUNTED=0 处`，`exit=0`（runId `5e1349e1`，见 `.scratch/t330/probe2.log`） |

探针记账：首跑 `3/5`（P1 动态基线＋P3 沙箱基线红），根因为 `dist/.gen-inputs.json` 为旧 v1 印记（本席 `tsc -b` 直调跳过了打印记，且树上有他席并发构建）；
照医嘱跑 `gen-cli.mjs --stamp`（只写 gitignored 印记，不碰跟踪文件，`exit=0`）后复跑转绿。静态面首跑即全绿（`UNACCOUNTED=0`、P2／P4／P5 PASS；P4 点名 `cmd_read 135／write 76／case 0`）。

## 五 · 变异自证（294 终态门鉴别力）

- 塞：`src/cli/write.ts` 的 `dispatchInner` 加 `case 'calorie.zz-mut'` → `node --test packages/skill-calorie/test/cmd-registry-294.test.mjs`：`tests 7／pass 6／fail 1`，`exit=1`，
  失败签名 `分派层仍有按键分派（#320 起老路应无活口）：calorie.zz-mut`（runId `7be48b9a`，见 `.scratch/t330/mut-red.log`）。
- 删（回退该两行，树内零残留）：同件 `tests 7／pass 7／fail 0`，`exit=0`（runId `bf03682e`，见 `.scratch/t330/mut-green.log`）。

## 六 · 结构五步对账

第一步清单（profile 门 5→4／cli 内拆分／shared 收标识段／根输出件改引用／两处文档）逐行有着落，偏差为零。
未建新顶层目录；未碰三个以上能力（标识段整搬不拆六能力）；共享件头写清六个在用能力。

## 七 · 未做项与环境记账

- 未改冻结面（`test/calorie-routing-81.test.mjs`、`src/triggers/scene-*.ts`）与判官（探针只跑未改）。
- 未动棘轮测试件（`cmd-registry-294.test.mjs` 一字未改；变异只动被测源码）。
- 未写票面、未 close 任何票；无 `--force`／删 `dist/`／`--amend`／rebase。
- 树上他席（328／325 系）在途未提交改动（`gen-cli.mjs`＋239 行印记 v2／配对门、`SKILL.md`、`package.json`、`gen-routes.mjs`、`photo/helpLookup.ts`、`templates/help.html` 及 `t328` 证据件）：
  本席不碰、不提交、不还原；全量与探针跑在合并树上（绿），提交只含第八节所列限定路径。
- `≤200` 经纯搬达成（135），无需改行为；无最小可达行数事项。

## 八 · 提交（限定路径）

`packages/skill-calorie/src/profile/receipt.ts`（新）、`packages/skill-calorie/src/profile/index.ts`、
`packages/skill-calorie/src/cli/write.ts`、`packages/skill-calorie/src/shared/contentSuffix.ts`（新）、
`packages/skill-calorie/src/output.ts`、`packages/skill-calorie/src/cli/readArgs.ts`（新）、
`packages/skill-calorie/src/cli/delivery.ts`（新）、`packages/skill-calorie/src/cli/cmd_read.ts`、
`docs/agents/命令事实边界.md`（新）、`docs/skills/skill-calorie/t330-共用位收敛-证据.md`（本件）。
