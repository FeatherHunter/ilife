# #253 跟随件（二席）：两处「仍是片段」的旧断言跟新口径 ＋ 补链外／训练计划族覆盖

票号 **253F2**。前一席（提交 `af0a7cbb`）把两处抽样换成「当刻仍是片段的命令」，**失败**：
上一席复核用探针实测证明当刻 **46 条会改数据库的命令全部出整页**（六个装配口共认 40 条，链外 6 条也已是整页）——
「仍是片段」这个前提已无样本。编排者裁定 **(b)**：把旧断言**翻成整页口径**，并**补上被漏掉的覆盖**
（上一席复核用受控变异抓出的洞：把训练计划那个装配口整体退回片段，两个用例件一条都没红）。

- 提交：`e350865`（`功能(253F2): 两处「仍是片段」旧断言跟新口径…`；4 文件 ＋285/−26）
- 本票基线：`fe51c32`（上一席 `af0a7cbb` 之后）
- 未 push（票面要求）。

## 一、影响清单（一行一个文件 ＋ 一句话理由）

| 文件 | 为什么动它 |
|---|---|
| `packages/skill-calorie/test/profile-doc-179.test.mjs` | 第 276-290 行那条用例钉的是「其余会改数据库的命令**仍是原回执片段**」（断言产物以 `<section class="ilife-page" data-slot="ilife:calorie:receipt">` 开头、且**不得**以 `<!doctype html>` 开头）。当刻被它抽样的两条（`calorie.goal.set`／`calorie.goal.water`）已由 #253 切整页，断言方向反了：翻成完整文档四断言（`assertFullDocPage`：doctype 起／charset／`<style`／`ilife-page`，另加「不得还落在旧片段标记上」）。 |
| `packages/skill-calorie/test/delivery-83.test.mjs` | 第 324-329 行那条 `assert.equal(frag.env.delivery.template, 'receipt')` ＋「片段族以 section 开头」同样钉在旧片段口径上（被测命令＝`calorie.goal.set`，已切整页）→ 翻成 `'doc-shell'` ＋ `assertFullDoc`（四断言 ＋ 不得落在旧片段标记上）；另补一组覆盖：链外六条（`calorie.exercise.*` 三条、`calorie.photo.*` 三条）＋ 训练计划族代表键 `calorie.workout.plan-set` 的产物已是完整文档。 |
| `packages/skill-calorie/src/cli/write.ts` | **只改件头注释两句**：第 3 行「35 写键一律 receipt 形」与第 17 行那句「其余 6 条（46 − 3 − 14 − 4 − 10 − 4 − 5……）」当刻都对不上实况（实况 46 条会改数据库的命令、六个装配口共认 40 条、链外 6 条也已是整页）。代码一行不动（`git diff`：2 增 1 删，全在件头注释里）。 |
| `docs/skills/skill-calorie/t253-跟随件-二席.md` | 本证据件（影响清单／改前红读数／改后绿读数／变异两行 ＋ 逐条 `GATE-RUN`）。 |

### 1.1 先定位「是哪条键」（票面第 2 条要求：改前先自己跑一遍）

被测命令＝ **`calorie.goal.set`**（参数与 `profile-doc-179.test.mjs` 那条抽样逐字同源：
`{ calorie: 1800, protein: 150, carbs: 200, fat: 50 }`，同一份种子库 `mkDb(true)`／`mkDb('write')`）。
改前读数（红）＝`assert.equal(frag.env.delivery.template, 'receipt')` 实得 `'doc-shell'` 而红，
下一行「片段族以 section 开头」也红——两条都被下面的真身输出钉住。

## 二、改前读数（红）

### 2.1 试过、**没拿到**的那一层（如实记）

想拿「已提交状态（`fe51c32` 的测试件 ＋ 配套 dist）真身复现」这一层：试了
`git archive fe51c32 -- packages/skill-calorie/dist` ⇒ **失败**（`fatal: pathspec 'packages/skill-calorie/dist' did not match any files`）——
`dist/` 不在版本控制里，已提交状态**没有**配套编译产物可取。探针脚本留在 `.scratch/t253F2/before-probe.mjs`（失败版），
下一手若要更硬的一层，需要一份 `fe51c32` 的干净编译。

另有一次早跑的读数 `.scratch/t253F2/before-both.log`（`ℹ tests 25 / pass 21 / fail 4`）——但那次是把
**已经改过的测试件**跑在临建 `dist` 上（本票测试件已落盘、`dist` 才刚被另一席编过），不能当「改前」，
故只作参考、不进判定。

### 2.2 当刻可复现的「旧断言必红」（硬证据在第四节变异 A）

把旧口径**放回**去跑（变异 A 做的事，落 `.scratch/t253F2/mutA.log`）——这就是改前那两条断言的真身读数：

```
✖ #83 写键同样有 delivery（receipt 产物族）
    AssertionError [ERR_ASSERTION]: 目标域整页回执的产物族（退回片段即 class 判定走不到这里）
    actual: 'doc-shell'    expected: 'receipt'
    at packages/skill-calorie/test/delivery-83.test.mjs:353
```

同一条变异里 `#179／#253` 那条（把 `assertFullDocPage` 的 doctype 断言换回片段断言）也红：
`AssertionError: calorie.goal.set 缺片段开头`（实测产物以 `<!doctype html>` 起头，片段开头断言当场失败）。
⇒ **旧断言方向反了、新断言方向对**，两面同一批产物、同一批读数。



## 三、改后读数（绿）

### 3.1 编译门（本票改的是测试件 ＋ 注释，先证「编译真跑过」）

```
GATE-RUN runId=dfcbada1-d1cd-4bfe-91da-c1371f746f41 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
RESULT: ticket=253F2 runId=dfcbada1-d1cd-4bfe-91da-c1371f746f41 waitedMs=0 exit=0
测 tsbuildinfo mtime：639251078072200017 → 639251078072200017（**未变**——tsc -b 的增量账记的是文件时间戳，
本票源码只动了注释，tsc 判定「产物不变」而**不写回** tsbuildinfo／dist）
```

补一发**强制全量**编译，把上面的歧义钉死（`--force`：全量重建，tsbuildinfo 必更新）：

```
前 mtime=639251078072200017
GATE-RUN runId=cc800133-ed90-41be-aab7-0430a2285cfa cmd="node node_modules/typescript/bin/tsc -b --force packages/skill-calorie"
RESULT: ticket=253F2 runId=cc800133-ed90-41be-aab7-0430a2285cfa waitedMs=20016 exit=0
后 mtime=639251078675033615  ← 变了（退出码 0 ＋ tsbuildinfo mtime 更新，「编译真跑过」两条都满足）
```

### 3.2 验收命令（两个测试件）

```
GATE-RUN runId=f0e0bb79-8f19-4393-8725-7fc85e950ba6 cmd="node --test packages/skill-calorie/test/profile-doc-179.test.mjs packages/skill-calorie/test/delivery-83.test.mjs"
RESULT: ticket=253F2 runId=f0e0bb79-8f19-4393-8725-7fc85e950ba6 waitedMs=50031 exit=1
ℹ tests 25   ℹ pass 21   ℹ fail 4   ℹ skipped 0   ℹ todo 0
```

本票点名的三条已从红转绿（逐条真身）：

```
✔ #179／#253 目标域两条会改数据库的命令：产物是完整文档（原「仍是片段」反面按当刻口径翻面）
✔ #83 写键同样有 delivery（receipt 产物族）
✔ #253F2 链外六条会改数据库的命令：产物已是完整文档（不是片段）
✔ #253F2 训练计划族代表键（calorie.workout.plan-set）：产物已是完整文档（不是片段）
```

`exit=1` 只因**本文件里属于别的票的 4 条红**（见第五节），与本票点名的三条同文件共存、互不牵连：
`#83 ⑤ 唤醒词命中即渲染` 等 11 条 delivery-83 用例与 profile-doc-179 的 5 条（含本票两条翻转／补覆盖）全绿。

## 四、变异自证（两行机器读数）

脚本：`.scratch/t253F2/mutate-253F2.mjs`（**只在持锁窗口内跑**，改／还原都在窗口内做完）。

```
GATE-RUN runId=57936dc4-8ee6-4bb3-a3ef-e97c43a79018 cmd="node .scratch/t253F2/mutate-253F2.mjs"
RESULT: ticket=253F2 runId=57936dc4-8ee6-4bb3-a3ef-e97c43a79018 waitedMs=30027 exit=0
---- 机器读数 ----
MUTATION-A exit=1 pass=18 fail=7 变红=yes :: ✖ #83 写键同样有 delivery（receipt 产物族） | ✖ #253F2 链外六条会改数据库的命令：产物已是完整文档（不是片段） | ✖ #179／#253 目标域两条会改数据库的命令：产物是完整文档（原「仍是片段」反面按当刻口径翻面）
MUTATION-B exit=1 pass=20 fail=5 变红=yes :: ✖ #253F2 训练计划族代表键（calorie.workout.plan-set）：产物已是完整文档（不是片段）
RESTORE   exit=1 pass=21 fail=4 三条用例回绿=yes :: ✖ #239 … | ✖ #247 … | ✖ #337 … | ✖ #365 …（只剩别的票那 4 条红）
MUTATION-VERIFY ok
```

- **变异 A**（把新翻的整页断言改回片段口径）：`delivery-83` 把 `full.env.delivery.template` 与链外那组的
  期望从 `'doc-shell'` 改回 `'receipt'`；`profile-doc-179` 把 `assertFullDocPage` 的 doctype 断言换成片段断言。
  ⇒ 三条用例同时变红（`fail` 4→7），**强度不降**得到证明。
- **变异 B**（把训练计划那个装配口整体退回片段）：`write.ts` 分派链里删掉 `?? workoutReceiptDoc(key, params, receipt, db)`。
  ⇒ **只有**新补的「训练计划族代表键」那条红（`fail` 4→5）——正是上一席复核抓出的那个洞，现在有人看着了。
  真身读数：`AssertionError: 训练计划族整页回执的产物族（退回片段即红）`，`actual: 'receipt'` / `expected: 'doc-shell'`。
- 还原：逐文件从 `.mutbak` 还原 ＋ **强制全量编译**（`--force`）后再跑 ⇒ 三条用例回绿、`fail` 回到 4。

> 踩过的坑（写进件里，免得下一手重踩）：`tsc -b` 的增量账只认**文件时间戳**，而「备份→改→还原」会把
> `write.ts` 的 mtime 带回旧值 ⇒ 还原那一步不 `--force` 时会被判成「没变」而跳过编译，`dist` 停在变异态，
> 于是「还原后仍红」是**假红**。脚本首版即栽在此处：变异 B 第一遍报「不变红」，也是**假绿**（dist 没跟上）。

## 五、别的票的红（范围外，只记账不碰）

同两个测试件里另有 4 条红，逐条给读数与归属；**本票不改这几条**（票面「不能动的东西」第 4 条）：

| 用例 | 失败签名（真身） | 归属 | 本票为何不碰 |
|---|---|---|---|
| `#239 四张页接上「复制日志」` | `AssertionError: 预检确认页按钮不对` | 档案／预检页族（#239） | 断言点的是**预检确认页**（写前页），不在本票写集；本票没动预检页任何件 |
| `#247 场景 07 五张页的复制数据是「三格式三选一」菜单` | `AssertionError: 预检确认页 菜单缺用途提示：粘贴给 AI / 自己看` | 档案族复制菜单（#247） | 同上：也是预检页那一面 |
| `#337 场景 03 四条体重写命令已是完整文档（三变体字段齐）` | `AssertionError: weight.log 缺：体重 · 写后回执` | 体重域（#337） | 体重域眉标，归体重域的票；本票一行没动体重件 |
| `#365 身体细节四条写命令已是完整文档（七条写词……）` | `AssertionError: 记体脂 缺场景 08 眉标` | 身体域（#365） | 同上：身体域眉标 |

读数出处：`.scratch/t253F2/acc-final.log`（`ℹ fail 4`）与 `.scratch/t253F2/restore.log`（还原复跑同一批 4 条）。

## 六、持锁运行留痕（逐条 `GATE-RUN runId=… cmd=…` ＋ 摘要行 ＋ 退出码）

```
GATE-RUN runId=dfcbada1-d1cd-4bfe-91da-c1371f746f41 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"                      RESULT exit=0
GATE-RUN runId=c5db3839-ba0b-4158-9c0c-44ced0d00f50 cmd="node --test packages/skill-calorie/test/profile-doc-179.test.mjs packages/skill-calorie/test/delivery-83.test.mjs"  RESULT exit=1（tests 25／pass 21／fail 4）
GATE-RUN runId=cc800133-ed90-41be-aab7-0430a2285cfa cmd="node node_modules/typescript/bin/tsc -b --force packages/skill-calorie"              RESULT exit=0
GATE-RUN runId=d50aaada-a6c5-445f-bca8-b1db8d59ed35 cmd="node .scratch/t253F2/mutate-253F2.mjs"                                              RESULT exit=1（首版：变异 B 假绿——dist 没跟上）
GATE-RUN runId=7604394f-508f-44f6-87ea-12f4b84ccfc2 cmd="node .scratch/t253F2/mutate-253F2.mjs"                                              RESULT exit=1（二版：还原态假红——同上一个原因）
GATE-RUN runId=76142376-586d-4a51-9971-cc5de4c8ea7d cmd="node node_modules/typescript/bin/tsc -b --force packages/skill-calorie"              RESULT exit=0（把 dist 拉回还原态）
GATE-RUN runId=57936dc4-8ee6-4bb3-a3ef-e97c43a79018 cmd="node .scratch/t253F2/mutate-253F2.mjs"                                              RESULT exit=0（MUTATION-VERIFY ok）
GATE-RUN runId=f0e0bb79-8f19-4393-8725-7fc85e950ba6 cmd="node --test packages/skill-calorie/test/profile-doc-179.test.mjs packages/skill-calorie/test/delivery-83.test.mjs"  RESULT exit=1（tests 25／pass 21／fail 4）
GATE-RUN runId=0d3e28ae-c7a5-4c77-98e8-8b9a7c59ddf2 cmd="node .scratch/t253F2/before-probe.mjs"                                                            RESULT exit=1（改前复现探针：git archive dist 取不到已提交产物）
GATE-RUN runId=45dfbee2-ed38-4edb-a6f6-76a4cfd8a85e cmd="git commit -m … -- <四路径>"                                                                          RESULT exit=1（未跟踪的证据件先 add 才认；下一条补上）
GATE-RUN runId=4e82be8b-386c-4ac2-b307-c7ac8add20a5 cmd="git add <四路径>"                                                                                     RESULT exit=0
GATE-RUN runId=cc9fe219-22dc-4782-b79e-a504f7e89a00 cmd="git commit -m …（无 pathspec，add 已做）"                                                             RESULT exit=0（e350865；4 files changed, 285 insertions(+), 26 deletions(-)）
```

## 七、没做完的／下一手缺什么

- 本票三条用例全绿、四条范围外红逐条记账（第五节）。**没有本票未做项。**
- 下一手若要把范围外那 4 条红收口，需要的是：体重域（#337）与身体域（#365）的眉标按当刻口径重钉，
  以及档案族预检页（#239／#247）的按钮与复制菜单——都不在本票写集。
