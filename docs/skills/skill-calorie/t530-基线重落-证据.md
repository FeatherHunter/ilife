# t530 · 基线重落（十页）· 证据件

票：`#530 卡路里场景09 · 视觉终审`（父图 `#159`）。本件记**判据 2「机器门全绿」里那条基线门**的收口：
`gen-photo-baseline.mjs --check` 连跑两次都必须 `BASELINE-OK 10/10`（上一轮的终审报告把它列为未做项）。

## 一、进场读数（两处病）

```text
BASELINE-DIFF 10 pages                      ← 十页都在本轮视觉/文本整改后漂了（预期）
UNSTABLE calorie.photo.remove:渲染当刻时钟未被剥净：2026-09-16 21:35:36
UNSTABLE calorie.photo.tag:渲染当刻时钟未被剥净：2026-09-16 21:35:36
```

第二条是**真缺陷**（不是漂移）：#528b 把回执页的「时间」从旧 `<td>` 表格形态换成**键值行**
（`.phr-fact` 一族：`<span class="phr-fk">删除时间</span><span class="phr-fv">2026-09-16 21:38:28</span>`），
而 #462 落的四条剥除规则 ①／② 认的是旧形态（`>时间</td><td…>`／`>写入时间</td><td…>`），于是**渲染当刻时钟剥不净**，
基线永远钉不住（`--write` 重落后下一轮 `--check` 还会随秒漂）。

## 二、改法（一处规则 ＋ 一次重落）

`packages/skill-calorie/scripts/gen-photo-baseline.mjs` 的 `CLOCK_STRIP_RULES` **追加第 ⑤ 条**（位置锚定，与 ①–④ 同口径：
只吃一处 19 字符时钟串、条数与吃掉的字符数对不上账即报错）：

```js
// ⑤ #528b 起回执页的「时间」改住键值行（旧 <td> 表格形态换成 .phr-fact 一族）：
//    <span class="phr-fk">删除时间</span><span class="phr-fv">2026-09-16 21:38:28</span>
//    标签共三种：时间（改／加／删标签页）／删除时间（删照页）／写入时间（存照页）。
[new RegExp('(<span class="phr-fk[^"]*">(?:时间|删除时间|写入时间)</span><span class="phr-fv[^"]*">)' + CLOCK_TEXT, 'g'),
  (p) => p + CLOCK_MARK],
```

然后按票面口径**重落十页 sha**：`--write` 一次，再连跑两次 `--check`。

## 三、机器读数（全部持锁跑，票号 530）

| 轮 | 命令 | 运行标识 | 读数 |
|---|---|---|---|
| 复现 | `--check` | `runId=a4e4696d-9685-42e7-8d63-c7099bdf16d6` | `BASELINE-DIFF 10 pages`，**`UNSTABLE` 已消失**（规则 ⑤ 生效），exit=1 |
| 重落 | `--write` | `runId=ecd74201-cf19-49b8-bf14-809471a98d96` | `BASELINE-WROTE 10 pages`，exit=0 |
| 第一轮 | `--check` | `runId=5b4d74ff-5322-41e1-97ec-32bbf39d966c` | **`BASELINE-OK 10/10`**，exit=0 |
| 第二轮 | `--check` | `runId=dfac9e7a-3dd3-40da-a820-3116c3126116` | **`BASELINE-OK 10/10`**，exit=0 |

顺带复跑告警线门（本轮改了超线件 `gen-photo-baseline.mjs`：283 → 289 LF）：

```text
node packages/skill-calorie/scripts/check-warning-line.mjs --sync --dry → SYNC-PLAN 改=1
node packages/skill-calorie/scripts/check-warning-line.mjs --sync       → SYNC-VERIFY ok（台账 44 行）
node packages/skill-calorie/scripts/check-warning-line.mjs              → RESULT: 91/91  PASS  exit=0
```

## 四、提交与「没有连坐」的交代

- 本票提交：`gen-photo-baseline.mjs`（规则 ⑤ ＋ 重落后的 `BASELINE` 常量）＋ 本证据件。
- **台账那一行（`gen-photo-baseline.mjs` 283→289）留在工作区未提交**：同一个 `packages/skill-calorie/AGENTS.md`
  里还有**别席在途**的台账改动（`gen-cli.mjs 796→933` 带 #343 注记、`build-help.mjs 743→742`）。
  按仓规「提交限定路径 ＋ 不把别人的件卷进提交」，本票**不整文件提交该件**——留给他们那一笔带走，
  盘上已是同步态（门读工作区，当刻 91/91 绿）。

## 五、未做项

- 基线门**没做「挪一天／改坏必红」的负向对照**：本轮是「补一条位置锚定规则 ＋ 重落」，判据本身（两次 `--check` 全绿）
  由票面给定；#493 那一轮已给过该机制的变异自证（改坏一页可见文本 → 点名该页 `CHANGED`）。
- 其余终审项（逐页 vision 分、导航页 82 处债、09-05 备注例外）仍按 `t530-场景09-视觉终审.md` §四 挂着。
