# t519 对抗复核席（续）· 证据与判定

> 复核席：对抗复核席（续）。前席上下文耗尽，未留下报告文本，仅留下机器读数（`.scratch/t519-review/**`）。
> 本报告全部关键结论：**能复跑的一律自己复跑**（输出落 `.scratch/t519-review2/*.log`），无法复跑的**标注「引用前席读数」**。
> 本席纪律：不改产品代码、不改别席证据件、不 close、不 push；只提交本文件。

## 0 · 判定与五维分

### 判定：**FAIL**

**理由（一句话）**：本票 W5 的 D4（图表横轴标签重叠）在**手机档没有闭环**——`.scratch/t387/html/` 的
**24／26／28／30 四页在 390 档 11／11 相邻对全部相交**（最大重叠 4.98px，截图肉眼可见字形相撞），
而 W5 新写的守卫 ⑨ 与其门槛常量（`LABEL_W=34.15`）**在 390 档恒绿**，代码注释与证据件又都写着「四档都不可能相交」
——**缺陷仍在、判据看不见、结论与真渲染相反**。其余机器门（分隔符 31/31、版面 31×3 逐格 0、十件测试 60/60、
告警线 91/91、`gen-cli --check`、`packages/base-render/**` 零改动、裁定 Q／S／T、W1／W2／W3 结构）**全部成立**。

### 五维分（协议 §5.1）

| 维度 | 满分 | 得分 | 依据 |
|---|---|---|---|
| 契约一致 | 30 | **20** | D1／D2／D3／D5／D6 与 J1／J2／J6／J8／J9 诸判据均成立；**D4 在 390（及 512）档未成立**，且判据自身写错了门槛 ⇒ 扣 10 |
| 证据真实可复现 | 25 | **18** | 本席独立复跑的机器读数与票面 13 条判据运行逐条吻合（`matched=13/13`、全部 `exit=0`）；但「四档都不可能相交」这一结论**不可复现**（真渲染反证）＋ ⑨ 门槛常量非真值 ⇒ 扣 7 |
| 新旧对照 | 20 | **17** | 改前／改后对照齐（D1–D7 逐条、产物级隔离负向对照、m8／m9／m10／m11／m12 变异各必红→还原必绿、W1 逐字节相同搬迁判据）；**缺** 390／512 档的真渲染改前改后对照（只按用户单位算过） ⇒ 扣 3 |
| 工程红线 | 15 | **15** | `packages/base-render/**` 本票零改动（`git diff` 空 ＋ 逐提交 `--name-only` 零命中）；裁定 Q／S／T 无「顺手改别的断言」（Q/S 两条 hunk 外零改动、T 两条纯增、删除行 = 0）；未 push、未 close、未动别席在途件 |
| 文档同步 | 10 | **7** | 证据件 W1–W5 ＋ `GATE-RUN` 声明齐、告警线台账 91/91、`gen-cli --check` 绿；但 W5 证据与件内注释含**一条与真渲染相反的结论**、门禁具名条数不闭合（32 vs 29） ⇒ 扣 3 |
| **合计** | **100** | **77** | |

### 缺陷清单（逐条带归属与级别）

| # | 缺陷 | 归属 | 级别 |
|---|---|---|---|
| 1 | 页 24／26／28／30 在 **390 档**横轴 12 枚日期标签相邻外框 **11／11 相交**（最大 4.98px），截图肉眼可见字形相撞 | **本票范围**（W5·D4 未修尽；改前更糟 ⇒ 不是本票引入的回归） | **S2** |
| 2 | 守卫 ⑨ 的门槛常量 `LABEL_W = 5 × 11 × 0.62 = 34.15` 与**任何一档的真值都不等**（390 档实测 53.9～54.91、1440 档 30.01～30.3）⇒「相邻标签外框不相交」这条断言在 390 档**恒绿** | **本票引入**（⑨ 是本票 W5 新建） | **S2** |
| 3 | `src/analysis/reportDocParts.ts:90` 注释与 W5 证据里「12 点时 46 用户单位 > 标签宽 32 单位 ⇒ **四档都不可能相交**（390 档缩放 0.597 后仍留 ~9px 净空）」与真渲染相反（390 档标签宽 53.9，净空实为 **−4.98px**） | **本票引入**（错误前提 → 错误判据的根因） | **S3** |
| 4 | D7 的「y 轴刻度不许带小数」只改对了代码、**没有判据守**：变异 C 让中刻度真印出 `2000.25` 而 60/60 全绿 | **本票引入**（⑧⑨ 守卫面漏了这一格） | **S3** |
| 5 | 512 档同源相交（引用前席 CDP 读数：页 28 `−6.23px`、页 30 `−4.67px`） | **本票范围** | **S2** |
| 6 | 门禁「未声称运行」具名条数不闭合：本窗实测 32 条 vs §六 具名 29 条（对账工具自指 3 次只具名 1 次） | **本票范围** | **S3** |
| 7 | 「折线顶点数 ≠ 刻度数」无判据（变异 D 观测到 `xlabels=3` 而 `verts=12` 时，红来自等距那条，与顶点数无关） | **本票引入**（⑧⑨ 面） | **S3** |
| 8 | 窗口内 `ed87c79 抢救(防丢)` 把工作区全部未提交改动一次性提交，其中含 `packages/base-render/test/ui-fix-154.test.mjs`（别票的件） | **范围外发现** | **S3** |

### **本票能不能关**：**不能关。**
须再开一个**窄整改窗**（只动页面侧：390／512 档的横轴刻度数／标签形态，或按移动档字号重算补偿算式；
**不许碰 `packages/base-render/**`**），并把守卫 ⑨ 的门槛从「按字宽比估算的常量」换成**按档实测的标签宽**（或直接量真渲染），
否则这一条会一直假绿。（本席按纪律**未改任何产品件**，只给量法与判据。）

## 1 · 机器证据（本席复跑）

全部经 `node tooling/run-locked.mjs --ticket 519 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000 -- <命令>` 持锁跑；
日志落 `.scratch/t519-review2/`。下表「读数」列是**摘要行原文**。

| # | 复跑项 | 命令（持锁） | 读数（摘要行原文） | 结论 |
|---|---|---|---|---|
| 1 | 几何真值（页 24／26／28／30 × 1440／390） | `node .scratch/t519-review2/geom-real.mjs` | 见 §2.1 表（`RE2-geom-real.log`） | **390 档四页全相交、1440 档全不相交** |
| 2 | 分隔符节点级 | `audit-separators.mjs --dir .scratch/t387/html` | `RESULT: 31/31`（exit=0） | 31 页节点级全零 ✔ |
| 3 | 响应式版面 | `measure-responsive.mjs --dir .scratch/t387/html` | `OVERFLOW-ZERO pages=31 cells=93 failed=0 scopeOutFailed=0 label=run`（exit=0） | 31 页 × 3 档 = 93 格逐格 0 ✔ |
| 4 | 十件靶向测试 | `node --test <10 件>` | `ℹ tests 60`／`ℹ pass 60`／`ℹ fail 0`（exit=0） | 与票面声称一致 ✔ |
| 5 | 告警线台账 | `check-warning-line.mjs` | `RESULT: 91/91` ＋ `PASS: 告警线台账齐全且与实况一致`（exit=0） | 台账与实况一致 ✔ |
| 6 | 命令登记生成物 | `gen-cli.mjs --check` | `GEN-CHECK ok …routes.generated.ts sha256=71364217a8b4…`（exit=0） | 生成物已同步 ✔ |
| 7 | 本席自设变异电池② | `node .scratch/t519-review2/mutate2.mjs` | 见 §2.2（`RE2-mutate.log`） | **两抓两不抓** |
| 8 | 门禁反向对账 | `check-gate-audit.mjs --evidence … --log … --ticket 519 --since … --until …` | 见 §2.3（`RE2-audit.log`） | 见 §2.3 |
| 9 | 工程红线：`packages/base-render/**` | `git log --name-only` ＋ `git diff --stat dbaecfd~1..88f2d85 -- packages/base-render` | `git diff` **空**；本票各提交的 `--name-only` 里 **零** `packages/base-render` 行 | 本票改动面零 base-render ✔（详见 §3） |

**如实记一处本席自己的红灯**：第 9 项第一次跑时我写的批处理脚本把 git 参数交给了 `node`（`['-C', '.', 'log', …]`），
`STEP GIT exit=1`（`node -C` 不存在）——是**本席脚本的 bug，不是仓里的问题**；改用包装器直跑
`git log --name-only --pretty=format:"COMMIT %h %ad %s" --date=short -25` 后 exit=0，25 个提交在案（`RE2-gitlog.log`）。

## 2 · 逐条路径（三个矛盾点）

### 2.1 矛盾点一：页 30 图表横轴标签到底相不相交 —— **有真重叠（390 档），1440 档无**

**自己复跑**（`.scratch/t519-review2/RE2-geom-real.log` ＋ `RE2-geom-real.json`；脚本 `.scratch/t519-review2/geom-real.mjs`；
经 `node tooling/run-locked.mjs --ticket 519 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000 -- …` 持锁跑）。
量法＝起 headless Chrome ＋ CDP，`Emulation.setDeviceMetricsOverride` 逐档覆盖视口，读**每个 `.ilife-charts-xlabel` 的
`getBoundingClientRect()`**（浏览器算完 CSS 含媒体查询之后的**真外框**），相邻外框 gap_px = left_j − right_{j−1}；
再用 `getScreenCTM().a` 把 px 换算回 SVG 用户单位做交叉核对。

| 页 | 档 | 刻度 | 屏幕比例 | svg 盒宽 | **生效 xlabel 字号** | 相邻 px 间隙 | **相交对** | 最大重叠 |
|---|---|---|---|---|---|---|---|---|
| 30 看健康趋势 | 390 | 12 | 0.5655 | 328px | **19.4 用户单位** | −3.78 ～ −3.85 | **11 / 11** | **3.85px** |
| 30 看健康趋势 | 1440 | 12 | 1.8103 | 1050px | 10.8 用户单位 | +32.22 ～ +32.93 | **0** | 0 |
| 28 看水分摄入报告 | 390 | 12 | 0.5655 | 328px | 19.4 用户单位 | −4.36 ～ −4.98 | **11 / 11** | **4.98px** |
| 28 看水分摄入报告 | 1440 | 12 | 1.8103 | 1050px | 10.8 用户单位 | +28.60 ～ +29.31 | **0** | 0 |
| 24 看BMI报告 | 390 | 12 | 0.5655 | 328px | 19.4 用户单位 | −4.36 ～ −4.98 | **11 / 11** | **4.98px** |
| 24 看BMI报告 | 1440 | 12 | 1.8103 | 1050px | 10.8 用户单位 | +28.60 ～ +29.31 | **0** | 0 |
| 26 看BMR报告 | 390 | 12 | 0.5655 | 328px | 19.4 用户单位 | −4.36 ～ −4.98 | **11 / 11** | **4.98px** |
| 26 看BMR报告 | 1440 | 12 | 1.8103 | 1050px | 10.8 用户单位 | +28.61 ～ +29.31 | **0** | 0 |

**标签外框真实宽度（用户单位，由外框 px 宽 ÷ 屏幕比例得到，非按字宽比估算）**：
390 档 **53.9 ～ 54.91 用户单位**；1440 档 30.01 ～ 30.3 用户单位。而横轴刻度间距（用户单位）恒为
48.1（页 30）／46.1（页 24／26／28）——**与档位无关**（x 是 viewBox 坐标）。
所以：**48.1 < 54.91，外框在 390 档必然相交**；1440 档字号降到 10.8 用户单位、标签宽 30.3 < 48.1，故不相交。

**肉眼复核**（同一探针顺手截的条带图，deviceScaleFactor=3）：`.scratch/t519-review2/shot2-30-390-xaxis.png`
——390 档页 30 的 12 枚日期标签字形**直接压在一起**（`08-04` 的末位与 `08-09` 的首位互叠、`07-19`／`07-24`／`07-30` 连锁相撞），
不是「只有外框相碰」的理论重叠。1440 档 `shot2-30-1440-xaxis.png` 同一条带清晰分离。

**结论（明确回答）**：
- **390 档：有真实重叠。** 页 30 与 24／26／28 **四页全部** 11/11 相邻对相交，最大重叠 3.85px（页 30）／4.98px（页 24／26／28），
  且**肉眼可见字形相撞**。→ **这是需要再开整改窗的缺陷。**
- **1440 档：没有重叠。** 最小间隙 +28.60 ～ +32.93px，相交对 0。

**三方分歧的成因（本席复核定死）**：
- 实施席「间距 48.1px、相交对 0」——48.1 是 **SVG 用户单位**、不是 px；且它拿 48.1 去比**桌面分支**的标签宽（34.15 用户单位）才对得上「不相交」，
  一比 **390 档真正生效的移动分支**（`19.4 用户单位` 字号）就翻号。**该结论只在 1440 档成立。**
- 视觉席「1440 重叠 0px、390 重叠 5px」——**与真渲染一致**（本席独立复量：390 最大 3.85～4.98px，1440 为 0）。
- 前席「按 SVG 用户单位 gap 48.1 < labelW 60.1 ⇒ 相交 11 对」——**方向与对数正确**（11 对，本席复量确为 11 对），
  但 `labelW=60.1` 是拿 `5 字符 × 19.4 × 0.62 字宽比`**算**出来的，**实测外框宽只有 54.91 用户单位**
  （本席读数：53.9～54.91）。结论不变，数字按实测更正。

**与守卫的关系（要害）**：`analysis-shape-report-520.test.mjs` ⑨ 的 `LABEL_W = 5 × 11 × 0.62 = 34.15`（用户单位）
**既不是 390 档的真值（54.91）也不是 1440 档的真值（30.3）**，它落在两者中间，而真刻度间距 46.1／48.1 恰好也在它右侧
——于是这条「相邻标签外框不相交」的断言**在 390 档看着是绿、实际是相交**。见 §2.2。

### 2.2 矛盾点二：新守卫 ⑧⑨ 的盲区 —— **能抓：刻度枚数越上限／横轴非等距；不能抓：少一枚刻度（仍等距）／y 轴刻度带小数**

**自己设计的变异电池②**（脚本 `.scratch/t519-review2/mutate2.mjs`；日志 `RE2-mutate.log`；json `RE2-mutate.json`）。
做法：**临时**改 `packages/skill-calorie/src/analysis/reportDocParts.ts` 的**唯一锚点** → `tsc -b` 编译 →
**可观测性探针** `obs-probe.mjs`（直调 `buildReportDoc`，证明这次变异真的上了屏、绿不是「改了个没用的地方」）→
跑十件靶向测试（`analysis-shape-report-520` 在内）→ **逐字节还原** → 校 sha256 → 再编译 → 复跑。
四次变异的原件 sha256_16 前后**全部相等**（`ff9828c637fa444a → ff9828c637fa444a`），还原后复跑一律 `tests 60 / fail 0`。

| 变异 | 改哪 | ⑧⑨ 读数 | **实测** | 变异后探针观测（证明上了屏） |
|---|---|---|---|---|
| **A** 横轴少一枚刻度但保持等距 | `CHART_MAX_POINTS 12 → 11` | `tests 60/60 fail 0` exit=0 | **不抓（盲区）** | 30 天页 `xlabels=11`、间距全 `50.8`、极差 `0`、`verts=11` |
| **B** 横轴刻度**超上限** | `CHART_MAX_POINTS 12 → 13` | `tests 59/60 fail 1` exit=1 | **抓**（断言原文：`横轴刻度超过 12 枚（降采样上限失效）：13`） | `xlabels=13`、间距 `42.3` |
| **C** y 轴刻度回到**带小数** | `yMin` 去整并下移 0.5 | `tests 60/60 fail 0` exit=0 | **不抓（盲区）** | y 刻度文本**真的变成** `["1751.5","2000.25","2249"]`（3 天页 `["1427.5","1800.25","2173"]`），xlabels 仍 12 |
| **D** 横轴刻度改**非等距** | `labels:'all' → 'select'`（回到「首＋峰值＋尾」） | `tests 59/60 fail 1` exit=1 | **抓**（断言原文：`横轴刻度不等距：[92.4,415.6]`） | `xlabels=3`、间距 `[92.4,415.6]`、极差 `323.2`、**`verts=12`** |

**结论（逐类定死）**：
1. **能抓**：①横轴刻度**枚数越上限**（⑨ 的 `xs.length <= 12`）；②横轴刻度**不等距**（⑧ 与 ⑨ 各有一条等距断言，
   极差 < 1 用户单位）——D 就是被这一条抓的。
2. **不能抓**：①**少一枚刻度而仍等距**（A：11 枚、间距 50.8、极差 0，全部断言绿）；
   ②**y 轴刻度回到带小数**（C：中刻度真印出 `2000.25`，整批不红）——因为全仓测试面里**只有
   `analysis-shape-report-520` 读 `ilife-charts-xlabel`**，没有任何一件读 `ilife-charts-tick` 的**值**
   （另两件数 tick 的测试只数**条数**，不判字面）；
   ③**「折线顶点数与刻度数不一致」不是「抓不到」，是「没有判据为它负责」**：D 的探针读数里已经**真的出现了**
   `xlabels=3` 而 `verts=12` 的顶点／刻度不一致状态，但那一票红是**等距**那条给的、与顶点数无关——
   换句话说，只要标签恰好落在「峰值居中」那种位置（3 枚既等距又能过 `LABEL_W`），顶点≠刻度这一状态就会**全绿通过**
   （本席此处只给读数与推理，未再构造第二例）。
3. **最要命的那条盲区不在变异里，就在现网产物里**：⑨ 的门槛常量 `LABEL_W = 5 × 11 × 0.62 = 34.15`（用户单位）
   **不等于任何一档的真值**（390 档实测标签宽 **53.9～54.91**、1440 档 **30.01～30.3**）。真刻度间距 46.1／48.1 落在
   `34.15` 右边、`54.91` 左边 —— 于是**「相邻标签外框不相交」这条断言在 390 档恒绿，而 390 档四个页面 11／11 相邻对全部相交**
   （§2.1，含肉眼截图）。A 变异同法：间距 50.8 用户单位 < 标签宽 53.9 ⇒ 390 档仍会相撞，而判据全绿。
   ⇒ **⑨ 现在守的其实是「等距 ＋ 枚数上限」，不是它标题里写的那条「外框不相交」。**

### 2.3 矛盾点三：`RE-audit-W5-strict.log` 的 exit=1 是什么 —— **探针口径问题（用了严格档），不是被审方假绿；但「条数」不闭合**

**自己复跑**（`RE2-audit-strict.log`／`RE2-audit-relax.log`，窗口与票面一致
`--since 2026-09-15T22:05:30.000Z --until 2026-09-15T22:21:00.000Z --ticket 519`）：

| 跑法 | 读数 | 判定 |
|---|---|---|
| 严格档（无放宽开关） | `matched=13/13 auditEntries=366 scoped=45 undeclared=32` ＋ `FAIL: 反向对账：窗口内有 32 条无人声明的 RUN 条目` | **exit=1 FAIL** |
| 具名放宽档（`--allow-undeclared`） | `matched=13/13 auditEntries=367 scoped=45 undeclared=32` | **exit=0 PASS** |

**红在哪**：`tooling/check-gate-audit.mjs` 的**判据②「反向对账」**（源码 `:21`、`:358`）——窗口内存在无人声明的 `RUN` 条目即 FAIL；
与 `matched` 无关。而 **`matched=13/13` 在两种档位下都一样**，说明票面声称的 13 条判据运行**逐条命中窗口且全部 `exit=0`**
（工具 `:191`：严格口径下 `exit !== '0'` 的条目**认领不到**，会被算作 `missing`）——**判据本体是干净的**。

**是口径问题，不是被审方问题**，三条根据：
1. 该放宽是**裁定 N 明文授权**的：「被声称为门禁证据的每次运行逐条写 `GATE-RUN runId=` 且**只领 exit=0**；
   **未被声称的重复迭代轮次**按「类别 ＋ 条数 ＋ 日志指针」具名即可」（证据件 §六 逐字引了原句）。
2. 证据件 §六 **确实具名了**，并留了协议 §6 要求的放宽声明行：
   `GATE-RELAX flag=--allow-undeclared reason=裁定 N＋裁定 I 具名授权：…`（五条类别：22 整改迭代／3 别席在途件编译红／
   1 别席提交／2 杂务提交／1 累积导出，日志指针落 `.scratch/t519/W5-*.log` 与 `.scratch/locks-m158v2/gate-runs.log`）。
3. 严格档对**任何**有迭代轮次的窗口都会红——那是设计行为（`:21`），不是缺陷；前席把严格档的 `exit=1` 亦如实登记，未拿它冒充门禁绿。

**但「条数」对不上机器读数（本席复跑逐条归类，`undeclared=32`）**：编译 `tsc -b` **5**、重出 `run-all.mjs` **3**、
`mutate.mjs m10/m11/m12` 各 **2**（共 6）、`audit-separators` **2**、`measure-responsive` **2**、`t516-判据-版式` **2**、
单件 `analysis-shape-report-520` 测试 **3**、十件测试 **2**、三件测试 **1**、`probes.mjs` **2**、`chart-probe.mjs` **1**、
**对账工具自身 3**（`--export` ／严格档 ／放宽档各一次）。§六 表具名合计 **29 条**，与本窗口实测 **32 条差 3**，差额成因两条：
① 对账工具**自指运行**在本窗口有 3 次，§六 只具名了「累积导出」1 条，另 2 条（严格档／放宽档各一次）**未具名**；
② §六 里的「别席提交 1 ＋ 杂务提交 2」这 3 条属 21:38 起算的**更宽窗口**，在本窗口的机器读数里并不存在，而本窗口的迭代面
实测 29 条 ＞ 具名的 25 条（22＋3）。
⇒ **「类别 ＋ 日志指针」做到了；「条数」没闭合（+3）**——按裁定 N 的口径这是具名不完整，**S3 级**（不影响 13 条判据运行的真实性）。

## 3 · 票面裁定项复核

### 3.1 裁定 Q／S／T 的「4＋2 处」断言改写 —— **成立**（同语义、只加强不删减、原文留底、无顺手改别的断言）

口径：**4 处**＝`analysis-accept-386.test.mjs` 内（Q 两条负向改写 ＋ Q 新增的正面半边 ＋ S 一条改写）；
**2 处**＝`analysis-shape-report-520.test.mjs` ⑦ 两条负向**补正向控制**。**逐条核法＝读提交 diff**（`git show <commit> -- <test file>`）。

| 处 | 提交 | 原文（改前） | 改后 | 同语义？ | 只加强？ | 原文留底？ | 顺手改别的断言？ |
|---|---|---|---|---|---|---|---|
| Q-1（`:337`） | `473e873` | `assert.ok(!rec.html.includes('卡路里 · 报告'), …)` | `assert.ok(!REPORT_PAGE_MARKS.some((m) => rec.html.includes(m)), …)`（红时点名哪一枚） | ✅ 同为「预测页不得落进报告页」 | ✅ 旧串已随形状化消失（原式**恒真空转**），新式判**新形状两枚标记** | ✅ 注释逐字留底 `!includes('卡路里 · 报告')` ＋ 证据件 §一 | ❌ 无（该提交在 accept-386 内只有这几段 hunk ＋ 新常量） |
| Q-2（`:358`） | `473e873` | 同上 | 同上（同法） | ✅ | ✅ | ✅ | ❌ 无 |
| Q-3（正面半边） | `473e873` | —（原来没有） | `for (const h of reportHtmls) for (const m of REPORT_PAGE_MARKS) assert.ok(h.includes(m), …)` | ✅ 新增 **正面半边**，把「会不会空转」变成可执行护栏 | ✅ 纯增 | ✅ 注释写明这是裁定 Q 的落点 | ❌ 无 |
| S-1（`:313`） | `53527df` | `assert.ok(!rec.html.includes('卡路里 · 报告'), …)` | `assert.ok(REPORT_PAGE_MARKS.every((m) => rec.html.includes(m)), …)` | ✅ 同件同句的**对偶条**（原判「不许出现旧串」） | ✅ 旧串已不存在，改判「新形状两枚标记必须齐全」 | ✅ 注释写了旧式原文，且指向 `t519-W5-视觉整改-证据.md` §裁定 S | ❌ 无（该提交在 accept-386 内只有这一段 hunk） |
| T-1／T-2（⑦） | `53527df` | ⑦ 的两条负向断言（`!bmrSafe.includes('⚠️ 危险信号')`／`!proteinNoGoal.includes('未达标')`） | **两条负向原样保留**，各**补**一条同批正向控制（`built.bmr.includes('⚠️ 危险信号')`／`built.protein.includes('未达标')`） | ✅ | ✅ **纯增不改**：`git show 53527df -- …shape-report-520.test.mjs` 里**删除行数 = 0** | ✅ 注释写明「否则上条负向恒真（裁定 T）」 | ❌ 无（同件其余断言未见改动） |

另：裁定 T 里点名**保留**的 `analysis-report-384:130`（`!includes('<!--')`，弱但非恒真）**一行未改** ——
`git show 53527df` 的文件面里没有 `analysis-report-384.test.mjs`（该件只在 W1 的 `dbaecfd` 里动过）✔

### 3.2 结构变更：**W1 一次性落底座＋8 页、W2／W3 改作复核窗** —— **成立**

| 窗口 | 提交 | `--stat` 事实 | 结论 |
|---|---|---|---|
| W1 底座＋8 页 | `dbaecfd` | 9 件：`reportDoc.ts`（底座）＋ `reportDocBmi.ts`／`reportDocParts.ts`／`reportDocScore.ts`／`reportDocTracked.ts`（页族）＋ **三件测试**（`analysis-accept-386`／`analysis-report-384`／`analysis-shape-report-520`）＋ 证据件；`+969 −269` | **一次落到位**：底座与 8 个形态（BMI／TDEE／BMR／蛋白／水分／评分／趋势／对比）在同一提交内接上公共层形状 ✔ |
| W2／W3 | `4a2c6ec` | **只两件文档**：`t519-W2-报告族-证据.md`（71 行）＋ `t519-W3-报告族-证据.md`（70 行）；**源码 0 改动、测试 0 改动**（`+141 −0`） | **复核窗成立**：目标追踪族四页与评分族三页逐族复核，未夹带任何代码改动 ✔ |
| W4 收尾 | `88f2d85` | 只 `t519-GATE-RUN.log` ＋ `t519-W4-收尾-证据.md`（`+237`） | 收尾窗同样零源码 ✔ |
| W5 整改 | `53527df` | 源码 5 件 ＋ 测试 2 件 ＋ 证据件 ＋ GATE-RUN 日志 | 整改落在 W5 单窗，未回改 W1 的产物形状 ✔ |

### 3.3 工程红线：`packages/base-render/**` 零改动 —— **本票成立**

- `git diff --stat dbaecfd~1..88f2d85 -- packages/base-render` → **空**（W1–W4 全程零改动）。
- `git show --name-only` 逐个看本票各提交（`dbaecfd`／`4a2c6ec`／`88f2d85`／`53527df`／`473e873`／`b2efec8`／`6fbc54b`）
  → **没有一行 `packages/base-render`**。
- 如实补一条**范围外观察**：`.scratch/t387/run-all.mjs`（裁定 R 修的那件）与本票的页面侧改法都**没动公共层**；
  但窗口内另有**别票的抢救提交** `ed87c79 抢救(防丢)：提交当前目录全部未提交改动`，其中动了
  `packages/base-render/test/ui-fix-154.test.mjs` 一件（**测试件、非源码**，且不是本票的件）——**不计入本票改动面**，
  但它证明确实存在「别席在途件被抢救提交裹进来」的机制性风险（S3，范围外发现）。

## 4 · 未做项与范围外发现

### 4.1 未做项（如实列，附为什么与下一手缺什么）

| # | 未做项 | 为什么没做 | 下一手缺什么 |
|---|---|---|---|
| 1 | 512／820 档的真渲染几何（本席只自跑了 **390／1440** 两档） | 预算铁律：先把「390 到底有没有重叠」这条生死判据量死；512 档已有前席 CDP 读数（`RE-cdp.log`：页 28 `−6.23px`、页 30 `−4.67px`）可**引用**，方向与本席结论一致 | 若整改窗要在 512 档动刀，需补 512／820 两档的真渲染读数 |
| 2 | 「顶点数 ≠ 刻度数」的第二例构造（峰值居中型取值） | 变异 D 已给出该状态的真实读数（`xlabels=3`／`verts=12`），再构造一例要改夹具、越出「戳守卫」的范围 | 判据面要新增「顶点数＝刻度数」一条时，需一个峰值居中的夹具 |
| 3 | 产品侧修复 | **本席纪律禁止改产品代码** | 整改窗认领 |
| 4 | 全量 `pnpm build` | 本席只跑 `tsc -b packages/skill-calorie`（页族所在包）＋ 十件靶向测试；`.scratch/t387/html/` 的 31 页是 W5 已重出的产物，本席直接在其上量 | 若整改窗改了页族，重出 31 页并复跑 `run-all.mjs` |
| 5 | 31 页产物逐字节重出（`RE-run3.log` 那一路） | 引用前席读数（第三次重出成功、逐页 bytes 与 `.scratch/t387/html/` 对账一致），未重跑 | — |
| 6 | 票面窗口之外的其他族复核（W1 之外的 23 页） | 票面只点名 24／26／28／30 四页量几何；其余由 `measure-responsive`／`audit-separators` 的 31 页面覆盖 | — |

### 4.2 范围外发现（非本票写集，但复核时撞见）

| # | 发现 | 级别 | 说明 |
|---|---|---|---|
| 1 | `ed87c79 抢救(防丢)` 把工作区全部未提交改动一次性提交（含别票的 `packages/base-render/test/ui-fix-154.test.mjs`） | S3 | 与并发纪律「只 add 自己声明的路径」相悖的仓内既有做法；本票未参与，但会影响「红线零改动」这类按窗口核的断言 |
| 2 | 移动档折线图标签补偿（`LINE_TEXT_MOBILE.xlabel = 19.4` 用户单位）在 12 枚日期刻度下**数学上放不下**（12 × 31px 标签 > 328px 绘图宽） | S2 | 本票只是撞上了它；根治要么在页面侧降刻度数，要么在公共层改标签密度策略——**后者属 `packages/base-render/**`，本票红线外** |
| 3 | 全仓测试面里**只有 `analysis-shape-report-520`** 读 `ilife-charts-xlabel`，**没有任何测试读 `ilife-charts-tick` 的字面** | S3 | 图表读数纪律（D4–D7）目前是「一个测试件守四件事」，判据面薄 |
