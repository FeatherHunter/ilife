# #702 收口记（2026-09-18 关闭）

票：`测试面改成打在接口上：页面走真正的交付出口、算式走能力门`（wayfinder:task／map [#699](https://github.com/FeatherHunter/ilife/issues/699)）。
本件是**长期存档**：票面已按本件结论更新并关闭，回执见 issue 评论。

## 结果（第 2、3、4 条交付；第 1 条改归属）

| 提交 | 内容 | 判据 |
| --- | --- | --- |
| `5ee99c94` | 结构门 `scripts/check-one-path.mjs` ＋ 修掉当刻唯一红条（`ALL_ROUTES` 换到正本件） | 自证 3/3 PASS；对树 PASS |
| `079e8faf` | 页面断言门 `scripts/check-page-assert.mjs` ＋ 包内 `AGENTS.md`「测试面口径」三条 | 自证 5/5 PASS；对树 PASS（0 红） |
| `2997c6fe` | 执行状态与下一手 | — |
| `62ddf503` | 复核二轮：症结在门的宽度，不在测试结构（含后果量化） | — |

收尾两绿（关闭当刻复跑）：`check-warning-line.mjs` **RESULT: 93/93 PASS**；`gen-cli.mjs --check` **PASS**（键 134／能力 10／未搬迁清单 0）。

## 第 1 条改归属的理由（两轮复核的结论）

1. **票面前提已作废**：#703 已删 10 个能力门上的 19 个 `run*`；`src/analysis/index.ts:12-14` 逐字写明算式**不出目录**，没有算式门可走。
2. **换缝天花板 8.4%**：测试面 115 条独立深路径里，整条能从包门（63 个值名）拿到的只有 5 条；可换语句 24／286。
3. **取点最密的是生产自己要用的声明面**：`routesFor`(20)／`CALORIE_COMBOS`(19)／`CALIPER_FIELDS`(14)／`dispatch`(18，已认口)——
   测试走深路径是因为**没有对外路径**，不是打错缝。给它们开门属**加门**，与 #703「包门只许收窄」冲突 ⇒ 归源侧裁定。
4. **「只碰内部件」的 29 件 / 87 条**：全是算法正本单测、棘轮件、门面单测——按 `AGENTS.md`「测试面口径」第 3 条**不动**。

## 留给搬迁票的账（量化）

- 搬迁票要动的件被测试直接取：`#704` 已迁 6 件→2 条语句；`#714` 0 条；`#715` 4 条；`#716` 8 条；合计 **14 条语句 / 12 个件**（其中 9 件已 spawn 真 CLI）。
- 全 `src/render/`（31 件）：**16 件 / 74 条语句 / 56 个测试件** 会被搬迁误红。
- 性质：误红**立刻且响亮**（跑测试即红），不静默；生产零风险。承接搬迁票时顺手清理，并在源侧裁定后决定那 109 条声明面取值的去向。

## 过程披露

- `test/` 全程是别席在途写集（测试隔离迁 `ILIFE_CONFIG_DIR`）：本票只写该目录一行 import；为跑门读过该目录多个件（只读）。
- 页面断言门初版口径过宽（把「只断 exit 码」与「只直调 dispatch」都算进判），实测 10 条假红后收紧到「子进程调用 ＋ 断成功」——**假红不许当交付**。
- 清理了两条评论（一条重复发布、一条因 PowerShell 参数编码坏成乱码），关闭回执已用 UTF-8 正文重发；早先那条「下一手缺什么」按 §3.2 第 3 条**写明取代关系**后保留，不静默改。
- 地图 #699：Decisions so far 已追加本票一行、任务清单 #702 打勾、Not yet specified 留一条雾（生产自己要用的声明面要不要开门）。
