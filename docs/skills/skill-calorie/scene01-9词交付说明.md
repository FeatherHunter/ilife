# 场景 01 主页 · 9 条唤醒词交付说明

- 归属：地图 #162《卡路里场景 01 主页》，票 #375《端到端验收 ＋ 肉眼终审》的交付说明件。
- 本件只核验与说明：不改任何代码，不动任何产物。表里每个数字都是 2026-09-14 当刻实测。
- 用词照 `docs/agents/wording.md`。「过程型」「结果型」「预检确认页」是正式词。
- 种子口径：固定种子库 `docs/research/t81-seed.mjs`，`SEED_TODAY = '2026-09-07'`（`t81-seed.mjs:47`）。9 份产物的页头日期逐份都是 2026-09-07，能对上。
- 产物目录：`D:\ilife\.scratch\t375\products\`。该目录被 `.gitignore:5` 的 `.scratch/` 排除，是磁盘上的看样件，不入库（见 §四 第 9 条）。

## 一、9 条链路表（唤醒词 → prompt → 命令 → 产物）

prompt 原文两段，这里逐字合为一行显示，方便对照；可直接复制的整段见本节末尾「可直接复制的 9 段 prompt 原文（逐字）」。

| # | 唤醒词 | 用户要复制的那句 prompt（原文逐字） | 走哪条命令（整句，照 `routes.ts`／`routes.generated.ts` 抄） | 产物绝对路径 | 形态 |
|---|---|---|---|---|---|
| 1 | 看今日主页 | 请你加载技能 卡路里,执行唤醒词「看今日主页」。 我想看今天的主页 dashboard。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。 | `calorie-cmd-read calorie.view.home --params '{"date":"今日"}'` | `D:\ilife\.scratch\t375\products\看今日主页.html` | 结果型完整文档 · 69225 字节 |
| 2 | 看今日饮食概览 | 请你加载技能 卡路里,执行唤醒词「看今日饮食概览」。 我想看今天饮食 widget。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。 | `calorie-cmd-read calorie.view.diet --params '{"window":"今日"}'` | `D:\ilife\.scratch\t375\products\看今日饮食概览.html` | 结果型完整文档 · 69543 字节 |
| 3 | 看今日运动概览 | 请你加载技能 卡路里,执行唤醒词「看今日运动概览」。 我想看今天运动 widget。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。 | `calorie-cmd-read calorie.view.exercise --params '{"window":"今日"}'` | `D:\ilife\.scratch\t375\products\看今日运动概览.html` | 结果型完整文档 · 66670 字节 |
| 4 | 看今日体重概览 | 请你加载技能 卡路里,执行唤醒词「看今日体重概览」。 我想看今天体重 widget。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。 | `calorie-cmd-read calorie.view.weight` | `D:\ilife\.scratch\t375\products\看今日体重概览.html` | 结果型完整文档 · 67553 字节 |
| 5 | 看今日目标进度 | 请你加载技能 卡路里,执行唤醒词「看今日目标进度」。 我想看今天 4 项目标(热量/蛋白/饮水/运动)完成度。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。 | `calorie-cmd-read calorie.view.goal-progress --params '{"window":"今日"}'` | `D:\ilife\.scratch\t375\products\看今日目标进度.html` | 片段 · 1666 字节（归场景 06） |
| 6 | 看本周主页 | 请你加载技能 卡路里,执行唤醒词「看本周主页」。 我想看本周 dashboard。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。 | `calorie-cmd-read calorie.view.home --params '{"windowDays":7,"date":"今日"}'` | `D:\ilife\.scratch\t375\products\看本周主页.html` | 结果型完整文档 · 69225 字节 |
| 7 | 看本月主页 | 请你加载技能 卡路里,执行唤醒词「看本月主页」。 我想看本月 dashboard。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。 | `calorie-cmd-read calorie.view.home --params '{"windowDays":30,"date":"今日"}'` | `D:\ilife\.scratch\t375\products\看本月主页.html` | 结果型完整文档 · 76593 字节 |
| 8 | 看连续记录天数 | 请你加载技能 卡路里,执行唤醒词「看连续记录天数」。 我想看我的连续记录。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。 | `calorie-cmd-read calorie.view.home --params '{"date":"今日"}'` | `D:\ilife\.scratch\t375\products\看连续记录天数.html` | 结果型完整文档 · 69225 字节 |
| 9 | 看今日热量预算 | 请你加载技能 卡路里,执行唤醒词「看今日热量预算」。 我想看今天还能吃多少。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。 | `calorie-cmd-read calorie.view.home --params '{"date":"今日"}'` | `D:\ilife\.scratch\t375\products\看今日热量预算.html` | 结果型完整文档 · 69225 字节 |

命令出处的三个位置逐字一致（本件已逐条比对，未发现分叉）：

- 路由层：`packages/skill-calorie/src/home/routes.ts:11-18`（8 条，场景 01）、`src/weight/routes.ts:8`（`看今日体重概览`，跨家，只接线不实现）。
- 派生件：`packages/skill-calorie/src/triggers/routes.generated.ts:12-20`（场景 01 的 9 条）。
- 冻结表与说明面：`src/triggers/scene-01-home.ts:5-13` 的 `main_prompt.cli` 与 `data_source`、`packages/skill-calorie/SKILL.md:236-244`。

### 可直接复制的 9 段 prompt 原文（逐字）

看今日主页：

```
请你加载技能 卡路里,执行唤醒词「看今日主页」。

我想看今天的主页 dashboard。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。
```

看今日饮食概览：

```
请你加载技能 卡路里,执行唤醒词「看今日饮食概览」。

我想看今天饮食 widget。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。
```

看今日运动概览：

```
请你加载技能 卡路里,执行唤醒词「看今日运动概览」。

我想看今天运动 widget。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。
```

看今日体重概览：

```
请你加载技能 卡路里,执行唤醒词「看今日体重概览」。

我想看今天体重 widget。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。
```

看今日目标进度：

```
请你加载技能 卡路里,执行唤醒词「看今日目标进度」。

我想看今天 4 项目标(热量/蛋白/饮水/运动)完成度。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。
```

看本周主页：

```
请你加载技能 卡路里,执行唤醒词「看本周主页」。

我想看本周 dashboard。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。
```

看本月主页：

```
请你加载技能 卡路里,执行唤醒词「看本月主页」。

我想看本月 dashboard。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。
```

看连续记录天数：

```
请你加载技能 卡路里,执行唤醒词「看连续记录天数」。

我想看我的连续记录。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。
```

看今日热量预算：

```
请你加载技能 卡路里,执行唤醒词「看今日热量预算」。

我想看今天还能吃多少。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。
```

## 二、流程说明

这 9 条是同一个形状，只有命令不同：你对着 AI 说唤醒词（或直接复制上一节末尾那段 prompt）→ AI 按 `SKILL.md` 场景 01 的工作流程，用该词对应的那条 `calorie-cmd-read …` 命令去取数 → 命令渲出一份结果型 HTML 写到磁盘（原始落盘在 `.scratch\t375\products\work-0`～`work-8\calorie_html\`，文件名带当天时间戳）→ 命令回执里 `data.output` 给出这份 HTML 的绝对路径，AI 把该绝对路径原样转给你。你双击那个路径就能在浏览器里看版面。这一步是本图「9 条都跑得通」的判据（① 出文件、② 字节不为 0、③ 回执给绝对路径），三档都由程序判；好看不好看由你肉眼看。

### 本图 0 条过程型（预检确认页）：理由与出处

**结论：本图 9 条一条都不需要过程型，0 条。** 三条机器读数与依据：

1. **冻结表逐条实测**：`packages/skill-calorie/src/triggers/scene-01-home.ts` 的 9 条记录（`:5-13`），`output_type` 字段 9 条全是 `"result"`，`"process"` 0 条。本件用 `Select-String -Pattern "output_type"` 计数得 9，逐条读值确认无例外。
2. **票面记账句**：`docs/skills/skill-calorie/t373-接线-证据.md:38`「本图 9 条全是查询命令，没有会改数据库的命令；过程型 HTML 0 条（记账）。」同一句另见 `packages/skill-calorie/SKILL.md:231`。
3. **地图的括号语**：`.scratch/wayfinder-s1/maps/map-01-主页.md:3` 的 Destination 把「（写类流程先出过程型 HTML）」写在括号里——这一句管的是写类流程；本图 9 条全是查询命令（`kind: 'exec'`，不改数据库），没有写类流程，所以这一句在本图不适用。

复核方法也一样：9 条 prompt 逐条读，没有一条写着「先问／先确认／我确认后再写」。先例是场景 04 的图主做过同一个判断（`docs/skills/skill-calorie/t167-重新设计-新底座.md:132`）。

**如实记一条**：这句「0 条过程型」的记账目前只落在 `t373-接线-证据.md:38` 与 `SKILL.md:231`，**没有单独写进地图 #162 的正文**——地图的「Decisions so far」现在写的是「暂无」（`map-01-主页.md:43`）。先例是场景 04 写进了地图。见 §四 第 7 条。

## 三、能看的产物绝对路径（9 行，逐行可双击）

| # | 唤醒词 | 绝对路径 | 字节 | 完整 / 片段 |
|---|---|---|---:|---|
| 1 | 看今日主页 | `D:\ilife\.scratch\t375\products\看今日主页.html` | 69225 | 完整文档（首行 `<!doctype html>`） |
| 2 | 看今日饮食概览 | `D:\ilife\.scratch\t375\products\看今日饮食概览.html` | 69543 | 完整文档（首行 `<!doctype html>`） |
| 3 | 看今日运动概览 | `D:\ilife\.scratch\t375\products\看今日运动概览.html` | 66670 | 完整文档（首行 `<!doctype html>`） |
| 4 | 看今日体重概览 | `D:\ilife\.scratch\t375\products\看今日体重概览.html` | 67553 | 完整文档（首行 `<!doctype html>`） |
| 5 | 看今日目标进度 | `D:\ilife\.scratch\t375\products\看今日目标进度.html` | 1666 | 片段：以 `<section class="ilife-page" data-slot="ilife:calorie:goal-progress"` 起，无 `<!doctype html>` |
| 6 | 看本周主页 | `D:\ilife\.scratch\t375\products\看本周主页.html` | 69225 | 完整文档（首行 `<!doctype html>`） |
| 7 | 看本月主页 | `D:\ilife\.scratch\t375\products\看本月主页.html` | 76593 | 完整文档（首行 `<!doctype html>`） |
| 8 | 看连续记录天数 | `D:\ilife\.scratch\t375\products\看连续记录天数.html` | 69225 | 完整文档（首行 `<!doctype html>`） |
| 9 | 看今日热量预算 | `D:\ilife\.scratch\t375\products\看今日热量预算.html` | 69225 | 完整文档（首行 `<!doctype html>`） |

**这是当前的生产产物，出的是旧 UI。** 你 2026-09-14 肉眼审这批产物时判「整体 UI 效果不通过」，转成 #401 重做；#401 到今天为止交出的只有设计件与两张样张，**还没有实施到生产产物上**：

- 样张甲：`D:\ilife\docs\skills\skill-calorie\t401-样张-今日总览.html`（34862 字节，完整文档）
- 样张乙：`D:\ilife\docs\skills\skill-calorie\t401-样张-今日总览-空态.html`（28801 字节，完整文档）
- 设计件：`D:\ilife\docs\skills\skill-calorie\t401-融合设计.md`（32070 字节）

判据是机器读数，不是记忆：在 `packages/skill-calorie/src/` 里搜 `t401`，**命中 0**——样张没有接进任何代码，所以上表 9 份仍是旧 UI。你要看新 UI 的长相，现在只能看那两张样张；要看生产产物的现状，看上表这 9 行。**新 UI 要等 #401 实施，现在只有样张。**

## 四、遗漏与缺口

逐条给票号、影响面与谁在推。**9 条唤醒词全部有产物**：完整文档 8 份、片段 1 份、缺 0 份；本件没有新补产物（无需重跑）。

1. **#401 老新融合的 UI 返修未实施（OPEN）**。影响面：上表 9 份产物全部——你看到的还是旧 UI。现状是设计件 ＋ 两张样张（§三 已列路径），生产产物未改。谁在推：地图 #162／#401，卡在等你点头（你点了头才实施到 9 份产物上）。
2. **#427 手机适配缺口（OPEN，公共层）**。影响面：所有卡路里结果型页面，含本图 9 份。三条实测缺口——① 641–820 这一档动作条钉死在 520px 且贴左（`packages/base-render/src/style.ts:311`；820 宽实测动作条 x=20、宽 520，右侧空 260px）；② 动作条 ghost 行没有窄屏档，恒两列平分，手机档两颗按钮上下不堆叠（`src/style.ts:327-332`）；③ 手机档 KPI 卡片没有单列规则、按日表没有 640 档 `white-space: nowrap`（`src/blocks.ts:870-874`、`:920-926`）。谁在推：公共层 `packages/base-render` 侧另立票，本图不动它。
3. **#399 跨家三页的「复制日志」是点不动的禁用态（OPEN）**。影响面：3 份产物——看今日饮食概览、看今日运动概览、看今日体重概览；这三页的「复制数据」点得动，「复制日志」是禁用态（`disabled`、没有 `data-t`）。总览族 5 词已在 #375 内修好，两族形状目前不一致。谁在推：#399，写集归场景 02／04／03 各自的能力目录，本图只记缺陷。
4. **#396 缺口口径与老技能不一致，且展示会误读（OPEN）**。影响面：总览族 5 份产物的横幅提示条（看今日主页、看本周主页、看本月主页、看连续记录天数、看今日热量预算）。种子库里本日 TDEE 2556 ＋ 运动 320 − 摄入 1189 = **1687**，而老技能 ADR-0013 §2 的主页缺口公式条走 BMR 口径（1649 ＋ 320 − 1189 = **780**）；同一页还把「目标 1800 卡」与「缺口 1687 卡」并排，两个数基准不同，人会先做 1800 − 1189 = 611。谁在推：#396（需你裁决用哪个口径，或两个口径各自标名）；#401 只承诺版面上不再并排误导，不动算法。
5. **「看今日目标进度」的产物是片段，不是整页（本图裁决：归场景 06）**。影响面：1 份产物（1666 字节，无 `<!doctype html>`，以 `<section` 起）。本图只核验它跑得通、能取到数；把它做成完整文档归场景 06 的图。产物侧有记账：`.scratch\t375\products\summary.json` 里这条的 `scene` 写的是 `"06"`、`shape` 写的是 `"fragment"`；`.scratch\t375\products\` 另留 `work-goal-progress` 的原始落盘。（另注：`.scratch\t375\` 顶层还留着更早一轮的 4 份同名文件，字节与本次不同，本件只认 `products\` 目录里这 9 份。）
6. **5 条主页词里 4 条的产物实质是同一份，待你裁决（A 接受同页／B 补专门视图）**。影响面：4 份产物——看今日主页、看本周主页、看连续记录天数、看今日热量预算，四件同为 69225 字节。逐行比对：四件只在**生成时间戳那一行**不同（`2026-09-14 17:31:0X · 版本 0.1.0`），**标题四件相同**（都是「卡路里·主页」），h1 也相同（都是「今日总览 2026-09-07」）——所以不是「只差标题」，是「只差时间戳」，请照这个读数理解。原因是 `packages/skill-calorie/src/home/today.ts:30` 的 `optNum(params,'windowDays') ?? 7`：看今日主页／看连续记录天数／看今日热量预算三条都不带窗口参数，默认就是 7 天；看本周主页传的 `windowDays` 也是 7——四条算出同一份数据，所以出同一份文档。只有看本月主页（`windowDays:30`）不同（76593 字节）。对照老技能：`D:\2Study\StudyNotes\SKILLS\卡路里\scripts\render_home.py:535` 有 `--section {diet,exercise,weight,goals,streak,budget}`、`:537` 有 `--period {week,month}`，当年是按 section／period 出专门视图的；新仓把 9 条都并到 `calorie.view.home` 一条命令上。谁在推：等你裁决——A 接受这 4 条同页（现状即交付），B 补回专门视图（另开票）。
7. **地图 #162 自己的记账缺行**。影响面：本图正文。`.scratch\wayfinder-s1\maps\map-01-主页.md` 仍写「进度：5%」、「下一步：只开图」，其「Decisions so far」写「暂无」（`:43`）；本图实际已交出 9 条接线（#373 已关）与 9 份产物，且 §二 那条「0 条过程型」的记账没写进地图正文，只在证据件与 SKILL.md 里。风险是后人把「本图 0 条过程型」当成漏项。谁在推：地图 #162 的所有者，照场景 04 的先例补一行（`t167-重新设计-新底座.md:132`）。
8. **#375 仍 OPEN，肉眼终审的结论是「不通过」**。影响面：本图整体验收。你 2026-09-14 肉眼审 9 份产物后判「整体 UI 效果不通过」并转出 #401（原话逐字抄在 #401 正文的「用户裁决原文」一节）。#375 的终审要等 #401 的新 UI 出来后再复看，本件只是把当下的真实状态摊开。谁在推：#375／#401。
9. **产物住在 `.scratch\` 下，是磁盘看样件、不入库**。影响面：9 份产物全部，以及 §三 的两张样张也在 `docs\` 下（样张入库、产物不入库）。`.gitignore:5` 的 `.scratch/` 把整个目录排除了，所以这 9 份不在 git 里；看效果完全没问题，但清理 `.scratch` 就会丢。要不要把 9 份留档成证据件（挪进 `docs\skills\skill-calorie\`），归你定；本件不动它。
