# #654 · 场景09 读页／过程页／HELP 两页的「复制日志」真接线（证据件）

> 票：**#654 读页复制日志**（第二席）；写集＝`packages/skill-calorie/src/photo/**`（7 件改）＋
> `src/render/wizardPortDocs.ts`＋`test/photo-read-copylog-654.test.mjs`（新）＋
> `test/photo-gif-page-352.test.mjs`（跟签名）＋`scripts/gen-photo-baseline.mjs`（重落）本件。
> 另笔 **#664**（陈旧断言对齐）见 `t664-陈旧断言对齐.md`。
> 时间：2026-09-16 夜（本席位窗口 `2026-09-16T16:17Z` 起；运行条目见 `t654-gate-runs.log`）。

## 一、治的病（负责人 2026-09-16 第二次验收原话）

> 「学习其他 HTML 应该是底部有并排的 2 个按钮『复制数据』和复制日志按钮，现在是有问题的，你检查下」

查明：参考页（`D:\ilife-验收\01…05`）每页底部都是**并排两颗**、连读页都给了真日志；照片族**只有写类
回执**（09-09～09-13）给了真日志，读页／过程页的日志那一格一直靠公共层 **#336 兜底**补一颗
**点不动的禁用占位**。#654 撤掉那条兜底（`base-render/src/controls.ts` 的 `renderActionBar` 只渲染真
存在的动作）之后，这些页底部就只剩一颗——**兜底撤了，真日志没补**。

## 二、改法（只接线、不另造日志格式）

- 落点：**每一页自己给** `copyArea({ data, log })` 双位齐全；日志六段仍走既有
  `shared/copyArea.ts` 的 `copyLog()` ＋ `base-paint` 的 `buildLogText`，第 1 段由 `shared/sceneEnvelope.ts`
  归一（#550，不许拼成 `calorie.calorie.…`）。**没有第二套日志格式**。
- 命令原文（第 4 段）：由**命令层**用共用件 `shared/writeParts.ts` 的 `commandLine(key, params)` 派生，
  页面件不自己拼——十页的命令原文见下表「调用链」列。

| 页面（交付面名） | 装配件 | 场景标识（第 1 段实测值） |
|---|---|---|
| 09-01 看身材照（画廊） | `photo/galleryDoc.ts` | `calorie.photo.list（list）` |
| 09-02／09-03 查身材照（单张两态） | `photo/viewerDoc.ts` | `calorie.photo.detail（detail）` |
| 09-04 对比两张照片 | `photo/compareDoc.ts` | `calorie.photo.compare（list）` |
| 09-05 生成身材照GIF（结果页） | `photo/gifDoc.ts` | `calorie.photo.gif（analysis）` |
| 09-06 记身材照（预检确认页） | `render/wizardPortDocs.ts` | `calorie.view.photo-log-wizard（stat）` |
| 09-07 生成身材照GIF（前置规划页） | `render/wizardPortDocs.ts` | `calorie.view.gif-planner（stat）` |
| 09-08／09-14 删身材照（候选两态） | `photo/pickerDoc.ts` | `calorie.view.photo-picker（list）` |
| 09-15／09-16 看身材照HELP（两态） | `photo/helpDoc.ts` | `calorie.help.center（list）` |

命令层四处接线（`commandLine()` 的调用点）：`photo/gallery.ts`（list／detail 两条）、
`photo/compare.ts`（compare／gif 两条）、`photo/picker.ts`、`photo/wizard.ts`（两页）、`photo/help.ts`。

**顺带一处必要的**：09-02 那张页的动作行里，「复制回画廊指令」原来**借了日志位的冻结 id**
（`ilife-copy-log`）；页尾现在有了一颗**真**日志（必占该 id），同一次渲染内两颗同 id 会让「按 id 找按钮」
分不清谁是谁 ⇒ 改借**数据位** id（`ilife-copy-data`，本页真数据那颗是三格式菜单开合器、不写 actionId）。
判据由 `photo-shape-read-281` 的「同一次渲染内复制按钮 id 须互不相同」把着。

## 三、逐页载荷证据（真跑 CLI，`--html` 落本票产物目录）

跑法（**产物只落本票目录**，不碰交付面）：

```
node tooling/run-locked.mjs --ticket 654 -- node .scratch/t654log/regen-read8.mjs
node .scratch/t654log/probe-pills.mjs .scratch/t654log/before --label 改前   # 当前交付面的副本
node .scratch/t654log/probe-pills.mjs .scratch/t654log/pages  --label 改后   # 本票产物
```

**改前（当前交付面 11 件副本）**：`RESULT: 0/11 件两颗真胶囊＋日志非空 FAIL(11)`——逐件
`胶囊=1[复制数据] … 日志载荷=空`。

**改后（本票产物 11 件）**：`RESULT: 11/11 件两颗真胶囊＋日志非空 PASS`。逐件读数（探针原文）：

| 产物 | 胶囊 | 调用链（第 4 段） |
|---|---|---|
| 09-01-看身材照（画廊·全窗22张·本页4张）.html | 2[复制数据｜复制日志] | `calorie-cmd-read calorie.photo.list --params '{"days":180}'` |
| 09-02-查身材照（单张·大图超1MiB）.html | 2[…] | `calorie-cmd-read calorie.photo.detail --params '{"id":19}'` |
| 09-03-查身材照（单张·大图已内嵌）.html | 2[…] | `calorie-cmd-read calorie.photo.detail --params '{"id":31}'` |
| 09-04-对比两张照片（跨标签·间隔48天）.html | 2[…] | `calorie-cmd-read calorie.photo.compare --params '{"id1":19,"id2":32}'` |
| 09-05-生成身材照GIF（结果页·内嵌GIF）.html | 2[…] | `calorie-cmd-read calorie.photo.gif --params '{"tag":"正面","days":365}'` |
| 09-06-记身材照（预检确认页）.html | 2[…] | `calorie-cmd-read calorie.view.photo-log-wizard --params '{}'` |
| 09-07-生成身材照GIF（前置规划页）.html | 2[…] | `calorie-cmd-read calorie.view.gif-planner --params '{"tag":"正面","days":365}'` |
| 09-08-删身材照（候选页·全窗）.html | 2[…] | `calorie-cmd-read calorie.view.photo-picker --params '{}'` |
| 09-14-删身材照（候选快照·id19）.html | 2[…] | `calorie-cmd-read calorie.view.photo-picker --params '{"id":19}'` |
| 09-15-看身材照HELP（q现找·记身材照）.html | 2[…] | `calorie-cmd-read calorie.help.center --params '{"q":"记身材照"}'` |
| 09-16-看身材照HELP（全量10键）.html | 2[…] | `calorie-cmd-read calorie.help.center --params '{"q":""}'` |

十一件的**行内 disabled＝0、全页 disabled 复制钮＝0**；第 5 段实测形如
`2026-09-17 00:38:41 · 版本 0.1.0`，第 6 段恒 `无`（探针原文见
`.scratch/t654log/logs/probe-after.txt`）。

**09-15／09-16 那个坑（值记一笔）**：这两态共用同一个产物主体
（`卡路里_照片HELP`），#245 的**复用窗口缺省一天**——不给 `--html` 直接跑，第二态读到的是第一态的产物
（本席首跑即栽在此处，测试里因此显式用 `--html` 逐态明说落点）。

## 四、判据读数（机械可判，红绿两向）

| 判据 | 读数 | 运行标识 |
|---|---|---|
| ① 十页底部 ghost 行**恰 2 颗真胶囊**（`复制数据`（菜单算 1）＋`复制日志`）、两颗都不带 `disabled`、全页 0 颗 disabled 复制钮 | `photo-read-copylog-654` 3/3 pass；逐页打印 `09-01 … 09-16 OK` | `runId=c9ff91b9-2c10-4f85-a6bb-b8c148535a10` exit=0 |
| ② 日志载荷非空且含 场景标识／AI 思考链／数据结构／调用链／时间戳版本／异常 六段，**值随页不同**（调用链十一行各一；场景标识＝本页自己的命令键＋形状；`calorie.calorie.` 0 命中） | 同上（逐页断言） | 同上 |
| ③ 不再依赖公共层兜底 | 十页的按钮**由页面自己给**（`copyArea({data,log})`）；页面目录内不再出现 `dataCopyArea('复制数据', …)` 这种只有数据位的调用（`grep` 复核：`src/photo/**` 只剩 `helpDoc.ts` 的行内手写胶囊，与页尾复制区无关） | 编译 runId=`da44d5b5-a2f9-440a-8f85-9d175e0ad38f` exit=0 |
| ④ **变异自证（源码级）**：把 `galleryDoc.ts` 的 `copyAreaOf()` 里 `log:` 那块摘掉 | 红行：`MUTATION-RED 摘掉 09-01 的 log ⇒ 判据① 必红：exit=1 命中「恰 2 颗复制胶囊」=true 摘要=AssertionError: 09-01 看身材照（画廊）：底部 ghost 行应恰 2 颗复制胶囊，实测 1 颗 [复制数据]` | `runId=4d61a24b-3ef9-40b5-904b-fdc30a1fa9af`（窗口内直调编译器） |
| ④ 同上，逐字还原 | 绿行：`MUTATION-GREEN 逐字还原 ⇒ 必绿：exit=0（文件 sha256=ad74ac57f9b8069a…）`；`RESULT: 变异两向 PASS` | 同上 exit=0 |
| ⑤ 相关门全绿 | `node --test` 21 件：**tests 171 / pass 171 / fail 0**（含点名九门 ＋ 本票新钉 ＋ #664 对齐后的四件 ＋ 相邻照片族／复制件） | `runId=47ccf3ff-7580-4bc2-9671-8609f12f6ec4` exit=0 |
| ⑥ 三档零溢出 | `OVERFLOW-ZERO pages=11 cells=33 failed=0 label=t654log-10pages`（390／768／1440 三档，逐页 `0 ✓`） | `runId=f8595b66-341e-4ae5-9667-ae059c1ea3f0` exit=0 |
| ⑦ 十页基线（规范化正文 sha256） | `--write` → `BASELINE-WROTE 10 pages`；`--check`×2 → `BASELINE-OK 10/10`（连跑两次，稳定非运气） | `5c935128-9bcc-4d3b-8b54-e9db31e272dc`／`da409df8-2f0d-4885-bca2-d1a70106a5d3`／`ce5fc1c9-13fb-4a77-87d8-fed81ee0cb73` 均 exit=0 |
| 产物重出 | `RESULT: 11/11 页重出到 .scratch/t654log/pages`（含 HELP 两页） | `runId=fe3a4f80-d170-46a0-a07f-b70a011d5c86` exit=0 |

**基线重落的成因（逐页点名，别只抄数字）**：`gen-photo-baseline.mjs` 的十页里，**六页**的漂移是本票
改动（`list`／`detail`／`compare`／`gif`／`gif-planner`／`photo-log-wizard`——复制区多一颗真日志，
页面正文规范化后必然变）；**另四页**（`help.center`／`photo.add`／`photo.remove`／`photo.tag`）的漂移
**不是本票引入**：`b126b743`（#654 撤窄屏单列，改的是公共层样式段）落在上一席重落（`b0374649`）**之后**，
把十页的正文全体改过一次。本票一并以 `--write` 归零（`--check` 两次全绿），此处如实登记，免得日后
有人拿「十页全变」反推成本票的成因。

## 五、未做项与风险（如实）

1. **台账两行的落点（已核实，不是欠账）**：`packages/skill-calorie/AGENTS.md` 里本票那一行
   `| \`src/photo/gifDoc.ts\` | — | 393 | …|`（原 365）与别席那一行
   `| \`src/diet/nutritionPortDocs.ts\` | — | 771 | …|`（原 755），**已由 `1d5cc25f`（feat(647)，00:32）**
   随 `--sync` 一并落库；本席交付时该文件工作区干净、`check-warning-line.mjs` = `RESULT: 91/91 PASS`。
   本席按编排者第 3 条**没有**把这行卷进本票提交；行内「超因与拆法」由本席补写（见下一条），
   与台账「改这张表就是改台账」的口径一致。
2. **超线报警（必报五步第四步）**：**「已超线，需要根据规则进行重构。」** `src/photo/gifDoc.ts` 现
   **393** 行（本票前 365）。超因：这一件同时住着「GIF 合成的编排（帧序／降级／落盘／超限改提示）」与
   「整页装配（三格 KPI／事实条／舞台／明细表／复制区）」两摊活，本票在它身上补复制区双位（命令原文入参
   ＋ 复制区小件 ＋ 注释）。**本次先不拆**：本票写集只做「日志接线」，同件重排会把「产物逐字节可解释」
   这条判据搅成两条变更的合成读数，且 `GifPageResult` 的四个读数出口归 #352 的门钉着；**拆法**＝按
   「合成／装配」切两件姊妹件（`photo/gifSynth.ts` 留 `synthOf`／`gifFileNameOf`／`byFrameOrder`／
   `stageHtml`／`GIF_SUB_DIR`，`photo/gifDoc.ts` 只留页装配与复制区），两件之间只过 `Synth` 与
   `GifPageData`、无共享状态；同族先例照 `sportDocs.ts`＋`sportUi.ts` 那次一分为二；待收口票认领。
   （这段与台账该行「结论」列同文。）
3. **HELP 两页的两态共用产物主体**（#245 复用窗口缺省一天）：调用方若不给 `--html` 逐态明说落点，
   第二态会读到第一态的产物。本票**没有**动复用窗口（那是 #245 的口径），只在测试与重出脚本里
   显式传 `--html`。
4. **范围外发现（记账，非本票引入）**：`packages/skill-calorie/test/photo-helpdoc-488.test.mjs` 的
   `ROWS_LEAD` 常量写死的是 #529 中间态那句清单读法，实现后来改过词 ⇒ 该断言恒 0 命中（读数从
   「恰一处」变成「0 处」）。已在 #664 另笔一并换对象（改成按当刻实现派生）。
5. 交付面 `D:\ilife-验收\scene09\` 与墙批次 `.scratch/t351-preview/wall/` **一字未动**（编排者统一重出）；
   本票产物全在 `.scratch/t654log/`（`pages/` 产物、`before/` 改前副本、`logs/` 日志、四个脚本）。
