# #521 链路总览页整改 ：视觉复评

判定：**过**（总分 **92／100**，过 90 线）。机器判据见 `t521-证据.md`。本件是视觉席（`dsh --profile visionreview`）对着**冻结产物**出的分与逐条结论。

> 口径出处：本仓 `docs/agents/视觉验收墙.md` 第 185 行那条坑 —— **视觉模型是提示器，不是证据**。所以本件里凡引用视觉席读数的地方，都用 `node` 逐条回源码／回 DOM 量过一遍，量不上的写清来源。视觉席自己的两条读图指控经实测**驳回**，记在「不成立」段，不按读数编缺陷。

## 一、产物身份（本件所有读数都对着这一个版本）

| 项 | 冻结界值 | 视觉席量到的值 | 我独立复量 |
|---|---|---|---|
| 页面 | `docs/skills/skill-calorie/t268-链路总览.html` | 同 | 同 |
| 字节 | 89,185 | 89,185 | **89,185** |
| mtime | `2026-09-15 13:03:19` | 同 | 同 |
| SHA256 | `C1468363…FAEB2`（全值见 `t521-证据.md`） | 同 | `C14683634CFB36C3844883A0DB55FB3777BF17A8C26911DAE9889FE99F7FAEB2` |

三项逐位一致 ⇒ 评的就是冻结版。18 张图**全部晚于** 13:03:19 重拍（最早 13:40:33）。旧的 `artifacts\t521-structure-view\` 三张（13:03:11／13:03:23／13:03:26）时间早于冻结时刻，属上一版，**未取用**。

视觉席那份报告落 `.scratch\t521\视觉报告-冻结版.md`（22,475 字节，mtime `13:47:56`，SHA256 `D79BB46C…AB98`）。`.scratch/` 在 `.gitignore` 里，按仓规**只作工作件留在盘上**，读数抄进本件。

### 两次取分的经过（照实记）

| 次 | 命令 | 结果 | 说明 |
|---|---|---|---|
| 1 | `dsh --profile visionreview "read .scratch\t521\视觉brief.md and follow it exactly"` | 收尾给 `VISION-SCORE=93 PASS`，但**报告文件没落盘** | 日志 `.scratch\t521\vision-3.log` 尾行是「All evidence gathered. Writing the report:」——在写报告那一步被截断，分只有日志，没有正式评审件，不能当交付 |
| 2 | 同命令（brief 加了 §七「先落地、后写报告。一批拍完、源码一次抓」） | `VISION-SCORE=92 PASS`，报告落盘 22,475 字节 | 日志 `.scratch\t521\vision-4.log`。**正式分取这一次**：身份三项重新核过、18 张图在这一次全拍、报告在盘上可查 |

两次都判 **PASS**，差 1 分来自手机档那处三列挤（第 1、4 次各自扣法不同）。**正式分以第二次为准**，因为只有它留下了可核的评审件。后端全程可用，没有出现 `TRANSPORT`／`VISION_BACKEND_UNAVAILABLE`，因此没有走「后端不可用未评分」那条出口。

## 二、总分与五维分

**总分 92／100 —— 过。**

| # | 维度 | 权重 | 得分 | 一句理由 |
|---|---|---|---|---|
| 1 | 结构纪律 | 25 | **24** | 39 条是真 `<article class="lc">`（240 行）不是伪表（全文无 `<table>`）。命令块 `pre.cmd` 带 `white-space:pre;overflow-x:auto`（89 行）三档实测都横滚不竖排。可点目标三档实测全 44px。三档 `scrollWidth == clientWidth`、卡内元素越界 0 处。断点取 820／500（137／146 行）。只扣「次按钮白底＋蓝描边」这半条底色。 |
| 2 | 信息层级与版式细调 | 20 | **19** | 字号字阶 10.5→26 单一路径、圆角 6/10/12/16、阴影单一 `--shadow`、同义同色。两列网格实测 `568px 568px` ＋ 12px 间距、39 张卡逐行对齐。数字卡／比例条／分布条／分段条都在承载信息（183／184／196 行）。只扣 390 档 ② 三张 step 卡被挤成竖条。 |
| 3 | 分隔符与文字纪律 | 20 | **19** | 上屏文本的间隔点（U+00B7）0 个、全角分号（U+FF1B）0 个（`•・‧∙` 这几个同形字也是 0），间隔点只活在 193 行的 `title=` 提示属性里。无 `——`／`…`／连缀标点、无重复印刷的路径行。行话都配了人话（597–602 行）。只扣 201／226 行一句语义重复。 |
| 4 | 跨宽自适应 | 20 | **17** | 1440 两列／768 单列 `736px`／390 单列 `366px` 成立。三档零横向溢出、零越界、零省略号截断。命令块三档都能横滚。吸顶工具条（120 行）锚点跳转后不压内容。扣 ≤820 规则集**漏收 `.steps`**（390 档仍三列），另计 390 档 139px 吸顶条占视口 16% 的代价。 |
| 5 | 手机端与 HELP 同档 | 15 | **13** | 按钮热区实测 44px（390 档两个按钮 44×164、44×166）。正文 13.5px，卡片纵向节奏稳（卡高 269–372px ＋ 12px 间距）。断点与仓内 help 模板同为 820／500。扣 `.steps` 三列不塌（段内正文 87px）＋ 命令 12.5px（154 行）低于 13px 下限。 |

合计 **24＋19＋19＋17＋13 ＝ 92**。

### 我自己复量过的几个硬读数（不转抄视觉席）

| 读数 | 复量方式 | 我的读数 |
|---|---|---|
| 三档横向溢出 | `node .scratch\t268-link\check-page.mjs` | 390／768／1440 三档 `scrollWidth − innerWidth = 0`，`CHECK-PAGE verdict=PASS ok=11`，exit 0 |
| 39 张卡与字节同源 | `node docs\skills\skill-calorie\t268-链路总览.build.mjs --dry` | `bytes=89185 cards=39`，39 份 65,028–121,054 字节、合计 2,977,349，与页上印的逐条一致。`--dry` 不落盘，页面 mtime 仍是 13:03:19 |
| `.steps` 三列 | 读源码 61 行 ＋ 看 137–145／146–160 两块规则 | 61 行 `repeat(3,minmax(0,1fr))`。820 块只收 `.grid`(138)／`.stats`(139)／`.stat`(140)／`.jumpbar`(143)／`.kv .k`(144)，**没有 `.steps`**。500 块（146–160）也没有。⇒ 视觉席这条**成立** |
| 命令字号 | 读源码 89 行与 154 行 | 89 行 `font-size:13px;line-height:1.7`。154 行（≤500 块内）`font-size:12.5px`。⇒ **成立**，比 13px 下限低 0.5px |
| 390 档越界元素 | `metrics-cdp.json` | 只有 1 个：`A.@425`，即 `.jumpbar` 里的锚点 —— 它属于第 27 行那个**行内横滚容器**，页面本身 `390 == 390`，不是页面横滚 |
| 18 张图 | `Test-Path` 逐张 | 18 张全部存在，mtime 13:40:33–13:45:27，**全部晚于**冻结 mtime |

## 三、三档逐档发现

| 档 | 版面 | 发现 |
|---|---|---|
| 1440×900（整页 8808px） | 两列，网格 `568px 568px` ＋ 12px 间距 | 卡片流整齐、逐行对齐。三张大数字卡横排。分布条／分段条清楚。卡下缘绿红胶囊带「展开看原因」。**未发现未达标项** |
| 768×1024（整页 13966px） | 单列 `736px` | 栅格按时塌成单列，工具条 87px 与 1440 档同高、**没有被挤成多行**（视觉席曾读图怀疑，DOM 实测驳回）。**未发现未达标项** |
| 390×844（整页 16089px） | 单列 `366px` | 卡片、按钮、命令块、胶囊都成立。**两处未达标**：② 「怎么用」仍是三列（每列 115.328px、段内正文只剩 87px，第三张段落被撑到 178px）——同页 `.stats` 在这一档已经是单列，两者手感不一致。命令字号 12.5px（154 行）。另记：吸顶工具条 139px，占 844px 视口 16%，手机档压得偏重 |

12 条必改项：**逐条「已改」，0 条「未改」**（第 11、12 条各留一条轻量保留，已折进上面两处扣分）。逐条证据（系统源码行号＋三档实测数字）在 `.scratch\t521\视觉报告-冻结版.md` 第四节，本件只收结论与扣分，不整段转抄。

## 四、未采纳建议（与已批准设计冲突，不当缺陷扣分）

| # | 建议／直觉判断 | 未采纳的理由 |
|---|---|---|
| 1 | 「命令块右边被切＝缺陷」 | 票面第 6 条＋brief 第五节：「不换行＋横向滚动是刻意设计，右边被切不是 bug」。实测支撑：三档 `scrollWidth/clientWidth = 681/480、681/648、655/278`，`white-space:pre` 与 `overflow-x:auto` 同在 89 行 |
| 2 | 「卡片底部 `<noscript>` 那行绝对路径算重复印刷」 | 已批准的无脚本兜底。脚本开着时不上屏（`innerText` 里 `.scratch` 命中 0 次），所以第 3 条必改项照样成立 |
| 3 | 「390 档页头锚点胶囊行右端被切＝元素被裁」 | 第 27 行 `.jumpbar` 是行内横滚容器（`overflow-x:auto`，28 行隐藏滚动条），实测它自己横滚 61px（`scrollWidth 399 / clientWidth 338`），页面 `scrollWidth == clientWidth == 390`。且已批准「锚点行不吸顶」 |
| 4 | 「不留表格＝信息密度不够」 | 已批准：全宽卡片流，桌面两列、≤820 单列 |
| 5 | 「没联网字体／没有 `<link>`」 | 零外部依赖是已批准设计。`<link>`／`@import`／`http(s)://` 全文零命中，唯一的 `file:///` 是产物链接本身（245 行） |
| 6 | 「吸顶工具条本身不该有」 | 已批准第 4 节清单工具条吸顶。只核它压不压内容 —— 锚点跳转后实测 h2 0–21／注脚 31–51／工具条 63–150／首卡 162–403，互不相压 |

## 五、视觉席读到、实测**驳回**的两条（不记缺陷）

| 视觉席读图指控 | 实测 | 结论 |
|---|---|---|
| 768 档「工具条被挤成多行」 | `.lt-in` 单行高 46px，搜索框 top 2967 与胶囊行 top 2966 同一行。工具条总高 87px，与 1440 档一致 | **不成立**（缩图读数误差） |
| 390 档「族徽章越出卡片内边距」「念不通胶囊左边被前一元素压住」 | 按卡逐元素实测 `.lcn/.lcw/.fam/.btn/.bnum/.bbar/.pill/.self/.cmd/details`：越出卡内容右边界 0 处、同行重叠 0 处、省略号截断 0 处 | **不成立**（窄档缩图读数误差） |

## 六、截图绝对路径清单（18 张，全部晚于冻结 mtime）

### 整页图（3 张）

1. `D:\ilife\.dsh-vision-router\artifacts\D--ilife-docs-skills-skill-calor-f046040a-shot-1440x900-fullpage.png` — 1440 档整页（1440×8808）
2. `D:\ilife\.dsh-vision-router\artifacts\D--ilife-docs-skills-skill-calor-f046040a-shot-768x1024-fullpage.png` — 768 档整页（768×13966）
3. `D:\ilife\.dsh-vision-router\artifacts\D--ilife-docs-skills-skill-calor-f046040a-shot-390x844-fullpage.png` — 390 档整页（390×16089）

### 分段图（12 张，不缩放）

1440 档（段界＝源码实测小节边界 ① 结束 2004／② 结束 2713／④ 中点 5571）：

4. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-1440-A.png` — 段 A：y 0–2004（页头 ＋ ① 一眼总览）
5. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-1440-B.png` — 段 B：y 2004–2713（② 怎么用 ＋ ③ 已知遗漏）
6. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-1440-C.png` — 段 C：y 2713–5571（④ 卡片上半，含吸顶工具条）
7. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-1440-D.png` — 段 D：y 5571–8808（④ 卡片下半 ＋ ⑤ 口径与出处 ＋ 页脚）

768 档（段界 2126／2895／8241）：

8. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-768-A.png` — 段 A：y 0–2126
9. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-768-B.png` — 段 B：y 2126–2895
10. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-768-C.png` — 段 C：y 2895–8241
11. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-768-D.png` — 段 D：y 8241–13966

390 档（段界 2278／3245／9415）：

12. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-390-A.png` — 段 A：y 0–2278
13. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-390-B.png` — 段 B：y 2278–3245
14. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-390-C.png` — 段 C：y 3245–9415
15. `D:\ilife\.dsh-vision-router\artifacts\t521-review\seg-390-D.png` — 段 D：y 9415–16089

### 单卡特写（2 张）

16. `D:\ilife\.dsh-vision-router\artifacts\t521-review\card-1440-closeup.png` — 1440 档单卡（600×282。序号圆徽／族徽章／命令块／两个按钮／字节比例条／状态胶囊／页首自称）
17. `D:\ilife\.dsh-vision-router\artifacts\t521-review\card-390-closeup.png` — 390 档单卡（382×302）

### 吸顶工具条（1 张）

18. `D:\ilife\.dsh-vision-router\artifacts\t521-review\sticky-1440-scrolled.png` — 1440 档滚到 `scrollY=5571`（视口 1440×900），看工具条吸住时压不压卡片内容

### 判读辅助（不计入 18 张）

390 档整页图 390×16089 超过读图侧长上限，改成从同一批不缩放分段里切 1:1 窗口读，另用 DOM 实测兜底：

- `D:\ilife\.dsh-vision-router\artifacts\t521-review\aid-390-top.png`（390×1500，y 3245–4745）
- `D:\ilife\.dsh-vision-router\artifacts\t521-review\aid-390-mid.png`（390×1500，y 8000–9500）
- `D:\ilife\.dsh-vision-router\artifacts\t521-review\aid-390-bottom.png`（390×1130，y 14959–16089）
- `D:\ilife\.dsh-vision-router\artifacts\t521-review\aid-768-top.png`（768×1500，y 2895–4395）
- `D:\ilife\.dsh-vision-router\artifacts\t521-review\aid-768-bottom.png`（768×1166，y 12800–13966）
- `D:\ilife\.dsh-vision-router\artifacts\t521-review\metrics-cdp.json` — 三档 DOM 实测（小节边界／网格列数／命令块 scrollWidth 与 clientWidth／可点目标高度／可见文本标点计数／横向溢出元素清单），本件第二节的复量取自它
- 视觉席自己的整页重拍副本：`D:\ilife\.dsh-vision-router\artifacts\t521-review\cdp-fullpage-1440.png`（910,522 字节，与上方第 1 张逐字节同尺寸）

`.dsh-vision-router/` 在 `.gitignore` 第 8 行，故以上路径**不入库**，只作盘上证据。

## 七、扣分项与遗留（照实记，不藏）

1. **390 档 ② 「怎么用」三列不塌** —— 本件最重的一处扣分（跨宽自适应 −1、手机端 −2 里的主要一块）。源码事实：`.steps` 在第 61 行写死三列，而 820／500 两块规则都没收它。`pkg=base-render` 的 help 模板在窄档是收紧的，本页这一处与它不同档。
   **本票不修**：票面把本席的活儿定在「拿分＋出评审件」，分已到线（92 ≥ 90）。这处属**页内呈现**，改它要动生成器重出页、重跑自检、重取分，是另一轮。**留给收口票**。
2. **命令字号 ≤500 档 12.5px**（154 行）低于票面「13–14px」下限 0.5px —— 同样留给收口票。
3. **390 档吸顶工具条 139px** 占视口 16% —— 已批准要有，只作观察项，并入维度 4 的扣分。
4. **次按钮 `.btn.ghost` 白底＋1px 蓝描边**（100 行）在纯白卡上可辨性偏弱，热区实测 44px 达标 —— 半条底色，并入维度 1 的扣分。

## 八、口径出处

- 视觉验收墙与「视觉模型是提示器，不是证据」：`docs/agents/视觉验收墙.md`（§7 坑表第 185 行）。
- 分隔符：`.scratch/sep-audit/probe.mjs`（口径见 issue #508）。溢出：`packages/skill-calorie/scripts/measure-responsive.mjs`（390／768／1440，读数看 `overflow === 0`）。
- 手机端同档：`packages/base-render/assets/help-template.html`（断点 820／500）。
- 用词：`docs/agents/wording.md`。
- 机器判据读数、变异自证两行、九步验收队列：`t521-证据.md`。
