# #319 对抗式复核报告 —— 场景 10「分析」搬迁（窗口 A：声明 ＋ 13 `case` ＋ 169 条路由记录）

- 受审提交：`cbb5da8`（`feat(319): 场景 10「分析」搬迁 —— 13 键住 src/analysis/（声明 ＋ 门 ＋ 路由记录）`）
- 复核席：`#319r`（对抗式复核）。**复核时点**受审提交为 `cbb5da8`，当刻工作树 `HEAD = 0354944`（`cbb5da8` 之后仅一条 docs 提交，受审 10 件逐字节未再变；`git diff --stat cbb5da8 HEAD` 只列 `docs/skills/skill-calorie/t315-搬迁-证据.md`）。**本报告落库时** `HEAD` 已前进（其间别人落了 `a39b86a` 等票），故「当刻」一律指复核窗口内的读数，运行留痕见下方 runId；受审 10 件在本报告落库前的最后改动仍是 `cbb5da8`。
- 复核窗口：`node tooling/run-locked.mjs --ticket 319r --lock-dir .scratch/locks-land --stale-minutes 30 -- pwsh -NoProfile -File .scratch/t319r/gate.ps1`
  - runId `e28c71c1-4642-4a70-b519-cba5ace7b294`（waitedMs=10010，exit=0）
  - runId `9ecfac02-83d8-4b7e-a8b6-03673302b2be`（waitedMs=20017，exit=0）
  - runId `23b69774-a24c-4ef7-98db-bb124569cbd1`（waitedMs=0，exit=0，第二轮）——三轮收尾 `TREE=空`
- 复核席口径提醒：本机 `Get-Content -LiteralPath` 读数不可信（对同一 13 行文件返回 6 行），本报告的行数一律用 `[System.IO.File]::ReadAllLines().Length` 或 git numstat；键名判定一律**只看记录行**（`^\s*\{ list:`，规则 25）。

## 判定

**FAIL（78／100）** —— S1=0、S2=3、S3=5。代码实质面（声明／分派／跨片删除／生成物／行为）逐维全绿且经得起逐字节对抗；**扣分全在证据与对账面**：证据件自己写明的门禁审计命令当刻 **exit 1**，证据件三处数字与当刻事实不符，且本提交在 `cmd_read.ts` 里留下了 2 个**本票新造成的未用 import 绑定**（仓内未配 `noUnusedLocals`／`noUnusedParameters`，任何门都不会红）。

---

## 逐维结论

### 维 1 · 声明面 —— **PASS**

| 判据 | 实测 | 结论 |
|---|---|---|
| `src/analysis/commands.ts` 恰 13 条声明 | 以 `^\s*\{ kind:` 计 = **13**；键名与票面 13 键逐字相同 | PASS |
| 字段齐（六件事） | 13 条全有 `kind`／`key`／`shape`／`title`／`run`／`example`；`wakeWord` 缺 2 条（`view.predict`／`view.anomaly`），与改前**逐字一致**（见下） | PASS |
| 与 `cli/keys.ts` 镜像 | 13/13 `shape`＋`title` 全等 | PASS |
| 与 `combos.yaml` 镜像 | 13/13 有条目，`shape`／`title` 同值 | PASS |
| 与 `registry.ts` 镜像 | `registry.ts:10,20` 引 `ANALYSIS_COMMANDS`；`build()` 有重复键抛错；`pnpm gen:check` PASS（能力 8 个含 `analysis`） | PASS |
| 搬迁保真 | 改前 `legacy/scene-10.ts` 13 条声明 × `shape`／`title`／`wakeWord`／`example` 四字段 **13/13 逐字相同**（脚本 `scan-dispatch.mjs`） | PASS |
| 全仓唯一 | 每键 `commands.ts` 声明 **1 处**、`cli/keys.ts` **1 处**、`combos.yaml` **1 处**；`predict`／`anomaly` 的「第 2 处」是 `commands.ts:219` 头注释里的键名（**注释不是声明**） | PASS |

证据（节选，`node .scratch/t319r/scan-mirror.mjs`）：
```
commands.ts 声明行数 = 13；keys.ts 条目数 = 101；combos.yaml 条目数 = 113
OK calorie.view.calorie-trend  shape=stat title=热量趋势 wake=看热量趋势
…
OK calorie.view.predict  shape=stat title=体重预测 wake=(缺)
OK calorie.view.anomaly  shape=stat title=异常诊断 wake=(缺)
有问题的键 = 0/13
```

### 维 2 · 分派面 —— **PASS**

- 13 键的 `case` 在 `packages/`＋`test/`＋`tooling/` 全仓 = **0 处**；13 个处理函数符号只出现在 `src/analysis/commands.ts`（`viewAnomaly` 另有 `analysis/exercise.ts` 的 `ReviewAnomaly` 接口名，非实装）。
- `cmd_read.ts` 本票 13 个 `case` **删净**：`calorie.*` case 由 **24 → 11**（`PRE 24／POST 11`，脚本 `scan-diff-imports.mjs`），现存 11 条全是 `profile-*`／`goal-*` 的老键，无一属本票。
- `legacy/scene-10.ts` **仍存在**（13 行，`export const LEGACY_SCENE_10: readonly LegacyCommandDecl[] = [];`），**记录行零残留**（该件头注释里也不出现任何 `calorie.*` 键名）。

### 维 3 · `cmd_read.ts` 的未用 import 绑定 —— **FAIL（S2）**

**（a）票面自述的数字错。** 证据 §① 第 18 行写「随之未使用的 **25** 个 import 绑定」；实测 **29**：
```
=== PRE/POST 导入段绑定集合（git show cbb5da8^ / cbb5da8）===
PRE  绑定=99   POST 绑定=70
删掉的绑定（29）：getCalorieHistory, buildPredictView, …, assertISO, latestFoodDate, needStr, todayISO
新增的绑定（0）：（无）
```
`29 − 4 ＝ 25` 的巧合指向漏算一处：`assertISO`／`latestFoodDate`／`needStr`／`todayISO` 四个都在被改写的 `../shared/params.js` 那一行里。

**（b）残留是实的，且本票新造了 2 个。**
```
PRE  未用绑定（7）：buildReviewDoc, buildReviewView, buildProcessProgressView,
                    buildProcessProgressDoc, buildContraDoc, renderPlanHtml, renderPlanWizardHtml
POST 未用绑定（9）：上述 7 ＋ anchorOf, windowRange
其中「本票造成的新未用」：2 -> anchorOf, windowRange
```
`anchorOf`／`windowRange` 原为被删的 13 个 `case`（`view.long-trend`／`view.six-factors` 等）在用；本票删 `case` 时**只从 `shared/params` 那行拿掉 4 个**，留下这两个。`packages/skill-calorie/src/cli/cmd_read.ts:62` 该行其余 6 个绑定（`dayField`／`defaultRange`／`fail`／`nums`／`optNum`／`optStr`）仍在使用。

**（c）为什么全绿**：`tsconfig.base.json` 只有 `"strict": true`（全文 15 行），仓内**没有任何** `noUnusedLocals`／`noUnusedParameters`（`grep` 全仓 tsconfig 仅命中 `strict`）；包内 `packages/skill-calorie/tsconfig.json` 只设 `outDir`／`rootDir`。⇒ `tsc -b`／`pnpm gen`／`pnpm test` 全绿，**静默残留**。判 **S2**（本票亲手造成的死代码 ＋ 与自述口径互相矛盾）。

### 维 4 · 跨片删除的正确性 —— **PASS（删除面）＋ FAIL（记账面，见 S2-3）**

逐行核 169 行（`node .scratch/t319r/scan-routes.mjs`，只认记录行）：
```
记录数：scene-10.pre=168 post=1 | scene-02.pre=8 post=6 | analysis/routes=169
① 实删记录行 = 169（scene-10 167 ＋ scene-02 2）
② 删掉的记录中「不属本票」的外来记录 = 0
③ 留在两片里的记录 = 7（scene-10.ts:11 goal-weight ＋ scene-02.ts 六条 scene:'02' 的 non-exec）
④ analysis/routes.ts：带 key=127 无 key=42；键名去重=13；带 key 但不属 13 键的=0；无 key 而 scene≠10 的=0
⑤ analysis/routes.ts 的 169 行在「实删集合」里逐字节命中：缺失=0 重复命中=0
⑦ 同 list 内 order 重复数 = 0（三个件都是 0）
```
- 判据 ②「不许连带删别票记录」：**删除集里 0 条外来记录**——带 `key` 的全部落在本票 13 键内，无 `key` 的全部 `scene: '10'`。
- 判据 ⑤ 是搬运保真的最强证据：169 条记录行与「从两个分片实删掉的那 169 行」**多重集逐字节相等**（缺失 0、重复 0），即「纯搬迁」在路由面成立。
- `order` 无重复 ⇒ 生成器按 `(list, order)` 复原三表不会撞键。
- 落点脚本自己的日志与我的实测互相印证：`03c-routes-apply.txt` 写「scene-02.ts：删 2 行（残留记录 6 条）／scene-10.ts：删 167 行（残留记录 1 条）／`analysis/routes.ts`（185 行；sha256=4b52864e959e3206）」，其中 185 行与 `4b52864e959e3206` 与我实测**完全一致**。
- **记账错**：票面「20 条外来键记录留在 `legacy/routes/scene-10.ts`，由 #314／#315／#316／#318 定点删」⇒ 当刻实测**只剩 1 条**（`calorie.view.goal-weight`，`wake:373`）。20 是**预备期**数字且自洽（187−167＝20，见证据 §⑥.3 的「187 条」），但那 19 条已被先落点的四票各自搬走——同批交付里**票面与落点脚本日志（「残留记录 1 条」）互相矛盾**。
- 另：证据 §⑧「`src/analysis/routes.ts` 里 5 个外来键的记录（20 条…）」⇒ 该文件带 `key` 记录 **127 条、键名去重 13**，**外来键记录 0 条**；那 5 键的 20 条早已不住这里（`diet/routes.ts` 收 diet-review／ranking，`home/routes.ts` 收 exercise／goal-progress，`goal-weight` 尚在 scene-03／06／10 三片）。

### 维 5 · 行为不变 —— **PASS**（我自己跑的读数）

第二轮窗口（干净 dist 态、无 `build` 步骤）读数：

| 步 | 命令（窗口内直接调用） | exit | 读数 |
|---|---|---|---|
| gen:check | `pnpm gen:check` | 0 | `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 8 个（analysis／body／diet／exercise／home／photo／weight／workout）声明 82 条，未搬迁清单 19 条` |
| 全量测试 | `pnpm test` | 0 | `tests 1492 / pass 1492 / fail 0`（duration_ms 75785） |
| 验收脚本 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | 0 | `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`；`UNACCOUNTED=0 处；树自相矛盾=0 处` |
| 快照 | `pnpm snapshot:html:check` | 0 | `RESULT: artifacts=186 changed=0 added=0 removed=0 base-* fingerprint=ef7cc02969e37e4bfc0eb35751b8dc1c` |
| gen 幂等 | `pnpm gen` 二次 | 0 | `registry.ts` 前后 sha256 同为 `002165f7…`，**idempotent=True** |

⇒ 票面判据「② 全量测试 exit 0 ＋ UNACCOUNTED 空」**独立复现**。

**一条必须记账的采样事故（不归本票）**：第一轮窗口里 `pnpm test` 读到 **1489 pass / 3 fail**，3 红全是 `dsh-bill-ilife client`（`test/client-bundle-48.test.mjs:65/74/104`，`SyntaxError: Cannot use import statement outside a module` @ `packages/plugin-bill-ilife/dist/client.js:7`）。查明：该件是 **gitignored 的 dist 产物**，当刻正被别人的窗口按「裸 `tsc -b` → 存根」写坏（mtime `09-13 23:34:29`，1663 B 存根），随后又被写回工厂包（`09-13 23:38:41`，3528 B，首行 `window.__ModuleLoader__.load({`）；我这轮窗口正好采样在两者之间。单跑该门当刻 **21 pass / 0 fail**。**结论：该红与本票无关**（本票 10 件不触该包；该失败集在 `docs/research/t92-baseline-failures.md` 已登记为既有基线红，`docs/skills/skill-calorie/t322-预热-证据.md:113` 也把它记为「环境」）。附带观察：`packages/plugin-bill-ilife/tsconfig.json` 的 `include: ["src"]` 含 `src/client.ts`，故**任何一次 `tsc -b --force` 都会把该客户端产物打回裸 ESM**——本仓既有雷（`docs/skills/skill-chef/t10-review-A.md:250`），不是本票引入，但它是「引别人 dist 残留」的常态来源。

### 维 6 · 免费判据（生成物 diff 只应出现 `src/cli/registry.ts`）—— **PASS**

- 提交面：`git show --numstat cbb5da8` 的 10 件里，生成物只有 `src/cli/registry.ts`（+2 行）；`cli/keys.ts`／`packages/base-combos/combos.yaml`／`scripts/build-help.mjs`／`src/triggers/routes.generated.ts`／`SKILL.md` **不在提交清单内**。
- 自跑面：窗口内跑 `pnpm build → pnpm gen` 后逐件比 sha256，**五件前后全等**：
```
HASH-BEFORE/AFTER 003… keys.ts       4DB43569FBAB400D…
HASH-BEFORE/AFTER 005… combos.yaml   CA17D2C3F62CE752…
HASH-BEFORE/AFTER 006… build-help.mjs 4D44F03F6FFB25D7…
HASH-BEFORE/AFTER 008… routes.generated.ts E9EA993518EC33E3…
HASH-BEFORE/AFTER 009… SKILL.md      DF63151429D5E55C…
registry.ts 002165F749FF653D…（gen 前后不变）
```
（`pnpm gen` 的四个写靶：`registry.ts`／`combos.yaml` 块／`build-help.mjs` 的 REPR 块／`routes.generated.ts`——后三件重生成后逐字节不变，正是「纯搬迁」的免费判据。）

### 维 7 · 证据面 —— **FAIL（S2）**

**① 门禁审计当刻 FAIL（机器可判）**：按证据件自己写的复核用法原样跑——
```
$ node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t319-搬迁-证据.md \
    --log .scratch/locks-land/gate-runs.log --ticket 319-land --since 2026-09-13T15:26:25Z
RESULT: matched=0/1 auditEntries=62 scoped=1 undeclared=1
gate-audit: FAIL        [exit code: 1]
FAIL: 1/1 条声明在审计日志中找不到对应条目
FAIL: 反向对账：窗口内有 1 条无人声明的 RUN 条目
```
根因定位（`probe-gateline.mjs`）：证据件**第 56 行**的 `GATE-RUN` 行在 `cmd="…"` 右引号之后**还拖了 146 个字符**（← 说明文字 ＋ 整条 markdown 表头 `| 步 | 命令 | exit | 日志 | |---|---|---|---|` 被并进同一行）。`check-gate-audit.mjs` 的 `parseClaimBody` 取「`cmd=` 之后到行尾整段都是命令」，`unquote` 因值不以右引号结尾而原样返回整段 ⇒ `cmdMatches` 两侧都对不上。**改动仅一处**（把该行在右引号处断行），同一命令即：
```
- 证据 :56 runId=614d7913-… cmd=pwsh -NoProfile -File .scratch/t319/land.ps1
RESULT: matched=1/1 auditEntries=62 scoped=1 undeclared=0
gate-audit: PASS        [exit code: 0]
```
⇒ 这是一条**一处断行即可修好的交付缺陷**，但按当刻状态，票面判据里的「证据＋对账」不成立。

**② 三处数字与当刻事实不符**（详见维 3／维 4／S2 清单）：
1. `−73 行`（13 个 `case` 块）⇒ 实测 **141 行**（逐块「`case` 行 → 闭合 `}`」6／7／8／9／10／11／11／12／12／15／15／15…，脚本 `scan-readcmd-diff.mjs`）；连块前的 `// #113` 注释与 3 行空行算进去是 **145 行**。同一提交里 `cmd_read.ts` 的**总删除行是 188**（其中导入段 43 行），**没有任何口径能得出 73**。
2. `25 个 import 绑定` ⇒ 实测 **29 个**。
3. 票面「20 条外来键记录留在 `legacy/routes/scene-10.ts`」⇒ 实测 **1 条**；§⑧ 的「`src/analysis/routes.ts` 里 5 个外来键的记录」⇒ 实测 **0 条**。

**③ 导出窗口覆盖**：导出件 `docs/skills/skill-calorie/t319-gate-runs.log`（33 行）里放的是 **`START` 行而非 `RUN` 行**（导出时间 `15:28:19Z`，而包装器写 `RUN` 是 `15:28:22.812Z`），而 `check-gate-audit.mjs` 只认 `^RUN` 条目 ⇒ 这份「受跟踪对账源」**不含本窗口任何审计条目**；步骤表还漏了窗口实际跑过的 **t293**（该认领运行只有 gitignored 的 `.scratch/t319/logs/10-t293.log`，无受跟踪对账源——与 `#317` 复核席记的那一类同形）。`--allow-*` 未见使用（故无 `GATE-RELAX` 配对问题）。

### 维 8 · 变异自证 —— **PASS（≥2 次，主树零接触）**

副本＝`git archive cbb5da8` → `.scratch/t319r/mut/`（含逐包 `node_modules` 联结 ＋ 各包 `dist` 复制；**不跑 pnpm，直调 node**）。副本内基线先绿：`gen:check exit=0`。

| # | 变异 | 变异后 | 复原后 |
|---|---|---|---|
| 1 | 删 `src/analysis/routes.ts` 一条记录（`calorie.view.calorie-trend`） | `gen:check` **exit=1**：`GEN-STALE FAIL … 印记 4b52864e959e… ≠ 现 e6473fd5e3b1…` | 还原该行 ⇒ **exit=0** |
| 2 | 删 `src/analysis/commands.ts` 一条声明（`calorie.history`） | `gen:check` **exit=1** | 还原 ⇒ **exit=0** |
| 3 | **语义变异**（副本内 `tsc -b --force` 重建 dist → `--stamp` → `gen`）：删一条路由记录后**重生成派生件** | `gen` **抛错**：`Error: list=new 的 order 不连续：第 45 位实得 46（packages/skill-calorie/src/analysis/routes.ts · "查数据健康"）——必须是 0..56 连续无洞`（`gen-routes.mjs:146` `groupDecls`） | 复原 ＋ 重建 ＋ `--stamp` ＋ `gen` ⇒ `routes.generated.ts` **逐字节回主树**，`gen:check exit=0` |

- **变异 3 是本席最想拿到的那一条**：删掉一条记录后，`gen` 不是「安静地少生成一条」，而是由 `gen-routes.mjs` 的 **`order` 连续性断言**（每 `list` 必须 0..N 连续无洞）**当场抛错**——即「丢记录／多记录」这类事故在生成器里有**语义护栏**，不只靠 `dist` 内容印记。该席自己那条变异（尾末加空格 ⇒ `GEN-STALE FAIL`）证明的是**印记机制**；我这两条补上了**派生语义**那一层。
- 另一发现（**不归本票**，属 #325 假绿族）：`gen` 的**产出**来自 `dist`（`dist/<能力>/commands.js` 等），而 `gen:check` 的**新鲜度**只按 `src` 内容印记判——故「只改 `src` ＋ `--stamp` ＋ `gen`」**不重建 `dist` 也能 exit 0**（实测：`routes.generated.ts` 与主树逐字节相同，即那条记录**没被删掉**，而 `gen` 退出码 0）。这条对**所有**窗口都成立，收口时应与 #325 一并处置。
- 该席的另一条前置自证（`apply-routes.mjs --check` → `ROUTES-CHECK OK mine=169`）我在文本层复核：日志/脚本一致（`03c-routes-apply.txt`），并已用我自己的多重集对账（缺失 0／重复 0）独立得到同一结论。

### 维 9 · 反假绿 —— **PASS（两大坑均未踩）**

- **`Copy-Item` 保 mtime ⇒ `tsc -b` 跳过**：本票三个新件是**新建**（不涉复制），受跟踪共享件走定点删除；我的窗口 `pnpm build` 后 `gen:check` PASS，且 `dist/.gen-inputs.json` 里 `analysis/commands.ts = b9684e228662…`／`analysis/routes.ts = 4b52864e959e3206…` 与在盘 sha256 逐字相同（印记确实覆盖本票新件）。
- **被 ignore 的 `dist` 被他人残留污染**：**已实证**——第一轮 `pnpm test` 的 3 红就是别人窗口当刻重写 `packages/plugin-bill-ilife/dist/client.js` 所致（见维 5）。这条坑**双向**成立：既能假绿也能假红，判读必须锁定「有主窗口的完整区间」。
- **整文件覆盖共享件会不会静默复活别票已删的 `case`**：**攻了，未成立，反而证明了该席的做法规避了它**。预备期整文件成品 `.scratch/t319/tree/…/cmd_read.ts`（619 行、28 个 `case`）比当刻受审件**多 11 个 `case`**：
```
calorie.today, calorie.view.nutrition-ratio, calorie.view.nutrition-detail, calorie.view.source-stats,
calorie.view.today-water, calorie.view.batch-import-preview, calorie.view.diet-review, calorie.view.ranking,
calorie.view.library, calorie.view.search, calorie.view.dedupe
```
若按整文件覆盖落点，这 11 条**别票已删**的 `case` 会被静默复活——而 #294 的 `case` 棘轮是「⊆ 冻结清单 ＋ size ≤ max」，**复活一条仍在冻结清单里的 `case` 不报红**。本提交走标签驱动定点删除（diff 里只有 9 个 `+` 行、全是导入行改写），该风险未发生。

---

## 三级问题清单

### S1（数据丢失／静默回归／主链破坏）：**0 条**

### S2（交付缺陷，必须整改）：**3 条**

| # | 问题 | 精确证据 | 最小整改 |
|---|---|---|---|
| S2-1 | 证据件的门禁对账当刻 **FAIL**：`GATE-RUN` 行在 `cmd="…"` 右引号后拖了 146 字符（表格头并入同一行） | `node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t319-搬迁-证据.md --log .scratch/locks-land/gate-runs.log --ticket 319-land --since 2026-09-13T15:26:25Z` ⇒ `matched=0/1 … gate-audit: FAIL`（exit 1）；断行后 ⇒ `matched=1/1 undeclared=0 PASS`（exit 0） | 证据件第 56 行在右引号处断行，把「← 说明＋表格头」挪到下一行 |
| S2-2 | 本票在 `cmd_read.ts` 留下 **2 个自己造成的未用 import 绑定**（`anchorOf`／`windowRange`），且全仓无 `noUnusedLocals`／`noUnusedParameters` ⇒ 静默死代码 | `scan-diff-imports.mjs`：`POST 未用绑定（9）` 中 `其中「本票造成的新未用」：2 -> anchorOf, windowRange`；`tsconfig.base.json` 仅 `"strict": true` | 删 `packages/skill-calorie/src/cli/cmd_read.ts:62` 该行 import 内的 `anchorOf` 与 `windowRange`（同行其余 6 个仍在用） |
| S2-3 | 证据数字与当刻事实不符三处：`−73 行`（实测 141／含注释空行 145）、`25 个 import 绑定`（实测 29）、票面「20 条外来键记录留在 `legacy/routes/scene-10.ts`」（实测 1 条）＋ §⑧「`analysis/routes.ts` 里 5 个外来键的记录」（实测 0 条） | `scan-readcmd-diff.mjs`、`scan-diff-imports.mjs`、`scan-routes.mjs` 三段输出；落点日志 `残留记录 1 条` 与票面互相矛盾 | 四个数字改为实测值，并说明 20 是预备期口径（当刻 1 条） |

### S3（记账／措辞／口径，随收口批量修）：**5 条**

1. 证据 §② 表的逐键 `list:order` 与实测 11/13 相符；`nutrition-analysis`／`six-factors` 各多 1 条（`new:48`／`new:51`，即 scene-02 那两条**未在表内逐条列出**，表内合计 125 ≠ 文件 127）——建议补两行或加一句「scene-02 两条见正文」。
2. 证据 §③ 的步骤表与导出件**漏列 t293**（窗口确实跑了、`P1..P5 = 5/5`／`UNACCOUNTED=0`），该认领运行只留 gitignored 日志、**无受跟踪对账源**。
3. 导出件放的是 `START` 行而非 `RUN` 行（导出早于包装器补写 3 秒）⇒ 作为「受跟踪对账源」不成条目；建议窗口退出后**再导出一次**（或用 `--export` 口径）。
4. 票面第 1 条的「子功能文件」未建（处理函数住 `commands.ts`）——派单限定所致，已在证据 §⑥.5 记账，**同意不判缺陷**，仅提示票面「按 HELP 子功能再分文件」这句与落点不符，收口时一并改口径。
5. `docs/skills/skill-calorie/t319-搬迁-证据.md` §⑧ 与票面「待确认 1」的措辞把「留在原片」与「`analysis/routes.ts` 里」混用，读起来像 `analysis/routes.ts` 含有外来键记录（实测 0 条）。

---

## 附：本席对「该席自述待确认 4 条」的独立结论

1. **169 条逐字节相同** ⇒ 成立（多重集缺失 0／重复 0），且 `routes.generated.ts` 我自跑重生成后逐字节不变。
2. **生成物只 `registry.ts`** ⇒ 成立（提交面 ＋ 自跑面双证，五件哈希前后全等）。
3. **`REGISTRY` 与 `switch` 同产物** ⇒ 未做逐键运行时对拍（无 DSH profile 场景，且须建库），但 `gen:check` PASS ＋ 1492/1492 ＋ t293 P1..P5（含 `cmd-registry-294` 断言）已覆盖「键→能力目录」这一层；**如实标为未完成动态项**。
4. **留原片 20 条的最终归属** ⇒ 当刻只剩 1 条（`goal-weight`），19 条已由 #314／#315／#316／#318 搬走；**该条属 #320 收口视野**，本席不改判。

## 附：未完成的动态项（如实声明）

- 未做运行时逐键对拍（`calorie-cmd-read <键>` 实跑需建库／profile），也未跑 `pnpm build` 之外的发布链门（`boundaries`／`publish:*`／`help:examples:check`）——不在票面判据内。
- 变异 v4（副本内重建后语义变异）见 `.scratch/t319r/logs/mut4-console.txt`。

## 附：复核用机器证据索引（可复跑）

| 脚本／日志 | 作用 |
|---|---|
| `.scratch/t319r/scan-decl.mjs` | 13 键全仓出现位置分类（含「注释行≠记录行」甄别） |
| `.scratch/t319r/scan-mirror.mjs` | `commands.ts` ↔ `keys.ts` ↔ `combos.yaml` 三处镜像 |
| `.scratch/t319r/scan-dispatch.mjs` | 声明四字段逐字保真 ＋ 全仓 `case` 扫描 ＋ 处理函数归属 |
| `.scratch/t319r/scan-diff-imports.mjs` | import 绑定前后集合差（29 删／9 未用／2 新造） |
| `.scratch/t319r/scan-readcmd-diff.mjs` | `cmd_read.ts` 删除行精确账（188＝导入 43 ＋ 正文 145；13 块 141 行） |
| `.scratch/t319r/scan-routes.mjs` | 169 条记录多重集对账／归属／order 重复 |
| `.scratch/t319r/scan-crossshard.mjs` | 5 个外来键记录去向 |
| `.scratch/t319r/scan-roster.mjs` | 名册 `list:order` 逐键核 |
| `.scratch/t319r/scan-resurrect.mjs` | 整文件覆盖复活 `case` 攻击 |
| `.scratch/t319r/probe-gateline.mjs` | GATE-RUN 行断行定位 ＋ 修复后审计 PASS |
| `.scratch/t319r/mut2.ps1`／`mut3.ps1`／`mut4.ps1` | 隔离副本变异 |
| `.scratch/t319r/logs/window-console.txt`／`window2-console.txt` | 两轮门禁窗口原始输出 |
| `node tooling/check-gate-audit.mjs …` | 门禁对账（当刻 FAIL，见 S2-1） |

---

# 返修复验（`cfe5bb0`）

- 复验时点：受审返修件 `cfe5bb0`（`docs(319): 证据件返修（对账行断行／数字三处）＋ 删本票新造的两个死绑定`，2 件：`docs/skills/skill-calorie/t319-搬迁-证据.md` ＋ `packages/skill-calorie/src/cli/cmd_read.ts`；`HEAD == origin/master == cfe5bb0`，开门 `TREE0-COUNT=0`）。
- 复验窗口（三条新规则口径：**不带 `--force`／不删 `dist`**、**窗口内直调 `node`（不碰 `pnpm` 垫片）**、**同一把 `--lock-dir .scratch/locks-land`**）：
  `node tooling/run-locked.mjs --ticket 319r-r --lock-dir .scratch/locks-land --stale-minutes 30 -- pwsh -NoProfile -File .scratch/t319r/gate-r.ps1`
  ⇒ runId `ee7aa5e3-c2bd-4c1a-b25c-ca467c23a655`（waitedMs=0，exit=0），`REVERIFY-GATE-DONE`，收尾 `TREE1-COUNT=0`。
- 因 #324 期间动过同目录件（`analysis/commands.ts`＋6/−6、`cmd_read.ts`＋2/−2、`render/health.ts`→`analysis/healthPlate.ts` 等），本席**在当刻 HEAD 上重核**了维 1／2／4，不只复核返修的三条。

## 三条整改逐条实测

**① 对账行断行 —— 全消。** 我按证据件自己写的新用法实跑（未做任何本地补丁）：
```
$ node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t319-搬迁-证据.md \
    --log .scratch/locks-land/gate-runs.log --ticket 319-land --since 2026-09-13T15:26:25Z
  - 证据 :56 runId=614d7913-df65-4508-a1ca-0ca6f813e3ca cmd=pwsh -NoProfile -File .scratch/t319/land.ps1
RESULT: matched=1/1 auditEntries=136 scoped=1 undeclared=0
gate-audit: PASS        [exit=0]
```
（返修 diff 逐行核过：第 56 行确实在 `cmd="…"` 右引号处断行，后半段移到第 57 行。）

**② 死绑定 —— 全消。** `cmd_read.ts` 该行现为 `import { dayField, defaultRange, fail, nums, optNum, optStr } from '../shared/params.js';`（6 个，同行另 6 个仍在用）。重跑 `scan-unused.mjs`：
```
=== 绑定总数=68 未用(代码内零次)=7 ===
UNUSED  line 42 buildReviewDoc / 43 buildReviewView / 44 buildProcessProgressView /
        45 buildProcessProgressDoc / 46 buildContraDoc / 49 renderPlanHtml / 49 renderPlanWizardHtml
```
绑定 70→68、未用 9→**7**，且这 7 条全是 `cbb5da8` **之前就存在**的继承死码（第一阶段已记，非本票责任）；`anchorOf`／`windowRange` 已不在清单。基线 `tsc -b`（不带 `--force`）exit=0 ⇒ **未误删在用符号**。

**③ 三处数字 —— 主体全消，残留 1 处（见下 S3-新 1）。**
- 证据 §① 第 18 行现为「删掉这 13 个块（**141 行**；连同块前注释与 3 行空行共 145，全文件 −188 ＝ 导入段 43 ＋ 正文 145）＋ 随之未使用的 **29 个** import 绑定（返修窗 2026-09-13T16:35:33Z 实测；本票新造成的 2 个死绑定 `anchorOf`／`windowRange` 于返修提交删去）」——四处数字与我第一阶段实测**逐字相符**。
- 证据 §⑧ 现为「`src/analysis/routes.ts` 里**外来键的记录 0 条**（169 条 ＝ 带 `key` 的 127 条〔键名去重恰本票 13 键〕＋ 无 `key` 的 42 条）」＋列出 5 个外来键当刻所在地。我逐条实测：`diet/routes.ts` 的 `view.diet-review` **18**／`view.ranking` **28**、`home/routes.ts` 的 `view.exercise` **22**／`view.goal-progress` **8**、`view.goal-weight` 3 处（`legacy/routes/scene-03.ts:15`／`scene-06.ts:19`／`scene-10.ts:11`）——**与该段文字逐条相符**。
- 票面：偏离记账已改为「**1 条**外来键记录留在 `legacy/routes/scene-10.ts`」✓；**「待确认」第 4 条仍写「留在 `legacy/routes/scene-10.ts` 的 20 条外来键记录」**，与同票面已改正的「1 条」自相矛盾 ⇒ 记 S3（理由见下）。

## 门禁电池（一条锁内、直调 node）

| 步 | 命令（窗口内直接调用，无 `pnpm`、无嵌套 `run-locked`） | exit | 读数 |
|---|---|---|---|
| r1 基线 | `node node_modules/typescript/bin/tsc -b`（**不带 `--force`**，不删 `dist`） | 0 | 基线绿 |
| r2 印记 | `node packages/skill-calorie/scripts/gen-cli.mjs --stamp` | 0 | 声明源 37 件 |
| r3 验真 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | 0 | `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 8 个（analysis／…）声明 82 条，未搬迁清单 19 条` |
| r4 生成 | `node …/gen-cli.mjs` | 0 | 四靶重生成 |
| r5 全量套件 | `node --test "test/*.test.mjs" …（与 `pnpm test` 同 15 段 glob）` | 0 | **`tests 1492 / pass 1492 / fail 0`**（duration 37867 ms） |
| r6 验收 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | 0 | `P1..P5 5/5`；`UNACCOUNTED=0 处；树自相矛盾=0 处` |
| r7 快照 | `node tooling/skill-html-snapshot.mjs --check` | 0 | `artifacts=186 changed=0 added=0 removed=0` |
| 幂等 | 再跑一次 `gen` | 0 | `registry.ts` sha256 前后同为 `002165f7…`，idempotent=True |

- 8 件受关注件哈希**前后全等**（`registry.ts`／`keys.ts`／`combos.yaml`／`build-help.mjs`／`routes.generated.ts`／`SKILL.md`／`analysis/routes.ts`＝`4B52864E…`／`cmd_read.ts`＝`A4BFFD4E…`）；`dist/.gen-inputs.json` 印记前后同为 `FEE758E8…`；收尾树空。

## 当刻 HEAD 上重核的旧判据（不只看返修件）

- 维 1：`commands.ts` 声明 **13** 条、三处镜像 **0 问题**（`scan-mirror`）；13 条 × `shape/title/wakeWord/example` 与改前 `130eacb` 声明**逐字相同**（`scan-dispatch`，`analysis/commands.ts` 虽被 #324 改过 6 行，改的是导入路径，声明行未动）。
- 维 2：全仓 13 键 `case` = **0**；`cmd_read.ts` 现存 `calorie.*` case = **11**。
- 维 4：`analysis/routes.ts` sha256 与 `cbb5da8` 时**相同**（`4B52864E959E3206`），169 条对账结论不变——实删 169（167＋2）、外来记录删 0、逐字节命中缺失 0／重复 0、`order` 无重复。

## 返修后问题清单

- **S1 = 0，S2 = 0**（第一阶段的 3 条 S2 全消，其中 S2-3 主体全消）。
- **S3 = 5**（沿用第一阶段 4 条中的在册项 ＋ 新增 1 条）：
  1. **（新增残留）** 票面「待确认」第 4 条仍写「留在 `legacy/routes/scene-10.ts` 的 20 条外来键记录」，与同票面偏离记账已改正的「1 条」矛盾。**判 S3 的理由**：① 同票面与证据 §⑧ 已各有一处写明当刻事实（1 条／0 条），读者不会因此对**落地件**形成错误认识；② 该条是一次**向前看**的追问（那批预备期记录能否被各自所有者清零，明标「本票视野外」）。严格些的席位可判 S2——建议 #320 收口批量改文本时一并改成「那批 20 条外来记录（当刻 1 条仍在 `scene-10.ts`）何时清零」。
  2. 证据 §② 小标题与 §⑥.3 仍用「留在原片…20 条」的预备期口径（§⑧ 已给当刻事实，属历史记账，可留）。
  3. 证据 §③ 的步骤表与导出件**仍漏列 t293**（该认领运行只有 gitignored 日志、无受跟踪对账源）。
  4. 导出件 `t319-gate-runs.log` 仍是 `START` 行（导出早于包装器写 `RUN` 3 秒），不是审计条目。
  5. 票面第 1 条「按 HELP 子功能再分文件」与落点不符（已在偏离记账，收口时改口径）。

## 返修复验结论

**转 PASS（93／100）**——S1=0、S2=0。三条整改**实跑复核全消**：① 对账 `matched=1/1 undeclared=0 PASS`（exit 0）；② 未用绑定 9→7、本票新造的 2 个已删且不影响在用符号；③ 证据件四处数字改为实测值（与我第一阶段实测逐字相符）、§⑧ 的 0 条与 5 键所在地逐条相符、票面偏离记账改为「1 条」。门禁电池在同一把锁内直调 node 跑完：基线 `tsc -b` 绿、`gen:check` PASS、**1492/1492**、t293 `P1..P5`＋`UNACCOUNTED=0`、快照 `changed=0 added=0 removed=0`、`gen` 幂等、收尾树空，**未发现新 S1／S2**。扣分只余 5 条 S3（记账与文字面，其中「票面 20 条」一条建议 #320 收口时一并改）。
