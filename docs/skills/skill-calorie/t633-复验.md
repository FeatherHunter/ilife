# t633 复验（审查者独立复跑用）

前置：仓根，`SKILLS_DB_PATH` 指向临时目录，真出口经 `dist` 跑（先重建）。

```powershell
node tooling/run-locked.mjs --ticket <复验票号> -- node node_modules/typescript/bin/tsc -b packages/skill-calorie --force
# 期望 exit 0（若他席在途件有类型错，属他人域：确认报错文件不是 libraryDocs.ts 即可）

node tooling/run-locked.mjs --ticket <复验票号> -- node --test packages/skill-calorie/test/t633-种子来源可读.test.mjs
# 期望 pass 2/fail 0；任一「来源：测试」出现即红

node tooling/run-locked.mjs --ticket <复验票号> -- node --test packages/skill-calorie/test/t274-食品库页.test.mjs
# 期望 pass 13/fail 0（食品库族回归）
```

肉眼验收：任取一张查食品库页（`calorie.view.library`），种子食品卡片来源行印「来源：内置食品」，
整页无「来源：测试」；缺来源行印「来源：未知」。

变异复核：把 `libraryDocs.ts:127` 的 `'内置食品'` 临时改回 `'测试'` → 重建 → 靶向测试须 pass 0/fail 2；
改回 → 重建 → pass 2/fail 0（本票实测 runId 见 `t633-证据.md` §三）。
