# #589 · 看目标推荐整页化 · 证据

> 票面：`看目标推荐`（`calorie.view.goal-recommend`）由**片段**换成**完整文档**，复用目标域同族装配
> （`src/goal/resultDocs.ts` 那套）；命令名／路由／`WAKE_TABLE`／HELP 卡片归属一行不动。
> 本件只记本席（实施席）实跑读数与对账；跑法、脚本、日志都在 `.scratch/t589/`（草稿，不入仓）与
> 本目录 `t589-GATE-RUN.log`（持锁运行记录导出，入仓）。

## 一、判定

**PASS**。改前产物是 1968 B 的片段（`<section class="ilife-page" …>`），改后是 73285 B 的完整文档
（`<!doctype html>` ＋ charset ＋ 样式段 ＋ 新版面），`data.metrics` 与改前**逐字段相同**（票面那串推荐数
2064／141／246／57／2471 与每日基准消耗 2564 一个不动）；A/B 对照证明**只有目标推荐这一页变了**，
其余 23 条页（目标域同族 ＋ 他域）逐字节相同；靶向测试 5/5 绿；变异自证红→绿两行齐。

## 二、改前／改后（同一条命令的两面，真跑 `dist/cli/cmd_read.js`）

数据面：`mkdtemp` 临时库 ＋ `docs/research/t81-seed.mjs::seedFull`（30 岁／男／175 cm／最新体重 70.6 kg）
＋ `CALORIE_TODAY=2026-09-07` ＋ 钉钟 `--require test/freeze-clock.cjs`、`FAKE_NOW_ISO=2026-09-07T00:00:00`
（饮水推荐按**当季**取 35 ml/kg，只钉 `CALORIE_TODAY` 不够——见新测试件头注）。

| 面 | 源码态 | exit | 字节 | 首行 | 产物 sha256（前 16） |
|---|---|---|---|---|---|
| A（改前） | `read.ts` 退回 `HEAD`＝`renderGoalRecommendHtml` 片段 | 0 | **1968** | `<section class="ilife-page" data-skill="` | `c5e647f13c4ab33d` |
| B（改后） | 本票版本＝`goalRecommendDoc.ts` 整页 | 0 | **73285** | `<!doctype html>` | `f50a3cea34de7cb5` |

产物要件（B 面，票面「doctype／charset／样式／版面齐全且字节 > 20KB」）：

- `<!doctype html>` ✓（A 面为假）；`<meta charset="utf-8">` ✓；`<style>` 共享样式段 ✓；`<script>` 页运行时 ✓；
- 版面：`ilife-block-page-shell`（页壳）＋ `meta-bar`（页头：`看目标推荐 · 目标管理` ＋ `type-badge` 类型徽章）
  ＋ `ilife-block-page-shell-title`（H1 `🎯 目标推荐`）＋ 结论句 ＋ `ilife-block-toc`（页内导航 3 锚点）
  ＋ `ilife-block-kpi-card-grid`（6 张读数卡）＋ `ilife-block-data-table`（算法口径 5 行）＋ 折叠区「计算依据」
  ＋ `ilife-block-caliber`（口径行 3 条）＋ 复制区双按钮（数据三格式菜单 ＋ 复制日志）；
- 字节 **73285 > 20000** ✓；`data.output` 是绝对路径且文件在盘 ✓（判据见新测试件 ①）。

## 三、推荐数（与当刻一致）

`dist` 回执 `data.metrics`（改前改后**同一个对象**，A/B 两面 `deepEqual` 为真）：

```json
{"calorieGoal":2064,"proteinGoal":141,"carbsGoal":246,"fatGoal":57,"waterGoal":2471,
 "tdee":2564,"bmr":1654,"weeklyRateKg":0.5,"weightKg":70.6,"recommendedWaterMl":2471,"mlPerKg":35}
```

上屏读数：卡面 `2064 卡／141 g／246 g／57 g／2471 ml`＋口径表 `基础代谢 1654 卡／每日基准消耗 2564 卡`；
依据句里 `2564`／`2064`／`141`／`57`／`246`／`2471` 逐条在场（测试 ① 逐条断言）。
**唯一的上屏改法**：依据句走 #160 那条**上屏归一**（`render/trendDocs.ts::humanText`），把数据层原文里的
英文缩写 `TDEE` 换成「日常消耗」——**只换词不换数**（`2564` 一字不动；同族先例 `src/goal/receipt.ts:282`
也把 `planReasons` 走同一套渲染）。

## 四、他域页面逐字节不变

对照口径＝**同一份 dist 之外只剩一处源码差**（`read.ts` 的 `html:` 一行）：A 面把 `read.ts` 退回 `HEAD`
（片段渲染），B 面留本票版本（整页渲染），两面的 `resultDocs.ts`／`goalRecommendDoc.ts` 完全一样。

| 面 | 电池 | exit0 条数 | 目标页字节 |
|---|---|---|---|
| A（片段） | `.scratch/t589/ab-fragment.json` | 24/24 | 1968 |
| B（整页） | `.scratch/t589/ab-full.json` | 24/24 | 73285 |

对照结果（`.scratch/t589/compare.mjs`）：**对照 23 条，变了 0 条**（目标域同族 10 条：`goal`／`goal-config`／
`goal-status`／`goal-weight`／`goal-expiring`／`goal-vs-actual`／`goal-predict`／`goal-wizard`／`goal-progress`×2；
他域 13 条：饮食／总览／体重／运动／档案／营养配比／排行／食品库／缺口／健康盘／运动目标／档案预检／HELP 速查台）；
`packages/base-render/dist` 逐文件同哈希（**变 0 件**）。

两条辅助读数（说明为什么必须做这一层对照，而不是只比「改前 dist vs 改后 dist」）：

1. **种子库的墙钟会漂**：`goal-status`／`view.profile`／`view.library` 三页把库里的 `updated_at` 印上屏，
   而临时库的 `CURRENT_TIMESTAMP` 是**秒级墙钟**——同一份 dist 连跑两次就只差这三页（实测 A／B 两跑差
   `2026-09-16 04:59:35` vs `04:59:42`）。探针因此先把 `created_at`／`updated_at` 归一到 `2026-09-07 12:00:00`
   （只动临时库、不动种子件），归零之后同 dist 两跑 **0 条差**（`.scratch/t589/detA.json` vs `detB.json`）。
2. **别人在途的件会漂**：`packages/base-render/**` 本窗口内有他席在途改动（`git status` 可见），窗口内实测
   共享样式从 `font-size:12.5px` 变到 `13px`（目标页字节 73262→73285 就是它）。故「逐字节不变」的读数
   取 A/B 同窗口对照，不跨窗口比。

## 五、靶向测试（新件）与反向锁

新件 `packages/skill-calorie/test/goal-recommend-589.test.mjs`（真跑 CLI，临时种子库，真库零触碰）：

```
✔ #589 ① 目标推荐＝完整文档：exit0 ＋ 绝对路径 ＋ 四断言 ＋ >20KB ＋ 冻结推荐数 ＋ 同族六样
✔ #589 ② 路由面一字不动：声明件与生成物同一条记录，且与产物复制日志逐字相同
✔ #589 ③ 同族同架：本页与 goal-config 共用同一套骨架标记（复用而非另起一套）
✔ #589 ④ 红线②：旧片段 renderGoalRecommendHtml 仍在，且仍是片段（只出台账、本票不删）
✔ #589 反向锁：片段形状必红、错配必抛、固定钟下两次实跑同哈希
ℹ tests 5 / pass 5 / fail 0
```

四条反向锁（防「判据恒绿白过」）：① 片段形状喂给四断言**必抛**（`缺 doctype`）；② 拿 `goal-config`
那份读数去核对推荐页冻结表**必抛**；③ 同一入参（固定钟）两次实跑**同哈希**（页内不掺墙钟）；
④ 旧片段函数**必须仍在且仍是片段**（红线②的可执行守望）。

`GATE-RUN runId=f36c8b64-fcc4-4c2d-b469-99299f9757bf cmd=node .scratch/t589/final-locked.mjs exit=0`
（窗口内此步＝`node --test …goal-recommend-589.test.mjs`，直调，见 §九）。

## 六、变异自证（源码级改坏致红 → 逐文件还原致绿）

变异点：`goalRecommendDoc.ts` 的 H1 字面量 `title: '🎯 目标推荐',` → `title: '目标推荐',`（产物页题坏掉）。
同一持锁窗口内：改坏 → `tsc -b --force` → 跑靶向测试 → **逐文件还原**（sha256 核对）→ `tsc -b --force` → 再跑。

```
MUT-RED:   file-sha=4b951ff866c2 build-exit=0 dist-has-old-title=true  test-exit=1  hit="✖ #589 ① 目标推荐＝完整文档…"
MUT-GREEN: restore-identical=true build-exit=0 dist-has-new-title=true  test-exit=0  summary="ℹ pass 5 ℹ fail 0"
MUT-VERDICT: PASS（改坏致红 ＋ 逐文件还原致绿）
```

两行读数＝**红**（`test-exit=1`，且 dist 指纹确认编出来的是坏写法：`dist-has-old-title=true`）与
**还原一致**（`restore-identical=true`，dist 指纹回 `dist-has-new-title=true`，`test-exit=0`）。
窗口退出时工作区：本票三件源码 sha256 与本窗口开始前一致（`ab-cycle` 亦打 `AB-FULL: restore-identical=true`）。

## 七、红线对账

**① 命令名／路由／`WAKE_TABLE`／HELP 卡片归属一字不动。**
`git status --porcelain` 对 `src/goal/commands.ts`／`src/goal/routes.ts`／`src/triggers/**`／`src/cli/**`／
`src/render/html.ts` **全空**（只出台账、不落改动）。可执行守望＝新测试 ②：声明件
（`src/goal/routes.ts` 第 37 行那条 exec 行）与生成物（`routes.generated.ts` 的 `COVERAGE_REPAIR_ROUTES`）
里 `看目标推荐` 记录的 `cli` 同为
`calorie-cmd-read calorie.view.goal-recommend --params '{"profile":"cut"}'`，且产物复制日志第 4 段逐字相同。

**② 不碰 `src/render/html.ts` 的旧片段函数（只出台账不删）。**

台账（`.scratch/t589/fragment-ledger.mjs` 现算，只统计不改件；口径＝「生产侧调用」＝`src/**` 里除
barrel 转出件 `render/index.ts` 之外的引用，注释里的名字不算）：`html.ts` 现有 **45 个** `render*` 片段出口，
其中 **39 个生产侧零调用**（只剩 barrel 转出 ＋ 测试直调），6 个仍在生产用（`renderHelpLookupHtml`／
`renderErrorHtml`／`renderPlanHtml`／`renderPlanVsActualHtml`／`renderPlanWritePreviewHtml`／`renderPlanWizardHtml`）。

本票涉及那一件：

| 件 | 位置 | 本票前的调用方 | 本票后 | 处置 |
|---|---|---|---|---|
| `renderGoalRecommendHtml` | `src/render/html.ts:262` | `src/goal/read.ts`（生产侧唯一） | 生产侧**零调用**（只剩 `render/index.ts` barrel 转出 ＋ 测试直调） | **本票不删**（写集只到 `goal/`），删除与其余 38 件的清尾另开票 |

## 八、结构五步对账（`docs/agents/structure.md`）

1. **影响清单（事前）**：`packages/skill-calorie/src/goal/` 三件——`read.ts`（入口那一行 `html:` 换装配）、
   `resultDocs.ts`（族内三个小件加 `export`，行为不动）、新建姊妹件 `goalRecommendDoc.ts`（本页版式）；
   加测试件与证据件。**实际触碰**＝这五件 ＋ 本目录 `t589-GATE-RUN.log`（记录导出），偏差零。
2. **结构设计**：新件一页一装配（同族先例 `goalWeightDoc.ts`），对外**一个名字** `buildGoalRecommendDoc`；
   复用族内 `cardOf`／`section`／`tailOf`（同目录内互相用，按铁律五不计入 `resultDocs.ts` 对外五名）；
   跨目录只引共用位（`shared/docPage.ts`＋`base-paint/blocks`）与同包渲染层 `humanText`（既有先例）。
3. **写代码**：常量只写一处（`DOC_TITLE` 与目标域各页同值、`CMD_KEY` 与路由同键、`source` 由调用方给）；
   页面里零字号／零色值（版面单源在 `base-paint`）。
4. **超线报警**：本包告警线 350（LF）。本票三件实况＝`goalRecommendDoc.ts` **111**、`read.ts` **176**、
   `resultDocs.ts` **341**——**都不越线**；`resultDocs.ts` 由 333 涨到 341（＋8 行＝一段头注 ＋ 三处导出说明），
   **未超线**故不触发第四步，仅记账：它已贴线，后续再往上加会当场挂号。
5. **交付对账**：第一步每一行都有着落（见上），无「说 A 碰 B」。

## 九、门禁运行记录（对账）

导出件：`docs/skills/skill-calorie/t589-GATE-RUN.log`（逐字复制 `.scratch/locks/gate-runs.log` 里
本席窗口 `ticket=589` 的 `START`／`RUN` 行；04:49–04:50 那两条是调查席的 `probe-baseline`，未收）。

| runId | 命令 | exit | 用途／摘要 |
|---|---|---|---|
| `227ed538` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` | 0 | 基线重建（改前 dist 对齐当刻源码） |
| `1c0689c4` | 同上 | 0 | 改后重建 |
| `d1bc7e49` | `node --test …/goal-recommend-589.test.mjs` | 1 | **自暴露**：`createHash` 错从 `node:fs` 取（导入期错，非判据红） |
| `eea421ea` | 同上 | 1 | **自暴露**：命令原文住 `data-t` 载荷、可见文本里读不到（断言写法错） |
| `fe8ff5dd` | 同上 | 1 | 同上未改完 |
| `f36c8b64` | `node .scratch/t589/final-locked.mjs` | **0** | **主绿窗口**：靶向测试 5/5 ＋ 变异自证红/绿 ＋ A/B 对照（窗口内三步均**直调**底层命令，未套第二层加锁包装器） |
| `fa3e71c7` | `node packages/skill-calorie/scripts/check-warning-line.mjs` | 1 | 台账门：**红在 4 件 `src/diet/**` 的陈化**（他人在途），与本票无关；本票三件均 < 350、不挂号 |

**非门禁运行（如实声明，不充作门禁证据）**：`.scratch/t589/probe.mjs`（电池）、`compare.mjs`（对照）、
`peek-cmd.mjs`、`fragment-ledger.mjs`、`export-gate-runs.mjs` 都**只写系统 tmp 与本票草稿目录**，
不动工作区、不落 dist、不写库，故未持锁运行（协议 §2「需要持锁」清单之外）；主绿窗口内那两次电池
（`probe.mjs --tag ab-fragment／ab-full`）在持锁窗口内、按直调口径随 `f36c8b64` 一并声明。

## 十、真库零接触

全部实跑都经 `SKILLS_DB_PATH=<系统 tmp 的 mkdtemp 目录>`（探针与新测试件同口径，源码里可查）；
真库 `D:\2Study\StudyNotes\.db\calorie_data.db` 只有**只读哈希**一枚：
`D40D58F50E74E294A11E410B4D9E2142FCBCD8993328A4EFB453F1A0D62A1083`（3072000 B，与 `t256-锁票-证据.md` §五同值）。

## 十一、没做完 ＋ 下一手

1. **验收墙／索引未同步（转票）**：`docs/skills/skill-calorie/scene06-验收墙/produce.mjs:181` 仍把
   `看目标推荐` 记为「无产物」（链路索引.html 同处也写旧说法）。本票写集不含验收墙，故未碰——
   下一手把这页接进验收墙（此刻它已出完整文档，73285 B）。
2. **旧片段清尾（转票）**：`src/render/html.ts:262` 的 `renderGoalRecommendHtml` 与本表另外 38 个
   生产侧零调用件，删除超出本票写集（红线②只许出台账）——归清尾票一次收。
3. **他人在途的红**：`check-warning-line` 的 4 条陈化（`nutritionPortDocs`／`libraryDocs`／`receipt`／
   `reviewDocs`）与 `goal-result-254` 的冻结值漂移（`t256-锁票-证据.md` §六已登记）都不是本票引入，
   本席只记账不动手。
4. 本席**未跑全量测试**（纪律禁 `pnpm test`），只跑靶向一件；`goal-result-254/290/566` 等邻接件未复跑。
