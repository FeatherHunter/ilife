# t337 体重盘与回执证据（页面⑤⑥ 11 词片段→完整文档）

> 票 `#337`（地图 `#154`）：今日体重盘 2 条读 ＋ 写后回执 9 条写（4 键），产物切完整文档。
> 形状照抄 `#179`／`#330`（能力目录整页回执端口经 `index.ts` 转出，分派层只调门）。
> **本证据件落盘时树被邻席半截工作挡住（见 §阻塞），门禁与真跑待树绿后持锁复跑，结论待确认。**

## 一、开工基线指纹（动前重采，LF 口径 `split('\n').length - 1`）

| 件 | LF | 字节 |
|---|---|---:|
| `src/weight/log.ts` | 149 | 7419 |
| `src/weight/edit.ts` | 66 | 4066 |
| `src/weight/index.ts` | 42 | 2382 |
| `src/cli/write.ts` | 81 | 5797 |
| `test/profile-doc-179.test.mjs` | 269 | 16958 |
| `src/profile/receipt.ts`（参照） | 30 | 1596 |
| `src/diet/receipt.ts`（参照） | 151 | 6604 |

- 认领：`gh issue edit 337 --add-assignee "@me"` exit 0（第一次缺引号 exit 1，属用法错，已纠正重跑）。
- 抢锁广播：`gh issue comment 337` exit 0（`#issuecomment-5659076778`），声明动 `src/cli/write.ts` 与 `profile-doc-179` 同条断言，跨图 02／04／06／09 见此止步协调。
- 树状态（动前）：`.scratch/locks/gate.lock` 不存在；`git status` 已有他人在途改动（`combos.yaml`、`present.ts`、`build-help.mjs`、`keys.ts`、`routes.generated.ts` 等），本票一律未碰。
- Map Notes 已查：权威表 `D:\2Study\StudyNotes\SKILLS\卡路里\scripts\_triggers.py` 指针、三指针＋`packages/skill-calorie/src/weight/`、生成链 4 步、两道门、抢锁 `--ticket 337`、判定三档、只 add 自己、SKILL 还原、`#310` 本地为准、共用 7 处（本票只涉其中 `src/cli/write.ts` 一处）。

## 二、影响清单与交付对账（偏差为零）

| # | 件 | 一句话 | 状态 |
|---|---|---|---|
| 1 | `src/weight/receipt.ts`（新增） | 三变体整页回执端口，唯一对外 1 件 `weightReceiptDoc` | 已落 |
| 2 | `src/weight/index.ts` | 转出 `weightReceiptDoc`（门 5→6，见 §四） | 已落 |
| 3 | `src/weight/log.ts` | 体重盘加今日卡／结论卡／7 天标题；批量回执 items 全量三态 | 已落 |
| 4 | `src/weight/edit.ts` | 改类 items 补改前 → 改后；删类 items 补快照行 | 已落 |
| 5 | `src/cli/write.ts`（抢锁广播后） | `?? weightReceiptDoc(…)` 一支续接，饮食分支原样保留 | 已落 |
| 6 | `test/profile-doc-179.test.mjs` | 钉死断言收窄＋新增 `#337` 整页测试（有意改口径，见 §口径变更） | 已落 |
| 7 | `test/weight-receipt-337.test.mjs`（新增） | 11 条真跑用例（2 读＋9 写，临时库回读对账） | 已落 |
| 8 | 本证据件 | 取证与交接 | 已落 |
| 9 | `.changeset/t337-weight-receipt.md`（新增） | 记账（文件名本票唯一） | 已落 |

禁碰面（均未动）：`render/index` barrel、`plate.ts`／`plateDocs.ts` 薄层、`legacy/scene-03.ts`、冻结唤醒词与 sha 断言、`base-render` 契约、生成物（手改无效）、`SKILL.md`（只有 `#338` 能动）。

## 三、设计（三变体＋今日盘）

- 单条记体重（老实物 `weight_log_receipt.html`）：本次体重（大字）／日期时间／较上次差值（`records.deltaLast`）／距目标差（`records.goalDiff`）／近 30 天均值与趋势＋小图（`figures.weightTrend` 正本，失败即降级无图仍整页）／备注行。
- 批量（老实物 `weight_batch_receipt.html`）：写入／跳过／失败三数（回执行状态计数）＋逐条明细表（日期／体重／状态／原因）。
- 改／删（老实物 `crud_receipt.html`）：改前 → 改后对照表；删除快照表＋「硬删除，不可恢复」口径块（`HARD_WORDING` 同源）。
- 公共头：`statusCard`＋影响行数＋写入字段、`reconcileDisclosure`、`copyArea`（三格式菜单＋复制日志，第 4 段为真跑命令原文），`assembleDocPage` 装完整文档。
- 今日盘（老实物 `weight_dashboard.html`）：既有 4 卡／曲线／明细／复制区一字不动（`#109` 8 针脚全留），加今日卡（今日值／日期／较上次差值）＋结论卡（趋势＋距目标一句）；7 天内曲线标题为「近 7 天体重曲线」（仍含「体重曲线」针脚）。
- 数据契约不动：摘要文案、`recordId`／`ids`／`writtenFields`／`affectedRows` 口径与改前一致（落库测试与 M5 探针只认这些）；`items` 只做加法（批量全量、改类对照、删类快照）。

## 四、口径变更（有意改钉死断言）

- `profile-doc-179.test.mjs` 末条「其余会改数据库的命令仍是原回执片段」抽样拿掉 `calorie.weight.log`（它已切整页），保留水／食品／目标三条；新增 `#337` 一条钉死体重 4 键整页与三变体关键字段（不断言、无跳过）。
- `src/cli/write.ts` 头注「其余 32 条一字不改」更新为「其余 15 条（35 − 3 − 13 − 4）」，算式写明。
- `src/weight/index.ts` 门 5→6 件（`weightReceiptDoc`）：票面 `#154` 2026-09-14 裁定 7 与本票遗留出口明令「经本能力 `index.ts` 转出」；备选（仿饮食直引）会留下两种形状，故从票面。后续图若统一端口形状，本门可回 5（记后续图债）。

## 五、超线检查（350 行 LF，本包口径）

| 件 | 改后 LF | 结论 |
|---|---|---|
| `src/weight/receipt.ts`（新增） | 344 | 未超线（初版 351，曾超 1 行，已压头注瘦身至 344，见本节） |
| `src/weight/log.ts` | 181 | 未超线 |
| `src/weight/edit.ts` | 85 | 未超线 |
| `src/cli/write.ts` | 88 | 未超线 |
| `src/weight/index.ts` | 45 | 未超线 |

- 结论：初版 `receipt.ts` 曾 LF=351（超 1 行），当场瘦身头注 6 行 → 344。
  现均未超线，无需报「已超线，需要根据规则进行重构。」。

## 六、阻塞（待树绿后持锁复跑，结论待确认）

- `GATE-RUN runId=211ee238-71a2-49cd-98f8-1bc52d77101b cmd=pnpm build exit=2`：本票 `log.ts` 括号笔误（`push(` 配 `]`），已修。
- `GATE-RUN runId=78db9aa8-1e63-4ebd-b527-29f1abc62341 cmd=pnpm build exit=2`：卡在他人在途文件 `packages/skill-calorie/src/analysis/routes.ts(109,84)` TS2820（`calorie.view.multi-trend` 未同步进 `RouteKey`）。同批他人在途：`analysis/commands.ts`、`weight/compare.ts`／`history.ts`／`volatility.ts`、`home/*`、`render/*`、`scene-10-analysis.ts`、未跟踪 `analysis/multiTrend*.ts` 等——本票一律未碰、未还原。
- 待跑（树绿后，持锁）：`pnpm build → pnpm gen → pnpm build → pnpm help:build`、`pnpm gen:check`、全量测试、棘轮两件、`weight-receipt-337` 11 条真跑（含落盘绝对路径与字节）、`t293` 探针 P1、自证变异红／还原一致两行读数。

## 七、进度与下一步

- 进度：85%（实施落盘＋提交，验证待确认，不 close）。
- 下一步：待邻席落定树绿 → 持锁跑 §六全链 → 11 条落盘路径与字节回填下表 → 变异自证 → 请编排者复核。
- 真出口落盘表（待复跑回填）：`view-today`／`view-overview`／`log`／`log-note`／`log-backfill`／`batch`／`update-id`／`update-date`／`remove-id`／`remove-date`／`remove-range` 各 `.html` 绝对路径与字节数。
