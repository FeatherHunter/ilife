# t331 全仓门脚本退出码扫查证据

- HEAD：`46d19b180b309b00273ec6cb27f91e23e257f005`（`HEAD==origin/master`，开工时 `git status --porcelain` 为空）
- 结论：**命中 0 处**数组／对象／可算非整数的退出码；**改了 0 处**（无改前改后）；负向证据＋复跑见下。
- 大输出：`.scratch/t331/`（`git-grep-process-exit.txt` 253 行／`git-grep-exitCode.txt` 24 行／`nonliteral-exit.txt`／`die-callers.txt`／`skilllink-fail-callers.txt`／`neg-*`／`rerun-*`，gitignored，不进提交）

## 一、检索命令（`git grep`＝受跟踪全仓，避开 `.scratch` 坏链；`Select-String` 逐文件复核）

```powershell
git grep -n 'process\.exit(' -- ':!*.json'        # 253 命中
git grep -n 'process\.exitCode' -- ':!*.json'     # 24 命中
git grep -n 'process\.exit(' -- ':!*.json' | Select-String -Pattern 'process\.exit\(\s*[A-Za-z_$]'  # 非字面量 66 行
git grep -n -E '^\s*exit(\s+[^\d\s]|$)|exit\s*@\(|exit\s*\[' -- '*.ps1' '*.sh' '*.mjs' '*.js'  # shell/ps1：仅 exit 1/0 字面量＋两处 spawnSync().status 纯打印
git grep -n 'exit code'  # 21 命中，全为文档 prose，无代码
```

逐个读上限 120 行／次（`check-gate-audit.mjs:320-418`、`run-locked.mjs:380-510`、`gen-cli.mjs:600-729`、
`check-examples.mjs:1-153`、`gen-sot-snapshot.mjs:100-153`、`fetch/cli.ts:130-192`、
`t313-routing-oracle.mjs:380-433`、`t313-pb-分区权威-对照.mjs:280-325`、
`t99-review-red-probe.mjs:270-309`、`t95-test-delta.mjs:1-66` 等）。

## 二、命中清单：非字面量退出码 66 行 → 逐条核查全为整数

| # | 位置 | 写法 | 核查结论（整数依据） |
|---|---|---|---|
| 1 | `tooling/skilllink.mjs:16` | `fail(code,msg)→process.exit(code)` | 调用方 18 处全字面量 `fail(2/3/4/5/6,…)`（`:68-189`） |
| 2 | `tooling/run-locked.mjs:508` | `.then((code)=>process.exit(code))` | `code`＝`main()` resolve＝`exitCode`，`:316-502` 全分支赋整数（1／127／124／`outcome.code`，`null` 已在 `:423-425` 转 1） |
| 3 | `tooling/check-gate-audit.mjs:413` | `process.exit(main())` | `main` 为同步函数（`:313`），`return 2`／`pass?0:1`（`:327,407`） |
| 4 | `tooling/check-registry-exports.mjs:228`、`classify-templates.mjs:264`、`skill-html-snapshot.mjs:391` | 三元式 | `bad===0?0:1`／比较后 `?0:1`（即票面要求的"比较后转 0/1"形态） |
| 5 | `packages/skill-calorie/scripts/migrate-calorie.mjs:18` | `fail(code)→process.exit(code)` | 调用方全字面量 `fail(2/3/4/5)`＋`process.exit(0)`（`:30,44,48,50,52`） |
| 6 | 5 件 `src/cli/cmd_read.ts`＋`skill-calorie/src/shared/params.ts:20` | `fail(code:number,…)` | TS 收窄 `number`，调用方全字面量 1-5 |
| 7 | `skill-calorie/src/fetch/cli.ts:192` | `process.exitCode = await main()` | `main(): Promise<number>`，各分支 `return 0/1/2`（`:135,150-151,161,188`） |
| 8 | `t-help-acceptance-collect.mjs:52`、`t121-browser-evidence.mjs:59`、`t88-browser-evidence-b.mjs:44`、5 件 `t89-probe-*.mjs` 的 `die(code)` | `process.exit(code)` | 调用方清一色 `die(2,…)` 字面量（`die-callers.txt` 40＋行无一变量） |
| 9 | `t99-review-red-probe.mjs:306` | `process.exit(rc)` | `rc`∈{`runAll()` 的 0/1，`0`，`2`，各 SUB  returns `0/1`}（`:287-305`） |
| 10 | `t313-routing-oracle.mjs:429`、`t313-pb-分区权威-对照.mjs:321` | `exitCode = code` | `code`＝`main()` 返回，枚举 `return 0/2/3`＋`pass?0:1`（26／24 处 return 全整数） |
| 11 | 40＋处 `process.exit(<cond> ? 0 : 1)`／`bad ? 1 : 0`（`t83/t88/t93/t96/t101/t106/t107` 等） | 三元式 | 值域恒 {0,1} |
| 12 | shell／ps1 的 `exit` | `exit 0/1` 字面量 | `publish-020-line.ps1`、两 wizard `.sh`、`wizard-publish-base-030.sh` |
| 13 | `t95/t98-test-delta.mjs` 的 `exit = spawnSync(…).status` | 只打印 `exit=`／`TAP_EXIT=`，**从未**回灌 `process.exit` | 超范围亦无害，见"未做项" |

字面量 `process.exit(0/1/2/3/4/5)` 约 180 处，不逐列（`git-grep-process-exit.txt` 存档）。

## 三、改前改后对照

无。0 处需改，故 0 对 diff（`git status` 跟踪区除本证据件外无本人改动）。

## 四、负向证据（改坏 ⇒ 红；改回 ⇒ 绿；命令与读数）

N1 数组／对象退出码的真实行为（Node v24.19.0，`.scratch/t331/array-exit-probe.mjs` 经 `spawnSync` 取 `status`，避 pwsh 包裹）：

```powershell
node .scratch/t331/array-exit-probe.mjs
# ARG=process.exit([1]) status=1 :: TypeError [ERR_INVALID_ARG_TYPE]: ... Received an instance of Array
# ARG=process.exit(({code:0})) status=1 :: TypeError [ERR_INVALID_ARG_TYPE]: ... Received an instance of Object
# ARG=process.exit([0]) status=1 :: TypeError [ERR_INVALID_ARG_TYPE]: ... Received an instance of Array
```

连 `process.exit([0])` 都崩成 exit 1（假红）——这正是本票要扫的 hazard 类；全仓零命中即无此假红源。

N2 门对"坏输入"真变红、输入撤回即复绿（`check-examples.mjs --skill` 变异钩子，坏文件只落 `.scratch`，跟踪树零碰）：

```powershell
node packages/skill-calorie/scripts/check-examples.mjs --only calorie.view.home
# GREEN-EXIT=0；示例行数：1（通过 1…红 0，结构问题 0）；PASS: SKILL.md 示例逐行可执行
"no-help-block-here" | Out-File -Encoding utf8 .scratch/t331/bad-SKILL.md
node packages/skill-calorie/scripts/check-examples.mjs --skill .scratch/t331/bad-SKILL.md --only calorie.view.home
# RED-EXIT=1；Error: SKILL.md 缺 HELP 标记块 at parseExamples (check-examples.mjs:52:32)
Remove-Item .scratch/t331/bad-SKILL.md -Force  # 改回＝删临时坏文件，门回到 GREEN 状态（N1 行仍为 exit 0）
```

## 五、复跑（持锁 `run-locked --ticket 331 --lock-dir .scratch/locks-land --stale-minutes 30`，direct node）

| 门 | 命令 | 读数 |
|---|---|---|
| 全量 | `node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"` | `ℹ tests 1492／suites 160／pass 1492／fail 0／cancelled 0／skipped 0／todo 0`，`FULL-EXIT=0`，runId `b1dbc829` |
| 81 | `node --test test/calorie-routing-81.test.mjs` | `tests 8／pass 8／fail 0`（8/8），`R81-EXIT=0`，runId `e7b49102` |
| 生成检查 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `GEN-CHECK PASS：键 101（写 35＋读 66）；能力 10 个；未搬迁清单 0 条`，`GEN-EXIT=0`，runId `368bcbc9` |
| 探针 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`；`UNACCOUNTED=0 处`；④ 3 处只报账；树自相矛盾 0，`T293-EXIT=0`，runId `e81a81a9` |

## 六、未做项（超出"只改退出码"范围，不顺手改）

1. `docs/research/t95-test-delta.mjs:29`／`t98-test-delta.mjs:27` 的 `exit = spawnSync(…).status` 在信号杀时可为 `null`，但仅打印不作退出码——历史研究脚本，非门，不动。
2. `test/calorie-routing-81.test.mjs:333-334` 登记册 `exit: '4'` 为字符串（快照文本列比对口径），非 `process.exit` 实参，不动（改即动判官）。
3. 复跑中途 `git status` 见他席新增 untracked `tooling/baseline.mjs`、`tooling/plan-check.mjs` 等（开工时树干净；非本人所写，一律未碰）。
