# t464 独立复核（地图#158 · 票 #464）

复核席与实现换人。被审 commit `89d92509e30f56342a265f9ae386bbf3c8636309`。复核当刻 HEAD 为 `d18a23e`，其间 444／456／162 三提交均未碰本票 5 件；`bodyDocs.ts` 最新触者仍为被审提交。

## 一、判定

**PASS**（94 分：契约 29／证据 23／对照 19／红线 15／文档 8）。无 S1；复核全程合规（全部编译／测试／变异／提交经 `tooling/run-locked.mjs` 持锁，同 runId 落盘 18 START＋18 RUN）。

缺陷清单：

- S3-1（本票范围）：证据 `t464-备注列口径证据.md` §六写本件 318 行，LF 口径实测 322（回执约 323）。4 行笔误，未超 350 线，不影响结论，建议顺手订正，不阻塞。
- S3-2（范围外）：中央 `gate-runs.log` 里实现 6 行 RUN 时间戳同为一秒且中文路径乱码，疑似窗末批量落盘；实体运行经本席独立复跑逐条证实无分歧。转票措辞见第四段。
- 票面遗留（`view.ts:37 trendDays` 分叉、`body_fat_pct` 不可达分支、老参照件不回改、围度 `:278`、唤醒词）均只登记未动，符合票面“不许动”。

## 二、机器证据（复核 runId `t464R-9f3a2c1e`）

- 初编 `npx tsc -b packages/skill-calorie` exit 0（首步 waitedMs=10008，排队过 444 活锁后即抢）。
- t464 初绿 `tests 3 pass 3 fail 0` exit 0；`T464-VISIBLE null格="—" 空串格="" 文本格="晚餐后测"`；`T464-PAYLOAD null=null 空串="" 含破折号行=0`；`T464-COMMENT 211/esc/空串值/input.note逐字命中`。
- t362 回归 `tests 15 pass 15 fail 0` exit 0。
- 变异红（判据改回 `''`）：`tests 3 pass 1 fail 2` exit 1；还原 `RESTORE-EDIT ok sha=b9b51cb3f99b match=true`，重编 exit 0，还原绿 `tests 3 pass 3 fail 0` exit 0（T464 三行与初绿逐字一致）。
- 新探针 `.scratch/t464/review-probe.mjs`（空格／缺键／数字 0／破折号文本四边角，被审 3 用例未覆盖）：红 `raw=[" ","","","—"] RESULT: FAIL 2` exit 1；绿 `raw=[" ","—","—","—"] RESULT: PASS 4/4` exit 0（变异后与还原后各跑一遍，试完已还原重编）。
- 真库 `D:\2Study\StudyNotes\.db\calorie_data.db` 跑前跑后 size=3072000、sha256=`D40D58F50E74E294A11E410B4D9E2142FCBCD8993328A4EFB453F1A0D62A1083` 全等，零写入。
- 实现零残留：`bodyDocs.ts` 终态 sha12=`b9b51cb3f99b` 与变异前一致，`git diff` 为空；`dist` 已随还原重编，无陈旧产物。

## 三、逐条路径（只读复核，实现文件未改）

- 实现 `packages/skill-calorie/src/body/bodyDocs.ts:162-174`：判据 `typeof x.note === 'string' ? x.note : '—'`；注释点名老正本 `:211 esc`＋`:366 esc(r.note)`＋`空串值`＋`input.note ?? ''`。相对 `89d9250^` 的 diff 仅此两处（判据＋注释）。
- 老正本当场重验：`body_composition_view.html:211` 为 `function esc(s) { return String(s == null ? '—' : s); }`，`:366` 行含 `${esc(r.note)}`；写侧 `fetch/body.ts:289` 为 `input.note ?? ''`（票面 :252 已漂移，实现注记正确）。
- 测试 `packages/skill-calorie/test/t464-备注列口径.test.mjs`（164 行 3 用例，tmp 库＋`SKILLS_DB_PATH` 指 tmp）；t362 原样未碰（最新触者 `747ab73`）。
- 被审提交 `89d9250` 仅 5 件自有文件（实现＋测试＋2 证据＋1 changeset），无他人文件；本席复核新增仅 2 件（本件＋`.scratch/t464/review-probe.mjs`），持锁中文提交，不 push。
- 持锁：544（pid 13504）／444（pid 54168）活锁两次不抢，空窗即抢；释放前源码哈希、重编、复绿三齐。

## 四、未做项与下一手

- 未做：`view.ts:37` 分叉、`body_fat_pct`、围度 `:278`、唤醒词、老参照件一律未碰（票面要求）；S3-1 行数笔误留实现顺手订正；不 push，由编排者统一收口。
- 转票措辞（S3-2）：中央运行日志中文乱码＋批量时间戳，建议另开一票统一 run-locked 落盘编码（UTF-8）与逐条实时写盘；本票结论不受影响。
- 下一手：关闭口径以合并点复核为准；合并点只需重认 `89d9250` 的 5 件＋本复核的 2 件。
