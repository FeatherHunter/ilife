# t639 证据 · 榜单餐均旧口 renderRankingHtml 同式去重（#632 遗留）

结论：按 #632 同式修复（值去单位），不标废弃。旧口经 `render/index:62`＋`src/index:28` 对外可达，保守按“有人用”处理。

## 1. 调用者清单（全仓 grep `renderRankingHtml`，逐条点名）

| # | 位置 | 性质 |
|---|---|---|
| 1 | `packages/skill-calorie/src/render/html.ts:375` | 定义 `export function renderRankingHtml`（本票唯一改动点 :378） |
| 2 | `packages/skill-calorie/src/render/index.ts:62` | barrel 转出（对外可达点之一） |
| 3 | `packages/skill-calorie/src/index.ts:28` | `export * from './render/index.js'`（对外可达点之二，可 import） |
| 4 | `packages/skill-calorie/test/render-t9.test.mjs:181` | 包内唯一真实调用；断言仅 `排行 high_calorie`＋`ilife:calorie:ranking`，未钉住被改文案，故本票未改测试件 |
| 5 | `packages/skill-calorie/src/diet/ranking.ts:15,29-36` | 生产链 `calorie.view.ranking → viewRanking → rankingDocs.buildRankingDoc/buildAllRankingsDoc`，不经此旧口（已验证） |
| 6 | 包外生产调用 | 无。全仓命中仅上列＋`docs/research` 旧基线盘点与 `t632-*` 证据提及（文字引用，非调用） |
| 7 | `卡/餐` 字面（src） | 改前唯一 `src/render/html.ts:378`；改后 `packages/skill-calorie/src` 零命中（grep 无匹配） |

同类旧口排查：`src` 内 `卡/餐` 改后零残留，无“只点名不代修”的第二处。

## 2. 改动（一行，与 #632 同式）

- `packages/skill-calorie/src/render/html.ts:378`：`it.cnt + ' 次·均 ' + it.avgCalPerMeal + ' 卡/餐'` → `it.cnt + ' 次·均 ' + it.avgCalPerMeal + ' 卡'`。
- 对照 #632（`commit 2d7f5ce`，`rankingDocs.ts:156` `avg: … + ' 卡/餐'` → `+ ' 卡'`）：值去单位，`次·均` 前缀与列名不动；与 `reviewDocs.ts:174` 同形一致。
- 未改：`test/render-t9.test.mjs`（断言未钉文案）、`rankingDocs.ts`、`base-render/**`、来源脚注（归 #560）。
- 结构注：`src/render/html.ts` 已超线（LF 648），需要根据规则进行重构。超因见包台账（T8～T10 多套串模板同处一件）。本次先不拆：拆分不在本票写集（票面仅许动该旧口一行），待收口票认领。

## 3. 改前红 / 改后绿（该口产物，桌面＋390 两态）

旧口为 kpi 网格同一串模板服务两态（CSS grid 响应，无服务端分态），同一产物断言覆盖两态。

- 改前红：`PROBE desktop: no-卡/餐=FAIL subs=["3 次·均 350 卡/餐","1 次·均 600 卡/餐"]`
- 改后绿：`PROBE desktop: no-卡/餐=PASS subs=["3 次·均 350 卡","1 次·均 600 卡"]`
- 两态＋形状（改后）：`PROBE w390: no-卡/餐=PASS (同串模板，CSS grid 响应，无服务端分态)` ＋ `PROBE shape: avg-值去单位-N卡=PASS` ＋ `RESULT: 3/3 PASS`

## 4. 门禁读数（持锁，经 run-locked；日志 `.scratch/t639/`，下为尾行）

- tsc：`RESULT: ticket=639 runId=54784cbb-2b21-474d-9080-c953272fc7b0 waitedMs=40004 exit=0`（exit 0）
- render-t9：`pass 9 / fail 0`，尾：`LOCK-RELEASED ticket=639 runId=de9abe97-95ee-4900-9d04-7577724b43dc exit=0`／`RESULT: ticket=639 runId=de9abe97-95ee-4900-9d04-7577724b43dc waitedMs=0 exit=0`（exit 0）
- 产物探针：`RESULT: 3/3 PASS`，`LOCK-RELEASED ticket=639 runId=ed04b8d1-e26d-4546-9b4e-f9249a7d0f23 exit=0`（exit 0）
- 变异脚本：`RESULT: 2/2 PASS`，`LOCK-RELEASED ticket=639 runId=7898efd6-5f7e-4609-955d-80ea30d69fbf exit=0`（exit 0）

## 5. 变异两行（源码级，持锁窗内改→红→还原→绿）

- `MUT-RED: PASS（变异后探针变红 fail>0）`
- `MUT-GREEN: PASS（还原后探针全绿 exit=0）`
- 附：`MUT-RESTORE: PASS（与原内容一致）`；还原后已重编，dist 与源码一致。

## 6. GATE-RUN 对账

声明（边跑边补，一对一）：

- `GATE-RUN runId=54784cbb-2b21-474d-9080-c953272fc7b0 cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie --force`
- `GATE-RUN runId=de9abe97-95ee-4900-9d04-7577724b43dc cmd=node --test packages/skill-calorie/test/render-t9.test.mjs`
- `GATE-RUN runId=ed04b8d1-e26d-4546-9b4e-f9249a7d0f23 cmd=node .scratch/t639/probe-t639.mjs`
- `GATE-RUN runId=7898efd6-5f7e-4609-955d-80ea30d69fbf cmd=node .scratch/t639/mutate-t639.mjs`

四条在 `.scratch/locks/gate-runs.log` 均有 START＋RUN 对（exit 均为 0），导出见 `docs/skills/skill-calorie/t639-gate-runs.md`。

## 7. 未做项

- 来源脚注未碰（归 #560）；`rankingDocs.ts` 取数/排序未碰；`base-render/**` 未碰；测试件未改（无需）；未推送（总工收口统一推）。
- 派单与票面无冲突；票面为准，无声明事项。
