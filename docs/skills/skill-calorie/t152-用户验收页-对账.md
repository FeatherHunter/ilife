# t152 用户验收页 · 逐条对账

> 交付物：`docs/skills/skill-calorie/t152-用户验收页.html`（30,813 字节，2026-09-13 实测）
> 本文件供对抗式审查：验收页里**每一个字、每一个数字、每一个路径**都要在这里指得出出处。
> 口径：**测不到就写「未测到」**，不写估计值。本票**不跑** `pnpm build`／不跑测试／不重生成产物（并发期约定）。

---

## 0. 本票边界

- 只新建 `docs/skills/skill-calorie/t152-用户验收页.html` 与 `docs/skills/skill-calorie/t152-用户验收页-对账.md`；
  草稿、副本、截图、清单落在 `.scratch/t152-user-accept/`。**未改任何源码、未碰测试、未碰数据库、未跑门禁。**
- 议题 #178 要求的三件事里，本票**只做第①件**（准备验收清单）；②「把技能装到真机」与③「请用户验收」由编排者与用户另办。

---

## 1. 四条唤醒词「用户要念的那句话」——逐字来源

**唯一定义地**：`packages/skill-calorie/src/triggers/scene-07-profile.ts`（整份 9 行，一个唤醒词一行）。

| 唤醒词 | 行号 | 取的字段 | 同一行同一文本的另一份 |
| --- | --- | --- | --- |
| 设置档案 | 第 5 行 | `prompt_template` | 同行 `main_prompt.text`（逐字相同） |
| 设活动量 | 第 6 行 | `prompt_template` | 同行 `main_prompt.text` |
| 改档案 | 第 7 行 | `prompt_template` | 同行 `main_prompt.text` |
| 查档案 | 第 8 行 | `prompt_template` | 同行 `main_prompt.text` |

验收页里四段 `<pre>` 是这四处字段的**逐字转录**（只把源码里的换行转义还原成真换行，其余一字未动）。

**为什么取这里**（其它候选处都实测过，都不含 prompt 全文）：

1. `gh issue view 175 / 176 / 177 --json body`（正文落盘到 `.scratch/t152-user-accept/issue-175.md`／`issue-176.md`／`issue-177.md`）
   → 三份正文里 **grep「请你加载技能」命中 0**。议题正文写的是目标、判定与验收方式，**没有** prompt 全文。
2. `packages/skill-calorie/SKILL.md` → 只在第 117–119 行有**命令表**（命令名 ＋ `--params` 一行），第 64／72 行讲预检确认页装配；
   **没有**「请你加载技能…」这段用户视角全文。
3. 议题 #152／#178 正文 → 只有验收口径与用户原话，**没有** prompt 全文。
4. `src/triggers/wake-assets.ts` 是同一批 prompt 的第二份存放处（#177 正文第 4 条说明两份并存），
   但本票取自 SoT 主文件 `scene-07-profile.ts`——它是 #177 点名要改的那一份，也是「同一行里连 `cli`／`data_source` 一起」的那一份。

**命令名索引**（只作对照，**不写进用户要念的话**）：

| 唤醒词 | 命令 | 出处 |
| --- | --- | --- |
| 设置档案 | `calorie.profile.set` | `SKILL.md:118`；`src/cli/keys.ts:44`（标题「设置档案」） |
| 设活动量 | `calorie.profile.activity` | `SKILL.md:117`；`src/cli/keys.ts:45`（标题「设活动量」） |
| 改档案 | `calorie.profile.update` | `SKILL.md:119`；`src/cli/keys.ts:46`（标题「改档案」） |
| 查档案 | `calorie.view.profile` | #177 正文与结论评论 |
| 预检确认页（三条写入词共用） | `calorie.view.profile-wizard` | `src/cli/keys.ts:140`（标题「档案预检」）；`SKILL.md:64` |

---

## 2. 「预期产物：过程型 / 结果型」从哪读出来的

- 三条写入词 → 先出**过程型 HTML（预检确认页）**，确认后写库、再出**结果型 HTML（写后回执页）**：
  `packages/skill-calorie/SKILL.md:64`（「`设置档案`／`设活动量`／`改档案` → `calorie.view.profile-wizard`（场景 07 三条写入词共用一页）」）、
  `SKILL.md:72`（「配置型 wizard 词命中即先出页，不再走文字 verify」）、
  `packages/skill-calorie/src/cli/write.ts:8`（#179 接线：三条写入词的回执页换成写后回执）。
- 预检确认页确实是**整文档**：`src/cli/cmd_read.ts:578-585`，`calorie.view.profile-wizard` 走 `buildProfileSettingDoc(v)`。
- 查档案结果页是**整文档**：`src/cli/cmd_read.ts:1033-1040`，`calorie.view.profile` 走 `buildProfileViewDoc(v)`。
- 查档案**没有**预检确认页：#177 正文「这条是场景 07 里唯一**不需要预检确认页**的一条 —— 也是验证「不是每条都必须有过程型页面」的样本」。
- 议题 #177 正文的页面三分法：「场景 07 的页面归 3 类：① 预检确认页 ② 写后回执页 ③ 查档案结果页」。

---

## 3. 产物路径与字节数——逐条实测记录

### 3.1 落盘通式

读自 `packages/skill-calorie/src/cli/cmd_read.ts` **第 11–13 行**件头说明：
`<SKILLS_DB_PATH>/calorie_html/<中文command>_<YYYYMMDD>_<HHMMSS>[_N].html`（同秒冲突自动加后缀）。

`SKILLS_DB_PATH` 本机实测：`$env:SKILLS_DB_PATH` → `D:\2Study\StudyNotes\.db`。

### 3.2 真库目录实测

```
Get-ChildItem 'D:\2Study\StudyNotes\.db\calorie_html\' -File   → 328 个（计数命令输出 TOTAL=328）
```

按文件名主体（`-replace '_\d{8}_\d{6}(_\d+)?$'`）分组后，**与场景 07 有关的只有 1 个**：

| 文件 | 字节数 | 落盘时刻 | 判定 |
| --- | --- | --- | --- |
| `D:\2Study\StudyNotes\.db\calorie_html\档案视图_20260911_211158.html` | **1,358** | 2026-09-11 21:11:58 | **片段**：我读过全文，以 `<section class="ilife-page" …>` 起、`</section>` 止，无 `<!doctype html>`；早于 #179（`3da2559` 2026-09-12 12:45）与 #177（`795cf6f`）收口 |

**设置档案／改档案／设活动量／档案预检四条词，在该目录里本次未测到任何产物**——不是「没找」，
是上面那次前缀分组的结果里确实没有这几个主体（分组结果列表见命令输出，主体含「档案视图」1 个，无「设置档案」「改档案」「设活动量」「档案预检」）。

### 3.3 手边实物（完整文档）——取自 2026-09-13 01:56 一次实跑

源目录 `.scratch\t152-probe\db\calorie_html\`（另一路探针的目录）。复制到本票目录
`.scratch\t152-user-accept\samples\`，清单落在 `.scratch\t152-user-accept\sample-manifest.tsv`：

| 验收页里给的副本路径 | 原文件名 | 字节数 | 原落盘时刻 | sha256（复制后实测） |
| --- | --- | --- | --- | --- |
| `D:\ilife\.scratch\t152-user-accept\samples\wizard-profile.html` | `档案预检_20260913_015639.html` | 65,439 | 2026-09-13 01:56:39 | `6916755F2DB7EA83111D95CC6D4C569011689DFA67D378ADF73F3D0622ED48DE` |
| `D:\ilife\.scratch\t152-user-accept\samples\view-profile.html` | `档案视图_20260913_015638.html` | 63,838 | 2026-09-13 01:56:38 | `8CFBC93DA5CC637B18DE53EE798732F5FF7E4CDBBA1CB613412FA80D6063660E` |
| `D:\ilife\.scratch\t152-user-accept\samples\receipt-set.html` | `设置档案_回执_20260913_015639.html` | 60,508 | 2026-09-13 01:56:39 | `3C525AFCECEBF636CABC232E9763FCD4C2558A072E9FE693CF3095738657284F` |
| `D:\ilife\.scratch\t152-user-accept\samples\receipt-update.html` | `改档案_回执_20260913_015639.html` | 59,905 | 2026-09-13 01:56:39 | `572DD33FC101296B5DDD92FFD2625CF802154FE4DFAA631F77848E341BAF88A9` |
| `D:\ilife\.scratch\t152-user-accept\samples\receipt-activity.html` | `设活动量_回执_20260913_015639.html` | 60,146 | 2026-09-13 01:56:39 | `C31598CEE7580A6523D9EF6CE781B63A24DE6EEFDBA033A751CE7426AD407CF5` |

- 五个都实测是**完整文档**：用 `Get-Content -Raw -Encoding utf8` 读首字符，五个都 `StartsWith('<!doctype html>')` → `True`。
- 中文关键字计数（同一命令，UTF-8 读法）：`档案视图` 那份 `复制数据 ▾`×1、`复制日志`×1；`档案预检` 那份 `复制数据 ▾`×1、`复制日志`×1、`复制指令`×2；`python` 在五份里**均为 0**。

**⚠️ 一个必须记账的不稳定项**：`.scratch\t152-probe\` 是**另一路探针正在用的目录**。我 2026-09-13 两次列它，
第一次看到的文件名是 `…_015316.html`／`…_015317.html`（档案视图 63,838／档案预检 65,439／设置档案_回执 60,508），
第二次已变成 `…_015638.html`／`…_015639.html`（**字节数相同**）。所以验收页里写明了「以此为准的请用我复制好的那份（`samples\`）」。
本对账表里的字节数与 sha256 全部来自**复制进 `samples\` 之后**对副本的实测，不受源目录再被改写影响。

---

## 4. 三条判定口径（验收页「这条怎么判」的出处）

| 验收页上的话 | 出处 |
| --- | --- |
| `改档案` 在**档案不存在时应报错**（不许静默建行后更新） | 议题 #178 正文「验收清单」第 1 条；实测口径 `packages/skill-calorie/src/fetch/profile.ts:145-158`（空库守卫，抛 `尚无档案（先设置档案，再改）`），`src/cli/write.ts:834-836` |
| `设活动量` 同样有档案守卫 | `src/fetch/profile.ts:122-137`，抛 `尚无档案（先设置档案，再设活动量）`；来源标注为 2026-09-12 与「改档案」同一条裁定的延伸 |
| `查档案` 在**空库**按既有缺失阻断口径（不落盘） | 议题 #178 正文「验收清单」第 2 条；议题 #177 正文写「空库沿用既有缺失阻断口径（exit 4、不落盘、`未设档案`）」；实测口径 `src/profile/view.ts:9,16,95-98`，抛 `missing-data`／`未设档案（user_profile#1 缺失，先设置档案）` |
| 三条写入词说出口后**预检确认页应先出现** | 议题 #178 正文「验收清单」第 3 条；`packages/skill-calorie/SKILL.md:64,72` |
| 退出码 4 | `src/cli/cmd_read.ts:5`（退出码对齐 skilllink 冻结：`4` 取数／超时；缺失阻断走它） |

---

## 5. 第 3 块（复制组件 · 议题 #239）逐条出处

| 验收页上的话 | 出处 |
| --- | --- |
| 组件落点 `packages/skill-calorie/src/shared/`（`copyArea.ts`／`docPage.ts`），底层在 `packages/base-render` | 议题 #239 正文「要做的事」第 1 条（扩既有 `docPage.ts`，不新建文件；`packages/base-render/**` 改动为零） |
| 实装提交 `a8d3b27`／`814bd09`／`d5c135a` | `git log -1` 逐条实测：`a8d3b27`（2026-09-12 13:50，组件 ＋ 场景 07 五张页接「复制日志」）、`814bd09`（2026-09-12 14:09，两轴自审收口）、`d5c135a`（2026-09-12 14:11，#239 交付对账） |
| 三格式菜单另两笔 `230db27`／`03b31e6` | `git log` 实测：`230db27` 2026-09-12 19:54（#247）、`03b31e6` 2026-09-12 20:51（#248） |
| 写后回执页 ＋ 查档案结果页**各 2 颗**按钮 | 议题 #239 正文「完成时」第 1 条：场景 07 四张页（设置档案回执／设活动量回执／改档案回执／查档案结果）各出「复制数据 ＋ 复制日志」两颗 |
| 预检确认页**3 颗**（复制指令／复制数据／复制日志） | 议题 #239 正文「完成时」第 1 条：「预检确认页另带「复制指令」」；并由 2026-09-13 我重拍的整页图视觉核对确认 |
| 缺省按钮文案「复制数据」「复制日志」 | `packages/base-render/src/spec/controls.ts:292-294`（`ACTION_BAR_DEFAULTS`） |
| 三项标签逐字是 `text`／`json`／`csv` | `packages/base-render/src/spec/text.ts:12`（`COPY_FORMATS = ['text','json','csv']`）；`controls.ts:1327-1333`（菜单项标签直接取键：`esc(key)`；另见 `controls.ts:1257-1259` 的注释「不在运行时另立一张中文表——`COPY_FORMATS` 的键就是标签」） |
| 开合按钮上带 ` ▾` | `packages/base-render/src/controls.ts:1325`（`const openLabel = button.label + ' ▾';`） |
| 三项右侧用途提示「粘贴给 AI / 自己看」「结构化存档」「表格导入」 | `packages/skill-calorie/src/shared/copyArea.ts:44-45`（`MENU_HINTS`） |
| 选中后提示「数据复制成功」 | `packages/base-render/src/controls.ts:1260`（`COPY_FORMAT_OK_MSG`） |
| 成功提示「已复制／粘贴给 AI」、失败提示「复制失败」 | `packages/base-render/src/spec/controls.ts:74-82`（`COPY_TEXT_DEFAULTS`：`okMessage:'已复制'`／`okDetail:'粘贴给 AI'`／`failMessage:'复制失败'`） |
| 「复制日志」复制**六段** | 议题 #239 正文「三条硬性质」第 3 条（六段逐一列出）；底层 `buildLogText` 固定 6 段（`packages/base-render/src/text.ts:470`，段源 `spec/index.ts:145`） |
| 「没有可复制的数据时给一句话、不出死按钮」 | 议题 #239 正文「三种反馈」第 3 条与「完成时」第 2 条 |

### 5.1 实拍图（`.scratch\t239\shots\`）

- 目录**存在**。`Get-ChildItem '.scratch\t239\shots\*.png'` → **12 个 `.png`**（6 张页面 × 390／1440 两档），同名 `.json` 12 个。
  逐张的字节数与文件名在验收页里逐个列出，全部来自该次 `Get-ChildItem` 的实测输出。
- **实测 mtime 区间：2026-09-12 14:02:24 – 14:08:06。** 与两笔三格式菜单提交（09-12 19:54 / 20:51）相比**更早** →
  **这 12 张图上看到的是普通「复制数据」按钮，没有 ` ▾`。** 验收页里已用醒目提示写清这一点，并写明「按钮的数量与位置看这批图；三格式菜单看你新跑出来的页面」。
- 视觉核对过一次（工具：`vision_describe`，同时看两张）：
  - `seeded-profile-wizard-1440.png`：主标题「档案预检」，**3 颗**按钮（复制指令／复制数据／复制日志），无 python／mmx／mavis 字样。
  - `seeded-view-profile-1440.png`：主标题「档案视图」，**2 颗**按钮（复制数据／复制日志），**无 ` ▾`**（与上面 mtime 推断一致），无 python 字样。

### 5.2 2026-09-13 我重拍的两张（带 ` ▾` 的现在长相）

- 渲染对象：我复制到 `samples\` 的 `wizard-profile.html`（65,439 字节）与 `view-profile.html`（63,838 字节）——**内容与源目录原件逐字节相同**（`Copy-Item`）。
- 工具：`vision_html_screenshot`，1440 宽、`fullPage: true`，成功返回。
  产物 PNG 落在 `.dsh-vision-router\artifacts\`，我复制到本票目录：
  - `D:\ilife\.scratch\t152-user-accept\shots\product-wizard-1440.png`（83,250 字节，pageHeight 1,462）
  - `D:\ilife\.scratch\t152-user-accept\shots\product-view-profile-1440.png`（85,673 字节，pageHeight 1,355）
- 视觉核对（`vision_describe`）结果，与验收页第 3 块写的一致：
  - 预检页：主标题「档案预检」；按钮 **3 颗** —— 「复制指令」「**复制数据 ▾**」「复制日志」；无 python／mmx／mavis 字样；输入项提示里写明了活动量五档字面「久坐/轻度/中度/活跃/高度活跃」。
  - 结果页：主标题「档案视图」；按钮 **2 颗** —— 「**复制数据 ▾**」「复制日志」；无 python／mmx／mavis 字样。

---

## 6. 验收页自身的视觉自检（硬性要求 3）

- 工具：`vision_html_screenshot`，**1440 宽**、`fullPage: true`。
- 结果：**成功**。
  - 返回：`width 1440`、`pageHeight 7109`、`bytes 842565`。
  - 我复制到：`D:\ilife\.scratch\t152-user-accept\shots\acceptance-page-1440.png`（842,565 字节，实测）。
- 我自己看过（`read_image` 看整页缩略 ＋ `vision_crop` 取 `0,0,1440,1300` 全分辨率细节）：
  - **无横向溢出**：正文容器 `max-width:1100px` 居中，1440 宽下左右留白对称；整页渲染宽度就是 1440，页面无横向滚动条。
  - **可读**：标题层级、卡片边框、`<pre>` 里的四段 prompt、表格、路径块都清晰；`<code>` 里的尖括号按字面显示（源码里写的是 `&lt;`／`&gt;`）。
  - 修过的问题：`<style>` 里有一处写坏的颜色声明（`background:#1c2husky;`），截图前已删除——`#1c2531` 接在后面本来就生效，属于清理，不影响版面。

---

## 7. 未确证项 / 已知偏差（诚实清单）

1. **真库目录里没有场景 07 四条词的产物**（除那个 2026-09-11 的早期片段）。
   也就是说：按新口径（完整文档 ＋ 两颗复制按钮）跑出来的场景 07 产物，目前只在 `.scratch\` 下有实物，**还没有落进真库目录**。
   本票不跑命令、不重生成产物（并发期约定），因此**只能如实列出这个现状**，并把实物指到 `.scratch\`。
2. **`.scratch\t239\shots\` 的 12 张图早于三格式菜单**，图上看不到 ` ▾`。验收页已显式标注，没有拿旧图冒充新长相。
3. **`.scratch\t152-probe\` 是另一路探针的目录，会被反复改写**（同一天两次列它文件名已变）。验收页与对账表都以我复制到 `samples\` 的副本为准。
4. 三条写入词预检确认页的**逐项版面细节**（「改前值／待写四项／活动量五档」的具体位置、五档系数与 TDEE 影响的具体数值），
   我核到的是**代码层与议题正文层**（`SKILL.md:64`、#175／#176 正文、`src/profile/setup.ts`）；页面上逐项摆放以用户自己打开时所见为准，本页不逐字断言。
5. **未跑** `pnpm build`／测试／门禁（并发期约定 ＋ 本票不含源码改动），因此没有任何 `GATE-RUN` 声明，也没有需要 `check-gate-audit` 对账的运行。
6. 写后回执页的**改前 → 改后对照表**我核对到代码层（`src/cli/write.ts:219-232`、`src/profile/update.ts:28`），
   没有对回执页实物做逐行视觉核对（只确认它是完整文档、含两颗复制按钮）。
7. `t152-用户验收页.html` 里出现的**每一个** `.scratch\t239\shots\*.png` 字节数，都取自同一次 `Get-ChildItem` 输出，未逐一 `Get-Item` 复核。
