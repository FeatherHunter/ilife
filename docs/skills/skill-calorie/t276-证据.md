# t276 命令面证据（卡路里场景 02 饮食：6 条补到有命令可跑）

> 票 #276（地图 #155 子票）。架构依据以 `docs/agents/命令登记纪律.md` 为准，票面“六处登记”是旧世界已过期。
> 用词照 `docs/agents/wording.md`：`calorie.*` 一律叫“命令”，写操作叫“会改数据库的命令”。

## 一、影响清单（事前，动手前报；每行一个能力目录＋理由）

| # | 路径 | 理由 |
|---|---|---|
| 1 | `packages/skill-calorie/src/diet/commands.ts` | 新命令只声明在自己能力目录（恰好导出一个声明数组，六字段 `kind/key/shape/title/wakeWord/example`）。对地图目标的帮助：6 条里“批量导入食品”今天没有命令可执行，加这一条声明才有命令可跑。 |
| 2 | `packages/skill-calorie/src/diet/routes.ts` | 路由声明住能力目录，`order` 保序（6 条唤醒词从 `legacy/routes/scene-02.ts` 的 non-exec 搬成 exec 记录）。对地图目标的帮助：唤醒词命中后能落到真实命令上，70 条里这 6 条才跑得通。 |
| 3 | `packages/skill-calorie/src/diet/today.ts` | 查询命令加备注筛选参数（取数已含 note，缺筛参；`items` 补 note 列）。对地图目标的帮助：“看有备注的饮食记录”×2 不用新增命令，加参数即可跑通。 |
| 4 | `packages/skill-calorie/src/diet/productImport.ts`（新建） | 新增一条会改数据库的命令的处理函数（读写分开；复用 `fetch/batch.ts` 的校验与去重口径＋`diet/productStore.ts` 写库）。对地图目标的帮助：“批量导入食品”的写入有命令承接，查询命令不再带写副作用。 |
| 5 | `packages/skill-calorie/src/cli/legacy/routes/scene-02.ts`（仅删行） | 老住处的 6 条 non-exec 记录随搬迁定点删除（一个唤醒词恰住一处）。对地图目标的帮助：不删就是同一条记录两个定义地，生成期即抛，70 条对账合不上。 |
| 6 | `pnpm gen` 派生物（`src/cli/keys.ts`、`src/cli/registry.ts`、`packages/base-combos/combos.yaml` 同值镜像、`scripts/build-help.mjs` 的 REPR＋EXAMPLE 标记块、`src/triggers/routes.generated.ts`、`SKILL.md` 命令表、HELP 产物） | 共用位一律由生成器写，人不手改。对地图目标的帮助：HELP 可查与命令表一致，AI 才知道从哪条工作流程走下去。 |
| 7 | `docs/skills/skill-calorie/t276-*`、`.scratch/t276/`、`.changeset/t276-*.md` | 证据、映射说明、handover、changeset。对地图目标的帮助：每条决策留一句帮助，复核可追踪。 |

不碰（排他）：`packages/skill-calorie/src/diet/receipt.ts`（不存在）、`packages/skill-calorie/src/cli/write.ts`、`src/cli/cmd_read.ts`（即 `packages/skill-calorie/src/cli/cmd_read.ts`）、`src/shared/` 下回执分派件、`src/cli/keys.ts`、`src/triggers/routing.ts`（旧登记点，已被生成器取代）。分派层 `switch` 只许变短，本次不加分支：新命令走注册表先行（`dispatch`／`dispatchWrite` 先查 `REGISTRY`），无需碰分派文件，结论落在 `.scratch/t276/write-dispatch-handover.md`。

## 二、6 条逐条方案（第一性原理：每个决策的唯一判据是是否有助于 70 条跑通＋真实 HTML＋HELP 可查）

| 唤醒词 | 处置 | 命令侧做法 | 对地图目标的帮助 |
|---|---|---|---|
| 看有备注的饮食记录 | 优化 | 给 `calorie.today` 加 `hasNote`（兼收 `withNote`）参数；`items` 补 `note` 列 | 不新增命令，加参数即可让这条词有命令可跑 |
| 看「有备注」的饮食记录 | 优化 | 同上一条能力（旧版遗留条目），同路由到 `calorie.today`＋`hasNote:true` | 两条同能指的词合成一条命令，避免为遗留条目另造命令 |
| 校验批量导入 | 纠正 | 路由接回现成命令 `calorie.view.batch-import-preview`（只读，不写库） | 已有命令能办的事指过去即可，不新增命令 |
| 批量导入食品 | 优化 | 新增一条会改数据库的命令 `calorie.product.import`（`items` 同步写库；`file` 走 `importProducts` 异步文件路，命令侧只收 `items` 保持同步） | 读写分开后写入有命令承接，查询命令不再带写副作用 |
| 拍营养表记一餐 | 优化 | 写库并到 `calorie.diet.add`；本票只做命令侧（路由＋映射说明），确认页归预检确认页票 | 识别在模型侧，命令侧复用现成写命令即可跑通 |
| 拍营养表补记一餐 | 优化 | 同上，带 `date`／`time`（`补记饮食` 已用同一形状） | 补记形状已有先例，照抄即能跑 |

4 处“接对”（窗口／接错／餐别参数）在新世界的位置：`calorie.view.diet` 归 `src/home/`、`calorie.view.home` 归 `src/home/`、餐别分布 5 条的 `meal` 参数归页面票窗口，本票只动饮食能力自己的 6 条路由 `cli` 文本；旧 `routing.ts:245` 种子日期窗口与 #250 口径差异若遇到，先判一次并记入遗留出口，不擅自改 #250 的东西。

## 三、运行记录（GATE-RUN 对账源，边跑边补）

| 运行 | 命令 | 结果 | 运行标识 |
|---|---|---|---|
| 第一阶段构建（5 条路由＋备注参＋新命令声明，不含 order:40） | `pnpm build` | exit 0 | GATE-RUN runId=fe2c66e1-ae87-4e43-9d90-3f0912050cd7 |
| 第一阶段生成（新命令进共用位：键 114＝写 46＋读 68） | `pnpm gen` | exit 0 | GATE-RUN runId=12cce146-9637-4add-8ca8-c011f903b53d |
| 第二阶段构建（含 order:40） | `pnpm build` | exit 2（阻塞：全部 TS 错误在他人未提交的 weight 五件，我方 diet 零报错，见 `.scratch/t276/build2.log`） | GATE-RUN runId=da8c1155-2567-4989-b333-2b74d5a7cb57 |

6 条实跑脚本已备好（`.scratch/t276/run-six.mjs`，隔离库，真库零触碰），待树绿后持锁执行。
靶向测试、探针 `t293`（P1 行 `UNACCOUNTED` 为空）、`test/calorie-routing-81.test.mjs`、变异自证（改坏必红、还原变绿两行），同样待树绿后持锁执行。

## 四、提交清单 · 一阶段（只加自己声明路径）

| 提交 | 内容 |
|---|---|
| `e6c050e` | 一阶段：6 条路由 ＋ `calorie.product.import` 声明与处理函数 ＋ 证据 |
| `4a3a1ac` | 静态对抗预审报告（PASS 87，无 S1） |
| `b6f4801` | `order: 40` 暂退（当时 `calorie.product.import` 未进 `src/cli/keys.ts`，`tsc` 报 TS2820；提交信息写明「待 gen 后复进」） |

---

# 二阶段收尾（新席，2026-09-14）

> 门禁运行记录（对账源）见同目录 [`t276-gate-runs.md`](t276-gate-runs.md)；一切编译／测试／`git add`／`git commit` 均经 `node tooling/run-locked.mjs --ticket 276 -- <命令>`。

## 五 · 5.1 复进 `order: 40`

- `packages/skill-calorie/src/diet/routes.ts`：在 `order: 39` 与 `order: 41` 之间加回那一行 exec 路由（照 `b6f4801` 的反向 diff 原文复进：唤醒词「批量导入食品」、`kind: 'exec'`、`key: 'calorie.product.import'`、cli 带 `--params` 示例）。
- `packages/skill-calorie/src/cli/legacy/routes/scene-02.ts`：删掉 `b6f4801` 加的那行 non-exec 占位（本件复归空数组；一个唤醒词恰住一处）。
- 派生件重生成后，`src/triggers/routes.generated.ts` 第 52 行由 non-exec 变为 exec（见 5.3 第 3 处）。

**复进后真库实跑**（窗口 `fa86d6f7-f316-418d-9e4c-fe019aa4b482`，`.scratch/t276/real-db-import.mjs`，逐字读数）：

```
REALDB path=D:\2Study\StudyNotes\.db\calorie_data.db bytesBefore=3072000
BEFORE rows=0 existing=[]
CLI exit=0 summary="批量导入食品：新增 1，更新 0，跳过 0，下架 0，失败 0" recordId=1940 affectedRows=1
AFTER rows=1 bytesAfter=3072000 readback=[{"id":1940,"product_name":"测试导入燕麦","brand":null,"calories":389,"protein":13,"fat":7,"carbohydrates":66,"sodium":5,"is_deprecated":0}]
RESULT: PASS
```

- 落盘绝对路径：`D:\2Study\StudyNotes\.db\calorie_data.db`；字节数 **3072000**（写前写后同值：SQLite 复用空闲页，行数 0 → 1 才是写进库的读数）。
- 用的是声明里的现值（`测试导入燕麦` 389／13／7／66／5），未造新数据。
- 该命令的写后回执是**片段**不是整页——按编排者澄清属「写后回执页」那一类的账（归票 #270），**不是本票缺陷**；本票判据按命令面量：exit 0 ＋ 真写进库 ＋ 库内回读。（`src/diet/receipt.ts` 不在本票声明路径，未动。）

## 5.2 两条欠账补齐

| 欠账 | 文件 | 做法（照同处既有写法，不另起一套） | 用例数 |
|---|---|---|---|
| 覆盖门「写后 SELECT 回读」 | `packages/skill-calorie/test/cmd-write-40-persist.test.mjs` | 新增用例「落库 · 食品 product.import（批量导入）：逐列回读 ＋ 重复项跳过不落第二行」——`runWrite` 跑 CLI 后 `withRead` 开只读句柄 `q1`／`qn` 回读 `nutrition_products` 逐列，再复跑同一条断言按 `product_name ＋ brand` 去重不落第二行 | 19 → **20** |
| #97 场景表 | `packages/skill-calorie/test/m5-receipt-97.test.mjs` | 场景表 ＝ 探针表（45 行）＋ 本件新增 1 行 `calorie.product.import`（形状照表内既有行 `{ key, params }`）→ 46 行，与 `CALORIE_WRITE_COMBOS` 46 条一一对上 | 54 → **55** |

**一处需编排者裁决**：探针表住历史归档件 `docs/research/t97-probe-receipts.mjs`，**不在本票声明路径内**，故那一行住本件（`SCENARIOS = [...PROBE_SCENARIOS, 新行]`）。若要求该行并回探针表，是一行位移，属编排者收口动作。

## 5.3 六处登记逐处核对（当刻读数）

| # | 登记处 | 当刻读数 | 生成物门 |
|---|---|---|---|
| 1 | `src/cli/keys.ts`（生成物） | `'calorie.product.import': { shape: 'receipt', title: '批量导入食品' }` 在位 | `GEN-CHECK ok … sha256=27f41c135ef4a9e6…` |
| 2 | `packages/base-combos/combos.yaml`（生成物） | 第 137 行 `- key: calorie.product.import` ＋ 第 140 行 `title: 批量导入食品` | `GEN-CHECK ok … sha256=eba38eb5664064ef…` |
| 3 | 路由记录面 `src/triggers/routes.generated.ts`（生成物；原派单写的 `routing.ts` 自 #313 起不承接记录数据、只剩类型与 `routesFor` 这类逻辑） | 第 52 行 `{ wakeWord: '批量导入食品', scene: '02', kind: 'exec', key: 'calorie.product.import', cli: … }` | `GEN-CHECK ok … sha256=67b776d289fb8dd9…` |
| 4 | 写读分派 `src/cli/write.ts`／`src/cli/cmd_read.ts` | 两份文件里 `product.import` 命中 **0**（注册表先行，不加分支；与 `.scratch/t276/write-dispatch-handover.md` 一致） | 分派层不出现命令字面量即达标（棘轮 `cmd-registry-294`） |
| 5 | `scripts/build-help.mjs`（生成物标记块） | `REPR` 第 48 行 `'calorie.product.import': '批量导入食品'`；`EXAMPLE` 第 163 行示例串在位 | `GEN-CHECK ok … sha256=9f03a25ba2c4fdb8…` |
| 6 | 重跑生成命令表 | `pnpm help:build` exit 0；`SKILL.md` 速查表第 83 行 `| 批量导入食品 | calorie.product.import | receipt | … |` | 见下 |

- `pnpm gen:check` 两次读数：窗口 `e4adcfa6…` 那次 **exit=1**，第 162 行起 `看今日运动` 的记录与生成器输出不一致（他线在途声明造成的陈旧，**已知环境性，见票 #392，不归本票**）；窗口 `515264d6…` 里先 `pnpm gen` 再 `gen:check` → **exit 0**，末尾 `GEN-CHECK PASS：键 118（写 46 ＋ 读 72）；能力 10 个 … 声明 118 条，未搬迁清单 0 条`。
- `pnpm help:examples:check` 读数（窗口 `c16743c7…`／`9eefc93e…` 两次同值）：**exit 0**，末行 `RESULT: 118/118` ＋ `PASS: SKILL.md 示例逐行可执行` —— 即 SKILL.md 速查表与命令册 118 条一致（含本票第 83 行那一行）。
- **越界未改、报编排者**：`SKILL.md` 第 39 行是 AUTO 块（第 80—204 行）**之外**的手写散文，仍写「读 64 键 + 写 35 键共 99 键」，与当刻命令册 118 条（写 46 ＋ 读 72）不一致；该行不在生成块内，`pnpm help:build` 不会覆盖它。
- 生成物六件（上表 1／2／3／5 与 `src/cli/registry.ts`、`SKILL.md`）**未由本票 `git add`**——按派单归编排者统一收口。

## 5.4 票面「关键事实」的 9 个测试文件计数断言（逐条：改前 → 改后）

来源＝`467cf64`（#295）把 9 处钉死计数改成与权威声明对账。逐个复核当刻形状：

| # | 文件 | 当刻断言（对账源） | 改前数字 | 改后数字 |
|---|---|---|---|---|
| 1 | `packages/skill-calorie/test/cmd-read-t11.test.mjs` | `Object.keys(CALORIE_COMBOS).sort() deepEqual DECLARED_KEYS` | 101 | 118（派生，不手写） |
| 2 | `packages/skill-calorie/test/cmd-write-40.test.mjs` | `[…WRITE_KEYS].sort() deepEqual DECLARED_WRITE_KEYS` ＋ 组合键表 deepEqual `DECLARED_KEYS` | 35／101 | 46／118 |
| 3 | `packages/skill-calorie/test/db-readonly-93.test.mjs` | 读命令 deepEqual `DECLARED_READ_KEYS`、写命令 deepEqual `DECLARED_WRITE_KEYS` | 66／35 | 72／46 |
| 4 | `packages/skill-calorie/test/output-naming-119.test.mjs` | `receiptCount == DECLARED_WRITE_KEYS.length` | 35 | 46 |
| 5 | `packages/skill-calorie/test/output-naming-87.test.mjs` | 组合键表 deepEqual `DECLARED_KEYS` | 101 | 118 |
| 6 | `packages/skill-calorie/test/render-t41.test.mjs` | 组合键表 deepEqual `DECLARED_KEYS` | 101 | 118 |
| 7 | `packages/skill-calorie/test/skill-t11.test.mjs` | 组合键表 deepEqual `DECLARED_KEYS` | 101 | 118 |
| 8 | `test/combos-42.test.mjs` | 组合键表 deepEqual `DECLARED_KEYS` | 101 | 118 |
| 9 | `test/calorie-routing-81.test.mjs` | `coveredKeys: DECLARED_KEYS.length`、`EXEC_ROUTE_BY_KEY` 键数 == `DECLARED_KEYS.length` | 101 | 118 |

结论：这 9 处**已全部改派生**（手写数字在 #295 退役），本票**不手改任何数字**，增长自动跟随。**但**第 9 个文件里另有两处**手写冻结值**——D2① 桶计数 `{ exec: 390, 'non-exec': 46 }` 与 FX-81-5 不变量的 `459`／新拟 `68`；当刻实测 `{ exec: 411, 'non-exec': 25 }`、`480`。它们属 #341／#357 正在重落的基线，**不在本票声明路径**，未动（见 5.6 归因）。

## 5.5 变异自证（机器读数两行，窗口 `515264d6-fd87-468f-a64b-7bdf4cb0de18`）

| 轮 | 变异 | 改坏读数 | 还原读数 |
|---|---|---|---|
| A 源码级 | `src/diet/productImport.ts` 的 calories 取值键改错（写不进库） | `persistExit=1 persistFail=2 m5Exit=1 m5Fail=2` | `sameAsBefore=1` → `persistExit=0 persistFail=0 m5Exit=0 m5Fail=0` |
| B 断言级 | 本票新加的回读断言 calories 期望 389→999 | `redExit=1 redFail=2 redPass=18` | `sameAsBefore=1` → `greenExit=0 fail=0` |
| C 表行级 | #97 场景表后加的那一行删掉 | `redExit=1 redFail=1 redPass=53` | `sameAsBefore=1` → `greenExit=0 fail=0` |

- **红／还原一致两行**：`改坏必红` ＝ A persist 红 2 ／ m5 红 2 ＋ B 红 2 ＋ C 红 1；`还原必绿` ＝ A `persistExit=0 m5Exit=0` ＋ B `greenExit=0` ＋ C `greenExit=0`，且三处 `sameAsBefore=1`（sha256 逐字节一致）。
- A 轮另有一条独立读数：`git status --porcelain -- packages/skill-calorie/src/diet/productImport.ts` 输出为空 ⇒ 源码级变异与 HEAD 逐字节还原。
- 说明：还原用「按改前内容逐文件写回 ＋ sha256 比对」，因为 B／C 两个测试文件当刻带着**本票未提交的改动**，用 `git checkout -- <文件>` 会连本票的正当改动一起去掉；A 那个源码文件另有上面那条 git 级读数佐证。
- 第一轮电池（窗口 `e4adcfa6…`）**作废**：脚本自身的 sha 比较写错（把改坏后的值当改前值），三条「还原一致」误报为 0；脚本修好后重跑得本表。

## 5.6 靶向与全量读数

| 时点 | 窗口 | routing-81 | help-center-106 | cmd-write-40-persist | m5-receipt-97 |
|---|---|---|---|---|---|
| 动前 | `1a0ce15e…` | 8 用例／pass 2／fail 6 | 10／6／4 | 19／18／**1** | 54／53／**1** |
| 动后 | `3641e1ae…` | 8／4／**4**（两条转绿） | 10／6／4（红用例名不变） | **20／20／0** | **55／55／0** |

- routing-81 转绿的两条：`D2③ 全量键（条数 == 权威声明）全部有可执行入口`、`FX-81-5 覆盖修复：wizard 降级词失去的唯一入口由 1 条单命令入口承接`——都是「某键没有任何可执行路由」的账，`calorie.product.import` 复进后自动消失。
- 全量（窗口 `3541c59e…`，`.scratch/t276/full-test.log`）：`tests 1616 ／ pass 1588 ／ fail 28`（`pnpm test` 自身 exit=1；读数脚本按设计只报读数、不传播退出码，故该窗口记账为 0，见导出的运行记录表注）。28 条红逐条落到文件：`help-center-106` 4（#341／#357）、`trend-homogeneity-110` 4（#110）、`render-copy-90` 4（#239）、`calorie-routing-81` 4（#341／#357 基线）、`copy-component-179` 2（#239）、`scene05-write-mutate` 1（#349）、`weight-history-333` 1、`weight-receipt-337` 1、`calorie-triggers` 1（prompt 全文 sha 对账）、`help-center-91` 1、`exercise-port-111` 1、`cmd-read-t11` 1（T10 照片 parity）、`t358-missing-ask` 1、`sport-homogeneity-109` 1、`profile-doc-179` 1（#269 席）。**本票声明路径下 0 红。**

## 5.7 未做项（越界即不碰，报编排者）

1. `test/calorie-routing-81.test.mjs` 的手写冻结值（桶计数 390／46、不变量 459、新拟 68）——不在本票声明路径，属 #341／#357 的重落基线。
2. `packages/skill-calorie/test/help-center-106.test.mjs` 的 `367/69` 与唯一 CLI `307`——不在本票声明路径，当刻实测已到 411 条 exec（他线在途）。
3. `SKILL.md` 第 39 行 AUTO 块外的手写散文（「99 键」）——编排者明令不改 `SKILL.md`，只报告。
4. `docs/research/t97-probe-receipts.mjs` 未加行（不在声明路径，见 5.2 备注）。
5. 生成物六件未 `git add`（编排者统一收口）。
6. `src/diet/receipt.ts` 未动（第 14 条整页回执归票 #270）。

## 5.8 提交清单 · 二阶段

| 提交 | 内容 | 入账件（`git show --name-only <sha>` 复核） |
|---|---|---|
| `bf274e6` | 二阶段：`order: 40` 复进、覆盖门与 #97 两条欠账补齐、证据件与门禁运行记录、changeset | `.changeset/t276-diet-commands.md`、`docs/skills/skill-calorie/t276-gate-runs.md`、`docs/skills/skill-calorie/t276-证据.md`、`packages/skill-calorie/src/cli/legacy/routes/scene-02.ts`、`packages/skill-calorie/src/diet/routes.ts`、`packages/skill-calorie/test/cmd-write-40-persist.test.mjs`、`packages/skill-calorie/test/m5-receipt-97.test.mjs` —— **恰 7 件，与声明路径逐条对上** |

- 提交窗口：`c16743c7-b9f0-4b77-a20c-0b315f4c74cc`（首轮：守卫按「看共用索引」判，被 `#342` 席暂存的件触发假阳性，**未提交、未动他人索引**）、`9eefc93e-2bec-44a9-97c0-f85fd2770fe9`（改为「看这一笔实际提交了什么」后提交并推送成功，`b646d6f..bf274e6 master -> master`）。
- **环境性说明（如实记）**：共享工作区有 8 家以上在并发、**共用同一个 git 索引**；本票提交时索引里另躺着 `#342`／`#359` 等席暂存的件。本票一律用 **pathspec 形式** `git commit -F <信息文件> -- <自己声明的 7 条路径>`（该形式只录被点名路径的工作区内容、不读暂存区），他人暂存的件**未动、未进本笔**；入账范围以上表 `git show --name-only` 的复核为准，属环境性，不构成过程违规。
