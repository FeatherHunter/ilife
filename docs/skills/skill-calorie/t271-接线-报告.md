# t271 接线 · 报告（看饮食总览走总览页 ＋ `calorie.view.diet` 两态分清）

- 票：**地图 #155 · #271**。工作目录 `D:\ilife-wt\wf155-j`；**基线＝`35e71b9cbfb1b5a279e43e47db8fef1715616a6c`**。

## 一、判定

| 判据 | 判定 | 依据 |
|---|---|---|
| 1 看饮食总览 exit 0 ＋ 落总览页 ＋ 与「看最近 7 天饮食」不同 | **真** | 锚点 `sec-week／sec-month`＋「统计到昨日」；sha `052b00b9…`≠`0fc5e6a5…`（§二甲） |
| 2 不给 `entry` 的 8 窗口词＋5 餐别词逐字节未变 | **真** | 两轮同库同参数，归一 sha 同 **14／14**；5 条餐别词只差渲染时刻戳（§二乙） |
| 3 空库 exit 4＋阻断原文＋0 产物；空窗 exit 0＋完整页＋空态句＋引导句 | **真** | 两条都能红：基线这一项四项全假；变异体乙即打它（§二丙、戊） |
| 4 `hasAnyDietRow` 定义只剩一处 | **真** | 全仓 1 处 `src/diet/nutritionPort.ts:53`（基线 3 处） |
| 5 改坏必红、还原必绿 | **真** | 三变异体命中 3／3／1 条红，还原后 0 红（§二戊） |
| 6 tsc exit 0；`gen:check` exit 0；`pnpm gen` 派生件跟着变 | **真** | 见 §二己 |

**本票引入的失败＝0**（§二庚）。开发中红过两次，当场归因、没动判据：① 空窗页引导句原写「**说**「记一餐」」⇒ 照 #275 参考件形状改「**先用**「…」」；② 判据 2 首轮 5 条餐别词 sha 不同 ⇒ 追到 `render/receipt.ts:155` 的 `nowStamp()`（日志第 5 段＝渲染时刻）⇒ 改成先归一该时刻戳再比。

## 二、机器证据

**甲 · 看饮食总览真跑**（隔离库：今日／昨日／本周／上周／上月／7 天／30 天＋四餐，含一条带备注）

| 一轮 | cli 参数 | exit | 锚点 | 字节 | 归一 sha |
|---|---|---|---|---|---|
| 基线（干净检出） | `{"window":"7d"}` | 0 | `sec-trend／sec-meals` | 89139 | `0fc5e6a5…bc29b` |
| 本票 | `{"window":"7d","entry":"overview"}` | 0 | **`sec-week／sec-month`** | 79614 | **`052b00b9…69ebb`** |

「看最近 7 天饮食」本票一轮＝`0fc5e6a5…bc29b` ⇒ 两词不再同页。总览页另有「统计到昨日」、本周／本月累计、复制区恰好 1 节且复制日志非死按钮。

**乙 · 判据 2**：`frozenTotal=14 / same=14 / diff=[]`；`tsOnly`＝5 条餐别词（差在 `21:05:42` vs `21:08:59`，字节数与其余逐字全同）。基线轮在另一份干净检出（`D:\ilife-wt\t271-base`，同 sha）重编重跑，不共用 `dist`。

**丙 · 两态**（每条自成一张新库）

| 态 | 参数 | exit | 产物 | 完整页 | 空态句／引导句 | 复制区 | 死按钮 |
|---|---|---|---|---|---|---|---|
| 空库 | `{"window":"7d"}` | **4**＋`ERR 4: 取数失败（缺失阻断）` | **0** | — | — | — | — |
| 空库 | 带 `entry=overview` | **4**＋同上 | **0** | — | — | — | — |
| 空窗（只有 400 天前的行） | `{"window":"7d"}` | **0** | 1 | 是 | 是／是 | 1 | 否 |
| 空窗 | 带 `entry=overview` | **0** | 1 | 是 | 是／是 | 1 | 否 |

**丁**：判据 4 的基线读数＝3 处（＋`rankingPlate.ts:43`＋`review.ts:35`）。

**戊 · 变异自证**（`.scratch/t271j/mut-两行机器读数.txt`；每步 `tsc -b --force`）

```
甲·路由丢入口标记 改坏 → tsc=0 node --test exit=1 红名单(3)：①带标记 ／ ②落总览页 ／ ②与看最近7天不同
甲·路由丢入口标记 还原 → tsc=0 node --test exit=0 红名单(0)：（空）
乙·两态判据反过来 改坏 → tsc=0 node --test exit=1 红名单(3)：③空库仍exit4 ／ ③空窗出完整页 ／ ③两态不是一条路
乙·两态判据反过来 还原 → tsc=0 node --test exit=0 红名单(0)：（空）
丙·review.ts 又长私有副本 改坏 → tsc=0 node --test exit=1 红名单(1)：④定义只剩一处
丙·review.ts 又长私有副本 还原 → tsc=0 node --test exit=0 红名单(0)：（空）
```

**己**：`tsc -b packages/skill-calorie` exit 0；`gen:check` exit 0（`GEN-CHECK PASS`）。`pnpm gen` 后 `keys.ts`／`registry.ts`／`combos.yaml`／`build-help.mjs` 四件哈希一字未变；唯一变的是 `src/triggers/routes.generated.ts` 第 57 行那条记录，只多 `,"entry":"overview"`。

**庚**：`packages/skill-calorie/test/*.test.mjs` 跑两棵树——干净 HEAD `1216／1136绿／78红`；本票 `1225／1148绿／75红`（＋9＝新探针）。**本票独有红＝0**。基线红／本票绿 3 条：两条告警线台账门（§四3）、一条 `db-readonly-93` 的 profile（单跑在两棵树都 8/8 绿＝并行跑旧抖动）。`t275-营养饮水总览页.test.mjs` **45/45 绿**。

**可复跑**：`node docs/skills/skill-calorie/t271-接线-run.mjs --label baseline|after`（读数落 `.scratch/t271j/<label>/readings.json`，逐条判据布尔在 `judgments`）；探针 `node --test packages/skill-calorie/test/t271-总览接线与两态.test.mjs`（9/9）。

## 三、逐条路径

- **入口标记**：`src/home/routes.ts:27` 那条记录 cli 加 `,"entry":"overview"`（照 #509／#511 先例）；同表其余 13 条记录一字未动。
- **判页**：`src/home/today.ts` 的 `viewDietOverview` 读 `entry`，命中传 `overviewView: buildDietOverviewView(db, date)`＋`command` ⇒ `render/dietDocs.ts:134` ⇒ `diet/todayDocs.ts` 的 `buildDietOverviewPage` ⇒ #275 交的 `buildDietOverviewBlock`（不另写装配）。
- **两态**：取数改 `try/catch`——`!hasAnyDietRow(db)` 原样抛（库为空逐字保持 exit 4、不落盘）；否则走 #275 的 `buildEmptyWindowDoc`，形状照 #272 `rankingPlate.ts` 与 #275 `nutrition.ts` 两份参考件。
- **概念收敛**：两份 `hasAnyDietRow` 私有副本改吃共用件——`diet/review.ts`（#273）＋`diet/rankingPlate.ts`（#272），两件其余内容一行未动。票面只点名 review.ts；rankingPlate.ts 是为判据 4 补的同一笔，属授权内必要件。
- **标记值**：`ENTRY_OVERVIEW` 与 #511 两标记同住 `diet/nutritionPortDocs.ts`；`diet/index.ts` 只加一行注释（这四件走深路径），门的导出面未增。台账 `nutritionPortDocs.ts` 728→734，按包规 `--sync` 只改两格数字。

## 四、未做项与下一手缺什么

1. **视觉未验**：只出结构化断言与绝对路径，版式要人眼过（`t166`：视觉不做脚本化判定）。
2. **#279 的锁不在本检出**：按「看饮食总览」搜测试面只有 #275 探针与 `diet-homogeneity-108`；按新状态重钉留给 #279（#275 报告未做项第 6 条）。
3. **范围外：基线告警线台账是红的**。干净 HEAD 上 `RESULT: 82/84`，两行陈化（`nutritionPortDocs 645 vs 728`、`nutritionPort 371 vs 383`，皆 #275 遗留）。本票改了其中一件 ⇒ `--sync`（改=2 增=0 删=0），顺带把另一格改对（一行数字）；`t445` 的 T5／T9 因此转绿。
4. **范围外：`diet-homogeneity-108` 有一条基线就红**——「view.diet 副题丢了窗口」，干净 HEAD 单跑同样红，形状是 #551 把窗口挪到页顶条后旧探针没跟。
5. **两笔留给编排者裁**：① 本键挂 15 条唤醒词，空窗页眉标只能写页名（带 `entry` 时写「看饮食总览 · 饮食」）；要像 #511 那样一词一页头，需给 8 条窗口词各带 `entry`。② `entry=overview` 遇空窗也落空窗页（照裁定 4 的窗口口径）；若要「总览页照出、读数置零」，短路到 `buildDietOverviewPage` 之前即可，但那与「窗口为空出空态句」走两条路。

**边界自查**：`src/cli/`／`src/triggers/`（除 gen 重生成那件）／`base-combos/`／`scripts/` 一行未改；`exit 4` 一处未放宽；判据一条未改。改动 8 件（6 源＋1 生成物＋1 台账）＋新增 4 件（探针／可复跑脚本／本报告／changeset）。
