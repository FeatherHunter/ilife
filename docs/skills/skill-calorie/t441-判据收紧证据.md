# #441 判据收紧证据（#443 实施，#361 原证只追加指针——本票写集不含原证件，反向指针见 §6）

## 1 判定与票面对照

- 本票目标（#443 票面五段）：收紧 #361 复核点名的两条 S3，不改行为——
  ① 全空行真出口断言（UPDATE 可造全空行，INSERT 触发器拦不住；并存时真出口 exit 0，
  仅全空行 exit 4）；② 载荷逐字段口径（全文 `JSON.stringify.includes('—')` 被备注「—」假红）。
- 结论：两条口径均已收紧，测试 `tests 8 / pass 8 / fail 0 / exit 0`，变异自证 4/4，真库零写入。
- 结构纪律：本票只动测试文件与文档；`docs/agents/structure.md` 管辖明示不管测试文件与文档，
  必报五步不触发。`src/**` 零改动（`git status` 无 src 项，见 §5）。

## 2 机器证据（门禁对账源：`.scratch/locks/gate-runs.log`，GATE-RUN 边跑边声明）

GATE-RUN runId=c63f463b-bdec-41a5-928b-8979cb48dd30 cmd="node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs"
GATE-RUN runId=8702267c-6bad-44c2-8bed-110de7d2ca47 cmd="node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs"
GATE-RUN runId=4c4d79d3-ae0f-42c6-82cf-55c38c2ea cmd="node .scratch/t443/mut-battery.mjs"

| 运行 | runId | exit | 读数 |
|---|---|---|---|
| 改前基线（本席重跑） | `c63f463b` | 0 | `tests 8 / pass 8 / fail 0` |
| 收紧后靶向门 | `8702267c` | 0 | `tests 8 / pass 8 / fail 0`，`T443-READOUT empty-row=13/13 via=shim`，`T361-READOUT names=13/13` |
| 变异电池（单次持锁，改坏→红→还原→绿） | `4c4d79d3` | 0 | `MUT-443 RESULT=4/4`：M3 全文复位 `exit=1 fail=1`／还原 `exit=0 pass=8`；M4 删新用例 `pass=7`／还原一致＋`exit=0 pass=8` |
| 行为预检探针（自建 tmp 库） | `e81490d1` (`probe3.log`) | 0 | `P1-insert-blocked=true P2-update-allnull=true P3-exit=0 P3-unfilled=true P3-dashcells=13 P4-exit=4 P5-old-red=true P5-new-green=true P5-note-kept=true P6-exit=0` |
| 废跑记账（非门禁） | `f626c0f9`（探针 CHECK 取值越界，已修值重跑）／`d0b78e4e`（加锁脚本单文件计数误判，已修重跑） | 1 | 均未充作门禁证据 |

- 真库零写入对账：`SKILLS_DB_PATH=D:\2Study\StudyNotes\.db`，
  `calorie_data.db` 开工前＝完工后 `LEN=3072000 / SHA256=D40D58F50E74E294A11E410B4D9E2142FCBCD8993328A4EFB453F1A0D62A1083`
 （基线见 `.scratch/t443/true-db-before.txt`；一切运行均 `SKILLS_DB_PATH` 指自建 tmp 库）。
- PATH 真出口同码证：`D:\2Study\nodejs\node_modules\skill-calorie` 为 junction
  指回 `D:\ilife\packages\skill-calorie`，`dist/cli/cmd_read.js` 两边 SHA256 均为
  `833D103C…1557C6CE`（2026-09-15 当场比对）；新用例优先垫片、无垫片退回本仓 BIN，落点打进读数行。
- 提交：C1 `5d9a6ba`（测试收紧，见 §5）；C2 本证据＋changeset（本件落仓即 C2，sha 见回执）。

## 3 改动（只动测试与证据口径，行为零改）

`packages/skill-calorie/test/t361-measure-full-table.test.mjs`（326 行→约 380 行，用例数 8→8，一增一替）：

1. 种子 R1 备注 `'r1'`→`'r1—d'`（头注同步）：全文口径的活探针。
2. 载荷断言（旧 `:251-252` 全文 `dump.includes('—')`）→ 13 字段逐个断言
   （`MEASUREMENT_FIELDS` 循环：`null/undefined/number` 且 `notEqual '—'`）＋ `r1.note` 原样保留断言。
   旧 M2 变异（payload 写「—」）仍必红：逐字段循环对 `'—'` 值直接红。
3. 旧合成视图直调用例（`:279-288`）→ 真出口新用例「全空行真出口」：
   自建库（单填肩围 44＋全值行）→ `UPDATE` 置空造全空行 → 真出口读表 →
   `exit 0`＋该行 13 格 `deepEqual` 全「—」＋页含「未填围度」＋ `T443-READOUT` 行。
   头注 M3／M4 登记两组负向对照（全文复位必红／删新用例计数 7）。

## 4 逐条路径

- 改动件：`packages/skill-calorie/test/t361-measure-full-table.test.mjs`（C1 逐件点名 `git add`）。
- 证据件：本件 `docs/skills/skill-calorie/t441-判据收紧证据.md`（新建，C2）。
- 记账件：`.changeset/t443-判据收紧.md`（新建唯一，`'skill-calorie': patch`，C2）。
- 草稿（不进仓）：`.scratch/t443/`（`probe-443.mjs`、`mut-battery.mjs`、`gate.ps1`、
  `impl-443.md`、`true-db-before.txt` 及各轮日志；`commit-msg-*.txt`、`commit1.ps1` 为提交辅助）。
- 日志件：`.scratch/t443/{base-test,tight-test,mut,probe,probe2,probe3,commit1*,commit1c}.log`，
  长命令一律落盘＋标记＋只看尾 5 行（红看 FAIL 前后 10 行）。
- 锁留痕：`.scratch/locks/gate-runs.log`（本票 RUN 行：`c63f463b`／`875f6371`／`e81490d1`／
  `8702267c`／`4c4d79d3`／`9fe2071b`＋两次废跑行＋官方包装器 C1 提交行
  `RUN ticket=443 runId=98fc8e84-6b82-416d-b793-07ad2c2663e2 … waitedMs=40024 exit=0`）；早期测试轮用本票 `gate.ps1`（目录锁＋探活＋RUN 行），
  后续提交改走官方包装器 `tooling/run-locked.mjs`（见 §6）。

## 5 提交范围（`git diff --cached --name-only` 逐次复核，只含自己件，不 push）

- C1 `5d9a6ba`（全 `5d9a6baba755e57a5d4e350b5a6aee92e44712f6`）：`feat(443): t361 两条口径收紧（全空行真出口新用例＋载荷逐字段断言＋备注含—种子）`
  → 仅 `packages/skill-calorie/test/t361-measure-full-table.test.mjs`。
- C2（本件落仓）：`docs(443): t441 判据收紧证据＋changeset`
  → 仅本件＋`.changeset/t443-判据收紧.md`。
- 禁止项遵守：无 `add -A/.`、无 stash／reset-hard／clean／切分支／amend／rebase／force；
  提交信息经 UTF-8 文件（`.scratch/t443/commit-msg-*.txt`）提交；默认由编排者统一 push。

## 6 未做项与下一手缺什么

1. **#361 原证反向指针未追加**：#361 票面要求原证据件「只追加指针」，但本票写集不含
   `docs/skills/skill-calorie/t361-实施证据.md`／`t361-复核.md`（他人路径，跨界停手）。
   本件 §1 已声明取代关系；下一手（编排者）：在 `t361-实施证据.md` 末尾追加一行指向本件的指针，
   或具名授权本席补加。
2. **遗留出口（票面原样转交，不在本票）**：触发器该不该挂 `BEFORE UPDATE` 另议；
   中文名与用词统一归 #440（已关，`449573f`「肩围」已合入，本件期望字串与之一致）。
3. **锁协议小记**：本席早期用自研 `gate.ps1`（owner 写 `gate.lock/owner.json` 内，
   与官方包装器 `owner.json` 平级路径不一致；RUN 行仍在同一 `gate-runs.log` 可对账）。
   等锁期间发现官方包装器 `tooling/run-locked.mjs` 后续提交已改走它；`gate.ps1` 仅本票草稿，不进仓。
4. 未关票、未 push（按本轮口径由编排者统一收口）。
