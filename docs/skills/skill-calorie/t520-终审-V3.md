# #520 视觉终审 · V3 席分表（页 17–23）

> 席：V3（第 3 段：页 `17`–`23`，含缺口族 `21`／`22` 与别名页 `23`）。
> 审面：`D:\ilife\.scratch\t387\html\` 的 7 个文件（`17-摄入预测(自定义)`／`18-摄入预测(营养目标达成预测)`／`19-摄入预测(卡路里缺口预测)`／`20-摄入预测(摄入稳定性预测)`／`21-查热量缺口`／`22-看热量缺口`／`23-看体重预测`）。
> 档：每页 `1440`（`width=1440 height=900 fullPage`）与 `390`（`width=390 height=844 fullPage`）各一张整页截图。
> 尺：结构纪律 **20** ｜ 信息层级与版式细调 **25** ｜ 分隔符与文字纪律 **20** ｜ 跨宽自适应 **15** ｜ 手机端与 HELP 同档 **20**；每页给五维分 ＋ ①等权平均 ＋ ②加权总分；达标线 **≥ 90**。
> 工具说明：vision 工具（`dsh --profile visionreview`）本批三轮实测不可用，复核一次即止；改用本席自写零依赖 CDP 截图器（`.scratch/t520/shots-V3/batch-shot.mjs`，起浏览器法抄 `.scratch/t517/shots-review/shot.mjs`），自己读图。
> 截图落盘：`D:\ilife\.scratch\t520\shots-V3\`，命名 `<页序>-<页名>-<档>.png`。

## 五维与权重（全批统一的尺，照抄派单件）

| 维 | 权重 | 判据 |
|---|---|---|
| 结构纪律（栅格／tnum／缺值／色 token） | 20 | 内容列居中收得住吗；数值走等宽数字吗；缺值三态一致吗；颜色只取少数几个 token 吗；间距走 4／8 的倍数吗 |
| 信息层级与版式细调 | 25 | 主角数字是否突出；同级标题字号是否一致；卡片／表格／图是否同列对齐；有没有「一片灰小字」 |
| 分隔符与文字纪律 | 20 | 有没有 `·`／`；`／`｜` 顶替设计的并列串；同一事实是否一页多处；有没有英文内部标识符、库表名、常量名上屏 |
| 跨宽自适应 | 15 | 1440 宽下内容列是否只是「更空」而不是「更好」；390 宽下表格／卡片／图是否还读得开、会不会孤行或挤压 |
| 手机端与 HELP 同档 | 20 | 断点是否成组；字号有没有下限；触摸目标 ≥44px；窄屏表格行为是否与 HELP 同类 |

## 证据摘要

**别名页字节比对（自己复跑）**：`21-查热量缺口.html` 与 `22-看热量缺口.html` 的 SHA256 均为 `87CFD3DD8DA69640119EB96C718E0AE77ABDA16C303E78B016D028AD8641CDF1`，字节完全相同（各 87075 字节）。另注：`23-看体重预测.html` 与 `01-预测体重(1周后).html` 的 SHA256 同为 `CC01F1389B4EFA0D45E16C8CDA89F872BECFD20B16C36EB1ACA698C1330200D3`，也是同一产物两个别名。

**截图机器读数（`_metrics.json`，本席自写 CDP 截图器输出）**：

| 页 | 1440 档 fullHeight | 1440 档 docScrollWidth | 390 档 fullHeight | 390 档 docScrollWidth | 横向溢出节点（两档） |
|---|---|---|---|---|---|
| 17 | 1341 | 1440 | 1460 | 390 | `[]` / `[]` |
| 18 | 900 | 1440 | 921 | 390 | `[]` / `[]` |
| 19 | 900 | 1440 | 844 | 390 | `[]` / `[]` |
| 20 | 900 | 1440 | 891 | 390 | `[]` / `[]` |
| 21 | 2084 | 1440 | 2761 | 390 | `[]` / `[]` |
| 22 | 2084 | 1440 | 2761 | 390 | `[]` / `[]` |
| 23 | 900 | 1440 | 999 | 390 | `[]` / `[]` |

⇒ **横向溢出回归底线守住：7 页 × 2 档全部为 0**。「31 页 × 3 档溢出全 0」这条已知事实在本席范围内**未被破坏**；但按票面口径，溢出读数**不算成绩**，不作为加分项。

## 逐页五维分表

| 页 | 文件 | 结构纪律 (20) | 信息层级与版式细调 (25) | 分隔符与文字纪律 (20) | 跨宽自适应 (15) | 手机端与 HELP 同档 (20) | ①等权平均 | ②加权总分 |
|---|---|---|---|---|---|---|---|---|
| 17 | `17-摄入预测(自定义).html` | 11 | 10 | 15 | 9 | 14 | 11.8 | 12 |
| 18 | `18-摄入预测(营养目标达成预测).html` | 15 | 13 | 17 | 11 | 16 | 14.4 | 14 |
| 19 | `19-摄入预测(卡路里缺口预测).html` | 13 | 11 | 15 | 11 | 15 | 13.0 | 13 |
| 20 | `20-摄入预测(摄入稳定性预测).html` | 14 | 12 | 15 | 11 | 15 | 13.4 | 13 |
| 21 | `21-查热量缺口.html` | 12 | 13 | 12 | 11 | 13 | 12.2 | 12 |
| 22 | `22-看热量缺口.html`（＝21 同字节） | 12 | 13 | 12 | 11 | 13 | 12.2 | 12 |
| 23 | `23-看体重预测.html` | 14 | 12 | 15 | 10 | 13 | 12.8 | 13 |

**族内汇总（7 页算术平均）**：结构纪律 **13.0**／信息层级与版式细调 **12.0**／分隔符与文字纪律 **14.9**／跨宽自适应 **10.6**／手机端与 HELP 同档 **14.1** ⇒ **①等权平均 12.9** ／ **②加权总分 12.96（取整 13）**。
**判定：FAIL**（本席 7 页无一页达到 90；离达标线 **90** 的差距为 **77.04 分 / 页均**）。本席给分与票面「零点锚＝30 分」同量级偏下：本段发现的都是**形状级**问题（内容列宽不统一、参数输入框无形、卡片列数族内四种、数值与单位在手机档断裂、公共层重复口径），不是「颜色差一点」。

## 三条恒出核验（逐页）

| 页 | 页内导航 `ilife-block-toc` | 口径说明行 `ilife-block-caliber` | 来源脚注 `数据来源` |
|---|---|---|---|
| 17 | 有（3 项，L1830 `<nav class="ilife-block-toc" aria-label="页内导航">`） | 有（2 行，L1856 起） | 有（L1856 `📊 数据来源：本机饮食记录，窗口 2026-09-02 至 2026-09-15`） |
| 18 | 有（3 项，L1830） | 有（L1851 起 2 行） | 有（L1851 `📊 数据来源：本机饮食记录，窗口 2026-08-17 至 2026-09-15`） |
| 19 | 有（3 项，L1830） | 有（L1845 起） | 有（L1845） |
| 20 | 有（3 项，L1830） | 有（L1848 起） | 有（L1848） |
| 21 | 有（6 项，L1830） | 有（L1872 起 3 行） | 有（L1872） |
| 22 | 有（与 21 同字节） | 有（同 21） | 有（同 21） |
| 23 | 有（3 项，L1830） | 有（L1857 起 2 行） | 有（L1857 `📊 数据来源：本机体重记录，窗口 2026-09-02 至 2026-09-15`） |

**三件齐备率 7/7**。但两处同一枚 `ilife-block-caliber` 类被用于「口径说明」与「数据来源」两种语义（L1856／L1872 直接 `<p class="ilife-block-caliber">📊 数据来源：…`），属**样式类复用承载两种语义**（登记，不单独扣分）。

## 未评维度与工具不可用记录

- **工具不可用**：`dsh --profile visionreview`（`vision_html_screenshot`／`vision_describe`／`vision_crop`）本批三轮实测不可用（`dsh-vision-router` patch 层 `entry "modules" not found`），复核一次即止，未再重试。⇒ 本席改用**自写零依赖 CDP 截图器** `.scratch/t520/shots-V3/batch-shot.mjs`（起浏览器法抄 `.scratch/t517/shots-review/shot.mjs`）＋**自己读图**（`read_image`），62 张整页图里的 **14 张（7 页 × 2 档）全部逐张看过**。
- **未评/无法评的子项（逐条列出）**：
  1. **缺值 `—` 三态一致性**：本段 7 页**均无缺值样例**（无空单元格、无空指标卡），**本席无法评**，不计扣分。
  2. **等宽数字 tnum**：本席只做源码定向检索，**未命中 `font-variant-numeric`**（未逐页深查 `ilife-block-data-table-cell-right` 的完整规则），390 档明细表右列目视不齐（页 21 缺陷 V3-D8）属**目视读数**，**测试级结论未证实**。
  3. **间距是否走 4/8 的倍数**：未做像素级量测（未能量测），仅在截图层面判「大块留白与分段感」，**未证实为 4/8 倍数违规**。
  4. **颜色 token 数量**：未逐页统计色值出现次数，只做源码侧扫（L9–L18 `--fg/--fg2/--fg3/--blue/--blue2/--ok`），**未证实是否越界取色**。
  5. **安全区（notch）**：截图在无安全区环境拍摄，**「被安全区吃掉」这一问无法证伪**，各页 ⑥ 只给「无横滚／触摸目标」实测。
  6. **长页中段**：页 21／22 两档图各为一张整页图（1440×2084／390×2761），受派单预算约束未再切段，**图表 y 轴中段未做像素级读数**（结论基于整页图目视）。
  7. **页 01–16／24–31**：不在本席范围，未评（页 01 仅做与页 23 的字节比对）。

## 缺陷清单（按严重度）

**S1（阻断，须返修）**

- **V3-D1 · 同一产物两个别名、页面无身份**：`21-查热量缺口.html` 与 `22-看热量缺口.html` 字节相同（87075 字节，SHA256 `87CFD3DD…41CDF1`），**h1 都写「热量缺口 2026-09-09 至 2026-09-15」、都不含「查／看」**（源码 `21-查热量缺口.html:1830`／同 22）。同类：`23-看体重预测.html` 与 `01-预测体重(1周后).html` 字节相同（SHA256 `CC01F138…0200D3`）。⇒ 用户在墙上打开任一格都分不清是哪条命令的产物。**范围外发现**（模板层）。
- **V3-D2 · 页 23 手机档数值断裂**：`23-看体重预测-390.png` 第二行右卡「预计区间」把 `74.7 至 74.8 kg` 拆成三段（`74.7 至`／`74.8`／右侧悬空 `kg`）。**本票范围**（产出源码 L1830 KPI 明细 `预计区间`＋`95% 置信带`；断行属 `ilife-block-kpi-card-value-row` 公共层）。
- **V3-D3 · 页 17 标题与文件身份不符**：文件名「摄入预测(自定义)」，h1 与结论都写「按当前速率 60 天」（`17-摄入预测(自定义).html:1813`、`:1830`），与页 14–16 的族标题同句式。**本票范围**。

**S2（重，返修后再评）**

- **V3-D4 · 同一页两条内容列左缘**：页 21 概览卡 x≈180–1256（宽约 1078）而「缺口明细」表卡宽约 680 居中（x≈378–1062）；页 17 卡 1076 与表 680 同病。**本票范围**（公共层 680px 表宽已知缺口已在 #567，此处记「同页混用」）。
- **V3-D5 · 参数输入框无控件形**：7 页同款「标签一行、值一行」，`<input class="ilife-block-param-form-input">` 无边框无底色，值宽约 60–90px。**截图实证**（`17-…-1440.png` y≈270–480、`23-…-1440.png` y≈258–465）；样式规则未逐行核对 ⇒ **形制未证实，现象已证**。**范围外发现**（#567 承接面）。
- **V3-D6 · 卡片列数族内四种**：页 18／21 四卡（256px）、页 19 两卡（528px）、页 20 三卡（349px）、页 17 五卡（末位通栏）。**本票范围**。
- **V3-D7 · 手机档 chip 触摸目标约 34px**：页 21／22 六项导航折两行，chips 高度目视约 34px，**低于 44px**。**截图实证**（`21-查热量缺口-390.png` y≈235–300）。**范围外发现**（`toc` 触摸区已在 #567）。
- **V3-D8 · 明细表数值列 390 档不齐**：`21-查热量缺口-390.png` 明细 7 行「缺口」右缘不共线（`+1027`／`+1349`）。**目视读数，未证实**（未测 tnum）。
- **V3-D9 · 口径行与参数说明逐句同义**：`21-查热量缺口.html:1872` 三 span 与 `:1830` 参数说明同义；页 19「7700 卡≈1 kg」两处（`:1830`＋`:1845`）；页 20「300 卡算稳」两处（`:1830`＋`:1848`）；页 23「95% 置信带」「不到 14 天」两处（`:1830`＋`:1857`）。**本票范围**。

**S3（中，记入返修票）**

- **V3-D10 · 结论行与 KPI 卡互为复述**：7 页一律（页 17「60 天后」×2、页 18 缺 209、页 19 +1194／1.09、页 20 1591／44、页 21 +1162／+8135／1.06、页 23 74.7）。**本票范围**。
- **V3-D11 · 复制按钮行与卡片群左缘不对齐**：7 页的按钮行均 x≈458–980，卡片群 x≈180–1256。**截图实证**（各页 1440 图 y≈880–910 区）。**范围外发现**（公共层 action-bar 宽度）。
- **V3-D12 · 页 17 表格首行区间「1647 至 1647」**：把当日实际值印成零宽区间（`17-…-1440.png` 表第一行）。**本票范围**。
- **V3-D13 · 页 21 明细表「缺口」与状态连排**：`+1027√ 达标`（1440 与 390 两档）。**截图实证**。
- **V3-D14 · 页 21 合计行 390 档标签折行**：`合计摄／入`、`合计消／耗`、`合计缺／口`。**截图实证**（`21-查热量缺口-390.png` y≈2330–2480）。
- **V3-D15 · 页 21 图例不完备**：图内橙色虚线（摄入目标 1800 卡）只在图上标注，图例只有「摄入／消耗（虚线）」，且三行口径才补出「横线是摄入目标」。**本票范围**。
- **V3-D16 · 页 21 图表上下分流、中段空**：绿虚线在 2.5k–3.4k、蓝实线在 1.4k–1.7k，y 轴 3,100／2,276／1,452 之间大片无信息区。**目视读数**。
- **V3-D17 · 页 18 底部约 230px 空**：内容在 y≈670 结束（1440×900）。**截图实证**。
- **V3-D18 · 页 18 第四卡与前三卡不同构**：「是否在轨」无大数字、只有说明＋状态药丸（页 20「是否稳定」同构，390 档还独占通栏）。**本票范围**。
- **V3-D19 · 页 18 标题 390 档孤行**：`18-…-390.png` h1「摄入预测（营养目标达成预」＋「测）」——单字成行。**截图实证**。
- **V3-D20 · 「+卡路里缺口」正负号口径族内不一致**：页 19 `+1194 卡/天`（正数是缺口）vs 页 21 `+1162 卡`（无 /天），页 23 `-0.36 kg/周`（负号表下降）vs 页 19 `1.09 kg/周`（正号表掉重）。**本票范围**。
- **V3-D21 · 页 21 同事实重复（状态行 vs 明细列）**：合计卡上方「达标 7 天／偏低 0 天／超重 0 天」与明细表末列 7 个 `√ 达标`。**本票范围**。
- **V3-D22 · 页 21 明细表 390 档占页高 38%**：约 1040px／2761px，7 行×5 段。**截图实证**。
- **V3-D23 · 页内导航项数族内不齐**：页 17 四项、页 18–20／23 三项、页 21／22 六项。**本票范围**。

**S4（轻，登记）**

- **V3-D24 · 源码注释含票号／常量名／旧标题**（不上屏，不计分）：`21-查热量缺口.html:1830` 注释含 `KCAL_PER_KG=7700`、`旧页标题写法：…`；`:1872` 含 `targetTdee: 2635`；`18-…:1968` 含 `#247`；`21-…:1989` 含 `#247`／`:2020` `#249`／`:2079` `#121`；`17-…:1099`／`:1823` 含 `#104`／`820`。
- **V3-D25 · 未使用的图例样式资产随页下发**：页 18 携带 `.ilife-charts-legend*`／`--ilife-charts-dot` 等（`:516`–`:537`），本页无图。**登记**。
- **V3-D26 · 页 21／22 明细表降级后丢失星期**：1440 档「2026-09-09 周三」在 390 档的「日期」段只出 `2026-09-09`。**目视读数，未证实**（可能是 `data-label` 取值方式所致）。
- **V3-D27 · 口径类复用两种语义**：`ilife-block-caliber` 同时承载「口径说明」与「数据来源」行（各页 L1856／1857／1872）。**登记**。

**跨页数值不互洽（记录，不作本页五维扣分）**

- **V3-D28**：页 18 报「缺口 209 卡」（均值 1591 vs 目标 1800），页 19 在**同一窗口（2026-08-17 至 2026-09-15）**报「平均缺口 1194 卡/天」，页 21 报「+1162 卡」。三个读数按不同口径（对目标／对消耗）都说得通，但**产物未在任一处交代「209 是相对目标、1194 是相对消耗」**，同族三页并列时读者会读成互相矛盾。**本票范围**（口径文案层）。
- **V3-D29**：页 20 报「日均摄入 1591 卡」（窗口 08-17 至 09-15），页 21 报「日均摄入 1601 卡」（窗口 09-09 至 09-15）——窗口不同，不矛盾，但两页 KPI 标签同为「日均摄入」且无窗口提示（页 21 标题带窗口、页 20 不带），易误读。**范围外发现**（标题是否带窗口族内不统一）。

## 页 17 · 摄入预测(自定义)

**截图**：`D:\ilife\.scratch\t520\shots-V3\17-摄入预测(自定义)-1440.png`（1440×1341）／`17-摄入预测(自定义)-390.png`（390×1460）。机器读数：两档 `docScrollWidth = innerWidth`（1440／390），溢出节点 `[]`。

**① 第一眼看到什么、层级清楚吗**：眉标「趋势分析」＋ h1「摄入预测（按当前速率 60 天）」＋ 三枚 chip ＋ 蓝色结论行。层级是清楚的（眉标 → 标题 → 标签 → 结论 → 页内导航 → 参数 → 指标卡 → 轨迹表）。但**标题与文件身份不符**：文件名是「摄入预测(自定义)」，页内 h1 却写「按当前速率 60 天」——与页 14–16 的族标题（按当前速率 1／3 月）同一句式，看成第 4 个「按当前速率」页，看不出这是「自定义」入口。源码命中：L1813 `<h1 class="ilife-block-page-shell-title">摄入预测（按当前速率 60 天）</h1>`；L1830 结论 `按当前速率，60 天后每天大约吃 1818 卡。目标 1800 卡。`

**② 排版与间距**：三处硬伤。**(a) 参数区是「标签一行、值一行」的竖排**，三组参数各占约 55px 竖向、而输入框宽仅约 60px 且**没有边框／底色**，`2026-09-02` 看起来像纯文本而不是可编辑控件（源码：`<span class="ilife-block-param-form-label">开始</span><input class="ilife-block-param-form-input" name="start" value="2026-09-02" />`）。(b) 参数区只占内容列左侧约 1/3，右侧整片留白。(c) **参数/指标卡列宽 1076px，下面的轨迹表只有约 680px 且居中**——同一页内容列从 1076 缩到 680，表格相对卡片缩进约 260px，左侧边缘不对齐，明显像「三张小卡拼在一起」。(d) 表头「预测摄入」右对齐后落在 x≈720，而卡片群的右缘在 x≈1256，右对齐的目标线两个区块不一致。

**③ `；`／`·` 挤行**：屏上正文**没有** `；`／`·`。检索命中 11 处 `·` 全部在 `<style>` 注释里（L7 `base-paint 共享样式资产 · ilife-base · v0.1.0`、L1099 `区块样式资产 · #104 · 12 区`、L1823 `窄屏（820 · 同 HELP）…`），不渲染。**不扣本维**。

**④ 同一件事写两遍／内部标识符**：正文无英文标识符、无库表名、无 snake_case（检索 `>[^<]*(id|_id|snake|_KEY)[^<]*<` 零命中）。但 **「60 天」这一事实在标题、参数「预测天数 60」、结论「60 天后」、KPI 明细「60 天后」共四处出现**——参数回显属正常，结论与 KPI 明细两处是同一句话的两个落点，判为轻度重复。另：页内导航与参数是**同一枚 chip 形状**（都是药丸），导航项与纯展示 chip 视觉未拉开。

**⑤ 数值列等宽／缺值／长小数**：表格数值列自身对齐尚可（`1647`／`1647 至 1647`／`1647` 成列），KPI 数字也与表内一致（`1647`／`1818`）。但**无 tnum 证据**：L1841 起的表格单元格只有 `ilife-block-data-table-cell-right` 类，样式里没看到 `font-variant-numeric` 命中。本页**没有缺值**，所以「缺值是否统一 `—`」本页**无法评**。无长小数（`2.9`／`1647`／`1818`）。另注第 1 行「预计区间 1647 至 1647」是把**当日实际值**当区间印出来，读者会误读成「区间宽度 0」。

**⑥ 手机档 390**：KPI 2×2 排布正常；表格降级为「日期 ／ 预测摄入1647 ／ 预计区间1647 至 1647」三段式，**值与其 `data-label` 之间没有间距、连成 `预测摄入1647`**（与页 21 的表内 `+1027` 同行问题是同一个公共层现象）；chip 行「参数／概览／模拟轨迹／数据与日志」在 390 下折成两行，四个项都读得开；页底两枚复制按钮各约 44px 高，点击目标够。

**五维**：结构纪律 **11**／信息层级与版式细调 **10**／分隔符与文字纪律 **15**／跨宽自适应 **9**／手机端与 HELP 同档 **14** ⇒ **①等权平均 11.8** ／ **②加权总分 11.8**（11.8 → 取整 **12**）。
> 本席说明：本页是 7 页里版式最散的一页（1080 圆角卡＋680 表＋无边框输入框＋竖排参数四件同时出现），扣分集中在「结构纪律」「版式细调」两维。

## 页 18 · 摄入预测(营养目标达成预测)

**截图**：`18-摄入预测(营养目标达成预测)-1440.png`（1440×900）／`-390.png`（390×921）。机器读数：两档零溢出。

**① 第一眼**：眉标「趋势分析」＋ h1「摄入预测（营养目标达成预测）」＋ 三 chip ＋ 结论「这段的日均摄入 1591 卡，比目标 1800 卡差了 209 卡。」→ 层级干净利落，四张 KPI 卡（均值／目标／缺口／是否在轨）主角数字 32px 级、单位 `卡` 小一号，是本段 7 页里最像样的一屏。**但页内导航只有 3 项（参数／概览／数据与日志），没有「趋势」「缺口」这类二级锚点**，而同族的页 17 有 4 项、页 21 有 6 项——族内导航项数不齐（见页 21 缺陷 V3-D4）。

**② 排版与间距**：**(a) 底部大片空**：整页内容在 y≈670 结束，1440×900 视口下**下方约 230px 全空**，页面重心全压在左上。(b) 参数区问题同页 17（竖排＋无边框输入框），而且**本页参数只有「开始／结束」两项，却占了 y≈258–410 约 150px**。(c) **内容列对齐断裂**：KPI 卡左缘 x≈180、右缘 x≈1256（宽 1076）；而「复制数据／复制日志」按钮行左缘 x≈458、右缘 x≈980（宽约 520，居中）。按钮行既没跟卡片左对齐、也没跟卡片同宽，视觉上像被挪到中间「偏左」。(d) 四张卡中前三张有 32px 大数字、第四张「是否在轨」只有小字说明＋一枚 `偏离` 状态药丸，**同级卡片的视觉权重不等**。

**③ `；`／`·`**：屏上正文零命中。检索到的 `·`（L1099）与 `；` 全部在 CSS/JS 注释内。**不扣本维**。

**④ 重复／标识符**：正文无 `#票号`、无库表名、无 snake_case（`#\d+` 命中全部落在 `<style>` 与 `<script>` 注释／CSS 变量值 `#1d1d1f` 等色值上，非上屏文本）。重复：结论行「比目标 1800 卡差了 209 卡」与 KPI「目标 1800」「缺口 209」是同一组数，**结论行与 KPI 卡互为复述**（属该模板的固定写法，仅轻度）。

**⑤ 数值列**：全部整数（`1591`／`1800`／`209`），无长小数；本页**无缺值**，故缺值三态本页无法评。宽度上注意：KPI 卡 256px 宽却只装 4 位数，数字被拉成「大字＋大留白」，是密度偏空而非对齐错误。

**⑥ 手机档 390**：KPI 2×2；`是否在轨` 卡落到第二行右列，仍读得开；chip 行 3 项一行放下；两枚复制按钮各约 44px；页底口径行 3 段折成 2 行，无横滚（`docScrollWidth = 390`）。

**五维**：结构纪律 **15**／信息层级与版式细调 **13**／分隔符与文字纪律 **17**／跨宽自适应 **11**／手机端与 HELP 同档 **16** ⇒ **①等权平均 14.4** ／ **②加权总分 14.2**（14.2 → 取整 **14**）。

## 页 19 · 摄入预测(卡路里缺口预测)

**截图**：`19-摄入预测(卡路里缺口预测)-1440.png`（1440×900）／`-390.png`（390×844）。机器读数：两档零溢出；`fullWidth=innerWidth`。

**① 第一眼**：h1「摄入预测（卡路里缺口预测）」＋眉标「趋势分析」＋三 chip＋结论「这段时间平均每天有 +1194 卡缺口，折算下来一周约 1.09 kg。」→ 层级清楚，页内导航 3 项（参数／概览／数据与日志）。**本页只有两张 KPI 卡（平均缺口／每周掉重）**，是本段 7 页里最空的一屏。

**② 排版与间距**：**(a) 两张卡各宽约 528px**（x≈180–772／x≈772–1256），装 `1194 卡/天`、`1.09 kg/周` 这样的短数字，横向留白极大——同一族页 18 是 4 卡×256px、页 20 是 3 卡×349px、本页 2 卡×528px、页 21 是 4 卡×256px，**族内卡片列数与列宽在同一批产物里出四种**（族级版式不稳定）。(b) 参数区竖排＋无边框输入框，同页 17／18；本页只有两项参数却占 y≈258–410 约 150px。(c) 内容在 y≈660 结束，**下方约 240px 全空**。(d) 复制按钮行（x≈458–980）与卡片群（x≈180–1256）左缘不对齐，同页 18。

**③ `；`／`·`**：屏上正文零命中（定向检索 `>[^<]*[；·｜][^<]*<` 无结果）；`·` 仅见于 `<style>` 注释。**不扣本维**。

**④ 重复／标识符**：结论行与两张 KPI 卡为同一组数的复述（`+1194`／`1.09`）；正文无 `#票号`、无库表名、无 snake_case 上屏。**标题没有带窗口日期**，与页 21 的「日期进标题」写法相反，族内不统一。另注：**「7700 卡约等于 1 kg」在参数说明、口径行两处出现**（源码 L1830 `每 7700 卡大约对应 1 kg。` ＋ L1857 口径 `<span>每 7700 卡大约折算 1 kg</span>`），同一事实一页两处。

**⑤ 数值列**：整数与小一位小数（`1194`／`1.09`／`+1194`），无长小数，无缺值（缺值三态本页无法评）。`+1194` 带正号但**单位写作 `卡/天`**、KPI 明细「正数是缺口」，与页 21 同数字写作 `+1162 卡`（无 `/天`），**同族两页同一量纲的写法不一致**。无 tnum 证据。

**⑥ 手机档 390**：两卡 2×2（此处为 1×2 并排，各约 190px 宽）读得开；chip 行 3 项一行；复制按钮两枚各约 44px；页底口径 3 段折 2 行；零横滚。

**五维**：结构纪律 **13**／信息层级与版式细调 **11**／分隔符与文字纪律 **15**／跨宽自适应 **11**／手机端与 HELP 同档 **15** ⇒ **①等权平均 13.0** ／ **②加权总分 12.7**（12.7 → 取整 **13**）。

## 页 20 · 摄入预测(摄入稳定性预测)

**截图**：`20-摄入预测(摄入稳定性预测)-1440.png`（1440×900）／`-390.png`（390×891）。机器读数：两档零溢出。

**① 第一眼**：h1「摄入预测（摄入稳定性预测）」＋结论「日均摄入 1591 卡，每天上下波动 44 卡，算稳。」→ 层级清楚；三卡（均值／波动／是否稳定）横向排开，前两张是数值卡、第三张是状态卡。

**② 排版与间距**：**(a) 第三张「是否稳定」卡结构与前两张不同构**——前两张是「label ＋ 大数字 ＋ 单位」，第三张是「label ＋ 说明小字 ＋ 状态药丸（绿色）」；三张卡**等高但重心不同**，状态卡里没有大数字，视觉上塌一截。(b) 三卡各约 349px 宽装 `1591`／`44` 这类 2–4 位数字，留白偏大。(c) 参数区同为竖排无边框。(d) 内容在 y≈665 结束，**下方约 235px 全空**。(e) 按钮行与卡片群左缘不对齐，同页 18／19。

**③ `；`／`·`**：屏上零命中；`·` 仅在样式注释。**不扣本维**。

**④ 重复／标识符**：结论「日均摄入 1591 卡，每天上下波动 44 卡」与两卡（`1591 卡`／`44 卡`）互为复述；正文无内部标识符上屏。**「300 卡算稳」在参数说明与口径行两处**（L1830 参数说明「每天上下波动不超过 300 卡就算稳」＋ L1848 口径 `<span>判据＝上下波动不超过 300 卡算稳</span>`），同一事实一页两处。

**⑤ 数值列**：`1591`／`44` 均为整数，无长小数；无缺值（缺值三态本页无法评）。无 tnum 证据。

**⑥ 手机档 390**：布局为「均值／波动」并排两卡 ＋「是否稳定」**通栏单行**（末位奇数通栏，与页 17 的 5 卡末位通栏同一处理），读得开；chip 3 项一行；复制按钮约 44px；零横滚。

**五维**：结构纪律 **14**／信息层级与版式细调 **12**／分隔符与文字纪律 **15**／跨宽自适应 **11**／手机端与 HELP 同档 **15** ⇒ **①等权平均 13.4** ／ **②加权总分 13.1**（13.1 → 取整 **13**）。

## 页 21 · 查热量缺口

**截图**：`21-查热量缺口-1440.png`（1440×2084）／`21-查热量缺口-390.png`（390×2761）。机器读数：两档零溢出、溢出节点 `[]`。

**① 第一眼**：眉标「趋势分析」＋ h1「热量缺口 2026-09-09 至 2026-09-15」＋ 三 chip（卡路里／热量缺口／趋势分析）＋ 蓝色结论「这段时间平均每天有 +1162 卡缺口，一周合计 +8135 卡，折算下来约 1.06 kg。」→ **本页是 7 页里信息层级最完整的一页**：6 项页内导航（参数／概览／每日摄入与消耗／缺口明细／合计／数据与日志）把长页切成了可跳的段。第一眼主角是 h1 里的日期窗口——**页标题被日期窗口取代了**，用户看不出这页叫「查热量缺口」（见 ④）。

**② 排版与间距**：**(a) 概览段 kpi-grid 宽 1076px 四卡（x≈180–1256），而图卡与「缺口明细」表卡宽约 1080px 与 680px 两种**：图卡 x≈180–1258（宽约 1078，与卡片对齐），明细表卡 x≈378–1062（宽约 680，居中）——**同一页三段内容两条左缘（x≈180 与 x≈378）**，是 7 页里最显眼的一处对齐断裂。**(b) 合计三行标签折行**：390 档里「合计摄入」「合计消耗」「合计缺口」被折成「合计摄／入」「合计消／耗」「合计缺／口」（标签列放过窄），屏幕上一眼就能看出两行字。**(c) 明细表内「缺口」与状态连排**：`+1027` 后紧贴 `√ 达标`，数字与符号之间无间距（300 档与 390 档同样出现）。**(d) 图表 y 轴刻度 3,100／2,276／1,452 与两条曲线**：绿色虚线（消耗）与蓝色实线（摄入）之间一大片空白，两条线各占图高上下端，**中间的 1,452–2,276 区间几乎无信息**，图内密度失衡。**(e) 图例 `■ 摄入 ■ 消耗（虚线）` 位于图卡左上**，而图内还有一条橙色虚线（摄入目标 1800 卡）**只在图上直接标注、不在图例里**——图例与图内元素不完备。

**③ `；`／`·` 挤行**：屏上正文零命中（`>[^<]*[；·｜][^<]*<` 无结果）。命中 19 处 `·`／`；` **全部在 `<style>`／`<script>` 注释**（L7 `base-paint 共享样式资产 · ilife-base · v0.1.0`、L1099 `区块样式资产 · #104 · 12 区`、L1989 `#247：复制数据的三格式形态——…`、L2020 `开合只翻**可视态**（…）；而 DOM 序＝内容序`）。**不扣本维**。

**④ 同一件事写两遍／内部标识符**：**(a) 标题无页面身份**——h1 是「热量缺口 2026-09-09 至 2026-09-15」，与文件身份「查热量缺口」不同，且与页 22「看热量缺口」的标题**完全相同**（两页字节相同，见页 22 小节）；用户从标题无法区分自己开的是「查」还是「看」。**(b) 口径行把屏上已有内容再说一遍**（源码 L1872）：`<span>缺口＝当天消耗减当天摄入（正数就是有缺口）</span> <span>消耗＝日常消耗加当天运动</span> <span>摄入只算吃进去的，喝水不算</span>` —— 这三句与参数说明 L1830「缺口就是当天消耗减掉当天吃的：正数代表有缺口。消耗算日常消耗加当天运动，摄入只算吃进去的，喝水不算。」**逐句同义**；同时「达标线＝每天 +300 卡缺口」与结论／状态列「达标」重复。**(c) 上屏无英文内部标识符**；但源码 L1830 注释里带 `KCAL_PER_KG=7700`、`旧页标题写法：热量缺口 2026-09-09 ~ 2026-09-15`、L1872 带 `targetTdee: 2635`（**注释不上屏，不计扣分，仅登记**）。**(d) 状态药丸「达标 7 天／偏低 0 天／超重 0 天」在合计卡上方又出现一次**，与明细表末列 7 个 `√ 达标` 重复。

**⑤ 数值列与缺值**：明细表七行 `1608/1649/1602/1536/1548/1620/1647`、`2635/2935/…`、`+1027/+1286/…` 成列，**全整数、无长小数**；页内**无缺值**，缺值三态本页无法评。合计行 `11210 卡`／`19345 卡`／`+8135 卡` 与「共 7 天有记录／逐日累加」一致。注意数字与单位间有空格（`1601 卡`），而明细表里的裸数不带单位、靠表头给单位，是合理分工。`+1162`（结论）与 KPI `+1162 卡` 一致。

**⑥ 手机档 390**：**(a) 明细表降级为「日期／摄入／消耗／缺口／状态」五段式**，每行成一块、共 7 块占总高约 1040px（整页 2761px 的 38%）——读得开但很长；**(b) `data-label` 与值连排**：`摄入  1608`（无间距），日期行是 `日期   2026-09-09 周三`；**(c) 明细表右列「缺口」的数字列不是等宽数字**，7 行个位数对不齐（`+1027` 与 `+1349` 的右缘不在同一竖线上）。**(d)** chip 行 6 项折成两行（参数／概览／每日摄入与消耗／缺口明细 → 合计／数据与日志），触摸目标约 34px 高、**低于 44px**（chips 行整体约 34px）。**(e)** 复制按钮两枚各约 44px；零横滚。

**五维**：结构纪律 **12**／信息层级与版式细调 **13**／分隔符与文字纪律 **12**／跨宽自适应 **11**／手机端与 HELP 同档 **13** ⇒ **①等权平均 12.2** ／ **②加权总分 12.2**（12.2 → 取整 **12**）。

## 页 22 · 看热量缺口（与页 21 字节相同的别名）

**先说结论：本页与页 21 是同一产物两个别名，字节完全相同。** 本席自己复跑的读数：两文件各 87075 字节，SHA256 均为 `87CFD3DD8DA69640119EB96C718E0AE77ABDA16C303E78B016D028AD8641CDF1`；两页的两档截图也逐张同哈希——`21-查热量缺口-1440.png` 与 `22-看热量缺口-1440.png` 同为 `EE52CF8664748E49943289AA86537699DC82AEDCCAAE6FE1A8D362918B6ED7B3`，390 档同为 `AA81E9F975AE30198393ABC16AEAF4F4FBC6A94967BE469A4BD1E1C10F36B370`。**墙上按两格出、本席逐页各评一次**，故本节按页 21 的同一读数独立给分。

**六问答案与页 21 完全一致**（同字节 ⇒ 同版式、同缺陷）：① 第一眼仍是「热量缺口 ＋ 日期窗口」，**看不出这页是「看」还是「查」**；② 同一处对齐断裂（1080 与 680 两条内容列左缘）、390 档「合计摄入」折行、`+1027√ 达标` 连排、图例缺橙色虚线、图表上下分流中间空；③ 屏上无 `；`／`·`（命中的 `·` 全在注释）；④ 口径行与参数说明逐句同义、状态行与明细表重复、标题**不带名字且与页 21 一模一样**；⑤ 无缺值、无长小数、明细表数字成列（390 档右列不齐）；⑥ 390 档表格读得开但明细占页高 38%、chip 触摸目标约 34px 不足 44px、无横滚。

**两页唯一的差别是文件名**（`查` vs `看`）：这带来一个页面之外的实质问题——**同一份 HTML 挂在两个命令别名下，用户从产物无法分辨两条命令是否真的做同一件事**；若其中一个别名本该有不同参数（如「看」是零参数浏览、「查」带窗口），当前产物把这个差别抹平了。此项属**范围外发现**（记入缺陷 V3-D1），不在本页五维里二次扣分。

**五维**（同页 21）：结构纪律 **12**／信息层级与版式细调 **13**／分隔符与文字纪律 **12**／跨宽自适应 **11**／手机端与 HELP 同档 **13** ⇒ **①等权平均 12.2** ／ **②加权总分 12.2**（取整 **12**）。

## 页 23 · 看体重预测

**截图**：`23-看体重预测-1440.png`（1440×900）／`-390.png`（390×999）。机器读数：两档零溢出。
**身份注记**：`23-看体重预测.html` 与页 01 `01-预测体重(1周后).html` **字节完全相同**（各 70740 字节，SHA256 均为 `CC01F1389B4EFA0D45E16C8CDA89F872BECFD20B16C36EB1ACA698C1330200D3`）——本页也是别名产物（本席列范围**不含**页 01，故只登记、不并评）。

**① 第一眼**：眉标「趋势分析」＋ h1「体重预测（7 天）」＋ 三 chip（卡路里／体重预测／趋势分析）＋ 结论「按最近的趋势，7 天后体重约 74.7 kg。」→ 层级清楚；四张 KPI 卡（当前 75.1／预测 74.7／速率 -0.36／预计区间 74.7 至 74.8）。**本页有 3 项参数（开始／结束／预测天数）**，与页 17 同构，参数说明写「能推 7 到 180 天」，比页 18–20 的说明更完整。

**② 排版与间距**：**(a) 参数区与指标卡之间的竖向净空约 63px，与参数区内每个字段之间的净空几乎相同**——参数区（y≈258–465）与卡片区（y≈528 起）之间的分段感被压平，读者难分出「这里换段了」。**(b) 竖排参数＋无边框输入框**：三组参数各「标签一行、值一行」，`2026-09-02`／`2026-09-15`／`7` 三个值都贴着左缘、无输入框形状，其中「预测天数 7」宽约 60px 却占约 55px 高（源码 L1830 `<input class="ilife-block-param-form-input" name="horizonDays" value="7" />`）。**(c) 内容在 y≈740 结束，1440×900 下下方约 160px 全空**。**(d)** 按钮行（x≈458–980）与卡片群（x≈180–1256）左缘不对齐，同页 18／19／20。

**③ `；`／`·`**：屏上正文零命中（同一批定向检索无结果）；`·` 仅见 `<style>` 注释（L7／L1099／L1823）。**不扣本维**。

**④ 重复／标识符**：正文无 `#票号`、无库表名、无 snake_case 上屏（`class="ilife-block-param-form-input" name="horizonDays"` 里的 `horizonDays` 是属性值，不上屏）。重复：结论「7 天后体重约 74.7 kg」与 KPI 卡「预测 74.7 ／ 明细 7 天后」为同一事实两处；「95% 置信带」在 KPI 卡明细与口径行两处；「体重记录不到 14 天不出预测」在参数说明与口径行两处（L1830＋L1857）。

**⑤ 数值列**：`75.1`／`74.7`／`-0.36`／`74.7 至 74.8` 全为小一位小数，**无长小数**；无缺值（缺值三态本页无法评）。`-0.36` 与页 19 的「每周掉重 1.09 kg/周」正负号口径不同（本页用负号表下降），**同族两页下降方向一个用正号一个用负号**。

**⑥ 手机档 390**：**(a) 「预计区间」卡在 390 档断裂**——标签下方先是 `74.7 至`，再换行 `74.8`，而单位 `kg` 单独落在右侧第二行位置，**数值、连接词、单位被拆到三处**；这是 7 页里手机档最明显的一处数值区排版故障（截图 `23-看体重预测-390.png` 第 2 行的右卡）。**(b)** 四卡 2×2；chip 3 项一行；复制按钮约 44px；页底口径 3 段折 2 行；零横滚（`docScrollWidth = 390`）。

**五维**：结构纪律 **14**／信息层级与版式细调 **12**／分隔符与文字纪律 **15**／跨宽自适应 **10**／手机端与 HELP 同档 **13** ⇒ **①等权平均 12.8** ／ **②加权总分 12.6**（12.6 → 取整 **13**）。

---

（本席报告完 —— 三条恒出核验、未评维度、缺陷清单见上，共 7 页、14 张整页截图。）
