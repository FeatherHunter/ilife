# t336 波动-证据（页面④体重波动 5 词）

> 票 `#336`（地图 `#154`）。分区：只动波动归属（`src/weight/volatility.ts`＋本票用例＋本证据件）；
> 未碰 `src/render/index.ts`、`plate.ts`／`plateDocs.ts`、`src/cli/write.ts`、`SKILL.md` 生成块。
> 老技能权威表 `D:\2Study\StudyNotes\SKILLS\卡路里\scripts\_triggers.py` 已按行取证（未整读 621KB）。

## 一、两处对不上（先写明，再改冻结数据）

以唤醒词语义为准（用户念的一句话，页面答那句话）。出处：老脚本 `render_weight_volatility_v2.py` 头注
（稳不稳默认 30 天；异常点 `--view anomalies-only` 仅异常列表）＋ `_triggers.py` 五条原文。

| # | 现状 | 应为 | 改哪两处 | 为什么 |
|---|---|---|---|---|
| 1 | 「看体重稳不稳（增强版）」路由窗口词 `{"window":"7d"}`，而说明写「看最近 30 天体重波动是否稳定」 | `{"window":"30d"}` | `src/weight/routes.ts:33`（order 106）＋`src/triggers/scene-03-weight.ts:32`（key `w_vol` 的 `main_prompt.cli` 与 `data_source` 同改） | 老技能 `w_vol` 的 `cli` 是无参 `render_weight_volatility_v2.py`，脚本无参即 30 天前～今天；`7d` 与文案对不上 |
| 2 | 「看波动异常点」与上一条同命令同参数（`{"window":"7d"}`），出的不是只看异常点 | `{"window":"30d","view":"anomalies-only"}`（窗口沿老缺省 30 天，形态走 `view`） | `src/weight/routes.ts:37`（order 110）＋`src/triggers/scene-03-weight.ts:36`（key `w_vol_anomalies` 的 `main_prompt.cli` 与 `data_source` 同改） | 老技能 `w_vol_anomalies` 的 `cli` 是 `--view anomalies-only`；今天缺 `view` 参数，命令层也不收 `view`，故两条跑出同一张整图 |

带 sha 断言的连带：`test/calorie-sot.snapshot.json`（`entry_sha[w_vol]=b9560bde6e5d908c`、
`entry_sha[w_vol_anomalies]=42a38e11704f4b87`）须走 `gen-sot-snapshot.mjs --write` 重算；
`test/calorie-routing-81.test.mjs:176-180` 要求路由与冻结逐字相同，故两处同改；
生成物 `src/triggers/routes.generated.ts` 走 `pnpm gen` 重生成，不手改。

**本次是否已改共享件：未改。** 树上 #334（`compare.ts` 情景面中间态，`tsc` 报 `scenario` 未登记）、
#372／#383 等并行在途，两道门本地持锁复跑已红（见 §五，红因均在他票文件）。
按并发协议跨票改动转票原则，共享两行＋快照重算留待干净窗口持锁执行（`run-locked.mjs --ticket 336`＋广播写入集），
本票先交付已验证的自有范围。命令层已就绪：`view` 参数与缺省 30 天本次即落地（§二），共享行一改即跑通。

## 二、本次改动（自有范围）

- `packages/skill-calorie/src/weight/volatility.ts`（LF 306，未超 350 线）：
  - `viewVolatility` 收 `view`（`full`／`anomalies-only`，缺省 `full`；非法报 `bad-input`），
    缺省窗口由 7 天改为 **30 天**（与老脚本无参口径一致；显式 `window` 仍优先）。
  - `buildVolatilityDoc(v, view)`：`anomalies-only` 只出异常点表（含原因列）＋结论句，不出曲线段与 σ 趋势
    （与老模板隐藏 KPI＋Canvas 口径一致，标题为「看波动异常点」）；
    整图补 σ 趋势曲线、异常原因列、结论句，曲线标题带出黄／红阈值（±σ 带文字口径）。
  - 算式未动：`weightVolatilityV2` 与老 `analysis/weight.py::weight_volatility_v2` 同形
    （近 7 点 stdev／全程差分 stdev、1.5σ 黄／2.0σ 红、σ 为 0 兜底 0.5、近 30 均值基线）。
  - 参数面核对：`volatility.ts:121`（现已移行，逻辑同）走 `shared/params.ts` 的 `defaultRange`，
    第一句即 `windowRange(params)`——本页无页面①式「窗口接错」，未重复修。
- `packages/skill-calorie/test/weight-volatility-336.test.mjs`（新建，6 用例）：
  算式／5 条窗口语义／只看异常点形态／页面补齐字段／`view` 非法／缺省 30 天。

## 三、老实物补齐对照（`weight_volatility_v2.html` → 新页面落点）

| 老实物位置 | 新页面落点 |
|---|---|
| KPI（诊断／趋势／σ／预警）＋基线值／目标值＋双基线切换 | KPI 格（波动分析＋基线值／切换语＋阈值＋σ＋基线模式＋预警） |
| Canvas 主图（±σ 带＋目标线 toggle） | 偏离基线曲线（标题带黄±／红±阈值） |
| 异常点列表＋计数 | 近期异常点表（计数 caption）＋原因列 |
| σ 趋势（`sigma_trend`） | σ 趋势曲线（本次补上） |
| 结论句（`summary`） | 副标题「预警｜结论句」（整图）／副标题（只看异常点）；口径照老 `_augment_kpis`（总体 stdev＋近 7 天日均波动＋黄／红计数） |

## 四、指纹基线

- 开工：`node .scratch/332/pages-fingerprint.mjs --check volatility` → `volatility SAME`，
  全量 `FINGERPRINT-CHECK PASS 5/5`（2026-09-14）。
- 收工：`volatility DIFF`（base `53389607…` → cur `30ed8a55…`）为本票页面补齐的预期内变化；
  同轮 `dashboard`／`history` 亦 DIFF，系并行他票在途改动，非本票范围（本票未碰那两页）；
  `compare`／`review` 保持 SAME。

## 五、持锁运行（GATE-RUN，对账源 `.scratch/locks/gate-runs.log`）

| 运行 | runId | exit |
|---|---|---|
| `pnpm build`（整树，红在他票 `compare.ts` 中间态） | `0d702061-8d5d-4189-b687-e172d2a98b05` | 2 |
| 单文件转译 `volatility.ts`→`dist`（ESM，树红时的隔离验证手段，收尾待整树重建覆盖） | `e633fa76-33b0-4386-822f-22cd2556c207` | 0 |
| 本票用例 6/6 | `75bd3d2f-7cb4-4ed4-8639-de8fd9754e27` | 0（`pass 6 fail 0`，日志 `.scratch/t336/test336c.log`） |
| 5 条真出口导出 | `c6b90d04-bb9e-4223-85ba-d58ca3441e97` | 0（`EXPORT-OK 5/5`，日志 `.scratch/t336/export5.log`） |
| `pnpm gen:check` | `393da579-6c01-4250-9a78-b26e7b8dab0d` | 1（`GEN-STALE FAIL`：`analysis/commands.ts`、`analysis/routes.ts` 为他票在途内容，非本票） |
| 棘轮两件 | `bf491a39-cb11-4994-bae8-ca7d20e0b76b` | 1（`cmd-registry-294`：`calorie.view.multi-trend` 未登记，他票新键，非本票） |

GATE-RUN 声明：`GATE-RUN runId=75bd3d2f-7cb4-4ed4-8639-de8fd9754e27 cmd="node --test packages/skill-calorie/test/weight-volatility-336.test.mjs"`；
`GATE-RUN runId=c6b90d04-bb9e-4223-85ba-d58ca3441e97 cmd="node .scratch/t336/export5.mjs"`。

## 六、真出口证据（持锁真跑，`calorie-cmd-read calorie.view.volatility`，种子库 2026-07-20～2026-08-18＋spike）

| 唤醒词 | 参数 | 落盘（绝对路径） | 字节 |
|---|---|---|---|
| 看体重稳不稳（增强版） | `{"window":"30d","today":"2026-08-18"}` | `D:\ilife\.scratch\t336\exports\vol-wen-bu-wen.html` | 76980 |
| 看本月波动 | `{"window":"本月","today":"2026-08-18"}` | `D:\ilife\.scratch\t336\exports\vol-ben-yue.html` | 71976 |
| 看最近 90 天波动 | `{"window":"90d","today":"2026-08-18"}` | `D:\ilife\.scratch\t336\exports\vol-d90.html` | 76980 |
| 看最近 180 天波动 | `{"window":"180d","today":"2026-08-18"}` | `D:\ilife\.scratch\t336\exports\vol-d180.html` | 76980 |
| 看波动异常点 | `{"window":"30d","today":"2026-08-18","view":"anomalies-only"}` | `D:\ilife\.scratch\t336\exports\vol-anomalies-only.html` | 58032 |

断言：5 条 `exit 0`；整图含曲线段（`偏离基线（黄±`）＋σ 趋势＋结论句；只看异常点不含曲线段与 σ 趋势、
标题为「看波动异常点」且含原因列与计数。文件为真实 HTML（`<html`＋`ilife-page` 整页模板），可直接打开肉眼验收。

## 七、行数与交付对账

- `volatility.ts` LF 306（`split('\n').length-1`），350 线未超，无需重构报警。
- 影响清单（事前）＝实际碰到：`src/weight/volatility.ts`（命令层＋页面装配补齐）、
  `test/weight-volatility-336.test.mjs`（新建）、本证据件。偏差为零。
- `git status` 本票集之外另有他票在途件（`compare.ts`、`analysis/commands.ts` 等），提交时只加本票三件。

## 八、进度与下一步（95%，不 close）

待确认：① 干净窗口执行 §一 的共享两行＋`pnpm build→pnpm gen→pnpm build→pnpm help:build`＋快照重算，
之后两道门复绿；② 用户肉眼验收 5 份 HTML（§六路径）。确认后由编排者定 close。
