# t621 定运动目标写命令证据（场景 04 运动 · 地图 #156）

目标：在目标能力新增一条会改数据库的命令 `calorie.goal.exercise`
（定运动目标，代表唤醒词「定运动目标」），把每日运动消耗目标写入
`daily_goal.exercise_goal`（单例 UPSERT，只 SET 这一列）；
`src/render/planPlate.ts` 的缺目标阻断文案点名下一步
（给出 `calorie-cmd-read calorie.goal.exercise` 原文）。

## 开工他席在途件（`git status --short`，本票一字未碰）

- `M docs/skills/skill-calorie/t620-第三轮-证据.md`
- `M packages/base-combos/src/present.ts`
- `M packages/skill-calorie/src/analysis/reportDoc.ts`
- `M packages/skill-calorie/src/analysis/reportDocScore.ts`
- `M packages/skill-calorie/src/analysis/reportDocTracked.ts`
- `M packages/skill-calorie/test/t620-第三轮.test.mjs`
- `?? docs/base/base-render/t567-公共层形状清账-证据.md`
- `?? docs/skills/skill-calorie/t618r-独立探针.mjs`

## 判据先行（红 → 绿）

- 红：`node tooling/run-locked.mjs --ticket 621 -- node --test packages/skill-calorie/test/exercise-goal-write-621.test.mjs`，fail 3（`t621 calorie.goal.exercise 应 exit 0，实测 3 ERR 3: 未知 calorie key`，点名本票新断言），见 `.scratch/t621/red-01.log`。
- 绿：同一条靶向命令，pass 3 ／ fail 0（写前 NULL → exit 0 ＋落盘完整文档→复读一致；只 SET 这一列；0／负数／非数字／缺参一律 exit 2），见 `.scratch/t621/green-02.log`。
- GATE-RUN runId：`5fa31852-8fb5-470e-bd6d-de49e93fa352`（绿那次持锁运行的 runId；红那次为 `740d26f9-d6f0-411f-b38b-302ac80f6206`）。

## 变异（SET 列名改坏 → 必红 → 逐字节还原 → 回绿）

- 变异红：把 `src/goal/write.ts` 里 UPSERT 的 `exercise_goal = excluded.exercise_goal` 改成 `exercise_goal_bad` 一处，重建后同一条靶向命令 fail 2（`实测 4 ERR 4: no such column: exercise_goal_bad`），见 `.scratch/t621/mut-red.log`（runId `a161bc36-afc2-48db-a754-83324bb2fab6`）。
- 变异绿：逐字节还原（与 `.scratch/t621/write.ts.good` 逐字节一致）后重建回绿 pass 3，见 `.scratch/t621/green-02.log`（runId `5fa31852-8fb5-470e-bd6d-de49e93fa352`）。

## 编译与派生

- 编译：`node tooling/run-locked.mjs --ticket 621 -- node node_modules/typescript/bin/tsc -b packages/skill-calorie`，exit 0（runId `fa0c1be1-58c0-4ebd-b5e3-dca9d67cd88f`，还原后复建 runId `aed299e7-2a11-4f3e-a5e0-ff1ebe64722a` exit 0）。
- 说明一：路由记录的 `key` 受 `RouteKey` 约束，而 `RouteKey` 由生成物 `src/cli/keys.ts` 派生，故分两段走——先只交写入口与命令声明、重建、派生出新命令的登记，再补路由记录、重建、再派生。两段编译都经持锁包装器。
- 说明二：`pnpm gen` 的 pnpm 包装层在本机缺 TTY（`ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`，见 `.scratch/t621/gen-01.log`），改走同一生成器的直接形态 `node tooling/run-locked.mjs --ticket 621 -- node packages/skill-calorie/scripts/gen-cli.mjs`（派生结果与 `pnpm gen` 同源），GEN-STALE 时按医嘱先 `--stamp` 再派生。
- `gen:check`：PASS（命令 129：会改数据库的命令 47 ＋ 查询命令 82），runId `5084994d-6300-4caa-b554-436483ce52c4`。
- 派生产物（`src/cli/keys.ts`、`src/triggers/routes.generated.ts`、`packages/base-combos/combos.yaml`、`scripts/build-help.mjs` 的新增行）只验证不提交：提交只含本票声明的路径，见下「提交」。

## 回归（靶向两条，经持锁包装器）

- `exercise-summary-goal-fusion-452.test.mjs`：pass 9 ／ fail 0（runId `8d15a09a-eff5-4a7b-bc0d-97c30f5b8b52`），#452 现有判据不回退。
- `exercise-accept-267.test.mjs`：pass 8 ／ fail 0（`T267-RESULT: 8/8`，runId `3686076a-5f13-432a-9dcc-adbec0014fda`）。

## 告警线门（只读，未加 `--sync`）

- `node tooling/run-locked.mjs --ticket 621 -- node packages/skill-calorie/scripts/check-warning-line.mjs`：首跑 `RESULT: 89/91`，唯一 RED 为 `RED 台账陈化：scripts/build-help.mjs 台账=742 实况=744`（runId `839383e2-20da-45ad-af95-80e1cd61f8be`，见 `.scratch/t621/warn-01.log`）。
- 定界：该 2 行增量正是本次派生给新命令加的登记行（`REPR`／`EXAMPLE` 各一），文件与台账同步都不在本票可写路径内；按票面「超线只报警不重排」「红在他人路径定界范围外记 S3，不修他人件」记 S3，不改他人件、不跑 `--sync`。
- 复跑（他席随后把台账同步到 744 之后）：`RESULT: 91/91`、`PASS: 告警线台账齐全且与实况一致`（runId `9a086aee-7dfc-461d-bd95-a0d80de5f408`，见 `.scratch/t621/warn-02.log`）。
- 本票六件源码均未超 350 行告警线（门内无本票件的 OVER 行）。

## 交付路径（本票提交只含这些）

- 改：`packages/skill-calorie/src/goal/write.ts`（新增 `writeGoalExercise`，参数 `goal` 正整数，0／负／非数字走失败 2；UPSERT 只 SET `exercise_goal` 列）
- 改：`packages/skill-calorie/src/goal/commands.ts`（声明 kind write，命令 `calorie.goal.exercise`，receipt 形状，标题「定运动目标」，代表唤醒词「定运动目标」）
- 改：`packages/skill-calorie/src/goal/routes.ts`（wake 记录，scene 06，kind exec，同命令，order 436——原 wake 面 0..435 连续，取下一位无空洞）
- 改：`packages/skill-calorie/src/goal/receipt.ts`（具名命令集与写入说明加分支：已写入运动目标，说「看目标状态」可复查；列登记加 `exercise_goal`「运动目标(卡)」；参数名 `goal` 映射到该列）
- 改：`packages/skill-calorie/src/triggers/scene-06-goal.ts`（冻结表加一行：目标管理／定目标／receipt／`templates/goal_config.html`，预检确认页＋确认后写入＋三句话限制，照定体重目标款，order 25）
- 改：`packages/skill-calorie/src/render/planPlate.ts`（阻断句保留原句，后补 `calorie-cmd-read calorie.goal.exercise --params '{"goal":300}'` 原文；既有 `/未设运动目标/` 断言仍过）
- 新：`packages/skill-calorie/test/exercise-goal-write-621.test.mjs`（判据先行）
- 新：本证据件
- 草稿：`.scratch/t621/`（红绿、变异、编译、派生、回归、门禁日志）

## 提交

- `git diff --cached --name-only` 复核只含本票 8 条路径后提交，信息 `feat(621): 定运动目标写命令加判据与证据`；不推送。
