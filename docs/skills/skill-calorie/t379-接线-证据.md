# t379 · 接线证据（场景 10 拆 A：88 条执行流程接线 SKILL.md＋HELP）

> 票：#379（地图 #160）。用词照 `docs/agents/wording.md`：命令／跑得通／跑不通／过程型／结果型／回执／页面交付／命令补齐。
> 数字以本文件所贴机器读数为准，不另存第二份清单。视觉判定留用户肉眼，本文件只给程序绿。
> 本票不加新命令、不改页面长相与字段、不动 `calorie_deficit`；`SKILL.md` 只动本图手写段，生成块零手改。

## 0 · 结论先行

- 判定① 对账绿：路由层 `cli` 与冻结表逐条同序逐字同，`ALIGN-PASS: 88/88`，`CHECK: PASS`（A＝两指标配对 71＋多指标整体趋势 15＋趋势对照 2）。
- 判定② 抽查绿：固定种子库（`CALORIE_TODAY=2026-09-07`）每组至少 1 条真出口，`TRUE-OUTLET: 6/6`，落盘／绝对路径／字节／指标全有限数全绿。
- 判定③ 视觉留用户肉眼：样例固化 `.scratch/t379/out/sample-*.html`（6 份），任选打开文图一致即过。
- 门绿：`pnpm build` 绿、`pnpm gen` 绿（键 118＝写 46＋读 72）、`pnpm build` 复绿、`pnpm help:build` 绿、`pnpm gen:check` 绿、示例 118/118 绿、双棘轮 13/13 绿。
- 判据② smoke 全量正在跑（`.scratch/379/smoke.md`，后台任务收数后补行）；全量测试留收口（本票区门全绿，见 §4）。
- 进度 95%，不 close（待确认见 §6）。

## 1 · 诊断（开工前）

- `Blocked by: #377, #378`：两票均 OPEN 95% 条件完成未 close，但接口已冻结可条件开工（#377 combined 71 抽样 15/15＋复用装配、`#378` 14＋2 铺开验证 17/17，证据件均已落盘）。
- 接口冻结点（本票只读复用，不改口径）：`calorie.view.multi-trend`（shape stat／示例 90d＋target／routes order 353／冻结 order 93）＋ `calorie.view.combined` 71 条＋对照 2 条。
- `.scratch/t379` 开工前不存在（已建）；`docs/skills/skill-calorie/t379-*` 开工前不存在（本文件为首件）。
- #379 已认领（assignee FeatherHunter，他人未占，先 `gh issue edit 379 --add-assignee @me` 已执行）。

## 2 · 对账表（判定①，88/88）

- 脚本：`.scratch/t379/align-88.mjs`（只读三份源文件，不 spawn CLI）。
- 口径：A＝`src/analysis/routes.ts` 中命令为 combined／multi-trend／long-trend／calorie-trend 的全部；冻结＝`src/triggers/scene-10-analysis.ts`（85 条旧词：`main_prompt.cli`＝`data_source`）＋`src/triggers/routes.generated.ts`（3 条新词 new 6／45／47）；逐条比路由 `cli`＝冻结 `cli`（旧词兼 `data_source`）＝生成 `cli`，同序逐字。
- 机器读数：

```
A-TOTAL: 88
A-COMBINED: 71
A-MULTI: 15
A-TREND2: 2
ALIGN-PASS: 88/88
CHECK: PASS
```

- 过程型核验：沿 #171 §6，本图 88 条全是查询命令，冻结表 prompt 无“先确认”写法，0 条适用先出过程型 HTML，记账不开票（SKILL 手写段已明示）。

## 3 · 真出口抽查（判定①②，6/6；固定种子库）

- 种子：`docs/research/t81-seed.mjs`（`seedFull`；`CALORIE_TODAY=2026-09-07`，只写系统 tmp）。
- 脚本：`.scratch/t379/true-outlet-379.mjs`（照抄 `.scratch/376/true-outlet.mjs` 口径：spawn 真 CLI→envelope→`data.output` 恒绝对路径→落盘字节＋sha256→指标全有限数）。
- 链路（每条）：说唤醒词 → 复制冻结表 prompt → 冻结 `cli` 执行 → 结果型 HTML 落盘、回执绝对路径。
- 逐条读数（样例固化 `.scratch/t379/out/sample-*.html`，`results.json` 全值）：

| 唤醒词 | 命令 | 字节 | sha256 | 判定①② |
|---|---|---|---|---|
| 看体重 vs 摄入(最近 7 天) | calorie.view.combined pair weight_calorie 7d | 74120 | a77bb9582937d769d4c28efdce05c0cb840b5d2bd798e095db2b8134d03da350 | 绿 |
| 看体重 vs 缺口(最近 7 天) | calorie.view.combined pair weight_deficit 7d | 73560 | e0d6caf2542eda43cdfb522f67e16b2e4c6629cb5d477aab7ad7539ec35183d7 | 绿 |
| 看整体趋势(体重+摄入+运动) | calorie.view.multi-trend 90d g1 target | 139519 | 2bb69e41ce31c03c7e767d1113f9398f49e6b8f17b335249ddb4790c61548590 | 绿 |
| 看整体趋势(含目标对比) | calorie.view.multi-trend 90d target | 139508 | bb38848efc0b038f20717166526d9cbd26b421b9566d951270b928d9838a4c05 | 绿 |
| 看热量趋势 | calorie.view.calorie-trend 7d | 65059 | 0aa0209e33a7eed6efeff678a736a1936284503dc9b3b2da3f49e0de49821166 | 绿 |
| 看整体趋势 | calorie.view.long-trend weight_calorie 30d | 81809 | fc79a78aec2d755952b61640951cdfa18fa54343274393e0972a6d479626f4d0 | 绿 |

```
TRUE-OUTLET: 6/6
CHECK: PASS
```

- 注：未设 `CALORIE_TODAY` 时看热量趋势走空窗 exit 4（可读缺失阻断，口径内算跑得通）；钉死 `2026-09-07` 后 6/6 落盘。字节与 #378 证据小差（约数十字节）系同期页面文案 drift，不影响判定（本票只验落盘／绝对／有限数，不钉字节）。

## 4 · 门绿读数（持锁运行，`--ticket 379`）

| 门 | 读数 |
|---|---|
| `pnpm build`（生成链第 1 步） | exit 0（GEN-STAMP ok，41 件声明源） |
| `pnpm gen` | 键 118（写 46＋读 72），exit 0 |
| `pnpm build`（生成链第 3 步） | exit 0 |
| `pnpm help:build` | HELP 已注入 SKILL.md，exit 0 |
| `pnpm gen:check` | GEN-CHECK PASS：键 118（5 件 ok） |
| `help:examples:check` | 示例 118/118 通过，红 0（SKILL 手写段未伤 AUTO 新鲜度） |
| 双棘轮 | 13/13 pass |
| 对账 | ALIGN-PASS 88/88 CHECK PASS |
| HELP 现找 | 旧词 5/5 绿（下）；3 条新词缺口见 §5 |
| 靶向真出口 | TRUE-OUTLET 6/6 CHECK PASS |

- HELP 现找脚本 `.scratch/t379/help-5.mjs`：`HELP-LOOKUP: 5/5 CHECK: PASS`（看体重 vs 摄入／看体重 vs 缺口／看整体趋势分组例／含目标对比／含月度对比，逐词 `cli` 含对应命令与参数片段）。
- smoke 全量：`node docs/research/t81-exec-smoke.mjs --out .scratch/379/smoke.md` 已收数：exec 479 条，原样实跑 exit 0 计 408＋占位符替换后 exit 0 计 59，非零 12（`.scratch/379/smoke.md` §0 汇总与 §642 起清单；12 条 exit 均为 4，键属 today／weight-history／weight-review／exercise-records／predict，全在 B 侧他人域，A-list 行 92/92 含 exit 0，无一条非零）。判据② smoke 行数＝exec 条数：479＝479（文件行以脚本输出为准）。
- A-list 92 口径：exec 88（SoT 85＋新拟 3，对应 combined 71＋multi-trend 15＋对照 2）＋§3 无参裸跑 4 键（combined／multi-trend／long-trend／calorie-trend 裸跑 exit 0），合计 92/92 全 exit 0，无一条非零。
- 全量测试：本票未重跑（工作区多票并发，收口定；本票区双棘轮 13/13 与示例 118/118 见上）。

## 5 · 交付对账（#171 §5 写入集＋结构五铁律）

| #171 切分＋本票声明 | 本票实际 | 偏差 |
|---|---|---|
| `SKILL.md` 只动本图手写段 | 新增 `场景 10 分析（拆 A）工作流程（88 条查询命令）` 一节（＋37 行：链路＋权威出处＋对账口径＋页面归类＋分组代表表＋2 条 prompt 原文＋HELP 面＋抽查口径）；生成块（HELP-AUTO）零手改（`pnpm help:build` 派生，AUTO 起止各 1） | 0 |
| HELP 相关页（以 #171 §3 为准） | 未新建页面（两类页已由 #376／#377／#378 交付）；HELP 注入已跑（SKILL AUTO 118 组合）；现找旧词 5/5 绿 | 0 |
| 生成物重算 | `pnpm gen`＋`help:build` 已跑；派生更新不提交，留收口（红线只 add 自己件） | 0（派生已出，提交按红线收窄） |
| 对账脚本 | `.scratch/t379/align-88.mjs`＋`true-outlet-379.mjs`＋`help-5.mjs`（＋`out/` 6 样例＋`results.json`） | 0 |
| 证据件 | 本文件 | 0 |
| 不动 | `calorie_deficit.html`／B 的词与线／`packages/base-render/`／任何命令声明与页面字段 | 0（未碰） |

- 文件卫生：`SKILL.md` LF 357 行、无 BOM、无字面反斜杠 n、AUTO 起止各 1；超线不触发（SKILL 系说明面，不在 `src/**/*.ts` 告警口径内）。
- 结构五铁律：无 src 改动即无违反（本票只写文档与 `.scratch/` 脚本）。
- 已知缺口（遗留，不在本票改）：3 条新词（看组合分析／看热量趋势／看整体趋势裸词）在冻结表无行，`calorie.help.lookup` 无精确命中（路由直达可执行，真出口 6/6 含其中 2 条命令形；裸看整体趋势走子串命中非精确）。补行属冻结表改动，留收口票统一补（本票红线不动他人区）。

## 6 · 进度与待确认（不 close）

- 进度 95%。
- 待确认（未确认不 close）：① 用户肉眼验收（判定③：`.scratch/t379/out/` 任选打开，文图一致即过）；② 全量测试 exit 0（收口复跑；smoke 非零 12 全在 B 侧已有归因，A-list 92/92 绿）；③ 提交范围裁定（工作区含多票同期改动，本票是否只交 SKILL 手写段＋本证据件，生成物留收口）。
- MAP 同步：本票不改 #160 正文；MAP Decisions 备一行见 §7（验收过后再回写）。

## 7 · MAP Decisions 备行（验收过后由编排者回写）

```
- 2026-09-14 #379 场景 10 拆 A 接线 88/88（路由 cli＝冻结 cli 同序逐字）＋种子抽查 6/6＋示例 118/118：唤醒词→prompt→命令→结果型链路在 SKILL 本图段可查，HELP 旧词现找通、新词 3 条补行留收口（证据 docs/skills/skill-calorie/t379-接线-证据.md）。
```

## 8 · 遗留出口记账

- 某条词 prompt 与命令语义对不上：本次对账 88/88 逐字同，未发现缺参式错位，不开票。
- HELP 新词 3 条无精确命中：属冻结表缺行（§5），补行留收口，不在本票改冻结表；是否另开编号票由编排者裁定。
- 超 350 行：未触发（无 src 改动）。
