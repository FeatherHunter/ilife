# #385 热量缺口页核验 · 证据（按老实物逐项核）

> 归属：MAP #161 的子票 #385。范围＝冻结表 order 200「查热量缺口」（有命令可执行）＋ new 别名
> 「看热量缺口」；两者 `routesFor` 命中同一条命令 `calorie.view.deficit`，cli 逐字
> `--params '{"window":"7d"}'`。
> 老实物：`D:\2Study\StudyNotes\SKILLS\卡路里\templates\calorie_deficit.html`（10,631 B）＋
> `scripts\render_calorie_deficit.py`（5,387 B）。
> 纠正：地图正文「`calorie_deficit.html` 13 条」不成立——冻结表里该模板只出现 1 次（#382 已对数，
> 本票按纠正后的范围做，不做 13 张缺口页）。

## 一、基线快照（红绿可归因）

| 项 | 值 |
|---|---|
| 仓库 | `D:\ilife`，分支 `master` |
| 起点 HEAD | `fd5c922f1cc679e879ef1500893684118ace32a4` |
| 起点 `git status --porcelain` 行数 | 67（他席在途件大量存在） |
| 本票写集起点状态 | `git status --porcelain -- <四条写集>` **空** ⇒ 四条路径开工前无他人在途改动 |
| 起点输入哈希 | `src/analysis/deficit.ts` `B616B7DC…`（LF 81）／`src/render/trendDocs.ts` `46FC4C01…`（LF 816，**全属本人改动**）／`src/render/analysisPlate.ts` `A8E62527…`（LF 184）／`src/analysis/commands.ts` `D16FA683…`（LF 324）／`src/analysis/routes.ts` `FCD920F6…`（LF 185）／`src/triggers/scene-10-analysis.ts` `5488E4C4…`（LF 181） |
| 起点告警线门 | 先红（`RESULT: 48/49`，台账陈化 `src/triggers/routes.generated.ts`）→ 他席同步后绿（`RESULT: 48/48`） |
| 起点 `pnpm build` | 绿（`runId=02fa545e-5d90-4c69-b314-e6abab4aa777`，exit 0） |

本票**未碰**：`src/analysis/commands.ts`、`src/analysis/routes.ts`、`src/triggers/scene-10-analysis.ts`、
`docs/skills/skill-calorie/t382-10B-对账表.md`、预测 15 条与报告 8 条（各归各的票）、公共层任何件。
**未新增命令、未改参数形状、未手改生成物**（故未跑 `pnpm gen`）。

## 二、逐字段核对表（10 项，全部对得上）

产物＝`t385-样张-查热量缺口.html`（7d 窗，75,354 B）；数据读数取自同一次运行的
`data.metrics`：`avgIntake 1943／avgBurn 2599／avgExerciseBurn 43／avgDeficit 656／
weeklyDeficit 4592／predictedLossKg 0.6／days 7／weekdayCount 5／weekendCount 2／
targetIntake 1800／targetTdee 2556`。

| # | 老侧字段（`render_calorie_deficit.py`） | 本页落点 | 判定 |
|---|---|---|---|
| 1 | `summary.avg_intake` | KPI「日均摄入」值 `1943` ＋详情「目标 1800 卡/天」 | ✅ 对得上 |
| 2 | `summary.avg_burn` | KPI「日均消耗」值 `2599` ＋详情「TDEE 2556 · 运动 43 卡」 | ✅ |
| 3 | `summary.avg_exercise_burn` | 同上详情「运动 43 卡」 | ✅ |
| 4 | `summary.avg_deficit` | KPI「日均缺口」值 `+656`（带符号） | ✅ |
| 5 | `summary.weekly_deficit` | KPI「理论减重」详情「周缺口 4592 卡」＋合计行「+4592 卡」 | ✅ 且＝窗内缺口合计（老侧 `weekly_deficit = total_d`） |
| 6 | `summary.predicted_loss_kg` | KPI「理论减重」值 `0.6` kg；口径 `4592 ÷ 7700 = 0.59636 → 0.6` | ✅ `KCAL_PER_KG=7700` 不变 |
| 7 | `summary.trend` | KPI「日均缺口」详情（减重方向／增重方向／持平） | ✅ 三值闭集 |
| 8 | `target.intake` | KPI「日均摄入」详情「目标 1800 卡/天」＋图表 markLine「摄入目标 1800 卡」 | ✅ |
| 9 | `series[]` | 「缺口明细」表 7 行 × 6 列（日期／摄入／消耗／缺口／目标／状态） | ✅ 见第三节 D1／D2 |
| 10 | `meta.weekday_count`／`weekend_count` | 表 caption「共 7 天 · 5 工作日/2 周末」 | ✅ 且 5＋2＝7 |

样张逐行（机器读出，缺口语义与老侧同源）：

```
2026-09-01 周二 | 1000 | 2556 | +1556 | +300 | ✓ 达标
2026-09-02 周三 | 2400 | 2556 |  +156 | +300 | ⚠ 偏低
2026-09-03 周四 | 2800 | 2556 |  -244 | +300 | ✗ 超量
2026-09-04 周五 | 1500 | 2856 | +1356 | +300 | ✓ 达标
2026-09-05 周六 | 2200 | 2556 |  +356 | +300 | ✓ 达标
2026-09-06 周日 | 2600 | 2556 |   -44 | +300 | ✗ 超量
2026-09-07 周一 | 1100 | 2556 | +1456 | +300 | ✓ 达标
TOT 合计摄入 | 13600 卡 | 7 天
TOT 合计消耗 | 18192 卡 | TDEE×天＋运动
TOT 合计缺口 | +4592 卡 | 周缺口 4592 卡
```

## 三、差异清单（判据先行跑红的就是这三条）

判据先行：新测试先写、先跑 → **红**（`tests 6／pass 3／fail 3`，exit 1），红的正是 D1／D2／D3。

- **D1 表格「目标」列取错字段（真错）**。老侧该列＝`target.weekly_deficit_per_day`（每日缺口目标，
  逐行 `+300`，且与同行「状态」列的判定阈值同源）；改前本页该列＝`d.target.intake`（摄入目标 `1800`）
  ——拿**摄入目标**去比**缺口**列，列值错、且与状态判定脱钩。**已按老侧改正为每日缺口目标（带符号）。**
- **D2 表格「状态」列少一态**。老侧三态（`calorie_deficit.html:174-178`）：
  `deficit >= 每日缺口目标` → `✓ 达标`；`deficit > 0` → `⚠ 偏低`；否则 → `✗ 超量`。
  改前本页两态（按缺口正负给「缺口／盈余」）。**已按老侧补齐三态**（样张三态全覆盖）。
- **D3 老侧 `tfoot` 合计行缺位**。老侧表底有合计行（总摄入／总消耗／总缺口，`calorie_deficit.html:187-192`）；
  改前本页无合计块。**已补**：B-03 表格块没有 footer 槽位（补它要改公共层，本票不改），
  故走 B-05 行列表给「合计摄入／合计消耗／合计缺口」三行——仓内既有同款先例
  `src/render/sportPortDocs.ts:262`（「摄入合计／TDEE 合计」）。
- **D4 老侧专色不移植（记偏离）**。老侧每行摄入／消耗用 `--intake:#5856d6`／`--burn:#34c759`
  专色、缺口三态用 `.deficit-pos/neg/zero`；本仓这套 token 零命中（硬约束 2），
  且技能侧颜色字面量必须为 0、样式一律走 base-paint ⇒ **本票不移植**。若后续要「摄入 vs 消耗」
  的系统专色，需另立票在公共层新增语义 token。
- **D5 老侧图表缺 Y 轴刻度值（已在本票消掉，见第六节）**。老侧 `calorie_deficit.html:149` 自注
  「Y 轴刻度文字缺失（记偏离）」——**这不是公共层能力缺口**：公共层支持刻度
  （`spec/charts.ts:98` 声明 `yTicks?: number|false`；`charts.ts:451-454` 收敛 2–6；
  `:465-472` 渲染 `<text class="ilife-charts-tick">`），只是折线缺省 `yTicks→false`（`charts.ts:777`）
  ⇒ **属「本页漏传参数」**。老侧另外还传了 `format`／`yMin`／`yMax`，新版三个都没传。

## 四、判据先行与红绿读数

| 阶段 | 命令 | 读数 |
|---|---|---|
| 判据先行（改前） | `node packages/skill-calorie/test/analysis-deficit-385.test.mjs` | `tests 6／pass 3／fail 3`，**exit 1**；红项＝D1 目标列＋D2 三态＋D3 合计行 |
| 实施后 | 同上 | `tests 6／pass 6／fail 0`，**exit 0** |

测试以唤醒词为起点：`routesFor('查热量缺口')`／`routesFor('看热量缺口')` 各取 `kind === 'exec'`，
断言命令＝`calorie.view.deficit`、cli 参数形状＝`{"window":"7d"}`，再照 cli 真跑；
每例断言 `exit 0`＋`data.output` 是**绝对路径**且等于本次 `--html`＋产物在盘上＋完整文档六连
（`assertDocPage`：doctype／charset／样式段／脚本段／`ilife-page`／无残留标记）。
窗口差异走程序断言三例：`7d`（7 天）／`本月`（7 天）／`自定义 2026-08-25~2026-09-07`（14 天），
逐例断言天数、明细表行数、标题起止、工作日＋周末＝天数、`data.output` 绝对路径。
另断言空窗阻断不变：窗内无饮食记录 ⇒ 非 exit 0、**不落盘**、报错文本含「无饮食记录」。

## 五、变异自证（源码级，两行机器读数）

变异点＝`src/render/trendDocs.ts` 缺口明细表「目标」列装配：把
`target: (targetDef >= 0 ? '+' : '') + targetDef,` 改回 `target: d.target.intake,`（即 D1 那处）。

```
MUT-RED  : pnpm --filter skill-calorie exec tsc -b → node .../analysis-deficit-385.test.mjs
           tests 6／pass 4／fail 2，exit 1
           红项文案：D1 目标列应是每日缺口目标 +300（老侧 weekly_deficit_per_day）：1800
                     D1+D2 合成三行：目标列恒为 +300、状态三态各自成对
MUT-GREEN: 还原后重编 ＋ 同测试 → tests 6／pass 6／fail 0，exit 0
           还原后 SHA256＝C58FB9097F8DEC4567EBD41C9AC5F50C36BA3835B82C30CF025D515634BB32E9
           变异前 SHA256＝C58FB9097F8DEC4567EBD41C9AC5F50C36BA3835B82C30CF025D515634BB32E9
           HASH-IDENTICAL=True（逐字节一致）
```

回滚连生成物一起复位：`dist/render/trendDocs.js` 由同一次 `tsc -b` 重编后，样张探针复跑仍出
三态＋3 条刻度（第一节读数即复位后重取）。

## 六、补 Y 轴刻度（前／后机器读数）

改动＝缺口页折线 options 补 `yTicks: 3`＋`labels: 'select'`＋`format`（同包先例
`src/analysis/multiTrendPage.ts:261`／`src/render/trendMiscPortDocs.ts:139`）。
**量程不写死**：老侧写死 `yMin:1000／yMax:3000` 会裁掉窗外数据，新版沿用图表层自适应域
（这是本次唯一一处**不**照抄老侧的地方，理由记在此）。

| 读数 | 值 |
|---|---|
| 补刻度前（同形状调用，只给 `series`／`markLine`） | `<text class="ilife-charts-tick"` 元素数＝**0** |
| 补刻度后（`yTicks:3`＋`format`） | 同口径元素数＝**3** |
| 样张产物（改后真跑） | 同口径元素数＝**3**，刻度文字＝`889`／`1,928`／`2,967` |

（`format` 用 `Math.round(v).toLocaleString()`。老侧同位置是 `v => Number(v).toLocaleString()`，
刻度值是内插值，整数化＋千分位更可读；同包 `axisOf` 口径也是整数卡。）

## 七、模板归类结论

**`calorie_deficit.html` = 只此一处用 ⇒ 就地住卡路里包，不做公共层模板名。**
落点＝`packages/skill-calorie/src/render/trendDocs.ts` 的 `buildDeficitDoc`（＋取数
`src/analysis/deficit.ts` 的 `buildDeficitData`），与 #382 对账表 Q3 一行逐字一致
（「只此一处用（就地，#385 按老实物逐项核）／现状落点 `trendDocs.ts` `buildDeficitDoc`」）。
本票实测确认该归类成立：该页挂 1 个模板、1 条命令、2 个唤醒词，没有第二处复用点；
其中**可提取到公共层的**只有「折线必传刻度＋格式」这一条口径（本票已就地照同包先例办）。
指针评论已发在 #382（只评论，不改其正文；#382 已 CLOSED）。

## 八、未做项与移交

1. **`yMin`／`yMax` 未照抄老侧写死值**——理由见第六节。若用户要老侧那套固定量程，请再定。
2. **缺口三态色／摄入消耗专色不移植**（D4）——要做须另立票在公共层新增语义 token，本票不自造。
3. **`calorie.view.combined` 的 10 条看体重 vs 缺口是否复用缺口字段**——属拆 A 图的账，
   本票只报告不代做（票面遗留出口原文如此）。
4. **`src/render/trendDocs.ts` 行数**：本票改后 LF＝818，与台账「当场实测」818 一致。
   该件早已超 350 告警线（挂号行在册，超因＝趋势／分析域同质文档装配全挤一件）。
   **已超线，需要根据规则进行重构**：本次先不拆——拆法不属本票写集，且本票只动其中缺口节一段。
5. **他席在途件不计入本票红绿**：本票实施期间告警线门一度红在
   `scripts/build-help.mjs`（LF 362）／`src/render/sportDocs.ts`（LF 362）两处漏报，
   `pnpm build` 一度红在 `src/analysis/reportPlate.ts`／`src/analysis/reportDoc.ts`（#384 在途件）
   ——均**不在本票写集**，属他席在途，非本票引入。

## 接管者更正（后补，不改上文原文）

本件是前任中断前的记录。有三处读数与**合流当刻**的实况不符，已由接任执行者独立复跑更正；
详细读数与 runId 见 `docs/skills/skill-calorie/t385-接管核验.md`（尤其其第三节与第九节）：

1. **§四 判据先行**：前任记 `tests 6／pass 3／fail 3`；当刻可复现读数是
   **`tests 6／pass 4／fail 2`**（exit 1）。同为「红」，但条数对不上——D2／D3 的断言与 D1 同处两测，
   被排在更后面的 D1 断言遮断。
2. **§五／§八 合流后的件**：前任记 LF **818**、SHA256 `C58FB909…`；当刻实况是
   **LF 826**、SHA256 `8BA32A31…`。原因是本补丁的基准是前任当时的 HEAD（LF 816），而 `#160` 随后
   在该节**上方**又提交了 8 行（HEAD 变 824），故套用后为 824＋2＝**826**。
   **缺口节内容与前任跑绿并做完变异自证的那一版逐字节一致**，只差上方他席新增的 8 行。
3. **§八.4 台账**：前任记「改后 LF＝818，与台账 818 一致」；他席随后已把台账「当场实测」列同步为
   **826**，与当刻实况一致（`check-warning-line.mjs` 当刻 `RESULT: 56/56`／`PASS`／exit 0）。

另更正**本件所属测试文件自身的头注释**：原 `D5` 段写「（公共层 `charts.ts` 能力缺口）…本票不改公共层」，
与它自己函数体（钉死 `ticks===3`）及本件第六节自相矛盾；已按实测改为
「**不是**公共层能力缺口，属本页漏传参数，本页补 `yTicks:3`＋`format`，不改公共层」。
