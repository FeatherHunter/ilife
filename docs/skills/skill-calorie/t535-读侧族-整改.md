# 票 #535 · 读侧族四页重排（看体脂／看体脂趋势／看围度／看围度趋势）· 整改证据

- 席位：接管席（上一席上下文耗尽、一行未提交；本席从零按编排者给的逐处清单做）
- 日期：2026-09-15 ｜ 写集：`src/body/bodyDocs.ts`、新件 `src/body/bodyReadUi.ts`、本族测试、本件
- 提交：`9f4710d`（06/07 重排）→ `d10215a`（08/09 重排 ＋ 06/07 复核三处）→ `7e2c388`（复核收口：读数卡收两张／单位留空格／趋势点单位）
- 判据源：`.scratch/m158v2/席位简报.md`、编排者 2026-09-15 两条视觉复核口信、节点级探针 `audit-separators.mjs`

## 1. 改前／改后读数（同一 `--dir`、同一探针）

| 页 | 改前命中（node 级） | 改后命中 | sha256（改后前 10 位） | 390 档整页高 |
|---|---|---|---|---|
| 06-看体脂 | 7（含 `<title>` 的 `·`、参数文档 `；` 串、窗口说三遍） | **0** | `6471A66654` | 2527px → **1404px** |
| 07-看体脂趋势 | 7 | **0** | `68B7E2CA1F` | — |
| 08-看围度 | 3（参数文档 `；`、表题 `·` 串） | **0** | `3116E2CE15` | 3812px → **3224px** |
| 09-看围度趋势 | 3 | **0**（与 08 **不再逐字节相同**） | `B645AB9C10` | — |

门：`audit-separators --dir .scratch/m158v2/t535/stage` → **RESULT: 4/4 PASS**（exit 0）；
`measure-responsive --label t535` → **OVERFLOW-ZERO pages=4 cells=12 failed=0**；
`版式探针 --widths 390,1440` → 四页 390 档：溢出 0／触摸不足 0／最小字号 **12px**（改前 08/09 有 11px 的 `span.msr-note`）／
`viewport-fit=true`／安全区 b/t/l/r=2/0/1/1／断点 400|640|720|820（不含自造断点）。

## 2. 逐处清单（编排者给的 9 条 ＋ 四页都要做的）

**06／07（体成分两页，同一个装配件按窗口种类换主次）**

1. `<title>`：`卡路里·运动身体` → **`卡路里 身体细节`**（编排者裁定，四页同值；空格分隔、无符号）。
   眉标 `calorie.view.body-composition · 运动身体域` → **`身体细节`**；主标题 `体成分看` → 按窗口取
   **`体脂记录`**（全部历史／自定区间）／**`体脂趋势`**（近 N 天）。
2. 副标题那条 CLI 参数文档整条删掉，改读者视角一句（记录页「每次测到的体脂率按日期倒序列出，最新一条在最上面。」／
   走势页「体脂率随时间的走势，一个点是一次测量。」）；参数表单的说明改「填一个来源就只看它的记录。不填则为全部来源。」
3. 窗口**只说一处**：新增 `windowBar()`（`窗口：全部历史`，单个文本节点——`#362` 判据逐字认这一句），
   读数卡明细／表题／图题里那三处窗口全部撤掉。
4. 皮褶 7 点表：3 列 → **2 列（部位／皮褶（毫米））**，7 行重复的日期收进表题 `皮褶 7 点原始值（2026-09-15）`；
   表体只列部位与毫米值（缺槽位 `—`）。半角括号 ＋ 大写单位 `皮褶(MM)` 收口为人话。
5. `距上次测量 1 天（2026-09-06）· 体脂率 下降 0.5%（上次来源 …）` 那条 `·` 串：判语与间隔并成一句人话进
   `距上次 Δ` 卡（`距上次测量 1 天（2026-09-06），这次下降`），上一次来源另出**卡外事实条**（`renderFactStrip`）。

**08／09（围度两页，`BodyMeasureView.windowGiven` 换主次）**

6. `<title>`／眉标／主标题同 06/07：主标题按 `windowGiven` 取 **`围度记录`**／**`围度趋势`**。
7. 副标题 `13 围度项按名筛选（空=全部并自动挑最近有数据部位出趋势；两期对比归组合分析）` 整条删掉，
   改人话一句；参数表单字段值改印部位中文名（不再回显驼峰参数名）。
8. 表题 `围度记录（共 9 条 · 13 项全量 · 单位 cm）` → 表题只留**表名**（`围度记录`／`围度记录明细`），
   条数由窗口条一枚胶囊承载、单位由一条口径行承载（`表里与卡上的数值单位都是 cm｜没量到的项写字面「—」`，
   `｜` 是版式分隔，落盘文本里不留该字符）。
9. 两页主次：记录页＝记录表在前、走势在后；走势页＝走势在前、记录在后；页内导航与 `<section id>` 跟着换序。
   **两页 sha256 不同**（见 §1）。

**四页都要做的**

- `assembleDocPage({ pageUi: true })` 四个装配点全部接上（`viewport-fit=cover`／安全区／820 档 44px／640 档字号下限／
  页内导航横滑／表格卡片化）。
- 页内定位：四页都出 `renderTocBlock`，区块吃 `pageUi` 的 `scroll-margin-top: 20px`。
- 读数卡值槽／正文比：本族页内 CSS 把 `ilife-block-kpi-card-value` 抬到 **24px**（共享层 22px ÷ 正文 15px ＝ 1.47×，
  24 ÷ 15 ＝ **1.6×**）。
- 390 档字号：`span.msr-note` 11px → **12px**；缺值统一 `—`；数值列 `tabular-nums`；颜色只用既有 token；间距走 4／8 倍数。

## 3. 判据冲突（逐条裁定与代价，供终审复核）

- **`#362` 要求可见文本里 `共 N 条` 至少两处且彼此相等**（`countReadings` 全量相等）。编排者要求「同一个数留一处」，
  与这条判据冲突。落法：**06/07 表题留 `共 N 条`**（判据另有一条断言钉死表题必须写「窗口内另有 M 条」），
  **第二处改住图上那句口径行**（`图上每个点是一次测量那天的读数｜窗口内共 N 条记录`），窗口条上那枚同样的胶囊按编排者要求删掉；
  **08/09 反过来**——条数只留在窗口条一枚胶囊里，表题只留表名（围度侧没有这条判据）。
- **`#361` 把围度页的读数卡措辞钉死**（`趋势点 3 天`／`81cm`／`44cm`）。编排者 2026-09-15 复核明确要求
  「趋势点 N 天 改 个」、「数值与单位之间留空格」，故**同改** `t361` 的三处**措辞**断言（手算值 3／95.5／95／96／+1／96
  与「自动挑部位＝胸围」一个没动，其余断言原样）。这条改动是本票唯一动到别人判据测试的地方，写在这里请终审裁。
- **`#362` 要求分组态（`source=all`）每张图自报窗口**（`每张分组图都自报窗口口径`）。四页缺省路径不走分组态，
  这四页的图上不再重复窗口；分组态逐来源图题仍是 `X 的体脂趋势（窗口：全部历史）`，属判据保留项。
- 皮褶表不能改成「一日多点卡」：`#362` 按表题「皮褶 7 点原始值」认那张表并要求 **7 行逐点成行**。
  落法取编排者给的第一选项——**7 行小表（部位／毫米两列）**，日期只进表题。

## 4. 本票门与测试

- `audit-separators`：**4/4 PASS**、exit 0（命令经 `run-locked --ticket 535 --lock-dir .scratch/locks-m158v2`）。
- `measure-responsive`：**OVERFLOW-ZERO pages=4 cells=12 failed=0 scopeOutFailed=0 label=t535**。
- `版式探针`：见 §1；JSON 落 `.scratch/m158v2/t535/fmt.json`。
- 测试：`t535-读侧族-重排.test.mjs`（**新件**，7/7 PASS）、`t361-measure-full-table`（PASS，措辞断言同 §3 更新）、
  `t362-窗口不截断`（15/15 PASS，逐字保留它钉的标签与注文）。
- `tsc -b packages/skill-calorie`：**本件绿**；当刻红在别人在途件——`src/render/dietDocs.ts`、
  `src/diet/todayDocs.ts`、`src/body/compare*.ts`、`src/body/receipt.ts`（都不是本票写集，未改一行）。
- 真库零写入：本票全部跑动都由 `run-pages.mjs` 带 `SKILLS_DB_PATH=.scratch/m158v2/t535/tmpdb`，没有跑过一次不带该变量的 CLI；
  仓内只找到 `.tmp-t123logs/`、`.tmp-t86-evidence/` 两处 2026-09-09 的历史夹具，本票未碰。

## 5. 偏离与遗留

- **锁目录偏离**：本席的探针命令一律用 `--lock-dir .scratch/locks-m158v2`（编排者许可：持锁者是 vision 类命令时）。
- 走势图没有纵轴刻度（共享图表件不给刻度文本）：06/07 补了「窗口内最低／最高」卡外事实条给量级；
  围度侧由既有的 `最小／最大` 两件读数承担。
- 390 档读数卡是一行一张：`pageUiCss` 自己在 `@media (max-width:400px)` 把读数卡栅格定成 `minmax(0,1fr)`（≤640 才是两格），
  故本族按「减卡」收首屏（围度页六张 → 两张 ＋ 一件事实条），没有去改共享层的栅格决定。
- 围度页窄屏卡一次列全 13 项（老口径，`#361` 保结构），7 条记录时整页仍长（3224px）；这是数据量而非版式债。
- `bodyDocs.ts` 里 `metricsOf` 是历史无用导入（本票未动，避免与未跟踪改动相互踩）。
- 皮褶分支在本波夹具（`run-pages.mjs` 的种子只给 `source`／`bodyFatPct`／`date`／`note`）里不渲染，
  故页级探针看不到它；该分支由 `#362` 的皮褶断言（7 行逐点 ＋ 缺槽位 `—`）与新增的 `t535` ⑥ 直接覆盖。

## 6. 自证截图（390×844，真 Chrome 渲染）

- `.scratch/m158v2/t535/06-390.png`（整页，1404px）：窗口条一处、三张数值卡、一行一条的记录小表（列名在表头一行）、
  图上「窗口内最低／最高」事实条。
- `.scratch/m158v2/t535/08-390-top.png`（首屏）：两张数值卡（`102 cm`／`7 个`）＋ 四件读数事实条（`101.79 cm` 等）＋
  页内导航两颗胶囊 ＋ 记录卡内每人一项「部位 ＋ 值」。
