# t288 摆正证据（目标域散件收进 `src/goal/`）

结论：三件全部入新家，旧路径零文件，引用一处不漏，tsc 干净，靶向测试无新增红，他域抽样 4/4 逐字节一致。

在途核查（开工前）：工作区脏件为 diet 域 6 件＋`triggers/help-lookup.ts`（他席）、stash 为 weight 域 6 件（他席）、
超前 `origin/master` 2 提交为 t413（记账域）、无后台任务、#251 已 CLOSED——目标路径无在途，判无在途开工。

命名说明：`fetch/nutritionGoal.ts`→`goal/nutritionGoal.ts`、`fetch/goalHistory.ts`→`goal/goalHistory.ts` 同名直搬（沿 f297a8a 纯搬迁先例）；
`render/goalPlate.ts`→`goal/goalPlates.ts` 取复数——`goal/goalPlate.ts` 之名已被 f297a8a 占给目标分析视图，
复数标“五视图盘”以别单数的分析视图，Plate＝盘与同目录 `goalPlate`／`goalExtraPlate` 同映射；调用面（函数名、签名、barrel 出口名）一字未动。

## 表一 · 移入新家（纯搬迁，逻辑一字未动）

| 移入前路径 | 移入后路径 | 行数 | 说明 |
|---|---|---|---|
| `src/fetch/nutritionGoal.ts` | `src/goal/nutritionGoal.ts` | 240 | 仅改内部 `from './errors.js'`→`from '../fetch/errors.js'` |
| `src/fetch/goalHistory.ts` | `src/goal/goalHistory.ts` | 74 | 仅改内部 `from './errors.js'`→`from '../fetch/errors.js'` |
| `src/render/goalPlate.ts` | `src/goal/goalPlates.ts` | 165 | 同目录引用改 `./` 前缀；`./errors.js`→`../render/errors.js` |

票面另列的 `fetch/goal.ts`、`render/goal.ts`、`render/goalExtra.ts` 在本票开工前已由 f297a8a（#324）搬走
（→`goal/goalStore.ts`、`goal/goalPlate.ts`、`goal/goalExtraPlate.ts`），本次不再重复搬。
三件新家文件均在 350 告警线内（240／74／165），不新增台账行。

## 表二 · 调用方更新（只换导入路径，调用面不变）

| 调用方 | 改点 |
|---|---|
| `src/goal/write.ts` | `../fetch/nutritionGoal.js`→`./nutritionGoal.js`；注文 `fetch/nutritionGoal.ts`→`goal/nutritionGoal.ts` |
| `src/goal/set.ts` | 3 行 `../fetch/nutritionGoal.js`→`./nutritionGoal.js` |
| `src/goal/receipt.ts` | 2 行 `../fetch/nutritionGoal.js`→`./nutritionGoal.js` |
| `src/goal/goalPlate.ts` | 4 行改 `./` 前缀；注文 `goalPlate.buildGoalProgress`→`goalPlates.buildGoalProgress` |
| `src/goal/goalExtraPlate.ts` | 3 行改 `./` 前缀；注文 `goalPlate（…）`→`goalPlates（…）`、`fetch/goalHistory.`→`goal/goalHistory.` |
| `src/goal/read.ts` | `../render/goalPlate.js`→`./goalPlates.js`；注文两处 `render/goalPlate.ts::`→`goal/goalPlates.ts::`，件头清单按实况改目标目录三件 |
| `src/goal/goalWeightDoc.ts` | type 行→`./goalPlates.js`；注文 `render/goalPlate.ts::`→`goal/goalPlates.ts::` |
| `src/goal/goalPlates.ts`（搬迁件自身） | C1 先改 `../fetch/*`→`../goal/*`，C2 再改 `../goal/*`→`./*` |
| `src/analysis/historyStore.ts` | `../fetch/nutritionGoal.js`→`../goal/nutritionGoal.js` |
| `src/home/today.ts` | `../render/goalPlate.js`→`../goal/goalPlates.js`、`../fetch/nutritionGoal.js`→`../goal/nutritionGoal.js` |
| `src/home/home.ts` | 2 行→`../goal/nutritionGoal.js`／`../goal/goalHistory.js` |
| `src/home/goalProgressDocs.ts` | type 两行→`../goal/goalHistory.js`／`../goal/goalPlates.js` |
| `src/profile/view.ts` | 2 行→`../goal/nutritionGoal.js`；注文 `fetch/nutritionGoal.`→`goal/nutritionGoal.` |
| `src/render/html.ts` | type 行→`../goal/goalPlates.js`（目标片段函数本身只出台账不删，见表三） |
| `src/render/index.ts` | 五盘转出源→`../goal/goalPlates.js`，对外名不变（沿本件 #370／#294／#332 先例，加 `#288` 注文行） |
| `test/fetch-t4.test.mjs` | `dist/fetch/goalHistory.js`→`dist/goal/goalHistory.js`、`dist/fetch/nutritionGoal.js`→`dist/goal/nutritionGoal.js` |
| `test/goal-upsert-127.test.mjs` | 注文 `fetch/nutritionGoal.ts`→`goal/nutritionGoal.ts`（行号 68-89 不变） |
| `test/goal-weight-deadline-548.test.mjs` | 注文 `src/fetch/nutritionGoal.ts:68-89`→`src/goal/nutritionGoal.ts:68-89` |
| `test/m5-receipt-97.test.mjs` | 注文 `fetch/nutritionGoal.ts:74-77`→`goal/nutritionGoal.ts:74-77` |

票面点名的 `cli/write.ts`、`cli/cmd_read.ts`、`render/trendDocs.ts`、`fetch/history.ts`、`render/home.ts`
经实测无旧路径引用（目标读写经 `goal/read.ts`／`goal/write.ts` 门面，CLI 不直引；`render/home.ts` 已不存在，对应 `home/today.ts` 已改）。
零转发壳：原路径无残留文件、无转发件。

## 表三 · `render/html.ts` 目标片段台账（本票只出台账不删）

| 函数 | 类型来源（已同步新家） | 现调用方 |
|---|---|---|
| `renderGoalHtml` | `../goal/goalPlate.js`（早已在新家） | `render/index.ts` 转出；`test/render-t8` |
| `renderGoalConfigHtml` | `../goal/goalPlates.ts` | `goal/read.ts:54`、`render/index.ts` 转出；`test/render-t9` |
| `renderGoalRecommendHtml` | `../goal/goalPlates.ts` | `goal/read.ts:68`、`render/index.ts` 转出；`test/render-t9` |
| `renderGoalWeightHtml` | `../goal/goalPlates.ts` | `render/index.ts` 转出；`test/render-t9`（src 内正链已切 `goalWeightDoc`，旧片段保留） |
| `renderGoalProgressHtml` | `../goal/goalPlates.ts` | `render/index.ts` 转出；`test/render-t9`（`home/today.ts` 已切整页 `buildGoalProgressDoc`，旧片段保留） |
| `renderGoalStatusHtml` | `../goal/goalPlates.ts` | `goal/read.ts:75`、`render/index.ts` 转出；`test/render-t9` |
| `renderGoalExpiringHtml` | `../goal/goalExtraPlate.ts`（早已在新家） | `render/index.ts` 转出；`test/render-t41` |
| `renderGoalPredictHtml` | `../goal/goalExtraPlate.ts`（早已在新家） | `render/index.ts` 转出；`test/render-t41` |
| `renderGoalVsActualHtml` | `../goal/goalExtraPlate.ts`（早已在新家） | `render/index.ts` 转出；`test/render-t41` |

删除归页面票，本票一行未删。

## 机器证据

- 提交：C1 `2bf2ac8`（fetch 双件＋15 引用方）、C2 `1f50d27`（render 盘＋6 引用方）、C3（本证据＋3 测试注文，待填）。
- 编译：`node node_modules/typescript/bin/tsc -b packages/skill-calorie` exit 0（C1 runId `27a77bed`、C2 runId `3e063401`、基线 runId `67476e6e`）。
- 靶向测试：基线 129 pass／3 fail（既有红：cmd-read-t11 当日标题、`fetch-t4 plan 校验 weeks 为空`、`goal-result-254 avgDeficit 1555≠1549`）；
  C1 后 20／1（仅既有 plan 红）；C2 后 41／2（两处与基线逐字同值，无新增红）。
- grep：`src/fetch/goal*`、`src/fetch/nutrition*`、`src/render/goal*` 零文件命中；
  `fetch/(nutritionGoal|goalHistory)`、`render/goalPlate` 在 `src/` 零引用残留（`test/` 仅余三处已同步的历史注文）。
- 他域抽样（`.scratch/t288/sample.mjs`：饮食总览页＋档案页＋主页＋主页 envelope，固定种子）：搬迁前后 sha256 4/4 一致。
- 变异自证：`goal/goalHistory.ts:64` 判定带 `80`→`800` 改坏一行，`fetch-t4 goalHistory` 单测变红
  pass 0／fail 1（runId `52b37853`，`['未完成','未完成','无记录']≠['完成','未完成','无记录']`）；
  逐行还原后复绿 pass 1／fail 0（runId `d3d6575c`），工作区还原干净。
- 告警线门：基线已红 4 行台账陈化（`trendPredictDocs` 939→1006、`nutritionPortDocs` 734→741、
  `rankingDocs` 359→364、`reviewDocs` 351→355，均为他票在途件；按派单只记录不上报不同步台账）。
  本票新家三件 240／74／165 均在线内，不新增台账行；改动不增减任何件行数。

## 第四步超线报警

已超线，需要根据规则进行重构。本票仅改导入行（行数不变）的两件超线件：
`src/home/goalProgressDocs.ts`（台账 446：四卡＋走势图量程判定＋达标表＋复制区同处一件）、
`src/render/html.ts`（台账 648：多套 HTML 串模板同处一件）。
本次先不拆：拆分不在 #288 写集（本票只许纯搬迁＋换导入路径），拆法沿台账既有结论（前者按页族切姊妹件，后者按页族切姊妹件），待收口票认领。
其余所碰件均在 350 线内。

## 未做项

- `render/html.ts` 9 个目标片段函数的删除（归属页面票，本票只出台账）。
- `goal/` 能力门（`index.ts` 仅 `GOAL_COMMANDS`／`runGoalView`／`runGoalWrite`）是否收编五盘 builders——
  调用方沿 f297a8a“搬迁只换住处、调用面不变”先例走深路径直引（铁律五判据记搬迁债务，不算新违规），收编待后续票裁定。
