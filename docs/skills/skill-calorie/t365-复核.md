# t365 · 身体细节七条写词的结果型整页 —— 独立复核（MAP #158 子票 #365）

- 复核对象：`1d352bf`（源码三件）／`6cc3be9`（测试＋证据＋changeset）／`53bf23b`／`c37eaca`（跨票旧针两件）／
  `99775fa`（证据补记），五笔均已 push。复核当刻 `HEAD=6612352`（其他席其后又提交两笔），
  **本票六件路径在 HEAD 上逐字节一致**（`git status --porcelain` 对六件为空；`git diff 99775fa HEAD -- <六件>` 空）。
- 独立复跑：复核席自建现场 `.scratch/t365-review/`（**禁复用 `.scratch/t365/`**，收尾即清），
  自建 tmp 库、自选参数与语料，期望值全部自立（手写种子 ＋ 另开 `node:sqlite` 句柄查库 ＋ `PRAGMA table_info` ＋ 老正本逐行）。
- 判定：**FAIL**。票面原始验收四条（六项读数／字段值 == 查库值／防回显／端口退回片段必红）**全绿**；
  融合基准 §二 序 2 与序 3 各有一半未落地（§四 F1／F2），另有一条注释与实况不符（F3）。

## 一、提交范围（逐个 `git show --name-only` 核过）

| 提交 | 件 |
|---|---|
| `1d352bf` | `src/body/receipt.ts`（**新建**）／`src/body/index.ts`／`src/cli/write.ts` —— 源码**只此三件** |
| `6cc3be9` | `test/t365-七条写词整页.test.mjs`（新）／`docs/skills/skill-calorie/t365-整页证据.md`（新）／`.changeset/t365-body-receipt.md`（新） |
| `53bf23b`／`99775fa` | 证据件追加 |
| `c37eaca` | 证据件追加 ＋ **两条跨票旧针**（`test/profile-doc-179.test.mjs`／`test/t364-删除取快照.test.mjs`） |

**0 命中**（五笔合计，逐条点数）：`src/triggers/**`、`src/body/{bodyDocs,bodyPlate,view,log,remove,routes}.ts`、
`src/shared/writeParts.ts`、`src/shared/copyArea.ts`、`scripts/check-warning-line.mjs` —— 各 `0`。
**中文无乱码**：八件（三源码 ＋ 三测试 ＋ 证据 ＋ changeset）逐件 UTF-8 解码后扫描 `U+FFFD`／典型 GBK 串 → 全部 `False`。

## 二、独立复跑机器证据（全部持锁，`tooling/run-locked.mjs --ticket 365`）

### 2.1 七条写词真出口（自建 tmp 库 `mkdtemp`，自选参数）

语料：皮褶钳 `home_caliper`（7 点＋sex/age）／外部测量 `hospital 24.75`／记围度 `82.5·96.25`／
补记体脂（撞种子日 `2026-08-01`）／补记围度（撞种子日 `2026-08-02`）／删体脂（种子 id）／删围度（种子 id）。

```
REV365-READING 记体脂（皮褶钳） exit=0 doctype=true charset=true css=36523B ext=0 pathOk=true 格=11 op=create 值槽=old 徽章=ilife-status-badge-ok/新增
REV365-READING 记体脂（外部测量） exit=0 doctype=true charset=true css=36523B ext=0 pathOk=true 格=11 op=create 值槽=old 徽章=ilife-status-badge-ok/新增
REV365-READING 记围度        exit=0 doctype=true charset=true css=36523B ext=0 pathOk=true 格=15 op=create 值槽=old 徽章=ilife-status-badge-ok/新增
REV365-READING 补记体脂      exit=0 doctype=true charset=true css=36523B ext=0 pathOk=true 格=11 op=create 值槽=old 徽章=ilife-status-badge-ok/新增
REV365-READING 补记围度      exit=0 doctype=true charset=true css=36523B ext=0 pathOk=true 格=15 op=create 值槽=old 徽章=ilife-status-badge-ok/新增
REV365-READING 删体脂        exit=0 doctype=true charset=true css=36523B ext=0 pathOk=true 格=11 op=delete 值槽=old 徽章=ilife-status-badge-danger/删除
REV365-READING 删围度        exit=0 doctype=true charset=true css=36523B ext=0 pathOk=true 格=15 op=delete 值槽=old 徽章=ilife-status-badge-danger/删除
REV365-SIX n/七条六项读数全绿=7/7      REV365-FAILS 0      （probe exit=0）
```

**逐格 == 自建查库值**：七条各按 `receipt.recordId` 用**另开的 `node:sqlite` 句柄**读整行，
页内 `[标签, 值]` 与库值逐格比（来源列取 `SOURCE_LABELS` 中文名）：标签序逐位相等、格数 11／11／15／11／15／11／15、
值逐字相等 —— `REV365-FAILS 0`。库列读数（**不作判据**）：`body_composition` 另有三列内部列
`age／sex／calculated_at`，#364 的 `items[0].detail` 与页面都不含它们（两处同一字段集），非缺陷。

### 2.2 防回显（跑写命令**之前**不经命令直改库）

```
REV365-ANTIECHO 删体脂体脂率=44.25 备注=改库甲 同日已有腰围=101.5 新写腰围在页=true
```
（种子是 `20.4`／`种子甲`／`78.5`；库内直改成 `44.25`／`改库甲`／`101.5` 后，删前快照与「同日已有记录」
两块都跟库真值走，页面不再出现种子旧值 ⇒ 不是参数回显。）

### 2.3 负向 M1：把 `cli/write.ts` 链上 body 那一口摘掉

```
改坏 M1   七条 doctype=false charset=false css=0B ext=0 格=-1   REV365-SIX=0/7   REV365-FAILS 96   exit=1
还原      七条 doctype=true  charset=true  css=36523B ext=0 格=11/15          REV365-SIX=7/7   REV365-FAILS 0    exit=0
```
还原后 `src/cli/write.ts` sha256 前 16 位回到 `dd11577aa861e55e`（与改坏前、与作者证据 §五同值）。
**隔离读数**（M1 态跑 179 件）：红 3 条＝`#239`／`#337`（他票，见 §五）＋ 本票新针
`#365 身体细节四条写命令已是完整文档`（`AssertionError: 记体脂 缺 doctype`）⇒ 前两条与本票无因果。

### 2.4 自设变异（复核席自己选的三处，改坏必红／还原必绿）

| 变异 | 改坏读数 | 还原读数 |
|---|---|---|
| **M2** `zhLabel` 换成第二份名表（`waist_cm→腰围X`） | `REV365-FAILS 4`／exit=1（标签序不符 2 条＋载荷／防回显连带） | `REV365-FAILS 0`／exit=0 |
| **M3** `payloadLines` 的缺项按可见文本写 `—` | `REV365-FAILS 20`／exit=1（七条载荷出现 `—`＋缺项未整位省） | `REV365-FAILS 0`／exit=0 |
| **M4** 把 create 的现值改落**新值槽**（老正本 `:362-367`） | `REV365-SLOT-VIOLATIONS 0`（7 条全 OK）；我的六项／逐格探针 `FAILS 0`；**作者自己的 t365 反红 3 条**（见 §四 F1 连带） | 还原后 `VIOLATIONS 5`、作者 t365 回 10/10 |

三处变异均**持锁期做**，释放前逐文件点名复原：`mut.mjs sha` 每次复原后打印
`write.ts=dd11577aa861e55e receipt.ts=9c79cb0f8041589e`，与 `backup/` 同值；
收尾 `git status` 对本票六件为空。（`copyFileSync` 会连原 mtime 一起搬回来 ⇒
`tsc -b` 会跳过重建；复核席复原后显式推 mtime 并核对 `dist` 自证：
`DIST-RESTORED 链上body口=True`／`DIST-FINAL 链上body口=True`。）

### 2.5 老正本四项 ＋ 新保留三项

| 项 | 老正本 | 读数 |
|---|---|---|
| ① id 卡三态 | `:26-29`／`:201` | 产物徽章 `ilife-status-badge-ok/新增`（create）／`ilife-status-badge-danger/删除`（delete），由 `receipt.op` 驱动；`update→更新` 在本域**走不到**（四条写命令只有 create／delete）。**图标未落地 —— 见 §四 F2** |
| ② diff 三列＋中文标签 | `:61-69`／`:276-291`／`:315-322` | 行结构 `label＋old＋arrow(visibility:hidden)＋new` 三段齐（见 `REV365-ROWRAW`）；中文标签逐位等于唯一来源（13 部位 `MEASUREMENT_ZH`／7 点 `CALIPER_SITE_LABELS`／来源 `SOURCE_LABELS`／日期／备注）。**值槽口径见 §四 F1** |
| ③ 删除前快照 | `:328-350`（`:340-347` 只留旧值、`:344` 箭位 `visibility:hidden`） | 删体脂 11 格／删围度 15 格**逐格 == 查库值**；箭位占位读数 `11/11`、`15/15` OK（不塌栏）。数据面确由 #364 供（字段与序取 `items[0].detail`，值读库内现值） |
| ④ 撤销按钮 | `:414-429` | 真出口七件产物**都没有「撤销」二字**（`REV365-UNDO … 含撤销=false`）；直接调端口注入撤销指令 ⇒ 出可复制块、按钮 `data-action-id` 非空、载荷含撤销指令，页面**内联冻结复制运行时**（点一下出 toast「已复制／粘贴给 AI」或「复制失败／长按选择文本手动复制」）⇒ 不是死按钮 |
| M5 自证进整页 | `render/receipt.ts:82-134` | 七件产物可见文本含 `影响行数来源／sqlite:total_changes／记录号来源／record／回执契约版本／v1`，且页上影响行数 == 回执读数 |
| 复制区接 `copyArea.ts:116` | `:41` 空态不出按钮 | `REV365-COPYAREA 空态按钮数=0 三格式菜单项=3 复制日志按钮=true` |
| **裁定 2**（两条分开） | 可见 `—` ／ 载荷保留原始空值 | `REV365-DASH 缺值格=8/11`（7 点＋备注逐格 `—`，不留空串）；`REV365-PAYLOAD 载荷数=4 含—=false 腰围=true 胸围标签出现=false`（缺项连标签一起省） |

失败分支（基准 §二 序 16／§七「保持」）读数：缺数据 `exit=4`／坏参数 `exit=2` 只给 stderr（不出页），
渲染失败 `exit=5` 出 `RECEIPT {…}` 失败回执载荷 —— 本票三件源码对该支 **0 命中**，属仓内既有口径（§四 范围外）。

### 2.6 结构／真库／只读目录自核

```
SELF src/body/receipt.ts LF=324 sha256=9C79CB0F8041589E      （＜350，未触发超线第四步）
SELF src/cli/write.ts    LF=98  sha256=DD11577AA861E55E      （只加了 body 那一口：两处 hunk ＝ 件头条数注释＋链上第五口）
SELF src/body/index.ts   LF=39  sha256=28168A60AAC12D04
REALDB D:\2Study\StudyNotes\SKILLS\卡路里\calorie_data.db size=0 mtime=2026-07-29T08:40:43 跑前跑后 sha256 同值 E3B0C442…（零写入）
OLDTEMPLATE crud_receipt.html size=26279 mtime=2026-08-13T20:27:54 sha256=54B9A6974095B50F（只读，未写）
```
写命令登记形状：`REV365-SHAPE body写命令=4 形状=receipt 非本域键返null=true`（注册表 `body/commands.ts` 为权威源）。

## 三、两条跨票旧针的改法（结论：**合规**）

1. `test/profile-doc-179.test.mjs`（`c37eaca`，hunk 仅 `:5-8`／`:265-276`／`:353+`）：
   只从「仍是片段」抽样里删掉 `['calorie.body.measure-add', …]` **一行**，抽样循环体（逐字钉片段形状）**一字未动**；
   另加末条新针钉身体族四条命令的整页六项读数（doctype／charset／内联样式 ≥2KB／无外链／回执路径可读／
   不再落在旧片段标记上）＋场景 08 眉标＋`change-row` 逐格行＋对账折叠区＋复制日志按钮。
   件头（`:8-10`）与抽样注释（`:269-273`）两处写明「#365 口径变更（有意改，票面与提交信息写清）」。
   **未夹带别的口径**：全文件其余断言逐字未动（`git show` 三处 hunk 之外无改动）。
2. `test/t364-删除取快照.test.mjs`（同笔，hunk 仅 `:16-27`／`:169-208`）：
   `detailFromPage()` 由抓片段 `<li> … · 日期 …` 改为按口径行 `删除前的原值` 切段后读 `renderChangeRows`
   的 `change-row`（label／old 两栏 ＋「第一格应是日期」锚），产出仍是「`标签 值` 以「、」连」的同一串
   ⇒ 下游 `pairsOf(detailFromPage(html))` 与 `pairsOf(items[0].detail)` 的 deepEqual、逐格比、软删语义、
   接通面**全部未动**：**t364 7/7 exit=0**（含不读落盘页的另 5 条）。件头与函数注释写明「#365 口径变更（有意改）」。

## 四、缺陷清单

**本票引入／本票范围**

- **F1（S2）create 视图的现值落在「改前」槽，被画成删除线（5/7 条写词）。**
  产物自带 CSS 读数：`.ilife-block-change-row-old {color: var(--fg2); text-decoration: line-through;}`
  （从**产物本身**读出，非引仓内测试）；`REV365-SLOT` 读数 5 条 `值槽=old 期望=new → VIOLATION`
  （记体脂×2／记围度／补记体脂／补记围度），删类 2 条 `值槽=old 期望=old → OK`。
  判据：老正本 `:351-372` 的 create 分支把值写进 `.diff-new`（`:364` `text-decoration:none`），
  融合基准 §二 序 2 与 §六-C 行 2 明写「旧值（红删除线）／**新值（绿）**三列」；
  `src/body/receipt.ts` 件头却以 `:315-322`（改模式装配）自述落点 —— 该分支**实际未落地**。
  反向自证：M4 把 create 现值改落新值槽后 `REV365-SLOT-VIOLATIONS 0`、我的六项／逐格探针 `FAILS 0`、
  删类两侧不受影响 ⇒ 改法＝create 那一支改落新值槽。
  **连带**：作者自己的 `test/t365-七条写词整页.test.mjs` 把现值**钉在旧槽**（`sectionRows()` 只读 `-old`），
  M4 态下该件 10 条红 3（`页内=，查库=2026-09-10`／裁定 2 缺项 `[]`）⇒ 收口这条要同改该测试的取值槽。
- **F2（S3）「图标」未落地。** 基准 §二 序 3／§六-C 行 1 写「三态配色**与图标**」；产物徽章只有文字
  （`<span class="ilife-status-badge ilife-status-badge-ok">新增</span>`），老正本 `:194` 的
  `opIcons`（`✓`／`✎`／`✕`）无对位物。
- **F3（S3）件头注释与实况不符。** `src/body/receipt.ts:19-21` 把老正本 `:337`「空值不摆」写成本件落地点，
  实况是删前快照**逐格摆空值为 `—`**（#364 的 `items[0].detail` 每格给值）。行为合基准（§二 序 1「逐字段铺」＋裁定 2），
  该改的是注释。

**范围外（只 S3 ＋ 转票措辞）**

- `src/weight/compare.ts` 台账陈化（台账 632／实况 630）⇒ 告警线门 `RESULT: 59/60` exit=1；
  本票一个台账行没改、`src/body/receipt.ts` 未挂号（LF 324＜350）。**转票**：该件归属席跑一次
  `node packages/skill-calorie/scripts/check-warning-line.mjs --sync` 即回绿。
- 失败分支口径：基准 §二 序 16 要求「缺数据／缺参数一律走失败回执整页」，本域实测 `exit=4`／`exit=2` 只给 stderr。
  **转票**：要落这句话得改 `cli/readArgs.ts`／`cli/delivery.ts` 的失败面口径，与本票写集无关（本票 0 命中）。
- `#239`（预检确认页多一颗 `ilife-copy-log`，已开 **#494**）／`#337`（体重「批量计数」表被 `473dd15 feat(483)`
  删除后旧针未收窄，已开 **#495**）／`#247`／`#269`／`体重记改删批量 C5 #43`：均为他席在途或他票旧针，**与本票无因果**。

## 五、他人红归属（第 8 条，要读数）

`node --test packages/skill-calorie/test/profile-doc-179.test.mjs` 本席测到的是**一个随时间漂移的红集**，
同一 #365 树、六件字节不变的前提下：

| 时点（runId） | 读数 | 红件 |
|---|---|---|
| `730edda3` / `2626d05d` / `c7d9721d` | `pass 8 fail 2` exit=1 | `#239`（`:192`）／`#337`（`:339`） |
| `b573f78e` | `pass 7 fail 3` | ＋ `#247`（预检确认页菜单缺用途提示） |
| `9a601cc8` | `pass 6 fail 4` | ＋ `#269`（饮食 `water.log` 缺改动分项表） |

**两条点名红与本票无关的机器依据**：① 断言文本与 HEAD **逐字相同**，且**不在本票任何 hunk 内**
（`c37eaca` 对 179 件的 hunk 只有 `:5-8`／`:265-276`／`:353+`；两条红在 `:192`／`:339`）；
② **M1 隔离**（摘掉 body 那一口）下两条**照旧红**，只有本票新针由绿转红 ⇒ 因果不在本票；
③ 失败页都不是身体域产物（预检确认页／体重页／饮食页），本席身体七件的三格式菜单＋用途提示、
   复制日志按钮读数全绿。漂移的**近因是他席在途**：工作区现存 `M src/shared/copyArea.ts`（把三格式菜单
   用途提示改留空，逐字注释在件内）与 `M src/shared/docPage.ts` —— 同一时间窗内 `#247` 由绿转红即此故。

## 六、下一手缺什么

- **F1 不收口则本票不算完**：给 create 那一支改落新值槽（`after:`），同改 `test/t365-*` 的 `sectionRows()`
  取值槽；改完复跑本席两条探针（`REV365-SLOT-VIOLATIONS 0` ＋ `REV365-FAILS 0`）。建议由原席回炉，
  本席只出读数（红线：禁改实现／测试）。
- **F2／F3** 建议同批做掉（各一行改动量级）。
- **#366 可开条件**：本票四条写命令的回执整页已就位，且复制区三格式菜单与「复制 prompt」通路实测可用 ⇒
  #366（复制与执行衔接）**数据面已具备**；仍卡在其票面自己写明的前置 **#397**（`ParamFieldInput`
  只有 `name／label／value／hint／required`，`readonly`／`step` 落不下去）。**#366 可在 #397 落地后开**；
  若只想先做「复制区与预检页」那一半，可与本票的 F1 收口同批（同域同页）。
- 本席未做（红线）：未跑 `pnpm gen`／`--filter build`／`--sync`／`--allow-*`，未碰真库与老技能只读目录，
  未动 `.scratch/t365/`；编译只 `npx tsc -b packages/skill-calorie`。现场 `.scratch/t365-review/` 收尾即清。

## 七、运行条目导出（协议 §2.3-4；`runId` 可在 `.scratch/locks/gate-runs.log` 复核）

```
730edda3-c229-47a6-8f75-2768dc6ea2f9  0      0  2026-09-15T00:15:55.080Z  pwsh -NoProfile -File .scratch/t365-review/run-a.ps1   （基线：编译＋七条探针＋三件测试＋告警线门）
174091bf-27c7-4c00-b5b5-c8fe19ab2131  0      0  2026-09-15T00:18:02.661Z  pwsh -NoProfile -File .scratch/t365-review/run-b.ps1   （负向 M1 ＋ 179 隔离）
2626d05d-2147-448a-9cd7-b7167d0927e8  0      0  2026-09-15T00:19:24.576Z  pwsh -NoProfile -File .scratch/t365-review/run-c.ps1   （还原自核＋自设变异 M2／M3／M4）
c7d9721d-4148-4225-ad6e-710be5499cf1  0      0  2026-09-15T00:22:12.388Z  pwsh -NoProfile -File .scratch/t365-review/run-d.ps1   （干净 M4＋全部还原＋真库自核）
b573f78e-d089-457e-b015-98c40c1b96a4  45025  0  2026-09-15T00:24:58.048Z  pwsh -NoProfile -File .scratch/t365-review/run-e.ps1   （收口复跑＋写域回归 99 条）
9a601cc8-47fc-4315-a43a-ff189ee113e3  15005  0  2026-09-15T00:26:40.907Z  pwsh -NoProfile -File .scratch/t365-review/run-f.ps1   （失败分支读数＋末次复跑＋导出）
```
