# t289 写后回执三族字段补齐 · 证据（实施席·调查席写集内）

> 票 #289（地图 #153 子票）。本席只做调查席写集内的事：13 条写词逐条实跑回执整页，三项数值与老实物口径对账并写成可复跑断言形态。
> 红线：不新增写命令；不改 25 条唤醒词名字；沿用 #251 三处有意不同口径（`src/goal/set.ts:10-19`）。

## 1. 13 条清单（唤醒词 → 命令 → 本次实跑参数）

种子库：`docs/research/t81-seed.mjs seedFull()`，`CALORIE_TODAY=2026-09-07`。
种子画像：`user_profile 30/male/175/moderate`，最新体重 70.6kg（2026-09-07），`daily_goal#1` 1800/150/200/50/饮水2000/体重68/截止2026-12-31。

| # | 唤醒词 | 命令 | 实跑参数 |
|---|---|---|---|
| 1 | 定营养目标 | `calorie.goal.set` | 1800/150/200/50 |
| 2 | 改营养目标 | `calorie.goal.set` | 1900/140/210/55（同库第二写） |
| 3 | 定饮水目标 | `calorie.goal.water` | 2000 |
| 4 | 改饮水目标 | `calorie.goal.water` | 2400（同库第二写） |
| 5 | 定体重目标 | `calorie.goal.weight` | kg 68 |
| 6 | 定体重目标(自动算截止) | `calorie.goal.weight` | kg 66＋deadline 2026-12-31 |
| 7 | 定体重目标(含起始日) | `calorie.goal.weight` | kg 65＋startKg 72＋startDate 2026-09-01＋deadline 2026-12-31 |
| 8 | 改体重目标 | `calorie.goal.weight` | kg 64＋deadline 2027-01-31（同库第四写） |
| 9 | 定营养目标(自动算) | 推荐 `calorie.view.goal-wizard` → 采纳后 `calorie.goal.set` | profile cut |
| 10 | 定饮水目标(自动算) | 推荐 wizard → 采纳后 `calorie.goal.water` | 体重口径见 §3 |
| 11 | 一键定全套目标 | 推荐 wizard → 采纳后 set＋water＋weight 三写 | profile cut（不新增组合写命令） |
| 12 | 暂停所有目标 | `calorie.goal.pause` | {} |
| 13 | 重启所有目标 | `calorie.goal.resume` | {} |

## 2. 逐条实跑（回执整页 exit＋字节＋三族标记在场性）

探针：`.scratch/t289/probe13.mjs`（只读 dist CLI，写系统 tmp＋本票草稿盘，不改源码）。
跑法：`node tooling/run-locked.mjs --ticket 289 -- node .scratch/t289/probe13.mjs`，日志 `.scratch/t289/probe13-run.log`。
GATE-RUN：`runId=6cf6a6ec-9935-4d7b-8e07-2ad0a61a8f62 exit=0`（等锁 10009ms，#290 活锁先放行）。

```
定营养目标 exit=0 bytes=86676 doc=Y nutri=[宏量/自洽] water=[对照] weight=[起点]
改营养目标 exit=0 bytes=86676 doc=Y nutri=[宏量/自洽] water=[对照] weight=[起点]
定饮水目标 exit=0 bytes=85460 doc=Y nutri=[] water=[对照] weight=[起点]
改饮水目标 exit=0 bytes=85508 doc=Y nutri=[] water=[对照] weight=[起点]
定体重目标 exit=0 bytes=85560 doc=Y nutri=[] water=[对照] weight=[起点]
定体重目标(自动算截止) exit=0 bytes=85964 doc=Y nutri=[] water=[对照] weight=[起点]
定体重目标(含起始日) exit=0 bytes=86637 doc=Y nutri=[] water=[对照] weight=[起点]
改体重目标 exit=0 bytes=86036 doc=Y nutri=[] water=[对照] weight=[起点]
定营养目标(自动算)·推荐 exit=0 bytes=76774 doc=Y（wizard 预检页，非写回执）
定饮水目标(自动算)·推荐 exit=0 bytes=76406 doc=Y（同上）
一键定全套目标·推荐 exit=0 bytes=76978 doc=Y（同上）
暂停所有目标 exit=0 bytes=85688 doc=Y
重启所有目标 exit=0 bytes=85626 doc=Y
```

结论：13 条全部 exit 0 且为完整文档（`<!doctype html>` 在场）。但**写回执页**三族判据缺位：
营养写回执仅摘要尾带 `宏量自洽/⚠换算差`，无 `BMR/基础代谢/换算式`区块；
饮水写回执仅 `对照`（通用对账 disclosure），无 `ml/kg/推荐/季节`对照；
体重写回执仅 `起点`（现值表行名），无 `建议速率/极端/kg/周/剩余天数`区块；
自动算三条的写回执无 `推荐值＋采纳写入值`两段对照（仅 wizard 预检页有）。**故 §5 判 RED。**

## 3. 三项数值对账（预期值＋出处 vs 页面实测）

固定夹具：体重 70.6kg／身高 175cm／年龄 30／男／moderate 1.55（`analysis/utils.ts:29-35`）。
`energyOf`：BMR=round(10×70.6＋6.25×175−5×30＋5)=round(1654.75)=1655；
TDEE=round(1654.75×1.55)=2565（`analysis/utils.ts:118-124`；显示口径与 `calcTdee` 一致）。

### 3.1 宏量合计（营养族）

算式出处：老 `nutrition_goal.py:84-85`（手工 `diff=calculated−calorie`，`|diff|≤50` 合规；
`:111-112` 合规才打印自洽句）、`:221-222,:231-232`（推荐路超 50 追加警告＋`self_check`）。
新仓同式：`goal/nutritionGoal.ts:67`，`goal/set.ts:138-139,150-152`（顺序：先 cal→蛋白→脂肪→碳水余量，全 `trunc`）。

| 向量 | 预期 | 出处 | 写回执实测 |
|---|---|---|---|
| cut（70.6kg） | cal 2064，蛋白 141，脂肪 57，碳水 246，饮水 2471；合计 2061，diff −3，一致 | `nutrition_goal.py:213-218` 模板 cut(−500/2.0/35/0.25) | RED：摘要尾只有 `宏量自洽` 四字，无合计 2061／diff −3／公式行 |
| 探针手填 1800/150/200/50 | 合计 1850，diff ＋50，边界一致（≤50） | `:84-85` | RED：同上，无公式行 |
| 反例 1800/100/100/100 | 合计 400＋400＋900=1700，diff −100，不一致 | `:231-232` | RED：摘要尾应出 `⚠宏量换算差 −100 卡`，但无独立对账区块 |

### 3.2 缺口影响（体重速率族）

算式出处：老 `weight_goal.py:116-118`（`gap=current−goal`，`required_daily=gap/days_left`，
`calorie_adjustment=int(required_daily×7700)`，常量 7700 见 `:8`）；
安全线 `±1000` 严格大于才警告（`:158-167`，1000 本身不警告，`int` 截断后判定）；
kg/周口径见 `render_goal_weight build_rate_info`（`t251-老技能体重与状态页.md:143-146`）：
`per_week=|gap|/days×7`，`per_month=per_week×52/12`，`per_week≥1.0` 极端，健康带 0.25–1.0。
新仓同式：`goal/set.ts:112-120`（`daysLeft` 今天起算；无截止即 null，不编 90 天——#251 有意不同③）。

| 向量（今天 2026-09-07） | 预期 | 出处 | 写回执实测 |
|---|---|---|---|
| 70.6→68，截止 2026-12-31（115 天） | gap 2.6，daily ＋174，per_week 0.16（偏低带外），不极端 | `weight_goal.py:115-118`＋体重页 `:143-146` | RED：无 `剩余天数/建议速率/公式` 行 |
| 70.6→60，截止 2026-10-01（24 天） | gap 10.6，daily ＋3400（>1000 极端），per_week 3.09（≥1.0 极端） | `:158`＋`:143-146` | RED：无极端红条（老文案 `每日需增加 … 缺口（极端目标…）`） |
| 无截止 | days null，速率 null，不编 90 天 | `set.ts:17-19`（有意不同③） | RED：无 `未设置截止日期，无法计算建议速率` 固定句（老回执页口径 `t251-体重页:233`） |

### 3.3 饮水推荐对照＋BMR 低值提示

算式出处：饮水 `recommend_water_goal`（`nutrition_goal.py:252-271`）：
体重显式→最新记录→70.0；季节显式→否则 6/7/8/9 月为夏；`35 if 夏 else 30`；`int(w×ml/kg)`。
9 月＝夏：70.6×35=2471；冬＝70.6×30=2118。只改饮水旧值进 `old_water_goal`（`:288-329`）。
BMR 提示出处：`t251-老技能写前页.md:1.1-1.2`（`bmr_warning{bmr,calorie_goal,below,text}`；
热量≥BMR 转绿 `BMR 安全确认`）。缺项口径沿 #251：`energyOf` 缺一即 null，页写 `缺×，不算`（有意不同①②）。

| 向量 | 预期 | 写回执实测 |
|---|---|---|
| 种子 70.6kg，9 月 | 推荐 2471ml（70.6×35，夏季偏高）；改 2000→2400 时对照 `旧 2000→新 2400 vs 推荐 2471` | RED：仅通用 `2000→2400` 对子，无 `ml/kg/季节/推荐` 行 |
| 热量 1200 vs BMR 1655 | `below=true`，出低值提示 | RED：写回执无 BMR 区块 |
| 热量 1800 vs BMR 1655 | 安全确认 | RED：同上 |

## 4. 可复跑断言（本席未落盘新测试件——跨界，需下一手认领）

以下断言已在探针与对账中定稿，直接可入 `packages/skill-calorie/test/goal-receipt-289.test.mjs`（新建件，**不在本席写集**）：

1. 营养写回执含 `宏量换算 {calculated} 卡` 且 `diff {±Y} 卡`，`|diff|>50` 时出 `建议复核`；公式 `蛋白×4＋碳水×4＋脂肪×9` 逐值对 §3.1。
2. 热量 `<` BMR（`energyOf` 出数时）出低值提示，`≥` 出安全确认；缺项时出 `缺×，不算` 且不出数字。
3. 饮水写回执含 `体重 {w} kg × {35|30} ml/kg`＋季节＋推荐值，改类含 `旧→新 vs 推荐` 三段。
4. 体重写回执含 `目标/起点体重/起始日/截止日/剩余天数/建议速率/公式`；`per_week≥1.0` 或 `|daily|>1000` 出极端红条；无截止出固定句；`formula` 形如 `<gap:.1f>kg ÷ <days>天 × 7 = <per_week:.2f> kg/周`。
5. 自动算三条：推荐值段（profile/bmr/tdee/weekly_rate/plan_reasons/missing/self_check）＋采纳后写入值段两段对照；写入值逐格读库。
6. 其它域页面逐字节不变：除 `src/goal/receipt.ts`（＋其唯一测试件）外，`git diff --name-only` 无新增件。

靶向基线（骨架未坏）：`goal-receipt-253.test.mjs` 5/5 绿（`.scratch/t289/t253-baseline.log`，
GATE-RUN `runId=a3d7a180-7c5b-48e0-be2b-5f01b68e4407 exit=0`）。

## 5. 判定与缺口

- 13 条回执整页：13/13 exit 0、全完整文档 —— 绿。
- 速率／宏量合计／缺口影响数值对账：预期值已按老文件行号算定（§3），但写回执页无对应区块 —— **红**。
- 其它域逐字节不变：本席零源码改动 —— 绿（工作区另有他席在途改动 `reportDoc.ts/trendPredictDocs.ts/t573*`，本席一律未碰）。
- 靶向测试：骨架 5/5 绿；§4 新断言件未建（跨界） —— 黄。

自证变异**未做**：源码级改坏一行＋还原需写 `src/goal/*`，超出本席派单声明路径（仅证据件），按纪律停手。持锁期内本席文件还原干净（无源码改动可还）。

下一手缺：请编排者把写集扩到 `packages/skill-calorie/src/goal/receipt.ts`（三族区块装配，吃 `set.ts GoalDraft/energyOf` 现成口径，不新增命令不改唤醒词）＋新建 `packages/skill-calorie/test/goal-receipt-289.test.mjs`（§4 六条）＋重跑 `tsc -b` 与靶向测试＋变异自证（改坏一行致红→还原绿，给两行读数）。
