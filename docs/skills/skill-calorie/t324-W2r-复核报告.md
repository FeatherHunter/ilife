# #324 · W2（场景 02 饮食）实现层搬迁 · 对抗式复核报告（试点窗）

复核对象：提交 `a39b86a`（`feat(324): W2 场景 02 饮食实现层六件搬进 src/diet/（试点窗，纯搬迁；生成物五件 sha256 不变）`）。
复核席：`#324 W2r`（对抗者口径：默认这窗有问题，去找出来）。
复核当刻：`HEAD = b06bbfd5c32ece1963773a4b16ae745b35c298e9`（`a39b86a` 之后 HEAD 已前进 3 次：`7228148`＝本窗证据件改正、`e9dc652`／`b06bbfd`＝他票）。

## 判定

- **判定：FAIL**（门槛「S1 = 0 且 S2 = 0」未过；S1 = 0、**S2 = 2**、S3 = 5）。
- **分数：86 / 100**。
- 一句话：**「搬」的那一半无可指摘——六件不多不少、逐字节纯搬迁、41 处引用改动逐字闭合、五件生成物与 SKILL.md 逐字节不变、1492／1492 与 t293 5/5 我都在锁内自跑复现；扣分全在「记账」那一半**：证据件**一条 `GATE-RUN` 声明都没有**（协议硬要求），且协议要的对账导出件不存在；另有 **11 处旧路径留在本窗自己改过的活源件注释里**，按派单口径「全仓 grep 旧路径归零」不成立。

## 逐维结论

### 维 1 · 搬的是不是恰好那 6 件 —— **PASS**

`git diff-tree -r -M --raw a39b86a^ a39b86a`：**rename 6 条、纯删除 0 条、新增只有证据件 1 件**。

| # | 提交里的 rename（相似度） | 方案 W2 hard 写集 | 件归属表 |
|---|---|---|---|
| 1 | `src/render/library.ts → src/diet/libraryPlate.ts`（R094） | 同 | 第 16 行 |
| 2 | `src/render/ranking.ts → src/diet/rankingPlate.ts`（R094） | 同 | 第 17 行 |
| 3 | `src/render/nutritionPort.ts → src/diet/nutritionPort.ts`（R099） | 同 | 第 18 行 |
| 4 | `src/render/nutritionPortDocs.ts → src/diet/nutritionPortDocs.ts`（R100） | 同 | 第 19 行 |
| 5 | `src/fetch/products.ts → src/diet/productStore.ts`（R099） | 同 | 第 20 行 |
| 6 | `src/analysis/diet.ts → src/diet/dietEngine.ts`（R098） | 同 | 第 21 行 |

- **多搬 0、少搬 0**（集合相等，不只条数相等）。
- 六件旧路 `src` 面当刻全不在盘；`git log a39b86a..HEAD -- <六件所在四个目录>` **为空** ⇒ 之后无人再动过本窗的件。
- 记账：票面 02 字面点名的 `render/diet.ts`／`render/dietDocs.ts`／`fetch/diet.ts` 三件**没搬**，依 `件归属表.md` 第 22–24 行判「真共用·留」。属计划层已裁定，不判缺陷（见 S3-3）。

### 维 2 · 引用点是否真同步干净 —— **可执行面 PASS；字面面 S2**

**可执行面（我自己的解析器，非信证据件）**：扫 726 个受跟踪代码件、**1841 条相对说明符**逐条真解析 ⇒ 解析不到 4 条，**全部与本窗无关**（`docs/research/t78-browser-evidence.mjs`、`docs/research/t99-review-red-probe.mjs` ×2、`packages/skill-bill/scripts/gen-wake-assets.mjs`，均为前存量）；**落点命中六件旧路的说明符 = 0 条**。仓内 4 个 `dist` 直引锚点（`test/fetch-t4`／`test/analysis-t5`／`test/softdelete-120`／`docs/research/t120-probe-softdelete.mjs`）逐条落点成立。

**证据数字复算**：§3 `ZERO-CHECK` 的「725 个源文件／3638 条说明符」我按同一扫描面独立重算 ⇒ **725 / 3638，逐字相同**。

**字面残留（`grep` 口径不归零 —— 见 S2-2）**：代码面 **11 处 / 9 件**，非代码面 22 处（史档 `.md` ＋ `t116-assets` 会话存档 `.json`）。其中 **10 处落在本窗自己改过或搬过的 8 个活源件里**：

```
src/diet/library.ts:4,5      src/diet/nutrition.ts:4      src/diet/products.ts:4
src/diet/ranking.ts:4        src/diet/review.ts:4         src/diet/today.ts:4,5
src/diet/nutritionPort.ts:13 src/diet/nutritionPortDocs.ts:18   ← 后两件＝被搬件自身的自我描述
src/cli/write.ts:21          ← 唯一一件本窗没碰过的（行号引用 fetch/products.ts:72,108,114,120）
```

**`dist` 面（被 ignore）**：六件旧编译产物**全在盘**（`dist/render/library.js` 等，mtime 2026-09-13 23:45:25）；它们自身含 4 条旧路 import 说明符 ＋ 1 条旧路注释（`dist/diet/nutritionPortDocs.js:18`）。**活 dist 件命中旧路 = 0** ⇒ 编译产物面自洽，测试跑的是新代码（另由变异 M-B′ 加固）。

### 维 3 · 纯搬迁（逻辑一行不许改）—— **PASS**

逐件 `git diff --no-index -U0`（`a39b86a^:<旧>` vs `a39b86a:<新>`）：

| 旧 → 新 | 差异行 | 判定 |
|---|---|---|
| `render/library.ts → diet/libraryPlate.ts` | 6（3 对） | 全是 import 说明符行 |
| `render/ranking.ts → diet/rankingPlate.ts` | 6（3 对） | 同上 |
| `render/nutritionPort.ts → diet/nutritionPort.ts` | 4（2 对） | 同上 |
| `render/nutritionPortDocs.ts → diet/nutritionPortDocs.ts` | 0 | **逐字节相同** |
| `fetch/products.ts → diet/productStore.ts` | 2（1 对） | 同上 |
| `analysis/diet.ts → diet/dietEngine.ts` | 6（3 对） | 同上 |

**非「import／export … from 说明符行」的差异 = 0** ⇒ 无一行逻辑、无一条断言、无一处输出被动过；件内改动合计 12 条说明符，与证据 §2 hard 表逐条一致（含 `nutritionPortDocs.ts` **零改** 的偏离裁定，我复核解析：新家里 `'./nutritionPort.js'` 正好落到同迁邻居，改反而指已删旧路 —— 该裁定成立）。

### 维 4 · 共享位是否定点删改 —— **PASS**

提交里**全部 82 个 `+/-` 行 ＝ 41 对**，逐行都是 `import`／`export … from` 说明符行：**非说明符改动 = 0** ⇒ `render/index.ts`（8 行）／`render/html.ts`（6）／`render/dietDocs.ts`（6）／`render/analysisPlate.ts`（4）／`fetch/batch.ts`（2）／`analysis/dashboard.ts`（2）全是**定点改路径**，没有整文件覆盖。

- **静默复活别票已删的东西：未发生**。没有任何 `+` 行引入新声明、新导出名或新 `case`；`render/index.ts` 的导出名单逐字不变（4 对改动只换来源路径）。
- **改集合闭合**：41 对 ↔ 证据 §2「35 条指令／次数列合计 41 处」**逐字闭合**；21 个文件 ↔ 提交里 16 个 `M` ＋ 5 个被搬件内改（`nutritionPortDocs.ts` 未改）**逐字闭合**。

### 维 5 · 判据④ 生成物五件 sha256 —— **PASS**（自己复算，不信证据件）

- **提交面**：`git rev-parse a39b86a^:<件>` 与 `a39b86a:<件>` **逐件同一个 blob** ⇒ 五件在本次提交里逐字节未变。
- **在盘面**（我自算 `SHA256`）与证据 §5／`GEN-CHECK` 读数**逐字相同**：

```
keys.ts              4db43569fbab400d5664d4ae86f8771c911f3d51351720a1e0182bbc7e96f4ea
registry.ts          002165f749ff653df6482bae2a4c4523281273be87055d29e62ee5e4d06e366c
combos.yaml          ca17d2c3f62ce7527d7747d216426a301c8dde6942f7e63732a965a54d466ec2
build-help.mjs       4d44f03f6ffb25d7e490ee8d69d39bda5773deb6ed6f9e8b76b7811454765ede
routes.generated.ts  e9ea993518ec33e3a69575dac1d8abf4a5e4c12e277b4c2d215ae3e5d339d7e9
```

- 锁内 `gen-cli.mjs --check` exit=0（逐件 `GEN-CHECK ok`）；`--stamp` 前后**印记件 `dist/.gen-inputs.json` sha256 不变**（比本窗自述的「印记件变了」更干净）。
- **口径备注**：派单括注把「五件」写成含 `SKILL.md`；仓内权威口径（`件归属表.md:222`／`窗口方案.md`）是上表五件。我**两套都核**：第六候选 `SKILL.md` = `df63151429d5e55c2273593f548e41d6f68271d3535bf4106adcc643cc5fa3f3`（与 `t319r` 席独立读数一致），**也不变** ⇒ 无论按哪套口径，判据④成立。

### 维 6 · 门禁 —— **PASS**（锁内自跑，直调 node 等价命令）

| 门 | 命令（规则 26 直调） | exit | 读数 |
|---|---|---|---|
| 基线编译 | `node node_modules/typescript/bin/tsc -b`（**不带 `--force`**） | 0 | — |
| 印记 | `node packages/skill-calorie/scripts/gen-cli.mjs --stamp` | 0 | GEN-STAMP ok 声明源 37 件 |
| 生成物 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | 0 | GEN-CHECK PASS：键 101／能力 8／声明 82 条／未搬迁 19 条 |
| 全量测试 | `node --test`（`package.json` 那 15 条 glob 原样） | 0 | **tests 1492／suites 160／pass 1492／fail 0／cancelled 0／skipped 0／todo 0** |
| 自治验收 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | 0 | **P1..P5 全 PASS、5/5；UNACCOUNTED = 0 处**；权威声明齐 9/9；未归类 0 |
| HTML 快照 | `node tooling/skill-html-snapshot.mjs --check` | 0 | **changed=0 added=0 removed=0** |

树空：锁内开门／收尾两次 `git status --porcelain -uall` **均为空**。以上六条读数出自同一持锁窗口（run1）。

**两次独立复读（规则 30）**：run4 与 run5 在同一棵树上复读同一条链，读数与 run1 **逐字相同** —— `tsc -b` exit=0、`gen:check` exit=0、**tests 1492／suites 160／pass 1492／fail 0／cancelled 0／skipped 0／todo 0**、`snapshot:html:check` exit=0（`artifacts=186 changed=0 added=0 removed=0`）；run4 的 t293 复读亦为 **`P1..P5 5/5`、`UNACCOUNTED=0 处`**。⇒ 1492／1492 与 t293 5/5 **各有两读、读数一致**，不是 flake；`boundaries` 也在 run4 记账跑到 `PASS`（非本票判据）。

### 维 7 · 变异自证 —— **PASS（3 次真变异变红 ＋ 2 条反假绿攻击复现）**

隔离副本（`git` 面外：`packages/skill-calorie` 全量复制 ＋ `tsc -b` 联结 `node_modules`，**主树零接触**，副本 src 的 `.ts` 数 214／214 与真树一致，副本基线先绿）。全部直调 `node`，不碰 `pnpm` 垫片。

| # | 变异 | 期望 | 实得 |
|---|---|---|---|
| **M-A** | `src/diet/library.ts` 的 `'./libraryPlate.js'` 改回 `'../render/library.js'`（旧件已删） | `tsc -b` 红 | **exit=1**：`error TS2307: Cannot find module '../render/library.js'`；还原 ⇒ exit=0 |
| **M-B′** | 已搬件语义变异：`dietEngine.ts` 的 `avgCal: round(avgCal)` → `round(avgCal) + 1`，强制重建后跑锚点测试 | 锚点测试红 | **exit=1**：`AssertionError 1168 !== 1167`（`analysis-t5:85`）；还原后 dist **逐字节回到基线**、复绿 13/13 |
| **M-E** | `src/diet/commands.ts` 末尾加一个换行（声明源内容变） | `gen --check` 红 | **exit=1**：`GEN-STALE FAIL … diet/commands.ts 的内容与上次 build 时不同`；还原 ＋ 重打印记 ⇒ exit=0 |

M-B′ 是这三条里最值钱的一条：它证明 **1492／1492 绿不是「拿旧 dist 跑出来的」**——被测的确实是搬进来的那份新编译产物。

**反假绿两条（都攻了，两条都成立 ⇒ 是「规则面」风险，不是本窗的过错）**：

| # | 攻击 | 实得 |
|---|---|---|
| **M-C** | 改 `dietEngine.ts` 内容 ＋ 把该件 mtime **回拨到 2020-01-01**（`Copy-Item` 保 mtime 的等价形态）后跑不带 `--force` 的 `tsc -b` | **陷阱复现**：`tsc` exit=0、101ms 直接跳过重编、**`dist` 逐字节未变**、锚点测试**仍绿**（13/13）⇒ 内容已改而门禁全绿 |
| **M-D** | 把 `softdelete-120` 的锚点改回 `'../dist/analysis/diet.js'`（该旧产物在盘，而 `src` 已无此件） | **假绿通道复现**：测试 **4/4 全绿**，跑的是旧代码 |

本窗自身**没有踩这两条**：W2 用 `git mv`（内容与 mtime 都是新的），4 处测试锚点逐条更新，活 `dist` 件 0 指向旧路。但规则 24 禁清 `dist` 之后，「旧产物还在盘」这件事就成了**后来窗的现成假绿通道**（见 S3-5、建议 3）。

> 自伤披露：第一版变异脚本的副本拷贝用「路径子串含 `dist` 就跳过」过滤，误跳过 `src/exercise/distribution.ts`，导致副本 `tsc -b` 因缺件而红、变异读数全部无效。我改成**逐名**排除 `dist`／`node_modules` 后重跑，上表是重跑结果（首轮无效读数留在 `_mut.txt` 备查）。

### 维 8 · 证据面 —— **FAIL（S2）**

- **`GATE-RUN` 声明：0 条。** 全件 `Select-String 'GATE-RUN'` **零命中**；`docs/skills/skill-calorie/` 下也只有 `t324-W2-证据.md`，**没有** `t324-gate-runs.log` 对账导出件。按《六票落点规则》§四-1／2，这两件是硬要求。
- 我按严格口径实跑（默认 `--require-claims`，不放宽）：

```
$ node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t324-W2-证据.md --ticket 324-land-W2
声称运行 0 条
FAIL: 证据里没有任何 `GATE-RUN runId=… cmd=…` 声明
gate-audit: FAIL        [exit code: 1]
```

- **数字面：逐项复算为真。** §3 的 725／3638 ✔；§5 五件 sha256 逐件 ✔；§2 的「41 处／21 个文件」与提交 82 行逐对 ✔；§4 的退出码与我自跑一致 ✔；1492／160／1492 ✔；t293 `RESULT: P1=PASS…5/5` ✔。§11 自改的三处（§7 印记断言、§9 的 35→41 口径、控制符 U+0007）我复核后确认**改后文本与实测一致**，且我另做控制符自检：正文控制符 0 处。
- **措辞面：一处不自洽**（S3-1）：§9「六件旧路盘上无」未限定 src 面，而 `dist` 面六件旧产物全在盘。
- **判据④的对照物缺失**（S3-2）：票面 ④ 明写「＋ 对照物 `DEEP-EQUAL=yes`（先 build）」，本窗证据件里 `DEEP-EQUAL` **0 次出现**（未跑／未记）。

### 维 9 · 反假绿 —— **PASS（两坑均未踩）＋ 两条规则面风险立此存照**

见维 7 的 M-C／M-D；另加两条独立核查：

- **印记污染（规则 22／24）**：本席 run1 的 `--stamp` ／ `--check` 自洽；但复核期间实测到**印记被他席污染**的实例（见「附：环境发现」），说明这一族风险是**活的**。
- **引用环**（纯搬迁最怕的隐性结构破坏）：我建了 `packages/skill-calorie/src` 214 件的 import 图做环检测 ⇒ **3 个环全部是前存量**（`weight/plate ↔ weight/volatility`、`render/helpFile ↔ render/helpShell`），与六件新家**无关**；六件新家确实新增 11 条跨目录入边（见 S3-4），但没有造环。

## 三级问题清单

### S1（数据丢失／静默回归／主链破坏）：**0 条**

### S2（交付缺陷，必须整改）：**2 条**

| # | 问题 | 精确证据 | 最小整改 |
|---|---|---|---|
| S2-1 | **门禁对账面整体缺失**：证据件 0 条 `GATE-RUN` 声明，且没有协议要求的对账导出件 ⇒ 票面「证据 ＋ 对账」这条完成判据**当刻不成立** | `Select-String -Path docs/skills/skill-calorie/t324-W2-证据.md -Pattern 'GATE-RUN'` ＝ 0 命中；`Get-ChildItem docs/skills/skill-calorie -Filter 't324*'` 只有证据件；实跑 `check-gate-audit.mjs` ⇒ `gate-audit: FAIL`（exit 1） | 证据件补 `GATE-RUN runId=3d66a14a-69d0-4457-af48-128dce423642 cmd="pwsh -NoProfile -File .scratch/t324/w2.ps1"`（`cmd="…"` 后**立即换行**，行尾止于右引号）＋ 再导出 `docs/skills/skill-calorie/t324-gate-runs.log`，然后重跑同一命令至 PASS |
| S2-2 | **活源件注释里的旧路径未同步**（「全仓 grep 旧路径归零」按当刻事实不成立）。11 处 / 9 件，其中 **8 件共 10 处是本窗自己改过或搬过的件**；§8 偏离 3 只申报了「史档散文」，**没申报活件注释** | `node .scratch/t324w2r/c-refs.mjs` 的「代码面」11 行清单；典型：`src/diet/today.ts:4` 仍写「装配走 `render/nutritionPort(Docs).ts`」、`src/diet/library.ts:4` 仍写「取数走 `render/library.ts`」 | 一条命令即可收口：对六件旧串做「**活源件（`.ts`／`.mjs`）0 命中**」核对，这 11 处按新家改名（`render/nutritionPort(Docs).ts`→`diet/nutritionPort(Docs).ts` 等）；史档 `.md` 维持不改并写进偏离 |

### S3（记账／措辞／口径，随收口批量修）：**5 条**

1. §9「六件旧路盘上无」**未限定 src 面**：`dist` 面六件旧编译产物全在盘（mtime 2026-09-13 23:45:25），且 §3 原始留痕的扫描面自述「排除 node_modules／**dist**／…」⇒ 同一件里两处口径互相打脸。建议改写为「src 面六件旧路盘上无；`dist` 面旧产物仍在（规则 24 不许清），**活件指向旧 dist 的说明符 = 0**」。
2. 判据④的**对照物未跑未记**：`node docs/skills/skill-calorie/t313-routing-oracle.mjs`（须 `DEEP-EQUAL=yes`，先 build）在证据件 0 次出现。定性照 `t323r` 先例：**S3-过程记账**，不判更重（本窗未动路由面，对照物预期不受影响）。
3. 票面 02 字面点名四件、实际只搬一件：`render/diet.ts`／`render/dietDocs.ts`／`fetch/diet.ts` 依 `件归属表` 第 22–24 行判「真共用·留」。计划层已裁定，但**票面与落点口径不一致**，收口时应补一句定位（W3–W6 会反复遇到同型的「票面点名 ≠ 件归属表裁定」）。
4. 搬迁的**依赖方向代价**：六件新家共 **11 条跨目录入边**（`render/index.ts`／`render/html.ts`／`render/dietDocs.ts`／`render/analysisPlate.ts`／`analysis/dashboard.ts`／`fetch/batch.ts`），即这 6 件**不再经 `src/diet/index.ts` 出门**——与 `diet/index.ts` 头注释「域内其他件**不出这个目录**」当刻不符。属计划层设计（soft 写集如此规定），未造环、未破门禁；留给 `#320` 收口统一处置。
5. `dist` 面残留 6 件旧编译产物 ＋ 旧路说明符 4 条（均在残留件自身内）：规则 24 禁止删 `dist` 的**副作用**，本窗无从清理，但与 #325 的假绿族同批（M-D 已复现其为现成假绿通道），建议收口票一并处置。

## 给 W3–W6 的格式与流程改进建议（≤3 条）

1. **把「门禁对账」做成窗口脚本的收尾步骤，不要事后补。** 每条门禁运行**当场**把 `GATE-RUN runId=… cmd="…"` 写进证据件，`cmd="…"` 的**右引号处就断行**（`t319r` 就是死在这条上：右引号后拖了 146 字符，`parseClaimBody` 把表格头并进 cmd，审计 `matched=0/1`）；并在**窗口退出之后**再跑一次 `check-gate-audit.mjs --export docs/skills/skill-calorie/t324-gate-runs.log`——退出前导出的会是 `START` 行而不是 `RUN` 行（`t319r` 同型坑）。
2. **「纯搬迁」多带一条注释核对，别只核 import。** 把自查口径从「件内改动＝只改 import」升级为「**import ＋ 头注释里的路径**」，判据写成一条可跑命令：全仓 grep 六件旧串 ⇒ **活源件（`.ts`／`.mjs`）必须 0 命中**，史档 `.md` 允许保留但必须在偏离里逐处点名。本窗的 11 处残留就是这么漏掉的。
3. **搬迁前先做一次「判据灵敏度自证」，并且**把 `dist` 面的记账句式固定下来。** 开窗前用一次假搬迁／假改动确认每条判据**改前是 FAIL**（本窗自己在 `w2-进度.md` 里已总结过这条教训：第一版归零核对器判据恒空）；同时固定一句记账：「src 面旧路归零；`dist` 面旧产物仍在（规则 24），**活件指向旧 dist 的说明符 = 0**」——否则 W3–W6 的「归零」字样会继续被读成全盘归零，而 M-D 已经证明旧产物就是现成的假绿通道。

## 附：本席自己的门禁对账（`GATE-RUN` 声明）

本席持锁运行（`--lock-dir .scratch/locks-land`）逐条声明如下；`cmd="…"` 之后**立即换行**（行尾止于右引号）：

GATE-RUN runId=5f5e56e0-236d-4ce8-b0b9-d4a4590fd341 cmd="node .scratch/t324w2r/r.mjs"

GATE-RUN runId=cb1cfebf-2695-43fc-a6fe-4b8b6edfaadb cmd="pwsh -NoProfile -File .scratch/t324w2r/m2.ps1"

GATE-RUN runId=2f8bf7a7-d9f4-4d3f-898d-3393db9d55ff cmd="pwsh -NoProfile -File .scratch/t324w2r/m3.ps1"

GATE-RUN runId=d06a0694-61bc-4ca5-a39e-8004a04794fb cmd="pwsh -NoProfile -File .scratch/t324w2r/r2.ps1"

GATE-RUN runId=8293650a-5017-4717-a5bd-99b76eadc28f cmd="pwsh -NoProfile -File .scratch/t324w2r/r3.ps1"

GATE-RUN runId=80733b72-5597-4d11-bc77-18f248d924ca cmd="pwsh -NoProfile -File .scratch/t324w2r/land.ps1"

- 六条 `exit` 全 0；`r.mjs`＝全量门禁链，`m2.ps1`／`m3.ps1`＝隔离副本变异，`r2.ps1`＝印记复位与复读，`r3.ps1`＝第二次全量复读，`land.ps1`＝本报告首次落库（对账 ＋ 逐文件 `git add` ＋ commit ＋ push；**本身不跑 build／test**，故当时未列为门禁声明 —— 返修复验时被「反向对账」逮到，已补声明，见下）。
- 对账命令（**返修复验后已扩成 8 条声明的窗口**：本表 6 条 ＋ 下方「返修复验」节的 2 条，后者带显式 `ticket=324w2r-verify`）：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t324-W2r-复核报告.md --log .scratch/locks-land/gate-runs.log --ticket 324w2r --since 2026-09-13T15:56:46Z --until 2026-09-13T16:45:00Z` —— **实测 PASS**（`RESULT: matched=8/8 … undeclared=0`、`gate-audit: PASS`、exit 0；原始输出见 `.scratch/t324w2r/_audit2.txt`；返修复验前那一版 5 条窗口的原始输出见 `_audit.txt`）。
- 记账教训（本席自己的）：`land.ps1` 当时**只跑了 1 条对账、没跑 build／test**，我据此没把它写成声明 —— 但反向对账是「窗口内**任何** `RUN` 条目都要求有人声明」，**不区分是否门禁运行**。返修复验时我自己撞上了这条（第一次落库尝试被守门拦下，`undeclared=1`），已补声明后复跑至 PASS。
- 规则 30（他席并发污染）遵守声明：本席**所有** `node --test`／`t293` 运行**都在持锁窗口内**（含隔离副本里的锚点测试），无裸跑；t293 出现过的唯一一次红（P1／P3）按规则 30「红后同树原样复跑」处置 —— 同树复跑得 **5/5**，两次不同判 ⇒ 判**环境（印记）**、不判回归。
- 本报告落库时树**不是空的**：当刻 `git status --porcelain -uall` 有 2 项，均属他席在建改动（` M packages/skill-calorie/test/cmd-read-t11.test.mjs`、`?? packages/skill-calorie/test/freeze-clock.cjs`）。本席**未触碰、未入库**它们，`git add` 逐文件点名只加本报告。

## 附：环境发现（不归本窗，但会误导后来复核席）

- 复核期间实测到一段**「干净树上门禁却是红」**的窗口：`git status --porcelain -uall` 为空、`HEAD=b06bbfd`，而 `node packages/skill-calorie/scripts/gen-cli.mjs --check` **exit=1（GEN-STALE）**，报 `cli/legacy/scene-06.ts`／`scene-07.ts`／`routes/scene-06.ts` 三处「印记 ≠ 现内容」（我另在不持锁的只读检查里独立复现同一段输出，非探针自身造成的假红）。
- 归属：这三件**都在 `a39b86a` 改动面之外**（`git show --name-only a39b86a` 无 `legacy/scene-0N`）；本席同一棵树的两次持锁读数（run1 15:57Z、run4 16:1xZ）都是 **exit=0**。⇒ gitignore 的 `dist/.gen-inputs.json` 被「在非 HEAD 内容上跑过的 build／`--stamp`」污染，与 `t319r` 席记的 #325 假绿族**同源、反向**（那席记的是「印记只按 src 判新鲜度、`gen` 的产出却来自 `dist`」）。
- **自愈**：run4 拿到锁时（等待 160s）印记**已被他席复位** —— `STAMP-SHA256-BEFORE = STAMP-SHA256-AFTER = 74f22c2886d56cddf9c3e742edb2f8335e06e4b4ee2cad65cdded4aec0df1ae1`，且修复前 `gen:check` 已是 `GEN-CHECK ok …`（exit 0）⇒ 本席那次 `tsc -b`／`--stamp` 是**空操作**，我没有改动任何东西（含印记件）。**哪一进程在哪棵树上写的印记，从现有留痕无法反推**——与 W2 证据 §11 对同一现象的口径一致。
- 影响：任何人在印记不一致的那段时间里重跑门禁都会拿到**假红**（本轮就吃掉了我一次 t293 读数）。建议收口前先 `build → gen → gen:check` 复位，并**把门禁读数与树空＋印记一致这两件事绑在一起记**。

## 附：本席未做的事（如实声明）

- 未跑 `pnpm test`／`pnpm build` 的垫片形态（规则 26：本机 `pnpm` 是 DSH 垫片，并发会静默卡死），一律直调等价 `node`；读数为等价命令所得。
- 未跑发布链门（`boundaries`／`publish:*`／`help:examples:check`）——不在派单判据内（`boundaries` 我在 run4 记账跑了，未作为判据）。
- 未做运行时逐键对拍（需建库／profile）；未改 `#324` 票面、未关票、未替其改代码。
- W2 证据件正文我**只做了机器可判的核对**（路径／行数／数字／哈希／控制符），散文语义未逐句对读。

## 附：复核用机器证据索引（可复跑）

| 脚本／日志 | 作用 |
|---|---|
| `.scratch/t324w2r/a-move.mjs` → `_d1d3.txt` | 维 1／3：搬迁集合比对 ＋ 六件逐字节内容对照 |
| `.scratch/t324w2r/b-shared.mjs` → `_d4.txt` | 维 4：提交 82 个 `+/-` 行逐行分类（是否只有说明符） |
| `.scratch/t324w2r/c-refs.mjs` → `_d2.txt` | 维 2：全仓解析式归零 ＋ 旧路字面残留分面归类 |
| `.scratch/t324w2r/d-numbers.mjs` | 维 8：复算 §3 的 725／3638 |
| `.scratch/t324w2r/e-graph.mjs` | 维 9：214 件 import 图环检测 ＋ 新家跨目录入边 |
| `.scratch/t324w2r/r.mjs` → `_gate.txt`／`_gate.json` | 维 5／6：锁内门禁链 ＋ 五件／印记哈希复算 |
| `.scratch/t324w2r/m.mjs` → `_mut.txt`／`_mut.json` | 维 7：M-A／M-C／M-B（首轮）／M-D |
| `.scratch/t324w2r/m3.mjs` → `_mut2.txt`／`_mut2.json` | 维 7：M-B′／M-E（重跑，有效读数） |
| `.scratch/t324w2r/r2.ps1` | 维 6／附：印记复位 ＋ 门禁复读 ＋ 五件哈希 |
| `.scratch/t324w2r/_t293.txt`／`_t293b.txt` | t293 全量捕获（两窗） |

---

## 返修复验（`c2dc1ab`）—— 复核席第二次上席

**结论：PASS**（返修三条**全部消除**，未引入新 S1／S2）｜**分数：93 / 100**｜复验当刻 `HEAD = cfe5bb0`（`c2dc1ab` 之后又前进 2 次：`8b3c81a` 纯排版、`cfe5bb0` 属他票）｜复验时 `git status --porcelain -uall` 为空。
返修件面：11 files ＝ 证据件（`§12`）＋ 新受跟踪导出件 `docs/skills/skill-calorie/t324-gate-runs.log` ＋ 9 个源件的**注释**。

### 三条逐条复跑（我自己的读数）

| 返修项 | 我跑的命令 | 我的读数 | 判定 |
|---|---|---|---|
| ① 对账（第三方口径） | `check-gate-audit.mjs --evidence docs/skills/skill-calorie/t324-W2-证据.md --log docs/skills/skill-calorie/t324-gate-runs.log --ticket 324-W2fix --since 2026-09-13T16:31:14.190Z --until 2026-09-13T16:31:24.190Z` | `matched=1/1 auditEntries=1 scoped=1 undeclared=0`、`gate-audit: PASS`、exit 0 | **消** |
| ① 对账（共享锁日志口径） | 同上，`--log .scratch/locks-land/gate-runs.log` | `matched=1/1 auditEntries=137 scoped=1 undeclared=0`、`PASS`、exit 0 | **消** |
| ② 活源件注释归零 | 我自己的核对器 `node .scratch/t324w2r/c-refs.mjs` | **代码面 0 处命中**（改前我自己读到 11 处／9 件） | **消** |
| ③ 对照物 | 先 `node node_modules/typescript/bin/tsc -b`（不带 `--force`）→ `node docs/skills/skill-calorie/t313-routing-oracle.mjs` | `RESULT: SUMMARY faces=16 diffFaces=0 diffs=0 surfaceMissing=0 surfaceExtra=0 DEEP-EQUAL=yes SNAPSHOT-SHA=d4da337219ea DIST-SHA=4602f79d6927`、exit 0 | **消** |

同一把锁内附带复读：`gen:check` exit=0（键 101／能力 8／声明 82 条／未搬迁 19 条）；`snapshot:html:check` exit=0（`artifacts=186 changed=0 added=0 removed=0`）；五件生成物 ＋ `SKILL.md` 的 sha256 与 §5 冻结读数**逐字相同**。

### 返修件本身的机械复核（防「假消」）

- **确实只改注释**：`git show -U0 c2dc1ab -- packages/skill-calorie/src` 的 11 对 `+/-` 行**逐行落在注释行**（`* …`），无 import、无代码、无断言被动；九个源件在 `a39b86a` 与 `c2dc1ab` 的**行数逐件相同**（212／46／49／263／321／63／27／28／41）⇒ 行数不变、只换字面。
- **行号引用没被换成错位引用**：`src/cli/write.ts:21` 改为 `diet/productStore.ts:72,108,114,120`；我逐行核对新件 72／108／114／120 与旧件**同号同文**（同一批 `nutrition_products` 语句）。
- **排除口径合理且不比我宽**：返修席的判据排除 `*.md`（史档／散文）、`docs/research/t116-assets/*.json`（会话存档）、gitignored 的 `.scratch/**` 与 `dist/**`；**我的独立扫描不排除任何代码件**（727 个受跟踪代码件全扫），读数同为 **0**。
- **生成物零接触**：9 件都不在生成器 37 个声明源内（我复算五件 sha256 与 §5 逐字相同，`gen:check` exit=0）。

### 门禁复读与规则 30 处置（全量套件）

同一把锁、同一棵树、同一条命令集（`package.json` 那 15 条 glob 原样）**三读**：**红（`tests 1492 / suites 160 / pass 1491 / fail 1`，exit 1）→ 绿（`1492／0`，exit 0）→ 绿（`1492／0`，exit 0）**。

- 留痕：红读数的控制台转录 ＝ `.scratch/t324w2r/_test-red-reading.md`（**转录**，原始流未落盘——如实声明）；两次绿读数的 TAP 全流 ＝ `_tap2.txt`／`_tap3.txt`（同一运行 `f01d4c65` 的两遍）。
- 判定：按规则 30「两次不同判 ⇒ **flake**」判**环境**、不判回归；失败用例名未捕获（细节行被过滤），**如实标为未确证边界**，不据此判任何方向。回归面为零的三条旁证：返修件只动注释、`a39b86a` 的两次绿读（本报告前节）与本次两次绿读一致、红读数一次即不可复现。

### 未消（S3，随收口批量修）

1. **§9 原文未回改**：「六件旧路盘上无」仍未限定 `src` 面。`§12.5-3` 只把**新**句式固定为「`src` 面归零 ＋ 活件指向旧 `dist` 的说明符 = 0」，§9 那句还是旧措辞（`dist` 面 6 件旧产物当刻仍在盘）。
2. **票面 02 点名四件只搬一件**（`render/diet.ts`／`render/dietDocs.ts`／`fetch/diet.ts` 依 `件归属表` 判真共用）——票面与落点口径的一致性留收口。
3. **新家 11 条跨目录入边**不经 `src/diet/index.ts` 出门（`diet/index.ts` 头注释「域内其他件不出这个目录」当刻不成立）→ `#320`。
4. **`dist` 面 6 件旧编译产物残留**（规则 24 不许清的副作用；活件指向旧 `dist` 的说明符 = 0；M-D 已证其为现成假绿通道）→ 收口票。
5. **（新）宽窗口对账会报 `undeclared`**：把 `--ticket 324-W2fix` 的时间窗放宽到本票全时窗 ⇒ `scoped=4 undeclared=3`（`9683d038` exit=1、`105b389d` exit=1、`57077e83` exit=0）。其中 `9683d038` 已在 `§12.6-2` 点名披露，另两条只以「两段式窗口」在正文描述、**未点 runId**。窄窗口径**成立且可复现**（上表两口径双 PASS），故不判 S2；建议 W3–W6 把红态／自伤中止的运行也写成声明行（照 `t180` 先例：`--allow-nonzero` ＋ `GATE-RELAX`），免得后来者用宽窗复核拿到假红。
6. （口径修正，记我自己的账）我此前把「`--stamp` 前后印记件 sha256 不变」写成读数旁注；返修席 `§12.5-4` 指出**它不是判据**（`--stamp` 本就按现行声明源重写印记件）——该修正在口径上成立，我给它的一直是**观察**定位、未当判据。

### 本席自己的门禁对账（返修复验段）

GATE-RUN ticket=324w2r-verify runId=4c232bff-dd23-4f81-aa80-63a502997cf6 cmd="pwsh -NoProfile -File .scratch/t324w2r/v.ps1"

GATE-RUN ticket=324w2r-verify runId=f01d4c65-bf44-48c4-a5c7-370569d9f6b0 cmd="pwsh -NoProfile -File .scratch/t324w2r/v2.ps1"

两条带显式 `ticket=324w2r-verify`，可与前节 6 条在**同一把窗**里对账（`check-gate-audit.mjs` 的声明侧支持跨票号认领）：`matched=8/8 undeclared=0`、`gate-audit: PASS`（原始输出见 `.scratch/t324w2r/_audit2.txt`；命令见前节「附：本席自己的门禁对账」）。
**本席自己撞上过的同型坑**：返修复验的第一次落库尝试按 7 条声明跑，被反向对账逮到 **`undeclared=1`**（`land.ps1`，本席首次落库那条：只跑对账、没跑 build／test）—— 说明「窗口内任何 `RUN` 条目都要有声明」这条口径对**复核席自己**同样成立，补声明后才 PASS。这正是下面 S3-5 那条建议的来源。

### 复验结论

返修三条**全部消除**、可复跑可复现；未引入新 S1／S2（返修件逐行可核、生成物零接触、判据④不受影响、判据③对照物当刻成立）。**判定由 `FAIL／86` 转 `PASS／93`**（余 6 条 S3 记账中含 1 条新记账与本席 1 条口径自正）。
