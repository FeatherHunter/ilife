# #520 视觉重评席 V3（页 17–23）

本席只评产品目录 `.scratch/t387/html/` 里的 7 页：17～23。**本文件每评完一页立刻落盘该页小节**，任何时刻都保持「已完成页完整、未完成页留空骨架」。

## 一、尺（全批统一，本席不自定）

| # | 维度 | 满分 | 折入总分权重 |
|---|---|---|---|
| 1 | 结构纪律 | 20 | ×0.20 |
| 2 | 信息层级与版式细调 | 25 | ×0.25 |
| 3 | 分隔符与文字纪律 | 20 | ×0.20 |
| 4 | 跨宽自适应 | 15 | ×0.15 |
| 5 | 手机端与 HELP 同档 | 20 | ×0.20 |

加权总分 ＝ 结构×0.20 ＋ 信息×0.25 ＋ 分隔符×0.20 ＋ 跨宽×0.15 ＋ 手机端×0.20，**逐页给分**。

## 二、用户硬要求（每页逐条现量，不凭印象）

1. 桌面 1440 与手机 390／768 三档无溢出、无塌列，触摸目标不少于 44px。
2. 手机档与 HELP 同档，参照 `fixtures/help-instances/卡路里_HELP_20260731_201530.html` 的断点与窄表堆叠做法。
3. 设计师审美：间距取 4 或 8 的倍数、字号成档有下限、数值列等宽右对齐、卡／表／图左缘共线、留白有节。
4. 文案零冗余：同一事实一页只讲一次，用词守 `docs/agents/wording.md`（禁黑话、禁英文内部标识符上屏）。
5. 分号／竖线／间隔号拼串一律改成卡片／分行／徽章／图例等可见结构。
6. 视觉审查用手拍（本席用 `vision_html_screenshot` 双档 fullPage ＋ `vision_crop` ＋ `vision_describe`）；`dsh visionreview` 已坏，未试。

## 三、判读口径与证据

- 视觉模型是提示器不是证据：凡视觉读到的，都在 HTML 源码里找到对应文本／类名才写进结论；找不到的写「源码未证实」，不作扣分依据。
- 尺子量到的东西用本席自建探针（headless Chrome＋CDP，三档 1440／768／390：文档横向溢出、元素越界清单、最小字号、触摸目标 <44px 清单、表格列数、分隔符计数、内部标识符命中）复核，读数落在 `.scratch/t520-v3/`。
- 中段未细看的地方，页内明写「中段未细看」。

## 四、页 17 · 摄入预测(自定义)

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/17-摄入预测(自定义).html` |
| 字节 | 76,526 |
| mtime | 2026-09-16 13:14:46 |
| SHA256 | `55C9DB1C…E2527` |
| 本席实拍 | `…17---4a1b4ff3-shot-1440x900-fullpage.png`（1440×1636）、`…-390x844-fullpage.png`（390×2247） |

### 1 第一眼看到什么、层级清不清

第一眼：页眉「趋势分析」→ 大标题「摄入预测（自定义 60 天）」→ 三枚胶囊（卡路里／摄入预测／趋势分析）→ 蓝边结论条「按当前速率，60 天后每天大约吃 1692 卡。目标 1800 卡。」→ 页内导航四项 → 参数／概览／摄入预测轨迹／数据与日志四节。层级是清的三层（页眉—结论—分节）。源码核对：`<p class="ilife-block-page-shell-eyebrow">趋势分析</p><h1 class="ilife-block-page-shell-title">摄入预测（自定义 60 天）</h1>` 与 `<span class="ilife-block-chip">卡路里</span>…<span class="ilife-block-chip">趋势分析</span>`（1814 行、1858 行）；结论条是 `<p class="ilife-block-conclusion">`（1858 行）；分节标题 `<h2 class="tpd-sec-title">`（1853 行定义，1866 行起使用）。

**但页眉与胶囊重复**：「趋势分析」既是页眉，又是第三枚胶囊；「摄入预测」既在大标题里，又是第二枚胶囊（1814 行 vs 1858 行，源码逐字可见）。第一眼会在同一屏里读两遍同样的两个词。

### 2 挤／空／错位／列宽失衡／截断／横滚

- **空（本页最大缺陷）**：1440 档「摄入预测轨迹」表只有 **336px 宽**，左缘 180、右缘 516，**右边 744px 全空**；768 档同样 336px，右边空 400px。源码根因可查：页内自定义样式 `.tpd-track-table--cols3 .ilife-block-data-table{display:table;width:auto;margin-inline:auto}`（1838 行）＋ `.ilife-block-page-shell .ilife-block-data-table{margin-left:0;margin-right:auto}`（1854 行，同特异性、后出现者赢），表格收成「内容宽 + 左贴边」。视觉席独立读图也指到同一处（「卡片宽度上大约只占画面左 1/3 略多一点，右半边几乎全是空白」）。
- **列宽失衡**：三列实宽 **168／66／100**（机器读数，日期列是数值列的 2.5 倍），而日期是定长 10 字符；`th:first-child{min-width:140px}`（1840 行）是这条失衡的出处。
- **不挤、不截断、不横滚**：三档 `documentElement.scrollWidth − innerWidth = 0`；越界元素 0 处；横向滚动容器 0 处（机器读数）。10 行日期单元格 `lines=1`——**日期没有被折断**（探针曾报 `wrapSuspects` 高 45px，实为 `td{padding:12px 14px}`（1402 行）＋行高，非折行，属探针假阳性）。
- 中段（参数节、口径行）已看：参数节三个 `label + input` 单列 246px 高，无错位。

### 3 有没有拿分号、间隔号、竖线把并列信息挤成一串

**没有。** 可见文本里 `；`／`|`／`｜`／`·`／`•`／`／` 计数全为 0（机器读数）。口径行是**三个独立 `<span>`**：`<p class="ilife-block-caliber"><span>摄入＝按最近的趋势线往外推</span> <span>预计区间按摄入残差向外放宽，取 95% 置信带</span> <span>摄入记录不到 14 天不出预测</span></p>`（1866 行尾段），靠 `gap:4px 12px` 分列，不是符号拼串。
- 视觉席读图把这三段读成「…｜…｜…」，与机器计数冲突 → **判为视觉误读**，不采信。
- 源码未证实之外的一条：复制载荷 `data-t="【calorie · calorie.view.predict】…"` 用「·」拼串且含命令标识符，但它**不上屏**（复制菜单里只有「纯文本／JSON／CSV」三个中文/格式名标签），按尺不作扣分依据，仅记一笔。

### 4 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真）**：① 「趋势分析」页眉 ＋ 胶囊各一次（1814／1858 行）；② 「摄入预测轨迹」小节标题（1866 行 `<h2 class="tpd-sec-title">摄入预测轨迹</h2>`）与紧随其后的表题 `<caption class="ilife-block-data-table-caption">摄入预测轨迹（共 60 天，每周一点）</caption>` 同屏相邻重复；③ 「60 天」四处：大标题「（自定义 60 天）」、结论条「60 天后」、表题「（共 60 天，每周一点）」、参数输入 `预测天数 60`；④ 「1692 卡」三处：结论条、概览第 4 张卡、表末行；⑤ 「1800 卡」两处：结论条与概览「目标」卡。
- **英文内部标识符上屏：无。** 可见文本无 snake_case／点号命令式标识符（机器正则 `snake/dotted/dunder/camel/allcaps` 命中集为空）；`calorie.view.predict`、`horizonDays` 只活在 `data-t` 复制载荷里，不在屏上。

### 5 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是。** 表 `.ilife-block-data-table-table{font-variant-numeric:tabular-nums}`（1382 行）＋ `td.ilife-block-data-table-cell-right{font-family:"SF Mono",monospace;font-variant-numeric:tabular-nums}`（1419–1424 行）；四张指标卡 `valNumeric=tabular-nums`（机器读数）。
- **右对齐：是。** 两个数值列表头 `th.ilife-block-data-table-cell-right` → `text-align:right`（机器读数 `align[left,right,right]`）。视觉席读图称「表头并未右对齐」→ **判为视觉误读**（分辨率下 12px 表头右对齐的位移不可辨），不采信。
- **小数位一致：是。** 表中整数全为整数（1589…1692），「日变化 1.7」一位小数，各自成档。
- **缺值写 —：本页无缺值**（`—` 计数 0，空单元格 0），不涉及。
- **一处写法不一致（真）**：「预计区间」列首行是单值 `1589`，其余九行都是「下限 至 上限」（如 `1515 至 1687`）；且首行与同行「预测摄入」1589 重复两次。源码逐字可见（1866 行 `<td … data-label="预计区间">1589</td>`）。

### 6 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **表格读得开**：≤640px 起每行变一张圆角卡（`tbody>tr{display:block;border:1px solid var(--line);border-radius:12px;padding:8px 12px;margin:0 0 8px}`，1846 行），格内「标签在左、值在右」（`td{display:flex;justify-content:space-between}` ＋ `td::before{content:attr(data-label)}`，1463–1480 行），`thead{display:none}`（1443 行）。视觉席读图结论与源码一致（「每行是一张独立卡片，标签在左值在右」）。
- **触摸目标**：390 档九个可点件**全部正好 44px 高**——页内导航四个锚点 `52×44／52×44／104×44／91×44`，三个参数输入 `358×44`，两个复制按钮 `175×44`；**低于 44px 的 0 处**（机器读数）。视觉席读图称「顶部 tab 按钮目测 24–28px」→ **判为视觉误读**，不采信。
- **日期没被折断**：日期格 `lines=1`（机器读数），`td{white-space:nowrap}`（1842／1847 行）。
- **字号**：可见最小字号 12px（页眉 `p.ilife-block-page-shell-eyebrow`，机器读数）。探针另报 11px 是 `th`，而 ≤640 档 `thead{display:none}`（1443 行）——**不可见，不计**。真正贴着下限的是移动版行标签伪元素 `.ilife-block-data-table td::before{font-size:11.5px}`（1478 行，不可由元素探针量到，源码可证），低于本批 12px 正文下限 0.5px。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **17** | 语义 `<section>`＋`<h2 class="tpd-sec-title">`＋`nav.ilife-block-toc`＋真表 `thead/tbody/caption/th[scope]`＋移动卡片化都在位；扣 3：同一张表在 ≤640 档有**两套互相覆盖的规则**（1845–1850 与 1861–1865，后者靠特异性败给前者，属死规则）＋ `<section id="sec-data">` 内再嵌无标题 `<section class="ilife-block-copy-block">` ＋ 按位置选择器 `.ilife-block-kpi-card:nth-child(4)`（1833 行）调字号 |
| 2 | 信息层级与版式细调 | 25 | **19** | 层级清楚、四卡 2×2 等宽（534＋12＋534＝1080）、数值 22/28px 成档；扣 6：表右 744px 空（1838/1854 行）、列宽 168／66／100 失衡（1840 行）、间距出 4 倍数——胶囊 `padding:2px 6px`、指标值行 `gap:6px`、表题与表头 `padding:10px`（1385／1392 行） |
| 3 | 分隔符与文字纪律 | 20 | **15** | 可见文本分隔符 0（机器读数）、口径行用三个 `<span>` 分列、无英文标识符上屏；扣 5：同一事实多写——「趋势分析」页眉＋胶囊、「摄入预测轨迹」标题＋表题、「60 天」四处、「1692 卡」三处、「1800 卡」两处 |
| 4 | 跨宽自适应 | 15 | **12** | 三档溢出 0、越界 0、横滚 0、窄表堆叠成立；扣 3：768 档仍按 336px 收缩（表格不自适应可用宽度），1440 档 2/3 内容区未被表格利用 |
| 5 | 手机端与 HELP 同档 | 20 | **17** | 可点件九处**全 44px**、窄表卡片化＋标签在左、日期不折、可见字号 12px；扣 3：移动行标签 11.5px 低于 12px 下限（1478 行）、行内 `gap:2px 10px`／卡 `padding:8px 12px` 出 4 倍数、断点取 640／820 而 HELP 取 600／900（同档但不同值，页面注释自称「同 HELP」） |

**加权总分：17/20×100×0.20 ＋ 19/25×100×0.25 ＋ 15/20×100×0.20 ＋ 12/15×100×0.15 ＋ 17/20×100×0.20 ＝ 85×0.20 ＋ 76×0.25 ＋ 75×0.20 ＋ 80×0.15 ＋ 85×0.20 ＝ 17.0 ＋ 19.0 ＋ 15.0 ＋ 12.0 ＋ 17.0 ＝ 80.0**

**页 17 加权总分 80（未过 90）。**

## 五、页 18 · 摄入预测(营养目标达成预测)

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/18-摄入预测(营养目标达成预测).html` |
| 字节 | 72,387 |
| mtime | 2026-09-16 13:14:46 |
| SHA256 | `1D41E3FC…EF61` |
| 本席实拍 | `…18---a1696c82-shot-1440x900-fullpage.png`（1440×900，整页正好一屏）、`…-390x844-fullpage.png`（390×964） |

### 1 第一眼看到什么、层级清不清

第一眼落在结论条「这段的日均摄入 1589 卡，比目标 1800 卡差了 211 卡。」（源码 `<p class="ilife-block-conclusion">`），它是整页对比最强的元素；视觉席读图同样第一眼落在它（「蓝竖条 ＋ 加粗三个数字，是整页对比最强、最像结论的元素」）。第二眼是大标题「摄入预测（营养目标达成预测）」与三枚胶囊「卡路里／摄入预测／趋势分析」。

**层级断档（真）**：页里 `<h1>` 之后**一个 `` `<h2>``／`<h3>` 都没有**（源码计数 h2=0 h3=0，section=5），三节 `<section id="sec-params">／#sec-overview／#sec-data` 全是没有标题的光节；同族的页 17 是有 `<h2 class="tpd-sec-title">参数</h2>` 的。读者只能靠顶部那排胶囊（`nav.ilife-block-toc`）才知道下面三节叫什么。

### 2 挤／空／错位／列宽失衡／截断／横滚

- **无挤、无空、无错位**：1440 档整页 895px 高，正好一屏；四张卡 2×2，实宽 `534＋12＋534＝1080`，与页壳内容宽严丝合缝（机器读数 `grid 534px 534px gap 12px`）；卡左缘 180／726 与页壳左缘 180 共线。
- **无溢出**：三档 `scrollWidth − innerWidth = 0`，越界元素 0 处，横滚容器 0 处（机器读数）。
- **无表无图**：`<table>` 计数 0，本页不存在表格列宽问题。
- 中段已看（参数表单 171px 高、四卡 262px 高、按钮行 44px 高、口径行 18px 高），无压字、无截断。视觉席读图称手机档「标题折成两行」——`<h1 class="ilife-block-page-shell-title">` 在 ≤820 档 `font-size:26px`（页内样式），折行但未被截断，非缺陷。

### 3 有没有拿分号、间隔号、竖线把并列信息挤成一串

**没有。** 可见文本 `；`／`|`／`｜`／`·`／`•`／`／` 计数全为 0（机器读数）。口径行仍是三个独立 `<span>`（`目标＝每天的热量目标`／`达标线＝日均偏离不超过目标的 10%`／`摄入记录不到 14 天不出判定`），靠 `p.ilife-block-caliber{gap:4px 12px}` 分列。视觉席把这三段读成「目标 = 每天的摄量目标」等（还漏字），属读图误差；源码 `目标＝每天的热量目标`，**以源码为准**。

### 4 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（本页最重）**：结论条一句就把三个数全说了——「日均摄入 1589 卡，比目标 1800 卡差了 211 卡」；紧接着三张卡又是 `均值 1589`／`目标 1800`／`缺口 211`；「缺口」卡里的 `相对目标 1800 卡` 第三次重复 1800。页眉「趋势分析」与第三枚胶囊「趋势分析」同屏重复（源码 `<p class="ilife-block-page-shell-eyebrow">趋势分析</p>` 与 `<span class="ilife-block-chip">趋势分析</span>`）。
- **英文内部标识符上屏：无**（机器正则命中集为空）。
- 页脚的判定依据（`达标线＝日均偏离不超过目标的 10%`）与第 4 张卡的判定（`偏离` 徽章）分居页首页尾，读者要来回跳才能确认判定是怎么来的——属层级问题，记在第 2 维。

### 5 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是。** 指标数值机器读数 `valNumeric=tabular-nums`（`.ilife-block-kpi-card-value`），右对齐同理在模板里（`text-align:right`）。
- **小数位一致：是**（1589／1800／211 全整数，无混排小数）。
- **缺值写 —：本页没有缺值**（`—` 计数 0，空单元格 0），不涉及。
- **一处成档不一（真）**：第 4 张卡「是否在轨」**没有数值**——源码是 `<div class="ilife-block-kpi-card-label">是否在轨</div><div class="ilife-block-kpi-card-detail">日均离目标偏开。</div><div class="ilife-block-kpi-card-badge"><span class="ilife-status-badge ilife-status-badge-warn">偏离</span></div>`，同页前三张卡都是「标签＋22px 数值＋单位」，第四张换成「标签＋小字句＋22px 徽章」。视觉席两档读图都独立点到这一处（桌面「第四张卡显示：是否在轨／日均离目标偏开。／一个橘黄色胶囊写着偏离」；手机「和其他三张看起来不一致」）。该卡文案「日均离目标偏开。」不成句、不带量（差 11.7% 这个数只存在于结论条）。

### 6 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **本页无表**，窄表堆叠一项不适用；四卡在 390 档仍 2 列（`grid-template-columns:repeat(2,1fr)`，页内 ≤820 档），实宽 173＋12＋173＝358（机器读数），没有塌列。
- **触摸目标**：390 档七个可点件**全部 44px 高**——导航三锚点 `52×44／52×44／91×44`、两个输入 `358×44`、两个复制按钮 `175×44`；低于 44px **0 处**（机器读数）。视觉席读图称「tab 按钮约 34px、chip 约 32px、复制按钮约 40px」→ **判为视觉误读**（源码 `.ilife-block-toc a{min-height:44px;padding:8px 12px}` 可证），不采信。
- **日期没被折断**：参数输入 `358×44` 单行容纳 `2026-08-18`／`2026-09-16`（机器读数）。
- **字号**：可见最小字号 **12px**（口径行 `.ilife-block-caliber{font-size:12px}` 与页眉 12px）。视觉席称页脚小字约 11px → 机器读数 12px，以机器为准。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **15** | 语义 `<section>`／`nav.ilife-block-toc`／`.ilife-block-kpi-card-grid` 在位、无溢出；扣 5：**全页 h2/h3 计数 0**，三节都是无标题光节（同族页 17 有 `<h2 class="tpd-sec-title">`）＋ 页内样式里 16 处 `.tpd-track-table--cols3 …` 规则对应 **0 张表**（本页无 `<table>`，整段是无用规则） |
| 2 | 信息层级与版式细调 | 25 | **18** | 结论条—卡片层级清楚、四卡同宽共线（534/726，与页壳 180 共线）、整页一屏不空；扣 7：结论条与三张卡同数据（冗余挤占层级）＋「是否在轨」卡不成形（前三张「标签＋数值」，它「标签＋句子＋徽章」）＋判定依据（10% 达标线）远在页脚 ＋ 间距出 4 倍数（卡 `padding:14px`、胶囊 `padding:2px 8px;margin:0 6px 6px 0`、数值行 `gap:6px`） |
| 3 | 分隔符与文字纪律 | 20 | **16** | 可见分隔符 0、口径行三段独立 `<span>`、无英文标识符上屏；扣 4：结论条复述三卡三数＋「相对目标 1800 卡」第三次复述 1800 ＋ 页眉与胶囊重复「趋势分析」＋ 判定句「日均离目标偏开。」不成句 |
| 4 | 跨宽自适应 | 15 | **14** | 三档溢出 0、越界 0、卡片 2 列不塌、1440 一屏 895px；扣 1：768 档与 1440 档版式完全同一套（无平板档位的自有处理，靠 ≤820 的收内距顶替） |
| 5 | 手机端与 HELP 同档 | 20 | **18** | 可点件七处全 44px、可见字号 12px、无横滚、日期单行不断；扣 2：窄屏阈值取 820（HELP 取 600／900），且本页无表可对照窄表堆叠做法 |

**加权总分：15/20×100×0.20 ＋ 18/25×100×0.25 ＋ 16/20×100×0.20 ＋ 14/15×100×0.15 ＋ 18/20×100×0.20 ＝ 75×0.20 ＋ 72×0.25 ＋ 80×0.20 ＋ 93.33×0.15 ＋ 90×0.20 ＝ 15.0 ＋ 18.0 ＋ 16.0 ＋ 14.0 ＋ 18.0 ＝ 81.0**

**页 18 加权总分 81（未过 90）。**

## 六、页 19 · 摄入预测(卡路里缺口预测)

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/19-摄入预测(卡路里缺口预测).html` |
| 字节 | 71,919 |
| mtime | 2026-09-16 13:14:46 |
| SHA256 | `68627C56…4177` |
| 本席实拍 | `…19---93376f3b-shot-1440x900-fullpage.png`（1440×900，整页一屏 733px）、`…-390x844-fullpage.png`（390×829，也在 844 一屏内） |

### 1 第一眼看到什么、层级清不清

第一眼是结论条「这段时间平均每天有 +1196 卡缺口，折算下来一周约 1.09 kg。」（`<p class="ilife-block-conclusion">`），第二眼是大标题与「卡路里／摄入预测／趋势分析」三枚胶囊。层级与页 18 同一套。
**层级断档（真，同族同病）**：全页 `<h2>`／`<h3>` 计数 **0**（源码计数），`#sec-params／#sec-overview／#sec-data` 三节无标题；页内还定义了 `.tpd-sec-title{font-size:16px…}` 却**一次也没用**（`tpd-sec-title` 全页仅 1 处＝定义处）。视觉席读图把顶部那排胶囊（`nav.ilife-block-toc`）当成了节标签（「二级标签行：参数／概览／数据与日志」），正好说明真正的节标题缺位。

### 2 挤／空／错位／列宽失衡／截断／横滚

- **不挤不错位、无截断、无横滚**：三档 `scrollWidth − innerWidth = 0`，越界元素 0 处，横滚容器 0 处（机器读数）。1440 整页 733px、390 整页 829px，都短，没有拉长撑白。
- **页脚一行塞四段（本页最重版式问题）**：口径行四个 `<span>` 在 1440 档排成**同一行**，从 x=180 一直排到 x=1196（跨 1016px），段与段之间只有 12px 空隙（机器实测：`[180..384] [396..540.3] [552.3..731.4] [743.4..1196.4]`，`caliberH=18`，`gap:4px 12px`）。最后一段本身 453px 宽。到 390 档才折成两行（机器读数 `p.ilife-block-caliber h36`）。
- **参数行整行拉满**：桌面档两个 `label`（开始／结束）实宽 **1080px**、里面的 `input` 也是 `1080×44`（机器读数；源码 `.ilife-block-param-form-input{width:100%}`），而值是「2026-08-18」这种 10 字符。属共享件的整批形状，只记不扣。
- 中段已看（参数 171px、两卡 100px、按钮行 44px、口径行 18px），无压字。

### 3 有没有拿分号、间隔号、竖线把并列信息挤成一串

**符号层面：没有。** 可见文本 `；`／`|`／`｜`／`·`／`•`／`／` 计数**全为 0**（机器读数）。
**但视觉席两次读图都坚称页脚印着竖线**（「缺口 = … ｜ 每 7700 卡… ｜ 健康区间 = … ｜ 符号 = …，至少出现 3 处」）。**机器复核把这条驳掉了**：四个 `<span>` 是四个独立盒子、y 坐标同为 621、高同为 18，段间隔**恰好等于 12px**（＝ `.ilife-block-caliber{gap:4px 12px}` 的列间距），`::after` 计算值 `none`，全页 `content:` 声明里没有任何含竖线／间隔号的生成内容（正则逐文件扫过 17–20 四页，均无）。→ **判为视觉误读，不采信、不扣分**；但**「四段并排挤在一行 1016px」这件事本身是真的**（见第 2 维）。

### 4 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真）**：结论条把两个数全说了（`+1196`、`1.09 kg`），紧接着两张卡又是 `平均缺口 +1196 卡/天`、`每周掉重 1.09 kg/周`；页眉「趋势分析」与第三枚胶囊「趋势分析」同屏重复。
- **英文内部标识符上屏：无**（机器正则命中集为空）。
- **用词（真）**：口径第四段「符号＝缺口取正数**表**缺口，每周掉重取掉量为正，体重速率页另取变化率为负**表**下降」——用「表」代「表示」，句子读不通，还夹带跨页对照（「体重速率页另取…」）。源码逐字可证。视觉席读图把这段读成「逐率页」「表明缺口」，属读图误差，**以源码为准**。

### 5 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是**（指标值 `tabular-nums`，机器读数）；本页无表，故无「数值列右对齐」一栏可量。
- **小数位**：`+1196` 整数带正号、`1.09` 两位小数，各成档，无混排。
- **缺值写 —：本页没有缺值**（`—` 计数 0，空格 0），不涉及。
- **单元不齐（记一笔）**：两张卡的单位分别是 `卡/天` 与 `kg/周`，都是「量/时间」，成对，不算缺陷。

### 6 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **本页无表**，窄表堆叠不适用；两张卡在 390 档仍左右并排（机器读数 `173＋12＋173＝358`，位置 x=16 与 x=201）。视觉席读图称「卡片排成 1 列（纵向堆叠）」→ **判为视觉误读**（同一张图的放大裁片里，视觉席自己也改口为「两张卡片，左右并排」），以机器为准。
- **触摸目标**：390 档七个可点件**全部 44px 高**（三个导航锚点 `52×44／52×44／91×44`、两个输入 `358×44`、两个复制按钮 `175×44`），低于 44px **0 处**。
- **日期没被折断**：`2026-08-18`／`2026-09-16` 在 390 档单行完整（机器读数，`lines=1`）。
- **字号**：可见最小 12px（口径行 12px／页眉 12px）。视觉席两档都说「最小约 11–12px」→ 机器取 12px，以机器为准。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **15** | 语义 `<section>`／导航／卡片网格在位、无溢出；扣 5：**h2／h3 计数 0**＋`.tpd-sec-title` 定义了不用（全页只在样式里出现 1 次）＋ 16 处 `.tpd-track-table--cols3 …` 规则对应 0 张表 |
| 2 | 信息层级与版式细调 | 25 | **18** | 两卡同宽（534＋12＋534）共线、整页 733px 不空、结论条位置正确；扣 7：口径四段挤在**一行 1016px**（段间隙仅 12px，末段单段 453px）＋ 结论条与两卡同数据（占层级不增信息）＋ 间距出 4 倍数（卡 `padding:14px`、胶囊 `2px 8px`／`margin 6px`、数值行 `gap:6px`）＋ 参数行整行 1080px 只为放 10 字符 |
| 3 | 分隔符与文字纪律 | 20 | **15** | 可见分隔符 0、口径用四个独立 `<span>` 而非符号拼串、无英文标识符上屏；扣 5：结论条复述两卡两数＋页眉与胶囊重复「趋势分析」＋ 口径第四段用「表」代「表示」不成句并夹跨页对照 |
| 4 | 跨宽自适应 | 15 | **14** | 三档溢出 0、两卡两档都保持 2 列、口径行在 390 档自动折成两行；扣 1：768 档与 1440 档同一套版式，无平板自有处理 |
| 5 | 手机端与 HELP 同档 | 20 | **18** | 可点件七处全 44px、可见字号 12px、无横滚、日期单行不断、390 档整页 829px 一屏；扣 2：窄屏阈值 820（HELP 取 600／900），本页无表可比窄表堆叠做法 |

**加权总分：15/20×100×0.20 ＋ 18/25×100×0.25 ＋ 15/20×100×0.20 ＋ 14/15×100×0.15 ＋ 18/20×100×0.20 ＝ 75×0.20 ＋ 72×0.25 ＋ 75×0.20 ＋ 93.33×0.15 ＋ 90×0.20 ＝ 15.0 ＋ 18.0 ＋ 15.0 ＋ 14.0 ＋ 18.0 ＝ 80.0**

**页 19 加权总分 80（未过 90）。**

## 七、页 20 · 摄入预测(摄入稳定性预测)

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/20-摄入预测(摄入稳定性预测).html` |
| 字节 | 72,150 |
| mtime | 2026-09-16 13:14:47 |
| SHA256 | `24E9E793…0068` |
| 本席实拍 | `…20---43df56f7-shot-1440x900-fullpage.png`（1440×900，整页一屏 895px）、`…-390x844-fullpage.png`（390×955） |

### 1 第一眼看到什么、层级清不清

第一眼是结论条「日均摄入 1589 卡，每天上下波动 42 卡，算稳。」，第二眼是标题与三枚胶囊。
**层级断档（真，同族同病）**：全页 `<h2>`／`<h3>` 计数 **0**，三节无标题，`.tpd-sec-title` 定义不用。
**层级错位（本页独有）**：判定卡「是否稳定」放在第三张，**它右边的格位在桌面档是空的**（见下）；也就是说这一节在桌面上会呈现「两块数字 ＋ 一块判定 ＋ 一块留白」，判定块并不比留白抢眼，而它才是本页的答案。

### 2 挤／空／错位／列宽失衡／截断／横滚

- **空（本页最重缺陷）**：三张卡走 2 列网格，第三张卡落在第二行左格（实框 `l=180 t=582 w=534 h=125`），**第二行右格 534×125 全空**（机器读数：该区域没有任何元素）。视觉席独立读图同一结论（「第三张卡右边没有第四张卡片，是空的」「右下角第二行右侧整块空白，大约占一整张卡的宽度」）。
- **不挤不错位、无截断、无横滚**：三档 `scrollWidth − innerWidth = 0`，越界 0 处。
- **一行塞三段**：口径行三段在 1440 档同排（`gap:4px 12px`），390 档折两行；与页 19 同一形状，量级更轻（三段中最长一段短于页 19 的第四段）。
- **参数行整行拉满**：同 19（共享件形状，只记不扣）。
- 中段已看（参数 171px、三卡 262px、按钮行 44px），无压字。

### 3 有没有分号、间隔号、竖线把并列信息挤成一串

**没有。** 可见文本 `；`／`|`／`｜`／`·`／`•`／`／` 计数全为 0（机器读数）；口径三段是三个独立 `<span>`（`波动＝每天摄入偏离日均的幅度（标准差）`／`判据＝上下波动不超过 300 卡算稳`／`摄入记录不到 14 天不出判定`），靠 `gap` 分列。视觉席读图把页面小字读成「判断 = …」「偏高日均」等，属读图误差，**以源码为准**（源码是「判据＝」「偏离日均」）。

### 4 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真，三处）**：① 结论条「1589 卡／42 卡」与两张数字卡同数据；② 「均值」卡的小字 `日均，窗口 2026-08-18 至 2026-09-16` 与页脚 `📊 数据来源：本机饮食记录，窗口 2026-08-18 至 2026-09-16` **把同一个窗口说两遍**（源码两处逐字可证）；③ 页眉「趋势分析」与第三枚胶囊「趋势分析」重复。
- **英文内部标识符上屏：无**（机器正则命中集为空）。

### 5 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是**（机器读数 `tabular-nums`）；本页无表，无表列可量。
- **小数位一致：是**（`1589`／`42` 全整数）。
- **缺值写 —：本页没有缺值**（`—` 计数 0，空单元格 0），不涉及。
- **一处成档不一（真，与页 18 同病）**：第三张卡「是否稳定」没有数值——源码是 `label` ＋ `detail`（`每天的摄入比较匀。`）＋ `badge`（`ilife-status-badge ilife-status-badge-ok`「稳定」），而同页前两张是「标签＋22px 数值＋单位」。判定依据（`上下波动不超过 300 卡`）在页脚，判定块里只有「稳定」两个字。
- **另记（源码可证，不计入五维）**：参数「开始／结束」是**可编辑 `<input>`**（源码无 `readonly`；`readonly` 全页只出现在复制用的离屏 `textarea` 上），但页面是静态产物，改了不接线。

### 6 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **本页无表**；窄屏下三张卡的排法是「前两张并排、末张通栏」——机器读数第三张卡实框 `l=16 w=358`（占满 358 内容宽），出处是页内 ≤820 档 `.ilife-block-kpi-card-grid>.ilife-block-kpi-card:nth-child(odd):last-child{grid-column:span 2}`。**这条规则只在 ≤820 档生效，桌面档没有对应处理，于是桌面留出 534×125 的空格**（本页第 2 维扣分即此）。
- **触摸目标**：390 档七个可点件全部 44px 高（三个导航锚点、两个输入 `358×44`、两个复制按钮 `175×44`），低于 44px **0 处**。
- **日期没被折断**：`2026-08-18`／`2026-09-16` 与卡内「窗口 2026-08-18 至 2026-09-16」在 390 档都是单行（机器读数 + 视觉席读图一致）。
- **字号**：可见最小 12px。视觉席称页脚小字约 11px → 机器读数 12px，以机器为准。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **15** | 语义 `<section>`／导航／网格在位、无溢出；扣 5：**h2／h3 计数 0**＋`.tpd-sec-title` 定义不用＋16 处 `.tpd-track-table--cols3 …` 规则对应 0 张表 |
| 2 | 信息层级与版式细调 | 25 | **16** | 前两卡同宽共线、页面 895px 不拉长；扣 9：第三卡右侧 **534×125 空格**（桌面档）＋ 判定卡不成形（无数值，判定依据在页脚）＋ 结论条与两卡同数据 ＋ 窗口事实卡内与页脚各说一遍 ＋ 间距出 4 倍数（卡 `padding:14px`、胶囊 `2px 8px`／`margin 6px`、数值行 `gap:6px`） |
| 3 | 分隔符与文字纪律 | 20 | **16** | 可见分隔符 0、口径三段独立 `<span>`、无英文标识符上屏；扣 4：结论条复述两卡两数＋页眉与胶囊重复＋窗口事实两遍 |
| 4 | 跨宽自适应 | 15 | **13** | 三档溢出 0、390 档末卡自动通栏不塌列；扣 2：**同一组件在 1440／768 档留空格、在 390 档才补齐**（≥821 档缺 `.nth-child(odd):last-child` 一类的补齐规则），跨档行为不一致 |
| 5 | 手机端与 HELP 同档 | 20 | **18** | 可点件七处全 44px、可见字号 12px、无横滚、日期与窗口行单行不断、390 档末卡通栏；扣 2：窄屏阈值 820（HELP 取 600／900），本页无表可比窄表堆叠做法 |

**加权总分：15/20×100×0.20 ＋ 16/25×100×0.25 ＋ 16/20×100×0.20 ＋ 13/15×100×0.15 ＋ 18/20×100×0.20 ＝ 75×0.20 ＋ 64×0.25 ＋ 80×0.20 ＋ 86.67×0.15 ＋ 90×0.20 ＝ 15.0 ＋ 16.0 ＋ 16.0 ＋ 13.0 ＋ 18.0 ＝ 78.0**

**页 20 加权总分 78（未过 90）。**

## 八、页 21 · 查热量缺口

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/21-查热量缺口.html` |
| 字节 | 90,013 |
| mtime | 2026-09-16 13:14:47 |
| SHA256 | `6B57286E…3166` |
| 本席实拍 | `…21---e5a69702-shot-1440x900-fullpage.png`（1440×1646）、`…-390x844-fullpage.png`（390×1643） |

**另记（字节级事实）**：`21-查热量缺口.html` 与 `22-看热量缺口.html` **SHA256 完全相同**（同一份字节，两页是两个唤醒词下的同一个产物）；`23-看体重预测.html` 与 `01-预测体重(1周后).html` 也 **SHA256 完全相同**。本席按目录里各页各自的字节评，页 22 的读数与页 21 逐项相同。

### 1 第一眼看到什么、层级清不清

第一眼是结论条「这段时间平均每天有 +1208 卡缺口，一周合计 +8454 卡，折算下来约 1.1 kg。」，第二眼是标题「热量缺口 2026-09-10 至 2026-09-16」与三枚胶囊；再往下是四张指标卡、两条占比行、折线图卡、折叠的明细、三行合计、复制按钮、页脚口径。
层级比同族页 18–20 厚实得多（9 节）：源码有 `<h2 class="ilife-block-chart-block-title">每日摄入与消耗</h2>`、`<details class="ilife-block ilife-block-disclosure"><summary class="ilife-block-disclosure-summary">缺口明细（点击展开）</summary>…`、真表 `<table class="ilife-block-data-table-table">`（5 列、7 行、`th[scope]`、每格 `data-label`）、`svg.ilife-charts-svg viewBox="0 0 580.0 260.0" role="img"`。
**但 9 节里只有 1 个 `<h2>`**（源码计数 h2=1），参数／概览／缺口明细／合计／数据与日志五节仍无标题，读者只能靠顶部六枚胶囊（`nav.ilife-block-toc`）认路。

### 2 挤／空／错位／列宽失衡／截断／横滚

- **不挤不截断不横滚**：三档 `scrollWidth − innerWidth = 0`，越界元素 0 处（机器读数）。四张卡一排 `261×4 ＋ 12×3 = 1080`，与内容宽严丝合缝。
- **图内标注贴线（真，轻）**：源码 `<text class="ilife-charts-marktext" x="564.0" y="174.0">摄入目标 1800 卡</text>` 与 `<line class="ilife-charts-markline" y1="178.0" y2="178.0" stroke-dasharray="5 4">`——**文字基线 174、虚线 178，只隔 1.5 个用户单位（1440 档渲染约 2.7px）**。视觉席两档都报「压在虚线上」：**源码证实为「贴线」而非「压线」**，按「留白不足」轻记，不按压字扣。
- **图表字号倒挂（真，本页最重版式问题）**：图是 `viewBox 0 0 580 260` ＋ `width:100%;height:auto`，整幅随容器缩放，于是**同一段 CSS 字号在两档渲染出来的大小差一倍多**——
  | 档 | 图内容宽 | 缩放 | 轴刻度（CSS 10.8px） | 图内标注（CSS 10px） | 图例（HTML 12px，不缩放） |
  |---|---|---|---|---|---|
  | 1440 | 1050 | ×1.810 | **≈19.5px** | **≈18.1px** | 12px |
  | 768 | ≈708 | ×1.221 | ≈15.1px | ≈12.2px | 12px |
  | 390 | 328 | ×0.566 | **≈11.0px** | ≈11.0px | 12px |
  机器自证两遍：1440 档 `<text>` 框实测「1,452」**51.1×26px**、与 22px 的指标值框（「1599」54.3×26.4px）几乎同高；390 档同一串实测 28.9×15px（比值 0.565，与缩放比 0.566 吻合）。**桌面档刻度比正文 13px 大一档半、手机档又掉到 12px 下限之下**。（视觉席读图排序说「图例最大、轴数字最小」——与机器相反，判为视觉误读，不采信。）
- **合计行右列空一格**：`.ilife-block-list-rows-row{grid-template-columns:minmax(44px,auto) minmax(0,1fr) auto}`，前三行里「合计缺口」一行没有右注释，机器读数只有 2 个子件（`合计摄入/合计消耗` 有「共 7 天有记录／逐日累加」）。属轻版式不齐。
- 折叠展开后表左缘对齐：`.ilife-block-disclosure-body{padding:0 14px 14px}` ＋ 表容器 `margin-left:-16px` 抵消，展开后表外框落在 ~180，与卡片共线（机器读数 `tr [182..1242]`）。**右侧比卡片窄 18px**，轻微不对称。
- 中段（参数／四卡／占比行／合计／页脚）已看，无压字、无折行断开。

### 3 有没有拿分号、间隔号、竖线把并列信息挤成一串

- **分隔符：0**（可见文本 `；`／`|`／`｜`／`·`／`•`／`／` 计数全为 0，机器读数）。口径行仍是独立 `<span>`；视觉席读图报「页脚用竖线串成一条」，机器复核（跨距恰为 `gap:4px 12px` 的 12px、`::after content:none`、全页无含竖线的 `content:`）**驳掉，不采信**。
- **但有一处同族符号拼串（真）**：第二张卡的细字是「日常消耗 2635 **＋** 运动 171 卡」——用全角加号把两项消耗拼在一行小字里，源码逐字可证。按第 5 条的立意（并列信息该给可见结构），**轻扣**。
- 另有两条占比行「日常消耗 94%／运动 6%」是独立行组件，不算拼串。

### 4 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真，本页最密）**：机器计数（`.scratch/t520-v3/text/21-1440.txt`）——`1800` **4 次**（卡细字「目标 1800 卡/天」、图例「摄入目标 1800 卡」、图内标注「摄入目标 1800 卡」、页脚「横线是摄入目标 1800 卡」）；`8454` **3 次**（结论条、卡细字「周缺口 8454 卡」、合计行「+8454 卡」）；`+1208` **2 次**（结论条＋卡）；`1.1` **2 次**（结论条＋卡）；窗口日期两个各 **2 次**（大标题＋页脚）。结论条一句就把三张卡的数复述齐了。
- **英文内部标识符上屏：无**（机器正则命中集为空）。但**类名带票号**：页内样式里有 `.t571-deficit-detail .ilife-block-data-table{margin-left:-16px}`（源码可证）——不在屏上，只记一笔。

### 5 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是**。表 `font-variant-numeric:tabular-nums`；四张卡 `valNumeric=tabular-nums`；合计行 `.ilife-block-list-rows-right{font-variant-numeric:tabular-nums}`（源码）。
- **数值单元格右对齐：是**（表 5 列机器读数 `align[left,right,right,right,left]`，数值列 `data-label="摄入/消耗/缺口"` 带 `ilife-block-data-table-cell-right`）。
- **小数位**：`+1208／+8454／1599／2806` 全整数，`1.1` 一位小数，各成档；千分位只出现在图轴（`1,452／2,276／3,100`），表里写 `1649／2935`——**同一页两种数字写法**（图轴带千分位、表不带），记一笔。
- **缺值写 —：本页没有缺值**（`—` 计数 0，空格 0），不涉及。

### 6 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **手机档表格读得开**：明细表在 `<details>` 里默认收起（`div.ilife-block-data-table` 实测 `w=0 h=0`）；本席把它强制展开后实测：390 档每格 `314×26.3`、**标签在左值在右**（`td::before{content:attr(data-label)}` 那套 ≤640 规则），日期格「2026-09-10 周四」`lines=1` **没有折断**；1440 档展开后回到 5 列、行高 44.5。
- **触摸目标**：390 档 **11 个可点件全部 44px 高**——六个导航锚点（`52／52／117／78／52／91 × 44`）、两个输入 `358×44`、折叠头 `summary.ilife-block-disclosure-summary 356×44`、两个复制按钮 `175×44`；低于 44px **0 处**。
- **字号**：可见正文最小 12px（页眉／口径行）；**图内文字渲染后 ≈11px，低于 12px 下限**（见第 2 维表格，机器按缩放比折算）。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **16** | 9 节有 id、六项导航、`<details>` 折叠、真表 5 列带 `th[scope]`＋`data-label`、图 `role="img"`、有 1 个 `<h2>`；扣 4：九节里**八个没有标题**（h2 计数 1）＋ 折叠头「缺口明细（点击展开）」与表题「缺口明细（共 7 天）…」同一句说两遍 ＋ 页内样式出现带票号的类名 `.t571-deficit-detail` |
| 2 | 信息层级与版式细调 | 25 | **19** | 四卡一排 261×4＋12×3＝1080 共线、图卡通栏、合计行成组、展开后表左缘与卡片共线；扣 6：**图表刻度 1440 档渲染 ≈19.5px、比正文 13px 与图例 12px 大一档半**＋ 结论条复述三卡 ＋ 图内标注与虚线只隔 ≈2.7px（留白不足）＋ 合计第三行右列空 ＋ 间距出 4 倍数（卡/图卡 `padding:14px`、行 `padding:10px 14px`、数值行 `gap:6px`） |
| 3 | 分隔符与文字纪律 | 20 | **16** | 可见分隔符 0、口径行独立 `<span>`、无英文标识符上屏；扣 4：`1800` 4 次／`8454` 3 次／`+1208`、`1.1` 各 2 次的复述 ＋ 卡细字用「＋」把两项消耗拼成一行 ＋ 同一页两种数字写法（图轴千分位、表不带） |
| 4 | 跨宽自适应 | 15 | **12** | 三档溢出 0、四卡 1440 排 4 列 390 排 2 列、图随容器缩放、表在 ≤640 档堆叠且日期不断；扣 3：**同一张图两档字号倒挂**（1440 ≈19.5px／768 ≈15.1px／390 ≈11.0px，档位没把渲染后字号钉住） |
| 5 | 手机端与 HELP 同档 | 20 | **16** | 可点件十一处全 44px、可见正文 12px、展开后窄表标签在左、日期单行、无横滚；扣 4：图内文字渲染 ≈11px 低于 12px 下限（渲染后字号没跟着档位走）＋ 窄屏阈值与 HELP（600／900）不同值 |

**加权总分：16/20×100×0.20 ＋ 19/25×100×0.25 ＋ 16/20×100×0.20 ＋ 12/15×100×0.15 ＋ 16/20×100×0.20 ＝ 80×0.20 ＋ 76×0.25 ＋ 80×0.20 ＋ 80×0.15 ＋ 80×0.20 ＝ 16.0 ＋ 19.0 ＋ 16.0 ＋ 12.0 ＋ 16.0 ＝ 79.0**

**页 21 加权总分 79（未过 90）。**

## 九、页 22 · 看热量缺口

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/22-看热量缺口.html` |
| 字节 | 90,013 |
| mtime | 2026-09-16 13:14:47 |
| SHA256 | `6B57286E…3166`（**与 `21-查热量缺口.html` 逐字节相同**） |
| 本席实拍 | `…22---fd1f1453-shot-1440x900-fullpage.png`（1440×1646）、`…-390x844-fullpage.png`（390×1643） |

**同页证据（机器证）**：两页 SHA256 相等（`Get-FileHash`）；本席实拍两张，`vision_pixel_diff` 判 **1440 档 2,370,240 像素中差异 0、390 档 640,770 像素中差异 0**，两档 `diffRatio=0`。故页 22 的 1–6 问结论与五维得分**逐项同页 21**，不再重复抄一遍读数；下面只记页 22 自己的身份事实与一条产品级观察。

### 1–6 六问

与页 21 完全一致。带一句话的现场确认：屏幕上的大标题是「热量缺口 2026-09-10 至 2026-09-16」（**没有**「看热量缺口」这个唤醒词的任何字样），页内也没有一处文字区分「查」与「看」——两个唤醒词下拿到的是一模一样的同一张纸。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据 |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **16** | 同页 21（逐字节相同，像素差 0） |
| 2 | 信息层级与版式细调 | 25 | **19** | 同页 21 |
| 3 | 分隔符与文字纪律 | 20 | **16** | 同页 21 |
| 4 | 跨宽自适应 | 15 | **12** | 同页 21 |
| 5 | 手机端与 HELP 同档 | 20 | **16** | 同页 21 |

**加权总分：16×0.20 ＋ 19×0.25 ＋ 16×0.20 ＋ 12×0.15 ＋ 16×0.20 ＝ 16.0 ＋ 19.0 ＋ 16.0 ＋ 12.0 ＋ 16.0 ＝ 79.0**

**页 22 加权总分 79（未过 90）。**

### 另记（跨页事实，不计入本页五维）

`22-看热量缺口.html` 与 `21-查热量缺口.html` 字节相同；`23-看体重预测.html` 与 `01-预测体重(1周后).html` 字节相同（`Get-FileHash` 两两相等）。**同一个产物挂在两个不同唤醒词下**，页面上没有任何文字告诉用户「你现在看的是哪一种」。这条属产品目录层面的重复，不在本席五维尺内，仅作事实登记。

## 十、页 23 · 看体重预测

### 0 产物身份

| 项 | 值 |
|---|---|
| 文件 | `.scratch/t387/html/23-看体重预测.html` |
| 字节 | 71,824 |
| mtime | 2026-09-16 13:14:47 |
| SHA256 | `C24FF66B…F282`（**与 `01-预测体重(1周后).html` 逐字节相同**） |
| 本席实拍 | `…23---b8f69afc-shot-1440x900-fullpage.png`（1440×900，整页一屏 777px）、`…-390x844-fullpage.png`（390×1070） |

### 1 第一眼看到什么、层级清不清

第一眼是结论条「按最近的趋势，7 天后体重约 74.8 kg。」，第二眼是大标题「体重预测（7 天）」，再往下是「卡路里／**7 天**／趋势分析」三枚胶囊、三节（参数／概览／数据与日志）。
**层级是清楚的**：三节都有真标题——源码 `<h2 class="tpd-sec-title">参数</h2>`、`<h2 class="tpd-sec-title">概览</h2>`、`<h2 class="tpd-sec-title">数据与日志</h2>`（h2 计数 3），比同批的 18／19／20 三页（h2 计数 0）齐整。
**词序不一致（真）**：页眉写「预测体重」，大标题写「体重预测（7 天）」（源码 `<p class="ilife-block-page-shell-eyebrow">预测体重</p>` 与 `<h1 …>体重预测（7 天）</h1>`）——同一件事两个词序并排出现在同一屏。

### 2 挤／空／错位／列宽失衡／截断／横滚

- **不挤不错位不横滚**：三档 `scrollWidth − innerWidth = 0`，越界元素 0 处（机器读数）。四张卡一排 `261×4 ＋ 12×3 = 1080`，与内容宽严丝合缝；整页 777px，一屏内讲完。
- **390 档第四张卡贴边（真，轻）**：精确读数——卡 `[201..374]`（宽 173，内距 16 → 内容宽 141），而卡内数值行 `[216..359]` **实宽 143**（值「74.7 至 74.8」121.9px ＋ `gap:6px` ＋ 单位「kg」15.1px ＝ 143），**比内容宽宽 2px，右侧内距只剩 1px**。视觉席读图独立点到同一处（「它顶到了所在卡片右边缘的内边距，几乎贴边」）。1440 档同一张卡内容宽 229，不贴。
- **视觉席读图另外说「顶部大片空白、结论条右侧一段空白、参数区下方留白多、按钮下方大面积空白」**——本席核对：结论条是整批共用的通栏条（源码 `<p class="ilife-block-conclusion">`），整页高度 777px 本来就短，**判为「页短不是空」，不扣**。
- 无表无图，故无列宽失衡问题。中段（参数 139px、概览 140px、复制行 44px、口径行）已看，无压字、无折行断开。

### 3 有没有拿分号、间隔号、竖线把并列信息挤成一串

**没有。** 可见文本 `；`／`;`／`|`／`｜`／`·`／`•`／`／` 计数**全为 0**（机器读数）；口径行是三个独立 `<span>`（`体重＝当前值加日速率乘天数`／`预计区间按体重残差向外放宽，取 95% 置信带`／`体重记录不到 14 天不出预测`），靠 `gap:4px 12px` 分列；全页 `content:` 声明里没有含竖线／间隔号的生成内容（正则扫过，计数 0）。视觉席读图把小字那段抄成带「｜」的一串 → **判为视觉误读**，不采信。

### 4 同一件事有没有写两遍、有没有英文内部标识符上屏

- **写两遍（真）**：① 结论条「7 天后体重约 74.8 kg」与「预测」卡的 `74.8`＋「7 天后」同数据；② `74.8` 还第三次出现在「预计区间」卡的 `74.7 至 74.8`；③ `95% 置信带` 两处（预计区间卡细字 ＋ 页脚口径行）；④ `7 天` 四处（大标题「（7 天）」、胶囊「7 天」、「预测」卡细字「7 天后」、结论条「7 天后」）；⑤ 同一条门槛说两遍——参数说明「体重记录不到 14 天就只说数据不够，不编预测。」与页脚「体重记录不到 14 天不出预测」；⑥ 页眉「预测体重」与标题「体重预测」词序互换。
- **英文内部标识符上屏：无**（机器正则命中集为空）。

### 5 数值列等宽数字吗、缺值写 — 吗、小数位一致吗

- **等宽数字：是**（四张卡机器读数 `valNumeric=tabular-nums`）。
- **小数位一致：是**——`75.1`／`74.8`／`74.7` 一位小数，`-0.34` 两位小数（速率自成一档，单位 `kg/周` 与其它 `kg` 区分）。
- **缺值写 —：本页没有缺值**（`—` 计数 0，空格 0），不涉及。
- **一处成档可疑（记一笔）**：「预计区间」卡把**区间字符串**放进了 22px 的大数值槽（`74.7 至 74.8`），而同批其它页这个槽放的都是单个数字；它不是数值，也就谈不上数值列对齐问题，但会让第四张卡的「大数」明显比前三张更长（390 档正是它把卡撑到贴边）。

### 6 手机档表格读得开吗、触摸目标够大吗、日期有没有被折断

- **本页无表**，窄表堆叠不适用；四张卡在 390 档排 2×2（`173＋12＋173＝358`，机器读数），没有塌列。
- **触摸目标**：390 档 **8 个可点件全部 44px 高**——三个导航锚点 `52×44／52×44／91×44`、三个输入 `358×44`、两个复制按钮 `175×44`；低于 44px **0 处**。视觉席估「约 40px」→ 机器取 44px，以机器为准。
- **日期没被折断**：`2026-09-03`／`2026-09-16` 与卡内「7 天后」在 390 档都单行完整（机器读数 + 视觉席一致）。
- **字号**：可见最小 12px（页眉／口径行）。视觉席称「约 11–12px」→ 机器取 12px，以机器为准。

### 7 五维得分

| # | 维度 | 满分 | 得分 | 依据（本页实测） |
|---|---|---|---|---|
| 1 | 结构纪律 | 20 | **17** | 三节都有真 `<h2 class="tpd-sec-title">`、语义 `<section>`＋`id`、六项导航、四卡网格；扣 3：`#sec-data` 里再嵌无标题 `<section class="ilife-block-copy-block">` ＋ 页内样式留了 1 处 `.tpd-track-table--cols3 …` 规则而本页 `<table>` 计数 0 |
| 2 | 信息层级与版式细调 | 25 | **20** | 层级齐、四卡一排等宽共线（261×4＋12×3＝1080）、整数/小数各成档、页面一屏不拉长；扣 5：结论条复述「预测」卡 ＋ `74.8` 三处／`7 天` 四处／门槛句两遍占掉版式注意力 ＋ 390 档第四卡内容宽 141px 被 143px 的数值行撑满（右侧只剩 1px 内距）＋ 间距出 4 倍数（数值行 `gap:6px`、胶囊 `padding:2px 8px`／`margin:0 6px 6px 0`） |
| 3 | 分隔符与文字纪律 | 20 | **16** | 可见分隔符 0、口径三段独立 `<span>`、无英文标识符上屏；扣 4：页眉「预测体重」与大标题「体重预测」词序互换 ＋ `74.8`／`7 天`／`95% 置信带`／门槛句四处复述 |
| 4 | 跨宽自适应 | 15 | **13** | 三档溢出 0、四卡 1440 排 4 列 / 390 排 2 列不塌；扣 2：**同一张卡在 390 档贴边、在 1440 档宽松**（数值行 143px vs 内容宽 141px），跨档没有给长值留退路 |
| 5 | 手机端与 HELP 同档 | 20 | **18** | 可点件八处全 44px、可见字号 12px、无横滚、日期单行不断；扣 2：390 档第四卡值＋单位挤满卡内宽（`74.7 至 74.8 kg` 贴右边）＋ 窄屏阈值与 HELP（600／900）不同值 |

**加权总分：17/20×100×0.20 ＋ 20/25×100×0.25 ＋ 16/20×100×0.20 ＋ 13/15×100×0.15 ＋ 18/20×100×0.20 ＝ 85×0.20 ＋ 80×0.25 ＋ 80×0.20 ＋ 86.67×0.15 ＋ 90×0.20 ＝ 17.0 ＋ 20.0 ＋ 16.0 ＋ 13.0 ＋ 18.0 ＝ 84.0**

**页 23 加权总分 84（未过 90）。**

### 另记（跨页事实，不计入本页五维）

`23-看体重预测.html` 与 `01-预测体重(1周后).html` 字节相同（`Get-FileHash` 相等）。本页大标题写的是「体重预测（7 天）」，而本席这次要评的唤醒词是「看体重预测」——页面上没有任何字样区分这两种入口。

## 十一、汇总

### 1 逐页加权总分（本席七页）

| 页 | 标题 | 结构 20 | 信息 25 | 分隔 20 | 跨宽 15 | 手机 20 | 加权总分 | 过 90 |
|---|---|---|---|---|---|---|---|---|
| 17 | 摄入预测(自定义) | 17 | 19 | 15 | 12 | 17 | **80.0** | 否 |
| 18 | 摄入预测(营养目标达成预测) | 15 | 18 | 16 | 14 | 18 | **81.0** | 否 |
| 19 | 摄入预测(卡路里缺口预测) | 15 | 18 | 15 | 14 | 18 | **80.0** | 否 |
| 20 | 摄入预测(摄入稳定性预测) | 15 | 16 | 16 | 13 | 18 | **78.0** | 否 |
| 21 | 查热量缺口 | 16 | 19 | 16 | 12 | 16 | **79.0** | 否 |
| 22 | 看热量缺口 | 16 | 19 | 16 | 12 | 16 | **79.0** | 否 |
| 23 | 看体重预测 | 17 | 20 | 16 | 13 | 18 | **84.0** | 否 |

- 七页**没有一页过 90**；最高 84（页 23），最低 78（页 20），均值 80.1。
- 五维里扣得最狠的是**分隔符与文字纪律**（七页共扣 26 分，全部来自「同一事实写两遍」）与**跨宽自适应**（共扣 17 分，主要来自图表字号倒挂与桌面留空格）。

### 2 跨页共性缺陷（七页里反复出现，按出现页数排序）

| # | 缺陷 | 出现在 | 源码／读数证据 |
|---|---|---|---|
| 1 | 结论条把下面卡片的数**原样复述一遍** | 17–23 全部七页 | 各页 `<p class="ilife-block-conclusion">` 与 `.ilife-block-kpi-card-value` 逐字同值 |
| 2 | 页眉与胶囊重复（「趋势分析」等） | 17、18、19、20、23（23 是「7 天」） | `<p class="ilife-block-page-shell-eyebrow">` 与 `<span class="ilife-block-chip">` 同词 |
| 3 | 同一条门槛句**参数说明与页脚各说一遍** | 17–23 全部七页 | 如页 23：「体重记录不到 14 天就只说数据不够，不编预测。」／「体重记录不到 14 天不出预测」 |
| 4 | 分节缺标题（只有胶囊导航认路） | 18、19、20 三页 h2＝0；21 九节只有 1 个 h2 | 源码 `<h2` 计数；`.tpd-sec-title` 在三页里定义了却一次没用 |
| 5 | 页内留着**本页用不到的表格规则**（`.tpd-track-table--cols3 …`） | 18、19、20（各 16 处）、23（1 处） | 源码正则计数；这些页 `<table>` 计数 0 |
| 6 | 间距出 4 倍数 | 17–23 全部七页 | 卡 `padding:14px`、胶囊 `padding:2px 8px; margin:0 6px 6px 0`、数值行 `gap:6px`、表题/表头 `padding:10px`（含 14、10、6、2 四种） |
| 7 | 第三／四张「判定卡」不成形（没有数值，只有句子＋徽章） | 18、20 | 页 18 `<div class="ilife-block-kpi-card-label">是否在轨</div><div …-detail>日均离目标偏开。</div><div …-badge><span class="ilife-status-badge ilife-status-badge-warn">偏离</span>`；页 20 同形 |
| 8 | 判定依据（10%／300 卡／达标线）住在页脚，离判定块很远 | 18、19、20 | 页脚 `p.ilife-block-caliber` 三段 |

### 3 本席驳掉的视觉读图（不作扣分依据，留档）

| 视觉席读图说法 | 机器复核 | 结论 |
|---|---|---|
| 页 19／21／23 页脚「用竖线｜把几段串成一条」 | 跨距恰为 `gap:4px 12px` 的 12px、`::after content:none`、全页 `content:` 无竖线（四页正则扫过计数 0）、可见文本竖线计数 0 | **视觉误读**（模型把 flex 间隙读成印了竖线） |
| 页 17／18／19／20 手机档「导航胶囊只有 24–34px、不够手指点」 | 390 档全部可点件实测**正好 44px**（页 17 九处、18 七处、19 七处、20 七处、21 十一处、23 八处），低于 44px 计数 0 | **视觉误读** |
| 页 19 手机档「两张卡排成 1 列（上下堆叠）」 | 实测两卡并排 `173＋12＋173`（x=16 与 x=201）；同一张图的裁片里视觉席自己改口成「左右并排」 | **视觉误读** |
| 页 21 图表「图例最大、轴数字最小」 | 实测轴数字框 51.1×26px、图内标注框 156.6×26px、图例 12px——轴数字**最大** | **视觉误读**（并据此判出真缺陷：刻度渲染 ≈19.5px） |
| 页 17／18／20 页脚小字「约 11px、低于下限」 | 实测可见最小字号 **12px**；报 11px 的那批是 `display:none` 的 `thead th`（探针假阳性，已剔除） | **视觉误读** |
| 页 21 图内标注「压在虚线上」 | 源码：文字基线 y=174、虚线 y=178，隔 1.5 用户单位（渲染 ≈2.7px） | **半成立**：贴线不压线，按留白不足轻记 |
| 各页 OCR 级错字（「摄量目标」「逐率页」「偏高日均」等） | 源码逐字：`每天的热量目标`／`体重速率页`／`偏离日均` | **视觉误读**，以源码为准 |

### 4 未细看与口径声明

- **中段未细看**：页 21／22 的折线图内部逐点坐标（本席只量了缩放比、字号、贴线间距与图例，没有逐点复核曲线数值）；页 17 表内 10 行只核了首末两行与列宽，中间行按同形处理。
- 凡本席写「源码未证实」的怀疑，本文件里**一条也没有**——所有写进结论的怀疑都在产物里找到了对应文本或类名。
- 三档取 1440×900／768×1024／390×844，`vision_html_screenshot` 用 1440×900 与 390×844 `fullPage=true` 实拍；`dsh visionreview` 本席**未试**（已知坏）。
- 本席探针与实拍读数全部落在 `.scratch/t520-v3/`（探针脚本 `probe.mjs`／`dump.mjs`／`tap.mjs`／`boxes.mjs`、几何 `geo/`、文本 `text/`、整页读数 `probe.json`），不属交付件。
