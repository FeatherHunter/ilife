# 票 #266 · 接线证据（场景 04 运动 · 唤醒词与工作流程接线）

- 票：地图 #156 的子票 #266；落点 `packages/skill-calorie/src/exercise/`
- 本席：接手席（同一票号上一席已死，树上有它的无主改动）
- **判定：完成**——无主 diff 判「补齐」（4 处换词逐处对上任务清单）；6 条 `new` 记录判 **A（留着）**
- 改动件：`packages/skill-calorie/src/exercise/commands.ts`、`test/exercise-wakewords-266.test.mjs`（新）
- 生成物：`SKILL.md`／`cli/keys.ts`／`routes.generated.ts` 由生成链重生成（本席不手改、不单独提交，见 §7）

## 1. 接手时的无主 diff 量现场（三行读数）

### 1.1 改动清单

`git diff -- packages/skill-calorie/src/exercise/commands.ts` ＝ **2 处 hunk**：

1. 文件头注释追加 #266 说明（+4 行）；
2. 4 条声明的 `wakeWord`：`-distribution`／`-trend`／`-recap` 换词，`-goal` **新增**该字段。

未跟踪件 **1 件**：`packages/skill-calorie/test/exercise-wakewords-266.test.mjs`（125 行、3 个 `test()`）。

### 1.2 逐件统计

| 件 | 读数 |
|---|---|
| `git diff --stat` 接手时 | `1 file changed, 9 insertions(+), 4 deletions(-)`（13 行变动） |
| 测试件 | 未跟踪、125 行、3 用例 |
| `git log -- <commands.ts>` | 最近两条 `b443838`(#342)、`a9fb35c`(#316)——**都不含**这 4 处换词 ⇒ 改动确属未提交的无主件 |
| 交付态 `git diff --stat` | `1 file changed, 11 insertions(+), 4 deletions(-)`（注释加长 2 行，4 处换词不变） |

### 1.3 关键内容能否对上任务清单（票面六行表逐行核）

| 票面行（命令 → 应换成的真词） | 无主改动 | 核对 |
|---|---|---|
| `-distribution` → 看运动类型分布 | `看运动分类占比` → `看运动类型分布` | **逐词一致** |
| `-trend` → 看运动趋势 | `看运动消耗趋势` → `看运动趋势` | **逐词一致** |
| `-recap` → 运动复盘（本周） | `看运动复盘` → `运动复盘（本周）` | **逐词一致** |
| `-goal` → 看今日运动（vs 目标） | 原**没有**该字段 → 补上该词 | **逐词一致** |
| `-strength` → 看力量训练总览 | 未改 | HEAD 上**已经是**真词（`git show HEAD:` 实测） |
| `-cardio` → 看有氧训练总览 | 未改 | HEAD 上**已经是**真词（同上） |

⇒ 票面第 5／6 行「现在的代表唤醒词」写的是 `看力量总览`／`看有氧总览`——**那两个词住 `routes.ts:52-53` 的 `new` 表，不在 `commands.ts`**；本文件 9+1 条声明的实测值一直是真词。
**判定＝补齐**：4 处与任务清单逐处对上；测试件不采信（它引用的证据件名 `t266-证据.md` 不在本席路径所有权内，且无任何运行读数），由本席重写（4 件判据＝票面 3 件 ＋ 票面六行表逐条钉死）。

## 2. 6 条 `new` 记录的去留：**A（留着）**

- **铁律一核对（今天是否只由 `new` 提供入口）**：6 个键在 `wake` 表**都已有同形（scene 04／kind exec）入口**
  （`-goal` 159／160、`-strength` 168、`-cardio` 169、`-distribution` 167、`-recap` 171–175、`-trend` 170）
  ⇒ 按 `new` 表自己的定义（`gen-routes.mjs:160`「键内无同形入口的补入口」）这 6 条记录**前提已不成立**，属冗余。
- **选 A 的依据**：B（删）会连带改到**别图的受版本控制断言**，本票明令不许改别人的行——
  ① `test/calorie-routing-81.test.mjs:246`／`:287`／`:288` 钉死 `NEW_KEY_ROUTES.length === 68`（删 6 条＝62，三处红）；
  ② `packages/skill-calorie/test/exercise-port-111.test.mjs:110-127` 用 `routesFor(word)` 逐词命中，
     其中 `看力量总览`／`看有氧总览`／`看运动分类占比`／`看运动复盘`／`看运动消耗趋势` **只由这 6 条 `new` 承载**，删掉即红。
- A 的机械后果已核对：`gen-routes.mjs` 自洽断言只查「同 list 内 order 不重复」＋「产物＝声明逐条对齐」，不受影响（§4 的 `gen:check` 为 0 佐证）。
- **代价记账**：路由层仍留 6 个用户不会说的新拟词。清理它属跨图改期（连带两条别图断言），本票只判定、不动手。
- **结论已按票面要求写进票面评论**（`gh issue comment 266`）。

## 3. 判据先行（先红读数）

红态构造：按 **HEAD 内容**取回 `commands.ts`（`cmd /c git show HEAD:<path> > …`），
以 `git diff --quiet -- <path>` 退出 0 证明与 HEAD 逐字节一致（`redIsHead=0`）。
> 说明：接手时树上已带无主换词，故「今天就是红的」这一读数只能按**无主改动之前的状态（HEAD＝票面描述的缺口态）**取。

- 生成链四步：`pnpm build`／`pnpm gen`／`pnpm build`／`pnpm help:build` ＝ **0/0/0/0**（红态本身是合法代码）；
- `node packages/skill-calorie/test/exercise-wakewords-266.test.mjs` → **exit=1**，`RESULT: 0/4 通过`，四件全红。
- **判据重心（第三件）红条逐字**：

```
AssertionError: HELP 两边打架：HELP 场景页没列「看运动分类占比」（命令 calorie.view.exercise-distribution）；
冻结表里没有「看运动分类占比」指向 calorie.view.exercise-distribution（该命令在冻结表的词：看运动类型分布）；
查找「看运动分类占比」查不到 calorie.view.exercise-distribution（查到：）；
HELP 场景页没列「undefined」（命令 calorie.view.exercise-goal）；
冻结表里没有「undefined」指向 calorie.view.exercise-goal（该命令在冻结表的词：看今日运动（vs 目标）／看本周运动（vs 目标））；
…「看运动复盘」（冻结表里的词：运动复盘（本周）…）；…「看运动消耗趋势」（冻结表里的词：看运动趋势）…
```

另三件红条（逐字）：① `这些命令没有代表唤醒词（速查表会退回列命令名）`（指 `calorie.view.exercise-goal`）；
② `速查表与声明对不上：calorie.view.exercise-goal：速查表列「calorie.view.exercise-goal」，声明是「undefined」`；
④ `与票面六行表对不上：…distribution：票面要求「看运动类型分布」，声明是「看运动分类占比」…`。

**如实记账**：本窗口第一次跑绿腿时出现过一次**假红**——`Copy-Item` 保留源文件 mtime ⇒ 源比 `dist` 旧 ⇒ `tsc -b` 跳过重编，
`pnpm gen` 以 `GEN-PAIR FAIL：声明源与编译产物对不上` 退出 1（这一步**不计作门禁证据**，已在下一窗口按提示 touch 源文件后重跑并通过）。

## 4. 换词后（绿读数）

- 生成链四步：**0/0/0/0**；
- 判据：**exit=0**，`RESULT: 4/4 通过`（`ℹ pass 4`／`ℹ fail 0`）；
- `pnpm gen:check`：**exit=0**（生成物与声明逐条自洽）。

## 5. 变异自证两行（单点源码级变异）

| 行 | 动作 | 生成链 | 判据 |
|---|---|---|---|
| 红 | `wakeWord: '看运动趋势'` → `wakeWord: '看运动消耗趋势'`（`.scratch/t266/mutate.mjs trend-back`，命中 1 次才落盘） | 0/0/0/0 | **exit=1**，`RESULT: 1/4 通过`（红在第 1／3／4 件） |
| 绿 | 改回 `wakeWord: '看运动趋势'`（`mutate.mjs trend-fix`） | 0/0/0/0 | **exit=0**，`RESULT: 4/4 通过`；`gen:check` exit=0 |

红条逐字：`这些代表唤醒词不在 436 条词表里：calorie.view.exercise-trend → 「看运动消耗趋势」`；
`HELP 两边打架：HELP 场景页没列「看运动消耗趋势」…（该命令在冻结表的词：看运动趋势）；查找「看运动消耗趋势」查不到…`；
`与票面六行表对不上：…trend：票面要求「看运动趋势」，声明是「看运动消耗趋势」`。
（第 2 件在变异态**仍绿**是设计使然：速查表跟着声明走，故单靠第 2 件抓不住换错词——这正是判据重心放第 3 件的原因。）

## 6. GATE-RUN 声明（本席运行，标识从运行记录抄录）

```
GATE-RUN runId=e78d0cb4-f775-4c8b-ab79-7805c72c753a cmd="pwsh -NoProfile -File .scratch/t266/hold1.ps1"
GATE-RUN runId=b653052b-e006-4733-bb8b-e1b69f015a28 cmd="pwsh -NoProfile -File .scratch/t266/hold2.ps1"
```

- `e78d0cb4…`：先红窗口（红态链四步 ＋ 判据红）。包装器条目 exit=0 系脚本末句为 `Write-Output`（未上抛内层退出码）；
  **内层读数**：链 0/0/0/0、判据 exit=1（0/4）、`gen:check` exit=1（时间戳跳过导致，见 §3 记账）。
- `b653052b…`：绿态窗口（链 0/0/0/0、判据 exit=0＝4/4、`gen:check` exit=0）＋ 变异电池（红 1/4 → 还原绿 4/4 ＋ `gen:check` exit=0）。

### 6.1 运行记录导出（`.scratch/locks/gate-runs.log` 内 `ticket=266` 全部条目，逐字）

```
START ticket=266 runId=t266-base-build cmd="pnpm build" waitedMs=230016 pid=40740 at=2026-09-14T06:55:05.460Z
RUN ticket=266 runId=t266-base-build cmd="pnpm build" waitedMs=230016 exit=0 pid=40740 at=2026-09-14T06:55:08.310Z
START ticket=266 runId=t266-base-test cmd="node packages/skill-calorie/test/exercise-wakewords-266.test.mjs" waitedMs=0 pid=31472 at=2026-09-14T06:55:12.716Z
RUN ticket=266 runId=t266-base-test cmd="node packages/skill-calorie/test/exercise-wakewords-266.test.mjs" waitedMs=0 exit=1 pid=31472 at=2026-09-14T06:55:12.875Z
START ticket=266 runId=t266-probe-abc cmd="node .scratch/t266/probe-abc.mjs" waitedMs=0 pid=41316 at=2026-09-14T06:55:42.498Z
RUN ticket=266 runId=t266-probe-abc cmd="node .scratch/t266/probe-abc.mjs" waitedMs=0 exit=0 pid=41316 at=2026-09-14T06:55:42.607Z
LOCK-STOLEN ticket=266 runId=t266-chain reason=owner-dead ownerPid=46084 ownerAlive=0 ownerBelongsToLock=1 ageMinutes=3.38 at=2026-09-14T06:59:09.503Z
START ticket=266 runId=t266-chain cmd="node .scratch/t266/chain.mjs" waitedMs=50005 pid=35272 at=2026-09-14T06:59:09.504Z
RUN ticket=266 runId=t266-chain cmd="node .scratch/t266/chain.mjs" waitedMs=50005 exit=0 pid=35272 at=2026-09-14T06:59:17.447Z
START ticket=266 runId=t266-gencheck cmd="pnpm gen:check" waitedMs=10002 pid=47592 at=2026-09-14T06:59:37.139Z
RUN ticket=266 runId=t266-gencheck cmd="pnpm gen:check" waitedMs=10002 exit=0 pid=47592 at=2026-09-14T06:59:38.201Z
START ticket=266 runId=t266-test-green cmd="node packages/skill-calorie/test/exercise-wakewords-266.test.mjs" waitedMs=0 pid=32868 at=2026-09-14T06:59:38.290Z
RUN ticket=266 runId=t266-test-green cmd="node packages/skill-calorie/test/exercise-wakewords-266.test.mjs" waitedMs=0 exit=0 pid=32868 at=2026-09-14T06:59:38.420Z
START ticket=266 runId=t266-help-face cmd="node .scratch/t266/help-face.mjs" waitedMs=10001 pid=40732 at=2026-09-14T06:59:57.292Z
RUN ticket=266 runId=t266-help-face cmd="node .scratch/t266/help-face.mjs" waitedMs=10001 exit=0 pid=40732 at=2026-09-14T07:00:02.418Z
START ticket=266 runId=e78d0cb4-f775-4c8b-ab79-7805c72c753a cmd="pwsh -NoProfile -File .scratch/t266/hold1.ps1" waitedMs=10000 pid=12396 at=2026-09-14T10:26:08.880Z
RUN ticket=266 runId=e78d0cb4-f775-4c8b-ab79-7805c72c753a cmd="pwsh -NoProfile -File .scratch/t266/hold1.ps1" waitedMs=10000 pid=12396 at=2026-09-14T10:26:28.016Z exit=0
START ticket=266 runId=b653052b-e006-4733-bb8b-e1b69f015a28 cmd="pwsh -NoProfile -File .scratch/t266/hold2.ps1" waitedMs=0 pid=40492 at=2026-09-14T10:26:57.120Z
RUN ticket=266 runId=b653052b-e006-4733-bb8b-e1b69f015a28 cmd="pwsh -NoProfile -File .scratch/t266/hold2.ps1" waitedMs=0 pid=40492 exit=0 at=2026-09-14T10:27:25.771Z
```

**前 7 对条目（`t266-*`，06:55–07:00Z）是上一席（已死）的运行**：不是本席门禁证据，也不在本席窗口的对账里；
其中 `LOCK-STOLEN … reason=owner-dead ageMinutes=3.38` 记录的正是上一席**持锁而死**（其 pid 46084 已不在）。
本席窗口（10:26Z 起）的条目＝上述 2 对，与 §6 的 2 条声明一对一。

## 7. 未做项与下一手

1. **生成物未由本席提交**：`SKILL.md`／`src/cli/keys.ts`／`src/triggers/routes.generated.ts` 由生成链按当刻声明重生成
   （内容已含本票 4 处换词），但树上同时混着别席在途内容，且这三件不在本席路径所有权内 ⇒ 留给收口席
   （判据：本席收尾时 `pnpm gen:check` exit=0，即盘上生成物与声明逐条自洽）。
2. **6 条冗余 `new` 记录（A）**：清理需同改 `calorie-routing-81.test.mjs`（68→62 三处）与 `exercise-port-111.test.mjs`（5 行命中表），
   属跨图改期 ⇒ 建议另立票。
3. **框架级缺口**：`scripts/gen-cli.mjs` 至今不校验 `wakeWord` 是否 ∈ 436 词表（票面已言明须另立票），本席未碰生成器。
4. **本席未碰**：`packages/base-render/`、别家场景目录、`docs/skills/skill-calorie/` 下既有证据件、样张。
