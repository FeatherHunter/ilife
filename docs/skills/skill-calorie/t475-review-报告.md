# t475 审查报告（独立对抗审查席）

**判定：PASS** —— 三条「审查席证明不红」的版式针独立复现为「改坏必红」，另一端「正常实现全绿」也给了；判据只增不减、实现件零改动。无 S1。

## 一、机器证据

门禁各一次（`node tooling/run-locked.mjs --ticket 475 -- <命令>`）：

| 门禁 | runId | 读数 |
| --- | --- | --- |
| `pnpm build` | `963a845e-c8b3-44d2-84ff-b08e49950099` | exit=0 |
| 111 件 | `a9e1f9a8-a6de-4717-b65a-2b50041cea0f` | 13/0 |
| 265 件 | `3a82387e-e585-42c8-8559-970fa08d22eb` | 3/0 |
| 423 族 | `1a199bf7-dd97-403c-8b69-5a2f60763334` | 20/0 |
| 451 族 | `21e66be7-0f1d-4426-9152-68a2781e8a60` | 8/0 |
| 452 族 | `89420d7a-2468-4cf1-94e5-3ce9364193f0` | 9/0 |
| 453 族 | `bf257613-f99b-43b6-83dd-e9a6aaf19e70` | 5/0 |
| 454 族 | `a2ffb8bc-9a1f-451a-96be-a489a8251489` | 9/0 |
| 告警线（首跑） | `7d69109c-4835-4f65-a6a3-c4eafd904514` | **exit=1**，`59/60` |
| 告警线（收尾复跑） | `19d81bc8-b8a6-43a3-8133-ef12e3f1777c` | `60/60`，exit=0，`PASS` |

告警线首跑红**不算本票**：`RED 台账陈化：src/fetch/body.ts 台账=426 实况=455`，该件当刻 ` M`（他席在途）；`f577314^..f577314` 对 `src` 改动 **0 件**，`test/*.mjs` 在告警线扫描面外。本席**未跑 `--sync`**；他席自己把台账改到 455 后，收尾复跑即回绿 `60/60`——归因成立。

镜像（`.scratch/t475-review/mirror`）：真 `dist` 1241 件 ↔ 镜像 1241 件，逐字节同值 **1241**、不同值／缺／多 **0**；判据件 sha256 前 16 ＝ 111 `e241748154bed7ec`／265 `2514eb67fd688821`＝工作区＝`HEAD` blob；变异件 `dist/render/sportPortDocs.js` ＝ `8194f7b804afe17d`（与证据件 §1 同值）。探针 runId `b6f32ef2-31cc-445d-8cce-84b415fe1977`（交付位那一份复跑 runId `25b893b7-b17e-4f6e-84e3-12d97f9d3412`，读数同），逐条全文 `.scratch/t475-review/probe-report.txt`。

## 二、逐条路径

1. **复跑门禁**：上表逐条对上票面判据；告警线按归因处理。
2. **三条正面可判**（隔离镜像，真件零写入；每条「改坏红／写回绿」＋ sha256 同值）：
   - ① 分布页窗口从**窗口卡**摘掉（caption 留着）→ 111 **12/1**，红断言原字「`distribution 窗口未写在自己的落点（窗口卡两格／明细表 caption）："分类明细（2026-09-05 ~ 2026-09-07，末行为合计）"`」，旧 `includes` 条仍绿；写回 **13/0**。
   - ② 复盘页块名改回「高频 TOP5」→ 111 **12/1**「`recap 块名不是逐字「高频运动」："高频 TOP5"`」，旧条被页内副题代跑仍绿；写回 **13/0**。
   - ③ 力量页来源句换写法（→「运动记录」）→ 111 **12/1**「`strength 来源句不是登记写法："数据来源 · 运动记录 · 2026-09-05 → 2026-09-07 · 共 2 条"`」；写回 **13/0**。
   - 三条变异件 sha256 改坏前＝写回后＝`8194f7b804afe17d`。
3. **自设探针**（四条）：④ 分布页题面连写窗口 → 111 **12/1** ＋ 265 **2/1**；⑤ 来源卡换 id → **12/1**「`产物里读不到区段 sec-source`」（形制换了当面红，不静默放过）；⑥a／⑥b／⑥c 只往属性里塞 `calorie.exercise.`／`移植`／`t475` → 各 **12/1**，可见面那三条仍绿（整份产物面确比可见面宽）；**未变异镜像 111 13/0、265 3/0 ＝ 非恒红**。
4. **归属**：`git show --stat f577314` 只有声明三件；`src` 改动 0 件；`sportPortDocs.ts`／`reviewDocs.ts`／`sportDocs.ts`／`receipt.ts`／`records.ts` 父提交↔本提交↔工作区三方逐字节同源、工作区干净。
5. **`+9/-2` 两行逐行核对**：`assert.` 计数 111 62→75、265 25→27；提交前每条 assert 逐字仍在提交后（丢 0 条）；被删整行只有两条——注释行（`…两处都是人话）。 */`）与数据行（`{ word: '看运动类型分布', key: 'calorie.view.exercise-distribution', … }`）。**无断言被删或弱化**。
6. **D2 复算**（真页渲染）：窗口串力量页 **2** 处（`#sec-figures` ＋ `#sec-table` caption）／分布页 **仅 1** 处（明细表 caption）；`力量训练总览` 全文 **4**／可见面 **2**；六页页题节点各 **1**；四类词面六页各 **0/0**（例外见缺陷段）。

## 三、缺陷段

- **本票引入**：无。
- **本票范围（S3）**：证据件 §2「依据」的复算命令指向 `.scratch/t475/probe2.mjs`／`probe3.mjs`／`mutate.mjs`——当刻在盘可跑，但未随提交交付，「当刻可复算」只在实施席工作区成立；缺口读数本席已独立复算替代。
- **范围外**（票面已转他票，复算证实）：111 件第 1、388 行「命名底座」（`wording.md` 禁用「底座」，#111 期遗留、本提交未碰）；分布页可见面仍有 **1** 处内部词「会话」（D3）；计划复盘页整份产物 `calorie.view.` **2** 处／可见面 0（D6／D7）——本票的整份产物面针只钉趋势页，抓不到它。

## 四、五维分

判据鉴别力 **30/30** · 门禁复跑 **25/25** · 范围纪律与归属 **20/20** · 自设探针 **15→14** · 证据件可复算 **10→9** ＝ **98/100**（≥85、无 S1）。
扣分：⑤ 首版锚错（真页题节点由共享页面模板 `renderPageShell` 发，不在本席首选那一支），改正后才取到读数；§2 三条复算脚本未随提交交付。

**结论：PASS。** 复算入口 `docs/skills/skill-calorie/t475-review-probe.mjs`（跑法见件头注释）。
