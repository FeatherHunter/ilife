# t520 视觉重评 V2 · 席 #520 · 页 09–16

- 席位：#520 视觉重评席（V2），只评页 09–16。
- 产品目录：`.scratch/t387/html/`（被评页为 09–16）。
- HELP 同档参照：`fixtures/help-instances/卡路里_HELP_20260731_201530.html`
  - 定向检索所得（源码行号即该文件行号）：`.container{max-width:960px;padding:0 12px 40px}`（21 行）；
    `@media (max-width:600px)`（283 行）内 `body{font-size:12.5px}`、`.container{padding:0 8px 60px}`、
    `.copy-btn{padding:5px 9px;font-size:11.5px}`（300 行）；`@media (min-width:601px) and (max-width:900px){.container{max-width:720px}}`（316–317 行）。
  - 窄表堆叠做法：HELP 全文无 `<table>`（检索无命中），窄屏靠折叠块 `.cat-block/.sub-block/.scene-card` 纵向堆叠，
    故「HELP 同档」的判定口径取：单列纵向流、右内距/字号同时收紧、正文 ≥11px、触摸件 ≥44px。
- 尺（全批统一）：结构纪律 20／信息层级与版式细调 25／分隔符与文字纪律 20／跨宽自适应 15／手机端与 HELP 同档 20。
- 加权总分 ＝ 结构×0.20 ＋ 信息×0.25 ＋ 分隔符×0.20 ＋ 跨宽×0.15 ＋ 手机端×0.20（逐页给）。
- 逐页六问（每页必答）：①第一眼/层级 ②挤/空/错位/截断/横滚 ③符号拼串 ④重复与英文标识符上屏 ⑤数值列等宽右对齐/缺值/小数位 ⑥手机档表格与触摸目标/日期折断。
- 证据方式：每页一次 `vision_html_screenshot`（1440×900 ＋ 390×844，`fullPage=true`）＋ ≤3 次 `vision_crop` ＋ 3~4 次 `vision_describe`。

## 汇总（评完后回填）

| 页 | 结构20 | 信息25 | 分隔符20 | 跨宽15 | 手机端20 | 加权总分 |
| --- | --- | --- | --- | --- | --- | --- |
| 09 模拟减重（每天 -700 卡） | 18 | 21.5 | 18.5 | 13 | 19 | **92.13** |
| 10 模拟减重（30 天减 X kg） | 18 | 20 | 18.5 | 13 | 19 | **90.25** |
| 11 模拟减重（60 天减 X kg） | 18 | 20 | 18.5 | 13 | 19 | **90.25** |
| 12 模拟减重（90 天减 X kg） | 18 | 20 | 18.5 | 13 | 18.5 | **89.75** |
| 13 模拟减重（自定义天数减 X kg） | 18 | 20 | 18.5 | 13.5 | 19 | **90.63** |
| 14 摄入预测（按当前速率 1 周） | 19 | 21.5 | 20 | 14 | 18.5 | **94.88** |
| 15 摄入预测（按当前速率 1 月） | 19 | 21.5 | 20 | 15 | 19 | **96.13** |
| 16 摄入预测（按当前速率 3 月） | 19 | 21.5 | 20 | 15 | 18.5 | **95.63** |

- 均分 **92.46**（(92.13+90.25+90.25+89.75+90.63+94.88+96.13+95.63)/8），最低 **12 页 89.75**，最高 **15 页 96.13**。
- 达标线（≥90）：**7／8 页**（只有 12 页 89.75 未达标）；按 >90 记也是 7 页（10／11 均为 90.25）。
- 跨页同源缺陷（定级用，落地时一次修）：①「超出健康范围」判词方向与同页数字相反（10／11／12／13，生成侧 `analysis/simulate2.ts` 的 `feasible` 分支＋`render/trendPredictDocs.ts:810`）；②`nth-child(3)` 单张卡片数值 28px（09–13）；③三列表首行「预计区间」只印单值（14／15／16）。

## 09 模拟减重（每天 -700 卡）

文件：`.scratch/t387/html/09-模拟减重(每天-700卡).html`（2466 行）。实拍：1440×900 fullPage（pageHeight 1550）、390×844 fullPage（pageHeight 1747）。

- 结构（section#sec-params→概览→模拟轨迹→数据与日志）源码确证：`<nav class="ilife-block-toc">` 四链＋四个 `<section id="sec-*">`，节标题均为 `<h2 class="tpd-sec-title">`（1854 行 `.tpd-sec-title{font-size:16px;font-weight:700}`）。
- 触摸目标源码确证：`.ilife-block-toc a{min-height:44px}`（1163 行）、`.ilife-copy-btn{min-height:44px}`（279、327–328 行）、`.ilife-copy-menu-item{min-height:44px}`（377、407–408 行）、页壳 `.ilife-block-page-shell summary{min-height:44px}`（1821 行）。实拍未见任何可点件小于 44px。

### 六问逐条

1. **第一眼／层级**：第一眼是主标题＋蓝色结论条「每天多减 700 卡，一周大约掉 1.72 kg。」，层级清楚（眉题 模拟减重 → H1 → 三胶囊 → 结论条 → 页内导航 → 四节）。源码：`ilife-block-page-shell-eyebrow`／`title`／`ilife-block-conclusion`（1859 行）。**缺陷**：四个白色胶囊（`ilife-block-toc a`，`border-radius:999px`＋`min-height:44px`，1159–1172 行）与上面三个 `ilife-block-chip` 胶囊外形、高度、左右位置都接近，一眼分不清哪些可点（实拍中我起初把胶囊行误读成 tab 组）。
2. **挤／空／错位／列宽失衡／截断／横滚**：桌面无横滚、无截断、无压字。**缺陷**：轨迹表只有两列且 `width:100%`（1379–1380 行），日期列又被 `@media (min-width:641px){td:first-child{width:1%;white-space:nowrap}}`（1425–1429 行）钉成最窄，结果 14 行数值全挤在右侧贴着右内距、表体中部大片空白——桌面实拍与读图两处独立确认「数值列被拉得过宽、右侧留有大片空白」。
3. **符号拼串**：**无此缺陷（源码已证实为结构化解法）**。脚注 `ilife-block-caliber` 用 `.ilife-block-caliber:has(> span){display:flex;flex-wrap:wrap;gap:4px 12px}` ＋ `.ilife-block-caliber > span + span{padding-left:8px;border-left:1px solid rgba(210,210,215,.6)}`（1177–1192 行），实拍底部确认三条之间是**浅色细竖分隔线**、第二条「数据来源」另起一行，非 `｜` 字符串。参数区也不用符号拼串，是三个 `<label class="ilife-block-param-form-field">` 结构。
4. **重复与英文上屏**：**无英文标识符上屏**（`calorie.view.predict`／`version`／`shape`／`key`／`cut_kcal` 只出现在 `data-t` 属性与 `data-fmt` 值里，1868–1888 行，均不上屏）。**缺陷**：同一事实说两遍——「每天多减 700」在结论条与指标卡各一遍，且柱标「模拟体重（kg）」＋ caption「模拟轨迹（共 90 天，每周一点）」＋数值 75.1 三处同题。
5. **数值列**：通过。`font-variant-numeric: tabular-nums` 在 `.ilife-block-kpi-card-value`（1346 行）与 `.ilife-block-data-table-table`（1382 行）双处；`.ilife-block-data-table-cell-right` 右对齐且 `min-width:5.5em`（1416–1424 行）；实拍体重值一律一位小数（75.1…53.0）无缺值，故无 `—` 需求，**不扣分**（缺值未在本页出现，「缺值写 —」源码未证实／本页无样本）。**缺陷**：`.ilife-block-kpi-card-grid > .ilife-block-kpi-card:nth-child(3) .ilife-block-kpi-card-value{font-size:28px;font-weight:800}`（1842 行）只把第三张「每周掉重」的数值抬到 28px，其余三张仍是 `.ilife-block-kpi-card-value{font-size:22px}`（1342 行）——同一行卡里无缘无故一个数更大更粗（源码确证；首轮读图把大小读反，故此项只以源码为准）。
6. **手机档**：通过。`@media (max-width:640px)` 内 `.tpd-track-table tbody>tr{display:flex;flex-wrap:wrap;gap:2px 10px}`＋`td{display:block;white-space:nowrap}`（1862–1866 行）压过通用 `td::before{content:attr(data-label)}` 堆叠（1474–1477 行，源码顺序在后故生效），实拍 390 宽日期与体重同行、日期未被折断、无横滚。指标卡 2×2（`@media (max-width:820px){.ilife-block-kpi-card-grid{grid-template-columns:repeat(2,1fr)}}`，1828 行）。参数区 `@media (min-width:821px)` 三列、窄档回落单列（1835–1839 行）。**缺陷**：`.ilife-action-bar` 桌面档 `max-width:none`（1858 行）把两个复制按钮拉成两端分开，实拍两按钮之间大片空荡（「复制日志」还带 `disabled`，1888 行）；另 768 档未实拍，`auto-fit minmax(150px,1fr)`（1315 行）在 1440 的四列与 820 的强制两列之间可能落在三列，**源码未证实（未实拍）**，不作扣分依据。

### 得分

结构 18／20（层级与节序干净，扣在胶囊与可点件同形＋复制按钮行两端分离）；信息 21.5／25（扣列宽失衡、数值列与卡间节奏、同题三处）；
分隔符 18.5／20（无符号拼串，扣全角「＝」与「数据来源」单行隔离带来的散）；跨宽 13／15（820/640 两断点齐全、无溢出，扣 641–820 表格宽定位与断点空档）；手机端 19／20（同 HELP 档、44px 齐备、日期不折，扣说明段在窄档由 12.5px 级正文承担密度）。

**加权总分 ＝ 18×0.20＋21.5×0.25＋18.5×0.20＋13×0.15＋19×0.20 ＝ 3.60＋5.375＋3.70＋1.95＋3.80 ＝ 18.425/20 ＝ 92.13/100。**

## 10 模拟减重（30 天减 X kg）

文件：`.scratch/t387/html/10-模拟减重(30天减Xkg).html`（2460 行）。实拍：1440×900 fullPage（pageHeight 1413）、390×844 fullPage（pageHeight 1587）。

### 六问逐条

1. **第一眼／层级**：第一眼仍是标题＋蓝色结论条＋胶囊＋四节结构，与 09 同族同形（`ilife-block-page-shell-title`／`ilife-block-conclusion`／`ilife-block-toc`，1850 行）。参数区比 09 多一个「天数」字段（四个 `<label class="ilife-block-param-form-field">`）。**缺陷**：结论条把「513 卡」与「超范围」两个判定先说完，`概览` 卡再一模一样说一遍（见第 4 条），第一眼读到的两条信息高度重合。
2. **挤／空／错位／截断／横滚**：桌面与手机两档均无横滚、无截断、无压字；表格只有 6 行（30 天／7 ≈ 每周一点，末点第 30 天），行数少、无 09 那种「数值挤在右端」的失控感。仍保留同族的桌面列宽问题（`td:first-child{width:1%}` 同 09 的 1425–1429 行规则），但 6 行时观感轻得多，**按轻扣**。
3. **符号拼串**：**无此缺陷（源码确证）**。脚注同为 `ilife-block-caliber` 三 span ＋ `border-left` 竖分隔线（1850 行末段），实拍确认为浅色竖线而非 `｜` 字符。
4. **重复与英文上屏（本页硬伤）**：**同一事实的两处说法方向相反**。源码：结论条「要在 30 天里减掉 2 kg，每天大约要留出 513 卡缺口。这个速度超出健康范围，建议拉长时间或降低目标。」；指标卡「每周掉重 0.47 kg/周」＋「可行性 这个期限赶得偏快。」＋徽章「超范围」（1850／1858 行）。0.47 落在 0.5 以下，是**慢于**健康区间，页里却写「超出健康范围／赶得偏快」——判词的方向被同页的数字当场推翻。生成侧同源：`packages/skill-calorie/src/analysis/simulate2.ts:66` 的 `feasible = HEALTHY_RATE[0] <= weeklyRate && weeklyRate <= HEALTHY_RATE[1]`（`HEALTHY_RATE=[0.5,1.0]`，9–13 行），只要低于 0.5 就一律走「不可行」文案；`src/render/trendPredictDocs.ts:810` 的不行分支写死「这个速度超出健康范围」。英文标识符仍未上屏（`feasible: 0`／`target_loss`／`needed_deficit` 只在 `data-t` 属性里，1859–1863 行）。
5. **数值列**：通过。`0.47`／`513` 均为 tabular-nums 环境下的右对齐数值列（同族 1382／1416–1424 行规则），体重列一位小数（75.1…73.1）无缺值，故无 `—` 样本，**不扣分**。「30 天合计约 15390 卡」＝513×30，数对。**缺陷**：仍是 `.ilife-block-kpi-card:nth-child(3) .ilife-block-kpi-card-value{font-size:28px}`（1833 行）只抬高「每周掉重」一张，同排其余 22px。
6. **手机档**：通过。同族 `@media (max-width:640px)` 的 `.tpd-track-table` 改造（1851–1857 行）＋`@media (max-width:820px)` 的卡 2×2（1825–1829 行），实拍 390 宽日期与体重同行、6 行日期完整未被折断、按钮胶囊可见。触摸目标同 09（TOC 44px、copy 44px、summary 44px，1163／279／1821 行）。**缺陷**：四条参数在窄档回落单列后纵向拉长（`@media (min-width:821px)` 才三列，1835–1839 行的同族规则），手机档首屏信息密度偏低。

### 得分

结构 18／20；信息 20／25（扣「判词方向被同页数字推翻」与结论条／卡重复）；分隔符 18.5／20（无拼串，扣全角「＝」与脚注孤立单行）；
跨宽 13／15；手机端 19／20。加权总分 ＝ 18×0.20＋20×0.25＋18.5×0.20＋13×0.15＋19×0.20 ＝ 3.60＋5.00＋3.70＋1.95＋3.80 ＝ 18.05/20 ＝ **90.25/100**。

## 11 模拟减重（60 天减 X kg）

文件：`.scratch/t387/html/11-模拟减重(60天减Xkg).html`（页壳 1814 行起，结构与 10 同族同文）。实拍：1440×900 fullPage（pageHeight 1591）、390×844 fullPage（pageHeight 1727）。

### 六问逐条

1. **第一眼／层级**：标题「模拟减重（60 天减 4 kg）」→ 三胶囊「卡路里／60 天减 4 kg／趋势分析」→ 结论条 → 四节（1850 行）。层级与 09／10 完全同形；**缺陷**同 10：结论条把结论说完一遍，概览卡再说一遍。
2. **挤／空／错位／截断／横滚**：桌面／手机均无横滚、无截断、无压字；轨迹 11 行（实拍桌面与手机独立数出 11 行，末行 `2026-11-15`／`71.1`，源码 1858 行同一串 `<td class="ilife-block-data-table-cell-left" data-label="日期">2026-11-15</td>`）。**缺陷**：与 09 同一处桌面失衡——数值列右贴、表体左侧留白（读图两页并看时明确写「两表数值列均右对齐且留有大片空白」）。
3. **符号拼串**：**无此缺陷（源码确证）**。脚注 `ilife-block-caliber` 三 span＋`border-left` 竖分隔线（1850 行末段）。手机读图把三条读成「竖线串」，但源码里对应处是 `<span>…</span>` 三段＋CSS 边框，**以源码为准，不作扣分依据**（读图的「｜」是细竖线被识别成字符）。
4. **重复与英文上屏（同 10 的硬伤）**：结论条「要在 60 天里减掉 4 kg，每天大约要留出 513 卡缺口。这个速度超出健康范围…」；卡「每周掉重 0.47 kg/周」＋「可行性 这个期限赶得偏快。」＋徽章「超范围」（1850／1858 行）。0.47 在 0.5 以下，是慢于区间，却被写成「超出健康范围／赶得偏快」——数字与判词方向相反。英文标识符未上屏（`target_loss: 4`／`days_target: 60`／`needed_deficit: 513` 仅在 `data-t` 属性）。
5. **数值列**：通过（tabular-nums 环境、右对齐、一位小数、无缺值）；「60 天合计约 30780 卡」＝513×60 数对，末行 71.1＝75.1−4 数对。**缺陷**同族：`nth-child(3)` 单张 28px（1833 行）与同排 22px 不一致。
6. **手机档**：通过。同族 640 窄档表改造、820 档 2×2 卡、44px 触摸件、日期未折断（源码 1851–1857／1825–1833／1163／279／1821 行的同族规则，实拍确认）。**缺陷**同 10：四条参数窄档单列纵向拉长；且有 11 行表，手机档滚动较长。

### 得分

结构 18／20；信息 20／25；分隔符 18.5／20；跨宽 13／15；手机端 19／20。
加权总分 ＝ 3.60＋5.00＋3.70＋1.95＋3.80 ＝ 18.05/20 ＝ **90.25/100**。

## 12 模拟减重（90 天减 X kg）

文件：`.scratch/t387/html/12-模拟减重(90天减Xkg).html`（页壳 1814 行起）。实拍：1440×900 fullPage（pageHeight 1769）、390×844 fullPage（pageHeight 1867）。

### 六问逐条

1. **第一眼／层级**：标题「模拟减重（90 天减 6 kg）」＋同族四节结构（1850 行）。层级清楚，**缺陷**同族：结论条与概览卡重复同一判定（见第 4 条）。
2. **挤／空／错位／截断／横滚**：桌面／手机均无横滚、无截断、无压字；轨迹 15 行（末行 `2026-12-15`／`69.1`，源码 1858 行）。**缺陷**同族：15 行 + 桌面右贴数值列，长表把左半屏留成空白更明显。
3. **符号拼串**：**无此缺陷（源码确证）**：脚注三 span＋`border-left` 竖分隔线（1850 行末段）；参数区为四个 `<label class="ilife-block-param-form-field">`。
4. **重复与英文上屏（同 10 的硬伤）**：结论条「要在 90 天里减掉 6 kg，每天大约要留出 513 卡缺口。这个速度超出健康范围…」；卡「每周掉重 0.47 kg/周」＋「可行性 这个期限赶得偏快。」＋徽章「超范围」。同 10：0.47 < 0.5，判词方向被同页数字推翻。英文标识符未上屏（`target_loss: 6`／`days_target: 90`／`needed_deficit: 513` 仅在 `data-t`）。
5. **数值列**：通过（右对齐、一位小数、无缺值）；「90 天合计约 46170 卡」＝513×90 数对，末行 69.1＝75.1−6 数对。**缺陷**同族：`nth-child(3)` 单张 28px（1833 行）。
6. **手机档**：通过但**最吃紧**：15 行表在 390 宽下仍两列同行、日期完整（读图确认 16 行含表头、无折断、无横滚）；触摸件 44px 齐备。**缺陷**：15 行表 + 四条参数单列，手机档整页 1867px，长滚动中段无任何视觉锚点（读图未见分段卡/分段线）。

### 得分

结构 18／20；信息 20／25；分隔符 18.5／20；跨宽 13／15；手机端 18.5／20（长表长滚动无锚点）。
加权总分 ＝ 3.60＋5.00＋3.70＋1.95＋3.70 ＝ 17.95/20 ＝ **89.75/100**。

## 13 模拟减重（自定义天数减 X kg）

文件：`.scratch/t387/html/13-模拟减重(自定义天数减Xkg).html`。实拍：1440×900 fullPage（pageHeight 1502）、390×844 fullPage（pageHeight 1657）。本页参数 45 天／3 kg（`name="days_target" value="45"`、`name="target_loss" value="3"`，1850 行）。

### 六问逐条

1. **第一眼／层级**：标题「模拟减重（45 天减 3 kg）」＋同族四节（1850 行）。层级清楚；**缺陷**同族：结论条与概览卡重复同一判定（第 4 条）。
2. **挤／空／错位／截断／横滚**：桌面与手机均无横滚、无截断、无压字；轨迹 8 行（末行 `2026-10-31`／`72.1`，两档实拍一致）。**本页明显好于 09／12**：读图明确「数值列右对齐，桌面上表体右侧没有大片空白」——同族 CSS 仍在（`td:first-child{width:1%}` 1427 行），但 8 行短表把失衡压到看不出来，故只作**轻微**扣分。
3. **符号拼串**：**无此缺陷（源码确证）**。脚注三 span＋`border-left` 竖分隔线（1850 行末段）；四条参数是四个 `<label class="ilife-block-param-form-field">`。
4. **重复与英文上屏（同族硬伤，本页第三例）**：结论条「要在 45 天里减掉 3 kg，每天大约要留出 513 卡缺口。这个速度超出健康范围…」；卡「每周掉重 0.47 kg/周」＋「可行性 这个期限赶得偏快。」＋徽章「超范围」（1850／1858 行）。0.47 < 0.5 是**慢于**区间，判词方向又被同页数字推翻。英文标识符未上屏（`target_loss`／`days_target`／`needed_deficit` 只在 `data-t` 属性）。
5. **数值列**：通过（右对齐、tabular-nums 环境、一位小数、无缺值）；「45 天合计约 23085 卡」＝513×45 数对，末行 72.1＝75.1−3 数对。**缺陷**同族：`.ilife-block-kpi-card:nth-child(3) .ilife-block-kpi-card-value{font-size:28px}`（1833 行）单独抬高「每周掉重」。
6. **手机档**：通过。`@media (max-width:640px)` 表改造（1854–1856 行）＋820 档卡 2×2；实拍 390 宽 8 行两列、日期完整、两个复制按钮并排（读图：「并排且够大」）；表头保留。触摸目标同族 44px（1163／279／1821 行）。

### 得分

结构 18／20；信息 20／25；分隔符 18.5／20；跨宽 13.5／15（8 行短表未暴露列宽失衡，实测无溢出）；手机端 19／20。
加权总分 ＝ 3.60＋5.00＋3.70＋2.025＋3.80 ＝ 18.125/20 ＝ **90.625/100**。

## 14 摄入预测（按当前速率 1 周）

文件：`.scratch/t387/html/14-摄入预测(按当前速率1周).html`（2462 行）。实拍：1440×900 fullPage（pageHeight 1280）、390×844 fullPage（pageHeight 1415）。本页是新族：三列表＋`horizonDays` 参数＋四张卡。

### 六问逐条

1. **第一眼／层级**：眉题「趋势分析」＋标题「摄入预测（按当前速率 7 天）」＋胶囊「卡路里／摄入预测／趋势分析」＋结论条「按当前速率，7 天后每天大约吃 1601 卡。目标 1800 卡。」（1858 行）。层级清楚；节标题为 `摄入预测轨迹`。**缺陷**：胶囊「摄入预测」不带天数，而同族 09–13 的胶囊都带参数（「30 天减 2 kg」），本族三页的天数只在标题里，胶囊退化成重复分类词。
2. **挤／空／错位／截断／横滚**：桌面／手机均无横滚、无截断、无压字。**缺陷（挤）**：`预计区间` 列在首行只印一个 `1589`（源码 `data-label="预计区间">1589`，1866 行），而表头叫「预计区间」、脚注写「预计区间按摄入残差向外放宽，取 95% 置信带」——一个数冒充区间，读图也把它当成误印。**缺陷（空）**：整张表只有 2 行数据（`2026-09-16`、`2026-09-23`，1866 行），却配 caption「摄入预测轨迹（共 7 天，每周一点）」；「每周一点」在 7 天视窗下自相矛盾，且 2 行数据撑不起「轨迹」这个词与整节篇幅。**桌面档表体右半空**（三列 `width` 未定，`td:first-child{width:1%}` 1364 行起同族规则仍在）。
3. **符号拼串**：**无此缺陷（源码确证）**。脚注三 span＋`border-left` 竖分隔线（1884 行末段）；参数说明不拼串；结论条用句号分两句。三列表把「预测摄入／预计区间」分成两列，属可接受的结构化（虽列序不如「预测摄入（区间）」并置）。
4. **重复与英文上屏（本页主要扣分点）**：`1601` 与 `199` 在**三处**出现——结论条「…每天大约吃 1601 卡。目标 1800 卡。」、卡「摄入预测 1601 卡／比目标低 199 卡」、表第二行 `1601`（1858／1866 行）；`1589` 在卡「当前摄入」与表首行「预测摄入／预计区间」各出现一次。同一事实一页讲三遍，属硬要求④。英文标识符未上屏（`horizonDays: 7`／`calorie.view.predict`／`【calorie · calorie.view.predict】` 只在 `data-t` 属性，1866–1869 行）。
5. **数值列**：通过。三列表数值列走 `ilife-block-data-table-cell-right`（`text-align:right`＋`min-width:5.5em`＋tabular-nums 同族规则），实拍右对齐；数值无小数位不一致（1589／1601／1800／1.7／199）；**无缺值样本**故 `—` 规则源码未证实、不扣分。**缺陷**：本页**没有** 09–13 的 `nth-child(3)` 28px 特例（`grep` 无命中），四张卡数值同档，这是本族做得好的一点。
6. **手机档**：通过。`@media (max-width:640px)` 的 `.tpd-track-table` 改造（1861–1865 行）＋通用 `td::before{content:attr(data-label)}` 堆叠（同族 1474–1477 行规则）在文档后段生效，日期与数值不折断。触摸目标：页内导航与复制件走 `min-height:44px` 同族规则（1163／279／1821 行）。**读图与源码冲突处**：本轮 390 宽读图称「三列表头保留、列名只出现一次」，与源码 `thead{display:none}`＋`::before` 标签堆叠不一致，**标为源码未证实，不作扣分依据**；另 `tpd-track-table--cols3` 在三列窄档下 `margin-left/right` 归零（1848 行）已源码确证。

### 得分

结构 19／20（节序与命名干净、无同形胶囊歧义之外的毛病）；信息 21.5／25（扣三处重复、区间单元格单值、caption 与行数不符）；
分隔符 20／20（无一处符号拼串，脚注与结论均用结构/句读分列）；跨宽 14／15（640/820 断点齐、无溢出，扣三段式表在桌面档的列宽失衡）；
手机端 18.5／20（无溢出、无折断、44px 齐备，扣 2 行数据+空段的手机档读起来空、且桌机两端按钮行留白大）。
加权总分 ＝ 3.80＋5.375＋4.00＋2.10＋3.70 ＝ 18.975/20 ＝ **94.875/100**。

## 15 摄入预测（按当前速率 1 月）

文件：`.scratch/t387/html/15-摄入预测(按当前速率1月).html`。实拍：1440×900 fullPage（pageHeight 1458）、390×844 fullPage（pageHeight 1831）。

### 六问逐条

1. **第一眼／层级**：眉题「趋势分析」＋标题「摄入预测（按当前速率 30 天）」＋胶囊「卡路里／摄入预测／趋势分析」＋结论条「按当前速率，30 天后每天大约吃 1640 卡。目标 1800 卡。」（1858 行）。四节＝参数／概览／摄入预测轨迹／数据与日志，层级清楚。**缺陷**同 14：胶囊不带天数（本页天数只在标题与参数里）。
2. **挤／空／错位／截断／横滚**：桌面／手机均无横滚、无截断、无压字。**本页比 14 好**：源码给三列表加了专门的桌面约束——`@media (min-width:641px)` 段里 `.tpd-track-table--cols3 .ilife-block-data-table{display:table;width:auto;margin-inline:auto}`、`.ilife-block-data-table-table{width:auto;margin-left:auto;margin-right:auto}`、`th:first-child,td:first-child{min-width:140px}`、`th/td{padding-left:8px;padding-right:8px}`（1838–1844 行），表格不再被 `width:100%` 拉满，6 行数据紧凑。**缺陷（轻）**：桌面档读图称「数值未右对齐、右侧留大片空白」，与源码 `.ilife-block-data-table td.ilife-block-data-table-cell-right{…text-align:right;font-variant-numeric:tabular-nums}`（1844 行）冲突——以源码为准，**该读图结论标为源码未证实，不作扣分依据**；但同段行序冲突（1839 行 `margin-left:auto` 与后出现的 1856 行 `margin-left:0`）会让桌面档表体左对齐、内容在单元格内居中，观感上表格与左缘共线而文字不齐左，属可记的细调隐患。
3. **符号拼串**：**无此缺陷（源码确证）**。脚注三 span＋竖分隔线（1884 行末段）；三列表本身就是把「预测摄入／预计区间」分成可见列。
4. **重复与英文上屏（同 14 的扣分点）**：`1640` 在三处出现（结论条／卡「摄入预测 1640 卡」／表末行），`1589` 在卡与表首行重复（1858／1866 行）；表首行「预计区间」仍只印单值 `1589`（1866 行），与「共 30 天，每周一点」并列时更像是漏印区间。英文标识符未上屏（`horizonDays: 30`／`calorie.view.predict` 只在 `data-t`）。
5. **数值列**：通过。数值列为 right＋tabular-nums（1844 行），体重／热量的整数与一位小数（1.7）各自成档，无缺值样本故 `—` 规则不涉。本页同样**没有** 09–13 的 `nth-child(3)` 28px 特例。
6. **手机档**：**本批最规范的一处窄表堆叠（源码确证）**。`@media (max-width:640px)` 段把三列表改成逐行卡片：`.tpd-track-table.tpd-track-table--cols3 .ilife-block-data-table tbody>tr{display:block;border:1px solid var(--line);border-radius:12px;padding:8px 12px;margin:0 0 8px}` ＋ `td{display:flex;justify-content:space-between;…}` ＋ `td::before{content:attr(data-label);color:var(--fg2);font-size:12px;flex-shrink:0}`（1846–1849 行）。390 宽下每行成一张带标签小卡，日期不折、数值不截、无横滚（实拍确认 14 张卡形态见 16 页同族规则）。触摸目标：页内导航与复制件 44px 同族（1163／279／1821 行）。**缺陷**：卡片式堆叠让 6 行的表在手机上要滚过 6 张卡，同屏信息量低于桌面档。

### 得分

结构 19／20；信息 21.5／25（三处重复、首行区间单值、说明与行数不匹配）；分隔符 20／20；跨宽 15／15（本页专门为三列表补了桌面约束与卡片堆叠，是本批跨宽做得最实的一页）；手机端 19／20。
加权总分 ＝ 3.80＋5.375＋4.00＋2.25＋3.80 ＝ 19.225/20 ＝ **96.125/100**。

## 16 摄入预测（按当前速率 3 月）

文件：`.scratch/t387/html/16-摄入预测(按当前速率3月).html`。实拍：1440×900 fullPage（pageHeight 1814）、390×844 fullPage（pageHeight 2663）。本页数据最多：90 天／14 行／每行三列。

### 六问逐条

1. **第一眼／层级**：标题「摄入预测（按当前速率 90 天）」＋结论条「按当前速率，90 天后每天大约吃 1743 卡。目标 1800 卡。」（1858 行）。层级同 15；**缺陷**同族：胶囊重复「摄入预测」，天数不在胶囊。
2. **挤／空／错位／截断／横滚**：桌面／手机均无横滚、无截断、无压字；`@media (max-width:640px)` 卡片堆叠（1846–1849 行）把 14 行拆成 14 张带标签小卡，实拍确认每张卡三行（日期／预测摄入／预计区间），标签逐行重复出现——这是本批唯一做到「手机档不靠横向滚动也不靠点号串」的长表。**缺陷**：手机整页 2663px，14 张卡首尾相连（`margin:0 0 8px`）无分组，滚到中段后分不清第几天；桌面档 14 行 × 3 列的数值列仍贴右、左侧留白（同族 `td:first-child{min-width:140px}`，1840 行）。
3. **符号拼串**：**无此缺陷（源码确证）**。`预计区间` 用「1435 至 2052」这种自然语言区间落在独立列，不是拿斜杠／波浪号把两个数拼在同一个格里；脚注三 span＋竖分隔线（1884 行末段）。
4. **重复与英文上屏（同族扣分点，本页最明显）**：`1743` 三处（结论条／卡「摄入预测 1743 卡／比目标低 57 卡」／表末行 `2026-12-15`），`1589` 两处；14 行里 `预计区间` 首行仍是单值 `1589`（1866 行）。英文标识符未上屏（`horizonDays: 90`／`【calorie · calorie.view.predict】` 只在 `data-t`）。
5. **数值列**：通过。数值列 right＋tabular-nums（1844 行），14 行全部一位不多一位不少（1589／1601／1613…1743，区间 14xx–20xx），无缺值样本。校验：末行 1743＝卡值，`2026-12-15`＝窗口末日 2026-09-16＋90 天，`比目标低 57 卡`＝1800−1743，数对。**缺陷**：`日变化 1.7 卡/天` 三页同一个值，页面上看不到它是按哪个窗口算的（参数区只有开始/结束/预测天数）。
6. **手机档**：通过且**同为最规范一处**——卡片堆叠＋标签逐行出现（1846–1849 行，实拍逐行抄出「日期／预测摄入／预计区间」三标签重复），日期未被折断、数值未截断、卡片未溢出右缘；触摸件 44px 同族齐备。**缺陷**：2663px 长滚动无分段锚点（14 张卡连续），与 12 页同病但更甚。

### 得分

结构 19／20（长表仍只有 4 节、无分段）；信息 21.5／25；分隔符 20／20；跨宽 15／15（同类三列表的桌面约束＋手机卡片堆叠都落实）；手机端 18.5／20（2663px 长滚动、14 卡无分组）。
加权总分 ＝ 3.80＋5.375＋4.00＋2.25＋3.70 ＝ 19.125/20 ＝ **95.625/100**。
