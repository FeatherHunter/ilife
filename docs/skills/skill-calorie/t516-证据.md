# t516 证据 · 场景 10 视觉整改基准

**判定：PASS**（三份文档 ＋ 三件判据工具按要求交付；两处读数待补、两条铁律外的裁定缺口已具名，见第四段。）

## 一 · 机器证据

**提交 sha**：（本次提交，见第五段）

**本票判据工具与测试（随本票入库，读数为上一席 #516 窗口的持锁运行记录）**

- `GATE-RUN runId=96ee7370-58e3-4018-a72b-1b5f1a6109c0 cmd=node tooling/run-locked.mjs --ticket 516 --poll-ms 15000 --max-wait-ms 600000 -- node --test packages/skill-calorie/test/separator-audit-520.test.mjs` → **exit 0**；`tests 7 / pass 7 / fail 0`；逐条：T1 合格样板判绿 `RESULT: 3/3`、T2 `--json` 落盘一致、T3 用法错 exit 2、**T4 十一锚点变异电池（各自改坏必红＋逐文件还原必绿）**、**T5 真基线 31 页必红 `RESULT: 0/31` exit 1**、T6 与 #467 已验收页交叉读绿、T7 行级与节点级分开（log `.scratch/t516/test-sep.log`）
- `GATE-RUN runId=9b818cd4-ff44-4eb4-a77e-580d7e2ac19e cmd=<同上一命令行>` → **exit 0**（第二遍，log `.scratch/t516/test-sep2.log`）
- `GATE-RUN runId=451f6b1e-3097-47a6-91c7-ebc73ece3d81 cmd=node tooling/run-locked.mjs --ticket 516 -- node packages/skill-calorie/scripts/check-warning-line.mjs` → `RESULT: 67/67`、`PASS`（超线件 `trendDocs.ts` 1019 LF／`reportPlate.ts` 389 LF 已按台账放行；log `.scratch/t516/warnline.log`）

**本席复跑（同命令行，逐条留痕）**

- `GATE-RUN runId=ff7156c4-75fb-488b-bcca-700361e790ee cmd=<node --test 上件>` → **exit 1：未取得锁**（`WAIT-OWNER-ALIVE pid=30936 ticket=522 ageMinutes=3.54` ＋ `FAIL: 等待锁超时 maxWaitMs=600000`）。**不作为门禁证据**；只证当刻闸门锁被 #522 占着（log `.scratch/t516/t516-test-sep.log`）
- `GATE-RUN runId=fc2671de-52a7-472f-8fb5-15ddedff1363 cmd=node tooling/run-locked.mjs --ticket 516 --poll-ms 15000 --max-wait-ms 600000 -- node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t387/html --json .scratch/t516/sep-final.json` → **exit 1：未取得锁**（同 `FAIL: 等待锁超时 maxWaitMs=600000`）。**不作为门禁证据**（log `.scratch/t516/t516-sep-final.log`）

**判据红绿两向读数**

- **红向（真基线 31 页）**：`RESULT: 0/31`、`FAIL`、exit 1；节点级 **131** 处、**31/31 页非零**；规则明细 `R1 75／R2 25／R3 8／R4 0／R5 1／R6 25／R7 8`（读数 `.scratch/t516/sep-current.json`，工具自记 `at=2026-09-15T04:31:23.925Z`、`green=0`、`broken=0`）
- **绿向（合格样板三份：#467 看今日目标进度／#401d 看今日主页／#401d 看本月主页）**：`RESULT: 3/3`、`PASS`、exit 0（`.scratch/t516/sep-sample.log`）
- **变异两行**：① 上表 T4（十一锚点，源码级）读数 `✔ T4 · 变异电池：十一个锚点各自改坏必红（点名规则）＋逐文件还原必绿`；② 本席另做一次独立变异（在 `.scratch/t516/mut-target.html` 副本上塞一处 `甲；乙；丙` → 必红；按原始字节还原 → 必绿），读数见第四段第 3 条
- **静态扫描（只读，不需持锁）** `.scratch/t516/static-scan.md`：31 页合计 `ilife-block-conclusion` 0／`toc` 0／`caliber` 0／`disclosure` 0／`chip` 0／`empty` 0／`status-badge` **7**；对照 `data-table` 1241／`kpi-card` 687／`param-form` 250／`copy-block` 31／`charts` 565；外部引用 **0**、`<img>` **0**、`min-height:44px` **155**、`env(safe-area-inset-*)` **31**、`prefers-reduced-motion` **31**、单页 `@media` 最多 **18** 条、`preserveAspectRatio="xMidYMid meet"` **14**
- **四档读数**：31 页 × 390／512／820／1440 → **overflow 124 格全 0**、**clip 4**（21／22@390，`ilife-block-list-rows-main` `cw=34/sw=55`、`cw=48/sw=57`）、`scroller 2`；源 `.scratch/t520/before-house-four.json`（label `t161-house-four`），与 `.scratch/t516/measure.json` **逐页 sha256 完全一致**（同一批产物）
- **版式探针**：`GATE-RUN runId=a22986a9-55b8-45b2-a3b0-62f73c987a11 cmd=… t516-判据-版式.mjs` → **exit 1**，`Error: 'Page.enable' wasn't found`（log `.scratch/t516/layout-smoke.log`）⇒ J6／J7 读数**待补**，不是放宽

## 二 · 逐条路径

- 交付文档：`docs/skills/skill-calorie/t516-场景10-视觉整改基准.md`（21,870 字符）、`docs/skills/skill-calorie/t516-改前读数.md`（19,068 字符）、本件
- 入库工具：`packages/skill-calorie/scripts/audit-separators.mjs`（276 行）、`packages/skill-calorie/test/separator-audit-520.test.mjs`、`docs/skills/skill-calorie/t516-判据-版式.mjs`（291 行）
- 读数与草稿（`.scratch/t516/`）：`sep-current.json`／`sep-final.json`（工具读数）、`static-scan.md`（31 页静态扫描）、`sep-per-page.md`／`defect-classes.md`／`sep-detail.md`（61 条逐处）、`readings-4x31.md`、`page-table.md`（逐页对账表）、`gen-tables.mjs`／`splice.mjs`／`patch-doc*.mjs`（表格与补丁的生成件）
- 上游不可缺的件：`.scratch/t387/html/`（31 页产物）、`.scratch/t520/before-house-four.json`、`.scratch/t467/看今日目标进度.html`

## 三 · 本票的判据口径（一句话，供审查者复跑）

`overflow == 0` **且** `clippedCount == 0` 才算横向量绿；分隔符只认**节点级** 0；形状件看 DOM 计数；字号与断点按静态抽取的取值集合判；红绿两向必须都给（真基线红 ＋ 合格样板绿）。

## 四 · 未做项与下一手缺什么

1. **J6／J7 的改前读数待补**：版式探针 `t516-判据-版式.mjs` 未跑通（`Page.enable` 错，见 §一）。补的人按基准件 §一 J6／J7 的「怎么量」列跑通后**回填该表**；补之前不许判绿。
2. **#455（轨迹末点口径）与 #463（TDEE 恒用 70.0kg 常量）的口径裁定未落**：前任三席勘察未覆盖这两个口径面，本票不新开勘察 ⇒ 具名未做项，交编排者转票或补席，**不许实施票顺手改口径**。
3. **本席对工具与测试的独立复跑被闸门锁挡住**：截至本件写成，锁由 `ticket=522`（`dsh --profile visionreview`，`startedAt 2026-09-15T04:40:49Z`）持有 20 分钟以上，本席两次加锁运行均以「等锁超时」失败（runId 与 FAIL 行见 §一）。⇒ 本件引用的红绿两向读数来自**上一席 #516 窗口的持锁运行记录**（runId 具名），本席的独立复跑状态如实登记为**未完成**；接手者取锁空闲时补跑同一命令行即可。
4. **公共层三处缺口只登记不代做**：G-1 641–820 KPI 栅格、G-2 字号下限门、G-3 `clip` 升级为失败判据（落点已写进基准件 §5.3）。
5. **关票与 #161「基准已冻」的 Decisions 由编排者做**，本票不关票；范围外缺陷照「遗留当场开票」另立并回写本票号。
