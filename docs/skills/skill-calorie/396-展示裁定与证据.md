# 396 · 今日缺口展示消歧——裁定与证据（实现席，只动展示、不改算法）

## 一、裁定二选一（回写票面正文）

1. **口径取 TDEE 系**（series 同源，今日值 1687 不变）。BMR 口径值 780 须改算法，
   本票禁区不许（series 算法、`today.ts`、`routes.ts` 一律不动；TDEE 体重恒 70
   老家 parity 不修，也不上屏）。
2. **口径名取老技能 ADR-0013 中文「热量缺口」**（TDEE 口径默认），英文 `TDEE`／`BMR`
   不上屏（`t425` 裁定 1 ＋ `t401c` 探针 A1）。
3. **展示取「口径名与算式同页可见」**，卡片并列不动（不拆并列，避免版式重排与守卫连锁）：
   缺口卡名即口径名，卡说明行即算式前半（缺口等于消耗减摄入），卡徽章即基准差异声明，
   卡说明行括号内即算式后半（消耗等于日常消耗加运动）＋静态值声明（日常消耗取档案静态值）。

旧主页实为标 TDEE 实取 BMR（模板 `home_dashboard.html:654` 栏名「基础代谢 TDEE」，
值走 `df.bmr`），新仓主页走 series TDEE 系（2556）值大一截属口径差异，非算错。
611＝目标 1800 − 摄入 1189 是预算侧「还能吃多少」，与缺口（消耗侧）基准不同——
本次即把这句话写进页里。

## 二、只动其一说明

- 动 `packages/skill-calorie/src/home/homeDocs.ts`，不动 `homeViewParts.ts`。
- 理由：①缺口卡定义（卡名／说明行／方向徽章）住本件，改一处即同页可见；
  ②本件 LF＝334 未超 350 告警线，行内 6 改 6 删、零加行；
  ③`homeViewParts.ts` 已 366 超线，动它必致告警线台账新增陈化，
  而台账文件不在本票写集内，无权同步。

## 三、改动（`homeDocs.ts`，6 改 6 删）

- 卡名：`今日缺口` → `今日缺口（热量缺口）`（仍含老针位子串 `今日缺口`）。
- 卡说明：`消耗 2876` → `消耗 2876（日常消耗加运动。日常消耗取档案静态值）。缺口等于消耗减摄入`。
- 卡徽章（三支）：`摄入比消耗少／多／持平` → 各追加 `。与目标减摄入基准不同`。
- 页脚来源行、`homeViewParts.ts`、算法、目标卡一字未动。

## 四、结构纪律对账（第五步）

- 事前清单：`src/home/homeDocs.ts`（展示文案）／`test/396-*`（新建判据）／
  `docs/skills/skill-calorie/396-*`（本证据）。实际逐行对上，偏差为零。
- 第四步：`homeDocs.ts` LF＝334≤350，未超线，不触发重构。
  台账行 328 vs 实况 334 的 6 行陈化系 HEAD 已存在（`git show HEAD:…` 实测 334），
  本票零加行未扩大。

## 五、读数

- 编译：`tsc -b packages/skill-calorie` exit 0（见 `.scratch/t396/build.log`）。
- 本票判据：`396-gap-caliber.test.mjs` tests 5／pass 5／fail 0（见 `.scratch/t396/test-396.log`）。
- 关联守卫：`t401-可见文本守卫` 2/2、`t401c-页面机器话探针` 5/5、
  `home-lock-374` 主页五条（今日／本周／本月／连续／预算）全绿（见 `.scratch/t396/test-guards.log`：
  tests 16／pass 15／fail 1——唯一红点是 `看今日体重概览` 空库 exit（体重能力，
  本票禁区，`src/weight/` 无改动，dist 体重码即 HEAD，见六节之注）。
- 变异自证：见六节。

## 六、变异自证（故意改坏一行→红→还原→绿）

- 变异：卡名 `今日缺口（热量缺口）` 回退为 `今日缺口`（一行）→ 重建 →
  `396-gap-caliber` 红：`tests 5／pass 4／fail 1`（见 `.scratch/t396/mut-red.log`）。
- 还原 → 重建 → 绿：`tests 5／pass 5／fail 0`（见 `.scratch/t396/mut-green.log`）。
- 还原后 `git diff --numstat` 回到 6／6，LF 钉 334。

注：守卫窗红点 `看今日体重概览 空库 exit 4（实得 exit 0）` 非本票引入——
本票 diff 只碰 `homeDocs.ts` 六行文案，体重视图（`src/weight/`、键 `calorie.view.weight`）
无一行改动亦无共享代码路径；`src/weight/` 在工作区无未提交改动，dist 体重码即 HEAD。
已上报编排者，不越界修别家目录。
