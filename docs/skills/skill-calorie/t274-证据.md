# t274 · 食品库类页（查食品／查食品（按分类）／看食品库（去重）／看食品来源统计）交付与判据记录

> 票 #274（地图 #155 子票）实施席证据件。强制对照基准＝`docs/skills/skill-calorie/t425-融合基准.md`；
> 过程纪律＝`docs/skills/skill-calorie/t155-派单/18-融合标准与过程纪律.md`。
> 老实物正本目录 `D:\2Study\StudyNotes\SKILLS\卡路里\templates\`（**只读**，未执行其中任何脚本）。

## 〇、本次窗口交付面

| 件 | LF | 字节 | sha256（前 16） | 说明 |
|---|---|---|---|---|
| `packages/skill-calorie/src/diet/libraryDocs.ts` | 396 | 19151 | `7bae0a8054a49db2` | 三页装配件（本票唯一作者） |
| `packages/skill-calorie/src/diet/library.ts` | 62 | 3835 | `94b613692ce62cb0` | 四个处理体（加命令原文拼装） |
| `packages/skill-calorie/test/diet-library-t274.test.mjs` | 380 | 24080 | `88bb42cd6dbf8635` | 本票真出口锁（上一席孤儿件改写而来） |

改到别人声明路径的一行也没有。`products.ts`／`libraryPlate.ts` 本票**未动**（判据没要求）。

## 一、四条词逐条真跑（判据 1）

播种：临时库，`user_profile` 1 行 ＋ `nutrition_products` 5 条（含 1 组同名同品牌重复、1 条缺来源、1 条缺分类）。
命令与产物（`SKILLS_DB_PATH=C:\Users\辰辰洋洋\AppData\Local\Temp\t274-final-Kj8e2p`，日志 `.scratch/t274/四页真跑.log`）：

| 唤醒词 | 命令原文 | exit | 产物绝对路径 | 字节 |
|---|---|---|---|---|
| 查食品 | `calorie-cmd-read calorie.view.search --params '{"keyword":"鸡胸"}'` | 0 | `...\calorie_html\查食品_20260915_110128.html` | 68262 |
| 查食品（按分类） | `calorie-cmd-read calorie.view.library --params '{"category":"主食"}'` | 0 | `...\calorie_html\食品库_20260915_110128.html` | 69908 |
| 看食品库（去重） | `calorie-cmd-read calorie.view.dedupe` | 0 | `...\calorie_html\去重报告_20260915_110129.html` | 67852 |
| 看食品来源统计 | `calorie-cmd-read calorie.view.source-stats` | 0 | `...\calorie_html\食品来源统计_20260915_110129.html` | 71835 |

四件产物都是完整文档（`<!doctype html>` ＋ `charset="utf-8"` ＋ `<style>` ＋ `<script>` ＋ `ilife-page`，零未替换标记），
`delivery.mode=file`、`delivery.path` 与 `data.output` 同源、`delivery.bytes` ＝ 落盘 `statSync().size`（测试逐条核过）。

**空库**（同一批，空 `calorie_data.db`，日志 `.scratch/t274/空库真跑.log`）——设计行为，四条约一样：

```
查食品     exit=4 stdout="" stderr="ERR 4: 取数失败（缺失阻断）：食品库无命中：鸡胸"
查食品（按分类）exit=4 stdout="" stderr="ERR 4: 取数失败（缺失阻断）：该分类空库：主食"
看食品库（去重）exit=4 stdout="" stderr="ERR 4: 取数失败（缺失阻断）：食品库空（无去重对象）"
看食品来源统计 exit=4 stdout="" stderr="ERR 4: 取数失败（缺失阻断）：食品库为空（无来源统计）"
```

## 二、十一条裁定逐条（判据 2）

| # | 对上了？ | 读数 |
|---|---|---|
| 1 页面最大一个字写内容／眉标只写「命令名 · 人话归属」／标识符不上屏 | **对上** | 三页眉标＝`查食品 · 饮食`／`查食品库 · 饮食`／`看食品库（去重） · 饮食`，右侧徽章＝`食品库`；可见文本零 `calorie.view`／`calorie_data`／`nutrition_products`／`MEAL_`／`null`／`undefined`／`is_deprecated`（测试逐条断言）。**来源统计页没对上**（无眉标行／无徽章）——它在 `diet/nutritionPortDocs.ts`（#275 的件），本票按红线不碰，已报编排者 |
| 2 结论句紧跟标题、句内含本页读数 | **对上（三页）** | `查食品`＝「在食品库里搜「鸡胸」，找到 1 条。」／`食品库`＝「分类「主食」下有 2 条（库内共 5 条）。」／`去重`＝「食品库 5 条里有 1 组重复，2 条可以合并后再用。」；测试断言 `标题 < 结论句 < 首个区块` 的次序。**来源统计页没对上**（第 4 行是副标题，不含读数），归 #275 |
| 2-补 结论句与来源脚注不走 `notice` 深底块 | **对上（三页）** | 结论句走 `assembleDocPage` 的 B 线 `summary`（`<p class="sub">`）；来源脚注走 `renderCaliberLine`（`.ilife-block-caliber`）。**来源统计页的脚注仍走 `notice()`**（`diet/nutritionPortDocs.ts:60-61`），与 2-补相冲——不在本票写集，已报编排者，编排者记成 #275 的欠账 |
| 3 页内导航 ＋ 口径说明行 ＋ 来源脚注三条恒出、都走公共层 | **对上（三页）** | 导航＝`renderTocBlock`（`<nav class="ilife-block-toc">`，逐卡锚点）；口径行＝`renderCaliberLine`；来源脚注＝`renderCaliberLine('📊 数据来源：本机食品库 · 在架食品共 N 条')`。**口径差异说明**：`shared/sourceLine.ts` 那支收「来源 · 起 → 止 · 共 N 条」，本类页没有窗口、传不出起止日期 ⇒ 未用它，脚注同样只走公共区块 `renderCaliberLine`，页面本地不造样式、不写色值 |
| 4 缺值一律 `—`；空窗出空态句 ＋ 引导句；空档绝不当 0 | **对上** | 四宏量缺值走 `fixed0`／`fixed1` → `—`；品牌空不出那一行（#511 第 74 条）、来源空归「未知」；去重页干净态出空态块（`ilife-block-empty`）＋「无需处理。」；卡片格空列表走 `emptyGuide`（图标＋原因＋「怎么加第一条」）。**命令面的空库＝缺失阻断 exit 4**（01 坑 3 的设计行为），故三页的空态只能从「去重页干净库」这一态真跑出来（测试里有） |
| 5 图表必须可读数／零值不画柱身／点数不足改说明句／按日汇总 > 7 天才出 | **本类不出图（对上）** | §五 第 ⑥ 行是 ○（有内容才出）；查食品／食品库／去重三页主体是卡片格与重复组表，三页都**零图元**。来源统计页有一条按来源分组的条图（属 #275 的件） |
| 6 有目标可比的读数卡带完成度条；无目标走空态 | **本类不适用** | 食品库三点数（命中／在架／重复组）都不是「对目标的完成度」，本类不出任何完成度条；条位（#418）与页面侧无交点 |
| 7 复制区恒双按钮、不出与按钮同名的标题、日志第 4 段＝命令原文 | **对上（三页）** | `copyArea({data, log})` 同时给两段 ⇒ 同行两颗按钮「复制数据 ▾（纯文本／JSON／CSV）」「复制日志」；无同名标题（测试反向断言）；日志六段齐（①场景标识②AI 思考链③数据结构④调用链⑤时间戳版本⑥异常），**第 4 段逐字＝本次命令原文**：`calorie-cmd-read calorie.view.search --params '{"keyword":"鸡胸"}'`（测试 `assertLogCallChain` 逐字比；无参命令就写 `calorie-cmd-read calorie.view.dedupe`，形状与 `routes.generated.ts` 的 `cli` 列一致） |
| 8 老侧手机两段式卡片／四色圆章／双形态表格＝公共层增量，不在页面各写一份 | **只登记不实施** | 本类页的卡片格是老实物自带的**食品卡**（不是「表格行转卡片」那一族）；卡片格局部样式逐值照老实物（`.food-grid`／`.food-card` 一族），色值全部取冻结 token（`--line`／`--card`／`--soft`／`--fg3`／`--blue2`），唯一的断点只把四宏量改两列、不新增第二套色值。裁定 8 的公共层增量本图不列验收项 |
| 9 失败面走新仓整页回执，不引入老侧 `error_receipt.html` | **对上** | 数据缺失走命令面的 `ERR 4: 取数失败（缺失阻断）`（stdout 空、不落半页）；三页产物里 `error_receipt.html` 的任何特有类名 0 次命中 |
| 10 食品库类页用卡片格，不许改成表格 | **对上** | 查食品／食品库两页零 `<table>`（测试反向锁）；卡片格类名 `food-grid`／`food-card` 在场；去重页保留老实物那张重复组表（老实物 `dedupe_report.html:74-79` 就是表）。P0#74「改成表格」的建议未采纳 |
| 11 被别的票钉住的文案不越界改引脚，改用别的槽位 | **本票实测命中一条，已照办** | `test/diet-homogeneity-108.test.mjs:215` 把 #496 的「搜索：鸡胸」逐字钉住（那件不在本票声明路径）。本票按裁定 2 把副标题槽改成结论句 ⇒ **不动那个测试件**，把那串放进读数卡副行承载同一信息。读数：不这么办时该条红（`search 缺：搜索：鸡胸`），这么办后 `diet-homogeneity-108` 与 `diet-library-t274` 两件合跑 22/22 绿 |

## 三、§五 骨架表 第 ⑤ 类那 15 行逐行（判据 3）

● 恒出／○ 有内容才出／— 本类不出。三页＝查食品（search）／食品库（library）／去重（dedupe）。

| 序 | 区块 | 目标 | 实测 | 落点 |
|---|---|---|---|---|
| 1 | 眉标行（命令名 · 人话归属 ＋ 类型徽章） | ● | ● 三页齐 | `assembleDocPage({metaLeft, badge})` |
| 2 | 内容标题（页面名） | ● | ● | `title: '🍱 食物热量查询'`／`'📦 食品库去重'`（照老实物 h1） |
| 3 | 结论句一行 | ● | ● | `summary`（B 线 `<p class="sub">`，普通小字） |
| 4 | 页内导航 | ● | ● | `renderTocBlock`（逐卡锚点 `sec-query`／`sec-count`／`sec-list`／`sec-banner`／`sec-groups`／`sec-advice`） |
| 5 | KPI 读数 | ● | ● | `renderKpiGrid`：匹配 N 条／本页 N 条·库内 M 条／重复组·可合并·库内三枚 |
| 6 | 主体图 | ○ | ○ 三页都不出（无图内容） | — |
| 7 | 字段变更对照（改类） | — | — | 本类不出 |
| 8 | 今日累计 | — | — | 本类不出 |
| 9 | 主表／主列表 | ● | ● | 卡片格（`food-grid`，两页）／重复组表（去重页） |
| 10 | 识别差异标记 | — | — | 本类不出 |
| 11 | 确认后操作行 | — | — | 本类不出 |
| 12 | 空态块＋引导句 | ● | ● | 去重页干净态空态块实测；卡片格空列表走 `emptyGuide`（图标＋原因＋「存食品」引导） |
| 13 | 口径说明行 | ○ | ● 三页都出 | `renderCaliberLine`（食品两页＝营养值每 100 克＋在架口径；去重页＝判定标准） |
| 14 | 复制区（双按钮） | ● | ● | `copyArea({data, log})` |
| 15 | 来源脚注一行 | ● | ● | `renderCaliberLine('📊 数据来源：本机食品库 · 在架食品共 N 条')` |

**来源统计页（⑤ 类第四页）**：第 ②③④⑮ 行当刻**没对上**（它在 `diet/nutritionPortDocs.ts`，属 #275 的件）；
编排者 2026-09-15 裁定 **(b) 切姊妹件 `src/diet/sourceStatsDocs.ts` 归 #274**，本轮随后落地（见 §八）。

## 四、§二 移植清单 第 ⑤ 类（判据 2 的补充口径）

**§二 41 条里 ⑤ 类专属条目 = 0 条**（逐行核过：1–9／13–17 属 ②，18–22 属 ①，23–26 属 ③，27–30 属 ④，
31–33 属 ⑥，34–38 属 ⑦，10／11／12／39–41 是通用条目）。⑤ 类的移植依据在 **§一 用途列**那三行：
`food_search.html`（搜索框＋条数＋食品卡网格）／`dedupe_report.html`（横幅＋3 读数＋重复组表＋建议行）／
`source_stats.html`（2 读数＋按来源分组占比条）；§二的通用条目 10（失败面→裁定 9）、11（来源脚注）、12（复制区）
三页逐条落地情况见 §二 表的裁定 9／3／7 三行。

### §一 第 ⑤ 类三张老实物逐条对账

- `food_search.html`（7913 B／157 行）：搜索框 ✅（`renderParamForm` 回填关键词 ＋ 老实物逐字占位提示「输入食物关键词，如 牛肉 / 鸡胸 / 可乐」）／条数 ✅（读数卡「匹配 N 条」＋副行「搜索：<词>」）／食品卡网格 ✅（类别标签／食品名／品牌／四宏量／来源·更新于）／零 `<table>` ✅／四宏量口径 ✅（热量 `toFixed(0)`、三大营养素 `toFixed(1)`，逐字核 `165 卡`／`31.0 g`）。
- `dedupe_report.html`（6458 B／124 行）：横幅 ✅（两态）／三读数 ✅／重复组表 ✅（食品名·品牌·条数·记录编号；表头 `ID` 经 #496 已改「记录编号」）／处理建议 ✅（两行）／口径行 ✅（老 `.footer .src` 的「名称和品牌都一样的算重复」）。
- `source_stats.html`（5539 B／107 行）：2 读数 ✅／按来源分组 ✅（当刻是条图＋表）——**缺 §五 四行**，见 §八。

## 五、孤儿测试件的处置（判据 4）

`packages/skill-calorie/test/diet-library-t274.test.mjs` 是上一席（2026-09-14 06:55 那次窗口，`.scratch/locks/gate-runs.log`
里能查到它当时的两条 runId）留下的**未提交件**，进场时 8 条测试里 **4 红**（`查食品`／`食品库`／`去重`／`干净库` 四条）。

- **它钉的是什么**：把老实物的**逐字文案**钉进断言——`查询关键词:鸡胸 · 匹配 1 条`、`查询全量 · 匹配 3 条`、
  `食品库（全量）（共 3 条）`、`⚠ 发现 1 组重复，共 2 条冗余记录`、`>冗余行<`、`无重复组（库内 1 条均唯一）`、
  `📊 数据来源：nutrition_products · product_name+brand 分组`、`📂 数据源：calorie_data.db · nutrition_products`。
- **形状对不对**：**形状对、文案过期**。卡片格／零表格／四宏量口径／转义／两态／空库 exit 4 这些形状断言与老实物、
  与 `t425` 裁定 10 完全一致（保留）；但那些逐字文案在 **#496／#511 已按 P0 文本审查改成人话并交付**——
  `.scratch/t155o/text-review-P0.md` 第 6／7／8／45／70 条逐条给出「原文 → 改成」的记录，例如
  `冗余行`→`可合并的重复食品`、`查询全量 · 匹配 4 条`→`全部食品 · 共 4 条`、`查询词:鸡胸 · 匹配 1 条`→`搜索：鸡胸 · 找到 1 条`。
  照旧句钉，等于要求把审查结论吃回去。
- **处置**：**按基准件改它，不恢复旧文案**（照 `t425` §四 裁定 11 的教训「钉形状、不钉文案」）。改写后：
  ① 形状断言全留并加厚（§五 九件骨架、结论句读序、裁定 1 的标识符零出现、裁定 7 的日志第 4 段＝命令原文、
  裁定 10 的反向锁、去重页两态、空库缺失阻断＋前缀）；② 逐字文案只在「它就是老实物形状本身」时钉
  （`165 卡`／`31.0 g` 这类取整口径，`t155-证据/复算-逐页残留探针v3.mjs:181` 已把它登成数据项）。
  **判据**：`node --test packages/skill-calorie/test/diet-library-t274.test.mjs` → `ℹ tests 10 / pass 10 / fail 0`；
  退回旧写法（本席改动全部还原）时其中 3 条必红（见 §六），证明断言真在守。
- **留下的历史**：旧件里的 8 条测试名与 4 条红不再复现；文件名沿用（票面声明路径 `test/*t274*`）。

## 六、不新增红 ＋ 归因自证（判据 5）

同一批命令（`node --test "packages/skill-calorie/test/*.test.mjs"`，持闸门锁），两个状态各跑一次：

| 状态 | 读数 | 日志 | runId |
|---|---|---|---|
| 本票改动在场 | `ℹ tests 1037 / pass 989 / fail 48` | `.scratch/t274/pkg-test-with-t274.log` | `d457c132-60d6-4618-8fb3-18a41c1a065a` |
| 退回 HEAD 旧写法（`libraryDocs.ts`＋`library.ts` 还原，重编后跑） | `ℹ tests 1011 / pass 969 / fail 42` | `.scratch/t274/pkg-test-without-t274.log` | `05771f95-8346-4df3-9286-5a048c168aa3` |

逐条名单对账（`.scratch/t274/归因对账.log`）：

- **两边都红 38 条**。
- **只在「退回旧写法」里红 4 条**：`#274 查食品…`／`#274 食品库…`／`#274 去重…` 三条＝本票断言（退回即红，
  说明断言真在守）；第 4 条 `互联区新鲜（构建期注入可复现…）` 是构建态抖动，当场绿。
- **只在「本票改动在场」里红 10 条**：全部来自 **`test/t272-排行榜页.test.mjs`**——该件 `CreationTime 2026-09-15 10:53:30`，
  **晚于**「退回旧写法」那一跑的起点，`git status` 里是未跟踪的新件；它 `dispatch` 的只有 `calorie.view.ranking`
  （别席的页），与 `libraryDocs.ts`／`library.ts` 无交集。⇒ **本票没有引入任何别处没有的红**。

`ATTRIBUTION`：A-only 集合 = 0 条属本票（10 条全部点名到别席新件）；B-only 3 条属本票断言。**PASS**。

## 七、变异自证（判据 6）

变异点：`src/diet/libraryDocs.ts` 的卡片四宏量口径——`fixed0(it.calories)`（热量取整）改成 `fixed1(it.calories)`。
脚本 `.scratch/t274/mutate.mjs`（逐文件点名，不整目录还原），还原后把 mtime 推到当前（#393 的 Windows mtime 陷阱）。

| 步 | dist/diet/libraryDocs.js sha256（前 16） | 测试读数 |
|---|---|---|
| 基线（未变异） | `307be04c60703c0f` | `ℹ tests 10 / pass 10 / fail 0` |
| 改坏一处后重编 | `d39248b5d542e18c` | `ℹ tests 10 / pass 8 / fail 2`：`#274 查食品：…`／`#274 食品库：…`，断言原文 `查食品 热量未取整（老实物 toFixed(0)）` |
| 逐文件点名还原 ＋ 重编 | **`307be04c60703c0f`（回到基线）** | `ℹ tests 10 / pass 10 / fail 0` |

`STEP dist-pollution-check polluted=[False]`（还原后 dist 指纹 ≠ 变异态、且 == 基线态）。

## 八、告警线台账与超线报警（判据 7）

- `pnpm gen:check` → `GEN-CHECK PASS：键 127（写 46 ＋ 读 81）；能力 10 个声明 127 条，未搬迁清单 0 条`（生成物六件本票未写、未 `git add`）。
- **超线报警（必报五步第四步）**：**「已超线，需要根据规则进行重构。」**
  `src/diet/libraryDocs.ts` 实测 **396 LF**（> 350）。超因：这一件同时住三页的整页装配 ＋ 三页共用的页框
  （眉标／结论句／导航／口径行／复制区双按钮／来源脚注）＋ 食品卡片格与两套页内局部样式 ＋ 老实物对照与裁定出处的逐段注释。
  拆法＝按「卡与样式」切姊妹件 `diet/libraryCard.ts`（`FOOD_CSS`／`DEDUPE_CSS`／`renderFoodGrid`／`fixed0`／`fixed1`），
  三页装配与页框留本件。**本次先不拆**：本票声明路径只给了 `libraryDocs.ts`／`library.ts`／`libraryPlate.ts`／`products.ts` 四件，
  新建件要走编排者派单。台账行已按实况写（挂号值 `—` 不回改，结论列写全超因与拆法）。
- 同步与复跑：`--sync --dry` 演练（`SYNC-PLAN 改=1 增=0 删=0`，只报别席 `src/photo/helpCenter.ts` 陈化）→ `--sync`（`SYNC-VERIFY ok`）
  → 复跑 `node packages/skill-calorie/scripts/check-warning-line.mjs` = **`RESULT: 67/67` ＋ `PASS: 告警线台账齐全且与实况一致`**。

## 九、没做的与为什么 · 遗留出口

1. **来源统计页**：当刻住 `diet/nutritionPortDocs.ts`（#275 的件）——本席按红线**一行没碰**，已报编排者；
   编排者 2026-09-15 裁 **(b)**：切姊妹件 `src/diet/sourceStatsDocs.ts` 归 #274，声明路径临时扩给本席
   （新件 ＋ `diet/index.ts` ＋ `diet/nutritionPortDocs.ts` 只许删那一支与改 import）。落地读数见
   `docs/skills/skill-calorie/t274-来源统计页搬迁.md`。
2. **事实订正两条**（派单文与基准件）：① 02 文件 #274 专属约束② 与 `t425` §九 第 1 条写「来源统计住
   `src/render/trendMiscPortDocs.ts`」——当刻事实是 `diet/nutritionPortDocs.ts`，`trendMiscPortDocs.ts` 里
   `SourceStats`／`source-stats` 零命中；② `diet/library.ts:4` 的「装配属 #275 的件」已按当刻事实改写（02 附表的顺手同步项）。
3. **#275 的欠账（不在本票写集，只报不动）**：`diet/nutritionPortDocs.ts:60-61` 的来源脚注走 `notice()`（深底块），
   与裁定 2-补相冲；编排者已认账，派 #275 时带给它。
4. **未做**：手机两段式卡片／餐别四色／双形态表格（裁定 8，公共层增量，本图只登记）；
   老实物 `food_search.html` 的**客户端实时过滤**脚本（新侧一命令一页、产物是静态文件，按裁定 8 同族「先登记不实施」）。
