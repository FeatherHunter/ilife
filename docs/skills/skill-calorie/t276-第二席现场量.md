# t276 · 第二席现场量（2026-09-15，作者席位自量，非受益者口径）

> 本席接手时前任已停手（提交 `ad90751`／`3592b54` 在仓，另有 3 件未提交的工作区改动）。
> **前任的未提交改动＝无主 diff**：下面全部按当刻盘上实况重新量，不引用前任读数当既成事实。
> 一切编译／跑产物／提交经 `node tooling/run-locked.mjs --ticket 276 --poll-ms 2000 --max-wait-ms 900000 -- <命令>`。

## 一 · 现场（当刻 15:32–15:45 实测）

1. **在仓提交**：`ad90751`（`src/diet/routes.ts` 的 `查高热量排行` 补 `category:"high_calorie"` ＋
   `test/diet-ranking-route-t276.test.mjs` 5 用例 ＋ 四处诊断件）、`3592b54`（证据回填）。
   命令面那 6 条的六处登记（`keys.ts`／`combos.yaml`／`routing.ts`／`cmd_read.ts`／`write.ts`／命令表）
   在 HEAD 里查得到（`calorie.product.import`／`calorie.view.batch-import-preview` 等），属前一阶段的在仓产物。
2. **未提交的工作区改动（3 件，全部为 ` M`＝只在工作区，没进索引）**：
   | 件 | 净增 | 内容 | 谁写的 |
   |---|---|---|---|
   | `src/home/routes.ts` | ＋24/−14 | ①5 条餐别词 `cli` 各加 `meal`（行内有 `#276` 注释）；②**另有** 5 条主页词各加 `section` | ①本票 ②**不是本票**（`#401m`） |
   | `src/home/today.ts` | ＋33/−3 | ①`viewDietOverview` 的 `meal` 支（`#276` 注释）；②**另有** `section` 取档与 `HOME_SECTIONS` 校验、`buildHomeDoc(h, section)` | ①本票 ②**不是本票**（`#401m`） |
   | `src/diet/index.ts` | ＋8/−1 | 第四道门 `buildMealDistributionView` 转出（＋类型再导出） | 本票 |
   **⇒ 干净度判定：`home/routes.ts` 与 `home/today.ts` 里混着别人的 hunk（`#401m`）**，
   按派单「有别人 hunk 就别 add」——**这两件本席不 `git add`、不提交**，报编排者。
   旁证：`src/home/homeViewParts.ts`（`#401m` 新建件，未跟踪）＋`src/home/homeDocs.ts`／`commands.ts` 同属那条线；
   15:32:16 起有他票（`ticket=517`）持锁，15:32:25 有人在跑生成器（`src/triggers/routes.generated.ts` 当刻被重写）。
3. **本席自己量的基线红名单**（免锁 `npx tsc --noEmit -p packages/skill-calorie`，15:33）：
   - `packages/skill-calorie/src/weight/history.ts(743,26)` TS2345（派单已知，体重域）
   - `packages/skill-calorie/src/weight/log.ts(326,26)` TS2345（`DocPageInput.subtitle` 缺失，体重域）
   **两条都在本票声明路径之外 ⇒ 记「范围外发现」，不修、不重跑**；判据是「我这条路不新增红」。
4. **前任证据件的完成度（逐条核，不采信结论）**：
   - `t276-餐别五条证据.md` §三（5/5）与 §四（6/6）**有机器读数支撑**：`.scratch/t276/d5-meal-five.out.txt`
     第 4–8 行逐条 `FIVE exit=0 页头=餐别分布 bytes=71331／71337／70825／70824／77408`、
     第 12 行 `NOMEAL exit=0 页头=饮食总览`。**这两项本席复核为真**（读数行在盘上、形状自洽）。
   - **§五的变异表（BASE／RED／GREEN）当刻拿不出支撑**：`d6-meal-five.out.txt`／`d6-mutate-meal.out.txt`／
     `mutate-meal.log` 三处都是 `BASE-NOT-GREEN`＋`five=NA all=NA digestLines=0`（当刻 dist 生成物损坏、探针连 import 都过不去）。
     ⇒ 这一表**本轮由本席重跑补上**（见第三节）。
   - **§六自认的两条缺口属实**，且本席另发现一条**不自洽**：`d5-meal-five.out.txt` 第 19 行只有
     **一条** `calorie.view.diet` 摘要（`看昨日饮食	e9fdc68ff031bf9b	77321`），而第 20 行写 `OTHER-COUNT 7`；
     同一脚本的 `digest-*.txt` 落盘件在盘上也**不存在**。⇒ 判据③的「逐条数字」在盘上**不成篇**（这是本席要补的活）。

## 二 · 门禁归因（窗口 e1，`runId=b6da6cde-fa8b-449c-9033-8b074d644ee8`，`waitedMs=66231 exit=1`）

| 步骤 | 退出码 | 归因 |
|---|---|---|
| `tsc -b packages/skill-calorie` | 1 | **只有两条红**，都在本票声明路径之外：`src/weight/history.ts(743,26)`、`src/weight/log.ts(326,26)`（`DocPageInput.subtitle` 缺失）。**记范围外发现**；本票这条路不新增红 |
| `word-family`（本席探针，第一次） | 2 | **本席探针自身的缺陷**：带占位符的记录 `看某段时间饮食` 会让 `windowRange()` 里的 `fail()` 当场结束探针进程 ⇒ 已修，见第三节 |
| `gen-check` | 1 | `GEN-PAIR FAIL：src/weight/commands.ts 的编译产物与印记对不上（dist 变了而印记没重打）` ⇒ **范围外（体重域）**，按派单记「范围外＋点 #540」，**不反复重跑** |
| `test-t276`（`diet-ranking-route-t276.test.mjs`，5 用例） | 0 | 本票自己的接线回归门在改动后仍绿（`test-t276` 逐条用例全 `✔`，末行 `exit=0`） |
| `check-warning-line --dry` | 1 | 三条 `台账陈化` 全在他人在途件上：`src/render/sportDocs.ts`（417→423）、`src/weight/volatilityDoc.ts`（406→411）、`src/home/homeViewParts.ts`（358→360，**#401m 的新件**）⇒ **范围外**；台账**无「漏报」行**＝本票路径没有新增超线件 |

## 三 · granted 窗口：判据③ 的逐条数字（本席自量）

**窗口**：`runId=b1efcee7-a014-4792-83a4-c8e207f13d03`（`waitedMs=0`）；步骤 `build` exit=1（＝只有第二节那两条范围外红，**本票路径零红**）、`word-family` exit=0。
**探针**：`.scratch/t276/word-family.mjs`；**读数件**：`.scratch/t276/word-family-run.out.txt`（批次日志 `.scratch/t276/e2.log`）。
**种库法**：`calorie.view.diet` 一族**每一条词按它自己的窗口单独种一份隔离库**——窗口不是手抄的，是拿命令自己的
`windowRange()` 从该条记录的 `cli` 参数里算出来的；窗口内**每天四餐**。库落系统临时区（`…\t276-wf-*`），用完即删，
**删前路径守卫**（必须以系统临时区为前缀且含 `t276-wf-`，否则拒绝递归删）。
**指纹**＝产物 HTML 抹平 `YYYY-MM-DD HH:MM:SS` 与 `YYYYMMDD_HHMMSS` 两形后的 sha256 前 16 位。

**BASE：`RESULT-BASE: 14/14`**（带占位符的 `看某段时间饮食` 按设计 exit 2，不入这道门）

| 词 | 窗 | exit | 页头（含落地的窗口） | bytes | 指纹 |
|---|---|---|---|---|---|
| 看今日饮食概览 | 今日 | 0 | 饮食总览 2026-09-15 ~ 2026-09-15 | 77337 | `63615ba8c5330e42` |
| 看昨日饮食 | 昨日 | 0 | 饮食总览 2026-09-14 ~ 2026-09-14 | 77337 | `da891df95cf1224a` |
| 看本周饮食 | 本周 | 0 | 饮食总览 2026-09-14 ~ 2026-09-15 | 82057 | `e4e09cea4a0814f0` |
| 看上周饮食 | 上周 | 0 | 饮食总览 2026-09-07 ~ 2026-09-13 | 98105 | `5fc6cc930d159bc7` |
| 看本月饮食 | 本月 | 0 | 饮食总览 2026-09-01 ~ 2026-09-15 | 123798 | `d59ca618156abe52` |
| 看上月饮食 | 上月 | 0 | 饮食总览 2026-08-01 ~ 2026-08-31 | 156838 | `ad91fd0fcb6884bd` |
| 看最近 7 天饮食 | 7d | 0 | 饮食总览 2026-09-09 ~ 2026-09-15 | 98105 | `429f5af40741b38e` |
| 看最近 30 天饮食 | 30d | 0 | 饮食总览 2026-08-17 ~ 2026-09-15 | 159137 | `c56e7a5b328280a3` |
| 看饮食总览 | 7d | 0 | 饮食总览 2026-09-09 ~ 2026-09-15 | 98105 | `429f5af40741b38e` |
| 看某段时间饮食 | custom | 2 | —（`ERR 2: start 非法（须 YYYY-MM-DD）：<开始日期>`，按设计） | 0 | — |
| 看早餐（最近 7 天） | 7d | 0 | 餐别分布 2026-09-09 ~ 2026-09-15 | 73881 | `7e199f6990a4b9fe` |
| 看午餐（最近 7 天） | 7d | 0 | 餐别分布 2026-09-09 ~ 2026-09-15 | 73881 | `256de9932daa08c5` |
| 看晚餐（最近 7 天） | 7d | 0 | 餐别分布 2026-09-09 ~ 2026-09-15 | 73881 | `450532cc4f630f8b` |
| 看加餐（最近 7 天） | 7d | 0 | 餐别分布 2026-09-09 ~ 2026-09-15 | 73871 | `7f5136de3dc9a902` |
| 看全部餐别分布（最近 7 天） | 7d | 0 | 餐别分布 2026-09-09 ~ 2026-09-15 | 88580 | `b951f0f97248b798` |

**变异两行（产物级：只改 `dist/home/today.js` 这一个忽略件，把「只在给了 `meal` 时才走」那一支换成常 `undefined`）**：

```
RESULT-C3: 不吃meal记录=9 指纹与页头全同=9/9 ⇒ PASS      ← 红：不吃 meal 的 9 条一字不动
RESULT-MUT: 吃meal的=5 变异后掉出（指纹／页头变）=5/5 ⇒ 承重（改坏即红）   ← 这 5 条全数回落到「饮食总览」页，指纹与「看饮食总览」同值 429f5af40741b38e
MUT-RESTORED sameAsBefore=1 sha=85c48f0725912dad        ← 还原一致（逐字节写回）
RESULT-GREEN: 还原后与 BASE 逐条一致=1 dist sha 还原一致=1   ← 绿：还原后 15 条逐条与 BASE 同
```

**判读**：这处接线**承重**（改坏即红），且**只影响吃 `meal` 的 5 条**——其余 9 条记录的产物**页头、字节数、指纹三项全同**（判据③）。
**前任 §五 那张表（BASE／RED／GREEN）在盘上没有支撑**（`d6`／`d7` 两次都 `BASE-NOT-GREEN`、`digestLines=0`）；上表是本席在同一个窗口里拿到的替代读数。

## 四 · 提交与未做项（本席收尾）

**已提交**：`ac9bed7`「feat(276): 判据③ 逐条数字与变异读数（第二席）＋ 来源统计接线门收口」——
只含 4 件：`packages/skill-calorie/src/diet/index.ts`、`packages/skill-calorie/test/diet-ranking-route-t276.test.mjs`、
`docs/skills/skill-calorie/t276-第二席现场量.md`、`docs/skills/skill-calorie/t276-餐别五条证据.md`。
提交脚本 `.scratch/t276/commit-t276-e3.mjs` 逐行打 `SUBMIT-STAGED`（暂存区**没有**别人的件）；**未推远端**。
同一锁窗内：`test-t276` exit 0（5＋1 组用例）、`tsc -b` exit 1（只有第二节那两条范围外红）。

**未提交、报编排者另裁**：`packages/skill-calorie/src/home/routes.ts` 与 `src/home/today.ts`——本票的 `meal` 改动
（5 条餐别词的 `cli` 各加 `meal`；`viewDietOverview` 只在给了 `meal` 时才走那一支）**与另一条线的 `section` 改动
（`#401m`：5 条主页词分档 ＋ `HOME_SECTIONS` 校验 ＋ `buildHomeDoc(h, section)`）同处一件**，按路径分不开
`git add`。按派单「有别人 hunk 就别 add」，本席**这两件一件未动、一件未提交**；同线的未跟踪件
`src/home/homeViewParts.ts`（`#401m` 新建）也在盘上。⇒ 这两件的落地要等 `#401m` 那条线提交之后再收，
或由编排者授权按 hunk 收窄。

**本席未做（下一手缺什么）**：
1. **`meal` 那一支没有常规回归门**：派单的「只做三件」不含新建测试，故当刻这条路的门只有本席的产物探针
   （`.scratch/t276/word-family.mjs`）。下一手若要把它变成常规门，需落一件 `test/*t276*` 并再过一次锁窗。
2. **范围外红一律未修**：`src/weight/history.ts(743,26)`／`src/weight/log.ts(326,26)` 两条编译红、
   `GEN-PAIR FAIL`（指 `src/weight/commands.ts`，另见 `#540`）、台账三条陈化件（`sportDocs.ts`／`volatilityDoc.ts`／`homeViewParts.ts`）。
3. **生成器未跑**（派单只放行 `gen:check`）：`src/triggers/routes.generated.ts` 当刻在别人手里
   （15:32 有他票的重生成动作在场），故 `gen-check` 的 `GEN-PAIR FAIL` 不与生成态混着说。
