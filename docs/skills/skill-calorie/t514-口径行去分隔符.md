# t514 体重口径行去分隔符 — 让路对齐结论（#505 按豁免保留｜，选项 a 作废）

> 结论前置：`#505` 状态 OPEN 且按豁免保留了 `｜`，本票选项 (a) 作废。
> 本票降级为判据对齐动作：`log.ts` 一行不动（`git status` 干净），只落对齐用例与本证据件。
> 产物口径行已由公共层结构槽还债（`renderCaliberLine` 按 `｜` 拆三枚 `<span>`），
> 票面验收 1 与收口判据（命中表只剩 L6 标题误报）当刻已成立。

## 一、现象逐字（票面抄，不转述）

- 产物旧址 `D:\ilife\.scratch\t375\products\看今日体重概览.html:1755`，票面原文逐字：
  `📊 数据来源：体重记录 ｜ 窗口 2026-09-01 ~ 2026-09-07 ｜ 共 7 条`
  （`｜`(U+FF5C) ×2＝三段并列：来源／窗口／条数）。
- 当刻实盘 `:1839`（`Select-String ilife-block-caliber` 实测）：
  `<p class="ilife-block-caliber"><span>📊 数据来源：体重记录</span> <span>窗口 2026-09-01 ~ 2026-09-07</span> <span>共 7 条</span></p>`
  可见文本 `📊 数据来源：体重记录 窗口 2026-09-01 ~ 2026-09-07 共 7 条`，`HASSEP=false`，`SPANS=3`。
- 出处 `packages/skill-calorie/src/weight/log.ts:265-266`（有窗页）与 `:342`（空窗页 `共 0 条`），
  出片口 `:243` 的 `renderCaliberLine('📊 数据来源：' + sourceText)`。产物不许手改（本票未改）。

## 二、#505 落点状态（开工第一件事，只读）

- `gh issue view 505`：`number=505 state=OPEN labels=[wayfinder:task]`，
  标题 `场景 03 体重 · 写后回执＋今日盘（11 页）：手机端适配 ＋ 去分隔符的形状化`。
- 票面「允许保留」段逐字：`日期区间的 ~、页脚口径行的 ｜（全仓同形元信息件）、机器面（复制载荷与命令原文）`。
- `gh issue view 505 --comments`：无评论（落点以票面＋盘上注释为准）。
- `#508 §三③「现存豁免（不许扩大）」`逐字含 `页脚口径行的 ｜（全仓同形元信息件…）`（`gh issue view 508` 实测）。
- `src/weight/weightUi.ts:22` 逐字：`允许保留的分隔符（不是正文串）：日期区间里的 ~；页脚口径行的 ｜（renderCaliberLine 全仓同形）`。
- `src/weight/log.ts:269` 逐字：`条数两处同值（页脚口径行另有窗口与条数，属允许项 §二）`。
- `git log --oneline -5 -- packages/skill-calorie/src/weight/log.ts` 首行 `465a8a5 fix(542)`，
  含 `4bf973a feat(505)`（#505 形状化已落盘）；`git status --porcelain packages/skill-calorie/src/weight/log.ts`
  当刻输出为空（干净，未动）。
- ⇒ **#505 按豁免保留了 `｜`**。按 #514 票面让路规则：`选项 (a) 作废，只剩选项 (b) 的判据对齐动作`，
  `不许抢 #505 的窗口`。本票 `log.ts` 两处（`:265-266`、`:342`）一行不动。

## 三、探针读数（两处，机器原样）

- 探针口径 `.scratch/sep-audit/probe.mjs:39`：`PARALLEL` 含 `｜`；`:28-34` R1 含 `·`／R2 含 `；`／R3 ≥3 段并列。
- 当刻实测 `node .scratch/sep-audit/probe.mjs .scratch/t375/products/看今日体重概览.html`：
  `产物: .scratch/t375/products/看今日体重概览.html 可见文本行: 2435`
  `【行级】含 · : 1 行 含 ；: 0 行 ≥3 段并列: 0 行 命中合计: 1 行`
  `L 6 R1 卡路里·体重`
  `【节点级】含 · : 1 处 含 ；: 0 处 ≥3 段并列: 0 处 命中合计: 1 处`
  `L 6 # 1 R1 卡路里·体重 <>`
  `EXIT=1`（只剩 L6 `<title>卡路里·体重` 已知误报，票面逐字：`本票不改标题、也不借它放宽判据，
  收口判据是「命中表只剩 L6 标题误报」`——当刻即此态，口径行那处已从命中表消失）。
- 口径行单串读数（对齐用例实测）：`T514-CALIBER VIS=📊 数据来源：体重记录 窗口 2026-09-01 ~ 2026-09-07 共 7 条`
  `T514-CALIBER HASSEP=false`，`parallelRun=null`（R3 未命中）。
- 对齐机理：公共层 `packages/base-render/src/blocks.ts:285-294` 的 `renderCaliberLine`
  按 `CALIBER_SEP='｜'` 拆段逐段一枚 `<span>`（签名一字不改），分隔由版式承担
 （`:1302-1308` 的 `flex`＋`border-left` hairline）。源文的 `｜` 不再进产物文本，
  故判据与豁免在此对齐：源侧豁免保留输入字符，产物侧文本零命中。公共层改动已在仓内
 （`1712317 fix(base-render): 数据表窄屏行卡化 ＋ 口径行分隔形状化`），本票一行不碰公共层。

## 四、三段事实去处表（删符号不删事实）

| 段 | 源文（log.ts） | 产物去处（:1839 三枚 span） |
|---|---|---|
| 来源 | `体重记录` | `<span>📊 数据来源：体重记录</span>` |
| 窗口 | `窗口 2026-09-01 ~ 2026-09-07`（`~` 非并列分隔符，保留） | `<span>窗口 2026-09-01 ~ 2026-09-07</span>` |
| 条数 | `共 7 条`（空窗页为 `共 0 条`） | `<span>共 7 条</span>` |

删掉的只有两个 `｜` 字符（改由版式分隔承担），事实零删除。

## 五、测试（持锁，机器读数）

- 用例 `packages/skill-calorie/test/514-caliber-exemption.test.mjs`（新建，4 用例， hermetic＋实盘抽查）：
  ① `log.ts` 两处口径行按豁免保留 `｜`；② 豁免条款仍在；③ 产物口径行无分隔符且三段俱全；
  ④ 变异自证（件内）。
- 持锁绿跑 `node tooling/run-locked.mjs --ticket 514 -- node --test …514-caliber-exemption.test.mjs`：
  `T514-SRC log.ts ｜字符数=4 口径行定义=2处 豁免标记=在`
  `T514-EXEMPT weightUi.ts 页脚口径行｜豁免=在`
  `T514-CALIBER VIS=…共 7 条 / HASSEP=false / SPANS=3 FACTS=来源/窗口/条数俱全 R3=null`
  `T514-PRODUCT VIS=…共 7 条`
  `T514-MUT 塞｜三段串 R3=｜x3 必红 还原 R3=null 必绿`
  `tests 4 / pass 4 / fail 0`，`RESULT: ticket=514 runId=fd84b519-9f84-4549-8bb6-31d4301b4f68 waitedMs=5013 exit=0`。
- 持锁编译 `node tooling/run-locked.mjs --ticket 514 -- npx tsc -b packages/skill-calorie`：
  无输出，`RESULT: ticket=514 runId=aae0ff83-0eec-4577-96b4-bb599a1a3469 waitedMs=120198 exit=0`。

## 六、变异自证两行（机器读数，log.ts 未动）

- 件内变异（用例④）：干净串 `HASSEP=false R3=null` 必绿；塞回 `｜` 三段串
  `📊 数据来源：体重记录 ｜ 窗口 2026-09-01 ~ 2026-09-07 ｜ 共 7 条`
  则 `assertCaliberClean` 必红且 `parallelRun=｜x3`；还原必绿。读数 `T514-MUT 塞｜三段串 R3=｜x3 必红 还原 R3=null 必绿`。
- 件外变异（故意改坏一行→红→还原→绿，不碰 `log.ts`）：
  把用例 `assertCaliberClean` 的期望由 `false` 改成 `true` 一行，
  持锁跑 `RESULT: ticket=514 runId=b132aa8a-fc27-4133-8ed4-995b46a247ec waitedMs=50115 exit=1`
 （`tests 4 / pass 2 / fail 2`，`false !== true` 必红）；改回 `false` 后
  `RESULT: ticket=514 runId=fd84b519-9f84-4549-8bb6-31d4301b4f68 waitedMs=5013 exit=0`
 （`pass 4 / fail 0` 必绿）。

## 七、结构纪律与写集对账

- 结构纪律 `docs/agents/structure.md` 管辖：`测试文件、页面模板、构建产物、生成的资产、文档` 不管。
  本票实际只碰测试（`test/514-*` 新建）与文档（`docs/…/t514-*`），`src/` 一件未动，
  故五条铁律＋必报五步不触发；超线报警不触发（未改 `src`，`log.ts` LF=402 的既有挂号行不动）。
- 派单声明写集 vs 实际：
  `packages/skill-calorie/src/weight/log.ts（口径行两处）`——按让路规则**未动**（选项 a 作废）；
  `packages/skill-calorie/test/514-*（新建）`——实落 `514-caliber-exemption.test.mjs` 1 件；
  `docs/skills/skill-calorie/514-*`——实落 `t514-口径行去分隔符.md`＋`t514-run.log`（文件名均带 514）。
  禁区（`weight/` 其余件、公共层、`home/`）一行未碰。偏差仅一处：`log.ts` 声明动而实际未动，
  理由即让路规则（撞车就让路，让路也是结论），偏差为有意对齐。
- 提交信息中文，格式如 `让路(514): 体重口径行按豁免保留｜＋判据对齐全绿`；只提交上述三件。

## 八、遗留出口

- 豁免条款（#508 §三③、#502–#505 允许保留段、`weightUi.ts:22` 等）的收回或改写属选项 (b) 的后半，
  需跨票裁定（判据与豁免自相矛盾的根治），不在本票（本票只做落盘一半：钉住现状＋产物已绿）。
- 全仓其余 `｜` 落点（#514 票面列的 `body/receipt.ts:330` 等）归 #508 家底清单，本票只认体重概览这一页。
- 口径行结构槽已由公共层 `renderCaliberLine` 承担（本票外已落定），`log.ts` 源文保留 `｜` 作拆段输入符，
  不再另开公共层票。
