# #451 记录级明细族融合 · 证据

> 票面：地图 #156（卡路里场景 04 运动）· 页面族第 2 票 · 形状照已验样板 #423。
> 交付面：实现 `packages/skill-calorie/src/exercise/records.ts`、判据 `packages/skill-calorie/test/
> exercise-records-fusion-451.test.mjs`、本件、样例 `.scratch/t451/out/`（不进版本库）。
> 命令与路由（`src/exercise/commands.ts`／`routes.ts`）本票未动；`packages/skill-calorie/AGENTS.md` 台账本票未改。

## 一、判定

**复核 ＋ 补证**（接手时前席已写完融合实现，本席不重做）：复核读数、跑通判据、补变异两行与告警线读数、
补本证据件，并提交入库。

### 三行量现场（接手当刻）

1. **改动清单**（`git diff -- packages/skill-calorie/src/exercise/records.ts`）：件头注释改成融合版式口径；
   新增常量 `EYEBROW`／`PAGE_TITLE`／`SOURCE`／`COPY_KEY`／`ROW_LIMIT`／`COLUMNS`；删旧 `modeText`；
   新增装配件 `shell`／`shownCount`／`captionOf`／`subtitleOf`／`caliberText`／`filterText`／`truncationText`／
   `windowCard`／`figureCard`／`detailCard`／`emptyCard`／`sourceCard`；`buildRecordsDoc` 整页重写
   （页内导航 ＋ 口径行 ＋ 四张卡 ＋ 三格式复制）；复制区由 `dataCopyArea` 换成 `copyArea`；装配加 `printable: true`。
2. **逐件统计**：`records.ts` 199 行（LF，HEAD）→ 303 行（LF）／13918 字节；`git diff --stat` 记 165 增／61 删＝226 行变化。
   判据件 342 行／21849 字节（新，接手时未跟踪）；本证据件新增；样例 7 件在 `.scratch/t451/out/`。
3. **对上票面**：六条逐条对得上（下表）。

| 票面条 | 落点 | 读数 |
|---|---|---|
| ① 八列明细表 | `COLUMNS` ＋ `detailCard` | 表头逐字逐序 `["日期","类型","分类","时长","消耗","距离","心率","备注"]`，判据 8／8 通过 |
| ② 截断必须明示 | `captionOf`／`subtitleOf`／`truncationText` | 52 条时表标题「共 52 条，本页显示 50 条；每页最多 50 条」，可见行 50；未超上限时无截断口径句 |
| ③ 筛选口径句 | `filterText`（走 `renderCaliberLine`） | 有备注／力量／有氧各命中 1 次、互不串（判据逐条 `countOf`）；无筛选页 0 次 |
| ④ 页头写人话 | `EYEBROW`／`PAGE_TITLE` | 眉标「运动 · 记录级明细」；三类工程话全文 0 命中 |
| ⑤ 融合构件接线 | 导航／来源脚注／可打印／复制 | 锚点 href 全有落点；`ilife-page-printable` 落版面根 ＋ `@page printable`；`data-fmt` 顺序 `text,json,csv` |
| ⑥ 空态带下一句话 | `emptyCard`（走 `shared/emptyGuide.ts`） | 「说「记运动」就能记下第一条」；力量／有氧各自指名唤醒词 |

## 二、5 条真跑读数（冻结参数，走真正的交付出口）

票面「5 条词各跑一次」实际是「**5 组冻结参数**各跑一次」；唤醒词→命令接线归 #266。
参数取 `src/triggers/scene-04-exercise.ts` 冻结表逐字，全部 `exit 0`、`assertDocPage` 过、信封交付路径＝本次 `--html`。

| 词（冻结参数） | 参数 | exit | 产物字节 | 样例文件 |
|---|---|---|---|---|
| 看今日运动 | `{"window":"今日"}` | 0 | 65711 | `D:\ilife\.scratch\t451\out\01-today.html` |
| 看昨日运动 | `{"window":"昨日"}` | 0 | 65717 | `D:\ilife\.scratch\t451\out\02-yesterday.html` |
| 看运动记录（有备注） | `{"window":"7d","hasNote":true}` | 0 | 66278 | `D:\ilife\.scratch\t451\out\03-hasnote.html` |
| 看运动记录（按力量筛选） | `{"window":"7d","category":"力量"}` | 0 | 65814 | `D:\ilife\.scratch\t451\out\04-strength.html` |
| 看运动记录（按有氧筛选） | `{"window":"7d","category":"有氧"}` | 0 | 65815 | `D:\ilife\.scratch\t451\out\05-cardio.html` |

边界两件：52 条超上限 `06-truncated.html`（exit 0／88184 字节，可见 50 行）；未超上限 `07-not-truncated.html`
（exit 0／66634 字节，可见 3 行）。空日子查「今日」时真正的交付出口按缺失阻断 **exit 4**（`08-empty-blocked`），
故空态由装配层 `buildRecordsDoc` 构造，走公共层空态构件 `ilife-empty`。

## 三、先红后绿

- **本席接手时判据首跑即为绿**（`runId=ed82395d…` exit 0，8／8），故本席没有「补齐前的红」可记。
- **上游轨迹（前席在册，非本席读数）**：`runId=0fc522cb…` exit 1 → `runId=d3acec3f…` exit 0（13:52:33Z）——红转绿发生在接班之前。
- **本席的先红后绿＝变异两行**（见下节）：变异态判据红（exit 1）→ 写回原字节后判据绿（exit 0，8／8）。

## 四、变异两行（设计红，`MUT-RUN` 口径，不经 `run-locked`、无 runId）

四步序逐行走完：变异 → `pnpm build` → 跑判据（红）→ 写回原字节 ＋ 刷 mtime → `pnpm build` → 跑判据（绿）。
判据读 `dist/`，两次变异后都重编译（编译 exit 0；红读数是重编译后的 `dist` 产生的，不是假绿）。

| 变异 | 变异文本 | 编译 | 判据（红） | 还原后编译 | 判据（绿） |
|---|---|---|---|---|---|
| ① 去掉截断明示 | 表标题句删「；每页最多 ' + ROW_LIMIT + ' 条」 | exit 0（3813 ms） | **exit 1**：`未超上限 表标题没写明行数上限：运动记录明细（共 3 条，本页显示 3 条）` | exit 0（2969 ms） | exit 0，8／8 |
| ② 表头删一列（距离） | `COLUMNS` 去掉 `'距离'` | exit 0（3500 ms） | **exit 1**：`看今日运动 八列表头不是逐字逐序：["日期","类型","分类","时长","消耗","心率","备注"]` | exit 0（3206 ms） | exit 0，8／8 |

**源码 sha256 同值读数**：变异前／还原后同为 `B318105BA58B2F4A1AEAE1B8B23AE1C982E42B7A9C28289E197403C44130BD4C`（13918 字节，git blob `b773a43`）；
还原用另存原字节写回（`[IO.File]::WriteAllBytes`），未用 `git checkout --`／整目录还原；还原后刷 mtime，变异态不单记 sha256（以判据红＋重编译取证）。

## 五、工程话三条全文断言 ＋ 告警线读数

- 判据内三条全文断言：`calorie.view.`／`\bt\d{3}\b`（票号样式）／「移植」各命中 0（`assertFusion`，另在空态页重复一遍）。
- 本席独立复算七件产物：`01-today`／`02-yesterday`／`03-hasnote`／`04-strength`／`05-cardio`／`06-truncated`／
  `07-not-truncated` 逐件 `key=0 ticket=0 移植=0`。
- 告警线：`node packages/skill-calorie/scripts/check-warning-line.mjs` → **exit 0**、`RESULT: 56/56`、
  `PASS: 告警线台账齐全且与实况一致`（台账 25 行；扫描面 282 件，超线 24 件）。`records.ts` 303 行 < 350，
  不触发必报五步的第四步，台账无需同步，`AGENTS.md` 本票未改。

## 六、`GATE-RUN` 对账行（runId 与 `cmd` 逐字抄自 `.scratch/locks/gate-runs.log`）

- GATE-RUN ticket=451 runId=ed82395d-7640-4059-9f13-66a558c342d7 cmd="node packages/skill-calorie/test/exercise-records-fusion-451.test.mjs" waitedMs=0 exit=0（15:09:32.653Z）
- GATE-RUN ticket=451 runId=0a68f7ae-c4a5-4cc8-bf5d-59c3a5f7f755 cmd="pwsh -NoProfile -File .scratch/t451/window-final.ps1" waitedMs=10001 exit=0（15:13:39.273Z）← 终局窗口五步全绿：build 0／本票判据 0（8／8）／`exercise-records-342` 0（5／5）／`doc-page-assert` 0（1／1）／告警线 0

提交与推送那一次的 runId 落在本件之后，写在回执里（本件不能自指它自己的提交）。

## 七、未做项

- **唤醒词→命令接线归 #266**：本票按冻结参数直跑命令，唤醒词到命令的那一段不在本票。
- **视觉终验归 #268**；本票只做记录级明细族，其余族各有其票。
- **四态头与变更卡载具不接（器件归类，不是漏做）**：`shared/operationHead.ts` 的三张表是写操作四态（新增／修改／删除），
  #422 的待接线清单第 1 行把它划给回执族；本族是只读页，没有写操作。本族接的共用件是 `shared/sourceLine.ts`
  与 `shared/emptyGuide.ts`。
- **`check-gate-audit` 对账未跑**（不在本票八步内）；第六节已给可对账的 `GATE-RUN` 行。
- `.scratch/t451/` 内的草稿、日志与样例不进版本库；本票未新增遗留票。
