# t653 · 装机面陈旧包拷贝处置（`.dsh\profiles\web\node_modules\skill-calorie`）

票：`#653`。落 `docs/skills/skill-calorie/`（而非插件侧）：被处置的对象是**技能包 `skill-calorie` 的那份拷贝**，
按 `docs/agents/doc-homes.md`「件名＝`packages/` 下的目录名逐字」归技能侧；插件侧只是它的解析消费者。

## 一、改前读数（2026-09-16 实测）

| 位置 | 形态 | 读数 |
|---|---|---|
| `C:\Users\辰辰洋洋\.agents\skills\skill-calorie` | **Junction** → `D:\ilife\packages\skill-calorie` | SKILL.md 77,337 B ✓（agent 读技能那条，本来就对） |
| `D:\2Study\nodejs\node_modules\skill-calorie` | **Junction** → 仓库本体 | ✓（命令行 shim 走这条） |
| `C:\Users\辰辰洋洋\.dsh\profiles\web\node_modules\skill-calorie` | **真目录（拷贝）480 件** | `SKILL.md` **31,133 B**（154 行）· sha `404F266E70A76855…`；`dist/photo/**` **根本不存在**；`dist/index.js` **972 B · mtime 09-08 09:15** |
| `…\.dsh\profiles\web\.dsh-module-fallback\node_modules\skill-calorie` | Junction | **→ 上面那份陈旧拷贝**（即：插件解析链落在旧包上） |

结论：这份不是「文档旧」，是**整包旧**——停在 2026-09-08，早于场景 09 整波；而 fallback 层正指向它。

## 二、改法与读数（照 #152 的处置形制：先留可回滚备份，再建 Junction）

```powershell
Rename-Item "…\profiles\web\node_modules\skill-calorie" "skill-calorie.bak-stale-20260916"   # 改前 480 件、旧 sha
cmd /c mklink /J "…\profiles\web\node_modules\skill-calorie" "D:\ilife\packages\skill-calorie"
```

| 项 | 改后 |
|---|---|
| `LinkType` | **Junction** |
| `Target` | `D:\ilife\packages\skill-calorie` |
| `SKILL.md` | **77,337 B**，sha256 **与仓库体逐字一致**（`F0C9E773AC0FEC32…`） |
| `dist/photo/gifDoc.js` | 可见 ✓（旧拷贝里没有） |
| fallback 层 | `…\.dsh-module-fallback\…\skill-calorie`（Junction → 本路径）随之解析到仓库本体 |
| 加载自检 | `import('file:///C:/Users/辰辰洋洋/.dsh/profiles/web/node_modules/skill-calorie/dist/index.js')` → **成功，导出名 294 个** |

**回滚（一行）**：`cmd /c rmdir "…\profiles\web\node_modules\skill-calorie"`，再把
`skill-calorie.bak-stale-20260916` 改名回 `skill-calorie`。

## 三、没动的

- 没动 `…\.agents\skills\skill-calorie`（agent 读技能那条，本来就对）。
- 没动 `D:\2Study\nodejs\node_modules\skill-calorie`（命令行那条，本来就对）。
- 没动同目录**其它技能**的拷贝（`skill-bill`／`skill-chef`／`skill-home`／`skill-memo-ilife`／`skill-schedule` 在 profile 下同样是真目录）——
  它们各有归属，本票只处置 `skill-calorie` 这一件；要不要一并处置**另开票**（同一形制、同一判据）。
- 没删任何备份件。

## 四、未做项与风险

- **未在 DSH 运行期实测插件行为**（本会话就在这个 DSH 里跑）：只做了「路径解析 ＋ 模块加载」自检，
  没有重启应用验证插件面板。若插件在启动期把包解析结果缓存住了，需重启一次 DSH 才吃到新包 —— 这属正常。
- 若那位别席曾往这份旧拷贝里写过东西（`dist/cli/cmd_read.js` 的 mtime 是 09-16 20:24，比同目录 `dist/index.js` 新），
  内容仍在 `skill-calorie.bak-stale-20260916` 里，可随时比对。
