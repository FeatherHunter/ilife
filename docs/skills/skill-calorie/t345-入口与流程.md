# #345 身材照片入口与流程（HELP 逐条可读出唤醒词与可执行命令）

## 一、判定：场景 prompt 只改一处

事实源选 `packages/skill-calorie/src/triggers/scene-09-photo.ts`，
`packages/skill-calorie/src/triggers/wake-assets.ts` 一眼不碰。

判定证据（上机取数，非拍脑袋）：

1. 交付 HELP 大文件（`卡路里_HELP_<时间戳>.html`）的取数链是
   `help.ts:77` → `helpFile.ts:53-66`（`buildHelpFileData`）→ `WAKE_GROUPS`
  （`wake-assets.ts`）。但该文件头注写明"由实物 JSON 机器生成，
   禁止手工改词"；`wake-assets-133.test.mjs:68-73` 把全量 prompt 指纹钉死
   （总字符 48357＋sha256），手改即红，且在仓内无重跑生成器。
2. 用户 HELP 另外两条活路全部同源于 `SCENE_09_PHOTO`：照片速查全表
   （`helpLookup.ts:8`，`calorie.help.center` 的 `q=""` 全量 10 行）、
   唤醒词速查（`help-lookup.ts` 的 `searchHelp`／`getHelpCards`，
   `calorie.help.lookup` 经 `TRIGGERS` 取数）。
3. 双生比对（脚本实测）：`scene-09-photo.ts` 10 条 `prompt_template`
   与 `wake-assets.ts` 的 `body_photo` 10 条逐字相等（10/10）。
4. 每条目内 `main_prompt.text` 与 `prompt_template` 逐条相等（10/10），
   本次两字段同步改，不造新的分叉。

## 二、10 条场景 × 唤醒词 × 命令 × 流程指示

`cli`／`data_source`／`key`／唤醒词一字未动（路由与命令面冻结）；
只给 prompt 加流程指示。写入 7 条点名第②环过程型页命令（票面语），
读 3 条统一读侧写法。

> **现状注（#450 词表订正起，本表「流程指示」列已按当刻 prompt 原文改过两处）**：本票当时
> 写的是自造页面名「看身材照向导」（预检确认页）与「选身材照」（候选页）；#450 把那两个自造
> 入口词去掉，prompt 里改回页面本来的叫法——「预检确认页（`calorie-cmd-read
> calorie.view.photo-log-wizard`）」与「候选页（`calorie-cmd-read calorie.view.photo-picker`）」。
> 现当刻盘上 `src/triggers/scene-09-photo.ts` 的 10 条 `prompt_template` 逐字与下表一致；
> **该页命令本来就没有代表唤醒词**（三个流程内页都不是入口词，读数见
> `docs/skills/skill-calorie/t450-词表订正.md`）。下表其余列与 §三 的读数是 #345 当时的
> 历史事实，一字未回改。

| 场景 | 唤醒词 | 命令 | 流程指示（prompt 新增句） |
|---|---|---|---|
| 存一张照片 | 记身材照 | `calorie.photo.add` | 动笔前请先出预检确认页（`calorie-cmd-read calorie.view.photo-log-wizard`）给我看（待存张数与标签对不对），我确认后再写入。 |
| 存照片（含备注） | 记身材照 | `calorie.photo.add` | 同上，核对项为待存张数、标签与备注。 |
| 批量存照片 | 记身材照 | `calorie.photo.add` | 同上，核对项为待存张数与默认标签。 |
| 看身材照 | 查身材照 | `calorie.photo.list` | 直接给我看结果，不用先出确认页；缺项再问我要。 |
| 对比两张照片 | 对比两张照片 | `calorie.photo.compare` | 同上。 |
| 生成身材照 GIF | 生成身材照GIF | `calorie.photo.gif` | 同上（原"请先确认照片范围"不动，仍是选片口径）。 |
| 删身材照 | 删身材照 | `calorie.photo.remove` | 动手前请先出候选页（`calorie-cmd-read calorie.view.photo-picker`）给我看（候选列表：缩略图＋日期＋标签，让我选）。选中后、删除前先给我看这张照片的内容（快照），我确认后再删。 |
| 改照片标签 | 改照片标签 | `calorie.photo.tag`（op=set） | 动手前请先出候选页（`calorie-cmd-read calorie.view.photo-picker`）给我看（候选→快照，先核对这张照片原来的完整标签列表），我确认后再写入。 |
| 加照片标签 | 加照片标签 | `calorie.photo.tag`（op=add） | 同上，先核对已有标签。 |
| 删照片标签 | 删照片标签 | `calorie.photo.tag`（op=remove） | 同上，先告诉我当前有哪些标签；至少保留 1 个、删空提示、清空指到「改照片标签」原口径不动。 |

写法说明：写入侧沿用他场景既有句式"请先出预检确认页给我看（…），
我确认后再写入"（如设置档案／定营养目标），只把页名与命令点准；
读侧三句共用同一尾句，不点名过程型页（结果页无确认页可出）。

## 三、机器证据

- 新测试 `packages/skill-calorie/test/photo-help-flow-345.test.mjs`：5 用例全绿
  （`① HELP 产物逐字唤醒词 ② 命令集 ③ 点名过程型页＋示例照抄即跑
  ④ 三处同源 ⑤ 变异自证`，tests 5／pass 5／fail 0）。
- ①② 真跑二进制（独立进程＋临时库）：`calorie.help.center {"q":""}`
  回 10 行（场景序与定义序一致），`calorie.help.lookup {"q":"身材照"}`
  命中 10 场景，产物含逐字唤醒词与该场景命令行。
- ③ 两条过程型页示例照抄即跑 exit 0：
  `calorie-cmd-read calorie.view.photo-log-wizard`（无参）、
  `calorie-cmd-read calorie.view.photo-picker`（种子库 2 张）。
- ④ 同源口径：HELP 命令行＝速查行 cli（逐字）；与命令示例同一命令、
  参数名集合逐字相等（标签三条 op 值不同，形状同为 id／op／tag）；
  页面 prompt（向导页／候选页 ```bash 块）同一命令、参数名集合包含 HELP 参数。
- 改坏必红：删掉一条流程指示真跑全套，exit 1（③⑤同红）。
- 改回必绿：恢复后重跑，tests 5／pass 5／fail 0。
- `pnpm build` exit 0；`pnpm help:build` 后 SKILL.md 字节无变化
 （sha256 前后一致，无命令增删故无注入漂移）；
  `pnpm gen:check` PASS（119 条命令）；`pnpm help:examples:check` 119/119 通过。
- `wake-assets-133` 8/8 通过（双生另一侧零触碰，指纹未动）。

## 四、回归红点归因（精确到他人文件，与本票无关）

回归 10 红，本票 diff 仅 prompt 文本（`cli`／命令／路由零触碰，
`git diff` 10 行删 10 行增为证），红点断言无一引用 prompt 文本：

- `render-copy-90` ×4、`help-center-91` ×1：复制按钮变成双按钮
  （多出"复制日志"），源头是他人工作树的 `src/shared/copyArea.ts`／`docPage.ts`。
- `help-center-106` ×4：exec 桶计数 419≠367，源头是他人工作树的
  `src/analysis/*`／`scene-10-analysis.ts`／`src/cli/keys.ts`（路由与命令增减）。
- `cmd-read-t11` T10 ×1：照片页出现 base64 内嵌，源头是他人工作树的
  `src/photo/compare.ts`／`gallery.ts`。
- 冻结文件零触碰：`routing.ts`、`routes.generated.ts`、`wake-assets.ts`、
  `commands.ts`、`helpLookup.ts`、`html.ts` 在工作树中均无本票改动。

## 五、未做项与下一手

- 未动 HELP 大文件（读 `wake-assets.ts`）：其 body_photo 10 条仍是旧 prompt；
  双生两处现已分叉（scene-09 新、wake-assets 旧），需后票裁决合并口径
  （选项：重跑实物生成，或把 scene-09 升为大文件的上游——/gallery 等他票先定）。
- 其余 9 个场景的同类对齐不在本票（票面遗留出口）。
- `help.center` 的代表唤醒词仍与 `photo.add` 撞词（记身材照），前置已知缺口，
  不在本票写集，未动。
- 照片速查行 `exec`（node 一行式）与命令示例（`calorie-cmd-read`）仍是两套
  可执行形态：既有单测（skill-t11／render-t10／render-copy-90）把 node 形态
  钉死，本票④按"同一命令＋同一参数形状"判同源，未改 exec 语义。

本件格式自检：无字节顺序标记；`## ` 章节标题独占一行；
用词用命令／过程型／预检确认页／页面交付／跑通。
