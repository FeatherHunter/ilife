# t269 独立复核 ＋ 收口判定（复核席：t269r）

> **判决更新（2026-09-15 15:47，本席续作 §八）：判定改为「PASS」，关票建议改为「可关」。** §〇 那条「未完成／暂不关」成立的前提是「编译面当刻不绿」；
> 该前提在本席窗口 I 已消失 —— `tsc -b packages/skill-calorie` **exit=0、0 条错**（与运行读数同一持锁窗口内先编译后立刻跑）、
> 生成物 `node --check` 全过、真出口冒烟 exit=0；且本票唯一那件超线件已落回线内（**D1 按实况撤销**）。
> 逐条判据读数与要转出的三件见 **§八·四**。§〇 至 §七 保留为**当时**的读数，不改写。

> 票：#269「卡路里场景 02 饮食：饮食域共用件与页面骨架（本图共用基础件）」（MAP #155）。
> 本席**只读全仓**，只写 `docs/skills/skill-calorie/t269-复核-*` 与 `.scratch/t269r/**`。
> 纪律照 `docs/subagent-concurrency-protocol.md` §5.1（独立复跑／自设新探针／归属标注／合并点安静态复核）
> 与 §6（量刑）。作者的任何结论、任何 sha 清单**一律不采信**；下面每一句读数都是本席自己跑出来的。
> 用词照 `docs/agents/wording.md`。

## 〇、判定（先给结论）

**判定：未完成**（＝本席出不了「可关」，也**出不了 FAIL**）。

- **#269 的交付物本身：未发现 S1-交付缺陷。** 票面最承重的两条判据，本席用**比作者更严**的断言独立复跑，**都成立**：
  ① 13 条写命令走真出口 `exit=0`、产物是**真整页文档**（`<!doctype html>` 落在第 0 字节 ＋ head 内 charset ＋
  >200 字符的样式块 ＋ `</html>` 收口）13/13；② 同一份编译产物上「摘掉那一跳」前后逐字节比对：
  13 条饮食**全变**（旧片段 217–314 字节 → 整页 68–80 KB），非饮食抽样 **5/5 逐字节同一**，
  另有命令集闭包 46 条实测兜底。
- **但收口判定做不了**：票面判据 ③（`pnpm build` exit 0）与 ④（全包测试不新增红）在**当刻合并点上复现不出绿**，
  原因是**本票范围外**的两件事——他席在途件让 `tsc -b` 当刻有 4 条类型错、且**生成物的语法错把整条卡路里 CLI 打死**
  （见第六节 D5／D6）；同时本票建的那一件超 350 告警线、台账结论空着（D1，已由编排者归 #270）。
  §5.1 的口径是「关闭以**合并点安静态复核**为准」——当刻**不安静**。

⇒ **关票建议：暂不关**；条件与「若关要同时落定的三件」见第七节。这一条与编排者已采纳的口径
（「关票判定等编译回绿」）一致。

## 〇·补　本席的可复跑件

`.scratch/t269r/` 下的 6 个脚本与全部日志（`.scratch/` 在忽略清单里，仓外不可复核），
已同批归档到仓内 `docs/skills/skill-calorie/t269-复核-脚本/`；报告里引的读数的原始日志在
`.scratch/t269r/logs/`（`c-*`＝窗口 C、`d-*`＝窗口 D、`e-*`＝窗口 E 且**多数污染**、`b-*`＝窗口 B 污染）。

## 一、量现场

### 1.1 票面当刻状态（`gh issue view 269`，本席 2026-09-15 14:2x 实读）

- `state: OPEN`；`labels: wayfinder:task`；评论 2 条；`blocking:` 列了 8 张票
  （#278／#277／#275／#274／#273／#272／#271／#270）——**这张票是那 8 张的前置**，与派单里「#277 被双重阻塞」一致。
- 票面「完成的样子」原文五段（本席的判据正本）：
  1. 13 条写命令逐条实跑 `exit 0`，产物为完整文档，附落盘路径与字节数；
  2. **其余 22 条写命令的产物逐字节未变**（前后快照逐 sha256 比对）；
  3. `pnpm build` exit 0；
  4. 全包测试不新增红；
  5. 证据写进票面评论，对账文件落 `docs/skills/skill-calorie/`（以本票号命名）。
- 票面进度自记 `90%`：「静态对抗预审 PASS，均分 90，无 S1，报告提交 9c719fe。下一步：等树绿后持锁做运行时终验……
  本票保持 OPEN，不关闭。」
- 票面评论 2 条：① 2026-09-13「设计修订补充」——写后回执整页分派进共用位 `src/shared/`；
  ② 2026-09-14「运行时终验与漏改修补（证据摘要）」——自报 PASS，引运行标识 `t269f-*`。

### 1.2 盘上证据件（`docs/skills/skill-calorie/t269-*`，13 件；名称／字节／修改时间）

| 件 | 字节 | 修改时间 |
|---|---|---|
| `t269-review-adversarial.md` | 5929 | 09-14 12:33 |
| `t269-null-diet-stub.mjs` | 270 | 09-14 13:45 |
| `t269-receipt-skeleton.md` | 11643 | 09-14 14:07 |
| `t269-handover-276.md` | 2093 | 09-14 14:07 |
| `t269-verify-final.mjs` | 15408 | 09-14 14:16 |
| `t269-null-diet-hook.mjs` | 645 | 09-14 14:16 |
| `t269-cli-run13.mjs` | 4522 | 09-14 14:16 |
| `t269-compare-full.mjs` | 3781 | 09-14 14:16 |
| `t269-fail-list.mjs` | 1261 | 09-14 14:16 |
| `t269-mutation-battery.mjs` | 4876 | 09-14 14:16 |
| `t269-final-verify.md` | 17836 | 09-14 14:18 |
| `t269-gate-runs.md` | 9303 | 09-14 14:19 |
| `t269-276-review-independent.md` | 16018 | 09-14 15:00（**另一次独立复核**，非本席） |

### 1.3 提交（`git log --all`，本席自己找的）

| 提交 | 日期 | 内容 | `--stat` 主要行 |
|---|---|---|---|
| `1f88525` | 09-14 | **本票唯一的生产改动** | `src/cli/write.ts` ＋7−1；新建 `src/diet/receipt.ts` 151 行；＋证据／交接／`.changeset` |
| `25ee756` | 09-14 | 门禁对账导出件 | 只 `docs/.../t269-gate-runs.md` ＋32 |
| `9c719fe` | 09-14 | 对抗预审报告 | 只 `t269-review-adversarial.md` ＋62 |
| `a347201` | 09-14 | 三处测试收窄 ＋ 终验证据 ＋ 7 个可复跑脚本 | `test/profile-doc-179.test.mjs`／`delivery-83.test.mjs`／`calorie-c43.test.mjs` ＋ 7 个脚本 ＋ 证据件 |
| `fd6b55a`／`442c780` | 09-14 | 门禁对账补两行 | 只 `t269-gate-runs.md` |
| `4901a7d` | 09-14 | 另一次独立复核（#269／#276 双 PASS ＋ 3 条 S3） | `t269-276-review-independent.md` |

生产面**极窄**：一次 `??` 续接 ＋ 一个新建能力件。这一点是本席后面「其余命令不受影响」判据的结构面依据。

### 1.4 作业面当刻事实（本次复核最要紧的一条背景）

**本席进场到写完这一节，HEAD 换了三次**：`d7a53f3`（14:2x）→ `60bb049` → `d2c80a6`；`src/diet/receipt.ts`
（本票建的那一件）当刻是 **` M` 状态**（工作区改动、未暂存、**不是我的**），行数在同一分钟内从 239 → 376 → 456 变化。
⇒ 本票的交付物**正在被别的席位改写**。凡以产物为前提的读数都必须连「当刻源码 sha256」一起记，否则不可复核。

## 二、工程红线与门禁对账（先做不需要编译的那几段）

### 2.1 改动是否只在声明路径内

- `1f88525`：`packages/skill-calorie/src/cli/write.ts`（工种目录**登记点**，票面允许「各加自己那一条分派」）
  ＋ 新建 `packages/skill-calorie/src/diet/receipt.ts`（票面要求的能力目录）＋ `docs/.../t269-*` ＋ `.changeset/t269-*`。
  **与本图位置纪律相符。**
- `a347201`：改了 `packages/skill-calorie/test/` 下 **3 个测试件**。按 2026-09-15 才成文的《全票路径所有权总表》，
  这三件挂在 **#270** 的声明路径那一行（「这三件改内容后要跟着收窄断言」）。
  **但不构成违规**：该表成文于 09-15，`a347201` 是 09-14 的提交，**当时不存在可分派的路径表**；
  且 #270 尚未开工、三件当刻无第二写者。本席按「无表可依 ⇒ 记观察不记违规」处理，并把它记进缺陷清单的**范围外**一条，
  供编排者在 #270 开工派单时写清「该表里已有 #269 的口径变更」。
- 其余 4 个提交只动 `docs/skills/skill-calorie/t269-*`。**零源码旁逸。**

### 2.2 门禁对账（`.scratch/locks/gate-runs.log` 逐条一对一）

本席在盘上筛出 **58 行 `ticket=269`** 的记录、其中 **28 条 `RUN`**（含 2 条 `START` 无配对的：`t269f-window` exit=20、
以及若干早于终验段的 `t269-*` 记录）。逐条对照票面评论 ② 引的标识：

| 票面引的标识 | 日志里的实况 | 判定 |
|---|---|---|
| `t269f-cli13-docs` | 不存在；票面自带替代说明「（`t269f-cli13` 同读数）」，该条 **exit=0** | 可对账（改写口径已写明） |
| `t269f-verify-docs` | 不存在；票面自带替代「（`t269f-verify-2` 同读数）」，该条 **exit=0** | 可对账（同上） |
| `t269f-build` | 在，**exit=0** | 对账成立 |
| `t269f-windowA` | 在，**exit=0**（`waitedMs=40009`） | 对账成立 |
| `t269f-full-before` | 在，**exit=1** | 引了非 0 条目 |
| `t269f-window2` | 在，**exit=1** | 引了非 0 条目 |
| `t269f-targeted-final` | **不存在**（日志里只有 `t269f-targeted-before` exit=1 与 `t269f-targeted-after` exit=1） | **对账缺口** |
| `t269f-commit2`／`t269f-commit3` | 在，**exit=0** | 对账成立 |

三条读数：
1. **`t269f-targeted-final` 这条标识在日志里查无此条**——证据件里那行 `tests=29 pass=28 fail=1` 现在**无法一对一复核**。
   邻条 `t269f-targeted-after`（exit=1）与它声称的红数一致，故本席判**记账缺口（S3）**而非造假。
2. `t269f-full-before`／`t269f-window2` **exit=1** 被当作「前后基线」引用。§6.2 的字面口径是「只领退出码为 0 的条目」；
   但这两条要证的正是「全包测试当刻有 28 条红、本票不新增红」，红是**被测量的事实**而非门禁结论。
   本席按「口径冲突、需编排者裁定」记 S3，不判 S1。
3. 其余引用的标识**逐条能在日志里找到且 exit=0**，`waitedMs` 也在（等待量可复核）。⇒ 这张票的**门禁对账主体成立**。

### 2.3 台账（告警线）

- 本席**未动任何源码**（动过源码才需要 `--sync`），故只做 `--dry` 读数（见第三节实测）。
- 但盘上有一条与本票直接相关的台账事实：`packages/skill-calorie/AGENTS.md` 的告警线台账里，
  **`src/diet/receipt.ts`（本票新建的那一件）当刻 LF=456、已超 350 线**，结论列是
  「超因与拆法待补（本行由 `--sync` 自动补出，请补写超因与拆法）」——即 **必报五步第四步未履行**。
  本票的骨架件从 151 行长到 456 行，是**后续多票（#496 等）加进去的**，但件是**本票建的那一件**，
  归属落在「本票范围」。

## 三、静态决定性发现：本票自己的断言块与当刻源码对不上（不依赖编译，可在盘上逐字复核）

**读数（本席自己取的，可用 `git show` 逐字复核）：**

1. `packages/skill-calorie/test/profile-doc-179.test.mjs:299` 有一条本票新增的测试
   `test('#269 饮食写命令已是完整文档（从「其余仍是片段」那张表里拿出来）')`，对
   `calorie.water.log` 与 `calorie.product.add` 各断言 5 条，其中两条是**逐字钉文案**：
   - 第 309 行 `assert.ok(r.file.includes('饮食 · 写后回执'), '缺场景 02 眉标')`
   - 第 310 行 `assert.ok(r.file.includes('改动字段对照'), '缺改动分项表')`
2. 提交 `1cefc90 checkpoint(499-seat3)`（09-15）把 #496 的在途改动扫进仓，其中 `src/diet/receipt.ts`：
   - `eyebrow: '饮食 · 写后回执'` → `eyebrow: ''`（第 227 行带 #496 注释：「与 head 标题重复一遍 ⇒ 不出这一行」）；
   - 表格标题 `改动字段对照` → `本次改动`（第 132 行 `caption: '本次改动'`）。
3. 本席在盘上逐字筛（`Select-String` 精确匹配）：**`'改动字段对照'` 在 `packages/skill-calorie/src` 全域命中 0 次**
   （HEAD 提交版与当刻工作区版都是 0）；`'饮食 · 写后回执'` 只剩 1 次命中，且**在一条注释里**
   （`src/diet/receipt.ts:225` 的 #496 注释原文），**不进产物**。
4. 同一提交里 `calorie-c43.test.mjs`（第 151-153 行）与 `delivery-83.test.mjs` 的口径**跟着 #496 改过了**
   （前者注释写明「现改标题『本次改动』、表头『本次结果』」），**唯独 profile-doc-179 这一条没跟**。

⇒ **合并点上，本票写的这条断言是红的**：`assert.ok(r.file.includes('饮食 · 写后回执'), …)` 先失败，
第二条断言即便跑得到也必失败。这不是「收窄变成了放宽」的反面，而是**「收窄之后没人守」**：
一条红的断言等于没有断言。（运行面读数见第四节。）

**归属**：**范围外发现**（把文案改掉的是 #496 的在途改动，经 `1cefc90` 扫进仓；改文案的那次没有同步这条测试）。
但它**直接关系本票的判据 ④「全包测试不新增红」在合并点上还成不成立**——按 §5.1，范围外发现里
「直接证伪本票验收结论」的那一类可以参与判定，本席据此把它列为**关闭前置条件**，而不是只记 S3 了事。

## 四、运行时读数（判据 ①②；全部本席自己跑，不信任何作者的脚本或 sha 清单）

本席自造的探针共 6 个脚本，全部落在 `.scratch/t269r/`（未调用、未引用作者那 7 个脚本里的任何一个）：
`probe.mjs`（A13／B46／CMP／INJ 四组）、`cmp.mjs`＋`cmpdiff.mjs`（前后逐字节）、`inject2.mjs`（取数失败）、
`mutate.mjs`（产物级变异与逐字节取回）、`mkbefore.mjs`。读数落在 `.scratch/t269r/logs/c-*.log`／`d-*.log`／`e-*.log`。

**断言强度对比（这是本席不采信作者读数的第一层理由）**：作者的 `t269-cli-run13.mjs` 判「完整文档」用的是
`lower.includes('<!doctype html>') && lower.includes('charset') && lower.includes('<style')`——**只查三个子串出现过**，
一句含 `charset` 字样的片段也能过。本席的严格断言要六项同时成立：
① `<!doctype html>` 落在**第 0 字节**；② 有 `<html … lang=…>`；③ `<head>` 段内**有** `<meta charset="utf-8">`；
④ `<style>` 块内容 **>200 字符**；⑤ 以 `</html>` 收口；⑥ 长度 >10000。

### 4.1 判据 ①：13 条写命令逐条实跑（真出口 `packages/skill-calorie/dist/cli/cmd_read.js`）

持锁窗口 C（`runId=a12adcdd-3e79-43eb-b01f-21cc23bef4c0`，HEAD `d10215a`，全窗未重编）：
`RESULT: A13 ok=13/13 strict=13/13 lenientAuthor=13/13`；13 条 `exit=0`、产物 68333–73385 字节、
`delivery.template=doc-shell`。当刻被测源码用 sha256 钉住：`src/diet/receipt.ts`＝`6173fbc7…`、
`src/cli/write.ts`＝`dd11577a…`。**判据 ① 达成**（按比作者更严的断言）。

顺带取了一条作者从没取过的读数（**新探针的方向之一**：不给 `--html` 时的默认落点）：

```
DEF key=calorie.diet.add  exit=0 dirOk=1 nameOk=1 onDisk=1 bytes=73114 strict=1
    path=D:\ilife\.scratch\t269r\w\d-calorie.diet.add\calorie_html\记一餐_回执_鸡胸_20260915_144002.html
DEF key=calorie.water.log exit=0 dirOk=1 nameOk=1 onDisk=1 bytes=72028 strict=1
    path=D:\ilife\.scratch\t269r\w\d-calorie.water.log\calorie_html\记喝水_回执_300ml_20260915_144002.html
```

⇒ 与共同口径「`<SKILLS_DB_PATH>/calorie_html/<中文名>_<YYYYMMDD_HHMMSS>.html`」逐项相符，且**默认落点那份也是完整文档**。

### 4.2 判据 ②：其余写命令的产物逐字节未变

**方法（本席自造，先写清「挑哪几条、为什么」）**：不复制整棵 `dist`（窗口 C 实测：复制出去的树**跑不起来**，
13 条全 `exit=1`，模块解析失败 ⇒ 复制法作废），改为**在同一份、同一次编译产物上做两次跑**：

- **after** ＝ 当刻产物；
- **before** ＝ 同一份产物，把装配链上那一跳的**编译产物**逐字摘掉（`?? dietReceiptDoc(key, params, receipt, db) ?? ` → 空）。
  依据 `git show 1f88525 -- …/src/cli/write.ts`：本票的生产改动**就是这一跳**，别处一字未动（本席自己看过 diff）。
  摘除命中数 `needle_hits=1`；跑完逐字节取回：`RESTORE-HOP pre=97821128… after=97821128… byte_identical=1`。

**抽样（20 条）**：13 条饮食（判据本身）＋ 7 条非饮食，7 条覆盖**全部非饮食域**且刻意分落在装配链的**不同位置**
——`profile.set`／`profile.update`（链首那两个口）、`exercise.add`（中段）、`weight.log`／`body.measure-add`
（后段、各自另有整页口）、`goal.set`／`goal.water`（落旧片段回退面）。理由：本票改的正是链上那一跳，
最该证明「摘掉这一跳，非饮食侧一个字节都不变」。

**读数**（持锁窗口 D，`runId=86866158-5a54-4230-916d-5ad87051989e`）：

| 侧 | 条数 | 读数 |
|---|---|---|
| 饮食 13 条 | 13/13 **全变** | before＝217–314 字节旧片段（`template=receipt`）→ after＝68333–73385 字节整页（`doc-shell`） |
| 非饮食抽样 | 5/5 **逐字节同一**（归一化后 sha256 相等） | `profile.set` 69280／`exercise.add` 72527／`weight.log` 76998／`body.measure-add` 79667／`goal.set` 275 字节 |
| 非饮食抽样 | 2 条**不usable** | `profile.update`／`goal.water` 两侧都 `exit=4`（缺前置：空库里没有档案／没有目标）——由 4.4 的新探针①单独读 |

`RESULT: CMPDIFF dietChanged=13/13 othersSame=5/7 unusable=2`。
归一化只做两件事（临时目录串→`<DIR>`、时刻串→`<TS>`），其余逐字节——这一点本席在报告里明写，
因为只归一化 sha 而不归一化时刻会得到「33 条全变」的假读数（作者自己的证据件里记了它踩过同一个坑）。

**机制面（本席的第二条独立证据，作者完全没有这一条）**——命令集闭包逐条验（46 条写命令，全部实测）：

```
RESULT: B46 total=46 dietMembers=14 othersEarlyNull=32 valueReturned=0
```

做法：以 `db=null`、`receipt=null` 调编译产物里的 `dietReceiptDoc(key, {}, null, null)`。非饮食命令必须**原样返回
`null`**（＝早退、摸不到任何东西、不可能有副作用），饮食命令必须不早退（会抛）。46 条逐条给读数，
`valueReturned=0` ⇒ 既没有「非饮食命令被认领」，也没有「拿空库编出一份东西」。**判据 ② 达成。**

**一条必须写清的口径漂移**：票面写的「13 条」与「其余 22 条」都不是当刻的数。当刻命令册 46 条写命令，
饮食这一族被接进整页回执的是 **14 条**（`#496` 把 `calorie.product.import` 也接了进来，源文件第 33-37 行有它自己的
说明与归因）；「其余」当刻是 **32 条**。作者在证据件里已写明「22 条是 35 条命令册时代的数字」，
本席复核：**当刻 46 条 ✓ 非 35 条 ✓**，故 22→32 的取代关系成立、不判缺陷，但票面数字与实况不一致这一条
要写进关票记录，免得下一手拿 22 去对。

### 4.3 判据 ③：`pnpm build` exit 0

**本席没有跑 `pnpm build`**，如实写明理由：它的后半段是 `node packages/skill-calorie/scripts/gen-cli.mjs --stamp`，
**会写 `src/cli/keys.ts`／`registry.ts`／`routes.generated.ts`**，超出本席声明路径（复核席只读全仓）。
替代读数是「编译 ＋ 生成物一致性」这一对：

- 窗口 A 持锁内先编译（`npx tsc -b`）：**exit=1**，两条错，**都不在本票范围**——
  `src/diet/todayDocs.ts(107,8)` TS1005（当刻是他席在途件，见 1.4）与
  `packages/base-render/test-d/contract-signatures.ts(390,20)` TS2344；
- 当刻（14:5x）`npx tsc --noEmit -p packages/skill-calorie`：仍有 4 条错，**全部在他席在途件**
  （`src/body/view.ts`／`src/body/wizardPlate.ts`×2／`src/diet/todayDocs.ts`）；
- `pnpm gen:check`：**exit=1**，原因是 `src/home/routes.ts` 的内容与上次 `pnpm build` 的印记不一致
  （印记 `6fe77990dbc2…` ≠ 现 `a1ce7202ec20…`），同样与 #269 无关。

⇒ **判据 ③ 在当刻树上复现不出「绿」**，而**不绿的原因 100% 在本票范围外**。作者声称的 `t269f-build`（exit=0）
在 `.scratch/locks/gate-runs.log` 里**确有其条**，但它测的是 09-14 的树，不能当「当刻绿」。本席按
「编译面当刻不绿 ⇒ 关票判定等它绿」处理（编排者已采纳同一口径）。

## 五、§5.1 硬要求：自设新探针与变异自证

### 5.1 五条作者脚本覆盖不到的探针（本席自造）

| # | 探针 | 作者覆盖？ | 读数 |
|---|---|---|---|
| ① | **取数失败不许落半成品**：让写命令的取数失败，看是 `exit 4` ＋ 不落产物，还是落一份半成品 | 无（作者只跑通顺路径） | **部分取到**（见下） |
| ② | 13 条的产物里有没有**仍是片段**的（缺 doctype／charset／样式三者之一） | 作者只查「三个子串出现过」，不查位置／强度 | 13/13 六项严格断言全过（doctype 在第 0 字节） |
| ③ | 13 条**之外**的写命令有没有被波及 | 作者用加载钩子做整体 sha，不查命令集闭包 | 命令集闭包 46 条 ＋ 非饮食抽样 5 条逐字节 ＋ 前後 20 条逐字节 |
| ④ | **不给 `--html`** 时的默认落点是否符合共同口径 | 无（作者每条都显式传 `--html`） | 2 条实测：目录／文件名／存在性／严格完整文档 **全对** |
| ⑤ | 命令集**封闭性**：到底哪些命令被认领（谁多认、谁漏认） | 无 | `dietMembers=14／othersEarlyNull=32／valueReturned=0` |

**探针①的读数（如实分两半写）**：

- **取到的那一半**：窗口 D 的两条非饮食命令因**缺前置**（空库里没有档案／没有目标）而取数失败，
  两侧都是 `exit=4`、`bytes=0`、`template=null`——**没有落任何半成品文档**。设计文案静态核对：
  `src/cli/cmd_read.ts:122` `if (e.code === 'missing-data') fail(4, '取数失败（缺失阻断）：' + e.message)`
  （`dist/cli/cmd_read.js` 第 118／82 行同形）。⇒ 「失败就不留东西」这条**成立**。
- **没取到的那一半**：`ERR 4: 取数失败（缺失阻断）：…` 的**逐字运行时文案**这一格**空着**——本席为此写的
  `.scratch/t269r/inject2.mjs`（4 条：空库档案／空库目标／记录号不存在 ×2）在窗口 E 里跑时，CLI 已被
  第六节那条**生成物缺陷**打死，4 条全 `exit=1`、stdout 全空，**不作数**。
  「`--html` 落点非法（父路径是文件）」那一条同样**没取到**。两条都留给下一手（各一条命令）。

### 5.2 变异自证（两行能分辨真假的读数）

变异**只打在编译产物** `packages/skill-calorie/dist/diet/receipt.js` 上（构建产物、不在版本控制里），
源码一行不碰（本席声明路径不含源码）；每处跑完**逐字节取回**并断言 sha256 相等。

**方向一「漏一条」——摘掉 `calorie.water.log`：**

```
绿（基线，健康产物）：RESULT: B46 total=46 dietMembers=14 othersEarlyNull=32 valueReturned=0
红（M1 摘掉一条）  ：RESULT: B46 total=46 dietMembers=13 othersEarlyNull=33 valueReturned=0
```

**这两行就是判据本身**：被认领的命令少 1、早退的命令多 1 ⇒ 本席那条「命令集闭包」断言按设计判假（**是脚本约定的红，
不是脚本自身失败**：脚本照常跑完并打出完整 `RESULT` 行）。取回读数：
`RESTORED pre=97821128… touched=ea310e… after=97821128… byte_identical=1`（`cli/write.js`），
`RESTORED pre=125aac92… after=125aac92… byte_identical=1`（`diet/receipt.js`）。

**方向二「多一条」——把不在 13 条里的 `calorie.goal.set` 塞进同一命令集**：仓内反面断言真会红。
窗口 B（产物健康的那一段）读到逐字一行：`AssertionError [ERR_ASSERTION]: 未切整页的命令仍是片段族`
（`delivery-83.test.mjs:327`）——**这条消息只可能由该变异引起**：`calorie.goal.set` 被认领成整页，
反面断言（未切整页的命令必须仍是 `receipt` 片段族）当场判假。该方向的 B46 那一行**没取到**
（窗口 E 只跑了仓内测试，那一跑被生成物缺陷打死），留作下一手。

> 归属与保留：本席**没有**做源码级变异（声明路径不允许写源码）。产物级变异＋逐字节取回是等价的做法，
> 已在上面逐条给出取回 sha；真 dist 的源码面**零改动**（`git status` 里 `src/**` 没有本席的任何改动）。

## 六、缺陷清单（逐条：分级 ＋ 归属）

**S1-交付缺陷：无。** 本席没找到契约违反、用户可见回归、证据造假或私自放宽、数据丢失风险。
判据①②（票面最承重的两条）在本席更严的断言下**都成立**，产物是真的整页文档，
其余命令在同一次编译产物上**逐字节不变**。

| # | 分级 | 归属 | 内容（附本席读数） |
|---|---|---|---|
| D1 | **S2**（关闭前应落定） | **本票范围**（件是本票建的；治理已由编排者具名归 **#270**） | 本票建的 `packages/skill-calorie/src/diet/receipt.ts` **超 350 告警线**，台账结论列还是「超因与拆法待补（`--sync` 自动补出）」——**必报五步第四步未履行**。本席实测告警线门当刻**红**：`RESULT: 77/78`、`RED 台账陈化：src/diet/receipt.ts 台账=456 实况=350`。该件当刻仍在被他席改写（LF 在 239→376→456→318→350 之间动）。**这一条不证伪 #269 的交付结论**（它证伪的是「这个件现在的形状合规」），故不判 FAIL；但**关票前应落定**（补写超因与拆法，或按台账里的拆法拆件）。 |
| D2 | **S3**（记账） | 本票范围 | 门禁对账有一条**引不到的运行标识**：`t269-final-verify.md` 引 `t269f-targeted-final`（`tests=29 pass=28 fail=1`），`.scratch/locks/gate-runs.log` 里**查无此条**（只有 `t269f-targeted-before`／`t269f-targeted-after`，均 `exit=1`）；邻条红数与它声称的一致 ⇒ 记「标识写错／漏记」，不记造假。 |
| D3 | **S3**（记账） | 本票范围 | 同一份证据把 `t269f-full-before`（`exit=1`）与 `t269f-window2`（`exit=1`）当门禁条目引。§6.2 字面口径「只领退出码为 0 的条目」与「用两次红跑做前后对照计数」相冲，需要编排者一句话裁定（本席不自行放宽）。 |
| D4 | **S3**（已由后人收口，记观察） | 本票范围 | 断言面抽样：a347201 把 `water.log`／`product.add` 之外 11 条的正面只挂在**仓外脚本**上（文件里自己写明「不断言＝口径无锚，跳过＝放宽，都不许」）。**当刻已收口**：#270 把这条改成钉**四块老实物逐字标题 ＋ 复制日志第 4 段命令原文 ＋ 页题**（`profile-doc-179.test.mjs:304-324`），比原口径**更强**；`calorie-c43` 的 C6 同样从 `<li>` → 「表格单元格 ＋ 四串」。⇒ 从「待跟进」降为「已收口，留档」。 |
| D5 | **范围外发现** | 他席／他票 | 本席复核期间，作业面上有**三处与本票无关的红**在持续干扰读数：① `src/diet/todayDocs.ts`、`src/body/view.ts`／`wizardPlate.ts` 的类型错（他席在途件，`tsc -b` 当刻 4 条错）；② `pnpm gen:check` 红（`src/home/routes.ts` 印记与上次 `pnpm build` 不一致）；③ **生成物缺陷致整条 CLI 死掉**（见 D6）。**均不判 #269**，按附录「红在声明路径外 ⇒ 记范围外发现」。 |
| D6 | **范围外发现（本席另发紧急件给编排者）** | 他席（生成器／路由 SoT） | `packages/skill-calorie/dist/triggers/routes.generated.js:3` **语法错**：某条路由的 `params` 对象被拼进了单引号字符串（`cli: '… --params ' }, { "date": "今日", "section": "overview" }, ' },,`），同形坏行 5 条，`node --check` exit=1；`src/triggers/routes.generated.ts` 当刻 ` M`，**重跑生成器仍复现**。后果：`calorie.*` 全部命令 `exit=1`、stdout 空。**本席没跑过任何生成器**，此缺陷与 #269 无关，但它是「判据③在当刻树上复现不出绿」的直接原因之一。 |
| D7 | **范围外发现（不可归因，只记读数）** | 不明 | 本席**持锁窗口内**观察到 `dist` 主要件被改写（窗口 B：`cmd_read.js`／`write.js`／`diet/receipt.js` mtime 14:34:21、`todayDocs.js` 14:35:12）。可能是别人更早窗口里起的编译长尾，日志无法归因。**本席不指名、不判违规**，只据此把窗口 B／E 的读数标「污染、不作数」，并把窗口 C／D 的读数用 sha256 钉死。 |

### 6.1 「收窄是否变成了放宽」（§5.1 的核心维度）

本席自造的判别样例就是 5.2 的两个方向（摘一条 / 塞一条），**两个方向都能被抓**；在此基础上逐件核对本票的三处收窄：

| 处 | 当刻状态 | 判读 |
|---|---|---|
| `profile-doc-179`（本票新增的 `#269／#270` 块） | 窗口 C 实测**绿**（HEAD `d10215a`）；断言内容是**四块老实物逐字标题 ＋ 复制日志第 4 段命令原文 ＋ 页题 ＋ 长度** | **收窄后更强**，未放宽 |
| `delivery-83` 两条 | 窗口 C 实测 **13/13 绿**；反面仍在（未切整页的 `calorie.goal.set` 必须仍是 `receipt` 片段族，M2 会当场红） | **未放宽** |
| `calorie-c43` 的 C6 | 窗口 C 实测**红**（当刻 dist 已按 #270 口径重写页面，而 c43 还钉 #496 的旧标题）；随后 c43 被改到新口径（现钉四串 ＋ `/📋 字段变更[\s\S]{0,900}?<td[^>]*>鸡胸<\/td>/`） | 方向是**收紧**；那一格红是**口径错位**，不是放宽 |

**一条结构性观察（S3 级，记给收口票）**：这三处收窄的锚点**全押在「页面上那几句文案」**上（眉标／表题／四块标题），
于是每换一次文案就必须同步三件测试，而三件测试分属不同席位的写集 ⇒ **红绿会随别人的文案改动漂**。
本席建议后续把它们改成「结构锚」（doctype 位置／表格单元格／块数），文案锚只留一两条——**这不属于 #269 的整改范围**，
本席只记账，不代改。

## 七、关票建议（编排者做关闭动作，本席只出建议）

### 7.1 票面五段判据逐条读数

| # | 票面原文 | 本席读数 | 达成？ |
|---|---|---|---|
| 1 | 13 条写命令逐条实跑 `exit 0`，产物为完整文档，附落盘路径与字节数 | `RESULT: A13 ok=13/13 strict=13/13`（真出口，窗口 C，HEAD `d10215a`）；13 条路径与字节数在 `.scratch/t269r/logs/c-a13-real.log` 逐行；另 2 条默认落点也过 | **达成** |
| 2 | 其余 22 条写命令的产物逐字节未变（前后快照逐 sha256） | 13 条全变（片段→整页）；非饮食抽样 **5/5 逐字节同一**；命令集闭包 46 条 `dietMembers=14／othersEarlyNull=32／valueReturned=0`。数字口径：当刻写命令 46 条，「其余」＝**32** 条（票面 22 是 35 条册时代的数） | **达成**（抽样 ＋ 机制面双证） |
| 3 | `pnpm build` exit 0 | **未达成/未判**：当刻 `tsc -b` 与 `tsc --noEmit` 都有错（全在他席在途件），`pnpm gen:check` 红（`src/home/routes.ts` 印记），且生成物缺陷把 CLI 打死；`pnpm build` 本席按声明路径**没跑**（它的 `gen-cli --stamp` 会写受版本控制的 `src/cli/*.ts`） | **未判**（原因全在本票范围外） |
| 4 | 全包测试不新增红 | **未判（合并点不可重测）**：全包基线已不可复现（树前移上百提交）。可测的是本票动过的三件，窗口 C 读数：`profile-doc-179` 6/10 pass（**#269／#270 那条绿**，4 条红全是他票的行）、`delivery-83` **13/13 绿**、`calorie-c43` 7/9（C6 红，系口径错位，随后已被改到新口径）。窗口 E 的复跑**全红不作数**（生成物缺陷 ＋ 别人重编） | **未判** |
| 5 | 证据写进票面评论，对账文件落 `docs/skills/skill-calorie/` | 13 件在盘并已入仓；票面 2 条评论；门禁日志 58 行 `ticket=269`、28 条 `RUN` 一对一可查（除 D2 那一条标识） | **达成**（附 D2 记账缺口） |

### 7.2 关 or 不关

**建议：暂不关（先别关）。** 理由不是「有 S1」——本席**没找到 S1**，①②两条硬判据都独立复核成立；
卡住的是**合并点当刻不安静**，而 §5.1 明写「关闭以**合并点安静态复核**为准」：

1. **编译面当刻不绿**（4 条类型错全在他席在途件）＋ **生成物缺陷把整条 CLI 打死**（D6）⇒
   判据③④当刻**复现不出绿**，这正是编排者已经采纳的那条口径（「关票判定等编译回绿」）。
2. **本票建的件超告警线且台账结论空着**（D1）⇒ 告警线门当刻红（`77/78`）。编排者已把治理归 #270，
   但**关闭动作应在它落定之后**，否则「关掉一张带红的票」。
3. 判据④的**全包基线**在合并点上已经不可能重测；要么由编排者书面接受「本票窗口内的读数 ＋ 窗口外的红归他人」，
   要么在编译回绿后重新取一次三件测试的读数。

**若决定关，需要同时落定/转出的三件（S3，不阻塞关闭本身）：**
- D2：把 `t269-final-verify.md` 里 `t269f-targeted-final` 改成日志里真有的标识（或补记该次运行）；
- D3：请编排者一句话裁定「`exit=1` 的前后对照跑能不能当门禁条目引」（§6.2 字面口径与之相冲）；
- D4／6.1 的结构性观察：三件测试的锚从「文案」改回「结构」——转给 #270／后续收口票。

### 7.3 未做项与下一手缺什么（可执行，逐条给到命令级）

| 缺口 | 为什么没做 | 下一手一条命令就能补 |
|---|---|---|
| `ERR 4: 取数失败（缺失阻断）：…` 的**逐字运行时文案** | 窗口 E 跑 `inject2.mjs` 时 CLI 已被生成物缺陷打死（4 条全 `exit=1`／stdout 空，不作数） | CLI 回绿后：`node .scratch/t269r/inject2.mjs`（4 条，含 2 条「记录号不存在」） |
| 「`--html` 落点非法」那一格 | 同上 | 同脚本的 `i-badout` 用例，或重跑 `.scratch/t269r/probe.mjs after inj` |
| 变异方向二的 B46 那一行（`dietMembers` 应由 14→15） | 窗口 E 只跑了仓内测试，那一跑被污染 | `node .scratch/t269r/mutate.mjs snapshot && node .scratch/t269r/mutate.mjs m2 && node .scratch/t269r/probe.mjs after b && node .scratch/t269r/mutate.mjs restore` |
| 三件测试的**当刻**红绿（窗口 C 之后 c43／profile-doc-179 又被改过） | 窗口 E 复跑被污染；当刻 CLI 死 | 树回绿后同一次持锁窗口内：`node --test packages/skill-calorie/test/profile-doc-179.test.mjs`（另两件同） |
| 全包 `pnpm test` 的前后对照 | 基线不可复现；且本席不写源码、不跑 `pnpm build` | 若编排者要这条，应指定一个**冻结提交**做评审基线，而不是拿当刻树比 |

### 7.4 本席自己踩到的坑（写给下一手，免得重复）

1. **复制整棵 `dist` 出去跑是不行的**：复制树里的 CLI 一律 `exit=1`（模块解析失败）——
   本席在窗口 C 实测 13 条全红后弃用该法，改用「同一份产物摘／接那一跳」。**别再用复制法。**
2. **别人会在你持锁期间重编 `dist`**：窗口 B／E 因此整体作废。**凡是产物读数，都要在窗口首尾各取一次主要件 sha256**，
   不一致就把该窗口标「污染」，不要拿去判分（本席的 `.scratch/t269r/windowC.ps1`／`windowD.ps1` 已内置该做法）。
3. **`node --test` 的摘要在 `ℹ tests N` 行**（不是 `# tests N`），用 `^# ` 去筛会筛空、把「全红」看成「没有摘要」。



---

# 第八节（本席续作，2026-09-15 15:3x）· 收口判定的续跑与量现场

> 本席＝MAP#155 一席独立复核者（票 #269 收口判定）。上面 §〇 至 §七 是**同席位前任在同一份报告件里的续断段**，
> 本节起是本席的续跑，替前任把 §七 里「未判」的两条（判据③④）与「下一手缺什么」那五格**全部补上读数**。
> 纪律照旧：只信提交号与自己复跑；作者的任何结论与 sha 清单一律不采信。
> 本席不跑 `pnpm gen`／`pnpm build`／`pnpm gen:check`（本单未放行，且会写受版本控制的生成物）。
> 编译一律持锁，且以产物为前提的读数**在同一持锁窗口内先编译后立刻跑**。

## 八·一　量现场（2026-09-15 15:31:49 与 15:35 两次读数）

| 项 | 读数 | 来源 |
|---|---|---|
| HEAD | `edf2469`（15:30，docs(351) 台账补齐） | `git log -1` |
| 工作区受跟踪改动数 | **148 件**（他图代理群持续在写） | `git status --porcelain` |
| `dist/triggers/routes.generated.js` 语法 | `node --check` **exit 0**（§六 D6 那条「生成物语法错打死整条 CLI」**已解除**） | 本席实测 |
| `dist/diet/receipt.js` 语法 | `node --check` **exit 0** | 本席实测 |
| 当刻写命令册条数 | **46 条**（从 `dist/cli/keys.js` 的 `CALORIE_WRITE_COMBOS` 现取，不抄任何名单） | §八·二 |
| 饮食键条数 | **14 条**（`dietReceiptDoc(key,{},null,null)` 不早退者） | §八·二 |
| 前任提交 | `9e93679`（docs(269-复核)：报告 377 行 ＋ 12 个可复跑件） | `git show --stat` |
| `src/diet/receipt.ts` 当刻状态 | ` M`（他席在途改写，未暂存；本票建的件仍在动） | `git status --porcelain --` |

### 八·一·补　门禁对账逐条（本席自己在 `.scratch/locks/gate-runs.log` 上筛，不抄任何人的对账表）

盘上 `ticket=269` 的记录：**30 对 `START`/`RUN`（60 行）**，其中 26 对属作者终验段（`t269f-*`）、6 对属更早段（`t269-*`）。逐条退出码：

- **作者票面引的两个标识**：`t269f-cli13-docs`、`t269f-verify-docs` —— 本席逐字筛（`Select-String` 精确匹配）确认这两条**查无此条**（0 行）；作者票面自带替代说明「（`t269f-cli13` 同读数）」「（`t269f-verify-2` 同读数）」，本席核实 **`t269f-cli13` RUN 行 exit=0**、**`t269f-verify-2` RUN 行 exit=0** ⇒ 按「标识写错、读数可对账」处理，**记 S3 记账缺口，不记造假**。
- `t269f-build` **exit=0**（对账成立）、`t269f-windowA` exit=0、`t269f-probe-shapes` exit=0、`t269f-probe-exercise` exit=0、`t269f-ex2` exit=0、`t269f-windowB1` exit=0、`t269f-commit`／`commit2`／`commit3` exit=0。
- **被当基线引用的三条非 0**：`t269f-full-before` exit=1、`t269f-window2` exit=1、`t269f-targeted-before`／`targeted-after` exit=1 —— 它们要证的是「当刻有多少红」，
  红是**被测量的事实**，但这与自证段「只领退出码为 0 的条目」字面相冲 ⇒ **记 S3，请编排者一句话裁定**（本席不自行放宽）。
- **`t269f-targeted-final` 查无此条**（前任已记 D2，本席复核：确认查无此条，邻条 `t269f-targeted-after` exit=1 且红数与它一致）。

## 八·二　编译面：三次窗口的守门读数链（本席自己踩的坑，逐条留盘）

**窗口 F**（`runId=a164f18f-7993-4039-8210-c8ab084c750e`，`waitedMs=66210`，exit=9）：
先编译 `node node_modules/typescript/bin/tsc -b packages/base-render packages/skill-calorie` ⇒ **exit=1、2 条错**，守门按「一红即中止」判 `BASE-NOT-GREEN` 并整段未跑。
两条错：`src/weight/history.ts(743,26) error TS2345`、`src/weight/log.ts(326,26) error TS2345`。

**窗口 G**（`runId=dcfc8493-b11b-4c22-87f3-c39caeeae38d`，`waitedMs=22104`，exit=9）：
守门改成「dist 五个关键件 mtime 必须落在本窗口内」。读数：**全 5 件 `fresh=0`**（`keys.js`／`cmd_read.js` mtime `15:25:43`，`write.js`／`receipt.js` `15:36:15`／`15:36:24`），
而同一窗口的冒烟：**exit=0、73183 字节、doctype 在第 0 字节、`</html>` 收口**。⇒ **mtime 守门是误判**：
`tsc -b` 是**增量编译**，没改动的 project 不重写产物，所以 mtime 旧、产物却是好的。
**这条写成给下一手的教训：判产物新鲜度别用 mtime。**

**窗口 H**（`runId=c34bdec3-2a2d-4934-95d8-ae50d11926ff`，`waitedMs=269203`，exit=9）：
守门改成「本包源码里有类型错即中止」。读数：`TSC-INPKG errors=2`（两条都是 `src/weight/**`），语法五件全 ok、冒烟 exit=0／73130 字节／严格完整文档 ⇒ 中止理由同样不成立（本单的判据是「**我这条路不新增红**」）。
**窗口 H 的正面读数也留档**：`src/diet/receipt.ts` sha16=`7F51178960A1F76D`（321 行，他席在途改写后的当刻值）、`src/cli/write.ts` sha16=`DD11577AA861E55E`（93 行）、
`dist/diet/receipt.js` sha16=`3A7AF1A717A2CACC`、`dist/cli/write.js` sha16=`978211282CFABBB4`。

**窗口 I**（守门＝①语法 ②冒烟 ③错的归因：错全在 `src/weight/**` ⇒ 记范围外红并继续；错落进本席判据路径才中止。④电池首尾五件 sha256 抗污染）：
三次窗口的 tsc 错**逐字相同**（`history.ts(743,26)` 与 `log.ts(326,26)` 两条 TS2345），其中 `history.ts` 那一条**正是本单派单里点名的当刻已知范围外红**（体重域）。
两条的当刻状态：`git status` 显示 `src/weight/` 下 6 件 ` M`（他席在途），两件 mtime 15:31:17／15:31:58 ⇒ **归因他席在途件，不是本票**。

## 八·三　判据①②的独立复跑（窗口 I，`runId=70701fce-5908-4ad3-9bc6-b7cc24d61762`，`waitedMs=172738`，exit=0）

**先编译后立刻跑**：本窗口第 1 步 `node node_modules/typescript/bin/tsc -b packages/skill-calorie` ⇒ **exit=0、0 条错**（窗口 F／G／H 那两条 `src/weight/**` 的错在他席手里已回绿）。
守门：五个关键件 `node --check` 全 ok；真出口冒烟 `calorie.diet.add` exit=0、73130 字节、doctype 在第 0 字节、`</html>` 收口。抗污染：电池首尾五件 sha256 **changed=0**。
被测源码钉住（`SRCPIN[start]`／`[end]` 两次相同）：`src/diet/receipt.ts` sha16=`3349877113DFF727`（321 行）、`src/cli/write.ts` sha16=`DD11577AA861E55E`（93 行）、
`src/cli/cmd_read.ts` sha16=`DA663329D260A569`、`src/cli/keys.ts` sha16=`568420B6CF6A695E`；产物侧 `dist/cli/write.js` sha16=`978211282CFABBB4`、`dist/diet/receipt.js` sha16=`5CE688C93FDAF465`。

### 判据①（13 条写命令逐条实跑 exit 0、完整文档、落盘路径与字节数）——**达成**

```
RESULT: SET total=46 dietMembers=14 othersEarlyNull=32 valueReturned=0
RESULT: A13 ok=13/13 strict=13/13 lenientAuthor=13/13
```

13 条**全部 exit=0**，产物 **68337–73401 字节**，严格六项断言（doctype 在第 0 字节／`<html lang>`／head 内 charset／样式块 >200 字符／`</html>` 收口／>10000 字节）**逐条全过**。
逐条落盘绝对路径与 sha16 在 `.scratch/t269r/logs/i-a13.log`（本节表末附一条样例）；另有 2 条**不给 `--html`** 的默认落点读数：
`D:\ilife\.scratch\t269r\w\d-calorie.diet.add\calorie_html\记一餐_回执_鸡胸_20260915_154607.html`（73130 字节）、
`...\记喝水_回执_300ml_20260915_154607.html`（72044 字节）——目录、文件名时刻格式、存在性、严格完整文档**全对**。

### 判据②（其余写命令的产物逐字节未变）——**达成**（同一份产物摘掉那一跳的前后比对）

```
RESTORE-RECIPE byte_identical=1
RESULT: DIFF dietChanged=13 dietSame=0 othersSame=6 othersChanged=0 unusable=6 n=25
RESULT: VERDICT PASS
```

- 饮食 13 条：before（那一跳换成桩）**全部 474 字节**、after **68337–73401 字节**，逐条 sha16 全不同 ⇒ **13/13 全变**（这就是本票的改动面）。
- 非饮食抽样 6 条**可用**且 **6/6 逐字节同一**：`profile.set` 69296／`weight.log` 76947／`weight.batch` 73905／`body.measure-add` 83860／`exercise.add` 72543／`goal.set` 275（`goal.set` 回退面仍是 `template=receipt`）。
- 6 条**不可用**（两侧同退出码、同 0 字节，不构成比对）：`profile.update`／`profile.activity`／`goal.water` 缺前置 exit=4；`body.composition-add`／`photo.add`／`workout.plan-set` 缺参数 exit=2。
- **抽样口径明写**：12 条非饮食抽样覆盖全部 9 个非饮食域（档案 3 条／体重 2／身体 2／目标 2／运动／照片／训练计划），样本清单在 `probe_new.mjs` 的 `SAMPLE` 数组里，每条带 `why` 字段说明挑它的理由。

### 本席自设的新探针（专打作者脚本的自我满足盲区）——stub 泄漏探针 ＋ 空库探针

**STUB（作者完全没做的一条）**：把**编译产物**里那一跳 `dietReceiptDoc` 的整体换成打标记的桩，然后再逐条跑写命令——**桩生效则产物里带标记**：

```
STUB needle_hits=1  applied orig16=5CE688C93FDAF465 stub16=BBC3ECBC800A7DE1
RESULT: STUB dietRuns=14 dietMarked=14 otherRuns=12 otherMarked=0
```

读数分两半、缺一不可：**14 条饮食命令 14/14 带标记**（＝桩确实生效，否则「没泄漏」是空话）；**12 条非饮食命令 0/12 带标记**（含 6 条真出了产物的）。
⇒ 「那一跳只被饮食那族摸到、非饮食侧一个字节都没经过它」这条**是拿反向自证过的**。

**EMPTY（空库）**——把前任 §七 里那一格**补上**，并修正前任的假设：

```
RESULT: EMPTY n=14 blocked=9/14 noArtifact=9/14 errShaped=9/14
ERR 4: 取数失败（缺失阻断）：来源无饮食记录（2026-09-08）      ← 与共同口径§九逐字一致
```

- **9 条按设计失败且 exit≠0、一个产物文件都没落**（`copy`／`remove-by-date`／`remove-by-range`／`remove-by-type`／`update-by-date` exit=4；`remove`／`update`／`product.update`／`product.deprecate` exit=2「缺参数 id」）。
- **5 条 exit=0 且落产物是对的**：`diet.add`／`diet.batch`／`product.add`／`water.log`／`product.import` 都是**新增行**的写命令，空库正是它们的正常输入 ⇒ **前任「空库下所有写命令都该失败」的假设本身不成立**，本席按实况写，不按假设写。
- 共同口径「空窗 ≠ 空库，exit 4 ＋ `ERR 4: 取数失败（缺失阻断）：…`」的**逐字运行时文案**这一格（前任留白的第 1 格）⇒ **本席取到并逐字相符**。

**BADOUT（落点非法）**——前任留白的第 2 格：

```
RESULT: BADOUT exit=5 blocked=1 noArtifact=0
ERR 5: 渲染失败：显式落点 …\blocker\receipt.html：EEXIST: file already exists, mkdir …\blocker
```

⇒ 「父路径是普通文件」时 **exit=5、目标文件未写出**（`fileWritten=0`）。`artifacts=1` 是**本席探针目录里那个 `blocker` 文件本身**（不是产物），如实写明以免误读。

### 变异自证（两处方向各一，红／还原两行可分辨；只打编译产物，跑完逐字节取回）

```
MUT M1 hit=1   RED-M1: RESULT: B46 total=46 dietMembers=13 othersEarlyNull=33 valueReturned=0
               RED-M1-LINE: B46 key=calorie.water.log earlyReturn=null
MUT M1 restored byte_identical=1
MUT M2 hit=1   RED-M2: RESULT: B46 total=46 dietMembers=15 othersEarlyNull=31 valueReturned=0
               RED-M2-LINE: B46 key=calorie.goal.set earlyReturn=throw
               RED-M2-STUB: STUB key=calorie.goal.set side=other exit=0 bytes=71780 marked=0
MUT M2 restored byte_identical=1
GREEN: RESULT: B46 total=46 dietMembers=14 othersEarlyNull=32 valueReturned=0
```

- **方向一「漏一条」**：摘掉 `calorie.water.log` ⇒ 认领数 14→**13**、早退数 32→**33**，那一条落回 `earlyReturn=null`（正好是 `B46` 断言的判据本身）。
- **方向二「多一条」**：把 `calorie.goal.set` 塞进饮食命令集 ⇒ 认领数 14→**15**、早退 33→**31**，那一条变 `earlyReturn=throw`（＝被误认领）。
- **还原**：两次 `byte_identical=1`（编译产物)**＋** 绿态重取回 `dietMembers=14 othersEarlyNull=32` ⇒ 红／还原两态可分辨。
- 口径（诚实写）：变异打在**编译产物**上而不是源码上，因为本席声明路径不含源码。前任已用同一口径；本席额外做了**漏一条／多一条两向**与**还原逐字节**这两件。

### 三件测试的当刻红绿（本票判据④「不新增红」的当刻读数）

| 测试件 | 当刻 sha16 | 读数 | 判定 |
|---|---|---|---|
| `test/profile-doc-179.test.mjs`（391 行） | `DB7D210C0D8B02FE` | `tests 10 / pass 6 / fail 4`，**4 条红** | 4 条红**全部不在本票范围**：2 条属预检确认页（`预检确认页按钮不对`、`预检确认页 菜单缺用途提示：粘贴给 AI / 自己看` ⇒ #277 那条线）、2 条属体重／身体域（`weight.log 缺：体重 · 写后回执`、`记体脂 缺场景 08 眉标`）。本票那条 `#269／#270 饮食写命令已是完整文档，且四块标题（老实物逐字）在产物里` **绿**。 |
| `test/delivery-83.test.mjs`（345 行） | `16CE3BF9ABEF48C5` | `tests 13 / pass 13 / fail 0` | **全绿** |
| `test/calorie-c43.test.mjs`（202 行） | `C4EEEC8F694B85DE` | `tests 9 / pass 9 / fail 0` | **全绿**（前任窗口 C 那一格 c43 红已被改到新口径，现全绿） |

⇒ **判据④达成**（我这条路不新增红：三件里除本票那条外，红的四条全可归因他票／他域）。

### 工程红线与台账（本席自己跑，不抄别人的）

- **改动是否只在声明路径内**：`git status --porcelain -- <本席声明路径>`（报告件与 `.scratch/t269r/`）输出为空（`.scratch` 在忽略清单）；`FULL-DIRTY-TRACKED count=169` 全是别人的在途件。本席全程未碰源码，也未跑任何生成器。
- **台账 `--dry`**：`LEDGER-DRY exit=1`、`RESULT: 76/82`、`SCAN-ROOT: D:\ilife\packages\skill-calorie`、`LEDGER: ...\AGENTS.md`。
  6 条 `RED 台账陈化` 逐条为：`src/weight/history.ts` 860/862、`src/render/sportPortDocs.ts` 666/757、`src/render/sportDocs.ts` 417/423、`src/weight/volatilityDoc.ts` 406/411、`src/weight/log.ts` 401/402、`src/render/planEditorRuntime.ts` 356/364。
  **全部在他席在途件上，本票的判据路径里一条都没有**；前任记的 D1（`src/diet/receipt.ts` 超线、结论列空着）当刻**已不在红名单里**（该件现 321 行、落回 350 线内）⇒ **D1 按实况撤销**。
- **抗污染**：`POLLUTION-CHECK changed=0`（电池首尾五个关键件 sha256 一致）⇒ 本窗口的产物读数不受别人中途重编影响。

## 八·四　逐条判据读数与关票建议（关闭动作由编排者做，本席只出建议）

### 八·四·一　票面五段判据逐条

| # | 票面判据（正本） | 本席当刻读数（窗口 I，15:45–15:47） | 达成？ |
|---|---|---|---|
| 1 | 13 条写命令逐条实跑 `exit 0`，产物为完整文档，附落盘路径与字节数 | `RESULT: A13 ok=13/13 strict=13/13`；68337–73401 字节；13 条路径＋sha16 在 `i-a13.log`；另 2 条默认落点全对 | **达成** |
| 2 | 其余写命令的产物逐字节未变（前后快照逐 sha256） | `RESULT: DIFF dietChanged=13 dietSame=0 othersSame=6 othersChanged=0 unusable=6`＋`RESULT: VERDICT PASS`；闭包 `total=46 dietMembers=14 othersEarlyNull=32 valueReturned=0`；stub 反向自证 `dietMarked=14 otherMarked=0` | **达成**（口径：当刻 46 条写命令，「其余」＝32 条，票面「22」是 35 条册时代的旧数） |
| 3 | `pnpm build` exit 0 | **本席没跑 `pnpm build`**（它后半段会写受版本控制的生成物，本席声明路径只读全仓）。**编译这一半的当刻读数是绿的**：`tsc -b packages/skill-calorie` **exit=0、0 条错**，且与运行读数**同一持锁窗口**内先编译后立刻跑 | **编译面达成；`pnpm build` 整条命令未跑，见八·四·二第 1 条** |
| 4 | 全包测试不新增红 | 本票判据路径上不新增红：`profile-doc-179` 6/10（4 条红全在预检确认页与体重／身体域）、`delivery-83` 13/13 绿、`calorie-c43` 9/9 绿。全包基线**不可重测**（树前移上百提交，前任已留档） | **达成**（按「我这条路不新增红」判） |
| 5 | 证据写进票面评论，对账文件落 `docs/skills/skill-calorie/` | 票面 2 条评论；盘上 14 件；门禁日志 30 对 `START`/`RUN` 逐条可查（除 D2 一条标识写错） | **达成**（附 S3 记账缺口） |

### 八·四·二　关 or 不关

**建议：关**。理由按当刻读数给：

1. **判据③的前置条件已满足**：前任卡关的唯一理由是「编译面当刻不绿」（4 条类型错 ＋ 生成物语法错把 CLI 打死）。本席三次窗口复测：
   `tsc -b packages/skill-calorie` 在窗口 I **exit=0、0 错**；`dist/triggers/routes.generated.js` 与 `dist/diet/receipt.js` `node --check` **均 exit=0**（D6 事故已解除）；真出口冒烟 exit=0。
   **一条口径要请编排者一句话落定**：本席**没有跑 `pnpm build` 整条命令**（它含生成步骤，会写受版本控制的 `src/cli/*.ts`、超本席声明路径），
   而票面判据③字面写的是 `pnpm build` exit 0。本席能给的替代读数是「**编译段 exit=0 ＋ 生成物语法对 ＋ 真出口冒烟通过**」三件。
   **若编排者要求字面满足**：由有权限的席位在安静窗口补跑一次 `pnpm build`（本席按纪律不跑）。
2. **本票自己的交付物没有未落定的缺陷**：前任记的 **D1（`src/diet/receipt.ts` 超 350 告警线、台账结论列空着）当刻已按实况撤销** ——
   该件现 **321 行**、已落回线内，台账 `--dry` 的 6 条红**一条都不在它身上**。⇒ 关票不再有「带着自己那件红关掉」的问题。
3. **判据④有当刻读数**：三件测试里红的 4 条**全部可归因他票／他域**（2 条预检确认页 ⇒ #277 线；2 条体重／身体域 ⇒ 他图）。
4. **判据①②在本席更严的断言下重新成立**（13/13 严格完整文档；13 条全变、非饮食抽样 6/6 逐字节同一、stub 反向自证 otherMarked=0、闭包 46 条）。
5. **当刻作业面仍然不安静**（`FULL-DIRTY-TRACKED=169`），但这不影响本票判据：本票窗口首尾五件 sha256 **changed=0**，
   且编译态当刻是**绿**的（窗口 I 实测）—— 与前任离场时的状态已经不同。

### 八·四·三　要转出去的三件（S3／记账，不阻塞关闭）

1. **D2**：`docs/skills/skill-calorie/t269-final-verify.md` 引的 `t269f-targeted-final`（`tests=29 pass=28 fail=1`）在 `.scratch/locks/gate-runs.log` **查无此条**；
   同票引的 `t269f-cli13-docs`／`t269f-verify-docs` 同样查无此条（本席逐字筛，0 行），作者票面自带替代说明（`t269f-cli13`／`t269f-verify-2`，两条 **RUN 行 exit=0**）。
   ⇒ 建议：把证据件里的标识改成日志里真有的那三条（**一行字**的事），或补记那一次运行。
2. **D3**：同一份证据把 `t269f-full-before`（exit=1）／`t269f-window2`（exit=1）／`t269f-targeted-before`／`after`（均 exit=1）当门禁条目引，
   与自证段「只领退出码为 0 的条目」字面相冲。**请编排者一句话裁定**：用两次红跑做前后计数对照，算不算门禁条目。
3. **判据②的数字口径**：票面写「其余 22 条」，当刻实况是 46 条写命令、「其余」32 条（作者证据件 `RESULT-DIFF keys=46 … others_same=33/33` 用的是 33，与 46−13=33 一致，
   作者把 `product.import` 也接进了整页，故饮食侧是 14、非饮食侧 33）。⇒ 建议在关票记录里写清 22→32/33 的取代关系，免得下一手拿 22 去对。

### 八·四·四　未做项与下一手缺什么

| 缺口 | 为什么没做 | 下一手一条命令 |
|---|---|---|
| 票面判据③字面的 `pnpm build` exit 0 | 该命令含生成步骤、会写受版本控制的生成物，本席声明路径只读全仓 | 有权限的席位在安静窗口跑一次 `pnpm build`（须持锁） |
| 全包 `pnpm test` 的前后对照 | 基线不可复现（前任已复核：树前移上百提交，「当前树 vs 当时树」不是可比对照） | 若编排者要这条，应指定**冻结提交**做基线 |
| 6 条非饮食抽样「不可用」的那 6 格 | 两侧都 exit≠0（缺前置／缺参数），不构成逐字节比对 | 各自补前置再跑一次：先 `calorie.profile.set` 再 `profile.update`／`profile.activity`；`goal.water` 先 `goal.set`；`body.composition-add`／`photo.add`／`workout.plan-set` 补必填参数 |
| 本席三次窗口的守门教训 | 窗口 F／G／H 因守门过严或误判**未跑成电池**（F：tsc 一红即止；G：拿 mtime 判产物新鲜度；H：把包内任何类型错当中止） | 已写进 `windowI.ps1` 头部注释与本节八·二，供后来者直接抄 |
