# t616r 复核席 · 结论：**PASS**（五维 93/100，无 S1）

> 被复核提交：`ee4c795`（修复件，唯一产品件 `packages/skill-calorie/src/render/trendMiscPortDocs.ts`
> sha256 前 16 位=`ace1b191e70d7a5c`／**30871 B**／LF **608** —— 本席进场当场复算，三项全等于票面头行）／
> `581ff47`＋`2b67897`（前序证据件）／`9b94a9c`（复验证据件）。
> 票面入口：`gh issue view 616 --json body`（场景02验收缺陷①：看营养分析/看每日六因素 标题去日期（日期进正文））。
> 本席只写：`docs/skills/skill-calorie/t616r-*`、`.scratch/t616r/**`；产品代码与他席测试**只读**。
> 运行正本：`docs/skills/skill-calorie/t616r-gate-runs.log`（16 趟 RUN 行，与 `.scratch/locks/gate-runs.log` 逐条对账；
> 按协议 §2.3.4，**本笔收口提交那趟**的 RUN 行构造上落不到导出件里，导出件比正本尾部少恰好一条 —— 复核请直接查正本里 `runId=t616r-commit`／`t616r-commit2` 两行）。

## 一、回执四段

**第一行：PASS。** 票面五条验收**逐条由本席独立重跑**（自写探针 `.scratch/t616r/`，不采信实施席抽法）：
编译 `exit 0`；两页真出口产物 H1 无日期＋正文**首件就是** `div.dui-window` 且带窗口日期＝**4/4 绿**；
三件测试 **34/34 绿**；场景10 前后**同名同数**（改前改后各 `82/79/3`，三条红逐条同名）；同件 7 页新旧比对
**变 2 页 / 未变 5 页**、NEW≡NEW2；变异自证（本席自选落点）**红 2 → 还原绿 0**；告警线门 **91/91 PASS**。

**第二段：本席运行记录（GATE-RUN 表，命令原文见导出件）**

| 趟次 | runId | 命令 | RESULT |
|---|---|---|---|
| 编译＋两页判据 | `t616r-acc2` | `node .scratch/t616r/acc.mjs` | **exit 0** → `BUILD exit=0`／`RESULT: 4/4 PASS` |
| 票面三件测试 | `t616r-trio` | `node --test …/t511-清尾二.test.mjs …/render-t9.test.mjs …/trend-misc-port-113.test.mjs` | **exit 0** → `tests 34／pass 34／fail 0` |
| 场景10 全量 | `t616r-analysis2` | `node --test packages/skill-calorie/test/analysis-*.test.mjs` | **exit 1** → `82 测／78 绿／4 红`（4 红里的 `#384 看健康趋势` 经复跑判为**他席在途**，见 §2.4） |
| 新旧对照＋同趟两遍场景10 | `t616r-rt` | `node .scratch/t616r/rt.mjs` | **exit 0** → `changed=2 unchanged=5`／`ANALYSIS 改前 82/79/3 ＝ 改后 82/79/3`／`ROUNDTRIP-PASS` |
| `#384` 单件复跑 | `t616r-384` | `node --test packages/skill-calorie/test/analysis-report-384.test.mjs` | **exit 0** → `tests 5／pass 5／fail 0` |
| 变异确认 | `t616r-mut` | `node .scratch/t616r/mut.mjs` | **exit 0** → `MUTATION-PASS`（§四两行） |
| 告警线门 | `t616r-warnline` | `node packages/skill-calorie/scripts/check-warning-line.mjs` | **exit 0** → `RESULT: 91/91` `PASS` |
| 新探针 | `t616r-newprobe2` | `node .scratch/t616r/newprobe.mjs` | **exit 0** → `RESULT: 13/13 PASS` |
| 门禁83（本席副本） | `t616r-gate83b` | `node .scratch/t616r/gate83.mjs` | **exit 0** → `非脚注零命中 83/83`；来源脚注命中 66（归 #560）；`GATE83-PASS` |

> **本席自己的四趟红（自曝，均为本席脚本缺陷／口径错／漏步骤，不是被测件红）**：`t616r-envprobe`（首个勘察脚本写崩）、
> `t616r-acc`（首版抽法把 `<style>` 里的 `page-shell-body` 当锚点，`4/4` 报 FAIL；改成
> `<div class="ilife-block-page-shell-body">` 后 `t616r-acc2` 4/4 绿）、`t616r-newprobe`（首版把「空窗」当可渲染态，
> 实测 exit 4 无产物；改成「缺项窗」后 `t616r-newprobe2` 13/13 绿）、`t616r-gate83`（本席副本**没先落盘**就开跑 →
> `MODULE_NOT_FOUND`；补 `Copy-Item` 后 `t616r-gate83b` 绿）。全部在 `.scratch/t616r/*.log` 与正本里留痕。
> 另 `t616r-analysis`／`t616r-analysis2` 两趟 exit 1 是**套件自带红**（他席陈旧断言／在途态），见 §2.4，不是本席脚本红。
> **一处必须自曝的操作失误**：`t616r-commit2` 那趟，本席首版提交脚本在暂存区里检出**他席在途件**（共 6 件以上，
> 属场景06／t576 一族），随后执行了 `git reset -- <全部暂存件>` —— 这会把别席已经暂存的件一并取消暂存。
> 影响面：本席 `git reset` **只取消暂存、不动工作区**，别席文件内容一字未改；且各席提交一律走
> `git commit -- <自己路径>`（路径提交），其暂存被取消不丢产出、下次 `git add` 即回。本席当场把提交脚本改成
> **非破坏式**（检出他席件只报不 reset），并以 `t616r-commit2b` 重提交。此条按「共享工作区红线」如实记账，请该席知悉。

**第三段：逐条路径**
- 本席新件：`docs/skills/skill-calorie/t616r-证据.md`（本件）、`docs/skills/skill-calorie/t616r-gate-runs.log`（运行条目导出）。
- 本席草稿（不入仓）：`.scratch/t616r/`：`libj.mjs`（自写判据小件：H1 精确取、正文首件按标签配平、全部标题位）、
  `acc.mjs`（票面 1～3 条）、`newprobe.mjs`＋`gate83.mjs`（本席副本，路径改本票目录）、`rt.mjs`（新旧对照）、
  `mut.mjs`（变异）、`inspect*.mjs`／`sha-audit.mjs`／`title-sites.mjs`／`side-effects.mjs`／`kpi.mjs`（勘察）、各趟 `*.log`／`*.json`。
- **一行未动**：产品件与他席测试全程只读；变异后 `git diff --numstat` 空、sha256 回到 `ace1b191e70d7a5c`（`t616r-mut` 末行自证）。
  两名未推：`git log --oneline origin/master..HEAD` 25 条（本票在未推集合内）⇒ 没单人推送。

**第四段：没做完啥 ＋ 下一手缺啥**
- **没做**：① 未重出 `scene02-验收墙/**`（不在本票写集；票面「复验由 #619 统一重出墙＋复拍」）。② 未替实施席补
  「`t280-真跑台账.json` 页内H1 陈化」的同步（不在写集，见 §六观察 3）。③ 未独立重跑 `boundary.mjs` 那套逐字节抹体件，
  改用更短的独立证法（§五·工程红线：`git show` 逐 hunk 落点核）。
- **下一手缺啥**：① 复拍／墙重出归 #619；② `t280-真跑台账` 两页「页内H1」行需随墙一并刷新；
  ③ 观察 1（参数表单退场）与观察 2（`dietUiCss()` 带来的死规则）建议在拆分票或下一轮墙审时点名确认。

## 二、票面验收逐条（本席读数，原样）

### 2.1 编译（票面第 1 条）
`BUILD exit=0`（`tsc -b packages/skill-calorie --force`，写死 node 入口，不用 `npx`）。

### 2.2／2.3 两页真出口产物（票面第 2、3 条）——**绿两行**
```
H1-NO-DATE   key=calorie.view.nutrition-analysis h1="营养分析"   命中日期=0 判=PASS
BODY-HAS-DATE key=calorie.view.nutrition-analysis 首件=div.dui-window✓ 首件日期=["2026-09-09","2026-09-15"] 正文日期=["2026-09-09","2026-09-15"] 判=PASS
H1-NO-DATE   key=calorie.view.six-factors      h1="每日六因素"   命中日期=0 判=PASS
BODY-HAS-DATE key=calorie.view.six-factors      首件=div.dui-window✓ 首件日期=["2026-09-15"] 正文日期=["2026-09-15"] 判=PASS
RESULT: 4/4 PASS
```
产物 sha256 前 16 位（本席重跑，与实施席读数**逐位相同**）：营养分析 `dc99bc36b0d39e8c`（78304 B）、
六因素 `03234a56ae67c2e6`（71633 B）；改前侧 `d3eab859652e2688`（77943 B）／`b1ea71aa4bc29f37`（70915 B）。

### 2.4 三件测试与场景10（票面第 4 条）
- 三件测试 `34/34`，`exit 0`。提交面复核：`ee4c795` **只改 1 件**（`git show --stat`），两件测试一字未动（实施席「未改测试」的声明成立）。
- 场景10：**同一受锁窗口内**用同一种子跑改前／改后**各一遍**（`t616r-rt`）→ 两侧都是 `82 测／79 绿／3 红`，
  红条逐条同名（`weightCompare：17 场景分发`／`weightCompare：平台期/极端月/同期`／`simulate：外推+模拟+摄入`）。
- **一处必须点名的读数**：此前一趟 `t616r-analysis2` 读出 `82/78/4`，多出的那条是
  `#384 拿错页即红：看健康趋势 非 exit 0`。本席判它**不是本票红**：① 该用例属报告族
  （`analysis-report-384.test.mjs`，`calorie.report.trend`），与本票 8 键写集无交集；② `ee4c795..HEAD` 之间
  报告族有他席三笔提交（`067c506`／`a9527ff`／`e1c4702`）；③ 单件复跑 `t616r-384` 当场 `5/5 绿` ⇒ 那一趟读的是**工作区在途态**。

### 2.5 新旧对照（票面第 4 条后半，本席自跑 `t616r-rt`）
```
CHANGED   calorie.view.nutrition-analysis 旧sha=d3eab859652e2688 新sha=dc99bc36b0d39e8c 旧H1="营养分析 2026-09-09 至 2026-09-15" 新H1="营养分析"
CHANGED   calorie.view.six-factors        旧sha=b1ea71aa4bc29f37 新sha=03234a56ae67c2e6 旧H1="每日六因素 2026-09-15" 新H1="每日六因素"
UNCHANGED 热量趋势 30ba8d8ca8b6dddd／整体趋势 9cefa87cae3f6af0／数据健康检查 b121036020cf0a9b／复盘报告 f4aea4b2a543b85a（四页逐页 sha 相同）
BLOCKED   落地训练进度 exit=4（无计划无运动，改前改后同态）
ROUNDTRIP pages=7 changed=2 unchanged=5   NEW≡NEW2（逐页）   旧侧源码=ee4c795^（sha 74b5a388cafc42ec／30378 B）
```
⇒ 「同件其余页一字未变」成立；`~` 与新单日形态都只落在本票两页。

## 三、自设新探针（被审脚本覆盖不到的三处，`RESULT: 13/13 PASS`）

被审脚本 `.scratch/t616/probe.mjs` 的覆盖面＝**每键第一条唤醒词 × 有数据种子窗 × 只读 H1**。本席新探针补三处：

- **NEWP-A 缺项窗**（有餐、无当日体重／饮水／运动）：营养分析 `{window:"custom",2026-08-01~2026-08-07}`、
  六因素 `{date:"2026-08-01"}` ⇒ 两页 H1 仍＝短名、首件仍是 `dui-window` 且带该窗口日期 → **2/2 PASS**。
- **NEWP-A-OBS 空窗**：`{window:"custom",2000-01-01~2000-01-31}`／`{date:"2000-01-01"}` ⇒
  `exit=4 ERR 4 取数失败（缺失阻断）`、**不产页**。⇒ H1 判据在该态**不适用**；这条取数层面口径
  已由 `docs/skills/skill-calorie/t279-报告.md:255` 记为逐格缺陷第 82 格、归属 #275 并被 `EMPTY_WINDOW_EXCEPTIONS` 锁住 ⇒ 不是本票缺陷。
- **NEWP-B 同键全部唤醒词**：`calorie.view.nutrition-analysis` 三条（含场景10「看钠糖纤维趋势／看钠糖纤维综合」）、
  `calorie.view.six-factors` 两条（含场景10「看每日 6 因素综合」）逐条真出口重跑 → **5/5 词判据 PASS**，
  且同键各词**产物字节相同**（78304 B／71633 B）⇒ 改的是共用装配，五个入口同产物，没有留下带日期的入口分支。
- **NEWP-C 全部标题位无日期**（`<title>`／`h1`～`h3`／眉标／副标题；有数据 ＋ 缺项窗两态）→ **4/4 PASS**；
  另核全页文本节点里「短名＋日期」旧标题串命中 **0**（两页各只有 H1 一处出现短名）。票面原话是「标题不能有日期」，
  被审脚本只读 H1 一处，本席把口径扩到全部标题位。

## 四、变异确认（本席自选落点，两行读数）

```
变异后 票面判据红条=2 本席新探针红条=2      （变异A＝两页 H1 日期改回旧写法；正文判据仍绿，两半互不冒充）
还原后 票面判据红条=0 本席新探针红条=0      （逐字节写回＋重编译；sha256 回 ace1b191e70d7a5c，git diff --numstat 空）
```
另有**一条专打被审脚本盲区**的变异 B：把日期挪进**眉标**（H1 保持干净）⇒
`被审探针口径（只看 H1）红条=0`、`本席 NEWP-C 口径（全部标题位）红条=2` —— 证明本席新探针**不是永绿重述**，
它挡住的是被审脚本结构上读不到的那一处。`MUTATION-PASS`。

## 五、五维打分（满分 100；任一 S1 ⇒ FAIL，本席未判出 S1）

| 维 | 分 | 依据 |
|---|---|---|
| 契约一致 30 | **29** | 票面目标逐条落位：H1＝人话短名（两页）、日期进正文首件**现成件** `diet/dietUi.windowStrip()`（单日退化成 `（单日）` 一枚块）、窗天数胶囊、原换算说明**一字未删**改住口径行、模块常量与其余 5 个函数一行未碰、两件测试未动、没新造形状（`dietUiCss`／`windowStrip` 均为包内现成件）。扣 1：撤掉老窗口参数表单比「最小改动」多一步（见观察 1）。 |
| 证据真实 25 | **24** | 票面 6 类读数**逐条复现**（编译／4-4／34-34／82-79-3 同名同数／两页 sha／变异红 2→绿 0），门禁83 83/83 与告警线门 91/91 独立重跑同值；`ee4c795` 只含 1 件、证据两笔只含 `t616-*`。扣 1：`§5.1` 自己那趟 `roundtrip.mjs exit=1` 的说明虽诚实，但证据件把「场景10 未变」的举证重心放在**新建件**上（原 `roundtrip.mjs` 落盘后失效），可核性打了折——本席用自建 `rt.mjs` 从 `ee4c795^` 重取基线证成。 |
| 新旧对照 20 | **20** | 本席从 `ee4c795^` 亲自换件重编译重跑：7 页**只 2 页变**、其余 5 页（含 1 页同态阻断）逐页 sha 相同；NEW≡NEW2；改前改后场景10 同名同数；还原自证干净。 |
| 工程红线 15 | **14** | LF **608→608**（`AGENTS.md` 台账「当场实测」列＝608 与实况相等，无需同步，门 91/91 绿）；`git diff` 逐 hunk 落点：4 个 hunk ＋ 1 行 import，全部落在 `buildNutritionAnalysisDoc`／`buildSixFactorsDoc` 两函数体内或 import 块（本席独立核，不采信 `boundary.mjs`）；未碰 `analysis/**`／取数口径／路由／`base-render/**`；无危险 git 命令、未推送。扣 1：`dietUiCss()` 顺带带进一条与票面无关的全局规则（观察 2，实测无作用面）。 |
| 文档同步 10 | **6** | 本票自己的证据面很齐（四段＋逐条读数＋边界＋时序说明，`t616-证据.md` 与实况逐条相符），包内行数台账无需改且实跑绿。扣 4：两处仓内文档仍停在改前——① `docs/skills/skill-calorie/scene02-验收墙/{看营养分析,看每日六因素}.html` 仍印旧 H1（含老参数表单，时间戳 13:29–13:30 早于 `ee4c795` 14:14）：票面已把墙重出／复拍写明归 **#619**，属**已声明延后**；② `docs/skills/skill-calorie/t280-真跑台账.json` 两页「页内H1」仍是旧值（`:2075` `营养分析 2026-09-09 ~ 2026-09-15`、`:2091` `每日六因素 2026-09-15`），证据件**没点名**这条陈化。两件都不在票面写集（只许写 `t616-*`），故不计为缺陷，只计同步缺口。 |

**S1 判定：无。** 逐条查过 S1 面（标题判据可红可绿、日期不是被删而是搬家、跨族/场景10 未变、写集未越、双半判据互不冒充、
新探针非永绿），未见必须打回的项。

## 六、观察项（不是缺陷，供下一手点名）

1. **老窗口参数表单退场**：营养分析页正文首件原来是 `renderParamForm`（两个可编辑 `<input>`＋换算说明）。改后换成只读窗口条。
   本席核过：`base-render/src/blocks.ts:948` 明写该件「**零 JS**」（宿主侧读），两版页面 script 段里提到表单字段的段数都是 **0**
   ⇒ 撤它**不断页内交互**；换算说明原样改住口径行；包内先例＝#523 R5 把记录级明细页的 `renderParamForm` 换成只读 `factStrip`
   （理由正是「日期只住 `value` 属性、复制读不到」）。⇒ 本席判**允许**，但票面没写这一条，建议在墙审／拆分票里点名确认。
2. **`dietUiCss()` 顺带带来一条无关规则**：`.ilife-block-kpi-card-grid>:last-child:nth-child(odd){grid-column:1/-1}`（#587 孤行版式）
   在旧页命中 0、新页命中 1。实测**无作用面**：两页各只有 1 个 KPI 网格，卡数 4（营养分析）／6（六因素）都是偶数，
   该规则不匹配。⇒ 是死规则，不是版式变更；建议归拆分票时一并处置。
3. **文档陈化（见 §五文档同步②）**：`t280-真跑台账.json` 两页「页内H1」需随墙刷新；`t155-产物册子.json` 是老册子（历史件，不属同步面）。
