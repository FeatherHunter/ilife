# 票 #392（MAP #638）证据：t11 场景行计数改派生

**判定：完成**（唯一改动件 `packages/skill-calorie/test/skill-t11.test.mjs`；t11 9/9 绿、`gen:check` 绿；变异红／还原绿两行齐）。

**提交：`4ebf46567012ef0efe887572798b365e6e0ee4bd`**（`fix(392): t11 场景行计数改派生……`，2026-09-16T20:15:18+08:00，1 file changed ＋24/−3；暂存区提交前复核恰一件）。

## 一 · 现状实测（含窗口内数据面漂移）

| 时点 | runId | TRIGGERS.length | WAKE_ASSETS.length | 场景源条数 | text 态 4 空格行 | 含 `calorie-cmd-read` 的 4 空格行 |
|---|---|---|---|---|---|---|
| 开工初测 | `7ac20507-5ffb-47d4-af1a-d030386d2984` | **440** | 436 | 440 | **440** | 0 |
| 收口实测 | `48357638-a4fe-474c-9f8b-3b2aa9466ae5` | **437** | 436 | 437 | **437** | 0 |

两次读数之间，工作区另有席位提交并重建：`5384ce63 feat(592)：删看不懂的唤醒词…并重生`（19:36）＋ `f3142e92 test(501)…`（20:09）；`dist/triggers/scene-10-analysis.js` mtime 20:11:06、`dist/photo/helpCenter.js` mtime 20:11:51。**即：总工单里「TRIGGERS 440」的初测值在窗口内被并发提交改成了 437**（`dist` 与 `src` 同源，见 `snapshot.mjs` 读数）。旧断言（冻结 436）在 440 与 437 两个状态**都红**；本票改成的派生口径在**两个状态都绿**——这正是改派生的理由，也是本票头号读数。

其余实测（`probe.mjs`／`probe2.mjs`）：唤醒词 440 条里唯一词组 438 条、重复样式 `记身材照×3`；**没有任何唤醒词含 ` · ` 分隔符或首尾空白**（故按首个 ` · ` 切词即原文，多重集判据可写）。

## 二 · 改动（只此一件，45 行 diff）

`packages/skill-calorie/test/skill-t11.test.mjs:112-134`：

1. 删掉手写冻结数 `assert.equal(<4 空格行数>, 436)`；
2. 改为三条**全部从数据派生**、只紧不松的判据（无删除、无跳过、无 `>=`）：
   - `assert.deepEqual(4 空格行, buildHelpSceneData() 场景源逐行还原)`——条数、序、逐字全钉；
   - `assert.deepEqual(4 空格行唤醒词多重集, TRIGGERS 唤醒词多重集)`——含重数，逐条对齐声明面；
   - `assert.equal(场景源条数, TRIGGERS.length)`——「行源规模 == 该渲染面真正依据的行源规模」直陈；
3. 上方注释改写为「场景行 4 空格／入口行 2 空格 + 行源是 `buildHelpSceneData()`（TRIGGERS 运行期投影）+ 数一律派生」口径。

派生依据的机械事实：`renderTextIndex` 对**每个** `subgroup.scenes[]` 恰好发一行 `'    ' + wake_word + ' · ' + id`（`src/photo/helpCenter.ts:479-488`），入口行恒 `'  '`（`:491`），故 4 空格行集合 == 场景源集合，是本渲染面的**唯一行源**。

## 三 · 机器读数（一律经 `node tooling/run-locked.mjs --ticket 392 -- …`）

| # | 命令 | runId | exit | 读数 |
|---|---|---|---|---|
| R0 | `node --test …/skill-t11.test.mjs`（改前） | `9d41ea88-f52e-4d74-9e28-0f8bc8228776` | 1 | 8/9，`AssertionError 440 !== 436`（`:113`） |
| R1 | `node …/gen-cli.mjs --check`（改前） | `5b9c5fa7-674a-47de-ab25-0b2aa73255e1` | 0 | 五件 `GEN-CHECK ok`，键 129（写 47＋读 82），未搬迁 0 |
| R2 | `node …/gen-cli.mjs --check`（改后） | `c600e8dc-713c-496d-ac14-8b55c0161187` | 0 | 同上，五件 sha256 **逐字不变**（派生件未碰） |
| R3 | `node --test …/skill-t11.test.mjs` | `ada038dd-20a7-4c63-ae78-0887378a0bdf` | 0 | **tests 9／pass 9／fail 0** |
| R4 | 同上（M1／M2 变异还原后） | `24238a51-f26c-4476-a0f2-c0dbe9b32e4c` | 0 | **9／9／0** |
| R5 | `node --test …/help-center-88.test.mjs` | `d4e50709-712f-4408-9ccb-fe10fdbb09e2` | 1 | 25 用例 15 过／**10 红**（见 §五，**红因非本票**） |
| R6 | 快照脚本（同窗记数据面＋跑 t11） | `48357638-a4fe-474c-9f8b-3b2aa9466ae5` | 0 | TRIGGERS 437／WAKE_ASSETS 436／场景 437／4 空格行 437／含 cmd-read 0；**t11 9／9／0** |
| R7 | `tsc -b packages/skill-calorie --dry` | `1073d02e-1aaf-47f0-a7ef-b80528ca8358` | 0 | `Project … is up to date`（编译面无待办） |
| R8 | 持锁提交（`git add` 点名一件＋复核暂存区＋`commit -F`） | `1eb4e1cb-4ddc-4439-85ab-86eb23b264fd` | 0 | staged 恰 `packages/skill-calorie/test/skill-t11.test.mjs`；`[master 4ebf4656]`；消息文件 UTF-8 无 BOM（首三字节 102,105,120＝`fix`） |
| R9 | 提交后终验（t11 ＋ `gen:check`，同窗） | `2fe19666-1cc6-445a-b110-aa4b3fd543a0` | 0 | **t11 exit=0（9／9／0）＋ gen:check exit=0（五件 sha 与 R1 逐字不变）**；当刻 TRIGGERS 437／scene 437／indent4 437 |

（另：提交首跑 `2dc01ace-d805-43d0-83cb-7049f339da60` exit=1——脚本误用 pwsh 已移除的 `Get-Content -Encoding Byte`，在 `git add` **之前**即中止，无任何 git 副作用；改 `-AsByteStream` 后 R8 通过。）

### 变异自证（两行）

- **变异红** `bfabd95c-f622-4b75-914b-bc36db651442`（M1：派生式改 `TRIGGERS.length + 1`）⇒ 8/9，红条文案 `MUTANT-M1 场景行源规模 == 声明面 TRIGGERS.length`，exit=1。
- **变异红（更强，直击本断言意图）** `01e962bd-588b-4f91-99cf-ce091d4b5fdf`（M2：`dist/photo/helpCenter.js` 里入口行也缩进 4 空格＝场景行源多出 6 条，模拟「入口行混进场景行」）⇒ 内层 `node --test` **exit=1**、8/9，红条＝`4 空格行必须与场景源逐行逐字对齐`；同一锁窗内还原，`dist` sha256 `d030fa981c2edada…` **补丁前=还原后**（`true`），无残留。
- **还原绿** `24238a51-f26c-4476-a0f2-c0dbe9b32e4c` / `48357638-a4fe-474c-9f8b-3b2aa9466ae5` ⇒ 9/9 exit=0。

## 四 · 红线遵守

- `wake-assets.ts` 未碰（WRITE：0）；`routes.generated.ts`／`keys.ts`／`SKILL.md` 未碰（R2 五件 sha 逐字不变）；
- 未碰 #343 OOV 清账、未碰别包、未改任何其他测试断言（`git diff --name-only` 仅本件）；
- 未用任何危险 git 命令（无 `switch`／`reset`／`stash`／`clean`／`restore .`／`--amend`／`rebase`／`push`）。

## 五 · 靶向回归 `help-center-88`：**红，且非本票所致**

R5 读数：25 用例／15 过／10 红。红条全部是**同一类陈旧冻结数**（另一批手写常量）：

| 断言 | 实况 | 冻结期望 |
|---|---|---|
| A1 10 分组／54 子功能／**436 场景** | 437 | 436 |
| A1 types 直方图 329/79/6/22 | 回执 **80** | 回执 79 |
| A1 顶层键 = F3 键集（比 subtitle 字面） | `10 分类 · 437 场景…` | `… · 436 场景…` |
| P-2 updatedAt（同上比字面） | `10 分类 · 437 场景` | `10 分类 · 436 场景` |
| D-1 三档徽章色逐档条数 | 329/80/6 | 329/79/6 |
| A2 复用冻结面 | **148** | 130 |
| A2 file 态 436 卡／1308 按钮 | 437 | 436 |
| 三态同源 436 个 `data-scene-id` | 437 | 436 |
| 守卫② id 唯一 436/436 | 437 | 436 |
| 守卫② HTML 层 436/436 | 437 | 436 |

**非本票所致的机械证明**：① 本票改动文件只有 `skill-t11.test.mjs`；② `help-center-88.test.mjs` 全文 `skill-t11` 命中 **0**，二者无 import 关系（各自独立进程）；③ 该件自身 `git diff` 为空（未改）。⇒ 它是**同一系统病的未清面**（手写冻结数与 base-paint 冻结面陈化），归属另一张票；本票按红线**不动它**。

## 六 · 未做项与下一手

1. **未跑 `pnpm build`**（`tsc -b && gen-cli --stamp && pnpm -r run build:client`）。代价：全 monorepo `tsc -b` ＋ 7 个插件包 `tsdown` 客户端打包（`plugin-manager`／`plugin-calorie`／`plugin-chef`／`plugin-home-ilife`／`plugin-memo-ilife`／`plugin-schedule-ilife`／`plugin-bill-ilife`），且会重写 7 包共享 `dist`；当刻工作区有并发席位在建、在提交（本席一次抢锁 **waitedMs=30001**），不宜再压一轮全仓构建。替代读数＝R7（编译面 `is up to date`）＋R2（`gen:check` 绿）。本改动是 `.mjs` 测试件，**不在编译面内**。
2. **未做「干净检出」验证**：本仓禁 `switch`／`stash`／`reset`／`clean`，需另开 worktree/克隆才能验，代价与并发风险大，未做。
3. 浅漂移提示（未改，属别票）：`src/photo/helpCenter.ts:474` 与 `:5`／`:223` 的注释仍写「436 场景」；`help-center-88` 10 条冻结断言仍红；`gen-cli.mjs --check` 绿但 `--stamp` 未重签（`pnpm build` 时自会签）。
4. 提交信息按实况写，**不嵌 440**：初测 440 在窗口内已被并发提交改为 437，派生口径与数字解耦，消息里嵌数字会立刻过时。

## 附 · 日志（只给路径，全在 `.scratch/t392/`）

`baseline-t11.log`・`gen-check.log`・`probe.log`（440 态）・`probe2.log`・`probe3.log`・`R2-gencheck-green.log`・`R3-t11-green.log`・`R4-t11-restored-green.log`・`R5-helpcenter88.log`・`R6-snapshot.log`・`R7-tsc-dry.log`・`M1-t11-red.log`・`M2-red-and-restore.log`・`final-diff.patch`；脚本 `probe.mjs`・`probe2.mjs`・`probe3.mjs`・`snapshot.mjs`・`mutate-dist.mjs`。
