# 交接：MAP #157 卡路里场景05 健身计划（32 条唤醒词跑通＋HTML 正在收口）

写给一个没有本次会话记忆的接手 agent。工作目录 `D:\ilife`。本图是 wayfinder 地图，地图 issue：`#157`（https://github.com/FeatherHunter/ilife/issues/157）。

> **本会话已停止派活**（负责人要求转交给新会话）。最后盘点时间 2026-09-14 15:41。接手第一件事见 §9。
>
> 本文件是 `.scratch/handoff/20260914-154109-健身计划HTML重做.md` 的副本（`.scratch` 被 gitignore，故在 `docs/` 留一份可提交副本）。

## 0. 一句话现状

32 条唤醒词链路已跑通（#346/#347/#348 已关）；负责人肉眼验收 HTML 后**打回视觉**，当前处于「HTML 重做到负责人能接受」阶段：176-185 十份出了 V2（审查 73/100，两个必改项已派未收口）、201-207 七份已统一、186-195 那 27 份**只需重跑**（源码早已改好）。**磁盘 C 盘将满（约 3.3GB）与一次未完成的清理是接手时的硬风险。**

## 1. 目标（Destination，摘自 #157 正文）

场景 05 健身计划 的 32 条唤醒词在 DSH 里都能从头走到尾：说唤醒词 →（写类流程先出过程型 HTML）→ 复制 prompt → AI 用命令执行 → 拿到结果型 HTML 落盘、回执给绝对路径。两条同时达成即本图完成：① 本图 32 条唤醒词都跑得通；② 产物是真实 HTML 文件、能打开、由负责人肉眼验收过。

32 条 = `packages/skill-calorie/src/workout/routes.ts` 里 `list:'wake' order 176–207`；对账总表 `docs/skills/skill-calorie/t168-scene05-reconcile.md`。其中 order 196–200（落地训练×3、训记×2）属外部 5 条，见 §3 #350。

## 2. 你的角色与纪律（必须遵守，否则会被负责人退回）

负责人只看最终效果与代码质量，不管技术细节。你的角色是**总工程师/编排者**：

- **只编排，不亲自实施**：每件活派 subagent；**每件活配 1 名独立对抗审查 subagent 并打分**（作者自评不算复核），你综合裁决整改/放行；细节决策你拍，但每个决策要用第一性原理自问“是否帮助达成目标”。
- **并发规划**：互相独立的活同时派；同文件冲突的活排队。
- **禁弹窗提问**（AGENTS.md 会话纪律）：问题写在对话正文，不用 `ask_user_question`。
- 必读纪律：`docs/agents/编排纪律.md`（地图执行者十一条）、`docs/subagent-concurrency-protocol.md`、`docs/agents/structure.md`（结构五铁律＋必报五步）、`docs/agents/wording.md`（用词纪律：禁「打通/闭环/落地(作实施讲)/壳/键/渲染层」，用规范词）。
- **issue 正文格式**：真实换行、每个 `## 章节` 独占一行、段落间空行、禁字面 `\n`、禁 BOM；一律 `gh issue edit <n> --body-file <文件>`；改地图正文必须「取回线上→就地改→写回」；进度契约 `## 进度：N%`＋空行＋`下一步：`；未确认不得 close。
- **git**：只 `add` 自己文件（禁 `-A` / `.`）；禁 `stash/reset --hard/clean/amend/rebase/switch`；长命令持锁 `node tooling/run-locked.mjs --ticket 351 -- <命令>`；他席并发作业，改动及时 commit＋push。
- **上下文纪律**：禁止让 subagent 整读 2163 行老 HTML（会爆上下文）；给规格文档＋小步任务。

## 3. 票据状态（`gh issue view <n>` 复核）

| 票 | 状态 | 要点 |
|---|---|---|
| #157 map | OPEN（正文记 60%，本轮 HTML 重做未回填） | 只在关票时同步 Decisions/迷雾 |
| #346/#347/#348 | CLOSED | 读筛选样板＋余 7 条读词＋创建类 5 条；提交 33ff950 / de748c0 / a97b59c |
| #349 | OPEN，已被认领（FeatherHunter） | 变更类 5 条；关票建议包 `docs/skills/skill-calorie/t349-close-ready-20260914.md`；对抗审查 **80/100 有条件放行**，待负责人回 2 句：①撤销是硬删除不可恢复、接受并另开可逆票 ②验收以**根** `test/` 为准（票面写的 `packages/skill-calorie/test/...` 不存在） |
| #350 | OPEN，票面 95% | 外部 5 条裁定材料 `docs/skills/skill-calorie/t350-external-five-verdict.md`（0621392＋7441e29），待负责人签 §4.1 与 §五（维持不做，只保命中＋文案） |
| #351 | OPEN | 真机端到端＋肉眼终审；预演证据 `docs/skills/skill-calorie/t351-dryrun-20260914.md`（c66af49） |
| #168 | OPEN | 只作对账证据，不再执行 |

## 4. 本轮 HTML 重做走到哪（重点）

负责人打回原话：① 201–207 七份 result 要删多语文本、要「复制数据＋复制日志」双按钮；② 其余 HTML 都不通过；③ 要在“老技能迁移 / 新页复用 / 新开发”里选融合方案。

已完成：
- 视觉审计（含截图）→ `.scratch/t351-visual/audit.md`
- 融合方案 → `docs/skills/skill-calorie/t351-visual-fix-plan-20260914.md`
- 201–207：删英文菜单、双按钮、补 `id="ilife-copy-data"/"ilife-copy-log"`（c9c836f）、眉标去英文命令键改中文（`render/sportPortDocs.ts:451`、`render/trendDocs.ts:428`＋证据第六节，**仍未提交**，原因见 §7）
- 176–185 V2：大标题「健身计划」＋副标题＋三指标卡＋每周 `<details open>`＋会话卡＋**六列真数据明细（动作/部位/类型/组数×次数/重量/备注）**＋空态整页＋底部双按钮（abb7c5c）；证据 `docs/skills/skill-calorie/t351-redesign-184-evidence.md`；规格 `docs/skills/skill-calorie/t351-redesign-184-spec.md`
- 195：撤销后读验证空态页（5255b01，新件 `render/planDeleteVerifyDocs.ts`＋`workout/plan.ts` 接线）
- 186–195 那 27 份**无需改代码**：侦察结论（会话 e9576b22 回执）——三处装配件（`render/workoutPlanDocs.ts` 的 `buildPlanProcessDoc`/`buildPlanWizardDoc`、`workout/receipt.ts`、`render/planDeleteVerifyDocs.ts`）四条判据（整页版式/双按钮/中文单语/成型版式）**源码全有**，差的只是那 24 份（186–194）是 V1 旧产物（产物 mtime 12:27 早于源码 13:17–14:52）→ **重跑即得**。

V2 对抗审查结论（会话 587fbd2f，全文 `D:\ilife\.scratch\t351-fix-review-v2\review-verdict.md`）：**73/100，小整改后放行**。
- 【必改 1】休息日标题重复渲染成「周二 · 休息日（休息日）」（`render/workoutPlanDocs.ts:114-116` 无条件追加）。
- 【必改 2】重跑夹具 sets 全空，导致卖点「组数×次数／重量」在证据里全是「—」；审查兵另搬生产库真会话渲染验证数值全对，但暴露必改 1。要求夹具补：休息日 1 条、多组不同次数 1 条、空 sets 1 条，并存真数据页留档。
- 【建议】指标卡「总周数」改用 `totalWeeks`（现口径与 detail.json 不同源）；证据里「页面壳/渲染层」改规范词；页头补 `config.description`。
- 两条已知降级**已写明**：不印休息列（库无 `rest` 字段，规格 md:66）、部位色块降级纯文本（证据 md:90）。

产物路径：
- V2 176–185：`D:\ilife\.scratch\t351-fix\final-v2\176-185\order176..185-result.html`（`order184-result.html` 为代表）
- V2 201–207：`D:\ilife\.scratch\t351-fix\final-v2\201-207\order201..207-result.html`
- V1（已判丑，仅对照）：`D:\ilife\.scratch\t351-fix\final\`（47 份）

## 5. 负责人已定口径（不要再问，除非负责人改口）

1. 底部一律「复制数据／复制日志」双按钮：冻结 `id`、中文单语、禁 `text/json/csv` 英文菜单。
2. 视觉沿用 201–207 那套共享区块卡片语言（负责人已接受），不自造样式、不新增公共层件。
3. **不印「休息」列**（库无该字段，老页在同一库上也只印 `-`）。
4. 部位色块、混排单元格本轮**降级纯文本**；要复原须另开公共层票（base-paint）。
5. 复制数据载荷：不照抄老页 `scene.snapshot`；把计划数据投影成现有 `SerializableEnvelope` 的 `list`（items 逐周、total 会话数）；日志用 `CopyLogFields`。
6. 老标杆（**只读、别整读**）：`D:/2Study/StudyNotes/.db/calorie_html/看完整计划_20260914_141334.html`（2163 行）。
7. 数据真源：生产库 `D:\2Study\StudyNotes\.db\calorie_data.db`；`workout_plans.movements` JSON 五键 `name/part/type/note/sets`，sets 元素 `{reps, weight, unit}`；实测 264 动作全有明细。

## 6. 未完成 / 下一手（按优先级）

1. **磁盘（先做，硬阻塞）**：C 盘只剩约 3.3GB（D 盘 383GB 富余）；`C:\Users\辰辰洋洋\AppData\Local\Temp` 占 47GB（DiagOutputDir 4.9GB、v21a 657MB、ps7install、t81-seed-* 等）。本会话已授权清理（只删 48 小时前、逐项 try/catch、排除 `dsh-rt-*`/`rt-clean`/`t81-seed-*`/`.dsh*` 等运行时或他席目录），**清理中途被叫停，未验证释放结果**。接手先 `Get-PSDrive C,D | Select Name,Free`。
2. **收 176–185 V2 的两个必改项**：会话 `f96dae62-6e5d-40bf-bd2a-0eb2069623d1`（修复兵）在停会话前被派出，**状态未验证**——先 `git status --short -- packages/skill-calorie/src/render/workoutPlanDocs.ts` 看它是否留下未提交改动（若有，补齐或回退后重跑）。
3. **全量重跑 47 份**：176–185（10）＋186–194（24，旧产物重跑）＋195（3）＋201–207（7）。脚本：`.scratch/t351-fix/176-185/run-176-185.mjs`、`.scratch/t351-fix/run-201-207.mjs`、`.scratch/t351-fix/195-verify/run-195.mjs`；旧收官口径见 `.scratch/t351-fix/final/detail.json`。机检四条：`<!doctype html>`、`id="ilife-copy-data"`/`-log` 各一、页内无 `op=`/`# 成功`/英文三格式菜单、结果页六列表头。
4. **对抗审查（截图）＋送负责人肉眼终审**（187-receipt、186-process 是 V1 最差的两份，重跑后重点看）。
5. **待负责人签字**：#349 两句、#350 两句（见 §3）。
6. **#157 地图回填**：只在关票时同步 Decisions so far / 迷雾毕业 / Out of scope；进度节按契约。
7. **另票**：`workout/planStore.ts` 369 行超线（既有 366 就超线）；`render/sportPortDocs.ts` 533、`render/trendDocs.ts` 816 超线；`workout/write.ts` 的 `asMovement` 不认 `note`/`sets` 元素 → 写命令回填丢备注。

## 7. 风险与坑

- **磁盘**：见 §6.1；ENOSPC 会导致提交/测试/subagent 失败（本会话已有数个 subagent 疑似因此失败）。
- **未提交改动**：`render/sportPortDocs.ts`（眉标中文，＋3 行）**干净可提**；`render/trendDocs.ts`（眉标中文 ＋3 行）**与他票「组合分析」改动同文件混存，无法选择性提交** → 等他席提交后再提，或与他席协调；证据 `docs/skills/skill-calorie/t351-fix-201-207-evidence.md` 第六节（＋59/61 行）同属未提交。
- **他席并发**：工作区长期 20–30 个 M/?? 文件（`weight/compare.ts`、`base-combos/*`、`diet/*`、`exercise/*`、`analysis/*` 等）。只 add 自己文件；提交前 `git diff --cached --name-only` 自查；曾发生他席文件被误暂存（`bf4604f` 轮次已 `reset --soft` 纠正）。
- **锁**：`run-locked.mjs` 常需等 10–120 秒（ticket 349/370/380 等并发持锁）；长命令必须持锁。
- **测试口径**：根 `test/` 为准；`test/scene05-read.test.mjs`、`test/scene05-write-create.test.mjs`、`test/scene05-write-mutate.test.mjs`。既有红与本票无关：`render-copy-90`、`copy-component-179`（#239 在册）、`exercise-port-111` 路由漂移。
- **本机投递工具不可用**：`dsh_im_return_file` 对本工作区文件连续报 “file is unavailable”（换过 `.scratch/`、`docs/`、ASCII 文件名均如此），只能给负责人文件路径，不要反复重试。
- **别追老页 JS 交互**：新页零内联脚本，页签用原生 `<details>` 替代；老页「休息」列本来就恒空。

## 8. 建议技能

- `wayfinder`（本图工作方式；`/wayfinder #157`）
- `code-review`（若要按 standards＋spec 复核本轮 diff）
- §2 的纪律文档同等重要

## 9. 新会话恢复动作（照做）

1. `Get-PSDrive C,D | Select Name,Free` 确认磁盘；若 C < 5GB，先按 §6.1 边界清 TEMP（或与负责人确认）。
2. `gh issue view 157 --repo FeatherHunter/ilife --json body,comments` 读地图。
3. 读 §2 的四个纪律文档（尤其中文用词与 issue 正文格式）。
4. `git log --oneline -12` 与 `git status --short` 摸清提交与在飞改动；核对 §7 的「未提交改动」两条。
5. 按 §6 顺序推进：先收 V2 必改项 → 全量重跑 47 份 → 对抗审查截图 → 请负责人肉眼终审 → 拿签字收 #349/#350 → 回填 #157。

## 10. 本会话（已停）留下的资产速查

- 提交（均已推送 origin/master）：`33ff950`#346、`de748c0`#347、`a97b59c`#348、`0621392`+`7441e29`#350 裁定、`c66af49`#349 关票包＋#351 预演、`c9c836f` 201–207 双按钮 id、`bf4604f` 176–185 首次穿版式、`5255b01` 195 空态页＋接线、`abb7c5c` 176–185 V2 重做。
- 证据文档：`docs/skills/skill-calorie/` 下 `t168-scene05-reconcile.md`、`t349-close-ready-20260914.md`、`t350-external-five-verdict.md`、`t351-dryrun-20260914.md`、`t351-visual-fix-plan-20260914.md`、`t351-redesign-184-spec.md`、`t351-redesign-184-evidence.md`、`t351-fix-201-207-evidence.md`、`t351-handoff-20260914.md`（本文件）。
- 草稿与证据：`.scratch/t349*`、`.scratch/t350*`、`.scratch/t351*`、`.scratch/t351-fix*`、`.scratch/t351-visual/`、`.scratch/t351-fix-review-v2/review-verdict.md`。
