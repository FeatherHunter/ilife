# #609 重冻第二批证据 —— #396 缺口口径 与 #566 结果页标题的冻结值（同成因 `7831393`）

实施席写集（票面「交付」栏四件 ＋ 票面点名的证据件）：`packages/skill-calorie/test/396-gap-caliber.test.mjs`、
`packages/skill-calorie/test/566-result-title.test.mjs`、`packages/skill-calorie/test/goal-result-254.test.mjs`（加一条）、
`docs/research/t81-seed.mjs`（体重段互链一句）、本件。
红线照票面：**不动产品代码（除变异自证的临时改坏—逐文件还原）、不代改他席件**（§五）。

**承接**：上游 `#591`（门禁与重冻那一班）的上游复核 `#590` 已判「甲」＝漂移来自**已提交**的共享取数变更，
第一批只重冻了 `goal-result-254`（`t589-重冻-证据.md`），跨件只记账；本票把账上那两件（`#396`／`#566`）收完，
并按 `#590` 复核点名的两条补件（`#254` 形态断言 ＋ `t81-seed.mjs` 互链）。

## 一、判定：甲（同成因 `7831393`，按当刻口径重冻）

| 证据 | 读数 |
|---|---|
| 成因提交 | `783139357bea38c8b40ef10446e27ff61b6ba3d6`（#518 W2）。逐字：「#463 档案 TDEE：`series.ts::loadProfileTdee` 改读最近一次称重，无记录回退 70.0（逐字不变）」。代码面（本席读码复核）：`packages/skill-calorie/src/analysis/series.ts:182` 由 `calcTdee(70.0, …)` 改成 `ORDER BY date DESC, time DESC, id DESC LIMIT 1` 取最近称重。 |
| 种子库体重入参 | `weight_log` **367 行**，首行 `2025-09-07 75`、末行 `2026-09-08 **70.6**`（本席实读，`.scratch/609/read-now.mjs`）⇒ 当刻 TDEE 的体重入参＝70.6kg。 |
| Δ 的推导（`#396`） | TDEE ＝ `Math.round((10×70.6 ＋ 6.25×175 − 5×30 ＋ 5) × 1.55)` ＝ `Math.round(1654.75×1.55)` ＝ **2565**；改前 70.0kg 时 2548.75×1.55→**2556**；**Δ＝＋9 卡/日**。缺口 ＝ TDEE ＋ 当日运动 − 摄入 ＝ 2565 ＋ 320 − 1189 ＝ **1696**（改前 2556＋320−1189＝**1687**）。 |
| 基线红（改前，判据原样） | `runId=510b10df-6df4-40e6-a762-f019016d6df6`：`ℹ tests 12 ／ pass 10 ／ fail 2` ——红的两条正是 `#396 算法未动：缺口值仍是 TDEE 系（1687）`（`1696 !== 1687`）与 `#566 ③`（`weeklyDeficit 10886 vs 10841`、`avgDeficit 1555 vs 1549`），**与 `#590` §五记账清单逐条对上**。 |
| 结论 | 漂移唯一来源是这条已提交的共享取数变更（体重由常量 70.0 换成种子库最近称重 70.6）；回退分支（清空 `weight_log`）数值一字未变 ⇒ **不是回归，判甲**，按当刻口径重冻。 |

## 二、重冻对照表（改前 → 当刻 ＋ 逐条出处）

| 用例·字段 | 原冻结 | 当刻口径（重冻后） | 出处 |
|---|---|---|---|
| `396` `deficitToday` | 1687 | **1696** | 成因提交 `7831393`；算式＝`calcTdee`（`src/analysis/utils.ts:59-63`，Mifflin-St Jeor × `TDEE_ACTIVITY_FACTORS.moderate=1.55`）＋当日运动 320 − 摄入 1189 |
| `396` 上屏「消耗」 | 2876 | **2885** | 同上（2565 ＋ 320） |
| `396` `intakeCal` | 1189 | **1189（未变）** | 当日饮食合计（种子库 09-07 四条） |
| `566` `goal-7d` `weeklyDeficit` | 10841 | **10886** | 成因提交 `7831393`（TDEE 读 70.6kg） |
| `566` `goal-7d` `avgDeficit` | 1549 | **1555** | 同上 |
| `566` `goal-30d` `weeklyDeficit` | 20276 | **20357** | 同上 |
| `566` `goal-30d` `predictedLossKg` | 2.63 | **2.64** | 同上（由 `weeklyDeficit` 派生） |
| `566` `goal-30d` `avgDeficit` | 676 | **679** | 同上 |
| `566` `CONTROL.daysLeft` | 107（**机器钟读数，冻不住**） | **移出冻结表**（改判形态） | 日期推导：`goal_deadline='2026-12-31'`（`docs/research/t81-seed.mjs:52`）− **机器今天**；「今天」出处＝`src/goal/goalExtraPlate.ts:42` 逐字 `today ?? new Date().toISOString().slice(0, 10)`（本命令**不读** `CALORIE_TODAY`）。当刻机器今天＝`2026-09-16` ⇒ 106（形态断言现算现比，不写死） |
| `#254` `⑥`（新增） | — | **无参 daysLeft ＝ 2026-12-31 − 机器今天；钉 `today=SEED_TODAY` 那支恒 115** | 算式出处同上 `:42`／`:49`；同族先例 `goal-result-290.test.mjs:204` |

出处**写在值处**（票面判据「预期值处写出处」）：`396-gap-caliber.test.mjs` 件头「#396 值的重冻」段 ＋
测试体内算式注释；`566-result-title.test.mjs` 件头「#609 重冻第二批」段 ＋ `CASES` 上方逐条出处 ＋
`CONTROL` 定义处 ＋ ③ 条内形态断言注释；`goal-result-254.test.mjs` 件头 ⑥ 条 ＋ `SEED_DEADLINE` 定义处 ＋ 新测试体内。

### 为什么 `566` 的 `daysLeft` 不「把 106 冻下来」

无参时它一天掉 1（`#590` 调查席已下这句判断，`goal-result-290.test.mjs:204`／`goal-weight-deadline-548.test.mjs:124` 同口径）：
把当刻的 106 冻进表里，明天这份件必再红一次——那不是「重冻」，是换一天的红。故照 `#254`／`#590` 的手法
**把日期钉住**：`CASES` 里 `goal-7d`／`goal-30d`／`vs-30d` 本身不吃墙钟（`CALORIE_TODAY=SEED_TODAY` 已钉），
只有`CONTROL`（`goal-expiring`）那一支吃，于是它不进冻结表、改由形态断言咬：`daysLeft ＝ 截止日 − 机器今天`。
**判据形态因此更严不更松**：原先是「等于 107」这种一次性读数（今天本来就没人比它），现在是**等式现算现比**。

## 三、机器证据（每条从 `.scratch/locks/gate-runs.log` 抄）

| 步 | GATE-RUN | 摘要行 | 退出码 |
|---|---|---|---|
| 当刻口径读数（本席自跑，种子库实读 ＋ 五键 metrics） | `runId=1c5a3fd6-1604-4ba5-85f7-fe009a8f6e17 cmd="node .scratch/609/read-now.mjs"` | 种子：`today=2026-09-07`、`weight_log 367 行`、末行 `2026-09-08 70.6`；`home-today deficitToday=1696`／`intakeCal=1189`；`goal-7d 10886／1555`；`goal-30d 20357／679／2.64`；`vs-30d` 与 `expiring`（无参 106） | 0 |
| 基线红（重冻前，票面判据原样） | `runId=510b10df-6df4-40e6-a762-f019016d6df6 cmd="node --test …396-gap-caliber …566-result-title …goal-result-254"` | `ℹ tests 12 ／ pass 10 ／ fail 2`（差异见 §一） | 1 |
| 重冻后（第一次跑，⑤ 对照件仍整表判等 ⇒ 红） | `runId=79ff98b1-4507-403c-aa9a-7e5793dd118a`（同命令） | 第一次跑（`ℹ tests 13 ／ pass 12 ／ fail 1`）里那条红是 `对照件 data.metrics 与当刻口径不一致 … + daysLeft: 106`——就是它把「整表判等」逼成「摘出 `daysLeft` ＋ 形态断言」 | 1 |
| 验收（重冻后全绿） | `runId=0b7eef0e-3308-432f-a0d0-8536a1e333cf`（同命令） | `ℹ tests 13 ／ pass 13 ／ fail 0`；并打 `T566-MUT 字串级：日期加回 H1 红=3、窗口抠出页头 红=3、窗口全挖掉 红=3；还原绿=3`；`✔ #254 ⑥ 无参随机器钟：daysLeft ＝ 截止日 − 机器今天` | 0 |
| 变异（改坏产品源码 ＋ 重建） | `runId=47651882-1f44-48cd-9b4b-011cbd5a53e7 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force"` | 编译 exit 0；`dist/analysis/series.js` 回读＝`ORDER BY date ASC, time ASC, id ASC`（变异已进产物） | 0 |
| 变异（致红） | `runId=45de7866-cf0b-4153-b37f-22c1e5844424`（同命令） | `ℹ tests 13 ／ pass 10 ／ fail 3`：`#396`（缺口 `1764 !== 1696`）、`#566 ③`（`weeklyDeficit 11226 vs 10886`、`avgDeficit 1604 vs 1555`）、`#254`（`10886 vs 11226`） | 1 |
| 逐文件还原 ＋ 重建 | `runId=01585a9e-539f-4289-a329-1dbe43980bac`（同编译命令） | 编译 exit 0；`dist/analysis/series.js` 回读＝`ORDER BY date DESC, time DESC, id DESC`（落盘 SHA256 `D1122A6F24C2089AD2926C6A034760A72C78A15551756693FAAC90A104A8078E`，`Get-FileHash -Algorithm SHA256` 可复算）；`src/analysis/series.ts` SHA256 `24C716BF…C356F7` 与变异前**逐字符相同** | 0 |
| 逐文件还原（致绿） | `runId=72781b3b-fc3c-49e7-a441-899b1446b173`（同命令，重跑） | `ℹ tests 13 ／ pass 13 ／ fail 0` | 0 |
| 语法／模块面复核 | 本席直跑 `node --check docs/research/t81-seed.mjs` | exit 0；`visible-text-probe.mjs` 可 import | 0 |

## 四、自证（变异红／还原绿两行 ＋ 还原字节一致）

- **红**（`runId=45de7866…`）：把**成因函数本身**改坏——`src/analysis/series.ts:182` 的
  `ORDER BY date DESC, time DESC, id DESC` 改成 `ASC`（体重入参由最近称重 70.6kg 变成最早一行 75.0kg），
  经 `--force` 重建后 ⇒ `exit=1`、`ℹ tests 13 ／ pass 10 ／ fail 3`：`396`「缺口值变了」`1764 !== 1696`、
  `566 ③` `+ 11226 / - 10886`、`+ 1604 / - 1555`，`254` 同值差异。**三件重冻件同时红** ⇒ 这次的「冻结值」不是纸面数。
- **还原**（`runId=01585a9e…` ＋ `72781b3b…`）：`git restore -- packages/skill-calorie/src/analysis/series.ts`
  后 `RESTORE-SRC-EQUAL True`（SHA256 `24C716BF34A44194CA42BAE9BF6DAB0364B36DF1BBFB6C95C4978827A1C356F7`
  与变异前逐字符相同，`git diff --numstat` 行数 0），重建后 `dist` 回读回到 `DESC`，
  再跑 ⇒ `exit=0`、`ℹ tests 13 ／ pass 13 ／ fail 0`。
- **判据不恒真**（结构面）：`566 ③` 末尾保留「拿 7 天窗冻结表核对 30 天窗读数 ⇒ 必须抛」；
  `254` 保留「片段形状必红／姊妹键必过」两发；`#254 ⑥` 自带反向对照（钉 `today` 那支恒 115，
  机器今天 ≠ 09-07 时两支读数必须不等）。

## 五、他件零改动 ＋ 遗留出口

改动面**只有本席声明的四件**（`git status --short` 逐件：`docs/research/t81-seed.mjs`、
`packages/skill-calorie/test/{396-gap-caliber,566-result-title,goal-result-254}.test.mjs`）；
`src/analysis/series.ts` 的变异改动**已逐字节还原**（§四），未进提交。工作树里他席在途件
（`docs/skills/skill-calorie/scene06-验收墙/**` 等）本席一字未碰。

遗留出口（票面「若发现第三件同成因红，只记账列清单，不代改」）：

| 件 | 当刻状态 | 归属 |
|---|---|---|
| `packages/skill-calorie/test/goal-h1-date-566.test.mjs` | 本席**未跑**（不在票面三件内），无法判定 | 如需核对，由归属席按 `--test` 单跑 |
| `packages/skill-calorie/test/analysis-shape-567.test.mjs`（`t589` 记 6 红）、`analysis-t5`（3 红）、`softdelete-120`（1 红） | 与 TDEE 数值漂移不同类（`t589` §五已逐条定性） | 他席在途，非本成因 |
| 包内告警线门 `check-warning-line.mjs` | 本席未跑（不写 `src/**`，不触台账） | 与 `#609` 无关 |

## 六、机器钟形态读数的附带发现（只记，不改）

本票跑测期间（`2026-09-16`）真 CLI 产物的**文件名**带着墙钟时间戳
（`…/calorie_html/目标分析_20260916_135235.html`，出处见 `read-now.mjs` 的 `data.output`），
与「`CALORIE_TODAY` 只钉取数窗口、不钉产物文件名」这条口径一致；`#254 ⑥` 判的 `daysLeft` 走的正是这条机器钟。
两处都不属本票写集，仅记账。

## 七、没做完什么 ＋ 下一手

1. 票面三件＋互链一句**已全部做完并全绿**（§三最后一行）。`#591` 的其余条目（门禁 83 清零、场景02 墙重出、
   `#511` 三条与点修两条）**不在 `#609` 写集**，归 `#591` 原席。
2. 本件 `566` 的 `daysLeft` 已改为**形态断言**：若将来有人把 `goal-extiring` 的「今天」改成读 `CALORIE_TODAY`，
   这条会红——那是口径变更，不该由本件静默放过。
3. 若发现第三件同成因红：只在此表记账，不代改（当前清单见 §五，无新增同成因件）。
