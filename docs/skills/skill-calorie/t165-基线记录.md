# t165 冻结基线记录（新架构重构的 S0）

> 票 `#165`（地图 `#154` 场景 03 体重）——本次重构的第一份账面：**冻结时刻的仓库状态与门禁基线**。
> 用户 2026-09-13 指令：全部 commit ＋ push 之后进入冻结；本图为「新架构重构」的试点。

## 一、冻结基线

| 项 | 值 |
|---|---|
| 冻结提交（HEAD ＝ origin/master） | `c121d34` |
| 工作区改动数 | **0**（`git status --porcelain` 空） |
| 基线门禁 | **全绿**：`pnpm test` → `# tests 1474`／`# pass 1474`／`# fail 0`；`duration_ms 32892`；exit 0 |
| 运行留痕 | `runId=7ee12ba5-76c8-46c4-ba64-0e94a1b2c999`，`waitedMs=0`，`exit=0`（落 `.scratch/t165/baseline-test.log`） |
| 已知基线红名单 | **空**（此前记录的 `fetch-t6:97` 环境时钟红项本次未复现） |

**GATE-RUN runId=7ee12ba5-76c8-46c4-ba64-0e94a1b2c999 cmd="pnpm test" ticket=165**

## 二、冻结前的在飞工作（已入仓保命，内容未改动）

| 提交 | 内容 |
|---|---|
| `5be4e45` | #250 路由窗口相对化（245 条）＋数据同批回抄＋错项纠正（**冻结前只在本机、未推**，已随之推上） |
| `2a86f2e` | 冻结前在飞工作快照（代码与测试）：目标线 `src/goal/`（precheck／set）＋ `src/shared/receiptParts.ts` ＋ `test/goal-wizard-251.test.mjs` ＋ `keys.ts`／`routing.ts`／`scene-06-goal.ts`／`combos.yaml`／`present.ts`／`build-help.mjs`／`SKILL.md`；`scene-02-diet.ts`；#238 的 `profile/setup.ts`／`update.ts`／`base-render/src/style.ts`；随之同步的 10 个计数测试与 `test/calorie-sot.snapshot.json` |
| `c121d34` | 冻结前证据页快照：`t164-*`（6）／`t168-*`（6）／`t169-*`／`t170-*`（3）／`t251-*`（3）／`t255-*`／`skill-schedule/t197-*` |

## 三、过程中发现的一处环境问题（要广播）

`.scratch/locks/gate.lock` 里躺着一把**锁龄 68 分钟的死锁**（某条 session 被停时正持锁，未走到 `finally`）。
后果：任何按协议抢锁的动作会**无限等待**。本次处置：按协议「owner 已死即可抢回」清除，并把抢锁循环改成**有界 ＋ 锁龄 > 1 分钟即抢回**。
**广播要求**：停机（或关窗口）前先确认 `.scratch/locks/gate.lock` 不存在；恢复开工前若见到它且锁龄过大，按死锁处理。

## 四、方法（可复跑）

```pwsh
# 基线门禁（锁内）
node tooling/run-locked.mjs --ticket 165 -- pnpm test *> .scratch/t165/baseline-test.log
# 只读摘要
Select-String -Path .scratch/t165/baseline-test.log -Pattern '^ℹ (tests|pass|fail)'
```
