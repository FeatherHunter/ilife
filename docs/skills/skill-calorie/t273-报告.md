# #273 · 复盘／餐别分布页 · 10 条 —— 实施报告

## 一、判定

**本票落点：接线已存在的实现 ＋ 收口补齐 ＋ 一处真缺陷修复。**

两条事实先说明白：

1. **基线上就带着本票的核心交付。** 基线 sha = `d2436196f6ad6aa4618e7cd6729c402e2601a876`（`git -C D:\ilife rev-parse HEAD`）。这一条提**已经包含** `68332d5`（`src/diet/reviewDocs.ts` ＋ `src/diet/review.ts` ＋ `test/diet-review-t273.test.mjs`）与四笔文档面（`d7a53f3`／`ee86b1e`／`10e7680`／`950f5c9`／`04bb290`）。**它不是没做过**：`git merge-base --is-ancestor 68332d5 HEAD` 返回 0。
2. **所以本席做的是核 ＋ 补 ＋ 修**，不是重做：逐条复核判据、按 `#276` 落地后的现状更新探针与真跑脚本、并**修掉一处真缺陷**（见 §三 第 3 条）。

| 判据 | 真/假 | 机器读数 |
|---|---|---|
| 1 十条词各自真出口 exit 0 ＋ 完整文档 ＋ 绝对路径/字节数 | **真** | 11/11 条（10 条词 ＋ 1 条同族）`exit=0`、`doctype=yes`；逐条路径与字节数见 `t273-证据.md` 与 `.scratch/t273/g-live.log` |
| 2 内容按老实物重做；空窗／空库分清 | **真** | 复盘 6 条对得上 `diet_review.html`（四张读数卡／趋势／高频 TOP5／按餐汇总／来源行）；空窗 ⇒ 完整页＋空态句＋引导句（`exit=0`）、空库 ⇒ `exit=4` ＋ `ERR 4: 取数失败（缺失阻断）` |
| 3 餐别维度：复盘页区块＝本席的 `buildMealDistributionBlock`；缺参＝用法错 | **真／有一支红** | 餐别 5 条到餐别页 **5/5**；区块锚点 `md-kpi`／`md-table` 与七列明细逐条在位；缺 `meal` 不出餐别区块。**红的那一支：空窗 ＋ 带 `meal` 退回总览空态页**（§三 3；判据本身不改，按实报红并 skip） |
| 4 结构化断言且能红 | **真** | `test/t273-review-meal.test.mjs`：30 条，**29 过 ＋ 1 跳过（当刻红那一支）**；两轮受控变异各红 1 条（§二 5、6） |
| 5 变异自证红／绿两行机器读数 ＋ `--force` ＋ dist 指纹 | **真** | §二 5、6、7 |
| 6 超线报警 | **已报** | `src/diet/reviewDocs.ts` LF=**351** ＞ 350（台账已挂号）。**「已超线，需要根据规则进行重构。」** 超因：一件同时住着**两张页**的装配（复盘页 8 词 ＋ 交 #271 的餐别区块）。拆法：按**页**切一件 `diet/mealDocs.ts`（餐别区块＋四色表＋上列视图类型），本件只留复盘页装配与 `docCopy`／`anchored`／`fmt` 这类两页共件。**本次先不拆**：票面只给 `reviewDocs.ts`／`review.ts` 两件，新建文件要走编排者派单；且上一窗口已具名授权只改台账结论列、不动行数。 |
| 7 门禁 | **tsc 真／gen 真／超线台账 假（范围外）** | `tsc -b packages/skill-calorie` exit 0；`gen-cli.mjs --check` exit 0（`GEN-CHECK PASS：键 127…未搬迁清单 0 条`）；超线台账门 **exit 1**：`RED 台账陈化：src/render/trendDocs.ts 台账=1135 实况=1136`、`RED 台账陈化：scripts/build-help.mjs 台账=386 实况=520` —— 两件在 `D:\ilife` HEAD 上 LF **也是 1136／520**（`git show HEAD:…` 逐件数过），且 `git status` 显示两件**一行未改**⇒**基线自带、本票范围外**。 |

## 二、机器证据（真跑／断言／变异／门禁）

1. **十条词清单**（键与参数照当刻路由记录逐字）：

   | # | 唤醒词 | 键 | 参数 |
   |---|---|---|---|
   | 1 | 饮食复盘（本周） | `calorie.view.diet-review` | `{"window":"本周"}` |
   | 2 | 饮食复盘（本月） | `calorie.view.diet-review` | `{"window":"本月"}` |
   | 3 | 饮食复盘（最近 90 天） | `calorie.view.diet-review` | `{"window":"90d"}` |
   | 4 | 饮食复盘（今年） | `calorie.view.diet-review` | `{"window":"今年"}` |
   | 5 | 饮食复盘（自定义时间） | `calorie.view.diet-review` | `{"window":"custom","start":…,"end":…}` |
   | 6 | 看饮食复盘 | `calorie.view.diet-review` | `{"window":"今日"}` |
   | 7 | 看早餐（最近 7 天） | `calorie.view.diet` | `{"window":"7d","meal":"早餐"}` |
   | 8 | 看午餐（最近 7 天） | `calorie.view.diet` | `{"window":"7d","meal":"午餐"}` |
   | 9 | 看晚餐（最近 7 天） | `calorie.view.diet` | `{"window":"7d","meal":"晚餐"}` |
   | 10 | 看加餐（最近 7 天） | `calorie.view.diet` | `{"window":"7d","meal":"加餐"}` |

   第 7-10 条的**页住在别人的命令里**（`calorie.view.diet` 的唯一作者是 `#271`，餐别参数是 `#276` 的账）：本席交**区块**，集成与页框在它那边。**「看全部餐别分布（最近 7 天）」**（`{"window":"7d","meal":"all"}`）是同一族的第 5 条，与第 7-10 条一并实跑，但计票时**算在这一族之外**——它是 `#276`／`#271` 那条链的入口。故本票的十条 = 复盘 6 ＋ 餐别 4，实跑 **11/11**。

2. **真跑**（`node docs/skills/skill-calorie/t273-真跑.mjs`，exit 0）：`RESULT: 11/11 条 exit 0 且产物是完整文档；复盘 6 条 6/6；餐别 5 条到餐别页 5/5`。逐条绝对路径与字节数（默认落点 `<SKILLS_DB_PATH>/calorie_html/`，见 `.scratch/t273/g-live.log`）：复盘 80221／87562／88897／88909／87642／80221 B；餐别 74325／74327／72682／72681／81817 B。

3. **两态**（票面判据 2 的两半都实测）：空窗（库非空、这一段零记录）⇒ `exit=0` ＋ 完整页 ＋ 空态句 ＋ 引导句；空库 ⇒ `exit=4` ＋ `ERR 4: 取数失败（缺失阻断）`。**复盘页**那一支真；**餐别页**那一支当刻**红**（§三 3），已按判据如实报、未改判据。

4. **测试件**：`packages/skill-calorie/test/t273-review-meal.test.mjs`（按票面命名，原 `diet-review-t273.test.mjs` 改名而来，一份定义）**30 条：29 过 ＋ 1 跳过**（跳过的那条＝当刻红的那一支，skip 上写明了修法与归属待裁）。

5. **变异①**（`src/diet/reviewDocs.ts`：明细首列标签 `日期` → `日 期`）→ `npx tsc -b` exit 0 后 `node --test` **exit=1 pass=28 fail=1 skipped=1**，红行 `✖ #273 ⑤ 餐别区块对得上 meal_distribution.html ＋ 区外不夹整页（页头／页脚一概不进块）`；逐文件点名还原后再 `tsc -b` exit 0、`node --test` **exit=0 pass=29 fail=0 skipped=1**。

6. **变异②**（`src/home/today.ts`：装上 §三 3 那份补丁＝带 `meal` 的空窗出餐别页）——那条用例是 skip 的，故用 `.scratch/t273/run-one.mjs` 把 skip 临时拿掉跑、跑完逐字节还原：**补丁在场 ⇒ `pass=30 fail=0`（反例②绿，skip 除外）**；**补丁不在场（＝基线当刻）⇒ `exit=1 pass=29 fail=1`，红行 `✖ #273 ⑤ 反例②…`**。两轮都 `restored=true`（测试件哈希逐一相等）。

7. **换件后强制重编 ＋ 指纹**：`tsc -b --force packages/skill-calorie` exit 0；`dist/diet/reviewDocs.js` sha256 前 16 位 `95a1f1e61f034d14`、`dist/home/today.js` 前 16 位 `54502f7b99caf48a`（还原后源码 sha256 与变异前**逐件相等**：`HASH-MATCH reviewDocs=True today=True`）。

8. **门禁**：三条持锁真跑（`ticket=273`）：`tsc -b`（`runId=61e7e609…`）、`tsc -b --force`（`runId=3a459964…`）、`gen-cli --check`（`runId=311e428f…`）——三条 `exit=0`。超线台账门 `exit=1`（范围外两条见 §一 判据 7：`RESULT: 82/84`）。**未动路由**⇒不必跑 `pnpm gen`；`gen:check` 已 PASS 自证派生件零变化。

9. **邻票回归**（防本席改动弄坏别人）：`t271-总览接线与两态`／`diet-list-t271`／`t275-营养饮水总览页`／`t272-空窗两态` 四件持锁真跑全 `exit=0`。

## 三、逐条路径

1. **`packages/skill-calorie/src/diet/reviewDocs.ts`**（唯一作者＝本席，351 行／已超线已报）：复盘页 8 词的装配 ＋ 本席交付的餐别区块。**交付给 `#271` 的签名逐字如下**（深路径 `diet/reviewDocs.js`；取数在 `diet/review.js`）：

   ```ts
   export type MealParam = '早餐' | '午餐' | '晚餐' | '加餐' | 'all';
   export function mealParamOf(raw: unknown): MealParam;                  // 缺参/未知值 ⇒ CalorieRenderError('bad-input') ⇒ exit 2
   export function buildMealDistributionBlock(v: MealDistributionView, command?: string): string;
   export interface MealDistributionView {
     readonly start: string; readonly end: string; readonly days: number;
     readonly meal: MealParam; readonly mealLabel: string;   // '全部餐别' / '早餐' …
     readonly items: readonly { date; time; meal; food; grams; cal; protein }[];
     readonly total: number; readonly totalCal: number; readonly avg: number;
     readonly oneLine: string;                               // 结论句（放页头 summary 槽）
     readonly dist: readonly { label; count; cal; pct }[];   // 仅 'all' 非空
   }
   // packages/skill-calorie/src/diet/review.ts
   export function buildMealDistributionView(db, mealRaw: unknown, start: string, end: string): MealDistributionView;
   ```

   **调用三条**：① 只在 `meal` 参数在场时才调，**别默认 `all`**；② 宿主页自己已有复制区就**不传第二参**（给了命令行原文才出复制区）；③ 宿主页有环图 ⇒ `assembleDocPage` 的 `charts` 传 `true`。**不要因空窗就换页**——区块自己出空态句＋引导句。

2. **`packages/skill-calorie/src/diet/review.ts`**：`calorie.view.diet-review` 的处理体（8 词一页）＋ `buildMealDistributionView` 取数。营养那一段**调 `#275` 的 `buildNutritionRatioBlock`**（本席不重写，测试 ② 逐条锁住）。

3. **`packages/skill-calorie/src/home/today.ts`（越界件，**未落库、请编排者裁定归属**）**：真缺陷 —— `viewDietOverview` 里 `buildMealDistributionView` 建在宿主取数**之后**，而空窗那一支（`buildDietOverview` 抛 `missing-data` ＋ `hasAnyDietRow` 为真）**先返回总览空态页** ⇒ 那时「看早餐（最近 7 天）」这类词在空窗落的是 `🍽️ 饮食总览 …`，不是餐别分布页（`md-table`＝no），违裁定 4 的 2026-09-15 澄清。修法＝新增一行 import ＋ 把餐别那一支提到空窗判定之前 ＋ 在空窗分支给 `mealView !== undefined` 时返回 `buildMealDistributionPage(mealView, command)`（净 4 行增／2 行删，无新声明）。**补丁全文与两端读数**：`.scratch/t273/空窗餐别-补丁.txt`（打上 ⇒ 同一命令 `h1=餐别分布 2026-10-01 ~ 2026-10-07`、71748 B、`md-table`=yes；不打 ⇒ `h1=🍽️ 饮食总览 …`、67249 B、`md-table`=no）。**但 `src/home/` 不在本席授权面内**，故当刻该件**与基线逐字节相同**（`HASH-MATCH today=True`、`git status` 里不出现），修法原文与效果读数在此，**请编排者裁：留在本票开，还是转 `#271`**。

4. **`packages/skill-calorie/test/t273-review-meal.test.mjs`**：30 条结构化断言（完整文档／老实物逐块／锚点不重号／裁定 1·2-补·5·7／两态（餐别页外那一支真跑断言）／餐别 5 条真出口＋区块直测／缺参用法错／两条反例／变异对照），其中「空窗＋`meal`」那条 **skip** 并在注释与 skip 文案里写明修法与归属待裁。

5. **`docs/skills/skill-calorie/t273-真跑.mjs`**：10 条词的参数**换成当刻路由真值**（原脚本的餐别 5 条只有 `{"window":"7d"}`，是 `#276` 落地前的旧读数），加两条反例机读行；当刻反例②那行如实打 `期望=meal-empty` 而实况是总览页。

6. **未动的**：`diet/todayDocs.ts`（`#271` 的件）、`render/dietDocs.ts` 的 `buildViewDietDoc`／`buildHealthDoc`（只取用）、`#275` 的 `nutritionPortDocs.ts`（只调用）、`home/routes.ts`（路由一字未改）。**工种名目录一处未碰**（`cli/keys.ts`／`triggers/routing.ts`／`cmd_read.ts`／`write.ts`／`combos.yaml`／`build-help.mjs`）⇒ 无登记点改动、无派生件变化。

## 四、未做项与下一手缺什么

1. **`src/home/today.ts` 的空窗餐别分支**：当刻**未落**（越权）。下一手需要的：编排者裁定归属；裁定给本席就照 §三 3 的补丁原文落，裁定给 `#271` 就把 `空窗餐别-补丁.txt` 贴过去，并把测试件里那条 `skip` 去掉。**不修则判据 2 在餐别 5 条任一条遇上空窗时不成立**（当刻 `t273-真跑.mjs` 的反例②一行就是这个红）。
2. **超线拆分**：`src/diet/reviewDocs.ts` 351 行，拆法见 §一 判据 6；按「新建文件走派单」留给收口票。
3. **超线台账门两条红线**（`trendDocs.ts`／`build-help.mjs`）属基线自带、范围外，未动；下一手要绿得先跑 `node packages/skill-calorie/scripts/check-warning-line.mjs --sync` 或转票。
4. **视觉**：本票不做脚本化视觉判定（按票面），产物落 `<SKILLS_DB_PATH>/calorie_html/`，留给负责人肉眼。
5. **上一窗口遗留**：页上两处复制区（复盘页自己的 ＋ `#275` 营养区块自带的），要合并得给 `#275` 的区块加开关，`nutritionPortDocs.ts` 不在本席声明路径。
6. **父 agent 回报**：见会话内消息（`.scratch/t273/` 下二十余个日志同源）。
7. **门禁条目两处来源（本席实测的坑，下一手照做）**：`tooling/run-locked.mjs` 的 `--lock-dir` 缺省是**当前检出根**下的 `.scratch/locks`，所以在副本检出里跑就落到 `D:\ilife-wt\wf155-k\.scratch\locks\gate-runs.log`，**主检出的那份一行不会多**。导出本票条目要读**两处并集**（按 `runId` 去重）：`docs/skills/skill-calorie/t273-gate-runs.md` 已按并集导出 54 行。这条只影响取证归档、不影响门禁本身。
