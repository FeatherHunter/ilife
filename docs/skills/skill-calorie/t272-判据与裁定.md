# #272 · 判据与十一条裁定逐条（横向排行榜页 · 21 条）

判定：**PASS**（本票交付范围内）。真跑读数见 `t272-21条真跑.md`；自证读数见 `t272-自证.md`；门禁运行条目见 `t272-gate-runs.md`。

对照基准＝`docs/skills/skill-calorie/t425-融合基准.md`（强制）；老实物＝`D:\2Study\StudyNotes\SKILLS\卡路里\templates\food_ranking.html`（14349 B／368 行，只读）；装配件＝`packages/skill-calorie/src/diet/rankingDocs.ts`（唯一作者＝本票）。

## 一、判据 1 · 21 条逐条真跑

- 22 条路由记录（21 条榜单词 ＋ `查高热量排行`）**全 `exit 0`、全有产物**，逐条命令／参数／exit／绝对路径／字节见 `t272-21条真跑.md` §一（表里 22 行）。合计：记录 22／exit 0 **22**／有产物 **22**。
- 复跑脚本：`.scratch/t272/run22.mjs`（真出口 `packages/skill-calorie/dist/cli/cmd_read.js`；播种 `docs/research/t81-seed.mjs` 的种子周，`CALORIE_TODAY` 钉在 2026-09-01；**每条跑前还原同一份快照**）。
- **空库是设计行为，读数如下**（本票判据 1 明写）：

  | 命令 | exit | stdout | stderr | 落盘 |
  |---|---|---|---|---|
  | `看高热量榜`（单榜） | 4 | 空 | `ERR 4: 取数失败（缺失阻断）：无饮食记录（2026-09-09 ~ 2026-09-15）` | 0 件 |
  | `看全部排行榜` | 4 | 空 | `ERR 4: 取数失败（缺失阻断）：无排行数据（2026-09-09 ~ 2026-09-15）` | 0 件 |

- 全图同一口径的全量真跑（`docs/skills/skill-calorie/t155-证据/复算-全量真跑.mjs`）：本命令 22/22 `exit 0`（与基线同数），逐条见 `t272-21条真跑.md` §三。

## 二、判据 2 · 十一条裁定逐条

| # | 裁定 | 判读 | 读数 |
|---|---|---|---|
| 1 | 页面最大一个字写内容；参数名／常量名／英文标识符不上屏 | **对上了** | 眉标＝「饮食 · 排行榜」（人话归属，无命令键）；H1＝「排行 高热量榜 2026-09-01 ~ 2026-09-07」。22 页逐页判：`MEAL_WINDOWS` 0／`MEAL_BUCKETS` 0／`undefined` 0／`null` 0／`NaN` 0／裸词 `up` 0／裸词 `down` 0（可见文本口径，剥掉复制载荷）。探针 `t272-排行榜页.test.mjs` ⑥ 机器话四类里只剩「JSON」＝公共层复制菜单的格式名（`COPY_FORMAT_LABELS`），与 `test/t401c` 的 `ALLOWED_UPPER` 同一处境（那条探针今天也为它红着）⇒ 本票放过这一个词，其余 0 命中 |
| 2 | 结论句紧跟标题 | **对上了** | 22/22 页有；走副标题槽 `ilife-block-page-shell-subtitle`，紧跟 `<h1 class="ilife-block-page-shell-title">`。例：高热量榜「本窗 7 种食物上榜，头名「米饭」：总热量 2050 卡，共 4 次。」；全榜「五类榜里 5 类本窗有数据，合计 33 种食物上榜；高热量榜的头名「米饭」（总热量 2050 卡，共 4 次）。」——逐句含本页读数 |
| 2-补 | 结论句与来源脚注都是普通小字行，不走深底块 | **对上了** | 22/22 页 `ilife-block ilife-block-feedback`（深底反馈块元素）**0 个**；结论在页壳副标题槽、来源在 `ilife-block-caliber`（12px `--fg2` 浅色行） |
| 3 | 页内导航 ＋ 口径说明行 ＋ 来源脚注三条恒出 | **对上了** | 22/22 页含 `<nav class="ilife-block-toc" aria-label="页内导航">`；22/22 含 `<p class="ilife-block-caliber">`；22/22 含来源脚注「数据来源 · 饮食记录 · 2026-09-01 → 2026-09-07 · 共 N 条」。三条全走公共层（`renderTocBlock`／`renderCaliberLine` ＋ `shared/sourceLine.ts`），页面本地不造 |
| 4 | 缺值一律 `—`；空窗出空态句与引导句；空档绝不当 0 | **对上了（#272 整改后；初版这一支没对上）** | 缺值：**对上了**——表里缺字段写 `—`；零值那条（💧水）营养结构写 `—`。空档不当 0：**对上了**——零值不画条身（见裁定 5）。**空窗整页：初版没对上、整改已改正**——初版把「窗口内零记录」与「库里零记录」并进同一条 rejection，空窗也走 `exit 4`（当时记作「遗留出口 1」）；2026-09-15 编排者在 `t425-融合基准.md:161-164` 裁明两态**不互斥**（窗口为空 ⇒ 出完整页 ＋ 空态句 ＋ 引导句；库为空才 `exit 4`，且「后续任何票不得据本条去改空库的 exit 4」）⇒ 整改把两态判在取数层 `diet/rankingPlate.ts`（库整表行数／窗内行数两条读数），空窗出整页（`exit 0`、落盘、回绝对路径），空库逐字保持 `exit 4` ＋ `ERR 4: 取数失败（缺失阻断）`、不落盘。机器读数见 `t272-整改-报告.md` 与 `t272-整改-run.mjs`。部分空：**对上了**——全榜页本窗无数据的榜读数卡写 `—` ＋「本窗无数据」、**不出折叠块**、口径行点名；用「只播一条 💧水」的库实测：5 类榜里 3 类有数据 ⇒ `<details>` 恰 **3** 个、口径行读到「本窗没有数据的榜不出明细块：低热量榜、常吃榜」 |
| 5 | 图表必须可读数，且不画无效图元 | **对上了** | 本族没有折线／柱图；「图」＝每行一条三段堆叠条：宽度按热量占比（每克蛋白 4／碳水 4／脂肪 9 千卡），第三段吃余数 ⇒ **每行三段恒好 100%**（老实物三段各自四舍五入可能加到 101% 把条撑出格，这里是改正）；文字百分比与条同格互为兜底；零值行**不画条身**、文字位写 `—`。无空档折线，故「跨空档同色虚线」不在本族 |
| 6 | 有目标才有完成度条 | **不适用（本票不接）** | ③ 类第 5 项（KPI 读数）本族的三张卡是「上榜种数／头名／上榜合计」，无目标可比 ⇒ 不带完成度条、也不空画条；条位归 **#418**，本票不碰 |
| 7 | 复制区恒双按钮、不出与按钮同名的标题、日志第 4 段写命令原文 | **对上了** | 22/22 页含「复制数据 ▾」开合器（`data-fmt-open="1"` ＋ 三格式项 `data-fmt="text|json|csv"`）与「复制日志」（`data-action-id="ilife-copy-log"`）；**日志按钮不带 `disabled`**（改前它是**死的**：基线产物里那颗按钮带 `disabled`——日志文本没接）。日志六段齐（场景标识／AI 思考链／数据结构／调用链／时间戳版本／异常），第 4 段＝本次命令原文，逐字带本次参数。读数（看高热量榜）：`calorie-cmd-read calorie.view.ranking --params '{"category":"high_calorie","topN":10,"window":"7d"}'`。无与按钮同名的标题（复制区标题「复制榜单」与两颗按钮都不同名） |
| 8 | 老侧手机两段式卡片＋餐别四色＝公共层增量，本图只登记 | **登记＋只落可落的那一处** | 本图落「前三名金银铜字形（🥇🥈🥉）＋该行左侧 3px 色条（金 `#f5b301`／银 `#a8a8ad`／铜 `#c77b3f`，老实物手机端同一处的三个值）」；**色值全族只有一处**（页内样式段 3 行，探针钉「金色值在样式段恰 1 次」）。**断点与两段式卡片形态仍登记在待切公共层票**，本图不作验收项 |
| 9 | 失败面走新仓整页回执，不救老件 | **对上了** | 本族失败面＝缺失阻断那条 `ERR 4`（`cli/cmd_read.ts` 的统一出口）：不落盘、不出页；产物里 `error_receipt.html` 的任何特有类名 0 命中 |
| 10 | 食品库类页用卡片格，不许改成表格 | **不适用** | ③ 类不是食品库类；本票一行不碰 `libraryDocs.ts` |
| 11 | 被别的票钉住的文案：不越界改引脚，改用别的槽位 | **对上了（两处按此办）** | ① **表题仍用 `dietEngine.ts` 的 `RANK_TITLES`（`r.title`）**：`test/diet-homogeneity-108.test.mjs:194` 逐字要「热量炸弹榜」、`test/t511-清尾二.test.mjs:68` 逐字要「📅 常吃榜」⇒ 不动那两条引脚，榜名的**页面侧单源**是 `RANK_ZH`（读数卡／页题／折叠标题）；② **复制区标题沿用「复制榜单」**（同一条测试钉着）⇒ 不改成与按钮同名 |

**裁定 7 段序**：以 `packages/base-render/src/spec/text.ts:25` 的 `LOG_SECTIONS` 为准（命令原文在**第 4 段「调用链」**），实测第 4 段就是命令原文（上表读数）。

## 三、判据 3 · §五 骨架表 ③ 列逐格

图例：● 恒出／○ 有内容才出／— 本类不出。读数取自 `.scratch/t272/run22/` 的 22 份产物。

| 序 | 区块 | ③ 该是 | 本票实测 | 读数 |
|---|---|---|---|---|
| 1 | 眉标行 | ● | ● | `ilife-block-page-shell-eyebrow` =「饮食 · 排行榜」（类型徽章位本族不用——不放英文页型词） |
| 2 | 内容标题 | ● | ● | H1 =「排行 <榜名> <起> ~ <止>」（如「排行 高碳水榜 2026-09-01 ~ 2026-09-07」） |
| 3 | 结论句一行 | ● | ● | 页壳副标题槽，逐页含读数（裁定 2） |
| 4 | 页内导航 | ● | ● | `<nav class="ilife-block-toc" aria-label="页内导航">`：单榜 3 锚点（上榜速览／营养结构／榜单明细）、全榜 2 锚点 |
| 5 | KPI 读数 | ● | ● | `renderKpiGrid`：单榜 3 张（榜名＋上榜种数／头名／上榜合计）；全榜 5 张（一类一张） |
| 6 | 主体图 | ○ | ○ | 有内容才出：**营养结构块**（每行一条三段堆叠条＋文字百分比）；全榜页不出（○＝可不出） |
| 7 | 字段变更对照 | — | — | 不出（本类无改类操作） |
| 8 | 今日累计 | — | — | 不出（本类无写后累计） |
| 9 | 主表／主列表 | ● | ● | `renderDataTable` 六列，列序按榜单类查表（§四） |
| 10 | 识别差异标记 | — | — | 不出 |
| 11 | 确认后操作行 | — | — | 不出 |
| 12 | 空态块＋引导句 | ● | ● | 表的 `emptyText`＝「本窗没有可上榜的食物（记一餐之后再看这张榜）」（带「怎么记第一条」的引导）；空榜读数卡＝`—` ＋「本窗无数据」；**窗口为空那张整页**（#272 整改）另出 `emptyGuide` 空态块：空态句「本窗 … 里一条饮食记录都没有，…」＋引导句「说「记一餐」…补以前的日期就说「补记饮食」」 |
| 13 | 口径说明行 | ○ | ○ | 出（`renderCaliberLine`）：单榜说折算与缺值口径，全榜说列序与空榜口径（并点名空榜） |
| 14 | 复制区（双按钮） | ● | ● | 出：数据三格式菜单 ＋ 复制日志六段（裁定 7） |
| 15 | 来源脚注一行 | ● | ● | 出：`数据来源 · 饮食记录 · 起 → 止 · 共 N 条` |

区块次序照 §五 那张表：眉标 → 标题 → 结论句 → 页内导航 → KPI → 营养结构 → 主表 → 口径行 → 复制区 → 来源脚注（复制区在来源脚注之前，与表的 14／15 序一致）。

## 四、老实物逐条（§二 移植清单 ③ 类那四条 ＋ 反面清单）

| 序 | 移植项 | 判读 | 落法／读数 |
|---|---|---|---|
| 23 | 五类榜共用一个渲染函数，表头与取数按类别查表 | **对上了** | `RANK_METRIC` ＋ `columnsOf()` 一处定死五类的列序，21 条词不写 21 份。实测表头（`.scratch/t272/run22/_summary.json`）：高热量／低热量＝排名｜食物｜总热量｜次数｜餐均｜营养结构；常吃＝排名｜食物｜次数｜总热量｜餐均｜营养结构；高碳水／高蛋白＝排名｜食物｜总碳水（总蛋白）｜次数｜总热量｜营养结构——**主指标恒在第 3 列、同类指标不重复出列**（首次自测得「常吃榜出两列次数」，本票当场改正后复跑） |
| 24 | 名次前三金银铜圆章，手机端同步成左侧色条 | **对上了** | 前三名次写 `🥇 1`／`🥈 2`／`🥉 3`（金／银／铜三字形，正文不写色值），该行带左侧 3px 色条；色值一处：`RANK_CSS` 里 `.r1{background:#f5b301}`／`.r2{background:#a8a8ad}`／`.r3{background:#c77b3f}`（老实物手机端同三个值）。断点与两段式卡片归公共层票（裁定 8） |
| 25 | 营养结构一条三段堆叠条 ＋ 文字百分比（图文互为兜底） | **对上了** | 表内「营养结构」列＝文字百分比（`蛋白 10%｜碳水 79%｜脂肪 11%`，可复制、可读屏）；表下「营养结构」块＝同数据的三段堆叠条（色取公共层 `CHART_PALETTE` 前三位 `#007aff`／`#34c759`／`#ff9500`）；零值行文字写 `—`、不画条。**逐色对照（#272 整改更正，原写「与老实物逐色一致」不实）**：碳水 `#34c759`、脂肪 `#ff9500` 与老实物 `--green`／`--orange`（`food_ranking.html:13`）同值；**蛋白段不同**——老实物 `.protein{background:var(--accent)}` 是 `#0071e3`（`:12`），新侧取公共层色板首位 `#007aff`（裁定 8：色值单源住公共层，页面不写色值；代价就是这一色与老实物有差） |
| 26 | 无数据页签不显示；列表空给占位列 | **对上了** | 全榜页只给**有数据**的榜出折叠块（实测：只播一条 💧水 ⇒ 3 个 `<details>`，空的两类在读数卡与口径行里点名）；表空走 `emptyText` 占位 |
| 反面 | 榜单表头用写死的通用列序，把「高蛋白」等榜的总热量挤到第 5 列 | **已改正** | 见第 23 行读数：主指标恒第 3 列（老实物行值／表头错位那处一并改正） |

老实物 `food_ranking.html` **有、新侧不搬**的（老页交互未移植登记，`t425` §二-补）：tab 切换（本仓一命令一页，改由 21 条词各自出页 ＋ 全榜页五折叠）、页内 `backTop` 锚、`copyToAI` 死函数。**未作验收项**。

## 五、§三 新保留清单（本页保留了哪些）

| 序 | 保留项 | 判读 |
|---|---|---|
| 1 | 整页装配一次成型、模板唯一 | **对上了**：`assembleDocPage`（`shared/docPage.ts`），产物 `<!doctype html>` ＋ charset ＋ 样式，双击可开（探针 `assertDoc` 同口径：`ilife-page`／`ilife-block`／`<style>`／`<script>` 全在） |
| 2 | 15 个共享区块只选不造 | **对上了**：用 `renderTocBlock`／`renderCaliberLine`／`renderKpiGrid`／`renderDataTable`／`renderDisclosure`；本族只自造「名次行＋三段条」这一个形状，样式 10 行住本件（无色值外泄，三色取公共层色板） |
| 5 | 空态不出按钮、与按钮同名的标题不出 | **对上了**（裁定 7 读数） |
| 6 | 缺值口径 `—` ＋ 复制数据保留原始空值 | **对上了**：可见文本 `—`；复制载荷是原始数字 |
| 8 | 空态句恒有 | **对上了**（骨架第 12 格） |
| 10 | 命令一页、页面一命令 | **对上了**：21 条词 → 一页；单榜／全榜两条装配路同住一件 |

## 六、遗留出口（要编排者裁定的事）

1. **空窗两态互斥**：`t425` 裁定 4 要「空窗仍出完整页＋引导句」，而本票判据 1 与派单 §十四坑 3 明写空库走「缺失阻断 `exit 4`」。我按**票面判据**执行（exit 4、不落盘）。若编排者要改判，落点＝`diet/rankingPlate.ts`（本票声明路径内），但会同时改动别的票的期望（`#279` 空库两态、`cmd-read` 系列断言），**需一并裁**。
2. **榜名两个来源**：页面侧 `RANK_ZH`＝唤醒词名（高热量榜／低热量榜／常吃榜…），表题侧 `dietEngine.ts:RANK_TITLES`＝老名（🔥 热量炸弹榜／🥬 低热量健康榜／📅 常吃榜…）⇒ `high_calorie`／`low_calorie` 两页会出现两个名字。统一只能改 `dietEngine.ts`（**不在本票声明路径**），且它被 `test/diet-homogeneity-108.test.mjs:194` 与 `test/t511-清尾二.test.mjs:68` 逐字钉住 ⇒ 需要一张收口票同时动源码与那两条引脚。**本次不碰**（裁定 11）。
3. **`查高热量排行` 丢窗口之外的主参数**：`src/diet/routes.ts` 的 `NEW_KEY_ROUTES` 那条 `cli` 是 `{"window":"7d"}`——**没有 `category`** ⇒ 跑出来是「全部排行」页（22 号记录实测：页题「全部排行 …」、五类榜全景），而词面要的是「高热量榜」。派单明写「别自己改路由」，**未动**，报编排者。（同款：scene 10 的 `查高热量榜` order 433 也是 `{"window":"7d"}`，不属本图。）
4. **入库证据脚本的播种缺陷**：`docs/skills/skill-calorie/t155-证据/复算-全量真跑.mjs` 播饮食记录时把碳水参数名写成 `carbohydrates`，而 `calorie.diet.add` 认的是 `carbs`（`shared/writeParts.ts` 的 `F.diet`）⇒ 那份库所有行碳水＝0，碳水相关读数恒 0%。**不在我声明路径，未动**；本票改用 `t81-seed` 另跑一套真数据（`t272-21条真跑.md` §一）。
5. **复制日志第 1 段「场景标识」**印成 `calorie.calorie.view.ranking`（`buildLogText` 拼 `<skill>.<key>`，而 key 本身已带 `calorie.` 前缀）——全仓同形，属公共层问题，不属本票；仅登记。
6. **告警线台账今天红在别人的件上**：`scripts/gen-cli.mjs`（台账 754／实况 796）与 `scripts/build-help.mjs`（台账 362／实况 396）陈化。两件都不在本票写集（后者还是 §五 点名的生成物，归编排者收口），我**不越界 sync**。本票两个改动件的 LF：`src/diet/rankingDocs.ts` **322**／`src/diet/ranking.ts` **33**（**更正**：本节初稿写 296／30，抄错了，疑似抄了提交的插入行数；复核席实测 322／33，两件自 `b2d82ab` 起逐字节未变），都在 350 以内，故本票无该挂的行。**#272 整改后**：`rankingDocs.ts` **359**（越线，已在 `packages/skill-calorie/AGENTS.md` 台账挂号）／`ranking.ts` **37**／`rankingPlate.ts` **131**。
7. **`pnpm run gen:check` 今天红**：`Error: 预检页表含注册表里没有的键：composition`（另有座位在途）。本票没碰命令声明与生成物，报编排者。
