# #454 独立对抗审查报告（趋势／复盘族融合）

**判定：PASS**（无 S1；五维 28／24／19／15／9 ＝ **95／100**）

被审：`487014c`（两支 702→760 LF／判据件 372 行／证据件 130 行／台账 1 行）＋ `9cef4fc`／`ef419ce`。
基线只取 `git cat-file blob 487014c:<件>`，`git hash-object` 对账同源：源码 `8e35df31`／判据件 `985fa2d9`／证据件 `ef1380df`／台账 `710183f2`，**CR 全 0**。
未读全仓；**真件零写入**（未变异／未还原 `sportPortDocs.ts`），变异全在隔离镜像 `.scratch/t454-review/mirror/`（`git archive HEAD` 树＋同名 junction）。自设探针 `docs/skills/skill-calorie/t454-review-probe.mjs`（26 条断言本席自写，**不 import 判据件**）。

## 一、机器证据（走 `node tooling/run-locked.mjs --ticket 454 -- …`）

| 门 | runId | 读数 |
|---|---|---|
| `pnpm build` 进场／tip `a08d488` | `2eb54737…`／`0a753df8…` | exit 0／**exit 2**：`analysis/multiTrendPage.ts(369) TS2304`，该件盘上 `M`（他席在途）⇒ 非本票 |
| 新判据 进场／tip | `c33a5601…`／`989e2497…` | 两次均 exit 0 · **9/9** |
| `doc-page-assert` 进场／tip | `75e4b6ba…`／`33c22283…` | 两次均 exit 0 · 1/1 |
| 告警线 进场／tip | `c770a4b4…`／`2ef83bbc…` | **58/58 exit 0**／57/58 exit 1＝同件 `台账=490 实况=531` ⇒ 非本票 |
| 镜像判据（绿基线） | `ad1e2233…` | exit 0 · 9/9；镜像源码 `2CEC26DA…`、dist `D42BFD51…` **＝证据件登记基线同值** |
| 镜像判据（旧版 7d4bd7f） | `654682e1…` | exit 1 · **1 pass／8 fail**，唯一绿「五窗同版」，8 条首红一律「缺页内导航」 |

- **parity**：镜像 dist 与真 dist 文件面 1112 件＝同 1076／差 36／镜像独有 0，36 件差异**全落盘上 `M` 的源件**；12 份趋势／复盘产物与真 dist **逐字节相同**（81005／71451／72259／71476／71500／72257／95977＋4 份五窗同版），6 份种子回执抹掉渲染当刻时钟后 **6/6** 相同。
- 证据件登记的 **22 条 runId 逐条命中共享 `gate-runs.log`**（cmd／exit 同值）。

## 二、自设变异两处（镜像内 src→定向编译→dist→判据；写回原字节）

| 号 | 改坏（与实施席两处不同） | 红读数 | 改回 |
|---|---|---|---|
| A | 趋势页删截断明示（`truncated ? '——已截断…' : '，未截断'` → 固定 `'，未截断'`） | dist `FE855297…`；`98b28ff6…` exit 1 · 8/1，断言原字**「截断没有明示」** | blob `8e35df31` → dist **`D42BFD51…`**；`c71618d2…` exit 0 · 9/9 |
| B | 复盘页结论条换纯文段（`renderFeedbackBlock({staticNotice…})` → `<p class="ilife-block-caliber">`，锚点与卡不在动） | dist `BB6C6A02…`；`9b1342fb…` exit 1 · 4/5，断言原字**「运动复盘（X） 缺一句话结论条」×5 窗** | 同 blob → 同 dist；`0a11894d…` exit 0 · 9/9 |

## 三、自设探针 26/26 绿（镜像 `d7327b19…`；真 dist 复跑 `2f88ef3d…`）

- ① 冻结表**枚举**命中两命令恰 6 条、逐字对上票面 6 条词；6 条逐条真跑 exit 0。
- ② 五窗同版：锚点 id 序列五窗逐条同＝`sec-window,sec-conclusion,sec-figures,sec-category,sec-badges,sec-daily,sec-source`；区块类名 36 个同值；导航项／三格式／版面根／锚点＝7/3/4/7；五份 h1 各含本窗起止日。
- ③ 双刻度：`sec-line` 恰 2 条序列（实线 1＋`stroke-dasharray="6 5"` 1）＋`data-s="1"`；断点：缺日 06-05 三格全「—」，点＝有记录天数，且**逐点用 `data-i` 反查日期**全落在有记录的日期上。
- ④ 长窗：页眉「共 120 天；本表列出 100 天——已截断，只列前 100 天」＝可见 100 行＝101 `<tr>`−表头；30 天窗印「未截断」；逐日表日期连续 30 行。
- 另查：锚点全落点、`ilife-page-printable` 落版面根＋`@page printable`、来源脚注、三格式顺序、口径行 ≥2、三类工程话 0（超集扫描 `calorie.view.`／`t\d{3}`／`#\d{3}`／「移植」／`calorie-cmd-read` 亦 0）、空窗真出口 exit 4 不落产物＋装配层空态印「说「记运动」」且五张卡整卡不出。

**逐条核票面**：趋势四件、复盘四件、页头人话、五窗同版、断点、截断、6 条词、锚点、可打印、脚注、三格式、空态逐条有落点（读数见 §一／§三）；未接的族内两件见 D4。

## 四、归属

`git show --name-only 487014c` **恰声明四件**；`487014c^`＝`f32a77d`。逐字节比：`buildDistributionDoc`／`buildStrengthDoc`／`buildCardioDoc`／`buildReviewDoc` **四支旧（7d4bd7f）＝新**（4006／3159／3033／2236B，sha256 各自同值）；改动只落 `buildRecapDoc`（2183→2473B）与 `buildTrendDoc`（2669→3494B）＋件头＋import 两行；CR 旧新皆 0。

## 五、缺陷段

- **D1 本票范围 S3**：证据件 §三 六件字节与本席复跑**每件恰 +85**；来源＝487014c 之后入库的 `base-render/src/blocks.ts`（#154 的 list-rows 规则，动的是每页共用样式段）。证据**未标读数时效**。
- **D2 本票范围 S3**：台账「结论」列在 487014c 仍是 #453 原文（「本票（#453）……两支是第 5 票的地盘、一行不碰」），只改「当场实测」702→760；只看台账会读成本票尚未实施。
- **D3 本票范围 S3**：两页**新增**可见文本印库表名 `exercise_log`（来源脚注）；兄弟票 452／453 票面有「可见文本零 `snake_case`」而 **454 票面漏了这条**，故不判违约，作族内不一致当场登记。
- **D4 本票范围 S3**：票面族票共同要求里的「四态头／变更卡载具」在证据件一句未交待（#453 证据件有一条「器件归类，不是漏做」），偏差只写在源码件头。
- **D5 范围外（注记）**：稀疏数据下折线路径只有 `M`、`L=0`＝点可见线不可见（引擎在空值处断段）；旧版同读数 ⇒ **非本票引入**，建议 #268 视觉终验收口。
- **D6 范围外（污染登记）**：tip 的 build 红与告警线红全归他席在途件；另两次包装器内探针跑落在他人 `pnpm build` 写 dist 的窗口里（exit 1 且无输出），锁一放开复跑 26/26——判据读共享 `dist` 的通病，非本票。

## 六、五维分（30／25／20／15／10）

| 维 | 分 | 依据 |
|---|---|---|
| 契约一致 | 28 | 票面逐条有落点；扣 1＝四态头／载具未在证据件交待（D4）；扣 1＝新增可见文本含库表名（D3） |
| 证据真实可复现 | 24 | 22/22 runId 命中门禁日志、基线 sha256 双向同值、红绿读数独立复现；扣 1＝六件字节未标时效（D1） |
| 新旧对照 | 19 | 旧版 1/8 红读数与实施席登记逐项同值；扣 1＝两条旧判据与新形状冲突未清零（登记在案、票面不许动） |
| 工程红线 | 15 | 只含声明四件；四支逐字节未动；CR 全 0；真件零写入；危险 git 零使用；台账 58/58 |
| 文档同步 | 9 | 证据件覆盖真跑／变异／告警线／未做项／样张；扣 1＝台账结论列陈化（D2） |

**无 S1，均分 95 ≥ 85 → PASS。** 遗留：D1–D4 归后续席按记账口径补正；D5 归 #268。
