# t525 · 对抗审查（独立席位）

审：**#525**（场景09 共享形状与样板页）· 作者提交 `4d38326`／`ab645c8`／`f8068ac`／`5061300`
审查日：2026-09-15 · 我这份只写审查件，未改任何产品代码
证据落点：`.scratch/review-525/`（登录门/探针/逐维分/公共债/越界 的原始读数）
审查环境：盘上有多条别线在途件（`blocks.ts`／`index.ts`／`pageShapes.ts`／`diet/*Docs.ts` 半截），
故**弃用工作树**，改用隔离构建（`isobuild.mjs`：两包从 `5061300` 取快照 → 私有 outDir → 私有
`node_modules/base-paint` → 私有 dist 跑页），A/B 只差票内文件，别的 session 重建 `dist/` 不影响读数。

## 一、判据表（作者声称／我实测／差）

| # | 作者声称 | 我实测 | 差 |
|---|---|---|---|
| 1 登录门 | 「不传 `pageUi` 的调用方产物逐字节不变」；四行读数 `before/after/mutant/restored`，`67/70 逐字节相同`，`mutant 63/70 变` | ①**这句成立**：t0 ↔ t0-ui（`docPage.ts` 回落到无该位）＝**63 相同 / 7 不同**，7＝4 页显式启用新位者 ＋ 3 页非确定页。②**但登录门没覆盖本票自己那笔冻结值变更**：作者 `gate/`、`gate2/` 四个目录 mtime 13:22～13:29，**都在 `ab645c8`（14:04）之前**，其产物里 `.ilife-copy-btn` 仍是 `min-height: 40px`、全无 `[disabled]` 规则 ⇒ 两臂都不是「票前/票后」。③我的隔离复算：只把冻结值回落 40（`t0-40`）→ **64/70 页变了**（末笔 ↔ 票前 `pre` 更是 56/62 不同）。④mutant 我测 **60/70 变**（作者 63/70，同量级）；还原后 67/70 相同（差 3 页非确定） | **差**：登录门读数被当成「整票改前≡改后」用，实际只对「`pageUi` 位」这一个增量成立；票面判据 1 的字面要求（旧调用方产物逐字节不变）**未成立**（冻结值一处就动 64/70 页） |
| 2 三档零溢出 | 390/768/1440 `docScrollWidth − innerWidth == 0` | 自己跑 `measure-responsive.mjs`：`390/768/1440 全 0`、`OVERFLOW-ZERO pages=1 cells=3 failed=0` | **无差**（且我按 `t524` 口径记：这是回归门不是成绩） |
| 3 触摸区 | 390/768/1440 **三档 0 处 <44px**；`ACTION_BAR_DEFAULTS.minHeightPx` 40→44；⑤ 组「由 ≤820 档提到全宽档」 | ①作者产物上确为 0（探针复跑 PASS）。②**交付提交里没有「⑤ 提到全宽档」这笔**：该改动只在工作树未提交（`pageUi.ts` 现未提交版＝`537194C7…`，HEAD 仍是 `@media (max-width: 820px)`）。③用交付提交（`5061300`）重出样板页（291854 B）→ 探针 **1440 档 4 处 <44px（4 个页内导航锚点）→ FAIL** | **差**：读数与「全宽档」描述属于未提交工作树；交付提交状态在 1440 档不达标（按 §4.2 口径这 4 处不进 H6，只记观感，但**产物与提交不一致**这一条必须消掉） |
| 4 分隔符 | 节点级全零 | 自己跑 `audit-separators.mjs`：`node·0 ；0 ≥3段0 ｜0 、0 ~0 标识符0 命中=0`、`RESULT: 1/1 PASS` | **无差** |
| 5 老调用方不回归 | `npx tsc -b` 本票写集 0 错；`contract-signatures` 48/48、`copy-format-menu-247`＋`style` 46/46 绿 | 自己跑 `node --test packages/base-render/test/contract-signatures.test.mjs` → **48/48 pass**；`check-warning-line.mjs` → **`RESULT: 73/73` PASS** | **无差**（构建当刻仍带别线的错，见 §五） |
| 6 样板页 ≥92 | VLM 原分 **62**；作者按五维自算 **94**，并声明「不自评过线」 | 版面档双档（390×844／1280×900，注入 `img,video{opacity:.10;filter:grayscale(1)}`）送 VLM：给 `12/16/22/12/21`（83，且把「图被压暗」当缺陷扣 H1×2——那是协议自己注入的，不成立）。我按 §4.1 唯一权重独立打分 **88/100**（D1 13／D2 15／D3 25／D4 13／D5 22，硬扣 0） | **差**：作者自算 94 用的是**已被 §4.1 判为废稿的权重（D4 20／D5 20）**；按唯一口径重算其自报表也只到 93.75，而我实测 88 **低于本票自测线 92** |

## 二、两处公共债（自己出页核，含真 Chrome 计算样式）

**① 读页「复制日志」置灰**——自己出 1 页读页（`calorie.photo.list`）＋1 页写后回执（`calorie.photo.add`）：

| 读数 | 读页 | 回执页 |
|---|---|---|
| 按钮 `disabled` 属性 | **在**（`class="ilife-copy-btn ilife-copy-btn-ghost"`，无 `data-t`） | **不在**（带 `data-t=…`） |
| 真 Chrome `getComputedStyle` | **`opacity: .45`／`cursor: not-allowed`** | `opacity: 1`／`cursor: pointer` |
| 命中区 | 256×44 | 256×44 |

- 改前同页对照（把我私有构建的 `style.ts` 单独回落 4d38326）：`disabled=true` 却 `opacity: 1／cursor: pointer`
  ⇒ 作者「属性在、观感不在」这句**成立**。
- 「原来**全仓没有一条 `[disabled]` 规则**」：`git grep '[disabled]' 5efb139 -- packages` **为空** ⇒ 成立。
  （精确地说：属性选择器 `[disabled]` 票前确实一条都没有；另有一条既有 `:disabled`——`packages/skill-calorie/src/render/planEditorCss.ts:25`
  `button:disabled{color:var(--fg3);cursor:not-allowed}`，与复制区无关，不构成反例。）
- 「定义地只留一处」：`ab645c8` 把 `pageUi.ts` 里那条同义规则删掉并留指向 `style.ts` 的注释 ⇒ 成立
  （本票后 `[disabled]`／`:disabled` 的复制区规则只 2 条，均在 `style.ts` 的 `copyButton` 区）。

**② 三格式菜单标签**——交付面 16 件逐件扫（Node 直读 UTF-8）：

| 组 | 逐字 | 件数 |
|---|---|---|
| 09-01～09-04、09-09、09-13、09-15、09-16（mtime ≥ 14:03 重出过） | `纯文本／JSON／CSV` | 8 |
| 09-05～09-08、09-10～09-12、09-14（mtime 08:15／08:33 旧产物） | `text／json／csv` | 7 |
| 00-场景09验收导航（无复制菜单） | — | 1 |

- 定义地：`packages/base-render/src/controls.ts:1261` `COPY_FORMAT_LABELS = { text: '纯文本', json: 'JSON', csv: 'CSV' }`（公共层，`#496` 落地）。
- `ab645c8` 的 diff 只动 `spec/controls.ts` 的 `minHeightPx`，**没碰**这张表 ⇒ 作者「本票不改代码、只重出＋留读数」**成立**；
  「交付面显英文是旧产物」也**成立**（同一条产线重出即中文）。

## 三、样板页版面档逐维分（我自己判）

| 维（§4.1 唯一权重） | 满权 | 我给 | 依据 |
|---|---:|---:|---|
| D1 结构纪律 | 15 | 13 | 页壳 960 是既有口径；三块节奏齐；扣 2：危险区一行三种高度 ＋ 1280 档纵向 2254px、主体占比小 |
| D2 信息层级与版式细调 | 20 | 15 | 主图占比偏小、纵向堆叠偏长（作者自认）；删除区「提示条＋两按钮＋脚下小字」三种字号挤一行 |
| D3 分隔符与文字纪律 | 25 | 25 | 探针节点级 0；无内部标识符／裸命令名；`JSON`／`CSV` 按公共层 `#247` 定案不计 H7（严读则 −2） |
| D4 跨宽自适应 | 15 | 13 | 三档 0 溢出；扣 2：1280 档横向利用低（页壳归 #530） |
| D5 手机端与 HELP 同档 | 25 | 22 | 390/768 触摸区 0 处 <44px、最小字号 12px；扣 3：窄屏表格未走 `data-label` 卡片化（该页 `[data-label]` 命中 0）——**照抄指引第 4.1 节自己没示范这条** |
| **合计** | 100 | **88** | 硬扣 H1–H7 逐条 0；D2 占满权 75%（低于 §4.2 的 80% 线） |

**「作者 62 分是否量纲错」**：**是**——票面 ≥92 是 §4.2 唯一算式（Σ逐维 − Σ硬扣）下的线，不是「问 VLM 要一个整数」
的绝对分，拿 62 直接比线确实量纲不齐；但作者用来替代它的 94 同样不是唯一口径（废稿权重），而我的独立打分
**88 < 92**，样板页按本票判据 6 **未过线**，不能当 #526～#529 的达标照抄件。

## 四、冻结值变更复核（#76 基线 40→44）

| 派生面 | 读数 | 一致 |
|---|---|---|
| `packages/base-render/src/spec/controls.ts:308` `minHeightPx: 44` | 44 | — |
| `packages/base-render/src/spec/index.ts:93` 签名串 `… minHeightPx: 44 …` | 44 | ✓ |
| `docs/base-paint-contract.md:325` FROZEN-SURFACE-TABLE 同串 | 44 | ✓ |
| `packages/base-render/test/contract-signatures.test.mjs:602` 钉死行 | 44 | ✓ |
| 3.6 节新增的 8 运行名（`PAGE_UI_CLASS`／`PAGE_UI_VIEWPORT`／`pageUiCss`／`MEDIA_RATIOS`／`pageShapeCss`／`renderFactStrip`／`renderMediaFigure`／`renderTimelineRows`） | 交付提交里**逐个存在** | ✓ |
| `contract-signatures.test.mjs` 自跑 | **48/48 pass** | ✓ |

⇒ **没有「只改数值漏改派生面」**；但**影响面被低估**：这个常量经 `style.ts` 四处插值进每页 CSS，
改一处＝改 64/70 页产物的字节（§一 判据 1 ③）。

## 五、越界与回归

- 四笔提交逐笔 `git show --stat`：`4d38326` 2 件／`ab645c8` 6 件／`f8068ac` 1 件／`5061300` 2 件，
  `src/photo/**` 命中 **0/0/0/0** ⇒ **没碰不该碰的照片页面件**；暂存集干净，无 `add -A` 迹象。
- **但这不是全部**：本票三个新件（`pageShapes.ts` 343LF／`pageUi.ts` 174LF／`pageKit.ts` 58LF）与
  `docPage.ts`、根出口是随 **`750bf16`（wip 提交）** 落盘的，那笔同时改了 **7 个 `src/photo/**` 文件**
  （#526／#529 的写集）＋ `src/photo/helpDocContent.ts`／`helpDocCss.ts` 两个新件。交付件与票面都没写这件事，
  #526～#529 照票面会以为共用件是独立成提交的。
- `check-warning-line.mjs`：`RESULT: 73/73` `PASS`（作者报 73/73，**真**）。
- `node --test packages/base-render/test/contract-signatures.test.mjs`：48/48（作者报 48/48，**真**）。
- 构建：当刻全仓 `tsc -b` 仍有**别线**在途件的错（`dietDocs.ts` 取 `todayDocs.js` 不存在的导出、
  `blocks.ts` TS6059），与本票写集无关，审查时**不计在作者账上**。

## 六、我另找到的缺陷（作者未报）

1. **交付提交状态下 1440 档页内导航 4 处 <44px**（样板页 1440 `FAIL`）；样板页与证据里的「三档 0 处」只在未提交工作树上成立。（作者在未提交注释里自己写过「1440 档导航只有 27px」，说明他知道，但没落进任何提交。）
2. **样板页的窄屏表格没卡片化**（`[data-label]` 命中 0），而同一份交付件的照抄指引第 4.1 节把「窄屏表格卡片化」列为必抄条 ⇒ 样板页没示范自己要求的那条。
3. **非确定页的归一化欠一条**：`goal-config`／`goal-status` 的不稳定源是「渲染当刻时钟」写进可见文本（`2026-09-15 06:22:21` → `06:22:26`），`gate2.mjs` 的四条 `CLOCK_STRIP_RULES` 都不匹配这个位置，导致任何 sha 门在这 3 页上永远读不准（作者只把它记成「页面抖动」，没提议补归一化规则）。
4. **作者自算分权重用废稿**（D4 20／D5 20，§4.1 明说该套已废），其余票若照抄这张表会继续走散。

## 七、总分与结论

按六项判据加权（权重／得分）：

| 项 | 权 | 得 | 为什么 |
|---|---:|---:|---|
| 交付物（3 新件＋照抄指引） | 25 | 20 | 件真在、线内，但样板页产物不能由提交复现；写集边界漏记 |
| 判据 1 登录门 | 20 | 8 | 方法有牙齿（mutant 60/70 变），但两臂错时点、结论被当「整票改前≡改后」用 |
| 判据 2＋4 三档／分隔符 | 15 | 15 | 自己复跑全绿 |
| 判据 3 触摸区 | 10 | 6 | 产物上达标，交付提交上 1440 档 4 处 <44px，「全宽档」不在提交里 |
| 判据 6 样板页 ≥92 | 20 | 10 | 我判 88、VLM 判 83，均未过线 |
| 两处公共债（顺手修） | 10 | 10 | 两处都真修好，真 Chrome 计算样式＋交付面逐字双证 |
| **总分** | **100** | **69** | 3 页非确定页的说明经证实为真（不加分，只说明作者没在此处遮掩）；若把那两条「文档说有、提交里没有」也算进去则更低 |

⇒ **总分 69／100，结论：退回（逐条必改项）**：

1. **把「⑤ 全宽档」落进提交**（现留在工作树的那笔），或把交付件／证据里「全宽档」「1440 档 0 处」逐字改成窄屏口径——不许留「文档说改了、提交里没有」。
2. **样板页与两档截图必须能由提交状态复现**：现在 292053 B 的产物吃进了两条**别人的未提交改动**（`pageUi.ts`／`blocks.ts`）；用提交状态重出，并把重出后的 1440 档触摸读数（当刻 4 处 <44px）落盘。
3. **登录门两臂重做或改写结论**：现在的两臂都早于 `ab645c8`、都是 40px、都无置灰规则；要么用票前提交重跑并如实写「冻结值变更动 64/70 页」，要么把结论收窄成「`pageUi` 位自身不回归」。
4. **自算分改回唯一口径（15／20／25／15／25）**，并按我给的 88 分逐条补：D2（主图占比／删除区层次）、D5（窄屏表格卡片化）、D1（危险区一行节奏）。
5. **交付件补记**：三个新件是随 `750bf16`（含 7 个 `src/photo/**` 件）落盘的，写清写集边界。

**不实两处**（作者说有、实测无）：①「⑤ 组已提到全宽档」；②「样板页三档 0 处 <44px」在交付提交下不成立。
**作者没遮掩**：3 页「页面自身逐字节不稳定」经同构建连跑两次证实**为真**（差异是渲染当刻时钟），不是托词。
