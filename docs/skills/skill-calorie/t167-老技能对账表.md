# t167 · 场景 04 运动：39 唤醒词 × 老技能逐词对账

- 票：**#167**（地图 **#156**）· 2026-09-13。
- 同名结论与页面/公共层归类见同目录 `t167-边界与公共层.md`。
- 参照对象 **#152（场景 07 基础信息）**只作判法参照，不处理那张图的账（用户 2026-09-13 裁定）。

一次只读事实核查。**未执行任何脚本、未跑 `pnpm test`、未改动除本文件以外的任何东西**；所有断言基于静态读源码，逐条给出 `文件:行`。

- 新仓权威列表：`D:\ilife\packages\skill-calorie\src\triggers\scene-04-exercise.ts`（下称 `scene-04-exercise.ts`）
- 老技能权威表：`D:\2Study\StudyNotes\SKILLS\卡路里\scripts\_triggers.py`（下称 `_triggers.py`）
- 老技能场景数据（第三份佐证）：`D:\2Study\StudyNotes\SKILLS\卡路里\.scratch\scene_data\04-运动.json`
- 老技能文档：`D:\2Study\StudyNotes\SKILLS\卡路里\SKILL.md`
- 老技能模板目录：`D:\2Study\StudyNotes\SKILLS\卡路里\templates\`
- 老技能脚本目录：`D:\2Study\StudyNotes\SKILLS\卡路里\scripts\`

---

## 0. 结论速览

| 项 | 结果 |
| --- | --- |
| 场景 04 唤醒词数 | **39**（与地图一致，无差异） |
| 老技能里存在 | **39 / 39**，且每词在 `_triggers.py` 中恰好出现 1 次（无重复、无缺） |
| 老技能条目完整（有 `output_type` + `html_template`） | **39 / 39**（13 × `receipt`，26 × `result`） |
| 老技能有具体实现命令（`data_source` 指向存在的 `render_exercise_*.py`） | **39 / 39**，脚本全部在 `scripts\` 下存在 |
| 归属判断分布 | `老技能做过` 39 / `必须多步交互` 0 / `新架构划出去不做` 0 / `老技能也没做过` 0 |
| 模板缺失 | 无。场景 04 引用的 8 个模板**全部存在**于老 `templates\` |
| 数目异常 | 无。新仓 39 ＝ 老表 39 ＝ `scene_data\04-运动.json` 39 |

**用词说明（重要）**：本表的 `归属判断` 按「老技能是否真做过这条能力」取值。场景 04 的 39 条**全部是老技能做过的**，所以 39 行都取 `老技能做过`（其定义里的「新仓没有该命令」只是这类词常见的表现形式；新仓侧是否接线、`routing.ts` 指向哪个键，见 §5，**不要把本列直接读成「新仓一定没有」**）。逐词附注里写的是新仓侧与老技能不一致的**事实**，不是评价。

---

## 1. 数目核对

**在 `scene-04-exercise.ts` 中找到 39 条。** 文件第 3 行注释自称「04-运动：39 唤醒词（SoT scripts/_triggers.py 原样拆分，顺序保留）」，`TRIGGERS` 数组名为 `SCENE_04_EXERCISE`，条目位于 `scene-04-exercise.ts:5` – `:43`，一符一行。地图所称的 39 **与实际完全一致，无出入**。

交叉核对：

- `_triggers.py` 中 `'category': '运动'` 的条目共 **39** 条，起始行 `_triggers.py:1531`，之后每条固定间隔 10 行，末条 `_triggers.py:1911`。
- `.scratch\scene_data\04-运动.json` 是 39 条对象的数组，13 字段形状（`key/name/wake_word/category/subfunction/output_type/html_template/data_source/prompt_template/user_intent/data_fields/depends_on_external/order`）。
- 三份的**唤醒词序列逐字逐序完全相同**（新旧顺序也逐字相同；老表 39 条的 `key` 与新仓 39 条的 `key` 逐条比对 **0 处差异**；`desc / output_type / html_template` 也逐条相同）。
- 全表 `'wake_word':` 出现 **436** 次，与题述「436 唤醒词」相符。
- 39 条**没有**出现在新仓其他 `scene-*.ts` 里（`scene-01`…`scene-10` 逐词查过，只有 `scene-04-exercise.ts` 命中）。

### 39 个唤醒词（逐字、按文件顺序）

```
记运动
记运动（含备注）
记力量训练
记有氧运动
记日常活动
补记运动
批量补记运动
复制昨日运动
改运动记录
改某日运动
删运动记录
删某日运动
批量删运动
看今日运动
看昨日运动
看本周运动
看上周运动
看本月运动
看上月运动
看最近 7 天运动
看最近 30 天运动
看某段时间运动
看今日运动（vs 目标）
看本周运动（vs 目标）
看运动记录（有备注）
看运动记录（按力量筛选）
看运动记录（按有氧筛选）
看最近 60 天运动
看最近 180 天运动
看最近 365 天运动
看运动类型分布
看力量训练总览
看有氧训练总览
看运动趋势
运动复盘（本周）
运动复盘（本月）
运动复盘（最近 90 天）
运动复盘（今年）
运动复盘（自定义时间）
```

---

## 2. 逐词对账表

`老技能里存在?` 列给老表行号；`老实现文件` 列给脚本 + 实参（取自老条目的 `data_source`，均为 `python scripts/<file>.py …`）。文件名一律指老技能 `scripts\`。

| 唤醒词 | 老技能里存在? | output_type | 模板名 | 老实现文件 | 归属判断 |
| --- | --- | --- | --- | --- | --- |
| 记运动 | 是 · `_triggers.py:1531` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-add` | `老技能做过` |
| 记运动（含备注） | 是 · `:1541` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-add --note <N>` | `老技能做过` |
| 记力量训练 | 是 · `:1551` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-add-strength --sets --load --reps` | `老技能做过` |
| 记有氧运动 | 是 · `:1561` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-add --category 有氧 --distance [--avg-hr] [--max-hr]` | `老技能做过` |
| 记日常活动 | 是 · `:1571` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-add-daily [--steps] [--period]` | `老技能做过` |
| 补记运动 | 是 · `:1581` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-backfill --date` | `老技能做过` |
| 批量补记运动 | 是 · `:1591` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-batch-add --items` | `老技能做过` |
| 复制昨日运动 | 是 · `:1601` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-copy --target` | `老技能做过` |
| 改运动记录 | 是 · `:1611` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-update --id [--field --value]` | `老技能做过` |
| 改某日运动 | 是 · `:1621` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-update-day --date [--field --value]` | `老技能做过` |
| 删运动记录 | 是 · `:1631` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-delete --id` | `老技能做过` |
| 删某日运动 | 是 · `:1641` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-delete-day --date` | `老技能做过` |
| 批量删运动 | 是 · `:1651` | `receipt` | `crud_receipt.html` | `render_exercise_receipt.py --live-delete-range --from --to` | `老技能做过` |
| 看今日运动 | 是 · `:1661` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode records --today` | `老技能做过` |
| 看昨日运动 | 是 · `:1671` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode records --yesterday` | `老技能做过` |
| 看本周运动 | 是 · `:1681` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --week` | `老技能做过` |
| 看上周运动 | 是 · `:1691` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --last-week` | `老技能做过` |
| 看本月运动 | 是 · `:1701` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --month` | `老技能做过` |
| 看上月运动 | 是 · `:1711` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --last-month` | `老技能做过` |
| 看最近 7 天运动 | 是 · `:1721` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --days 7` | `老技能做过` |
| 看最近 30 天运动 | 是 · `:1731` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --days 30` | `老技能做过` |
| 看某段时间运动 | 是 · `:1741` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --from --to` | `老技能做过` |
| 看今日运动（vs 目标） | 是 · `:1751` | `result` | `exercise_goal_view.html` | `render_exercise_goal_view.py --period today` | `老技能做过`（老规则：目标未设时 AI 先问目标值再写库） |
| 看本周运动（vs 目标） | 是 · `:1761` | `result` | `exercise_goal_view.html` | `render_exercise_goal_view.py --period week`（周目标 ＝ 日目标 × 7） | `老技能做过`（同上） |
| 看运动记录（有备注） | 是 · `:1771` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode records --has-note` | `老技能做过`（新仓 routing 判「不执行」：`view.exercise` 无备注筛选参数） |
| 看运动记录（按力量筛选） | 是 · `:1781` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode records --category 力量` | `老技能做过` |
| 看运动记录（按有氧筛选） | 是 · `:1791` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode records --category 有氧` | `老技能做过` |
| 看最近 60 天运动 | 是 · `:1801` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --days 60` | `老技能做过` |
| 看最近 180 天运动 | 是 · `:1811` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --days 180 --downsample 3` | `老技能做过`（新仓无 `downsample` 实现，见 §5.6） |
| 看最近 365 天运动 | 是 · `:1821` | `result` | `exercise_summary.html` | `render_exercise_summary.py --mode summary --days 365 --downsample week` | `老技能做过`（同上） |
| 看运动类型分布 | 是 · `:1831` | `result` | `exercise_distribution.html` | `render_exercise_distribution.py --mode distribution` | `老技能做过`（新仓 routing 指向通用 `view.exercise`，专用键存在但未映射，见 §5.5） |
| 看力量训练总览 | 是 · `:1841` | `result` | `exercise_strength.html` | `render_exercise_strength.py` | `老技能做过` |
| 看有氧训练总览 | 是 · `:1851` | `result` | `exercise_cardio.html` | `render_exercise_cardio.py` | `老技能做过` |
| 看运动趋势 | 是 · `:1861` | `result` | `exercise_trend.html` | `render_exercise_trend.py [--days 30]` | `老技能做过`（同 §5.5） |
| 运动复盘（本周） | 是 · `:1871` | `result` | `exercise_recap.html` | `render_exercise_recap.py --period week` | `老技能做过`（同 §5.5） |
| 运动复盘（本月） | 是 · `:1881` | `result` | `exercise_recap.html` | `render_exercise_recap.py --period month` | `老技能做过`（同 §5.5） |
| 运动复盘（最近 90 天） | 是 · `:1891` | `result` | `exercise_recap.html` | `render_exercise_recap.py --period 90d` | `老技能做过`（同 §5.5） |
| 运动复盘（今年） | 是 · `:1901` | `result` | `exercise_recap.html` | `render_exercise_recap.py --period year` | `老技能做过`（同 §5.5） |
| 运动复盘（自定义时间） | 是 · `:1911` | `result` | `exercise_recap.html` | `render_exercise_recap.py --period range --from --to` | `老技能做过`（同 §5.5） |

**为什么 0 条是「必须多步交互」**：场景 04 的 39 条老 `data_source` 全是**单条命令**；引用的 8 个模板里没有一个是 wizard（老 `templates\` 下确有 `plan_builder_wizard.html` / `nutrition_label_wizard.html` / `body_*_wizard.html`，但没有运动 wizard）。唯一沾交互的是 §2 表中 2 条 vs 目标的「目标未设先问值」，那是渲染器**之外**的一句 AI 追问，老渲染器本身照常单命令出盘并输出空状态（`scripts\render_exercise_goal_view.py:12-14`）。

**为什么 0 条是「新架构划出去不做」**：新仓 `routing.ts` 里四条 OOS 理由（定时复盘／训记／营养表／落地）挂在 `routing.ts:167`、`:168`（营养表·场景 02）、`:354`–`:358`（落地／训记·场景 05）、`:580`、`:583`、`:591`（定时复盘·场景 10），**没有一条落在场景 04**。题面提到的 `sync_plan.py`／`xunji_bridge`／`render_cron_setup.py` 服务于老技能「健身计划」类别（新老都另有 32 词，老表行 `_triggers.py:1991`–`:2151`+，含 定训练计划／落地训练／同步到训记），**不属于场景 04**。

**为什么 0 条是「老技能也没做过」**：39/39 都在老表，且都有 `output_type` + `html_template` + 存在的实现脚本。

---

## 3. 模板引用统计（8 个模板）

老表 39 条的 `html_template` 取值与新仓 39 条的 `html_template` 取值**逐条相同**，分布如下（两仓一致）：

| 模板名 | 存在? | 字节 | 场景 04 引用数 | 引用它的唤醒词 |
| --- | --- | --- | --- | --- |
| `crud_receipt.html` | 是 | 26279 | **13** | 记运动 / 记运动（含备注）/ 记力量训练 / 记有氧运动 / 记日常活动 / 补记运动 / 批量补记运动 / 复制昨日运动 / 改运动记录 / 改某日运动 / 删运动记录 / 删某日运动 / 批量删运动 |
| `exercise_summary.html` | 是 | 11856 | **15** | 看今日运动 / 看昨日运动 / 看本周运动 / 看上周运动 / 看本月运动 / 看上月运动 / 看最近 7 天运动 / 看最近 30 天运动 / 看某段时间运动 / 看运动记录（有备注）/ 看运动记录（按力量筛选）/ 看运动记录（按有氧筛选）/ 看最近 60 天运动 / 看最近 180 天运动 / 看最近 365 天运动 |
| `exercise_recap.html` | 是 | 6652 | **5** | 运动复盘（本周）/ 运动复盘（本月）/ 运动复盘（最近 90 天）/ 运动复盘（今年）/ 运动复盘（自定义时间） |
| `exercise_goal_view.html` | 是 | 7662 | **2** | 看今日运动（vs 目标）/ 看本周运动（vs 目标） |
| `exercise_distribution.html` | 是 | 13875 | **1** | 看运动类型分布 |
| `exercise_strength.html` | 是 | 6302 | **1** | 看力量训练总览 |
| `exercise_cardio.html` | 是 | 5003 | **1** | 看有氧训练总览 |
| `exercise_trend.html` | 是 | 7117 | **1** | 看运动趋势 |

合计 13+15+5+2+1+1+1+1 = **39**。

### 结构描述（各 1–3 行；id 取自模板内 `id="…"`）

- **`exercise_summary.html`**（11856 B / 202 行）——「运动报表」双模式壳（`modeBadge` 显示 mode，`h1Title` "🏃 运动报表"）。含 4 格 KPI 行（`k1`–`k4`）、可折叠图表区（`chartSection`/`chart`/`chartTitle`/`legend`）、明细表（`tableTitle`/`tableHead`/`tableBody`）、`srcLine` 数据来源行；`payload` 脚本块注入数据，`<!--SHARED-CSS-->` / `<!--SHARED-HELPERS-->` 占位。
- **`crud_receipt.html`**（26279 B / 435 行）——13 条写操作共用的「操作回执」：`recordId` 记录号、`kpiGrid` 今日累计、`diffCard` 字段变更（改前/改后）、`ctxCard` 记录上下文、`itemsCard` 批量/复制明细（标题 `itemsTitle`）、`summaryLine` 一句话、`undoBtn`「↩ 撤销」按钮、`actionbar-zone` 操作条。
- **`exercise_recap.html`**（6652 B / 121 行）——运动复盘盘：`kpiGrid` KPI + `distBox`「🧩 类型分布」+ `topBox`「🔥 高频运动」+ `trendBox`「📈 每日消耗趋势」+ `oneLine`。
- **`exercise_goal_view.html`**（7662 B / 123 行）——运动目标达成视图：环形进度（`ringBar` 环形 donut + `ringPct` 百分比）+ `kGoal`/`kActual`/`kGap`/`kRange` 四数 + `verdict` 达标/未达标胶囊 + `summaryLine` 一句话 + `emptyBox`（未设目标时输出空状态，由 AI 引导首问）。
- **`exercise_distribution.html`**（13875 B / 238 行）——运动分布报告：`modeBadge` 双模式（distribution / contribution）+ `k1`–`k4` KPI + 双饼图（`pie1`「按类型占比」/`pie2`「按热量分布」+ `legend1`/`legend2`）+ `tableTitle` 运动明细表 + `srcLine`。
- **`exercise_strength.html`**（6302 B / 120 行）——力量训练总览：`kpiGrid` + 「📋 按动作聚合」表（`tHead`/`tBody`）+ `trendBox`「📈 重量轨迹（最近 10 个训练日 · 单侧口径 Σkg×次数）」。
- **`exercise_cardio.html`**（5003 B / 90 行）——有氧训练总览：`kpiGrid` + 「📋 按类型聚合」表（`tHead`/`tBody`）；**无图表区**（全 8 个里最小）。
- **`exercise_trend.html`**（7117 B / 130 行）——运动趋势：`kpiGrid` + `calChart`「🔥 每日消耗（卡）」+ `weekChart`「🗓️ 每周运动频次（次）」两张折线。

### `goal_config.html` 核实

**确认：老 `templates\` 下没有 `goal_config.html`**，只有 `goal_config_nutrition.html`（23191 B）与 `goal_config_water.html`（14012 B）。而老表 `_triggers.py` 在 4 处把 `html_template` 写成 `templates/goal_config.html`：`:2247`（定营养目标）、`:2297`（定饮水目标）、`:2427`（改营养目标）、`:2447`（改饮水目标）——**这是老表里的悬空引用**（同一条目的 `data_source` 指向 `render_goal_config.py`，而该脚本实际加载的是那两个带后缀的模板：`scripts\render_goal_config.py:31-32`；文档也是这么写的：`SKILL.md:180-181`）。场景 04 的 8 个模板**没有**这类悬空引用（名称与磁盘文件逐一对上）。此条不属于场景 04，仅作为对账口径的对照。

---

## 4. 老技能条目结构（原文摘录，供核对）

老表的「运动」块每条是 10 行一个 dict，字段与新版 13 字段 scene 形状一致，但 `cli` / `data_source` 存的是**可直接跑的 python 相对路径**（新仓换成了 `calorie-cmd-read …`）。以第一条为例（`_triggers.py:1530-1539`，原文照抄，未删节）：

```python
    {
            'category': '运动',     'wake_word': '记运动',     'desc': '记录一次运动(类型/时长/消耗/时间)',
        'main_prompt': {
        'cli': 'python scripts/render_exercise_receipt.py --live-add --type <T> --calories <C> [--minutes <M>] --chain "1.解析→2.写库→3.回执"', 'text': '请你加载技能 卡路里,执行唤醒词「记运动」。\n\n我做了运动,请记下来。交付 HTML 时,文字只回复精简而全面概括的信息,文字不允许超过三句话。\n\n运动类型:____\n时长(分钟):____\n热量(卡,选填):____'},
        'fill_hints': [],
            'variants': [],
            'key': 'exercise_add', 'name': '记运动', 'subfunction': '记运动', 'output_type': 'receipt',
            'html_template': 'templates/crud_receipt.html', 'data_source': 'python scripts/render_exercise_receipt.py --live-add --type <T> --calories <C> [--minutes <M>] --chain "1.解析→2.写库→3.回执"', 'prompt_template': '请你加载技能 卡路里,执行唤醒词「记运动」。…',
            'user_intent': '记录一次运动(类型/时长/消耗/时间)', 'data_fields': ["exercise_type", "duration_minutes", "calories_burned", "time", "is_estimated"],
            'depends_on_external': False, 'order': 0},
```

（只有 `prompt_template` 与 `name` 两处为版面宽度用 `…` 截断，其余字段逐字原样；`prompt_template` 与同一行的 `main_prompt.text` 是同一个字符串。）

**字段读法**（据此填表）：`wake_word`（权威词）、`desc`（一句话）、`main_prompt.cli` + `data_source`（**老实现文件与实参**）、`key`、`subfunction`、`output_type`（`receipt`/`result`）、`html_template`（**模板名**）、`user_intent`、`data_fields`、`order`（`subfunction` 内计数）。文件头 `_triggers.py:13-16` 明确「`wake_word` 唯一且非空……本文件是触发词权威源」，`:38` 记「运动 39」。

**各实参确实存在**（脚本源码里的 `add_argument`，逐条可查）：

- `scripts\render_exercise_receipt.py`：`--live-add` / `--live-add-strength` / `--live-add-daily` / `--live-backfill` / `--live-batch-add` / `--live-copy` / `--live-update` / `--live-update-day` / `--live-delete` / `--live-delete-day` / `--live-delete-range`，加 `--type --calories --minutes --note --category --distance --avg-hr --max-hr --steps --period --sets --load --reps --id --items --target --from --to --field --value --chain`。
- `scripts\render_exercise_summary.py`：`--today --yesterday --week --last-week --month --last-month --days --from --to --mode{records,summary} --category{有氧,力量,柔韧,日常} --has-note --type --downsample{3,week} --chain`（文件头 `:1-25` 逐词列了对应 17 个唤醒词）。
- `scripts\render_exercise_goal_view.py`：`--period today|week`（文件头 `:3-15`）。
- `scripts\render_exercise_recap.py`：`--period week|month|90d|year|range`（文件头 `:3-15`）。
- `scripts\render_exercise_distribution.py`：`--mode distribution|contribution`。`render_exercise_strength.py` / `render_exercise_cardio.py` / `render_exercise_trend.py [--days N]`：无必填参。

`SKILL.md` 侧同源：模板→唤醒词映射表 `SKILL.md:168`–`:173`；速查表 `SKILL.md:572`（看运动记录（有备注）→ `--mode records --has-note`）、`:583`–`:584`、`:592`。

---

## 5. 意外与不确定项

### 5.1 场景 04 的词，老表一条不缺，而且与新仓逐字同序

39/39 命中，每词**恰好 1 次**（无重复）。老 39 条的 `wake_word` 序列与 `scene-04-exercise.ts:5-43` 的序列**逐字逐序相同**，`key` 也 39/39 相同（`exercise_add` … `exercise_recap_range`）。老表里没有「同唤醒词多 key」的 3 卡合并情形（那是身材照片：`_triggers.py:31-32`）。

### 5.2 `routing.ts` 里有 6 个挂在场景 04、但两仓都不存在的词

`routing.ts` 中 `scene: '04'` 的记录共 **45** 条，其中 **39** 条与 `scene-04-exercise.ts` 对得上，另 **6** 条是：

| 词 | routing.ts 行 | 映射到 | 在 `scene-04-exercise.ts`? | 在老表? |
| --- | --- | --- | --- | --- |
| 看运动目标 | `:644` | `calorie.view.exercise-goal` | 无 | 无（但老脚本 `render_exercise_goal_view.py:18` 的 `COMMAND_CN = '看运动目标'` 是这个中文串） |
| 看力量总览 | `:651` | `calorie.view.exercise-strength` | 无 | 无 |
| 看有氧总览 | `:652` | `calorie.view.exercise-cardio` | 无 | 无 |
| 看运动分类占比 | `:653` | `calorie.view.exercise-distribution` | 无 | 无 |
| 看运动复盘 | `:654` | `calorie.view.exercise-recap` | 无 | 无 |
| 看运动消耗趋势 | `:656` | `calorie.view.exercise-trend` | 无 | 无 |

这 6 个词**只出现在 `routing.ts`**：不在任何新仓 `scene-*.ts`，也不在老表（逐词查过）。它们正是唯一被映射到 4 个「运动专键」的词。这不影响 39 词的归属判断，但设计时若要「按 39 词核对键」，会在 routing 里读到 45 条编号 04。

### 5.3 `routing.ts` 自身对「运动类型分布／趋势／复盘」指向通用键

39 词中 `routing.ts:324`（看运动类型分布）、`:327`（看运动趋势）、`:328`–`:332`（运动复盘 ×5）映射的是 **`calorie.view.exercise`**（通用运动总览），而**专用键其实存在**：`keys.ts:111` `calorie.view.exercise-distribution`、`:112` `-recap`、`:114` `-trend`，且 `cmd_read.ts:420 / :429 / :448` 都有各自 case 与各自的文档构建器（`sportPortDocs.ts:214` `buildDistributionDoc` / `:288` `buildRecapDoc` / `:432` `buildTrendDoc`）。专键被映射给了 §5.2 那 6 个词。**只是事实罗列，不代表判定**；按题述纪律，routing 标注不可作为「能不能跑」的依据。

### 5.4 唯一被 routing 判「不执行」的词：看运动记录（有备注）

`routing.ts:318` 把「看运动记录（有备注）」标成 `kind: 'non-exec'`、`bucket: 'legacy-chain'`，理由 `NON_EXEC_REASONS.noNoteFilter`（`routing.ts:78-79`：「95 键无『备注』筛选参数」）。核对：新仓 `calorie.view.exercise` 取参只走 `defaultRange`（`cmd_read.ts:214-231`，只认 `start`/`end`/`date`/`today`），handler 直接 `buildExerciseView` + `buildExerciseDoc`（`cmd_read.ts:391-399`），**确实没有备注筛选参数**。而老技能这一条**是真做过**：`render_exercise_summary.py --mode records --has-note`（实参在脚本里，见 §4；`SKILL.md:572` 亦然）。→ 这是场景 04 里唯一「老技能做过、新仓自己说不做」的词。

### 5.5 降采样：老技能有，新仓代码里没有

老表 180/365 天两条明确带 `--downsample 3` / `--downsample week`（`_triggers.py:1811`、`:1821`），模板 180/365 的词条描述也写着「(每 3 天降采样)」「(每周降采样)」。新仓 `packages/skill-calorie/src` 下**没有任何 `downsample` 实现**（全仓搜 `downsample` 只命中提示词文本：`scene-04-exercise.ts:33-34`、`wake-assets.ts:1860`/`:1870` 等）。`view.exercise` 只吃一个日期段。同类情形在场景 03 体重曲线里也存在（`scene-03-weight.ts:28-31`）。

### 5.6 新仓存在第二份 39 词拷贝：`wake-assets.ts`

`packages/skill-calorie/src/triggers/wake-assets.ts`（255362 B）是 HELP 中心的资产数据，含 `id/title/wake_word/status/prompt_template/types`，共 **436** 个 `wake_word`（与老表 `_triggers.py` 的 436 相同），场景 04 的 39 词在其中**全部命中**（分组 id `exercise`，子组 `exercise_1`…；例：`wake-assets.ts:1566` 记运动）。**该文件的 `status` 字段在全部 436 条上都是空字符串**，因此它不带「跑不通」一类标注；新仓关于场景 04 的可执行性标注只在 `routing.ts` 里（38 exec / 1 non-exec）。

### 5.7 老表文件头自述的「未完成」状态

`_triggers.py:23-39` 注明场景体系重构 Phase 2 进行中、分类逐个同步，并说「已确认分类：……运动 39……」。`:36-37` 指开发期数据在 `.scratch/scene_data/NN-分类.json`（13 字段、`check_scene_data.py` 校验）。已核对 `04-运动.json` 确为 39 条，与 `_triggers.py:1531-1911` 的 39 条字段一致 → 场景 04 是**已同步完成**的分类，不存在「表里是旧版运行时数据」的风险。

### 5.8 老实现文件与老模板的对应（一句话用途）

| 文件 | 大小 | 一句话 |
| --- | --- | --- |
| `scripts\render_exercise_receipt.py` | 24072 B | 13 个写操作（记/补记/批量补记/复制/改/删）的运动回执渲染器，`--live-*` 一整族开关，出 `crud_receipt.html`；文件头 `:3-19` 逐词列了对应 13 词 |
| `scripts\render_exercise_summary.py` | 15837 B | 17 个「看类」运动报表一体渲染器（`--mode records|summary` ＋ 时间窗/筛选/降采样），出 `exercise_summary.html` |
| `scripts\render_exercise_goal_view.py` | 6365 B | 2 词的运动目标达成视图（`--period today|week`），出 `exercise_goal_view.html`；未设目标时输出空状态 |
| `scripts\render_exercise_distribution.py` | 7551 B | 运动分布/贡献双模式报告（也服务「查运动分布／查运动贡献」），出 `exercise_distribution.html` |
| `scripts\render_exercise_strength.py` | 5211 B | 力量训练按动作聚合总览（Σkg×次数口径），出 `exercise_strength.html` |
| `scripts\render_exercise_cardio.py` | 5031 B | 有氧按类型聚合总览（配速＝时长/距离），出 `exercise_cardio.html` |
| `scripts\render_exercise_trend.py` | 4762 B | 运动趋势折线（默认最近 30 天，`--days` 可调），出 `exercise_trend.html` |
| `scripts\render_exercise_recap.py` | 6881 B | 5 词的运动复盘（`--period week/month/90d/year/range`），出 `exercise_recap.html` |
| `scripts\exercise_tracker.py` | 36751 B | 运动读写数据层（被上面若干渲染器共用） |
| `scripts\exercise.py` | 12647 B | 运动域命令行入口／域逻辑 |
| `scripts\exercise_review.py` / `render_exercise_review_html.py` | 3200 B / 5497 B | 训练计划 vs 实绩复盘（后者 subprocess 调前者 `--format json`，出 `exercise_review.html`）——**属场景 05 健身计划**（老表 `_triggers.py:2171`/`:2181`/`:2191` 的 计划复盘（本周/本月/全部）），不在场景 04 的 39 词内 |

题面提到的 `sync_plan.py`（14381 B）、`xunji_bridge.py`（719 B）＋`xunji_bridge\`（目录）、`render_cron_setup.py`（1691 B）确实在 `scripts\` 下存在，但**服务的是老技能「健身计划」类别与定时复盘**（老表 `_triggers.py:1971`+ 的 `'category': '健身计划'`，含 同步到训记 `:2151`、落地训练 `:2121`），**不是**场景 04 的实现文件；场景 04 只落在 `render_exercise_receipt.py` ＋ `render_exercise_summary.py` ＋ 6 个 `render_exercise_{goal_view,distribution,strength,cardio,trend,recap}.py`。

### 5.9 新仓已声明移植了 6 个运动模板

`packages/skill-calorie/src/render/sportPortDocs.ts:1-13` 的头注释写明 #111 移植 6 项：`exercise_strength / exercise_cardio / exercise_distribution / exercise_recap / exercise_review / exercise_trend`，并明说 **process_progress（落地/训记二期，O3/oosLanding/oosXunji）「不碰」**。

- 这 6 项里，**5 项**（strength / cardio / distribution / recap / trend）正是场景 04 那 8 个模板中的 5 个；第 6 项 `exercise_review` 服务的是场景 05 的「计划复盘」，**不在场景 04 的 8 个模板里**。
- 场景 04 的另外 3 个模板——`exercise_summary.html` / `crud_receipt.html` / `exercise_goal_view.html`——**不在**该移植清单内。
- 新仓对应的键：`keys.ts:109-114` 六个运动专键；13 个写词由 `calorie.exercise.add/update/remove` 承接（`write.ts:606-705`）；`exercise_summary` / `exercise_goal_view` 的对应物是通用键 `calorie.view.exercise`（`cmd_read.ts:391-399`）与 `calorie.view.exercise-goal`（`cmd_read.ts:976`）。

**只是记录现状，不构成判定。**
