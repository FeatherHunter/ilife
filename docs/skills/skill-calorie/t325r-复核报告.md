# t325r 独立复核报告（被审 9757617）

票：#325。被审提交：9757617（限定 3 路径，已 push）。
复核席：#325r（独立对抗式复核，只复核这一张票）。

## 0 开工自复算

- 开工 `HEAD == origin/master == 9757617`，`git status --porcelain -uall` 无受跟踪改动（干净）。
- 主树 `packages/skill-calorie/src/workout/commands.ts` 长度 2651 字节，与被审证据一致。
- 收尾时 `HEAD == origin/master == e9d499c`（别席 330r 仅加 `docs/skills/skill-calorie/t330r-复核报告.md` 一件文档，无代码改动；本席主树复跑在该位进行，代码面与 9757617 一致）。
- 本席纪律：只读仓内文件（唯一可写本报告）；终端走 better sidebar 终端（标题 t325r-复核）；未用 `--amend/--force/rebase`；未碰 issue 票面；大输出落 `.scratch/t325r/`。

## 1 改动面审计（被审 9757617）

- `git show 9757617 --stat`：恰 3 件，314 增 30 删。
  - `docs/skills/skill-calorie/t325-生成检查假绿-证据.md` 71 行新增。
  - `packages/skill-calorie/scripts/gen-cli.mjs` 267 行变更（26905 -> 36931 字节）。
  - `packages/skill-calorie/scripts/gen-routes.mjs` 6 行（仅 `loadDecls` 加 `export` 与注释）。
- 无越界：`git diff 9757617~1 9757617 --name-only` 恰为上述 3 件。
- `packages/skill-calorie/src/` 零改动（`--stat` 为空）。
- 冻结面未动：`test/calorie-routing-81.test.mjs`、`packages/skill-calorie/src/triggers/` 均为空。
- 判官未动：`tooling/` 为空。
- 印记现为 v2（`packages/skill-calorie/dist/.gen-inputs.json` version 2），旧 STALE/CHECK 口径保留（见 §3）。

## 2 独立复现原攻击（隔离副本，只在副本里做，主树未改）

做法：`git archive HEAD~1` 解到 `.scratch/t325r/iso-old`（修前，STAMP v1），`git archive HEAD` 解到 `.scratch/t325r/iso-new`（修后，STAMP v2）；`node_modules` 用 junction 指回主树；构建只编 `packages/skill-calorie`。

基线（双副本）：`tsc -b --force` exit 0 → `--stamp` exit 0（印记 41 件）→ `--check` exit 0（`GEN-CHECK PASS：键 101（写 35 + 读 66）`）。

攻击链（双副本同步骤）：改 `src/workout/commands.ts` 标题 `训练计划看`→`训练计划着`（等长，2651 字节不变）→ 刷回原 mtime（`MTIME_KEPT`，old 9:43:28 / new 9:44:31）→ `tsc -b`（增量）exit 0 但 `dist/workout/commands.js` 仍含 `看`不含`着`（`S1_DIST_SKIPPED=true`）→ `--stamp` exit 0 → `--check`：

- 修前（iso-old）：exit 0，`GEN-CHECK PASS`（假绿成立，与被审自报一致）。
  - 日志：`.scratch/t325r/33-old-check-attack.log`。
- 修后（iso-new）：exit 1，`GEN-PAIR FAIL：声明源与编译产物对不上（1 件）`，点名 `packages/skill-calorie/src/workout/commands.ts`，`源事实 3c99ba38015f… != 编译事实 c88651d5a4e5…`（与被审证据哈希逐字一致）。
  - 日志：`.scratch/t325r/43-new-check-attack.log`。

结论：修前逻辑下攻击确为 exit 0；修后 exit 1 且点名被改文件。

## 3 四样例（修后 iso-new，逐条记 exit 与关键行）

| 样例 | 做法 | 期望 | 实测 |
| --- | --- | --- | --- |
| S1 真改动必红 | §2 攻击链（改标题＋保时间戳＋构建＋stamp＋check） | 红 | 红 exit 1，`GEN-PAIR FAIL（1 件）` 点名 workout/commands.ts |
| S2 纯时间戳必绿 | 只刷 `src/weight/commands.ts` mtime（9:44:31→9:48:56），内容哈希不变 `44221B88…2E82` | 绿 | 绿 exit 0，`GEN-CHECK PASS 101`（`.scratch/t325r/50-S2-check.log`） |
| S3 正常生成必绿 | 跑 `gen` 写盘再 `--check` | 绿 | 绿 gen exit 0（`gen 完成：键 101`）＋ check exit 0；五件哈希写前后一致且幂等（keys `4db43569…`／registry `5c81bfbd…`／combos `ca17d2c3…`／build-help `9ed0c9ad…`／routes.generated `e9ea9935…`，二次 gen 同值） |
| S4 删记录必红 | 删 `src/diet/commands.ts` 第 38 行（`calorie.diet.add`），不构建直接 check | 红 | 红 exit 1，`GEN-STALE FAIL`，`diet/commands.ts 印记 0b67456ff9a2… != 现 64bf11802e79…`（与被审哈希一致）；恢复后 exit 0 |

- “纯时间戳改动不判红”成立：增量构建的正常提速路径没被误杀（S2）。
- “删记录判红”成立：S3-3 那类缺口被这版覆盖（S4 走旧 `GEN-STALE` 口径，修后仍红；恢复后回绿）。

## 4 主树复跑（持锁 `run-locked --ticket 325r --lock-dir .scratch/locks-land --stale-minutes 30`，direct node）

- `node node_modules/typescript/bin/tsc -b`：exit 0，runId `2b3e14b8-984d-4019-9f72-94288e1bc973`（`.scratch/t325r/80-main-tsc.log`）。
- `node packages/skill-calorie/scripts/gen-cli.mjs --stamp`：exit 0，印记 v2、41 件，runId `c132800f-24f7-4d72-93d7-8b1fbe2df235`。
- `node packages/skill-calorie/scripts/gen-cli.mjs --check`：exit 0，`GEN-CHECK PASS：键 101（写 35 + 读 66），能力 10 个，声明 101 条，未搬迁清单 0 条`，runId `cab956a3-7ae8-448c-aa3d-ad13aa7bf352`。
- `node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"`：exit 0，tests 1492 / pass 1492 / fail 0 / suites 160，runId `38caec4f-8bb1-4b82-8412-01646957e5a5`。
- `node --test test/calorie-routing-81.test.mjs`：exit 0，tests 8 / pass 8 / fail 0，runId `58bff11b-6b98-426a-8ff3-21bb46bb4b98`。
- `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`：exit 0，`P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`，`UNACCOUNTED=0`，runId `dc78a183-6f33-4482-8643-9e0e438374dc`。

全绿，与被审自报一致（本席全部自己跑，不采信被审文本）。

## 5 S 清单与打分（100 制，PASS 要求 S1=0 且 S2=0）

- S1（假绿／漏报，关键）：0。
  - 复现命令（修前假绿）：隔离副本 `iso-old` 内改标题＋刷回 mtime＋`tsc -b`＋`--stamp`＋`--check`，实测 exit 0（本报告 §2）；修后同链 exit 1 且点名，漏报已堵。
  - 复现命令（新文件／汇总件边界）：`routeDeclarationSources` 新文件无印记对照时 `GEN-STALE`（缺 dist）或下游 `GEN-CHECK` 兜底；汇总件无事实（展开式拼接），现场跳过但印记仍绑定字节——逐项核 `gen-cli.mjs:551 pairMismatches` 与 `gen-routes.mjs:48 routeDeclarationSources`，无可利用的事实级绕过。
- S2（误报／正常路径被杀，关键）：0。
  - 复现命令：`iso-new` 内只 touch `src/weight/commands.ts`（哈希不变）后 `--check`，实测 exit 0（§3 S2）；正常 `gen`＋`check` 双绿且幂等（§3 S3）；主树全量／路由／探针全绿（§4）。
- S3（残余／越界／文档）：0。
  - 复现命令：`git diff 9757617~1 9757617 --stat/--name-only` 恰 3 件；`src/`、`test/calorie-routing-81.test.mjs`、`src/triggers/`、`tooling/` 均为空；`gen-routes.mjs` 仅导出词。
  - S3-3 覆盖：删 `diet/commands.ts` 首条后 `--check` exit 1 `GEN-STALE FAIL`（§3 S4），缺口已被覆盖，不记 S3。
  - 观察项（不计分）：`docs/agents/命令登记纪律.md` 仍是 v1 口径（被审已在证据 §5 声明留给纪律维护者，本票不限定路径是对的）；路由汇总件现场跳过为有意保留边界（事实住分片）。

总分：100，判定 PASS（S1=0 且 S2=0）。

## 6 提交与未做项

- 本报告限定路径提交（仅本文件），push 后补 sha（见回执）。
- 未做项：未碰 issue 票面、未写完成度、未 close；未改冻结面／判官／能力目录源码；未动别席在飞文件（收尾时 `t330r-复核报告.md` 为已提交态，只读）；隔离副本与日志全在 `.scratch/t325r/`（gitignored，不进提交）。
