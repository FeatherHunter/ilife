# t365 · 身体细节七条写词的结果型整页（#365）

- 归属票：`#365`（MAP #158 场景 08 子票）；前置 `#363`／`#364` **均已关**（#364 交来「删前快照」数据面与按 id 复取口）。
- 基准：`docs/skills/skill-calorie/t395-融合基准.md`（§六-C 回执页骨架、§七 最大缺口专节、§四 裁定 2）。
- 老正本（**只读参照**）：`D:\2Study\StudyNotes\SKILLS\卡路里\templates\crud_receipt.html`（465 行／26279 字节）。
- 起点：`HEAD=9608f588c4547a137bd317b215a7982cf2e976f1`（短 `9608f58`）。
- 行号口径：本仓件写「文件:行号」，老技能件写「`:行号`」，均指本件落笔时的内容。

## 一、判定

**七条写词的产物六项读数全绿，字段值逐字等于脚本查库值，防回显与「端口退回片段」两条负向对照均成立。**
唯一未收口的是一条**跨票的旧针**（见 §六），它钉的正是本票要换掉的那个片段形状，属本票的**预期后果**，
处置权在 MAP（本票写集硬限 `test/t365-*`，不许动他人测试件）。

## 二、目标与落法

七条写词 → 四条命令（命令名以 `src/body/commands.ts` 注册表为准，不按票面抄唤醒词）：

| 唤醒词 | 命令键 |
|---|---|
| 记体脂（皮褶钳）／记体脂（外部测量）／补记体脂 | `calorie.body.composition-add` |
| 记围度／补记围度 | `calorie.body.measure-add` |
| 删体脂 | `calorie.body.composition-remove` |
| 删围度 | `calorie.body.measure-remove` |

接入点：`src/cli/write.ts` 的整页装配链加**第五个装配口**（形状照 `workoutReceiptDoc`／`weightReceiptDoc`）：

```
profileReceiptDoc(...) ?? dietReceiptDoc(...) ?? weightReceiptDoc(...)
  ?? workoutReceiptDoc(...) ?? bodyReceiptDoc(key, params, receipt, db) ?? res.html
```

接入前七条落在 `src/shared/writeParts.ts:83` 的 `receiptHtml()`（只有 `h1` ＋ 一行摘要 ＋ items）。

老正本逐条落点（`src/body/receipt.ts` 件头注释亦有同表）：

| 老正本出处 | 落点 |
|---|---|
| `:26-29` id 卡三态 ＋ `:201`（`op` → class） | `OP_BADGE`（create→ok／update→warn／delete→danger）补在共用件 `statusCard` 上，**三态由 `receipt.op` 驱动** |
| `:61-69` diff 三列 ＋ `:276-291` 中文标签 ＋ `:315-322` 装配 | 一律走共用件 `renderChangeRows`；`arrow:false` **仍占箭位**（与老 `:344` 的 `visibility:hidden` 同一手法，左右两栏不塌） |
| `:328-350` 删除前快照 | `deleteSnapshotRows()`：**字段与序**取 #364 的 `items[0].detail`（`标签 值` 按「、」切、首空格分列），**值**改读库内现值（软删不改内容，两者恒等） |
| `:414-429` 撤销按钮 | `undoBlock()`：给了撤销指令才出可复制指令块；本域四条写命令都不传 ⇒ 真出口产物里连「撤销」二字都没有 |
| M5 自证（新有老无） | `m5Disclosure()`：影响行数／影响行数来源／记录号来源／回执契约版本／写入时间 |
| 复制区（新有老无） | `shared/copyArea.ts:116` 的 `copyArea`（数据位恒出三格式菜单，与老 `:98` 的 `.fmt-menu` 同形） |

**裁定 2** 的两半分开落：可见文本缺值一律 `—`（`cell()`）；**复制数据里缺项整项缺位**（`payloadLines()` 只收有值的项，
连标签一起省，载荷里不含 `—`）。复制数据的 `message` **不取 `receipt.summary`**——那是含 #363 冲突段与 `—` 占位的人类话，
进载荷会违反裁定 2；本件另拼 `payloadMessage()`（`本次<增删>体脂/围度记录 #id ｜ 标签 值 …`）。

## 三、机器证据

### 3.1 七条写词真出口六项读数（改后，全绿）

`GATE-RUN runId=c642b81c-dbd1-4d16-8ee4-d8430025c6ed cmd="pwsh -NoProfile -Command \"npx tsc -b packages/skill-calorie; node .scratch/t365/probe.mjs; node --test packages/skill-calorie/test/t365-七条写词整页.test.mjs; node packages/skill-calorie/scripts/check-warning-line.mjs\"" exit=0`

```
PROBE 记体脂（皮褶钳） exit=0 | doctype=true charset=true css=36523B(cssOk=true) ext=[] pathOk=true | visibleLen=616
PROBE 记体脂（外部测量） exit=0 | doctype=true charset=true css=36523B(cssOk=true) ext=[] pathOk=true | visibleLen=543
PROBE 记围度           exit=0 | doctype=true charset=true css=36523B(cssOk=true) ext=[] pathOk=true | visibleLen=573
PROBE 补记体脂         exit=0 | doctype=true charset=true css=36523B(cssOk=true) ext=[] pathOk=true | visibleLen=769
PROBE 补记围度         exit=0 | doctype=true charset=true css=36523B(cssOk=true) ext=[] pathOk=true | visibleLen=816
PROBE 删体脂           exit=0 | doctype=true charset=true css=36523B(cssOk=true) ext=[] pathOk=true | visibleLen=616
PROBE 删围度           exit=0 | doctype=true charset=true css=36523B(cssOk=true) ext=[] pathOk=true | visibleLen=622
```

同一 runId 内：本票测试 `tests 10 / pass 10 / fail 0`；告警线门 `RESULT: 60/60` ＋ `PASS: 告警线台账齐全且与实况一致`
（该门是他席在途件的活快照，本票提交后复跑落到 `56/60`，四件全属他席 —— 归属见 §六-3）。

### 3.2 变异 M1：把整页端口改回片段 → 六项读数必红

改坏点：`src/cli/write.ts` 装配链摘掉 `?? bodyReceiptDoc(key, params, receipt, db)`（其余一字不动）。

`GATE-RUN runId=2f486d10-c3eb-4d2c-8a36-c524d186e225 cmd="pwsh -NoProfile -Command \"npx tsc -b packages/skill-calorie; node .scratch/t365/probe.mjs\"" exit=0`

```
PROBE 记体脂（皮褶钳） exit=0 | doctype=false charset=false css=0B(cssOk=false) ext=[] pathOk=true | visibleLen=165
PROBE 记体脂（外部测量） exit=0 | doctype=false charset=false css=0B(cssOk=false) ext=[] pathOk=true | visibleLen=78
PROBE 记围度           exit=0 | doctype=false charset=false css=0B(cssOk=false) ext=[] pathOk=true | visibleLen=96
PROBE 补记体脂         exit=0 | doctype=false charset=false css=0B(cssOk=false) ext=[] pathOk=true | visibleLen=280
PROBE 补记围度         exit=0 | doctype=false charset=false css=0B(cssOk=false) ext=[] pathOk=true | visibleLen=294
PROBE 删体脂           exit=0 | doctype=false charset=false css=0B(cssOk=false) ext=[] pathOk=true | visibleLen=218
PROBE 删围度           exit=0 | doctype=false charset=false css=0B(cssOk=false) ext=[] pathOk=true | visibleLen=213
```

**改坏必红／还原必绿两行读数**：

```
M1   七条 doctype=false charset=false css=0B(cssOk=false)  pass=0/7
M1⁻¹ 七条 doctype=true  charset=true  css=36523B(cssOk=true) pass=7/7
```

还原后 `src/cli/write.ts` sha256 前 16 位回到改坏前同值 `dd11577aa861e55e`（改坏／还原各自的哈希都留在 §五 的收尾对账里）。
改坏读数里的 `visibleLen`（165/78/96/280/294/218/213）与本票**改动前的基线读数逐字相同**，即变异确实退回了本票进场时那一版片段。

### 3.3 期望值来源（不是新实现输出）

- **脚本查库**：`SELECT date, source, body_fat_pct, … FROM body_composition WHERE id = ?`（另开 `node:sqlite` 句柄，
  按 `receipt.recordId` 读整行），逐格比页内 `renderChangeRows` 的 `[标签, 值]`。
- **手写样例**：`SEED_C`（3 条体脂：有备注／无 7 点／有 7 点）＋ `SEED_M`（2 条围度：两项／13 项全填），由 SQL 直插。
- **唯一来源**：13 部位 `MEASUREMENT_ZH`、7 点站名 `CALIPER_SITE_LABELS`、来源 `SOURCE_LABELS`；
  测试里各写一份冻结副本与实现引的表**逐字对账**，不抄第二份真名表。
- **来源列口径**：页内值写 `SOURCE_LABELS[库值]`（中文名，与 #364 的删前快照同口径）；其余各格＝库内原始值文本。
  故测试对来源格断言 `SOURCE_LABELS[dbValue]`，对其余各格断言**逐字等于查库值**。

### 3.4 防回显（负向之一）

跑之前**不经命令直改库一个字段**：

- 删类：种子 `body_fat_pct=18.5` → `UPDATE … SET body_fat_pct=33.3` → 跑「删体脂」→ 删前快照那格是 **33.3**，页面不再出现 18.5。
- 记类：`UPDATE body_measurements SET waist_cm=111` → 跑「补记围度」→「同日已有记录 #1」那格是 **111**（不是种子 86）。

测试行：`T365-ANTI-ECHO 删体脂快照体脂率=33.3 同日已有腰围=111`。

### 3.5 不许动

- 写命令登记形状：`BODY_COMMANDS` 四条写命令 `shape` 仍是 `receipt`（测试逐条断言）。
- 另外 32 条写命令：体重键仍走体重整页（`calorie.weight.remove` 产物含「体重」、不含「身体细节 · 写后回执」）；
  链上另外四口（profile／diet／weight／workout）在 `src/cli/write.ts` 里逐条断言仍在、位次不变。
- `src/body/receipt.ts` LF=**324**（＜350，未触发超线第四步）；`src/body/index.ts` LF=39；`src/cli/write.ts` LF=98。
- 告警线门：本票**一个台账行都没改**（`--sync --dry` 计划里无本票任何件）；见 §六-3 的归属说明。

## 四、逐条路径

| 路径 | 动作 |
|---|---|
| `packages/skill-calorie/src/body/receipt.ts` | **新建**（第五个装配口本体，LF 324） |
| `packages/skill-calorie/src/body/index.ts` | 改（对外 3 件 → 4 件，加 `export { bodyReceiptDoc } from './receipt.js'`） |
| `packages/skill-calorie/src/cli/write.ts` | 改（链上加 body 那一口 ＋ 件头注释的条数口径；其余域分支一字未动） |
| `packages/skill-calorie/test/t365-七条写词整页.test.mjs` | **新建**（10 条测试，LF 466） |
| `docs/skills/skill-calorie/t365-整页证据.md` | **新建**（本件） |
| `.changeset/t365-body-receipt.md` | **新建** |
| `.scratch/t365/probe.mjs`／`peek.mjs`／`dumprow.mjs`／`attrib.mjs` ＋ `*.out`／`*.err` | 草稿（不入库） |

未动（票面禁碰，逐条点名）：`src/body/bodyDocs.ts`／`bodyPlate.ts`／`view.ts`／`log.ts`／`remove.ts`／`routes.ts`、
`src/triggers/**`、`scripts/check-warning-line.mjs`；`src/shared/writeParts.ts` 与 `src/shared/copyArea.ts` **确需判定为否、一行未动**
（`copyArea` 直接可用、缺值口径由本件自己实现，无需改共用位）。

## 五、收尾对账（文件哈希与台账）

改动前后各件 sha256 前 16 位（`LF` 为 LF 口径行数）：

```
src/cli/write.ts                改前（HEAD 版）  改后 dd11577aa861e55e  LF=98
src/body/index.ts               改后 28168a60aac12d04  LF=39
src/body/receipt.ts             新建 9c79cb0f8041589e  LF=324
test/t365-七条写词整页.test.mjs   新建 792b0729fdbabf9a  LF=466
```

（`src/cli/write.ts` 的 `dd11577aa861e55e` 在变异 M1 前、还原后各测一次，同值 ⇒ 还原逐字节干净。）

## 六、未做项与下一手缺什么

### 1（要紧）跨票旧针：`test/profile-doc-179.test.mjs` 的「其余仍是片段」抽样

`profile-doc-179.test.mjs:268-283` 逐字钉住「这些命令的产物仍以片段开头」，抽样里含 `calorie.body.measure-add`
（`:273`）。本票按票面把该键切成整页 ⇒ 断言必红：

```
AssertionError: calorie.body.measure-add 的产物不再是原回执片段（本条钉住的命令尚未切整页）
at profile-doc-179.test.mjs:279
```

**归属：本票的预期后果**（变异 M1 下该断言转绿，见 §六-4 的隔离读数）。
仓内已有**同一情形的两处先例**，且都是「搬家那张票顺手改这条抽样」：
`#337`（体重 4 条）与 `#269`（饮食 13 条）——两处都在本测试件里留了「口径变更（有意改，票面与提交信息写清）」注释，
把该族从抽样里拿出来，另加一条钉该族整页的新测试。

**本票没动它**：写集硬限 `packages/skill-calorie/test/t365-*.test.mjs`（新建唯一），改他人测试件不在写集。
**下一手缺什么**：MAP 决定 (a) 扩写集让本票顺手改（照 `#337`／`#269` 注释体例：抽样里删掉 `:273` 那一行 ＋
补一条「身体细节 4 条已是完整文档」的新测试），或 (b) 另开一条收口票。仓内有跨票改针的先例：`3f9971a
test(485,487): 同质针随体重域换词收紧——波动 7 针与今日盘 3 针按新文案改写（只换词，不删针）`。

### 2（同类）`test/t364-删除取快照.test.mjs` 的落盘页解析器

`:164-171` 的 `detailFromPage()` 从落盘页里抓 `<li>…</li>` 再切 ` · 日期 ` 段。那是**片段**的标记；
本票整页把同一份数据面改由 `renderChangeRows` 呈现（`<div class="ilife-block-change-row">` …），
故 `assert.ok(li, '落盘页应有 items 逐条行（`<li>`）')` 必红（2 条测试，体脂／围度各一）。
**数据面无损**：#364 承诺的「删前快照逐字段」在本票页上是「删除前的原值」那一段，
本票测试用 `assertRows()` 把它的 **13＋2／7＋3 格逐格对过脚本查库值**，标签序取 `items[0].detail`。
另 #364 的其余 5 条测试（裁定 2 两条、全填、软删语义、接通面）**全绿**——它们读的是回执结构化载荷，不读落盘页标记。
**下一手**：同上，归 MAP 处置（改 `detailFromPage` 的解析器，或让两条测试改读新标记）。

### 3 不属本票的在他席红（逐条归属，未假绿）

`node --test` 全量跑时的其余红件经**变异隔离**确认与本票无关（摘掉 body 那一口后**照旧红**）：

| 红件 | 归属 |
|---|---|
| `#448 判据①×2／③`（`docpage-printable-448.test.mjs`） | 他席在途（`src/shared/docPage.ts` 工作区已改）；样本是**运动回执**，与本域无关 |
| `#239 四张页接上「复制日志」`＋同族 3 条（`copy-component-179.test.mjs`） | 他席在途；症状是预检确认页出现**重复的 `ilife-copy-log` 按钮**（`planCopyBlock.ts` 件头已记该 #336 冲突） |
| `120 · analysis 11 处查询`（`softdelete-120.test.mjs`） | 他席在途（`src/analysis/**` 工作区已改） |
| `体重记/改/删/批量 + 缺身高仍记（C5 #43）` | 他席在途（体重域换词在途） |
| `#337 场景 03 四条体重写命令已是完整文档` | 他席在途（同上，体重域） |
| 告警线台账陈化 4 件 | **本票进场时即红**：3 件是**他席工作区在途**改动撑高的，1 件（`src/weight/review.ts`，HEAD_LF=648 而 HEAD 台账=647）**在 HEAD 上就已陈化**。此后他席自跑 `--sync` ⇒ 本票 §3.1 那次（runId `c642b81c`）读到 `RESULT: 60/60` **绿**；但**他席随后继续改**，本票提交后复跑（runId `022c6082`）又读到 `RESULT: 56/60`，逐条为 `src/weight/history.ts` 742→775／`src/weight/review.ts` 648→665／`src/render/trendMiscPortDocs.ts` 597→625／`src/weight/compare.ts` 572→574 —— **四件全属体重／趋势域的他席在途件**。本票 `--sync --dry` 计划（两次都跑过）里**始终无本票任何件** ⇒ 未跑 `--sync`（跑它会把**他席在途**的行数烘进共用台账＝假绿，且他席一继续改就又陈化）。本票新件 `src/body/receipt.ts`（LF 324＜350）既未挂号、也未被点名。 |

### 4 变异隔离读数（归属的机器依据）

同一批测试，**改坏态**（摘掉 body 那一口）vs **还原态**：

```
他席 5 件（#448×3／120／体重C5）   改坏态=红  还原态=红   ⇒ 与本票无关
#337 体重四条已是完整文档          改坏态=红  还原态=红   ⇒ 与本票无关
#239 四张页接上复制日志（＋同族3）  改坏态=红  还原态=红   ⇒ 与本票无关
#179 其余仍是片段（body.measure-add）改坏态=绿  还原态=红   ⇒ 本票预期后果（见 §六-1）
#364 判据①②③④ 体脂／围度          改坏态=绿  还原态=红   ⇒ 本票预期后果（见 §六-2）
```

隔离 runId：`a0234892-792e-4372-8c03-a5f6c6bb8cbe`（`profile-doc-179` 单跑，还原态 exit=1，红的就是 §六-1／§六-3 那三条）。

### 5 其余未做项

- 票面「交付物路径」写的 `docs/skills/skill-calorie/t13-整页证据.md` 是旧十七票编号；按本条派活指令改用
  `docs/skills/skill-calorie/t365-整页证据.md`（本件）。
- 未跑 `pnpm --filter skill-calorie build`／`pnpm gen`／`gen:check`（红线）；编译只 `npx tsc -b packages/skill-calorie`。
- `src/triggers/routes.generated.ts` 零接触、未提交。
- 真库零写入：全部数据走 `mkdtempSync` tmp 库（`SKILLS_DB_PATH` 指过去）。

## 七、收口席（第二棒）· 跨票旧针两件改完（2026-09-15）

**判定：本票写集内完成。** §六-1／§六-2 两条「下一手」即本棒的活；前棒中途死亡留下的半成品先量现场再判完成度
（协议 §3.2 第 4 条），本条只记本棒的动作与读数，不回改上文一字。

### 7.1 接手时量到的现场（前棒留下的无主 diff）

- `test/profile-doc-179.test.mjs`：工作区 **+68/−3**。前棒已把场景 08 身体细节 4 条从「仍是片段」抽样里移出
  （抽样只剩 `calorie.goal.set`／`calorie.goal.water`）、件头与抽样注释已写明「#365 口径变更（有意改，票面与提交信息写清）…见本文件末条测试」，
  且**末条新针已写完**（不是半截）。判据：`node --check` exit 0 ＋ 每处改动逐条对上交付清单 ⇒ **接管，不重做**（本棒对 179 件零新增改动）。
- `test/t364-删除取快照.test.mjs`：**完全未动**（`detailFromPage()` 仍抓片段 `<li>`）⇒ 本棒改。

### 7.2 改完的两件

| 件 | 动作 | 「#365 口径变更（有意改）」注释位置 |
|---|---|---|
| `test/profile-doc-179.test.mjs` | 片段抽样只留目标域两条；末条新针 `#365 身体细节四条写命令已是完整文档（七条写词；从「其余仍是片段」那张表里拿出来）` 钉身体域整页六项读数（首行 `<!doctype html>`／charset／内联样式 ≥2KB／无外链／回执路径可读／不再落在旧片段标记上）＋场景 08 眉标 ＋ `change-row` 逐格行 ＋ 页尾对账折叠区 ＋ 复制日志按钮，四条命令逐条跑真出口 | 件头（`:8-10`）＋ 抽样注释（`:269-273`） |
| `test/t364-删除取快照.test.mjs` | `detailFromPage()` 由抓片段 `<li>… · 日期 …` 改读**整页**里 `renderChangeRows` 的 `change-row`：按口径行 `<p class="ilife-block-caliber">` 切段、取「删除前的原值」那一段、读 label／old 两栏（与同目录 `t365-七条写词整页.test.mjs` 的 `sectionRows()` 同一判据，箭头位 `arrow:false` 的 `visibility:hidden` 仍占栏故不读它的内容）；首格锚「日期」，HTML 实体还原后与 `items[0].detail` 逐格同源 | 件头（`:19-27`）＋ `detailFromPage()` 函数注释 |

**断言意图一字不变**：删前值逐格比（体脂 11 格＝日期／来源／体脂率／7 点皮褶／备注，围度 15 格＝日期／13 部位／备注）、
标签序与格数取 `items[0].detail`、软删语义与接通面一字未动 ⇒ `t364` **7/7**（含不读落盘页的另 5 条：裁定 2 两条、全填、软删语义、接通面）。

### 7.3 验收读数（本棒全部运行条目，逐条声明）

```
GATE-RUN runId=a4bd3d94-558f-4f60-a746-691c5e15a57a cmd=node --test packages/skill-calorie/test/profile-doc-179.test.mjs   （接手基线 exit=1）
GATE-RUN runId=443b0adf-601e-4070-970d-8ef876465948 cmd=node --test packages/skill-calorie/test/t364-删除取快照.test.mjs   （接手基线 exit=1）
GATE-RUN runId=2cade89e-289b-4038-97eb-e9c904c6a353 cmd=node --test packages/skill-calorie/test/t365-七条写词整页.test.mjs （读数③ exit=0）
GATE-RUN runId=6fa8dcb4-c525-42cb-80dd-6fd2a493b4d9 cmd=node .scratch/t365-needles/probe-attribution.mjs                  （归属探针 exit=0）
GATE-RUN runId=09bf9c87-84d7-4199-9747-2135e6b88f64 cmd=node --test packages/skill-calorie/test/t364-删除取快照.test.mjs   （改后 exit=0）
GATE-RUN runId=8aa23928-a171-4f45-812e-1cbd08412ab4 cmd=node --test packages/skill-calorie/test/profile-doc-179.test.mjs   （改后 exit=1）
GATE-RUN runId=02d191ec-c7a6-4b3c-9ba6-cd77e545bccb cmd=node --test packages/skill-calorie/test/t364-删除取快照.test.mjs   （负向·改坏 364 解析器 exit=1）
GATE-RUN runId=cfc7bf2c-4684-4976-bb39-8e7a38d3e78e cmd=node --test packages/skill-calorie/test/profile-doc-179.test.mjs   （负向·改坏 179 新针 exit=1）
GATE-RUN runId=62e16258-cdf1-4de2-9966-fe5adb9b0d47 cmd=node --test packages/skill-calorie/test/profile-doc-179.test.mjs   （还原·读数① exit=1）
GATE-RUN runId=5c8f8a3b-1107-4bb2-a7d8-f5269fc56422 cmd=node --test packages/skill-calorie/test/t364-删除取快照.test.mjs   （还原·读数② exit=0）
GATE-RUN runId=8d05f01b-57f5-400e-85b0-130c497fe3c4 cmd=node --test packages/skill-calorie/test/t365-七条写词整页.test.mjs （还原·读数③ exit=0）
GATE-RUN runId=2cf69ec3-3357-4415-ae0f-9803c480732f cmd=node packages/skill-calorie/scripts/check-warning-line.mjs          （读数④ exit=1）
GATE-RUN runId=2357ec16-6ab2-4c88-95f7-43d3c6152d74 cmd=node packages/skill-calorie/scripts/check-warning-line.mjs          （读数④复跑 exit=0）
```

| # | 读数 | 结果 | 日志（本票草稿目录，不入库） |
|---|---|---|---|
| ① | `node --test test/profile-doc-179.test.mjs` | **exit 1**／`pass 8 fail 2`（余 2 条非本票，见 7.4） | `.scratch/t365-needles/final-179.log` |
| ② | `node --test test/t364-删除取快照.test.mjs` | **exit 0**／`pass 7 fail 0` | `.scratch/t365-needles/final-364.log` |
| ③ | `node --test test/t365-七条写词整页.test.mjs` | **exit 0**／`pass 10 fail 0` | `.scratch/t365-needles/final-365.log` |
| ④ | `node packages/skill-calorie/scripts/check-warning-line.mjs` | **exit 1**／`RESULT: 59/60`；`RED 台账陈化：src/weight/compare.ts 台账=625 实况=632` ⇒ **他席在途件**（`src/weight/compare.ts` 工作区已改），本票**未跑 `--sync`**、一行台账未改。本票四件路径本体不在扫描面内（扫描面＝包内 `src/**/*.ts` 与 `scripts/**/*.mjs`；`test/*.mjs` 与 `docs/**` 依本包 AGENTS 明列剔除）⇒ 该红与本票无因果关系。**复跑（提交前）：该席此后自行 `--sync` 回绿 ⇒ `RESULT: 60/60`、`PASS: 告警线台账齐全且与实况一致`、exit 0**（runId `2357ec16-6ab2-4c88-95f7-43d3c6152d74`） | `.scratch/t365-needles/final-gate.log`／`regate.log` |
| ⑤ | 负向自证（两行读数） | 见 7.5 | `mut364.log`／`mut179.log` |

### 7.4 读数①的两条红：逐条归属（未假绿，也未替他人改口径）

两条红件的**断言文本与 HEAD 逐字相同**、且**不在本棒任何改动 hunk 内**（本棒两件的 diff hunk 仅覆盖
`profile-doc-179.test.mjs:8-9／268-273／279／356-414` 与 `t364` 的件头＋解析器），机器证据：

| 红件 | 症状（本棒复跑） | 归属 |
|---|---|---|
| `#239 四张页接上「复制日志」`（`profile-doc-179.test.mjs:192`） | 预检确认页出 **3 颗按钮**（`ilife-help-copy-prompt` ＋ `ilife-copy-log` ×2，其中一颗 `disabled`）；探针 `PROBE-WIZARD exit=0 actionIds=["ilife-help-copy-prompt","ilife-copy-log","ilife-copy-log"]` 证明是**产物**如此、非测试问题 | 与 §六-3 同行归属一致：重复复制日志按钮的既有冲突（`planCopyBlock.ts` 件头已记 #336 冲突）。装配链 `profile/setup.ts:328` → `shared/copyArea.ts:116` → base-paint `renderCopyBlock`，**三件皆无在途改动**（`git status` 无）⇒ 已提交态的旧针 |
| `#337 场景 03 四条体重写命令已是完整文档`（`:332`） | `weight.batch 缺：批量计数`；探针 `PROBE-WEIGHT-BATCH exit=0 has批量计数=false`（页 63763 字符仍在，改由摘要与结论句给三数） | 他票 #483 的**已提交**改动：`473dd15 feat(483): 场景03 体重写后回执 9 词文本审查…` 删掉「批量计数」表（`src/weight/logReceipt.ts:190` 件内注释），是 #337 旧针未随该票收窄；另有体重域他席在途件同域 |

**处置**：MAP 已明确本票口径变更**只覆盖身体域这一族**；把 #239／#337 的针改绿＝替他人票改口径（#239 那处还可能是真缺陷：
用户可见的重复按钮），**故本棒一字未动**，只归属、不 `--sync`、不假绿。

### 7.5 负向自证（改坏 → 必红；还原 → 必绿）

两处变异都打在**本棒新加/新改的针**上（不是源码级——本票红线禁碰 `src/**`），读数为「红没红 ＋ 还原后与改前一致」两行：

```
负向-364  detailFromPage() 改回片段解析（抓 <li>…· 日期 …）  exit=1  ℹ pass 5 fail 2  AssertionError: 落盘页应有 items 逐条行（`<li>`）×2
负向-179  assertBodyPage() 的「不再落在旧片段上」改回断言「仍落在片段上」 exit=1  ℹ pass 7 fail 3  AssertionError: 记体脂 还落在旧回执片段上
还原-364  取回本棒解析器版本                             exit=0  ℹ pass 7 fail 0
还原-179  取回 `false` 断言版本                            exit=1  ℹ pass 8 fail 2（余 2 条＝7.4 归属的两条，非本针）
```

（还原＝逐文件取回**自己改过的件**，未用任何整目录还原／清空批量命令；还原后两件内容与变异前逐字节一致，
见 7.6 的 SHA256 前 16 位。）

### 7.6 收尾对账导出（协议 §2.3-4；`runId` 逐条可在 `.scratch/locks/gate-runs.log` 复核）

本棒窗口内全部运行条目原样导出（字段＝ledger 的 RUN 行：`runId／waitedMs／exit／at`）：

```
a4bd3d94-558f-4f60-a746-691c5e15a57a  0      1  2026-09-15T00:03:53.597Z  node --test …/profile-doc-179.test.mjs
443b0adf-601e-4070-970d-8ef876465948  0      1  2026-09-15T00:03:56.573Z  node --test …/t364-删除取快照.test.mjs
2cade89e-289b-4038-97eb-e9c904c6a353  0      0  2026-09-15T00:04:03.112Z  node --test …/t365-七条写词整页.test.mjs
6fa8dcb4-c525-42cb-80dd-6fd2a493b4d9  1      0  2026-09-15T00:05:16.810Z  node .scratch/t365-needles/probe-attribution.mjs
09bf9c87-84d7-4199-9747-2135e6b88f64  15010  0  2026-09-15T00:07:06.032Z  node --test …/t364-删除取快照.test.mjs
8aa23928-a171-4f45-812e-1cbd08412ab4  0      1  2026-09-15T00:07:13.927Z  node --test …/profile-doc-179.test.mjs
02d191ec-c7a6-4b3c-9ba6-cd77e545bccb  0      1  2026-09-15T00:07:49.619Z  node --test …/t364-删除取快照.test.mjs（负向）
cfc7bf2c-4684-4976-bb39-8e7a38d3e78e  0      1  2026-09-15T00:07:57.428Z  node --test …/profile-doc-179.test.mjs（负向）
62e16258-cdf1-4de2-9966-fe5adb9b0d47  0      1  2026-09-15T00:08:15.384Z  node --test …/profile-doc-179.test.mjs
5c8f8a3b-1107-4bb2-a7d8-f5269fc56422  0      0  2026-09-15T00:08:18.198Z  node --test …/t364-删除取快照.test.mjs
8d05f01b-57f5-400e-85b0-130c497fe3c4  0      0  2026-09-15T00:08:24.540Z  node --test …/t365-七条写词整页.test.mjs
2cf69ec3-3357-4415-ae0f-9803c480732f  0      1  2026-09-15T00:08:24.692Z  node packages/skill-calorie/scripts/check-warning-line.mjs
2357ec16-6ab2-4c88-95f7-43d3c6152d74  0      0  2026-09-15T00:20:46.984Z  node packages/skill-calorie/scripts/check-warning-line.mjs（复跑）
（非本棒、仅备查：前棒接手基线 c204fefd…／85bf3143… 两条，见 .scratch/t365-needles/baseline-*.log）
```

改动件 SHA256 前 16 位（`LF` 为 LF 口径行数；**收尾时逐件当场算**）：

```
packages/skill-calorie/test/profile-doc-179.test.mjs  7378cf38b395aeb2  LF=414
packages/skill-calorie/test/t364-删除取快照.test.mjs   70425f8602f066f5  LF=419
docs/skills/skill-calorie/t365-整页证据.md           7c51af38a29a31e3  LF=327
```

（前两行的哈希在负向自证的**还原之后**当场算，与变异前同源；本件自身那一行是本节填写占位符**之前**的读数——
本件随后被本次提交收入，最终以提交 sha 为准。）

### 7.7 本棒的未做项与下一手

- **未做**：① 读数按当刻树为 exit 1（余 2 条见 7.4，MAP 处置归属）；未跑 `pnpm build`／`pnpm gen`／`pnpm gen:check`／`--sync`
  （红线与「只归属」口径）；未 push（按派单由编排者统一收口）。本棒未改任何 `src/**`、未碰 `test/t365-*`、未碰包内 `AGENTS.md`
  与 `scripts/check-warning-line.mjs`。
- **下一手缺什么**：① `#239`（重复 `ilife-copy-log` 按钮）与 `#337`（「批量计数」旧针）两条要不要收口，需 MAP 定票
  ——本票只把它们**归属**清楚；② `.scratch/t365-needles/` 是本票草稿目录（`.gitignore` 已忽略 `.scratch/`），
  日志随工作区留存，不入库。

### 7.8 本棒两次提交的运行条目导出（协议 §2.3-4 续）

`git add`（限声明件）＋ `git commit -F` 都在持锁包装器里跑，ledger RUN 行原样导出：

```
7efd85f2-3e23-49fd-979e-fbe18deef229  15011  0  2026-09-15T00:10:35.559Z  pwsh -NoProfile -File .scratch/t365-needles/commit.ps1   第 1 次提交 → c37eaca
（第 2 次提交＝写入本节这次；其 START／RUN 行随本文件入库后在同目录 gate-runs.log 可复核，runId 见回执）
```

第 1 次提交 `c37eaca`：`git diff --cached --name-only` **恰好三件**（`packages/skill-calorie/test/profile-doc-179.test.mjs`／
`packages/skill-calorie/test/t364-删除取快照.test.mjs`／本文件），`git show --stat` 复核同三件（+225/−10）；
提交后本票声明路径下**干净**（`git status` 对这三件为空）；`.scratch/` 在 `.gitignore` 内（`git check-ignore -v` 命中
`.gitignore:5:.scratch/`），草稿与日志**不入库**。未 `push`（按派单由编排者统一收口）。


## 回炉记（F1／F2／F3，2026-09-14 编排者收口）

独立复核判 FAIL 的拦关项 **F1** 已修：`src/body/receipt.ts` 的行槽落点改成按操作类型分派——**create 落「新值」槽（`after`，绿、无删除线）**、其余落「改前」槽（`before`），箭头占位 `arrow:false` 与老正本 `:344`／`:365` 同法；作者自己的 `test/t365-七条写词整页.test.mjs` 的取值槽同步更正（+1 用例）。

- **F2（S3）**：id 卡三态图标按**具名理由不补**（本域四条写命令走不到 `update` 分支；徽章形态照 `diet/nutritionPortDocs.ts:67` 的同款先例），已在件内注释写明。
- **F3（S3）**：`receipt.ts` 件头注释改为如实说明——老正本删支 `:337`／增支 `:360` 对空值是**整行不摆**，本件按基准逐格写 `—`，属**有意偏离**（行为未改，只改归因）。

机器读数（编排者持锁复跑，runId `41340bb3-412f-4dab-9d4c-69d712b1e835`）：`t365` 测试 `tests 11 / pass 11 / fail 0`；`t364` 测试 `tests 7 / pass 7 / fail 0`（未伤）。`src/body/receipt.ts` 当刻 **348 行**（＜350，未触发第四步）。

**来源**：`docs/skills/skill-calorie/t365-复核.md` 的 F1／F2／F3；原回炉席中途中断，未提交成果由编排者验证后收口。复核席的**独立复验**（`REV365-SLOT-VIOLATIONS 0`／`REV365-FAILS 0`）见其复核件的后续追加。