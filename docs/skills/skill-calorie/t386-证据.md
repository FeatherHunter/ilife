# #386 证据 · 卡路里场景 10B 锁：真正的交付出口用例

> 票：MAP #161 · #386「锁·真出口用例」。**只加测试**：本票**不改被测命令行为**、**不写视觉脚本**、
> **不写手写计数**（条数一律与权威声明对账）、**不另起第二份共用件**。
> 结构落点：`packages/skill-calorie/test/` 不在结构纪律管辖范围内；完整文档断言**只读复用**
> `packages/skill-calorie/test/doc-page-assert.mjs::assertDocPage`（#264 建，唯一定义地），
> 既有三份五连（`profile-receipt-175`／`wizard-86`／`profile-doc-179`）一行未动。

## 一 · 交付物

| 件 | 说明 |
|---|---|
| `packages/skill-calorie/test/analysis-accept-386.test.mjs` | 新（含变异自证入口）；六组断言＋范围派生＋真库只读自查 |
| 本文件 | 机器读数存档（探针读数、变异两行、真库对账、遗留出口） |

## 二 · 范围分母（**由权威声明派生，无手写计数**）

| 段 | 派生口径 | 条数（当刻） |
|---|---|---|
| 预测模拟 | 冻结表 `SCENE_10_ANALYSIS` 里 `subfunction === '预测模拟'` | 20 |
| 报告 | 冻结表里 `main_prompt.cli` 命令名以 `calorie.report.` 起 | 8 |
| 缺口 | 冻结表里 `subfunction === '缺口分析'` | 1 |
| new 别名 | `ANALYSIS_ROUTES` 里 `list === 'new'` 且命令键落在上面那批命令键内 | 2（`看热量缺口` new 7／`看体重预测` new 29） |
| **合计** | `WORDS` | **31** |

「报告 8」另有一条机械判据：8 条命令的形态名集合必须与 `src/analysis/reportDoc.ts` 的
`KIND_LABELS` 形态集**相等**（少一个或多一个即红）；`new` 别名覆盖的命令键集合与范围命令键集合自洽。

## 三 · 六组断言与机器读数

命令：`node tooling/run-locked.mjs --ticket 386 --poll-ms 15000 --max-wait-ms 600000 -- node packages/skill-calorie/test/analysis-accept-386.test.mjs`

| 轮 | runId | exit | 摘要 |
|---|---|---|---|
| 全绿（基线复跑） | `59aa8521-e194-41b1-a62e-9d22eaf46d69` | 0 | tests 8／pass 8／fail 0 |
| 全绿（最终树） | `f0bcb56d-71d8-41dc-b763-e384ecf49095` | 0 | tests 8／pass 8／fail 0 |
| 全绿（真库对账那一轮） | `19462b5c-466c-4cba-8da3-1a524332b7c1` | 0 | tests 8／pass 8／fail 0 |

八条断言：① 范围派生；② 第一组 以唤醒词为起点；③ 第二组 两处定义地对账；④ 第三组 页面归属；
⑤ 第四组 阻断；⑥ 第五组 回归灵敏度；⑦ 第六组 产物形态不得回退；⑧ 真库只读自查。

要点（当刻实测）：

- **第一组**：31 条逐条 `lookupWake` 拿命令**原样跑**（不补 `--html`），`exit 0`、
  `data.output` 绝对路径且落在 `calorie_html/`、`delivery.bytes` ＝ 落盘字节 ＝ 读回字节、
  `assertDocPage` 全过。命令原文与**唤醒词两个入口**（速查台 `lookupWake`／路由层 `routesFor`）
  给出的命令逐字一致（两处都有条目的 29 条）。
- **第二组**：29 条冻结表 `main_prompt.cli` ↔ `analysis/routes.ts` 的 `cli` 逐字相同；
  2 条别名与**同键冻结词**的命令原文逐字相同。
- **第三组**：8 报告各带自己形态的页头（`KIND_LABELS`）＋`卡路里 · 报告`徽标＋本形态命令回执行，
  8 页**两两互不相同**、**都不等于**对照基准「看健康盘」那一页；20 条预测每条页面读数都带上
  **它自己参数**（`horizonDays`／`cut_kcal`／`target_loss`／`days_target`／`target`），
  20 页**两两互异**；缺口页页头必须匹配 `热量缺口 <起> ~ <止>` 且带四个 KPI 格。
  缺口别名与本体**产物逐字节相同**（同一条命令、两个入口）。
- **第四组**（空库／不足 `SIM_MIN_DAYS`＝14 天）：
  - 空库 20 条预测词**全部**阻断：非 0、`缺失阻断`、stdout 无产物路径、`calorie_html/` 一个文件都没有；
  - 空库报告／缺口族二档必居其一：阻断档同前；空态档（当刻只有「看TDEE报告」一条）落盘但
    **每个读数位都是 `—`**、页面点名缺项、不挂降级文案；
  - 13 天库里：**落盘的不得是降级页**（页面无「数据不足／无法预测」）；**不落盘的必须点名门槛**，
    门槛天数与 `SIM_MIN_DAYS`（`dist/analysis/simulate.js` 导出）对账，或为「至少 1 条体重记录」
    这一条无天数的门槛；落盘的预测词必须带真实起点读数（`metrics.current`）。
- **第五组**：8 报告页各带**它自己那一件**的独有构件针（`逐日体重与 BMI`／`活动量系数`／
  `低于基础代谢`／`每日蛋白量`／`每日饮水量`／`分项分数表`／`评分序列`／`两期变化量`），
  且这 8 条针**两两互斥**（每条针的归属形态集恰等于它自己）。
- **第六组**：31 份产物全部是完整文档：doctype 起、`</html>` 止、`<title>` 非空、
  `ilife-page`、无残留标记、`复制数据`＋三格式菜单（`text`／`json`／`csv` 逐项对账）。

## 四 · 变异自证（两行必给）

| 轮 | 改坏的件与位置 | runId | exit | 断言读数 |
|---|---|---|---|---|
| **MUT-A 红**（拿错页） | `packages/skill-calorie/src/analysis/commands.ts:371` `viewReport('bmi',…)` → `viewReport('tdee',…)` | `9392b238-c9ea-4e02-9049-e031fa2cd150` | **1** | tests 8／pass 6／**fail 2**：第三组「看BMI报告 页面标题不是它自己那个形态：期望「卡路里·BMI 报告」」、第五组「缺「逐日体重与 BMI」」 |
| **MUT-A 还原**（逐件点名） | 同上文件改回 `viewReport('bmi',…)`；sha256 与 `commands.pristine.ts` 一致（`7CBD30CE469BADC5…`） | `b6319b6c-fbc2-4a12-b16f-20d33c0519a3` | **0** | tests 8／pass 8／fail 0 |
| **MUT-B 红**（换掉一处模板件名） | `packages/skill-calorie/src/analysis/reportDoc.ts:84` `bodyOf` 里 `case 'bmi'` 的块装配件名 `buildBmiBlocks` → `buildTdeeBlocks` | `189536fd-c751-4aad-8142-4cfcb0998dda` | **1** | tests 8／pass 7／**fail 1**：第五组「看BMI报告 页面正文不是它自己那一件的构件：缺「逐日体重与 BMI」」 |
| **MUT-B 还原**（逐件点名） | 同上文件改回 `buildBmiBlocks(plate)`；sha256 与 `reportDoc.pristine.ts` 一致（`4E39FC93D0187583…`） | `f0bcb56d-71d8-41dc-b763-e384ecf49095` | **0** | tests 8／pass 8／fail 0 |
| **断言级自证**（换页身份） | `T386_MUT_TPL=1`：把首条报告的期望页身份换成另一形态 | `edb0ec73-93a5-4506-a152-0798ebfabda3` | **1** | tests 8／pass 6／**fail 2**（第三组＋第五组） |

还原**逐文件点名、禁批量命令**：MUT-A 只改 `analysis/commands.ts`；MUT-B 只改 `analysis/reportDoc.ts`；
两轮各自用 `edit` 反向改回并当场比对 `sha256`。**共享输入哈希**（跑前／跑后两次快照逐行对比）：
`scene-10-analysis.ts`／`analysis/routes.ts`／`analysis/commands.ts`／`analysis/reportDoc.ts`／
`analysis/reportPlate.ts` 五件**哈希不变**（`AGENTS.md` 在他席在途改动中，与本票无关）。
每轮门禁前记 `git status` 摘要：跑前 67 行、最终 87 行，增量全是他席在途件（`skill-bill/src/record/*`、
`skill-calorie/src/body/log.ts`、`src/fetch/body.ts`、`src/render/*Port*.ts` 等），红绿可归因。

基线 `pnpm build`：runId `ced4a0a6-ea67-4b01-b849-ddc146ef52d9` exit 0；
各变异轮 `pnpm build`：`1b6aee66…`／`73dec326…` 皆 exit 0（构建全绿，红绿全部出自断言本身）。

## 五 · 探针读数（`t293-验收-命令自治.mjs`）

| 轮 | runId | exit | 摘要行 |
|---|---|---|---|
| 基线 | `aa307de1-5674-46d4-b338-5709b5ffebc8` | 0 | `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5` |
| 最终树 | `40f20f7f-ffe1-48db-83f0-09765cc1cf3a` | 0 | `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5` |

`P1` 行原文（最终树）：

```
③ UNACCOUNTED（主体＝权威总量、既没派生也没登记，0 处）：
⇒ UNACCOUNTED 为空 ⇒ 手写面已全部落在 ①/② 内
动态（派生桶）证据齐=true；UNACCOUNTED=0 处；④ 主体判不出=1 处（只报账、不判红）；树自相矛盾=0 处
```

**`UNACCOUNTED` 为空**（0 处）——票面要求的探针读数达标，不是探针误报。

## 六 · 真库只读对账

真库：`D:\2Study\StudyNotes\.db\calorie_data.db`（`SKILLS_DB_PATH` 当刻值＝`D:\2Study\StudyNotes\.db`）。
对账方法：以 `node:sqlite` `readOnly: true` 打开，取 `sha256` ＋ 文件字节数 ＋ 逐表行数 ＋ mtime；
**在一整轮测试（31 条命令 × 100 天夹具 ＋ 空库 ＋ 13 天库）前后各取一次**。

| 项 | 跑前 | 跑后 | 相同 |
|---|---|---|---|
| `sha256` | `57ce9d87f5f232822332d7057c2ae8411d99ba7fdecea289f2028e19fc3ffc15` | 同左 | **是** |
| 字节数 | 3072000 | 3072000 | **是** |
| 逐表行数（12 表） | `body_composition 0／body_measurements 1／body_photos 22／daily_goal 1／exercise_log 8297／food_log 562／nutrition_products 1931／sqlite_sequence 8／user_profile 1／weight_log 129／workout_plan_config 1／workout_plans 100` | 同左 | **是** |
| `mtime` | `2026-09-14T05:53:37.729Z` | 同左 | **是** |

测试全部跑动经 `SKILLS_DB_PATH` 指向系统临时根下的夹具目录（测试内另有一条自查：
夹具目录必须在临时根下、不得等于或落在真库目录内）。

## 七 · 遗留出口

### 7.1 当场开票（#466）

**预测族降级文案把窗口天数当数据天数报**：`src/analysis/simulate.ts:23`／`simulate2.ts:12` 的
`degrade()` 用 `series.length`（＝窗口天数）填「当前只有 N 天」，空库时会报出
「需要 ≥14 天摄入记录,当前只有 30 天。」这种自相矛盾的读数。
→ 已开票 **https://github.com/FeatherHunter/ilife/issues/466**。本票测试按「必须有明确提示＋门槛天数与
`SIM_MIN_DAYS` 对账」判，**不把错读数写成正确**。

### 7.2 HELP 两边词不一致（点名报告）

本图 2 条 new 别名**跑得通**（路由层各 1 条可执行记录），但**唤醒词速查台（`HELP_LOOKUP`）里 0 条**：

| 别名 | 路由层可执行 | 速查台条目 | 冻结表条目 | 命令登记表条目 | `SKILL.md` 行 |
|---|---|---|---|---|---|
| `看热量缺口`（new 7 → `calorie.view.deficit`） | 1 | **0** | 0 | 1 | 有 |
| `看体重预测`（new 29 → `calorie.view.predict`） | 1 | **0** | 0 | 0 | 无 |

即：同一条命令在「唤醒词速查台」与「命令登记表」两边的**词不一致**——`看热量缺口` 只在命令登记表，
`看体重预测` 两边都没有（只有路由层）。**代表唤醒词机器不校验属框架级事项**（已有票），
本票不改生成器，只按票面「点名报告」。

### 7.3 口径偏差（记录，不开票）

票面第四组写「空库／数据不足 14 天时……不落盘」，当刻实测**报告族有一条不落盘不成立**：
空库下「看TDEE报告」`exit 0` 并落盘。查产物：每个读数位都是 `—`（每日总消耗／日均摄入／静态缺口／
活动量系数／BMR／活动系数／身高年龄性别全为 `—`），并点名「档案四要素不全，出不了总消耗」「缺：身高、年龄、
性别、体重、活动量」，**没有编造默认值**，属#384 的空态指引页而非缺陷。本票判据据此写为
「阻断档／空态档二选一，二档都必须明确提示、都不编默认值」，并把这一偏差如实记在本节。
