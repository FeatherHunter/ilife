# #401 样板文本与结构返修 —— 证据（`看今日主页` 一族三页）

**判定：三组改动做完，本票范围内全绿。** 被审件＝样板产物 `.scratch/t401d/看今日主页.html`（真出口重出，
固定种子库 `SEED_TODAY=2026-09-07`）；对照件＝改前基线 `.scratch/t401c/看今日主页.html`（#401c 那一轮产物）。
只动 `packages/skill-calorie/src/home/` 三件 ＋ 新增一个测试件；公共层一行未碰。

**一句话结论**：8 处分隔符债逐条改完（全部用现成件，没有新造件）；9 条冗余每类只留一处；
KPI 六张减到四张；分隔符探针三条读数在「看今日／看本周／看本月」三页上**全部为 0**。

## 一、第一组：8 处分隔符债（照报告落点件，无一处新造件）

| # | 改前（可见文本片段） | 改后 | 落点件 |
|---|---|---|---|
| 1 | `<title>` ＝ `卡路里·主页` | `卡路里 主页`（产品名＋空格＋页名，去 `·`） | `docShell` 的 `docTitle` 参数 |
| 2 | eyebrow ＝ `卡路里 · 主页` | `卡路里`（只留产品名）；页面身份改由「主页」胶囊承担 | `renderPageShell` 的 eyebrow 槽 ＋ `renderChips`（`blocks.ts:459`） |
| 3 | subtitle ＝ `2026-09-01 ~ 2026-09-07 · 窗内有记录 5 天 · 连续 3 天` | subtitle 只留窗口；另两件改两枚胶囊 `窗内有记录 5/7 天`／`连续记录 3 天` | `renderPageShell` 的 subtitle 槽 ＋ `renderChips` |
| 4 | 今日摄入卡 detail ＝ `目标 1800 卡 · 完成度 66.06%` | detail ＝ `目标 1800 卡`；徽章 ＝ `接近目标 66%` | `renderKpiCard` 的 detail 槽 ＋ status／statusText 槽 |
| 5 | 饮水卡 detail ＝ `目标 2000 毫升 · 完成度 100%` | detail ＝ `目标 2000 毫升`；徽章 ＝ `达标 100%` | 同 #4 |
| 6 | 页级口径 ＝ `缺数一律写 —；有记录才有数。` | `缺数一律写 —。`（后半是实现说明，删） | `renderCaliberLine` |
| 7 | 表下口径 ＝ `窗内每天一行，没有记录的那天留空、不当 0 算；缺口＝消耗 − 摄入，正数是缺口。` | 只留缺口定义一条：`缺口＝消耗 − 摄入，正数是缺口。` | `renderCaliberLine` |
| 8 | 来源脚注 ＝ `数据来源 · 饮食记录 ＋ 运动记录 ＋ 每日目标 · 2026-09-01 → 2026-09-07 · 共 5 条` | 拆两条口径行：`数据来源：饮食记录，运动记录，每日目标。` ＋ `窗内共 5 条记录。` | 两条 `renderCaliberLine`（不再走 `sourceLine`） |

**两处取舍（工单要求写清理由）**

1. **`<title>` 去 `·`**：工单说 title 不删、写法与 eyebrow 统一。分隔符探针把 `<title>` 的文本也算可见文本
   （报告 L6 那处命中就是它），留着 `·` 则三条读数不可能同时为 0 ⇒ 保留两段字、去掉 `·`，产品名与 eyebrow
   逐字一致（都写「卡路里」）。
2. **页头胶囊落在正文首行**：页面模板（`renderPageShell`）只有 eyebrow／title／subtitle 三槽、**没有徽章槽**，
   故按工单给的第二个选项并进页内导航那一行的位置关系里——放在正文最前、紧贴副题；**不改公共层签名**
   （要给它加槽就是动公共层，另开票）。
3. **窗口只留一处**：工单第一组说「副标题只留窗口」、第二组又说「窗口 ×2 → 只留一处（冗余 R2）」。
   两处取前者（窗口住副题），脚注第二条只写条数；`R2` 的读数因此从 2 → 1（见下表）。
4. **表格空格改印 `—`**：工单第七处把表下那句空值口径删掉、只留页级一句「缺数一律写 —」。删掉之后，
   表格里那些空白格会让页级那句话变成假话 ⇒ 空日两列改印 `—`（仍不是 0，也不是「补 0」）。

## 二、第二组：冗余 R1–R9（同一脚本 `.scratch/t401d/count-redundancy.mjs` 跑两版产物）

| 条 | 同一事实 | 改前 → 改后 | 留哪处／删哪处 |
|---|---|---|---|
| R1 | 页面名「卡路里」 | 2 → 2 | 两处角色不同（标签页／眉标）都留；**写法统一**（都写「卡路里」，都不带 `·`） |
| R2 | 窗口区间 | 2 → 1 | 留副题那处（`~` 形式）；删脚注里的 `→` 形式 |
| R3 | 连续记录天数 | 2 → 1 | 留页头胶囊 `连续记录 3 天`；删副题那处，删 KPI 卡（第三组） |
| R4 | 空值口径 | 3 → 1 | 留页级口径行（最靠上最一般）；删图表区整条、删表下那半句 |
| R5 | 缺口定义「正数是缺口」 | 2 → 1 | 留表下口径行；KPI 缺口卡 detail 缩成 `比消耗少这么多` |
| R6 | 有记录天数 `5` | 3 → 2 | 留页头胶囊 `窗内有记录 5/7 天`（带 7 天分母）；脚注那条改成**行数口径**的 `窗内共 5 条记录。`（措辞与天数口径分开，报告 R6 的要求） |
| R7 | 目标值 `1800` | 8 → 2 | 留 KPI 卡 detail ＋ 表 caption（`，目标 1800 卡`）；**删掉表格那个 7 行同值的常量列** |
| R8 | 今日数 `1189`／`1687` | 2/2 → 2/2 | KPI 卡与表末行都必留；给今日行加标记 `2026-09-07（今日）`（行标记读数 0 → 1） |
| R9 | 节名（导航 ↔ 标题） | 各 2 → 各 2 | 两处都留（角色不同）；**写法统一**：导航也带上图标（带图标节名读数 4 → 8） |

## 三、第三组：KPI 六张 → 四张

留 **今日摄入／蛋白／饮水／今日缺口**（「今日速览」的核心四张）；撤掉 **连续记录**（并入页头胶囊：它说的是
窗口的事，不是今天的数）与 **周均摄入**（随卡位撤出，口径改挂在折线说明行 `虚线＝周均摄入，只算有记录的天。`）。
三张有目标的卡统一成「detail 只留目标 ＋ 徽章给状态与完成率」：蛋白卡为此在 `home.ts` 的 `HomeData` 上补了
`proteinGoal`（取值口径与 `proteinPct` 同源），否则它只能印 `完成度 39.33%`、与另两张不同形。
桌面 1000px 实拍：四张一行、不再挤成小卡；**栅格列数一行未改**（属公共层，另开票）。

## 四、分隔符探针（`.scratch/sep-audit/probe.mjs`，三条读数）

| 产物 | 行级 含`·`／含`；`／≥3 段并列 | 节点级 含`·`／含`；`／≥3 段并列 | 退出码 |
|---|---|---|---|
| 改前 `.scratch/t401c/看今日主页.html` | 3 行／1 行／2 行（命中 3 行） | 6 处／2 处／2 处（命中 8 处） | 1（有命中） |
| 改后 `.scratch/t401d/看今日主页.html` | 0／0／0 | **0／0／0** | 0 |
| 改后 `.scratch/t401d/看本周主页.html` | 0／0／0 | **0／0／0** | 0 |
| 改后 `.scratch/t401d/看本月主页.html` | 0／0／0 | **0／0／0** | 0 |

逐页读数原文在 `.scratch/t401d/探针-*.txt`；冗余读数原文在 `.scratch/t401d/冗余-改前.txt`／`冗余-改后.txt`。

## 五、测试（`node --test`，四个件一起跑：tests 23／pass 21／fail 2）

| 测试件 | 读数 |
|---|---|
| `render-t8.test.mjs` | 7／7 通过（0 红） |
| `home-lock-374.test.mjs` | 8／9 通过；**第 4 例 `看今日体重概览` 红**，见第八节 |
| `t401c-页面机器话探针.test.mjs` | 4／5 通过；**第 1 例红**，见第八节（另：本票按工单 R7 改了它的 caption 锁，见下） |
| `t401-可见文本守卫.test.mjs`（**本票新增**） | 2／2 通过（守卫 ＋ 变异自证） |

- **探针入仓**：`.scratch/sep-audit/probe.mjs` 已入仓为 `packages/skill-calorie/test/t401-可见文本守卫.test.mjs`，
  三条判据逐字同源（并列分隔符闭集、每段 ≤40 字、≥3 段才判），可见文本抽取复用仓内既有的
  `test/visible-text-probe.mjs`；并另加一条显式锁：`<title>` 文本里也不许有 `·`。
- **「改坏必红」负向证据（源码级变异，协议 §5）**：把 `subtitleText` 改回改前那串
  `2026-09-01 ~ 2026-09-07 · 窗内有记录 5 天 · 连续 3 天` → 重编译 → 守卫**红**
  （`AssertionError: 可见文本里还有 ·：2 处`，exit=1）→ **逐文件还原**（SHA256 与原样件一致：
  `F38379FEC215BD5E1504F01B9E88BF61B101F766AFFB3F272D98441C057CA9C3`）→ 重编译 → 守卫**绿**（exit=0）。
  产物级变异演示也在测试件里（塞回一处 `·` 并列 ＋ 一处 `；` → 红；拿掉 → 绿）。
- **t401c 那个 caption 锁**：工单 R7 要「删常量列、目标写进 caption」，原锁锁的是 `…（单位：卡）</caption>`
  整串 ⇒ 按本票裁定改成「锁前缀 ＋ 另锁 `，目标 1800 卡`」。这是锁跟着裁定走，不是为凑绿改判据。

## 六、实拍（Chrome 无头，亲眼看过的四点）

- `.scratch/t401d/拍-桌面-1000x1600.png`（1000×1600，83271 字节）
- `.scratch/t401d/拍-窄屏-512x1400.png`（512×1400，72269 字节）

看点：① 桌面四张卡一行、不再挤成小卡；② 页头三件套 ＋ 三枚胶囊里没有 `·` 并列；③ 表格只剩
`日期／摄入／缺口` 三列、没有常量列；④ 口径行变短（页级一句、表下一句、图表下一句、脚注两句）。

## 七、工单第七条「不许做」逐条留档

| 不许做 | 本票做法 |
|---|---|
| 改公共层（`packages/base-render/**`） | 一行未碰（只调用它已有的产出器：`renderPageShell`／`renderKpiCard`／`renderChips`／`renderDataTable`／`renderCaliberLine`／`renderTocBlock`） |
| KPI 栅格列数、等宽栈、触摸目标、`viewport-fit=cover`／安全区、`prefers-reduced-motion` | 都不做——落点全在公共层，另开票；本页靠「KPI 六张减到四张」缓解桌面挤 |
| 碰 `src/shared/copyArea.ts`、`src/render/html.ts`、别家能力目录、唤醒词与命令声明 | 都没碰（改动路径见第十节） |
| 加吸顶 | 没加 |

**手机适配参照取用情况**：`.scratch/help-ref/HELP壳手机适配清单.md` 里，本轮只取了**页内可做**的两条——
① 页内可见文本变短（长行与并列串减少，窄屏折行压力随之下降）；② KPI 由 6 张减到 4 张（窄屏少一次折行）。
清单里其余各条（断点档位、字号降档、表格窄屏口径、导航横滚四件套、等宽栈、44px 触摸目标、安全区、
`prefers-reduced-motion`、`100dvh`）落点都在公共层，本轮不碰，留给那些条目各自的票。

## 八、如实记：两条红不在本票范围（未改测试凑绿）

1. **`home-lock-374` 第 4 例 `看今日体重概览`**：`exit 0（stderr：ERR 4: 未知失败：renderKpiGrid: cards 必须是非空数组）`
   —— 这是体重域的页面在别人的在途改动里把 KPI 卡数组取空了，与主页无关。**未改该测试。**
2. **`t401c` 第 1 例「可见文本零机器话」**：命中 `A1 全大写常量＝「JSON」`。来源是**在途件**：
   `packages/base-render/src/controls.ts`（复制菜单标签由 `text／json／csv` 改成 `纯文本／JSON／CSV`）＋
   `packages/skill-calorie/src/shared/copyArea.ts`（菜单提示改空串）。两件都不是本票的路径，**未改该测试**。
   本票产物里可见文本因此还带这段在途改动（老仓提示 `粘贴给 AI / 自己看` 等已退场）——重出产物时重编译了
   home 那一圈的编译产物，`base-render` 的编译产物是别的窗口重建的。
3. **包内告警线门**（`check-warning-line.mjs`）：`RESULT: 56/63`、exit=1，7 条红**全部落在在途件**
   （`src/weight/history.ts`／`review.ts`／`compare.ts`／`volatilityDoc.ts`／`src/render/trendMiscPortDocs.ts`／
   `src/workout/write.ts`／`src/diet/nutritionPortDocs.ts`）。本票两处源码：
   `src/home/homeDocs.ts` **347 行**（改前 314，未越 350）、`src/home/home.ts` **103 行**，都未触发第四步；
   台账（`packages/skill-calorie/AGENTS.md`）属他人路径，**未跑 `--sync`**。

## 九、GATE-RUN 对账声明

本席窗口内的运行条目**全部逐条列在** `docs/skills/skill-calorie/t401-样板-文本结构-门禁运行.md`
（受版本控制的对账导出件，源＝`.scratch/locks/gate-runs.log`）；下表只把**取为门禁证据的绿条**逐条声明，
红条一并列出以示对账完整（协议 §2.4 第 3 条：跑了却失败不得充作门禁证据）。
导出件按协议 §2.3 第 4 条**抄到抄录当刻为止**：刷新导出件那一笔提交与紧随的推送不在表内，
那一条已在回执里按 runId 点名。

**编译（4 条）**

- GATE-RUN runId=846b4df4-52bd-4d3c-a363-749d0fdbe341 cmd=`npx tsc -p .scratch/t401d/tsconfig.home.json` （exit=2，被在途件挡住，见下）
- GATE-RUN runId=f9a0f86e-833c-472c-b847-348250022706 cmd=`npx tsc -p .scratch/t401d/tsconfig.home.json` （exit=0）
- GATE-RUN runId=770af0ac-62c3-4f76-8f25-6646dacca949 （变异轮重编译，exit=0）
- GATE-RUN runId=40e1074d-6eb1-4096-b5e6-a0a187151191 （还原后重编译，exit=0）
- GATE-RUN runId=4a3f53a6-b1cc-45c7-9a0e-5219629995f0 cmd=`npx tsc -p .scratch/t401d/tsconfig.home.json` （终稿，exit=0）

**重出产物（4 条，cmd=`node .scratch/t401d/run.mjs`，exit 全 0）**：
GATE-RUN runId=fb03d3d9-5501-44d0-adad-31155d79fffd ／ GATE-RUN runId=e63d04e0-d022-409d-9f9d-8a085ed33007 ／
GATE-RUN runId=cbedbcbf-26e4-4c19-aaaf-71584a57c4e8 ／ GATE-RUN runId=9c9ddbe8-890d-4a2f-bde4-328ae23f6ffe

**探针与冗余读数（2 条，cmd=`pwsh -NoProfile -File .scratch/t401d/取证.ps1`，exit 全 0）**：
GATE-RUN runId=2d82b558-48e8-46c8-afa6-a900c9fa886d ／ GATE-RUN runId=0e40ca40-3a26-4380-9d02-c7e4d7613c3c

**测试**：GATE-RUN runId=d011a3ce-f1c4-4052-956d-317e78b66eab （守卫首跑，exit=0）；
GATE-RUN runId=11362e3c-1684-438f-9579-62c0d42bd9ee （还原后守卫，exit=0）；
批量 4 件三条（exit=1，含第八节那两条范围外红）：GATE-RUN runId=eef3e464-50d4-4ffc-8919-bac3dc19d05b ／
GATE-RUN runId=c5795da4-5393-4071-be3d-864231a20d02 ／ GATE-RUN runId=f0901468-285c-4e63-ab74-c67e23ce3398

**实拍（6 条）**：GATE-RUN runId=5fdeafa8-9af0-4199-950d-29422fc7e018 （旧脚本、相对路径写不进 →
记下原因后换脚本）／ GATE-RUN runId=0b14a53b-7822-4618-a85b-4779adbe69c8 ／
GATE-RUN runId=d97de6b6-0d4d-4802-9bbf-cb8bd0e0f7cd （中间轮）／
GATE-RUN runId=3f661a7d-5d73-4ee4-a23c-b07854181797 ／ GATE-RUN runId=5df4fad2-d319-40fb-b01e-cafd084d0ca8 （终稿两张，exit 全 0）

**变异轮（1 条红，那是设计要的红）**：GATE-RUN runId=76760b51-911d-43d5-b618-0484f51c046a
（守卫 exit=1，`AssertionError: 可见文本里还有 ·：2 处`）

**其它（3 条）**：GATE-RUN runId=029ba80d-7f81-42f3-9caa-57b5a978cb7d ／
GATE-RUN runId=1ff722ce-0d7e-47e4-aef2-c6acfa463668 （整包 `tsc -b`，被在途件的语法／类型错挡住，exit=1）、
GATE-RUN runId=650b80d3-3222-44b3-8a40-d14ea256dc37 （告警线门，exit=1，红全在在途件）

**编译路径的如实交代**：整包 `tsc -b packages/skill-calorie/tsconfig.json` 三次都被**别人的在途件**挡住
（先 `src/weight/volatilityDoc.ts` 语法错、后 `weightCompare2.ts` 类型错），故本票改用作用域编译
`.scratch/t401d/tsconfig.home.json`（`include` 只收 `src/home`，仍写进包自己的 `dist/`，`composite/incremental` 关掉、
不碰别人的 `.tsbuildinfo`）。产物与测试都走这份编译产物；`base-render`／别的域的编译产物是各自窗口重建的。

## 十、路径

- 改动（源码）：`packages/skill-calorie/src/home/homeDocs.ts`（314→347 行）、`packages/skill-calorie/src/home/home.ts`（99→103 行）
- 新增（测试）：`packages/skill-calorie/test/t401-可见文本守卫.test.mjs`
- 改动（测试锁）：`packages/skill-calorie/test/t401c-页面机器话探针.test.mjs`（caption 那条，理由见第五节）
- 本件 ＋ 对账导出件：`docs/skills/skill-calorie/t401-样板-文本结构-证据.md`、`docs/skills/skill-calorie/t401-样板-文本结构-门禁运行.md`
- 产物与实拍（工作副本，未入仓）：`.scratch/t401d/`（三页 HTML、两张 PNG、探针与冗余读数、`run.mjs`、`取证.ps1`、`拍照.ps1`、`count-redundancy.mjs`）

## 十一、#401e 页内文字修正（审查必改项 #6／#7）

**判定：#6（R-a～R-f）与 #7 三条全部落页；同一类写法已统一，逐条次数复算在案。**

### 1. 逐条改了什么（旧 → 新，只看关键短语）

| 审查条 | 旧文（改前） | 新文（改后） | 落点 |
|---|---|---|---|
| R-a | 胶囊「窗内有记录 5/7 天」＋ 脚注「窗内共 5 条记录。」 | 胶囊「有记录 5/7 天」；脚注**整条删** | `headBadges()`／页脚口径行 |
| R-b | 三张卡说明行「目标 1800 卡／目标 150 克／目标 2000 毫升」 | 「目标 1800／目标 150／目标 2000」（单位在值那行已给） | `goalDetail(goal, unit?)` |
| R-c | caption「📊 按日汇总（单位：卡），目标 1800 卡」 | caption「📊 按日汇总」＋ 表头「摄入（卡）／缺口（卡）」；目标只留「今日摄入」卡 | `renderDataTable` 入参 |
| R-d | 结论句「热量在目标内，还能吃 611 卡。」 | 「热量在目标内，距目标还差 611 卡。」 | `conclusionText()` |
| R-d | 缺口卡说明「比消耗少这么多」 | 「相对消耗」（与结论句各写各的参照） | KPI 卡说明行 |
| R-e | 副题「2026-09-01 ~ 2026-09-07」 | 「2026-09-01 至 2026-09-07」 | `subtitleText()` |
| R-f | caption 里那条「（单位：卡）」 | 单位归表头槽位（消除）；「（今日）」留日期格 | 同上 |

三处**超出审查点名字面**的同类收口（理由都写在源码同处注释里）：

1. 饮水卡去「毫升」时，把摄入卡「卡」、蛋白卡「克」一并去了——审查只点名「毫升」，但三张同类卡留两种
   长法就是新的不一致。判据：**说明行的单位与值那行重复即删**。
2. 脚注整条删、而不是只删「窗内」：那个 N 取自 `d.week.loggedDays`（**天数**），与胶囊那枚同源同值，
   却写成「条记录」（种子里 7 天有 5 天有记录、共 16 条 food_log 行）——同一个数一页说两遍，删第二处比改词干净。
3. 括号统一口径「**括号只装所注文本自己的属性**」：「（单位：卡）」装的是**列**的属性 ⇒ 归表头；
   「（今日）」装的正是本行日期 ⇒ 留。公共层 `renderDataTable` 没有行标记槽（色点／加粗要动公共层），本票不动。

### 2. 次数复算（同一脚本跑改前／改后产物，剥掉复制载荷）

脚本 `.scratch/t401e/数重复.mjs`。改前产物＝`.scratch/t401d/看今日主页.html`（读数于 9:27:42 取；
该文件 9:27:53 被别席重出覆写，原始读数已落 `.scratch/t401e/重复-改前.txt`）。

| 条 | 关键词 | 改前 → 改后 |
|---|---|---|
| R-a① | 「窗内」 | 2 → 0 |
| R-a② | 「有记录 5/7 天」＋「共 5 条记录」 | 1＋1 → 1＋0 |
| R-a③ | 「有记录 」＋「条记录」（同一个数两种说法） | 1＋1 → 1＋0 |
| R-b | 「毫升」 | 2 → 1 |
| R-c① | 「1800」 | 2 → 1 |
| R-c② | 「单位：卡」 | 1 → 0 |
| R-d | 「还能吃」／「距目标还差」 | 1／0 → 0／1 |
| R-e | 可见文本里的 `~` | 1 → 0 |
| R-f | 「（今日）」／「摄入（卡）」 | 1／0 → 1／1 |

### 3. 探针（`.scratch/sep-audit/probe.mjs`，节点级三条读数）

三页（今日／本周／本月，同一份固定种子）**全 0／0／0**、exit 0：

```
看今日主页  含 · 0 处   含 ；0 处   ≥3 段并列 0 处   命中合计 0 处
看本周主页  含 · 0 处   含 ；0 处   ≥3 段并列 0 处   命中合计 0 处
看本月主页  含 · 0 处   含 ；0 处   ≥3 段并列 0 处   命中合计 0 处
```

### 4. 测试读数

- `test/t401-可见文本守卫.test.mjs`：**2／2 绿**（含变异自证）。变异锚点按 #7 跟上屏文本走了（副题 `~`→「至」），
  但**三条读数与变异期望值一字未动**——锚点是夹具，不是判据。
- `test/render-t8.test.mjs`：**7 pass／0 fail**。
- `test/home-lock-374.test.mjs`：**8 存 1 红**。红的仍是第 4 例「看今日体重概览」，这次红在**空库那一档**
  （`exit 0 !== 4`，体重页空库不再阻断）——`src/weight/*` 属在途件，与主页无关，**未动该测试**。
- `test/t401c-页面机器话探针.test.mjs`：**3 存 2 红**，两条都不在本票范围：
  ①「可见文本零机器话」命中 `JSON`（复制菜单标签，`base-render/src/controls.ts` 在途改动）——**改前产物同样命中**；
  ②「视觉升级四件在场」报「缺结论条」：**别席 #507 正在同一件里**把结论条形状搬进公共层 `renderConclusionBar`，
  新产物不再输出旧的 `class="ilife-block-page-shell-conclusion" style="…"`，那条锁随之过时，归 #507 席。
  本票在该件里改的三处（窗口串 needle 跟「至」、caption 三锁、变异锚点「缺口（卡）」）均已转绿。

### 5. 实拍

`.scratch/t401e/拍-桌面.png`（Chrome 无头，`--window-size=1000,1600`，实测 1000×1600，seed 日 2026-09-07）。
亲眼看过的六点：副题「2026-09-01 至 2026-09-07」；胶囊「有记录 5/7 天／连续记录 3 天」；结论条「距目标还差 611 卡」；
KPI 四卡「值带单位、说明行只写目标／相对消耗」；表 caption「📊 按日汇总」＋表头「摄入（卡）／缺口（卡）」＋今日行「（今日）」；
页脚只剩「数据来源：饮食记录，运动记录，每日目标。」一条。

### 6. 并发与台账（如实记）

- **同件并发**：#507 席 9:27:31 也改了 `packages/skill-calorie/src/home/homeDocs.ts`（结论条产出器接线），
  两席改动在盘上并存（本席的文本改动一行未丢）；故本席 `git add` 的这一版同时带着 #507 的那几行。
- **告警线**：本件改后 **350 行**（LF 口径，未越线）。台账 `packages/skill-calorie/AGENTS.md` 里
  `src/home/homeDocs.ts` 那行（他席 `--sync` 在本席编辑途中自动补出，当时读到 360）当场实测已对齐 350。
  台账属他人路径，**本席未跑 `--sync`、未改台账**。

### 7. 路径

- 源码（本票只动这一件源码）：`packages/skill-calorie/src/home/homeDocs.ts`
- 测试（只动夹具／锚点，判据未动）：`test/t401-可见文本守卫.test.mjs`、`test/t401c-页面机器话探针.test.mjs`、
  `test/visible-text-probe.mjs`（`rangeOccurrences` 多一个可省的 `sep`，缺省仍是 ` ~ `，photo 两页不受影响）
- 产物与读数（工作副本，未入仓）：`.scratch/t401e/`（`成品/` 三页 HTML、`拍-桌面.png`、`探针-*.txt`、
  `重复-改前.txt`／`重复-改后.txt`、`run.mjs`、`数重复.mjs`、`拍照.ps1`、`tsconfig.home.json`）

## 十二、#401g 截图复审「页内必改六条」（#4～#9）

依据 `.scratch/t401f/review/复审报告.md` §三 必改清单里**属于页内**的六条（#4 第 4 卡档位槽／#5 `（今日）` 行标记／
#6 日期长法／#7 emoji／#8 三条口径行／#9 段标题长法）。#1～#3（图内字号、y 轴刻度与网格线、表格列宽）与 #10
（手机端吸顶／安全区）属别席，本票**一行未碰**；公共层 `packages/base-render/**` 全程只读。

**重出产物**：`node .scratch/t401g/出片.mjs` → `.scratch/t401g/看今日主页.html`（固定种子库 `SEED_TODAY=2026-09-07`，
74358 字节）。编译走锁：`node tooling/run-locked.mjs --ticket 401 -- node node_modules/typescript/bin/tsc -b
packages/base-render packages/skill-calorie`（exit=0；runId=b5814b19-3454-4644-a68a-8a6dacbbfbce）。

### 1. 逐条旧 → 新（能只给关键片段的就给片段）

| # | 旧 | 新 | 机器读数（改后产物） |
|---|---|---|---|
| 4 | 第 4 卡 `detail:'相对消耗'`、**无 `status`** ⇒ 四卡里只有它没有档位徽章（产物 3 枚） | 补**方向胶囊** `deficitDirection()`：`消耗多于摄入`（ok）／`摄入多于消耗`（warn）／`两者持平`（中性灰 empty）；`相对消耗` 那半截裸名词撤掉，参照改由徽章说 | `kpi-card-badge` 4 枚（旧 3 枚）；缺口卡那枚 `<span class="ilife-status-badge ilife-status-badge-ok">消耗多于摄入</span>`；可见文本「相对消耗」1→**0** |
| 5 | `s.date + '（今日）'`（行标记用括号拼进日期文本） | **保留现状**并写明缺口：`DataTableInput`（`blocks.ts:588-593`）只有 columns／rows／caption／emptyText 四槽，**没有行标记槽**，单元格只吃纯文本（行前置色点／首列加粗都落不进去）⇒ 改为**需公共层加槽** | `<td class="ilife-block-data-table-cell-left">09-07（今日）</td>`（1 处，与改前同法） |
| 6 | 同页三种：副题 ISO 区间／表内 ISO 单日／图轴 `MM-DD`（`s.date.slice(5)`） | **两条规则各有其位**：① 窗口区间仍在页头一处写 ISO（它是窗口标识：跨年在场读得出年份、与复制载荷里的命令对得上，且入仓探针锁「可见文本恰 1 处」）；② **单日一律 `MM-DD`**，表内与图轴同走新函数 `dayLabel()`（跨年补两位年份，同 `analysis/multiTrendPage.ts:145-152` 先例） | 可见文本 ISO 串只剩副题那 1 处（`2026-09-01 至 2026-09-07`）；表内 7 行 `09-07（今日）…09-01`；图轴刻度 7 个同源同值（`09-01`／`09-07`） |
| 7 | emoji **9 枚实例**：4 枚段标题 ＋ 同一组 4 枚在导航再印一遍 ＋ 结论句 💡 | 只留**段标题**一处、每块一枚（导航只给名、结论句去 💡）；图标与名字同走 `SECTIONS` 一份清单 | 可见文本 emoji **9→4 枚**（🔥📈📊💰 各 1）；`<nav>` 内图标 **4→0**；`💡` **1→0** |
| 8 | `虚线＝周均摄入，只算有记录的天。`／`缺口＝消耗 − 摄入，正数是缺口。`／`缺数一律写 —。`（页首第 4 行） | ① `这条虚线是周均摄入，只算有记录的天。`；② `缺口是消耗减摄入的差。`（去掉重复的「缺口」与 `＝`／`−`）；③ `缺数一律写 —` 并入页脚来源行：`数据来源：饮食记录，运动记录，每日目标。缺数一律写 —。`（页首只剩胶囊行＋结论条） | 可见文本 `＝` **2→0**、`−` **1→0**；页级口径行节点 3 条（图下／表下／页脚），产物片段 `片段.txt` |
| 9 | 段标题三种长法：`:240`、`:320` 手写 `<h2>`（借 `kpi-card`／`copy-block` 两个**区块内部**类名）＋ `renderChartBlock({title})` 产出器＋表格 caption | 文本形状**一处派生**（`secTitle(SECTIONS)`），产出收敛成：产出器 ×3（chart `title`／**copy `title`**／表格 caption-ish 槽）＋ 手写 `<h2>` ×1（「今日速览」）；「数据与日志」改走 `copyArea` 的 `title` 位，由公共层 `renderCopyBlock` 出同一个类名 | 手写 `<h2>` **2→1**；`block-copy-block-title` 那枚现在来自产出器（片段 `<h2 class="ilife-block-copy-block-title">💰 数据与日志</h2>`）；四枚段标题字号字重仍由 #507 的 CSS 同尺（15／700） |

### 2. 探针与守卫（机器读数）

- `node .scratch/sep-audit/probe.mjs .scratch/t401g/看今日主页.html` → **exit 0**：
  `【行级】含 · 0 行 含 ；0 行 ≥3 段并列 0 行 命中合计 0 行`／`【节点级】0 ／ 0 ／ 0 ／ 0`（三数字全 0）。
- `test/t401-可见文本守卫.test.mjs`：**2／2 绿**（含变异自证）。本票改了**一处夹具锚点**（判据一字未动）：
  取 `缺数一律写 —。` 那条独立口径行的变异锚点随 #8 的并入换成页脚新串 `数据来源：…。缺数一律写 —。`
  ——**是断言过时、不是违反**：该测试锁的是「塞一处 `；` ⇒ 读数 1」与三条读数全 0，两条都仍绿。
- `test/render-t8.test.mjs`：**7 pass／0 fail**。
- `test/home-lock-374.test.mjs`：**8 存 1 红**，红的仍是第 4 例「看今日体重概览」（这次红在**空库那一档**
  `exit 0 !== 4`，`src/weight/*` 属在途件）——与主页无关，如实记，未动该测试。
- `test/t401c-页面机器话探针.test.mjs`：**3 存 2 红**，两条都**不在本票范围且改前产物同红**（取证见
  `.scratch/t401g/对照.txt`：改前 `.scratch/t401f/看今日主页.html` 与新产物一样命中 `JSON`、一样没有
  `ilife-block-page-shell-conclusion`）：① 机器话命中复制菜单标签 `JSON`；② 结论条锁还指着 #507 搬走前的
  旧类名 `ilife-block-page-shell-conclusion`（现由 `renderConclusionBar` 出 `ilife-block-conclusion`）。
  本票在该件里**一行未动**。

### 3. 实拍（亲眼看）

`.scratch/t401g/拍-1000.png`（Chrome 无头 `--window-size=1000,1700`，seed 日 2026-09-07）。看过的点：
副题仍是 ISO 区间、其下三枚页头胶囊；结论条无 💡；导航四枚**不带图标**、区块标题四枚**带图标**（一行一枚）；
四张 KPI 卡**都有徽章**（接近目标 66%／偏少 39%／达标 100%／消耗多于摄入）；图下口径「这条虚线是周均摄入…」；
表内日期 `09-07（今日）` 起倒序、缺数 `—`、表下口径「缺口是消耗减摄入的差。」；页脚一条来源行含缺数口径。
两处**如实记的残留**：① 第 4 卡比同排少一行 `detail`，它的徽章比另三枚高约一行；② 页面底部（页脚之后）有大片空白。

### 4. 行数与台账

本件改后 **348 行**（LF 口径；改前 350）——**未越线**，故不触发第四步、不动台账。台账
（`packages/skill-calorie/AGENTS.md`）里 `src/home/homeDocs.ts` 那行现读「当场实测=348」，与本件实况一致
（那一列是**别席同步器**写进去的；本席**未跑 `--sync`**，也未改台账任何一格）。结论列里「LF=350」的措辞成了旧值，
按「只在本件越线时才动那一行」的规矩本席未改。

### 5. 需公共层加槽（三条，均为本票不越权动公共层而留下的缺口）

1. **按日表的行标记槽**：`DataTableInput` 四槽里没有「行标记」（行前置色点／首列加粗）⇒ 今日行仍靠 `（今日）` 文本。
   建议签法：`rows[i].marker?: string`（出 `<td class="…cell-marker">`）或行级 `data-row-mark="today"` ＋ CSS。
2. **页面级段标题产出器**：公共层没有 `renderSectionTitle` 这类件，且 `renderKpiGrid`（`blocks.ts:570-574`）
   不吃 `title` ⇒ 「今日速览」那枚 `<h2>` 只能页内手写（全页唯一一处）。建议签法：`renderSectionTitle(text: string)`
   出 `<h2 class="ilife-block-section-title">`，样式随 `pageShell` 区落盘，与 `chart-block-title` 同尺；
   表名（`data-table-caption` 13px）与段标题（15px）同档那半条同属表格区，一并归别席。
3. **KPI 卡底部对齐**：`block-kpi-card`（`blocks.ts:1429-1435`）是普通块、不是 flex 列，徽章（`margin-top:8px`）
   不贴底 ⇒ 少一行 `detail` 的卡徽章天然高一行（上面实拍残留①）。建议签法：卡片 `display:flex;flex-direction:column`
   ＋ 徽章 `margin-top:auto`。第四条替代方案：若要求第 4 卡也有一行「参照 ＋ 数字」（`消耗 2876`），那要数据面
   在 `HomeData` 上补 `burnToday`——属 `home.ts`（本票写集外），记为开票建议。

### 6. 写集与并发（如实记）

- 源码**只动** `packages/skill-calorie/src/home/homeDocs.ts`；测试只动 `test/t401-可见文本守卫.test.mjs`
  **一处夹具锚点**（理由见 §2）；本文件追加本节。除此之外**一行未动**（公共层、他席在途件、
  图表／表格那两条必改均未碰）。
- 工作副本（未入仓，`.scratch/` 在 `.gitignore` 里）：`.scratch/t401g/`（`出片.mjs`／`查.mjs`／`拍.mjs`／
  `对照.mjs`／`JSON在哪.mjs`、`看今日主页.html`、`拍-1000.png`、`读数.txt`、`片段.txt`、`探针.txt`＋四份测试输出）。
- 入仓路径：`packages/skill-calorie/src/home/homeDocs.ts`、`packages/skill-calorie/test/t401-可见文本守卫.test.mjs`、
  本文件。

### 7. 折线左轴数字（第二席·调用方补参）

- **判定**：主页「每日摄入」折线没有 y 轴数字，属**本页漏传参数**（公共层本来就能出刻度），非公共层缺能力。
- **读到的真实签名**（当刻，公共层正在改，行号与来料不符）：
  - 声明 `packages/base-render/src/spec/charts.ts:98` `readonly yTicks?: number | false;`（在 `LineChartOptions` 内）；
  - 收敛 `packages/base-render/src/charts.ts:492-495` `tickCount()`：非数 → 0 条，否则 `Math.max(2, Math.min(6, Math.round(v)))`
    ⇒ **合法取值是 2–6 的整数或 `false`**，`3` 在域内；
  - 缺省 `charts.ts:817`（来料给的是 `:787`）：`opts.yTicks === undefined ? false : opts.yTicks`——**本席不动这条缺省**；
  - 出字 `charts.ts:516-533` `ticksSvg()`（`<line class="…charts-ytick">` ＋ `<text class="…charts-tick">`），
    折线在 `charts.ts:1207` 调它，`tickN` 同时喂 `charts.ts:1204 gridSvg(frame, tickN)` ⇒ 标注与网格线**同一个 `tickN`**。
  - 来料给的行号（`1171`／`493`／`787`）与当刻读到的（`1207`／`492`／`817`）不一致：公共层文件当刻已由 2,027 行涨到 2,033 行。
- **改了**：`packages/skill-calorie/src/home/homeDocs.ts:252` 新增一行 `yTicks: 3,`（在 `connectNulls` 与 `markLine` 之间）。
  同包先例一律 `yTicks: 3`：`analysis/reportDocParts.ts:53`、`analysis/multiTrendPage.ts:307/328/436/457`、
  `render/trendMiscPortDocs.ts:174/250/269`；`test/analysis-deficit-385.test.mjs:306-309` 把「`yTicks:3` ⇒ 3 条
  `<text class="ilife-charts-tick">`」钉死（同一张票面里缺口页就是这么补的）。
- **实测读数**（一次性 `node -e` 读渲染出来的 HTML，未截图、未跑构建链）：seed（`docs/research/t81-seed.mjs`，
  `SEED_TODAY=2026-09-07`）→ `buildHomeDoc(buildHomeData(db))` 得 `<text class="ilife-charts-tick">` **3 条**
  （`87.66`／`669.5`／`1251.34`），网格线 `charts-grid` **3 条** ⇒ 一一对齐。
- **两测试读数**：`node --test packages/skill-calorie/test/render-t8.test.mjs` **7 通过／0 失败**；
  `node --test packages/skill-calorie/test/t401-可见文本守卫.test.mjs` **2 通过／0 失败**（三条读数仍全 0）。
- **行数**：348 → **349**（LF 口径，**未越线**）。台账（`packages/skill-calorie/AGENTS.md`）本件那行「当场实测」仍读 348
  ⇒ 需**别席**跑 `node packages/skill-calorie/scripts/check-warning-line.mjs --sync` 把那一格改成 349；台账在本席写集外，未动。
- **如实记一条口径选择**：刻度文字现在**不取整**（`87.66`／`669.5`／`1251.34`）。按票面只补 `yTicks`，未加缺口页那一组
  `labels: 'select'` ＋ `format`（`reportDocParts.ts:53-54`）；要不要给整数刻度属**可见文本决策**，留编排者裁。
- **入仓路径（本席）**：`packages/skill-calorie/src/home/homeDocs.ts`、本文件。

