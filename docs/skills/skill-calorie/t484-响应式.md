# #484 · 场景 09 十页响应式适配（证据）

一句话判定：六张带图页＋两张无图页在 390／768／1440 三档的横向溢出**逐格归零**（改前 12 格非零＝`+40…+1355px`，改后 0 格非零），每张 `<img>` 都带宽度约束且三档都放得进容器；详情页两条命令块走公共层 `renderPreBlock` 的类名（页面已无裸 `<pre>`）、翻页链接触摸区 44px、GIF 舞台 `width:min(320px,100%)` 四条各有正反两向机器读数；八件 60 例回归全绿。`calorie.help.center`（HELP 速查页）三档仍溢出，但它**不在本票归零面**（`src/render/html.ts` 只读、本票一行未动），**归 #488**（该票已在途接手）；工具已把它标为**范围外**、读数照打不藏。

## 〇、判据面（谁归零、谁不归零）

- **本票归零面＝10 页 × 3 档 ＝ 30 格**：`calorie.photo.list`／`calorie.photo.detail`／`calorie.photo.compare`／`calorie.photo.gif`／`calorie.view.photo-picker`／`calorie.photo.add`／`calorie.photo.remove`／`calorie.photo.tag`／`calorie.view.photo-log-wizard`／`calorie.view.gif-planner`。判据＝每格「文档无横向溢出（`docScrollWidth − innerWidth == 0`）」**且**「该页每张 `<img>` 都放得进容器（`img-not-fit == 0`）」；两条任一破即该格失败。
- **不在本票归零面：`calorie.help.center`（HELP 速查页）。** 它三档仍溢出（+844／+1636／+964；这是**本票作准面**的读数，见 `.scratch/t484/pages-after`），根因是 `src/render/html.ts` 的 `helpRowHtml` 裸 `<pre>`＋整页无 `<style>`，而本票写集不含该件（只读核对，一行未动）。**该页归 `#488`（该票已在途接手）**：当刻工作树里 `#488` 的在途件（`src/photo/help.ts` 换走新整页件 `photo/helpDoc.ts`）已进 `dist`，`--seed` 复跑同一页已量到**三档 0**——归零确由 `#488` 兑现，本票不认领。本工具以 `--exclude calorie.help.center` 显式标范围外：读数照打、JSON 里 `inScope:false`、汇总单列 `scopeOutFailed`——**范围外不等于放宽，只是不进本票退出码**。

## 一、根因（当刻复核读数）

| 现 象 | 根因 | 复核方式 |
| --- | --- | --- |
| 照片按天然像素上屏，页被撑破 | `packages/base-render/src` 全目录**零 `<img>` 规则**（无 `img { … }` 选择器） | 全目录 20 个 `.ts` 逐件 grep `<img`／`img{`：命中 0 |
| 390 档越界、1440 档仍越界 | 同上：图宽＝夹具天然宽 900／1200／2048px，远超容器 | §三 读数表（`img-not-fit` 列每档都有） |
| 翻页链接在手机上只有 20px 高 | 裸 `<a>` 无触摸区约束（低于 44px 下限） | 改前详情页 `<a>` 无 `min-height` |
| GIF 只占屏约 16% | 64×64 的 GIF 原样上屏，舞台无宽度 | 改前 GIF 页 `img` 无任何尺寸约束 |
| 详情页 122px 溢出（前一版取证） | 那时是两处**裸 `<pre>`**（无类名不中样式） | **当刻已不存在**：#473 已改走 `renderPreBlock`；本票三档复量 detail＝0／0／0 并做成恒常断言（测试 ②） |
| 字号 <14px | 六件里**零** `font-size` 声明（grep 六件＝0 命中）；12px 的字全来自公共层区块（`renderChips` 与命令块） | 只报告不改（见 §六-6） |

## 二、逐条改法（写集六件）

| 件 | 改法（逐字） |
| --- | --- |
| `src/photo/galleryDoc.ts` | 图卡 `<img … style="max-width:100%;height:auto" />`（画廊不再按天然像素铺开） |
| `src/photo/compareDoc.ts` | 两卡容器 `<div style="display:flex;flex-wrap:wrap;gap:12px">`；每张卡 `<figure … style="flex:1 1 240px;min-width:0">`（宽屏并排、窄屏自动上下排列）；图同上约束 |
| `src/photo/pickerDoc.ts` | 候选缩略 `/style="max-width:100%;max-height:160px;height:auto;object-fit:cover"/`；快照 `/style="max-width:100%;max-height:60vh;height:auto;object-fit:contain"/` |
| `src/photo/receipt.ts` | 存照逐张图与删前快照图 `/style="max-width:100%;max-height:240px;height:auto"/`（两处） |
| `src/photo/gifDoc.ts` | 舞台 `<img … style="width:min(320px,100%);height:auto;image-rendering:pixelated" />`（不动 `gif.ts` 的 `maxEdge`） |
| `src/photo/viewerDoc.ts` | 翻页 `<a>` 共用一个常量 `NAV_LINK_STYLE='min-height:44px;display:inline-flex;align-items:center'`（两邻同约束）；两处裸 `<pre>` 已是 `renderPreBlock`，本票未再动 |

新工具：`scripts/measure-responsive.mjs`（350 LF；本包告警线判据为 `LF > 350`，故未超线）——本票的验收工具，真浏览器三档读数（`--seed` 自造页／给路径量现成页／`--dir` 量一目录／`--exclude` 显式标范围外件）。

## 三、三档读数（改前 → 改后，逐件）

工具：`node packages/skill-calorie/scripts/measure-responsive.mjs --seed --exclude calorie.help.center --json …`；
夹具＝隔离临时库＋3 张**真 PNG**（天然像素 900×1200／1200×1600／2048×3072，文件字节小故不触发 1 MiB 预算退让）。
读数＝`document.documentElement.scrollWidth − window.innerWidth`（单位 px）；括号＝该页三档里「放不进容器」的图数（改前→改后）。

| 页 | 390 档 | 768 档 | 1440 档 | 图放不下（390／768／1440） |
| --- | --- | --- | --- | --- |
| 看身材照 `calorie.photo.list` | 0 → **0** | +492 → **0** | +40 → **0** | 2／2／2 → 0／0／0 |
| 详情 `calorie.photo.detail` | 0 → 0 | 0 → 0 | 0 → 0 | 0 → 0 |
| 对比 `calorie.photo.compare` | 0 → **0** | +492 → **0** | +40 → **0** | 2／2／2 → 0／0／0 |
| GIF 结果 `calorie.photo.gif` | 0 → 0 | 0 → 0 | 0 → 0 | 0 → 0 |
| 候选 `calorie.view.photo-picker` | +544 → **0** | +1340 → **0** | +888 → **0** | 4／4／3 → 0／0／0 |
| 存照回执 `calorie.photo.add` | 0 → **0** | +192 → **0** | 0 → **0** | 1／1／1 → 0／0／0 |
| 删照回执 `calorie.photo.remove` | +559 → **0** | +1355 → **0** | +903 → **0** | 1／1／1 → 0／0／0 |
| 标签回执 `calorie.photo.tag`（无图） | 0 → 0 | 0 → 0 | 0 → 0 | — |
| 向导 `calorie.view.photo-log-wizard`（无图） | 0 → 0 | 0 → 0 | 0 → 0 | — |
| 规划器 `calorie.view.gif-planner`（无图） | 0 → 0 | 0 → 0 | 0 → 0 | — |
| HELP `calorie.help.center`（**范围外**） | +844 → +844 | +1636 → +1636 | +964 → +964 | —（无图） |

合计：**在判据面的 10 页 30 格里，改前 12 格非零 → 改后 0 格非零**（`OVERFLOW-ZERO pages=11 cells=33 failed=0 scopeOutFailed=3`）。
390 档三处「文档级 0 但图放不下」的说明：那些页的容器给了 `overflow` 兜底，图被**裁在容器外**（不是没溢出，是溢出被藏了）——故本工具另报 `img-not-fit`，它同样按判据计红（改前 add／compare／list／remove／picker 三档全红）。

读数 JSON：`.scratch/t484/before.json`（改前）、`.scratch/t484/after.json`（改后）；页冻结件：`.scratch/t484/pages-before/`、`.scratch/t484/pages-after/`。
成对读数的编排（先还原六件到 HEAD → 构建 → 量 → 复原 → 构建 → 量）：`.scratch/t484/before-after.mjs`（`try/finally` 无条件复原，盘上另留 `.scratch/t484/mine/` 备份）。

## 四、机器测试读数

- 新件 `test/photo-responsive-484.test.mjs`：7 例（① 每张 `<img>` 带宽度约束〔七页图数逐一钉住〕／② 详情页命令块走 `renderPreBlock` 类名且无裸 `<pre>`／③ 翻页两条链接都 ≥44px／④ GIF 舞台 `width:min(320px,100%)`／⑤ 本票三个图上限与对比卡可折行／⑥ 形状与 `PHOTO_LIST_PAGE_MAX_BYTES` 冻值／⑦ 变异自证）**7 绿**。
- 回归八件合跑（含本票新件）：`ℹ tests 60 ／ pass 60 ／ fail 0`，`TESTS-EXIT=0`。

变异两行（⑧ 例 ⑦ 的原话）：

```
MUTATION-RED #484 四处改坏都必红：图片宽度约束=看身材照：这张 `<img>` 没有宽度约束（天然像素直接上屏）｜ 裸 pre=详情页出现裸 `<pre>`（无类名即不中样式、窄屏必溢出）｜ 触摸区=翻页链接缺触摸区下限（手机上是 20px 高）｜ GIF 舞台=GIF 图缺 `width:min(320px,100%)` 约束
MUTATION-GREEN #484 改回必绿：四条闸门全过（list 图数=3）
```

## 五、GATE-RUN 单行

```
RUN ticket=484 runId=eb358e75-7785-4a46-af9d-3a942969a92a cmd="node --test …photo-shape-341／photo-budget-438／photo-gif-page-352／photo-shape-read-281／photo-picker-283／photo-receipt-docs-282／wizard-86／photo-responsive-484" waitedMs=0 exit=0 pid=21660 at=2026-09-14T17:51:58.421Z
```

（同票：构建 `pnpm build` runId `63644bc4-f1fb-4ff1-a616-6652f5712b35` exit=0；改后度量 runId `c9690327-b883-49f7-821e-daed5e27a6ca` exit=0，**复跑** runId `a98dcacb-99b9-45d1-ac52-5f6851648274` exit=0、逐页字节与读数逐格相同（可复跑）；改前度量 runId `a0e40036-8537-430f-9bbb-2ef76ceae122` exit=1〔**该 1 就是改前的病**，不是工具坏〕；`gen-cli.mjs --check` runId `e9b1722e-0338-4fe2-a739-da6cda273d85` exit=0。）

## 六、未做项与下一手

1. **`calorie.help.center` 三档仍溢出（+844／+1636／+964）**：该页走 `src/render/html.ts:206` 的 `helpRowHtml`——裸 `<pre>` 且整页**没有 `<style>`、没有 doctype**（实测 `styleCount=0`），长命令原样 `white-space:pre` 撑破版面。本票写集不含该件，工具以 `--exclude calorie.help.center` 显式标为范围外（不是放宽：读数照打、JSON 里 `inScope:false`、汇总单列 `scopeOutFailed`）。**归该件改版票。**
2. **`src/render/html.ts` 裸 `<pre>` 只读核对结论（报告不动）**：`:209`＝`helpRowHtml`（被 `renderPhotoHelpHtml`／`renderHelpLookupHtml` 用）**真裸 `<pre>`**；`:239`／`:240`＝`renderErrorHtml`（失败回执）**两处真裸 `<pre>`**。三处都无类名 → 不中 `blocksCss` 的 `pre-wrap`／`overflow-x`，正是第 1 条的溢出来源。本票一行未动。
3. 公共层 `img` 兜底（全页 `img{max-width:100%}` 一类）与 `renderDataTable` 单元格徽标：另票（本票只在卡路里照片六件里逐张加约束）。
4. `gif.ts` 的 `maxEdge` **没动**（会改 GIF 产物体积与既有断言）；舞台放大后是 320px 的**像素化**画面，不是更高分辨率的合成。
5. 十页基线重落：本票改了页面内联样式 → `gen-photo-baseline.mjs --check` 的 `calorie.photo.*`／`view.photo-picker` 相关 sha 必变；本票**禁** `--write`，重落归收口席。
6. 字号：六件零 `font-size` 声明，<14px 的可见字全在公共层区块（chip 12px／命令块 12px）——**只报告不改**（改它＝改公共层，本票写集外）。
7. 环境注（不掩盖）：本票窗口内他席在途件多次重编 `dist`（`src/shared/docPage.ts` 与他席的 `src/weight/*`）；`pnpm build` 一度因 `src/weight/receipt.ts` 的 TS 错 exit=2（随后自行恢复，末次 exit=0）；`check-warning-line.mjs` 当刻仍红在**他席件**（`src/weight/history.ts` 683→741、`src/weight/review.ts` 600→647、`src/weight/compare.ts` 558→571 台账陈化）——本票新增／改动件**无一条**出现在它的 RED／OVER 名单里。
8. **方法学警告（审查席实测，复跑必记）**：**还原源码后 `tsc -b` 增量编译会因 mtime 未变而跳过重编、`dist` 里残留变异版 → 必须 `tsc -b --force`。** 踩上它就会拿到**假红／假绿**读数（盘上源码已复原，量到的却是上一轮的变异产物）。本票「还原六件 → 构建 → 量 → 复原」的成对口径因此**一律**走 `tsc -b --force`（本票在改前量读数时正是靠它才拿到干净基线）。
9. **工具硬化两条（审查席点出，属后手、本轮未改，留作收口后的工具票）**：① `fits` 判据拿**父元素 `clientWidth`** 当容器内容宽，比真实可用宽略宽一格（边角图可能被误判「放得下」）；② 工具**没有**「页数应 11／判据面页数应 10」的自断言——种页失败或 `--dir` 漏进文件时读数会**静默变小**而不报警。两条都不影响本票结论（30 格读数逐格可复跑），但都会削弱工具将来的信噪比。
