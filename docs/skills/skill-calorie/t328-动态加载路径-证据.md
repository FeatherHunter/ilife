# t328 动态加载路径证据

## 0 自复算与开工条件
- `git rev-parse HEAD`＝`61caf456cda1e80169a5b418022497771b347590`，`git rev-parse origin/master` 同值，分支 `master`，`git status --porcelain` 空，`git status` 为 `nothing to commit, working tree clean`。
- 已读 `docs/agents/structure.md`、`docs/agents/wording.md`、`docs/agents/命令登记纪律.md`；终端为 better sidebar 终端 `t328-git-check`。

## 1 复验读数（逐字）
- 读 `packages/skill-calorie/src/photo/helpLookup.ts:11` 为 `export const PHOTO_HELP_MODULE = 'skill-calorie/dist/photo/photo.js'`；`:27` 的 `execFor` 从 `'skill-calorie/dist/index.js'` 引 `openDb`；`:35-37,41` 四条为 `'skill-calorie/dist/fetch/photos.js'`。
- 读 `packages/skill-calorie/package.json:8-15` 的 `exports` 仅 `./`、`./triggers`、`./fetch`、`./render`、`./cli`、`./package.json`，从不导出 `./dist/...` 子路径。
- 仓根三条（`D:\ilife`，`node --input-type=module -e "import('...')"`）：
  - `import('skill-calorie/dist/photo/photo.js')` → `FAIL ERR_MODULE_NOT_FOUND`
  - `import('skill-calorie/dist/fetch/photos.js')` → `FAIL ERR_MODULE_NOT_FOUND`
  - `import('skill-calorie/dist/index.js')` → `FAIL ERR_MODULE_NOT_FOUND`
  - 日志 `.scratch/t328/repro-root2.log`。
- 包内四条（`D:\ilife\packages\skill-calorie`，同形 `-e`）：
  - `import('skill-calorie/dist/photo/photo.js')` → `FAIL ERR_PACKAGE_PATH_NOT_EXPORTED`
  - `import('skill-calorie/dist/fetch/photos.js')` → `FAIL ERR_PACKAGE_PATH_NOT_EXPORTED`
  - `import('skill-calorie/fetch')` → `OK addPhotos=undefined`
  - `import('skill-calorie/render')` → `OK gallery=function compare=function gif=function tag=function`
  - 日志 `.scratch/t328/repro-inpkg2.log`。
- 相对路径实证（包内 `./dist/...`）：
  - `./dist/photo/photos.js` 有 `addPhotos=function deletePhoto=function`
  - `./dist/photo/photo.js` 有 `gallery=function compare=function gif=function tag=function`

## 2 修法选择（按顺序试，第一个成立即用）
- (a) 改成已导出子路径：逐个 `Test-Path` 全 `True`（`dist/index.js`、`dist/fetch/index.js`、`dist/render/index.js`、`dist/photo/photo.js`、`dist/photo/photos.js`、`dist/fetch/photos.js`、`dist/render/photo.js`），但函数面不成立——`skill-calorie/render` 有 4 个照片函数，`skill-calorie/fetch` 与 `skill-calorie` 均无 `addPhotos`/`deletePhoto`（`undefined`）。6 键成立、4 键不成立，整体不成立。另 `dist/fetch/photos.js` 与 `dist/render/photo.js` 无 `src` 对应物（`src/fetch` 无 `photos.ts`、`src/render` 无 `photo.ts`），属孤儿，不应作为目标。
- (b) 补 `exports` 子路径：成立，采用。
  - 加 `"./photo/photo": "./dist/photo/photo.js"`、`"./photo/photos": "./dist/photo/photos.js"`，目标均为现行文件（`src/photo/photo.ts`、`src/photo/photos.ts` 对应物，相对导入已证函数全在）。
  - 改 `helpLookup.ts`：`PHOTO_HELP_MODULE`→`'skill-calorie/photo/photo'`；4 条 fetch 串→`'skill-calorie/photo/photos'`；`execFor` 的 `openDb`→`'skill-calorie'`（根导出已证 `openDb=function`）。
  - 结构五步见 `.scratch/t328/structure-5steps.md`（动手前已报；无新增目录，接口仍 3 件不超 5，未超线）。
- (c) 未走到。

## 3 文案同步
- `scripts/build-help.mjs:244` 与 `SKILL.md:186` 同改为 `身材照片 HELP 模块：skill-calorie/photo/photo＋skill-calorie/photo/photos（gallery/compare/viewer/gif + buildPhotoHelp/lookupPhotoHelp，现找直达可执行 exec）。`（两处逐字一致，保持 `help:examples:check` 的 AUTO 对齐）。
- `SKILL.md:194` 改为 `模块 \`skill-calorie/photo/photo\`＋\`skill-calorie/photo/photos\`，函数须存在（单测逐条 import 断言）。`
- `templates/help.html:29` 改为 `skill-calorie/photo/photo ＋ skill-calorie/photo/photos · buildPhotoHelp/lookupPhotoHelp · 每条命中自带 exec（node 一行式）+ legacyCli`。
- 重跑 `node packages/skill-calorie/scripts/gen-cli.mjs --stamp`（持锁，见 §4）。

## 4 解析成功读数（修后，包内当刻可解析）
- 命令（持锁，探针文件在包内以获自引用解析）：`node tooling/run-locked.mjs --ticket 328 --lock-dir .scratch/locks-land --stale-minutes 30 -- node packages/skill-calorie/t328-probe.mjs`（探针已删，仅留日志）。
- 成功读数（`.scratch/t328/verify-locked.log`，逐字）：
  - `PHOTO OK gallery=function compare=function gif=function tag=function`
  - `PHOTOS OK add=function del=function`
  - `ROOT OK openDb=function`
- `runId=f42147b7-157b-4192-ae57-a97ea0b9a991 exit=0`。另有未持锁复证 `.scratch/t328/verify-new.log` 同三行。

## 5 复跑（持锁 `run-locked --ticket 328 --lock-dir .scratch/locks-land --stale-minutes 30`，direct node、不嵌套）
- `node node_modules/typescript/bin/tsc -b`：`exit=0`，`runId=a4d27b4e-c0e9-4549-97ac-992e5e81f93d`。
- `node packages/skill-calorie/scripts/gen-cli.mjs --stamp`：`exit=0`，`runId=da0997ac-f545-499c-9e41-eee8b32f2b86`（重打印记，见 §3）。
- `node packages/skill-calorie/scripts/gen-cli.mjs --check`：`exit=0`，`GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 10 个；声明 101 条，未搬迁清单 0 条`，`runId=e9f54e1e-c1c6-4aba-aa7c-f35e6d77cee5`，日志 `.scratch/t328/gencheck.log`。
- `node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"`：`tests 1492 / suites 160 / pass 1492 / fail 0`，`exit=0`，`runId=dbb4fde7-44df-42fa-b3d6-99f7ec43bc6b`（等锁 `waitedMs=110048`），日志 `.scratch/t328/full-test.log`。
- `node --test test/calorie-routing-81.test.mjs`：`tests 8 / pass 8 / fail 0`，`exit=0`，`runId=5144c3ce-6adf-4f07-a7ad-5882b21d50c1`，日志 `.scratch/t328/test81.log`。
- `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`：`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`，`UNACCOUNTED=0 处`，`exit=0`，`runId=adbf78fe-9985-4962-9cd2-a5a6a157efed`，日志 `.scratch/t328/t293.log`。

## 6 dist 孤儿结论（只报告、不删）
- 结论：不可删（本席不删）。
- 理由清单：
  - 本票直接相关的 3 件（`dist/fetch/photos.js`、`dist/render/photo.js`、`dist/render/help.js`）确无 `src` 对应物（`src/fetch` 无 `photos.ts`、`src/render` 无 `photo.ts`/`help.ts`），且 `src/` 零相对引用、测试零相对引用、新 `exports` 不指它们、现行 `dist` 互引仅孤儿指孤儿（`dist/render/photo.js:3` 指 `../fetch/photos.js`），修后裸串引用亦为零——随一次干净重建消失属预期，但干净重建须持锁另票做，本席不执行。
  - 其余 117 件不在本票闭包，未逐件核对是否仍被冻结测试经相对路径引用；`dist/` 是忽略清单内的构建产物，正确清法是持锁干净重建＋重打印记，不是手删 120 件；手删会与在飞票的编译产物竞态，且下一步即毁 `gen:check` 的陈旧守卫对照。
  - 故全量 120 件当刻一律不删。

## 7 结构五步对账
- 第一步每行均有着落：`package.json`（加 2 条导出）、`src/photo/helpLookup.ts`（改 1 常量＋4 串＋1 模板）、`scripts/build-help.mjs`（改 1 行）、`SKILL.md`（改 2 处）、`templates/help.html`（改 1 处）、证据与草稿、构建产物随之重建——偏差为零。
- 未新增目录与文件（探针 `t328-probe.mjs` 已删）；`helpLookup.ts` 仍 74 行，接口仍 3 件不超 5；铁律一／二／三／四／五均成立。
