# #511 · 卡路里场景 02 饮食「文本清尾二」· 证据

- 票：#511（地图 #155 子票）　分支：`master`　日期：2026-09-15
- 范围：**#509 交付时报出的两类余量**——① 升级后探针收在「告示段」里的**面外 10 条残留**；
  ② 两组**同源入口页**（`78 看营养素明细／38 看营养素深度`、`80 看今日饮水／24 看今日喝水`）的「判据待定」。
  ＋ 把探针从「只判面内」升级成**全量判定**，并补上它自己的第三类盲区（短词）。
- 主线读数：真跑 83 条词 → 81 页产物（2 条按设计 `exit 4` 缺失阻断）→ 抽可见文本 **33,913 字**（#509 是 33,524）。

## 一、编排者裁定（本席照办）

本席进场先把 12 条余量的**产出件**逐条查清后报了一次范围问题，编排者裁定 **(B) 维持现路径**：

| 裁定 | 内容 | 本席落法 |
|---|---|---|
| ① | **P0 #43（18 看上周饮食）归 #271**：落点 `src/render/dietDocs.ts` 的 `buildViewDietDoc` 正是 #271 票面写明的原地改点，且 #271 带着「核场景 01 的看今日饮食概览不被弄坏」这条判据——文本票没有那条判据 | 探针标「已归位」＋证据件开「归位交接」一节（见第十节） |
| ② | **P0 #63（65 看早餐）归 #276**：不是文本问题，是**参数问题**——5 条餐别词全走 `calorie.view.diet`、餐别参数整个丢失，正是 #276 票面「接对 4 处」的第 4 处；动它要同时核场景 01 | 同上 |
| ③ | 归位的两条**不标红、也不标绿**（「红」会读成清尾没做完，它们不是本票欠账） | 探针里单列一段 `已归位`，带归属票与落点；不进红点、不进已清 |
| ④ | **#86／#87 走 Docs 层人话映射**（不动取数层 `src/render/trendMiscPort.ts:215-227`） | 照 #160 的 `humanText` 先例落 `trendMiscPortDocs.ts` |
| ⑤ | 路径内其余 8 条**做真改**，不许用登记判据糊过去 | 逐条改源码，见第三节 |

## 二、落入的文件（LF 按节点口径 `readFileSync(f,'utf8').split('\n').length - 1`）

| 件 | LF | 改动 |
|---|---|---|
| `src/diet/nutritionPort.ts` | 367→**371**（**超线，已挂号**） | 营养素深度那张表的单位 `g`／`mg` 改「克」／「毫克」（`DRI` 三条） |
| `src/diet/nutritionPortDocs.ts` | 462→**492**（**超线，已挂号**） | ① 深度页单位跟取数侧一起上屏；② 配比页推荐范围对比表改「克 / N 天合计」＋表题补口径；③ 两组同源入口页按进来的那条唤醒词出页头（`ENTRY_DETAIL`／`ENTRY_DRINK` 两个标记常量） |
| `src/render/trendMiscPortDocs.ts` | 650→**676**（**超线，已挂号**） | ① 营养分析页副标题改「这页有什么」；② 六因素页 `humanText()` 上屏归一（条目名带单位、三句「无 X 目标」改人话）＋副题补单位口径 |
| `src/diet/rankingDocs.ts` | 120→125 | 榜名表五处共用一处，`frequent` 统一成「常吃榜」（原来同页三个名字） |
| `src/diet/dietEngine.ts` | 194→196 | `RANK_TITLES.frequent` 由「📅 频繁吃榜」改「📅 常吃榜」 |
| `src/diet/reviewDocs.ts` | 117→126 | 复盘页 `周末/工作日 0/708` 拆成「工作日平均／周末平均」两张带单位的卡 |
| `src/diet/libraryDocs.ts` | 248→248 | 食品卡的空品牌不再印破折号占位（那行整行不出） |
| `src/diet/routes.ts` | 99→103 | 两条记录各加入口标记（`看今日喝水` → `entry:"drink"`、`看营养素明细` → `entry:"detail"`）＋一段说明注释 |
| `src/diet/today.ts` / `src/diet/nutrition.ts` | 60→63 / 52→55 | 两个处理体读 `entry` 并交给装配件（不给标记时行为一字不差） |
| `src/triggers/routes.generated.ts` | 生成物 | `pnpm gen` 派生（**非手改**），只动上面那两条记录的两段参数 |
| `packages/skill-calorie/AGENTS.md` | — | 告警线台账（`--sync` 刷当场实测 ＋ 两件结论列补 #511 一段） |
| `packages/skill-calorie/test/t511-清尾二.test.mjs` | 新 | 本票探针 12 例（① 逐页残留 ② 同源入口页 ③ 标记三条硬要求 ④ 变异自证） |
| `docs/skills/skill-calorie/t511-清尾二-证据.md` | 新 | 本件 |
| `.changeset/t511-清尾二.md` | 新 | 变更单 |
| `.scratch/t511/**` | 新 | 升级后的探针（`p0-page-recheck.mjs` v3）＋真跑／抽文本／归因／复跑脚本 |

## 三、逐页残留表（本票收的 10 页 · 探针 v3 全量判定）

逐字取自 `.scratch/t511/probe-511.txt`（`node .scratch/t511/p0-page-recheck.mjs --all`）：

```
=== #511 逐页残留表（本票收的 10 页）===
· 24_看今日喝水   P0 # 3  substring  已清
· 75_搜食品      P0 #13  substring  已清
· 75_搜食品      P0 #14  substring  已清
· 82_看营养分析    P0 #16  substring  已清
· 18_看上周饮食    P0 #42  substring  已清
· 18_看上周饮食    P0 #43  relocated  已归位（归 #271；落点 src/render/dietDocs.ts:139-142）
· 38_看营养素深度   P0 #51  substring  已清
· 38_看营养素深度   P0 #52  substring  已清
· 38_看营养素深度   P0 #53  slash      0g=已清　0g/天=已清　≥25g/天=已清　540mg=已清　77.1mg/天=已清
· 38_看营养素深度   P0 #54  slash      膳食纤维 0 对 0%，钠 540mg=已清　77.1mg/天 = 3.9=已清
· 38_看营养素深度   P0 #55  substring  已清
· 41_看频繁吃榜    P0 #58  substring  已清
· 41_看频繁吃榜    P0 #59  substring  已清（没跟「榜」的「常吃」有 0 处；页上有「常吃榜」=true）
· 41_看频繁吃榜    P0 #60  substring  已清
· 60_饮食复盘（本周） P0 #61  slash      周末/工作日=已清　0/708=已清
· 65_看早餐（最近 7 天） P0 #63  relocated  已归位（归 #276；落点 src/home/routes.ts:28 ＋ src/home/today.ts）
· 75_搜食品      P0 #73  substring  已清
· 75_搜食品      P0 #74  slash      鸡胸肉=该留　—=已清　热量=该留　165 卡=该留　蛋白=该留　31.0 g=该留
· 77_查营养配比    P0 #75  substring  已清
· 77_查营养配比    P0 #76  substring  已清
· 77_查营养配比    P0 #77  substring  已清
· 77_查营养配比    P0 #78  slash      10%（48g）=已清　20%（96g）=已清
· 77_查营养配比    P0 #79  substring  已清
· 78_看营养素明细   P0 #80  desc       已清（不与 38 逐字节相同 ＋ 标题对得上「看营养素明细」）
· 80_看今日饮水    P0 #81  desc       已清（不与 24 逐字节相同 ＋ 标题对得上「看今日饮水」）
· 82_看营养分析    P0 #82  slash      卡路里·趋势其他移植=已清　…趋势其他移植域=已清　配比＋微量＋规则建议（…）=已清
· 82_看营养分析    P0 #83  substring  已清
· 83_看每日六因素   P0 #84  substring  已清
· 83_看每日六因素   P0 #85  substring  已清
· 83_看每日六因素   P0 #86  slash      热量达标=已清　热量=该留
· 83_看每日六因素   P0 #87  slash      无热量目标（先设目标）=已清　无蛋白目标（先设目标）=已清　无饮水目标（先设目标）=已清
```

### 逐处改法（改前 → 改后，逐字）

| P0# | 页 | 改前（逐字） | 改后（逐字） |
|---|---|---|---|
| 53 | 38 看营养素深度 | 累计列 `0g`；日均列 `0g/天`；推荐列 `≥25g/天`／`≤2000mg/天`／`≤50g/天`；钠行 `540mg`／`77.1mg/天` | `0克`／`0克/天`／`≥25 克/天`／`≤2000 毫克/天`／`≤50 克/天`（单位取自 `nutritionPort.ts` 的 `DRI`） |
| 78 | 77 查营养配比 | 「推荐范围对比」表：`10%（48g）`／`20%（96g）`／`144g（30%）`／`+48g` | `10%（48 克 / 7 天）`／`20%（96 克 / 7 天）`／`144 克（30%）`／`+48 克`；表题 → `推荐范围对比（…；克数均为这 7 天合计）`；三张配比卡的值单位 `g`→`克` |
| 59 | 41 看频繁吃榜 | 页题 `排行 常吃 …`、榜单卡 `常吃`、表题 `📅 频繁吃榜（…）` | 页题 `排行 常吃榜 …`、榜单卡 `常吃榜`、表题 `📅 常吃榜（…）`（一处表驱动三处） |
| 61 | 60 饮食复盘 | 一张卡：标签 `周末/工作日`、值 `0/720`、说明 `均值（卡）` | 两张卡：`工作日平均 720 卡／按有记录的工作日算`、`周末平均 —／本窗没有周末的记录` |
| 74 | 75 搜食品 | 食品卡里品牌那一行印破折号占位 `—` | 品牌没填时那一行整行不出（`—` 归零） |
| 82 | 82 看营养分析 | 副题 `配比＋微量＋规则建议（建议阈值见数据层注释，不编造结论）` | `三大营养素比例、其他营养素摄入、以及根据这些数据给出的建议` |
| 86 | 83 看每日六因素 | 条目名 `热量达标`；全页没有一处说清热量的单位 | 条目名 `热量（千卡）达标`；副题尾补「（热量按千卡计）」 |
| 87 | 83 看每日六因素 | `无热量目标（先设目标）`／`无蛋白目标（先设目标）`／`无饮水目标（先设目标）` | `没有设热量目标，所以判不了达标。要设请说“定营养目标”`／`没有设蛋白目标，所以判不了达标。要设请说“定营养目标”`／`没有设饮水目标，所以判不了达标。要设请说“定饮水目标”` |

**#86 只改条目名、不往枚举句里塞（千卡）**：副题那句「六因素＝这 6 项：热量、蛋白、饮水、运动、称重、三餐」是 #496 的交付物、被 `test/t496-文案统一.test.mjs` 逐字钉住（`want: ['六因素＝这 6 项：热量、蛋白、饮水、运动、称重、三餐']`，判法是 `includes`），**该测试件不在本票声明路径内**。故单位改由**条目名**与**句末口径**承载（首尾各一处，读者两处都读得到），钉住的那半句原样保留（句末追加的口径不破坏 `includes`）—— 这是本次唯一一处为不动别人的引脚而选的做法，已在探针的判据里写明。

**#74 只清破折号、不改卡片格**：审查件的改法是「改成表格」。这两张页按老实物 `food_search.html` 做（#274 票面：「老实物零 `<table>`」）且判据逐字钉在 `test/diet-library-t274.test.mjs`（`/food-macro-value">\d+ 卡</`、`\d+\.\d g`）⇒ 本票按**老实物卡片格**不动版式；审查件那半句按「取文本把一张四面卡压成逐行」归为读法差异，探针为它登记了 HTML 级判据（一条食品＝一张 `.food-card` ＋ 一行四格 `.food-macros`），并**点名要编排者裁**（见第十一节）。

## 四、同源入口页：标题按唤醒词对上（票面判据 2）

**信号从入口那条路由记录带过去**（做法沿 #509 的 `source:"photo"`，同一次裁定 4 的三条硬要求）：

| 唤醒词 | 路由记录 | 页头 | 副题 |
|---|---|---|---|
| 看今日饮水（默认，不给标记） | `--params '{"date":"今日"}'` | `💧 今日饮水 <日期>` | `今天的饮水 · 还差 1200 ml` |
| 看今日喝水 | `--params '{"date":"今日","entry":"drink"}'` | `💧 今日喝水 <日期>` | `今天的喝水 · 还差 1200 ml` |
| 看营养素深度（默认，不给标记） | `--params '{"window":"7d"}'` | `🧪 营养素深度 <区间>` | `膳食纤维、钠、糖每天平均摄入与每天推荐量的对比（…）` |
| 看营养素明细 | `--params '{"window":"7d","entry":"detail"}'` | `🧪 营养素明细 <区间>` | `与「看营养素深度」同源：同一份数据，…（…都在下面的表里）` |

**正文也要各自有对应差异**：饮水两页的读数卡标签与进度环中心（`今日喝水`／`今日饮水`）跟着入口叫法走；营养两页的副题各自不同 —— 探针按「抹掉标题那一行后两页仍不相同」判（不只看标题）。

探针读数（`probe-511.txt:31-37`）：

```
· 38_看营养素深度  标题「🧪 营养素深度 2026-09-09 ~ 2026-09-15」  对上唤醒词「看营养素深度」=true　与 78_看营养素明细 逐字节相同=false
· 78_看营养素明细  标题「🧪 营养素明细 2026-09-09 ~ 2026-09-15」  对上唤醒词「看营养素明细」=true　与 38_看营养素深度 逐字节相同=false
· 24_看今日喝水   标题「💧 今日喝水 2026-09-15」  对上唤醒词「看今日喝水」=true　与 80_看今日饮水 逐字节相同=false
· 80_看今日饮水   标题「💧 今日饮水 2026-09-15」  对上唤醒词「看今日饮水」=true　与 24_看今日喝水 逐字节相同=false
· 同源入口页两两逐字节相同 = 0 对
```

**`entry` 标记的三条硬要求**（照 #509 裁定 4，测试第 ③ 例逐条钉住）：① **未知参数不报错** —— 给一个命令不认识的参数名跑 `exit 0`，标记取不认识的取值也 `exit 0` 并回落默认那一支；② **标记名绝不上屏** —— 两页的可见文本里都读不到 `entry`；③ **命令原文带全它** —— 从 `src/triggers/routes.generated.ts` 取那两条记录的命令原文逐字核 `"entry":"drink"`／`"entry":"detail"`，再**照抄跑一遍**，页头必须正好落在带标记那一支（这一条同时证了「入口 → 页头」这条链真通）。

## 五、探针升级：v2（只判面内）→ v3（全量判定 ＋ 第三类盲区）

件：`.scratch/t511/p0-page-recheck.mjs`（v2 件是 `.scratch/t509/p0-page-recheck.mjs`）。

| v2（#509） | v3（本票） |
|---|---|
| 面内 16 条判定；**面外 71 条只打印「别的票的在途件，本件不代判」** | **87 条全判**；红点＝全量里还有「还在／判据待定／缺页」的行 ⇒ 退出码也看全量 |
| 三类引文：`substring`／`slash`／`desc` | 同上，另加 **短词口径**与**归位态** |
| 短词（不足 4 字）拿 2 字片段做子串命中：命中就报「还在」「常吃」在「常吃榜」里也照样报 | **短词命中不再当残留**：必须有**登记判据**；没登记的报「判据待定」，不静默算绿也不算残留 |
| 面外没有「已归位」这一态 | **`已归位`**：判据在别的票的写集里，逐条记归属票与落点，**不进红点、不进已清**，单列一段 |

**第三类盲区（本票补的）夹具读数**（`probe-511-fixtures.txt`）：

```
FIXTURE ok   C 还原态：41 页 P0#59（短词「常吃」）已清=true　83 页 P0#86（短词「热量」）已清=true
FIXTURE ok   C 改坏态：把「常吃榜」拆回「常吃」→ P0#59 还在=true；把「热量（千卡）」拆回裸「热量」→ P0#86 还在=true　探针 RED=true（v2 在这里只会拿 2 字片段做子串命中：短词一旦在页上出现就无从判）
FIXTURE ok   D 改坏态：把 24 的字节盖回 80 页 → P0#81（desc 型）还在=true　80＝24 逐字节=true　同源入口页检查过=false　探针 RED=true（v2 对这两行只报「判据待定」，且不判绿）
FIXTURE ok   E 归位态：单列 2 条（18_看上周饮食#43、65_看早餐（最近 7 天）#63）；不进红点、也不进已清=true　RED=0 条
FIXTURE ok   新旧对照：旧件对 4 处残留全部报「已清」（查不到就当清了），v3 全部报「还在」=4/4
FIXTURES PASS：五类盲区「改坏必红、还原必绿」都成立
```

同一份改坏的字节上，旧件（`.scratch/t155o/p0-recheck.mjs`）、v2、v3 三件对照：

```
· P0 #46 33_校验批量导入　旧件=已清（片段「精确名匹配 / 未匹配 / 导入后建议补」命中 0 页）　v2=还在　v3=还在（slash）
· P0 #47 33_校验批量导入　旧件=已清（片段「热量卡 / 库热量 / 匹配 与单元格 」命中 0 页）　v2=还在　v3=还在（slash）
· P0 #21 02_记一餐（含备注）　旧件=已清（片段「全页与 01_记一餐.txt 逐字节相同」命中 0 页）　v2=还在　v3=还在（desc）
· P0 #27 05_拍营养表记一餐　旧件=已清（片段「全页与 01_记一餐.txt 逐字节相同」命中 0 页）　v2=还在　v3=还在（desc）
```

**「全 81 页两两逐字节相同 = 41 对」是告信息，不判绿**：那 41 对里绝大多数是同一条命令按不同窗口／不同入口出的页在夹具数据相同（按设计同形，如 `26_查食品＝75_搜食品`）；牵到 `65_看早餐` 的那一组与 P0 第 63 条是同一件事，已归位 #276。

## 六、判据实测读数（逐条）

| 判据 | 命令 | 读数 |
|---|---|---|
| ① 升级后的探针全绿（面内 ＋ 面外都判） | `node .scratch/t511/p0-page-recheck.mjs` | 进场「还在 10 ＋ 判据待定 2」（v2 件只打印，见 `.scratch/t511/probe-before.txt`）→ 改后 **`RED = 0 条（全量判定：面内 ＋ 面外）`**、`RESULT: GREEN`；其余面外 40 条全部已清／该留、0 还在、0 判据待定 |
| ① 探针升级自证（五类盲区夹具） | `node .scratch/t511/p0-page-recheck.mjs --fixtures` | `FIXTURES PASS：五类盲区「改坏必红、还原必绿」都成立`（8 条 fixture 全 ok） |
| ② 同源入口页两两逐字节相同 = 0 对 ＋ 各自标题对得上唤醒词 | 同上「同源入口页检查」段 | 四个入口标题逐条 true；**同源入口页两两逐字节相同 = 0 对**（逐字表见第四节） |
| ③ 本票探针 12 例 | `node --test packages/skill-calorie/test/t511-清尾二.test.mjs` | `tests 12 / pass 12 / fail 0` |
| ③ 本包全量测试 ＋ 归因自证 | `… pwsh -File .scratch/t511/suite.ps1` ＋ `… revert-run.ps1` | 见第七节 |
| ④ 变异自证 | `node --test …/t511-清尾二.test.mjs`（第 ④ 例）＋ `probe --fixtures` 的 C／D 两组 | 把 `≥25 克/天`→`≥25g/天`、`常吃榜`→`常吃`、`营养素明细`→`营养素深度` 改回去，同一段断言逐条必红；原样必绿 |
| ⑤ 告警线台账 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | `--sync --dry` 演练（`SYNC-PLAN 改=3 增=0 删=0`）→ `--sync`（`SYNC-VERIFY ok`）→ 非 dry 复跑 **`RESULT: 65/65` ＋ `PASS: 告警线台账齐全且与实况一致`**（台账表**保持全表口径**，一行未改判据） |
| 生成物同步 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `GEN-CHECK PASS：键 127（写 46 ＋ 读 81）；能力 10 个 … 未搬迁清单 0 条` |

## 七、不新增红（本包全量测试 ＋ 归因自证）

- **权威归因**（判据③ 的口径：把本票改动退回旧写法再跑同一批）：
  `… revert-run.ps1` 把本票改的 11 件源码 `git checkout HEAD --`（＝旧写法）＋把本票测试件挪出扫描面，重建 dist，跑**同一批**全量；跑完立刻还原（还原后刷 mtime，避开 Windows mtime 陷阱）。

```
本票改后红点（去重）= 41
退回旧写法红点（去重）= 41
只在「退回旧写法」里有的红点 = 0
只在「本票改后」里有的红点 = 0

ATTRIBUTION PASS：两批红点逐条同名（41＝41）——红点与本票改动无关
```

  ⇒ 本票改动**一个红点也没多**。读数落 `.scratch/t511/suite-after.txt`（`tests 1009 / pass 969 / fail 40`）、
  `.scratch/t511/suite-reverted.txt`、`.scratch/t511/attribution-511.txt`。
  本票改动面直接覆盖的测试件**全绿**（单跑读数落 `.scratch/t511/t511-touch-surface.txt`：
  `ℹ tests 102 / pass 102 / fail 0`）：`t511-清尾二`（12/12）、`t509-清尾`（5/5）、`nutrition-port-112`、
  `trend-misc-port-113`、`diet-homogeneity-108`、`render-t9`、`t496-文案统一`。
  （`diet-library-t274` 那件在进场基线里就是红的——它钉的是 #496 已改掉的旧文案，与本票无关，见第三节末的 #74 说明。）
- **与进场基线对照有 2 条出入，都不是本票**（`.scratch/t511/attribution-vs-baseline.txt`）：
  基线是进场那一刻取的，此后**并发席**动过别人的件 —— ① `T5 真包门禁`／`T9 真包台账与当刻实况一致` 基线里红
  （那时台账陈化：`homeDocs.ts` 350→354），本席 `--sync` 后转绿；② `calorie SKILL 与模板（M6 范式）`／
  `互联区新鲜（… combos …）` 基线里绿、改后红 —— 差在另一席把 `身体/commands.ts` 的代表唤醒词改成
  「记体脂（皮褶钳）」（`SYNC-*` 与 `build-help.mjs` 的 `REPR` 表同时变），与本票无关。
- **那 41 条红按件看**：体重／波动（`analysis-t5`／`weight-*` 族）、运动与身体（`#109`／`#120`）、
  复制区接线（`copy-component-179`／`render-copy-90`／`profile-doc-179`）／HELP 映射面（`help-center-106`／
  `cmd-read-t11`）／主页族（`#374`／`#87`／`db-readonly-93` 里 `dist/home/homeDocs.js` 的 `ICON_COPY` 未定义，
  是别的席在途的件）／页面机器话探针（`t401`／`t401c`／`t401-可见文本守卫`）／`t445` 族。

## 八、改了生成物（必读）——`src/triggers/routes.generated.ts`

- 改了什么：**只有两条记录**（`看今日喝水` order 32／`看营养素明细` order 41）的 cli 参数末尾各加一个入口标记
  （`"entry":"drink"`／`"entry":"detail"`）；**唤醒词、命令名、`scene`、`kind`、`list`、`order` 一字不动**。
- 怎么改的：改**声明** `src/diet/routes.ts` → `npx tsc -b packages/skill-calorie` → `gen-cli.mjs --stamp`
  → `gen-cli.mjs`（写盘）→ 再 `tsc -b` → `gen-cli.mjs --check`。逐条读数：`GEN-STAMP ok … 声明源 41 件`、
  `GEN-CHECK PASS：键 127（写 46 ＋ 读 81）`。`src/cli/keys.ts`／`src/cli/registry.ts` 逐字节不变（哈希在 `GEN-CHECK ok` 里列出）。
- 影响面：运行时只有这两条命令的这两个入口多带一个参数；命令侧不给这个参数时**行为与从前一字不差**
  （口径写在该处注释与测试第 ③ 例里）。

## 九、超线件第四步（必报）

**已超线，需要根据规则进行重构。**（三件都在本票动过，逐件报）

- `src/render/trendMiscPortDocs.ts` 650→**676**：超因＝#113「趋势 2＋其他 6」共 8 键的 HTML 填充器同处一件，
  本票又给其中两页（营养分析／每日六因素）补读者话与 `humanText()`，另加逐段注释。
  拆法：按页族切姊妹件（营养分析／六因素／批量导入预览／其余薄页分开），块层共用的小件
  （`windowForm`／`dateLabelOf`／`axisOf`）提为共件，出口经本件薄转出。**本次先不拆**：编排者已裁「一行不搬」，
  拆分不在本票写集。
- `src/diet/nutritionPortDocs.ts` 462→**492**：超因＝#112 营养 4 页的全文档装配同处一件，本票给其中三页
  （营养素深度／营养配比／今日饮水）补单位、口径与入口页头，另加两段注释。
  拆法：按页切姊妹件（配比页／营养素深度页／来源统计页），块层共用的小件
  （`sourceLine`／`goalDetail`／`kcalNote`）提为共件，出口经 `diet/index.ts` 薄转出。**本次先不拆**：同上。
- `src/diet/nutritionPort.ts` 367→**371**（本就超线）：本票只把 `DRI` 三条的单位从 `g`／`mg` 改成中文，
  **＋4 行是两行注释**；拆法待归属票（#112 营养 4 键取数与「看饮食总览」视图分家）。

三件均已按 `--sync` 同步台账（`RESULT: 65/65`），结论列补了 #511 一段。其余件均在 350 以内：
`diet/rankingDocs.ts` 125、`diet/reviewDocs.ts` 126、`diet/libraryDocs.ts` 248、`diet/routes.ts` 103、
`diet/dietEngine.ts` 196、`diet/today.ts` 63、`diet/nutrition.ts` 55。

## 十、归位交接（编排者裁定 ①② 的硬要求：写到下一手能照着做）

两条**现象仍在、但落点不在本票声明路径**的行。探针里标 `已归位`（不进红点、不进已清），本节给足线索。

### ① P0 第 43 条 → **归 #271**

- **现象（原文逐字）**：`18_看上周饮食.txt:26-36` ──「`2026-09-07` / `1800`（下面 5 天全空）」；
  审查件判「看不懂（无记录日只剩日期和 1800，读者以为是「吃了 1800」，实际是目标值）」。
- **落点（`文件:行号`）**：`packages/skill-calorie/src/render/dietDocs.ts:139-142`
  —— `buildViewDietDoc` 的「按日汇总」表 `rows: days.map((d) => ({ date: d.date, cal: d.calories, … , goal: d.calorieGoal }))`；
  无记录日的 `d.calories` 是 `null`，那一格于是空着，行里只剩日期与 `目标` 列的 1800。
- **建议的具体改法**（照审查件给的两条之一）：`cal: d.calories === null ? '—（无记录）' : d.calories`
  （或 `date` 那一格拼「无记录」）——**不编数**，只是把「这一天没吃」明示出来。
- **归属票 ＆ 为什么是它**：#271（条目列表页 11 条）票面写明的原地改点就是 `buildViewDietDoc`
  （「只许原地改内容、不许搬它」），且带着「改完必须核场景 01 的『看今日饮食概览』不被弄坏」这条判据；
  本票（文本清尾）没有那条判据。

### ② P0 第 63 条 → **归 #276**

- **现象（原文逐字）**：`65_看早餐（最近 7 天）.txt:1-18` ──「`卡路里·饮食` / `饮食总览 <区间>` / `餐别分布`」；
  审查件判「看不懂（页名说只看早餐，正文通篇没有「早餐」二字）」。
- **落点（`文件:行号`）**：`packages/skill-calorie/src/home/routes.ts:28`
  `{ wakeWord: '看早餐（最近 7 天）', … key: 'calorie.view.diet', cli: '… --params \'{"window":"7d"}\'' }`
  ＋ 处理体 `src/home/today.ts`（`calorie.view.diet` 的 `viewDietOverview`）＋ 装配件 `src/render/dietDocs.ts`。
- **根因（本轮挖出来的线索）**：**5 条餐别词的餐别参数整个丢失** —— `看早餐／看午餐／看晚餐／看加餐／
  全部餐别分布（最近 7 天）` 四条（外加「看全部餐别分布」）全走 `calorie.view.diet`，参数只有 `{"window":"7d"}`，
  命令侧拿不到「哪一餐」⇒ 页头与明细都出不了餐别维度。**这正是 #276 票面「接对 4 处」的第 4 处。**
- **建议的具体改法**：在 `src/home/routes.ts` 这 5 条记录的 cli 里补一个餐别参数（照 `calorie.diet.remove-by-type`
  已有的 `mealType` 口径：`"mealType":"早餐"`／`"午餐"`／`"晚餐"`／`"加餐"`），处理体把它交给装配件，
  装配件按餐别出页头（`早餐 · 最近 7 天（<区间>）`）与过滤后的明细。
- **归属票 ＆ 为什么是它**：#276（命令面：补齐 6 条 ＋ 接对 4 处，至今 OPEN）。**注意**：`calorie.view.diet`
  是**跨能力命令**（场景 01 的「看今日饮食概览」也走它）⇒ 动它必须同时核场景 01 不被弄坏；那是命令面 ＋
  跨能力的账，不是清尾票能顺手做的。

## 十一、没做与为什么

- **不重审 #496／#509 已清的 16 条面内引文**（票面「不做」）：探针逐条按页打了出来，全绿。
- **不动唤醒词、命令名、别的图的页面、`src/render/html.ts`**：`src/diet/routes.ts` 只加了两个 cli 参数，
  生成物由 `gen-cli.mjs` 派生。
- **不做 #270–#277 七张页面票的内容重做**：本票只动可见文本与标题（**一行不搬**）。
- **#74 的「改成表格」没做**（见第三节末）：与 #274 已落的老实物卡片格相冲突、且判据钉在
  `test/diet-library-t274.test.mjs` 里；**要点名编排者裁** —— 若裁定要改成表格，请连那条老实物判据一起改。
- **#86 的单位没塞进枚举句**（见第三节末）：那句被 `test/t496-文案统一.test.mjs` 逐字钉住，该测试件不在本票声明路径内。
- **两条「还在」与两条「判据待定」**：#43／#63 归位（见第十节），#80／#81（同源入口页）本票收掉。
- **陈旧引脚**：本票改动没有把别的件的引脚改红（全量 41 条红与本票逐条无关，归因见第七节）。

## 十二、复跑清单

```powershell
node tooling/run-locked.mjs --ticket 511 --max-wait-ms 900000 -- pwsh -NoProfile -File .scratch/t511/regen.ps1      # 声明改了才跑：tsc → 印记 → gen → tsc → gen:check
node tooling/run-locked.mjs --ticket 511 --max-wait-ms 900000 -- pwsh -NoProfile -File .scratch/t511/live.ps1       # 真跑 83 条词（写词逐条还原库快照）→ 抽可见文本
node .scratch/t511/p0-page-recheck.mjs --all                                                                        # 探针 v3 全量判定 ＋ 逐条全表
node .scratch/t511/p0-page-recheck.mjs --fixtures                                                                   # 五类盲区夹具（改坏必红、还原必绿）
node --test packages/skill-calorie/test/t511-清尾二.test.mjs                                                        # 本票探针 12 例
node packages/skill-calorie/scripts/check-warning-line.mjs                                                          # 告警线台账
node tooling/run-locked.mjs --ticket 511 --max-wait-ms 900000 -- pwsh -NoProfile -File .scratch/t511/suite.ps1       # 本包全量读数落 suite-after.txt
node tooling/run-locked.mjs --ticket 511 --max-wait-ms 900000 -- pwsh -NoProfile -File .scratch/t511/revert-run.ps1  # 归因自证：退回旧写法跑同一批 ＋ 还原
```
