# #332 归位证据：页面装配与视图模型按子功能归位（纯搬迁）

> 对照表（票面原表逐字执行）：体重曲线／明细页→`history.ts`、两段对比页→`compare.ts`、
> 体重复盘页→`review.ts`、体重波动页→`volatility.ts`、今日体重盘→`log.ts`。
> 行为不变：声明六件事一件未动，冻结数据、路由声明、生成物一律未手改。

## 1. 搬迁前后对照（逐件摘要）

| 页 | 视图模型（自 `plate.ts`） | 整页装配（自 `plateDocs.ts`） | 归口文件 | 行数（节点口径） |
|---|---|---|---|---|
| 体重曲线／明细 | `buildWeightHistoryView`（27 行体） | `buildWeightHistoryDoc`（54 行体） | `history.ts` | 29→128 |
| 两段对比 | `buildWeightCompareView` | `buildWeightCompareDoc`＋`COMPARE_COLUMNS`（单页独用，随装配走） | `compare.ts` | 32→125 |
| 体重复盘 | `buildWeightReviewView` | `buildWeightReviewDoc` | `review.ts` | 23→94 |
| 体重波动 | `buildVolatilityView` | `buildVolatilityDoc` | `volatility.ts` | 131→225 |
| 今日体重盘 | `WeightDashboard`＋`buildWeightDashboard` | `buildWeightDoc` | `log.ts` | 57→149 |
| — | 类型 5 件＋`assertDate`／`assertRange`（5 页共用，唯一定义地） | `DOC_VERSION`／`DOC_SKILL`／`DOC_TITLE`（5 页共用，唯一定义地） | `plate.ts` 169→65、`plateDocs.ts` 313→14（留薄层，非空壳） | — |
| 汇总出口 | 对外名字不变，转出源改到 5 个新家；类型仍自 `plate.js` | — | `src/render/index.ts`（2 行） | 108→116 |

- 逐字节原则：函数体原样迁入，只动 import 与归属注释；删掉的只有 `plateDocs.ts` 的死码 `fmt`（全仓零引用，未导出）。
- `commands.ts`、`weight/index.ts` 经核对零改动（命令处理函数早已住各子功能文件，声明六件事一件不变）。
- 冻结件零触碰：`routes.ts`、`legacy/routes/scene-03.ts`、`scene-03-weight.ts`、快照、路由计数、`SKILL.md` 生成块、`cli/write.ts`。
- 超线检查（本包告警线 350 行，节点口径）：8 件最高 225 行，**未超线**（无需报警句）。

## 2. 五张页面逐件 sha256 前后对照（固定种子临时库，命令层直跑）

基线 `.scratch/332/pages-fingerprint.json`（`--write`，搬迁前持锁跑）→ 搬后 `--check`：

| 页 | bytes | sha256（前后一致） | --check |
|---|---|---|---|
| dashboard | 65920 | `7a78e6d2…a3da3` | SAME |
| history | 65832 | `2cfaf745…11cc5a12` | SAME |
| compare | 59478 | `ae9e74e8…8ba9c1014d39f` | SAME |
| review | 58681 | `f78ec5dc…b3ac165a959d` | SAME |
| volatility | 67657 | `53389607…c330d579` | SAME |

**FINGERPRINT-CHECK PASS 5/5**（本票硬判据达成）。

## 3. 持锁运行记录（GATE-RUN 对账）

- GATE-RUN runId=`644a019b-917f-4d55-8caa-89f7fc199866` cmd=`pnpm --filter skill-calorie build` exit=0（搬迁前基线构建）
- GATE-RUN runId=`5df15d7c-2564-4d69-b45c-9f11e548629f` cmd=`node .scratch/332/pages-fingerprint.mjs --write` exit=0（基线指纹落盘）
- GATE-RUN runId=`e6cbe3ca-341e-4f84-bb34-6f6dc5d15f55` cmd=`pnpm --filter skill-calorie build` exit=0（搬后重建，转绿）
- GATE-RUN runId=`38753ac7-13d0-4273-9afe-838d2ab4cb88` cmd=`node .scratch/332/pages-fingerprint.mjs --check` exit=0（5/5 SAME）
- GATE-RUN runId=`054ca892-defb-4829-9026-158afa96205d` cmd=`pnpm build ＋ pnpm gen:check`（一次持锁）：build exit=0；gen:check 见 §4
- GATE-RUN runId=`8a6e7a7b-7859-4b2d-8b5d-5f3e358b09f3` cmd=`node --test test/*.test.mjs packages/*/test/*.test.mjs` exit=1：**1502 pass／22 fail**（见 §4，零体重失败）
- GATE-RUN runId=`f366665e-8766-4b53-baaf-2ecfb3133f4f`／`c0ece8ce-242e-44cc-af33-85827305f3a3` cmd=t293 探针：P1=FAIL P2=PASS P3=FAIL P4=PASS P5=PASS（见 §4；复跑两次一致）
- GATE-RUN runId=`101995a0-e4d3-4ab8-844c-319da4a9cb6f` cmd=`pages-fingerprint.mjs --export dashboard` exit=0（真出口）

## 4. 红项归因（逐条点名：全是邻席在途改动，本票写入集之外）

1. `pnpm gen:check` FAIL：唯一差异是 `src/triggers/routes.generated.ts` 第 52 行 diet 场景 02「批量导入食品」（盘上 non-exec vs 生成 exec `calorie.product.import`）。该生成物文件处于 M 状态且属 diet 邻席写入集（`src/diet/routes.ts`、`commands.ts`、`today.ts` M＋`productImport.ts`、`receipt.ts` 未跟踪）；`keys.ts`／`registry.ts`／`combos.yaml`／`build-help.mjs` 四件 GEN-CHECK ok——**本票零命令增减（committed 113前后一致（写45+读68；工作区114含邻席 product.import 半落定）），weight 段零差异**。
2. 全量测试 22 fail：diet-02 写命令覆盖／收据（`product.import` 缺落库断言等）、workout 触发词、`photo-gif-284`／`photo-shape-341` GIF、`#179` 写后回执、help 357 计数、routing-81 D2 计数（exec 367 vs 357 等）、SoT parity sha。**体重域全绿**：`#41 体重系五盘 + HTML 字段断言`✔、`#294 棘轮＋能力门`✔、体重落库／CRUD✔、`#111 唤醒词→key→HTML 链`✔、skilllink 18 读键✔；两道机器门中属本票的两块（`cmd-registry-294`、`legacy-ratchet-295`）✔。
3. t293 P1／P3 FAIL：静态部分 **UNACCOUNTED=0**（手写面全落在①／②内）；红来自动态实验——沙箱基线 `gen:check` exit=1（即第 1 条的 diet 半落定态），探针按既定口径显式判红。P2（9 键单一源）／P4（分派零 case）／P5（7/7 parity 点名，含 sport-homogeneity-109）PASS。
4. 构建曾两次被邻席半存盘挡住（`exercise/log.ts` 缺 `out`、`sportDocs.ts` 缺类型，均为他人 M 文件），第三次持锁重跑转绿；我 8 件在三次构建日志里零报错，`tsc --noEmit` exit=0。

## 5. 真出口证据

`pages-fingerprint.mjs --export dashboard`：`D:\ilife\.scratch\332\exports\dashboard.html` 落盘 65920 字节，sha256 与搬迁前基线逐字一致（`7a78e6d2…a3da3`）。落盘✔／绝对路径✔／可打开✔（`Get-Item` 实读）／字节如实✔。

## 6. 交付对账（偏差为零）

事前声明写入集 9 件＝实际提交 9 件：`src/weight/{history,compare,review,volatility,log,plate,plateDocs}.ts`＋`src/render/index.ts`＋本证据件。`commands.ts`／`index.ts`／路由／冻结数据／生成物／`write.ts`／`SKILL.md` 零改动（`SKILL.md` 的构建附带改写已还原）。推送按窗口口径留给编排者统一收口，本票只提交不推送。

## 7. 给 #333–#337 的交棒

每张页面只有一个主人：①曲线／明细 `history.ts`、②对比 `compare.ts`、③复盘 `review.ts`、④波动 `volatility.ts`、⑤体重盘＋写 `log.ts`；共用断言／文档头只读 `plate.ts`／`plateDocs.ts`，五张页面票互不撞笔。遗留：`render/sportDocs.ts:6` 注释仍写"体重五页已迁 plateDocs.ts（#294）"（他人在途文件，未动，可顺手刷新者认领）。
