# t582 证据（场景02饮食·B4排行族文案形状化）

> 已超线，需要根据规则进行重构。超因：本件同住单榜页与全榜页两页装配＋页内样式段 `RANK_CSS`＋堆叠条块（#272 整改引入时 322→359，当场实测 369）。本次先不拆：拆分不在 582 写集（票面只许动 `rankingDocs.ts`、`t272-排行榜页.test.mjs` 两件文本与装配），拆法照台账既有那条＝页内样式段另立 `rankingDocsCss.ts`，本件只留两页装配与复制区，出口经本件薄转出；待收口票认领。另：包台账 `AGENTS.md` 的本件“当场实测”列当刻已是 369，与实况一致（并行席同步），本票未改台账。

## 1. 机器证据

- 编译：`node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` → exit 0（GATE-RUN `f7b24462-aa57-4982-b908-a127c3999c84`，经 `node tooling/run-locked.mjs --ticket 582` 排队；中途曾因别席在途 `trendPredictDocs.ts` 报 5 处 TS1005 而红，重试时对方已落定转绿，本票未碰该件）。
- t272：`node --test packages/skill-calorie/test/t272-排行榜页.test.mjs` → 26/26（GATE-RUN `1d92031c-3175-4f6c-975a-b762d1037bc6` exit 0）。尾 5 行：
  - `ℹ pass 26`／`ℹ fail 0`／`ℹ cancelled 0`／`ℹ skipped 0`／`RESULT: ticket=582 runId=1d92031c… exit=0`
- t279：`node --test packages/skill-calorie/test/t279-真出口用例.test.mjs` → 99/99（GATE-RUN `8d3c7f2f-a525-40e1-ae01-8ba26c2ec2f0` exit 0）。
- 门禁：`t279-真跑.mjs` 的 `readOne` 把 22 个榜词（5 基准＋全部排行＋15 窗口变体＋查高热量排行）跑进 `.scratch/t582/products/`，再 `audit-separators.mjs --dir .scratch/t582/products --json .scratch/t582/gate.json`（GATE-RUN `0cb509b9-35c8-4925-a60a-a99166ce13e0`）。22 件每件节点命中恰 1，且全是来源脚注行（`ilife-block-caliber` 的 `数据来源 · 饮食记录 · 起 → 止 · 共 N 条`，归 #560，本票不碰）：`gate.json` 读数 `files=22 green=0 totals={"R1":22,"R2":0,"R3":22,"R4":0,"R5":0,"R6":0,"R7":0} nodeHits=22`。R2／R4／R5／R6／R7 全零＝本族可见文本的 `；`／`｜`／`、`／`~`／机器标识全清。
- 变异自证（产物级，源码未动）：
  - 改坏：口径行 `热量 碳水和蛋白` → `热量、碳水和蛋白` 后单文件门禁 `FILE 01-看高热量榜.html node·1 ；0 ≥3段1 ｜0 、1 ~0 标识符0 命中=2`（R5 在 `L2169 ilife-block-caliber` 点名，变红）。
  - 还原：拷回备份后单文件门禁 `FILE 01-看高热量榜.html node·1 ；0 ≥3段1 ｜0 、0 ~0 标识符0 命中=1`（仅剩来源脚注行，回绿到基线）。

## 2. 逐条路径（只写派单列出的文件）

`packages/skill-calorie/src/diet/rankingDocs.ts`（形状只接 `base-paint/blocks` 现成件，未新造形状）：

- `:56` `DOC_TITLE='卡路里·饮食'` → `'卡路里 饮食'`（照运动族 `卡路里 运动` 去·先例，不换件）。
- `:59` `EYEBROW='饮食 · 排行榜'` → `'饮食排行榜'`（不换件）。
- `:141` 占比 triple `'%｜碳水 '` → `'% 碳水 '`（空格并列，不换件；堆叠条形状已是现成图形兜底，`aria-label` 随 `nutriText` 同改）。
- `:224` 空结论句 `'本窗 '+start+' ~ '+end` → `'至'`（不换件）。
- `:233` 空态句 `'本窗 '+start+' ~ '+end` → `'至'`（不换件）。
- `:234` 引导句 `'…有内容了；补以前…'` → `'…有内容了。补以前…'`（分号拆成两句，不换件）。
- `:258` KPI detail `'窗口 '+start+' ~ '+end` → `'窗口 '+start+' 至 '+end`（票面遗留出口“窗口 A 至 B”，不换件）。
- `:285-287` 单榜口径 1 个 `renderCaliberLine`（内含 2 个 `；`＋3 个 `、`）→ 4 个 `renderCaliberLine` 各一句：`口径：…热量 碳水和蛋白…合计。`／`营养结构按每克蛋白 4 千卡 碳水 4 千卡 脂肪 9 千卡…。`／`零值不画条，缺值一律写 —。`／`条数＝上榜食物数。`（—与＝保留，顿号改空格／和）。
- `:322` KPI detail `headPhrase+'｜本窗 '+n+' 种'` → `headPhrase+'。本窗 '+n+' 种'`（KpiCard detail 只吃纯文本，拆成两句，不换件）。
- `:339` 全榜结论 `head='；'+榜名…` → `'，'+榜名…`（去分号，不换件；非空结论句的 `head` 变量同改）。
- `:352-353` 全榜口径 1 个 `renderCaliberLine`（内含 2 个 `；`＋1 个 `、`＋`join('、')`）→ 3 个 `renderCaliberLine`：`口径：五类榜同一个窗口和同一套取数口径。`／`每类榜的列序按榜单类查表，主指标排在食物名之后第一列。`／条件行（空榜名 `join(' ')` 空格并列：`本窗没有数据的榜不出明细块：低热量榜 常吃榜。`，不新造形状）。
- `:363` 全榜空结论句 `'本窗 '+start+' ~ '+end` → `'至'`（不换件）。
- 不碰项：`caption` 的 `rankShortName`（只去日期不改名）、`windowStrip` 的 `→`、来源脚注 `·`／`→` 行（归 #560）均未改；`dietEngine.ts` 未碰（不够用即退回，本票未退回）。

`packages/skill-calorie/test/t272-排行榜页.test.mjs`（断言随文案同改，只改本票测试文件）：

- `:37` `WEEK='2026-09-01 ~ 2026-09-07'` → `'2026-09-01 至 2026-09-07'`。
- `:87` `split(' ~ ')` → `split(' 至 ')`。
- `:133` `'…明细块：低热量榜、常吃榜'` 单行顿号串 → `'…明细块：低热量榜 常吃榜'`（空格并列）＋逐名双含断言（不再断言单行顿号串）。
- `:199/200/201` 三处 `'~'` 窗口断言 → `'至'`；`:142` 来源脚注 `·` 不断言不改；`:82` `join(｜)`／`:145` `join(／)` 系报错信息拼接不改。

`docs/skills/skill-calorie/t582-证据.md`（本件）＋ `.scratch/t582/{products/（22 件）,gate.json,run-gate.mjs,t272.log,db/,snap/}`。

## 3. 风险对账（红即停手项全部确认）

- t511 41 页三处＋裸常吃禁令：`RANK_ZH` 常吃榜与页题表题前缀保留，去·未吞榜名；t511 ①-41 与裸常吃项绿（本票 t511 抽查 10/12，41 相关两条绿）。
- t511 h1Of：本票改的 `DOC_TITLE` 只进 `<title>`（探针可见文本剥掉 head），t511 同源入口（营养素深度／今日饮水）不在本族，41 页断言走 `includes` 不走 h1Of，保持绿。
- homogeneity 针（#108）：12/12 绿（高热量榜／米饭／复制榜单／餐均与全部排行＋五榜名＋`<details>` 保留；caption 未改榜单明细）。
- t272 自带⑥零机器话＋⑦日志第 4 段命令原文＋⑩变异自证：26/26 绿（命令键库表名未上屏，`data-t` 载荷未动，总碳水表头与 `calorie-cmd-read` 原文可命中）。
- t279 真出口：99/99（只改文案形状，取数口径路由参数与五榜名映射未动）。
- 任务提示的 t511 HD34／D53／D04／D09 三处：盘内 t511 仅见审查件 53 条列形态与 83 页等营养页判据，本族无交集，保持绿；若指他票视觉码则超出 582 写集，不改。

## 4. 没做完啥＋下一手缺啥（跨族红归 D 轮，不改那些文件）

- t511 抽查 2 红归 D 轮（均在本票写集之外，未改）：①-60 饮食复盘读不到 `工作日平均`（`reviewDocs` 族）；④变异后半 `把标题改回旧写法之后读数没变`（同源入口页 `h1Of` 读数错位，`nutrition-detail`／`today-water` 族）。本票 ranking 侧 41 相关断言绿，红项与本票无交集。
- 台账：`rankingDocs.ts` 现 369 行（改前 367，净＋2：单榜口径 1→4、全榜口径 1→3），包台账当刻同为 369，已一致；台账本身不在 582 写集，本票未改台账（并行席同步所致）。若收口时再增减行，归 D 轮／收口票。
- 缺依赖：无（未装包）。提交只含自己文件（`git diff --cached --name-only` 复核），不推。
