# #948 证据（故障 6 示例同口径 ＋ 故障 9 编辑器两态与可落库载荷）

- 票面：`.scratch/bug-plan-preview/t3-final.md`（报障单 #944 的故障 6、9）；派单 `.scratch/bug-plan-preview/dispatch-948.md`。
- 本窗**另外代 #946 落了故障 2 的确认契约那几句**（编排者 2026-09-23 转票具名授权：`SKILL.md` 这一窗归本席）——
  见下面「代 #946 的那一段」。页面载荷那一半仍是 #946 的活。
- 一切编译／测试／`git`／`pnpm gen` 均经 `node tooling/run-locked.mjs --ticket 948 -- <命令>`；runId 从
  `.scratch/locks/gate-runs.log` 抄。

---

## 一、判据摘要行（三条读数，全部打在真交付出口上）

跑法：`node tooling/run-locked.mjs --ticket 948 -- node --test packages/skill-calorie/test/948-示例与编辑器两态.test.mjs`
读数（`runId=d7e981d7`，exit=0）：`tests 8 / pass 8 / fail 0`。

| 判据 | 机器读数（测试件 `T948-*` 打印行） |
|---|---|
| ① 示例与冻结表同形 | `T948-EX1 示例=冻结表="calorie-cmd-read calorie.view.plan"` ／ `T948-EX2 速查表例="calorie-cmd-read calorie.view.plan"` |
| ② 空模板 | `T948-W1 空模板 exit=0 weeks=0 写库=0` |
| ② 部分预填（只给标题） | `T948-W2 只给标题 exit=0 weeks=0 标题上屏=1` |
| ② 部分预填（只给第 1 周） | `T948-W3 给第1周 exit=0 weeks=1 动作上屏=1` |
| ③ 载荷＝命令串 | `T948-CMD 计划编辑器 逐字相等=true len=353` ／ `T948-P1 载荷=命令串 len=353 模板同源=1` |
| ③ 原样执行＋库内对账 | `T948-P2 原样执行 exit=0 周=1 行=1 标题="t948 计划"` |
| 载体自证 | `T948-CARRIER 按钮=1 预览块=1` |

生成物门（`check-examples.mjs`，跑在标准种子库上逐行真 spawn）：

- `--list`（`runId=c957138a`，exit=0）：`calorie.view.plan → calorie-cmd-read calorie.view.plan`；`RESULT: 137/137`。
- 全量门（`runId=7178535f`，exit=0）：`示例行数：137（通过 137，白名单跳过 0，红 0，结构问题 0）用时 42.5s`
  ＋ `RESULT: 137/137` ＋ `PASS: SKILL.md 示例逐行可执行`。

其余门：

- `pnpm gen:check`（`runId=47791147`，exit=0）：`RESULT: 6/6` ＋ `PASS: 六份说明面与唯一来源逐字同源，块外无旧写法`。
- `check-page-assert.mjs`（`runId=b41bd651`，exit=0）。
- `check-one-path.mjs`（`runId=641bded3`，exit=0）。

## 二、变异自证（两行机器读数）

| 变异 | 改法 | 机器读数 |
|---|---|---|
| A · 示例改回窄口径 | `src/workout/commands.ts` 的 `calorie.view.plan` 示例改回 `--params '{"date":"今日"}'`（并把 `SKILL.md` 速查表那一行一并改回，模拟「现状」） | `tsc -b`（`runId=ec290bfb`）**exit=1**（`GEN-STALE`：声明变了而 dist 未重建）＋ `node --test`（`runId=b67e1fab`）**exit=1**：`✖ #948 ① 示例：…与冻结表逐字同形`、`✖ #948 ① 速查表那一行…不含 --params`，`pass 6 / fail 2` |
| B · 载荷换回占位串 | `src/workout/planEditorDocs.ts` 的 `prompt` 换回 `'请你加载技能 卡路里，执行唤醒词「…」。明细见下面的计划表。'` | `tsc -b` **exit=0** ＋ `node --test` **exit=1**：`✖ #948 ③ 载荷：去空白后逐字等于命令串`、`✖ #948 ③ 原样执行该串 ⇒ exit 0`，`pass 6 / fail 2` |
| 还原 | 两处逐字还原 | `tsc -b`（`runId=648e41dc`）**exit=0** ＋ `node --test`（`runId=d7e981d7`）**exit=0**，`pass 8 / fail 0` |

A 的第一条读数还顺带证了另一件事：`pnpm gen` 那条 `GEN-STALE` 门**真的会拦**「声明改了没重建」——改坏的那一刻就红，
不是摆设。

## 三、GATE-RUN 行（协议 §2.4）

```
GATE-RUN runId=7c9d242e cmd=node packages/skill-calorie/scripts/check-examples.mjs --list            （exit=0）
GATE-RUN runId=648e41dc cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie          （exit=0）
GATE-RUN runId=d7e981d7 cmd=node --test packages/skill-calorie/test/948-示例与编辑器两态.test.mjs  （exit=0）
GATE-RUN runId=7178535f cmd=node packages/skill-calorie/scripts/check-examples.mjs                  （exit=0）
GATE-RUN runId=47791147 cmd=pnpm gen:check                                                          （exit=0）
GATE-RUN runId=b41bd651 cmd=node packages/skill-calorie/scripts/check-page-assert.mjs               （exit=0）
GATE-RUN runId=641bded3 cmd=node packages/skill-calorie/scripts/check-one-path.mjs                  （exit=0）
GATE-RUN runId=77fb28e7 cmd=pnpm build（先 build 后 gen：gen 要按 dist 印记判 dist 是否当前产物）  （exit=0）
GATE-RUN runId=14a422ee cmd=pnpm gen                                                                （exit=0）
GATE-RUN runId=47791147 cmd=node packages/skill-calorie/scripts/build-help.mjs                      （exit=0，见上一行的同一批）
```

生成器顺序（实测口径，供收口令引用）：**`pnpm build` → `pnpm gen` → `node scripts/build-help.mjs`**。
理由：`pnpm gen` 先按 `dist/` 的内容印记判「dist 是不是这些声明现在的产物」，声明变了必须**先 `pnpm build`**
（`tsc -b` 本身不重打印记）；而 `pnpm build` 里那次 `build-help` 跑在 `gen` **之前**，读的是旧的 `EXAMPLES` 表，
故 `gen` 之后要**再跑一次** `build-help` 才能把新的「例」列写进 `SKILL.md`。三条都实测过（`GEN-STALE FAIL`／
`AUTO 块 != 生成器输出`各红过一次），不是纸面推理。

## 四、改动件清单（实际写集，与票面「交付物路径」逐条对账）

票面「交付物路径」列的件（全部动了）：

| 件 | 改了什么 |
|---|---|
| `packages/skill-calorie/src/workout/commands.ts` | ① `calorie.view.plan` 的 example 去掉 `--params`（故障 6，与冻结表同形）；② `calorie.view.plan-wizard` 的注释写明两态与「为什么本格只留第 1 例」 |
| `packages/skill-calorie/src/workout/planEditorRuntime.ts` | 页内运行时：复制载荷改由 TS 侧同源的模板产出（`commandText()`），页底预览块与产物区同步写同一条命令；载荷那几段薄转出 |
| `packages/skill-calorie/src/workout/planEditorDocs.ts` | 占位串撤掉：`prompt` 改 `planEditorSetCommand(state)`；页内脚本换 `PLAN_EDITOR_PAGE_JS` |
| `packages/skill-calorie/SKILL.md` | **手写段**：新增三条（编辑器两态／编辑器页复制到手的是一条命令／写前预览页出页之后怎么走＝代 #946 的故障 2 契约）；AUTO 块由生成器刷（未手改） |
| `packages/skill-calorie/src/cli/keys.ts`、`src/cli/registry.ts`、`src/triggers/routes.generated.ts`、`packages/base-combos/combos.yaml`、`scripts/build-help.mjs` 的派生块 | 生成器刷出的派生件（`pnpm build` ＋ `pnpm gen` ＋ `build-help` 三条一起跑；`gen:check` 6/6 绿） |
| 新建 `packages/skill-calorie/test/948-示例与编辑器两态.test.mjs` | 票面点名的验收用例（8 条） |
| 本文件 `docs/skills/skill-calorie/t948-证据.md` | 证据 |

**与票面清单的偏差（编排者具名授权，逐条标出）：**

1. `packages/skill-calorie/src/workout/planPayload.ts`（**新建**）——票面没列。来意：故障 9② 要「TS 侧同一出处直出」，
   而页内运行时是**逐字进页面**的 JS 文本，故把「命令模板 ＋ 状态转写 ＋ 器材归属 ＋ 页内换法」收成一份源，
   TS 真函数与页内 JS 文本都由它派生（`new Function('return ' + EQUIP_OF_JS …)`），两侧必然同源。
2. `packages/skill-calorie/src/workout/planEditor.ts`——加一位 `setCommand`（那条命令的模板随状态进页面）。
3. `packages/skill-calorie/src/workout/planEditorPort.ts`——接线：把模板挂到状态上（一行 ＋ 注释）。
4. `packages/skill-calorie/src/workout/planEditorCss.ts`——加 `.pe-cmd` 与 `.pe-cmd-hint` 两条（3 行）；这三件同属
   「定训练计划」编辑器页那一族，与 #946／#947／#949 **零重叠**（写前已 `git status` 复核，三席的件当时都干净）。

**没动**（红线逐条）：`packages/base-render/**`；冻结表 `src/triggers/scene-05-workout.ts`（本票只读它，一行未改）；
生成物只经生成器刷、无手改；`packages/skill-calorie/AGENTS.md`（台账没写）；#946／#947／#949 的件一件未碰
（提交前 `git diff --cached --name-only` 复核，见下）。

## 五、结构面（必报五步的第四步）

- **`src/workout/planEditorRuntime.ts`：改动前 426 行 → 改动后 412 行（LF，`split('\n').length - 1`）**：
  净减 14 行，**不是**本票把它撑过线的；但**它仍在 350 告警线之上**（426 就已超，本票减了 14）。
  → **已超线，需要根据规则进行重构。** 超因：这一件同时住着「编辑器的状态与交互」（周页签／日／段／参数／
  动作库选择层的装配与事件委派，约 250 行）与「按状态重写产物与复制载荷的接线」（约 60 行），
  另加件头注释与逐段说明。本窗**先不拆**：本票在它身上动的是载荷那一段（已把可搬的那部分搬去
  `planPayload.ts`），再叠一次结构重排会把「载荷改由同一出处直出」这条判据搅成两条变更的合成读数；
  **拆法**＝按「状态与交互／版本与复制」切两件姊妹件：`planEditorView.ts`（`*Html()` 那一族渲染函数 ＋
  事件委派）另立一件，本件只留状态（`S`／`week`／`daySel`／`picker`）与 `render()`／`syncCopy()` 的接线，
  两件之间只过状态对象与几个回调；待收口票认领。
- **`src/workout/planPayload.ts`（新建）149 行**，**`planEditorDocs.ts` 73 行**，`planEditorPort.ts` 257 行，
  `planEditorCss.ts` 142 行，`planEditor.ts` 108 行，`commands.ts` 92 行 —— 均在线内。

## 六、代 #946 的那一段（故障 2 确认契约）

写在 `SKILL.md` 的「Wizard Verify 铁则」节（与故障 9 那两条两态说明**同段相邻**），内容：写前预览页出页之后，
用户回「确认」（含只回两个字）＝场景 3 的明确授权，授权对象是**上一轮预检页的那个 `op`**，直接跑该 `op` 的确认命令落库
（`op` → 确认命令＝`src/workout/planPreviewPayloads.ts` 的九条字面量表，逐条列出；唤醒词出处＝`src/workout/routes.ts`）；
**不得**改走计划编辑器。措辞按 `docs/agents/wording.md`（写「会改数据库的命令」，不写「写键」）；
#943 刚提交的「落地训练族两态」那一段一字未动。

## 七、未做项与下一手缺什么

1. **告警线台账未同步（按红线上报，台账不由本席写）**：收尾那一刻跑
   `node tooling/run-locked.mjs --ticket 948 -- node packages/skill-calorie/scripts/check-warning-line.mjs`
   （`runId=dcec52c4`）**exit=1，`RESULT: 92/97`**，逐条红如下（**只有第一条属本票**）：
   - `RED 台账陈化：src/workout/planEditorRuntime.ts 台账=426 实况=412` ← **本票**（净减 14，理由见 §五）
   - `RED 台账陈化：src/workout/write.ts 台账=643 实况=653` ← #949 的件
   - `RED 台账陈化：src/workout/planStore.ts 台账=398 实况=414` ← #949 的件
   - `RED 漏报（台账没有）：src/render/planPlate.ts LF=447` ← 别人的在途件
   - `RED 漏报（台账没有）：src/render/workoutPlanDocs.ts LF=379` ← #946 的件（`git status` 里它已 clean，说明已提交）
   - 复位命令（编排者收口时跑）：`node packages/skill-calorie/scripts/check-warning-line.mjs --sync`
     （先 `--dry` 演练更稳）。
2. **`SKILL.md` 速查表「例」列只出空模板那一条**（票面要求「两种形态都写进 AI 读得到的面」**部分达成**）。
   三条试过的路都被机器判红，故另一形态改写在手写段（`SKILL.md` 的 Wizard Verify 节「计划编辑器两种调用形态」），
   命令声明的注释里也逐条记了读数：
   - `<br>`／`<br />` 行内换行 ⇒ 示例门的「未登记替换值的占位符」检查（`/<[^<>]+>/`）当场判红（实测 `RED exit=? calorie.view.plan-wizard 未登记替换值的占位符：<br>`）；
   - 真换行（`\n`）⇒ 生成器写 `EXAMPLES` 表那一段是单引号串，真换行把产物切成两行（`node` 报 `Invalid or unexpected token`，门 exit=2）；
   - 只留第 2 例 ⇒ 「例」列就不是「照抄即跑」的那一条。
   **下一手缺什么**：要在一个格子里放两条命令，需要先动 `scripts/gen-cli.mjs` 的 `renderExampleBlock`（改引号转义）
   或 `scripts/check-examples.mjs` 的占位符检查（把 `<br>` 一类标签剔出）——两者都是生成器／门的件，本票写集不含，
   故留给编排者裁。现状交付：第 1 例照抄即跑、另一形态在说明面写清。
3. **`planEditorRuntime.ts` 的拆分**（§五）待收口票。
4. 票面「遗留出口」三条（速查表「预检页」列指到计划族写命令／冻结表补「空模板／部分预填」词／「已谈好 80%」口径）
   本票一律不碰，照票面留给后续票。
