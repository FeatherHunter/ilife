# #472 · 场景 09 读侧 A 组：看身材照与生成身材照GIF 结果页 文本与展示（证据）

一句话判定：两张读侧页面（`calorie.photo.list` 的看身材照页、`calorie.photo.gif` 的结果页）已按项目负责人三条判据改完 —— 眉标整行下屏、重复读数各自只留一处、内部说法（内嵌／入片／未校验／cs／字节上限）换成用户读得懂的说法、异常态改状态徽标、体积横幅换成公共层静态提示块；两页真跑逐页有机器读数（看身材照 1,039,701 字节／14 条旧句 0 命中／11 条新句全命中；GIF 页 68,057 字节／19 条旧句 0 命中／8 条新句全命中），变异自证两行见 §五，票面要求的六步在**一把锁里**跑完（`FINAL-GATE steps=6 failed=0`，见 §六）。

## 一、逐条登记：删了什么、为什么删、改成了什么

「删掉的信息不丢，一条条登记在这里」。

### A. 看身材照（`packages/skill-calorie/src/photo/galleryDoc.ts`）

| # | 删掉了什么（原句逐字） | 为什么删 | 改成了什么 |
| --- | --- | --- | --- |
| A1 | 眉标 `calorie.photo.list · 身材照片域` | 内部命令名＋内部词「域」，对用户零信息 | 整行不出（`eyebrow: ''`；公共层该位空串即不写这一行） |
| A2 | KPI 说明位的窗口区间 `2026-04-12 ~ 2026-07-10` | 与页头／取数参数同一事实，用户看的是「最近一张是哪天」 | 说明位改「最近一张 2026-07-10」；窗口区间全页 0 命中（口径仍在取数命令的参数与复制数据里） |
| A3 | KPI `内嵌 3/12 张`、说明 `缺失 8 张 · 超预算未嵌 1 张` | 「内嵌」是内部词，「3/12」要把两数相减才知道缺几张 | 该格标签改「本页」、值改 `8 张已显示`，说明改 `1 张找不到文件 · 3 张太大未显示`（没有异常即不出说明） |
| A4 | 横幅 `照片较多：已嵌 N 张／还有 M 张未嵌入（按体积预算，单页上限 1048576 字节）。替代操作：用「标签」或日期筛选分次查看。` | 字节上限是内部技术细节；「替代操作」是系统口吻 | 公共层静态提示块（`notice()`）：`照片较多，本页只显示 8 张；还有 3 张没显示` ＋ `可按「标签」或日期分批看`。**二选一的理由**：KPI 位已经承担「N 张已显示」，提示块多承担的是**没显示的还得怎么办**（按标签或日期分批看）与 N／M 两数关系（#438 判据 ② 的机器读数）；并进 KPI 徽标会让这两句挤进一格、并丢掉行动指引。**真缺陷一并修**：老版自造的 `budget-banner` 类全仓无任何样式（复核：`packages/base-render/src/style.ts` 与 `blocks.ts` 都没有这个类名），改用 `src/shared/copyArea.ts` ⑤ 的 `notice()`（走公共层 `renderFeedbackBlock` 的浅色静态形态，不出「知道了」按钮） |
| A5 | 图注 `#19 2026-05-30 17:44:45 正面 · 2026-05-30_011.jpg · 文件存在` | 照片 id 与明细表 ID 列重复；秒对用户没用；「文件存在」逐张喊一遍是零信息（没显示的那张才该出声） | `2026-05-30 17:44 · 正面 · 59 天前`（相对天数在**装配层**按 `todayISO()` 现算：0 天＝「今天」、将来日期不出这一段）；文件名收成 `<code>` ＋「复制文件名」小块；`文件存在` 只在缺失时说（见 A6） |
| A6 | 缺失清单 `缺失照片 1 张：#16 2026-05-30_008.jpg（文件缺失：2026-05-30_008.jpg）；…`（文件名写两遍、多张挤一行） | 同一张写两遍；计数句与 KPI 明细重复 | 逐张一行：`#16 · 2026-05-30_008.jpg · ` ＋ 状态徽标「找不到文件」；计数句删（张数已在 KPI 明细） |
| A7 | 表注 `身材照明细（2026-04-12 ~ 2026-07-10，共 12 张）` | 区间与 A2 同一事实 | `身材照 12 张` |
| A8 | 明细表状态列 `存在／缺失／未校验` | 「存在」是零信息值（正常行不必喊） | 正常行留空；异常写 `缺文件`；`未校验` 保留（没配照片目录时唯一能说的） |
| A9 | `照片齐全（N 张均已内嵌）`／`本窗照片文件齐全（M 张因体积预算未嵌入，见上方横幅）` | 与 KPI「N 张已显示」＋提示块 100% 冗余 | 整句不出 |
| A10 | 占位句 `照片未内嵌（体积预算未内嵌）`／`照片未内嵌（文件缺失：X）`／`…（文件过大未内嵌（N 字节）：X）` | 「未内嵌」「体积预算」都是内部说法 | `照片太大，本页没显示`／`找不到文件：X`／`照片太大，本页没显示`；另外两态 `读不出这张照片：X`／`没配照片目录，读不到文件` |

### B. 生成身材照GIF 结果页（`packages/skill-calorie/src/photo/gifDoc.ts`）

| # | 删掉了什么（原句逐字） | 为什么删 | 改成了什么 |
| --- | --- | --- | --- |
| B1 | 眉标 `calorie.photo.gif · 身材照片域` | 同 A1 | 整行不出 |
| B2 | 十格 KPI 里的 `时间跨度`／`首张日期`／`末张日期`／`标签` 四格 | 与页头副标题同一事实（标签本来就在副标题里，首末日期与会话文件名里的两个日期重复） | 窗口区间搬进副标题（全页唯一一处）：`标签「正面」 · 2026-06-17 ~ 2026-07-10 · …` |
| B3 | `画布 64×64 px／画布边上限 64px`、`体积 0.4 KB／0.4 KB · 上限 512000B` 两格 | 能力上限对用户无用；两格说的是同一个对象的两个面 | 合成一格：`尺寸 2×2`＋`大小 0.2 KB`（上限 `maxEdge`／`maxBytes` 下屏） |
| B4 | `帧延时 50cs · 循环播放` | `cs`（百分之一秒）用户看不懂；「循环播放」是动图自带 | `每帧停 0.5 秒`（按 `GIF_LIMITS.delayCs` 现算） |
| B5 | `文件位置 C:\Users\…\Temp\…gif` 整条 | 用户不需要看临时目录全路径 | KPI 只显文件名 ＋ 一个「复制路径」小块（路径只进复制载荷）；未内嵌降级时的「请本地打开：<路径>」原样保留（那是替代操作） |
| B6 | `内嵌 1/1 已内嵌` 格 ＋ 说明 `页内嵌字节与盘上文件同源` | 页里看得见 GIF 本身就说明内嵌成功；后半句是内部验收口径 | 该格整格不出；`data.gif.embedded`／`note` 等复制数据一字未动 |
| B7 | 状态列 `入片／未校验／文件缺失` | 「入片」与「未校验」是内部说法 | `已用上／找不到文件`；另留 `未核对` 一档（没配照片目录、压根没核过——写成「找不到文件」是把没核过的说成没有） |
| B8 | 表注 `入片明细（按帧顺序）` | 「入片」「帧序」是内部说法 | `合成用到的照片（按时间从上到下，就是动画顺序）` |
| B9 | 副标题 `标签「正面」 · 12 张照片合成` | 与实际只用上几张打架（真跑：计划 6 张、用上 5 张） | `标签「正面」 · 2026-06-17 ~ 2026-07-10 · 6 张里挑出 5 张能用的合成（1 张找不到文件）`；一张都没用上时写 `N 张照片都没用上（原因见下）` |

## 二、两页真跑读数（隔离库＋隔离照片目录）

跑法：`.scratch/t472/run-pages.mjs`（造 12 张：4 张约 700KB ＋ 8 张 1×1；最小那张的本体删掉；再删 GIF 计划窗内的 `2026-06-17_001.png`）。
隔离：`SKILLS_DB_PATH=$TEMP\t472\db`、`CALORIE_PHOTOS_DIR=$TEMP\t472\photos`、`CALORIE_TODAY=2026-07-10`。产物：`.scratch/t472/pages/看身材照.html`、`.scratch/t472/pages/生成身材照GIF.html`；原始读数 `.scratch/t472/readings.json`。
判据用**可见文本**口径（`packages/skill-calorie/test/visible-text-probe.mjs`：剔除 `head/style/script/注释` 与全部标签属性 —— 复制载荷里的命令键不算上屏）。

| 页 | 字节数 | 旧句命中 | 新句命中 | 另加读数 |
| --- | --- | --- | --- | --- |
| 看身材照 | **1,039,701**（≤ 1 MiB 上限） | 14 条旧句**全 0**（`calorie.photo.list`／`身材照片域`／`文件存在`／`内嵌`／`缺失`／`超预算未嵌`／`单页上限`／`1048576`／`照片未内嵌`／`体积预算`／`无缺失`／`照片齐全`／`缺失照片`／`文件未校验`） | 11 条**全命中**（`最近一张 2026-07-10`／`8 张已显示`／`1 张找不到文件`／`3 张太大未显示`／提示块整句／`可按「标签」或日期分批看`／`照片太大，本页没显示`／`找不到文件：2026-05-30_001.png`／`身材照 12 张`／`复制文件名`…） | 窗口区间串可见文本 **0** 命中；图注三条样例：`2026-07-10 08:00 · 正面 · 今天`／`2026-06-29 08:00 · 正面 · 11 天前`／`2026-06-26 08:00 · 正面 · 14 天前` |
| 生成身材照GIF | **68,057** | 19 条旧句**全 0**（`calorie.photo.gif`／`身材照片域`／`合成照片总数`／`时间跨度`／`首张日期`／`末张日期`／`画布`／`帧延时`／`循环播放`／`50cs`／`上限 512000B`／`画布边上限`／`页内嵌字节与盘上文件同源`／`文件位置`／`入片`／`未校验`／`内嵌`／`体积`／`张照片合成`） | 8 条**全命中**（`每帧停 0.5 秒`／`复制路径`／`已用上`／`找不到文件`／`张里挑出 5 张能用的合成`／`合成用到的照片（按时间从上到下，就是动画顺序）`／`标签「正面」`／`大小 `） | 整条照片目录路径在可见文本里 **0** 次；臂：计划 6 张、帧 5、165 字节、2×2、`正面_2026-06-17_2026-07-10.gif` |

可见文本整段取样见 `.scratch/t472/logs/visible.log`（人工核对：提示块、KPI 三格／四格、图注、表注、状态列都在位）。

## 三、逐条判据 → 读数

1. `pnpm build` exit **0**；`pnpm gen:check` exit **0**（runId `c603e563-…`／`389169cf-…`；最终链内重跑亦为 0，见 §六）。
2. 三件测试改后全绿且断言更紧（不是放宽）：341 新增 5 条可见文本判据（内部命令名／「域」／`文件存在`／区间串 0 命中＋图注形状＋`N 张已显示`＋表注）；438 的六条判据一字未松，把「横幅句」换成新提示块句、把「占位原因」收到占位句上（变异二即栽在此处），另加「提示块不许再印体积上限」；352 新增 KPI 格数＝3、四个被删标签 0 命中、`cs`／`循环播放`／尺寸上限／内部验收口径 0 命中、整条路径不上屏、区间串全页 1 处，并加一例「计划 4 张、用上 3 张」的副标题与状态列真跑。
3. 两页真跑读数见 §二。
4. 变异自证两行见 §五。
5. 回归：`photo-receipt-docs-282`／`photo-picker-283`／`photo-shape-read-281` 三件均为 **exit 0**（见 §六第 ④ 步）。

## 四、写集

- `packages/skill-calorie/src/photo/galleryDoc.ts`（改，166→203 行）：`notShownReason`／`relativeDays`／`latestDateOf` 三个小函数；图注重写＋文件名复制块；`budgetBannerHtml` → `budgetNoticeHtml`（改走 `notice()`）；`detailOf` 人话版；缺失清单逐张一行＋状态徽标；表注与状态列；眉标整行去掉；`todayISO()` 现算相对天数。
- `packages/skill-calorie/src/photo/gifDoc.ts`（改，271→288 行）：十格 KPI → 三格＋「复制路径」块；`subtitleOf` 新增；状态列与表注人话化；眉标整行去掉。
- `packages/skill-calorie/test/photo-shape-341.test.mjs`（改）：钉住当刻今天（`CALORIE_TODAY`），新增 #472 可见文本判据。
- `packages/skill-calorie/test/photo-budget-438.test.mjs`（改）：提示块读数与两态口径跟着改，判据条数只增不减。
- `packages/skill-calorie/test/photo-gif-page-352.test.mjs`（改）：三格读数判据＋新增「少一张」用例。
- `docs/skills/skill-calorie/t472-读侧A-文本.md`（本件）。
- `.scratch/t472/`：`run-pages.mjs`／`readings.json`／`pages/`（两页产物）／`gate.mjs`／`final-gate.mjs`／`dump-visible.mjs`／`logs/`（各步原始日志）。

两件源码都在包内告警线（350 LF）以内，故未动本包 AGENTS.md 的台账表。

## 五、变异自证两条（源码级）

改坏点＝「眉标删除」（把 `galleryDoc.ts` 的 `eyebrow: ''` 还原成 `'calorie.photo.list · 身材照片域'`，编译后跑 341）：

```
改坏
RUN ticket=472 runId=7eac00ec-666a-42c7-995b-fe8c7c2a73b0 cmd="node .scratch/t472/gate.mjs packages/skill-calorie/test/photo-shape-341.test.mjs" waitedMs=30027 exit=1
  → AssertionError：可见文本里不许出现内部命令名（眉标那句）
改回
RUN ticket=472 runId=b7ba3428-b3d0-4349-8f0e-79e894e8439f cmd="node .scratch/t472/gate.mjs packages/skill-calorie/test/photo-shape-341.test.mjs" waitedMs=40023 exit=0
  → tests 4 / pass 4 / fail 0
```

另 438 自带的产物级变异三行（本票只换口径、条数不变）：

```
MUTATION-RED 变异一/二/三（删提示块／占位原因换空话／触发条件还原旧口径）＝提示块句缺失（须含「本页只显示 N 张；还有 M 张没显示」） | 占位原因须是「照片太大，本页没显示」：这张没显示 | 仅文件缺失时不该有 N／M 提示句
MUTATION-GREEN 改回必绿 bytes=834866 limit=1048576
```

## 六、GATE-RUN 单行

```
RESULT: ticket=472 runId=d88e4d70-74ce-44c5-b497-f654ab497cce waitedMs=10006 exit=0   # cmd="node .scratch/t472/final-gate.mjs"
FINAL-GATE steps=6 failed=0
  ① 编译（pnpm build）exit=0   ② 生成物门禁（pnpm gen:check）exit=0
  ③ 三件测试 exit=0            ④ 回归三件（282／283／281）exit=0
  ⑤ 真跑两页（隔离库）exit=0    ⑥ 两页可见文本取样 exit=0
```

**为什么串成一步跑**：期间别席（#160／#473／#474）在并发 `pnpm build`，实测撞上一次 dist 半写（`dist/fetch/body.js` 整件缺失，`dist/fetch/index.js` 引它失败），分次跑会拿到不可比的读数。串成一步＋同一把锁（`run-locked.mjs` 非重入），读数才可比。

## 七、未做项与下一手（审查席重点）

1. **表格单元格里的真徽标**：公共层 `renderDataTable` 的单元格只收纯文本（`blocks.ts` 的 `cellText` 一律转义），徽标 HTML 进不了单元格；本票把徽标落在**缺失清单那几行**（`#id · 文件名 · 找不到文件`），表格状态列写短词 `缺文件`。要在单元格里出真徽标属公共层表格改造，另票（已写进票面遗留出口）。
2. **文档陈化（本席未改，因不在写集）**：`docs/skills/skill-calorie/t341-页面形状.md` 的「缺失口径」节（`缺失照片 N 张：#id 文件名（原因）`、`figcaption` 带 `文件缺失` 位、示例含「缺失」字样）、`t438-体积退让.md`（横幅 N／M 旧句）、`t281-读侧同形.md`（同）都随本票陈化，请归口票一并改。
3. **两处仍重复的事实（票面未点名，未动）**：① 页头副标题 `标签 正面` 与 KPI `标签筛选 正面`；② `距上次拍照 0 天`（当天拍过时读作「0 天」，图注同一天写「今天」）。两处各改 `galleryDoc.ts` 一处即可，但都不在票面 17 条内，本席未自决。
4. **283 一度红、现已绿**：本席跑到第 ④ 步之前，`photo-picker-283` 曾因**别席 #474 在途件**（候选页文本改造：`pickerDoc.ts`／`picker.ts`／该测试）红两例（`#474：候选行不再抄文件名`、prompt 抽取 exit 4）；本席按纪律未碰，最终链内第 ④ 步已全绿。
5. **未做 git 写操作**：本票只改盘上文件，未 `add`／`commit`／`push`（提交另有专人）。
6. **告警线门当前是红的，5 件全不在本票写集**（本席只跑不改：`node packages/skill-calorie/scripts/check-warning-line.mjs` → `RESULT: 54/59`）：台账陈化 4 件是别席在途增长（`src/render/trendDocs.ts` 826→891、`trendMiscPortDocs.ts` 571→592、`src/analysis/multiTrendPage.ts` 490→501、`src/render/wizardPort.ts` 265→266），另 1 件是**既有**超线未挂号（`src/photo/photo.ts` LF=355，本席未碰该件、`git diff` 为空）。本票改的两件（`galleryDoc.ts` 203、`gifDoc.ts` 288）都在 350 线内，故本包 AGENTS.md 台账表未动。
7. **`gen-photo-baseline.mjs --write` 未跑**（票面：基线统一另票重落）。
8. 响应式适配、复制区三格式中文标签与读页禁用按钮属公共层，票面已记遗留出口。
9. **一处隐式耦合（请审查席知悉）**：`notShownReason` 把内嵌件（`photoThumb.ts`）的失败句翻成人话时按**句里认得出的词**判态（含「过大」→太大没显示、含「未配照片目录」→没配目录、含「文件缺失」→找不到文件、其余→读不出这张照片）。那件不在本票写集，故未改成显式状态位；它改字会让映射掉到兜底句（不会崩、也不会说错成「找不到文件」，但归类会粗一档）。要根治＝给 `PhotoEmbed` 加一个 `kind` 字段（属 `photoThumb.ts` 那张票）。
