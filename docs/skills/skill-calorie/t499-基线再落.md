# t499 场景 09 十页基线再落（含对比页横幅人话化；共享件在途态已记账）

票：`#499 场景 09：对比页超预算横幅文案与同域口径统一（去 1 MiB／字节与系统口吻）`（父图 `#159`）。
本席（第三席 ＝ 基线重落席）**只动两件**：`packages/skill-calorie/scripts/gen-photo-baseline.mjs`（仅 `BASELINE` 块十条 sha）＋本证据件。改前一件与本票的对比横幅正文住姊妹件 `t499-对比横幅.md`。

**一句话判定：成（本条已落库并推送）。** 十页基线按**当刻实情**重落（含别席在途共享件的可见改动），两轮 `BASELINE-OK 10/10`；闸门有双向牙齿（改坏必红点名单页、还原必绿）；照片域 11 件回归 `tests 77 / pass 77 / fail 0`；本席提交 `a3c875b` 已推 `origin/master`（同步位 0／0）。**收尾复核：两件共享件已按同一 blob 落库，故本票的再漂项当日闭合（第四节）；但推送之后他席又落新改动，基线随即再次陈化——如实记在第七节。**

## 一、本票自身改动只应影响 compare 一页

本席进场时，改可见文本的**本票自身改动已经落地**：承载提交 `d5c674a`（`fix(499): 场景09 对比页超预算横幅改人话口径（去 1 MiB/字节与开发前缀）`），改三件＝`src/photo/compareDoc.ts` ／ `test/photo-shape-read-281.test.mjs` ／ `docs/skills/skill-calorie/t499-对比横幅.md`，且 `d5c674a` 已是进场时 HEAD `4bf973a` 的祖先（`git merge-base --is-ancestor d5c674a HEAD` → exit 0）。

横幅由「超预算横幅：已嵌入 N 张，还有 M 张未嵌入（单页上限 1 MiB）· 替代操作：…」改成走公共层 `notice()` 的同域人话提示块（与 `galleryDoc.ts` 的 #472 口径同族）：点名**哪张没显示 ＋ 为什么**并给替代操作；页上不再出现 `1 MiB`／字节数／开发前缀。

**故本票自身的可见改动面只有 `calorie.photo.compare` 一页**——十页里另外九页与 `compareDoc.ts` 无因果。

## 二、实测为何十页同变：共享件在途态，不是本图回归

十页同变的原因不是本票，而是**别席在途、未提交**的两件共用装配件：

| 件 | 在途 blob | `4bf973a` 处 blob | `git diff --stat 4bf973a` | 源文件 mtime |
|---|---|---|---|---|
| `src/shared/copyArea.ts` | `f1a3b16e9065b98e5dec0d375b22712677c06efd` | `477df64fdd5be90a10c65b66d4eac363c1fbb625` | `13 ++++++++---` | 2026-09-15 **08:22:25** |
| `src/shared/docPage.ts` | `c3aa6a4abdb1be3ddebd6e079b04dcac0143247d` | `921bfe558adc02785af50c4cc6bbae7c85b3ec3a` | `31 +++++++++++++++++++++++----` | 2026-09-15 **08:23:03** |

两件合计 `2 files changed, 37 insertions(+), 7 deletions(-)`；改动**上屏可见**（故十页的规范化 sha256 必变）：`copyArea.ts` 把三格式菜单的用途提示 `MENU_HINTS` 由 `['粘贴给 AI / 自己看','结构化存档','表格导入']` 改成**留空** `['','','']`（复制区在**每一页**都有）；`docPage.ts` 增了眉标源码标识符筛（`SOURCE_IDENTIFIER_RE` ＋ `screenEyebrow`，含命令键／下划线连写英文的整行不出）。二者正是「所有页面的共用装配件（复制区／整页壳）」，故十页**同时**变。

**取证过程（可复算，非推测）**：

1. 进场读数（当刻树＝在途态，`dist` 已于 09:18 前含在途版本）：`--check` → **`BASELINE-DIFF 10 pages`**，十页 `want=`（`4bf973a` 在册值）与 `got=` 全部不等。
2. `tsc -b --force` 强制重建后复跑：仍 **`BASELINE-DIFF 10 pages`**，且 `got=` 与第 1 步逐页逐一相同（例：`calorie.photo.compare want=4268835e… got=40e74395…`）——排除「dist 陈旧／本轮重建才漂」这一解释。
3. **隔离复算（决定性）**：把两件共享源码**逐字节置回 `4bf973a` 版本**（`copyArea 8C9E78DB92D2B67825B9AB4764EEB9CE08BA211D8E3F7D2DDAA742D788C74AB4`／`docPage 3AF4B720CA008AC946E61B70C2F0676EE37BBF497D5019F5620850327E86F410`，取自 `git show 4bf973a:…`）→ `pnpm --filter skill-calorie exec tsc -b`（exit 0）→ `--check` → **`BASELINE-OK 10/10`，十页 sha 与在册值逐页同等**。
   ⇒ **两件共享件是在途态，就是十页同变的全部原因；`4bf973a` 在册基线与该二件的已提交版本自洽。**
4. 随后逐字节还原在途版本（`copyArea 0416D9E7…`／`docPage 39A7A0EE…`，与原备份 sha256 一致）→ `tsc -b --force` → `--write` → **`BASELINE-UNCHANGED 10 pages`**（十页 sha 与第 1／2 步同值），记下重落值。

**落定复核（收尾实测）**：本席作业期间该席的改动同步落库——两件**已按同一 blob 提交**（`copyArea f1a3b16e9065b98e5dec0d375b22712677c06efd`／`docPage c3aa6a4abdb1be3ddebd6e079b04dcac0143247d`，即上表在途 blob 本身，内容一字未变），工作区与 `origin/master` 在本票两件上逐字节一致，`git status` 两件为空（干净）。⇒ **本轮所落十条 sha 即「共享件已提交态」的 sha，不再是「在途态快照」。**

- **本席的重落经由 checkpoint 提交承载**：`--force` 重建前为保全别席在途成果，本席提了一次 checkpoint `1cefc90`（`git commit -a`，含当时**全部**在途件，非本席单独成果）；重落后的十条 sha 即随该提交落库，`1cefc90` 已是 `origin/master` 祖先（`git branch -r --contains 1cefc90` → `origin/master`）。因 `git commit -a` 已把该件内容收进提交，本次收尾提交 `-- packages/skill-calorie/scripts/gen-photo-baseline.mjs` 与 `HEAD` 逐字节等同（无差异可暂存），故本轮提交**只新增本证据件**；脚本的十条 sha 已在库中（`git diff HEAD -- …gen-photo-baseline.mjs` 为空、`git show HEAD:…` 内含新值可验）。

## 三、轮询留痕（前席 30 分钟未等到落定）

前席留痕 `.t499-wait-log.txt`（仓库根，未入仓）：**round=1 `[08:39:39]` → round=25 `[09:09:40]`**，共 **25 轮**，每轮 `dirty=True`、`status=` 恒为 ` M packages/skill-calorie/src/shared/copyArea.ts |  M packages/skill-calorie/src/shared/docPage.ts`；该时段其归属席连提 `52978f5`／`c4dc758`／`ca3d2d7`／`cc7e7c1`／`f6d01cf`／`992aa9e`／`18e15d1`／`4bf973a` 等多票，**这两件从未被提交**；末行 `POLL-DONE`。

本席**当刻复测**（09:12:36）：`git status --short` 里两件仍为 ` M`，mtime 仍停 **08:22:25／08:23:03**（约 50 分钟未动）⇒ 结论同前席：**继续等＝空转**，故按实情重落并如实记账。

## 四、接续条（写进关票评论）：再漂项已关闭，派活时的接续条作废

- 接续条原文＝「共享件落定后本基线可能再漂一次，届时应重跑 `--write` ＋两轮 `--check`」。**该条本轮已闭合**：两件共享件已按**同一 blob**（= 本席复算所用的在途 blob）落库，工作区与 `origin/master` 在两件上逐字节一致，且落定后复跑 `--check` → **`BASELINE-OK 10/10`（第 1／2 轮）**、负向两行照旧（`BASELINE-DIFF 1 pages` 点名 `calorie.photo.compare`／还原后回绿）。
- ⇒ 本轮基线**不需要再落一次**。仅当 `copyArea.ts`／`docPage.ts` 的**内容**再变（sha 不同于 `f1a3b16e…`／`c3aa6a4a…`）时才须重跑 `--write` ＋双轮 `--check`；这是**任何**共用装配件改动后的通例，不是本票遗留。
- 重落时只动 `BASELINE` 块（十条 sha），机制（**#493 的钉时钟：进程内 `CALORIE_TODAY=2026-09-06` ＋ #462 的四条渲染时钟剥除规则**）沿用**未改**。

## 五、机器读数

### 5.1 逐页 sha 对照（重落前 → 重落后）

| 页（命令键） | 重落前（`4bf973a` 在册） | 重落后（本轮落定，两轮同值） |
|---|---|---|
| `calorie.help.center` | `58c4245397d4d99337107303c44d26598e5434e4e498266535fcd985a9300d4e` | `9cf27744697216ad4381ab3860dd62839a457c268680465e630d624aa63e2672` |
| `calorie.photo.add` | `e32aa6bea297b182fdea2c88b35e42a88c3c36da1d55407c9b40df9948592e6d` | `93e2604ce2b02729367f6a5f11559282e1338c8220e451fbff5f72ce6842b129` |
| `calorie.photo.compare` | `412af936b36263ddd98bdeaa385014077992b95626ab028ee14b20363833c2f7` | `4268835e4766de60459f70ea12004d522b19a9c99b012cd650045fa44f4f111e` |
| `calorie.photo.detail` | `5e06ac53393efc8ad638395a5b31d75de184e6fe6b4881502083ae5c462533f8` | `a99b1875048f27ea87de6f076618e9110e84c33784a2fe86b0720da50b1d1c8b` |
| `calorie.photo.gif` | `0fa5f355618aa124e3b6ecc90b21dd1a8856178af8f86608c0596db535c72b48` | `221cd678a1e46e0f3d9e36b6b4751d12c6c61030c83c016bf4ce7d3dc54183fc` |
| `calorie.photo.list` | `c20a0147a74d3f66923a00f1d8382fff6f0d8a7b4af519d67c885ad3726840ed` | `1c899bb50af5b7eec6c37ef7ce9e5d4b4ebb89c62ddaba5ae53ea531180132b3` |
| `calorie.photo.remove` | `88dfe5395e635e39efb11ffcdaf477bfb0ac85a09715745298e455360d477669` | `3f13ea7507a3ee01ee371a98298cf0c2ba79e46bd89f7d8921bcb37583de0f25` |
| `calorie.photo.tag` | `c7fefda34dc77fe8af5815a48654cba2681f6f0c0b4c7d822e2e63efeea4d27c` | `d9e11a1ddc1f79881a6675b0b2da525c7dc19fbb5069714d035ec10289ee3c8a` |
| `calorie.view.gif-planner` | `1880d6e65793b094c09e76cbf189186240bb1b6e5d7c913034a9d8234b3713e4` | `0ffffa36ca57e749a68f6f2240a3a05fcea8508d21ca339d57d4704bbeffdca4` |
| `calorie.view.photo-log-wizard` | `11906f26b450d81cb294920e51d2b85ac42056963efd0a1ca845a0e0cfa2609a` | `f7e2e052796b4af3ecc14a46d01575b7be764231a5220c93fd77fc22c88232c2` |

- `--check`（重落前）：**`BASELINE-DIFF 10 pages`**，`RESULT: ticket=499 exit=1`。
- `--write`（重落后）：**`BASELINE-WROTE 10 pages`**，十行 `new …`，`RESULT: ticket=499 exit=0`；脚本 sha256 `C9103721FA03B5CDA61A025674B1FA446EECE45CDCF8DD18646B35B5E4DE2B73` → `DAC10569F363CE2563BEBA1243A90EEDDABD98FD3288B4FF179F0AE0B7452106`。
- 脚本**机制零改**：`git diff --stat 4bf973a` ＝ `1 file changed, 10 insertions(+), 10 deletions(-)`，diff 仅十条 sha 那一行一换（`--check`／`--write` 逻辑、`PIN_TODAY`、`CLOCK_STRIP_RULES` 一行未动）。

### 5.2 两轮 `--check`

| 轮次 | 读数 | exit |
|---|---|---|
| 第 1 轮 | 十行 `ok …` ＋ **`BASELINE-OK 10/10`** | **0** |
| 第 2 轮 | 十行 `ok …`（与第 1 轮逐字同值）＋ **`BASELINE-OK 10/10`** | **0** |

### 5.3 负向两行（改坏必红 → 逐字节还原必绿）

| # | 动作 | 读数 |
|---|---|---|
| 1 | 改 `compareDoc.ts` 一句可见文本（`对比两张照片 · 间隔 ` → `对比两张照片 · 间隔XX `）→ `tsc -b`（exit 0）→ `--check` | **红**：`BASELINE-DIFF 1 pages`，**点名 `calorie.photo.compare`**，`RESULT: exit=1` |
| 2 | **逐字节还原**（`restored-byte-identical=true`；`compareDoc.ts` sha256 `0DFED7D6DA97BE3F48F5053028C73EA2A0F1ACE1B8F6A19C1506BF58C0289617`，与 `d5c674a` 版一致）→ **`tsc -b --force`**（exit 0）→ `--check` | **绿**：**`BASELINE-OK 10/10`**，`RESULT: exit=0` |

- **还原后必须 `tsc -b --force`**（本仓实测，本轮复现）：`tsc -b` 只按 mtime 判增量，逐字节还原会保留旧 mtime ⇒ **跳过重编、dist 留变异版** ⇒ 假红／假绿。本轮中途即撞过一次：`git checkout` 还原两件共享件后跑普通 `tsc -b`，读数照旧报 `10 pages`（那次的十页值其实全是被跳过的旧 dist；改用 `--force` 从源码重建才拿到干净读数）。上一项第 2 步照 `--force` 口径跑。

### 5.4 回归（一把锁内跑完，照片域 11 件）

`node tooling/run-locked.mjs --ticket 499 -- node --test <11 件>` → **`ℹ tests 77` ／ `ℹ pass 77` ／ `ℹ fail 0` ／ `ℹ cancelled 0` ／ `ℹ skipped 0` ／ `ℹ todo 0`**，`RESULT: exit=0`。

件名单：`photo-shape-341`／`photo-budget-438`／`photo-gif-page-352`／`photo-shape-read-281`／`photo-picker-283`／`photo-receipt-docs-282`／`wizard-86`／`photo-responsive-484`／`photo-helpdoc-488`／`photo-help-flow-345`／`skill-md-photo-285`。

**红项：0 件**，故无需归因。

### 5.5 GATE-RUN 单行

`GATE-RUN ticket=499 runId=d9c1e985-7409-4632-b0e0-88fa202af8cd cmd=node packages/skill-calorie/scripts/gen-photo-baseline.mjs --write waitedMs=20005 exit=0`

（同批：`--check` 单跑 `runId=450e761f… waitedMs=190021 exit=1`＝重落前应有之红；双轮 `runId=2f0e294d… waitedMs=0 exit=0`；回归 `runId=20c5466d… waitedMs=1 exit=0`。全部经 `tooling/run-locked.mjs`，未嵌套加锁。）

## 六、别席在途件与未做项

- **未做（本席不碰）**：不代改 `copyArea.ts`／`docPage.ts`（他席领地，只记账）；上述隔离复算里两件**已逐字节还原**在途版本并留 sha256 备份对照。
- **`pnpm build` 报错只报告**：`tsc -b --force` 中途一度红在 `src/render/workoutPlanDocs.ts(223,35) error TS2304: Cannot find name 'PreviewLine'`（该件属他席在途类型错误，不属本票，本席不代改）；随后即恢复（同一命令 exit 0），故未绕过、未改他件。
- **未做**：不重出样张（09-04／09-05 已由前席按 `d5c674a` 重出，三档溢出 0，见 `t499-对比横幅.md` 第四节）；不动 `AGENTS.md`／`.scratch/**`／别席在途件。
- **提交面**：本席只提交本证据件（脚本十条 sha 已随 checkpoint `1cefc90` 在库、与 `HEAD` 逐字节等同，无可暂存之差）；checkpoint `1cefc90` 含当时**全部**他席在途件（属保全动作，非本席成果）。
- **接续**：见第四节——**本票的两件共享件**再漂项已闭合；但推送后他席新落改动又致基线陈化（第七节），故「整轮重构波收尾后重落一次」仍是待办，归编排者。

## 七、推送之后又漂了：并发窗口的性质，不是闸门失灵

本席推送 `a3c875b` 后复跑 `--check`（`runId=bc01da51-1f3b-4b06-b6d2-b2867eab98ed`）→ **`BASELINE-DIFF 10 pages`**，十页 `got=` 全变。归因实测：

1. **不是本票那两件共享件**：`copyArea.ts`／`docPage.ts` 的索引 blob 仍是 `f1a3b16e…`／`c3aa6a4a…`，工作区干净、与 `HEAD` 逐字节一致。
2. **不是闸门失灵**：同一条闸门在本轮已证明双向有牙（5.3 节），且该轮 10 页 `want=` 正是本席刚落库的十条值。
3. **是他席推送后新落的可见改动**：`git status` 显示 `packages/base-render/src/blocks.ts`／`charts.ts`（公共层块层与图表——所有页共用）处 ` M`，另 `src/render/trendDocs.ts`／`workoutPlanDocs.ts` 亦 ` M`；`HEAD` 已由 `a3c875b` 前进到 `3c876e7`（他席 `#154`／`#366`／`#504` 等连提）。
4. **隔离复算（把 `blocks.ts`／`charts.ts` 置回 `HEAD` → `tsc -b` → `--check`）**：仍 `BASELINE-DIFF 10 pages`（`restored-byte-identical=true` 已逐字节还原）——说明只置回 `src` 不够：`dist` 由他席按**整树**重建（`packages/base-render/dist/*.js` mtime 09:26:09，`src` mtime 09:27:03，读数与重建在同秒级窗口内交错）。
5. **整树强制重建后仍不同**：`pnpm exec tsc -b --force`（exit 0）→ `--check` → 仍 `BASELINE-DIFF 10 pages`（`got=` 与第 4 步不同值）。

**结论（当刻性质）**：本仓正处**整轮可见文本／形状重构波**，多席以约半分钟级节奏改共用件＋重建 `dist`。**在此期间，任何「对当刻树取 sha」的基线都会在落库后数分钟内再次陈化**——这不是本票缺陷、不是闸门失灵，是「基线钉在移动靶上」的固有性质。

- **给编排者的接续**：本轮波次收尾（`git status` 的共用件全清）后，重跑一次 `node packages/skill-calorie/scripts/gen-photo-baseline.mjs --write` ＋两轮 `--check`（一条命令，应得 `BASELINE-OK 10/10`）。
- **不改闸门机制**：本席不趁此改判据（例如「共用件脏时跳过比对」）——那属口径变更，得由编排者／负责人裁，不在本票写集。
