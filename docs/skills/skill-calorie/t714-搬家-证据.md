# t714 · 搬家证据（第三批：计划复盘簇两件：`src/render/` → `src/workout/`）

> **判据（本票主判据）**：**搬迁前后逐页逐字节相同**（票面「判据」段逐字）。
> 本窗不改任何可见文本、不改任何形状：搬的是代码住在哪个目录，产物必须一个字节都不动。
> 全部编译／门禁经 `node tooling/run-locked.mjs --ticket 714 -- …` 排队；本窗写集＝票面申报的路径。
>
> **本窗五支脚本的入仓副本**（与本文同处 `docs/skills/skill-calorie/`，后续搬迁票直接抄）：
> `t714-regen.mjs`（重出器：逐页 sha256＋bytes 双比、A/B 自证、可比基线）／`t714-move.mjs`（搬迁执行器：
> 锚点命中数断言 ＋ 搬完全 `src` 相对引用自检）／`t714-mutate.mjs`（变异自证驱动，含逐字节还原断言）／
> `t714-toggle.mjs`（**同窗对摆**：把「搬迁前／搬迁后」两个状态摆进同一只持锁窗口背靠背比 —— 本窗新立的件，
> 见 §三 的具名理由）／`t714-counters.mjs`（票面三个计数器的量尺，口径按 #704 收工读数标定）。
> 对账导出件 `t714-运行记录.txt`（协议 §2.3 第 4 条）。当窗工作副本与步骤表住 `.scratch/714/`（不入仓）。

## 〇 · 本窗做了什么（一句话）

按**归属律**（`structure.md` 铁律一「一个能力的代码住在自己那个目录里」），把 `src/render/` 里
**只被 `workout` 一个能力目录引用**的计划复盘两件**原样迁入** `src/workout/`：
`reviewDocs.ts`(241 LF)／`reviewDocsCss.ts`(131 LF)，共 **372 LF**。产出者＝`calorie.view.exercise-review`
（计划复盘，order201–206 ＋ `list:'new'` 一条）。`git` 把两件都识别为**重命名**（`R097`／`R099`）。

本批是 #704 解决评论 §七 分批表里**唯一无阻塞**的一批：生产调用面 1 行、测试调用面 0 行。

## 一 · 重出器与产物基线（判据的口径与产物来源）

- **覆盖口径按「搬迁面能影响到的页」定，不按页数好看定**。被搬的两件只喂一条产出
  `calorie.view.exercise-review`（`src/workout/commands.ts:50`），故把票面点名的**全部七条唤醒词**
  （`src/workout/routes.ts:39-44` 的 order201–206 ＋ `:48` 的 `list:'new'`「看训练计划复盘」）
  各出一遍，再加**窗口为空那一态**，再加**只有第二只种子库走得到的一支**；共 **10 条**（9 条出页 ＋ 1 条阻断路）。
- **种子库两只**（`.scratch/714/db1`／`db2`），照 `packages/skill-calorie/test/exercise-port-111.test.mjs:44`
  的 `seedPort` 铺开：`db1` 走「有命中／动作对不上／未完成／休息日」四支；`db2` 走
  「计划无题（副题退 null）／计划未登记动作（`plannedMovements === 0`）／`movementPct === null`
  （值位写 `—`、不出进度条）／`completionPct === 0`（进度条最低一档）」。真库零触碰。
- **窗口口径按当刻实测写下来**（不是在票面上猜）：`analysis/series.ts:63` 的 `resolveWindow` ——
  `本周`＝`[本周周一, 今天]`（锚点 2026-09-07 恰是周一 ⇒ **该窗只有一天**），
  `7d`＝`[今天-6, 今天]`＝`2026-09-01 ~ 2026-09-07`，`本月`＝`[本月 1 日, 今天]`＝同一条区间。
  本窗第一次跑就按「周一到周日」推错过一次，故把这条读法写进件头。
- **断言（失败即不落盘）**：page 条 exit 0 ＋ 文档完整性四条（doctype／charset／`</html>` 收尾／零槽位残留）；
  阻断条 exit 4 ＋ stdout 为空 ＋ stderr 命中断绝文案。
- **A/B 自证**：同一份源码连出两遍必须逐字节相同。

### 1.1 时钟冻结：用**仓内那件正本**，不在 `.scratch/` 抄第二份

票面写「冻结件照 `.scratch/t704/freeze.mjs` 抄」。本窗**没有抄第二份**，改用
`packages/skill-calorie/test/freeze-clock.cjs`（`node --require` 预载 ＋ `FAKE_NOW_ISO`，经 `NODE_OPTIONS` 传）
——它**就是那套手法的正本**（#326 起在仓，被 `test/helpers/config-test.mjs:85` 的 `freezeClock` 引用；
在 `87ea157f` 那棵树上实测 **201 件** `*.test.mjs` 引这个辅助件）：
同样只换 `Date` 一个内建、同样盖住无参构造与 `now()`，故 `render/receipt.ts:155` 的 `nowStamp()`
（`new Date()` 直读、无注入口）一并冻住。理由＝结构标准「同一件事只留一处定义」；
效果与 t704 那份一致，读数为 `STABLE-A-vs-B cases=10/10 DIFF=none`（不冻结时这个自证必红）。
**判据口径一字未放宽、源码一字未动。**

## 二 · 搬迁逐件（必报五步第一步 → 第五步）

**第一步 · 影响清单（申报的写集，共 5 个路径，逐行指向一个能力目录）**

| 路径 | 动作 | 一句话 |
|---|---|---|
| `src/render/reviewDocs.ts` → `src/workout/reviewDocs.ts` | 重命名 | 计划复盘整页装配，只被 `workout` 引 |
| `src/render/reviewDocsCss.ts` → `src/workout/reviewDocsCss.ts` | 重命名 | 该族页内样式（唯一产出者），同上 |
| `src/workout/review.ts` | 改 2 行 | 生产调用面 1 行（import 改指同目录）＋ 件头注释里点名的旧地址 1 行 |

**第二步 · 结构设计**：不住新目录、不新建件、不给新接口；两件在新家仍是「一族两件」
（装配 ＋ 页内样式），对外只剩 `buildReviewDoc` 一个名字，与搬迁前逐字相同。

**搬走的两件 + 改到的 6 处锚点**（`move.mjs` 逐条锚定，命中数不符即停）：

- 搬走件自身的出向引用 3 处：`reviewDocs.ts` 的 `./exercisePort.js` → `../render/exercisePort.js`、
  `./receipt.js` → `../render/receipt.js`；`reviewDocsCss.ts` 的 `./pageChromeCss.js` → `../render/pageChromeCss.js`。
- 搬走件自身的件头注释 1 处：`reviewDocs.ts` 里点名的 `./exercisePort.ts` 跟着挪（TS 文档注释，不进产物）。
- 生产调用面 1 处：`workout/review.ts` 的 `../render/reviewDocs.js` → `./reviewDocs.js`。
- 同件件头注释 1 处：`workout/review.ts` 里点名的 `render/reviewDocs.ts` 改为「同目录」。
- **4 处出向引用天然不用改**：`src/render/` 与 `src/workout/` 同为 `src/` 下第一层 ⇒
  `../analysis/utils.js` 与三条 `../shared/*.js` 从新家发出去**逐字相同**（`move.mjs` 的 `KEEP` 逐条断言它们在）。

**没动**：`render/index.ts`（该 barrel 里没有这两件的转出）；`packages/base-render/**`；任何可见文案与版式。

**自检**（`move.mjs` 第 4 步）：搬完遍历 `src/**/*.ts`，**相对 specifier 每条都解析到盘上真件** —— 绿。
条数**按树报**（独立审查席复算时抓到本件此处原写 1703）：**`87ea157f` 那棵树上 351 件 `.ts`／1697 条**；
当窗是在**工作区**数的（1703 条），工作区比提交树多别席的在途件 ⇒ 两个数都对，但口径必须写明是哪棵树。

### 2.1 ⚠️ 一条必须具名的陷阱：「看着像注释，其实是页面字节」

`reviewDocsCss.ts` 的 `STATIC_CSS` 里这一条：

```
'   页壳内距与指标卡那两条页面级规则住共用件 `./pageChromeCss.ts`，本段不再重述 ── */',
```

**它看着像注释，其实是 CSS 文本** —— `reviewViewCss()` 把 `STATIC_CSS` 这些片段拼进页面 `<style>` 发出去。
本窗第一次搬迁顺手把 `./pageChromeCss.ts` 改成 `../render/pageChromeCss.ts`（一个「路径跟着挪」的常规动作），
重出立刻 **9/9 全红、每页恰 +8 B**（＝ `../render/` 比 `./` 多的字节数）。

**处置：把它改回原文**（搬迁判据是逐字节，任何「顺手修正」都会破判据），并把这一条写进 §六 未做项。
**给后续批次（#715／#716）的提醒**：搬页内样式件时，先确认要改的字符串**是 TS 注释还是页面文本**——
`grep` 到路径不等于那行不进产物。

## 三 · 判据读数（逐条）

### 3.1 主判据：9 条页分支逐字节相同

**先把「为什么它必然成立」钉死（结构性理由，与下面的实测互为印证）**：`git show 87ea157f` 的 diff 一共 **6 行**，
逐行分类只有两种——**① import 的 specifier**（3 行：`./exercisePort.js`→`../render/exercisePort.js`、
`./receipt.js`→`../render/receipt.js`、`./pageChromeCss.js`→`../render/pageChromeCss.js`）；
**② TS 文档注释**（3 行，全是件头 `/** … */`，不可能进产物）。夹具 `move.mjs` 对这 3 条 specifier 逐条解析过：
**新旧都落在同一个文件上**（`./receipt.js` 与 `../render/receipt.js` 都解析到 `src/render/receipt.ts`，余同）。
⇒ **两态的模块图逐边相同**，产物与「跑了哪条分支」无关：**任一分支下搬迁都改不了字节**。
下面 9 条的实测不是用来「碰运气抽到全绿」，而是用来证明**这页的产物真的会随源码变**（见 §3.3 的变异轮）。

| 轮次 | 命令 | 读数 |
|---|---|---|
| 开窗当刻（HEAD `0e62aa89`） | `regen.mjs --save 基线.json` | `RESULT: 10/10 条落盘 pages=9 blocked=1 bytes_total=727957` |
| 搬迁后（重编＋重出） | `tsc -b packages/skill-calorie` → `regen.mjs --tag MOVE --compare 基线.json` | **`MOVE byte-identical pages=9/9 DIFF=none`**；`total_before=727957 total_after=727957` |

比对口径＝逐页 `sha256` ＋ `bytes` 双比，9 条逐条比、不抽样；阻断那一条按 exit ＋ 归一化 stderr 比。

### 3.2 同窗对摆（本窗新立的读法，具名理由在这里）

**为什么必须有这一条**：本窗开窗当刻 **#717 正在同一棵树里改源码**（它的 `tsc` 在本席存基线前后各跑过一次，
B2 那次 `tsc` 实测 1,081 ms 的真重编 —— 说明这中间源码又动过）。于是「开窗存基线 → 收窗比」这条跨窗读法
读出来的是**两条变更的合成读数**，分不清是搬迁还是别席改的。本窗第一次比确实红了（9/9 各 +8 B），
而真因是 §2.1 那处自查出来的自家改动、不是别席——**但当时分不出来**，这就是合成读数的代价。

**处置**：`t714-toggle.mjs` 把两个状态摆进**同一只持锁窗口**背靠背比 —— `--revert` 从 `git show HEAD:`
逐字取回搬迁前的三个件（我一行未提交，HEAD 即原文），`--reapply` 再逐件写回：

```
PRE-CHECK  byte-identical pages=9/9 DIFF=none   total_after=727957   ← 摆回搬迁前，仍逐字等于开窗基线
POST-CHECK byte-identical pages=9/9 DIFF=none   total_after=727957   ← 再摆到搬迁后，仍逐字等于开窗基线
```

两侧都等于开窗基线 ⇒ **搬迁这个动作本身对产物零影响**，且开窗基线在这段时间里没有因别席而变。

### 3.3 变异自证两行（打的就是本窗判据本身的识别力）

把搬走后的 `src/workout/reviewDocs.ts` 里一处**会落到页面上**的可见文案改坏
（`每日完成情况</h2>` → `每日完成情况（改坏了）</h2>`），重编 → 重出 → 与基线比；再**逐字节写回原文** → 重编 → 重出 → 再比。

```
MUT-RED byte-identical pages=0/9 DIFF=01(3f869450ad05->6bf93a4ead2c, 77696B->77711B),02(…),…,10(…)
  total_before=727957 total_after=728092
RESTORE 相等=true sha256_12=7256d928da27
GREEN   byte-identical pages=9/9 DIFF=none
```

- 9 页全红、每页恰好多 **15**（＝「（改坏了）」的字节数）⇒ 判据打的是**页面产物**，不是文件名或路径。
- 变异轮与还原轮**共用一次持锁窗口**。还原判据＝文件字节 `sha256` 与改前逐字相同（**逐文件写回**，不是整目录还原）。
- 锚点第一次取光秃秃的 `每日完成情况` 时命中 2 处（件头 JSDoc 一次、`<h2>` 一次）——脚本按命中数断言当场停下，
  改成带 `</h2>` 的唯一上屏标记。这条也写进脚本件头。

### 3.4 三绿（票面「收尾三绿」）

| 门 | 命令 | 摘要行 | exit |
|---|---|---|---|
| 告警线（先演练再落） | `check-warning-line.mjs --sync --dry` → `--sync` → `check-warning-line.mjs` | 演练 `SYNC-PLAN mode=dry 行=45 改=0 增=0 删=0`＋`SYNC-DRY ok`；落盘 `SYNC-VERIFY ok（回读逐字节等于计划）`；门 `RESULT: 93/93` ＋ `PASS: 告警线台账齐全且与实况一致` | 0 |
| 生成链 | `gen-cli.mjs --check` | `GEN-CHECK PASS：键 134（写 52 ＋ 读 82）；能力 10 个…未搬迁清单 0 条` | 0 |
| 基线 | `baseline.mjs --save 仓库基线-收窗.json` → `--compare 仓库基线-收窗.json` | `SAVED（源码 3627 件／派生 1590 件／冻结 11 件）` → `baseline: PASS（差异 0 项）` | 0 |

**台账零改动**：`--sync` 的计划是 `改=0 增=0 删=0`，落盘后 `git diff packages/skill-calorie/AGENTS.md` 为空
——与票面「若两件都未超线则台账零改动」的预计一致（两件 241／131 LF，线内 350 按单件计）。

**外加两道（票面没点名，但它俩是 #702 立的搬迁相关门，顺手一起跑，避免搬迁把测试面打红）：**
`check-one-path.mjs` → `PASS: 测试面每个符号只有一条 dist 路径`（exit 0）；
`check-page-assert.mjs` → `PASS: spawn 真交付出口并断成功的测试件都断到了信封`（exit 0）。
两条都为绿，与 #702 的 `t702-收口.md` 记的「`#714` 0 条要清」互相印证。

### 3.5 收尾后复跑：两条在窗口 H 之后又变绿的复跑，与一条**本窗做不到**的复跑

清完 §六.8 的 dist 残渣后复跑时，工作区里别席的在途改动一度把两件基础设施改到不自洽。处置＝
**不拿别席的中间态当本票读数**：先按可归因另出一次读数，等别席把它自己的台账同步掉之后再复跑一次。

- **告警线门**：中途在工作区跑过一次**红**，两行都不是本票的文件 ——
  `RED 台账陈化：scripts/gen-cli.mjs 台账=940 实况=847` 与 `RED 台账陈化：src/render/html.ts 台账=637 实况=586`
  （`git status` 现场确认两件都被别席改着）。本票两件 241／131 LF，都在线内、也不在台账里 ⇒ 门红与本票无关。
  **可归因读数**＝把本票提交树（`87ea157f`）的包面 `git archive` 出来，用门的 `--root`／`--agents` 指过去跑：
  `RESULT: 93/93` ＋ `PASS`（`t714-extract-pkg.mjs` 认口过旧址已不在、新址两件在位）。
  **别席随后把台账同步了，工作区复跑恢复为绿**：`RESULT: 93/93` ＋ `PASS: 告警线台账齐全且与实况一致`（exit 0）。
  ⇒ **本票收尾时门是绿的**；中途那道红是别席尚未 `--sync` 的中间态，**不得由本票替它 `--sync`**（那会写别人的台账行）。
  同一次复跑里 `gen-cli.mjs --check` 也是绿：`GEN-CHECK PASS：键 134（写 52 ＋ 读 82）；能力 10 个…`（exit 0）。
- **基线：本窗最后**做不到**一次复跑，如实记。** 本票的第三条绿取自窗口 H
  （`baseline: SAVED（源码 3627 件／派生 1590 件／冻结 11 件）` → `baseline: PASS（差异 0 项）`），
  那是在**索引与工作区自洽的树**上取的，成立。清残渣之后想再存一份新基线时，
  `baseline.mjs` 自己抛 `ENOENT: … src/cli/legacy/index.ts`：它的源面是**全仓 `git ls-files`**，
  要求每条在册路径**都在盘上存在**，而 **#705 已把 `src/cli/legacy/**` 从工作区删掉、却还没把删除暂存**
  （那批在 `git status` 里仍是 `D` 未入索引）⇒ **任何人都存不了基线**，与本票无关。
  两条如实记：① 本票那条绿成立；② 清残渣之后旧基线的 `dist` 面**按构造**会多出 8 处删除（本票有意为之，已在 §六.8 具名）。
  **附带一条给工具的观察（不属本票写集）**：`baseline.mjs` 假定「`git ls-files` 列出的件都在盘上」，
  这在共享工作区里会被「删了文件但没暂存」这种中间态打破 —— 建议它把缺件**报成差异面的一项**而不是抛错终止。

## 四 · 票面三个计数器：本批的**可归因**变化

量尺口径按 #704 **收工读数标定**（拿 `918a9706` 的树重算，本件算出来与 #704 报的 186／11／31 件 9,528 LF
**逐字吻合**，故两张票的读数在同一条数轴上）：① 外面 → `render/` 的**深路径 import 行数**；
② `render/` 反向 import 的**能力目录数**（`src/` 下第一层的目录，`src/` 直接下的散件如 `kcal.ts` 不算）；
③ `render/` 自身的**件数与 LF**。

| 读数 | 建票当刻 | 开窗当刻（`0e62aa89`） | **本票提交**（`87ea157f`） | **本批可归因** |
|---|---|---|---|---|
| ① 外面 → `render/` 深路径 import 行数 | 186 | 186 | **188** | **+2** |
| ② `render/` → 能力目录数 | 11 | 11 | **11** | **0** |
| ③ `render/` 自身 | 31 件／9,528 LF | 31 件／9,528 LF | **29 件／9,156 LF** | **−2 件／−372 LF** |

- **① 是涨的，这不是笔误。** 拆开的形状：**−1** 行（`workout/review.ts` 那一行从「外面指向 `render/`」
  变成「同目录」）＋ **+3** 行（两件搬家后出向改指 `../render/`：`exercisePort`／`receipt`／`pageChromeCss`）
  ＝ **+2**。**为什么涨是合理的**：这一批搬的是**页面件**，而 `render/` 里剩下的是**真共件**与**还没搬的件**
  （`exercisePort` 属第六批）；搬走的件当然还要接着用它们。① 要真正降下来得等**最后一批**把真共件收口
  （`errors`／`receipt`／`copy` 过得了「写得出哪两个能力在用」）。**票面没承诺本批降①**，读数照实报。
- **② 不变**：搬家只减 `render/` 的出向边（`analysis` 40→39，是 `reviewDocs.ts` 那条 `../analysis/utils.js`
  随件迁出），而没有任何目录因此归零（#704 那批靠 `xunji` 归零才降 1）。
- **③ 逐字吻合**：241 ＋ 131 ＝ 372 LF，2 件。

**把别席的贡献分开**（票面「读数要按可归因报」）：收工当刻整棵工作区再量一次是
**① 188／② 11／③ 29 件 9,129 LF** ⇒ ③ 比本票提交多 −27 LF，来自 **#717 的在途未提交改动**
（`render/diet.ts`、`render/analysisPlate.ts` 两件在 `render/` 里、那一席正改着）。
① 与 ② 不受它影响（188／11）。**−27 不计入本票的账。**

## 五 · 必报五步第五步：交付对账（偏差为零）

提交 **`87ea157f`**（`feat(714): 计划复盘簇两件按归属律搬回 src/workout/（纯搬迁，页产物逐字节相同）`）
只含本票申报的路径，`git show --name-status` 逐条：

```
M  packages/skill-calorie/src/workout/review.ts
R097  packages/skill-calorie/src/render/reviewDocs.ts    -> packages/skill-calorie/src/workout/reviewDocs.ts
R099  packages/skill-calorie/src/render/reviewDocsCss.ts -> packages/skill-calorie/src/workout/reviewDocsCss.ts
3 files changed, 6 insertions(+), 6 deletions(-)
```

6 处改动的分布与 §二 的清单逐条对得上（`reviewDocs.ts` 3／`reviewDocsCss.ts` 1／`review.ts` 2），
**偏差为零**。暂存前用 `git diff --cached --name-status` 复核过：暂存区只有这三条，无一件他席的在途件。

**一条共享索引的实测（本窗踩到，照实记）**：`git diff --cached` 读的是**全仓共用的一份索引**，
别席也会往里 `git add`。本窗最后一个提交（`90642a72`，归档脚本改锚）当刻，索引里就躺着 **#715 的 15 条**
（`packages/skill-calorie/AGENTS.md`／`src/exercise/` 7 件／`src/render/exercisePort.ts`／5 个测试件）。
⇒ 靠「暂存区只有我的件」这条判据在共享工作区里**不一定成立**；真正兜住的是
`git commit -F <信息> -- <自己声明的路径>` 这个**限定路径提交**（本窗四个提交逐条 `--name-status` 复核，
每个都只含自己的件）。别席那 15 条本席**一行未动、也未撤出暂存区**（撤了就是动别人的在途状态）。

同一时刻工作区里另有**别席未提交的件**，本席一行未碰、也未带进提交（也不该带）：`#717` 的
`src/{analysis/anomaly/common.ts,diet/review.ts,fetch/diet.ts,render/analysisPlate.ts,render/diet.ts}`
＋新增 `src/shared/meal.ts`／`test/t717-口径归一.test.mjs`；另有别席的 `pnpm-lock.yaml`、
`packages/plugin-manager/src/health-view.ts`、`packages/plugin-calorie/src/settings.ts` 与若干 `docs/**`。

### 5.1 一条必须具名的限制：**开窗基线没存下来**

票面「判据」段照 #704 的口径写「开工第一件事是开窗存基线」。本窗**没做到**：开窗那次持锁窗口
第一件就是 `tsc -b packages/skill-calorie`，而当时 #717 的源码正改到一半、**编译不过**
（`render/analysisPlate.ts` 参数隐式 `any`／`render/diet.ts` 找不到 `MEAL_WINDOWS`／`render/index.ts` 转出名字不存在），
窗口按「失败即停」在第 2 步就止住，`baseline.mjs --save` 那一步没跑到——所以
`仓库基线-开窗.json` **当刻不存在**，票面那条「开窗比」无法执行。

⇒ 本窗的「搬迁前后一致」**由 §3.1 的页产物逐字节判据承担**（9 条分支、搬迁前后各一遍），
「本席只碰了申报的路径」由 §五 的提交对账承担，而「这期间没有别席把这片源码改走样」由 **§3.2 的同窗对摆**
承担（比跨窗比更硬：中间没有别席落笔的机会）。这一条与 `t704-搬家-证据.md` §3.4、
`t518-W1-搬家-证据.md` 具名的同类限制同性质：**判据的强形式在当时那棵树上不可复算，必须与读数同页出现。**

## 六 · 本窗具名未做项（不阻塞本票，逐条给下一手）

1. **`analysis/utils.ts` 那处跨能力内部件引用：裁定＝本窗不摆正、留给 #717。** 票面把处置留了出口
   （「要么改走 `analysis` 的能力门，要么在本次裁定里说明为什么先留着」）。三条理由，逐条有据：
   - `src/analysis/index.ts` 的件头**逐字写明**域内算式与取数件「**不出这个目录**，故不在这里转出：
     它们由 `render/` 侧的视图层直接消费，导入面照旧」——为这一处开门＝**加门**，与 #703「包门只许收窄」
     正面冲突（#702 的 `t702-收口.md` 已把「算式走门」改归属为源侧裁定）。
   - 走这条深路径的是**14 个文件、跨 8 个目录**（`analysis` 内 8 处 ＋ `home`／`diet`／`photo`／`shared`／
     `render`／`weight`／`exercise`／`workout`），不是本页一处的孤例。
   - **【717](https://github.com/FeatherHunter/ilife/issues/717) 批③ 已经认领了这次搬家**：它的当刻锚点表逐字列着
     「时钟（正本）`src/analysis/utils.ts:135`（`todayISO`）、`:32` 族与 `shiftISODate`」，并写明
     「页面件搬回能力目录：另有一票在跑，本次只与它排窗」。本窗若先动，会与它的写集撞车并做两遍。
   - 另外：搬家**没有加重**这处引用——`../analysis/utils.js` 从新家发出去与老家**逐字相同**（`move.mjs` 的 `KEEP` 证过）。
2. **`STATIC_CSS` 里那句 CSS 注释仍写着 `./pageChromeCss.ts`（搬迁后已陈化）**：见 §2.1——改它＝改页面字节，
   本窗按主判据刻意留原文。**交给一个不做逐字节冻结的窗口**（#719 窗口② 本来就要重做复盘页那份窗口条与
   来源脚注那一类，落点就是 `src/workout/reviewDocs.ts`）。
3. **其他件里按旧地址写死的指针**（本窗一行未碰，属**他席/他批的写集**，不静默改；地址按本窗收工当刻写）：
   `src/render/sportPortDocs.ts:829`「整支已搬进姊妹件 `./reviewDocs.ts`」（两件已不再同目录，
   「姊妹件」这个说法也不再成立）、`src/render/pageChromeCss.ts:4`、`src/shared/sceneEnvelope.ts:26`、
   `src/weight/{weightUi,volatilityDoc,log,review}.ts` 与 `src/photo/wizardUi.ts` 的「同型先例
   `src/render/reviewDocsCss.ts`」若干处。它们都是**历史引用**，建议随各自能力的搬迁票或收口票一并扫。
   **收工后当场又变了一次**：`sportPortDocs.ts` 那一处正被 **#715（第二批）**在途搬去 `src/exercise/`
   —— 同一条缝两侧都深引的例子，两侧现在都在各自的能力目录里了。
   **独立审查席补了一处本件漏列的（很值得记，性质与上面不同）**：`docs/skills/skill-calorie/t475-review-probe.mjs:240`
   把 `packages/skill-calorie/src/render/reviewDocs.ts` **写进一个可跑脚本的清单**，`:255` 还要
   `fs.readFileSync(ROOT, p)` ⇒ **该件今天跑必抛 ENOENT**。上面那几处只是注释（编译无影响），
   这一处是**会被执行的代码**。**不改它**（§3.2 第 2 条：已入仓证据件的历史原文不回头改；
   t704 对同类件也是「不改历史，只记账」），如实登记给它的归属票或收口票。
   ⇒ **给后续搬迁票的一条检查项**：搬完除了 grep 源码注释，还要 grep **`docs/**/*.mjs` 里按旧路径写死的脚本清单**。
4. **台账 `AGENTS.md` 里 `src/render/sportPortDocs.ts` 那一行的结论列**仍写着 T351-v7 把复盘那一半
   「搬进姊妹件 `src/render/reviewDocs.ts`」。本窗**没有动台账**（票面预计就是零改动，§3.4 已证
   `--sync` 计划为空）；它是**历史事实陈述**，按 #704 裁定「挂号值与结论原样带过去」不回改，
   新地址以本件与 #714 的解决评论为准。
5. **与 #717／#719 的排窗**：本窗期间 #717 一直在同一棵树上改源码（§3.2）。#717 批③ 落地时
   `shiftISODate` 的五个引用面里会包括 `src/workout/reviewDocs.ts`（**新地址**），请按新地址改；
   `#719` 窗口② 要收的「复盘页那份窗口条与来源脚注」也落在同一件上。
6. **测试面：本批 0 条要清**。#702 的 `t702-收口.md` 逐字记着「搬迁票要动的件被测试直接取：`#714` 0 条」
   ——本窗复核一致（没有测试件深引 `dist/render/reviewDocs*.js`），故本批没有任何搬迁误红要顺手清。
7. **`.changeset`：本窗未加**。纯搬迁、包对外可见名与行为零变化（生成链 `键 134／能力 10` 前后一致），
   照 #704 的先例（`918a9706` 也没加）。
8. **旧编译产物残渣：本票那 8 件**已在收窗后清掉；**全包还有 284 件**，横跨五张票，具名记账不越界清。**
    **独立审查席把它定为 `本票范围 S2`**（比本件初稿的「照 t704 先例留着」更准，已按其结论处置）：
    `tsc -b` **不删**已删源码的旧输出，而 `package.json` 的 `files:["dist"]` 会把它**打进 tarball**；
    审查席实测 `import('dist/render/reviewDocs.js')` **能 load 且导出 `buildReviewDoc`** ⇒ 包里躺着一份
    **可加载的旧页装配实现**（本窗复现：清之前读数 `BEFORE 旧 … 能 load 且导出 buildReviewDoc`）。
    本窗的残渣是自己造的（§3.2 对摆把「搬迁前」那一态也编过一次），故**清掉**：
    `t714-dist-sweep.mjs` 按「当刻内容」派生（`dist/**` 的 `.js`／`.d.ts`／`.js.map`／`.d.ts.map` → 对应 `src/` 源件不在即判残渣），
    **逐条点名 ＋ 路径守卫 ＋ 只许清本票前缀**，实测：

    ```
    BEFORE  旧 dist/render/reviewDocs.js：能 load 且导出 buildReviewDoc
    AFTER   旧 dist/render/reviewDocs.js：文件不在 ／ 新 dist/workout/reviewDocs.js：能 load 且导出 buildReviewDoc
    REBUILT 旧件没被复活（`tsc -b` 后仍「文件不在」）／新件照常能 load
    DIST-SWEEP 已删 8 件残渣（限 render/reviewDocs）
    ```

    **全包那 284 件不属本票写集，只报数不清**，三条理由：① 本票只声明过 `render/reviewDocs*` 与 `workout/reviewDocs*`；
    ② 最大一片是 `cli/legacy/**`（#705 按 ADR-0002 退役整目录留下的），另有 `analysis/*`（#717／#704 面）等；
    ③ 想一次性弄干净只有 `tsc -b --clean` ＋ 重编一条路 —— 而**别的席此刻源码正改到一半、编不过**，
    半路 `--clean` 会把 `dist` 弄得更碎。⇒ **这是一条成套搬迁的通用缝**，建议在「最后共件与 barrel 收口」
    那批或另开一张收口票里统一做（做法＝收窗编译后按同一判据扫一遍 ＋ 把这条检查并进搬迁自检）。

## 七 · 独立复核（协议 §5.1：实施者不得兼任自己票的审查者）

本窗由**另一席独立对抗式复核**（结论：**PASS**，无 S1-交付缺陷）。它**没有**拿本件的脚本当证据，自己另走一条路：

- **自己的结构证明**：`git archive` 取 parent 与 commit 两棵树，自写 `cmp-trees.mjs` 逐件比 ——
  352 件对 352 件，**只有 3 件变**；被搬件逐行 diff ＝ **6 行（4 specifier ＋ 2 注释）**，
  4 条 specifier **新旧都落到同一绝对文件**。与本件 §3.1 的结构性理由**独立同向**。
- **自己的端到端重出**：不跑本件的重出器，改用 `node --experimental-strip-types` ＋ 自写 resolve hook
  直跑 TS、自造 `ReviewView`、进程内假 `Date` —— parent 与 commit 两态渲染结果
  `sha256=92b3bebee6bef802…`／`80645 B` **逐字节相同**。
- **自己的变异探针**：把 `STATIC_CSS` 里那句改成 `../render/pageCss…` ⇒ **RED ＋8 B**（独立证实本件 §2.1 的发现）；
  换成同一目标文件的另一种拼法 ⇒ GREEN；插一行 TS 注释 ⇒ GREEN。
- **自己的计数器**（不 import 本件任何脚本）：`918a9706` **①186／②11／③31 件 9528 LF**；
  `87ea157f` **①188／②11／③29 件 9156 LF** ⇒ 与本件 §四 **逐字吻合**，Δ①+2／②0／③−2 件 −372 LF。
  另证 `render/→analysis` 边 40→39（§四 的说法成立）。
- **交叉验证**：它算出的 `workout/reviewDocs.ts` `sha256_12=7256d928da27` **等于**本件 §3.3 的 RESTORE 读数；
  `728092−727957=135=15×9`（MUT-RED 每页 +15 自洽）。

**它设计的必做新探针（本件 10 条覆盖之外）**：同一进程里让两棵树各渲 **8 种自造视图** —— 空一切／
**无未完成（`unhit=[]`）**／`planTitle=null`／**100 天触 `HEAT_CAP` 截断明示**／`start>end` 空区间／单日／
`plannedMovements=0` 而 `movementPct=0`／全命中。结果 **`BRANCH-SWEEP byte-identical 8/8`**（76598–149935 B 各档）。
它另做正向检查：渲染出的页面**含** `./pageChromeCss.ts` 而不含 `../render/…` ⇒ 那句 CSS 注释确实进页面字节、提交没动它是对的。
**它的反证尝试**（构造「搬迁改产物而 10 例盖不住」的场景）：结论是**这样的分支不存在** ——
两件里 `import.meta`／`__dirname`／`process.*`／文件读取**零命中**，唯二的变化面就是那 6 行，
而 `reviewViewCss()` **无条件进每一页**，任何「顺手修正」都会在任意一页上红。

**它提出的缺陷与本窗的处置**：

| # | 归属 | 级别 | 处置 |
|---|---|---|---|
| 1 | 本票范围 | **S2** | 旧编译产物残渣（可 load、会进 tarball）—— 见 §六.8：**本票那 8 件已清并复验**，全包另 284 件具名记账 |
| 2 | 本票范围 | S3 | 漏列一处**可跑脚本**（`t475-review-probe.mjs:240`）—— 已补进 §六.3 并加了一条给后续搬迁票的检查项 |
| 3 | 本票范围 | S3 | 陈化指针（`sportPortDocs.ts:829` 是唯一变成断路径的一处）—— 已在 §六.3 具名 |
| 4 | 范围外发现 | S3 | 本件两个支撑数写错：相对 specifier **1703→1697**（当窗数的是工作区，提交树是 1697）、引 `config-test.mjs` 的测试 **199→201** —— **两条都已改正**（§1.1／§二） |
| 5 | 范围外发现 | S3 | 冻结件改用仓内正本（票面写「照 t704 抄」）—— 已在 §1.1 具名，口径未放宽 |

**它没能验证的（如实照录，不粉饰）**：三绿它没重跑（当时树上已有 #715／#717 的在途改动，跑出来不可归因，它只静态证了 `gen-cli --check` 的实质）；本件的绝对读数 `727957`／9 页／两只种子库它无法复现（没有种子库），只做了内部自洽核对；§3.2 同窗对摆没跑（需把树摆回搬迁前，超出它只许写 `.scratch/714-review/` 的写面）；编译入口它**故意没在脏树上跑**（会编译别席半成品并再写 `dist`），改用自己的直跑 TS 渲染器绕过。

## 八 · 一个「看着像注释、其实是页面字节」的前车（给后续所有搬迁票）

（已在 §2.1 与 §六.2 记过，此处只留一行索引，免得被当成两件事。）

## 九 · 持锁运行对账声明（协议 §2.4 第 2 条）

证据里**声称**跑过的每次运行，都在导出件 `t714-运行记录.txt` 里有**一对一**条目（同一 runId）：

```
GATE-RUN runId=33c7a033-549e-41fa-9094-0564788ecb47 cmd="node .scratch/714/seq.mjs D .scratch/714/steps-D.json（再摆到搬迁后）"           exit=0
GATE-RUN runId=cbdb3d42-9912-47f7-9026-c0600e96f352 cmd="node .scratch/714/seq.mjs E .scratch/714/steps-E.json（主判据＋同窗对摆＋自检）"  exit=0
GATE-RUN runId=31cafc92-de85-48a7-ad0c-d67ec00fb36b cmd="node .scratch/714/seq.mjs F .scratch/714/steps-F.json（变异自证＋告警线演练＋生成链）" exit=0
GATE-RUN runId=c6b2e43b-8a19-41b6-91f6-0dfed4ffa311 cmd="node .scratch/714/seq.mjs H .scratch/714/steps-H.json（基线三绿＋提交）"          exit=0
GATE-RUN runId=03de230a-1360-47f4-a5b9-3688b649f952 cmd="node .scratch/714/seq.mjs K .scratch/714/steps-K.json（归档脚本改锚＋提交）"      exit=0
GATE-RUN runId=31aea8f4-088f-4567-800d-2abe339b2e61 cmd="node .scratch/714/seq.mjs L .scratch/714/steps-L.json（证据件 §五 补充＋提交）"  exit=0
GATE-RUN runId=3841fedd-715d-404b-9153-d9a6b78cd9ce cmd="node .scratch/714/seq.mjs N .scratch/714/steps-N.json（清 dist 残渣＋复跑门）"     exit=1（N7 告警线被别席中间态挡下，见 §3.5）
GATE-RUN runId=e00b2d32-2beb-4fb2-aba6-2e16742ee94c cmd="node .scratch/714/seq.mjs P .scratch/714/steps-P.json（可归因告警线＋基线）"      exit=1（P2 基线被别席未暂存的删除挡下，见 §3.5）
```

（失败那几次也留在导出件里，不删：开窗三次被 #717 的中间态挡下、C 窗对摆暴出 §2.1 那处自家改动、
F 窗第一次被变异锚点命中数挡下、G 窗被 `baseline.mjs` 的「树须与索引一致」挡下、N/P 两窗被别席的
未同步台账与未暂存删除挡下 —— 每一步都在本件里有名有姓。对账按协议 §2.4 只领 exit=0 的条目，
未声明的调试红不按裸跑追究。）
