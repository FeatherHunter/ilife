# #367 独立复核 · 呈现面的门（框架级）

> 复核对象：`506a64d`（feat，5 件）＋`d0da878`（docs，2 件）。复核者＝MAP#158 独立第二席，只复核、不改实现、不关票。
> 票面权威：`gh api repos/FeatherHunter/ilife/issues/367`（默认分支上 **comments=0**，票面正文即全文）：目标＝让
> 「代表唤醒词必须是唤醒词表里真有的词」这道门**覆盖每个能力目录**；验收＝门的扫描面覆盖各能力目录的声明 ＋
> 三处命令名取自同一份注册表、**双向差集为空**；负向＝从唤醒词表删掉某条代表唤醒词 → 门必红；
> 不许动＝棘轮数字（只加扫描面）、其它门的判据。复核日 2026-09-15（本地 +08:00，日志时刻 UTC）。

## 〇 · 判定

**PASS。** 验收两条我都自己持锁重跑过、并且**另写一份独立探针**（不引门里任何代码）从 **src 侧**与 **dist 侧**各算一遍：
扫描面确实铺到 **10 个能力目录的 127 条声明**；双向差集两向皆 0；票面的负向对照（从词表删一条代表唤醒词）
逐条复现——**门红并点名**、**逐字节复原**（sha 相同）、**复跑绿**；棘轮两件与其它门的判据/读数一字未动。
三条 S3 级缺陷（登记表按键豁免、门不查「词路由回同键」、对账导出件漏一窗）见 §七，均未推翻验收。

## 一 · 现场与环境（先说清楚，免得读数被误读）

| 项 | 值 |
|---|---|
| 仓库 | `D:\ilife`；HEAD 起查时 `d0da878` |
| 工作区 | **他席在途脏**：复核期间 `M` 件最多 17 个（`src/diet/*`、`src/render/*`、`src/workout/planStore.ts`、`src/workout/write.ts`、`src/triggers/routes.generated.ts`、`AGENTS.md` 等），并随他席提交起伏；**本票五个声明路径全程不在 `M` 列表内** |
| 编译口径 | 窗口内一律 `node node_modules/typescript/bin/tsc -b packages/skill-calorie`（**未跑** `pnpm gen`／`gen:check`／`pnpm --filter build`） |
| 门读的对象 | 新门读 **`dist/`**（`dist/<能力>/index.js` 的声明数组 ＋ `dist/cli/registry.js` 的 `REGISTRY` ＋ `dist/triggers/wake-assets.js` 的词表）⇒ 复跑前必须先 `tsc -b`；本条已在门件头写明 |
| 真库 | `SKILLS_DB_PATH=D:\2Study\StudyNotes\.db`；复核全程该目录内 20 个库文件最新时间＝`calorie_data.db 2026/9/14 13:53:37`、`calorie_html/ 2026/9/14 21:36:47`，全部早于我全部窗口（`2026-09-15 02:26Z–02:32Z`）⇒ **真库零写入** |
| 已知环境抖动 | `2026-09-15T02:29:53Z` 窗口内 `tsc -b` 曾退出 1（4 条 `src/workout/planStore.ts(121/127/146) TS2304: Cannot find name 'dayPhrase'`）＝**他席在途件**；`02:31Z` 窗口已回 exit=0。四个棘轮门／六个身体域门／新门读数在抖动前后**完全相同** |

## 二 · 提交范围（第 1 条）

`506a64d` 5 件：`.changeset/367-唤醒词门.md`、`docs/skills/skill-calorie/t367-门证据.md`、
`docs/skills/skill-calorie/t367-gate-runs.log`、`packages/skill-calorie/src/body/commands.ts`、
`packages/skill-calorie/test/t367-唤醒词门.test.mjs`。
`d0da878` 2 件：`t367-gate-runs.log`、`t367-门证据.md`（都只动 docs）。

- **0 命中**：两笔提交的 `--name-only` 里没有 `src/triggers/routes.generated.ts`、没有 `src/triggers/wake-assets.ts`、
  没有 `SKILL.md`、没有 `cmd-registry-294`／`legacy-ratchet-295`／`legacy-frozen-295`／`exercise-wakewords-266`、
  没有 `src/` 下别的能力目录（`src/` 面只动 `body/commands.ts` 一件）。
- `src/body/commands.ts`：`git diff --numstat 506a64d^ 506a64d` ＝ **10＋/5−**（与作者报的一致）。把两侧的
  `{ kind: … }` 声明行按 `wakeWord: '<X>'` 归一后 `Compare-Object` ⇒ **差异只在那 5 行的 `wakeWord` 字段**
  （另 5 行 ＋ 是件头注释块）：`composition-add` 记体脂→记体脂（皮褶钳）；`view.body-composition`／`view.body-measure`
  **补**真词；两条向导**去掉** `wakeWord` 字段。
- 附带说明：`git diff 506a64d^..d0da878` 会多出 `src/weight/{logReceipt,receipt}.ts` 等 4 件——那是夹在中间的他席提交
  `0655d56`，**不在本票两笔之内**。

## 三 · 门读数（第 2 条，逐字复制门自己打的机器行）

新门四次读数（`t367r-gates-before` / `t367r-negative` / `t367r-after` / `t367r-gates-after`，窗口内 `node --test`）：

```
SCAN dirs=10 table=434 registry=127
DIR analysis decl=22 / DIR body decl=10 / DIR diet decl=25 / DIR exercise decl=10 / DIR goal decl=14
DIR home decl=4 / DIR photo decl=11 / DIR profile decl=5 / DIR weight decl=9 / DIR workout decl=17
SCAN-TOTAL decl=127 withWord=115 blank=12
OFF-TABLE 19 registered=19 unregistered=0
DIFF-DIR-ONLY 0 DIFF-REG-ONLY 0
SELFTEST offTable=1 blank=0 registered=1
RESULT: 5/5 通过 exit=0
```

**扫描面＝10 个能力目录，不是只跑体重那一份**（体重那份是既有 `cmd-registry-294` 里的 `for (const spec of WEIGHT_COMMANDS)`，
`:296-301`，本票未动）——逐目录读数逐对上 `src/<能力>/commands.ts` 的文件系统扫描（我自己数的 decl
22/10/25/10/14/4/11/5/9/17 ＝ 127，与门一致）。

## 四 · 双向差集（第 3 条，我自己的两份探针）

探针 `.scratch/t367-review/probe-src.mjs`（不 import 门里任何东西；字面量解析 `src/**` 与 `dist/**`）：

```
TABLE srcEntries=434 distEntries=436 distDistinct=434 onlySrc=0 onlyDist=0      ← 词表两侧集合相等
DIRS 10 analysis／body／diet／exercise／goal／home／photo／profile／weight／workout
SRC-DECL total=127 withWord=115 blank=12 distinct=127
REGISTRY-SOURCES srcDirs=10 …（与文件系统扫描逐一相同，两侧差 ∅）
REGISTRY-KEYS 127
DIFF-DIR-ONLY 0 DIFF-REG-ONLY 0        ← 跨层算的：src 文本声明 ↔ dist 注册表
OFF-DECL 19 {"analysis":8,"diet":4,"goal":2,"photo":2,"profile":1,"weight":1,"workout":1}
OFF-REG  19                             ← 注册表侧算的同一批行（逐键逐词相同）
BODY calorie.body.composition-add 「记体脂（皮褶钳）」 inTable=true reg="记体脂（皮褶钳）"
BODY calorie.view.body-composition 「看体脂」 inTable=true reg="看体脂"
BODY calorie.view.body-measure 「看围度」 inTable=true reg="看围度"
BODY calorie.view.composition-wizard wakeWord=(缺) / measure-wizard wakeWord=(缺)
```

**「DIFF-DIR-ONLY 0／DIFF-REG-ONLY 0」名副其实**：门是在 dist 内部比（同一批编译产物的两个视图），
我额外做了**跨层**比（源文本 ↔ dist 注册表）也是 0／0；生成物 `src/cli/registry.ts` 的 `SOURCES` 名单
恰是文件系统扫出的那 10 个目录，无多无少。

## 五 · 负向对照（第 4 条，票面那条＋dist 变异串自证）

窗口 `t367r-negative`（`.scratch/t367-review/window-negative.mjs`，一个持锁窗口内做完全部七步）：

```
N0 SRC sha=73262a20…ba6c mtime=2026-09-15T02:14:29Z dist mtime=02:14:29Z
N1 PRE srcHits=1 distHits=1                    ← 变异目标串（"wake_word": "看体脂",）源与 dist 各恰好 1 处
N3 MUTATED sha=8062fff1…e530 «"看体脂"→"看体脂复核变异"» mutationHits=1   （并碰 mtime）
N4 TSC(变异后) exit=0
N5 DIST-PROOF mutationHits=1 真词行命中=0      ← **dist 真的带上了变异版**（不是只看门退出码）
N6 GATE-RED exit=1   OFF-TABLE 20 registered=19 unregistered=1
   UNREGISTERED body calorie.view.body-composition 「看体脂」
   RESULT: 4/5 通过（红：③ 代表唤醒词必须是唤醒词表（436 条）里真有的词） exit=1
N7 RESTORED sha=73262a20…ba6c equal=true       ← 逐字节复原
N8 TSC(复原后) exit=0
N9 DIST-PROOF mutationHits=0 真词行命中=1      ← dist 里零命中变异串
N10 GATE-GREEN exit=0  OFF-TABLE 19 registered=19 unregistered=0  RESULT: 5/5 通过 exit=0
N11 wake-assets-133 exit=0                     ← 复原后词表旁证（436↔场景表逐字）
N12 git status -- src/triggers/wake-assets.ts = 空
```

作者报的 Windows 坑我复现并按同样对策处置：**`CopyFile` 保留源时间戳 ⇒ `tsc -b` 增量不重建 dist**。
故变异腿与复原腿都先 `LastWriteTime = now`（只碰 mtime、不动内容）再编译，并以「dist 里变异串命中数」自证
（`N5=1`／`N9=0`）。窗口内**未跑** `pnpm gen`／`gen:check`／包 build，只 `tsc -b`。

## 六 · 登记表鉴别力（第 5 条，自设探针）

窗口 `t367r-probe`（`.scratch/t367-review/window-probe.mjs`）：

```
P0 sha(body)=d4ecbcf2…918e  sha(goal)=d2dab7bb…0c74
I1-body-fake      FROM=「看体脂」 TO=「看体脂复核假词」 distHits=1 GATE exit=1
   OFF-TABLE 20 registered=19 unregistered=1
   UNREGISTERED body calorie.view.body-composition 「看体脂复核假词」
   RESULT: 4/5 通过（红：③…）      ← 未登记键上的表外词：必红且点名
I2-body-realword  FROM=「看体脂复核假词」 TO=「看围度」（436 表真词） distHits=2 GATE exit=0
   OFF-TABLE 19 registered=19 unregistered=0   RESULT: 5/5 通过   ← 改成真词：必绿
I3-RESTORE body   shaEqual=true gitStatus=[] GATE exit=0（5/5）   ← 复原无残留
II1-goal-registered-key-newfake  FROM=「看目标状态」 TO=「看目标状态复核假词」（同为表外词） distHits=1
   OFF-TABLE 19 registered=19 unregistered=0   GATE exit=0  RESULT: 5/5 通过
   ← **已登记键换成另一个表外词：门仍然全绿**
II2-RESTORE goal  shaEqual=true gitStatus=[] GATE exit=0（5/5）
PROBE-WINDOW end shaEqual=true
```

三条结论（都可复跑）：

1. **新键上的表外词必红并点名**——登记表**不是**把门蒙起来的静默豁免（票面要求的部分成立）。
2. **已登记键是「按键豁免」且登记表第三列（词面）从不断言**：`REGISTERED = new Set(dir|key)`、
   ④ 只用 `dir|key`，`e[2]` 只进报错文字 ⇒ 在该键上换任何表外词都不会红。
3. **门的 ② 也救不了第 2 条**：`dist/cli/registry.js` 的 `REGISTRY` 是 `build(SOURCES)` 用**同一批编译出来的数组**
   现算的，声明改一字两侧一起改 ⇒ ② 抓的是「能力目录漏在生成物 `SOURCES` 名单外」，抓不到已登记键的词面漂移。
   同一条也解释了 I2：`看围度` 落在 `calorie.view.body-composition` 上仍 5/5——**门不查「词路由回同键」**。

## 七 · 棘轮与既有门（第 6、8 条）

四个棘轮/旁证门 ＋ 六个身体域门，**动前（02:27Z）／动后（02:31Z）各一次，读数完全一致**：

| 门 | 动前 | 动后 |
|---|---|---|
| `cmd-registry-294` | `tests 7 / pass 7 / fail 0` exit=0 | 同 |
| `legacy-ratchet-295` | `6 / 6 / 0` exit=0 | 同 |
| `wake-assets-133` | `8 / 8 / 0` exit=0 | 同 |
| `exercise-wakewords-266` | `RESULT: 4/4 通过 exit=0` | 同 |
| `wizard-86` | `10 / 10 / 0` exit=0 | 同 |
| `t357-skinfold` | `8 / 8 / 0` exit=0 | 同 |
| `t363-补记查冲突` | `7 / 7 / 0` exit=0 | 同 |
| `t364-删除取快照` | `7 / 7 / 0` exit=0 | 同 |
| `t365-七条写词整页` | `11 / 11 / 0` exit=0 | 同 |
| `t366-复制执行闭环` | `14 / 14 / 0` exit=0 | 同 |

**棘轮文件本身未被改**：`git status --porcelain`／`git diff HEAD --` 对 `test/cmd-registry-294.test.mjs`、
`test/legacy-ratchet-295.test.mjs`、`test/legacy-frozen-295.mjs` 均为空；两个冻结读数仍是
`FROZEN_READ_LINES=1146`／`FROZEN_WRITE_LINES=745`，`FROZEN_LEGACY_MAX=92`／`FROZEN_LEGACY_LINES=119`。
本票只在**新件**里加了扫描面与登记表，未给既有棘轮加码。

**两条读命令的词是真词且路由回同键**（探针 `probe-routes.mjs`，读 `dist/triggers/index.js`＋`routing.js`）：

```
CHECK calorie.body.composition-add 「记体脂（皮褶钳）」 inTable=true 路由数=1 回同键=1 exec回同键=1
CHECK calorie.view.body-composition 「看体脂」            inTable=true 路由数=1 回同键=1 exec回同键=1
CHECK calorie.view.body-measure     「看围度」            inTable=true 路由数=1 回同键=1 exec回同键=1
WIZARD calorie.view.composition-wizard「看体脂向导」inTable=false 路由数=1（routes.generated.ts:505 exec）
WIZARD calorie.view.measure-wizard   「看围度向导」inTable=false 路由数=1（routes.generated.ts:504 exec）
```

## 八 · 两条口径裁决（第 7 条）

**① 19 条登记表算不算「放宽」——算「有限的放宽」，但不是静默豁免。**
理由＝§六 三条读数：新表外词（未登记键）必红点名；**已登记键换任何表外词仍绿**（`II1` 实测）；
② 因两侧同源而管不到词面。所以它对**新错行**有鉴别力，对**那 19 个键**是本门内的长期免检，且登记表第三列
（当时的词）从不与实况比对、陈化不报（只有「变回真词」才在 ④ 报陈化）。作者提交信息里
「登记＋双向断言＝有鉴别力的登记」这句**说得比实测强**。
可执行收紧（三行、不动棘轮）：把登记从 `dir|key` 升成「词面双断言」——
`const row = ALL_ROWS.find(r => regKey(r.dir, r.key) === k); assert.equal(row?.wakeWord, e[2], '登记词与声明词漂移：' + …)`，
即登记表写哪个词就必须是现在那个词（换词即红、清掉即红由 ④ 保留）。

**② 两条向导键去掉代表词，照 #450 流程内页先例——认。**
依据我自己核过：436 表里**没有** `看体脂向导`／`看围度向导`（`TABLE-HAS …=false`）；两个键**各只有 1 条路由词**
（`routes.generated.ts:504/505`）且都不是表里的词 ⇒ 没有真词可回同键；契约 `shared/commandSpec.ts` 的
`readonly wakeWord?: string` 注解明写「**可缺**——缺了速查表退回命令名本身」；#450 先例
（`t450-词表订正.md:45`）正是照片域三条流程内页**删掉 `wakeWord` 字段**、理由同。附带条件两条：
(a) 两个自造词**留在路由面**（用户说得出、命得中，实测仍各 1 条 exec 路由），删／并入属路由面收口票；
(b) `SKILL.md` 速查表 5 行陈旧（全在 body、正是本票改的 5 行）必须随 #368 重生成落地——本票按红线未碰
`SKILL.md`（两笔提交无它，`git status` 也为空）。

## 九 · 对账面（我另外核的）

- 作者证据 §六 列的 12 个 `GATE-RUN`：导出件 `docs/skills/skill-calorie/t367-gate-runs.log` 里 12 行 `RUN ticket=367`，
  **逐行在活日志 `.scratch/locks/gate-runs.log` 中逐字存在**（缺失 0 条），**12/12 都带 `cmd=`**，
  exit 与声称一致（`t367-judge-first exit=1` 为有意红，其余 0）。
- 活日志里票号 367 的运行共 **13** 条：多出 `t367-commit2 cmd="pwsh -NoProfile -File .scratch/t367/commit2.ps1" exit=0 at=02:21:11Z`
  （晚于 `t367-final` 02:20Z，即第二笔 docs 提交那一窗）——**既不在对账表、也不在导出件切片内**。
- 一次抢锁：`LOCK-STOLEN ticket=367 runId=t367-commit reason=owner-dead ownerAlive=0 ownerBelongsToLock=1`，
  属协议 R-2-1-3 的正当抢回（死 owner），非活锁抢占。
- 本次复核自证：`t367r-probe-src`／`-src2`／`-gates-before`／`-negative`／`-probe`／`-gates-after`／`-routes`＋提交窗，
  全部经 `tooling/run-locked.mjs --ticket 367 --poll-ms 15000 --max-wait-ms 600000 -- …`，`cmd=` 齐全。

## 十 · 缺陷清单

**本票引入／范围内**

- **S3-A（登记表按键豁免）**：见 §八①。建议按上面三行收紧，或在门件头把「登记＝该键在词面上免检」写明。
- **S3-B（门不查路由回同键）**：`I2` 实测 `看围度` 落在 `calorie.view.body-composition` 上仍 5/5；
  既有 `cmd-registry-294:301` 对体重域是查这一条的（`assert.equal(r.key, spec.key)`）。本票门的判据只到「词在表里」。
  建议加一条读数/判据 `routesFor(spec.wakeWord).some(r => r.key === spec.key)`，或在门件头显式写「本条不在本门范围」。
- **S3-C（对账导出件漏一窗）**：`t367-commit2` 未进对账表与导出切片（§九）。修法＝表里补一行
  `GATE-RUN runId=t367-commit2 cmd=pwsh -NoProfile -File .scratch/t367/commit2.ps1 exit=0`，
  导出切片末行改为 `t367-commit2`（或注明「切片止于 t367-final，其后另有 commit2 一窗」）。
- **S3-D（交付物路径与票面字面不一致）**：票面「交付物路径」写 `docs/skills/skill-calorie/t15-门证据.md`，
  实交 `t367-门证据.md`（按票号命名更合理，但票面字面不同）。建议证据件首行注明票面出处，或由编排确认改口径。

**范围外（只报 S3＋转票措辞）**

- **S3-E（他席在途红）**：`src/workout/planStore.ts(121/127/146) TS2304: Cannot find name 'dayPhrase'`（未提交、
  复核窗口 02:29Z 曾使本包 `tsc -b` 退出 1），以及 `calorie-routing-81`／`sport-homogeneity-109` 等
  作者已登记的他席在途红。**转票措辞**：「workout 域 `planStore.ts` 未提交改动使整包编译不过（TS2304 ×4），
  请该席收口或转 `#511` 之外的 workout 票；本票只读复核，不代改。」

**范围外（S3，供编排）**

- **S3-F（`calorie.help.center`「卡路里HELP」的口径）**：它按登记躺在表外，作者已写明「本门该不该接受它属 HELP 口径问题，
  归照片／HELP 侧裁定」。**转票措辞**：「请 HELP／照片侧裁定技能级 HELP 词（`卡路里HELP`）是否属 436 场景词表例外；
  裁定前保持登记，勿在 #367 里放宽。」

## 十一 · 下一手缺什么（给编排）

1. **S3-A 的抉择**：要不要把登记表升成词面双断言（三行改动）——建议要，代价极小、收益是「已登记键换假词也红」。
2. **S3-B 的抉择**：本门要不要把「词路由回同键」纳入判据（既有 #294 已有同形判据可抄）。
3. **S3-C／S3-D**：纯文档面，可由本票补一笔或随 #368 一并落地。
4. **#368 的硬前置**：`SKILL.md` 速查表 5 行陈旧（`calorie.body.composition-add`／`view.body-composition`／
   `view.body-measure`／两条向导）必须随 HELP 重建落地——本票按红线未碰它。
5. **路由面收口票**：`看体脂向导`／`看围度向导` 两条自造入口词要不要从路由面删／并入（与 #266 转票的 6 条同办）。
6. 若要复核我这份复核：`.scratch/t367-review/` 是一次性现场，已按守卫清理（脚本与原始输出留在本节所列运行记录里，
   `.scratch/t367-review/probe-src.mjs`／`window-negative.mjs`／`window-probe.mjs`／`probe-routes.mjs` 四个探针的口径
   见 §四／§五／§六／§七 的机器行）。
