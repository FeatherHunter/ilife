# #481 文本审查 · 对比族（17 词 · 体重场景 03）

> 口径正本：`.scratch/t154/text-review/口径.md`（五条红线／术语替换表／页脚句式／数字写法／值槽纪律）。
> 编辑点清单：`.scratch/t154/text-review/literals/compare.ts.txt`（67 点）＋ `weightCompare{,2,3}.ts.txt`。
> 读者画像：**不懂技术的项目负责人**（不知道 σ／n=1／门槛／情景代号／库表名是什么）。

## 一、头部

- **票号**：#481（体重 · 对比族 17 词文本审查）；**范围**：第 7／14／21／28／35／39／43／46／50～58 条唤醒词。
  - 窗口面 9 页（`a1–a8`＋`b1` 别名）：07／14／21／28／35／39／43／46 ／（第 48 条归 #436，本票一字符不碰）。
  - 情景面 8 页（`b8`／`e1`／`e2`／`e3`×2／`e5`／`e6`／`c5`／`d4`）：50／51／52／53／54／55／56／57／58。
  - 注：清单里 07／14／21／28／35／39／43／46 是**窗口面**（眉标 `… · 运动身体域`，本票不动）；
    50～58 是**情景面**（眉标印内部代号，本票整行删）。逐页副标题已核（见逐页表）。
- **写集清单**（件 ＋ 改前→改后 LF；LF＝`\n` 计数）：

| 件 | 改前 LF | 改后 LF | 改前字节 | 改后字节 |
|---|---|---|---|---|
| `packages/skill-calorie/src/weight/compare.ts` | 558 | 572 | 29195 | 30887 |
| `packages/skill-calorie/src/weight/weightCompare.ts` | 225 | 234 | 11548 | 12153 |
| `packages/skill-calorie/src/weight/weightCompare2.ts` | 209 | 210 | 10930 | 10978 |
| `packages/skill-calorie/src/weight/weightCompare3.ts` | 192 | 193 | 9891 | 10119 |
| `packages/skill-calorie/test/weight-compare-334.test.mjs` | 88 | 103 | — | — |

- **改动落点（源码）**：`compare.ts` 的 `perDayG`／`rhythmOf`／`premiseNotice`／`sourceText`／`COMPARE_COLUMNS`／
  `renderSegmentsTable`／`compareConclusion`／`deltaCard`／`segmentCard`／`rhythmCard`／`compareCopyArea` 的节奏键／
  `buildWeightCompareDoc`／`buildScenarioCompareDoc`；`weightCompare.ts` 的 `a2/a3/a6-a8`；
  `weightCompare2.ts` 的 `e2/e3/e5/e6`（新增 `ratePerDayText`，住 `weightCompare.ts`）；`weightCompare3.ts` 的 `c5/d4`。
- **口径 §4 明令不动的**：眉标（非情景页 9 页的 `… · 运动身体域`）、复制区形态与深色 toast、第 48 条整页、
  `plate.ts`／`plateDocs.ts`／`figures.ts`／`records.ts`／`shared/*`／`base-render/*`——本票零触碰。

## 二、逐页表（17 页，逐页一组；数据源＝`.scratch/t154/text-review/b481/diff-real.log` 改前→改后逐行差分）

> 判据一栏：**冗**＝百分百冗余（同屏同一事实说两遍以上，删掉一处读者不丢信息）；**难**＝看不懂的专业话；
> **形式**＝该换展示形式（只用已有公共件）；**纪律**＝口径 §3 统一句式／数字／术语替换。

### 第 7 条 07 对比体重：最近 30 天 vs 之前 30 天（窗口面）

| 项 | 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|---|
| 节奏卡 | `节奏` ＋ 值槽 `+10 g/天` ＋ 副说明 `幅度更大 · 本期 +0.3 kg（日均 +10 g/天） vs 对比期 +0.1 kg（日均 +3 g/天）` ＋ 徽章 `日均速率对照` | `每天变化` ＋ 值槽 `每天 +10 克` ＋ 副说明 `幅度更大` ＋ 徽章 `两段可比` | 冗／纪律 | 值槽与副说明**同一个数印两遍**；两段净变化表里已有（`变化 +0.3 kg`／`+0.1 kg`）⇒ 删；单位两种（`kg` 与 `g/天`）⇒ 统一「克」 |
| 表题 | `两期对比（均值差 +0.3 kg · 上升 ↑ · 节奏幅度更大）` | `两期对比（均值差 +0.3 kg）` | 冗 | 括号堆三样，方向与节奏判断结论块与卡片各有一处 |
| 表头 | `期别`／`期首` | `时段`／`期初` | 难 | 「期别」不是人话；「期首」与同页「期末」不对称 |
| 结论 | `本期均值 70.3 kg vs 对比期均值 70 kg，差值 +0.3 kg（上升 ↑），节奏幅度更大（本期 +0.3 kg（日均 +10 g/天） vs 对比期 +0.1 kg（日均 +3 g/天））。` | `本期均值 70.3 kg vs 对比期均值 70 kg，差值 +0.3 kg（上升），每天变化量：本期：每天 +10 克 vs 对比期：每天 +3 克，幅度更大。` | 冗／难 | 两段净变化删（表里已有）；`g/天`→「每天 +N 克」；方向箭头删（判断语已说方向） |
| 页脚 | `📊 数据来源:calorie_data.db · weight_log · 本期 …（30 条）／对比期 …（30 条） · 区间内记录齐` | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 本期 2026-08-09 ~ 2026-09-07 ｜ 30 条／对比期 2026-07-10 ~ 2026-08-08 ｜ 30 条 ｜ 区间内记录齐` | 纪律 | 口径 §3.1 统一句式（全角冒号 ＋ `体重记录` 人话名 ＋ `｜` 分隔） |

### 第 14 条 14 对比体重：自定义两段时间（窗口面）

| 项 | 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|---|
| 节奏卡 | `节奏`／`+17 g/天`／`幅度更大 · 本期 +0.1 kg（日均 +17 g/天） vs 对比期 +0 kg（日均 +0 g/天）`／`日均速率对照` | `每天变化`／`每天 +17 克`／`幅度更大`／`两段可比` | 冗／纪律 | 同第 7 条；`+0 g/天` 这种写法本身违反「正零不写加号」 |
| 表题／表头／结论／页脚 | 同第 7 条四种 | 同上 | 同上 | 同上（共用一处定义，两面同时生效） |
| 值槽零值 | `对比期 每天 0 克`（原 `+0 g/天`） | 同左 | 纪律 | 口径 §3.3：正零不写加号 |

### 第 21 条 21 对比体重：本周 vs 上周（窗口面 · 本期只有一天）

| 项 | 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|---|
| 页顶横幅 | 标题 `样本与口径`；首行 `⚠️ 本期 1 条，低于门槛 3 条。`；说明 `每段要 ≥3 条才有像样的均值差；样本不够也照常出页，差值与节奏只当参考值看；本期只有一天：均值就是当天读数，期初／期末／变化三列并成一列，节奏无从对照，这一天不代表趋势。` | 标题 `记录与说明`；首行 `⚠️ 本期 1 条，低于 3 条。`；说明 `两段各要 3 条以上，均值差才有参考价值。记录不够也照常出页，差值与每天变化量只当参考值看；本期只有一天：均值就是那天的读数；这一页期初／期末／变化三列并成一列；只有一天，看不出快慢，也不代表趋势。` | 难 | 「门槛」「样本」「口径」「无从对照」四个工程词换人话；分号串改成三段 |
| 两段卡徽章 | `单日数据` | `只有一天` | 难 | 口径 §3.2：`n=1`／单日段写「只有一天」；徽章只说状态 |
| 节奏卡 | `节奏`／值槽 `—`／副说明 `本期 +0 kg vs 对比期 +0.1 kg：单日段没有跨天跨度，读不出速率`／徽章 `无从对照` | `每天变化`／值槽 `—`／副说明 `看不出快慢`／徽章 `看不出快慢` | 冗／难 | 副说明与徽章重复同一判语 ⇒ 副说明只留判语；两段净变化归表格「变化」列 |
| 结论 | `…差值 +0 kg（持平 →），节奏无从对照（本期 +0 kg vs 对比期 +0.1 kg：单日段没有跨天跨度，读不出速率）。 含单日段（n=1）：均值＝当天读数，不代表趋势。` | `…差值 +0 kg（持平），看不出快慢：本期：读不出快慢 vs 对比期：每天 +17 克（记录不足 2 条）。` | 冗／难 | 原句同一事实三遍（`无从对照`＋`读不出速率`＋`含单日段`）⇒ 只留一处；`n=1`→人话；方向箭头删 |
| 表格 | 均值格 `70.4（n=1）` | `70.4` | 冗 | 「只有一天」页顶说明与卡片徽章已各说一处，表内第三处删 |
| 表题／表头／页脚 | 同第 7 条 | 同上 | 同上 | 同上 |

### 第 28 条 28 对比体重：本月 vs 上月（窗口面）

本页无改动以外的额外项：节奏卡／表题／表头／结论／页脚四项与第 7 条同形同改（差分逐字见 `b481/diff-real.log:63-79`）。

### 第 35 条 35 对比体重：近 N 天 vs 上一个 N 天（窗口面）

本页同第 28 条（同一套共用定义，四面同时生效）；差分逐字见 `b481/diff-real.log:81-97`。

### 第 39／43／46 条 对比体重：今天 vs 一年前／半年前／三月前今天（窗口面 · 两段各只有一天）

| 项 | 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|---|
| 页顶横幅 | `样本与口径`／`⚠️ 本期 1 条／对比期 1 条，低于门槛 3 条。`／`每段要 ≥3 条…本期／对比期只有一天：均值就是当天读数，期初／期末／变化三列并成一列，节奏无从对照，这一天不代表趋势。` | `记录与说明`／`⚠️ 本期 1 条／对比期 1 条，低于 3 条。`／`两段各要 3 条以上，均值差才有参考价值。记录不够也照常出页，差值与每天变化量只当参考值看；本期／对比期只有一天：均值就是那天的读数；这一页期初／期末／变化三列并成一列；只有一天，看不出快慢，也不代表趋势。` | 难 | 同第 21 条 |
| 两段卡徽章 ×2 | `单日数据`（两处） | `只有一天`（两处） | 难 | 同上 |
| 节奏卡／表题／表头／结论／页脚 | 同第 21 条五种 | 同上 | 同上 | 同上 |
| 补充对照 | `容差命中 2025-09-07(±0 天)`／`一年前内区间均值 6?  kg` | `一年前那天 2025-09-07（前后 0 天内的最近一条）`／`最近 365 天均值 … kg` | 难 | 「容差」是工程词；`(±0 天)` 这种半角括号混排换全角 |

### 第 50 条 50 对比体重：当前 vs 平台期首日（情景面 · `b8`）

| 项 | 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|---|
| 眉标 | `calorie.view.weight-compare · 情景 b8` | **整行删**（`eyebrow: ''`） | 冗／难 | 内部代号；同页副标题已逐字写业务名（口径 §3.2）；非情景页眉标不动 |
| 副标题 | `对比体重：当前 vs 平台期首日 · 平台期首日 2026-08-26(持续 14 天) vs 当前 2026-09-08` | `平台期首日 2026-08-26(持续 14 天) vs 当前 2026-09-08` | 冗 | 业务名与标题行逐字相同 ⇒ 第二处删，留两段区间 |
| 页顶横幅 | 标题／首行／说明（同第 21 条形态） | `记录与说明`／`⚠️ 当前 1 条，低于 3 条。`／`两段各要 3 条以上…当前只有一天：均值就是那天的读数；…；只有一天，看不出快慢，也不代表趋势。` | 难 | 同上 |
| 两段卡徽章 | `单日数据` | `只有一天` | 难 | 同上 |
| 节奏卡 | `节奏`／`—`／`当前 +0 kg vs 平台期首日 +0.1 kg：单日段没有跨天跨度，读不出速率`／`无从对照` | `每天变化`／`—`／`看不出快慢`／`看不出快慢` | 冗／难 | 同上 |
| 表题 | `对比体重：当前 vs 平台期首日（差值 +0.06 kg ↑ 上升 · 节奏无从对照）` | `对比体重：当前 vs 平台期首日（差值 +0.06 kg）` | 冗 | 方向与节奏判断结论块已有 |
| 表格 | `70.4（n=1）` | `70.4` | 冗 | 表内第三处删 |
| 结论 | `…差值 +0.06 kg（上升 ↑），节奏无从对照（当前 +0 kg vs 平台期首日 +0.1 kg：单日段没有跨天跨度，读不出速率）。 含单日段（n=1）：…` | `…差值 +0.06 kg（上升），看不出快慢：当前：读不出快慢 vs 平台期首日：读不出快慢（记录不足 2 条）。` | 冗／难 | 同一事实三遍 ⇒ 一处 |
| 页脚 | `📊 数据来源:…` | `📊 数据来源：体重记录（…） ｜ … ｜ … ｜ 区间内记录齐` | 纪律 | §3.1 |

### 第 51 条 51 对比体重：当前 vs 历史最低（情景面 · `e1`）

与第 50 条同形同改（眉标整行删／副标题压区间／横幅人话／徽章／节奏卡／表题／结论／页脚八项）；
差分逐字见 `b481/diff-real.log:218-249`。

### 第 52 条 52 对比体重：当前 vs 历史最高（情景面 · `e2`）

| 项 | 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|---|
| 八项同上 | 同第 50 条 | 同上 | 同上 | 同上 |
| 补充对照 | `下降速率 0.019 kg/天` | `每天变化 19 克` | 纪律／难 | `kg/天` 与同族「克」两种单位并存 ⇒ 统一；标签换人话 |

### 第 53 条 53 对比体重：减重 5kg 那天 vs 今天（情景面 · `e3`）

| 项 | 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|---|
| 八项同上 | 同第 50 条 | 同上 | 同上 | 同上 |
| 补充对照 | `期间速率 -0.016 kg/天` | `每天变化 每天 -16 克` | 纪律／难 | 同上 |
| 段标签（卡片／表格／结论／页脚） | `减重 5kg 那天`（数字与单位无空格） | `减重 5 kg 那天` | 纪律 | 口径 §3.3：数字与单位一个空格。**表题里的情景名照旧**（冻结表原文，见下「不做的事」） |
| 轨迹行标签 | `体重轨迹` | `体重变化曲线` | 形式 | 该行驱动 `renderChartBlock`（轨迹图），标签与图标题统一成人话；数据与 spark **一个不删** |
| 表格（两段各只有一天） | `今天 70.4（n=1）／减重 5kg 那天 75（n=1）` | `今天 70.4／减重 5kg 那天 75` | 冗 | 见「删掉的信息登记」第 9 条 |

### 第 54 条 54 对比体重：减重 10kg 那天 vs 今天（情景面 · `e3`）

与第 53 条同形同改（差额只在数值与锚点日）；差分逐字见 `b481/diff-real.log:319-350`。

### 第 55 条 55 对比体重：当前 vs 入夏最低（情景面 · `e5`）

与第 50 条同形同改；差分逐字见 `b481/diff-real.log:352-383`。

### 第 56 条 56 对比体重：当前 vs 入冬最低（情景面 · `e6`）

与第 50 条同形同改；差分逐字见 `b481/diff-real.log:385-416`。

### 第 57 条 57 对比体重：运动多 vs 运动少的两个月（情景面 · `c5`）

| 项 | 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|---|
| 八项同上 | 同第 50 条 | 同上 | 同上 | 同上 |
| 段标签 ×3 处（卡片／表格／结论／页脚） | `2026-09(运动最多)`／`2026-08(运动最少)` | `2026-09 · 运动最多`／`2026-08 · 运动最少` | 难／纪律 | 括号混排（半角括号夹中文）换 ` · `，读者一眼读出「哪个月／运动多少」 |
| 补充对照 | `睡眠数据 缺失(外部技能未记录)` | `睡眠 没有记录` | 难 | 「外部技能」是内部概念；「缺失(…)」半角括号混排。**语义变化登记见第四节** |

### 第 58 条 58 对比体重：工作日 vs 周末（情景面 · `d4`）

| 项 | 改前（逐字） | 改后（逐字） | 判据 | 依据 |
|---|---|---|---|---|
| 眉标 | `calorie.view.weight-compare · 情景 d4` | **整行删** | 冗／难 | 同第 50 条 |
| 页顶横幅 | `样本与口径`／`⚠️ 对比期 2 条，低于门槛 3 条。`／`每段要 ≥3 条才有像样的均值差；样本不够也照常出页，差值与节奏只当参考值看。` | `记录与说明`／`⚠️ 对比期 2 条，低于 3 条。`／`两段各要 3 条以上，均值差才有参考价值。记录不够也照常出页，差值与每天变化量只当参考值看。` | 难 | 同上 |
| 两段卡徽章／节奏卡徽章 | `样本不足 2 条`／`样本不足` | `记录太少（2 条）`／`记录太少` | 难 | 口径 §3.2：「样本」→「记录太少」 |
| 节奏卡／表题／表头／结论／页脚 | 同第 7 条五种 | 同上 | 同上 | 同上 |

## 三、删掉的信息登记（不许丢）

| # | 删掉的文本（逐字） | 出现在 | 去处（为什么读者不丢信息） |
|---|---|---|---|
| 1 | `calorie.view.weight-compare · 情景 b8`（e1／e2／e3／e5／e6／c5／d4 同，共 8 页） | 8 页眉标 | 同页副标题逐字写着业务名（「对比体重：当前 vs 平台期首日」）；`eyebrow: ''` 不再渲染该行 |
| 2 | `对比体重：当前 vs 平台期首日`（副标题首段） | 情景页副标题 | 情景卡副说明与表题各有一处同名业务名；删后整页仍剩 2 处（用例断言 `=== 2`） |
| 3 | 方向箭头 `↑`／`↓`／`→`（结论句与表题内） | 结论／表题 | 判断语已写「上升／下降／持平」；差值卡副说明仍保留「方向词 ＋ 箭头」这一处 |
| 4 | `节奏幅度更大`／`节奏无从对照`（表题内） | 表题 | 节奏卡的副说明（`幅度更大`／`看不出快慢`）与结论句各一处 |
| 5 | `本期 +0.3 kg（日均 +10 g/天） vs 对比期 +0.1 kg（日均 +3 g/天）` 中的两段净变化 | 节奏卡副说明／结论 | 表格「变化」列逐行印同一对数（`+0.3 kg`／`+0.1 kg`）；每天变化量归节奏卡值槽与结论 |
| 6 | `日均速率对照`（徽章） | 节奏卡 | 徽章与副说明原是同一事实两处 ⇒ 徽章改说状态（`两段可比`） |
| 7 | `单日数据`（两段卡徽章） | 两段卡 | 换 `只有一天` 状态词；条数在卡副说明 `2026-09-07 · 1 条` |
| 8 | `含单日段（n=1）：均值＝当天读数，不代表趋势。`（结论尾句） | 结论 | 页顶说明「只有一天：均值就是那天的读数」＋两段卡徽章「只有一天」各一处 |
| 9 | `（n=1）`（表格均值格） | 表格 | 同上（第三处）；表格仍保留三个 `—` 表示「三列并成一列」 |
| 10 | `容差命中 …(±0 天)` 行 | a6／a7／a8 补充对照 | 换成同日期的可读写法 `一年前那天 2025-09-07（前后 0 天内的最近一条）`——**不是删**，是改写 |
| 11 | `差值暂无`（差值卡副说明／徽章） | 差值卡 | 换 `差值暂时算不出来`（人话），事实保留 |
| 12 | `缺失(外部技能未记录)` 中的「外部技能」 | c5 补充对照 | 换 `没有记录`。**语义变化**：读者看不到「这条来自作息管家没记」这一层；登记见第四节 |

## 四、语义变化登记（不是纯文本改写，编排者需裁定）

1. **c5 睡眠行**：`缺失(外部技能未记录)` → `没有记录`。原意是「依赖外部技能库、本次没读到」；
   新写法读者读作「没记睡眠」。若编排者要求保留来源说明，建议改成「没有记录（睡眠数据来自作息管家）」。
2. **d4 一致率**：`一致率 83%` → `两段均值接近程度 83%`。分子分母（`1 − |均值差|`，封顶 100%）仍是页上算的数，
   只是把标签换成可自解的写法；该数无外泄口径。
3. **`n=1`／`无从对照`／`门槛`／`样本` 四个记号与词**在本族可见文本中**命中数 0**（机器读数见第五节）。

## 五、机器读数

- **提交**：`14eb13e`（`feat(481): 对比族 17 词可见文本审查…`，6 件 ＋348／−84；add／commit／push 在**同一条持锁命令**里做完，
  runId=b78cadf4-e3b5-4be8-a3bb-f6836d53b4e9）；本证据件自身的收尾改动随 `docs(481)` 第二笔提交落盘。
- **编译（持锁）**：`node tooling/run-locked.mjs --ticket 481 -- npx tsc -b packages/skill-calorie`
  - `runId=a5028041-…` 之后的最后一次为 **exit=0**；此前三次红**全部来自别席在途件**（本票四件零命中）：
    `src/weight/volatilityDoc.ts`＋`volatility.ts`（#485 改 `deviationText` 签名）／`src/weight/logReceipt.ts`＋`receipt.ts`（另一席在途）。
- **出页**：`node .scratch/t154/text-review/render-family.mjs --family compare --out .scratch/t154/text-review/out-compare`
  → `TALLY {"通过":17} TOTAL 17 HTML=17`；末行 `OK-RENDER`；17 页 HTML 合计 **1193936 字节**。
- **差分**：`python .scratch/t154/text-review/b481/diffpages.py` → `SAME-PAGES 0/17 ；CHANGED-PAGES 17 ；DELTA-LINES 460`
  （逐页原始差分：`.scratch/t154/text-review/b481/diff-real.log`）。
  - ⚠️ **编排者注意**：`.scratch/t154/text-review/diff-pages.py` 本机恒打 `0 行变化`（我这边 17 页全变），
    根因在它第 52 行 `if d[:1] in ('-','+')` —— `diff` 项都是单字符前缀，`d[:1]` 恒等于 `d`（多字符串），
    与 `('-','+')` 永不相等 ⇒ 一条都不打印。**修法**：改判 `d.startswith('-') or d.startswith('+')`。
    我用自写的 `b481/diffpages.py`（同 transform，只改这一行）出的数填本表。
- **截图**：`node .scratch/t154/shoot.mjs …` → `SHOT 17 files`；已用 `read_image` 真看 3 张
  （50 平台期首日／53 减重 5kg／58 工作日 vs 周末）：眉标行已消失、无省略号截断、无压字、表格「期初」列头正常、
  页脚单行不折行。
- **用例（持锁）**：`node tooling/run-locked.mjs --ticket 481 -- node --test packages/skill-calorie/test/weight-compare-334.test.mjs`
  → `RESULT: ticket=481 runId=d4bd37de-728e-4ba8-b892-81bff6043db4 waitedMs=10004 exit=0`、
  `ℹ pass 3`／`ℹ fail 0`；旧断言同步收紧（`/未达成/`→`/还没减到/`；新增眉标零代号、
  `无从对照／n=1／仅一天` 命中 0、`g/天／kg/天` 命中 0、情景业务名整页恰 2 处、段标签「减重 5 kg 那天」带空格
  而表题情景名仍是冻结表原文）。
- **变异自证（两行机器读数）**：
  - 改坏：`compare.ts` 的 `word: '看不出快慢'` → `'无从对照'` ⇒ `MUT-TEST exit=1`，
    `AssertionError: b8 旧记号（无从对照／n=1／仅一天）命中数须为 0`（`b481/mut-test.log`）。
  - 改回：⇒ `REVERT-TEST exit=0`（`ℹ pass 3`／`ℹ fail 0`，`b481/rev-test.log`）。
- **门禁声明**：
  - GATE-RUN runId=d4bd37de-728e-4ba8-b892-81bff6043db4 cmd=node --test packages/skill-calorie/test/weight-compare-334.test.mjs
  - GATE-RUN runId=e2dc058d-8de3-4b65-9d61-b8f2b516d69f cmd=npx tsc -b packages/skill-calorie
- **关键词审计**（17 页 HTML 计数，`Select-String -SimpleMatch`）：
  `g/天`=0、`kg/天`=0、`无从对照`=0、`n=1`=0、`仅一天`=0、`门槛`=0、`样本不足`=0、`单日数据`=0、
  `情景 b8`=0、`情景 e1`=0、**`节奏`=0**（全族 17 页一处不留，见下节）；`数据来源：`=17 页（全角冒号）。
- **「节奏／快慢」改用什么词（编排者要的逐字答复）**：
  - 卡片标签：`节奏` → **`每天变化`**；值槽：`+10 g/天` → **`每天 +10 克`**（正零写 `每天 0 克`）。
  - 卡片徽章：`日均速率对照` → **`两段可比`**；记录不足时 `样本不足` → **`记录太少`**。
  - 判语词（卡片副说明／结论）：`幅度更大`／`幅度更小`／`幅度相当`／**`看不出快慢`**（原 `无从对照`）。
  - 结论句整块现在逐字是：`本期均值 70.3 kg vs 对比期均值 70 kg，差值 +0.3 kg（上升），每天变化量：本期：每天 +10 克 vs 对比期：每天 +3 克，幅度更大。`
  - 复制载荷里那个键：`'节奏'` → **`'每天变化量'`**（键名也会出现在 HTML 里，故 `节奏` 整页计数为 0）。
  - ⇒ `test/sport-homogeneity-109.test.mjs:218` 的 needle 请把 `'节奏'` 换成 **`'每天变化'`**（该串在 17 页每页都有）。

## 六、未做项与下一手缺什么

1. **别席在途件挡住包级绿灯**：`src/weight/volatilityDoc.ts`／`volatility.ts`（#485 正在改 `deviationText` 签名）
   与 `logReceipt.ts`／`receipt.ts` 在我这边三次编译里报错；本票四件零命中 ⇒ 包级 `tsc -b` 的绿灯要等那两席落地。
2. **AGENTS.md 台账**（告警线）需编排者统一同步：`compare.ts` 558→572 仍在超线（挂号行已有，值待改）。
   本票按票面**不碰**该台账。
3. **`package-lock` 级口径未做的**：`weightCompare3.ts` 里 `SCENARIO_LABELS` 的 `a5: '…近 N 天…'` 里的 `N`
   仍是字面占位（老实物同名），本票未改；若要改成实际天数需动 `SCENARIO_LABELS` 的键形（影响路由），另开票。
4. **日期区间分隔符** `~`（如 `2026-08-09 ~ 2026-09-07`）仍是全族旧样式，口径 §3 未裁 ⇒ 未动。
5. **复制载荷**：节奏判语键由 `节奏` 改名 `每天变化量`（载荷是机器面，不属可见文本）；载荷里那句「结论」
   仍是本页结论原文逐字（口径 §2 第二条）。
6. **下一手缺什么**：包级绿灯（等 #485／回执席落地）＋ 编排者对第四节两条语义变化的裁定。
7. **不做的事（编排者补的两条已照办）**：① 段标签已统一 `减重 5 kg 那天`（`weightCompare2.ts` 那一处生成点）；
   **`src/weight/routes.ts:53-54` 的唤醒词原句未碰**，且表题里的情景名仍走冻结表原文 `减重 N kg 那天 vs 今天`
   （用例同时断言两种写法各在其位）。② 本次提交按「一条持锁命令里 add＋commit＋push」的形状做完。
