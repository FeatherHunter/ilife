# #565 证据：目标预检页一页只留一条「复制日志」通道

票面：#565（ISSUE《卡路里场景 06：目标预检页一页出两条复制通道（两组「复制日志」按钮）》）。
物件：`packages/skill-calorie/src/goal/precheck.ts`（唯一源码件）。

## 一、影响清单（一行一个文件 ＋ 一句话理由）

| 件 | 为什么动它 |
| --- | --- |
| `packages/skill-calorie/src/goal/precheck.ts` | 预检确认页的整页装配住这一件——上部指令区那一排复制按钮与页底复制区都由它出，两条「复制日志」都在这里 |
| `packages/skill-calorie/test/565-precheck-copy.test.mjs` | 新建用例：11 条带空位写词逐条真跑，钉住「复制日志」1 次／copy-btn 2 个／载荷非空／完整文档 |
| `docs/skills/skill-calorie/565-证据.md` | 本文件：影响清单、改前改后读数、变异两行 |

一个字未动的件（点名）：`src/shared/copyArea.ts`、`src/shared/docPage.ts`、`src/render/copy.ts`、`packages/base-render/**`（#565 票面：只许动 `src/goal/precheck.ts`，`src/goal/` 之外一律不碰）。

## 二、改前读数（2026-09-16 01:0x，当刻盘上实测）

扫件脚本 `.scratch/t153-视觉缺陷/probe.mjs`（负责人肉眼验收那条探针，33 件产物全扫）：

```
件数（命中任一缺陷）：14 / 全量 33
一键定全套目标-预检.html          「复制日志」出现 2 次  copy-btn 元素 4 个
定体重目标(含起始日)-预检.html    「复制日志」出现 2 次  copy-btn 元素 4 个
定体重目标(自动算截止)-预检.html  「复制日志」出现 2 次  copy-btn 元素 4 个
定体重目标-预检.html             「复制日志」出现 2 次  copy-btn 元素 4 个
定营养目标(自动算)-预检.html      「复制日志」出现 2 次  copy-btn 元素 4 个
定营养目标-预检.html             「复制日志」出现 2 次  copy-btn 元素 4 个
定饮水目标(自动算)-预检.html      「复制日志」出现 2 次  copy-btn 元素 4 个
定饮水目标-预检.html             「复制日志」出现 2 次  copy-btn 元素 4 个
改体重目标-预检.html             「复制日志」出现 2 次  copy-btn 元素 4 个
改营养目标-预检.html             「复制日志」出现 2 次  copy-btn 元素 4 个
改饮水目标-预检.html             「复制日志」出现 2 次  copy-btn 元素 4 个
```

逐颗按钮读数（`.scratch/t565/scan.mjs`，只读探针）：

| 产物 | copy-btn 明细（文档顺序） |
| --- | --- |
| `定营养目标-预检.html` | ① 复制指令 `ilife-help-copy-prompt`（有 `data-t`）② **复制日志 `ilife-copy-log`（无 `data-t`、`disabled`）** ③ 复制数据开合器（无 `data-action-id`）④ 复制日志 `ilife-copy-log`（有 `data-t`） |
| `定营养目标-回执.html`（同墙对照组） | ① 复制数据开合器（无 `data-action-id`）② 复制日志 `ilife-copy-log`（有 `data-t`）——1 次／2 个 |

两条日志通道的来路（读源码定死，不是猜）：

- 页底那条：`precheck.ts` 的 `copyArea({ prompt, data, log })` 走数据位 ＋ 日志位，出「复制数据开合器 ＋ 复制日志」（真按钮，六段载荷）。
- 上部那条：同一句的 prompt 位走 `promptCopyArea` → `copyActionHtml` → `renderActionBar({ copyData })`；公共层 `packages/base-render/src/controls.ts:1389-1396` 的 #336 兜底在「数据位在场、日志位缺席」时**自动补一颗禁用态「复制日志」**（文案／id 取 `ACTION_BAR_DEFAULTS.copyLogLabel`／`COPY_ACTION_IDS.actionBar.copyLog`）——于是同一页出现第二颗同名按钮。同一处兜底在本仓已有先例与修法记录（`docs/skills/skill-calorie/t351-v3-rerun-evidence.md:102-108`）。

## 三、改后读数

留哪一处：**上部指令区只留「复制指令」一颗，页底技术原件区只留「复制日志」一颗**（按页内层级定——上部的正事是把指令交给 AI，日志这一份技术原件归页底那一块；同墙其余 22 件的日志钮也都落在页底复制区）。数据位不再给：它一在场，公共层就按 #336 补出禁用态「复制日志」，同页又出两条同名通道。技术原件没丢：「复制日志」那颗的六段载荷原样在（命令原文含本次参数、库文件名与来源、时间戳与版本）。

11 条带空位写词逐条真跑读数（`.scratch/t565/sweep11.mjs`，临时种子库 ＋ 真 CLI，命令串与验收墙 `produce.mjs:156` 同）：

```
[定营养目标]             exit=0 bytes=73496 copy-btn=2 「复制日志」=1 日志载荷=338字符 指令载荷=255字符 完整文档=true
[定营养目标(自动算)]      exit=0 bytes=73500 copy-btn=2 「复制日志」=1 日志载荷=309字符 指令载荷=232字符 完整文档=true
[定体重目标]             exit=0 bytes=73032 copy-btn=2 「复制日志」=1 日志载荷=246字符 指令载荷=165字符 完整文档=true
[定体重目标(自动算截止)]  exit=0 bytes=73337 copy-btn=2 「复制日志」=1 日志载荷=290字符 指令载荷=211字符 完整文档=true
[定体重目标(含起始日)]    exit=0 bytes=73298 copy-btn=2 「复制日志」=1 日志载荷=358字符 指令载荷=201字符 完整文档=true
[定饮水目标]             exit=0 bytes=72886 copy-btn=2 「复制日志」=1 日志载荷=250字符 指令载荷=132字符 完整文档=true
[定饮水目标(自动算)]      exit=0 bytes=73132 copy-btn=2 「复制日志」=1 日志载荷=309字符 指令载荷=164字符 完整文档=true
[一键定全套目标]          exit=0 bytes=73704 copy-btn=2 「复制日志」=1 日志载荷=306字符 指令载荷=283字符 完整文档=true
[改营养目标]             exit=0 bytes=73529 copy-btn=2 「复制日志」=1 日志载荷=315字符 指令载荷=261字符 完整文档=true
[改体重目标]             exit=0 bytes=73140 copy-btn=2 「复制日志」=1 日志载荷=248字符 指令载荷=180字符 完整文档=true
[改饮水目标]             exit=0 bytes=72928 copy-btn=2 「复制日志」=1 日志载荷=250字符 指令载荷=137字符 完整文档=true
SWEEP 11 条：不合格 0 条
```

按钮明细（改后 `定营养目标-预检.html`）：① `ilife-help-copy-prompt`（有 `data-t`）② `ilife-copy-log`（有 `data-t`）——**2 颗、无禁用态**。

**内容一字不改的正证**（`.scratch/t565/diff-old-new.mjs`，墙上旧产物 ↔ 改后实跑产物）：

```
prompt 原文块：旧 255 字符／新 255 字符 → 逐字相同
剥掉复制载荷后，可见文本里旧有新无（4）：["复制数据","纯文本","JSON","CSV"]
剥掉复制载荷后，可见文本里新有旧无（0）：[]
```

即：改后页面上唯一消失的是那个三格式菜单（开合器 ＋ 三个格式项）；页头文案、页内三块、「库内现值（改前基准）」表、prompt 原文一段没少。另在用例 ⑤ 里逐条比：页上 `<pre>` 预览块与「复制指令」载荷，**逐字等于** `src/triggers/scene-06-goal.ts` 里该条词的 `prompt_template`（11 条全等）。

## 四、变异自证（两行机器读数）

变异点＝`promptRow()` 里 prompt 的槽位由**日志位**换回**数据位**（一处实参名）——公共层 #336 兜底随即补回本票删掉的那颗禁用态「复制日志」。脚本 `.scratch/t565/mutate.mjs`，在持锁窗口内跑完并逐字还原：

```
MUT-READING 改坏态 compile=0 test=1 红行数=4 ／ 还原态 compile=0 test=0
RED-LINE AssertionError [ERR_ASSERTION]: 「定营养目标」预检页「复制日志」应恰 1 次，实测 2 次（两条同名通道没并成一条）
MUT-SELFCHECK ok（改坏必红、还原变绿）
RESTORE OK（逐字还原，sha=31fced61c93c → 后一轮 d9396840c5de，两次都逐字回到原样）
```

## 五、加锁运行声明（`GATE-RUN` 行从 `.scratch/locks/gate-runs.log` 抄出，≥ 号后为摘要行）

```
GATE-RUN runId=5e7dc7c0-bd47-4cd8-8423-fce1147f324e cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"          exit=0  # 改前基线编译
GATE-RUN runId=0a036a71-d53a-44c7-9d9c-403aa40c88b9 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"          exit=0  # 实现后编译（tsbuildinfo 01:09:57 → 01:10:26 已更新）
GATE-RUN runId=ed453f5f-2387-451b-b78e-e20519224530 cmd="node --test packages/skill-calorie/test/565-precheck-copy.test.mjs"      exit=1  # 首跑：用例⑤ 我自己的针写错（把空位表当所有词共有），改针后复跑
GATE-RUN runId=b2ff929f-196b-4f63-9f30-f3159c60b36f cmd="node --test packages/skill-calorie/test/565-precheck-copy.test.mjs"      exit=0  # 5/5 绿
GATE-RUN runId=c27dab33-e378-45c5-9a2b-5792df65aea3 cmd="node .scratch/t565/mutate.mjs"                                         exit=0  # 变异窗口（内中：改坏 test=1／还原 test=0）
GATE-RUN runId=475b1136-16c1-4fe9-b19b-7c9299b8cc59 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"          exit=0  # 增量无改动＝空跑，tsbuildinfo 未变
GATE-RUN runId=6fad1ca1-0560-4787-a838-bc96acdedbd1 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"          exit=0  # 触到源件 mtime 后重编，tsbuildinfo 01:14:19 → 01:14:57 已更新
GATE-RUN runId=e84ed1d6-b594-4d47-b0e0-7ae6c8886a21 cmd="node --test packages/skill-calorie/test/565-precheck-copy.test.mjs"      exit=0  # 5/5 绿
GATE-RUN runId=4a069d70-5adc-4e43-864a-c34150e48990 cmd="node --test …/t561-goal-no-source.test.mjs …/goal-wizard-251.test.mjs"    exit=1  # 兄弟用例抓到**本票引入的回归**：不带 wake 进来时 prompt 为空串，`renderPreBlock` 拒绝空串（exit 4）；已按共用件口径补空串分支
GATE-RUN runId=da1e4a73-9899-45f8-a107-94c5d45682f4 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"          exit=0  # 补空串分支后编译
GATE-RUN runId=790f20ab-888b-4503-a2d2-755879320c81 cmd="node --test packages/skill-calorie/test/565-precheck-copy.test.mjs"      exit=0  # 5/5 绿
GATE-RUN runId=d433be14-f31d-49a5-bbce-c4fdaf963ef3 cmd="node --test …/t561-goal-no-source.test.mjs …/goal-wizard-251.test.mjs"    exit=0  # 8/8 绿（回归已平）
GATE-RUN runId=e64b7391-788d-40a9-9fd3-dd13be7377f1 cmd="node .scratch/t565/mutate.mjs"                                         exit=0  # 定稿源件上重跑变异：改坏 test=1／还原 test=0
GATE-RUN runId=b39b1290-03fa-4b38-9c2b-8d3523f07187 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"          exit=0  # 验收·编译（tsbuildinfo 01:17:49 → 01:18:12 已更新）
GATE-RUN runId=ff2b2ecb-e307-4827-82d5-85e8ea7ecf43 cmd="node --test packages/skill-calorie/test/565-precheck-copy.test.mjs"      exit=0  # 验收·测试 5/5 绿（ℹ tests 5／pass 5／fail 0）
GATE-RUN runId=2817be9e-2250-4c09-b632-adea245fe3e3 cmd="node packages/skill-calorie/scripts/check-warning-line.mjs"               exit=0  # RESULT: 87/87 PASS（本票只改 240 LF 的件，台账不用动）
```

## 六、结构必报五步

1. **影响清单**：见第一节（改 1 件源码、新建 1 件用例、新建 1 件证据，逐行给理由）。
2. **结构设计**：不新建文件、不搬件——两个复制区仍住 `precheck.ts`；新增的对外名字 0 个（`promptRow` 是件内私有函数），`precheck.ts` 对外仍只有 `GoalPrecheckView`／`buildGoalPrecheckDoc`。
3. **写代码**：prompt 按钮的 actionId 与文案取冻结表 `CALORIE_COPY_ACTION`（`../render/copy.js`，概念唯一出处，不自造名字）；日志仍走 `copyArea`／`copyLog`；冻结表与公共层一行未改。
4. **超线报警**：`precheck.ts` 211 → **240 LF**（< 350，未超线）；新建用例 203 LF（`test/*.mjs` 不在扫描面）；行数门 `RESULT: 87/87` PASS。未超线，不触发第四步。
5. **交付对账**：实际碰到的件与第一节逐行相同，偏差 0（`git status` 只有这三件）。

## 七、未做项与出口

1. **验收墙那 11 件产物仍是改前的模样**：`docs/skills/skill-calorie/scene06-验收墙/*-预检.html` 是 `produce.mjs` 生成的静态产物，重生成要写 `docs/skills/skill-calorie/scene06-验收墙/` 下的件（外加 `manifest.json`／索引），**不在本票写集**；本票只把出页源码改对，墙由持有该目录的票重跑 `node docs/skills/skill-calorie/scene06-验收墙/produce.mjs` 刷新。本票的读数因此取自**临时库里真跑的 11 件产物**（读数见第三节），不是墙上的旧件。
2. **本页不再出「复制数据」三格式菜单**：这是「copy-btn 恰 2 个」这条判据逼出来的——一页要同时留「复制指令」与「复制日志」，第三条通道（数据菜单）就装不下。若负责人要保留数据通道，得改判据或把这颗挪到另一页，另开票。
3. **不带 wake 进来的那一档（`--params '{"profile":"cut"}'`）只有 1 颗复制钮**（没有 prompt 就没有指令区，与共用件「空串不出」口径同）；本票的 11 条带空位写词都带 wake，不受影响。
