# t517 复核 · 独立复跑与自设探针（对抗复核席）

> 票：**#517**（父地图 #161）。复核对象＝实施席件 `docs/skills/skill-calorie/t517-W1-共享形状与样板-证据.md`（400 行）＋其产物、`t517-gate-runs.log`、`.scratch/t517/**` 的脚本与读数。
> 强制基准＝`docs/skills/skill-calorie/t516-场景10-视觉整改基准.md`（J1–J9／§二 回归底线／§5 断点字号／§8 打分锚点）。
> 审查纪律＝`docs/subagent-concurrency-protocol.md` §2.2／§2.3／§2.4／§5／§5.1（独立复跑、自设新探针、归属标注、量刑）。
> **本席的读法**：实施席说绿的每一条都自己跑；它给的每个哈希都自己算；实施席的结论一律不采信。
> 写集：本件 ＋ `.scratch/t517-review/**`；另按派单要求做过一次**源码级变异**（对象 `packages/skill-calorie/src/render/trendDocs.ts`：备份字节 → 变异 → 跑红 → **按备份逐字节写回** → 核 sha256 → 重编译 → 跑绿；不留变异态，读数见 §三）。
> 一切编译／测试／探针经加锁包装器：`node tooling/run-locked.mjs --ticket 517 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000 -- <命令>`；长输出落 `.scratch/t517-review/r-*.log`，只读摘要行与尾 5 行。

---

## 〇 · 判定与五维

| 项 | 读数 |
|---|---|
| **判定** | **FAIL** |
| 五维 | 契约一致 **22/30** ｜ 证据真实可复现 **17/25** ｜ 新旧对照 **17/20** ｜ 工程红线 **13/15** ｜ 文档同步 **6/10** ＝ **75/100** |
| 量刑（§6） | **无 S1-交付缺陷**；**S2 三条**（A 变异替换不成立／退让未在基准件具名记账／对账计数与导出件不符）；**S3 五条**。均分 75 < 85 ⇒ **FAIL** |
| 一句话 | 机器面（J1／J2／J3／J6–J9、靶向测试 21/21、重出产物逐字节相同）**我独立复跑全部复现**；失分全在**证据与记账**：一条变异读数归属错、退让没按票面在基准件具名记账、对账退出码计数与导出件不符 |

**复现的事实（本席自己跑的）**：四件产物 sha256／bytes 逐字节相同；`audit-separators` `RESULT: 2/2` `PASS` 节点级 0；`measure-responsive` 四档 `overflow=0` **且逐档 `clippedCount=0`**；版式探针 1440 档 `shell=1120`、两侧余量各 160、顶层区块全 1080、`outOfBounds=0`、KPI 每排 2+2／2+2／2+2／4、同排高差 0；导航 6 项 ↔ `<section id>` 6 个双向一一对应；靶向测试 `tests 21 ｜ pass 21 ｜ fail 0`；`gen-cli --check` `GEN-CHECK PASS`；用当刻 dist **独立重出样板 2/2 与盘上产物逐字节相同**。

---

## 一 · 产物哈希（自己算，四件比对）

| 件 | bytes | sha256 |
|---|---|---|
| `.scratch/t517/样板/21-查热量缺口.html` | 87080 | `b76ae0358756fe83f49adab9e3d8103d3c6143f2cf798f8f06b2725862e3c036` |
| `.scratch/t517/样板/22-看热量缺口.html` | 87080 | 同左 |
| `.scratch/t387/html/21-查热量缺口.html` | 87080 | 同左 |
| `.scratch/t387/html/22-看热量缺口.html` | 87080 | 同左 |

⇒ 与实施席件 §十二 的 `sha256=b76ae035…`／`bytes=87080` **逐字对上**，四件**逐字节相同** ✓。

**独立重出**（本席自写的 `regen.mjs`，照 `gen-样板.mjs` 的种子口径复刻、落点改本席目录，**不覆盖**实施席证据件）：`REVIEW-REGEN-RESULT: PASS（本席重出与盘上证据产物逐字节相同 2/2）` ✓——产物在当刻 HEAD 上**可复现**。
**可复现的前提（具名）**：`todayISO()` 取的是 **UTC 日**（`src/analysis/utils.ts:135-139`），样本库按机器日播种 ⇒ 本地 08:00（UTC 换日）之后重跑，窗口与字节都会变。本席重出与实施席同 UTC 日，故逐字节相同。

---

## 二 · J1／J2／J3／J6／J7／J8／J9 逐条重跑（本席读数）

| 判据 | 命令（逐字，均经加锁包装器） | 本席读数 | 与实施席件对照 |
|---|---|---|---|
| **J1** | `node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t517/样板` | `RESULT: 2/2`、`PASS`、exit 0；两页 `node·0 ；0 ≥3段0 ｜0 、0 ~0 标识符0 命中=0` | **一致** ✓ |
| **J2** | `node docs/skills/skill-calorie/t516-判据-版式.mjs --dir .scratch/t517/样板 --widths 390,512,820,1440` | 元素级 `toc=1 conclusion=1 caliber=3 chip=6 status-badge=1 list-rows-row=3 kpi-card=4 data-table=1` | **一致** ✓ |
| **J3** | `node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t517/样板 --widths 390,512,820,1440` | `OVERFLOW-ZERO pages=2 cells=8 failed=0`；**逐档**（我读 JSON，不只读控制台那个只打首档的 `clip=` 列）：390／512／820／1440 两页 `clippedCount` **全 0**、`scrollerCount` 全 0 | **一致** ✓（并补了控制台看不到的三档） |
| **J6** | 同 J2，读 `shell`／`bodyChildren` | 1440：`shell.x=160`、`shell.w=1120`、`shell.right=1280`、`vw=1440` ⇒ 两侧各 **160 对称**；`body=1080`；`ilife-*` 顶层区块**全 1080**；`outOfBounds` 四档 **0** | **一致** ✓ |
| **J7** | 同 J2，读 `kpiPerRow`／`kpiMaxSpread` | 390=`[2,2]`／512=`[2,2]`／820=`[2,2]`／1440=`[4]`；`kpiMaxSpread=0`（四档） | **一致** ✓ |
| **J8** | 同 J2 ＋ 本席自写 `countDom.mjs` | 导航 6 项 `[参数,概览,每日摄入与消耗,缺口明细,合计,数据与日志]` ↔ `<section id>` 6 个 `[sec-params,sec-overview,sec-chart,sec-detail,sec-totals,sec-data]`，双向一一对应 = **true** | **一致** ✓ |
| **J9** | 同 J8 | `toc=1`＋`caliber=3`（含「📊 数据来源：」来源脚注）＋结论条 1；可见文本并列分隔符 `['·','；','~','｜','、']` 命中 **0** | **一致** ✓ |
| **改前半场**（本席自己跑） | `measure-responsive --dir .scratch/t517/改前` | 390 档**每页 `clippedCount=2`**：`ilife-block-list-rows-main cw=34/sw=55/ox=hidden`、`cw=48/sw=57/ox=hidden`；四档 `overflow=0` | **与实施席「改前」读数逐值对上** ✓ |
| **改前半场**（J6／J7） | `t516-判据-版式.mjs --dir .scratch/t517/改前` | 1440：`shell.w=1000`、`shell.x=220`、`body=960`；820 `kpiPerRow=[4]`；390／512 `[2,2]`；`kpiMaxSpread=0`；**390 档 `outOfBounds=20`**（改后＝0） | 与基准件 §一「改前」读数对上 ✓，并多一条「改后把 390 档 20 处越界收到 0」 |

---

## 三 · 对抗点 A：变异自证的替换（只读对照）**不成立**

### 3.1 被审读数：红的**归属**是错的
实施席 §9.1 用 `.scratch/t517/neg-datalabel.mjs` 把授权 1 要的那条变异（「把产出器里的 `data-label` 去掉 → 该断言必红」）换成了**等价只读对照**，读数：

```
NEG-DATALABEL before=red? true :: data-label=0 行=7 格=42  :: ⑨ 表头与期望不符：…
NEG-DATALABEL after =green? true :: data-label=40 行=7 格=35
NEG-DATALABEL-RESULT: PASS
```

我读该脚本源码（`:30-39`）：`assertCellLabels` 里**先**判表头、**后**判每格 `data-label`。负向样例是 `1712317` **之前**的产物——它是**六列**，而期望 `LABELS` 是**五列** ⇒ 抛错走的是 `表头与期望不符` 那条**表头分支**，`data-label` 那条**根本没被执行到**。证据件自己抄下的红字就是「⑨ 表头与期望不符」，却在下一句写「——证明这条正向断言对『没有 `data-label` 的产物』有识别力」：**红归错了断言**。这条读数因此**证明不了**它声称的东西。

### 3.2 本席自设隔离探针（把混淆项抽掉）：`.scratch/t517-review/neg-labelled.mjs`
拿**当刻产物**（五列、表头与期望一致）只剥每个数据格的 `data-label`，其余逐字节不动：

```
NEG-A 原产物 … 表头=日期|摄入|消耗|缺口|状态 data-label 处数=37 → 绿
NEG-A 副本（只剥 data-label，35 格）表头=日期|摄入|消耗|缺口|状态 data-label 处数=2 → 红 :: LABEL ⑨ 每格的 data-label 必须等于该列列头，实得：||||
NEG-A 原产物 sha256 跑前=b76ae0358756fe83 跑后=b76ae0358756fe83 未变=true
NEG-A-VERDICT: PASS（红只能出自 data-label 那条断言）
```

⇒ **断言本身有识别力**（这一点由本席补上了）；但**被审的那条替换读数不成立**（红的归属错，且它测不到「产出器→断言」这一段耦合）。

### 3.3 本席自己做两次**源码级**变异（红／还原／绿 ＋ 前后 sha256）
脚本 `.scratch/t517-review/mut-source.mjs`（整段在一个持锁窗口内，进程任何退出路径都先还原）：

```
MUT-A-1-conclusion-bar-RED  build=0 tests=1 pass=19 fail=2 :: ✖ #517 ① 三条恒出：页内导航 ＋ 口径说明行 ＋ 来源脚注（J9）
                                  另：✖ #517 ③ 结论条只用页里已有的数，且不吃新增口径
MUT-A-1-conclusion-bar-RESTORE 逐字节写回：改前 sha256=32aad9361872f669 还原后=32aad9361872f669 相等=true
MUT-A-1-conclusion-bar-GREEN build=0 tests=0 pass=21 fail=0
MUT-A-2-h1-tilde-RED  build=0 tests=1 pass=17 fail=4 :: ✖ #386 第三组：8 报告各拿自己那页…（H1 正则那条）
                              另：✖ #385 窗口差异：7d／本月／自定义 三例…  ✖ #517 ⑤ 可见文本零并列分隔符
MUT-A-2-h1-tilde-RESTORE 逐字节写回：改前 sha256=32aad9361872f669 还原后=32aad9361872f669 相等=true
MUT-A-2-h1-tilde-GREEN build=0 tests=0 pass=21 fail=0
MUT-A-DIST 重编后 dist/render/trendDocs.js sha256 改前=9fd7b0b733cd34e1 收尾=9fd7b0b733cd34e1 相同=true
MUT-A-SRC  收尾 sha256=32aad9361872f669 与改前相同=true
MUT-A-RESULT: PASS
```

- 变异 ①（`renderConclusionBar(deficitConclusion(d)),` → 行内 `<p>`）⇒ J9 判据必红；②（H1 的「至」→ `~`）⇒ **授权 2／3 改写后的两条断言仍然抓人**（`analysis-accept-386` 的 H1 正则与 `analysis-deficit-385` 的窗口断言同时被打红）——即 §5.1 要求的「**收窄没有变成放宽**」这一维度，我自造样例证过。
- 还原法按派单：**备份字节 → 变异 → 跑红 → 按备份逐字节写回 → 核 sha256 相等 → 重编译（生成物一起复位）→ 跑绿**；收尾源码与 `dist` 的 sha256 都与改前相等。另外，还原后的源码 sha256_16 ＝ `32aad9361872f669`，与实施席 `MUT-LINE-GREEN` 自己登记的还原哈希**一致**（交叉印证它那三处源码级变异确实还原干净）。

### 3.4 判：**替换不成立**（S2）
协议 §5 的口径是「**默认源码级变异**，故意破坏 → 相关测试变红 → 还原变绿」；授权 4 把 `data-label` 那条的落点明确放到**编译产物** `packages/base-render/dist/blocks.js` 并给了三个附加条件（同一持锁窗口／前后 SHA256 相等／丢锁即校验还原）。接管窗把它换成「拿两份现成产物喂同源判定」：
1. 它不是变异——没有「改坏」也没有「还原」这一步，**不满足 §5 的自证形状**；
2. 它的红**归属错**（见 3.1），连「断言有识别力」这半个结论都没站住；
3. 「读数不稳」这个理由是**当刻的事实**（我复核当刻也看到别席在途件），但可选的正确处置不止这一条：① 换到无人并发编译的窗口内重做原过程；② 退一步用**本席 3.2 那种隔离探针**并**如实写清它证的是哪一半**。实施席选了最弱的一条并把它记成「等价」，**同时把「这不是原过程」写进证据件**——如实记值得肯定，但**如实记不等于过关**。

⇒ 授权 1 的「去掉产出器 `data-label` → 断言必红」在本票**没有以任何变异形式给出**；「产出器 → 断言」这段耦合在本票**未被证明**（本席的隔离探针只证到断言一侧）。**S2（关闭前必须补）**。

---

## 四 · 对抗点 B：J1 的零分隔符**不是假绿**（探针自己抓得住）

`.scratch/t517-review/probeB.mjs`（副本注入，原产物一个字节不动）：

```
B-INJECT 眉标：趋势分析 → 趋势分析 · 缺口
B-INJECT H1  ：热量缺口 2026-09-09 至 2026-09-15 → 热量缺口 2026-09-09 ~ 2026-09-15
B-RUN 对照 base   exit=0 RESULT: 2/2 PASS   FILE 21-查热量缺口.html node·0 … 命中=0 ；FILE 22 … node·0 … 命中=0
B-RUN 注入 ·      exit=1 RESULT: 0/1 FAIL   FILE 21-查热量缺口.html node·1 ；0 ≥3段0 ｜0 、0 ~0 标识符0  命中=1  (行级命中=1)
B-RUN 注入 ~      exit=1 RESULT: 0/1 FAIL   FILE 21-查热量缺口.html node·0 ；0 ≥3段0 ｜0 、0 ~1 标识符0  命中=1  (行级命中=1)
B-ORIGINAL 原产物 sha256 跑前=b76ae0358756fe83 跑后=b76ae0358756fe83 四件未变=true
B-VERDICT: PASS（注入必红且点名该文件；原产物未动）
```

⇒ 门对 `·`（R1）与 `~`（R6）各有识别力、**点名到件**，且跑完原产物 sha256 未变。**J1 的 `2/2 PASS` 不是假绿** ✓（本条未发现缺陷）。

---

## 五 · 对抗点 C：J3 的 `clip=0` —— 口径已核，**探针有盲区**（S3）

**口径（引源码行号原文）**，`packages/skill-calorie/scripts/measure-responsive.mjs`：

```
 :241      if (el.clientWidth > 0 && el.scrollWidth > el.clientWidth + 1) {
 :242        if (cs.overflowX === 'hidden' || cs.overflowX === 'clip') clipped.push(rec);
 :243        else if (cs.overflowX === 'auto' || cs.overflowX === 'scroll') scrollers.push(rec);
 :244      }
```

即 `clippedCount` 只统计「① 本元素 `clientWidth > 0` ② `scrollWidth > clientWidth + 1` ③ 计算样式 `overflow-x ∈ {hidden, clip}`」三者同时成立的**横向**裁切；且 `clip` **不进退出码**（`:259-260` 的 `cellWhy` 只看 `overflow`／`img-not-fit`／视口守卫，`:318` 只对这三样计数——基准件 §5.3 G-3 已登记「`clip` 不是失败判据」）。
另记一处读数口径陷阱：控制台那列 `clip=` 打的是 **`row.widths[WIDTHS[0]].clippedCount`**（`:327`），四档里**只有第一档**；四档要去看 JSON（本席 §二 就是这么核的）。

**它看不见的情形（本席自造夹具，两档视口实测）**：

| 夹具 | 构造 | `overflow` | `clippedCount` | 探针能不能抓 |
|---|---|---|---|---|
| `C1-横向裁切-应被抓.html` | `width:200px;overflow:hidden` 里放 `width:400px` | 0 | **1**（`c1-box cw=200/sw=400/ox=hidden`） | **抓得住**（对照组，证明前提成立） |
| `C2-纵向裁切.html` | `width:200px;height:20px;overflow:hidden` 里放 `height:200px` | 0 | **0** | **抓不住**（只判 `scrollWidth/clientWidth`，纵向裁切完全在视野外） |
| `C4-零宽裁切盒.html` | `width:0;overflow:hidden` 里放长 `nowrap` 文本 | 0 | **0** | **抓不住**（`clientWidth > 0` 这条前置把整段被裁到零宽的元素排除掉） |

⇒ **本票的读数是成立的、也是够用的**：被修的缺陷就是 `.ilife-block-list-rows-main` 的**横向**裁切（本席自己跑改前件：390 档每页 2 处、`cw=34/sw=55` 与 `cw=48/sw=57`，与实施席逐值相同），这类缺陷 C1 证明探针抓得住，改后四档全 0。
⇒ **但它证不到的那一半要写明**：`clip=0` **不等于**「页上没有内容被裁掉」——纵向裁切与零宽盒子是**探针盲区**（C2／C4 实测 `clip=0` 却肉眼可见被裁）。**S3：读数边界如实入档**（探针本身要扩口径属公共层/工具票，不在本票写集）。

---

## 六 · 对抗点 D：共享形状落点**没有按票面收口**（S2）

### 6.1 票面原话（逐字）
- 落点判定（`:41`）：「共享形状**落 `packages/base-render/src/{style,blocks,charts}.ts`**……不是可选项。」
- 判定顺序 ③（`:43`）：「只有在「落公共层会让别的技能的门变红、且改造成本高于收益」时才退到卡路里包内共享件，**且必须在基准件里具名记账**：退让的是哪一条形状、为什么、代价是什么。」
- 判定依据（`:45`）：「**判「能不能落公共层」的依据**：跑 `packages/base-render/test/*` 与另外五个技能的靶向测试；变红的**具体是哪几条断言、属于哪件**要写进证据。**不许用「影响面大」当理由**——影响面大要先量出来。」
- 遗留出口（`:75`）：「……剩下的公共层改动**当场开一张具名票**，并把编号回写本票。」

### 6.2 实际退让了几条
| # | 要求落公共层的形状 | 本票实际落点 | 性质 |
|---|---|---|---|
| 1 | **页面壳宽度档**（宽屏余量 J6） | 包内**三族共用件** `src/render/pageChromeCss.ts`（调用处 `trendDocs.ts:553 content: pageChromeCss(1120) + parts.join('')`；该件既有两档：计划族 1080／复盘与写族 960） | **退让**（共享形状落包内件，不是公共层） |
| 2 | **字号阶梯（J4）／断点档数（J5）** | **未做**（0） | 未做＝另一种退让 |
| 3 | **数据表「吃满余量」（J6 第三条路）** | **未做**（表宽封顶 `max-width:680px` 在公共层 `blocks.ts`） | 未做 |
| 4 | **`状态` 列徽章做进单元格** | **未做**（要公共层 `renderDataTable` 的 `cellHtml` 能力） | 未做 |
| 5 | **页内导航胶囊触摸区 ≥44px** | **未做**（根因在公共层 `.ilife-block-toc a`） | 未做 |

### 6.3 记账核对（本席逐处 grep 与读件）
- **基准件里：零记账**。`docs/skills/skill-calorie/t516-场景10-视觉整改基准.md` 里与 #517 相关的改动只有 §一 J6／J7 两行的「改前／改后读数回填」（我核到改动落点＝提交 `465a8a5` 里那两行），**没有**「退让的是哪一条形状／为什么／代价是什么」三件套；`退让`／`记账` 两个词在基准件里**零命中**。
- **证据件里：有具名的「未做／须另开票」，但不是票面要的那种记账**——§五 1／3、§七、§十一 1／2／4／6 逐条写了原因（裁定 3 公共层只读、表宽封顶在 #512），但都落在**证据件**而不是票面点名的**基准件**，也没有「代价」一列。
- **判定依据缺失**：票面 `:45` 要求「跑 `packages/base-render/test/*` 与另外五个技能的靶向测试，把变红的具体断言与件号写进证据」——证据件里 `base-render/test` **零命中**，**没有任何公共层门或被影响技能的靶向测试读数**。本票确实没改公共层（我核过：`git diff --name-only 73f482a 465a8a5 -- packages/base-render/src packages/base-render/test` **空**；当刻 `git status --short packages/base-render/` **空**），但「不改」与「量过之后不能改」是两件事，票面要的是后者。
- **承接票未开具名票**：`gh issue list` 检索「字号阶梯／断点档数／公共层」未见本票为此新开的票；证据件写「须另开票」「具名新票」但**没有编号回写**（票面 `:75` 明写要回写）。

### 6.4 裁定 3（公共层只读）的原文出处核对
- **有出处的一半** ✓：票面评论《编排者具名授权清单》「**两条约束同时生效、且优先于上表**」段逐字写着「**`packages/base-render/**` 只读**（裁定 3）：不新增件、不改规则。要改先停手报编排者转票。」；《接管记录》的只读清单也写着「**只读、不许碰**：`packages/base-render/**`（裁定 3）」。
- **查无出处的一半** ✗：证据件 §6.2 授权 4 引文写「『**裁定 3**：本票不许新增任何 `packages/base-render/**` 的件**……`packages/base-render/**` 只读」＋「**J6 的解不许靠新增公共层件**，用既有件解决」」——后半句在本票正文与 3 条评论里**grep 三个串（`J6 的解`／`既有件解决`／`不许新增`）零命中**，无法核实。

### 6.5 判
**退让没记账 ⇒ S2**（按派单口径与票面 `:43` 的明文）：至少 1 条共享形状（页面壳宽度档）落到包内件、另有 4 条改成了未做，**票面点名的记账位（基准件）一条都没有**，判定依据（公共层与其它技能的测试读数）也没有。**S3**：① 引文半句无出处；② 承接票未开、编号未回写。

---

## 七 · 靶向测试与包内两道门

| 项 | 命令 | 本席读数 |
|---|---|---|
| 靶向测试三件 | `node --test packages/skill-calorie/test/analysis-deficit-385.test.mjs … analysis-accept-386.test.mjs … analysis-shape-520.test.mjs` | `ℹ tests 21 ｜ pass 21 ｜ fail 0`、exit 0 ✓（与实施席一致） |
| 包内门 2 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `GEN-CHECK PASS：键 128（写 46 ＋ 读 82）；能力 10 个；未申报清单 0 条`、exit 0 ✓ |
| 包内门 1 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | **当刻红**：`RESULT: 87/88` ＋ `RED 漏报（台账没有）：src/exercise/receipt.ts LF=427`、exit 1 ✗ |

**包内门 1 的归属＝范围外发现（S3，转票措辞）**：`packages/skill-calorie/src/exercise/receipt.ts` 是**别席在途未提交件**（`git status` 显示 ` M`；HEAD 版 322 LF、当刻工作区 **427 LF**、mtime **2026-09-16 03:23:35**），实施席的绿读数测于 **03:11:13**（`.scratch/t517/pkgwarn.log`：`RESULT: 87/87`＋`PASS`＋「超线 38 件」，我的当刻读数「超线 **39** 件」）⇒ 那条红**晚于**实施席读数，**不是本票写集的件**，本票台账行 `src/render/trendDocs.ts 1136` 与实况一致（该门其余 87 行全对）。⇒ 本席**不以它判本票红**，但「两道门当刻全绿」这条**在复核当刻不可复现**。
**复核收尾时的跟踪**（本席不代跑该门，只读现场）：该席已于 **03:33:18** 跑 `--sync` 把 `| src/exercise/receipt.ts | — | 427 |` 补进台账（`git diff packages/skill-calorie/AGENTS.md`＝**1 insertion**，尚未提交）⇒ 该红属**瞬时过程态**，已由归属席收口；本条保留为「复核当刻的现场读数」。

---

## 八 · GATE-RUN 声明与对账复算（§2.4）

**① 声明与导出件的一一对应：成立** ✓。实施席件里的 `GATE-RUN runId=…` 共 **13 条**，`docs/skills/skill-calorie/t517-gate-runs.log` 里 `ticket=517` 的 `RUN` 共 **13 条**：**13↔13、runId 全不同、零遗漏、零多余**（我逐条比对）。
**② 退出码计数：证据件的三处数字与导出件不符** ✗。导出件实测 **6 条 `exit=0`、7 条 `exit≠0`**（`d079f44f`/`87300dcb`/`71374957`/`337e127f`/`63c9feca`/`3fbb72ad`/`22b834e7` 为 1；其余 6 条为 0）。而证据件写：§十二 3「**10 条 exit=0**、3 条 exit=1」；§十二 6「**10 条 exit=0**、3 条 exit≠0（…另有 3 条对账运行 exit≠0 也在窗口内）」；`GATE-RELAX` 行「13 条 RUN 里有 **6 条 exit≠0**」。三处**互相矛盾**，且都不等于 6/7。
**③ 拒复算读数（本席跑的）**：
```
（a）复算实施席的原命令（`--log` 指它那份**受跟踪的导出件**，`--allow-nonzero`）：RESULT: matched=13/13 auditEntries=13 scoped=13 undeclared=0 ＋ gate-audit: PASS   ← 实施席 §十二 6 的读数复现 ✓
（b）同窗口去掉 --allow-nonzero（严格口径）：RESULT: matched=6/13 scoped=13 undeclared=7 ＋ gate-audit: FAIL        ← 正是「6 条 0／7 条非 0」
（c）按证据件 §十二「零放宽复算」给的配方（--since 19:11:13.753Z）：RESULT: matched=6/13 scoped=11 undeclared=5 ＋ gate-audit: FAIL
（d）只留「全 exit=0」的三条（--since 19:11:13.753Z --until 19:12:40.426Z）：RESULT: matched=3/13 scoped=3 undeclared=0 ＋ gate-audit: FAIL
```
- (c) ⇒ 证据件说的「把 `--since` 收窄到 `19:11:13.753Z` 即可零放宽复算」**不成立**：该窗口里仍有 5 条 exit≠0。
- (d) ⇒ 窗口侧**能做到** `scoped=3 undeclared=0`（零放宽），但 `gate-audit: FAIL` 的原因是**声明面**：13 条声明里那 10 条落在窗口外/非零，被记成「未命中」。⇒ 正确的零放宽做法是**声明面与窗口面同时收窄**（只声明那 3 条、窗口也只取那一段），这也是本票最省的一条修法。
**④ 放宽的定性（本席只出建议，§6.1④）**：`--allow-nonzero` 属工具件头「④ 看 `exit`」的放宽开关，必须带 `GATE-RELAX` 留痕——**留痕在位** ✓，按本仓先例（`t180-review-falsify.md:136`「放宽是具名放宽（GATE-RELAX 行在位，所以不是 S1『私自放宽』）」、`t120-review-blue.md:51`）**不判 S1-私自放宽**；但协议 §2.4 第 4 条要求「**必须由编排者在派单里具名授权**……证据里**引用该授权句**」，本票《具名授权清单》五条里没有这一条（第 5 条还明写「**只限「等锁」这一件事，不是放宽任何门禁口径**」），证据件 §十二 口径说明是以「§2.4 第 3 条②」自证，而 §6.1④ 明禁实施者／审查者自行援引 ⇒ **建议编排者按 §2.4 第 4 条／§6.1 具名定性**（若走 §6.1，§6.1① 的三条我都能出读数：① 我持锁独立复跑结论一致；② `waitedMs` 逐条在日志里可复核；③ 门禁可对账见上）。
**⑤ 对账计数错误判 S2**（§九 缺陷 3）。

---

## 九 · 缺陷清单（归属 ＋ 分级）

| # | 缺陷 | 归属 | 级 | 机器读数／依据 |
|---|---|---|---|---|
| 1 | `data-label` 负向对照的**替换不成立**：红归错断言（走的是表头分支），且不是 §5 的源码级变异；授权 1 要的「去掉产出器 `data-label` → 必红」在本票**没有以变异形式给出** | **本票引入／本票范围** | **S2** | §三 3.1／3.2：`NEG-DATALABEL before…⑨ 表头与期望不符` vs 本席隔离探针 `LABEL ⑨ 每格的 data-label 必须等于该列列头` |
| 2 | **退让未按票面在基准件具名记账**：≥1 条共享形状落包内件（页面壳 1120）、4 条改未做；基准件里 `退让`／`记账` 零命中；票面 `:45` 的公共层判定依据（`packages/base-render/test/*` 与另外五个技能靶向测试读数）**零命中**；承接票未开、编号未回写 | **本票引入／本票范围** | **S2** | §六：`Select-String` 读数＋`git diff … -- packages/base-render/` 空＋`gh issue list` 检索 |
| 3 | **对账退出码计数与导出件不符**（§十二 3／§十二 6／`GATE-RELAX` 三处：10/3、10/3＋3、6≠0；实测 **6 条 0／7 条非 0**），且给复核席的「零放宽复算」配方**不成立** | **本票引入／本票范围** | **S2** | §八 (b)(c)(d) 四条 `check-gate-audit` 读数 |
| 4 | `--allow-nonzero` 放宽**没有引用编排者具名授权句**（票面第 5 条明写不覆盖门禁口径） | 本票范围 | **S3**＋请编排者按 §2.4 第 4 条／§6.1 定性 | §八 ④ |
| 5 | 证据件 §6.2 授权 4 引文里的「J6 的解不许靠新增公共层件，用既有件解决」在票面**查无出处**（只读那半句有出处） | 本票范围 | **S3** | §六 6.4 |
| 6 | **包内门 1（告警线）当刻红**：`src/exercise/receipt.ts` LF=427 未挂号（HEAD 322 LF、worktree mtime 03:23:35，晚于本票 03:11 的绿读数） | **范围外发现**（别席在途件） | **S3**＋转票 | §七 |
| 7 | `clip` 探针**盲区**（纵向裁切／零宽裁切盒抓不住）——本票 J3 读数只证到「横向裁切 0」 | **范围外发现**（工具口径；基准件 §5.3 G-3 已登记） | **S3** | §五 C2／C4 夹具读数 |
| 8 | 本票改动被卷进**他人票号的混合提交** `465a8a5`（同一提交里还有 #542 的 `weight/*` 七件与三个 weight 测试），#517 的落点无法按 commit 单独回溯 | **范围外发现**（共享工作区的提交纪律） | **S3** | `git diff --name-status 73f482a 465a8a5 -- packages/skill-calorie/{src,test}` |

**没有发现的**（逐条查过，如实记）：证据造假（四件 sha256／bytes、J1–J9 读数、靶向测试、重出产物我全部复现一致）；`packages/base-render/**` 被越权改动（源码 diff 空、工作区干净）；`buildDeficitDoc` 之外的 `build*Doc` 被碰（`465a8a5` 对 `trendDocs.ts` 的 4 个 hunk：imports ＋ `DEFICIT_*` 小件 ＋ `buildDeficitDoc` 两段，其余 9 个 `build*Doc` 未动）；产物被本席探针改坏（原产物跑前跑后 sha256 相等）；J1 假绿；超线报告与台账不同步（`trendDocs.ts` 1136 与台账一致，第四步原话＋超因＋拆法齐）。

---

## 十 · 未做项与下一手缺什么（本席）

1. **未做**：`packages/base-render/test/*` 与另外五个技能的靶向测试我**没有跑**——本票没改公共层（已量证），跑它们只为票面 `:45` 的判定依据；本条已作 S2 记在缺陷 2，建议由承接票补。
2. **未做**：没有做 `dist/blocks.js` 上「原过程」的那条变异（授权 4 的三个附加条件要求整段在一个没人并发编译的窗口内；本席复核当刻仍有别席在途件在动 `src/exercise/**`，且该对象在裁定 3 的只读路径上）——**下一手若必须这条读数**：请在编排者确认「窗口内无人并发编译 `packages/base-render`」后重做，并按授权 4 记前后两个 SHA256。
3. **未做**：视觉面（1440／390 截图与五维视觉打分）归**视觉审查席**，本席不代做；基准件 §8.2 要求两席独立，本席只给机器面。
4. **下一手（编排者）要的三件**：① 缺陷 2 的**基准件具名记账**（退让条目＋为什么＋代价）由实施席补、或编排者具名改期；② 缺陷 4 的放宽定性（§2.4 第 4 条／§6.1 具名记录，本席自己那条 `GATE-RELAX` 同样落在这一条上）；③ 缺陷 6 已由 `src/exercise/receipt.ts` 的归属席于 03:33 跑 `--sync` 收口（见 §七），编排者只需在收口时确认那行结论列的超因／拆法补写。
5. **给复核复算的最小配方**（零放宽）：
   - 对**实施席件**：只声明 `d5e1e1c4`／`79b9891b`／`40d7116d` 三条（全 exit=0），窗口 `--since 2026-09-15T19:11:13.753Z --until 2026-09-15T19:12:40.426Z` ⇒ 我实测的一半读数是 `scoped=3 undeclared=0`（另一半取决于声明面收窄，本席不代改他人件）。
   - 对**本件**：见 §十一 的窗口与七条声明（本席窗口里 **3 条 exit=0**、4 条过程红轮逐条在册），复算读数 `matched=7/7 scoped=7 undeclared=0`＋`gate-audit: PASS`。

---

## 十一 · 本席的门禁运行声明（§2.3／§2.4，边跑边补）

对账窗口：`--since 2026-09-15T19:27:30.609Z --until 2026-09-15T19:34:45.905Z`（＝本席首条运行的 START 到**本件自身那条对账运行**的 RUN 时刻），`--ticket 517`，日志＝活日志 `.scratch/locks-m158v2/gate-runs.log`。复算命令（逐字）：

```
node tooling/run-locked.mjs --ticket 517 --lock-dir .scratch/locks-m158v2 --max-wait-ms 2700000 --poll-ms 15000 -- \
  node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t517-复核-证据.md \
  --log .scratch/locks-m158v2/gate-runs.log --ticket 517 \
  --since 2026-09-15T19:27:30.609Z --until 2026-09-15T19:34:45.905Z --allow-nonzero
```

**收尾复算读数**（本席跑，`runId=bef58965-77d6-43c7-8f6a-7221397c418f`，exit 0）：`RESULT: matched=7/7 auditEntries=61 scoped=7 undeclared=0` ＋ `gate-audit: PASS`。**自指限制**：那条复算运行自己也是 `ticket=517` 的 RUN，且时刻**晚于本窗口上界** ⇒ 它在窗口外、**不回填进声明面**（实施席 §十二 的同一限制）；本席给出的窗口上界＝本件那条被声明的对账运行（`23a39fe7`）的 RUN 时刻，故上表 7 条与窗口内 7 条**一一对应**。**本件在收尾复算之后的文字修订**（§八 (a) 一处括号、§十 第 4／5 条、§十一 本节三条说明）**不触及任何 `GATE-RUN`／`GATE-RELAX` 行，也不改窗口区间** ⇒ 上述复算结论对定稿文件仍成立（本席逐行核对过）。

```
GATE-RELAX flag=--allow-nonzero reason=本席窗口 7 条 RUN 里 4 条 exit≠0：072ba963 红在包内门 1（别席在途件 src/exercise/receipt.ts 越线，非本席件）；85b3e4c1 红在末段「严格口径复算」（本席刻意跑的对照，FAIL 就是读数）；28cb2c84 红在「按实施席配方复算」（同上）；642d30b0 红在「零放宽配方复算」（同上）。四条都是本席自写探针/复算命令的过程红，逐条在上文有红在哪；门禁证据只领 175da5fa／57db0484／23a39fe7 三条 exit=0。同一处境与实施席 §十二 的 GATE-RELAX 一样，需编排者按 §2.4 第 4 条具名定性。
```

```
GATE-RUN runId=072ba963-2bcc-4967-ac6c-99e4f8526cf4 cmd="node .scratch/t517-review/steps.mjs build sep measure probe tests pkgwarn gencheck regen negA probeB probeC analyzeC"
GATE-RUN runId=175da5fa-355d-4f89-a7ed-3a3a02749b0b cmd="node .scratch/t517-review/steps.mjs gencheck regen negA probeB probeC analyzeC"
GATE-RUN runId=57db0484-c31c-4180-9fa4-0d2812994b03 cmd="node .scratch/t517-review/steps.mjs mutA"
GATE-RUN runId=85b3e4c1-4e8b-4a0a-926d-111309f16ade cmd="pwsh -NoProfile -Command \"node .scratch/t517-review/steps.mjs preclipMine; node .scratch/t517-review/steps.mjs auditRelax; node .scratch/t517-review/steps.mjs auditStrict\""
GATE-RUN runId=28cb2c84-94a5-4813-b00e-d80e3802bd38 cmd="node .scratch/t517-review/steps.mjs auditStrict2"
GATE-RUN runId=642d30b0-0cee-4220-ab9f-a3c1a26338ed cmd="node .scratch/t517-review/steps.mjs probeBefore auditZeroRelax"
GATE-RUN runId=23a39fe7-d762-425b-bad4-8854c1f2be6b cmd="node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t517-复核-证据.md --log .scratch/locks-m158v2/gate-runs.log --ticket 517 --since 2026-09-15T19:27:30.609Z --until 2026-09-15T19:32:27.725Z --allow-nonzero"
```

- `072ba963` 里跑到 `tests` 为止是**绿**（`sep 2/2 PASS`／`probe 2/2`／`tests 21/21`），红在第 6 步 `pkgwarn`（别席件），故后续步骤未在本条内执行、由 `175da5fa` 补齐。
- `175da5fa`＝`gencheck`（PASS）＋`regen`（2/2 逐字节相同）＋`negA`（PASS）＋`probeB`（PASS）＋`probeC`＋`analyzeC`，exit 0。
- `57db0484`＝两次源码级变异（`MUT-A-RESULT: PASS`），exit 0。
- `85b3e4c1`＝改前半场 `measure-responsive`（绿）＋两轮对账复算（FAIL 是读数，exit 1）。
- 本件自身的两条对账运行：`23a39fe7`（按 §十一 窗口复算，`matched=7/7`，**已声明**）与 `bef58965`（收尾复算，`matched=7/7`、exit 0）——后者时刻晚于窗口上界，**在窗口之外、不回填进声明面**（自指限制与实施席 §十二 声明一致）；未用 `--export`（本席不代他人票导出对账源；如编排者要导出件，本席可另跑 `--export .scratch/t517-review/gate-runs.log`）。

**逐条路径**：本件 `docs/skills/skill-calorie/t517-复核-证据.md`；本席脚本与读数 `.scratch/t517-review/`（`steps.mjs`／`regen.mjs`／`neg-labelled.mjs`／`probeB.mjs`／`analyzeC.mjs`／`countDom.mjs`／`extractProbe.mjs`／`mut-source.mjs`／`fixtures/`／`probe-A/`／`probe-B/`／`regen-out/`／`r-*.log`／`r-*.json`）。
