# T351-v11 · 构建向导（order186）从「结果回显」改成能干活的工具 —— 证据

负责人 2026-09-14 第 3 条原话：`v4-186-构建向导.html` **功能完全缺失**——「看不到如何帮助用户构建出健身计划」。
旧版只把**校验结论**印出来（结论卡 ＋ 错/警折叠块），**看不到计划本身长什么样**，也就无从「构建」。

本批把它做成工具，并守住本仓契约。前四批（`cc7e7c1`／`18e15d1`／`e8f0bca`）做的是 201–206／176–185／
186–195＋207 的**六条口径**；本批是**功能**那一半。

---

## 1 做成什么样

页面结构（自上而下）：

1. **结论条**：可落地 / 有硬止 ＋ 错误 N 项 ＋ 警告 N 项 ＋ 已检查 N 个训练场次（一行读数，不再散成四张卡）。
2. **一句话读法**（口径行）：这份计划按 计划 → 周 → 日 → 时段 → 动作 五层摊开。
3. **计划级**：名字、说明、起日、等级、器械 ＋ 一颗「改计划本身」。
4. **时间线**：逐周 → 逐日 → 逐时段 → 逐动作；**每一层挂一颗「改这一层」**。
5. 空的地方**就地指出下一步该说什么**（例如「这一周还没排——想排就说『定一周计划』」）。
6. 硬止错误 / 警告折叠块（保留）。
7. 复制区：复制指令 ＋ 复制数据 ＋ 复制日志。

**勾选的三种反应**（都在一张静态页里，零刷新）：

- 勾选的胶囊**变蓝**（`:has(.ilw-mark:checked)`）；
- 该层**就地展开**「说哪句话能改它」（`.ilw-tick:has(...)~.ilw-how`），逐条排成胶囊
  （计划级＝改训练计划；周级＝定一周计划／复制训练计划；日级＝改某天训练／删某天训练／定休息日；
  时段级＝加训练动作；动作级＝改动作）；
- 右上角「**你已改的内容 N**」加一 —— 由 **CSS 计数器**现算
  （`.ilw-wiz{counter-reset:changed}` ＋ `.ilw-mark:checked{counter-increment:changed}`）。

## 2 零脚本怎么做到「就地改与计数」

本仓契约 `docs/base-paint-contract.md` 写死「禁内联脚本（AC-7 零注入面）」，故不能用脚本做交互。
本页用**原生控件 ＋ CSS** 做出三种反应（见 §1）。三条实现要点（都踩过）：

1. **计数器要排在最后**：CSS 计数器取的是**文档顺序上它之前**的累计值，把计数元素摆在页头会永远读到 0；
   故它排在全部勾选之后，再用 `position:absolute` 钉回区块右上角。
2. **`:has()` 而不是 `~`**：选钮放在 `<label>` **里面**（整块胶囊才是点击区），所以同级兄弟是 label、
   不是 input——`input:checked ~ .ilw-how` 够不着。改用 `.ilw-tick:has(.ilw-mark:checked) ~ .ilw-how`。
   （`:has()` 在 Chrome 105＋／Safari 15.4＋／Firefox 121＋ 可用；本页是给人双击打开看的页。）
3. **窄屏计数改浮动**：`position:absolute` 钉右上角在 390 宽下会挤坏页头，故 820 段改成
   `position:fixed` 的**底部浮动胶囊**，勾一颗抬头就看得见；页尾留 64px 不压住复制按钮。

**这是零脚本能做到的上限**（如实记账）：勾选与计数是真交互，展开的是那一层该用哪条写词；
但「把用户勾的选项拼成一条可执行命令」需要脚本，本页不做。要走那一步得先改契约（见 §5）。

## 3 结构纪律

- 新件 `src/render/planWizardDocs.ts`（页面装配）＋ `src/render/planWizardCss.ts`（页内样式，唯一产出者）。
  搬出来的理由：它要摊五层时间线 ＋ 零脚本交互，是本族最大的一页；`workoutPlanDocs.ts` 本已 296 行
  （告警线 350），塞进去必超线。
- 数据面 `planPlate.ts` 的 `PlanWizardView` 加一个 `tree`（计划→周→日→时段→动作 ＋ 页眉读数）——
  只报校验条数帮不了「怎么构建」，**结构必须上页**。纯映射，不触库、不校验。
- `workoutPlanDocs.ts` 删掉旧函数（296→约 250），出口由 `./html.ts` 直接 import 姊妹件。

## 4 读数

- 全量机检 **66/66**（产物 37 ＋ 判据 29）· PROBE PASS；告警线门 65/65 PASS。
- 机审（`t351-v8-style-audit.mjs`，全量 37 份）：缺 820 断点／缺触摸目标／缺触屏三件／分隔符懒政／重复句
  **全部 0 份**（新页也过）。
- **零脚本交互的实证**（不是推断）：造一份把前两颗勾上 的变体件截图，读数三件都对——
  勾选胶囊变蓝、展开块出现（内含「改训练计划」胶囊）、右上角「你已改的内容 **2**」。
  脚本：`.scratch/t351-ux/make-wiz-checked-demo.mjs`；截图：v11 桌面（勾选态）／v11 手机（浮动计数）。

## 5 遗留与要负责人定的一件事

**「点按钮就地改」还有半步没到位**：现在勾完能看见「该说哪句话」，但**不能由页面把勾选拼成一条命令**。
要走到那一步，这一页需要**一小段页面级脚本**（拼字符串、写进复制区），而本仓契约明写「禁内联脚本」。
两条路，请负责人裁：

1. **维持零脚本**（当前）：页面是「看得懂的计划结构 ＋ 每层的改法提示 ＋ 你标了几处」，命令仍由 AI 按那句话执行。
2. **给这一页开一个例外**：允许一段页面级脚本，做到「勾完直接出可复制的确认命令」。这要改契约（另开票），
   且与「零注入面」那条冲突，我不自行改。

另外两条遗留：① `planPlate.ts` 现在 300＋ 行（含新 `tree`），仍在告警线内，暂不拆；
② 本页的时间线只画输入计划（AI 带来的那份），不看库——这是「构建向导」的本分，看库是「看完整计划」那一族的事。

---

## 6 验收入口：全部 37 份样张总索引（同轮补）

**问题**：预览台首页一路是**按版本叠**的（v3→v11 各留一节），到本轮已经**六个「一、」**，
负责人**没有一处能看全 37 份**——「肉眼验收」缺入口。

**做法**：新增入仓脚本 `docs/skills/skill-calorie/t351-v11-publish-preview.mjs`
（`node <脚本> [产物目录]`）：把该批 **37 份按可读名**（`v11-<号>-<类型>-<唤醒词>.html`）发到预览台，
并生成 **`v11-全部样张.html`**——按族分组（看计划 10／构建向导 1／写前预览 9／写后回执 10／复盘与扫描 7），
每份一张卡，**末尾列清另外 6 条为什么没有产物**（外部 5 条按 Out of scope；撤销后的读验证应当阻断）。
首页顶部已加一行醒目入口指向它。

**读数**：索引页 37/37 链接可达；首页 60/60；合计 **97/97，死链 0**（新写了一件
`check-page-links.mjs` 校验**任意预览页**的链接——原来那件只看首页）。

顺带补跑一条本图一直没跑的**仓门**：`node packages/skill-calorie/scripts/gen-cli.mjs --check`
→ **PASS**（键 127：写 46＋读 81；十个能力声明 127 条，未搬迁 0 条）。本图只动渲染件，命令登记未受影响。

