# 页面波派单·逐票专属节（MAP #155）

> 每张票的派单 ＝ `page-wave-brief-appendix.md`（共同附页，含 12／13 节两条硬裁定）＋ 本文件里那一节。
> **派单顺序**：`#275 → #273 → #271`（作者裁定所致）；`#270／#272／#274／#277` 独立，可与 #275 同开。

---

## #270 · 写后回执页 · 15＋1 条

- **词**：记一餐／记一餐（含备注）／补记饮食／批量补记饮食／记喝水／复制昨日饮食／改饮食记录／改某日饮食／删饮食记录／删一餐／删某日饮食／批量删饮食／存食品／改食品／下架食品 ／ **＋批量导入食品（第 14 条，见票面「本票范围补一条」）**
- **老实物**：`crud_receipt.html`（26279 B）——操作回执／字段变更（`diff-card`）／今日累计（`sum-kpis`）／复制明细
- **装配件（唯一作者＝你）**：`packages/skill-calorie/src/diet/receipt.ts`（已在位，133 行）
- **专属约束**：① 改类（改饮食记录／改某日饮食／改食品）出「改前 → 改后」对照；删类出被删记录的快照（老实物 `diff-card` 口径）；导入类是新增不是改动，**别硬套 diff-card**。② 复制区接 `src/shared/copyArea.ts` 的「复制数据／复制日志」，**日志含命令原文并带本次 `--params`**，照抄可重跑。③ 15 条词的 **prompt 文本**照「先给我看…我确认后再写入」核对——老技能这一类**不是 wizard**，**不新造预检页**。④ 老实物把「今日累计」摆在回执页上（`sum-kpis`），今天新仓回执**没有这一块**。
- **声明路径**：`src/diet/receipt.ts`；`test/profile-doc-179.test.mjs`、`test/calorie-c43.test.mjs`、`test/delivery-83.test.mjs`（这三件 #269 刚改过一轮，你改内容后要跟着收窄断言——**这是你的路径**）；`docs/skills/skill-calorie/t270-*`；`.changeset/t270-*`；`.scratch/t270/**`。
- **判据**：15＋1 条逐条实跑 exit 0、产物为完整文档、**内容逐块对得上老实物**，附落盘绝对路径与字节数；变异自证；不新增红。
- **#269 席交接给你的两件**（编排者 2026-09-14 转发，均已核）：
  1. **老实物那四块标题（`✅ 操作回执`／`📋 字段变更`／`📊 今日累计`／`📋 复制明细`）当刻在产物里一个都不存在**——#269 实测过，它只立骨架、不摆内容，所以它**没有**按这四串断（这是对的，那正是你这一票的字段级对齐范围）。**你摆上这四块后，才由你来断它们逐字出现。**
  2. **D4 余两处禁词待顺手改**：`src/diet/receipt.ts:29` 与 `src/cli/write.ts:67` 的源码注释里还有「键」字族（用词纪律禁），#269 因不在其声明路径未碰。**你这一票要碰 `src/diet/receipt.ts`，顺手改成规范词**；`src/cli/write.ts` 若你要动（分派链）也一并改，**动之前先报编排者**（那是共用件入口）。
  3. **第 14 条 `calorie.product.import` 的产物形状由你定**：#276 席交付时它仍是**片段**（不在 #269 的 13 条里），#276 未决定其形状（它只管命令面）。按本票「本票范围补一条」办：**收进整页**，导入是新增不是改动，**别硬套 diff-card**。

---

## #275 · 营养／饮水／总览页 · 4 条　**（本波第一张，先派它）**

- **词**：`calorie.view.nutrition-ratio`（查营养配比）／`calorie.view.nutrition-detail`（看营养素深度、看营养素明细）／`calorie.view.diet` 的「看饮食总览」／`calorie.view.today-water`（看今日喝水、看今日饮水）
- **老实物**：`nutrition_ratio.html`（12604 B）／`nutrition_detail.html`（6147 B）／`diet_overview.html`（7682 B）／`today_water.html`（10247 B）
- **装配件（唯一作者＝你）**：`packages/skill-calorie/src/diet/nutritionPortDocs.ts`（已在位，308 行）
- **专属约束**：① **`看今日喝水` 今天接了总览命令**（`calorie.view.home`，走老式渲染器），专面命令 `calorie.view.today-water` 有整页装配却没人用——按 #276 的结论把它接对；核对两条词语义没有互调。② 你还要**额外交付两个具名区块函数**（不做集成，别人调）：给 `calorie.view.diet-review` 用的**营养区块**、给 `calorie.view.diet` 用的**看饮食总览区块**；函数名与签名写进回执，交接给 #273／#271。
- **声明路径**：`src/diet/nutritionPortDocs.ts`、`src/diet/nutritionPort.ts`、`src/diet/nutrition.ts`；你自己新建的 `test/*t275*`；`docs/skills/skill-calorie/t275-*`；`.changeset/t275-*`；`.scratch/t275/**`。
- **判据**：4 条逐条实跑 exit 0、完整文档、内容对得上老实物；两个区块函数有直测；变异自证；不新增红。

---

## #273 · 复盘／餐别分布页 · 10 条

- **词**：复盘 6 条（饮食复盘（本周／本月／最近 90 天／今年／自定义时间）、看饮食复盘）＋ 餐别 5 条（看早餐／看午餐／看晚餐／看加餐／看全部餐别分布，均「最近 7 天」）
- **老实物**：`diet_review.html`（9041 B）／`meal_distribution.html`（11004 B）
- **装配件（唯一作者＝你）**：`packages/skill-calorie/src/diet/reviewDocs.ts`（#393 从 `render/dietDocs.ts` 搬 `buildDietReviewDoc` 而来）
- **专属约束**：① `calorie.view.diet-review` 全 8 条词（含 #275 的「看营养结构」「看今日营养」）**都是你这张页**；营养区块由 #275 交具名函数给你，你集成。② **交付 `buildMealDistributionBlock()`**（照 `meal_distribution.html`）**供 #271 调用**——#271 是 `calorie.view.diet` 的唯一作者，餐别页的集成在它那边。③ 餐别 5 条的**餐别参数**是 #276 的账；#276 未到位时按「参数缺失即用法错」走，**不编数**。
- **声明路径**：`src/diet/reviewDocs.ts`、`src/diet/review.ts`；你自己新建的 `test/*t273*`；`docs/skills/skill-calorie/t273-*`；`.changeset/t273-*`；`.scratch/t273/**`。
- **判据**：复盘页 6 条实跑 exit 0、完整文档、对得上 `diet_review.html`；`buildMealDistributionBlock()` 有直测且对得上 `meal_distribution.html`；变异自证；不新增红。

---

## #271 · 条目列表页 · 11 条

- **词**：看今日饮食（`calorie.today`，含备注筛选）／看昨日饮食／看本周饮食／看上周饮食／看本月饮食／看上月饮食／看最近 7 天饮食／看最近 30 天饮食／看某段时间饮食（以上 8 条走 `calorie.view.diet`）／看有备注的饮食记录／看今日饮食记录
- **老实物**：`today_meals.html`（19918 B）／`today_diet.html`（18790 B）
- **装配件（唯一作者＝你，但分两处，见下）**：
  - `calorie.today` 那条线（看今日饮食／看有备注的饮食记录／看今日饮食记录）→ `packages/skill-calorie/src/diet/todayDocs.ts`（#393 从 `render/dietDocs.ts` 搬 `buildTodayDietDoc` 而来）。
  - **`calorie.view.diet` 那条线（8 个窗口词 ＋ 1 个总览词 ＋ 5 个餐别词）不在 `src/diet/`**：实测它的命令声明住 **`src/home/commands.ts:21`**（`run: viewDietOverview`），处理体住 **`src/home/today.ts`**，装配件是 **`render/dietDocs.ts` 的 `buildViewDietDoc`**——因为**场景 01 的「看今日饮食概览」也走这条命令**（`src/home/routes.ts:12`）。
  - ⇒ 你可以**原地改** `buildViewDietDoc` 的内容（它就是本图的页面），但：**不搬它**、**不动 `src/home/` 下的声明**；改完**必须顺带核一遍场景 01 的「看今日饮食概览」仍能跑通**（那条词不归本图，但你不能把它弄坏）。要动 `src/home/` 下的任何文件 → 先报编排者。
  - 带**餐别参数**时出的餐别页，由你调 #273 交付的 `buildMealDistributionBlock()`；「看饮食总览」区块调 #275 交付的具名函数。
- **专属约束**：① **备注列与筛选**——取数已含 `note`（`src/fetch/diet.ts:289`），页面要长出备注列；筛参由 #276 给。② **窗口要合词面**：`看本周饮食` 今天路由层只有 1 天（应为 7 天），交付时按 #276 的结论核对（登记在 #250 的账上）。③ `buildViewDietDoc` 因 `src/home/today.ts` 在用**留在 `render/dietDocs.ts` 不搬**——你要改它内容可以**原地改**，但动之前先报编排者。
- **声明路径**：`src/diet/todayDocs.ts`、`src/diet/today.ts`；`src/render/dietDocs.ts`（**只许原地改 `buildViewDietDoc` 的内容，不许搬它**）；你自己新建的 `test/*t271*`；`docs/skills/skill-calorie/t271-*`；`.changeset/t271-*`；`.scratch/t271/**`。
- **判据**：11 条逐条实跑 exit 0、完整文档、内容对得上老实物；备注列有结构化断言；变异自证；不新增红。

---

## #272 · 横向排行榜页 · 21 条

- **词**：5 类榜（高热量／低热量／频繁吃／高碳水／高蛋白）× 4 窗口（默认／最近 30 天／本月／自定义）＋ 看全部排行榜；另 `查高热量排行` 同走 `calorie.view.ranking`
- **老实物**：`food_ranking.html`（14349 B）
- **装配件（唯一作者＝你）**：`packages/skill-calorie/src/diet/rankingDocs.ts`（#393 搬 `buildRankingDoc`＋`buildAllRankingsDoc` 而来）
- **专属约束**：① 榜单**表列与排序口径照老实物**；② 每类榜的**窗口参数**去命令面核对（今天 21 条是否都拿到了对的窗口）——不对就记遗留出口报编排者，别自己改路由；③ 「看全部排行榜」要出 5 类榜的**全景**（老实物一张表一族的做法）。
- **声明路径**：`src/diet/rankingDocs.ts`、`src/diet/ranking.ts`、`src/diet/rankingPlate.ts`；你自己新建的 `test/*t272*`；`docs/skills/skill-calorie/t272-*`；`.changeset/t272-*`；`.scratch/t272/**`。
- **判据**：21 条逐条实跑 exit 0、完整文档、内容对得上老实物；窗口逐条核对表；变异自证；不新增红。

---

## #274 · 食品库类页 · 4 条

- **词**：查食品／查食品（按分类）（走 `calorie.view.search`／`calorie.view.library`）／看食品库（去重）（`calorie.view.dedupe`）／看食品来源统计（`calorie.view.source-stats`）
- **老实物**：`food_search.html`（7913 B）／`dedupe_report.html`（6458 B）／`source_stats.html`（5539 B）
- **装配件（唯一作者＝你）**：`packages/skill-calorie/src/diet/libraryDocs.ts`（#393 搬 `buildSearchDoc`＋`buildLibraryDoc`＋`buildDedupeDoc` 而来）
- **专属约束**：① **`看食品来源统计` 今天接错命令**（接的是分类食品列表 `calorie.view.library`，应为 `calorie.view.source-stats`；专面命令被近义词 `看食品来源分布` 占着）——按 #276 的结论接对，并核对两条词语义没有互调。② 来源统计那一页今天住在 `render/trendMiscPortDocs.ts`（**与分析的几页混住**）：**先报编排者定归属再动手**，不要自行改那个共用杂项件；本票先做能做的三页。
- **声明路径**：`src/diet/libraryDocs.ts`、`src/diet/library.ts`、`src/diet/libraryPlate.ts`、`src/diet/products.ts`；你自己新建的 `test/*t274*`；`docs/skills/skill-calorie/t274-*`；`.changeset/t274-*`；`.scratch/t274/**`。
- **判据**：4 条逐条实跑 exit 0、完整文档、内容对得上老实物；变异自证；不新增红。

---

## #277 · 预检确认页 · 4 条

- **词**：批量导入食品／校验批量导入／拍营养表记一餐／拍营养表补记一餐
- **老实物**：`batch_import_preview.html`（24324 B）／`nutrition_label_wizard.html`（22873 B）
- **装配件（唯一作者＝你）**：`packages/skill-calorie/src/diet/precheck.ts`（**新建**）
- **专属约束**：① `批量导入食品` 出**预览页**（导入条数／跳过条数／失败明细），确认后再调 #276 新增的写命令。② `校验批量导入` 同一张预览页按**校验形态**出（逐行结果与失败原因，**不写库**）。③ 拍营养表两条出「**识别结果确认页**」（照片 ＋ 识别出的营养 ＋ 补录日期），确认后调 `calorie.diet.add`；**识别不确定处要标出**（老实物有 `diff-tag`）。④ 「教 AI 第一步走对」：这 4 条词的 prompt 文本写明「先出确认页，我确认后再写入」，改 `src/triggers/scene-02-diet.ts` 与 `src/triggers/wake-assets.ts` **两份**，并更新指纹与 `entry_sha`。⑤ **⚠️ 现装配挤在 `packages/skill-calorie/src/render/wizardPort.ts`，实测 457 行、已超本包 350 行告警线**——按包规当场报一句「已超线，需要根据规则进行重构。」＋为什么超＋拆法；新件落 `src/diet/`。
- **声明路径**：`src/diet/precheck.ts`（新建）；`src/triggers/scene-02-diet.ts`、`src/triggers/wake-assets.ts`；你自己新建的 `test/*t277*`；`docs/skills/skill-calorie/t277-*`；`.changeset/t277-*`；`.scratch/t277/**`。
- **本票被 #269＋#276 双重阻塞**：开工前先确认两票已收口。
- **判据**：4 条逐条实跑 exit 0（两条写类要真写进库、用与现值相同的值）；预览页／校验页／确认页三张产物为完整文档且对得上老实物；prompt 文本与指纹一致；变异自证；不新增红。

---

## 附：搬迁后「说明文字」的顺手同步归属（编排者 2026-09-14 派定，**不另开票**）

#393 把七页装配件搬进 `src/diet/` 时，按派单限定「只许删掉／只许改 import 指路」，**没有顺手改说明文字**，于是留下了两处**只述位置、不影响行为**的过时说明：

1. `packages/skill-calorie/src/render/dietDocs.ts:3-7` 的文件头**仍列已搬走的六页**；
2. `packages/skill-calorie/src/diet/{today,review,ranking,library}.ts:4` 仍写「装配走 `render/dietDocs.ts`」。

**归属（就地顺手改，各自改自己那一行）**：

| 过时说明 | 归谁 |
|---|---|
| `diet/ranking.ts:4` | **#272**（它的声明路径含该件） |
| `diet/library.ts:4` | **#274** |
| `diet/review.ts:4` | **#273** |
| `diet/today.ts:4` | **#271** |
| `render/dietDocs.ts:3-7` 文件头 | **#271**（它的声明路径含该件，为 `buildViewDietDoc`） |

**口径**：只改**说明文字**（把它写成「装配住 `diet/<x>Docs.ts`，本件只留 `buildViewDietDoc`／`buildHealthDoc`」这类当刻事实），**不许顺手改行为**。
