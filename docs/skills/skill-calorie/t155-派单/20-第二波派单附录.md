# MAP #155 第二波派单附录（编排者 2026-09-15）

> 本附录与 `19-第一波派单附录.md` **同时生效**：19 号管红线、自证、回执形状、全票路径所有权总表、作业面事实与锁的口径；本文只记**第二波的交接面与已交付存量**。
> 逐票专属节见 `02-逐票专属节.md`。

## 一、已交付存量（**不要重做**；动手前先核它在盘上）

| 票 | 状态 | 落在盘上的东西 |
|---|---|---|
| **#273** 复盘／餐别 | **已提交 `68332d5`** | `src/diet/reviewDocs.ts`、`src/diet/review.ts`、`test/diet-review-t273.test.mjs`、`.changeset/t273-复盘餐别页.md`；27/27 探针全绿、6 条复盘词真出口 exit 0、`buildMealDistributionBlock()` 直测已过。**剩两项受锁所限未跑**：作用面基线对照、全量红名单（已按协议停手上报，不是漏做） |
| **#275** 营养／饮水／总览 | 已改完待提交 | `src/diet/nutritionPortDocs.ts`、`src/diet/nutrition.ts`、`src/diet/today.ts`（3 行＋1 行 import，见 §三）、`test/t275-*` |
| **#276** 命令面 | 已改完待提交 | `src/diet/routes.ts`（`查高热量排行` 补 `category:"high_calorie"`）、`test/diet-ranking-route-t276.test.mjs`（5 用例）、`docs/skills/skill-calorie/t276-接对四处诊断.md`、`t276-第三阶段证据.md`；已拿到 `gen:check`／`pnpm build`／`help:build` exit 0 与覆盖门 75/75 |
| **#272** 排行榜 | 已交付，复核在飞 | 见 `t272-*.md` |
| **#274** 食品库 | 已交付，复核待重派 | 见 `t274-*.md`＋`diet/sourceStatsDocs.ts` |

## 二、交接签名（2026-09-15 由编排者逐条回源核实，带行号）

**不要自己重写这些函数，只 import 调用。**

| 函数 | 签名（逐字） | 住在 | 交给 |
|---|---|---|---|
| 餐别分布区块 | `buildMealDistributionBlock(v: MealDistributionView, command?: string): string` | `src/diet/reviewDocs.ts:287` | **#271** |
| 营养配比区块 | `buildNutritionRatioBlock(v: NutritionRatioView): string` | `src/diet/nutritionPortDocs.ts:208` | **#273**（已集成） |
| 看饮食总览区块 | `buildDietOverviewBlock(v: DietOverviewView, command?: string): string` | `src/diet/nutritionPortDocs.ts:628` | **#271** |
| 总览取数 | `buildDietOverviewView(db, date)` | `src/diet/nutritionPort.ts` | #271 |
| 餐别分布取数 | `buildMealDistributionView(...)` | `src/diet/review.ts:117` | #273 内部 |

两条默认行为（#275 交出来的，别猜）：
- `buildDietOverviewBlock` **不给 `command` 则不出复制区**；
- 宿主页 `assembleDocPage` 的 **charts 传 `true`**（两个区块都如此）。

## 三、`src/diet/today.ts` 上有 #275 的一处改动（#271 必读）

`viewTodayWater` 的第三个实参 `commandLine('calorie.view.today-water', params)`＝复制日志第 4 段的**本次命令原文**（裁定 7；不传就是**死按钮**）。同一处改动在 `src/diet/nutrition.ts` 也有一份。

**#271 可以改这个文件，但必须保留这处改动。** 归因说明见 19 号附录「十·补」的「追加说明」——第一版"不许碰"清单漏列该路径，**归因编排者，不判 #275**。

## 四、告警线台账（2026-09-15 编排者实测，席位开工前以当刻 `--dry` 为准）

| 件 | 行数 | 状态 |
|---|---|---|
| `diet/nutritionPortDocs.ts` | **571** | **超线（350）**，归 #275 报（必报五步：已超线，需要根据规则进行重构。＋为什么超＋拆法） |
| `diet/libraryDocs.ts` | **380** | 超线，**已知欠账**，留给收口票 |
| `diet/nutritionPort.ts` | 346 | 贴着线 |
| `diet/reviewDocs.ts` | 332 | 线内 ✓ |
| `diet/rankingDocs.ts` | 301 | 线内 ✓ |
| 其余 | ≤210 | 线内 ✓ |

## 五、第二波的发送顺序（作者裁定：`#275 → #273 → #271`）

1. **#273 已发车并已交核心。**
2. **#271 是下一张**，但**受两件实际约束**：① `buildMealDistributionBlock()` 与 `buildDietOverviewBlock()` 的签名已核实在盘（§二），可以直接开工；② 它要改 `src/render/dietDocs.ts`（原地改 `buildViewDietDoc`，**不许搬**），而那件在 #272／#274 之后**没有别的写者**，可以开。
3. **#270 独立**，路径 `src/diet/receipt.ts` ＋ 三个测试件，与 #271／#273／#275 都不重叠，可与 #271 同时开。
4. **#277 仍被 #269＋#276 双重阻塞**，等两票收口。
5. **关于席位数量**：门锁当刻是全局瓶颈（见 `t155-锁争用实测.md`：等待／持锁 3.4:1，他图两张视觉复核占 71% 的持锁时间）。**并发度贴着锁的吞吐定**——在锁的争用缓解之前，本图同时在飞**不超过 5 席**。

## 六、第二波席位必须遵守的三条（第一波实测换来的）

1. **红只按「我这条路不新增红」判**；声明路径之外的一律记「**范围外发现**」，不修、不重跑。
2. **基线自己当刻量**，交活时比同一份名单；**不许引用别人给的基线**（树上同时有他图的代理群在写，13:58 实测 43 个受跟踪件处于已改状态）。
3. **抢锁一律 `--poll-ms 2000`**，并把持锁动作**拆小、能合并的合并**；等满协议上限仍抢不到 ⇒ **停手上报，不许空等、不许假装跑过**。**读数陷阱**：包装器成功而 pwsh 报 `[exit code: 1]` 是常态，判成功只看 `RUN … exit=0` 与 `LOCK-RELEASED … exit=0` 两行。
