# t253F · 跟随件：两处旧断言跟 #253 的新口径（回到整页）

票：ISSUE #253 跟随（#253 已交付，提交 `488cff56`：目标管理 5 条会改数据库的命令的回执页从片段换成整页）。
本票只动票面点名的范围，不借机改别的。

## 一、影响清单（一行一个文件 ＋ 一句话理由）

| 文件 | 动作 | 理由 |
| --- | --- | --- |
| `packages/skill-calorie/test/profile-doc-179.test.mjs` | 待定（见 §二定位结论） | 276–290 那条抽样正钉 `calorie.goal.set`／`calorie.goal.water` 仍是片段，#253 已有意切整页 |
| `packages/skill-calorie/test/delivery-83.test.mjs` | 待定（见 §二定位结论） | 红点在 324–329（`calorie.goal.set` 产物族 `receipt` 片段）；180 一带是帮助域查询命令的内联态，不属于本票 |
| `packages/skill-calorie/src/cli/write.ts` | 只改件头陈化计数那一句注释，代码零改动 | 当刻实况 46 条会改数据库的命令、六个装配口共认 40 条（3／14／4／10／4／5），旧注释 `其余 1 条（35 − 3 − 13 − 4 − 10 − 4）` 对不上 |
| `docs/skills/skill-calorie/t253-跟随件.md` | 新建（本文件） | 影响清单 ＋ 改前改后 ＋ 两条测试的实跑读数 ＋ 变异自证读数 |

## 二、定位结论（先跑后定，都有机器读数）

基线验收（编译后、改前跑出）：`tests 23 ／ pass 17 ／ fail 6`，退出码 1。6 条红点名：

| # | 红在哪条用例 | 归谁 |
| --- | --- | --- |
| 1 | `#179 其余会改数据库的命令仍是原回执片段（没被一刀切换页）` | 本票口径变更（正钉 `calorie.goal.set`／`calorie.goal.water` 仍是片段） |
| 2 | `#83 写键同样有 delivery（receipt 产物族）`（用例原名逐字照抄） | 本票口径变更（正钉 `calorie.goal.set` 产物族 `receipt` 片段） |
| 3 | `#337 场景 03 四条体重写命令已是完整文档` | 体重域（别的票在场，只记账） |
| 4 | `#365 身体细节四条写命令已是完整文档` | 身体域（别的票在场，只记账） |
| 5 | `#239 四张页接上「复制日志」` | 档案域预检确认页（别的票在场，只记账） |
| 6 | `#247 场景 07 五张页的复制数据是「三格式三选一」菜单` | 同上（别的票在场，只记账） |

其中第 3、4 条红在眉标文字上（产物已是整页），反向印证体重／身体两族确已整页。

链外 6 条探针（真 CLI ＋ 临时库，加锁跑出，退出码 0）：

```
exercise.add status=0 prefix=doc template=doc-shell
exercise.update status=0 prefix=doc template=doc-shell
exercise.remove status=0 prefix=doc template=doc-shell
photo.add status=0 prefix=doc template=doc-shell
photo.tag status=0 prefix=doc template=doc-shell
photo.remove status=0 prefix=doc template=doc-shell
```

端口计数（静态核对命令登记表 `src/cli/keys.ts` 写 46 条）：档案 3 ／ 饮食 14 ／ 体重 4 ／
训练计划 10 ／ 身体 4 ／ 目标 5，六个装配口共认 40 条；链外 6 条（`calorie.exercise.*` 三条、
`calorie.photo.*` 三条）各自由能力目录自己的实现出整页（运动域 `src/exercise/receipt.ts` 的
`buildExerciseReceiptDoc`、身材照域 `src/photo/receipt.ts` 的 `writeShell`，都是已提交的实现），
不经 `write.ts` 那条链。**当刻没有仍是片段的会改数据库的命令，46 条全是整页。**

`delivery-83.test.mjs:180` 一带的内联态抽样是 `calorie.help.lookup`（帮助域查询命令），
基线里它是绿的，不属于本票，**不动**。

**结论**：票面要点的修法（把抽样换成当刻仍是片段的命令）在当刻无解——没有这样的命令可取样。
要让那两条变绿，只能改断言意图本身（例如把「仍是片段」翻成「已是整页」或退役「没被一刀切」那条），
那超出本票写集（断言形状不动、测试意图不翻转），须编排者裁定。两处测试文件本票**一行不动**，见 §五。

## 三、改动（本票实际只改这一句注释）

`packages/skill-calorie/src/cli/write.ts` 件头第 16 行：

改前：`其余 1 条（35 − 3 − 13 − 4 − 10 − 4）一字不改，仍是原回执片段。`
改后：`其余 6 条（46 − 3 − 14 − 4 − 10 − 4 − 5：calorie.exercise.* 三条、calorie.photo.* 三条）各自由能力目录自己的实现出整页，不经这条链。`

代码零改动（`git diff` 只应有一行注释增删）。

另注意到同件头第 3 行 `35 写键一律 receipt 形` 同样陈化（当刻 46 条），但票面只许改第 16 行那句，
本票不动它，记 §五第 2 条。

## 四、实跑读数（GATE-RUN 逐条 ＋ 摘要行 ＋ 退出码）

```
GATE-RUN runId=7ac21096-788f-4692-85d8-dfd18c345030
        cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
        摘要行 RESULT: ticket=253F runId=7ac21096-… waitedMs=0 exit=0
        退出码 0（tsconfig.tsbuildinfo mtime 已更新到 2026-09-15T21:27:57+08:00）

GATE-RUN runId=a04b90f1-c9f3-4485-a330-81209c461ad9
        cmd=node --test packages/skill-calorie/test/profile-doc-179.test.mjs packages/skill-calorie/test/delivery-83.test.mjs
        摘要行 ℹ tests 23 ／ ℹ pass 17 ／ ℹ fail 6（改前基线；红点见 §二）
        退出码 1（日志 .scratch/t253F/baseline.log，Unicode 落盘）

GATE-RUN runId=a994e0cd-1d18-41f5-8cd4-7315bfa0ecdd
        cmd=node .scratch/t253F/probe.mjs
        摘要行 链外 6 条全 doc／doc-shell（读数见 §二），exit=0
        退出码 0
```

GATE-RUN runId=44436ba0-9010-4a95-9fff-95de4e49d587
        cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
        摘要行 RESULT: ticket=253F runId=44436ba0-… waitedMs=10007 exit=0
        退出码 0（注释改后重编；tsconfig.tsbuildinfo mtime 已更新到 2026-09-15T21:36:00+08:00）

GATE-RUN runId=1e64f8b9-7f16-4fb6-8294-3448994850d8
        cmd=node --test packages/skill-calorie/test/profile-doc-179.test.mjs packages/skill-calorie/test/delivery-83.test.mjs
        摘要行 ℹ tests 23 ／ ℹ pass 17 ／ ℹ fail 6
        退出码 1（日志 .scratch/t253F/after.log；红点与 §二逐字相同，无新增红——注释改动零行为影响）

## 五、未做项与下一手缺什么

1. 两处抽样（`profile-doc-179.test.mjs:276-290`、`delivery-83.test.mjs:324-329`）本票一行未改：
   当刻 46 条会改数据库的命令全是整页，没有仍是片段的命令可取样（§二探针为证）。
   下一手缺编排者裁定（三选一）：(a) 退役「没被一刀切」那条测试（它的使命已完成）；
   (b) 把那两处翻成整页断言（断言意图要变，须票面明写）；(c) 另开一票把某族回执退回片段（不建议）。
   变异自证记在这里（不再另补）：反例「抽样改回 `calorie.goal.set` 必红」当刻恒成立，
   基线第 1、2 条红正是该反例的读数（`calorie.goal.set 的产物不再是原回执片段：<!doctype html>`／
   `未切整页的命令仍是片段族`）；注释改动零行为影响由改前改后同为 23／17／6 自证。
2. `src/cli/write.ts` 件头第 3 行 `35 写键一律 receipt 形` 同样陈化，本票按票面只改第 16 行那句，
   下一手改第 3 行时以命令登记表（`src/cli/keys.ts` 写 46 条）为唯一来源。
3. 别域 4 条红（§二第 3–6 条）各有别的票在场，本票只记账：体重域眉标、身体域眉标、档案域预检确认页两处。
