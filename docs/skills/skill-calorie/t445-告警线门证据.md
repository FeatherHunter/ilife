# #445 告警线门证据（`REQUIRED` 硬清单 → 扫描面＋台账逐件对账）

**判定：PASS**（真门禁 `RESULT: 48/48`、`exit 0`；验收 9 条读数零 MISS；测试 `pass 9 / fail 0`；
对账两窗口 `matched` 全中、`undeclared=0`；未 push）

日期：2026-09-14（本席＝接管席；本轮按编排者回改意见补做两处结构修正，见 §2）。
票面：`gh issue view 445 --repo FeatherHunter/ilife`（OPEN，标签 `wayfinder:task`）。

## 1 现场量法（接管席复验，不采信上一席说法）

- **改前件**（无主 diff）：`packages/skill-calorie/scripts/check-warning-line.mjs`
  旧 blob `66175d75c82af0fbe47fe1989ee26d10400b558f`（80 行，脚本只查 `REQUIRED` 两行「存在」）；
  接管席接手时的工作区 blob `669227e0ff4ad0c16c0dbef5c802833c435989d5`（219 行）。
  **判定：那 219 行确实实现了 ④⑤⑥⑦**（跑给它看才认，READING A–F），故第一轮**原样采纳**；
  本轮按回改意见重写为 346 行（新增 `--sync` 与生成物判据 ⑧，见 §2）。
- **缺件**：`AGENTS.md` 台账段（原三列表、无标记 → 脚本读不到解析源）、测试件、证据件、changeset
  —— 均不存在，本席新建／改对。
- 本包源码目录（`packages/skill-calorie/src` 之下）一概未碰（#440 在飞）；真库零写入；
  `src/triggers/routes.generated.ts` 未改也未提交。

## 2 本轮两处结构回改（编排者意见，只动本票声明路径）

**① 修法必须入仓（不能让红门变成死门）**：同步能力原只在被忽略的 `.scratch/t445/` 草稿里，
别的席撞上红门没有可用修法。现做进 `scripts/check-warning-line.mjs`：

- `--sync`（落盘）与 `--sync --dry`（只演练：打 `SYNC-PLAN 改=／增=／删=` 与逐条 `SYNC-CHANGE／ADD／DROP`，**不写文件**）；
- 只改 `begin/end` 之间那块表；§2.5-1 全流程＝先在内存算新全文 → 跑完全部断言（含**块外前后缀逐字节相同**
  与**冻结挂号值未被改写**）→ 全过才落盘 → 落盘后回读并打 `SYNC-VERIFY ok`；任一断言红即 `exit ≠ 0` 且**不落盘**；
- 红条文案末段直接点这条仓内命令：`修法：node packages/skill-calorie/scripts/check-warning-line.mjs --sync`。

**② 生成物剔出扫描面与台账（判据 ⑧，名单以生成器自己的输出声明为准）**：`structure.md` 明写不管
「生成的资产」，而重跑 `pnpm gen` 会改生成物行数，逼无关的票来同步台账。剔除名单**不做手写常表**，
只读本包 `scripts/gen-*.mjs` 的输出声明：`const <名> = join(SRC_DIR, 'a', 'b.ts')`（名字里带
`OUT`／`TARGET(S)`／`DST`／`DEST`／`GEN…` 段）与 `targets` 数组里的 `path: join(SRC_DIR, …)`；
认不出的段（变量、模板串、拼接）一律放弃——**宁可漏剔，不可误剔**。当前剔出三件并逐条打 `GENERATED-SKIP`：

| 件 | LF | 凭谁的声明 | 为什么剔 |
|---|---|---|---|
| `src/cli/keys.ts` | 167 | `scripts/gen-cli.mjs`（`targets` 里 `path: join(SRC_DIR,'cli','keys.ts')`） | `pnpm gen` 重生成，无人手编辑 |
| `src/cli/registry.ts` | 50 | `scripts/gen-cli.mjs`（同数组 `path: join(SRC_DIR,'cli','registry.ts')`） | 同上 |
| `src/triggers/routes.generated.ts` | 529 | `scripts/gen-cli.mjs`（同数组 `path: join(…,'routes.generated.ts')`）＋ `scripts/gen-routes.mjs`（`const OUT = join(SRC_DIR, 'triggers', 'routes.generated.ts')`） | 件头「勿手改」，`pnpm gen` 重生成 |

两条**自证**（防生成器重构后判据静默失效）：本包有生成器却一条输出声明都抽不到即红
（`RED 生成物判据自证`）；扫描面里**带「勿手改」印记却没被认出**的件即红（`RED 生成物印记已认领：<件>`）。
**手写的 `src/triggers/routing.ts` 仍在扫描面内**：它 `import` 生成物，但本身是人写的逻辑与类型再导出件
（`src/triggers/routeSpec.ts` 同）——「谁生成」才算生成物，**「谁 import 生成物」不算**；人改了它照常挂号。
台账随之从 22 行降到 21 行（删 `routes.generated.ts` 那一行，其余 20 个超线件 ＋ `wizardPort` 保留）。

## 3 判据（跑哪条命令 → 看哪个读数 → 等于什么）

命令（真实门禁一律无参）：`node packages/skill-calorie/scripts/check-warning-line.mjs`

| 判据 | 读数（机器行） | 等于 |
|---|---|---|
| ①②③ 静态三条（#354 原样） | `AGENTS.md：…（台账 21 行；扫描面 275 件，超线 20 件，剔出生成物 3 件；挂号台账命中 2/2）` ＋ `RESULT: 48/48` | 48/48 且末行 `PASS: 告警线台账齐全且与实况一致` |
| ④ 台账解析源可读 | 同上「台账 21 行」；读不到即 `台账解析源缺标记` | ≥1 行、四列齐全 |
| ⑤ **陈化**逐行 | 断言 `台账=999 实况=360` → `RED 台账陈化：src/over.ts 台账=999 实况=360` | 台账「当场实测」＝当刻 LF（不等即 `exit 1`） |
| ⑥ **漏报**逐件 | 断言未挂号的 360 行件 → `RED 漏报（台账没有）：src/surprise.ts LF=360` | 扫描面内 LF>350 的件必须在台账里有一行 |
| ⑦ 冻结两行仍在 | `挂号台账命中 2/2`；删任意一行 → `RED 台账缺行：<件>` | 两行挂号值未被改写（457／729） |
| ⑧ 生成物剔除法自证 | `GENERATED-SKIP <件> LF=<n>（由 … 生成，不在扫描面内）` 三条 | 名单来自生成器自己的输出声明；抽不到即红 |
| ⑨ 回绿灯 | `SYNC-PLAN mode=dry 行=21 改=0 增=0 删=0` ＋ `SYNC-DRY ok` | 台账已与当刻实况一致，无需改 |

## 4 验收读数（全文 `.scratch/t445/logs/accept3.log`，一次电池跑出；运行条目＝`t445-accept2`，
声明在 `docs/skills/skill-calorie/t445-告警线门回改证据.md` 的窗口里）

```
READING 0-baseline-base64 HOLDS exit=0 RESULT: 5/5 盘上超线件=21（含未挂号的 src/fetch/body.ts LF=382）
READING A-fixture-green HOLDS exit=0 RESULT: 48/48 PASS=true
READING B-probe-red HOLDS exit=1 RESULT: 48/49 点名=YES :: RED 漏报（台账没有）：src/t445-probe-360.ts LF=360；修法：node packages/skill-calorie/scripts/check-warning-line.mjs --sync
READING C-shrunk-face-blind HOLDS exit=0 RESULT: 5/5 同一棵 src/scripts 树（含未挂号的 src/t445-probe-360.ts，LF=360）上缩面口径（HEAD 脚本＋HEAD 台账）零红条；扩面真门在同一棵树上 exit 1（见 READING B）
READING D-probe-removed-green HOLDS exit=0 RESULT: 48/48 探针件已删=true
READING E1-aging-red HOLDS exit=1 RESULT: 47/48 点名=RED 台账陈化：src/render/html.ts 台账=654 实况=647
READING E2-aging-restored-green HOLDS exit=0 RESULT: 48/48 还原字节一致=true
READING F1-real-aging-red HOLDS exit=1 RESULT: 47/48 点名=RED 台账陈化：src/render/html.ts 台账=654 实况=647
READING F2-real-restored-green HOLDS exit=0 RESULT: 48/48 还原字节一致=true
ACCEPT-DONE readings=9 miss=0
```

- **① 基线**：改前（HEAD 脚本＋HEAD 台账）在同一棵真树上 `exit 0`／`RESULT: 5/5`，而盘上超线件已有 **21 件**
  （含未挂号的 `src/fetch/body.ts` `LF=382`）→ 票面「对新增超线件无感」当场复现。
- **② 漏报**：夹具多一件 360 行的 `src/t445-probe-360.ts`（不入台账）→ `exit 1`、点名该件与行数；
  删掉 → 回 `PASS`。探针件只活在 `.scratch/t445/tmp/` 夹具里，用完即清（`CLEANUP … 删除后存在=false`）。
- **③ 陈化**：夹具把 `src/render/html.ts` 那行改成 `654`（实况 `647`）→ `exit 1`＋`RED 台账陈化`；
  逐字节还原 → `exit 0`；**真** `AGENTS.md` 上再做一遍（F1／F2），还原后 sha256 一致。
- **④ 负向对照**：同一棵 `src/scripts` 树、同一个未挂号的 360 行件，缩面口径（只剩 `REQUIRED` 存在性检查）
  `exit 0`／零红条——扩面真门在同一棵树上 `exit 1`。⇒ 这道门的识别力确实靠扫描面撑着。
- 同步器另有 4 条用例读数（测试 T6–T9）：生成物不挂号也不算漏报、判据自证必红、`--dry` 不改文件、
  `--sync` 回绿且台账块外一字不动、真包 `--sync --dry` 零改动。

## 5 台账（当刻现数，不抄任何预备值）

22 → 21 行（本轮删掉生成物 `routes.generated.ts` 那行）：**20 个超线件逐件一行**
（`#445` 当场扫描才发现，`#354`／`#398` 在册只有三件；「其余件均在 350 以内」那句整句结论当场就不成立）
＋ `src/render/wizardPort.ts`（挂号 457、实测已落回线内的 265，**挂号行保留**）。

- **挂号值**＝第一次挂号时的 LF，历史事实、**永不回改**；冻结值 `457`／`729` 来源＝需求原文
  `docs/skills/skill-calorie/t169-设计定稿.md` 票 2 票面。脚本只核对它没被改写，不拿它跟实况比。
- **当场实测**＝当刻盘上数出来的 LF（按换行符切分后行数减一）；**只有这一列**拿来对实况，不等即红。
- 快照口径：本件成文当刻（2026-09-14，回改轮提交前）超线 20 件，LF 降序＝
  `wake-assets.ts 4747`、`render/trendDocs.ts 816`、`gen-cli.mjs 754`、`weight/history.ts 670`、
  `render/html.ts 647`、`weight/review.ts 590`、`workout/write.ts 570`、`render/trendMiscPortDocs.ts 563`、
  `weight/compare.ts 547`、`render/exercisePort.ts 541`、`render/sportPortDocs.ts 533`、
  `migrate/migrate.ts 494`、`analysis/multiTrendPage.ts 485`、`photo/helpCenter.ts 485`、
  `render/trendMiscPort.ts 456`、`profile/setup.ts 428`、`diet/nutritionPortDocs.ts 423`、
  `fetch/body.ts 382`、`workout/planStore.ts 369`、`diet/nutritionPort.ts 367`。
  （`src/triggers/routes.generated.ts 529` 已按 §2-② 判为生成物、不在扫描面内。）
- **成文后他席在飞、台账漂移与再同步（活快照门的现场实证）**：`src/render/trendDocs.ts` 816→**818**、
  新增未跟踪件 `src/analysis/reportPlate.ts`（**411**，#384 在途），于是本门当场转红
  （`RED 台账陈化：src/render/trendDocs.ts 台账=816 实况=818` ＋ `RED 漏报（台账没有）：src/analysis/reportPlate.ts LF=411`）。
  照 §9 的同步口径跑一次 `--sync` 即回绿，再读数为 **台账 22 行；扫描面 277 件，超线 21 件，剔出生成物 3 件；
  挂号台账命中 2/2；`RESULT: 50/50`；`PASS`；`exit 0`**（运行标识 `t445-sync2`，属落点窗口条目、不在门禁声明窗口内）。
- **同步口径**：跑 `--sync`（先 `--dry` 看计划）即可；**改了扫描面内的件而不同步台账，本门必红**
  ——这是设计行为（`pnpm test` 里的 `packages/skill-calorie/test/t445-告警线门.test.mjs` T5／T9 也会红）。

## 6 负向对照与变异自证

- ④ 缩面（`T4`／READING C）：去掉扫描面 → 未挂号超线件拿不到红条。
- ⑤ 陈化（`T3`／READING E1、F1）：台账行数改错 → 红并指出「台账 ≠ 实况」。
- ⑥ 漏报（`T2`／READING B）：未挂号超线件 → 红并点名。
- ⑦ 删行／改写冻结值：逐行断言 `挂号值＝冻结值`，删行即 `RED 台账缺行`（#354 起的负向对照继续成立）。
- ⑧ 生成物判据（`T6`／`T7`）：T6 声明过的生成物不挂号也不算漏报；T7 把输出声明改成认不出的写法 →
  `RED 生成物判据自证` ＋ 生成物掉回扫描面被点名。
- ⑨ 同步器（`T8`）：`--dry` 报计划但文件逐字节不变；`--sync` 落盘后门回绿、台账块外一字不动、
  冻结挂号值未被改写。
- 全部变异在持锁窗口内做、释放锁前复原；真 `AGENTS.md` 的还原以 sha256 一致为准（READING F2）。

## 7 对账（GATE-RUN 声明）

窗口＝`--ticket 445 --since 2026-09-14T13:12:00Z --until 2026-09-14T13:23:30Z`（首轮交付）。
对账在落点窗口内直调（`--export` 生成受跟踪的对账源），机器读数＝`gate-audit: PASS`／
`RESULT: matched=5/5 … undeclared=0`。**回改轮的读数与对账（窗口 B）在
`docs/skills/skill-calorie/t445-告警线门回改证据.md` 里声明**——本件窗口只声明下面这 5 条，
§4 里 48/48 那一批读数的运行条目声明在那边（一件一窗口，声明与窗口一对一）。

GATE-RUN runId=t445-01-baseline cmd=node packages/skill-calorie/scripts/check-warning-line.mjs
GATE-RUN runId=t445-land1 cmd=pwsh -NoProfile -File .scratch/t445/win-land.ps1
GATE-RUN runId=t445-accept1 cmd=node .scratch/t445/t445-accept.mjs --log .scratch/t445/logs/accept.log
GATE-RUN runId=t445-test1 cmd=node --test packages/skill-calorie/test/t445-告警线门.test.mjs
GATE-RUN runId=t445-c2 cmd=pwsh -NoProfile -File .scratch/t445/win-c2.ps1

- `t445-01-baseline`＝**上一席**留下的改前基线条目（`exit 0`，语义＝HEAD 脚本在未同步台账上过 5/5）。
  本席不采信其结论，另行用钉死 blob 的夹具复跑同语义读数＝§4 的 READING 0（`HOLDS`），两次独立口径一致。
- **窗口外、未声明**的运行条目（本窗口不覆盖，故不计入门禁证据，逐条报备）：
  `t445-c3`（13:24:41Z，`exit 0`，提交证据件那一窗）、`t445-final`（13:24:53Z，`exit 0`，首轮收尾复跑）、
  `t445-w5`（13:31:42Z，`exit 20`）与 `t445-w5b`（13:32:16Z，`exit 20`）——后两条是回改轮的两次红掉重跑
  （夹具缺生成器输出声明、判据自证红了，§2.5-1 生效：**断言红即不落盘、不提交**），修好后由 `t445-w5c` 全绿落地
  （声明在 `docs/skills/skill-calorie/t445-告警线门回改证据.md` 的窗口 B）。
- 导出件（受版本控制的对账源）：`docs/skills/skill-calorie/t445-gate-runs.log`（本窗口）；
  回改轮窗口 B 的导出见 `docs/skills/skill-calorie/t445-gate-runs-b.log`。

## 8 影响清单与交付对账

- 改：`packages/skill-calorie/scripts/check-warning-line.mjs`（346 行，判据 ①–⑨ ＋ `--sync`）；
  `packages/skill-calorie/AGENTS.md`（台账四列口径／生成物判据／同步器修法写入包内规矩）。
- 新建：`packages/skill-calorie/test/t445-告警线门.test.mjs`（9 例）；
  `docs/skills/skill-calorie/t445-告警线门证据.md`（本件）；
  `docs/skills/skill-calorie/t445-gate-runs.log`、`docs/skills/skill-calorie/t445-gate-runs-b.log`；
  `.changeset/t445-warning-line-ledger.md`。
- 草稿（`.scratch/` 被忽略，不入仓）：`.scratch/t445/t445-accept.mjs`（验收电池）、
  `t445-write-agents.mjs`（首轮同步器，已被仓内 `--sync` 取代）、`win-*.ps1`、`logs/*`。
- 未碰：本包源码目录之下的任何件、其它票的件、`src/triggers/routes.generated.ts`；真库零写入；未 push。

## 9 未做项与下一手缺什么

1. **本门是「活快照」门**：`当场实测` 列对的是当刻 LF。任何人改扫描面内的件，本门（含 `T5`／`T9`）就会红
   ——这是 #445 要的行为，不是误报；**修法＝仓内一条命令**：`node packages/skill-calorie/scripts/check-warning-line.mjs --sync`。
   **生成物不再触发本门**（`pnpm gen` 重跑 `keys.ts`／`registry.ts`／`routes.generated.ts` 都不会逼无关的票来同步）。
   台账快照取的是 `#440` 落地后（`src/fetch/body.ts`＝382）的读数；若之后再动该件，跑上面那条命令即回绿。
2. **超线件的拆法不在本票**：20 件只挂号、不拆（票面「不许动」）；逐件拆法待各件归属票认领。
   扫描面内包内测试件仍不算（`structure.md` 管辖），`scripts/**` 与 `src` 之下的 `*.ts` 全算（生成物除外）。
3. **其它包的同类脚本不在本票**（票面「遗留出口」）；若要做全仓一道门，当场转票。
