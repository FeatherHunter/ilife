# #445 告警线门证据（`REQUIRED` 硬清单 → 扫描面＋台账逐件对账）

**判定：PASS**（真门禁 `RESULT: 49/49`、`exit 0`；验收 9 条读数零 MISS；测试 `pass 5 / fail 0`；未 push）

日期：2026-09-14（本席接管席）。票面：`gh issue view 445 --repo FeatherHunter/ilife`（OPEN，标签 `wayfinder:task`）。

## 1 现场量法（接管席复验，不采信上一席说法）

- **改前件**（无主 diff）：`packages/skill-calorie/scripts/check-warning-line.mjs`
  旧 blob `66175d75c82af0fbe47fe1989ee26d10400b558f`（80 行，脚本只查 `REQUIRED` 两行「存在」）；
  新 blob `669227e0ff4ad0c16c0dbef5c802833c435989d5`（219 行）。
  **判定：采纳（不改一字）。** 理由＝本席按票面四条验收逐条跑给机器看，④⑤⑥⑦ 都真的红／真的绿
  （见 §3 的 READING 行），不是只有注释；`check-warning-line.mjs` 本席全程未编辑，提交内容与上一席
  留在工作区的内容逐字节相同。
- **缺件（本席补齐）**：`AGENTS.md` 台账段（原三列表、无标记 → 脚本读不到解析源）、
  告警线门测试件（本包测试目录下的 `t445-` 新用例）、证据件、changeset —— 均不存在，本席新建／改对。
- 本包源码目录（`packages/skill-calorie/src` 之下）一概未碰（#440 正在 `src/body/bodyDocs.ts`／
  `src/fetch/body.ts`／`src/body/compare.ts` 一带在飞）；真库零写入；`src/triggers/routes.generated.ts`
  未改也未提交。

## 2 判据（跑哪条命令 → 看哪个读数 → 等于什么）

命令（真实门禁一律无参）：`node packages/skill-calorie/scripts/check-warning-line.mjs`

| 判据 | 读数（机器行） | 等于 |
|---|---|---|
| ①②③ 静态三条（#354 原样） | `AGENTS.md：…（台账 22 行；扫描面 276 件，超线 21 件；挂号台账命中 2/2）` ＋ `RESULT: 49/49` | 49/49 且末行 `PASS: 告警线台账齐全且与实况一致` |
| ④ 台账解析源可读 | 同上「台账 22 行」；读不到即 `台账解析源缺标记` | ≥1 行、四列齐全 |
| ⑤ **陈化**逐行 | 断言 `台账=999 实况=360` → `RED 台账陈化：src/over.ts 台账=999 实况=360` | 台账「当场实测」＝当刻 LF（不等即 `exit 1`） |
| ⑥ **漏报**逐件 | 断言未挂号的 360 行件 → `RED 漏报（台账没有）：src/surprise.ts LF=360` | 扫描面内 LF>350 的件必须在台账里有一行 |
| ⑦ 冻结两行仍在 | `挂号台账命中 2/2`；删任意一行 → `RED 台账缺行：<件>` | `src/render/wizardPort.ts｜457`、`scripts/gen-cli.mjs｜729` 两行挂号值未被改写 |

## 3 验收四条：机器读数（全文 `.scratch/t445/logs/accept.log`，本席一次电池跑出）

```
READING 0-baseline-base64 HOLDS exit=0 RESULT: 5/5 盘上超线件=21（含未挂号的 src/fetch/body.ts LF=382）
READING A-fixture-green HOLDS exit=0 RESULT: 49/49 PASS=true
READING B-probe-red HOLDS exit=1 RESULT: 49/50 点名=YES :: RED 漏报（台账没有）：src/t445-probe-360.ts LF=360
READING C-shrunk-face-blind HOLDS exit=0 RESULT: 5/5 同一份夹具里未挂号的探针件 LF=360 却零红条
READING D-probe-removed-green HOLDS exit=0 RESULT: 49/49 探针件已删=true
READING E1-aging-red HOLDS exit=1 RESULT: 48/49 点名=RED 台账陈化：src/render/html.ts 台账=654 实况=647
READING E2-aging-restored-green HOLDS exit=0 RESULT: 49/49 还原字节一致=true
READING F1-real-aging-red HOLDS exit=1 RESULT: 48/49 点名=RED 台账陈化：src/render/html.ts 台账=654 实况=647
READING F2-real-restored-green HOLDS exit=0 RESULT: 49/49 还原字节一致=true
ACCEPT-DONE readings=9 miss=0
```

- **① 基线**：改前（HEAD 脚本＋HEAD 台账）在同一棵真树上 `exit 0`／`RESULT: 5/5`，而盘上超线件
  已有 **21 件**（含未挂号的 `src/fetch/body.ts` `LF=382`）→ 票面「对新增超线件无感」当场复现。
- **② 漏报**：夹具多一件 360 行的 `src/t445-probe-360.ts`（**不入台账**）→ `exit 1`、点名该件与行数；
  删掉 → 回 `PASS`（`exit 0`）。探针件只活在 `.scratch/t445/tmp/` 夹具里，用完即清
  （`CLEANUP … 删除后存在=false`），仓库 `git status` 里没有任何探针件。
- **③ 陈化**：夹具上把 `src/render/html.ts` 那行改成 `654`（实况 `647`）→ `exit 1`＋`RED 台账陈化`；
  逐字节还原 → `exit 0`；**真** `AGENTS.md` 上再做一遍（F1／F2），还原后 sha256 一致。
- **④ 负向对照**：同一份夹具、同一个未挂号的 360 行件，缩面口径（只剩 `REQUIRED` 存在性检查）
  `exit 0`／零红条 —— 扩面真门在同一棵树上 `exit 1`。⇒ 这道门的识别力确实靠扫描面撑着。

## 4 台账（本席当刻现数，不抄任何预备值）

`AGENTS.md` 台账段改成 `件／挂号值／当场实测／结论` 四列、加 `warning-line-ledger:begin/end` 标记，
22 行：**21 个超线件逐件一行**（`#445` 当场扫描才发现，`#354`／`#398` 在册的只有三件，
「其余件均在 350 以内」那句整句结论当场就不成立）＋ `src/render/wizardPort.ts`（挂号 457、
实测已落回线内的 265，**挂号行保留**）。

- **挂号值**＝第一次挂号时的 LF，历史事实、**永不回改**；冻结值 `457`／`729` 来源＝需求原文
  `docs/skills/skill-calorie/t169-设计定稿.md` 票 2 票面。脚本只核对它没被改写，不拿它跟实况比。
- **当场实测**＝当刻盘上数出来的 LF（按换行符切分后行数减一）；**只有这一列**拿来对实况，
  不等即红（这就是「台账陈化」）。
- 快照口径：本件成文当刻（2026-09-14，`#445` 提交前）超线 21 件，LF 降序＝
  `wake-assets.ts 4747`、`render/trendDocs.ts 816`、`gen-cli.mjs 754`、`weight/history.ts 670`、
  `render/html.ts 647`、`weight/review.ts 590`、`workout/write.ts 570`、`render/trendMiscPortDocs.ts 563`、
  `weight/compare.ts 547`、`render/exercisePort.ts 541`、`render/sportPortDocs.ts 533`、
  `triggers/routes.generated.ts 529`（生成物，不触发第四步）、`migrate/migrate.ts 494`、
  `analysis/multiTrendPage.ts 485`、`photo/helpCenter.ts 485`、`render/trendMiscPort.ts 456`、
  `profile/setup.ts 428`、`diet/nutritionPortDocs.ts 423`、`fetch/body.ts 382`、`workout/planStore.ts 369`、
  `diet/nutritionPort.ts 367`。
- **同步口径（下一手照这个做）**：把台账「当场实测」列改成当刻 LF；超线件补行、落回线内的行保留并改实测值。
  **这是设计行为**：改了超线件／加了扫描面内的件而不同步台账，本门必红
  （`pnpm test` 里的 `packages/skill-calorie/test/t445-告警线门.test.mjs` T5 也会红）。

## 5 负向对照与变异自证

- ④ 缩面（`T4`／READING C）：去掉扫描面 → 未挂号超线件拿不到红条。
- ⑤ 陈化（`T3`／READING E1、F1）：台账行数改错 → 红并指出「台账 ≠ 实况」。
- ⑥ 漏报（`T2`／READING B）：未挂号超线件 → 红并点名。
- ⑦ 删行／改写冻结值：`⑦` 逐行断言 `挂号值＝冻结值`，删行即 `RED 台账缺行`（#354 起的负向对照继续成立）。
- 全部变异在持锁窗口内做、释放锁前复原；真 `AGENTS.md` 的还原以 sha256 一致为准（READING F2）。

## 6 对账（GATE-RUN 声明）

声明窗口＝`--ticket 445 --since 2026-09-14T13:12:00Z --until 2026-09-14T13:23:30Z`（窗口内 RUN 条目
全部声明；`t445-audit1` 等**本次对账之后的**运行按 §2.4-3-② 的窗口界定落在窗口外，未计入）。

GATE-RUN runId=t445-01-baseline cmd=node packages/skill-calorie/scripts/check-warning-line.mjs
GATE-RUN runId=t445-land1 cmd=pwsh -NoProfile -File .scratch/t445/win-land.ps1
GATE-RUN runId=t445-accept1 cmd=node .scratch/t445/t445-accept.mjs --log .scratch/t445/logs/accept.log
GATE-RUN runId=t445-test1 cmd=node --test packages/skill-calorie/test/t445-告警线门.test.mjs
GATE-RUN runId=t445-c2 cmd=pwsh -NoProfile -File .scratch/t445/win-c2.ps1

- `t445-01-baseline`＝**上一席**留下的改前基线条目（`exit 0`，语义＝HEAD 脚本在未同步台账上过 5/5）。
  本席不采信其结论，另行用钉死 blob 的夹具复跑同语义读数＝§3 的 READING 0（`HOLDS`），两次独立口径一致。
- 导出件（受版本控制的对账源）：`docs/skills/skill-calorie/t445-gate-runs.log`
  （由 `node tooling/check-gate-audit.mjs … --export` 生成，复核用法写在该文件头部）。

## 7 影响清单与交付对账

- 改：`packages/skill-calorie/scripts/check-warning-line.mjs`（上一席留下的扩面实现，本席逐条验后**原样采纳**）；
  `packages/skill-calorie/AGENTS.md`（台账段改四列口径＋标记＋两种口径来历）。
- 新建：`packages/skill-calorie/test/t445-告警线门.test.mjs`；
  `docs/skills/skill-calorie/t445-告警线门证据.md`（本件）；`docs/skills/skill-calorie/t445-gate-runs.log`；
  `.changeset/t445-warning-line-ledger.md`。
- 草稿（`.scratch/` 被忽略，不入仓）：`.scratch/t445/t445-write-agents.mjs`（台账同步器，
  先在内存算→跑完全部断言→`--write` 才落盘）、`.scratch/t445/t445-accept.mjs`（验收电池）、
  `win-land.ps1`／`win-c2.ps1`／`logs/*`。
- 未碰：本包源码目录之下的任何件、其它票的件、`src/triggers/routes.generated.ts`；真库零写入；未 push。

## 8 未做项与下一手缺什么

1. **本门是「活快照」门**：`当场实测` 列对的是当刻 LF。任何人改扫描面内的件、或跑 `pnpm gen`
   重新生成 `routes.generated.ts`，本门（含 `T5`）就会红 —— 这是 #445 要的行为，不是误报；
   照 §4 的同步口径改一列即可回绿（同步器在 `.scratch/t445/t445-write-agents.mjs`，`--write` 落盘）。
   台账快照取的是 `#440` 落地后（`src/fetch/body.ts`＝382）的读数；若 `#440` 之后又动该件，按上式同步。
2. **超线件的拆法不在本票**：21 件只挂号、不拆（票面「不许动」）；逐件拆法待各件归属票认领。
   扫描面内包内测试件（`*.test.mjs`）仍不算（`structure.md` 管辖），`scripts/**` 与 `src` 之下的 `*.ts`
   全算。
3. **其它包的同类脚本不在本票**（票面「遗留出口」）；若要做全仓一道门，当场转票。
