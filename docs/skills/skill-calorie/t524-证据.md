# t524 · 场景 09 视觉整改基准 · 证据件

> 票：[#524](https://github.com/FeatherHunter/ilife/issues/524)（Part of [#159](https://github.com/FeatherHunter/ilife/issues/159)）。
> 本件记**命令与读数**；判据口径见 [`t524-场景09-视觉整改基准.md`](./t524-场景09-视觉整改基准.md)，逐格读数见 [`t524-改前读数.md`](./t524-改前读数.md)。
> **本波为订正波**：按 [`t524-对抗审查.md`](./t524-对抗审查.md) §6 的必改项逐条订正，**只订正基准三件 ＋ 新增判分脚本，不改产品代码**。
> 加锁口径：构建／生成类命令一律 `node tooling/run-locked.mjs --ticket 524 -- <cmd>`；**只读探针**（`audit-separators.mjs`／`measure-responsive.mjs`／`t516-判据-版式`）照审查席位先例直接跑，并逐条给 `runId` 或 exit。

## §0 被检物与页群

| 件 | 事实 |
|---|---|
| 交付目录（**只读**） | `D:\ilife-验收\scene09\`（16 件样张 ＋ `00-场景09验收导航.html`） |
| 工作页群（读数用这个） | `.scratch/t524/pages16/`（**16 件，与交付目录 16 件逐字节同哈希**；逐件字节与 sha256 见读数件 §0） |
| 工作区内的截图源 | `.scratch/t351-preview/scene09/*.html`（`vision_html_screenshot` 只吃工作区内路径） |
| 合格样板（变异对照） | `.scratch/t524/probe/good-sample.html`（6975 字节，手工构造；两条判据都绿） |
| **订正波新增页群** | `.scratch/t524/out/readings-before/`（16 页基线读数）／`readings-09-01/`（反例）／`readings-draft/`（正例）／`readings-floor-draft/`（下限页） |
| 页群字节合计 | 45,328,374 字节（09-08 18.8 MB ＋ 09-14 23.0 MB 两页占 42 MB，减重归 #461） |
| 页群 mtime | 2026-09-15 08:14:48（＝**改前**；源码 13:01～13:28 已被改到，见基准件 §1.3 的「时刻差」） |

## §1 机器门读数（订正波复跑）

### §1.1 分隔符与内部标识符探针（**当刻判红**）

命令（订正波现场复跑）：

```
node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t524/pages16 --json .scratch/t524/out/sep-rerun-16.json
```

读数逐字（**与审查席位自跑逐值同**）：`files 16 / nodeHitsTotal 231`；摘要行 **`RESULT: 0/16`** ＋ `FAIL`，`exit=1`。
规则逐条：R1 `·` 154／R2 `；` 6／R3 ≥3 段 83／R4 `｜` 0／R5 `、` 2／R6 `~` 1／R7 内部标识符 121。
工具身份：`packages/skill-calorie/scripts/audit-separators.mjs`，**另一 session 的在途件**，本票只读跑、不改、不提交；
当刻 sha256 ＝ `A8FEA10F5E92C3810D7D0404A7EE6CF59D44A81FDBB11AFC728401C72F9481CE`（**235 行**；旧稿写 275 行，已订正）。

### §1.2 三档响应式（**回归门，当前全零**）

命令：`node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t524/pages16 --widths 390,768,1440 --json .scratch/t524/out/resp-before-16.json`

摘要行逐字：**`OVERFLOW-ZERO pages=16 cells=48 failed=0`**，`exit=0`（与审查席位自跑同）。

### §1.3 版式与形状读数（触摸目标／字号／触屏三件／断点集）

**读数脚本的可复现性（订正波写清「取自哪个 sha」）**：

| 跑法 | 命令 | 当刻状态 |
|---|---|---|
| 工作树版 | `node docs/skills/skill-calorie/t516-判据-版式.mjs --dir … --json …` | **坏**：`RESULT: -2/1 件读数完成`、字段全空、exit 1（本席位实测；审查件记的 `-32/16` 是同现象） |
| **HEAD 版（本波采用）** | `git show HEAD:docs/skills/skill-calorie/t516-判据-版式.mjs > .scratch/t524/t516-head.mjs` 再 `node .scratch/t524/t516-head.mjs --dir .scratch/t524/out/readings-09-01 --widths 390,768,1440 --json .scratch/t524/out/readings-09-01/fmt.json` | **可跑**，出与读数件 §1.3 逐值同的读数（exit 0） |

逐页读数（本波实测，`readings-before/fmt.json` 即原 `fmt-16.json` 的副本）：

| 样张 | 390 minFont | **390 touch<44** | **768 touch<44** | 1440 touch<44（只记观感） | 页壳 max |
|---|---:|---:|---:|---:|---|
| 09-01 | 11 | **0** | **0** | 49 | 960px |
| 09-02 | 11 | **0** | **0** | 7 | 960px |
| 09-03 | 11 | **0** | **0** | 7 | 960px |
| 09-04 | 11 | **0** | **0** | 5 | 960px |
| 09-05 | 11 | **0** | **0** | 7 | 960px |
| 09-06 | 12 | **0** | **0** | 7 | 960px |
| 09-07 | 11 | **0** | **0** | 7 | 960px |
| 09-08 | 12 | **0** | **0** | 7 | 960px |
| 09-09 | 11 | **0** | **0** | 5 | 960px |
| 09-10 | 11 | **0** | **0** | 5 | 960px |
| 09-11 | 11 | **0** | **0** | 5 | 960px |
| 09-12 | 11 | **0** | **0** | 5 | 960px |
| 09-13 | 11 | **0** | **0** | 5 | 960px |
| 09-14 | 11 | **0** | **0** | 7 | 960px |
| 09-15 | 12 | **0** | **0** | 8 | 960px |
| 09-16 | 12 | **0** | **0** | 15 | 960px |

三条当刻事实：① **触摸档（390／768）16 页逐页 0 处不足**（达标）；② **1440 档 5～49 处**不足，
逐条是 `button.ilife-copy-btn`（`78x40`／`476x40`）与 `button.ilife-copy-menu-item`（`186x40`）＝ **40px < 44px**，
**只记观感、不进 H6**（`pageUi.ts` 的 44px 修法本来只在 `@media (max-width: 820px)` 段里）；
③ **390 档字号下限 11px 的有 12 页**（06／08／15／16 四页是 12px），责任元素 `th.ilife-block-data-table-cell-left`／`-right`（表头）。

### §1.4 静态 DOM 事实（订正波新增，逐页实测；脚本 `.scratch/t524/correction-probe.mjs`）

| 量 | 逐页读数 | 合计 | 旧稿说法 | 判定 |
|---|---|---:|---|---|
| `overflow-x: auto`（含加空格写法） | 每页 **5** | 80 | 「16 页**没有任何** `overflow-x:auto`（静态命中 0）」 | **不实**：旧稿只数了 `overflow-x:auto` 无空格变体（该写法逐页 0）；其中 `.ilife-block-data-table { … overflow-x: auto; … }` **表格容器本身就是横滚容器** |
| `class="…ilife-block-toc`（**元素**） | 每页 **0** | 0 | 「改前产物里 HELP 页有 4 个」 | **不实**：那 4 个是**同名 CSS 规则**（`.ilife-block-toc` 选择器每页 4 条），不是元素；与基准件 §2.0「页内定位 0/16」互斥，已订正 |
| `td[data-label]` | 每页 **0** | 0 | 「卡片化规则已备」 | 成立但**未生效**：规则在 `pageUi.ts`，产出链未接（`ilife-page-ui` 逐页 0） |
| `aspect-ratio` | 每页 **0** | 0 | 「容器无圆角／无比例」 | 成立 |
| `scroll-margin-top` | 每页 **0** | 0 | 「0 处」 | 成立 |
| `@media` 条数 | 每页 **14** | 224 | 「每页 14 条」 | 成立 |
| `viewport-fit` | 每页 **0** | 0 | 「0/16」 | 成立 |
| `env(safe-area-inset-*)` | 每页 **1** | 16 | 「每页 1 处、只 bottom」 | 成立 |

### §1.5 文本探针（重复句候选）

命令：`node .scratch/t524/probe-text.mjs`（**本票草稿，不进库**）→ 机器重复节点 **235 处**、人核后真冗余 **29 处**（读数件 §3）。

### §1.6 视觉读数（32 张）

`vision_html_screenshot` 出图：390×844 与 1280×900 各 16 张 ＋ 长页 3 张（09-01／09-08／09-16 的 390×3600）；落 `.scratch/t524/out/shots/`。
审图 `vision_describe` 逐页 32 次；逐页分与扣分理由见读数件 §7，复核表见 §7.1（6 条里 2 条判「不成立」、1 条「未证」、1 条「模型误读」）。

## §2 变异自证（判据有没有牙齿；订正波重跑）

`good-sample.html` 原件当刻是干净样板（`RESULT: 1/1` PASS），**审查席位当时那份被改过的 `probe-mut-sep.html` 已被还原/改动，不可复现** —— 故订正波用正例页群**重造**变异，两行都在 `audit-separators.mjs`（同一 sha）上跑：

```
红向（往合格样板塞一处「；」并列）：exit=1  RESULT: 0/1 ｜ FAIL  nodeHitsTotal=1
绿向（还原成合格样板）        ：exit=0  RESULT: 1/1 ｜ PASS  nodeHitsTotal=0
MUTATION-OK 判据有牙（红 exit 1／绿 exit 0）
```

- 脚本：`.scratch/t524/correction-mut.mjs`；落盘 `.scratch/t524/out/mut/red/sep.json`、`mut/ok/sep.json`。
- 变异手法：`.scratch/t524/out/readings-draft/09-01-….html` 的 `</body>` 前插 `<p class="fault">第一张已选；第二张待选。</p>`（一个节点、一处 `；`）。
- 另做的**真页变异**（审查席位做过、订正波复核）：往 09-02 产物副本塞同一处 `；` → `RESULT: 0/1` `FAIL` exit=1 ⇒ 探针对真产物同样有牙。

## §3 一致性自证（同一页：用脚本算 ＝ 用字面公式算，差 0）

命令（`t524-判分.mjs` 自带两条腿，**不共享实现**：一条走函数，一条把算式逐字重打）：

```
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-before
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-draft
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-09-01
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-floor-draft
```

| 读数目录 | 比对项数 | `RECONCILE` 最大绝对差 | exit | 结论 |
|---|---:|---:|---:|---|
| `readings-before`（16 页基线） | 112 | **0** | 0 | 差 0 |
| `readings-draft`（正例） | 7 | **0** | 0 | 差 0 |
| `readings-09-01`（反例） | 7 | **0** | 0 | 差 0 |
| `readings-floor-draft`（下限页） | 7 | **0** | 0 | 差 0 |

**审查件 §4 那两行读数的对话**：旧口径 `20.9`（字面公式）vs `63.9`（作者草稿）⇒ 差 **43**；
订正后同一个 09-02 两腿**差 0**。基线均分：**旧口径 61 → 订正口径 18.75**（差不是「修好了」：
旧口径用的是**另一套权重 ＋ 比例式 ＋ 不扣 H1～H4**）。

## §4 可达性验算（反例红／正例绿／下限页压线绿）

| 线 | 逐维 D1／D2／D3／D4／D5 | 逐维合计 | 硬扣分 | **页分** | 每维 ≥80% |
|---|---|---:|---:|---:|---|
| 反例·09-01 当刻产物 | 12／13／0／15／7 | 47 | −90（H1×34 H2×6 H3×2） | **0** | 否 |
| 正例·手工合格样板 | 15／20／25／25／25 | 100 | −0 | **100** | 全满足 |
| 下限页·每维贴 80% | 12／16／25／12／25 | 90 | −0 | **90** | 全满足 |

正例／下限页的两条自证读数：
`RESULT: 1/1` PASS（exit 0，`readings-draft/sep.json`）、
`OVERFLOW-ZERO pages=1 cells=3 failed=0`（exit 0，`readings-draft/resp.json`）。
构造脚本 `.scratch/t524/correction-sample.mjs`；两向与第三向的汇总落 `.scratch/t524/out/readings-two-way.json`。

**判分脚本的 H6 逐页读数**（390／768 进扣分、1440 只记观感）：09-01 `390:0/768:0/1440:49`、
09-16 `390:0/768:0/1440:15`、其余 14 页 390／768 全 0、1440 在 5～8 之间。

### §4.1 开放项（本波如实记，别当结论用）

1. **H7 的折叠态**：页尾「复制数据 ▾」菜单里 3 项（`data-fmt=text/json/csv`，16 页 × 3 ＝ 48 项），
   折叠态 `opacity:0; visibility:hidden`、两档截图不出现 ⇒ 本波 H7 记 **0**（只数可视区）；
   展开后该不该扣 −1×3 是**开放项**（要扣先改 `copyArea.ts` 中文标签，归 #525），终审 #530 判前定。
2. **`facts.d4cut`**：D4 的人核位（1440 档只多留空的观感债）。正例 0、基线全 0、下限页 3。
   它的**取值表**还没逐页写全——后续票若要拿它压 09-01 这类页，先把人核表补到 `facts.json`。

## §5 环境异常（范围外，归 #530 收口）

### §5.1 回归门工具当刻报「基线不一致」（**病因已订正**）

命令：`node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/gen-photo-baseline.mjs --check`
`runId=f1cb67c5-81d5-4fb2-8914-aa5a59a9fe72`（`waitedMs=370243 exit=1`；等待期间另有一张票持锁，本票等到 `LOCK-ACQUIRED` 才跑，没抢窗）。

读数逐字（节选，**当刻实况**）：

```
CHANGED calorie.help.center want=9cf27744… got=16289e7e…
CHANGED calorie.photo.add want=93e2604c… got=ERROR:渲染当刻时钟未被剥净：2026-09-15 13:51:17
CHANGED calorie.photo.compare / detail / gif / list / tag / view.gif-planner / view.photo-log-wizard 各一条
CHANGED calorie.photo.remove want=3f13ea75… got=ERROR:渲染当刻时钟未被剥净：2026-09-15 13:51:18
UNSTABLE calorie.photo.add:渲染当刻时钟未被剥净：2026-09-15 13:51:17 | calorie.photo.remove:渲染当刻时钟未被剥净：2026-09-15 13:51:18
BASELINE-DIFF 10 pages
```

定性：**当刻的病因是 `BASELINE-DIFF 10 pages` ＋ 两页「渲染当刻时钟未被剥净」**（10 条 `CHANGED` 全是 `calorie.photo.*`／`calorie.help.center`／两张过程页），
**不是**旧稿写的 `dist/diet/review.js` 导入 `buildDietReviewEmptyDoc` 失败（那是**更早一次的过时病因**，现象在、病因已换）。
本票边界**不许动 `packages/**`、不许跑构建**，故不复跑、不修 ⇒ **归 #530 收口**（#530 复跑前先重建基线，并把两页的渲染时钟剥净）。

### §5.2 行锚漂移（订正波逐条实测）

| 件 | 旧稿行锚 | **当刻实测** | 备注 |
|---|---|---|---|
| `src/photo/galleryDoc.ts` | `:219` | **`:236`**（`buildPhotoListDoc`） | 漂 17 行 |
| `src/photo/viewerDoc.ts` | `:198` | **`:211`**（`buildPhotoViewerDoc`） | 漂 13 行 |
| `packages/base-render/src/blocks.ts` | 2158 LF；样式段 `:1254`·`:1547`·`:1691` | **2169 LF**；`@media (max-width: 640px)` 六处 `:1254`·`:1542`·`:1547`·`:1669`·`:1689`·`:1691`；`overflow-x` 五处 `:1560`·`:1678`·`:1862`·`:2114`·`:2126` | 件在途编辑中（#499） |
| `packages/base-render/src/style.ts` | 1310 LF；`:1167`／`:1178`／`:1183` | **1327 LF**；`@media (max-width: 640px)` **`:1183`**、`(max-width: 400px)` **`:1194`**、`prefers-reduced-motion` `:1199` | **旧稿三条逐条引错** |
| `packages/base-render/src/pageUi.ts` | `:99`·`:100`（安全区）／`:68`（`scroll-margin-top`）／`:60`（置灰规则） | `env(safe-area-inset-*)` 的 `max(16px, env(...))` **`:92`·`:93`**；`scroll-margin-top:20px` **`:62`**；`@media (max-width: 820px)` **`:71`**、`min-height:44px` **`:78`**；**置灰规则不在本件**（`:57`–`:58` 注释说明已上移到 `style.ts` 的 `copyButton` 区，当刻 `:430`·`:431` 两条 `[disabled]`／`:disabled`） | 旧稿四处引错 |
| `src/shared/copyArea.ts` | `:22`·`:24`·`:48` | `:22`·`:48`·`:66` | 露英文处 |

**记法**：**行号会漂，引用要带函数名／选择器**（基准件 §1.2 末）。

## §6 复现清单（一条命令一行，照抄即可）

```
# ① 分隔符与内部标识符（当刻判红）
node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t524/pages16 --json .scratch/t524/out/sep-rerun-16.json
# ② 三档零溢出（回归门）
node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t524/pages16 --widths 390,768,1440 --json .scratch/t524/out/resp-before-16.json
# ③ 版式与形状读数（触摸目标／字号／断点）——必须用 HEAD 版，见 §1.3
git show HEAD:docs/skills/skill-calorie/t516-判据-版式.mjs > .scratch/t524/t516-head.mjs
node .scratch/t524/t516-head.mjs --dir .scratch/t524/out/readings-before --widths 390,768,1440 --json .scratch/t524/out/fmt-16.json
# ④ 变异两行（红／绿）
node .scratch/t524/correction-mut.mjs
# ⑤ 判分（含一致性自证，exit 0 ＝ 两腿差 0）
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-before
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-09-01
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-draft
node docs/skills/skill-calorie/t524-判分.mjs --dir .scratch/t524/out/readings-floor-draft
# ⑥ 静态 DOM 事实（overflow-x／toc／data-label／aspect-ratio／行锚）
node .scratch/t524/correction-probe.mjs
# ⑦ 回归门（当刻红，见 §5.1）
node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/gen-photo-baseline.mjs --check
# ⑧ 交付面哈希核对（16 件逐字节同哈希）
Get-ChildItem .scratch/t524/pages16, 'D:\ilife-验收\scene09'
```
