# t524 · 场景 09 视觉整改基准 · 证据件

> 票：[#524](https://github.com/FeatherHunter/ilife/issues/524)（Part of [#159](https://github.com/FeatherHunter/ilife/issues/159)）。
> 本件记**命令与读数**：每条都经加锁包装器 `node tooling/run-locked.mjs --ticket 524 -- <cmd>` 跑，逐条给 GATE-RUN `runId`。
> 判据口径见 [`t524-场景09-视觉整改基准.md`](./t524-场景09-视觉整改基准.md)，逐格读数见 [`t524-改前读数.md`](./t524-改前读数.md)。

## §0 被检物与页群

| 件 | 事实 |
|---|---|
| 交付目录（**只读**） | `D:\ilife-验收\scene09\`（16 件样张 ＋ `00-场景09验收导航.html`） |
| 工作页群（读数用这个） | `.scratch/t524/pages16/`（**16 件，与交付目录 16 件逐字节同哈希**；逐件字节与 sha256 见读数件 §0） |
| 工作区内的截图源 | `.scratch/t351-preview/scene09/*.html`（`vision_html_screenshot` 只吃工作区内路径） |
| 合格样板（变异对照） | `.scratch/t524/probe/good-sample.html`（6975 字节，手工构造；两条判据都绿） |
| 页群字节合计 | 45,328,374 字节（09-08 18.8 MB ＋ 09-14 23.0 MB 两页占 42 MB，减重归 #461） |
| 页群 mtime | 2026-09-15 08:14:48（＝**改前**；源码 13:01～13:28 已被改到，见基准件 §1.3 的「时刻差」） |

## §1 机器门读数（GATE-RUN）

### §1.1 分隔符与内部标识符探针（**当刻判红**）

命令（本票现场重跑，2026-09-15）：

```
node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t524/pages16 --json .scratch/t524/out/sep-rerun-16.json
```

读数逐字（JSON 摘要字段）：

```
"dir": ".scratch/t524/pages16", "files": 16, "green": 0, "broken": 0, "nodeHitsTotal": 231
```

摘要行逐字：**`RESULT: 0/16`** ＋ `FAIL`，`exit=1`。
GATE-RUN：`runId=ad93f5e3-d72f-471a-a1d9-111ca53171b7`（`waitedMs=30009 exit=1`）。

- 规则命中（本票页群）：R1 `·` 154／R2 `；` 6／R3 ≥3 段 83／R4 `｜` 0／R5 `、` 2／R6 `~` 1／R7 内部标识符 121。
- 交付目录 17 页版（编排者基线，同日）：**`RESULT: 0/17`**（含 `00-场景09验收导航.html` 自身 82 命中，归 #530 收口）。
- 工具身份：`packages/skill-calorie/scripts/audit-separators.mjs`，**另一 session 的在途件**，本票只读跑、不改、不提交；当刻 sha256 ＝ `A8FEA10F5E92C3810D7D0404A7EE6CF59D44A81FDBB11AFC728401C72F9481CE`（275 行）。

### §1.2 三档响应式（**回归门，当前全零**）

命令：`node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t524/pages16 --widths 390,768,1440 --json .scratch/t524/out/resp-before-16.json`

摘要行逐字：**`OVERFLOW-ZERO pages=16 cells=48 failed=0 label=t524-before-16pages`**，`exit=0`。
编排者同日对交付目录 17 页：**`OVERFLOW-ZERO pages=17 cells=51 failed=0`**（**#484 的成果 ⇒ 本波按回归门用，不报成绩**）。

### §1.3 版式与形状读数（触摸目标／字号／触屏三件／断点集）

命令：`node tooling/run-locked.mjs --ticket 524 -- node docs/skills/skill-calorie/t516-判据-版式.mjs --dir .scratch/t524/pages16 --widths 390,768,1440 --json .scratch/t524/out/fmt-16.json`
GATE-RUN：`runId=5cd9b8cf-edc3-4ac3-a106-883253000b60`（`waitedMs=10012 exit=0`）；工具摘要行：`RESULT: 16/16 件读数完成（四档=390/768/1440）`。

**该工具只出读数、不判红绿**（退出码 0＝读数完整落盘，1＝有页读不动，2＝用法错），阈值写在基准件 §4.1／§4.2。

逐页读数（390 档 字号下限／390 档触摸不足／1440 档触摸不足／390 档越界／1440 档越界／1440 档内容列／页壳上限／`toc` 个数）：

| 样张 | 390 minFont | 390 touch<44 | 1440 touch<44 | 390 越界 | 1440 越界 | 1440 列宽 | 页壳 max | 390 toc |
|---|---:|---:|---:|---:|---:|---:|---|---:|
| 09-01 | 11 | 0 | 49 | 0 | 0 | 1000 | 960px | 0 |
| 09-02 | 11 | 0 | 7 | 0 | 0 | 1000 | 960px | 0 |
| 09-03 | 11 | 0 | 7 | 0 | 0 | 1000 | 960px | 0 |
| 09-04 | 11 | 0 | 5 | 0 | 0 | 1000 | 960px | 0 |
| 09-05 | 11 | 0 | 7 | 0 | 0 | 1000 | 960px | 0 |
| 09-06 | 12 | 0 | 7 | 0 | 0 | 1000 | 960px | 0 |
| 09-07 | 11 | 0 | 7 | 0 | 0 | 1000 | 960px | 0 |
| 09-08 | 12 | 0 | 7 | 0 | 0 | 1000 | 960px | 0 |
| 09-09 | 11 | 0 | 5 | 0 | 0 | 1000 | 960px | 0 |
| 09-10 | 11 | 0 | 5 | 0 | 0 | 1000 | 960px | 0 |
| 09-11 | 11 | 0 | 5 | 0 | 0 | 1000 | 960px | 0 |
| 09-12 | 11 | 0 | 5 | 0 | 0 | 1000 | 960px | 0 |
| 09-13 | 11 | 0 | 5 | 0 | 0 | 1000 | 960px | 0 |
| 09-14 | 11 | 0 | 7 | 0 | 0 | 1000 | 960px | 0 |
| 09-15 | 12 | 0 | 8 | 0 | 0 | 1000 | 960px | 0 |
| 09-16 | 12 | 0 | 15 | 0 | 0 | 1000 | 960px | 0 |

两条当刻事实：① **1440 档触摸不足 5～49 处／页**，逐条都是 `button.ilife-copy-btn`（`78x40`／`476x40`）与 `button.ilife-copy-menu-item`（`186x40`）——**40px < 44px**；② **390 档字号下限 11px 的有 12 页**，责任元素 `th.ilife-block-data-table-cell-left`／`-right`（表头）。

### §1.4 文本探针（重复句候选）

命令：`node .scratch/t524/probe-text.mjs`（**本票草稿，不进库**；机器口径＝同一可见文本节点 ≥4 字同页 ≥2 次）。GATE-RUN `runId=b2c77c96-3e50-4486-aeb3-c2f59ff67b07`。
读数：机器重复节点 **235 处**；**人核后真冗余 29 处**（逐条见读数件 §3）。两者之差＝逐条如实报（逐张占位句／逐条候选状态／逐段复制按钮）。

### §1.5 视觉读数（32 张）

`vision_html_screenshot` 出图：390×844 与 1280×900 各 16 张 ＋ 长页 3 张（09-01／09-08／09-16 的 390×3600）；落 `.scratch/t524/out/shots/`。审图 `vision_describe` 逐页 32 次；逐页分与扣分理由见读数件 §7，复核表见 §7.1（6 条里 2 条判「不成立」、1 条「未证」、1 条「模型误读」）。

## §2 变异自证（判据有没有牙齿）

### §2.1 两行原文（本票现场重跑，逐字抄）

**红向**（往合格样板塞一处 `；` 并列）：

```
FILE probe-mut-sep.html  node·0 ；1 ≥3段0 ｜0 、0 ~0 标识符0  命中=1  (行级命中=3)
   L  119 # 48 R2  第一张已选；第二张待选。   <fault> zone=body
RESULT: 0/1
FAIL
```

`exit=1`；GATE-RUN `runId=0169253d-deb1-4208-8cbf-43f2dbfe696b`（`waitedMs=0 exit=1`）。
变异手法：`good-sample.html` 里 `<div class="note">` 前插入 `<p class="fault">第一张已选；第二张待选。</p>`。

**绿向**（还原成合格样板）：

```
LOCK-ACQUIRED ticket=524 runId=e2033dd5-8d2d-4983-9b0b-605b8ec40d9e waitedMs=20019 pid=44216
RESULT: 1/1
PASS
```

`exit=0`；GATE-RUN `runId=e2033dd5-8d2d-4983-9b0b-605b8ec40d9e`（`waitedMs=20019 exit=0`）。
反面前置（原型自证）：`audit-separators.mjs .scratch/t524/probe/good-sample.html` → **`RESULT: 1/1` PASS，`exit=0`**，GATE-RUN `runId=1020fd64-3295-45b7-95ae-f40e402533cb`。

### §2.2 溢出判据的变异两行（回归门的识别力，2026-09-15T05:06:45Z 落盘）

| 变异 | 判据 | 注入后 | 还原后 | 结论 |
|---|---|---|---|---|
| 塞一条 `width:1200px` 的元素 | 三档读数 | `exit=1`（768 档 `+432` 溢出、390 档视口守卫破） | `exit=0`（48 格全零） | 成立 |
| 塞一张不带 `max-width` 兜底的 2000px 宽图 | 三档读数 | `exit=1`（390 `+440`／768 `+1232`／1440 `+560`，且 `img-not-fit=1`） | `exit=0`（全零） | 成立 |

落盘：`.scratch/t524/out/mutations.json`（`m0_sep=0 m0_resp=0 m1_red=1 m1_green=0 m2_red=1 m2_green=0 m3_red=1 m3_green=0`）。

## §3 环境异常（范围外，归 #530 收口）

### §3.1 回归门工具当刻不可用：`gen-photo-baseline.mjs --check` 报构建产物不同步

命令：`node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/gen-photo-baseline.mjs --check`
GATE-RUN：`runId=c2df8726-618d-40ac-9173-063c117026c1`（`waitedMs=20009 exit=1`）；同日另一次 `runId=d790b05f-543c-4201-ad32-0011a49446cb`（`waitedMs=60036 exit=1`）。
报错逐字（节选）：

```
file:///D:/ilife/packages/skill-calorie/dist/diet/review.js:7
import { buildDietReviewDoc, buildDietReviewEmptyDoc, mealParamOf } from './reviewDocs.js';
SyntaxError: The requested module './reviewDocs.js' does not provide an export named 'buildDietReviewEmptyDoc'
```

定性：**不是本票的件**——`dist/`（构建产物）与 `src/diet/reviewDocs.ts`（别的 session 的在途改件）不同步；本票边界**不许动 `packages/**`、不许跑构建**，故不复跑、不修。⇒ 读数记「当刻不可用」，**归 #530 收口**（#530 复跑前先重建）。

### §3.2 并发记账（加锁包装器的等待，说明本票没有抢占别人的窗）

同批命令里出现过对 #272r／#276／#522／#525／#527 的 `WAIT-OWNER-ALIVE`（等待它们释放锁），本票每次都等到 `LOCK-ACQUIRED` 才跑，**没有抢回别人的窗**。

## §4 复现清单（一条命令一行，照抄即可）

```
# ① 分隔符与内部标识符（当刻判红）
node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t524/pages16 --json .scratch/t524/out/sep-rerun-16.json
# ② 三档零溢出（回归门）
node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t524/pages16 --widths 390,768,1440 --json .scratch/t524/out/resp-before-16.json
# ③ 版式与形状读数（触摸目标／字号／断点）
node tooling/run-locked.mjs --ticket 524 -- node docs/skills/skill-calorie/t516-判据-版式.mjs --dir .scratch/t524/pages16 --widths 390,768,1440 --json .scratch/t524/out/fmt-16.json
# ④ 变异两行（红／绿）
node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/audit-separators.mjs .scratch/t524/probe/probe-mut-sep.html --json .scratch/t524/out/sep-mut-verify-red.json
node tooling/run-locked.mjs --ticket 524 -- node packages/skill-calorie/scripts/audit-separators.mjs .scratch/t524/probe/good-sample.html --json .scratch/t524/out/sep-mut-verify-green.json
# ⑤ 交付面哈希核对（16 件逐字节同哈希）
Get-ChildItem .scratch/t524/pages16, 'D:\ilife-验收\scene09'
```
