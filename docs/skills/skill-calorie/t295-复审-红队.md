# #295 定点复审（红队席）：契约与真伪

范围 `5af4dd1..HEAD`（`8f5b1a6`／`b3e9aed`／`59bf368`）＋ `docs/skills/skill-calorie/t295-返修证据.md`；判据＝`.scratch/orchestrator/t295-rework-brief.md` A1–A8、`.scratch/orchestrator/t295-rereview-brief.md`。
**一切复跑都是我自己的读数；返修席自述只用来对账，不当证据。**

```
VERDICT: PASS
SCORE: 契约=28/30 证据=23/25 parity=19/20 红线=15/15 文档=9/10 → 均分=94
```

## A1 · 五处钉死计数改派生对账 —— **消除**

七条断言逐字核（非四处）：`cmd-registry-294.test.mjs:193` → `DECLARED_CAPABILITY_KEYS.length`；`cmd-write-40-persist.test.mjs:750/753/755` → `deepEqual([...WRITE_KEYS].sort(), DECLARED_WRITE_KEYS)`／`readsByKey.size === WRITE_KEYS.length`／`total >= WRITE_KEYS.length`；`m5-receipt-97.test.mjs:96/97` → `deepEqual(...)`／`SCENARIOS.length === WRITE_KEYS.length`；`output-naming-119.test.mjs:67` → `receiptCount === DECLARED_WRITE_KEYS.length`。对账源唯一：`test/declared.mjs`（`LEGACY_COMMANDS` ＋ `REGISTRY`）。**没有删成「没有断言」**：数量断言全在，覆盖门（每写键 ≥1 次真实回读）也留。

行为判据（仓外临时根 `D:\_t295rr\repo`＝HEAD 工作树拷贝 ＋ `node_modules` junction，协议 §2.1；runId `6be3f862`）：
- **加**一条带 `example` 的读命令（只手改 `src/weight/commands.ts` 一件）→ 四步链 `build①→gen→build②→help:build` 全 exit 0、`gen:check` exit 0、**`test/` 下改动面 0 件**；靶向 7 文件里四个计数文件（`cmd-write-40-persist`／`m5-receipt-97`／`output-naming-119`／`cmd-read-t11`）＋`test/combos-42` **全绿**，红的只有路由/parity（`cmd-registry-294` 的「代表唤醒词须真唤醒词」＋`calorie-routing-81` 的 `#81`／`D2①`／`D2③`／`FX-81-5`）＝票面已认下的 #297 面。
- **删**一条真命令（`src/cli/legacyCommands.ts` 的 `calorie.view.dedupe`）→ `build②` 直接 `TS2820 routing.ts(193,54)/(648,51): "calorie.view.dedupe" is not assignable to 'RouteKey'` ⇒ 删命令必动 `routing.ts`（票面明写）；四个计数文件仍无需改一行。

观察（S3，不翻判）：`cmd-registry-294:193` 的计数两边同源（生成物 `registry` vs 源声明），单看是「生成物 == 自身生成源」的对称对账，独立鉴别力靠同块上一行的键集合对账与 `gen:check`。

## A2 · 账目（第五件 · 进度）—— **消除**

`gh issue view 295` 实读：标题下进度 **`95%（待复审——返修已交，等定点复审）`**；五件表第 ⑤ 行 **`**未做 → 归 #297**（地图记 4/5）`**；并明写「今天加／删一条命令仍要手改 `src/triggers/routing.ts`，否则 `calorie-routing-81` 的 D2③ 与 `cmd-registry-294` 变红」＋「连带还要动 `test/calorie-routing-81.test.mjs` 的 58／344／403／495 与 `docs/research/t81-exec-smoke.md` 快照」＋「#296 的纪律文本不得以『共用位零接触』为前提落笔」。4/5 口径写清。

## A3 · `exampleFor` 由声明产出 —— **消除（两条判据都成立）**

- 契约面：`5af4dd1` 的 101 条手写 `case` 与新生成块 `EXAMPLES` 逐条比：**101/101 逐字节相同、0 处改写、0 处缺**；新 `build-help.mjs` 里手写 `case` **0 处**、`default: throw` 已不存（`exampleFor` 改查表＋缺即抛）。
- 行为：加一条带 `example` 的新命令 → **`gen` 自己改写了 `build-help.mjs`**（sha 变化＝true，人手没碰它），`SKILL.md` 自动多出一行 `| 看探针体重 | calorie.view.zzz-probe | stat | \`calorie-cmd-read calorie.view.zzz-probe\` |`。
- 逐字节：基线 `help:build` 后 `git status` 空，`SKILL.md` sha256 = `DF63151429D5E55C2273593F548E41D6F68271D3535BF4106ADCC643CC5FA3F3` —— 与派单给定常量逐字相同，**重生成前后一致**（runId `506e2b6a`）。

## A4 · `gen:check` 假绿 —— **消除**

新鲜态 `gen:check` exit 0；只改声明不 build → `gen:check` **exit 1** ＋ 逐字 `GEN-STALE FAIL：dist/ 不是这些声明的产物…`；`gen` 同样 exit 1；build 后复绿（runId `b5cf61c4`）。
尖锐边（S3）：守卫判据是 **mtime**，而 `tsc -b` 增量在内容未变时**不重写 `dist`** ⇒「dist 内容正确但比 src 旧」（如 `git checkout -- .` 刷新声明 mtime）会持续报 `GEN-STALE`（runId `b5cf61c4` 的 base2 段），要删 `packages/skill-calorie/dist` 才复绿。方向保守（不假绿），但证据 ⑧.3 的「build → gen」次序不足以复绿。

## A5 · `legacyCommands.ts` 棘轮 —— **消除**

四步链 build 后往未搬迁清单加一条新键 → 新件 `legacy-ratchet-295.test.mjs` **exit 1**，逐字 `未搬迁清单出现了冻结清单外的新键（新命令要住它自己的能力目录 src/<能力>/commands.ts，这里只进不出）：calorie.view.zzz-intruder` ＋ `未搬迁清单条数变高：93 > 92`；未 build 时先咬 `行数变高：120 > 119`（runId `b5cf61c4`）。

## A6 · 重生成入口 —— **消除**

根 `package.json` 有 `"help:build"`；`build-help.mjs` 文件头写明 `build → gen → build → help:build` 与 `gen:check` 的分工；仓外根 `help:build` exit 0。

## A7 · 对账命令照抄可复现 —— **消除**

照抄证据 ⑦ 字面命令（含写死窗口 ＋ `--allow-nonzero`）我独立跑：`matched=11/11 auditEntries=1373 scoped=11 undeclared=0`，`gate-audit: PASS`，exit 0；导出源 `t295-返修-gate-runs.log` 受 git 跟踪。副作用一条：该命令会重写导出文件的「导出时间」一行（`git status` 会脏 1 行），我已 `git checkout` 还原，第三方照抄也只需还原。

## A8 · CI 一步红不吞门 —— **消除**

`gh run view 34745203987`（commit `8f5b1a6`，我独立拉 GitHub API，6 个 matrix 全核）：`build-test` 15 步**全真跑、0 skipped**（`gen:check`／`doctor` success、`test` failure、`boundaries` success、`snapshot:html:check` success、`gate:selftest:html` failure、`changeset:status` success、`help:examples:check` failure），job 结论 failure。`ci.yml` 七道门各带 `if: always()`，全文无 `continue-on-error`（新件 `test/ci-gates-295.test.mjs` 机器盯着）。

## 红线复核

1. **棘轮未放宽**：`git diff 1396d67 HEAD -- packages/skill-calorie/test/cmd-registry-294.test.mjs` = 仅 5 行（＋import、＋注释、换断言）；`FROZEN_READ_CASES=61`／`FROZEN_READ_LINES=1146`／`FROZEN_WRITE_CASES=31`／`FROZEN_WRITE_LINES=745` 逐字未动；新棘轮是新增件（92／119 上限）。
2. **门未削弱**：新鲜态往生成物 `src/cli/keys.ts` 手加一行 → `gen:check` **exit 1**，还原 exit 0；CI 无 skip／`continue-on-error` 掩盖；五处计数断言都留了断言。CI 里 `pnpm test` 红是票外既有（插件 client 产物 ＋ Windows 语义测试），红没被吞。
3. **无证据造假**：`--allow-nonzero` 有配套 `GATE-RELAX` 行与理由；对账 11/11 我独立复现；导出源入库。

## 探针

`node docs/skills/skill-calorie/t293-验收-命令自治.mjs` → **`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`**（我实跑，`UNACCOUNTED=0`，P1 的假命令探针已绿）。

## 新探针（我设计的这一个）

仓外临时根三探针（`D:\_t295rr\repo`＝HEAD 拷贝，四步链，runId `6be3f862`／`b5cf61c4`，用完即清）：加读命令（test/ 0 件、SKILL.md 自动出行）、删真命令（`build②` 撞 `routing.ts` 的 `RouteKey` 类型门）、**加写命令**。第三个读数：`cmd-write-40-persist` 覆盖门红在「以下写键没有『写后 SELECT 回读』断言：calorie.weight.zzz-probe」，`m5-receipt-97` 红在「场景表条数 == 写键数」——即**加一条写命令要在 `packages/skill-calorie/test/cmd-write-40-persist.test.mjs` 补该键落库块 ＋ 在 `docs/research/t97-probe-receipts.mjs` 补一行场景**。这是 #101 H2 覆盖门（新写键要有真实回读断言）的既有要求，不是计数数字、也不是同一事实的第二份手写副本；但**探针的三桶没把这面算进 UNACCOUNTED**（它只看生成物／计数／分派／路由四类），P1 的 `UNACCOUNTED=0` 口径偏窄。

## 复跑（我自己的 runId 与 exit）

| runId | 内容 | exit |
| --- | --- | --- |
| `506e2b6a` | 基线：`gen:check` 0、`help:build` 0、SKILL.md sha 不变、靶向 7 文件 0 | 0 |
| `b5cf61c4` | 红线②（手改生成物→`gen:check` 1，还原 0）／A4（`GEN-STALE` 1）／A5（棘轮 1） | 0（各条已单列） |
| `6be3f862` | 加读／删真／加写三探针 | 0 |
| `9917711b` | 第一批探针（`tsc.CMD` 直调写坏、build 未真跑，结论**作废**，自证记账） | 0 |
| `8a372004` | 六道门＋`gen:check` 复跑（干净 HEAD 拷贝）：`build` 0／`gen:check` 0／**`test` 0（1486 tests，pass 1482，fail 0，skipped 4）**／`doctor` 0／`boundaries` 0／`snapshot:html:check` 0（changed=0）／`help:examples:check` **1（100/101，票外既有红：`calorie.view.exercise-review`，已转 #308）** | 0／1 |

对账窗口（本席，供审计）：`2026-09-13T07:48:41Z ～ 2026-09-13T07:58:53Z`（含）内 ticket=295 的 5 条 RUN：`506e2b6a`／`9917711b`／`b5cf61c4`／`6be3f862`／`8a372004`——**除本席外无其他持锁运行**，即此窗口内不存在「无人声明的运行」。

## 未消除 / 新缺陷（逐条标归属）

1. **S3 · 口径（归属：返修席／#296 文本）**：票面「除 routing 外…**加一条命令只需改它自己的能力目录声明**」对**写命令**不成立——还要补 `cmd-write-40-persist` 的落库块与 `docs/research/t97-probe-receipts.mjs` 一行（见「新探针」）。建议票面改成读写两路分开说。**不翻 A1**：那四处计数不在必改面内，且这是覆盖门要求。
2. **S3 · 探针盲区（归属：探针席）**：`t293-验收-命令自治.mjs` 的三桶只覆盖生成物／计数／分派／路由；「新键的逐键测试数据」既不在 ① 也不在 ③。请把 ③ 的判据补上「逐键测试数据算不算手写面」，否则 `UNACCOUNTED=0` 会被读成「加一条命令零手改」。
3. **S3 · 实现尖锐边（归属：返修席）**：A4 守卫的 mtime 口径 ＋ `tsc -b` 增量不重写 dist ⇒「内容正确、mtime 更旧」的工作树持续 `GEN-STALE`（不假绿、只误报；复绿须删 `packages/skill-calorie/dist`）。
4. **过程卫生（归属：某 session 未还原，非本席）**：复核期间工作树被留下**未提交**变异——`packages/skill-calorie/src/weight/commands.ts` 尾部追加 `// stale-probe`（mtime `2026-09-13 15:47:02 +08:00`，我 15:47:34 起多次复查仍在；`.scratch/locks/gate-runs.log` 在该时刻无持锁运行）。它不在返修提交内，但留着不还原会让「④工作区干净」不成立、且该树上 `pnpm gen:check` 会因第 3 条报红。按纪律我未动它。

**结论**：A1／A2／A3／A4／A5／A6／A7／A8 **八条全部消除**（逐条有我的读数），三条红线均未触发，客观验收线 `P1=PASS`。判 **PASS**；上面四条是口径与卫生项，归 #296／探针席／编排者收尾，不构成返修阻断。
