# t482 · 卡路里场景 03「体重」复盘族（6 词）文本审查 · 证据件

## 一、头部

- **票号**：#482（场景 03「体重」· 地图 #154；本票由切分评论收窄为**复盘 6 词**，波动 5 词移 **#485**——见 `.scratch/t154/text-review/comment-482.md`／`comment-482-fix.md`）。
- **范围（6 页，逐页全过）**：第 15 条 体重复盘（本周）／第 22 条 体重复盘（本月）／第 29 条 体重复盘（最近 90 天）／第 36 条 体重复盘（今年）／第 40 条 体重复盘（自定义时间）／第 44 条 看里程碑回溯。
- **读者画像**：不懂技术的项目负责人（口径 §一）。他不想在看「这周体重怎么样」时读到 `weight_log`／`vs`／`等长区间`／`5/10/15/20kg` 这类实现话与内部记号。
- **口径正本**：`.scratch/t154/text-review/口径.md`；**改前基线**：`.scratch/t154/text-review/inventory/review.md`（本票改前逐页清单）。
- **写集（改前 LF → 改后 LF，LF 口径只数 `\n`）**：
  | 件 | 改前 | 改后 | 说明 |
  |---|---|---|---|
  | `packages/skill-calorie/src/weight/review.ts` | 600 | 647 | 本票唯一改动件（窗口形态＋回溯形态两处装配＋两处视图模型结论句） |
  | `packages/skill-calorie/test/weight-review-335.test.mjs` | — （未跟踪的无主件，0） | 224 | **采纳无主测试件**：写于 2026-09-14 13:02，`git status` 里是 `??`，从未入库；采纳前先量现场（9/9 绿、断言有效），随本票入库 |
  | `docs/skills/skill-calorie/t482-文本审查-review.md` | — | 本文件 | 新建（证据件） |
- **形态纪律（口径 §二）**：只用了已有公共件 —— `renderCaliberLine`／`renderDataTable`／`renderListRows`／`renderDisclosure`／`renderKpiGrid`／`renderChartBlock`／`notice`；**无新造样式、无行内样式、无新 CSS 类**。复制区形态与深色 toast、眉标、第 48 条整页：一字符未动。
- **红线自检**：① 口径未丢（空态句自带口径、来源行写清窗口与条数、结论块一页恰一处 `renderDisclosure({ title: '结论' })`）；② 复制载荷＝所见（`结论` 原句同住 `v.summary`，页面与三种格式逐字同源）；③ 复制区形态未动；④ 未新造样式；⑤ 删掉的逐条登记在下面第三节。

## 二、逐页表（6 页逐条过）

### 第 15 条 · 体重复盘（本周）

| 现文本（逐字） | 判据 | 判定 | 改后文本 | 依据 |
|---|---|---|---|---|
| `2026-09-07 ~ 2026-09-07 · 1 条记录`（副标题） | 冗 | 删（条数那半句） | `2026-09-07 ~ 2026-09-07` | 条数与页脚来源行是同一条事实；口径 §3.1 已把「窗口＋条数」钉在页脚 |
| `样本与口径`（页顶提示标题） | 难 | 改 | `记录太少` | 口径 §3.2 术语表：`样本`／`口径` 不上屏 |
| `⚠️ 本窗只有 1 条记录` | 难 | 改 | `⚠️ 这段时间只记了 1 条` | `本窗` 是实现话，换成读者话 |
| `单点无变化：曲线只有这一点，横线就是该点均值；一条记录谈不上趋势` | 冗＋难 | 改 | `曲线上只有这一个点，图上那条横线就是这个点的平均值；一条记录谈不上趋势` | 前半句与「期间变化」卡副说明同一条事实（删）；`均值`→`平均值` |
| `0.0 kg`（期间变化值槽） | 形式 | 改 | `0 kg` | 口径 §3.3：零差值写 `0 kg`（正零不带符号、不带小数） |
| `共 1 条 · 最高 70.4 kg／最低 70.4 kg`（期间均值副说明） | 冗 | 删＋换 | （单点整条删；多点写 `最高 70.4 kg · 最低 70.4 kg`） | `共 N 条` 归页脚；单点最高＝最低＝该值，复述它零信息量；全角斜杠换成间隔号 |
| `vs 上周`＋副说明 `均值差 vs 上周` | 冗＋难 | 改 | `比上周`＋`前面同样长的 1 天：2026-09-06` | 副说明是标签的复述（删）；标签去 `vs`、写明对照的是哪一段 |
| `1`／`覆盖 1 天`（记录天数卡） | 冗 | 改 | `1 天`（副说明删） | 值槽带单位（口径 §3.3）；`覆盖 N 天` 在满窗时是值槽的重述 |
| `期间均值 70.4kg`（图上横线标签） | 形式 | 改 | `期间均值 70.4 kg` | 口径 §3.3：数字与单位之间留一个空格 |
| `（本周）变化 0.0 kg（均值 70.4kg） · vs 上周均值 0.0kg`（结论句） | 冗 | 改 | `只有 1 条记录，看不出这段时间是涨是跌。` | 三个数全在卡片上，结论块复述＝百分百冗余；§5.3 结论块要说判断 |
| `📊 数据来源:weight_log ｜ 窗口 2026-09-07 ~ 2026-09-07 ｜ 1 条`（页脚） | 难＋形式 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-09-07 ~ 2026-09-07 ｜ 共 1 条` | 口径 §3.1 统一句式（全角冒号、读者话来源名、`共 N 条`） |

### 第 22 条 · 体重复盘（本月）

| 现文本（逐字） | 判据 | 判定 | 改后文本 | 依据 |
|---|---|---|---|---|
| `2026-09-01 ~ 2026-09-07 · 7 条记录` | 冗 | 删（条数那半句） | `2026-09-01 ~ 2026-09-07` | 同第 15 条 |
| `共 7 条 · 最高 70.4 kg／最低 70.3 kg` | 冗 | 删＋换 | `最高 70.4 kg · 最低 70.3 kg` | 同上 |
| `vs 上月`＋`均值差 vs 上月` | 冗＋难 | 改 | `比上月`＋`前面同样长的 7 天：2026-08-25 ~ 2026-08-31` | 同上 |
| `7`／`覆盖 7 天` | 冗 | 改 | `7 天`（副说明删） | 同上 |
| `期间均值 70.4kg` | 形式 | 改 | `期间均值 70.4 kg` | 同上 |
| `（本月）变化 +0.1 kg（均值 70.4kg） · vs 上月均值 +0.1kg` | 冗 | 改 | `平均每天多 17 克，在往上走。` | 卡片已有变化／均值／对照三个数；结论改说卡片上没有的「快慢」，全族统一「克」（口径 §3.2） |
| `📊 数据来源:weight_log ｜ 窗口 2026-09-01 ~ 2026-09-07 ｜ 7 条` | 难＋形式 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-09-01 ~ 2026-09-07 ｜ 共 7 条` | 口径 §3.1 |

### 第 29 条 · 体重复盘（最近 90 天）

| 现文本（逐字） | 判据 | 判定 | 改后文本 | 依据 |
|---|---|---|---|---|
| `2026-06-10 ~ 2026-09-07 · 90 条记录` | 冗 | 删（条数那半句） | `2026-06-10 ~ 2026-09-07` | 同第 15 条 |
| `共 90 条 · 最高 70.5 kg／最低 69.9 kg` | 冗 | 删＋换 | `最高 70.5 kg · 最低 69.9 kg` | 同上 |
| `vs 上一段等长区间`＋`均值差 vs 上一段等长区间` | 冗＋难 | 改 | `比前面一段`＋`前面同样长的 90 天：2026-03-12 ~ 2026-06-09` | `等长区间`是不懂技术的读者读不出意思的话；副说明原是标签复述，改成写明对照区间 |
| `90`／`覆盖 90 天` | 冗 | 改 | `90 天`（副说明删） | 同上 |
| `4`／`月均值表 4 行`（月份数卡） | 冗 | 改 | `4 个月`（副说明删） | 副说明是值槽的重述；下方表的表注自带月份数 |
| `期间均值 70.1kg` | 形式 | 改 | `期间均值 70.1 kg` | 口径 §3.3 |
| `月均值（共 4 个月）`＋表头 `月均值`＋行值 `70` | 形式 | 换形态 | 表注 `按月平均体重（单位 kg，共 4 个月）`＋表头 `平均体重`＋行值 `70.0` | 与卡片标签（期间均值）不再撞词；重量一律一位小数（与图上刻度同一写法）；单位进表注（公共层会把表头转成大写，`（kg）` 会渲染成 `（KG）`，故不写进表头） |
| `（最近 90 天）变化 -0.1 kg（均值 70.1kg） · vs 上一段等长区间均值 -1.4kg` | 冗 | 改 | `平均每天少 1 克，基本算稳定。` | 同上；平稳阈值用既有「日均 ±10 克」口径（`figures.ts` 同源），不新造阈值 |
| `📊 数据来源:weight_log ｜ 窗口 2026-06-10 ~ 2026-09-07 ｜ 90 条` | 难＋形式 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-06-10 ~ 2026-09-07 ｜ 共 90 条` | 口径 §3.1 |

### 第 36 条 · 体重复盘（今年）

| 现文本（逐字） | 判据 | 判定 | 改后文本 | 依据 |
|---|---|---|---|---|
| `2026-01-01 ~ 2026-09-07 · 250 条记录` | 冗 | 删（条数那半句） | `2026-01-01 ~ 2026-09-07` | 同第 15 条 |
| `共 250 条 · 最高 74.3 kg／最低 69.9 kg` | 冗 | 删＋换 | `最高 74.3 kg · 最低 69.9 kg` | 同上 |
| `vs 上一段等长区间`＋`均值差 vs 上一段等长区间` | 冗＋难 | 改 | `比前面一段`＋`前面同样长的 250 天：2025-04-26 ~ 2025-12-31` | 同上（今年这段的对照段跨到 2025 年，写明起止后读者才看得懂「前面一段」是谁） |
| `250`／`覆盖 250 天` | 冗 | 改 | `250 天`（副说明删） | 同上 |
| `9`／`月均值表 9 行` | 冗 | 改 | `9 个月`（副说明删） | 同上 |
| `期间均值 71.5kg` | 形式 | 改 | `期间均值 71.5 kg` | 口径 §3.3 |
| `月均值（共 9 个月）`＋表头 `月均值`＋行值 `71`／`70` | 形式 | 换形态 | 表注 `按月平均体重（单位 kg，共 9 个月）`＋表头 `平均体重`＋行值 `71.0`／`70.0` | 同第 29 条 |
| `（今年）变化 -3.9 kg（均值 71.5kg） · vs 上一段等长区间均值 -5.6kg` | 冗 | 改 | `平均每天少 16 克，在稳步往下走。` | 同上 |
| `📊 数据来源:weight_log ｜ 窗口 2026-01-01 ~ 2026-09-07 ｜ 250 条` | 难＋形式 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-01-01 ~ 2026-09-07 ｜ 共 250 条` | 口径 §3.1 |

### 第 40 条 · 体重复盘（自定义时间）

| 现文本（逐字） | 判据 | 判定 | 改后文本 | 依据 |
|---|---|---|---|---|
| `2026-09-01 ~ 2026-09-07 · 7 条记录` | 冗 | 删（条数那半句） | `2026-09-01 ~ 2026-09-07` | 同第 15 条 |
| `共 7 条 · 最高 70.4 kg／最低 70.3 kg` | 冗 | 删＋换 | `最高 70.4 kg · 最低 70.3 kg` | 同上 |
| `vs 上一段等长区间`＋`均值差 vs 上一段等长区间` | 冗＋难 | 改 | `比前面一段`＋`前面同样长的 7 天：2026-08-25 ~ 2026-08-31` | 同上 |
| `7`／`覆盖 7 天` | 冗 | 改 | `7 天`（副说明删） | 同上 |
| `期间均值 70.4kg` | 形式 | 改 | `期间均值 70.4 kg` | 口径 §3.3 |
| `（自定义时间）变化 +0.1 kg（均值 70.4kg） · vs 上一段等长区间均值 +0.1kg` | 冗 | 改 | `平均每天多 17 克，在往上走。` | 同上 |
| `📊 数据来源:weight_log ｜ 窗口 2026-09-01 ~ 2026-09-07 ｜ 7 条` | 难＋形式 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-09-01 ~ 2026-09-07 ｜ 共 7 条` | 口径 §3.1 |

### 第 44 条 · 看里程碑回溯

| 现文本（逐字） | 判据 | 判定 | 改后文本 | 依据 |
|---|---|---|---|---|
| `从历史最高 80kg 起算 · 共 495 条记录`（副标题） | 冗 | 改 | `每减 5 kg 记一次，记的是第一次达到那天` | 条数归页脚；`起算` 与卡片徽章同义；把这页**怎么读**说成一句话，替掉卡上的机器记号 |
| `5/10/15/20kg`（里程碑数卡副说明） | 难 | 删 | （删，事挪进副标题） | 内部记号不上屏（口径 §3.2） |
| `历史最高`卡副说明 `起算点`＋徽章 `基准` | 冗 | 合并 | 徽章 `起算点`，副说明删 | 同一件事说两遍，只留读者能懂的那一处 |
| `当前体重`卡徽章 `已减` | 冗 | 改 | `最新` | 这一格只说「这是最新一条记录」（状态）；方向词全页只留「总减重」那一处 |
| `总减重`卡副说明 `距历史最高 · 已减`＋徽章 `减重` | 冗＋难 | 改 | 徽章 `已减`，副说明删 | 副说明是值槽与徽章的重述；**一页只留一套方向词**（`已减／回涨／持平`），不再与 `减重／增重` 混用 |
| 列表行 `2025-11-27 · 减重 5kg · 75 kg`／右列 `距首条 210 天` | 难＋形式 | 改 | `2025-11-27 · 减重 5 kg · 当天 75 kg`／右列 `距第一条记录 210 天` | 数字与单位留空格（§3.3）；`当天` 点明 75 kg 是哪天的体重；`首条` 换读者话 |
| `共达成 2 个里程碑：减重 5kg（2025-11-27）、减重 10kg（2026-06-20）`（结论句） | 冗 | 改 | `最近一次达标在 79 天前，之后回涨了 0.4 kg。` | 逐条日期与档位列表行已经逐条列出＝百分百冗余；结论改说两件列表没说的事（离今天多久、达标之后往哪走） |
| `📊 数据来源:weight_log ｜ 全量至 2026-09-07 ｜ 495 条` | 难＋形式 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 全部记录（到 2026-09-07） ｜ 共 495 条` | 口径 §3.1（这页没有窗口，「窗口」那一段如实写「全部记录（到某日）」） |

**六页之外**：本件里还有第三形态「目标复核」（`today`／`date`／无参），不属于本票 6 词，**本票一字符未碰**（见第四节未做项 1）。

## 三、删掉的信息登记（不许丢）

| 删掉的原句 | 所在页 | 为什么算百分百冗余 | 这条事实现在的去处 |
|---|---|---|---|
| 副标题里的 ` · N 条记录` | 15／22／29／36／40 | 与页脚来源行重复（§3.1 把条数钉在页脚） | 页脚 `｜ 共 N 条`；另有「记录天数」卡的值槽 |
| 期间均值卡副说明前缀 `共 N 条 · ` | 22／29／36／40 | 同上 | 页脚 `｜ 共 N 条` |
| 期间均值卡副说明 `共 1 条 · 最高 70.4 kg／最低 70.4 kg`（单点那一档） | 15 | 单点时最高＝最低＝该卡值槽，复述零信息量 | 值槽 `70.4 kg`；卡副说明 `单点无变化` |
| 对照卡副说明 `均值差 vs 上周／上月／上一段等长区间` | 15／22／29／36／40 | 是同一张卡标签的复述 | 标签本身（`比上周`／`比上月`／`比前面一段`）＋新的对照区间副说明 |
| 记录天数卡副说明 `覆盖 N 天` | 15／22／29／36／40 | 满窗时就是值槽那个数 | 值槽 `N 天`；有缺口时另出 `窗口共 M 天`；缺口天数在页脚 `｜ 缺 K 天没记` |
| 月份数卡副说明 `月均值表 N 行` | 29／36 | 是值槽那个数的重述 | 值槽 `N 个月`；表格表注 `按月平均体重（单位 kg，共 N 个月）` |
| 结论句里的三处数字（`（本月）变化 …（均值 …） · vs … 均值 …`） | 15／22／29／36／40 | 与「期间变化／期间均值／比上一段」三张卡的值槽逐个重复 | 卡片值槽（同一个数，逐字一致）；结论改说卡片上没有的「快慢」 |
| 结论句 `共达成 N 个里程碑：…（逐条日期）` | 44 | 与列表行逐条重复 | 列表行（日期／档位／当天体重／距第一条记录天数）逐条列出 |
| 里程碑数卡副说明 `5/10/15/20kg` | 44 | 内部记号，读者读不出意思，且与副标题要说的事重复 | 副标题 `每减 5 kg 记一次，记的是第一次达到那天` |
| 历史最高卡副说明 `起算点` | 44 | 与同卡徽章重复 | 徽章改为 `起算点`（读者话） |
| 总减重卡副说明 `距历史最高 · 已减` | 44 | 与值槽、徽章两处重复 | 徽章 `已减`／`回涨`／`持平`（一页一套方向词） |
| 当前体重卡徽章 `已减` | 44 | 与总减重卡同一条事实 | 总减重卡徽章；本卡徽章改 `最新` |
| 页顶提示标题 `样本与口径`／`单点无变化：` 前缀 | 15 | 术语＋与卡片副说明重复 | 标题 `记录太少`；前缀删（副说明 `单点无变化` 仍在卡上） |

**没有丢掉的**：上面每一条的原句都在本表登记；无一条事实因删除而在页面上失去唯一出处。

## 四、机器读数

- **出页**（`node .scratch/t154/text-review/render-family.mjs --family review --out .scratch/t154/text-review/out-review`）：末行 `OK-RENDER D:\ilife\.scratch\t154\text-review\out-review`，`TALLY {"通过":6} TOTAL 6 HTML=6`。
  逐页字节（改前 → 改后）：15 `72873→72725`／22 `74021→73890`／29 `92783→92487`／36 `126700→126448`／40 `74281→74003`／44 `66460→66303`；**合计 507118 → 505856（−1262）**。
- **用例**（`node tooling/run-locked.mjs --ticket 482 -- node --test packages/skill-calorie/test/weight-review-335.test.mjs`）：
  - 采纳前量现场（无主件）：`RESULT: ticket=482 runId=6128eea5-d374-49a5-80ea-1c07e3c1604b waitedMs=0 exit=0`（`tests 9 / pass 9 / fail 0`）
  - 改后终态（补了 #482 两条新用例，共 11 条）：`RESULT: ticket=482 runId=e99ec6d1-e7c4-48bc-8c34-b33402acfc0d waitedMs=0 exit=0`（`tests 11 / pass 11 / fail 0`）
  - 另两次同结果读数为 `runId=20d25282-b6b3-482c-a0a8-dba5b3711f06`／`runId=5ab387da-df7c-47ed-a1f3-7f5074cea2cf`（均 `exit=0`）。
- **变异自证（两行机器读数）**：
  - 改坏（页脚全角冒号改回半角 `📊 数据来源:`）：`AssertionError: 本周 仍有旧句：📊 数据来源:`；`tests 11 / pass 10 / fail 1`；`RESULT: ticket=482 runId=d2ec3d4c-e587-4bfe-a660-65a3b6f14623 waitedMs=0 exit=1`。
  - 改回（还原到全角冒号）：`tests 11 / pass 11 / fail 0`；`RESULT: ticket=482 runId=e99ec6d1-e7c4-48bc-8c34-b33402acfc0d waitedMs=0 exit=0`。
- **无主测试件采纳**：`packages/skill-calorie/test/weight-review-335.test.mjs`（2026-09-14 13:02 写、`git status` 为 `??`、从未入库）采纳前先跑（上条 9/9 绿、断言有效：判别式三形态／窗口区间 pin 值／回溯语义／冻结 cli 与 data_source／路由 6 条），本票改文本后**同步收紧旧断言**（`vs 上周`→`比上周`、副标题去掉条数、`减重 5kg`→`减重 5 kg`、`从历史最高 75.5kg 起算`→`每减 5 kg 记一次`），并新增两条 #482 用例（旧句归零／结论不复述 与 零差值写法）。
- **看图**：`node .scratch/t154/shoot.mjs .scratch/t154/text-review/out-review .scratch/t154/text-review/shots-review` → `SHOT 6 files`；已用 `read_image` 逐张看 **15／29／36／44** 四张（改动最大的四页）：无省略号截断、无压字、无换行折断；副标题、卡片副说明、表注与页脚均一行放下。
- **编译**（`node tooling/run-locked.mjs --ticket 482 -- npx tsc -b packages/skill-calorie`）：
  - 本票改动首次全绿：`RESULT: ticket=482 runId=05e01603-6081-433d-97dd-2e6c45cde498 waitedMs=0 exit=0`。
  - 其后三次为红（`2c7a0d81`／`6a6dda7e`／`fda01cf6`），红点**全在别席在途件**（`src/weight/logReceipt.ts`／`src/weight/receipt.ts`／`src/weight/volatilityDoc.ts`，多为 `TS2554 Expected 2 arguments, but got 4` 与 `TS2304 Cannot find name 'reconcileDisclosure'`），本票文件零错误；TS 仍 emit，故上方页面与用例读数有效（字节变化可核对）。
- **GATE-RUN 声明（窗口 `2026-09-14T17:40:00Z` ~ `2026-09-14T17:51:00Z`，ticket=482 共 11 条 `RUN`，逐条登记；三段提交步骤（add／commit／push）都在窗口之后，按对账口径「提交后的 git 条目不受影响」不入窗）**：

GATE-RUN runId=6128eea5-d374-49a5-80ea-1c07e3c1604b cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"
GATE-RUN runId=df2a89dc-82f8-475b-98a2-519a7fbe1551 cmd="npx tsc -b packages/skill-calorie"
GATE-RUN runId=05e01603-6081-433d-97dd-2e6c45cde498 cmd="npx tsc -b packages/skill-calorie"
GATE-RUN runId=3cdc3403-6103-437c-b5d2-c411e859ca71 cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"
GATE-RUN runId=20d25282-b6b3-482c-a0a8-dba5b3711f06 cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"
GATE-RUN runId=2c7a0d81-be13-45ef-8799-ee7ea06ddae9 cmd="npx tsc -b packages/skill-calorie"
GATE-RUN runId=5ab387da-df7c-47ed-a1f3-7f5074cea2cf cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"
GATE-RUN runId=6a6dda7e-0a47-4389-b01b-dd7655c88837 cmd="npx tsc -b packages/skill-calorie"
GATE-RUN runId=d2ec3d4c-e587-4bfe-a660-65a3b6f14623 cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"
GATE-RUN runId=fda01cf6-883a-43a8-a5ea-266fa50349c8 cmd="npx tsc -b packages/skill-calorie"
GATE-RUN runId=e99ec6d1-e7c4-48bc-8c34-b33402acfc0d cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"

GATE-RELAX flag=--allow-nonzero reason=窗口内 6 条 exit≠0 如实登记不隐藏：`df2a89dc`／`2c7a0d81`／`6a6dda7e`／`fda01cf6` 是别席在途件把 `npx tsc -b packages/skill-calorie` 弄红（`logReceipt.ts`／`receipt.ts`／`volatilityDoc.ts`，非本票文件）；`3cdc3403` 是本票新用例自身的种子 bug（富种子到 09-06 为止，我把「本周」当天写成 09-07 走了空窗阻断，当场修好）；`d2ec3d4c` 是变异自证故意改坏（已还原，见上）。

- **格式门自检**：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t482-文本审查-review.md --ticket 482 --log .scratch/locks/gate-runs.log --since 2026-09-14T17:40:00Z --until 2026-09-14T17:51:00Z --allow-nonzero` → `EXIT=0`、`RESULT: matched=11/11 auditEntries=4639 scoped=11 undeclared=0`、`gate-audit: PASS`。
  窗口口径的来历（如实登记两次读数）：先用 `--until 2026-09-14T17:55:00Z` 跑过一次，`RESULT: matched=11/11 scoped=14 undeclared=3`、`FAIL: 反向对账：窗口内有 3 条无人声明的 RUN 条目`——那 3 条正是**三段提交步骤**（`git add`／`git commit`／`git push`）；提交步骤不是 build／test 门，按对账口径「提交后的 git 条目不受影响」，把窗口收到提交前一刻（`17:51:00Z`）后 11/11 全中、零未声明。

## 五、未做项与下一手缺什么

1. **同件第三形态（目标复核）未过**：`src/weight/review.ts` 里 `today`／`date`／无参走的目标复核形态，其页脚仍是 `📊 数据来源:weight_log ｜ 复核日 … ｜ 当前 … vs 目标 …`（半角冒号、未按 §3.1 统一）。它**不属于本票 6 词**，本票按「只动 6 词」未碰一字符 —— 但**该件归本票写集**，也就是说这一形态现在**没有归属票**（#486 票面写明「本票只动 `log.ts`」）。**下一手缺什么**：需要编排者裁定这一形态归哪张票（或并入 #486 扩写集），并把它按 §3.1／§3.2 一并收口。
2. **告警线台账未同步（本票不动，按派单归编排者）**：`packages/skill-calorie/AGENTS.md` 台账里 `src/weight/review.ts` 的「当场实测」仍是 600，实况 **647**；`node packages/skill-calorie/scripts/check-warning-line.mjs` 现为红：`OVER src/weight/review.ts LF=647 已超线…` ＋ `RED 台账陈化：src/weight/review.ts 台账=600 实况=647`（`RESULT: 57/60`）。同红还有别席两件（`history.ts` 683→741、`compare.ts` 558→571），修法是编排者跑 `--sync`。
3. **复制日志第 3 段的来源写法两族未统一**：本件仍是 `COPY_SOURCE = 'weight_log（体重复盘）'`（机器面，不在本票可见文本范围，未动）；#480 在 `history.ts` 里已把同一段改成 `体重记录（库 · 表） ｜ 窗口 … ｜ 共 N 条`。要不要统一，需要编排者一句话。
4. **空态句 `本窗无体重记录` 未改**：它有多个空态位共用、且是全族写法（改它要整族一起动，属族级裁定），本票只在顺手改写的句子里把 `本窗` 换成了「这段时间」。
5. **编译门终态为红（非本票所致）**：见第四节；本票文件零错误，但「编译全绿」这条验收要等别席在途件落地。


---

## 六、对抗审查整改（2026-09-15）

独立审查席判 **PASS 93 分**，8 条缺陷逐条整改；由**编排者接手提交**（该票整改席在写本节前因后端故障中断，
源码改动已落盘并经复核：编译 exit 0、用例绿、关键词回扫 0 命中）。

| 缺陷 | 严重度 | 整改后 |
|---|---|---|
| 15 页结论块「只有 1 条记录」是同屏第 3 次 | S2 | 删掉，结论只说「看不出这段时间是涨是跌」 |
| `期间均值` 与同页 `平均值／平均体重` 两套词（卡名＋图上横线标签 11 处） | S2 | `期间平均值`／`平均值 70.4 kg` |
| 记录天数卡徽章 `满窗`（内部口径词） | S2 | `整段都记了` |
| 月份数卡与表注同数两槽 | S3 | 表注删「，共 4 个月」 |
| 结论句 `…，在往上走。`（方向＝徽章第 3 次） | S3 | 删「在往上走」 |
| 期间变化副说明 `首末 70.3 → 70.4 kg` | S3 | `开头 … → 最后 …` |
| 单点页副说明与徽章同说一件事 | S3 | 徽章删；副说明改人话 |
| 基准段说明塞 KPI 卡折两行 | S3 | 移出卡外一行小字 |

**编排者再收一轮**：单点页「期间变化」副说明原为 `单点无变化`（圈内词）⇒ 改 `只有 1 个点，没法算变化`，
全页可见文本不再出现「单点」（用例断言同步收紧成「整页不含『单点』」）。

**读数**：`weight-review-335` 全绿；六族关键词回扫 32 词全 0；出页 `TALLY {"通过":6}`。提交：`577c88f`。

## 七、形状化与手机端（#504 · 2026-09-15）

负责人六条第 1／2／5 条与共同口径 `.scratch/t154/text-review/口径-UI.md`（同型先例＝
`packages/skill-calorie/src/render/reviewDocsCss.ts` 件头）。形状词汇全部用同目录 `weightUi.ts`
（`weightUiCss()`／`windowStrip()`／`factStrip()`／`pairStrip()`／`chip()`／`note()`／`bulletList()`／`verdict()`），
两族各自把 `weightUiCss()` 放进整页装配 `parts` 的**第一项**。

### ① 逐页「原来串什么 → 改成什么形状」

| 页 | 原来（`·`／`；` 串） | 改成 |
|---|---|---|
| {A} | （逐页表见下） | |

**复盘族 6 页（第 15／22／29／36／40／44 条）**

| 页 | 原来串什么 | 改成什么形状 |
|---|---|---|
| 15／22／29／36／40 | 页顶「记录太少」提示的详情：`曲线上只有这一个点，图上那条横线就是这个点的平均值；一条记录谈不上趋势` | `notice()` 只报「这段时间只记了 1 条」＋ `bulletList()` 两条逐条成行（`；` 串拆开） |
| 22／29／36／40 | 「期间变化」卡副说明 `开头 70.3 → 最后 70.4 kg`（`→` 串） | 撤到卡下 `periodStrip()`：一对量走 `pairStrip()`（两端各一枚加粗值、中缝箭头） |
| 22／29／36／40 | 「期间平均值」卡副说明 `最高 70.4 kg · 最低 70.3 kg`（一个 `·` 串两条事实） | 同一条 `periodStrip()` 里两枚「标签 ＋ 值」（`最高`／`最低`） |
| 44 | 里程碑每行 `2025-11-27 · 减重 5 kg · 当天 75 kg`（**一行三个 `·`**） | 三个槽各自成形：`left`＝档位`减重 5 kg`、`main`＝日期、`right`＝`当天 75 kg` |
| 全部 6 页 | 卡片 `detail` 槽里塞多件事实（那一槽由公共层 `esc`，形状落不进去） | 卡内只留**一句指引**，事实搬到卡下的形状块（同族 `history.ts:progressStrips` 同款做法） |

**波动族 5 页（第 5／13／20／27／34 条）**

| 页 | 原来串什么 | 改成什么形状 |
|---|---|---|
| 5／13／20／27／34 | **卡②副说明**：`警戒线 ±0.098 kg · 注意线 ±0.073 kg · 波动幅度 0.049 kg`（三件事一行 `·`，本族的样板处） | 卡下 `thresholdStrip()`：三枚「标签 ＋ 值」（`波动幅度`／`注意线`／`警戒线`）同排，窄屏塌一列；卡内只写「两条线各是多少，看这张卡下面那条」 |
| 5／13／20／27／34 | 卡④副说明 `近 7 天：超过注意线 0 天 · 超过警戒线 5 天` | 卡下另一条事实条里的两枚「标签 ＋ 值」（`超过注意线 近 7 天 0 天`／`超过警戒线 近 7 天 5 天`）；卡内只写「近 7 天越线的天数」 |
| 5／13／20／27／34 | 结论块 `体重很稳：整体离散度 0.05 kg，近期没有超过注意线的天；2026-09-07 比平均线高 0.03 kg，在正常范围内。`（`；` 串判决与读数） | `verdict(判语)`（一句话）＋ `note(今天那条读数)`（日期＋当天体重＋偏离＋越没越线）；`renderDisclosure` 的 `contentHtml` **不再套 `<p>`**（`<p>` 里塞不下块级件） |
| 34（只看异常点读法） | 页顶提示 `这一页只列超过波动带的点（共 N 个），不出两张曲线图；要看整图请说…` | 提示只报「列了点什么」＋ `bulletList()` 两条（「这一页不出两张曲线图」／「要看整图，请说…」） |

### ② 删掉的信息登记（若有）

**没有删任何事实**。逐条去处：

- `距第一条记录 N 天`（第 44 条每行）：**留在右列**（`elapsedDays` 在别处没有去处，逐行照给；曾试过撤到「序差」口径，判定为丢事实，已收回）。
- `最高`／`最低`／`开头`／`最后`（窗口页）：从「期间平均值」卡与「期间变化」卡副说明**撤到卡下 `periodStrip()`**，
  三个数一个不少（`开头 70.5 kg → 最后 70.4 kg`／`最高 70.5 kg`／`最低 69.9 kg`）。
- `波动幅度`／`注意线`／`警戒线`（波动页）：从卡②副说明**撤到卡下 `thresholdStrip()`**，
  且 #485 D1 钉的那句 `波动幅度 <σ> kg` 仍逐字在同一处（`weight-volatility-336.test.mjs:140` 那条判据照旧绿）。
- 页头副标题、页脚口径行、复制区：**一行未动**（眉标整族已删，本轮不出）。

### ③ 分隔符回扫读数（逐页）

工具：`python .scratch/t154/text-review/scan-separators.py --dir <出页目录>`（交付包口径，含 `<title>`）。

| 出页目录 | 回扫读数 |
|---|---|
| `.scratch/t154/text-review/ui482/out-review`（6 页） | **6 页共 6 处**，逐页 1 处，全部是 `<title>卡路里·体重</title>` |
| `.scratch/t154/text-review/ui482/out-volatility`（5 页） | **5 页共 5 处**，逐页 1 处，同样是文档标题那一处 |

**正文命中数＝0**：把 `<title>` 剔掉再数（脚本 `.scratch/t154/text-review/ui482/scan-body.py`，
日志 `scan-body-*-final.log`）——两族**11 页逐页 0 处**（`·` 与 `；` 各 0）。

页题 `卡路里·体重` 是**文档名**（`plateDocs.ts:29` 的 `DOC_TITLE`，全仓 58 页逐字同形），
按口径 §二 的允许项处理：**不动它**，两族交付页的逐页读数是 1 处（全在 `<title>`）。

**改动前基线**：复盘族 6 页 15 处（`·` 14 ＋ `；` 1）、波动族 5 页 25 处（`·` 20 ＋ `；` 5），
合计 40 处 → 改动后**正文 0 处**（日志 `scan-*-before.log`／`scan-*-final.log`）。

### ④ 手机端探针读数（`SW`／`VW`／越界元素）

工具：`node .scratch/t154/text-review/mobile-frame.mjs probe 390 2400 "<产物 html 绝对路径>"`（390px 真视口 iframe 壳）。

| 页 | 读数 |
|---|---|
| 波动 05 看体重稳不稳（增强版） | `SW=390 VW=390 || 无溢出` |
| 复盘 15 体重复盘（本周） | `SW=390 VW=390 || 无溢出` |
| 复盘 44 看里程碑回溯 | `SW=390 VW=390 || 无溢出` |
| 波动 27 看最近 180 天波动 | `SW=390 VW=390 || 无溢出` |
| 波动 34 看波动异常点 | `SW=390 VW=390 || 无溢出` |

截图并看图（`read_image` 真看）：桌面 1200 档 22／05 两页＋手机 390 档 15／44／05／27 四页。
看图当场改的两处：

1. **卡下事实条改成竖排会把标签与值拉到 960px 的两头**（桌面 1200 截图实测：中间空空荡荡、读不成对）
   ⇒ 两族的卡下条子都保持**横排**（窄屏由 820 段 ③ 自然塌一列）。
2. **里程碑行的日期进 `left` 那 44px 列，在 390px 下折成两行**（`2025-`＋`11-27`，手机截图抓到）
   ⇒ 槽位改成 `left`＝档位、`main`＝日期、`right`＝当天体重；把三条读数一起塞进 `main` 的那一版
   被 `nowrap` 截成省略号（`距第一条记录 415…`），也一并否掉。

### ⑤ 用例与变异读数

| 判据 | 读数 |
|---|---|
| 持锁编译 `npx tsc -b packages/skill-calorie` | 我的三件（`review.ts`／`volatility.ts`／`volatilityDoc.ts`）**零错**；全程只有别席在途件的红（`weightCompare2.ts`／`homeDocs.ts`／`logReceipt.ts`／`workout/write.ts`，非本票） |
| 持锁用例（本族两件） | runId `42adbdf3-e569-47ee-b540-49f879084845`：`tests 21／pass 21／fail 0`（绿） |
| 变异：把波动页卡②那行形状改回 `·` 串 | runId `f3b1b342-2207-4628-82c8-ac3b3e369c58`：`pass 19／fail 2`（**必红**）——红在 ①`可见文本里「波动幅度」只许 1 处（卡②说明）`②`正文仍有 `·`` |
| 还原 | runId `42adbdf3-…`（同上，绿） |
| 出页 | `TALLY {"通过":6}`（复盘）＋`TALLY {"通过":5}`（波动），两族全 `OK`、逐页 `exit=0` |
| 告警线门 | 本票两项已同步进台账（`review.ts` 669→724；`volatilityDoc.ts` 392 **新挂号**）；门仍红在别席在途件（`history.ts`／`compare.ts`／`trendMiscPortDocs.ts`／`workout/write.ts`／`diet/nutritionPortDocs.ts` 的陈化），**本票两件零红** |

新增/收紧的判据（两件各一条 #504 用例，把形状钉住）：

- `weight-review-335.test.mjs`：正文零 `·`／`；`（剔 `<title>` 后逐页数）、形状是真 DOM（不吃 `&lt;div class=&quot;wui`）、
  卡槽与行槽零 HTML、`@media (max-width:820px)` 与触摸面两条随页到场、`periodStrip` 逐枚形状到位、
  里程碑三槽逐槽到位、单点页不出首末对。
- `weight-volatility-336.test.mjs`：正文零 `·`／`；`、卡②指向卡下条子、条子三枚「标签 ＋ 值」与 σ／两线同数、
  结论 `verdict` ＋ `note` 两件到位、只看异常点那页同样带形状与 820 段、`toVisible` 之上新增 `bodyText()`（剔文档标题）。

### ⑥ 未做项与下一手缺什么

- **别席在途件**：编译与告警线门的残余红全在别席在途件（清单见 ⑤），本票不碰、也不代改。
- **本票在途项**：无。11 页逐页看过的两处形态问题（卡下条子竖排、里程碑行槽位）都已在本轮改掉并复看。
- **下一手**：交付包（`.scratch/t154/delivery/`）由编排者重出；本票只出页到 `.scratch/t154/text-review/ui482/`。


## 八、设计视角审查整改（#510，2026-09-16）

**本族整改完成**（逐条「改了／为什么没改」见下）。依据：整改单 `.scratch/t154/text-review/fix-brief-uireview.md`
（八节）＋审查席回执与四份读数 `.scratch/t154/text-review/rev-ui/`。本席出页与读数在
`.scratch/t154/text-review/rev-fix/`（`out-<族>`／`out-all`／`*.log`／`probe-fix-readings.py`／`readings-fix.txt`）。

### 八.1 第二节四类硬读数（复跑）

| 类 | 整改前（审查席实测） | 整改后（本席复跑） | 判据 |
|---|---|---|---|
| ① 硬编码色值 | 页内件面 6 处自造：`#e7f8ee/#1a7a3a`、`#fff4e5/#a05a00`、`#ffeceb/#c0392b` | 页内件面只剩 `#fff5e0`／`#a25b00`——**逐值等于冻结正本** `base-render/src/style.ts` 的 `statusBadge` warn；`ok`／`danger` 两档连类带值删（58 页里 0 页用过＝死 CSS，`chip()` 类型同步收到 `warn`／`plain`） | PASS |
| ② 字号阶梯 | 5 档 `{12, 12.5, 13, 14.5, 15}px` | **3 档**：`12px:456`／`13px:228`／`15px:171`（`.wui-bullets li` 12.5→12、手机档 `.wui-verdict` 14.5→15） | PASS |
| ③ 死 CSS | `wui-strip-note`／`wui-strip-v`／`wui-chip-ok`／`wui-chip-danger` 各 0 页 | 声明 22 个 `wui-*` 类、**0 页用到 0 个**：`-note` 2 页、`-v` 17 页真用上，`-ok`／`-danger` 删 | PASS |
| ④ 同屏事实 ≥3 | **51/58 页** | **7/57 页**（第 48 条归 #436 未纳入本批；残留 7 页全在写集外的 `logReceipt.ts`／`receipt.ts`） | 本族 0 页 |

复跑命令（口径与审查席四脚本同源，源目录换 `PROBE_DIR`）：

```
python .scratch/t154/text-review/rev-fix/probe-redundancy.py     # → .scratch/t154/text-review/rev-fix/redundancy-after.txt（等价于 rev-ui/probe-redundancy.py）
python .scratch/t154/text-review/rev-fix/probe-css.py            # → .scratch/t154/text-review/rev-fix/css-after.txt
python .scratch/t154/text-review/rev-fix/probe-fix-readings.py   # → .scratch/t154/text-review/rev-fix/readings-fix.txt（色值／字号／死 CSS／形状槽顿号四类一次出）
```

其余三面读数（审查席已判达标、本轮未推翻）：间距页内件面 12 档无孤值｜圆角 2 档＋零 box-shadow｜
手机 390 真视口 **57/57 页 `SW=390 VW=390`**（`.scratch/t154/text-review/rev-fix/mobile-probe.log`）。

### 八.2 本族（复盘族 6 页）逐条

- **S3 复盘族结论块未走 `verdict()`（页 15/22/29/36/40；`wui-verdict` 在页 15 命中 0）→ 已改**：
  `renderDisclosure` 的正文由裸 `<p>` 换成 `verdict(v.summary)`——六族结论块同形；结论句一字未改
  （`v.summary` 仍是本族唯一出处）。
- **S2 关键数字三处（页 22／40 的 `70.4 kg`）→ 已改**：期间事实条的「最高／最低」与首末两端**同值时不再单独报**
  （本窗一路上行时，同一个数原来在「期间平均值」值槽、两端值、最高三处各印一遍）；
  路径中途出过更极端的值时那两行照旧出（新增用例钉这一条：`#510 期间事实条：极端值落在窗口中间时照旧出`）。
  ⇒ 组内不再出现重复读数，`70.4` 收到 2 处（值槽 ＋ 两端值）。
- **S2 页 15 退化窗口条（`2026-09-07 ~ 2026-09-07`）→ 已改**：副标题与页脚来源行改走 `windowText()`
  ——单日窗写 `2026-09-07（单日）`（与 `weightUi.ts` 那条 `windowStrip()` 退化分支同一口径）；
  复制载荷里的「窗口」字段仍写原始区间（机器面，一格一问）。
- **同类项（审查席 S3 只点了页 05，本族同一件）：期间变化卡副说明的指路牌**「这一段的上下界看卡下那条」→
  **已改**：整条撤掉，卡下那条形状自己会说话（事实没有被移出卡再叫读者去别处看）。
- **未改（为什么）**：无本族残留项；页 44（看里程碑回溯）本就不出结论块，不涉及。

### 八.3 自证

- 持锁编译：`node tooling/run-locked.mjs --ticket 510 -- npx tsc -b packages/skill-calorie` → **exit=0**
  （`.scratch/t154/text-review/rev-fix/tsc-2.log`）。
- 出页六族全 `OK`：history 18／compare 17／review 6／volatility 5／plate 2／receipt 11（含 08／30 复用）
  ——`TALLY {"通过":n}` 且 `OK-RENDER`，日志 `.scratch/t154/text-review/rev-fix/render-*.log`。
- 用例（持锁）：五件体重族测试 **43/43 通过**（`tests 43 / pass 43 / fail 0`，`.scratch/t154/text-review/rev-fix/tests-final.log`；
  其中 `weight-history-333` 自打 `RESULT: 18/18`）。旧断言随新文案同步收紧（h1、徽章、里程碑副说明、
  单日窗副标题、期间事实条、卡②徽章与指路牌、今日盘形状件与卡网格）。
- **变异两行**：把 `history.ts` 的节奏／首末事实条改回 `·` 串 ⇒ 必红
  （`runId=0270d68d-6937-4f6d-8de4-cc1aaa0b579a`，`fail 1`：`看上周体重 可见面 · 命中数不对`）；
  改回 ⇒ 必绿（`runId=800175d3-1eec-43a3-8484-38a5ec5dca23`，`RESULT: 18/18`／`pass 3`）。
  日志 `.scratch/t154/text-review/rev-fix/mut-red.log`／`.scratch/t154/text-review/rev-fix/mut-green.log`。
- 手机 390 真视口：`node .scratch/t154/text-review/mobile-frame.mjs probe 390 2400 .scratch/t154/text-review/rev-fix/out-all` →
  **57/57 页 `SW=390 VW=390`**；截图两档 `.scratch/t154/text-review/rev-fix/shots-desktop/*.png`（1200）与
  `.scratch/t154/text-review/mobile/*.png`（390），审查席点名的 04／08／22／30／45／47／53 逐张看过。
- **告警线台账**：`packages/skill-calorie/AGENTS.md` 台账里本票六件的「当场实测」列已随实况（别席同步器在场）；
  本席复跑 `node packages/skill-calorie/scripts/check-warning-line.mjs` 的残余红只剩
  `src/render/trendDocs.ts 台账=994 实况=996`（**别席在途件**，不在本票写集，不代改）。
- 未动（照整改单第八节）：命令与路由／`shared/*`／`packages/base-render/*`（共享层一行未碰——间距 16 档、
  字号 18 档、数据表 1200 下两列相距 ~1000px、图表末点标签压均值虚线／`70.4kg` 少空格，均归 #424）／眉标／
  复制区形态与深色瞬时 toast／第 48 条整页（归 #436）／`.scratch/t154/delivery/`（只有编排者重出交付包）。
- **写集外残留（本票不改，供编排者派下一手）**：`weight/logReceipt.ts` 与 `weight/receipt.ts` 两件里的
  7 页 ★（01／09／16／17／23／24／31）与 3 处顿号——见 `t483-文本审查-receipt.md` 八.2 的逐条表。

---

## 九、W2 第二轮复验（2026-09-15，本席）

**事由**：首轮（§二～§五）＋ 对抗整改（§六）＋ #504（§七）＋ #510（§八）之后，
同一件又经公共层 `1712317`（口径行 `｜` 分隔改形状化）与眉标整族删除两批改动，
按三条判据重验 6 页。**本节只追加，不改上文一字。**

### 九.1 逐页复验（读者＝不懂技术的项目负责人）

| 页 | 判据①冗余 | 判据②人话 | 判据③形态 | 处置 |
|---|---|---|---|---|
| 15 本周（单日） | 副标题只留窗口、条数归页脚；结论只说涨跌 | `记录太少`／`这段时间只记了 1 条`／`只有 1 个点，没法算变化` | 提示＋分条、卡片＋`verdict` 结论 | 通过，不动 |
| 22 本月 | 同上 | `比上月`／`整段都记了`／`平均每天多 17 克` | 卡下事实条＋对照段小字 | 通过，不动 |
| 29 最近 90 天 | 同上；月份数只留卡片一处 | `比前面一段`＋写明对照区间 | 月表 `平均体重`＋一位小数 | 通过，不动 |
| 36 今年 | 同上（对照段跨到 2025 年已写明起止） | 同上 | 同上（最低 69.9 kg 中途值照旧出） | 通过，不动 |
| 40 自定义时间 | 同第 22 条 | 同第 22 条 | 同第 22 条 | 通过，不动 |
| 44 里程碑回溯 | 副标题说读法、结论只说两件列表没有的事 | `每减 5 kg 记一次`／`起算点`／`最新`／`已减` | **结论块原来裸 `<p>`，窗口五页已是 `verdict()`** | **改 1 行**：`verdict(v.summary)`（句子一字未改；附带 `esc` 转义） |

其余候选逐条核实为**不动**：`基准`／`分月` 徽章（对抗审查 93 分已审过，非百分百冗余）；
15 页 `持平`×2（期间变化 vs 比上周，答的是两道题）；15 页 `警告` 徽章（通用回落词，
与卡片值槽／提示条说的是 severity／count／解释三件事）；复制区形态、眉标（已删）、第 48 条（`src/goal/` 零改动，
`git status` 已验干净）。

### 九.2 删掉的信息登记（不许丢）

**本次零删除**：`review.ts` 改 1 行只换结论块容器（`<p>` → `verdict()` 同句）；
测试件改的是断言写法（§九.3），页面事实零增减。无登记项。

### 九.3 公共层改动同步（测试断言，不放宽）

基线重跑发现 `weight-review-335` 12/13 红（`runId=bb3ba61f`，`本周 页脚未统一句式`）：
公共层已提交件 `1712317` 把 `renderCaliberLine` 的 `｜` 分隔改成各段独立 `<span>`，
HTML 字面不再含 `｜`，旧断言逐字匹配落空。**读者可见文本一字未变**
（`📊 数据来源：体重记录 窗口 … 共 N 条`，`verify-text.py` 十项全 0）。
处置（写集内）：两处页脚断言改判**可见文本**（人话来源 ＋ `共 N 条` 同页同序），
意图与旧断言逐字一致；另加一条钉住本次源码改动（44 页结论含 `<p class="wui-verdict">最近一次达标`）。
两份契约件（`weight-wake-58-339` 60/60、`render-t41` 10/10）判据一字未动、双绿。

### 九.4 机器读数

- **编译**（真实入口，`node node_modules/typescript/bin/tsc -b packages/skill-calorie`，`npx tsc` 在本机是假出口）：
  改前 `runId=f7618751-b80a-45c9-8b86-2ed9f2443a29 exit=0`；
  改后 `runId=f9cc368e-eeac-4e2d-84ac-93710329947d exit=0`；
  变异轮 `runId=772d1a58 exit=0`；还原后 `runId=80aede46 exit=0`。
- **靶向用例**（`node --test packages/skill-calorie/test/weight-review-335.test.mjs`）：
  基线红 `runId=bb3ba61f-b026-... exit=1`（`tests 13 / pass 12 / fail 1`，§九.3 那一条）；
  改后绿 `runId=4ae5379e-525d-441b-a82d-43a62d0e926f exit=0`（`tests 13 / pass 13 / fail 0`）；
  还原绿 `runId=e7d3f992-fac4-4591-9617-2e7b3f749019 exit=0`（`tests 13 / pass 13 / fail 0`）。
- **契约件**（判据未动）：`weight-wake-58-339` `runId=2cd044d8-e970-4f53-9a4f-07aa875217ac exit=0`
  （`tests 60 / pass 60 / fail 0`）；`render-t41` `runId=500c3f48-06bd-4e81-b45f-88bb954faca2 exit=0`
  （`tests 10 / pass 10 / fail 0`）。
- **变异自证**：改坏页脚全角冒号→半角 ⇒
  `AssertionError: 本周 仍有旧句：📊 数据来源:`，`tests 13 / pass 12 / fail 1`，
  `RESULT: ticket=482 runId=a2548534-0195-473f-b305-38fa0d5bc7b3 waitedMs=0 exit=1`；
  还原 ⇒ `tests 13 / pass 13 / fail 0`，
  `RESULT: ticket=482 runId=e7d3f992-fac4-4591-9617-2e7b3f749019 waitedMs=0 exit=0`。
- **出页**：`TALLY {"通过":6} TOTAL 6 HTML=6` ＋ `OK-RENDER`（44 页 `74369→74389` 字节，
  其余 5 页字节不变）；截图 `SHOT 6 files`，本席用眼睛看过 44／15 两张（结论加粗同形、
  里程碑三槽无折行无省略号、单点图平均线标签可读）。
- **台账**：`review.ts` LF=754（台账值 754，一行换一行，零陈化；`AGENTS.md` 未碰，归编排者同步）。
- **GATE-RUN 声明（本窗，ticket=482）**：

GATE-RUN runId=c8cf7c88-74c4-4655-8118-ed38c6b40df9 cmd="npx tsc -b packages/skill-calorie"
GATE-RUN runId=f7618751-b80a-45c9-8b86-2ed9f2443a29 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=bb3ba61f-2a79-49c0-b026-030bff946e27 cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"
GATE-RUN runId=2cd044d8-e970-4f53-9a4f-07aa875217ac cmd="node --test packages/skill-calorie/test/weight-wake-58-339.test.mjs"
GATE-RUN runId=500c3f48-06bd-4e81-b45f-88bb954faca2 cmd="node --test packages/skill-calorie/test/render-t41.test.mjs"
GATE-RUN runId=f9cc368e-eeac-4e2d-84ac-93710329947d cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=4ae5379e-525d-441b-a82d-43a62d0e926f cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"
GATE-RUN runId=772d1a58-2261-4b5a-bb90-754030db70e8 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=a2548534-0195-473f-b305-38fa0d5bc7b3 cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"
GATE-RUN runId=80aede46-fc0d-4d5d-ba52-6d4f2153cd1c cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=e7d3f992-fac4-4591-9617-2e7b3f749019 cmd="node --test packages/skill-calorie/test/weight-review-335.test.mjs"

首条 `c8cf7c88` 是旧入口（`npx tsc`）的读数，如实登记但不作门禁依据（AGENTS.md 已写明该入口是假出口）；
门禁只认真实入口的 `f7618751`（改前）／`f9cc368e`（改后）。

### 九.5 未做项与下一手

1. `基准`／`分月`／单点页双 `持平`（§九.1）：三轮已审过的判断，本席按「非百分百冗余不动」，
   如编排者要收紧需另开票（改徽章词要同步两份契约件的冻结断言，不属本票）。
2. `weightUi.ts` 未碰（`verdict()` 复用现成件）；`shared/*`／`base-render/*` 一行未读只用；
   `.scratch/t154/delivery/` 未碰（编排者重出）；`git push` 未做（总工统一收口）。
3. 日志：`.scratch/t154/w2-482/`（`tsc*.log`／`test-*.log`／`mut-*.log`／`render*.log`／`shoot.log`／`claim.md`）。
