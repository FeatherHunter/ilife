# #315r 对抗式复核报告 —— 场景 02 饮食 24 键搬迁

- 复核席：`#315r`（对抗者立场：默认交付有问题，去找出来）
- 被复核：代码提交 `86786e8`「feat(315): 场景 02 饮食 24 键搬进 src/diet/」（17 件 ＋694/−503）＋ 证据提交 `3f9c131`（改正了 `f1b0e60`／`3971fc8`／`8d3aa98`；以 `3f9c131` 为准）
- 复核时点 HEAD：`3f9c131`（分支 `master`）；改前基线：`86786e8^` ＝ `9140f39`（＝证据自称 BASE-SHA）
- **判定：FAIL（86／100）。S1＝0、S2＝3、S3＝4。**
  代码实质面**全绿**（声明面／分派面／跨片删除／行为／免费判据四维全过，且是本席独立复跑所得）；扣分全在**证据件的三处事实**与**一处不成立的机器保证断言**。

---

## 一 · 维1 声明面（24 条／六字段／镜像一致）——**PASS**

**方法**：不复用被审对象产出的任何脚本结论；本席自写 `.scratch/t315r/dim1-mirror.mjs`，从四个权威面各自解析再互比。

```
DECLS rows=24 uniqKeys=24 fieldViolations=0
MIRROR keys.ts write=35 read=66 mismatches=0
COMBOS-YAML total24 missing=0
COMBOS-YAML fieldMismatch=0
REGISTRY dietImport=true
CLAIM-24-SET-EQUAL=true
WAKEWORD-MISSING=calorie.view.dedupe
```

- `src/diet/commands.ts` 恰 **24** 条，键两两不同；`kind`／`key`／`shape`／`title`／`example`／`run` 六件事**逐条齐**（0 缺）；仅 `calorie.view.dedupe` 缺 `wakeWord`——与 #323 起的「`wakeWord` 可缺」口径一致。
- `src/cli/keys.ts`：写表 35 ＋ 读表 66 ＝ **101** 条（与生成器自报数一致）；24 键的 `shape`／`title` **逐条相符，0 处不符**。
- `packages/base-combos/combos.yaml`：24 键全在、`title` 全对。
- `src/cli/registry.ts` 引 `../diet/index.js` 的 `DIET_COMMANDS` 并入 `SOURCES`。
- 24 键集合与提交信息自述的 24 键**逐字相同**。
- **本席自设探针**：全仓 17 个声明文件（6 个能力 `commands.ts` ＋ 11 个 `legacy/scene-NN.ts`）里，24 键**每键恰 1 处声明**，0 重复、0 缺失：

```
C1 声明文件扫描面=17 件；24 键有声明处=24/24 声明处≠1 的键=0
```

**附（证据两处 wakeWord 数字，独立复核都成立）**

```
A1 改前 11 个分片声明=56 缺wakeWord=4
A2 改前 scene-02 缺wakeWord=calorie.view.dedupe        ← 场景 02 仅此 1 条
改前 6 个能力声明文件：声明 45 条 缺 wakeWord=6
⇒ 全仓 101 条声明里缺 wakeWord ＝ 4＋6 ＝ 10 条
```

---

## 二 · 维2 分派面（唯一实装／净面／片文件仍在）——**PASS**

```
EVID-DECL scene-02.ts@pre = 24  非24键=0  键集与票面24一致=true
DISPATCH cmd_read.ts: case@pre=41 本票case@pre=11 删行=151 删case=11 外来case=0
DISPATCH write.ts:    case@pre=24 本票case@pre=13 删行=245 删case=13 外来case=0
   删case集合==票面24键∩本文件case=true（两件皆 true）
CASE-SITE 24 键里还有 case 标签的=11（全在 src/output.ts 的「写回执字段」switch，非处理体）多处实装=0
IMPL 24 键 run 目标不在 src/diet/ 唯一者=0
```

- 24 键在 `cmd_read.ts`／`write.ts` 的老 `case` **删净**，且**删掉的 case 集合恰等于「票面 24 键 ∩ 该文件原有 case」**——**没连带删到别票的 case**（`外来case=0`）。
- `src/cli/legacy/scene-02.ts` **文件仍在**（12 行，数组清空），24 键零残留。
- 24 键的 `run` 目标函数各自**唯一**落在 `src/diet/`。

**附（证据 §3 两个数字，独立复核都成立）**

```
[pre ] cmd_read.ts nodeLines=799 switches=2 cases=41  主 switch(key) 内 case=35
[post] cmd_read.ts nodeLines=653 switches=2 cases=30  主 switch(key) 内 case=24
```

⇒「1146 → 799 行」「35 → 24 个 case（−11）」**都对**。

---

## 三 · 维3 跨片删除的正确性（本票最容易出事的地方）——**PASS**

不只核「删的行属于谁」，还核**全仓路由记录多重集在提交前后是否守恒**（无丢失／无新增／无重复／无改写）。

```
DEL scene-02+scene-10: totalDeletedLines=80 recordLines=77 nonRecordLines=3 foreignKey=0 recordsWithoutKey=0
DEL split: scene-02 records=61   scene-10 records=16   sum=77
ROUTES before(shards only)=303   after(shards+diet)=303
ROUTES-MULTISET-EQUAL=true
SHARDS@86786e8 files=11 records=226 residualTicketKeys=0
DIET-ROUTES@86786e8 records=77 distinctKeys=24 recordsNotIn24=0
DIET-ROUTES byList={"wake":66,"new":11}     DIET-ROUTES non-exec 记录数=0
AFTER 命中饮食模式的记录里、key 不属本票 24 键的条数=0
```

1. 两片合计删 **80 行** ＝ 77 行路由记录 ＋ 3 行 `routes/scene-02.ts` 表头注释（三条注释我也逐行读了，只讲本件与 `order` 口径，不涉别票）。
2. **77 行记录行的 `key` 无一例外落在本票 24 键内**（`foreignKey=0`）；**无一条无 `key` 的 `non-exec` 被顺手搬走**。
3. 分片侧 61＋16＝77，与 `src/diet/routes.ts` 的 77 条数目对齐。
4. **最强判据**：把「11 个老分片 ＋ `diet/routes.ts`」的全部记录行取出来做多重集比对，`86786e8^` 与 `86786e8` **逐条字符串相同**（303＝303）⇒ 同时否掉记录丢失、重复、改写（`list`／`order`／`cli` 一字未动）。
5. 落点后 11 个老分片里本票 24 键残留 **＝ 0**（一条不剩）。
6. 归属规则「无 `key` 的 `non-exec` 跟 `scene`」被遵守：6 条 `non-exec` 与另 2 条别人的 `exec`（`calorie.view.nutrition-analysis`／`calorie.view.six-factors`）都留在原位。

> 派单口径校正：派单说「删了 74 行＋16 行＝90 行」，那是 `git show --stat` 的**变更行数**（74 ＝ 10 插入 ＋ 64 删除）。**逐行核的是 80 行删除**（64＋16），本席已按 80 行逐条核完。

---

## 四 · 维4 行为不变——**PASS**（读数为干净树所得，见 §9）

`pnpm test`（全量，落点锁窗口 v3）：

```
ℹ pass 1492   ℹ fail 0   ℹ cancelled 0   ℹ skipped 0   ℹ todo 0   ℹ duration_ms 60072.6106
-> exit=0
```

⇒ **1492/1492 全绿**，与票面口径一致。

`t293` 探针（落点锁窗口 v4，build 洗过印记后）：

```
P1: PASS   ③ UNACCOUNTED（必须手改、既没派生也没纪律覆盖，0 处）⇒ UNACCOUNTED 为空
          动态（派生桶）证据齐=true；UNACCOUNTED=0 处；树自相矛盾=0 处
P2: PASS   P3: PASS   P4: PASS   P5: PASS
RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5      -> exit=0
```

`pnpm gen` 幂等（窗口 v1／v4 各一次）：

```
GEN-IDEMPOTENT treeStillClean=True      （gen 前后 git status --porcelain -uall 均为空）
```

`build`／`gen`／`gen:check` 在窗口 v1／v4 两轮均 `exit=0`；`gen` 产出的五个生成物 sha256 两轮都**逐字等于**提交里的值（`4db43569…`／`82d40b02…`／`ca17d2c3…`／`4d44f03f…`／`e9ea9935…`）。

---

## 五 · 维5 免费判据（生成物 diff 只应出现 `src/cli/registry.ts`）——**PASS**

不等窗口日志，直接对提交做全树 numstat：

```
$ git diff --numstat 86786e8^ 86786e8 -- packages/skill-calorie/src/triggers \
    packages/skill-calorie/src/cli/keys.ts packages/base-combos packages/skill-calorie/scripts \
    packages/skill-calorie/SKILL.md
(空输出)
$ git diff --numstat 86786e8^ 86786e8 | 文件数 = 17
```

- `keys.ts`／`src/triggers/**`（含 `routes.generated.ts`）／`base-combos/**`（含 `combos.yaml`）／`scripts/**`（含 `build-help.mjs` 的 REPR 段）／`SKILL.md`：**逐字节不变**。
- 唯一生成物改动 ＝ `src/cli/registry.ts` ＋2／−0：`import { DIET_COMMANDS } from '../diet/index.js';` ＋ `SOURCES` 加一行（原文已核）。
- 证据 §4 的 `git diff --stat`「6 files changed, 22 insertions(+), 503 deletions(-)」与「pre-commit 树」口径自洽：22 ＝ 5＋10＋3＋2＋2；503 ＝ 151＋64＋16＋27＋245；694−22＝672 ＝ `src/diet/` 11 件新文件行数和。**这组数字是对的**。

---

## 六 · 维6 证据面（假绿重灾区）

### ① 每个路径／行数／数字——自己核，不信文本

| 证据原文 | 当刻事实 | 判 |
|---|---|---|
| §1 改前 `legacy/scene-02.ts` 24 条 | 24，键集＝票面 24 键 | ✓ |
| §1 改前 `cmd_read.ts` 11 个 `case` ＋ `write.ts` 13 个 `case` | 11／13 | ✓ |
| §1 改后 `src/diet/` 11 件（＋ 8 个子功能件） | 11 件＝3＋8 | ✓ |
| §1 改前 `routes/scene-02.ts` **83 条** | **69 条记录／80 行**（`9140f39` 活树） | **✗ S2-1** |
| §1 `routes/scene-10.ts` 定点删 16 条 | 184 → 168 | ✓ |
| §2 表 24 行（键 → 子功能件 → 每条 `(list@order)`） | 机比 `EV-TABLE mismatches=0`（路由位次与 `run` 目标文件全对） | ✓ |
| §3 `cmd_read.ts` 1146 → 799 行 | 799 | ✓ |
| §3 `35 → 24 个 case（−11）` | 35 → 24 | ✓ |
| §4 五个生成物 sha256 | 逐个与当刻盘上文件相符（✓×5） | ✓ |
| §5「全仓 10 条同形老键之一」 | 101 条声明里缺 `wakeWord` ＝ 10 | ✓ |
| §1／提交信息「**7** 个子功能文件（列出 8 个名字）」 | 实为 **8** 个 | **△ S3-1** |
| §5 风险 1「覆盖 ⇒ 当场红在 case 冻结清单上」 | 实测**抓不住** | **✗ S2-3** |

**S2-1（本票引入）** — §1 表「改前」列写 `routes/scene-02.ts` **83 条**；BASE `9140f39` 当刻活树**只有 69 条**。83 的出处是**预备期快照** `.scratch/t315/baseline/routes-scene-02.ts`（本席实测该快照＝83 条记录／94 行）。差的 14 条是 `2979b40`（#314）在这份快照之后从同一片搬走的：`git log --oneline -- packages/skill-calorie/src/cli/legacy/routes/scene-02.ts` 只有 `57cccd8`(#313)／`2979b40`(#314)／`86786e8`(#315) 三次，可定位。**手术本身没受污染**（`surgical-routes.mjs` 的 dry-run 自述「81 − 61 − 7 ＋ 14 = 27」，与活树 80/26 行对得上，说明它读的是当刻活树），污染的是**证据表的「改前」列**——正是硬规则 10「行号／字节快照比对不得作为依据」针对的那类事实错位，只是这次错在**记账**而非**手术**。

### ② `GATE-RUN` 行止于右引号——**合规**

```
GATE-RUN runId=0e418f44-fd2d-45ab-9cb5-3b672840a56c cmd="pwsh -NoProfile -File .scratch/t315/land.ps1"
```

`cmd="…"` 之后**没有**再挂 `waitedMs=`／`exit=`／`at=`。

### ③ 导出窗口收窄——**收窄合理；但披露不全（＝ S2-2）**

三种口径本席都跑了：

```
(A) --log docs/skills/skill-calorie/t315-gate-runs.log --ticket 315-land --since … --until …
    RESULT: matched=1/1 auditEntries=1 scoped=1 undeclared=0      gate-audit: PASS   exit=0
(B) --log .scratch/locks-land/gate-runs.log（同窗，活日志）
    RESULT: matched=1/1 auditEntries=52 scoped=1 undeclared=0     gate-audit: PASS   exit=0
(C) --log .scratch/locks-land/gate-runs.log（不加窗，全 315-land）
    RESULT: matched=1/1 auditEntries=52 scoped=9 undeclared=8
    FAIL: 反向对账：窗口内有 8 条无人声明的 RUN 条目                 gate-audit: FAIL  exit=1
```

- 把**导出件**与**活日志**在同一窗口下重新导出做逐字节比：`Compare-Object` 唯一差异是「导出时间」那行的 UTC 时间戳，`RUN` 条目原文**完全一致** ⇒ 导出件是**忠实**对账源，不是裁剪件。
- **收窄合理**（三条都对得上协议字面）：被排除的 6 条里 5 条 `exit≠0`（3 条 `exit=9` 开门守卫 abort ＋ 2 条 `exit=1` 断言红已回滚），协议 §2.4④ 明令「只认领 `exit=0` 的条目」；另 2 条 `exit=0` 是 `land2.ps1`／`land3.ps1` **文档提交窗口**（非门禁）。这 7 条**都在证据里如实列了表**。⇒ **不构成「把红的运行排除在声明外」**。
- 但表**漏了第 8 条**（下面）——它 `exit=0`、非门禁，与已列出的 `bd985162` 同类，**不影响门禁结论**，只影响「如实记账」这句的成色。

**S2-2（本票引入）** — 当刻 `.scratch/locks-land/gate-runs.log` 里 `ticket=315-land` 的 RUN 共 **9 条**，证据表只列 **7 条**，漏了：

```
RUN ticket=315-land runId=dd827764-5a5e-4d77-9a32-978f4824d518 cmd="pwsh -NoProfile -File .scratch/t315/land3.ps1" waitedMs=1 exit=0 pid=30044 at=2026-09-13T15:09:11.167Z
```

该条是**导出那一次窗口自己的 RUN 行**（`START 15:09:06.747` → 导出 `15:09:08.673` → `RUN 15:09:11.167`）：导出落在自己窗口内，天然看不见自己。

### ④ `check-gate-audit` 复跑与 `--allow-*`

(A)／(B) 均 `gate-audit: PASS`、`exit=0`、`undeclared=0`。证据里**没有**任何 `--allow-*`，因此**不需要** `GATE-RELAX` 配对行。唯一 FAIL 出现在本席**自己**放宽到全 `315-land` 范围时（口径 C），那是我的口径、不是它的认领口径，照实记录如上。

---

## 七 · 维7 变异自证（≥2 次真变异必须变红）——**PASS**

**场地**：仓内隔离副本（`git archive 3f9c131` ＋ 根／逐包 `node_modules` 联结），**全程不调 `pnpm`**（避免 auto-install 触到共享 `node_modules`），主树零接触（脚本收尾核：`主树 status 非空=0`、`主树 dist 抽指纹未变=True`）。副本先自证忠实：

```
[base] keys=4db43569fbab ==EXPECT:True  registry==EXPECT:True  keys含MUT标记:False
GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 7 个（…）声明 69 条，未搬迁清单 32 条
base-gencheck -> exit=0
```

**M1p · 改坏一条声明（`src/diet/commands.ts` 的 `title: '记一餐'` → `'记一餐MUT'`）**

```
M1p-tsc exit=0 → M1p-stamp exit=0 → M1p-gencheck exit=1
    GEN-CHECK FAIL：键 101（写 35 ＋ 读 66）；能力 7 个（…）声明 69 条，未搬迁清单 32 条
```
回正（源文件拷回 ＋ 刷 mtime ＋ 重建 ＋ `gen`）：
```
[after-M1p-restore] keys=4db43569fbab ==EXPECT:True  registry==EXPECT:True
M1p-R-gencheck -> exit=0        GEN-CHECK PASS
```
⇒ **真变异变红 ✓；回正后回绿且 `keys.ts` 逐字回到改前 sha ✓**

**M2p · 删掉一条路由记录（`src/diet/routes.ts` 第 13 行，`calorie.diet.add` 的 `wake@9`）**

```
M2p-tsc exit=0 → M2p-stamp exit=0 → M2p-gencheck exit=1
    Error: list=wake 的 order 不连续：第 9 位实得 10（packages/skill-calorie/src/diet/routes.ts · "记一餐（含备注）"）
           ——必须是 0..434 连续无洞。      （gen-routes.mjs:146 → gen-cli.mjs:470）
```
回正：
```
M2p-R-gencheck -> exit=0        GEN-CHECK PASS
```
⇒ **真变异变红 ✓；回正回绿 ✓**

**M5p · 反证：同一处声明改动，只要强制全量重编就能被抓到**

```
字节数 8348 -> 8348（同字节长度）  mtime 保持=True
M5p-tsc exit=0 → M5p-stamp exit=0 → M5p-gencheck exit=0 （假绿）
删掉 *.tsbuildinfo 强制全量重编 → M5p-gencheck exit=1   （GEN-CHECK FAIL：真红）
```
⇒ 变异确实被门覆盖，M5p 的绿是**构建增量**造成的，不是门不认账。

---

## 八 · 维8 反假绿主动攻击

### ① `Copy-Item` 保 mtime ⇒ `tsc -b` 增量跳过（**本机复现，范围外**）

见 §7 的 M5p：**同字节长度**改动 ＋ `Copy-Item -Force` ＋ 显式回写 `LastWriteTime` ⇒ `tsc -b` exit=0、`--stamp` 无条件签字、`GEN-CHECK PASS`（exit=0），而**盘上 `keys.ts` 与源码事实不符**（`keys.ts 含「记二餐」=False`，源码已是「记二餐」）。删掉 `*.tsbuildinfo` 强制全量重编后同一变异立刻 `GEN-CHECK FAIL`。
⇒ 与 **#325**（`gen:check` 假绿缺口，`#316r`／`#323r` 两席已独立复现）**同族**。按派单「只记录、不据此判 #315 不合格」处置 → **S3**。

### ② `gen:check` 读 `dist` ⇒ 只改源码不重编仍可能 PASS（**同族，范围外**）

同一现象的另一面：只要 `dist/` 与其印记自洽，`gen:check` 判的就是「盘上生成物 == 从 `dist` 算出的产物」，不是「== 从源码算出的产物」。本席在 M5p 里看到的绿就是这么来的。→ 与上条合并记 **S3**，转 #325。

### ③ 攻击「整文件覆盖共享件会不会静默复活别票已删的 `case`」——**会被静默接受（S2-3）**

干净态下**唯一**变异：把 `cmd_read.ts` 整文件覆盖成 `.scratch/t315/preview/cmd_read.ts`（1013 行，早于 #314／#316／#317 的落点），然后三条门各跑一遍：

```
覆盖后 cmd_read.ts 行数=1013（干净 HEAD 同件=653）
M3p-ratchet      -> exit=0   ℹ tests 7  ℹ pass 7  ℹ fail 0
    ✔ #294 棘轮：分派层的 case 标签只许从冻结清单里减少
    ✔ #294 棘轮：两个分派文件的行数只许减少
    ✔ #294 对账：注册表每条声明与 cli/keys.ts 的登记逐条一致
    ✔ 全部 7 条
M3p-routing81    -> exit=0   ℹ tests 8  ℹ pass 8  ℹ fail 0
M3p-legacyratchet-> exit=0   ℹ tests 6  ℹ pass 6  ℹ fail 0
M3p-gencheck     -> exit=0   （生成物与 cmd_read.ts 无关，预期不报）
M3p-t293         -> P1: PASS  UNACCOUNTED=0 处
回正 cmd_read.ts sha 相等=True ；[after-M3p] keys==EXPECT:True
```

**结论：抓不住。** 机制已查明（读该棘轮源码 `packages/skill-calorie/test/cmd-registry-294.test.mjs` 第 92–111 行）：

```js
assert.ok(frozen.has(k), file + ' 出现清单外的新 case（往分派层加分支＝破坏接缝）：' + k);
assert.ok(frozen.size <= max, file + ' 的冻结清单变长了（加码）：' + frozen.size + ' > ' + max);
assert.ok(caseLabels('cmd_read.ts').lines <= FROZEN_READ_LINES, …);   // 1146
```

判据是「文件里的标签都在**白名单**里」＋「白名单本身不长」「行数不超顶」。而 `FROZEN_READ_CASES`／`FROZEN_WRITE_CASES` **仍逐条列着这 24 条饮食键**（`FROZEN_WRITE_CASES` 第 69–72 行仍见 `calorie.diet.add`…`calorie.water.log`／`calorie.product.*`），`FROZEN_READ_CASES_MAX=61`／`FROZEN_WRITE_CASES_MAX=31` 也按编排者裁决（「棘轮上限／冻结集不下调，终态归 #320」）未下调 ⇒ 复活回来的标签仍在白名单内、1013 ≤ 1146 行也未触顶 ⇒ **门全绿，覆盖被静默接受**。

---

## 九 · 动态读数与「干净树」证据链

| 窗口 | 时刻 | 门 | 结果 |
|---|---|---|---|
| v1（锁内，`locks-land`） | 23:18:08→23:20:41 | build1／gen／gen:check／t293 | 全 `exit=0`；`GEN-IDEMPOTENT treeStillClean=True`；t293 **5/5 PASS**、`UNACCOUNTED=0` |
| v1 | 同上 | `pnpm test` | `exit=-1073741510`（`STATUS_CONTROL_C_EXIT`）⇒ **本次读数作废** |
| v3（锁内） | 23:22:06→23:23:09 | `pnpm test` | `pass 1492 / fail 0` ⇒ **exit=0** |
| v3 | 同上 | t293 | `P1=FAIL P3=FAIL`——根因是 `GEN-STALE FAIL`（`src/exercise/routes.ts` 现 6114a81e ≠ 印记 1a094bf8），**外来席留下的陈旧印记**，非本票缺陷 |
| v4（锁内，先 build 洗印记） | 23:24:33→ | build／gen／gen:check／t293 | 全 `exit=0`；t293 **5/5 PASS**；`CLOSE-TREE-EMPTY=True` |

**干净树证据链（机器动作，非事后自述）**：v3／v4 窗口开头都打 `OPEN-HEAD` ＋ `OPEN-STATUS-BEGIN/END`（空则 `OPEN-TREE-EMPTY=true`，非空即 `WINDOW-ABORT` 且一行不动）＋ 八个受审文件的 sha256／mtime ＋ `dist/.gen-inputs.json` 印记指纹；收尾再打 `CLOSE-TREE-EMPTY`／`CLOSE-HEAD`。三轮窗口的 `OPEN-HEAD` 与 `CLOSE-HEAD` 都是 `3f9c131ed185d8ad2c9031b96898fdbd5915b986`，收尾树都为空。

**两次被脏树牵连的留痕**：`.scratch/t315r/lock-blocked.txt`（`319-dry` 持锁期间）；v1 的 `TREE-EMPTY ok` 是在**拿到锁之后**才判的——编排者 23:17:58 采到的 `M cmd_read.ts`／`?? src/analysis/**` 是 319-dry 持锁期间的现场，已由编排者书面更正。

---

## 十 · 三级问题清单

### S1（关闭阻塞项）：**0 条**

### S2（关闭前必须修完）：**3 条**

1. **S2-1 · 本票引入 · 证据件数字** — 证据 §1 表「改前 `routes/scene-02.ts` **83 条**」与当刻事实（**69 条**）不符；83 是预备期快照数，其中 14 条已被 `2979b40`(#314) 搬走。
   **精确改法**：该格改成「`routes/scene-02.ts` **69 条**（`9140f39` 当刻活树；预备期快照 83 条，其中 14 条已由 `2979b40`／#314 搬走）」。
2. **S2-2 · 本票引入 · 证据件漏行** — §3 末「不认领、如实记账的其余落点锁运行」表只列 7 条，漏 `dd827764-5a5e-4d77-9a32-978f4824d518`（`land3.ps1`，`exit=0`，`15:09:11.167Z`）。
   **精确改法**：补一行「`2026-09-13T15:09:11.167Z` ｜ `dd827764-…` ｜ `pwsh -NoProfile -File .scratch/t315/land3.ps1` ｜ 0 ｜ 文档提交窗口（非门禁）——**本行是导出那一次窗口自己的 RUN 行，导出快照天然看不见自己**」。
3. **S2-3 · 本票引入 · 证据件断言了一个不成立的机器保证** — §5「风险 1」写「谁用预备期快照覆盖 `cmd_read.ts`，就会**当场红**在 `test/cmd-registry-294.test.mjs` 的 case 冻结清单上」。**实测不成立**（§8③：覆盖 653→1013 行后该测试 `7/7 PASS`，且 `routing81`／`legacy-ratchet`／`gen:check`／t293-P1 全不报）。
   **精确改法**：改成「当前棘轮**抓不住**整文件覆盖：冻结集仍含该 24 键、`MAX=61/31` 未下调，判据是『标签在白名单内 ＋ 白名单不长 ＋ 行数 ≤ 1146/745』⇒ 覆盖会被静默接受；棘轮下调与终态归 **#320** 收口票一次性到位」。

### S3（记账跟进，不阻塞关闭）：**4 条**

1. **S3-1 · 本票引入 · 措辞** — 提交信息与证据 §1 都写「**7** 个子功能文件」，却各列了 **8** 个名字（`log`／`edit`／`today`／`library`／`products`／`nutrition`／`ranking`／`review`）；目录实为 11 件 ＝ 3＋8。数据无影响，改 7→8。
2. **S3-2 · 范围外 · 转 #319／#324** — 票面 Destination ①「**饮食全部键**在未搬迁清单里归零」按字面**未完全达成**：落点后未搬迁清单 32 条里仍有 1 条名字属饮食场景的键 `calorie.view.nutrition-analysis`（唤醒词「看营养分析」，场景 02 的 `new@48` 记录），它住 `src/cli/legacy/scene-10.ts`，随其 owner（场景 10／#319）搬。证据 §1 已披露该类残留，编排者也已把本票范围裁到 24 键 ⇒ 记 S3。
3. **S3-3 · 范围外 · 转 #325** — `Copy-Item` 保 mtime ⇒ `tsc -b` 增量跳过 ⇒ `--stamp` 无条件签字 ⇒ `gen:check` **假绿**（§8①，本机隔离副本复现；删 `.tsbuildinfo` 强制重编即转红）。与 #325 同族，按派单「只记录、不据此判本票不合格」处置。
4. **S3-4 · 范围外 · 建议并入 #325／#320** — **gitignored 的 `dist/` 与 `dist/.gen-inputs.json` 印记会被「外来席半迁移的残留」污染，而 `git status --porcelain` 看不见**：v3 窗口实测 `GEN-STALE FAIL`（`exercise/routes.ts` 印记 1a094bf8 ≠ 当刻 6114a81e，而树是干净的），直接把 t293 的 P1／P3 打成假红；v3→v4 之间该印记的 sha256 从 `D4D7DAF4` 变成 `4AC8A9AD`（23:23:27 被外来席重打），同一棵干净树两次读数不一致。**建议**：把「印记/`dist` 的干净度」也纳入落点窗口的开门判据（例如 `OPEN-STAMP` 指纹比对 ＋ 必要时无条件 `pnpm build` 洗一遍），否则复核席与后续票都会读到假红／假绿。

---

## 十一 · 判定与分数

**判定：FAIL。** 门槛是「S1＝0 且 S2＝0」；本票 **S1＝0 但 S2＝3**（全部落在**证据件**，代码实质面无一失）。

**分数：86／100**（协议 §5.1 五维，仅作摘要；verdict 与缺陷清单才是正文）

| 维 | 权重 | 得分 | 依据 |
|---|---|---|---|
| 契约一致 | 30 | 29 | 声明面／分派面／跨片删除／路由归属四条全过；Destination ① 按字面差 1 键（已披露，扣 1） |
| 证据真实可复现 | 25 | 17 | parity 六条、§2 表、sha256、门禁链我都独立复现；三处事实错误（S2-1／2／3） |
| parity | 20 | 20 | 路由记录多重集守恒 303＝303；24 键声明唯一；镜像三条一致 |
| 工程红线 | 15 | 15 | 纯追加提交、只写声明路径、落点锁、派生式删除、fail-closed、复制刷 mtime、棘轮遵守裁决 |
| 文档同步 | 10 | 5 | 证据件齐、逐维可复现；但三处 S2 |

**必须整改（≤3 项，全部只改文本，不动代码）**

1. 证据 §1「改前 `routes/scene-02.ts` 83 条」→ **69 条**（并注明 83 是预备期快照、14 条已由 #314 搬走）。
2. 证据 §3 表补 `dd827764-5a5e-4d77-9a32-978f4824d518` 一行。
3. 证据 §5「风险 1」改成「当前棘轮抓不住整文件覆盖；终态归 #320」。

**未完成的动态项**：无（门禁链 v1／v3／v4 与变异电池 M1p／M2p／M3p／M5p 都跑完，主树收尾为空）。**未确证项**：① 24 条处理体的**逐字**等价性只由我做的「声明／路由／行为」三面兜（`pnpm test` 1492 全绿 ＋ t293 5/5）＋ 票面自述的「人眼逐块搬」，未做 AST 级等价比对（与票面「待确认①」同）；② 副本里 t293 的 `P2` 因副本无 `.git` 而无意义（已在主树 5/5 覆盖）。

---

## 十二 · 过程留痕（本席自己的纪律）

- 终端：`terminal_create` 标题 `t315r`／`t315r2`（sidebar 终端；`t315r` 中途 pty 无输出，按会话内已知坑另开 `t315r2` 继续）。
- 改树／构建／测试／生成一律经 `node tooling/run-locked.mjs --ticket 315r --lock-dir .scratch/locks-land --stale-minutes 30 -- …`；窗口内**未**嵌套 `run-locked`；三轮窗口收尾树都为空。
- 抢锁被挡留痕 `.scratch/t315r/lock-blocked.txt`。
- 变异电池走 `git archive 3f9c131` 隔离副本（`.scratch/t315r/mut*`），**不占共享锁、主树零接触**（每轮收尾核 `主树 status 非空=0` ＋ `主树 dist 抽指纹未变=True`）。
- **本席自己的一次过程风险（如实记账）**：变异电池 v1 在副本里跑 `pnpm build` 触发 pnpm 自动 `install`（副本 `node_modules` 是指向主树的联结）——**未写入任何共享文件**：`node_modules` 全树扫描 2822 个文件里 `mtime ≥ 23:17` 的为 **0**，`.modules.yaml` mtime 仍是 `2026-09-12 23:28:38`，主树 `git status` 为空。事后已把 v2 起的电池**全部改为直调 `node`**（不再调 `pnpm`），并删掉联结重做。
- 只写 `.scratch/t315r/` ＋ 本报告落库文件；未 `amend`／`force-with-lease`／`rebase`／`stash`／`reset --hard`／`clean`／`checkout -- .`／`git add -A`；未装任何包；未改 #315 票面、未关票、未替被复核票改代码。
