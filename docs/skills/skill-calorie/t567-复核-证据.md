# #567 公共层形状清账 · 独立复核席证据

> 被审＝提交 `8920faa`（14 件）｜地图 #161｜仓库 FeatherHunter/ilife｜复核席与实施席不同席
> 全部编译／测试／git 写操作经 `node tooling/run-locked.mjs --ticket 567 --lock-dir .scratch/locks-m158v2` 持锁；
> 实施者证据件里每一句「已绿」本席**一律不采信**，全部独立复跑并给自己的 runId。

## §0 判定

**判定：PASS（可关票）**，附 3 条登记项（不拦路，见 §5）。

四件必查全部独立跑通：① 公共层回归面零红（本包 688/688 ＋ 他技能靶向 79/79）；② 8 条断言改坏必红、
逐件还原必绿（两组各 5／6 红 → 还原 22/22 绿）；③ 自造影子树基线证明**默认行为未变**（表格 7 例逐字节同、
图表 225 组逐字节同、公共层 CSS 30 条声明差异全在改动族内）；④ 写集与提交逐件对上、无别席路径、无新越线。
§3 基线 A/B 归因**方向成立**（数字不可复现，见 §4）。

## §1 复核四件事（逐条机器读数）

### ① 公共层回归面

| 口径 | runId | 读数 |
|---|---|---|
| `node --test packages/base-render/test/*.test.mjs` | `1d8a58a2-ae2a-4606-a1ce-d5071ee5a30f` | **688 测试／688 过／0 红**，exit 0（与实施者同值，独立复跑） |
| 他技能靶向 8 件（chef 2＋home 2＋schedule 2＋memo 2） | `4d2c88c2-63bd-4a28-8973-5d10645253cc` | **79 测试／79 过／0 红**，exit 0 |

他技能靶向件＝`skill-chef/test/{help-delivery-216,help-file-214}`、`skill-home/test/{help-delivery-190,help-assets}`、
`skill-schedule/test/{help-delivery-204,help-file-202}`、`skill-memo-ilife/test/{cli-help-230,help-file-228}`
（这四个包都消费共享 HELP 模板与 `base-paint` 样式，是公共层改动的直接下游）。
**dist 新鲜度自证**（`387ec8c1-5ba0-40be-ab7f-e3c9bff2fa76` 的第 2 步）：`--force` 重编前后
`dist/index.js`＝`811f5d6396518830`、`dist/blocks.js`＝`ef79252ae628b1c2`、`skill-calorie/dist/index.js`＝`e3062238b6667c75`
**三件逐字节不变** ⇒ 上面两条读数跑的是**提交态源码**，不是陈旧 dist。

### ② 探针识别力（改坏必红 → 逐件还原必绿）

本席**自造**变异探针 `.scratch/t567-review/probe-mutation.mjs`（不用实施者的 `probe-shape.mjs`），整轮持锁
（runId `a0360cda-346b-4610-abe3-a260d605f170`），两行机器读数：

- **A 组（公共层形状面：D1／D2／D6／状态列 cellHtml 四条各改坏一次）**：重编 exit 0 → 靶向测试
  **exit 1，`pass 17 / fail 5`**，红名单逐条对上：`#567 D1 表卡吃满内容列`、`#567 D2 列表行首列不折行`、
  `#567 D6 页内导航触摸目标 44px`、`#567 状态列 cellHtml`、`#512 ② 基座无宽度上限、无居中边距`。
- **B 组（图表四开关：`valueThin`／`yTicks`／`showValues:'last'`／`labelEvery` 各改坏一次）**：重编 exit 0 →
  靶向测试 **exit 1，`pass 16 / fail 6`**，红名单：开关一／二／三／四 ＋ `valueThin:30 → 抽稀到 2–58 枚`、
  `yTicks:3 → 3 条刻度＋3 条网格线同位置`、`负向：yTicks:false／7`、`showValues:'last' → 恰一枚`、
  `highlightLast 同开时不双印`、`showDots:true＋labelEvery:7`。
- **还原后**：重编 exit 0 → 同批靶向测试 **`pass 22 / fail 0`（A、B 两次都是）**；末了全量
  **`node --test packages/base-render/test/*.test.mjs`＝688／0，exit 0**。
- **还原自证**：`RESTORE-CHECK blocks.ts 与原始逐字节同=true`、`charts.ts 逐字节同=true`。

⇒ 8 条断言**都不是恒真**：改坏必红、还原必绿，双向可复现。

### ③ 收窄是否变放宽：自造真样例证明**默认行为没变**

做法（实施者没做过的一条路）：把 `8920faa^` 的 `packages/base-render` ＋ `packages/base-link-core` 经
`git archive` 抽到影子树 `.scratch/t567-review/oldroot/`，**单独编译成基线 dist**
（runId `9b90347c-0aa3-461a-bcb9-fa3e33b71f9d`，exit 0；基线自证：老 CSS 含 `max-width: 680px`＝true、
含 `th{text-transform:uppercase}`＝true ⇒ 确系改前件），再与当刻 dist **同输入逐字节对比**
（探针 `.scratch/t567-review/probe-default.mjs`）：

| 面 | 读数 |
|---|---|
| A 表格默认形态（7 例：常规／空表两态／单列／引号尖括号／三对齐／中文表头带 kg·ml） | **7/7 逐字节相等** |
| A2 表格非法输入拦截文本（空列／非法对齐／对象单元格／列非对象／行非对象） | **5/5 错误文本逐字相等** |
| B 图表默认产物（`bar／line／donut／progress／combo／sparkline／gauge／scatter` × 15 组旧开关 × 多档点数 ＝ **225 组**） | **222/225 逐字节相等**；3 组不同＝`bar` ＋ `showValues:'edge'`（见 §5 登记 1） |
| C 公共层 CSS 声明级 diff（`blocksCss`／`buildStyleSheet`／`pageUiCss`／`pageShapeCss`／`chartsCss`） | **30 条差异，逐条落在本票改动族内（越族 0 条）**：`font-size` ×21、`max-width` 撤 1、`margin-inline` 撤 1、`th` 的 `text-transform` 撤 1、`grid-template-columns` 1、`toc a` 的 `padding 6px→8px 12px`／`display`／`align-items`／`box-sizing`／`min-height` 5 |
| C2 `chartsCss` 段 | **零差异**（图表样式段本票不该动，实测确实没动） |

⇒ 「表卡撤 680 上限」是**放宽**（撤约束）而非收窄，但它是票面 D1 的明写动作，且**没有连带放开别的**：
除上表 30 条外，公共层 CSS 与默认渲染产物**一个字节都没变**。

### ④ 铁律与结构

- **写集与提交逐件对上**：`git show --name-only 8920faa` ＝ **14 件**，与实施证据 §1 声明表逐行相同；
  `git diff --stat 8920faa HEAD -- packages/base-render packages/skill-calorie/src/render/trendPredictDocs.ts
  packages/skill-calorie/test/analysis-shape-567.test.mjs` **空** ⇒ 提交后无人再动这批件。
- **无别席路径**：14 件里没有 `packages/skill-calorie/src/goal/**`、`src/triggers/scene-04-exercise.ts`、
  `packages/skill-bill/**`、`AGENTS.md`。
- **350 告警线**：本票改过的 7 件源件 LF（改前→改后）＝`blocks.ts 2194→2223`、`charts.ts 2081→2154`、
  `pageShapes.ts 449→452`、`pageUi.ts 244→245`、`spec/charts.ts 273→282`、`style.ts 1374→1383`、
  `trendPredictDocs.ts 1162→1162`。**没有一件因本票新越线**（base-render 六件改前就已远超 350，且该包无
  `AGENTS.md` 台账，历史口径见 `docs/base/base-render/t154-ui-fix-公共层-证据.md:261`＝blocks.ts 早已 1880 行）；
  `trendPredictDocs.ts` 与 `packages/skill-calorie/AGENTS.md` 台账「当场实测 1162」**逐值一致**（delta 0）。
- **专项门复跑**（runId `387ec8c1-5ba0-40be-ab7f-e3c9bff2fa76`，一次持锁 6 步）：
  全仓 `tsc -b` exit **0**；`base-render＋skill-calorie --force` exit 0；31 页重出 **31/31 exit 0**；
  `audit-separators --dir .scratch/t387/html` **RESULT 31/31 PASS**；`measure-responsive` **OVERFLOW-ZERO pages=31 cells=93 failed=0**。

## §2 用户对本批 HTML 的硬要求（逐条现量，不凭印象）

现量工具：本席自造 CDP 探针 `.scratch/t567-review/probe-visual-567.mjs`（3 页 × 3 档 ＝ 9 格，
runId `ed3dfb5b-1e57-4c26-b4c9-268d762a17f4`）＋ 31 页门 ＋ `vision_html_screenshot` 双档实拍。

| # | 要求 | 现量读数 | 判 |
|---|---|---|---|
| ① | 1440／768／390 三档无溢出无塌列 | 31 页 × 3 档 **0 溢出**（93 格 failed=0，`clip=0`、图 `0/0 放得下`）；抽 21／24／30 三页 9 格再量 **溢出 0/9** | 达标 |
| ① | 触摸目标 ≥44px | 9 格 **`过小=0`**（可见可点件 7–10 件/页）；页内导航实测 **全部 44px 高**：`52×44 / 78×44 / 91×44 / 117×44`（21 页 6 枚、24 页 6 枚、30 页 5 枚，三档同值） | 达标（D6 生效） |
| ② | 手机档与 HELP 同档 | HELP 正本 `fixtures/help-instances/卡路里_HELP_20260731_201530.html` 断点＝`max-width:600px`／`601–900px`（老骨架，无窄表堆叠段）；当刻页族断点＝`400／640／641／720／721／820／821／875／876`＋`print`／`prefers-reduced-motion`，手机档按 ≤640 走「表列行卡化」（`td::before{content:attr(data-label)}`，data-label 与 `th` 同源）。**同档＝同为手机单列档、同一套行卡化做法**；断点数值不同（600 vs 640）属老 HELP 骨架与现行底盘三档的口径差，非本票引入 | 达标（口径差已登记） |
| ③ | 间距取 4／8 倍数 | 9 格**全部**命中非 4 倍值：`ilife-block padding 14px`、`chip padding 2px/6px`、`kpi-card-value-row gap 6px`、`dist-row padding 5px`、`page-shell-title marginTop 6px`、`chart-block-title marginBottom 10px`、`charts-legend-item gap 6px`、`data-table-caption padding 10px/14px` —— **逐条都在本票写集外**（我的 CSS 声明级 diff 只 30 条，无一条引入非 4 倍间距；D6 恰好把 `toc a` 的 6px 纵向内距改成 8px） | 未达标（**既有债，非本票引入**，见 §5 登记 3） |
| ③ | 字号成档有下限 | 9 格最小字号 ＝ **12px（21 页）／11px（24、30 页）**，**`<11px` 命中 0** | 达标 |
| ③ | 数值列等宽右对齐 | 768／1440 档：`td.cell-right` **align=right ＋ tabular-nums，劣化 0**；390 档行卡化后该列归左对齐（`align=left`，24／30 页各 1 格）——行卡形态下标签＋值竖排，右对齐不适用；**该规则本票未动**（CSS diff 无对应条目） | 达标（手机档例外形态已登记） |
| ③ | 卡／表／图左缘共线 | 双档实拍读图：21 页四卡与下方图卡左缘同一条竖线、右缘也对齐；24 页四卡＋告警条＋胶囊组＋大图卡同一条竖线；30 页统计卡与折线图卡左缘与页签左缘对齐 | 达标（读图判；本席探针的左缘集合把嵌套子件也收进来了，不作判据，见 §6） |
| ④ | 文案零冗余（同一事实一页只讲一次） | 缺口页现场：参数说明＝「开始和结束是这页统计的窗口，共 7 天。」；口径行＝「缺口＝当天消耗减当天摄入（正数就是有缺口）」，两者最长公共子串 <8 字（`analysis-shape-567` 同口径断言 ＋ 本席复跑绿） | 达标 |
| ⑤ | 分号／竖线／间隔号拼串改可见结构 | `audit-separators --dir .scratch/t387/html` **RESULT 31/31 PASS**，节点级 `· 0／； 0／≥3 段 0／｜0／、0／~0／标识符 0`（31 页逐页命中 0） | 达标 |
| ⑥ | 双档实拍 ＋ 读图 | 6 张实拍（21／24／30 × 1440×1000 ＋ 390×900）＋ 3 次 `vision_describe` 读图；结论：**无压字、无文字截断、无元素重叠、无塌列**；30 页两处轻微可优化（第四张卡副说明基线比前三张低约一行；390 档 2×2 卡左列比右列高 10–16px），21 页参数区留白偏松 | 达标（两处轻微项登记，非本票引入） |

**读图与量尺冲突的处理**：视觉模型在 390 档实拍上给过「胶囊触摸目标仅 22–24px」「顶部胶囊溢出容器右沿」两条判断；
本席量尺读数（CDP `getBoundingClientRect`）＝**导航件全部 44px 高、9 格溢出 0、31 页 `clip=0`** ⇒ 以量尺为准，
读图那条判为缩图读数失真（视觉判断不作像素判据）。

## §3 §3 基线 A/B 归因核实（101 红→95 红）

**结论：方向成立，数字不可复现，且其中一条根因在本席窗口已消失。**

- 本席独立复跑全仓测试面（与根 `pnpm test` 同 glob 清单，runId `ed3dfb5b-1e57-4c26-b4c9-268d762a17f4`）：
  **3032 测试／2942 过／90 红**（实施者当刻记 95 红；两次不同源共享树的正常漂移）。
- **90 红的归属**（按失败用例名回查所属件）：全部落在 `packages/skill-calorie/test/**` 与仓根 `test/**`，
  点名件为 photo 族（`photo-gif-page-352`／`photo-budget-438`／`photo-receipt-docs-282`／`photo-responsive-484`／
  `photo-helpdoc-488`／`photo-picker-283`／`photo-shape-read-281`／`photo-shape-341`）、`#109`／`#111` 运动族、
  `#239`／`#247` 复制族、`#448` printable、`#514`／`#551` 分隔符、`#320`／`#449`／`#440` 单源、`T5`／`T9` 告警线门、
  `calorie-routing-81`／`calorie-triggers` 路由 parity 等。**`packages/base-render/test/**` 与两件新增 567 测试
  零命中**；抽读失败明细（`fail-detail.txt`）确认断言面是**别席在途的页面文案/体积/台账**，不是本票 CSS 面。
- **实施者 §3.1 那条根因已不成立**：其记「全仓 `tsc -b` 被 `skill-bill/scene-collect.ts` 打断 exit 2」
  （其 runId `cff99e4e`）；本席复跑**全仓 `tsc -b` exit 0**（`387ec8c1` 第 1 步）⇒ 别席已修，该阻塞解除。
- **实施者 §3.2 那条根因完全复现**：本席复跑 `check-warning-line.mjs` ＝ **RESULT 87/91、4 条台账陈化**
  （`nutritionPortDocs.ts 745→750`／`libraryDocs.ts 407→408`／`receipt.ts 398→425`／`reviewDocs.ts 359→362`，
  与其实证**逐字相同**）；四件均在本票写集外。其后别席已 `--sync`（`packages/skill-calorie/AGENTS.md` 台账
  「当场实测」列已改到 750／408／425／362），本票未使台账失配。
- ⇒ **「本票引入 0 条新红」成立**：写集内的件（base-render 全套 688／两件新增 567 测试）零红，
  残余红全部在写集外且有独立根因。

## §4 未做项与下一手

- **未做 D7**（票面已判可接受形态，本席只复读、未复核其形态）：390 档明细表 7×5 塌成 15 行键值卡。
- **未做**：全仓 3000 测试的三位 A/B（把 14 件逐件还原再跑一遍）——代价过高且当刻共享树在动，改以
  「全仓 90 红逐条归属 ＋ 写集内零红 ＋ 默认行为逐字节同」三条独立读数替代（判据更硬，见 §1③／§3）。
- **未做**：`pnpm gen:check` 复核（实施者记 GEN-CHECK PASS 128 命令）——本票未动命令面，本席未复跑。
- 下一手：登记项见 §5；实施席若认领「旧选项 `edge` 口径变更」与「非 4 倍间距既有债」，另开票即可，不阻塞关票。

## §5 登记项（不拦关票，须留在册）

1. **旧选项 `showValues:'edge'` 在柱图上的行为变了**（唯一的默认面外差异）：`charts.bar` 给
   `showValues:'edge'` 时，改前 5／31／59 柱分别标 **5／31／59** 枚，改后一律 **2** 枚（首尾有效点）。
   这是**修 bug**（同族 `packages/skill-calorie/src/analysis/reportDocScoreSeries.ts` 件头已登记该病，
   本票改后它不再用 bar＋edge），且**仓内零调用方**再给柱图传 `edge`（实测全仓 grep 仅该注释一处）
   ⇒ 影响面 0；但提交信息与实施证据的「默认行为不变」表述**只覆盖「不给任何开关」**，
   本条属公共层对**既有选项**的口径更正，已在册备查。
2. **`renderDataTable.cellHtml` 是受信 HTML 透传位**（不转义，调用方自负转义；非函数／非 string 返回值 → `bad-input`）。
   与既有 `renderDisclosure.contentHtml` 同口径，属**公共层能力放宽**（新增注入面）。实测
   `packages/**` 内**零生产调用方**（只有两件 567 测试用合成表覆盖）⇒ 当前是「登记未接线」的能力位，
   接线时须在调用方强制转义。
3. **非 4 倍间距既有债**（§2③）与 **390 档数值列左对齐**（行卡形态）：均为公共层既有值，本票未动，
   本席只登记不判本票缺陷。

## §6 本席读数口（可复跑）

`.scratch/t567-review/`：`probe-default.mjs`（③ 默认行为对照，需先建影子树 `oldroot/`）、
`probe-mutation.mjs`（② 变异自证，须经 run-locked）、`probe-visual-567.mjs`（§2 CDP 现量）、
`gates.mjs`（专项门 6 步）、`final-batch.mjs`（全仓测试面＋视觉现量）、
`gate-summary.txt`／`mutation.log`／`fail-attrib.txt`／`visual-measure.json`（读数落盘）。
影子树 tsconfig 相对 `extends` 曾写错两级（两次 exit 2，runId `a32d5781`／`e108040e`），第三次
`9b90347c` 起为有效基线——读数只取有效轮。
