# 视觉验收墙 · 场景02 饮食（83 件）

> 终态时刻 2026-09-16（源码线全部落地后重出；终审 **93/100 PASS**）。双击文件即看，不起服务、不装东西。

## 一 · 覆盖与结论

- 运行期总表筛 `scene==='02'`＝**83 条**唤醒词，清单 83 行，产物 83 件，逐条对上（重出脚本跑前先对账，缺一条即停）；设计期老词表 70 条一条不漏全含其中。
- **机器判据**：门禁 83 件**非脚注命中 0**（分隔符与内部标识符全清）；`measure-responsive` 四档（390／512／820／1440）83 页 **332 格横向溢出全 0**；告警线台账 `RESULT: 91/91 PASS`。
- **视觉终审 93/100**（量尺＝`t579-场景02视觉整改基准.md` 冻结件）：J1 门禁 30／自适应 22／文案 22／vision 抽检 19。评分件：`t605-视觉复评.md`。
- 有意不出 5 项（空库态／空窗态／其他场景／HELP）在《总索引》末节写了原因，不算遗漏。

## 二 · 入口（4 个，双击即开）

| 看什么 | 绝对路径 |
|---|---|
| 链路总表（prompt→唤醒词→命令→产物，点绝对路径直达，每行带复制按钮） | `D:\ilife\docs\skills\skill-calorie\scene02-验收墙\链路总表.html` |
| 总索引（按页族分组＋机器读数＋有意不出） | `D:\ilife\docs\skills\skill-calorie\scene02-验收墙\总索引.html` |
| 桌面墙 1280（看排布） | `D:\ilife\docs\skills\skill-calorie\scene02-验收墙\桌面墙-1280.html` |
| 手机墙 390（看塌列） | `D:\ilife\docs\skills\skill-calorie\scene02-验收墙\手机墙-390.html` |

过程型也在墙里：⑦预检确认页 2 格（先确认、不写库）＋①写后回执页 18 格（已写库、四块齐）＋拍营养表 2 格；链路总表「流程」列逐条标了去向。

## 三 · 三步验收

1. **亲手跑**：链路总表顶部两段演示 prompt（`记一餐`／`看本周饮食`）各开一个新 session 发，看回执给的**绝对路径**＋双击能否开页；其余 81 行点「复制」即拿该条的 prompt。
2. **肉眼滚**：两张墙滚完，看挤／塌列／被藏起来的横滚；本轮重点是**每页顶部**（标题已无日期，日期住两枚日期块＋箭头＋计数胶囊的窗口条，单日退化成一枚块），以及**手机档**的触摸区与卡片化表格。
3. **签字**：`逐格缺陷清单.md` 最后一列逐格填（无缺陷写「看过，无」）——该清单是旧版措辞，重核以墙内新页为准。

## 四 · 自检读数（重出时正反两面都走过）

`83 格；链接 419 条；缺失 0 -> 可发`（exit 0）；反例改坏一格文件名 exit 1 且点名，改回 exit 0。字节区间见 `manifest.json` 的 `readings`。重出命令：`node docs/skills/skill-calorie/scene02-验收墙/restage.mjs`；复评量尺见 `t605-视觉复评.md`。

## 五 · 已知（**不拦本次验收**，逐条带归属）

| 项 | 现象 | 归属 |
|---|---|---|
| 屏上来源脚注 66 处 | 页脚「数据来源 · …」仍在（门禁唯一剩余命中，全部落 `ilife-block-caliber`） | **#560**（正主在删脚注） |
| `t275 ①／⑥ 看饮食总览` | 2 条红：缺来源脚注（#560 撤脚注所致）／复制日志接线已变（#399 今晨接线变更） | **#560／#399** |
| `analysis-t5` 3 条陈旧断言 | 期望值与源侧早已改过的文案不符（测试件未随动） | 场景10 线 |
| `看营养分析`空窗 | 报 `exit 4` 而非完整空页（与融合基准裁定 4 冲突，锁里具名只许它一条） | **#275**，场景归属待裁 |
| 多行写入两页「字段变更」表 | 复制昨日饮食／批量补记饮食仍 9 行 `—`（多行写入无单行 id，形状待定） | #591 遗留，待立票 |

## 六 · 终审新发现（待立票，不影响 93 分达标）

- **D1 公共层图例条标签被裁**（10 页 × 3 档，2 页可复现）：`ilife-block-dist-row-name` 78px 固定宽＋`overflow:hidden`，标签如「蛋白（推荐 10–20%）」被切。落 `packages/base-render/` 分布行。
- **D2 一页题名与唤醒词不符**：`看今日营养` 上屏题名与眉标是「饮食复盘」，但页内导航与内容是本条自己的。
- **D3 复盘页「距范围」列自相矛盾**：脂肪实际 17% 低于下限 20%，表内写 `-8 克` 与状态列读法不一致。
- **D4 榜页「餐均」列**：值印 `620 卡/餐`，390 档卡片化后读成「餐均 620 卡/餐」，单位与列名语义重复。

> 本批 HTML 的完整机器读数见 `t605-证据.md`（34 张截图逐张路径）与 `t579-场景02视觉整改基准.md`。
