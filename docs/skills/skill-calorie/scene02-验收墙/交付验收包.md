# 场景 02 饮食 · 交付验收包（给负责人）

> 这一步是地图 #155 的#280：**您亲自跑一遍 + 肉眼看一遍**。程序能做的都做完了（见下「已经证过的」），剩下的两件只有您能做：**在一个全新空白 session 里亲手问一次**，和**用眼睛看两张墙**。

---

## 〇 · 装机：不需要动作

agent 读技能的入口＝`%USERPROFILE%\.agents\skills\`，其中 `skill-calorie` **是指向本仓 `packages/skill-calorie` 的联结**（不是拷贝）。所以：

- 本仓的 `dist/` 编好了，agent 读到的那份就是它 —— **没有「装旧包」问题**；
- 实测当刻：运行期总表里场景 02 = **83 条路由，83 条都是可执行**；`dist/diet/` 下本图新增的模块（`precheck`／`nutritionPortDocs`／`rankingDocs`／`libraryDocs`／`todayDocs`／`reviewDocs`／`receipt`）**全在**。
- 唯一要做的：**新开一个 session**（这正是#280 要的「全新空白」）。

---

## 一 · 您亲手跑（#280 判据 1 与 2）

### 怎么跑

开一个**新的空白 session**，把下面这段**整段复制**过去发出去（这就是「复制 prompt → AI 用命令执行」那条路）：

```
请你加载技能「卡路里」，执行唤醒词「记一餐」。

我刚吃了午饭：鸡胸 200 克、蛋白质 35 克。请帮我记录。
```

然后**再开一个**新 session，发这一句：

```
请你加载技能「卡路里」，执行唤醒词「看本周饮食」。
```

### 跑完看三样

| 看什么 | 合格的样 |
|---|---|
| 回的**文字** | 只说结论、不超过三句话 |
| 有没有**落盘一份 HTML** | 回执里给的是**绝对路径**（`D:\...\calorie_html\....html`） |
| **双击那份 HTML** | 能打开、样式在（不是白页、不是一行纯文本） |

### 30 秒自检（可选，想核数字时用）

回执给的路径直接贴进文件资源管理器就能打开；页面上应有完整的版式（卡片／表格／图例），不是片段。

---

## 二 · 您肉眼看（#280 判据 3 与地图判据 ②）

两张墙已在侧栏排队；也可直接双击：

| 入口 | 路径 | 看什么 |
|---|---|---|
| **总索引** | `docs\skills\skill-calorie\scene02-验收墙\总索引.html` | 83 格逐条列出「唤醒词 ｜ 命令 ｜ 参数 ｜ exit ｜ 字节 ｜ 该格看什么」 |
| **桌面墙 1280** | `docs\skills\skill-calorie\scene02-验收墙\桌面墙-1280.html` | 83 格同时渲染，横向溢出／挤／塌列 |
| **手机墙 390** | `docs\skills\skill-calorie\scene02-验收墙\手机墙-390.html` | 390 宽三列排布，触控区与换行 |
| **逐格缺陷清单** | `docs\skills\skill-calorie\scene02-验收墙\逐格缺陷清单.md` | 83 行待您逐格判：**没缺陷也请写「看过，无」** |

墙的自检（程序已跑）：**83 格；链接 419 条；缺失 0 → 可发**（exit 0）；反例（改坏一格文件名）**exit 1 且点名那一份**，改回 exit 0 —— 说明这墙的链接是真的，没有死格。

---

## 三 · 已经证过的（您不必重做）

| 项 | 读数 | 证据件 |
|---|---|---|
| 83 条逐条真出口 | exit 0 **83/83**、落盘 **83/83**、`<!doctype html>` 在第 0 字节 **83/83**，字节 68351–168483 | `scene02-验收墙/manifest.json`、`t280-真跑台账.md` |
| 83 条逐条结构化锁 | **99 用例 / 99 通过 / 0 失败**（约 230 次真出口调用） | `packages/skill-calorie/test/t279-真出口用例.test.mjs` |
| 70 条老词表 ⊂ 83 条运行期词表 | 老表 **70 条一条不漏全含**其中 | `t279-报告.md`、`t278-报告.md` |
| HELP 里每个饮食场景查得到底层唤醒词 | **70/70 命中**；「唤醒词 → 命令 → 工作流程」表**70 行由构建期生成**，命令列与路由层**逐字一致** | `packages/skill-calorie/SKILL.md`、`test/t278-唤醒词与工作流程.test.mjs` |
| 空窗／空库两态 | 空库 ⇒ `exit 4` ＋ 不落盘；窗口为空 ⇒ 完整页＋空态句＋引导句 | `test/t272-空窗两态.test.mjs`、`t273`、`t274`、`t275` |
| 全仓编译与派生件 | `pnpm build` exit 0；`gen:check` PASS（键 128） | 本次合并点实测 |

---

## 四 · 已知残留（不阻塞验收，如实摆在您面前）

1. **`看营养分析` 空窗**报 `exit 4` 而不出完整页 —— 与融合基准裁定 4 冲突。**已在锁里具名钉住且只许它一条**（`t279` 的名单），修好后摘名单即可；归属 **#275**，它同时挂场景 10，**场景归属待您裁**。
2. **两条过程型词的落盘文件名**仍是写键口径（`记一餐_回执_…`），页头已是确认页 —— 外观遗留，留您定夺。
3. **`改饮食记录`／`删饮食记录` 空库时的失败面**少了「（缺失阻断）」四个字（同批其他命令有）—— 出处 `src/fetch/diet.ts`，本图票面没点过，未纳进断言。
4. **`test/t275-营养饮水总览页.test.mjs` 有 1 条红** —— 成因是#560 那笔去掉了屏上来源脚注，而那条断言钉的是旧口径；**已在 #560 留前哨通知**，不是本图的账。
5. **83 件产物里的 10 处**已在逐格缺陷清单里点了名（各带读数与归属票），请您逐格再过一遍。

---

## 五 · 这件票怎么收

按地图的进度契约：#280 是**负责人亲手跑**的票，所以我们停在 **95%** 等您的三条结论：

1. 上面**两个 prompt** 在新 session 里跑出来了吗？回执给的是绝对路径吗？双击能开吗？
2. 两张墙滚完了吗？有没有挤／塌列／被藏起来的横向滚动？
3. 逐格缺陷清单的最后一列填了吗？

您把这三条（原话即可）回给我，我就把 #280 写到 100% 并关票；**在那之前它保持 95% 与 OPEN**。
