# 场景 02 饮食 · 交付验收包（给负责人）

> 这一步是地图 #155 的 #280：**您亲自跑一遍 ＋ 肉眼看一遍**。程序侧能做的都做完了（见下「已经证过的」），剩下两件只有您能做：**在一个全新空白 session 里亲手问一次**，和**用眼睛看两张墙**。
>
> 本版读数＝终版墙（#619 席位按当刻源码重出，提交 `dc92e55b`），四处负责人点名缺陷另经**总工程师席独立复跑**。

---

## 〇 · 装机：不需要动作

agent 读技能的入口＝`%USERPROFILE%\.agents\skills\`，其中 `skill-calorie` **是指向本仓 `packages/skill-calorie` 的联结**（不是拷贝）。所以本仓 `dist/` 编好了，agent 读到的就是它——没有「装旧包」问题。

- 实测当刻：运行期总表里场景 02 ＝ **83 条路由，83 条全部可执行**；`dist/diet/` 下本图新增的模块（`precheck`／`nutritionPortDocs`／`rankingDocs`／`libraryDocs`／`todayDocs`／`reviewDocs`／`receipt`）全在。
- 唯一要做的：**新开一个 session**。

---

## 一 · 您亲手跑（#280 判据 1 与 2）

### 怎么跑

开一个**新的空白 session**，把下面这段**整段复制**过去发出去：

```
请你加载技能「卡路里」，执行唤醒词「记一餐」。

我刚吃了午饭：鸡胸 200 克、蛋白质 35 克。请帮我记录。
```

然后**再开一个**新 session，发这一句：

```
请你加载技能「卡路里」，执行唤醒词「看本周饮食」。
```

### 跑完看三样

| 看什么 | 合格的样 |
|---|---|
| 回的**文字** | 只说结论、不超过三句话 |
| 有没有**落盘一份 HTML** | 回执里给的是**绝对路径**（`D:\...\calorie_html\....html`） |
| **双击那份 HTML** | 能打开、样式在（不是白页、不是一行纯文本） |

---

## 二 · 您肉眼看（#280 判据 3 ＋ 地图判据 ②）

四张入口都在 `docs\skills\skill-calorie\scene02-验收墙\`：

| 入口 | 文件 | 看什么 |
|---|---|---|
| **总索引** | `总索引.html` | 83 格逐条列出「唤醒词 ｜ 命令 ｜ 参数 ｜ exit ｜ 字节 ｜ 该格看什么」 |
| **桌面墙 1280** | `桌面墙-1280.html` | 83 格同时渲染：横向溢出／挤／塌列 |
| **手机墙 390** | `手机墙-390.html` | 390 宽排布：触控区与换行 |
| **逐格缺陷清单** | `逐格缺陷清单.md` | 83 行待您逐格判：**没缺陷也请写「看过，无」** |

---

## 三 · 已经证过的（您不必重做）

| 项 | 读数 | 证据件 |
|---|---|---|
| 83 条逐条真出口 | exit 0 **83/83**、落盘 83/83、每件第 0 字节都是 `<!doctype html>`；字节 **73,948 – 171,446 B**（中位 97,082 B） | `scene02-验收墙/manifest.json` |
| 门禁（分隔符） | 83 件产物页**非脚注命中 0**；`ilife-block-caliber` 来源脚注行 **66 处单列计数**（脚本聚合行仍打 `RESULT: 25/83 FAIL`／exit 1——两套口径如实并列，不是全绿） | `t619-证据.md` |
| 四处负责人点名缺陷 | `FIX-VERIFY 4/4 PASS`：①看营养分析 H1「营养分析」无日期 ②看每日六因素 H1「每日六因素」无日期 ③拍营养表记一餐那列 8 格全是「识别得到」、可见 `<span`／`precheck-tag` 命中 0、徽章 8 枚 ④看本周／看上周等 8 张窗口页趋势卡＝上升／下降／持平、裸英文 0 | `t619-复验.md`、`.scratch/t619/fix-verify-final.log` |
| 同上·总工独立复跑 | 直读当刻墙逐格判 → **5/5 PASS**（复跑脚本 `.scratch/conv/verify-wall.mjs`） | 本席复算，非席位自述 |
| 自适应 | 83 件产物页四档（390/512/820/1440）横向溢出 **全 0**；两张墙／总索引／链路总表**逐件写在命令行上豁免**（`--exclude=总索引.html,桌面墙-1280.html,手机墙-390.html,链路总表.html`，非默认放宽） | `.scratch/t619/s2-responsive.log` |
| 83 条逐条结构化锁 | 99 用例 / 99 通过 / 0 失败 | `packages/skill-calorie/test/t279-真出口用例.test.mjs` |
| 70 条老词表 ⊆ 83 条运行期词表 | 老表 70 条一条不漏全含其中 | `t279-报告.md`、`t278-报告.md` |
| HELP 里每个饮食场景查得到底层唤醒词 | 70/70 命中；「唤醒词 → 命令 → 工作流程」表 70 行**构建期生成**、命令列与路由层逐字一致 | `packages/skill-calorie/SKILL.md`、`test/t278-唤醒词与工作流程.test.mjs` |
| 空窗／空库两态 | 空库 ⇒ `exit 4` ＋ 不落盘；窗口为空 ⇒ 完整页 ＋ 空态句 ＋ 引导句 | `test/t272-空窗两态.test.mjs`、`t273`、`t274`、`t275` |
| 视觉复评 | **96/100 PASS**（量尺＝`t579-场景02视觉整改基准.md` §二，≥90 过线） | `t619-复验.md` |

---

## 四 · 已知残留（不阻塞验收，如实摆在您面前）

1. **多行写入的回执「字段变更」表**仍印 9 行 `—`（`复制昨日饮食`／`批量补记饮食`）：只对单条写入路径修好，多行写入**没有承接票**，本图不扩范围。
2. **屏上「数据来源」脚注行**在场景 02 的 83 件产物里仍有 66 处（每页 1 行）：**本图已裁定不做**（口径源自 #560，但 #560 只写场景 01 且已关；落到场景 02 是跨场景新口径，会改动冻结基准）——要全删须另开跨场景票。
3. **`看营养分析` 空窗**报 `exit 4` 而不出完整页（与融合基准裁定 4「空窗出完整页」冲突），已在锁里具名钉住且只许它一条；该命令同时挂场景 10，**场景归属待您裁**。
4. **两条过程型词的落盘文件名**仍是写命令口径（`记一餐_回执_…`），页头已是确认页——外观遗留，留您定夺。
5. `test/t275-营养饮水总览页.test.mjs` 有 2 条红（成因是 #560 去掉了屏上来源脚注、断言钉旧口径）；#560 已关且范围只到场景 01 ⇒ **这两条红目前无主**，只记账不代修。
6. **分布行窄槽裁切**（`ilife-block-dist-row-name` 78px 槽装 131px 文本、`overflow:hidden`）：属 D1 跨技能件，归另一张图（#629），不在本图写集。
7. **清单行内字节字样是上一代的**：`manifest.json` 每行 `check` 里嵌的「（NN,NNN B，盘上 X.html）」仍写着上一代墙的数字（46/46 与当刻盘面不符，实际一律更大）。这是已知缺陷：`restage.mjs` 只重算并覆写 `readings.字节区间`，不刷行内字样。⇒ **单格核字节请直接量文件本身**，权威读数看 `readings.字节区间`（= 73,948 – 171,446 B）＋ `gen-wall.mjs --check` 的自检行。同处已在 `gen-wall.mjs` 头部加更正注记。
8. **当刻 83 件产物无法从任何源目录重导**：清单 `source` 指的 `.scratch/t280/products/` 是**上一代**来源（83 件 sha256 与墙内复制件全不同），而真正重出这一代的 `.scratch/restage-83/products/` 已不在盘上。⇒ 复核只能靠 git HEAD ＋ 逐字节读数，不能从源目录重放。属取证链缺口，不阻塞验收。

---

## 五 · 本席（总工）在收口轮独立跑过的门（不采信任何席位自述）

| 门 | 命令 | 读数 |
|---|---|---|
| 70／83 词真出口锁 | `node --test packages/skill-calorie/test/t279-真出口用例.test.mjs`（runId `d1d0955a`） | **tests 99 / pass 99 / fail 0**（约 230 次真出口调用） |
| HELP 接线 | `node --test packages/skill-calorie/test/t278-唤醒词与工作流程.test.mjs` | tests 10 / pass 10 / fail 0 |
| 四处修复 | 直读当刻墙逐格判（`.scratch/conv/verify-wall.mjs`） | **5/5 PASS** |
| 墙自检 | `node docs/skills/skill-calorie/scene02-验收墙/gen-wall.mjs --check <墙目录>` | `83 格；链接 419 条；缺失 0 -> 可发`（exit 0） |
| 83 件可打开 | 逐件读第 0 字节＋结构判据 | **83/83** `<!doctype html>`、完整 `</html>` 收尾、0 条死链 |

---

## 六 · 这件票怎么收

按地图的进度契约：#280 是**负责人亲手跑**的票，所以停在 **95%** 等您三条结论：

1. 上面**两个 prompt** 在新 session 里跑出来了吗？回执给的是绝对路径吗？双击能开吗？
2. 两张墙滚完了吗？有没有挤／塌列／被藏起来的横向滚动？
3. 逐格缺陷清单的最后一列填了吗？

您把这三条（原话即可）回给我，我就把 #280 写到 100% 并关票；**在那之前它保持 OPEN**。
