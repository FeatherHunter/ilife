# #625 第四轮返修 A团 · 证据（预测族结论去复述＋胶囊去重）

> 票 #625 ｜ 地图 #161 ｜ 归属 `docs/skills/skill-calorie/`
> 先建骨架每节立刻落盘；不通读整份 HTML，用定向检索。
> 全部编译/测试/git 写操作经 `node tooling/run-locked.mjs --ticket 625 --lock-dir .scratch/locks-m158v2 -- <命令>`；判据只认 exit=0；长输出落盘只读尾行。

## §0 判定（本轮收口前＝未完成）

**判定：A团实施完成，B团待实施。**

## §1 写集（文件级，事前声明与事后对账逐行相同）

| 件 | 改动 |
|---|---|
| `packages/skill-calorie/src/render/trendPredictDocs.ts` | A团全部：`predictChips` 加 `short` 参（18/19/20 去第三枚）；Goal／Deficit／Stability／Forecast 四结论改综合不断复述数；18 判定句补主语；19 口径错字；Stability 末格补齐本地样式 `t625StabilityCss` |
| `packages/skill-calorie/test/t625-第四轮.test.mjs`（新） | 9 条断言（胶囊／结论／判定句／错字／补齐规则＋作用对象） |
| `docs/skills/skill-calorie/t625-第四轮-证据.md` | 本件 |
| `packages/skill-calorie/AGENTS.md` | 仅 `--sync` 派生（trendPredictDocs 1166→1190） |

**不碰**：`buildPredictDoc`／`buildSimCutDoc`／`buildSimTargetDoc` 的结论（t569 前缀针＋t620 方向针＋92＋ 分数，动即高风险）；`trackTable` 列宽（R-17 140px 冻结）；区间单值显示（R-18 冻结）；21／22 缺口族（冻结）；`base-render/**`（公共层）；别席在途件。

## §2 对抗式审查（每个决策是否帮助 MAP 目的地）

| 决策 | 第一性原理 | 对抗式审查 | 结论 |
|---|---|---|---|
| 结论改综合不断复述数（18/19/20/14–17），保留 383 词针 | V3 ①是 17–23 第一拖分维；结论必须有数是 06 专针（518-③只查 06），其余页结论无数字内容针 | 反方：14–16（94–96）同构件连带改动，有回落风险。正方：只改结论 prose，卡／导航／表／口径一字不动，其余维度读数不变；全回归＋383 真跑为证 | 做 |
| predictChips 缺省 false，18/19/20 传 true | 眉标 `趋势分析` 被 shape-520:114 钉死，只能动胶囊侧；缺省 false 保 01–17 逐字节不动（V1 零风险） | 反方：分叉共享件行为。正方：缺省分支旧行为逐字节同（变异翻转缺省即红为证），新分支只三页用 | 做 |
| 19 口径不断行（t570 首匹配针），错字照改 | t570:150 取首个口径行匹配 `符号＝`＋`缺口取正数`；断行即红 | 反方：1016px 单行仍显挤。正方：针是冻结行为， cosmetic 让路；错字不占针位，改 | 错字改，不断行 |
| 20 末格补齐用本页本地样式，不动 t570IntakeCss | R-20 两列覆盖被 t570:125 钉死；族共用样式一动影响 14–20 | 反方：多一件本地样式。正方：`#sec-overview` 限定只吃本页；3 张顶格卡探针实证规则命中 | 做 |
| SimTarget／SimCut／23／21-22／17-表／1589／nth-child／V1④⑤／05 不动 | 各有冻结针（t569 前缀／t620 方向／R-17／R-18／R-59／t569 nth-child／#567 口径差／#505 豁免）或 92＋ 薄缓冲 | 反方：残余仍在，V 席还会扣分。正方：破冻结＝红机检换 taste 分，MAP 净亏；锁现况记 S3，下一轮由用户肉眼定夺 | S3（见 §5），不动 |

## §3 门禁（边跑边补 GATE-RUN，只领 exit=0）

- `tsc -b packages/skill-calorie --force` exit=0（干净；GATE-RUN runId=17e1e90d-117e-4069-a0e0-32dcba9e79ad）
- `node --test t625` 9/9 PASS
- 变异缺省翻转 → `14页胶囊保持三枚` exit=1 必红；改回 → 复绿
- 变异结论复述数 → `结论仍在复述缺口数：…有 300 卡缺口…` exit=1 必红；改回 → 复绿
- 全批 `t625+t620+383+shape-predict-520+466+t568+t569+t570+t573` 87/87 PASS exit=0（GATE-RUN runId=1ff886c2-87c9-4de5-809c-e1171ea9837c）
- 台账 `--sync`（trendPredictDocs 1166→1190 本票行；SYNC-VERIFY ok）后 `RESULT: 91/91 PASS`
- `gen-cli --check` GEN-CHECK PASS（129 命令）
- 探针 `.scratch/t625/probe-grid.mjs`：概览网格顶格卡 3 张（规则有作用对象）

## §4 未做项与下一手

- 教训：本轮一次 `edit` 用过短 old_string 切断了 `t573CrossCss` 的文档注释块（`node --check` 抓获，逐文件接回后复绿）。长注释块只许用跨函数级唯一锚。
- B团（报告族尾巴：27/28 状态列徽章、24-1R、日志浮层、26-4R/26-6R、31-7、29-1R、24-6R、1589 除外项、V1 ①②③微项）待实施。
- 31 页重出＋三门＋靶向重评（18/19/20/14–17 重点）＋双墙视觉逐格＋关票，均待 B团后合并一次。

## §5 S3（记账，不拦关票）

- S3-7（SimTarget 10–13 结论与卡同判）：t569 前缀针＋t620 方向针＋92＋ 薄缓冲，改即高风险；V2 Top1 转 backlog。
- S3-8（17 表宽）：R-17 140px 地板冻结，V3-17② 无解，锁现况。
- S3-9（1589 单值）：R-18 零宽折叠冻结，V2 Top2 无解，锁现况。
- S3-10（23 全页）：R-59 同字节架构，动 23＝动 01–05，锁现况。
- S3-11（21/22 图字号）：base-render charts 层，另开公共票，#625 不碰。
- S3-12（V1④ 断点／V1⑤ 发丝线）：#567 口径差裁定＋#505 豁免＋t514，锁现况。
- S3-13（05 自定义字样／19 单行 1016px／17 胶囊）：micro／针冲突／共享件保护，锁现况或接受残余。
- S3-14（nth-child 28px）：t569:161/164 钉死，锁现况。
