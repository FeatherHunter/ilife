# #602 · 重墙证据：39 页按当刻源码重出 ＋ 两墙与索引重出 ＋ 自检正反例

**一句话判词**：#577 的 N1（读数卡／分布块补可见标题）漏了 **23／24** 两页（目标两页的 `#sec-figures` 只有裸 KPI 卡格、块内无可见标题）；
本轮把头两页补上可见 h2「数值对照」（与页内导航同源），**判据件先盖住这两页（改坏必红）**，
再在**一次持锁窗**内按当刻源码把 39 页全部重出、两墙与索引同步重出，自检正反例走完：
N1 由 **19/21 FAIL** 转为 **21/21 PASS**，其余 N2–N8 读数两轮逐字相同（本轮只动了这一处）。

根因链（判据单源写错 → 施工照枚举落 → 判据件也没盖目标页）与文档更正见 §一、§二；
机器读数见 §三–§六。全部日志住在 `.scratch/t602/`（该目录被 ignore、不提交，路径照写以便复跑）。

---

## 一、改的是什么（源码落点与改法）

| 项 | 事实 |
|---|---|
| 落点 | `packages/skill-calorie/src/render/sportDocs.ts:422`（`buildExerciseGoalDoc` 的卡片表第 2 项） |
| 改前 | `card('sec-figures', '数值对照', goalFigures(v, goalTotal))`——块内只有 `renderKpiGrid` 的裸卡格，无题 |
| 改后 | `card('sec-figures', '数值对照', '<h2 style="margin:0;font-size:15px;font-weight:700">数值对照</h2>' + goalFigures(v, goalTotal))` |
| 题词来源 | 与**页内导航同源**：`card()` 的 `label` 既喂 `tocOf()`（导航胶囊）又作块题，两处不会走散（词＝该页既有导航词「数值对照」） |
| 位置与风格 | 题作 `<section id="sec-figures">` 的**首个子节点**，风格同档 15px／700——与同件 `:228`（`sec-kpi` 核心数字）、`:248`（`sec-dist` 类型消耗分布）同一写法 |
| 面 | 只影响 `calorie.view.exercise-goal` 两页（23 看今日运动 vs 目标／24 看本周运动 vs 目标）；汇总页与其余 37 页字节无关 |

**行数守恒（有意为之，报备）**：本件在场内已超 350 告警线（台账挂号行 `src/render/sportDocs.ts｜—｜434`）。
改法把说明并进既有那两行注释、修复本身不增行，**LF 434 → 434（逐字复核）**，
因此 `packages/skill-calorie/AGENTS.md` 的台账**不需要同步**（改前改后台账「当场实测＝434」与实况一致，门不陈化）。
那一件当场是**他席在途脏件**（`git status` 实测 ` M packages/skill-calorie/AGENTS.md`），本票禁区点名不许碰——
若强行涨行数就得改它、必与在途改动互踩；守恒改法是本票能在不越界前提下保持台账诚实的最小解。
（本件仍超线、拆法照 `#452`／`#523` 那条「按页切 `sportSummaryDocs.ts`／`sportGoalDocs.ts`」，属结构票，不在本票。）

## 二、判据先行：判据件盖住目标两页，改坏必红

判据件 `packages/skill-calorie/test/t576-n1n8.test.mjs` 的 `t576-N1` 补了**目标两页**断言（`:119–130`）：
每页断言 ① `#sec-figures` 块内有 h2「数值对照」、② 该题词与页内导航**同源**（`navTexts` 里 `sec-figures` 的文本＝数值对照）；
夹具 `goalView()` 覆盖今日（单日窗）与本周（7 天窗、周口径）两态。

**变异自检（两编译两测试同一持锁窗，`node tooling/run-locked.mjs --ticket 602 -- node .scratch/t602/变异-改坏必红.mjs`）**：

| 态 | 源件 sha256 前 12 | 编译 exit | 判据件 exit | 判据件逐字 |
|---|---|---|---|---|
| 改坏（撤掉那行 h2） | `bc4f68762eee` | 0 | **1** | `AssertionError [ERR_ASSERTION]: 目标页今日(23)读数卡缺h2数值对照：<div class="ilife-block-kpi-card-grid">…` |
| 还原 | `b37d6faff4ae`（＝改前） | 0 | **0** | `T576-N1 h2=9/9（含#602目标两页2/2）` |

⇒ **改坏必红、还原必绿**；变异态只在窗内存在，`finally` 里按备份还原并核 sha 后才继续（日志 `.scratch/t602/变异.log`、原文 `.scratch/t602/原文-改坏后判据件.log`）。

## 三、39 件体积与 sha256 前 12 位（重出后，落 `.scratch/t541/out`）

| 序 | 件 | 体积(B) | sha256[:12] |
|---|---|---|---|
| 01 | 01-记运动.html | 77786 | 8471b718ed0b |
| 02 | 02-记运动（含备注）.html | 78120 | d0953ac8373b |
| 03 | 03-记力量训练.html | 78398 | 91547524133a |
| 04 | 04-记有氧运动.html | 78458 | 218793f3a792 |
| 05 | 05-记日常活动.html | 78442 | 81960b2c7e07 |
| 06 | 06-补记运动.html | 77826 | a3fb1da94ed5 |
| 07 | 07-批量补记运动.html | 79140 | 3a9630716452 |
| 08 | 08-复制昨日运动.html | 75072 | f30e49e92d42 |
| 09 | 09-改运动记录.html | 75716 | 5cd2b4494ee5 |
| 10 | 10-改某日运动.html | 76909 | 398ada30ac2a |
| 11 | 11-删运动记录.html | 77604 | eaab8f83803e |
| 12 | 12-删某日运动.html | 87613 | 570804c7f93f |
| 13 | 13-批量删运动.html | 87684 | 9ce6ec47fe61 |
| 14 | 14-看今日运动.html | 77422 | 2c1da7562b5d |
| 15 | 15-看昨日运动.html | 77422 | 4175a4860f90 |
| 16 | 16-看本周运动.html | 85384 | a27ec5a54ccf |
| 17 | 17-看上周运动.html | 86415 | b894cbe23771 |
| 18 | 18-看本月运动.html | 88162 | a17046fd1ef5 |
| 19 | 19-看上月运动.html | 90116 | 90aab13ecbb5 |
| 20 | 20-看最近 7 天运动.html | 86636 | 4b5da00a330f |
| 21 | 21-看最近 30 天运动.html | 90821 | 520f52a1ed03 |
| 22 | 22-看某段时间运动.html | 130636 | 781df36ec96c |
| 23 | 23-看今日运动（vs 目标）.html | 74312 | 13e04685451a |
| 24 | 24-看本周运动（vs 目标）.html | 74391 | 6f9d2991176d |
| 25 | 25-看运动记录（有备注）.html | 77085 | 1fe7569c9a8e |
| 26 | 26-看运动记录（按力量筛选）.html | 77059 | 05574bed9feb |
| 27 | 27-看运动记录（按有氧筛选）.html | 82069 | 030b9f9ced90 |
| 28 | 28-看最近 60 天运动.html | 104376 | 3e55e877b2d0 |
| 29 | 29-看最近 180 天运动.html | 130617 | 27d71e19592c |
| 30 | 30-看最近 365 天运动.html | 130660 | a0855f5650e2 |
| 31 | 31-看运动类型分布.html | 100130 | 7f6a951c59b1 |
| 32 | 32-看力量训练总览.html | 99229 | a16844701554 |
| 33 | 33-看有氧训练总览.html | 102648 | 5aa877748d6f |
| 34 | 34-看运动趋势.html | 110433 | 3c2aa774b998 |
| 35 | 35-运动复盘（本周）.html | 98121 | f6444bd80e7d |
| 36 | 36-运动复盘（本月）.html | 98753 | d7c671d2d820 |
| 37 | 37-运动复盘（最近 90 天）.html | 101094 | cc4b5fe0b260 |
| 38 | 38-运动复盘（今年）.html | 99738 | 98d7fef5d892 |
| 39 | 39-运动复盘（自定义时间）.html | 100134 | 2538fef1b2b4 |

（表＝`node .scratch/t541/t577-核验.mjs` 的 `CHK <序> <件> bytes= sha= doc= charset= style=` 逐行读数，39/39 `doc=true charset=true style=true`；读数全文 `.scratch/t541/t602-核验-改后.log`。）

## 四、重出窗：一次持锁、按序四步

**窗内按序**（`node tooling/run-locked.mjs --ticket 602 --child-timeout-ms 1800000 -- node .scratch/t602/窗口.mjs`，
**GATE-RUN runId＝`cae981c1-d339-4d52-8c6a-7c241a18b927`**，waitedMs=0，窗内 `RESULT: … exit=0`）：

| # | 步 | 命令 | exit | 用时 | 读数（尾部逐字） |
|---|---|---|---|---|---|
| 1 | 编译 | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | 0 | 0.1s | （无输出） |
| 2 | 重出 01–13 | `node .scratch/t578/run13.mjs` | 0 | 22.5s | `乙：39 条 OK=39 BLOCKED=0`／`out 落盘件数=39`／`MINE13 n=13 exit0=13 complete=13 BAD=0` |
| 3 | 重出 14–30 | `node .scratch/t523/run17.mjs` | 0 | 22.7s | `乙：39 条 OK=39 BLOCKED=0`／`out 落盘件数=39`／`MINE17 n=17 exit0=17 complete=17 BAD=0` |
| 4 | 重出 31–39 | `node .scratch/t544/run9.mjs` | 0 | 22.4s | `乙：39 条 OK=37 BLOCKED=2`／`out 落盘件数=37`／`MINE9 n=9 exit0=9 complete=9 BAD=0` |
| 5 | 合入 | `node .scratch/t541/t577-合入.mjs` | 0 | 0.1s | `合入件数=39 认领序号=39 缺号=[] 重号=0`／`交付目录序号件=39 字节域=[74312,130660]`／`合入 RESULT=PASS` |

- 三源按票面指名：**01–13←`.scratch/t578/out`、14–30←`.scratch/t523/out`、31–39←`.scratch/t544/out`**；
  01–13 必须取 t578 那一源（t544 的落盘轮 `乙` 有两条词 **BLOCKED=2**——`09 改运动记录`／`11 删运动记录` 都要 `id`，
  该轮的种子面给不出，故 `t544/out` 只出 **37 件、缺 09／11**；逐件核对：`t544/out` 里对 39 件清单缺的正是这两件）。
  同脚本另有一轮实况读数 `甲`（产物不收）：`甲：39 条 OK=14 BLOCKED=25`——**落盘只认 `乙` 那一轮**（`out 落盘件数=37`）。
- 合入后 `t541-清单.json` 的 39 件**逐件在盘**（§三 表），合入只搬字节、不重出、不改写。
- 窗内每一步的原文：`.scratch/t602/窗-1-编译.log` … `窗-5-合入.log`。

## 五、N1 判据件：19/21 FAIL → 21/21 PASS（先红后绿留痕）

判据件＝`.scratch/t541/t577-核验.mjs`（与 `t576-n1n8.test.mjs` 同名同形的操作镜像，输入换成交付目录里的真产物；
N1 面＝`N1_SCOPE` 14…34 共 21 页，四选择器 `#sec-kpi`／`#sec-figures`／`#sec-dist`／`#sec-ratio`，块开头 300 字内无 h2／h3／题元素即无题）。

| 轮 | 命令 | N1 逐字 | 末行 | exit |
|---|---|---|---|---|
| 改前（旧产物，源码未修） | `node .scratch/t541/t577-核验.mjs` | `MARK N1 有题页=19/21 无题页=[23-看今日运动（vs 目标）、24-看本周运动（vs 目标）]` | `CHK RESULT=FAIL` | **1** |
| 改后（本窗重出） | 同上 | `MARK N1 有题页=21/21 无题页=[]` | `CHK RESULT=PASS` | **0** |

两轮日志：`.scratch/t541/t602-核验-改前.log`、`.scratch/t541/t602-核验-改后.log`。
改后 23／24 两件的体积与 sha（§三）＝`74312／13e04685451a`、`74391／6f9d2991176d`。

**其余 N2–N8 两轮逐字相同**（本轮只动了目标两页那块题，不见别的漂移）：

| 条 | 改前 | 改后 |
|---|---|---|
| N2 跨年轴 | `MARK N2 cross=2025-09-16\|2026-09-15 same=4` | 同左 |
| N3 页19 圆点 | `MARK N3 page19 circles=2` | 同左 |
| N4 导航同源 | `MARK N4 nav=12/12` | 同左 |
| N5 精度 | `MARK N5 受检页=26 小数卡=0` | 同左 |
| N6 截断说明 | `MARK N6 截断页=22,29,30 note<table=ok` | 同左 |
| N7 明细卡同层 | `MARK N7 disclosure=3/3` | 同左 |
| N8 眉标／单位／卡题 | `MARK N8 eyebrow=2/2 km=1 nok2=1（力量「力量训练总览」／有氧「有氧训练总览」）` | 同左 |
| 汇总 | `CHK-SUM 件=39 齐=39 缺件=0 缺件0->可发=true` | 同左 |

**#578 重跑读数（记族那一族）**：窗内第 2 步逐字 `MINE13 n=13 exit0=13 complete=13 BAD=0`（13／13 全落、无 BAD），
与同一票以前那轮（`.scratch/t578/gate-final.log` 一线）同形；本票不改记族任何件，读数只作在场景内对账。

## 六、两墙与索引重出 ＋ 自检正反例

**同目录同生成器、一次持锁窗**（`node tooling/run-locked.mjs --ticket 602 -- node .scratch/t602/墙窗.mjs`，
**GATE-RUN runId＝`3f3650c6-0099-4365-9344-ee9dd316f935`**，waitedMs=100097（等他席放锁），窗内 `exit=0`）：

| # | 步 | 命令 | 读数逐字 | exit |
|---|---|---|---|---|
| ① | 正例·手机墙 | `node docs/skills/skill-calorie/t541-墙生成器.mjs D:\ilife\.scratch\t541\out t541-手机墙.html 390` | `手机墙 t541-手机墙.html：39 格；链接 78 条；缺失 0 -> 可发` | 0 |
| ② | 正例·桌面墙 | 同上，`t541-桌面墙.html 1280` | `桌面墙 t541-桌面墙.html：39 格；链接 78 条；缺失 0 -> 可发` | 0 |
| ③ | 正例·索引 | `node docs/skills/skill-calorie/t541-索引生成器.mjs D:\ilife\.scratch\t541\out` | `索引 t541-索引.html：39 件；链接 41 条；缺失 0 -> 可发` | 0 |
| ④ | 反例·塞假条目 | `node .scratch/t541/t541-假条目.mjs add` | `清单 rows：39 -> 40；夹具件在盘上？false` | 0 |
| ⑤ | 反例·墙必红 | 同① | `手机墙 t541-手机墙.html：39 格；链接 78 条；缺 1 件 -> 99-不存在的页（反例夹具）.html（清单第 99 行：盘上没有这个文件）`＋`缺失 99-不存在的页（反例夹具）.html（清单第 99 行：盘上没有这个文件）` | **1** |
| ⑥ | 反例·索引必红 | 同③ | `索引 t541-索引.html：39 件；链接 41 条；缺 1 件 -> 99-不存在的页（反例夹具）.html（清单点名而盘上没有）` | **1** |
| ⑦ | 撤假条目 | `node .scratch/t541/t541-假条目.mjs remove` | `清单 rows：40 -> 39；夹具件在盘上？false` | 0 |
| ⑧ | 回绿·手机墙 | 同① | 与①逐字相同 | 0 |
| ⑨ | 回绿·索引 | 同③ | 与③逐字相同 | 0 |

**清单 sha256 三态**（防「撤了假条目却改了字节」）：改前 `7f12e7e25faf` → 藏假 `06bd13ab49bf` → **撤回 `7f12e7e25faf`（＝改前，逐字节回原）**。
自检总判：`自检 正例(39格/39格/39件)=true 反例必红并点名=true 撤回回绿且字节同=true -> PASS`（日志 `.scratch/t602/墙窗.log`，
九步原文 `.scratch/t602/墙-1-…` 至 `墙-9-…`）。

## 七、混版 → 统一说明

| 项 | 事实 |
|---|---|
| 以前为什么会「混版」 | 三个来源目录各存一轮产物：`#268 §2.1` 记过 `t541/out` 出现「14–30 是 06:29 重出版、31–39 还是 05:13 旧版」这种**跨构建**并置 |
| 本轮做法 | 三族在**同一个持锁窗**内按序重出（§四 表，13:22–13:24 之间），窗内**只编译一次**（step 1 `tsc -b` exit 0），三族产物都出自**同一次 dist 构建** |
| 交付目录怎么来 | `.scratch/t541/out` 的 39 件全部由第 5 步 `t577-合入.mjs` 从三源按族指名搬来（01–13／14–30／31–39），**没有一件是上一轮的残留** |
| 残留（如实记） | 逐族生成时刻差数十秒：页 01 的页内复制日志带生成时刻 `2026-09-16 13:22:37`、页 23 带 `2026-09-16 13:23:07`（实测；页 14／31 未见同类时刻串）——这是生成器**按生成时刻落盘**的设计，不是源码混版 |
| 窗内树况 | 窗内起始 `HEAD=0c105c8`，`packages/skill-calorie/src/**` 与 `scripts/**` 无脏件；他席在途脏件是 `packages/skill-calorie/AGENTS.md`（本票未碰）与 `packages/base-combos/src/present.ts`、`packages/skill-bill/src/record/*`（与运动 39 页无关，且 `tsc -b packages/skill-calorie` 未牵入） |

## 八、文档更正 ＋ 本票写集

| 路径 | 动作 |
|---|---|
| `docs/skills/skill-calorie/t268-终评-全批39-重跑.md:109` | N1 行枚举由「14–22、25–30、31–34（**21 页**）」更正为「**14–24、25–30、31–34（21 页）**」（原文留在行内括注），并在该表下补**原文＋更正＋依据**一条：依据①同票核验件 `N1_SCOPE` 14…34＝21 页、改前 `19/21`＋`FAIL`、补题后 `21/21`＋`PASS`；依据②复核席实测同一读数（无题页 21＝含 23／24） |
| `.scratch/t268/视觉终评-全批39-重跑.md` | 同步三处（该目录被 ignore、不提交）：§9-4 的 N1 行、§四 P1-2／P1-3 两条的页枚举、§2.3 那句「14–34 共 19 页命中」；都按「原文保留 ＋ 括注更正 ＋ 依据」写，逐页分数与其余读数一字未改 |
| 本票写集（只这四件进 git） | `packages/skill-calorie/src/render/sportDocs.ts`、`packages/skill-calorie/test/t576-n1n8.test.mjs`、`docs/skills/skill-calorie/t268-终评-全批39-重跑.md`、本件 |

## 九、未做项 ＋ 下一手

1. **未跑整包测试与整包门禁**：本票只编译 `packages/skill-calorie` ＋ 跑该包的 N1–N8 判据件；整包门禁归收口票。
2. **未碰的件（禁区）**：`packages/base-render/**`、`packages/skill-calorie/AGENTS.md`（他席在途）、
   `src/goal/resultDocs.ts`、`src/diet/**` 等他席在途件；真库全程只读（三源脚本只 `copyFileSync` 真库到临时副本）。
3. **墙／索引页不在 git 里**（落在被 ignore 的 `.scratch/t541/out/`）：本批的墙是给负责人滚的验收面，
   逐格缺陷清单仍待负责人填（`t541-逐格缺陷清单.md`）。
4. **下一手**：① 负责人滚两墙（`t541-手机墙.html`／`t541-桌面墙.html`）填逐格清单；
   ② `t541/out` 的 09／11 两页取自 t578 那一源（t544 取数面 BLOCKED），若日后统一源，需先解 t544 那两条写类词的取数缺口；
   ③ sportDocs.ts 仍在 350 线之上，拆法照 `#452`／`#523` 那条（按页切两件），待收口票认领。
