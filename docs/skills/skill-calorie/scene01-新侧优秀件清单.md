# 场景 01 主页 · 新侧优秀件清单

本件只盘点**新仓（`packages/`）里今天已经能用的东西**：主页这类结果型 HTML 是谁装出来的、哪些组件可以直接取用、哪些界面约定已经被裁定过、同一份主页上还缺什么。供「老新融合」的模板设计取用。

- 归属：`docs/skills/skill-calorie/scene01-新侧优秀件清单.md`（研究票，只 add 本件一个文件）。
- 地图：#162《卡路里场景 01 主页》。
- 行号口径：本仓件写「文件:行号」，指本件落笔时的内容；盘点期间 `src/home/homeDocs.ts`／`src/shared/copyArea.ts`／`src/render/html.ts` 等多件正被他人改动，行号可能漂移，结论所引均按内容核对过。
- 全程只读源码与文档，未改任何源码、测试、生成物。

## 一、主页当前区块顺序

出口：`packages/skill-calorie/src/home/homeDocs.ts` 的 `buildHomeDoc`（:89 起，今日总览族 5 词共用同一组装配）。文档包裹走 `../shared/docPage.js` 的 `assembleDocPage`（:165），图表页标志 `charts` 由「有没有可画的日子」决定（:100 起：`d.week.series` 里至少一天有摄入才加）。

拿到的数据是 `HomeData`（`src/home/home.ts:19`）：`date`／`daily`（今日摄入合计与水量）／三个目标与完成度（`calorieGoal`／`waterGoal`／`caloriePct`／`proteinPct`／`waterPct`）／今日缺口 `deficitToday`／本周窗口（`week.start`／`week.end`／`week.series`／`avgIntake`／`avgDeficit`／`loggedDays`）／连续记录 `streakDays`。

按区块顺序，它今天摆了四块：

| 序 | 区块 | 吃什么数据 | 出处 |
|---|---|---|---|
| 1 | KPI 卡片格（6 张，一张一读数） | 今日摄入＋目标与完成度／蛋白＋完成度／饮水＋目标与完成度／今日缺口／连续记录＋窗口内有几天有记录／周均摄入＋窗口起止 | `homeDocs.ts:92-99` |
| 2 | 折线图「每日摄入」（有条件出） | 窗口每日摄入序列，标签取日期后五位；参考线画在周均值上，标「周均」 | `homeDocs.ts:101-112` |
| 3 | 按日汇总表（5 列） | 日期／摄入／缺口／TDEE／目标；表注写窗口与「无记录日留空，不断 0」 | `homeDocs.ts:113-126` |
| 4 | 提示条（toast 形态） | 一句话「今日已超热量目标」或「热量在目标内」，详情写摄入／目标／缺口 | `homeDocs.ts:127-130` |
| 5 | 复制区 | 数据位（envelope＋度量投影）＋日志位（命令原文／数据来源／时间戳／版本）；不给区块标题 | `homeDocs.ts:131-164` |

头部三件套（标题／眉标／副标题）由 `assembleDocPage` 出：标题「今日总览 ＋ 日期」，眉标「calorie.view.home · 主页」，副标题写窗口、连续天数与有记录天数占比（`homeDocs.ts:165-172`）。5 条唤醒词（`src/home/routes.ts` 的 order 0／5／6／7／8）都落到同一条命令 `calorie.view.home`，区别只在窗口大小。

同件里另有一份**旧片段** `renderHomeHtml`（:56 起）：它自拼 `pageShell`（:23）＋自写 KPI（:32）＋完成度条（:41），走深色 token 内联样式（`base-paint` 的 `STYLE_TOKENS`，紫调强调色）；今天不走 live 出口，只被 `render-t8` 锁形状。

## 二、可直接取用的组件

件名前缀：区块层是 `base-paint/blocks`（源 `packages/base-render/src/blocks.ts`），控件层是 `base-paint` 主入口（源 `controls.ts`／`style.ts`／`text.ts`／`charts.ts`），整页与复制区是 `packages/skill-calorie/src/shared/` 两件。

| 件名 | 出什么 | 能填什么 | 出处文件 |
|---|---|---|---|
| `renderPageShell` | 单页容器＋眉标／标题／副标题三件套＋正文 | 标题（必填）／副标题／眉标／已拼好的正文；眉标空串、副标题空值都不写这一行 | `blocks.ts:159-183` |
| `renderKpiGrid`／`renderKpiCard` | KPI 卡片格（自适应列宽，最小 150px 一列，窄屏自动换行） | 每张：标签／值／单位／说明／状态徽章（`ok`／`warn`／`danger`／`empty`）＋徽章文案 | `blocks.ts:189-230`，样式区 :756 |
| `renderDataTable` | 语义表格（`table`／`thead`／`th`／`td`） | 列（键／中文表头／对齐三值）／行／表注（`caption`）／零行时的空态句；零行不画空表 | `blocks.ts:236-298` |
| `renderChartBlock` | 图表区块（8 种：柱／折线／环／进度／组合／迷你／仪表／散点） | 图种／图表入参／区块标题；折线可加参考线、断点连接、面积、峰值点等选项 | `blocks.ts:304-341`，入参表 `spec/charts.ts:69-151` |
| `renderListRows` | 三槽行列表（左／主／右），已完成行出删除线 | 行项（左／主文／右／完成态）／空列表时的空态句 | `blocks.ts:347-385` |
| `renderPreBlock` | 命令原文块（`<pre>`）＋可选复制按钮 | 命令原文（必填）／小标题／复制按钮 id 与文案；空命令抛错，不画空板 | `blocks.ts:391-420` |
| `renderDisclosure` | 折叠区（原生 `details`／`summary`，交互纯 CSS） | 折叠标题／内层已拼好的 HTML／是否默认展开 | `blocks.ts:493-511` |
| `renderParamForm` | 参数表单（静态标签＋输入框，零脚本） | 字段（名／中文标签／值／提示／必填）／表单说明；今天只支持这五样，只读与步进约束尚缺 | `blocks.ts:517-575` |
| `renderEmptyBlock` | 空态区块 | 一句话（必填）／引导句／图标／可选动作 HTML／区块标题 | `blocks.ts:577-591` |
| `renderCopyBlock` | 复制区（动作条） | 数据位（单格式文本，或三格式三份文本＋三行用途提示）／日志位／自定场景按钮／区块标题；与按钮同名的标题自动不出 | `blocks.ts:593-642` |
| `renderFeedbackBlock` | 反馈区块 | 提示 toast 和／或错误回执，至少其一；区块标题 | `blocks.ts:644-664` |
| `renderActionBar` | 按钮行本体（复制区下面那一层） | 复制数据／复制日志两颗＋自定场景按钮；有数据位无日志位时自动补一颗点不动的日志占位，两边界恒定 | `controls.ts:1361-1399` |
| `renderStatusBadge` | 状态徽章（四态） | 状态（`ok`／`warn`／`danger`／`empty`，非法值降级 `empty`）／文案 | `controls.ts:1405-1411` |
| `renderEmptyState` | 空态本体 | 图标／一句话（必填）／引导句／受信动作 HTML | `controls.ts:1415-1432` |
| `renderErrorReceipt` | 错误回执（失败标题＋修正重试指令，可带两份复制按钮） | 失败原因（必填）／重试指令文案／数据文本／日志文本／两颗按钮 id | `controls.ts:1443-1470` |
| `renderToast`／`createToastController` | toast 提示本体与排队控制器 | 五种图标（复制／成功／警告／危险／信息）／一句话／详情／徽章／动作按钮／计数／多行／代号码／存活时长 | `controls.ts:187` 起，`spec/controls.ts:190-222` |
| `copyText`／`createCopyRuntime`／`bindCopyAction` | 复制运行时（剪贴板优先，命令兜底，已复制态与提示） | 复制文本／是否静默／成功与失败两种提示文案／回调 | `controls.ts:341-437` |
| `buildSharedHelpersJs` | 页面运行脚本资产（复制、提示、菜单委派等） | 类名前缀／数据承载属性名 | `controls.ts:587` 起，`index.ts:17-28` |
| `buildStyleSheet`／`blocksCss` | 样式资产两块：控件层样式（含 11 个浅色 token）＋12 个区块样式；整页装配把它们拼进共享 CSS | 类名前缀（`blocksCss`） | `style.ts:71-88`、`blocks.ts:1131-1141` |
| `fillTemplate` | 占位符填充器（裸标记模板＋资产→完整文档） | 模板／资产（共享 CSS／共享脚本／图表脚本）／正文 | `template.ts`，`docPage.ts:17`、`:95` |
| `escapeHtml`／`cx`／`token` | 转义（五字符冻结表）／类名拼接（自动加 `ilife-` 前缀）／取深色 token | 待转义文本／类名若干／token 名 | `contract.ts:38-40`、`style.ts:49-56` |
| `assembleDocPage`／`metricsOf` | 整页装配（区块 HTML＋标题三件套→完整文档，图表页另带图表脚本）／度量投影 | 文档标题／正文标题／眉标／副标题／正文／图表页标志；另有一路「参数一行＋徽章＋结论摘要」的老 A 版式形态 | `src/shared/docPage.ts:77-113` |
| `copyArea`／`copyLog`／`notice`／`promptCopyArea`／`dataCopyArea` | 一页复制区的三样（prompt／数据／日志）＋日志六段入参＋静态提示块 | 区块标题／prompt 文本与小标题／数据（数据结构＋三种格式）／日志（命令原文／来源／影响行那行／时间戳／版本）／三样全空时的空态句 | `src/shared/copyArea.ts:101-187` |
| `buildDataText`／`buildLogText` | 复制文本序列化（数据三种格式／日志六段） | envelope＋`format` 三选一／日志六段字段 | `text.ts`，`index.ts:32` |
| `charts`／`buildChartsHelpersJs` | 八种图表的产出与图表运行脚本（图表页必须带） | 见图表区块一格 | `charts.ts`，`index.ts:37` |
| `nowStamp`／`buildErrorReceipt`／`statusCard`／`reconcileDisclosure` | 渲染时刻戳／失败回执整页数据／回执状态格／页尾对账折叠区 | 见各函数签名 | `src/render/receipt.ts:155`、`:212`；`src/shared/receiptParts.ts` |

## 三、已被裁定过的界面约定

以下每条都是**用户或编排者已经拍过板的口径**，主页照做即可，不必重新发明。

1. **一页复制区恒为「复制数据 ＋ 复制日志」两颗**，日志里带能照抄重跑的渲染命令。老技能当年也是一颗组件（`actionBar` 出两颗按钮）；本仓收在 `shared/copyArea.ts`。出处：#239（`docs/skills/skill-calorie/t239-delivery.md`）、#154 融合规范第 7 条（`t154-体重页面-老新融合规范.md`）。
2. **数据位的复制按钮是三格式菜单**：点开是纯文本／JSON／CSV 三选一，三项用途提示逐字取老仓（「粘贴给 AI / 自己看」「结构化存档」「表格导入」）。裁定出处：**#247**（用户 2026-09-12 决策 4A「恢复老仓原样」，`t247-delivery.md:4`）。同一份数据的三种合法表示由共用件序列化，页面只递一份数据。
3. **同一条约定的另一面（T351）**：「复制数据 ▾ ＋ 三选一菜单」不许带英文菜单项、不许把指标写成可见裸文本；融合方案明写要改成「复制数据／复制日志」双按钮，**以单格式数据文本落承载属性**。出处：**#351**（`t351-visual-fix-plan-20260914.md:12`、`:26`、`:67`）。今天页面上两种形态并存（走数据位的出菜单，直挂单格式文本的出双按钮），做主页时要挑定一种并记账。
4. **与按钮同名的区块标题一律不出**（只留动作不留说明文本）。这条有两个落点：共用件侧 `copyArea` 的标题去重（`copyArea.ts:97`、`:127`），公共层侧 `renderCopyBlock` 的兜底（`blocks.ts:636-637`）。出处：**#238**（`t238-delivery.md:63` 清单 9①）、#179 与 #336 的续案。
5. **缺值的可见文本一律写 `—`**；复制出去的数据保留原始空值（显示层与数据层两套口径，不许互相污染）。这条同时覆盖 KPI 位、表格单元、首条无基线时的差值。出处：**#395** 融合基准裁定 2（`t395-融合基准.md:83-90`，老正本 `body_composition_view.html:133-134`／`:342`）。
6. **空态不许只写「无数据」**：要带「怎么记第一条」的引导句。出处：**#395** 融合基准第二节序 6（`t395-融合基准.md:35`，老正本 `body_composition_view.html:354-356`）。
7. **没有可复制内容时出空态句、不出点不动的按钮**；有数据位就要补日志位，不留禁用态按钮。出处：**#154** 融合规范第 4 条（`t154-体重页面-老新融合规范.md:82`，落点 `copyArea.ts:40`、`:137-140`；主页横幅下那次补齐记在 `homeDocs.ts:11-14`，**#375**）。
8. **触控目标：窄屏 ≥44px／桌面 ≥40px**（桌面沿用公共层冻结值 `ACTION_BAR_DEFAULTS.minHeightPx = 40`）。出处：**#238** 票面裁定 1（`t238-delivery.md:116`）、#179 的读数与建议（`t179-ui-issues.md:47-50`）。
9. **响应式只有一档断点 640px**：只改内边距与标题字号（`blocks.ts:746-753`），表格不塌成卡片、交给横向滚动（`blocks.ts:813`、`:939`）；KPI 卡片格靠自适应列宽自动换行。出处：**#156** 新仓盘点（`t156-新仓HTML盘点.md:138`）。同一份盘点同时裁掉了一种说法：全仓 `@media print` 命中数为 0，**「打印友好」没有出处，不许写**（`t395-融合基准.md:54`）。
10. **图表页必须在整页装配时声明图表标志**，否则文档缺图表运行脚本；折线上的参考线走图表入参的参考线字段（主页的「周均」就是它）。出处：`docPage.ts:31`、`:84`、`homeDocs.ts:108`。
11. **空窗口／空库不返空页**：数据层一律抛缺失阻断，页面层不许拿空数组冒充正常。出处：**#395** 新保留清单第 7 条（`t395-融合基准.md:66`）；主页的落点是 `home.ts:57-59`。

## 四、新侧短板

同一份主页上「该有的东西没有」的逐条，一句话一条。

1. **完成度那三行的位丢了**：旧片段有热量／蛋白／饮水三条完成度条（`homeDocs.ts:67`），完整文档只把完成度塞进 KPI 卡说明里，没有可一眼比长短的条。
2. **首屏没有「还剩多少」**：KPI 有「今日缺口」并靠「正=缺口」解释符号（`homeDocs.ts:96`），却没有「今日剩余预算」这一格——预算页最常用的读数要用户自己减。
3. **超目标这件事只有一条静态提示条**：`notice` 是静态块，不带状态徽章；KPI 有 `status` 位却一处没用（`homeDocs.ts:92-99` 六张全空）。
4. **提示块的解释位是空的**：`notice` 的区块标题位没给（`homeDocs.ts:127`），一句话与详情悬在页中段，没有落点说明它评的是哪一格。
5. **失败面缺整页**：数据层抛缺失阻断时（`home.ts:57-59`），主页没有接 `renderErrorReceipt`／`buildErrorReceipt`，跑不通只剩命令行报错，没有失败回执整页（对照身体细节各页的用法）。
6. **窗口在页面上不可调**：主页不动 `renderParamForm` 的窗口表单，窗口只能靠命令参数或唤醒词换（`routes.ts` 的 order 5／6／7／8 落在窗口 7／30），结果页上看不出、也改不了 `windowDays`。
7. **30 天档的长表没有截断明示**：按日汇总表对窗口长度零上限（`windowDays` 限 1..90，`home.ts:50`），不像运动各页先写 100 条截断常量再进表。
8. **数值单元没写 `—`**：表格单元由公共层把空值渲染成空串（`blocks.ts:253-259`），与「缺值可见文本一律 `—`」的裁定不一致；同一页里 KPI 用 `fmt` 写 `—`、表格留白，两套口径同页并存。
9. **表格数字列没有对齐与等宽数字保证**：五档列都只标了右对齐字段（`homeDocs.ts:113-120`），表体没有表注之外的读数辅助。
10. **没有当前锚点**：首屏没有「最新一条／窗口峰值」这类锚点行，KPI 六格全是窗口统计量，读数口径与「结果页首屏＝锚点」的老页约定相反。
11. **同一页两套配色口径**：旧片段 `pageShell`（:23-30）走深色 token 内联样式（紫调强调色），完整文档走浅色区块样式（`--blue` 系）；同件里留着两套，谁被打开看到的不一样。
12. **窄屏只有横向滚动**：六列 KPI 格与七列表在窄屏不塌成卡片（裁定允许，但 #395 基准里「宽屏表格、窄屏卡片」的老页做法本页没有）。
13. **没有目录／锚点，也没有回顶**：长窗口下从头部走到复制区全靠手滚；页面本身也没有区块跳转锚点。
14. **周长表没有筛选与搜索位**：复制区之外的控件层今天没有筛选件；长窗口想单看某几天只能出页重跑。
15. **周均那两格在无记录时仍是 `—`，但没写清口径**：KPI 说明只写窗口起止（`homeDocs.ts:98`），没说这个均值是该窗口几天的均值（`avgIntake` 出自 `analysis/series.ts`）——「周均」这个词在窗口不是 7 天时会误导。
16. **`loggedDays` 与 `streakDays` 两个量同名不同义**：前者是窗口内有记录的天数、后者是自今往前的连续天数，页面上两处都只写「有记录」。
