# #565 复核（独立复核席）

> 被审对象：`b5fc9f9`（实现，3 件）＋ `cdfb939`（证据件补提交与暂存区复核，1 件），均未 push。
> 复核席只信自己的退出码与读数：编译、被审用例、自设探针、受控变异全部经
> `node tooling/run-locked.mjs --ticket 565-R[|‑MUT|‑MUT2] -- <命令>` 重跑（runId 见 §二／§三）。
> 复核席写集只有本件（`docs/skills/skill-calorie/565-复核.md`）与 `.scratch/t565-R/` 下的探针与日志；
> 源码只在受控变异里临时改一行，还原后 `precheck.ts` 的 SHA256 前 16 位回到 `d9396840c5deb2f8`（§三）。
> 实施者结论一律不采信——下面每条读数都是复核席自己跑出来的。

## 一、圈定范围

| 提交 | 件 | 在票面写集内 | 说明 |
|---|---|---|---|
| `b5fc9f9` | `packages/skill-calorie/src/goal/precheck.ts` | 是 | 唯一动过的源码件；`35 +++-`（＋23 行注释与新函数、−3 行调用） |
| `b5fc9f9` | `packages/skill-calorie/test/565-precheck-copy.test.mjs` | 是 | 新建用例（203 行，`test/*.mjs` 不在告警线扫描面） |
| `b5fc9f9` | `docs/skills/skill-calorie/565-证据.md` | 是 | 新建证据件（124 行） |
| `cdfb939` | `docs/skills/skill-calorie/565-证据.md` | 是 | 只补记第八节提交与暂存区复核（＋13 行） |

- **工作区与提交一致**：`git status --short -- <上面三件>` 无输出、`git diff HEAD -- <上面三件>` 无输出（核过两次：
  开工时与全部变异跑完之后）。
- **源码 diff 逐行核**（`git show b5fc9f9 -- packages/skill-calorie/src/goal/precheck.ts`）：只有四处——
  ①件头补一段 #565 说明；②import 加 `renderActionBar`／`renderPreBlock`／`CALORIE_COPY_ACTION`；
  ③件内私有 `promptRow()`；④`buildGoalPrecheckDoc` 的 content 数组里把原来的
  `copyArea({ prompt, data, log })` 拆成 `promptRow(v.prompt)` ＋ `copyArea({ log })`。
  页内三块（写入词／改前值／推荐）、「库内现值（改前基准）」表、页头文案、`weightBlock`／`fillForm`／`currentTable`、
  唤醒词／命令键／参数名／退出码**一行未动**；`src/goal/` 之外的件一件未碰。
- **禁区未越**：`packages/base-render/**`、`src/shared/**`、`src/render/**`、`src/home/**` 全不在 diff 里；
  既有测试一件未改（两件交付里一件是新用例、一件是证据件）；无 `package.json` 改动、无生成物改动。
- **提交链**：`b5fc9f9` ← `cdfb939`；复核时 HEAD＝`cdfb939`（本席开工时 `git log -1` 读到的）。
- 复核期间工作树里别席在途件一堆（`docs/skills/skill-calorie/scene05-验收墙/**`、`src/workout/**`、
  `src/exercise/**`、`src/render/planEditorPort.ts` 等为 `M`／`??`），复核席一行未碰、更未带进任何提交。

## 二、独立重跑（新 runId，只信自己的退出码）

| GATE-RUN | 命令 | exit | 摘要行 |
|---|---|---|---|
| `e5e9f85a-fba4-416d-9b7e-db0c2cf5e21b` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | 0 | 空跑（判定最新、tsbuildinfo mtime 未变）——按票面口径这一行**不算**「真跑过」 |
| `724e5dbc-3d80-4cdd-829e-8120fb99140a` | 同上（先 `touch` 源件 mtime 再跑） | 0 | 真编译：tsbuildinfo `01:20:15.054 → 01:22:17.941` 已更新；`dist/goal/precheck.js` 里读到 `promptRow`／`copyLog`（编译产物与源件一致） |
| `acfc63cc-e1e7-48b2-9381-0565ef9cf2a3` | `node --test packages/skill-calorie/test/565-precheck-copy.test.mjs` | 0 | `tests 5 / pass 5 / fail 0`（五组判据逐条 ✔） |
| `f57cda04-b995-4068-8015-354b73245e91` | `node .scratch/t565-R/mutate565R.mjs`（受控变异①，§三） | 0 | 改坏态 compile=0／用例=1（pass=4 fail=1）／no-wake 探针=0；还原态 compile=0／用例=0（pass=5 fail=0）／探针=0 |
| `a64d0358-8f43-4d45-91d5-5f4ffb49738f` | `node .scratch/t565-R/mutate565R2.mjs`（受控变异②，§三） | 1 | 本件自身的自检读数：改坏态 compile=0／用例=1（pass=2 fail=3）／id 探针=1；还原态三者全 0（§三记录两处「预期不符」，均已核清） |
| `4c0c6a44-89fa-4a76-af1d-b0516d65d8c9` | `node packages/skill-calorie/scripts/check-warning-line.mjs` | 0 | `RESULT: 87/87`＋`PASS: 告警线台账齐全且与实况一致` |

编译口径按票面写死：入口一律 `node node_modules/typescript/bin/tsc -b packages/skill-calorie`（本机无
`node_modules/.bin`）。第一趟空跑不代表没编译过——第二趟按「源件 mtime 更新则必重编」的口径跑出真编译，
否则本席下面的探针读数就可能落在**旧 dist** 上。

## 三、新试法（自设，打被审用例覆盖不到的面）

### 3.1 复核席探针 `.scratch/t565-R/no-wake-probe.mjs`（不带 wake 那一档，真 CLI ＋ 临时种子库）

被审用例的 5 组判据**全部只跑带 wake 的 11 条**，对「不带 wake 进来」那一档一条判据都没有——而实施者
自报的那处回归（`--params '{"profile":"cut"}'` 时 prompt 是空串、`renderPreBlock` 拒空串 exit 4）正落在
这一档。探针换四处口径：①exit 0 ＋ 完整文档四断言；②按钮面貌按 **actionId × 能不能点**读（这一档恰 1 颗
可点 `ilife-copy-log`，多一颗同名通道或那颗禁用态占位回来即红）；③页上不得有空 `<pre>`（空 prompt 不许落成
空预览框）、不得出现 prompt 预览块与某条词的原文；④**二次跑得稳**：同一份库同一条命令跑两趟，剥掉复制载荷
与时间戳后两个产物必须逐字节相同（带时间的页面上不该混进两次不一样的东西）。

```
NO-WAKE-PROBE 绿：copy-btn=1（可点复制日志）空 pre=0 页内三块齐 完整文档=true（bytes=73882）
```

### 3.2 复核席探针 `.scratch/t565-R/id-probe.mjs`（11 件，冻结表原值）

被审用例只数「几颗按钮」「载荷里有没有那几句」，**不读** `data-action-id` 的原值。这一条读的是
「11 件各 2 颗、逐件 `[ilife-help-copy-prompt, ilife-copy-log]`」，并先自证冻结表出处
（`src/render/copy.ts` 的 `HELP_COPY_ACTIONS.prompt`）认得出来。

```
ID-PROBE 绿：11 件各 2 颗，actionId 逐件＝[ilife-help-copy-prompt, ilife-copy-log]（冻结表原值，未自造）
```

### 3.3 受控变异①（`.scratch/t565-R/mutate565R.mjs`，持锁窗口 `565-R-MUT`）

变异点＝`promptRow()` 里那一行由**日志位**换回**数据位**
（`copyLog: {…}` → `copyData: {…}`，一处实参名）——公共层 `controls.ts` 的 #336 兜底随即补回那颗禁用态
「复制日志」，本票要修的两条同名通道原样回来。

| 状态 | 编译 | 被审用例 | no-wake 探针 | 红行 |
|---|---|---|---|---|
| 改坏态（sha `92f5522678c0e315`） | exit 0 | exit 1（pass 4／fail 1） | exit 0 | `✖ #565 ② 11 件产物各：「复制日志」恰 1 次、copy-btn 恰 2 个、无禁用态复制钮` |
| 还原态（sha `d9396840c5deb2f8`） | exit 0 | exit 0（pass 5／fail 0） | exit 0 | — |

`RESTORE OK（sha 逐字节回到 d9396840c5deb2f8）`。**读数含义**：本票那个缺陷本尊（多一条同名通道）
被用例②咬得住——这一处判据不是永真；同时它照不到「不带 wake」那一档（3.1 补上）。

### 3.4 受控变异②（`.scratch/t565-R/mutate565R2.mjs`，持锁窗口 `565-R-MUT2`）

第一轮先做了一次**空对照**：把 actionId 换成字面量 `'ilife-help-copy-prompt'`——它**逐字等于**冻结表的
值，产物逐字节不变，于是用例与两个探针都绿（这一轮恰好证明两件事：判据没被字面量假阳性糊住，实施者的
`CALORIE_COPY_ACTION.actionId` 取值就是 `ilife-help-copy-prompt`）。第二轮换成**真的不一样**的字面量
`'copy-prompt'`：

| 状态 | 编译 | 被审用例 | id 探针 | 读数 |
|---|---|---|---|---|
| 改坏态（sha `3925da238a5be0c4`） | exit 0 | exit 1（pass 2／fail 3） | exit 1 | 用例红 3 条（②③⑤：按钮 id 一改，`data-t` 取值与「上屏可见文本」跟着变）；id 探针点名 11 件里 actionId＝`["copy-prompt","ilife-copy-log"]` |
| 还原态（sha `d9396840c5deb2f8`） | exit 0 | exit 0（pass 5／fail 0） | exit 0 | — |

`RESTORE OK`。复核席如实记两处「预期不符」：3.3 里本席原以为用例照绿（实测它红），3.4 空对照那轮同理——
两处都是**复核席自己的预判**错，不是被审件的读数错；修正预判后重跑的读数（上表）才是结论。
按票面「红行指向不属于你的文件就记录后重跑一次」，这两轮红行都指向本席自己的变异与自检，不存在同仓多席互踩。

### 3.5 真人会看的那个面：把 11 件重出到临时目录，用本席自己的判据核

「墙」（`docs/skills/skill-calorie/scene06-验收墙/`）是 `produce.mjs` 的静态产物，本票写集不含该目录，
故复核席用与 `produce.mjs:156` 同形的命令串（`calorie.view.goal-wizard --params '{"wake":"…"}' --html …`）
在临时库里重出 11 件到 `.scratch/t565-R/out/`，再按**负责人那条探针的原口径**（`「复制日志」`次数 ＋
`class="…copy-btn…"` 元素个数）逐件数：

```
== 墙上旧产物：33 件，不符合「1 次／2 个」的 11 件    ← 缺陷仍在墙上（静态产物，重生成不在本票写集）
   定营养目标-预检.html  「复制日志」=2 copy-btn=4 …（11 件全是 2／4）
== 本席重出产物：12 件，不符合「1 次／2 个」的 1 件  ← 那一件是刻意重出的「不带 wake」档（1／1，应然）
WALL-CHECK 墙不合格=11 ／ 重出不合格=0（11 件带 wake 的）
```

逐件按钮一览（`.scratch/t565-R/scan.mjs` 自设判据，文档顺序）：

| 产物 | copy-btn | 钮1 | 钮2 | 「复制日志」可见 | 数据菜单残留 | 完整文档 |
|---|---|---|---|---|---|---|
| 11 件带 wake 的预检页（逐件全等） | 2 | `复制指令` `ilife-help-copy-prompt` 可点、有载荷 | `复制日志` `ilife-copy-log` 可点、有载荷 | 1 | 0 | true |
| 无wake档-预检.html | 1 | `复制日志` `ilife-copy-log` 可点、有载荷 | —（上部无 prompt ⇒ 不出指令区） | 1 | 0 | true |

可见文本层面的新旧对照（`.scratch/t565-R/compare.mjs`，墙上旧件 ↔ 本席重出件，剥掉复制载荷后逐段比）：

```
prompt 预览块：旧 253 字符 ／ 新 253 字符 → 逐字相同
可见文本段数：旧 66 ／ 新 61
旧有新无（5）：["复制日志","复制数据","纯文本","JSON","CSV"]
新有旧无（0）：[]
按钮（旧）：["复制指令［可点］","复制日志［disabled］","复制数据［可点］","复制日志［可点］"]
按钮（新）：["复制指令［可点］","复制日志［可点］"]
```

即：页上消失的正是**多出来的那一颗**「复制日志」（旧第 2 颗，禁用态、无载荷）与**页底的数据通道**
（「复制数据」开合器 ＋ 三格式菜单三项）；页头文案、页内三块、「库内现值（改前基准）」表、
prompt 预览块原文一段没少、没有别的新增文字。留的是「上部一颗复制指令 ＋ 页底一颗复制日志」，
与同墙其余 22 件（日志钮都在页底）同层级——负责人「留哪一处由你按页内层级定」那一句给的自由度用得住。

### 3.6 被审用例自身的盲区（复核席如实记账，只记 S3）

1. **只跑带 wake 的 11 条，不跑不带 wake 那一档**——实施者自报的那处回归（空串）恰好这一档；本席 3.1 的探针补上
   （现绿的读数见 3.1）。这条不是缺陷，是覆盖面的记账。
2. **不读 `data-action-id` 原值**——`copy-btn` 只数个名，actionId 换成裸字面量它看不到（3.4 第二轮实测它仍红，
   是因为载荷与可见文本跟着变，不是因为它直接看了 id）。本席 3.2 的 id 探针补上。
3. **不跑二次**——同一输入两次跑是否逐字节稳定（除时间戳）它覆盖不到；本席 3.1 探针的第④条补上。
   三条都只记 S3（范围外发现，不影响本票判定），要落进用例由后续票认领。

## 四、缺陷逐条（本票引入 ／ 本票范围 ／ 范围外）

| # | 类别 | 缺陷 | 证据 | 处置 |
|---|---|---|---|---|
| 1 | **本票引入** | 这页**不再出「复制数据」三格式菜单**：页底数据位一撤，第二颗按钮没了，顺带把「复制数据 ▾ ＋ 纯文本／JSON／CSV」这条通道一起撤了（旧有新无那 5 段＝它那 5 个可见文本段） | §3.5 新旧对照；实施者 §七-2 已自报 | 判**已声明并留作负责人裁定**的那个取舍：票面要「一页只留一条『复制日志』通道」＋「copy-btn 恰 2 个」，三条通道装不下；技术原件没丢（都住在那一颗日志的六段载荷里）。**不作为 S1**，但负责人若要求保留数据通道，判据与版面都得分家另开票 |
| 2 | 本票范围 | `promptRow()` 的注释与 `buildGoalPrecheckDoc` 里那条新注释都写「**一颗「复制指令」**」，而这一颗实际装在 actionBar 的**日志位**、带的是日志的 id／文案（上屏仍是「复制指令」，因为 `HELP_COPY_ACTIONS.prompt.label` 就叫「复制指令」）——注释与实现措辞口径不一 | 源码 `precheck.ts:140-155`／`:218` 对照 `:151-154` 的实参 | 不是用户可见缺陷（上屏正确），不改实现；建议后续票把那两处注释改成「指令区的一颗（走日志位）」 |
| 3 | 本票范围 | 被测源码件头（`precheck.ts:1-24`）与共用件 `shared/copyArea.ts:1-29`、`render/copy.ts:1-17` 的件头都还写着「上部 prompt 区经 `promptCopyArea`／`renderActionBar({copyData})`」的**旧接线** | 三件头注释 | 非缺陷（注释），本轮未改；建议由同族后续票一并刷新 |
| 4 | 范围外发现（S3） | **验收墙那 11 件产物仍是改前的模样**（两条日志／copy-btn=4）：`docs/skills/skill-calorie/scene06-验收墙/` 不在本票写集 | §3.5 墙上读数（33 件里 11 件不合格） | 实施者已在证据 §七-1 如实记账；建议下一手持该目录的票跑 `node docs/skills/skill-calorie/scene06-验收墙/produce.mjs` 刷新。**本席不作 S1**：墙上静态产物不是「用户运行代码后看到的样子」，且不在写集 |
| 5 | 范围外发现（S3） | 工作树里别席在途件（`scene05-验收墙/**` 有件被删／改、`src/workout/**`、`src/exercise/**`、新建 `src/render/planEditorPort.ts` 等）与本票无关 | `git status --short` | 复核席一行未碰；只记着，不判 |

**S1 逐条核**：用户可见回归——除 §四-1 那个已声明的取舍外无（无选项确实少了，但那是「两条日志并一条」这条要求的
直接后果，且实施者明写、复核席亲眼核过）。证据造假或私自放宽——证据件里的 GATE-RUN 行逐条与
`.scratch/locks/gate-runs.log` 对得上（本席抽核 `b5fc9f9` 交付链上的 6 条：`b39b1290`／`ff2b2ecb`／`e64b7391`／
`d433be14`／`2817be9e`／`6fb00685` 均在册且 exit 与摘要相符）；本席自跑的读数与实施者不一致处只有两处
「复核席自己的预判」（§3.3／§3.4 已记），无一处是被审件读数被改。动禁区文件——无（§一）。数据风险——无（全程临时库）。

## 五、判定

**PASS，87 分。**

| 维度（满分） | 得分 | 依据 |
|---|---|---|
| 契约一致（30） | 28 | 11 条词逐条真跑 exit 0、产物「复制日志」恰 1 次／copy-btn 恰 2 个／载荷非空、完整文档四断言——本席独立重跑与重出各得一遍同结论；留哪一处按页内层级定得住（与同墙 22 件同层级）。扣 2：本页少了数据通道这一条取舍虽然已声明，但同一张票里没有负责人的裁定意见 |
| 证据真实可复现（25） | 24 | 证据件里 6 条 GATE-RUN 抽核与锁日志逐字对得上；读数脚本（`sweep11.mjs`／`diff-old-new.mjs`／`mutate.mjs`）在 `.scratch/t565/` 里都在，本席按同法重跑得同量级读数（字节数逐条相同）。扣 1：证据件引用的 `MUT-READING` 那两行是脚本自报，未附脚本原文（本席另跑自己的变异才把这一环补实） |
| 新旧对照（20） | 19 | 本席自己的 `compare.mjs` 给出「旧有新无 5 段／新有旧无 0 段」＋按钮逐颗对照，与实施者的正证同结论；prompt 预览块逐字相同。扣 1：`<h3>` 级小标题在两边都读不到（本席判据抽不出「给 AI 核对的技术原件」这类块名，只能确认不是本票删的） |
| 工程红线（15） | 15 | 只动 `src/goal/precheck.ts`；既有测试一件未改；编译入口写死且真跑（tsbuildinfo `01:20:15.054 → 01:22:17.941`）；告警线 `RESULT: 87/87` PASS（240 LF < 350，台账不用动）；提交路径限定、别席在途件未入提交；变异两轮均逐字节还原（sha `d9396840c5deb2f8`） |
| 文档同步（10） | 9 | 证据件七节齐、未做项如实（含墙上 11 件待重生成、数据通道取舍）；扣 1：`SKILL.md` 那句「页面摆库内现值／推荐值与依据／改前→改后对照…」不涉复制通道，无需改，但同族件头注释（§四-3）未同步刷新 |

## 六、未做项与下一手缺什么

1. **验收墙 11 件未重生成**（不在本票写集）：下一手持 `docs/skills/skill-calorie/scene06-验收墙/` 的票跑
   `node docs/skills/skill-calorie/scene06-验收墙/produce.mjs`，再按负责人那条探针复扫（应得 33 件全「1 次／2 个」）。
2. **数据通道那个取舍**要负责人一句话：要保留「复制数据」三格式菜单就得改判据（copy-btn 不再恰 2）或另开一页放它。
3. **同族件头注释**（`precheck.ts` 件头、`copyArea.ts` 件头、`render/copy.ts` 件头）与 `promptRow()` 的措辞口径
   建议一并刷新，属后续票。
4. 复核席探针（`no-wake-probe.mjs`／`id-probe.mjs`）现住 `.scratch/t565-R/`，未入仓——要不要落成 `test/*.mjs`
   用例由后续票裁。

## 七、复核件自己的入档

```
提交 2270b32  复核(565)：独立复跑判PASS——编译真跑过／用例5/5绿／两处受控变异必红且逐字节还原／自设探针补no-wake与id盲区／11件重出产物1次2个
  docs/skills/skill-calorie/565-复核.md | 185 ++++++++++++++++++++++++++++++++++
  1 file changed, 185 insertions(+)
GATE-RUN runId=b60e6c77-97f1-4a09-bb21-180c032badc9 cmd="git commit -m 复核(565)… -- docs/skills/skill-calorie/565-*"  exit=0（一次性路径限定＋暂存 glob，别席在途件一件未带）
```

本件入档分两条：`2270b32`（复核件本体）＋ 紧随其后那条「本件补记入档」的文档提交（只动本件）。
两条的提交 sha 以 `git log --oneline -2 -- docs/skills/skill-calorie/565-复核.md` 当场读到的为准——
本段不预写后续提交自己的 sha（写了就得为改它再改一次本件，那正是「注释与实现走散」的老毛病）。

暂存区复核（提交前逐行）：`docs/skills/skill-calorie/565-复核.md`——只有复核席这一件；本席全程未 `git add -A`、
未 amend、未 rebase、未 push。
