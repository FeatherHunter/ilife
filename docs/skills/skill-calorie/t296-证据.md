# #296 交付证据：纪律文本与恢复开工稿（定稿）

票：[#296](https://github.com/FeatherHunter/ilife/issues/296)（地图 [#293](https://github.com/FeatherHunter/ilife/issues/293) 最后一票）
前置：`#295` 已关闭（100%，单席定点复审 PASS · 均分 94／红线 15/15），本票解阻后定稿。
起草期过程稿（不入仓）：`.scratch/orchestrator/t296-票面草稿.md`。

## ① 定稿落点（原三条待定项处置）

| 原待定项 | 裁决 | 结果 |
| --- | --- | --- |
| `AGENTS.md` 两行指针 | 照草稿加 | **已应用**（diff ＝ `### 命令登记` ＋ 一行正文 ＋ 空行，共 4 行；`git diff --numstat` ＝ `4 0`） |
| 纪律正文归位 | 照 `doc-homes` 改到 `docs/agents/` | `docs/skills/skill-calorie/t296-纪律正文.md` → **`docs/agents/命令登记纪律.md`**（`git mv`，**一处不留两份、无转发文件**） |
| 照抄说明落点 | 独立成件 | `docs/skills/skill-calorie/t296-照抄说明.md` → **`docs/agents/命令登记纪律-照抄说明.md`** |
| 广播稿落点 | 留原地 | **`docs/skills/skill-calorie/t296-广播稿.md`**（本图恢复开工的产物，票级证据；体裁维持恢复开工清单） |

`docs/skills/skill-calorie/t296-AGENTS.md-指针.md` 同步改成「已应用」状态记录（含已追加文本与归位说明）。

## ② 逐条：四件事写到哪

| # | 定调要求 | 落在哪 | 判据 |
| --- | --- | --- | --- |
| ① | 新命令怎么写（`src/<能力>/commands.ts` 加一行声明，实现住能力目录） | 《照抄说明》①；正文「形状一」 | 六字段写全；`example` 必填非空、**照抄即能跑**、与 `--params` 形状绑定 |
| ② | 共用位什么时候才能动、动前要不要抢锁 | 《照抄说明》②（三种情形对照表）；正文「形状二」 | 日常加改＝零接触；新增能力／框架变更＝**先抢锁＋广播**；名单**以 `pnpm gen:check` 逐件输出为准**（不抄名单） |
| ③ | 撞车怎么办 | 《照抄说明》③；正文「形状一」末段 | 声明写集；跨图撞车**停下报告、不自行改别人的行**；点明 `legacyCommands.ts`／`routing.ts` 两处全场共用 |
| ④ | 恢复开工前动作 | 《照抄说明》④；广播稿第一节 | 三件：`git pull`；`git status --short` **出现不属于你的路径就停下报告**；确认 `.scratch/locks/gate.lock` 不存在 |

**④ 由两件变三件**是本票起草期的实证增量（编排者裁定入清单）：写稿期间我的 `git status` 露出另一个席位的两件未提交改动，该场景已写进两份文本。

## ③ `#295` 关闭后新增的三条判据（逐条落在哪）

三条都写进 **`docs/agents/命令登记纪律.md`「形状二」末段**（第 1、2 条）与 **「三 · 判据」第 4 条**（第 3 条）：

1. **陈旧守卫的边界**（形状二）：印记缺失时**不判陈旧**，打 `GEN-STAMP WARN` 后放行，取向＝**不假红优先**；
   CI 上不会踩（`pnpm build` 无条件重打印记且排在 `gen:check` 之前）；
   被它挡住时**不许删文件、不许改门**，照医嘱重建（重建要走 `pnpm build`，打印记挂在 `build` 里）。
2. **守卫的覆盖面缺口**（形状二）：新增能力目录后改其声明内容，报的是下游 `GEN-CHECK FAIL` 而**不是** `GEN-STALE FAIL`
   （印记里还没这条，无从对照）；**所以「没报陈旧」不等于「没问题」**，判定权在下游那道门；
   把「印记缺该声明且它已有编译产物也算陈旧」留给后续动生成器的票。
3. **加／删命令后的自检**（判据第 4 条）：跑一次独立验收探针 `node docs/skills/skill-calorie/t293-验收-命令自治.mjs`，
   三桶（① 已派生／② 受纪律约束的 `routing.ts`／③ 漏网面）里 **`UNACCOUNTED` 必须为空**。

**三条判据的核实方式**：不照抄派单措辞，逐条回到实现读——`packages/skill-calorie/scripts/gen-cli.mjs` 的
`staleDeclarations()` 与文件头、根 `package.json` 的 `build`（＝`tsc -b && node … gen-cli.mjs --stamp`）。

## ④ 交付前自检（四项，全部实跑）

| # | 自检项 | 结果 |
| --- | --- | --- |
| 1 | 全仓 grep 旧路径 `t296-纪律正文` 应为 0（除过程稿）；纪律正文恰一份 | **PASS**：**陈旧交叉引用 0 处**——命令 `git grep -n "t296-纪律正文\|t296-照抄说明" -- . ':(exclude).scratch' ':(exclude)docs/skills/skill-calorie/t296-证据.md'` 得 **0 命中**；余下 3 处命中**全在本文件「① 定稿落点」的搬家记录里**，那是搬家留痕、不是引用（连本行判据文字本身也计入，故把它一并排除）。`git ls-files "*纪律正文*"` **空**，无转发文件；`docs/agents/命令登记纪律.md` 恰一份 |
| 2 | 本票改动不含任何代码路径 | **PASS**：`git show --stat HEAD` 只有 6 件文本（`AGENTS.md` ＋ 5 件文档，含 2 处 `git mv` 改名）；按 `^(packages/|src/|test/|scripts/|tooling/)` 过滤 **0 命中** |
| 3 | `AGENTS.md` diff 只有指针 | **PASS**：`git show --numstat HEAD -- AGENTS.md` ＝ `4 0`（1 空行 ＋ `### 命令登记` ＋ 1 行正文 ＋ 无其他） |
| 4 | 照抄说明四件事齐全、不含会过期的清单 | **PASS**：四件事标题齐全；CJK 约 1300 字（≈0.9 页，一页以内）；**无行数基线／条数上限／共用位名单**。**如实说明**：文里还有别的数字（「9 条命令」「三桶」「436 条路由」「19 字段 sha」等），它们**都能指得出权威出处**（`src/weight/commands.ts`／`t293-验收-命令自治.md`／`routing.ts` 本身），且**没有一个被写成规矩**——初稿那句「唯一数量词是 9 条命令」不实，已按复核席实查改正 |

## ④-补 · 复核 PASS（93／红线 15/15）后的四条 S2 定点修复

复核报告：`docs/skills/skill-calorie/t296-复核-单席.md`（`af3184f`）。它做了「可照抄性实测」——挑 `calorie.view.ranking`，
只读本票文档写出搬迁清单再与真仓核对，差异 3 处。四条都只改所指句子，不扩大：| # | 改前 | 改后 |
| --- | --- | --- |
| S2-1 | 同批文件在正文里叫「共用位」（第二/三/四段）、在《照抄说明》里叫「共用位」、在设计正本里叫「汇总位」，一路混用 | 统一为「**共用位**」；正文标题下加一行「叫法」：说清设计正本（`命令登记与分派-方案.html`／`-冲突为何消失.html`）里的「汇总位」就是它，本纪律只用「共用位」。正文里旧词只剩「叫法」那一行的引述 |
| S2-2 | 只说「随搬迁数下调棘轮上限」，**没点名哪两个文件**，复核席推不出要手改哪两个 | 「形状三」加一张两行表，**点名** `packages/skill-calorie/test/cmd-registry-294.test.mjs`（分派层 `case` 冻结清单 ＋ 两分派文件行数上限）与 `packages/skill-calorie/test/legacy-ratchet-295.test.mjs`（未搬迁清单冻结键集 ＋ 条数／行数上限 ＋ 一个键恰住一处），并写清**搬一条要下调哪一处**。**只点名与判据，数字不抄**（住那两个文件里，铁律二） |
| S2-3 | 「实测加一条读命令 `test/` 下 0 件必改」与同段「命令集一变要手跟」自相矛盾 | 拆成两个口径：**搬迁**既有命令 → `test/` 0 件（计数已改对账）；**新增**命令 → 要动 `src/triggers/routing.ts` **与** `test/calorie-routing-81.test.mjs` 的路由覆盖断言（`routing.ts` 的唤醒词路由**逐条手写**、与冻结场景表按位对齐、不可派生）。原来挂在「命令集一变」那条下的 `calorie-routing-81` 上移到这里，不再重复 |
| S2-4 | 广播稿表格里「各共用位（生成物）｜手改会被 `gen:check` 抓红」对 `SKILL.md` **不成立** | 补一行专写 `packages/skill-calorie/SKILL.md`：由 `pnpm help:build` 重生成、**不在** `gen:check` 的四件名单里；「例」列另由 `pnpm help:examples:check` 守（该门因 #308 恒红） |
| S3-① | 证据 ④ 第 4 条称「唯一数量词是 9 条命令」 | 按复核席实查改成如实：文里还有「三桶」「436 条路由」「19 字段 sha」等，但**都指得出权威出处、且没有一个被写成规矩** |
| S3-② | 「两次 build」只写「前后两次都要」 | 写精确：第一次把声明编译进 `dist/` 并打印记；第二次把 `pnpm gen` 刚改写的 `keys.ts`／`registry.ts` 编译进 `dist/`，好让分派层读到新键表 |



## ⑤ 体裁判断（留痕，两段依据）

**判定：恢复开工清单**（受众＝停机冻结、要恢复开工的其他场景图）。

- 16:07 实测四条：`gate.lock` 不存在且无 `owner.json`；`gate-runs.log` 末条是 ticket=295 的 `git push` 且此后无 `START`；工作区干净且 `HEAD == origin/master`；最新过程稿 16:00。
- 16:10–16:11 实测到有 build／gen 在写 `dist/` 与 `tsconfig.tsbuildinfo`，一度改判为「有在飞 session」；
  **编排者确认为他派的 `#295` 返修席**（修 A4 假红自锁），不是场景图开工——体重图 `#154` 仍被本票阻塞、其余图仍冻结。
- **改判判据**：有没有**场景图** session 在跑（不是「有没有任何进程在跑」）。两段依据都写进广播稿正文。

## ⑥ 偏离记账（必报五步第五步）

| 第一步清单 | 实际 | 偏差 |
| --- | --- | --- |
| `docs/skills/skill-calorie/t296-*`（4 件） | 广播稿与指针留原地；纪律正文／照抄说明归位到 `docs/agents/` | **+2 件**：`AGENTS.md`（定稿令要求）、`docs/skills/skill-calorie/t296-证据.md`（本文件，定稿令要求） |
| — | `docs/agents/命令登记纪律.md`、`docs/agents/命令登记纪律-照抄说明.md` | 归位动作按定稿令第 2／3 条，不算偏差 |

多碰 0 件（定稿令要求的两件已列上）；少碰 0 件。

## ⑦ 未做／未确证

- **未 close #296、未改地图正文**（按定稿令：停下交编排者验）。
- **铁律四那条 `weightCompare2/3.ts` 命名欠账**：照编排者裁示**不立规矩**（已在 #294 证据记账）。
- **`docs/agents/structure.md` 未改**（本票是纯文本票，只新增指针与纪律，不动既有权威纪律的条文）。
- **未跑代码门禁**：本票 `src/**`／`test/**`／`scripts/**`／`packages/**` 零改动，故不跑 `pnpm test` 等；
  唯一与门禁相关的动作是**读**实现以核实三条判据（见 ③）。

## ⑧ 风险 top3

1. **指针的稳定面**：`AGENTS.md` 每轮加载，若将来有人往「命令登记」段里补纪律内容，就长出第二份会走散的副本；
   本段只留到正文的路，靠审查守。
2. **守卫缺口尚未补**（③-2）：新增能力目录后改声明内容只报下游 `GEN-CHECK FAIL`，不报陈旧；
   文本已写明，但**缺口本身**等后续动生成器的票。
3. **照抄说明会随形状走**：它是给下游 session 的动作清单，形状一变就得跟；本次已按 `#295` 最终形态核对到实现级。
