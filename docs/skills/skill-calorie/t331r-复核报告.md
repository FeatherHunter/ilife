# t331r 独立复核报告（被审提交 a4466bf）

- 被审对象：`a4466bf`（`git show --stat` 仅 `docs/skills/skill-calorie/t331-退出码-证据.md` 83 行，无源码改动，属实）。
- 开工核验：`HEAD==origin/master==a4466bf`；工作树非空但全为别席在飞改动（首检 4 个 M：`package.json`、`packages/plugin-bill-ilife/src/index.ts`、`packages/plugin-bill-ilife/tsconfig.json`、`packages/skill-calorie/test/output-naming-87.test.mjs`；复核中途新增 `packages/base-render/test/output-save-html-237.test.mjs`、`docs/skills/skill-calorie/t170-scene09-reconcile.md` M、`docs/skills/skill-calorie/t167-重新设计-新底座.md` ??），复核全程只读、未碰。
- 大输出：`.scratch/t331r/`（gitignored，不进提交）。

## 一、独立重扫（HEAD 基准，换关键词，没漏）

| # | 关键词／命令 | 读数 | 结论 |
|---|---|---|---|
| K1 | `git grep -n 'process\.exit(' HEAD -- ':!*.json'` | 271 行 | 基数。证据写 253 是在其扫描基线 `46d19b1` 处复算一致（253/24），差值 18 来自中间提交 `e04a070`（t327 三件套＋测试），见 S3 |
| K2 | `git grep -n 'process\.exitCode' HEAD -- ':!*.json'` | 25 行（正文 8 类赋值见下） | 没漏 |
| K3 | `process\.exit\(\s*[A-Za-z_$]` 非字面量 | 79 行（含证据表自匹配 prose） | 逐条过：三元 `?0:1`／`bad?1:0` 值域恒 {0,1}；`fail/die` 见 K5；`main()` 见 K6；`rc/code` 见 K7。没漏 |
| K4 | `process\.exit\s*\(\s*[\[{]` 直接 hazard | 3 行，全在 t331 证据文档 prose（N1 描述行） | 代码 0 命中，没漏 |
| K5 | `fail(`（skilllink／migrate／5 件 cmd_read `fail(code:number)`＋params）／`die(`（8 处定义＋调用方） | 分文件小批量查：skilllink 调用方全 `fail(2/3/4/5/6)`；migrate 全 `fail(2/3/4/5)`；params 全 `fail(2)`；die 调用方清一色 `die(2)` | 没漏 |
| K6 | 新增三件 `process.exit(main(...))`（baseline／make-evidence／plan-check） | 三件 `main` 上下 120 行全读：`return` 仅 0／1／2，`catch→exit(1)` | 没漏（此即 18 行差值主体） |
| K7 | `run-locked outcome.code`／`t99 rc`／`t313 code`／`check-gate main()`／`fetch/cli main()` | run-locked `:316-502` 全读：初值 1，分支 127／124／null→1／`outcome.code`（child close 事件 int，null 已转 1），finally 只 0→1；t99 `rc`∈{runAll 0/1，0，2，各 SUB 0/1}；t313 `code`＝枚举 return 0/2/3；check-gate return 2／0／1；fetch cli `Promise<number>` return 0/1/2 | 没漏 |
| K8 | shell／ps1 `exit`（`^\s*exit(\s+[^\d\s]|$)`＋全量 exit） | 仅 `exit 0／exit 1` 字面量（publish-020-line.ps1、wizard-*.sh）＋`exit=$code` 打印、`[[ BUNDLE_BAD == 0 ]] || exit 1` | 没漏 |
| K9 | `spawnSync(...).status` 回灌检查 | `t95/t98-test-delta` 仅打印不回灌；`r.status !== 0` 比较后转 `die(2)`／计数 | 没漏 |

总判：**0 命中成立**。证据"13 类全恒整数"在 HEAD 下依然成立（新增 18 行亦全整数）。

## 二、N1／N2 复现（方法有效）

N1（Node v24.19.0，spawnSync 取 status，避 pwsh 包裹）：

```powershell
node -e "const {spawnSync}=require('node:child_process');for(const a of ['process.exit([1])','process.exit({code:0})','process.exit([0])']){const r=spawnSync(process.execPath,['-e',a],{encoding:'utf8'});console.log('ARG='+a+' status='+r.status)}"
# ARG=process.exit([1]) status=1 :: TypeError [ERR_INVALID_ARG_TYPE]（"code" must be of type number）
# ARG=process.exit({code:0}) status=1 :: 同上 TypeError
# ARG=process.exit([0]) status=1 :: 同上 TypeError（连 [0] 都假红）
```

N2（坏文件必红、删即复绿，坏文件只落 `.scratch/t331r/`，跟踪树零碰）：

```powershell
node packages/skill-calorie/scripts/check-examples.mjs --only calorie.view.home
# GREEN-EXIT=0；示例行数：1（通过 1，红 0）；PASS: SKILL.md 示例逐行可执行
"no-help-block-here" | Out-File -Encoding utf8 .scratch/t331r/bad-SKILL.md
node packages/skill-calorie/scripts/check-examples.mjs --skill .scratch/t331r/bad-SKILL.md --only calorie.view.home
# RED-EXIT=1；Error: SKILL.md 缺 HELP 标记块 at parseExamples (check-examples.mjs:52:32)
Remove-Item .scratch/t331r/bad-SKILL.md -Force
node packages/skill-calorie/scripts/check-examples.mjs --only calorie.view.home
# REGREEN-EXIT=0
```

## 三、复跑（持锁 `run-locked --ticket 331r --lock-dir .scratch/locks-land --stale-minutes 30`，direct node）

| 门 | 命令 | 读数 |
|---|---|---|
| 全量 | `node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"` | `tests 1492／suites 160／pass 1492／fail 0／cancelled 0／skipped 0／todo 0`，`FULL-EXIT=0`，runId `af48d0f7-f7dc-4a02-b8dd-1cc1dde6f18` |
| 81 | `node --test test/calorie-routing-81.test.mjs` | `tests 8／pass 8／fail 0`（8/8），exit 0，runId `3f85bbde-9193-42bc-a5d0-3db752c7f97e` |
| 生成检查 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `GEN-CHECK PASS：键 101（写 35＋读 66）；能力 10 个；未搬迁清单 0 条`，exit 0，runId `56f2a36e-b989-4674-8c50-da73b92294ce` |
| 探针 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`；`UNACCOUNTED=0 处`；④ 3 处只报账；树自相矛盾 0，exit 0，runId `3cf3a180-23c3-413a-b87e-f545a3e48962`（复查行另 runId `1c8e8526-…` 同绿） |

零改动属实：`git show --stat a4466bf` 只有证据件；复核前后 `git status` 增量全为他席文件，本席跟踪区 0 写（本报告除外）。

## 四、达标判定

票面判据"扫一遍＋每处复现"：**达标（PASS）**。零命中时"每处复现"无客体，N1＋N2 构成等价完成——N1 证明此类写法一旦出现必以 TypeError 假红（扫查目标真实存在且有害）；N2 证明门对坏输入真变红、撤回即复绿（扫查所守护的门体系可信）。两者合起来覆盖了"复现"判据的立法意图（证明扫查方法不是走过场），且本席已独立换关键词重扫证实真零命中而非漏检。

## 五、打分（100 制，PASS 要求 S1=0 且 S2=0）

- S1＝漏检的数组／对象／非整数退出码处数：**0**（复现：K4 直接 hazard 代码 0 行；K3 非字面量逐条整数，命令见表 K1-K9）。
- S2＝负向／复跑方法失效项数：**0**（复现：N1 三式全 TypeError＋status 1；N2 绿→红→绿；四门全绿读数见表）。
- S3＝证据瑕疵处数：**1**（证据头写 `HEAD==46d19b1` 但提交落在其后 3 提交之上，`process.exit(` 计数 253 是旧基线数，HEAD 实为 271；结论不受影响，差值 18 本席已补审全绿。复现：`git grep -n 'process.exit(' 46d19b1 -- ':!*.json'` 计 253 vs HEAD 计 271）。
- 总分：**98（100－2×S3）⇒ PASS**。

## 六、未做项

1. `t95/t98-test-delta.mjs` 的 `status` 信号杀 null 情形：仅打印、非退出码，不动（同证据判断）。
2. `test/calorie-routing-81.test.mjs:333-334` 登记册 `exit: '4'` 字符串快照列：非 `process.exit` 实参，不动。
3. 工作树他席改动（§首列）一律未碰；`.scratch/t331r/` 大输出不进提交。
