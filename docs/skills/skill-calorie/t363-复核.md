# #363 独立复核（补记查冲突）

- 复核者：独立复核席（不复用作者现场 `.scratch/t363/`；本席自建 `.scratch/t363-review/`）
- 复核对象：`e0f7fe9`（feat·4 件）／`ff33eee`（证据＋对账导出）／`0e65fc0`（证据补记）；三笔均已在 `origin/master`。
- 结论：**PASS**（票面四条判据自持锁复跑全过；两处自设变异与作者两处变异「改坏必红／还原必绿」全过；
  §5 放宽合规性成立，但有一处**清单漏记**需作者补记后方可由编排者出具名确认——见 §六）。
- 期望值来源：**本席手写种子**（SQL 直插，不经命令）＋ **直接 `node:sqlite` 查库**（经 CLI 的 `--params` 跑真出口，
  取数对照不走被测代码的取数口）＋ 自己的日期与数值（与作者样例不同）。
- 读数时点：2026-09-15 00:40–01:00（+08:00）。

## 一、提交范围（逐个 `git show --name-only`）

| 提交 | 文件 | 件数 | 禁碰命中 |
|---|---|---|---|
| `e0f7fe9` | `.changeset/t363-body-conflict.md`／`packages/skill-calorie/src/body/log.ts`／`packages/skill-calorie/src/fetch/body.ts`／`packages/skill-calorie/test/t363-补记查冲突.test.mjs` | 4 | **0** |
| `ff33eee` | `docs/skills/skill-calorie/t363-冲突证据.md`／`docs/skills/skill-calorie/t363-门禁对账.md` | 2 | **0** |
| `0e65fc0` | `docs/skills/skill-calorie/t363-冲突证据.md` | 1 | **0** |

- 禁碰模式（逐笔过滤）：`src/triggers/**`、`src/body/routes.ts`、`packages/skill-calorie/AGENTS.md`、
  `scripts/check-warning-line.mjs`、`bodyDocs.ts`、`bodyPlate.ts`、`view.ts` → 三笔合计 **0 命中**。
- 乱码探针：三笔 diff 全文搜 `U+FFFD`／`鏂`／`鍐`／`锛`／`鈥`／`锟` → **0 命中**。
- 票面小口径出入（备案，不判缺陷）：票面「交付物路径」沿用了上一票的文件名（`t11-冲突证据.md`），
  实际落在 `docs/skills/skill-calorie/t363-冲突证据.md`（本图按票号命名，与该票同批的 `t362-窗口不截断`／`t360`／`t361` 同例）。

## 二、判据复跑（本席自建探针 `.scratch/t363-review/probe.mjs`，库＝`.scratch/t363-review/db/`）

自选日期与数值：体脂 D=`2026-07-11`（既有 gym／21.75%／胸 11.5、腹 13.5，其余 5 槽空）、D2=`2026-07-12`（无既有）；
围度 D=`2026-08-19`（既有 腰 82.5／右小腿 36.25，其余 11 项空）、D2=`2026-08-20`、D3=`2026-08-21`（既有 2 条）。

**判据①（同日已有记录 → exit 0 ＋ 补记仍写 ＋ 既有值逐字等于查库值）**

```
CONFLICT-message status=0 日条数=1->2 既有id=1 既有值=["来源 健身房 InBody","体脂率 21.75%",
  "皮褶 7 点 胸 11.5、腹 13.5、大腿 —、三头肌 —、肩胛下 —、髂上 —、腋中线 —","备注 —"]
CONFLICT-html    status=0 日条数=1->2 既有id=1 （回执 message 与落盘页可见文本同值，逐格 7/7 对齐查库列值）
CONFLICT-MEASURE status=0 日条数=2 既有id=2 13格=[["胸围","—"],["腰围","82.5"],["腹围","—"],["臀围","—"],
  ["左大腿","—"],["右大腿","—"],["左小腿","—"],["右小腿","36.25"],["左上臂","—"],["右上臂","—"],
  ["左前臂","—"],["右前臂","—"],["肩围","—"]]
```

- 逐格对照物是**另开 `node:sqlite` 只读句柄的查库值**；13 格 ＋ 7 点 ＋ 来源 ＋ 体脂率 ＋ 备注全部逐字相等。
- 「补记仍写」＝同日条数 `1→2`（体脂）、`→2`（围度），结论句照旧跟在冲突段之后
  （`已记体脂：2026-07-11 健身房 InBody 22.5%`）。

**判据②（换一天 → 不出现冲突段）**

```
NO-CONFLICT 体脂="已记体脂：2026-07-12 医院测 17.25%" 围度="已记围度：2026-08-20（waistCm 79）" 别日在库=1
```

- 同一库内别的日期有记录（`别日在库=1`）→「无冲突」是按**当天**判的，不是「库空才不打」。
- 同种子连跑对照：`PAIR 有既有="冲突：2026-07-11 已有 1 条体脂记录 #1（来源 健身房 InBod…" 无既有="已记体脂：2026-07-12 健身房 InBody 20.5%"`。

**自设探针（作者未覆盖）**

```
MULTI status=0 段条数=2 ids=[3,4] 查库ids=[3,4,5]      ← 同日 2 条既有逐条摆出、顺序＝id 升序
MISSING-DASH 缺项=11/13 名单=["胸围","腹围","臀围","左大腿","右大腿","左小腿","左上臂","右上臂","左前臂","右前臂","肩围"]
  库内原始={"waist_cm":82.5,"right_calf_cm":36.25,"chest_cm":null}   ← 可见文本 `—`／库内 null 两条分开
```

**作者测试复跑**：`node --test packages/skill-calorie/test/t363-补记查冲突.test.mjs` → `AUTHOR-TEST exit=0 tests=7 pass=7 fail=0`。

## 三、负向对照与变异（持锁期做，只动 `dist/`，逐文件点名复原并核 sha）

口径：变异只落在 `packages/skill-calorie/dist/` 下的编译产物（不受版本控制、不在本票声明路径、不在告警线扫描面）；
`src/` 全程零改动（复原后逐件核 sha 同值）。跑前先证「跑作者测试不会顺手重编 dist」：
`DIST-REBUILD-CHECK 跑前跑后 dist 变文件数=0`。

| 变异 | 改动点 | 作者测试 | 本席探针 | 复原 |
|---|---|---|---|---|
| M-a 冲突检查短路 | `compositionsOnDate`／`measurementsOnDate` 恒回空 | `exit=1 tests=7 pass=2 fail=5` | `conflict exit=1 VERDICT=RED 失败=4` | `sha 与基线同=true` → `还原必绿 测试exit=0 pass=7 fail=0 探针 GREEN` |
| M-b 同日多条顺序改坏 | `ORDER BY id ASC` → `DESC` | `exit=1 tests=7 pass=5 fail=2` | `multi exit=1 VERDICT=RED 失败=5`（conflict 案非本变异靶面，仍绿） | 同上 → `还原必绿 测试exit=0 探针 multi GREEN` |
| M-c 既有值取错列 | 体脂率那一格改读 `note` 列 | `exit=1 tests=7 pass=6 fail=1` | `conflict exit=1 VERDICT=RED 失败=2`（`体脂率格=体脂率 % 查库=21.75`） | 同上 → `还原必绿` |
| M-d 冲突段恒不打 | `conflictLine` 短路回空串 | `exit=1 tests=7 pass=3 fail=4` | `conflict exit=1 VERDICT=RED 失败=4` | 同上 → `还原必绿` |

```
RESTORE-CHECK dist/body/log.js   sha=10c720ab34c8e5b4 基线=10c720ab34c8e5b4 同=true
RESTORE-CHECK dist/fetch/body.js sha=57f6a625a20c36fb 基线=57f6a625a20c36fb 同=true
RESTORE-CHECK src/body/log.ts    sha=0cbd488ca26b8912 基线=0cbd488ca26b8912 同=true
RESTORE-CHECK src/fetch/body.ts  sha=ec06bfed7c69e3cc 基线=ec06bfed7c69e3cc 同=true
```

作者的两处变异（M1 短路／M2 取错列）与本席 M-a／M-c 同源；票面「冲突检查短路 → 必红」成立。

## 四、结构与本票自身告警线

- `src/body/log.ts` LF=**223**（≤350，未越线）；`src/fetch/body.ts` LF=**426**（>350，**已越线**）。
- 包内 `AGENTS.md` 台账该行＝`| src/fetch/body.ts | 369 | 426 |`：**当场实测列与本席实测 LF 426 一致**，
  与作者报数一致。该行的同步是**他席** `e0034c4`（`chore(465): 告警线台账随实况回绿（exercisePort.ts 544／fetch/body.ts 406 属别票在飞件）`，
  00:22）落的；本票 `e0f7fe9`（00:29）之后台账**未再同步**。
- **台账已陈化**（本席当刻实跑告警线门，读数随并行窗口在变，取最近一次）：`WARNING-LINE exit=1 RESULT: 56/60`，四条红为
  `RED 台账陈化：src/render/trendDocs.ts 台账=826 实况=889`、`src/render/trendMiscPortDocs.ts 台账=571 实况=592`、
  `src/analysis/multiTrendPage.ts 台账=490 实况=501`、`src/render/wizardPort.ts 台账=265 实况=266`
  —— **四条都不是本票路径**。（本席更早一次读数为 `55/59`，红名单里另含 `src/photo/photo.ts LF=355` 漏报一条；
  该条已被他席在 00:53:56 的 `packages/skill-calorie/AGENTS.md` 同步里挂号 → 现在的读数里它已不红。）
  本票自身那行 `fetch/body.ts | 369 | 426` 与实况一致，**不在红名单**。
- 必报五步第四条（超线那句）：`src/fetch/body.ts` 426 行已越线（挂号 369），需按规则重构；本票只新增两个取数口
  ＋ 私有 `sameDay`，拆法不在本票写集（#398 已挂号：按「写侧校验／围度取数／体成分取数」切姊妹件）。
- 连带面（如实登记）：写命令回执仍是**裸回执片段**（`writeParts.ts` 的 `receiptHtml`：`<section class="ilife-page" data-slot="…receipt">`
  单段，无 doctype、无 `ilife-copy` 复制菜单，本席实测 481 字节）——冲突段在今天只进 `message`／落盘页与 `items[]`，
  「可见文本 vs 复制 payload」的整页版式仍归 **#365／#366**。

## 五、真库零写入（sha／size／mtime 自核）

```
跑前 REAL-DB sha256=57CE9D87F5F232822332D7057C2AE8411D99BA7FDECEA289F2028E19FC3FFC15 size=3072000 mtime=2026-09-14T13:53:37.7293890+08:00 calorie_html=364
跑后 REAL-DB sha256=57CE9D87F5F232822332D7057C2AE8411D99BA7FDECEA289F2028E19FC3FFC15 size=3072000 mtime=2026-09-14T13:53:37.7293890+08:00 calorie_html=364
```

- 与作者证据件所记同值。本席一切数据走 `.scratch/t363-review/db/`（`SKILLS_DB_PATH` 指过去）；未跑任何打真库的写命令。
- 受版本控制文件全量指纹（2179 件聚合 sha256）跑前 `726ac89756e06063…`／跑后 `36a0e6c77dfa11a6…`：差异全部落在
  `.scratch/locks/gate-runs.log`（本席运行留痕）与本席现场文件；声明件、`AGENTS.md`、`routes.ts`、`bodyDocs.ts`、
  `bodyPlate.ts`、`view.ts`、`check-warning-line.mjs` 的 sha 与 LF **跑前跑后逐件同值**。

## 六、放宽合规性（§5，供编排者出具名确认）

复跑命令（**按作者声明的逐字命令复现，不放宽我自己任何门**）：

1. 逐字复现：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t363-冲突证据.md --ticket 363 --since 2026-09-14T15:50:00Z --until 2026-09-14T16:30:00Z --allow-nonzero`
   → `RESULT: matched=17/18 auditEntries=4308 scoped=17 undeclared=0`（`gate-audit: FAIL`，缺 1 条＝证据件 :105 那条 `--export` 对账自身，
   它落在 `--until` 之外，故**反向对账 `undeclared=0`**、声明侧缺 1 是时点自指）。
2. 已入库导出件当对账源：`… --log docs/skills/skill-calorie/t363-门禁对账.md … --allow-nonzero`
   → `RESULT: matched=17/18 auditEntries=17 scoped=17 undeclared=0`（导出件 17 行逐条可落地）。
3. 去掉放宽（同窗口）：`matched=10/18 … undeclared=7` → 放宽**只**吞掉 `exit≠0` 的那 7 条（作者 GATE-RELAX 自述口径一致）。
4. 窗口扩到作者收尾运行（`--until 16:39:00Z`，**不带任何 `--allow-*`**）：`matched=11/18 scoped=25 undeclared=14` ——
   缺的 7 条仍是那 7 条红；undeclared 14 条里 8 条是 `--until` 之后的收尾运行（作者已在证据件 §五 末段逐条备案），
   另 6 条是**本席**自己的运行。
5. 格式门复跑：`node tooling/make-evidence.mjs --check docs/skills/skill-calorie/t363-冲突证据.md` → `make-evidence: PASS（格式 0 项）`。

逐条结论：

- **① 7 条是否只用于取证、没被当门禁证据** ——「**窗内确为 7 条**」（本席自己点数：`59015277`／`936b21bb`／`0bfade79`／
  `a432cdbc`／`4fd0702f`／`1950091a`／`f677eb38`，全部 `exit=1`）；作者在 §五 表内逐条给了「红在何处」的读数，
  并把它们逐条声明为**不作门禁通过证据**（表头与 §六 两处明写）→ **成立**。
  但有两处**清单漏记**（结论仍成立，只是留痕不全）：
  - GATE-RELAX 行点名了**10 条** `exit=0` 的通过证据，**漏了第 11 条 `exit=0` 的 `2262629e`**（对账 ＋ 导出那条，它自己就在表内第 17 行）；
  - 表内第 13 行 `f677eb38` 的正文写「`tests 217 / pass 216 / fail 1`（唯一红＝`cmd-write-40` 的体重措辞）」——
    该行 `exit=1` 是**测试红所致**（不是门禁红），与作者 GATE-RELAX 把「他席红回归面 3 条」算进 7 条的口径**自相矛盾一处**：
    若按「红读数＝不作门禁证据」，这一条恰是**回归面取证**而非「门禁通过证据」，作者未在放宽理由里点名它。
- **② `GATE-RELAX` 行是否点名每条 runId 与理由** —— 规则（`tooling/check-gate-audit.mjs` 的 28–30 行与 359 行）只要求
  「有 `flag=` ＋ `reason=`，否则 exit≠0」，**不要求**逐条列 runId；作者的行**写了 flag、写了理由、并按类别分点到位**
  （首跑口径错 1／变异首跑 1／变异红 2／他席红回归面 3）→ **机制上满足**，缺的只是上一条那两处点数。
- **③ 声明侧 `matched` 全命中、`missing=0`、无「声称却没留痕」** —— 在**作者声明的窗口**内：
  声明 18 条、`missing=1`（＝那条 `--export` 自指条目，在 `--until` 之外），窗口内条目 `scoped=17`、`undeclared=0`；
  即「窗口内没有人跑过却没声明」成立。**但 `missing=0` 不成立**（1 条），原因是窗口时点自指，不是漏声明。
- **需补什么**：把 GATE-RELAX 的通过证据清单补成 **11 条**（加 `2262629e`），并把 `f677eb38`（`exit=1`）
  在「红读数」里逐条点名（它与另 6 条同属不作门禁证据的红），另在证据件里写明「对账/导出自身那条落在 `--until` 之外，
  故声明侧 `missing=1` 是时点自指」。补记入 **`0e65fc0` 之后的第四笔文档提交**即可，不必碰实现。
- 本席**未放宽任何门**：未用 `--allow-undeclared`／`--allow-no-claims`／`--allow-no-runid`；`--allow-nonzero`
  仅出现在**复现与复核作者声明命令**（先证「去掉它就多出那 7 条红」，以此说明放宽的最小必要范围），未用于给本票任何门开绿灯。

## 七、本席机器读数（持锁证明）

```
GATE-RUN runId=5a599449-1880-4d39-b256-dd82b82f031e cmd=pwsh -NoProfile -Command "& { pwsh -NoProfile -File .scratch/t363-review/fingerprint.ps1 before; …mutate.ps1; fingerprint.ps1 after }"   exit=0
GATE-RUN runId=f37d7c1a-c4a1-4f93-843b-5df0362ca2c5 cmd=pwsh -NoProfile -File .scratch/t363-review/rebuild.ps1（半写 dist 后复建）          exit=255（引号口径错，读数无效，已弃）
GATE-RUN runId=d524a278-55c5-4cb1-a6c2-28bba589faad cmd=pwsh -NoProfile -File .scratch/t363-review/rebuild.ps1（复建：dist 与备份同 sha）  exit=0
GATE-RUN runId=12ea6f47-6481-4e4a-bb07-2c0bbf93aa40 cmd=node .scratch/t363-review/mutate.mjs（四处变异电池）                              exit=0
GATE-RUN runId=d511445a-a0e9-426d-872a-87b6aede36b6 cmd=pwsh -NoProfile -File .scratch/t363-review/final.ps1（收尾：作者测试 7/7 ＋ 探针五案）  exit=0
```

中途一次意外（备案）：本席第一次用内联 `pwsh -Command` 跑电池时引号口径出错（`runId=f37d7c1a`，
**该次读数无效、已弃**，不计入本件任何结论），其后并发窗口里 `packages/skill-calorie/dist/fetch/body.js`
又处于半写/被移走态。本席 `rebuild.ps1`（`runId=d524a278`）复建后，`dist/fetch/body.js` 与跑前备份
**sha 完全相同**（`57f6a625a20c36fb`）——即「由当刻 `src/` 编译出的产物」与跑前产物逐字节一致，
故 `12ea6f47` 那一轮（§三 表里四个变异）与 `d511445a` 收尾轮的读数有效。
复建那次 `tsc -b` 报 `src/render/trendDocs.ts(191,17): error TS2304: Cannot find name 'corrInterp'`——**他席在途件**，
不是本票路径（本票两件在 `tsc -b` 里 error 为 0），本席未碰该件。

## 八、缺陷清单

**本票引入（需作者处置）**

1. **留痕不全（S3 级，见 §六）**：GATE-RELAX 通过证据清单漏 `2262629e`；`f677eb38`（`exit=1`）未在放宽理由里逐条点名；
   证据件未写明声明侧 `missing=1` 系窗口时点自指。→ 补记一笔文档即可。

**本票范围内（登记，不拦）**

2. `src/fetch/body.ts` 426 行已越 350 告警线（挂号 369），拆法归 #398；本票只新增两个取数口，未动既有函数。
3. 票面「交付物路径」里的证据名沿用 `t11-冲突证据.md`（实际 `t363-冲突证据.md`）——票面文字与落点的口径出入。
4. 写回执今天仍是裸片段（无复制菜单／无 doctype）：冲突段的「可见文本」有，但「复制 payload」那一轨要等 **#365／#366**。

**范围外（S3 级，转票措辞）**

5. **告警线台账已陈化**（本席实跑 `RESULT: 55/59`）：
   `src/render/trendMiscPortDocs.ts`（台账 571／实况 587）、`src/analysis/multiTrendPage.ts`（490／501）、
   `src/render/wizardPort.ts`（265／266）三件台账未随实况同步，`src/photo/photo.ts`（LF 355）**新越线且未挂号**。
   四件都不是 #363 路径（本票自身那行与实况一致）。**转票措辞**：请其归属票（或 #445 台账维护口径）跑
   `node packages/skill-calorie/scripts/check-warning-line.mjs --sync` 后入库，勿由 #363 带走。
6. **他席编译红**：`src/render/trendDocs.ts:191` `Cannot find name 'corrInterp'`（他席在途件），
   会让任何 `tsc -b packages/skill-calorie` 收红并留下半写 dist。**转票措辞**：请该件归属票修复后再让全包编译门回绿。
7. 回归面既有红（`profile-doc-179` 的重复 `ilife-copy-log`、`cmd-write-40` 的体重措辞）经作者撤改探针与
   本席口径核对确认**不归本票**（本票两件改动撤掉后仍红同一条，作者 `e2891b41`／`ffa28c42` 两轮探针留痕）。

## 九、下一手缺什么

1. 作者补记 §八 第 1 条（GATE-RELAX 点数／`f677eb38` 点名／`missing=1` 说明）→ 编排者据此出具名确认。
2. 本席**未**改实现／测试／台账，**未**关票；本件为复核证据唯一新增件。
3. 现场处置（点名）：`.scratch/t363-review/probe.mjs`（本席探针五案）／`mutate.mjs`（变异电池）／`fingerprint.ps1`
   （跑前跑后指纹）／`rebuild.ps1`／`final.ps1` 作为本件的读数脚本人证**按要求保留**（sha256 前 16 位分别为
   `6603FB9547B23B84`／`560A05A4F1022386`／`E54C98150B11872D`／`E91576C091F05A65`／`A5DC9E60337017B6`；
   另有 `audit.ps1` 为门禁对账复跑脚本；`.scratch/t363-review/real-db-before.txt` 为真库跑前指纹）；
   两处 dist 备份、tmp 库与落盘页、全量指纹文本**已清除**。作者现场 `.scratch/t363/` 全程未触碰。
