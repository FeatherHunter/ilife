# t165 · 场景 03「体重」新仓现状（58 条逐条取证）

> 取证对象：`packages/skill-calorie`（新仓）。取证时间：2026-09-13。
> 方法：只读源码 ＋ 只读既有证据文件；**未实跑任何命令，未改任何源码，未动数据库**。
> 冻结表原文 `src/triggers/scene-03-weight.ts`（58 条，第 5–62 行），路由原文 `src/triggers/routing.ts`（`WAKE_ROUTES` 第 235–292 行，58 条逐位对齐）。

---

## 一、58 条逐条表

列说明：**key** ＝冻结表自己的资产 id（不是命令）；**路由 kind** 与**路由 key／cli** 取自 `routing.ts:235-292`；
**命令实现在哪** ＝分发里那条 `case` 的行号 ＋ 命令登记表 `src/cli/keys.ts` 的行号；
**今天产出形态** ＝信封 shape（`keys.ts`）＋ 渲染源文件 ＋ 是完整 HTML 文档还是片段。

| 序 | 唤醒词 | key | 路由 kind | 路由 key／cli | non-exec 理由标识 | 命令实现在哪(file:line) | 今天产出形态 |
|---|---|---|---|---|---|---|---|
| 1 | 记体重 | `w_log` | exec | `calorie.weight.log`<br>`calorie-cmd-read calorie.weight.log --params '{"kg":70.5}'` | — | src/cli/write.ts:528（登记 src/cli/keys.ts:31） | 信封 receipt；**片段**——`src/cli/write.ts:202` receiptHtml 出 `<section class="ilife-page" data-slot="ilife:calorie:receipt">`，无 doctype／无 style |
| 2 | 记体重（含备注） | `w_log_note` | exec | `calorie.weight.log`<br>`calorie-cmd-read calorie.weight.log --params '{"kg":70.5,"note":"晨起空腹"}'` | — | src/cli/write.ts:528（登记 src/cli/keys.ts:31） | 信封 receipt；**片段**——`src/cli/write.ts:202` receiptHtml 出 `<section class="ilife-page" data-slot="ilife:calorie:receipt">`，无 doctype／无 style |
| 3 | 补录体重 | `w_backfill` | exec | `calorie.weight.log`<br>`calorie-cmd-read calorie.weight.log --params '{"kg":70.5,"date":"2026-09-06"}'` | — | src/cli/write.ts:528（登记 src/cli/keys.ts:31） | 信封 receipt；**片段**——`src/cli/write.ts:202` receiptHtml 出 `<section class="ilife-page" data-slot="ilife:calorie:receipt">`，无 doctype／无 style |
| 4 | 批量补录体重 | `w_backfill_batch` | exec | `calorie.weight.batch`<br>`calorie-cmd-read calorie.weight.batch --params '{"items":[{"date":"2026-09-06","kg":70.5}]}'` | — | src/cli/write.ts:593（登记 src/cli/keys.ts:34） | 信封 receipt；**片段**（同 write.ts:202） |
| 5 | 看今日体重 | `w_today` | exec | `calorie.view.weight`<br>`calorie-cmd-read calorie.view.weight --params '{"start":"2026-09-07","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:882（登记 src/cli/keys.ts:90） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:189` buildWeightDoc → :236 assembleDocPage |
| 6 | 改体重记录 | `w_update` | exec | `calorie.weight.update`<br>`calorie-cmd-read calorie.weight.update --params '{"id":1,"kg":70.2}'` | — | src/cli/write.ts:540（登记 src/cli/keys.ts:32） | 信封 receipt；**片段**（同 write.ts:202） |
| 7 | 改某日体重 | `w_update_by_date` | exec | `calorie.weight.update`<br>`calorie-cmd-read calorie.weight.update --params '{"date":"2026-09-06","kg":70.2}'` | — | src/cli/write.ts:540（登记 src/cli/keys.ts:32） | 信封 receipt；**片段**（同 write.ts:202） |
| 8 | 删体重记录 | `w_delete` | exec | `calorie.weight.remove`<br>`calorie-cmd-read calorie.weight.remove --params '{"id":1}'` | — | src/cli/write.ts:565（登记 src/cli/keys.ts:33） | 信封 receipt；**片段**（同 write.ts:202） |
| 9 | 删某日体重 | `w_delete_by_date` | exec | `calorie.weight.remove`<br>`calorie-cmd-read calorie.weight.remove --params '{"date":"2026-09-06"}'` | — | src/cli/write.ts:565（登记 src/cli/keys.ts:33） | 信封 receipt；**片段**（同 write.ts:202） |
| 10 | 批量删体重 | `w_delete_batch` | exec | `calorie.weight.remove`<br>`calorie-cmd-read calorie.weight.remove --params '{"start":"2026-09-01","end":"2026-09-02"}'` | — | src/cli/write.ts:565（登记 src/cli/keys.ts:33） | 信封 receipt；**片段**（同 write.ts:202） |
| 11 | 看本周体重 | `w_detail_week` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-09-07","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 12 | 看上周体重 | `w_detail_last_week` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-08-31","end":"2026-09-06"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 13 | 看本月体重 | `w_detail_month` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-09-01","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 14 | 看上月体重 | `w_detail_last_month` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-08-01","end":"2026-08-31"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 15 | 看最近 7 天体重 | `w_detail_7d` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-09-01","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 16 | 看最近 90 天体重 | `w_detail_90d` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-06-10","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 17 | 看某段时间体重 | `w_detail_range` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-09-01","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 18 | 看体重曲线 | `w_curve` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-08-09","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 19 | 看体重曲线（带目标） | `w_curve_target` | non-exec | 路由无 cli<br>冻结表 cli＝`看带目标线的体重曲线，默认最近 30 天` | `compoundCurve` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 20 | 看体重曲线（带里程碑） | `w_curve_milestone` | non-exec | 路由无 cli<br>冻结表 cli＝`看带里程碑标注的体重曲线，默认最近 30 天` | `compoundCurve` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 21 | 看体重曲线（带异常点） | `w_curve_anomaly` | non-exec | 路由无 cli<br>冻结表 cli＝`看带异常点标注的体重曲线，默认最近 30 天` | `compoundCurve` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 22 | 看本月体重曲线 | `w_curve_month` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-09-01","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 23 | 看上月体重曲线 | `w_curve_last_month` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-08-01","end":"2026-08-31"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 24 | 看最近 90 天体重曲线 | `w_curve_90d` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-06-10","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 25 | 看最近 180 天体重曲线 | `w_curve_180d` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-03-12","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 26 | 看最近 365 天体重曲线 | `w_curve_365d` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2025-09-08","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 27 | 看某段时间体重曲线 | `w_curve_range` | exec | `calorie.view.weight-history`<br>`calorie-cmd-read calorie.view.weight-history --params '{"start":"2026-09-01","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:894（登记 src/cli/keys.ts:91） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:248` buildWeightHistoryDoc → :293 assembleDocPage |
| 28 | 看体重稳不稳（增强版） | `w_vol` | exec | `calorie.view.volatility`<br>`calorie-cmd-read calorie.view.volatility --params '{"start":"2026-09-01","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:933（登记 src/cli/keys.ts:94） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:410` buildVolatilityDoc → :457 assembleDocPage |
| 29 | 看本月波动 | `w_vol_month` | exec | `calorie.view.volatility`<br>`calorie-cmd-read calorie.view.volatility --params '{"start":"2026-09-01","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:933（登记 src/cli/keys.ts:94） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:410` buildVolatilityDoc → :457 assembleDocPage |
| 30 | 看最近 90 天波动 | `w_vol_90d` | exec | `calorie.view.volatility`<br>`calorie-cmd-read calorie.view.volatility --params '{"start":"2026-06-10","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:933（登记 src/cli/keys.ts:94） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:410` buildVolatilityDoc → :457 assembleDocPage |
| 31 | 看最近 180 天波动 | `w_vol_180d` | exec | `calorie.view.volatility`<br>`calorie-cmd-read calorie.view.volatility --params '{"start":"2026-03-12","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:933（登记 src/cli/keys.ts:94） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:410` buildVolatilityDoc → :457 assembleDocPage |
| 32 | 看波动异常点 | `w_vol_anomalies` | exec | `calorie.view.volatility`<br>`calorie-cmd-read calorie.view.volatility --params '{"start":"2026-09-01","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:933（登记 src/cli/keys.ts:94） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:410` buildVolatilityDoc → :457 assembleDocPage |
| 33 | 看「有备注」的体重记录 | `w_notes` | non-exec | 路由无 cli<br>冻结表 cli＝`看带备注的体重记录，默认最近 30 天` | `noNoteFilter` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 34 | 对比体重：最近 30 天 vs 之前 30 天 | `w_cmp_30d` | exec | `calorie.view.weight-compare`<br>`calorie-cmd-read calorie.view.weight-compare --params '{"start":"2026-08-09","end":"2026-09-07","compareStart":"2026-07-10","compareEnd":"2026-08-08"}'` | — | src/cli/cmd_read.ts:910（登记 src/cli/keys.ts:92） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:314` buildWeightCompareDoc → :349 assembleDocPage |
| 35 | 对比体重：自定义两段时间 | `w_cmp_custom` | exec | `calorie.view.weight-compare`<br>`calorie-cmd-read calorie.view.weight-compare --params '{"start":"2026-09-01","end":"2026-09-07","compareStart":"2026-08-01","compareEnd":"2026-08-31"}'` | — | src/cli/cmd_read.ts:910（登记 src/cli/keys.ts:92） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:314` buildWeightCompareDoc → :349 assembleDocPage |
| 36 | 对比体重：本周 vs 上周 | `w_cmp_week` | exec | `calorie.view.weight-compare`<br>`calorie-cmd-read calorie.view.weight-compare --params '{"start":"2026-09-07","end":"2026-09-07","compareStart":"2026-08-31","compareEnd":"2026-09-06"}'` | — | src/cli/cmd_read.ts:910（登记 src/cli/keys.ts:92） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:314` buildWeightCompareDoc → :349 assembleDocPage |
| 37 | 对比体重：本月 vs 上月 | `w_cmp_month` | exec | `calorie.view.weight-compare`<br>`calorie-cmd-read calorie.view.weight-compare --params '{"start":"2026-09-01","end":"2026-09-07","compareStart":"2026-08-01","compareEnd":"2026-08-31"}'` | — | src/cli/cmd_read.ts:910（登记 src/cli/keys.ts:92） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:314` buildWeightCompareDoc → :349 assembleDocPage |
| 38 | 对比体重：近 N 天 vs 上一个 N 天 | `w_cmp_ndays` | exec | `calorie.view.weight-compare`<br>`calorie-cmd-read calorie.view.weight-compare --params '{"start":"2026-09-01","end":"2026-09-07","compareStart":"2026-08-25","compareEnd":"2026-08-31"}'` | — | src/cli/cmd_read.ts:910（登记 src/cli/keys.ts:92） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:314` buildWeightCompareDoc → :349 assembleDocPage |
| 39 | 对比体重：今天 vs 一年前今天 | `w_cmp_1y` | exec | `calorie.view.weight-compare`<br>`calorie-cmd-read calorie.view.weight-compare --params '{"start":"2026-09-07","end":"2026-09-07","compareStart":"2025-09-07","compareEnd":"2025-09-07"}'` | — | src/cli/cmd_read.ts:910（登记 src/cli/keys.ts:92） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:314` buildWeightCompareDoc → :349 assembleDocPage |
| 40 | 对比体重：今天 vs 半年前今天 | `w_cmp_6m` | exec | `calorie.view.weight-compare`<br>`calorie-cmd-read calorie.view.weight-compare --params '{"start":"2026-09-07","end":"2026-09-07","compareStart":"2026-03-07","compareEnd":"2026-03-07"}'` | — | src/cli/cmd_read.ts:910（登记 src/cli/keys.ts:92） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:314` buildWeightCompareDoc → :349 assembleDocPage |
| 41 | 对比体重：今天 vs 三月前今天 | `w_cmp_3m` | exec | `calorie.view.weight-compare`<br>`calorie-cmd-read calorie.view.weight-compare --params '{"start":"2026-09-07","end":"2026-09-07","compareStart":"2026-06-07","compareEnd":"2026-06-07"}'` | — | src/cli/cmd_read.ts:910（登记 src/cli/keys.ts:92） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:314` buildWeightCompareDoc → :349 assembleDocPage |
| 42 | 对比体重：当前 vs 目标体重 | `w_cmp_target` | exec | `calorie.view.goal-weight`<br>`calorie-cmd-read calorie.view.goal-weight --params '{"start":"2026-08-09","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:658（登记 src/cli/keys.ts:73） | 信封 stat；**片段**——`src/render/html.ts:296` renderGoalWeightHtml → :38 pageShell（旧串模板，无 doctype） |
| 43 | 对比体重：当前 vs 平台期首日 | `w_cmp_plateau` | non-exec | 路由无 cli<br>冻结表 cli＝`对比当前体重与平台期首日体重` | `anchorCompare` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 44 | 对比体重：当前 vs 历史最低 | `w_cmp_min` | non-exec | 路由无 cli<br>冻结表 cli＝`对比当前体重与历史最低体重` | `anchorCompare` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 45 | 对比体重：当前 vs 历史最高 | `w_cmp_max` | non-exec | 路由无 cli<br>冻结表 cli＝`对比当前体重与历史最高体重` | `anchorCompare` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 46 | 对比体重：减重 5kg 那天 vs 今天 | `w_cmp_5kg` | non-exec | 路由无 cli<br>冻结表 cli＝`对比今天与累计减重 5 公斤那天的体重` | `anchorCompare` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 47 | 对比体重：减重 10kg 那天 vs 今天 | `w_cmp_10kg` | non-exec | 路由无 cli<br>冻结表 cli＝`对比今天与累计减重 10 公斤那天的体重` | `anchorCompare` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 48 | 对比体重：当前 vs 入夏最低 | `w_cmp_summer` | non-exec | 路由无 cli<br>冻结表 cli＝`对比当前体重与入夏以来最低体重` | `anchorCompare` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 49 | 对比体重：当前 vs 入冬最低 | `w_cmp_winter` | non-exec | 路由无 cli<br>冻结表 cli＝`对比当前体重与入冬以来最低体重` | `anchorCompare` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 50 | 对比体重：运动多 vs 运动少的两个月 | `w_cmp_exercise` | non-exec | 路由无 cli<br>冻结表 cli＝`对比运动最多的月份与运动最少的月份的体重变化` | `anchorCompare` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 51 | 对比体重：工作日 vs 周末 | `w_cmp_weekend` | exec | `calorie.view.weight-compare`<br>`calorie-cmd-read calorie.view.weight-compare --params '{"start":"2026-08-31","end":"2026-09-04","compareStart":"2026-09-05","compareEnd":"2026-09-07"}'` | — | src/cli/cmd_read.ts:910（登记 src/cli/keys.ts:92） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:314` buildWeightCompareDoc → :349 assembleDocPage |
| 52 | 看体重总览 | `w_overview` | exec | `calorie.view.weight`<br>`calorie-cmd-read calorie.view.weight --params '{"start":"2026-08-09","end":"2026-09-07"}'` | — | src/cli/cmd_read.ts:882（登记 src/cli/keys.ts:90） | 信封 stat；**完整文档**——`src/render/sportDocs.ts:189` buildWeightDoc → :236 assembleDocPage |
| 53 | 体重复盘（本周） | `w_review_week` | non-exec | 路由无 cli<br>冻结表 cli＝`按本周复盘体重（周一到今天）` | `reviewWindowOnly` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 54 | 体重复盘（本月） | `w_review_month` | non-exec | 路由无 cli<br>冻结表 cli＝`按本月复盘体重（月初到今天）` | `reviewWindowOnly` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 55 | 体重复盘（最近 90 天） | `w_review_90d` | non-exec | 路由无 cli<br>冻结表 cli＝`按最近 90 天复盘体重` | `reviewWindowOnly` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 56 | 体重复盘（今年） | `w_review_year` | non-exec | 路由无 cli<br>冻结表 cli＝`按今年（1 月 1 日到今天）复盘体重` | `reviewWindowOnly` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 57 | 体重复盘（自定义时间） | `w_review_range` | non-exec | 路由无 cli<br>冻结表 cli＝`按自定义日期区间复盘体重` | `reviewWindowOnly` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |
| 58 | 看里程碑回溯 | `w_milestones` | non-exec | 路由无 cli<br>冻结表 cli＝`回看已达成的体重里程碑（达成日期与当时数值）` | `milestoneForwardOnly` | **无命令可执行**（legacy-chain） | 今天不产出文件（无命令可执行） |

**口径注解（读表前先看这三条）**

1. 未实跑，故「今天产出形态」是**静态判定**：shape 取自 `src/cli/keys.ts` 的 `CALORIE_COMBOS`；
   「完整文档／片段」取自渲染源文件里是否走 `src/shared/docPage.ts:47` `assembleDocPage`（该函数用的模板在
   `docPage.ts:36-44`，首行就是 `<!doctype html>` ＋ `<meta charset="utf-8">` ＋ `<!--SHARED-CSS-->`）。
2. 「片段」不是缺陷描述，是现状：写类命令一律出 `<section class="ilife-page" data-slot="ilife:calorie:receipt">`，
   由 `src/cli/write.ts:202` `receiptHtml` 拼出。仓库自己把这条现状钉在测试里，见 `test/profile-doc-179.test.mjs:216-231`
   （该断言列表含 `calorie.weight.log`，并明写「其余会改数据库的命令仍是原回执片段」「32 条本不该变」）。
3. 18 条 non-exec 在路由里**没有 cli**；表中的「冻结表 cli」是该词在冻结表 `main_prompt.cli` 里的原文——
   是一句中文说明，不是命令。下表 58 条与这一事实**完全重合**：命令形态 40 条全落 exec，非命令形态 18 条全落 non-exec，零交叉。

---

## 二、按命令分组的词数

40 条 exec 落在 **9 条命令**上。按词数从多到少：

| 命令（路由 key） | 词数 | 词名清单 |
|---|---|---|
| `calorie.view.weight-history` | 14 | 看本周体重、看上周体重、看本月体重、看上月体重、看最近 7 天体重、看最近 90 天体重、看某段时间体重、看体重曲线、看本月体重曲线、看上月体重曲线、看最近 90 天体重曲线、看最近 180 天体重曲线、看最近 365 天体重曲线、看某段时间体重曲线 |
| `calorie.view.weight-compare` | 9 | 对比体重：最近 30 天 vs 之前 30 天、对比体重：自定义两段时间、对比体重：本周 vs 上周、对比体重：本月 vs 上月、对比体重：近 N 天 vs 上一个 N 天、对比体重：今天 vs 一年前今天、对比体重：今天 vs 半年前今天、对比体重：今天 vs 三月前今天、对比体重：工作日 vs 周末 |
| `calorie.view.volatility` | 5 | 看体重稳不稳（增强版）、看本月波动、看最近 90 天波动、看最近 180 天波动、看波动异常点 |
| `calorie.weight.log` | 3 | 记体重、记体重（含备注）、补录体重 |
| `calorie.weight.remove` | 3 | 删体重记录、删某日体重、批量删体重 |
| `calorie.view.weight` | 2 | 看今日体重、看体重总览 |
| `calorie.weight.update` | 2 | 改体重记录、改某日体重 |
| `calorie.weight.batch` | 1 | 批量补录体重 |
| `calorie.view.goal-weight` | 1 | 对比体重：当前 vs 目标体重 |

另有 `routing.ts:636-639` 四条**不在 58 条之内**的场景 03 新拟入口（`看体重历史`／`看体重对比`／`看体重复核`／`看波动分析`）：
前两条分别复用 `calorie.view.weight-history`／`calorie.view.weight-compare`（即上表第 1／2 行那两条命令），
`看波动分析` 复用 `calorie.view.volatility`（上表第 3 行）；只有 `看体重复核` 指向 `calorie.view.weight-review`，
而这条命令**不在**上表 9 条之内——58 条里没有任何词指向它。本次核实只针对 58 条。

---

## 三、统计与判定口径

### 3.1 计数

| 指标 | 数 | 依据 |
|---|---|---|
| 冻结表条数 | 58 | `scene-03-weight.ts:5-62`；根测试 `test/calorie-triggers.test.mjs:34` 写死 `'03': 58` |
| 路由记录条数 | 58 | `routing.ts:235-292`，与冻结表逐位同唤醒词（脚本核对，误差 0） |
| `kind: 'exec'` | **40** | `routing.ts:235-252,256-266,268-276,285-286` |
| `kind: 'non-exec'` | **18** | `routing.ts:253-255,267,277-284,287-292` |
| 40 条 exec 落在几条命令上 | **9** | 见第二节 |
| 命令实现在分发里找不到（死引用） | **0** | 见 3.2 |
| 今天跑得通 | **40** | 见 3.3 |
| 今天跑不通（没有命令可执行） | **18** | 见 3.3 |
| 冻结表 `html_template` 指向本仓不存在的模板 | 8 个名字／覆盖 58 条 | 见 3.4 |

### 3.2 命令是否真的存在（逐 key 核实，不看路由标注）

9 条命令在**登记表**与**分发**两处都能找到，无死引用：

| 命令 | 登记（`src/cli/keys.ts`） | 分发实现 | 判定 |
|---|---|---|---|
| `calorie.weight.log` | :31 | `src/cli/write.ts:528` | 存在 |
| `calorie.weight.update` | :32 | `src/cli/write.ts:540` | 存在 |
| `calorie.weight.remove` | :33 | `src/cli/write.ts:565` | 存在 |
| `calorie.weight.batch` | :34 | `src/cli/write.ts:593` | 存在 |
| `calorie.view.weight` | :90 | `src/cli/cmd_read.ts:882` | 存在 |
| `calorie.view.weight-history` | :91 | `src/cli/cmd_read.ts:894` | 存在 |
| `calorie.view.weight-compare` | :92 | `src/cli/cmd_read.ts:910` | 存在 |
| `calorie.view.volatility` | :94 | `src/cli/cmd_read.ts:933` | 存在 |
| `calorie.view.goal-weight` | :73 | `src/cli/cmd_read.ts:658` | 存在 |

出口形态：`package.json:29` 把 `calorie-cmd-read` 指向 `dist/cli/cmd_read.js`；
`src/cli/cmd_read.ts:1194-1196` 按 `isCalorieWriteKey` 分流——会改数据库的命令走 `dispatchWrite`，查询命令走 `dispatch`。
`src/cli/write.ts:361` `dispatchInner` 与 `src/cli/cmd_read.ts:322` `dispatch` 即是上表两列的落点。

路由登记的 cli 参数与实现入口校验**逐条对得上**：
`view.weight-history` 认 `start`／`end`（`cmd_read.ts:895-896`）；`view.weight`／`view.volatility`／`view.goal-weight`
走 `defaultRange`（`cmd_read.ts:214-231`，认 `start`／`end`）；`view.weight-compare` 四个参数
`start/end/compareStart/compareEnd` 必传（`cmd_read.ts:911-914`），路由里该命令的 9 条记录都带齐；
写命令 `kg／id／date／start+end／items` 各自落到 `write.ts:528/540/565/593` 的分支，无参数名错配。

### 3.3 「今天跑得通／跑不通」的判定口径

**跑得通**（40 条）＝ 五条同时成立：
1. 路由 `kind: 'exec'`，且 cli 首词是 `calorie-cmd-read`（不是老脚本原文）；
2. 该命令在 `keys.ts` 登记表里有条目；
3. 该命令在 `cmd_read.ts`／`write.ts` 分发里有 `case`；
4. 路由 cli 带的参数名能被该 `case` 的入口校验接受；
5. 产物形态明确（信封 shape ＋ 渲染源文件 ＋ 完整文档或片段）。

**跑不通**（18 条）＝ 判据 1 不成立：路由判 `non-exec`，冻结表里那句话本身也不是命令形态。
这 18 条**不是**实现缺失，是路由层判定「今天没有单条命令能达成该词所述能力」，
理由标识与全文见 3.5。注意：这 18 条在 `help` 索引里仍会被命中并展示（`src/render/helpCenter.ts:46,115` 读 `routesFor`），
只是没有可执行的命令行。

**加一条实跑旁证（旁证，不是今天现跑）**：`docs/research/t81-exec-smoke.md` 是 #81 阶段的逐条实跑快照，
其 §1 第 74–113 行逐条列出场景 03 的 40 条 exec 记录，**exit 全为 0**；该快照全库唯一一条失败是 `复制昨日运动`
（`t81-exec-smoke.md:540-541`），不在场景 03。**但**该快照落盘时间 2026-09-12 10:18，早于 `cmd_read.ts`（09-12 20:30）
与 `write.ts`（09-13 0:57）的最后改动，故**不能**当成今天的实跑结果——今天是否仍然全部 exit 0，本次未复跑、无法确证。

**数据依赖**（不算命令缺失）：读类命令在空窗／无记录时一律 `missing-data` 阻断，
见 `src/render/weightPlate.ts:52-54／119-121／165-167` 与 `src/fetch/weight.ts:110`；
`test/sport-homogeneity-109.test.mjs:257` 把「空库 9 条一律 exit 4」钉成断言。

### 3.4 冻结表 `html_template` 的 8 个名字在本仓都不存在

58 条冻结表各自带一个 `html_template`，共 8 个名字，指向老技能的模板文件。本仓 `templates/` 只有 6 个文件
（`diet.html`／`exercise.html`／`goal.html`／`help.html`／`home.html`／`photo-gallery.html`），
装载允许清单 `src/render/templates.ts:7-15` `CALORIE_TEMPLATES` 也只认这 6 个名字，`:23-24` 对清单外的名字直接抛「未知模板」。
故这 8 个 `html_template` 名字是**取证层面的死引用**（只出现在冻结数据里，不被任何渲染路径读取）：

| 冻结表 html_template | 条数 | 本仓是否存在 |
|---|---|---|
| `templates/weight_log_receipt.html` | 3 | 否 |
| `templates/weight_batch_receipt.html` | 1 | 否 |
| `templates/weight_dashboard.html` | 2 | 否 |
| `templates/crud_receipt.html` | 5 | 否 |
| `templates/weight_history.html` | 18 | 否 |
| `templates/weight_volatility_v2.html` | 5 | 否 |
| `templates/weight_compare.html` | 18 | 否 |
| `templates/weight_review.html` | 6 | 否 |

这**不影响跑通**：今天的页面由 `src/render/*Docs.ts` 的构页函数现拼，读的模板只有 `src/shared/docPage.ts:36` 那一份
整页模板（write 类回执照旧走 `write.ts:202`，不读模板文件）。

### 3.5 18 条 non-exec 逐条（词名 ＋ 理由标识 ＋ 理由全文）

| 唤醒词 | 理由标识 | 理由全文（原文逐字；其中「95 键」是当时的命令数口径，今天登记表是 100 条命令） |
|---|---|---|
| 看体重曲线（带目标） | `compoundCurve` | 命中但不执行：该词要的是「曲线＋标注」（目标线／里程碑／异常点）复合形态；view.weight-history 只给历史点与变化量，view.anomaly 只给异常点，无单键同形。 |
| 看体重曲线（带里程碑） | `compoundCurve` | 命中但不执行：该词要的是「曲线＋标注」（目标线／里程碑／异常点）复合形态；view.weight-history 只给历史点与变化量，view.anomaly 只给异常点，无单键同形。 |
| 看体重曲线（带异常点） | `compoundCurve` | 命中但不执行：该词要的是「曲线＋标注」（目标线／里程碑／异常点）复合形态；view.weight-history 只给历史点与变化量，view.anomaly 只给异常点，无单键同形。 |
| 看「有备注」的体重记录 | `noNoteFilter` | 命中但不执行：95 键无「备注」筛选参数（饮食／体重／运动三面的备注均非任何键的筛选维度），单命令不可达成（逐条见 docs/research/t81-route-evidence.md §2.3）。 |
| 对比体重：当前 vs 平台期首日 | `anchorCompare` | 命中但不执行：该词需先派生锚点日期（平台期首日／历史最低／历史最高／里程碑日／季节最低／极端月）再对比；view.weight-compare 的四个参数皆为调用方给出的日期段，无锚点派生参数，单命令不可达成。 |
| 对比体重：当前 vs 历史最低 | `anchorCompare` | 命中但不执行：该词需先派生锚点日期（平台期首日／历史最低／历史最高／里程碑日／季节最低／极端月）再对比；view.weight-compare 的四个参数皆为调用方给出的日期段，无锚点派生参数，单命令不可达成。 |
| 对比体重：当前 vs 历史最高 | `anchorCompare` | 命中但不执行：该词需先派生锚点日期（平台期首日／历史最低／历史最高／里程碑日／季节最低／极端月）再对比；view.weight-compare 的四个参数皆为调用方给出的日期段，无锚点派生参数，单命令不可达成。 |
| 对比体重：减重 5kg 那天 vs 今天 | `anchorCompare` | 命中但不执行：该词需先派生锚点日期（平台期首日／历史最低／历史最高／里程碑日／季节最低／极端月）再对比；view.weight-compare 的四个参数皆为调用方给出的日期段，无锚点派生参数，单命令不可达成。 |
| 对比体重：减重 10kg 那天 vs 今天 | `anchorCompare` | 命中但不执行：该词需先派生锚点日期（平台期首日／历史最低／历史最高／里程碑日／季节最低／极端月）再对比；view.weight-compare 的四个参数皆为调用方给出的日期段，无锚点派生参数，单命令不可达成。 |
| 对比体重：当前 vs 入夏最低 | `anchorCompare` | 命中但不执行：该词需先派生锚点日期（平台期首日／历史最低／历史最高／里程碑日／季节最低／极端月）再对比；view.weight-compare 的四个参数皆为调用方给出的日期段，无锚点派生参数，单命令不可达成。 |
| 对比体重：当前 vs 入冬最低 | `anchorCompare` | 命中但不执行：该词需先派生锚点日期（平台期首日／历史最低／历史最高／里程碑日／季节最低／极端月）再对比；view.weight-compare 的四个参数皆为调用方给出的日期段，无锚点派生参数，单命令不可达成。 |
| 对比体重：运动多 vs 运动少的两个月 | `anchorCompare` | 命中但不执行：该词需先派生锚点日期（平台期首日／历史最低／历史最高／里程碑日／季节最低／极端月）再对比；view.weight-compare 的四个参数皆为调用方给出的日期段，无锚点派生参数，单命令不可达成。 |
| 体重复盘（本周） | `reviewWindowOnly` | 命中但不执行：view.weight-review 只吃 today／date（复核形态），无期间窗口参数；view.weight-history 为历史点＋变化量，非复盘形态 → 期间复盘无单命令同形。 |
| 体重复盘（本月） | `reviewWindowOnly` | 命中但不执行：view.weight-review 只吃 today／date（复核形态），无期间窗口参数；view.weight-history 为历史点＋变化量，非复盘形态 → 期间复盘无单命令同形。 |
| 体重复盘（最近 90 天） | `reviewWindowOnly` | 命中但不执行：view.weight-review 只吃 today／date（复核形态），无期间窗口参数；view.weight-history 为历史点＋变化量，非复盘形态 → 期间复盘无单命令同形。 |
| 体重复盘（今年） | `reviewWindowOnly` | 命中但不执行：view.weight-review 只吃 today／date（复核形态），无期间窗口参数；view.weight-history 为历史点＋变化量，非复盘形态 → 期间复盘无单命令同形。 |
| 体重复盘（自定义时间） | `reviewWindowOnly` | 命中但不执行：view.weight-review 只吃 today／date（复核形态），无期间窗口参数；view.weight-history 为历史点＋变化量，非复盘形态 → 期间复盘无单命令同形。 |
| 看里程碑回溯 | `milestoneForwardOnly` | 命中但不执行：view.weight-review 的 milestone 是「目标达成预测」（estDays／estDate 前向），不是里程碑回溯列表 → 无单命令同形。 |

按理由标识归并（5 个理由标识）：

- compoundCurve（3 条）：命中但不执行：该词要的是「曲线＋标注」（目标线／里程碑／异常点）复合形态；view.weight-history 只给历史点与变化量，view.anomaly 只给异常点，无单键同形。
- noNoteFilter（1 条）：命中但不执行：95 键无「备注」筛选参数（饮食／体重／运动三面的备注均非任何键的筛选维度），单命令不可达成（逐条见 docs/research/t81-route-evidence.md §2.3）。
- anchorCompare（8 条）：命中但不执行：该词需先派生锚点日期（平台期首日／历史最低／历史最高／里程碑日／季节最低／极端月）再对比；view.weight-compare 的四个参数皆为调用方给出的日期段，无锚点派生参数，单命令不可达成。
- reviewWindowOnly（5 条）：命中但不执行：view.weight-review 只吃 today／date（复核形态），无期间窗口参数；view.weight-history 为历史点＋变化量，非复盘形态 → 期间复盘无单命令同形。
- milestoneForwardOnly（1 条）：命中但不执行：view.weight-review 的 milestone 是「目标达成预测」（estDays／estDate 前向），不是里程碑回溯列表 → 无单命令同形。

---

## 四、相关测试与现有 token 模板

### 4.1 与场景 03 直接相关的测试

**仓根 `test/`**

| 文件:行 | 断言什么（一句） |
|---|---|
| `test/calorie-triggers.test.mjs:34` | 冻结表 10 场景计数写死，场景 03 ＝ 58 条 |
| `test/calorie-triggers.test.mjs:41-48` | 逐条 sha 与 SoT 快照一致（含这 58 条的 prompt 全文） |
| `test/calorie-routing-81.test.mjs:169-195` | 路由记录逐位对齐冻结表；命令形态的词必须 exec 且 cli 与冻结表逐字相同，非命令形态的词必须 non-exec |
| `test/calorie-routing-81.test.mjs:95` | 全库 exec 341／non-exec 95 两桶计数 |
| `test/calorie-routing-81.test.mjs:331-351` | 实跑快照行数与路由层一致，且每行 exit 码／envelope 命令名与登记一致 |

**包内 `packages/skill-calorie/test/`**

| 文件:行 | 断言什么（一句） |
|---|---|
| `sport-homogeneity-109.test.mjs:87-117` | 「看今日体重」等 9 条体重词命中到预期命令，且该命令已登记 |
| `sport-homogeneity-109.test.mjs:119-133` | 拿路由 cli 直跑（含「看体重曲线」），产物必须是完整文档（doctype／ilife-page／style／无残留标记） |
| `sport-homogeneity-109.test.mjs:168-182` | 体重盘：首末／均值／变化／曲线／记录表，metrics 数字逐项对 |
| `sport-homogeneity-109.test.mjs:183-196` | 体重历史：曲线 ＋ 含时间／BMI／备注列的全量记录表 |
| `sport-homogeneity-109.test.mjs:198-221` | 体重对比 ＋ 体重复核 ＋ 波动：两期表 ＋ 里程碑 ＋ 基线预警 |
| `sport-homogeneity-109.test.mjs:257-278` | 空库时这 9 条命令一律 exit 4、stdout 干净（不返假数据） |
| `sport-homogeneity-109.test.mjs:291-300` | 复制区带的信封版本与冻结版本一致（防漂移） |
| `render-t41.test.mjs:70-81` | 100 条命令名合法（registry 正则）＋ 新增 18 条查询命令 shape 全为 stat |
| `render-t41.test.mjs:83-115` | 体重系五盘（盘／历史／对比／复核／波动）HTML 字段断言 |
| `render-t41.test.mjs:232-253` | 空库时 18 条新查询命令一律 missing-data 阻断，不返空 |
| `render-t41.test.mjs:279-289` | 分发直调：18 条新命令 metrics 全为数字且 HTML 与 CLI 同源 |
| `cli-smoke-t41.test.mjs:96-125` | 真 CLI 串行跑 18 条新查询命令（`cli-smoke-t41.test.mjs:75-94` 的清单含体重盘／历史／对比／波动 4 条＋不在 58 条里的体重复核）：exit 0 ＋ stat ＋ metrics 全数字 ＋ `--html` 含 ilife-page |
| `db-readonly-93.test.mjs:120-125,281` | 65 条查询命令在只读句柄上逐个可用，且 data／html 与可写句柄全等 |
| `no-script-commands-180.test.mjs:24-35,63` | `scene-03-weight.ts` 在受管清单内，三个字段不得再出现老脚本命令 |
| `cmd-write-40.test.mjs:70-83` | 35 条会改数据库的命令一律 receipt 形 ＋ 名字合法 |
| `cmd-write-40.test.mjs:132-158` | 体重记／改／删／批量四条命令的行为 ＋ 缺身高仍记（BMI 待补） |
| `cmd-write-40-persist.test.mjs:231-278` | 体重 log／update／batch／remove 四种删法各自真删，逐列回读落库结果 |
| `profile-doc-179.test.mjs:216-231` | 除 3 条档案命令外，其余会改数据库的命令**仍是回执片段**（列表含 `calorie.weight.log`） |
| `m5-receipt-97.test.mjs:95-146` | 场景表覆盖 35 条会改数据库的命令；affectedRows 由只读句柄独立复核 |
| `output-naming-119.test.mjs:125-130,222-231` | 写命令落盘文件名后缀（如 `68kg`）；「记体重」落 `记体重_回执_68kg_<TS>.html` |
| `delivery-83.test.mjs:288-295` | 产物族判定次序：`<!DOCTYPE` → doc-shell，receipt 形 → receipt，其余 → fragment |
| `wake-assets-133.test.mjs:23,34-51` | 唤醒词资产清单：`weight` 组为实测 58 条 id（脚本核对：与 `scene-03-weight.ts` 的 key 集合逐条相等，零差集） |
| `help-center-106.test.mjs:65-77` | 341 条有命令的场景，其展示的 CLI 逐字等于路由层 exec 记录 |
| `help-center-106.test.mjs:79-93` | 帮助产物里零老脚本命令泄漏 |

### 4.2 `packages/skill-calorie/templates/` 现有 token 模板

目录下**只有 6 个**文件，每个都是一份完整的静态 HTML（约 40 行），带两个标记槽：

| 文件 | 标记槽 | 说明 |
|---|---|---|
| `templates/home.html` | `<!--SHARED-CSS-->`（:7）、`<!--SHARED-HELPERS-->`（:37） | 「今日总览」看板页 |
| `templates/diet.html` | 同上 | 饮食页 |
| `templates/exercise.html` | 同上 | 运动页 |
| `templates/goal.html` | 同上 | 目标页 |
| `templates/photo-gallery.html` | 同上 | 身材照画廊页 |
| `templates/help.html` | 同上 | HELP 页 |

- 这 6 个名字就是 `src/render/templates.ts:7-15` 的允许清单；清单外一律抛「未知模板」。
- **没有**「每唤醒词一份模板」：6 个模板是**按域**的静态页，不含逐词内容槽；逐词内容由 `src/render/*Docs.ts` 现拼。
- 唯一的消费点是帮助中心的看板入口（`src/render/helpCenter.ts:49,326-330`），
  与场景 03 的 58 条页面**没有关系**——体重页面不走 `loadTemplate`。
- 因此，58 条里**没有任何一条**由 `templates/` 下的文件渲染；`html_template` 那 8 个名字在本仓属于死引用（见 3.4）。

---

## 五、方法

**数据抽取**

1. `src/triggers/scene-03-weight.ts` 每条一行 JSON（第 5–62 行），逐行 `JSON.parse` 取出
   `wake_word／key／subfunction／output_type／html_template／main_prompt.cli`，共 58 条。
2. `src/triggers/routing.ts` 的 `WAKE_ROUTES` 中 `scene: '03'` 的记录（第 235–292 行）逐条取
   `kind／key／cli／bucket／reason 的条目名`，共 58 条；与第 1 步按顺序逐位对唤醒词，**零错位**。
3. 理由全文从 `routing.ts:75-119` `NON_EXEC_REASONS` 按条目名取原文，不在本文里改写。

**命令存在性核实（不看路由标注）**

4. 对 40 条 exec 的 9 个不同命令名，分别在 `src/cli/keys.ts`（登记）与 `src/cli/cmd_read.ts`／`src/cli/write.ts`（分发）里
   用逐字匹配找出落点行号；再回到 `cmd_read.ts:214-231,895-896,911-914` 与 `write.ts:528/540/565/593` 核对参数名是否被接受。
5. 出口形态核实：`package.json:29`（`calorie-cmd-read` → `dist/cli/cmd_read.js`）＋ `cmd_read.ts:1194-1196`（读／写分流）。
6. 确认命令集合与场景 03 的 58 条之间没有「路由写了、分发没写」的死引用。

**产物形态判定**

7. 信封 shape 取 `keys.ts` 的 `CALORIE_COMBOS`；完整文档／片段的判据是渲染源文件是否走
   `src/shared/docPage.ts:47` `assembleDocPage`（模板见 `docPage.ts:36-44`），不走它的记为片段
   （`calorie.view.goal-weight` 走 `src/render/html.ts:38` `pageShell`；写命令走 `src/cli/write.ts:202` `receiptHtml`）。
8. 用仓库自己的断言交叉验证上述判定：`test/profile-doc-179.test.mjs:216-231`（写命令是片段）、
   `test/delivery-83.test.mjs:288-295`（产物族判定次序）、`test/sport-homogeneity-109.test.mjs:119-133`（读命令是完整文档）。

**测试与模板盘点**

9. 在 `packages/skill-calorie/test/` 与仓根 `test/` 里按 `体重／weight／scene 03／weight-history／volatility／weightPlate` 检索，
   逐个打开确认断言内容，只列与场景 03 或体重五盘直接相关的。
10. `templates/` 目录逐文件读首尾，提取标记槽；与 `src/render/templates.ts` 的允许清单、`loadTemplate` 的消费点对照。

**边界与未做**

11. 未跑 `pnpm build`／`pnpm test`／`node --test`／`tsc`（并发协议要求持锁，本次不持锁）。
12. 未执行任何命令、未动数据库、未改任何源码。临时脚本在 `D:\ilife\.scratch\t165\`（`c-*.mjs`／`c-*.json`／`c-*.md`），
    正文只写本文件一份。
13. git 只跑过 `git status --short`。

**无法确证的点（列在明处）**

- 今天是否仍然逐条 exit 0：未实跑，只有 #81 阶段的快照（已过时于后续改动，见 3.3）。
- 各条命令在**真实数据库**上的取数结果（有无记录、是否有目标体重）：需要种子库，本次未建。
- 渲染结果在浏览器里的实际呈现：未截图、未渲染。
- `docs/research/t81-route-evidence.md` §2.3 的逐条映射未被本次逐一复核，只转引其结论所在的理由标识。
