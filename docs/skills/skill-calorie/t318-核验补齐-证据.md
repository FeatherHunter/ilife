# #318 核验与补齐 · 证据（场景 06 目标管理 ＋ 07 基础信息）

票：`#318`（地图 `#293`，阻塞于 `#322`／`#313`）。车道：`src/goal/`、`src/profile/`（点名能力目录）
＋ 自有的 `legacy/scene-06.ts`／`scene-07.ts`／`legacy/routes/scene-06.ts`／`scene-07.ts`
＋ 本票那几个键在 `src/cli/cmd_read.ts`／`write.ts` 里的 `case`。

> 索引（本文件章节）：一 核验 · 二 核验发现 vs 补齐动作 · 三 搬迁清单 · 四 路由记录 ·
> 五 免费判据与偏离记账 · 六 门禁对账（落点窗口内生成）· 七 落点实测（落点窗口内生成）。

---

## 一 · 核验（逐条给证据，不预设结论）

### 1. 两个目录是否各有 `commands.ts`（唯一声明数组）与对外门 `index.ts`（≤5 件）？

**改前：都没有。** 实测（`Get-ChildItem` 逐件行数，2026-09-11 盘＝基线 `2137e93`）：

| 目录 | 改前实际住着 | `commands.ts` | `index.ts` |
| --- | --- | --- | --- |
| `packages/skill-calorie/src/goal/` | `set.ts`(192)／`precheck.ts`(211) | 无 | 无 |
| `packages/skill-calorie/src/profile/` | `setup.ts`(428)／`update.ts`(90)／`view.ts`(265)／`labels.ts`(59) | 无 | 无 |
| 参考实现 `packages/skill-calorie/src/weight/` | 15 件 | 有（9 条，唯一数组） | 有（5 件） |

**改后：各有三件登记面 ＋ 两件入口。** `src/goal/`：`commands.ts`（13 条）／`routes.ts`（22 条）／
`index.ts`（3 件：`GOAL_COMMANDS`＋`runGoalView`／`runGoalWrite`）／`read.ts`（8 读入口）／`write.ts`（5 写入口）。
`src/profile/`：`commands.ts`（5 条）／`routes.ts`（6 条）／`index.ts`（5 件：`PROFILE_COMMANDS`＋
`runProfileView`／`runProfileWrite`＋`buildProfileSettingReceiptDoc`／`buildProfileUpdateReceiptDoc`）／
`read.ts`（2 读入口）／`write.ts`（3 写入口）。既有 6 件一个字未动（`set.ts`／`precheck.ts`／`setup.ts`／
`update.ts`／`view.ts`／`labels.ts`）。

### 2. 两场景的键是否还留在未搬迁清单／老 switch 里？分派层还有没有它们的 `case`？

**改前：都在。** `src/cli/legacy/scene-06.ts:12-24`（13 条）＋ `scene-07.ts:12-16`（5 条）＝18 条；
分派层 `cmd_read.ts` 10 处 `case`（`:529` profile-wizard／`:537` goal-wizard／`:601` view.goal／
`:614` goal-config／`:623` goal-recommend／`:654` goal-status／`:899` goal-expiring／`:906` goal-predict／
`:913` goal-vs-actual／`:949` view.profile）；`write.ts` 8 处（`:598`·`:599` profile.set·update／
`:629` profile.activity／`:637` goal.set／`:655` goal.water／`:663` goal.weight／`:674` goal.pause／`:680` goal.resume`）。

**改后：清单归零、老 `case` 全删。** 两张分片留空数组（文件保留不删，`legacy/` 只进不出）；
`cmd_read.ts` 的 `case 'calorie.` 标签 61 → 51（恰减我 10 条），行数 1146 → 1026；
`write.ts` 34 → 26（恰减我 8 条），行数 745 → 622。**保留下来的 `case 'calorie.profile.set/update/activity':`
各有 1 处，住 `write.ts` 的 `profileReceiptDoc`**——那是按命令名认领「整页回执」的小 switch，
`dispatchWrite` 调它（`?? res.html`），必须留。

### 3. 域内件是否都在能力目录内（不再散在 `render/`／`fetch/`）？

**半新半旧，确如票面所说。** 写前草稿与预检页早已搬进能力目录（`goal/set.ts`、`goal/precheck.ts`、
`profile/setup.ts`、`profile/view.ts`）；但**读链的取数与装页**住 `render/`（`goal.ts:32` `buildGoalView`、
`goalPlate.ts:45/65/88/126/160`、`goalExtra.ts:38/71/107`、`html.ts:122/261/278/296/306/321/565/587`、
`trendDocs.ts:441`），**写库**住 `fetch/`（`goal.ts`／`nutritionGoal.ts`／`profile.ts`），
**写命令体**内联在 `cli/write.ts` 的 `case` 里。

本票处置见 §二「散件」一行：把**内联在分派层的入口体**搬进能力目录（这是「域内件」里真属于本票的那一半）。
`render/`／`fetch/` 的取数装页件**本票不搬**——编排者裁决（六席一致）：票面第 2 条的**实现层部分**转专票 **#324**
（窗口风险控制），本票只做「窗口 A」＝声明 ＋ `case` ＋ 路由记录 ＋ 核验与补齐。越界记账见 §五.5。

---

## 二 · 核验发现 vs 补齐动作（分列）

| # | 核验发现（改前事实，带地址） | 补齐动作（本票做的） |
| --- | --- | --- |
| A | `src/goal/`、`src/profile/` **没有** `commands.ts`／`index.ts`（§一.1 表） | 建两个能力的三件登记面 ＋ 两件入口（§一.1 改后） |
| B | 18 键仍住未搬迁清单（`scene-06.ts:12-24`、`scene-07.ts:12-16`） | 声明搬进 `goal/commands.ts`／`profile/commands.ts`；分片搬空（不删文件） |
| C | 18 键的老 `case` 仍在分派层（`cmd_read.ts` 10 处、`write.ts` 8 处，地址见 §一.2） | 老 `case` ＋ 随之变死的 import 一并删除（机器自证见 §五.1） |
| D | **散件**：读/写入口体内联在分派层（`cmd_read.ts`／`write.ts`），能力目录里没有它们 | 搬成 `goal/read.ts`＋`goal/write.ts`／`profile/read.ts`＋`profile/write.ts`（逻辑一字未动，见 §五.4） |
| E | 路由声明住在两场景分片（`legacy/routes/scene-06.ts:11-39`、`scene-07.ts:11-16`） | 28 条记录搬进 `goal/routes.ts`／`profile/routes.ts`（`order` 原值）；**留下 7 条**（键属别票） |
| F | **写命令的两处覆盖已存在**：`test/cmd-write-40-persist.test.mjs:443-508`（档案 3 条＋目标 5 条逐列回读）＋ `docs/research/t97-probe-receipts.mjs:109-116`（8 条场景行） | **无需补**——票面第 3 项「加写命令补两处」在这两个场景**已经是既成事实**；它们经 `REGISTRY` 新路仍会跑通，这正是本票判据 |
| G | `calorie.view.profile` 声明**缺 `wakeWord`**（`scene-07.ts:15`）；`CommandSpec.wakeWord` 当必填（`shared/commandSpec.ts:46`） | 改由票 **#323** 把该字段改成可缺；本件**照原样省略**，不替产品补词（编排者裁决） |
| H | **3 条声明的代表唤醒词在路由表里落的是别的键**（#294 对账口径「代表唤醒词须路由回同键」，`test/cmd-registry-294.test.mjs:220-229`；该断言今天只跑 `WEIGHT_COMMANDS`，故不报红）：`calorie.view.goal`＝`看今日目标进度`→落 `goal-progress`；`calorie.view.goal-config`＝`定营养目标`→落 `goal.set`；`calorie.view.goal-recommend`＝`定营养目标(自动算)`→落 `goal-wizard` | **只记账、本票不改**（编排者裁决：代表唤醒词属产品内容，改它要连带 `help:build`＋`SKILL.md`）。逐条账见 §五.6；候选词只作建议，未落进声明 |
| I | 分派层直接摸能力目录内部件：`write.ts:59-60` 从 `../profile/setup.js`／`../profile/update.js` 导入回执页装配函数（对照 `render/goalExtra.ts:19` 走的是 `../weight/index.js` 那道门） | 改经 `../profile/index.js` 取用（门转出这两个函数，仍 ≤5 件）；`render/index.ts:37` 直接转出 `../profile/view.js` 属**越界**，本票不碰，记账报告（§五.5） |

---

## 三 · 搬迁清单（18 键 · 六字段逐字照抄）

来源＝`src/cli/legacy/scene-06.ts:12-24`（13 条）／`scene-07.ts:12-16`（5 条）。落点见「落点」列。

| 键 | kind | shape | title | 代表唤醒词 | 迁出（file:line） | 落点 |
| --- | --- | --- | --- | --- | --- | --- |
| `calorie.goal.pause` | write | receipt | 暂停目标 | 暂停所有目标 | `scene-06.ts:12` | `goal/commands.ts` |
| `calorie.goal.resume` | write | receipt | 重启目标 | 重启所有目标 | `scene-06.ts:13` | `goal/commands.ts` |
| `calorie.goal.set` | write | receipt | 定营养目标 | 定营养目标 | `scene-06.ts:14` | `goal/commands.ts` |
| `calorie.goal.water` | write | receipt | 定饮水目标 | 定饮水目标 | `scene-06.ts:15` | `goal/commands.ts` |
| `calorie.goal.weight` | write | receipt | 定体重目标 | 定体重目标 | `scene-06.ts:16` | `goal/commands.ts` |
| `calorie.view.goal` | read | stat | 目标分析 | 看今日目标进度（发现 H） | `scene-06.ts:17` | `goal/commands.ts` |
| `calorie.view.goal-config` | read | stat | 目标配置 | 定营养目标（发现 H） | `scene-06.ts:18` | `goal/commands.ts` |
| `calorie.view.goal-expiring` | read | stat | 即将到期目标 | 看即将到期的目标 | `scene-06.ts:19` | `goal/commands.ts` |
| `calorie.view.goal-predict` | read | stat | 目标预测达成 | 看目标预测达成 | `scene-06.ts:20` | `goal/commands.ts` |
| `calorie.view.goal-recommend` | read | stat | 目标推荐 | 定营养目标(自动算)（发现 H） | `scene-06.ts:21` | `goal/commands.ts` |
| `calorie.view.goal-status` | read | stat | 目标状态 | 看目标状态 | `scene-06.ts:22` | `goal/commands.ts` |
| `calorie.view.goal-vs-actual` | read | stat | 目标对比实际 | 看目标对比实际 | `scene-06.ts:23` | `goal/commands.ts` |
| `calorie.view.goal-wizard` | read | stat | 目标预检 | 看目标预检 | `scene-06.ts:24` | `goal/commands.ts` |
| `calorie.profile.activity` | write | receipt | 设活动量 | 设活动量 | `scene-07.ts:12` | `profile/commands.ts` |
| `calorie.profile.set` | write | receipt | 设置档案 | 设置档案 | `scene-07.ts:13` | `profile/commands.ts` |
| `calorie.profile.update` | write | receipt | 改档案 | 改档案 | `scene-07.ts:14` | `profile/commands.ts` |
| `calorie.view.profile` | read | stat | 档案视图 | **无**（发现 G，照原样省略） | `scene-07.ts:15` | `profile/commands.ts` |
| `calorie.view.profile-wizard` | read | stat | 档案预检 | 看档案预检 | `scene-07.ts:16` | `profile/commands.ts` |

**键集一字未增未减**（18 条进、18 条出）⇒ 命令数不变、行为不变。

---

## 四 · 路由记录（28 条 · `order` 原值照抄，顺序不动）

- `src/goal/routes.ts`：**22 条**（`wake` 20 ＋ `new` 3 ＋ `repair` 1 中属本票的 22 条；`order` 208–215、221–223、226–232、`new` 4／5／57、`repair` 0）——
  逐行照抄自 `legacy/routes/scene-06.ts:11-39` 里键属本票的那些行。
- `src/profile/routes.ts`：**6 条**（`wake` 233–236 ＋ `new` 33、56）——照抄自 `legacy/routes/scene-07.ts:11-16`。

**留在 `legacy/routes/scene-06.ts` 的 7 条**（键不属本票，按「记录归属＝键的所有者」归别票）：
`order=216/217/218/220/224/225` 六条 → `calorie.view.goal-progress`（键属场景 01＝#314）；
`order=219` 一条 → `calorie.view.goal-weight`（键属场景 03＝#315）。`routes/scene-07.ts` 留空数组。

机械自证（`.scratch/t318/scan-routes.mjs`）：全 495 条记录里 **`(list, wakeWord)` 跨件组 = 0**
（`gen-routes.mjs:120-134` 的整组守卫不会被踩）；本票 18 键的**全部**记录都在我这两片里
（**别的分片里 0 条** ⇒ 不需要去别人的片里定点删除）。

---

## 五 · 免费判据、自证与偏离记账

### 1. 分派层手术的机器自证（内容定位版，非行号版）

落点用 `.scratch/t318/surgeon.mjs`：**读落点时刻的现值** → 按内容定位摘我那些 `case` 块 →
摘掉随之变死的 import → 写回（默认 dry-run，`--write` 才落盘）。自证（`--selftest --from-head`，
拿已提交 blob 跑）：`cmd_read.ts` 1146 → 1026 行、`case 'calorie.` 61 → 51；`write.ts` 745 → 622 行、
34 → 26；**断言全过**（我的 18 个 case 计数符合预期、16／14 个死引用 0 残留、6／9 个仍在用的标识符
仍在、无 3 行以上连续空行、行数与 case 计数都下降）。

### 2. 防回滚守卫（编排者裁决）

写树前逐条比「仓内现值 vs 预备期 SHA」（`.scratch/t318/prep-hashes.txt`）：
`HARD`（我独占：两张**命令**分片 ＋ 我自己新建的 10 件）不等 ⇒ `TRIPPED` 退 1；
`SOFT`（`cmd_read.ts`／`write.ts`／**两张路由分片**）不等 ⇒ 只记 `SHARD-DRIFT`，
因为这几件一律走**定点删除**（读现值 → 只删我的行 → 写回），整文件覆盖会静默回滚别人的搬迁
（#294 的 case 棘轮是「⊆ 冻结清单 ＋ size ≤ max」，复活一条**仍在冻结清单里**的 case 是合法的 ⇒ 不红）。

### 3. 免费判据（实测见 §七）

期望：生成物里**只出现 `src/cli/registry.ts`**（能力声明从 1 家变 3 家）＋ 我删的那些 `case` 行。
`keys.ts`／`combos.yaml`／`routes.generated.ts`／`build-help.mjs` **逐字节不动**——因为本票
**既没加也没删命令**，六字段（含 `wakeWord`）一字未改 ⇒ REPR／EXAMPLE 两段与 `keys.ts` 的键表都不变。

### 4. 偏离记账（与派单／规则书的差）

1. **多加了 4 件文件**：派单说「只加 `commands.ts`／`routes.ts`／扩 `index.ts`」，本席另在
   `src/goal/`／`src/profile/` 各加了 `read.ts`／`write.ts`（两件入口）。理由有二：
   ① 票面完成判据②要的是「形状与**参考实现**一致」，而 `src/weight/` 的形状是
   `commands.ts` 只放声明、处理函数住同目录的专门件（`log.ts`／`edit.ts`／`history.ts`…）；
   把 18 条命令的处理体塞进 `commands.ts` 会让该文件从「声明数组」退化成「声明＋实现混装」。
   ② 派单「不许动既有导出与实现」与票面「散件搬进来」直接冲突：既有 6 件不能改，散在分派层的入口体
   又必须有个住处 ⇒ 新增两件是唯一同时满足两条的落法。**既有 6 件一字未动**（可用 `git diff` 自证）。
2. **没下调任何棘轮上限／冻结集**（编排者裁决：终态归 #320）。本票是**纯搬迁**，`⊆` ＋ `≤` 自动绿。
3. **没跑 `pnpm help:build`**（编排者裁决）；`test/skill-t11.test.mjs:53` 的「互联区新鲜」
   断言本票天然满足（REPR 不变 ⇒ `buildHelpBlock()` 不变；预备期实测盘上块 == 生成块 = `true`）。
4. **没给 `calorie.view.profile` 补 `wakeWord`**、**没改 3 条不符的代表唤醒词**（发现 G／H，见 §二）。

### 6. 记账：3 条代表唤醒词与路由不同键（**只记账，不许改**——编排者裁决）

判定依据：代表唤醒词是**产品内容**（决定 `SKILL.md` 速查表第一列），本票是**纯搬迁、行为不变**；
改它还会连带 `pnpm help:build` 与 `SKILL.md` 重生成，超出本票射程。**候选词只作建议，未落进声明。**

| 键 | 现代表唤醒词（声明里） | 它实际落到的键（路由表） | 建议的同键候选词（均在路由表内 · 未采用） |
| --- | --- | --- | --- |
| `calorie.view.goal` | `看今日目标进度` | `calorie.view.goal-progress`（`routes/scene-01.ts:14`，`list=wake` `order=4`） | `看目标完成度`（`routes/scene-06.ts` `order=222` → 本键）／`看目标历史完成`（`order=231` → 本键） |
| `calorie.view.goal-config` | `定营养目标` | `calorie.goal.set`（`routes/scene-06.ts` `order=208`） | `看目标配置`（`routes/scene-06.ts` `list=new` `order=4` → 本键） |
| `calorie.view.goal-recommend` | `定营养目标(自动算)` | `calorie.view.goal-wizard`（`routes/scene-06.ts` `order=209`） | `看目标推荐`（`routes/scene-06.ts` `list=repair` `order=0` → 本键） |

判红口径在哪：#294 的对账断言是「代表唤醒词必须路由回同键」（`test/cmd-registry-294.test.mjs:220-229`），
但它今天**只跑 `WEIGHT_COMMANDS`**，故这 3 条不报红。**转场景图／#324 定。**

### 7. 越界／未确证（报告，不擅自改）

- `packages/skill-calorie/src/render/index.ts:37-38` 直接转出 `../profile/view.js` 的 `buildProfileView`／
  `ProfileView`——按铁律一该走能力目录那道门（`../profile/index.js`）。`render/` 不在本票车道，**未碰**。
- `packages/skill-calorie/src/cli/write.ts` 的 `case` 区里仍内联着别的场景的写命令体（本票只摘自己 8 条）。
- 读链的取数／装页件仍在 `src/render/`（`goal.ts`／`goalPlate.ts`／`goalExtra.ts`／`html.ts`／`trendDocs.ts`）、
  写库件仍在 `src/fetch/`——票面第 2 条的**实现层部分**按**编排者裁决转专票 #324**（窗口风险控制）；
  本票只做「窗口 A」＝声明 ＋ `case` ＋ 路由记录 ＋ 核验与补齐。把它们搬进能力目录要动
  `render/html.ts`／`render/index.ts`（全场共用、别的票的车道），本票**不碰**。
- 代表唤醒词 3 条不符（§五.6）＋ `calorie.view.profile` 缺字段（发现 G／#323）**未修**，转场景图／收口票。
- 另加的两件入口（`read.ts`／`write.ts`）与派单「只加三件」的偏差**已获编排者批准**（理由见 §五.4.1）。

## 六 · 门禁对账（`GATE-RUN` 声明 · 本窗口 ticket=318-land）

> 本窗门禁（build／test／gen:check／t293／snapshot）在**已持锁的窗口进程内**直接调用（编排者 #319 裁决：窗口内禁嵌套 `run-locked`＝第二把锁）⇒ 载体＝**本窗口自身那一次持锁运行**。
> 每行对应锁日志里同 runId 的条目（命令与退出码以日志为准）；导出件（受跟踪）：`docs/skills/skill-calorie/t318-gate-runs.log`。

- GATE-RUN runId=4099491b-da8e-43ef-99bc-82bc2f516b49 cmd=pwsh -NoProfile -File .scratch/t318/run-window.ps1

> 上条 runId 取自该次运行的 `START` 行（抢锁时落盘）；其 `RUN` 行在**释放锁时**落下（两行 runId 相同）⇒ 本档在窗口内只声明载体，**对账与导出按 §2.4.4 在窗口退出之后做**：
> `node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t318-核验补齐-证据.md --log .scratch/locks-land/gate-runs.log --ticket 318-land --since 2026-09-13T17:58:00.390Z --export docs/skills/skill-calorie/t318-gate-runs.log`

## 七 · 落点实测（写树之后、提交之前）

```text
== git status --short ==
 M packages/skill-calorie/src/cli/cmd_read.ts
 M packages/skill-calorie/src/cli/legacy/routes/scene-06.ts
 M packages/skill-calorie/src/cli/legacy/routes/scene-07.ts
 M packages/skill-calorie/src/cli/legacy/scene-06.ts
 M packages/skill-calorie/src/cli/legacy/scene-07.ts
 M packages/skill-calorie/src/cli/registry.ts
 M packages/skill-calorie/src/cli/write.ts
?? packages/skill-calorie/src/goal/commands.ts
?? packages/skill-calorie/src/goal/index.ts
?? packages/skill-calorie/src/goal/read.ts
?? packages/skill-calorie/src/goal/routes.ts
?? packages/skill-calorie/src/goal/write.ts
?? packages/skill-calorie/src/profile/commands.ts
?? packages/skill-calorie/src/profile/index.ts
?? packages/skill-calorie/src/profile/read.ts
?? packages/skill-calorie/src/profile/routes.ts
?? packages/skill-calorie/src/profile/write.ts

== git diff --stat ==
 packages/skill-calorie/src/cli/cmd_read.ts         | 127 +------------------
 .../src/cli/legacy/routes/scene-06.ts              |  22 ----
 .../src/cli/legacy/routes/scene-07.ts              |   6 -
 packages/skill-calorie/src/cli/legacy/scene-06.ts  |  22 +---
 packages/skill-calorie/src/cli/legacy/scene-07.ts  |  14 +--
 packages/skill-calorie/src/cli/registry.ts         |   4 +
 packages/skill-calorie/src/cli/write.ts            | 134 +--------------------
 7 files changed, 21 insertions(+), 308 deletions(-)

== 生成物逐件 sha256 ==
packages/skill-calorie/src/cli/registry.ts  CHANGED
packages/skill-calorie/src/cli/keys.ts  unchanged
packages/base-combos/combos.yaml  unchanged
packages/skill-calorie/scripts/build-help.mjs  unchanged
packages/skill-calorie/src/triggers/routes.generated.ts  unchanged
```

## 偏离与风险：墙钟耦合的票外既有红（已由 326 修复席处置）与两道机器控制

### 现象与结论（**票外**，非本票回归、非 flake）
- `packages/skill-calorie/test/cmd-read-t11.test.mjs:201-202` 用 `calorie.history {days:7}` 断言 `total >= 2`，而种子历史锚在 **09-06／09-07** 两条：**09-13 23:58** 的窗口覆盖 2 条 ⇒ 绿；**09-14 00:01 起**只剩 1 条 ⇒ **确定性红**（与该测试的墙钟耦合，与并发、与本票落点内容**都无关**）。
- 证据：同一份落点内容两跑结果不同（`ℹ tests 1492／pass 1492／fail 0` @23:58 对 `pass 1491／fail 1` @00:01），差异点可用「7 天窗覆盖 2 条 vs 1 条」的算术解释。
- 处置：**已由 #326 处置并关闭** —— 落地 `6e34766`（`test(326): calorie.history 门与墙钟解耦`）：`--require` 预载把「当刻」钉到种子锚 `2026-09-07`，`days:7` 与 `total>=2` 一字未改，另**加**一条 `days` 语义断言；全量套件回到 `ℹ tests 1492／pass 1492／fail 0`，t293 `5/5`、`UNACCOUNTED=0`，11 个插件 `dist/client.js` 未破。本票**不背**这条，也不把它当 flake 反复重跑。

### 两道机器控制（本窗在链上，保留）
- **控制 1 进场前静默检查**：跑套件前查 `Get-CimInstance Win32_Process -Filter "Name='node.exe'"` 里命令行含 `--test` 的进程；有 ⇒ 等 30 s 重试，最长 5 分钟；超时即停手回报。
- **控制 2 红后同树原样复跑**：`fail > 0` 时在**同一棵未改动的树**上原样再跑一次；两次不同 ⇒ 判环境性红，两次原始输出都留档并**继续落地**；两次同红 ⇒ 停手（真回归或确定性环境红，交编排者判）。

### 规则 30（编排者立）
- 凡跑全量套件、或会写共享库的 CLI 探针（含 `t293`），一律先持同一把 `--lock-dir .scratch/locks-land`；复核席在隔离副本里跑整套件同样要持这把锁（隔离副本只隔离文件，不隔离 `%APPDATA%`／共享历史库）。

### 归属
- 墙钟耦合的红归**该测试属主／326 修复席**处置；共享历史库的并发隔离也归测试属主（或 #320 收口）。本票只做 18 键搬迁与「语义不动」的复验。

### t293 P1 的判官误报（第五轮查明；交探针席修判官，本票不动判官、不动测试件）
- **硬规则 31 对照实验**：同一把锁、同一探针、**不落点**跑一次 ⇒ `RESULT: 5/5`、`UNACCOUNTED=0 处`（ticket `318-t293-baseline`，runId `0df73fb2`，HEAD `2ec54d3`）；**落点后** ⇒ `P1=FAIL`、`UNACCOUNTED=6 处`、`4/5`（runId `d3b7e009`）。两组原始输出：`.scratch/t318/logs/win/land-20260914-002553.log`（落点后）与终端 `t318 v5`（基线组）。
- 机械根因：`pinnedCountSurfaces()` 拿 `authorityTotals()` 的 `total/writes/reads/weight` 当**搜索锚**（落点把未搬迁清单从 19 条压到 1 条 ⇒ 锚从 `28/13` 变成 **`10/4/6`**），凡断言行里以裸数字命中即判「钉死计数」并**无条件**入 ③。
- 6 处逐条：`help-center-91.test.mjs:126`（命中的数只在**消息文本**里）、`:204`／`:219`（**照片 HELP 自己的 10 键**）、`profile-guard-175.test.mjs:72`（**退出码 4**）、`test/calorie-routing-81.test.mjs:320`／`:213`（命中的数只在**注释**里）。其中「照片 10 键」在声明里**无对应族**（含 photo 的声明键只有 8 条：7 条 `calorie.photo.*` ＋ `calorie.view.photo-log-wizard`；`buildPhotoHelp()` 的 10 条是 `body_photo_*` 渲染侧条目）⇒ **不存在诚实的 ① 派生源**，硬加 `DECLARED_*` 只是骗正则。
- **② 纪律桶是死代码**：`buckets.disciplined` 只在 365 行初始化为 `[]`、467-469 只打印，**全文件无 `disciplined.push`** ⇒ 没有可登记的「纪律位」。故处置口径＝探针席按**主体**收窄锚判定（域内计数与「权威总量」数值巧合不算钉死）＋ 落地或诚实删掉 ② 桶；**本票未改任何测试件**，`test/calorie-routing-81.test.mjs`（冻结地基）**一行未动**。
- 记账（如实）：曾备 `.scratch/t318/derive-photo-help-count.mjs`（定点、fail-closed、幂等）想把 204／219 两处派生化，**因找不到诚实派生源而废弃**（实测：`buildPhotoHelp()` 的 10 条是渲染侧 `body_photo_*` id；声明源里含 "photo" 的键只有 8 条＝7 条 `calorie.photo.*` ＋ `calorie.view.photo-log-wizard`；`calorie.body-photo.*` 为 0 条）。**该脚本未接线、未运行**；编排者裁决 A（判官按主体收窄）＋ B（② 桶落地）后，本票无需改动任何测试件。

### 门的射程＝**不一致**（不是变更本身；编排者裁决，本窗据此重做 step 12 的 M2）
- 判据口径：**一致改名**（声明 ＋ 重建 ＋ 重生成）**不触发任何门**——设计如此，不是门失灵。机器门的射程是「声明与生成物**不一致**」；被门抓住的**只能**是不一致。
- 前任那版 M2 期望「一致改名 ⇒ t293 验收探针必红」是把射程读错（一致改名是**合法改动**，探针不该红），故按裁决**改期望、不改判官、不改测试件**：新 M2 只改声明 key、**故意不跑 gen**（`calorie.goal.water` → `calorie.goal.waterMUT`）⇒ `build` 须 0（声明本身类型无误）、紧跟 `gen:check` 须非 0；复正后 `build／gen／gen:check` 全 0，且 **5 件生成物 sha256 与变异前逐字节相同**。
M2（**不一致**改名；只改声明、故意不跑 gen）：变异后 build exit=0、gen:check exit=1（须非 0）；复正后 build／gen／gen:check 全 0，且 5 件生成物 sha256 与变异前**逐字节相同**（SAME 5/5）
M2-b（手改镜像、声明不动）：盘上镜像无 goal 键 ⇒ 退化为尾部追加一行注释的手改 ⇒ gen:check exit=1（须非 0）；复正后 gen:check 0、registry.ts sha256 复原

### 教训①：读红先认 runId ＋ 日志名（不跨轮混读）
- 本链路每轮写同一批日志名字；先入为主会把**前一／两轮**的 `registry.ts TS2307` 当成当轮红，而**当轮**其实是**路径守卫**红（`Abs-Checked` 越界：`packages/base-combos/combos.yaml`）。规程：任何「红」先认 **runId ＋ 日志文件名**，再只读那一段原文。
- 本席可证部分：`Abs-Checked` 的注释（行 122–126）自记「01:30 那轮 gate 11 全绿后被它挡死：`FAIL: 路径不在能力包内：…\packages\base-combos\combos.yaml`」⇒ 路径守卫确曾当轮红；「误读成 `registry.ts TS2307`」一节为**前任自陈**（编排者转述），本席未复现，按「未复现者标源」记账。

### 教训②：写树必须刷 mtime ＝ 治本点（`--force` 只是兜底）
- 治本点＝**每次写树都刷 mtime**：`Copy-Item` 保旧时间戳 ⇒ 若复制件内容变了而 mtime 仍旧，`tsc -b` 的增量判据会把「内容已变、时间戳未变」的件当成最新，出现假绿／假红。
- **本席回读订正（第六任，按现行树逐行核）**：交办原话是「step 3 复制 12 件后未刷 `$FILES` 与其父目录，只有 `$SOFT` 在 4d 被刷」，**该说法对现行树已不成立**——step 3（行 217–228）对复制的**每一件**都刷了 mtime（行 225，注释即「Copy-Item 保旧 mtime ⇒ tsc -b 假绿」），step 4d（行 254）对 4 件 `$SOFT` 写盘后也刷。**仍未刷的是这些件的父目录时间戳**（行 223 只 `New-Item -Force` 建目录，不刷已存在目录）。故本账如实记为「文件级已刷、目录级未刷」，不照抄失真的原话。
- 规则 24 口径：基线绿**不带** `--force`；确需强制只收窄到 `packages/skill-calorie`，⛔ 不删 `dist/`、⛔ 不仓根 `--force`。本席回读 `land.ps1` 全文件：**执行路径里 `--force` 出现 0 处**（唯一命中本段说明文字）⇒ 本窗口门禁链无一处以强制起跑。
- 强制不得当长期手段：治本点仍是「写树即刷 mtime（含父目录）」。

### dry 与真跑必须同树状态
- `dry` 与真跑跑**同一条完整链路**（门禁链 ＋ 变异自证都真跑），只在收尾短路：dry **只**跳过 14 暂存／15 提交／16 推送／17 关门／18 票面（13 证据件在 dry 里也真写，由 dry 收尾的 `Rollback` 点删）。故 dry 的绿只有在「两段从同一树状态起步」时才可外推到真跑；dry 结束若活树没回干净（`git status --porcelain -uall` 非空）就**不得**进真跑。
- **本窗实测**：未出现 `fail > 0`（两道控制未触发）。
