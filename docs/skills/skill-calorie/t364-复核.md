# #364 · 删除取快照（独立复核）

- 复核对象：#364「卡路里场景 08 身体细节 · 删除取快照」。票面权威＝`gh issue view 364`（该票**零评论**，正文即全部票面）。
- 作者四笔：`beeeb69`（feat，5 件）／`a05a4b0`／`0cd9938`／`8618878`（后三笔＝证据件与对账导出补收尾），已 push。
- 结论：**判据面全绿**（票面四条 ＋ 负向对照 ＋ 软删语义 ＋ 本席自设 4 条新探针全部复现），**带 1 条本票引入的台账陈化**（见 §七），关票前须一行 `--sync` 收口。
- 本件只写复核结论；实现件、测试件、台账一字未改（逐文件 sha 自证见 §四 末行）。复核现场 `.scratch/t364-review/`（草稿，不随提交），**未复用**作者 `.scratch/t364/`。

## 一、提交范围与禁碰清单（逐笔自己核）

`git show --name-only` 四笔并集＝6 件，与声明一致：

| 笔 | 件 |
|---|---|
| `beeeb69` | `.changeset/t364-body-delete-snapshot.md`／`docs/skills/skill-calorie/t364-快照证据.md`／`packages/skill-calorie/src/body/remove.ts`／`packages/skill-calorie/src/fetch/body.ts`／`packages/skill-calorie/test/t364-删除取快照.test.mjs` |
| `a05a4b0` | `docs/skills/skill-calorie/t364-快照证据.md`／`docs/skills/skill-calorie/t364-门禁对账.md` |
| `0cd9938`／`8618878` | `docs/skills/skill-calorie/t364-快照证据.md` |

- **禁碰清单 0 命中**：`src/triggers/**`、`src/body/routes.ts`、`src/body/bodyDocs.ts`／`bodyPlate.ts`／`view.ts`／`log.ts`、`scripts/check-warning-line.mjs`、`packages/skill-calorie/AGENTS.md`——四笔并集里一件未现。
- **中文无乱码**：6 件逐件扫，替换符 U+FFFD 命中 0、拉丁扩展字符命中 0（汉字数：`remove.ts` 540／`fetch/body.ts` 1616／测试件 2007／证据件 3799）。
- 票面正文写的证据路径是 `docs/skills/skill-calorie/` 下的 `t12-快照证据.md`（票面自身笔误，`t12` 应为 `t364`）；作者落的 `docs/skills/skill-calorie/t364-快照证据.md` 符合仓内惯例（`docs/skills/` 下按件名一目录、件名内文件名带票号）。

## 二、判据面：期望值自立的复跑（自建 tmp 库）

现场：`.scratch/t364-review/db`（`SKILLS_DB_PATH` 指它；schema 由产品自己 `openDb` 建，行与期望值由**本席另开 `node:sqlite` 手柄**直插直读，不经命令）。
自选样例（与作者件全不同）：体脂 `2026-01-17`／`hospital`／`23.75`，7 点皮褶只填胸 `11.5`、大腿 `14.5`，备注空；围度 `2026-02-08`，13 项只填腰围 `79.5`、肩围 `112.25`、左小腿 `36.75`，备注 `空备注对照`。

| 判据 | 读数（机读；命令经 `tooling/run-locked.mjs --ticket 364`） |
|---|---|
| ① 两条命令 exit 0 | `RUN C key=calorie.body.composition-remove exit=0`／`RUN M key=calorie.body.measure-remove exit=0`（命令名以 `packages/skill-calorie/src/body/commands.ts` 注册表为准） |
| ② 逐字等于删前查库值 | 52 条断言全绿。体脂逐字段 11 格：`日期 2026-01-17、来源 医院测、体脂率 23.75、胸 11.5、腹 —、大腿 14.5、三头肌 —、肩胛下 —、髂上 —、腋中线 —、备注 —`；围度逐字段 15 格：`日期 2026-02-08、胸围 —、腰围 79.5、腹围 —、臀围 —、左大腿 —、右大腿 —、左小腿 36.75、右小腿 —、左上臂 —、右上臂 —、左前臂 —、右前臂 —、肩围 112.25、备注 空备注对照`（基线均取自**跑命令之前**的另开句柄整行读） |
| ② 摘要句 | `已删除体脂记录 #1（日期 2026-01-17 · 体脂率 23.75% · 来源 医院测）（软删除：…）`；围度摘要只摆已填 3 项 `腰围 79.5cm、左小腿 36.75cm、肩围 112.25cm`，11 处缺项不摆 |
| ② 缺项写 `—` | 体脂 7 点里 5 处 `—`、围度 13 项里 10 处 `—`、空备注 `—`（**按格定位**断言，不做全文 includes） |
| ③ 被删主键不可查（软删口径） | 本仓读侧口径＝`COALESCE(is_deprecated,0)=0`（`listCompositions`／`listMeasurements` 同句，见 `packages/skill-calorie/src/fetch/body.ts` 第 164／318 行）：`WHERE id=? AND COALESCE(is_deprecated,0)=0` → **0 行**；`SELECT id,is_deprecated WHERE id=?` → **行仍在**且 `is_deprecated=1`；两表全表行数不变（1／1） |
| ④ 快照路径存在可读 | `envelope.data.output` 指向的落盘页存在、非空、可读（口径见 §六）。体脂 576 字节／围度 695 字节 |
| ⑤ 负向：去掉快照段 → 必红 | 见 §五 M1：改坏 `exit=1`（`RESULT-baseline: FAIL pass=3 fail=5`，首红＝判据②三主格）→ 复原 `exit=0`（`PASS pass=52 fail=0`） |
| 不许动：软删仍置废 | 全程无物理删：删后行数不变、`is_deprecated=1`、`DELETE` 语句在 `packages/skill-calorie/src/fetch/body.ts` 零命中 |

**防「回显输入」**（本席自设，作者件未覆盖）：跑删除**之前不经命令直接改库**——体脂率 23.75→29.5、来源 hospital→gym、大腿皮褶 14.5→17.5；围度腰围 79.5→71.25、胸围 null→97.5。两条回执逐格**跟库真值**：`体脂率 29.5%`／`来源 健身房 InBody`／`大腿 17.5`／`腰围 71.25`／`胸围 97.5`（10 条断言全绿），且可见文本里**没有**旧值 23.75／79.5。⇒ 回执取的是库，不是参数回显。

**删后仍可回读快照**（作者称取数口不加 `is_deprecated` 过滤）：删后按 id 回读，`body_fat_pct` 仍 23.75、`waist_cm` 仍 79.5、`chest_cm` 仍 `null`（库内不存 `—` 字）。

## 三、本席自设的新探针（作者件未覆盖）

| 探针 | 读数 |
|---|---|
| A 删不存在的 id | 两条命令均 `exit=4`，`stderr` 逐字 `ERR 4: 取数失败：id=987654 不存在`，`data.output` 空（不落空回执） |
| B 同 id 二次删 | 首次 `exit=0`，二次仍 `exit=0`（`UPDATE … WHERE id=?` 无 `is_deprecated` 过滤，`changes` 仍 1）；二次删后全表仍 1 行、置废位仍 1、回执仍带删前值（软删不抹内容）。此行为**改前改后一致**（`beeeb69` 之前的 `delete*` 同句 `UPDATE` ＋ 同句 `changes` 判据），非本票引入 |
| C 次序随库列声明序 | 13 项标签逐位＝库列声明序（胸围／腰围／腹围／臀围／左大腿／右大腿／左小腿／右小腿／左上臂／右上臂／左前臂／右前臂／肩围），且**不是**字典序；体脂 7 点逐位＝胸／腹／大腿／三头肌／肩胛下／髂上／腋中线 |
| D 标签唯一来源 | `remove.ts` 只 `import`：`CALIPER_SITE_LABELS`（`packages/skill-calorie/src/body/bodyPlate.ts`）、`MEASUREMENT_ZH`／`MEASUREMENT_FIELDS`／`CALIPER_FIELDS`（`packages/skill-calorie/src/fetch/body.ts`）、`SOURCE_LABELS`（`packages/skill-calorie/src/kcal.ts`）；**无第二份名表字面量**（21 个部位／来源中文名的字面量命中 0） |

## 四、变异：改坏必红／还原必绿（持锁窗口内做，释放前逐文件复原并核 sha）

| 变异 | 改坏读数 | 还原读数 |
|---|---|---|
| M1 去掉快照段（摘要回旧一行 ＋ `detail` 置空） | `tsc=0 变异已 emit=true`，`RESULT-baseline: FAIL pass=3 fail=5`，首红＝判据②日期／体脂率／来源 | 两件 sha 相符，`tsc=0`，`PASS pass=52 fail=0` |
| M2 标签不走唯一来源（改印库列名） | `FAIL pass=50 fail=2`，红格＝体脂 7 点／围度 13 部位标签位 | 同上，全绿 |
| M3 快照改「删后 ＋ 活行」口径 | `FAIL pass=2 fail=6`，首红＝判据① `exit=4` | 同上，全绿 |

- 每条都核了「变异真的被编译进 `dist`」（变异后 emit 的 js 与复原后 emit 的 js 不同），避开「不重编则读数全假」。
- 末行自证：`RESTORED packages/skill-calorie/src/body/remove.ts sha256=8293c0ca… 相符=true｜packages/skill-calorie/src/fetch/body.ts sha256=b582c721… 相符=true`，随后 `tsc -b packages/skill-calorie exit=0`。
- **本席过程读数（如实记）**：变异脚本首跑 `exit=1`——M3 的替换锚点写错文件（锚点属 `fetch/body.ts`，脚本按 `remove.ts` 应用），**M1／M2 已按 finally 复原且 sha 相符，未留下半改状态**；修脚本后复跑全绿。
- M3 的副产品结论：软删不改字段值，单把「读快照」挪到置废之后**观不出来**——必须连活行过滤一起改才构成「取删后值」。这与作者证据件 §五「M2 的一句实话」逐字同向，本席独立复现。

## 五、结构与边界（第 7 条）

- 当刻行数（LF 口径）：`packages/skill-calorie/src/body/remove.ts` **104**（原 29）／`packages/skill-calorie/src/fetch/body.ts` **455**（原 426）／`packages/skill-calorie/test/t364-删除取快照.test.mjs` 385（`test/` 下的 `.mjs` 不在扫描面）。
- `fetch/body.ts` 455 超 350 → 必报五步第四步成立，作者已在证据件 §九 写「已超线，需要根据规则进行重构」＋超因＋拆法（按「写侧校验／围度取数／体成分取数」切姊妹件，本票新增两个快照取数口随表归位）。本席认同「本次先不拆」（本票写集只有两件，拆分不在写集）。
- 真库零写入自核：`D:\2Study\StudyNotes\.db\calorie_data.db` 复核前后同为 sha256 `57CE9D87F5F232822332D7057C2AE8411D99BA7FDECEA289F2028E19FC3FFC15`、3072000 字节、mtime `2026-09-14T05:53:37.7293890Z` 未变（本席一切数据走 `.scratch/t364-review/db`）。

## 六、待裁决口径：「快照文件路径存在可读」＝落盘回执页（第 5 条）

**核事实（本席打开老正本逐行读）**：`D:\2Study\StudyNotes\SKILLS\卡路里\scripts\render_body_delete_receipt.py`

- 件头第 8 行：`流程: 删除前快照(日期/数值/来源) → 软删除 → 回执`；第 11 行：`呈现数据契约: 删除前快照 / 确认回执`。
- 第 88-94 行：删前按活行读整行成 `snapshot`；第 96-100 行：软删；第 105-116 行：由 `snapshot` 造 `detail`；第 128-129 行塞进 `items[0]`。
- 第 184-188 行：落盘 `out_path`⋯`print(f'✅ {out_path}')` ＋ `print(f"⚠️ ACTION=SEND_TO_USER | HTML={out_path.absolute()}")`——**交给用户的「路径」就是这份回执 HTML 的绝对路径**；全脚本无第二份产物（无 JSON 落盘）。
- 仓内先例：`packages/skill-calorie/src/cli/delivery.ts` 的文件态＝`data.output` 与 `delivery.path` 同值同源（落盘页）；`packages/skill-calorie/src/exercise/receipt.ts` 亦把删除快照直接铺进回执页（`删除前快照（共 N 条）`），未另存 JSON。全仓写 `.json` 的两处（`packages/skill-bill/src/fetch/db.ts`／`packages/skill-memo-ilife/src/fetch/db.ts`）都是数据存储，不是回执快照。

**结论：作者口径成立，且与票面一致。** 票面「快照文件路径存在可读」在仓内唯一可落地的读法＝「承载该快照的落盘件＝`envelope.data.output` 指向的回执页」，其存在／非空／可读即判据④，其可见文本里的快照即判据②的那一半。老正本同物同形（`HTML=<abs path>`）。**本席不另立读法**；若上游坚持按字面要求「独立快照文件」，那会是一份全仓无先例的新形状（票面另有「别自己发明新形状」的约束），且需另开票定义文件名／保留期／清理口径。建议在下游票面里补一句口径注：**「快照的载体＝回执落盘页（`envelope.data.output`）」**，免得后续票再走散。

## 七、台账漂移读数（第 6 条）

- **已提交的**台账（`git show HEAD:packages/skill-calorie/AGENTS.md`）该行：`| src/fetch/body.ts | 369 | 426 | …`——「当场实测」列＝**426**；当刻实况＝**455**（LF 口径，与 `beeeb69` 提交件逐字相符）。⇒ **已提交即陈化 +29，不一致**。
- 该行在两个历史点都是 426：`beeeb69^` 的 `fetch/body.ts` 是 426、台账也是 426（一致）；本票把件抬到 455 而四笔**均未碰** `packages/skill-calorie/AGENTS.md` ⇒ **漂移由本票引入**。作者证据件 §三.1／§九 的写法（「已由他席 `--sync` 落成 455」）**只对工作树的未提交 diff 成立**；`AGENTS.md` 当刻确实是 M（他席在途），但它 HEAD 版仍是 426。
- `node packages/skill-calorie/scripts/check-warning-line.mjs --sync --dry`（只演练）读数两支：
  - **对本席存的 HEAD 版台账**（`--agents .scratch/t364-review/AGENTS.head.md`）：`SYNC-PLAN mode=dry 行=27 改=8 增=1 删=0`，逐条：`trendDocs 826→892`／`history.ts 683→741`／`review.ts 600→647`／`trendMiscPortDocs 571→592`／`compare.ts 558→571`／`multiTrendPage 490→501`／**`fetch/body.ts 426→455`**／`wizardPort 265→277`，另 `SYNC-ADD photo.ts LF=355`；`RESULT: 50/59`，9 条 RED 末句都给同一修法。
  - **对工作树台账**（无参）：`改=3 增=0 删=0`——只剩 `history.ts 683→741`／`review.ts 600→647`／`compare.ts 558→571`（工作树台账里 `fetch/body.ts` 已是 455，故本票那行已不漂）。
- **归谁（逐行核 HEAD 行数）**：`trendDocs`／`history`／`review`／`trendMiscPortDocs`／`compare`／`multiTrendPage` 六行＝**他席在途**（HEAD 行数与台账同值，改动未提交）；`fetch/body.ts`（426→455）＝**本票，已提交即陈化**；`wizardPort.ts`（265→277，来源 `3dfe48e` #474）与 `photo.ts`（台账缺行，LF 355，来源 `ec6ee2e` #476）＝**他票已提交的陈化／漏报**。
- `--dry` 未落盘自证：跑前跑后 `packages/skill-calorie/AGENTS.md` sha256 同为 `AF2D6AD10AAC5D902A9BEC120802BEE467326099021BB7BAD8EC6A2730BD5853`；本席**未**跑 `--sync`、未改台账。

## 八、缺陷清单

**本票引入（1 条，须收口）**

1. **台账陈化：`src/fetch/body.ts` 已提交台账 426 ≠ 实况 455**（§七）。这是仓规明写的门（「改了超线件…就必须同步这张台账表，否则本门必红。这是设计行为」）。修法＝谁拿到持锁窗口谁跑一次 `node packages/skill-calorie/scripts/check-warning-line.mjs --sync` 并随票提交（该次同步会把当刻整表快照写实，含他席在途件的新数）——**本席不动台账**（红线），故留给收口窗口或下游票。

**本票范围（2 条措辞级，无实现影响）**

2. 证据件把 `.scratch/t364/mutate.mjs` 与 `.scratch/t364/logs/*.log` 列为「新（草稿）」交付路径，但 scratch 不入 git ⇒ 第三方仅凭提交无法复算变异电池；本席已用自建探针＋自建变异电池独立复现（§四），此条只作口径提示。
3. 票面「交付物路径」把证据文件名写成了 `t12-快照证据.md`（票面自身笔误，`t12` 应为 `t364`，路径同在 `docs/skills/skill-calorie/`）。作者落的 `t364-快照证据.md` 合惯例，不需动作，只需在地图里别按票面字面找 `t12`。

**范围外（只列 S3 ＋ 转票措辞）**

4. **S3** `packages/skill-calorie/src/fetch/body.ts` 第 317／392 行残留变异锚点注释 `// M1-ANCHOR`／`// M2-ANCHOR`（来源＝`f12ebc7` #398；全仓无任何测试或脚本引用这两处标记，属变异电池脚手架漏在产线件里）。**转票措辞**：「转 #398 归属票（或结构/卫生票）：删掉 `src/fetch/body.ts` 两处 `// M1-ANCHOR`／`// M2-ANCHOR` 注释即可，无行为影响；本票#364 未碰该两行、不代修。」
5. **S3** 他票已提交的台账陈化：`wizardPort.ts` 265→277（#474 `3dfe48e`）、`photo.ts` 漏报 LF=355（#476 `ec6ee2e`）。**转票措辞**：「转 #474／#476 或结构票：与 #364 的 `fetch/body.ts` 426→455 同属一次整表 `--sync` 的收口面，一次提交可清三行；本席（#364 复核）不落盘。」

## 九、GATE-RUN 声明（逐条与 `.scratch/locks/gate-runs.log` 同 runId）

GATE-RUN runId=f3a490fc-1cab-4593-afe8-7a52154cca40 cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
GATE-RUN runId=ccb41ded-49d4-48dc-9d5d-27072cbf3bed cmd=node .scratch/t364-review/probe364.mjs baseline
GATE-RUN runId=0eda966d-c8dd-4c46-9d8b-62f2e6303a3d cmd=node .scratch/t364-review/probe364.mjs echo
GATE-RUN runId=e6236a3d-3ab7-41e9-b699-c0741277f898 cmd=node .scratch/t364-review/probe364.mjs probes
GATE-RUN runId=ad2e16fd-6748-4cb5-9289-67143fe074a9 cmd=node .scratch/t364-review/mutate364.mjs
GATE-RUN runId=da4df255-d168-4d78-809d-a7177ebe4862 cmd=node .scratch/t364-review/mutate364.mjs
GATE-RUN runId=e2c2f9f4-7bea-44fc-b909-7a94a4a7c4cf cmd=node --test packages/skill-calorie/test/t364-删除取快照.test.mjs
GATE-RUN runId=29cc6f54-18d6-45d1-a4b5-e65d901bdc1a cmd=node packages/skill-calorie/scripts/check-warning-line.mjs --sync --dry
GATE-RUN runId=19cb6156-5819-4956-ac56-9d2bea0416bf cmd=node packages/skill-calorie/scripts/check-warning-line.mjs --sync --dry --agents .scratch/t364-review/AGENTS.head.md
GATE-RUN runId=b218dc42-7ce1-4a85-bd9f-74ada13c220a cmd=node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t364-快照证据.md --log docs/skills/skill-calorie/t364-门禁对账.md --ticket 364 --since 2026-09-14T17:09:00Z --until 2026-09-14T17:31:59Z
GATE-RUN runId=8d562279-5fd9-4973-bb96-2919c08c8886 cmd=node --test packages/skill-calorie/test/cmd-write-40.test.mjs

**红跑点名（不作通过证据）**：`da4df255`（本席变异脚本锚点写错文件，未落源码即退，见 §四）；`b218dc42`（对账复跑**不加** `--allow-nonzero` ⇒ `matched=5/9 undeclared=3 FAIL`，4 处未命中＝3 条负向红跑 `41c08e87`／`a62b4127`／`226dffdc` ＋ 1 条 `8fa4bf87` 在本导出窗口之外的自指运行）；`8d562279`（他席红复现，见下）。抽查另两条：`29cc6f54`／`19cb6156` 是 `--dry` 演练，脚本自身对「计划非空」给 exit 1，**非落盘失败**（AGENTS.md sha 未变）。

**他席红独立归属**：`node --test packages/skill-calorie/test/cmd-write-40.test.mjs` 当刻 `exit=1`，断言 `/70.2→70 kg/` vs 实收 `已更新体重 #2：70.2 kg → 70 kg（BMI 22.9）`——而**本票两件与 HEAD 逐字节相同**（`git diff --stat HEAD -- <两件>` 空输出），该句来自 `packages/skill-calorie/src/weight/edit.ts`（HEAD 已在，来源 `2e71033`）。⇒ 与作者 §七 归属同结论，且本席用的是更省的一种独立做法（不用撤改即可判定）。

## 十、下一手缺什么

1. **关票前**：一行台账收口（§八 第 1 条）——`--sync` ＋ 提交 `packages/skill-calorie/AGENTS.md`（会把 wizardPort／photo.ts 两行一并写实，见 §八 第 5 条）。
2. 快照承载口径若被上游改判（§六），需在下游票面补「载体＝`envelope.data.output` 落盘页」一句，并保留本件作为该口径的复核依据。
3. #365 整页回执要接的数据面形状，作者已在证据件 §八 写清（`compositionSnapshot`／`measurementSnapshot` ＋ `items[0].detail` 的 `标签 值`／「、」分隔）；本席复跑确认这三样当刻就位（§二／§三 C）。
