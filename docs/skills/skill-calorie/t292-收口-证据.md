# #292 收口证据（目标管理 28 条工作流程 ＋ SKILL 收口 ＋ Q4 判据洞）

> 本票只做文档与生成链，不写页面，不改产品代码（Q4 只加断言）。
> 写集 4 件：`docs/skills/skill-calorie/06-目标管理-工作流程.md`（新，真源）、
> `packages/skill-calorie/SKILL.md`（改 AUTO 块之外）、
> `packages/skill-calorie/test/goal-h1-date-566.test.mjs`（新）、本件（新）。
> 草稿一律在 `.scratch/t292/`（`probe-28.mjs`／`probe-h1.mjs`／`probe-metrics.mjs`／`align-28.mjs`／`mutate-cycle.mjs`／`skill-auto-before.bin`），`.scratch/` 已在 `.gitignore`，不进提交。

## 一、开工状态

- 开工 HEAD：`2c94ebd`（`证据(291)：乙实施证据`；#291 刚落地，`WAKE_TABLE` 已是 6 行）。
- 本票 4 条交付路径开工时 `git status --porcelain` 里只有本席自己的行（`M SKILL.md`、`?? 06-…`、`?? goal-h1-date-566…`、本件 `??`）；全树另有别席在途脏件（`src/diet/*`、`AGENTS.md`、`src/render/goalPlate.ts` 的 staged 删除等），本席一行未碰、不带进提交。
- `SKILL.md` AUTO 块开工基线：`AUTO-SHA256=8c05b674ea3734c10a7c7593c9b3d5497635c78504f81caa6d97fd1fbd250b00`、`AUTO-LF=215`（`.scratch/t292/skill-auto-before.bin`）。
- 编译／测试／对账／变异／提交一律经 `node tooling/run-locked.mjs --ticket 292 -- <命令>`，无裸跑；审计条目落 `.scratch/locks/gate-runs.log`（§七逐条抄 runId）。

## 二、结构纪律必报五步（`docs/agents/structure.md`）

### 第一步 · 影响清单

| 路径 | 归属 | 一句话理由 |
|---|---|---|
| `docs/skills/skill-calorie/06-目标管理-工作流程.md` | 技能文档（归属件自己的目录） | 28 条真源（25 场景＋3 自造），唯一手写表 |
| `packages/skill-calorie/SKILL.md` | 说明面（不在告警线扫描面） | 只改 AUTO 块之外：Wizard 表加目标管理 1 行 ＋ 新增「场景 06」常驻规则与指针 1 节 |
| `packages/skill-calorie/test/goal-h1-date-566.test.mjs` | 测试件（不在告警线扫描面） | Q4 判据洞：宽口径日期 ＋ 90d 三窗 |
| `docs/skills/skill-calorie/t292-收口-证据.md` | 文档 | 本件 |

禁区未碰：`HELP-AUTO` 块内一字节未动（§四）；#152 的账未动；`pnpm gen` 未跑；产品代码零改动（变异只在持锁窗内临时改一行，还原 SHA 一致，§六）。

### 第二步 · 结构设计

- 无新增目录、无新增源码文件；对外接口零变化（文档与测试 only）。
- 真源 1 处：28 行表住披露件；SKILL.md 只留常驻规则＋指针（同一含义只留一处）；对账脚本住 `.scratch/t292/align-28.mjs`（草稿，不进提交）。

### 第三步 · 写代码

- 不适用（无产品代码变更）。测试与文档见 §三–§五。

### 第四步 · 超线报警

- 本票未改 `src/**/*.ts` 与 `scripts/**/*.mjs`（变异还原后 `git diff` 无痕，§六），告警线台账无需同步，不触发第四步。
- 新建 3 件行数（LF 口径）：披露件 67、测试件 248；SKILL.md 543→553（＋10 行全在 AUTO 块外）。

### 第五步 · 交付对账

见 §八（偏差为零）。

## 三、28 条对账（`ALIGN-PASS: 28/28`）

- 脚本：`.scratch/t292/align-28.mjs`（25 条场景数据 `main_prompt.cli` ＋ 3 条别名 `WAKE_TABLE`，逐条核路由层首个可执行 `cli`、精确查首命中、子串搜含本表命令、真出口 50 条内含本表命令）。
- 持锁实跑 runId `68f13dfc-029b-4e70-acff-5dbae471b1d6`，`exit=0`：
  - `ALIGN-PASS: 28/28`；命令键 15 个（写 5：`goal.set`／`goal.water`／`goal.weight`／`goal.pause`／`goal.resume`；读 10：`view.goal`／`goal-progress`／`goal-weight`／`goal-vs-actual`／`goal-expiring`／`goal-predict`／`goal-config`／`goal-status`／`goal-recommend`／`goal-wizard`）。
  - `ALIGN-NOTE` 1 条（如实记录，非失败）：`看体重目标进度` 子串搜首命中是邻词 `calorie.view.goal-progress --params '{"window":"今日"}'`，取 `wake_word` 相等那条（本表第 12 行 `calorie.view.goal-weight --params '{"window":"7d"}'`）；精确查 28/28 首命中即本表命令，真出口 28/28 含本表命令。
- 探针结论（`.scratch/t292/probe-h1.mjs`）：`goal`／`vs-actual` 在 7d／30d／90d 六件 H1 均只留页面名，`meta-bar` 左行均带窗口区间；`goal-weight` 无 `meta-bar`（窗口住正文 `windowStrip`），故新测试三件取 `goal` 三窗（§五），`vs-90d` 另在探针里验过（同绿）。

## 四、SKILL.md（AUTO 块逐字节相同）

- 改动（AUTO 块之外两处）：① Wizard 映射表加 1 行（11 条带空位写词 → `calorie.view.goal-wizard`，页面装配 `src/goal/precheck.ts`，暂停／重启无空位不出页）；② 新增「场景 06 目标管理常驻规则与工作流指针」1 节（常驻规则 4 条 ＋ 指向真源的指针 1 行）。
- 自证：改后 `AUTO-SHA256=8c05b674ea3734c10a7c7593c9b3d5497635c78504f81caa6d97fd1fbd250b00`、`AUTO-LF=215`，与开工基线逐字节相同（`AUTO-DIFF-EMPTY=true`）。

## 五、新测试（`goal-h1-date-566.test.mjs`，4 条用例全绿）

- 三件：`calorie.view.goal` 在 7d（窗口 `2026-09-01 至 2026-09-07`）／30d（`2026-08-09 至 2026-09-07`）／90d（`2026-06-10 至 2026-09-07`）各一件；对照 `goal-expiring`（页头左行逐字 `看即将到期的目标 · 目标管理`）。
- 判据：① H1 与 `<title>` 三种写法（ISO／中文全写 `2026年9月1日`／中文短写 `9月1日`）都不含日期且 H1 含页面名；② 窗口区间住页头 `meta-bar` 左行 ＋ 可见文本里没丢；③ 完整文档四断言 ＋ `data.output` 绝对路径；④ 字串级变异（ISO／中文全写／中文短写加回 H1 各红 3、窗口抠出页头红 3、窗口全挖掉红 3，还原绿 3）。
- 持锁实跑 runId `03e33071-7762-4280-a534-08c798b8f0d1`，`exit=0`：`tests 4 / pass 4 / fail 0`（＋ `T292-MUT 字串级：ISO加回H1红=3、中文全写红=3、中文短写红=3、窗口抠出页头红=3、窗口全挖掉红=3；还原绿=3`）。

## 六、变异自证两行读数（真身：改源码 → 重建 → 跑 → 逐字还原 → 重建 → 跑）

- 变异点：`resultDocs.ts` 的 `title: '🎯 目标分析',` 改回改前形状 `title: '🎯 目标分析 ' + v.start + ' 至 ' + v.end,`（＝把窗口日期又写进大标题），脚本 `.scratch/t292/mutate-cycle.mjs`（还原写 `finally` 并回读比 SHA）。
- **红读数**：`node --test packages/skill-calorie/test/goal-h1-date-566.test.mjs` → `exit 1`；红行原文：
  > AssertionError [ERR_ASSERTION]: 「goal-7d」（变异①b） 的 H1 里还有 ISO 窗口日期（日期不该住标题）："🎯 目标分析 2026-09-01 至 2026-09-07 2026年9月1日 至 2026年9月7日"
- **绿读数**（还原后重建复跑）：`tests 4 / pass 4 / fail 0`，`exit 0`。
- 还原校验：`resultDocs.ts` 还原前后 SHA256 同为 `a54b9c0d4b3764b9ec6b4e5d1ca7cdc968bd576b070908d56eb83a0c8e9fd106`（与 #566 收工态同值）；`git diff --stat -- packages/skill-calorie/src/goal/resultDocs.ts` 无输出；两场编译都 `exit 0`。
- 持锁 runId `fc1ea66e-fe2e-4e6c-ba7d-a0603870597f`，`exit=0`（里面红 `exit 1` → 绿 `exit 0`，还原一致 `true`）。

## 七、持锁门禁（`ticket=292`）

| runId | 命令 | exit | 摘要行 |
|---|---|---|---|
| `af348f45-4d72-4537-aee0-b8095f197234` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie` | 0 | 编译干净（tsbuildinfo 判定后 exit 0；变异窗内两轮重建亦 exit 0，见 §六） |
| `68f13dfc-029b-4e70-acff-5dbae471b1d6` | `node .scratch/t292/align-28.mjs` | 0 | `ALIGN-PASS: 28/28`（＋1 条如实 NOTE） |
| `03e33071-7762-4280-a534-08c798b8f0d1` | `node --test packages/skill-calorie/test/goal-h1-date-566.test.mjs` | 0 | `tests 4 / pass 4 / fail 0` |
| `fc1ea66e-fe2e-4e6c-ba7d-a0603870597f` | `node .scratch/t292/mutate-cycle.mjs` | 0 | 里面红 `exit 1` → 绿 `tests 4 / pass 4 / fail 0`；还原 SHA 一致 |

tsc 说明：首轮 `tsc -b` 即 `exit 0`（本票无源码变更）；真编译证据取变异窗两轮重建（改后重建与还原后重建各 `exit 0`）。

## 八、交付对账（第五步）

| 第一步清单 | 事后对账 |
|---|---|
| `docs/skills/skill-calorie/06-目标管理-工作流程.md` | 已新建（LF 67：28 行表 ＋ 预检 11 条接线 ＋ 权威源） |
| `packages/skill-calorie/SKILL.md` | 已改（553 行；AUTO 块 SHA 与基线同值，§四） |
| `packages/skill-calorie/test/goal-h1-date-566.test.mjs` | 已新建（LF 248，4 条用例） |
| `docs/skills/skill-calorie/t292-收口-证据.md` | 已新建（本件） |

偏差为零：提交只含上面 4 件（`git add` 逐件声明，`git diff --cached --name-only` 复核）；产品代码零改动；`src/goal/` 之外一行未碰；既有测试一件未改。

## 十、提交

- 提交 1 `0f70c93`（真源＋SKILL＋测试，3 件，325 insertions）：`git commit -m "文档(292)：目标管理28条工作流程真源＋SKILL收口＋H1日期宽口径测试" -- <上面三件>`，runId `0601c425-1c6f-4022-bc92-e18479e74ae5`。
- 提交 2（本件）：见回执 commit sha 清单。

## 九、未做项与下一手

- 未跑全仓测试（纪律禁止 `pnpm test`）；只跑靶向新用例（§五）＋ 对账脚本（§三）＋ tsc（§七）。邻接回归（566／254／251）未重跑——本票产品代码零改动，面已够；如下轮改动 `resultDocs.ts` 再补跑。
- 未重生成验收墙（`scene06-验收墙/` 那 3 件仍是改前形状，#566 复核 §六第 2 条已登记，属编排者裁定事项，本票不碰 `docs/` 之外）。
- `看体重目标进度` 子串搜首命中邻词（§三 NOTE）：本票 disclosure 已写明取词规则；检索排序本身的改动留给路由票，本票不改产品代码。
- 提交与推送见回执（rejected 即停手上报，不 pull 不 rebase）。

## 十一、补一致追加段（#252 落地后，2026-09-16，补一致实施席）

> 只改披露件 `docs/skills/skill-calorie/06-目标管理-工作流程.md`（2 行行为补记，28 行命令字串一字未动）；产品代码零改动；SKILL.md 一字节未碰。
> 补差来源：只读比对当刻实现（`src/goal/read.ts:128-135` 两守卫系 #252 `a8ed49c` 新增，原披露件缺文）——调查席若另有清单，以本段机器读数为准，行为描述逐字同源码。

- 改动：① 口径段追加预检两守卫一句（改类 19／20／21 空库 `missing-data`/exit 4；暂停／重启 22／23 带 `wake` 调页 `bad-input`/exit 2；定类空库 exit 0 照常出页）；② 预检接线末段展开同一组守卫（含两处原文案与实现行号），并声明 28 条命令字串不变。
- 提交 3 `1b453da`（披露件补守卫，1 件，2 insertions）：runId `a18a0cc2-d4fd-417c-bff5-b727fd70df86`（`git add` 逐件声明；提交时暂存区另有他席已暂存的 `t272/t275` 两件，本提交用 `-- <本件>` 限定，只含自己的件）。
- 持锁门禁（`ticket=292`，重建一律 `--force`）：

| runId | 命令 | exit | 摘要行 |
|---|---|---|---|
| `daffe300-a189-446f-a66a-dafcbd0cc916` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` | 0 | 重建干净；dist 指纹 `read.js 4b9262c14b79f04b`／`resultDocs.js d4f93c9b115730e1`／`scene-06-goal.js b17aaf2c879ab13a` |
| `3e272113-4950-4d29-9196-aaf3deccb71b` | `node .scratch/t292/align-28.mjs` | 0 | `ALIGN-PASS: 28/28`（NOTE 1 条同收口态：`看体重目标进度` 子串搜首命中邻词，取 `wake_word` 相等条） |
| `1c234766-233e-48fd-a443-25a1c44a1c99` | `node .scratch/t292/compensate-mdcheck.mjs` | 0 | `MD-ALIGN-PASS: 28/28`（披露件 28 行命令 vs `scene-06-goal.ts`＋`WAKE_TABLE` 逐字一致） |
| `f26b39a4-9099-40f4-8efc-cff986d0fb4d` | `node --test packages/skill-calorie/test/goal-h1-date-566.test.mjs` | 0 | `tests 4 / pass 4 / fail 0`（三窗 7d／30d／90d） |
| `993d79d9-1ff1-43ae-8d6a-db2402e7b757` | `node --test packages/skill-calorie/test/t252-goal-wizard.test.mjs` | 0 | `tests 6 / pass 6 / fail 0`（两守卫活断言） |
| `c01f3449-f14f-4518-9ec1-86f55bfb5441` | `node .scratch/t292/compensate-mutate.mjs` | 0 | 变异自证 PASS（内含红 exit 1 → 还原 SHA 一致 → 绿 exit 0；两场 tsc 均 exit 0） |

- SKILL 标记块空 diff：改前后 `AUTO-SHA256=509576b7949ed8ecb2f5c8f06d07394cc72c6f1a2c2058772c301598b4d03fdc`、`AUTO-LF=215`、`AUTO-DIFF-EMPTY=True`（基线存 `.scratch/t292/skill-auto-before-292fix.bin`；注：与收口态 `8c05b674…` 不同系他票再生 HELP-AUTO 块所致，行数同为 215，本窗内零改动）。
- 变异两行读数（真身 `src/goal/resultDocs.ts` H1 加回窗口日期 → 还原）：
  - 红：`MUT-TEST-RED-EXIT=1`（`✖ #292 ① 三窗 H1 不含日期…` 变红）。
  - 绿：`MUT-TEST-GREEN-EXIT=0`（`tests 4 / pass 4 / fail 0`），`MUT-RESTORED=true SHA=c1c4ad295d115439`，`git diff --name-only -- packages/skill-calorie/src packages/skill-calorie/SKILL.md` 无输出。
- 未做：全仓测试（纪律禁止）；`t252` 之外邻接回归未重跑（产品代码零改动）；验收墙重生成未做（编排者裁定事项）；推送未做（总工程师统一收口）。
