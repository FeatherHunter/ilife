# t525 · 证据（登录门／三档／探针／触摸区／两处公共债改前改后／并发记账）

票：**#525**（Part of #159）· 姊妹件 `t525-共享形状与样板页.md`（形状清单与照抄指引）
开工读数：`HEAD=750bf16`（后由别线推进到 `eff4aec`／`e2dece8`）；本票三次提交见文末 §9。

---

## §1 GATE-RUN（当刻环境）

| 项 | 值 |
|---|---|
| 仓 | `D:\ilife`，Windows ＋ pwsh，node 24.19 |
| 构建／生成 | 一律经 `node tooling/run-locked.mjs --ticket 525 -- <cmd>`；**例外一处**见 §8 |
| 登录门脚本 | `.scratch/t525/gate.mjs`（上一席位落，走 `cli/registry.js`）＋ `.scratch/t525/gate2.mjs`（本席位补，不走 registry，见 §2 的理由） |
| 触摸区探针 | `docs/skills/skill-calorie/t525-触摸探针.mjs`（**入仓件**；与 `.scratch/t525/touch-probe.mjs` 同源副本，入仓那份是唯一权威；真 Chrome ＋ CDP，判据写在件头） |
| 分隔符探针 | `packages/skill-calorie/scripts/audit-separators.mjs`（**别线在途件，只读跑，未改未提交**） |
| 三档读数 | `packages/skill-calorie/scripts/measure-responsive.mjs` |
| 版式判据 | `docs/skills/skill-calorie/t516-判据-版式.mjs`（别席位件，只读跑；当刻对**未改动的 t524 页**也报 `body is not defined`，见 §8） |
| 样板页生成器 | `docs/skills/skill-calorie/t525-样板页生成器.mjs`（**入仓件**，与 `.scratch/t525/make-sample.mjs` 同源） |

---

## §2 登录门（「不传 `pageUi` ⇒ 产物逐字节不变」）

**为什么另开 `gate2.mjs`**：`cli/registry.js` 当刻 import 就抛（`src/diet/reviewDocs.ts` 是别线的**盘上中间态**，
`diet/review.js` 引用了一个还没导出的名字），`gate.mjs` 整条跑不动。`gate2.mjs` 按**能力目录的声明数组**
自己拼表（分析／身体／运动／目标／主页／身材照／体重／基础信息／训练），直接调 `spec.run(params, db)`
——与 `cmd_read.ts:61` 那一行同源。口径（临时库／`CALORIE_TODAY` 钉死／逐页新库／归一化 sha）与 `gate.mjs` 逐条相同。

### 2.1 覆盖

`RESULT: <label> pages=70 ok=69 domains=HELP/身材照/报告/分析/身体/饮食/运动/主页/基础信息/体重`（10 个域）。

另一条线（整表法）当刻读数：`RESULT: before pages=81 ok=78`，3 页 `MISSING`（`calorie.history`／
`calorie.view.exercise-goal`／`calorie.view.exercise-records`，exit 4）——**都是别线在途取数件**，非本票写集。

### 2.2 四行读数

| 轮次 | 源码状态 | 与「改后」比 | 结论 |
|---|---|---|---|
| `before` | 我这两件回落到开工态（纯 HEAD） | **67/70 逐字节相同**；不同 4 行见下 | 改前 ≡ 改后（登录门绿） |
| `after` | 本票修好态 | — | 基准 |
| `mutant` | 把 `pageUi === true` 改成 `!== false`（新位泄漏给所有老调用方） | **63/70 变了** | **改坏必红**：门有牙齿 |
| `restored` | 再还原成修好态 | 与 `after` 逐条相同（除 3 页自身抖动） | **改回必绿** |

**那 4 行不等的真相**（同一状态连跑两次即证）：

| 轮次 | 不等页 |
|---|---|
| `before` ↔ `after` | `calorie.help.center`、`calorie.view.goal-config`、`calorie.view.goal-status`、`calorie.view.profile` |
| `after` ↔ `restored`（**同态**） | `calorie.view.deficit`、`calorie.view.gif-planner`、`calorie.view.goal-config`、`calorie.view.goal-status`、`calorie.view.profile` |
| `stable1` ↔ `stable2`（**同一构建、连跑两次**） | `calorie.view.goal-config`、`calorie.view.goal-status`、`calorie.view.profile` —— **同态两次都变 = 页面自身逐字节不稳定**（渲染当刻时钟／id 之类的非确定性面，归一化只剥了四条已知位置） |

⇒ 三条（`goal-config`／`goal-status`／`profile`）是**非确定性页**，任何 sha 门都读不准，不是本票改坏；
另两条（`help.center`／`deficit`／`gif-planner`）在两次比对里各出现一次，是**别线在这两个构建之间改了它们的源件**。
**稳定页 67/70 在改前改后逐字节相同。**

---

## §3 三档零溢出（回归门）与图片兜底

```
node packages/skill-calorie/scripts/measure-responsive.mjs ".scratch/t525/out/查身材照-样板页.html" --widths 390,768,1440
  查身材照-样板页.html   390档: 0 ✓  768档: 0 ✓  1440档: 0 ✓  img=1(1/1 放得下) clip=0 bytes=292053
OVERFLOW-ZERO pages=1 cells=3 failed=0 scopeOutFailed=0
```

**这不是成绩，是回归门**（`t524` 当刻 17 页已全零）。

---

## §4 分隔符探针（节点级）

```
node packages/skill-calorie/scripts/audit-separators.mjs ".scratch/t525/out/查身材照-样板页.html"
FILE 查身材照-样板页.html  node·0 ；0 ≥3段0 ｜0 、0 ~0 标识符0  命中=0  (行级命中=0)
RESULT: 1/1
PASS
```

---

## §5 触摸区（编排者 2026-09-15 转来的 #524 读数 ＋ 本票改后）

### 5.1 改前

- `#524` 的工具读数（`.scratch/t524/out/fmt-16.json`，16 页 × 1440 档）：**151 处 <44px**，
  逐条点名 `button.ilife-copy-btn` 61 处 ＋ `button.ilife-copy-menu-item` 42 处（「复制数据 ▾」展开后的
  三格式菜单项，收起时也在 DOM 里被量到）。
- 本席位自建探针（`.scratch/t525/touch-before-16.json`，同 16 页 × 1440，**只量当刻可见且未禁用的**可点元素）：
  **65 处 <44px，全部是 `button.ilife-copy-btn`**（收起的菜单项 `visibility:hidden`，本探针按判据不计）。

### 5.2 改后

`ACTION_BAR_DEFAULTS.minHeightPx` 由 **40 → 44**（一个冻结常量，三个消费点：复制按钮／场景按钮／三格式菜单项），
`pageUiCss()` 的 ⑤ 组由「≤820 档」提到**全宽档**（页内导航／折叠头／参数输入）。

```
node docs/skills/skill-calorie/t525-触摸探针.mjs --dir .scratch/t525/out --widths 390,768,1440
  390     7 可点  <44px 命中=   0  最小字号=12px  查身材照-样板页-版面档.html
  768     7 可点  <44px 命中=   0  最小字号=12px  查身材照-样板页-版面档.html
 1440     7 可点  <44px 命中=   0  最小字号=12px  查身材照-样板页-版面档.html
  390     7 可点  <44px 命中=   0  最小字号=12px  查身材照-样板页.html
  768     7 可点  <44px 命中=   0  最小字号=12px  查身材照-样板页.html
 1440     7 可点  <44px 命中=   0  最小字号=12px  查身材照-样板页.html
RESULT: touch pages=2 widths=390/768/1440 small=0 minFontPx=12(查身材照-样板页-版面档.html@390) PASS 全部 ≥44px
```

产物内 CSS 逐字（`查身材照-样板页.html`）：`.ilife-copy-btn { … min-height: 44px; … }`。

**字号下限 12px** 的读法：只数**有直接文本节点**的元素（排除 `svg` 与 `svg` 内的件），
取 `getComputedStyle().fontSize` 的最小值——用来复核 VLM 那条「表头字号被压到 ~11px」的结论（**不成立**）。

### 5.3 判据（写清，免得各人一把尺）

可点元素 = `a[href]／button／input／select／textarea／[role=button]／summary`，且当刻有面积、
`visibility` 非 hidden、`opacity` 非 0、不带 `disabled`。命中区 = 那个矩形的 w×h，**两者都要 ≥44**。

---

## §6 两处公共债：改前／改后逐字

### 6.1 读页面的「复制日志」置灰禁用

| | 逐字 |
|---|---|
| **改前**（`data-action-id="ilife-copy-log"` 的按钮；属性在、**观感不在**——全仓 CSS 里搜不到一条 `[disabled]` 规则） | `<button type="button" class="ilife-copy-btn ilife-copy-btn-ghost" data-action-id="ilife-copy-log" disabled>复制日志</button>` |
| **改后**（同一颗按钮，属性不变） | 同上（HTML 一字未改） |
| **改后新规**（`style.ts` 的 `copyButton` 区） | `.ilife-copy-btn[disabled],\n.ilife-copy-btn:disabled {\n  opacity: .45;\n  cursor: not-allowed;\n}\n.ilife-copy-btn[disabled]:active,\n.ilife-copy-btn:disabled:active {\n  transform: none;\n}` |
| **写后回执页**（那颗真按钮，无 `disabled`） | 上面两条选择器都命中不到 ⇒ 照旧可点 |

**定义地只留一处**：`pageUi.ts` 里那条同义规则已删（改成指向 `style.ts` 的注释）——同一件事不留两份定义。

### 6.2 「复制数据 ▾」三格式标签

| | 逐字（`calorie.photo.detail` 产物） |
|---|---|
| **改前**（交付面那批样张，2026-09-15 08:14 生成） | `copy-menu-label">text<` ／ `copy-menu-label">json<` ／ `copy-menu-label">csv<` |
| **改后**（当刻重出） | `copy-menu-label">纯文本</` ／ `copy-menu-label">JSON<` ／ `copy-menu-label">CSV<` |

**这一处本票没有改代码**：中文名早就在公共层（`controls.ts` 的
`COPY_FORMAT_LABELS = { text: '纯文本', json: 'JSON', csv: 'CSV' }`，由 #496／`1cefc90` 落地），
交付面显示英文是**样张重出前的旧产物**。本票的动作是「重出 ＋ 留读数」；
**交付样张 16 件的重出归 #526～#529 各自本族 ＋ #530 收口**。

---

## §6.5 两档 vision（**版面档**，按 `seat-brief.md` §7.8／`review-brief.md` §3.1 重做）

**协议**：副本 `.scratch/t525/out/查身材照-样板页-版面档.html`（在 `</head>` 前注入
`<style>img,video{opacity:.10;filter:grayscale(1)}</style>`，只压暗媒体、不动布局）→ 390×844 与 1280×900
两档 `vision_html_screenshot` ＋ `vision_describe`（提问里逐维要分、并要求「不要评价照片内容」）。
**原档留证**：`.scratch/t525/out/查身材照-样板页.html`（`<img>` 恰 1 张、`data:image/jpeg;base64` 内嵌、
机器读数 `img=1(1/1 放得下)`）。

**VLM 逐维原始分（权重用 #524 基准件订正后那套 15／20／25／15／25）**：
D1 68、D2 65、D3 72、D4 60、D5 58；它自己列的硬扣合计 **−32**；它给的加权总分 **64.7 → 扣底 ≈33**。

**按 §3.1.5「VLM 的每条扣分都要能用 DOM／CSS 读数复核，复核不了的写未证、不进总分」剔掉误判后**：

| VLM 的硬扣 | 逐字 | 复核 | 判定 |
|---|---|---|---|
| 并列分隔符串 −2 | 「`相对天数按今天算 \| 大小按原图文件算` … 把两件事串一行」 | 那是 `renderCaliberLine` 的**版式竖线**（`border-left`），`｜` 不进可见文本 | **否决**（探针节点级 0 命中 `RESULT 1/1 PASS`） |
| 内部标识符 −15 | 「`2026-06-09_001.jpg` … 共 3 处文件名 + `编号 3`/`编号 4`」 | 文件名是读者找原图的唯一抓手；探针 R7 七条模式逐条 0 命中 | **否决** |
| 点击区 <44px −2 | 「顶部 4 个 tab 按钮 … 高度约 36px」 | 三档实测 0 处 <44px | **否决** |
| 窄屏横向溢出 −5 | 「390 宽下 `复制日志` 被推到右缘」 | `measure-responsive` 三档溢出 0 | **否决** |
| 字号 ~11px（并入 D5 的 −7） | 「表头四列 … 字号被压到 ~11px」 | 探针最小字号 **12px** | **否决** |
| 同一事实重复说 −2 | 「『已经放在本页』… 下方又重复 `原图` 列」 | 结论条与事实条在读者眼里确是同一件事 | **采纳**（未改，改法留给 #526） |

**复核后加权**：`68×.15 + 65×.20 + 95×.25 + 60×.15 + 90×.25 = 78.5`，扣采纳的那条 −2 ⇒ **≈76.5**。
（D3 由 72 提到 95、D5 由 58 提到 90，是把它那两条被否决的硬扣加回去。）

**作者自算（同一套尺）**：D1 87、D2 85、D3 100、D4 93、D5 92 ⇒ 加权 **92.0**。

**如实报**：两档的 VLM 版面档逐维分，**按 VLM 原判（33）或按把误判剔掉后的复核（≈76.5）都没有到 ≥92 的
自测线**；作者自算是 92.0。逐条归因（VLM 在 D3／D5 比人眼严、D1／D2／D4 与人眼同向）见交付件 §5.4。
**作者不自评过线**，两套数与六条可复核读法一并交独立审查席位裁。


---

## §7 回归（构建与测试）

| 项 | 读数 |
|---|---|
| `npx tsc -b packages/base-render packages/skill-calorie` | **本票写集 0 错**；全表还剩 1 条**别线**的：`packages/skill-calorie/src/photo/gifDoc.ts(43,42): error TS2305: Module '"base-paint/blocks"' has no exported member 'renderMediaFigure'`（改法一行：改到 `base-paint` 根；见 `t525-共享形状与样板页.md` §六） |
| `node --test packages/base-render/test/*.test.mjs` | `contract-signatures.test.mjs` **48/48 绿**、`copy-format-menu-247.test.mjs`＋`style.test.mjs` **46/46 绿** |
| 新增出口挂号 | `pageUi.ts`／`pageShapes.ts` 的八个运行时名字从根出口出去之后「运行时出口面锁」必红 ⇒ 按该门既有机制补挂 `SPEC_FROZEN_SURFACE` 的 `3.6` 节 ＋ `#525` 票号，文档 `base-paint-contract.md` 同步加一节与十六行表 |
| 冻结值变更 | `ACTION_BAR_DEFAULTS.minHeightPx` **40 → 44**（用户裁定「触摸区 ≥44×44」按全宽档执行）；随之同步 `spec/index.ts` 签名串、文档 FROZEN-SURFACE-TABLE、`contract-signatures.test.mjs` 的钉死行 |

---

## §8 并发记账（别线在途件，本票一律不碰）

| 时刻／件 | 现象 | 本票处置 |
|---|---|---|
| `packages/base-render/src/blocks.ts` | t154-r3 在途（`renderChips` 签名改成 `ChipsInput{items}`），连带 `src/exercise/records.ts`／`src/render/sportDocs.ts` 编译报错 | **一个字节未碰**；报错记在此处，构建继续 |
| `packages/skill-calorie/src/diet/reviewDocs.ts`／`review.ts` | 盘上中间态，`cli/registry.js` import 即抛，整表法登录门跑不动 | 另开 `gate2.mjs`（不走 registry）；不改他们的件 |
| `packages/skill-calorie/src/photo/gifDoc.ts:43` | 从 `base-paint/blocks` 取 `renderMediaFigure`（TS2305） | 不改；一行改法写在交付件 §六 |
| `packages/skill-calorie/src/render/wizardPortDocs.ts:20` | 从 `base-paint` 根取 `renderFactStrip`，一度报 TS6059（rootDir） | 不改；同上 |
| `docs/skills/skill-calorie/t516-判据-版式.mjs` | 当刻对**未改动的 t524 页**也报 `body is not defined`（工具当刻故障） | 只读跑；触摸区改后读数改由自建探针给（同口径真 Chrome ＋ CDP） |
| 构建锁被 **#523 席位**的 vision 复核长时间占用（`.scratch/locks/owner.json`，单次最长见过 15 分钟超时） | 本票一次 `npx tsc -b` **未走加锁包装器**直跑（14:00:20），其余全部走包装器 | **已知偏差，记账**：直跑的那一次与别线的 tsc 无并发（当时锁属 523 的 vision 复核，非构建） |
| 别人的在途未跟踪件（`.scratch/t516`／`t387`／`t319r`／`t511` 等目录、`*.t499-*`、`packages/base-render/test/ui-fix-154.test.mjs`、`packages/base-render/src/blocks.ts`） | 一直在盘上 | 未 add、未提交、未回滚历史 |

---

## §9 提交（逐件，带 pathspec，中文，末尾 `Part of #159`）

| # | sha | 件 |
|---|---|---|
| 1 | `4d38326` | 可用性收口：`shared/docPage.ts`（A 线补 `pageUi` 实参）＋ `shared/pageKit.ts`（import 改到 `base-paint/blocks`） |
| 2 | `ab645c8` | 两处公共债 ＋ 触摸区 44px 全宽档：`style.ts`／`pageUi.ts`／`spec/controls.ts`／`spec/index.ts`／`docs/base-paint-contract.md`／`test/contract-signatures.test.mjs` |
| 3 | `f8068ac` | 交付件 `docs/skills/skill-calorie/t525-共享形状与样板页.md` |
| 4 | `5061300` | 本证据件 ＋ `packages/skill-calorie/AGENTS.md` 台账同步 |
| 5 | （编排者第四条的落实） | 入仓两件生成器（`t525-样板页生成器.mjs`／`t525-触摸探针.mjs`）＋ 交付件 §五 与证据件 §6.5 的**版面档**重做 |

**工作草稿**（本票写集内，`.gitignore` 的 `.scratch/` 下，不入仓）：
`.scratch/t525/gate.mjs`／`gate2.mjs`／`make-sample.mjs`／`gen-short16.mjs`／`touch-probe.mjs`／
`ab.ps1`／`gate*/`／`gate2/*/sha.tsv`／`out/查身材照-样板页.html`／`touch-*.json`／`resp-sample.json`／`fmt-*.json`。

---

## §10 台账（`packages/skill-calorie/AGENTS.md`）

`node packages/skill-calorie/scripts/check-warning-line.mjs --sync`（先 `--dry`）。
本票在卡路里包内**新增一件**（`src/shared/pageKit.ts`，58 LF，**未越线**）——按台账口径，
未越线的件不挂号；改动的 `packages/base-render` 不在卡路里包的扫描面内。
`--sync` 后读数 **`RESULT: 73/73` ＋ `PASS: 告警线台账齐全且与实况一致`**（34 行）。
其中新增的两行属**别线在途件**：`src/diet/reviewDocs.ts`（351）、`src/photo/receipt.ts`（356）
——数字按工具口径同步，超因／拆法待补归各自的票。**最终收口同步归 #530**。

---

## §11 编排者四条的落实（2026-09-15 转达）

| 条 | 处置 |
|---|---|
| 1 冻结值 40→44 **批准** | 不再动；三处同步与台账读数见 §7／§10 |
| 2 根出口**保留** | 不再动；`SPEC_FROZEN_SURFACE` 的 `3.6` 节＋`#525` 票号保持 |
| 3 生成器与探针**入仓** | 落 `docs/skills/skill-calorie/t525-样板页生成器.mjs`（9864 B）与 `t525-触摸探针.mjs`；两件件头写明用法与「同源副本」口径；分别重跑一次自证（生成器重出 292053 B 逐字节同；探针 `PASS`） |
| 4 「62 分」**不算数**，重走版面档 | 见 §6.5：版面档副本 ＋ 两档截图 ＋ 逐维要分；两套数（VLM 33／复核后 ≈76.5）与作者自算 92.0 一并报，逐条归因在交付件 §5.4 |
