# #372 核验结论：其余 4 词逐条核验（不动别家装配）

- 基线：`1be7858`（开工 `git pull` 已是最新；锁空已确认后开工）。
- 前置：#173 对账收口（9 条全老做过新仓已接；字段差异 4 点归 #371×3＋#372×1，证据 `docs/skills/skill-calorie/t173-对账.md`）、#370 已关（`src/home/homeDocs.ts` 就绪）。
- 写集（文件级）：`.scratch/t372/**`（核验矩阵＋日志）＋本件。其余一切（`src/home/`、`src/weight/`、`src/render/`、路由与命令声明、共用位）一行未碰。
- 本票零源码改动：票面禁改四条装配，`streak_longest` 缺位归 #371，体重三字段经下表已覆盖无需补，故无字段补齐改动，只有核验与转票。

## 一、核验矩阵与判定

矩阵：`.scratch/t372/verify-4.mjs`（照抄 `docs/research/t81-exec-smoke.mjs` 的种子用法再改：固定种子库 `docs/research/t81-seed.mjs::seedFull`，`CALORIE_TODAY=2026-09-07`，每条全新库，真 CLI 落盘，断言落盘／绝对路径／可打开／字节如实）。

票面 4 条验收命令逐字照抄（`view.weight` 无参数）：

| # | 唤醒词 | 命令 | A 档种子库 | B 档空库 |
|---|---|---|---|---|
| 1 | 看今日饮食概览 | `calorie.view.diet --params '{"window":"今日"}'` | PASS：完整文档落盘 | PASS：exit 4 可读缺失阻断、不落盘 |
| 2 | 看今日运动概览 | `calorie.view.exercise --params '{"window":"今日"}'` | PASS：完整文档落盘 | PASS：同上 |
| 3 | 看今日目标进度 | `calorie.view.goal-progress --params '{"window":"今日"}'` | PASS：片段落盘（见注） | PASS：同上 |
| 4 | 看今日体重概览（跨家，只核验） | `calorie.view.weight` | PASS：完整文档落盘 | PASS：同上 |

注：目标页仍是 `<section>` 片段（无 doctype），与 #173 记录一致，整页化归场景 06，本票只判跑得通，不判红。

- 判定口径：地图 #162 裁定第 4 条（有当天段出结果页；无段给可读缺失阻断不落盘也算跑得通；真坏仅报错／白页／空页／非绝对路径／打不开）。
- 结构化断言：3 张完整文档走 `test/doc-page-assert.mjs` 五连（doctype＋charset＋样式段＋脚本段＋版面＋无残留标记）；4 条走 139 形状（stdout 单行 JSON、`shape:'stat'`、envelope key 一致、`data.output` 绝对路径、落 `calorie_html/`、存在、`delivery.bytes`＝落盘字节）；目标片段走片段断言（以 `<section` 起、不已是完整文档、含 `ilife:calorie:goal-progress` 身份位）。
- 机器读数（绿）：`RESULT: 8/8 PASS`（`.scratch/t372/logs/run2.log`）。

## 二、字段位置对照（老 `data_fields` → 页上位置）

| 词 | 老字段 | 页上位置 | 结论 |
|---|---|---|---|
| 饮食 | `diet_calories` | 累计／日均 KPI＋按日汇总摄入列 | 有位 |
| 饮食 | `diet_protein` | 按日汇总蛋白列＋窗口明细蛋白列 | 有位 |
| 饮食 | `diet_goal` | 目标 KPI＋按日汇总目标列 | 有位 |
| 饮食 | `diet_vs_target` | 趋势 KPI＋目标列同表对照 | 有位 |
| 运动 | `exercise_burn` | 总消耗／日均／每日消耗／类型消耗 | 有位 |
| 运动 | `exercise_duration` | 总时长＋按类型明细时长列 | 有位 |
| 运动 | `exercise_goal` | 页上无位 | GAP-1，转票 |
| 运动 | `exercise_vs_target` | 页上无位（同页无目标即无对比；能力在 `calorie.view.exercise-goal` 另一条命令） | GAP-1，转票 |
| 目标 | `goal_calorie_pct` | 完成 x/y＋目标完成率（口径注：历史完成带 80%–120%，非当日热量百分比） | 有位（口径已注） |
| 目标 | `goal_protein_pct`／`goal_water_pct`／`goal_exercise_pct` | 页上无分项位（仅单完成度＋热量目标） | GAP-2，随场景 06 整页化补，不另开票 |
| 体重 | `latest_weight` | 体重盘 `lastWeight`＋体重记录表 | 有位（#173 差异点归本票，已覆盖） |
| 体重 | `weight_goal_gap` | 距目标 `gapKg` | 有位 |
| 体重 | `weight_delta_7d` | 变化 `changeKg`（本词无参数走默认 7 天窗，同口径） | 有位 |

跨家结论：`calorie.view.weight`（命令住 `src/weight/`）跑得通，三字段全有位，weight 侧无需转票。

## 三、变异自证两行读数

- 变异红：`T372_BREAK=1`（饮食针加一处必 miss 的位）→ `RESULT: 7/8 FAIL`（`.scratch/t372/logs/mutate-red.log`，`FAIL A档 看今日饮食概览 … 页上无位：__MUTATED_MISSING__`）。
- 还原绿：去掉该变量重跑 → `RESULT: 8/8 PASS`（`.scratch/t372/logs/run2.log`）。

## 四、`gen --check` 红的说明与本票零新增举证

- `pnpm gen:check` 在本窗口内红：`GEN-STALE FAIL`（`.scratch/t372/logs/gen-check2.log`）——树上有他人的在途改动（`src/analysis/`、`src/home/homeDocs.ts`、`src/home/today.ts`、`src/weight/`、`src/cli/keys.ts` 等 13 处已改＋5 处新增，见收工时 `git status`），印记对不上。红与本票无关。
- 零新增举证：① 本票全部持锁运行只有 `node .scratch/t372/verify-4.mjs` 与 `pnpm gen:check`（见 §六运行记录），无一次写 `packages/` 或 `docs/` 的命令；② 本票写集（`.scratch/t372/**`＋本件）之外的一切改动均非本票所为，本票未改任何 `commands.ts`／`routes.ts`，命令与路由零新增；③ 待树绿后重跑 `gen --check` 即应绿（判据：生成物＝生成器输出，无本票增量）。

## 五、他人致红事件（已按协议停手，待树绿重跑）

- 他人（334／336 窗口）在本票绿跑之后重建了 `dist/`：`src/weight/history.ts` 引 `weightVolatilityV2` 而 `src/weight/volatility.ts` 未导出它，致全部 CLI 启动即崩（`.scratch/t372/logs/run3-postbuild.log` 原文：`SyntaxError: The requested module './volatility.js' does not provide an export named 'weightVolatilityV2'`）。
- 按并发协议 §2.1.5 停手：不修他人文件、不重建、保留现场日志；本节绿读数以致红前的 `run2.log` 为准，待树绿后重跑矩阵。

## 六、持锁运行记录（对账源，`GATE-RUN` 声明边跑边补）

- GATE-RUN runId=c7a7993b-ddc6-4ee7-af1d-6a13a6f49871 cmd="node .scratch/t372/verify-4.mjs"（首绿前针误版，7/8，exit=1）
- GATE-RUN runId=74ec37c4-5ab5-4a82-971d-283a05fbd683 cmd="node .scratch/t372/verify-4.mjs"（绿，8/8，exit=0）
- GATE-RUN runId=46c6a7d8-528f-4eed-8d67-35c9392e7b08 cmd="node .scratch/t372/verify-4.mjs"（变异红，7/8，exit=1）
- GATE-RUN runId=6293fb35-87ab-46b9-b7bd-bb702efad7ae cmd="pnpm gen:check"（GEN-STALE 红，exit=1，非本票所致）
- GATE-RUN runId=9d314f6b-f04c-4e27-8b21-3314798d674a cmd="node .scratch/t372/verify-4.mjs"（他人改坏 dist 后重跑，启动崩，exit=1，见 §五）

## 七、转票清单

- 新开 1 票（GAP-1）：运动概览页缺目标对比位（`exercise_goal`／`exercise_vs_target` 页上无位）。`Part of #162`，修面涉及共用装配 `src/render/sportDocs.ts`（场景 04 共用），需与场景 04（#156）协同；路由与命令声明不许改。
- GAP-2 不另开票：目标页分项位缺失随场景 06 整页化补（地图 #162 Out of scope 已定归属），属已承诺目标，无超出范围的新缺陷。
- weight 侧：跑得通且三字段有位，不转票。

## 八、产物路径

- `.scratch/t372/verify-4.mjs`（核验矩阵）
- `.scratch/t372/logs/run2.log`（绿 8/8）、`.scratch/t372/logs/mutate-red.log`（变异红 7/8）、`.scratch/t372/logs/gen-check2.log`（`gen --check` 红原文）、`.scratch/t372/logs/run3-postbuild.log`（他人致红现场）
- 本件 `docs/skills/skill-calorie/t372-核验.md`
