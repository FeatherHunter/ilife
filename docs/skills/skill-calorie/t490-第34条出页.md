# t490 · 第 34 条「看波动异常点」出页分流（路由补 `view=anomalies-only`）

来源：票 [#490](https://github.com/FeatherHunter/ilife/issues/490)（map #637）。本件是交付证据，落点照 `docs/agents/doc-homes.md`。

## 一 · 缺陷与根因（实施前后各一次实测）

| 读法 | 路由 cli | exit | 字节 | 正文 sha16 | 图区块 | 「只看异常点」提示 |
|---|---|---|---|---|---|---|
| 看体重稳不稳（增强版） | `… --params '{"window":"7d"}'` | 0 | 88472 | `811573feaba6b286` | 2 | 否 |
| 看波动异常点（**改前**） | `… --params '{"window":"7d"}'`（逐字同上一行） | 0 | 88472 | `6853d76ee585b21b`（与上行逐字节相同） | 2 | 否 |
| 看波动异常点（**改后**） | `… --params '{"window":"7d","view":"anomalies-only"}'` | 0 | 75818 | `abde61610f3b81fe` | 0 | **是** |

- 探针：`.scratch/t490/probe.mjs`（按 `dist/triggers/routes.generated.js` 的 `ALL_ROUTES` 取两条词的 cli，用 `docs/research/t81-seed.mjs` 的 `createHarness()` 种子库真跑，产物落 tmp）。
- 改前「两条词出页逐字节相同」＝**同一个 sha**（88472 B 两份），改后分流（75818 B／无图区块／页顶提示在位）。
- 取数层早就支持两读法：`src/weight/volatility.ts:171-179` 的 `parseVolatilityView`（`full`／`anomalies-only`，缺省 `full`）与 `volatilityDoc.ts` 的 `only` 分支——缺的只是**路由没传 `view`**。

## 二 · 改动面（四处，全在声明与生成物，取数与文案一行未动）

1. `src/weight/routes.ts` order 110（看波动异常点）：cli 补 `"view":"anomalies-only"`（附注释记 sha 读数）。
2. `src/triggers/scene-03-weight.ts` 的 `w_vol_anomalies`：`main_prompt.cli` 与 `data_source` 同补（冻结表是 HELP／58 条真出口锁的权威源）。
3. `src/triggers/routes.generated.ts`：`pnpm gen` 重出（`gen:check` 逐件绿）。
4. `SKILL.md` 场景 03 第 5 组那一段：把「走同一条命令」改成两条分流各一行（说明面同步实况；不在 HELP-AUTO 生成块内）。

`src/weight/volatility.ts`（取数与文案）、`commands.ts`（命令自身 example）、`build-help.mjs`（生成块）、`wake-assets.ts`（无 cli 字段）**均未动**。

## 三 · 机器读数

```
node tooling/run-locked.mjs --ticket 490 -- node node_modules/typescript/bin/tsc -b packages/skill-calorie   → exit 0
node tooling/run-locked.mjs --ticket 490 -- node packages/skill-calorie/scripts/gen-cli.mjs --check         → GEN-CHECK PASS（129 键 / 10 能力 / 未搬迁 0）
node tooling/run-locked.mjs --ticket 490 -- node --test packages/skill-calorie/test/weight-volatility-336.test.mjs  → tests 11 / pass 11 / fail 0（runId b675ccc2）
node tooling/run-locked.mjs --ticket 490 -- node --test packages/skill-calorie/test/weight-wake-58-339.test.mjs    → 58/58（OK 54 ＋ 缺失阻断 4 ＋ 真坏 0，runId 06461ce3）
node packages/skill-calorie/scripts/check-warning-line.mjs                                                   → RESULT 91/91 PASS
```

新判据（`test/weight-volatility-336.test.mjs` 末条 `#490`）从**路由表**取参数，四处断言：
① 第 34 条参数带 `view=anomalies-only` 且两条词的参数不再相同；② 两份正文 sha 不同（经 `bodyText` 削掉标签，故与渲染时刻无关）；
③ 第 34 条页含「只看异常点」页顶提示且不出整图曲线段；④ 第 5 条页仍出曲线段。
**变异自证**：把 `view` 摘掉后两份正文立刻回到逐字相同（断言内含此自证，判据的鉴别力就在这一个参数上）。

## 四 · 就地摆正（顺带，一件）

同文件 `#336 5 条全有命令` 用例的第一条原来只给 `window: '30d'` 不钉「今天」，窗口按当刻倒推 30 天，与种子库那段史（2026-07-20~2026-08-18）不重叠 ⇒ `points` 为 0、该用例恒红（改前实测 10 例里 1 红，与本票无关）。本次照同文件其余四条的写法补上 `today: '2026-08-18'`，文件回 11/11 全绿。

## 五 · 别处既有红（不在本票写集，只登记）

- `sport-homogeneity-109`（8 条）：第一条断在「看今日运动 → 期望 `calorie.view.exercise`、实得 `calorie.view.exercise-records`」，他席路由改动引起，与本票无关。
- `t291-自造词别名` 乙⑤：冻结词表条数 437 ≠ 436（他席新增三条分析词）。
- `calorie-c43` C6：饮食回执「改动分项」结构化断言，他席饮食文本改动引起。
