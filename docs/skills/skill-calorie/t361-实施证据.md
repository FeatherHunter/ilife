# #361 · 围度全量表 13 列 · 实施证据

> 票面：`FeatherHunter/ilife#361`（OPEN，2026-09-14 取正文，无评论）。
> 基准：`docs/skills/skill-calorie/t395-融合基准.md` §二-10／§二-12、§四-裁定 2、§六-B。
> 本席＝**接管席**：上一席实施中途死亡留下无主改动，按协议 §3.2-4／§7-规则 39，
> 本席**不把该改动当既成事实采信**，先量现场（§1）再自行判定完成度，全程保持「非受益者」身份。
> 期望值只认三样：**手算**（冻结种子）／**变更前快照**／**需求原文**（老正本 + 票面 + 基准）。

## 0 · 结论先行

| 维 | 结论 | 读数出处 |
|---|---|---|
| 票面验收：列表页可见文本 13 部位名逐个出现 | **成立** | §3 判据 1：`pass=8 fail=0`；独立探针 `A1-NAMES hit=13/13` |
| 票面负向：删掉一列 → 必红 | **成立** | §4：`mut1` → `test_exit=1 fail=2`，红句 `表头缺「肩宽」（字段 shoulder_cm）` |
| 融合①：表体缺值可见 `—` | **成立** | 独立探针 `A2-DASH-CELLS count=11 expect=11` |
| 融合②：**复制数据 payload 保留原始空值、绝不写 `—`**（裁定 2 判据二，与①分开） | **成立** | 判据 3 ＋ `probe-a4` 两种空值形态实测 |
| 融合③：窄屏卡片（老 `:128-151`／`:389-399`，未填「未填围度」`:397`） | **成立** | 判据 4、判据 5 ＋ 独立探针 `A3-CARD leak=0 filled=2` |
| 融合④：降序摘要（老 `:527-534`）**若本页有对比块则适用** | **不适用**（本页无对比块，理由见 §6） | 代码内 `最大变化／降序／摘要` 命中数 0 |
| 不许动：围度趋势页行为（#360） | **未动** | `bodyPlate.ts`／`fetch/body.ts`／`compare.ts` 工作区状态为空；判据 6 回归绿 |
| 编译 | **绿** | `tsc -b packages/skill-calorie` `exit=0`（runId `9b00912b…`） |

**未做项**：不关票、不 push（按编排者 2026-09-14 指令，sha 清单交编排者统一 push）。

## 1 · 现场测量（接管依据）

协议 §3.2-4 要求的最小接管证据＝**语法检查通过 ＋ 改动每处标题能逐条对上任务清单**。逐件量法：

| 量法 | 读数 |
|---|---|
| `git status --porcelain -- <件>` | `bodyDocs.ts` ＝ ` M`（已改未提交）；测试件 ＝ `??`（未跟踪） |
| `git diff --stat -- bodyDocs.ts` | `1 file changed, 63 insertions(+), 13 deletions(-)` |
| `git hash-object bodyDocs.ts` | `060742afdf5fb67fb43f08dcc54931e1fe669dde`（工作区 blob） |
| `git rev-parse HEAD:…bodyDocs.ts` | `cffd122c6cf255a3159a3bae9e6e491cff82ac6f`（HEAD blob，确实不同） |
| 工作区原文 SHA-256 | `902b715b3ea15f59e422657220687ecf0ce873a0832742e38ee9ba8ab88b01a1`，`bytes=12655`，`LF=255` |
| 与 `.scratch/t361/bodyDocs.ts.bak` 比对 | `git diff --no-index` 无输出（`exit=0`）→ 备份与工作区**逐字节一致**，上一席确实已还原干净 |
| `.scratch/t361/orig-sha.txt` | `ORIG-SHA256=902b715b…01a1`，与本席独立复算**一致** ⇒ 该记录可信（非「只写不核」） |

**改动逐处标题 ↔ 任务清单对账**（§3.2-4 的「最小证据」后半条）：

| # | 改动处（`bodyDocs.ts` 行） | 对上的任务项 |
|---|---|---|
| 1 | `:113-115` `MEASURE_FIELDS` 直引 `MEASUREMENT_FIELDS` | 票面「13 列」＋基准序 10「全量表 13 列」；避免第二份定序 |
| 2 | `:117-134` `MEASURE_CSS`（`msr-` 前缀、640px 断点） | 基准 §二-10／§六-B-5「窄屏卡片」，老 `:128-151` |
| 3 | `:136-157` `renderMeasureCards`（只列已填项＋「未填围度」） | 老 `:389-399`／`:397` |
| 4 | `:159-161` 注释改口径，**趋势闸门代码未动** | 票面「不许动围度趋势页」＋基准 §四-裁定 4「#361 严格排在 #360 之后」 |
| 5 | `:221-236` 全量分支：13 列展开＋`numOrDash` 表体「—」 | 裁定 2 判据一（表体） |
| 6 | `:240` `rows = v.items.map(r => ({ ...r }))` 原样透传 | 裁定 2 判据二（payload 保原始空值） |
| 7 | `:238` 宽表＋卡片同源装配 | 基准序 10「二者同一份数据」 |

**留痕口径更正**：上一席日志里出现过 `tsc -b --force`（runId `5d01437c…`）与一次失败后重试。
按协议 §5「回滚逐文件点名取回自己的受跟踪件」，上一席实际是**逐文件恢复**（`.bak` 比对逐字节相等可证），
本席据此判定其回滚有效，**但不是本席的绿**——本席所有绿读数都来自 §2 自己重跑的 runId。

## 2 · 门禁记录（GATE-RUN 声明 ＋ 对账）

全部编译／测试经加锁包装器 `tooling/run-locked.mjs` 执行（§2.4-1）；变异窗口内为**直调**（§2.4-5），窗口由外层包装器持锁，窗口内**未再抢锁**（无嵌套）。

```
GATE-RUN runId=9b00912b-dbed-4ef5-ba1c-fe05f81510b0 cmd="npx tsc -b packages/skill-calorie"
GATE-RUN runId=2eadbbf8-5877-4512-940f-8fe50bc9d59b cmd="node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs"
GATE-RUN runId=4bad51a9-2b69-4b87-9681-698db7c373a8 cmd="node .scratch/t361/mut-battery.mjs"
```

| runId | 命令 | waitedMs | exit | 说明 |
|---|---|---|---|---|
| `9b00912b…` | `npx tsc -b packages/skill-calorie` | 0 | **0** | 编译闸门（本席） |
| `2eadbbf8…` | `node --test …/t361-measure-full-table.test.mjs` | 0 | **0** | 靶向门 `pass=8 fail=0`（本席） |
| `4bad51a9…` | `node .scratch/t361/mut-battery.mjs` | 0 | **0** | 变异电池 5 相位全绿（本席） |

**对账窗口的取法（按 §2.4-3④ 只领 `exit=0`）**：本席另有 runId `0dae8d1b…`
（`npx tsc -b packages/skill-calorie`，`waitedMs=530460`，**`exit=1`**）——它**不是门禁绿**，
故**不进上表、不作声明**（§2.4-3④：跑了却失败不得充作门禁证据）。对账窗口取
`since=12:06:00Z .. until=12:34:00Z`＝**只含上表三条 `exit=0` 条目**，该红条目落在窗口**之外**，
作为「阻塞过程」记账（见下段），不参与门禁对账。

**阻塞记录（如实自曝，不假绿）**：本席首次编译 `exit=1`，红句为
`packages/skill-calorie/src/photo/galleryDoc.ts(158,22): error TS2339: Property 'set' does not exist on type 'ReadonlyMap<string, string>'`。
该件**不属于本票声明路径**（属 #341 的在途工作：`src/photo/` 三改一新增），本席**未碰**，
按协议 §2.1-5「编译因他席件失败 → 停手上报、不在他席路径上改动」处置：停手、重试。
该席于 20:26 落定后，本席重跑即得 `exit=0`（`9b00912b…`）。
即：**本票的绿不是靠绕开他席红换来的，是他席自行收敛后本席重跑所得。**

对账导出（§2.3-4）：`docs/skills/skill-calorie/t361-gate-runs.log`（本票窗口内的 3 条 `RUN` 条目，均为 `exit=0`）。
独立复核命令（只读，不需持锁）：

```
node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t361-实施证据.md \
  --log docs/skills/skill-calorie/t361-gate-runs.log --ticket 361 \
  --since 2026-09-14T12:06:00Z --until 2026-09-14T12:34:00Z
```

**提交后复验（入库态）**：两笔提交落定后，本席对**入库后的工作区**再跑一次靶向门：
`node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs` → `pass=8 fail=0`（runId `33ad4b9b-0907-4211-8ce3-a62446b66ee8`，`exit=0`，`waitedMs=1`）。
**该条不属于上表门禁声明**（它跑在本席对账窗口之后，且本节是事后追记），
仅作「入库态与原判据一致」的旁证，**不参与上面对账**；其全文见 `.scratch/t361/my-verify-committed.log`。
另核入库态：工作区 blob `060742afdf5fb67fb43f08dcc54931e1fe669dde` **等于** `0de70cb` 内该路径的 blob
（`git hash-object` vs `git rev-parse 0de70cb:…`），且 `git status --porcelain` 在本票声明路径下为空
⇒ 本席内容逐字节入库、无在途残留、无他席内容被裹进本票提交。

实测该命令在**本席自证时先红过两次**，两条红句值得记下来（对账工具确实有识别力、本席据此改了证据）：

1. 首次窗口取 `12:05:00Z..12:33:00Z` → `RESULT: matched=1/4 auditEntries=4 scoped=3 undeclared=2` ＋ `gate-audit: FAIL`。
   原因①本席把 `exit=1` 的条目也写进了声明（违反 §2.4-3④）；②窗口下界／上界切在条目上，导致绿条目落窗口外。
2. 改后仍红一次：`matched=2/3 undeclared=1` ＋ `gate-audit: FAIL`。原因是本席声明的 `cmd` 写成
   `node node_modules/typescript/bin/tsc …`（本席电池脚本内的调用写法），而审计日志记的是外层包装器收到的
   `npx tsc -b packages/skill-calorie` —— `cmdMatches` 是**文本**口径，不归一化这两者 ⇒ 声明必须逐字抄日志。

**最终（本席交付口径，未用任何 `--allow-*` 放宽开关）**：

```
RESULT: matched=3/3 auditEntries=3 scoped=3 undeclared=0
gate-audit: PASS
```

对账窗口取本席自己的三条绿条目，故窗口内**无**无人声明的条目；
上一席在 11:18–11:21 的 9 条条目**不在本席窗口**，本席**不为它们背书**（其中 3 条 `exit=1`：两条是上一席变异电池的预期红，一条是还原后首次重跑失败、随后 `f49d8cf6` 重试转绿）。
未使用任何 `--allow-*` 放宽开关 ⇒ 无需 `GATE-RELAX` 留痕。

## 3 · 判据逐条（跑哪条命令 → 看哪个读数 → 等于什么）

**判据 1 · 票面验收：列表页可见文本 13 部位名逐个出现（冻结种子）**
- 跑：`node --test packages/skill-calorie/test/t361-measure-full-table.test.mjs`
- 看读数：`ℹ pass 8` ／ `ℹ fail 0`（runId `2eadbbf8…`，exit 0）；同参数下日志标记行
  `T361-READOUT names=13/13 fields=chest_cm,waist_cm,abdomen_cm,hip_cm,left_thigh_cm,right_thigh_cm,left_calf_cm,right_calf_cm,left_arm_cm,right_arm_cm,left_forearm_cm,right_forearm_cm,shoulder_cm`
- 等于：13／13 命中且 `fail 0`。
- 期望值来源：**需求原文**——13 个部位名的**字段序**取 `fetch/body.ts:25-32 MEASUREMENT_FIELDS`
  （变更前快照）；中文名取老正本 `scripts/render_body_measurements_view.py` 的 `METRIC_LABELS`
  与新侧 `bodyDocs.ts:106-111 MEASURE_ZH`（12／13 同字）。
- **唯一差异**：`shoulder_cm` 老 py 写「肩围」，新侧向导页／本页／#360 趋势图题一律「肩宽」。
  本票**认新侧冻结口径「肩宽」**（理由：新侧三处一致，改一个名会连带 #360 已关的趋势图题；
  且与用户可见的其余两处不一致才是更大的不一致）。差异登记于此，供编排者裁决。

**独立复核（本席自设，不采信票内测试的自我论证）**：`node .scratch/t361/probe-independent.mjs`
→ `RESULT: PASS`，五行读数 `A1-NAMES hit=13/13`、`A2-DASH-CELLS count=11 expect=11`、
`A3-CARD leak=0 expect=0 filled=2 expect=2`、`A4-PAYLOAD absent_kept=true contains_dash=false`、
`A5-SRC-OFFIELD spread_present=true literal_order_count=0`。
`literal_order_count=0` 即「本文件里不存在第二份硬写的列序」，正面落实基准序 10 的「同一份数据／同一定序」。

**判据 2 · 融合①：表体缺值格可见 `—` 且无连续空单元**
- 跑：同上测试的第 2 例；独立探针 A2。
- 看读数：一条「只填 2 项（胸 95／肩 45）」的记录渲染后，缺值格 `<td>—</td>` 个数
- 等于：`13 - 2 = 11`（**手算**：全量 13 项减已填 2 项）。实测 `count=11`。
- 老正本口径 `body_measurements_view.html:385` 与 #360 沿用口径一致。

**判据 3 · 融合②：复制数据 payload 保留原始空值、绝不写 `—`（与判据 2 分开断言）**
- 跑：`node .scratch/t361/probe-a4.mjs`（两种空值形态）＋ 测试第 3 例。
- 看读数（`probe-a4`）：
  - `CASE-NULL waist=null isNull=true`、`CASE-NULL dash=false` —— 真库 sqlite 返 **显式 `null`** 时原样保留；
  - `CASE-UNDEFINED waist=undefined hasOwn=false`、`CASE-UNDEFINED dash=false` —— 字段**缺位**时该键不出现；
  - 两场景 `dash=false` ⇒ payload 全文**不含**「—」；
  - `CASE-NULL tableDashCells=11 expect=11` ⇒ 同一次渲染里表体**照旧**是「—」。
- 等于：**同一页同时满足两条相反口径**——表体缺值写「—」、payload 保留原始空值。
- 期望值来源：**需求原文**（基准 §四-裁定 2、票面「融合验收」第一条）：payload 该字段是空值
  （`""` 或**字段缺位**），不含「—」。本实现两种形态都不写「—」。
- 测试第 3 例还独立断言库内源头 `SELECT waist_cm … = null`（变更前快照口径）。

**判据 4 · 融合③：窄屏卡片与宽表同源、只列已填项**
- 跑：测试第 4 例；独立探针 A3。
- 看读数（A3，一行只填胸／肩）：`leak=0`（其余 11 个未填键在卡片区**一个都不出现**）、`filled=2`（已填 2 项都在）。
- 等于：`leak=0` 且 `filled=2`。期望值来源：**老正本** `:393-395` 的 `filter(c => r[c] != null)`。
- 另断言卡片日期列与宽表日期列**逐行相同**（`cardDates == tableDates`），落实基准「二者同一份数据」。

**判据 5 · 融合③续：整行无值写「未填围度」（老 `:397`）**
- 跑：测试第 5 例（合成视图直调 `buildBodyMeasureDoc`）。
- 看读数：`html.includes('未填围度')` 为真。
- 期望值来源：**老正本** `:397` 的 `items || '<div class="rec-empty">未填围度</div>'`（原文逐字「未填围度」）。
- **口径诚实说明**：库触发器（`schema.ts:144-152`）禁全空行落库 ⇒ 真库语义下该分支**不可达**，
  故只能走合成视图直调。本席**明确标注此为合成口径**，不断言真出口（不假绿）。
  同时该例断言同一合成视图下表体仍写「—」，证明两条口径互不塌陷。

**判据 6 · 不许动：围度趋势页行为（#360）**
- 跑：测试第 6 例；另核 `git status` 三个禁碰件。
- 看读数：`胸围趋势`（平局取首位口径）＋ `<svg>` 存在 ＋ KPI `趋势点 3 天／均值 95.5／最小 95／最大 96／变化量 +1／最新 96`。
- 等于：**手算**——13 部位最新非空同为 R3 ⇒ 平局按 `MEASUREMENT_FIELDS` 序取首位 `chest_cm`；
  胸围序列 `[95, 95.5, 96]` ⇒ 点数 3、均值 95.5、最小 95、最大 96、变化量 +1。
- 禁碰件工作区状态：`body/bodyPlate.ts`＝空、`fetch/body.ts`＝空、`body/compare.ts`＝空（均未被本席触碰）。

**判据 7 · 整页骨架与空库语义**
- 跑：测试第 7、8 例。
- 看读数：`<!doctype html>`／`<meta charset="utf-8">`／内联样式 ≥2048 字节／无外链／回执绝对路径可读；
  空库 `exit=4` 且 `stdout` 纯净（不编兜底页）。
- 等于：8／8 全绿即两项均成立。

**真库零写入对账**：全部数据走 `mkdtempSync` 临时库并以 `SKILLS_DB_PATH` 指过去（测试头注已声明）；
测试对真库的唯一接触是第 3 例的 `openDb(临时库)` 只读 `SELECT`。本席**未**对真库执行任何写语句。

## 4 · 变异自证（源码级，票面负向）

跑：`node tooling/run-locked.mjs --ticket 361 -- node .scratch/t361/mut-battery.mjs`（runId `4bad51a9…`）。
电池一次持锁窗口内做 5 相位，每相位＝一次编译 ＋ 一次靶向测试；窗口内一律**直调**（§2.4-5），无嵌套抢锁。

| 相位 | 变异内容 | 期望 | 实测 | 判定 |
|---|---|---|---|---|
| P0 `baseline` | 内容＝当刻工作区 | 绿 | `compile_exit=0 test_exit=0 pass=8 fail=0` | OK |
| P1 `mut1` | **删掉一列**：列展开前 `filter(f => f !== 'shoulder_cm')` | 红 | `compile_exit=0 test_exit=1 pass=6 fail=2` | OK |
| P2 `restore1` | 还原当刻工作区 | 绿 | `compile_exit=0 test_exit=0 pass=8 fail=0` | OK |
| P3 `mut2` | payload 也把空写成「—」 | 红 | `compile_exit=0 test_exit=1 pass=7 fail=1` | OK |
| P4 `restore2` | 还原当刻工作区 | 绿 | `compile_exit=0 test_exit=0 pass=8 fail=0` | OK |

**负向两行机器读数（票面「删掉一列 → 必红」）**：

```
MUT-RED     phase=mut1 test_exit=1 pass=6 fail=2  ✖ 表头缺「肩宽」（字段 shoulder_cm）／✖ 表头缺列 肩宽
MUT-GREEN   phase=restore1 test_exit=0 pass=8 fail=0
```

**M2 的负向两行**（裁定 2 判据二的独立自证）：

```
MUT-RED     phase=mut2 test_exit=1 pass=7 fail=1  ✖ 载荷 R2 腰围应保留原始空值 null（不得写「—」）
MUT-GREEN   phase=restore2 test_exit=0 pass=8 fail=0
```

**还原一致性（§5：回滚要连生成物一起复位）**：电池在 `finally` 里逐文件写回内存中的当刻原文并复算哈希：

```
RESTORE-CHECK packages/skill-calorie/src/body/bodyDocs.ts sha=902b715b3ea15f59e422657220687ecf0ce873a0832742e38ee9ba8ab88b01a1 expect=902b715b…01a1 equal=true
RESULT-PHASE restore-final compile_exit=0 test_exit=0 pass=8 fail=0
RESULT: restore_clean=true
```

即：还原后**重新编译**（重编）＋**重跑靶向测试**（重签，用重新编译后的产物）双双为绿，
且受版本控制件内容与变异前**逐字节相等**。窗口内工作区自变更**清零**。

**变异日志路径**：`.scratch/t361/m-mut1-test.log`、`m-mut2-test.log`、`m-restore*-test.log`、
`m-*-compile.log`、`mut-battery.log`（机器读数全文）。

## 5 · 文件哈希（交付物对账）

| 件 | SHA-256 | bytes | LF |
|---|---|---|---|
| `packages/skill-calorie/src/body/bodyDocs.ts` | `902b715b3ea15f59e422657220687ecf0ce873a0832742e38ee9ba8ab88b01a1` | 12655 | 255 |
| `packages/skill-calorie/test/t361-measure-full-table.test.mjs` | `6e8423d2738a3e360fa45fcf26c9c1d8a4307a6f987e0141973f19f0a3d77132` | 16782 | 325 |

**代码行数告警线**（`packages/skill-calorie/AGENTS.md`：350 行，LF 口径）：
`bodyDocs.ts` ＝ **255 行**，未超线，无需触发必报五步第四步。测试件不计入本口径。

**本席未新增／未修改任何测试用例**：接管席判定上一席的 8 个用例已覆盖票面四项
（13 名逐个／缺值「—」／payload 空值无「—」／窄屏卡）且**每项都有改坏必红**（§4 两轮变异实测），
故**补齐项为空**——按「非受益者」原则本席用**独立探针**（§3 判据 1）另开口径复核，
而不是采信上一席测试的自我论证。测试件哈希与 `.scratch/t361/` 首跑日志一致即为未变。

## 6 · 融合验收第四条「降序摘要」为何不适用（具名理由）

票面/基准原话：`：527-534` 对比降序＋最大变化摘要「**若本页有对比块则适用**」。

- 读：`git grep -n "最大变化\|降序" -- packages/skill-calorie/src` → **命中 0**（全包尚无该块）。
  即新侧降序摘要**根本不是本页产物**，也不在 `bodyDocs.ts` 里。
- 读：基准 §二-12 与 §四-裁定 4 的落点列写的是 **`body/compare.ts`** 与 **#360／#361**；
  而老 `:527-534` 属**对比页**（两期快照），本票页是**列表页**（`calorie.view.body-measure`）。
- 读：`bodyDocs.ts` 两个分支（`:197` 带 `metric` 的单部位分支、`:217` 全量分支）**均无对比块**
  → 「若本页有对比块」的前件**为假** ⇒ 本条不适用。
- 结论：**不适用，且不需要动 `compare.ts`** ⇒ 本票「禁碰 `compare.ts`，除降序摘要确需且先在影响清单点名」
  的例外条件**未被触发**，`compare.ts` 工作区状态为空可证。

## 7 · 交付物与路径

| 类别 | 路径 | 状态 |
|---|---|---|
| 实现 | `packages/skill-calorie/src/body/bodyDocs.ts` | 已改（63 增 13 删） |
| 测试 | `packages/skill-calorie/test/t361-measure-full-table.test.mjs` | 新增（未跟踪，本席提交） |
| 证据 | `docs/skills/skill-calorie/t361-实施证据.md` | 本件（新建） |
| 对账导出 | `docs/skills/skill-calorie/t361-gate-runs.log` | 新建（§2.3-4） |
| changeset | `.changeset/t361-measure-full-table.md` | 新建（唯一） |
| 草稿 | `.scratch/t361/`（含 `mut-battery.mjs`、`probe-independent.mjs`、`probe-a4.mjs` 及全部日志） | 独占，不进版本库 |

## 8 · 未做项与下一手缺什么

1. **未关票、未 push**（按编排者 2026-09-14 指令，由编排者统一 push）。票面仍是 OPEN。
2. **`shoulder_cm` 中文名「肩宽」vs 老正本「肩围」** 的口径冻结需编排者确认；本席按新侧三处一致取「肩宽」，
   若编排者裁定回溯老正本，则需连带 #360 已关的趋势图题一并改（本票范围为「不许动 #360」）。
3. **判据 5 的出口是合成视图**（库触发器禁全空行 ⇒ 真库不可达）。若审查者要求真出口覆盖，
   需先解掉全空行落库禁令（属别票范围），本票不擅自放宽。
4. **上一席的 9 条审计条目（11:18–11:21）未获本席声明**，其中 3 条 `exit=1`（`e8e4f635`／`62be0058`／`410d095f`）。
   本席已在上文如实列出并给出归属（上一席变异红／还原重试），**未把它们充作门禁绿**。
   上一席那批的**绿**（`ce115017`／`f49d8cf6`）本席同样**不引用**——本席所有绿读数均出自 §2 自己重跑的 runId。
5. 审查者若做「合并点安静态复核」，注意 `src/photo/` 有 #341 的在途件（本席未碰）；
   本票提交只含 §7 的五件（逐件点名 `git add`，`git diff --cached --name-only` 复核）。
