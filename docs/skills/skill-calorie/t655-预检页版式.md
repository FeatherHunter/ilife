# t655 · 09-06 记身材照（预检确认页）／09-07 生成身材照GIF（规划器）两页版式整改

- 票号：#655（负责人 2026-09-16 验收打回：图一／图二「很丑陋，需要整改，需要更漂亮简约大气的设计风格」）
- 落点：`packages/skill-calorie/src/render/wizardPortDocs.ts`（两页装配件）、
  新建 `packages/skill-calorie/src/photo/wizardUi.ts`（这两页的确认清单形状与页内样式）、
  新建 `packages/skill-calorie/test/photo-wizard-shape-655.test.mjs`（形状钉）。
- 样张：改前 `.scratch/t655/before/`、改后 `.scratch/t655/after/`（四份 HTML，渲染器入仓见下）、
  截图 `.scratch/t655/shots/`（每页 390／1280 各一对）。
- 渲染器（入仓，可重出样张）：`docs/skills/skill-calorie/t655-样张渲染.mjs`
  `node docs/skills/skill-calorie/t655-样张渲染.mjs --out .scratch/t655/after --label after`

---

## 一、缺陷 → 改法 → 读数（逐条）

| # | 打回原话（缺陷） | 改法（落在哪一行） | 读数 |
|---|---|---|---|
| 1 | 字段三层噪音：字段名（小灰字）＋框＋框内灰示例层层叠 | 值不再住输入框：输入框只在**展开后**出现，展开体里的字段名仍带限制（如「每帧多久（毫秒，50 到 5000）」），空栏才留 `placeholder` | 折叠态页面上一个 `placeholder` 都不渲染；两页 `<details class="phu-edit">` 各 3／9 个 |
| 2 | 像空表单不像确认页：三个框全空，看不到值 | 值先以**只读一行**上屏：行本体 ＝ 原生 `<details>` 的 `<summary>`（行名 ＋ 当刻值），点开哪一行才出哪一行的输入 | 预检页预填态 `照片文件路径` 行显示 `D:\照片\正面1.jpg`／`D:\照片\正面2.jpg`（带「2 张」角标）；空态显示「还没填」「没填（这栏可以不填）」 |
| 3 | 粗体警示句当标题用 | 那句改住 `noticeBar()` 的浅底提示条：13px、`font-weight:400`、不再 `<strong>` | 两页 `<strong>` 命中 **0**；告知句恰 1 处且住 `<p class="phu-alert">` |
| 4 | 层级不分明（页首句／字段名／值／示例几乎同档） | 三档定死：块标题 `.phu-sec` 15/600、行名 `.phu-edit-k` 12/600 弱色、值 `.phu-edit-v` 14/400 主色 | 页内样式表里三条规则读数：`15px…font-weight:600`／`12px…font-weight:600`／`14px…font-weight:400`（`photo-wizard-shape-655` ③ 钉住） |
| 5 | 留白不均、块与块之间没有分割 | 块标题行 `.phu-sechead`（上留白 22px）＋清单白卡行间细线 `.phu-edit{border-top:1px solid --line}`＋提示条独立成块 | 规划器页新增三处块标题（选哪些照片／播放／画面与文件）＋候选清单补「会进 GIF 的照片」块标题 |
| 6 | 边框偏重、底色单调 | 只读值住白卡 ＋ 细线分隔（不再一层层描边）；可编辑位单独给形状（1px `--line` 细边 ＋ 白底 ＋ 极浅浮起），与只读位分得开 | 输入框 1px 细边（原来公共层是 `border:0` ＋ 页底色填充，在页上几乎看不见） |
| 7 | 首屏被最上面「常用标签」折叠块吃掉 | **下移**：常用标签从页首移到三个值之后、复制区之前；仍是 8 个词逐字可见（不再折叠） | 位置判据：`常用标签` 下标 ＞ `name="srcPaths"` 且 ＜ `给 AI 的指令` 下标（`photo-wizard-shape-655` ④） |
| 8 | 图二把 8 个默认值全画成输入框、像要用户逐个核对 | 九项参数位改**只读键值行**（当前值上屏），按变化频率分三组，只展开「要改的那一项」 | 折叠态可见值：`正面`／`全部 2 张`／`500 毫秒`／`无限循环`／`硬切`／`400 像素`／`600 像素`／`没填`／`没填` |

**不削功能（逐条对过）**：预检页三个填写位（`name="srcPaths"`／`tag`／`note`）在、
常用标签 8 个词在、「这些是 AI 已经用的值。改了不会自动生效——要改就直接跟 AI 说一句。」在（恰 1 处）、
「给 AI 的指令」块在；规划器九项参数位（`tag`／`photoIds`／`duration`／`loop`／`width`／`height`／
`watermark`／`transition`／`output`）在，两个下拉**显示当刻值**（`<option value="3" selected>3 次</option>`、
`<option value="fade" selected>淡入淡出</option>`），KPI 四格与候选行在。

**顺带摆正的两处（同一批改动里改掉的重复句，未见削功能）**：
① 预检页表单说明「照片在你手机或电脑上，这里只登记路径与标签，不会动照片本身」与页头副标题
「照片本身不会被动」说同一件事 → 删表单那句（副标题留着）；
② 规划器表单说明原是一段 13px 灰字，与新提示条抢同一格 → 收成一条 12px 辅助行
（「下面每项都已填好常用值，点开哪一项就改哪一项。改完把下面那段指令复制给 AI。」）。

---

## 二、机器面读数（判据两条，红绿两向）

### 2.1 三档横向溢出（`measure-responsive.mjs`，真 Chrome／CDP）

```
改前  node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t655/before --label before
  09-06-预检确认页-空.html      390档:0 ✓  768档:0 ✓  1440档:0 ✓
  09-06-预检确认页-预填.html    390档:0 ✓  768档:0 ✓  1440档:0 ✓   （另报 clipped：一个 input 内横滚，如实记录）
  09-07-GIF规划器-改过两项.html 390档:0 ✓  768档:0 ✓  1440档:0 ✓
  09-07-GIF规划器.html         390档:0 ✓  768档:0 ✓  1440档:0 ✓
  OVERFLOW-ZERO pages=4 cells=12 failed=0 scopeOutFailed=0 label=before

改后  node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t655/after --label after
  09-06-预检确认页-空.html      390档:0 ✓  768档:0 ✓  1440档:0 ✓
  09-06-预检确认页-预填.html    390档:0 ✓  768档:0 ✓  1440档:0 ✓   （改前的 clipped 那处随输入框收进展开体一并消失）
  09-07-GIF规划器-改过两项.html 390档:0 ✓  768档:0 ✓  1440档:0 ✓
  09-07-GIF规划器.html         390档:0 ✓  768档:0 ✓  1440档:0 ✓
  OVERFLOW-ZERO pages=4 cells=12 failed=0 scopeOutFailed=0 label=after
```

读数件：`.scratch/t655/logs/measure-before.log`／`measure-before.json`／`measure-after.log`／`measure-after.json`。

### 2.2 分隔符与内部标识符（`audit-separators.mjs`，节点级）

```
改前  FILE 09-06-预检确认页-空.html    R1..R7 全 0  命中=0
      FILE 09-06-预检确认页-预填.html  标识符 1     命中=1   （L1930，`<ilife-block-pre-block-code>` ＝ 给 AI 的指令原文）
      FILE 09-07-GIF规划器-改过两项.html 标识符 1   命中=1   （L1929，同上）
      FILE 09-07-GIF规划器.html          标识符 1   命中=1   （L1929，同上）
      RESULT: 1/4   （红＝工具口径；本票判据是「不新增」，见下）

改后  FILE 09-06-预检确认页-空.html    R1..R7 全 0  命中=0
      FILE 09-06-预检确认页-预填.html  标识符 1     命中=1   （同上，位置同族）
      FILE 09-07-GIF规划器-改过两项.html 标识符 1   命中=1   （同上）
      FILE 09-07-GIF规划器.html          标识符 1   命中=1   （同上）
      RESULT: 1/4
```

**逐页逐条不新增：0→0、1→1、1→1、1→1**。残留的 3 处 R7 全部落在「给 AI 的指令」预览块里
（命令键 ＋ 命令原文），那是这页的**功能**（复制给 AI 的原文），改前就在，本票一字未动
（出处件 `wizardPort.ts` 的 `buildPhotoLogWizardPrompt`／`buildGifPlannerPrompt` 不在写集内）。
**本轮自证抓到并改掉过一处新增**：规划器页那条辅助行原写「…点开哪一项就改哪一项**；**改完把…」，
`；` 触发 R2 → 当场拆成两句（读数回到 1）。

读数件：`.scratch/t655/logs/sep-before.log`／`sep-before.json`／`sep-after.log`／`sep-after.json`。

---

## 三、门读数（`node --test`，逐件）

```
node tooling/run-locked.mjs --ticket 655 -- node --test \
  test/photo-wizard-shape-655.test.mjs test/wizard-86.test.mjs test/photo-shape-341.test.mjs \
  test/photo-responsive-484.test.mjs test/photo-picker-283.test.mjs test/photo-gif-page-352.test.mjs \
  test/photo-receipt-docs-282.test.mjs test/photo-budget-438.test.mjs test/photo-shape-read-281.test.mjs \
  test/photo-help-fork-437.test.mjs
⇒ ℹ tests 69 ／ ℹ pass 67 ／ ℹ fail 2
```

| 件 | 读数 |
|---|---|
| `photo-shape-341.test.mjs` | 4/4 pass |
| `photo-responsive-484.test.mjs` | 7/7 pass（三档溢出、图宽约束、触摸区、GIF 舞台）|
| `photo-picker-283.test.mjs` | 全 pass |
| `photo-gif-page-352.test.mjs` | 全 pass |
| `photo-receipt-docs-282.test.mjs` | 全 pass |
| `photo-budget-438.test.mjs` | 4/4 pass |
| `photo-shape-read-281.test.mjs` | 全 pass |
| `photo-help-fork-437.test.mjs` | 2/2 pass |
| `photo-wizard-shape-655.test.mjs`（本票新增，形状钉） | **6/6 pass** |
| `wizard-86.test.mjs`（本两页的直接覆盖件） | **7/9 pass**：本票那 7 条全绿（身材照 wizard／GIF 框选器／#474 缺 ID 归位／空库不抛／CLI 端到端／复制双通道／唤醒词）；**2 条红属范围外**，见 §5 |
| 合计 | **67/69**，两条红都是范围外的既有红（§5 第 2 条）|

变异自证两行机器读数（`photo-wizard-shape-655` ⑥）：
`MUTATION-RED #655 值行形状改坏必红（输入框排到只读行之前 → false） ｜ MUTATION-GREEN 改回必绿（原页 → true）`。

结构门：`node packages/skill-calorie/scripts/check-warning-line.mjs` →
`RESULT: 91/91`、`PASS: 告警线台账齐全且与实况一致`（扫描面 332 件；新件 `src/photo/wizardUi.ts` 118 LF，未越线、不入台账）。

日志件：`.scratch/t655/logs/gates-final.log`（末轮）、`gates4.log`（新增件单跑）、
`build5.log`（末轮编译，经 `run-locked --ticket 655`）、`sep-*.log`／`measure-*.log`。

---

## 四、必报五步（`docs/agents/structure.md`）

**第一步 · 影响清单**（动手前报，逐行一个能力目录）

| 目录／文件 | 改什么 | 为什么 |
|---|---|---|
| `src/render/wizardPortDocs.ts` | 两个 `build*Doc` 的区块序列 | 两页的装配件，本票唯一落点 |
| `src/photo/wizardUi.ts`（新建） | 确认清单的形状与页内样式 | 这两页的形状件；**为什么不并进 `photoUi.ts`**：那个件被本族另外七页共用，样式串进了它们每一页的 `<style>`，并进去等于本票顺手改了别人的页 |
| `test/photo-wizard-shape-655.test.mjs`（新建） | 形状钉＋变异自证 | 现有门只钉「哪些字在不在」，钉不住「值是不是只读一行」 |

**第二步 · 结构设计**：无新目录层级。新件 `wizardUi.ts` 对外给 **3 个**东西
（`wizardUiCss()` / `editRows()` / `noticeBar()`）＋ 1 个类型（`EditRowInput`），不超过五个；
`wizardPortDocs.ts` 对外仍是 2 个 `build*Doc`。共用件：`wizardUi.ts` 的使用者是**本族两张过程型页**
（`src/photo/wizard.ts` 经 `wizardPortDocs.ts` 调用）；徽章走公共层 `renderChips`、参数表单走公共层
`renderParamForm`、样式只用冻结 token，`packages/base-render/**` 一行未碰。

**第三步 · 写代码**：跨目录只走已有公开件（`photoUi.ts` 的 `chipRow`/`photoPickRows`/`photoUiCss`、
`shared/docPage.ts` 的 `assembleDocPage`）；常量单一定义地（`FORM_NOTICE` 一份、`LOOP_OPTIONS`／
`TRANSITION_OPTIONS` 沿用既有）；行形状只在 `wizardUi.ts` 定义一次。

**第四步 · 超线报警**：当场 LF —— `src/photo/wizardUi.ts` **118**、`src/render/wizardPortDocs.ts` **316**、
`src/photo/photoUi.ts` **258**（本票未改，逐字节等于 HEAD）。**三件都在 350 以内，本次不触发第四步。**

**第五步 · 交付对账**：实际碰上 3 个路径，与第一步逐行一致，**偏差为零**。
`photoUi.ts` 一度被本票改过（CSS 并进去），复核时判为「会把读侧七页的产物字节一起改」→
**逐字节还原成 HEAD 版**（`git show HEAD:… > 文件`，字节 20799，`git diff --stat` 为空），
样式改住新件；这条偏差在交付前已归零。
最终写集：`src/render/wizardPortDocs.ts`（改）、`src/photo/wizardUi.ts`（新，118 LF）、
`test/photo-wizard-shape-655.test.mjs`（新）、`docs/skills/skill-calorie/t655-预检页版式.md`（新）、
`docs/skills/skill-calorie/t655-样张渲染.mjs`（新）。**未碰**别的席的路径
（`packages/base-render/**`、照片族其它页的可见文本、`packages/skill-calorie/AGENTS.md` 的在途改动、
`.scratch/t351-preview/wall/`、`D:\ilife-验收\scene09\`）。

### 改前／改后样张路径

```
改前 HTML   .scratch/t655/before/09-06-预检确认页-空.html ／ -预填.html ／ 09-07-GIF规划器.html ／ -改过两项.html
改后 HTML   .scratch/t655/after/  同名四份
截图（390／1280）  .scratch/t655/shots/09-06-改前-390.png ／ -1280.png ／ 09-06-改后-*.png
                  .scratch/t655/shots/09-07-改前-390.png ／ -1280.png ／ 09-07-改后-*.png
```

---

## 五、未做项与风险（如实说）

1. **`gen-photo-baseline.mjs --check` 是红的，且不是本票判据**（票面门清单里没有它）。
   实测：该基线由 #530 于 2026-09-16 21:42（`054315c3`）重落十页；此后 `91563b45`（#654，改
   `packages/base-render/**` 共享块层）、`22366683`（#437）、`3e6a1189`（#461）、`e1a79ce2`（#343）
   陆续落地 ⇒ **在 HEAD 上该门就已经是 10/10 CHANGED**（与 #655 无关）。
   本票复核用的是一条可查的隔离证据：把 `photoUi.ts` 逐字节还原成 HEAD 版之后，本票的写集只剩
   `wizardPortDocs.ts` ＋ 新建 `wizardUi.ts`，而 `git grep -ln "wizardPortDocs\|wizardUi" -- src`
   只命中 `src/photo/wizard.ts`（本件的唯一调用方）⇒ **另外八页的产物字节本票一字未动**。
   **没有跑 `--write`**：重落会把 #654／#437／#461／#343 那八页的漂移一并当成既成事实签字，
   那是编排者合并点的动作，不是本票的。**下一手**：合并点安静态跑一次
   `node packages/skill-calorie/scripts/gen-photo-baseline.mjs --write`（两轮 `--check` 应 10/10 OK）。
2. **`wizard-86.test.mjs` 有 2 条红，属范围外发现（S3，建议转票）**：
   - `#86 围度 wizard`：`assert.ok(empty.html.includes('复制 prompt'))` —— 该页复制区小标题已被
     `8a576155`（2026-09-15「卡路里场景08 过程型五页重排：体成分向导／围度向导」）改成「复制指令」，
     测试件未同步；
   - `#86 体脂 wizard`：`assert.ok(bare.html.includes('请选来源'))` —— 该页 prompt 占位已改成
     「请先选来源。本页按默认的「家测皮褶钳」打开。」，测试件未同步。
   两条都落在 `src/body/wizardDocs.ts`／`wizardPlate.ts`（本票写集外），**HEAD 上就是红的**：
   本票两个改动件不在它们的引用链上（`body/wizardDocs.ts` 的 import 里没有 `photoUi`／`wizardPortDocs`）。
   本票未动它们（不许动别的页的可见文本）。
3. **只出 390／1280 两档截图**：768 档读数在 `measure-responsive` 的 JSON 里（三档全 0），未另出截图。
4. **展开态没有截图**：`<details>` 折叠／展开是原生行为，截图工具只截静态首屏；展开后的形状由
   `photo-wizard-shape-655.test.mjs` ① 的 DOM 判据钉住（输入框在 `</summary>` 之后）。人工复核时
   在浏览器里点开任一行即可看到。
5. **观感只做了自查**：用本机 vision 工具比过改前／改后两档截图（结论：层级、留白、可读性均优于改前，
   未发现改后新增的明显缺陷）；最终观感验收仍归负责人。
6. **一处形状取舍**：≤640 档确认清单的行从「两列」塌成「名字一行、值一行」（长路径不再挤进 76px 窄柱，
   同族 `phu-fact` 的既有口径）。代价是窄屏上九行参数比改前高约 100px（实测整页 2015 → 2113px）。
   如负责人更想要窄屏保持两列紧凑，改一行媒体查询即可（`.phu-edit-row` 的 `≤640` 段）。
