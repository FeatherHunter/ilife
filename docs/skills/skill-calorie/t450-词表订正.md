# 场景 09 唤醒词词表订正 · 证据（t450）

> 票号说明：本轮由编排方交办的是 **MAP #159（`[wayfinder] 卡路里场景 09 身材照片`）下的实施席**，实施时没有子票，本文当时按施工单给的命名模板留位成 `t159-词表订正.md`。**收口席已补开正票 [#450](https://github.com/FeatherHunter/ilife/issues/450)（`wayfinder:task`，已挂成 #159 的子议题）**，本件随之改名成 `t450-词表订正.md`：旧名路径 `docs/skills/skill-calorie/t159-词表订正.md` 已不存在；仓内指向本件的 7 处引用（`src/photo/commands.ts:12`／`workflows/09-身材照片.md:97`／`test/wizard-86.test.mjs:71`／`test/skill-md-photo-285.test.mjs:98`／`t400-融合基准.md:160`／`t285-工作流落点.md:31`／`t283-候选与快照.md:19`）已同步改成新名，逐条见 §四。地图设计件 `t159-map-design.md` 仍按地图号命名（它是地图的设计正本，不是本票的证据件）。本轮顺带开出的两张遗留票见文末（#446／#447）。

- 交付日期：2026-09-14（实施席 ＋ 收口席同日两段）
- 写集（10 件，逐条见 §四）：`src/photo/commands.ts`／`src/triggers/scene-09-photo.ts`／`workflows/09-身材照片.md`／`package.json`／`SKILL.md`（生成＋场景 09 那一段一处手写）＋两张测试＋三张文档。
- 收口席（#450）另改：`SKILL.md:72` 手写区最后一处残留（1 行）；上列 7 处旧名引用（逐处只换路径，语义一字未动）；证据件按票号改名；新建对账导出件 `docs/skills/skill-calorie/t450-gate-runs.log`。
- **未做**：`src/photo/routes.ts` 一行未动（含 `order`）；其它 8 个场景的 `routes.ts` 未动；不做 git 提交、不 push。

---

## 一、九词在老技能的复核读数（`Select-String`，只读目录，已跳过 `.个人笔记不允许参考/`）

复核目录：`D:\2Study\StudyNotes\SKILLS\卡路里`。方向：老技能有的借鉴落地，没有的去掉。

| 自造词 | 根目录件 | 递归 397 个候选件 | 结论 |
|---|---|---|---|
| 移除身材照 | 0 | 0 | 去掉 |
| 设置照片标签 | 0 | 0 | 去掉 |
| 查身材照详情 | 0 | 0 | 去掉 |
| 对比身材照 | 0 | 0 | 去掉（唯一命中在 `.scratch/查身材照-图片丢失-HTML过大/issue-body.md:62`，是一份草稿 issue 正文，不是技能内容） |
| 做身材照GIF | 0 | 0 | 去掉 |
| 看身材照HELP | 0 | 0 | 去掉 |
| 看身材照向导 | 0 | 0 | 去掉（规划席所称「老提示语里有据」经复核不成立：老提示语里 0 命中） |
| 看GIF规划器 | 0 | 0 | 去掉（近似词 `GIF规划器` 全库 1 命中，在 `scripts/render_body_photo_gif_planner.py:283` 的**产物文件名** `身材照GIF规划器_<suffix>`，不是唤醒词） |
| 选身材照 | 0 | 0 | 去掉（同上，0 命中） |

递归口径：`docs`／`tests`／`scripts`／`templates`／`html`／`calorie_html`／`references`／`body_photos_gif` 八个子目录，扩展名 `.md/.py/.html/.ts/.json/.txt/.js`，共 **397** 个候选件；根目录件另算（`SKILL.md`／`config-calorie.ts` 等）。

近似词复核（同一 397＋根目录口径）：`身材照详情` 0、`身材照向导` 0、`身材照HELP` 0、`照片向导` 0、`移除身材照` 0、`选身材照` 0。

**老技能真有的词（同口径命中数）**：记身材照 35／查身材照 26／对比两张照片 22／生成身材照GIF 22／删身材照 26／改照片标签 37／加照片标签 24／删照片标签 26；真名另有 `存身材照` 9 与 `看身材照` 18。

**真名行号复核**：`scripts/_triggers.py:2669` = `'key': 'body_photo_add_single', 'name': '存一张照片', 'subfunction': '存身材照'`；`scripts/_triggers.py:2699` = `'key': 'body_photo_list', 'name': '看身材照', 'subfunction': '看身材照'`。

**「卡路里HELP」是老技能正文注册的唤醒词**：老 `SKILL.md:5`（「说「卡路里HELP」打开完整能力速查台」）、`:219`、`:291`（「用户说"卡路里 help"（v2.4.10 起注册为正式唤醒词"卡路里HELP"）」）、`:299`（命令表一行）。

---

## 二、改了什么（逐条，含改动前后）

1. `src/photo/commands.ts:27` `calorie.photo.gif`：代表唤醒词 `做身材照GIF` → **`生成身材照GIF`**（老词）。
2. `src/photo/commands.ts:23` `calorie.help.center`：代表唤醒词 `记身材照` → **`卡路里HELP`**（老技能正文注册的唤醒词）。**改词不删行**。
3. `src/photo/commands.ts:28` `calorie.photo.list`：**保留 `看身材照`**。理由三条：① 是老技能真名（`:2699` 的 `name`）；② `calorie.photo.detail` 的代表词现状是 `查身材照`，改成同词会两键同代表词，撞本仓「代表词回同键」标准；③ `看身材照` 在路由表 `src/photo/routes.ts:24`（new 列表 order 13）指向 `calorie.photo.list`——**回同键成立**。
4. `src/photo/commands.ts:31/32/33`：`calorie.view.gif-planner`／`calorie.view.photo-log-wizard`／`calorie.view.photo-picker` **删掉 `wakeWord` 字段**（老技能这三个页面是流程内页，唤醒词表里没有它们）。机制出处：`shared/commandSpec.ts:45-48` 明写「**可缺**——缺了速查表退回命令名本身」；生成侧 `scripts/build-help.mjs:311` 的 `const wake = REPR[k] || k;` 就是那一步。
5. `src/triggers/scene-09-photo.ts`：写入侧流程句**保留老句式**，只把自造页名换成页面本身的叫法——
   - 记身材照三行（`:5/:6/:7`）：「看身材照向导」预检确认页 → **预检确认页**（仍点名 `calorie.view.photo-log-wizard` 与该步的确认语义）；
   - 删身材照与标签三行（`:11﹣:14`）：「选身材照」候选页 → **候选页**（仍点名 `calorie.view.photo-picker`）；删照那行的「先列候选→快照确认→回执」三步一字未动。
   - 该文件九词计数：改前 14 处命中（看身材照向导 6＋选身材照 8）→ **改后 0**。
6. `workflows/09-身材照片.md`：`10 个场景索引`表的第一列子功能括注、逐词工作流、过程型页入口一览三处，把自造页名换成 `预检确认页／GIF 规划页／候选页`；`看身材照HELP` → `卡路里HELP`；并新增一节「老技能对账：场景 09 的唤醒词只有 8 个」把词表钉住。该件九词计数：改前 20 处 → **改后 0**；`卡路里HELP` **2** 处。
7. `package.json` 的 `files` 追加 **`"workflows/*.md"`**（让 `SKILL.md` 的指针在装机态不断链）。
8. `SKILL.md`：AUTO 块经 `pnpm help:build` 生成（未手改）；手写区**只在场景 09 那一段**加了一行「入口词以老技能为准（8 个）…三个流程内页没有唤醒词…HELP 那个入口是「卡路里HELP」」。
9. 连带同步：`test/wizard-86.test.mjs`（`:72/:73` 两条自造词不再当入口词断言，改断「两个流程内页命令仍在册且已无代表唤醒词」）、`test/skill-md-photo-285.test.mjs:97`（改断「速查表该列已退回命令名」）、`docs/.../t400-融合基准.md`（十条形态表第一列换成老词＋A1 清单与三处分区标题的相关行）、`t285-工作流落点.md`、`t283-候选与快照.md`。
10. 生成侧副产物 `scripts/build-help.mjs` 的 **`REPR` 标记块**由 `pnpm gen` 重写（2 处改值＋3 处删行），**未手改**；`keys.ts`／`registry.ts`／`combos.yaml`／`routes.generated.ts` 四件与改动前逐字相同（本票不动路由，故不变）。

---

## 三、机器证据

### 3.1 生成链（六步全持锁，单行留痕取自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=85e6379d-1e4a-4a00-a0f4-234d8aa68c18 cmd=pnpm-build  exit=0
GATE-RUN runId=7fe55da2-a4bd-4902-a229-524519c19ced cmd=pnpm-gen    exit=0  waitedMs=70071（等 #440 释放）
GATE-RUN runId=77ea67ca-28b6-4980-8f2b-4379a64666ea cmd=pnpm-build  exit=0
GATE-RUN runId=f20bf579-9cd0-44df-ab03-a5e99e8052b5 cmd=pnpm-help:build exit=0
GATE-RUN runId=63c48a8a-c144-452d-a73a-437fa47a6e63 cmd=pnpm-gen:check    exit=0
GATE-RUN runId=5f352c12-7069-44cf-a1c5-65b3e7b8d1c9 cmd=pnpm-help:examples:check exit=1（预置红，见 §六）
```

`pnpm gen:check` 的生成物读数（**5 件**，逐件 `GEN-CHECK ok`）：
`keys.ts` sha256=1e23dc43… ／ `registry.ts` sha256=5c81bfbb… ／ `combos.yaml` sha256=e7a077c4… ／ `scripts/build-help.mjs` sha256=0c429707… ／ `triggers/routes.generated.ts` sha256=74707c9c…；末行 `GEN-CHECK PASS：键 119（写 46 ＋ 读 73）；能力 10 个…未搬迁清单 0 条`。
第 6 个生成物是 `SKILL.md`（由 `pnpm help:build` 注入），其新鲜度由 `help:examples:check` 的第①条（AUTO 块 == 生成器输出）盯——该条当前因 §六 的预置红而整体不可判。

**收口席（#450）在同一窗口复跑同一六步**（每步持锁；`GATE-RUN` 单行取自导出件 `docs/skills/skill-calorie/t450-gate-runs.log`——它是 `.scratch/locks/gate-runs.log` 里本窗口 10 次运行的**全文导出**，§2.3 第 4 条要求的可追踪对账源）：

```
GATE-RUN runId=e0a3fb90-4b76-4ac1-aa47-a721d149ba10 cmd=pnpm-build                    exit=0
GATE-RUN runId=6bee6520-19df-4751-88dd-8aca89440724 cmd=pnpm-gen                      exit=0
GATE-RUN runId=8b2e27e3-9a3b-4885-bbcc-0b068130688d cmd=pnpm-build                    exit=0
GATE-RUN runId=73db8f14-209c-482c-a20b-95a8ad4585eb cmd=pnpm-help:build               exit=0
GATE-RUN runId=f869fe7d-1b4d-44eb-8a87-299d94507905 cmd=pnpm-gen:check                exit=0
GATE-RUN runId=a680c969-d1b4-4252-9855-30a67c3dafc2 cmd=node --test（照片域 7 件）     exit=0
GATE-RUN runId=dc50f1af-5abe-4409-9c7a-27f8c53471fb cmd=node --test（receipt-docs-282）exit=0
GATE-RUN runId=8b941c13-39e8-459e-89c2-8753ea06aa36 cmd=node --test（calorie-routing-81）exit=1（他席三红，见 §3.2）
GATE-RUN runId=b115122e-1795-41a8-8a75-599d3719e994 cmd=pnpm-help:examples:check     exit=1（预置红，见 §六）
GATE-RUN runId=c9f67a6b-49f3-4b83-a70d-7fbfcc1aac2d cmd=pwsh -File .scratch/t450/probe-line72.ps1（守卫探针） exit=0
```

收口席复跑的生成物读数与实施席**逐字相同**（`GEN-CHECK ok` 5 件 ＋ `GEN-CHECK PASS：键 119…未搬迁清单 0 条`）：`keys.ts` sha256=1e23dc43… ／ `registry.ts` 5c81bfbb… ／ `combos.yaml` e7a077c4… ／ `build-help.mjs` 0c429707… ／ `routes.generated.ts` 74707c9c…。**6 件生成物里前 5 件由 `gen:check` 当面复核，第 6 件 `SKILL.md` 由 `help:build` 当场注入成功**（日志末行「HELP 已注入：D:\ilife\packages\skill-calorie\SKILL.md」）；其新鲜度条仍受 §六 预置红牵连、不可判。

### 3.2 靶向测试读数

| 测试 | 读数 |
|---|---|
| `calorie-routing-81` | tests 8／pass 5／**fail 3**——三条红逐条归因见下 |
| `wizard-86` | 并入下组 |
| `skill-md-photo-285` | 并入下组 |
| `photo-picker-283` | 并入下组 |
| `photo-help-flow-345` | 并入下组 |
| `photo-shape-341` | 并入下组 |
| `photo-budget-438` | 并入下组 |
| `photo-gif-page-352` | 并入下组 |
| 上组七件合跑 | **tests 40／pass 40／fail 0，exit=0** |

**终态复跑**（还原之后、生成链收口之后，八件合跑一次）：**tests 48／pass 45／fail 3，exit=1**——三条红即下表那三条，别无其它。

`calorie-routing-81` 的三条红（都是**他席在途件**，与本票写集无关，且与 `docs/skills/skill-calorie/t283-候选与快照.md:28` 已记的「r81 余三红」逐字同名）：
1. `D2④ …：#13 拍营养表记一餐 命令字段非命令形态时路由层应判 non-exec`（饮食侧唤醒词翻 exec 未登记）；
2. `FX-81-5 …：看有备注的饮食记录 exit 应为 0（实测 4）`；
3. `A7 明确不做桶…`：实际桶少 `拍营养表补记一餐`／`拍营养表记一餐` 两条。
**本票相关的计数一条没变**：`D2⑤` 那条（`NEW_KEY_ROUTES.length == 69`／唤醒词路由合计 `506`／`EXEC_ROUTES.length == 419`）**pass**——因为路由表一行未动。故 r81 计数无需同步。

`photo-help-flow-345` 与 #345 的断言面：**不受影响**，`⑤ 变异自证` 全绿（用例 5／5）。它的变异正则 `/动笔前请先出.*我确认后再写入。/` 在换了页名之后仍然命中现在这句「动笔前请先出预检确认页(`…photo-log-wizard`)给我看(…)，我确认后再写入。」，故未改该测试。

**收口席复跑（改了 `SKILL.md:72` 与 7 处旧名引用之后重跑，`GATE-RUN` 见 §3.1）**：

| 命令 | 读数 |
|---|---|
| `node --test` 照片域 7 件（`wizard-86`／`skill-md-photo-285`／`photo-picker-283`／`photo-help-flow-345`／`photo-shape-341`／`photo-budget-438`／`photo-gif-page-352`） | **tests 40／pass 40／fail 0，exit=0**（与实施席同读数） |
| `node --test packages/skill-calorie/test/photo-receipt-docs-282.test.mjs` | **tests 10／pass 10／fail 0，exit=0** |
| 上两行合起来＝照片域／向导 **8 件 50/50 全绿** | — |
| `node --test test/calorie-routing-81.test.mjs` | **tests 8／pass 5／fail 3，exit=1**；三条红逐条点名与 §3.2 表**同名**：`D2④ 与冻结表逐条对齐…`／`FX-81-5 不变量：exec ⟺ 实跑 exit 0…`／`A7 明确不做桶…`（A7 的 `actual` 正是少 `拍营养表补记一餐`／`拍营养表记一餐` 两条）。**没有多出第四条** |
| `pnpm help:examples:check` | **exit=1**，末行 `RESULT: 0/0` ＋ `STRUCT 缺示例行：…`（预置红，#447，见 §六） |

### 3.3 字面对账（`Select-String`）

**收口席（#450）复算的逐词计数表**（每词一行，`Select-String -SimpleMatch`）：

| 自造词 | `packages/skill-calorie/SKILL.md` | `packages/skill-calorie/workflows/09-身材照片.md` |
|---|---|---|
| 移除身材照 | 0 | 0 |
| 设置照片标签 | 0 | 0 |
| 查身材照详情 | 0 | 0 |
| 对比身材照 | 0 | 0 |
| 做身材照GIF | 0 | 0 |
| 看身材照HELP | 0 | 0 |
| 看身材照向导 | 0 | 0 |
| 看GIF规划器 | 0 | 0 |
| 选身材照 | **0**（改前 **1**） | 0 |
| **九词合计** | **0**（改前 **1**） | **0** |
| `卡路里HELP` | 6 | 2 |

`SKILL.md` 改前那 1 处残留＝`:72` 手写区「verify 前置」段。**收口席按交办把它改成老词口径**：
`` `calorie.view.photo-picker`（选身材照；删身材照与标签三条的候选快照） `` → `` `calorie.view.photo-picker`（删身材照与标签三条；候选快照） ``——该段是「哪个命令服务哪条流程」的说明，删照流程该写老词 `删身材照`，不该写自造页名；后半个「页面内容」义项保留（`候选快照`）。**只动这一行**：AUTO 块与别的手写段一字未动，`pnpm help:build` 之后再复算仍是 0。

### 3.4 变异两行机器读数（判据⑤；实施席读数，收口席未复跑）

- **改坏必红**：把 `calorie.view.photo-picker` 的代表唤醒词改回自造词 `选身材照`，持锁跑 `pnpm build → pnpm gen → pnpm help:build`（三步 exit=0），然后
  `node --test packages/skill-calorie/test/skill-md-photo-285.test.mjs` → **tests 6／pass 5／fail 1，exit=1**，红在 `⑤ verify 映射含第三键 photo-picker`（"SKILL.md 速查表里候选页的代表唤醒词列应已退回命令名（仍是自造词即红）"）；同刻 `SKILL.md` 的 `选身材照` 计数 1 → **2**。
- **改回必绿**：把 `src/photo/commands.ts` 还原（sha256 回到 `9CFDC125F5ED967E71CC0E848230275C23F9C068513E09380CA1726223D7C527`；还原后 `pnpm gen` 曾报一次 `GEN-PAIR FAIL`——`Copy-Item` 保时间戳让 `tsc -b` 跳过重编，`touch` 源文件后复跑即好，已按仓规处置），持锁跑 `pnpm build → pnpm gen → pnpm build → pnpm help:build → pnpm gen:check`（五步全 exit=0），
  `node --test packages/skill-calorie/test/skill-md-photo-285.test.mjs` → **tests 6／pass 6／fail 0**；`SKILL.md` 的 `选身材照` 计数回到 **1**。

### 3.5 另一条变异（`files` 项）的读数：**当前无守卫**

把 `package.json` 的 `"workflows/*.md"` 挪掉，没有任何一条现有判据会红——`tooling/check-publish.mjs` 只按 `templates/` 做安装态清单断言（`:229-239`），没有对 `workflows/` 的同类断言。故这一项目前是**纸面保证**；本票按票面要求做了（`files` 已含 `"workflows/*.md"`），守卫另见文末遗留。

### 3.6 收口席守卫探针：`SKILL.md:72` 那处**无机器守卫**

探针脚本 `.scratch/t450/probe-line72.ps1`，**一个持锁窗口内完成**（`GATE-RUN runId=c9f67a6b-…`，见 §3.1）：先把 `:72` 改回自造词（`（选身材照；删身材照与标签三条的候选快照）`），直接调 `node --test`（自己已持锁，按 §2.4 第 5 条直调、不套第二层包装器），`finally` 里逐字节还原。

- `PROBE-MUTATED 九词命中=1（期望 1）`
- `PROBE-TEST exit=0`，`ℹ tests 40／pass 40／fail 0`——**七件无一变红**：即「九词在 `SKILL.md`／`workflows/09` 计数为 0」这条判据**目前没有任何测试守着**（`skill-md-photo-285` 只断言 AUTO 表那行与披露文件在册，不断言手写区的词表）。
- `PROBE-RESTORED sha256一致=True（3309A309D56335DF6BFEEAE29BDB0F991B6CFC49C404E84ABD11CF317C514BB2）九词命中=0`——还原后与探针前逐字节相同。

---

## 四、逐条路径（写集全清单）

| 件 | 改动 | 状态 |
|---|---|---|
| `packages/skill-calorie/src/photo/commands.ts` | 4 条代表唤醒词（改 2／去 3／留 1）＋头注 | 已改 |
| `packages/skill-calorie/src/triggers/scene-09-photo.ts` | 7 行流程句的页名 | 已改 |
| `packages/skill-calorie/workflows/09-身材照片.md` | 索引表＋逐词＋入口一览＋新增对账节 | 已改 |
| `packages/skill-calorie/package.json` | `files` 加 `workflows/*.md` | 已改 |
| `packages/skill-calorie/SKILL.md` | AUTO 块（生成）＋场景 09 段一行（手写） | 已改 |
| `packages/skill-calorie/scripts/build-help.mjs` | `REPR` 标记块（`pnpm gen` 重写） | 生成侧副产物 |
| `packages/skill-calorie/test/wizard-86.test.mjs` | `:72/:73` 断言面同步 | 已改 |
| `packages/skill-calorie/test/skill-md-photo-285.test.mjs` | `:97` 断言面同步 | 已改 |
| `docs/skills/skill-calorie/t400-融合基准.md` | 十条形态表第一列＋A1 清单＋三处标题 | 已改 |
| `docs/skills/skill-calorie/t285-工作流落点.md` | 别名覆盖段＋三处基准对照行 | 已改 |
| `docs/skills/skill-calorie/t283-候选与快照.md` | 十条形态表对照行 | 已改 |
| `docs/skills/skill-calorie/t450-词表订正.md` | 本件（原 `docs/skills/skill-calorie/t159-词表订正.md`，收口席按票号改名） | 新建＋已改名 |
| `docs/skills/skill-calorie/t450-gate-runs.log` | 本窗口 10 次运行条目的导出（§2.3 第 4 条对账源） | 收口席新建 |

**收口席（#450）改到的件**（逐条；除第一行外都是**只换证据件路径**的定点替换，语义一字未动）：

| 件 | 改动 | 机器读数 |
|---|---|---|
| `packages/skill-calorie/SKILL.md` | `:72` 一处：`` （选身材照；删身材照与标签三条的候选快照） `` → `` （删身材照与标签三条；候选快照） ``；AUTO 块由 `pnpm help:build` 重写（未手进） | 九词 **1 → 0**（§3.3） |
| `packages/skill-calorie/src/photo/commands.ts` | `:12` 头注路径 `t159-` → `t450-`（代码行零改动） | 5 件生成物 sha256 逐件不变（§3.1） |
| `packages/skill-calorie/workflows/09-身材照片.md` | `:97` 路径 | 九词 0／`卡路里HELP` 2 |
| `packages/skill-calorie/test/wizard-86.test.mjs` | `:71` 注释内路径 | 七件 40/40 绿（§3.2） |
| `packages/skill-calorie/test/skill-md-photo-285.test.mjs` | `:98` 注释内路径；`:96` 的 `photo-picker` 断言**原样保留** | 同上 |
| `docs/skills/skill-calorie/t400-融合基准.md` | `:160` 路径 | 文档面，无门 |
| `docs/skills/skill-calorie/t285-工作流落点.md` | `:31` 路径 | 同上 |
| `docs/skills/skill-calorie/t283-候选与快照.md` | `:19` 路径 | 同上 |

改名后旧路径解不到，故上述 7 处**一并同步**（这 7 件都在本轮写集内；每处只换路径字符，别的一字未动，若审查席不认可可逐处回退）。

历史证据件 `t313-*`／`t156-*`／`t282-*`／`t314-*`／`t345-*`／`t352-*` 一律未改。`t283`／`t285` 里描述**当时施工写集与生成物**的段落（`t283:6/48/59/64/68`、`t285:56`）按「历史记录不改」保留，其中的「选身材照」字样属当时事实，不是本轮的入口词声明（收口席只动了这两件里**本轮新增的那条路径引用**：`t283:19`／`t285:31`）。

---

## 五、判据逐条对账

| 判据 | 读数 |
|---|---|
| ① 生成物全 `GEN-CHECK ok` | 5 件逐件 ok，末行 PASS；第 6 件 `SKILL.md` 由 `help:build` 当场注入成功，其新鲜度条受 §六 预置红牵连、不可判 |
| ② `pnpm help:examples:check` ok | **未达成，且是预置红**——见 §六（收口席复跑读数同实施席：exit=1／`RESULT: 0/0`） |
| ③ 八条靶向测试全绿 | 照片域／向导 **8 件 50/50 绿**（收口席复跑：七件 40/40 ＋ `receipt-docs-282` 10/10，均 exit=0）；`calorie-routing-81` tests 8／pass 5／fail 3，三条红逐条点名与实施席**同名**、**无第四条**（§3.2） |
| ④ 字面对账 | 收口席复算：`SKILL.md` 九词 **0**（改前 1）；`workflows/09` **0**；`卡路里HELP` `SKILL.md` 6／工作流 2 |
| ⑤ 改坏必红／改回必绿 | 两行齐全，见 §3.4（**实施席**读数；收口席未复跑这一对） |
| ⑥ 六步生成链（**收口席**复跑） | `pnpm build`／`gen`／`build`／`help:build`／`gen:check` 五步全 exit=0；两组测试 exit=0；`r81` exit=1（允许的三红）；`help:examples:check` exit=1（预置红） |
| ⑦ 收口席守卫探针 | `SKILL.md:72` 那处**无机器守卫**（§3.6）——判据②目前是纸面保证 |

---

## 六、未做项与下一手

1. **`pnpm help:examples:check` 全仓预置红（不是本票改出来的）**。
   - 读数：`RESULT: 0/0`，`STRUCT 示例行数 0 != 组合键数 119`＋119 条「缺示例行」，exit=1。
   - 归因：`packages/skill-calorie/scripts/check-examples.mjs:58` 的 `const [, wake, key, shape, rawCmd] = cells;` 停在 **4 列**时代；`SKILL.md` 的表自 #338（`ac5cfa4`）起有 **5 列**（唤醒词／命令／形状／流程／例），`rawCmd` 落在空的「流程」列 → `startsWith('calorie-cmd-read ')` 恒假 → 所有行被跳过。
   - 与改动无关的证明：复算探针 `.scratch/t159-parse-probe.mjs` —— `HEAD` 版 `SKILL.md` 解析出的示例行 = **0**（工作区也是 0）；按 5 列复算，`HEAD` = **119**、工作区 = **119**；AUTO 表列数集合 `[7]`。
   - **已开票：`#447`**（票面写了判据、变异要求、不许放宽断言）。
2. **路由表里的 9 条自造词未清**（本票硬边界：不许删 `src/photo/routes.ts` 的行、不许改任何 `order`）。
   - 残留位置：`src/photo/routes.ts:22/23/25/26/27/28/29/30/31`（各一行，new 列表 order 1／2／14／15／16／17／54／55／68），派生件 `src/triggers/routes.generated.ts:453/454/466/467/468/469/506/507/520`。
   - **已开票：`#446`**，票面写清三条前置（先冻结其它线／必须重排序号／必须同步 `calorie-routing-81` 计数与 `docs/research/t81-exec-smoke.md`）。
3. **`SKILL.md:72` 那处残留——收口席已清**：改成老词口径 `（删身材照与标签三条；候选快照）`，九词在 `SKILL.md` 归 **0**（§3.3）；`test/skill-md-photo-285.test.mjs:96` 的 `calorie.view.photo-picker` 断言**原样保留**、复跑全绿。**但它带出一条遗留**：「九词计数为 0」这条判据**没有机器守卫**——§3.6 的探针把它改回自造词后，七件仍 40/40 绿。下一手若加守卫，加在 `skill-md-photo-285` 里（断言手写区与 `workflows/09-身材照片.md` 不含那九个词即可）。
4. **`package.json` 的 `workflows/*.md` 目前无机器守卫**（§3.5 读数）：`tooling/check-publish.mjs` 未对 `workflows/` 做安装态清单断言。建议并入 **`#447`** 或另开一张小票。
5. `calorie.photo.detail` 的代表唤醒词仍是 `查身材照`，而路由表里 `查身材照`（`src/photo/routes.ts:253`）指向的是 `calorie.photo.list`——**不满足「代表词回同键」**。本票未改（改它要动路由表，属 `#446` 范围），登记在此。
6. **`#446` 票面正文仍写旧路径**（「来源：#159 的唤醒词词表订正（`docs/skills/skill-calorie/t159-词表订正.md`）」）：改名后那句解不到新名。按「已发出的结论不回头改」，收口席**未改 #446 正文、也未加评论**，留给编排者定（一条评论写清改名即可）。
7. **`docs/research/t81-exec-smoke.md:514` 仍写 `选身材照`**：那是 #283 时期的逐行快照（`docs/research/` 属历史归档、不再新增），与该件「唤醒词」列同源，**归 #446 的路由清理一并同步**，本票不动。
8. 本票**不做** git 提交、不 push（票面要求）。

---

## 七、守卫补强席（#450 第三段）：词表对账有机器守卫了

**背景**：§3.6 的探针证明「九词在 `SKILL.md`／`workflows/09` 计数为 0」这条判据没有测试守着（改回自造词后七件仍 40/40 绿），§六 第 3 条据此把它记为遗留。本节**取代**那两条的「无守卫」读数（那两条是当时事实，按「历史记录不改」保留原文）。

**加在哪**：`packages/skill-calorie/test/skill-md-photo-285.test.mjs`（本件全件即场景 09 面，无 `describe` 分段，故按既有编号风格续挂两条）。件头注明来源；新增常量子串表 `SELF_MADE_WORDS`（9 词）与 `OLD_ENTRY_WORD='卡路里HELP'`、计数器 `countAll`、两文档面 `docFaces()`。

- **⑦ 词表对账：自造入口词清零（9 词 × 2 件，逐词报出件名与次数）**＝`SKILL.md` 与 `workflows/09-身材照片.md` 全文里 9 词各 `assert.equal(n, 0)`；失败信息逐条带词、件路径、次数。末段附**计数器变异自证**（把 9 词逐词注入副本，须各报 1），打 `MUTATION-WORDLIST`。
- **⑧ 老词口径：`卡路里HELP` 在册（正向断言）**＝`SKILL.md` ≥2 次、工作流 ≥1 次（防「九词清零」被用「连老词一起删光」的方式做绿）。

**机器证据**（持锁运行；`GATE-RUN runId` 抄自 `.scratch/locks/gate-runs.log`，本条运行条目见本席报告）：

```
GATE-RUN runId=c14e83cf-c28e-4bfe-8793-fbbaf0015900 cmd=pwsh -NoProfile -File .scratch/t450/guard-mutation.ps1（改坏＋测试＋还原，一个持锁窗口） exit=0
```

| 步骤 | 命令（持锁） | 读数 |
|---|---|---|
| ① 先跑一次全绿 | `node --test …/skill-md-photo-285.test.mjs`（runId=85484dc2-5c8c-4d0d-84f3-32e223fdff18） | **tests 8／pass 8／fail 0，exit=0**；`MUTATION-WORDLIST ok（逐词注入后实测：移除身材照×1／设置照片标签×1／查身材照详情×1／对比身材照×1／做身材照GIF×1／看身材照HELP×1／看身材照向导×1／看GIF规划器×1／选身材照×1）`；`WORDLIST-OLD-ENTRY ok（SKILL.md=6，workflows/09-身材照片.md=2）` |
| ② 改坏必红 | 同窗口内：`SKILL.md:72` 手写区 `` （删身材照与标签三条；候选快照） `` → `` （选身材照；删身材照与标签三条的候选快照） ``，窗口内直调 `node --test …`（runId=c14e83cf-…） | 九词命中 **0 → 1**；`GUARD-TEST-MUTANT exit=1`，**tests 8／pass 7／fail 1**；失败原文：`AssertionError [ERR_ASSERTION]: 词表对账未过：自造入口词「选身材照」在 packages/skill-calorie/SKILL.md 出现 1 次（期望 0）`（`actual: 1`／`expected: 0`） |
| ③ 改回必绿 | 同窗口 `finally` 逐字节还原（`Copy-Item` 自备份）后复跑（runId=3a1d2f1a-a5ea-4415-8593-fb252ad7936a） | `GUARD-SHA-BEFORE`＝`GUARD-SHA-AFTER`＝**3309A309D56335DF6BFEEAE29BDB0F991B6CFC49C404E84ABD11CF317C514BB2**，`GUARD-RESTORED 一致=True 九词命中=0`；复跑 **tests 8／pass 8／fail 0，exit=0** |
| ④ 互不干扰 | `node --test …/skill-md-photo-285.test.mjs`（runId=3a1d2f1a-…）与 `node --test …/photo-help-flow-345.test.mjs`（runId=0184f6d4-5786-41a9-8a05-1bf032db2b85）各自单跑，再两件合跑（runId=131de5a9-b7ed-40eb-9a4f-038c388b3532） | 单跑 8/8 exit=0、5/5 exit=0；合跑 **tests 13／pass 13／fail 0，exit=0** |
| ⑤ 照片域七件复跑（§3.2 那组的同一组） | runId=7cf940c8-17cf-42da-a73c-878874098967（`waitedMs=20010`，等 #448 释放锁） | **tests 42／pass 42／fail 0，exit=0**（较 §3.2 的 40/40 多出的 2 条＝本轮新增 ⑦／⑧） |

**写集**：只改 `packages/skill-calorie/test/skill-md-photo-285.test.mjs`（新增 ⑦／⑧ ＋ 件头 4 行注）与本证据件本节。**未碰**源码／`src/photo/routes.ts`／他席在途件；真库只读；不做 git 写操作。日志件（未受版本控制）：`.scratch/t450/21-guard-baseline.log`、`22-guard-mutant.log`、`23-guard-mutation-window.log`、`24-guard-post-restore-285.log`、`25-guard-flow-345.log`、`26-guard-both-together.log`、`27-guard-photo7.log`、`.scratch/t450/guard-mutation.ps1`。

**#446 评论**：已发一条（`https://github.com/FeatherHunter/ilife/issues/446#issuecomment-5664863503`，id=5664863503）：说明本件按票号改名（正文若引旧名以评论为准）、`docs/research/t81-exec-smoke.md:514` 仍写「选身材照」随该票重生成快照时同步、九词现有 ⑦／⑧ 守卫。**未改 #446 正文**。

**仍未被本节覆盖的口子**（不属本席写集）：`src/photo/routes.ts` 那 9 条自造词仍在（§六 第 2 条，#446 范围）；§3.5 的 `package.json` `files` 项仍无守卫。

---

## 八、落地席（第四段）：重跑全绿；生成物缺的两件派生面经编排裁定由本票代带（B 快路径）

**重跑（持锁 `runId=05e4c797-a048-4e17-b081-0515e7a66e27`；运行条目见 `t450-gate-runs.log` 末段，逐步日志 `.scratch/t450/31..36-regen-*.log`）**：`pnpm gen`／`pnpm build`／`pnpm help:build`／`pnpm gen:check` 四步 exit=0（末步 `GEN-CHECK PASS：键 127`）；靶向八件 **tests 52／pass 52／fail 0**（含本节 ⑦／⑧ 守卫）；九词在 `SKILL.md`／`workflows/09` 复算仍为 **0／0**，`卡路里HELP` **6／2**。`calorie-routing-81` exit=1，**四红全属他席在途、无第五条**：`D2①`（#384 八键 non-exec→exec，实测 exec 427／non-exec 9）／`D2④`（拍营养表记一餐 intent 未登记）／`FX-81-5`（exec 桶记录数 497≠489，同为 #384 的 ＋8）／`A7`（桶少 `拍营养表补记一餐`／`拍营养表记一餐`）。

**落地受阻（如实登记）**：`a18a289`（#384 席的派生面补提）已把生成物里与本票相关的**三件带上车**——`src/cli/keys.ts`、`src/triggers/routes.generated.ts`、`SKILL.md`；其中 `SKILL.md` **含本票全部改动**（`卡路里HELP`／`生成身材照GIF` 两行＋三个流程内页退回裸键名＋`:72` 手写区老词口径），该提交正文已如实登记夹带。`src/cli/registry.ts` 本就无那八键内容（故无 diff）。**仍缺两个派生面**：`packages/base-combos/combos.yaml`（+40 行＝`calorie.report.*` 八键各 5 行，属本票的行 **0**）与 `scripts/build-help.mjs`（+18/-5：**+16 行属 #384** 的八键 `REPR`／`EXAMPLES`，-5/+2 行属本票）。⇒ 本票落地集的生成物 diff **必然含 #384 八键新增量**，与落地票面「生成物 diff 只含场景 09 增量」的硬闸冲突。落地席据此**先停手上报**（未自行 `add`／未 commit／未 push）；**经编排（总工程师）裁定走 B 快路径**：由本票这一笔代带上述两件派生面落库，提交正文如实登记夹带——理由＝#384 的声明面已随 `a18a289` 入库，缺的只是派生面（属「落库半成品」），代带落库正好修掉 fresh-clone 的 `gen:check` 脏。提交 sha／`git show --name-only`／push 读数见落地席报告。

**本节读数替代关系**：§四／§六 里「`SKILL.md` 待落盘」的表述已被本节的 HEAD 实况取代（旧文按「历史记录不改」保留）。
