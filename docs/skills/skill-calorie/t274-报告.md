# t274 · 食品库类页 4 条 —— 交付报告

> 票 #274（地图 #155）。强制对照基准 `docs/skills/skill-calorie/t425-融合基准.md`；过程纪律
> `t155-派单/18-融合标准与过程纪律.md`；老实物 `D:\2Study\StudyNotes\SKILLS\卡路里\templates\`（只读，未执行其中任何脚本）。
> 本票页面源码已在 `1ee8141`（三页）与 `ca52b5d`（来源统计姊妹件）落盘；本窗＝按票面四条判据逐条复核 ＋ 补齐声明缺件。

## 一、判定

**PASS。四条判据逐条为真。**

| 判据 | 真/假 | 读数 |
|---|---|---|
| ① 四条词真出口 exit 0、产物是完整文档 | **真** | 8/8 词 exit 0；四件产物 `<!doctype html>` 在第 0 字节、含 charset／样式段／`ilife-page`、零未替换标记。绝对路径与字节数见 §三 |
| ② 内容与字段按老实物重做 | **真**（3 处有意没对上 ＋ 1 处不适用） | 三张老实物逐块对账见下表 |
| ③ 结构化断言、且能红 | **真** | `test/t274-食品库页.test.mjs` 11 条；`ℹ tests 11 / pass 11 / fail 0` |
| ④ 变异自证 | **真** | `fixed0(it.calories)`→`fixed1`：`pass 9 / fail 2`（红＝查食品、食品库两条）→ 逐文件点名还原 → `pass 11 / fail 0`；`源码回到基线=true dist回到基线=true polluted=[false]` |

### 四条词的原文与命令（路由实测，逐字抄 `src/diet/routes.ts`）

票面点名的四条＝`查食品`／`搜食品`／`查食品（按分类）`／`看去重报告`（search 占两条、未含来源统计）。按
「以 `scene:'02'` 的实际路由为准」数一遍：本票**四条命令共 8 条唤醒词**，全部实跑。

| 唤醒词（原文） | 命令 | 路由 `cli` 原文 |
|---|---|---|
| 查食品 | `calorie.view.search` | `calorie-cmd-read calorie.view.search --params '{"keyword":"鸡胸"}'` |
| 搜食品 | `calorie.view.search` | 同上 |
| 查食品（按分类） | `calorie.view.library` | `calorie-cmd-read calorie.view.library --params '{"category":"蛋白类"}'` |
| 查食品库 | `calorie.view.library` | `calorie-cmd-read calorie.view.library` |
| 看食品库（去重） | `calorie.view.dedupe` | `calorie-cmd-read calorie.view.dedupe` |
| 看去重报告 | `calorie.view.dedupe` | 同上 |
| 看食品来源统计 | `calorie.view.source-stats` | `calorie-cmd-read calorie.view.source-stats` |
| 看食品来源分布 | `calorie.view.source-stats` | 同上 |

### 逐块对老实物

| 老实物块 | 新页落点 | 判定 | 读数 |
|---|---|---|---|
| `food_search.html:7` `<title>`／`:66` h1 `🍱 食物热量查询` | `libraryDocs.ts` `DOC_TITLE_SEARCH`／`title` | 对上 | 逐字同 |
| `:67` `.sub`「查询X · 匹配 N 条」 | 结论句「在食品库里搜「鸡胸」，找到 1 条。」 | 对上（换槽位） | 裁定 2；「搜索：鸡胸」按裁定 11 移到读数卡副行 |
| `:70-73` `.search-box` 输入框＋占位提示／`.count` | `renderParamForm` ＋ 读数卡「匹配 1 条·搜索：鸡胸」 | 对上 | 提示逐字「输入食物关键词，如 牛肉 / 鸡胸 / 可乐」 |
| `:31-40` `.food-grid`／`.food-card`／`.cat`／`.name`／`.brand`／`.macros` 四格／`.source` | `renderFoodGrid` 的 `food-grid`／`food-card`／`food-cat`／`food-name`／`food-brand`／`food-macros`／`food-src` | 对上 | 类名是页面局部（非公共层），结构逐格对得上；四宏量口径 165 卡／31.0 g 逐字同 |
| `:117` 缺品牌印 `—` 占位 | 缺品牌不出该行 | **没对上（有意）** | #511 第 74 条：占位破折号夹在中间读者不知是哪一格 |
| `:78` `.footer .src`「数据源:calorie_data.db · nutrition_products」 | 脚注「📊 数据来源：本机食品库 · 在架食品共 N 条」 | 没对上（有意） | #496 去库表名（裁定 1）＋裁定 3 恒出 |
| `:99` 空态 `emptyState(🔍)` | `emptyGuide`（图标＋原因＋引导句） | 对上（2026-09-15 整改后） | 原先零命中那一支在取数层就被 `exit 4` 挡掉、这一格真跑不出来，登「对上」不成立（#274 收口复核 §四 缺陷 1／2）；整改后「库非空、本次零命中」出完整页走这一格：空态块 `ilife-block-empty` ＋ 空态句「没有找到匹配的食品」＋ 引导句「…说「存食品」就能加进第一条。」；「整个库为空」仍 `exit 4`、不落盘 |
| `:136-149` 客户端实时过滤 | 无 | **不适用** | 本仓一命令一页、产物是静态文件（同裁定 8 口径，只登记） |
| `dedupe_report.html:61` h1 `📦 食品库去重`／`:100,103` `.banner` 两态 | `title`／`.dedupe-banner` | 对上 | 「✅ 食品库无重复，数据干净」逐字同；warn 态「冗余记录」→「可合并的重复食品」（#496 人话） |
| `:66-69` `.kpi-grid` 重复组／冗余行／库内食品 | `renderKpiGrid` 重复组／可合并的重复食品／库内食品 | 对上 | 中间一格按 #496 改名 |
| `:74-79` 重复组表 食品名·品牌·条数·ID | `renderDataTable` 食品名·品牌·条数·记录编号 | 对上 | `ID`→`记录编号`（#496） |
| `:114-116` `.advice` 处理建议 | `renderListRows` 两行 | 对上 | 「标废弃」→「下架」；拆两行 |
| `:84` `.footer .src`「product_name+brand 分组」 | 口径行「判定标准：名称和品牌都一样的算重复…」 | 对上（换槽位） | 去源码词，语义同 |
| `source_stats.html:61` h1 `📦 食品来源统计` | `title: '📦 食品来源统计'` | 对上 | 逐字同 |
| `:7` `<title>卡路里 · 食品来源统计` | `卡路里·饮食` | **没对上（有意）** | 饮食族页共用族名；`t511-清尾二`／`t276` 两件别票测试钉住该族名，按裁定 11 不越界改 |
| `:64-67` `.kpi-grid` 来源数／食品总数 | `renderKpiGrid` 两枚 | 对上 | 另加副行「数据范围：全部在架食品」（去 `nutrition_products` 表名） |
| `:92-99` `.bar-row` 按来源分组占比条 | 公共层柱图「按来源分组」＋来源明细表（来源／条数／占比%） | 对上（换图元） | 占比信息进表列；条图由 `renderChartBlock` 出（裁定 5 可读数由公共层承载） |
| `:101` `.footer .src`「source 分组 · 日期」 | 脚注＋表题「按来源统计（已下架的食品不算…）」 | 对上（换槽位） | 去源码词 |

**另两处有意偏离**（`t425` §六 老页缺陷不照抄）：三张老实物页脚都留着按钮组样式、正文里没有按钮（新走
公共层复制区恒双按钮，无点不动的死按钮）；老页数据直插未转义（新走公共层五字符转义，测试有直测）。

### 十一条裁定

| # | 判定 | 读数 |
|---|---|---|
| 1 | 对上 | 四页眉标＝`查食品 · 饮食`／`查食品库 · 饮食`／`看食品库（去重） · 饮食`／`看食品来源统计 · 饮食`，徽章恒`食品库`；可见文本零 `calorie.view`／`nutrition_products`／`null` 等（测试逐条断言） |
| 2 | 对上 | 结论句紧跟标题且含本页读数，四页各一句（`标题 < <p class="sub"> < 首个区块` 有断言） |
| 2-补 | 对上 | 结论句走 `summary`（`<p class="sub">`）、脚注走 `renderCaliberLine`，四页均不走深底块 |
| 3 | 对上 | 导航／口径行／来源脚注三条四页恒出，全走公共层；`shared/sourceLine.ts` 要起止日期，本类无窗口故未取用 |
| 4 | 对上 | 缺值一律 `—`；干净库（命令有、库非空、零重复）实测 exit 0＋完整页＋空态块＋空态句＋引导句；不动 `exit 4` 阻断态 |
| 5 | 不适用（公共层承载） | ⑤ 类只有来源统计一条柱图，由 `renderChartBlock` 出；查食品／食品库两页零图元 |
| 6 | 不适用 | 本类四枚读数都不是「对目标的完成度」，不出完成度条 |
| 7 | 对上 | 四页恒双按钮（复制数据／复制日志）、无同名标题；日志六段齐、第 4 段＝本次命令原文（含 `--params`），测试逐字比 |
| 8 | 只登记不实施 | 本类卡片是老实物自带的食品卡，不是「表格行转卡片」那一族；卡片格样式用冻结 token，局部断点只把四宏量改两列 |
| 9 | 对上 | 取数失败走 `ERR 4: 取数失败（缺失阻断）`（stdout 空、不落半页）；产物里 `error_receipt` 特有类名零命中 |
| 10 | 对上 | 查食品／食品库零 `<table>`（反向断言锁住）；去重页保留老实物那张重复组表 |
| 11 | 对上（命中一处） | `diet-homogeneity-108.test.mjs:215` 钉住「搜索：鸡胸」→ 不动别票的引脚，改用读数卡副行承载；来源统计 head 标题同理不动 |

## 二、机器证据

- 基准 `645aa37`；本票页面源码 `1ee8141`（三页）／`ca52b5d`（来源统计姊妹件，纯搬产物 sha256 逐字节同）；本窗 `de5e91b`（测试件改名＋真跑脚本），本报告与门禁导出为同窗第二条提交。
- `GATE-RUN` runId：`eeb2baff-8c27-400a-8387-1ccdd4aca172`（本票测试件 11/11，exit 0）；`eefb7768-f69b-4ea9-aecd-d82974a279ee`（全量有本票件名，exit 1）；`d9e5bb5e-176b-4b6f-9db1-7422b8240601`（退回旧件名，exit 1）；`f953dcd7-a63b-4aca-9fb6-c83c7b18ba14`（变异自证，exit 0）；`2479d057-8a27-4537-9975-6fdedce97deb`（提交，exit 0）。
- `RESULT:` 行：`8/8 条 exit 0 且产物是完整文档`；`RESULT-EMPTY: 8/8 条 exit 4 ＋ 缺失阻断`；`CLEAN calorie.view.dedupe exit=0 完整文档=yes 空态块=yes 空态句=yes 引导句=yes`；`RESULT 变异必红=true 还原必绿=true 源码回到基线=true dist回到基线=true`；`STEP dist-pollution-check polluted=[False]`。
- **不新增红（归因自证）**：本票改动在场／退回旧件名，两跑均 `tests 1160 / pass 1082 / fail 76`，红点名到的
  测试件集合 `33 = 33`，双向差集为空；全部 11 条 `#274` 测试在两跑里都绿。⇒ `ATTRIBUTION PASS`。
- `gen:check`：**红**，`routes.generated.ts` 与生成器输出不一致（第 462 行起，`查高热量排行` 的
  `calorie.view.ranking` 参数）——#272 的路由，非本票；生成物六件本票未写、未 `git add`。
- `check-warning-line.mjs`：**红**，5 条台账陈化全是别票件（`diet/nutritionPortDocs.ts` 675→645／
  `scripts/build-help.mjs` 504→386／`scripts/gen-photo-baseline.mjs` 429→283／`diet/receipt.ts` 350→239／
  `home/homeDocs.ts` 328→334）；本票 `diet/libraryDocs.ts` 台账行＝实况 `396`，不在陈化名单。

## 三、逐条路径

- 源码：`packages/skill-calorie/src/diet/libraryDocs.ts`（396 LF／19151 B／sha256 前 16 `7bae0a8054a49db2`，本票唯一作者）、`src/diet/library.ts`（65 行）、`src/diet/sourceStatsDocs.ts`（123 行）、`src/diet/libraryPlate.ts`（72 行，未动）。
- 测试：`packages/skill-calorie/test/t274-食品库页.test.mjs`（本窗由 `diet-library-t274.test.mjs` 改名，测试内容逐字不动）。
- 脚本：`docs/skills/skill-calorie/t274-真跑.mjs`；日志 `.scratch/t274/真跑.log`、`pkg-test-with-t274.log`、`pkg-test-without-t274.log`、`mutate-locked.log`、`gen-check.log`、`warning-line.log`。
- 证据件：本件、`t274-证据.md`、`t274-来源统计页搬迁.md`、`t274-gate-runs.md`；changeset `.changeset/t274-食品库类页.md`（`1ee8141`）与 `.changeset/t274-来源统计页搬迁.md`（`ca52b5d`）。
- 四件产物（播种库 5 条在架食品，`SKILLS_DB_PATH`＝临时目录）：
  | 唤醒词 | 命令 | 产物绝对路径（`<SKILLS_DB_PATH>/calorie_html/`下） | 字节 |
  |---|---|---|---|
  | 查食品／搜食品 | `calorie.view.search --params '{"keyword":"鸡胸"}'` | `查食品_20260915_192352.html`／`…_2.html` | 70354 |
  | 查食品（按分类） | `calorie.view.library --params '{"category":"蛋白类"}'` | `食品库_20260915_192352.html` | 70427 |
  | 查食品库 | `calorie.view.library` | `食品库_20260915_192352_2.html` | 76639 |
  | 看食品库（去重）／看去重报告 | `calorie.view.dedupe` | `去重报告_20260915_192351.html`／`…_2.html` | 70033 |
  | 看食品来源统计／看食品来源分布 | `calorie.view.source-stats` | `食品来源统计_20260915_192353.html`／`…_2.html` | 75821 |
  （绝对前缀 `C:\Users\辰辰洋洋\AppData\Local\Temp\t274-live-DpGxXB\calorie_html\`；干净库那件 `去重报告_…html` 68705 B。）

## 四、未做项与下一手缺什么

1. **`check-warning-line` 未跑 `--sync`**：它写 `packages/skill-calorie/AGENTS.md`，不在本票声明路径 ⇒ 按红线
   报编排者、本窗不碰。5 条陈化行都属别票，收口要一次全表同步。
2. **`diet/libraryDocs.ts` 396 LF 超 350 告警线**：已在台账挂号（挂号值 `—` 不回改，结论列写全超因与拆法）。
   拆法＝按「卡与样式」切姊妹件 `diet/libraryCard.ts`（`FOOD_CSS`／`DEDUPE_CSS`／`renderFoodGrid`／`fixed0`／
   `fixed1`），三页装配与页框留本件。**本窗未拆**：新建件要走编排者派单。
3. **`gen:check` 红**归编排者（生成物六件）与 #272（`calorie.view.ranking` 参数）；本票未写生成物。
4. **裁定 8 的公共层增量**（手机两段式卡片／餐别四色圆章／双形态表格）本图只登记不实施，本票同。
5. **老 `food_search.html` 的客户端实时过滤**未移植（静态产物、一命令一页）。
6. **视觉**：本票不做脚本化判定，四张产物留负责人肉眼两档（390px／1440px）。
7. **环境事实订正**：本检出的**包级 `node_modules` 联结缺失**（根 `node_modules` 联结在，但
   `packages/skill-calorie/node_modules/{base-paint→packages/base-render, base-link-core}` 没有）⇒
   `npx tsc -b packages/skill-calorie` 首跑 188 条 `Cannot find module 'base-paint'`。已按 `D:\ilife` 的形状补
   `base-render`／`base-combos`／六个 `skill-*` 的包级联结（不进版本控制）。**其他检出会同样踩到**，派单书
   「node_modules 已就绪」只对了根联结那一半。
8. **三处别票证据件仍写测试件旧名**（`t366-复核.md`／`t509-清尾-证据.md`／`t511-清尾二-证据.md`、
   `t425-融合基准.md:210`）：不属本票声明路径，未碰，仅报。
