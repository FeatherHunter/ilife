# #617 证据 · 预检确认页表格不再印 HTML 标签字面量

> 票：`gh issue view 617`（Part of #155，负责人验收缺陷③）。产出提交：`7085b8e5e7c3427b563ca51c01d0f69eb47157d7`（`7085b8e`，改动三件）／
> `dd3a355e2ab8d5878a55edec5285c5fb67b9ffac`（`dd3a355`，证据件＋运行记录导出＋入仓版探针）。
> 运行记录导出（协议 §2.3 第 4 条对账源）：`docs/skills/skill-calorie/t617-运行记录.md`。

## 一 · 判定

**PASS**。票面五段逐条落地：那一列不再印 HTML 源码字面量，改印读者话（「识别得到」／「要你核对」），
形状走现成徽章件；改前红／改后绿两行、变异红／还原绿两行读数齐；未越写集；未动 `base-render/**`、`precheckPort.ts`、`precheck.ts`、其它测试。

## 二 · 改了什么（票面「目标」）

| 处 | 改前 | 改后 |
|---|---|---|
| `src/diet/precheckLabel.ts` 那一列（`tag`）的值 | `'<span class="precheck-tag is-ai">识别得到</span>'`（不确定的格子再接一串 `is-check`） | 读者话：`识别得到`／`识别得到，要你核对` |
| 那一列的产物形状 | 单元格是**纯文本面**（公共层 `cellText` 一律转义）⇒ 整串源码被**原样印在页上**（负责人看到的「奇怪内容」） | 经 `renderDataTable` 的 `cellHtml` 受信位（#567）由**现成徽章件** `renderStatusBadge` 产出：中性灰 `empty`＝识别得到、橙 `warn`＝要你核对（档位色同老实物 `nutrition_label_wizard.html:214` 的 `.diff-tag`） |
| `src/diet/precheckParts.ts` | `PRECHECK_CSS` 里两条 `.precheck-tag` 规则 | 撤掉（改后无一处引用它，不留死规则）；`.precheck-note` 保留 |
| `test/t277-预检确认页.test.mjs` | 断旧类名 `precheck-tag is-check` | 断言随形状同改（数那一列里 `warn` 档徽章的格数）＋新增 #617 回归锁两条 |

**源码里已无 HTML 字面量**：单元格的值是读者话，结构由公共层徽章件产出——票面「不许把带 HTML 的字符串塞进单元格」按这一条落实；
「能用现成徽章件就用结构不用字符串」按 `cellHtml` ＋ `renderStatusBadge` 落实（未自造形状、未改公共层）。

## 三 · 验收命令逐条（票面「验收命令」）

### ① 编译

```
MUT-BUILD  tag=final exit=0
```
（`node node_modules/typescript/bin/tsc -b packages/skill-calorie --force`；GATE-RUN 见第四节。）

### ② 真出口产物断言（改前红／改后绿两行）

探针（随证据入仓，可直接复跑）：`docs/skills/skill-calorie/t617-探针-tag.mjs`——两条「拍营养表」唤醒词走真 cli，
加一例点名两格不确定；判 `visibleText()` 里的 `&lt;span`／`<span`／`precheck-tag` 命中数。
**同一个脚本、同一段判据，改前跑一次、改后跑一次**：

```
# 改前（缺陷在场）
T617 拍营养表记一餐      exit=0 bytes=84617 可见文本：&lt;=0 裸<span=8  precheck-tag=8  ｜ 结构：徽章=5  橙徽章=1 旧类标记=0  VERDICT=RED
T617 拍营养表补记一餐    exit=0 bytes=84828 可见文本：&lt;=0 裸<span=8  precheck-tag=8  ｜ 结构：徽章=5  橙徽章=1 旧类标记=0  VERDICT=RED
T617 预检确认页（点名两格不确定） exit=0 bytes=85441 可见文本：&lt;=0 裸<span=10 precheck-tag=10 ｜ 结构：徽章=5 橙徽章=1 旧类标记=2 VERDICT=RED
RESULT: 0/3 FAIL

# 改后（同一脚本）
T617 拍营养表记一餐      exit=0 bytes=84429 可见文本：&lt;=0 裸<span=0 precheck-tag=0 ｜ 结构：徽章=8  橙徽章=0 旧类标记=0 VERDICT=GREEN
T617 拍营养表补记一餐    exit=0 bytes=84640 可见文本：&lt;=0 裸<span=0 precheck-tag=0 ｜ 结构：徽章=8  橙徽章=0 旧类标记=0 VERDICT=GREEN
T617 预检确认页（点名两格不确定） exit=0 bytes=85251 可见文本：&lt;=0 裸<span=0 precheck-tag=0 ｜ 结构：徽章=10 橙徽章=2 旧类标记=0 VERDICT=GREEN
RESULT: 3/3 PASS
```

读数口径：`&lt;`＝可见文本里转义后的尖括号；`裸<span>`＝可见文本里印出来的标签；`precheck-tag`＝类名当文本印出来。
「结构：徽章」只数体里的 `<span class="…ilife-status-badge…`（样式段里那几条规则不计，票面口径）。
改后两页各 8 枚灰徽章（八行每行一枚），点名两格不确定那页再加 2 枚橙徽章。

### ③ t277

```
node --test packages/skill-calorie/test/t277-预检确认页.test.mjs    → exit 0（19/19）
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 19580
```
（同一次运行里 `ℹ tests 19`／`ℹ pass 19`；新增两条 `#617 那一列不再印 HTML 字面量，只见读者话与徽章结构` 在册。）

### ④ 变异自证（两行机器读数）

变异＝**把那串 HTML 写回 `tag` 格**（并去掉 `cellHtml`），即把负责人抓到的缺陷原样放回；还原＝按备份逐字节取回（sha256 回原值）：

```
MUT-BUILD tag=baseline exit=0    MUT-TEST tag=baseline pass=19 fail=0  MUT-PROBE RESULT: 3/3 PASS exit=0
MUT-BUILD tag=mut1     exit=0    MUT-TEST tag=mut1     pass=14 fail=5  MUT-PROBE RESULT: 0/3 FAIL exit=1   ← 改坏一处即红
MUT-BUILD tag=restored exit=1※   MUT-TEST tag=restored pass=19 fail=0  MUT-PROBE RESULT: 3/3 PASS exit=0   ← 还原绿（※编译那一段红在他席在途件，见下）
MUT-BUILD tag=final    exit=0    MUT-TEST tag=final    pass=19 fail=0  MUT-PROBE RESULT: 3/3 PASS exit=0   ← 他席收笔后的终态复跑，全绿
```
变异轮红的 5 条逐条点名：`#277 ② 营养表确认页的字段与标记 —— 记一餐`／`……补记一餐`／
`#617 那一列不再印 HTML 字面量，只见读者话与徽章结构 —— 记一餐`／`……补记一餐`／`#277 ② 识别不确定的格子有标记，其余没有`。
还原逐字节判据：`precheckLabel.ts` sha256 前后同为 `173A3EE5D18E6037…`、`precheckParts.ts` 同为 `0CDDF1D4B2F10FA5…`；
`restored` 那一轮的 t277 与探针已按还原后的源码重编产物判绿，**唯一那段红是他席在途件的语法错**（下表第 7 条），与他席收笔后的 `final` 轮同判据全绿。

### ⑤ 邻接复跑（本票范围外的守卫，只点名不代修）

```
node --test t277 t509-清尾 t496-文案统一 trend-misc-port-113 t270-回执四块 t279-真出口用例
ℹ tests 181 / pass 179 / fail 2
```
红的两条都在 `t509-清尾.test.mjs`（`#509 ②`／`#509 ④`）：它按「一行以 `· 回执` 收尾」取回执页题名，而 `src/diet/receipt.ts:388` 现在的题名是 `receipt.scene + '回执'`（`973b897` 功能(581) 去分隔符那一笔改的）⇒ 取到空串。
**与本票无关**（`t509` 判的第 01 页是纯「记一餐」回执页，本票两件都不在那条路上），且 `t509` 不在本票写集 ⇒ **只点名，不代修**，转 `#581` 归属票处置。

## 四 · 机器证据

| # | runId | 命令 | exit | 摘要行 |
|---|---|---|---|---|
| 1 | `3c86ffe1-7a5d-44ee-b21c-fc1fea686d82` | `node .scratch/t617/probe-tag.mjs --out .scratch/t617/before` | 1 | 改前红：`RESULT: 0/3 FAIL` |
| 2 | `50c38ddb-6b8a-4cf7-a6d1-1b18dda8ad56` | `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` | 0 | 改后首次编译绿 |
| 3 | `b98c821d-2b01-415a-b7e1-40e88ee033c9` | `node .scratch/t617/probe-tag.mjs --out .scratch/t617/after` | 0 | 改后绿：`RESULT: 3/3 PASS` |
| 4 | `50377682-a3a5-4c5c-a849-82d22a40cc11` | `node --test packages/skill-calorie/test/t277-预检确认页.test.mjs` | 0 | `tests 19 / pass 19 / fail 0` |
| 5 | `8e59d96a-f66a-4e66-ae4d-087e2326babf` | `node .scratch/t617/mut-run.mjs baseline` | 0 | 窗口内直调：编译 0／t277 19 绿／探针 3/3 |
| 6 | `f9138632-40f7-4b7a-b250-96379226023e` | `node .scratch/t617/mut-run.mjs mut1-defect-back` | 1 | **变异红**：t277 `pass=14 fail=5`；探针 `RESULT: 0/3 FAIL` |
| 7 | `39648d6d-df74-419d-8e6f-68deea8f0a87` | `node .scratch/t617/mut-run.mjs restored` | 1 | 还原绿（t277 19 绿／探针 3/3）；编译那一段红在**他席在途件**（见下） |
| 8 | `ebb59bee-26ee-490c-b8a1-b44b18dca804` | `node --test t277 t509 t496 trend-misc-port-113 t270 t279` | 1 | 邻接 181 测：`pass 179 / fail 2`（两条 `t509` 他席红，见 ⑤） |
| 9 | `d0417f1b-1bc7-46fd-9b6b-ec8f292ea80e` | `node .scratch/t617/mut-run.mjs final` | 0 | **终态复跑**：编译 0／t277 `pass=19 fail=0`／探针 `RESULT: 3/3 PASS` |
| 10 | `918c7049-e8b2-4a8b-8d3d-b2c2c03371e6` | `node .scratch/t617/commit.mjs` | 0 | 提交 `7085b8e`，暂存区恰本票三件 |
| 11 | `1aba74bd-41c7-4349-ac74-b2b57a6b4186` | `node docs/skills/skill-calorie/t617-探针-tag.mjs --out .scratch/t617/after-repo-probe` | 0 | 入仓版探针复跑：`RESULT: 3/3 PASS`（三页读数逐字节同第三节改后那三条） |
| 12 | `cd41ae98-13a9-4272-b7a4-938d9251d3f3` | `node .scratch/t617/commit-docs.mjs` | 0 | 提交 `dd3a355`（证据件＋运行记录导出＋入仓版探针），暂存区恰本票三件 |
| 13 | `b5b76153-6467-4566-9e29-007a595b6c4b` | `node .scratch/t617/commit-final-note.mjs` | 1 | **不是门禁红**：提交脚本的暂存区守卫拦下（他席 `t616-证据.md` 在暂存区，本席不带进提交、也不动它） |
| 14 | `bcce72d2-f363-4064-acdf-e14d68eda92d` | `node .scratch/t617/commit-final-note.mjs` | 0 | 守卫改成「带路径闸门提交＋提交后复核他席暂存状态」后落提交 `2f611be`（补记两件） |

窗口内直调（协议 §2.4 第 5 条，不再抢锁）：第 5／6／7／9 条的四次窗口都在持锁区内**直接**调用编译器、测试运行器与探针，逐段读数落 `.scratch/t617/logs/<tag>-{build,t277,probe}.log`。

**一处他席红（只点名，不代修）**：第 7 条那次窗口里 `tsc --force` 红在 `packages/skill-calorie/src/render/trendPredictDocs.ts(995,72): error TS1005: ',' expected.`——另一位席位当时正在改那一件（该文件 14:16 仍在被写）。该席落笔收尾后，第 9 条终态复跑同一条命令 **exit 0**，本票两件无一字改动。

## 五 · 逐条路径

- 改动件：`packages/skill-calorie/src/diet/precheckLabel.ts`（LF 201）、`packages/skill-calorie/src/diet/precheckParts.ts`（LF 84）、`packages/skill-calorie/test/t277-预检确认页.test.mjs`（已提交 `7085b8e`）。
  - 告警线：三件都在本包 350 线之内（两件源码 201／84），**不触发必报五步第四步**，台账无需同步。
- 证据件：`docs/skills/skill-calorie/t617-证据.md`（本件）、`docs/skills/skill-calorie/t617-运行记录.md`（对账源导出）。
- 探针（随证据入仓，可复跑）：`docs/skills/skill-calorie/t617-探针-tag.mjs`；窗口脚本与日志留在草稿目录 `.scratch/t617/`（`probe-tag.mjs`、`mut-run.mjs`、`commit.mjs`、`shots/`）。
- 日志件：`.scratch/t617/probe-before.log`（改前红）、`.scratch/t617/probe-after.log` 与 `mut-final.log`（改后绿）、`.scratch/t617/t277.log`、`.scratch/t617/neighbors.log`、`.scratch/t617/logs/*.log`（窗口内逐段）。
- 截图（改前／改后对照，宽 1000 与 390 两档整页；草稿目录，不随版本控制）：`.scratch/t617/shots/改前-1000-整页.png`、
  `.scratch/t617/shots/改后-1000-整页.png`、`.scratch/t617/shots/改后-390-整页.png`；产物副本 `.scratch/t617/before/`、`.scratch/t617/after/`。

## 六 · 未做项与下一手缺什么

- **不做的**：`precheck.ts`／`precheckPort.ts`／`base-render/**`／其它测试，一字未动（写集之外）；屏上来源脚注行未碰（归 #560）。
- **遗留出口逐条**：票面点名的「另两处表格（导入预检页）」已排查——`src/diet/precheck.ts` 全件没有 `<span` 之类手拼写法，`renderListRows`／`renderDataTable` 的槽都是纯文本值，**无同类写法**，故不转票。
- **下一手**：#619 统一重出墙＋复拍（本票只交源码与读数；本席未重出 `scene02-验收墙/**`，那是 #619 的写集）。
- **一处待办的账（不在本票写集）**：`t509-清尾.test.mjs` 的 `· 回执` 题名断言过期（第 ⑤ 节），需 #581 归属票跟改。
