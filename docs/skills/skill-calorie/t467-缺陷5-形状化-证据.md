# t467 切片 · 用户缺陷⑤两句话形状化 · 证据

票：[#467 卡路里场景 01：看今日目标进度 整页重做](https://github.com/FeatherHunter/ilife/issues/467) 的切片。
本切片只做目标页两句话形状化（用户缺陷⑤），整页其余不动。不关票（关票待整页终审）。

两句话：

- 「日均按窗口天数摊平…」
- 「达标口径：当日摄入…」

要求：功能体现在 UI 形状上而非纯文字（结论条／口径行／事实条三选其位，复用既有件，不重造形状）。

## 一、用户缺陷⑤的口径

用户原话⑤（逐字，载 `docs/skills/skill-calorie/t516-场景10-视觉整改基准.md:12`）：

- 「需要『；』『·』分割＝该处要做 UI 设计」。

总规则（同件 `:56`）：可见文本里出现 `；`／`·`／`｜`／`、`，凡是把多个语义串成一行的，一律判债——该处要换形状，不是把符号删掉。

本页两处正犯同一条（只是连词是 `。`，探针不点名但形状债同源）：

- 旧 `goalProgressDocs.ts:360`：一个 `renderCaliberLine` 里串了两个事实（日均口径 ＋ 缺口口径）。
- 旧 `goalProgressDocs.ts:396`：一个 `renderCaliberLine` 里串了两个事实（达标带 ＋ 没记录处理）。

同族已定做法（`src/render/sportDocs.ts:20`、`src/exercise/sportUi.ts:21`、`src/exercise/records.ts:23`）：口径行里 `；` 串 → 每条事实一个 `renderCaliberLine`（公共层既有件，不重造形状）。本切片照同一条办。

## 二、必报五步

### 第一步 · 影响清单（动手前）

| # | 目录 | 文件 | 一句话 | 理由 |
|---|---|---|---|---|
| 1 | `home` | `packages/skill-calorie/src/home/goalProgressDocs.ts` | 两处口径行一拆为二（一条事实一行），同行内追加第二个 `renderCaliberLine`，LF 不变 | 缺陷⑤的落点；复用既有件 |
| 2 | `home` | `packages/skill-calorie/test/t467-goal-progress.test.mjs` | 文末追加一个新用例，只增不动旧 | 给本切片落机器判据；不重断旧形 |
| 3 | 文档 | `docs/skills/skill-calorie/t467-缺陷5-形状化-证据.md` | 本件（新证据件，文件名带票号） | 派单声明的新证据件 |

一行里没有出现两个能力名。`today.ts`（在途）、别家目录、公共层一行不动。

### 第二步 · 结构设计

- 不新增目录层级，不新增 `src` 文件，不新增导出，不改公开接口（`buildGoalProgressDoc` 签名不变）。
- 每个文件对外给的东西数量不变；跨能力引用无；常量与类型无新增。
- 落位选择：口径行（`renderCaliberLine`，公共层既有件，本件已在用）。
  不选结论条（那是主读法判定句的槽，不是旁注口径的槽）；
  不选事实条（`factStrip` 是 `weight`／`exercise` 两族各自的本地件，`home` 直接引即跨能力取内部件，抄一份更违反铁律一；公共层无此件）。
- LF 设计：两处拆分都做在同一物理行内（`+ renderCaliberLine(A) + renderCaliberLine(B)`），LF 452 → 452，台账「当场实测」列不陈化。

### 第三步 · 写代码

diff（`git diff` 实录，整页其余不动）：

```diff
-    + renderCaliberLine('日均按窗口天数摊平，没记录的日子也算一天。缺口是消耗减摄入的差，正数表示摄入比消耗少。')
+    + renderCaliberLine('日均按窗口天数摊平，没记录的日子也算一天。') + renderCaliberLine('缺口是消耗减摄入的差，正数表示摄入比消耗少。')
-      + renderCaliberLine('达标口径：当日摄入占目标的 80% 到 120% 之间算达标。没记录的日子不列表，既不算达标也不算落空。')
+      + renderCaliberLine('达标口径：当日摄入占目标的 80% 到 120% 之间算达标。') + renderCaliberLine('没记录的日子不列表，既不算达标也不算落空。')
```

旧测试五个用例一字未动；新用例只量「各占一行、合串行消失」，不锁口径行总数与其他区块（见 §四）。

### 第四步 · 超线报警

已超线，需要根据规则进行重构。`src/home/goalProgressDocs.ts` LF=452（＞350，台账行 `goalProgressDocs.ts｜452` 与实况一致，本切片 LF 不变故不陈化）。
超因与拆法照台账行原文（本件同时住着四卡与结论条、走势图与目标线量程判定、达标表与两档窗口转场、三项目标那一块、复制区与页框接线；拆法＝目标线量程判定＋走势图＋图下读法另立姊妹件）。
本次先不拆：拆分不在本切片写集（票面只许动本件两行口径＋测试＋证据）。

### 第五步 · 交付对账

实际碰到的目录逐行与第一步对得上，偏差为零：`home` 两件 ＋ 文档一新件。禁区（`today.ts`、别家目录、公共层）一行未碰。

## 三、让路检查

开工先 `git status --porcelain=v1 -- packages/skill-calorie/src/home/goalProgressDocs.ts`：空输出（该件无他人改动），故不让路、照常开工。
工作区其余在途改动与本切片无关，本切片提交只带派单声明的三条路径（见 §七）。

## 四、测试（只增不断言旧形）

新增用例：`#467 缺陷⑤两句话形状化：一条事实一行口径行`（`t467-goal-progress.test.mjs:296`）。

```js
const lines = calibers(r.file);
assert.ok(lines.includes('日均按窗口天数摊平，没记录的日子也算一天。'), '缺日均口径独占的一行');
assert.ok(lines.includes('缺口是消耗减摄入的差，正数表示摄入比消耗少。'), '缺缺口口径独占的一行');
assert.ok(lines.includes('达标口径：当日摄入占目标的 80% 到 120% 之间算达标。'), '缺达标带独占的一行');
assert.ok(lines.includes('没记录的日子不列表，既不算达标也不算落空。'), '缺没记录处理独占的一行');
assert.ok(!lines.some((t) => t.includes('摊平') && t.includes('缺口是')), '两事实仍挤在一个口径行里');
assert.ok(!lines.some((t) => t.includes('达标口径') && t.includes('不列表')), '两事实仍挤在一个口径行里');
```

读数（持锁，`node --test packages/skill-calorie/test/t467-goal-progress.test.mjs`）：

- `tests 6／pass 6／fail 0`（旧 5 个全绿 ＋ 新 1 个绿），runId `20e4ad3c-5a49-4c48-bd56-90b0503b7581` exit 0。

## 五、变异自证（两行机器读数）

变异点：把第二处拆分故意合回去一行（重现缺陷⑤的合串行），编译经持锁直调 `tsc`（见 §六）后跑同一测试。

变异（红）：

```text
tests 6／pass 5／fail 1
AssertionError [ERR_ASSERTION]: 缺达标带独占的一行
```

FAIL 前后（`.scratch/t467-defect5/mut-test-red.log`）：

```text
✖ failing tests:

test at packages\skill-calorie\test\t467-goal-progress.test.mjs:296:1
✖ #467 缺陷⑤两句话形状化：一条事实一行口径行 (12949.7219ms)
  AssertionError [ERR_ASSERTION]: 缺达标带独占的一行
      at TestContext.<anonymous> (file:///D:/ilife/packages/skill-calorie/test/t467-goal-progress.test.mjs:302:10)
```

还原（绿）：合串行拆回两行 `renderCaliberLine`，持锁重建后复跑：

```text
tests 6／pass 6／fail 0
```

两行读数即上面红绿各一行；红的正是本切片新增用例（旧 5 个在变异下仍绿，定位精确）。

## 六、门禁运行记录（持锁，一律经包装器）

| 运行标识 | 命令 | 退出码 | 读数 |
|---|---|---|---|
| `5d617aed-e45e-473c-9d9a-c5f27b53aae6` | `pnpm build` | 0 | 改后首次全量编译绿 |
| `adbbf3d1-e39b-4ce1-91d0-69b48f82030e` | `node --test packages/skill-calorie/test/t467-goal-progress.test.mjs` | 0 | tests 6／pass 6／fail 0 |
| `a590ff33-291e-4c95-a566-7fa56c804a22` | `pnpm build`（变异后重建） | 0 | 变异可编译，判据才有意义 |
| `522bdde8-6381-49d8-bb1e-e752312c2ccf` | `node --test …t467-goal-progress.test.mjs`（变异） | 1 | tests 6／pass 5／fail 1（上节红） |
| `dfe23c6e-6130-4645-b96e-78054ed5c62f` | `pnpm build`（还原后） | 1 | 红的只是环境：`'tsc' is not recognized`（`node_modules/.bin` 缺失，见下） |
| `49b7366e-76e5-4c9e-8808-e96760bb4bb7` | `node node_modules/typescript/bin/tsc -b`（还原后重建） | 0 | 同一包装器、同一票号，直调仓内现存 `typescript@5.9.2` 重建绿；未装包 |
| `20e4ad3c-5a49-4c48-bd56-90b0503b7581` | `node --test …t467-goal-progress.test.mjs`（还原后） | 0 | tests 6／pass 6／fail 0（上节绿） |

如实记账：还原后第一次 `pnpm build` 因 `node_modules/.bin` 缺失（`Test-Path node_modules/.bin` 为 False，但 `node_modules/typescript/bin/tsc` 在且 `--version` 为 5.9.2）而红；
按「缺依赖停手上报，不装包」办——没有跑任何安装，只改用仓内现存同一版本 `tsc` 经同一持锁包装器重建（`--ticket 467` 不变），`exit 0` 后再跑测试复绿。
另有两次持锁等待超时（分别与 ticket 548／396 的活锁相撞，按包装器纪律等它释放；无 RUN 条目，不充门禁证据）。

长命令输出落本票日志：`.scratch/t467-defect5/build.log`、`test.log`、`mut-build.log`、`mut-test-red.log`、`restore-tsc.log`、`test-green.log`（只看尾 5 行；红的已翻 FAIL 前后 10 行见 §五）。

## 七、提交

只提交派单声明的三条路径（中文提交信息）：

- `packages/skill-calorie/src/home/goalProgressDocs.ts`
- `packages/skill-calorie/test/t467-goal-progress.test.mjs`
- `docs/skills/skill-calorie/t467-缺陷5-形状化-证据.md`

提交信息格式如 `修复(467): 目标页两句话一条事实一行 + goalProgressDocs.ts/判据全绿`。

## 八、未做项

- 整页其余不动（结论条文本、KPI、走势图、达标表转场句、三项目标块、复制区、来源脚注均未碰）。
- 告警线拆分不在本切片写集（见 §二第四步），待收口票认领。
- 那 7 条复用唤醒词的验收面未扩（形状提升随同一装配自然落到它们头上，记账不验收）。
