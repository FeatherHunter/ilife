# #180 对抗式审查（证伪视角）

> 审查席位：对抗式审查者 A（证伪视角）。工作区 `D:\ilife`。
> 审查对象（**已提交历史**，不可变）：`a27a4a2`（场景 01–05 数据）／`9b4441a`（场景 06–10 数据）／`9ca9eea`（场景 02 两条多步词回改）／`fd65fab`（快照重算脚本＋重算）／正本 `de32e54`、`1e3fc6e`、`aec079a`。
> 取证时点：**2026-09-13T03:10Z–03:26Z（本地 +08:00，即 UTC 2026-09-12T19:10–19:26Z）**，当时 `HEAD=1313a08`。
> 并发声明：审查期间另有 session 在同一仓推进 #180 收口，`HEAD` 在 03:22 前进到 `7855699`／`e3e3ad1`。**这两个提交不在审查对象内**，我只对上面那组不可变 sha 负责。工作区当时含 5 个未提交改动（`help-lookup.ts`／`help-center-106`／`render-t10`／`skill-t11`／`calorie-routing-81`），**未计入审查对象**；为取得提交态读数，我另建隔离树（见 §三）。

## ① Verdict

**PASS**（无 S1-交付缺陷、无 S1-过程违规）。检测到 **2 项 S2**（关闭前必须处置）＋ **6 项 S3**（记账跟进）。分数摘要：契约一致 28/30、证据真实可复现 18/25、parity 19/20、工程红线 15/15、文档同步 8/10。

一句话：**数据改写本身干净、零残留、无夹带，防回退断言与「逐字相同」断言都真会红（我亲自变异复现）；扣分全在「证据文件的可复核性」与「被钉死测试的口径同步时点」上。**

## ② 六项必答

### 一、命门：命令文本与路由层真的逐字相同吗

**结论：① 断言不恒真（实测有鉴别力）；② 覆盖率 321/341，逃逸 20 条，归属已逐条分清；③ 逃逸者部分被别的断言覆盖，「路由-数据语义对齐」无覆盖（S3-4／S3-5）。**

我自己写 `probe-a-verbatim.mjs`，**逐行复刻 HEAD 提交版 `test/calorie-routing-81.test.mjs:165-187` 的分支链**并展开成计数（不是跑到第一次失败）：

```
BRANCH-ENTERED=321  SAME=321  DIFF=0  KIND_BAD=0  KEY_BAD=0
WIZARD_BRANCH=5  OVERRIDE_BRANCH=0  NO_BRANCH=110
ESCAPE_EXEC=20   ESCAPE_BY_REASON={"SPECIAL_FLIP_WORDS 排除":2,"legacy（无 key）":18}
```

**独立复现了前任审查者的读数**（321／321／0／20），且我另得一条它没报的：`OVERRIDE_BRANCH=0`——提交版 `:182-186` 那条「必须取补偿表」的分支在我这个对象上**一次也没进**（因为数据 cli 已是命令形态，全部被 `:177` 分支先接走），属死分支。

**① 恒真风险评估**：该段**不是恒真**。条件 `cli.startsWith('calorie-cmd-read calorie.')` 只决定「进不进来」，进来之后比的是两张**互相独立**的字面表（`src/triggers/routing.ts` 只 `import type` 了 `cli/keys.js`，**不引用任何 scene 数据**，我核过它的 import 与全文）。我用变异证伪了恒真（见 §三）。但**确有一个设计性覆盖缺口**：数据侧一旦不是命令形态就整段跳过，所以「把一条命令换成自然语言」不会被这条断言发现（只有 legacy 因卡面 id 约束被允许这样写）。

**② 逃逸者归属（20 条）**：
- **18 条＝本票范围**：`今日复盘／复盘／复盘日期范围／本周复盘／本年复盘／本月复盘／查低热量榜／查健康报告／查卡路里数据／查热量趋势／查营养结构／查运动分布／查运动贡献／查频繁吃榜／查食物排行／查高热量榜／查高碳水榜／查高蛋白榜`。它们无 `key`，命令字段会被 `helpCenter.ts:242` 当**卡面 id**，故本票按议题补记第 3 条改成自然语言 → 逐字断言按其定义不适用。**非缺陷**，规模由 `:218 legacyCardIdExecZone().length === 18` 钉死。
- **2 条＝`SPECIAL_FLIP_WORDS` 排除**：
  - `看目标预测达成`：实测 data cli 与 route cli **逐字相同**（`--params '{"start":"2026-08-25","end":"2026-09-07"}'`），被过度保守地排除，无害。
  - `记体脂（皮褶钳）`：两者**不同**（data 缺 7 个皮褶参数）。该值 `9b4441a` 前后**逐字未变** → **范围外既有**（#177／FX-81-7 遗留）→ S3-5。

**③ 逃逸者是否被别的断言覆盖**：
- 18 条 legacy：卡面 id 与尖括号口径**有**覆盖——`help-center-88.test.mjs:169-179`（text 态尖括号集合恰为 `['<N>']`、逐字来自 prompt、不得 HTML 转义）、`help-center-106.test.mjs:170-181`（SoT 5 条变体不可路由、产物零出现）、`calorie-routing-81:218`（规模 18）。**但没有任何断言把它们的文本与路由语义对齐**，`:218` 只钉**计数**不钉**成员身份**（换掉任意一条仍绿）→ **S3-4，不足以升到 S3 以上**。
- 2 条 SPECIAL_FLIP：`看目标预测达成` 有窗口断言 `:159-163`（但读的是补偿表不是数据），`记体脂（皮褶钳）` 只有 `:152-155` 的成员身份断言 → **都没有「数据 cli 可用性」覆盖** → S3-5。

### 二、归零与「没夹带」

**结论：三字段归零属实且无夹带，文件范围与字段范围都在声明写集内。**

- **归零**：`probe-b-residual.mjs` 用比实施者更宽的口径扫全量 436 条——不只三字段，而是**整条 entry 的任意字符串路径**（递归 walk 全部键）。结果 `WIDE_HITS=0`；三字段命中 `0`；`variants[].cli` 共 5 条、全为自然语言；`ROUTING_SRC python=0 scripts/=0 mavis=0`。
- **没夹带**：`probe-c2-fielddiff.mjs` 把每个场景文件逐行 `JSON.parse`（**按编者提示先去行末逗号**），比较 `sha^` 与 `sha` 两侧**逐字段**（不是看去重行文本）：

| 提交 | 条目数 | 改动条目 | 改动字段 |
|---|---|---|---|
| `a27a4a2` | 208 | 171 | `main_prompt.cli` 171 ／ `data_source` 170 |
| `9b4441a` | 228 | 205 | `main_prompt.cli` 205 ／ `data_source` 184 ／ `variants` 3（宿主 3 条／5 值） |
| `9ca9eea` | 70 | 2 | `main_prompt.cli` 2 ／ `data_source` 2 |

`EXTRA_DIFFS=0`；`prompt_template` 与 `main_prompt.text` **改动 0**；键集变化 0；条目增删 0。行数 `171↔171`／`205↔205`／`2↔2` 与「改动条目数」一一对上 ⇒ **没有非 entry 行被改**。`git show --stat` 文件范围＝`a27a4a2`:5 个 `scene-01..05`、`9b4441a`:5 个 `scene-06..10`、`9ca9eea`:`scene-02-diet.ts`，均在各自写入集内。
  - 顺带核实「123 对齐／48 自然语言」与「143／62」：`probe-c3-split.mjs` 得 **123/48** 与 **143/62**，与提交信息逐字一致。
- **`routing.ts` 自己的命令文本**：exec 路由的 `cli` **全是** `calorie-cmd-read calorie.*`，无脚本词。源码里 4 处脚本词命中全在**注释**与 `NON_EXEC_REASONS` 闭集文案（`:113`／`:200`／`:589` 提到旧链 `render_*.py`，`:741` 是 t71 出处注释）；我 grep 全 `src` 确认 `NON_EXEC_REASONS` 与路由 `reason` **没有任何渲染读者** ⇒ **用户不会仍看到脚本命令**（但这 4 处无断言覆盖 → S3-3）。
- **`data_source` 只改值未删字段**：`DATA_SOURCE 缺字段=22 空串=0 null/undefined=0`，22 条名单已打印（与议题「22 条 legacy 没有这个字段」一致；**不是被塞了空串**）。`a27a4a2` 的 `cli` 171 vs `data_source` 170 差 1、`9b4441a` 的 205 vs 184 差 21，两者相加恰为 22——账目自洽。

### 三、变异自证是不是真的

**结论：真的。我独立重做，断言确会红、还原确复绿。**

因工作区测试文件正被并发修改，我**用 `git archive HEAD` 抽出提交态整树到 `.scratch/h180/tree`**（不改仓内任何文件），junction 各包 `node_modules`，自建 `tsc -b --force`。该树的 `test/calorie-routing-81.test.mjs` blob `798c8ecc` **与 `1313a08`／`aec079a` 的提交 blob 逐字节相同**（`git hash-object` 核过）。变异脚本只写该树。

| 变异 | 目标 | sha256(前→后) | 结果 |
|---|---|---|---|
| M1 | `scene-03-weight.ts` 的 `记体重` cli `{"kg":70.5}`→`71.5` | `66c7c6048cb16f4a`→`53ce0a5ee8541617` | `calorie-routing-81` **失败点从 `:209` 前移到 `:180`**：`AssertionError: #79 记体重 直连 cli 必须与冻结表逐字一致` |
| M1 还原 | 从 `HEAD` 回写 | `53ce0a5e…`→`66c7c604…`，`byte_equal_to_HEAD=true` | 失败集合回到基线（`:209`／`:248`／`:310`）——**复绿** |
| M2 | `scene-09-photo.ts` 首条 `记身材照` cli → `python scripts/render_photo.py --mode add` | `6abb8a8d80396225`→`a34138e7b0062ae9` | `no-script-commands-180` **变红**：`场景数据里仍有脚本命令：命中 1 条（main_prompt.cli 1 ／ data_source 0 ／ variants[].cli 0）`——与实施者声称的变异二态输出**逐字相同** |
| M2 还原 | 同上 | 回 `6abb8a8d…`，`byte_equal_to_HEAD=true` | `2 通过／0 失败` |

我的 runId：`e02c641d-4e27-45f1-a586-48bb9680718c`（整轮变异在一次持锁内完成，`--ticket 152`）。

**`no-script-commands-180.test.mjs` 会不会恒真？→ 不会**：① `:56-61` 自带**鉴别力自测**（旧形态必命中、新形态必不命中），regex 被削弱会立刻红；② `:79` 钉 `records === 436`，解析空转即红；③ 它**读 `src` 不读 `dist`**，构建过期不影响鉴别力；④ 我的 M2 实测红。唯一小口子：`watchedFields` 静默跳过非字符串值、且只管这三字段（新字段带脚本词不在监控内）——与议题第 5 条范围一致。

### 四、红名单归属是否诚实

**结论：诚实。我实测的红名单与正本 §9 逐条对上；`calorie-triggers:47` 那条已被 `fd65fab` 修掉，正本自己也写了时效声明。**

隔离 HEAD 树靶向实测（`r7`／`r4`，`ℹ tests 70 / pass 64 / fail 6`）：

```
help-center-106.test.mjs:84   actual=false expected=true
render-t10.test.mjs:180       actual=false expected=true
skill-t11.test.mjs:154        actual=false expected=true
calorie-routing-81.test.mjs:209  actual=18   expected=237
calorie-routing-81.test.mjs:248  actual=74   expected=43
calorie-routing-81.test.mjs:310  actual=1    expected=0
```

我另用 `probe-d-headred.mjs` **不看测试只看数据**独立复算，得 `18／74／1`——与真跑输出**逐字一致**（两条互证）。`no-script-commands-180`（2/2）与 `help-center-88` **绿**。

逐条归属（**是否本票引入**）：

| 红 | 归属 | 判断 |
|---|---|---|
| `help-center-106:84` | 议题《要做的事》第 4 条**已登记** | 前提被本票正当消灭，须改 `=0`。**非缺陷**（但关闭前必须改，S2 口径） |
| `calorie-routing-81:209` | 议题第 4 条第 3 项**已登记** | 派生常量塌缩（237→18），**须重导**。非缺陷 |
| `calorie-routing-81:248` | 议题第 4 条＋该测试 `:238` 自述**已登记** | 基线由 43→74，`LEGACY_OVERRIDE_EXCEPTIONS` 该撤。非缺陷 |
| `calorie-routing-81:310` | 议题《开工前要先裁的基线红》，用户 2026-09-11 已裁**甲** | **改前即红**，与本票无因果 |
| `calorie-triggers:47` | 议题第 4 条第 1 项 | **我的对象上已绿**（`fd65fab` 已重算快照）→ 不计入红 |
| `render-t10:180`／`skill-t11:154` | **本票引出**（见下） | **S2-1** |
| `fetch-t6:97` | 环境时钟 | 与数据无因果；**活红**（我实测当前仓 `1469/1468/1`，唯一红就是它） |

**那 2 条前提型断言的归类（派单重点）**：我判**「前提被本票正当地消灭、断言口径需随规格更新」**，**不是**「本票引出的交付缺陷」。理由三条：① 断言的判据就是「SoT 里还有 python 命令」，而消灭它正是用户裁定的本票目标——本票达成**必然**令其为假；② 断言所锁的**同源性**（`legacyCli` ≡ `SoT.main_prompt.cli`）并未坏，只是 SoT 值按用户要求换了形态；③ 议题《要做的事》第 4 条本身就承认「被钉死的测试必须随数据同批改」这一类工作，这两条只是**同族漏点名**。**量刑差别**：若判「本票引出的交付缺陷」＝S1-交付缺陷 → 按 §6 直接 **FAIL ＋ 成果作废重做**；我判 **S2**（关闭前必须改口径复绿，或编排者书面改期并给具名票号）→ **不单独否决 PASS**。**但**：本票《完成的样子》写死「`pnpm test` 通过」，故若关闭时仍红，它就升级为**验收未达成**——这正是 S2-1 必须在关闭前处置的原因。

**两份正本的红名单与我实测是否一致**：`06-10` 正本 §9 的 8 条**完全一致**（且它主动声明「第 6 条 sha 那条可能已转绿，以收口波复跑为准」——我实测确已绿，属**如实时效声明**，不是记账不实）。`01-05` 正本 §七 是**更早时点**的快照（列 `:145` 等），已被 `9ca9eea` 修掉，正本 §5.2 自己写了「失败位置前移到 `:209`」→ **不构成记账不实**。

### 五、门禁放宽是否成立

**结论：`06-10` 的放宽完全成立且**今日逐字可复现**；`01-05` 的放宽**理由枚举不完整**，且它自己有一句与审计日志相抵的陈述 → S2-2。**

我自己跑（未沿用实施者结论）：

```
01-05: node tooling/check-gate-audit.mjs --evidence …t180-scenes-01-05.md --ticket 180 \
         --since 2026-09-12T17:56:00Z --allow-undeclared
       → 声称 6 条 / 反向对账 45 条无人声明 / RESULT: matched=6/6 scoped=51 undeclared=45 / gate-audit: PASS
06-10: … --since 2026-09-12T17:59:30Z --until 2026-09-12T18:12:00Z --allow-nonzero --allow-undeclared
       → 声称 14 条 / 反向对账 20 条 / RESULT: matched=14/14 scoped=34 undeclared=20 / gate-audit: PASS
```

`06-10` 的 `matched=14/14 scoped=34 undeclared=20` 与正本 §6.2 **逐字相同**（因为它给了 `--until`，窗口冻结，故今日可复现）✓。我把窗口内全部 `RUN ticket=180` 条目逐条打印核对：那 20 条**全部**属同票号其它波次（t180-a 12 条 ＋ t180-c 6 条 ＋ 2 条 t180-a 误操作），**没有一条是它该认领的** → 放宽成立。

`01-05` 则不然：它**只给 `--since` 不给 `--until`**，窗口随时间增长，今日复跑从正本记的 `scoped=25／undeclared=19` 变成 `scoped=51／undeclared=45`，**该 PASS 口径第三方无法复现**。更关键的是我逐条核对 45 条后确认：**其中 5 条是 01-05 席位自己的 exit=0 运行**，而正本 §九.3 写的是「**不是本席位的漏声明**」：

| runId | 时刻(UTC) | 命令 | 是否可声明 |
|---|---|---|---|
| `294b5eb3` | 18:06:41.966 | `git add packages/skill-calorie/src/triggers/scene-02-diet.ts` | **可**，却未声明 |
| `80c9b69c` | 18:06:42.088 | `git commit -F .scratch/t180-a/msg-wizard.txt`（＝commit `9ca9eea`） | **可**，却未声明 |
| `85f7b7a4` | 18:06:53.839 | `pnpm build` | **可**（门禁类），却未声明 |
| `e9acca5a` | 18:06:58.743 | `git add docs/skills/skill-calorie/t180-scenes-01-05.md` | 结构上自身提交，不可自声明 |
| `eee10b0e` | 18:06:58.866 | `git commit -F .scratch/t180-a/msg-doc.txt`（＝正本 `de32e54`） | 同上 |

这 5 个 runId **在两份正本里一次都没出现**（我逐串 grep 过）。且该正本**没有** `06-10` 那条自指提交的书面说明（`06-10:341` 有）。成因可理解：它的 `check-gate-audit` 是在 18:04:33–18:06:41 之间跑的（故 `scoped=25` 正好等于当时的条目数），那 5 条发生在**对账之后、提交之前**。但这不改变结论：**放宽是具名放宽（GATE-RELAX 行在位，所以不是 S1「私自放宽」），可其「理由」与「不是漏声明」的陈述对不上审计日志**。
- **§2.4.4**：两半都**没有**把对账源导出到受跟踪路径（`docs/skills/skill-calorie/t180-gate-runs.log` 在审查对象时点**不存在**；`--export` 目标被视为写集外）。收口波 `e3e3ad1` 才建了该文件，但它只覆盖**收口波自己的** `19:12–19:20:30Z` 窗口（13 行／7 条），**不含两半的 `17:56–18:12Z`** → 两半的门禁对账至今**没有第三方可复核的对账源** → S3-1。

### 六、我的新探针

见 §④ 与下面第 ④ 节；最有力的一条是 **`probe-g-helpdiff.mjs`**：把「HEAD 24 条补偿表」与「当前 0 条补偿表」两套 `help-lookup` 并列，逐查询对拍 `searchHelp` 全部 436 条唤醒词 ＋ 8 条合成路径查询（共 444 次），并全表对拍 `buildHelpLookup` 的 440 个键 —— **`QUERIES=444 DIFF=0`，`KEYS 440/440 same=true`，`VALUE_DIFF=0`**。即：**「清补偿表」这一步对用户可见的 HELP 出口是行为中性的**（我之前怀疑它会让 18 条 legacy 词的回条从命令退化成自然语言，实测**不成立**——那 18 条本来就没有 `key`，补偿表按内部 key 取值，从来没覆盖过它们）。这条是纯自设探针，实施者脚本覆盖不到。

## ③ 缺陷清单

| # | 级别 | 归属 | 内容 |
|---|---|---|---|
| **S2-1** | S2（关闭前必须处置） | **本票引出** | `render-t10.test.mjs:180`／`skill-t11.test.mjs:154` 两处前提型断言在审查对象上仍红（我在隔离 HEAD 树实测）。归类＝前提被正当消灭、口径需随规格更新（**非** S1-交付缺陷）；但《完成的样子》要求 `pnpm test` 通过 → 关闭前必须改口径复绿，或编排者书面改期＋具名票号。 |
| **S2-2** | S2（证据可复核性） | **本票范围（正本 `de32e54`）** | `t180-scenes-01-05.md` §九.3「不是本席位的漏声明」与审计日志相抵：窗口内有该席位自己 5 条 exit=0 运行未声明（3 条本可声明）；且对账命令只给 `--since`，窗口无上界，PASS 口径不可复现（今日 `scoped=51/undeclared=45`）。 |
| **S3-1** | S3 | 本票范围（两半正本） | 违反协议 §2.4.4：对账源未落受跟踪路径。后果：两半门禁对账无第三方可复核源（收口波新建的 `t180-gate-runs.log` 只覆盖收口波自己的窗口）。 |
| **S3-2** | S3 | 范围外 | `fetch-t6.test.mjs:97` 环境时钟活红（当前仓 `1469/1468/1` 的唯一红）。正本归因正确，但未登记到任何票。 |
| **S3-3** | S3 | 本票范围 | `routing.ts` 仍有 4 处脚本词（`:113`／`:200`／`:589` 属 `NON_EXEC_REASONS` 文案、`:741` 注释）。经核实**无渲染读者**、用户不可见；但 `D2②` 只钉 `python`、防回退断言只扫三数据字段 → 这 4 处**无任何断言覆盖**。 |
| **S3-4** | S3 | 本票范围 | 18 条 legacy 逃逸词只被**计数**钉死（`calorie-routing-81:218`），无成员身份钉子；其文本与路由语义无对齐断言。 |
| **S3-5** | S3 | **范围外发现** | `记体脂（皮褶钳）` 的 data cli 缺 7 个皮褶参数、与路由 cli 不同（`9b4441a` 前后逐字未变，属 #177／FX-81-7 遗留）。**未确证**是否真 exit≠0（写库命令，未跑）。转票措辞。 |
| **S3-6** | S3 | 范围外（议题文本） | `gh issue view 180` 写「`HELP_EXEC_OVERRIDES` 23 条」，我解析提交文本实测 **24** 条。属议题文本，不属交付物。 |

**无 S1-交付缺陷，无 S1-过程违规。** 我自己的全部 build／test／变异轮都经 `node tooling/run-locked.mjs --ticket 152 --`；审查期间未改仓内任何源码／测试／文档，未跑 `git add -A`／`stash`／`reset`／`clean`／切分支／push。

## ④ 我的新探针与读数

| 探针 | 文件 | 关键读数 |
|---|---|---|
| A 命门覆盖 | `probe-a-verbatim.mjs` | `BRANCH-ENTERED=321 SAME=321 DIFF=0`；`ESCAPE=20`（18 legacy ＋ 2 SPECIAL_FLIP）；`OVERRIDE_BRANCH=0`（提交版 `:182-186` 是死分支） |
| B 残留（更宽口径） | `probe-b-residual.mjs` | 整条 entry 任意字符串路径 `WIDE_HITS=0`；`data_source` 缺字段 22／空串 0／null 0 |
| C 字段级夹带核 | `probe-c2-fielddiff.mjs`／`probe-c3-split.mjs` | `EXTRA_DIFFS=0`；123/48 与 143/62 复现 |
| D 提交态断言复算 | `probe-d-headred.mjs` | `:209`=18（期望 237）／`:248`=74（期望 43）／`:310`=1（期望 0）——与真跑逐字一致；`:286 orphaned` 与该绿的部分均绿 |
| E 门禁对账逐条列 | `probe-e-gateaudit.mjs` | 01-05 窗口 51 条／声明 6；06-10 窗口 34 条／声明 14；导出件当时不存在 |
| **F 真跑 CLI** | `probe-f-exec.mjs` | 5 条只读词**数据文本 vs 路由命令**：4 条 `data==route=true` 且**真跑出口一致**（`看体重曲线` exit 0 envelope key 相符；`运动复盘（本周）`／`看营养素深度` exit 4「取数失败（缺失阻断）」两条同码）；`看BMI报告` 属 92 条无入口词（data 为自然语言、无 exec 路由），符合分类。18 条 legacy 的**路由**命令真跑：3 条 exit 0、15 条 exit 4（全为「缺失阻断」数据依赖失败），**无一条 exit 2/3**（即都合法） |
| **G HELP 出口对拍** | `probe-g-helpdiff.mjs` | `QUERIES=444 DIFF=0`；`buildHelpLookup` 440 键 `VALUE_DIFF=0` → 清补偿表行为中性 |
| M 变异自证 | `mutate.mjs` ＋ `mutation-driver.ps1` | 见 §三；runId `e02c641d-4e27-45f1-a586-48bb9680718c` |
| 门禁（我自己的） | `r1`／`r2`／`r7` | `pnpm build` exit 0（`b7338f02-34ef-4658-bbec-29187cbdb663`）；当前仓 `pnpm test` = `1469 测试／1468 通过／1 失败`（`26562f6c-c181-4dfc-b0ed-04100a1db128`） |

证据文件与日志落在 `.scratch/t180-review-a2/`（gitignored）。**隔离 HEAD 树**在 `.scratch/h180/tree`（blob 校验：`test/calorie-routing-81.test.mjs` = `798c8ecc…` ≡ `1313a08`）。

## ⑤ 我无法确证的项

1. **`记体脂（皮褶钳）` 的数据 cli 是否真 exit≠0**：它是写库命令（`calorie.body.composition-add`），我不跑写库命令以免污染真库 → **未确证**。
2. **`_triggers.py` 与 `scripts/_triggers.py` 之外的原始 SoT**：本票只改 TS 场景文件；我未核生成链是否还有第二处副本（议题称命令表在 `SKILL.md` 与缺省 HELP 里各实测 `python` 0，我未复跑该两项）。
3. **补偿表被清空后 `status` 语义是否受影响**：`help-lookup.ts` 在工作区／`7855699` 才清表，`buildHelpLookup` 与 `searchHelp` 我实测中性；但 **`docs/skills/skill-calorie/py-hardcode-scan.md` 的口径与 `.scratch/py-scan/` 脚本**我没有复跑（它们不在审查对象内）。
4. **并发导致的部分不可复现**：审查期间 `HEAD` 由 `1313a08` 前进到 `e3e3ad1`、工作区 5 个测试／源文件被并发改动。凡我引用的提交态读数，一律取自 **隔离 HEAD 树**而非当时工作区；工作区读数（`r7` 全量）只代表我那一刻的现场，**不保证与后续 `HEAD` 一致**。
5. **`01-05` 席位 5 条未声明运行的动机**：我只能证明「它们在窗口内且未声明」，无法证明是刻意还是疏漏 → 按 §2.4.6「查漏不防伪」处理，**不指控造假**。
