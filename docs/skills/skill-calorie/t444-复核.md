# t444 独立复核（#444 对比未知来源校验）

## 1 判定：PASS（总分 98：契约 29／证据 24／对照 20／红线 15／文档 10，无 S1，无过程违规）

票面五段已重读（`gh issue view 444`）。实现两提交均在主干，复核逐句重跑，未采信任何结论。归属：本票引入缺陷 0；本票范围问题 0；范围外观察 1（S3，他席在途注释改动，行为无影响，无需转票，见 §4）。

## 2 机器证据（复核 runId＝3740e9ad-d6a9-481b-8a17-ea8488fa9ddc，记录 `.scratch/locks/gate-runs.log`）

- 提交：`a3d1dba8ec1518c75388a452f92e08368bbce2a9`（`compare.ts`＋t444 测试两件）／`610bddab73ef403617fbeb458bcc7eef6e924e83`（证据＋changeset 两件），`show --stat` 与回执一致；实现 GATE-RUN 五组在日志一对一（`07b4bac7` exit 0／`3195db49` 构建 0＋中间红／`e6d20c51` 构建 0＋三件 0／`e7f30194` exit 0／`c6f70d47` 变异红 exit 1＋还原绿 exit 0）。
- 复核重跑（同 runId）：构建 exit 0；t444 exit 0（`ℹ pass 6／ℹ fail 0`）；相关件 `body-compare-536＋t398` exit 0（`ℹ pass 12／ℹ fail 0`）；`REVIEW-PROBE: 5/5` exit 0。
- 变异还原（删 `assertSourceFilter` 行）：t444 红 exit 1（`ℹ pass 4／ℹ fail 2`，红为两条未知来源）／还原绿 exit 0（`ℹ pass 6／ℹ fail 0`），与实现 `impl-mutate.log` 一致。
- 自设新探针 `.scratch/t444/review-probe.mjs`（空库排序＋大小写盲区，被审六例全带种子库覆盖不到）：绿 `REVIEW-PROBE: 5/5` exit 0（空库 nope 报未知来源／空库合法源报第一段无记录／空库 CLI exit 4／`ALL` 拒／空串拒）；变异下红 exit 1（空库 nope 误报第一段无记录）／还原绿 exit 0。过程注记：复核变异还原时 pwsh 写回带入 BOM（`git diff` 剩 1 行）＋并发窗他席破窗致一次重建 exit 1（`goal/resultDocs.ts` 他席在途错），均持锁修复并以 `--force` 重建 exit 0＋t444 6/6＋探针 5/5 收尾，当刻 `compare.ts` 无改动（`git diff/status` 均空）。

## 3 逐条路径

- 改：`packages/skill-calorie/src/body/compare.ts`（LF 实测 213，未超 350；复用 `fetch/body.ts` 已导出 `assertSourceFilter`＋`SOURCE_FILTER_ALL`，未知值经既有 catch 转 `missing-data` exit 4，`all` 映射缺省使取数／显示与缺省一致；`fetch/body.ts` 未动——树上仅他席注释 1 行在途差，与本票无关）。
- 新建（实现）：`packages/skill-calorie/test/t444-对比来源校验.test.mjs`（六例：口径／直调／CLI／三例手算＋SQL／all 逐字节／围度去处）、`docs/skills/skill-calorie/t444-对比来源校验证据.md`、`.changeset/t444-compare-source-check.md`（UTF-8 中文有效，`skill-calorie: patch`）。
- 新建（复核）：`.scratch/t444/review-*`（探针＋构建／测试／变异红绿日志）与本件。命令名与示例（`body/commands.ts:35`）、对比口径与变化率算法、体成分看行为、唤醒词（`body/routes.ts`）均未动；两提交暂存区均仅含本票件。
- 对照抽查：改前 `source=nope` exit 4 原文第一段无记录／改后 exit 4 点名 `未知来源: nope（合法值: home_caliper / hospital / gym 或 all）`；基线前后 html sha 一致（caliper `b290767fc8dc`、围度 `9ecdf4860632`）。

## 4 未做项与下一手

- `all` 产品口径追认票：待编排者当场另开，转票措辞——“#444 已实现 `all`＝全来源对比（与 `listCompositions` 同式，产物与缺省逐字节同），请产品追认该口径或另定语义”。
- 复核未跑全量门禁（与实现同口径，仅 t444／536／t398＋自设探针，共 23 断言绿）；真库零写入（复核全部运行走 mkdtemp tmp 库）。实现文件本复核零改动；下一手：编排者收口（复核件已提交，不 push）。
