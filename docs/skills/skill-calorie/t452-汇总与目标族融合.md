# #452 运动汇总族与对照目标族：融合版式落地证据

票面：`gh issue 452`（地图 #156 场景 04 运动 · 页面族第 3 票）｜实现件：`packages/skill-calorie/src/render/sportDocs.ts`
判据件：`packages/skill-calorie/test/exercise-summary-goal-fusion-452.test.mjs`｜样例：`D:\ilife\.scratch\t452\out\`（14 份，可双击）
身份：**续作席**（前一席网络中断、未提交即停）。本文件只记本席进场复核与补做的读数，不代抄前一席的过程。

## 一、判定：修补 ＋ 收尾

进场时实物已成形（实现件有未提交改动、判据件已存在、14 份样例已生成），判据**首跑即绿**（`223fecee…`，exit 0，8/8），
故不必重做、也无须补齐版面；本席只补三处缺口：

1. **判据里「工程话三条全文断言」名不副实**：原文只判版面壳那一截——版面壳的 `</section>` 是嵌套的，非贪婪匹配实测
   只取到 71266 字符里的 1772 字符，等于没判全文。已改成「摘掉复制载荷后整份产物」的真·全文断言。
2. **票面族票共同要求「可见文本零 snake_case」当场不成立**：来源脚注露出库表名（`exercise_log`／`daily_goal`），
   来源名换成读者看得懂的说法（`运动记录`／`每日目标 ＋ 运动记录`），库表名只留在复制日志里，并落判据。
3. **周口径那句在 CLI 面上跑不到**：冻结锚点 `2026-09-14` 是周一，「看本周运动」实到 1 天，周口径那句永远不印。
   补一条装配层用例（7 天窗）钉住「整周才印、日窗口不印」。

### 进场三行读数

| 读数 | 值 |
|---|---|
| 改动量 | `git diff --stat`＝288 增／113 删（401 行变化），基线 `f743995` |
| 件长 | 进场 346 行（`f743995` 版 171 ＋ 在途净增 175）；本席补来源名常量后 351 行，**越过 350 告警线** |
| 编译产物 | `dist/render/sportDocs.js` 已含在途实现（源 21:54、产物 22:36，源先产物后），首跑判据读的就是它 |

## 二、12 条词真跑读数（＝12 组冻结参数各跑一次，唤醒词接线归 #266）

最终全绿一次：`a876f2b6-1906-4581-aca1-b8ca87913462`，exit 0，9/9 过、35 条读数。12 条**全部** exit 0、产物存在、
`assertDocPage` 过、信封 `data.output` ＝本次 `--html`（判据逐条断言）。产物同时落 `.scratch/t452/out/`。

| # | 词 | 冻结参数 | 窗内天数 | 逐日表行数 | 来源条数 | 产物字节 |
|---|---|---|---|---|---|---|
| 01 | 看本周运动 | `{"window":"本周"}` | 1 | 2 | 2 | 73414 |
| 02 | 看上周运动 | `{"window":"上周"}` | 7 | 8 | 3 | 76377 |
| 03 | 看本月运动 | `{"window":"本月"}` | 14 | 15 | 7 | 79160 |
| 04 | 看上月运动 | `{"window":"上月"}` | 31 | 32 | 10 | 80266 |
| 05 | 看最近 7 天运动 | `{"window":"7d"}` | 7 | 8 | 5 | 77228 |
| 06 | 看最近 30 天运动 | `{"window":"30d"}` | 30 | 31 | 12 | 82194 |
| 07 | 看最近 60 天运动 | `{"window":"60d"}` | 60 | 61 | 22 | 86005 |
| 08 | 看最近 180 天运动 | `{"window":"180d"}` | 180 | 101 | 62 | 89435 |
| 09 | 看最近 365 天运动 | `{"window":"365d"}` | 365 | 101 | 124 | 92134 |
| 10 | 看某段时间运动 | `{"window":"custom","start":"2025-09-15","end":"2026-09-14"}` | 365 | 101 | 124 | 92134 |
| 13 | 看今日运动（vs 目标） | `{"window":"今日"}` | — | — | 1 | 65115 |
| 14 | 看本周运动（vs 目标） | `{"window":"本周"}` | — | — | 1 | 65115 |

（11-365d-trunc／12-365d-range 是第四条那两条截断复核产物，读数见下。）

### 截断一致（长窗口）

`08/09/10` 三条都是「窗内天数 365／180＞上限 100」：页眉条数只报本窗天数、可见行数只摆最近一截，两数在同一句里
明示——实测 `TRUNC 窗内天数=365 可见行数=101 上限=100`，逐日表行数＝上限＋表头行；短窗（≤100 天）不出「截断」
二字且行数＝天数＋表头，两条口径由判据正反两面钉住。365 天窗的「某段时间」那条词走同一条截断口径（行数 101 相同）。

### 装配层三条（不走 CLI，直接调装配函数）

| 用例 | 读数 |
|---|---|
| 目标缺席（`goalTotal`／`pct` 为 null） | `GOAL-EMPTY 环形容器=false conic=false bytes=61131`；走专门空态、页内无环形进度容器 |
| 环取 `min(pct,100)` | 实际 420% 时环心仍 100%，超额百分比由文字承载（`420%`／`超出目标`） |
| 判决胶囊两态 | `done=ok short=no`（同一个类的两个档） |

## 三、变异两行（四步序：变异 → 定向编译 → 判据红 → 写回＋刷 mtime → 编译 → 判据绿）

件按原字节另存 `.scratch/t452/sportDocs.ts.pristine`（sha256 `3F9F67BB…`），**还原只写回这份另存字节**，未用
`git checkout --`。两次变异都在**最终实现字节**上重做，读数与提交件一一对应。

| 步 | ①「无目标也画环」 | ②「去掉截断明示」 |
|---|---|---|
| 变异后源 sha256 | `F91E6FF8DEE66530065E416B4AA20566F496B7AB011632779153C5C67DB2D4D2` | `B0F186626B49694B65AD180EB7A86CD7F2593CE3BD4F57C9B5419388DD9F133A` |
| 定向编译 `pnpm build` | runId `788a79a2…` exit 0；dist sha256 `ED3B8BC0…` | runId `21fbbda8…` exit 0；dist sha256 `806722AF…` |
| 判据（红） | runId `ebf413d1…` **exit 1**，fail 1：`没有目标却画了空环（变异①的靶子）` | runId `13c13b78…` **exit 1**，fail 2：`看最近 180 天运动 长窗口没有截断明示` ＋ `长窗口没有截断明示` |
| 写回＋刷 mtime | 逐字节等于 pristine（`True`） | 逐字节等于 pristine（`True`） |
| 定向编译 `pnpm build` | runId `662a737f…` exit 0；dist 回 `A43C7501…` | runId `4ac9543e…` exit 0；dist 回 `A43C7501…` |
| 判据（绿） | runId `18980ef8…` exit 0，8/8 | runId `82a2f3d5…` exit 0，8/8 |

最终件：源 sha256 `3F9F67BBDC4811177FBD3E0A4221D8C6BA244578419DB4C12E08EC8465DC7C80`（＝pristine，逐字节同一）；
dist `dist/render/sportDocs.js` sha256 `A43C750189FF6F7698AA15782CCA1027137395C808D145B24190107E681A6E81`（还原后回到变异前的读数）。
dist 是编译产物，随提交一并由 `pnpm build` 生成，不入版本库。

## 四、口径单源（周口径那句）

- 判据口径（包内 `src`）：命中文件＝1 → `src/render/sportDocs.ts`（`CALIBER-SRC 命中文件=1`），判据逐字断言。
- 全仓读数（如实登记）：命中 8 个文件，其中**生产代码里的定义只有 1 处**（本件 `weekCaliberLine`，`days === 7` 才算）；
  其余为判据件自身 2 处、公共层 `base-render/src/blocks.ts` 的注释举例、`page-finish-420.test.mjs`、`t420-review-probe.mjs`、
  历史盘点／设计／样张三份。后几类不在本票写集（公共层不改、已关票文档不碰），故全仓文件数不是 1，取值口径以包内 `src` 为准。

## 五、工程话三条 ＋ 可见文本零 snake_case（全文读数，14 份产物）

| 项 | 全文命中 | 摘掉复制载荷后 |
|---|---|---|
| 命令键 `calorie.view.`／`calorie.exercise.` | 56（14 份 × 4，全在 `data-t` 复制载荷里） | **0** |
| 票号样式 `/\bt\d{3}\b/` | **0** | **0** |
| 工序词「移植」 | **0** | **0** |
| snake_case（库表名那种写法） | 30（复制日志的「来源」段，技术原件） | **0** |

复制载荷是**有意**的例外：复制日志末段是可照抄重跑的命令原文、三格式数据里带信封的 `key`，判据据此把命令键判在
「摘掉载荷后的全文」上。修正前可见文本里有 `exercise_log`／`daily_goal` 两词，现已清零（见判定第 2 条）。

## 六、告警线（必报五步第四步）

`node packages/skill-calorie/scripts/check-warning-line.mjs` → exit 0、`RESULT: 58/58`、`PASS: 告警线台账齐全且与实况一致`。

**`src/render/sportDocs.ts` 已超线，需要根据规则进行重构。** 351 行（进场 346 ＋ 本席来源名常量 5），台账行已由
`--sync` 补出、结论列的「超因与拆法待补」由本席补写（`packages/skill-calorie/AGENTS.md`，随本票提交）。超因：运动两页
（汇总／对照目标）的融合版式装配同处一件。本次先不拆：拆分不在 #452 写集（票面只许动本件版面，出口位与调用方归结构票）；
拆法见台账行＝按页切两姊妹件 ＋ 提一件共件（页卡、导航、复制区、来源脚注、截断口径）。

收尾复核时该门一度报红，**不是本票的件**：`RED 台账陈化：src/analysis/reportPlate.ts 台账=393 实况=406`（#384 在途席改到一半）。
本席按票面「红就 `--sync`」同步了那一行（`SYNC-VERIFY ok`，改 1 行，未动其结论文字），再跑回绿＝`RESULT: 58/58`；
该行不是本席的件，本文件如实登记，免得日后对账时以为本票动过报告件。

同一次收尾里 `pnpm build` 是红的，**归因在别席**：`packages/skill-bill/src/help/lookup.ts`／`src/record/scene-income.ts`
报 `error TS1490: File appears to be binary`（别席在途、未落盘），本席不改别席文件。本席件的编译产物不因此失效：
最后一次成功编译（runId `4ac9543e…` exit 0）在本席最后一次改源码之后，dist sha256 `A43C7501…` 与最终源
sha256 `3F9F67BB…` 对应，收尾那次判据读的就是这一份（9/9 过）。

## 七、未做项与留给下一席的三句

1. **字段中文标签未改走 `src/shared/fieldLabel.ts`**：本票两页的可见面没有出现任何库字段名（无 snake_case 字段标签），
   表头中文标签是渲染位内联字面量；该共件的读权不在本席四条路径内，改与不改请父席给口径。
2. **目标页来源脚注的「共 N 条」实为窗内天数**：读数 `数据来源 · 每日目标 ＋ 运动记录 · 2026-09-14 → 2026-09-14 · 共 1 条`
   （当日实有 2 条记录），与判据件里 `sourceFootnote` 注释的「N 即本窗记录数」口径不合。未改：输入接缝
   `ExerciseGoalView` 不带记录数（该件不在本票写集），票面也未钉这个数。
3. **用词纪律**：本席未读 `docs/agents/wording.md`（不在四条路径内、票面要求「要读更多先问我」），本文件用词照票面原词。

GATE-RUN runId=223fecee-f7d5-4efc-a8a1-ae76abd4185a exit=0（首跑 8/8）｜runId=ebf413d1-cd78-4e94-a5aa-84cb1744fbb4 exit=1（变异①红）｜runId=18980ef8-bad2-4527-8861-21f38ecbe795 exit=0（变异①还原绿）｜runId=13c13b78-532f-4859-a19f-9f17d3d2480c exit=1（变异②红）｜runId=82a2f3d5-f23d-41d2-8c40-8207bd249350 exit=0（变异②还原绿）｜runId=a876f2b6-1906-4581-aca1-b8ca87913462 exit=0（最终 9/9）　对账源：`.scratch/locks/gate-runs.log` 的 `RUN ticket=452 …` 各行。
