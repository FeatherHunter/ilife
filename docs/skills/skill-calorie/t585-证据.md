# t585 证据（复核 #582 B4排行族：独立重跑＋新探针）

PASS

## 二、机器证据（只信提交号与自己复跑；编译/测试一律经 `node tooling/run-locked.mjs --ticket 585` 排队；长日志落 `.scratch/t585/` 只看尾5行，红才翻FAIL前后10行）

- 被审提交：`5201dfd`（全 `5201dfddd0ca5c7944f2052a420827e50cca9708`，排行族去分隔符＋口径拆行）＋ `9e2834c`（全 `9e2834cb2ffc0e25b2cc308e9c6467e62ed40681`，断言随文案＋空榜空格并列）。`git diff 9e2834c HEAD -- rankingDocs.ts t272` 为空（跨 `e764c25→b887fbf` 在途提交未碰两件），复跑即审定稿。
- 编译：`GATE-RUN runId=3d4fca9e-619c-4265-8d77-cc42c35b0a64 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force"` → exit 0（入口写死，无 `npx`）。
- 主门禁：`GATE-RUN runId=7ebf0d3f-59e4-4b0e-8782-92b44d47bc3a cmd="node --test t272-排行榜页.test.mjs t279-真出口用例.test.mjs"` → `tests 125 pass 125 fail 0` exit 0（26＋99）。
- 回归面：`GATE-RUN runId=88f1a368-7e35-4af9-946c-c53aecd7d8e1 cmd="node --test t511-清尾二.test.mjs"` → `tests 12 pass 10 fail 2` exit 1（41两条绿，2红见§四）；`GATE-RUN runId=ffa418ea-2250-436f-8af2-7f38e3e30360 cmd="node --test diet-homogeneity-108.test.mjs"` → `tests 12 pass 12 fail 0` exit 0。
- 22榜门禁：`GATE-RUN runId=f9de71c1-8380-45d9-a32e-53efcc6ce3e9 cmd="node .scratch/t585/run-gate.mjs"` → 22件落盘 exit 0；`GATE-RUN runId=507bf0f3-5a06-4c50-baf6-a1e1e3363f64 cmd="node audit-separators.mjs --dir .scratch/t585/products --json .scratch/t585/gate.json"` → 原始 `RESULT: 0/22 FAIL` exit 1，`gate.json files=22 green=0 nodeHits=22 totals={R1:22,R2:0,R3:22,R4:0,R5:0,R6:0,R7:0}`，22命中全是来源脚注行（`ilife-block-caliber` 的 `数据来源 · …`，归#560，票面除外），非脚注命中0件，R2/R4/R5/R6/R7全零＝本族 `；｜、~` 标识符全清。
- 新探针：`GATE-RUN runId=5fdc76ce-dd38-493d-b697-780038b3294b cmd="node .scratch/t585/probe-b4-factkeep.mjs"` → `RESULT: 14/14 PASS` exit 0（另有v1 `7596e4af…` 12/14 exit 1系探针把来源脚注计入口径数，已修正，见§四）。
- 变异电池：`GATE-RUN runId=3173686a-1472-45ae-a36f-f8a71e231b28 cmd="node .scratch/t585/mutate.mjs"` → exit 0；内含 `MUT-READING red t272 exit=1 :: ℹ fail 1`／`MUT-READING red probe exit=1 :: PROBE-FAIL P6-empty-space`／`RESTORE-SAME: True`／`MUT-READING green t272 exit=0`／`MUT-READING green probe exit=0 :: RESULT: 14/14`（变异点 `join(' ')→join('、')`，持锁窗内改坏→编译→跑断言→逐文件还原→重编复跑）。
- 对账源：`docs/skills/skill-calorie/t585-GATE-RUN.log`（本票9次START/RUN全导出，18行）。

## 三、逐条路径（产品与他人测试只读；新探针变异试完已逐文件还原，`git diff` 两件为空）

- 只读复核：`packages/skill-calorie/src/diet/rankingDocs.ts`（369行：DOC_TITLE/眉标去·、占比空格、4处 `~→至`、引导 `；→。`、KPI `至`、单榜1→4口径行、全榜 `｜→。`／`；→，`／3口径行＋空榜 `join(' ')`、全榜空结论 `至`；`caption rankShortName`／`windowStrip →`／来源脚注未动，`dietEngine.ts` 未碰）。
- 只读复核：`packages/skill-calorie/test/t272-排行榜页.test.mjs`（WEEK `至`、split `至`、空榜空格＋双含、3处窗口 `至`）。
- 本票交付：`docs/skills/skill-calorie/t585-证据.md`（本件）＋ `docs/skills/skill-calorie/t585-GATE-RUN.log`（对账导出）。
- 本票草稿：`.scratch/t585/{tsc.log,t272-t279.log,t511.log,homogeneity108.log,run-gate.mjs,run-gate.log,products/(22件),gate.json,gate.log,probe-b4-factkeep.mjs,probe.log,probe2.log,mutate.mjs,mutate.log}`（产物与日志备查，不入仓）。

## 四、缺陷清单（归属逐条）＋未做项＋量刑

- D1（范围外发现·S3）：t511 10/12，红 `①-60饮食复盘读不到工作日平均`（`reviewDocs` 族）与 `④变异后半标题改回读数没变`（`nutrition-detail/today-water` 的 `h1Of` 错位）。41两条（`41看频繁吃榜三个名字统一`／`41页常吃只作常吃榜一部分`，即 `排行 常吃榜／📅 常吃榜／常吃榜` 三处）绿，B4表题改动无回归；红项与本票无交集，转D轮，不判FAIL。
- D2（范围外发现·S3·票面缺陷）：票面 `t511的HD34D53D04D09三处` 全仓44命中无一在t511（D04/D09是#516报告族徽章/键值行码，HD34无出处），属不可验证指针。本席已按真实回归面（41三处＋裸常吃禁令＋homogeneity榜名针）复核：homogeneity 12/12绿（含单榜 `高热量榜/米饭/复制榜单/餐均` 与全榜五榜名＋`<details>`）。请总工澄清指针，不判B4 FAIL。
- D3（本票范围·S3·已修正）：自设探针v1口径计数把来源脚注（含同类名）计入，12/14红；v2剔除 `数据来源` 行后14/14绿。系探针bug非产品缺陷。
- 本票引入缺陷：0。未改产品代码与他人测试（变异窗内单行改坏已逐字节还原＋重编，`RESTORE-SAME: True`）。
- 未做项：无（tsc＋t272/t279＋22榜门禁＋t511/homogeneity＋新探针＋变异两行全做）。下一手：PASS→等D轮收口；D1/D2转票措辞见上。
- 量刑：无S1。五维：契约一致28/30（扣2＝D2指针不可验）／证据真实24/25（扣1＝实施者t511只抽查，本席已补全量）／新旧对照19/20（扣1＝实施者风险段对HD过于简略）／工程红线15/15／文档同步9/10（扣1＝台账369行一致但D2说明可更细）。合计95，均分95≥85 → PASS。
