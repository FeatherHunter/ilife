# #950 · 主页照已认可原型重做 —— 证据（判据 / 命令 / 读数 / 结论）

**票面（用户裁定原文）**：「需要：用公共层组件（base-paint）把「看今日主页」这一页的 HTML 按已认可的原型重做出来。
需要**完全按照原型**把效果做出来。」

- 视觉正本（用户已认可）：`packages/skill-calorie/.scratch/ui-drafts/proto-final-A+C.html`（草图，只定信息架构与形状）
- 验收基准图：`.scratch/ui-drafts/shots/E-final-{desktop,mobile}.png`
- 改造前真产物（对照）：`.scratch/ui-drafts/_delivered-baseline-20260924.html`
- 本节两档实拍（**带真实数据，只住 .scratch**）：`.scratch/t950/shot-desktop-1200.png`／`.scratch/t950/shot-mobile-390.png`
- 只读探针（抓产物片段用的，不进测试面）：`.scratch/t950/probe.mjs`

---

## 一、写集与改动清单（都在 `src/home/**`，逐件一行）

| 件 | 改动 |
|---|---|
| `src/home/homeCards.ts`（**新建**） | 「今日速览」六张卡（热量／蛋白／碳水／脂肪／饮水／今日缺口）＋ `equationBlock`（等式条）＋ 周卡／连续记录卡；`gap` 槽出「还差 N 单位」、`status` 槽出「完成 N%」与方向词；`fmt` 上屏取整 |
| `src/home/homeFrame.ts`（**新建**） | 页框三件：`stateBannerBlock`（态声明条，只在目标暂停时出）／`navBlock`（分段导航）／`dayStripBlock`（记录带：一格一天 ＋ 四条窗口事实） |
| `src/home/homeDocs.ts`（**重写**：334→217 LF） | 页框读序改成原型的读序（态 → 结论 → 分段导航 → 记录带 → 本档正文 → 复制区）；卡件与页框件搬去上面两件；`renderHomeHtml` 老片段页**一字未动**（`render-t8` 锁着它） |
| `src/home/homeViewParts.ts`（366→372 LF） | 五档身份各补一枚导航图标（`nav`）；`overview` 补一行前提说明；缺口那条两段关系改由等式条承载；折线补 `yMin: 0`；`weekCardInputs` 移出（去 `homeCards.ts`） |
| `src/home/home.ts`（108→122 LF） | 补 `carbsGoal`／`fatGoal`／`carbsPct`／`fatPct`／`goalsPaused`（暂停位走目标能力的 `getPausedState`，不另写 SQL） |

**没动的红线**：`packages/base-render/**` 一行未改；命令键／唤醒词／取数口径／复制区契约（`ilife-copy-data`／`ilife-copy-log` 与 `title`／`hint` 两个自由位）一行未改；五档档名与锚点 id 一字未改。

## 二、七个改造点逐条落点（判据：产物里能指出来）

| # | 改造点 | 落点与读数（真产物 `page-real-20260923.html` 上抓） |
|---|---|---|
| 1 | 裸胶囊 → `renderChipRow` | 原先那三枚裸页头胶囊（「主页／有记录 N/M 天／连续记录 N 天」）**整条退场**：窗口那几件事实改由记录带承载（一处说一次）；页上不再有裸 `<span class="ilife-block-chip">` 独占整行 |
| 2 | 页内导航 pill → `renderSegmentedNav` | `<nav class="ilife-block-seg-nav is-sticky" aria-label="页内导航">` ＋ 三格各带一枚内联 SVG，当前格 `is-on` 与 `aria-current="page"`（动作形 vs 状态形分住两套类名） |
| 3 | 「有记录 1/7 天」 → `renderDayStrip` | `<div class="ilife-block-day-strip is-comfortable">`：7 格（`09-17 … 09-23`，缺数印 `—`、今天高亮并带**「今天」二字**，同原型那格 `09-23 今天`）＋ 带下四条：`本窗 1/7 天有记录`／`连续记录 1 天`／`周均摄入 860 卡`／`周均缺口 2012 卡`。**窗口长过 14 天时格带截到最近 14 天并明示**（见 §七） |
| 4 | 缺口算式 → `renderEquationBar` | `<div class="ilife-block-equation-bar">`：两段轨道（摄入 1189｜缺口 1696 → 真数据 860／2012）＋ 逐段读数 ＋ `合计 2885`（真数据 2872） |
| 5 | 前提句／判语分住 | `<div class="ilife-block-state-banner is-warn">目标暂停中｜按暂停前的目标`（**只在 `goal_paused=1` 时出**）＋ `<p class="ilife-block-conclusion">热量在目标内，距目标还差 990 卡。</p>` |
| 6 | 判定卡：省 `value`／`gap`／`pending` | 有目标的四张走 `gap`：`还差 135 克`／`还差 170 克`／`还差 70 克`／`还差 4000`；补满那天出 `已达标`；热量卡走 `status` ＋ 自定义文案 `完成 46%`；缺口卡不给进度条（它相对的不是目标） |
| 7 | 页列/页宽 | **半落地**：`pageUi` 位主页早已开（`homeDocs.ts` 的 `assembleDocPage({ pageUi: true })`），手机端配方在位；正文**列宽档换不了**——`assembleDocPage` 把 `pageUiCss()` 写成无参（`src/shared/docPage.ts`，正被另一席改），而公共层 `PAGE_COLUMNS` 的四档要经那个口子进来。见 §四 |

## 三、判据与读数（命令 / 结果）

1. **编译**：`node tooling/run-locked.mjs --ticket 950 -- node node_modules/typescript/bin/tsc -b packages/skill-calorie` ⇒ `exit=0`（零 `error TS`）。
2. **真跑交付出口**（仓内出口，非技能安装位——原因见 §五）：
   `node packages/skill-calorie/dist/cli/cmd_read.js calorie.view.home --params '{"date":"2026-09-23"}'`（pwsh 里 JSON 要写成 `'{\"date\":\"2026-09-23\"}'`）
   ⇒ `exit 0`；envelope 六字段 `version/skill/shape/key/data/delivery`；`data.output` 落 `…\.db\calorie_html\今日总览_<时间戳>.html`，`delivery.bytes=121643`；产物里 `seg-nav`／`day-strip`／`equation-bar`／`state-banner` 四件俱在，卡名依次为 `今日摄入／蛋白／碳水／脂肪／饮水／今日缺口（热量缺口）`。
3. **与原型逐块对读**（1200 档）：页头 `卡路里／近 7 天总览／2026-09-17 至 2026-09-23`；态声明条 `目标暂停中`；结论条 `热量在目标内，距目标还差 990 卡。`；分段导航三格（今日速览／每日摄入／按日汇总）；记录带 7 格 ＋ 四条事实；今日速览六卡（卡面文案 `完成 46%`／`还差 135 克`／`还差 170 克`／`还差 70 克`／`还差 4000` 与原型逐字相同）；等式条 `摄入 860 ｜ 缺口 2012 ｜ 合计 2872`；折线纵轴 `0／478／956`（下界恒 0）＋ 周均虚线；按日汇总表今日行高亮、缺数印 `—`；复制区双按钮。**与原型的差异只剩两处**（见 §四）。
4. **本包测试面**（逐件真跑，`node --test`）：`home-lock-374` 九条里主页那八条全绿（`看今日饮食概览` 一条是**既有失败**：另一席 diet 小票版在途、把它断言的「累计」二字改掉了，与本票无关）；`396-gap-caliber` 5/5 绿；`t401-可见文本守卫` 2/2 绿（`·`／`；`／≥3 段并列三条读数全 0）；`t401c-页面机器话探针` 4/4 绿；`render-t8` 绿（老片段页一字未动）。
5. **告警线台账门**：`node packages/skill-calorie/scripts/check-warning-line.mjs` ⇒ `RESULT: 100/100`、`PASS: 告警线台账齐全且与实况一致`（两件读数：`src/home/homeDocs.ts` 217／`src/home/homeViewParts.ts` 372）。
6. **长行与机器话**：`t401c` 的长行（≥50 字符）读数未升（仍 0 条）；可见文本零机器话（`machineWords` 只命中复制载荷里有意承载的 `JSON`）。

## 四、与原型的差异（两处，都是「公共层没有这个位」，不是漏做）

1. **六张卡是 4＋2，不是原型的 3×2**。公共层读数卡网格按可用宽自动定列数（正文列 880 ⇒ 4 列）。页内能凑出 3×2 的唯一走法是把六张拆成**两张三卡网格**——**试过并实拍复评**：1200 档确成 3＋3，但 390 档塌成两段「2＋1」（窄屏是公共层强制的 2 列，每段多出一张半宽卡 ＋ 一个空格）。屏幕上的洞比列数差异更难看，故退回一张网格（1200 的 4＋2、390 的 2＋2＋2 两档都齐整）。**登记缺口**（按「公共面缺口不自己扩」）：读数卡网格缺一个**列数位**（`renderKpiGrid(cards, { columns })`），或整页装配缺一个**正文列宽位**（`assembleDocPage` 未透出 `pageUiCss({ column })`）。任一落地即可补齐 3×2。
2. **段标题仍带 emoji、段头小字走公共层口径行**。原型那张图的段标题是纯文本、段头另有一行灰色小字；仓内两件冻着：`t401c` 锁「区块标题带图标」、`t401-可见文本守卫` 锁页级口径行 `<p class="ilife-block-caliber">缺口是消耗减摄入的差。缺数一律写 —。</p>` 这一整行。段头那行小字改由公共层 `ilife-block-caliber` 承载（不自造类名、不写页内样式）。另：卡片第一张沿用卡名「今日摄入」（原型写「热量」）——`home-lock-374` 的针位冻着「今日摄入」这个词。
3. **复制区那行提示（`复制后粘回对话，可继续追问或换窗口重算。`）没出**。原型在按钮排上方有一行 `.ilife-block-copy-block-hint`；公共层 `renderCopyBlock` 有「说明行」位，但**本包的门外壳 `src/shared/copyArea.ts` 的 6 个位里没有这一位**（只有 `title`／`prompt`／`data`／`dataFormats`／`log`／`emptyText`），页面侧递不进去。**登记缺口**：给 `CopyAreaInput` 加一位 `hint` 并透传（加法，不给即逐字节相同）——本票没做，因为它是**跨写集**的共享件（6 个页域都在引），本窗另两席正在动共享件，不宜在交付窗里扩面。**已核对**：本页改造前后都没有这一行（两颗按钮与 `ilife-copy-log` 的 id 与改造前逐字同形），所以这不是本票改出来的差异。

## 五、两处必须知道的环境事实（踩过的坑）

1. **交付出口分两处**：DSH 工具 `run_calorie_command` 走的是**技能安装位** `C:\Users\辰辰洋洋\.dsh\profiles\web\node_modules\skill-calorie`（拷贝，dist 停在 2026-09-17／09-23），**不是本仓**；本票的验收走 **仓内出口** `node packages/skill-calorie/dist/cli/cmd_read.js`（`run_calorie_command` 那一跑拿回的是旧版式，别拿它当读数）。
2. **截图工具只吃会话工作区内的源**：产物落在 `D:\2Study\...\calorie_html\`（包外），直接指过去会报 `source must stay inside the session workspace`；**先复制进 `.scratch/` 再截图**。

## 六、五档巡检与 30 天窗口那一处修正（第二轮在场）

主页那个键出**五张页**（`section` 参数），原型只覆盖缺省档；本轮把另外四档也在种子库上逐档真跑、逐档实拍看了一遍
（探针：`node .scratch/t950/probe.mjs <section> [windowDays]`，产物落 `.scratch/t950/page-<档>.html`）：

| 档 | 读数 |
|---|---|
| `week` | 分段导航当前格 `#sec-week`（`is-on` ＋ `aria-current`）、记录带 7 格、无等式条（等式条归「今日速览」那一块） |
| `streak` | 当前格 `#sec-streak`、记录带在位、连续记录档自己的两块正文一字未动 |
| `budget` | 当前格 `#sec-budget`、`剩余预算` 卡仍在、**不出**「今日缺口（热量缺口）」那张卡（`396` 的分档判据绿） |
| `month`（`windowDays:30`） | 页名 `本月主页`、导航 `#sec-week`、**格带按 §七 截到最近 14 天** |

**实拍抓到的缺陷与修法（本轮唯一的返修）**：`section=month` 且 `windowDays=30` 时，记录带 30 格挤在 880 那一列里，
每格只剩 ~29px，桌面档日期串是 `nowrap`（公共层为圆角给了 `overflow:hidden`）⇒ 实拍图上「08-0908-1008-11…」互相压字。
修法：`homeFrame.ts` 加 `STRIP_MAX_DAYS = 14`——窗口长过 14 天时只画**最近 14 天**，并在带下补一句截断明示
`带上是最近 14 天`（照同族先例：运动族那句「本窗共 N 天，显示最近 100 天，其余 M 天」）。**截断不丢事实**：
本月／本周那两档的逐日事实另有「逐日明细」列表整列出来（30 行逐日）。修后实拍 1200／390 两档：14 格读数清楚、带下五条事实齐。

**「窗内概览」由五张补成六张（第三轮修）**：`week`／`month` 两档原先五张卡在公共层网格（880 档 4 列）里排成 **4＋1**——第二行孤零零一张，看着像没排完。补的那张是**今日消耗**（`burnToday` ＝ 日常消耗 ＋ 当日运动）：它与末尾的「今日摄入」本是一对读数（进出两侧），补齐后两档都齐整（1200 排 4＋2、390 排 2＋2＋2）。改的是 `homeCards.ts::weekCards`，只动 `week`／`month` 两档，`overview`／`streak`／`budget` 三档的卡件一行未动。

## 七、结论

七个改造点里 **六条已落地产物**、第 7 条（列宽档）**半落地并登记缺口**；原型的信息架构与形状（态声明条／结论条／分段导航／记录带／六卡／等式条／0 基准折线／今日行高亮）在真产物上逐块可指；五档（含 30 天窗）逐档真跑、实拍过，本轮抓到并修掉记录带在长窗下的压字缺陷；本包与本次改动相关的判据全绿（含 `t445` 告警线门 `100/100`），台账已同步。与原型的剩余差异只有 §四那两处，都是公共层缺位、按规矩不自己扩。

**第二轮复跑读数**（同一批命令，2026-09-24）：`home-lock-374` 主页八条绿；`396` 5/5；`t401` 2/2；`t401c` 4/4；`t445` 绿；本域合计 23 件 / 22 绿，唯一红是既有的 `看今日饮食概览`（另席 diet 在途）；编译 exit 0；真出口 121,643 B、七格记录带 `09-17 … 09-23`。

**第三轮复跑读数**（2026-09-24）：记录带今天那格补上「今天」二字（照原型 `09-23 今天`；公共层 `DayCellInput.label` 注释写明由调用方决定带不带）⇒ 真出口 121,650 B，两档实拍重拍；本域 30 件 / 29 绿（唯一红仍是既有那条）；**全量套件 1873 件 / 1807 通过 / 53 失败，用 `^✖ .*(home|主页|今日总览|home-lock|t401|396|告警线|day-strip)` 过滤失败名单零命中**——53 条全在运动／身体／档案／饮食／报告／公共面 spec 这些域，来自另外两席的在途改动（与本票写集零重叠）。

**第四轮复跑读数**（2026-09-24，用户当场点名「要我从视觉上看你处理好的 HTML」那一轮）：
- 复制区提示行落地：`src/shared/copyArea.ts` 透出公共层 #870 早就有的 `hint` 位（一个可选字段、不给即不进公共层入参），主页传原型那句 `复制后粘回对话，可继续追问或换窗口重算。` ⇒ 真出口 121,750 B；
- 「窗内概览」补齐六张（见上）⇒ 本月主页 1200 档排 4＋2、390 档排 2＋2＋2；
- 本域 30 件 / 29 绿（唯一红仍是既有那条）；台账门 `100/100` 绿；
- **全量套件 1873 件 / 1822 通过 / 51 失败**（三轮趋势 57 → 53 → 51，仍是别席在途那一片），失败名单里按 `home|主页|home-lock|t401|396|告警线` 过滤**零命中**；
- 交付给用户看的三件：`.scratch/t950/page-real-20260923.html`（真数据近 7 天）、`.scratch/t950/page-month-30.html`（30 天窗）、`.scratch/ui-drafts/proto-final-A+C.html`（原型）。

**第六轮：在**当天还没记任何东西**的那一页上抓到两处**假精度**，都修了（这是用户当天打开「看今日主页」时看到的那一页，不是构造出来的边角态）。
实跑 `--date 今日`（真库，今天 0 条饮食记录）拿到的产物：

| 读数 | 改前 | 改后 |
|---|---|---|
| 等式条 | `摄入 0 ｜ 缺口 0 ｜ 合计 2872` —— **算式不成立**（0＋0≠2872：那天的「缺口 0」是取数层的占位、不是算出来的差） | **整块不出**（`equationBlock` 加「今天没有记录 → 不出」这一支） |
| 六张卡 | `0 卡／0 克／0 毫升` ＋ 徽章「完成 0%」「还差 135 克」「摄入与消耗持平」——把「没记」印成「吃了 0」 | **值位 `—`、徽章「未记录」**（走 `renderKpiCard` 的 `pending` 位；目标行与消耗行照旧在） |
| 结论条 | 「热量在目标内，距目标还差 1850 卡。」——替没记的那顿背书 | 「今天还没有记录。记一餐，完成度与缺口就有数了。」 |

`pending` 这一位本来就是 #950 B1 为「有目标、无记录」造的槽（重构点 6 里列的就是它），这一轮把它用在了它该用的地方。
**有记录的那一页一字未动**：`--date 2026-09-23` 的产物仍 121,750 B（逐字节同改前）；种子库那几页的判据（`396`／`t401c`／`home-lock`）全绿。
`t950-主页原型框架.test.mjs` 补第 9 条判据锁住这一态：拿「种子日次日」当今天（窗口仍有记录、不触发缺失阻断），断六张卡值位全是 `—`＋「未记录」、等式条不出、结论条不说「在目标内」。

**第八轮：排面收齐**。缺口卡原先把三句**读法**（缺口等于消耗减摄入／日常消耗加运动／日常消耗取档案静态值）整个塞在卡的说明行里——那张卡因此比同排高出一截、排面参差（实拍可见）。这一轮把读法搬到「今日速览」段尾的口径行（文本收在 `DEFICIT_CALIBER` 一处，卡与口径行不各写一遍），卡上只留参照物「消耗 2872 卡」。
判据未动：`396-gap-caliber` 的三条针逐字仍在页上（5/5 绿），`t401`／`t401c`／`t950` 全绿（四件合计 24 条）；有记录那天的产物 121,750 → **121,795 B**（多出的 45 字节就是那条口径行）。

**第七轮：边角态巡检**（探针 `.scratch/t950/probe-states.mjs`，五态真跑）。逐态读数都老实，**这一轮没抓到新缺陷**，于是把这几个态补进形状锁：

| 态 | 读数 |
|---|---|
| 没设目标（删 `daily_goal` 行） | 结论条「热量目标还没有，先在「定营养目标」里设一个。」；四张有目标的卡说明行写「未设目标」、**不印完成度**；等式条照出（它不依赖目标） |
| 吃超了（当天多灌 3000 卡） | 缺口转负（`-1304 卡`）、**等式条收起**（两段轨道铺不出来）、缺口卡徽章「摄入比消耗多」、完成度如实印 `完成 233%` |
| 1 天窗（`windowDays:1`） | 记录带 1 格；其余形状不变 |
| 90 天窗（`windowDays:90`） | 记录带截到 14 格 ＋ 明示；窗口天数那句照在 |
| `budget` 档 ＋ 没设目标 | 第六张「剩余预算」值位 `—`（没目标就没有预算），无假徽章 |

`t950-主页原型框架.test.mjs` 因此从 9 条涨到 **12 条**（新增「没设目标」「吃超了」「窗口两端」三条）。

**第五轮：给这次改造补一道形状锁**（`test/t950-主页原型框架.test.mjs`，8 条全绿）。
判据都打在**生产真正走的那条路**上（spawn `dist/cli/cmd_read.js`、解析 envelope、读落盘产物），锁的是本票新做的形状：
① 页框三件在位（分段导航＋记录带；带下事实与 envelope 的 `metrics` 对齐，不字面锁数字）；
② 态声明条**只在** `goal_paused=1` 时出、此时与结论条**同时在场**（态与结论分住的判据）；
③ 短窗一格一天、今天那格带「今天」；④ 长窗截到 14 格并明示；⑤ 等式条三段在场且**合计＝摄入＋缺口**（算式断言）；
⑥ 纵轴含 0 且无负数刻度（#544 那类缺陷的反向锁）；⑦ 五档都带导航与记录带、等式条只归 `overview`、`budget` 出剩余预算；⑧ 变异自证：删掉等式条元素，同一条判据必翻转。

> **一道坑记在这里**：第一版这几条判据写成「`html.includes('ilife-block-equation-bar')`」，三条当场红——
> 公共层的**样式段里恒有这些类名的 CSS 规则**，于是「样式在、元素不在」被误判成在场。判据必须写成
> **元素形**（`<div class="…">`）。本件已按元素形写，并把这条坑写在件头。
