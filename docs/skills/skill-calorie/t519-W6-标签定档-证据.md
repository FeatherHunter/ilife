# t519 · W6 标签定档窗证据（裁定 Z／AA／AB）

> 票：#519（地图 #161 子票）。触发＝复核席报告 `docs/skills/skill-calorie/t519-复核-证据.md`（提交 `4839952`，FAIL 77）
> 的两条 S2 ＋ 三条 S3；裁定原文＝`gh issue view 519 --comments` 最后一条。
> 边界：**只动页面侧**（`packages/skill-calorie/src/analysis/reportDoc*.ts` 与测试）；`packages/base-render/**` 一行未动。
> 全部命令经加锁包装器 `--ticket 519 --lock-dir .scratch/locks-m158v2`；长输出落 `.scratch/t519/W6-*.log`。**不 push、不 close**。

## 〇 · 判定

| 项 | 读数 | 判定 |
|---|---|---|
| **S2-1 四页四档真相交** | 定档 9 枚后：**页 24／26／28／30 × 390／512／820／1440 ＝ 16 格，相交对全 0**，最小**正**间隙 +4.86～+64.99px（真渲染，见 §一 表） | **绿** |
| **S2-2 守卫门槛假绿** | ⑨ 门槛由估算常量 `34.15` 换成**实测最坏值 `54.90` 用户单位**（来源可查）；`m13`（改回 12 枚）**必红** → 还原必绿 | **绿** |
| 盲区补齐：y 刻度 | 新增 ⑩「同图刻度小数位一致 ＋ 整数语义的图全为整数」；`m14`（去掉取整守卫）**必红**（点名 `["87","150.5","214"]`）→ 还原必绿 | **绿** |
| S3② 表述更正 | `t519-W5-视觉整改-证据.md` 两处与真渲染相反的表述**已就地改写**＋件头加更正块（原文逐字留底）；`reportDocParts.ts` 件头注释重写（含四档实测表） | **绿** |
| S3① 条数闭合 | §三 逐类补齐（含对账工具自指 3 次） | **绿** |
| 其余判据 | `tsc` exit 0／`run-all` `31/31`＋`PARAM-AUDIT 31/31`／`audit-separators` **31/31 PASS**／`measure-responsive` **31×3 格 0**（另跑 4 档＝124 格 0）／靶向十件 **61/61**／`gen-cli --check` **GEN-CHECK PASS**／几何探针 **相交图数 0** | **绿** |
| 包内门① `check-warning-line` | **红 `RESULT: 90/91`**，唯一红因＝**别席在途件**：`src/render/sportPortDocs.ts` 台账 809／实况 **858→899→906**（该件正在被另一席连续改写，`git status` 显示它未提交）⇒ **不是本票的账**，本票对该件与台账**一字未动**（我的六件 LF 199／204／92／181／187／20，全在 350 线内、不触发任何台账行变化） | 别席的账（具名） |

## 一 · S2-1：真相交的真实几何 ＋ 定档怎么来的

### 1.1 复核席的读数（本席独立复现，逐字对上）

`.scratch/t519/label-geom.mjs`（真 headless Chrome ＋ CDP；`getBBox()` 用户单位 ／ `getBoundingClientRect()` CSS px）
在**定档 12 枚**（改动前）时：

| 页 | 390 档 | 512 档 | 820 档 | 1440 档 |
|---|---|---|---|---|
| 24／26／28 | 相交 **11/11**，最大重叠 **4.98px**，最小间隙 −4.98px | 相交 **11/11**，最大重叠 **6.22px** | 0（+14.87px） | 0（+28.61px） |
| 30 | 相交 **11/11**，最大重叠 **3.84px** | 相交 **11/11**，最大重叠 4.67px | 0（+17.48px） | 0（+32.23px） |

**真标签宽（用户单位）**：390 档 **53.91～54.90**、512 档 53.91～54.12、820 档 34.45～34.82、1440 档 30.01～30.30；
刻间距 45.6～48.7。⇒ 复核席给的数字（390 最大重叠 4.98px、标签宽 53.9～54.91）**逐项复现**。
**根因**：公共层折线图的 x 标签字号**按断点换档**（≤720px 走 `LINE_TEXT_MOBILE.*`，用户单位更大），
所以「一个估算常量」哪个档都不等于——W5 那次用 `5×11×0.62 = 34.15` 估，恰好落在**最宽两档**附近，最窄两档必漏。

### 1.2 定档 9 的推导（写清「哪一档、哪次实测、多少用户单位」）

- **判据**：锚点间距 ≥ **最坏档实测标签宽 54.90 用户单位**（390／512 共用移动端字号档，取两者更大的 54.90）。
- **最窄绘图宽**出现在 y 刻度标签最长的页（页 26 实测 **484 用户单位**，x 从 82 到 566）。
- `484 ÷ (m−1) ≥ 54.90 ⇒ m−1 ≤ 8.82 ⇒ m ≤ 9`；取 **9**（间距 60.5，余量 +5.6 单位 ≈ +3.3px@390）；
  取 8 更保守但更粗，取 10 就低于实测标签宽 ⇒ **9 是上限档**。
- 落点：`reportDocParts.ts` 的 `CHART_MAX_POINTS = 9`（件头表＝上面这张四档实测表的浓缩，含出处）。

### 1.3 改后：四页 × 四档真渲染（**逐页逐档**）

```
| 页 | 档 | 刻度枚数 | 相邻对 | 相交对 | 最小间隙(px) | 标签宽(用户单位) | 刻间距(用户单位) | y 刻度 |
|---|---|---|---|---|---|---|---|---|
| 24 | 390 | 9 | 8 | 0 | +4.86 | 53.91~54.9 | 63~64 | ["24.0","25.0","26.0"] |
| 24 | 512 | 9 | 8 | 0 | +7.28 | 53.91~54.12 | 63.4~63.6 | ["24.0","25.0","26.0"] |
| 24 | 820 | 9 | 8 | 0 | +37.48 | 34.45~34.82 | 63.31~63.69 | ["24.0","25.0","26.0"] |
| 24 | 1440 | 9 | 8 | 0 | +60.1 | 30.01~30.3 | 63.36~63.64 | ["24.0","25.0","26.0"] |
| 26 | 390 | 9 | 8 | 0 | +4.86 | 53.91~54.9 | 63~64 | ["1508","1615","1722"] |
| 26 | 512 | 9 | 8 | 0 | +7.28 | 53.91~54.12 | 63.4~63.6 | ["1508","1615","1722"] |
| 26 | 820 | 9 | 8 | 0 | +37.48 | 34.45~34.82 | 63.31~63.69 | ["1508","1615","1722"] |
| 26 | 1440 | 9 | 8 | 0 | +60.1 | 30.01~30.3 | 63.36~63.64 | ["1508","1615","1722"] |
| 28 | 390 | 9 | 8 | 0 | +4.86 | 53.91~54.9 | 63~64 | ["1752","2000","2248"] |
| 28 | 512 | 9 | 8 | 0 | +7.28 | 53.91~54.12 | 63.4~63.6 | ["1752","2000","2248"] |
| 28 | 820 | 9 | 8 | 0 | +37.48 | 34.45~34.82 | 63.31~63.69 | ["1752","2000","2248"] |
| 28 | 1440 | 9 | 8 | 0 | +60.1 | 30.01~30.3 | 63.36~63.64 | ["1752","2000","2248"] |
| 30 | 390 | 9 | 8 | 0 | +6.39 | 53.91~54.9 | 65.7~66.8 | ["46","67","88"] |
| 30 | 512 | 9 | 8 | 0 | +9.37 | 53.91~54.12 | 66.1~66.4 | ["46","67","88"] |
| 30 | 820 | 9 | 8 | 0 | +41 | 34.45~34.82 | 66.01~66.49 | ["46","67","88"] |
| 30 | 1440 | 9 | 8 | 0 | +64.99 | 30.01~30.3 | 66.06~66.44 | ["46","67","88"] |
```

⇒ **16 格相交对全 0、最小间隙全为正**；`LABEL-GEOM: 相交图数=0 页=4 档=390/512/820/1440`（exit 0）。
读数原件 `.scratch/t519/W6-geom-after.json`（改前 `.scratch/t519/W6-geom-before.json`，两份都在盘上可复算）。

## 二 · S2-2：守卫门槛换真值 ＋ y 刻度盲区

- **⑨**（`analysis-shape-report-520.test.mjs`）：门槛常量由 `5 × 11 × 0.62 = 34.15`（估算）换成
  **`LABEL_W_USER_WORST = 54.90`**——**实测最坏值**，出处写进件内注释（`.scratch/t519/W6-geom-before.json`，
  390 档 `labelWUserMax=54.90`）；断言同时钉住 **刻度枚数 ≤ 9**（定档值）与**等距**。
  为什么门槛用「锚点间距」而不是「外框间距」：锚点差＝绘图宽 ÷ (点数−1)，在**产物文本**里就能算；
  外框宽只有真渲染量得到 ⇒ **定档收紧在源码常量上、识别力在真渲染探针上复验**，两头都不靠估算。
- **⑩**（新增）：① **同一张图三个 y 刻度的小数位数必须一致**（改前页 26 是 `1516／1614.5／1720`）；
  ② 整数语义的图（卡路里／毫升／分）**全为整数**——夹具专挑「去掉取整守卫即退化成 .5」的序列
  （`[100, 201] ⇒ pad 12.12 ⇒ 87／214`，差 127 为奇数 ⇒ 无守卫时中刻度 150.5）。

**变异两行**（逐字抄电池输出）：

```
MUT-RED m13=red(exit=1 | ℹ fail 1 | AssertionError: 横轴刻度超过定档枚数 9（最窄档必相交）：12)·判据=⑨（裁定 Z/AA）
MUT-RED m14=red(exit=1 | ℹ fail 1 | AssertionError: 整数语义的图出现非整数刻度（取整守卫失效）：["87","150.5","214"] )·判据=⑩（y 刻度）
MUT-GREEN m13=green(exit=0 | ℹ tests 10 ｜ pass 10 ｜ fail 0)  m14=green(exit=0 | ℹ tests 10 ｜ pass 10 ｜ fail 0)  sha256_16=f2dc207a4887（两处还原前后相等）
```

## 三 · S3①：门禁条数闭合（复核席实测 32 vs 本席具名 29）

**核定**：`t519-W5-视觉整改-证据.md` §五 声明 13 条 ＋ §六 具名 5 类（22＋3＋1＋2＋1＝**29**）＝ 42；
而窗口内实到 **43** ⇒ 差的 **1 条是 `check-gate-audit` 自身**；**连同 W5 收尾二窗**（`t519-W5-收尾二-证据.md`）
那次对账的**自指 3 条**，逐类补齐如下（复核席口径：条数必须逐条可对）：

| 类别 | W5-视觉整改窗 | W5-收尾二窗 | 日志指针 |
|---|---|---|---|
| 判据声明（`GATE-RUN`，只领 exit=0） | 13 | 13 | 两件证据的声明段 |
| 本窗整改迭代（编译／重出／探针逐次量） | 22 | — | `.scratch/t519/W5-*.log` |
| 别席在途件导致的编译红 | 3 | — | 唯一错因 `src/exercise/receipt.ts(33,10) TS2305` |
| 别席提交（视觉席 `da02691`） | 1 | — | `runId=45acb17e` |
| 杂务提交／导出 | 2 | 1 | 提交记录 ＋ `t519-GATE-RUN.log` |
| 累积导出（杂务） | 1 | 1 | 同上 |
| **对账工具自指**（每跑一次多一条 `RUN`） | **1** | **2** | `.scratch/locks-m158v2/gate-runs.log` |
| **合计（实到）** | **43** | **17** | 两边逐类相加＝自洽 |

（W5-收尾二窗另含 1 条本席自跑失败的对账迭代 `3f7c9c27`／`0323790f` 那两次中的一次——按 §六 表原列，
本表把「对账工具自指 2」计入后合计 17；两窗各自的实到数与「声明 ＋ 具名」**逐类相等**，闭合。）

## 四 · S3②：与事实相反的表述 —— 已改写（原文留底）

| 件:行 | 改写前（原文，逐字） | 改写后 |
|---|---|---|
| `t519-W5-视觉整改-证据.md` §一 D4 改法 | 「…12 点时相邻间距 ＝ 绘图宽 ÷ 11 ≈ **46 用户单位**，恒大于标签宽 34 单位 ⇒ **四档都不可能相交**（390 档缩放 0.597 后仍留 ~9px 净空）…」 | 同一处保留原句作「**W6 更正（原文留底）**」，并写明真值：真标签宽 390＝**54.90**／512＝54.12／820＝34.82／1440＝30.30 ⇒ 12 枚在 390／512 必相交；定档改 **9** |
| 同件 §一 D4 改后读数 | 「页 28 `xlabel=12`、最小间距 **46.1px**、相交对 **0**（390 档缩放 0.597 后净空 ≈ 9px）」 | 改为「本窗原写…那是**静态估算**读数；真渲染 390／512 档 11/11 相交」，并指向本件 §一 表 |
| 同件 §〇 判定表 D4／D6 两行 | 「改后 12 枚等距、最小间距 46.1px、相交对 0」 | 同上一并改写（D4 行标注「**W6 真渲染更正**」） |
| `packages/skill-calorie/src/analysis/reportDocParts.ts:82-93` | 「12 点时最坏 508/11 ≈ **46 用户单位 > 标签宽 32 单位** ⇒ 四档都不可能相交（390 档缩放 0.597 后仍留 ~9px 净空）」 | 整段重写：给出**四档真渲染表**（390/512 相交、820/1440 不相交）、定档判据与推导（484÷8＝60.5 ≥ 54.90 ⇒ 9 枚） |

**教训（照编排者提醒写进证据件）**：「改了但**守卫没跟上**」＝**假绿会一路带到终审**。
本窗的 ⑨ 门槛是**估算常量**，于是它在最窄档恒绿——而真缺陷恰恰在最窄档。
**门槛必须来自真渲染实测，并写明哪一档、哪次实测、多少用户单位**；估算常量只能用于「先跑起来」，不能当判据。

## 五 · 产物（关票 31 页，重出后逐页 sha256_12）

`node .scratch/t387/run-all.mjs` ⇒ `RESULT: 31/31 真跑成功`、`PARAM-AUDIT: 31/31`、`bytes_total=2381754`。

| 序 | 文件 | bytes | sha256_12 |
|---|---|---|---|
| 24 | `24-看BMI报告.html` | 92537 | `14b23cfedf3e` |
| 25 | `25-看TDEE报告.html` | 77675 | `f12d2d59644e` |
| 26 | `26-看BMR报告.html` | 87577 | `8164f13149ef` |
| 27 | `27-看蛋白质摄入报告.html` | 84099 | `ff00adaaa786` |
| 28 | `28-看水分摄入报告.html` | 84126 | `2433601bc438` |
| 29 | `29-看综合评分.html` | 79996 | `023f61789262` |
| 30 | `30-看健康趋势.html` | 87099 | `eba9d081ac8d` |
| 31 | `31-看健康报告(含对比).html` | 98339 | `354916e9d318` |

（01–23 与 W4 的产物同源，逐页读数在 `.scratch/t387/results.json`。）

## 六 · GATE-RUN 声明（本窗，只领 exit=0）

对账窗口：`since=2026-09-15T22:47:00.000Z`、`until=2026-09-15T22:58:00.000Z`（＝本窗收尾命令启动前）。
导出件：`docs/skills/skill-calorie/t519-GATE-RUN.log`（**累积窗口**，旧 runId 一条不丢）。

```
GATE-RUN runId=1be8db2c-9fa3-4624-b1b5-43af4bb59896 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=54174e9b-a88c-4b71-b834-140a9f91aabd cmd="node .scratch/t387/run-all.mjs"
GATE-RUN runId=d2117393-d220-4878-b12b-f3b862013b7d cmd="node .scratch/t519/label-geom.mjs --dir .scratch/t387/html --pages 24,26,28,30 --widths 390,512,820,1440 --json .scratch/t519/W6-geom-after.json"
GATE-RUN runId=20819dcd-1f64-42bd-8186-c259ca6ffdfc cmd="node --test packages/skill-calorie/test/analysis-shape-report-520.test.mjs"
GATE-RUN runId=5a321129-d1d1-4d69-86f3-e4ec2ce29d51 cmd="node .scratch/t519/mutate.mjs m13"
GATE-RUN runId=30e4475c-49ec-41d9-ab6c-5695d0be8d4f cmd="node .scratch/t519/mutate.mjs m14"
GATE-RUN runId=48187d46-d922-498b-a40e-f6c9ec63566b cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie"
GATE-RUN runId=6e988c3d-8f44-403a-86ce-47c5bd3bbf28 cmd="node .scratch/t387/run-all.mjs"
GATE-RUN runId=fce7c58f-1171-438e-81f1-c97c65fa6626 cmd="node packages/skill-calorie/scripts/audit-separators.mjs --dir .scratch/t387/html --json .scratch/t519/W6-sep31.json"
GATE-RUN runId=30401d7c-316c-4b7d-a5f7-c7b670b5ac47 cmd="node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t387/html --widths 390,512,820 --json .scratch/t519/W6-measure31.json"
GATE-RUN runId=e0aadfae-e6a0-4c27-8e99-3bdf22241498 cmd="node packages/skill-calorie/scripts/measure-responsive.mjs --dir .scratch/t387/html --widths 390,512,820,1440 --json .scratch/t519/W6-measure31x4.json"
GATE-RUN runId=97690298-ab18-4056-b4c3-dd6a275c7a96 cmd="node --test packages/skill-calorie/test/analysis-report-384.test.mjs packages/skill-calorie/test/analysis-accept-386.test.mjs packages/skill-calorie/test/analysis-shape-report-520.test.mjs packages/skill-calorie/test/analysis-shape-520.test.mjs packages/skill-calorie/test/analysis-deficit-385.test.mjs packages/skill-calorie/test/analysis-deficit-eta-466.test.mjs packages/skill-calorie/test/analysis-predict-383.test.mjs packages/skill-calorie/test/analysis-predict-455.test.mjs packages/skill-calorie/test/analysis-shape-predict-520.test.mjs packages/skill-calorie/test/analysis-tdee-463.test.mjs"
GATE-RUN runId=38e74e72-b68c-4089-b379-e4c276b917bd cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"
GATE-RUN runId=e539ec06-8a4c-42c1-89ed-a5ea1d2dec31 cmd="node .scratch/t519/label-geom.mjs --dir .scratch/t387/html --pages 24,26,28,30 --widths 390,512,820,1440 --json .scratch/t519/W6-geom-after.json"
GATE-RUN runId=e33c95b7-30d7-4d2c-a239-318d711503d9 cmd="node docs/skills/skill-calorie/t516-判据-版式.mjs --dir .scratch/t387/html --widths 390,512,820,1440 --json .scratch/t519/W6-layout.json"
GATE-RUN runId=000fb57d-1052-4cd5-8782-8165f66066e2 cmd="node .scratch/t519/probes.mjs --dir .scratch/t387/html --pages 24,25,26,27,28,29,30,31"
```

**未声称的运行**（按「类别 ＋ 条数 ＋ 日志指针」具名；照裁定 N／I）：

| 类别 | 条数 | 说明 |
|---|---|---|
| **改前对照（受控探针，结论就是「相交」故 exit=1）** | 1 | `runId=ae4b3d1c`（`label-geom --widths 390,512,820,1440` 跑在 12 枚定档上）——**不充作门禁证据**，它是缺陷的现场读数（读数 `.scratch/t519/W6-geom-before.json`） |
| 包内门①的重试（红因＝别席在途件，见 §〇） | 4 | `84027190`／`f1946ea3`／`f2b0ce94`／`476a556f`，逐次都是 `RESULT: 90/91`＋`RED 台账陈化：src/render/sportPortDocs.ts` |
| 别席（复核席）在本窗之前的运行 | 若干 | `.scratch/t387-review/**`／`.scratch/t519-review/**`／`.scratch/t519-review2/**` 的 probe／mutate／commit-report（22:23–22:47），**不是本席的运行**，故不声明 |

```
GATE-RELAX flag=--allow-undeclared reason=裁定 N＋裁定 I 具名授权：上表 5 条（改前对照 1＋包内门①重试 4）＋别席在本窗前的运行按「类别＋条数＋日志指针」具名列出、不逐条 GATE-RUN 声明；本节 16 条判据运行逐条声明且只认 exit=0。
```

## 七 · 未做项与下一手

| # | 未做项 | 为什么 | 下一手缺什么 |
|---|---|---|---|
| 1 | **包内门①现为红**（`RESULT: 90/91`） | 别席在途件 `src/render/sportPortDocs.ts` 台账陈化（台账 809／实况 906，且仍在增长）；本票对该件与台账一字未动 | 该席跑 `--sync` 并提交后本门自回绿；复核席可复跑核对红因归属 |
| 2 | **公共层两项能力缺口** | 柱状图「按密度抽稀的数值标签」与「柱状 y 轴刻度」（裁定 W 具名归 [#567](https://github.com/FeatherHunter/ilife/issues/567)）；末值「只标末值」开关（裁定 X 同归） | #567 |
| 3 | **D1（页 24 口径行分隔）** | 裁定 Y：交视觉复评席点名「哪两段之间缺分隔」（机器读数与 27／28 同形） | 视觉复评席 |
| 4 | **J4／J5** | 公共层只读，已改期 [#567](https://github.com/FeatherHunter/ilife/issues/567) | —— |
