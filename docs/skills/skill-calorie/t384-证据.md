# #384 · 8 报告子形态页：证据件

本件是 #384 的门禁证据与对账读数。**引用冻结读数一律带 runId ＋ 当刻值**（他席在途会让它漂移）。

## 一、判据先行（开工前必须红）

前任与本手共用的可红判据：8 条词在冻结表里当刻 `kind:'non-exec'`、`src/analysis/commands.ts`
14 键无 `calorie.report.*` ⇒ 测试第一条断言「唤醒词没有命令可执行」当场红。

## 二、交接现场度量（本手接手时）

前任（同一票）中途死掉，留下的**无主改动**逐件判定：

| 件 | 状态 | 判定 | 依据 |
|---|---|---|---|
| `src/analysis/routes.ts` | 已改（M） | **接管** | order 331–338 由 non-exec 转 exec，位次不动；8 条各指自己键，逐条对得上票面 8 条任务清单 |
| `src/triggers/scene-10-analysis.ts` | 已改（M） | **接管** | 8 条 `main_prompt.cli`／`data_source` 逐字同步新命令，其余不动 |
| `src/analysis/commands.ts` | 已改（M） | **接管＋定点修** | 8 键声明＋薄转调齐备；`metrics` 投影有 2 处缺陷（见第三节），本手修 |
| `src/analysis/reportPlate.ts`（新） | 未跟踪 | **接管** | 取数底座 8 kind 齐备，语法／类型过 |
| `src/analysis/reportDoc.ts` 等 6 件（新） | 未跟踪 | **接管** | 渲染分片齐备，6 个形态的区块构建函数都存在 |
| `test/analysis-report-384.test.mjs`（新） | 未跟踪 | **接管** | 4 条断言，判据先行可红 |
| `docs/skills/skill-calorie/t384-*` | 不存在 | **缺** | 本手补（本件） |
| `.changeset/t384-*` | 不存在 | **缺** | 本手补 |
| `.scratch/t384/HANDOFF.md` | 不存在 | **缺** | 前任死前来不及写；本手补 |

前任停点（`git log` 无 #384 提交、`.scratch/t384/green-final.log` 与 `green-3.log` 最后两条
各差一个 metrics 键）＝**「形状已跑通、差把 8 条铺绿」**，不是「刚起手」，也不是「散件不可用」。
本手因此**接管而非重做**；未用任何批量还原命令。

## 三、本手修的 2 处缺陷（都属于「形状对、投影错」）

1. **`metrics.weightKg` 在 compare 形态恒缺**（决定性红点，前任日志里就是这个）。
   `viewReport` 只从 `plate.bandPoints` 取体重，而 `bandPoints` 只有 BMI 形态填——compare
   页没有逐日 BMI 点，于是恒 `undefined`。修法：BMI 形态照旧读逐日点，其余形态退回日序列
   末个非空体重（同一个「体重」概念只在一处取）。
2. **`metrics.weakest` 编了个无意义的数**：原式 `weakest.hits * 100 + Math.round(weakest.rate)`
   把「第几项」和「命中率」乘加成一个数（命中率 20.8 会变成 121）。改成**最低分项命中率**本身。
   `plate.items` 已按命中率升序，故语义即「最弱项的命中率」——**不是**老侧「恒定值归一化给 50」那个 hack。

## 四、门禁机器证据（runId ＋ 当刻值）

| 门 | runId | exit | 结论 |
|---|---|---|---|
| 基线 build | `4880cd12-e96b-4b87-8157-0497373d8e74` | 0 | 前任末次红是第三方在途态，本手当刻全仓编译绿 |
| 384 测试（决定性） | `823e8344-0726-4728-adf9-59c1b5656c57` | 0 | `tests 4 / pass 4 / fail 0` |
| 变异（改坏） | `ea0a930b…`（build）＋`a0afafa1-7eec-48bb-a5e4-6ed319c5ad65` | 1 | 见第五节第一行 |
| 还原（回绿） | `328bb81f-b46c-46d8-be9f-4dd88bd319a9` | 0 | `tests 4 / pass 4 / fail 0` |
| `gen:check` | `8e0b04eb-7738-4881-84b2-1da921b64f01` ＋ `306e67e7-4df9-4e4e-882a-c0a8b9887d7d` | 0 | 两次都 PASS：键 127（写 46＋读 81）；`keys.ts` sha256=`568420b6cf6a…`、`routes.generated.ts` sha256=`bcb16fa36ab8…`；未搬迁清单 0 条 |
| 命令登记纪律 4 件合跑 | `a8ea208c-9b44-4798-b6f7-e818156a692e` | 0 | `tests 16 / pass 16 / fail 0`（含 #294「每条声明的代表唤醒词都是真唤醒词且落回同键」、#320「分派层无按键字面量」） |
| 低风险回归（姊妹票 #385） | `1756e9ac-3397-4bbb-8e4d-85f7ae2760ca` | 0 | `tests 6 / pass 6 / fail 0`（未碰 `deficit.ts`／`trendDocs.ts`） |
| 终跑（落盘态，HEAD 上） | `66adb82f-eef6-444c-8c93-f4930be41a32` | 0 | `tests 4 / pass 4 / fail 0` |

### 落盘与一次**他席误并**（如实登记，供对账）

本票的源码／测试／证据件**不是**由本手的提交带上车的：本手 `git add` 了 13 件后，
在他席连续提交（HEAD 从 `0da6472` 漂到 `d910391`）期间，这 13 件被**并进了 `d910391`**
（提交信息写的是 `fix(base-render): …（#457）`）。本手核对过：`d910391` 里的
`commands.ts` 含本手两处定点修（`plate.bandPoints.length > 0` 分支与 `weakest.rate`），
工作树亦无残留改动，故**不重做、不回改**。

三个**派生面**当时没被带上车（HEAD 里 `calorie.report.*` 命中数为 0），新仓一克隆就是 `gen:check` 脏，
故本手另提一次把它补齐：

- **`a18a289`** `feat(384): 8 报告键的派生面落盘（keys.ts／routes.generated.ts／SKILL.md 键表）`
  —— `keys.ts` 8 条、`routes.generated.ts` 331–338 转 exec、`SKILL.md` 键表 8 行。
  落盘后四个面在 HEAD 上命中数：`keys.ts` 8／`routes.generated.ts` 8／`SKILL.md` 9／`analysis/routes.ts` 9。

## 五、变异自证两行（改坏→红→还原→绿）

**第一行（改坏 → 红）**：把 `src/analysis/reportDoc.ts` 的 `KIND_LABELS.score` 由 `'综合评分'`
改成 `'健康总分'` → `pnpm build` → 跑测试：

```
✖ #384 8 条逐条跑通：exit 0＋绝对路径＋完整文档＋各自字段
  AssertionError [ERR_ASSERTION]: 看综合评分 缺页面标题（疑似落回 full 健康盘）
RESULT: ticket=384 runId=a0afafa1-7eec-48bb-a5e4-6ed319c5ad65 exit=1
```

**第二行（还原 → 绿）**：改回 `'综合评分'` → `pnpm build` → 跑测试：

```
✔ #384 判据：8 条词各有命令可执行（routesFor exec＋键逐条对得上）
✔ #384 8 条逐条跑通：exit 0＋绝对路径＋完整文档＋各自字段
✔ #384 拿错页即红：8 条产物互不相同，且都不是 full 健康盘
✔ #384 回归：full 健康盘 11 条窗口词行为不变（calorie.view.health）
RESULT: ticket=384 runId=328bb81f-b46c-46d8-be9f-4dd88bd319a9 exit=0
```

### 变异途中查明的一处**判据弱点**（留给下一手，本手不改测试）

先试的变异是把 `BMI_BANDS` 的正常档标签由 `'正常'` 改成 `'标准区间'`：**测试照样全绿**。
原因是 `html: ['偏瘦','正常','超重','肥胖']` 这组针**不是唯一来源**——BMI 页的 KPI 明细里有一句
硬编码的阈值说明 `偏瘦<18.5 正常<24.9 超重<28 肥胖≥28`，它也含 `'正常'`，于是分级标签改坏了
针照样命中。即：**分级文案那 4 根针实际只验「阈值说明在不在」，不验分级判定**。
分级判定的真变异要用 `expect` 之外的读数（如 `metrics`＋页面结构化字段），或把阈值说明句改成
从 `BMI_BANDS` 派生（那样改标签会连说明句一起变）。本手**不改测试**（越权改判据＝自证失真），
如实登记于此。

## 六、8 条各自状态（现场实跑，非只看结构断言）

同一夹具（100 天窗、档案 30 岁男 175cm moderate、目标 150g 蛋白／2000ml 水）逐条真跑：

| 唤醒词 | 键 | exit | 当刻 metrics（摘） | 页面读数合理否 |
|---|---|---|---|---|
| 看BMI报告 | `calorie.report.bmi` | 0 | `bmi=21.9`、`heightCm=175`、`weightKg=67.05`、`points=90` | 21.9 落「正常」档 ✔ |
| 看TDEE报告 | `calorie.report.tdee` | 0 | `tdee=2510`、`avgIntake=1636.7`、`activityFactor=1.55` | 4 KPI 无图 ✔ |
| 看BMR报告 | `calorie.report.bmr` | 0 | `bmr=1619`、`underBmrDays=4` | ≥3 天 ⇒ 危险信号段触发 ✔ |
| 看蛋白质摄入报告 | `calorie.report.protein` | 0 | `avgProtein=140`、`proteinGoal=150`、`hitDays=0`、`hitRate=0` | 日均 140＜目标 150 ⇒ 0% 达标，自洽 ✔ |
| 看水分摄入报告 | `calorie.report.water` | 0 | `avgWater=2100`、`waterGoal=2000`、`hitDays=30`、`hitRate=100` | 2100≥2000 ⇒ 100% ✔ |
| 看综合评分 | `calorie.report.score` | 0 | `score=67`、`historyDays=30`、`weakest=0` | 67＝4/6 命中折算 ✔（见下注） |
| 看健康趋势 | `calorie.report.trend` | 0 | `earlyAvg=67`、`lateAvg=67`、`turns=0`、`seriesDays=90` | 恒定夹具故无拐点 ✔ |
| 看健康报告(含对比) | `calorie.report.compare` | 0 | `days=7`、`weightKg=67.05`、`deltaTdee=-46`、`deltaWeightKg=-0.4`、`deltaCalorie=-485.7` | Δ 三项自洽 ✔ |

> 注：`weakest=0` 是**夹具**使然（六因素里「饮水达标」在该夹具下命中率 0），不是底盘编的恒定值。
> 换算口径 `score = 命中项数/6 × 100`；夹具命中 4 项 ⇒ 67。

实跑脚本落 `.scratch/t384/verify-8kinds.mjs`（可复跑：`node .scratch/t384/verify-8kinds.mjs <临时目录>`）。

## 七、三条硬约束的核验

1. **不自造样式第二份表**：技能侧 6 件渲染件颜色字面量**实测 0**（正则扫 `#[0-9a-fA-F]{3,8}｜rgb()｜hsl()`，
   命中仅在**文档注释**里提「颜色字面量 0」几个字，无一是取值）。样式一律经既有 `assembleDocPage`，
   其内部第 88 行＝`buildStyleSheet().css + blocksCss()`（本图未碰 `extraCss`、未改 `:root`、未新增色彩表、`--blue` 未改值）。
   技能侧 6 件 `templates/*.html` 实测颜色字面量也是 0，且**新页根本不走模板**（走 `assembleDocPage`），
   故它们是死模板、不是新页样式来源。
2. **老侧 token 未移植**：新 7 件里 `--ink`／`--r-sm`／`--ease`／三档阴影零命中（只作设计意图参考）。
3. **未动公共层契约**：8 个折线一律传 `yTicks: 3`＋`format`（`reportDocParts.ts` 的 `lineOf` 统一给），
   补在调用侧，未改 `base-paint` 的 `spec/charts.ts`。

## 八、未做项 / 下一手缺什么

> 交棒文书另落 `.scratch/t384/HANDOFF.md`。**注意 `.scratch/` 在 `.gitignore` 内**（`.gitignore:5`），
> 故它是工作区实物、不进提交——下一手换机器就取不到，关键停点本件已复述。

- **告警线台账**：`check-warning-line.mjs` 当刻红，4 条「台账陈化」点名
  `src/weight/history.ts 670≠681`、`src/weight/review.ts 590≠599`、`src/weight/compare.ts 547≠555`、
  `src/analysis/commands.ts 437≠443`。其中**只有 `commands.ts` 是本票写集内的**；其余三件属 #340／#335
  在途改动（`git log` 点名），**不在本票路径所有权内，本手不写它们的数**（按票面纪律，他人在途 hunk 不得提交）。
  `src/analysis/commands.ts` 那一行在本手作业期间已由他席更新为 443（见包内 `AGENTS.md` 台账当前值），
  `src/analysis/reportPlate.ts｜393` 一行亦已在册且现值相符。
  ⇒ **本手交工时该门仍红，红因全在票外三件**；他席收工后跑一次 `node packages/skill-calorie/scripts/check-warning-line.mjs`
  即可判定（若那三件数再变，用 `--sync` 但不提交本票路径）。
- **`SKILL.md` 落盘时夹带了他席内容**（`a18a289` 里如实登记）：这份聚合快照当时已有他席在途改动，
  本票只能整件落盘（派生面落单件会破坏 `gen:check`）。夹带两类：
  ① #282／285 席的 2 行改名（`记身材照`→`卡路里HELP`、`做身材照GIF`→`生成身材照GIF`）；
  ② 该席**部分重生成**造成的 3 行退化——`看GIF规划器`／`看身材照向导`／`选身材照` 三个显示名
  被写成了裸键名（`calorie.view.gif-planner` 等）。**本票不代签**，只随快照落盘；
  他席收工重跑 `pnpm gen` 即归一。
- **判据弱点**（第五节末）未改测试，留给下一手决定。
- **`calorie.report.compare` 缺省窗口 7 天**：照老侧 `data_source` 口径。前任写的 `commandOf`
  用的是半角转义引号（`'… --params \'{"window":"7d"}\''`，与 `routes.ts` 的 `cli` 同形），
  与测试第 163 条断言 `html.includes('calorie.report.' + kind)` 一并成立 —— 页面底部「重跑这条报告」
  是**照抄即能跑**的命令原文。本手未改这段（`git diff` 里它是 `+` 行，属前任产物）。

## 整改更正（2026-09-14）

### D2 更正：本件 §六 把 `deltaTdee=-46` 判为「Δ 三项自洽 ✔」是**误判**

- **原文判定作废**。原文（本件 §六 第 8 行，`compare` 那一行）写：`days=7`、`weightKg=67.05`、
  `deltaTdee=-46`、`deltaWeightKg=-0.4`、`deltaCalorie=-485.7` ⇒ 「Δ 三项自洽 ✔」。
- **为什么作废**：那个 `-46` 不是自洽证据，**是本缺陷本身的指纹**。它＝`2510 − 2556`，
  其中 `2556 = round((10×70 + 6.25×175 − 5×30 + 5) × 1.55)`——**用 70 kg 虚构体重算出、与窗口和用户真实体重都无关的常量**；
  本期 `2510` 则是该夹具档案口径的值。判「自洽」时只核了三个数「看着像一回事」，
  **没有把任何一个数追到它的取数来源**（这正是复核员三路取证抓到的那条 S1 用户可见错读数）。

### 缺陷编号与修法（#384f）

- **缺陷编号**：#384 在途件引入的 S1 用户可见错读数（MAP #161 独立复核 #384 八报告页抓到）。
  位置：`packages/skill-calorie/src/analysis/reportPlate.ts` 的 `comparePlate` 里「日均总消耗」一行——
  原式 `row('日均总消耗', base.profile.tdee ?? seriesAvg(base.series, 'tdee'), seriesAvg(prevSeries, 'tdee'), '卡')`，
  对比期读的是 `buildSeries` 的 `tdee` 字段，而该字段每行恒为 `loadProfileTdee(db)`
  （体重恒 70.0 parity，`packages/skill-calorie/src/analysis/series.ts:172-176`）。
  ⇒ 对比期是与体重无关的常量，**Δ 恒 +79「上升」**，且 |Δ| 最大 ⇒ 霸占「前 3 项变化量」头条与页头摘要。
- **修法（首选：两期同源真体重口径）**：新增 `energyOfWindow(prof, series)`（「档案四要素 ＋ 该期窗口内最后一次称重」，
  内部仍是原来底座那一句 `energyOf(...)`）；底座改调它（本期逐值不变）；对比期改调
  `energyOfWindow(loadProfile(db), prevSeries).tdee`，**不再读序列的 `tdee` 字段**。
  序列 `tdee` 的 70 kg parity 是既有设计（deficit 全窗口静态值依赖它），本票不动 `series.ts`。
  口径注同步写实（`reportDocScore.ts` 的「日均总消耗怎么算」一条）。
- **复跑读数**：7d Δ **-4**、30d Δ **-23**（补前两窗都是 **+79**）；产物中 `+79` 出现次数 4→**0**。
- **老侧对照**：老侧 `scripts/render_analysis.py:438-448` 的 compare 只有六项、**没有「总消耗／TDEE」这一项**；
  老侧 `scripts/analysis/series.py:85-95` 的体重同样回落 `70.0`（因 `scripts/profile.py:205-213` 的 `get_profile()`
  不含 `latest_weight_kg`），但它从不拿这个常量做两期相减。故本修法是补新侧自己引入的错，不偏离老侧口径。
- **回归覆盖**：`test/analysis-report-384.test.mjs` 增一条 `#384f` 断言（7d／30d 该行 Δ 均按权威声明
  Mifflin-St Jeor 独立复算对账 ＋ 两窗 Δ 不同 ＋ 不得等于 79）；变异自证：改坏该行 ⇒
  `deltaTdee=-46 期望=-5` 红，点名还原 ⇒ 5/5 绿。

