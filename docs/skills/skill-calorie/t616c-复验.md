# t616c 复验 · 616实施席独立重跑（源码已在HEAD，不新增改动）

> 背景：本席认领 #616 时源码改动已落盘（`ee4c795`，其后该件零提交：`git log ee4c795..HEAD -- <件>` 为空；
> 进场 sha256=`ace1b191e70d7a5c` 与 `t616-证据.md §五` 进场值逐字相同）。本席做**只读复验**：
> 票面五条逐条重跑（runId 前缀 `t616c-`），源码、测试、他席件一行未改。
> 主证据见 `docs/skills/skill-calorie/t616-证据.md`；本件只记本窗读数与对账。

## 一、回执四段

**第一行：PASS（复验）。** 编译 exit 0；两页 H1 无日期 4/4 绿、正文首件含日期 4/4 绿；
三件测试 34/34/0 exit 0；场景10 回归 82/79/3（三红与主证据同名，他席陈旧，只点名不代修）；
变异自证 MUT-A 红 2 → MUT-B 红 4 → 还原绿 0，MUTATION-PASS，还原后与进场逐字节一致。

**第二段：机器证据。** 沿用提交 `ee4c795`（源码）、`581ff47`／`2b67897`／`9b94a9c7`（主证据链）；
本窗新增提交见本笔（本件）。GATE-RUN 对账（对账源 `.scratch/locks/gate-runs.log:19123-19160`，原文抄录见第三节）：

| 本窗趟次 | GATE-RUN runId | RESULT／退出码 |
|---|---|---|
| 编译 `node node_modules/typescript/bin/tsc -b packages/skill-calorie --force` | `t616c-build` | waitedMs=20004 **exit 0** |
| 两页判据探针 `node .scratch/t616/probe.mjs --quiet` | `t616c-probe` | waitedMs=10002 **exit 0** → `RESULT: 4/4 PASS` |
| 三件测试 `node --test test/{t511-清尾二,render-t9,trend-misc-port-113}.test.mjs` | `t616c-trio` | waitedMs=30002 **exit 0** → tests 34／pass 34／fail 0 |
| 场景10 回归 `node --test test/analysis-*.test.mjs` | `t616c-analysis` | waitedMs=0 **exit 1** → 82／79／3（三红同名，见第四节） |
| 变异自证 `node .scratch/t616/mut.mjs` | `t616c-mutation` | waitedMs=20001 **exit 0** → MUT-A=2／MUT-B=4／还原一致=true／MUTATION-PASS |

**第三段：逐条路径。**

- 改动件：无（本窗零改动；`packages/skill-calorie/src/render/trendMiscPortDocs.ts` 与 `ee4c795` 逐字节相同，
  `git diff ee4c795 HEAD -- <件>` 为空；两件测试一字未动）。
- 证据件（本窗新件）：`docs/skills/skill-calorie/t616c-复验.md`（本件，含本窗 GATE-RUN 对账）。
- 日志件（`.scratch/t616/`，不入仓）：`verify-616c-build.log`／`verify-616c-probe.log`／
  `verify-616c-trio.log`（tests 34／pass 34／fail 0）／`verify-616c-analysis.log`（82／79／3）／
  `verify-616c-mut.log`（MUT-A=2／MUT-B=4／MUTATION-PASS）。
- 一行未动：`analysis/**`、取数口径、路由、`base-render/**`、来源脚注行、场景10 断言、他席在途件
 （含工作区他席未提交的 `packages/skill-calorie/src/diet/rankingDocs.ts`，本席未碰）。

**第四段：未做项与下一手缺什么。**

- 未做：① 未改源码／测试——改动已在 HEAD 且全绿，重改即 churn，按「不加戏」处置。
  ② 未跑 `scene02-验收墙` 重出——不在本票写集，归 #619。
- 他席红（只点名，不代修）：`analysis-t5.test.mjs` 三条——`weightCompare：17 场景分发`／
  `weightCompare：平台期/极端月/同期`／`simulate：外推+模拟+摄入`（与主证据 §2.4① 同名；#603 复核席已记范围外）。
- 下一手：复验墙＋复拍归 #619；本票不推送（总工统一收）。

## 二、两行读数（本窗实测）

```
H1-NO-DATE   key=calorie.view.nutrition-analysis h1="营养分析" 命中日期=0 判=PASS
BODY-HAS-DATE key=calorie.view.nutrition-analysis 首件窗口条=dui-window✓ 首件日期数=2 正文日期=["2026-09-09","2026-09-15"] 判=PASS
H1-NO-DATE   key=calorie.view.six-factors h1="每日六因素" 命中日期=0 判=PASS
BODY-HAS-DATE key=calorie.view.six-factors 首件窗口条=dui-window✓ 首件日期数=1 正文日期=["2026-09-15"] 判=PASS
MUT-A 变异A红条=2（期望＝2：两页 H1 各一条）
MUT-B 变异A+B红条=4（期望＝4：两页 H1 与正文首件各一条）
RESTORE-SELFCHECK 还原一致=true 还原后红条=0
```

## 三、运行记录原文（`.scratch/locks/gate-runs.log:19123-19160` 抄录）

```
RUN ticket=616 runId=t616c-build cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force" waitedMs=20004 exit=0 pid=44600 at=2026-09-16T09:17:07.298Z
RUN ticket=616 runId=t616c-probe cmd="node .scratch/t616/probe.mjs --quiet" waitedMs=10002 exit=0 pid=16684 at=2026-09-16T09:17:31.659Z
RUN ticket=616 runId=t616c-trio cmd="node --test packages/skill-calorie/test/t511-清尾二.test.mjs packages/skill-calorie/test/render-t9.test.mjs packages/skill-calorie/test/trend-misc-port-113.test.mjs" waitedMs=30002 exit=0 pid=32068 at=2026-09-16T09:18:19.274Z
RUN ticket=616 runId=t616c-analysis cmd="node --test packages/skill-calorie/test/analysis-*.test.mjs" waitedMs=0 exit=1 pid=71084 at=2026-09-16T09:18:52.168Z
RUN ticket=616 runId=t616c-mutation cmd="node .scratch/t616/mut.mjs" waitedMs=20001 exit=0 pid=33352 at=2026-09-16T09:19:32.684Z
```
