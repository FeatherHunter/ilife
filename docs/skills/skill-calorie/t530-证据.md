# t530 · 场景09 视觉终审 · 证据件（2026-09-16 重跑）

正本报告：`docs/skills/skill-calorie/t530-场景09-视觉终审.md`。本件只记**跑过什么、读数是几**，供复核逐条复跑。

## 一、跑过的命令与读数

| # | 命令 | 读数 |
|---|---|---|
| 1 | `node .scratch/t530/regen-read.mjs`（只读页 11 件重出，夹具＝`.scratch/t287/backup-real`） | 11 件 `exit=0`；09-05／09-08／09-14 三件按当刻源码变了字节 |
| 2 | `node packages/skill-calorie/scripts/measure-responsive.mjs --dir "D:\ilife-验收\scene09" --json .scratch/t530/measure-after.json` | `OVERFLOW-ZERO pages=17 cells=51 failed=0` |
| 3 | `node packages/skill-calorie/scripts/audit-separators.mjs --dir "D:\ilife-验收\scene09" --json .scratch/t530/sep-after2.json` | `RESULT: 13/17`；非零四件＝导航页 82、09-07 1、09-14 1、09-05 1（定性见报告 §三） |
| 4 | `node docs/skills/skill-calorie/t530-墙生成器.mjs --stage "D:\ilife-验收\scene09" ".scratch/t351-preview\wall"` | `16 格；链接 80 条；缺失 0 -> 可发`，exit=0 |
| 5 | 同件 `--check` 同上目录 | exit=0 |
| 6 | **反例A**：册子加一行 `09-99-不存在的件.html` → `--check` | exit=1，「册子点名而盘上没有」 |
| 7 | **反例B**：手机墙一条链接改成 `09-01-不存在的件.html` → `--check` | exit=1，「墙页引用而盘上没有」 |
| 8 | 复位：重跑 `--stage` → `--check` | exit=0（两条反例都回绿） |
| 9 | `node .scratch/t530/serve-preview.mjs 43210`（只读 GET，根 `.scratch/t351-preview`） | `PREVIEW-READY http://127.0.0.1:43210/` |
| 10 | `pwsh -NoProfile -File .scratch/t530/check-http.ps1 -Base http://127.0.0.1:43210` | `合计 19 条 200 的 19 条 非 200 0 条 字节与本地不一致 0 条`；`RESULT: HTTP-OK total=19 nonOk=0 mismatch=0`，exit=0 |
| 11 | `node packages/skill-calorie/scripts/gen-photo-baseline.mjs --check` | `BASELINE-DIFF 10 pages`，exit=1；另报 `UNSTABLE calorie.photo.remove／calorie.photo.tag：渲染当刻时钟未被剥净` |
| 12 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | RED 一项：`src/photo/helpCenter.ts 台账=590 实况=593`（**别席在途未提交件**，非本票范围；本票未改该件） |

## 二、样张与墙的落点

- 16 件样张：`D:\ilife-验收\scene09\`（字节表见报告 §二·5）
- 墙与册子（墙 ＋ 16 件产物同目录）：`.scratch/t351-preview\wall\`（`场景09-手机墙.html`／`场景09-桌面墙.html`／`场景09-总索引.html` ＋ `清单.json`）
- 预览台：`http://127.0.0.1:43210/wall/场景09-总索引.html`（手机墙／桌面墙同目录）

## 三、变异自证（两行）

```text
MUTATION-RED  反例A 册子点名缺件 → exit=1（点名 09-99-不存在的件.html）
MUTATION-RED  反例B 墙页引用死链 → exit=1（点名 场景09-手机墙.html -> 09-01-不存在的件.html）
RESTORE-GREEN 两条反例后重跑 --stage／--check → exit=0
```

## 四、未做项（如实登记）

1. **逐页 vision 打分**：本会话无 `vision_*` 工具，未出分。
2. **基线重落**：未做（须先补 `normalize()` 的时钟剥除规则，再 `--write` ＋ 两次 `--check`）。
3. **写类五页（09-09～09-13）未随本轮重出**：沿用 09-15 那一批；本轮源码改动不涉及这五页。
4. **导航页 82 处债**与 **09-05 备注例外**：未动，等负责人一句话（P4／例外登记）。
5. 未跑 `check-scene09-links.mjs`（旧布局脚本，`.scratch` 件）：其职责（墙／索引引用逐条存在）
   已由**仓内** `t530-墙生成器.mjs --check` 承担，且带两条反例自证；旧脚本布局已与新生成器不符。
