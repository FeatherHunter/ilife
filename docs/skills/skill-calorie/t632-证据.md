# t632 证据（D4 榜单餐均列单位去重）

票面：`gh issue 632`（终审 D4，榜单表「餐均」列值 `620卡/餐` 与列名语义重复；390 档卡片同病）。派单与票面一致，无冲突。

## 1. 认领

- 首动作 `gh issue edit 632 --add-assignee "@me"` 已执行（返回 `https://github.com/FeatherHunter/ilife/issues/632`）。
- 备注：查票时 assignees 仍仅列 `FeatherHunter`——认领命令已按并发硬规矩首发，若身份映射未生效请编排者复核，不影响本票改动面。

## 2. 改动件（src 一行＋靶向 test 一件）

- `packages/skill-calorie/src/diet/rankingDocs.ts:156`：`avg: it.avgCalPerMeal + ' 卡/餐'` → `avg: it.avgCalPerMeal + ' 卡'`。
  - 票面写集为「108–122 行列定义与值渲染」；实盘 `avg` 值渲染住 `rowOf()`（148–159 行，行号较票面漂移，同一函数族、同一文件内），列定义（`columnsOf` 110–126）一字未动。声明：未超文件级写集。
- `packages/skill-calorie/test/t632-avg-unit.test.mjs`（新建，6 条）：三类有餐均列单榜页无 `卡/餐`（桌面表＋390 卡片同源断言 `data-label="餐均"` 格）／全榜页同断言／复盘页只复核／变异自证（改回即红）。

## 3. 运行记录导出（§2.3.4 对账源，抄自 `.scratch/locks/gate-runs.log` 对应行）

```text
RUN ticket=632 runId=0282cfaf-3a5b-46f4-aa3a-6c3e9c4ac762 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force" waitedMs=0 exit=0
RUN ticket=632 runId=2de4d69d-0b5b-4a20-8065-7c3f42569843 cmd="node --test packages/skill-calorie/test/t632-avg-unit.test.mjs" waitedMs=0 exit=0
RUN ticket=632 runId=21353821-20b7-4766-bee6-110e05d563f3 cmd="node --test packages/skill-calorie/test/t272-排行榜页.test.mjs packages/skill-calorie/test/t272-空窗两态.test.mjs packages/skill-calorie/test/t273-review-meal.test.mjs packages/skill-calorie/test/diet-homogeneity-108.test.mjs" waitedMs=0 exit=0
RUN ticket=632 runId=77426ae8-ce09-47c5-8422-43f3a851c8b0 cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force" waitedMs=1 exit=0
RUN ticket=632 runId=934c7545-5789-497f-a7e6-4b1469a21a41 cmd="node --test packages/skill-calorie/test/t632-avg-unit.test.mjs" waitedMs=10000 exit=1
RUN ticket=632 runId=8a0a46c2-3013-470c-9d88-02ffe0e59e3c cmd="node node_modules/typescript/bin/tsc -b packages/skill-calorie --force" waitedMs=10001 exit=0
RUN ticket=632 runId=ad92b233-81af-406c-a48d-59b29b098efc cmd="node --test packages/skill-calorie/test/t632-avg-unit.test.mjs" waitedMs=80012 exit=0
```

读数：靶向 `pass 6/fail 0`；回归 `pass 74/fail 0`；变异红 `pass 1/fail 5`（绿的 1 条是 reviewDocs 只复核项）→ 还原 `pass 6/fail 0`。编译入口均为 `node node_modules/typescript/bin/tsc`，未用 `npx`；未执行 `pnpm install`。

## 4. 未做项与下一手

1. `src/render/html.ts:378`（旧口 `renderRankingHtml`）：`it.cnt + ' 次·均 ' + it.avgCalPerMeal + ' 卡/餐'` 同形残留——超出本票写集未碰。若该旧口仍在线 serving，需另开一票（或由页族票认领）做同式去重；若已下线，忽略。
2. `src/diet/reviewDocs.ts:160`：票面要求只复核不改——已复核（无重复），未改。
3. 页族捎带检查：`rankingDocs.ts` 近史（`5201dfdd` 等）未动 `avg` 行，无页族票捎带本修复，本票独立交付，不引用关闭。
4. 审查者需持锁独立复跑 §3 的 7 个 runId 所涉命令；本席未兼任审查。
5. 未推送（总工收口统一推）；提交 sha 清单见回执。
