# t285 · 身材照片工作流落点（SKILL.md 常驻规则＋包内披露）证据

## 目标与做法（一句话）

`packages/skill-calorie/SKILL.md` 里身材照片一段只留常驻规则 D2–D7＋一行指针，逐词工作流下放包内披露文件 `packages/skill-calorie/workflows/09-身材照片.md`（10 个场景每条从哪一步往下走）。

新命令名抄 #283 冻结声明，不增删：`calorie.view.photo-picker`（代表唤醒词已在 #159 词表订正里去掉——老技能这个页面是流程内页；命令名与 `example` 原文 `calorie-cmd-read calorie.view.photo-picker` 一字未动）。

## 为什么披露（指针措辞）

- 按 `/writing-for-agents`：常驻只放常驻规则＋一条指针，逐场景工作流下放成披露文件，命中指针才去读。老 SKILL.md 长到 158 KB 就是全塞正文的结果，本票不重蹈。
- 指针措辞（SKILL.md「场景 09 身材照片常驻规则与工作流指针」节，AUTO 块之外）：
  - `逐词工作流（10 个场景每条从哪一步往下走）：见 \`workflows/09-身材照片.md\`（随包安装，SKILL.md 只留本节常驻规则＋这一行指针）。`
- 随包安装理由：安装器只装 `packages/skill-calorie/` 这一层，`docs/` 进不了 agent 读得到的范围，故披露文件住包内 `workflows/`，不进 `docs/`。

## 10 场景索引（t170 四列对照表顺序，披露文件同序同字）

| # | 场景 | 底层唤醒词 → 命令 |
|---|---|---|
| 1 | 存一张照片 | 记身材照 → `calorie.photo.add` |
| 2 | 存照片（含备注） | 记身材照 → `calorie.photo.add`（带 `--note`） |
| 3 | 批量存照片 | 记身材照 → `calorie.photo.add`（多 `srcPaths`） |
| 4 | 看身材照 | 查身材照 → `calorie.photo.list`／单张 `calorie.photo.detail` |
| 5 | 对比两张照片 | 对比两张照片 → `calorie.photo.compare` |
| 6 | 生成身材照GIF | 生成身材照GIF → `calorie.photo.gif`（前置 `calorie.view.gif-planner`） |
| 7 | 删身材照 | 删身材照 → `calorie.photo.remove`（前置 `calorie.view.photo-picker`） |
| 8 | 改照片标签 | 改照片标签 → `calorie.photo.tag`（`op=set`） |
| 9 | 加照片标签 | 加照片标签 → `calorie.photo.tag`（`op=add`） |
| 10 | 删照片标签 | 删照片标签 → `calorie.photo.tag`（`op=remove`） |

入口词覆盖（t400 §六-补 十条唤醒词形态表同形）：老技能场景 09 只有 8 个词——记身材照／查身材照／对比两张照片／生成身材照GIF／删身材照／改照片标签／加照片标签／删照片标签；真名另有「存身材照」与「看身材照」（`_triggers.py:2669`／`:2699`）。三个流程内页（预检确认页／GIF 规划页／候选页）没有唤醒词，披露文件里用页面本身的叫法、不把它们当入口词。本件曾按老仓「十个别名」写，其中 6 个在老技能 0 命中，已在 #159 词表订正里换成老技能真有的词（读数见 `docs/skills/skill-calorie/t450-词表订正.md`）。

## t400 基准对照（条款号精确）

> **核对注（本件按 #479 逐词核对，只核不改口径）**：① 本件用的命令名与入口词与当刻盘上一致——
> 全文只有一处提到自造入口词，且那处正是说明它**已在 #159 词表订正里去掉**；`packages/skill-calorie/`
> 下（`wake-assets.ts`／`SKILL.md`／`workflows/09-身材照片.md`）现无「选身材照」「看身材照向导」字样。
> ② 下面几行引的是**披露文件的口径措辞**（「跨标签警告」「首尾禁用」这类口径词），#472／#473 换的是
> **页面上的句面**（对比页提醒改人话「这两张的角度不一样…」、详情页首尾改「已是第一张／已是最后一张」）；
> 披露文件与 SKILL.md 属包内说明面，本票不动，只在此登记两者关系。

- 裁定 2（配置型 prompt 即 CLI）：披露文件场景 7–10 均写“先跑候选页看快照→复制 prompt→再走写命令”，复制的即写命令照抄即跑形状（含 `--params` 与选中 id）；缺项时为指步骤占位句。过程型页验复制 prompt 的口径与 #283 同形。
- 裁定 4（浏览链与可比性）＋ §七：对比跨标签警告＋间隔 N 天、详情上一张／下一张首尾禁用，披露文件场景 4–5 照录，不另定口径。
- §二序 2（配置型回填 prompt 即 CLI）＋ §二-补（复制按钮文案不夸大，只称复制不称发送）：披露文件只写复制，不写发送。
- §三序 8（两张过程型页已是完整文档）：预检确认页／GIF 规划页照此形状复用，新过程型页候选页同形（`pickerDoc.ts` 经 `assembleDocPage` 出完整文档）。
- §六-补（十条唤醒词形态表）：命令名以 `photo/commands.ts:25-35` 为准，路由表的唤醒词以 `photo/routes.ts:11-31` 为准，本件未加命令。路由表里那批自造词（本票不动）另开票清理。
- §五序 1（HELP 载体未盘点）：另开票，不进本票。

## 验收命令 exit 码＋读数（持锁窗口实测）

- `node --test packages/skill-calorie/test/skill-md-photo-285.test.mjs` exit=0：tests 6／pass 6／fail 0（含票面 4 条＋手写区 2 条；变异 `MUTATION-POINTER ok（改名必红）`）。
- `node packages/skill-calorie/scripts/build-help.mjs`（刷新 AUTO 块）exit=0：仅 `身材照片 HELP 模块` 行加 `/picker`，手写区三处存活。
- `pnpm gen:check` exit=0：`GEN-CHECK PASS：键 119（写 46 ＋ 读 73）`；五件生成物全绿（含本票改的 `build-help.mjs` 手写行，REPR／EXAMPLE 零手改）。
- `pnpm help:examples:check` exit=0：`RESULT: 119/119`，`PASS: SKILL.md 示例逐行可执行`。
- 等锁读数：`gen:check` 窗 `waitedMs=10002`（同仓 #281 持锁中，按纪律等，不绕）。

GATE-RUN 单行（运行记录 `.scratch/locks/gate-runs.log` 可复核）：

- `GATE-RUN runId=79f696a9-66cd-4692-a4d2-c276da42c1e2 cmd="node --test packages/skill-calorie/test/skill-md-photo-285.test.mjs" waitedMs=0 exit=0`（票面验收命令）

## 逐条路径（改了哪些文件各一句话）

**（本段是 #285 当时的写集记录：下文 `SKILL.md` 那行里出现的「选身材照」是当刻写进去的括注，#159 词表订正后
已不在盘上；本行未回改，只在此说明。）**

- `packages/skill-calorie/SKILL.md`（改手写区三处，AUTO 块未手进）：`:72` verify 映射追加第三键 `calorie.view.photo-picker`（选身材照；删身材照与标签三条的候选快照）；`:212`「照片 10 键」→「照片 11 键」；HELP 节后新增「场景 09 身材照片常驻规则与工作流指针」节（D2–D7＋指针行，AUTO 块之外）；`:204` AUTO 行经生成器变为 `gallery/compare/viewer/gif/picker`。
- `packages/skill-calorie/scripts/build-help.mjs`（改 1 行手写行）：`:281` HELP 模块行加 `/picker`，同步新子功能文件 `picker.ts`／`pickerDoc.ts`；REPR／EXAMPLE 标记块零手改。
- `packages/skill-calorie/workflows/09-身材照片.md`（新）：10 个场景逐条在册，每条给唤醒词→命令→从哪一步走，D2–D7 照录，`calorie.view.photo-picker` 键名抄 #283 声明。
- `packages/skill-calorie/test/skill-md-photo-285.test.mjs`（新）：票面 4 条＋手写区 2 条，纯文档断言，不碰库。
- 本证据件 `docs/skills/skill-calorie/t285-工作流落点.md`（新）：为什么披露、指针措辞、10 场景索引。
- 生成物（`build-help.mjs` 派生，人未手改 AUTO）：`SKILL.md` AUTO 块 1 行（`:204` 加 `/picker`）。

## 未做项与下一手

- 未做：其余 9 个场景的同类工作流不在本票；若发现同一段规则被多个场景共用，当场提上移成技能级常驻的建议——本轮未发现需上移项（D2–D7 均为身材照片专用）。
- 未做：`package.json` 的 `files` 未加 `workflows/`（随包安装按“装整层”口径已满足，是否补 `files` 由安装器主人裁定，不在本票夹带）。
- 审查席重点：指针行是否逐字含 `workflows/09-身材照片.md` 且解得到真实文件；披露文件 10 场景是否逐字在册且每条有可执行命令形；D7 是否逐字含「交给用户，不只给路径」；`:72` 第三键与 `:212` 11 键是否在手写区、AUTO 块是否零手改（`git diff` 里 AUTO 块仅 `:204` 1 行派生变化）。
- 真库只读：全程未跑写命令，`SKILLS_DB_PATH` 未指向真库；测试为纯文本断言，零建库。

## 结构纪律五步对账

影响清单（第一步）即「逐条路径」节；本票只碰身材照片说明面（`SKILL.md` 手写区）、包内披露目录（`workflows/` 新目录＋1 件）、根测试 1 件、归属文档 1 件与生成器手写行 1 行，未碰命令声明、未手改共享生成物、未碰 `src/triggers/routing.ts` 与棘轮文件，老技能目录只读（`.个人笔记不允许参考/` 未读）。
新增目录树：`packages/skill-calorie/workflows/`（新目录，内仅本票 1 件）。公开接口：无（文档票，未加命令与函数）。文件行数：披露文件约 90 行、测试约 80 行，均远低于 350 行告警线（说明面与测试件不计入告警范围）。
交付对账（第五步）：本清单即账，偏差为零。
