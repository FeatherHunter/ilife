# t632 复验（D4 榜单餐均列单位去重）

判定：PASS（自验；待审查者独立复跑）。

## 1. 改动（一行，值去单位）

- `packages/skill-calorie/src/diet/rankingDocs.ts:156`
  - 改前：`avg: it.avgCalPerMeal + ' 卡/餐'`（列头 `餐均` ＋ 值 `620卡/餐` ＝ 语义重复；390 档卡片由同一格 `td data-label="餐均"` 经 `td::before{content:attr(data-label)}` 出「餐均 620 卡/餐」，同源同病）。
  - 改后：`avg: it.avgCalPerMeal + ' 卡'`。
- 选「值去单位」不选「列名改餐均(卡)」的理由：既有靶向测试（`t272-排行榜页`、`t273-review-meal`）已把列头 `餐均` 钉死；且 `reviewDocs.ts:163` 同形位早已是 `+ ' 卡'`，本改动使两处同式，全榜族（五类榜共用 `rowOf`）一次对齐。

## 2. 门禁（GATE-RUN 对账，源 `.scratch/locks/gate-runs.log` 行号见证据件）

| # | GATE-RUN runId | 命令 | 退出码 | 读数 |
|---|---|---|---|---|
| 1 | `0282cfaf-3a5b-46f4-aa3a-6c3e9c4ac762` | `tsc -b packages/skill-calorie --force` | 0 | exit 0 |
| 2 | `2de4d69d-0b5b-4a20-8065-7c3f42569843` | 靶向 `t632-avg-unit.test.mjs` | 0 | pass 6 / fail 0 |
| 3 | `21353821-20b7-4766-bee6-110e05d563f3` | 回归 `t272-排行榜页`＋`t272-空窗两态`＋`t273-review-meal`＋`diet-homogeneity-108` | 0 | pass 74 / fail 0 |
| 4 | `77426ae8-ce09-47c5-8422-43f3a851c8b0` | 变异重编（改坏态） | 0 | exit 0 |
| 5 | `934c7545-5789-497f-a7e6-4b1469a21a41` | 靶向测试＠改坏态 | 1（预期红） | pass 1 / fail 5 |
| 6 | `8a0a46c2-3013-470c-9d88-02ffe0e59e3c` | 还原重编 | 0 | exit 0 |
| 7 | `ad92b233-81af-406c-a48d-59b29b098efc` | 靶向测试＠还原态 | 0 | pass 6 / fail 0 |

- 变异两行：`MUT-RED fail=5（唯一仍绿的是 reviewDocs 只复核项，符合预期）` ／ `MUT-GREEN pass=6 fail=0`。
- 响应式：390 卡片与桌面表同源（同一 `td`），不断点新增；`data-label="餐均"` 格断言即卡片态断言。真浏览器三档溢出量尺（`measure-responsive.mjs`）未跑——一字文本改动不碰任何形状，不断言形状。
- 日志：`.scratch/t632/`（`tsc.log`／`t632-test.log`／`regress.log`／`mut-*.log`，git 忽略，只看尾 5 行已验）。

## 3. 未动项

- `src/diet/reviewDocs.ts:160` 同形位只复核不改（复盘页 TOP5 `餐均` 列本就无重复，靶向测试第 6 条钉住）。
- `src/render/html.ts:378` 旧渲染口 `renderRankingHtml` 仍有 `卡/餐` 字面—— legacy 面，不在本票写集，要改另报（见证据件 §4）。
- 不改取数／排序逻辑、base-render、他席件。
