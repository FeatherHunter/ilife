# #367 呈现面的门（框架级）· 证据

> 票面权威：`gh issue view 367`（目标＝让「代表唤醒词必须是唤醒词表里真有的词」这道门覆盖**每个能力目录**；
> 验收＝扫描面覆盖各能力目录的声明 ＋ 三处命令名取自同一份注册表、差值集合为空；负向＝从唤醒词表里删掉
> 某条代表唤醒词 → 门必红；不许动＝棘轮数字与其它门的判据；交付物＝测试件（门）＋`src/body/commands.ts`）。
> 交付日期 2026-09-15（本地 +08:00；运行记录里的时刻是 UTC）。
> **本文只报机器读数**；四条判据的明细读数全部来自下面点名的运行（对账表见 §六）。

## 〇 · 判定与一句话

**门铺开了、身体域三处错行清了、负向两行读数成立。** 新增 `packages/skill-calorie/test/t367-唤醒词门.test.mjs`
（扫描面＝**10 个能力目录的 127 条声明**，判据＝代表唤醒词必须 ∈ 436 词表 ＋ 声明↔注册表配对集合双向差集为空），
并把 `src/body/commands.ts` 里**三条表外词/缺词行**改到「真词且路由回同键」。

## 一 · 现场量测（开工前，只读探针）

| 读数 | 值 | 运行 |
|---|---|---|
| 能力目录（`src/<能力>/commands.ts`） | **10** 个：analysis／body／diet／exercise／goal／home／photo／profile／weight／workout | `t367-probe01` |
| 声明条数 | **127**（带代表唤醒词 115 ＋ 缺词 12） | `t367-probe01` |
| 唤醒词表 | `WAKE_ASSETS` 436 条 / **434 个不同词**（「记身材照」重复 3 条） | `t367-probe01` |
| 生成物注册表 | `dist/cli/registry.js` **127** 键；与各能力目录声明的 (键→词) 配对集合**双向差集 0／0** | `t367-probe01` |
| **表外词（词不在 436 表）** | **22 条**：analysis 8／**body 3**／diet 4／goal 2／photo 2／profile 1／weight 1／workout 1 | `t367-probe02` |
| **身体域那 3 条** | `calorie.body.composition-add`「记体脂」／`calorie.view.composition-wizard`「看体脂向导」／`calorie.view.measure-wizard`「看围度向导」 | `t367-probe02` |
| 旁证 | `docs/skills/skill-calorie/t266-review-报告.md` §D3 的全包量化＝「118 条声明里 24 条不在 436 词表（… body 3 …）」——本票实测 22 条（photo 4→2 是 #450 清掉的），**body 3 与它逐数吻合** | 只读 |
| 向导两键的真词面 | `calorie.view.composition-wizard` 只有 1 条路由词「看体脂向导」、`calorie.view.measure-wizard` 只有 1 条「看围度向导」，**两条都不是 436 表里的词** ⇒ 向导两键**没有任何真词可回同键** | `t367-probe03` |

## 二 · 门（新增测试件）

件：`packages/skill-calorie/test/t367-唤醒词门.test.mjs`（**新建**，不改 `cmd-registry-294.test.mjs` 那一段体重判据）。
运行：`node --test packages/skill-calorie/test/t367-唤醒词门.test.mjs`（读 `dist/`，须先 `tsc -b packages/skill-calorie`）。

五条判据（`RESULT: 5/5`）：

1. **① 扫描面覆盖每个能力目录**——逐目录报读数（目录／声明条数／数组名），并断言注册表每个键都能在某个能力目录的声明里找到定义地。
2. **② 双向差集为空**——各能力目录声明的 (键→代表唤醒词) 配对集合 与 生成物注册表的同一集合，两向都是空集。
3. **③ 真词**——每条写了代表唤醒词的声明，那个词必须在 436 词表里；**缺词的行不算错**（契约明写 `wakeWord` 可缺、速查表退回列命令名，照片域三个流程内页就是照这条办的），只进读数。
4. **④ 登记表只许变短**——③ 报出来的、属**别的图**的行逐行登记；漏报即红（新错行必被抓）、陈化即红（已清掉的行必须从登记表删掉）。
5. **⑤ 判据自证**——合成夹具上「不在表里的词必被报出、在表里的词不报、登记位生效」三行，防判据空转。

**判据先行读数**（门先写完、body 未改时跑，`t367-judge-first`）：`exit=1`，`RESULT: 3/5`
（红：③ 与 ⑤——⑤ 是自证夹具自身写错，当场修；③ 报出 **22 条未登记**，其中**身体域恰 3 条**，
逐条与票面「身体域的三处错行」对上）。

门转绿读数（body 改完＋登记表填好后，`t367-edit-green`）：

```
SCAN dirs=10 table=434 registry=127
DIR analysis array=ANALYSIS_COMMANDS decl=22 / DIR body array=BODY_COMMANDS decl=10 / …（10 行逐目录）
SCAN-TOTAL decl=127 withWord=115 blank=12
OFF-TABLE 19 registered=19 unregistered=0
DIFF-DIR-ONLY 0 DIFF-REG-ONLY 0
SELFTEST offTable=1 blank=0 registered=1
RESULT: 5/5 通过 exit=0
```

**登记表（19 行，逐行点名归属）**——全在**别的图**，本票一行不碰（票面「遗留出口：登记后各归自己的图」）：

| 能力目录（归属） | 行（命令名 → 代表唤醒词） |
|---|---|
| analysis（分析域自己的图，8 条） | `calorie.view.calorie-trend`「看热量趋势」／`calorie.view.long-trend`「看整体趋势」／`calorie.view.nutrition-analysis`「看营养分析」／`calorie.view.deficit`「看热量缺口」／`calorie.view.health`「看健康盘」／`calorie.view.review-template`「看复盘报告」／`calorie.view.six-factors`「看每日六因素」／`calorie.history`「查热量历史」 |
| diet（饮食域，4 条） | `calorie.view.batch-import-preview`「看批量导入预览」／`calorie.view.library`「查食品库」／`calorie.view.nutrition-ratio`「查营养配比」／`calorie.view.ranking`「查高热量排行」 |
| goal（目标域，2 条） | `calorie.view.goal-status`「看目标状态」／`calorie.view.goal-wizard`「看目标预检」 |
| photo（照片域，2 条） | `calorie.photo.list`「看身材照」／`calorie.help.center`「卡路里HELP」**——后者是老技能 `SKILL.md` 正文注册的技能级 HELP 词（不在 436 场景词表内，出处见 `t450-词表订正.md` §一），「本门该不该接受它」属 HELP 口径问题，归照片／HELP 侧裁定**；裁定前按登记处理，不悄悄放宽 |
| profile（档案域，1 条） | `calorie.view.profile-wizard`「看档案预检」 |
| weight（体重域，1 条） | `calorie.view.weight-review`「看体重复核」 |
| workout（训练计划域，1 条） | `calorie.view.process-progress`「看落地训练进度」 |

## 三 · 身体域：改了什么、凭什么（`src/body/commands.ts`，10＋／5−）

| 行 | 改前 | 改后 | 依据 |
|---|---|---|---|
| `calorie.body.composition-add` | `wakeWord: '记体脂'`（**不在 436 表、且无任何路由**） | `'记体脂（皮褶钳）'` | 436 表词；`routesFor` 回**同键**（`body/routes.ts:11` order 237）；照 #266 先例（把自造词换成真词） |
| `calorie.view.body-composition` | **无代表唤醒词**（速查表退回列命令名） | `'看体脂'` | 票面「两条读命令补上真唤醒词（照 t169 的 13 条词表）」；13 条词表里 看体脂 → 本键（`t169-设计定稿.md:33`）；`routesFor('看体脂')` 回同键 |
| `calorie.view.body-measure` | **无代表唤醒词** | `'看围度'` | 同上（`t169-设计定稿.md:35`；`body/routes.ts:18` order 244） |
| `calorie.view.composition-wizard` | `wakeWord: '看体脂向导'`（**自造词**） | **去掉该字段** | 436 表无此词；`t367-probe03` 证明该键**只有这一条路由词、且不是真词** ⇒ 没有真词可回同键；借别的键的词会撞「代表词回同键」；照 #450 照片域流程内页先例（`t450-词表订正.md` §二.4：老技能没给流程内页词 ⇒ 删 `wakeWord` 字段），速查表退回列命令名 |
| `calorie.view.measure-wizard` | `wakeWord: '看围度向导'`（**自造词**） | **去掉该字段** | 同上 |

三处错行清完：`t367-probe02`（改前）body 3 条 → 门复跑（`t367-edit-green`）body **0 条**。
`看体脂向导`／`看围度向导` **仍在路由面**（用户说得出、命得中，`wizard-86` 10/10 绿）；那两条自造入口词
归**路由面收口票**（与 #266 转票的那 6 条 `new` 列表词同一处置）。

**连带读数（交给 #368，本票按红线不跑 `help:build`／不碰 `SKILL.md`）**：`SKILL.md` 速查表（AUTO 块）
逐行对账 127 行里 **陈旧 5 行，全在 body 且全是本票改的那 5 行**（`t367-skill-table`）：

```
STALE body calorie.body.composition-add      skill=「记体脂」                        声明=「记体脂（皮褶钳）」
STALE body calorie.view.body-composition     skill=「calorie.view.body-composition」 声明=「看体脂」
STALE body calorie.view.body-measure         skill=「calorie.view.body-measure」     声明=「看围度」
STALE body calorie.view.composition-wizard   skill=「看体脂向导」                     声明=「calorie.view.composition-wizard」
STALE body calorie.view.measure-wizard       skill=「看围度向导」                     声明=「calorie.view.measure-wizard」
STALE-BODY 5
```

## 四 · 负向对照（两行机器读数，一个持锁窗口内做完）

做法（`t367-mutation2`，窗口内先记 sha256 → 改一个词 → 编译 → 跑门 → **立即逐字节复原** → 编译 → 复跑）：

```
M2  MUTATED  "wake_word": "看体脂", → "wake_word": "看体脂（已从词表删掉）",  hits=1  sha=d53c75e0…
M4  M4-GATE exit=1   OFF-TABLE 20 registered=19 unregistered=1
    UNREGISTERED body calorie.view.body-composition 「看体脂」
    RESULT: 4/5 通过（红：③ 代表唤醒词必须是唤醒词表（436 条）里真有的词） exit=1     ← 改坏必红
M5  RESTORED sha=73262a20… orig=73262a20… equal=true          ← 逐字节复原
M6/M7  dist 变异串命中=0；M7-GATE exit=0  OFF-TABLE 19 registered=19 unregistered=0
    RESULT: 5/5 通过 exit=0                                                          ← 还原必绿
M8  wake-assets-133 exit=0（复原后词表旁证：436 条与场景表逐字仍相同）
M9  git status -- src/triggers/wake-assets.ts → 空（受版本控制件回原状）
```

**窗口内发现的坑（记账，供后续变异电池照用）**：Windows 的 `CopyFile` **保留源文件时间戳** ⇒ 逐字节复原后
`tsc -b` 的增量判定（按 mtime）认为无需重建，`dist/triggers/wake-assets.js` 里**留着变异版**（第一轮
`t367-mutation` 的还原腿因此误红）。对策＝改完／复原后**碰一下源件 mtime** 再编译，并以「dist 里零命中变异串」
＋复跑门＋`wake-assets-133` 三样作自证（v2 已这么跑）。第一轮的错读数保留在本文件，不静默改写。

## 五 · 既有门与棘轮：动前／动后各一次读数（只加扫描面，数字未动）

| 既有门 | 动前（`t367-base`，11:11 本地） | 动后（`t367-after`，11:15 本地） | 归属 |
|---|---|---|---|
| `cmd-registry-294`（分派层棘轮＋体重那份唤醒词判据） | `tests 7 / pass 7 / fail 0` exit=0 | 同 | 本票未动该件 |
| `wake-assets-133`（436 条与场景表逐字） | `8/8` exit=0 | `8/8` exit=0 | 未动词表 |
| 告警线门 `check-warning-line.mjs` | `RESULT: 61/65` **exit=1**（4 条「台账陈化」：`render/trendDocs.ts` 台账=994 实况=996／`render/trendMiscPortDocs.ts` 650→674／`diet/nutritionPortDocs.ts` 462→492／`diet/nutritionPort.ts` 367→371） | `RESULT: 65/65` `PASS: 告警线台账齐全且与实况一致` exit=0 | **他席**：那 4 件是别席在途件，期间别席已 `--sync` 台账（`packages/skill-calorie/AGENTS.md` 当场实测列已随实况更新）；本票唯一落进扫描面的件是 `body/commands.ts`（37 行 <350、从不在台账内） |
| `exercise-wakewords-266`（同形先例门） | `4/4` exit=0 | `4/4` exit=0 | 未动 |
| `legacy-ratchet-295`（未搬迁清单棘轮） | `6/6` exit=0 | `6/6` exit=0 | 未动 |
| `calorie-routing-81` | `tests 8 / pass 4 / fail 4` **exit=1** | 同 **exit=1**（fail 4，数字未变） | **他席在途**：`src/diet/routes.ts`＋`src/triggers/routes.generated.ts` 未提交改动把「拍营养表记一餐／补记一餐」转入 exec（OUT_OF_SCOPE 10→8）——该测试自己在 `:401` 标明「营养表票在途…归主人，以主人版为准重改」 |
| 身体域相关门（改后新增读数） | — | `wizard-86` 10/10、`t357-skinfold` 8/8、`t363-补记查冲突` 7/7、`t364-删除取快照` 7/7、`t365-七条写词整页` 11/11、`t366-复制执行闭环` 14/14 全 exit=0 | 本票写面内 |
| `sport-homogeneity-109` | 未在动前批跑（改后才发现红，逐条归属见下） | `tests 12 / pass 7 / fail 5` exit=1 | **他席在途**，逐条：①「唤醒词错键：看今日运动」actual=`calorie.view.exercise-records`（路由事实，本票未动路由）；②「`view.exercise` 缺：运动总览 …」③「`exercise-goal` 缺：目标 vs 实际」④「`view.weight` 缺：从 70.5 kg 到 70.1 kg」⑤「`calorie.view.weight` 空库未阻断（status=0）」——全部落在运动／体重两域的页面与空库阻断上，对应 `src/weight/*.ts`（未提交，5 件）与别席路由改动；**本票 diff 只有 `src/body/commands.ts` 一个件、且只动 5 行的 `wakeWord` 字段**（`git diff --stat` ＝ 1 file changed, 10 insertions(+), 5 deletions(-)） |

**棘轮数字**：`cmd-registry-294` 的 `FROZEN_READ_LINES=1146`／`FROZEN_WRITE_LINES=745` 与 `legacy-ratchet-295`
的条数／行数上限**一字未动**（两件都在上表里复跑为绿，数字不变）。本票新增的「登记表」只加在**新件**里，
不构成对既有棘轮的加码。

## 六 · 对账（§2.3 留痕 ＋ §2.4 机械门禁）

窗口内**本票**的运行条目全文导出件：`docs/skills/skill-calorie/t367-gate-runs.log`
（源＝未受版本控制的 `.scratch/locks/gate-runs.log`，按本票首个运行到末个运行截取；同窗口内他席条目一并保留，便于复核「谁在窗口里跑过」）。

| 声称跑过的运行（`GATE-RUN runId=… cmd=…`） | 退出码 |
|---|---|
| `GATE-RUN runId=t367-probe01 cmd=node .scratch/t367/probe.mjs` | 0 |
| `GATE-RUN runId=t367-probe02 cmd=node .scratch/t367/probe2.mjs` | 0 |
| `GATE-RUN runId=t367-probe03 cmd=node .scratch/t367/probe3.mjs` | 0 |
| `GATE-RUN runId=t367-base cmd=pwsh -NoProfile -File .scratch/t367/run-gates.ps1 -Tag base`（动前批跑） | 0（批内 4 红 2 绿，见 §五） |
| `GATE-RUN runId=t367-judge-first cmd=node --test packages/skill-calorie/test/t367-唤醒词门.test.mjs` | 1（判据先行，有意红） |
| `GATE-RUN runId=t367-edit-green cmd=pwsh -NoProfile -File .scratch/t367/compile-gate.ps1 -Tag after-edit`（`tsc -b` ＋门） | 0 |
| `GATE-RUN runId=t367-mutation cmd=pwsh -NoProfile -File .scratch/t367/mutation-battery.ps1`（第一轮，还原腿受 mtime 坑误红，保留） | 0 |
| `GATE-RUN runId=t367-mutation2 cmd=pwsh -NoProfile -File .scratch/t367/mutation-battery.ps1`（负向两行读数） | 0 |
| `GATE-RUN runId=t367-after cmd=pwsh -NoProfile -File .scratch/t367/run-gates.ps1 -Tag after`（动后批跑 14 门） | 0 |
| `GATE-RUN runId=t367-skill-table cmd=node .scratch/t367/probe4-skill-table.mjs` | 0 |

编译口径：窗口内一律 `node node_modules/typescript/bin/tsc -b packages/skill-calorie`（**未跑** `pnpm build`／`pnpm gen`／`pnpm gen:check`）。

## 七 · 未做项与遗留出口

1. **其它 19 条表外词**：逐行登在门的登记表里、各归自己的图（见 §二表）；本票不改别家能力目录。
2. **两条路由面自造入口词**（`看体脂向导`／`看围度向导`）留在 `src/body/routes.ts` 的 `new` 列表里：
   删／并入属**路由面收口票**（与 #266 转票的 6 条 `new` 词同办），本票写集合不含 `routes.ts`。
3. **`SKILL.md` 速查表 5 行陈旧**（§三末）：`SKILL.md` 与 HELP 生成器是 **#368** 的地盘，本票按红线
   **不跑 `help:build`／不碰 `SKILL.md`** ⇒ 这 5 行由 #368 重生成时一并落地。
4. **`help:examples:check` 带票外既有红**（`命令登记纪律.md` §形状一已记）：本票未跑、未改它。
5. **没跑全量 `pnpm test`**：本席只跑与写面相关的 14 门 ＋ 两批动前／动后读数（§五）；
   全量里别席在途红（`weight-*` 四件、`sport-homogeneity-109`、`calorie-routing-81`）不属本票，
   逐条归属见 §五，未假绿。
6. **未做**：`git push`（按派单口径只报 sha）；未建 GitHub 票（登记表的存在即「登记」，建票归编排者）。

## 八 · 红线自查

- 未提交 `src/triggers/routes.generated.ts`（提交前 `git diff --cached --name-only` 复核，只含本票声明的 5 个路径）。
- 未跑 `pnpm --filter skill-calorie build`／`pnpm gen`／`gen:check`；编译只 `tsc -b packages/skill-calorie`。
- 未用 `stash`／`checkout --`／`reset`／`clean`／`restore`／`rebase`／`amend`／`add -A`／`add .`；未切分支、未装包。
- 真库零写入（全部读数走 `dist/` 与只读探针；变异只动一个词且已逐字节复原、`git status` 为空）。
- 共用面临时改动（`src/triggers/wake-assets.ts`）只在单个持锁窗口内存在，窗口内未跑生成器；复原自证见 §四。
