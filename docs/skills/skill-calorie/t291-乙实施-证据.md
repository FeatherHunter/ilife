# #291 乙实施证据（三自造词走查找层别名）

- 裁定：`docs/skills/skill-calorie/t291-三件裁定.md` §裁定三 suggest 乙（本席按编排者选定乙落地）。
- 目标：看目标推荐／看目标配置／看目标状态三词经查找层别名查得到跑得通；权威词表与快照零改动。

## 改动（写集内三件，别处未碰）

1. `packages/skill-calorie/src/triggers/help-lookup.ts`：WAKE_TABLE 加三行（key／cli 逐字同
   `src/goal/routes.ts:34-35,37`）；`buildHelpLookup`／`searchHelp` 两处注入按 key 通用化——饮食三行
   命中形状逐字不变（scene 02／key `diet_add_meal`／记一餐 desc），目标三行各走自己的读命令
   （scene 06／key 取命令键）。`routeWakeword` 本就通用，零改。未碰 TRIGGERS、scene-06-goal.ts、
   wake-assets.ts、helpCenter.ts、任何快照与冻结读数，未补 HELP 卡。
2. `packages/skill-calorie/test/t291-自造词别名.test.mjs`（新）：六组判据（表行数／路由／首命中／
   help.lookup 真出口／SoT436＋饮食回归／三 view 真出口 exit 0，seedFull 照抄 goal-wizard-251）。
3. 本证据件。

结构纪律：只改 triggers 能力内一件，无新目录新导出；help-lookup.ts 约 190 行，未超本包 350 告警线。

## GATE-RUN（持锁，票 291）

- tsc：`node node_modules/typescript/bin/tsc -b packages/skill-calorie` → exit 0
  （runId `b9f4f7f9`；还原后重编 runId `2236804c` exit 0）。
- 本票测试：`node --test packages/skill-calorie/test/t291-自造词别名.test.mjs`
  → pass 6／fail 0，exit 0（runId `5a7bd983`；还原后 runId `3bc68c42`）。
- 冻结计数（SoT 436 口径全绿）：`exercise-wakewords-266` 4/4、`wake-assets-133` 7/7、
  `help-center-88` A1 436／54／1308、`help-center-106` ②卡面 436 均绿。
- 三 view 真出口（seedFull 临时库）：`calorie.view.goal-recommend`／`goal-config`／`goal-status`
  均 exit 0（看目标状态附绝对路径落盘＋字节一致，见本票测试⑥）。

## 变异自证（源码级，逐文件还原）

- 变异：WAKE_TABLE「看目标配置」cli 改坏一行（指向 `...goal-status`）→ 重编后本票测试
  pass 2／fail 4，exit 1（runId `77c8485e`；红断言：①②③④，⑤⑥仍绿）。
- 还原该行后重编重测 → pass 6／fail 0，exit 0（runId `3bc68c42`）。
- 持锁期内还原干净：`git diff` 仅剩本意改动（见提交 `c6ff7e2`）。

## 提交

- `c6ff7e2` 功能(291)：三自造词走查找层别名（WAKE_TABLE加三行，SoT零改动）
  （含源码＋本票测试两件）。
- 本证据件随第二提交落盘（sha 见推送后 issue 回执）。

## 未做项与下一手

- `calorie-c43.test.mjs:71` 钉 `WAKE_TABLE.length === 3`，本票后实况为 6：该断言非 SoT 冻结口径
  （SoT 436 全绿），但会红；改它不在本票写集，请编排者放行后续票把 3→6（本票新测试已钉 6）。
- `help-center-106` ①唯一 CLI（392≠384）与两处 base-render 冻结面（148≠130）三红与本票无关：
  取数源（helpCenter／routing／base-render）均未引用本票改的 `help-lookup.ts`（grep 零命中），
  系并行席在途改动所致，请编排者收口时重跑确认。
- HELP 卡未补：按裁定乙本就不补（三张新卡片属新增产品面，待用户拍板，另开票）。
