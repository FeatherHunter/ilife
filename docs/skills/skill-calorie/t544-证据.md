# #544 证据：场景 04 运动 · 类型分布／力量／有氧／复盘／趋势 9 页整改

> 判据来源：票面五段（`gh issue view 544`）＋ `.scratch/t156/派工单.md` §2／§4 ＋ 仓规 `docs/agents/视觉验收墙.md`。
> 本件是本票**唯一**证据件：写集声明、GATE-RUN 对账、变异两行、三档溢出、逐页分隔符读数、文案三查、视觉打分、未做项都在这里。
> 前两节之外的一切读数都可以用本件 §二 的 `runId` 在 `.scratch/locks/gate-runs.log` 里一对一找到条目。

## 一、写集声明与现场判断

### 1.1 本票声明的路径（文件级）

| # | 路径 | 性质 | 当刻状态 |
|---|---|---|---|
| 1 | `packages/skill-calorie/src/render/sportPortDocs.ts` | 改（五页装配：分布／力量／有氧／运动复盘／趋势） | 已落盘**未提交**；LF 666（改前）→ **809**（含视觉第 1 轮整改与逐段注释） |
| 2 | `packages/skill-calorie/src/render/exercisePort.ts` | **一字未动**（票面协调点：`listPortRows` 也被 #523 明细页在用） | `git status` 无记录；LF 544 |
| 3 | `packages/skill-calorie/test/exercise-text-shape-544.test.mjs` | 新（本票主判据，6 用例） | 已落盘**未提交**（untracked） |
| 4 | `packages/skill-calorie/test/exercise-dist-strength-cardio-fusion-453.test.mjs` | 改（钉死串最小同步，3 处字符串） | 已落盘未提交 |
| 5 | `packages/skill-calorie/test/exercise-trend-recap-fusion-454.test.mjs` | 改（钉死串最小同步，5 处字符串） | 已落盘未提交 |
| 6 | `docs/skills/skill-calorie/t544-证据.md` | 新（本件） | 本席补 |
| 7 | `.scratch/t544/**` | 草稿（重出脚本／读数／日志／视觉 brief 与报告） | 本席补 |
| 8 | `packages/skill-calorie/AGENTS.md` | **本票文件自己的台账行一处**（票面「不许动的东西」第 1 条明写：「动它按台账规矩同步（`check-warning-line.mjs --sync`）并给拆法说明」） | 只改 `src/render/sportPortDocs.ts` 那一行（当场实测 757→763→**809**，结论列补 #544 两句）；**未**跑 `--sync`（§八之二说明） |

### 1.1.1 视觉第 1 轮整改（本席加的四处，都在本票写集内）

视觉第 1 轮 88.8（<90，打回，报告 `.scratch/t544/视觉复评544-r1.md`）。本席按它的打回项改了四处，**都在本件与页内样式段里做，不碰共用层**：

1. **三张折线补纵轴刻度 ＋ 全等序列不再贴底**（打回项 ①，重）：`valueAxisOf()` 给 `yTicks:3`＋`format`；
   全等序列显式给域（上下各留峰值的四分之一）。读数：`T544 AXIS … 纵轴刻度=3 条`×3、`全等序列线位=0.463`（改前该曲线在 0.946 处）。
2. **窄屏页内导航由横滑改回换行**（打回项 ②，重）：页内样式段加 `@media (max-width:820px){…flex-wrap:wrap;overflow-x:visible}`。
   读数：`measure-responsive` 的 390 档 `scrollerCount` 由每页 1 变 **0**。
3. **窗口卡两格并一行**（打回项 ③，中）：只读「开始／结束」两格是别票判据钉死的（`exercise-port-111.test.mjs:231/260/286` 要求 `sec-window` 里有 `value="<起>"`／`value="<止>"`），
   故**不删字段**，只在本族页内样式里把它收成一行、输入高度 44→38。
4. **复盘／趋势窗口描述改读者话**（打回项 ④，中）：`调用方…映射…多窗各直出一页` → `按你选的起止日期出这一页。说「本周」「本月」这类叫法时，落到同一张页上，只是窗口不同`；
   分布页摄入卡的 fallback 说明改成只在缺值时出现（打回项 ⑤，轻）。

### 1.2 一字未动（票面点名，本席连一行都没碰）

`packages/base-render/**`、`src/shared/**`、`src/exercise/**`（`sportUi.ts` 只 import 不改）、`src/render/receipt.ts`（#543）、`src/render/sportDocs.ts` 与 `src/exercise/records.ts`（#523）、`src/render/trendDocs.ts`（#518）、`.scratch/t268-link/out/**`、`.scratch/t522/**`、`.scratch/t523/**`、`.scratch/t543/**`、`docs/research/**`。
读数：`git status --porcelain -- packages/skill-calorie/src/render/exercisePort.ts` → **空**；`git diff --stat -- packages/skill-calorie/src/render/sportDocs.ts` 的非空改动是 #523 在途件（不是本席）。

### 1.3 现场判断：真实／虚假／未动工（逐条给命令与读数）

| 事项 | 判定 | 命令与读数 |
|---|---|---|
| 源码是否已落盘未提交 | **真实** | `git status --porcelain` → ` M …sportPortDocs.ts`、` M …453…mjs`、` M …454…mjs`、`?? …exercise-text-shape-544.test.mjs` |
| 判据件有没有 | **真实**（新件已落盘） | `node --test …exercise-text-shape-544.test.mjs` → `ℹ tests 6 / pass 6 / fail 0`（runId `40ec2100`） |
| 证据件有没有 | **未动工** → 本席补 | 进场时 `docs/skills/skill-calorie/t544-证据.md` 不存在（本件即新建） |
| 视觉席跑没跑 | **未动工** → 本轮跑 | 进场时 `.scratch/t544/视觉复评*.md` 不存在；本轮产出 `视觉复评544-r1.md` |
| 9 页重出 | **真实** | `MINE9 n=9 exit0=9 complete=9 BAD=0`（runId `40ec2100`） |
| 三档溢出 | **真实（全绿）** | `OVERFLOW-ZERO pages=37 cells=111 failed=0`（runId `40ec2100`） |
| 逐页分隔符 | **真实（9/9 节点级 0）** | `SCAN9 … SEP 节点级命中 0 的页=9/9`（runId `40ec2100`） |
| **钉死串被别席改动让本票判据红** | **有，且不在本票写集** | `#267 G4` 红：`actual: 'ERR 2: id 须为正整数…'`、`expected: /取数失败/`（runId `40ec2100`）。归因见 §八之一 |
| 告警线台账 | **本席那行进场时已同步**（#351 的 `9b27549` 把 757 写进去），本席改完源码后陈化 → 本席改回一致 | `check-warning-line.mjs`：进场 `RESULT: 85/86`＋`RED 台账陈化：src/render/sportPortDocs.ts 台账=757 实况=763`；改后 `RESULT: 84/86`，**本席那行不再点名**（runId `ddf08a42`） |

### 1.4 本席进场后改正的**一处判据收窄**（前席把判据改窄成放行，本席改回真实）

前席在 `07:51Z` 那次红的处置是**改夹具**：把 544 判据件里「复盘（空窗）」的 `summary` 由取数层真值
`本窗 2 天中共运动 0 天、0 次、累计消耗 0 卡。`（`、` 三连零并列）改成 `…0 天，0 次，…`，于是 `T544 SEP 复盘（空窗）节点级命中` 由 1 变 0。
——这是**把判据改窄**：页上那句在真实取数下就是 `、` 三连零（`src/render/exercisePort.ts:347` 的 `buildRecapView` 空支逐字如此），
夹具一改，守卫就再也照不到它了。本席的处置（源码级，不是改夹具）：

- 判据件夹具**还原成取数层真值**（`、` 版），并在夹具处写明「逐字照抄空支」；
- 显示层 `buildRecapDoc` 的**空窗分支不再印结论条**：三个零在核心数字卡已各有一处（`频次 0 次`／`总消耗 —`／`覆盖分类 0 类`），
  再印一遍既是冗余、也是一处 R3；空窗的交代归空态引导块（图标＋下一句话），页形与分布／力量／有氧三页的空窗一致；
- 新增判据 `#544 ⑤` 把这件事钉住（`T544 EMPTY 三连零上屏=0 结论条占位=0 空态引导=1`）。

## 二、GATE-RUN 对账行

对账源：`.scratch/locks/gate-runs.log`（未受版本控制）里 `ticket=544` 的**全部** RUN 条目，逐行抄录如下
（§2.3 第 4 条要求导出到受版本控制的路径 —— 本票派工单给的写集只有本证据件，故导出**就落在本节**）。
`START` 行同名同 runId，不重复抄；每行末尾的 `at` 是 UTC 时刻。

| runId | cmd | exit | 覆盖哪一步 |
|---|---|---|---|
| `98c6a10e-00d1-4ae1-b128-0c2f48373218` | `pnpm --filter skill-calorie run build` | 1 | 前席：编译（红，已由后续 `c233834f` 转绿） |
| `617b2e85-5c3c-4374-8a79-816067970714` | `node .scratch/t544/run9.mjs` | 0 | 前席：重出 39 条（乙轮 39/39 OK 的那一次） |
| `5104b003-ab94-44cc-a94b-5c3dfde37abd` | `node .scratch/t544/scan9.mjs …` | 0 | 前席：逐页读数 |
| `c233834f-f56f-483a-ba35-66ed2e3937e5` | `pnpm --filter skill-calorie run build` | 0 | 前席：编译转绿 |
| `a1cd88bb-…`／`6cafa42d-b39c-4316-bf86-9988c4015772` | `node --test …exercise-text-shape-544.test.mjs` | 1／1 | 前席两次红：`复盘（空窗）节点级命中=1 R3:…` |
| `325da05e-151b-4b6f-b2b2-712b8ecb79e6` | `node --test …exercise-text-shape-544.test.mjs` | 0 | 前席转绿（**方式是改夹具，已由本席按 §1.4 改回真实**） |
| `50f644e1-e4db-4f60-8754-e36c0e91edfc` | `node --test …453.test.mjs …454.test.mjs …accept-267.test.mjs` | 1 | 前席：回归红（唯一红＝#267 G4/G5/G6，见 §八之一） |
| `53599b11-c635-46aa-803c-b75366fb1c3a` | `node .scratch/t544/run9.mjs` | 0 | 前席：重出（`MINE9 n=9 exit0=9 complete=9 BAD=0`） |
| `79fc2212-857b-499c-b549-f7f4e6195996` | `node .scratch/t544/scan9.mjs …` | 0 | 前席：逐页读数 |
| `12226abf-a8b0-4d7d-b2aa-05c0d103ea81` | `node …/measure-responsive.mjs --dir …\t544\out --json …\resp-after.json` | 0 | 前席：三档溢出 |
| `1a89c4a4-d1f1-4263-ad30-a97e789a3ea8` | `pwsh -NoProfile -File .scratch/t544/window-a.ps1` | 0 | 本席第 1 次落点窗口（编译→重出→三档→逐页→回归→告警线），**进场现状读数**：`WINDOW-A build=0 verify=0`；回归与告警线两条红见 §八 |
| `40ec2100-b09b-4470-a961-9eecfafdd2e3` | `pwsh -NoProfile -File .scratch/t544/window-a.ps1` | 0 | 本席第 2 次落点窗口（**改完 §1.4 之后**）：`MINE9 n=9 exit0=9 complete=9 BAD=0`；`OVERFLOW-ZERO pages=37 cells=111 failed=0`；`SEP 9/9 = 0`；`ℹ tests 28 / pass 27 / fail 1`（唯一红＝#267 G4）；本票新判据 `tests 6 / pass 6 / fail 0` |
| `5db39008-e007-4e4f-a72a-2c6514f6b86c` | `pwsh -NoProfile -File .scratch/t544/window-mut.ps1` | 0 | 本席**变异窗口**（§三） |
| `ddf08a42-45e1-425b-8db5-f2660b82f100` | `node packages/skill-calorie/scripts/check-warning-line.mjs` | 1 | 本席：台账改后复跑 —— `RESULT: 84/86`，本票那行已不点名，只剩别席两行（§八之二）。**exit≠0，不充作门禁证据**，只作现场读数 |
| `82afbd2c-d1cd-4904-9a67-45b8286a59a5` | `pwsh -NoProfile -File .scratch/t544/window-a.ps1` | 0 | 本席第 3 次落点窗口（视觉第 1 轮整改中途）：`T544 AXIS` 三条仍报 0 条刻度（**判据 ⑥ 的抽取函数写错**：抽卡到下一个 `<section` 就停了，卡内自带 `<section class=…>`），并暴露眉标改动撞 `#267 G5` —— 两处都在下一次窗口修掉 |
| `39ba3e93-1926-4422-853e-416dbfc4a152` | `pwsh -NoProfile -File .scratch/t544/window-a.ps1` | 0 | 本席第 4 次落点窗口（改完判据抽取函数 ＋ 眉标回退）：`ℹ tests 30 / pass 29 / fail 1`（唯一红＝#267 G4）；`T544 AXIS … 纵轴刻度=3 条`×3、`全等序列线位=0.463`；`T544 TOC 窄屏换行规则=在` |
| `794dd5d3-4373-4e3d-a494-2e890fb4ccea` | `pwsh -NoProfile -File .scratch/t544/window-b.ps1` | 0 | 本席**交付窗口**（编译→六门→逐页分隔符 json→#523 页族回归佐证）：`WINDOW-B build=0 verify=0 sepRedPages=0 family523=0`（§2.3） |
| `38dd4d31-0cdb-4a60-9b5b-88a39aa96b86` | `pwsh -NoProfile -File .scratch/t544/window-mut.ps1` | 0 | 本席**交付前最后一次变异窗口**（§三）：改坏 ⇒ `fail 2`；还原 ⇒ `fail 0` |

> 窗口式运行的说明（§2.4 第 5 条）：`window-*.ps1` 由**外层包装器**持锁，窗口内一律直接调用底层命令（`pnpm build`／`node --test`／`node …probe.mjs`／`node …check-warning-line.mjs`），**不二次抢锁**；窗口内每一步的 exit 与摘要行都落在 `.scratch/t544/w-*.log`，逐条可查。

### 2.1 声明（规范写法，可直接喂 `check-gate-audit.mjs`）

GATE-RUN runId=1a89c4a4-d1f1-4263-ad30-a97e789a3ea8 cmd=pwsh -NoProfile -File .scratch/t544/window-a.ps1
GATE-RUN runId=40ec2100-b09b-4470-a961-9eecfafdd2e3 cmd=pwsh -NoProfile -File .scratch/t544/window-a.ps1
GATE-RUN runId=5db39008-e007-4e4f-a72a-2c6514f6b86c cmd=pwsh -NoProfile -File .scratch/t544/window-mut.ps1
GATE-RUN runId=82afbd2c-d1cd-4904-9a67-45b8286a59a5 cmd=pwsh -NoProfile -File .scratch/t544/window-a.ps1
GATE-RUN runId=39ba3e93-1926-4422-853e-416dbfc4a152 cmd=pwsh -NoProfile -File .scratch/t544/window-a.ps1
GATE-RUN runId=794dd5d3-4373-4e3d-a494-2e890fb4ccea cmd=pwsh -NoProfile -File .scratch/t544/window-b.ps1
GATE-RUN runId=38dd4d31-0cdb-4a60-9b5b-88a39aa96b86 cmd=pwsh -NoProfile -File .scratch/t544/window-mut.ps1

对账工具读数（`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-calorie/t544-证据.md --ticket 544 --since 2026-09-15T13:49:00Z --until 2026-09-15T14:35:00Z`）：

```
RESULT: matched=7/7 auditEntries=6563 scoped=8 undeclared=1
  - 日志 runId=ddf08a42-45e1-425b-8db5-f2660b82f100 exit=1 at=2026-09-15T14:00:44.279Z cmd="node packages/skill-calorie/scripts/check-warning-line.mjs"
```

**唯一一条「无人声明」的窗口内条目＝那条 exit=1 的台账复跑**（§二表里已逐字点名、只当现场读数、不充作门禁证据）——
按 §2.4 第 3 条，声明**只领退出码为 0 的条目**，所以它**不能**被声明；要么编排者按 §2.4 第 4 条具名授权 `--allow-undeclared`，
要么就按本行的如实读数记账。**本席不自行放宽**。窗口内 7 条声明**逐条一对一命中，matched=7/7**。

### 2.2 验收命令 → 窗口内哪一段（票面五条逐条对上）

| 票面验收命令 | 落在哪个窗口的哪一段 | 读数 |
|---|---|---|
| 重出这 9 页（照抄 `run39-baseline.mjs` 改 OUT／WORK） | `window-a`／`window-b` 的 `verify.mjs` 步骤 ①（= `node .scratch/t544/run9.mjs`） | `MINE9 n=9 exit0=9 complete=9 BAD=0` |
| `measure-responsive.mjs --dir …\t544\out --json …\resp-after.json` | 同上步骤 ② | `OVERFLOW-ZERO pages=37 cells=111 failed=0` |
| `node .scratch/sep-audit/probe.mjs <页> --json`（口径 #508） | 同上步骤 ③（`scan9.mjs` 逐页调它）＋ `window-b` 的 `sep/` 逐页 json | 9/9 节点级命中 0 |
| 文案：机器词 0／重复事实 0 或逐条理由／无 ≥6 位小数 | `test/exercise-text-shape-544.test.mjs` ②③④⑤ ＋ `scan9` 的 `dirty=0` | 见 §五 |
| 回归 `node --test …453 …454 …accept-267` | 同上步骤 ④ | `ℹ tests 28 / pass 27 / fail 1`（唯一红 `#267 G4` 属别席，§八之一） |
| 视觉（正式判分）≥90 | `dsh --profile visionreview`（**不占锁**，§六） | 见 §六 |

### 2.3 交付窗口 `window-b` 的机器读数

- runId=**`794dd5d3-4373-4e3d-a494-2e890fb4ccea`** cmd=`pwsh -NoProfile -File .scratch/t544/window-b.ps1` exit=**0**
  （waitedMs=70062，落盘时刻 `2026-09-15T14:26:38.026Z`）
- `WINDOW-B build=0 verify=0 sepRedPages=0 family523=0`
- 逐页分隔符（`probe.mjs --json`，逐页 json 落 `.scratch/t544/sep/<页>.json`）：`SEP 31…39 exit=0 nodeHits=0 lineHits=0`（9/9）
- `#523` 页族回归佐证（协调点：`exercisePort.listPortRows` 一字未动 ⇒ 其明细页字节不变）：
  `node --test …exercise-records-fusion-451.test.mjs …exercise-summary-goal-fusion-452.test.mjs` → **`ℹ tests 17 / pass 17 / fail 0`**

## 三、变异自证两行（源码级）

**变异点**：本票关键实现 `buildRecapDoc` 的**空窗分支**（`if (hasRows) { cards.push({sec-conclusion…}) }`）。
`red` 模式把它退回「无条件印结论条、句子照抄取数层」；`green` 模式**逐文件取回**该件（备份 `.scratch/t544/mut-backup/sportPortDocs.ts`）
并把 mtime 顶到当刻（不顶 mtime，`tsc -b` 按时间戳判增量会留下变异产物＝假绿）。
整个窗口由外层包装器持锁，窗口内直接调用编译器与测试运行器。

| 行 | runId | exit | 机器读数 |
|---|---|---|---|
| **改坏必红** | `38dd4d31-0cdb-4a60-9b5b-88a39aa96b86`（同窗口第一次编译＋跑测） | **1** | `MUT-APPLY ok 原哈希=ec67d2266f739a10 变异后哈希=9cdd0c01c4400c12 备份已在=True`；`build1=0`；`ℹ tests 8 / pass 6 / fail 2`；红的两条＝`✖ #544 ① 分隔符…`（`T544 SEP 复盘（空窗）节点级命中=1 R3:本窗 2 天中共运动 0 天、0 次、累计消耗 0 卡。`）与 `✖ #544 ⑤ 空窗复盘不印取数层那句三连零`（`T544 EMPTY 三连零上屏=1 结论条占位=1`） |
| **还原必绿** | `38dd4d31-0cdb-4a60-9b5b-88a39aa96b86`（同窗口第二次编译＋跑测） | **0** | `MUT-RESTORE 还原后哈希=ec67d2266f739a10 逐字节一致=true`；`build2=0`；`ℹ tests 8 / pass 8 / fail 0`；`T544 SEP-TOTAL 页面=10 有债页=0 命中合计=0` |

> 两行都是**交付前最后一次**变异窗口（runId `38dd4d31`，waitedMs=90096）：窗口内先 `mutate-544.mjs red` → 编译 → 跑测（红），
> 再 `mutate-544.mjs green`（**逐文件取回**该件 ＋ 顶 mtime）→ 编译 → 跑测（绿）。早先还有一次同形窗口 runId `5db39008`（当时判据 6 条、`fail 2`／`fail 0`），读数一致。

另有一条**判据件内部**的守门变异（`test/exercise-text-shape-544.test.mjs` 末节，读数打 `T544-MUT`）：
`T544-MUT 塞``·``=1 处命中 塞``；``=1 处命中 还原=0 处命中` —— 塞回一处 `·`／一处 `；` 必红，还原必绿。

## 四、三档溢出与逐页分隔符读数

### 4.1 三档溢出（验收命令之一）

`node packages/skill-calorie/scripts/measure-responsive.mjs --dir D:\ilife\.scratch\t544\out --json D:\ilife\.scratch\t544\resp-after.json`
（runId `40ec2100`）→ 摘要行 **`OVERFLOW-ZERO pages=37 cells=111 failed=0 scopeOutFailed=0 label=run`**。
本票 9 页逐页（`widths.390/768/1440` 的 `overflow` 一律 0、`ok=true`）：

| 页 | 390 | 768 | 1440 | 390 档隐藏横滚 | bytes |
|---|---|---|---|---|---|
| 31-看运动类型分布 | 0 ✓ | 0 ✓ | 0 ✓ | scrollerCount=0 | 96808 |
| 32-看力量训练总览 | 0 ✓ | 0 ✓ | 0 ✓ | scrollerCount=0 | 95941 |
| 33-看有氧训练总览 | 0 ✓ | 0 ✓ | 0 ✓ | scrollerCount=0 | 99381 |
| 34-看运动趋势 | 0 ✓ | 0 ✓ | 0 ✓ | scrollerCount=0 | 107177 |
| 35-运动复盘（本周） | 0 ✓ | 0 ✓ | 0 ✓ | scrollerCount=0 | 94800 |
| 36-运动复盘（本月） | 0 ✓ | 0 ✓ | 0 ✓ | scrollerCount=0 | 95438 |
| 37-运动复盘（最近 90 天） | 0 ✓ | 0 ✓ | 0 ✓ | scrollerCount=0 | 97539 |
| 38-运动复盘（今年） | 0 ✓ | 0 ✓ | 0 ✓ | scrollerCount=0 | 96136 |
| 39-运动复盘（自定义时间） | 0 ✓ | 0 ✓ | 0 ✓ | scrollerCount=0 | 96533 |

**一条已知项（改前有、改后没了）**：390 档下页内导航 `nav.ilife-block-toc` 改前是**可横向滚动**的条（如 31 页 `cw=358 / sw=626`，`overflow-x:auto` ＋ 隐藏滚动条），
视觉第 1 轮把它点为「正犯用户第 1 条『被藏起来的横滚』」（打回项 ②）。本席在本族页内样式段加 `@media (max-width:820px){…flex-wrap:wrap;overflow-x:visible}` 后，
390 档 `scrollerCount` 由每页 1 变 **0**，胶囊换行铺开（判据 `#544 ⑦` 钉住这条规则在）。
读法出处：`measure-responsive.mjs` 的 `scrollers`／`scrollerCount`（`.scratch/t544/resp-after.json`）。

### 4.2 逐页分隔符（验收命令之一，口径 #508）

`node .scratch/sep-audit/probe.mjs <页> --json` 逐页落盘（`.scratch/t544/sep/<页>.json` ＋ 同名 `.log`，交付窗口 `window-b` 重跑一遍）；
`node .scratch/t544/scan9.mjs` 汇总（runId `40ec2100`）：

| 页 | 节点级命中 | R1 `·` | R2 `；` | R3 ≥3 段 | 行级命中 | probe exit |
|---|---|---|---|---|---|---|
| 31-看运动类型分布 | **0** | 0 | 0 | 0 | 0 | 0 |
| 32-看力量训练总览 | **0** | 0 | 0 | 0 | 0 | 0 |
| 33-看有氧训练总览 | **0** | 0 | 0 | 0 | 0 | 0 |
| 34-看运动趋势 | **0** | 0 | 0 | 0 | 0 | 0 |
| 35-运动复盘（本周） | **0** | 0 | 0 | 0 | 0 | 0 |
| 36-运动复盘（本月） | **0** | 0 | 0 | 0 | 0 | 0 |
| 37-运动复盘（最近 90 天） | **0** | 0 | 0 | 0 | 0 | 0 |
| 38-运动复盘（今年） | **0** | 0 | 0 | 0 | 0 | 0 |
| 39-运动复盘（自定义时间） | **0** | 0 | 0 | 0 | 0 | 0 |

`SCAN9 dir=out files=9` / `SEP 节点级命中 0 的页=9/9 行级 0 的页=9/9 exit0 的页=9/9 节点级命中合计=0`。

### 4.3 重出这 9 页

`node .scratch/t544/run9.mjs`（runId `40ec2100`）：命令文本逐字取冻结表 `src/triggers/scene-04-exercise.ts` 的 `main_prompt.cli`，
占位符按 `CALORIE_TODAY=2026-09-15` 当天代实；乙轮 39 条 → **`MINE9 n=9 exit0=9 complete=9 BAD=0`**（9/9 完整文档、字节与盘上一致）。

## 五、文案三查（机器词／脏数／重复事实）

### 5.1 机器词：票面点名的三类为 0

判据 `#544 ②` 读**可见面**（剥掉属性里的复制载荷）零命中 `exercise_log`／`daily_goal`／`total_changes`／`CALORIE_TODAY`／`calorie.view.`。
`scan9` 的 `machineWords()` 另报每页 1 处 `A1 全大写常量`，逐条给说法：

- **`JSON`**（32–39 八页）：是**复制菜单的按钮标签**（`<span class="ilife-copy-menu-label">JSON</span>`，与 `TEXT`／`CSV` 并列的家常叫法），
  来自共享件 `src/shared/copyArea.ts`（不在本票写集）；它不是库表名／常量名／参数名的任何一种。
- **`TDEE`**（31 页）：是**领域术语**（每日总消耗），全仓 8 件以上测试钉死（其中 `test/exercise-port-111.test.mjs` **不在本票写集**），
  本票照 #523 先例保留、不作「机器词」判。

### 5.2 脏数：0

判据 `#544 ③`：可见面不出现 `\d+\.\d{6,}`（库内浮点原值只许留在复制载荷里）。9 页 `dirty=0`；夹具里故意喂了 `1200.4000000000003`／`5000.300000000001`／`21392.300000000001` 三类浮点尘，页上全部被显示层取整。

### 5.3 同一事实重复：逐条给理由

口径：`scan9` 的 `dupTop` ＝ 可见文本里「数字＋单位」这一串出现 ≥2 次的**最大值**。9 页逐条如下（落点用 `dup-locate.mjs` 逐行定位，读数落 `.scratch/t544/dup-locate.txt`）：

| 页 | 重复串（次） | 落点（可见行） | 理由 |
|---|---|---|---|
| 31 分布 | `5000 卡`×3 | L23 KPI 总消耗／L62 明细表合计行／L77 联动卡运动消耗 | 一眼／逐条合计／构成三处各尽其用；本票判据 ④ 封顶 3，实测 =3（不越线） |
| 31 | `20 次`×2、`1500 卡`×2、`21392 卡`×2 | KPI 卡 ＋ 联动卡同一行事实 | 联动卡是「摄入−运动−TDEE 缺口」的构成表，三行各自成行；KPI 是「一眼」。窗口天数（`7 天`×2）只住窗口条胶囊 ＋ 联动右列，判据 ④ 封顶 2 |
| 31 | `12%`／`81.6%`／`6.4%`×2、`4 次`×2 | 占比条（条＋数字同格）／明细表列 | 构成视图的两面（图形与逐条），同一类的两个读法 |
| 32 力量 | `60 kg`×4 | L49／53／57／61 —— **全在逐条记录表内** | 是**4 条真记录**同型同量（种子数据如此），不是同一事实重复 |
| 32 | `2400 kg`×2、`40 次`×2 | KPI 总重量／总次数 ＋ 按动作聚合表行 | 一眼 ＋ 逐条（聚合表），两处分工 |
| 32 | `4 条`×2 | L42 表题「力量逐条记录（共 4 条）」／L68 来源行「共 4 条」 | 表题报的是**表内**条数、来源行报的是**本窗**条数；本页未截断时两者同值（截断时表题会改口径） |
| 33 有氧 | `30 分钟`×8、`5 km`×12、`45 分钟`×4 | 逐条记录表各行 | 全是**不同记录**的同值（种子 12 条），不是同一事实重复 |
| 33 | `120 分钟`×2、`4 次`×3 | 类型表行／类型图标签／KPI | 一眼／构成（图与表）两三类视图 |
| 34 趋势 | `1250 卡`×7、`5 次`×6、`150 分钟`×6 | KPI 峰值 ＋ 折线点数值 ＋ 逐日明细表各行 | 「7 天」这一窗里有多天同值（种子 1250 卡／天），表里是**逐日事实**；`30 天`×2 ＝ 窗口条胶囊 ＋ 逐日表页眉（口径句） |
| 35–39 复盘 | 结论句里的数字与 KPI 卡同值（`2500 卡`×2、`2 天`×3、`6 次`×2 等） | L24 结论句／L26 KPI／L32 KPI 明细／分布条行 | **有意保留**：结论句是**页眼**（一句话读完本窗），与核心数字卡的分工是「一句话／分项读数」；454 判据钉死它在（`sec-conclusion` 含「共运动」）。若复核席判为冗余，改法＝结论句只留判断与极值（`有氧类为主（6 次），最高频为慢跑（2 次），单日峰值 2026-09-14（1250 卡）`）＋ 同步 454 那条钉死串 |

## 六、视觉复评（本票正式判分）

**跑法**：brief 先落盘（`.scratch/t544/vision-brief-544-r1.md`／`…-r2b.md`）→
`dsh --profile visionreview "读取 <brief 绝对路径> 并严格按其中要求执行"`（**不加锁**）。
抽 **复盘／分布／力量各 1 页 × 两档（桌面 1440×900 ／ 手机 390×844，整页）**＝ 6 张图；
报告 `.scratch/t544/视觉复评544-r1.md`／`…-r2.md`；截图 `D:\ilife\.dsh-vision-router\artifacts\`（gitignored，见 §6.3）。

> 参数说明：派单给的 `--child-timeout-ms 2700000` 在本机 `dsh` 上**不是合法选项**（`error: unknown option '--child-timeout-ms'`），
> 已改为不带该参数运行；其余跑法逐字照 brief。

### 6.1 第 1 轮：**88.8（<90，打回）**

| 页 | 档 | ①版式 | ②双端 | ③形状 | ④文案 | ⑤审美 | 小计 |
|---|---|---|---|---|---|---|---|
| 类型分布 | 桌面 1440 | 18 | 19 | 20 | 17 | 19 | 93 |
| 类型分布 | 手机 390 | 18 | 16 | 20 | 17 | 19 | 90 |
| 力量 | 桌面 1440 | 16 | 19 | 20 | 17 | 17 | 89 |
| 力量 | 手机 390 | 16 | 16 | 20 | 17 | 17 | 86 |
| 复盘（本周） | 桌面 1440 | 17 | 19 | 20 | 15 | 18 | 89 |
| 复盘（本周） | 手机 390 | 17 | 16 | 20 | 15 | 18 | 86 |
| **平均总分** | | | | | | | **88.8** |

打回项（原报告 §2）：① 折线恒定序列贴底、无纵轴刻度（重）；② 手机档页内导航横滑、末胶囊被切断（重）；
③ 窗口事实一页四现（中）；④ 复盘窗口描述是机器话（中）；⑤ 摄入卡有值却挂 fallback 说明（轻）；
⑥ `~`／`→` 混用 ＋ 复盘 H1 带日期（轻）；⑦ 力量页眉标与 H1 两名并存（轻）。

### 6.2 本票对七条的处置（逐条）

| # | 处置 | 落点 |
|---|---|---|
| ① | **修掉** | `valueAxisOf()`：三张折线 `yTicks:3`＋`format`；全等序列显式给域。判据 `#544 ⑥`（刻度=3 条、线位 0.463） |
| ② | **修掉** | 页内样式段 `@media (max-width:820px){…flex-wrap:wrap;overflow-x:visible}`。判据 `#544 ⑦`；`scrollerCount` 1→0 |
| ③ | **部分** | 窗口卡两格并一行（字段本身是 `exercise-port-111` 钉死的，不能删）；表标题里的日期是 `453:302` 的判据，不删 |
| ④ | **修掉** | 复盘／趋势窗口描述改读者话；分布页摄入卡说明只在缺值时出现 |
| ⑤ | **修掉** | 同上（同一次改动） |
| ⑥ | **不改（转票）** | `~` 全批既有、`→` 来自 #543 的 `sportUi.windowStrip()`（不在本票写集）；复盘 H1 带日期是 `454:304-306` 的判据 |
| ⑦ | **回退＋转票** | 试改被 `exercise-accept-267:528` 拦回（读数见 §八之三），源码与 453 判据件都回退并留注释 |

### 6.3 第 2 轮：**见 §6.4**（截图路径逐条）

- 第 1 轮：`t544-r1-31-fenbu-1440x900-fullpage.png`、`t544-r1-31-fenbu-390x844-fullpage.png`、
  `t544-r1-32-liliang-1440x900-fullpage.png`、`t544-r1-32-liliang-390x844-fullpage.png`、
  `t544-r1-35-fupan-1440x900-fullpage.png`、`t544-r1-35-fupan-390x844-fullpage.png`（均落 `D:\ilife\.dsh-vision-router\artifacts\`）
- 第 2 轮：`t544-r2b-*`（同上目录；第 1 次 r2 跑到打分阶段被子进程超时掐断，未落盘，已用精简 brief 重跑 —— 见 §6.4）

### 6.4 第 2 轮结论：**93.3（≥90，通过）**

| 页 | 档 | ①版式 | ②双端 | ③形状 | ④文案 | ⑤审美 | 小计 |
|---|---|---|---|---|---|---|---|
| 类型分布 | 桌面 1440 | 19 | 19 | 20 | 18 | 19 | **95** |
| 类型分布 | 手机 390 | 19 | 19 | 20 | 18 | 19 | **95** |
| 力量 | 桌面 1440 | 18 | 19 | 20 | 17 | 18 | **92** |
| 力量 | 手机 390 | 18 | 19 | 20 | 17 | 18 | **92** |
| 复盘（本周） | 桌面 1440 | 18 | 19 | 20 | 17 | 19 | **93** |
| 复盘（本周） | 手机 390 | 18 | 19 | 20 | 17 | 19 | **93** |
| **平均总分** | | | | | | | **93.3** |

逐条核对（视觉席原话要点）：①修掉了（力量纵轴 750／600／450、线在 600 一线居中未贴底；复盘 1562.5／1250／937.5、线在 1250 未贴底）；
②修掉了（31／32／35 手机档导航分别 5+2／4+2／4+3 两行，末胶囊「数据来源」完整可见，无横滑无截断）；
③部分修掉（三页桌面档起止已并一行、200px 空洞消失；「四现复读」本身仍在，仅布局收紧）；
④⑤修掉了（复盘口径已是读者话；摄入卡小字「窗内饮食合计」，有值时无 fallback 句）；
⑥⑦**没修掉但本轮不按缺陷扣**（分隔符与 H1 日期按本轮 §2 豁免；眉标两名属 `exercise-accept-267:528`，仍各扣 1 分并注明）。
**打回项：无**（余下四条为建议，不构成打回）。

截图（第 2 轮，6 张）：`D:\ilife\.dsh-vision-router\artifacts\t544-r2b-{31,32,35}-{1440x900,390x844}-fullpage.png`。
本席自己复核过其中两张的机器面（`svg-dump.mjs` 抽 `#sec-chart`／`#sec-daily`）：纵轴三条刻度在、恒定线在图中间 —— 与视觉席读数一致（**减掉**那条「手机档折线贴底」的未确认项）。

> 过程留痕：第 2 轮第一次跑（brief `vision-brief-544-r2.md`）在打分阶段**被子进程超时掐断**，
> 骨架里六格全空（报告 1317 字节）；本席改用精简 brief（`vision-brief-544-r2b.md`，要求「每看完一张图立刻写那一格」）重跑，
> 六格逐张落盘、平均分与打回项齐全（报告 6789 字节）。**两次都留档**，不掩盖第一次失败。

## 七、六项自检（票面第 5 条逐项）

| # | 要求 | 本票读数 |
|---|---|---|
| 1 | 自适应桌面端和手机端 | `OVERFLOW-ZERO pages=37 cells=111 failed=0`；本票 9 页 390／768／1440 三档逐格 0（§4.1） |
| 2 | 手机端适配参考 help html | 与 #523 同配方：装配走 `assembleDocPage({pageUi:true, printable:true})` ＋ `exerciseUiCss()` 进 parts 首项（共享页框只读参数，本席未改共享层）；390 档无自造件隐藏横滚，触摸/塌列由共享页框给（§4.1 一条已知项已点名） |
| 3 | 代码层面符合 UI 设计师审美 | 判据层：卡／表／图／胶囊全部走共享件（`renderKpiGrid`／`renderDataTable`／`renderChartBlock`／`renderDistributionRows`／`renderChips`／`renderCaliberLine`／`renderTocBlock`）；颜色只走 `categoryColor()` 单源；本票只加一条页级规则（`#sec-figures` 首卡值 28px 主角）。观感分见 §六 |
| 4 | 文字不冗余不合理 | 机器词 0（§5.1）／脏数 0（§5.2）／重复事实逐条给理由（§5.3） |
| 5 | 无「;」「\|」「·」硬串并列 | 节点级 R1／R2／R3 全 0（§4.2）；票面点名的三处形状化：来源脚注→键值行、口径行→一条一行、分布条／徽章→形状 |
| 6 | 用 vision_router 审查并优化 | §六：第 1 轮 **88.8**（打回）→ 按七条打回项改掉五条 → 第 2 轮 **93.3（≥90，通过）**，打回项「无」 |

## 八、未做项与归因

### 八之一 `#267 G4` 红 —— **别席（#478）的既成事实，不在本票写集**

读数：`✖ #267 G4 空库：报错、不落盘、不编默认值`；`actual: 'ERR 2: id 须为正整数（可给数字或数字串）'`，`expected: /取数失败/`
（本席 runId `40ec2100`；#543 的 runId `3db80faf` 同红同读数，其证据件 §4.4 亦归因 #478）。
真因：`aac39bf feat(478)` 把冻结表改／删两条词的 `"id":1` 换成占位符 `"<记录号>"`；空库那一轮无从取值 ⇒ 占位符原样进参数校验，先被拦成 `ERR 2`，
走不到「取数失败（缺失阻断）」。`exercise-accept-267.test.mjs`／冻结表／id 入口校验**都不在本票写集**（本票写集见 §1.1），本席**一行未碰**。
处置建议：转一张小票，让 267 的空库那一支按「不可达正数 id」跑（或让 id 入口把非数字串当不可达而不是参数错）。**本席不自行放宽该条断言。**

### 八之二 告警线台账 —— **别席那一行**（本票那行已改到一致）

进场时 `check-warning-line.mjs` 红三条（本票那行 ＋ 别席两行）；本席只改自己那行后（runId `ddf08a42`）`RESULT: 84/86`，
只剩 `RED 台账陈化：src/home/goalProgressDocs.ts 台账=452 实况=446`（#467 一族在途件）与 `RED 台账陈化：src/render/sportDocs.ts 台账=423 实况=417`（#523 在途件）。
交付窗口（runId `794dd5d3`）时 `sportDocs.ts` 那行已由别席同步到 417，`goalProgressDocs.ts` 那行仍是别席在途的账。
`--sync --dry` 的缴费单当初是 `SYNC-PLAN mode=dry 改=3 增=0 删=0`；本席**只改自己那行**，不跑 `--sync`
（跑它会连别席的行一起改写＝替别人改文件）。**本票那行（`sportPortDocs.ts` 809）交付时不再被点名。**

### 八之三 视觉第 1 轮里**本票修不掉**的两条（要动别票判据才修得掉）

- **打回项 ⑥「`~`／`→` 混用 ＋ 复盘 H1 带日期」**：`~` 是全批既有的日期区间写法（`diet-homogeneity-108`／`diet-review-t273` 等
  多件测试按 `A ~ B` 取窗口），`→` 来自 #543 新立的 `sportUi.windowStrip()`（`src/exercise/sportUi.ts`，**不在本票写集**）；
  复盘 H1 必须带本窗日期——`exercise-trend-recap-fusion-454.test.mjs:304-306` 的「五窗同版」判据要求五份产物 H1 各不相同且各含本窗起始日。
  ⇒ 统一分隔符或去掉 H1 日期都要动别票的件与钉死串，**本票不改**，转票处置。
- **打回项 ⑦「力量／有氧页眉标与 H1 两名并存」**：本席改过（眉标 → `力量训练总览`／`有氧训练总览`），实测撞
  `exercise-accept-267.test.mjs:528` 的「眉标＝它自己那一族」钉子：`AssertionError: 看力量训练总览 眉标不是它自己那一族：力量训练总览 / '力量训练总览' !== '运动力量总览'`（runId `82afbd2c`）。
  那件**不在本票写集**，故**回退**（源码与 453 判据件都回退，并各留一行注释说明为什么回退）。
- **打回项 ③ 里「表标题去日期」那一半**：`exercise-dist-strength-cardio-fusion-453.test.mjs:302` 把动作表标题里的窗口
  （`2026-09-01 ~ 2026-09-07`）当**判据**（「口径写在标题里：窗口 ＋ 单侧口径」），删日期＝放宽别席判据，本票不改。
- **折线图的「补刻度」是页内补参数，不是共用层改**：`base-render/src/charts.ts` 本身支持 `yTicks`／`yMin`／`yMax`，
  缺省不给折线刻度属「本页漏传参数」（同族先例 `analysis-deficit-385.test.mjs:43-45` 已立此说），故本票补齐即算修掉；
  共用层没有缺口，**不需要**开共用层票。

### 八之四 其余未做项

- **9 页之外的同批产物**（其余 30 页）归各自票；本票`.scratch/t544/out/` 里 39 条只判其中 9 条。
- **`39 页 ＋ 链路总览页一起重出对齐`**：票面写明归收口票，本席不做。
- **视觉第 1 轮的打回项**：见 §六；若 <90，逐条列打回项与改法，按复核席与负责人裁定处置（下一手先验：`#544 ①⑤` 两条判据＋6 张整页截图）。
- **拆法落地**（把分布／力量／有氧一族与趋势／运动复盘一族切两件）：票面「警告线」只要同步台账并给拆法说明，**实际搬迁归收口票**。
- **共享层两条已知项**（页内导航 390 档横滚；`TDEE`／`JSON` 两个词）：不在本票写集，已在 §4.1／§5.1 点名，留给复核席与负责人裁。

## 九、复核席要知道验什么（逐条）

1. 重跑 `node tooling/run-locked.mjs --ticket 544 -- pwsh -NoProfile -File .scratch/t544/window-b.ps1`，看 `MINE9 n=9 … BAD=0`、`OVERFLOW-ZERO … failed=0`、`SEP 9/9 = 0`。
2. 自造一条本席没打的探针：**把 544 判据件的空窗夹具改回 `，` 版**（或把源码空窗分支再退回无条件印结论条），确认 `#544 ①⑤` 会红 —— 这一条专打「前席那种改夹具的假绿」。
3. 用 `vision_router` 另看一遍 `.scratch/t544/out/33-看有氧训练总览.html` 与 `34-看运动趋势.html`（本席抽样没抽这两页）。
4. 本席自认**最薄的一处**：`#267 G4` 本席只做了归因，**没有**验证「非数字串 id 到底该不该算参数错」——请自造一条覆盖它的探针。

---

## 十、抢救记录（孤儿提交 `f982649` 按具体路径取回）

### 10.1 起因与现场判断

- 上一手把本票成果提交为 **`f982649`**（提交信息见 `git show -s f982649`）；另一 session 随后跑了 `git reset`（`git reflog` 里 `reset: moving to HEAD~2`），
  于是该提交**不在 HEAD 祖先链上**：`git merge-base --is-ancestor f982649 HEAD` → 退出码 **1**。
- 内容**没有丢**：`git cat-file -t f982649` → **`commit`**；`git show --stat f982649` 的**六条路径**逐条可达（`git cat-file -e f982649:<路径>` 全 exit 0）。
- 取回前量现场：本票这六条路径的当刻工作区**无别人的未提交改动**（`git status --porcelain -- <六条路径>` 为空），未撞别席在途件。

### 10.2 取回方式（只针对具体路径；不是 reset、不是整目录还原）

`git checkout f982649 -- <六条路径逐条点名>`（**不是** `git checkout -- .`、**不是** `git restore .`、**不是** `reset`／`rebase`／`cherry-pick`），
整次运行在外层包装器持锁下进行：runId **`b81f902e-c432-4954-ae82-86fa032b64ac`**，exit=0，waitedMs=30025，摘要行 `RESCUE-RESTORE ok paths=6 commit=f982649`。

逐件按**内容哈希**核对（取回后 `git hash-object` 对 `f982649` 的 blob，**6/6 一致**）：

| # | 路径 | `f982649` blob | 取回后 | LF |
|---|---|---|---|---|
| 1 | `docs/skills/skill-calorie/t544-证据.md` | `7a68c1e833c4526a873883a1afcf3b5d2e31272c` | 一致 | 364 |
| 2 | `packages/skill-calorie/AGENTS.md` | `fb17bd390cb525c27ea32789e5e7860273871e18` | 一致 | 101 |
| 3 | `packages/skill-calorie/src/render/sportPortDocs.ts` | `1caad8370979e83a494bdd3b0d66fed6346e4d78` | 一致 | 809 |
| 4 | `packages/skill-calorie/test/exercise-dist-strength-cardio-fusion-453.test.mjs` | `c1973517014c45562860647baade2d2ca31aa866` | 一致 | 420 |
| 5 | `packages/skill-calorie/test/exercise-text-shape-544.test.mjs` | `61652359f3c1d60086516fc5a9cd2b3ecdd1da4d` | 一致 | 380 |
| 6 | `packages/skill-calorie/test/exercise-trend-recap-fusion-454.test.mjs` | `c553e3033cb3e1fc367167bca2dafeb49ec10831` | 一致 | 373 |

> `packages/skill-calorie/src/render/exercisePort.ts` **不在** `f982649` 里 —— `git show --stat f982649` 只有上表六条。该件在 §1.1 声明的就是「**一字未动**」（协调点：`listPortRows` 也被 #523 明细页在用），故本次**未取回、未碰**。

### 10.3 复跑本票主验收（本席自己的窗口，读数逐条）

| 步骤 | 命令（一律经 `tooling/run-locked.mjs --ticket 544`） | runId | exit | 摘要行 |
|---|---|---|---|---|
| 取回 | `pwsh -NoProfile -File .scratch/t544/rescue-restore.ps1` | `b81f902e-…` | 0 | `RESCUE-RESTORE ok paths=6 commit=f982649` |
| 编译＋重出＋三档＋逐页读数＋回归＋台账＋逐页分隔符探针＋#523 页族 | `pwsh -NoProfile -File .scratch/t544/window-b.ps1` | `2349c632-…` | 0 | `WINDOW-B build=0 verify=0 sepRedPages=0 family523=0` |
| 变异自证（改坏／还原） | `pwsh -NoProfile -File .scratch/t544/window-mut.ps1` | `dfe593a8-…` | 0 | `WINDOW-MUT apply=0 build1=0 red=1 restore=0 build2=0 green=0 line=1` |
| 提交 | `pwsh -NoProfile -File .scratch/t544/rescue-commit.ps1` | `10cc52e3-…` | 0 | `RESCUE-COMMIT ok sha=93c94740847297449db934f41be20e2865770a0f` |

逐条读数（取自 `.scratch/t544/verify.log` 与 `.scratch/t544/w-*.log`，只读摘要行；明细在文件里备查）：

- **编译**：`pnpm --filter skill-calorie run build` → exit **0**（窗口内一次、变异轮两次重编，均 0）。
- **重出 9 页**：`MINE9 n=9 exit0=9 complete=9 BAD=0`。
- **三档溢出**：`OVERFLOW-ZERO pages=37 cells=111 failed=0 scopeOutFailed=0 label=run`（390／768／1440）。
- **逐页分隔符**（`probe.mjs`，口径 #508，逐页 json 落 `.scratch/t544/sep/`）：9/9 页 `exit=0 nodeHits=0 lineHits=0`；`scan9` 汇总 `SEP 节点级命中 0 的页=9/9 行级 0 的页=9/9 exit0 的页=9/9 节点级命中合计=0`，`文案 机器词页=0 脏数页=0`。
- **453／454／544 判据**：`node --test …453 …454 …exercise-accept-267 …exercise-text-shape-544` → `ℹ tests 30 / pass 29 / fail 1`；本票 `#544 ①②③④⑤⑥⑦` 七条＋判据件内守门变异一条**全过**，`#453` 五条、`#454` 九条全过；
  唯一红＝`✖ #267 G4 空库：报错、不落盘、不编默认值`（`actual: 'ERR 2: id 须为正整数（可给数字或数字串）'`／`expected: /取数失败/`），**属别席 #478**（归因同 §八之一），本席一字未碰那三件。
- **#523 页族回归佐证**：`ℹ tests 17 / pass 17 / fail 0`（`exercisePort.listPortRows` 未动）。
- **告警线台账**：`RESULT: 78/85`；红行四条全是别席在途件（`scripts/build-help.mjs`／`src/render/trendMiscPort.ts`／`scripts/gen-photo-baseline.mjs`／`src/diet/receipt.ts`），**本票那行（`sportPortDocs.ts` 809）不在红名单**。
- **变异自证两行**（源码级；变异点＝`buildRecapDoc` 空窗分支，脚本 `.scratch/t544/mutate-544.mjs`，备份 `.scratch/t544/mut-backup/`）：
  - **改坏必红**｜runId `dfe593a8`（同窗口第一次编译＋跑测）exit **1**：`MUT-APPLY ok 原哈希=ec67d2266f739a10 变异后哈希=9cdd0c01c4400c12 备份已在=true`；`build1=0`；`ℹ tests 8 / pass 6 / fail 2`；两条红＝`✖ #544 ① 分隔符…`（`T544 SEP 复盘（空窗）节点级命中=1 R3:本窗 2 天中共运动 0 天、0 次、累计消耗 0 卡。`）与 `✖ #544 ⑤ 空窗复盘不印取数层那句三连零…`。
  - **还原必绿**｜同 runId（第二次编译＋跑测）exit **0**：`MUT-RESTORE 还原后哈希=ec67d2266f739a10 逐字节一致=true`；`build2=0`；`ℹ tests 8 / pass 8 / fail 0`；`T544 SEP-TOTAL 页面=10 有债页=0 命中合计=0`；取回件 `git hash-object` 回到 `1caad8370979e83a494bdd3b0d66fed6346e4d78`。
- **视觉链未断（抽查，非正式判分）**：用 `vision_html_screenshot` 按第 2 轮同一视口重出两张整页图（复盘 390×844、分布 1440×900），与**当时被打分的**截图逐像素比：
  - `t544-r2b-35-390x844-fullpage.png` ←→ 重出图 → `differingPixels=0 / totalPixels=690300`，`diffRatio=0`（字节数同为 87744）
  - `t544-r2b-31-1440x900-fullpage.png` ←→ 重出图 → `differingPixels=0 / totalPixels=2619360`，`diffRatio=0`（字节数同为 91656）

  ⇒ 取回后的源码**重出即当时被打分的那一版**，§六的 **93.3（≥90，打回项无）** 落在同一批产物上。

### 10.4 GATE-RUN 对账（本次抢救窗口）

对账源：`.scratch/locks/gate-runs.log` 里 `ticket=544` 的 RUN 条目，窗口 **2026-09-15T15:06:34Z → 2026-09-15T15:21:21Z** 内**全部**逐行抄录如下
（§2.3 第 4 条的导出就落在本节）；同名同 runId 的 `START` 行不重复抄：

```
RUN ticket=544 runId=b81f902e-c432-4954-ae82-86fa032b64ac cmd="pwsh -NoProfile -File .scratch/t544/rescue-restore.ps1" waitedMs=30025 exit=0 at=2026-09-15T15:06:37.223Z
RUN ticket=544 runId=2349c632-6251-411e-99bb-cb9690756be2 cmd="pwsh -NoProfile -File .scratch/t544/window-b.ps1" waitedMs=10008 exit=0 at=2026-09-15T15:08:14.051Z
RUN ticket=544 runId=dfe593a8-afec-41cf-a6b0-8fd61050dc05 cmd="pwsh -NoProfile -File .scratch/t544/window-mut.ps1" waitedMs=110081 exit=0 at=2026-09-15T15:21:05.595Z
RUN ticket=544 runId=10cc52e3-ffb6-466a-9d62-6f22c700692a cmd="pwsh -NoProfile -File .scratch/t544/rescue-commit.ps1" waitedMs=0 exit=0 at=2026-09-15T15:21:21.079Z
```

GATE-RUN runId=b81f902e-c432-4954-ae82-86fa032b64ac cmd=pwsh -NoProfile -File .scratch/t544/rescue-restore.ps1
GATE-RUN runId=2349c632-6251-411e-99bb-cb9690756be2 cmd=pwsh -NoProfile -File .scratch/t544/window-b.ps1
GATE-RUN runId=dfe593a8-afec-41cf-a6b0-8fd61050dc05 cmd=pwsh -NoProfile -File .scratch/t544/window-mut.ps1
GATE-RUN runId=10cc52e3-ffb6-466a-9d62-6f22c700692a cmd=pwsh -NoProfile -File .scratch/t544/rescue-commit.ps1

**对账实跑读数（`tooling/check-gate-audit.mjs`）—— 两个作用域各一条，都要看**：

- **本次窗口**（`--ticket 544 --since 2026-09-15T15:06:34Z --until 2026-09-15T15:21:30Z`）用**分件** `docs/skills/skill-calorie/t544-证据-抢救.md` 跑：
  `RESULT: matched=4/4 auditEntries=6792 scoped=4 undeclared=0` → **`gate-audit: PASS`（exit 0）**。四条声明一对一命中、窗口内无「无人声明」条目、四条 exit 全 0。
- **同一条命令用本件跑会红**（实测 `matched=3/11`，8 条报「未命中」）：工具**声明集按文件收、条目集按窗口收** ——
  本件同时携带上一手窗口（`13:49–14:35`）的七条声明（§2.1）与本次四条（§10.4），在本次窗口下上一手那七条一律「未命中」；
  另 `10cc52e3` 的 `at=15:21:21.079Z` 落在 `--until 15:21:21Z` **之外** 79ms，故 `scoped` 只数到 3。
  这是**工具的作用域限制，不是留痕缺失**（四条声明的条目已在上表逐行抄录，`.scratch/locks/gate-runs.log` 可逐条复核）。
- 处置：本次四条声明**另立可对账副本** `docs/skills/skill-calorie/t544-证据-抢救.md`（同批声明，只收本次窗口），使本次抢救窗口能被独立复核为绿；本件保留全部声明与读数。
  **本席不自行放宽任何口径**：`--allow-undeclared`／`--allow-no-runid`／`--allow-nonzero`／`--allow-no-claims` 一个都没开。

### 10.5 提交

- **`93c94740847297449db934f41be20e2865770a0f`**（短 sha `93c9474`），信息＝
  `功能(544): 抢救被 reset 的成果 —— 运动五页去分隔符形状化＋文案去冗余＋HELP 同档手机端（9 页重出全绿）`，
  ＝§10.2 那六条路径**逐条点名**提交（`git commit -F <信息件> -- <六条路径>`，包装器内跑，runId `10cc52e3`）。
  `git show --stat 93c9474` → **6 files changed, 1023 insertions(+), 130 deletions(-)**。
- 提交前复核 `git diff --cached --name-only`：别席在途的四件（`t449-字段中文标签.md`／`t486-字段绑定判据.md`／`t561-goal-no-source.md`／`field-label-bindings-486.test.mjs`）**留在暂存区、未进本提交**。
- **未推送**（纪律：绝对不要 push）。本次抢救共三笔提交，逐笔如下（`git log --oneline` 可查）：
  | 提交 | 信息 | 内容 |
  |---|---|---|
  | `93c94740847297449db934f41be20e2865770a0f` | `功能(544): 抢救被 reset 的成果 …（9 页重出全绿）` | §10.2 那六条路径（被救的成果本体） |
  | `9541537dc813416718d2f646360afe03a7679fed` | `文档(544): 抢救记录落盘 —— 取回提交 sha 与复跑读数补进 t544-证据件第十节` | 本件第十节 |
  | 第三笔（本节的对账分件那段） | `文档(544): 本次抢救窗口的对账分件 …` | 本件 §10.4 的对账作用域说明 ＋ `t544-证据-抢救.md` |

  （第三笔的 sha 见 `git log --oneline -1`；本节所写的读数在它落盘前已实测，笔数只增不改。）

### 10.6 未做项与归因（本轮）

- **视觉正式复评未重跑**（归因＝不必重跑）：§10.3 末尾的逐像素比对已证明重出产物与第 2 轮**被打分的产物逐像素相同**，93.3 就落在这批产物上；本轮只做抽查。若要另出一次正式分，下一手跑 `.scratch/t544/vision-brief-544-r2b.md` 那份 brief（`dsh --profile visionreview`，不占锁）即可。
- **抽查用的 `vision_describe` 读数与正式分不同（约 81／100，口径是它自拟的五维）**：它**没有**报任何渲染缺陷（无隐藏横滚、无截断、无 `·`／`；`、无 ≥6 位小数、纵轴刻度在），所提十条均为产品口径建议（其中「导航两行铺开」「结论句」「`×N` 计数」「口径行」正是本票**有意**的形状与 §6.2 豁免项）。**正式分仍以 §六为准**；本行只作抽查留痕，不取代 §六。
- **`#267 G4` 仍红**：属别席 #478（§八之一）；本轮只做归因，未自行放宽该条断言。
- **其余 30 页／链路总览页重出对齐、拆法搬迁**：归收口票（§八之四）。
- **未推送**：本席不推；只在本地 `master` 上追加提交。

## 十一、终审席返修（票 #268 打回项 · 第二次返修）

> 触发：票 #268 终审席报告 `.scratch/t268/视觉终评-全批39.md`（全批 39 页 · 口径乙 94.1）。它的 §4 P1 里有两条、
> §5 K／E 里有四条点名 **#544 的 31–39 九页**。本席按打回项逐条复现、逐条改，全部落本票写集（`sportPortDocs.ts`
> ＋本票判据件），不碰 `packages/base-render/**`（§11.6 写转票建议）。

### 11.1 逐条复现（打回就当刻的 `D:\ilife\.scratch\t541\out\31–39`）

| 打回项 | 复现命令 | 打回当刻读数 |
|---|---|---|
| **P1-5／K2** 38／39 纵轴印负数刻度 | `node .scratch/t544r2/dump-chart2.mjs ".scratch/t541/out/38-…html"`（抽 SVG 里 `class="ilife-charts-tick"` 的文本节点） | 38 页 `-87.4／1065.1／2217.7`；39 页同为 `-87.4／1065.1／2217.7`；37 页 `31.4／1118.0／2204.5` |
| **P1-6／K6** 35–39 三节无可见标题 | `node .scratch/t544r2/probe544.mjs <目录> <json> 35,37,38,39`（读 `h1–h6／卡题／表题` 的可见文本） | 「核心数字／类型分布／高频运动」三个词只出现在**页内导航胶囊**与分布条的 `aria-label` 里，正文无任何节标题 |
| **E1** 输入框 38px | 同上探针的 `input` 尺寸读数 | 九页一律 **434×38**（1440 档）／**173×38**（390 档）——页级 `min-height:38px` 把 #525 配方的 44px 压下来了 |
| **K1** KPI 首卡字号混用 | 同上探针的 KPI 卡值字号读数 | 九页一排四卡 **28／22／22／22**（首卡一条页级规则放大） |
| **K5** <1% 分布条读成空行 | 同上探针的 `.ilife-block-dist-row-fill` 实渲宽 | 38 页 1440 档「柔韧」条 **1.33px**、390 档 **0.55px**；37 页 390 档「柔韧」**0.4px** |
| **K4** 窄屏长序列读不出点在哪天 | 同上探针 + `geo3.mjs`（量折线绘图区与点簇的实渲坐标） | 37／38／39 三页 x 轴只有 `'edge'` 两条日期（共享层折线缺省口径），点簇实渲只占绘图区中段 |

### 11.2 改法（逐条 → 源码落点 → 改后读数）

1. **纵轴量程一次算齐**（`sportPortDocs.ts` 的 `valueAxisOf()`＋`axisHeightOf()`）：下界**恒 0**，上界取
   「刻度步长 ×（条数−1）」，步长从 1／2／5×10ᵏ 里挑「把峰值放在图高 0.60 附近」的那一档。
   读数（改后 38／39）：`0／2000／4000`（37 同为 `0／2000／4000`；35 本周窗 `0／1000／2000`）。
2. **三节可见标题**（`shell()` 里给 `<section>` 补首个子节点 `<h2>`；`Card.head` 只在复盘三节给）：正文可见。
   读数：`T268 HEAD 复盘（两日窗／长窗） sec-figures=核心数字 sec-category=类型分布 sec-badges=高频运动`。
3. **输入框回到 44px**（页级规则 `min-height:38px` → `44px`）：读数 `434×44`／`173×44`，最小命中区 44（改前 38）。
4. **KPI 一排同档**（撤掉首卡 28px 那条页级规则）：读数 `KPI 字号=22`（一排四卡同一档）。
5. **细条 4px 下限**（页级 `.ilife-block-dist-row-fill{min-width:4px}`，几何归样式层，不改共用件入参口径）：
   读数 38 页「柔韧」1440 档 1.33→**4px**、390 档 0.55→**4px**。
6. **长窗日期轴**（`xRulerOf()`）：≥45 天的窗口在折线卡**画布容器内**补一条**只出窗口内部日期**的轴
   （等宽槽＋轴线＋刻度点；内距按 viewBox 比例：左 10%＝58/580、右 2.414%＝1−566/580，再加 4% 槽内距）。
   读数 38 页 1440 档：`02-13／03-28／05-10／06-21／08-03`（5 条；首条左缘 34px，与绘图区左缘 85px 同一列域内），
   390 档 `02-13／03-28／05-10／06-21／08-03`（槽宽 52px，标签 42px ⇒ 空档 10px 不压字）。

### 11.3 视觉复评（第 3 轮，三次逐条量）

产物未定形前**连出三轮**，每轮的实扣分点都当场量、当场改（`dsh --profile visionreview`，`--child-timeout-ms` 那个旗标在当前版本已不存在，直接跑）：

| 轮 | brief | 报告 | 平均分 | 结论 |
|---|---|---|---|---|
| r3 | `vision-brief-544-r3.md` | `视觉复评544-r3.md` | **93.0（<93.3 打回）** | 唯一打回项＝日期轴初版：7 条标签（含窗口首尾）挤在卡片内宽中段 24.5%（52.91%→77.45%）压字，且与折线自带的首尾标签逐字重复 |
| r3b | 同上（产物已改第 2 版） | `视觉复评544-r3b.md` | **93.67（≥93.3 通过）** | 五条打回项全部「修掉了」；余留一条实扣分点＝日期轴**挂在折线卡外**（y=1423–1437 vs 卡底 1406）、轴线比绘图区左宽 100px |
| r3c | `vision-brief-544-r3c.md` | 未落盘（进程在写报告前被杀） | — | 其**几何读数仍可用**（下表就是它的读数）：桌面档标尺容器 335→1110、绘图区 380→1124.5 ⇒ 右端短 14.5px |

第 2 版之后的对齐是按 r3b／r3c 量出的偏差逐笔修的：**内距改成 viewBox 比例**（左 10%＝58/580、右 2.414%＝1−566/580）＋ 4% 槽内距。

### 11.4 机器证据（一条命令跑完本票全部门）

`node tooling/run-locked.mjs --ticket 544 -- node .scratch/t544/verify.mjs` → **`RESULT: ticket=544 runId=2f6bf419-afcc-4741-9dee-11ba7172f8b7 exit=0`**，五步全绿：

| 步 | 命令 | 读数 |
|---|---|---|
| ① 重出 39 条 | `.scratch/t544/run9.mjs` | `MINE9 n=9 exit0=9 complete=9 BAD=0`（本票 9 页逐条 exit 0 且文档完整） |
| ② 三档溢出 | `scripts/measure-responsive.mjs` | `OVERFLOW-ZERO pages=37 cells=111 failed=0 scopeOutFailed=0 label=run` |
| ③ 逐页读数 | `.scratch/t544/scan9.mjs` | `节点级命中 0 的页=9/9 行级 0 的页=9/9 exit0 的页=9/9 节点级命中合计=0`；`mw=1`（全大写常量 `JSON`，命中处是复制菜单的可见标签，非机器话） |
| ④ 回归＋新守卫 | `node --test`（453／454／267／544 四件） | `pass 35 / fail 0`（新增 `#268 ①…④b` 五条判据全绿） |
| ⑤ 告警线台账 | `check-warning-line.mjs` | `RESULT: 91/91`＋`PASS: 告警线台账齐全且与实况一致`（`sportPortDocs.ts` 当场实测经 `--sync` 同步到 **944**） |

- **提交**：**`82b063f`**（信息＝`修(544): 终审席五条打回项（纵轴量程锚 0＋三节可见标题＋输入框 44px＋一带四卡同档＋日期轴）`），
  四件逐条点名（`git add -- <四条路径>` → `git diff --cached --name-only` 复核只含这四件 → `git commit -F … -- <四条路径>`），
  `git show --stat 82b063f` → **4 files changed, 368 insertions(+), 28 deletions(-)**；**未推送**。
- 提交后自查：`git merge-base --is-ancestor 82b063f HEAD` → **exit 0**（在主干祖先链上）。

### 11.5 未做项与归因（本轮）

- **K4 的「零日子与有记录的日子在点簇上不可分」**：属共享层图表能力（`charts.ts` 只画点、不画「无记录」的日底色），
  本节不动它 ⇒ 转票建议（§11.6 第 4 条）。
- **`~`／`→` 混用、复盘 H1 带日期、眉标与 H1 两名并存**：分别由 `#454` 五窗同版判据与 `exercise-accept-267.test.mjs:528`
  钉死，不在本票写集（视觉复评 r2／r3 均判「属别票判据、不扣分」）。

### 11.6 转票建议（共用层缺口，本票不修）

| # | 缺口 | 症状（本票实测） | 建议去处 |
|---|---|---|---|
| 1 | **折线 x 轴标签口径只有 `'edge'`／`'all'`／`'select'`**，没有「按画布宽定条数」的一档 | 201 天窗的 x 轴只有首尾两条日期（`labels:'edge'` 是 line 的缺省）；调用方只能自己在卡外补一把日期轴（本票 `xRulerOf()` 就是那个绕法） | 共用层图表件（建议加 `labels:'auto'`：按画布宽与标签宽估条数、等距取点） |
| 2 | **折线纵轴量程缺省会把下界算到 0 以下**（`domainOf` 两端各外扩 6%） | 38／39 页印出 `-87.4`；调用方只能自己显式给 `yMin/yMax`（本票 `axisHeightOf()`） | 共用层图表件（建议：数据全非负时下界夹 0） |
| 3 | **图表刻度字号最小档 10.8px（≥876px 档）** | 与正文最小档 12px 不成套（票 #268 §5 B2） | 共用层图表件 |
| 4 | **「窗内没有记录的那一天」在图上看不出来**（只能靠断线猜） | 37／38／39 的点簇稀疏处与「有记录但值低」不可分 | 共用层图表件（建议：给折线加「无数据日」的浅底纹或轴下小刻度语义） |
