# #509 · 卡路里场景 02 饮食「文本清尾」· 证据

- 票：#509（地图 #155 子票）　分支：`master`　日期：2026-09-15
- 范围：**探针盲区里的两处残留**（33／81 校验批量导入那套内部词 ＋ 02／05／06 同形页标题）＋ **把回查探针升级成按页按类型**。
- 主线读数：真跑 83 条词 → 81 页产物（2 条按设计 `exit 4` 缺失阻断）→ 抽可见文本 **33,524 字**。

## 一、编排者裁定（本席照办）

`05 拍营养表记一餐` 这一页「按唤醒词出标题」没有别的实现路径：`calorie.diet.add` 收不到唤醒词，而两个拍照入口的参数与普通记一餐／补记饮食全同（都带 `note`）——拿 `note` 的文字判来源＝按夹具的文字判来源，不成立。编排者裁定 **(A) 批准**，逐条授权与做法：

1. 允许改 `src/diet/routes.ts` 的 **cli 参数**（在本票声明路径 `src/diet/**` 内）；
2. 允许跑 `pnpm gen` 重生成 `src/triggers/routes.generated.ts`（**生成物只能派生、不许手改**），跑完必须 `pnpm gen:check` 确认同步；
3. **05 与 06 一起做**（两条拍营养表的词都要能看出「拍照／营养表」），并对 `01／02／03／05／06` 加一条判据：**抹平时间戳后两两不再逐字节相同**；
4. 参数名自定，但三条硬要求：未知参数不报错、参数名绝不上屏、复制日志的命令原文要带全它（照抄可重跑）。

**做法**：参数名 `source`，取值 `photo`；先例＝同文件 `看有备注的饮食记录` 那条由 #276 给 `calorie.today` 带 `{"hasNote":true}`（同一文件、同一手法）。

## 二、落入的文件（LF 按节点口径 `split('\n').length - 1`）

| 件 | LF | 改动 |
|---|---|---|
| `packages/skill-calorie/src/render/trendMiscPortDocs.ts` | 650（**超线，已挂号**） | 33／81 的读数卡与逐条表改读者的话（读数卡两组标签＋两条说明、四个列名、单元格两处、副标题去掉命令名） |
| `packages/skill-calorie/src/diet/log.ts` | 140 | 读 `source` 取标题与副题（拍照记一餐／拍照补记一餐 ＋「来源：营养表照片」） |
| `packages/skill-calorie/src/diet/routes.ts` | 99 | 两条拍营养表记录的 cli 参数加 `"source":"photo"`（唤醒词、命令名、kind 一字不动）＋ 一段说明注释 |
| `packages/skill-calorie/src/triggers/routes.generated.ts` | 生成物 | `pnpm gen` 派生（**非手改**），只动上面那两条记录的两段参数 |
| `packages/skill-calorie/test/t509-清尾.test.mjs` | 新 | 本票探针 5 例（①33／81 ②五页标题与两两不同 ③来源标记三条硬要求 ④变异自证） |
| `packages/skill-calorie/AGENTS.md` | — | 告警线台账（`--sync` 刷当场实测 ＋ 本件结论列补 #509 一段） |
| `docs/skills/skill-calorie/t509-清尾-证据.md` | 新 | 本件 |
| `.changeset/t509-清尾.md` | 新 | 变更单 |
| `.scratch/t509/**` | 新 | 升级后的探针（`p0-page-recheck.mjs`）＋ 变异／归因／复跑脚本 |

## 三、残留一 · 33／81 两页的内部词归零（票面第 46／47 条）

两页是**同一条命令**（`calorie.view.batch-import-preview`）的两个入口，同形；改动只落在 `buildBatchImportPreviewDoc`。

| 位置 | 改前（逐字） | 改后（逐字） |
|---|---|---|
| 读数卡① | `已匹配库` ／说明 `精确名匹配` | `食品库里有同名的` ／说明 `可以直接导入` |
| 读数卡② | `未匹配` ／说明 `导入后建议补录` | `食品库里没有的` ／说明 `建议先「存食品」再导入` |
| 表头 | `热量卡`｜`库热量`｜`匹配` | `这一条的热量（卡）`｜`食品库里的热量（卡）`｜`是否已匹配` |
| 单元格 | 库热量格 `—`；状态格 `✗ 缺库` | `食品库中无此食物`；`未匹配`（命中时 `已匹配`） |
| 副标题 | `库匹配＋合计试算（仅预览，不写库；确认后走 diet.batch 写入）` | `食品库对照＋合计试算（只预览，不写库；确认之后再写入）` |

**副标题这一处是票面六词之外的同一类残留（本席一并清）**：`diet.batch` 是**源码命令名直接上屏**——正是同页第 10 条被判过的毛病（「源码命令名印在页面上」），#496 清了眉标那一处、漏了副标题这一处；`库匹配` 也是读数卡那套内部说法。改法与 #496 已定口径同（读者的话＋标识符不上屏，`t425-融合基准.md` 裁定 1）。

**「未匹配」这个词的处理**：票面改法点名要写它（明细表那一列写「未匹配」），故它作为**读者的话**留在表里；清掉的是它当**读数卡的卡标签**那一处。探针按页按位置判：读数卡区（「逐条预览」之前的可见文本）不得出现「未匹配」。

## 四、残留二 · 同形页标题按唤醒词对上（票面第 21／27 条）

改前：`01／02／05` 三页的标题/副题只差时间戳（02 与 05 连标题都同是「记一餐（含备注） · 回执」）。改后五页标题逐字：

```
01_记一餐.txt            → 记一餐 · 回执
02_记一餐（含备注）.txt    → 记一餐（含备注） · 回执
03_补记饮食.txt          → 补记饮食 · 回执
05_拍营养表记一餐.txt      → 拍照记一餐 · 回执      ＋副题「… · 来源：营养表照片 · 备注：营养表识别」
06_拍营养表补记一餐.txt     → 拍照补记一餐 · 回执    ＋副题「… · 来源：营养表照片 · 备注：营养表补记」
```

「来源」这一行落在**副题**（标题后那一行）：与 #496 落「备注原文」同一位置，读者一眼能看到「这顿是照着包装营养表记的」，理由＝同族页的差异都走副题、不另造区块（`t425-融合基准.md` 裁定 2-补：结论类小字行不走深底块）。

**`source` 参数的三条硬要求（编排者裁定 A 第 4 条）逐条落法**：① 未知参数不报错——命令一直是按名取参（不给就 `undefined`），第 ③ 例用一个命令不认识的参数跑了一遍，`exit 0`；② 参数名不上屏——`source` 只出现在**复制载荷**里（有意承载可照抄技术原文的那几段），可见文本里读不到；③ 命令原文带全——复制日志「调用链」那一段是可照抄重跑的命令原文（`LOG_SECTIONS` 的第 4 段；编排者信里写的是第 3 段，按 `base-render/src/spec/text.ts:25` 的实际段序是第 4 段），它带 `"source":"photo"`，照抄能跑出同一页。

## 五、判据实测读数（逐条）

| 判据 | 命令 | 读数 |
|---|---|---|
| ① 33／81 内部词归零 | `node .scratch/t509/p0-page-recheck.mjs` | 两页面内 16 条**全绿**；P0 #46 `精确名匹配=已清`、`未匹配=该留（只在明细表那一列）`、`导入后建议补录=已清`；P0 #47 `热量卡=已清`、`库热量=已清`、`✗ 缺库=已清`、`—=已清` |
| ① 五页标题对得上唤醒词 ＋ 两两不逐字节相同 | 同上「同形页检查」段 | 五页标题逐条 true；**两两逐字节相同 = 0 对** |
| ② 探针升级自证（两类盲区夹具） | `node .scratch/t509/p0-page-recheck.mjs --fixtures` | `FIXTURES PASS`；见第六节 |
| ③ 本票探针 | `node --test packages/skill-calorie/test/t509-清尾.test.mjs` | `tests 5 / pass 5 / fail 0` |
| ③ 本包全量测试 | `node --test "packages/skill-calorie/test/*.test.mjs"` | 见第七节（读数与逐条归因） |
| ③ 告警线台账 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | 改前 `RESULT: 63/65`（本件陈化）→ `--dry` 演练 → `--sync` → **`RESULT: 65/65` ＋ `PASS: 告警线台账齐全且与实况一致`** |
| ④ 变异自证 | `node .scratch/t509/mutation.mjs` | `CLEAN pass=5 fail=0` → 改坏 `pass=1 fail=4` → 还原 `pass=5 fail=0`：`MUTATION PASS` |
| 生成物同步 | `pnpm gen` ＋ `pnpm gen:check` | `GEN-CHECK PASS：键 127（写 46 ＋ 读 81）…` |

## 六、探针升级：按页 ＋ 按类型（`.scratch/t509/p0-page-recheck.mjs`）

**旧件为什么看不见**（`.scratch/t155o/p0-recheck.mjs`）：全文子串匹配，两类引文永远查不到——
① 「 / 」分隔的（「精确名匹配 / 未匹配 / 导入后建议补录 / 警告」整串不在页面上）；② 描述型（「全页与 01_记一餐.txt 逐字节相同」不是页面上的字）。

**新件的判据**：`substring`／`slash`／`desc` 三型——slash 按「 / 」拆开**逐个在该页**查；desc 必须命中**具名判据**（同形页判据＋标题判据），没登记判据的 desc 行报「判据待定」**不再静默算绿**（旧件的病根）；页面范围＝票面点名的**那一页**，页不在场报「缺页」。

**同一份改坏的字节，新旧对照**（`--fixtures` 里的读数，把两处残留一起塞回去）：

```
· P0 #46 33_校验批量导入　旧件=已清（片段「精确名匹配 / 未匹配 / 导入后建议补」命中 0 页）　新件=还在（slash）
· P0 #47 33_校验批量导入　旧件=已清（片段「热量卡 / 库热量 / 匹配 与单元格 」命中 0 页）　新件=还在（slash）
· P0 #21 02_记一餐（含备注）　旧件=已清（片段「全页与 01_记一餐.txt 逐字节相同」命中 0 页）　新件=还在（desc）
· P0 #27 05_拍营养表记一餐　旧件=已清（片段「全页与 01_记一餐.txt 逐字节相同」命中 0 页）　新件=还在（desc）
FIXTURE ok   新旧对照：旧件对 4 处残留全部报「已清」（查不到就当清了），新件全部报「还在」=4/4
```

**面外读数是告信息，不判绿**：面内 16 条全绿；面外 71 条里「判据待定 2 条」（78 看营养素明细／80 看今日饮水——两张同源入口页，归别的票）、「还在 10 条」（18／38／41／60／65／75／77／82／83 页，属别的页票）。

## 七、不新增红（本包全量测试）

- 命令：`node --test "packages/skill-calorie/test/*.test.mjs"`（经共享闸门锁 `tooling/run-locked.mjs --ticket 509`；读数落 `.scratch/t509/suite-509.txt`）。
- 读数：**`tests 989 / suites 4 / pass 942 / fail 47`**（47 行里含一行 `✖ failing tests:` 段标，去重后**红点 46 条**，落在 **21 件**测试文件：`analysis-t5`／`cmd-read-t11`／`cmd-write-40`／`cmd-write-40-persist`／`copy-component-179`／`diet-library-t274`／`docpage-printable-448`／`field-labels-449`／`help-center-106`／`home-lock-374`／`profile-doc-179`／`render-copy-90`／`render-t41`／`softdelete-120`／`sport-homogeneity-109`／`t401c-页面机器话探针`／`t445-告警线门`／`weight-history-333`／`weight-receipt-337`／`weight-review-335`／`weight-volatility-336`）。
- **归因自证**（`node .scratch/t509/attribution.mjs`，读数落 `.scratch/t509/attribution-509.txt`）：把本票两处改动在**构建产物**上退回旧写法（`dist/diet/log.js` 的 `photo` 开关与三处文案、`dist/render/trendMiscPortDocs.js` 的九处文案），再跑上面那 21 件：

```
红件（取自 .scratch/t509/suite-509.txt）= 21 件
退回旧写法：fail=46
还原本票改动：fail=46
ATTRIBUTION PASS：红点与本票改动无关（同 46 条，名一一对上）
```

  ⇒ **那 46 条红与 #509 无关**；按件看，主体是别的席在途件在改的件：体重／波动（`weight-history/receipt/review/volatility`＝#504／#505／#333／#335／#336／#337 席）、运动（`sport-homogeneity-109`＋`docpage-printable-448` 的运动回执金标）、HELP 映射面（`help-center-106`）、复制区接线（`render-copy-90`／`copy-component-179`／`profile-doc-179` 里的 #247 用途提示与 `calorie.water.log` 眉标＝#496 起就陈化的引脚）、`t401c`（首页那页的复制菜单 `JSON` 标签＋结论条）、`t445` 的 T5／T9（台账与当刻实况——另两席同一分钟内又改了 `weight/compare.ts`／`weight/volatilityDoc.ts`，台账随即陈化，属并发席的正常抖动，本席当场复跑为 `RESULT: 65/65`）。
- **本票改动面直接覆盖的测试件都不在上面那 21 件里（＝全绿）**：`t509-清尾`（5/5）、`trend-misc-port-113`（批量导入预览页）、`diet-homogeneity-108`、`calorie-c43`、`m5-receipt-97`、`t496-文案统一`、`db-readonly-93`；逐条点名见 `.scratch/t509/suite-509.txt`。


## 八、改了生成物（必读）——`src/triggers/routes.generated.ts`

- 改了什么：只有两条记录（`拍营养表记一餐` order 13／`拍营养表补记一餐` order 14）的 cli 参数末尾各加 `,"source":"photo"`；**唤醒词、命令名（`calorie.diet.add`）、`scene`、`kind`、`list`、`order` 一字不动**。
- 怎么改的：改**声明** `src/diet/routes.ts` → `npx tsc -b packages/skill-calorie` → `node packages/skill-calorie/scripts/gen-cli.mjs --stamp` → `… gen-cli.mjs`（写盘）→ 再 `tsc -b` → `… gen-cli.mjs --check`。逐条读数：`GEN-STAMP ok … 声明源 41 件`、`GEN-CHECK PASS：键 127（写 46 ＋ 读 81）`。
- `src/cli/keys.ts`／`src/cli/registry.ts` 未被改动（生成器跑完它们逐字节不变，哈希在 `GEN-CHECK ok` 里列出）。
- 影响面：运行时只有 `calorie.diet.add` 的这两个入口多带一个参数；命令侧不给这个参数时**行为与从前一字不差**（口径写在该处注释与测试第 ③ 例里）。

## 九、超线件第四步（必报）

**已超线，需要根据规则进行重构。**
- `src/render/trendMiscPortDocs.ts` 641→**650**：超因＝#113「趋势 2＋其他 6」共 8 键的 HTML 填充器同处一件，本票又给其中一页（批量导入预览）补了两处读者话、去了一处命令名，另加逐段注释。
- 拆法：按页族切姊妹件（营养分析／六因素／批量导入预览／其余薄页分开），块层共用的小件（`windowForm`／`dateLabelOf`／`axisOf`）提为共件，出口经本件薄转出。
- **本次先不拆**：拆分不在 #509 写集（票面只动可见文本与标题，编排者已裁「一行不搬」）。台账已按 `--sync` 同步（`RESULT: 65/65`）。
- 其余件均在 350 以内：`src/diet/log.ts` 140、`src/diet/routes.ts` 99、`src/diet/receipt.ts` 239（未改）。

## 十、没做与为什么

- **不重审 #496 已清的 71 条**（票面「不做」）：升级后的探针把它们逐条按页打了出来（面内 16 条全绿），没有逐条复核改法。
- **不动唤醒词、命令名、别的图的页面、`src/render/html.ts`**：`src/diet/routes.ts` 只改了 cli 参数（编排者裁定 A 第 1 条），生成物由 `pnpm gen` 派生。
- **不做 #270–#277 七张页票的内容重做**：33／81 两页的版式（老实物 `batch_import_preview.html` 那套九读数／进度条／页签）不在本票写集，本票只改可见文本。
- **面外 10 条「还在」与 2 条「判据待定」**：见第六节末，属别的页票。
- **陈旧引脚**：本票的测试改动没有把别的件的引脚改红（`diet-homogeneity-108`／`calorie-c43`／`m5-receipt-97`／`trend-misc-port-113`／`t496-文案统一` 这几件覆盖本票改动面的测试**全绿**）；全量里剩下的红逐条归因见第七节。

## 十一、复跑清单

```powershell
node tooling/run-locked.mjs --ticket 509 --max-wait-ms 900000 -- pwsh -NoProfile -File .scratch/t509/regen.ps1   # 声明改了才跑：tsc → 印记 → gen → tsc → gen:check
node tooling/run-locked.mjs --ticket 509 --max-wait-ms 900000 -- pwsh -NoProfile -File .scratch/t509/verify.ps1  # 编译 → 真跑 83 条词 → 抽文本 → 探针（含夹具）→ 本票 5 例
node .scratch/t509/p0-page-recheck.mjs --fixtures                                                               # 探针 按页＋按类型 ＋ 两类盲区夹具
node --test packages/skill-calorie/test/t509-清尾.test.mjs                                                       # 本票探针 5 例
node packages/skill-calorie/scripts/check-warning-line.mjs                                                       # 告警线台账
node tooling/run-locked.mjs --ticket 509 --max-wait-ms 900000 -- pwsh -NoProfile -File .scratch/t509/tests.ps1   # 本包全量 ＋ 变异自证
node tooling/run-locked.mjs --ticket 509 --max-wait-ms 900000 -- pwsh -NoProfile -File .scratch/t509/suite.ps1   # 全量读数落文件 ＋ 红点归因自证
```
