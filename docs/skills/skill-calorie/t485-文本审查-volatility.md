# #485 卡路里场景 03 体重 · 波动族（5 词）文本审查 · 证据件

> 范围：`#336` 融合页 `calorie.view.volatility`（一命令两读法，共一份页装配）下的 5 条唤醒词——
> 第 5 条「看体重稳不稳（增强版）」／第 13 条「看本月波动」／第 20 条「看最近 90 天波动」／
> 第 27 条「看最近 180 天波动」／第 34 条「看波动异常点」。
> 统一口径正本：`.scratch/t154/text-review/口径.md`（四席照做）；本件只记本族逐页结论与机器读数。
> 读者画像：不懂技术的项目负责人（不知道 σ／rolling／阈值／基线是什么，也不想在看「这周体重怎么样」时读实现细节）。

## 一、写集与改前后 LF

| 件 | 改前 LF | 改后 LF | Δ | 改了什么 |
|---|---|---|---|---|
| `packages/skill-calorie/src/weight/volatility.ts` | 242 | 260 | +18 | 偏离量两套写法（值槽带符号 `deviationText` ＋ 句子带方向词 `baselineDeltaText`，新增）、平均线口径标签（`vs 目标 68kg`／`vs 近 30 天均值` → `目标体重 68 kg`／`近 30 天平均体重`）、今日偏离副说明句、异常原因句（`anomalyReason` 收参）、结论句（`volatilitySummary`） |
| `packages/skill-calorie/src/weight/volatilityDoc.ts` | 310 | 317 | +7 | 档位词（黄／红 → 注意／警戒）、副标题、四张 KPI 卡、两张图题与图上标注、异常表（表注／表头／表行／空态句）、页顶提示、页脚来源行、只看异常点页顶提示、复制日志来源段 |
| `packages/skill-calorie/test/weight-volatility-336.test.mjs` | 120 | 126 | +6 | 旧断言随文本同步收紧（曲线段／波动幅度趋势／全角冒号 needle）＋ 新增「旧术语句命中数恒 0」判据 |

- 两件源码都远在 350 告警线内（260／317），**不需要**动 `packages/skill-calorie/AGENTS.md` 台账（台账归编排者）。
- 本票一行未碰：眉标（`eyebrow`）、复制区形态与深色 toast、`plate.ts`／`plateDocs.ts`／`figures.ts`／`records.ts`／`review.ts`／`shared/*`／`packages/base-render/*`、
  第 48 条整页、`test/weight-wake-58-339.test.mjs`、`test/render-t41.test.mjs`、`.scratch/t154/delivery/`。
- 出页命令（未手改 HTML，产物由源拼出）：`node .scratch/t154/text-review/render-family.mjs --family volatility --out .scratch/t154/text-review/out-volatility`。

## 二、逐页表（5 页逐条过）

> 逐字文本出处：改前＝`.scratch/t154/text-review/inventory/volatility.md`（本轮基线）；
> 改后＝`.scratch/t154/text-review/b485/after-volatility.md`（同一抽槽脚本、源换成本族 `out-volatility`，见 §四）。
> 判据列：**冗**＝百分百冗余（同一事实别处已有）／**难**＝专业话读者看不懂／**形式**＝需要更好的显示形式。

### 05 看体重稳不稳（增强版）｜`--params '{"window":"7d"}'`

| 槽位 | 现文本（改前，逐字） | 判据 | 判定 | 改后文本（逐字） | 依据 |
|---|---|---|---|---|---|
| 副标题 | `窗口 2026-09-01 ~ 2026-09-07 · 7 条记录 · 基线 70.37 kg` | 冗＋难 | 删＋改 | `以近 7 天平均体重为参照` | 窗口已在 H1、条数已在页脚「共 7 条」、值已在卡① 值槽——三段全冗余；只留整页唯一的「参照是谁」，并给 `平均线` 第一次见面的人话解释（口径 §3.2 `基线`→「平均线」） |
| 卡① 标签 | `波动分析` | 冗 | 改 | `平均线` | 与页标题 `波动分析 …` 同字，卡名不提供信息（口径 §3.5） |
| 卡① 说明 | `基线 vs 近 7 天均值 · 7/7 天有记录` | 难 | 删＋改 | `7/7 天有记录` | `vs`／`均值`／`基线` 三个术语；定义已上移副标题，卡里只报记录齐不齐 |
| 卡① 徽章 | `共 7 条` | 冗 | 改 | `记录齐全` | 条数页脚已有一处；徽章按口径 §3.5 只说状态 |
| 卡② 标签 | `阈值` | 难 | 改 | `波动带` | 口径 §3.2：`阈值`→合称「波动带」 |
| 卡② 说明 | `黄线 ±0.073kg · σ=0.049kg · rolling 基线 · σ 对照 5 点` | 难＋冗 | 改 | `警戒线 ±0.098 kg · 注意线 ±0.073 kg · 波动幅度 0.049 kg` | 四个术语全换人话（口径 §3.2）；值槽那个 `±0.098` 由本句认领（否则读者不知道它是哪条线）；`σ 对照 5 点` 是本页内部标定量，删（去处见 §三） |
| 卡② 徽章 | `正常对照` | — | 改 | `按本窗数据` | 徽章说状态（两条线是从本窗算出来的），不说判语 |
| 卡③ 标签 | `预警` | 冗 | 改 | `今日偏离` | 与徽章 `正常`／`注意`／`警戒` 重复表达「有没有事」；标签应说这个数是什么 |
| 卡③ 值槽 | `±0.03kg` | 形式 | 改 | `+0.03 kg` | 口径 §3.3：差值一律带符号与单位（值槽也一样）；带符号才看得出偏高还是偏低 |
| 卡③ 说明 | `今天在正常范围内(±0.03kg)` | 难＋冗 | 改 | `今天比平均线高 0.03 kg，在正常范围内` | 半角括号＋裸数字（没说是相对谁）；值槽已有数字，句子里补的是方向与参照 |
| 卡④ 说明 | `黄线 0 · 红线 0 · 共 7 点` | 难 | 改 | `近 7 天：超过注意线 0 天 · 超过警戒线 0 天` | 颜色词换线名；`点` 换读者单位 `天`；同时点明这是**近 7 天**口径（异常点按近 7 天取，90／180 天页尤其容易被误读成本窗全覆盖） |
| 卡④ 徽章 | `无异常点` | — | 保留 | `无异常点` | 已是状态词 |
| 图① 图题 | `偏离基线（黄±0.073 红±0.098kg）` | 难＋冗 | 改 | `每天离平均线多远` | 术语＋同一组数字第四次出现；图题只回答「这张图在问什么」，两条线的数在卡② |
| 图① 标注 | `红线 ±0.098kg` | 难 | 改 | `警戒线 ±0.098 kg` | 线的名字随口径 §3.2；数字单位补空格（§3.3） |
| 图② 图题 | `σ 趋势` | 难 | 改 | `波动幅度趋势` | 口径 §3.2：`σ`→「波动幅度」 |
| 图② 空态 | `点数不足，出不了 σ 趋势（要 ≥3 条记录）` | 难 | 改 | `记录太少，画不出波动幅度趋势（每段时间至少 3 条记录才看得出差别）` | 口径 §3.2：`样本不足`→「记录太少」；`≥3 条` 补上「才看得出差别」这句为什么 |
| 表 空态句 | `近期无异常点（基线 70.37 kg，黄±0.073 红±0.098）` | 冗＋难 | 改 | `近期没有异常点（平均线 70.37 kg）` | 阈值两个数在卡② 已有（空态句自带口径＝自带参照，不必抄满一屏） |
| 结论 | `体重很稳：标准差 0.05kg，无异常点，日均波动 0.017kg；2026-09-07 偏离基线 0.03kg，档位「正常」（σ 趋势 5 点）。` | 难＋形式 | 改 | `体重很稳：波动幅度 0.05 kg，没有异常点，最近 7 天平均每天变化 0.017 kg；2026-09-07 比平均线高 0.03 kg，在正常范围内（波动幅度趋势 5 个点）。` | 标准差→波动幅度、日均波动→最近 7 天平均每天变化、偏离基线→比平均线高／低、`档位「正常」`→`在正常范围内`、σ 趋势→波动幅度趋势；单位空格按 §3.3 |
| 口径行（页脚） | `📊 数据来源:calorie_data.db · weight_log · 窗口 2026-09-01 ~ 2026-09-07（7 天 · 7 条记录 · 窗口内记录齐）` | 形式 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-09-01 ~ 2026-09-07 ｜ 共 7 条` | 口径 §3.1 统一句式（全角冒号、库表名进括号、三段 `｜`、末段恒为「共 N 条」）；`weight_log` 退到括号里，正文用「体重记录」 |
| 眉标／复制区 | `calorie.view.volatility · 运动身体域` ／ `复制数据 ▾ ＋ 三格式 ＋ 复制日志` | — | 不动 | 同改前 | 本轮四族一致：眉标等用户裁定；复制区形态与深色 toast 是用户 2026-09-12 卡面 |

### 13 看本月波动｜`--params '{"window":"本月"}'`

本页与第 5 条是**同一命令、同一实际窗口（2026-09-01 ~ 2026-09-07）**，页面逐字相同：上表 20 行改动**逐条同样适用**，改后文本逐字见 `.scratch/t154/text-review/b485/after-volatility.md` 第 18-33 行（本页无独有槽位）。**本页无其它问题。**

### 20 看最近 90 天波动｜`--params '{"window":"90d"}'`

与第 5 条共享的 20 行改动不再重复；下表是本页**独有**的槽位（窗口 90 天、有 5 个异常点）：

| 槽位 | 现文本（改前，逐字） | 判据 | 判定 | 改后文本（逐字） | 依据 |
|---|---|---|---|---|---|
| 副标题 | `窗口 2026-06-10 ~ 2026-09-07 · 90 条记录 · 基线 70.27 kg` | 冗＋难 | 删＋改 | `以近 30 天平均体重为参照` | 同上；本页还多一层难：窗口 90 天但参照是近 30 天平均，旧文案写成「基线 70.27 kg」读者无从知道这 30 天从哪来——副标题现在直说「近 30 天平均体重」 |
| 卡① 说明／徽章 | `基线 vs 近 30 天均值 · 90/90 天有记录` ／ `共 90 条` | 难＋冗 | 改 | `90/90 天有记录` ／ `记录齐全` | 同上 |
| 卡② 说明 | `黄线 ±0.073kg · σ=0.049kg · rolling 基线 · σ 对照 88 点` | 难＋冗 | 改 | `警戒线 ±0.098 kg · 注意线 ±0.073 kg · 波动幅度 0.049 kg` | 同上；`σ 对照 88 点` 的去处在结论句 |
| 卡③ 值槽 | `±0.13kg` | 形式 | 改 | `+0.13 kg` | 本页是越线页：带符号后「偏高」一眼可见（口径 §3.3） |
| 卡③ 说明 | `今偏离 ±0.13kg 超过 2sigma 红线，谨紧张` | 难 | 改 | `今天比平均线高 0.13 kg，超过警戒线 ±0.098 kg，请留意` | `2sigma`／`红线`／`谨紧张`（原文疑为错字）全换人话；补上「超过多少算警戒」那个数 |
| 卡③ 徽章 | `红` | 难 | 改 | `警戒` | 颜色裸字换线名（口径 §3.2） |
| 卡④ 说明／徽章 | `黄线 0 · 红线 5 · 共 90 点` ／ `黄线 0 / 红线 5` | 难＋冗 | 改 | `近 7 天：超过注意线 0 天 · 超过警戒线 5 天` ／ `有异常点` | 徽章与说明原来把同一组数说两遍；徽章退成状态词；`共 90 点` 被读成「窗口全覆盖」的风险一并消掉（异常点只取近 7 天） |
| 表 表注 | `近 7 天异常点（共 5 个，黄/红阈上）` | 难 | 改 | `近 7 天异常点（共 5 个，超过波动带的点）` | `黄/红阈上` 是三词合写的术语；「波动带」是卡② 的卡名，同一页同词 |
| 表 表头 | `日期/体重/偏离/级别/原因` | 难 | 改 | `日期/体重/偏离平均线/级别/原因` | 「偏离」没说是相对谁；本页参照是近 30 天平均体重，「偏离平均线」与副标题、卡名同词 |
| 表 行（5 行同构） | `2026-09-03 ｜ 70.4 kg ｜ 0.13 kg ｜ 红 ｜ 偏离基线 +0.13kg，超过 2σ 红线（黄±0.073 红±0.098）` | 冗＋难 | 改 | `2026-09-03 ｜ 70.4 kg ｜ +0.13 kg ｜ 警戒 ｜ 超过警戒线` | 原因列原来把「偏离多少」与「两条线的数」重复了一遍（同一行里 `+0.13kg` 出现两次、两个阈值数 5 行 × 各 1 次）；偏离值归偏离列（带符号）、越哪条线归原因列，两数留在卡② |
| 结论 | `体重基本稳定：标准差 0.16kg，异常 5 次（黄 0/红 5）；2026-09-07 偏离基线 0.13kg，档位「红」（σ 趋势 88 点）。` | 难 | 改 | `体重基本稳定：波动幅度 0.16 kg，近 7 天有 5 天超过波动带（注意线 0 天／警戒线 5 天）；2026-09-07 比平均线高 0.13 kg，超过警戒线（波动幅度趋势 88 个点）。` | 术语换人话；`异常 5 次（黄 0/红 5）` 的 `次` 在按天记录的页里应读作 `天`，并补「近 7 天」这个真实口径 |
| 口径行（页脚） | `📊 数据来源:calorie_data.db · weight_log · 窗口 2026-06-10 ~ 2026-09-07（90 天 · 90 条记录 · 窗口内记录齐）` | 形式 | 改 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-06-10 ~ 2026-09-07 ｜ 共 90 条` | 口径 §3.1 |

### 27 看最近 180 天波动｜`--params '{"window":"180d"}'`

与第 20 条共享全部槽位改动（本页把 90 换成 180、`88 点` 换成 `178 点`、结论首句换成 `体重波动较大：…，建议关注饮食与饮水`）。
**本页无独有槽位、无其它问题**；改后逐字见 `b485/after-volatility.md` 第 51-65 行。

### 34 看波动异常点｜`--params '{"window":"7d"}'`（本页在 delivery 里是**整图读法**）

- 页面逐字与第 5 条相同 → 上表 20 行改动逐条适用。**本页无独有槽位。**
- 前置事实（如实登记）：本页在交付产物里**没有**带 `view: 'anomalies-only'`，出的是整图（两张曲线都在，没有「这一页只看异常点」页顶提示）。
  因此「只看异常点」读法那三句改动（页顶提示标题／表注／副标题）**不在出页里可见**，由用例覆盖（`#336 只看异常点：不含曲线段、含异常点与原因` 用 `view:'anomalies-only'` 真跑）。

## 三、删掉的信息登记（不许丢）

| 删掉的文字（改前逐字） | 原位置 | 去处（改后还看得见吗） |
|---|---|---|
| `窗口 2026-09-01 ~ 2026-09-07` | 页副标题 | H1 标题 `波动分析 2026-09-01 ~ 2026-09-07` ＋ 页脚来源行（同数） |
| `7 条记录` / `90 条记录` / `180 条记录` | 页副标题 | 页脚来源行末段 `｜ 共 7 条`（同数） |
| `基线 70.37 kg` / `基线 70.27 kg` | 页副标题 | 卡① 值槽 `70.37 kg`（同数） |
| `共 7 条` / `共 90 条` / `共 180 条` | 卡① 徽章 | 页脚来源行 `共 N 条`；`无缺口` 由新徽章 `记录齐全` 承担 |
| `σ 对照 5 点` / `88 点` / `178 点` | 卡② 说明 | 结论句 `（波动幅度趋势 N 个点）`（整图读法）；只看异常点读法不提（该读法没画那张图） |
| `rolling 基线` | 卡② 说明 | 页副标题与卡① 说明的「近 30 天平均体重」；复制载荷 `基线模式: rolling` 仍在（机器面） |
| `正常对照` | 卡② 徽章 | 换成语义等价的状态词 `按本窗数据`（记录不足时 `兜底值`） |
| `黄±0.073 红±0.098kg` | 图① 图题 | 卡② 说明（两条线的数，逐个在场）＋ 图上标注 `警戒线 ±0.098 kg` |
| `黄±0.073 红±0.098` | 表空态句 | 卡② 说明（同一页、同一屏） |
| `偏离基线 +0.13kg，（黄±0.073 红±0.098）` | 表行原因列 | 偏离列 `+0.13 kg`（带符号）＋ 级别列 `警戒` ＋ 卡② 的两条线数字；原因列只留 `超过警戒线` |
| `7 天 · 7 条记录 · 窗口内记录齐` | 页脚口径行 | 窗口天数与缺口改由同一行 `｜ 缺 N 天没记`（有缺口才出现；本族 5 页实测 0 缺口，故不出现）；`窗口内记录齐`→卡① 徽章 `记录齐全` |
| `本读法只列越阈异常点，不出偏离曲线（要看整图请说「看体重稳不稳（增强版）」）` | 只看异常点页副标题 | 页顶提示 `这一页只看异常点…`（同页同义句，原来两句说同一件事；现只留页顶一处） |
| `只看异常点`（表注尾四字） | 只看异常点表注 | 页顶提示 ＋ H1 `看波动异常点 …` |
| `谨紧张`（原文） | 卡③ 说明 | 换成 `请留意`（原文疑为错字；同一事实仍在） |

**没删的数字（刻意保留）**：卡② 说明里的 `警戒线 ±0.098 kg` 与值槽同数——它是值槽那个数的**认领**（删掉读者就不知道 `±0.098 kg` 是哪条线）；口径 §3.5 允许同一屏留两处。

## 四、机器读数

1. **出页**：`node .scratch/t154/text-review/render-family.mjs --family volatility --out .scratch/t154/text-review/out-volatility` → 末行 `OK-RENDER`，`TALLY {"通过":5}`；5 页全 `OK`（exit=0）：
   `05=78645 B`／`13=78649 B`／`20=116748 B`／`27=156021 B`／`34=78645 B`。
2. **改前／改后逐字对照**：正本 `dump-page-text.py`／`inventory.py` 的抽取逻辑逐字复用，只把源目录换成本族产物（wrapper：`.scratch/t154/text-review/b485/diff-prep.py`）→
   `b485/after-volatility.md`（改后版式槽位清单）＋ `b485/after-pages/*.txt`（改后逐页可见文本），与 `text-review/inventory/volatility.md`／`pages/*.txt`（改前基线）逐条对照。
   清单里 `空态句: A ／ A` 是抽槽脚本同一节点取两次的产物：HTML 里 `近期没有异常点` 实测**恰 1 处**（`([regex]::Matches(...)).Count`）。
3. **靶向用例**：`node tooling/run-locked.mjs --ticket 485 -- node --test packages/skill-calorie/test/weight-volatility-336.test.mjs`
   → 首跑 `RESULT: ticket=485 runId=496dcd65-a0cf-40ac-a523-7c9b13092946 waitedMs=0 exit=0`，`ℹ tests 7 ／ ℹ pass 7 ／ ℹ fail 0`；
   收紧「旧术语零命中」清单后再跑一次：`runId=50ec9973-1bdc-4fbf-a5b0-6e2a37de9272 waitedMs=30020 exit=0`，`ℹ tests 7 ／ ℹ pass 7 ／ ℹ fail 0`（末次＝交付态读数）。
4. **变异自证（两行机器读数）**：
   - 变异红：把页脚来源行改回半角冒号（`📊 数据来源:`）→ `runId=e2f7ef31-3599-42b4-a331-fecf6fe1384c exit=1`，`ℹ pass 5 ／ ℹ fail 2`，断言 `AssertionError: 整图面缺：📊 数据来源：`；
   - 还原绿：改回全角冒号 → `runId=5c4c3bf5-b25a-4cd4-9892-5ae5c62a6373 exit=0`，`ℹ pass 7 ／ ℹ fail 0`。
5. **编译**：`node tooling/run-locked.mjs --ticket 485 -- npx tsc -b packages/skill-calorie`
   首跑 `runId=3e30f146-c8ff-4450-a139-ccc2b53a0795 exit=1`，5 条错误全在**别席在途件**（`weight/log.ts`、`weight/logReceipt.ts`、`weight/receipt.ts`），本族两件零错误（emit 正常，故出页不受阻）；
   后续两跑 `runId=a8cfb320-5ff1-4273-8253-262a18434901 exit=0`、`runId=9b42398c-19b4-43cb-9281-79ea0695dbba exit=0`（别席已收口）。
6. **GATE-RUN**：`.scratch/locks/gate-runs.log` 内 `ticket=485` 的 START/RUN 记录 **20 条**（编译 3／用例 4／出页外的 git add·commit·push 各 1，含上述 runId；`git push` 那条 `waitedMs=90029` 是等锁等满 90 秒后拿到，未绕过锁）。
   提交：`44fc25c feat(485): 波动族 5 页文本审查——删冗余·换人话·页脚句式统一`（4 文件，＋267／−90），已随 `origin/master` 生效（`git branch -r --contains 44fc25c` → `origin/master`）。
7. **包内告警线门（只读跑，非本票交付面）**：`node packages/skill-calorie/scripts/check-warning-line.mjs` → `exit=1`、`RESULT: 58/60`，
   两条 RED 都是**别席在途件**的陈化（`src/render/trendMiscPortDocs.ts` 台账 592／实况 595；`src/analysis/multiTrendPage.ts` 台账 511／实况 516）；
   本族两件（`volatility.ts` 260、`volatilityDoc.ts` 317）**都在 350 线内、不在台账表里**，与本次结论无关。
8. **术语命中数**（页 20 全 HTML，含复制载荷；`b485` 探针）：
   `阈值=0`、`偏离基线=0`；新词 `平均线=8`、`注意线=6`、`警戒线=32`、`波动带=6`。
   载荷键名仍含 `基线kg`（3 处，均在复制菜单按钮的 `data-t` 属性里，**非正文**，见 §五 第 2 条）。
9. **载荷 ↔ 页面对账**（页 20 复制菜单 text 段）：`基线kg 70.27`／`基线σkg 0.049`／`黄线kg 0.073`／`红线kg 0.098`／`窗口天数 90`／`有记录天数 90`／`缺口天数 0`／`点数 90`／`σ趋势点数 88`／`近期异常数 5`／`最新偏离kg 0.13`／`基线口径 近 30 天平均体重`／`预警档位 警戒`
   ↔ 页面卡①`70.27`、卡②`±0.098／±0.073／0.049`、卡③`+0.13`、卡④`5`、页脚`90 条`、结论`88 个点`：逐个对得上。
10. **看图**（`node .scratch/t154/shoot.mjs …` → `shots-volatility/`，`read_image` 真看了 3 张：20／27／34 全页）：
    四张卡的副说明无省略号截断、值槽未被长句撑高、四卡等高；值槽 `70.27 kg` 的数字与单位之间有间隔（`renderKpiCard` 的 `value-row` 是 `flex` ＋ `gap:6px`，故值槽不必另塞空格符）。
    两处既有毛病（**改前就有**，已用改前产物 `b485/pre-shots/20-…png` 对比确认，非本票引入）：图①横向虚线右端标注与右端异常点重叠；图②横轴标签 `06-10/06-16` 粘连。
11. **跨票守门（只读跑，不改）**：`test/render-t41.test.mjs` → `exit=0`，`ℹ pass 10 ／ ℹ fail 0`（#41 登记册一条判据都没放宽；
    它断的 `/基线/`、`/预警/` 仍由复制载荷的中文键名满足）。

## 五、未做项与下一手缺什么

1. **跨票必红点（要编排者裁）**：`test/sport-homogeneity-109.test.mjs:230` 的 needle 表
   `['波动分析', '基线', '阈值', '预警', '近期异常', '偏离基线', '复制数据']` 里，`阈值` 与 `偏离基线` 已被本票换词消灭（页上 `阈值=0`、`偏离基线=0`），该断言**必红**。
   本轮 109 读数 `pass 6 ／ fail 6`（改前基线 `pass 8 ／ fail 4`）里看不到它，是因为同一测试函数在更早一行 `weight-compare 缺：节奏`（别席在途页）就失败了——两处都在，本席未改该文件（不在写集）。
   建议 needle 改：`['波动分析', '平均线', '波动带', '今日偏离', '近期异常', '每天离平均线多远', '复制数据']`。
2. **复制载荷的中文键名仍是旧术语**（`基线kg`／`基线σkg`／`黄线kg`／`红线kg`／`σ趋势点数`／`基线口径`／`基线模式`）：属机器面（粘贴给 AI／结构化存档），
   且 `test/render-t41.test.mjs:113` 的 `/基线/` 正靠它成立（#41 跨票登记册不许放宽）——**本席不动**，要改名请报编排者走 #41／载荷契约票。
3. **第 34 条页在 delivery 里是整图读法**（命令未带 `view`），「只看异常点」分支的三句改动只在源码与用例面被验证，未在出页里可见；若编排者要出页可见，需在出页参数里补 `view: 'anomalies-only'`。
4. **页顶提示两条（只有 1 条记录／记录少于 3 条）本轮 5 页都不触发**（7／90／180 天记录齐），改动只经源码审读＋类型检查，未在出页与用例里验过；要用例覆盖需另建夹具（单点库／两三天库）。
5. **归 `base-render` 的显示问题（不在本票写集，未动）**：图①横向虚线右端标注压住右端异常点；图②横轴 `labels:'select'` 密集时标签粘连；波动幅度趋势纵轴出现负数刻度（`-0.10 kg`／`-0.30 kg`，波动幅度本不该有负值）——三处都在图表摆放／量程，属 `packages/base-render`。
6. 本席未碰：眉标（待用户裁定）、复制区形态与深色 toast、第 48 条整页（归 #436）、`review.ts`（归 #482）、老技能目录与 `SKILL.md`／`commands.ts`／`routes.ts`。

## 六、对抗审查整改（执行席 · 按 `fix-brief-volatility.md` 逐条）

审查席判 FAIL（1 条 S1）后的整改落点。改前＝本轮起点（`after-volatility/`，含前一轮换词），
改后＝本席重出（`fix485/out`，`TALLY {"通过":5}`）。

### 1. 跨族裁定 F · 页脚只留人话来源（5 页）

| | 文本 |
|---|---|
| 改前 | `📊 数据来源：体重记录（calorie_data.db · weight_log） ｜ 窗口 2026-09-01 ~ 2026-09-07 ｜ 共 7 条` |
| 改后 | `📊 数据来源：体重记录 ｜ 窗口 2026-09-01 ~ 2026-09-07 ｜ 共 7 条` |

可见文本里 `calorie_data.db`／`weight_log` 命中 **0**（用例逐页断言）；机器面原样住在复制日志第 4 段
（`COPY_SOURCE = DB_FILENAME + ' ｜ 体重记录（本窗体重波动）'`，与页脚是两处字符串）与复制载荷。

### 2. D1（S1）· 「波动幅度」一词一屏三义 → 一词一义

改前第 27 页（180 天）同一屏：卡②说明 `波动幅度 0.049 kg`（σ）／图②题 `波动幅度趋势`（轴上 −0.30…0.50）／
结论 `波动幅度 0.81 kg`（整窗离散度）——三个互不相容的数共用一词。

落法（四处）：

1. `volatilitySummary` 里那个整窗标准差改用**自己的名字**：`整体离散度`（σ 是它去掉逐日涨跌后的那一份，
   两个数不再同名）；「波动幅度」这个词**全页只留给卡②说明**那一个数（＝值槽的 σ）。
2. 图②题 `波动幅度趋势` → `去掉涨跌后的波动`（同一张图，只是不再借用卡②的词）。
3. 卡②徽章 `按本窗数据` → `这两条线按本窗数据算`（说的是两条线的算法，不是那个数）。
4. 结论尾部的 `（波动幅度趋势 N 个点）` 删掉（那个词与卡②撞名，且点数读者在图②题上一眼看得到）。

改后第 27 页同屏：

```
波动带 / ±0.098 kg / 警戒线 ±0.098 kg · 注意线 ±0.073 kg · 波动幅度 0.049 kg / 这两条线按本窗数据算
…
去掉涨跌后的波动
…
体重波动较大：整体离散度 0.81 kg，近 7 天有 5 天超过波动带，建议关注饮食与饮水；2026-09-07 比平均线高 0.13 kg，超过警戒线。
```

同屏「波动幅度」剩 1 处（卡②说明）且后面恒跟卡②值槽那个数；缩手检查：拿 30 天里一天 75 kg 的尖峰库
（σ 与整窗离散度必然分家）出页，可见文本里 `波动幅度`＝1（卡②说明 1.848）、结论句里 `波动幅度`＝0。

### 3. D2–D6 逐条

| # | 页 | 改前 | 改后 |
|---|---|---|---|
| D2 | 05／13／34 结论 | `体重很稳：波动幅度 0.05 kg，没有异常点，最近 7 天平均每天变化 0.017 kg；2026-09-07 比平均线高 0.03 kg，在正常范围内（波动幅度趋势 5 个点）。` | `体重很稳：整体离散度 0.05 kg，近期没有超过注意线的天；2026-09-07 比平均线高 0.03 kg，在正常范围内。` |
| D3 | 20／27 结论 | `…近 7 天有 5 天超过波动带（注意线 0 天／警戒线 5 天）；2026-09-07 比平均线高 0.13 kg，超过警戒线（波动幅度趋势 88／178 个点）。` | `体重基本稳定：整体离散度 0.16 kg，近 7 天有 5 天超过波动带；2026-09-07 比平均线高 0.13 kg，超过警戒线。`（27 页同形，判决词 `体重波动较大`） |
| D4 | 20／27 卡③说明 | `今天比平均线高 0.13 kg，超过警戒线 ±0.098 kg，请留意` | 前一轮已改成 `今天比平均线高 0.13 kg，超过了波动带，请留意`（线值只在卡②出现，本轮复核 `±0.098` 非表槽位＝3：值槽／卡②说明／图①标注） |
| D5 | 05／13／34 卡④徽章 | `无异常点` | `没越线`（说明句 `近 7 天：超过注意线 0 天 · 超过警戒线 0 天` 保留） |
| D6 | 20／27 卡①徽章 | `记录齐全` | `没有漏记`（说明句 `180/180 天有记录` 保留） |

### 4. 新断言（把 D1 钉住）

`packages/skill-calorie/test/weight-volatility-336.test.mjs` 新增：

```js
test('#485 对抗审查 D1：一屏一义——「波动幅度」全页只许指卡②那个数（值槽同数同精度）', () => {
  const db = tmpDb();
  seedVol(db);
  const html = viewVolatility({ window: '30d', today: '2026-08-18' }, db).html;
  const vis = toVisible(html);
  assert.equal(vis.split('波动幅度').length - 1, 1, '可见文本里「波动幅度」只许 1 处（卡②说明）');
  const readings = vis.match(/波动幅度 (\d+\.\d+) kg/g) || [];
  assert.equal(readings.length, 1, '「波动幅度」后面必须紧跟一个数：' + JSON.stringify(readings));
  const sigma = metricsOf(weightVolatilityV2(db, '2026-07-20', '2026-08-18').data).baselineSigma;
  assert.equal(readings[0], '波动幅度 ' + sigma + ' kg', '「波动幅度」跟随的数必须恒等于卡②值槽的 σ=' + sigma);
  assert.equal((vis.match(/波动带 ±/g) || []).length, 1, '「波动带」＋线值只许卡②说明 1 处');
  assert.equal(vis.includes('这两条线按本窗数据算'), true, '卡②徽章说清两条线是按本窗数据算的');
  const conclusion = betweenTags(html, /体重很稳|体重基本稳定|体重波动较大/).join('\n');
  assert.ok(conclusion.includes('整体离散度'), '结论句那个标准差用自己的名字（不是「波动幅度」）');
  assert.equal(conclusion.split('波动幅度').length - 1, 0, '结论句不含「波动幅度」（那个词只归卡②）');
  for (const gone of ['最近 7 天平均每天变化', '超过警戒线 0 天', '注意线 0 天', '超过警戒线 ±', '平均线高 0.03 kg']) {
    assert.ok(!conclusion.includes(gone), '结论句复述了卡面读数：' + gone);
  }
  db.close();
});
```

配套：新增可见文本层探针 `toVisible`／`cleanText`／`betweenTags`／`quotedStrings`——先把 `script`／`style`
与**整条属性**（复制区把整份载荷塞进 `title`／`data-t`，属性值里带转义过的 `<`）换成一个空格，再剔标签，
免得载荷里的 `结论`／`基线kg` 被算成页上的字（第一版探针就是把载荷算进去才误报 2 处／9 处）；
`#336 5 条全有命令` 用例同步加页脚口径与库表名可见性；旧 needle `波动幅度趋势` 换成 `去掉涨跌后的波动`。

### 5. 机器读数

| 项 | 读数 |
|---|---|
| 编译（持锁） | `node tooling/run-locked.mjs --ticket 485 -- npx tsc -b packages/skill-calorie` → exit 0（runId `0c34f2b5`／`e037d59d`／`b173ff68`） |
| 用例绿（持锁） | `node --test packages/skill-calorie/test/weight-volatility-336.test.mjs` → `RESULT: ticket=485 runId=1c6fb284-977d-469d-9096-ba72ac48c99f waitedMs=0 exit=0`；`tests 8 ／ pass 8 ／ fail 0` |
| 变异第 1 行 | 图②题改回 `波动幅度趋势`（＝把 D1 改坏）→ `runId=a6091305-ad4e-41d8-a4b1-a2d850960c1e exit=1`；`pass 5 ／ fail 3`，其中 `AssertionError: 可见文本里「波动幅度」只许 1 处（卡②说明）`（另有两条旧 needle 同步红） |
| 变异第 2 行 | 改回 `去掉涨跌后的波动`（重编）→ `runId=7bc68e09-97cd-487c-a3bb-8322867aa610 waitedMs=0 exit=0`；`pass 8 ／ fail 0` |
| 出页 | `node .scratch/t154/text-review/render-family.mjs --family volatility --out …/fix485/out` → `OK 05 / OK 13 / OK 20 / OK 27 / OK 34`，`TALLY {"通过":5} TOTAL 5 HTML=5`，`OK-RENDER` |
| 本席差分（改前＝`after-volatility/`） | 5／5 页有变化，`DELTA-LINES 56`（每页 10 或 12 行；未改块一行未动） |
| 行数（LF） | `volatility.ts` 257（改前 260）／`volatilityDoc.ts` 328（改前 317）／用例 180——都在 350 线内 |
| 看图 | `fix485/shots/`，实看 05／20／27：四张卡与两张图、表、结论、页脚逐块对齐；`波动幅度` 只在卡②说明出现 |

### 6. 本轮未做项（与 §五 的旧账分开记）

1. **结论里那句判决词仍由整窗离散度分档**（`<0.3` 很稳／`<0.5` 基本稳定／否则较大），只是换了名字——
   与卡②的 σ 是两条口径（σ 去掉逐日涨跌、离散度不去）。要让两者同口径得另开票（改算式，不在本票写集）。
2. **第 34 条页在出页里仍是整图读法**（同 §五.3），「只看异常点」分支只经源码＋用例面验证。
3. 页顶两条「记录太少」提示、单点页的结论句（`只有 1 条记录：… 看不出波动`）仍只在源码层面改过，
   出页里不触发（5 页记录都齐）。
4. 归 `packages/base-render` 的三处显示问题（图①虚线标注压右端点／图②横轴标签粘连／纵轴负刻度）与
   第 13／34 条页面参数未生效，均**不在本票写集**，未动（与 §五.5 同）。
5. 本席只写 `volatility.ts`／`volatilityDoc.ts`／`weight-volatility-336.test.mjs`／本文件与
   `.scratch/t154/text-review/fix485/`；未碰眉标、复制区形态、复制载荷键名、别席在途件。

## 七、形状化与手机端（#504 · 2026-09-15）

负责人六条第 1／2／5 条与共同口径 `.scratch/t154/text-review/口径-UI.md`（同型先例＝
`packages/skill-calorie/src/render/reviewDocsCss.ts` 件头）。形状词汇全部用同目录 `weightUi.ts`
（`weightUiCss()`／`windowStrip()`／`factStrip()`／`pairStrip()`／`chip()`／`note()`／`bulletList()`／`verdict()`），
两族各自把 `weightUiCss()` 放进整页装配 `parts` 的**第一项**。

### ① 逐页「原来串什么 → 改成什么形状」

| 页 | 原来（`·`／`；` 串） | 改成 |
|---|---|---|
| {A} | （逐页表见下） | |

**复盘族 6 页（第 15／22／29／36／40／44 条）**

| 页 | 原来串什么 | 改成什么形状 |
|---|---|---|
| 15／22／29／36／40 | 页顶「记录太少」提示的详情：`曲线上只有这一个点，图上那条横线就是这个点的平均值；一条记录谈不上趋势` | `notice()` 只报「这段时间只记了 1 条」＋ `bulletList()` 两条逐条成行（`；` 串拆开） |
| 22／29／36／40 | 「期间变化」卡副说明 `开头 70.3 → 最后 70.4 kg`（`→` 串） | 撤到卡下 `periodStrip()`：一对量走 `pairStrip()`（两端各一枚加粗值、中缝箭头） |
| 22／29／36／40 | 「期间平均值」卡副说明 `最高 70.4 kg · 最低 70.3 kg`（一个 `·` 串两条事实） | 同一条 `periodStrip()` 里两枚「标签 ＋ 值」（`最高`／`最低`） |
| 44 | 里程碑每行 `2025-11-27 · 减重 5 kg · 当天 75 kg`（**一行三个 `·`**） | 三个槽各自成形：`left`＝档位`减重 5 kg`、`main`＝日期、`right`＝`当天 75 kg` |
| 全部 6 页 | 卡片 `detail` 槽里塞多件事实（那一槽由公共层 `esc`，形状落不进去） | 卡内只留**一句指引**，事实搬到卡下的形状块（同族 `history.ts:progressStrips` 同款做法） |

**波动族 5 页（第 5／13／20／27／34 条）**

| 页 | 原来串什么 | 改成什么形状 |
|---|---|---|
| 5／13／20／27／34 | **卡②副说明**：`警戒线 ±0.098 kg · 注意线 ±0.073 kg · 波动幅度 0.049 kg`（三件事一行 `·`，本族的样板处） | 卡下 `thresholdStrip()`：三枚「标签 ＋ 值」（`波动幅度`／`注意线`／`警戒线`）同排，窄屏塌一列；卡内只写「两条线各是多少，看这张卡下面那条」 |
| 5／13／20／27／34 | 卡④副说明 `近 7 天：超过注意线 0 天 · 超过警戒线 5 天` | 卡下另一条事实条里的两枚「标签 ＋ 值」（`超过注意线 近 7 天 0 天`／`超过警戒线 近 7 天 5 天`）；卡内只写「近 7 天越线的天数」 |
| 5／13／20／27／34 | 结论块 `体重很稳：整体离散度 0.05 kg，近期没有超过注意线的天；2026-09-07 比平均线高 0.03 kg，在正常范围内。`（`；` 串判决与读数） | `verdict(判语)`（一句话）＋ `note(今天那条读数)`（日期＋当天体重＋偏离＋越没越线）；`renderDisclosure` 的 `contentHtml` **不再套 `<p>`**（`<p>` 里塞不下块级件） |
| 34（只看异常点读法） | 页顶提示 `这一页只列超过波动带的点（共 N 个），不出两张曲线图；要看整图请说…` | 提示只报「列了点什么」＋ `bulletList()` 两条（「这一页不出两张曲线图」／「要看整图，请说…」） |

### ② 删掉的信息登记（若有）

**没有删任何事实**。逐条去处：

- `距第一条记录 N 天`（第 44 条每行）：**留在右列**（`elapsedDays` 在别处没有去处，逐行照给；曾试过撤到「序差」口径，判定为丢事实，已收回）。
- `最高`／`最低`／`开头`／`最后`（窗口页）：从「期间平均值」卡与「期间变化」卡副说明**撤到卡下 `periodStrip()`**，
  三个数一个不少（`开头 70.5 kg → 最后 70.4 kg`／`最高 70.5 kg`／`最低 69.9 kg`）。
- `波动幅度`／`注意线`／`警戒线`（波动页）：从卡②副说明**撤到卡下 `thresholdStrip()`**，
  且 #485 D1 钉的那句 `波动幅度 <σ> kg` 仍逐字在同一处（`weight-volatility-336.test.mjs:140` 那条判据照旧绿）。
- 页头副标题、页脚口径行、复制区：**一行未动**（眉标整族已删，本轮不出）。

### ③ 分隔符回扫读数（逐页）

工具：`python .scratch/t154/text-review/scan-separators.py --dir <出页目录>`（交付包口径，含 `<title>`）。

| 出页目录 | 回扫读数 |
|---|---|
| `.scratch/t154/text-review/ui482/out-review`（6 页） | **6 页共 6 处**，逐页 1 处，全部是 `<title>卡路里·体重</title>` |
| `.scratch/t154/text-review/ui482/out-volatility`（5 页） | **5 页共 5 处**，逐页 1 处，同样是文档标题那一处 |

**正文命中数＝0**：把 `<title>` 剔掉再数（脚本 `.scratch/t154/text-review/ui482/scan-body.py`，
日志 `scan-body-*-final.log`）——两族**11 页逐页 0 处**（`·` 与 `；` 各 0）。

页题 `卡路里·体重` 是**文档名**（`plateDocs.ts:29` 的 `DOC_TITLE`，全仓 58 页逐字同形），
按口径 §二 的允许项处理：**不动它**，两族交付页的逐页读数是 1 处（全在 `<title>`）。

**改动前基线**：复盘族 6 页 15 处（`·` 14 ＋ `；` 1）、波动族 5 页 25 处（`·` 20 ＋ `；` 5），
合计 40 处 → 改动后**正文 0 处**（日志 `scan-*-before.log`／`scan-*-final.log`）。

### ④ 手机端探针读数（`SW`／`VW`／越界元素）

工具：`node .scratch/t154/text-review/mobile-frame.mjs probe 390 2400 "<产物 html 绝对路径>"`（390px 真视口 iframe 壳）。

| 页 | 读数 |
|---|---|
| 波动 05 看体重稳不稳（增强版） | `SW=390 VW=390 || 无溢出` |
| 复盘 15 体重复盘（本周） | `SW=390 VW=390 || 无溢出` |
| 复盘 44 看里程碑回溯 | `SW=390 VW=390 || 无溢出` |
| 波动 27 看最近 180 天波动 | `SW=390 VW=390 || 无溢出` |
| 波动 34 看波动异常点 | `SW=390 VW=390 || 无溢出` |

截图并看图（`read_image` 真看）：桌面 1200 档 22／05 两页＋手机 390 档 15／44／05／27 四页。
看图当场改的两处：

1. **卡下事实条改成竖排会把标签与值拉到 960px 的两头**（桌面 1200 截图实测：中间空空荡荡、读不成对）
   ⇒ 两族的卡下条子都保持**横排**（窄屏由 820 段 ③ 自然塌一列）。
2. **里程碑行的日期进 `left` 那 44px 列，在 390px 下折成两行**（`2025-`＋`11-27`，手机截图抓到）
   ⇒ 槽位改成 `left`＝档位、`main`＝日期、`right`＝当天体重；把三条读数一起塞进 `main` 的那一版
   被 `nowrap` 截成省略号（`距第一条记录 415…`），也一并否掉。

### ⑤ 用例与变异读数

| 判据 | 读数 |
|---|---|
| 持锁编译 `npx tsc -b packages/skill-calorie` | 我的三件（`review.ts`／`volatility.ts`／`volatilityDoc.ts`）**零错**；全程只有别席在途件的红（`weightCompare2.ts`／`homeDocs.ts`／`logReceipt.ts`／`workout/write.ts`，非本票） |
| 持锁用例（本族两件） | runId `42adbdf3-e569-47ee-b540-49f879084845`：`tests 21／pass 21／fail 0`（绿） |
| 变异：把波动页卡②那行形状改回 `·` 串 | runId `f3b1b342-2207-4628-82c8-ac3b3e369c58`：`pass 19／fail 2`（**必红**）——红在 ①`可见文本里「波动幅度」只许 1 处（卡②说明）`②`正文仍有 `·`` |
| 还原 | runId `42adbdf3-…`（同上，绿） |
| 出页 | `TALLY {"通过":6}`（复盘）＋`TALLY {"通过":5}`（波动），两族全 `OK`、逐页 `exit=0` |
| 告警线门 | 本票两项已同步进台账（`review.ts` 669→724；`volatilityDoc.ts` 392 **新挂号**）；门仍红在别席在途件（`history.ts`／`compare.ts`／`trendMiscPortDocs.ts`／`workout/write.ts`／`diet/nutritionPortDocs.ts` 的陈化），**本票两件零红** |

新增/收紧的判据（两件各一条 #504 用例，把形状钉住）：

- `weight-review-335.test.mjs`：正文零 `·`／`；`（剔 `<title>` 后逐页数）、形状是真 DOM（不吃 `&lt;div class=&quot;wui`）、
  卡槽与行槽零 HTML、`@media (max-width:820px)` 与触摸面两条随页到场、`periodStrip` 逐枚形状到位、
  里程碑三槽逐槽到位、单点页不出首末对。
- `weight-volatility-336.test.mjs`：正文零 `·`／`；`、卡②指向卡下条子、条子三枚「标签 ＋ 值」与 σ／两线同数、
  结论 `verdict` ＋ `note` 两件到位、只看异常点那页同样带形状与 820 段、`toVisible` 之上新增 `bodyText()`（剔文档标题）。

### ⑥ 未做项与下一手缺什么

- **别席在途件**：编译与告警线门的残余红全在别席在途件（清单见 ⑤），本票不碰、也不代改。
- **本票在途项**：无。11 页逐页看过的两处形态问题（卡下条子竖排、里程碑行槽位）都已在本轮改掉并复看。
- **下一手**：交付包（`.scratch/t154/delivery/`）由编排者重出；本票只出页到 `.scratch/t154/text-review/ui482/`。


## 八、设计视角审查整改（#510，2026-09-16）

**本族整改完成**（逐条「改了／为什么没改」见下）。依据：整改单 `.scratch/t154/text-review/fix-brief-uireview.md`
（八节）＋审查席回执与四份读数 `.scratch/t154/text-review/rev-ui/`。本席出页与读数在
`.scratch/t154/text-review/rev-fix/`（`out-<族>`／`out-all`／`*.log`／`probe-fix-readings.py`／`readings-fix.txt`）。

### 八.1 第二节四类硬读数（复跑）

| 类 | 整改前（审查席实测） | 整改后（本席复跑） | 判据 |
|---|---|---|---|
| ① 硬编码色值 | 页内件面 6 处自造：`#e7f8ee/#1a7a3a`、`#fff4e5/#a05a00`、`#ffeceb/#c0392b` | 页内件面只剩 `#fff5e0`／`#a25b00`——**逐值等于冻结正本** `base-render/src/style.ts` 的 `statusBadge` warn；`ok`／`danger` 两档连类带值删（58 页里 0 页用过＝死 CSS，`chip()` 类型同步收到 `warn`／`plain`） | PASS |
| ② 字号阶梯 | 5 档 `{12, 12.5, 13, 14.5, 15}px` | **3 档**：`12px:456`／`13px:228`／`15px:171`（`.wui-bullets li` 12.5→12、手机档 `.wui-verdict` 14.5→15） | PASS |
| ③ 死 CSS | `wui-strip-note`／`wui-strip-v`／`wui-chip-ok`／`wui-chip-danger` 各 0 页 | 声明 22 个 `wui-*` 类、**0 页用到 0 个**：`-note` 2 页、`-v` 17 页真用上，`-ok`／`-danger` 删 | PASS |
| ④ 同屏事实 ≥3 | **51/58 页** | **7/57 页**（第 48 条归 #436 未纳入本批；残留 7 页全在写集外的 `logReceipt.ts`／`receipt.ts`） | 本族 0 页 |

复跑命令（口径与审查席四脚本同源，源目录换 `PROBE_DIR`）：

```
python .scratch/t154/text-review/rev-fix/probe-redundancy.py     # → .scratch/t154/text-review/rev-fix/redundancy-after.txt（等价于 rev-ui/probe-redundancy.py）
python .scratch/t154/text-review/rev-fix/probe-css.py            # → .scratch/t154/text-review/rev-fix/css-after.txt
python .scratch/t154/text-review/rev-fix/probe-fix-readings.py   # → .scratch/t154/text-review/rev-fix/readings-fix.txt（色值／字号／死 CSS／形状槽顿号四类一次出）
```

其余三面读数（审查席已判达标、本轮未推翻）：间距页内件面 12 档无孤值｜圆角 2 档＋零 box-shadow｜
手机 390 真视口 **57/57 页 `SW=390 VW=390`**（`.scratch/t154/text-review/rev-fix/mobile-probe.log`）。

### 八.2 本族（波动族 5 页）逐条

- **S2 条数「7 天」一屏三处（页 05/13 副标题＋两张卡副行；页 20/27/34 卡副行＋表题＋判语块）→ 已改**：
  ①「平均线」卡满窗时不再报「7/7 天有记录」（改「每天都有记录」，有缺口时照实报 N/M 天）；
  ② 异常表表题去掉「近 7 天」（`近 7 天异常点（…）`→`异常点（…）`，这一张表本来列的就是近 7 天）。⇒ 本族 0 页 ★。
- **S3 胶囊里装句子（页 05 徽章 `这两条线按本窗数据算`，11 字）→ 已改**：收到 4 字状态词「本窗算出」
  （胶囊／徽章位只装 2～4 字状态词；「按本窗数据算」这层意思由这一个词承担，不再另起一句）。
- **S3 卡副行写成指路牌（页 05 波动带卡 `两条线各是多少，看这张卡下面那条`）→ 已改**：
  整条副说明撤掉，该卡改成**无副行的纯指标卡**（值槽 ＋ 徽章 ＋ 卡下那条阈值条子，各说一件事）。
- **未改（为什么）**：结论句那一句 `volatilitySummary()` 的文本住在 `weight/volatility.ts`（**不在本票写集**），
  本票只动了本件的装配与卡面；审查席对结论句本身的判词（不许复述卡面数字）由 #504／#485 两轮已收口，本轮无新增项。

### 八.3 自证

- 持锁编译：`node tooling/run-locked.mjs --ticket 510 -- npx tsc -b packages/skill-calorie` → **exit=0**
  （`.scratch/t154/text-review/rev-fix/tsc-2.log`）。
- 出页六族全 `OK`：history 18／compare 17／review 6／volatility 5／plate 2／receipt 11（含 08／30 复用）
  ——`TALLY {"通过":n}` 且 `OK-RENDER`，日志 `.scratch/t154/text-review/rev-fix/render-*.log`。
- 用例（持锁）：五件体重族测试 **43/43 通过**（`tests 43 / pass 43 / fail 0`，`.scratch/t154/text-review/rev-fix/tests-final.log`；
  其中 `weight-history-333` 自打 `RESULT: 18/18`）。旧断言随新文案同步收紧（h1、徽章、里程碑副说明、
  单日窗副标题、期间事实条、卡②徽章与指路牌、今日盘形状件与卡网格）。
- **变异两行**：把 `history.ts` 的节奏／首末事实条改回 `·` 串 ⇒ 必红
  （`runId=0270d68d-6937-4f6d-8de4-cc1aaa0b579a`，`fail 1`：`看上周体重 可见面 · 命中数不对`）；
  改回 ⇒ 必绿（`runId=800175d3-1eec-43a3-8484-38a5ec5dca23`，`RESULT: 18/18`／`pass 3`）。
  日志 `.scratch/t154/text-review/rev-fix/mut-red.log`／`.scratch/t154/text-review/rev-fix/mut-green.log`。
- 手机 390 真视口：`node .scratch/t154/text-review/mobile-frame.mjs probe 390 2400 .scratch/t154/text-review/rev-fix/out-all` →
  **57/57 页 `SW=390 VW=390`**；截图两档 `.scratch/t154/text-review/rev-fix/shots-desktop/*.png`（1200）与
  `.scratch/t154/text-review/mobile/*.png`（390），审查席点名的 04／08／22／30／45／47／53 逐张看过。
- **告警线台账**：`packages/skill-calorie/AGENTS.md` 台账里本票六件的「当场实测」列已随实况（别席同步器在场）；
  本席复跑 `node packages/skill-calorie/scripts/check-warning-line.mjs` 的残余红只剩
  `src/render/trendDocs.ts 台账=994 实况=996`（**别席在途件**，不在本票写集，不代改）。
- 未动（照整改单第八节）：命令与路由／`shared/*`／`packages/base-render/*`（共享层一行未碰——间距 16 档、
  字号 18 档、数据表 1200 下两列相距 ~1000px、图表末点标签压均值虚线／`70.4kg` 少空格，均归 #424）／眉标／
  复制区形态与深色瞬时 toast／第 48 条整页（归 #436）／`.scratch/t154/delivery/`（只有编排者重出交付包）。
- **写集外残留（本票不改，供编排者派下一手）**：`weight/logReceipt.ts` 与 `weight/receipt.ts` 两件里的
  7 页 ★（01／09／16／17／23／24／31）与 3 处顿号——见 `t483-文本审查-receipt.md` 八.2 的逐条表。
