# t445 收尾证据（窗口 C：`e06fe48` 那一轮的对账补记）

本件只为独立复核点名的 S3-3 补齐：`e06fe48`（台账跟实况再同步一行）那一轮当时**无窗口、无受跟踪导出**，
本件给它一个**自成一窗**的对账（声明与窗口一对一，照 #445 已有的「一件一窗口」口径）。

- 窗口：`--since 2026-09-14T13:39:00Z --until 2026-09-14T13:40:30Z --ticket 445 --log docs/skills/skill-calorie/t445-gate-runs-c.log`
- 导出件：`docs/skills/skill-calorie/t445-gate-runs-c.log`（本窗口内 START 4 行 ＋ RUN 4 行，逐字自 live 日志抄录）

## 一、声明（exit=0 的条目，逐条 `runId` ＋ `cmd`）

GATE-RUN runId=t445-c6c cmd="pwsh -NoProfile -File .scratch/t445/win-c6c.ps1"
GATE-RUN runId=t445-c7 cmd="pwsh -NoProfile -File .scratch/t445/win-w7.ps1"
GATE-RUN runId=t445-test2 cmd="node --test packages/skill-calorie/test/t445-告警线门.test.mjs"

## 二、放宽留痕（GATE-RELAX）

GATE-RELAX flag=--allow-undeclared reason=红跑与被测试拦下的首跑技术上不可能被声明认领（严格口径只认 exit=0），故逐条点名。

- `t445-final2`（exit=1，2026-09-14T13:39:34.331Z）：红跑，**非门禁证据**——node packages/skill-calorie/scripts/check-warning-line.mjs

## 三、读数

（对账读数见本轮提交信息与票面关闭评论；声明侧要求 `matched` 全命中、`missing=0`。）
