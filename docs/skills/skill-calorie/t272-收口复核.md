# #272 收口复核（横向排行榜页 · 21 条）

裁决：**FAIL（需整改）** —— 一条 **S1-交付缺陷（契约违反）**。

## 一、机器证据

检出＝`D:\ilife-wt\wf155-c`（master `645aa37`）。参考提交 `b2d82ab`／`04f33a8`／`62a3103`／`e326983` **都在历史里**（`git merge-base --is-ancestor` 逐条 `exit 0`）。

- 编译：`run-locked.mjs --ticket 272 -- pnpm build` → `exit=2`，唯一红点 `base-render/test-d/contract-signatures.ts(390,20) TS2344`（该文件自本票提交起未改，来源 `a770132`，与本票无涉）；包作用域 `tsc -b packages/skill-calorie` ＋ `gen-cli.mjs --stamp` → `exit=0`。
- 逐条真跑（路由表从编译产物 `dist/diet/routes.js` 取，不抄字面量；每条跑前还原同一份库快照并清空产物目录）：**`RESULT-RUN22: exit0 22/22 有产物 22/22`** —— 作者核心读数独立复现。
- 空库：**`RESULT-EMPTYDB: PASS`** —— 单榜／全榜均 `exit=4`、0 产物、`ERR 4: 取数失败（缺失阻断）`。
- **空窗（库里有数据、窗口内零记录）：`exit=4`、0 产物、不出文档** —— 单榜 `ERR 4: … 无饮食记录（2026-11-01 ~ 2026-11-07）`，全榜 `ERR 4: … 无排行数据`。
- 新探针：**`RESULT-NEW: PASS 红=0 检查项=31`**；`RESULT-C: 表头逐类 5/5 对上；两两不同 10/10`；`RESULT-D` 权威写死的假样例 —— `高热量 第3格=330 卡 第5格=165 卡/餐 条宽=24/48/28`、`高碳水 第3格=40 克 第5格=330 卡`、`高蛋白 第3格=20 克 第5格=330 卡`；`RESULT-E: 全榜 exit=0 details=3 空榜点名=true`。
- 变异自证（源码级；持锁窗口内改坏→编译→跑断言→逐文件还原→重编重签）：
  - `MUTATION-甲: 源码sha=dc654bb1177e8940 断言批 tests=26 pass=22 fail=4 | 新探针=FAIL 红=7`（列序退回「写死一套」）
  - `RESTORE-甲: 源码sha=a0c8992da64eb5c8 与进场一致=true 断言批 tests=26 pass=26 fail=0 | 新探针=PASS 红=0`
  - `MUTATION-乙: 源码sha=7dca2459717bbcc3 断言批 tests=26 pass=26 fail=0 | 新探针=FAIL 红=6`（第三段退回「各段四舍五入」）
  - `RESTORE-乙: 源码sha=a0c8992da64eb5c8 与进场一致=true 断言批 tests=26 pass=26 fail=0 | 新探针=PASS 红=0`
- 门禁条目对账（我按导出件逐条比）：**`RESULT-GATE: PASS`** —— 声明 25 ＝ 导出 25、无重复、无人声明的 0，其中 `exit=0` 17 条；8 条 `exit=1` 都按基线／归因如实声明，未见拿失败条目充门禁证据。

## 二、逐条路径

- 新探针与环境补齐（随报告入仓，均在 `docs/skills/skill-calorie/`）：`t272-收口复核-复跑22条.mjs`、`t272-收口复核-新探针.mjs`、`t272-收口复核-变异.mjs`、`t272-收口复核-环境补齐.mjs`。
- 日志与库（不入仓）：`.scratch/t272/logs/{build3,build4,probe1,probe2,test-baseline,mutation}.log`、`.scratch/t272/probe1|probe2/`；对账脚本 `.scratch/t272/gate-recon.mjs`。
- 被审交付件：`packages/skill-calorie/src/diet/rankingDocs.ts`（322 行）、`src/diet/ranking.ts`（33 行）、`packages/skill-calorie/test/t272-排行榜页.test.mjs`、`.changeset/t272-排行榜页.md`。
- 被审证据件：`docs/skills/skill-calorie/t272-{21条真跑,判据与裁定,自证,gate-runs}.md`、`t272-复核-{报告,复跑21条,新探针-全景与空窗,新探针-表列与取值}`。
- 对照基准（只读，未执行其中任何脚本）：`D:\2Study\StudyNotes\SKILLS\卡路里\templates\food_ranking.html`（14349 B／368 行）。

## 三、新旧实样对照（③ 类那四条 ＋ 反面清单）

| 项 | 判读 | 读数 |
|---|---|---|
| 23 五类共用一个渲染函数、表头按类查表 | **对上了** | 与老实物 `:242-247` 的 `CATEGORIES.thead` 逐类同序（标签改用新仓用词）；主指标恒第 3 列 |
| 老实物行值与表头错位 | **确有，且已改正** | 老实物 `:301-321` 行序＝主指标／均热量／次数，与 `:242-247` 表头对不上（五类皆然）；新侧逐格对齐 |
| 24 前三金银铜＋手机左色条 | **对上了** | 老实物手机 `:162-164` 三色值与新侧 `RANK_CSS` 逐字相同；桌面渐变圆章（`:81-83`）改为字形，未搬，属已登记项 |
| 25 三段堆叠条＋文字百分比 | **对上了（两处演进）** | 老实物文字用「蛋／碳／脂」（`:317`）、各段各自四舍五入（`:297-299`，可加到 101%）；新侧写全称、第三段吃余数（恒 100，实测 24/48/28） |
| 26 无数据不出页签、列表空给占位 | **对上了** | 只播一条 💧水 ⇒ `details=3`／5，口径行点名「低热量榜、常吃榜」 |
| 老实物「食物名下总 Ng」小字 | **没对上（未列验收项）** | 老实物 `:306` 有，新表无此列 |
| 页内 tab／返回顶／死函数 `copyToAI` | **不适用** | 本仓一命令一页；作者已登记未移植 |

**收窄有没有变成放宽**：按老实物＋纸上手算写死期望（测试饭×2 ⇒ 330 卡／2 次／餐均 165／蛋白 20／碳水 40／脂肪 10 ⇒ 占比 24／48／28），三页逐格命中；变异乙证明这条口径一旦被放回各段四舍五入，我的探针立刻变红 —— 收窄未被放宽。

## 四、缺陷清单

1. **【S1·契约违反·本票范围】空窗那一支没出完整页。** 基准件 `t425-融合基准.md` 裁定 4 的 2026-09-15 澄清（提交 `f0decc8`，11:17:41，**晚于**本票证据提交 `04f33a8` 的 11:14:42 三分钟）明写：**窗口为空 ⇒ 出完整页 ＋ 空态句 ＋ 引导句**；只有**库为空**才 `exit 4`、不落盘。实测两态在代码里塌成一条（`dietEngine.ts:125` 对两句同样返 rejection），空窗 `exit=4`、不落盘，本票自带的 `emptyText` 在这条路上永远出不来。本票**未引入**该行为（本体住 `rankingPlate.ts`），但该支明确落在本票页面上且裁决后一直没落实。落点＝`rankingPlate.ts`（作者自述在本票声明路径内）／`rankingDocs.ts`，需与 #279 及 `cmd-read` 系列断言一并裁。
2. **【S3·证据措辞不实·本票范围】「三色与老实物逐色一致」不成立。** 老实物蛋白段取 `--accent:#0071e3`（`:12`），新侧取公共层色板首位 `#007aff`，蓝不同；碳水／脂肪两段同值。取公共层色板本身对，是这句结论不实。
3. **【S3·数字不准·本票范围】行数抄错。** 两件实测 **322／33 行**（证据件写 296／30，疑似抄了提交的插入行数）；两件仍在告警线 350 以内，「本票无该挂的行」这一结论不变。老实物「368 行／14349 B」与实测一致。
4. **【S3·探针盲区·本票范围】作者 26 条没盖住本票自称改正过的那条口径。** 变异乙下 26 条**全绿**（`fail=0`），只有我的新探针抓到（红=6）。
5. **【S3·范围外】全仓 `pnpm build` 今天红在 `base-render/test-d/contract-signatures.ts:390`**（来源 `a770132`），与本票无关。
6. **【S3·范围外】上一轮复核把探针命名成「空窗」，实际测的是空库**（种子库零记录），并以「空窗是缺失阻断」结案；它写于澄清之后，却没测澄清点明的那一支。
7. **【S3·范围外】`查高热量排行` 的 `category` 是后补的。** `b2d82ab` 时确无（作者读数与遗留出口 3 属实），后被 `ad90751`（#276）补上 ⇒ `t272-21条真跑.md` 第 22 行「页题＝全部排行」在当刻已过期（现出单榜页）。两条都不归本票。

五维：契约一致 18／证据真实可复现 20／新旧对照 15／工程红线 15／文档同步 8 ＝ **76**（低于 85）；且 S1 单独即可否决。

## 五、未做项与下一手缺什么

**未做**：未开视觉验收墙逐格审版式（本次是收口复核，墙＋逐格缺陷清单＋自检退出码三样齐不在本次范围）；未跑全量测试与 `pnpm test`（作者自证里那批红与他席在途件的关系未由我独立复核）；未核窗口内有无无人声明的裸跑（对账查漏不防伪）。

**环境缺口（不是本票的问题）**：本检出的 `node_modules` 只挂到主工作区、缺 26 条包级链接，`pnpm build` 起不来。我在本检出内用 `.scratch/t272/link-workspace-deps.mjs` 补齐（只指向本检出、未装任何包）。下一手若在此检出复跑，先跑这个脚本。

**下一手缺什么**：① 空窗那一支要出完整页＋空态句＋引导句，落点在取数层把「窗口内零记录」与「库里零记录」分开，同时守住「`exit 4` 只留给库为空」（澄清件明写不许改）；② 现在**没有任何断言**能区分这两态，须补一条钉住；③ 改完必须重跑本席三件探针＋作者 26 条；④ 需编排者裁：空窗整页与 #279、`cmd-read` 系列断言的期望是否一并改。
