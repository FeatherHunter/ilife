# #364 · 删除取快照（证据）

- 票面：**#364 卡路里场景 08 身体细节 · 删除取快照**（地图 #158／本图第 12 票；前置 #363 已实施完成）。
- 一句话判定：**「删体脂」／「删围度」两条写命令删前先把这条记录取出来（取数在置废之前），回执摘要与
  `items[0].detail` 带着**逐字段的删前值**（中文标签唯一来源、缺项 `—`）；票面三条判据（可见文本逐字等于
  删前查库值／被删主键读侧查不到／落盘页路径存在可读）全有机器读数，四处源码级变异改坏必红／还原必绿，
  外加归属探针证明唯一那条他席红不在本席改动面上。**
- 期望值来源（只认票面点名的三种）：**手写样例**（种子逐格读出）＋**脚本查库**（同库另开 `node:sqlite`
  句柄、**删前**读基线整行）＋**需求原文**（唤醒词 prompt 与老技能同词条 `SKILL.md:1155-1156`
  「软删除(先列候选 → 快照确认 → 回执)」）；基准 `docs/skills/skill-calorie/t395-融合基准.md` §四 裁定 2。
- 读数日期：2026-09-14；全部读数取自 `dist/` 真出口（先 `npx tsc -b packages/skill-calorie` 再跑）。

## 一、逐条交付路径

| 路径 | 状态 | 说明 |
|---|---|---|
| `packages/skill-calorie/src/fetch/body.ts` | 改（LF 426 → **455**） | 新增**删前快照取数口** `measurementSnapshot`／`compositionSnapshot`（按主键读出全部列的原始值，不加 `is_deprecated` 过滤：软删只置废、内容一字不动，故删后仍可回读）；两条 `delete*` 改为**在置废之前**读快照并随返回值带回（不存在即抛仍看 `changes`，原判据不动） |
| `packages/skill-calorie/src/body/remove.ts` | 改（LF 29 → **104**） | 两条写命令的回执带快照：摘要一句（日期 ＋ 体脂率 ＋ 来源；围度给已填各项）＋ `items[0].detail` 逐字段删前值（「中文标签 值」、项间「、」、缺项 `—`）＋ `items[0].date`；**写库行为一字未动**（仍置废、不物理删） |
| `packages/skill-calorie/test/t364-删除取快照.test.mjs` | 新（LF 385） | 7 条判据：体脂逐格／围度 13 项逐位／13 项全填／裁定 2 可见那一轨／裁定 2 原始空值那一轨／软删语义＋不存在主键 exit 4／唤醒词接通面 |
| `.scratch/t364/mutate.mjs` | 新（草稿） | 变异电池（M1 去快照段／M2 删前值改删后值／M3 标签取错／M4 体脂率取错列 ＋ A 归属探针） |
| `.scratch/t364/logs/*.log` | 新（草稿） | §2.2 长命令落盘日志（编译／测试／回归／门／变异逐份） |
| `docs/skills/skill-calorie/t364-快照证据.md` | 新 | 本件 |
| `docs/skills/skill-calorie/t364-门禁对账.md` | 新 | §2.3 第 4 条的对账导出件（`check-gate-audit --export` 的逐字产物） |
| `.changeset/t364-body-delete-snapshot.md` | 新 | changeset（`'skill-calorie': patch`） |
| `body/bodyDocs.ts`／`bodyPlate.ts`／`view.ts`／`log.ts`／`body/routes.ts`／`src/triggers/**`／`packages/skill-calorie/AGENTS.md`／`scripts/check-warning-line.mjs` | **未动** | 票面「禁碰」清单。`bodyPlate.ts` 只被 `remove.ts` **只读 import**（`CALIPER_SITE_LABELS`，与 `log.ts` 同款用法）；`AGENTS.md` 当刻是 M，但那是**他席 `--sync`** 的未提交 diff（见 §三 第 1 条），本席不碰该件、不带走它 |

**为什么是这两条命令名**：`src/body/routes.ts` 里「删体脂」→ `calorie.body.composition-remove`、
「删围度」→ `calorie.body.measure-remove`（判据⑦把唤醒词与 `src/body/commands.ts` 注册表一起钉住；
本票**只读**这两件，一行未改）。

## 二、票面四条可跑判据 ＋ 负向对照的机器读数

| 判据 | 跑法 | 读数（机器行） |
|---|---|---|
| ① exit 0（两条命令真出口） | `node --test …/t364-删除取快照.test.mjs` | `T364-C exit=0 id=1 逐格=11 摘要="日期 2026-09-05 · 体脂率 18.5% · 来源 健身房 InBody · 备注 晨起" 库内可查=0`；`T364-M exit=0 id=1 缺项=11/13 摘要="日期 2026-09-06 · 腰围 86cm、臀围 95cm"` |
| ② 可见文本里的日期／体脂率／来源（围度＝13 项里已填的那些）**逐字等于删前查库值** | 同上（落盘页 `<li>` 与回执 `items[0].detail` 两处各自解析后逐格比；基线由**跑命令之前**另开句柄读整行） | 体脂逐格 11 格（日期／来源／体脂率／7 点站名／备注）；围度逐格 15 格（日期／13 部位／备注）。`T364-M13 全填=13 缺项=0 摘要项数=13`（第二位小数如 `80.25`／`100.5` 也逐字）；摘要另按 `已删除体脂记录 #id（日期 … · 体脂率 …% · 来源 …）` 解析出三样逐字（区间段比对不做全文 `includes`） |
| ③ 被删主键在库里**不可查**（本仓软删口径：读侧查不到 ‖ 行仍在） | 同上 `assertSoftDeleted` | 读侧口径 `WHERE id=? AND COALESCE(is_deprecated,0)=0` → **0 条**（`库内可查=0`）；同时 `SELECT … WHERE id=?` → **行仍在**且 `is_deprecated=1`（`T364-SOFT 两表行数=2`：只有置废、没有物理删）。两句分开断言，缺一不算 |
| ④ **快照文件路径存在可读** | 同上 `assertSnapshotFile`（口径见 §四） | `T364-FILE calorie.body.composition-remove path=…\calorie_html\删体脂_回执_20260915_011951.html bytes=604`；`T364-FILE calorie.body.measure-remove path=…\删围度_回执_20260915_011951.html bytes=623`（`data.output` 指向的文件存在、非空、可读，且它的可见文本里就是这份快照） |
| ⑤ 负向对照：**把快照字段去掉 → 必红** | `node .scratch/t364/mutate.mjs`（持锁窗口内直调） | 见 §五 M1 两行 |
| ⑥ 变异自证：**把删前值取成删后值 → 必红** | 同上 | 见 §五 M2 两行（另加 M3 标签取错／M4 取错列两处） |
| 不许动：软删语义（仍置废不物理删） | 同上 | 两表行数不变（3 条／2 条样例全在），`is_deprecated=1`；不存在的主键仍 `exit 4`（`T364-SOFT 不存在id exit=4`） |

裁定 2 两条**分开**断言（不用全文 `includes` 代理口径）：

- `T364-DASH-VISIBLE 体脂缺项=5/7 围度缺项=11/13`——可见文本缺项**按格**是 `—`（并断言缺值格不得留空串）；
- `T364-RAW-NULL 取数层=null 库内列=null 备注="晨起" 删后回读腰围=86`——原始空值留在取数层与库内
  （`measurementSnapshot`／`compositionSnapshot` 给 `null`；库内任一围度列不得出现 `—`；删后回读同值）。

## 三、三处现成约定照做

1. **告警线活快照门**（约定①）：进场与收尾各一次真跑，`RESULT: 60/60` ＋ `PASS: 告警线台账齐全且与实况一致`
   （`25eb5c90`，exit 0）。本席 `src/fetch/body.ts` 那行台账「当场实测」已是 **455**（当刻实况），
   是**他席 `--sync`** 落在他未提交的 `packages/skill-calorie/AGENTS.md` 里的**整表快照**（该件当刻是 M、
   含他席多行数字），故本席**不碰 AGENTS.md、不带走他席 diff**；本席进场时该门亦为绿，未跑过 `--sync`。
2. **13 部位名单一来源**（约定②）：围度快照标签取 `fetch/body.ts` 的 `MEASUREMENT_ZH`（用词「肩围」），
   7 点站名取 `body/bodyPlate.ts` 的 `CALIPER_SITE_LABELS`，来源中文名取 `kcal.ts` 的 `SOURCE_LABELS`；
   本件不写第二份名表。测试件另写**手写冻结名表**与三者逐字对账（`assert.equal(MEASUREMENT_ZH[f], PART_13[i])`）。
   用词：13 名逐位 = 胸围／腰围／腹围／臀围／左大腿／右大腿／左小腿／右小腿／左上臂／右上臂／左前臂／右前臂／**肩围**。
3. **复制 payload 保留原始空值**（约定③，裁定 2 的另一轨）：本票的回执今天是**裸回执片段**
   （`shared/writeParts.ts` 的 `receiptHtml`，没有复制菜单那一层），「复制 payload」轨的落点分两处——
   ① **本票新出的取数口**（`*Snapshot` 给 `null`，正是 #365 复制区要吃的那份数据）；
   ② 库内列。两处都按 §二 逐格/逐列断言，与「可见文本写 `—`」那一轨**各自独立**。

## 四、「快照文件路径存在可读」的判据口径（写清，免得复核走散）

票面这句在老正本里对应 `render_body_delete_receipt.py:184-188`：删除回执 HTML 落盘后
`print(f'⚠️ ACTION=SEND_TO_USER | HTML={out_path.absolute()}')`——**那份回执 HTML 本身就是「删除前快照」的载体**
（老技能同词条口径「软删除(先列候选 → 快照确认 → 回执)」，`crud_receipt.html` 的 delete 模式把删前旧值铺出来）。
新架构里同一个东西＝**`envelope.data.output` 指向的落盘页**（`cli/delivery.ts` 三态交付的文件态）。
故本票的判据④＝：该路径**存在且可读**（`statSync` 是文件、非空、`readFileSync` 读得出），
且它的**可见文本里就是这份快照**（§二 判据② 的落盘页那一半）。全仓没有任何写命令往库里/盘上另存 JSON 快照文件，
本票不发明这种新形状（票面「别自己发明新形状」）。
本票落盘页仍是**裸回执片段**（604／623 字节，无 doctype／无共享样式／无复制区）——换成结果型整页是 **#365** 的正题
（#365 票面「最大缺口」一条逐字点名了这个片段落点）。

## 五、变异两行（源码级；持锁窗口内直调编译器与测试运行器；还原＝逐字节写回 ＋ 重编）

基线：`src/body/remove.ts` sha256 前 12 位 `8293C0CA3E17`（LF 104）／`src/fetch/body.ts` `B582C7217734`（LF 455）。
每条都验了「编译确实发生」（dist 里出现／消失变异印记，见下 `dist印记=` 列），避开「不重编则读数全假」。

| 变异 | 改什么 | 改坏读数 | 还原读数 |
|---|---|---|---|
| M1（票面⑤） | `remove.ts`：删体脂回执**去掉快照段**（回旧一行回执） | `T364-M1 变异=去掉回执里的快照段（回旧一行回执） 源码sha=5A085970D4EF 重编 exit=0 dist印记=有 判据红 exit=1 pass=5 fail=2 首红=#364 判据①②③④体脂…` | `T364-M1-还原 源码sha=8293C0CA3E17 与基线同=YES 重编 exit=0 dist印记=无 判据绿 exit=0 pass=7 fail=0` |
| M2（票面⑥） | `fetch/body.ts`：快照读**移到置废之后**且按活行口径（＝取删后值） | `T364-M2 … 源码sha=D86E66878143 重编 exit=0 dist印记=有 判据红 exit=1 pass=4 fail=3 首红=#364 判据①②③④体脂…` | `T364-M2-还原 源码sha=B582C7217734 与基线同=YES 重编 exit=0 dist印记=无 判据绿 exit=0 pass=7 fail=0` |
| M3（另加） | `remove.ts`：围度标签取错（中文名换成库列名） | `T364-M3 … 源码sha=B8A2023C84DE 重编 exit=0 dist印记=有 判据红 exit=1 pass=5 fail=2 首红=#364 判据①②③④围度…` | `T364-M3-还原 … 与基线同=YES 重编 exit=0 dist印记=无 判据绿 exit=0 pass=7 fail=0` |
| M4（另加） | `remove.ts`：体脂率取错列（改读胸皮褶）→ **纯值级红**（不动退出码） | `T364-M4 … 源码sha=CF4BFD0A56A4 重编 exit=0 dist印记=有 判据红 exit=1 pass=6 fail=1 首红=#364 判据①②③④体脂…` | `T364-M4-还原 … 与基线同=YES 重编 exit=0 dist印记=无 判据绿 exit=0 pass=7 fail=0` |

- **M2 的一句实话**：软删**不改字段值**（只动 `is_deprecated`/`updated_at`），所以「删前读 vs 删后读」单靠换顺序
  观不出来——必须连**活行口径**一起改（删后按 `COALESCE(is_deprecated,0)=0` 读＝读不到）才构成「取删后值」。
  这正是本票取数口**故意不加 `is_deprecated` 过滤**、并在 `delete*` 里把读排在置废**之前**的理由，两处各由 M2 的一半钉住。
- **归属探针 A**（不采信「大概是别人的」）：把本席两处改动**逐字节写回 HEAD 版**（`git diff --quiet` 复核＝YES）后重编，
  `cmd-write-40` 那条红**仍在**：`T364-A-撤改 源码=HEAD 版 git与HEAD同=YES 重编 exit=0 cmd-write-40 该条 exit=1 pass=0 fail=1 首红=体重记/改/删/批量 + 缺身高仍记（C5 #43）`；
  随后逐文件还原：`T364-A-还原 remove.ts sha=8293C0CA3E17 fetch/body.ts sha=B582C7217734 重编 exit=0 与基线同=YES`。
- 收尾自证：`T364-RESTORED remove.ts sha=8293C0CA3E17 fetch/body.ts sha=B582C7217734 与基线同=YES`，
  `RESULT: M1／M2／M3／M4 改坏必红＋还原必绿 八行读数齐；归属探针 A 撤改后他席红仍在`。

## 六、门禁 GATE-RUN 对账（逐次运行都在 `gate-runs.log` 有对应条目）

| # | 命令（简写；逐字见 `t364-门禁对账.md`） | runId | exit | 读数 |
|---|---|---|---|---|
| 1 | `npx tsc` 等价：`node node_modules/typescript/bin/tsc -b packages/skill-calorie` | `9ad78197-9de0-4983-b98c-bb96c5ed7664` | 0 | 编译（等锁 195 s；`error TS` 命中 0） |
| 2 | `node --test …/t364-删除取快照.test.mjs`（首跑） | `41c08e87-dc07-42d4-bd89-18ac64b3cf2c` | **1** | 过程读数（**测试件自身**口径写错：基线读取少了 `date` 列）→ 已修，非交付物缺陷 |
| 3 | 同一条测试（修后复跑） | `f77e3aed-6c63-46ad-a604-a9bbc6ac7559` | 0 | `tests 7 / pass 7 / fail 0` |
| 4 | 回归面 22 件（body 域 ＋ 写键／取数／注册表／命名） | `a62b4127-f3db-440c-a82a-d44fbfdc8a9f` | **1** | `tests 233 / pass 232 / fail 1`（唯一红逐条归属见 §七） |
| 5 | `node packages/skill-calorie/scripts/check-warning-line.mjs` | `25eb5c90-6273-4903-a821-0eb2c15e31e5` | 0 | `RESULT: 60/60`＋`PASS: 告警线台账齐全且与实况一致` |
| 6 | `node .scratch/t364/mutate.mjs`（首跑） | `226dffdc-c1a5-491f-843a-f013ba5c8676` | **1** | 过程读数：变异脚本自身键名写错（`base[file]` 取到 undefined，TypeError），**未落到源码**即退出；修后复跑 |
| 7 | 同一条变异电池（改后） | `86f38a1d-b61c-46cf-ab62-334c7fab595d` | 0 | §五 八行读数 ＋ A 探针两行齐 |

**逐条声明**（对账只认运行标识相同的条目；红跑列出但**不作门禁通过证据**）：

GATE-RUN runId=9ad78197-9de0-4983-b98c-bb96c5ed7664 cmd=node node_modules/typescript/bin/tsc -b packages/skill-calorie
GATE-RUN runId=41c08e87-dc07-42d4-bd89-18ac64b3cf2c cmd=node --test packages/skill-calorie/test/t364-删除取快照.test.mjs
GATE-RUN runId=f77e3aed-6c63-46ad-a604-a9bbc6ac7559 cmd=node --test packages/skill-calorie/test/t364-删除取快照.test.mjs
GATE-RUN runId=a62b4127-f3db-440c-a82a-d44fbfdc8a9f cmd=node --test packages/skill-calorie/test/calorie-c43.test.mjs packages/skill-calorie/test/cli-smoke-t41.test.mjs packages/skill-calorie/test/cmd-registry-294.test.mjs packages/skill-calorie/test/cmd-write-40.test.mjs packages/skill-calorie/test/cmd-write-40-persist.test.mjs packages/skill-calorie/test/deprecated-126.test.mjs packages/skill-calorie/test/fetch.test.mjs packages/skill-calorie/test/fetch-t4.test.mjs packages/skill-calorie/test/fetch-t6.test.mjs packages/skill-calorie/test/m5-receipt-97.test.mjs packages/skill-calorie/test/output-naming-119.test.mjs packages/skill-calorie/test/render-t41.test.mjs packages/skill-calorie/test/t357-skinfold.test.mjs packages/skill-calorie/test/t358-missing-ask.test.mjs packages/skill-calorie/test/t359-seven-point.test.mjs packages/skill-calorie/test/t360-measure-trend.test.mjs packages/skill-calorie/test/t361-measure-full-table.test.mjs packages/skill-calorie/test/t362-窗口不截断.test.mjs packages/skill-calorie/test/t363-补记查冲突.test.mjs packages/skill-calorie/test/t364-删除取快照.test.mjs packages/skill-calorie/test/t398-source-group.test.mjs packages/skill-calorie/test/t440-部位名单一来源.test.mjs
GATE-RUN runId=25eb5c90-6273-4903-a821-0eb2c15e31e5 cmd=node packages/skill-calorie/scripts/check-warning-line.mjs
GATE-RUN runId=226dffdc-c1a5-491f-843a-f013ba5c8676 cmd=node .scratch/t364/mutate.mjs
GATE-RUN runId=86f38a1d-b61c-46cf-ab62-334c7fab595d cmd=node .scratch/t364/mutate.mjs

**待补（本件首次提交之后逐条补记）**：提交运行、收尾三项运行、`check-gate-audit --export` 运行 —— 见本件末段
「## 十、补记（首次提交之后）」；对账窗口 `--since 2026-09-14T17:09:00Z`。

GATE-RELAX flag=--allow-nonzero reason=票面明确要求**负向对照改坏必红**与**变异自证改坏必红**，故对账窗口内必有 exit≠0 的条目；这 **3 条逐条点名**（`41c08e87` 本票测试首跑·测试件自身基线读取少 date 列已修／`a62b4127` 回归面 22 件·唯一红＝`cmd-write-40` 体重措辞他席红／`226dffdc` 变异电池首跑·脚本自身键名写错未落到源码），它们只作「红在何处」的**机读取证**，**不作门禁通过证据**；本票的门禁通过证据只取 exit=0 的那几条（§六 表 1／3／5／7 ＋ 补记里的提交／收尾／对账导出）。

## 七、他席红逐条归属（不假绿）

- **`cmd-write-40.test.mjs` →「体重记/改/删/批量 + 缺身高仍记（C5 #43）」**：断言 `/70.2→70 kg/`，
  实收 `已更新体重 #2：70.2 kg → 70 kg（BMI 22.9）`。
  **归属证据两条**：① 该措辞**已在 HEAD 里**（`git show HEAD:packages/skill-calorie/src/weight/edit.ts`
  就是这一句，来源提交 `2e71033 fix(340): 改体重记录回执的统一改前→改后写法（补单位与空格）`），
  且 `packages/skill-calorie/src/weight/edit.ts` 当刻**无未提交改动**（`git status --short -- packages/skill-calorie/src/weight/` 空）；② 归属探针 A：把本席两处改动逐字节撤到 HEAD 版后
  重编再跑，这条**仍红同一条**（`T364-A-撤改`）。本席写集不含体重域与该测试件，故**不假绿、也不代修**。
- 回归面 22 件里其余 232 条全绿（含 body 域 9 件、写键／取数／注册表／命名 12 件、本票 1 件）。

## 八、未做项与下一手缺什么

1. **整页回执不在本票**（归 #365）：本票交的是**数据面**，落盘页仍是裸片段。**#365 要接的快照数据面接口形状**：
   - ① `fetch/body.ts` 导出 **`compositionSnapshot(db, id)`**／**`measurementSnapshot(db, id)`** →
     `Record<string, unknown> | null`，给的是**逐字段的列原始值**，列序＝
     `['id','date','source','body_fat_pct', ...CALIPER_FIELDS, 'note']`（体成分）／`['id','date', ...MEASUREMENT_FIELDS, 'note']`（围度）；
     **不加 `is_deprecated` 过滤**（软删后按 id 仍能回读），缺项是 `null`／空备注是 `''`（不是 `—`）。
     #365 的端口 `bodyReceiptDoc(key, params, receipt, db)` 拿 `params.id` 调它即可拿到删前逐字段值——
     与老正本 `crud_receipt.html:328-350` 的「按 `old_record` 键铺 diff 行」一一对应（跳过 `id`/`created_at`/`updated_at`／空值不摆）。
   - ② 回执 `items[0]` 已带 `{ id, date, status:'已删除（软，不可恢复）', reason:'', detail:'<逐字段删前值>' }`；
     `detail` 是 **`标签 值` 以「、」分隔**的字符串（标签内无空格、值内可有），#365 直接
     `detail.split('、')` ＋ 首个空格切分即可得到有序 `[中文标签, 值]`（与 `exercise/receipt.ts` 的
     `renderChangeRows({ rows:[{label, before, arrow:false}] })` 同形，箭头可见性占位＝`arrow:false`）。
   - ③ 标签单一来源：#365 拼 diff 行时标签取 `MEASUREMENT_ZH`（13 部位）／`CALIPER_SITE_LABELS`（7 点皮褶，
     次序与 `CALIPER_FIELDS` 逐位对齐）／`SOURCE_LABELS`（来源）＋「日期」「备注」两个通用标签。
   - ④ 复制区三格式**不在本票**：本票把裁定 2 的「原始空值」轨落在 ① 的取数口与库内（#365 复制区就吃这份数据）。
2. **告警线台账**：#364 让 `src/fetch/body.ts` 426→455（见 §九）；该行台账「当场实测」由**他席 `--sync`** 已更新为 455，
   本门当刻绿。本席**未**运行 `--sync`、**未**改 `AGENTS.md`（免得把他席在途 diff 带进本票提交）。
3. **未做**：#365／#366 那一层（整页版式、写前确认页、同日多条时的处置）；装机后用户肉眼验收（归 #369）。
4. **真库零写入**：本票一切数据走 mkdtemp tmp 库（`SKILLS_DB_PATH` 指过去）；真库
   `D:\2Study\StudyNotes\.db\calorie_data.db` 跑前跑后同为 sha256
   `57CE9D87F5F232822332D7057C2AE8411D99BA7FDECEA289F2028E19FC3FFC15`（3072000 字节、mtime 2026-09-14 13:53:37 未变——
   与 #363 证据件记的那一份**同值**）。

## 九、必报五步·第四步（告警线）

`src/fetch/body.ts` **已超线，需要根据规则进行重构。** 超因：这一件今天同时住着「写侧校验（`validateCompositionInput`／
`validateMeasurementInput`）＋围度取数（列表／趋势／同部位最新／对比）＋体成分取数（列表／两条趋势／按来源分组／
区间均值）＋本票新加的**删前快照取数口**」，四组活挤在一件（挂号原文见 `packages/skill-calorie/AGENTS.md`，
挂号值 369 永不回改，本票后当场实测 455）。本次**先不拆**：拆分不在 #364 写集（本票只许动 `src/body/remove.ts`
与 `src/fetch/body.ts` 两件），拆法沿用既有挂号行的方案＝按「写侧校验／围度取数／体成分取数」切姊妹件，
本票新增的两个快照取数口随各自表归到对应姊妹件；待结构票认领。

## 十、补记（首次提交之后逐条补记；边跑边补，不集中补一轮）

### 补记 1 · 提交 ＋ 收尾三项（同一个持锁窗口＝落点窗口，窗口内直调不再抢锁）

| # | 运行（逐字命令见 `t364-门禁对账.md`） | runId | exit | 读数 |
|---|---|---|---|---|
| 8 | `pwsh -NoProfile -File .scratch/t364/w1-commit.ps1`（① 限路径 `git add` 5 件 → `git diff --cached --name-only` 复核 → `git commit -- <5 件>`；② 收尾三项） | `2e29e1c0-6087-40ea-9e5c-fcaf8807d751` | 0 | ① 暂存区**只含本席 5 件**（`.changeset/t364-body-delete-snapshot.md`／`docs/skills/skill-calorie/t364-快照证据.md`／`packages/skill-calorie/src/body/remove.ts`／`packages/skill-calorie/src/fetch/body.ts`／`packages/skill-calorie/test/t364-删除取快照.test.mjs`）⇒ `beeeb69643320c41b16d894a87529a2f33d619fb`（5 files changed, 674 insertions(+), 15 deletions(-)，**他席已暂存的件一件未带走**）；② 收尾三项：`T364-FINAL-GATE exit=0`（`RESULT: 60/60`＋`PASS`）／`T364-FINAL-TEST exit=0`（本票判据复跑：8 行 `T364-` 标记全在）／`T364-FINAL-3` 声明路径 `git status` **空** |

GATE-RUN runId=2e29e1c0-6087-40ea-9e5c-fcaf8807d751 cmd=pwsh -NoProfile -File .scratch/t364/w1-commit.ps1

### 补记 2 · 格式门 ＋ 门禁对账＋导出（同一个持锁窗口）

| # | 运行（逐字命令见 `t364-门禁对账.md` 与本行） | runId | exit | 读数 |
|---|---|---|---|---|
| 9 | `pwsh -NoProfile -File .scratch/t364/w2-audit.ps1`（① `node tooling/make-evidence.mjs --check docs/skills/skill-calorie/t364-快照证据.md`；② `node tooling/check-gate-audit.mjs --evidence … --ticket 364 --since 2026-09-14T17:09:00Z --until 2026-09-14T17:31:59Z --allow-nonzero --export docs/skills/skill-calorie/t364-门禁对账.md`） | `8fa4bf87-9d6e-4aea-8a49-824e2fef6521` | 0 | ① 格式门**首跑红**：`FAIL: 引用不可落地（第 23 行）：docs/skills/skill-calorie/t364-门禁对账.md`（该导出件**在同一次运行的后半步**才生成）＋`FAIL: 引用不可落地（第 130 行）：packages/skill-calorie/src/weight/**`（通配写法不可落地）⇒ `make-evidence: FAIL（格式 2 项）`；两处已修（第 130 行改点具体件 `packages/skill-calorie/src/weight/edit.ts`；第 23 行那份导出件在本窗口已生成）——**格式门复跑读数见末次窗口**。② 对账＋导出：`RESULT: matched=8/8 auditEntries=4519 scoped=8 undeclared=0`＋`gate-audit: PASS`＋`EXPORT: docs/skills/skill-calorie/t364-门禁对账.md（窗口内 RUN 条目 8 条，受 git 跟踪）` |

GATE-RUN runId=8fa4bf87-9d6e-4aea-8a49-824e2fef6521 cmd=pwsh -NoProfile -File .scratch/t364/w2-audit.ps1

**末次窗口自指（照 #363 的既有做法写清）**：本件此后只剩**一个**末次窗口（格式门复跑 ＋ 本件与导出件的提交 ＋ 声明路径 `git status` 复核），
它的 `runId`／`sha` 与三项 exit **记在回执第二段**——本件无法在自己的末次提交里引用自己（自指差 1，与本仓 #363 证据件同款口径）。
对账窗口 `--until 2026-09-14T17:31:59Z` 之外追加的条目（`8fa4bf87` 之后的末次窗口运行）逐条见 `.scratch/locks/gate-runs.log` 里 `ticket=364` 的条目，一切以该日志为准。


