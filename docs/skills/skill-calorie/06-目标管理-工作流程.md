# 06-目标管理-工作流程（28 条真源）

> 票 #292 收口件：目标管理 25 场景 ＋ 3 条自造词的「唤醒词 → 该跑的命令 → 工作流程」**唯一真源**。
> SKILL.md 只留常驻规则 ＋ 指向本件的指针，不存第二份表（同一含义只留一处）。
> 本票只做文档与生成链，不写页面；不改产品代码。

## 口径

- 28 ＝冻结词表 `src/triggers/scene-06-goal.ts` 25 条（`order` 0–24）＋ 查找层别名 3 条（看目标推荐／看目标配置／看目标状态，上游唯一是 `src/triggers/help-lookup.ts` 的 `WAKE_TABLE`，SoT 零改动，见 #291 乙）。
- 「该跑的命令」＝ 路由层可执行 `cli`（`src/goal/routes.ts`；跨家 `calorie.view.goal-progress` 住 `src/home/`，本表只接线不实现）。25 条逐条与场景数据的 `main_prompt.cli`／`data_source` 逐字相同（对账脚本 `.scratch/t292/align-28.mjs`，`ALIGN-PASS: 28/28`）；3 条别名逐字同路由层。
- 写类 13 条（`receipt`）：11 条带空位先出预检页 `calorie.view.goal-wizard`，确认后写；暂停／重启无空位不出页，直接写。读类 15 条（`result`／`stat`）：直接跑读命令出结果页。预检两守卫（`src/goal/read.ts::viewGoalWizard`，#252 落地后补记）：改类 3 词（19／20／21）无目标行时抛 `missing-data`（exit 4，不落盘）；暂停／重启（22／23）带 `wake` 调预检页直接抛 `bad-input`（exit 2，不落盘）；定类空库照常出页（exit 0）。
- 查词用精确：`calorie.help.lookup --params '{"q":"<唤醒词>"}'` 取 `wake_word` 与查询逐字相等那条的 `cli`。唯一例外：子串搜 `看体重目标进度` 的首命中是邻词 `calorie.view.goal-progress`（`{"window":"今日"}`），取第二条（`wake_word` 相等、即本表第 12 行）才是本词命令；精确查（`lookupWake`）28/28 首命中即本表命令。
- 交付判据：① HTML 落盘、`data.output` 是绝对路径；② 字节不为 0；③ 文字回复只概括结论、不超过三句话；④ 该缺数据的（窗内无记录、无目标）出可读缺失阻断且不落盘——这一档算完成，不算缺陷。

## 28 条逐条表（一行一条唤醒词，命令照抄即跑）

| # | 唤醒词 | 该跑的命令（照抄即跑） | 工作流程 |
|---|---|---|---|
| 1 | 定营养目标 | `calorie-cmd-read calorie.goal.set --params '{"calorie":1800,"protein":150,"carbs":200,"fat":50,"water":2000}'` | 写前预检：先 `calorie-cmd-read calorie.view.goal-wizard --params '{"wake":"定营养目标"}'` → 确认后跑本行写命令 → 写后回执页 |
| 2 | 定营养目标(自动算) | `calorie-cmd-read calorie.view.goal-wizard --params '{"profile":"cut","wake":"定营养目标(自动算)"}'` | 本行即预检页 → 确认后跑 `calorie.goal.set` → 写后回执页 |
| 3 | 定体重目标 | `calorie-cmd-read calorie.goal.weight --params '{"kg":68}'` | 写前预检：先 `calorie-cmd-read calorie.view.goal-wizard --params '{"wake":"定体重目标"}'` → 确认后跑本行写命令 → 写后回执页 |
| 4 | 定体重目标(自动算截止) | `calorie-cmd-read calorie.goal.weight --params '{"kg":68,"deadline":"<日期>"}'` | 写前预检：先 `calorie-cmd-read calorie.view.goal-wizard --params '{"wake":"定体重目标(自动算截止)"}'` → 确认后跑本行写命令 → 写后回执页 |
| 5 | 定体重目标(含起始日) | `calorie-cmd-read calorie.goal.weight --params '{"kg":68,"startKg":72,"deadline":"<日期>","startDate":"<日期>"}'` | 写前预检：先 `calorie-cmd-read calorie.view.goal-wizard --params '{"wake":"定体重目标(含起始日)"}'` → 确认后跑本行写命令 → 写后回执页 |
| 6 | 定饮水目标 | `calorie-cmd-read calorie.goal.water --params '{"water":2000}'` | 写前预检：先 `calorie-cmd-read calorie.view.goal-wizard --params '{"wake":"定饮水目标"}'` → 确认后跑本行写命令 → 写后回执页 |
| 7 | 定饮水目标(自动算) | `calorie-cmd-read calorie.view.goal-wizard --params '{"profile":"cut","wake":"定饮水目标(自动算)"}'` | 本行即预检页 → 确认后跑 `calorie.goal.water` → 写后回执页 |
| 8 | 一键定全套目标 | `calorie-cmd-read calorie.view.goal-wizard --params '{"profile":"cut","wake":"一键定全套目标"}'` | 本行即预检页 → 确认后依次跑 `calorie.goal.set` ＋ `calorie.goal.weight` ＋ `calorie.goal.water`（不设组合写命令，见 #291 裁定二）→ 各回执页 |
| 9 | 看今日目标 | `calorie-cmd-read calorie.view.goal-progress --params '{"window":"今日"}'` | 结果：直接跑本行读命令 → 结果页（体重是累计目标，想看体重走第 12 行） |
| 10 | 看本周目标 | `calorie-cmd-read calorie.view.goal-progress --params '{"window":"本周"}'` | 结果：直接跑本行读命令 → 结果页 |
| 11 | 看营养目标进度 | `calorie-cmd-read calorie.view.goal-progress --params '{"window":"7d"}'` | 结果：直接跑本行读命令 → 结果页 |
| 12 | 看体重目标进度 | `calorie-cmd-read calorie.view.goal-weight --params '{"window":"7d"}'` | 结果：直接跑本行读命令 → 结果页（查词取 `wake_word` 相等那条，见口径） |
| 13 | 看饮水目标进度 | `calorie-cmd-read calorie.view.goal-progress --params '{"window":"今日"}'` | 结果：直接跑本行读命令 → 结果页 |
| 14 | 看目标对比实际 | `calorie-cmd-read calorie.view.goal-vs-actual --params '{"window":"30d"}'` | 结果：直接跑本行读命令 → 结果页 |
| 15 | 看目标完成度 | `calorie-cmd-read calorie.view.goal --params '{"window":"7d"}'` | 结果：直接跑本行读命令 → 结果页 |
| 16 | 看即将到期的目标 | `calorie-cmd-read calorie.view.goal-expiring` | 结果：直接跑本行读命令 → 结果页（默认 14 天内到期） |
| 17 | 看目标完成率(按周) | `calorie-cmd-read calorie.view.goal-progress --params '{"window":"7d"}'` | 结果：直接跑本行读命令 → 结果页 |
| 18 | 看目标完成率(按月) | `calorie-cmd-read calorie.view.goal-progress --params '{"window":"7d"}'` | 结果：直接跑本行读命令 → 结果页 |
| 19 | 改营养目标 | `calorie-cmd-read calorie.goal.set --params '{"calorie":1800,"protein":150,"carbs":200,"fat":50}'` | 写前预检：先 `calorie-cmd-read calorie.view.goal-wizard --params '{"wake":"改营养目标"}'` → 确认后跑本行写命令 → 写后回执页（含改前→改后） |
| 20 | 改体重目标 | `calorie-cmd-read calorie.goal.weight --params '{"kg":67.5}'` | 写前预检：先 `calorie-cmd-read calorie.view.goal-wizard --params '{"wake":"改体重目标"}'` → 确认后跑本行写命令 → 写后回执页（含改前→改后） |
| 21 | 改饮水目标 | `calorie-cmd-read calorie.goal.water --params '{"water":2200}'` | 写前预检：先 `calorie-cmd-read calorie.view.goal-wizard --params '{"wake":"改饮水目标"}'` → 确认后跑本行写命令 → 写后回执页（含改前→改后） |
| 22 | 暂停所有目标 | `calorie-cmd-read calorie.goal.pause` | 回执：直接跑本行写命令 → 写后回执页（无空位，不出预检页；回执带恢复入口提示） |
| 23 | 重启所有目标 | `calorie-cmd-read calorie.goal.resume` | 回执：直接跑本行写命令 → 写后回执页（无空位，不出预检页） |
| 24 | 看目标历史完成 | `calorie-cmd-read calorie.view.goal --params '{"window":"30d"}'` | 结果：直接跑本行读命令 → 结果页（按现值回算历史达成，不另设历史表，见 #291 裁定一） |
| 25 | 看目标预测达成 | `calorie-cmd-read calorie.view.goal-predict --params '{"window":"14d"}'` | 结果：直接跑本行读命令 → 结果页 |
| 26 | 看目标推荐 | `calorie-cmd-read calorie.view.goal-recommend --params '{"profile":"cut"}'` | 结果：直接跑本行读命令 → 结果页（自造词：别名，见口径） |
| 27 | 看目标配置 | `calorie-cmd-read calorie.view.goal-config` | 结果：直接跑本行读命令 → 结果页（自造词：别名，见口径） |
| 28 | 看目标状态 | `calorie-cmd-read calorie.view.goal-status` | 结果：直接跑本行读命令 → 结果页（自造词：别名，见口径） |

## 预检 11 条接线（写前页 → 写命令）

11 条带空位写词共用一页 `calorie.view.goal-wizard`（页面摆库内现值／按档案算的推荐值与依据／改前→改后对照；用户确认＝复制页上 prompt 回给 AI）。写入仍走 5 条既有写命令，不新增组合写命令：

| 写词（本表 #） | 预检页命令 | 确认后跑的写命令 |
|---|---|---|
| 1 定营养目标／19 改营养目标 | `calorie.view.goal-wizard`（`wake`＝本词） | `calorie.goal.set` |
| 2 定营养目标(自动算) | 本表第 2 行即预检页 | `calorie.goal.set` |
| 3 定体重目标／4 自动算截止／5 含起始日／20 改体重目标 | `calorie.view.goal-wizard`（`wake`＝本词） | `calorie.goal.weight` |
| 6 定饮水目标／21 改饮水目标 | `calorie.view.goal-wizard`（`wake`＝本词） | `calorie.goal.water` |
| 7 定饮水目标(自动算) | 本表第 7 行即预检页 | `calorie.goal.water` |
| 8 一键定全套目标 | 本表第 8 行即预检页 | `calorie.goal.set` ＋ `calorie.goal.weight` ＋ `calorie.goal.water`（逐项确认，不合成一条） |

暂停／重启（22／23）无空位不出页：带 `wake` 调本页直接用法阻断（`bad-input`，exit 2，不落盘，文案「暂停／重启不出预检页（没有可确认的内容，直接跑写命令）」，实现 `src/goal/read.ts:128-130`）。改类 3 词（19 改营养目标／20 改体重目标／21 改饮水目标）在无目标行（`draft.current === null`）时缺失阻断（`missing-data`，exit 4，不落盘，文案「尚无目标（先定目标，再改目标）」，实现 `src/goal/read.ts:133-135`）；定类空库照常出页（exit 0）。28 条命令字串本身不变，对账仍 `ALIGN-PASS: 28/28`。页面装配在 `src/goal/precheck.ts`，命令在 `src/goal/commands.ts`，接线在 `src/goal/routes.ts`。

## 权威源（改一处即生效，本件是唯一手写表）

- 唤醒词与 prompt：`src/triggers/scene-06-goal.ts`（25 条）＋ `src/triggers/help-lookup.ts` 的 `WAKE_TABLE`（3 条自造别名）。
- 命令与形状：`src/goal/commands.ts`（写 5 ＋ 读 9）；路由：`src/goal/routes.ts`（键属本能力的行）＋ 跨家 `calorie.view.goal-progress`（`src/home/`）。
- 老技能对照：`t164-老技能对账表.md`（25 条逐条对账）；页面清单：`t164-页面清单与公共层.md`；数据面裁定：`t291-三件裁定.md`。
- 分支读法（按问题挑一处，不整件通读）：目录归属 → `t164-页面清单与公共层.md` §一；命令归属 → 同件 §三；票图与关票证据 → 同件 §五；裁定争议 → 同件 §六（结论本体）＋ `t291-三件裁定.md`（数据面）。
