# `#313` B 段 · 文档与验收脚本席（标签 `313b3`）证据

写集：`docs/**`。三件交付：① 纪律正本改后事实；② 照抄说明改后事实 ＋ 编排者裁定的两句；
③ 自治验收探针 `t293-验收-命令自治.mjs` 的 legacy 分区修复（＋ 重跑实测）。

- 起手基线（修前）观测：`.scratch/t313b3/baseline-探针.txt`（`RESULT: P1=FAIL P2=PASS P3=PASS P4=PASS P5=PASS 4/5`，exit 1）
- 修后观测：`.scratch/t313b3/after-探针.txt`（`RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`，exit 0）
- 变异观测：`.scratch/t313b3/变异-撞键.txt`（`RESULT: P1=FAIL …4/5`，exit 1）
- 仓内 `gen:check`（持锁 `--ticket 313b3`）：`PASS：键 101（写 35 ＋ 读 66）；能力 1 个（weight）声明 9 条，未搬迁清单 92 条`

**路径口径更正（照编排者更正、已同步进三份文本）**：未搬迁命令的**路由**声明落位是
`src/cli/legacy/routes/scene-NN.ts`（＋ 汇总 `src/cli/legacy/routes/index.ts`），**不是** `routes-scene-NN.ts`。
理由（编排者给的）：`scripts/gen-cli.mjs` 扫 `src/cli/legacy/` 只收 `*.ts` 文件且要求每个文件恰导出一个**命令**声明数组，
扁平分片放路由文件会被当成命令清单校验而让 `pnpm build` 全红；放子目录天然被跳过，既有门禁零改动。
实测旁证：`gen-cli.mjs:63` 的 `LEGACY_NON_SCENE = new Set(['index.ts','types.ts'])` 与 `:90 scanLegacySceneNames()` 都是扁平口径。

---

## ① 三处文本 · 改前／改后逐句对照

| 位置 | 改前（一句） | 改后（一句） |
| --- | --- | --- |
| `docs/agents/命令登记纪律.md` 原 :112-113（现 :114-121） | 「**还有一处必须手跟、且没有门替你算**：新键要能被唤醒词命中，`src/triggers/routing.ts` 得有一条路由。它是手写路由表、与冻结的场景表按位对齐（19 字段 sha 是 #180 重导的锚），**天生不可从 `commands.ts` 派生**。」 | 「**还有一处必须手跟，但 `#313` 起改法变了**：新键要能被唤醒词命中，得有一条**路由声明**。路由的**声明**住各能力目录（`src/<能力>/routes.ts`）；**未搬迁**的键住自己场景那一份 `src/cli/legacy/routes/scene-NN.ts`……`src/triggers/routing.ts` 的记录面已是**生成物**：`pnpm gen` 重生成、`pnpm gen:check` 守，日常**零接触**，别再往它里面手写一行」 |
| 同上 原 :114 | 「加／删一条命令仍要动它，动前**抢锁**，**只加／删自己那一行**。」 | 「加／删一条命令动的是**自己那一份**，动前**抢锁**。」＋「哪些能力今天已搬、各自对应哪个 `routes.ts`＝**会过期的名单**，不抄在这里，以 `pnpm gen` 的逐件输出为准（同 §四）。」（照铁律二：不在这里维护文件名单） |
| 同上 原 :117 | 「**删**命令时它更早暴露：`routing.ts` 的 `RouteKey` 是编译期约束，删了声明直接过不了 `packages/skill-calorie` 的类型门。」 | 「**删**命令时它更早暴露：`RouteKey` 是编译期约束（类型唯一处 `src/triggers/routeSpec.ts`），删了声明直接过不了 `packages/skill-calorie` 的类型门。」 |
| `docs/agents/命令登记纪律-照抄说明.md` 原 :49 | 「`src/triggers/routing.ts`（手写路由表：加／删一条命令仍要动它，**动前抢锁，只加／删自己那一行**）；」 | 「**路由声明**：已搬迁的住 `src/<能力>/routes.ts`，未搬迁的住自己那一片 `src/cli/legacy/routes/scene-NN.ts`（**动前抢锁**）。`src/triggers/routing.ts` 的记录面**已不是手写位**：`#313` 起它是生成物（`pnpm gen` 重生成、`pnpm gen:check` 守），日常**零接触**；」 |
| 同上 原 :67-69 | 「**新增**一条命令 → 要动 `src/triggers/routing.ts`（加唤醒词命中）**与** `test/calorie-routing-81.test.mjs` 的路由覆盖断言——那条断言是**手写路由表**的守门人（`routing.ts` 的唤醒词路由逐条手写、与冻结场景表按位对齐，**天生不可从 `commands.ts` 派生**）。」 | 「**新增**一条命令 → 只写自己能力目录的声明：`commands.ts` 一条命令声明，要能被唤醒词命中就再在 `src/<你的能力>/routes.ts` 里加一条路由声明，然后 `pnpm gen` 重生成——`src/triggers/routing.ts` 的记录面是生成物，**不必手改、也别手改**。`test/calorie-routing-81.test.mjs` 的路由覆盖断言仍守着这一条（覆盖数 ≠ 权威声明数即红），但它守的现在是**生成物**。」 |
| 同上 ③（新增两句，编排者裁定） | （无） | 「**`src/cli/legacy/` 是什么**：它是**未搬迁清单的过渡住处**——清单按场景分片（`scene-NN.ts`），路由声明同构住 `routes/scene-NN.ts`（子目录，`gen-cli.mjs` 扫 `legacy/*.ts` 时天然跳过）。词源是 `#295` 起的既有术语（「未搬迁清单」「老命令就地搬迁」），`legacy/` 是那批命令在代码里的落位，**不是自造词**。」 |
| 同上 ③（新增，可判收口口径，供 `#320` 核） | （无） | 「**收口口径（可判，供 `#320` 收口时核）**：清单清空后整个 `src/cli/legacy/` 目录必须**删掉**。判据三条：① `packages/skill-calorie/src/cli/legacy/` 目录不存在（`scene-NN.ts` 与 `routes/` 一片不留）；② 旧单文件名 `src/cli/legacyCommands.ts` 不复现；③ 生成器不再报出 `legacy` 这个扫描面、棘轮测试的条数／行数上限降到它的地板（上限本身住那个棘轮文件，不抄在这里）。**留一个空壳目录＝没搬完。**」 |

**顺带改掉的「已删文件的陈旧指认」（同一文件内、A 段删了 `legacyCommands.ts` 后就成了假话，一并改成改后事实）**：

| 位置 | 改前 | 改后 |
| --- | --- | --- |
| `命令登记纪律.md` 形状三（原 :83） | 「还没搬的命令住 `src/cli/legacyCommands.ts`（过渡期住处，**只进不出**）。搬一条：」 | 「还没搬的命令住 `src/cli/legacy/`（过渡期住处，**只进不出**）：按场景分片 `scene-NN.ts`，一条命令的键恰住一片；它的路由声明同构住 `src/cli/legacy/routes/scene-NN.ts`（子目录，生成器的清单扫描面看不见它）。搬一条：」 |
| 同上 形状三 第 2 步（原 :86） | 「**手删 `legacyCommands.ts` 里那一行**——一个键恰住一处，两边都声明生成器就抛；」 | 「**手删自己场景分片 `legacy/scene-NN.ts` 里那一行 ＋ 同号 `legacy/routes/scene-NN.ts` 里那条路由声明**（一个键恰住一处，两边都声明生成器就抛），路由声明拷进 `src/<能力>/routes.ts`；」 |
| 同上 棘轮表（原 :101）／判据 3（原 :126） | 「**搬走一条命令**：`legacyCommands.ts` 删掉那一行…」／「搬完一条，`legacyCommands.ts` 与棘轮上限是不是都变小了？」 | 「**搬走一条命令**：自己场景分片删掉那一行…」／「搬完一条，自己场景分片与棘轮上限是不是都变小了？」 |
| 同上 出处表（原 :155） | 「`routing.ts` 为何不可派生、再加两处手跟点 ｜ `t295-复审-蓝队.md`…」 | 「路由记录面从 `routing.ts` 迁成生成物、声明住能力目录／场景分片（`#313`）｜ 生成物门 `pnpm gen:check` 的逐件输出 ＋ `docs/skills/skill-calorie/t313b3-*` 证据」 |
| `照抄说明` ③ 首条／搬迁条／删命令条（原 :48、:51、:73） | 「`src/cli/legacyCommands.ts`（未搬迁清单…）」／「手删 `legacyCommands.ts` 里那一行」／「`routing.ts` 的 `RouteKey` 是编译期约束，删了声明不先动 `routing.ts`」 | 「`src/cli/legacy/scene-NN.ts`（未搬迁清单：一个场景一片…）」／「手删自己场景分片里那一行 ＋ 同号 `routes/scene-NN.ts` 里那条路由声明」／「`RouteKey` 是编译期约束（类型唯一处 `src/triggers/routeSpec.ts`），删了声明不先动自己那份路由声明（`src/<能力>/routes.ts`／`src/cli/legacy/routes/scene-NN.ts`）」 |

铁律二自检：三份文本里**没有**任何「今天哪些能力已搬／哪些 `routes.ts` 存在」的名单；只有模式（`src/<能力>/routes.ts`、`scene-NN.ts`）
＋ 指向权威出处（`pnpm gen`／`pnpm gen:check` 的逐件输出、棘轮文件）。

---

## ② 验收探针修复 · 逐处（照 A 段 §⑧ 的改法，行号是**本席提交后**的当前行号）

| 位置 | 改法 | 备注 |
| --- | --- | --- |
| `:32` → `:32` | `LEGACY: '…/cli/legacyCommands.ts'` ⇒ `LEGACY_DIR: '…/cli/legacy'` | 另加 `LEGACY_FILES`（十片 `scene-NN.ts`，由 `LEGACY_DIR` 拼出） |
| 读面（新 `readLegacy()`，`:70-74`；`LEGACY_FILES` 常量在 `:51`） | 逐片 `read()` 拼接；**任一片读不到 ⇒ null** | 调用方按「探针失能」判红，不许当空清单 |
| 原 `:158-159` → `:172-173` | `read(P.LEGACY) \|\| ''` ⇒ `readLegacy()`；null 即 `{ok:false, why:'未搬迁清单读不到…'}` | 权威总数恢复 101（写 35／读 66），下游钉死计数锚点随之正确 |
| 原 `:311` → `:326` | 正则 `/legacyCommands\.js/` ⇒ `/scanLegacySceneNames/ \|\| /'cli',\s*'legacy'/`；标签改 `LEGACY_DIR`（场景分区，编译后 `dist/cli/legacy/scene-NN.js`） | 实测生成器输入面已恢复报出 legacy 这一项 |
| 原 `:395`／`:406` → `:156`（helper）＋ `:410`／`:421` | `sandboxSha(P.LEGACY)` ⇒ `sandboxShaLegacy()`（逐片 sha 拼接，缺片记 `MISSING`） | 「十片逐字节未动」不再是空话；实测 `legacy:true` |
| 原 `:409` → `:424` | 文案「+ legacyCommands 逐字节未动」⇒「+ 未搬迁清单十片（legacy/scene-NN.ts）逐字节未动」 | 报告不失真 |
| 原 `:411-423` → `:426-449` | 撞键兜底 `legacyKeys[0] \|\| 'calorie.diet.add'` **删掉**（取键 `:432-433`）；取不到键 ⇒ 显式判红（`:435-439`，`dupCaught=false`、`dynOk=false`）；判据 `:445` **加强**为「exit≠0 ＋ 报『命令键重复登记』＋ 报错里点的正是这个键」 | 见 ③.3：变异自证证明它靠真撞键判 |
| 原 `:437` → `:461` | 允许清单路径正则 ⇒ `/^packages\/skill-calorie\/src\/cli\/legacy\/scene-\d\d\.ts$/`，类名 `AUTHORITY②（未搬迁清单的场景分区，键恰住一处）` | 体重 9 键「仍留在清单里」的检查恢复真实（原为恒报 0 处的假绿） |
| 原 `:505-507` → `:529-536` | `read(P.LEGACY)` ⇒ `readLegacy()`；**读不到清单时按「9 键仍留」判红**（`stillLegacy` 置满），不再静默当 0 处 | 同上 |
| `changeSurfaces()` else 支（`:307`） | 「钉死计数扫描跳过」⇒ 明确写成「**探针失能…按 FAIL 记**：读不到权威源不等于『没有手写面』」 | 假绿改成显式红 |

**② 的分类留待后续席位（按派单不动）**，见 ④。

---

## ③ 探针实测结果

### 3.1 修前基线＝假红（`.scratch/t313b3/baseline-探针.txt`）

```
RESULT: P1=FAIL P2=PASS P3=PASS P4=PASS P5=PASS 4/5     （exit 1）
```

- 形态一（假红）：`P.LEGACY` 指向已删的 `legacyCommands.ts` ⇒ `legacy` 空 ⇒ 权威总数 **101→9**，
  钉死计数用了错锚点（写＝4、读＝5），于是三个**无关**断言被误报成「钉死计数」：
  `test/profile-guard-175.test.mjs:72`、`test/calorie-routing-81.test.mjs:320`、`test/help-center-91.test.mjs:106`
  ⇒ `UNACCOUNTED=3` ⇒ P1=FAIL。
- 形态二（假绿隐患）：撞键探针「共 **0** 条候选」并退到字面量 `calorie.diet.add`——**这次靠巧合撞上**，
  一旦该键被搬走就是「不撞键的假绿」。

### 3.2 修后（环境干净时，`.scratch/t313b3/after-探针.txt`）

```
RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5      （exit 0）
```

- `UNACCOUNTED=0`、`树自相矛盾=0`；生成器输入面**恢复**报出 `…/src/cli/legacy（未搬迁清单的场景分区 scene-NN.ts…）`
- `分派两文件 + 未搬迁清单十片（legacy/scene-NN.ts）逐字节未动={"read":true,"write":true,"legacy":true}`
- 撞键（**真撞**）：`假能力声明一条仍在未搬迁清单里的键 calorie.view.diet，共 92 条候选 → gen exit=1，报「命令键重复登记」=true`
- P2 的清单面：`体重 9 键仍留在未搬迁清单（legacy/scene-NN.ts）里的：0 处（随搬迁已删净）`（不再是空清单假绿）
- 环境噪声一轮（如实记）：中途有一窗口，另一席的 `src/**` 未提交改动在途（`git status` 见
  `?? src/cli/legacy/routes/`、`?? src/triggers/routeSpec.ts`、`?? src/weight/routes.ts`），
  沙箱里 `gen:check` 基线为 1 ⇒ 探针把 P1/P3 记为 PENDING（不是 FAIL）。同一时刻在**仓内**持锁复跑
  `gen-cli.mjs --check` 是 **PASS**（键 101）；等在途窗口过去后复跑探针即得 5/5。

### 3.3 变异自证：撞键探针**有鉴别力**（`.scratch/t313b3/变异-撞键.txt`）

把 `const dupKey = legacyKeyList[0];` 换成 `calorie.zzz.notreal`（清单里**没有**的键），其余不动：

```
RESULT: P1=FAIL P2=PASS P3=PASS P4=PASS P5=PASS 4/5      （exit 1）
  迁移撞键（假能力声明一条仍在未搬迁清单里的键 calorie.zzz.notreal，共 92 条候选）→ gen exit=0，报「命令键重复登记」=false
```

⇒ 该探针**不会**不撞键也报绿；「同键重复必须被抛出」这条断言未变弱（另加「报错里点的正是这个键」一条，严格更强）。
变异件（`docs/skills/skill-calorie/t313b3-变异-撞键探针.mjs`）用完即删，提交里没有它。

---

## ④ `routing.ts` 的归类：**待后续席位**（本席按派单不动，并说明今天为何不动）

**现状事实（本席实测）**：`src/triggers/routes.generated.ts` **不存在**；`routing.ts:151` 仍是
`export const WAKE_ROUTES: readonly WakeRoute[] = [`，逐条记录还在里面。
⇒ 探针今天把它记进②「DISCIPLINED-SHARED（手写路由表）」**仍与事实相符**（P1 也因此 PASS），
故按派单**不改**，只把待改点写在这里：

| 行号 | 现在写着什么 | 生成器落地后应改成什么 |
| --- | --- | --- |
| `:40` | `ROUTING: '…/src/triggers/routing.ts'` | **保留**（文件仍在，只是不再含记录） |
| `:165` | 注释「② DISCIPLINED-SHARED 必须手改，但已被认下…（routing.ts）」 | 改成「② 已无 routing 这一项：路由声明住能力目录／`legacy/routes/scene-NN.ts`，汇总位是生成物」 |
| `:252` | 注释「`routing.ts`：手写路由表…」 | 改成「路由记录面＝生成物 `src/triggers/routes.generated.ts`（`pnpm gen` 重生成、`gen:check` 守）」 |
| `:253-268`（实读在 `:256`） | `routingSurface()` 直接读 `routing.ts` 数 `kind:'exec'` 的键 | 改为读**生成物** `src/triggers/routes.generated.ts`（或按声明面 `src/<能力>/routes.ts` ＋ `legacy/routes/scene-NN.ts` 计），否则生成后这里会数成 0 键 |
| `:310-314` | `out.disciplined.push({where: P.ROUTING…})`（归②） | 移到 `out.derived`（新键不必手改任何汇总位），文案改成「路由声明住各能力目录，`pnpm gen` 派生汇总」 |
| `:461` | `[/^…\/src\/triggers\/routing\.ts$/, 'ROUTE（唤醒词路由表，手写）']` | 改成 `'DERIVED（路由记录面，gen-routes 生成）'`；并**补两条**：`routes.generated.ts` ⇒ DERIVED、`src/cli/legacy/routes/scene-\d\d\.ts$` ⇒ `ROUTE/AUTHORITY②（路由声明，键恰住一处）` |
| `:535` | 结论口径字符串里的 `ROUTE` 一名 | 名字随之改（否则口径句子与 P2 的分类输出不一致） |

---

## ⑤ 未做／未确证

1. **未跑** `pnpm build`／`pnpm test`／`pnpm gen`：不在本席验收面（B 段别席负责），本席只跑验收探针与
   仓内 `gen-cli.mjs --check`（均持锁 `--ticket 313b3`），未写任何 `packages/**`、`src/**`、`scripts/**`。
2. **未确证**「声明里的 `order` 字段保顺序」这条的生成器实现——本席按 `.scratch/t313/接口冻结.md` 的冻结口径写进纪律；
   生成器（`gen-routes.mjs`）今天尚未落地，若最终不用 `order` 保序，纪律那一句要跟着改一处。
3. 纪律／照抄说明写的是**目标态**（`routing.ts` 记录面＝生成物、`legacy/routes/scene-NN.ts` 落位）：
   今天 `routes.generated.ts`／`routeSpec.ts`／`weight/routes.ts` 尚未提交（另一席在途）。
   本席刻意只写**模式**不写名单，落位口径若再变，只需改这三处文本里的路径词。
4. 探针 `P4`／`P5` 只跑不逐条复核（不是本席的派单面）；P2 的 `ROUTE` 分类未动（见 ④）。
5. 未核 `docs/skills/skill-calorie/t313-routing-oracle.mjs` 与全量测试（别席的验收面）。

## ⑥ 风险

1. **探针的「沙箱基线脏」仍是 PENDING**：并发席在途时动态部分（撞键、假能力）会静默降级为「未判」。
   本席把「读不到清单」这类**自身失能**改成了显式判红，但「沙箱 `gen:check` 基线不为 0」这条仍是 PENDING——
   建议后续席位顺手也改成显式（不在本席派单内，未动）。
2. 若另一席最终把路由声明落位或生成物名改了（例如不叫 `routes.generated.ts`），
   本席在 `命令登记纪律.md`／`照抄说明`／本文件里各有一处路径词要同步；④ 的表也要跟着改行号。
3. 收口口径（`legacy/` 整个目录删掉）是**口径**不是脚本门：`#320` 收口时要**人核**三条判据，
   今天没有任何机器门会拦住「留一个空壳 `legacy/` 目录」。
