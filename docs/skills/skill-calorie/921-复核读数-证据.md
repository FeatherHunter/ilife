# #921 收口复核读数（2026-09-27，HEAD `b7232ce7`）

票：[铺开页面级配方开关 pageUi：卡路里 64 处调用点按域分批 ＋ 居家管家 60 页接配方](https://github.com/FeatherHunter/ilife/issues/921)。

这份是**关票前的一次独立复跑**：不采信施工席与编排者评论里的自述，四条验收命令逐条在当刻树上重跑，
并给每条红做归因。命令一律经 `node tooling/run-locked.mjs --ticket 921 -- …`；日志落 `.scratch/t921/08-复核/`。

## 一、四条判据的当刻读数

| # | 判据 | 命令 | 读数 |
|---|---|---|---|
| ① | 源码侧开关铺开面 | `node .scratch/pageui-preview/scan-source-pageui.mjs` | 卡路里 126 处命中 − 2 处非调用点（`src/shared/docPage.ts:148` 函数定义体、`packages/skill-calorie/.scratch/t947/dist-heap/shared/docPage.d.ts:39` 类型声明）＝**真调用点 124 处，未传 `pageUi` 的 0 处** |
| ① | 居家接线（fail-closed） | `node .scratch/t921/06-居家管家接配方/check-wiring.mjs` | 模板原样（`class="page"`／旧 viewport 串）→ 投影后 `class="page ilife-page-ui"` ＋ `viewport-fit=cover`；`SHARED_CSS` 含 `pageUiCss()`＋`pageShapeCss()` 且以其结尾（84010／19341／12169 字节） |
| ② | 几何门（收口抽样） | `check-two-col-align.mjs`（卡路里预览 2 页 ＋ 居家探针 2 页）`--widths 390,1280` | `RESULT: 对位全绿；页数=4 受检列=8 受检内距表=2` |
| ② | 几何门（当刻树真产物面） | `check-two-col-align.mjs --dir .scratch/t452/out --widths 390,1280` | `RESULT: 对位全绿；页数=14 受检列=84 受检内距表=12` |
| ③ | 卡路里全量 | `node --test "packages/skill-calorie/test/*.test.mjs"` | 1919 件／1869 绿／**50 红**（票面基线 1874／41 红；对账 NEW 13／GONE 4） |
| ③ | 居家全量 | `node --test "packages/skill-home/test/*.test.mjs"` | 357 件／354 绿／3 红（3 条隔离复跑**稳定复现**，非并行抖动） |
| ③ | 本票改过的卡路里测试件 | `451`／`452`／`523`／`543`／`t467`／`weight-receipt-337` | **55/55 全绿** |
| ③ | 本票改过的居家测试件 | `express`／`items-1`／`items-2`／`items-3`／`scaffold`／`setup`／`space-pages`／`style-audit` | **133/133 全绿** |
| ④ | 观感复核实拍 | `node .scratch/t921/08-复核/shot-pages.mjs` | 6 批 × 390／1280 共 **12 张**，落 `.scratch/t921/08-复核/shots/`；原交付里没有这批实拍，本席补拍 |

抽样页：`view-weight.html`（01 体重）／`.scratch/t452/out/01-week.html`（02 运动，当刻树重出）／`view-goal.html`、`view-profile.html`（03 饮食目标档案）／`report-compare.html`（04 主页分析趋势）／`08-复核/pages/plan-write-preview.html`（05 plan，本席现场生成）／`probe/home/item-search.html`、`care-backup-list.html`（06 居家）。
其中 390 档真的走「一行一条事实」那一型的是 02／03／04 与两张预览页（`恰两列=true` 由截图脚本逐页打印）。

## 二、红的归因（逐条给证据）

**卡路里 13 条 NEW，一条都不是本票**：

- `#176`×4、`#175`×2、`#365 id 卡三态`、`#374 看今日饮食概览`：`#970` 的「全量重构」批次改了同一批字段与卡片（提交信息逐条点名 176／175 等），断言跟着搬家；本票的 6 个测试件当刻全绿。
- `概念读数：结论条／徽章／空态各有几条定义地`：多了一处空态定义地（判据要求先登记）。
- `S1-② 发布包体干净安装`：`base-link-core` 不导出 `readDataSchema`（安装态与本地版本不齐），与页面配方无关。
- `T5 真包门禁`／`T9 --sync --dry`：当刻台账陈化件是 `src/triggers/wake-assets.ts`（台账 5158／实况 7509）——**不在本票改过的件里**。
- `#93 ① 68 读键 … data/html 全等`：已知跨秒挂钟抖动（单跑复现绿）。

**居家 3 条，同样不是本票**：`HOME_KEY_SHAPES` 多了 `home.data.query`（无对应模板 ⇒ `HomeRenderError: 无模板映射`）、闲置天数没上屏 —— 都是别的窗口在途的功能件；本票的接线自证与 8 个测试件全绿。

**抽样页上另有 5 条 1280 档对位红**（`report-compare` 3 条 14px／`view-weight` 1 条 14px／居家 `care-lint` 1 条 4px）：

- 复现：`check-two-col-align.mjs --dir .scratch/t921/probe/calorie --widths 390,1280`（判红 4 条）、`--dir .scratch/t921/probe/home --widths 1280`（判红 1 条）。
- 归因：本席做了「配方开／关孪生件」对照（`make-off-twins.mjs`：只把版面根类那一小段去掉，规则全套挂在根类之下 ⇒ 配方整棵失效，正文与表格一字不动），**两态读数逐字相同** ⇒ 与配方无关，是这些表自身在宽档的既有欠账（#879 那条几何门的家族），归公共视觉层另议。

## 三、遗留（带「谁、看哪个读数」）

1. **作息族 A 组未重生成**（票面「遗留出口」第 1 条允许登记）。那批页住
   `.scratch/t783·t787·t788·t790·t792·t888·t891·t892·t893·t895` 的 `成品／产物`，10 张票的临时目录里
   **没有统一的出页入口**（逐目录只有各票自己的探针脚本）。本席在能跑的 5 个目录上重取了当刻读数
   （`check-two-col-align.mjs --dir <目录> --widths 390`）：

   | 目录 | 页数 | 读数 |
   |---|---|---|
   | `.scratch/t783/成品` | 7 | 对位全绿（受检内距表=0：这批页不走那一型） |
   | `.scratch/t787/成品` | 11 | 对位全绿（受检内距表=0） |
   | `.scratch/t788/成品` | 13 | **判红 6 条（受检内距表=3）** |
   | `.scratch/t790/成品` | 4 | **判红 1 条（=1）** |
   | `.scratch/t792/产物` | 61 | **判红 7 条（=4）** |

   红签名就是 #919 修掉的那两条：`表标题文字左缘与首格文字左缘差 10px`、`行文字到卡片边框内距 左 1px／右 1px（下限 8px）`。
   **谁**：接手「作息产物重生成」的那张票（本票按票面不另开票）；**看哪个读数**：上面这条命令在这批目录上跑出 `对位全绿` 且 `受检内距表 > 0`。
2. 上面那 5 条 1280 档对位红：本票已证明与本票无关，但欠账本身仍在（复现命令见 §二）。

## 四、交付面更正

票面评论里「提交 `95cfb029`（48 件：卡路里 31 源 ＋ …）」与提交实际不符：`95cfb029` 只带走 17 件。
卡路里其余源件与 6 个测试件的改动，是被**别席的** `5445c86b`（`feat(915/925/678)`）连工作区在途件一并扫入的
（`git show --numstat 5445c86b -- packages/skill-calorie/src`）。两条提交合起来才是本票的交付面。
