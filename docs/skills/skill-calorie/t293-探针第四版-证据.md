# `#293` 验收探针**第四版** · 证据（t293-探针第四版）

> 席位：独立改动席 `t293v4`（接手一份**无主**的未提交 diff）。基线：`master`，接手时 `HEAD == origin/master == fb72a31`；
> 落点前编排者已代提交规则 38 那一句（`2ac92a2`），故本席的提交接在 `8652d34` 之上。
> 探针：`docs/skills/skill-calorie/t293-验收-命令自治.mjs`（本席改动前 `sha256=F9162332…A700633`，
> 改前 `git diff` ＝ 上一位席的 `+124 −10`、11 hunk）。
> 本文件由人工据当次日志誊写；所有数字来自 `.scratch/t293v4/` 下的落盘输出（逐条给文件名）。

---

## §0 接手的那份 diff：怎么判的、补了什么、重做了什么

**采信的部分（方向对、可用，未回退重做）**

| 件 | 判定 |
| --- | --- |
| `pinnedCountOfLine()` 的 `forced` 分支（两处 `return null` → 落 ④ ＋ 注明出处） | **采信**：正是第一件事要的；三例实测落 ④（§1） |
| `undetGroupOf()`／`UNDET_GROUPS`／`groupUndetermined()`（空组也发） | 采信为骨架，**顺序重做**（见下） |
| `shapeGuard()` 的两套样名（CI 半 ＋ 大写半）、不吃文件路径 | **采信**：误伤面实测为 0（§3） |
| ④ 渲染改逐组带计数 ＋ 头部注释写入那句话 | 采信 |

**补齐／重做的部分（缺了这四件，四件事只做完了不到一半）**

1. **守卫只渲染、不接线**（最关键的一处）：那份 diff 把 `shapeGuard()` 写进 `buckets` 又打印出来，
   却**没有**把它接进 P1 的状态机——`verdict()` 仍只认 `UNACCOUNTED`／`incomplete`／静态面。
   等于"登记样名落进 ④"只会多打两行字，P1 照样 `PASS` ⇒ 第三件事（记 `PENDING`）**没有实现**。
   本席新增纯函数 `undetVerdict(guard)`（命中 ⇒ `'PENDING'`，否则 `null`）并在 `verdict()` 里**先于**静态判定短路。
2. **④ 分组顺序与验收口径不一致**：原diff 的顺序是「退出码／差值 · **访问器调用** · 域内 metrics · 域内量 · 裸标识符 · 其它」。
   按验收口径改为「退出码／差值 · **访问器调用** · **域内量** · **域内 metrics** · 裸标识符 · 其它」，
   并把「顺序＝判定顺序＝输出顺序、换序会静默改掉既有行的组别」写进 `UNDET_GROUPS` 的注释（顺序是口径，不是排版）。
   受序影响的只有「调用后取量」一种形状（`buildPhotoHelp().length`）：本判据先判**调用形状** ⇒ 它留在**访问器**组，
   与第三版逐行一致（当刻活树 4 行归组未变）。
3. **锚集一个字节都没动**（那份 diff 完全没碰 `authorityTotals()`）：锚仍是「残留未搬迁清单 ＋ 体重声明」＝`10/4/6/9`，
   而真实权威是 `101/35/66/9`。后果是 ④ 里 81 行里 50 行是 `.status` 退出码噪声，**而真被写死的权威总量（`101`）连扫都扫不到**。
   本席按编排者追加裁决 ＋ 对抗审查 S2-1 重写 `authorityTotals()`，与生成器同源（§4）。
4. **没有夹具、没有变异自证、没有端到端证据**：本席补 `.scratch/t293v4/fixture.mjs`（真例／假例／必测三例／分组／守卫／锚集）、
   `mutate.mjs`（改坏一处必须变红）、`tree-mutate.mjs`（整体副本里真跑探针：`PASS → PENDING → PASS`）、
   `r38-proof.mjs`（规则 38 两个方向）。

**为什么没有 `git checkout -- <文件>` 重来**：diff 的主体（消静默、分组、守卫、渲染）是对的且已可用，
缺口是"没接线 ＋ 顺序不符口径 ＋ 锚集没动 ＋ 无证据"，四件都能在其上补齐；回退重做只会丢掉已经对的部分。

---

## §1 第一件事：关掉两处静默出口（必测三例必须出现在 ④，不能消失）

判据链：`pinnedCountOfLine()` 里两条原 `return null`（`/\[\s*\d/` 与 `DECLARED_[A-Z_]+`）改成
`{ subject:'undetermined', forced:<出处>, other, text }` ⇒ `classifyPinnedHit()` 归 ④ 并在 `what` 里注明是从哪个静默出口捞出来的。
保留的唯一一条静默出口是「锚不在被比操作数位」（消息串／注释里的数字本就不该当操作数看）。

| 例（原文） | 锚 | 期望桶 | 实测桶 | 另一侧 | `forced` 出处 |
| --- | --- | --- | --- | --- | --- |
| `assert.equal(KEYS.length,28,'分组 [3] 的键数');` | 28 | ④ | **④ undetermined** | `KEYS.length` | 数组下标／数组字面量形状（原 `/\[\s*\d/` 静默出口） |
| `assert.equal(KEYS[0].length, 28);` | 28 | ④ | **④ undetermined** | `KEYS[0].length` | 同上 |
| `assert.equal(DECLARED_KEYS.length, 28);` | 28 | ④ | **④ undetermined** | `DECLARED_KEYS.length` | 与声明对账的派生量（原 `DECLARED_` 静默出口） |
| 同三例配当刻锚值 `101` | 101 | ④ | **④ undetermined** | 同上 | 同上（锚值换掉不改形状） |
| `assert.equal(x.length, 3, '应为 101 条');` | 101 | 消失（保留出口） | **null** | — | 锚不在被比操作数位 |

出处：`.scratch/t293v4/fx-base3.txt`（基线 `RESULT: ALL PASS`）、`fx-mutated.txt`（变异）。

## §2 第二件事：④ 分组（按另一侧的形状，裸标识符单列，每组给计数）

`UNDET_GROUPS = ['退出码／差值', '访问器调用', '域内量', '域内 metrics', '裸标识符', '其它']`（顺序＝判定顺序＝输出顺序）。
**空组也发**：计数 0 是"这一形状被查过"的证据，不发出来会被读成"没查"。

**当刻活树 ④ 分组计数（第四版口径，`g-probe.txt`）**：
`[退出码／差值] 0 处 · [访问器调用] 0 处 · [域内量] 1 处 · [域内 metrics] 0 处 · [裸标识符] 0 处（单列） · [其它] 2 处`，共 3 处。
逐行（3 行全列）：

| 组 | where | 另一侧 | 行 |
| --- | --- | --- | --- |
| 域内量 | `test/calorie-routing-81.test.mjs:342` | `c.length` | ``assert.equal(c.length, 9, `smoke §1 列数异常：${c.join(' | ')}`);`` |
| 其它 | `packages/skill-calorie/test/cmd-write-40-persist.test.mjs:144` | `row.protein` | `assert.equal(row.protein, 35);` |
| 其它 | `packages/skill-calorie/test/cmd-write-40-persist.test.mjs:419` | `row.carbohydrates` | `assert.equal(row.carbohydrates, 66);` |

**改锚前的同一棵树（第三版口径，`.scratch/t293v4/recon3.txt`）**：④ ＝ **81 处**
——退出码／差值 **50** · 访问器调用 4 · 域内 metrics 5 · 域内量 22 · 裸标识符 0 · 其它 0。
⇒ 改锚把 ④ 从 **81 塌到 3**，且消掉的全是 `.status` 退出码那类噪声（噪声正是"④ ≥ 60 就记 PENDING"这个错药的来因）。

## §3 第三件事：形状守卫 ⇒ 记 `PENDING`（不是 FAIL、更不是静默通过）

- 判定面：`shapeGuard(rows)` 只看**另一侧操作数文本 ＋ 整行原文**（**不吃文件路径**——否则 `legacy-partition-313`／
  `cmd-registry-294` 这类件名会让 P1 永久黄灯）；样名分两半：不分大小写的 `registry／注册表／declared／keys?／SCENARIO_KEYS／未搬迁`，
  只认大写的 `TOTAL／总数／命令总数／权威`（`d.total`／`out.data.total` 是域内量，不许误伤）；另加「裸标识符比较裸数字」形状。
- **状态接线**：`undetVerdict(guard)` 命中 ⇒ `'PENDING'`；在 `verdict()` 里**先于**静态判定短路（`UNACCOUNTED` 非空仍优先判 FAIL）。
  **没有**任何"④ ≥ 60 就 PENDING"的计数阈值——按形状与命名判，不按行数判。
- **当刻活树：守卫未命中 0 行 ⇒ `P1..P5` 仍 `5/5`**（`g-probe.txt`）。
- 头部注释与证据件都写入了那句口径：「④ 只保证看得见，不保证看得住——凡登记样名落进 ④，本判据记 `PENDING`，不静默放行。」

**端到端（不是纯函数，是整体副本里真跑探针本体，`.scratch/t293v4/tree-*.txt`）**：把整树复制到
`.scratch/t293v4/mini/`（`node_modules` 走 junction）＋ `git init/commit`，注入**一行**登记样名
`assert.equal(KEYS.length, 101, '分组 [3] 的键数');`（`git add` 后 `git grep` 才看得见）：

| 状态 | P1 | ④ 计数 | 形状守卫 | 命中行 |
| --- | --- | --- | --- | --- |
| 基线（未注入） | **PASS** | 域内量 1 · 其它 2 | 未命中 0 行 | — |
| 注入一行 | **PENDING** | 域内量 2 · 其它 2 | **命中 1 行** | `packages/skill-calorie/test/x-inject.test.mjs:3｜登记样名（keys／registry／declared 一类）｜另一侧=KEYS.length` |
| 移除该行 | **PASS** | 域内量 1 · 其它 2 | 未命中 0 行 | — |

副本里 `P3=FAIL` 是副本环境事实（副本里 `gen:check` 基线红），与真仓 `P3=PASS` 无关；本表只读 P1 与守卫。

## §4 追加裁决：锚集与权威同源（旧锚值 → 新锚值）

**旧**：`authorityTotals()` ＝ `未搬迁清单（残量）＋ 体重声明` ⇒ `total=10 / writes=4 / reads=6 / weight=9`。
**新**：遍历生成器自己的两个声明源——`src/<能力>/commands.ts`（能力名升序，判据与 `gen-cli.mjs` 的 `scanCapabilityNames()` 同）
＋ `src/cli/legacy/scene-NN.ts`（文件名升序，跳过 `index.ts`／`types.ts`），按 `key` 合流（同键两处声明即报红，同 `merge()`）
⇒ **`total=101 / writes=35 / reads=66 / weight=9`**。
外证（生成器自报）：`GEN-CHECK PASS：键 101（写 35 ＋ 读 66）；能力 10 个（analysis／body／diet／exercise／goal／home／photo／profile／weight／workout）声明 100 条，未搬迁清单 1 条`（`.scratch/t293v4/g-gencheck.txt`）。
内证（探针自证）：`authorityTotals()` 末尾拿**生成物** `src/cli/keys.ts` 的键表反查锚集（写块 35 ＋ 读块 66 ＝ 101），
不符即判**失能**——`别拿错锚去扫`（这正是"锚集与权威脱节"的复发位）。

**「真实总量现在会被扫到」的样例**（`.scratch/t293v4/fx-base3.txt` §六；走探针自己可换的 `grepFn` 出口，
把仓里有哪些行固定成一行虚拟文件，两次只换 `totals`）：

| 锚集 | 扫 `assert.equal(KEYS.length, 101);` | 结果 |
| --- | --- | --- |
| 旧 `10/4/6/9` | `\b10\b`／`\b4\b`／`\b6\b`／`\b9\b` 都不匹配 `101` ⇒ **命中 0 行** | **进不了任何桶**（看不见的洞） |
| 新 `101/35/66/9` | `\b101\b` 命中 ⇒ 1 行 | 桶＝**`unaccounted`（③ ⇒ P1=FAIL，判红）** |

## §5 夹具双向验证（真例／假例／必测三例 逐例对桶）

夹具：`.scratch/t293v4/fixture.mjs`（判据全走探针出口函数，不另写复判）；输出 `.scratch/t293v4/fx-base3.txt`
（`RESULT: ALL PASS`）。**每例的输入／期望桶／实测桶**：

| kind | 输入 | 期望桶 | 实测桶 |
| --- | --- | --- | --- |
| 真例 | `assert.equal(KEYS.length, 101, 'registry 里的总键数');` | ③ unaccounted | **③ unaccounted** |
| 真例 | `assert.deepStrictEqual(KEYS.length, 101);` | ③ | **③** |
| 真例 | `assert.equal(WRITE_KEYS.length, 35);`（写键数） | ③ | **③** |
| 真例 | `assert.equal(READ_KEYS.length, 66);`（读键数） | ③ | **③** |
| 真例 | `assert.equal(WEIGHT_KEYS.length, 9);`（体重键数） | ③ | **③** |
| 假例 | `assert.equal(d.total, 101);` | ④ | **④** |
| 假例 | `assert.equal(out.data.metrics.meals, 101);` | ④ | **④** |
| 假例 | `assert.equal(r.status, 101);` | ④ | **④** |
| 假例 | `assert.equal(buildPhotoHelp().length, 101);` | ④ | **④** |
| 假例 | `assert.equal(items.length, 101);` | ④ | **④** |
| 假例 | `assert.equal(n, 101);`（裸标识符） | ④ | **④** |
| 必测① | `assert.equal(KEYS.length,28,'分组 [3] 的键数');` | ④ | **④**（`forced=`原 `/\[\s*\d/` 出口） |
| 必测② | `assert.equal(KEYS[0].length, 28);` | ④ | **④** |
| 必测③ | `assert.equal(DECLARED_KEYS.length, 28);` | ④ | **④**（`forced=`原 `DECLARED_` 出口） |
| 保留出口 | `assert.equal(x.length, 3, '应为 101 条');` | 无桶（出局） | **null** |

**已知边界（写进证据，不粉饰）**：`assert.equal(TOTAL, 28)` 这类**裸标识符对裸数字**、另一侧无线索、
消息串也不点名出处的行，判据**判不出主体** ⇒ 落 ④（并由守卫按「裸标识符」形状记 `PENDING`）。
这不是判过，是没判——与对抗审查 §二(b)「守卫识别的是名字不是事实」同一条残余风险（§9）。

## §6 变异自证（改坏一处 ⇒ 必须变红；改回 ⇒ 复绿）

变异体：`.scratch/t293v4/mutate.mjs apply|restore`（把第四版的 `forced` 分支改回第三版的 `return null`，只此一处）。
判分：`fixture.mjs --mutate` 的第零节四行。退出码链：`MUTBASE=0 → APPLY=0 → MUTATED=1 → RESTORE=0 → REBASE=0`
（`.scratch/t293v4/fx-verdict2.txt`）。

| 例 | 基线实测 | 变异后实测 | 说明 |
| --- | --- | --- | --- |
| 必测① | ④（守卫命中） | **③ unaccounted** | 变回静默出口后不再落 ④ ⇒ 夹具红 |
| 必测② | ④（守卫命中） | **③ unaccounted** | 同上 |
| 必测③ | ④（守卫命中） | **null（行消失）** | 静默出口复活，行真的没了 ⇒ 夹具红 |
| 对照假例 `d.total, 101` | ④（守卫不命中） | ④（守卫不命中） | 变异不影响它 ⇒ 证明夹具不是"整片变红" |

## §7 规则 38 修订（回滚路径必须重签印记）＋ 自证

- 落笔：`docs/agents/编排纪律.md` 的「把派生面与中间态当基线」行补上回滚路径——
  `tsc -b packages/skill-calorie --force` 只重编、**不写印记** ⇒ 必须紧跟
  `node packages/skill-calorie/scripts/gen-cli.mjs --stamp` 重签印记，否则下一窗一开就是 `GEN-STALE` 假红
  （与 `#325` 的印记假绿互为反面）。该句由编排者代提交为 `2ac92a2`（席位落笔、编排者代提交）。
- 自证（仓外沙箱，`.scratch/t293v4/r38-proof.mjs` → `r38-proof.txt`，`RESULT: ALL PASS`）：

| 步 | 命令 | exit | 报出 |
| --- | --- | --- | --- |
| ① | 基线 `--check` | 0 | `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）` |
| ② | 改一个声明源 ⇒ `--check` | 1 | `GEN-STALE FAIL`（源变了没重建，正确的红） |
| ③ | `--stamp` ⇒ `--check` | 0 | 绿（印记追上） |
| ④ | 还原那一行 ⇒ `--check` | 1 | `GEN-STALE FAIL`——**假红**：生成物与当刻源一致，只有印记停在旧内容上 |
| ⑤ | `--stamp` ⇒ `--check` | 0 | **绿（重签印记 ＝ 修法）** |

- 真仓回滚路径原样跑一遍（`.scratch/t293v4/r38.txt`）：`tsc -b packages/skill-calorie --force` **exit=0** →
  `gen-cli.mjs --stamp` **exit=0** → 印记 sha 前后同为 `751DCC8E…67A3E8`（印记是**内容**口径、不是 mtime，源没变则重签是幂等的）
  → 随后 `gen:check` **exit=0（绿）**。

## §8 门禁（全部经 `tooling/run-locked.mjs --ticket t293v4b --lock-dir .scratch/locks-land --stale-minutes 30 --`）

| 门 | 命令（经锁包装） | exit | 关键输出 |
| --- | --- | --- | --- |
| build | `node node_modules/typescript/bin/tsc -b` | **0** | — |
| gen:check | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | **0** | `GEN-CHECK PASS：键 101（写 35 ＋ 读 66）…` |
| 探针 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | **0** | `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`；`③ UNACCOUNTED 0 处`；④ 3 处（§2 分组计数）；守卫未命中 0 行 |
| 全量 | `node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"` | **0** | `ℹ tests 1492 / pass 1492 / fail 0 / skipped 0 / todo 0`（`g-suite.txt`） |

落盘：`.scratch/t293v4/g-exits.txt`＝`BUILD=0 GENCHECK=0 PROBE=0 SUITE=0`；锁审计 `.scratch/locks-land/gate-runs.log`。

## §9 边界与未做项（诚实清单）

1. **守卫仍按"名字／形状"识别**：中性命名的权威写死（如 `assert.equal(specs.length, 101, '声明条数')`）会落 ④ 而**不**记 `PENDING`
   ——对抗审查 §二(b) 已点名，属已知残余风险。真要堵它需要「域内量 vs 权威量」的事实判据（需要按域算量），
   不在本席红线内，**本次未扩面**。
2. `docs/skills/skill-calorie/t293-验收-命令自治.md` 里的快照段仍写着当时的 `P1=FAIL`／`UNACCOUNTED 1 处`（早于第四版），
   **未改**：它自称"最近一次实跑快照"，当刻已不成立，但改写别人的叙述不在本席范围；此处留档提示。
3. 本席未改任何测试文件、未动 `packages/skill-calorie/src/` 实现文件、未碰任何 issue 票面；`git status` 只剩探针一个改动文件。
4. `.scratch/t293v4/mini/`（整体副本）与 `r38sandbox/`（规则 38 沙箱）是证据件，留在 `.scratch/`（gitignore）。
   两者**曾经**各含一个 `node_modules` junction：收尾时已用 `cmd /c rmdir` **只删联接、不删目标**（删后复验
   `D:\ilife\node_modules` 仍在、`.pnpm` 仍在、两个联接位已不存在）——留着联接会让后手一句
   `Remove-Item -Recurse .scratch` 顺着联接把真 `node_modules` 删掉，这个雷不留给下一窗。

---

## §10 落点实录：本席的改动由**别人**的提交并入（内容未改）＋ 新／旧 sha256

**事实（如实记，不含褒贬）**：本席把两份件 `git add` 后、自己的 `git commit` 尚未执行时，
`#324r` 复核席的一次**未限定路径**的提交把暂存区一并收走：

| 项 | 值 |
| --- | --- |
| 收走本席两份件的提交 | `07af0e3 docs(324r): S2 维度定向复评 —— 五窗对账全绿，判定由 FAIL 转 PASS` |
| 该提交的文件面 | `t293-探针第四版-证据.md`（+200）＋ `t293-验收-命令自治.mjs`（+236 −20）＋ `t324r-复核报告.md`（+65 −20） |
| 随后该席的披露提交 | `90e7810 docs(324r): S2 复评补记 —— 披露提交扫入他席暂存件（内容未改）` |
| 当刻 `HEAD == origin/master` | `90e7810`（已 push；本席此后未再提交探针，故**没有**重复提交） |
| 内容是否被改 | **未改**：`git hash-object` 三对比对，工作树 blob 与 `HEAD` blob 逐字相等 |

**字节级自证（blob 相等 ＝ 我验过的字节就是落地的字节）**：

| 件 | 文件 sha256（我测试的那份） | git blob hash（== HEAD 里的） |
| --- | --- | --- |
| `t293-验收-命令自治.mjs` | `352CC4DEB0944848AFF8EE823AD66DE96352F43A04DF4BF7E03DE4175FA08F95` | `1b351e94fda5ab04f436f5f42caeee50e5e9ba24` |
| `t293-探针第四版-证据.md`（本节之前） | `0FF041C91B1191E80C4D1CA07F8927CD6CB59118DEA2290A6707EC588BBC3FCC` | `d93a0bffd81fec62fd2374ec600b8f7310975b40` |

**探针 sha256 新旧对照**（文件内容，非 git blob）：

| 时点 | sha256 | 来源 |
| --- | --- | --- |
| 接手前（上一席留下的未提交态） | `F9162332F068E1166B24E1C72C9152F568C9C61F9C5B8824921D08B2CA700633` | 交接简报 ＋ 本席开工时复算一致 |
| 更早的已提交版（第三版，`4023da6`） | `025AB252DC1A27AF766435557FC0E82D166DE505556E943D5AD433CD4B246DF0` | 交接简报 |
| **本席交付（第四版）** | **`352CC4DEB0944848AFF8EE823AD66DE96352F43A04DF4BF7E03DE4175FA08F95`** | 落地后的工作树复算 |

**在落地字节上复跑的门禁（第二遍，`FINAL-HEAD=90e7810`）**：`.scratch/t293v4/g2-exits.txt`＝
`BUILD=0 GENCHECK=0 PROBE=0 SUITE=0`；探针 `RESULT: P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`（`g2-probe.txt`）、
`③ UNACCOUNTED 0 处`、④ 3 处、守卫未命中 0 行；全量 `ℹ tests 1492 / pass 1492 / fail 0`（`g2-suite.txt`）。

**纪律教训（写给后手，不是追责）**：`git commit` **必须限定路径**（`git commit -- <自己的路径>`），
并在提交前用 `git diff --cached --name-only` 确认索引**只含自己的文件**——本轮已有两席因缺这一步而互相并入。

