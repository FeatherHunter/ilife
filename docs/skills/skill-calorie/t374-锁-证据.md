# t374 锁证据（场景 01 主页 9 条真正的交付出口用例）

- 票：#374（地图 #162 子票）；基线：开工 `git pull` 已是最新，HEAD `df2e07f`；提交时 HEAD `6b61ddc`（他方已落码，见 §5）。
- 结论先行：新增 `home-lock-374.test.mjs` 9/9 绿；变异 8/9 红、还原 9/9 绿；`gen:check` 绿（PASS，118 键）；视觉留 #375。
- 写集（文件级，只此两件）：`packages/skill-calorie/test/home-lock-374.test.mjs`（新建 95 行）、本件。不碰 #371／#372／#373 写集，不改装配接线。

## ① 9 条（唤醒词＝`main_prompt.cli` 逐字照 `src/triggers/scene-01-home.ts` order 0–8）

| 唤醒词 | 命令 | 参数 | 形状 |
|---|---|---|---|
| 看今日主页 | calorie.view.home | date 今日 | 完整文档 |
| 看今日饮食概览 | calorie.view.diet | window 今日 | 完整文档 |
| 看今日运动概览 | calorie.view.exercise | window 今日 | 完整文档 |
| 看今日体重概览 | calorie.view.weight | 无 | 完整文档（跨家，只验） |
| 看今日目标进度 | calorie.view.goal-progress | window 今日 | 片段（整页化归场景 06，只判跑得通） |
| 看本周主页 | calorie.view.home | windowDays 7＋date 今日 | 完整文档 |
| 看本月主页 | calorie.view.home | windowDays 30＋date 今日 | 完整文档 |
| 看连续记录天数 | calorie.view.home | date 今日 | 完整文档 |
| 看今日热量预算 | calorie.view.home | date 今日 | 完整文档 |

- 跑法：固定种子库（`t81-seed.mjs::seedFull`，`CALORIE_TODAY＝SEED_TODAY=2026-09-07`）上 spawn `dist/cli/cmd_read.js` 真跑；每条 A 档（有段出页）＋B 档（空库 exit 4 可读缺失阻断、不落盘算通）。
- 断言：① 落盘／绝对路径／`calorie_html/`／存在／`delivery.bytes`＝落盘字节／`delivery.path`＝`data.output`／stdout 单行 JSON（`help-delivery-139` 形状）；② 完整文档走 `doc-page-assert` 五连＋无残留、片段走 `<section` 形态、页上针位（`help-shell-134` 一侧）。

## ② 机器读数（持锁，日志 `.scratch/t374/`，gitignored）

- 首绿：`node --test packages/skill-calorie/test/home-lock-374.test.mjs` → tests 9／pass 9／fail 0（`green.log`，GATE-RUN runId=`71e48d5a-…` exit=0）。
- 变异红：`T374_BREAK=1`（首条加 `__MUTATED_MISSING__` 针）→ tests 9／pass 8／fail 1（`FAIL 看今日主页 页上无位：__MUTATED_MISSING__`，`mutate-red.log`，GATE-RUN runId=`f870ac72-…` exit=1）。
- 还原绿：去变量重跑 → tests 9／pass 9／fail 0（`restore-green.log`，GATE-RUN runId=`e17192dd-…` exit=0）。
- 中途两次红（已修）：路径少一级 `packages/packages…`（`ERR_MODULE_NOT_FOUND`）与误断 `data.bytes`（视图命令字节数只在 `delivery.bytes`，照 t372 口径改），均在落码前修复，不属交付缺陷。

## ③ `gen:check` 与零新增举证

- `node packages/skill-calorie/scripts/gen-cli.mjs --check` → `GEN-CHECK PASS：键 118（写 46＋读 72）；能力 10 个；未搬迁清单 0 条`（`gen-check.log`，GATE-RUN runId=`6baa5728-…` exit=0）。当刻树绿，无需“红系他方”举证。
- 零新增：本票写集含零生成器输入（未改任何 `commands.ts`／`routes.ts`／legacy／SKILL.md／HELP）；`git diff --cached --name-only` 仅自声明两件。

## ④ 持锁运行记录（对账源，边跑边补）

- GATE-RUN runId=`71e48d5a-…` cmd="node --test packages/skill-calorie/test/home-lock-374.test.mjs"（首绿 9/9 exit=0）
- GATE-RUN runId=`f870ac72-…` cmd="node --test packages/skill-calorie/test/home-lock-374.test.mjs"（T374_BREAK=1 变异 8/9 exit=1）
- GATE-RUN runId=`e17192dd-…` cmd="node --test packages/skill-calorie/test/home-lock-374.test.mjs"（还原 9/9 exit=0）
- GATE-RUN runId=`6baa5728-…` cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"（PASS exit=0）

## ⑤ 交接

- 未新开票：9 条全通，无可复现缺陷；目标进度片段形与 #173 记录一致（归场景 06），运动页 GAP 归 #372 转票，本票不重复开。
- 下一手：肉眼终审归 #375（用户亲手看结果型完整文档）；本票不关地图。
- 产物：`packages/skill-calorie/test/home-lock-374.test.mjs`、本件 `docs/skills/skill-calorie/t374-锁-证据.md`。
