# t373 接线证据（场景 01 主页 9 条工作流程接线）

- 票：#373（地图 #162 子票）；执行席已认领（assignee FeatherHunter，评论 5659171637）。
- 基线：HEAD `e28773b95261e3b3ecde611d397b9deb05c15ad2`（开工 `git pull` 已是最新；锁空确认后开工）。
- 结论先行：对账 9/9 绿（`RESULT: 9/9 ALIGN-PASS`）；真跑 18/18 绿；
  HELP 面 10/10 绿；变异 17/18 红、还原 18/18 绿；路由 scene 01＝9 条全 `exec`；
  场景源 prompt＝HELP 资产 prompt＝SKILL.md 手写段（逐字）；`scene-01-home.ts` 一行未改
  （只验不动）；`gen:check` 红系他方在途声明 drift，本票零新增（见 §5）；
  `check-examples` 118/118 绿。

## ① 基线与写集（影响清单→交付对账，偏差零）

- 票面写集：`packages/skill-calorie/SKILL.md`（只加手写段，不动 HELP-AUTO 生成块）、
  `.scratch/t373/`（3 脚本＋日志）、`docs/skills/skill-calorie/t373-接线-证据.md`（本件）。
- 老行为不许动清单：`src/home/commands.ts`、`src/home/routes.ts`、`src/weight/`、
  `src/home/` 装配、一切生成物（`keys.ts`／`registry.ts`／`routes.generated.ts`／
  combos 镜像／`build-help.mjs`）——`git diff --name-only` 本票仅 SKILL.md 1 件 src 面改动；
  `scene-01-home.ts` 经对账已逐字相同，只验不动（见 §2）。
- 前置已就绪：#371（`c9c8743`，5/5 完整文档）与 #372（8/8 核验）已落码待关；
  跨家 `calorie.view.weight`（命令住 `src/weight/`）只接线不实现（#173 裁决）。

## ② 对账表：9 行（唤醒词／命令／参数／data_source／路由 cli）逐字相同

- 跑法：`.scratch/t373/align-9.mjs`（纯数据对照，不 spawn CLI）。
  五源：场景源（逐行正则抽取，缺字段大声失败）× 路由记录 × `searchHelp` 现找 ×
  HELP 资产 prompt × SKILL.md 手写段（含入断言）。
- 机器读数：`RESULT: 9/9 ALIGN-PASS`（`.scratch/t373/align-green.log`，
  GATE-RUN runId=`5a939fd8-078e-42d9-997f-3f41483dedf2` exit=0）。
- 路由面：`WAKE_ROUTES` 中 scene 01＝9 条，kind 全 `exec`（9/9）。
- 跨家行：`看今日体重概览` → `calorie.view.weight`（`home_today_weight_overview`），
  命令与路由声明住 `src/weight/`（order 3），本图只接线、不实现、不碰别家目录。

## ③ SKILL.md 手写段与 HELP 面

- 新增节 `场景 01 主页工作流程（9 条查询命令）`：读类直出流程（说唤醒词→复制 prompt→
  AI 用命令执行→结果型 HTML 落盘、`data.output` 回绝对路径）＋ 9 行对照表 ＋
  9 条 prompt 原文围栏块（逐字同场景源 `prompt_template`，半角标点原样）。
- 本图 9 条全是查询命令，没有会改数据库的命令；过程型 HTML 0 条（记账）。
- HELP 面脚本 `.scratch/t373/help-9.mjs`：逐词 `calorie.help.lookup` 落盘断言
  （唤醒词＋转义后 cli＋`<pre>`＋复制按钮位＋`ilife:calorie:help`）9/9，
  主 HELP 文件含 9 条 prompt 原文 1/1 → `RESULT: 10/10 PASS`
  （`.scratch/t373/help9-green.log`，GATE-RUN runId=`cc93ec74-…` exit=0；
  落盘例 `卡路里_HELP_20260914_130202.html`，tmp 隔离）。
  注：lookup 页内 cli 经 base-paint 五字符转义（`"`→`&quot;`、`'`→`&#39;`），
  断言用同映射转义后比对（初版直比曾 2/10 红，修后 10/10，见同目录 `help-9.mjs` `esc`）。
- 真跑脚本 `.scratch/t373/run-9.mjs`（照抄 t372 口径：A 档种子库出页／B 档空库
  exit 4 阻断不落盘也算跑通；目标进度为片段形，只判跑得通）：
  `RESULT: 18/18 PASS`（`.scratch/t373/run9-green.log`，
  GATE-RUN runId=`a6d33a50-…` exit=0）。

## ④ 变异自证（T373_BREAK=1，环境变量只改针，不碰工作区）

- 变异红：`RESULT: 17/18 FAIL`（唯一红行 `FAIL A档 看今日主页 ::
  页上无位：__MUTATED_MISSING__`，`.scratch/t373/run9-mutate-red.log`，
  GATE-RUN runId=`40191780-…` exit=1）。
- 还原绿：去变量重跑 `RESULT: 18/18 PASS`（`.scratch/t373/run9-restore-green.log`，
  GATE-RUN runId=`e32d6d4a-…` exit=0）。

## ⑤ 他方致红事件与 `gen:check` 零新增举证

- 他方（#333 窗口）在途改 `src/triggers/scene-03-weight.ts`（体重复盘 6 行）时，
  cli 串内双引号一度未转义，`tsc` 带病发射致 `dist` 坏件，全图 CLI 启动即崩
  （现场 `.scratch/t373/routing-81.log`，GATE-RUN runId=`9a25b14a-…` exit=1）。
  按并发协议 §2.1.5 停手等待；他方修复重建后树绿，本票对账脚本因只导入本图单模块
  （不走 triggers 聚合），绿读数全程不受影响。
- `gen --check` 红因举证（`.scratch/t373/gen-check2.log`，
  GATE-RUN runId=`d1716188-…` exit=1）：4 件生成物与生成器输出不一致
  （`keys.ts`／`combos.yaml`／`build-help.mjs` REPR／`routes.generated.ts`
  scene-03 行），`registry.ts` 为 ok；红因全系他方在途声明
  （118 条命令含 `exercise-records` 等新增；`routes.generated.ts` 第 143 行起 scene-03 行）。
  零新增三条：① 本票写集含零生成器输入（未改任何 `commands.ts`／`routes.ts`／legacy；
  `git diff --name-only` 本票 src 面仅 SKILL.md，且 §⑨ 证其 AUTO 块与 HEAD 逐字节一致）；
  ② 红行点名的文件全在本票写集之外；③ 10 个能力目录中 `home` 干净。
  待树绿（他方提交＋重生成）后重跑即应绿。
- `routing-81`（`.scratch/t373/routing-81-green.log`，
  GATE-RUN runId=`7679a8e1-…`）：4 pass／4 fail，fail 全系他方场景 drift
  （exec 397 vs 390、总数 466 vs 459、out-of-scope 表差两条拍营养表词，均属场景 02/05 面）；
  场景 01 零卷入（路由 scene-01 行在 `git diff` 中零 hunk，对账 §2 9/9 绿）。
- `help:examples:check`（`.scratch/t373/examples-check.log`，
  GATE-RUN runId=`c67bbaf8-…` exit=0）：`RESULT: 118/118`，
  `PASS: SKILL.md 示例逐行可执行`——本票 SKILL.md 改动（纯手写段）未伤 AUTO 新鲜度。

## ⑥ 持锁运行记录（对账源，GATE-RUN 边跑边补）

- GATE-RUN runId=`6ce6da74-…` cmd="node .scratch/t373/align-9.mjs"（手写段缺失版，0/9 红，负向基线）
- GATE-RUN runId=`416f8597-…` cmd="node .scratch/t373/align-9.mjs"（标点修复后，9/9 绿）
- GATE-RUN runId=`5a939fd8-…` cmd="node .scratch/t373/align-9.mjs"（固化 `align-green.log`，9/9 绿）
- GATE-RUN runId=`541d265a-…` cmd="node .scratch/t373/align-9.mjs"（AUTO 块恢复 HEAD 后复验，`align-final.log`，9/9 绿）
- GATE-RUN runId=`a6d33a50-…` cmd="node .scratch/t373/run-9.mjs"（真跑，`run9-green.log`，18/18 绿）
- GATE-RUN runId=`cc93ec74-…` cmd="node .scratch/t373/help-9.mjs"（HELP 面，`help9-green.log`，10/10 绿）
- GATE-RUN runId=`40191780-…` cmd="node .scratch/t373/run-9.mjs"（T373_BREAK=1 变异，`run9-mutate-red.log`，17/18 红）
- GATE-RUN runId=`e32d6d4a-…` cmd="node .scratch/t373/run-9.mjs"（还原，`run9-restore-green.log`，18/18 绿）
- GATE-RUN runId=`9a25b14a-…` cmd="node --test test/calorie-routing-81.test.mjs"（他方坏件致崩现场，`routing-81.log`）
- GATE-RUN runId=`7679a8e1-…` cmd="node --test test/calorie-routing-81.test.mjs"（树绿后，`routing-81-green.log`，4 pass＋他方 drift 4 fail）
- GATE-RUN runId=`0d8522fc-…` cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"（`gen-check.log`，GEN-STALE 红，零新增）
- GATE-RUN runId=`d1716188-…` cmd="node packages/skill-calorie/scripts/gen-cli.mjs --check"（`gen-check2.log`，GEN-CHECK 红，零新增）
- GATE-RUN runId=`c67bbaf8-…` cmd="node packages/skill-calorie/scripts/check-examples.mjs"（`examples-check.log`，118/118 绿）

## ⑦ 遗留与交接

- 未新开票：`calorie.help.lookup` 的声明 `wakeWord` 占用 `看今日主页`
  （`src/analysis/commands.ts:321`），致 SKILL.md 速查表该词两行
  （`help.lookup` 与 `view.home`）——改声明超出本票禁区，记账在此，
  由编排者裁定是否另开票（候选归属：analysis 侧声明词）。
- 下一手：树绿后持锁重跑 run-9／help-9／变异对＋`gen:check`，把 §6 待补行补齐；
  肉眼终审归 #375（用户亲手看结果型完整文档）；本票不关地图。

## ⑧ 产物路径

- `.scratch/t373/align-9.mjs`（对账）、`.scratch/t373/run-9.mjs`（真跑）、
  `.scratch/t373/help-9.mjs`（HELP 面）、`.scratch/t373/dump-prompts.mjs`／
  `splice-auto.mjs`（一次性探针与 AUTO 块恢复），
  `.scratch/t373/align-green.log`、`align-final.log`、`run9-green.log`、
  `run9-mutate-red.log`、`run9-restore-green.log`、`help9-green.log`、
  `gen-check.log`、`gen-check2.log`、`routing-81.log`、`routing-81-green.log`、
  `examples-check.log`（现场）。
- 本件 `docs/skills/skill-calorie/t373-接线-证据.md`。
- 改动：`packages/skill-calorie/SKILL.md`（只加手写段 1 节，＋91 行；AUTO 块与 HEAD 逐字节一致）。
