# t330 独立复核报告（#330 复核席 t330r）

席位：#330 独立对抗式复核席（只复核这一张票）。工作区 `D:\ilife`，分支 `master`。
被审对象：提交 `29a7762`（限定 10 路径，见第八节）。

## 零 · 基线（开工自复算）

- 开工当刻：`HEAD == origin/master == 29a7762e831c79d1072b8cffe013b5539e38fe2f`。
- 树不干净：`M packages/skill-calorie/scripts/gen-cli.mjs`、`M packages/skill-calorie/scripts/gen-routes.mjs`（他席 t325 在飞），`?? docs/skills/skill-calorie/t329r-复核报告.md`。本席全程只读这些文件，未碰。
- 收工时：`HEAD` 已推进到 `8b6be35`（t329r 报告提交），`origin/master` 到 `9757617`（t325 配对门）。复核结论不受影响：
  `git diff 29a7762 --stat -- packages/skill-calorie/src packages/skill-calorie/test` 为空（被审 src/test 零漂移）；
  工作树脏的两个 gen 脚本经核对与 `9757617` 逐字节一致（即 t325 新版门，非被审当刻版本，见第四节记账）。

## 一 · 行为等价（全部本席独立计算，不采信被审文本）

| # | 搬迁 | 独立对照结果 |
| --- | --- | --- |
| 1 | `writeSuffixFor`：`src/output.ts` → `src/shared/contentSuffix.ts` | 函数体逐字节相等，`sha256 = 8b36975000e9…8b0de`（搬前＝搬后）；jsdoc＋函数体 107 行亦逐字节相等（`fe9f6844e5c…da4f`）；`case` 数 21＝21。调用点 `deliverHtml` 只改引用（`output.ts:257`），另加 import＋再导出两行。 |
| 2 | `profileReceiptDoc`：`src/cli/write.ts` → `src/profile/receipt.ts`（经 `src/profile/index.ts` 门转出） | 两具名键集逐字节相等；函数体除 `export` 关键字外逐字节相等（旧非导出→新导出）。门 5→4：去 `buildProfileSettingReceiptDoc`／`buildProfileUpdateReceiptDoc` 两转出，加 `profileReceiptDoc`，剩 `PROFILE_COMMANDS`／`profileReceiptDoc`／`runProfileView`／`runProfileWrite`（4 件，≤5）。旧门两导出在仓内代码已零引用（仅剩 `setup.ts`／`update.ts` 定义处与 `receipt.ts` 直引，定义本身保留）。 |
| 3 | `cmd_read.ts` 340→135（`readArgs.ts` 53＋`delivery.ts` 173） | `toast`／`preflight`／`parseArgs`／`parseReadArgs`／`USAGE`／`DEFAULT_TIMEOUT_MS`／`ReadArgs` 逐字节照搬进 `readArgs.ts`；`assertEnvelopeData`／`buildEnvelope`／`dataTextOf`／`describeDeliveryTarget`／`failWithReceipt` 逐字节照搬进 `delivery.ts`；`dispatch`／`main` 留原文件逐字节不变。唯一字差：`buildDeliveredEnvelope` 入参 `out: DispatchOut` → `out: ViewOut`，而 `DispatchOut = ViewOut`（同文件别名），类型同一、零运行时影响。入口名与 `export function dispatch` 未挪；`buildDeliveredEnvelope` 经原路径再导出（`cmd_read.ts:34`）。 |
| 4 | `case 'calorie.` 计数 | `src/cli/write.ts` 与 `src/output.ts` 经检索均为 0（`dispatchInner` 只剩 `default` 兜底）。旧导入面 `dist/output.js → writeSuffixFor` 经再导出仍可用（`output-naming-119.test.mjs:19-28` 现行取用，全量绿为证）。 |

复现命令（均在仓根，`29a7762^` 为搬前）：

```text
git diff 29a7762^ 29a7762 -- packages/skill-calorie/src/cli/write.ts packages/skill-calorie/src/output.ts packages/skill-calorie/src/profile/index.ts
python .scratch/t330r/hash_cmdread.py   # 各函数搬前搬后对照（唯一 DIFF 为 DispatchOut→ViewOut 同义改名）
node --test packages/skill-calorie/test/output-naming-119.test.mjs  # 标识段逐键行为锁
```

## 二 · 行数与入口（`29a7762` blob 实测，与被审自报逐项一致）

| 文件 | 搬前 | 搬后 | 公开件数（搬后） |
| --- | --- | --- | --- |
| `src/cli/cmd_read.ts` | 340 | 135 | 4（`dispatch`、`buildDeliveredEnvelope` 再导出、`DispatchOut`、`DeliveryKind`，面不变） |
| `src/cli/readArgs.ts`（新） | — | 53 | 4（`toast`／`preflight`／`parseReadArgs`／`USAGE`） |
| `src/cli/delivery.ts`（新） | — | 173 | 3（`buildDeliveredEnvelope`／`describeDeliveryTarget`／`failWithReceipt`） |
| `src/shared/contentSuffix.ts`（新） | — | 118 | 1（`writeSuffixFor`） |
| `src/profile/receipt.ts`（新） | — | 30 | 1（`profileReceiptDoc`） |
| `src/cli/write.ts` | 104 | 76 | — |
| `src/output.ts` | 416 | 313 | — |
| `src/profile/index.ts` | 41 | 41 | 5→4（见一·2） |

## 三 · 口径文档判定（`docs/agents/命令事实边界.md`，新建 34 行）

- 自洽：边界三问与两处归属裁定一一对应（单一能力用→能力目录收 `profileReceiptDoc`；六能力共用＋纯派生→共享件收 `writeSuffixFor`，共享件头已写清六能力，同 `shared/writeParts.ts` 例）。
  “非目标”段诚实声明两处不是 #320 字面的分派分支（同族欠账，不回头改 #320 措辞），与 t293 对抗审查 §七 Q2／Q4（最省修法与转票判据）原文相符，无空引。
- 未放宽命令登记纪律：Q3“生成物以 `pnpm gen:check` 为准、手改即红”仍是收紧口径；全文无新增手改生成物的许可。

## 四 · 复跑（持锁 `run-locked --ticket 330r --lock-dir .scratch/locks-land --stale-minutes 30`，direct node）

| 门 | 命令 | 读数 |
| --- | --- | --- |
| 类型 | `node node_modules/typescript/bin/tsc -b` | `exit=0` |
| 生成物 | `node packages/skill-calorie/scripts/gen-cli.mjs --check` | `exit=0`；`GEN-CHECK PASS：键 101（写 35＋读 66）；能力 10 个声明 101 条，未搬迁清单 0 条`（注：跑的是工作树 t325 v2 新版门，比被审当刻门更严，仍绿） |
| 全量 | `node --test`（根 `test` 脚本 15 组显式 glob） | `tests 1492／pass 1492／fail 0／cancelled 0／skipped 0／todo 0`，`exit=0`（`.scratch/t330r/full.log:1839-1845`） |
| 冻结 | `node --test test/calorie-routing-81.test.mjs` | `tests 8／pass 8／fail 0`，`exit=0` |
| 探针 | `node docs/skills/skill-calorie/t293-验收-命令自治.mjs` | `P1=PASS P2=PASS P3=PASS P4=PASS P5=PASS 5/5`，`UNACCOUNTED=0 处`，`exit=0` |

## 五 · 变异自证复现（本席手写 key，不复用被审 key）

- 塞：`src/cli/write.ts` 的 `dispatchInner` 加三行 `case 'calorie.t330r-mut'` → `node --test packages/skill-calorie/test/cmd-registry-294.test.mjs`：`tests 7／pass 6／fail 1`，`exit=1`，失败签名 `分派层仍有按键分派（#320 起老路应无活口）：calorie.t330r-mut`（`test:157` 终态断言）。
- 删：回退该三行（`git diff` 该文件为空，`hash-object` 与 HEAD blob `5859181…` 一致，零残留）→ 同件 `tests 7／pass 7／fail 0`，`exit=0`。

## 六 · S 清单与打分（100 制，PASS 要求 S1=0 且 S2=0）

- S1（阻断正确性）：0 条。搬迁逐字节等价、调用点只改引用、旧导入面可用、五门全绿、变异门鉴别力复现。
- S2（严重）：0 条。
- S3-1（证据口径瑕疵，-2）：被审自报“函数体 `sha256 = 21402c26…`（搬前＝搬后）”本席四种口径（函数体／体＋jsdoc／内部体／CRLF）均无法复现；实测函数体 `8b369750…` 搬前＝搬后（逐字节相等，结论成立且证据更强）。复现：`python .scratch/t330r/hash_jsdoc.py`。建议修正证据件中的 hash 口径。
- S3-2（环境记账，-2）：复跑时 `gen:check` 已是他席 t325 v2 新版门（非被审当刻版本），读数仍绿，结论只强不弱；开工树不干净（他席在飞）已全程只读避让。
- 总分：**96，PASS**。

## 七 · 未做项

- 未碰他席在飞文件（两个 gen 脚本的 `M` 原样保留）；未改任何源码与测试；变异文件已回退零残留。
- 未写 issue 票面、未 close 任何票；无 `--amend`／`--force`／rebase；`dist/` 由 `tsc -b` 正常构建（gitignored，不入提交）。
- 大输出与脚本：`.scratch/t330r/`（diff×3、hash×3、run/log 工具、full/routing/t293 日志），不入提交。

## 八 · 提交（限定路径）

本报告单文件：`docs/skills/skill-calorie/t330r-复核报告.md`。
