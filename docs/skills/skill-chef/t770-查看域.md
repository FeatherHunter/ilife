# #770 查看域：8 卡读数 ＋ 变异自证 ＋ 审查结论

菜：辣椒炒肉（副本 `.scratch/t770/chef_data.db`，真库只读、mtime 未动）。命令面逐字节不变（`chef.recipe.view`，既有 view 闭环／契约／fetch／render／policy 测试全绿）。

## 8 行读数（正例 exit 0，缺卡 0）

`node tooling/run-locked.mjs --ticket 770 -- node docs/skills/skill-chef/t770-run-view.mjs`

- view_full_recipe → exit=0 → `.scratch/t770/pages/view_full_recipe.html`（103347 B，全 14 格减替换节：11 味三列＋切配＋6 步＋真营养＋真背景＋历史＋页脚）
- view_for_beginner → exit=0 → 新手关键成功点置顶（6 步验收逐条）＋全量节
- view_recipe_with_substitution → exit=0 → 替换对照 11 行（库内全标无替代品，照印＋口径行，不落库）
- view_ingredients_only → exit=0 → 只留食材节（锚点 `section-ingredients` 与全量页逐字同）
- view_ingredients_grouped → exit=0 → 按类分组（蔬菜 2／肉类 1／调料 6／葱姜蒜 2）
- view_steps_only → exit=0 → 只留步骤节（火候＋时长＋锅温＋验收俱全）
- view_nutrition_only → exit=0 → 真营养表（530 千卡／蛋白 25 克／脂肪 45 克／碳水 8 克／纤维 4 克／钠 850 毫克，每 250g）
- view_background_only → exit=0 → 真背景三段（来历＋历史＋文化）

## 变异自证（反例必跑）

`--mutate view_nutrition_only`（删菜改坏数据源）→ `view_nutrition_only → exit=1 → 无此菜谱：辣椒炒肉`（点名该卡，无空页冒充；判据通过）。

## 取数面（配方 §4 四条，本票领的全到）

营养 `nutrition_info` 1 行真数（旧占位 `estimated:false` 已换真行，无行仍显式占位不编数）／背景 `background_knowledge` 1 行／徽标六表（菜系 1／口味 2／季节 4／餐次 2／饮食标签 2／烹法 2）／替换 11 行全无替代品（全量页不印、专卡照印＋口径）。

## 质量门（t768-质量门.mjs，8 页）

静态＋真浏览器双端（390／1280）六列全绿：溢出 0／触摸目标全 ≥44px／触屏三件齐／分隔符 0／英文裸词 0／重复句 0。途中抓到两处真问题当场改（分隔符 ；→ 拆行；标题复读 → docTitle 错开）。

## 代码层 UI 审查清单（票 3 交的五条，逐条结论）

1. 硬编码样式：活色值 28、字号 20 档——全部来自公共层共享样式表，本页补丁 0 色值（`extraCss` 只拼 `pageUiCss()＋pageShapeCss()`，零自写样式）。
2. 内联样式：1 处（读数卡进度条 `width:80%`，公共层读数卡按数据产出，与 t768 同口径；页面自己 0 处）。
3. 字号与间距标尺：20 档全在公共层档位里，无自造（含正文段落件 #860 的 15px／1.7）。
4. 组件复用：非 `ilife-` 类名 1 个（`wrap`，公共层文档壳类，与 t768 同）；无自造组件类。
5. 重复样式块：产物样式块 1，无复制段（高频 51 条是公共层内部复用）。

## vision 审查（过程性，缺陷已清）

见 `t770-vision.json`：双端截图 `.scratch/t770/shot-390-final.png`／`shot-1280-final.png`；2 条成立已改（事实条单列→三列；分隔符／标题复读），1 条不成立（徽章裁切：CDP 实量 13 枚全可见，误读）。

## 未关票原因与干扰声明（并行窗口实测）

- 本票内判据全绿时（70／70）有过一次；随后别席（#772 做菜域、relation／setup 域）在途改动使 `cooking steps + shopping 合并` 1 项与 `pnpm gen:check` 变红（缺 `chef.relation.query`／`chef.setup.init`）。皆非本票路径（本票只碰 `src/view/`＋本票文档），未碰别席文件。
- 下一步：待别席落定后复跑 `node --test` 与 `pnpm gen:check` 全绿即关票（届时 map Decisions 追加一行）。
