# t873 采购＋开始使用席：两页各升 3 分与 1 分（83→**86**／84→**85**），孤卡与列头两条扣分项已销，**均未达 ≥90**

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**采购＋开始使用席**（第 35 格 ＋ 第 45 格）。
量尺＝`t873-派单册.md` §二 的冻结问句（逐字、每页一次调用两张图、不带 `json: true`）。

**一句话结论**：第 35 格把「四条读数画两遍 ＋ 读数卡网格在 ≤640 档排出的孤卡」收成**一条读数带**（四条读数、一行、格间发丝线），
第 45 格把「业务表／状态／方式」从一行散字收成**读数带**（标签 12px／数值 17px、差 5px），两页各加一枚页头图标位与暖色标记；
冻结尺读数 **83 → 86** 与 **84 → 85**。**两页都没到 90**，差距与原因见 §五。

---

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `src/shopping/pages.ts:141-149` | `renderKpiGrid` 三张读数卡（菜数／食材／分组）＋ 3 条事实条（菜数／食材／份量）——同一组读数画两遍，且 ≤640 档公共层把读数卡栅格写成两列 ⇒ 奇数张必落一张孤卡 | 撤 `renderKpiGrid`；读数收进**一条 4 格读数带**（菜数／食材／分组／份量，`extraClass: 'ilife-chef-deck'`） | 第 35 格「四张统计卡 390 端塌成两行 ＋ 孤行『4 组』」＋ 同源的复读 |
| `src/shopping/pages.ts:53-124`（新 `shoppingPageCss()`） | 无页内样式 | 读数带面板（暖色描边＋渐变底＋浅影）、格间**发丝线**分区、标签 12px（`letter-spacing:.06em`）／数值 17px·700、桌面档格内居中；页头图标位（采购袋，`data:` SVG，纯装饰） | 「缺形状件（分隔、分组、图标位）」＋「色彩锚点少」 |
| `src/shopping/pages.ts:153` | 四个分组全部折叠（首屏只有四张空壳条） | **第一组默认展开**（首屏看得见这一单到底买什么） | 冻结尺复评原话「四张分类卡只显示标题不展开内容属冗余壳」 |
| `src/shopping/pages.ts:155-168` | 分组表带 `caption`（＝分组名，与折叠条标题「葱姜蒜（2味）」重复）＋ 每行都画「来自哪道菜」（一道菜时每行同值） | 去表注；「来自哪道菜」**只在多道菜合并时**成一列 | 冻结尺复评原话「『葱姜蒜』标题与组内表格首行重复…让精炼项明显丢分」（文案精炼 14 → 17） |
| `src/shopping/pages.ts:140/179/183-188` | 结论条「清单已按两倍份量备齐，直接照单采购。」／口径行「库存核对走调用契约：复制清单找居家管家核对，本页不直连他库。」／按钮「复制清单去核库存」 | 「已按〈份量〉算好，照着买就行。」／「家里的库存交给居家管家核对。」／「复制清单核对库存」 | 文案精炼（去实现语「调用契约」「本页不直连他库」，去结论与按钮的同义复述） |
| `src/setup/pages.ts:127-134` | 事实条三条散字（业务表／状态／方式），标签与值只差 3px | 同一套读数带（`ilife-chef-deck`）：业务表／状态／**建库时机**（值「用到时才建」），标签 12px／数值 17px | 第 45 格「『业务表／状态／方式』表头与正文同号、难辨列」 |
| `src/setup/pages.ts:53-124`（新 `setupPageCss()`） | 无页内样式 | 读数带面板 ＋ 页头图标位（汤锅）＋ 开合标记换暖色圆底（公共层的蓝 `▸` 落在圆底上读起来像播放键） | 「缺形状件」「图标位」；冻结尺复评原话「步骤行英文 play 图标裸用」 |
| `src/setup/pages.ts:126/136/140/144/147/155` | 结论条「菜谱库已就绪，空库直接录第一道菜即上手。」／三段折叠正文带「装前命令」「缺表则补齐，表齐则跳过，老库仅提示迁移不自动迁移」／口径行「只在需要时建库建目录，已齐则只读不写。」 | 「菜谱库已就绪，录第一道菜就能开工。」／「读写环境正常，有缺项会在这里列出来，并给出安装方法。」「缺的会补上，齐的直接跳过，旧数据只提醒，不自动搬动。」「初始化只做一次，以后每次进来都不会重复建。」／「用到时才建库建目录，建好以后不再改动文件。」 | 文案精炼（#871 未清完的实现语残留：装前命令／迁移／只读不写；口径行与读数带「建库时机」的复读一并收口） |
| `src/setup/run-init.ts:18,20` | 回执「首次使用已就绪：本次建齐17张表（空库直接录第一道菜即上手）」／「菜谱库已就绪：17张表齐全，跳过建库（老库仅提示迁移，不自动迁移）」 | 「首次使用已就绪：本次建齐 17 张表，录第一道菜就能开工」／「菜谱库已就绪：17 张表齐全，不用再建（旧数据只提醒，不自动搬动）」 | 同上（回执是用户可见面上屏，`t778-终审扫描.mjs` 扫不到这一条，按 §七 用词纪律手工收） |

**页内样式与公共层的关系**：两页仍只调 `chefSceneCss()`，页内样式**追加在它后面**（`extraCss: chefSceneCss() + LF + <本页>PageCss()`），
没有把公共层两层拆回去（`test/skin-873.test.mjs` 第 ③ 组断言覆盖这两个文件，19/19 绿）。

## 二 逐页销账（两行读数）

读数面＝`.scratch/t873-采购/shots/01-shopping_generate-{390v,1280v}.png` 与
`.scratch/t873-开始使用/shots/01-first_use-{390v,1280v}.png`（`t873-席跑.mjs` 出的真渲染首屏）。
改前＝`t873-开工基线.md` §一（★ 该基线是**皮肤之前**的产品；本席另记一列「改前（皮肤后·同批复跑）」，见 §二.1）。

| 格 | 页名 | 改前基线（§一） | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|
| 35 | 生成清单 | 18/18/17/14/16 = **83** | 17/18/17/**16**/**18** = **86** | ①「四张统计卡 390 端塌成两行 ＋ 孤行『4 组』」→ 四条读数收进一条读数带、读数卡网格下线，390 端实测**一行四格**、无孤卡 ⇒ 视觉生动 14→**16**、风格一致 16→**18**；②同源复读（菜数／食材同值画两遍）→ 只画一遍；③分组空壳 → 首组默认展开；④「来自哪道菜」列与分组表注 → 多道菜时才出列、去表注 ⇒ 文案精炼 17→17（反例里这三处改回去立刻掉到 14，见 §三）。**总分 83 → 86（+3）** |
| 45 | 首次使用 | 18/17/16/15/18 = **84** | 18/18/16/**16**/17 = **85** | ①「『业务表／状态／方式』表头与正文同号、难辨列」→ 读数带 + 标签 12px／数值 17px（差 5px）＋ 格间发丝线 ⇒ 双端自适应 17→**18**、视觉生动 15→**16**；②「步骤行英文 play 图标裸用」→ 开合标记换暖色圆底（字形不显）；③实现语残留（装前命令／迁移／只读不写／按需建库）→ 全部换成人话（`pages.ts` ＋ `run-init.ts`）。**总分 84 → 85（+1）** |

### 二.1 「改前（皮肤后）」这一列，以及为什么没拿它当销账基线

公共层席交付皮肤后，本席先跑了一轮**未改源码**的同批复跑（`GATE-RUN runId=01b69b7d…`／`550858da…`），
但**没有当场把那一批图复评**（第二轮回跑把 `shots/` 覆盖掉了，图上只剩最终态）。照实记：**这一列读数缺失，不补造**。
销账一律以 `t873-开工基线.md` §一 为改前口径（派单册 §四.1 的指定口径）。

### 二.2 冻结尺的两条器具事实（两次独立复评）

- **同一对图 ＋ 逐字同一问句 ⇒ 逐字同一个 JSON**：第 45 格最终态连跑两次独立复评，两次都是
  `{"信息层级":18,"双端自适应":18,"文案精炼":16,"视觉生动":16,"风格一致":17,"total":85,"一句话":"层级与卡片化清晰…"}`
  （逐字相同）；同一图连跑两次都返回同一句话。⇒ **读数不含采样抖动**，页与页之间的差是图与图之间的差。
- **本席两次中间版本的读数（留档，不进销账）**：第 45 格「三步桌面横排」一版 **76**；改回竖排 **85**；第 35 格带分组表注一版 **83**、去掉后 **86**。

## 三 反例（改坏必红／还原一致）

反例都在**持锁窗口内**做（`seat.ps1` 自带编译 ＋ 重出 ＋ 出图），红读数与绿读数都是 §二 的同一把冻结问句。

| 步 | 页 | 改回哪一处 | 读数 | 判 |
|---|---|---|---|---|
| 红 | 35 生成清单 | 把「分组名表注 ＋『来自哪道菜』列」改回去（＝复读那一处） | 18/17/**14**/16/18 = **83**（文案精炼 17→14） | **红**（低于正式版 86） |
| 绿 | 35 生成清单 | 逐字还原 | 17/18/17/16/18 = **86**；产物 sha256 `b0aca78ac968f4109719e659db1288660e35a94032a5a452bfa9c072059f82ec`（与正式版**逐字节相同**） | **绿** |
| 红 | 45 首次使用 | 把「三步竖排」改成「桌面三张并排」 | 17/18/16/15/17 = **83** | **红**（低于正式版 85） |
| 绿 | 45 首次使用 | 逐字还原 | 18/18/16/16/17 = **85**（与反例前逐字相同）；产物 sha256 `f1395124e75d13da94153ce672f363afb26ffaf84daee3f8d721d5670d13aa05`（逐字节相同） | **绿** |

（反例批的 `shots/` 与正式批共用同一个落点，还原那一轮把它覆盖回正式态 —— 图上只剩正式态；
四条读数、四个 runId 与两次 sha256 对账都在上面逐条给出，反例批不提交。）

**一条阴性读数（照实记，不当成功销账）**：第 35 格把**读数卡网格原样改回去**（＝ #778 记档的孤卡缺陷回来：
该批产物 102877 字节、标记面含 `block-kpi-card-grid`、`.scratch/t873-采购/反例.log` 记 `runId=438d298e-a04e-443e-be15-96e833682cf8`，
本席当场看那一批的 390 首屏图，图上确有一张孤立的「分组 4 组」卡）
→ 冻结尺给 **87**，**没有变红**（比正式版 86 还高 1）。⇒ **这把尺对「孤卡／复读」这一类缺陷判不出来**
（判词反而说「卡片与统计块层级清晰」）。本席第 35 格的销账因此不靠这条下降，靠的是
「同一批图里孤卡消失 ＋ 机器六列全绿 ＋ 组内不再画两遍」三件可复核的图证与机证。这条器具账请收口席一并处置。
（该反例批的 `shots/` 已被后续迭代覆盖，反例批本身不提交，只留本条读数与 runId 对账。）

## 四 机器面

### 4.1 门禁运行记录（§2.4 第 2 条：一次运行一行，runId 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=01b69b7d-74b3-4dc5-bacb-9d246260f5a3 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=550858da-a803-4ef8-932f-4559d2f0d801 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 开始使用" exit=0
GATE-RUN runId=d6995408-3af9-469a-a9db-fbcf87a711a1 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=259cee66-0be5-48aa-b86a-6ca63cb7f1d0 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=fa379c45-6df0-4c72-8e24-99bff7df1bb6 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 开始使用" exit=0
GATE-RUN runId=16ddd81e-fa22-4067-be4d-798ced0565dd cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 开始使用" exit=0
GATE-RUN runId=438d298e-a04e-443e-be15-96e833682cf8 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=99b1055f-59e5-47aa-a106-198b2e6b5d60 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=35238d2a-c0e8-4574-8893-d32f994f6246 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 开始使用" exit=0
GATE-RUN runId=d839d94b-d046-415b-8bea-96996e1b60f5 cmd="node --test packages/skill-chef/test/skin-873.test.mjs" exit=0
```

（`seat.ps1` 一个窗口里做三件事：`tsc -b packages/skill-chef` → `t873-席跑.mjs <域>`（跑域驱动器／切本域页／出双端首屏图／跑 `t768-质量门.mjs`）。
其余 runId 是本席迭代过程中的中间版本轮次，按 §2.4 第 3 条不计入对账。）

### 4.2 编译与包内测试

- 编译：`node node_modules/typescript/bin/tsc -b packages/skill-chef` → **exit=0**（末轮 runId=d6995408…／fa379c45… 的窗口内）。
- 包内测试 `skin-873.test.mjs`：`tests 19 / pass 19 / fail 0`，exit=0（含「10 处壳只调 `chefSceneCss()`」——覆盖本席两个文件）。
- 包内全量 `node --test "packages/skill-chef/test/*.test.mjs"`：`tests 103 / pass 101 / fail 2`，**两条红都在别席在途写集**——
  「录入·成功回执 页面无工单黑话／实现语」命中 `src/add/pages.ts`、「数据管理体检页…不出现读数卡网格」命中 `src/data/pages.ts`；
  本席写集（`src/shopping/**`、`src/setup/**`）零红。

### 4.3 质量门（`t768-质量门.mjs`，六列，390／1280 两档）

```
采购   汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 1 行）   exit=0
开始使用 汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 1 行）   exit=0
```

### 4.4 终审扫描

```
.scratch/t873-采购     扫 1 件产物；三类字样命中 0 处 -> 干净     exit=0
.scratch/t873-开始使用 扫 1 件产物；三类字样命中 0 处 -> 干净     exit=0
```

### 4.5 本域逐件点名

- 采购域 4 件同一份产物：`shopping_generate.html` ＋ 墙上的三个同源变体（`--查清单`／`--清空清单`／`--排除可选`，由 `t777-run-all.mjs` 复制宿主内容），本席改一处处处生效。
- 开始使用域 1 件：`first_use.html`。

### 4.6 结构纪律

- 落点 LF 实测：`src/shopping/pages.ts` **185**｜`src/setup/pages.ts` **156**｜`src/setup/run-init.ts` **17**，均在告警线 350 以内。
- 第一步说会碰的 = 实际碰的：`src/shopping/pages.ts`、`src/setup/pages.ts`、`src/setup/run-init.ts` ＋ 本证据件。**偏差 0**，未碰白名单外任何路径。

## 五 未做项与下一手缺什么

1. **两页都没到 ≥90**，这是如实结论：第 35 格 83 → **86**、第 45 格 84 → **85**，各差 4 分与 5 分。
   两页每一维都已在 16–18；卡住的分集中在**视觉生动 16**（尺子的原话是「缺插画／缺微动效／视觉同质化」）与**文案精炼 16–17**
   （尺子仍能找出「录第一道菜／看全部菜谱／复制上手说明」这类同族动作词的语义重叠）。
   页内能做的形状与色彩已到顶：公共层席自己的结论同此（`t873-席公共层.md` §五.2「CSS 只能把色彩锚点与形状件补到够，再加就只剩图」）。
2. **要不要出图**：这是量尺口径之外的一条决定（技能侧要不要给菜品图位），本席无权定 → 建议收口席并入「图从哪来」那一票。
3. **器具账（重要，请收口席处置）**：§三 的阴性读数说明这把尺**判不出孤卡与复读**——第 35 格把缺陷原样改回去反而得 87。
   若别的席也出现「改前改后同分」，先看是不是这一类；冻结尺的六维对**版式位**的识别力需要一条独立读数来校准。
4. **越界报备**：无。本席只写 `src/shopping/**`、`src/setup/**`、本证据件；临时产物只在 `.scratch/t873-采购/`、`.scratch/t873-开始使用/`。
5. **数据侧受限**：无（两页都不读库里的长正文，没有「连写无句读」需要排版层断句的段落）。
6. **`git add` 只加白名单**（提交前 `git diff --cached --name-only` 复核过：只有 3 个源码件 ＋ 本证据件）。

---

# 第二轮（收口席定向返修：目标两页 ≥90）

收口席同尺复评点名：**第 35 格是降幅最大的一页（同尺改前 87 → 改后 79）**，第 45 格 86；两页的原话见
`t873-逐格缺陷清单.md` 第 35／45 行。本节记这一轮的改动、逐跑读数与三行对照（87／79／本轮 X）。

## 六 第 35 格的退步：查到了哪一处

**查法**：把上一轮那三个改动**逐个单独改回去**再复评（同尺、同调用形状）。

| 单独改回的那一处 | 读数 | 判 |
|---|---|---|
| 读数带（一张通栏面板）→ 改回**读数卡栅格** | 85 → **88**（再跑 **89**） | **就是它**：上一轮把公共层那三张读数卡换成了自己拼的读数带，页面的「视觉体量」小了一档 |
| 「分组表注 ＋ 来自哪道菜列」改回去 | **83**（文案精炼 17→14） | 不是它，这一处留着（改回去反而掉） |
| 「第一组默认展开」改回全合 | **79** | 不是它，这一组留着（改回去也掉） |

⇒ 结论：**退步出在「撤读数卡、改读数带」这一处**。本轮把它改回读数卡，并且为了不再落进 #778 记的孤卡账，
**按偶数张开卡（4 张）**：≤640 档公共层那条两列栅格正好排满两行、没有空位（#871 §四 E 的账是「奇数张必落孤卡」）。
「四张统计卡 390 端塌成两行」这一条随之从「塌」变成「排满两行」——这是本席对 #778 那一条的处置口径，如实记在这里。

## 七 第二轮改了哪几处

| 落点 | 改前（第一轮交付） | 改后（第二轮） | 依据 |
|---|---|---|---|
| `src/shopping/pages.ts:120-125` | 四条读数走自拼的「读数带」（一张通栏面板＋发丝线） | **四条读数改回公共层读数卡栅格**（4 张＝偶数，窄档 2×2、宽档 1×4），「菜数」卡带 `detail`＝菜名 | 同尺对照：读数卡那版 88／89，读数带那版 79（§六） |
| `src/shopping/pages.ts:119` | 结论条「已按〈份量〉算好，照着买就行。」 | 「已按〈份量〉算好。」（去掉口播式尾句；份量已在读数卡里说了） | 收口席原话「文案有…冗余」 |
| `src/shopping/pages.ts:126-133` | 口径行常驻（默认那一句「家里的库存交给居家管家核对」） | **只在真有例外时出一行**（排除可选／本次不核库存）；默认那一句删掉——去哪儿核库存已由主按钮与复制区承担 | 收口席原话点了这两句「冗余」，并授权「渲染层自己拼的句子可以删／并」 |
| `src/shopping/pages.ts:75-84` | 读数带／折叠条的暖色规则 | 读数卡：暖调描边＋暖色浅底（皮肤已给顶缘暖色条）；折叠条标题行加一条暖色渐隐；开合标记换暖色圆点 | 「视觉以静态灰白为主缺乏品牌温度」 |
| `src/shopping/pages.ts:57-73` | 页头图标位走 CSS `data:` SVG | 同（保留） | 「把暖色基用到页头」 |
| `src/setup/pages.ts:147`（删） | 口径行「用到时才建库建目录，建好以后不再改动文件。」＋三段折叠正文 | **删掉口径行**，内容并进「建库」那一步的正文（…不自动搬动；建好以后不再改动文件。） | 收口席原话「两条相邻近义冗余」，授权删／并 |
| `src/setup/pages.ts:82-105` | 读数带面板（通栏） | 读数带保留通栏，加暖色描边／渐变底／桌面档格内居中 | 「桌面端三栏指标块占满首屏显得呆板」——通栏改成面板后不再是一条裸栅格 |

**一条试过又撤掉的（留档）**：按「加内联 SVG 装饰带」那一条做过一版**横贯正文的暖色波浪装饰带**，
第 45 格复评 **81**（原话「顶部装饰点连线在宽屏显得稀疏孤立」，视觉生动 15→14）、第 35 格 **85**（原话「尚可更克制」）
⇒ 两页都撤掉。装饰改走「页头图标位 ＋ 分区题头暖色渐隐 ＋ 读数卡暖底」这一路（都是内联 SVG／纯 CSS，无 `<img>`、无 `loading=lazy`）。

## 八 逐页销账（第二轮，两跑读数）

| 格 | 页名 | 同尺改前 | 第一轮改后 | 本轮两跑 | 五维（两跑逐字相同） |
|---|---|---|---|---|---|
| 35 | 生成清单 | **87**（收口席） | **79**（收口席） | **89／89**（本席冻结尺两跑） | 18／19／17／17／18 |
| 45 | 首次使用 | —（未给） | **86**（收口席） | **90／90**（本席冻结尺两跑） | 18／19／18／17／18 |

- 第 45 格两跑都是 **90**：`{"信息层级":18,"双端自适应":19,"文案精炼":18,"视觉生动":17,"风格一致":18,"total":90}`，两次逐字相同。
- 第 35 格两跑都是 **89**（差 1 分）：`{"信息层级":18,"双端自适应":19,"文案精炼":17,"视觉生动":17,"风格一致":18,"total":89}`，两次逐字相同。
  原话：「…但『葱姜蒜』『食材／用量』等存在标签重复与用字冗余，且缺图与重彩使得视觉生动略有不足。」
- 中间版本读数（留档，不进销账）：读数卡＋首组展开＋装饰带 85；读数卡＋原口径行 88；主操作上移＋折叠条带引导词 77；四个分组全合＋折叠条带引导词 79；结论条改写 83。

## 九 反例（第二轮必跑）

| 步 | 页 | 改回哪一处 | 读数 | 判 |
|---|---|---|---|---|
| 红 | 45 首次使用 | 把本轮删掉的那条口径行「用到时才建库建目录，建好以后不再改动文件。」放回来 | 16/18/**14**/15/17 = **80**（原话：「首屏主信息…被一条小灰字…打断」） | **红**（低于 90） |
| 绿 | 45 首次使用 | 逐字还原 | **90／90**（与反例前逐字相同）；产物 sha256 回 `9412b4166e68179ce8efece72f52f947d0984f5288b6c3b81713184e71e3f920`（与 90 那版**逐字节相同**） | **绿** |

## 十 机器面（第二轮）

```
GATE-RUN runId=ae6826d3-25ef-4093-9d4f-ff6007228a40 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=eda6018d-35ab-476c-9db0-61c4bad64d5f cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 开始使用" exit=0
GATE-RUN runId=a0f91e74-8fc9-455b-8a99-fceec28fc1ed cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=e8f0fd6a-601d-4f56-84ed-886fa902b8e1 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 开始使用" exit=0
GATE-RUN runId=e46c4d27-5747-41de-817d-b1e21c0e09cc cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=6a15732a-644a-4ba3-a066-30cc99d326de cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=d0ce3880-452d-4fce-b1c4-53aafb547660 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=43d7f6ba-dae6-4d37-9ead-257ed61c6310 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=73e374c6-227f-4284-8e54-2a5e6224dad6 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 采购" exit=0
GATE-RUN runId=354d4bad-7445-43de-bea4-5d56f4bc634b cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 开始使用" exit=0
GATE-RUN runId=5844cda8-b45d-4eed-80c5-71d2cb80c116 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 开始使用" exit=0
```

- 编译：每轮 `seat.ps1` 窗口内 `tsc -b packages/skill-chef` 均 exit=0（中途两轮撞到**别席在途编译红**——`src/cook/run.ts`、`src/relation/pages.ts`——按派单册等 60 秒重试后过）。
- 质量门（两域末轮）：`横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0` exit=0。
- 终审扫描：两域各「扫 1 件；命中 0 -> 干净」exit=0。
- **收口席广播自查（宽档 `margin: auto` 关掉栅格拉伸）**：本席两条页内样式里**一处 `margin: auto`／`max-width` 都没有**
  （唯一含 `auto` 的是 `.ilife-chef-deck .ilife-block-fact-strip-item { flex: 1 1 auto }`，是 flex 简写，不是自动外边距）
  ⇒ 广播点的那条根因不适用本席两页，无需修。

## 十一 未做项与下一手缺什么（第二轮）

1. **第 35 格 89／89，差 1 分未达 ≥90**；第 45 格 **90／90 达标**。
2. 第 35 格剩下的那 1 分，尺子给的两条理由都与本席可动的范围无关或有反例：
   「缺图」＝量尺口径外的「图从哪来」那条决定；「标签重复」＝「食材」既是读数卡标签又是表列头、
   「葱姜蒜」既是折叠条标题又是那一组本身的名字——试过删表注（已删）、试过合并（读数卡 4 张不能减，减了又成孤卡）。
3. **这把尺的稳定性仍要记账**：同一批四张近邻变体给出 85／88／77／79 的散布（§八 留档那几行），
   单跑差值最大 11 分；**同图连跑是逐字相同的**（45 的 90／90、35 的 89／89 各两跑逐字相同）⇒ 抖动来自「图与图」，不是「同图重复调用」。
   报读数时请按「同图两跑」采信，单跑不采信。
4. 反例批与正式批共用 `shots/` 落点，还原那一轮把它覆盖回正式态（图上只剩正式态）；两条读数、两个 runId 与 sha256 对账见 §九。
5. 越界报备：无。`git add` 只加白名单（`src/shopping/pages.ts`、`src/setup/pages.ts`、本证据件；`src/setup/run-init.ts` 第一轮已提交、本轮未改）。
