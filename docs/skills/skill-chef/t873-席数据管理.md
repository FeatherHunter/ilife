# t873 数据管理席：3 页（46 体检／47 批量改／48 备份）逐页收口 —— 机器六列全绿、三类字样 0、包内守门 32/32；**冻结尺未达 ≥90**（85／87／86）

票：[#873](https://github.com/FeatherHunter/ilife/issues/873) · 席位＝**数据管理**（写集 `packages/skill-chef/src/data/**`）· 页＝46 数据质量报告（体检）／47 批量改回执（批量改）／48 备份回执（备份）。

**一句话结论**：三页按「视觉生动／文案精炼／双端自适应／信息层级」四条改了页面装配件
（`src/data/pages.ts`），机器面**六列全绿**、本域产物**三类字样命中 0**、包内两条守门测试 **32/32**；
冻结尺下三页总分 **85／87／86**（改前基线 81／80／74），**都还没到 ≥90**，差距与原因见 §五。

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `src/data/pages.ts:38-41` | 无 | 完整度条两色常量：达标取 `--blue`、未达标取 `CHART_PALETTE[2]`（品牌暖色，不新造色值） | 46「缺色彩锚点」 |
| `src/data/pages.ts:43-83` | 无 | 新件 `seatFigure()`：一张 240×96 的**纯装饰**内联 SVG（`aria-hidden`、**一个字都不含**；暖色取 `CHART_PALETTE`，底衬走冻结 token） | 47／48「整页近乎纯文字、缺温度与节奏」 |
| `src/data/pages.ts:85-141` | 无 | 新件 `dataSeatCss(page)`：① 复制区单按钮那一行收成按内容宽；② 插图宽度 340px；③ 逐菜卡里完整度条的间距与名称字重；④ 变更行收成一张卡、标签按内容宽 | 47「按钮一字占一整行／铺满整行」、48「大小卡…」同族版式 |
| `src/data/pages.ts:144-150` | `extraCss: chefSceneCss()` | `extraCss: chefSceneCss() + LF + dataSeatCss(page)`（**公共层入口一行不动**，页内样式追加在后 —— 两层不拆） | 与公共层席的接线口径一致 |
| `src/data/pages.ts:165-225`（体检页） | 结论句「最该补的是X（0分），先补它。」；四条读数「菜数／满分／待补／满分线 八十分」；逐菜只有折叠标题（`X（N分）`）＋两列读数表＋每缺一项一句「X待补Y」 | 结论句改「先补「X」：完整度 N 分。」；读数改「菜数／达标／待补／达标线 80 分」并给语气（达标 0 道不再显绿、待补非零显暖）；逐菜卡默认全展开：**完整度条**（条长＝得分）＋**五项读数胶囊**（食材N味…背景 有/无）＋**缺口一行**（`X还缺：…`） | 46「三张 KPI 卡按两列排、第三张孤行」（#871 已收口，本席复核仍在）＋「表头与正文同号、难辨列」（两列表在 ≤640 折成「列名一行＋值一行」，五行读数摊成十行 ⇒ 换成一行一事的胶囊）＋「缺色彩锚点」 |
| `src/data/pages.ts:228-258`（批量改页） | 结论「已对照改前改后，确认无误再复制口令交回。」；主按钮「复制修改口令」；说明「只改已有的食材与步骤，不新增；关联步骤暂不支持。」；校准「缺数字用量与时长会当场拦下，补齐再试。」；复制区标题「复制修改说明」 | 结论「改前改后已对上，确认无误再复制。」；主按钮「复制这次的修改」；说明「只改已经有的食材与步骤，不会新增。」；校准「用量与时长要写数字。」；复制区标题「改动说明」（与动作行那颗按钮不再同说一次「复制」）；页尾加插图 | 47「「复制修改口径」中「口径」是行话、与首页用词不一」（#871 改「口令」，本席再按首页／HELP 的实词收成「复制这次的修改」，全页不再出现「口令」／「口径」）＋文案冗余＋视觉平淡 |
| `src/data/pages.ts:261-288`（备份回执页） | 结论「备份已落盘，全量表一次导出。」；旁注「恢复时解压再逐个导入，定时与增量以后再做。」；校准「默认不含已废弃。」；复制区只有标题＋按钮 | 结论「备份已经落盘，一次导出全部数据。」；**删掉排期口吻的那句旁注**（「解压」与这个 JSON 文件也不符）；校准「默认不含已废弃的菜。」；读数条下加插图；复制区加一行说明（「复制的是这份回执原文，随文件一起留档。」） | 48「大段辅助文案重复冗余、视觉几乎纯蓝文字缺温度与节奏」＋「1280 首屏「大小 24916 字节」被裁到右缘」（#871 已随读数卡下线消掉，本席复核仍在） |

**没动的**：`run-batch.ts`／`run-query.ts`（批改写库逻辑与取数一律不动）、公共层 `src/render/**`、任何别的域；
判据（`t768-质量门.mjs` 六列、`t778-终审扫描.mjs` 字样表、墙与册子）**一处没放宽**。

## 二 逐页销账（两行读数）

尺＝`t873-派单册.md` §二 的冻结问句（每页一次调用、两张图、不带 `json: true`）；改前＝`t873-开工基线.md` §一。
本席另跑了**改前图**（改动前的产物，同一批 `.scratch/t873-数据管理/shots/`）做同尺对照。

| 格 | 页名 | 改前基线（§一） | 改前复跑（本席） | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|---|
| 46 | 数据质量报告（体检） | 18/18/16/**12**/17 = **81** | 18/17/16/14/18 = **83** | 18/17/16/**16**/18 = **85** | 「三张 KPI 卡按两列排、第三张孤行」→ **产物复核：孤卡不在**（事实条 4 格一行排开，`block-kpi-card-grid` 在产物里 0 次，包内守门测试钉着这条）；「表头与正文同号、难辨列」→ 五行读数表换成一行一事的胶囊 ⇒ 视觉生动 **12 → 16（+4）**、信息层级 18；总分 **81 → 85（+4）**（对本席改前复跑 +2） |
| 47 | 批量改回执（批量改） | 18/17/**13**/15/17 = **80** | 16/14/13/14/15 = **72** | 17/20/16/**16**/18 = **87** | 「「复制修改口径」中「口径」为行话」→ 全页不再出现「口径」／「口令」，按钮改「复制这次的修改」、复制区标题改「改动说明」；「说明文案偏冗」→ 两句并一句；「按钮一字占一整行」→ 复制区单按钮收成按内容宽、变更行收成一张卡（标签按内容宽、新旧值紧随）；「视觉仅靠色块」→ 页尾加装饰插图 ⇒ 文案精炼 **13 → 16（+3）**、视觉生动 **15 → 16**、双端自适应 **17 → 20**；总分 **80 → 87（+7）** |
| 48 | 备份回执（备份） | 16/17/**13**/**12**/16 = **74** | 17/14/16/12/15 = **74** | 17/19/18/**15**/17 = **86** | 「大小 24916 字节卡在 1280 被裁到右缘」→ **产物复核：三格读数都在版心里**（#871 下线读数卡后即消，本席复核仍在）；「大段辅助文案重复冗余」→ 删掉排期口吻那句旁注、结论句与校准句各收一档 ⇒ 文案精炼 **13 → 18（+5）**；「视觉几乎纯蓝文字缺温度与节奏」→ 读数条下加暖色装饰插图、复制按钮不再铺满整行 ⇒ 视觉生动 **12 → 15（+3）**、双端自适应 **17 → 19**；总分 **74 → 86（+12）** |

**尺子的一条实测（对收口席有用）**：同一批图 ＋ 逐字同一问句，`vision_describe` 给的是**逐字相同**的 JSON
（46 的两跑都是 80／80，见 §三 表注）；换一批产物才变（46 全展开 85 ↔ 只展开最差那道 80）。
⇒ 同批读数不含采样抖动，**但「改哪一版」会真差 5 分**（本席按实测把逐菜卡改回全展开）。

## 三 反例（改坏必红／还原一致）

**反例做法**：把本席第 46 格的机器可见扣分项**改回去** —— 在体检页重新挂上 3 张读数卡网格
（`renderKpiGrid`，即 #871 为 390 端孤卡下线的那一组），编译后跑包内守门测试
`packages/skill-chef/test/t871-文案与结构.test.mjs`（它钉着「体检页不出读数卡网格」）；然后**逐字还原**再跑同一件。

| 步 | 运行（持锁，`runId` 抄自 `.scratch/locks/gate-runs.log`） | 机器读数 |
|---|---|---|
| 改坏 | `runId=07ad1eb4-47df-48fe-a215-d0869889acc5` `pwsh -NoProfile -File .scratch/t873-数据管理/mut-check.ps1` | **红**：`TSC-OK` ＋ `ℹ tests 13 / ℹ pass 12 / ℹ fail 1`，`✖ 数据管理体检页：读数走事实条，不出现读数卡网格` ⇒ `TEST-EXIT 1` |
| 还原（逐字回绿） | `runId=15a61e74-e6b5-4065-9901-0f3720bf4bbd` 同一条命令 | **绿**：`TSC-OK` ＋ `ℹ tests 13 / ℹ pass 13 / ℹ fail 0` ⇒ `TEST-EXIT 0` |

（另一条反例读数在冻结尺上：46 的逐菜卡「只展开最差那道」这一改动与「全展开」是同一份判据的两态，
两态读数 **80 ↔ 85** —— 本席按读数把状态定在全展开那一版。）

## 四 机器面

### 4.1 门禁运行记录（§2.4 第 2 条：一次运行一行，`runId` 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=8d6713ac-bcaa-4194-b0dd-bde2e42dc4d4 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=4668ea6c-6b6d-45e1-9825-874d796da6cf cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=d8363e4e-fb3e-41d7-bb93-8d78fa5c8251 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=2cc5cb45-ceee-4d38-845a-1da19f2391a3 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=07494989-3aea-439e-b7fa-1c3d5ad7f9c6 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=0807a81c-783b-4436-a761-4738b6a43c57 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=07ad1eb4-47df-48fe-a215-d0869889acc5 cmd="pwsh -NoProfile -File .scratch/t873-数据管理/mut-check.ps1" exit=1
GATE-RUN runId=15a61e74-e6b5-4065-9901-0f3720bf4bbd cmd="pwsh -NoProfile -File .scratch/t873-数据管理/mut-check.ps1" exit=0
GATE-RUN runId=c7ab8299-727a-4e70-83f4-a8535a89afb9 cmd="pwsh -NoProfile -File .scratch/t873-数据管理/final-check.ps1" exit=0
```

（**最终产物对应的那一次是 `0807a81c`**（全展开那一版的编译 ＋ 重出产物 ＋ 双端首图 ＋ 质量门）。
另有 4 条本席**不计入门禁证据**的 tsc 红：`8ba8593c`／`e2d3ebd3` 红在别人写集的
`src/view/page.ts`／`src/relation/pages.ts`、`75dfe11c` 红在 `src/view/pageCss.ts`、`8c8879c4` 是反例态的第一次跑
（脚本自己的过滤器写窄了、只拿到 `TEST-EXIT 1`）—— 按派单册「别席正在改，等 60 秒重试」处置，重试后本席文件编译通过。）

### 4.2 质量门（`.scratch/t873-数据管理/质量门.json`，六列 × 390／1280 两档）

```
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 3 行）   exit=0
```

逐件点名（本席 3 页 **六列全绿**，无红行）：
`data_quality_report.html`｜`data_batch_edit.html`｜`data_export_backup.html`。

### 4.3 终审扫描

```
扫 3 件产物；三类字样命中 0 处
共 3 件；命中 0 -> 干净     exit=0
```

### 4.4 包内守门测试

```
ℹ tests 32 / ℹ pass 32 / ℹ fail 0     exit=0
```
（`packages/skill-chef/test/skin-873.test.mjs` ＋ `test/t871-文案与结构.test.mjs`；后者钉着「数据管理两页不出读数卡网格、
读数在事实条（4 条／3 条）」——本席的两处结构性改动都在这两条断言之下做的，没有放宽它。）

### 4.5 结构纪律（必报五步 · 第四步）

`src/data/pages.ts`：**260 LF**（口径＝只数换行符），低于本包告警线 350 ⇒ 不必走必报第四步。

## 五 未做项与下一手缺什么

1. **3 页都没到 ≥90**（85／87／86）。差距集中在两维，而且这两维**不是排版能补的**：
   - **视觉生动**（16／16／15）：本席已把能加的形状件加满 —— 品牌带与页头柔光（公共层皮肤）、完整度条、
     读数胶囊、变更行卡、页内装饰插图，全批这几页仍然没有**任何一张真实图片**。公共层席的证据件已实测同一结论
     （「视觉生动这一维的天花板：4 页改后无一页超过 17」），并建议另立票裁决「图从哪来」。本席同样的判断：
     **再加分只剩出图**，而本批 48 页数据源里没有任何图片字段，且 `renderMediaFigure` 的产出器会写 `loading="lazy"`
     （验收墙造册拒收）。⇒ 下一手：由编排者裁决「是否给私家大厨页出菜品图位」。
   - **双端自适应**（17／20／19）与**文案精炼**（16／16／18）：机器六列全 0 的前提下，这两维扣的是**评委在图上的观感**
     （「手机端信息密度偏高」「进度条与标签略显单薄」「提示句反复出现」）。三页的信息量本身很小
     （体检 3 道菜／批量改 1 处／备份 3 个读数），文案能压的已压到极限；再压就是删信息。
2. **尺子本身的刻度**：`t873-开工基线.md` §九 记着这把尺比首跑严约 5 分、且**未改过的页也全部 <90**；
   本席实测同批图可复现（逐字相同的 JSON），所以 85／87／86 是**这三页在这把尺上的稳定值**，不是抖动。
   若收口席要 ≥90，需要的是**器具侧的决定**（问句／维度口径）或**出图**，不是再改这三页的排版。
3. **越界报备（未碰，只记录）**：
   - `t776-run-small-domains.mjs` 是这三页的产物驱动器（`docs/skills/skill-chef/` 下，不在本席写集）。
     备份页要显示「备份文件路径」或「逐表条数」就得改它＋`runDataQuery` 的返回面 ⇒ 本席**没碰**，
     本页因此只有 3 个读数可摆（这也是 48 页偏「薄」的根因之一）。要加，请给数据管理席开一行写集。
   - 体检页的「五项读数」在 ≤640 档若走两列表（`renderDataTable`），会被公共层的窄档配方折成
     「列名一行 ＋ 值一行」；本席改成胶囊回避。**公共层层面的修法**（窄档两列表的列名与正文分号）属公共层席，
     本席没碰 `packages/base-render/**`。
4. **给收口席的一条**：本席的产物在 `.scratch/t873-数据管理/`（`数据管理/*.html` ＋ `manifest.json` ＋
   `shots/*-390v.png`／`*-1280v.png` ＋ `质量门.json`）；反例那一轮的产物**没有单独留批**（反例打在编译态上、
   由包内测试读出红绿），两行读数在 §三。

---

## 六 第二轮（定向返修：桌面宽档重排 ＋ 删复述文案 ＋ 页族插画带）

**判定（两跑读数，同一批产物、逐字同一问句）**：46 = **84／84**｜47 = **80／70**｜48 = **84／84**。三页仍未到 ≥90；
46／48 两跑逐字相同（可复现），47 两跑差 10 分（同一批图两个不同读数，见 §6.5）。

### 6.1 这一轮改了哪几处

| 落点（file:line） | 改前（第一轮收口） | 改后（第二轮） | 对应哪条复评 |
|---|---|---|---|
| `src/data/pages.ts:43-83`（`FIGURE_ART`／`recordMark`／`seatFigure`） | 240×96 的**小卡**（340px 宽，画布 `meet` 等比缩） | **页族插画带**：720×120 满宽、`preserveAspectRatio="slice"` 按高度铺满（窄档不再缩成一条细线）；图形记号居中、两侧各两枚「记录」记号（由内向外变小变淡）——三页各一枚：体检表／改前改后对照／叠表＋暖色印记 | 46「桌面端卡片横向留白过多却未补视觉元素」＋48「首屏中部图标区空白偏大」＋47「整体略显呆板」 |
| `src/data/pages.ts:85-198`（`dataSeatCss`） | 复制按钮按内容宽；插图 max-width 340 | 增四条：① 插画带满宽定高（88px／宽档 116px）；② **宽档复制区单行**（标题＋说明在左、按钮贴右，`@media (min-width:1001px)`）；③ `.chef-data-pair` 两栏（宽档 340px ＋ 1fr、窄档上下、恒 12px 气口）；④ 体检页完整度条 8→12px、分数 15px、五项读数胶囊补内距 | 47「说明卡片在大屏下仍以小宽度居中、内容稀松」＋46「进度条与标签略显单薄」 |
| `src/data/pages.ts:325-345`（批量改页） | 结论「改前改后已对上，确认无误再复制。」＋另起一行说明「只改已经有的食材与步骤，不会新增。」；主按钮「复制这次的修改」 | **删掉那行说明**（与结论条同义），把口径并进结论条：「只改已经有的食材与步骤，改前改后已对上。」；主按钮收成「复制改动」 | 47「文案『只改已经有的食材与步骤，不会新增』与上一条蓝色提示语义重复」＋「按钮文案拖沓」 |
| `src/data/pages.ts:345-355`、`:379-390`（两页页尾） | 插图单独一行；复制区单独一张满宽卡 | 两件收进 `.chef-data-pair`：宽档 **装饰带 340px ＋ 复制区 1fr** 并排、窄档上下 | 47「说明卡片内容稀松」＋「桌面端空槽」；两页同处置保持页族一致 |
| `src/data/pages.ts:252-256`（体检页） | 逐菜卡上方只有事实条 | 事实条与逐菜卡之间加插画带 | 46「桌面端…未补视觉元素」 |

**没动的**（与第一轮一字不变）：`run-batch.ts`／`run-query.ts`、公共层 `src/render/**`、任何别的域、任何判据。

### 6.2 两跑读数（冻结尺，逐页一次调用两张图、不带 `json:true`）

| 格 | 页名 | 第一轮收口读数 | 第二轮第 1 跑 | 第二轮第 2 跑 | 评委两跑共同指向（原话摘） |
|---|---|---|---|---|---|
| 46 | 数据质量报告 | 85 | **84**（18/17/15/16/18） | **84**（同值） | 「派生菜标签在 5 枚时挤压成两行」「『还缺…当前 0』句式冗余」「桌面端中部留白偏多」 |
| 47 | 批量改回执 | 87（本席）／77（编排者同尺） | **80**（18/17/15/12/18） | **70**（16/14/12/13/15） | 「首屏堆叠过满」「说明栏紧贴图例下方违和」「缺乏视觉锚点」 / 「首屏信息密度偏低、改动说明卡片留白空洞」——**两跑方向相反** |
| 48 | 备份回执 | 86 | **84**（18/18/17/14/17） | **84**（同值） | 「视觉以纯几何块＋单色为主、缺乏食材或厨意插画」 |

### 6.3 机器面（第二轮）

```
GATE-RUN runId=6a8ae49b-a9b4-447e-826a-9e1d9f0b5cbb cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=c1617b1a-2a16-4abf-8ffb-20b4d8e69b7b cmd="pwsh -NoProfile -File .scratch/t873-数据管理/mut-check.ps1" exit=1
GATE-RUN runId=18adb6ac-a9ec-4a65-867d-35998efaf7fc cmd="pwsh -NoProfile -File .scratch/t873-数据管理/mut-check.ps1" exit=0
GATE-RUN runId=2f45fc92-67be-4a6d-b1cf-95458816b9c7 cmd="pwsh -NoProfile -File .scratch/t873-数据管理/final-check.ps1" exit=0
```

- 编译 ＋ 重出 ＋ 双端首图 ＋ 质量门（最终产物）：`runId=6a8ae49b` exit=0，`TSC-OK`；
  汇总行仍为 `横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 3 行）`。
- 终审扫描：`扫 3 件产物；三类字样命中 0 处 → 干净`（`runId=2f45fc92`）。
- 包内守门：`ℹ tests 32／pass 32／fail 0`（`runId=2f45fc92`）。
- 迭代中间轮（本席不计入门禁证据的调试轮）：`a9ec6fa6`／`cc3fafcf`／`1171d54f`／`ee13425c`／`ece1c0bc`（均 exit=0），
  以及别席在途引起的两条 tsc 红 `d8ffff97`（`src/cook/run.ts`）／`172fc331`（`src/history/pages.ts`）—— 等 60 秒重试后转绿。

### 6.4 反例（第二轮同一条，打在第二轮源码上）

| 步 | 运行 | 机器读数 |
|---|---|---|
| 改坏（把体检页的 3 张读数卡网格挂回去＝#871 孤卡收口的回退） | `runId=c1617b1a-2a16-4abf-8ffb-20b4d8e69b7b` | **红**：`TSC-OK` ＋ `ℹ tests 13／pass 12／fail 1`，`✖ 数据管理体检页：读数走事实条，不出现读数卡网格` ⇒ `TEST-EXIT 1` |
| 逐字还原 | `runId=18adb6ac-a9ec-4a65-867d-35998efaf7fc` | **绿**：`ℹ tests 13／pass 13／fail 0` ⇒ `TEST-EXIT 0` |

### 6.5 未达标还差在哪（逐页）

1. **差的分数都吃在「视觉生动」**（46＝16、47＝12/13、48＝14；其余四维 46＝18/17/15/18、48＝18/18/17/17 已接近满）。
   两跑评委原话都指向**同一件事**：要「食材卡／料理场景／菜品预览」这类**具体食物意象**。
   本席这一轮按派单把能做的都做了（页族插画带 ＋ 图形记号 ＋ 满宽铺满 ＋ 宽档重排），
   但**不许编造数据、不许画写实菜品图**这条红线没动 ⇒ 这一维在本席写集里已经到顶。
2. **47 的两跑不自洽**：同一批产物，两跑给出 80 与 70，且评委原话互相矛盾（一跑说「堆叠过满」、
   另一跑说「密度偏低、留白空洞」）。⇒ 47 的读数落在**器具噪声**里，不是这一版排版的属性；
   同一批图的第三/四次复评才能把它夹住（本席已跑两跑，读数如实记）。
3. **46／48 两跑逐字相同（84／84）**，说明**这一版**在这把尺上稳定落在 84；
   要再往上，只能是「出图」或「器具口径」两件事之一（与第一轮 §五 的结论一致，本轮补了桌面宽档与插画带，仍未越过）。
4. **越界报备（第二轮，未碰）**：同 §五.3 —— 备份／批量改两页要「加信息」（文件路径／逐表条数）必须改
   `docs/skills/skill-chef/t776-run-small-domains.mjs`（采购席共用驱动器）＋ `runDataQuery` 返回面；
   编排者本轮已裁定**不开那行写集**，故两页只做了「把现有信息摆好」的那一半。

### 6.6 广播自查：本席写集里没有「宽档收窄居中」那条根因（2026-09-21 广播）

广播的根因是：宽档把某一块写成 `margin-left/right: auto`／`margin: 0 auto` —— 它是
`.ilife-block-page-shell-body` 的**栅格项**，`margin: auto` 会关掉栅格项的 `justify-self: stretch`
⇒ 整块 shrink-to-fit 居中。**本席自查结论：写集里没有这条写法，也没有等价写法。**

| 查什么 | 读数 |
|---|---|
| `packages/skill-chef/src/data/pages.ts` 全文 grep `auto｜justify-self｜margin｜max-width｜width:` | 14 处命中，逐条看：`width: 100%`（插画带）、`margin: 14px 0 0`／`margin: 0`／`margin: 2px 0 0`／`margin-top`／`margin-bottom`（全是定值，**无一处 `auto`**）；`auto` 只出现在两处**非 margin** 的写法：`flex: 0 1 auto`（变更行标签的 flex 简写）与 `grid-template-columns: minmax(0, 1fr) auto`（宽档复制区**按钮那一轨**按内容宽——卡本身仍通栏） |
| 产物实测（`.scratch/t873-数据管理/shots/02-data_batch_edit-1280v.png`，原图像素外框） | ① 顶部结论条 x≈215→1075；② 页尾插画带 x≈215→541；③「改动说明」卡 x≈541→1075 —— ②＋③ 合起来与 ① **同左右边界**；**没有一块比 ① 窄且水平居中** |

⇒ 第 47 格那句「说明卡片在大屏下仍以小宽度居中」在本席产物上**复现不出来**（②③ 都通栏、合起来满版心），
它要么是评委把「卡里内容靠左、右侧空白」读成了「卡被居中」，要么是别的域的根因被串到本页；
本席这一轮已把该卡在宽档收成单行（标题＋说明在左、按钮贴右），留白按 §6.1 处理过，
`margin: auto` 这条**无需修、也无可修**。

---

## 七 第三轮（族级带收口 ＋ 文案逐句审 ＋ 首屏填满度）

**同会话读数（同一把尺、同一批图的「本轮前 → 本轮后」，跨会话绝对值不可比）**：

| 格 | 页名 | 本轮前（本轮开工时，公共层族带已落地） | 本轮后第 1 跑 | 本轮后第 2 跑 |
|---|---|---|---|---|
| 46 | 数据质量报告 | **89**（18/20/16/17/18） | **89**（18/20/16/17/18） | **89**（逐字同值） |
| 47 | 批量改回执 | **81**（17/18/16/14/16） | **80**（16/17/17/14/16） | 未跑（<88，按派单「继续改」处置；本席这一轮能改的都改了，剩余是评委对「回执该长什么样」的口味，见 §7.6） |
| 48 | 备份回执 | **85**（17/17/16/17/18） | **88**（18/19/17/16/18） | **88**（逐字同值） |

⇒ 三页都**没到 ≥90**；46 两跑 89／89、48 两跑 88／88（都停在同一分位），47 在噪声带里（本会话 80–81）。

### 7.1 ① 页内那条 720×120 满宽带已撤，页头只留族级带

| 读数（探针 `node .scratch/t873-数据管理/带计数.mjs .scratch/t873-数据管理`） | 值 |
|---|---|
| `data_quality_report.html`（46·体检） | **装饰带 1 条**（族 `result`，期望 `result`）／页内旧宽带领 **0** 条／**完整度条 3 行**（数据可视化，**保留**） |
| `data_batch_edit.html`（47·批量改） | 装饰带 1 条（族 `receipt`）／页内旧宽带领 0 条／完整度条 0 行 |
| `data_export_backup.html`（48·备份） | 装饰带 1 条（族 `receipt`）／页内旧宽带领 0 条／完整度条 0 行 |
| `RESULT` | `全绿（每页 1 条族级带、无页内旧带）` exit=0 |

**这几条带各自载什么信息**：页头那条是**族级装饰带**（纯装饰，`aria-hidden`、无可见文本、不载读数）；
46 里那 3 行**完整度条**是**数据可视化**（一行＝一道菜，条长＝完整度分数 0–100，值列写 `N 分`），
按派单「例外保留」，色基与族带同取 `CHART_PALETTE` 的下标（本页用 `--blue` 与下标 2 的暖色），
卡片的左缘改取同一枚暖色、圆角 14（闭集内）——**页内不再有第二条装饰带**。

改动落点（同一件 `src/data/pages.ts`）：删掉第二轮的 `FIGURE_ART`／`recordMark`／`seatFigure`（约 4KB）、
`.chef-data-figure` 与 `.chef-data-pair` 两组样式；`shell()` 改成**在每一页的调用点上逐字给族名**
（`{ page: 'quality', family: 'result' }` / `{ page: 'batch'|'backup', family: 'receipt' }`）——
公共层席交付时三页都钉的 `family: 'receipt'`，体检页本应是 `result`，这一处**本席改正**。

### 7.2 ② 文案逐句审（渲染层自拼的句子，库里的字段值一个字没动）

问法：**删掉这句，用户会少知道什么吗？** 答案「否」即删（判定结果逐句记）。

| 页 | 渲染层那句话 | 删掉会少知道什么 | 处置 |
|---|---|---|---|
| 46 | 结论条「先补「X」：完整度 N 分。」 | 会少知道「先动哪道」——但菜名与分数在紧邻的卡片标题与条上**已经写着**（判官两轮点名「标语与缺失说明语义重复」） | **改**：换成「从下面第一道开始补。」（只留动作，不复述名与分） |
| 46 | 「X还缺：…」／「X五项齐全，不用补。」 | 「还缺」句：会少知道**差在哪几项、差在哪条线上** ⇒ 留；「五项齐全，不用补。」：菜名与「不用补」都是重复的 ⇒ 删尾 | **改**：齐全时只写「五项齐全。」；还缺句**保留**（阈值信息只此一处） |
| 47 | 校准行「用量与时长要写数字。」 | 它是**输入那一刻**的规矩；本页是改完之后的回执、页上没有输入位 ⇒ **不会少知道什么** | **删**（判官第 3 次点名的那一类） |
| 47 | 主按钮「复制改动」 | 这颗按钮在产物里**没有 `data-t`（点了不复制）**，与复制区那颗真按钮同说一件事 ⇒ 删掉不会少做任何事 | **删**；动作行只留「再看一遍菜谱」，并在页内样式里让**只剩一颗的动作按钮占满整行**（半格宽会被读成「少了件」） |
| 47 | 结论条「只改已经有的食材与步骤，改前改后已对上。」 | 会少知道「批量改不新增」这条边界 ⇒ 留（第二轮已把原来那行重复句并到这里） | 留 |
| 48 | 复制区说明行「复制的是这份回执原文，随文件一起留档。」 | 与标题「复制备份回执」＋按钮「复制数据」同义 ⇒ **不会少知道什么** | **删** |
| 48 | 校准行「默认不含已废弃的菜。」 | 会少知道**哪些菜不在这一份里** ⇒ 留；但复评把它读成病句（「默认不会…」）⇒ 换一口读得懂的短句 | **改**：「已废弃的菜不在这一份里。」 |
| 48 | 结论条「备份已经落盘，一次导出全部数据。」 | 这是回执的抬头 ⇒ 留 | 留 |

### 7.3 ②' 宽档复查（按新契约再查一遍）

| 查什么 | 读数 |
|---|---|
| `src/data/pages.ts` 全文 grep `auto` | 命中 3 处，**无一处是 margin**：`flex: 0 1 auto`（变更行标签）、`grid-template-columns: minmax(0, 1fr) auto`（宽档复制区**按钮那一轨**按内容宽，卡本身通栏）、探针脚本里的数组下标；**`margin: auto` 0 处**（收窄若要做，按新契约写 `width: 100% ＋ max-width ＋ justify-self: center`——本域三页这轮没有需要收窄的块） |
| 产物外框（1280 首屏，`02-data_batch_edit-1280v.png` 原图像素） | 结论条 x≈215→1075；读数格与变更行卡、复制卡都落在**同一左右边界**内；没有「比版心窄且水平居中」的块 |

### 7.4 ③ 首屏填满度（46 属 result 族，公共层没给填满度）

探针：`node .scratch/t873-公共层/填满度探针.mjs .scratch/t873-数据管理`（390×820，gap＝820 − 最底文本底边）

| 页 | 本轮**前** | 本轮**后** | 判据 |
|---|---|---|---|
| 46 体检 | 0px（内容本就溢出首屏） | **16px** | ≤120 ✓ |
| 47 批量改 | 85px | **110px** | ≤120 ✓ |
| 48 备份 | 105px | **110px** | ≤120 ✓ |

本席这一轮在 46 上试过「块距抬到 26px ＋ 胶囊补内距」，**探针没改善（gap 仍 0，因为内容本就溢出）**，
但复评把它读成「五项读数折成两行＝视觉冗余」「句式几乎重复」，同会话读数从 89 掉到 81 ⇒ **如实回退**
（块距与胶囊字号内距回公共层缺省，只留条高 12px、分数 15px、行距 8px），读数回到 89。
⇒ 结论：**46 的首屏从来不是「空」**，是「满到溢出」；填满度这条它在**开工时就已经达标**，本轮只需不回退。

### 7.5 机器面（第三轮）

```
GATE-RUN runId=981e1e26-69b0-4663-a7c9-72e8e65c2924 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=8b1e52ce-304b-41fe-b40a-d8bfbb15b9d9 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0   （反例态：族名改回 receipt）
GATE-RUN runId=ceebf21b-6334-448f-910f-2e10c170e461 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0   （逐字还原）
```

- 编译＋重出＋双端首图＋质量门（最终产物 `981e1e26`）：`TSC-OK`，汇总仍是
  `横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 3 行）`。
- 带计数探针（本轮新建，`.scratch/t873-数据管理/带计数.mjs`）：`全绿（每页 1 条族级带、无页内旧带）` exit=0。
- 填满度探针：`量到 3／3 页；gap 均值 78.7；区间 16–110`（三页都 ≤120）。
- 终审扫描：`扫 3 件产物；三类字样命中 0 处 → 干净` exit=0。
- 包内守门：`ℹ tests 38／pass 37／fail 1` —— **唯一那条红不在本席写集**：
  `✖ packages/skill-chef/src/history/pages.ts 只调 renderSceneShell()，且带一个合法族名`（历史席在途）。
  本席这一件 `src/data/pages.ts` 的同名断言**已绿**（改动过程中它曾红过一条：族名走表查值不满足
  「调用点上逐字给族名」的字面口径，本席随即把族名改成在每一页调用点上逐字给出 ⇒ 绿）。

### 7.6 反例（第三轮：撤掉本轮那条「按页给族名」的改动）

| 步 | 运行/读数 | 判 |
|---|---|---|
| 改坏：把体检页的族名从 `result` 改回 `receipt`（＝撤掉本轮那处改正）→ 重出 | `runId=8b1e52ce` exit=0 ＋ `node .scratch/t873-数据管理/带计数.mjs` → `✗ data_quality_report.html → 装饰带 1 条（族 receipt，期望 result）`／`RESULT: 有红 1 条` **exit=1** | **红** |
| 逐字还原 → 重出 | `runId=ceebf21b` exit=0 ＋ 同一条探针 → `全绿（每页 1 条族级带、无页内旧带）` **exit=0** | **绿** |

### 7.7 未达标还差在哪

1. **46 停在 89／89、48 停在 88／88**：两页两跑逐字相同 ⇒ 这是**这一版在这把尺上的稳定值**。
   两页的扣分句都是「视觉偏素／装饰条与图标条略冗」——即**视觉生动**那一维仍差 1–2 分；
   公共层席的族带＋色彩锚点已经把「形状件与色彩锚点」做到本批的上限，**再加只剩真实照片／菜品图**，
   那是派单红线（不许画写实菜品图）之外的裁决。
2. **47 停在 80–81**：评委原话是「像后台表单而非面向用户的回执」——这是对**回执这一族该长什么样**的口味判断，
   本席可动的只有措辞与形状（已按 §7.2 逐句审过）；再往下压文案就要删「改了什么」这条主信息。
3. **守线页 21／28／45 未碰**：本轮提交只含 `packages/skill-chef/src/data/pages.ts` 与本证据件两件，别的域一行没动。
