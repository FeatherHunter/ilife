# t873 数据管理席：3 页（46 体检／47 批量改／48 备份）逐页收口 —— 机器六列全绿、三类字样 0、包内守门 32/32；**冻结尺未达 ≥90**（85／87／86）

票：[#873](https://github.com/FeatherHunter/ilife/issues/873) · 席位＝**数据管理**（写集 `packages/skill-chef/src/data/**`）· 页＝46 数据质量报告（体检）／47 批量改回执（批量改）／48 备份回执（备份）。

**一句话结论**：三页按「视觉生动／文案精炼／双端自适应／信息层级」四条改了页面装配件
（`src/data/pages.ts`），机器面**六列全绿**、本域产物**三类字样命中 0**、包内两条守门测试 **32/32**；
冻结尺下三页总分 **85／87／86**（改前基线 81／80／74），**都还没到 ≥90**，差距与原因见 §五。

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `src/data/pages.ts:38-41` | 无 | 完整度条两色常量：达标取 `--blue`、未达标取 `CHART_PALETTE[2]`（品牌暖色，不新造色值） | 46「缺色彩锚点」 |
| `src/data/pages.ts:43-83` | 无 | 新件 `seatFigure()`：一张 240×96 的**纯装饰**内联 SVG（`aria-hidden`、**一个字都不含**；暖色取 `CHART_PALETTE`，底衬走冻结 token） | 47／48「整页近乎纯文字、缺温度与节奏」 |
| `src/data/pages.ts:85-141` | 无 | 新件 `dataSeatCss(page)`：① 复制区单按钮那一行收成按内容宽；② 插图宽度 340px；③ 逐菜卡里完整度条的间距与名称字重；④ 变更行收成一张卡、标签按内容宽 | 47「按钮一字占一整行／铺满整行」、48「大小卡…」同族版式 |
| `src/data/pages.ts:144-150` | `extraCss: chefSceneCss()` | `extraCss: chefSceneCss() + LF + dataSeatCss(page)`（**公共层入口一行不动**，页内样式追加在后 —— 两层不拆） | 与公共层席的接线口径一致 |
| `src/data/pages.ts:165-225`（体检页） | 结论句「最该补的是X（0分），先补它。」；四条读数「菜数／满分／待补／满分线 八十分」；逐菜只有折叠标题（`X（N分）`）＋两列读数表＋每缺一项一句「X待补Y」 | 结论句改「先补「X」：完整度 N 分。」；读数改「菜数／达标／待补／达标线 80 分」并给语气（达标 0 道不再显绿、待补非零显暖）；逐菜卡默认全展开：**完整度条**（条长＝得分）＋**五项读数胶囊**（食材N味…背景 有/无）＋**缺口一行**（`X还缺：…`） | 46「三张 KPI 卡按两列排、第三张孤行」（#871 已收口，本席复核仍在）＋「表头与正文同号、难辨列」（两列表在 ≤640 折成「列名一行＋值一行」，五行读数摊成十行 ⇒ 换成一行一事的胶囊）＋「缺色彩锚点」 |
| `src/data/pages.ts:228-258`（批量改页） | 结论「已对照改前改后，确认无误再复制口令交回。」；主按钮「复制修改口令」；说明「只改已有的食材与步骤，不新增；关联步骤暂不支持。」；校准「缺数字用量与时长会当场拦下，补齐再试。」；复制区标题「复制修改说明」 | 结论「改前改后已对上，确认无误再复制。」；主按钮「复制这次的修改」；说明「只改已经有的食材与步骤，不会新增。」；校准「用量与时长要写数字。」；复制区标题「改动说明」（与动作行那颗按钮不再同说一次「复制」）；页尾加插图 | 47「「复制修改口径」中「口径」是行话、与首页用词不一」（#871 改「口令」，本席再按首页／HELP 的实词收成「复制这次的修改」，全页不再出现「口令」／「口径」）＋文案冗余＋视觉平淡 |
| `src/data/pages.ts:261-288`（备份回执页） | 结论「备份已落盘，全量表一次导出。」；旁注「恢复时解压再逐个导入，定时与增量以后再做。」；校准「默认不含已废弃。」；复制区只有标题＋按钮 | 结论「备份已经落盘，一次导出全部数据。」；**删掉排期口吻的那句旁注**（「解压」与这个 JSON 文件也不符）；校准「默认不含已废弃的菜。」；读数条下加插图；复制区加一行说明（「复制的是这份回执原文，随文件一起留档。」） | 48「大段辅助文案重复冗余、视觉几乎纯蓝文字缺温度与节奏」＋「1280 首屏「大小 24916 字节」被裁到右缘」（#871 已随读数卡下线消掉，本席复核仍在） |

**没动的**：`run-batch.ts`／`run-query.ts`（批改写库逻辑与取数一律不动）、公共层 `src/render/**`、任何别的域；
判据（`t768-质量门.mjs` 六列、`t778-终审扫描.mjs` 字样表、墙与册子）**一处没放宽**。

## 二 逐页销账（两行读数）

尺＝`t873-派单册.md` §二 的冻结问句（每页一次调用、两张图、不带 `json: true`）；改前＝`t873-开工基线.md` §一。
本席另跑了**改前图**（改动前的产物，同一批 `.scratch/t873-数据管理/shots/`）做同尺对照。

| 格 | 页名 | 改前基线（§一） | 改前复跑（本席） | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|---|
| 46 | 数据质量报告（体检） | 18/18/16/**12**/17 = **81** | 18/17/16/14/18 = **83** | 18/17/16/**16**/18 = **85** | 「三张 KPI 卡按两列排、第三张孤行」→ **产物复核：孤卡不在**（事实条 4 格一行排开，`block-kpi-card-grid` 在产物里 0 次，包内守门测试钉着这条）；「表头与正文同号、难辨列」→ 五行读数表换成一行一事的胶囊 ⇒ 视觉生动 **12 → 16（+4）**、信息层级 18；总分 **81 → 85（+4）**（对本席改前复跑 +2） |
| 47 | 批量改回执（批量改） | 18/17/**13**/15/17 = **80** | 16/14/13/14/15 = **72** | 17/20/16/**16**/18 = **87** | 「「复制修改口径」中「口径」为行话」→ 全页不再出现「口径」／「口令」，按钮改「复制这次的修改」、复制区标题改「改动说明」；「说明文案偏冗」→ 两句并一句；「按钮一字占一整行」→ 复制区单按钮收成按内容宽、变更行收成一张卡（标签按内容宽、新旧值紧随）；「视觉仅靠色块」→ 页尾加装饰插图 ⇒ 文案精炼 **13 → 16（+3）**、视觉生动 **15 → 16**、双端自适应 **17 → 20**；总分 **80 → 87（+7）** |
| 48 | 备份回执（备份） | 16/17/**13**/**12**/16 = **74** | 17/14/16/12/15 = **74** | 17/19/18/**15**/17 = **86** | 「大小 24916 字节卡在 1280 被裁到右缘」→ **产物复核：三格读数都在版心里**（#871 下线读数卡后即消，本席复核仍在）；「大段辅助文案重复冗余」→ 删掉排期口吻那句旁注、结论句与校准句各收一档 ⇒ 文案精炼 **13 → 18（+5）**；「视觉几乎纯蓝文字缺温度与节奏」→ 读数条下加暖色装饰插图、复制按钮不再铺满整行 ⇒ 视觉生动 **12 → 15（+3）**、双端自适应 **17 → 19**；总分 **74 → 86（+12）** |

**尺子的一条实测（对收口席有用）**：同一批图 ＋ 逐字同一问句，`vision_describe` 给的是**逐字相同**的 JSON
（46 的两跑都是 80／80，见 §三 表注）；换一批产物才变（46 全展开 85 ↔ 只展开最差那道 80）。
⇒ 同批读数不含采样抖动，**但「改哪一版」会真差 5 分**（本席按实测把逐菜卡改回全展开）。

## 三 反例（改坏必红／还原一致）

**反例做法**：把本席第 46 格的机器可见扣分项**改回去** —— 在体检页重新挂上 3 张读数卡网格
（`renderKpiGrid`，即 #871 为 390 端孤卡下线的那一组），编译后跑包内守门测试
`packages/skill-chef/test/t871-文案与结构.test.mjs`（它钉着「体检页不出读数卡网格」）；然后**逐字还原**再跑同一件。

| 步 | 运行（持锁，`runId` 抄自 `.scratch/locks/gate-runs.log`） | 机器读数 |
|---|---|---|
| 改坏 | `runId=07ad1eb4-47df-48fe-a215-d0869889acc5` `pwsh -NoProfile -File .scratch/t873-数据管理/mut-check.ps1` | **红**：`TSC-OK` ＋ `ℹ tests 13 / ℹ pass 12 / ℹ fail 1`，`✖ 数据管理体检页：读数走事实条，不出现读数卡网格` ⇒ `TEST-EXIT 1` |
| 还原（逐字回绿） | `runId=15a61e74-e6b5-4065-9901-0f3720bf4bbd` 同一条命令 | **绿**：`TSC-OK` ＋ `ℹ tests 13 / ℹ pass 13 / ℹ fail 0` ⇒ `TEST-EXIT 0` |

（另一条反例读数在冻结尺上：46 的逐菜卡「只展开最差那道」这一改动与「全展开」是同一份判据的两态，
两态读数 **80 ↔ 85** —— 本席按读数把状态定在全展开那一版。）

## 四 机器面

### 4.1 门禁运行记录（§2.4 第 2 条：一次运行一行，`runId` 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=8d6713ac-bcaa-4194-b0dd-bde2e42dc4d4 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=4668ea6c-6b6d-45e1-9825-874d796da6cf cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=d8363e4e-fb3e-41d7-bb93-8d78fa5c8251 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=2cc5cb45-ceee-4d38-845a-1da19f2391a3 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=07494989-3aea-439e-b7fa-1c3d5ad7f9c6 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=0807a81c-783b-4436-a761-4738b6a43c57 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 数据管理" exit=0
GATE-RUN runId=07ad1eb4-47df-48fe-a215-d0869889acc5 cmd="pwsh -NoProfile -File .scratch/t873-数据管理/mut-check.ps1" exit=1
GATE-RUN runId=15a61e74-e6b5-4065-9901-0f3720bf4bbd cmd="pwsh -NoProfile -File .scratch/t873-数据管理/mut-check.ps1" exit=0
GATE-RUN runId=c7ab8299-727a-4e70-83f4-a8535a89afb9 cmd="pwsh -NoProfile -File .scratch/t873-数据管理/final-check.ps1" exit=0
```

（**最终产物对应的那一次是 `0807a81c`**（全展开那一版的编译 ＋ 重出产物 ＋ 双端首图 ＋ 质量门）。
另有 4 条本席**不计入门禁证据**的 tsc 红：`8ba8593c`／`e2d3ebd3` 红在别人写集的
`src/view/page.ts`／`src/relation/pages.ts`、`75dfe11c` 红在 `src/view/pageCss.ts`、`8c8879c4` 是反例态的第一次跑
（脚本自己的过滤器写窄了、只拿到 `TEST-EXIT 1`）—— 按派单册「别席正在改，等 60 秒重试」处置，重试后本席文件编译通过。）

### 4.2 质量门（`.scratch/t873-数据管理/质量门.json`，六列 × 390／1280 两档）

```
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 3 行）   exit=0
```

逐件点名（本席 3 页 **六列全绿**，无红行）：
`data_quality_report.html`｜`data_batch_edit.html`｜`data_export_backup.html`。

### 4.3 终审扫描

```
扫 3 件产物；三类字样命中 0 处
共 3 件；命中 0 -> 干净     exit=0
```

### 4.4 包内守门测试

```
ℹ tests 32 / ℹ pass 32 / ℹ fail 0     exit=0
```
（`packages/skill-chef/test/skin-873.test.mjs` ＋ `test/t871-文案与结构.test.mjs`；后者钉着「数据管理两页不出读数卡网格、
读数在事实条（4 条／3 条）」——本席的两处结构性改动都在这两条断言之下做的，没有放宽它。）

### 4.5 结构纪律（必报五步 · 第四步）

`src/data/pages.ts`：**260 LF**（口径＝只数换行符），低于本包告警线 350 ⇒ 不必走必报第四步。

## 五 未做项与下一手缺什么

1. **3 页都没到 ≥90**（85／87／86）。差距集中在两维，而且这两维**不是排版能补的**：
   - **视觉生动**（16／16／15）：本席已把能加的形状件加满 —— 品牌带与页头柔光（公共层皮肤）、完整度条、
     读数胶囊、变更行卡、页内装饰插图，全批这几页仍然没有**任何一张真实图片**。公共层席的证据件已实测同一结论
     （「视觉生动这一维的天花板：4 页改后无一页超过 17」），并建议另立票裁决「图从哪来」。本席同样的判断：
     **再加分只剩出图**，而本批 48 页数据源里没有任何图片字段，且 `renderMediaFigure` 的产出器会写 `loading="lazy"`
     （验收墙造册拒收）。⇒ 下一手：由编排者裁决「是否给私家大厨页出菜品图位」。
   - **双端自适应**（17／20／19）与**文案精炼**（16／16／18）：机器六列全 0 的前提下，这两维扣的是**评委在图上的观感**
     （「手机端信息密度偏高」「进度条与标签略显单薄」「提示句反复出现」）。三页的信息量本身很小
     （体检 3 道菜／批量改 1 处／备份 3 个读数），文案能压的已压到极限；再压就是删信息。
2. **尺子本身的刻度**：`t873-开工基线.md` §九 记着这把尺比首跑严约 5 分、且**未改过的页也全部 <90**；
   本席实测同批图可复现（逐字相同的 JSON），所以 85／87／86 是**这三页在这把尺上的稳定值**，不是抖动。
   若收口席要 ≥90，需要的是**器具侧的决定**（问句／维度口径）或**出图**，不是再改这三页的排版。
3. **越界报备（未碰，只记录）**：
   - `t776-run-small-domains.mjs` 是这三页的产物驱动器（`docs/skills/skill-chef/` 下，不在本席写集）。
     备份页要显示「备份文件路径」或「逐表条数」就得改它＋`runDataQuery` 的返回面 ⇒ 本席**没碰**，
     本页因此只有 3 个读数可摆（这也是 48 页偏「薄」的根因之一）。要加，请给数据管理席开一行写集。
   - 体检页的「五项读数」在 ≤640 档若走两列表（`renderDataTable`），会被公共层的窄档配方折成
     「列名一行 ＋ 值一行」；本席改成胶囊回避。**公共层层面的修法**（窄档两列表的列名与正文分号）属公共层席，
     本席没碰 `packages/base-render/**`。
4. **给收口席的一条**：本席的产物在 `.scratch/t873-数据管理/`（`数据管理/*.html` ＋ `manifest.json` ＋
   `shots/*-390v.png`／`*-1280v.png` ＋ `质量门.json`）；反例那一轮的产物**没有单独留批**（反例打在编译态上、
   由包内测试读出红绿），两行读数在 §三。
