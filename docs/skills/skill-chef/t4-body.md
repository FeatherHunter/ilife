## Question

两件事一起定，定完票 7 照它做：

**① 命名与落盘的管线归属**：

- 照 #143 的裁决 (b)：私家大厨**自持一份最小管线**（只抄通式那一小块，不搬卡路里的命令名→中文段落映射／动态段／三态）；
- 还是按新架构规则的共用件条款收成共用位——`docs/agents/structure.md`：**共用件是从第二个用法里长出来的**，并要写得出哪两个能力在用。今天的现实是卡路里／记账／居家／大厨已经是第三、四处在做同一件事。

**要把 #147 自己写下的三条合流触发点摆到桌面**（verbatim）：1. 出现**第三个**消费者；或 2. base-paint 因别的原因要开交付类子路径；或 3. 这份逻辑出现**第二次 bugfix**。**私家大厨正是第三个消费者，第 1 条已命中**——所以 (b) 对本票不再自然适用。

另要把铁律一（把对方那份抄一遍放进自己目录，不算走了接口）、铁律二（同一个东西只有一个定义地）、铁律五（接口小、里面厚）对这件事怎么说讲清，并复核 #147 当时的三条理由（`base-paint` 是被 #96 主动冻结的渲染包、落盘是 IO、技能间零依赖不可破）今天还成不成立。

**② 缺省出口口径**：

- 照 #139／#144 的判法：**缺省＝HELP 文件**（对 AI 说「私家大厨help」就落盘＋回执绝对路径），**速查走显式参数**；
- 还是保留现在 `chef.help.lookup` 回一份列表的缺省行为。

要带上桌的事实：老目录 `<SKILLS_DB_PATH>/CookHub/help/`、**文件名主体 `私家大厨_HELP`**、老通式 `_N` 从 1 起步绝不覆盖、**固定名镜像没有源码依据（新仓不写镜像）**、新表 4 条 HELP 唤醒词（私家大厨HELP／菜谱HELP／查帮助／能做什么）、票 1 的照抄清单。另注 `tooling/check-publish.mjs:63` 把 `dsh-chef` 钉在 `chef.help.lookup` 上——换命令名会让那道门红。

产出：决定＋理由，写回票面（含落盘目录、文件名主体、速查支产物名与显式参数名），并决定 `combo` 侧要不要同步登记。

## 进度：0%

下一步：等票 1 关票（照抄清单），然后开一张 grilling 会话把①②定死。
