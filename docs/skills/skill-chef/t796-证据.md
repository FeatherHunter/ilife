# #796 证据 · 私家大厨设置页收窄（照记账 #749 样板）· **技能侧**

判定：**PASS**（技能侧判据全绿：新 t796 用例 11／11、包级全量 68／68、真出口回执与真实 `~/.ilife/chef.yaml`
逐字对账一致、体检无 `ILIFE_CONFIG_DIR` 且无做不到的指引、源码级变异「打掉必红／还原必绿」跑齐；
#766 落点后按本票遗留出口顺带加第 4 格，同复绿）。

## 一、交付了什么（逐件一句，为什么）

| 件 | 改了什么 | 为什么 |
|---|---|---|
| `src/config.ts` | 默认值表删 `files` 组（3 键）；`CHEF_CONFIG_RETIRED=['files.help','files.lookup']`；加 `writableDefaults()`（`db.dir` 写 `configPaths('chef').dataDir`）＋ `withoutBlankEditableDir()`；记忆按配置文件记 | #795 删键 ＋ 补注验收①（读认空串、写落绝对） |
| `src/fetch/paths.ts` | 纯函数一族（`dbDirOf`／`dbFileOf`／`htmlDirOf`／`sceneDirOf`）＋ `resolve*` 薄壳 ＋ `resolvedChefPaths()` 四格；`DEFAULT_*` 具名引默认值表 | 票面：落点算式唯一定义地（末格是 #766 落点后按遗留出口顺带的） |
| `src/cli/config.ts` | `chef.config.read` 回执 `data` 新增 `resolved` 四格 | 票面验收 3；六家同形 |
| `src/help/manifest.ts` | 两主体回代码常量（`HELP_FILE_STEM`／`LOOKUP_FILE_STEM`）；段默认具名引 paths | 删键后文件名回常量，产物名逐字不变 |
| `src/health.ts` | 调纯算式；删自带切分与相对逻辑；`html.dir` 不可写指引改指配置文件 | 补注验收② ＋ #795 体检三条 |
| `test/t796-设置页收窄.test.mjs`（新） | 11 条：回执四格齐＋对账／读认空串／保存与重置落绝对／段串三态／体检同源／退休过渡／新键缺省补 | 票面验收命令 ＋ 两条补注的正面验收 |
| `test/config-695.test.mjs` | 默认值 3 键＋退休清单；首读／重置断言按写盘口径（绝对路径）；③ 产物名按常量断言 | 写盘口径变了，旧断言必须跟着走（#762 语义没变） |

`src/cli/cmd_read.ts` 一行没动：它 import 的三件在新 `paths.ts` 里原样保留，HELP 经 `buildChefHelpDelivery`
走常量主体（开工前影响清单第 4 行取消，对账偏差零）。

## 二、机器读数（门禁运行，逐条与 `.scratch/locks/gate-runs.log` 对账；导出件见 `t796-门禁运行记录.md`）

GATE-RUN runId=6d8e8a7f-09e8-453f-af4e-9ac63f58e9cb cmd=node node_modules/typescript/bin/tsc -b packages/skill-chef
GATE-RUN runId=b65dc4f3-e9f2-427c-bf6e-cf5f8d9f6117 cmd=node --test packages/skill-chef/test/*.test.mjs
GATE-RUN runId=909ad4c0-a273-4d16-b151-ec1af1c03775 cmd=node packages/skill-chef/dist/cli/cmd_read.js chef.config.read --params {}
GATE-RUN runId=9066a8fa-975f-48a7-b1c1-2d3a41bf0b24 cmd=node packages/skill-chef/dist/cli/cmd_read.js chef.config.check --params {}

| runId（首段） | 命令 | exit | 摘要 |
|---|---|---|---|
| `6d8e8a7f` | `tsc -b packages/skill-chef` | 0 | 编译过（3 键态） |
| `b65dc4f3` | 技能侧包级全量（3 键态终态） | 0 | `tests 67／pass 67／fail 0` |
| `cb4283a1` | 技能侧 t796 单件（3 键态） | 0 | `tests 10／pass 10／fail 0` |
| `787ec852` | `tsc -b` 双包（#766 落点吸收后） | 0 | 编译过 |
| `30bc1d58` | 技能侧包级全量（终态，4 键＋4 格） | 0 | `tests 68／pass 68／fail 0` |
| `909ad4c0` | 真出口 `chef.config.read`（3 键态） | 0 | `resolved` 三格，见下（历史读数） |
| `9c0da69a` | 真出口 `chef.config.read`（终态） | 0 | `resolved` 四格与真实配置逐字一致，见下 |
| `9066a8fa` | 真出口 `chef.config.check` | 0 | 无 `ILIFE_CONFIG_DIR`、无“页面上去改”，见下 |
| `59bea14b` | 变异（故意打掉 `resolved` 组） | 1 | 红（`resolved` 缺席即 `TypeError`，用例全红） |
| `5c116f86` | 变异还原后重跑 | 0 | `tests 10／pass 10／fail 0` |

### 真出口回执（终态逐字，`created:false` 且真实文件 mtime 未动＝读不写回）

```
{"version":"0.1.0","skill":"chef","shape":"detail","key":"chef.config.read","data":{
 "path":"C:\\Users\\辰辰洋洋\\.ilife\\chef.yaml","dataDir":"C:\\Users\\辰辰洋洋\\.ilife\\data","created":false,
 "values":{"db":{"dir":"","name":"chef_data.db"},"html":{"dir":"cook_html/help","sceneDir":"cook_html"}},
 "resolved":{"dbDir":"C:\\Users\\辰辰洋洋\\.ilife\\data",
             "dbFile":"C:\\Users\\辰辰洋洋\\.ilife\\data\\chef_data.db",
             "htmlDir":"C:\\Users\\辰辰洋洋\\.ilife\\data\\cook_html\\help",
             "sceneDir":"C:\\Users\\辰辰洋洋\\.ilife\\data\\cook_html"}}}
```

对账：真实文件是 09-19 落的**老文件**（无 `files` 组、无 `sceneDir` 键），读回来取值已按新表补齐——
退休过渡与新键缺省补在真实文件上当场生效；`db.dir` 空串 ⇒ 四格全落默认数据目录下。
真实文件读后 mtime 未动。

### 真出口体检（逐字，`db.file` 那条红是预期内，见 §五.1）

`config.file` 绿（能解析）；`db.dir` 绿（在且能写）；`db.file` 红（不在：新默认落点下无库）；
`html.dir` 绿（还没建）；`value.source` 照旧；`templates.dir`／`scenarios.file` 绿。全文无
`ILIFE_CONFIG_DIR`，`html.dir` 无行动指引（空串），`db.file` 指引不含“改到别处”。

## 三、变异自证（源码级：打掉一处 ⇒ 必红；还原 ⇒ 必绿）

| 变异 | 红读数 | 还原读数 |
|---|---|---|
| `src/cli/config.ts` 读分支去掉 `resolved` 组 | runId `59bea14b` exit=1（`resolved` 缺席，t796 全红） | 逐字还原 ＋ `tsc -b`（runId `c956cdb3` exit=0）⇒ runId `5c116f86` exit=0 `10/10` |

## 四、结构纪律（必报五步）

- **第一步影响清单**（开工前已报）：技能侧 6 件（`config`／`fetch/paths`／`cli/config`／`help/manifest`／
  `health`＋新测试），插件侧 3 件＋ 2 测试，证据 4 件——每行一件，零跨件。事中取消 1 行
  （`cli/cmd_read.ts`：import 面不变，无需改动），偏差已在 §一记明。
- **第二步结构设计**：不新建目录；`fetch/paths.ts` 升为落点算式唯一定义地（纯算式 ＋ 读配置薄壳），
  体检与 HELP 段值都转调它。对外面没收窄也没放宽（`src/index.ts` 与 `package.json` 的 `exports` 一字未动；
  `manifest.ts` 新增的两个常量不经 `help/index.ts` 对外）。
- **第四步超线报警**：**已超线，需要根据规则进行重构。** `src/health.ts` 461 行（LF 口径）——
  超因：受限子集解析器／目录与库探针／报告装配三摊活同处一件；拆法＝按三摊切三件姊妹件。
  **本次先不拆**（判据对齐窗口内大搬会把「判据住技能侧」搅成合成读数，待收口票认领；本票净减 38 行 499→461）。
  其余件均线内（`config.ts` 106／`paths.ts` 79／`cli/config.ts` 82／`manifest.ts` 36）。
- **第五步交付对账**：实得改动件与清单逐行一致（含取消的 1 行说明），偏差为零。

## 五、范围外发现／如实记

1. **真实 `db.file` 红是预期内**：新默认落点 `~/.ilife/data/chef_data.db` 现不存在；老库在
   `D:\2Study\StudyNotes\.db\chef_data.db`（#795 已裁：在面板上把数据目录指过去，本图不搬数据）。
2. **在途席共存**：`f2f3f83f` 等待 #757 席（`waitedMs=10009`）、`c956cdb3`／`b65dc4f3` 等待 #754 席
   （`waitedMs=10005／20014`）——活进程一律不抢回，等释放后跑，读数不受影响（#757 碰卡路里件、
   #754 碰环境变量件，均不在本票路径集合里）。
3. **无主 diff 接管**：开窗时树上有已关 #763 扫荡遗留的未提交改动（含本票 7 件里的注释级行，
   其中 `health.ts` 那句正是补注要的终态）；持锁 pid 已死、锁空闲，按规则 39 作无主 diff 接管为“补齐”，
   本提交信息与本节具名披露，接管者保持非受益者身份。提交只含本票声明路径。
4. **#766 串行碰撞与吸收（已落地 `f2f22a7e`）**：本席验证终态后，#766 席向同一文件
   `src/config.ts` 追加 `html.sceneDir`（09:21–09:24 在途），并一度暂存含本席在途行的混合树。
   本席按串行约束停手并发起协调（#766 下评论 `5754247820`）；#766 席以提交 `f2f22a7e` 落点，
   提交信息具名“config/test 含 796 在途行、原样保留”。本席随后按本票遗留出口吸收第 4 格
   （`sceneDirOf`／`resolveSceneDir`／`resolved.sceneDir`，与 `htmlDirOf` 同形；面板第 4 行见插件侧证据），
   重跑门禁复绿（`787ec852`／`30bc1d58`／`9c0da69a`）。归属：`src/config.ts` 与 695 测试的 #796 部分
   随 `f2f22a7e` 先行入仓（其提交信息已披露），本票提交只含其余声明路径，本节与提交信息双处具名。
4. **中间红是本席调试轮**（`c5ede05f`／`2d620678`／`c2896a3d`）：测试先行、实现跟上，非门禁证据，
   不计入 §二（门禁证据只认 exit=0 的 runId；故意变异红 `59bea14b` 另计 §三）。

## 六、未做项（如实记）

1. **发版与装机**：归 #755（本图末段收口），本票只交源码与读数。
2. **真机点屏**：本席无 GUI 点击能力。面板行为已用真产物量过（插件侧证据件），真机那一跳按先例归维护者。
3. **`html.sceneDir` 第四行**：#766 已落点（`f2f22a7e`），本票已按遗留出口顺带加第 4 只读行
   （标题「场景产物目录」与「HELP 产物目录」区分开，断言见插件侧 t796-①）。
