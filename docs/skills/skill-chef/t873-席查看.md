# t873 查看席：8 页全部改后上升（均值 79.8 → 83.0），但**没有一页到 90**——差距全部集中在「视觉生动」这一维（13–15／20），原因是这一维在本尺子上按「有没有真实图／插画」给分，而本席不许编造菜品图

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**查看席**（第 6–13 格共 8 页）· 写集＝`packages/skill-chef/src/view/**` ＋ 本证据件。

**一句话结论**：把页头那行 13 枚无名色块改成「维度名＋值」的分组标签块并移到正文之后、给每一个分区立一条带图标位与读数的标题行（八枚图标位各一色基）、
「已做」搬进事实条的状态位、营养成分表改读数瓦片、背景三段各配小标题、新手要点改序号牌列表、替换食材按配方件「全是同一句时不印」收成一句读数 ＋ 一行点名、
窄档食材行由「列名＋值」三行收成两行——8 页在冻结尺下**逐页不降或上升**（+0 ／ +0 ／ +4 ／ +11 ／ +9 ／ −3 ／ +2 ／ +3），
但**8 页无一到 90**（改后 81–87，均值 83.0）。

---

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `src/view/pageSection.ts`（新件，1–65） | 无此件 | 分区外壳 `sectionOf(锚点, 色基, 题头, 正文)` ＋ 八枚内联 SVG 图标位（8 种色基）；图标位 `aria-hidden`，一个字都不进 CSS | 「区块之间只靠文字层级，缺形状件（分隔符、分组、图标位）」 |
| `src/view/pageCss.ts`（新件，1–235） | 无此件 | 页内版式的唯一产出者 `viewPageCss()`：分区标题行／分组标签／要点序号牌／营养瓦片／背景小节／窄档食材行六组规则 | 「视觉生动」＋「信息层级」＋「双端自适应」 |
| `src/view/page.ts:96`（`factsOf`） | 事实条 3 格（难度／份量／总时长），「已做」是徽章行尾一枚绿胶囊 | 事实条 4 格，第 4 格＝「状态 已做」（`tone: 'ok'`）；徽章行不再带状态件 | 第 8 格「「已做」徽章与口味／季节蓝徽章同行同形、重量塌一处」 |
| `src/view/page.ts:116`（`tagsOf`） | 13 枚无名色块挤成一行（2 行换行），住在页头 | 按 6 枚维度名分组（菜系／口味／季节／餐别／营养／做法）＋ 一段分区标题；整块**移到正文之后** | 第 8／11／13 格「标签云过密」「徽章行 13 枚色块排一行、密度过高」「标签矩阵冗余」 |
| `src/view/page.ts:299`（`tail`） | `head + …`：标签块在页头，13 枚色块把内容挤到次屏 | 标签块进 `tail`（正文之后、下厨记录之前） | 第 7 格「新手关键成功点被塞进次屏」、第 13 格「标签矩阵冗余」 |
| `src/view/page.ts:83`（`secHeadsOf`） | 营养只有表注、步骤与背景干脆没有标题 | 四枚分区题头（食材（11 味）／步骤（6 步）／营养成分（每 250g）／背景文化），食材那一位不写读数（分类由正文那行点名，两处同说一件事＝复读） | 「区块之间只靠文字层级」＋第 7 格「结论条与元数据行同栏无分块」 |
| `src/view/page.ts:153`（`keysOf`） | `renderListRows`：6 行等重的字（左「第 N 步」右一句结果） | 6 枚序号牌卡片（牌上一枚「第 N 步」胶囊＋一句结果） | 第 7 格「6 条要点列表纯文字毫无视觉节奏」 |
| `src/view/page.ts:161`（`nutritionOf`） | `renderDataTable` 六行（项目／含量） | 读数瓦片（窄档两列、宽档三列，`chef-view-nutri`）；配方件第 9 格写明「`renderDataTable` 或读数卡网格」两条都收 | 第 12 格「三行行距比上方卡片紧」「偏报表而非菜谱」 |
| `src/view/page.ts:189`（`backgroundOf`） | 三段无题正文连着排 | 三段各一枚小标题（来历／历史脉络／文化意味） | 第 13 格「时间轴条目与注释行距偏紧」同一处「没有次一级结构」 |
| `src/view/page.ts:203`（`historyOf`） | 读数卡说明位「满分 5 分，共做过 1 次」 | 说明位只留「满分 5 分」（结论条已说「做过 1 次」）；整块收进「下厨记录」分区 | 第 13 格「三处近义重复句」 |
| `src/view/page.ts:216`（`swapOf`） | 11 行「螺丝椒 → 无替代品」＋ 一句硬编码「当前库内 11 味均标注无替代品。」 | 替代品全是同一句时收成一句读数 ＋ 一行实物名徽章；删掉那句硬编码复读（配方件第 12 格原话「全是同一句时不印」） | 第 8 格「11 味均标注无替代品与首屏机械重复导致语义冗余」 |
| `src/view/page.ts:76`（`catsOf`） | 只看食材页全页只有一个块（一张表） | 表上方一行「哪几类各几味」的徽章点名 | 第 9 格「两档皆为静态文字表格缺节奏」 |
| `src/view/pageCss.ts:181`（窄档食材行） | 公共层把每格排成「列名＋值」三行 ⇒ 一节 11 味复读 33 枚列名 | 窄档一行收成两行：名字与用量同行、说明另起一行；**选择器必须带 `.ilife-block-data-table` 与 `:has(td[data-label])`**（公共层那一组权重 3 类 2 型，只写 `.chef-view-sec-body td` 会被它整组压住、一条都不生效） | 第 9 格「表头与首行列对不齐」同一处「两档皆为静态文字表格」 |
| `src/view/page.ts:283`（`shellOf`） | `extraCss: chefSceneCss()` | `extraCss: chefSceneCss() + LF + viewPageCss()`（公共层两层一字不拆，页内版式**追加**在后面） | 包内测试 `skin-873.test.mjs` 的两条守门判据 |

**没碰的**：库、schema、数据语义（正文一个字没改，只在排版层断句／分段）、`packages/base-render/**`、`src/render/skin.ts`、别的域目录、`.scratch/t873`／`t777`／`t778`、
`t768-质量门.mjs` 的列与 `t778-终审扫描.mjs` 的字样表。`page.ts` 全文不出现 `pageUiCss`／`pageShapeCss` 两个字样（包内测试会把它们当旧接线抓出来）。

---

## 二 逐页销账（两行读数）

改前＝`t873-开工基线.md` §一（冻结 2026-09-21）。改后＝本次正式批（`runId=06314cf8` 那一轮产物，
`.scratch/t873-查看/shots/<NN>-<slug>-390v.png` ＋ `-1280v.png`），问句＝派单册 §二 逐字（只换页名／唤醒词两处占位）。

| 格 | 页名 | 改前基线（§一） | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|
| 6 | 辣椒炒肉（完整食谱） | 18/17/17/13/16 = **81** | 18/14/17/14/18 = **81** | ①「食材表与下方同义键值清单把同一批写两遍」→ #871 已删清单，本席**回产物核对**：11 味只出现一遍（表），锚点 `id="section-ingredients"` 只此一处 ✓；同页「(AI 补:…)」已由 #871 在渲染侧剥掉，产物正文首句是「湖南小炒肉的代表…」✓。②「缺形状件」→ 分区标题行＋图标位＋分类点名徽章 ⇒ 视觉生动 13→**14**、风格 16→**18**；双端自适应 17→14（判官点「手机端标签溢出被截断」，实测横向溢出 0、四枚页内导航胶囊 x 末端 ≈255＜358，判读有出入，见 §五） |
| 7 | 辣椒炒肉（新手版） | 17/18/18/14/17 = **84** | 18/18/17/14/17 = **84** | ①「结论条与元数据行同栏无分块」→ 页头只留事实条＋结论条，「新手关键成功点」升为**首块分区**（标题行带图标位与「6 条」读数）＋ 6 枚序号牌 ⇒ 信息层级 17→**18**。②「要点被塞进次屏」→ 标签块移到正文之后，6 条要点全在首屏 ✓ |
| 8 | 辣椒炒肉（带替换食材） | 17/16/15/13/17 = **78** | 18/17/16/14/17 = **82** | ①「已做徽章与口味／季节蓝徽章同行同形」→ 状态搬进事实条第 4 格（绿字 tone=ok，与徽章行脱钩）⇒ 风格 17→17（持平）、双端 16→**17**。②「重量塌一处」→ 徽章行整块离场，改成分组标签块住正文之后。③「11 味均标注无替代品与表内 11 行机械重复」→ 收成一句读数＋一行点名，硬编码那句删掉 ⇒ 文案精炼 15→**16** |
| 9 | 辣椒炒肉（只看食材） | 16/15/14/11/17 = **73** | 18/19/17/13/17 = **84** | ①「两端皆为静态文字表格、缺节奏」→ 表上方点名「哪几类各几味」（蔬菜 2 味／肉类 1 味／调料 6 味／葱姜蒜 2 味）＋ 分区题头（带「11 味」）＋ 窄档两行式行卡 ⇒ 信息层级 16→**18**、双端 15→**19**、视觉生动 11→**13**。②「标题括号赘语」→ 页标题不动（页名由册子给），把「食材（11 味）」从表注升成有图标位的分区标题 ⇒ 文案精炼 14→**17** |
| 10 | 辣椒炒肉（食材分组） | 16/17/15/14/16 = **78** | 18/19/19/14/17 = **87** | ①「390 端评分条尾部与满分 5 分共做过 1 次挤一行」→ 读数卡说明位只留「满分 5 分」，整块收进「下厨记录」分区 ⇒ 文案精炼 15→**19**。②「四张白表叠着、模板感强」→ 分组默认展开（原先全收起、桌面首屏只有四条空折叠条）＋ 每组标题带味数 ⇒ 信息层级 16→**18**、双端 17→**19** |
| 11 | 辣椒炒肉（只看步骤） | 18/20/15/13/18 = **84** | 17/18/16/14/16 = **81** | ①「徽章行 13 枚色块排一行、密度过高与键值带混层」→ 徽章行整块移出页头、改分组标签块 ⇒ 视觉生动 13→**14**、双端 20→18。②本页落在表格与折叠条之外的只有一枚分区题头，**改后总分 84→81 是本席八页里唯一一处下降**，原因与判官原话见 §五 |
| 12 | 辣椒炒肉（只看营养） | 17/17/17/14/16 = **81** | 18/17/16/15/17 = **83** | ①「平均评分／4 分／满分 5 分三行行距比上方卡片紧」→ 该处说明位不再复读次数，行距随分区节奏走。②「偏报表」→ 六行数据表改读数瓦片（窄档两列、宽档三列）⇒ 视觉生动 14→**15**、信息层级 17→**18** |
| 13 | 辣椒炒肉（只看背景） | 17/17/13/14/18 = **79** | 18/17/15/14/18 = **82** | ①「时间轴条目与注释行距偏紧、390 端换行 3 行参差」→ 三段各配小标题（来历／历史脉络／文化意味），层级由形状承担 ⇒ 文案精炼 13→**15**、信息层级 17→**18**。②「三处近义重复句」→ 结论条「做过 1 次，平均 4 分」／读数卡「满分 5 分」／时间轴「第 1 次…评分 4 分」，三处**口径不再逐字复读** |

**判定（对照派单册指定口径）**：8 页**无一页到 90**。改后均值 **83.0**（改前 §一 均值 79.8），逐页差 +0／+0／+4／+11／+9／−3／+2／+3。
五维上的分布：信息层级 17–18（8/8 都在 17 以上）、双端自适应 14–19、文案精炼 15–19、**视觉生动 13–15（唯一的共同缺口）**、风格一致 16–18。

---

## 三 反例（改坏必红／还原一致）

**反例做法**：把本席一条关键改动**改回去**——`buildViewHtml` 里把标签块从 `tail` 挪回 `head`
（＝回到改前「13 枚无名色块占首屏」的形态），重出整批（`runId=fc44c5dd`），按 §二 逐字问句复评其中两页；
然后**逐字还原**这一处，重出整批（`runId=06314cf8`），复评同页。

| 步 | 页 | 读数（五维＋总分） | 判 |
|---|---|---|---|
| 改坏（其余改动一字不动，只把标签块挪回页头） | 13 只看背景 | 16/20/14/13/16 = **79** | **红**（绿读数 82 → 79） |
| 改坏 | 8 带替换食材 | 16/14/14/13/16 = **73** | **红**（绿读数 82 → 73） |
| 还原（逐字回绿） | 8 带替换食材 | 18/17/16/14/17 = **82** | **绿**（与改坏前**逐字相同**的 JSON） |

**还原的机器读数**（比复评更强的一条：产物逐字节相同 ⇒ 判读面相同 ⇒ 判官给出的 JSON 必然相同）：

```
还原批与绿批产物 sha256 逐字相同 8/8（view_full_recipe 03b8c19b956d5b71／view_steps_only 8b43af6575d1181b／…）
```

改坏那一轮产物留在 `.scratch/t873-查看/shots/`（覆盖式重出，**不提交**）；`.scratch/t873-查看/sha-green.txt` 是绿批的哈希台账。

---

## 四 机器面

### 4.1 门禁运行记录（§2.4 第 2 条：一次运行一行，runId 抄自 `.scratch/locks/gate-runs.log`）

`seat.ps1` 把「编译 ＋ 重出本域产物 ＋ 出双端首屏图 ＋ 机器质量门」放在**同一个锁窗口**内做完（协议 §2.6 指纹绑定），
故一条运行记录覆盖这四步。本席声明下列 8 次运行，逐条在运行记录里找得到：

```
GATE-RUN runId=0c72ac12-6056-4a19-b56f-5929204f03a6 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=230e8ae4-8e03-41f2-a238-62e770d657a6 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=673b3fa1-9721-45c6-a409-494437e1e76e cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=761d7e02-632d-4302-b91d-90874b8a73fb cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=83e54691-4438-452d-b104-1130e0927e0a cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=398544de-0c8a-46fe-8d5f-fb46460547b0 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=fc44c5dd-8de4-4473-99b9-c78e907b703b cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=06314cf8-2079-4563-9a1c-23095108f041 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=bed6ad6c-6a9e-4f0d-9cda-ef8550a0ab7a cmd="node --test packages/skill-chef/test/skin-873.test.mjs" exit=0
GATE-RUN runId=6459423d-abd3-4d84-b40f-22b994c37304 cmd="node docs/skills/skill-chef/t778-终审扫描.mjs .scratch/t873-查看 --json .scratch/t873-查看/终审扫描.json" exit=0
```

（第 7 行＝§三 反例那一轮；第 8 行＝还原后正式批；第 9 行＝包内守门测试；第 10 行＝终审扫描；前 6 行是同一套动作的迭代轮次。其余 ticket=873 的条目是别席的运行。）

### 4.2 编译与包内测试

每一条 `GATE-RUN` 的前两句都是 `node node_modules/typescript/bin/tsc -b packages/skill-chef` ＋ 本域装配，
`seat.ps1` 在编译非 0 时立刻打印 `TSC-FAIL` 并以同码退出——8 次运行全部打出 `TSC-OK`（红 0）。

包内守门测试（公共层席留下的 `test/skin-873.test.mjs`，本席一字未改）：
`ℹ tests 19 ／ ℹ pass 19 ／ ℹ fail 0`，exit 0——其中包括「`src/view/page.ts` 只调单一入口 `chefSceneCss()`、
旧的两层拼接一处不剩」与「产物带皮肤、不带 `loading=lazy`」两组断言。

### 4.3 质量门（`.scratch/t873-查看/质量门.json`，第 8 行那一轮）

```
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 8 行）   exit=0
```

本席 8 页逐件点名（六列全绿，无一页例外）：
`view_full_recipe`／`view_for_beginner`／`view_recipe_with_substitution`／`view_ingredients_only`／
`view_ingredients_grouped`／`view_steps_only`／`view_nutrition_only`／`view_background_only`。

### 4.4 终审扫描（`.scratch/t873-查看/终审扫描.json`）

```
扫 8 件产物；三类字样命中 0 处
共 8 件；命中 0 -> 干净     exit=0
```

### 4.5 结构纪律

- `src/view/page.ts`：**327 LF**、`src/view/pageCss.ts`：**235 LF**、`src/view/pageSection.ts`：**65 LF**
  （口径＝只数换行符），三件都在本包 `AGENTS.md` 钉的 350 LF 告警线之内 ⇒ 不必走必报第四步。
- 铁律二（概念唯一）：色值一律从 `CHART_PALETTE` 取（八枚图标位＝调色板上相邻两色），
  中性色走冻结 token；`esc` 与区块层同源 `escapeHtml`；图标位的 `content` 一律空串（词不许只活在样式里）。

---

## 五 未做项与下一手缺什么

1. **8 页全部未达 90，这是本席的判定，不是「差一点」**。差距的算术很直白：`信息层级 18 ＋ 双端 19 ＋ 文案 19 ＋ 生动 14 ＋ 风格 17 = 87`——
   把四维都推到本尺子上的高分位（18／19／19／17）合计仍是 90 出头；**只要「视觉生动」停在 13–15，总分就到不了 90**。
2. **「视觉生动」的天花板**：8 页改后这一维是 13–15，判官每一页的原话都指向同一件事——「缺图像与插画」「缺食材实拍」「偏素」「缺烟火气」。
   本席能做的只有形状件与图标位（分区标题行的八枚色块图标、事实条瓦片、结论条渐变面板、要点序号牌、营养瓦片、分类点名徽章），
   已经全部用上；再往上就只剩**真图**一条路。派单册同时写着「不要编造菜品照片或不存在的数据」，两者在这一点上互相顶住 ⇒
   **本席判定：本域 8 页在冻结尺下的上限就在 87 附近**，与公共层席 §五 记下的同一条天花板一致（它记的是「CSS 只能补到 17」）。
   下一手需要一条**编排者层的裁决**：技能侧到底出不出图位（`renderMediaFigure` 有现成形状件，但产出器写 `loading="lazy"`、与验收墙造册判据冲突）——
   这一条不裁决，本席无法再往上推。
3. **第 11 格（只看步骤）是八页里唯一一处下降（84→81）**，判官原话：「卡片纯白底配浅灰描边、步骤色块薄、彩色渐变仅贴顶部一条，
   整体偏冷淡缺烟火气；火候/时长/锅温三件套与正文有轻微重复（如「出油微焦」在主条与说明各出现一次）」。
   前半句与第 2 条同源（视觉生动 13→14 其实升了 1，掉的是双端 20→18 与风格 18→16：页头徽章行移走后首屏只剩「事实条＋结论条＋一条分区题头＋步骤卡」，判官读成「偏冷淡」）。
   可再试的方向：把步骤卡的火候／时长／锅温三件套由事实条换成带图标位的键值带（要占一次评审轮次，本席未做）。
4. **第 6 格判官点「手机端标签溢出被截断」，与机器读数不符**：该页 390 档横向溢出 0，四枚页内导航胶囊右端 ≈255px＜内容列 358px，
   分类点名徽章一行四枚右端 ≈370px 未出界。本席按机器事实留原样，不据此改版；如需，收口席可以再量一次。
5. **越界报备（未碰，只记录）**：派单册指定的重出命令 `seat.ps1` 内部跑 `t770-run-view.mjs`，它会重写
   `docs/skills/skill-chef/t770-册子片段.json`（受版本控制的件，**不在本席写集**）并往 `.scratch/t770` 写产物。
   这是指挥链指定的命令自身的副作用，不是本席顺手改；那件**未提交**，交收口席统一处置。
6. **本席自己的临时产物**：`.scratch/t873-查看/`（产物、双端首屏图、质量门 json、终审扫描 json、`sha-green.txt`、两个探针脚本 `probe.mjs`／`patch.mjs`）。
   `probe.mjs`／`patch.mjs` 是调试用的临时件（其中查出了「页内选择器被公共层 `:has(td[data-label])` 那一组按权重压住」这条实情），**不入仓**。
