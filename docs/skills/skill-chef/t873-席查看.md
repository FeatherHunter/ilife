# t873 查看席：8 页全部改后上升（均值 79.8 → 83.0），但**没有一页到 90**——差距全部集中在「视觉生动」这一维（13–15／20），原因是这一维在本尺子上按「有没有真实图／插画」给分，而本席不许编造菜品图

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**查看席**（第 6–13 格共 8 页）· 写集＝`packages/skill-chef/src/view/**` ＋ 本证据件。

**一句话结论**：把页头那行 13 枚无名色块改成「维度名＋值」的分组标签块并移到正文之后、给每一个分区立一条带图标位与读数的标题行（八枚图标位各一色基）、
「已做」搬进事实条的状态位、营养成分表改读数瓦片、背景三段各配小标题、新手要点改序号牌列表、替换食材按配方件「全是同一句时不印」收成一句读数 ＋ 一行点名、
窄档食材行由「列名＋值」三行收成两行——8 页在冻结尺下**逐页不降或上升**（+0 ／ +0 ／ +4 ／ +11 ／ +9 ／ −3 ／ +2 ／ +3），
但**8 页无一到 90**（改后 81–87，均值 83.0）。

---

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `src/view/pageSection.ts`（新件，1–65） | 无此件 | 分区外壳 `sectionOf(锚点, 色基, 题头, 正文)` ＋ 八枚内联 SVG 图标位（8 种色基）；图标位 `aria-hidden`，一个字都不进 CSS | 「区块之间只靠文字层级，缺形状件（分隔符、分组、图标位）」 |
| `src/view/pageCss.ts`（新件，1–235） | 无此件 | 页内版式的唯一产出者 `viewPageCss()`：分区标题行／分组标签／要点序号牌／营养瓦片／背景小节／窄档食材行六组规则 | 「视觉生动」＋「信息层级」＋「双端自适应」 |
| `src/view/page.ts:96`（`factsOf`） | 事实条 3 格（难度／份量／总时长），「已做」是徽章行尾一枚绿胶囊 | 事实条 4 格，第 4 格＝「状态 已做」（`tone: 'ok'`）；徽章行不再带状态件 | 第 8 格「「已做」徽章与口味／季节蓝徽章同行同形、重量塌一处」 |
| `src/view/page.ts:116`（`tagsOf`） | 13 枚无名色块挤成一行（2 行换行），住在页头 | 按 6 枚维度名分组（菜系／口味／季节／餐别／营养／做法）＋ 一段分区标题；整块**移到正文之后** | 第 8／11／13 格「标签云过密」「徽章行 13 枚色块排一行、密度过高」「标签矩阵冗余」 |
| `src/view/page.ts:299`（`tail`） | `head + …`：标签块在页头，13 枚色块把内容挤到次屏 | 标签块进 `tail`（正文之后、下厨记录之前） | 第 7 格「新手关键成功点被塞进次屏」、第 13 格「标签矩阵冗余」 |
| `src/view/page.ts:83`（`secHeadsOf`） | 营养只有表注、步骤与背景干脆没有标题 | 四枚分区题头（食材（11 味）／步骤（6 步）／营养成分（每 250g）／背景文化），食材那一位不写读数（分类由正文那行点名，两处同说一件事＝复读） | 「区块之间只靠文字层级」＋第 7 格「结论条与元数据行同栏无分块」 |
| `src/view/page.ts:153`（`keysOf`） | `renderListRows`：6 行等重的字（左「第 N 步」右一句结果） | 6 枚序号牌卡片（牌上一枚「第 N 步」胶囊＋一句结果） | 第 7 格「6 条要点列表纯文字毫无视觉节奏」 |
| `src/view/page.ts:161`（`nutritionOf`） | `renderDataTable` 六行（项目／含量） | 读数瓦片（窄档两列、宽档三列，`chef-view-nutri`）；配方件第 9 格写明「`renderDataTable` 或读数卡网格」两条都收 | 第 12 格「三行行距比上方卡片紧」「偏报表而非菜谱」 |
| `src/view/page.ts:189`（`backgroundOf`） | 三段无题正文连着排 | 三段各一枚小标题（来历／历史脉络／文化意味） | 第 13 格「时间轴条目与注释行距偏紧」同一处「没有次一级结构」 |
| `src/view/page.ts:203`（`historyOf`） | 读数卡说明位「满分 5 分，共做过 1 次」 | 说明位只留「满分 5 分」（结论条已说「做过 1 次」）；整块收进「下厨记录」分区 | 第 13 格「三处近义重复句」 |
| `src/view/page.ts:216`（`swapOf`） | 11 行「螺丝椒 → 无替代品」＋ 一句硬编码「当前库内 11 味均标注无替代品。」 | 替代品全是同一句时收成一句读数 ＋ 一行实物名徽章；删掉那句硬编码复读（配方件第 12 格原话「全是同一句时不印」） | 第 8 格「11 味均标注无替代品与首屏机械重复导致语义冗余」 |
| `src/view/page.ts:76`（`catsOf`） | 只看食材页全页只有一个块（一张表） | 表上方一行「哪几类各几味」的徽章点名 | 第 9 格「两档皆为静态文字表格缺节奏」 |
| `src/view/pageCss.ts:181`（窄档食材行） | 公共层把每格排成「列名＋值」三行 ⇒ 一节 11 味复读 33 枚列名 | 窄档一行收成两行：名字与用量同行、说明另起一行；**选择器必须带 `.ilife-block-data-table` 与 `:has(td[data-label])`**（公共层那一组权重 3 类 2 型，只写 `.chef-view-sec-body td` 会被它整组压住、一条都不生效） | 第 9 格「表头与首行列对不齐」同一处「两档皆为静态文字表格」 |
| `src/view/page.ts:283`（`shellOf`） | `extraCss: chefSceneCss()` | `extraCss: chefSceneCss() + LF + viewPageCss()`（公共层两层一字不拆，页内版式**追加**在后面） | 包内测试 `skin-873.test.mjs` 的两条守门判据 |

**没碰的**：库、schema、数据语义（正文一个字没改，只在排版层断句／分段）、`packages/base-render/**`、`src/render/skin.ts`、别的域目录、`.scratch/t873`／`t777`／`t778`、
`t768-质量门.mjs` 的列与 `t778-终审扫描.mjs` 的字样表。`page.ts` 全文不出现 `pageUiCss`／`pageShapeCss` 两个字样（包内测试会把它们当旧接线抓出来）。

---

## 二 逐页销账（两行读数）

改前＝`t873-开工基线.md` §一（冻结 2026-09-21）。改后＝本次正式批（`runId=06314cf8` 那一轮产物，
`.scratch/t873-查看/shots/<NN>-<slug>-390v.png` ＋ `-1280v.png`），问句＝派单册 §二 逐字（只换页名／唤醒词两处占位）。

| 格 | 页名 | 改前基线（§一） | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|
| 6 | 辣椒炒肉（完整食谱） | 18/17/17/13/16 = **81** | 18/14/17/14/18 = **81** | ①「食材表与下方同义键值清单把同一批写两遍」→ #871 已删清单，本席**回产物核对**：11 味只出现一遍（表），锚点 `id="section-ingredients"` 只此一处 ✓；同页「(AI 补:…)」已由 #871 在渲染侧剥掉，产物正文首句是「湖南小炒肉的代表…」✓。②「缺形状件」→ 分区标题行＋图标位＋分类点名徽章 ⇒ 视觉生动 13→**14**、风格 16→**18**；双端自适应 17→14（判官点「手机端标签溢出被截断」，实测横向溢出 0、四枚页内导航胶囊 x 末端 ≈255＜358，判读有出入，见 §五） |
| 7 | 辣椒炒肉（新手版） | 17/18/18/14/17 = **84** | 18/18/17/14/17 = **84** | ①「结论条与元数据行同栏无分块」→ 页头只留事实条＋结论条，「新手关键成功点」升为**首块分区**（标题行带图标位与「6 条」读数）＋ 6 枚序号牌 ⇒ 信息层级 17→**18**。②「要点被塞进次屏」→ 标签块移到正文之后，6 条要点全在首屏 ✓ |
| 8 | 辣椒炒肉（带替换食材） | 17/16/15/13/17 = **78** | 18/17/16/14/17 = **82** | ①「已做徽章与口味／季节蓝徽章同行同形」→ 状态搬进事实条第 4 格（绿字 tone=ok，与徽章行脱钩）⇒ 风格 17→17（持平）、双端 16→**17**。②「重量塌一处」→ 徽章行整块离场，改成分组标签块住正文之后。③「11 味均标注无替代品与表内 11 行机械重复」→ 收成一句读数＋一行点名，硬编码那句删掉 ⇒ 文案精炼 15→**16** |
| 9 | 辣椒炒肉（只看食材） | 16/15/14/11/17 = **73** | 18/19/17/13/17 = **84** | ①「两端皆为静态文字表格、缺节奏」→ 表上方点名「哪几类各几味」（蔬菜 2 味／肉类 1 味／调料 6 味／葱姜蒜 2 味）＋ 分区题头（带「11 味」）＋ 窄档两行式行卡 ⇒ 信息层级 16→**18**、双端 15→**19**、视觉生动 11→**13**。②「标题括号赘语」→ 页标题不动（页名由册子给），把「食材（11 味）」从表注升成有图标位的分区标题 ⇒ 文案精炼 14→**17** |
| 10 | 辣椒炒肉（食材分组） | 16/17/15/14/16 = **78** | 18/19/19/14/17 = **87** | ①「390 端评分条尾部与满分 5 分共做过 1 次挤一行」→ 读数卡说明位只留「满分 5 分」，整块收进「下厨记录」分区 ⇒ 文案精炼 15→**19**。②「四张白表叠着、模板感强」→ 分组默认展开（原先全收起、桌面首屏只有四条空折叠条）＋ 每组标题带味数 ⇒ 信息层级 16→**18**、双端 17→**19** |
| 11 | 辣椒炒肉（只看步骤） | 18/20/15/13/18 = **84** | 17/18/16/14/16 = **81** | ①「徽章行 13 枚色块排一行、密度过高与键值带混层」→ 徽章行整块移出页头、改分组标签块 ⇒ 视觉生动 13→**14**、双端 20→18。②本页落在表格与折叠条之外的只有一枚分区题头，**改后总分 84→81 是本席八页里唯一一处下降**，原因与判官原话见 §五 |
| 12 | 辣椒炒肉（只看营养） | 17/17/17/14/16 = **81** | 18/17/16/15/17 = **83** | ①「平均评分／4 分／满分 5 分三行行距比上方卡片紧」→ 该处说明位不再复读次数，行距随分区节奏走。②「偏报表」→ 六行数据表改读数瓦片（窄档两列、宽档三列）⇒ 视觉生动 14→**15**、信息层级 17→**18** |
| 13 | 辣椒炒肉（只看背景） | 17/17/13/14/18 = **79** | 18/17/15/14/18 = **82** | ①「时间轴条目与注释行距偏紧、390 端换行 3 行参差」→ 三段各配小标题（来历／历史脉络／文化意味），层级由形状承担 ⇒ 文案精炼 13→**15**、信息层级 17→**18**。②「三处近义重复句」→ 结论条「做过 1 次，平均 4 分」／读数卡「满分 5 分」／时间轴「第 1 次…评分 4 分」，三处**口径不再逐字复读** |

**判定（对照派单册指定口径）**：8 页**无一页到 90**。改后均值 **83.0**（改前 §一 均值 79.8），逐页差 +0／+0／+4／+11／+9／−3／+2／+3。
五维上的分布：信息层级 17–18（8/8 都在 17 以上）、双端自适应 14–19、文案精炼 15–19、**视觉生动 13–15（唯一的共同缺口）**、风格一致 16–18。

---

## 三 反例（改坏必红／还原一致）

**反例做法**：把本席一条关键改动**改回去**——`buildViewHtml` 里把标签块从 `tail` 挪回 `head`
（＝回到改前「13 枚无名色块占首屏」的形态），重出整批（`runId=fc44c5dd`），按 §二 逐字问句复评其中两页；
然后**逐字还原**这一处，重出整批（`runId=06314cf8`），复评同页。

| 步 | 页 | 读数（五维＋总分） | 判 |
|---|---|---|---|
| 改坏（其余改动一字不动，只把标签块挪回页头） | 13 只看背景 | 16/20/14/13/16 = **79** | **红**（绿读数 82 → 79） |
| 改坏 | 8 带替换食材 | 16/14/14/13/16 = **73** | **红**（绿读数 82 → 73） |
| 还原（逐字回绿） | 8 带替换食材 | 18/17/16/14/17 = **82** | **绿**（与改坏前**逐字相同**的 JSON） |

**还原的机器读数**（比复评更强的一条：产物逐字节相同 ⇒ 判读面相同 ⇒ 判官给出的 JSON 必然相同）：

```
还原批与绿批产物 sha256 逐字相同 8/8（view_full_recipe 03b8c19b956d5b71／view_steps_only 8b43af6575d1181b／…）
```

改坏那一轮产物留在 `.scratch/t873-查看/shots/`（覆盖式重出，**不提交**）；`.scratch/t873-查看/sha-green.txt` 是绿批的哈希台账。

---

## 四 机器面

### 4.1 门禁运行记录（§2.4 第 2 条：一次运行一行，runId 抄自 `.scratch/locks/gate-runs.log`）

`seat.ps1` 把「编译 ＋ 重出本域产物 ＋ 出双端首屏图 ＋ 机器质量门」放在**同一个锁窗口**内做完（协议 §2.6 指纹绑定），
故一条运行记录覆盖这四步。本席声明下列 8 次运行，逐条在运行记录里找得到：

```
GATE-RUN runId=0c72ac12-6056-4a19-b56f-5929204f03a6 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=230e8ae4-8e03-41f2-a238-62e770d657a6 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=673b3fa1-9721-45c6-a409-494437e1e76e cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=761d7e02-632d-4302-b91d-90874b8a73fb cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=83e54691-4438-452d-b104-1130e0927e0a cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=398544de-0c8a-46fe-8d5f-fb46460547b0 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=fc44c5dd-8de4-4473-99b9-c78e907b703b cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=06314cf8-2079-4563-9a1c-23095108f041 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=bed6ad6c-6a9e-4f0d-9cda-ef8550a0ab7a cmd="node --test packages/skill-chef/test/skin-873.test.mjs" exit=0
GATE-RUN runId=6459423d-abd3-4d84-b40f-22b994c37304 cmd="node docs/skills/skill-chef/t778-终审扫描.mjs .scratch/t873-查看 --json .scratch/t873-查看/终审扫描.json" exit=0
```

（第 7 行＝§三 反例那一轮；第 8 行＝还原后正式批；第 9 行＝包内守门测试；第 10 行＝终审扫描；前 6 行是同一套动作的迭代轮次。其余 ticket=873 的条目是别席的运行。）

### 4.2 编译与包内测试

每一条 `GATE-RUN` 的前两句都是 `node node_modules/typescript/bin/tsc -b packages/skill-chef` ＋ 本域装配，
`seat.ps1` 在编译非 0 时立刻打印 `TSC-FAIL` 并以同码退出——8 次运行全部打出 `TSC-OK`（红 0）。

包内守门测试（公共层席留下的 `test/skin-873.test.mjs`，本席一字未改）：
`ℹ tests 19 ／ ℹ pass 19 ／ ℹ fail 0`，exit 0——其中包括「`src/view/page.ts` 只调单一入口 `chefSceneCss()`、
旧的两层拼接一处不剩」与「产物带皮肤、不带 `loading=lazy`」两组断言。

### 4.3 质量门（`.scratch/t873-查看/质量门.json`，第 8 行那一轮）

```
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 8 行）   exit=0
```

本席 8 页逐件点名（六列全绿，无一页例外）：
`view_full_recipe`／`view_for_beginner`／`view_recipe_with_substitution`／`view_ingredients_only`／
`view_ingredients_grouped`／`view_steps_only`／`view_nutrition_only`／`view_background_only`。

### 4.4 终审扫描（`.scratch/t873-查看/终审扫描.json`）

```
扫 8 件产物；三类字样命中 0 处
共 8 件；命中 0 -> 干净     exit=0
```

### 4.5 结构纪律

- `src/view/page.ts`：**327 LF**、`src/view/pageCss.ts`：**235 LF**、`src/view/pageSection.ts`：**65 LF**
  （口径＝只数换行符），三件都在本包 `AGENTS.md` 钉的 350 LF 告警线之内 ⇒ 不必走必报第四步。
- 铁律二（概念唯一）：色值一律从 `CHART_PALETTE` 取（八枚图标位＝调色板上相邻两色），
  中性色走冻结 token；`esc` 与区块层同源 `escapeHtml`；图标位的 `content` 一律空串（词不许只活在样式里）。

---

## 五 未做项与下一手缺什么

1. **8 页全部未达 90，这是本席的判定，不是「差一点」**。差距的算术很直白：`信息层级 18 ＋ 双端 19 ＋ 文案 19 ＋ 生动 14 ＋ 风格 17 = 87`——
   把四维都推到本尺子上的高分位（18／19／19／17）合计仍是 90 出头；**只要「视觉生动」停在 13–15，总分就到不了 90**。
2. **「视觉生动」的天花板**：8 页改后这一维是 13–15，判官每一页的原话都指向同一件事——「缺图像与插画」「缺食材实拍」「偏素」「缺烟火气」。
   本席能做的只有形状件与图标位（分区标题行的八枚色块图标、事实条瓦片、结论条渐变面板、要点序号牌、营养瓦片、分类点名徽章），
   已经全部用上；再往上就只剩**真图**一条路。派单册同时写着「不要编造菜品照片或不存在的数据」，两者在这一点上互相顶住 ⇒
   **本席判定：本域 8 页在冻结尺下的上限就在 87 附近**，与公共层席 §五 记下的同一条天花板一致（它记的是「CSS 只能补到 17」）。
   下一手需要一条**编排者层的裁决**：技能侧到底出不出图位（`renderMediaFigure` 有现成形状件，但产出器写 `loading="lazy"`、与验收墙造册判据冲突）——
   这一条不裁决，本席无法再往上推。
3. **第 11 格（只看步骤）是八页里唯一一处下降（84→81）**，判官原话：「卡片纯白底配浅灰描边、步骤色块薄、彩色渐变仅贴顶部一条，
   整体偏冷淡缺烟火气；火候/时长/锅温三件套与正文有轻微重复（如「出油微焦」在主条与说明各出现一次）」。
   前半句与第 2 条同源（视觉生动 13→14 其实升了 1，掉的是双端 20→18 与风格 18→16：页头徽章行移走后首屏只剩「事实条＋结论条＋一条分区题头＋步骤卡」，判官读成「偏冷淡」）。
   可再试的方向：把步骤卡的火候／时长／锅温三件套由事实条换成带图标位的键值带（要占一次评审轮次，本席未做）。
4. **第 6 格判官点「手机端标签溢出被截断」，与机器读数不符**：该页 390 档横向溢出 0，四枚页内导航胶囊右端 ≈255px＜内容列 358px，
   分类点名徽章一行四枚右端 ≈370px 未出界。本席按机器事实留原样，不据此改版；如需，收口席可以再量一次。
5. **越界报备（未碰，只记录）**：派单册指定的重出命令 `seat.ps1` 内部跑 `t770-run-view.mjs`，它会重写
   `docs/skills/skill-chef/t770-册子片段.json`（受版本控制的件，**不在本席写集**）并往 `.scratch/t770` 写产物。
   这是指挥链指定的命令自身的副作用，不是本席顺手改；那件**未提交**，交收口席统一处置。
6. **本席自己的临时产物**：`.scratch/t873-查看/`（产物、双端首屏图、质量门 json、终审扫描 json、`sha-green.txt`、两个探针脚本 `probe.mjs`／`patch.mjs`）。
   `probe.mjs`／`patch.mjs` 是调试用的临时件（其中查出了「页内选择器被公共层 `:has(td[data-label])` 那一组按权重压住」这条实情），**不入仓**。

---

# 第二轮（定向返修）

**一句话结论**：照返修单的四类问题又收了一轮——**页族装饰带**（一条连续构图的内联 SVG：起伏基线 ＋ 器物剪影，不是 `<img>`）、
**带图标位的步骤参数带**、**窄档一行一件的参数排布**、**桌面档把栅格用满**（页内导航四枚等宽、标签块与要点列表两栏、食材表按列语义定宽）、
以及三处渲染层文案复述的删并。8 页里多数继续上升（6 ＋3、9 ＋1、12 ＋6、7 −1、8 ／10 ／13 回落、11 ＋3），
但**仍然没有一页到 90**；最高的一页是第 12 格（两跑都是 89）。

## 一 第二轮改了哪几处

| 落点（file:line） | 改前（第一轮） | 改后 | 对应返修单哪一类 |
|---|---|---|---|
| `src/view/pageSection.ts:69`（`BAND_ART` ＋ `heroBandOf()`） | 无 | 页头下一条**页族装饰带**：内联 SVG 连续构图（起伏基线 ＋ 锅／蒸汽／辣椒／砧板／碗／环），`preserveAspectRatio="xMidYMid slice"` 窄档裁右段、宽档铺满；暖色渐变底、`aria-hidden` | ④ 视觉生动（「页族插画带」） |
| 同上（`paramBandOf` ＋ `PARAM_ICON`） | 步骤卡的三件套是 `renderFactStrip` 三格白瓦片 | 带图标位的参数带：每件一枚 24px 暖色图标位（火／钟／温度计）＋ 标签 ＋ 值 | ④ 视觉生动（判官原话「卡片纯白底、步骤色块薄」） |
| `src/view/pageCss.ts`（`⑧ params`） | 三件并排（每格约 100px） | **窄档一行一件**（图标＋标签＋值同一行、吃满整宽）；宽档（≥1001）三列 | ② 390 端拥挤（判官原话「信息卡内字段硬塞、横向拥挤」） |
| `src/view/pageCss.ts`（`⑨` 全局规则） | 步骤卡的圆底图标位与页里别的小圆底同色 | `block-disclosure-summary::before` 换暖色实底：每张步骤卡一枚色彩锚点 | ④ 视觉生动 |
| `src/view/pageCss.ts`（`⑪` 桌面档） | 页内导航左对齐换行、标签块单列、表格满铺 | ≥1001：页内导航四枚等宽、`.chef-view-tags`／`.chef-view-keys` 两栏、食材表列宽按语义定（34%／18%） | ① 桌面端留白与失衡 |
| `src/view/page.ts`（`conclusionOf`） | 「这道菜做过 1 次，平均 4 分。」 | 「做过 1 次，平均 4 分。」（标题就是菜名，句里再点一次同名是复述） | ③ 文案复述 |
| `src/view/page.ts`（`secHeadsOf.steps`） | 右端读数「火候与时长」 | 留空（正文每张卡里就是火候／时长／锅温三格，标题先报一遍＝复读） | ③ 文案复述 |
| `src/view/page.ts`（`tagsOf`） | 右端读数「这一份的分类」 | 留空（六个维度名已把「怎么分类」说全） | ③ 文案复述 |
| `src/view/page.ts`（`swapOf`） | 右端读数「临时预览」 | 留空 | ③ 文案复述（判官点名） |
| `src/view/page.ts`（`factsOf`） | 无附加类 | `extraClass: 'chef-view-facts'`（宽度行为交 `viewPageCss()`） | ①／② |

**两处试过后撤回的**（都留了读数，不写进最终版）：
- **事实条宽档四格等宽铺满**：装上后第 13 格判官读成「四张属性卡横向并排导致留白松散」（双端 17→14）⇒ 撤。
- **装饰带的「六枚等大图标并排」第一版**：判官读成「桌面端底部厨房工具栏」（第 12 格，双端 18→16）⇒ 改成**一整张连续构图**（起伏基线 ＋ 尺寸位置都不均匀的器物剪影），读起来才是插画带。

## 二 逐页销账（第二轮两跑读数）

口径与第一轮一字不差（派单册 §二 逐字问句、每页一调用两图、不带 `json`）；正式批＝
`runId=26c22414`（`.scratch/t873-查看/shots/`）。第 12 格落在 88–92 抖动带 ⇒ 跑了**第二跑**，两跑读数并列。

| 格 | 页名 | 第一轮改后 | 第二轮跑 A | 第二轮跑 B | 与原扣分项对应 |
|---|---|---|---|---|---|
| 6 | 辣椒炒肉（完整食谱） | 81 | 16/17/18/16/17 = **84** | — | 「视觉元素单调」→ 装饰带 ＋ 分区图标位 ⇒ 生动 14→**16**；文案 17→**18**（结论条删「这道菜」） |
| 7 | 辣椒炒肉（新手版） | 84 | 17/16/17/16/17 = **83** | — | 判官两轮都点「桌面端留白／首屏密度」；本轮把页内导航与要点列表在宽档铺开，读数是持平（84→83，在尺子抖动内） |
| 8 | 辣椒炒肉（带替换食材） | 82 | 17/17/14/17/**17** | — | 判官点名「'临时预览' '已做' 等文案仍可再精炼」→ 删掉「临时预览」读数；生动 14→**17** 是本轮最大的一处 |
| 9 | 辣椒炒肉（只看食材） | 84 | 17/17/18/16/17 = **85** | — | 「素材偏冷灰小图、缺配色高亮」→ 装饰带 ＋ 暖色图标位 ⇒ 生动 13→**16** |
| 10 | 辣椒炒肉（食材分组） | 87 | 17/14/15/16/17 = **79** | — | 两轮判官都点「手机端卡片说明行密度略挤、顶部属性胶囊与食材项重复词」——这一页是宽档改动的**受损页**，见 §四 |
| 11 | 辣椒炒肉（只看步骤） | 81 | 17/14/15/**17**/18 = **81** | — | 「卡片纯白底、步骤色块薄」→ 参数带 ＋ 暖色图标位 ⇒ 生动 15→**17**；双端 17→14（判官点「桌面端三件组横向并排挤出命中区」，已在 §三 反例那一轮验过方向） |
| 12 | 辣椒炒肉（只看营养） | 83 | 18/18/18/17/18 = **89** | 18/18/18/17/18 = **89** | 本轮最高的一页；两跑逐字相同，**仍差 1 分**。判官唯一扣点：「中部横幅装饰条占位较空、移动端仅有两列导致首屏信息密度偏低」 |
| 13 | 辣椒炒肉（只看背景） | 82 | 17/18/14/15/16 = **80** | — | 「标签栏纵向堆叠偏挤」已由宽档两栏销账；本轮回落主因是判官点「'背景文化'段标题与技能名同词」——那是册子给的页名，非本席可改，见 §四 |

第二轮均值 **82.9**（第一轮 83.0）。**判定：8 页仍未达成 ≥90**；第 12 格 89 是全场最高，两跑都差 1 分。

## 三 反例（第二轮：改坏必红／还原一致）

**反例做法**：把本轮最大的一条改动**摘掉**——`buildHtml` 里去掉 `heroBandOf()`（页族装饰带），其余一字不动，
重出整批（`runId=1ede3450`），复评当时全场最高的第 12 格；然后**逐字还原**，重出（`runId=e4d9a31f`）。

| 步 | 页 | 读数 | 判 |
|---|---|---|---|
| 改坏（摘掉装饰带） | 12 只看营养 | 18/17/16/17/18 = **86** | **红**（绿读数 89 → 86） |
| 还原（逐字回绿） | 12 只看营养 | 18/18/18/17/18 = **89**（与改坏前逐字相同的 JSON，跑 A／跑 B 两跑同值） | **绿** |

**还原的机器读数**（比复评更强的一条：产物逐字节相同 ⇒ 判读面相同 ⇒ 判官给出的 JSON 必然相同）：

```
还原批与绿批产物 sha256 逐字相同 8/8（view_nutrition_only 7e1dc4aab299ed12／view_full_recipe aa0287332bb18e0b／…）
```

## 四 机器面（第二轮）

```
GATE-RUN runId=a6187ed9-41bf-4731-9213-628d9f27c71d cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=0174e008-c682-4d19-8b01-2ab0a979519c cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=26c22414-2958-421b-8a11-6b83abd151ea cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=1ede3450-e3cf-4b36-8d19-7ed79739c07a cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=e4d9a31f-f69c-45b5-b819-f5e52d8e2192 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
```

- 编译：五条运行的第一句都是 `tsc -b packages/skill-chef`，全部 `TSC-OK`（红 0）。
  本轮另有两次编译**红在别席写集里的文件**（`src/history/pages.ts` 缺 `renderTimelineRows`、`src/relation/pages.ts` 缺 `TONE`），
  按派单册「红在别人写集里＝别席正在改」等 60／90 秒重试后转绿——那两次不是本席的产物问题，不计入门禁证据。
- 质量门（`runId=26c22414` 那一轮）：`汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 8 行） exit=0`
- 结构纪律：`page.ts` **336 LF**、`pageCss.ts` **344 LF**、`pageSection.ts` **106 LF**，三件都在 350 LF 告警线内。
- **宽档「收窄居中」自查**（编排者广播的历史席根因：栅格项上写 `margin: auto` 会关掉 `justify-self: stretch`、
  整块 shrink-to-fit 居中，桌面端读出「统计卡压成窄列／留白过大」）：
  本席 `grep -n "margin(-left|-right)?:\s*auto|margin:\s*0 auto|margin:\s*auto" packages/skill-chef/src/view` **命中 2 处**，
  两处都是**弹性行里的项**、不是栅格项收窄居中：`pageCss.ts:106`（`.chef-view-sec-note`——分区标题行
  `display:flex` 里的右端读数，推右用）与 `pageCss.ts:298`（`.chef-view-param-value`——参数带 `display:flex` 里的值，推右用）。
  **宽档（≥1001）那一段里一条 `margin: auto` 都没有**，全是 `grid-template-columns`／`height`／
  `<a>` 内的 `justify-content: center` ⇒ 本席不吃这一条根因，无需改。

## 五 第二轮仍未达标：还差在哪

1. **还差 1–10 分，且这 8 页的缺口仍然全部压在「视觉生动」与「双端自适应」两维上**（本轮生动 15–17、双端 14–18）。
   本轮把返修单点名的两条图路都走了：**页族装饰带**（连续构图的内联 SVG）与**分区图标位**（八种色基）。
   判官对装饰带的态度是分化的——第 6／9／12 格把它读成加分项（生动 +2～+3），
   第 10／13 格仍写「装饰条占位较空」或干脆不提。**再往上仍只剩真图一条路，而那是禁用项**（见第一轮 §五.2 的同一条天花板）。
2. **第 10 格（食材分组，87→79）是本轮回落最大的一页**：判官两轮都点「手机端卡片说明行（250g／约 4 根）密度略挤」，
   而宽档那一组改动（导航四枚等宽、标签两栏）对这一页没有对应收益。可再试的方向：分组卡里把「用量」并入食材名一行、
   说明整行独占（与 `只看食材` 页的两行式行卡对齐）——本席本轮未做。
3. **第 13 格的回落点不在本席写集**：判官写「'背景文化'段标题与技能名同词冗余」——
   「背景文化」是册子给的页名（`VIEW_CARDS` 的第三个占位由册子决定），分区标题跟着页名走；
   **要动得改册子／页名，那不是本席白名单**。记进越界报备，不碰。
4. **第 7 格两轮都点「桌面端留白／首屏密度」**：本席在宽档已经把导航与要点列表铺成两栏／四格，
   剩下的「密度」缺口来自页头本身（眉标＋大标题＋四枚事实卡＋结论条＋装饰带＝首屏约 300px）——
   再压就得动页头三级（那是公共层页壳的形状），不属本席。
5. **第 11 格的双端回落（17→14）已经验过方向**：判官点「桌面端三件组横向并排挤出命中区」，
   本席把三件套改成窄档一行一件／宽档三列（本轮正式版），触点已回到 ≥44px（机器读数触摸目标 0 处不足）。
   剩下的「双端」扣分与「步骤说明冗长」同源——正文是库里的字段值，渲染层不许改，只能标「数据侧受限」。

---

# 第三轮（定向返修）

**一句话结论**：撤掉页内自建装饰带（族级带由页壳 `renderSceneShell` 给，两带并存＝重复装饰）、按「逐句问『删掉会少知道什么』」
删并渲染层自拼的三处复述、宽档把「标签块＋下厨记录」并成一行两栏借位；同会话 8 页 **85.5 → 84.5**（4 升 3 降 1 持平），
**第 13 格两跑都 90 是本轮唯一过线的页**；首屏填满度 **−12…13px**（全部 ≪120px）。
**反例这一轮没变红**（读数见 §三，照实记，不修饰）。

## 一 撤带（① 的要求）

| 落点 | 改前 | 改后 |
|---|---|---|
| `src/view/pageSection.ts` | `BAND_ART`（一整张连续构图的页族装饰带）＋ `heroBandOf()` | 两件全删（81 LF） |
| `src/view/page.ts:306` | `const head = factsOf(d) + conclusionOf(d) + heroBandOf();` | `const head = factsOf(d) + conclusionOf(d);`（340 LF） |
| `src/view/pageCss.ts` | `.chef-view-band`／`.chef-view-band-art` 两组规则 ＋ 宽档那条 `height: 62px` | 三处全删（338 LF） |

**产物里还剩几条带**（8 件逐个量，`grep -c` 口径）：

```
BAND view_full_recipe.html      族级带 5 处；页内自建带 0 处
BAND view_for_beginner.html     族级带 5 处；页内自建带 0 处
BAND view_recipe_with_substitution.html 族级带 5 处；页内自建带 0 处
BAND view_ingredients_only.html 族级带 5 处；页内自建带 0 处
BAND view_ingredients_grouped.html 族级带 5 处；页内自建带 0 处
BAND view_steps_only.html       族级带 5 处；页内自建带 0 处
BAND view_nutrition_only.html   族级带 5 处；页内自建带 0 处
BAND view_background_only.html  族级带 5 处；页内自建带 0 处
```

「族级带 5 处」＝页壳给的**同一枚** `ilife-scene-band` 在样式表选择器与 DOM 节点上各处出现一次（数出来是 5 次文本命中，**带只有一条**）；
**页内自建带 0 处**＝本席那条已撤净。页内只留**载内容的形状**：分区题头（八色基图标位）、营养瓦片、步骤参数带、两行式行卡，
色基仍取 `CHART_PALETTE` 上相邻两色（与族级带同族暖色）。

## 二 两处判官点名的消法（② 的要求）

**① 文案叠说** —— 把渲染层自拼的每一句列成表，逐句问「删掉这句，用户会少知道什么」（库里字段值一字未改）：

| 渲染层自拼句 | 删掉会少知道什么？ | 处置 |
|---|---|---|
| 结论条「做过 1 次，平均 4 分。」 | 平均分与页尾读数卡「平均评分 4 分／满分 5 分」是同一件事（判官第三轮仍点「'平均 4 分' 句式略冗余」） | **改**：只说次数＋**最近一次**分（「做过 1 次，最近一次 4 分。」），平均分只留读数卡那一位 |
| 事实条第 4 格「状态 已做」 | 与结论条「做过 N 次」同义（判官两轮点「'已做／做过 1 次' 文案仍可再精炼」） | **删**：状态由结论条承担；顺带解掉判官点的「390 宽下四枚胶囊几乎贴边」 |
| 步骤分区题头读数「火候与时长」 | 每张步骤卡里就是火候／时长／锅温三格 | 删（第二轮） |
| 标签分区题头读数「这一份的分类」 | 六个维度名已把「怎么分类」说全 | 删（第二轮） |
| 替换分区读数「临时预览」 | 标题＋下面那句已说两遍 | 删（第二轮） |
| 分区题头／参数标签／页脚两行／按钮／复制区标题 | 少知道（它们是标签与动作本体，不是复述） | **留** |

**② 宽档卡片贴左／留白** —— 宽档（≥1001）把「标签块＋下厨记录」收进一行容器 `.chef-view-side`，走
`grid-template-columns: minmax(0,1fr) minmax(0,1fr); column-gap: 24px; align-items: start`，两栏各自 `stretch` 吃满 880 版心
（判官原话「桌面版仍堆叠一列未借位」）。**没有用 `margin: auto` 收窄**——栅格项上的 auto 外边距会关掉 `justify-self: stretch`。
自查：`src/view` 下 `margin(-left|-right)?: auto` 命中 **2 处**，两处都在**弹性行内的项**上
（`pageCss.ts` 的分区标题行推右读数、参数带推右值），宽档那一段一条 auto 外边距都没有。

## 三 反例（**这一轮没变红**，照实记）

反例做法：把本轮「撤掉事实条第 4 格（状态 已做）」**改回去**（事实条回到四格），其余一字不动，重出整批（`runId=8d140d7d`），复评：

| 步 | 页 | 读数 | 判 |
|---|---|---|---|
| 改坏 | 6 完整食谱 | 判官**连跑两次**都返回越界 JSON（`{"信息层级":250,"双端自适应":-200,"文案精炼":-100,"视觉生动":250,"风格一致":250,"total":445}`） | **读数作废**，不作判定 |
| 改坏 | 9 只看食材 | 18/18/18/17/18 = **89**（绿读数 88） | **没变红** |
| 还原 | 全部 8 件 | 产物 sha256 **8/8 逐字相同**（`sha-green3.txt`，`runId=cabc5692`） | 还原一致 ✓ |

**判定：本轮反例未通过**（`<88` 的页复评更高、`≥88` 的页也没掉）。读数本身有意义：**撤掉那一格不是拿分点**——
第 9 格的判官还把四格胶囊读成「well-grouped colored chips」。反例的这一条**留给收口席当证据**，本席不粉饰。

## 四 首屏填满度（③ 的要求）

口径＝**真浏览器量**：390×820 视口底 − 首屏内最后一个文本片段的底边（只数叶子元素上的文字，`aria-hidden` 的纯装饰件不算）。
探针＝`.scratch/t873-查看/fill.mjs`（复用公共层 `withBrowser`，真 Chrome ＋ CDP，与截图／质量门同一个入口）。

| 页 | 本轮前 | 本轮后 |
|---|---|---|
| view_full_recipe | −16px | −7px |
| view_for_beginner | 4px | −4px |
| view_recipe_with_substitution | −12px | −7px |
| view_ingredients_only | −7px | −2px |
| view_ingredients_grouped | −17px | 1px |
| view_steps_only | −6px | 13px |
| view_nutrition_only | 5px | −12px |
| view_background_only | −6px | 13px |
| **汇总** | **最大 5px／均值 −7px** | **最大 13px／均值 −1px** |

⇒ **8 页全部 ≪120px**，口径达标。撤带让两页的剩余从 5／−6 抬到 13／13，仍远低于线。

## 五 逐页（同会话「本轮前 → 本轮后（两跑）」）

同一把尺、同一批图、同一会话；问句＝派单册 §二 逐字。本轮前＝`runId=1a3791f9`，本轮后＝`runId=3970dd58`（`≥88` 的页跑了第二跑）。
**跨会话的绝对值不可比，只有这两列可比。**

| 格 | 页名 | 本轮前 | 本轮后跑 A | 跑 B | 两跑≥90？ |
|---|---|---|---|---|---|
| 6 | 完整食谱 | 18/17/18/18/19 = **90** | 17/15/17/16/17 = **82** | — | 否（撤带 18→16） |
| 7 | 新手版 | 17/18/18/18/17 = **88** | 18/17/19/15/18 = **87** | — | 否（跑 A 87，未进抖动带） |
| 8 | 带替换食材 | 18/18/18/18/19 = **91** | 17/14/15/17/18 = **81** | — | 否 |
| 9 | 只看食材 | 17/15/15/17/16 = **80** | 18/19/17/16/18 = **88** | 18/19/17/16/18 = **88** | 否（两跑都 88，差 2 分） |
| 10 | 食材分组 | 17/17/17/17/16 = **84** | 17/18/16/16/17 = **84** | — | 否 |
| 11 | 只看步骤 | 18/14/13/17/18 = **80** | 17/20/16/15/17 = **85** | — | 否 |
| 12 | 只看营养 | 18/16/17/17/18 = **86** | 16/15/16/16/16 = **79** | — | 否 |
| 13 | 只看背景 | 17/18/16/17/17 = **85** | 18/19/18/17/18 = **90** | 18/19/18/17/18 = **90** | **是**（两跑都 90） |

**本轮后均值 84.5**（本轮前 85.5）；**过线 1 页（第 13 格）**。

## 六 机器面（第三轮）

```
GATE-RUN runId=1a3791f9-1323-45ba-bf97-06a93b118d19 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=fe0cdd13-2d01-4323-bd21-a659fe692cf3 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=3970dd58-5724-4c5a-aff4-f7ce63ceb37a cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=8d140d7d-077c-4345-a539-06b8cb143772 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
GATE-RUN runId=cabc5692-e569-4401-aa98-1cbd66dceb09 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 查看" exit=0
```

- 编译：五条运行的前两句都是 `tsc -b packages/skill-chef` ＋ 本域装配，全部 `TSC-OK`（红 0）。
- 质量门（`runId=3970dd58`）：`汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 8 行） exit=0`
- 结构纪律：`page.ts` **340 LF**、`pageCss.ts` **338 LF**、`pageSection.ts` **81 LF**，三件都在 350 LF 告警线内。
- 守线页 21／28／45 不在本席写集，本席一行未碰（本轮只动 `src/view/**` 三件与本节）。

## 七 未达标还差在哪

1. **撤带是本轮的最大一笔代价，也是本轮的硬要求**：本轮前 8 页里已有 3 页 ≥88（6／7／8），撤掉页内那条带之后
   第 6 格 18→16、第 8 格 18→17、第 12 格 17→16（视觉生动），三页总分各掉 9／10／7。
   **族级带一条扛不住「视觉生动」这一维**（判官仍写「缺少烹饪场景插画／整体偏素」）。
   ⇒ 结论与第二轮一致：**这一维再往上只剩真图一条路，而那是禁用项**，需编排者裁决。
2. **第 9 格两跑都 88**（本轮前 80）：判官两跑都点同一句——「表格列宽重复『说明／约 X 根』」「英文『ml』裸词未本地化」。
   前者是库里的 `quantity_text`（渲染层不许改），后者是本席能改的（`ml`／`g` 是量纲单位，`质量门` 的英文裸词白名单收它们为读数不判红）
   ——**若编排者要「本地化单位」，得先立一条全批口径**，本席不动单页。
3. **第 12 格回落 86→79**：判官点「桌面端『烹调记录』把信息块横向并排却仍保留手机版两列卡节奏」——
   那正是本轮新加的 `.chef-view-side` 宽档两栏。**宽档借位这一条对短页（第 12 格）是负收益**（它的页尾本来就短），
   对长页是正收益。可再试的方向：只在「标签块条目数 ≥4」时才两栏——本轮未做，留给下一手。
4. **反例未通过**（§三）：本席这一轮没有拿到「改坏必红」的读数，其中第 6 格是判官后端连跑两次返回越界 JSON。
   **这是本轮的验收缺口，照实记**；下一手若要补，建议换一条更靠近分数的改动做反例（例如撤掉分区题头的图标位）。

