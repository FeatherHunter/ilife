# #840 运行面：副本库器械 ＋ 本机配置指向真实库 ＋ 端到端读数

## 结论

三条目标全部达成，真库主文件全程未动（294,912 B，mtime 2026-08-09 13:38:15，复制／读／写探针前后逐次比对一致）。

## 1. 沙箱器械

落点：`docs/skills/skill-chef/t840-沙箱.mjs`（只读本票写面，不碰 `packages/`）。

```text
node tooling/run-locked.mjs --ticket 840 -- node docs/skills/skill-chef/t840-沙箱.mjs --ticket 840
```

实测回执（exit 0）：

```text
副本=D:\ilife\.scratch\t840\chef_data.db 字节=294912 真库 mtime 未变
隔离家目录=D:\ilife\.scratch\t840\home
用法：以家目录注入跑副本（Windows 设 USERPROFILE／POSIX 设 HOME 指向上述目录），再跑 dist 命令
通道=家目录注入（CHANNEL-PENDING-#756；ILIFE_CONFIG_DIR 口子已随 #754 删除）
```

器械做的四件事：① 复制前记真库 stat；② `copyFileSync` 到 `.scratch/t<票号>/chef_data.db`；
③ 落隔离家目录 `<scratch>/home/.ilife/chef.yaml`（只写 `db.dir` 指副本目录＋`db.name`，不碰其它五个技能的配置）；
④ 复制后重验真库 stat，不一致即 exit 1。重复跑即刷新出一份 pristine 副本。

反例（必跑，两式都 exit 1 并拒绝）：

```text
node docs/skills/skill-chef/t840-沙箱.mjs --ticket 840 --dest "D:\2Study\StudyNotes\.db\chef_data.db"
→ t840-沙箱：拒绝：沙箱落点与真库是同一文件（D:\2Study\StudyNotes\.db\chef_data.db），不复制即写真库
node docs/skills/skill-chef/t840-沙箱.mjs --ticket 840 --dest "D:\2Study\StudyNotes\.db"
→ 同上拒绝（目录式指向归一到同一文件）
```

拒绝线＝落点与真库同一文件，或落在真库目录树里（含子目录），一律 exit 1。

## 2. 通道口径修正（实测推翻票面假设，如实记录）

票面写「隔离通道是测试基座既有口子 `ILIFE_CONFIG_DIR`」——落地时该口子已不存在：
`packages/base-link-core/src/config/dirs.ts` 头注「环境变量一律不参与（位置覆盖那个口子已随 #754 删除）」，
`packages/skill-chef/src/fetch/paths.ts` 头注「改造前读 `SKILLS_DB_PATH`……两个环境变量一并删掉（#695）」。
本器械改走 #756 已裁的家目录注入（Windows 设 `USERPROFILE`／POSIX 设 `HOME`），票面遗留出口维持：
`CHANNEL-PENDING-#756`（最终裁定仍归 #756，本票只声明＋照此跑通）。

域票使用法（7 张域票逐票复制本式，把 840 换成自己票号，一份副本只归一张票，并行票不得共用）：

```text
node tooling/run-locked.mjs --ticket <票号> -- node docs/skills/skill-chef/t840-沙箱.mjs --ticket <票号>
```

之后凡跑 dist 命令，先把 `USERPROFILE`（Windows）／`HOME`（POSIX）指向回执里的隔离家目录。
Git Bash 下注意引号层：`--params` 的 JSON 在 Bash 单引号里按票面字面写即可；
在 PowerShell 里双引号会被吞，须改走脚本文件（本票实测踩过，证据见 `e2e-*.sh` 已删，结论留这里）。

## 3. 端到端读数

本机配置 `C:\Users\辰辰洋洋\.ilife\chef.yaml` 的 `db.dir` 已指到真实库所在目录：

```yaml
db:
  dir: "D:/2Study/StudyNotes/.db"
  name: chef_data.db
```

（其余行未动：`html.dir` 照旧；`files.help`／`files.lookup` 是 #796 已退休键，读时容忍，本票不顺手删。其它五个技能的 yaml 的 mtime 全部停在 2026-09-18／19，未碰。）

`chef.config.read` 生效值（exit 0）：

```text
db.dir = D:/2Study/StudyNotes/.db
resolved.dbFile = D:\2Study\StudyNotes\.db\chef_data.db
```

日常路径读数（exit 0，`chef.recipe.view --params '{"name":"辣椒炒肉"}'`）：

- 菜名：辣椒炒肉（`id 230ce6e5-…`，湖南家常菜，难度快手菜，2 人份，18 分钟，状态已做，有来源 `扬帆远航【紫食谱2.0】-214-313`）
- 食材数：11（螺丝椒 250g／五花肉 250g／浏阳豆豉／小米椒／生姜／大蒜／蚝油／黄酒／生抽／老抽／味精，全有数字用量＋单位）
- 步骤数：6（煸五花肉 5 分钟中火 → 虎皮螺丝椒 3 分钟大火 → 豆豉小米椒 → 姜蒜 → 蚝油 → 调味回锅，全有数字时长＋火候）

与只读直查（Python `mode=ro`）的计数一致：11／6。字段非空、非占位（`nutrition` 那格是产品自己标的 `estimated:false` 占位说明，与菜数据无关）。

## 4. 真库只读证据

| 时刻 | 主文件字节 | 主文件 mtime | 菜名集合（只读查） |
|---|---|---|---|
| 开工前 | 294,912 | 2026-08-09 13:38:15 | 辣椒炒肉 |
| 沙箱复制后 | 294,912 | 2026-08-09 13:38:15 | 辣椒炒肉 |
| 沙箱写探针后 | 294,912 | 2026-08-09 13:38:15 | 辣椒炒肉（探针菜只出现在副本） |
| 日常路径读后 | 294,912 | 2026-08-09 13:38:15 | 辣椒炒肉 |

两点如实说明：① 真库目录里原有 `-shm`（32,768 B）／`-wal`（0 B）伴生文件（`journal_mode=wal`，`integrity_check=ok`）；
日常读的干净关闭把空 WAL 回收了，伴生文件消失——这是 SQLite 的 ephemeral 行为，行数据零变（上表末行）。
② 写探针 `沙箱写探针_840` 经隔离通道写盘回执 ok，副本里可查、真库里无，之后重跑器械已把副本刷回 pristine（现副本内仅辣椒炒肉一道）。

## 5. 规矩（域票照办）

- 真库全程只读：任何写只发生在 `.scratch/t<自己票号>/chef_data.db`；跑前跑后对上表那三列。
- 一份副本只归一张票：并行票各用各的 `--ticket` 号，不得共用副本。
- 批量真跑跑副本库，`db.dir` 指副本目录；日常使用才指真库目录。

## 6. 遗留出口（记录，不实施）

- 「把老库搬进缺省数据目录（`~/.ilife/data/chef_data.db`）」是另一条可选路径：搬过去则 `db.dir` 可留空串走默认落点。
  不实施——本次维持「配置指过去、库不动」的只读姿势。
- 隔离通道最终裁定归 #756，本票声明 `CHANNEL-PENDING-#756`（见 §2）。
