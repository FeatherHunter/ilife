## Question

四个小域共 8 卡：派生（添加派生关系／查看派生关系(家族树)／从已有派生新菜）、采购（生成采购清单）、开始使用（首次使用(初始化工作流)）、数据管理（数据质量报告／批量编辑／导出备份）。它们在 HELP 里**除体检外的 7 张全标着【待开发】**，且各有独立难点：家族树要有环检测、采购清单要「核对家里库存」但 `#682` A6-21 明令**技能包之间零依赖**、初始化要真建库、备份要真导出文件。这 8 卡怎么走通？

## 目标

1. 派生 3 卡：关系落库 ＋ 家族树可视化（含环检测）＋ 从母本派生新菜（复制后改差异）。
2. 采购 1 卡：多菜合并清单 ＋ 份数换算 ＋ 排除可选；**核对库存走调用契约**（跨技能不落代码依赖），拿不到就显式降级提示。
3. 开始使用 1 卡：初始化工作流（真建库／建目录，且只在需要时）。
4. 数据管理 3 卡：体检报告（质量评分口径要裁定：老件是「数据完整度」，新件 `chef.history.query kind=quality` 是「口碑」——两者语义不同）、批量编辑（改动前对比 ＋ 回执）、导出备份（真出文件，落点与命名在本票定）。
5. 8 卡逐卡一行读数。

另加三条本图口径（并发与收口，逐条可判）：

- **过程性 vision 审查**：本票产物须过一次 vision_router 视觉审查，把查出的不合理之处**当场改掉**（缺陷不许拖到收口票）。
- **册子片段**：本票产物登记成 `docs/skills/skill-chef/t<票号>-册子片段.json`（字段：卡 id／唤醒词／命令／参数／产物绝对路径／exit／bytes／sha256），供收口 A 合并；**不许直接改共用册子**。
- **副本库沙箱**：数据一律走票 17 交的沙箱器械，复制到自己票号下的 `.scratch/t<票号>/`；**不许与其他票共用同一份副本**。

## 验收命令


- 正例：`node tooling/run-locked.mjs --ticket 776 -- node docs/skills/skill-chef/t776-run-small-domains.mjs` → 打印 8 行 `卡 → exit=0 → 产物绝对路径`（备份卡另打印产物文件字节数）且 exit 0
- 反例（必跑）：造一个成环的关系对 → 家族树必须报环并 exit≠0（不许画出一张自环图）

- 判据（片段与视觉）：本票运行脚本带 `--check` 时必须同时打印「片段行数＝本票卡数」与「vision 审查缺陷 0（或逐条已改）」，两者缺一即红。

## 不许动的东西


- 老件一行不动；其余 6 个域不碰。
- 技能包之间零依赖（`#682` A6-21）：采购核对库存不许 import 居家管家的代码。
- 不改老库 schema；不碰真库。
- 「体检」语义不许把老件的「数据完整度」偷换成「口碑」。

## 交付物路径

- 代码：`packages/skill-chef/src/{relation,shopping,setup,data}/**`
- 证据：`docs/skills/skill-chef/t776-小域.md`（8 行读数 ＋ 体检口径裁定 ＋ 跨技能调用契约说明）

## 遗留出口

- 体检若最终定为「两个都要」（完整度 ＋ 口碑）→ 明确拆成两张卡另立票。
- 备份的定时／增量 → 另立票。

## 进度：0%

下一步：先裁「体检」的口径（本票内第一件事），再按域逐个跑通。
