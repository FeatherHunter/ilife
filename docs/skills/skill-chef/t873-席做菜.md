# t873 做菜席：本域 5 页的原扣分项逐条销账完毕、4 页总分上升；冻结尺下 5 页落在 75–86，**未达 ≥90**

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**做菜席**（第 1–5 格）。
写集：`packages/skill-chef/src/cook/**`（`run.ts` ＋ 新建 `page-css.ts`／`page-text.ts`）＋ 本证据件。命令一律持锁，工具＝`tooling/run-locked.mjs`。

**一句话结论**：5 页的**原扣分项逐条有销账读数**（三列错列／行距挤／留白／绕嘴文案／连写无句读五处都动了源头，
读数见 §二），机器质量门六列全绿、终审扫描三类字样命中 0；但**冻结尺下没有一页到 90**——
改后五维 **79／81／75／86／79**，仍是「视觉生动（13–16）＋文案精炼（14–15）」两维拖着总分，
而这两维的剩余扣分**主要来自库里不能改写的原文**（见 §五 第 1 条）。两条要交给收口席的器具事实见 §五 第 2、3 条。

---

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `src/cook/page-css.ts`（新件，1–215） | 无此件 | 页内收口样式段 `cookPageCss()`：页头事实条四等分、步骤参数条三等分、当前步／已做步两套形状、正文与说明行的行高与行尾余量、进度卡读数与条色、页头装饰图形定位；断点只用仓内闭集（640／1001），色只用冻结 token ＋ 调色板下标 | 第 1 格的 390 端错列（①）＋ 第 2 格的行距紧（③）＋ 第 3 格的留白（③）＋ 第 4 格的行高挤（③） |
| `src/cook/page-text.ts`（新件，1–53） | 无此件 | 页内文案与正文排版：`servingsNote()`（结论句，命令信封与页面同一句）、`stepProseHtml()`（按**已有标点**分行）、`stepTitle()`（状态写进标题）、`usageChip()`／`qtyText()` | 第 2 格的绕嘴文案＋第 5 格的连写无句读（②） |
| `src/cook/run.ts:201-211` | 无页头图形 | 内联 SVG「锅＋三缕热气」（`COOK_HERO_ART`，`aria-hidden`、`pointer-events:none`，一笔数据一个字都不带） | 全场最低维「视觉生动」的「整页近乎纯文字、缺图」（主攻方向 ①） |
| `src/cook/run.ts:45,51`（`buildCookingRun`） | 结论句写死在两处（信封一处、页面一处），措辞不一 | 两处都走 `servingsNote(servings, base)`：同一句话只有一个定义地 | 第 2 格的「原谱份量料即可，无需换算」绕嘴（②） |
| `src/cook/run.ts:227` | `data.servings === data.baseServings ? '按原谱份量备料即可，无需换算' : '用量是原谱（2 人份）的 2 倍，照这份清单备料就行。'` | `份量照原谱，不用换算。` ／ `份量已按 4 人份配好（原谱 2 人份），不用换算。`（不再要求读者做「原谱的几倍」这道算术） | 第 2 格的绕嘴与冗余（②） |
| `src/cook/run.ts:233` | `中火 5 分钟` | `本步 中火 5 分钟`（明写「本步」：它说的是当前这一步，不是全谱的） | 第 2 格的「键值贴边」同一条读数里的「两处各说一遍」（②） |
| `src/cook/run.ts:247` | `上次做这道菜，评分 4 分` | `评分 4 分`（卡标题已经是「上次经验」、时间槽已经是那次日期） | 第 2 格的冗余（②） |
| `src/cook/run.ts:256` | `断点续接：会话记住做到第 3 步，页面只承当前进度与下一步，跨会话进度暂不保留。` | `断点续接：上次做到第 3 步，这次从第 4 步接着做。换新会话后进度不保留。`（45→40 字，去掉「页面只承…」这句实现语） | 第 4 格的说明段（②③） |
| `src/cook/run.ts:261-281` | 六张步骤卡直接排在正文层 | 每张卡外挂状态类 `ilife-cook-step-{done,current,todo}`，六张卡收进 `ilife-cook-steps`（宽档这一层排两栏） | 第 3 格的留白＋主攻方向 ④「进度、步骤、参数要各有自己的形状」 |
| `src/cook/run.ts:264` | `renderProseBlock({ text: s.action })` | `renderProseBlock({ html: stepProseHtml(s.action) })`：**库里的动作原文一字不动**，只在既有标点之后换行 | 第 5 格的「正文连写无句读、长行末尾贴边」（②③） |
| `src/cook/run.ts:294` | `本谱无炖烤腌类典型等待步骤，以下按耗时最长的两步给并行建议。` | `本谱没有炖烤腌这类等待步骤，按最耗时的两步给并行建议。` | 主攻方向 ② 文案精炼 |
| `src/cook/run.ts:345` | `extraCss: chefSceneCss()` | `extraCss: chefSceneCss() + LF + cookPageCss()`（**两层不拆回去**，包内测试 `test/skin-873.test.mjs` 守着） | 公共层席交代的接线口径 |

`run.ts` 333→347 LF、`page-css.ts` 215 LF、`page-text.ts` 53 LF——**均低于本包 350 LF 告警线**，不触发必报第四步。

---

## 二 逐页销账（两行读数）

口径：**改前＝`t873-开工基线.md` §一**（冻结 2026-09-21）。另列一行**皮肤刚接上时**（公共层席交付、本席一行未动）的读数——
本席是皮肤之后才动源的，两行一起看才分得清「皮肤抬了多少、本席抬了多少」。

| 格 | 页名 | 改前（基线 §一） | 改前（皮肤后·本席复跑） | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|---|
| 1 | 做菜模式：辣椒炒肉 | 17/19/15/**13**/17 = **81** | 18/14/15/13/16 = **76** | 17/16/14/**15**/17 = **79** | ①「火候／时长／锅温三列标签与值在 390 端错列」→ 参数条改**三等分栅格**：真浏览器实测三格 `x=34／144／255`、`w=104／104／104`（改前按内容宽排是 `w=54／68／67`、整行右侧空 115px，末格右沿与卡片内沿差 115px）⇒ 标签成一列、值成一列；②「整页缺图」→ 页头「锅＋热气」装饰图形 ⇒ 视觉生动 13（基线）→ 15 |
| 2 | 做菜模式：辣椒炒肉·带上次经验 | 17/18/13/**12**/**14** = **74** | 17/18/15/14/17 = **81** | 17/16/**15**/**16**/17 = **81** | ①「1/6 步 与 中火 5 分钟 行距紧、参数键值贴边」→ 进度卡说明行 `margin-top:8px／line-height:1.6`、读数抬到 28px、参数条三等分＋内距 6/8px、步骤卡四块逐块给气口（12–14px）；②「绕嘴与冗余」→ 结论句两句全改（`份量照原谱，不用换算。`／`份量已按 4 人份配好（原谱 2 人份），不用换算。`）＋上次经验主行去掉「上次做这道菜」＋进度卡说明加「本步」二字 ⇒ 文案精炼 13（基线）→ 15、视觉生动 12→16 |
| 3 | 做菜模式：辣椒炒肉·4 人份加倍 | 18/20/17/15/18 = **88** | 17/14/14/15/16 = **76** | 17/15/14/13/16 = **75** | ①「当前进度 与 第 1 步 两块在 390 端纵向留白偏多」→ 步骤列表与进度卡之间 18px、步骤卡之间 16px、进度卡内部说明行与读数行各给一格气口、读数抬到 28px ⇒ 读数卡不再是一块「空读数」；②结论句同第 2 格。**注意**：本页的那 12 分缺口**主要不是本席造成的**——皮肤接上时它就从 88 掉到 76（见 §五 第 2 条） |
| 4 | 做菜模式：辣椒炒肉·暂停后继续 | 17/18/15/14/16 = **80** | 未单独复跑（同源页，见 §五 第 4 条） | 17/17/**18**/**16**/**18** = **86** | ①「断点续接说明段 390 端行高挤」→ 口径行 `line-height 1.5→1.75`、`margin-top 10px`（真浏览器实测行高 18px→21px）；②同段文案由 45 字压到 40 字、去掉实现语「页面只承当前进度与下一步」⇒ 文案精炼 15（基线）→ 18，本页是本席 5 页里唯一四维齐升的一页 |
| 5 | 做菜模式：辣椒炒肉·等待计时 | 17/19/15/**13**/17 = **81** | 未单独复跑（与第 1 格同源同形，见 §五 第 4 条） | 17/16/14/**15**/17 = **79** | ①「第 1 步正文连写无句读、长行末尾贴边」→ 排版层断句（`贵州黑山猪五花肉(②)切片不要太薄,` / `锅内不加油,` / `下五花肉煸至半焦(不是半熟,出油微焦)` 三行；**原文一字不动，只加换行**）＋ `line-height 1.7→1.85` ＋ 行尾 4px 余量 ⇒ 视觉生动 13→15；②等待并行卡的说明句同改。**数据侧受限**：库里正文本来是半角逗号与括号，本席只许排版、不许改写 |

**抖动带口径**：改后 5 页为 79／81／75／86／79，**没有一页落在 88–92**，故按派单册 §四 第 5 条不需要「两次独立复评」。
第 1 格另跑过一次**同图同问句的独立复评**，两次都给 **17/16/14/15/17 = 79**、理由句逐字相同
（本会话里同一对图＋同一问句可复现，与公共层席记的那条器具事实一致）。

---

## 三 反例（改坏必红／还原一致）

本席做了两条反例：一条打**视觉复评**（第 1 格的原扣分项），一条打**机器质量门**。

**反例 A（视觉复评，第 1 格「三列错列」）**：把参数条的三等分栅格退回公共层那套「按内容宽排、随内容换行」
（`display:flex; flex-wrap:wrap; gap:8px 22px`），重出本域产物再按冻结问句复评同一页；然后逐字还原。

| 步 | 页 | 机器几何读数（真浏览器） | 冻结尺读数 | 判 |
|---|---|---|---|---|
| 改坏（只退这一条规则） | 1 | 三格 `x=34／106／192`、`w=50／64／63`（右端空 115px） | 17/18/14/16/18 = **83** | **未变红**（好态 79 → 坏态 83） |
| 还原（逐字回绿） | 1 | 三格 `x=34／144／255`、`w=104／104／104` | 17/16/14/15/17 = **79** | 绿（与改后**逐字相同**的 JSON） |

**这条反例的结论要交给收口席**：**冻结尺对「标签与值成不成列」这一类版式事实不敏感**——坏态那一次评委反倒给了 83
（它没提错列，提的是「首屏冗余」）；**真正分开两态的是机器几何**（`50/64/63` 参差 ＋ 整行右侧空 115px 对 `104/104/104` 三列齐）。
⇒ 第 1 格这条扣分项按**机器读数销账**，不按评委分销账。

**反例 B（机器质量门，第 4 格那句「断点续接」）**：把那句话里的句号改回分号（`接着做。换新会话` → `接着做；换新会话`），
重出本域产物并跑机器质量门；然后逐字还原。

| 步 | 机器读数（两行） |
|---|---|
| 改坏 | `runId=ce6b1431-4fd7-41df-a4a7-737376551e96` → `汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 1／英文裸词 0／重复句 0`，红行点名 `.scratch/t873-做菜/做菜/cooking_resume_after_pause.html — 1 处（版式位 85 个）·「；」断点续接：…`，**exit=1** |
| 还原 | `runId=7d20b9cc-f54d-4852-bc25-925ba06540a2` → `汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0`，**exit=0** |

（这条不是「原扣分项」的回退，而是本席**在版式位里写进了分隔符**这一次改动的回退；它证明这一列真的会红，
并且红行会点名到件——本席第一轮就是被它拦下的，见 §四 的 `ac6873c1`。）

---

## 四 机器面

### 4.1 门禁运行记录（派单册 §四 的一致口径：一次运行一行，runId 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=9e7799b0-e1cd-40a4-aa85-bac3fe73cb4e cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （编译＋重出＋双端首屏图＋质量门；§二 五行读数与 §三 好态读数都以此批产物为准）exit=0
GATE-RUN runId=a5fad906-fc89-4b70-b381-6a733bad4b79 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 A 坏态：参数条退回按内容宽排）exit=0
GATE-RUN runId=316a52c7-ab5f-4a05-8aa6-e7b6ae92ce04 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 A 还原后重出）exit=0
GATE-RUN runId=ce6b1431-4fd7-41df-a4a7-737376551e96 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 B 坏态：分隔符懒政 1 页 → 质量门 exit=1，作为反例红读数引用，不作门禁放行）exit=1
GATE-RUN runId=7d20b9cc-f54d-4852-bc25-925ba06540a2 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 B 还原后：本席交付态，六列全绿）exit=0
GATE-RUN runId=ac6873c1-113b-41e2-8049-27b6a79af0d2 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （第一轮：分隔符懒政 1 页命中 → exit=1，本席据此改了那句文案）exit=1
```

### 4.2 编译

`node tooling/run-locked.mjs --ticket 873 -- node node_modules/typescript/bin/tsc -b packages/skill-chef` → `RESULT: ticket=873 runId=65f319bd-ae72-4c57-bbb4-dec349c1cfab waitedMs=10001 exit=0`；
此后每一次席位运行（上表 6 条）内部都先跑同一条 `tsc -b packages/skill-chef` 并打 `TSC-OK`（红即停在编译、不重出产物）。

包内两条与本域直接相关的测试（皮肤接线 ＋ 上屏字样）：
`node tooling/run-locked.mjs --ticket 873 -- node --test packages/skill-chef/test/skin-873.test.mjs packages/skill-chef/test/t871-文案与结构.test.mjs`
→ `ℹ tests 32 ／ ℹ pass 32 ／ ℹ fail 0`，`runId=be73f9c6-00a2-4dc8-a92d-0ca4ed967509 exit=0`
（前者守 `run.ts` 仍只调 `chefSceneCss()`、两层没被拆回去；后者守本页上屏文字不含三类禁用字样）。

### 4.3 质量门（`.scratch/t873-做菜/质量门.json`，视口 390／1280）

```
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 5 行）   exit=0
本域 5 件逐件（页 | 横向溢出 | 触摸目标 | 触屏三件 | 分隔符懒政 | 英文裸词 | 重复句）：
  01 cooking_start_fresh.html           ✓ 390档 0｜1280档 0  ✓ 13 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
  02 cooking_start_with_history.html    ✓ 390档 0｜1280档 0  ✓ 14 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
  03 cooking_start_double_servings.html ✓ 390档 0｜1280档 0  ✓ 13 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
  04 cooking_resume_after_pause.html    ✓ 390档 0｜1280档 0  ✓ 13 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
  05 cooking_during_waiting_step.html   ✓ 390档 0｜1280档 0  ✓ 14 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
```

### 4.4 终审扫描（`node docs/skills/skill-chef/t778-终审扫描.mjs .scratch/t873-做菜`）

```
扫 5 件产物；三类字样命中 0 处
共 5 件；命中 0 -> 干净     exit=0
```

### 4.5 结构纪律

- 三件均在本席写集内（`src/cook/**`）：`run.ts` 347 LF、`page-css.ts` 205 LF、`page-text.ts` 53 LF，**都在 350 LF 告警线以内**。
- 新建两件的理由：页内样式段（栅格／形状／排印）与页内文案（结论句、正文分行、标题状态）是**两批各自会独立再改的东西**，
  混在已经是 347 LF 的 `run.ts` 里会当场越过告警线；拆法照本包既有先例 `history/page-css.ts`（同族命名、同一条「在皮肤之后追加」的口径）。
- 铁律二（概念唯一）：结论句只有 `servingsNote()` 一处定义（命令信封与页面共用）；颜色不新造——只用冻结 token 与
  `CHART_PALETTE` 的下标（热气那一色），热气／锅线各只有一条规则。
- 铁律一（能力自治）：本席一行都没碰 `src/render/**` 与别的域；跨域只经 `base-paint` 的公开接口
  （`renderProseBlock`／`escapeHtml`／`CHART_PALETTE`）。
- 第一步影响清单 → 第五步对账：声明＝`src/cook/**` ＋ 本证据件；**实际＝同三件源码 ＋ 本件**，偏差 0。

---

## 五 未做项与下一手缺什么

1. **「文案精炼」这一维的剩余扣分，本席解不掉**。5 页改后这一维仍是 14／15／14／18／14，评委每次点到的都是
   **库里的原文**：`贵州黑山猪五花肉(②)切片不要太薄,锅内不加油,…`（半角逗号、`(②)` 全角括号）与做菜反馈
   `味道重了10%,但很适合配米饭;米饭吃得少的话调料需降低20%。整体比较满意。`。派单册把本席限于「只在排版层断句／分段」，
   **改这些字＝改写内容／改库**，本席一个字都没动。⇒ 要再往上抬，需要一条**数据侧**的决定（清洗库里的标点与机器标注），
   那不在本席写集内。
2. **器具事实一：皮肤本身会让个别页掉分**。第 3 格（4 人份加倍）在**冻结基线里是 88**，公共层席交付后、本席一行未动时
   复跑是 **76**（−12：双端 20→14、风格 18→16）。同一把尺、同一页、两次读数；本席的改动只把它从 76 挪到 75。
   ⇒ 建议收口席对 48 页做一次「皮肤前后」的同尺对照，别把「<90」一律记在域席头上。
3. **器具事实二：冻结尺判不了「列对不对齐」这类版式事实**。反例 A 里，把三列栅格退回参差（`50/64/63` ＋ 右侧空 115px），
   评委给分反而从 79 升到 83。⇒ 这类扣分项的销账证据应以**真浏览器几何读数**为准（本席的做法：探针脚本
   `.scratch/t873-做菜/probe.mjs`，只读、落本席草稿目录，未入仓），冻结尺读数只作旁证。
4. **没单独复跑的两页**：第 4、5 格的「皮肤后起点」读数未单独取（第 4 格与第 2 格同源、第 5 格与第 1 格同源）。
   §二 表里已如实标注，不拿同源页的数顶替。
5. **越界报备（未碰，只记录）**：
   - 派单册指定的席位命令内部会跑本域驱动器 `t772-run-cooking.mjs`，它会**重写受版本控制的 `docs/skills/skill-chef/t772-册子片段.json`**
     （工作区里现为 ` M`）。这是**指挥链指定的命令自身的副作用**，不属本席写集，**未提交**，交收口席统一处置。
   - 公共层皮肤的「事实条瓦片」在 390 档把页头四格压到每格 57px 高、值一行放不下时会折行；本域用页内四等分栅格兜住了，
     但**根因在 `src/render/skin.ts`**（公共层席的件），本席一个字没碰，只记录。
6. **本席没做**：菜品图／步骤插画位（数据源里没有图，也不许编造）；`t778-验收墙.mjs` 与册子的重出（收口席统一做）。

---

## 六 第二轮（定向返修：宽档挤压／390 贴边／文案复述／装饰脱节）

第一轮记录见 §一–§五，**本节只追加，不改上面任何一个读数**。返修依据＝`t873-逐格缺陷清单.md`（改后 48 行）
里本席 5 页的评委原话，与派单第二轮给的四类问题（按收益排序）。

### 6.1 改了哪几处（第二轮）

| 落点（file:line） | 改前（第一轮交付态） | 改后 | 对应哪条 |
|---|---|---|---|
| `src/cook/page-css.ts:229-247` | 宽档把六张步骤卡排成**两栏**（`repeat(2, minmax(0,1fr))`） | 撤回两栏，宽档**单列满宽**；改成一栏之内按 **2:1 分栏**（正文列 ≈545px，侧栏 ≈244px），卡高由 281→197px | 第 3 格「桌面端步骤卡双列后左侧被严重挤压」 |
| `src/cook/page-css.ts:53-84` | 参数三条**等分瓦片**（`repeat(3,…)` ＋ 浅底 ＋ 蓝左缘），390 档必然铺满整行 | 参数改**键值行**（一行一条，标签 52px 一列、值一列，行间发丝线，无底无框） | 第 1／5 格「手机端三粒紧贴边缘几近溢出」 |
| `src/cook/run.ts:274-285` | 步骤卡正文下面还有渲染层自己拼的一句 `这一步做成：…` | **删**（库里的 `expected_result` 字段一字未改，只是这一步不再上屏） | 第 1／5 格「与底部『肉片出油、边缘微焦、体积缩小』复述同义」 |
| `src/cook/page-text.ts:14-30` | 结论条按「同份／改份量」分两句，改份量那句写「已按 4 人份配好（原谱 2 人份）」 | 页面结论条收成**一句**：`用料已算好，照清单备料。`（人数与倍率不再复述，事实条上已有一份）；命令回执那一面另留 `servingsNoteReceipt()` 带倍率 | 第 3 格「『份量已按 4 人份配好』与『第 1 步』内同义冗余偏多」 |
| `src/cook/run.ts:229-235` | 进度卡有一行说明 `本步 中火 5 分钟` | **删**说明行（火候／时长下一张卡里就有一份；同尺复评把首屏读成「四处都在重复」） | 第 1 格「文案冗余」 |
| `src/cook/page-text.ts:36-42` | 当前步标题写 `第 1 步（正在做）` | 当前步**不写字**，状态改由形状承担（主色左缘 ＋ 卡面浅底 ＋ 标题位主色圆点）；已做步仍写「（已做）」 | 第 1 格「『当前进度 1 / 6 步』与『第 1 步（正在做）』重复进度信息」 |
| `src/cook/page-art.ts`（新件）＋ `page-css.ts:190-215` | 页头图形只有线稿（无卡面），无装饰带 | 图形收成**一张与别的卡同色基／同描边／同圆角的牌**；另加一条横贯波线装饰带（都是内联 SVG，不是 `<img>`） | 第 3 格「插画与暖色渐变风格与卡片式表单略脱节」＋派单第二轮第 4 条 |
| `src/cook/run.ts:266-269` | 步骤卡外层只挂状态类 `ilife-cook-step-{done,current,todo}` | 补挂本体类 `ilife-cook-step`（**第一轮的一处真缺陷**：样式段里按卡本体写的规则整条都没命中，卡间距、并排、侧栏全失效）；顺带修：`page-art.ts` 拆件让 `run.ts` 回到 339 LF | 第 2／3 格「桌面端留白松散／挤压」＋本包 350 LF 告警线 |

第二轮**没有**改动任何库字段、没有改判据、没有碰写集外路径。

### 6.2 逐页两跑读数（同尺，冻结问句逐字）

口径照派单第二轮：**<88 的页跑一跑即可**；≥88 的才需要两次独立复评。本席 5 页改后全部 <88，故每页一次；
第 1 格另有一次**独立复审**（反例的还原跑），两跑逐字相同；第 5 格与第 1 格**首屏图逐字节相同**
（`sha256` 同为 `831cb418a3da485e`／`4f3c65b813595c50`），故读数同一个。

| 格 | 页名 | 第一轮读数 | **第二轮读数（两跑）** | 差 | 主要上升维 |
|---|---|---|---|---|---|
| 1 | 做菜模式：辣椒炒肉 | 79 | **83**（17/18/14/17/17）｜复评 **83**（逐字相同） | **+4** | 双端 14→18（桌面并排）、视觉生动 15→17、风格 17→17 |
| 2 | 做菜模式：辣椒炒肉·带上次经验 | 81 | **82**（17/17/16/15/17） | +1 | 文案 15→16 |
| 3 | 做菜模式：辣椒炒肉·4 人份加倍 | 75 | **85**（17/18/17/16/17） | **+10** | 双端 15→18、文案 14→17、风格 16→17、视觉生动 13→16 |
| 4 | 做菜模式：辣椒炒肉·暂停后继续 | 86 | **86**（17/18/16/17/18） | 0 | 双端 17→18，文案 18→16（读数摆动） |
| 5 | 做菜模式：辣椒炒肉·等待计时 | 79 | **83**（与第 1 格同画面同读数） | **+4** | 同第 1 格 |

中途两版读数（记在案，便于复核这次的取舍）：第 3 格先到 **81**（键值行 ＋ 2:1 首次落地），再加**侧栏浅底面板**后到 **85**；
第 1 格在「删进度卡说明行 ＋ 当前步标题不写字」之前是 **80**（那版评委点了「『当前进度 1 / 6 步』与『第 1 步（正在做）』重复」）。
**都没有到 90**，差距见 §6.5。

### 6.3 反例（改坏必红／还原一致）

**反例 A（视觉复评，第 1 格，回退第二轮的关键修复）**：把步骤卡外层的本体类去掉（只留状态类），
重出本域产物后按冻结问句复评同一页；然后逐字还原。

| 步 | 机器几何读数（真浏览器，1280 档） | 冻结尺读数 | 判 |
|---|---|---|---|
| 改坏（`204e6d06`） | `step1-body` `display=block`（并排失效）·`prose w=847`（正文列由 545 缩回满宽）·`param-strip w=847`（侧栏消失、参数又铺满整行） | 17/17/14/**13**/16 = **77** | **红**（83 → 77） |
| 还原（`e0448042`） | `step1-body` `display=grid`·`prose w=545`·`param-strip w=244` | 17/18/14/**17**/17 = **83** | **绿**（与改坏前**逐字相同**的 JSON） |

**反例 B（机器质量门，第 4 格那句「断点续接」）**：把那句话里的句号改回分号，重出并跑质量门；然后逐字还原。

| 步 | 机器读数（两行） |
|---|---|
| 改坏 | `runId=fe22f7f1-3155-4d1d-92c9-760d36b38d42` → `汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 1／英文裸词 0／重复句 0`，红行点名 `cooking_resume_after_pause.html — 1 处（版式位 78 个）`，**exit=1** |
| 还原 | `runId=91eb3cdb-1a12-4720-b7f5-e7cc7d8df4f4` → `汇总：…／分隔符懒政 0／…`，**exit=0** |

### 6.4 机器面（第二轮）

```
GATE-RUN runId=5b8e5190-ce52-45e7-ac6f-fc921ba3a114 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （第二轮首版：参数键值行 ＋ 侧栏 ＋ 宽档 2:1）exit=0
GATE-RUN runId=26ffc0d9-e1b6-4915-a209-b0a93240d589 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （修 `.ilife-cook-step` 本体类缺失）exit=0
GATE-RUN runId=59c9a2d3-0e91-4e2e-9d98-f04fcf09b3b9 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （侧栏浅底面板 ＋ 首行改实线）exit=0
GATE-RUN runId=54b9023b-0efd-4cd5-acd4-88bff4e5b878 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （结论句改词 ＋ 宽档值右对齐）exit=0
GATE-RUN runId=e4d4d229-e5b6-4019-95c5-438393753d78 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （当前步标题不写字；§6.2 四页读数以此批产物为准）exit=0
GATE-RUN runId=204e6d06-6ade-4a12-a95c-9005d5efb147 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 A 坏态）exit=0
GATE-RUN runId=e0448042-f7a4-499a-b354-df461c08ad83 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 A 还原）exit=0
GATE-RUN runId=fe22f7f1-3155-4d1d-92c9-760d36b38d42 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 B 坏态，质量门 exit=1，作反例红读数引用）exit=1
GATE-RUN runId=82833415-2639-451d-b0fc-378e93cab3dd cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （交付态：拆 `page-art.ts` 后重出，六列全绿；首屏图与评分那批**同字节**）exit=0
GATE-RUN runId=65d6c106-a5b2-4dad-9470-d5a55a70b410 cmd="node --test --test-name-pattern=做菜 packages/skill-chef/test/t871-文案与结构.test.mjs"   （做菜那两条用例 2/2）exit=0
GATE-RUN runId=4f69f702-eb10-49d7-97ba-d7b572472b65 cmd="node --test packages/skill-chef/test/skin-873.test.mjs"   （19/19：10 处壳接线仍只调 `chefSceneCss()`）exit=0
```

- 编译：`tsc -b packages/skill-chef` 在每条席位运行内先跑，打 `TSC-OK`；`bf95d7b8` 那条**红在别人写集**
  （`src/relation/pages.ts` 的 `TONE` 未定义，派生席在途），等 60 秒重试后 `5b8e5190` 转绿——按派单册动作序列第 3 条处置。
- 质量门（交付态 `82833415`）：`汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 5 行）exit=0`；
  第 1／5 格 13 件可点、最短边 44px；第 2／5 格 14 件可点、最短边 44px。
- 终审扫描：`扫 5 件产物；三类字样命中 0 处 -> 干净`。
- **包内测试有一条红，不是本席的**：`录入·成功回执 页面无工单黑话／实现语` 抛
  `pageShapes: renderFactStrip: input.items[2].value 必须是非空字符串`（`runId=dd1589ff／172cbdf9／907c9044`），
  出处在 `src/add/pages.ts`（工作区里是 ` M`，录入席在途改动 116 行），**不属本席写集**。
  本席域内两条独立复跑全绿：`--test-name-pattern=做菜` 2/2（`65d6c106`）、`skin-873` 19/19（`4f69f702`）。

### 6.5 未做项与下一手缺什么（第二轮）

1. **5 页仍未到 90**（83／82／85／86／83）。剩余扣分两处**都不在本席写集内**：
   - **文案精炼 14–17**：评委每轮点到的都是**库里的正文与反馈原文**（`贵州黑山猪五花肉(②)切片不要太薄,锅内不加油,…`、
     `味道重了10%,但很适合配米饭;米饭吃得少的话调料需降低20%。`）。派单册只许本席在排版层断句／分段，
     改这些字＝改写内容／改库 ⇒ 需要一条**数据侧**决定。
   - **视觉生动 15–17**：本域数据源里没有菜品图，也不许编造写实菜品图；本席已把能补的都补上
     （页头装饰牌 ＋ 装饰带 ＋ 本域形状语言）。这一维的天花板与公共层席 §五.2 记的是同一条。
2. **器具事实（本轮新增一条）**：同一页同尺读数在本轮内摆动 ±3（第 1 格：83／80／83；第 3 格：81／85）。
   ⇒ 每页一次读数只能当**方向**，不能当**判定**；<88 的页按要求「跑一跑」时，单次掉分不当退步（这一点与派单第二轮口径一致）。
3. **编排者广播的 `margin: auto` 自查（已查，本席无此写法）**：`src/cook/**` 三件里没有一条
   `margin-left/right: auto` 或 `margin: 0 auto`（唯一 `margin-left: 8px` 是当前步标题位那枚圆点；
   唯一 `width: 100%` 是装饰带 SVG）。第 3 格第一轮那条「宽档步骤卡左侧被严重挤压」的根因是**双列步骤卡**，
   已在第二轮撤回并改成 2:1 分栏（几何读数：正文列 545px、侧栏 244px）。
4. **越界报备（未碰，只记录）**：同 §五.5（`t772-册子片段.json` 被席位命令重写；皮肤瓦片压扁页头四格的根因在
   `src/render/skin.ts`）。另加一条：`src/add/pages.ts` 上录入席的在途改动让包内全量测试红一条（见 §6.4 末行），
   **不是本席引入，本席未碰**。

---

## 七 第三轮（定向返修：页头只留族级带／文案叠说／首屏填满度）

前两轮记录见 §一–§六，**本节只追加**。本轮起点＝公共层席第三轮 `f2283532`（族级装饰带 ＋ 族级样式）
**已自动进到本席 5 页**之后的状态。

### 7.1 改了哪几处（第三轮）

| 落点（file:line） | 改前（第三轮起点） | 改后 | 对应哪条 |
|---|---|---|---|
| `src/cook/page-art.ts`（**整件删除**）＋ `run.ts` 的 `head` ＋ `page-css.ts` 的 `.ilife-cook-art*`／`.ilife-cook-band*`／版面根 `position: relative` | 页头有本域自己两件装饰：一张「锅＋热气」图形牌（`position: absolute`）与一条横贯波线带；标记里 `<svg>` 3 个 | **两件都撤**：页头只剩 `sceneShell` 插的 **process 族级带**；标记里 `<svg>` 1 个；样式段里本域装饰类名 0（常量 `STEAM_COLOR` 与 `CHART_PALETTE` 引用一并撤） | 派单第三轮第 ① 条「页头只能有族级带」 |
| `src/cook/page-text.ts:15-27`（新 `cookCountNote()`）＋ `run.ts:214` | 结论条写「用料已算好，照清单备料。」 | 结论条写「**这道菜做过 N 次**」（`historyStats` 的读数；一次没做过＝「第一次做这道菜」） | 派单第三轮第 ② 条「文案叠说」 |
| `src/cook/run.ts:262-267` | `断点续接：上次做到第 3 步，这次从第 4 步接着做。换新会话后进度不保留。` | `断点续接：换新会话后进度不保留。`（前半句进度卡与展开的那张卡上都有） | 同上（同尺复评第 4 格原文摘「『断点续接』提示语与第 4 步说明仍偏冗余」） |
| `src/cook/run.ts:305` | `本谱没有炖烤腌这类等待步骤，按最耗时的两步给并行建议。` | `本谱没有炖烤这类等待步骤。`（后半句是把实现讲给读者听，下面两行已经写着第几步、几分钟、备什么料） | 同上 |
| `src/cook/page-css.ts:78,149` | 步骤列表上气口 18px、时间轴行距 8px | 16px／7px（首屏填满度用；只动行距与块距，**一个字的内容都没编**） | 派单第三轮第 ③ 条 |

### 7.2 逐页「同会话本轮前 → 本轮后」（同一会话、同一把尺、同一问句）

跨会话绝对值不可比（派单第三轮的原话：同一张图在五个会话里拿到过 92／92／92／83／80／76），故下表**只用同会话两列**。

| 格 | 页名 | 本轮前 | **本轮后（两跑）** | 差 |
|---|---|---|---|---|
| 1 | 做菜模式：辣椒炒肉 | 80（18/16/13/16/17） | **79**（17/14/13/17/18） | −1（视觉 16→17、风格 17→18 升，双端 16→14 降） |
| 2 | ·带上次经验 | 73（16/14/12/15/16） | **81**（16/17/15/17/16） | **+8** |
| 3 | ·4 人份加倍 | 81（18/16/12/17/18） | **85**（18/17/15/17/18） | **+4** |
| 4 | ·暂停后继续 | 86（18/17/16/17/18） | **88 ／ 88**（两跑逐字相同：17/20/18/16/17） | **+2**（落 88–92 带，按口径跑了两跑） |
| 5 | ·等待计时 | 80（与第 1 格同画面） | **79**（与第 1 格同画面，`sha256` 同） | −1 |

5 页均值 **80.0 → 82.4**。**没有一页到 90**；第 4 格两跑都是 88（差 2 分）。

**第 4 格的一次器具故障（如实记）**：该页的**原图对**在视觉后端连挂 4 次（3 次整对返回
`{"…":0,…, "一句话":"图未能在此会话中渲染"}`，1 次单图返回但问句不同、不入账）；最后用
`vision_crop` 把两张首屏图**同帧无损重编码**（`0,0,390,820` 与 `0,0,1280,860`，像素不变）后取到读数，两跑逐字相同。
PNG 本体完好（10 张签名与 IEND 都核对过），不是产物问题。

### 7.3 首屏填满度（机器读数，口径＝视口内完整可见的文本片段底边到视口底）

| 格 | 390 | 1280 |
|---|---|---|
| 1 | **0px** | 11px |
| 2 | 123px → **13px** | 44px |
| 3 | **0px** | 11px |
| 4 | 11px | 7px |
| 5 | **0px** | 11px |

口径与派单第三轮给的一致（≤120px）。改前只有第 2 格 390 档越线（123px），靠 §7.1 末行的两处块距各收 1px 落回 13px；
其余四页本来就在线内。**没有为填满度编任何内容**。

### 7.4 删了哪条重复带 ＋ 对照读数（「剩几条带」）

```
交付态（逐页，标记面）：族级带 1（process）｜本域图形牌 0｜本域波线带 0｜标记里 svg 1｜样式段里遗留本域装饰类名 0
改前（第三轮起点）：      族级带 1（process）｜本域图形牌 1｜本域波线带 1｜标记里 svg 3
```

**同会话对照（第 1 格，三态）**：本轮前（两件都在）**80**｜本轮后（两件都撤）**79**｜
反例态（只把**波线带**挂回来）**86**。
⇒ **冻结尺分不开这三种状态**，甚至偏向装饰更多的版本（这条读数与公共层第三轮给的第 21 格 +11 并不矛盾：
那一格是**族级带**从无到有，本席这里是**页内第二条同题材带**）。按公共层的页头契约，本席保持**两件都撤**，
**不拿尺子的浮动换契约**。对照态是临时产物，已逐字还原（还原后首屏图 `sha256` 与评分那批逐字节相同）。

### 7.5 文案叠说：渲染层自拼句逐句过一遍（第 ② 条）

判据：**删掉这句，用户会少知道什么吗**？答案「不会」就删。**库里的字段值（含半角逗号、`(②)` 标注、
做菜反馈原文）一个字都没动。**

| 渲染层自拼的句 | 在哪 | 删掉会少知道什么 | 处置 |
|---|---|---|---|
| 用料已算好，照清单备料。 | 结论条 | 不会——份量在页头事实条、用料在备料清单、步骤就在下面六张卡 | **本轮改**：换成「这道菜做过 N 次」（这条别处都没有，不是复述） |
| 断点续接：上次做到第 3 步，这次从第 4 步接着做。 | 口径行 | 不会——进度卡写着「4 / 6 步」、展开的正是第 4 步卡 | **本轮删**（只留「换新会话后进度不保留」——这条读者真会少知道） |
| …按最耗时的两步给并行建议。 | 等待卡口径行 | 不会——是把实现讲给读者听；下面两行的「第 N 步／约 X 分钟／备下一步用料」已经把建议说全 | **本轮删** |
| 本步 中火 5 分钟 | 进度卡说明行 | 不会——下一张卡的参数表里就是这两项 | 第二轮已删 |
| 这一步做成：… | 步骤卡底部 | 不会——库里正文的收尾已经写了成品迹象（原文一字未改，只是不再复述一遍） | 第二轮已删 |
| （正在做） | 当前步标题 | 不会——进度卡写着 1/6，当前那张卡还展开着 | 第二轮已删 |
| 本谱没有炖烤腌这类等待步骤 | 等待卡口径行 | **会**——读者不知道这份谱为什么没有等待位 | 留 |
| 火候／时长／锅温、第 N 步、第 N 步（已做）、当前进度 1 / 6 步、备料清单（N 人份）、炊具、上一步／下一步／做完了，记一次／看这道菜的历史、复制备料清单与进度、上次经验／评分 4 分 | 参数表标签、卡标题、读数卡、折叠卡标题、动作条、复制区 | **会**——它们是结构标签或数据读数，删了读者认不出这块是什么 | 留 |

### 7.6 宽档（1280）自查（第 ② 条后半）

- `src/cook/**` 三件里**没有** `margin-left/right: auto`、`margin: 0 auto`，本轮也**没有新增任何收窄规则**
  （唯一 `margin-left: 8px` 是当前步标题位那枚圆点）。
- 宽档唯一的「分栏」是步骤卡**卡内**的 `grid-template-columns: minmax(0, 2fr) minmax(0, 1fr)`
  （正文列 545px／侧栏 244px，几何读数见 §6.3），不是对整块的收窄；步骤卡本身仍是单列满宽（880px 版心列）。
- 未动 process 族的公共节奏（`sceneRhythm.ts` 只管回执族的填满度），故**不涉及守线页 45 的同会话对照**。

### 7.7 反例（两行机器读数）

**反例 A（视觉复评，第 1 格，回退本轮关键改动「撤掉波线带」）**：把波线带挂回来再评。

| 步 | 冻结尺读数 | 判 |
|---|---|---|
| 改坏（`3da563f6`，标记里 svg 2） | 18/16/17/17/18 = **86** | **未变红**（撤掉的态是 79，挂回来的态反而高 7） |
| 还原（`008d7c60`，标记里 svg 1） | 17/14/13/17/18 = **79**（首屏图 `sha256` 与评分那批逐字节相同） | 绿（回到本轮后读数） |

⇒ 记实：**这一轮的反例在冻结尺上不成立**——尺子对「页内多一条同题材装饰带」不敏感（第 1 格三态 80／79／86）。
本席按契约保持撤掉，并把这条当作器具事实交给收口席。

**反例 B（机器质量门，回退本席那句口径的标点修复）**：把那句话改回用分号收尾，重出并跑质量门；然后逐字还原。

| 步 | 机器读数（两行） |
|---|---|
| 改坏 | `runId=53d6b36c-cf06-47a2-8c12-86d00d8cd972` → `汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 1／英文裸词 0／重复句 0`，红行点名 `cooking_resume_after_pause.html — 1 处（版式位 78 个）`，**exit=1** |
| 还原 | `runId=7fbd7b14-7f65-42d7-82ff-d5022fb43ad3` → `汇总：…／分隔符懒政 0／…`，**exit=0** |

### 7.8 机器面（第三轮）

```
GATE-RUN runId=2815b931-059d-4012-adba-268b44583d3d cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （编译红在别人写集：data/pages.ts 的 seatFigure、relation/shapes.ts）exit=1
GATE-RUN runId=98ca2b12-b70c-4554-82e2-a1bfab5d72a2 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （等 90 秒重试转绿；本轮 5 页读数以此后的产物为准）exit=0
GATE-RUN runId=adaa0988-16fc-4a66-acb0-b40d32bd4ae0 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （补撤宽档块里残留的 `.ilife-cook-art` 两条）exit=0
GATE-RUN runId=3da563f6-bdd5-4ad4-9fd4-b82f482b023d cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 A 坏态：波线带挂回）exit=0
GATE-RUN runId=008d7c60-c8e4-4d7c-bbba-f2a6628a8f3f cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 A 还原）exit=0
GATE-RUN runId=53d6b36c-cf06-47a2-8c12-86d00d8cd972 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 B 坏态：分隔符懒政 1 页 → exit=1，作反例红读数引用）exit=1
GATE-RUN runId=7fbd7b14-7f65-42d7-82ff-d5022fb43ad3 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （交付态：六列全绿）exit=0
GATE-RUN runId=7a92441f-fd01-4b02-98fc-5785d3f8edf6 cmd="node --test --test-name-pattern=做菜 packages/skill-chef/test/t871-文案与结构.test.mjs"   （做菜两条用例 2/2）exit=0
```

- 质量门（交付态 `7fbd7b14`）：`汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 5 行）exit=0`。
- 终审扫描：`扫 5 件产物；三类字样命中 0 处 -> 干净`。
- 结构：`run.ts` 347 LF、`page-css.ts` 210 LF、`page-text.ts` 70 LF（**都在 350 LF 告警线以内**；`page-art.ts` 已整件删除）。
- **包内有一处红，不是本席的**：`skin-873.test.mjs` 的 `packages/skill-chef/src/history/pages.ts 只调 renderSceneShell()，
  且带一个合法族名` 红（`runId=bc5c31b2`，`AssertionError: 这一处没给族名（或给了闭集外的值）`）——
  那一行是**历史席**的件，本席未碰；本席自己的 `src/cook/run.ts` 那一条断言在 19 条里全过。
  做菜侧另跑 `--test-name-pattern=做菜` 2/2（`7a92441f`）。

### 7.9 未做项与下一手缺什么（第三轮）

1. **5 页仍未到 90**（79／81／85／88／79），差口与第二轮同源、都不在本席写集内：
   - **文案精炼 13–18**：评委每轮点到的都是**库里的原文**（半角逗号、`(②)`、`不是半熟,出油微焦`、做菜反馈长句）。
     派单把本席限于「只在排版层断句／分段」，改这些字＝改写内容／改库 ⇒ 需要**数据侧**决定。
   - **视觉生动 16–17**：数据源无菜品图、不许编造写实菜品图；本轮又把页内两件装饰按契约撤了，
     这一维只剩族级带 ＋ 本域的卡面形状语言。
2. **器具事实三条**（交收口席）：
   ① 冻结尺判不了「页内多一条同题材装饰带」（第 1 格三态 80／79／86，撤掉的那态最低）；
   ② 同一会话内同页读数仍摆 ±8（第 1 格 79/80/86，第 2 格 73→81）；
   ③ 第 4 格原图对在视觉后端连挂 4 次（PNG 完好），最后用同帧无损重编码取到两跑 88／88。
3. **守线页**：本轮**没有**动 process 族的公共节奏（`sceneRhythm.ts` 只给回执族做填满度），故未触及第 45 格；
   本席 5 页的填满度改动都写在本域 `page-css.ts` 里，选择器全挂在 `.ilife-cook-step*`／`.ilife-page-ui` 之下。
4. **越界报备（未碰，只记录）**：`skin-873` 那条红在 `src/history/pages.ts`（历史席在途，族名缺失）；
   编译红那次在 `src/data/pages.ts` 与 `src/relation/shapes.ts`（数据管理席／派生席在途）。
   `t772-册子片段.json` 被席位命令重写（同前两轮），未提交。
