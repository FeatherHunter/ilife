# t873 做菜席：本域 5 页的原扣分项逐条销账完毕、4 页总分上升；冻结尺下 5 页落在 75–86，**未达 ≥90**

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**做菜席**（第 1–5 格）。
写集：`packages/skill-chef/src/cook/**`（`run.ts` ＋ 新建 `page-css.ts`／`page-text.ts`）＋ 本证据件。命令一律持锁，工具＝`tooling/run-locked.mjs`。

**一句话结论**：5 页的**原扣分项逐条有销账读数**（三列错列／行距挤／留白／绕嘴文案／连写无句读五处都动了源头，
读数见 §二），机器质量门六列全绿、终审扫描三类字样命中 0；但**冻结尺下没有一页到 90**——
改后五维 **79／81／75／86／79**，仍是「视觉生动（13–16）＋文案精炼（14–15）」两维拖着总分，
而这两维的剩余扣分**主要来自库里不能改写的原文**（见 §五 第 1 条）。两条要交给收口席的器具事实见 §五 第 2、3 条。

---

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `src/cook/page-css.ts`（新件，1–215） | 无此件 | 页内收口样式段 `cookPageCss()`：页头事实条四等分、步骤参数条三等分、当前步／已做步两套形状、正文与说明行的行高与行尾余量、进度卡读数与条色、页头装饰图形定位；断点只用仓内闭集（640／1001），色只用冻结 token ＋ 调色板下标 | 第 1 格的 390 端错列（①）＋ 第 2 格的行距紧（③）＋ 第 3 格的留白（③）＋ 第 4 格的行高挤（③） |
| `src/cook/page-text.ts`（新件，1–53） | 无此件 | 页内文案与正文排版：`servingsNote()`（结论句，命令信封与页面同一句）、`stepProseHtml()`（按**已有标点**分行）、`stepTitle()`（状态写进标题）、`usageChip()`／`qtyText()` | 第 2 格的绕嘴文案＋第 5 格的连写无句读（②） |
| `src/cook/run.ts:201-211` | 无页头图形 | 内联 SVG「锅＋三缕热气」（`COOK_HERO_ART`，`aria-hidden`、`pointer-events:none`，一笔数据一个字都不带） | 全场最低维「视觉生动」的「整页近乎纯文字、缺图」（主攻方向 ①） |
| `src/cook/run.ts:45,51`（`buildCookingRun`） | 结论句写死在两处（信封一处、页面一处），措辞不一 | 两处都走 `servingsNote(servings, base)`：同一句话只有一个定义地 | 第 2 格的「原谱份量料即可，无需换算」绕嘴（②） |
| `src/cook/run.ts:227` | `data.servings === data.baseServings ? '按原谱份量备料即可，无需换算' : '用量是原谱（2 人份）的 2 倍，照这份清单备料就行。'` | `份量照原谱，不用换算。` ／ `份量已按 4 人份配好（原谱 2 人份），不用换算。`（不再要求读者做「原谱的几倍」这道算术） | 第 2 格的绕嘴与冗余（②） |
| `src/cook/run.ts:233` | `中火 5 分钟` | `本步 中火 5 分钟`（明写「本步」：它说的是当前这一步，不是全谱的） | 第 2 格的「键值贴边」同一条读数里的「两处各说一遍」（②） |
| `src/cook/run.ts:247` | `上次做这道菜，评分 4 分` | `评分 4 分`（卡标题已经是「上次经验」、时间槽已经是那次日期） | 第 2 格的冗余（②） |
| `src/cook/run.ts:256` | `断点续接：会话记住做到第 3 步，页面只承当前进度与下一步，跨会话进度暂不保留。` | `断点续接：上次做到第 3 步，这次从第 4 步接着做。换新会话后进度不保留。`（45→40 字，去掉「页面只承…」这句实现语） | 第 4 格的说明段（②③） |
| `src/cook/run.ts:261-281` | 六张步骤卡直接排在正文层 | 每张卡外挂状态类 `ilife-cook-step-{done,current,todo}`，六张卡收进 `ilife-cook-steps`（宽档这一层排两栏） | 第 3 格的留白＋主攻方向 ④「进度、步骤、参数要各有自己的形状」 |
| `src/cook/run.ts:264` | `renderProseBlock({ text: s.action })` | `renderProseBlock({ html: stepProseHtml(s.action) })`：**库里的动作原文一字不动**，只在既有标点之后换行 | 第 5 格的「正文连写无句读、长行末尾贴边」（②③） |
| `src/cook/run.ts:294` | `本谱无炖烤腌类典型等待步骤，以下按耗时最长的两步给并行建议。` | `本谱没有炖烤腌这类等待步骤，按最耗时的两步给并行建议。` | 主攻方向 ② 文案精炼 |
| `src/cook/run.ts:345` | `extraCss: chefSceneCss()` | `extraCss: chefSceneCss() + LF + cookPageCss()`（**两层不拆回去**，包内测试 `test/skin-873.test.mjs` 守着） | 公共层席交代的接线口径 |

`run.ts` 333→347 LF、`page-css.ts` 215 LF、`page-text.ts` 53 LF——**均低于本包 350 LF 告警线**，不触发必报第四步。

---

## 二 逐页销账（两行读数）

口径：**改前＝`t873-开工基线.md` §一**（冻结 2026-09-21）。另列一行**皮肤刚接上时**（公共层席交付、本席一行未动）的读数——
本席是皮肤之后才动源的，两行一起看才分得清「皮肤抬了多少、本席抬了多少」。

| 格 | 页名 | 改前（基线 §一） | 改前（皮肤后·本席复跑） | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|---|
| 1 | 做菜模式：辣椒炒肉 | 17/19/15/**13**/17 = **81** | 18/14/15/13/16 = **76** | 17/16/14/**15**/17 = **79** | ①「火候／时长／锅温三列标签与值在 390 端错列」→ 参数条改**三等分栅格**：真浏览器实测三格 `x=34／144／255`、`w=104／104／104`（改前按内容宽排是 `w=54／68／67`、整行右侧空 115px，末格右沿与卡片内沿差 115px）⇒ 标签成一列、值成一列；②「整页缺图」→ 页头「锅＋热气」装饰图形 ⇒ 视觉生动 13（基线）→ 15 |
| 2 | 做菜模式：辣椒炒肉·带上次经验 | 17/18/13/**12**/**14** = **74** | 17/18/15/14/17 = **81** | 17/16/**15**/**16**/17 = **81** | ①「1/6 步 与 中火 5 分钟 行距紧、参数键值贴边」→ 进度卡说明行 `margin-top:8px／line-height:1.6`、读数抬到 28px、参数条三等分＋内距 6/8px、步骤卡四块逐块给气口（12–14px）；②「绕嘴与冗余」→ 结论句两句全改（`份量照原谱，不用换算。`／`份量已按 4 人份配好（原谱 2 人份），不用换算。`）＋上次经验主行去掉「上次做这道菜」＋进度卡说明加「本步」二字 ⇒ 文案精炼 13（基线）→ 15、视觉生动 12→16 |
| 3 | 做菜模式：辣椒炒肉·4 人份加倍 | 18/20/17/15/18 = **88** | 17/14/14/15/16 = **76** | 17/15/14/13/16 = **75** | ①「当前进度 与 第 1 步 两块在 390 端纵向留白偏多」→ 步骤列表与进度卡之间 18px、步骤卡之间 16px、进度卡内部说明行与读数行各给一格气口、读数抬到 28px ⇒ 读数卡不再是一块「空读数」；②结论句同第 2 格。**注意**：本页的那 12 分缺口**主要不是本席造成的**——皮肤接上时它就从 88 掉到 76（见 §五 第 2 条） |
| 4 | 做菜模式：辣椒炒肉·暂停后继续 | 17/18/15/14/16 = **80** | 未单独复跑（同源页，见 §五 第 4 条） | 17/17/**18**/**16**/**18** = **86** | ①「断点续接说明段 390 端行高挤」→ 口径行 `line-height 1.5→1.75`、`margin-top 10px`（真浏览器实测行高 18px→21px）；②同段文案由 45 字压到 40 字、去掉实现语「页面只承当前进度与下一步」⇒ 文案精炼 15（基线）→ 18，本页是本席 5 页里唯一四维齐升的一页 |
| 5 | 做菜模式：辣椒炒肉·等待计时 | 17/19/15/**13**/17 = **81** | 未单独复跑（与第 1 格同源同形，见 §五 第 4 条） | 17/16/14/**15**/17 = **79** | ①「第 1 步正文连写无句读、长行末尾贴边」→ 排版层断句（`贵州黑山猪五花肉(②)切片不要太薄,` / `锅内不加油,` / `下五花肉煸至半焦(不是半熟,出油微焦)` 三行；**原文一字不动，只加换行**）＋ `line-height 1.7→1.85` ＋ 行尾 4px 余量 ⇒ 视觉生动 13→15；②等待并行卡的说明句同改。**数据侧受限**：库里正文本来是半角逗号与括号，本席只许排版、不许改写 |

**抖动带口径**：改后 5 页为 79／81／75／86／79，**没有一页落在 88–92**，故按派单册 §四 第 5 条不需要「两次独立复评」。
第 1 格另跑过一次**同图同问句的独立复评**，两次都给 **17/16/14/15/17 = 79**、理由句逐字相同
（本会话里同一对图＋同一问句可复现，与公共层席记的那条器具事实一致）。

---

## 三 反例（改坏必红／还原一致）

本席做了两条反例：一条打**视觉复评**（第 1 格的原扣分项），一条打**机器质量门**。

**反例 A（视觉复评，第 1 格「三列错列」）**：把参数条的三等分栅格退回公共层那套「按内容宽排、随内容换行」
（`display:flex; flex-wrap:wrap; gap:8px 22px`），重出本域产物再按冻结问句复评同一页；然后逐字还原。

| 步 | 页 | 机器几何读数（真浏览器） | 冻结尺读数 | 判 |
|---|---|---|---|---|
| 改坏（只退这一条规则） | 1 | 三格 `x=34／106／192`、`w=50／64／63`（右端空 115px） | 17/18/14/16/18 = **83** | **未变红**（好态 79 → 坏态 83） |
| 还原（逐字回绿） | 1 | 三格 `x=34／144／255`、`w=104／104／104` | 17/16/14/15/17 = **79** | 绿（与改后**逐字相同**的 JSON） |

**这条反例的结论要交给收口席**：**冻结尺对「标签与值成不成列」这一类版式事实不敏感**——坏态那一次评委反倒给了 83
（它没提错列，提的是「首屏冗余」）；**真正分开两态的是机器几何**（`50/64/63` 参差 ＋ 整行右侧空 115px 对 `104/104/104` 三列齐）。
⇒ 第 1 格这条扣分项按**机器读数销账**，不按评委分销账。

**反例 B（机器质量门，第 4 格那句「断点续接」）**：把那句话里的句号改回分号（`接着做。换新会话` → `接着做；换新会话`），
重出本域产物并跑机器质量门；然后逐字还原。

| 步 | 机器读数（两行） |
|---|---|
| 改坏 | `runId=ce6b1431-4fd7-41df-a4a7-737376551e96` → `汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 1／英文裸词 0／重复句 0`，红行点名 `.scratch/t873-做菜/做菜/cooking_resume_after_pause.html — 1 处（版式位 85 个）·「；」断点续接：…`，**exit=1** |
| 还原 | `runId=7d20b9cc-f54d-4852-bc25-925ba06540a2` → `汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0`，**exit=0** |

（这条不是「原扣分项」的回退，而是本席**在版式位里写进了分隔符**这一次改动的回退；它证明这一列真的会红，
并且红行会点名到件——本席第一轮就是被它拦下的，见 §四 的 `ac6873c1`。）

---

## 四 机器面

### 4.1 门禁运行记录（派单册 §四 的一致口径：一次运行一行，runId 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=9e7799b0-e1cd-40a4-aa85-bac3fe73cb4e cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （编译＋重出＋双端首屏图＋质量门；§二 五行读数与 §三 好态读数都以此批产物为准）exit=0
GATE-RUN runId=a5fad906-fc89-4b70-b381-6a733bad4b79 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 A 坏态：参数条退回按内容宽排）exit=0
GATE-RUN runId=316a52c7-ab5f-4a05-8aa6-e7b6ae92ce04 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 A 还原后重出）exit=0
GATE-RUN runId=ce6b1431-4fd7-41df-a4a7-737376551e96 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 B 坏态：分隔符懒政 1 页 → 质量门 exit=1，作为反例红读数引用，不作门禁放行）exit=1
GATE-RUN runId=7d20b9cc-f54d-4852-bc25-925ba06540a2 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （反例 B 还原后：本席交付态，六列全绿）exit=0
GATE-RUN runId=ac6873c1-113b-41e2-8049-27b6a79af0d2 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 做菜"   （第一轮：分隔符懒政 1 页命中 → exit=1，本席据此改了那句文案）exit=1
```

### 4.2 编译

`node tooling/run-locked.mjs --ticket 873 -- node node_modules/typescript/bin/tsc -b packages/skill-chef` → `RESULT: ticket=873 runId=65f319bd-ae72-4c57-bbb4-dec349c1cfab waitedMs=10001 exit=0`；
此后每一次席位运行（上表 6 条）内部都先跑同一条 `tsc -b packages/skill-chef` 并打 `TSC-OK`（红即停在编译、不重出产物）。

包内两条与本域直接相关的测试（皮肤接线 ＋ 上屏字样）：
`node tooling/run-locked.mjs --ticket 873 -- node --test packages/skill-chef/test/skin-873.test.mjs packages/skill-chef/test/t871-文案与结构.test.mjs`
→ `ℹ tests 32 ／ ℹ pass 32 ／ ℹ fail 0`，`runId=be73f9c6-00a2-4dc8-a92d-0ca4ed967509 exit=0`
（前者守 `run.ts` 仍只调 `chefSceneCss()`、两层没被拆回去；后者守本页上屏文字不含三类禁用字样）。

### 4.3 质量门（`.scratch/t873-做菜/质量门.json`，视口 390／1280）

```
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 5 行）   exit=0
本域 5 件逐件（页 | 横向溢出 | 触摸目标 | 触屏三件 | 分隔符懒政 | 英文裸词 | 重复句）：
  01 cooking_start_fresh.html           ✓ 390档 0｜1280档 0  ✓ 13 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
  02 cooking_start_with_history.html    ✓ 390档 0｜1280档 0  ✓ 14 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
  03 cooking_start_double_servings.html ✓ 390档 0｜1280档 0  ✓ 13 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
  04 cooking_resume_after_pause.html    ✓ 390档 0｜1280档 0  ✓ 13 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
  05 cooking_during_waiting_step.html   ✓ 390档 0｜1280档 0  ✓ 14 件可点，最短边 44px  ✓ 有／有  ✓ 0 处  ✓ 0 处  ✓ 0 处
```

### 4.4 终审扫描（`node docs/skills/skill-chef/t778-终审扫描.mjs .scratch/t873-做菜`）

```
扫 5 件产物；三类字样命中 0 处
共 5 件；命中 0 -> 干净     exit=0
```

### 4.5 结构纪律

- 三件均在本席写集内（`src/cook/**`）：`run.ts` 347 LF、`page-css.ts` 205 LF、`page-text.ts` 53 LF，**都在 350 LF 告警线以内**。
- 新建两件的理由：页内样式段（栅格／形状／排印）与页内文案（结论句、正文分行、标题状态）是**两批各自会独立再改的东西**，
  混在已经是 347 LF 的 `run.ts` 里会当场越过告警线；拆法照本包既有先例 `history/page-css.ts`（同族命名、同一条「在皮肤之后追加」的口径）。
- 铁律二（概念唯一）：结论句只有 `servingsNote()` 一处定义（命令信封与页面共用）；颜色不新造——只用冻结 token 与
  `CHART_PALETTE` 的下标（热气那一色），热气／锅线各只有一条规则。
- 铁律一（能力自治）：本席一行都没碰 `src/render/**` 与别的域；跨域只经 `base-paint` 的公开接口
  （`renderProseBlock`／`escapeHtml`／`CHART_PALETTE`）。
- 第一步影响清单 → 第五步对账：声明＝`src/cook/**` ＋ 本证据件；**实际＝同三件源码 ＋ 本件**，偏差 0。

---

## 五 未做项与下一手缺什么

1. **「文案精炼」这一维的剩余扣分，本席解不掉**。5 页改后这一维仍是 14／15／14／18／14，评委每次点到的都是
   **库里的原文**：`贵州黑山猪五花肉(②)切片不要太薄,锅内不加油,…`（半角逗号、`(②)` 全角括号）与做菜反馈
   `味道重了10%,但很适合配米饭;米饭吃得少的话调料需降低20%。整体比较满意。`。派单册把本席限于「只在排版层断句／分段」，
   **改这些字＝改写内容／改库**，本席一个字都没动。⇒ 要再往上抬，需要一条**数据侧**的决定（清洗库里的标点与机器标注），
   那不在本席写集内。
2. **器具事实一：皮肤本身会让个别页掉分**。第 3 格（4 人份加倍）在**冻结基线里是 88**，公共层席交付后、本席一行未动时
   复跑是 **76**（−12：双端 20→14、风格 18→16）。同一把尺、同一页、两次读数；本席的改动只把它从 76 挪到 75。
   ⇒ 建议收口席对 48 页做一次「皮肤前后」的同尺对照，别把「<90」一律记在域席头上。
3. **器具事实二：冻结尺判不了「列对不对齐」这类版式事实**。反例 A 里，把三列栅格退回参差（`50/64/63` ＋ 右侧空 115px），
   评委给分反而从 79 升到 83。⇒ 这类扣分项的销账证据应以**真浏览器几何读数**为准（本席的做法：探针脚本
   `.scratch/t873-做菜/probe.mjs`，只读、落本席草稿目录，未入仓），冻结尺读数只作旁证。
4. **没单独复跑的两页**：第 4、5 格的「皮肤后起点」读数未单独取（第 4 格与第 2 格同源、第 5 格与第 1 格同源）。
   §二 表里已如实标注，不拿同源页的数顶替。
5. **越界报备（未碰，只记录）**：
   - 派单册指定的席位命令内部会跑本域驱动器 `t772-run-cooking.mjs`，它会**重写受版本控制的 `docs/skills/skill-chef/t772-册子片段.json`**
     （工作区里现为 ` M`）。这是**指挥链指定的命令自身的副作用**，不属本席写集，**未提交**，交收口席统一处置。
   - 公共层皮肤的「事实条瓦片」在 390 档把页头四格压到每格 57px 高、值一行放不下时会折行；本域用页内四等分栅格兜住了，
     但**根因在 `src/render/skin.ts`**（公共层席的件），本席一个字没碰，只记录。
6. **本席没做**：菜品图／步骤插画位（数据源里没有图，也不许编造）；`t778-验收墙.mjs` 与册子的重出（收口席统一做）。
