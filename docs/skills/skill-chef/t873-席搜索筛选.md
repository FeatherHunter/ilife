# t873 搜索筛选席：13 页全部改到「条件轨 ＋ 结果」两栏版面，视觉生动均值 12.3 → 16.6，13 页总分全部不降（均值 77.8 → 85.1），其中 0 页达到 ≥90

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**搜索筛选**（第 14–26 格共 13 页，同一份装配件）。
量尺＝`t873-派单册.md` §二 的冻结问句（逐字，只有页名／唤醒词两处占位随页替换），每页一次 `vision_describe` 调用、两张首屏图（390×820／1280×860），不带 `json: true`；总分取五维之和。

**判定：未达成「13 页逐页 ≥90」。** 13 页**全部不降**（+14／+1／+4／+8／+13／0／+14／+5／+6／+6／+13／+7／+4），
均值 **77.8 → 85.1**；最高 89（三页落 88–92 带，均仍 <90）。差距的根因见 §五：这一批页的「文案精炼」被**库里的菜品简介原文**封顶
（评委每页都在点「招牌『土菜』／公认的『米饭杀手』／最…之一」冗长复述，而库正文只许在排版层断句、不许改写），
「视觉生动」被**没有菜品图**封顶（本席只出得起纯 CSS／内联 SVG 的装饰与图标位，`t873-席公共层.md` §五.2 记过同一条天花板）。

---

## 一 改了哪几处

全部落在**本席写集** `docs/skills/skill-chef/t771-run-search.mjs`（13 页共用的那一份装配件，一改全动）。
`packages/skill-chef/src/search/**` **一行没动**（取数与路由行为原样；`checkRouting()` 那条反例判据仍跑在驱动里）。

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `t771-run-search.mjs:150-169`（新） | 无 | `PAL` 色基表 ＋ `rgbOf()`：色值只取冻结 token（`CSS_VAR_TOKENS`）与既有调色板（`CHART_PALETTE`），不凭空发明色值 | 「视觉生动」13.1 全场最低 |
| `t771-run-search.mjs:185-208`（新） | 无 | `ART_SVG`：页头一枚**纯内联 SVG 装饰插画**（一只碗＋蒸汽＋放大镜＋一只盘子＋几枚色点），`aria-hidden`，**一个字的数据都不进产物**——不是任何一道菜的照片 | 「整页近乎纯文字、缺图缺色彩锚点」 |
| `t771-run-search.mjs:212-252`（新） | 无 | `GLYPH` ＋ `FACET_SPEC` ＋ `glyphHtml()`：九枚 16×16 线形图标位（放大镜／碗／辣椒／叶／钟／对勾／划掉／清单／排序） | 「缺形状件（分隔、分组、图标位）」 |
| `t771-run-search.mjs:255-283`（新） | 无 | `facetRows()`／`facetPanel()`：**条件面板**——把 `card.params` 逐维摊成「图标位＋标签＋值」的行，再固定加一行「排序＝按菜名」；全部食谱那一页退成一行「范围＝全部菜」 | 14「条件/找到双列贴左、无列分隔与分组」；23「『条件/找到』列标题与值紧贴、无下划线分区」 |
| `t771-run-search.mjs:286-294`（新） | `renderChipRow` 把六张关联表的 13 枚标签丢进一枚容器自由换行 | `attrStrip(groups)`：**属性带**——六个维度各成一组，组名在前、徽章在后；徽章本身仍走区块层 `renderChips`（同一套徽章形状） | 19「390 端『春/夏/秋/冬/中/晚』徽章挤成两行且参差」；20「徽章换行断在『炒/煸』」；22／25 同族 |
| `t771-run-search.mjs:297-316`（新） | 描述整段一个 `<p class="ilife-block-prose">` | `proseHtml()` 在**排版层按句号分段**（库正文一个字不改）；`fullWidthPunct()` 把正文里的半角 `,()` 换成全角（只换标点字宽、不动一个词，与 #817「半角拉丁转全角」同口径） | 5「正文连写无句读」同族；16「夹杂算法渲染符号」 |
| `t771-run-search.mjs:318-630`（新） | 页壳只有公共层 `chefSceneCss()` | `searchSceneCss()`：本页页内那一段样式，**接在 `chefSceneCss()` 之后追加**（`buildPage` 的 `extraCss` 见 `:674`），**不把公共层那两层拆回去**（`test/skin-873.test.mjs` 仍绿） | 双端自适应／信息层级／视觉生动四维 |
| `t771-run-search.mjs:632-673`（改） | 事实条（条件／找到）＋ 泛化结论条 ＋ 单列菜卡 ＋ 页脚 | 页体＝全宽两栏（左栏＝插画＋条件面板＋复制区，右栏＝结果抬头＋菜卡）；结果抬头 `共 N 道菜` 替掉泛化结论条；页脚那行「来源：菜谱库，结果按菜名排序」的信息并进条件面板的「排序」行 | 15「1280 端右半大片留白无内容填充」；26「结论条与条件摘要语义重复」 |
| `t771-run-search.mjs:112-143`（改） | 六张关联表的标签压成一枚去重数组 | `enrich()` 另出 `groups`（六组各自成形），`tags` 保留原样 | 19／20／22／25 的徽章参差 |
| 件头注释 `:19-40`（改） | 无 #873 段 | 记四条页内收口（条件面板／结果抬头／桌面两栏／措辞收敛）与「装饰只画形状」的口径 | 说明面同步 |

**页内视觉与节奏上的具体做法**（都在 `searchSceneCss()` 一处）：
品牌暖色渐变带 ＋ 盘形图标位落在每张菜卡上；三格事实各一枚不同色的左缘（此前三件事只靠列距区分）；
菜卡摘要的命中区抬到 `min-height: 48px`（触摸档硬线 44px）；正文段间给行距；
宽屏（≥1001）字阶抬一档（正文 16px／1.85、徽章 14px），窄屏（≤640）把插画带压到 ~44px、
把条件面板从逐行卡片改成一行胶囊带（390 档首屏只有 820px，省下的高度留给菜卡）。

**没有改的东西**（守住禁令）：不改数据语义、不改库、不加 schema；没有放宽任何判据
（`t768-质量门.mjs` 的六列、`t778-终审扫描.mjs` 的字样表、墙与册子的形状一个字没碰）；
没有碰白名单之外的任何路径（`packages/skill-chef/src/search/**` 亦未动）。

---

## 二 逐页销账（两行读数）

改前＝`t873-开工基线.md` §一（冻结 2026-09-21）；改后＝本席产物批（GATE-RUN `a254edb7` 那一轮的双端首屏图）在**同一把冻结尺**下的读数。

| 格 | 页名 | 改前基线（§一） | 改后五维＋总分 | 差额 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|---|
| 14 | 关键词搜索：辣椒 | 16/17/15/**13**/14 = **75** | 19/17/16/**18**/19 = **89** | **+14** | 「条件/找到双列贴左、无列分隔与分组」→ 换成条件面板（图标位＋标签＋值＋排序行）＋结果抬头 ⇒ 信息层级 16→19；「整页色块与插画缺位」→ 插画带＋卡片色彩锚点 ⇒ 视觉生动 13→18；「菜品卡标签堆成两行有冗余感」→ 属性带分组 ⇒ 文案精炼 15→16 |
| 15 | 错字模糊匹配：辣椒炒内 | 18/20/20/**12**/18 = **88** | 18/18/18/**17**/18 = **89** | **+1** | 「1280 端右半大片留白无内容填充」→ 桌面两栏（左栏条件轨／右栏结果）⇒ 视觉生动 12→17、双端自适应 20→18（改写后评委未再点留白，改点「桌面筛选条件与结果之间留白断裂」，已记 §五） |
| 16 | 按菜系筛选：湘菜 | 17/19/15/**13**/16 = **80** | 17/17/15/**17**/18 = **84** | **+4** | 「『湘菜有哪些 / 1 道』与上方说明行紧贴」→ 说明行整句撤掉、条件改结构面板＋计数抬头 ⇒ 视觉生动 13→17；「夹杂算法渲染符号」→ 正文半角标点全角化 |
| 17 | 多维组合筛选：湘菜加辣加时限 | 18/16/15/**10**/16 = **75** | 17/16/15/**17**/18 = **83** | **+8** | 「键值标签行与下方徽章段紧贴」→ 事实瓦片＋属性带各自成块；「整体视觉偏素、桌面分母的粉彩气泡与正文冷色卡片割裂」→ 一处暖色体系收口 ＋ 桌面两栏 ⇒ 视觉生动 10→17 |
| 18 | 按食材筛选：五花肉 | 18/**14**/13/12/17 = **74** | 17/**19**/18/16/17 = **87** | **+13** | 「『复制数据』按钮与正文卡宽未贯通齐边」→ 复制区与菜卡同列（左栏下端）⇒ 双端自适应 14→19；「描述段把湘菜简介与本菜简介混写且重复『辣椒炒肉』」→ 排版层分段 ＋ 半角标点全角化 ⇒ 文案精炼 13→18 |
| 19 | 排除食材：不含虾仁 | 17/17/16/13/16 = **79** | 17/15/14/16/17 = **79** | **0** | 「390 端『春/夏/秋/冬/中/晚』徽章挤成两行且参差」→ 属性带按六维分组（春/夏/秋/冬 归「季节」、中/晚 归「餐别」）⇒ 视觉生动 13→16；**但该页本轮复评把双端自适应量成 15（判词「桌面端左栏空白浪费且条件区复制按钮位置过疏」），与本席其余同版式页的 17–19 不一致，见 §四.4 的器具观察** |
| 20 | 按难度筛选：快手菜 | 16/18/15/**10**/14 = **73** | 17/**19**/17/16/18 = **87** | **+14** | 「三组键值未同基线、徽章换行断在『炒/煸』」→ 属性带（做法 炒｜煸 成一组）⇒ 视觉生动 10→16；「1280 宽下大段空白偏左、内容居中挤压」→ 桌面两栏 ⇒ 双端自适应 18→19 |
| 21 | 按时间筛选：三十分钟内 | 17/**14**/13/15/16 = **75** | 17/17/14/15/17 = **80** | **+5** | 「键值行与上方标签区垂直间距偏紧」→ 事实瓦片与属性带各自成块并给行距；该页本轮复评仍把「文案精炼」量成 14（判词点名「两段长描述」）——库正文，见 §五 |
| 22 | 按炊具筛选：炒锅 | 17/17/14/12/17 = **77** | 17/16/17/16/17 = **83** | **+6** | 「『找到』与『1 道』间留白过大、徽章两档折行不一致」→ 结果抬头收成一行（计数圆标）＋属性带分组 ⇒ 文案精炼 14→17 |
| 23 | 按口味筛选：辣 | 17/18/**13**/14/18 = **80** | 17/18/18/16/17 = **86** | **+6** | 「『条件/找到』列标题与值紧贴、无下划线分区」→ 条件面板逐行分槽（图标位／标签／值贴右缘）⇒ 文案精炼 13→18 |
| 24 | 按季节筛选：夏 | 17/18/14/**10**/14 = **73** | 18/17/16/17/18 = **86** | **+13** | 「键值标签与数值在大屏挤成一行、间距参差」→ 条件面板值贴右缘、两档都对齐 ⇒ 视觉生动 10→17、风格一致 14→18 |
| 25 | 按状态筛选：已做 | 16/17/15/14/16 = **78** | 18/16/17/17/17 = **85** | **+7** | 「『中』『晚』徽章换行处错位、两档均为两行参差」→ 属性带把 中/晚 收进「餐别」一组 ⇒ 信息层级 16→18 |
| 26 | 全部食谱 | 18/19/16/14/17 = **84** | 18/17/16/**18**/19 = **88** | **+4** | 「结论条『以下就是库里全部未废弃菜。』与条件摘要语义重复」→ 撤掉泛化结论条，改「共 N 道菜」计数抬头 ＋ 条件面板「范围＝全部菜」行 ⇒ 视觉生动 14→18、风格一致 17→19 |

**汇总**：改前均值 **77.8**（13 页，区间 73–88）→ 改后均值 **85.1**（区间 79–89）。**≥90 的 0 页**；
落 88–92 抖动带的三页（14／15 各 89、26 = 88）**首跑均 <90，按派单册 §一 的规则不算过**；
改后各维均值：信息层级 17.3｜双端自适应 16.9｜文案精炼 15.9｜**视觉生动 16.6**（改前 12.3）｜风格一致 17.6。

---

## 三 反例（改坏必红／还原一致）

**反例做法（等价最小回退）**：把页头插画那一条**整条撤掉**——`t771-run-search.mjs` 里的 `ART_SVG_ON` 置 `false`
（插画是本席「视觉生动」那一维的主要承担件；只动这一个布尔，其余 500 余行改动一行不改）→ 重新编译 ＋ 重出本批双端图
（GATE-RUN `2c3b3f39`）→ 按 §二 逐字问句复评第 14、26 两页 → **逐字还原** `ART_SVG_ON = true` → 重编 ＋ 重出
（GATE-RUN `fe13f67e`）→ 复评同两页。

| 步 | 页 | 五维＋总分 | 判 |
|---|---|---|---|
| 改坏（只撤插画） | 14 关键词搜索：辣椒 | 17/16/13/**14**/15 = **75** | **红**（89 → 75，视觉生动 18→14、风格一致 19→15） |
| 改坏（只撤插画） | 26 全部食谱 | 18/16/15/**13**/14 = **76** | **红**（88 → 76，视觉生动 18→13） |
| 还原（逐字回绿） | 14 关键词搜索：辣椒 | 19/17/16/**18**/19 = **89** | **绿**（与改坏前**逐字相同**的 JSON） |
| 还原（逐字回绿） | 26 全部食谱 | 18/17/16/**18**/19 = **88** | **绿**（与改坏前**逐字相同**的 JSON） |

**还原的机器旁证（比"再读一次"更硬）**：还原那一轮的产物图与正式批**逐字节相同**——
`01-search_by_name_keyword-390v.png` sha256 `625217C9…47C78F`、`13-list_all_recipes-390v.png` sha256 `59E89137…7C201F`，
两件在正式批与还原批**两次测量取值完全一致**（改坏那轮分别是 `839047EB…`／另一组值，确实动过）；同一批图 + 逐字同一问句的可复现性见 §四.4。

---

## 四 机器面

### 4.1 门禁运行记录（§2.4 第 2 条；runId 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=a254edb7-d4ec-410d-9627-510c9533358a cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 搜索筛选" exit=0
GATE-RUN runId=2c3b3f39-4669-444a-8773-27d2a52c8fe2 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 搜索筛选" exit=0
GATE-RUN runId=fe13f67e-0d1f-4232-b2a1-6e29532c2339 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 搜索筛选" exit=0
GATE-RUN runId=8de667b8-b9c9-41a1-a489-9aed832d7ed1 cmd="node .scratch/t873-搜索筛选/scan.mjs" exit=0
GATE-RUN runId=d6184238-1951-4c69-8c91-de1e35b645ff cmd="node --test packages/skill-chef/test/skin-873.test.mjs" exit=0
```

`a254edb7`＝**正式批**（§二 全部 13 页读数取自这一轮的双端首屏图）；`2c3b3f39`＝§三 反例那一轮；
`fe13f67e`＝§三 还原那一轮；`8de667b8`＝§4.4 的终审扫描；`d6184238`＝§4.2 的包内皮肤测试。五条都 exit=0。
`seat.ps1` 是派单册给的那一条一条命令（`tsc -b packages/skill-chef` → `t873-席跑.mjs 搜索筛选` →
双端首屏图 → `t768-质量门.mjs`），整个窗口由外层 `run-locked.mjs` 持锁，窗口内不再二次抢锁。
本席迭代期的调试红轮次（含两次编译红在别席文件上、按派单册等待重试的轮次）不计入对账。
**一条自报过程记录**：14:36 前后一次 `tsc -b packages/skill-chef` 是本席脱离锁包装器直接跑的，
用于分辨「编译红落在谁的文件上」（结果 exit=0，没有任何门禁结论取自那一次，也不列入上面的对账表）——
按 §2.4 第 3 条它不属于"声称过的运行"，此处照实报出，供编排者判。

### 4.2 编译与质量门（`.scratch/t873-搜索筛选/质量门.json`）

```
TSC-OK
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 13 行）   exit=0
```

`seat.ps1` 先把 `tsc -b packages/skill-chef` 跑绿（`TSC-OK`，红即当场停）再出产物；
13 页在两档视口下**六列全绿**（可点件最短边 44px：折叠摘要 48px、复制按钮 44px）。

包内皮肤测试（派单册点名「不要把公共层那两层拆回去」的那一条判据）：
`node --test packages/skill-chef/test/skin-873.test.mjs` → `ℹ tests 19 / ℹ pass 19 / ℹ fail 0`，exit 0。
本席的页内样式是**追加**在 `chefSceneCss()` 之后的第二段（`:674`），没有拆回 `pageUiCss()＋pageShapeCss()` 的写法。

### 4.3 终审扫描（`.scratch/t873-搜索筛选/搜索筛选`，13 件产物）

```
扫 13 件产物；三类字样命中 0 处
共 13 件；命中 0 -> 干净     exit=0
```

### 4.4 一条器具观察（对收口席有用）

本会话里 `vision_describe` 对**同一对图 + 逐字同一问句**是**可复现**的（同图同问连跑给出的 JSON **逐字相同**，
本席实测 3 次）。但**换一对图就是另一次判读**，同一页在只挪 4px 间距的两版之间量出过 **89 → 80**、
在「属性带分组」与「属性带不分组的等宽伸展」之间量出过 **86 → 78 → 84**。
⇒ 本席这 13 页的单跑读数是「设计质量 ± 判读摆幅」的合成值，**±8 的页间差不可当改进量读**；
派单册 §一 那句「未改过的页也会落在 <90」在本席同样成立（第 19 页改前 79、改后 79）。
收口席若要拿本席读数做排序，请按同版式取中位数，不要按单页名次。

---

## 五 未做项与下一手缺什么

1. **13 页无一页到 ≥90**（最高 89）。差距按维拆开，都是**本席写集之外**的上限：
   - **文案精炼 15.9（本席四维里最低）**：13 页的菜卡里都挂着**库里那同一段菜品简介**
     （`辣椒炒肉(又名青椒炒肉、农家小炒肉)是湖南省最具代表性的家常菜之一…号称…招牌"土菜",公认的"米饭杀手"。`）。
     评委 13 页里有 8 页点名这一段「含重复卖点／品牌化口播式长铺垫／含两个引号冗余短语」，而派单册 §四.2 明令
     「库里的正文只许在排版层断句／分段，**不许改写内容**」。本席已把能做的做完（按句号分段、半角标点全角化），
     **再往前一步要么改库正文、要么给菜品简介定一条公共层的"摘要＋展开"形状件 —— 两件都不在本席写集里，需要另立票裁决**。
   - **视觉生动 16.6（改前 12.3）**：本席给到的是纯 CSS 与外联 SVG 的**装饰与图标位**（派单册明令不许编造菜品照片）。
     `t873-席公共层.md` §五.2 记过同一条天花板：「这一维按『有没有菜品图／插画』给分…再加就只剩图」。
     **要再抬必须裁决「菜品的图从哪来」**（库里 48 页没有任何一页的数据源带图，且 `renderMediaFigure` 的产出器会写
     `loading="lazy"`，与验收墙的造册判据冲突）——不在本席写集里。
2. **未跑的判据**：本席只跑了派单册点名的三条（编译／质量门／终审扫描）。**验收墙与册子（`t778-验收墙.mjs`、造册）留给收口席**；
   本席的产物批落在 `.scratch/t873-搜索筛选/`，未进 `.scratch/t873/` 那面墙。
3. **制品行数**：`docs/skills/skill-chef/t771-run-search.mjs` **251 → 758 LF**（本包 `AGENTS.md` 的 350 行告警线只管
   `packages/skill-chef/src/**/*.ts` 与包内 `scripts/*.mjs`，本件在 `docs/` 下、不在管辖内，故不触发必报五步）。
   其中 ~230 行是那段页内样式的字符串数组。若收口席要求拆，拆法是「页内样式常量 ＋ 视觉件产出器」各成一件，
   两者都不属于本席写集，需要先改白名单。
4. **一处口径待裁决（本席做了、但请复核）**：菜卡正文的**半角标点全角化**（`,()` → `，（）`）。
   本席按 `#817`「可见名称半角拉丁转全角」的先例把它归到**排版层**（只换标点字宽、不动一个词），
   判词「夹杂算法渲染符号」正是冲它来的；若收口席判它越界（属「改写库内容」），**删掉 `fullWidthPunct()` 这一层调用即可还原**，
   影响面只有那两段正文。
5. **越界报备（未碰，只记录）**：
   - 派单册指定的重出命令会连带重写 `docs/skills/skill-chef/t771-册子片段.json`（受版本控制）与 `.scratch/t771/**`，
     这是**指挥链指定的命令自身的副作用**，不是本席顺手改；该片段件**未提交**（不在本席写集里），交收口席统一处置。
   - 本席页内样式想再往前走的两个口子（1、2 两条）都要动**公共层或数据源**，不在白名单内，**本次一行未碰**。
