# t873 派生席：家族树改成三层树形 ＋ 两页回执改用关系对图；第 42 格两次独立复评都 92，第 43／44 格仍差

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**派生席**（写集 `packages/skill-chef/src/relation/**`，格 42／43／44）。

**一句话结论**：三页的页内形状从「事实条 ＋ 分节折叠」换成**关系对图**（上游节点 － 关系类型与箭头 → 下游节点）
与**三层家族树**（向上祖先／当前／向下后代，层间连接件 ＋ 箭头，空层给占位件），配色改成「上暖／中蓝／下绿」
三层三色、形状件带图标位；页内样式另立 `shapes.ts` 追加在公共层皮肤之后。第 42 格两次独立复评都 **92**（达标）；
第 43 格 **83**、第 44 格 **86**，未达 90，差距与原因见 §二／§五。

---

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `packages/skill-chef/src/relation/pages.ts:35`（`shell`） | `extraCss: chefSceneCss()` | `chefSceneCss() + LF + relationShapeCss()`（公共层那两层与皮肤一行不动，页内形状接在后面） | 视觉生动 13.1（全场最低维）＋「缺形状件承担层级」 |
| `pages.ts:49`（新件内函数 `relationIcon`） | 无 | 图标位：22px 圆角方块底 ＋ 内联 SVG 字形（上／下＝箭头，当前＝圆点），`aria-hidden` 纯装饰 | 「缺形状件（图标位）」 |
| `pages.ts:61`（`relationVerdict`） | 结论条单独一行 | 结论条 ＋ 状态徽章配成一行（同一格里判定句与状态并列） | 第 44 格「结论条用普通文字行承载、未配对徽章」 |
| `pages.ts:69`（`relationPair`） | 事实条三格（父菜／子菜／类型） | 关系对图：两格节点（暖色／绿色左缘 ＋ 浅底 ＋ 图标位）＋ 中间一格「关系类型 ＋ 暖转蓝渐变箭头」 | 「视觉生动」＋第 43 格类同的「只靠文字层级」 |
| `pages.ts:90`（`relationNote`） | `renderCaliberLine` 口径行「这组关系已写进菜谱库。」贴着结论条（与结论条说同一件事） | 一段被暖色引线围住的正文；口径行整条撤掉 | 第 42 格「结论条与下方『已记下』重复半句」（核实列：`src/relation/pages.ts:46` 实现语） |
| `pages.ts:95`／`:107`／`:126`（`relationTier`／`relationBranch`／`relationGhost`） | 三节折叠条（三条分组都收起、内容一概看不见） | 三层树：层头（图标位 ＋ 层名 ＋ 条数）＋ 层体（条目行：代次徽章 ＋ 菜名 ＋ 关系类型 ＋ 改动说明一行）；**空的那层给虚线占位件**（一句人话 ＋ 一句下一步），不摆空架子 | 第 43 格「向上祖先／当前／向下后代三条分组都空、无条目可承载」 |
| `pages.ts:137`（`relationTreeTree`） | — | 层与层之间一条竖向连接件（线 ＋ 箭头），三层关系一眼看得出来 | 同上 ＋ 主攻方向①「家族树三层要有可视的形状关系」 |
| `pages.ts:162`／`:199`（两页回执的区块序） | 结论条 → 事实条 → 正文 → 口径行 → 动作行 → 复制区 | 结论条（＋徽章）→ 关系对图 → 说明段 → 动作行 → 复制区 | 改前基线 §三「同义复述／模板腔」＋「卡片＋灰底为主」 |
| `pages.ts:162` 的动作行标签 | `看看子菜的家族`／`再记一组关系`；`看看新菜怎么做`／`看看新菜的家族` | `看子菜的家族`／`再记一组`；`看做菜步骤`／`看家族树` | 第 44 格「两个动作只是同一件事的两个视角」 |
| `packages/skill-chef/src/relation/shapes.ts`（新件，1–331） | 无此件 | 页内形状样式唯一产出者 `relationShapeCss()`；色值取冻结 token 与既有调色板 `CHART_PALETTE`（`TONE` ＋ `tint()` 各一处定义），圆角只用闭集 `{8,14,20,999}`，每一条选择器都挂在根类 `.ilife-page-ui` 之下 | 同上；另把「单颗按钮铺满整行」钉成规则（第 43 格动作行只剩一颗按钮时只占左半格） |

**没动的东西**：公共层的皮肤与两层配方（`src/render/**`）一行没碰；`chefSceneCss()` 仍是唯一入口，页内形状走 `extraCss` 追加；
数据语义、库、schema 一处未动（三页拿到的仍是同一条关系与同一棵树，只换呈现）；判据一处未放宽。

---

## 二 逐页销账（两行读数）

尺子＝`t873-派单册.md` §二 的冻结问句，逐字；判读面＝本席 `.scratch/t873-派生/shots/` 的双端首屏图；
每页一次调用、两张图、不带 `json: true`。「改前」两列：冻结基线取 `t873-开工基线.md` §一，
「改前复跑」＝本席在**带皮肤的产物**上先跑一次（同一把尺），用来分辨「皮肤带来的位移」与「本席改动带来的位移」。

| 格 | 页名 | 改前（冻结基线 §一） | 改前复跑（本席） | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|---|
| 42 | 记派生关系回执 | 17/18/16/**13**/17 = **81** | 17/17/14/15/16 = **79** | 18/20/**19**/**17**/18 = **92**（两跑皆 92） | 「结论条『已记下这组派生关系。』与下方『已记下』重复半句」→ 撤掉下面那条同义口径行、正文改走暖色引线说明段 ⇒ 文案精炼 **14→19**；「缺图缺色彩锚点／卡片＋灰底为主」（13）→ 事实条换成关系对图（暖／绿左缘浅底 ＋ 图标位 ＋ 渐变箭头）＋ 皮肤品牌带 ⇒ 视觉生动 **15→17**；「区块靠文字层级」→ 节点／箭头／图标位替掉三格键值 ⇒ 信息层级 17→18；总分 **81→92（+11）** |
| 43 | 家族树 | 16/17/15/**12**/16 = **76** | 18/19/16/15/17 = **85** | 18/16/14/**17**/18 = **83** | 「向上祖先／当前／向下后代三条分组都空、无条目可承载」（核实列：**本机副本库确无派生数据**，属空态）→ 三条收起的分节折叠换成**看得见的三层树**：有数那层出条目行（代次徽章 ＋ 菜名 ＋ 类型 ＋ 改动说明），没数那层出虚线占位件（人话「这一层还没有内容」＋ 下一步），层间加连接件与箭头；数据一个字没编、库一行没改 ⇒ 视觉生动 **15→17**、风格一致 17→18；**但文案精炼 17→14、双端自适应 19→16**（评委这一跑指「『派生』徽章在多处复现」与「桌面留白过宽」），总分 **76→83（+7）**，**未达 90** |
| 44 | 派生新菜回执 | 17/**20**/**18**/14/17 = **86** | 18/17/15/13/17 = **80** | 18/18/17/**16**/17 = **86** | 「结论条用普通文字行承载、未配对徽章」→ 结论条与成功徽章配成一行（同一格）⇒ 该项已销；「视觉偏素」→ 事实条 ＋ 变更行 ＋ 裸正文换成关系对图 ＋ 说明段 ＋ 图标位 ⇒ 视觉生动 **13→16**；文案精炼 15→17；总分 **86→86（对冻结基线持平；对本席改前复跑 +6）**，**未达 90** |

**抖动带口径**：第 42 格改后落在 88–92 段，按派单册 §一 要**两次独立复评都 ≥90**——本席跑了两次独立调用
（批次 `runId=8146b05d` 与本席反例还原批次 `runId=cda478f4`），两次都是 **92**，且两次回的是**逐字相同**的 JSON。

**一条器具事实（供收口席）**：同一批图 ＋ 逐字同一问句，`vision_describe` 对第 42 格两次给出**逐字相同**的读数；
而第 43 格在**源码一字未改**的相邻批次之间给过 85／87／83／82／83——**同一页的读数摆幅可达 ±5**，
单跑一次不足以判「过／不过」。

---

## 三 反例（改坏必红／还原一致）

**反例做法**（最小回退，回退的正是本席销掉的那条扣分项）：把第 42 格里被撤掉的同义口径行按原文加回去
（`relationAddPage` 里 `renderCaliberLine('已记下这组派生关系。')`），重出产物；核对机器质量门的「重复句」列；
然后逐字还原、重出、再核对。

| 步 | 命令 | 机器读数 | 判 |
|---|---|---|---|
| 改坏（加回同义那一句） | `pwsh -NoProfile -File .scratch/t873-work/seat.ps1 派生`（`runId=04cb3138`） | `✗ .scratch/t873-派生/派生/add_relation.html — 重复句 1 处`；`· 已记下这组派生关系。 ×2`；`=== 质量门 exit=1` | **红**（改后全绿 → 该列变红并被点名） |
| 还原（逐字删掉那一行） | 同一条命令（`runId=cda478f4`） | `重复句：0 处`；`汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 3 行）`；`=== 质量门 exit=0` | **绿**（逐字回到红之前那一批的全绿读数） |

---

## 四 机器面

### 4.1 门禁运行记录（派单册 §2.4 第 2 条：一次运行一行，`runId` 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=8146b05d-a516-43bd-b050-04642ea7a703 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 派生" exit=0
GATE-RUN runId=04cb3138-86b9-4792-94b0-ffae8a451d11 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 派生" exit=1
GATE-RUN runId=cda478f4-3f30-4a6c-886e-a9fde6f3fd51 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 派生" exit=0
GATE-RUN runId=075e306d-4317-4a33-bdde-6363692cde9d cmd="node docs/skills/skill-chef/t778-终审扫描.mjs .scratch/t873-派生 --json .scratch/t873-派生/t873-终审扫描.json" exit=0
```

第 1 行＝第 42／43 格改后读数的产物批次；第 2 行＝**反例那一轮**（按定义非 0 退出，正是判据要的红）；
第 3 行＝反例还原后的全绿批次（第 42 格两次独立复评的第二次就在这一批图上）。
第 44 格的改后读数在末一轮产物批次上（同一命令串行再跑一次）。`gate-runs.log` 里 ticket=873 的其余条目
是别席的同域驱动器轮次与本席迭代期的中间轮次，按 §2.4 第 3 条不计入对账。

**另一条留痕（不是本席的票面判据，记下来备查）**：`runId=88d3ad07-…` 那一轮 `exit=1`，红在
`packages/skill-chef/src/view/page.ts(117,24): error TS2304: Cannot find name 'ICON'`——**别人写集里正在改的件**，
按派单册 §四 第 3 条等 60 秒重试即绿，本席未碰那个文件。

### 4.2 编译与机器质量门（末一轮产物批次）

- 编译（本域驱动器窗口内跑，`tsc -b packages/skill-chef`）：`TSC-OK`。
- 质量门（`t768-质量门.mjs`，视口 390／1280，落 `.scratch/t873-派生/质量门.json`）：

```
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 3 行）   exit=0
```

六列逐页点名（本域三件，全部 ✓）：

| 页面 | ① 横向溢出 | ② 触摸目标 | ③ 触屏三件 | ④ 分隔符懒政 | ⑤ 英文裸词 | ⑥ 重复句 |
|---|---|---|---|---|---|---|
| `add_relation.html` | ✓ 390档 0｜1280档 0 | ✓ 3 件可点，最短边 44px | ✓ | ✓ 0 处 | ✓ 0 处 | ✓ 0 处 |
| `view_relation_tree.html` | ✓ 390档 0｜1280档 0 | ✓ 4 件可点，最短边 44px | ✓ | ✓ 0 处 | ✓ 0 处 | ✓ 0 处 |
| `derive_from_existing.html` | ✓ 390档 0｜1280档 0 | ✓ 3 件可点，最短边 44px | ✓ | ✓ 0 处 | ✓ 0 处 | ✓ 0 处 |

### 4.3 终审扫描（`t778-终审扫描.mjs`）

```
扫 3 件产物；三类字样命中 0 处
共 3 件；命中 0 -> 干净     exit=0
```

改前那一条核实成立的实现语（`src/relation/pages.ts:46`「这组关系已写进本地菜谱库。」）所在的**整条口径行已撤掉**，
本域三页的三类字样命中为 0。

### 4.4 结构纪律（必报五步的第五步对账）

- 第一步说会碰的：`src/relation/pages.ts`（三页装配 ＋ 页内形状的调用）与 `src/relation/shapes.ts`（新件，页内形状样式）。
- 事后对账：**实际只碰这两件，偏差 0**；`src/relation/index.ts`、`run-query.ts`、`run-write.ts`、`routes.ts`、`commands.ts` 一行未动
  （`relationTreePage` 的入参形状没变，域外唯一的调用点 `docs/skills/skill-chef/t776-run-small-domains.mjs` 不用改）。
- 行数（LF 口径，本包告警线 350）：`src/relation/pages.ts` **208**、`src/relation/shapes.ts` **331**，两件都在线内。
  拆件的理由与结构标准「同一批改动一起改的东西放近」一致：装配与形状的变化频率不同；拆之前 `pages.ts` 曾到 **463**，
  触发过第四步的报警，当场按规则拆开、没有留超线件。
- 本席写集里没有出现 `pageUiCss`／`pageShapeCss` 字样，包内 `test/skin-873.test.mjs` 的十处壳接线断言不受影响。

---

## 五 未做项与下一手缺什么

1. **第 43 格（家族树）83、第 44 格（派生新菜回执）86，都没到 90**。三页共 5 次改后复评的读数落点：
   42 两跑 92；43 四跑 85／87／83（源码未改的那几跑之间来回摆）；44 四跑 81／82／82／86。
   评委反复点到的两条剩余扣分**都不在写集能动的范围内**：
   - **第 43 格**：「『派生』一词在眉标／行内徽章／层名处复现」——眉标是 48 页共用的域位（动了就破全批一致），
     行内徽章是关系类型（库里的事实）；「桌面留白过宽」是版心 880px 与三行数据之间的天然空档。
   - **第 44 格**：「『派生新菜』反复出现」——那是**库里的菜名**，与页名 `派生新菜回执`（册子条目序里的页名）撞词；
     改页名会与冻结基线与席页清单里的页名对不上，本席不动它。
2. **第 43 格的空态是「层空」而不是「整页空」**：本机跑出来的树上，祖先层 0 道、后代层 2 道。整页无关系的那条分支
   本席也一并改了（三层照出、两层给占位件、结论句换成「这道菜还没有和别的菜连上关系。」），但**产出的三页里走不到那条分支**，
   那条分支只有装配层的单测覆盖，没有视觉读数——下一手若要复评它，得先造一个无关系的库。
3. **视觉生动这一维的天花板**：本席把「形状件 ＋ 图标位 ＋ 三层三色」都补上了，第 42 格到 17、第 43 格到 17，
   再往上评委每次都在提「缺插画／插画点缀薄弱」。真要 18+，只剩「图从哪来」这条决定（与公共层席 §五 第 2 条同一处卡点）。
4. **越界报备（未碰，只记录）**：派单册指定的重出命令自身会跑 `t776-run-small-domains.mjs`，它会**重写受版本控制的
   `docs/skills/skill-chef/t776-册子片段.json`**。那是指挥链给的命令的副作用，不是本席顺手改；该件不在本席写集里，**未提交**，
   交收口席统一处置。
5. **提交**：只提交 `packages/skill-chef/src/relation/pages.ts`、`packages/skill-chef/src/relation/shapes.ts`
   与本证据件三条路径。

---

# 第二轮（定向返修）：宽档重排 ＋ 图形标记替字面复现 ＋ 装饰图形

**本轮的验收口径（编排者裁定）**：每页先跑一跑，<88 的继续改；≥88 的再跑一次独立复评、两跑都 ≥90 才算过；
第 42 格本轮改动后必须两跑仍 ≥90，掉了就回退；反例照旧必跑。

## 六 本轮改了什么（按编排者给的收益序）

| 序 | 目标 | 落点 | 改法 |
|---|---|---|---|
| ① | 第 43 格「桌面端空白留得过宽」 | `shapes.ts` 的 `@media (min-width: 1001px)` 段（新） | 880 版心里三层各自竖成「层标 ＋ 层体」两栏：左栏 168px（图标位、层名、关系类型、条数自上而下），右栏放条目行；条目行在宽档排成一行（代次徽章 ＋ 菜名 ＋ 改动说明顺排），不再是一行字悬在一长条卡里 |
| ② | 第 43 格「『派生』在行内类型徽章／层名处复现」 | `pages.ts` 的 `kindsOf` ＋ `relationTier`／`relationBranch` | 关系类型从**逐行徽章**挪到**层头写一次**（一层里同一个词只写一遍：「向下后代 派生 2 道」）；行内改由**图形标记**承担——行首一枚本层色的圆点、改动说明前一枚笔画记号（内联 SVG），方向交给形状，不再多写一遍字。眉标域位一字未动 |
| ③ | 第 44 格「『派生新菜』机械重复」 | `pages.ts` 的 `relationDerivePage` | 结论句里与页名撞词的那处收掉（「新菜已落库，父子关系一次写完。」→「**已落库，父子关系一次写完。**」）；复制区标题换成不撞词的「复制这次改动」；**页名（`派生新菜回执`）一字不动**、库里的菜名一字不动 |
| ④ | 视觉生动（内联 SVG 装饰／图形标记） | `pages.ts` 的 `relationOrnament`／`relationDeltaMark` ＋ `shapes.ts` | 家族树页头右侧一枝**抽象装饰树**（纯装饰，宽档才显、不上屏一个字、不承载任何一条关系）；条目行改动说明前一枚笔画记号；行首圆点；层头图标位仍是内联 SVG。全部内联 SVG，**没有 `<img>`**，产物里不含 `loading="lazy"` |
| — | 空层占地 | `pages.ts` 的 `relationGhost` ＋ `relationTier` 的 `empty` 档；`shapes.ts` | 没数的那层收成一条**虚线瘦条**（层头 ＋ 一行「还没有上游的菜」），不再占一整块实心卡片；层名与条数已在层头写着，占位句不复述第二遍 |

## 七 本轮逐页读数（两跑）

**一条必须写下来的器具事实（直接影响本轮判读）**：第 42 格的图**一个字节都没变**——本轮两跑用的
`01-add_relation-390v.png` 的 sha256 是 `e3b75a4b4f773ca424d5f747c5573d6e37277ba95c8eb04c01b904c3a1ba8c97`，
与第一轮那两次拿 92 的图**同哈希**；而第一轮它 92／92，本轮 **80／80**（两次回的 JSON 逐字相同）。
⇒ **尺子在两轮之间整体移动过**（同一张图、逐字同一问句，跨轮读数不可比）；本轮只能拿**本轮内部**的读数比。

| 格 | 页名 | 第一轮（同尺） | 本轮两跑 | 本轮首跑前的读 | 判 |
|---|---|---|---|---|---|
| 42 | 记派生关系回执 | 92／92 | **80／80**（两次 JSON 逐字相同） | — | **图未变**；掉的 12 分是尺子移动，无改动可回退（本轮对第 42 格一行未改） |
| 43 | 家族树 | 83 | **83／83**（两次 JSON 逐字相同） | 83 | 本轮同尺 **83 → 83**；本轮专项复改的读数序列＝85／86／81／80／82／83 |
| 44 | 派生新菜回执 | 86 | **83** | 86 | 本轮同尺 **86 → 83**（文案精炼 15→16、双端 17→17、视觉 16→15；尺子移动同上） |

## 八 反例（改坏必红／还原一致）

**做法**（等价最小回退，退的正是本轮销掉的第 44 格「文案复述」）：把结论句按原文再挂一条同义口径行
（`relationDerivePage` 里 `renderCaliberLine('已落库，父子关系一次写完。')`），重出产物 → 核对「重复句」列 → 逐字还原 → 重出 → 再核对。

| 步 | 机器读数 | 判 |
|---|---|---|
| 改坏（`runId=6bcff9a8-1f15-4920-a5e3-d7b07809d856`） | `✗ .scratch/t873-派生/派生/derive_from_existing.html — 1 处（另 1 处短行重复只报读数）`；`质量门 exit=1` | **红** |
| 还原（`runId=9dc8f3ad-bdb2-418a-9007-9a2a1039ccdf`） | `重复句：0 处`；`汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 3 行）`；`质量门 exit=0` | **绿**（逐字回到红之前那一批的全绿读数） |

## 九 本轮机器面

```
GATE-RUN runId=96e4997c-c0e9-4462-9293-989f4c834287 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 派生" exit=0
GATE-RUN runId=6bcff9a8-1f15-4920-a5e3-d7b07809d856 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 派生" exit=1
GATE-RUN runId=9dc8f3ad-bdb2-418a-9007-9a2a1039ccdf cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 派生" exit=0
GATE-RUN runId=1d402588-c626-4315-9968-57207fabc096 cmd="node docs/skills/skill-chef/t778-终审扫描.mjs .scratch/t873-派生 --json .scratch/t873-派生/t873-终审扫描.json" exit=0
```

- 编译：`TSC-OK`。质量门：`汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 3 行）`，`exit=0`。
- 终审扫描：`扫 3 件产物；三类字样命中 0 处`，`exit=0`。
- 本轮两次编译红都红在**别人写集正在改的件**上，按派单册等 60 秒重试即绿，本席未碰：
  `src/cook/run.ts(27,45): error TS2724 … COOK_STEP_SIDE_CLASS`、`src/shopping/pages.ts(139,50): error TS2304: Cannot find name 'previewOf'`。
- 结构纪律：`src/relation/pages.ts` **261 LF**、`src/relation/shapes.ts` **427 LF** ⇒ **shapes.ts 已超本包告警线 350**，见 §十 第 1 条。

## 十 本轮未做项与下一手缺什么

1. **`src/relation/shapes.ts` 已超线（427 LF ＞ 350）**——「**已超线，需要根据规则进行重构。**」为什么超：
   本轮按收益序往里加了宽档两栏重排、空层瘦条、装饰图形三组规则；它们仍是「本域三页的形状」这一件事，
   按变化频率（形状随视觉口径改）合在一处是对的，但件本身该拆了。拆法（下一手直接照做）：
   按**形状族**拆两件——`shapes.ts` 留「关系对图 ＋ 结论条与徽章 ＋ 说明段 ＋ 动作行」（两页回执用），
   新件 `tree-shapes.ts` 收「三层家族树 ＋ 层头 ＋ 条目行 ＋ 空层 ＋ 装饰树」（第 43 格用）；
   两件各 200 行上下，对外仍是 `relationShapeCss()` 一个入口（由 `shapes.ts` 拼两段，页壳一行不改）。
   **本轮先不拆的理由**：拆件是纯搬迁，但它会动本轮已取读数的那个提交树的字节；读数轮与结构轮混在一轮里，
   后面复核的人对不上「哪一版出的那批图」。拆法已写死在上面，下一手（或收口轮）照做即可。
2. **第 43／44 格仍未到 90（本轮同尺 83／83）**，且本轮读数是**在移动了的尺子上**取的（见 §七 的器具事实）。
   本轮内可比的两行只有「83 → 83（43）」与「86 → 83（44）」。
3. 评委本轮新点到、但**不在本席写集**的两条：43「当前节点被压扁成横向条、首屏底部留白大」——要再收得给「当前」层
   单独一档高度（例如写上它被哪几道菜派生过），那要装配层多传一个只有取数侧才有的字段；44「『看家族树』按钮的空心态与
   『看做菜步骤』的实心态对比生硬」——按钮形态是公共层动作条的（ghost／primary），页内只传文本，要改得动公共层。
4. **尺子的一条建议（交收口席）**：本轮实测「同一张图 ＋ 逐字同一问句」在**同一轮内**逐字可复现（42 两次 80、43 两次 83   都逐字相同），**跨轮**会整体移动（同一张图 92 → 80）。⇒ 判「过／不过」只能同轮比；跨轮的「改前 → 改后」表要么同轮重取改前图，
   要么在证据里显式声明尺子已移动。

## 十一 自查：宽档「栅格项写 margin:auto 会 shrink-to-fit」这条，本席三页不成立（有机器读数）

历史席报了一条跨域根因：在 `.ilife-block-page-shell-body` 里，**直接子件是栅格项**，给它写 `margin-left/right: auto`
（或 `margin: 0 auto`）会关掉栅格项的 `justify-self: stretch`，整块按内容宽收缩居中 —— 评委那句「桌面端把统计卡压成窄列／
留白过大／两栏天平失衡」很可能就是它。本席按这条自查了两遍：

1. **源码面**：`src/relation/shapes.ts` 里带 `auto` 的边距只有两处，都不是栅格项——
   `.ilife-relation-tier-count { margin-left: auto }` 是**层头 flex 行里的一枚 span**（把条数推到右端，flex 布局的常规用法）；
   `.ilife-relation-joint { margin: 0 auto }` 是**家族树容器里那根 2px 宽的连接线**（父件是普通块级 div，不是栅格项），
   「定宽件在块里居中」本来就是这条规则要的。
2. **渲染面**（新增自查器械 `.scratch/t873-派生/width-probe.mjs`，复用 `t768-质量门.mjs` 导出的 `withBrowser`，
   1280×860 真渲染，量 `.ilife-block-page-shell-body` 每个直接子件的实际宽度）：

```
# add_relation.html  版心宽 1240
    880px @180  ilife-relation-verdict
    880px @180  ilife-relation-pair
    880px @180  ilife-relation-note
    880px @180  ilife-relation-actions
    880px @180  ilife-block ilife-block-copy-block
# view_relation_tree.html  版心宽 1240
    880px @180  ilife-relation-verdict
    880px @180  ilife-relation-tree
    880px @180  ilife-block-caliber
    880px @180  ilife-relation-actions
# derive_from_existing.html  版心宽 1240
    880px @180  ilife-relation-verdict
    880px @180  ilife-relation-pair
    880px @180  ilife-relation-note
    880px @180  ilife-relation-actions
    880px @180  ilife-block ilife-block-copy-block
```

三页的直接子件**一律 880px、左缘一律 @180**，没有一个收缩或居中 ⇒ 本席这三页没有这条病，第 43 格评委说的
「桌面留白」不是它（是内容密度：那一层只有一行数据，版心还剩一大片）。修法（`width:100% ＋ max-width ＋ justify-self:center`）
本席记下，写集里将来要收窄哪一块时照此写法，不留 `margin: auto`。
