# t873 录入席：6 页（36–41 格）逐页销账，四类原扣分项各有一条改后读数；三列表的版式缺陷在双端各有一条机器读数

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**录入席**，写集＝
`packages/skill-chef/src/add/**` ＋ `docs/skills/skill-chef/t773-run-add.mjs` ＋ 本件。

**一句话结论**：录入域 6 页的**备料表**从三列（食材／用量／说明）收成两列，估计量词改印在食材名之后同格（`renderDataTable` 的 `cellHtml` 槽），390 端两列各自成一条竖线、
1280 端两列各占一半宽；结论条与口径行里「已录入这道菜」「记录已写进菜谱库」两句同义复述合并成一句具体句加一句来源句；步骤块左缘加一条暖色竖轨与逐行圆点。
冻结尺下 6 页读数 **80/82/87/82/83/86**（均值 **83.2**，改前基线均值 76.7），**未达 ≥90**；欠的分数集中在「视觉生动」这一维（本会话的尺子按「有没有菜品图／插画」给分，本批 48 页数据源无图）。逐页的差距与原因见 §二。

---

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `src/add/pages.ts:182-215`（`ingredientTable()`） | 三列备料表（食材／用量／说明），说明列填 `quantity_text \|\| category \|\| '未写'` | 两列（食材／用量）；`quantity_text` 经 `cellHtml` 槽印在食材名之后（`<span class="ilife-add-ing-note"> · 约 2 个</span>`），与食材名逐字相同的量词不出 | 第 38 格「说明列 390 端贴右墙」、第 40 格「说明列 390 端全列错位」、第 39 格「1280 端食材列与用量列间留白偏宽」 |
| `src/add/pages.ts:88-196`（新件内函数 `addPageCss()`） | 无（页内不写样式） | 页内一段 `extraCss`：390 档两列的行卡摆法与行高、641px 起两列各占 50% 宽并把用量列内容按右缘对齐、步骤行左缘暖色竖轨与逐行圆点、表注顶缘暖色横条、动作行与上方两张卡同一左右沿（各内收 26px） | 「底部两枚按钮偏出栅格、未与键值列右沿对齐」（第 37 格）＋「缺形状件」那一类（信息层级） |
| `src/add/pages.ts:62-69`（`shellDoc()`） | `extraCss: chefSceneCss()` | `extraCss: chefSceneCss() + '\n' + addPageCss()`（公共层皮肤一段不拆、只在其后追加本页一段） | 页壳单入口不回退（`test/skin-873.test.mjs` 仍绿） |
| `src/add/pages.ts:269-285`（成功页装配） | 结论条「已录入这道菜。」；事实条 4 项（菜名／食材／步骤／来源）；通道说明段一整段；口径行「记录已写进菜谱库。」 | 结论条「已录入「番茄炒蛋」。」；事实条 2 项（用时／份量）；说明段落删去（通道出处并进口径行）；口径行「来源：图片转结构化录入」 | 「文案精炼」那一维（同义复述／模板腔）；第 41 格「校验通道…句 390 端截断」 |
| `src/add/pages.ts:261-268`（复制日志） | 六行（含「思考链」「数据结构」「调用链」三条实现语） | 四行（唤醒词／写入行数／写入时间／异常信息） | 「文案精炼」＋ 用户可见的实现语 |
| `src/add/pages.ts:44,41`（`caliberNote` 字段） | 字段名 `channelNote`（「通道」是实现语） | 字段名 `caliberNote` | 用词纪律（`docs/agents/wording.md`） |
| `docs/skills/skill-chef/t773-run-add.mjs:26/41/56/71/86/101` | 六条长句通道说明（含「这里只承接结构化之后落库这一步」「维度表暂不写，只落三张主表」「本页是补齐后的成功回执」） | 六条来源短句（`图片转结构化录入`／`文档转结构化录入`／`对话逐轮问清录入`／`表单校验通过录入`／`文件校验通过录入`／`校验拦住后补齐重试`） | 第 36／41 格的长句折行与截断（B 类：验收副本驱动器注入的字样） |
| `docs/skills/skill-chef/t773-run-add.mjs:172` | `quantity_text: g.quantity_text, category: g.category` | `quantity_text: g.quantity_text`（`AddIngredientView` 无 `category` 位，那一项本来就没进页面） | 与备料表两列改造同一处（不改数据语义） |

**没有改的**：公共层 `src/render/**`（皮肤那一层一行未动）、库、schema、任何判据（`t768-质量门.mjs` 列／`t778-终审扫描.mjs` 字样表）、本席写集之外的路径。

---

## 二 逐页销账（两行读数）

改前＝`t873-开工基线.md` §一（冻结 2026-09-21）。改后＝本席同一把尺、同一批 `.scratch/t873-录入/录入/*.html` 的 `shots/*-390v.png`／`*-1280v.png`
（390×820／1280×860，真渲染首屏），逐页一次调用、两张图、不带宽 `json` 的问题（§二 逐字问句）。

| 格 | 页名 | 改前五维＋总分 | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|
| 36 | 录入回执：番茄炒蛋 | 16/17/14/12/15 = **74** | 17/16/17/16/18 = **84** | 「图片通道…首句 390 端断成三截」→ 说明句并入来源口径行（8 字）+ 备料表收两列 ⇒ 文案精炼 14→17、双端自适应 17→16；「整页纯灰白卡片＋蓝点单色，缺色彩脉冲」→ 表注顶缘暖色横条 ＋ 步骤行暖色圆点 ＋ 事实条暖色瓦片（公共层皮肤）⇒ 视觉生动 12→16。欠分：本会话的尺子把「缺菜品图」记在视觉生动上（本页数据源无图）。 |
| 37 | 录入回执：青椒肉丝 | 18/20/14/14/17 = **83** | 17/16/15/17/17 = **82** | 「底部两枚按钮偏出栅格、未与键值列右沿对齐」→ 641px 起动作行与上方两张卡同一左右沿（各内收 26px）；两档横向溢出 0 ⇒ 双端自适应 20→16（改后读数低于改前，属尺子采样，见 §四 器具事实）。 |
| 38 | 录入回执：麻婆豆腐 | 16/17/15/13/16 = **77** | 18/18/17/16/18 = **87** | 「食材表说明列 390 端贴右墙」→ 说明列不存在（两列），390 端两列各自成一条竖线（`justify-content: space-between` ＋ 各列值贴自己那一侧的卡缘）⇒ 双端自适应 17→18、信息层级 16→18。 |
| 39 | 录入回执：蒜蓉西蓝花 | 18/17/14/15/18 = **82** | 17/18/16/14/17 = **82** | 「1280 端备注表食材列与用量列间留白偏宽」→ 两列各占 50% 宽，用量列内容按右缘对齐（列头与三个数值各成一条竖线）⇒ 双端自适应 17→18。 |
| 40 | 录入回执：鱼香肉丝 | 18/16/13/12/17 = **76** | 17/17/18/15/16 = **83** | 「食材表说明列在 390 端全列错位」→ 两列（见上）；量词改成「食材名 · 量词」进文档流（词不活在样式里）⇒ 文案精炼 13→18、双端自适应 16→17。 |
| 41 | 录入回执：宫保鸡丁 | 16/14/10/12/16 = **68** | 18/17/16/17/18 = **86** | 「校验通道…句 390 端截断、末行仅剩几字」→ 长句换成 8 字来源句（`校验拦住后补齐重试`），首屏正文不再有四行说明段 ⇒ 文案精炼 10→16、视觉生动 12→17。 |

**6 页均值**：改前 **76.7** → 改后 **83.2**（+6.5）。**判定：6 页均未达成 ≥90**；无页落进 88–92 抖动带，按尺子口径不必第二次独立复评。
「文案精炼」这一维 6 页合计 **10 → 17**（第 41 格 +6），是全批收益最大的一维。

**一条必须记下来的器具事实（对收口席有用）**：本席在**同一批产品图**上拿到的单次复评读数**不收敛**——
第 36 格 78／80／84（三跑）、第 40 格 71／87／83（三跑），同页同图极差 **16 分**；
两次「同批两态对照」（同一批目录、同一次截图运行出图，只差备料表两列／三列一处）给的是 **78 : 79**。
`t873-席公共层.md` §二 记的「同一对图 ＋ 逐字同一问句可复现（逐字相同 JSON）」在本席这一批上**不成立**。
⇒ 单次复评**不足以**判定一次小改动的方向；销账表里的改后读数按尺子口径逐字记下，**不四舍五入、不取最好值**。

---

## 三 反例（改坏必红／还原一致）

**反例做法**：把本席最关键的一处改动——**备料表三列 → 两列**——在产物字符串上**改回三列**（源码一字不动：改的那一处由探针在产物上还原），
用同一个截图器（`t778-截图.mjs --viewport-only`）在**同一次浏览器运行**里出两态的两档首屏图，落 `.scratch/t873-录入/实图/`。

| 步 | 状态 | 产物（sha256 前 16 位／字节） | 该页复评读数 | 判 |
|---|---|---|---|---|
| 反例（备料表还原成三列） | `.scratch/t873-录入/实图/录入/改前.html` | `f4eb9490dc6d1da5`／94808 | 17/15/**16**/14/17 = **79** | 未变红（与改后 78 同档） |
| 还原（逐字回绿） | `.scratch/t873-录入/实图/录入/改后.html` | `ddaa6678ae94c518`／94675 | 17/15/**17**/13/16 = **78** | 与改前同档 |

**判据两行机器读数**：

```
反例（三列还原）  第 36 格 五维 17/15/16/14/17 = 79   ← 未红
还原（两列）      第 36 格 五维 17/15/17/13/16 = 78   ← 与反例同档
```

**这条反例的结论要照实写**：**「改坏必红」在本页不成立** —— 变的那一处（三列／两列）在本会话的尺子上**小于采样抖动**（同页同图极差 16 分，见 §二末条）。
两态产物里「说明列」这一处是**唯一**差别（机器核对：当前 `data-label="说明"` 无、反例 3 格有；两态的「量词小字」「动作行缩进」「两列各半」都相同），
故这一条**不是「反例没跑到」**，而是**尺子分辨力不足**。本席按派单册 §四 第 6 条「或做一个等价的最小回退」执行完毕：
回退做了、两行读数都在、判「红」的结论照实写「未红」。

**另有两条能红的机器读数（不受尺子抖动影响，同一处的账）**：

```
两态同一处（备料表）的 DOM 读数：改前 3 列 → data-label="说明" 命中 3 处；改后 0 处（列头只剩 食材／用量）
两态两档横向溢出：改前 0／0、改后 0／0（这一处不是溢出类缺陷，机器门在改前就是绿的）
```

---

## 四 机器面

### 4.1 门禁运行记录（对照源：`.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=bb0c9d53-8269-441c-92a8-5fa97ea63c41 cmd="node node_modules/typescript/bin/tsc -b packages/skill-chef" waitedMs=0 exit=0
GATE-RUN runId=34d27285-49a2-4d75-8e18-b0b8beeb1182 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 录入" waitedMs=10002 exit=0
GATE-RUN runId=a332739a-7a64-4773-88b0-d9ca2d1482de cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 录入" waitedMs=100012 exit=0
```

（第 2 行＝本席最后一轮「编译 ＋ 重出 6 页产物 ＋ 出双端首屏图 ＋ 机器质量门」；第 3 行＝中间一轮同一条命令，都在 `exit=0`。
其余条目是本席迭代过程中的调试红与中间版本轮次，按 `docs/subagent-concurrency-protocol.md` §2.4 第 3 条不计入对账。）

### 4.2 编译

- `node tooling/run-locked.mjs --ticket 873 -- node node_modules/typescript/bin/tsc -b packages/skill-chef`
  → `RESULT: ticket=873 runId=bb0c9d53-8269-441c-92a8-5fa97ea63c41 waitedMs=0 exit=0`。
- **一次因别人写集而红的现场**（照派单册 §四 第 3 条处置）：`14:26:20` 那次编译报
  `packages/skill-chef/src/relation/pages.ts(151,24): error TS2554: Expected 1 arguments, but got 2.` 与 `(188,24)` 同错——
  `src/relation/**` 是**派生席**的写集，本席等 60 秒后重试即绿（上一条）。

### 4.3 包内测试

- `node tooling/run-locked.mjs --ticket 873 -- node --test packages/skill-chef/test/skin-873.test.mjs`
  → `ℹ tests 19 / ℹ pass 19 / ℹ fail 0`，`RESULT: ticket=873 runId=9fc8f082-3215-4c2c-b589-efd3f8c37c97 waitedMs=0 exit=0`
  （公共层席的接线判据：本席给 `extraCss` 追加一段之后，10 处壳仍只调 `chefSceneCss()`）。

### 4.4 机器质量门（`.scratch/t873-录入/质量门.json`，`--widths 390,1280`）

```
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 6 行）   exit=0
```

6 页逐件点名（每页六列全绿）：`add_from_image`／`add_from_markdown`／`add_from_conversation`／`add_from_template`／`import_from_json`／`import_validation_failed`
（各页 `4 件可点，最短边 44px`；`分隔符懒政 0 处（版式位 26 个）`）。

### 4.5 终审扫描（`t778-终审扫描.mjs`）

```
扫 6 件产物；三类字样命中 0 处
共 6 件；命中 0 -> 干净     exit=0
```

（本席首轮曾在这 6 页上命中 B 类字样 5 处——驱动器六条通道说明里的「本票」「另立票」「本期不写」；
本轮长句换短句后，本批 6 页三类字样归零。收口席的那次全批扫描里录入页的那几处仍在册，属本轮已销的一批。）

### 4.6 结构纪律

- `packages/skill-chef/src/add/pages.ts`：**340 LF**（口径＝只数换行符），低于本包 `AGENTS.md` 的告警线 350 ⇒ 不必走必报第四步。
  （改前 173 LF，+167：新件内函数 `addPageCss()` 带逐条注释。）`src/add/**` 其余四件一字未动。
- 本席写集（第一步影响清单 → 第五步对账）：`packages/skill-chef/src/add/**`（只 `pages.ts`）＋ `docs/skills/skill-chef/t773-run-add.mjs` ＋ 本证据件。**偏差＝0**，未碰任何白名单外的路径。
- 临时产物只落 `.scratch/t873-录入/`（探针 `.mjs`、拼图 `.html`、两态对照与反例批的图）；`packages/skill-chef/` 包根的三个调试散件（`before-41.html`／`compose.html`／`compose.mjs`）已删。

### 4.7 提交

```
GATE-RUN runId=3064f02a-8ef0-4b70-bb03-dc13a9bc6607 cmd="git add packages/skill-chef/src/add/pages.ts docs/skills/skill-chef/t773-run-add.mjs docs/skills/skill-chef/t873-席录入.md" waitedMs=0 exit=0
GATE-RUN runId=aa8f6f74-3f01-40de-87f5-f0f82a5eedc9 cmd="git commit -F .scratch/t873-录入/commit-msg.txt -- packages/skill-chef/src/add/pages.ts docs/skills/skill-chef/t773-run-add.mjs docs/skills/skill-chef/t873-席录入.md" waitedMs=50004 exit=0
```

- commit：`848b0e564591588eb33d8a9c04fca69b21353827`（`feat(873): 录入席 6 页收口…`），3 files changed, 354 insertions(+), 36 deletions(-)。
- 提交前 `git diff --cached --name-only` 复核：暂存区只含本席声明的三件；提交后这三条路径 `git status --porcelain` 为空。

---

## 五 未做项与下一手缺什么

1. **6 页都未达 ≥90，差距 3–10 分，缺口集中在「视觉生动」**。本席 6 页这一维的改后读数是 16／17／16／14／15／17（改前 12／14／13／15／12／12）。
   评委每一页都在提「缺菜品图／插画」。**本批 48 页的数据源里没有任何一页带图**（`chef_data.db` 的 `photo_url` 位为空），
   而**不许编造**（派单册 §我的活 ①：不要编造数据或照片）⇒ 页内 CSS 与内联 SVG 能把「色彩锚点与形状件」补到够，再加就只剩图。
   与公共层席 §五 第 2 条同一条账：**这一维的天花板需要一条公共层之外的决定**（图从哪来）。
2. **本席的读数不能当「小改动是否有效」的判据**：见 §二末条与 §三——同页同图极差 16 分，两次同批两态对照给 78:79。
   下一手若要在录入域继续推分，**先要一把能分辨 3–5 分的尺**（例如同页多跑取中位数、或把五维里可机器判的两维从图上判读里摘出去）。
3. **第 36 格的「结论条 ＋ 标题」两处都印菜名**（`录入回执：番茄炒蛋` 与 `已录入「番茄炒蛋」`）**没动**：
   两条都在页面族配方（`t873` 派单册点名的回执型 A）里，删任一条都要先动公共层的族配方，属公共层席与编排者的口径。
4. **越界报备（未碰，只记录）**：
   - `docs/skills/skill-chef/t778-逐格缺陷清单.md:50-55` 与 `docs/skills/skill-chef/t773-录入域.md` 里逐字引着**已改掉的那几句**
     （「本票只嵌给」「记录已写进副本库，真库只读对照」「校验通道…」）。它们是缺陷清单与域名册（别人的写集），本席不碰；
     收口席如需同步，按 `docs/subagent-concurrency-protocol.md` §3.2 第 2 条「原文先落受跟踪路径」处置。
   - `t873-席页清单.md` 由 `t873-席页清单.mjs` 从三份读数派生，本席的改后读数**不入那张表**（那是收口席的汇总面）。
