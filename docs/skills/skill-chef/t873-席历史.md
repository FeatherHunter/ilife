# t873 历史席：4 页同尺复跑三升一降（+4／−4／+4／+10），最高 88 未达 90；第 32 格的下降已定位到同一把尺的批次差

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**历史席**，写集 `packages/skill-chef/src/history/**`，页＝第 31–34 格共 4 页。

**一句话结论**：4 页的**原扣分项逐条销账完毕**（逐条见 §二），机器面六列全绿、终审扫描命中 0；判读面在同一把冻结尺、同一会话里复跑，**改后对改前（本席复跑）＝ 31 格 +4、32 格 −4、33 格 +4、34 格 +10**，均值 82.0 → 85.5。**没有一页达到 ≥90**（最高 31 格 88，落在派单册 §一 的 88–92 抖动带）——差距与原因见 §五。

---

## 〇 一条器具事实（收口席必读：本票的读数不能与 `t873-开工基线` §一 直接相减）

`t873-开工基线.md` §一 那 48 个总数是 **#871 会话里**的法官读数；本席在**本会话**里对 `t873-开工基线` 用的**同一批改前图**（`.scratch/t873/shots/31…34-*.png`，一个字节都没动）复跑同一段问句，两批读数**不同**：

| 格 | 冻结基线 §一（#871 会话） | 本席复跑同一张改前图（本会话） | 差 |
|---|---|---|---|
| 31 | 17/18/16/13/16 = **80** | 18/18/16/15/17 = **84** | +4 |
| 32 | 18/20/18/14/17 = **87** | 18/20/17/14/18 = **87** | ±0 |
| 33 | 18/19/17/15/17 = **86** | 18/16/17/15/16 = **82** | −4 |
| 34 | 18/17/16/13/18 = **82** | 16/18/13/12/16 = **75** | −7 |
| 均值 | **83.8** | **82.0** | −1.8 |

另有两条同源实测（本会话内）：

- **同一批图 ＋ 逐字同一问句，连跑两次给出逐字相同的 JSON**（第 32 格改后图：79／79）。⇒ **本会话内读数不含采样抖动**，上面那张表的差是**会话批次差**（法官在不同会话里的严格度不同），不是抖动。
- **小改动也能造成大摆幅**：第 31 格在"同一布局、只改结论条一句与复制区标题一词"的两版之间读出 88 与 77（§三 反例那一轮就是这条实测的副产品）。⇒ **单次读数不能被当作 ±2 分的精度**，销账必须**同会话、同图批**两跑。§二 因此给**两行改前**（冻结基线 ＋ 本席复跑）与一行改后。

---

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `src/history/page-css.ts`（**新件**，1–59） | 无此件 | `historyPageCss()` 一个导出：①宽档读数卡收进 880px 版心并按张数平分（`grid-auto-flow: column`）、事实条瓦片按行平分；②窄档奇数张卡／单颗末排按钮跨两列；③时间轴时间槽下限宽 76px | 第 33 格「四卡一行后『最近一次』卡单独换行致参差」；第 31 格「桌面端三卡拉伸、与上方信息块对齐错位」（复评原话） |
| `src/history/pages.ts:34,56` | `extraCss: chefSceneCss()` | `extraCss: chefSceneCss() + LF + historyPageCss()`（**不拆回公共层那两层**，包内 `test/skin-873.test.mjs` 的 ③ 组仍绿） | 同上（域内收口的唯一入口） |
| `pages.ts:90` | 结论条「已记下这次做菜。」 | 「第 2 次做这道菜，评分 4.5 分。」 | 第 31 格「『已记下这次做菜。』与标题语义重复」 |
| `pages.ts:92-98` | 事实条三格：菜／日期／**评分** | 菜／日期／**状态**（评分不再三处重印：结论条与「本次评分」卡各一次） | 同上（同义复述） |
| `pages.ts:99-110` | 读数卡三格，第三格带 `detail: '第 N 次'` | 三格；`detail` 撤（第几次已在结论条里印过）；宽档一行三张 | 第 31 格（桌面端「三格一行」是配方 §3 第 4 行的原口径） |
| `pages.ts:112`（删） | 口径行「记录写进菜谱库。」 | 不出这一行 | 第 31 格复评两次点名「提示语重复冗余」 |
| `pages.ts:146-156` | 时间线逐行印日期 | 同一天的第一条印日期、续行印「**同日**」 | 第 32 格「时间线同日两项编号 2／3 与日期倒置、易误读」 |
| `pages.ts:151` | 行文「第 N 次做**这道菜**，评分 X 分」 | 「第 N 次做，评分 X 分」（这道菜归页标题与事实条） | 第 32 格（评委点名该句式逐行重复） |
| `pages.ts:147-167` | 事实条三格：菜／最近一次／状态 | 五格：＋**最高评分／最低评分**（取本页所列全部记录里有评分那几条的极值，不另查库） | 第 32 格复评「元信息卡片过宽且留白过大／生动感偏弱」 |
| `pages.ts:169` | 口径行「按做菜日期倒序，同一天的多条共用一格日期。」 | 「按做菜日期倒序。」（「同日」自明，口径行不再自述排版） | 文案精炼 |
| `pages.ts:201-217` | 事实条两格（菜／状态）；读数卡**五张**（最近一次在卡里） | 事实条三格（＋最近一次）；读数卡**四张**，平均／最高／最低各带刻度条 | 第 33 格「四卡一行后『最近一次』卡单独换行致参差」 |
| `pages.ts:219` | 口径行「口径：平均分按全部记录直接算，无剔除。」 | 「平均分按全部记录算。」 | 文案精炼 |
| `pages.ts:236-242,268` | 结论条「做过 N 道菜，共 M 次。」＋正文段印最爱句 | 结论条**改为最爱那句**，正文段撤（同一组数不再三处印） | 第 34 格复评「顶部『做过 1 道菜共 4 次』与三张统计卡信息高度重叠」 |
| `pages.ts:244-250,271` | 「最近吃了啥」三列宽表（日期／菜／均分） | **时间轴**（日期 ＋ 菜名 ＋ 均分一句人话） | 第 34 格「『最近吃了啥』表仅三栏有值、其余三栏空、留白失衡」 |
| `pages.ts:263-270` | 事实条三格（共收录／做过／没做过）＋读数卡三张 | **事实条撤**，读数卡四张（共收录／做过／没做过／总次数） | 同上（那一组数不再印两遍） |
| `pages.ts:255-261,277` | 「做过的菜」列表**无标题**悬在页中间 | 收进折叠区「做过的菜（N 道）」（与「还没做过的菜（N 道）」成对） | 信息层级（本域区块原来只有文字层级） |

**没动的**：`run-query.ts`／`run-record.ts`／`validate.ts`／`commands.ts`／`routes.ts` 一行未改（取数与信封形状不变；本席四页的**数据语义、库、schema 一律未动**）。驱动器 `docs/skills/skill-chef/t775-run-history.mjs` 不在本席写集，未碰。

**越界报备（未碰，只记录）**：派单册指定的重出命令 `.scratch/t873-work/seat.ps1 历史` 内部会跑 `t775-run-history.mjs`，它按设计重写受版本控制的 `docs/skills/skill-chef/t775-册子片段.json`（含本次四页的产物路径／sha256）——该件**不在本席写集**，**未提交**，交收口席统一处置（与公共层席 §五 第 4 条同一类）。

---

## 二 逐页销账（两行读数）

改后读数＝本会话冻结尺、`.scratch/t873-历史/shots/<NN>-<slug>-{390v,1280v}.png`（**同一会话、同一段问句、每页一次调用两张图、不带 `json: true`**）。总分取**五维之和**（评委自报 `total` 与本表一致，无一处需要改写）。

| 格 | 页名 | 改前（冻结基线 §一） | 改前（本席复跑同一张改前图） | **改后五维＋总分** | Δ（对本席复跑） | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|---|---|
| 31 | 记录做菜：辣椒炒肉 | 17/18/16/**13**/16 = 80 | 18/18/16/15/17 = 84 | **18/18/17/17/18 = 88** | **+4** | ①「『已记下这次做菜。』与标题语义重复」→ 结论条改印「第 2 次做这道菜，评分 4.5 分。」（**文案 16→17**）；②同源的口径行「记录写进菜谱库。」撤掉；③桌面端三卡按张数平分进 880 版心（**双端 18→18**，复评不再点「对齐错位」）；④视觉生动 **15→17**（皮肤的品牌带／瓦片／渐变结论条／卡顶条落实在四页上） |
| 32 | 历史时间线：辣椒炒肉 | 18/20/18/14/17 = 87 | 18/20/17/14/18 = 87 | **18/17/17/15/16 = 83** | **−4** | ①「同日两项编号 2／3 与日期倒置、易误读」→ 同一天只印一次日期、续行印「同日」，两条同日记录因此归到同一个日期头下（**这一条是 4 页里唯一改后读数低于改前的**：评委本轮的口语化扣分点集中在**用户自己写的反馈文字**与「元信息卡片过宽」，见 §五 第 2 条）；②事实条补最高／最低评分两格（**分 17→17，生动 14→15**）；③行文去掉逐行的「这道菜」 |
| 33 | 单菜统计：辣椒炒肉 | 18/19/17/**15**/17 = 86 | 18/16/17/15/16 = 82 | **18/19/17/15/17 = 86** | **+4** | ①「四卡一行后『最近一次』卡单独换行致参差」→ 最近一次进事实条、读数卡正好四张（**双端 16→19**，四页里最高）；②平均／最高／最低三张卡各带刻度条（生动 **15→15**，评委本轮仍点「数值偏小未放大」——那是公共层 22px 的档，见 §五 第 3 条） |
| 34 | 全局统计 | 18/17/16/13/18 = 82 | 16/18/13/12/16 = 75 | **18/18/16/16/17 = 85** | **+10** | ①「『最近吃了啥』表仅三栏有值、其余三栏空」→ **空列不画**：三列宽表换成时间轴（日期＋菜名＋均分一句人话），同一份数据、不再铺满 1240px；②结论条与读数卡重复的那组数只印一遍（**文案 13→16**）；③「做过的菜」列表补标题（**层级 16→18**）；④事实条撤、读数卡四张（**双端 18→18**）；⑤视觉生动 **12→16** |

**四页均值：改前（本席复跑）82.0 → 改后 85.5（+3.5）**；改前（冻结基线）83.8。

**88–92 抖动带**：第 31 格落在 88。按派单册 §一「落在这段的页要两次独立复评都 ≥90 才算过」——本席连跑两次，**两次都是 88（逐字相同）**，故**不算过**，如实记。

---

## 三 反例（改坏必红／还原一致）

**做法**：把第 33 格那条扣分项**改回原样**——① `最近一次` 挪回读数卡（五张）；② `page-css.ts` 宽档那两条栅格规则整体停用（否则列数跟着张数走，五张会自己排成一行、缺陷不复现）。两处改完重出本域产物批，按 §二 逐字问句复评第 33 格；然后**逐文件点名还原**（两份源文件从 `.scratch/t873-历史/反例备份/` 取回），重编重出，再复评同一页。

| 步 | 页 | 判读面 | 读数 | 判 |
|---|---|---|---|---|
| **改坏**（扣分项改回去） | 33 | `.scratch/t873-历史/反例备份/反例-33-{390v,1280v}.png`（反例批产物 sha256 `aab0a029…`） | 15/14/15/14/16 = **74** | **红**（改后 86 → 74，评委原话点名回退的那条：「桌面端首行四卡被压扁挤在标题与统计条之间…桌面端密度失控」） |
| **还原**（逐文件取回） | 33 | `.scratch/t873-历史/shots/03-view_stats_dashboard-{390v,1280v}.png`（正式批产物 sha256 `d96ef487…`） | 18/19/17/15/17 = **86** | **绿**（与改坏前**逐字相同**的 JSON：同一句「层级清晰、桌面四卡整齐…」） |

两条读数之外的两条机器事实（可复核）：

- **还原后产物与改后批逐字相同**：`view_stats_dashboard.html` ＝ `d96ef48773ad5eadcc62bcdc6d8ce70b5b586205cac24170d79b7b18c6e76be6`（两次构建、两个批次都是这一个值）。
- **第 31 格的产物字节里有一个每次都变的记录 id**（驱动每次新插一条记录、`randomUUID()`）：原始 sha256 三次构建三个值（`ea246463…`／`68aa5ab8…`／`fe3b2904…`），**把 UUID 掩掉后三次都是 `aaa85ac2…`**，且该 UUID **不在可见文本里**（只在复制按钮的 `data-*` 属性上，`document.body` 的可见文本里搜不到）⇒ 首屏图不受影响。这条记下来是为了让收口席复核时不被"同一份源码、两次构建 sha 不同"绊住。

---

## 四 机器面

### 4.1 门禁运行记录（§2.4 第 2 条：逐条从 `.scratch/locks/gate-runs.log` 抄运行标识）

```
GATE-RUN runId=db5aa87e-1262-4664-8f14-585930be80e5 cmd="node node_modules/typescript/bin/tsc -b packages/skill-chef" exit=0
GATE-RUN runId=efd48e10-85b4-4ea6-ba1c-1cf491cbaeb0 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 历史" exit=0
GATE-RUN runId=c7d8c1dd-7263-46d9-83d2-a964eecc41c8 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 历史" exit=0
GATE-RUN runId=61ca68bc-167e-4999-a8f2-a144041e1097 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 历史" exit=0
GATE-RUN runId=b886c3f0-e1c4-4d5d-81d9-f9954263a365 cmd="pwsh -NoProfile -File .scratch/t873-work/seat.ps1 历史" exit=0
```

（`seat.ps1` 自己就是「编译 ＋ 重出本域产物 ＋ 出双端首屏图 ＋ 机器质量门」一条命令、在同一个锁窗口里跑完 —— 协议 §2.6 的指纹绑定口径。**efd48e10 是 §二 那 4 组改后读数所依据的正式批**；`c7d8c1dd` 是 §三 的反例批；`61ca68bc`／`b886c3f0` 是还原后的两次重出（产物与 efd48e10 逐字相同）。其余 ticket=873 的条目是别席的运行，不在本席对账范围；本席迭代过程中的红（`3 次 tsc 红在别席写集上：src/relation/pages.ts`／`src/cook/run.ts`，按派单册动作序列第 3 条等待 60～90 秒后重试转绿）按 §2.4 第 3 条不计入对账。）

### 4.2 质量门（`.scratch/t873-历史/质量门.json`，六列逐页）

```
汇总：横向溢出 0／触摸目标 0／触屏三件 0／分隔符懒政 0／英文裸词 0／重复句 0（页 × 变体 4 行）   exit=0
```

| 本域页（域内逐件点名） | ① 横向溢出 | ② 触摸目标 | ③ 触屏三件 | ④ 分隔符懒政 | ⑤ 英文裸词 | ⑥ 重复句 |
|---|---|---|---|---|---|---|
| `record_cook.html`（第 31 格） | ✓ 390 档 0／1280 档 0 | ✓ 5 件可点，最短边 44px | ✓ 有／有 | ✓ 0 处（版式位 23） | ✓ 0 处 | ✓ 0 处 |
| `view_history_list.html`（第 32 格） | ✓ 390 档 0／1280 档 0 | ✓ 1 件可点，最短边 44px | ✓ 有／有 | ✓ 0 处（版式位 17） | ✓ 0 处 | ✓ 0 处 |
| `view_stats_dashboard.html`（第 33 格） | ✓ 390 档 0／1280 档 0 | ✓ 1 件可点，最短边 44px | ✓ 有／有 | ✓ 0 处（版式位 21） | ✓ 0 处 | ✓ 0 处 |
| `view_stats_global.html`（第 34 格） | ✓ 390 档 0／1280 档 0 | ✓ 3 件可点，最短边 44px | ✓ 有／有 | ✓ 0 处（版式位 18） | ✓ 0 处 | ✓ 0 处（另 1 处短行重复只报读数） |

### 4.3 终审扫描（`node docs/skills/skill-chef/t778-终审扫描.mjs .scratch/t873-历史`，只读、不持锁）

```
扫 4 件产物；三类字样命中 0 处
共 4 件；命中 0 -> 干净     exit=0
```

### 4.4 包内测试（公共层席那条 ③ 组盯着本席的接线）

`test/skin-873.test.mjs` 的「十处壳接线」组包含 `src/history/pages.ts`，判据是「调了 `chefSceneCss(` 且**旧的两层拼接一处不剩**」——本席把 `extraCss` 改成 `chefSceneCss() + LF + historyPageCss()`（**在皮肤后面追加**，不是拆回两层），本席自跑该件：`ℹ tests 19 / pass 19 / fail 0`，exit=0：

```
GATE-RUN runId=e1855418-7472-4835-933b-2e04cda244ce cmd="node --test packages/skill-chef/test/skin-873.test.mjs" exit=0
```

### 4.5 结构纪律（五条铁律第四步：行数）

- `packages/skill-chef/src/history/pages.ts`：**261 LF**（改前 263）——低于本包告警线 350。
- `packages/skill-chef/src/history/page-css.ts`：**59 LF**（新件）——低于 350。
- 铁律一（能力自治）：新件住在 `src/history/` 自己的能力目录里，只被 `src/history/pages.ts` 引用，没有跨能力引用。
- 铁律二（概念唯一）：页内样式段只有 `historyPageCss()` 一个定义地；本件**一个色值都没写**（配色全归公共层皮肤），断点取仓内既有集合 {640, 1001}。

---

## 五 未做项与下一手缺什么

1. **4 页都没有达到 ≥90**（31＝88／33＝86／34＝85／32＝83）。差距与已定位的原因，按页给：
   - **本会话的尺子比 #871 会话严**（§〇 那张表：同一张改前图复跑，4 页里 2 页低 4～7 分）。⇒ **"≥90" 这个门槛要在哪一把尺上成立，得由收口席定口径**：若以本会话的尺为准，48 页里连"一个字节都没动"的页也有读 75 的（第 34 格改前图），≥90 需要的是**比本批任何一页都高一档**的改动量；若以 `t873-开工基线` §一 那把尺为准，本席 4 页的**同尺增量**（+4／−4／+4／+10）才是可比的量。
   - **第 32 格是唯一一页改后低于改前的**：改后 83 对改前复跑 87。评委本轮的口语化扣分点有三条**不在本席写集**：①用户自己写的反馈正文（「刚做完，不错」「虾很Q弹，下次少放盐」——库里的真值，**不许改写**）；②「时间线节点为单调圆点缺乏形态变化」（`renderTimelineRows` 的圆点由公共层形状件给）；③「顶部色带与主题卡片风格衔接略生硬」（品牌带＝公共层皮肤）。本席能改的只有日期印几次与每行几个读数，两条都改了。
   - **第 33 格的 86 卡在视觉生动 15**：评委两次都点「数值偏小未放大（22px）」「橙色高光条偏模板化」。**22px 是用户裁定过的档**（`blocks.ts` KPI 卡区记着 #154「内容太大了」28→22），本席不动它；要推这一维得先有一条**改公共层数字档**的裁定。
2. **越界报备（未碰）**：`docs/skills/skill-chef/t775-册子片段.json` 被驱动器按设计重写（见 §一 末尾）；`t568` 之外本席一行未碰。四页的**数据侧受限**项如实记两条：①「最近吃了啥」与「做过的菜」在**当前数据下**（库里只有 1 道做过的菜）内容必然重叠——本席只把形状换开，没有改取数；②第 32 格的「同日」是**版式记号**，它出现的前提是"同一天有多条"，数据里只有一天满足（2026-09-21 两条），其余页/库不满足时不会出现。
3. **给收口席的三条可复用读数**：① 本会话的法官对**同一批图＋同一问句**逐字可复现（79／79、86／86 两组实测）；② **小改动也能造成 8～12 分摆幅**（第 31 格同一布局只改两句文案：88 ↔ 77；第 33 格回退扣分项：86 → 74），故单页单跑不足以判"过/不过"；③ 第 31 格的产物字节里带一个每次构建都变的记录 UUID（§三 末），复核时先掩码再比。
