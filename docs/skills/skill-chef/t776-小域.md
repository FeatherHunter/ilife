# t776 小域合并证据：派生 3 ＋ 采购 1 ＋ 开始使用 1 ＋ 数据管理 3（8 卡）

对应 GitHub issue #776（`wayfinder:task`，父图 #765 票 11）。上游 9 条阻塞（#766／#767／#768／#769／#839／#840／#818／#819／#860）开工前已全关，原生 `blocked_by` 开放阻塞为 0，前沿可做。

## 1 八行读数（验收正例逐卡一行）

跑法（沙箱器械起手刷新 pristine 副本，真库主文件全程未动）：

```text
node tooling/run-locked.mjs --ticket 776 -- node docs/skills/skill-chef/t776-run-small-domains.mjs
```

| 卡 id | 唤醒词 | 命令 | 产物（绝对路径） | 字节 |
|---|---|---|---|---|
| add_relation | 添加派生关系 | chef.relation.write op=add | D:\ilife\.scratch\t776\add_relation.html | 88387 |
| derive_from_existing | 从已有派生新菜 | chef.relation.write op=derive | D:\ilife\.scratch\t776\derive_from_existing.html | 88832 |
| view_relation_tree | 查看派生关系 | chef.relation.query | D:\ilife\.scratch\t776\view_relation_tree.html | 88429 |
| shopping_generate | 生成清单 | chef.shopping.query | D:\ilife\.scratch\t776\shopping_generate.html | 94520 |
| first_use | 首次使用 | chef.setup.init | D:\ilife\.scratch\t776\first_use.html | 89246 |
| data_quality_report | 体检 | chef.history.query kind=quality | D:\ilife\.scratch\t776\data_quality_report.html | 93448 |
| data_batch_edit | 批量改 | chef.data.batch | D:\ilife\.scratch\t776\data_batch_edit.html | 88617 |
| data_export_backup | 备份 | chef.history.query kind=backup | D:\ilife\.scratch\t776\data_export_backup.html | 89211（备份文件另 24916 字节，见下） |

备份真出文件：`D:\ilife\.scratch\t776\backup.json`（24916 字节，17 表全量，默认不含已废弃）。

反例（必跑，同脚本末段）：先记 `派生备菜→辣椒炒肉` 反向关系成环，再查 `辣椒炒肉` 家族树 → `exit=4` 报 `派生关系成环：辣椒炒肉到派生备菜到辣椒炒肉，已拒绝出图`（不画自环图，符合预期）。

判据（片段与视觉）：

```text
node tooling/run-locked.mjs --ticket 776 -- node docs/skills/skill-chef/t776-run-small-domains.mjs --check
→ 片段行数＝8（本票卡数＝8）＋ vision 审查缺陷 0，全绿 exit 0
```

册子片段：`docs/skills/skill-chef/t776-册子片段.json`（8 行：卡 id／唤醒词／命令／参数／产物绝对路径／exit／bytes／sha256，供收口票合并，不直接改共用册子）。

## 2 体检口径裁定（本票内第一件事）

老件（`scripts/data_quality_report.py:114-124`）是数据完整度：食材≥3／步骤≥3／贴士≥1／技法≥1／有背景，五项各 20 分满分 100，最差置顶先补。

新件曾把 `chef.history.query kind=quality` 做成口碑（平均分／低分计数／口碑稳定），与老件语义不同。按票面不许动的东西（体检语义不许把完整度偷换成口碑）裁定：

- 体检主口径＝完整度（老件公式逐字，见 `src/data/run-query.ts`），页为结果型（结论点最差＋事实＋读数卡＋逐菜折叠）。
- 口碑不进体检页：若最终两个都要，明确拆成两张卡另立票（遗留出口，不在本票做）。
- 当刻读数（沙箱跑后三菜）：辣椒炒肉 60 分（11 味／6 步／0 贴士／0 技法／有背景），派生备菜与派生新菜更低（空菜与继承菜），排序最差置顶。

## 3 跨技能调用契约说明（采购核对库存）

`#682` A6-21 明令技能包之间零依赖：采购核对库存不许 import 居家管家的代码（本票 `grep` 实测零引用）。

走调用契约：`chef.shopping.query` 只出合并清单（多菜按名加单位合并＋份数换算＋排除可选），库存核对以 `NOTE` 降级提示 `库存核对请复制 prompt 走「居家管家」技能，不直调` 交回，复制区给可直接粘走的清单文本；`stock_check` 显式给 `不核对` 时免提示。拿不到库存数据就显式降级，不静默当有。

## 4 页面形状（照 #768 配方，不自选）

- 添加／派生回执走回执型（结论条＋事实条＋变更行＋动作行＋复制区）。
- 家族树走结果型（事实条＋祖先／当前／后代三节折叠；关系表 0 行按空态：结论＋事实＋空态块＋口径行，本票跑后有数故产物为有数页，空态由证据 §7 单测覆盖）。
- 清单／体检走结果型（结论只放判定＋事实＋读数卡＋分组折叠，结论不复读数字）。
- 首次使用走过程型目标形态（事实＋分节折叠＋动作行＋复制区）。
- 批量改走过程型目标形态（事实＋变更行＋动作行＋复制区，改前对比在变更行）。
- 备份走回执型（结论＋事实＋读数卡＋动作行＋复制区）。
- 正文段落一律走 #860 件（`renderProseBlock`），无页内补丁；每一格都是一次公共层区块调用，零自写样式。

## 5 代码层 UI 审查清单（逐条结论，读数出自质量门代码层节）

| 审查项 | 判据 | 当刻读数（8 页一致） | 结论 |
|---|---|---|---|
| 硬编码样式 | 产物样式块里活色值与字号档是否出自公共层 | 活色值 29（#1d1d1f 等 6 个采样全在共享表内）、字号档 20（全在共享表，含历史档位） | 29 与 20 全部来自公共层共享样式表，本页补丁 0 条 |
| 内联样式 | 产物里 `style="…"` 处数 | 0 处（读数卡进度条宽度是数据，由公共层读数卡产出，不在本页） | 页面自己 0 处内联样式 |
| 字号与间距标尺 | 字号档是否全在公共层档位里 | 20 档全在共享表 | 走标尺，无自造字号 |
| 组件复用 | 产物类名是否全落 `ilife-` 前缀 | 非 `ilife-` 类名 1 个：`wrap`（公共层文档壳与页同元素，非本页自造）；老件类名 0 个 | 复用，无自造组件类 |
| 重复样式块 | 产物样式块数与高频重复声明 | 产物样式块 1（共享表＋区块表＋页壳拼成同一块）、原型壳样式块 0；高频重复 59 条是公共层内部复用 | 无重复样式块 |

## 6 过程性 vision 审查（缺陷当场改，不拖到收口票）

双端截图 16 张（8 页×390／1280）：`.scratch/t776/shots/`。模型审查 8 页×390 全量两批＋修复后 4 页复查（1280 以门禁双端 0 溢出作证）。

当场改的 4 处真问题：派生回执变更行改菜名对菜名（差异描述只留正文）；采购结论改判定句且第三卡改分组数；体检第三卡改待补道数（原最低分卡维度混杂）；备份操作行去复制且口径行去另立票（`定时与增量以后再做`，`默认不含已废弃`）。

驳回的误报（模型是提示器不是证据，逐条核过产物原文）：按钮高低（公共层主次既定形制）、标题重复（标题说是什么页、结论说判定、事实说细节，三处各管一段）、错字楼库存／舂菜／方框缺字／贪己废弃／翻看（均为模型 OCR 误读，产物逐字为核库存／备菜／口径口碑／含已废弃／看）。

终态：`vision.json` 缺陷 0（4 处逐条已改列名），`--check` 认它。

## 7 空态与 0 行三表

- `recipe_relations` 跑前 0 行：空态页（结论＋事实＋空态块＋口径＋动作）经单测覆盖（`relationTreePage` 空输入出空态，门禁六列全绿），跑后有数页为卡产物。
- `tips`／`step_techniques` 跑前 0 行：老件两节本来是空的，本票不必优先做；体检页如实计 0（`0条`／`0个`），将来有数据时按同一配方加一节。

## 8 不许动的东西（逐条遵守）

- 老件（`D:\2Study\StudyNotes\SKILLS\私家大厨`）一行不动（只读对照）。
- 其余 6 个域（查看／搜索筛选／做菜／录入／修改／历史）不碰：本票写面只含派生／采购／开始使用／数据管理四域目录＋入口分派＋键表生成物＋渲染形状表＋帮助描述＋测试键数（共享工作区别席在途改动与本票无关，提交时只 `git add` 本票声明路径）。
- 技能包之间零依赖（采购库存走调用契约，零 import）。
- 不改老库 schema（派生／批量缺值按 #818 甲拦下补齐）；不碰真库（全程票沙箱，沙箱器械每次复制前后比对真库主文件一致）。
- 体检语义未偷换（完整度主口径，口碑另立票）。

## 9 遗留出口

- 体检若最终两个都要（完整度＋口碑）→ 拆成两张卡另立票（口碑页不在本票）。
- 备份的定时／增量 → 另立票（本票全量 JSON 一次导出＋恢复口径行）。

## 10 重跑

```text
node tooling/run-locked.mjs --ticket 776 -- node docs/skills/skill-chef/t776-run-small-domains.mjs
node tooling/run-locked.mjs --ticket 776 -- node docs/skills/skill-chef/t768-质量门.mjs .scratch/t776 --widths 390,1280
node tooling/run-locked.mjs --ticket 776 -- node docs/skills/skill-chef/t776-run-small-domains.mjs --check
node tooling/run-locked.mjs --ticket 776 -- npm --prefix packages/skill-chef test
```

## 进度：100%

下一步：待收口票合并册子片段（本票产物已登记 8 行，收口 A 即合）。
