# t772 做菜域：端到端搬迁证据（5 卡，过程型为主）

票：[#772](https://github.com/FeatherHunter/ilife/issues/772)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 7）。
载体菜：辣椒炒肉（真库唯一菜谱：2 人份／6 步／11 味／`step_ingredients` 12 行／`cookware` 2 行／历史 1 条评分 4）。

## 1 五卡读数（一卡一行）

| 卡 id | 唤醒词／命令／参数 | 产物 | bytes | 要点 |
|---|---|---|---|---|
| `cooking_start_fresh` 全新开始 | 做菜模式／`chef.cooking.run`／`{"name":"辣椒炒肉"}` | `.scratch/t772/做菜-全新开始.html` | 99172 | 当前第 1 步展开，其余折叠 |
| `cooking_start_with_history` 含上次经验 | 做菜模式／`chef.cooking.run`／`{"name":"辣椒炒肉"}` | `.scratch/t772/做菜-含上次经验.html` | 99821 | 结论条下加「上次经验」时间轴（2026-07-21／评分 4／反馈原文） |
| `cooking_start_double_servings` 双份份量 | 做菜模式／`chef.cooking.run`／`{"name":"辣椒炒肉","servings":4}` | `.scratch/t772/做菜-双份份量.html` | 99209 | 倍率 2，全部用量 ×2（如五花肉 500 g），结论条只说相对原谱倍率 |
| `cooking_resume_after_pause` 断点续做 | 做菜模式／`chef.cooking.run`／`{"name":"辣椒炒肉","progress":3}` | `.scratch/t772/做菜-断点续做.html` | 99365 | 已做 3 步（标题缀已做），当前第 4 步展开，进度 67% |
| `cooking_during_waiting_step` 等待并行 | 做菜模式／`chef.cooking.run`／`{"name":"辣椒炒肉"}` | `.scratch/t772/做菜-等待并行.html` | 100172 | 「等待时可并行」块：本谱无炖烤腌等待型，口径如实写，按最长两步给建议 |

每步内联用料（真行数）：第 1 步 1 味（五花肉）／第 2 步 1 味（螺丝椒）／第 3 步 2 味／第 4 步 2 味／第 5 步 1 味／第 6 步 5 味，共 12 行（改前是全量 11 味挂每一步，共 66 格）。

## 2 过程族复用情况（配方 §1 十二格逐格）

| 格 | 放什么 | 区块 | 落点 |
|---|---|---|---|
| 1 | 眉标「私家大厨 ｜ 做菜」 | `renderPageShell`（并列分隔出细竖线，字符不进产物） | 五页一致 |
| 2 | 页标题「做菜模式：辣椒炒肉」 | `renderPageShell` 的 h1（文档标题缀技能名，与页标题错开，避重复句） | 五页一致 |
| 3 | 事实条：份量／步骤数／预计／状态 | `renderFactStrip` | 五页一致 |
| 4 | 结论条：份量放大说明 | `renderConclusionBar`（改写：默认只说无需换算，放大只说相对原谱倍率，不复读事实条） | 五页一致 |
| 5 | 进度读数卡（第 N 步／共 6 步＋条） | `renderKpiCard`（`bar`） | 当前步随卡变 |
| 6 | 当前步卡：动作正文＋火候三格＋本步用料＋验收旁注 | `renderDisclosure`（展开）＋`renderProseBlock`（#860 件）＋`renderFactStrip`＋`renderChipRow`＋`renderCaliberLine` | 每步本步用料 |
| 7 | 上一步／下一步 | `renderActionBar`（`data-action-id`，宿主驱动） | 五页一致 |
| 8 | 备料清单（12 行＝1 表头＋11 味，三列） | `renderDisclosure` 内 `renderDataTable`（分类退成徽章，窄屏三列口径） | 五页一致（双份按倍率） |
| 9 | 炊具「炒锅（锅）」「锅铲（其他）」 | `renderDisclosure`（题）内 `renderChipRow`（题外加折叠题便于指认，块调用仍逐格） | 五页一致 |
| 10 | 全部步骤速览 | 六张折叠步骤卡承载，不另起速览清单（另起即与卡内正文重复，重复句会红） | 五页一致 |
| 11 | 收尾动作（做完了，记一次／看这道菜的历史） | `renderActionBar` | 五页一致 |
| 12 | 复制区（备料清单与进度与评分合一） | `renderCopyBlock` | 五页一致 |

文档壳 `renderDocShell(pageUi:true)`，`extraCss ＝ pageUiCss()＋pageShapeCss()`，零自写样式，零 `.v768-prose` 补丁（正文走 `renderProseBlock`）。
归宿主的（页内只摆形状与状态）：暂停／声音／重做／计时器／份量步进器（静态 HTML＋`data-action-id`）。

## 3 断点续做口径与完结闭环衔接

- 断点续做：会话记忆由 AI 侧承担，页面只承「当前进度＋下一步」一段（`progress` 只决定哪一步展开与已做缀，不编跨会话字段）；跨会话持久化与计时器／提醒另立票（票面遗留出口）。
- 完结闭环：`historyHint` 已改人话（命令键不上屏：「做过 1 次，平均评分 4，做完后记一次」）；收尾动作「做完了，记一次」即调 `chef.history.record`（历史域）的指引，做完即记。

## 4 代码层 UI 审查清单（票 3 交，逐条结论，读数出自质量门「代码层读数」）

1. 硬编码样式：活色值 29 个、字号档 20 档——全部来自公共层共享样式表（`buildStyleSheet`＋`blocksCss`＋`pageUiCss`＋`pageShapeCss`），本页补丁 0 条、0 个色值。
2. 内联样式：1 处（读数卡进度条填充 `width:N%`，由公共层读数卡产出，条宽是数据不是样式），页面自己 0 处。
3. 字号与间距标尺：20 档全在公共层档位里，页面无自造字号（正文 15px 走 `renderProseBlock`）。
4. 组件复用：非 `ilife-` 产物类名 1 个（文档壳自带的 `wrap`，与 `ilife-page` 同一元素，由公共层产出）；老件类名 0 个。
5. 重复样式块：产物样式块 1（共享表＋区块表由 `renderDocShell` 拼成同一块），高频重复声明 59 条是公共层内部复用，不是复制。

## 5 机审六列与反例

- `node docs/skills/skill-chef/t768-质量门.mjs .scratch/t772` 六列全绿（5 页×零命中；短行重复 9 处只报读数，系火候／锅温／用量等数据巧合）。
- 反例两条皆红并点名：`--inject` 六列随质量门自带；本票 `--break 4`（删坏第 4 步关联）→ exit 1 并点名「第 4 步没有关联食材」。
- 双端截图 10 张（5 页×390／1280）同目录；vision 过程性审查见 `.scratch/t772/vision-审查.md`（改掉 1 条已复验，剩余缺陷 0）。
- `--check` 同时打印「片段行数＝5」与「vision 剩余缺陷 0」，缺一即红。

## 6 环境注记（与本票无关，留给收口）

- `render.test.mjs`「8 key×shape」一条红（12≠8）：别席落位中新键（`chef.data.batch`／`chef.relation.*`／`chef.setup.init`）所致，
  本票零新键；生成物收敛归收口票 12（地图写集表既定）。
- 全包 `tsc -b` 与 `cli.test.mjs` 6/6 在本票交付时全绿；`shopping/pages.ts` 曾有一处别席在途类型错，已由其所有者修掉（本票未碰该文件）。

## 7 重跑

```text
node tooling/run-locked.mjs --ticket 772 -- node docs/skills/skill-chef/t772-run-cooking.mjs
node tooling/run-locked.mjs --ticket 772 -- node docs/skills/skill-chef/t772-run-cooking.mjs --break 4
node tooling/run-locked.mjs --ticket 772 -- node docs/skills/skill-chef/t772-run-cooking.mjs --check
node tooling/run-locked.mjs --ticket 772 -- node docs/skills/skill-chef/t768-质量门.mjs .scratch/t772
```
