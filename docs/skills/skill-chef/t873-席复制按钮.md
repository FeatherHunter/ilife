# t873-席复制按钮 · 证据件（复制数据／复制日志对齐卡路里同款）

判定：**未完成（转裁定）**——18 处调用已全切三格式菜单、包内测试 22/22、全包 131/131；
48 页产物菜单 46 页齐＋委派 48/48 在位、终审扫描十域 0 命中；
但质量门十域::英文裸词列因公共层冻结菜单标签 `JSON`／`CSV` 全红（48 页、仅此两 token、其余五列全绿）。
本席无权放宽判据、 backlog无权改公共层与门限件：请编排者裁定（详见 §九）。

## 一、改了哪几处（白名单内，共 12 路径）

新建 2 件：

| 件 | LF | 说明 |
|---|---|---|
| `packages/skill-chef/src/render/copyArea.ts` | 150（告警线 350，未超） | 共用件：`chefCopyArea`（数据位恒三格式菜单）＋`chefCopyLog`（日志 2–6 段）。形状照抄卡路里 `copyArea.ts`，只装本包要的两样（`prompt`／`notice` 无调用方，不装） |
| `packages/skill-chef/test/t873-copy-area.test.mjs` | 194（测试件不计告警线） | 22 条：helper 菜单／日志／标题去重／空态 ＋ 八域真渲染 ＋ 十处源码 grep ＋ 委派在位 |

改 8 个 src（调用从 `dataText` 迁到经新 helper 的 `dataFormats`；标题与 `dataActionId` 一字不动）：

| 件 | 站点 | 信封 key／形 |
|---|---|---|
| `src/add/pages.ts` | 录入回执 | `chef.recipe.write`／`receipt`（摘要由菜名／份量／备料味数／步骤数派生）＋ 日志位 |
| `src/cook/run.ts` | 做菜模式 | `chef.cooking.run`／`list`（备料行／进度行／评分行逐行同义透传） |
| `src/data/pages.ts` | 批量改 | `chef.data.batch`／`receipt`（原文原样进 `message`） |
| `src/data/pages.ts` | 备份回执 | `chef.history.query`（kind=backup）／`list`（菜数／表数／大小成结构行） |
| `src/history/pages.ts` | 记录做菜 | `chef.history.record`／`receipt`（原文进 `message`）＋ 日志位 |
| `src/history/pages.ts` | 历史时间线 | `chef.history.query`／`list`（日期／第几次／评分／反馈成行，`total` 取域内总数） |
| `src/history/pages.ts` | 单菜统计 | `chef.history.query`／`list`（七项成一行，空值按三格式空值口径） |
| `src/history/pages.ts` | 全局统计 | `chef.history.query`／`list`（逐菜名／次数／均分，`total` 取行数） |
| `src/relation/pages.ts` | 记派生关系／派生新菜 | `chef.relation.write`／`receipt`（原文原样进 `message`，两处） |
| `src/setup/pages.ts` | 首次使用 | `chef.setup.init`／`receipt`（原文原样进 `message`） |
| `src/shopping/pages.ts` | 生成清单 | `chef.shopping.query`／`list`（食材／用量成行，多菜合并加 `来自` 列，`total` 取行数） |
| `src/view/page.ts` | 查看 8 卡 | `chef.recipe.view`／`detail`（菜名／食材数／步骤数） |

改 2 个驱动器（经编译产物取新 helper，不直调公共层）：

| 件 | 站点 | 信封 |
|---|---|---|
| `docs/skills/skill-chef/t771-run-search.mjs` | 搜索筛选 13 卡 | `chef.recipe.search`／`list`（菜名／难度／份量／总时长成行，`total` 取域内总数） |
| `docs/skills/skill-chef/t774-run-update.mjs` | 修改 4 卡 | `chef.recipe.write`／`receipt`（原文原样进 `message`，四处） |

未碰：`packages/base-render/**`（零改动）、`src/render/index.ts`（新件经相对路径深引用，与 `sceneShell.ts`先例同形）、其余 UI（一字不动）。

## 二、逐页销账（48 页＝资产 48 卡）

| 域 | 页数 | 菜单齐 | 委派在位 | 终审扫描 | 质量门 |
|---|---|---|---|---|---|
| 查看 | 8 | 8 | 8 | exit 0 | 仅英文裸词 `JSON,CSV`，其余五列绿 |
| 做菜 | 5 | 5 | 5 | exit 0 | 同上 |
| 搜索筛选 | 13 | 13 | 13 | exit 0 | 同上 |
| 修改 | 4 | 4 | 4 | exit 0 | 同上 |
| 历史 | 4 | 4 | 4 | exit 0 | 同上 |
| 录入 | 6 | 6 | 6 | exit 0 | 同上 |
| 采购 | 1 | 1 | 1 | exit 0 | 同上 |
| 派生 | 3 | 2（家族树页本无复制区，改前亦无） | 3 | exit 0 | 同上 |
| 开始使用 | 1 | 1 | 1 | exit 0 | 同上 |
| 数据管理 | 3 | 2（体检页本无复制区，改前亦无） | 3 | exit 0 | 同上 |
| 合计 | 48 | 46（另 2 页本无复制位） | 48 | 十域 0 命中 | 红列唯一成分见 §九 |

菜单标记：`ilife-copy-menu-wrap` ＋ `data-fmt-open="1"` ＋ `data-fmt="text|json|csv"` 三项 ＋
开合器 `aria-label="复制数据（点开选格式）"`；空串 hints 不出提示行（与卡路里同口径）。
委派在位判据：产物 `<script>` 内含 `data-fmt-open` 委派串（与 `buildSharedHelpersJs` 同源探针复核）。

## 三、复制日志审计（逐处）

- 录入回执（`add/pages.ts`）：手拼 4 行 → 迁六段。`command=chef.recipe.write`（可照抄重跑的前缀）、
  `source` 取输入来源句、`m5Line` 取菜谱 1 行＋食材 n 行＋步骤 m 行（计数由输入派生）；
  时间戳给不出如实缺省（`(未知)` 口径），见产物 `时间戳版本／(未知)`。
- 记录做菜（`history/pages.ts`）：手拼单行 → 迁六段。`command=chef.history.record`、
  `m5Line` 沿用原先那句过程证据、`actionAt` 取这次做菜日期；来源给不出如实缺省。
- 其余 16 处：改前即无日志位。六段全空只会产出五个 `(未知)`，无信息量，故不新增（保持无日志）。
- 录入失败页（`renderErrorReceipt`）：公共层错误回执无三格式位，动它须改 `base-render`，不在本票范围（见 §七）。

## 四、底部动作按钮验明（无需新建件）

- `src` 内零手写 `<button>`（grep 0 命中）；底部动作 11 处 `renderActionBar({ buttons: [...] })`
  全走公共层（`add` 1／`cook` 2／`data` 3／`history` 1／`relation` 1（布局 `div` 包裹，仍走同一入口）／
  `setup` 1／`shopping` 1／`view` 1），入参形 `{ label, kind, actionId }` 与卡路里同形态。
- 结论：无缺件。`packages/base-render/**` 缺省不碰成立，本次零改动。

## 五、反例两行（变异自证）

- 变异：`src/view/page.ts` 一处改回 `dataText` → `t873-copy-area.test.mjs` exit=1（断言红）。
- 还原：逐文件取回后复跑 → tests 22／pass 22／fail 0（与改前读数一致）。

## 六、机器面（门禁对账源＝`.scratch/locks/gate-runs.log`，声明边跑边补）

- GATE-RUN runId=786582f4-a833-4c64-8e29-ec774532bec2 cmd="node node_modules/typescript/bin/tsc -b packages/skill-chef" exit=0（编译）
- GATE-RUN runId=0ba9c633-36c3-443c-b1ef-d258de5e89b5 cmd="node --test packages/skill-chef/test/t873-copy-area.test.mjs" exit=0（新测试 22/22）
- GATE-RUN runId=711eb4b8-fe17-49be-840a-7e820e15e996 cmd="node --test packages/skill-chef/test/t873-copy-area.test.mjs" exit=1（变异红，非门禁引用）
- GATE-RUN runId=1efec9ca-ade7-4853-b940-53eba35c13cf cmd="node --test packages/skill-chef/test/t873-copy-area.test.mjs" exit=0（还原绿 22/22）
- GATE-RUN runId=724df599-88e9-412f-8d45-a23dd59681aa cmd="node --test packages/skill-chef/test/*.test.mjs" exit=0（全包 131/131）
- GATE-RUN runId=7f00ac4c-b0e0-47b4-9650-6c3b890553bb cmd="node docs/skills/skill-chef/t873-席跑.mjs 查看" exit=1（产物 8 件；红列见 §九）
- GATE-RUN runId=5b8a5250-f17a-4249-8fc9-787cd95e6558 cmd="node docs/skills/skill-chef/t873-席跑.mjs 做菜" exit=1（产物 5 件；同上）
- GATE-RUN runId=9912577f-948a-4ed6-b49e-ed33ae38b207 cmd="node docs/skills/skill-chef/t873-席跑.mjs 搜索筛选" exit=1（产物 13 件；同上）
- GATE-RUN runId=42fa8d9b-0064-49ce-8110-b26f55c434a8 cmd="node docs/skills/skill-chef/t873-席跑.mjs 修改" exit=1（产物 4 件；同上）
- GATE-RUN runId=809c1b39-7108-4636-ad8b-271a3eb524a9 cmd="node docs/skills/skill-chef/t873-席跑.mjs 历史" exit=1（产物 4 件；同上）
- GATE-RUN runId=209dc320-f922-4e8b-af48-5f1541ad4e47 cmd="node docs/skills/skill-chef/t873-席跑.mjs 录入" exit=1（产物 6 件；同上）
- GATE-RUN runId=129c17fa-3911-4145-a0e2-0656f92819a2 cmd="node docs/skills/skill-chef/t873-席跑.mjs 采购" exit=1（产物 1 件；同上）
- GATE-RUN runId=5d59884c-b0b5-4929-9f5e-b1b5d1361c26 cmd="node docs/skills/skill-chef/t873-席跑.mjs 派生" exit=1（产物 3 件；同上）
- GATE-RUN runId=5b3a5ef7-3c14-4a8a-9b3c-874227d672ec cmd="node docs/skills/skill-chef/t873-席跑.mjs 开始使用" exit=1（产物 1 件；同上）
- GATE-RUN runId=0c27607f-4266-4317-8f3c-6f5ad3372ec1 cmd="node docs/skills/skill-chef/t873-席跑.mjs 数据管理" exit=1（产物 3 件；同上）
- 十域 `t778-终审扫描.mjs` exit=0（runId：dd7cccdf／2d23dea7／f273cb22／7eb5d948／5541fa9e／1b63cfe9／54dfd946／470903f3／99d5eab2／3342b2b3，三类字样 0 命中）。
- 席跑 exit=1 的唯一原因是质量门英文裸词列（§九）；产物缺件 0、其余五列 0 红。

## 七、未做项

1. 复制日志：16 处无日志位的不新增（§三）；失败页错误回执的三格式位需公共层先加位，未动。
2. `stat` 形：共用件已支持，本次 18 处无纯数值口径可用（统计页含空值与日期），故未启用。
3. 底部按钮：验明无缺件，未新建公共层控件（§四）。
4. 质量门英文裸词列：见 §九，转裁定。在裁定前不放宽、不改门限件。
5. 驱动器跑后 `t770–t776-册子片段.json` 被连带重写（产物哈希随新菜单变化）：非白名单，未提交，留归属票处理。

## 八、交付对账（结构纪律第五步）

事前清单＝新建共用件＋8 个 src＋2 个驱动器＋包内测试＋证据件；事后逐行对上，偏差为零。
`packages/base-render/**` 未碰；`src/render/index.ts` 未碰（深引用与 `sceneShell.ts`先例同形）。
新文件对外 2 个名字（`chefCopyArea`／`chefCopyLog`，入参类型不导出），`copyArea.ts` 150 LF 未超线。

## 九、门禁冲突说明（请编排者裁定，不在本席改动）

- 事实：三格式菜单的可见标签 `纯文本／JSON／CSV` 取自公共层冻结表 `COPY_FORMAT_LABELS`
  （`controls.ts:1379` 经 `COPY_FORMATS` 逐项渲染）；其中 `JSON`／`CSV` 被 `t768-质量门.mjs`
  英文裸词列判红（白名单 `ASCII_OK` 只收单位与小写惯用缩写，见该件 `:56`）。
- 卡路里先例：同一组标签在卡路里侧记为已知项（`t524-场景09-视觉整改基准.md:179`：
  「页尾复制区三格式标签…公共层 #247 定案」，由判分器具单独记账，不进页面红）。
- 本席能做的已做完：标签一字未改（冻结面）、判据一档未放宽（禁令）、门限件一字未动（白名单外）。
- 请求裁定（三选一）：① 门限票把公共层三格式标签记为已知项（照卡路里口径）；② 本票改判「附条件完成」；
  ③ 其他。在裁定前本席停止，不擅自改 `packages/base-render/**` 与 `t768-质量门.mjs`。
