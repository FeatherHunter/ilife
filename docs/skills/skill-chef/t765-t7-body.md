## Question

做菜域 5 卡（全新开始(每步内联 ＋ 完结闭环)／含上次经验／双份份量／断点续做(AI 会话记忆)／等待步骤中并行做其他）是**过程型**的主场：老件有 `cooking_mode.html`（54,904 B）承载整条做菜流程，新仓只有一个 `chef.cooking.run` 的 `list` 信封。「断点续做」在老件里靠 AI 会话记忆、没有任何字段。这 5 卡的过程型页面怎么在新仓真跑出来？

## 目标

1. 5 卡逐卡走通并出**过程型** HTML（走③的过程族）：每步内联食材、完结闭环、份量放大说明、历史经验提示、等待型步骤的并行提示。
2. 「断点续做」定口径：会话记忆由 AI 侧承担，页面侧只承载「当前进度 ＋ 下一步」这一段（缺字段不得编造）。
3. 完结闭环与 `chef.history.record`（历史域）的衔接写明（做完即记）。
4. 逐卡一行读数。

另加三条本图口径（并发与收口，逐条可判）：

- **过程性 vision 审查**：本票产物须过一次 vision_router 视觉审查，把查出的不合理之处**当场改掉**（缺陷不许拖到收口票）。
- **册子片段**：本票产物登记成 `docs/skills/skill-chef/t772-册子片段.json`（字段：卡 id／唤醒词／命令／参数／产物绝对路径／exit／bytes／sha256），供收口 A 合并；**不许直接改共用册子**。
- **副本库沙箱**：数据一律走票 17 交的沙箱器械，复制到自己票号下的 `.scratch/t772/`；**不许与其他票共用同一份副本**。

## 验收命令


- 正例：`node tooling/run-locked.mjs --ticket 772 -- node docs/skills/skill-chef/t772-run-cooking.mjs` → 打印 5 行 `卡 → exit=0 → 产物绝对路径` 且 `缺卡 0`，exit 0
- 反例（必跑）：把某步的食材关联改坏 → 该卡必须 exit≠0 并点名（不许静默少显示一步）

- 判据（片段与视觉）：本票运行脚本带 `--check` 时必须同时打印「片段行数＝本票卡数」与「vision 审查缺陷 0（或逐条已改）」，两者缺一即红。
- 判据（代码层 UI 审查）：页面进墙前须逐条过票 3 交的**代码层 UI 审查清单**（硬编码样式／内联样式／字号与间距标尺／组件复用／重复样式块），逐条给出结论。

## 不许动的东西


- 老件一行不动；其余 9 个域不碰。
- `chef.cooking.run` 的信封形状（`list`）与既有字段不许破坏。
- 不许为了页面好看编造步骤／食材；缺就显式缺。

## 交付物路径

- 代码：`packages/skill-chef/src/cook/**`
- 证据：`docs/skills/skill-chef/t772-做菜域.md`（5 行读数 ＋ 过程族复用情况）

## 遗留出口

- 断点续做的跨会话进度持久化：本票只做「当前会话内」，跨会话另立票。
- 计时器／提醒类交互（等待步骤）：若要做另立票。

## 进度：0%

下一步：先跑现有 `chef.cooking.run` 拿基线，再照③的过程族改造页面。
