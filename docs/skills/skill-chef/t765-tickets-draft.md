<!-- TICKET 1 | task | [规格] skill-chef 形状票：历史域试点 ＋ 派生链 ＋ 配置键 html.sceneDir -->
## Question

`packages/skill-chef/src` 今天按「工种」分目录（`cli`／`fetch`／`policy`／`render`，只有 `help/` 站得住），10 个功能域在代码里没有落点；同一件事（命令的事实）全压在 `cmd_read.ts`（460 行，已超线）里。本图要新增四十多份产物与 7 张域票，**若不先定下按域的代码形状，产物会继续堆进一个文件**。用什么形状证明「按 HELP 一级分组」的形态走得通？

## 目标

1. 取**历史域**做最小试点：建 `src/history/{commands,routes,index}.ts` 三件套（能力自治 ＋ 能力门），把历史域的命令声明与路由声明搬进去。
2. 建**共用位派生链**：`scripts/gen-cli.mjs`（声明表 → `src/cli/keys.ts` 等生成物）＋ `--check` 比对门，并接进根 `package.json` 的 `gen:check`。
3. 新增配置键 `html.sceneDir`（默认 `cook_html`）；`html.dir` 的语义与默认值一动不动。
4. 交「必报五步」全额：影响清单、结构设计、超线报警、交付对账。

**本票只碰 `src/` 与配置面，不碰页面、不改任何命令的对外行为。**

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node packages/skill-chef/scripts/gen-cli.mjs --check` → exit 0
- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node node_modules/typescript/bin/tsc -b packages/skill-chef` → exit 0
- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node --test "packages/skill-chef/test/*.test.mjs"` → 全绿（含 HTML 快照 changed=0）
- 反例（必跑）：删掉 `src/history/commands.ts` 里一条声明 → `gen-cli.mjs --check` 必须 exit 1 并点名该条；改回即绿
- 反例（必跑）：把 `html.sceneDir` 默认值改坏 → 配置测试必须红

## 不许动的东西

- 老技能（`D:\2Study\StudyNotes\SKILLS\私家大厨`）一行不动（只读对照）。
- `html.dir` 的默认值与语义不许动——存量配置文件在盘上，改默认值会静默漂移（配置件无迁移机制）。
- 8 条命令的对外行为（argv 形态、envelope 字段、exit 码）不许变；本票是纯形状搬迁。
- 其余 9 个域**不许建目录**（只建 `history/` 一个试点域）。
- 其余五个技能包与 `base-render` 公共层：不碰。

## 交付物路径

- 代码：`packages/skill-chef/src/history/{commands,routes,index}.ts`、`packages/skill-chef/scripts/gen-cli.mjs`、生成物 `packages/skill-chef/src/cli/keys.ts`、`packages/skill-chef/src/config.ts`
- 文档：`docs/skills/skill-chef/t1-形状.md`（必报五步全额 ＋ 变异红／还原一致两行读数）

## 遗留出口

- 其余 9 个域的搬迁 → 资产票（②）与各域纵向票（⑤–⑪）。
- `cli/`／`fetch/`／`policy/`／`render/` 四个工种目录的最终去留：搬迁完成后另立小票，不在本票内。

## 进度：0%

下一步：先报必报五步第一步影响清单与第二步结构设计，等维护者点头再动源码（新建目录层级 ＋ 碰三个以上能力须先点头）。

<!-- TICKET 2 | task | [规格] 域与唤醒词资产单一源：老 scenes/*.yaml → 仓内 typed 资产 ＋ 50 词／48 卡对账表 -->
## Question

域与卡的划分依据在新仓（`sceneData.ts`，机器生成），场景与唤醒词的权威清单在老仓（`scenes/*.yaml`），两侧今天靠一份一次性生成器的注释互相对账；**50 条唤醒词 ↔ 48 张卡的映射、13 条新表多出词的归属、域目录的中文名与产物 slug 规则，都还没有仓内的单一事实源**。这三件事的唯一事实源该长什么样？

## 目标

1. 把老件 `scenes/*.yaml` 的 48 场景／33 组名抽成仓内 typed 资产（生成器 ＋ `--check` ＋ sha256 摘要锁），与既有 `WAKE_TABLE` 双向对账。
2. 交出**对账表**：50 条唤醒词 ↔ 48 张卡 ↔ 命令 ↔ 域，每条标明来源（老组名／新表多出词／HELP 词）、是否可路由、对应哪张卡。
3. 定下**域目录中文名**（10 个，取自 HELP 的域 label）与**卡产物的发布名规则**（slug），供纵向票与收口票**共用同一套**（禁各算一套）。
4. 裁定 13 条新表多出词的 prompt 示例口径（沿用同域卡的 prompt 还是新写），落表。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node packages/skill-chef/scripts/gen-chef-scenes.mjs --check` → exit 0
- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t2-对账.mjs` → 打印 `词 50／卡 48／差集 0／slug 冲突 0` 且 exit 0
- 反例（必跑）：从 `WAKE_TABLE` 删一条 → 对账脚本 exit 1 并点名；改回即绿
- 反例（必跑）：给两张卡同一个 slug → 对账脚本 exit 1 并点名

## 不许动的东西

- 老件一行不动。
- `sceneData.ts` 的 48 卡内容不许与老件漂移（sha256 锁）；改内容＝改生成器声明表再重跑。
- `WAKE_TABLE` 既有 37 条不改语义；若要补 13 条老组名，逐条在票内列出并说明影响。
- HELP 页契约（`base-paint/help-shell`）不动。

## 交付物路径

- 资产与生成器：`packages/skill-chef/src/triggers/chef-scenes.ts`（或等价的域／组／卡资产）、`packages/skill-chef/scripts/gen-chef-scenes.mjs`
- 对账与命名：`docs/skills/skill-chef/t2-对账表.md`、`docs/skills/skill-chef/t2-对账.mjs`、`docs/skills/skill-chef/t2-命名.md`（域中文名 ＋ 卡 slug 规则）
- 证据：变异红／还原一致两行读数

## 遗留出口

- 待开发卡的实现 → 各域纵向票。
- 老件 20 个模板 → 新页面族的信息架构对照表 → 横向票③。

## 进度：0%

下一步：先读老件 `scenes/*.yaml` 全量 ＋ `WAKE_TABLE`，出对账表草稿，再定命名。

<!-- TICKET 3 | prototype | [原型] 页面族：过程型／结果型／回执型三族 ＋ 双端自适应（先裁形状再铺开） -->
## Question

今天业务命令的「HTML」只是一段 `<section>`（`packages/skill-chef/src/render/html.ts:41-56`）套在 16 行壳里（`templates/*.html`）：没有页面族、没有双端自适应、没有信息架构。本图要的产物必须**双端自适应、文字无冗余、内容生动、分隔符处要出设计**，最终按五维打分 ≥90。这三族页面（过程型／结果型／回执型）该长什么样？

## 目标

1. 走公共层装配：内容＝`base-paint/blocks` 的区块、文档＝`renderDocShell`，不新造一套样式（与卡路里同路）。
2. 出**三页原型**（过程型／结果型／回执型各一），**双端真实渲染**：手机 390 ＋ 桌面 1280 都出图；手机端适配能力照 HELP HTML（触屏三件、触摸目标、无横向溢出）。
3. 交**老件信息架构对照表**：老件 20 个模板逐页列出分区与顺序，标明新族怎么承载（内容一条不丢、视觉不复刻）。
4. 落「`;`／`|`／`·` 分割＝该做 UI 设计」这条：给出三族里分割符的替代设计（分栏／标签／卡片）并在原型上体现。
5. 形状由**维护者裁过**再铺开。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t3-原型.mjs` → 出三页原型 ＋ 六张截图（390／1280），打印 `横向溢出 0／分隔符懒政 0／英文裸词 0` 且 exit 0
- 反例（必跑）：把原型页塞进一个超过视口的表格 → 脚本必须报横向溢出并 exit 1
- 人裁（关票条件）：维护者看过三页原型后回「过」或逐条列出要改的地方

## 不许动的东西

- `base-render` 的区块契约与 `docShell` 契约：只调用、不改（要改另立票）。
- 老件 20 个模板不作视觉基准（`#682` A3-1），只作内容对照。
- 不做像素复刻；不引外部字体／CDN（产物必须单文件自足，验收墙 §7）。

## 交付物路径

- 原型与截图：`.scratch/<批名>/`（三页 ＋ 六张图）
- 设计与对照：`docs/skills/skill-chef/t3-页面族设计.md`（三族结构 ＋ 老件对照表 ＋ 分割符设计 ＋ 判据读数）

## 遗留出口

- 三族的变体（确认页／进度页／失败页）→ 各域纵向票按需派生。
- 公共层若确需新区块 → 单独立票，不在本票内。

## 进度：0%

下一步：先读 `base-paint/blocks` 现有 12 区块与 HELP 模板的手机端适配做法，出三族骨架图报维护者。

<!-- TICKET 4 | research | [研究] 老库 schema 与写侧字段对账：48 卡逐卡列出要写的表与字段 -->
## Question

Q3 裁「写侧真落库」、Q4 裁「老 schema 为权威、本次不改 schema」。但 48 张卡里有多少写侧、每条写路径要写哪张表哪个字段、老库里有没有这个字段、没有的话在「不改 schema」约束下怎么落——今天没有一份对账。**哪些卡的写路径在老 schema 下走得通，哪些卡根本落不了？**

## 目标

1. 逐卡（48 行）列出：写侧／读侧、要写的表与字段、老库现状（有／无／类型不符）、结论（可落／需降级／必须立票）。
2. 读侧也列（每卡读数来自哪张表、有没有缺列）。
3. 对「落不了」的卡给出两条出路（降级方案／立 schema 票出图），并标出影响哪些纵向票。
4. 取数口径固定：以**老库文件结构 ＋ 老件源码**为准，不以老文档为准（老文档有 15 条与实现不一致）。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t4-schema-audit.mjs --check` → 打印 `卡 48／行齐 48／未知 0` 且 exit 0
- 反例（必跑）：从对账表删一行 → 脚本 exit 1 并点名缺哪张卡

## 不许动的东西

- 老库文件**只读**（不得连写）；老件源码一行不动。
- 不改 schema；不改任何命令。
- 取数一律走**副本库**（复制库文件到临时目录），不碰真库。

## 交付物路径

- 报告：`docs/skills/skill-chef/t4-写侧字段对账.md`（48 行表格 ＋ 结论）
- 校验脚本：`docs/skills/skill-chef/t4-schema-audit.mjs`

## 遗留出口

- 需要 schema 变更的卡 → 单独立票（出图范围）。
- 降级方案的取舍 → 由受影响域票在实现时裁。

## 进度：0%

下一步：先摸清老库的表结构（只读副本），再逐卡对照。

<!-- TICKET 5 | task | 查看域：端到端搬迁（8 卡／5 组，纯读） -->
## Question

查看域 5 组 8 卡（完整食谱／新手强调／替换食材预览／只看食材／食材分组(11 大类)／只看步骤／只看营养／只看背景）在新仓只落到**一个** `chef.recipe.view` ＋ 一份 `recipe_view.html` 壳；「只看营养」是显式占位（`render/views.ts:43` 写死 `estimated:false`），「新手强调」「替换食材预览」没有对应数据面。这 8 卡各自的结果型页面怎么在新仓真跑出来？

## 目标

8 卡逐卡走通并出结果型 HTML（双端自适应，走横向票③的页面族；分区照老件信息架构对照表；文字无冗余、内容生动）。逐卡自证：每卡一行读数。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t5-run-view.mjs` → 打印 8 行 `卡 → exit=0 → 产物绝对路径`，`缺卡 0` 且 exit 0
- 反例（必跑）：把某卡的数据源改坏 → 该行必须 exit≠0 并点名该卡（不许返空页冒充）

## 不许动的东西

- 老件一行不动；其余 9 个域不碰。
- `chef.recipe.view` 的对外行为不许破坏（既有测试全绿）。
- 不许编数字：营养未估算就显式标未估算。
- 路由不许动：`看菜`／`看菜谱` 归 view（`#43` F1 已定）。

## 交付物路径

- 代码：`packages/skill-chef/src/view/**`
- 产物：按册子（收口 A ⑫）落验收副本
- 证据：`docs/skills/skill-chef/t5-查看域.md`（8 行读数 ＋ 变异自证）

## 遗留出口

- 营养估算：本票只保证「未估算」显式呈现；真估算另立票。
- 老件 `templates/查看/` 的模板差异 → 交③对照表。

## 进度：0%

下一步：先跑一遍现有 `chef.recipe.view` 拿基线读数，再逐卡补数据面。

<!-- TICKET 6 | task | 搜索筛选域：端到端搬迁（13 卡；含 5 张待开发 ＋ 3 条路由错位） -->
## Question

搜索筛选域 13 卡（关键词搜索／错字模糊匹配／按菜系筛选／多维组合筛选／按食材筛选／排除食材／筛选难度／筛选时间／筛选炊具／筛选口味／筛选季节／筛选状态／查看全部）里：**5 张卡在 HELP 里标着【待开发】**（难度／时间／炊具／状态 4 条筛选 ＋ 相关组合），命令层其实已经能收 `difficulty`／`maxTime`／`cookware`／`status` 参数；另有 **3 条路由错位**——`筛选食材`／`筛选口味`／`筛选季节` 的 `filter` 在 `cmd_read.ts:176` 被统一映射成 `cuisine`。这 13 卡怎么逐卡走通？

## 目标

1. 修掉 3 条路由错位（`筛选食材`→ingredient、`筛选口味`→flavor、`筛选季节`→season），并保证 `筛选菜系` 仍走 cuisine。
2. 5 张待开发卡补齐到真能跑（难度／时间／炊具／状态 四类筛选 ＋ 组合），不再靠命令层「能收参数但页面不认」。
3. 13 卡逐卡出结果型 HTML（双端自适应，走③的页面族），每卡一行读数。
4. 模糊匹配（错字／谐音）给出明确口径（命中就提示纠错，不命中就如实说没找到）。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t6-run-search.mjs` → 打印 13 行 `卡 → exit=0 → 产物绝对路径` 且 `缺卡 0`、`路由错位 0`，exit 0
- 反例（必跑）：把 `筛选口味` 的映射改回 `cuisine` → 脚本必须报路由错位并 exit 1

## 不许动的东西

- 老件一行不动；其余 9 个域不碰。
- `chef.recipe.search` 既有 4 个预设（`查看全部` 等）的行为不许破坏。
- 不许返空冒充：搜不到就如实说没找到（既有 `CHEF_EMPTY_RESULT` 缺失阻断的语义保持）。
- 不改老库 schema。

## 交付物路径

- 代码：`packages/skill-chef/src/search/**`
- 证据：`docs/skills/skill-chef/t6-搜索筛选域.md`（13 行读数 ＋ 路由错位修复前后对照）

## 遗留出口

- 多维组合筛选的上限（老件写 ≤3 维）：本票按老件口径实现，若要放宽另立票。
- 模糊匹配若需字典／拼音表 → 另立票。

## 进度：0%

下一步：先复现 3 条路由错位（跑命令看回包），再动映射。

<!-- TICKET 7 | task | 做菜域：端到端搬迁（5 卡，过程型为主） -->
## Question

做菜域 5 卡（全新开始(每步内联 ＋ 完结闭环)／含上次经验／双份份量／断点续做(AI 会话记忆)／等待步骤中并行做其他）是**过程型**的主场：老件有 `cooking_mode.html`（54,904 B）承载整条做菜流程，新仓只有一个 `chef.cooking.run` 的 `list` 信封。「断点续做」在老件里靠 AI 会话记忆、没有任何字段。这 5 卡的过程型页面怎么在新仓真跑出来？

## 目标

1. 5 卡逐卡走通并出**过程型** HTML（走③的过程族）：每步内联食材、完结闭环、份量放大说明、历史经验提示、等待型步骤的并行提示。
2. 「断点续做」定口径：会话记忆由 AI 侧承担，页面侧只承载「当前进度 ＋ 下一步」这一段（缺字段不得编造）。
3. 完结闭环与 `chef.history.record`（历史域）的衔接写明（做完即记）。
4. 逐卡一行读数。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t7-run-cooking.mjs` → 打印 5 行 `卡 → exit=0 → 产物绝对路径` 且 `缺卡 0`，exit 0
- 反例（必跑）：把某步的食材关联改坏 → 该卡必须 exit≠0 并点名（不许静默少显示一步）

## 不许动的东西

- 老件一行不动；其余 9 个域不碰。
- `chef.cooking.run` 的信封形状（`list`）与既有字段不许破坏。
- 不许为了页面好看编造步骤／食材；缺就显式缺。

## 交付物路径

- 代码：`packages/skill-chef/src/cook/**`
- 证据：`docs/skills/skill-chef/t7-做菜域.md`（5 行读数 ＋ 过程族复用情况）

## 遗留出口

- 断点续做的跨会话进度持久化：本票只做「当前会话内」，跨会话另立票。
- 计时器／提醒类交互（等待步骤）：若要做另立票。

## 进度：0%

下一步：先跑现有 `chef.cooking.run` 拿基线，再照③的过程族改造页面。

<!-- TICKET 8 | task | 录入域：端到端搬迁（6 卡，写侧） -->
## Question

录入域 6 卡（图片录入／MD 文件录入／对话录入(逐步收集)／结构化模板录入(表单)／JSON 文件导入／导入校验失败(补齐后重试)）在新仓只有 `chef.recipe.write` 的 `op:add` 一条路径，四种输入源与「校验失败重试」都没有对应页面与数据面。这 6 卡怎么真跑出来（写侧真落库）？

## 目标

1. 四种输入源（图片／MD／对话／表单）各自的采集与落库路径走通；图片录入要说明「图片怎么给到技能」这条真实通道。
2. 「导入校验失败」出可重试的失败页（缺什么字段、怎么补），不静默丢弃。
3. 6 卡逐卡出结果型／回执型 HTML（双端自适应，走③的页面族）。
4. 写侧全部落**副本库**自证，真库只读对照。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t8-run-add.mjs` → 打印 6 行 `卡 → exit=0 → 产物绝对路径` ＋ 每卡的写后回读（行数／关键字段）且 exit 0
- 反例（必跑）：故意少给一个必填字段 → 必须走「校验失败」页并 exit≠0（不许写进半条脏数据）

## 不许动的东西

- 老件一行不动；其余 9 个域不碰。
- 不改老库 schema（缺字段的处置按④的结论走）。
- 不碰真库；写侧只在副本库上跑。
- `chef.recipe.write` 既有 `op` 语义务必保持向后一致。

## 交付物路径

- 代码：`packages/skill-chef/src/add/**`
- 证据：`docs/skills/skill-chef/t8-录入域.md`（6 行读数 ＋ 写后回读 ＋ 校验失败样本）

## 遗留出口

- 图片 OCR／识别若需外部能力 → 另立票（本票只做「给了结构化内容之后的录入」）。
- 批量导入的进度与中断恢复 → 另立票。

## 进度：0%

下一步：等④的字段对账结论，先跑通「对话录入」这条最短路径。

<!-- TICKET 9 | task | 修改域：端到端搬迁（4 卡，写侧） -->
## Question

修改域 4 卡（修改食谱主信息／修改步骤(内容/重排)／修改食材(用量/添加/关联步骤)／废弃食谱(只增不删)）在新仓只有 `chef.recipe.write` 的 `op:update`／`op:deprecate` 两条粗路径；HELP 里「修改步骤」「修改食材」两张卡标着【待开发】。改动前的**对比**、改动后的**回执**、以及「废弃只增不删」的语义都要真落库。这 4 卡怎么走通？

## 目标

1. 4 卡逐卡走通：改动前对比 → 确认 → 回执三段，全部真写副本库。
2. 步骤重排与食材关联步骤这两条语义定清楚（顺序字段怎么改、关联怎么落）。
3. 废弃语义：只置废弃标记、不物理删除，并在查询侧可见「已废弃」。
4. 逐卡一行读数 ＋ 写后回读。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t9-run-update.mjs` → 打印 4 行 `卡 → exit=0 → 产物绝对路径` ＋ 写后回读且 exit 0
- 反例（必跑）：把「废弃」实现成物理删除 → 脚本必须红并点名（只增不删是硬要求）

## 不许动的东西

- 老件一行不动；其余 9 个域不碰。
- 不改老库 schema；不碰真库。
- 「废弃」不许实现成物理删除。

## 交付物路径

- 代码：`packages/skill-chef/src/update/**`
- 证据：`docs/skills/skill-chef/t9-修改域.md`（4 行读数 ＋ 对比页与回执页样本）

## 遗留出口

- 批量修改 → 属数据管理域（小域合并票⑪）。
- 修改历史（审计追溯）→ 另立票。

## 进度：0%

下一步：等④的字段对账结论，先跑通「废弃食谱」这条最简路径。

<!-- TICKET 10 | task | 历史域：端到端搬迁（4 卡；记录做菜＝写、查看历史／查看统计＝读） -->
## Question

历史域 4 卡（记录做菜(完整＋快速＋补录)／历史时间线／单菜统计／全局统计）里，「记录做菜」要求**评分可 0-5 含小数、反馈必填真实内容**（禁「无」占位）、补录可指定日期；统计侧今天只有一个 `list` 信封把 timeline／stats／quality／backup 全塞成条目（`render/views.ts:102`）。这 4 卡怎么走通并出双端页面？

## 目标

1. 「记录做菜」三条路径（完整／快速／补录）走通，校验按老件口径（评分 0-5 可小数、反馈必填且禁占位词、补录日期可指定）。
2. 「历史时间线」按时间轴出结果型页；「单菜统计」「全局统计」各自出统计型页（不许用同一个列表信封糊过去）。
3. 4 卡逐卡一行读数 ＋ 写侧回读。
4. 本域是形状票（①）的试点域，代码要落在①定下的 `src/history/` 形状里。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t10-run-history.mjs` → 打印 4 行 `卡 → exit=0 → 产物绝对路径` 且 exit 0
- 反例（必跑）：提交 `feedback` 为「无」→ 必须被校验拦下并 exit≠0

## 不许动的东西

- 老件一行不动；其余 9 个域不碰。
- 不改老库 schema；不碰真库（写侧跑副本）。
- 「体检」这条质量报告虽然也走 `chef.history.query`，但它是数据管理域的卡，**本票不碰**（归⑪）。

## 交付物路径

- 代码：`packages/skill-chef/src/history/**`
- 证据：`docs/skills/skill-chef/t10-历史域.md`（4 行读数 ＋ 校验反例读数）

## 遗留出口

- 全局统计的口径（哪些指标算「整体画像」）→ 与老件对账后若仍有歧义，另立裁定票。
- 统计图表若要用公共层图表区块 → 沿用③的页面族。

## 进度：0%

下一步：等①的形状落地，再补本域页面与校验。

<!-- TICKET 11 | task | 小域合并：派生 3 ＋ 采购 1 ＋ 开始使用 1 ＋ 数据管理 3（8 卡） -->
## Question

四个小域共 8 卡：派生（添加派生关系／查看派生关系(家族树)／从已有派生新菜）、采购（生成采购清单）、开始使用（首次使用(初始化工作流)）、数据管理（数据质量报告／批量编辑／导出备份）。它们在 HELP 里**除体检外的 7 张全标着【待开发】**，且各有独立难点：家族树要有环检测、采购清单要「核对家里库存」但 `#682` A6-21 明令**技能包之间零依赖**、初始化要真建库、备份要真导出文件。这 8 卡怎么走通？

## 目标

1. 派生 3 卡：关系落库 ＋ 家族树可视化（含环检测）＋ 从母本派生新菜（复制后改差异）。
2. 采购 1 卡：多菜合并清单 ＋ 份数换算 ＋ 排除可选；**核对库存走调用契约**（跨技能不落代码依赖），拿不到就显式降级提示。
3. 开始使用 1 卡：初始化工作流（真建库／建目录，且只在需要时）。
4. 数据管理 3 卡：体检报告（质量评分口径要裁定：老件是「数据完整度」，新件 `chef.history.query kind=quality` 是「口碑」——两者语义不同）、批量编辑（改动前对比 ＋ 回执）、导出备份（真出文件，落点与命名在本票定）。
5. 8 卡逐卡一行读数。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t11-run-small-domains.mjs` → 打印 8 行 `卡 → exit=0 → 产物绝对路径`（备份卡另打印产物文件字节数）且 exit 0
- 反例（必跑）：造一个成环的关系对 → 家族树必须报环并 exit≠0（不许画出一张自环图）

## 不许动的东西

- 老件一行不动；其余 6 个域不碰。
- 技能包之间零依赖（`#682` A6-21）：采购核对库存不许 import 居家管家的代码。
- 不改老库 schema；不碰真库。
- 「体检」语义不许把老件的「数据完整度」偷换成「口碑」。

## 交付物路径

- 代码：`packages/skill-chef/src/{relation,shopping,setup,data}/**`
- 证据：`docs/skills/skill-chef/t11-小域.md`（8 行读数 ＋ 体检口径裁定 ＋ 跨技能调用契约说明）

## 遗留出口

- 体检若最终定为「两个都要」（完整度 ＋ 口碑）→ 明确拆成两张卡另立票。
- 备份的定时／增量 → 另立票。

## 进度：0%

下一步：先裁「体检」的口径（本票内第一件事），再按域逐个跑通。

<!-- TICKET 12 | task | [收口 A] 链路总表 ＋ 产物册子 ＋ 批量驱动器（验收副本） -->
## Question

链路总表要「prompt → 唤醒词 → 命令 → HTML 绝对路径」一页可点，且 50 条唤醒词各自都有产物；但今天绝对路径回执**只有 HELP 有**（`delivery` 只由 `chef.help.lookup` 产生），其余 7 键默认不落 HTML、`--html` 也不回显路径；且产物按配置推算会落在**副本库**的父目录下。这批产物怎么跑出来、册子怎么记、链路页怎么让人点开？

## 目标

1. 写**批量驱动器**：读唤醒词表 ＋ 卡表 → 逐条真跑（副本库 ＋ 副本配置）→ 每条落一份产物 → 收集绝对路径、exit、bytes、sha256 → 落 `manifest.json`。
2. 出**链路总表页**（照 `docs/skills/skill-calorie/t369-链路总表.mjs`）：50 行，每行 prompt／唤醒词／命令／参数／产物绝对路径（可点）；页顶写明「链接指向验收副本」。
3. 出**产物册子**：`manifest.json` 同时是收口 B⑬的墙清单来源；两侧**共用同一套 slug 规则**（用②定下的命名），禁各算一套。
4. **覆盖对账**：打印 `词 50／卡 48／缺 0`；任何缺项点名并 exit 1。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t12-链路总表.mjs` → 打印 `词 50／卡 48／链接 N 条／缺失 0` 且 exit 0
- 反例（必跑）：故意改坏 `manifest.json` 里一个文件名 → 脚本必须 exit 1 并点名那一份
- 反例（必跑）：从唤醒词表删一条 → 覆盖对账必须报缺并 exit 1

## 不许动的东西

- **不碰真库**（全部跑副本）；隔离通道写明 `CHANNEL-PENDING-#756`，不抢 `#756` 的裁定。
- 不改命令面（不加「回执带路径」的新字段）——路径由驱动器自选 `--html` 决定。
- 链路页不得用相对路径跨目录引用（会死链）；不得加 `loading="lazy"`。

## 交付物路径

- 器械（入仓）：`docs/skills/skill-chef/t12-链路总表.mjs`、`docs/skills/skill-chef/t12-run-all.mjs`
- 产物：`.scratch/<批名>/`（50 份产物 ＋ `manifest.json` ＋ 链路总表页）
- 证据：`docs/skills/skill-chef/t12-收口A.md`（正反例退出码 ＋ 覆盖对账读数）

## 遗留出口

- 墙与终审 → 收口 B⑬。
- 若发现某卡产物跑不出来 → 当场在对应域票开缺陷，不在这张票里修。

## 进度：0%

下一步：等 7 张域票全关，再起驱动器。

<!-- TICKET 13 | task | [收口 B] 双端视觉验收墙 ＋ vision_router 终审 ≥90 分 -->
## Question

最后一步：48 卡的**双端产物要能一页看全**，并被 vision_router 打分 ≥90。墙怎么出、分数怎么记、缺陷怎么收口？（验收墙 §4 的红线：别把「墙出得来」当成「验收过了」。）

## 目标

1. 照 `docs/agents/视觉验收墙.md` §3 收口七步：机检绿 → 发一批产物（与墙页同目录）→ 双端墙（390／1280，格高一致）→ 总索引页（含「有意不出产物及其原因」一节）→ 生成器自检正反例 → 人滚一遍 → 三样一起交。
2. 生成器照 `docs/skills/skill-calorie/scene02-验收墙/gen-wall.mjs` 起手，**必须带 `dropped` 判据**（`dropped` 与 `dead` 一起判；别照抄 `t154-mobile-wall.mjs` 的假绿灯）。
3. **vision_router 终审**：五维各 20 分（信息层级／双端自适应／文案精炼／视觉生动／风格一致）逐页打分 ＋ 整批复评，**≥90 分才关票**；不合格项逐条整改后复评。
4. 逐格缺陷清单（哪一格、什么症状、要不要改）。

## 验收命令

- 正例：`node tooling/run-locked.mjs --ticket <本票号> -- node docs/skills/skill-chef/t13-验收墙.mjs <产物目录> <输出名>` → 打印「N 格；链接 M 条；缺失 0 -> 可发」且 exit 0
- 反例（必跑）：在清单里故意写一个不存在的文件名 → 必须 exit 1 且点名那一份
- 打分：vision_router 终审总分记录在案，≥90 才关票

## 不许动的东西

- 不改产物内容（墙只呈现；发现缺陷回对应域票改）。
- 生成器不得加 `loading="lazy"`；清单不带签名（BOM）。
- 视觉模型报的缺陷必须**回产物 grep 核对**（`#682` C3-12）；核对不上不算缺陷。

## 交付物路径

- 器械：`docs/skills/skill-chef/t13-验收墙.mjs`
- 交付三样：两张墙（390／1280）＋ 逐格缺陷清单 ＋ 自检正反例退出码，落 `docs/skills/skill-chef/`
- 分数：`docs/skills/skill-chef/t13-终审打分.md`

## 遗留出口

- 未修的缺陷 → 当场转成有编号的票并回写本票（`编排纪律.md` 第十条）。
- 发版 → 另立收口票（出本图）。

## 进度：0%

下一步：等收口 A⑫交出册子，再出墙与打分。
