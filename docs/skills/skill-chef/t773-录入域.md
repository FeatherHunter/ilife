# t773 录入域端到端搬迁证据（写侧 6 卡）

票：[#773](https://github.com/FeatherHunter/ilife/issues/773)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 8）。本件交验收命令的全部读数与三条本图口径（过程性 vision 审查／册子片段／副本库沙箱）。

## 结论

6 卡逐卡经真出口写副本库成功并出回执页，反例缺字段走失败页且无半条脏数据。验收命令正例与反例、`--check`、质量门六列全绿。

## 1 沙箱与真库只读

- 器械：`docs/skills/skill-chef/t840-沙箱.mjs --ticket 773`，副本 `D:\ilife\.scratch\t773\chef_data.db`（294912 字节），隔离家目录 `D:\ilife\.scratch\t773\home`，通道家目录注入（CHANNEL-PENDING-#756）。
- 真库 `D:\2Study\StudyNotes\.db\chef_data.db` 全程只读：每次沙箱复制前后比对 stat，不一致即 exit 1；本票 4 次复制全部打印“真库 mtime 未变”。
- 一票一份副本：本票只用 `.scratch/t773/`，未碰他票副本。

## 2 六行读数（正例）

跑法：`node tooling/run-locked.mjs --ticket 773 -- node docs/skills/skill-chef/t773-run-add.mjs`

| 卡 | 唤醒词 | 命令 | 菜名 | 产物绝对路径 | 字节 | 写后回读 |
|---|---|---|---|---|---|---|
| add_from_image | 录入食谱 | chef.recipe.write | 番茄炒蛋 | D:\ilife\.scratch\t773\t773-add-from-image.html | 92523 | 食材3 步骤2 首味番茄 用量300 |
| add_from_markdown | 录入食谱 | chef.recipe.write | 青椒肉丝 | D:\ilife\.scratch\t773\t773-add-from-markdown.html | 92506 | 食材3 步骤2 首味青椒 用量200 |
| add_from_conversation | 录入食谱 | chef.recipe.write | 麻婆豆腐 | D:\ilife\.scratch\t773\t773-add-from-conversation.html | 92516 | 食材3 步骤2 首味嫩豆腐 用量400 |
| add_from_template | 录入食谱 | chef.recipe.write | 蒜蓉西蓝花 | D:\ilife\.scratch\t773\t773-add-from-template.html | 92473 | 食材3 步骤2 首味西蓝花 用量300 |
| import_from_json | 导入食谱 | chef.recipe.write | 鱼香肉丝 | D:\ilife\.scratch\t773\t773-import-from-json.html | 92478 | 食材3 步骤2 首味猪里脊 用量200 |
| import_validation_failed | 导入食谱 | chef.recipe.write | 宫保鸡丁 | D:\ilife\.scratch\t773\t773-import-validation-failed.html | 92516 | 食材3 步骤2 首味鸡腿肉 用量250 |

6 行全部 `exit=0`。落库三张主表（recipes 一行＋ingredients 三行＋cooking_steps 两行），维度表本期不写（t769 口径不变，818 定案）。

## 3 校验失败样本（反例必跑）

跑法：同上加 `--fail-case`

- 故意少给必填字段（食材缺数字用量，步骤缺数字时长）：写 exit 4（CHEF_BAD_QUERY，老库 NOT NULL），随后按名回读 exit 4（无此菜谱），证明未写半条脏数据。
- import_validation_failed 卡内另有一次缺值预演：先缺花生用量失败并出失败页 `t773-import-validation-failed-failure.html`（老件四段：操作名／失败原因／关键数据／建议下一步），确认无脏后补齐重试成功，本卡产物为补齐后的成功回执。
- 原子性修法：`src/add/run-write.ts` 新增 `assertAddPayload`，内嵌行在碰库前按与单行口径同文逐行预检，红即抛不碰库。此前实测缺第二味用量仍留下菜名与首味（半条脏数据），修后复测无残留。

## 4 页面族与质量门

- 形状：走票 3 配方回执型 A（结论条＋事实条＋备料三列表＋步骤行列表＋变更行＋口径行＋动作行＋复制区）；失败页走老件四段＋修正重试。每格一次公共层区块调用，页面零自写样式（extraCss 只拼公共层两份配方样式）。
- 机审：`node docs/skills/skill-chef/t768-质量门.mjs .scratch/t773 --widths 390,1280` 全绿。横向溢出 390 与 1280 双 0；触摸目标每页 3 至 4 件可点、最短边恒 44px；触屏两件齐；分隔符懒政版式位 0；英文裸词 0；重复句 0。途中红过两次并当场改掉：ASCII 菜名（MD 与 JSON 进版式位）改中文；文档标题与页标题同句重复改两名。
- 代码层 UI 审查清单（票 3 第 7 节，逐条结论）：
  - 硬编码样式：活色值 29 个、字号档 20 档，全部来自公共层共享样式表；本页补丁 0 个色值。结论：通过。
  - 内联样式：产物 `style=` 0 处。结论：通过。
  - 字号与间距标尺：20 档全在共享表档位里，页面无自造字号。结论：通过。
  - 组件复用：非 `ilife-` 产物类名仅 1 个 `wrap`（公共层文档壳自带，与 `ilife-page` 同元素）；老件类名 0 个。结论：通过。
  - 重复样式块：产物样式块 1，高频声明 59 条系公共层内部复用，非同段复制。结论：通过。

## 5 过程性 vision 审查

- 看法：`vision_describe` 审 3 截图（手机成功／手机失败／桌面成功），报 20 项；截图由本机 Chrome 直出（390 与 1280），产物 grep 复核（#682 C3-12：模型是提示器不是证据）。
- 当场改 2 项：菜名去来源前缀消标题冗余；步骤统一为行列表消双形状。改后复跑质量门仍全绿，抽页复看确认。
- 驳回 18 项（理由记 `.scratch/t773/vision.json`）：加图与 emoji 违反 t768 第 5 节第 4 条；桌面留白与行距属公共层既定；副本库与维度表说明系票面要求；按钮截断系截图视口截断（机审溢出 0）；失败关键数据原文正确系模型误读。
- 判据：`.scratch/t773/vision.json` 缺陷 0。`--check` 打印片段行数与缺陷两行，缺一即红。

## 6 册子片段

- 落点：`docs/skills/skill-chef/t773-册子片段.json`（6 行，字段卡 id／唤醒词／命令／参数／产物绝对路径／exit／bytes／sha256），供收口 A 合并；未改共用册子。
- `--check` 读数：片段行数 6（本票卡数 6），vision 缺陷 0。

## 7 图片通道说明（目标第 1 条）

图片录入的真实通道：用户先发菜谱图片，AI 把图片里的菜名与用料转成结构化内容后再调用录入。本票只做给了结构化内容之后的落库（ params  ingredients 与 steps 数组），图片识别本身按遗留出口另立票。四种输入源的落库路径收敛为同一条三表写入（818 甲：缺数字用量与时长当场问用户要），差异在采集说明与页面，不在库路径。

## 8 测试与门禁

- 靶向：fetch 与 cli 与 policy 20 项全绿。
- 全量：render 形状计数 1 红（期望 8 实测 12），系他席在途加键（relation 与 setup 与 data 四键）所致，不在本票写集内；本票路径零触碰该件，归属范围外发现。
- 构建：`tsc -b packages/skill-chef` exit 0。

## 9 运行记录（声称即对账）

- GATE-RUN runId=07190a83-3028-4585-8423-884e198917c9 cmd=tsc -b packages/skill-chef exit=0
- GATE-RUN runId=e30b99b3-f31b-461b-9efc-8786b45cf258 cmd=node --test fetch＋cli＋policy exit=0（20/20）
- GATE-RUN runId=c2826bea-1b2b-43bf-bcaa-a941904b7015 cmd=t773-run-add.mjs 正例 exit=0
- GATE-RUN runId=52005a03-dc12-4ae5-a8fd-5fa55647997f cmd=t773-run-add.mjs --fail-case exit=0（反例 exit 4 且无脏）
- GATE-RUN runId=0f12fbe4-4b4a-4c0b-aaf6-b4f80ef9bffb cmd=t768-质量门 390/1280 exit=0
- GATE-RUN runId=95e262eb-2d5c-4ca6-939b-fccc3602b119 cmd=t773-run-add.mjs --check exit=0

## 10 遗留出口（本票不做）

- 图片识别若需外部能力另立票；批量导入的进度与中断恢复另立票。维度表写侧未开（t769 原口径）。

## 11 写集对账（结构纪律第五步）

事前清单：`src/add/**`（预检＋页面＋能力门）＋ `docs/skills/skill-chef/t773-*`（驱动＋片段＋证据）＋ `.scratch/t773/**`（副本＋产物＋截图＋日志）。实际暂存区只含这三处，他席文件一律不在内，偏差为零。

超线报警：已超线，需要根据规则进行重构。本票新件 `src/add/pages.ts` 约 170 行、`src/add/run-write.ts` 约 210 行，均在 350 线内，无需拆；包内既有超线三件按 AGENTS 挂号不拆，本票未碰。
