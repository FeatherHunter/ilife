# t775 历史域端到端：4 卡读数 ＋ 校验反例 ＋ 审查结论

票：[#775](https://github.com/FeatherHunter/ilife/issues/775)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 10）。
跑法：`node tooling/run-locked.mjs --ticket 775 -- node docs/skills/skill-chef/t775-run-history.mjs`（`--check` 见 §6）。

## 1 四卡读数（副本库 `.scratch/t775/chef_data.db`，真库只读）

| 卡 | 唤醒词 | 命令与参数 | CLI exit | 产物（绝对路径） | bytes |
|---|---|---|---|---|---|
| hist-1 记录做菜 | 记录做菜 | `chef.history.record {"name":"辣椒炒肉","rating":4.5,"feedback":"虾很Q弹，下次少放盐","date":"2026-09-21"}` | 0 | `D:\ilife\.scratch\t775\hist-1-记录做菜回执.html` | 90204 |
| hist-2 历史时间线 | 查看历史 | `chef.history.query {"name":"辣椒炒肉"}` | 0 | `D:\ilife\.scratch\t775\hist-2-历史时间线.html` | 89663 |
| hist-3 单菜统计 | 查看统计 | `chef.history.query {"kind":"stats","name":"辣椒炒肉"}` | 0 | `D:\ilife\.scratch\t775\hist-3-单菜统计.html` | 89341 |
| hist-4 全局统计 | 查看统计 | `chef.history.query {"kind":"stats"}` | 0 | `D:\ilife\.scratch\t775\hist-4-全局统计.html` | 90154 |

三路径（同属 hist-1，逐条 CLI exit 0）：完整（上表行，评分 4.5＋反馈＋指定日期）／快速（`{"rating":5,"feedback":"刚做完，不错"}`，日期缺省今天）／补录（`{"rating":4,"feedback":"昨天补记，火候过了","date":"2026-09-20"}`）。
写侧回读：辣椒炒肉写前 1 条 → 写后 4 条（`historySingleStats.count` 断言 `写前+3`）；时间线 `cook_date` 逐行倒序断言通过；单菜统计回 `count/avgRating/maxRating/minRating/lastDate` 五字段；全局画像回 `cookedCount/neverCookedCount/totalCooks/recipeTotal/favoriteAvg/favoriteMost/recent/neverCooked` 八字段。
真库：`D:\2Study\StudyNotes\.db\chef_data.db` 跑前后 294912 B／mtime 2026-08-09 13:38:15，未动。

## 2 校验反例读数（必跑）

- `chef.history.record {"name":"辣椒炒肉","rating":4,"feedback":"无"}` → exit 2（口径错：占位词拦下），满足 `exit≠0`。
- 评分缺失仍走 818 定案：`{"name":"辣椒炒肉","feedback":"缺评分测试"}` → exit 4（`CHEF_BAD_QUERY`，不写历史），存量测试 `cli.test.mjs:132` 作证。
- 评分越界／非数字：`validateRating` 0-5 含小数（`policy/category.ts` 既有口径，动的是它，用的是它，不重复定义）。
- 补录日期非法（非 `YYYY-MM-DD` 或非真日期）→ 口径错 exit 2。

## 3 取数与页面口径（本票定的三件事）

1. 时间线倒序：`cook_date` 倒序、同日按 `cook_sequence` 倒序（老件 `history_manager.py list` 的 `ORDER BY cook_date DESC`）。
2. 单菜统计全指标：总次数／均分／最高／最低／最近日期（老件 `stats` 五字段；均分直接算无剔除，818 定案）。
3. 全局画像：做过几道／总次数／菜总数／最爱（均分最高＋做得最多）／最近 5／没做过（老件 `global-stats` 单查询口径逐字对齐；均分保留两位；歧义已消除，不另立裁定票）。
4. 信封形状不变：`chef.history.record=receipt`、`chef.history.query=list`（`gen:check` 绿）；四页是各卡自己的结果型／回执型页，不共用同一个列表信封糊过去。
5. 边界：「体检」走 `chef.history.query kind=quality`，住数据管理域，本票不碰；「改评分」唤醒词走同一条记录路径（记一次新的，路由见 `src/history/routes.ts:33-38`，不发明按 id 改评分）。

## 4 代码层 UI 审查清单（票 3 交的五条，逐条结论）

机审读数（质量门 `t768-质量门.mjs`，390／1280）：六列 4 页全绿（横向溢出 0／触摸目标最短边≥44／触屏三件齐／分隔符 0／英文裸词 0／重复句 0）。

| # | 审什么 | 读数 | 结论 |
|---|---|---|---|
| 1 | 硬编码样式 | 活色值 29 个、字号档 20 档，四页与 `t768` 读数逐字相同 | 全部来自公共层共享样式表，本页补丁 0 个色值（`extraCss` 只拼 `pageUiCss＋pageShapeCss`） |
| 2 | 内联样式 | hist-1 2 处、hist-3 1 处（例 `width:90%`）、hist-2／4 0 处 | 全部由公共层读数卡产出（条宽是数据，不是样式），页面自己 0 处 |
| 3 | 字号与间距标尺 | 20 档全在共享表 | 走标尺；页面无自造字号（正文走 `#860` 正文段落件 15px／1.7） |
| 4 | 组件复用 | 非 `ilife-` 产物类名 1 个：`wrap`（公共层文档壳根类） | 复用；页面无自造组件类 |
| 5 | 重复样式块 | 产物样式块 1、原型壳样式块 0、高频声明 59 条 | 无重复样式块；59 条是公共层内部复用（同口径 `t768` §7） |

## 5 过程性 vision 审查（缺陷当场改，不拖到收口票）

结论文件：`docs/skills/skill-chef/t775-vision.json`（390 全页 ×4、1280 全页 ×2，逐张肉眼核过）。

- V1 hist-1「已做 → 已做」恒等变更行 → 已改（状态未翻转时不印该行，只留次数行）。
- V2 hist-4 最爱同菜名印两遍 → 已改（合句）。
- V3 hist-4 均分三处两种精度 → 已改（取数即两位＋显示统一两位）。
- V4 hist-4 桌面端折叠区收进中间列 → 误报（公共层 `pageUi` ≥1001 双版心既有口径，`t768` §9.2 已登记，另立公共层票，本票不动公共层）。
- G1 机审重复句（`<title>` 与 `<h1>` 逐字重复 ×3）→ 已改（文档标题加「 ｜ 私家大厨」后缀）。
- G2 机审分隔符（口径行「；」×1）→ 已改（单句逗号版）。
- 当前：未闭环 0 条；质量门重跑六列全绿。

## 6 `--check` 判据（票面双判据，缺一即红）

`node docs/skills/skill-chef/t775-run-history.mjs --check` 打印 `片段行数=4（本票卡数=4）` 与 `vision审查缺陷 0（已改 5 条，误报 1 条，未闭环 0 条）`，双行齐则 exit 0。

## 7 交付对账（必报第五步：事前清单逐行有着落，偏差为零）

- `src/history/validate.ts`（新建，66 行）：反馈＋日期校验，已落。
- `src/history/run-record.ts`（改）：接校验＋`date`／`cook_date` 别名，818 行为不变；本地 `today()` 已删改调共用定义（概念唯一）。
- `src/history/run-query.ts`（改）：倒序＋单菜全指标＋全局画像；`list` 形状不变。
- `src/history/pages.ts`（新建，4 导出）：四页装配，零自写样式。
- `src/history/index.ts`、`commands.ts`、共用位、四工种目录、其余 9 域：未碰（偏差零）。
- `tsc -b packages/skill-chef` 干净；`gen:check` 绿；`node --test` 69／70——唯一红色是 `render.test.mjs` 的「8 key」断言（`src/render/envelope.ts` 被小域合并票在途 ＋4 键，非本票写集，见 §8）。
- 超线报警：四文件最大 254 行，均未超 350 线（无需报「已超线，需要根据规则进行重构。」）。

## 8 在途干扰声明（非本票债务）

全量测试唯一红色与本票无关：在途小域合并票正在改 `src/render/envelope.ts`（`chef.relation.*`／`chef.setup.init`／`chef.data.batch` ＋4 键），`render.test.mjs:13` 的 8 键断言在其写集里变红。
本票历史域三项测试（record／query 闭环、818 缺值、fetch 历史统计闭环）全绿；待其落定收口票统一修测试基线。
