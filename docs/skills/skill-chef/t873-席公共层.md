# t873 公共层席：一件视觉皮肤 ＋ 10 处域壳接线，4 个样本页视觉生动全部上升、总分全部不降

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**公共层席**。

**一句话结论**：新件 `packages/skill-chef/src/render/skin.ts` 给 48 页加了一层只对根类 `.ilife-page-ui`
生效的视觉皮肤（品牌暖色带 ＋ 页头柔光 hero ＋ 眉标图标位 ＋ 标题横条 ＋ 分区标题暖色方块图标位 ＋
事实条瓦片 ＋ 结论条渐变面板 ＋ 表头带 ＋ 分卡浅影），10 个域壳改调单一入口 `chefSceneCss()`；
4 个样本页在冻结尺下**视觉生动四页全升、总分四页全升**（对照 `t873-开工基线.md` §一 的改前读数）。
改的是**公共层**：页内版式与文案那部分留给域席，本席一行域模块装配逻辑都没动。

---

## 一 改了哪几处

| 落点（file:line） | 改前 | 改后 | 对应哪条扣分项 |
|---|---|---|---|
| `packages/skill-chef/src/render/skin.ts`（新件，1–295） | 无此件 | `chefSkinCss()`／`CHEF_SKIN_CSS`／`chefSceneCss()` 三个导出；17 组规则，每一条都挂在 `.ilife-page-ui` 之下 | 「视觉生动」13.1（全场最低）＋「信息层级」的「缺形状件（分隔、分组、图标位）」 |
| `packages/skill-chef/src/render/index.ts:7-10` | 无 | 转出 `CHEF_SKIN_CSS`／`chefSkinCss`／`chefSceneCss`／`ChefSkinCssInput`（域壳与 2 个 mjs 驱动器从这里取） | 同上（单一入口） |
| `src/add/pages.ts:8,66` | `pageUiCss() + '\n' + pageShapeCss()` | `chefSceneCss()` | 整批 48 页共用的样式入口 |
| `src/cook/run.ts:25,331` | 同上 | 同上 | 同上 |
| `src/data/pages.ts:29,35` | 同上 | 同上 | 同上 |
| `src/history/pages.ts:26,50`（＋件头注释第 7 行同步改口径） | 同上 | 同上 | 同上 |
| `src/relation/pages.ts:20,26` | 同上 | 同上 | 同上 |
| `src/setup/pages.ts:17,63` | 同上 | 同上 | 同上 |
| `src/shopping/pages.ts:18,86` | 同上 | 同上 | 同上 |
| `src/view/page.ts:15,189-191`（＋接线那行的上一句注释同步） | 同上 | 同上 | 同上 |
| `docs/skills/skill-chef/t771-run-search.mjs:42,166`（＋件头第 21 行注释同步） | 同上 | 同上 | 同上 |
| `docs/skills/skill-chef/t774-run-update.mjs:89-96,106` | 同上 | 同上 | 同上 |
| `packages/skill-chef/test/skin-873.test.mjs`（新件） | 无此件 | 19 条断言（见 §四） | 把三条设计约束钉成可跑的判据 |

**皮肤里都做了什么**（每条都只加形状与色彩，**不动任何区块的标记结构**）：

| 组 | 落点 | 做什么 | 销哪条账 |
|---|---|---|---|
| ① 品牌带 | `.ilife-page-ui::before` | 顶端 6px 暖色渐变（`#ffcc00→#ff9500→#ff2d55`，全部取 `CHART_PALETTE`） | 「缺色彩锚点」「缺温度」 |
| ② 页头 hero | `.ilife-page-ui` 的 `background-image` | 两团柔光（右上暖 `#ffcc00`、左上主色）＋ 一条暖→主色→透明的渐隐（止于 360px） | 同上；域位与正文区从此有「面」的差别 |
| ④ 眉标 | `-eyebrow-app::before`／`> span + span` | 品牌位前一枚 10px 暖色圆点（图标位）、域位改成 `--soft` 底胶囊 | 「缺形状件」 |
| ⑤ 标题横条 | `-page-shell-title::before` | 标题上一枚 44×4 暖色横条 | 「缺形状件」 |
| ⑥ 结论条 | `-conclusion` | 4px 主色左缘 ＋ 主色 14% 渐变面板 ＋ 浅影（原为白卡＋3px 蓝边） | 「卡片＋灰底为主」 |
| ⑦ 事实条 | `-fact-strip`／`-item` | 每格一枚浅瓦片（6px／10px 内距、`--line` 描边、3px 主色左缘、圆角 14） | 「只靠文字层级」「一行字看不出是几件事」 |
| ⑧ 分区标题 | 五枚标题类名 | 标题前一枚 18px 暖色方块**图标位**（圆角 8，`content: ""` 纯装饰，一个字都不进 CSS） | 「缺形状件（分隔、分组、图标位）」 |
| ⑨ 读数卡 | `-kpi-card`／`::before` | 顶缘一条品牌暖色条 ＋ 暖调描边 | 「缺色彩锚点」 |
| ⑩ 数据表 | `-data-table`／`th`／偶数行 `td` | 表卡浅影、列头一行浅蓝底（表头带）、斑马行换极浅蓝 | 「区块靠文字层级」 |
| ⑪ 折叠条 | `-disclosure`／`-summary::before` | 左缘暖色条 ＋ 浅影；开合标记换成 24px 圆底图标位（`▸` 那个字形仍由公共层给，本件只换底盘） | 「缺形状件」「图标位」 |
| ⑫ 变更行 | `-change-row-old`／`-new`／`-arrow` | 旧值红底胶囊、新值绿底胶囊（`CHART_PALETTE[3]`／`--ok` 派生的 10%／16% 浅底） | 「改了什么全靠读字」 |
| ⑬ 列表行 | `-list-rows`／`-left` | 卡浅影；左槽（标记位）取主色——此前与右槽同为灰、整行等重 | 「层级只靠文字」 |
| ⑭ 时间轴 | `-timeline`／`-dot` | 轴线加粗到 2px 并上主色、圆点带主色光晕 | 「缺形状件」 |
| ⑯ 复制区 | `-copy-block` | 收进一张卡（描边＋圆角 14＋浅影），不再只有一个标题悬在页底 | 「区块之间看不出边界」 |
| ⑰ 主动作按钮 | `action-btn-primary` | 主色渐变（`--blue`→`--blue2`）＋ 主色浅影 | 「色彩脉冲只剩顶上一根细蓝条」 |

**撤掉的两条（各有一条读数作证，写在件内注释里）**：

- **页底收口带（`.ilife-page-ui::after`）**：`::after` 跟在内容后头，内容比一屏短就悬在页面半空的空白里
  （看得出是渲染残迹）；要压到视口下沿得把版面根改成纵向 flex，为一个 3px 装饰动版面根的盒模型不划算 ⇒ 撤。
- **徽章与页内导航的「浅渐变 ＋ 主色描边」**：改完第一轮复评时，全批**仅有的两页带徽章行与页内导航**的样本
  （06／17）双双掉分（06 84→77、17 77→74，「Tag 胶囊过多／挤占视口」）⇒ 撤。撤后同一批图 06 回到 81、17 升到 79。

---

## 二 逐页销账（两行读数）

改前＝`t873-开工基线.md` §一（冻结 2026-09-21）。另列一行**本席自己复跑改前图**的读数
（同一把尺、同一批 `.scratch/t873/shots/` 图，独立一次调用）——两行一起看才看得出来尺子本身抬了多少。

| 格 | 页名 | 改前（冻结基线 §一） | 改前（本席复跑） | 改后五维＋总分 | 原扣分项逐条：改了什么 → 改后读数 |
|---|---|---|---|---|---|
| 46 | 数据质量报告（体检） | 18/18/16/**12**/17 = **81** | 16/18/17/14/15 = **80** | 18/17/16/**14**/18 = **83** | 「缺图缺色彩锚点」（12）→ 品牌带 ＋ hero ＋ 事实条瓦片 ＋ 折叠条暖色左缘 ⇒ **14（+2）**；「区块靠文字层级」→ 分区图标位 ＋ 卡浅影 ⇒ 层级 18（+0）；总分 **81 → 83（+2）** |
| 6 | 辣椒炒肉（查看食谱） | 18/17/17/**13**/16 = **81** | 18/16/17/15/18 = **84** | 17/18/17/**15**/17 = **84** | 「缺图解感、色彩锚点少」（13）→ 品牌带 ＋ hero 柔光 ＋ 表头带 ＋ 食材分隔暖色竖条 ⇒ **15（+2）**；总分 **81 → 84（+3）**（对本席复跑值持平）；剩余扣分（桌面两侧留白、无菜品图）**不属公共层**，见 §五 |
| 17 | 多维组合筛选（筛选菜系） | 18/16/15/**10**/16 = **75** | 17/17/13/14/16 = **77** | 18/17/15/**17**/18 = **85** | 「整体视觉偏素、粉彩气泡与冷色卡片衔接割裂」（10）→ 一处暖色体系（品牌带／hero／图标位／分区竖条）收口全页配色 ⇒ **17（+7）**；总分 **75 → 85（+10）**（对本席复跑值 +8） |
| 41 | 录入回执：宫保鸡丁（导入食谱） | 16/14/10/**12**/16 = **68** | 17/17/14/14/17 = **79** | 17/17/15/**13**/17 = **79** | 「整页纯灰白卡片＋蓝点单色」（12）→ 事实条瓦片 ＋ 变更行红/绿语义胶囊 ＋ 表头带 ⇒ **13（+1）**；总分 **68 → 79（+11）**（对本席复跑值持平） |

**判定（对照派单册指定的改前口径 `t873-开工基线.md` §一）**：4 个样本页**视觉生动全部上升**（+2／+2／+7／+1）、
**总分全部不降**（+2／+3／+10／+11）。四页都**没落进 88–92 抖动带**，按尺子口径不需要第二次独立复评。

**一条器具事实（对收口席有用）**：本会话里 `vision_describe` 对**同一对图 + 逐字同一问句**是**可复现**的
——06 的改后读数连跑两次给出**逐字相同**的 JSON（77／77），改前也是（84／84）。⇒ 同一批图内的读数**不含采样抖动**；
改前基线（81／75／68）与本席复跑（84／77／79）之间的差是**两次不同批次**的尺子差，不是抖动带。

---

## 三 反例（改坏必红／还原一致）

**反例做法**：把皮肤里承担视觉生动的**两条关键规则注释掉**——② 页头 hero（`background-image` 那一组）
与 ⑧ 分区标题暖色方块图标位（`::before` 那一组）——重出产物批到 `.scratch/t873-公共层/反例批/`，
按 §二 逐字问句复评同两页；然后**逐字还原**这两组规则，重编 ＋ 重出，再复评同两页。

| 步 | 页 | 读数 | 判 |
|---|---|---|---|
| 改坏（16 条规则一条不改，只注释掉那两条） | 17 | 18/15/13/14/17 = **77** | **红**（改后 85 → 77） |
| 改坏 | 6 | 17/18/17/11/15 = **78** | **红**（改后 84 → 78） |
| 还原（逐字回绿） | 17 | 18/17/15/17/18 = **85** | **绿**（与改坏前**逐字相同**的 JSON） |
| 还原（逐字回绿） | 6 | 17/18/17/15/17 = **84** | **绿**（与改坏前**逐字相同**的 JSON） |

反例那一轮产物留在 `.scratch/t873-公共层/反例批/`（**不进验收墙、不提交**），正式批在 `.scratch/t873-公共层/批/`。

---

## 四 机器面

### 4.1 门禁运行记录（§2.4 第 2 条：一次运行一行，runId 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=98925d95-31f3-4834-9d5f-1e92d96d8633 cmd="node node_modules/typescript/bin/tsc -b packages/skill-chef" exit=0
GATE-RUN runId=3c5acf6c-8ce4-44b3-aba9-88e1012d24b4 cmd="node --test packages/skill-chef/test/skin-873.test.mjs" exit=0
GATE-RUN runId=0ee341b0-69a1-4608-9b09-273f5d5af646 cmd="pwsh -NoProfile -File .scratch/t873-work/regen.ps1 .scratch/t873-公共层/批" exit=0
GATE-RUN runId=c4a0807a-02a3-4cd0-b3ed-b75535115c4a cmd="pwsh -NoProfile -File .scratch/t873-work/walls.ps1 .scratch/t873-公共层/批" exit=0
GATE-RUN runId=41dbf6e4-e9e8-4179-a3cf-09af94789349 cmd="node node_modules/typescript/bin/tsc -b packages/skill-chef" exit=0
GATE-RUN runId=c226fe55-34f6-4266-acd3-0d33e053b9a8 cmd="pwsh -NoProfile -File .scratch/t873-work/regen.ps1 .scratch/t873-公共层/反例批" exit=0
GATE-RUN runId=9bbd2b9e-9fb1-4fc9-9d72-0260cc0fa3a8 cmd="node docs/skills/skill-chef/t778-截图.mjs .scratch/t873-公共层/反例批 .scratch/t873-公共层/反例批/shots --viewport-only" exit=0
```

（第 5–7 行＝§三 反例那一轮：41dbf6e4 是「注释掉两条规则」后的编译，c226fe55／9bbd2b9e 是那一轮的重出与出图。
`gate-runs.log` 里同一 ticket 的其余条目是本席迭代过程中的调试红／中间版本轮次，按 §2.4 第 3 条不计入对账。）

### 4.2 编译与包内测试

- 编译：`node tooling/run-locked.mjs --ticket 873 -- node node_modules/typescript/bin/tsc -b packages/skill-chef`
  → `RESULT: ticket=873 runId=98925d95-… waitedMs=1 exit=0`。
- 包内测试（新件 `test/skin-873.test.mjs`）：`ℹ tests 19 / ℹ pass 19 / ℹ fail 0`，exit 0。
  三组判据：① 皮肤每一条选择器都在 `.ilife-page-ui` 之下（含换前缀后同判）＋ 圆角只用闭集 `{8,14,20,999}`、
  断点只用仓内既有集合；② 不含那三种禁入写法；③ 10 处壳逐文件 grep `chefSceneCss(` 且旧的两层拼接一处不剩。
  另加一条端到端断言：真装配出的整页产物里带皮肤、且不含 `loading="lazy"`。

### 4.3 质量门（`.scratch/t873-公共层/批/t778-质量门.json`）

```
汇总：横向溢出 0／触摸目标 1／触屏三件 0／分隔符懒政 0／英文裸词 1／重复句 0（页 × 变体 53 行）   exit=1
红行仅一条：HELP/私家大厨_HELP_验收副本.html => 触摸目标::100 件可点，最短边 14px | 英文裸词::1 处
```

**这两条是改前就有的既有红**（#871 已记档，`t873-派单册.md` 动作序列第 6 条逐字交代），**HELP 不上墙**，
不是本席引入的：本席改动只落在 10 个域壳的 `extraCss` 一行 ＋ 新皮肤件，HELP 页走的是另一条装配链
（`src/help/helpFile.ts` → `base-paint/help-shell`），一行都没碰。**48 个域页那 48 行六列全绿**。

### 4.4 验收墙（两条都必须 exit 0）

```
自检：墙格 48／册子 48／不进墙 0；手机墙-390.html 97 ＋ 总索引.html 50；质量门 横向溢出红 0／触摸目标红 0／触屏三件红 0／分隔符懒政红 0／英文裸词红 0／重复句红 0；册子 manifest.json
出墙：48 格；链接 147 条；缺失 0 -> 可发     exit=0
自检：墙格 48／册子 48／不进墙 0；桌面墙-1280.html 97 ＋ 总索引.html 51；质量门 横向溢出红 0／触摸目标红 0／触屏三件红 0／分隔符懒政红 0／英文裸词红 0／重复句红 0；册子 manifest.json
出墙：48 格；链接 148 条；缺失 0 -> 可发     exit=0
```

### 4.5 终审扫描

```
扫 53 件产物；三类字样命中 0 处
共 53 件；命中 0 -> 干净     exit=0
```

### 4.6 结构纪律

- `packages/skill-chef/src/render/skin.ts`：**295 LF**（口径＝只数换行符），低于本包告警线 350 ⇒ 不必走必报第四步。
- 本席写集（第一步影响清单 → 第五步对账）：`src/render/**`（新件 ＋ `index.ts` 转出）＋ 派单册点名的
  10 个壳文件（各只改接线那一行与 import，另把直接描述那一行的三处注释同步改口径）＋
  `test/skin-873.test.mjs`（新件）＋ 本证据件。**偏差＝0**；未碰任何域模块的页面装配逻辑。
- 铁律二（概念唯一）：暖色与浅底色基全部从 `base-paint` 取（`CHART_PALETTE` 按**下标**取、
  `CSS_VAR_TOKENS` 取 token 值再 `rgbOf` 派生），本件一个凭空发明的十六进制色值都没有；
  品牌渐变与厂商标记各只有一处定义（`BRAND_WARM`／`BRAND_MARK`）。

---

## 五 未做项与下一手缺什么

1. **只验了 4 页**：另外 44 页的读数留给各域席与收口席。本席给的是「公共层这一层」，域页里
   页内专属的版式与文案问题（表格列错位、结论条与摘要复读、KPI 卡孤行、长描述段断句）**一律没碰**。
2. **`视觉生动` 维的天花板**：这一维在本会话的尺子上按「有没有菜品图／插画」给分——4 页改后
   无一页超过 17，且评委每一页都在提「缺菜品图／步骤插画」。公共层的 CSS 只能把「色彩锚点与形状件」
   补到够，**再加就只剩图**。真要把这一维推到 18+，需要一条**公共层之外的**决定：技能侧要不要出
   菜品图位（`renderMediaFigure` 已有现成形状件，但本批 48 页没有任何一页的数据源里有图，
   且它的产出器会写 `loading="lazy"`，与验收墙的造册判据冲突）⇒ 建议另立票裁决「图从哪来」。
3. **`txt 精炼` 维是本批第二低（15.19）**，本席一行没动：那要改页面装配的文案（域席写集的活）。
4. **越界报备（未碰，只记录）**：
   - 派单册指定的重出命令 `regen.ps1` 内部会跑 7 个域驱动器与 `t777-run-all.mjs`，它们会**重写
     `docs/skills/skill-chef/t770…t776-册子片段.json`** 这 7 个受版本控制的件、并往
     `.scratch/t770…t776`／`.scratch/t777` 写产物。这是**指挥链指定的命令自身的副作用**，不是本席顺手改；
     那 7 个件**未提交**（不在本席写集里），交收口席统一处置。
   - 批目录落在 `.scratch/t873-公共层/批/`（而不是 `.scratch/t873-公共层/` 本身）：`t778-验收墙.mjs`
     的 造册第 139 行会 `rmSync` 整个批目录，**日志文件放在批目录里会被它删到一半时锁住 → EPERM**
     （实测第一轮就是这么红的，报错在 Node 的 `rmSync`）。故批目录下沉一层、日志留在 `.scratch/t873-公共层/*.log`。
5. **域席接手要注意的一条**：公共层不再暴露 `pageUiCss()`＋`pageShapeCss()` 的拼接写法，
   页壳一律调 `chefSceneCss()`；域席若要加**页内**专属样式，请继续走 `extraCss` 追加，
   **不要**把两层拆回去（`test/skin-873.test.mjs` 会红）。
   （第三轮起这一条升级为：页壳一律调 `renderSceneShell()`，见下节 §六.5。）

---

# 第三轮（2026-09-22）：把已证明能得分的视觉模式收成一份共用标准

**一句话结论**：新出三件（`src/render/sceneBand.ts`／`sceneRhythm.ts`／`sceneShell.ts`）把「族级装饰带 ＋
色彩锚点密度 ＋ 首屏填满度」做成**按页族一份**的共用标准，10 处页壳改成一次调用 `renderSceneShell()`；
48 页首屏最底一行距视口底的间距**均值 49.6 → 26.9px、最差 355 → 185px**；7 页样本在同一会话里量出
**本轮前 → 本轮后**：21 **76/76 → 87/87（+11）**、12 87 → **85/85（−2）**、45 83 → **82/82（−1）**、
35 78 → **79/79（+1）**、27 79 → **83/83（+4）**、47 77 → **82/82（+5）**、42（对照）83 → **83/83（0）**。

**一句必须与结论同读的器具事实**：**本轮 7 页在本会话里没有一页拿到 ≥90**，包括那页**产物字节一字未改**的
第 21 格——它在 `.scratch/t873/shots/` 上的旧图（终批拿 92 的那一份）在本会话同一问句下只拿 **76/76**。
⇒ 跨会话位移 **16 分**，比本批任何一页的设计差都大；「≥90」只能同会话同批判（与 `t873-终审打分.md` §四.1 同源、
量更大）。本轮因此**用「同会话 本轮前 → 本轮后」当判定口径**，并把两跑都登出来。

## 六.1 改了哪几处（逐条对派单的三件事）

| 交付项 | 落点 | 做什么 |
|---|---|---|
| **① 族级装饰带** | 新件 `src/render/sceneBand.ts`（190 LF） | `renderSceneBand(family)` 出一条**内联 SVG** 横带（不是 `<img>`、无 `loading`、无外部资源），三族各一条变体：`result`＝碗＋椒＋放大镜＋盘；`process`＝炒锅＋锅铲＋火苗＋计时器；`receipt`＝砧板＋菜刀＋白纸回执＋笔。物件是**器物剪影 ＋ 饱和色填充**（色值全部按 `CHART_PALETTE` 下标取），底是暖色浅渐变；窄档 52px／宽档 58px（**不缩成细线**，包内测试钉了 ≥40px 的下限） |
| ①之接线 | 新件 `src/render/sceneShell.ts`（61 LF）＋ 10 处页壳 | 装饰带插在**版面根的第一个子节点**（眉标之前），满宽（上下负边距吃掉版面根内距）；10 处页壳由「自己拼 `renderDocShell`」改成**一次调用** `renderSceneShell({ family, … })` |
| **② 首屏填满度** | 新件 `src/render/sceneRhythm.ts`（155 LF）的 `receiptFillCss` | **只给回执族**：块距 16→26px、瓦片／结论条／表格／列表行／复制区内距各抬一档、可点件抬到 54px、`prose` 行高 1.85；窄档再加一条**分布**（版面根撑到一屏高 ＋ 正文列 `space-between`）。**一个字的内容都没编**——只用行距／块距／内距／可点件高度铺 |
| **③ 色彩锚点密度** | 同上 `densityCss`（全族） | 胶囊与瓦片统一吃到同一个暖色基（`CHART_PALETTE` 的橙／黄派生浅底），再按 `nth-child` 轮一圈**蓝→橙→青**点缀色（胶囊 6 条 ＋ 瓦片 6 条，共 12 条，出处＝第 21 格「三色胶囊矩阵」那条做法） |
| 转出 | `src/render/index.ts:11-17` | 转出 `SCENE_FAMILIES`／`SceneFamily`／`renderSceneBand`／`sceneBandClass`／`sceneBandCss`／`sceneFamilyCss`／`renderSceneShell`／`SceneShellInput` |
| 包内守门 | `test/skin-873.test.mjs` | 25 条（新增 7 条：三族各一条带且无 `<img>`／无 `loading`／无可见文本／带 `aria-hidden`；族名闭集外即抛；带的样式全在根类下且高度 ≥40px；三段合成后仍只作用于根类；回执族带填满度而另两族不带；三族都有 12 条轮转点缀色），并把 10 处接线的断言从 `chefSceneCss(` 改成 `renderSceneShell(` ＋ 一个合法族名 |

**族怎么分（按域一个值，与派单「能按域或按族切换」同口径）**：result＝查看／搜索筛选／历史／采购；
process＝做菜／开始使用；receipt＝录入／修改／派生／数据管理。

**中途撤掉的一版**：`receipt` 族原有一条「绿底白勾印记」，复评读出「与功能无关的圆形应用标记，喧宾夺主」
（形状撞上了别的品牌）⇒ 换成**白纸回执 ＋ 笔**。撤的那一版读数见 §六.3 的 27 号记录（79 → 83 里含这一改）。

## 六.2 逐页销账（同会话两跑，本轮前 → 本轮后）

判读面：本轮前＝`.scratch/t873/shots/<NN>-…-{390v,1280v}.png`（第二轮的终批图）；本轮后＝
`.scratch/t873-公共层/批/shots/` 同一对图。问句逐字＝派单册 §二，每页一次调用、两张图、不带 `json: true`。

| 格 | 页名 | 族 | 本轮前 | 本轮后（两跑） | Δ | 判官本轮后原话里还差什么 |
|---|---|---|---|---|---|---|
| 21 | 按时间筛选：三十分钟内 | result | **76／76** | **87／87** | **+11** | 桌面端条件卡左侧留白；库里正文那段「色泽红亮…米饭杀手」两处叠说（**文案，域席活**） |
| 12 | 辣椒炒肉（只看营养） | result | 87 | **85／85** | −2 | 「只吃 4 克／450g」同质化重复；营养卡偏密（域席活） |
| 45 | 首次使用 | process | 83 | **82／82** | −1 | 三颗按钮文案句式叠说；桌面三块留白偏宽（域席活） |
| 35 | 生成清单 | result | 78 | **79／79** | +1 | 顶部 4 张卡在 390 排两行；「用量」列中英混排（域席活） |
| 27 | 修改食谱主信息 回执 | receipt | 79 | **83／83** | +4 | 桌面卡片贴左、装饰略多于信息密度（域席活） |
| 47 | 批量改回执 | receipt | 77 | **82／82** | +5 | 「批量改回执／改动说明／复制数据」表述啰嗦（域席活） |
| 42 | 记派生关系回执（**对照页**） | receipt | 83 | **83／83** | 0 | 桌面首屏底部留白偏多（域席活） |

**两跑都逐字相同**（本会话 7 页 × 2 跑 ＝ 14 次调用，每一次的第二跑与第一跑 JSON 逐字一致）；
另有一条更强的确定性证据：第 21 格的**旧图**在两次调用里也都是 **76/76**。
⇒ 本会话的尺子是**确定性**的，「−1／−2」不是采样抖动。

**没达标页还差在哪（逐页点名，全部落在域席写集里）**：

1. **七页无一 ≥90**，差的**不是装饰带也不是填满度**——判官这一轮的原话里，扣分点**七页有七页**落在
   「文案叠说」（第 21／12／27／42／47 页）与「宽档留白／卡片贴左／栅格不铺满」（第 21／27／35／45／42 页）两处，
   两处都**不在公共层写集里**：前者要改页面装配的文案（域席），后者是各域自己的宽档栅格（域席）。
2. **第 12／45 格各 −1／−2**：两页都不在回执族（节奏一字未动），掉的是胶囊／瓦片那 12 条轮转点缀色
   带来的「色板稍乱／英文图标点缀可再克制」。这两页的判词同时点名「按钮文案冗余」「桌面留白过宽」——
   与第 21 格的 +11 同一批改动、方向相反，属**页内文案与栅格的既有缺口被放大**，不是本轮新增缺陷。
3. **第 33／45 格的首屏 gap 仍是 185／171px**（探针读数，见 §六.4）：两页分属 result 与 process 族，
   本轮**没有**给这两族加填满度——因为第 21 格（92）与第 45 格（90）都在这两族里，是票面的守线页。
   要补这两页，正确做法是**域席先确认守线页不会掉**，再把 `receiptFillCss` 那一段按族放开。

## 六.3 反例（改坏必红／还原一致）

**反例做法**：把本轮那条**族级装饰带**撤掉——`renderSceneShell` 里把 `const band = renderSceneBand(input.family)`
临时改成 `const band = ''`（其余一字不改），重出产物批到 `.scratch/t873-公共层/反例批/` 并出图；
然后逐字还原，重编 ＋ 重出 ＋ 重跑读数面，再复评同一页。

| 步 | 页 | 读数 | 判 |
|---|---|---|---|
| 撤掉族级装饰带 | 47 批量改回执 | 16/14/12/15/17 = **75** | **红**（本轮后 82 → 75） |
| 撤掉族级装饰带 | 21 按时间筛选 | 18/17/16/17/18 = **86** | 未红（87 → 86；该页判词扣在桌面留白与文案，与带无关） |
| 逐字还原 | 47 批量改回执 | 16/18/17/15/16 = **82** | **绿**（与改坏前**逐字相同**的 JSON） |

## 六.4 机器面

### 4.1 门禁运行记录（runId 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=cbaffd84-b96a-4086-86bf-899fe218c803 cmd="node node_modules/typescript/bin/tsc -b packages/skill-chef" exit=0
GATE-RUN runId=81fab81e-24d2-497d-9ecd-f9fccafcfc08 cmd="node --test packages/skill-chef/test/skin-873.test.mjs" exit=0
GATE-RUN runId=7e5fde4e-c224-4123-b8ba-e6554db5c37b cmd="pwsh -NoProfile -File .scratch/t873-work/regen.ps1 .scratch/t873-公共层/批" exit=0
GATE-RUN runId=11e095e6-a7b6-4e7a-83d9-e2ed8239b738 cmd="pwsh -NoProfile -File .scratch/t873-work/walls.ps1 .scratch/t873-公共层/批" exit=0
GATE-RUN runId=edf45a36-ac6c-4cd8-bb4b-4a25f5f9bca0 cmd="node node_modules/typescript/bin/tsc -b packages/skill-chef" exit=0
GATE-RUN runId=383d9a90-2737-40e2-b72c-b2df42408d5b cmd="pwsh -NoProfile -File .scratch/t873-work/regen.ps1 .scratch/t873-公共层/反例批" exit=0
GATE-RUN runId=37f9b0e3-9b0c-4a55-8f56-15e6ee9305b4 cmd="node docs/skills/skill-chef/t778-截图.mjs .scratch/t873-公共层/反例批 .scratch/t873-公共层/反例批/shots --viewport-only" exit=0
```

（第 5–7 行＝§六.3 反例那一轮：`band` 撤掉后的编译、那一轮的重出与出图。
同 ticket 的其余条目是迭代期的中间版本轮次，按 §2.4 第 3 条不计入对账。）

### 4.2 首屏填满度探针（本轮新建，`Δ` 是同一把尺量的 48 页，不是抽样）

```
node .scratch/t873-公共层/填满度探针.mjs <批目录> --json <路径>
判据：390×820，取版面根内所有文本片段里起点在折线以上的那些，gap = 820 − max(文本底边)
本轮前（.scratch/t873）：量到 48／48 页；gap 均值 49.6；区间 0–355
本轮后（.scratch/t873-公共层/批）：量到 48／48 页；gap 均值 26.9；区间 0–185
逐页（本轮前 → 本轮后）：42 355→110｜44 355→110｜48 244→105｜33 229→185｜47 224→85｜45 215→171｜
                          30 195→38｜43 174→73｜31 108→64｜27 75→47｜其余多在 0–45
```

**本轮给「合理区间」的口径**：390×820 首屏，正文最底一行距视口底 **≤120px**（≈视口的 15%）。
回执族 11 页里 9 页落在 ≤110px；**未达标的 4 页**：33（185）／45（171）＝另两族**本轮不动**（守线页所在族），
47（85）／48（105）已达标。第 42／44 格（110）＝全批最短的两页，再往下铺就是「给空白而不是给内容」，
按票面「不许编内容」停在 110。

### 4.3 质量门（`.scratch/t873-公共层/批/t778-质量门.json`）

```
汇总：横向溢出 0／触摸目标 1／触屏三件 0／分隔符懒政 0／英文裸词 1／重复句 0（页 × 变体 53 行）   exit=1
红行仅一条：HELP/私家大厨_HELP_验收副本.html => 触摸目标::100 件可点，最短边 14px | 英文裸词::1 处
```

**仍是改前就有的既有红**（#871 已记档、HELP 不上墙），**48 个域页那 48 行六列全绿**——
本轮的装饰带是**内联 SVG ＋ `aria-hidden` ＋ 零可见文本**，没有往页面上加一个字，
所以「重复句／英文裸词／分隔符」三列一格未动。

### 4.4 验收墙与扫描（两条都必须 exit 0）

```
自检：墙格 48／册子 48／不进墙 0；手机墙-390.html 97 ＋ 总索引.html 50；质量门 六列红全 0；册子 manifest.json
出墙：48 格；链接 147 条；缺失 0 -> 可发     exit=0
自检：墙格 48／册子 48／不进墙 0；桌面墙-1280.html 97 ＋ 总索引.html 51；质量门 六列红全 0；册子 manifest.json
出墙：48 格；链接 148 条；缺失 0 -> 可发     exit=0
扫 53 件产物；三类字样命中 0 处 -> 干净       exit=0
```

### 4.5 结构纪律

| 件 | LF | 结论 |
|---|---|---|
| `src/render/skin.ts` | 295 | 未超线（第一轮那段一字未动） |
| `src/render/sceneBand.ts` | 190 | 新件，未超线 |
| `src/render/sceneRhythm.ts` | 155 | 新件，未超线 |
| `src/render/sceneShell.ts` | 61 | 新件，未超线 |

**必报五步对账**：第一步报过「`src/render/**` 三件新件 ＋ `index.ts` 转出 ＋ 那 10 处页壳各一处 ＋
`test/**` ＋ 本证据件」；实际碰到的一致，**偏差 0**。第三轮**没有**动任何域模块的页面装配逻辑，
也没有动别的技能的路径。

## 六.5 未做项与下一手缺什么（第三轮）

1. **48 页的读数仍只有 7 页是本席量的**（同会话两跑）；其余 41 页的复评交收口席。
2. **两条带并存（本席最大的未决项，需域席或收口席收口）**：本轮的族级带落在**页头**（满宽、器物剪影），
   而第二轮各域自建的那条带仍在**页内**（`t771-run-search.mjs` 的 `.ilife-block-search-art`、
   `view/page-section.ts` 的 `.chef-view-band`、`data/pages.ts` 的 `chef-data-figure`、
   `add/pages.ts` 的 `ADD_ICONS_CSS`、`cook/page-art.ts` 的 `.ilife-cook-band`、
   `update`／`relation`／`history`／`shopping`／`setup` 各自的器物件）。**它们不在本席写集里，本席一行没碰。**
   两轮下来「一条带各写一遍」正是这轮要收的那笔账；下一手要的是：**各域把自己的那条带删掉、族级切换收进本席这一件**
   （删完每页只剩一条，且三族风格才真正合一）。**本席没有替它们删**——那会静默毁掉别席的交付物，
   而且其中 `history` 的 SVG 评分圆环、`update` 的「改前→改后」对照带**载着真数据**，不是纯装饰，不能一律删。
3. **第 33／45 两页的首屏 gap 未收**（185／171px）：要放开另两族的填满度，先得有人证明守线页 21／45 不会掉
   （本会话里这两页的「本轮前」是 76／83，都不到 90，判据本身也悬）。
4. **宽档（1280）的留白与卡片贴左**是判官这一轮反复点的那一条（21／27／35／42／45 五页），
   它住在**各域自己的宽档栅格**里（本席写集外）。公共层能做的只有「页头三级与正文列同宽」那一条（`pageUi` 已有），
   再往下就是各域的事。
5. **口径升级提示（给域席）**：`extraCss` 的拼装从本轮起由 `renderSceneShell()` 统一承担——
   域里只需给 `family` 和自己的 `extraCss`，**不要再自己拼 `renderDocShell`**（`test/skin-873.test.mjs` 会红）。
