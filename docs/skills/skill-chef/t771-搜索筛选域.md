# t771 搜索筛选域：13 卡读数 ＋ 路由修复对照 ＋ 审查记录

票 #771（父图 #765 票 6）。复跑：`node tooling/run-locked.mjs --ticket 771 -- node docs/skills/skill-chef/t771-run-search.mjs`（`--check` 加片段与 vision 判据，`--shots` 出双端截图）。

数据只走 #840 沙箱器械：副本 `.scratch/t771/chef_data.db`（294,912 B，真库 mtime 未动）；不与其他票共用副本。真库单菜谱（辣椒炒肉：湘菜／快手菜／18 分钟／已做），13 卡参数全部取该菜能命中的值，空结果仍走 `CHEF_EMPTY_RESULT` 缺失阻断（`{ q: "不存在的菜xxx" }` 实测 exit 4）。

## 一、13 行读数（正例全绿）

| 卡 | 参数 | kind | 总数 | 产物字节 |
|---|---|---|---|---|
| search_by_name_keyword | `{"q":"辣椒"}` | search:辣椒 | 1 | 89864 |
| search_fuzzy_match | `{"q":"辣椒炒内"}` | search-fuzzy:辣椒炒内 | 1 | 89878 |
| filter_cuisine_basic | `{"cuisine":"湘菜"}` | filter:cuisine | 1 | 89832 |
| filter_combined | `{"cuisine":"湘菜","maxTime":30,"flavor":"辣"}` | filter:cuisine,flavor,maxTime | 1 | 89917 |
| filter_by_ingredient_basic | `{"ingredient":"五花肉"}` | filter:ingredient | 1 | 89862 |
| filter_exclude_ingredient | `{"ingredient_exclude":"虾仁"}` | filter:ingredient_exclude | 1 | 89861 |
| filter_difficulty_easy | `{"difficulty":"快手菜"}` | filter:difficulty | 1 | 89840 |
| filter_time_quick | `{"maxTime":30}` | filter:maxTime | 1 | 89859 |
| filter_by_cookware | `{"cookware":"炒锅"}` | filter:cookware | 1 | 89836 |
| filter_by_flavor | `{"flavor":"辣"}` | filter:flavor | 1 | 89828 |
| filter_by_season | `{"season":"夏"}` | filter:season | 1 | 89834 |
| filter_by_status | `{"status":"已做"}` | filter:status | 1 | 89822 |
| list_all_recipes | `{"kind":"all"}` | all | 1 | 89828 |

产物：`.scratch/t771/<卡 id>.html`（13 页）＋ 双端截图 26 张（390／1280）＋ `t771-册子片段.json`（13 行：卡 id／唤醒词／命令／参数／产物绝对路径／exit／bytes／sha256，供收口 A 合并，不直接改共用册子）。

正例：`缺卡 0`、`路由错位 0`，exit 0。反例（已跑并还原）：把 `filterRecipes` 里 flavor 查询改查 `recipe_categories` → 脚本报 `路由错位 1（筛选口味未走 flavor）` 并 exit 1；改回即绿。

## 二、路由错位修复前后对照

| 唤醒词 | 修前 | 修后 |
|---|---|---|
| 筛选菜系 | `filter` → cuisine | 不变（`filter` 别名保留，`{"filter":"湘菜"}` 与 `{"cuisine":"湘菜"}` 同命中；既有测试与 HELP 示例对齐） |
| 筛选食材 | `filter` → cuisine（`{"filter":"虾"}` 按菜系查，必 miss） | 显式键 `ingredient`（`exists ingredients.name`，新增）；路由声明示例改为 `{"ingredient":"五花肉"}` |
| 筛选口味 | `filter` → cuisine（`{"filter":"辣"}` 按菜系查，exit 4） | 显式键 `flavor`（既有）；路由声明示例改为 `{"flavor":"辣"}` |
| 筛选季节 | `filter` → cuisine | 显式键 `season`（既有）；路由声明示例改为 `{"season":"夏"}` |

另补三条口径（老件 `scenes/搜索筛选.yaml` 为准）：排除食材 `ingredient_exclude`（别名 `exclude_ingredient`／`exclude`，NOT 条件，老件 search-6）；组合至多 3 维（超限 exit 2 并点名维数，老件 search-4；4 维实测已拦）；错字模糊精确命中直接返、无结果去尾一字再查、命中记 `search-fuzzy:<原词>` 并印纠错提示、仍无结果如实抛（字典／拼音表另立票）。

`WAKE_TABLE`（`src/policy`，共用位）本次未动：写集纪律要求域票只碰自家目录；该表 `needs: ["filter"]` 的槽位收窄留给 #841 说明面同步时统一翻，命令层显式键已全通， runner 走显式键。

## 三、代码层 UI 审查清单（票 3 交的五条，逐条结论）

读数出自质量门「代码层读数」一节（13 页一致）：产物样式块 1／内联 style 0 处／活色值 29（`#1d1d1f` 等，全部来自公共层 `buildStyleSheet`＋`blocksCss`＋`pageUiCss`＋`pageShapeCss`，本票 `extraCss` 之外无手写样式）／字号档 20（公共层标尺）／非 `ilife-` 类名仅 `wrap`（文档壳根类，随 `renderDocShell` 来）／高频重复声明 59（公共层样式本体）。

- 硬编码样式：无。本票未写一条样式规则，唯一 `extraCss` 是 `pageUiCss() + pageShapeCss()` 两个公共层函数。
- 内联样式：0 处（门实测）。
- 字号与间距标尺：走公共层标尺（字号档 20 档全部来自公共层，无字面量字号）。
- 组件复用：每格一次区块调用（`renderPageShell`／`renderFactStrip`／`renderConclusionBar`／`renderChipRow`／`renderProseBlock`（#860 已落地）／`renderDisclosure`／`renderCaliberLine`／`renderCopyBlock` 经 `renderDocShell`（`pageUi` 开）装配）。
- 重复样式块：无新增（产物样式块 1 即文档壳自带那一份）。

## 四、过程性 vision 审查（缺陷当场改，不拖到收口票）

抽样 4 页（模糊／组合／排除／全部）过视觉模型，模型打分偏低（57–64，主因是静态单文件约束下无插画动效、 chip 与状态未做特形——三项分别撞 #768 不引外部资源、配方 §5-4 已裁不引入 emoji、配方 §2 形状，既有裁定在先，不采纳，逐条 grounds 见 `.scratch/t771/vision.json` 的 rejected）。

当场改掉 3 条（已重跑门与截图）：模糊卡双结论条堆叠 → 只留纠错提示一条；通用结论条与事实条重复计数 → 去计数；纠错提示括号啰嗦 → 照老件 search-2 原文精简为「你是不是想找：辣椒炒肉」。门六列改前后两次全绿（13 页 × 390／1280 真浏览器）。

`--check` 读数：片段行数＝13（本票卡数 13），vision 审查缺陷 0（3 条已改）。

## 五、回归与写集对账

- 本包测试：`policy` 5／5、`fetch` 9／9、`cli` 6／6（含 `filter: 川菜` 别名、`cuisine`＋`difficulty`、`maxTime` 全 exit 0）、`scene-data` 全过（含待开发 14 张不断言变化——本票未动 HELP 资产，翻徽章归 #841）、`skill` 全过；`render` 36／37，唯一红是「键表 8 vs 12」——他席在途改动（工作区 `src/render/envelope.ts`、`src/cli/keys.ts` 已被改出 12 键，HEAD 为 8），本票未碰命令声明与信封（`git diff --name-only -- packages/skill-chef/src/search/` 仅两件），与本票无关。
- 结构纪律：影响清单（`src/search/run.ts` 取数与口径、`src/search/routes.ts` 三行示例）与交付一致，无偏差；两件 LF 为 124／64（告警线 350，未超线）；无新增目录、无新增对外接口（`index.ts` 未动，页装配住 docs 运行器，复用 t768 原型先例）；共用位（`policy`／`cli`／`fetch`／`render`）一行未碰。
- 标签口径：结果卡徽章行＝六张关联表聚合为一行 `renderChipRow`（换行不横滚），与 #770 共用同一条口径（同一批关联表、同一套徽章形状）；#770 实现时请复用此形状。
- HELP 待开发徽章：本票 4 卡（难度／时间／炊具／状态）数据与页面已跑通，徽章翻可用归 #841（本票未动 `sceneData.ts` 与 `WAKE_TABLE`，`WAKE_TABLE` 仍 37 条，`policy` 测试作证）。
