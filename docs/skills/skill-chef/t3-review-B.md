# t3 对抗式审查（B：可施工性与下游风险）

> 审查对象：`docs/skills/skill-chef/t3-template-contract.md`（61,994 B／475 行；LF／无 BOM，实测 `CRLF=0 bareLF=475`）。
> 审查日 2026-09-12。基准＝地图 `docs/skills/skill-chef/map-chef-body.md` 的目的地与判定口径。
> 只读审查：未改任何源码，未 `git add`／`commit`／`checkout`／`stash`，未跑 `gen:help-shell`，未跑仓级 build／test，未跑 `chef-cmd-read`，未联网。实验件 `.scratch/chef-help/t3-review-B/`。
> 角色分工：A 核「报告引用的行号与字段对不对」；本报告核「照这份报告，票 6 能不能一次干成、干出来会不会被判不过、老骨架 10 域／33 组／48 场景会不会做丢」。

## 判定

- **总分：5/10**（可施工性 35% ＝ 1.5/3.5；内容不丢 30% ＝ 1.5/3；新规合规 20% ＝ 1/2；下游风险揭示 15% ＝ 1/1.5）
- **一句话结论**：契约层的**事实**做得很扎实（A 路 5＋4 字段的渲染落点、两层→三层的模板硬约束、发布态缺口都钉住了），但作为票 6 的施工图**缺三样东西**——五项必填里三项仍留三支没人选、`dimensions`／`{{菜名}}` 的落法在三张票之间**没有归属**、页面结构预期没有一处成文（票 11 没有验收基准）；同时**失位**（没把自己定位成票 6 的第一／二步）与**越界**（把票 6／票 7 已写死的口径重新列为待选）。
- **硬伤条数：8 条**（§二）。

---

## 一、逐条审查（对应 10 问）

| # | 审查问题 | 结论 | 依据（路径:行号／命令输出） | 严重度 |
| --- | --- | --- | --- | --- |
| 1 | 可施工性：五项必填有没有缺料 | **不合格**。只有 `skill_name` 给了确定值；`title`／`contact` 各列 3 支归票 6；`subtitle` 的计数口径（含不含 33）明确留空；`groups` 有素材（10 域／33 组／48 卡）但**中间层归属未定**（§4.3 甲／乙／丙）。**5 项里 4 项留白** | 报告 `:16`（"只能照邻居"）、`:147`（title 三支）、`:148`（subtitle 口径）、`:149`（contact 无老家对应物）、`:415-424`（B 段 8 项待选） | 高 |
| 2 | 三块可选（`meta_blocks`／`version`／`init_banner`） | **部分不合格**。不渲染这条**说得准**，且**后果说到位了**（`:229`「派生只影响 `help-data` 载荷与外部消费者」）。但没把「传＝白做」推到底：**A 路产出的 HTML 里，传与不传逐字节相同**（唯一差别在 `help-data` 载荷段），故"照记账传／照卡路里不传"在本图**对产物不可观测**，这一句报告没写。并且票面口径（地图 Notes 的"三块可选内容"）与本报告结论冲突时该就地改票面，报告也没说 | 报告 `:161-163`（实测哨兵静态段 0 次）、`:229`；`.scratch/chef-help/t3/probe_render.stdout.txt`（`META-*` 静态段 0／载荷段 1） | 中 |
| 3 | 两层→三层：选项集完整性与是否与用户定案冲突 | **合格**（本问报告做对了）。丁与地图 Q2 冲突，报告自己标了「与本图既定口径冲突，列出仅供对照」（`:221`）。甲／乙／丙都在，且事实前提够硬（`:209`「33 组每组只落一个域」）。**唯一缺口**：报告只按 `html.template` 目录名反推域（`:212` 自己标了边界），**没有引用同图票 2 的 10 域中英名对照表**（`docs/skills/skill-chef/t2-content-reconcile.md:19-47`，那里有 5 处独立出处与 `domain.key` 英文名），故选项集少了「域 id／label／icon 从哪来」这一支 | 报告 `:212`／`:216-221`；`t2-content-reconcile.md:13`（`domain.key`＝权威英文名） | 中 |
| 4 | 48 条内容会不会做丢 | **不合格（本图最可能翻车处）**。5 项逐项核对见 §三：`aliases` 系 3 项说清了（都是"不迁"，有出处）；**`dimensions` 只有一句"归票 5"，且与票 2 已定稿的 6 键资产草案冲突**（`t2-content-reconcile.md:396`／`:400-410`：草案场景 6 键、映射表里没有 dimensions）→ 内容会静默丢；**`{{菜名}}` 的字符形状报告写错了层级**（老载荷是双花括号 `{{菜名}}`，报告与探针都按单花括号记，见 §三） | 报告 `:404`／`:428`；`t2-content-reconcile.md:396`／`:407`／`:410`；`.scratch/chef-help/t3-review-B/keys.py` 输出 | 高 |
| 5 | `init_banner` 与「跑完不建库」 | **不合格（失位）**。事实全对（`paths.ts:21` mkdir／`db.ts` 开库建表／`cmd_read.ts:98-99` 在 `switch` 前就 `openChefDb`，已复核）。但它把这条列成 `:325` **「归票 6」的三支可选口径**——而**票 6 与票 7 的票面早已各自写死**：不建库是票 7 的行（`t7-body.md:8`「该命令在**开库之前**分派（跑完不建 `.db`）」），票 6 的票面也点名（`t6-body.md`：只读页不许调 `openChefDb` 一类会 DDL 自愈建库的入口）。报告既没把这些既有票面接上，也没指出票 6 要让出口**纯函数**（`initialized` 由调用方传入，照 `packages/skill-bill/src/render/helpFile.ts:84-87`）。对目的地第 1 条（明确拿到 HELP 文件）而言，今天跑一次 `chef.help.lookup` 会顺手建出 `chef_data.db`＋DDL，这正是「看帮助把库建出来」那种副作用 | 报告 `:313-325`；`packages/skill-chef/src/fetch/paths.ts:20-23`；`packages/skill-chef/src/cli/cmd_read.ts:97-102`；`docs/skills/skill-chef/t7-body.md:8`；`docs/skills/skill-chef/t6-body.md` | 高 |
| 6 | 告警线（`packages/skill-chef/AGENTS.md` ＋ 350＋LF） | **不合格（漏项）**。全文 grep `AGENTS.md` 只命中两处 CRLF 语境，**没有一个字**提 350、告警线、或 `packages/skill-chef/AGENTS.md`（实测 `Test-Path` ＝ False，该文件今天不存在）。用户 Q4b 原话指定的落点，报告未覆盖。实测两件已超线、且我两次读到不同数字（并发改动中）：`src/cli/cmd_read.ts` LF 388、`src/fetch/db.ts` LF 451 | `Select-String -Path 'docs/skills/skill-chef/t3-template-contract.md' -Pattern 'AGENTS\.md|告警线|350|LF'` 仅 2 命中且均无关；`map-chef-body.md:53`／`:156`；LF 实测 | 中 |
| 7 | 必报五步：失位与越界 | **不合格（两种都有）**。①**失位**：全文 grep `五步｜影响清单｜结构设计｜structure.md｜铁律` ＝ **0 命中**，报告没把自己定位成"票 6 的第一／二步还没做"。②**越界方向反了**：该列选项的地方（三层落法）列得对，但**票面已钉死的两处（不建库、五步报点）被它重新降级成待选**。③票 6 票面点名要报的东西（新增件住哪个能力目录、每个文件公开接口给什么、共用件被哪两个能力用）报告**一项都没给**——而这正是第一步／第二步的正文 | 报告全篇（grep 0 命中）；`docs/agents/structure.md:72-88`；`docs/skills/skill-chef/t6-body.md` | 高 |
| 8 | 发布态归位 | **合格**。事实与归位都正确：仓内 `packages/base-render/package.json:11` 有 `./help-shell`，发布态缺（`3e46ab0` 那次发版不含），并明确交回发版票、不在本图修（`:371`）。**未钉死那一条它自己标了**（`:373` 本机不联网无法读 registry）——这是诚实的写法 | 报告 `:367-373`；`map-chef-body.md:102`（Out of scope 已划出图外） | 低 |
| 9 | 肉眼终审风险：页面结构预期 | **不合格（票 11 会没有基准）**。报告把"页面长什么样"的信息**打散在至少 6 处**（`:146-152` 字段落点、`:175` 横幅位、`:197-199` 三层、`:152` 徽章、`:168` 关于页、`:381` 搜索面），**没有任何一处把整页拼出来**；也没有字面写出 **11 个 Tab**、每 Tab 下面是什么、点开是什么。它只在 `:124` 给了字节数（整页 107,128 B／载荷 1,298 B）与 `:122` 的一行模板自带文案统计。票 11 的验收要"肉眼比对"，而地图明写老件是两层、无 tab、无关于页（`map-chef-body.md:40`）⇒ 维护者**只能凭临场观感判** | 报告 `:146-152`／`:168`／`:175`／`:197-199`；`packages/base-render/assets/help-template.html:1772-1832`（实测结构） | 高 |
| 10 | 给票 6 的最小整改清单 | 见 §六（6 条） | — | — |

---

## 二、硬伤（会让票 6 做错或让维护者判「不过」的）

1. **`dimensions`（46/48 卡，键并集 42＝合法 41＋1 个畸形键）在三张票之间没有归属。** 报告只说"归票 5"（`:404`），而票 5 的上游已有一份定稿的资产草案：`t2-content-reconcile.md:396` 明写"场景 6 键 = `id`／`title`／`wake_word`／`types`／`status`／`prompt_template`"，`:400-410` 的映射表**没有 `dimensions` 这一行**。照那条走，`dimensions` 在上游就不入库，票 6 拿不到也没权造——**最高频的 `recipe` 23／`input` 6／`focus` 4／`scope` 3 静默消失**。老件里它对应的正是提示词的填写位（`recipe: 指定菜名`），维护者一眼能看出卡上少了参数。
2. **`{{菜名}}` 的字符形状报告写的是错的层级。** 老载荷原文是**双**花括号：`"prompt": "帮我做一道{{菜名}},我要按步骤来。"`（`.scratch/chef-help/legacy-chef-help-payload.json:22`，另有 `{{N}}`）。报告与它自己的探针按**单**花括号记（报告 `:401`「`{菜名}` ×15」；`probe_prompt.stdout.txt` 的 `placeholder_names` 只有 `菜名`／`N`——正则 `\{[^}]*\}` 吃掉了外层的 `{`）。A 路不做任何替换（`:393` 说对了），所以**复制出去的正文是字面的 `{{菜名}}`**，不是 `{菜名}`。照报告写文案或写断言，两边都会错。
3. **`types` 的取值口径两份同图报告互相冲突，施工图上没有裁决。** 报告 `:403`／`:429` 把"带括号的限定词（`(过程型)`／`(下载)`）怎么处理"留成待选；票 2 已给死口径：`t2-content-reconcile.md:405`「按 `+` 拆开、**去掉「(过程型)」这类括号注**」，并在 `:427-431` 逐条落成 `["向导","选择","回执"]`。两文并存，票 6 会先撞上口径分叉。
4. **`init_banner` 的显隐口径被错记成"票 6 的三支选择"，与票 7 票面直接打架。** `t7-body.md:8` 已写「该命令在**开库之前**分派（跑完不建 `.db`）」；`t6-body.md` 也点名不许调 `openChefDb`。报告 `:325` 的三支里，"丙·复用 `openChefDb` 的 `initialized`"是**票面已禁**的那一支，报告却把它平列成可行项。
5. **页面结构预期全篇无一处成文 ⇒ 票 11 的肉眼终审没有基准。** 见一问第 9 条。
6. **必报五步 0 命中，且报告不是"票 6 的第一步输入"的姿态。** 报告没报：票 6 要新增哪个能力目录（目录名取自 HELP 一级分组）、新增件叫什么、每个文件公开接口导出几个、共用件被哪两个能力用。`docs/agents/structure.md:72-88` 与票 6 票面都点名要这些。
7. **告警线整项漏掉。** 用户 Q4b 原话指定的 `packages/skill-chef/AGENTS.md` ＋ 350＋LF 口径，报告一个字没提；实测该文件今天不存在（`Test-Path` 全 `packages/*/AGENTS.md` 皆 False），而 `src/cli/cmd_read.ts` LF 388／`src/fetch/db.ts` LF 451 已双超线（地图记的是约 371／约 433，两个件都在并发改动中——票 6 写代码时会当场撞上第四步）。
8. **`t3` 没有与同图票 2 的报告对齐，留下三处接口缝。** ①域层的权威英文名在 `t2-content-reconcile.md:13`（`domain.key`＝`cook`／`view`／`search`…），报告只按模板目录名反推且自己标了"只有 10 条在磁盘上存在"（`:212`）；②票 2 的 10 域中英名表（`:23-34`）就是票 6／票 7 的目录名来源，报告没接；③票 2 已把 `{{菜名}}` 逐字保留进资产草案（`:407`），报告却把参数化整包推给票 5。

---

## 三、内容不丢核对表

对照老件 11 个键（`.scratch/chef-help/t3-review-B/keys.py` 实测：`scenario_id`／`scenario_title`／`dimensions`／`prompt`／`status`／`result`／`wake_word`／`type`／`html`／`variants` 各 48，`domain` 13）逐项核对报告的落法。

| 项 | 老件实况（本席实测） | 契约里的字段位 | 报告的落法／缺口 | 判定 |
| --- | --- | --- | --- | --- |
| `dimensions` | 46/48 卡有；键并集 **42**（合法 41 ＋ 污染键 `默认不含)`），`recipe` 23／`input` 6／`focus` 4／`scope` 3 | **无**（契约场景 6 键里没有）→ 只能转 `editable_fields` | 报告 `:404`：「A 路没有 `dimensions` 字段位…转成表单字段还要给 `label`（中文）与 `required` —— **归票 5**」 | **缺口（硬伤 1）**：票 2 的资产草案 `:396`／`:400-410` 已不含 `dimensions`，票 5 是"照草案入库"，没有任何一票被指派做这条转换 |
| 4 条别名 | `开始做菜`／`不想要`／`删掉`／`废弃`，挂在 `做菜模式`／`废弃食谱` 两组 | 无（`Scene.wake_word` 是**单个**字符串） | 报告 `:430` 明写「含老件 4 条别名…这是票 2 的对账表」，且票 2 已裁「契约无字段位，草案不含」（`t2-content-reconcile.md:410`） | **说清了（不迁＋有出处）**；但报告**没写"产物上会少 4 条词"**，而老件的 Hero 数字 37 正是 33＋4 |
| `aliases_expanded_count=37` | 顶层键，老页面 Hero「唤醒词」数字来源 | 无 | 报告 `:430` 同上一并列出，归票 2 对账 | **说清了（不迁）**；副作用没写：新 Hero 的计数是 **`ALL.length`＝48 场景**（不是 37 唤醒词、也不是 33 组），这个数字替换关系报告没点名 |
| `types`（11 型） | `type` 单数 11 种；切分后 10 个原子里 5 个不在模板配色表（`对比`／`确认`／`回执(过程型)`／`勾选(过程型)`／`转移(下载)`） | `types` 数组（模板只读复数） | 报告 `:403`：切法给出、表外 5 个"会退到查看蓝底"、括号注怎么处理**留成待选**（`:429`） | **说清了现象，留了口径**；与票 2 已定的"去括号注"（`t2-content-reconcile.md:405`）冲突（硬伤 3）。另：`type` 单数照搬会**静默无徽章**这条报告说得极准（`:135`），是本报告最有价值的一处 |
| 16 条 `{{…}}` 占位符 | 16/48：`{{菜名}}`×15、`{{N}}`×1（**双花括号**） | 无占位符机制；A 路不替换（`:393`），只能用 `editable_fields` | 报告 `:401`：说对了机制（"不带 `editable_fields` 就会把 `{菜名}` 原样复制"）、给对了票（票 5），但**字符形状写成单花括号**、**没点名"老件里 `editable_fields` 为 0 条"**（实测 0/48，等于全要新造） | **半缺口**：机制说清，事实形状有误（硬伤 2） |

**另外三项报告没覆盖、会被肉眼看见的内容**（都属"没说不迁、也没说怎么迁"）：

- `result`（48/48 有值，中文整句的"这个场景产出什么"）：契约 6 键里没有，**报告全文未提**。它是老卡片唯一承载"结果说明"的字段。
- `html.template`（18 个路径，10 域的来源）：报告只用它反推域，**没说这个字段本身新契约里没有字段位**。
- `variants`：载荷里 96 处全是空数组（`"variants": []` ×96 实测），无内容损失，但报告未提，属低风险。

---

## 四、可施工性缺口（票 6 会在哪里卡住）

1. **`title` 卡住**：三支（老家原文 `私家大厨 HELP · 能力速查`／邻居形状 `私家大厨 · 使用手册(HELP)`／新造）影响**文档标题＋hero 大字＋是否保留 🍳**，报告 `:294` 明确"归票 5／6"——票 6 手上没有裁决就不能落笔。
2. **`contact` 卡住**：老家 chef 无对应物（`:329` 实测 0 命中），报告给"三项／两项／不传"且地址指 `SKILLS` 还是 `ilife` 也未定（`:331`）。这三支会直接决定关于页第一段出不出现。
3. **`subtitle` 与 `version` 是一对，不能分头定**：照记账的派生式 `:100-103` 里嵌了 `版本 <version>`，而 `version` 又要在页面上以 `v<version> · HELP 模板 v4` 出现（`:168`）。报告把它们拆成两个待选项（`:418`／`:424`），票 6 会做出自相矛盾的两个数字。
4. **中间层（域→组）卡住**：`groups[].id` 用中文组名还是英文域 key、`subgroups[]` 用老 33 组还是别的，直接决定 **Tab 上写什么字**。报告 `:415-424` 把它列为待选，而票 2 已经给了英文 `domain.key`（`t2-content-reconcile.md:13`）。
5. **`init_banner` 卡住**：三支里有一支（复用 `openChefDb.initialized`）与票 7 票面的"不建库"冲突（硬伤 4）；且 `prompt` 的单源候选（老家「首次使用」那张卡的 `prompt`）在**票 6 的纯函数里读不到库状态**——状态必须由调用方给（照 `skill-bill/src/render/helpFile.ts:84-87` 的 `HelpFileOptions.initialized`），报告没写这条形状。
6. **`dimensions` / `{{菜名}}` 卡住**：见 §三（这两条是"票 6 想补也没有素材"的卡点，不是选择问题）。
7. **`types` 原子卡住**：现象有、口径无（硬伤 3），票 6 要么自己造切法（越权动票 5 的内容资产），要么照报告留蓝底（维护者会看到 5 个颜色不对的徽章）。
8. **第一步／第二步交不出**：报告没给"新增哪个能力目录、文件叫什么、公开接口导出几个、共用件被谁用"，票 6 按新规**必须先报这两步拿用户点头**，而现在没有可报的正文。
9. **附带的落盘冲突（报告未提）**：票 7 票面里写的落点仍是老目录 `<SKILLS_DB_PATH>/CookHub/help/`（`t7-body.md:5`），而地图已改判 `cook_html/help/`（`map-chef-body.md:31`／`:170`）。这条不是票 6 的活，但报告既然核到了 `resolveDbPath` 的 mkdir，顺手点一句更省事。

---

## 五、页面结构预期（按报告推 + 模板实测，供票 11 肉眼终审当基准）

**一句话**：**11 个底部 Tab**（10 个域 ＋ 末尾「关于」）＋ 顶部 hero（技能名小字／大标题／「48 场景」＋ 三步引导）＋ 搜索框 ＋ 每域一页（域内若干**折叠组**，每组里若干**场景卡**，点卡开**底部抽屉**）。

逐层（行号以 `packages/base-render/assets/help-template.html` 为准，本席实读）：

| 位置 | 会长成什么 | 字段来源 | 代码 |
| --- | --- | --- | --- |
| 文档标题 | `私家大厨 · <title>` 或原样（取决于 `composeDocTitle` 的 `includes` 判定） | `skill_name`＋`title` | `src/helpShell.ts:66-70` |
| hero 左上小字 | `私家大厨` | `skill_name` | `:1772` |
| hero 大字 ＋ hero 计数 | `<h1>`＝`title`；下方「**48 场景** · 点击卡片查看详情并复制指令」＋右上角徽章「48 场景」；计数**一律＝场景卡总数**（不是 37 唤醒词、不是 33 组） | `title`；`ALL.length` | `:1772` |
| hero 三步引导 | 🔍 找场景 → 📋 复制指令 → 💬 发给 AI（模板固定文案） | — | `:1773` |
| hero 与搜索框之间 | 「首次使用」横幅（**只有传了 `init_banner` 且 `hidden !== true` 才出现**；含标题／副标题／一枚复制按钮／✕） | `init_banner` | `:1775-1777` |
| 搜索框 | 一个搜索框，匹配**卡片可见文字**（含唤醒词 chip 与徽章文字） | — | `:1778`／`:2019` |
| 底部 Tab 条 | **11 个按钮**：10 个域（图标走 `SVG_ICONS[icon]`，不在表里就当 emoji）＋ 末尾「关于」；第 1 个默认点亮 | `groups[].id`／`label`／`icon` | `:1828-1833` |
| 每个域页 | 域内若干 `<details class="subgroup" open>` 折叠组，标题＝组名＋场景数角标（默认展开） | `groups[].subgroups[].label` | `:1782-1784` |
| 组内场景卡 | 一行一卡：唤醒词 chip（绿）＋类型徽章（按 10 词表配色，表外词退「查看」蓝）＋场景名＋「复制」按钮 | `wake_word`／`types`／`title` | `:1785-1793` |
| 点卡 | 底部抽屉：场景标题＋chip＋参数输入框（**只有 `editable_fields` 非空才有**）＋「Prompt 预览」＋复制；有参数时边填边重组预览 | `editable_fields`／`prompt_template` | `:1915-1942` |
| 关于 Tab | ①「联系作者」（`contact` 有才画，逐项 label＋值，`url && http` 开头才成链接；`copy_all` 再加「一键复制」）②「版本」＝`私家大厨` ＋ `v<version> · HELP 模板 v4`（**不判空，不传就显示 `v · HELP 模板 v4`**）③「其他技能」（`recommendations` 有才画） | `contact`／`version`／`recommendations` | `:1799-1826` |
| 域页默认顺序 | 按 `groups[]` 顺序，第 1 个域（做菜）默认选中 | `groups[]` 序 | `:1830` |

**与老件的结构性差异（终审时不该按老件判不过的点）**：老件是**单列一层手风琴**（33 组）＋ hero 三格统计（唤醒词 37／场景 48／待开发 0）（`D:\2Study\StudyNotes\SKILLS\私家大厨\templates\help.html:206-214`，19,938 B），**没有**底部 Tab、没有关于页、没有搜索框、没有底部抽屉、没有首次使用横幅。新页面**必然**多出这五样、并且**必然**多一层「域」。

---

## 六、最小整改清单（按性价比，≤6 条）

1. **补一节「页面结构预期」**（§五那张表搬过去，含"11 个 Tab＝10 域＋关于"这一句）。这是票 11 唯一的验收基准，也是让维护者知道"该看什么"的最短路径。性价比最高。
2. **把内容不丢的五个口子钉成"谁负责＋默认怎么走"**：`dimensions`（要么进 t5 资产草案做参数、要么正式记为"不迁"并在票面写清产物会少参数）、`{{菜名}}`（**改写成双花括号**并二选一：转 `editable_fields` 或明说不转、正文就长 `{{菜名}}`）、`types` 括号注（照 t2 已定的"去括号注"并写进报告）、`aliases`＋`aliases_expanded_count`（补一句"产物上 Hero 计数变成 48 场景"）、`result`（明说"不迁"）。任何一项不写，票 6 就静默丢。
3. **与同图票 2 报告对齐三处**：域英文名以 `t2-content-reconcile.md:13` 的 `domain.key` 为准（模板目录名只作旁证）；资产草案是 6 键（`t2-content-reconcile.md:396`）；`{{菜名}}` 在草案里是逐字保留（`:407`）。
4. **把"已钉死的"从"待选"里摘出来**：①不建库／不开 `openChefDb` → 事实改成"票 7 的票面已写死，且票 7 的落点要改成 `cook_html/help/`"；②`init_banner` 在票 6 侧只是"要不要传"，且 `initialized` 必须由调用方传入（零 IO 纯函数），不是票 6 自己判库。
5. **补票 6 的第一步第二三步输入**：新增件住哪个能力目录（10 域英文名之一／或 HELP 能力目录）＋每个文件公开接口导出几个＋共用件被谁用；并写明"本次要动 `packages/skill-chef/package.json` 加 `base-paint`＋移出 `SKILLS_BASE_FROZEN`"。
6. **补告警线一条**：`packages/skill-chef/AGENTS.md` 今天不存在、350＋LF 口径要随本图立；实测 `src/cli/cmd_read.ts` LF 388、`src/fetch/db.ts` LF 451 **已超线**，票 6 新建文件也会当场触发第四步——报告应把这条前置提醒（并发会话正在改这两件，数字会变，以开工时为准）。

**能否当施工图**：**需先补 6 条**（1／2 是硬门，3～6 是开工前必报项）。报告的事实层可放心引用；结论层（选项集）在补齐 1～4 之前不足以让票 6 一次干成。
