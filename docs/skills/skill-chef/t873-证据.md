# t873 收口证据：48 页重出产物批 ＋ 双端墙 ＋ 同尺两跑打分件（批级）

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**收口席**。
跑法一律经锁：`node tooling/run-locked.mjs --ticket 873 -- <命令>`。收口席本次运行标识
`runId=6ea9b65e-3812-47cd-96cd-c1b92b0b2977`（`waitedMs=0`、`exit=0`）。

## 一 一句话结论

公共层席 ＋ 9 个域席的改动**全部落地并提交**（13 个 commit，10 席）；收口席按票面「验收命令」重出正式批
（`.scratch/t873/`：53 件产物 ＋ 双端墙 ＋ 96 张双端首屏图），**两条批级判据都绿**（`--check` exit 0、终审扫描 exit 0），
质量门只剩一条**改前就在、不上墙**的 HELP 验收副本既有红；
48 页在**同一会话、同一把尺、同一批图上**做的改前／改后两跑给出：均值 **80.02 → 83.54（+3.52）**，
**≥90 从 0 页到 1 页**（第 42 格 92），升 34 格／平 4 格／降 10 格——**「48 页逐页 ≥90」这个目标改后仍未达成**，
未达成的算术原因只有一条：**「视觉生动」这一维被「库里没有图」封顶**（改后均值 15.25，48 格没有一格超过 18）。

## 二 三条批级读数（原样抄回）

命令：`node tooling/run-locked.mjs --ticket 873 -- pwsh -NoProfile -File .scratch/t873-work/accept.ps1`
（① 重出 48 页产物 ＋ 4 变体 ＋ HELP 造册进 `.scratch/t873/`；② 质量门 → 双端墙 → 首屏截图 → 终审扫描；③ `--check`；④ 终审扫描）。
全文日志 `.scratch/t873-复评/accept.log`。

```
===== ③ 批级判据一：验收墙自检 --check
48 格；链接 245 条；缺失 0 -> 可发
自检：墙格 48／册子 48／不进墙 0；手机墙-390.html 97 ＋ 桌面墙-1280.html 97 ＋ 总索引.html 51；质量门 横向溢出红 0／触摸目标红 0／触屏三件红 0／分隔符懒政红 0／英文裸词红 0／重复句红 0；册子 manifest.json
墙 --check exit=0
```

```
===== ④ 批级判据二：终审扫描
扫 53 件产物；三类字样命中 0 处

共 53 件；命中 0 -> 干净
扫描 exit=0
```

```
===== ② 质量门汇总行
汇总：横向溢出 0／触摸目标 1／触屏三件 0／分隔符懒政 0／英文裸词 1／重复句 0（页 × 变体 53 行）
exit=1
```

**质量门那条红是既有红，且不在墙上**：逐件读 `.scratch/t873/t778-质量门.json` 的 53 行，只有
`HELP/私家大厨_HELP_验收副本.html` 一行两列变红——`触摸目标::100 件可点，最短边 14px` 与 `英文裸词::1 处`（令牌 `HELP`，
出在「私家大厨 HELP · 能力速查」这一行）。**48 个域页 ＋ 4 个变体那 52 行六列全 `red:false`**；
两条墙的自检行也因此都是「质量门 红 0」。改造前 `#871` 已把这两条记成既有红，HELP 走的是另一条装配链
（`src/help/helpFile.ts` → `base-paint/help-shell`），本票 10 席一处都没碰它。**收口席照实记，不放宽也不改绿。**

两条墙的读数（同一次运行）：

```
出墙：D:\ilife\.scratch\t873\手机墙-390.html（48 格 × 390 宽，格高 820）
48 格；链接 147 条；缺失 0 -> 可发     exit=0
出墙：D:\ilife\.scratch\t873\桌面墙-1280.html（48 格 × 1280 宽，按比例缩显示，格高 860）
48 格；链接 148 条；缺失 0 -> 可发     exit=0
截图 99 张 -> D:\ilife\.scratch\t873\shots（产物页 48 × 2 档整页＋墙／索引首屏）     exit=0
```

## 三 十席的 commit 清单（从 `git log` 抄）

| 席 | commit | 提交信息（原文首行） |
|---|---|---|
| 公共层席 | `60eedb34c000c7309cef510f4b5f37dfe4eee89f` | feat(873): 公共层席 chef 视觉皮肤 ＋ 10 域壳接线 ＋ 包内测试 |
| 做菜席 | `f2385807803d7ff0bdbabdad0962c51bc7710118` | feat(873): 做菜席 5 页页内收口（参数三列成列／正文按标点分行／文案去绕嘴／步骤状态形状＋页头装饰图形）＋ 质量门六列全绿、双端首屏图逐页复评 |
| 查看席 | `302e313e8eec5a2b7fc65c7fe31463dcdbe6ce16` | feat(873): 查看席 8 页页内收口（分组标签块＋分区题头＋营养瓦片＋窄档行卡）＋ 冻结尺逐页复评与反例两行读数 |
| 搜索筛选席 | `bb2a6af2dfcca00bdbe420bb0957a02cad78d0e9` | feat(873): 搜索筛选席 13 页改「条件轨＋结果」两栏版面（插画带／条件面板／属性带分组／桌面两栏／措辞收敛）＋ 质量门六列全绿、终审扫描 0 命中 |
| 搜索筛选席（证据件补读数） | `0dc80426818795e224e4cf1db9b4b1c7690bd9ff` | docs(873): 搜索筛选席证据件补两条机器读数（包内皮肤测试 19/19 与它的 runId、还原批两页图的逐字节旁证） |
| 录入席 | `848b0e564591588eb33d8a9c04fca69b21353827` | feat(873): 录入席 6 页收口（备料表收两列＋量词同格、说明句并进口径行、步骤轨＋圆点，动作行与卡片齐沿）＋ 质量门六列全绿／终审扫描 0 命中 |
| 录入席（证据件补提交记录） | `823621c54ce3a2b1b9eb3ec3d51fd22cfea413ee` | docs(873): 录入席证据件补提交记录（runId 与 commit sha） |
| 修改席 | `c60f4d8bddd482720201b01e28a21fd12ea8ddfc` | feat(873): 修改席 4 页收口（改前改后分组卡＋标记行＋暖色锚点＋实现语清理）＋ t774-run-update.mjs 与证据件 |
| 历史席 | `3fc6275753af8c27e6c5e5a52a6311ff7f40a30d` | feat(873): 历史席 4 页收口 —— 结论条去同义复述／时间线同日只印一次日期／单菜统计四卡与事实条分家／全局统计空表改时间轴 ＋ 新增域内页内收口件 page-css ＋ 同尺复跑与反例读数 |
| 历史席（行数订正） | `90459f4e78705ebd031617b4614e6ad0e68e4735` | fix(873): 历史席证据件订正两处行数读数（按提交树 LF 口径重测：pages.ts 286／page-css.ts 75） |
| 数据管理席 | `d6c23ffd5bc1be54dfc075c853fe4c546625dc3f` | feat(873): 数据管理席 3 页收口 —— 体检逐菜卡带完整度条与五项读数胶囊、批量改去掉行话并收窄复制按钮、备份页删排期旁注加装饰插图 ＋ 六列质量门全绿／三类字样 0／包内守门 32-32 |
| 派生席 | `fb2888bd479fd0e7bd948d00b7c52c02d946e1d8` | feat(873): 派生席三页改关系对图与三层家族树（页内形状另立一件）＋ 42 格两跑 92、43/44 格 83/86、质量门六列全绿 |
| 采购＋开始使用席 | `e294c74c1e745315564135a0e7305e3813dcf887` | feat(873): 采购＋开始使用席 2 页页内收口（读数带替读数卡网格消 390 孤卡、首组展开、分组表去复读、两页去实现语）＋ 冻结尺复评 83→86／84→85 ＋ 质量门六列全绿与反例两红两绿 |

票级前置件（不是席位件，列出以便溯源）：`2b374e10`（开工基线 48 页冻结读数）、`7e1bcdce`（#871 把复评器具文本冻结进
`t778-终审打分.md` §一.1）、`af1892f5`（#873 接进地图 765）。

## 四 同尺两跑的汇总读数

出处 `.scratch/t873-复评/同尺两跑.json`（48 条，逐格含改前／改后各五维、总分、评委一句话原文）。
完整表格见 `docs/skills/skill-chef/t873-终审打分.md`；逐格症状见 `docs/skills/skill-chef/t873-逐格缺陷清单.md`。

- **均值**：改前 **80.02**（区间 71–89）→ 改后 **83.54**（区间 75–92），**Δ +3.52**。
- **≥90 页数**：改前 **0** → 改后 **1**（第 42 格「记派生关系回执」92）。
- **最高／最低**：改后最高 92（第 42 格），最低 75（第 3 格）。
- **方向**：升 34 格／平 4 格／降 10 格。降得最多的三格是第 21 格（−9）、第 35 格（−8）、第 37 格（−4）；
  搜索筛选席在它自己的证据件 §四.4 里记过这把尺的页间摆幅约 ±8，**第 21 格的 −9 略出该带，照实记在表里、不做归因**；
  采购席还单独记了第 35 格的器具账（把孤卡缺陷原样改回去反而得 87）。
- **逐维均值**：信息层级 17.31→17.58｜双端自适应 17.58→17.23｜文案精炼 15.13→16.21｜**视觉生动 13.25→15.25**｜风格一致 16.75→17.27。
- **分组均值**（改前→改后）：做菜 78.8→80｜查看 82→83｜搜索筛选 80.38→85.08｜修改 78→83｜历史 78.25→85.5｜采购 87→79｜
  录入 80.33→82｜派生 77.67→87（≥90 1 页）｜开始使用 80→86｜数据管理 79.67→82.67。
- **口径**：改前／改后都是**本会话同一把尺**；`t873-开工基线.md` §一 的那一列是另一个会话的第三方参照，**两者不可直接相减**。
- **器具事实**（照实登，不裁，详见打分件 §四）：同图同问句的可复现性 —— 公共层／历史／修改／派生／搜索筛选／采购／数据管理七席都报「逐字可复现」，
  录入席报「同一对图三跑给 78／80／84、71／87／83（极差 16）」；收口席本会话实测到「可复现」（第 18 格同图两次同 JSON）。
  两边的共同点是**摆幅出现在换了一对图之后**，所以 ±8 以内的页间差不可当改进量读。
  另有一条素材事实：第 1 格与第 5 格的改前图**逐字节相同**（改后图同样如此），那两格四行读数相同是素材相同、不是尺子抖。

## 五 未做项与下一步

1. **目标未达成，且缺口是同一条**：48 页里改后 ≥90 的只有 1 页。把「视觉生动」以外的四维全部推到本尺的高分位（18/19/19/17）
   合计仍是 90 出头，**只要「视觉生动」停在 13–16，总分就到不了 90**（查看席 §五.1 的算术写在它的证据件里）。
2. **「图从哪来」这条未决是本票最有价值的下一步**：本批 48 页的数据源里**没有任何一页带图**
   （`chef_data.db` 的 `photo_url` 位为空），而派单册明令不许编造菜品照片；
   `renderMediaFigure`（公共层现成的形状件）可以直接用，但它的产出器会写 `loading="lazy"`，
   与验收墙的造册判据冲突（验收墙拒收 `loading=lazy` 的产物）。⇒ **建议由编排者转票裁决**：
   技能侧到底出不出菜品图位；若出，图从哪来、谁提供、`loading=lazy` 这条冲突怎么解。
   公共层席 §五.2 ＋ 查看／录入／修改／派生／数据管理／搜索筛选／采购各席都独立记了同一条。
3. **「文案精炼」这一维的第二条天花板**：搜索筛选席的 13 页与做菜席的 5 页，评委每页都在点**库里的原文**
   （菜品简介的品牌化口播式长铺垫、半角逗号与 `(②)` 机器标注、做菜反馈里的口语化数字）。
   派单册把各席限于「只在排版层断句／分段、不许改写内容」，所以这段缺口**要改库正文或加一条「摘要＋展开」的公共层形状件**才能消。
   ⇒ 也需要一条编排者层的裁决。
4. **文档侧两条待同步（越界报备的转票项，本轮未碰）**：
   - 录入席 §五.4：`docs/skills/skill-chef/t778-逐格缺陷清单.md` 与 `docs/skills/skill-chef/t773-录入域.md` 里逐字引着录入席这一轮**已改掉**的那几句。
   - `docs/skills/skill-chef/t873-派单册.md`、`t873-席页清单.md`／`.mjs`、`t873-席跑.mjs` 目前**都还是未跟踪件**（编排者的件），本轮未提交。
5. **读取口径的两条边界**（详见打分件 §四）：① 冻结尺对「版式位」分辨力不足（做菜席列对齐反例给 83、采购席孤卡反例给 87）；
   ② 88–92 抖动带里本批有四格（42＝92 计过；14／15＝89、26＝88 不计过）。

## 六 不许动的东西：逐条声明

- **判据一处没放宽**：`docs/skills/skill-chef/t768-质量门.mjs` 的六列、`t778-终审扫描.mjs` 的三类字样表、`t778-验收墙.mjs` 的墙与册子形状
  —— 收口席本次**一个字节都没改**（写集里没有这三件，`git status` 也确认它们未被改动）。
- **墙／册子／扫描件都没被手工改绿**：`.scratch/t873/` 下的 53 件产物、`手机墙-390.html`、`桌面墙-1280.html`、`总索引.html`、
  `manifest.json`、`t873-终审扫描.json`、`t778-质量门.json` 全部由票面指定的那条 `accept.ps1` **一条命令重出**，
  收口席没有手工编辑其中任何一件；质量门那条 HELP 既有红**照实留在读数里**，没有抹掉。
- **真库只读**：48 页的产物由 7 个域驱动器 ＋ `t777-run-all.mjs` 现跑现出；收口席没有对 `chef_data.db` 做任何写操作，
  也没有改 schema、没有改数据语义。第 1／2／3／4／5 格等页里的「修改验证菜<时间戳>」这类候选菜名是**既有驱动器按设计新建的记录**（只增不删），不是收口席造的。
- **提交范围只含本票件**：本次提交只含 `docs/skills/skill-chef/t770…t776-册子片段.json` 这 7 个受版本控制的件
  （重出命令的副作用，各席都报备过并留给收口席处置）＋ 收口席新建的三件
  `t873-终审打分.md`／`t873-逐格缺陷清单.md`／`t873-证据.md`。提交前 `git diff --cached --name-only` 复核过，
  暂存区不含任何别席的件、不含 `packages/**`、不含别席的证据件、不含 `.scratch/t873-<别的域>/**`。
- **并发纪律**：全程没有用 `git switch`／`checkout <分支>`／`reset`／`stash`／`clean`／`restore .`／`--amend`／`rebase`／`push --force*`；
  编译／测试／git 写一律走 `node tooling/run-locked.mjs --ticket 873 -- <命令>`。
- **收口席自己的写集**：`.scratch/t873/**`、`.scratch/t873-复评/**`、上面三份 `t873-*.md`、7 个册子片段 JSON——**偏差 0**，别的路径一律未碰。
