# t873 收口证据：48 页重出产物批 ＋ 双端墙 ＋ 同尺两跑打分件（批级）

票：[#873](https://github.com/FeatherHunter/ilife/issues/873)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 22 收口 C）· 席位＝**收口席**。
跑法一律经锁：`node tooling/run-locked.mjs --ticket 873 -- <命令>`。收口席本次运行标识
`runId=6ea9b65e-3812-47cd-96cd-c1b92b0b2977`（`waitedMs=0`、`exit=0`）。

## 一 一句话结论

公共层席 ＋ 9 个域席的改动**全部落地并提交**（13 个 commit，10 席）；收口席按票面「验收命令」重出正式批
（`.scratch/t873/`：53 件产物 ＋ 双端墙 ＋ 96 张双端首屏图），**两条批级判据都绿**（`--check` exit 0、终审扫描 exit 0），
质量门只剩一条**改前就在、不上墙**的 HELP 验收副本既有红；
48 页在**同一会话、同一把尺、同一批图上**做的改前／改后两跑给出：均值 **80.02 → 83.54（+3.52）**，
**≥90 从 0 页到 1 页**（第 42 格 92），升 34 格／平 4 格／降 10 格——**「48 页逐页 ≥90」这个目标改后仍未达成**，
未达成的算术原因只有一条：**「视觉生动」这一维被「库里没有图」封顶**（改后均值 15.25，48 格没有一格超过 18）。

## 二 三条批级读数（原样抄回）

命令：`node tooling/run-locked.mjs --ticket 873 -- pwsh -NoProfile -File .scratch/t873-work/accept.ps1`
（① 重出 48 页产物 ＋ 4 变体 ＋ HELP 造册进 `.scratch/t873/`；② 质量门 → 双端墙 → 首屏截图 → 终审扫描；③ `--check`；④ 终审扫描）。
全文日志 `.scratch/t873-复评/accept.log`。

```
===== ③ 批级判据一：验收墙自检 --check
48 格；链接 245 条；缺失 0 -> 可发
自检：墙格 48／册子 48／不进墙 0；手机墙-390.html 97 ＋ 桌面墙-1280.html 97 ＋ 总索引.html 51；质量门 横向溢出红 0／触摸目标红 0／触屏三件红 0／分隔符懒政红 0／英文裸词红 0／重复句红 0；册子 manifest.json
墙 --check exit=0
```

```
===== ④ 批级判据二：终审扫描
扫 53 件产物；三类字样命中 0 处

共 53 件；命中 0 -> 干净
扫描 exit=0
```

```
===== ② 质量门汇总行
汇总：横向溢出 0／触摸目标 1／触屏三件 0／分隔符懒政 0／英文裸词 1／重复句 0（页 × 变体 53 行）
exit=1
```

**质量门那条红是既有红，且不在墙上**：逐件读 `.scratch/t873/t778-质量门.json` 的 53 行，只有
`HELP/私家大厨_HELP_验收副本.html` 一行两列变红——`触摸目标::100 件可点，最短边 14px` 与 `英文裸词::1 处`（令牌 `HELP`，
出在「私家大厨 HELP · 能力速查」这一行）。**48 个域页 ＋ 4 个变体那 52 行六列全 `red:false`**；
两条墙的自检行也因此都是「质量门 红 0」。改造前 `#871` 已把这两条记成既有红，HELP 走的是另一条装配链
（`src/help/helpFile.ts` → `base-paint/help-shell`），本票 10 席一处都没碰它。**收口席照实记，不放宽也不改绿。**

两条墙的读数（同一次运行）：

```
出墙：D:\ilife\.scratch\t873\手机墙-390.html（48 格 × 390 宽，格高 820）
48 格；链接 147 条；缺失 0 -> 可发     exit=0
出墙：D:\ilife\.scratch\t873\桌面墙-1280.html（48 格 × 1280 宽，按比例缩显示，格高 860）
48 格；链接 148 条；缺失 0 -> 可发     exit=0
截图 99 张 -> D:\ilife\.scratch\t873\shots（产物页 48 × 2 档整页＋墙／索引首屏）     exit=0
```

## 三 十席的 commit 清单（从 `git log` 抄）

| 席 | commit | 提交信息（原文首行） |
|---|---|---|
| 公共层席 | `60eedb34c000c7309cef510f4b5f37dfe4eee89f` | feat(873): 公共层席 chef 视觉皮肤 ＋ 10 域壳接线 ＋ 包内测试 |
| 做菜席 | `f2385807803d7ff0bdbabdad0962c51bc7710118` | feat(873): 做菜席 5 页页内收口（参数三列成列／正文按标点分行／文案去绕嘴／步骤状态形状＋页头装饰图形）＋ 质量门六列全绿、双端首屏图逐页复评 |
| 查看席 | `302e313e8eec5a2b7fc65c7fe31463dcdbe6ce16` | feat(873): 查看席 8 页页内收口（分组标签块＋分区题头＋营养瓦片＋窄档行卡）＋ 冻结尺逐页复评与反例两行读数 |
| 搜索筛选席 | `bb2a6af2dfcca00bdbe420bb0957a02cad78d0e9` | feat(873): 搜索筛选席 13 页改「条件轨＋结果」两栏版面（插画带／条件面板／属性带分组／桌面两栏／措辞收敛）＋ 质量门六列全绿、终审扫描 0 命中 |
| 搜索筛选席（证据件补读数） | `0dc80426818795e224e4cf1db9b4b1c7690bd9ff` | docs(873): 搜索筛选席证据件补两条机器读数（包内皮肤测试 19/19 与它的 runId、还原批两页图的逐字节旁证） |
| 录入席 | `848b0e564591588eb33d8a9c04fca69b21353827` | feat(873): 录入席 6 页收口（备料表收两列＋量词同格、说明句并进口径行、步骤轨＋圆点，动作行与卡片齐沿）＋ 质量门六列全绿／终审扫描 0 命中 |
| 录入席（证据件补提交记录） | `823621c54ce3a2b1b9eb3ec3d51fd22cfea413ee` | docs(873): 录入席证据件补提交记录（runId 与 commit sha） |
| 修改席 | `c60f4d8bddd482720201b01e28a21fd12ea8ddfc` | feat(873): 修改席 4 页收口（改前改后分组卡＋标记行＋暖色锚点＋实现语清理）＋ t774-run-update.mjs 与证据件 |
| 历史席 | `3fc6275753af8c27e6c5e5a52a6311ff7f40a30d` | feat(873): 历史席 4 页收口 —— 结论条去同义复述／时间线同日只印一次日期／单菜统计四卡与事实条分家／全局统计空表改时间轴 ＋ 新增域内页内收口件 page-css ＋ 同尺复跑与反例读数 |
| 历史席（行数订正） | `90459f4e78705ebd031617b4614e6ad0e68e4735` | fix(873): 历史席证据件订正两处行数读数（按提交树 LF 口径重测：pages.ts 286／page-css.ts 75） |
| 数据管理席 | `d6c23ffd5bc1be54dfc075c853fe4c546625dc3f` | feat(873): 数据管理席 3 页收口 —— 体检逐菜卡带完整度条与五项读数胶囊、批量改去掉行话并收窄复制按钮、备份页删排期旁注加装饰插图 ＋ 六列质量门全绿／三类字样 0／包内守门 32-32 |
| 派生席 | `fb2888bd479fd0e7bd948d00b7c52c02d946e1d8` | feat(873): 派生席三页改关系对图与三层家族树（页内形状另立一件）＋ 42 格两跑 92、43/44 格 83/86、质量门六列全绿 |
| 采购＋开始使用席 | `e294c74c1e745315564135a0e7305e3813dcf887` | feat(873): 采购＋开始使用席 2 页页内收口（读数带替读数卡网格消 390 孤卡、首组展开、分组表去复读、两页去实现语）＋ 冻结尺复评 83→86／84→85 ＋ 质量门六列全绿与反例两红两绿 |

票级前置件（不是席位件，列出以便溯源）：`2b374e10`（开工基线 48 页冻结读数）、`7e1bcdce`（#871 把复评器具文本冻结进
`t778-终审打分.md` §一.1）、`af1892f5`（#873 接进地图 765）。

## 四 同尺两跑的汇总读数

出处 `.scratch/t873-复评/同尺两跑.json`（48 条，逐格含改前／改后各五维、总分、评委一句话原文）。
完整表格见 `docs/skills/skill-chef/t873-终审打分.md`；逐格症状见 `docs/skills/skill-chef/t873-逐格缺陷清单.md`。

- **均值**：改前 **80.02**（区间 71–89）→ 改后 **83.54**（区间 75–92），**Δ +3.52**。
- **≥90 页数**：改前 **0** → 改后 **1**（第 42 格「记派生关系回执」92）。
- **最高／最低**：改后最高 92（第 42 格），最低 75（第 3 格）。
- **方向**：升 34 格／平 4 格／降 10 格。降得最多的三格是第 21 格（−9）、第 35 格（−8）、第 37 格（−4）；
  搜索筛选席在它自己的证据件 §四.4 里记过这把尺的页间摆幅约 ±8，**第 21 格的 −9 略出该带，照实记在表里、不做归因**；
  采购席还单独记了第 35 格的器具账（把孤卡缺陷原样改回去反而得 87）。
- **逐维均值**：信息层级 17.31→17.58｜双端自适应 17.58→17.23｜文案精炼 15.13→16.21｜**视觉生动 13.25→15.25**｜风格一致 16.75→17.27。
- **分组均值**（改前→改后）：做菜 78.8→80｜查看 82→83｜搜索筛选 80.38→85.08｜修改 78→83｜历史 78.25→85.5｜采购 87→79｜
  录入 80.33→82｜派生 77.67→87（≥90 1 页）｜开始使用 80→86｜数据管理 79.67→82.67。
- **口径**：改前／改后都是**本会话同一把尺**；`t873-开工基线.md` §一 的那一列是另一个会话的第三方参照，**两者不可直接相减**。
- **器具事实**（照实登，不裁，详见打分件 §四）：同图同问句的可复现性 —— 公共层／历史／修改／派生／搜索筛选／采购／数据管理七席都报「逐字可复现」，
  录入席报「同一对图三跑给 78／80／84、71／87／83（极差 16）」；收口席本会话实测到「可复现」（第 18 格同图两次同 JSON）。
  两边的共同点是**摆幅出现在换了一对图之后**，所以 ±8 以内的页间差不可当改进量读。
  另有一条素材事实：第 1 格与第 5 格的改前图**逐字节相同**（改后图同样如此），那两格四行读数相同是素材相同、不是尺子抖。

## 五 未做项与下一步

1. **目标未达成，且缺口是同一条**：48 页里改后 ≥90 的只有 1 页。把「视觉生动」以外的四维全部推到本尺的高分位（18/19/19/17）
   合计仍是 90 出头，**只要「视觉生动」停在 13–16，总分就到不了 90**（查看席 §五.1 的算术写在它的证据件里）。
2. **「图从哪来」这条未决是本票最有价值的下一步**：本批 48 页的数据源里**没有任何一页带图**
   （`chef_data.db` 的 `photo_url` 位为空），而派单册明令不许编造菜品照片；
   `renderMediaFigure`（公共层现成的形状件）可以直接用，但它的产出器会写 `loading="lazy"`，
   与验收墙的造册判据冲突（验收墙拒收 `loading=lazy` 的产物）。⇒ **建议由编排者转票裁决**：
   技能侧到底出不出菜品图位；若出，图从哪来、谁提供、`loading=lazy` 这条冲突怎么解。
   公共层席 §五.2 ＋ 查看／录入／修改／派生／数据管理／搜索筛选／采购各席都独立记了同一条。
3. **「文案精炼」这一维的第二条天花板**：搜索筛选席的 13 页与做菜席的 5 页，评委每页都在点**库里的原文**
   （菜品简介的品牌化口播式长铺垫、半角逗号与 `(②)` 机器标注、做菜反馈里的口语化数字）。
   派单册把各席限于「只在排版层断句／分段、不许改写内容」，所以这段缺口**要改库正文或加一条「摘要＋展开」的公共层形状件**才能消。
   ⇒ 也需要一条编排者层的裁决。
4. **文档侧两条待同步（越界报备的转票项，本轮未碰）**：
   - 录入席 §五.4：`docs/skills/skill-chef/t778-逐格缺陷清单.md` 与 `docs/skills/skill-chef/t773-录入域.md` 里逐字引着录入席这一轮**已改掉**的那几句。
   - `docs/skills/skill-chef/t873-派单册.md`、`t873-席页清单.md`／`.mjs`、`t873-席跑.mjs` 目前**都还是未跟踪件**（编排者的件），本轮未提交。
5. **读取口径的两条边界**（详见打分件 §四）：① 冻结尺对「版式位」分辨力不足（做菜席列对齐反例给 83、采购席孤卡反例给 87）；
   ② 88–92 抖动带里本批有四格（42＝92 计过；14／15＝89、26＝88 不计过）。

## 六 不许动的东西：逐条声明

- **判据一处没放宽**：`docs/skills/skill-chef/t768-质量门.mjs` 的六列、`t778-终审扫描.mjs` 的三类字样表、`t778-验收墙.mjs` 的墙与册子形状
  —— 收口席本次**一个字节都没改**（写集里没有这三件，`git status` 也确认它们未被改动）。
- **墙／册子／扫描件都没被手工改绿**：`.scratch/t873/` 下的 53 件产物、`手机墙-390.html`、`桌面墙-1280.html`、`总索引.html`、
  `manifest.json`、`t873-终审扫描.json`、`t778-质量门.json` 全部由票面指定的那条 `accept.ps1` **一条命令重出**，
  收口席没有手工编辑其中任何一件；质量门那条 HELP 既有红**照实留在读数里**，没有抹掉。
- **真库只读**：48 页的产物由 7 个域驱动器 ＋ `t777-run-all.mjs` 现跑现出；收口席没有对 `chef_data.db` 做任何写操作，
  也没有改 schema、没有改数据语义。第 1／2／3／4／5 格等页里的「修改验证菜<时间戳>」这类候选菜名是**既有驱动器按设计新建的记录**（只增不删），不是收口席造的。
- **提交范围只含本票件**：本次提交只含 `docs/skills/skill-chef/t770…t776-册子片段.json` 这 7 个受版本控制的件
  （重出命令的副作用，各席都报备过并留给收口席处置）＋ 收口席新建的三件
  `t873-终审打分.md`／`t873-逐格缺陷清单.md`／`t873-证据.md`。提交前 `git diff --cached --name-only` 复核过，
  暂存区不含任何别席的件、不含 `packages/**`、不含别席的证据件、不含 `.scratch/t873-<别的域>/**`。
- **并发纪律**：全程没有用 `git switch`／`checkout <分支>`／`reset`／`stash`／`clean`／`restore .`／`--amend`／`rebase`／`push --force*`；
  编译／测试／git 写一律走 `node tooling/run-locked.mjs --ticket 873 -- <命令>`。
- **收口席自己的写集**：`.scratch/t873/**`、`.scratch/t873-复评/**`、上面三份 `t873-*.md`、7 个册子片段 JSON——**偏差 0**，别的路径一律未碰。

## 七 收口席的运行与提交记录（runId 抄自 `.scratch/locks/gate-runs.log`）

```
GATE-RUN runId=6ea9b65e-3812-47cd-96cd-c1b92b0b2977 cmd="pwsh -NoProfile -File .scratch/t873-work/accept.ps1" waitedMs=0 exit=0
GATE-RUN runId=3e04be9d-632c-4686-a1be-872826eb0d08 cmd="git add docs/skills/skill-chef/t770…t776-册子片段.json docs/skills/skill-chef/t873-终审打分.md docs/skills/skill-chef/t873-逐格缺陷清单.md docs/skills/skill-chef/t873-证据.md" waitedMs=0 exit=0
GATE-RUN runId=83fac57b-e24d-4786-a67b-f07f49d7ed4e cmd="git commit -F .scratch/t873-复评/commit-msg.txt -- <同上十条路径>" waitedMs=0 exit=0
```

- 提交：**`3b0ec07d0d8586df34f5571b86e97e99576ab90d`** — `docs(873): 收口 — 重出产物批＋双端墙＋48 页同尺两跑打分件与新清单`，
  10 files changed, 445 insertions(+), 100 deletions(-)。提交前 `git diff --cached --name-only` 复核过：暂存区只含上列 10 条路径。
- 提交信息走 `git commit -F <UTF-8 文件>`（`docs/subagent-concurrency-protocol.md` §3.1 的硬要求：先把提交信息落一份 UTF-8 文件再提交，
  防终端代码页损坏中文）；提交后回读 `git log -1 --pretty=%s`，中文正常。信息文本与票面给的 `-m` 一字不差。
- 收口席本次**没有**用 `git switch`／`checkout <分支>`／`reset`／`stash`／`clean`／`restore .`／`--amend`／`rebase`／`push --force*`；
  编译／测试／git 写全部经 `node tooling/run-locked.mjs --ticket 873 -- <命令>`（上表三条即全部持锁运行）。

---

# 末次收口（第二轮之后）

**席位＝末次收口席**（第二轮之后的终批复评与收口）。本节**只追加**，上面 §一–§七 一字未改。判读面＝**当刻正式批**的产物图
`.scratch/t873/shots/<NN>-<slug>-{390v,1280v}.png`；问句＝`t873-派单册.md` §二 的冻结问句逐字；每页一次调用、两张图、不带 `json: true`、严格顺序跑；
48 格逐格读数落 `.scratch/t873-复评/终批.json`（本席新建，含每格五维／自报 total／五维之和／一句话原文／两张图的 `sha256`／记时刻）。

## 一 批级读数（五条原文，照登）

1. `tsc -b packages/skill-chef` **exit=0**；包内全量测试 **tests 103 ／ pass 103 ／ fail 0**。
2. `node docs/skills/skill-chef/t778-验收墙.mjs --check .scratch/t873` → `48 格；链接 245 条；缺失 0 -> 可发`，**exit=0**。
3. `node docs/skills/skill-chef/t778-终审扫描.mjs .scratch/t873` → `共 53 件；命中 0 -> 干净`，**exit=0**。
4. 质量门 53 行：`横向溢出 0／触摸目标 1／触屏三件 0／分隔符懒政 0／英文裸词 1／重复句 0`；
   **唯一红行＝ `HELP/私家大厨_HELP_验收副本.html` 的既有红**（改前就在、按设计不上墙）。
5. 两张墙各自 `48 格；缺失 0 -> 可发` **exit=0**；首屏图 **96 张**已落 `.scratch/t873/shots/`。

出处：编排席跑的 `.scratch/t873-work/final.ps1`，全文日志 `.scratch/t873-work/final.log`（本席逐条对过日志原文，上面五条与日志逐字一致）。

## 二 终批汇总（本席一次跑，48 格）

- **均值 83.48**（区间 **70–92**），**≥90 三页**：第 21 格 **92**、第 29 格 **90**、第 45 格 **90**；最低第 47 格 **70**。
- **逐维均值**：信息层级 17.40｜双端自适应 16.90｜文案精炼 **15.69**｜视觉生动 **16.19**｜风格一致 17.31。
- **按域均值**：做菜 83.80｜查看 82.88｜搜索筛选 84.15（≥90 1）｜修改 83.00（≥90 1）｜历史 84.00｜采购 89.00｜录入 83.33｜派生 82.00｜开始使用 90.00（≥90 1）｜数据管理 79.33。
- **四个口径的均值对照**：改前（`t873-开工基线.md` §一，第三方参照）79.5（68–88，≥90 0 页）｜第一轮同尺 83.54（75–92，≥90 1 页）｜
  第二轮两跑（域席首跑）84.02（73–92，≥90 3 页）｜终批 83.48（70–92，≥90 3 页）。**这四列之间不许相减**（口径见 `t873-终审打分.md` §二）。
- **按票面口径的过线页只有 4 页**（「88–92 抖动带内两跑都 ≥90」）：21／28／45 在**第二轮**两跑，42 在**第一轮**两跑；
  **「48 页逐页 ≥90」未达成，44 页未达**（第 29 格终批 90 落在抖动带内、只有一跑，不计过）。
- **器具事实**（本票最重要的读数，四条原文 ＋ 本席一条对账，详见 `t873-终审打分.md` §四）：
  同一份产物横跨 92 与 80（第 42 格：第一轮 92／92，第二轮一行未改、`sha256` 相同却 80／80；本席终批同样 **80**，
  390v 图的 `sha256` ＝ `e3b75a4b4f773ca424d5f747c5573d6e37277ba95c8eb04c01b904c3a1ba8c97`，与 `.scratch/t873-work/图源与器具事实.md` §二 记的 `e3b75a4b…` 逐字相同）；
  同一页四批图（第 14 格 89／86／83／73）；同一对图三跑（第 36 格 78／80／84、第 40 格 71／87／83，极差 16，而第二轮同图连跑 38／39／41 逐字相同）；
  尺子判不出的事（第 35 格孤卡改回去反而 87；做菜席第 1 格三列参差两态 83:79 分不开，真浏览器几何分得开 `50/64/63`＋右端空 115px 对 `104/104/104`）。
  ⇒ **「≥90」在同会话同批内可判、跨会话／跨批不可判。**

## 三 本轮（第二轮之后）的 `873` commit 清单

`git log --all --format="%h %s" --grep=873`（＝题目给的 `git log --oneline --all --format="%h %s" | grep 873` 的等价写法；完整清单落在
`.scratch/t873-复评/git873.txt`，共 32 行，其中末行 `0fb014d7 feat(855)…` 只命中 commit 正文、**不是本票的件**，未计入下表）：

```
f3b09b09 feat(873): 修改席第二轮——宽档 1080 版心＋组卡与改后表并排＋页族插画带＋第29格换形与菜名截断试撤
1e56bd57 docs(873): 录入席证据件补第二轮提交记录（runId 与 commit sha）
d1970be5 feat(873): 录入席第二轮定向返修（内联 SVG 器物带＋删三行变更行与来源行＋宽档五块收窄居中、窄档正文列 374px）＋ 六页全数未达 90 的逐页差口
82caecfb feat(873): 搜索筛选席第二轮定向返修（统计行并进属性带／宽档属性矩阵／插画带升格跨两栏＋页头左缘对齐／复制区去标题／窄屏内距）
6b2035ef feat(873): 采购＋开始使用席第二轮（读数改回偶数张读数卡消孤卡、删复述口径行、暖色收到页头与分区题头）＋ 冻结尺两跑 45=90/90 达标、35=89/89 ＋ 反例 80 红／还原 90 绿
6aaf2d19 feat(873): 做菜席第二轮定向返修（撤回宽档双列改 2:1 分栏／参数改键值行去 390 贴边／删三处文案复述／装饰收成卡面＋装饰带）
27d633ae feat(873): 查看席第二轮定向返修（页族装饰带＋步骤参数带＋窄档一行一件＋宽档栅格用满＋三处文案复述删并）＋ 两跑读数与反例
06e271f3 docs(873): 数据管理席证据件追加 §6.6 —— 广播自查「宽档收窄居中」根因不在本席写集（grep 无 margin:auto／产物外框实测两件与结论条同左右边界）
2ff9869d docs(873): 派生席证据件补一节自查——宽档栅格项 margin:auto 收缩这条本席三页不成立 ＋ 1280 真渲染宽度读数
ae8cceca feat(873): 派生席第二轮定向返修（家族树宽档两栏重排＋图形标记替字面复现＋装饰图形；派生新菜回执去撞词复述）＋ 质量门六列全绿／扫描 0／反例一红一绿
eeac40e1 feat(873): 数据管理席第二轮定向返修 —— 页族插画带（720x120 满宽 slice ＋ 两侧记录记号）、宽档复制区单行与页尾成对布局、删掉与结论条同义的说明句把口径并进蓝条＋ 六列质量门全绿／三类字样 0／包内守门 32-32／反例红绿两行
8ccb530d feat(873): 历史席第二轮返修（时间线 SVG 评分圆环／页顶色带收边／390 事实条两列／刻度条刻线／33 评分区间合并／34 复制作标题改口）＋ 修掉宽档读数卡被 margin:auto 压成窄列 ＋ 同尺读数与反例（72-72 红 / 87 绿）
a75b7c08 docs(873): 编排席器械入仓 —— 派单册／席位页清单（含生成器）／席位驱动器
28802e7c docs(873): 收口证据件补运行标识与 commit sha（accept／add／commit 三条 runId ＋ 3b0ec07d）
3b0ec07d docs(873): 收口 — 重出产物批＋双端墙＋48 页同尺两跑打分件与新清单
823621c5 docs(873): 录入席证据件补提交记录（runId 与 commit sha）
848b0e56 feat(873): 录入席 6 页收口（备料表收两列＋量词同格、说明句并进口径行、步骤轨＋圆点，动作行与卡片齐沿）＋ 质量门六列全绿／终审扫描 0 命中
302e313e feat(873): 查看席 8 页页内收口（分组标签块＋分区题头＋营养瓦片＋窄档行卡）＋ 冻结尺逐页复评与反例两行读数
c60f4d8b feat(873): 修改席 4 页收口（改前改后分组卡＋标记行＋暖色锚点＋实现语清理）＋ t774-run-update.mjs 与证据件
0dc80426 docs(873): 搜索筛选席证据件补两条机器读数（包内皮肤测试 19/19 与它的 runId、还原批两页图的逐字节旁证）
bb2a6af2 feat(873): 搜索筛选席 13 页改「条件轨＋结果」两栏版面（插画带／条件面板／属性带分组／桌面两栏／措辞收敛）＋ 质量门六列全绿、终审扫描 0 命中
d6c23ffd feat(873): 数据管理席 3 页收口 —— 体检逐菜卡带完整度条与五项读数胶囊、批量改去掉行话并收窄复制按钮、备份页删排期旁注加装饰插图 ＋ 六列质量门全绿／三类字样 0／包内守门 32-32
fb2888bd feat(873): 派生席三页改关系对图与三层家族树（页内形状另立一件）＋ 42 格两跑 92、43/44 格 83/86、质量门六列全绿
f2385807 feat(873): 做菜席 5 页页内收口（参数三列成列／正文按标点分行／文案去绕嘴／步骤状态形状＋页头装饰图形）＋ 质量门六列全绿、双端首屏图逐页复评
90459f4e fix(873): 历史席证据件订正两处行数读数（按提交树 LF 口径重测：pages.ts 286／page-css.ts 75）
3fc62757 feat(873): 历史席 4 页收口 —— 结论条去同义复述／时间线同日只印一次日期／单菜统计四卡与事实条分家／全局统计空表改时间轴 ＋ 新增域内页内收口件 page-css ＋ 同尺复跑与反例读数
e294c74c feat(873): 采购＋开始使用席 2 页页内收口（读数带替读数卡网格消 390 孤卡、首组展开、分组表去复读、两页去实现语）＋ 冻结尺复评 83→86／84→85 ＋ 质量门六列全绿与反例两红两绿
60eedb34 feat(873): 公共层席 chef 视觉皮肤 ＋ 10 域壳接线 ＋ 包内测试
2b374e10 #873 开工基线：冻结尺子下 48 页读数（均值 79.5／≥90 0 页／最低第 41 格 68）＋最差十页＋同类症状＋复跑与销账口径；两处异常（第 15 格维度超上限封顶、第 47 格换词后重评）如实留档
7e1bcdce #871 收尾：复评器具文本冻结进 t778-终审打分.md §一.1（问句逐字＋调用形状＋刻度说明）——原问句不可复现，#873 各席照这一条跑
af1892f5 #873 接线：票22=#873（32页逐页推≥90·分席）接进地图765；13→[12,21,22]／22→[21]；全图校验 PASS 22张/75边
```

## 四 末次收口席自己这一轮的跑法与落点

- 48 次 `vision_describe`（一次一格、两张图、顺序跑）落在 `.scratch/t873-复评/终批.json`（48 条）＋ `.scratch/t873-复评/终批-原文.txt`（评委原文逐格留档）；
  追加器具＝本席新建的 `.scratch/t873-复评/append.mjs`。
- 本席**没有改**任何判据、墙、册子、扫描件、产物；**只读** `.scratch/t873/**` 与 `.scratch/t873-work/**`，
  **只写**写集里的路径（`.scratch/t873-复评/**` ＋ 三份 `t873-*.md` ＋ 7 个册子片段 JSON 的提交）。
- **第 48 格的一处运行事实（照实记）**：本席第一次调它时问句被我打字打错（`可点件命中区 ≥44px` 被我写成 `可点件命中率 ≥44px`），
  且那一次返回的是**非读数**（每题五维与 total 全 0、一句话是「请提供可访问的图片文件或URL以进行评估。」＝图片没被后端取到；
  **没有 `VISION_*` 错误码**）。按「不许编数字」，那一次**不作为读数落库**；本席用**逐字正确的冻结问句**对同一对图**重跑一次**，
  拿到 18／18／17／14／17 ＝ 84 并落库——这是第 48 格唯一的读数。全过程没有换问法、没有第二次以上的重试。

## 五 未做项与下一步（末次收口席）

1. **「48 页逐页 ≥90」未达成**（44 页未达）。缺口是**同一条**：终批「视觉生动」均值 16.19、48 格没有一格超过 19，
   而这一维的天花板是「图从哪来」（`.scratch/t873-work/图源与器具事实.md` §一：七个副本库各只有 1 条菜带 `photo_url`，
   值是占位服务 `picsum.photos` 的随机图，不是这道菜的照片；其余全空）；「文案精炼」均值 15.69 是本批最低一维，
   被**库内正文**封顶（派单册不许改写内容）。两条都要**维护者裁决**，不是渲染层能解的。
2. **本席这一跑是「一跑」**：48 格各一次，没有做第二跑。要让「≥90」成为可判定的结论，下一手需要在**同一批图**上
   对落进 88–92 抖动带的 5 格（12＝89、15＝89、19＝88、29＝90、35＝89）补第二跑；其余 43 格 <88，一跑即可判。
3. **第 42 格的 92 只在第一轮成立**（同一份产物图 `sha256` 逐字节相同，第二轮 80／80、终批 80）。
   任何「引用第一轮那两跑当它的过线证据」的写法都不成立。
4. **第 33／34 格缺第二轮读数**（历史席 §五.1：预算用尽未复评）；本席在终批各补一跑（84／83），仍未补第二跑。
5. **两条结构告警仍未拆**：`src/relation/shapes.ts` **427 LF**、`src/add/pages.ts` **448 LF**（各席已给拆法，见 `t873-终审打分.md` §六.3）。
6. **7 个受版本控制的册子片段 JSON**（`docs/skills/skill-chef/t770…t776-册子片段.json`）由重出命令连带重写，
   十席都报备过、都没提交——**由本席在本轮同一批提交里处置**（提交前 `git diff --cached --name-only` 复核只含那 7 件 ＋ 本席改的三份 `t873-*.md`）。

---

# 末次验收（第三轮之后）

**席位＝末次验收席**。本节**只追加**，上面两节一字未改。九个域席 ＋ 公共层席的**第三轮**改动全部提交、树干净之后，
本节记：重出的五条批级读数、**校准条**、48 页逐页结论、本轮 commit 清单与未做项。
判读面＝**第三轮重出后的正式批** `.scratch/t873/shots/<NN>-<slug>-{390v,1280v}.png`；问句＝`t873-派单册.md` §二 冻结问句逐字；每页一次调用、两张图、不带 `json: true`、严格顺序跑。
本会话读数落 `.scratch/t873-复评/校准条.json`（3 条）与 `.scratch/t873-复评/终批2.json`（48 第一跑 ＋ 22 第二跑 ＝ 70 条）。

## 一 五条批级读数（原文，照登）

命令：`node tooling/run-locked.mjs --ticket 873 --child-timeout-ms 2400000 -- pwsh -NoProfile -File .scratch/t873-work/final.ps1`
（编译 → 包内全量测试 → 重出 48 页产物＋4 变体＋HELP → 造册 `.scratch/t873/` → 质量门 → 双端墙 → 首屏截图 96 张 → 终审扫描 → `--check`）。
全文日志 `.scratch/t873-复评/regen3.log`。

1. `tsc -b packages/skill-chef` → **TSC exit=0**。
2. 包内全量测试 → **`ℹ tests 109` ／ `ℹ pass 108` ／ `ℹ fail 1` ／ `TEST exit=1`**（**红一条**，见 §五.2）。
3. 两条墙自检（原文）：
   - `出墙：D:\ilife\.scratch\t873\手机墙-390.html（48 格 × 390 宽，格高 820）` → `48 格；链接 147 条；缺失 0 -> 可发` → `exit=0`
   - `出墙：D:\ilife\.scratch\t873\桌面墙-1280.html（48 格 × 1280 宽，按比例缩显示，格高 860）` → `48 格；链接 148 条；缺失 0 -> 可发` → `exit=0`
   - 两行自检都是：`自检：墙格 48／册子 48／不进墙 0；…；质量门 横向溢出红 0／触摸目标红 0／触屏三件红 0／分隔符懒政红 0／英文裸词红 0／重复句红 0；册子 manifest.json`
4. 批级判据一：`48 格；链接 245 条；缺失 0 -> 可发` ＋ `墙 --check exit=0`。
5. 批级判据二：`扫 53 件产物；三类字样命中 0 处` ＋ `共 53 件；命中 0 -> 干净` ＋ `扫描 exit=0`；
   质量门汇总行：`横向溢出 0／触摸目标 1／触屏三件 0／分隔符懒政 0／英文裸词 1／重复句 0（页 × 变体 53 行）`（唯一红行＝ HELP 验收副本的既有红）。
   截图：`截图 99 张 -> D:\ilife\.scratch\t873\shots`。

## 二 校准条读数（本会话零点）

三对**冻结图**（`.scratch/t873-work/改前图/`，字节不变）在**同一会话**各跑一次，问句＝冻结问句逐字：

| 对照 | 图对 | 本会话 | 历史读数（跨会话） | 历史均值 | 本会话 − 历史均值 |
|---|---|---|---|---|---|
| C1 | `21-filter_time_quick`（390v sha256 `a4bd66f5…`） | **76**（17/17/14/13/15） | 76／80／92／92／92 | 86.4 | **−10.4** |
| C2 | `42-add_relation`（`fe459322…`） | **79**（16/17/16/14/16） | 80／80／83／92／92 | 85.4 | **−6.4** |
| C3 | `47-data_batch_edit`（`9a7c8a5f…`） | **81**（18/18/16/12/17） | ~70（另见二轮读数 80／70） | ~75 | **+6.0**（对 ~70 为 +11） |

**零点判断**：本会话三点 **76／79／81（均值 78.67）** vs 历史三点均值 82.27 ⇒ **−3.6**；
但**方向不一致**——C1 的 76 是它的历史最低值，C2 的 79 **比历史五次读数的最低值还低 1 分**（本会话偏严），
C3 的 81 **比历史 ~70 高 11 分**（本会话偏松）。⇒ **不存在一个可以拿去校正 48 页的单一零点偏移**；同会话内可比、跨会话不可比。
顺带产出本批**唯一可相减**的一对——三对冻结图正好是第 21／42／47 格的改前态：**21 76→85（+9）／42 79→89（+10）／47 81→80（−1）**。

## 三 48 页逐页结论

- **均值 82.81**（区间 **66–91**），**≥90 两页**：第 13 格 **90**、第 17 格 **91**；
  按票面「88–92 抖动带内两跑都 ≥90」的口径**过线 2 页、未达 46 页** ⇒ **「48 页逐页 ≥90」未达成**。
- **第二跑**（第一跑 ≥85 的 22 页，比票面「88–92 才要两跑」更严）：**22／22 与第一跑逐字相同**（五维＋总分全同）⇒ 同会话零抖动。
- 逐维均值：信息层级 17.04｜双端自适应 16.73｜文案精炼 **15.85**｜视觉生动 **16.04**｜风格一致 17.15。
- 按域均值：做菜 82.60｜查看 84.50（≥90 1）｜搜索筛选 80.62（≥90 1）｜修改 78.25｜历史 84.25｜采购 88.00｜录入 82.00｜派生 87.33｜开始使用 89.00｜数据管理 85.67。
- **两处判读面的例外**（照实记，未编分）：第 4 格正式批原图对在视觉后端**连挂 3 次**（返回非读数、无 `VISION_*` 错误码），
  第 39 格两次被**评委侧拒答**；两格都用**同帧无损重编码副本**（`.scratch/t873-复评/reencoded/`，`reencode.mjs` 用 `sharp` 解码后**逐像素校验与原件相同**）取到读数（89／89 与 85／85），
  `终批2.json` 的 `备注` 字段记着原由。做法出处＝做菜席 §7.9.2③（同一页同一现象，它当时也是这么取到的）。

## 四 本轮（第三轮）的 `873` commit 清单

`git log --all --format="%h %s" --grep=873`（完整清单落 `.scratch/t873-复评/git873-3.txt`，44 行，含本票全部历史提交）。本轮十一条：

```
f2283532 feat(873): 公共层席第三轮 —— 页族共用视觉标准（族级装饰带＋首屏填满度＋色彩锚点密度）
e8fe668e feat(873): 历史席第三轮 —— 四页落族（receipt/result）＋撤页内自建装饰带只保留族级带与评分圆环＋删两处口径行与按钮语序＋result 族页内首屏填满度＋同会话两行读数（80→83／81→83／92→89）＋反例 81/83
70582484 feat(873): 派生席第三轮收口（撤纯装饰树、逐句清文案、shapes.ts 按形状族拆两件）＋ 同会话 42 格 82→89／43 格 79→84
e40c559d feat(873): 查看席第三轮（撤页内自建装饰带＋三处渲染层复述删并＋宽档标签块与下厨记录两栏借位）＋ 同会话两跑与填满度读数
70878fdf feat(873): 数据管理席第三轮 —— 撤页内 720×120 满宽带（页头只留族级带，体检页族名由 receipt 改正为 result、完整度条按例外保留）、文案逐句审（删校准行与复制区说明行、删同义主按钮、结论条只留动作）、46 首屏 gap 16px ＋ 六列质量门全绿／带计数与填满度两探针绿／三类字样 0
fdc3dac2 feat(873): 采购＋开始使用席第三轮（开始使用页撤页头图标改折角删结论条＋窄档填满度收进 119px；采购页 A/B 三支更低故保留）＋ 同会话 45=82→89/89、35=88→88/88 ＋ 反例 88 红／还原 89 绿
02fee458 feat(873): 做菜席第三轮返修（页头只留族级带／结论条改说做过几次／删两句叠说文案／首屏空白收进 ≤120px）
79dd0447 feat(873): 修改席第三轮——对照带并入共用视觉标准（同调色板／虚线改前＋实心改后／封宽）＋ 渲染层文案逐句销账 ＋ 填满度复测 8 行全绿
65cc3e9a feat(873): 录入席第三轮（撤页内自建装饰带＋结论句不复述菜名＋宽档五块同宽同左沿）＋ 族带负外边距与窄档内距的硬耦合读数
f002fa08 docs(873): 录入席证据件补第三轮提交记录与包内测试那条非本席红线的归属
66570077 feat(873): 搜索筛选席第三轮（删页内重复装饰带／删条件卡题头句与状态重印／宽档左栏改单栅格项＋按行距收首屏空白）
```

## 五 未做项与下一步（末次验收席）

1. **「48 页逐页 ≥90」未达成**（46 页未达）。缺口两处、都在判据之外：**「图从哪来」未裁决**（数据源无图、不许编造；本批视觉生动均值 16.04）
   ＋**「文案精炼」被库内正文封顶**（本批均值 15.85，全场最低）。⇒ 需维护者裁决，不是渲染层能解。
2. **包内测试红一条**（本轮新出现的机器面红）：`tests 109／pass 108／fail 1`，唯一那条是
   `#873 十处壳接线（③） › packages/skill-chef/src/history/pages.ts 只调 renderSceneShell()，且带一个合法族名`（断言 `这一处没给族名（或给了闭集外的值）`）。
   根因：该用例按**字面量** `/family:\s*'(result|process|receipt)'/` 匹配源码文本，而 `src/history/pages.ts:98-103` 第三轮改成 `docOf(family, …)` 的参数传递，字面量不再出现。
   **两件都在已提交的树里、都不在收口席写集** ⇒ 本席**不放宽判据、不手改**，需公共层席或历史席在自己的写集里收口（或另立票）。
   旁证：录入席 §15.2 与做菜席 §7.9.4 都独立记过这条红的归属。
3. **第 4 格与第 39 格的判读面例外**（§三末）：原图对被后端连挂／被拒答，用同帧无损重编码取读数；下一手若要复核，`reencode.mjs` 可直接重跑（它有逐像素校验）。
4. **抖动带内 9 格两跑都停在 88–89**（4／9／34／35／42／44／45／46／48）：差 1–2 分且两跑逐字相同 ⇒ 是这一版的稳定值，不是读错。
5. **两条结构告警的现状（按工作区实测，该两件 `git status` 干净 ⇒ 工作区＝提交树）**：
   `src/relation/shapes.ts` **150 LF** ＋ 新件 `src/relation/treeShapes.ts` **288 LF**（派生席第三轮按形状族拆两件，**427 LF 那条告警已消**）；
   `src/add/pages.ts` **372 LF**，**仍超本包 350 行告警线**（录入席第三轮已把它从 448 压到 372，但未拆）。
6. **7 个受版本控制的册子片段 JSON** 由重出命令连带重写——十席都报备过、都没提交，**由本席在本轮同一批提交里处置**
   （提交前 `git diff --cached --name-only` 复核只含那 7 件 ＋ 本席改的三份 `t873-*.md`）。
