# t777 收口 A 证据：正反例退出码 ＋ 覆盖对账读数

票：[#777](https://github.com/FeatherHunter/ilife/issues/777)（地图 [#765](https://github.com/FeatherHunter/ilife/issues/765) 票 12）。
前置：7 张域票 #770–#776 已全关（查验 2026-09-21：6×CLOSED＋#770 CLOSED；本票阻塞条件满足）。

## 1 正例读数（必跑，全部 exit 0）

| # | 命令 | 输出 | exit |
|---|---|---|---|
| 正1 | `node tooling/run-locked.mjs --ticket 777 -- node docs/skills/skill-chef/t777-run-all.mjs` | `卡 48／复制 48／变体 4／HELP 1／缺失 0` | 0 |
| 正2 | `node tooling/run-locked.mjs --ticket 777 -- node docs/skills/skill-chef/t777-链路总表.mjs` | `词 50／卡 48／链接 50 条／缺失 0`＋`OK：50 词条条有产物，48 卡逐卡有落点，链接无死链` | 0 |
| 正3 | `node tooling/run-locked.mjs --ticket 777 -- node docs/skills/skill-chef/t777-链路总表.mjs --open-check`（真 Chrome 逐行打开） | `词 50／卡 48／链接 50 条／缺失 0`＋`可打开 50／死链 0` | 0 |

## 2 反例读数（必跑，全部 exit 1 并点名）

反例一律用临时副本跑（`.scratch/t777-tmp/`，真文件一个不动；`--manifest`／`--asset`／`--out` 三个覆盖位就是为反例留的）。

| # | 做法 | 输出（含点名） | exit |
|---|---|---|---|
| 反1 | `manifest-badname.json`（`cooking_start_fresh` 文件名加 `-不存在` 后缀）＋ `--out` 临时目录 | 首行 `册子：册子落点缺失（cooking_start_fresh）：…cooking_start_fresh-不存在.html`，另有 `行：行产物缺失（做菜模式／开始做菜）` | 1 |
| 反2 | 资产副本删整行 `{ phrase: '体检' … }`（`--asset` 指副本） | `词 49／卡 47／链接 49 条`＋`缺卡：data_quality_report`＋`缺词：体检` | 1 |
| 反3 | `manifest-badpath.json`（`view_full_recipe` 指向 `t777-tmp/根本没有这个文件.html`）＋ `--open-check` | `册子落点缺失（view_full_recipe）`＋三行 `点不开：file://…根本没有这个文件.html（loc=chrome-error://chromewebdata/）`（浏览器真去开了，落到错误页为证） | 1 |

注：反例用 `--out` 临时目录时，HELP 与 4 个变体文件不在该目录（它们只由 run-all 建在真批目录），故附带多行无关缺失；判定只看首行点名＋exit。真批目录跑正例时 `缺失 0`。

## 3 覆盖对账读数

- 唤醒词 50＝老组名 33（可路由 20＋不可路由 13，域票已实现）＋新表多出词 13＋HELP 词 4（资产 `chef-scenes.ts`，`t767-对账.mjs` 复核 `词 50／卡 48／差集 0／slug 冲突 0`）。
- 卡 48＝7 片段行数 8＋13＋5＋6＋4＋4＋8（t775 用 hist-1…4 别名，映射见 §4），合并时同一 id 出现两次即红（本次 0）。
- 验收副本 `.scratch/t777/`：HTML 54 份＝canonical 48（`<域中文名>/<slug>.html`，t767-命名 §二）＋变体 4（§三.2）＋HELP 1＋链路页 1；`manifest.json` 48 格（同一份产物只记一格）；链路页 50 行（表头外 50 个 `<tr>`）。
- 链路页硬约束：相对 href 0 条（链接一律 file:// 绝对路径）＋`loading=` 0 处（脚本内断言，命中即红）。

## 4 口径对账（与票面目标逐条对）

1. **目标 1（批量驱动器逐条真跑）与写集表（收口 A 只读 7 份片段）的取舍**：逐条真跑的事实发生在 7 张域票（各自副本库＋运行脚本＋证据件 §1 读数）；收口驱动器逐条重验（源存在＋复制＋bytes/sha256 重算＋`</html>` 标记复核）并落验收副本，不再重复写库（重复写会污染各票副本且越写集）。唯一在收口真跑的是 HELP（dist 纯渲染＋explicit 落点，零 DB）。
2. **目标 3（同一套 slug 规则）**：canonical 与变体命名逐字照 t767-命名 §二／§三.2；9 条复用词（看菜谱／看菜／搜菜／查食材／加菜／开始做菜／继续做菜／补录做菜／改评分）与 canonical 同文件（§三.2“参数相同复用”），4 条参数不同词（完成做菜／查清单／清空清单／排除可选）各走独立变体路径；HELP 4 词复用同一份 HELP 文件（§三.1）。地图裁决 8 的“50 个不重复路径”按 §三.2 落：复用词共享 canonical，变体词路径独立。
3. **t775 别名**：hist-1→record_cook、hist-2→view_history_list、hist-3→view_stats_dashboard、hist-4→view_stats_global（t775-历史域.md §1 表格）。
4. **不碰真库**：两脚本零 SQLite（grep `openChefDb|sqlite|better-sqlite|ILIFE_CONFIG_DIR` 零命中）；真库 `D:\2Study\StudyNotes\.db\chef_data.db` 跑后仍 294912 B／mtime 2026-08-09 13:38:15，未动。隔离通道声明 `CHANNEL-PENDING-#756`（收口未新建通道，不抢 #756 的裁定）。
5. **不改命令面**：`packages/skill-chef/src/**` 零改动（`git status` 作证）；路径由驱动器自选 explicit 落点。
6. **中途修过一个真缺陷**：`--open-check` 首版把 CDP `Runtime.evaluate` 回包拆错（按 `st.value` 取，实为 `st.result.value`），50 行全判死链；探针直连证明浏览器本可打开，修拆包＋死链判据（落到 `chrome-error://` 即死）后 `可打开 50／死链 0`。教训已记进脚本注释（判据段）。

## 5 交付物

- 器械（入仓）：`docs/skills/skill-chef/t777-run-all.mjs`、`docs/skills/skill-chef/t777-链路总表.mjs`
- 产物（不入库，`.scratch/` 惯例）：`.scratch/t777/`（54 份 HTML＋`manifest.json`＋链路页）
- 证据：本件 `docs/skills/skill-chef/t777-收口A.md`

## 6 遗留出口

- 墙与终审 → 收口 B [#778](https://github.com/FeatherHunter/ilife/issues/778)（`manifest.json` 即墙清单来源，共用同一套 slug）。
- 变体 4 词的内容目前复用宿主卡产物（命令分支待实现，链路页 prompt 列已逐行注明）；若收口 B 视觉审查认为不妥，在对应域票开缺陷，不在这张票里修。
