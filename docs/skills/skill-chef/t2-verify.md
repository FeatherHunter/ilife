# t2 整改验证

> 被验对象：`docs/skills/skill-chef/t2-content-reconcile.md`（**149,623 B／1,347 行，全 LF、无 BOM**，票 #210）。
> 验证席：只读。**没改任何文件**（含被验报告与两份审查报告）、没 `git add`／`commit`／`checkout`／`stash`、没跑仓级 `pnpm build`／`pnpm test`／snapshot 门、**没跑 `chef-cmd-read`**、没联网。
> 手法：`JSON.parse` 老载荷逐字段重算；`node` 正则解析 `wakewords.ts` 数 37 词；自建十份域文件行号地图后把报告里**全部 177 处** `scenes/*.yaml` 引用逐条回原文核对；`Get-FileHash` ＋ `certutil` 双算 SHA-256；逐行读 `help-template.html`／`help.ts`／`helpShell.ts`／`cmd_read.ts`／`wake-assets.ts` 的原行。
> 实验件：`D:\ilife\.scratch\chef-help\t2-verify\`（`v1-payload.cjs`／`v2-html-variants.cjs`／`v3-chip.cjs`／`v4-lines.cjs`／`v5-allrefs.cjs`／`v5b-allrefs.cjs`／`v6-format.cjs`／`v7b-tables.cjs`／`v7c-tables.cjs`／`v8-counts.cjs`）。

## 判定

- 结论：**需再修 3 处**
- 一句话：B 席 5 条硬伤**全部真落地**（H1／H3／H4／H5 逐项为真，H2 主体为真、四类规则漏 11 键），A 席 7 条 **7/7 已处理**、SHA **双算法复算为真**，**行号判定整改席对、A 席错**（场景级原值即真值，96 处逐卡回核不合 = 0）——但当前版**仍有 2 处场景级引用指向 `- id:` 行**、`dimensions` **11 键无归属**、**自检 6 的 EOL 记录已成假话**；把这三处改掉即可关票，现在这版**还差一口气**。

## 一、B 席 5 条落地核对

| # | 整改自称 | 我的独立证据 | 已落地？ |
| --- | --- | --- | --- |
| H1 | §三 加第 9 列；3.1 覆盖 20/37；3.2 三选一＋代价＋标「需用户裁」 | 第 9 列表头在 `:213`，**9 列 × 48 行，列数不一致 = 0**。我自解析 `wakewords.ts` 得 **37 词**（`L13–L49`），与 33 个老组名求交：**20 个逐字同名**（清单与 3.1 列出的 20 个**逐字一致**）、**13 个不在新表**（清单亦逐字一致）；按 `$.wake_words[].scenarios[]` 数卡得 **34 认／14 不认**，14 张不认卡的 `scenario_id` 集合与 §三 ✗ 列、3.2 表**三方全等**（`导入食谱` 确为 2 张：`import_from_json`／`import_validation_failed`）；域内条数 **5/8/13/4/4/1/6/3/1/3 = 48** 与报告一致；3.2 给甲/乙/丙三路＋逐路代价，末尾 `:342`「**本报告不替用户裁**」 | ✅ |
| H2 | 迁 `editable_fields`，四类逐键规则 | 对应位、四类规则、畸形键与 2 条空对象处置、归属票、「不做的后果」**全在**（`:623-646`）；映射口径表补了 `dimensions` 行。我 `JSON.parse` 复核：**48/48 有键、46/48 非空、80 条键值对、键并集 42（合法 41 ＋ 畸形 `默认不含)`）、2 张空对象恰是 `list_all_recipes`／`first_use`**，高频 `recipe` 23／`input` 6／`focus` 4／`scope` 3（其后 `action`／`cuisine`／`ingredient`／`keyword`／`time_max`／`servings` 各 2）——**逐项全对**。⚠️ 但四类只覆盖 **31/42 键**，**11 键无归属**（见「七」第 1 条） | ⚠️ 主体落地 |
| H3 | 6 项不迁清单 | 表在 `:650`，**6 数据行**：`result`／`variants`／`html.*`／`aliases_expanded_count=37`／4 条别名／载荷侧 `domain`，**六项齐**。理由抽验为真：`result`「模板全文不读它」——`help-template.html` 里 `result` 唯一命中是 CSS 类名 `.t-result`（`:81`）；`aliases_expanded_count` 照抄是假数——`:1772` hero 用的是 `ALL.length`（场景卡总数）；`variants` 零内容损失——96 个键位**全是空数组、0 元素**；4 条别名契约无位——`help.ts:41` 是单个 `wake_word: string`。把老载荷 10 键过一遍：**无「该进却没进」的字段**（7 键进映射口径表、3 键进不迁清单、`dimensions` 有落法）；只有 `generated_at`（构建时间戳，×2）未提，无内容风险。`html.*` 口径与 t1 一致（t1 `:257` 不整块进新页、`:245` 只作交叉校验） | ✅ |
| H4 | 5.1 表 13 行（8 复用／4 新写／1 参数升格）＋ bill 先例 | 表在 `:505`，**13 数据行**；逐行核结论列：8 条判「老卡近义触发／改名」（看菜谱／看菜／搜菜／加菜／开始做菜／继续做菜／补录做菜／改评分）、4 条判「真新条目」（查食材／完成做菜／查清单／清空清单）、1 条判「参数升格不造卡」（排除可选）——**与汇总句 8/4/1 完全一致**。bill 先例 `wake-assets.ts:12-16` 实读：`L13-14` 新增 3 条场景「现 `WAKE_TABLE` 比老 HELP 多出的 3 条……补进对应域」、`L15`「`prompt_template` 按老实样重写（`____` 空槽 ＋ `(唤醒词:…)` 尾注）」——**引用命中**。⚠️ 规矩的另一半「**接在对应二级组末尾**」的原文在**同文件 `:54`**，不在 `:12-16` 内 | ✅（引用范围小瑕） |
| H5 | 票 6 入参表，逐项标「读了不渲染／真渲染」 | 表在 `:1018`，**10 数据行**（`skill_name`／`title`／`subtitle`／`contact`／`groups`／`version`／`meta_blocks`／`init_banner`／`recommendations`／`about_extra`）。**标注逐项实测为真**：`subtitle` 全模板仅 `:1650` 一处（`var SUBTITLE=…`）→ 读了不渲染 ✅；`meta_blocks` 仅 `:1651` → 读了不渲染 ✅；`recommendations` `:1656`／`:1819`／`:1821-1823` → 真渲染 ✅；`contact` `:1653`／`:1801`（`CONTACT.items.length` 为条件）✅；`init_banner` `:1775-1776` 真渲染 ✅；`version` `:1654` → `:1817` 关于 Tab「`v0.1.0 · HELP 模板 v4`」真渲染 ✅；`groups` `:1830` 真渲染 ✅；`about_extra` `:1655` 全文未被使用 ✅。附注亦实测为真：`helpShell.ts:16` 接入示例恰 5 项、`:22-28` 接口恰 5 键、`:17` groups 空抛 `missing-data`；`recommendations` 字段名不一致（模板读 `r.name`／`r.desc`／`r.wake`，契约 `help.ts:86-90` 写 `reason`／`wake_word`）✅ | ✅ |

**B 席 10 问逐条**：报告「整改记录」的处置列与我复核结论一致（问题 1／5／8／9／10 的落地见上表；问题 2／7 见「四」的 12 条无落点行；问题 6 见 `6.4①`）。

## 二、A 席 7 条核对（含 SHA 重算）

A 席 7 条按它自己 §二 的编号（F1 dimensions／F2 另 4 字段／F3 行号／F4 35＋13／F5 `场景合并.py` 行号／F6 配色／F7 伪断言），另加 6 处须修正（W2–W6 ＋ SHA）。

| A 席 | 整改自称 | 我的独立证据 | 已处理？ |
| --- | --- | --- | --- |
| F1 `dimensions` | 补落法 | 已补，见 H2；⚠️ 11 键缺口 | ✅（有缺口） |
| F2 另 4 字段 | 开「不迁清单」 | 6 项齐，见 H3 | ✅ |
| F3 行号 | 文件级 78 处 −1；场景级 96 处撤回误改 | 文件级 **79 处引用 79/79 落在文件级 `domain:` 行**；场景级 **96 处不合 = 0**；**但 2 处漏网**（见「三」） | ⚠️ 半 |
| F4 35＋13 | 改「同一批 48 条按有无 `domain` 拆成 35＋13」 | 标题（`:344`）＋正文读法（`:346`）＋自检 6 D4 三处一致；我算：主表 35 条、附表 13 条、**交集 0、并集 48**、主表逐 id == 载荷 48 减附表 13 | ✅ |
| F5 `场景合并.py` 行号 | A 席自己降级为提示，未动 | `:29-30` `PATCH_FIELDS`（不含 `domain`）✓、`:61` 命中补丁循环 ✓、`:74` 追加写 `domain` ✓；`:131` `domain=(data.get("domain") or {}).get("name", …)` 确证写的是**文件级 `domain.name`（中文名）** ✓ | ✅（无需改） |
| F6 配色 5→4 | 改 4 个（`勾选`／`对比`／`确认`／`转移`） | 我读 `TYPE_DEFAULT`（`:1698-1709`，10 键）与草案实际用到的 9 个原子（向导／选择／回执／查看／对比／确认／采集／勾选／转移）求差集 = **恰 4 个：勾选／对比／确认／转移** ✅；`下载` 确在 `转移(下载) → ['转移']` 被丢，从不进草案 ✅ | ✅ |
| F7 伪断言 | 换真断言（载荷 `scenario_id` 集合 == 映射后 `id` 集合） | 载荷 48 卡**无 `id` 键**（实测 `has_id_key = false`，键名是 `scenario_id`）→ 旧断言确为空集对空集；新断言我独立复算：两侧各 48 个唯一值、**集合全等 = true** | ✅ |
| W2 分节 9 vs 14 | 统一为 14 | `wake_word_variants.md` 实测 **14 个 `###` 分节**，行号 `L19/38/58/77/96/119/123/127/128/134/144/152/156/159` **逐字全对**；文件 194 行 ✓ | ✅ |
| W5 映射口径表 7 行 | 补齐为 **13 行** | 表在 `:606`，新增 `dimensions`／`result`／`html`／`variants`／`domain` 五行、末行改写**均已做**；⚠️ 但实测 **14 数据行**，与自称「13 行」差 1（自检 5／6 都没查这张表） | ⚠️ 半 |
| W6 `domain` 值域 | 补「总账存中文名、域文件场景级存英文 key」 | 载荷侧 13 条 `domain` 实测值**全为中文名**（8 种：搜索筛选／修改／历史／采购／录入／派生／开始使用／数据管理）✓；域文件场景级为英文 key ✓；两处都已点明（`:53`、`:347`） | ✅ |
| §六 SHA－256 | 改 `C09F11D9FFA49E6B`，补实测 mtime | **`Get-FileHash` 与 `certutil` 双算**：`C09F11D9FFA49E6B14C2ADE094B5AB428F22FD440B2608442166E470DB47D2AB` → **前 16 位 `C09F11D9FFA49E6B` 为真** ✅；字节 **74,581** ✅；mtime **2026-09-12 10:11** ✅。9.3 指纹表其余五项（`wakewords.ts` 4,809／`cmd_read.ts` 22,335／`lookup.ts` 2,998／`fetch/index.ts` 570／`SKILL.md` 7,756 B·78 行）**逐项与实际一致** | ✅ |

**附注两条（不抵整改）**：① A 席报告**自身**的 F 编号不一致——摘要清单写 F4＝`domain.key` 列名错标、F5＝35＋13，而它 §二 标题写 F4＝35＋13、F5＝`场景合并.py`；t2 用的是 §二 编号，故 t2 的「A 席 F4」指 35＋13 **是对的**。② A 席摘要里那条「`domain.key` 列名错标」在 t2 里**未被单独处理**：§一 附表列头仍写「⑤`scenes/<域>.yaml` 文件级 `domain.key`」，而格内值已指向 `domain:` 行并用正文给出 `key:` 行——单元格已消歧，列头残留旧标签（低危）。

## 三、行号判定专节（文件级 vs 场景级：谁对，当前版是否全部指向正确对象）

**谁对：整改席那半条判对，A 席那半条判错。**

`D:\2Study\StudyNotes\SKILLS\私家大厨\scenes\做菜.yaml` 逐行（L30–L50 区段）：

```
L36: domain:          ← 文件级 domain:
L37:   key: cook
L38:   name: 做菜
L42: scenes:
L43:   - id: "cook-1"  ← A 席给的「场景级 −1 = 43」落在这一行
L44:     domain: cook  ← 场景级 domain: 的真行
L46:     wake_word: 做菜模式
```

⇒ A 席的 43 指向 **`- id:` 行**，不是 `domain:` 行。**A 席错，整改席对**：场景级引用原值就是真值。

**自建十份域文件行号地图后的逐条回核**：

| 引用形态 | 处数 | 核对方式 | 不合 |
| --- | --- | --- | --- |
| 文件级 `scenes/X.yaml:NN` | **79** | NN 是否 = 该文件的文件级 `domain:` 行 | **0** |
| 同行二级 `:NN` = `key:`／`name:`／`icon:` | **89** | NN 是否 = 同行的 `key`／`name`／`icon` 行且值逐字相同 | **0**（第 89 处是散文里描述旧方案的 `scenes/*.yaml:37`，非真文件，不计对错） |
| 场景级 `L NN`（§二 33 组行 ＋ §二 表内 33 行 ＋ §四 主表 35 行） | **96** | 按载荷的组→卡顺序映射到域文件卡，逐卡比该卡**卡内 `domain:` 行** | **0** |
| 硬编码注释引用 | 2 | `scenes/采购.yaml:9` = 「…**排除可选** = 生成清单的 4 个参数旋钮,不是独立场景」✓；`scenes/修改.yaml:13` = 「难度/份量/多字段 = 修改食谱的参数特例,不占独立卡」✓ | **0** |

**但当前版不是「全部指向正确对象」——有 2 处错：**

- `:572`（6.4①）与 `:1003`（§八 第 4 条）都写「`scenes/数据管理.yaml:48`（该卡场景级 `domain: data`）」；
- 实测 **L48 = `- id: "data-1"`**，该卡场景级 `domain: data` 在 **L49**（该文件文件级 `domain:` 在 L41、`key:` L42、`name:` L43、`scenes:` L47）；
- 这两处**与报告自己的正确说法打架**：`:157` 与 `:192` 两处都写「`体检` 组场景级 `domain:` 行 = **L49**」✓；
- 自检 6 [D8] 把它们归进「非『文件级』语境的 scenes 引用（注释行／整改说明引用的旧值等，**不判对错**）：9 处」而放过（我数该类实为 10 处：8 处注释 ＋ 这 2 处），所以自检报了「三处不合均为 0」，实际上按「是否指向正确对象」判它们**是真错**。

**结论（只认一条）**：**当前版行号引用不是全部指向正确对象——177 处里 175 处对、2 处错**，错的两处是同一个错值（`数据管理.yaml:48` 应为 `:49`）。整改席推翻 A 席的那半条判定**成立**；但「撤回误改、恢复原值」的脚本只认 `L NN` 形态，漏掉了这两处 `scenes/X.yaml:NN` 形态的场景级引用。

## 四、内容不丢总账核对表

| 老件里的东西 | 报告给了落法或明确不迁？ | 我的独立证据 |
| --- | --- | --- |
| `dimensions`（42 键） | **半**：对应位＝`editable_fields`＋四类规则在，但 **11 键无归属** | 48/48 有键、46/48 非空、80 条、键并集 42（合法 41＋`默认不含)`）、空对象 2 张 = `list_all_recipes`／`first_use` —— 全对；四类覆盖 31 键，剩 `cookware`／`difficulty`／`extra`／`group_by`／`history`／`ingredient_exclude`／`ingredient_swap`／`progress`／`step_type`／`time_max`(2)／`user_state`＝**11 键 12 条键值对无处置** ⚠️ |
| `result`（48/48、1,970 字） | ✅ 不迁清单 | 我算：48/48 非空、字符合计 **1,970** —— 与报告逐字相同；模板侧确无此位（唯一 `result` 命中是 CSS 类名） |
| `html.*`（4,301 字） | ✅ 不迁清单 | 我算：48/48、子键恒 `template`／`command_cn`／`data_source`、`template` **18 个**路径 ✓；字数用 Python `json.dumps` 默认分隔符（`, `／`: `）口径**复现出 4,301**（紧凑 JSON 口径为 4,061，差值 48×5）＝报告的数可复算 |
| `variants`（恒空） | ✅ 不迁清单 | 48 张卡键都在、**元素合计 0**、非空卡 0（「96 处」的口径问题见「五」第 4 条） |
| 4 条别名 | ✅ §6.1＋不迁清单 | 挂 `$.wake_words[0]`（`开始做菜`）与 `[19]`（`不想要`／`删掉`／`废弃`）✓；`开始做菜` 在新表已升为独立词 ✓ |
| `aliases_expanded_count=37` | ✅ 不迁清单 | 实测 **37** ✓ |
| 单数 `type` 的 11 型 | ✅ §八 第 2 条＋§九.2 TYPE_MAP | 实测 **11 型**、逐型分布与 TYPE_MAP 段**逐字相同**（向导+选择+回执 5／查看 25／查看+选择 1／对比+确认+回执(过程型) 3／确认+回执 1／采集+回执 7／查看+勾选(过程型) 1／采集+确认+回执 1／向导+回执 1／采集+确认+回执(过程型) 2／转移(下载) 1） |
| `{{菜名}}` 与 `{{N}}` | ✅ 映射口径表写「`prompt` → `prompt_template` 逐字（含填写位）」＋ `:66` 把「转不转」指向 t3 §3.5 口子 2 | 实测 **16/48 卡**带占位符：`{{菜名}}` 15 ＋ `{{N}}` 1 = **16 次**；⚠️ 报告**全文没给这个量**（只有「这类填写位」） |
| 12 条无落点卡 | ✅ §三「无落点的 12 条」表＋§八 第 3 条 `status` 后果 | 表 12 行，与 §三 判「无」的 12 条**集合完全相等**；§八 写明「标 `''` 与可用卡外观完全相同，只有 `【待开发】` 出徽章」（`:1667`／`:1726` 实测为真） |
| 3 条仅老文档有 | ✅ §6.3 | `修改难度`＝`wake_word_variants.md:148` ✓、`修改份量`＝`:149` ✓、`排除可选`＝`:157`（属 `### 采购清单` L156 分节）✓ —— 行号逐条全对 |

**总账结论：10 项里 9 项说清、1 项半说清（`dimensions` 的 11 键）。**

## 五、新引入的矛盾

1. **【实质】`scenes/数据管理.yaml:48` 与报告自己的 L49 打架**（`:572`、`:1003` 两处；真值 L49）——见「三」。
2. **【实质】自检 6 [D10] 与文件现状不符**：D10 记「150970 字节；行数 = 1348；CRLF = 1347；BOM = false」，结论第 5 条写「**CRLF 无裸 LF、无 BOM**」；编排方归一成 LF 后实测为 **149,623 B／1,347 行／CRLF = 0／全 LF／无 BOM**。格式本身**合规**，是**记录**成了假话。
3. **【记录】映射口径表行数**：整改记录称「补齐为 **13 行**」，实测 **14 数据行**（自检 5／6 均未查该表）。
4. **【口径】`variants` 的「96 处」**：写在 `$.scenarios[]` 前缀下应为 **48**；96 是整个载荷里 `variants` 键的出现数（48 顶层 ＋ 48 `$.wake_words[].scenarios[]` 嵌套视图）。同一文件 `:1230` 自己写过这个 96 计数陷阱，零内容损失的结论不变。
5. **【引用】bill 先例范围**：`wake-assets.ts:12-16` 只覆盖「`prompt` 按老实样重写」；「接在对应二级组末尾」的原文在**同文件 `:54`**。
6. **【判据内部不一致，可争】5.1 的 `查食材`**：判「真新条目」，理由只比了老「查看食材」与「筛选食材」两卡；但它落的 `recipe.search` 槽位 `q`，同一张表下一行 `搜菜` 正因「与 `search_by_name_keyword` 同命令同槽位」判复用，而该老卡标题逐字写着「关键词搜索(**菜名/食材**)」——按 5.1 自己的判据 A，`查食材` 更应判复用。不丢内容，只影响票 5 多写一条文案。
7. **【引用范围】`docs/agents/structure.md:72-88`**：L72 是「文件行数告警线」、L74 才是「必报五步」标题、L78 第一步、L85 第二步（到 L90 止）。引「票 6 第一／二步」更准的是 `:78-90`。
8. **【时点】9.3 指纹表 `SKILL.md` mtime 写 `2026-09-12 11:25`，实测 `11:43`**（字节 7,756／78 行仍对得上，HELP-AUTO `L31–L73`、表格数据行 `L34–L70` = 37 行实测全对）——报告已自带「引 `SKILL.md` 行号时以当次文件为准」的免责声明。
9. **【自指】旧 SHA 值在交付件里出现 3 次**（自检 D9 记「2 处」；第 3 处在 D9 自己贴出的输出行里）。无实质影响。

**跨票行号漂移（本次重点）**：**未发生**。t2 引 `t3-template-contract.md` 一律用**节号**（§3.5 口子 1／口子 2），引 t1 只用**文件名**（`:71`）——`grep` 全文**没有一处** `t3-template-contract.md:NN` 或 `t1-bill-recipe.md:NN` 形态的引用。t1（120,846 B／964 行）与 t3（113,132 B／816 行）改版**不造成任何漂移**。

**章节号与同事实数字**：31 个标题，**无重复**；正文一～九连续无断号（整改记录内部的一～四与正文同号，但都用父级前缀「整改记录 · 三」消歧）。关键数字在两处以上出现时**全部同值**：48/33/10、34 认／14 不认、13 个不认词、20/37、33/37、42 键、80 条、46/48、1,970 字、11 型——逐处核对无冲突。

## 六、格式与自洽

- **无 BOM** ✅（首三字节非 `EF BB BF`）。
- **全 LF** ✅：`CRLF = 0`、`LF = 1347`、无 CRLF 残留。与仓库文档口径一致。
- **字面 `\n` 30 处，全部合法**：27 处在第七节 ` ```json ` 草案里（`prompt_template` 值的 JSON 转义，正确写法）；1 处是「整改记录」散文里说「用 node 按 `\n` 读十份域文件」；2 处在 ` ```text ` 引用块里逐字转录老载荷片段。**无裸的假换行**。
- **表格**：全文 **30 张表，列数不一致的行 = 0** ✅（第三节 9 列 × 48 行、第五节 5 列 × 15 行、票 6 入参表 5 列 × 10 行、映射口径表 3 列 × 14 行、第一节 5 列 × 10 行、第二节 4 列 × 33 行、第四节附表 5 列 × 13 行、不迁清单 4 列 × 6 行、3.2 表 3 列 × 13 行、12 条表 4 列 × 12 行、5.1 表 6 列 × 13 行）。
- 括号／引号／粗体标记未发现未闭合（Markdown 渲染层面无异常）。

## 七、票面覆盖与可用性（含与 t3 口径一致性）

**`t2-body.md` 6 问：6/6 打勾**

| # | 票面要什么 | 落点 | 判 |
| --- | --- | --- | --- |
| 1 | 老 48 条每一条在新表里的落点（有／无／改名／合并） | §三 表 48 行 × 9 列（有 20／改名 1／合并 15／无 12） | ✅ |
| 2 | 那 35 条没有域归属的卡逐条归域＋三处佐证 | §四 主表 35 行（另如实标注 `wake_word_variants.md` 只覆盖 8/10 域、`render_*.py` 中文名只覆盖 5/10 域） | ✅ |
| 3 | 新表多出的条目补进哪个域、哪个组 | §五 表 15 行 | ✅ |
| 4 | 4 条别名进不进 HELP、进了写在哪 | §6.1（逐条给「新表有没有／命令层能不能承接」＋契约无字段位这一硬事实） | ✅ |
| 5 | 载荷有老文档没有的 6 条 ＋ 文档有载荷没有的 3 条 | §6.2／§6.3（3 条与票面点名的 `修改难度`／`修改份量`／`排除可选` 完全一致） | ✅ |
| 6 | 10 个域的中英名对照表 | §一 表 10 行（中文＋英文＋场景数＋三处佐证） | ✅ |

产出物也对：逐条对账表（§三／§四／§五）＋ 可入库骨架的机器可读草案（§七 JSON，`json.loads` 可解析，三层 10／33／48）。

**以「票 5 的施工图 ＋ 票 6 的接口」为标准，还缺什么（逐条）**

1. **【最实质】`dimensions` 四类规则漏 11 键**：票 5 照「逐键归位四类」施工，`time_max`(2)／`difficulty`／`cookware`／`ingredient_swap`／`ingredient_exclude`／`progress`／`step_type`／`user_state`／`history`／`group_by`／`extra` 共 **12 条键值对**会静默丢——正是本票最可能翻车处。t3 §3.5 口子 1 **同一缺口**（两票同口径但同缺，不相抵）。
2. **2 处行号错**（`数据管理.yaml:48` 应为 `:49`）：直接照抄会把「体检」卡定位到 `- id:` 行。
3. 草案 JSON 仍是 **6 键**、不含 `editable_fields`、无 `t185` 那样的溯源字段（`prompt_source`／`origin`）——报告已在「四、仍未闭合项」第 1 条自认，归票 5。
4. 12 条新条目只有「内容来源判据」，**没有成文的 `title`／`prompt_template` 文本**；报告自认归票 5，但 5.1 已给到「复用哪张卡／必须新写／不造卡」三档，可施工 ✅。
5. `subtitle`／`contact` **无源可搬**（报告明写「需用户给值」，不编造）；`meta_blocks`／`init_banner`／`recommendations` 由票 6 供或本图不做——口径与 t3 一致 ✅。
6. 三件待用户／结构设计闸门票裁的事（chip 三选一、`dimensions` 整体进不进契约、12 条卡的 `status` 标法）**都挂在了正确票上，未越权代裁** ✅。
7. `{{菜名}}`／`{{N}}` 的「转不转」本报告不给结论、指向兄弟件 t3 §3.5 口子 2（跨票，可接受）；但**报告未给出「16 条」这个量**。

**与 t3 的 `dimensions` 落法口径是否一致：逐条一致 ✅**

| 口径点 | t2 §七 | t3 §3.5 口子 1 | 一致？ |
| --- | --- | --- | --- |
| 对应位 | `editable_fields`，**不是 chip** | 同 | ✅ |
| 键→／值→ | 键→`name`（英文原词）；值→`hint` | 同 | ✅ |
| 另补两键 | `label`（中文，可沿用老 33 组名／域名词表）＋`required`（老值带「必填／选填」可派生） | 同措辞 | ✅ |
| 实测数 | 48/48 有键、46/48 非空、80 条、42 键（合法 41＋畸形 `默认不含)`，值 `null`，卡 `data_export_backup`）、2 张空对象、高频 `recipe` 23／`input` 6／`focus` 4／`scope` 3 | 同（t3 高频「各 2」只列 4 个词，t2 列全 6 个 → **t2 更准**） | ✅ |
| 模板读法 | `:1669-1671`→抽屉表单、复制正文追加 `label: value`（`:1919-1924`／`:1753-1764`） | 同机制（另给 `:1747-1751`／`:1920-1927`） | ✅ |
| chip 只取 `s.wake_word` | 明写「`dimensions` 任何一键都不进 chip」 | 逐字同 | ✅ |
| 归属票 | 转换属票 5；「要不要整体进契约」是跨票，**须结构设计闸门票收敛** | 逐字同（t3 还点出「票 2 草案要不要从 6 键扩到 7 键」） | ✅ |
| 甲类键集合 | 28 键 | 28 键、集合完全相同 | ✅（**同缺 11 键**） |

## 八、判决与理由

**判决：需再修 3 处。**

| # | 性质 | 要改什么 | 为什么它挡住关票 |
| --- | --- | --- | --- |
| 1 | 实质（内容不丢） | `dimensions` 的「逐键归位四类规则」补上漏掉的 **11 键 12 条键值对**（`cookware`／`difficulty`／`extra`／`group_by`／`history`／`ingredient_exclude`／`ingredient_swap`／`progress`／`step_type`／`time_max`(2)／`user_state`）；t3 §3.5 口子 1 同步改 | 票 5 照四类构造 `editable_fields` 会**静默丢**这 12 条——页面不报错、快照不断言，正是本票点名的翻车模式 |
| 2 | 实质（行号） | `:572`（6.4①）与 `:1003`（§八 第 4 条）的 `scenes/数据管理.yaml:48` 改 **`:49`** | 两处指向 `- id: "data-1"` 行而非场景级 `domain: data` 行，且与本文件 `:157`／`:192` 的 L49 **自相矛盾** |
| 3 | 记录（自洽） | 自检 6 [D10] 与结论第 5 条：`150970 字节／1348 行／CRLF = 1347／「CRLF 无裸 LF」`改为 LF 口径实数（`149623 字节／1347 行／CRLF = 0／全 LF`） | 交付件里留着一条与文件现状相反的自检结论，下一轮验证必挑 |

**低危、可一并清（不单独挡关票）**：映射口径表「13 行」实为 14 行；`variants` 的「96 处」口径；bill 引用补 `:54`；`structure.md:72-88` 改 `:78-90`；5.1 的 `查食材` 判据；9.3 指纹表 `SKILL.md` mtime；`{{菜名}}`／`{{N}}` 补「16 条」这个量。

**理由（正面）**：这份整改是**真落地**而不是「补了一段话」——B 席 5 条硬伤里 H1／H3／H4／H5 的每一个数我都自己重算过（37 词求交 20、34 认／14 不认、13 个不认词、14 张卡逐卡集合、42 键、46/48、80 条、6 项不迁、8/4/1、10 行入参表逐项渲染性），**没有一处对不上**；A 席 7 条 **7/7 已处理**，SHA 双算法复算**为真**，`wake_word_variants.md` 的 14 个分节行号**逐字全对**；行号判定上整改席**推翻了 A 席且推对了**（场景级原值即真值，96 处逐卡回核不合 = 0，A 席的 43 确实落在 `- id:` 行）。跨票引用一律用节号，t1／t3 改版**零漂移**；格式**无 BOM、全 LF、30 张表列数全齐**。

**理由（为何不能就这么关）**：剩下这 3 处恰好都落在「票 5 一开工就照着做」的位置上——`dimensions` 的 11 键是**静默丢内容**，行号两处是**指错对象**，自检记录是**与文件相反**。改这三处是**小改**（补一张 11 行的分类、改两个数字、改一行自检记录），改完不必再走一轮对抗式审查，做一次抽取式复算即可关票。

**能否作票 5 入库与票 6 接口依据**：**改完这 3 处即可**。票 6 侧（chip 路由、`subtitle`／`meta_blocks` 读了不渲染、`recommendations` 字段名不一致、`init_banner`／`version`／`contact` 真渲染、`<details open>` 版面、`filter` 槽位打偏）**本版已可直接用**，与 t3 口径逐条一致；票 5 侧在 `dimensions` 那 11 键补齐前**不能**当逐键施工图用。
