# t3 整改验证

验证日期 2026-09-12。只读验证：未改任何文件、未 `git add`／`commit`／`checkout`／`stash`、未跑 `gen:help-shell`（及其 `:check`）、未跑仓级 `build`／`test`／`snapshot`、未真跑 `chef-cmd-read`、未联网。
实验件在 `D:\ilife\.scratch\chef-help\t3-verify\`。被验件 `docs/skills/skill-chef/t3-template-contract.md`（实测 **111,942 B／814 LF**，与整改自称逐字节相符）。

## 判定

- 结论：**需再修 5 处**
- 一句话：B 席 8 条硬伤的事实层**全部落地且我逐条复现一致**（42＝41＋1、双花括号 16＝15＋1、表外 3 原子、11 Tab、LF 388／451、`AGENTS.md` 不存在、门常量逐字、10 域英文名逐字、11 种 `type`），A 席证伪的 `recommendations` **四处全改对**、哨兵法**已撤换为可达性判据**、6 处行号错**全改对**，`_N` 裁定与两处出处**全对**；但整改把**该件自身的章节号**（`§六`／`§八` 两处指向错节）、**一张表的引用对象**，以及**引 `t2-content-reconcile.md` 的 8 处行号**（全部指向 t2 **改版前的旧行**，实测整体漂移约 125 行）写错，另有 1 处留证件未随正文口径更新——这些是新引入的矛盾，**均不改契约事实与施工结论**，修完即可作为票 6 施工图。

---

## 一、B 席 8 条落地核对

| # | 整改自称 | 我的独立证据 | 已落地？ |
| --- | --- | --- | --- |
| 1 dimensions | 补 §3.5 口子 1（42 键＝合法 41＋畸形键 `默认不含)`，归属 `data_export_backup`；逐键四类归位；标跨票归属） | §3.5 口子 1 在 `:304-329`。我自己 `JSON.parse` 老载荷（`.scratch/chef-help/legacy-chef-help-payload.json`，路径 `$.scenarios`，48 条）数出：`dim_scenes_present 48`／`nonempty 46`／`empty 2`／`key_union 42`／畸形键 `["默认不含)"]`／归属卡 `data_export_backup` 值 `null`；高频 `recipe 23`／`input 6`／`focus 4`／`scope 3`／`action`／`cuisine`／`ingredient`／`keyword` 各 `2`。**42＝合法 41＋畸形 1 成立**（41 个合法键我逐名列过，无中文键、无带括号键）。四类归位表在 `:320-325`：甲 28 键／乙 `scope`／丙 `input`／丁 `默认不含)`；`:327` 明标「跨票归属，需由结构设计闸门票收敛」。另核 `:316` chip 结论：模板 `:1665` `chip: s.wake_word` 是 chip 的唯一来源，`dimensions` 任何一键都进不去 chip | **✅** |
| 2 `{{…}}` | 改双花括号（15＋1、单花括号 0） | 全文单花括号**占位符 0 处**（我自己逐行剥掉合法代码块／类型字面量／正则后核过，残留全是 `{name,label,value,required,hint}` 这类形状记法，不是占位符写作）。**「15＋1」＝16 条 prompt 含 `{{…}}`，其中 `{{菜名}}` 15 次、`{{N}}` 1 次**——我自己 parse 得 `prompt_with_double_brace 16`／`tokens {菜名:15, N:1}`／`total_occurrences 16`／`single_brace_after_strip 0`，**拆法正确**（不是「15 条带花括号＋1 条别的」）。`:331-356` 口子 2 给全样本逐字与甲／乙二选一；模板侧 `\{\{` 全文 0 命中（我自己数 `double_brace_count=0`）也对 | **✅** |
| 3 types | 照 t2 `:408`／`:430` 写成已定（去括号注），表外仅剩 3 个 | §3.5 口子 3 在 `:358-364`、§8.2 在 `:645`，都写成**已定**。**表外原子确实是 `对比`／`确认`／`转移`**：我自己按 `+` 拆老 `type` 再去括号注得原子 9 个（`向导`／`选择`／`回执`／`查看`／`对比`／`确认`／`采集`／`勾选`／`转移`），对照模板 `TYPE_DEFAULT`（`:1698-1709`，9 键 `采集`／`查看`／`结果`／`向导`／`批量`／`校验`／`选择`／`过程`／`回执`），表外恰 3 个。**口径与 t2 对得上**（t2 L571 逐字「按 `+` 拆开、去掉「(过程型)」这类括号注」），**但报告引的 t2 行号 `:408`／`:430` 已失效**（见第五节·跨件行号漂移） | **✅（内容层）** |
| 4 init_banner | 改 §6.3／§10.2.3：不建库＝票 7 已钉死（落点 `cook_html/help/`），票 6 只剩「传不传」＋ `initialized` 由调用方传入 | §6.3 在 `:503-530`：`:527` 明写「**「不建库」不是本票的待选，是已经定死的口径**」（引 `t7-body.md:8`＋`t6-body.md:6`），`:528` 「票 6 侧真正要决定的只剩「传不传 `init_banner`」」＋ `initialized` 由调用方传入（零 IO 纯函数）；`:529` 明写原文的「丙 · 复用 `openChefDb.initialized`」**已删除**。§10.2 在 `:702-719`：**「不建库」已从待选清单里摘出去**（`:706-707` 摘出说明段），待选 8 条里只剩第 3 条「`init_banner` 传不传」。**越界项已消除** | **✅** |
| 5 页面结构预期 | 补 §七整节（11 Tab／关于页 3 段／hero／搜索／抽屉） | **`## 七、页面结构预期` 在 `:582`**（整节 `:582-615`，含 7.1 逐层表 15 行 ＋ 7.2 结构差异）。**11 Tab ＝ 10 域＋关于**：模板 `:1829-1832` 是 `GROUPS.forEach` 出 N 个 ＋ 固定一个 `data-nav="about"`，域 10 个 ⇒ 11，与 `:586`／`:598`／`:615` 三处一致。**关于页 3 段＝①联系作者 ②版本（恒在） ③其他技能**（模板 `:1799`／`:1816`／`:1818` 注释逐号 ＋ `:1801`／`:1817`／`:1819` 三个 `if`／无条件拼），`:602`／`:605` 写对。**搜索框已从「多出的」里移出**：`:609` 老件实测「`tab-bar` 0／`data-nav` 0／`about` 0／`sheet` 0／`init-banner` 0／`<details` 0」我逐个数**完全一致**；`:611` 明写「老件**有**搜索框（`:238-240`…过滤逻辑 `:458`／`:476`），B 席列的「无搜索框」这一项**不成立**」——我自己读老件 `:238-240` 确有 `<div class="search-card">`＋`<input id="search-input" type="search">`，`:458`／`:476` 确有过滤逻辑；`:613` 的「必然多出**四样**」已不含搜索框 | **✅** |
| 6 必报五步 | 补 §10.1＋§11.2 | §10.1 在 `:672-700`（（一）新增件住哪三个候选＋代价；（二）每件对外给什么；（三）必然要动的两处包外件；并声明只给素材不定案）；§11.2 在 `:746-756`（五步报点 ↔ 本报告素材对照表）。两节都在，且 `:674` 引 `structure.md:72-88` 与 `t6-body.md:11-15` 均逐字对得上 | **✅** |
| 7 告警线 | 补 §11.1（350＋LF、`packages/skill-chef/AGENTS.md` 今天不存在、`cmd_read.ts` LF 388／`db.ts` LF 451 当场报）＋§11.3 门 | §11.1 在 `:735-744`、§11.3 在 `:758-769`。**两个行数我自己只数 `\n`**：`packages/skill-chef/src/cli/cmd_read.ts` **LF 388**（CRLF 0）、`src/fetch/db.ts` **LF 451**（CRLF 0）——**逐数相符**。`Test-Path` 全 `packages/*/AGENTS.md`（22 个目录）**皆 False**，`skill-chef` 确不存在。§11.3 描述正确：我读 `tooling/test/skill-html-snapshot.test.mjs:103-104` 得 `EXPECT_KEYS = { bill: 16, chef: 8, home: 21, schedule: 8, memo: 10 }`／`EXPECT_TPL = { bill: 16, chef: 8, home: 21, schedule: 8, memo: 6 }`，与报告 `:764`／`:766` **逐字一致**；`:57` `MARKER_ALLOW` 允许清单为空、`:110-111` 逐技能比对，也都对 | **✅** |
| 8 对齐票 2 | 补 §2.2（`domain.key` 十名）＋§1.3 纠正地图 Notes 的 `scenario_title→name` 错说 | §2.2 在 `:247-261`，§1.3 的纠正段在 `:172`（「⚠️ 地图 Notes 里那句 `scenario_title→name`／`scenario_id→key` 是错的」）。**十个英文名与 t2 逐字一致**：我读 t2 `L77`／`L877`（现行行号）得 `cook`／`view`／`search`／`update`／`history`／`shopping`／`add`／`relation`／`setup`／`data`，与报告 `:257` 逐字相同，顺序、中文对应、域内条数 `5／8／13／4／4／1／6／3／1／3＝48` 也全同。**但报告给的 t2 行号 `:23-34`／`:38-49`／`:412` 已失效**（见第五节） | **✅（内容层）** |

**小结：B 席 8 条，内容层 8/8 落地，我逐条独立复现一致。**（第 3、8 条附带的行号失效属新引入问题，记在第五节，不改变「内容已落地」的判定。）

---

## 二、A 席证伪与方法学核对

### 证伪 ①：`recommendations` 是真渲染 —— 整改自称改了 4 处，**逐处核完，4/4 全改对**

| A 席指出的原位置 | 整改后的位置 | 现在怎么写的 | 判定 |
| --- | --- | --- | --- |
| 结论摘要 4（原 `:15`） | `:83` | 「「读了不渲染」的只有 `meta_blocks`／`subtitle` 两块」＋「页面真显示的是 `version`／`init_banner`／`contact` 与 **`recommendations`**（关于 Tab 第三段）」 | **✅ 已摘出** |
| §1.5 第 5 行（原 `:134`） | `:215` | 「**真渲染**的有 `skill_name`／`title`／`contact`／`groups`／`version`／`init_banner`／**`recommendations`**；**读了不渲染**的只有 **`meta_blocks`** 与 **`subtitle`**」 | **✅ 已摘出** |
| §10 A（原 `:413`） | `:670` | 「⚠️ **`recommendations` 不在「读了不用」名单里**——它是**真渲染**的（关于 Tab 第三段，`:1819-1825`／`:1834`；可达性 `true`），施工按 §3.4 走」 | **✅ 已摘出** |
| 自检 2 · 抽查 β（原 `:465`） | `:804` | 「抽查 β · `meta_blocks`／`subtitle` 不渲染……判这两者 `RENDERED=false`，而 `RECOMMENDATIONS=true`」 | **✅ 已摘出** |

**我自己复核模板，逐字属实**：`RECOMMENDATIONS` 大小写敏感 **4 次／3 行**（`:1656` 声明、`:1819` `if (RECOMMENDATIONS && RECOMMENDATIONS.length) {`、`:1821` `RECOMMENDATIONS.forEach(function(r){`）；`:1822` 把 `esc(r.name)`／`esc(r.desc)`／`esc(r.wake)` 拼进 `h`；`:1834` `screen.innerHTML = h`。对照：`META_BLOCKS` **1 次／1 行**（`:1651` 仅声明）、`SUBTITLE` **1 次／1 行**（`:1650` 仅声明）——**「读了不渲染」现在确实只剩这两项**。

**全文再扫一遍没有漏网**：我 grep `RECOMMENDATIONS` 与 `recommendations`（大小写都扫），共 14 处，逐处读过——除上面 4 处改对的位置外，其余是 §3.4（`:294-298`，原文即正确）、§7.1（`:602`）、§10.2 第 6 条（`:717`，列为待选「传／不传」正确）、§1.3 声明段（`:159`，是模板原文引用）、§1.5 判据段（`:203` 可达性 `true`）与 §1.5 哨兵表（`:212`，标了「不代表不渲染」）。**没有任何一处再把 `recommendations` 列入「不渲染」**。

### 方法学缺陷：哨兵法 —— **已撤换，且撤得干净**

- §1.5 现在**开头就是判据段**（`:202-203`）：「判「渲染与否」**不能**用「哨兵是否出现在静态段」……⇒ 正确判据是**可达性**：该变量有没有出现在 `help-template.html:1771-1834` 的 `h +=` 链上（直至 `:1834` 的 `screen.innerHTML = h`）」；并点名 `contact`／`groups` 铁定渲染、哨兵同样 0 次，故无分辨力。
- **哨兵法只剩「标注过的残留」，不再充当判据**：全文 14 处含「哨兵」，逐处读过——`:205` 明写「下面保留原探针读数（它对「是不是服务端插值」仍然有效，只是**不能**用来推「不渲染」）」；`:212` 那一格写「**静态段 0 次**（⚠️ **不代表不渲染**，见上面的判据）」；`:804` 写「哨兵法实测……**不再用作渲染判据**（它对 `contact`／`groups` 这类铁定渲染的字段同样给 0 次，无分辨力）」。**没有一处再把「静态段 0 次」当渲染判据用**。
- 判据段引的 A 席探针结论 `SUBTITLE false`／`META_BLOCKS false`／`RECOMMENDATIONS true` 与我自己数出的变量行数（1／1／4）**方向一致、无矛盾**。

**小结：A 席 1 条证伪 ＋ 1 处方法学缺陷，处理完整。**

---

## 三、A 席 6 处行号／事实核对

| # | A 席说 | 当前版怎么写 | 我自己复读的事实 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | `packages/skill-chef/package.json` 的 `version` 在 `:3`（不是 `:4`） | §3.2 `:281` 写 `package.json:3`；§6.2 `:500` 写 `package.json:3`（**两处都改了**） | 我读该文件：`L3: "version": "0.1.0",`、`L4: "description": …` | **✅ 已改（两处）** |
| 2 | `pnpm-lock.yaml` 里 `base-paint` 的 `link:` 在 `:136-138`（不是 `:132-134`） | §6.6（二）`:575` 写 `pnpm-lock.yaml:136-138`；自检 3 `:812` 写 `pnpm-lock.yaml:136-138`（**两处都改了**） | 我读该文件：`L132 dependencies:`／`L133 base-link-core:`／`L136 base-paint:`／`L137 specifier: ^0.3.0`／`L138 version: link:../base-render` | **✅ 已改（两处）** |
| 3 | `公共组件/docs/scene-data-contract.md` 顶层表在 `:23-27`（报告曾写 `:1`） | §1.5 对照表第 3 行 `:225` 写 `D:\2Study\StudyNotes\SKILLS\公共组件\docs\scene-data-contract.md:23-27`（**路径前缀已补**） | 我读该文件：`L23 \| 字段 \| 类型 \| 必填 \| 说明 \|`、`L25 skill_name`／`L26 title`／`L27 subtitle`／`L28 meta_blocks`／`L29 groups`——**:23-27 是表头＋前三行，不是全表**（A 席自己给的是 `:23-29`）。表头在 `:23`，五字段在 `:25-29` | **✅ 已改**（`:23-27` 与 A 席的 `:23-29` 差 2 行，属更紧但不含 `meta_blocks`／`groups` 两行的记法；结论「顶层表 5 个字段」不受影响） |
| 4 | §1.5「老文档多出 `meta_blocks`」是错的；真缺的是 `contact`／`init_banner`／`version`／`recommendations` | §1.5 对照表第 3 行 `:225`：「`meta_blocks` **已在表内**，不是多出来的那个」；「真未收录的是 **`contact`／`init_banner`／`version`／`recommendations`**」 | 老文档顶层表 `:28` 逐字 `\| \`meta_blocks\` \| array \| 可选 \| 技能特有元信息透传块（见 §4） \|`——**确在表内**；`contact`／`init_banner`／`version`／`recommendations` 全文 0 命中 | **✅ 已改** |
| 5 | `escapeTitleText` 只转义 4 个字符 `& < > "`（不转义 `'`），报告曾写「五字符」（混了页面侧 `esc()` 模板 `:1696`） | §6.1 `:492`：「它只替换 **4 个字符**：`&`／`<`／`>`／`"`，**不转义 `'`**；页面侧那个五字符的 `esc()`（`[&<>"']`，模板 `help-template.html:1696`）是**另一个函数**」；并点出生成物 `:53` 注释「五字符即够」与下面那行代码不符，以代码为准 | 我读生成物 `src/helpShell.ts:53-56`：`:53` 注释逐字「文本节点转义（标题只进 `<title>`，五字符即够）。」、`:55` 代码只有 4 个 `.replace`（`&`／`<`／`>`／`"`）；模板 `:1696` `esc()` 的字符类是 `[&<>"']`。**报告把注释与代码的不符也一并点出了** | **✅ 已改（且改得比 A 席要求更完整）** |
| 6 | 单测断言行号 `:34` | §1.3 `:134`：「断言在生成物 `test/help-shell-136.test.mjs:34`，发射处是生成器 `gen-help-shell.cjs:243`」 | 我读生成物测试件（**65 行**）：`:34 assert.equal(renderHelpShell, renderHelpShellHtml, '标准出口须为同一实现');`；生成器 `gen-help-shell.cjs:243 T.push("  assert.equal(renderHelpShell, renderHelpShellHtml, …");`——**两处都对** | **✅ 已改** |

**小结：A 席 6 处行号／事实，6/6 已改对**（第 3 处写成 `:23-27` 而非 A 席的 `:23-29`，不构成错，因为 `meta_blocks`／`groups` 两行的存在与结论已由同格文字另行点明）。

---

## 四、`_N` 裁定核对

- **§九存在**：`## 九、命名与落点：`_N` 从 `_1` 起步（用户已定案，票 4／票 7 必读）` 在 `:656`（整节 `:656-664`）。裁定句在 `:658`：「chef 的 HELP 产物在重名递补时用 `_1` 起步（照老家），不得照抄记账的 `_2`」。
- **理由恰三条**（`:662`）：① 用户已定「通式不变」（`map-chef-body.md:31`）；② 老产出就在同一目录 `cook_html/help/` 里，同一目录下编号语义须与历史件一致；③ 肉眼验收第一眼就是文件名，差 1 会被读成缺陷。
- **显式提醒票 4／票 7 有**（`:664`）：「⚠️ **显式提醒票 4／票 7**：`t7-body.md:7` 写的「落盘走独占写（`wx` ＋ `EEXIST` 递补）」这条机制照搬没问题，但**递补的起始数字不得照抄记账的 `_2`**——要照老家的 `_1`。票 4（管线归属＋缺省口径）裁产物名时同此。」标题 `:656` 也带「票 4／票 7 必读」。
- **bill 的 `_2` 出处 `output.ts:26`／`:38` 对**：我读 `packages/skill-bill/src/output.ts`——`:26` 注释逐字「下一独占候选（可单测）：`〈stem〉_<YYYYMMDD_HHMMSS>[_N].html` 的 `_N` 递增；**无 `_N` 则 `_2`**。」、`:38 return join(dir, stem + '_2' + '.html');`。报告引的这两行**逐字属实**。
- **老家出处 `align_08.py:52-65` 对**：我读 `D:\2Study\StudyNotes\SKILLS\私家大厨\scripts\align_08.py`——`:52` 函数头注释逐字「`_N` 后缀防覆盖(12.X 共同基础 · N=1 起步)」、`:53 def unique_output_path(out_dir: Path, stem: str, ext: str = ".html") -> Path:`、`:54-56` docstring「首次 → `<stem><ext>`;冲突 → `<stem>_1<ext>` / `<stem>_2<ext>` ...(N=1 起步)」、`:62-65 n = 1; while (out_dir / f"{stem}_{n}{ext}").exists(): n += 1; return out_dir / f"{stem}_{n}{ext}"`。**范围与内容逐行相符**。

**小结：§九写入到位，三条理由齐，票 4／票 7 显式提醒在，两处出处（bill `:26`／`:38`、老家 `:52-65`）全部核对为真。**

---

## 五、新引入的矛盾

### 5.1 章节号：无重复、无断号，但**整改记录里有两处指向错节**

- 我抓出全部标题排序核对：**无重复编号、无断号**。30 个 `n.n` 编号子节（1.1–1.5／2.2／3.1–3.5／4.1–4.3／6.1–6.6／7.1–7.2／8.1–8.2／10.1–10.3／11.1–11.3）逐一唯一；12 个中文顶级节（一–十二）＋独立「结论摘要」「自检」，**A 席提到的「两个 4.1／两个 5.1」这类问题在现版不存在**。
- **唯一编号跳号**：`## 二、` 下只有 `### 2.2`，**没有 2.1**（`:233` 直接到 `:247`）。不构成矛盾（2.2 的「2」是与 t2 报告 `§二` 对齐的记法），但它是一个查找断点。
- **两处指向错节（新引入）**：
  1. `:27`（整改记录 · 8 条硬伤表第 5 行）写「**补** §六「页面结构预期」整节」，`:43`（整改记录 · 7 问表第 9 行）写「**补齐**。§六」——**页面结构预期实际是 §七**（`:582`）。同一份整改记录的**另一张表**（`:43` 所在表之外的 `:42`）却写「**补齐**。§10.1 第一／二步输入 ＋ §11.2 五步报点」，与正文章节号一致。⇒ 整改记录内部对同一节给出了 `§六` 与 `§七` 两个号。
  2. `:85`（结论摘要第 6 条）写「老注入器的硬校验（含 **group id 与 scene id 共用一个唯一集合**）在新仓 A 路**没有对应实现**，重名会静默错（**§1.4／§8**）」——**§八 是「`prompt_template` 与 `wake_word` 的取法」**（`:619`），与这条毫无关系；该内容的真正落点是 **§1.4（`:176`）＋ §1.3 末段（`:174`）**。同一条结论在 `:70` 写的是「**§1.4** 老注入器 15 条硬校验」，在 `:807` 也写 §1.4——**只有 `:85` 这一处指向 §八**。

### 5.2 同一件事两处说法不一致（1 条）

**老契约顶层表：本件的 5 个字段 vs 引 t2 的「10 域中英名对照表」**（引错了对象）

- `:225`（§1.5 对照表第 3 行）：「老契约文档顶层表只有 **5 个字段**（`D:\2Study\StudyNotes\SKILLS\公共组件\docs\scene-data-contract.md:23-27`，逐行是 `skill_name`／`title`／`subtitle`／`meta_blocks`／`groups`）」——**这是对的**（我读老文档 `:23` 表头、`:25-29` 五字段）。
- `:253`（§2.2 表格第一行，讲 `groups[].id` 的 `domain.key` 出处）：「该表在 **`:23-34`**，五处独立出处的行号在 `:38-49`」——**指代含混**：`t2-content-reconcile.md` 的 `:23-34` 是**「10 域中英名对照表」**（域序／中文域／英文目录名／场景数／三处佐证），**不是老契约顶层表**；句子里那个「该表」承前指代的是 `:249` 提到的「同图票 2 的 10 域中英名对照表」，但 `:23-34` 这个范围**漏掉了表头两行**（表头在 `:23-24`，域行 `:25-34`），且与 `:225` 同一个「老契约顶层表」被写成两种东西，读者容易把两张表并成一张。
- 判定：**真矛盾在「范围记法」与「指代」，不在字段数**。字段数 5 这一条我没有查到第二处相反说法。

**一处已核实为「不成立的怀疑」，为免误报一并记明**：`type`（单数）字符串的种类数，`:645`（§8.2）与 `:792`（证据目录）都写「**11 种**」，我三次独立 `JSON.parse` 老载荷（`$.scenarios`，48 条，`type` 键无缺失、无空值）得 **distinct 11、计数之和 48**，与旧探针 `.scratch/chef-help/t3/probe_prompt.stdout.txt:55-67` 的 `type_string_freq` 11 项、整改期 `verify_payload.out.json` 的 `type_distinct` 11 项**三方一致**。⇒ **11 正确，无矛盾**。同理，`:362` 的「表外 3 个」（去括号注后原子 9 个、表内 6 个）与 `:792` 的探针原表（未去括号注的 5 项）**不矛盾**，后者已在同段标注了「该表按**未去括号注**的原子列」。

### 5.2b 留证件未随正文口径更新（1 条）

- 正文（对）：`:65`／`:331`／`:347`／`:643` 全写**双**花括号，并明说单花括号 0 条。
- 但整改期产出的**渲染产物留证** `.scratch/chef-help/t3/probe-render.html` 的 `help-data` 载荷段里，夹具仍是 **`"prompt_template":"PROMPT-SENTINEL {菜名}"`（单花括号）**；`.scratch/chef-help/t3/probe_render.mjs` 的夹具也是同一版。**这不是正文的错**（正文口径正确、另有 `verify_payload.out.json` 为整改期新证），但它意味着「渲染落点」那份留证还留着被推翻的那一版形状——票 11／后续复核若翻这份 HTML 会与正文对不上。**属新引入的留证不一致**。注：生成物测试件的 `FIXTURE`（`test/help-shell-136.test.mjs:22`）用的是 `"prompt_template":"p"`，**不含花括号**，与冻结哈希自洽——我实跑 `dist/helpShell.js` 复算得 `3d00cfb6…fc4a`，与该件 `:33` 常量相符，**不受此项影响**。

### 5.3 跨件行号漂移（最重的一条新矛盾）

整改把「与同图票 2 对齐」写进了正文，引了 `docs/skills/skill-chef/t2-content-reconcile.md` 的 **8 处行号**，我逐处实读该文件（**现行 1,157 行**，含它自己的「整改记录」段）：

| 报告引的 t2 行号 | 报告说那里是什么 | 实际是什么 | 真正在哪（现行行号） |
| --- | --- | --- | --- |
| `:13` | `domain.key` 权威英文名 | t2 自己的**用户裁定**段 | `:77`／`:85`／`:877` |
| `:23-34` | 10 域中英名对照表 | 中文域表**主体**（表头其实在 `:23-24`，`:25-34` 是域行） | 表头 `:23-24`／域行 `:25-34`——**表本身对，行范围包不齐表头** |
| `:38-49` | 五处独立出处的行号 | 附表**缩进两格**的 12 行（`  \| --- \|` 起） | `:36`（标题）／`:38-49`（行）——**大致可用** |
| `:396` | 场景 6 键 = `id`／`title`／… | 第四节附表第 7 行（`add_relation`） | `:562` |
| `:403-413` | 映射表 | 第四节合计行与「三处佐证」段 | `:566-576` |
| `:408` | 「按 `+` 拆开、去掉括号注」 | 「35 条全部同时满足…」段 | `:571` |
| `:410` | 「逐字（含 `{{菜名}}`）」 | 「## 五、新表多出的条目」标题 | `:573` |
| `:412` | 「`id` 取域文件 `domain.key`」 | 「## 五」下的空行 | `:575` |
| `:413` | 「契约无字段位，草案不含」 | 37 条分类计数的引子 | `:576` |

**漂移量约 125 行**（`:396 → :562` 差 166，`:408 → :571` 差 163，`:412 → :575` 差 163）。**成因清楚**：报告的 `:396-413` 是 t2 **改版前**的行号，t2 随后在自己的开头插入了「整改记录」（`:9-71`，约 73 行）并在别处增补，行号整体后移；**t3 的整改发生在 t2 改版之后，却没有重新对齐**。

**后果**：票 6 若要按报告引的行号去读 t2，`§2.2`／`§3.5 口子 2/3/4`／`§10.3.4` 共 8 处会让它翻到**完全无关的内容**。**内容层不受影响**（10 域英文名、6 键、去括号注、`{{菜名}}` 逐字、无名单位五条我都已在 t2 现行文本里核到、逐字一致），所以这条是**可施工性问题，不是事实错**——但它正是 B 席第 8 条「与同图票 2 对齐」要求的一部分，**尚未真正闭环**。

### 5.4 其他一致性抽查（这些**没有**矛盾，为免误报一并记明）

- `recommendations` 渲染与否：全篇一致（见第二节）。
- `meta_blocks`／`subtitle`「读了不渲染」：`:83`／`:215`／`:241`／`:272`／`:804` 五处一致。
- 16 条双花括号：`:65`／`:331-337`／`:643`／`:793` 四处一致（15＋1）。
- 11 Tab：`:586`／`:598`／`:615` 三处一致。
- 42 键：`:310`／`:646`／`:793` 三处一致（42＝41＋1）。
- 两个行数 388／451：`:739`／`:741`／`:743` 三处一致，且与我实测相符。
- `type` 11 种：`:645`／`:792` 两处一致，且与两份自有证据件、与我的独立复算三方一致。
- 六个口子：`:300`（标题）／`:388`（总口径）／`:39`（整改记录）三处都写「六个」，`dimensions`／`{{…}}`／`types`／aliases／`result`／`html`＋`variants` 逐一对得上。
- 「老注入器 15 条硬校验」：`:15`／`:70`／`:178`／`:807` 四处一致，且 §1.4 表确实是 15 行。
- 「必然多出的四样」：`:613` 与 `:611` 的改正说明**自洽**（搜索框已在四样之外）。
- 表外 3 原子：`:362`／`:645`／`:724` 三处一致，且与我按 `TYPE_DEFAULT` 9 键复算相符。
- §11.2 表引 `structure.md:72-104`：我读该文件 126 行，`:72 ## 必报五步`～`:105` 交付对账确实在范围内，逐条对得上。

**总账**：新引入的矛盾 **4 条**——5.1（章节号指向错节，2 处落点）＋ 5.2（一张表的引用对象含混，1 处落点）＋ 5.3（引 t2 的 8 处行号漂移，按 1 条记）＋ 5.2b（留证 HTML 单花括号，1 处落点）。折成「可修的落点」是 **5 处**（章节号那 2 处在整改记录里各出现两次）。

---

## 六、格式与自洽

| 项 | 实测 | 判定 |
| --- | --- | --- |
| BOM | 首 3 字节 `35,32,116`（`# t`），**无 BOM** | **✅** |
| 行尾 | LF **814**、CRLF **0**、末行以 LF 结束 | **✅** |
| 字面 `\n` 转义 | 全文 2 处：`:117` 是**正则原文** `/(?<!\r)\n/`（引用生成器代码，合法）；`:632` 在 §8.1 的 `buildPrompt` **代码块**里 `return lines.concat(paramLines).join('\n');`——**这是标本代码的原文**（模板 `:1763` 逐字如此）。审读提示：全文只有这一处在围栏代码块内，任何把它们当「字面 `\n` 转义混进正文」的机械检查都会报 2 处命中，属**标本代码，不是转义泄漏** | **✅（但有 2 处机械命中，需人工分辨）** |
| 围栏代码块 | 被验件全文共 30 行围栏 ＝ 15 对，**配对完整**；围栏外无落单反引号（我扫出的 30 处「反引号奇数行」全部就是这 30 行围栏本身）。注：**本验证报告自己不用围栏代码块**（避开「围栏内标本代码含字面 `\n`」与「该行是否算违规」的歧义），引文一律写成行内代码 | **✅** |
| 表格完整 | 184 行以 `\|` 起始；我逐节核过分隔行与列数，**未见缺分隔行或列数不齐**（`:23-27` 那类引老文档的记法在表内被写成普通文字，不影响本件表格结构） | **✅** |
| 用词纪律 | 全文「壳」0 命中（照 `docs/agents/wording.md`），未用黑话 | **✅** |
| 控制面自洽 | `## 十二、证据目录`（`:771`）与 `## 自检`（`:799`）均标了「只读／未跑 gen:help-shell／未跑仓级 build·test／未跑 chef-cmd-read／未联网」，与我这次复核的约束一致 | **✅** |

**额外发现（1 条，非格式但影响自查可信度）**：`:233` 的 `## 二、` 下缺 `### 2.1`，`### 2.2` 悬空（见 5.1）。

---

## 七、票 6 可用性（含 `title`／`contact`／`subtitle` 待选是否站得住）

### 7.1 三项「仍留待选」的理由**站得住**

整改自称：「`title`／`contact`／`subtitle` 三项仍是待选——这三项属票 6 票面点名要用户点头的范围，本票无权代裁」（`:36`，落点 `§10.2.1／10.2.2`）。

我去票面逐条找「点名要用户点头」的出处：

- `t6-body.md` 本身**没有**逐项点名 `title`／`contact`／`subtitle`——它点名的是三件小口径（`version`／初始化状态／显隐开关）、解冻 `SKILLS_BASE_FROZEN`、以及第 13–15 行的**「第一步「影响清单」与第二步「结构设计」先报用户点头再动手」**。
- 而 `docs/agents/structure.md:88` 逐字写「**新建目录层级、或者要碰三个以上能力时，等用户点头再动**」，`:78` 写「用户看过再动」，`:80-81` 要求第一步「每行指向**一个**能力目录」。
- 票 6 新增件必然新建目录层级（`§10.1（一）` 甲／乙／丙候选）⇒ **第一步／第二步要报用户点头是硬约束**，而 `title`（决定文档标题与 hero h1）、`contact`（决定关于页第一段出不出现）、`subtitle`（`§10.2 第 8 条`已论证它与 `version` 是一对、不能分头定）**三项都直接改变产物**，落在「第二步 · 结构设计要报的内容」范围内。

⇒ **理由成立**：这三项不是报告漏答，而是**被正确地识别为「须用户裁」并明示了**（`§6.1 :495`、`§6.4 :536`、`§10.2 :710-719` 都写了「属票 6 第一／二步要报用户点头的范围」）。**不构成缺陷。**

### 7.2 「只读这一份能不能一次干成」

**能——但有条件。** 判定拆开看：

**已经够用的部分（票 6 可直接落笔）**

- 出口、注入形状、文档标题派生、三层结构、场景字段名映射、空 `groups` 抛错码：`§1.1`／`§1.3`／`§6.1` 给全，且我抽查的三条（`groups` 空即抛、`composeDocTitle` 三支、`escapeTitleText` 4 字符）**逐条与代码一致**。我另用 `dist/helpShell.js` 实跑：夹具哈希 `3d00cfb6…fc4a` **与生成物测试件 `:33` 的冻结常量相符**，`HELP_SHELL_PREFIX`／`SUFFIX` 两条哈希也相符，`renderHelpShell === renderHelpShellHtml` 为真——**`§1.3` 关于出口与不变量的描述可直接照抄**。
- 六个内容口子的落法、归属票、不做的后果：`§3.5` 给全，且「哪几条能在票 5 自动落地、哪几条不能」写清了（`:302`）。
- 必报五步的素材、包外两处必动件、告警线与门：`§10.1`／`§11` 给全，我核过 `:103-104` 的门常量、`:57` 的允许清单、`LF 388/451`、`AGENTS.md` 不存在**全部为真**。
- `_N` 起步：`§九` 给全且出处为真。
- 页面验收基准：`§七` 给全（11 Tab／3 段／hero／搜索／抽屉／与老件的结构差异）。

**会卡住票 6 的（就是「需再修 5 处」的那 5 处）**

1. `:27`／`:43` 的 `§六` 应为 `§七`（整改记录指向错节，同一条在另一张表里又写对了）。
2. `:85` 的 `§八` 应为 `§1.4`＋`§1.3`（结论摘要指向错节，同一条在 `:70`／`:807` 写对）。
3. `:253` 那句 t2 引文说「该表在 `:23-34`」，与 `:225` 讲的「老契约顶层表 5 个字段」撞成两张表（引错了对象，且范围漏表头两行）。
4. 8 处引 t2 的行号整体漂移约 125 行（第 5.3 节表），其中 `:396`／`:408`／`:410`／`:412`／`:413` 五处会翻到完全无关的内容。
5. `.scratch/chef-help/t3/probe-render.html` 留证里仍是单花括号（与正文双花括号口径不符）。

这 5 处**都不改变任何契约事实与施工结论**（票 6 按 §1.3／§3.5／§6／§7／§10／§11 的正文照做不会做错），但第 4 处会让票 6 **翻错文件**，第 1／2／3 处会让它在整改记录里找不到同一节、把两张表当一张。

⇒ **判决：需再修 5 处**；修完之后（只是改行号、改指代、重生成一份留证，不动任何结论），这一版可以作为票 6 的施工图。

---

## 八、判决与理由

**需再修 5 处。**

**理由**

1. **B 席 8 条硬伤：内容层 8/8 落地，我逐条独立复现一致。** 42 键＝41 合法＋1 畸形（归属 `data_export_backup`，值 `null`；41 个合法键我逐名列过，全 ASCII、无带括号者，故「畸形」判据可靠）、双花括号 16＝`{{菜名}}`15＋`{{N}}`1 且单花括号 0、`type` 11 种（三方一致）、去括号注后表外恰 3 个（`对比`／`确认`／`转移`，对照 `TYPE_DEFAULT` 9 键）、「不建库」已从待选摘出且 `§6.3` 明写票 7 钉死、页面结构预期整节在 `§七`（11 Tab 与模板 `:1829-1832` 结构相符、关于页 3 段与 `:1801/1817/1819` 相符、搜索框已移出「多出的」）、必报五步两节齐、告警线两项行数**我自己只数 `\n` 得 388／451 与报告一致**且 `AGENTS.md` 确不存在、`EXPECT_KEYS`／`EXPECT_TPL` 逐字相符、10 域英文名与 t2 逐字一致。
2. **A 席证伪与方法学：处理完整。** `recommendations` 四处全改对（我按模板 `RECOMMENDATIONS` 4 次／3 行 ＋ `:1819`／`:1821`／`:1822`／`:1834` 复核），全文再扫无漏网；哨兵法已撤换为可达性判据，残留处都明确标注「不再用作判据」。
3. **A 席 6 处行号／事实：6/6 已改对**，其中 `escapeTitleText` 一处改得比 A 席要求更完整（连生成物注释与代码不符也点出了）。
4. **`_N` 裁定：写入到位、三条理由齐、票 4／票 7 显式提醒在、两处出处全部核对为真**（bill `output.ts:26`／`:38` 逐字、老家 `align_08.py:52-65` 逐行）。
5. **新引入矛盾 4 条**（章节号指向错节 2、一张表的引用对象含混 1、引 t2 的 8 处行号整体漂移 1、留证 HTML 单花括号 1；拆到「可修的落点」是 5 处，因为章节号那 2 处在整改记录里各出现两次）。**均不改变契约事实与施工结论**。另核实并**排除**了两条我一度怀疑的项：`type` 11 种正确（三方一致）；`:362` 的「表外 3 个」与 `:792` 探针原表的 5 项并存属**去括号注／未去括号注的口径差**，同段已标注，不构成矛盾。
6. **格式合规**：无 BOM、LF、无字面 `\n` 转义泄漏（2 处机械命中皆为围栏内的标本代码）、15 对围栏配对完整、表格完整、用词合纪律。

**修法建议（每条一句话，全部只动行号、指代与一份留证，不动结论）**：① `:27`／`:43` 的 `§六` 改 `§七`；② `:85` 的 `§八` 改 `§1.4＋§1.3`；③ `:253` 的 t2 引文补明「10 域中英名对照表」并把范围改成 `:23-34`（含表头）；④ 按第 5.3 节表把 8 处 t2 行号刷成现行值（`:13→:77`、`:396→:562`、`:403-413→:566-576`、`:408→:571`、`:410→:573`、`:412→:575`、`:413→:576`）；⑤ `.scratch/chef-help/t3/probe-render.html` 重生成一遍或就地注明「该留证用的是改正前的单花括号夹具」。

---

## 附：本次跑过的只读命令与实验件

**实验件（全部在 `D:\ilife\.scratch\chef-help\t3-verify\`）**：`payload-check.cjs`／`payload-check2.cjs`（老载荷重解析）、`keys-audit.mjs`（42 键全列与畸形键归属）、`headings.cjs`（标题与 `§` 引用审计）、`readtest.cjs`／`fixture-hash.mjs`（生成物测试件与冻结哈希复核）、`t2-head.mjs`／`t2-map.mjs`／`t2-lines.mjs`／`t2-lines2.mjs`／`t2-sec7.mjs`／`t2-search.mjs`（t2 现行行号定位）、`uppers.cjs`（模板顶层变量名）、`probe-prompt.mjs`／`read-verify.mjs`（自有证据件核对）。

**跑过的只读命令（节选）**：`Get-Content -Encoding utf8`／`Select-String -CaseSensitive`／`[System.IO.File]::ReadAllBytes`（BOM 与字节数）／`[System.IO.File]::ReadAllText` 数 `\n`／`Test-Path`／`node <纯解析脚本>`／`node --import` 读 `dist/helpShell.js` 复算哈希与 `composeDocTitle` 三支／`git status --porcelain`。

**未跑**（按硬约束）：`pnpm --filter base-paint gen:help-shell`（及其 `:check`）、仓级 `pnpm build`／`pnpm test`／`pnpm snapshot:*`、`chef-cmd-read`（会建库）、任何 `git add`／`commit`／`checkout`／`stash`、任何联网命令。**未改任何文件。**
