# t659 · 备忘侧不变量清单（以老代码为准）

- 出件票：#659（横切票，`Part of` 地图 #658；规格 #657）
- 判据与接缝总件：`docs/agents/合成写判据.md`
- 逐条出处（静态读码，含逐字片段）：`docs/skills/skill-memo-ilife/t659-取证-老实现.md`
- 机器读数（老 ↔ 新对拍，三个边界）：`docs/skills/skill-memo-ilife/t659-对拍读数.json`
- 口径：**以代码为准**，不引文档。
- 老实现位置：`D:\2Study\StudyNotes\SKILLS\备忘录\`（仓外，只读对照）。

## 一 · 不变量清单

| 编号 | 不变量 | 判据格 | 老代码出处 | 机器读数 |
|---|---|---|---|---|
| M-01 | 记一条心愿＝一条命令同时做本地 ＋ 飞书任务 | 本地键：**没有**（直插 INSERT，无唯一约束）；远端键：`summary` 全等 ＋ `due_at` 前缀匹配 | `memo_cli.py:118`、`:143`、`:161`；`init.sql:68` | M-A（老：本地落 ＋ 远端建 `原备忘 #1`；新：只落本地） |
| M-02 | 到期日字段名 `due`（不是 `due_date`），**仅心愿生效**，非心愿静默置空 | 建时参数 `--due YYYY-MM-DD`；远端 `task +create --due` | `init.sql:13`、`memo_cli.py:129-139`、`:1550` | M-A 老本地行 `due=2026-09-25`、远端调用带 `--due`；新无该字段（缺口） |
| M-03 | 本地侧**建前不查重** | 无 SELECT、无 UNIQUE | `memo_cli.py:140`、`:143` | M-B 老：同题同到期日再记一条 → 本地变成 2 行 |
| M-04 | 远端侧建前查重**只在有到期日时执行** | 查重字段：`item.summary == summary` 且 `item.due_at.startswith(due_iso)` | `feishu_sync.py:245`、`:312-315` | M-B 老：只剩 1 次 `task +search`，复用 `tk_1`，不重复建 |
| M-05 | 归属标记逐字 `原备忘 #{memo_id}`，写进任务描述 | 反查正则逐字 `原备忘\s*#(\d+)`，只扫 `description` | `feishu_sync.py:328`、`:617`、`:626` | M-A 老建对象描述＝`原备忘 #1`；新不建对象（缺口） |
| M-06 | 远端标识回写 `notes.feishu_task_guid`，且**只在 `ok` 与 `task_guid` 同时成立时**写 | 回写两处：建时、对账补建时 | `memo_cli.py:166-169`、`feishu_sync.py:526` | M-A 老本地行 `feishu_task_guid=tk_1`；M-C／M-D 老侧失败时不写（留 `null`） |
| M-07 | 五个同步操作齐备：建／改标题／标完成／改期／清期 | 对应 `add_wish_sync`／`update_wish_sync`／`complete_wish_sync`／`update_due_sync`／`clear_due_sync` | `feishu_sync.py:272`、`:349`、`:363`、`:373`、`:398` | M-A（建）、M-G（改期 → `task +update --due`）、M-E（标完成） |
| M-08 | 删心愿：本地硬删 ＋ 远端**只标完成，没有删除路径** | 无 task 删除子命令；自认「task 无 +delete shortcut」 | `memo_cli.py:407`、`:414-417`；`feishu_sync.py:834` | M-E（老：本地删、远端 `task +complete tk_1`；新：远端零调用）→ D-06 要补删除 |
| M-09 | 反向对账三步：本地缺标识补建／远端完成→本地／远端改期→本地 | 补建键 `feishu_task_guid IS NULL`；回写用「正则反查 id ＋ `id=? AND feishu_task_guid=?` 双向校验」 | `feishu_sync.py:503`、`:513`、`:620-660`、`:661+` | M-F（老：`scanned_done=1`、`synced=1`，心愿删掉并生成打卡；新：`memo.sync` 只报门） |
| M-10 | `create_task` 没有幂等键 | 全库无 `client_token` | `feishu_sync.py:325-330` | 与 D-05 同型；远端判重靠 M-04 兜（只在有到期日时） |
| M-11 | 远端不可用时**本地照落**，回执记 `synced:false`，退出码 0 | 降级口径：先 commit 再同步，失败只记不阻断 | `memo_cli.py:148`、`:184`、`:76`；`feishu_sync.py:172` | M-C（老：本地落 ＋ `synced:false` ＋ 退出码 0；新：本地落、无远端字段） |
| M-12 | 飞书自检 sentinel：真打建／改／完成三个写操作，任务域**不清除** | 前缀逐字 `[备忘录测试]`；差集不过就不跑 | `feishu_sync.py:62`、`:831-858` | 本票未布景（能力属 #661，落地时补读数）→ D-03 |
| M-13 | 远端拉取**没有分片**：一次全量 ＋ 逐条 `tasks get` 补 due（N+1） | list 失败返回 `[]`，对账静默记 0 | `feishu_sync.py:455`、`:465`、`:472`、`:477` | M-F 老调用留痕：`task +get-related-tasks` ×1、`task tasks get` ×0（本例无 due 补齐需求） |

## 二 · 老实现自身的缺陷（照抄会坏，已登记偏离）

| 偏离 | 老行为 | 新行为 | 出处 | 两行机器读数 |
|---|---|---|---|---|
| D-05 | `create_event`／`task +create` 都没有幂等键 | 补幂等键 | 老 `feishu_sync.py:288`、`:325` | **改坏必红**：M-B 远端判重只在有到期日时生效；去掉 `due` 就会重复建；**还原必绿**：M-B（有到期日时复用 `tk_1`，远端只有 1 个对象） |
| D-06 | 删心愿只标完成，飞书任务永远留着 | 补 task 删除 | 老 `memo_cli.py:414-417`；`feishu_sync.py` 全库无 task delete | **改坏＝现状必红**：M-E（本地删、远端 `task +complete`，任务仍在）；**还原必绿**：待 #661 落地后 `task +delete` 出现且任务清掉 |
| D-03 | 自检直接跑、留真任务、清理只写在注释里 | 搬但三处改造（默认不跑／点名留痕／能删的删干净） | 老 `feishu_sync.py:831-921` | **改坏**：M-12 未布景；**还原**：待 #661 |

「本地无判重」（M-03）不是偏离，而是**照实登记的现状**：契约要求两侧各按自然键判重，责任全在被调方，故 #661 落地时本地侧也必须判重——这一条按「缺口」记，见下节。

## 三 · 新侧当刻缺口（谁欠谁）

| 缺口 | 属主票 | 读数 |
|---|---|---|
| 到期日字段（`due`）整条没有 | #661 | 对拍 M-A／M-G |
| 心愿的飞书任务（建／改／完成／改期／清期）整条没有 | #661 | 对拍 M-A／M-E／M-G |
| 建前查重（本地侧与远端侧）整条没有 | #661 | 对拍 M-B |
| 反向对账三步 | #661 | 对拍 M-F |
| 远端任务删除 | #661（D-06） | 对拍 M-E |
| 自检 sentinel 与授权引导 | #661（D-03） | 未落 |

## 四 · 老实现里查不清／自相矛盾之处（照实记，不替它圆）

| 项 | 实况 |
|---|---|
| 查重键全文 vs 写入截断 | 查重拿全文 `content` 比对，创建时写 `content[:200]`（`feishu_sync.py:313` vs `:327`）→ 长正文必然比不中 |
| 注释说「add 前先查」 | 代码锁在 `if due_iso:` 里（`feishu_sync.py:288` 注释 vs `:312`）→ **无到期日的心愿完全不查重** |
| 删心愿的远端失败不可回滚 | 先删本地（`memo_cli.py:407`）再调远端完成（`:417`）；远端失败无重试入口，下一轮对账只落 `skipped_no_local_note`（`feishu_sync.py:640`）——代码未提供收尾路径，不替它推断产品意图 |

## 五 · 复跑

```
node tooling/run-locked.mjs --ticket 659 -- node packages/skill-memo-ilife/scripts/t659-parity-probe.mjs
node tooling/run-locked.mjs --ticket 659 -- node --test test/synthetic-write-659.test.mjs
```

备忘侧的建表在仓外是另一条路（老 `script/init.sql`），对拍件照它的既有用法先铺表——那是**布景**，不是被测行为。
