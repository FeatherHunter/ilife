## 决议

**① 管线归属＝甲：备忘录自持一份最小管线。** 三条硬条件：只抄通式那一小块（`wx` 独占 ＋ `EEXIST` 递补 ＋ `resolve` ＋ 本地时间戳）；`_N` **从 `_2` 起**；自持件头注释写明「第 4 份同逻辑 ＋ 指向 `#237` ＋ 指向本图迁移票 `#240`」。

决定性证据（编排会话亲验）：三家现役实现与老实物 `备忘录_HELP_20260820_150143_2.html` **全部 `_2` 起**（`packages/skill-bill/src/output.ts:26-38` 逐字「无 `_N` 则 `_2`」）；而共用件同族的裁定是 `_1` 起（`#208:20` 逐字「`_N` **从 1 起步**，**不照抄 bill 的 `_2`**」，`:129`／`:245` 同），`#237:23` 又把 `_2`／`_3` 记为「三家 HELP 现在的行为」⇒ **上游两票自相矛盾**；`#208:145` 把备忘逐字划出该图、`:30` 锁定为「三家」⇒ 用户「乙」原话推不出「本图现在买单」；`#237` 下一步是「新建目录层级先点头」（`structure.md:88`）的人工闸门，且它阻塞 `#215`。

**欠债挂账**：#240「备忘录出口落盘迁入共用件 `saveHtmlFile`」，`blocked_by = #237`。`#229` **不挂** `#237`（保持本图关键路径不断）。依据 `structure.md:23` 铁律一。

**② 缺省出口口径＝缺省即 HELP 文件，速查走显式参数。** 速查支产物名 `备忘录_速查表`，参数 `mode:"lookup"`。
**③ 落盘目录＝扁平 `<SKILLS_DB_PATH>/memo_html/`，不加 `help/` 层。**
**④ `combo` 侧＝必须登记（推翻本票草案原先的「不登记」）**：`memo.help.lookup` 进 `packages/base-combos/combos.yaml` 并重跑生成器，不许手改 `present.ts`。证据：`test/combos-p8.test.mjs:107-114` 断言 `MEMO_KEY_SHAPES` 每键都在 `PRESENT_KEYS` 里；`:115-120` 断言 `present.ts` 与生成输出逐字相等。
**⑤ 出口方向相反**（`src/cli/cmd_read.ts:129` 分派前无条件 `openMemoDb`）修法照草案第四节，主归 `#229`。

## 两份对抗式复审的评分与采纳

| 复审 | 评分 | 结论 | 采纳情况 |
|---|---|---|---|
| A（管线归属裁决，`t224-review-A.md`） | 草案决策质量 **58/100**；被推荐的「乙」对达成目的地 **32/100** | 判甲 ＋ 三条硬条件 ＋ 挂账迁移票 | **全盘采纳**（结论），**引用票号更正**（`_1` 出自 `#208` 非 `#237`） |
| B（逐条打假，`t224-review-B.md`） | **65/100**（事实 20/35／证据 16/25／格式 14/15／目的地 15/25） | 骨架「已定案⇒不必再问」撑不住；行号与来源标注多处需订正 | **采纳更正**，结论不变 |

**本票据此更正的三处**：
1. **「已定案」的骨架收回**：`help/` 那道题地图三处点名「归票 4 裁」（`map-220-body.md:38`／`:57`／`:126`），**由本票裁**，不是「用户已定案不必再问」。本票的裁决（不加 `help/`）建立在**三条独立证据**上（Destination 文字 ＋ 磁盘实测 ＋ 两张已完工且终审通过的同流水线地图产物皆扁平），不依赖「用户原话」。
2. **来源标注订正**：本票第二／三节把 5 条都标成「用户原话（逐字）」不准确——用户对 Q1–Q6 的真原话只有取值本身（`map-220-body.md:156-163`）。其中「落盘目录＝`memo_html/`」有**用户点选**支持（`决策待确认-备忘录HELP地图.html:162` 的 Q3 选项 A 原文即写 `.db\memo_html\`，但该选项通篇未提 `help/`）；「缺省＝HELP 文件」由 Destination 坐实；「`_N` 起数」是老代码实测。
3. **行号订正**：`src/fetch/db.ts:21-29` 实为 **`:19-27`**（缺目录抛在 `:22`）；`combos.yaml:578-614` 属 `channels:` 段（两段各 10 条＝20 条投影）；甲／乙／丙对比表实为 `t1-bill-recipe.md:604-608`。

## 本票明确不做

- 不写业务代码（未改 `packages/`）；不做整包重排（另立票）；不写固定名镜像（`#131`／`#143` 已裁）。

## 进度：100%

下一步：已关票。`#229` 照上列决议施工；欠债迁移票 #240；形状闸门在 `#225`。
