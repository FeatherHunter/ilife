## Question

两件事一起定，定完出口票照它做（照 `#183` 票 4 的同款题目）。

**① 命名与落盘的管线归属**

- 照 `#143` 的裁决 (b)：备忘录**自持一份最小管线**（只抄通式那一小块，不搬卡路里的命令名→中文段落映射／动态段／三态回退）；
- 还是按新代码架构规则的共用件条款收成共用位——`docs/agents/structure.md`：**共用件是从第二个用法里长出来的**，并要写得出哪两个能力在用。今天的现实是卡路里／记账／居家已经是第三、四处在做同一件事。

要把铁律二（同一个东西只有一个定义地）与铁律五（接口小、里面厚）摆上桌，并判 `#147` 当时的理由（`base-paint` 是被 `#96` 主动冻结的渲染包、落盘是 IO、技能间零依赖不可破）今天还成不成立。

**② 缺省出口口径**

- 照 `#139`／`#144` 的判法：**缺省＝HELP 文件**（对 AI 说「备忘录 help」就落盘 ＋ 回执绝对路径），**速查走显式参数**；
- 还是保留别的缺省（今天 `memo.help.lookup` 根本不存在，等于没有缺省行为可保）。

要带上桌的事实：老目录 `<SKILLS_DB_PATH>/memo_html/`、老通式 `备忘录_HELP_<YYYYMMDD_HHMMSS>.html`、老技能一次出三份（含**覆盖技能根 `备忘录.html`**，这条已被 `#131`／`#143` 裁掉）、新表 28 条短语／10 条命令、票 1 的照抄清单。

产出：决定 ＋ 理由写回票面（含落盘目录、文件名主体、速查支产物名与显式参数名），并决定 `combo` 侧要不要同步登记。

**并入票 1（`#221`）／票 2（`#222`）的实测**：

- ⚠️ **出口方向相反，本票要先解决这条**：`src/cli/cmd_read.ts:129` 在分派前**无条件 `openMemoDb`**，而 `src/fetch/db.ts:21-29` 缺目录即抛 ⇒ 只读实测 `memo.search` 退出码 4。账单是 `src/cli/cmd_read.ts:493-495` **在开库之前**分派。⇒ 备忘录今天**没有库就看不了帮助**；「缺省＝HELP 文件」要成立，分派必须先于开库。
- `memo.help.lookup` 今天不存在：`src/render/envelope.ts:5-16` 只登记了 10 条命令。
- 命名**不必改老名**：老目录 `memo_html`、文件名主体 `备忘录_HELP`（出处老 `script/memo_render.py:602`）。「沿用老名」与「与兄弟同形」的冲突只在**要不要多一层 `help/`**（见地图 Notes）。
- 老技能把一份 HELP 写成**三个副本**（时间戳件 ＋ 覆盖技能根 `备忘录.html` ＋ `--output`）；后两条已被 `#131`／`#143` 裁掉，本票只需确认「只留时间戳件」。
- 管线归属要多带一条事实：卡路里／记账／居家／作息／大厨**已经是第 3–5 个**在做同一件事；兄弟图 `#208` 的 Notes 里写着 `#147` 那三条「合流触发点」的**第 1 条「出现第三个消费者」已经命中**。

## 进度：0%

下一步：开一轮 grilling 把①②定死（票 1 已关，前置解除）。
