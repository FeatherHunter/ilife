## Question

两件事一起定，定完出口票照它做（照 `#183` 票 4 的同款题目）。

**① 命名与落盘的管线归属**

- 照 `#143` 的裁决 (b)：备忘录**自持一份最小管线**（只抄通式那一小块，不搬卡路里的命令名→中文段落映射／动态段／三态回退）；
- 还是按新代码架构规则的共用件条款收成共用位——`docs/agents/structure.md`：**共用件是从第二个用法里长出来的**，并要写得出哪两个能力在用。今天的现实是卡路里／记账／居家已经是第三、四处在做同一件事。

要把铁律二（同一个东西只有一个定义地）与铁律五（接口小、里面厚）摆上桌，并判 `#147` 当时的理由（`base-paint` 是被 `#96` 主动冻结的渲染包、落盘是 IO、技能间零依赖不可破）今天还成不成立。

**② 缺省出口口径**

- 照 `#139`／`#144` 的判法：**缺省＝HELP 文件**（对 AI 说「备忘录 help」就落盘 ＋ 回执绝对路径），**速查走显式参数**；
- 还是保留别的缺省（今天 `memo.help.lookup` 根本不存在，等于没有缺省行为可保）。

要带上桌的事实：老目录 `<SKILLS_DB_PATH>/memo_html/`、老通式 `备忘录_HELP_<YYYYMMDD_HHMMSS>.html`、老技能一次出三份（含**覆盖技能根 `备忘录.html`**，这条已被 `#131`／`#143` 裁掉）、新表 28 条短语／10 条命令、票 1 的照抄清单。

产出：决定 ＋ 理由写回票面（含落盘目录、文件名主体、速查支产物名与显式参数名），并决定 `combo` 侧要不要同步登记。

**并入票 1（`#221`）／票 2（`#222`）的实测**：

- ⚠️ **出口方向相反，本票要先解决这条**：`src/cli/cmd_read.ts:129` 在分派前**无条件 `openMemoDb`**，而 `src/fetch/db.ts:21-29` 缺目录即抛 ⇒ 只读实测 `memo.search` 退出码 4。账单是 `src/cli/cmd_read.ts:493-495` **在开库之前**分派。⇒ 备忘录今天**没有库就看不了帮助**；「缺省＝HELP 文件」要成立，分派必须先于开库。
- `memo.help.lookup` 今天不存在：`src/render/envelope.ts:5-16` 只登记了 10 条命令。
- 命名**不必改老名**：老目录 `memo_html`、文件名主体 `备忘录_HELP`（出处老 `script/memo_render.py:602`）。「沿用老名」与「与兄弟同形」的冲突只在**要不要多一层 `help/`**（见地图 Notes）。
- 老技能把一份 HELP 写成**三个副本**（时间戳件 ＋ 覆盖技能根 `备忘录.html` ＋ `--output`）；后两条已被 `#131`／`#143` 裁掉，本票只需确认「只留时间戳件」。
- 管线归属要多带一条事实：卡路里／记账／居家／作息／大厨**已经是第 3–5 个**在做同一件事；兄弟图 `#208` 的 Notes 里写着 `#147` 那三条「合流触发点」的**第 1 条「出现第三个消费者」已经命中**。

## 决议（2026-09-12 定案，逐条带证据）

**① 管线归属＝甲：备忘录自持一份最小管线。** 三条硬条件：只抄通式那一小块（`wx` 独占 ＋ `EEXIST` 递补 ＋ `resolve` ＋ 本地时间戳）；`_N` **从 `_2` 起**；自持件头注释写明「这是第 4 份同逻辑 ＋ 指向 `#237` ＋ 指向本图迁移票」。
判甲的决定性证据（编排会话亲验）：`packages/skill-bill/src/output.ts:26-38` 逐字「无 `_N` 则 `_2`」，三家现役实现与老实物 `备忘录_HELP_20260820_150143_2.html` **全部 `_2` 起**；而共用件同族的裁定是 `_1` 起——`#208:20` 逐字「`_N` **从 1 起步**，**不照抄 bill 的 `_2`**」（`:129`／`:245` 同），`#237:23` 又把 `_2`／`_3` 记为「三家 HELP 现在的行为」⇒ **上游两票自相矛盾**，且 `_1` 会**改掉三家现役行为**、撞 #233「与老实物并排」验收；`#208:145` 把备忘逐字划出该图、`:30` 锁定为「三家」⇒ 用户「乙」原话**推不出「本图现在买单」**；`#237` 的下一步是「新建目录层级先点头」（`structure.md:88`）的人工闸门，且它阻塞 `#215`。
**欠债挂账**：另开迁移票「备忘录出口落盘迁入共用件 `saveHtmlFile`」，`blocked_by = #237`；`#229` **不挂** `#237`（保持本图关键路径不断）。依据 `structure.md:23` 铁律一。裁决正本见 `t220-orchestrator-decisions.md` 第二节裁决 3、复审员 A 报告 `t224-review-A.md`。

**② 缺省出口口径＝缺省即 HELP 文件，速查走显式参数。** 速查支产物名 `备忘录_速查表`，参数 `mode:"lookup"`（照样板记账 `helpPaths.ts:28` ＋ `cmd_read.ts:71-73,93`）。

**③ 落盘目录＝扁平 `<SKILLS_DB_PATH>/memo_html/`，不加 `help/` 层。** 三条独立证据见 `t224-delivery-path-evidence.md`（含幂等复核命令）。老技能那条「覆盖技能根 `备忘录.html`」确认**不做**（`#131`／`#143` 已裁）。

**④ `combo` 侧＝必须登记（推翻本草案原先的「不登记」）。** `memo.help.lookup` 要进 `packages/base-combos/combos.yaml` 并**重跑生成器**，不许手改 `present.ts`。证据：`test/combos-p8.test.mjs:107-114` 断言 `MEMO_KEY_SHAPES` 每键都在 `PRESENT_KEYS` 里；`:115-120` 断言 `present.ts` 与 `renderPresent(combosKeys(yaml))` 逐字相等。

**⑤ 出口方向相反**（`src/cli/cmd_read.ts:129` 分派前无条件 `openMemoDb`）修法照本草案第四节，**主归 `#229`**，兼记 `#228`（接缝＝`dispatchHelp` 放哪），`#230` 做回归锁。

**⑥ 事实更正（本草案的一处不准确）**：草案第二／三节把 5 条都标成「用户原话（逐字）」并不准确——用户对 Q1–Q6 的**真正原话只有取值本身**（`map-220-body.md:156-163`，`Q3=A` 一个字符、无题干）。其中「缺省＝HELP 文件」最扎实，「不加 `help/`」的依据是三条独立证据而非原话，「`_N` 起数」是老代码实测。结论不变，**来源标注须按此更正**。

## 进度：100%

下一步：已关票。`#229` 照上列决议施工；本图欠债迁移票见本票评论。

草案落 `docs/skills/skill-memo-ilife/t224-decision-draft.md`（六节：题目总览／已被用户原话定案的条目／一页纸提问稿／出口方向相反的修法与归票／证据附录／事实不清与矛盾）。

**只出决定，不写业务代码**：未改 `packages/` 任何文件、未 commit、未动任何 issue（含本图正文）。

### 已被用户原话定案（本项目不再问用户）

- **落盘目录** `<SKILLS_DB_PATH>/memo_html/`（**不加** `help/` 层）——地图 `## Destination:3` 逐字 ＋ Notes「文档与产出落点（用户 Q3=A）」`:83`。
- **`help/` 那一层**：不加。三条独立证据 —— ①本图 Destination ＋ Q3=A 原话逐字写扁平；②老 `memo_render.py:114-121` 只建一层、磁盘 `.db\memo_html\` 无 `help/` 子目录；③四家已完工兄弟目录（`calorie_html`／`biscuit_accountant_html`／`home_manager_html`／`memo_html`）实测全扁平（`schedule_html` 那个 `help/` 是老作息世代）。并行会话证据件 `t224-delivery-path-evidence.md` 与本文独立测得同一结论。
- **文件名主体** `备忘录_HELP`、通式 `备忘录_HELP_<YYYYMMDD_HHMMSS>[_N].html` ——同上原话；老名出处老 `script/memo_render.py:602`。
- **缺省＝HELP 文件**（落盘 ＋ 绝对路径回执）——`## Destination:3` 第一句。
- **不写固定名镜像**（老那条覆盖技能根 `备忘录.html` 不做）——Notes `:83`（`#131`／`#143` 已裁）。

### 真的还需要负责人裁（两条，一页纸提问稿在草案第三节）

1. **管线归属**：甲＝自持最小管线（照 `#143` 裁决 (b)）／乙＝收成共用位（消费 `#237` 的 `saveHtmlFile`）。**推荐乙**——用户在隔壁 `#208` 同一题已裁乙（原话「我们用乙（建共用件，推荐）」），`#147` 三条合流触发点第 1 条已命中，铁律一禁止「把对方那份抄一遍」。选乙的代价：出口票要等 `#237`。
2. **速查支的名字与参数名**：推荐 `备忘录_速查表` ＋ `mode:"lookup"`／`q`／`--html`（与账单一字不差，出处 `skill-bill/src/render/helpPaths.ts:28`／`src/cli/cmd_read.ts:88-111`）。

**`combo` 侧：不登记**（先例：`combos.yaml` 全文 0 处 `bill.`，居家／作息／大厨同样未登记；卡路里那条 `calorie.help.lookup` 是页面不是文件交付）。

### 出口方向相反（本票要先解决的那条）

`src/cli/cmd_read.ts:129` 无条件开库 ＋ `src/fetch/db.ts:21-29` 缺目录即抛 ⇒ 今天没有库就看不了帮助。修法＝照 `skill-bill/src/cli/cmd_read.ts:493-495`，把 `memo.help.lookup` 在开库之前分派（`dispatchHelp` 不需要 `MemoDb`，初始化判据用「memo 库目录存在」）。**归票 9（`#229`）**，兼记票 8（接缝＝`dispatchHelp` 放哪），票 10 做回归锁（跑完断言目录里 0 个库件）。

### 事实不清／矛盾（如实上报）

- 地图 Notes `:57` 记的「与 `calorie_html/`／…／`memo_html/`／`schedule_html/` **同形**」是 `help/` 层在隔壁图的理由句，而这五个目录里**四个今天没有 `help/` 子目录**（只有 `schedule_html/` 与老 `CookHub/` 有）⇒ 该理由句与结论句方向相反，**理由句的事实归属＝事实不清**（本图判「不加 `help/`」用的是三条独立证据，不依赖这句话）。
- 票面／报告引的 `tooling/check-boundaries.mjs:37` 今天已漂到 `:41`（该文件带别的会话未提交改动，`skill-schedule` 已移出）；本席未改该文件。
- 老备忘录 `_N` 实测从 `_2` 起，而 `#237` 建议共用件统一从 `_1` 起 ⇒ 走乙则产物名递补数变化，需在出口票明写。
