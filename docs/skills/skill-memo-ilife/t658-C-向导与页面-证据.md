# t658-C · 备忘侧「向导与页面」移植证据（#665）

- 票：[#665 备忘侧：向导／提醒域 ＋ 页面交付（接共享渲染层）](https://github.com/FeatherHunter/ilife/issues/665)（`wayfinder:task`，Part of 地图 #658，依据规格 #657）。
- 接缝：技能统一出口（spawn `packages/skill-memo-ilife/dist/cli/cmd_read.js`）；两个注入点：`SKILLS_DB_PATH`（临时库）＋ `LARK_CLI_PATH`（挡板）。
- 老实现（仓外只读）：`D:\2Study\StudyNotes\SKILLS\备忘录\script\memo_cli.py`、`memo_render.py`、`scripts/feishu_auth_helper.py`、`script/init.sql`、`script/reminder_scheduler.py`。
- 取证日：2026-09-17。负责人中途裁定三条（DB 须与老库完全一致直连 `memo.db`；老技能权威；Q3/Q4 按推荐批），见本件第四节与 #665 进度区。

## 一、判定

**PASS**。票面五项逐条落地（§三），验收三条逐条有机器读数（本节）；老行为对照除第四节登记的偏离外逐条对上；六轮「改坏必红／还原必绿」全绿（§二）。

### 1.1 「一条命令出一张页」读数（`wizard-pages-665.test.mjs`，6 用例全绿）

| 路 | 命令 | delivery＋落盘 | 内容断言 |
|---|---|---|---|
| 排期向导 | `memo.wish wizard:plan suggestDue` | `delivery{mode,path,bytes}`＋落盘存在＋体积一致 | `<title>心愿排期向导</title>`＋条目正文，三标记填完；默认仅未排期，`all:true` 含已排期 |
| 完成向导 | `memo.wish wizard:complete` | 同上 | `<title>`＋默认不勾选（`selected:false`） |
| 批量改分类 | `memo.batch fromCategory＋toCategory` 收集；`ids＋toCategory` 执行 | 收集出页同上；执行回执 `updated/skipped/errors` | 页含向导标题；执行后分类真改 |
| 同步报告 | `memo.sync`（随行，缺省也出） | 缺省落 `memo_html/同步报告*.html`＋显式路径逐字落点，两支体积一致 | 页含同步报告标题 |
| 授权引导 | `memo.auth step:init/qr/poll/status` | init 回 `device_code`，qr 回 `.png` 路径，poll 回 `ok` | argv 逐条对老 helper 形状；lark 缺席 exit 4 |

### 1.2 相关套件（最终验证轮，全绿）

- 新用例 `wizard-pages-665` 6／6；`wish-sync-661` 11／11（含 T7 步 2 D-24 回摆）；memo 五件（fetch／cli／policy／render／skill）＋HELP 五件（228／229／230／243／245）全绿；`memo-e2e`、`combos-p8`、`synthetic-write-659`、`skilllink` 全绿。
- `snapshot --check` exit 0（187 件）；`gen:check`、`boundaries`、`doctor` 全 exit 0。
- 门禁留痕 runId：全扫 `38429e3c`（1 红，combos 注册表缺 memo.auth，见 1.4）→ 补登记＋重编后 `combos-p8` 复绿 `40e88405`；快照写 `f9ae0b1c`／验 `2e7d3fb4`；变异 `b91f9487`；gen `4c76aa69`；边界 `80b5724b`；doctor `75b47cc7`。

### 1.3 页面渲染走共享渲染层

- 填充机制：共享 `fillTemplate`（`base-paint`，含容器校验与 `<` 转义）；落盘：共享 `saveHtmlFile`（`delivery{mode,path,bytes}`）。
- 快照：`tooling/skill-html.snapshot.json` 重写（187 件），`--check` 全绿；模板探针由 `fillSharedMarkers` 切 `fillTemplate`（`tooling/skill-html-snapshot.mjs` 一行）。
- 影响面断言：零标记渗入（`MARKER_ALLOW` 保持为空）——见 D-28。

### 1.4 combos 注册表

- `memo.auth` 登记进 `combos.yaml`（`combos:` 段，照 `memo.help.lookup` 前例；`channels:` 段 help.lookup 本就不在，一并沿例）。
- 派生产物重出：`present.ts`（142 键）。**附带收口**：重出时把 HEAD 既有不一致一并带正——`combos.yaml@HEAD` 含 `calorie.goal.exercise` 而 `present.ts@HEAD` 没有（在途他席的地盘，本票只收口文件一致性，不动其语义）。

## 二、变异自证（六轮，改坏必红／还原必绿）

电池：`docs/skills/skill-memo-ilife/t665-变异电池.mjs`（经排队锁跑）；读数：`docs/skills/skill-memo-ilife/t665-变异读数.txt`。`MUTATION RESULT: PASS`。

| 轮 | 改坏成什么 | 红在哪条读数 | 还原 |
|---|---|---|---|
| MUT-1 | 排期向导填错模板（`wish_plan`→`memo_query`） | W1（`<title>` 真断言） | 绿，产物无残留 |
| MUT-2 | 提醒时间放宽回日期版 | policy 提醒用例 | 绿，产物无残留 |
| MUT-3 | 对账步 2 改成空操作 | T7（M-09） | 绿 |
| MUT-4 | 完成向导默认勾选 | W2 | 绿 |
| MUT-5 | 模板标记改坏一处 | W1（exit 5） | 绿 |
| MUT-6 | 手改 SKILL.md 一字节 | skill 互联区新鲜 | 绿 |

**三条实操教训（都写进电池头注释）：**
1. **增量门在“改完即编”节拍下漏编过一次**（改后 `tsc -b` exit 0 但产物未更新，测试假绿）：电池构建一律 `--force`，且有针的轮次断言产物里真有坏行。
2. **断言须验页面 chrome，不能只验注入数据**：首版 W1 只查了向导标题字符串——它同时存在于注入 JSON 里，错模板也绿；改查 `<title>` 后真红。
3. **`selected` 默认值翻转事件**：实现中途 `wizards.ts` 完成分支的 `selected` 被改成 `true`（与 W2 红相对应的真错位，非电池注入；并行提交实锤存在，见 5.3）。教训：收尾前必全量重跑一次，电池的“还原绿”不能代替它。

## 三、老行为对照

| 票面项 | 老出处 | 新侧当刻 | 读数落在哪 |
|---|---|---|---|
| 排期向导（批量、可带建议日期） | `wish-batch-plan`（默认未排期50／`--all`／`--ids`／`--suggest-due`／`--html`） | `planWizard`＋`wish_plan` 页，口径逐项同形 | W1 |
| 完成向导（勾选＋填打卡内容） | `wish-complete`（默认全列200／`--only-overdue`／`--ids`／`--content`／默认不勾选） | `completeWizard`＋`wish_complete` 页；执行走 `completeWish` 原子转换 | W2 |
| 批量改分类（勾选＋选目标分类） | `batch-update-category`（200／from／to／冲突计数）＋`update-category`（单条，sub 不动） | `collectBatchItems`＋`applyBatchCategory`＋`change_category` 页 | W3 |
| 提醒到期判定 | `due`→`list_due_reminders`（提前10分／延后窗口／循环跨周期重置；定时壳不搬） | `checkDueReminders`（`now` 可注入） | memo.remind 用例（cli／fetch 覆盖） |
| 提醒废弃／完成 | `dismiss`（标 dismissed，已废弃再废大声失败）／`completed`（打卡反查视图） | `abandonReminder`／`listCompletedReminders` | 同上 |
| 授权引导三步 | `feishu_auth_helper.py`（init／qr／poll＋status，非阻塞） | `fetch/auth.ts`＋`memo.auth`（argv 逐字） | W5／W6 |
| 页面交付六页 | `memo_render.py`（信封＋快照＋三标记注入） | `render/pages.ts`（信封＋快照移植）＋共享 filler＋窄兼容资产，模板除解两处预包裹外一字不动 | W1～W4＋首屏截图（证据附件） |
| 排期字段说法 | 老 `DIM_LABEL_MAP` 排期日期 | `WISH_DUE_LABEL`＝排期日期，面向使用者无英文术语 | HELP 现成＋W1 |

## 四、有意偏离清单

- **D-24 回摆（已签，2026-09-17 负责人确认）**：#661 曾把对账步 2 改为只标完成；DB 对齐后 `done` 列不存在，老原文是删心愿＋转打卡——本票照老执行（T7 覆盖）。
- **D-28 页面资产窄兼容**：模板只解两处预包裹（`<!--SHARED-CSS-->`／`<!--SHARED-HELPERS-->` 裸化，12 行），填充走共享 `fillTemplate`；资产不用共享 CSS／helpers——共享 CSS 是深色 token（`--bg:#16181d`）与模板浅色体系（`--bg:#f5f5f7`）互斥，共享 helpers 无六个 `window.*` 运行时，硬接即六页全红；搬老 `base.css／base.js`（47KB）即搬老公共组件，票面明令不搬。故资产为窄行为镜像（token A 组 11 变量＋六全局），新鲜代码非拷贝。影响面零渗入。
- **D-29 同步明细降级为计数**：对账回执只带 11 项统计不带逐条，报告页明细段用计数行（页面信息需求不变）。
- **D-30 版本位省略**：页面 `skill_version` 不再写老版本号 `1.3.0`（新实现不冒充，页面显示未知）。
- 沿用 #661：D-21（截断键统一）／D-22（一律查重）／D-23（C 口径）／D-25（退出码非零）／D-26（本地判重）／D-27（HELP 对齐）——DB 对齐后逐条复验仍成立（T2／T9／T10／T6／T2／HELP 五件）。

## 五、不隐瞒

- 全量 `pnpm test` 跑一遍：失败条目 26 条，逐条落在 `skill-calorie`（`#109／#255／#275／#291／#359／#367／#440／#561／wake-assets／#86／D2／FX-81-5／A7／triggers`，与 #88／#40／#86／#654＋在途 `goal.exercise` 一族同源）；**本票改动面 0 红**（引用检索：失败条目含 `memo|seam|snapshot|combos|skilllink` 者为 0）。
- 附带发现另开票（不动）：① 1.4 的 present 收口；② `combos-p8` 用例在 import 时自愈重写 `present.ts`（故自带相等断言恒过，树已保持干净）；③ 5.3 的 `selected` 事件。
- 测试安全：本地测试远端闸门一律钉死（死路径／挡板），本机 PATH 无真 lark；活库只做过存在性探查，从未打开。

## 六、交付物路径

- `packages/skill-memo-ilife/src/wish/{wizards,complete}.ts`、`src/fetch/{reminders,batch,auth}.ts`、`src/render/{pages,pageAssets}.ts`、`src/cli/cmd_read.ts`（向导／提醒／授权／交付）、`templates/**`（12 行解包裹）、`test/**`（含 `wizard-pages-665.test.mjs`）。
- 跨件（生成链派生）：`tooling/{contract-seam,skill-html-snapshot}.mjs`、`tooling/skill-html.snapshot.json`、`packages/base-combos/{combos.yaml,src/present.ts}`。
- HELP：速查表＋SKILL.md 随生成链重出（`memo.auth` 场景未建——HELP 场景管线被 228／243 钉死 30 场景，另开 HELP 票承接，此处只到速查表）。
- 提交：`0656b1ed`（数据层对齐）＋`391bd567`（向导／授权／页面交付）。

## 七、遗留出口

- D-24 已签（本条上收）。
- HELP `memo.auth` 场景（HELP 管线票）。
- 六页视觉统一（深色／新控件）若以后要做，另开票按共享 blocks 模型重写，不在本票。
