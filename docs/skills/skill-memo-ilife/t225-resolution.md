## 闸门裁决与关票依据

本票是必报五步第一、二步的门。闸门复审（复审员 G，`t225-review-G.md`）判 **有条件放行（68/100）**，其列出的**四条阻塞项已全部由裁决解决**，故门放行：

| G 的阻塞项 | 解决方式 |
|---|---|
| ① 报告 §1.3／§2.3／§五／§九 是「甲／乙」裁决前的旧快照（写着「乙 ＋ 同名同签名临时件」） | **裁决 3 判「甲」**（`t220-orchestrator-decisions.md` 第二节）：备忘录**自持一份最小管线**，`_N` 从 `_2` 起，头注释指向 `#237` ＋ `#240`，**不新造 `saveHtmlFile`**、**不挂 `#237` 阻塞边**。G 已给出可替换的 4 段文字，报告订正由票 5 会话并行落盘；**权威源是裁决正本，不是报告旧文** |
| ② B3（`src/help/index.ts` 转发）违反铁律五 | **裁决 16：B3 取消**——不转发、不扩包根出口（维持 49）。G 实测 B3 会让包根出口 49 → 58，撞 `structure.md:58` |
| ③ 资产形状漏 `aliases` 落点／`version` 要读 yaml／`editable_fields` 该写清洗后 64 条 | **裁决 5**（别名住技能侧资产、渲染时剥离）／**裁决 9**（`version` 从老 yaml 顶层读）／**裁决 6**（清洗 **22** 条：1 布尔 ＋ 9 非 html 落回 ＋ 12 html ⇒ 字段 **64** 条／29 场景） |
| ④ `D4` 归错票、且是「明天才红」 | **裁决 19**：归 **#231**（兼记 #229），并写明时序 |

## 本票已定的形状（放行给 #227／#228）

- **点头授予**：新建目录层级 `src/help/scenes/` 的点头由编排会话以项目负责人授权**授予**（裁决 17）。同时记录 G 指出的更省形状（8 个域文件平铺 `src/help/`，不新建目录层级）——**不采用**，理由是内容资产（机器生成、禁手改）与机器件（手写）分居两处，平铺会让 `src/help/` 变成 13 个文件混放。
- **告警线＝350 ＋ LF 口径**，落 `packages/skill-memo-ilife/AGENTS.md`。本包今天 **0 件超线**（最大 `src/cli/cmd_read.ts` 147；G 复核 22 件里 20 件逐字对上，两处已订正：`package.json` 34、`SKILL.md` 63）。⚠️ `AGENTS.md` 末行原引「`packages/skill-chef/AGENTS.md` 逐字同数」——**该文件不存在**，350 的真出处是用户答复（`docs/skills/skill-chef/map-chef-body.md:197`／`:212`），订正由票 5 会话并行落盘。
- **单文件必超线的事实基础**（G 实测）：同形先例 `packages/skill-schedule/src/help/scenes/help-assets.ts` ＝ 2198 LF／87 场景 ＝ **25.3 LF/场景**，备忘录 30 场景折算 ≈ **759 LF**。

## 另一条与本票口径相关的既定事实（记此备查）

包内 `npm test` **今天已经是红的**（`node tooling/write-snapshot.mjs --check` → 快照过期 `0.1.0@932e7b25…` ≠ `0.1.0@ef9b1647…`）⇒ **#230 的完成判据不许写成「包内 npm test 绿」**（裁决 18）。

## 进度：100%

下一步：已关票。#227（内容资产）与 #228（渲染接线）据本票形状与裁决正本开工；报告文字订正与 `AGENTS.md` 出处订正由票 5 会话并行落盘，**不影响放行**。
