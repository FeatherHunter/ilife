# 地图 #220 编排裁决记录（项目负责人授权：过程决策由编排会话定，每条须经对抗式复审）

本文件是**裁决正本**，供波次 2／3／4 的实施票直接引用。每条裁决都给「依据」与「影响谁」。
凡标注「本席亲测」的，是编排会话**自己读源码／跑命令**得到的，不采信任何转述。

---

## 一、交付路径与命名

### 裁决 1：落盘目录＝扁平 `<SKILLS_DB_PATH>/memo_html/`，**不加** `help/` 一层

- **依据**：①地图 `## Destination` 与 Notes「文档与产出落点（用户 Q3=A）」两处原话逐字扁平；②验收票 #233 原文要求「产物落老目录 `memo_html/`，与老实物并排」；③本席亲测——六张兄弟目录只有 `schedule_html` 有子目录，而它的 `help/` 最新一件是 **2026-08-11**（老产线世代）；**两张已完工并被维护者终审「过」的同流水线地图产物都扁平**：`calorie_html\卡路里_HELP_20260912_001033.html`、`biscuit_accountant_html\饼干记账_HELP_20260911_163117.html`；④记账落点代码 `packages/skill-bill/src/render/helpPaths.ts:53` 明写 `<dbDir>/biscuit_accountant_html/〈主体〉_<stamp>.html`，**无 `help/` 段**；⑤`memo_html\` 已有 12 件以上扁平老实物，`备忘录_HELP_20260820_150143_2.html` 证明 `_N` 递补后缀已在用。
- 证据正本：`docs/skills/skill-memo-ilife/t224-delivery-path-evidence.md`（含幂等复核命令）。
- **影响**：#229。文件名主体 `备忘录_HELP`、通式 `备忘录_HELP_<YYYYMMDD_HHMMSS>[_N].html`。

### 裁决 2：速查支产物名＝`备忘录_速查表`，显式参数＝`mode:"lookup"`

- **依据**：照抄样板是饼干记账（地图 Notes 点名）；`skill-bill/src/render/helpPaths.ts:28` `LOOKUP_FILE_STEM = '饼干记账_速查表'`，与 HELP 分名（#139 判法：别让用户按一个名字打开到另一个东西）；`skill-bill/src/cli/cmd_read.ts:71-73,93` 缺省＝HELP 文件、`mode:"lookup"`＝速查表、`q` 与 `mode` 互斥。
- **影响**：#229。

---

## 二、管线归属（本图分歧最大的一条）

### 裁决 3：**判「甲」——备忘录自持一份最小管线** ＋ 三条硬条件 ＋ 一张挂账的迁移票

> 本条**推翻了本席先前的「乙」倾向**，也推翻票 4 草案的推荐。裁决依据＝复审员 A（`t224-review-A.md`）＋ 本席逐条亲验。

- **判甲的决定性证据（本席亲验）**：
  1. **`_N` 递补口径**：`packages/skill-bill/src/output.ts:26-38` 逐字「**无 `_N` 则 `_2`**」（`:38` `return join(dir, stem + '_2' + '.html');`），`writeFileExclusiveWithRetry` 首试 `wx`、`EEXIST` 则 `_N+1`。实测三家现役实现（bill／calorie／`helpPaths.ts:13`）**全部 `_2` 起**，老实物 `备忘录_HELP_20260820_150143_2.html` 亦同。
     ⚠️ **归属更正（两位复审员在此互相矛盾，本席已回原始出处裁定）**：`_1` 起的裁定出自**大厨图 `#208`**，不是 `#237`——`#208:20` 逐字「① `_N` **从 1 起步**（照老家 `align_08.py:52-65`），**不照抄 bill 的 `_2`**」，`:129`／`:245` 同。而 `#237:23` 把 `_2`／`_3` 记为「**三家 HELP 现在的行为**」⇒ **两处上游自相矛盾**，走乙前必须先对齐。无论最终取哪个，`_1` 都会**改掉三家现役行为**，并与老实物 `_2` 不同形、撞 #233「与老实物并排」的验收。复审员 A 的**结论成立**，引用的票号需按此更正；复审员 B 的**更正准确**。
  2. **用户「乙」原话的范围**：`#208:145` 逐字把备忘划出该图（「其余 5 个技能（…／**备忘**）：本图只做私家大厨」），`#208:30` 的决议文自己锁定为「**三家**（私家大厨／饼干记账／卡路里）」。⇒ 方向可采纳，**但推不出「本图现在买单」**。
  3. **`#237` 不是文书动作，是人工审批闸门**：其下一步是「新建目录层级必须先点头再动手」（`structure.md:88`）；实测 `#237` OPEN、无 assignee、零评论、进度 0%。且 `#215`（大厨票 7）的 `blocked_by` **含 `#237`**（本席实测）⇒ 备忘排它＝同时排大厨的队。
  4. **乙对目的地零贡献**：目的地是「明确拿到文件」；乙把一道人工审批串进关键路径，还改产物名。复审员 A 给乙的「帮助达成目的地」评分 **32/100**（草案自身决策质量 58/100）。
  5. **成本差异只影响一个文件**：在跑的票 5 报告 `t225-structure-design.md:101` 自认「若裁甲，成本差异只影响 A5 一个文件的落点与件数」。
- **三条硬条件（#229 必须照做）**：
  1. **只抄通式那一小块**（`wx` 独占 ＋ `EEXIST` 递补 ＋ `resolve` 绝对路径 ＋ 本地时间戳），**不搬**卡路里的命令名→中文段落映射／动态段／三态回退；
  2. **`_N` 从 `_2` 起**（照三家现役行为与老实物）；
  3. **自持件头注释**写明：这是第 4 份同逻辑实现、指向共用件票 `#237`、指向本图的迁移票（下一行的新票号）。
- **欠债要挂账（不许默默抄）**：另开迁移票「备忘录出口落盘迁入共用件 `saveHtmlFile`」，**`blocked_by = #237`**；`#229` **不挂** `#237` 阻塞边（保持本图关键路径不断）。依据：`structure.md:23` 铁律一确实命中甲，故必须留可查的偿还记录；而三张票面里**没有任何一张记录过备忘录要迁**，不挂这张欠债就永远没人接。
- **影响**：#229（主）、#227（形状）。

> ⚠️ **本席先前通报的一处事实有误，此处更正**：`packages/skill-calorie/**` **工作树是干净的**（`git status --porcelain` 无条目）。带未提交改动的是 `packages/plugin-chef/**`、`packages/skill-chef/{SKILL.md,package.json}`、`packages/skill-schedule/**`、`pnpm-lock.yaml`、`tooling/check-boundaries.mjs`。由复审员 A 指出，本席已复验。

---

## 三、渲染契约的三条硬门（本席亲测，不是转述）

### 裁决 4：`memo.help.lookup` **必须**登进 `packages/base-combos/combos.yaml` 并重新生成 `present.ts`（推翻了票 4 草案「不登记」的结论）

- **本席亲测证据**：
  - `test/combos-p8.test.mjs:107-114` —— `for (const [key, shape] of Object.entries(MEMO_KEY_SHAPES)) assert.equal(reg.resolve(key).key, key)`，其中 `reg = createRegistry(PRESENT_KEYS)`。**命令表里多一条而 registry 里没有，这条测试当场红。**
  - `test/combos-p8.test.mjs:115-120` —— 断言 `present.ts` 与 `renderPresent(combosKeys(yaml))` **逐字相等** ⇒「手改 `present.ts` 无效（会被判红）」，**必须改 `combos.yaml` 再跑生成器**（`packages/base-combos/scripts/gen-present.mjs`）。
- **现状实测**：`combos.yaml` 已有 memo 的 10 条命令（`PRESENT_KEYS` 共 87 ＝ calorie 77 ＋ memo 10），**缺 `memo.help.lookup`**；`calorie.help.lookup` 在（`combos.yaml:116`）。
- **影响**：#229（加命令表那条 ＋ 登 registry 必须同批，否则红）。

### 裁决 5：`aliases` **留在技能侧资产，渲染时剥离**——不进渲染载荷

- **本席亲测证据**：`packages/base-render/src/spec/help.ts:150-190`，`scenes[]` 是 `additionalProperties: false` 的**闭集 7 键**（`required: id/title/wake_word/status/prompt_template`，另许 `types`、`editable_fields`）——**没有 `aliases` 位**；记账／作息／卡路里三个先例也无此位。
- **裁决**：别名（老唤醒词）**必须留在仓内资产**（这正是本图「把被漏掉的唤醒词做成官方源」的目的），但 `buildMemoHelpFileData` 组装渲染载荷时**剥离 `aliases`**；**不改 `base-render` 的共享 schema**（改它会牵动三家已完工技能）。
- **收词规则（措辞已由裁决 21 订正）**：只收**会路由的词**——**老侧或新表的唤醒词**都算（老 yaml `wake_word` ＋ 老 `SKILL.md` 三张表与 `:300`／`:302-305` 的别名 ＋ 新表里能归到某个老场景的词，后者由**裁决 7** 授权）。**42 条口语样例不进**——它们是自然语言话术，不是路由键。
- **影响**：#227（资产形状）、#228（组装时剥离）。

### 裁决 6：`editable_fields` 清洗是**硬门**，不是整洁偏好

- **本席亲测证据**：`help.ts:174-188`，`editable_fields[]` 亦 `additionalProperties: false`，`name`／`label`／`value` **必须 `string`** ⇒ 那条布尔脏数据**过不了 schema**；`help.ts:172` `status` 只许 `''` 或 `'【待开发】'`。
- **裁决**：清洗口径**已由复审员 C 更正**（侦察报告原文的算术是错的，**不许照抄**）：
  - 要清的**共 22 条**，不是 23 条——**①⊂②，布尔行被重复计入**。正确分解＝**1 条布尔 ＋ 9 条非 html 非布尔的 ASCII label 回落 ＋ 12 条 html ＝ 22**；
  - 需补中文名的**非布尔**落回行是 **9 条**（`remind_at`×2／`repeat_rule`×2／`start`／`end`／`status`／`tasklist_guid`／`reminder_id`），不是 10 条；
  - **12 条 `html` CLI 开关剔除** ⇒ 字段条数 76 − 12 = **64**（这半句原文是对的）；
  - ⚠️ **本席原写「落完断言涉及场景 = 29」是清洗前的数，已订正**：实测生成器产出 **64 条／含字段场景 27**。原因是那 12 条 `html` 里有 **2 个场景只有 `html` 字段**，剔除后归零 ⇒ 场景数 **29 − 2 = 27**。本席 2026-09-12 复核了产物侧的场景清单（8 个域文件共 **30** 个场景 id：checkin 3／init 1／memo 6／mood 3／remind 4／search 7／sync 1／wish 5），并按「无字段场景 = 30 − 27 = 3」逐一落位（`memo_init_setup` 本就无字段、`memo_sync_feishu` 与 remind 域一个场景的字段**只有** `html`）⇒ **27 是对的、29 是旧的**。
  - 生成器 `--check` 的自证行逐字：「形状：域 8／二级组 13（兜底 4）／场景 30／字段 64（含字段场景 27）／别名 12」。
  ⚠️ **杀伤**：这是侦察报告里**唯一被写成施工量的错数**，照它写 fail-closed 断言会**必红**（本席原话照抄了报告的口径，同错，此处一并订正）。
- **影响**：#227。

---

## 四、并发纪律（本席亲测的活雷，实施票必读）

1. **`pnpm-lock.yaml` 正被别的会话改写**（实测 13:06:42 还在动）。
   ⇒ #228 给 `packages/skill-memo-ilife/package.json` 加 `base-paint` 依赖时，**避开全量 `pnpm install`**；写完必须核锁里各家条目都在，别把别人的改没了。
2. **`tooling/check-boundaries.mjs` 带别的会话未提交改动**：`'skill-schedule'` 已被移出 `SKILLS_BASE_FROZEN`，**行号已从票面写的 `:37` 漂到 `:41`**。
   ⇒ #228 **只能就地改工作树**、只删 `'skill-memo-ilife'` 一项、**保留对方的改动与注释**，绝不按 `HEAD` 重写整个文件。
3. **本图工作树里只许出现自己人的改动**：`packages/skill-memo-ilife/**`、`packages/plugin-memo-ilife/**`、`docs/skills/skill-memo-ilife/**`，外加 #228 必改的 `tooling/check-boundaries.mjs` 那一项与 #229 必改的 `packages/base-combos/**`。
   `packages/skill-chef/**`、`packages/skill-calorie/**`、`packages/skill-schedule/**`、`packages/plugin-chef/**` 属别的会话，**一律只读**。
4. **门基线（供归因，本席实测）**：`pnpm boundaries` **PASS**；`pnpm --filter base-paint gen:help-shell:check` **OK**。波次 2 落地后若变红，是本图的责任，不许赖给别人。

---

## 五、内容裁决（侦察报告 §7F 待裁项中，本席就地定案的）

| # | 待裁项 | 裁决 | 依据 |
|---|---|---|---|
| 1 | `备忘改分类` 一词对两场景（老 yaml `:105` 单条／`:361` 批量） | **逐字保留，两场景共用该唤醒词** | 老 yaml 自己的头注 `:16` 明写「30 个场景 = 29 个唯一唤醒词（28 业务 + 首次使用;**备忘改分类 单条+批量 共用**）」⇒ 是**有意共用**，非缺陷。#227 验收判据是「与老骨架逐字对得上（条数、名称、顺序）」，且渲染 schema **无唯一性约束**；另造一个词反而会造出「路由不到的词」，比忠实照搬更糟。对账里注明「共用为老骨架自述，非缺陷」 |
| 2 | 二级组 `id`：老生成器 0 起，票 6 裁新资产 1 起 | **照票 6：新资产从 1 起** | 票 6 为用户裁定项；与老生成器不同之处**必须在对账里逐条声明** |
| 3 | 8 条 `prompt` 含 CLI 开关／子命令 | **清洗**（用户 U6 立规：命令不上页面） | 地图 Notes「命令不出现在页面上」 |
| 4 | 另 6 条 `prompt` 含 DB／实现细节（`notes.due`／`note`／`task_guid`／`Cron` 等） | **一并清洗** | 老 yaml 自己的契约 `:9` 就禁止「暴露 DB」；与「命令不上页面」同一条理——页面只出现用户能说的话。逐条在对账声明 |
| 5 | 42 条口语样例进不进 `aliases` | **不进** | 见裁决 5 的收词规则 |
| 6 | 包内 `test` 脚本盖不到新用例 | **同批修** | 本席亲测 `packages/skill-memo-ilife/package.json` 的 `scripts.test` 只跑 `../../test/scaffold.test.mjs`，盖不到包内 5 个 `test/*.test.mjs` |

---

## 六、复审落定情况

- **裁决 3 的执行形态**：已由复审员 A 裁决为**甲**，本席亲验后采纳（见第二节裁决 3）。
- **#224 草案的事实断言**：复审员 B 打假完毕（65/100），三处更正已并入 #224 的决议与关票评论。
- **#227 侦察报告**：复审员 D 判「优秀的**老侧考古字典**，但**不能当 #227 的开工简报**」（覆盖度 62／可执行性 58／一致性 72），另开复审员 C 专攻数字。其 M1–M8 修正已并入下节。

---

## 七、内容与生成器的施工规则（#227 照此做；来源＝复审员 D ＋ 票 6 裁决 ＋ 本席裁定）

### 裁决 7：场景数＝**30**，新表多出的词**不新开场景卡**

- 老骨架 30 场景逐字搬；**情绪孪生 4 条各占一行场景是对的**（票 6 U4 针对的是新表那 4 条新词）。**主词取老词**。
- 新表 28 条里老骨架没有的那些词：**能归到某个老场景的 → 进该场景的 `aliases`**（票 6 U4「老词为主名、新词进 `aliases`」）；**无老场景可归的（如 `memo.stats`）→ 不落页面**（票 6 U5「都按老的来」）。
- ⚠️ Destination 里那句「新表多出的条目补进对应域」按本条解释为**补进对应域的既有场景**，不新增场景卡——票 6 的 U4／U5 是更具体、更晚的裁决。
- **`aliases` 的落点见裁决 5**（住技能侧资产、渲染时剥离）。注意老侧 `references/schema.md:30` 逐字「禁字段 `aliases`」——那是**老 yaml 的**约束，管不到新资产，但要在对账里说明这层区分。

### 裁决 8：`types` 白名单要**自己编码**，模板不会挡

- 实测 `packages/base-render/src/spec/help.ts:158-171` 对 `types` **没有 enum，任意字符串都过** ⇒ 白名单从老侧到新仓**没人继承**，本图必须自己编码。
- 老侧白名单出处：`references/schema.md:14`（七值）／`script/validate_scenarios.py:22-25`（八字面量）。**实际用到 4 个原子**：`{回执:30, 采集:20, 查看:10, 向导:4}`，合计 64。落完**断言**这组计数，且 `types` 里不出现「选择」。

### 裁决 9：生成器纪律（照记账，不照抄作息）

- **`version` 从老 yaml 顶层读**（实测 `GLOBALS {'skill':'备忘录','version':'1.3.0'}`），**不许写死**成第四份副本。
- **老条目加摘要锁**（照 `packages/skill-bill/scripts/gen-wake-assets.mjs` 与 `wake-assets.test.mjs` 的 SHA-256 摘要断言）；`--check` 只比对不落盘；**禁手改生成物**。
- ⚠️ **不许照抄作息的单文件资产**：`packages/skill-schedule/src/help/scenes/help-assets.ts` 实测 **2198 行**，一照抄就撞裁决 10 的 350 线。作息还有两处**不可照抄**：它**有意不落 `types`**（备忘录必须落）、它**主动标 `【待开发】`**（备忘录 U1／U2／U3 禁止标缺失）。
- 对账里**不要引 `tests/test_html_trigger_coverage.py`**——实测**该文件不存在**（老 `SKILL.md:290-292` 声称有它）。

### 裁决 10：资产必须拆件守住 350 线

- 本包告警线＝**350 ＋ LF 口径**（已落 `packages/skill-memo-ilife/AGENTS.md`，管 `src/**/*.ts` ＋ `scripts/*.mjs`）。**本席亲测本包当前最大的源文件只有 63 行**，而兄弟产物量级：作息资产单文件 2198 行、记账 986 行 ⇒ 备忘录资产**若落单文件必然超线**。
- ⇒ 按票 5 的形状拆 **8 个域文件**（`src/help/scenes/<域>.ts`，各 40–150 LF）＋ `sceneData.ts` 组装件。**这是本图对作息形状的有意偏离**，理由即本条。

### 裁决 11：「#227 只需读 4 处源、没有第五处」**作废**

- 复审员 D 证伪：至少还有 6 处——`references/schema.md`（含 `:30` 禁 `aliases`、`:14` types 七值）、`script/validate_scenarios.py`（影子强制门，含 `PROMPT_FORBIDDEN`）、老 `SKILL.md:1135-1141`（页面级验收 5 条）、yaml 顶层 `skill`／`version`、`tests/`、`references/examples.md`。
- ⇒ #227 的开工简报按**这份扩充后的源清单**走，不按「4 处」。

### 裁决 12：`base-paint` 依赖是本票的硬前置

- 实测 `packages/skill-memo-ilife/package.json` 的 `dependencies` **只有 `base-link-core`**，`tsconfig.base.json` 无 `paths` ⇒ 出口层 `import ... from 'base-paint/help-shell'` **直接 TS2307**。
- ⇒ #228 必须**同批**加依赖（手工加一行，值同记账 `^0.3.0`），并按第四节第 1 条避开全量 `pnpm install`。

### 裁决 20：**A 路正确优先**——「满足校验器」与「页面渲染正确」冲突时取页面正确

> ⚠️ **补录说明**：本条曾以**消息形式**发给票 8 并被执行，但**漏写入本正本**，由复审员 J 在审票 8 时发现（`t228-review-J.md`：「它引作最高依据的『裁决 20』在裁决正本里根本不存在」）。**这是本席的记账失误**，2026-09-12 补录，编号保持 20 以免与既有引用错位。

- **实测事实链**（票 8 逐维隔离探针）：通用模板有**两条消费路**——**A 路** `renderHelpShellHtml`（出整页 HTML，**不做 schema 校验**）与 **B 路** `renderHelpShell` → `validateSceneData`（fail-closed 校验器）。**本图的交付路径就是 A 路**，维护者的验收也**只看页面**。
- **裁决**：**凡「满足校验器」与「页面渲染正确」冲突时，取页面正确那一边**，并把该分歧**逐条记账**（记进交付报告 ＋ 共享层缺陷票 `#242`），**不许改共享层**（`packages/base-render/**` 当时正被别的会话改，且改它会牵动卡路里／记账／大厨／作息四家）。
- **据此落的四键**（票 8 执行）：
  | 键 | 取边 | 理由 |
  |---|---|---|
  | `init_banner.steps` | 传 **`{title,desc}[]`** | 传 `string[]` 能过校验器但**六个步骤格文案全空**——那是**用户看得见的缺陷** |
  | `init_banner.hidden` | **传** | 模板**唯一的显隐开关**，不传则「已初始化」的差异永远显不出来（实测两份产物**只差这 1 字节**） |
  | `init_banner.closable` | **不传** | 缺省已等价老的 `closable:true`，少一处闭集外字段是更优选择 |
  | `contact.items[].url` | **不传**，链接明文进 `value` | 共享 schema 的 `contact.items[]` 是闭集，加 `url` 必 `schema-invalid` ⇒ 用户 V6=B 的「可点」那半条**落空**，根因归 `#242` |
- **留证要求**（已执行）：证明**本图最终载荷喂给 B 路会因 `hidden` 与 `steps` 对象形被判 `schema-invalid`**，且**其余形状（groups／scenes／`editable_fields`／contact／version／subtitle）在 B 路全过**——**这是共享层缺陷的证据，不是本图交付的缺陷**，免得 `#233` 的读者误判。
- ⚠️ **踩坑留证**：B 路校验器在**包根 `base-paint`**；子路径 `base-paint/help-shell` 导出的 `renderHelpShell` 只是 `renderHelpShellHtml` 的**别名**（复审员 J 实测 `===` 为 true），**不做 schema 校验**。

### 裁决 21：票 7 交付时上报的 D1–D7 逐条裁决（2026-09-12）

| # | 上报事项 | 裁决 |
|---|---|---|
| D1 | 「涉及场景＝29」与算术 27 冲突 | **判 27**（清洗掉 12 条 `html` 后，`memo_completed_reminders`／`memo_sync_feishu` **只剩 `html` 一个维度 ⇒ 零字段**，29−2=27）。**两个数都断言**（老口径 29／清洗后 27）。票 5 报告里的 29 是旧数，由本席订正 |
| D2 | DB 类清洗从裁决点名的 6 处**扩到 12 处** | **保持 12 处，不算越界**：裁决给的是**规则**（页面只许出现用户能说的话），不是「只许在 6 处执行」；同规则续扫出的 6 处是正常适用，**只清 6 处才是选择性执行**。逐条进对账 |
| D3 | 生成器 **411 LF** 超告警线 61 行 | **维持「先不拆」**（已按必报五步第四步逐字报出；一次性生成器、不进 build／test 管线、拆了多一层间接；真判据是 9 个资产件全部 ≤132 LF） |
| D4 | `sceneData.ts` 导出 **3** 个（票 5 记 2 个） | **批 3 个**（`MEMO_HELP_GROUPS`／`buildHelpSceneIndex()`／`MEMO_HELP_VERSION`）：`version` 依裁决 9 必须从老 yaml 读、不许写第四份副本，为它多一个导出是正解。⚠️ 前提＝裁决 16 仍有效，**这 3 个名字不得转发进 `src/help/index.ts`、不得扩包根出口（维持 49）** |
| D5 | 残余**可见**文案 4 处（1 个 title 的 `(同步飞书 due)` ＋ 3 处 hint 的 `GUID`／`active(默认)/dismissed`／`'null' 清除`） | **要清**——与 D2 同一条规则且**可见性更高**（在 title 与 hint 上）。**保留语义、只换说法，不许删信息**（`(同步飞书 due)`→`(同步到飞书)`；`GUID(可选)`→`任务清单 ID(可选)`；`active(默认)/dismissed`→`活跃(默认)/已忽略`；`'null' 清除`→`留空即清除`）。若某处改了会丢用户需要的语义，**允许保留并说明理由** |
| D6 | 4 条别名（`改子分类`／`查提醒`／`记一条`／`添加笔记`）只有新表依据，裁决 5 与裁决 7 口径相左 | **保持 12 条**——是本席两条裁决的**措辞**打架：**裁决 7 更具体更晚，优先**；裁决 5 那条的真意是**排除 42 条自由口语样例**，不是排除新表路由词。**裁决 5 措辞已订正为**：「只收**会路由的词**（老侧或新表的唤醒词），排除自由口语样例」 |
| D7 | `memo_init_setup` 的 prompt 保留 `Python`／`CLI`／`环境变量` | **套用 D5 同一条规则**（实现细节字面改成用户能说的话，保留语义不删信息）。⚠️ 例外：改了**会误导用户怎么做**的（初始化确实要动环境）**保留并说明**。侦察那句「属正常，不建议清」是**建议不是裁决** |

### 裁决 22：老实物载荷的**场景顺序 ≠ yaml 书写序**（票 7 实测发现，入档）

- **事实**：`memo_batch_change_category` 在 yaml 里写在 `:361`，却被老转换层 `script/memo_render.py:551-560` 收进 **`memo` 域「分类调整」组**——因为该转换层**先按 `category` 分域、再按 `subfunction` 收组**，与 yaml 的书写顺序无关。
- **裁决**：**新资产照「载荷序」**（＝HELP 实际呈现序，也就是老用户看到的那一版顺序），并在生成器里**与 yaml 书写序双向钉住**（两源对不上即 fail-closed）。
- **影响**：#227 已按此落；#228 的对账与 `#233` 的肉眼终审都以**载荷序**为准。这条是「逐字对得上」判据里最容易被误判成偏差的一环，故单独入档。

### 裁决 23：接受票 7 在 D5／D7 上的两处**更优改词**，并采纳其「以老侧自己的用户词为准」的方法

票 7 执行裁决 21 时**没有照抄本席给的示例词**，而是回老侧取词，本席复核后**判定它的版本更优、采纳**：

| 处 | 本席示例 | 票 7 实落 | 为什么它的更好 |
|---|---|---|---|
| `memo_add_wish` 的 `hint:tasklist_guid` | `任务清单 ID(可选)` | **`飞书任务清单 ID(可选)`** | 示例词**丢了归属**（不说明是飞书的任务清单），属于「换说法时删了信息」，与本席「保留语义不删信息」的要求相悖 |
| `memo_reminders_active` 的 `hint:status` | `活跃(默认)/已忽略` | **`有效(默认)/已废弃`** | 它用的是**老侧自己的用户词**——老 `SKILL.md` 写「查看所有**有效**提醒」，老 `dismiss` 的中文名是「**废弃**提醒」。**同义但更贴老侧口径**，用户读起来与唤醒词一致 |

**D7 的处置同样采纳，且它的理由比本席给的那条例外更硬**：本席只给了「若改了会误导用户怎么做则可保留」这条例外；票 7 实际给出的是**「与老侧同词」**——老 `SKILL.md:312-317` 的安装 prompt **自己就用**「运行环境／数据存储／飞书联动(可选)／初始化数据库」这套用户话，故新措辞不是新造、而是回归老侧口径。它同时**保留「数据库／初始化数据库」**（同处老 prompt 逐字写「初始化数据库:建好备忘录的数据表」），理由是**删了反而丢信息**。

**由此立一条通用口径（本图适用）**：凡「页面出现实现细节字面」要改时，**优先回老侧找它自己的用户词**，而不是自造同义词；**改词不得削弱归属信息，也不得删除老侧已在用的用户可见名词**。有疑义时**保留并说明**优于擅自删除。

### 裁决 24：票 7 关于 `git add -A` 的事实澄清——**记下两个相左的说法，不改处置**

- 票 8 报「票 7 在同一时刻 `git add -A`」；票 7 澄清「**从未跑过 `git add -A`，两次都是显式列路径**」。
- 本席**无法从现有证据判定谁对**（两者的可观测后果相同：票 7 的件出现在暂存区）。**处置不变**：本席已核索引**无污染残留**，并已立规**本图所有 agent 一律不许再动暂存区**（不许 `git add`／`restore --staged`／`reset`／`commit`），暂存与提交由编排会话独占。
- **入档理由**：这是「同一事实两个 agent 各执一词」的又一例（与 `_N` 起数、63 vs 147 行同类）。**处置不依赖谁对**，但**说法要留痕**，免得后续复审把它当成定论。

### 裁决 25：**撤销「不建 `~/.agents/skills`」**——实机证据推翻了静态分析（2026-09-12）

**本条是一次由维护者实机证据触发的判断纠正，记全来龙去脉。**

- **原判断（本席先前采纳复审员 E／F 的意见）**：「`~/.agents/skills` 先不建」。理由（E）该根是**用户全局根**、插件提供方那一路已把技能送进技能表；（F）机制层——`dsh-skill` 的 `collectFresh` 是 `layers=[global, ...chainLayers]` ＋ `merged.set` 覆盖 ⇒ **跨层「最近者赢」、rank 无效且静默**，而 `skill-calorie` 的**陈旧拷贝**正在该根生效。
- **推翻它的实机证据（维护者在 DSH 里说「备忘录 HELP」的返回）**：DSH 报「**备忘录这块我这儿没挂技能**」，而它列出的可用技能恰是 **`skill-bill`／`skill-calorie`／`skill-chef`** 三个。
- **本席实测这一比一对应**：`~/.agents/skills` 里正是 `skill-bill`(Junction→仓库)／`skill-calorie`(真目录·陈旧拷贝)／`skill-chef`(Junction→仓库)，**`skill-memo-ilife` 不存在**。
  ⇒ **DSH 认技能的入口就是 `~/.agents/skills`**，**不是**（或不只是）插件提供方那一路。**原判断错在"插件提供方够用"这个前提上。**
- **处置（已执行）**：建 **Junction**（**不是拷贝**——`skill-calorie` 的陈旧拷贝就是反例）：
  `cmd /c mklink /J "%USERPROFILE%\.agents\skills\skill-memo-ilife" "D:\ilife\packages\skill-memo-ilife"`
  实测 `LinkType=Junction`／`Target=D:\ilife\packages\skill-memo-ilife`／经链接可读 `SKILL.md`。
- **⚠️ 连带推翻本席另一处说法**：本席先前告诉维护者「**真机往返需要重载 DSH**」（依据：进程启动 00:39:56 早于产物写入 13:24:36）。**实测不需要**——建完链接后**技能目录立刻更新**，`skill-memo-ilife` 当场出现在可用技能表里。⇒ **DSH 是运行时读该根，不是启动时快照**；「需要重载」那句**作废**。
- **端到端复验（本席在 DSH 内完成）**：加载 `skill-memo-ilife` 成功（说明面从该根经 Junction 读出）⇒ 按其唯一出口真跑 ⇒ `exit=0`、`delivery.path` ＝ `D:\2Study\StudyNotes\.db\memo_html\备忘录_HELP_20260912_140336.html`、`delivery.bytes`＝**130825** 与实盘一致、**跑完 `<db>\memo` 仍不存在**。
- **教训（入档）**：**静态机制分析（"哪一层赢"）不能替代"这个入口到底认不认"的实机验证**；复审员 F 的层序分析本身没错，错的是由它推出「所以不必建」这一步。

### 裁决 13：#229 的「同批必改四处测试」——本席已逐处亲验，四处都真的会红

命令表从 10 条加到 11 条（`memo.help.lookup`）会连带撞四处既有断言。**#229 必须同批改，否则门红**：

| # | 文件:行 | 现断言 | 改成 |
|---|---|---|---|
| D1 | `test/memo-split.test.mjs:15` | `assert.equal(rows.length, 10)` | 11 —— ⚠️ **配套另一半本席已亲验**：该测试 `:9-11` 是**解析** `docs/memo-migration-split.md` 里 ```` ```memo-keys ```` 围栏块（`:18-29`，现有 10 行 `memo.search … memo.stats`）⇒ 必须**同批给该围栏块补第 11 行 `memo.help.lookup \| list`**，并把该节标题「## 联动 key×shape 映射（**10 处**…）」（`:16`）改成 11 处，否则测试改 11 仍然红 |
| D2 | `packages/skill-memo-ilife/test/render.test.mjs:20` | `assert.equal(Object.keys(MEMO_KEY_SHAPES).length, 10)` | 11 —— ⚠️ **配套另一半本席已亲验**：同文件 `:5-16` 的 `GOOD` 载荷夹具正好 10 条（`memo.search … memo.stats`）⇒ 必须**同批补第 11 条** `'memo.help.lookup'` 的载荷（shape 为 `list`），否则该用例 `:21-23` 的逐键遍历会取到 `undefined` |
| D3 | `test/combos-p8.test.mjs:107-114` | 逐条断言 `MEMO_KEY_SHAPES` 每键都在 `createRegistry(PRESENT_KEYS)` 里 | **不改测试**——改 `packages/base-combos/combos.yaml` ＋ 重跑生成器（见裁决 4）；另 `:115-120` 断言 `present.ts` 与生成输出逐字相等 |
| D4 | `packages/plugin-memo-ilife/test/skills-provider.test.mjs:90` | `assert.ok(!def.content.includes('memo.help.lookup'), …)`（反向断言，注释自述「#229 落地说明面后本行须改成正向断言」） | 翻成正向断言（归 #231，与说明面同批） |

> 本席已逐处打开文件核对原文，四处均与上表相符。

### 裁决 14：两条被复审员 C 打掉的派生数（#227 不许照抄侦察报告原文）

复审员 C 把报告 `:647` 的 15 条自证断言逐条重算，**15 条全对**；承重数（30 场景／8 域／13 二级组含新老 id 映射／4 兜底／30 行 types 表／76 条字段／29 涉及场景／skeleton mismatch=0／两表缺 3）**一条不错**。打掉的是派生数：

1. **`--html` 的条数**：侦察报告 `:179` 写「`--html` 4 条」→ 实际 **6 个场景**（`search_keyword`／`get_detail`／`reminders_active`／`complete_wish`／`wish_schedule`／`sync_feishu`）。正确且不重不漏的分解：**`--html` 6 ＋ 子命令名 4 ＋ `-c` 1，并集 8**（总数 8 仍对）。⇒ 去命令化的**改写清单仍是 8 条**，照 8 条做，只是别按「4＋4」理解。
   ⚠️ **口径补充（本席据产物实测）**：上面那 8 是**场景数**；若按**改动处数**数，生成器 `--check` 报的是 **prompt 改动 20 处（CLI 9 ／ DB 11；牵动场景 14 个）**——即 14 ＝「8 条 CLI 场景」∪「6 条 DB 细节场景」，20 ＝ 逐处计数（有的场景含 2 处）。**两个口径都对，别混用**：断言场景数用 8／6／14，断言改动处数用 20。
2. **`types` 的原子分布**：侦察报告 `:584` 写「33 个场景组里 27 条两原子、3 条三原子」→ 实际 **30 场景 ＝ 26 条两原子 ＋ 4 条三原子**。原子计数 `{回执:30, 采集:20, 查看:10, 向导:4}`（合计 64）本身是对的。

### 裁决 15：`meta_blocks` 是**合规落点**，但用户 V1 已裁不用

- 侦察报告 `:476`（B.6）把顶层允许键写成 7 个，**实际 9 个**（补 `meta_blocks`、`recommendations`，闭集到 `help.ts:238` 才闭合），故它 F.8 那句「`help_only` 9 条**无源可派生**」**漏报了一个现成合规落点**——老实物 HELP 自己的唤醒词就装在 `meta_blocks`（`packages/skill-bill/src/triggers/wake-assets.ts:980` 逐字承认，`packages/base-render/src/spec/help.ts:117-124` 明确允许）。
- **但结论不变**：票 6 **V1=A** 是用户裁定——「**不传**『怎么喊我』块、HELP 自身唤醒词不上页面」⇒ 落点**存在但被有意不用**。#227 的措辞应是「有合规落点，用户 V1 已裁不用」，不是「无源可派生」。

### 裁决 16：**取消 B3**——不把新件转发进 `src/help/index.ts`，不扩包根出口

- 证据（复审员 G 实测）：`packages/skill-memo-ilife/src/index.ts` 逐字 `export * from './help/index.js'`，**包根运行时出口实测 49 个**；B3 一加变 **58**。`docs/agents/structure.md:58` 明列「一层只做转发的包装」为**违反样子**；票 5 报告引 `:94` 给它开豁免是**引反了**（`:94` 是缩小接口用的）。
- **裁决**：新件**不进** `src/help/index.ts` 的转发，**不扩包根出口（维持 49）**。包内消费者（`cmd_read.ts`）直接 import；真正需要的对外面今天是零。理由＝铁律五「接口小、里面厚」。

### 裁决 17：资产仍落 `src/help/scenes/`（**新建目录层级的点头由本席授予**）

- 事实基础：单文件必超 350——同形先例 `packages/skill-schedule/src/help/scenes/help-assets.ts` 实测 **2198 LF／87 场景 ＝ 25.3 LF/场景**，备忘录 30 场景折算 ≈ **759 LF**。
- 复审员 G 指出一个**更省但未被比较**的合法形状：8 个域文件**平铺进 `src/help/`**，同样合规且**不新建目录层级** ⇒ 直接消掉 `structure.md:88` 的点头触发条件。
- **裁决**：维持 `src/help/scenes/`，**点头由本席以项目负责人授权授予**。理由：内容资产（机器生成、禁手改）与机器件（手写）分居两处；平铺会让 `src/help/` 变成 13 个文件混放，伤 AI 可导航性。**票 5 报告须补上这个对比**（哪怕结论是维持）。

### 裁决 18：包内 `npm test` **今天已经是红的**——#230 的完成判据不许写成「包内 npm test 绿」

- 证据（复审员 G 实测）：`node tooling/write-snapshot.mjs --check` → `FAIL: 快照过期（文件 0.1.0@932e7b250d278d50 ≠ 实际 0.1.0@ef9b16473d03cf19）`。
- ⇒ 票 5 报告 §3.1 说的「给包内 `test` 脚本加一行 `test/*.test.mjs`」**不会让包内 `npm test` 变绿**。**#230 的完成判据必须写成「新用例真 spawn 全绿 ＋ 变异自证」**，不许写成「包内 npm test 绿」；那条快照红是本图**之外**的既有状态，须与 §229／#230 的账分开记。

### 裁决 19：`D4` 归 **#231**（兼记 #229）

- `packages/plugin-memo-ilife/test/skills-provider.test.mjs:88` 注释逐字写「归 #231／#229」、`:90` 语义写「#229 落地说明面后」。该断言锁的是 **`SKILL.md` 正文**是否含 `memo.help.lookup`，而正文归 **#231**（票 12 的分界注释自己写了「本票只动正文」）。
- 时序也要写清：它是**明天才红**，不是「不改就红」。

### 引用订正（复审员 D 实测）

- 票面／侦察报告写 `packages/skill-memo-ilife/src/render/helpFile.ts:152` —— **该文件不存在**（`src/render/` 下只有 `envelope/errors/html/index/templates.ts`）。正确出处是 `packages/skill-bill/src/render/helpFile.ts:151-153`。
- `db.ts` 缺目录抛在 `:22`，整段实为 `:19-27`（票面与报告写 `:21-29`）。

### 两位席位的数字冲突，本席已回原始出处裁定

- **复审员 D 称「本包当前最大源文件 63 行」——不成立。** 本席按 `AGENTS.md` 的口径（LF，只数 `\n`，范围 `src/**/*.ts` ＋ 包内 `scripts/*.mjs`）逐件实测，最大是 **`src/cli/cmd_read.ts` ＝ 147**，其后 `fetch/db.ts` 108、`fetch/feishu.ts` 100、`render/html.ts` 62、`policy/wakewords.ts` 58。**票 5 的 147 是对的**，D 的数字错。
  但 D 的**结论仍然有效**：资产若照抄作息的单文件形状（实测 2198 行）必撞 350 线，故裁决 9／10 照旧。
- **`aliases` 的真实来源三处（订正侦察报告 A.7 的出处栏）**：侦察报告把 A.7 的第 8–12 条出处写成「21 条子命令」，实测老 `script/memo_cli.py` 里 `alias|aliases|HELP_` **零命中** ⇒ **子命令这一处贡献 0 条**。真实来源是：① 老 `references/scenarios.yaml`；② 老 `SKILL.md` 的两张表与 `:300`／`:302-305`（后者是「分类子唤醒词」12 条）；③ `references/examples.md`。⇒ #227 落 `aliases` 时**不要去找 CLI 那一处**，并须交代 `:302-305` 那 12 条子唤醒词是否入选（本席规则：只收老侧会路由的词，故入选，但主名仍取老词）。

