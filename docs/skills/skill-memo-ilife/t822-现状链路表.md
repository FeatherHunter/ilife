# #820 现状链路表：30 场景 × prompt → 唤醒词 → 命令 → 参数

**谁读**：#822（册子冻结）、#825（验收形制）、#834（收口）与 8 张域票开工前先读。**这一页给的是「今天是什么样」，不是「应该是什样」** —— 目标态由 #837 的八条 ＋ #838 的实施定。

**怎么量**：唤醒词与命令取自官方源（`src/help/scenes/*.ts`）；**「现路由」那一列是把主名真喂给 `routeWakeword()` 跑出来的**（`dist/policy/wakewords.js`），不是读表猜的。生成器 `research/gen-scene-route-table.mjs` 可重跑。

## 一 汇总

| 状态 | 条数 | 含义 |
|---|---|---|
| **通** | **9** | 词直接命中、命令分支也在 |
| **命中但需补槽位** | **5** | 词命中了，但表里给它挂了 `needs` 槽位；场景 prompt 里本来就有对应字段，AI 填进去即可 —— **不是缺陷，是口径**（`POLICY_MISSING_SLOT`）。⚠️ 其中 `备忘改子分类` 是**靠别名子串侥幸命中**的，不是真命中 |
| **可路由但接不住** | **5** | 词命中，但命令层做不到场景承诺的事（#821 §4① 坐实） |
| **路由不到** | **11** | 路由表里根本没有这条 HELP 主名（`POLICY_NO_MATCH`） |
| **合计** | **30** | —— |

### 路由不到的 11 条（场景 → 主名）

- 📝 **记备忘**（`memo_add_basic`，基础记录）
- 📝 **改备忘**（`memo_update_basic`，基础记录）
- 📝 **删备忘**（`memo_delete_basic`，基础记录）
- 📝 **备忘改分类**（`memo_change_category_single`，分类调整）
- 📝 **备忘改分类**（`memo_batch_change_category`，分类调整）
- 🔍 **查情绪**（`memo_search_mood`，分类查找）
- 💭 **记情绪**（`memo_add_mood`，基础）
- 💭 **删情绪**（`memo_delete_mood`，基础）
- 💭 **改情绪**（`memo_update_mood`，基础）
- 🔄 **备忘录同步**（`memo_sync_feishu`，基础）
- 🚀 **首次使用**（`memo_init_setup`，基础）

⚠️ **探针自己踩过的一个坑**（写在这里免得下一个探针再踩）：`routeWakeword()` 抛**两类**错，别混成一类 ——
`POLICY_NO_MATCH`＝路由表里真没有这条词；`POLICY_MISSING_SLOT`＝**词命中了**，只是 `needs` 槽位没给。本探针第一版把两类都当 NO_MATCH，读数虚高成 20 条。

### 可路由但接不住的 5 条

- 🔍 **按时间搜备忘**（`memo_search_by_date`）：`timeRange` 没人读：`memo.search` 只认 q／category／sub，排期过滤走 `dueMatches`　→ 待裁：⑤ timeRange 语义
- ⏰ **设提醒**（`memo_remind_existing`）：没有命令分支：`memo.create` 只走「先建新笔记再挂提醒」，全包没有 noteId 通道（旧侧是 `remind [note_id]` 位置参数）　→ 待裁：⑥ 设提醒落法
- ⏰ **查已提醒备忘**（`memo_completed_reminders`）：preset 进错分支：`{done:false}` 不满足 `mode==="done" || done===true`，落到 status active　→ 待裁：—
- 🎯 **删心愿**（`memo_delete_wish`）：**键不删**：指向 `memo.update`（只做字段 patch），删除只有 `memo.remove`（须 confirm）　→ 待裁：⑦ 删X 的 confirm 从哪来
- ✅ **删打卡**（`memo_delete_checkin`）：同上（preset `{category:"打卡"}`）　→ 待裁：⑦ 删X 的 confirm 从哪来

⚠️ **另 1 条「路由不到」的场景，命令层也带着同一个毛病」**（别因为它在上面那一列就漏掉）：

- 💭 **删情绪**（`memo_delete_mood`）：主名路由不到，**且**它的别名词今天指向 `memo.update` —— 同上（preset `{category:"情绪日记"}`）

## 二 30 行逐条

| 序 | 域 | 组 | 场景 id | 唤醒词 | prompt 首句 | 命令键（现路由真跑） | 预设 | HELP 页型 | 现状 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 📝 | 基础记录 | `memo_add_basic` | **记备忘** | 记一条备忘(唤醒词:记备忘): | **NO_MATCH** | 路由表里没有这条词 | 采集＋回执 | **路由不到** |
| 2 | 📝 | 基础记录 | `memo_update_basic` | **改备忘** | 修改一条已有的备忘(唤醒词:改备忘): | **NO_MATCH** | 路由表里没有这条词 | 采集＋回执 | **路由不到** |
| 3 | 📝 | 基础记录 | `memo_delete_basic` | **删备忘** | 删除一条或多条备忘(唤醒词:删备忘): | **NO_MATCH** | 路由表里没有这条词 | 采集＋回执 | **路由不到** |
| 4 | 📝 | 分类调整 | `memo_change_category_single` | **备忘改分类** | 修改单条备忘的顶层分类(唤醒词:备忘改分类,单条): | **NO_MATCH** | 路由表里没有这条词 | 采集＋回执 | **路由不到** |
| 5 | 📝 | 分类调整 | `memo_change_subcategory` | **备忘改子分类** | 修改单条备忘的子分类(唤醒词:备忘改子分类): | `（命中，缺槽位 id）` | 词命中了；槽位 id 待补 | 采集＋回执 | 需补槽位 |
| 6 | 📝 | 分类调整 | `memo_batch_change_category` | **备忘改分类** | 批量改分类(唤醒词:备忘改分类 · 批量场景): | **NO_MATCH** | 路由表里没有这条词 | 向导＋采集＋回执 | **路由不到** |
| 7 | 🔍 | 基础查找 | `memo_search_keyword` | **搜备忘** | 搜备忘录(唤醒词:搜备忘): | `memo.search` | 无预设参数 | 查看＋回执 | 通 |
| 8 | 🔍 | 基础查找 | `memo_search_alias` | **查备忘** | 查备忘录(唤醒词:查备忘): | `memo.search` | 无预设参数 | 查看＋回执 | 通 |
| 9 | 🔍 | 基础查找 | `memo_get_detail` | **看备忘** | 查看某条备忘的详情(唤醒词:看备忘): | `（命中，缺槽位 id）` | 词命中了；槽位 id 待补 | 查看＋回执 | 需补槽位 |
| 10 | 🔍 | 时间查找 | `memo_search_by_date` | **按时间搜备忘** | 按时间范围搜索备忘录(唤醒词:按时间搜备忘): | `（命中，缺槽位 timeRange）` | 词命中了；槽位 timeRange 待补 | 查看＋回执 | **接不住** |
| 11 | 🔍 | 分类查找 | `memo_search_wish` | **查心愿** | 查看心愿(唤醒词:查心愿): | `memo.wish` | 参数 category | 查看＋回执 | 通 |
| 12 | 🔍 | 分类查找 | `memo_search_checkin` | **查打卡** | 查看打卡记录(唤醒词:查打卡): | `memo.search` | 参数 category | 查看＋回执 | 通 |
| 13 | 🔍 | 分类查找 | `memo_search_mood` | **查情绪** | 查看情绪日记(唤醒词:查情绪): | **NO_MATCH** | 路由表里没有这条词 | 查看＋回执 | **路由不到** |
| 14 | ⏰ | 创建提醒 | `memo_remind_with_note` | **记提醒** | 记一条提醒(唤醒词:记提醒 · 两步合一:添笔记 + 设提醒): | `（命中，缺槽位 remindAt）` | 词命中了；槽位 remindAt 待补 | 采集＋回执 | 需补槽位 |
| 15 | ⏰ | 创建提醒 | `memo_remind_existing` | **设提醒** | 给已有笔记加提醒(唤醒词:设提醒): | `（命中，缺槽位 remindAt）` | 词命中了；槽位 remindAt 待补 | 采集＋回执 | **接不住** |
| 16 | ⏰ | 查看提醒 | `memo_reminders_active` | **看提醒** | 查看有效提醒(唤醒词:看提醒): | `memo.remind` | 无预设参数 | 查看＋回执 | 通 |
| 17 | ⏰ | 查看提醒 | `memo_completed_reminders` | **查已提醒备忘** | 查看已提醒过的备忘(唤醒词:查已提醒备忘): | `memo.remind` | 参数 done | 查看＋回执 | **接不住** |
| 18 | 🎯 | 心愿推进 | `memo_complete_wish` | **完成心愿** | 把心愿标记为已完成(唤醒词:完成心愿): | `memo.update` | 参数 done | 向导＋采集＋回执 | 通 |
| 19 | 🎯 | 心愿推进 | `memo_wish_schedule` | **心愿排期** | 给心愿设排期日期(唤醒词:心愿排期): | `memo.wish` | 无预设参数 | 向导＋采集＋回执 | 通 |
| 20 | 🎯 | 心愿管理 | `memo_add_wish` | **记心愿** | 快速添加心愿(唤醒词:记心愿 · 子唤醒词自动带心愿分类): | `memo.create` | 参数 category | 采集＋回执 | 通 |
| 21 | 🎯 | 心愿管理 | `memo_delete_wish` | **删心愿** | 删除心愿(唤醒词:删心愿 · 子唤醒词自动带心愿过滤): | `（命中，缺槽位 id）` | 词命中了；槽位 id 待补 | 采集＋回执 | **接不住** |
| 22 | 🎯 | 心愿管理 | `memo_update_wish` | **改心愿** | 改心愿(唤醒词:改心愿 · 子唤醒词自动带心愿过滤): | `（命中，缺槽位 id）` | 词命中了；槽位 id 待补 | 采集＋回执 | 需补槽位 |
| 23 | ✅ | 基础 | `memo_add_checkin` | **记打卡** | 快速添加打卡(唤醒词:记打卡 · 子唤醒词自动带打卡分类): | `memo.create` | 参数 category | 采集＋回执 | 通 |
| 24 | ✅ | 基础 | `memo_delete_checkin` | **删打卡** | 删除打卡(唤醒词:删打卡 · 子唤醒词自动带打卡过滤): | `（命中，缺槽位 id）` | 词命中了；槽位 id 待补 | 采集＋回执 | **接不住** |
| 25 | ✅ | 基础 | `memo_update_checkin` | **改打卡** | 改打卡(唤醒词:改打卡 · 子唤醒词自动带打卡过滤): | `（命中，缺槽位 id）` | 词命中了；槽位 id 待补 | 采集＋回执 | 需补槽位 |
| 26 | 💭 | 基础 | `memo_add_mood` | **记情绪** | 快速添加情绪日记(唤醒词:记情绪 · 子唤醒词自动带情绪日记分类): | **NO_MATCH** | 路由表里没有这条词 | 采集＋回执 | **路由不到** |
| 27 | 💭 | 基础 | `memo_delete_mood` | **删情绪** | 删除情绪日记(唤醒词:删情绪 · 子唤醒词自动带情绪日记过滤): | **NO_MATCH** | 路由表里没有这条词 | 采集＋回执 | **路由不到** |
| 28 | 💭 | 基础 | `memo_update_mood` | **改情绪** | 改情绪日记(唤醒词:改情绪 · 子唤醒词自动带情绪日记过滤): | **NO_MATCH** | 路由表里没有这条词 | 采集＋回执 | **路由不到** |
| 29 | 🔄 | 基础 | `memo_sync_feishu` | **备忘录同步** | 跑备忘录和飞书的双向对账(唤醒词:备忘录同步): | **NO_MATCH** | 路由表里没有这条词 | 查看＋回执 | **路由不到** |
| 30 | 🚀 | 基础 | `memo_init_setup` | **首次使用** | 初始化备忘录,我是第一次使用(唤醒词:首次使用): | **NO_MATCH** | 路由表里没有这条词 | 向导＋采集＋回执 | **路由不到** |

## 三 这张表与下游的关系

- **链路总表（Destination 第 2 条）**要用后四列 —— `唤醒词 → 命令 → 参数 → 产物路径`。今天第 4 列有 16 行填不出来，正是 #837／#838 要清掉的。
- **8 张域票**拿它当核对清单：本域哪几行今天是红的，做完要变绿。
- **#837 的八条**：会改到本表的第 ④（首次使用）／⑤（timeRange）／⑥（设提醒）／⑦（删X 的 confirm）条，另加「补主名行」与「撤别名行」两条机械改动。

## 四 这一页**不**回答的

- 目标态的 `命令键` 取值（取决于 #837 的八条）。
- 每个场景最终出几份产物、叫什么名（归 #822）。
- `prompt` 的最终文案（本表只取首句做指认，不是定稿）。