## 决议

**关票**。16 条全部定案（2026-09-12），定案表在票面；决策卡定稿 `docs/skills/skill-memo-ilife/t226-decisions.html`（16 张卡，每张带定案与用户原话）。

### 三条从备注／定案里长出来的硬规矩（已并进票 7／票 8 与地图 Notes）

1. **HELP 是完整体，不是现状快照**（U1／U2／U3 备注）：「我们开发 help html 就是那个最终功能全部实现的完整体！」⇒ 页面上**不许**出现「当前无唤醒词可路由」这类缺失标记；缺口靠后续补齐图去补。
2. **命令不出现在页面上**（U6 备注）：「HELP HTML 的 prompt 中不应该出现具体的命令硬编码，只有唤醒词，唤醒词在 SKILL.md 中内部流程才涉及到命令」。
3. **HELP 自身的唤醒词不上页面**（V1 定案）：「HELP HTML 中不包括 HELP 的唤醒词场景。用户说 备忘录HELP 就能拿到」⇒ 不渲染「怎么喊我」那一块，也不把 HELP 自己的唤醒词当成一条场景卡。

### 一处更正（本票把它从「问题」改回「说明」）

**U7 不是决策，是地图数错了**：老 `SKILL.md:1086-1095` 的灵活匹配表有 8 行示例，最后一行 `备忘录 manual` **和** `备忘录 guide` 同格——按词数是 **9 条**，地图与票面早先记成「1 条字面 ＋ 7 条变体」（＝8），漏算 `guide`。按 **9 条**更正即可（地图 Notes 已改）。沾边的只有「新技能 `SKILL.md` 将来把 9 种说法写全」，按 U2 口径＝写全，属后续补齐图的活。

### 两条非推荐项，按用户裁落

- **V6 = B**：`contact` 照老两项，但**补 `url` 字段使其可点**（归票 8）。
- **V7 = A**：`editable_fields` **进**，按清洗后的 76 条落（归票 7／票 8）。

### 本票没有关掉的雾

无。票 2／票 3／票 14 交来的待裁项 16 条全部落定；引用出处的细节仍在三份调查报告里（`t222-content-reconcile.md`／`t223-template-contract.md`／`t234-cli-inventory.md`）。
