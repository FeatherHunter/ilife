# t878 · 屏上有、文本判据读不到的分隔符：实施证据

> *本件由 AI 依 [#878](https://github.com/FeatherHunter/ilife/issues/878) 落盘；命令、运行号与读数照实抄录，没跑的一律不写。*

## 一、落地了什么

`生成时刻 · 场景` 这种**拿 `·` 顶版式**的串，在三处换成**公共层的键值行**（`ilife-block-fact-strip`：一格「标签 ＋ 值」，格与格靠版式分开）——**事实一条不减，`·` 一枚不留**。

## 二、变更落点（4 件）

| 件 | 改了什么 |
|---|---|
| `packages/skill-memo-ilife/src/render/memoPageAssets.ts` | 新增页面侧产出者 `window.factStrip(facts)`：**只拼公共层类名，不产样式**（类名、字号与间距全吃 `pageShapeCss()` 的形状①）。为什么不三份模板各拼一遍：那是「同一件事各写一遍」的病，`t849-视觉基准.md` §3 点名的那一族。件头「页面级全局」表与记账段同步补一行。文件 229 → 250 行（告警线 350，未越线）。 |
| `packages/skill-memo-ilife/templates/sync_report.html` | 删掉 `.status-meta span+span::before{content:"·"}` 这条伪元素规则；状态行改由 `window.factStrip` 出键值行（事实仍是「生成时间」「场景」两条）。 |
| `packages/skill-memo-ilife/templates/wish_complete.html` | 页头 `#meta` 由「`<p class="meta">` ＋ `textContent` 写一串」改「`<div id="meta">` ＋ 键值行」；随之作废的 `.meta{…}` 规则删掉。 |
| `packages/skill-memo-ilife/templates/wish_plan.html` | 同上（`场景` 的兜底值仍是 `wish-batch-plan`）。 |

**形状为什么是键值行、不是徽章列**：`t849-视觉基准.md` §3 的形状对照表里，「`·` 串**单实体的多字段**」这一行明写出口＝**键值行**，举的实例正是「**心愿快照行**」（本票这两页就是心愿页）；「生成时刻」与「场景名」是同一份报告／同一次向导的两个字段，不是并列的多个实体或状态标签。出口只许公共层既有形状 ⇒ 形状＝事实条（本仓词汇里 `factStrip` 即键值行）。
**没做的事**：没把 `·` 换个标点（那是懒政，见 #880 票面同一条裁定）；没动 #883 的判据件（判据面归它）。

## 三、读数（持锁执行，逐条可复核）

运行记录：`.scratch/locks/gate-runs.log`（未受版本控制）；下表每行的运行号可在其中一对一找到。

| 读数 | 命令 | 运行号 | 结果 | 退出码 |
|---|---|---|---|---|
| 编译与指纹绑定 | `node docs/agents/t540-指纹绑定.mjs --scope packages/skill-memo-ilife` | `5f22e7bf-0b18-4856-8fc2-376512d440c0` | `RESULT: src-n=100 dist-n=404 src-sha=0ed86822b2508ca2 dist-sha=733cbbb7ded6d6c3 一致（dist 与 src 指纹一致）` | 0 |
| 整批真跑（铺墙第一步） | `node docs/skills/skill-memo-ilife/t834-gen-batch.mjs` | `faf404d7-6536-4518-a70b-ee66cc83357a` | `RESULT: 34/34 格真跑成功；源目录 34 件（册子 34）` | 0 |
| 发批（造册＋双端墙＋总索引＋链路总表＋机审名单） | `node docs/skills/skill-memo-ilife/t834-发批.mjs --src packages/skill-memo-ilife/.scratch/t834/源 --batch packages/skill-memo-ilife/.scratch/t834/验收` | `3a99570d-e4e5-4022-a40b-6479f2e937ba` | `RESULT: 造册＋双端墙＋总索引=0；链路总表=0；机审名单=0` | 0 |
| **屏上自查**（本票的主判据） | `node .scratch/t878/render-check.mjs` | `19b430cb-0926-4308-860d-196dcc336d53` | `RESULT: 3/3 PASS：三处目标位的渲染后可见文本里都没有「·」，且键值行都渲染出来了` | 0 |
| 分隔符门（回归） | `node packages/base-render/test/separator-probe.mjs packages/skill-memo-ilife/.scratch/t834/验收/备忘录同步.html` | `705bf0fd-92ef-41c5-975d-eac90b45f07d` | 行级 0 行／节点级 0 处 | 0 |
| 六列机审（回归） | `node docs/skills/skill-memo-ilife/t869-机审.mjs --dir packages/skill-memo-ilife/.scratch/t834/验收 --quiet` | `0b6289bc-bf00-4de6-898b-5c7435786f0f` | `RESULT: 34/34 PASS ①0 ②0 ③0 ④0 ⑤0 ⑥0 缺件0 名单=批目录` | 0 |

GATE-RUN runId=5f22e7bf-0b18-4856-8fc2-376512d440c0 cmd="node docs/agents/t540-指纹绑定.mjs --scope packages/skill-memo-ilife"
GATE-RUN runId=faf404d7-6536-4518-a70b-ee66cc83357a cmd="node docs/skills/skill-memo-ilife/t834-gen-batch.mjs"
GATE-RUN runId=3a99570d-e4e5-4022-a40b-6479f2e937ba cmd="node docs/skills/skill-memo-ilife/t834-发批.mjs --src packages/skill-memo-ilife/.scratch/t834/源 --batch packages/skill-memo-ilife/.scratch/t834/验收"
GATE-RUN runId=19b430cb-0926-4308-860d-196dcc336d53 cmd="node .scratch/t878/render-check.mjs"
GATE-RUN runId=705bf0fd-92ef-41c5-975d-eac90b45f07d cmd="node packages/base-render/test/separator-probe.mjs packages/skill-memo-ilife/.scratch/t834/验收/备忘录同步.html"
GATE-RUN runId=0b6289bc-bf00-4de6-898b-5c7435786f0f cmd="node docs/skills/skill-memo-ilife/t869-机审.mjs --dir packages/skill-memo-ilife/.scratch/t834/验收 --quiet"

**屏上自查的三行原文**（对**终版墙**，2026-09-22 02:30 铺的那一批）：

| 页 | 目标位 | 渲染后可见文本 | `·` | 键值行格子 |
|---|---|---|---|---|
| `备忘录同步.html` | 状态行（`.status-meta`） | `生成时间 02:30 场景 同步飞书` | 0 | 有 |
| `完成心愿-向导.html` | 时刻行（`#meta`） | `生成时间 2026-09-22 02:30:18 场景 心愿完成` | 0 | 有 |
| `心愿排期-向导.html` | 时刻行（`#meta`） | `生成时间 2026-09-22 02:30:18 场景 心愿排期` | 0 | 有 |

三页**整页**渲染后可见文本里的 `·` 计数也都是 0（旁证读数，不判红绿）。

## 四、变异自证（改坏必红、还原必绿）

| 态 | 改了什么 | 命令与运行号 | 读数 | 退出码 |
|---|---|---|---|---|
| **变异红** | 把 `wish_complete.html` 的时刻行改回改前写法（`` textContent = `生成时间：${…} · 场景：${…}` ``），重铺墙 | 重铺 `4027e446-811c-473d-a0e8-96e67cb176a0` ＋ `d21ae961-c7a0-4389-89b9-4e90467cc143`；自查 `55881907-dcfe-455d-80eb-1f891b3c2d96` | `RESULT: 2/3 PASS：完成心愿-向导.html（目标位里还有 1 枚「·」）` | 1 |
| **还原绿** | 还原该件，重铺墙（＝终版墙） | 重铺 `faf404d7-…` ＋ `3a99570d-…`；自查 `19b430cb-…` | `RESULT: 3/3 PASS…` | 0 |

「变异红」那条运行按协议 §2.4.3 是**故意变异**，不充作门禁证据，故不进上面的 GATE-RUN 声明行；运行号仍可在一处核对。

## 五、屏上证据（人看，落在未受版本控制的批目录里）

批目录：`packages/skill-memo-ilife/.scratch/t834/验收/`（34 件 ＋ 双端墙 ＋ 总索引 ＋ 链路总表）。
另存了一份供人直看的截图：`.scratch/t878/屏/`（`备忘录同步-390.png`／`备忘录同步-1280.png`／`完成心愿-向导-390.png`／`心愿排期-向导-1280.png`），改前那一批墙整份留在 `.scratch/t878/墙-改前/`，可对开比。

**人看要判的一件事**：状态卡是居中版式（`.status-card{text-align:center}`），而键值行按公共层形状**左对齐**落在卡底那条细线之下——这是形状本来的样子（本票不额外加对齐样式），读起来是「卡片页脚」。若维护者要它随卡片居中，那是一行 `justify-content:center` 的事，另说。

## 六、包内测试套件：两条读数与归属（**有一条存量红，不属本票**）

| 读数 | 命令 | 运行号 | 结果 | 退出码 |
|---|---|---|---|---|
| 全量（包内规范命令） | `pnpm --filter skill-memo-ilife test` | `e09b6df4-3580-426c-be37-3473bd594060` | 267 通过／46 条 ✖，**集中在 `cli.test.mjs` 与 `cmd-850-four-questions.test.mjs` 的 spawn 用例** | 1 |
| 单跑其中一个件 | `node --test packages/skill-memo-ilife/test/cmd-850-four-questions.test.mjs` | `c280c661-0458-4fd7-a3ff-92823a48aaf7` | **17 全通过** | 0 |

**归属一（并行度，不是代码）**：那 46 条 ✖ 的断言原文是 `assert.equal(r.status, 0)` 实得 `null`——`spawnSync` 连子进程都没起来（不是 CLI 报错）。把同一个件单独跑就全绿 ⇒ 是**全量并行 spawn 时的资源/环境现象**，与本票改动无关（本票没碰 CLI 与 spawn 路径）。与 dist 新旧也无关（先按陈旧 dist 跑过一轮、重编后又跑一轮，两次同样这些 ✖）。

**归属二（存量红，一条，确定可复现）**：`test/cli-help-229.test.mjs` 的「#240 · 自持件已删」结构烟雾锁红——`packages/skill-memo-ilife/src/cli/health/probe.ts:51` 出现禁词 `flag: 'wx'`。`git blame` 指到 `4b034d72`（#855「超线件拆分第一件」，2026-09-21 13:23），**不是本票、也不是本次会话的改动**；该件不在本票写集（本票没碰 `src/cli/**`），故只记账不修。

## 七、没做的（如实记账）

1. **同源发现不夹带**（票面「遗留出口」）：`templates/init_report.html:144-145`（该模板没有渲染调用点，疑死件）／`templates/sync_report.html` 里 warn 档状态图标那枚 `·`（装饰性字形，不是并列分隔符）／`templates/memo_query.html:94`（`已展开全部 · 滚到底可看完`，超 50 条并点过「显示全部」才上屏）——三处都**没动**，处置待定（见票面「待确认 Q2」）。
2. **判据件没碰**：让判据看得见「渲染后 ＋ CSS 生成内容」是 [#883](https://github.com/FeatherHunter/ilife/issues/883) 的活（本票的下游票）。`.scratch/t878/render-check.mjs` 只是**未入仓的自查件**，复用 `t834-渲染面探针.mjs` 的 `renderedDom`／`stripHidden` 同一条管线，不构成第二份判据。
3. **本票的改动分落在两笔提交里**：`sync_report.html`／`wish_complete.html`／`memoPageAssets.ts` 三件在施工途中被另一席的整仓提交（`c3c97c6e`／`e1b31966`，提交信息写明「依人直接指令，当前工作区全部变更一次性入仓」）一并收走；本席自己那笔只收剩下的 `wish_plan.html` 与本证据件。**内容无损**（终版墙就是从这份工作树铺出来的），只是提交范围与「一票一作者」的口径不符，记在这里备查。
4. **包内测试套件那条存量红没修**（见 §六「归属二」）：`src/cli/health/probe.ts:51` 的 `flag: 'wx'` 撞 `#240` 的结构烟雾锁，归 #855 那次拆分，票与件都不在本票范围。
