Part of #220

## Question

还 `#224` 判「甲」时挂下的架构欠债：备忘录把「命名通式 ＋ 独占落盘 ＋ 回执」这一小块**自持**在包内（第 4 份同逻辑实现，前 3 份＝饼干记账 `packages/skill-bill/src/output.ts`、卡路里 `packages/skill-calorie/src/output.ts`，以及老作息世代）。共用件 `saveHtmlFile` 一旦就绪，本票把备忘录的落盘改成**消费共用件**，删掉自持件。

## 为什么另开这张票（裁决出处）

- 裁决正本：`docs/skills/skill-memo-ilife/t220-orchestrator-decisions.md` 第二节**裁决 3**。
- 复审员 A 报告：`docs/skills/skill-memo-ilife/t224-review-A.md`。
- **`#224` 为什么不直接判乙**：①`#237` 拟把 `_N` 统一成 `_1`，而三家现役实现与老实物**全部 `_2` 起**（`packages/skill-bill/src/output.ts:26-38` 逐字「无 `_N` 则 `_2`」）⇒ 那是**改掉三家现役行为**、且撞 `#233`「与老实物并排」的验收；②`#208:145` 逐字把备忘划出该图（「其余 5 个技能（…／**备忘**）：本图只做私家大厨」），`#208:30` 锁定为「三家」⇒ 用户「乙」原话的范围不含备忘录；③`#237` 的下一步是「新建目录层级先点头」（`docs/agents/structure.md:88`）的人工审批闸门，其实测进度 0%、无 assignee；④`#237` 的 `blocked_by` 关系里它**阻塞 `#215`**（大厨票 7）⇒ 备忘录排它等于同时排大厨的队。
- 但 `docs/agents/structure.md:23` 铁律一确实命中「甲」（把对方那份抄一遍放进自己目录，不算走了接口）⇒ **欠债必须留可查的偿还记录**，否则第 4 份代码永远没人接。

## 本票要做的

1. 等 `#237` 的共用件 `saveHtmlFile` 落地（含它的对外函数名、参数形状 `{ dir, stem, html, onExists? }`、`onExists` 四态）。
2. 把备忘录的落盘从自持件改为 `import ... from <共用件>`；**删掉自持件**（两个不许同时在仓）。
3. 保持产物名与落盘路径**逐字不变**：`<SKILLS_DB_PATH>/memo_html/备忘录_HELP_<YYYYMMDD_HHMMSS>[_N].html`、速查支 `备忘录_速查表_<...>.html`；`_N` 递补口径若 `#237` 要改，须先在该票裁明并同步 `#233` 的验收口径。
4. 交付时报必报五步第五步「交付对账」；跑 `pnpm boundaries`（备忘录 import 的是共用件而非 `base-paint`，边界口径须复核）与 `#230` 落下的 CLI 级用例（五例必须仍全绿，尤其「并发 6 次独占递补」与「退出码矩阵」）。

## 完成判据

备忘录落盘走共用件、自持件已删；`#230` 那套 CLI 级用例全绿；产物名与落盘路径与迁移前逐字相同。

## 阻塞

本票 `blocked_by = #237`（共用件票）。`#237` 未落地前本票**不开工**；`#229` 不因本票而阻塞。

## 进度：0%

下一步：等 `#237`（共用件 `saveHtmlFile`）关票。
