## Question

把目的地两条口径在**真机**上验完，并请维护者肉眼终审（照 `#151`／`#194`）。

## ⚠️ 验收判据的措辞已订正（复审员 H 指出，2026-09-12）

原措辞「与老骨架**条数、名称、顺序逐字**对得上」**对 `title` 已不成立**：按裁决 21 D5／裁决 23 授权，`title` 有 **1 处**换说法（`给心愿设排期日期(同步飞书 due)` → `(同步到飞书)`），`hint` 有 **3 处**，`memo_init_setup` 的 prompt 有 **3 片段**。**照原措辞终审会当场判红**。订正为：

> **条数／顺序逐字**（30 场景／8 域／13 二级组／64 字段／12 别名，一条不差）＋ **名称／可见文案在「声明五」那 7 处换说法之后逐字**（其余 23 个 `title`／27 个 `prompt` 逐字未动）。

## 交付物与证据（真机，2026-09-12 编排会话已亲自复跑）

```
$env:SKILLS_DB_PATH = "D:\2Study\StudyNotes\.db"
node packages/skill-memo-ilife/dist/cli/cmd_read.js memo.help.lookup
EXIT CODE = 0
"delivery":{"mode":"file","path":"D:\\2Study\\StudyNotes\\.db\\memo_html\\备忘录_HELP_20260912_134016.html","bytes":130885}
```

- **① 明确拿到文件**：回执顶层给绝对路径，文件真在盘（`130 885` B ＝ 回执 `bytes`），扁平落盘（`memo_html\` 子目录 0 个），**跑完 `<db>\memo` 仍不存在**（不建库）；
- **② 由现成通用 help 模板渲染**：与**已被维护者终审过**的 `calorie_html\卡路里_HELP_20260912_001033.html` **指纹逐项同形**（`help-data`×2／`<title>`×1／`@media`×6／老家世代 `__DATA__`×0），`<title>备忘录 · 使用手册</title>`；
- **三条硬规矩**在产物上复核为 0 处；`待开发` 2 处经逐处看上下文确认是**共享模板自带运行时 JS**（卡路里那份同样 2 处），本资产 `status` 全空串 ⇒ 徽章不画。

## 肉眼终审

维护者看过产物后的一句话是关票凭据（照 `#143` 的「已经通过我的审查了，我觉得非常不错。」）。产物由编排会话发送给维护者。

## 从票 12（`#232`）接走的 T1–T5

T1 重启后核技能表真可见（唯一能证伪「静默 pending」的动作）｜T2 核日志无 `failed to apply loader entry dsh-memo-ilife`｜T3 定 `~/.agents/skills` 要不要建（**已裁「先不建」**）｜T4 核 `inject` 三连是否全需（含历史遗留的 `webServer`）｜T5 `test/skills-provider.test.mjs:106` 那条断言在说明面落地后翻正向（归 `#231`）。
**另**：`#232` 报告 §9 第 4 条（`test/skills-export-47.test.mjs` 的 PKGS 要不要收备忘录）在本图**未决**，随本票一并裁。

## 进度：0%

下一步：等票 10（`#230`）＋票 11（`#231`）关票；产物已交维护者肉眼终审。

