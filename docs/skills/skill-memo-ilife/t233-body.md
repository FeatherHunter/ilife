## Question

把目的地两条口径在**真机**上验完，并请维护者肉眼终审（照 `#151`／`#194`）。

- **生产目录真跑**：`SKILLS_DB_PATH=D:\2Study\StudyNotes\.db` 下真跑 `memo-cmd-read memo.help.lookup`，产物落老目录 `memo_html/`，与老实物并排；回执给绝对路径 ＋ 字节数。
- **两条口径各自留证**：① 明确拿到文件（真跑输出 ＋ 文件落地截图／哈希）② 由通用 help 模板渲染（与卡路里／记账／居家的模板指纹一致，例：`<title>` 派生规则、`help-data` 脚本、双断点响应式）。
- **肉眼终审**：维护者看过产物后的一句话是关票凭据（照 `#143` 的「已经通过我的审查了，我觉得非常不错。」）。
- 证据落 `docs/skills/skill-memo-ilife/`（按本票号前缀）；若维护者要归档夹，另存一份到老技能目录的落地文件夹（照记账先例）。

完成判据：真机交付成立 ＋ 维护者终审「过」。本票关票即地图目的地达成、地图关门。

## 进度：0%

下一步：等票 10（锁）＋票 11（说明面）＋票 12（装机）关票。
