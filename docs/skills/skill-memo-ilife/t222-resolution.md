## 决议

**关票**。产物四件（全在 `docs/skills/skill-memo-ilife/`）：报告 `t222-content-reconcile.md`（93976 B）、机器可读骨架 `t222-skeleton.json`（57989 B，`JSON.parse` 通过、与报告附录 B 逐字相同）、只读抽取脚本 `t222-extract.mjs`（七模式）、原始输出 `t222-evidence/`（6 份）。只读调查：未改源码、未写 git、未动 issue。

### 最要紧的一条：新表把老核心动词整体换掉了（量化）

`记备忘`／`改备忘`／`删备忘`／`备忘改分类`／`备忘录同步`／`记情绪`／`删情绪`／`改情绪`／`首次使用` 用新表口径复算**全部 `POLICY_NO_MATCH`**（新表只有 `添加笔记` `:29`／`记一条` `:28`）；`memo.sync` 有命令、有形状、有实现，却**没有任何唤醒词指向它**。

老 29 个唯一唤醒词：**逐字命中 18**／仅子串可路由 1（`备忘改子分类`→`改子分类`）／**完全不可路由 10**。新仓 `test/` 对这 10 个老词的覆盖是 **1/10**。

⇒ 这就是地图「用户原话采访区」里 Q2 那句话的量化证据。**本图不修这些缺口**（Out of scope 已写明：补齐另立图）；本图要的是把 HELP 做成**官方源**——这份对账表就是那张图的第一份参照。

### 三组数（脚本数出，不抄票面）

1. 老 30 场景：**无逐字落点 12**／逐字命中 18；`deprecated` 候选 1（`memo_init_setup` 首次使用）。12 条再拆：可子串路由 1／改名对齐 4（情绪族）／需补唤 6。
2. 新表 28 条里老骨架没有的 **10 条**：真新增 6（批量改分类／改子分类／查提醒／废弃提醒／记一条／添加笔记）＋ 改名孪生 4（记查改删情绪日记）。两边并集 39（both 18）自洽。
3. 骨架计数 **8 域／13 二级组／30 场景／29 唯一唤醒词／76 `editable_fields`／4 处「基础」兜底**（兜底＝sync／checkin／mood／init 四个单子功能域）；逐域场景数：memo 6／search 7／remind 4／wish 5／checkin 3／mood 3／sync 1／init 1。

### 第二事实源：算

老 `SKILL.md` 除 yaml 外还有三处权威表：`:1076-1174` HELP 分支、`:147-172` 口语→CLI **反向表**（居家口径里的「老路由表」）、`:226-292` HTML 生成对照表；`:300` 载有 yaml 里没有的别名（`完成打卡`／`初始化`／`新手`）。⇒ **「照搬老骨架」的原料＝yaml ＋ 老 `SKILL.md` 三张表**，票 7 照此落资产。

### 一处口径更正（地图 Notes 已改）

老 HELP 触发短语实为 **9 条**（1 字面 ＋ 8 变体，其中 `manual`／`guide` 同行），地图与票面原记「1＋7」——**以报告为准**。

### 八条要用户拍板的（U1–U8，已并进票 6）

- **U1** `记备忘`／`改备忘`／`删备忘` 无落点 → 补唤还是改名？
- **U2** `memo.sync` 有实现无唤醒词 → 用老词 `备忘录同步` 还是新造？
- **U3** 老 `备忘改分类` 单条＋批量共用一个词，新表拆出 `批量改分类`，**单条改分类在新表无词可用**。
- **U4** 情绪族 5 词改名（`…情绪`→`…情绪日记`）→ 老词收进 `aliases` 还是作废？
- **U5** `memo.stats` 有形状而**老骨架 0 条内容** → HELP 展不展？（等 `#234` 复核）
- **U6** 新词 `废弃提醒` 无场景；老「删 X」4 条语义归 `memo.remove` 还是 `memo.update`（新表 `删心愿` 的命令就是 `memo.update`）。
- **U7** 老 HELP 触发短语 9 条的口径确认。
- **U8** 4 处「基础」兜底在通用模板下要不要真建二级组。

### 一条没关掉的雾，已立新票

老 `script/memo_cli.py`（78635 B）**未逐行读**——CLI 子命令全集只取自老 `SKILL.md` 两张表 ⇒ **可能漏列子命令，U5 的「stats 无老内容」有反转风险**。已立 [复核：老 memo_cli.py 的 CLI 子命令全集](https://github.com/FeatherHunter/ilife/issues/234)（research），并作为票 6 的第二道阻塞。

### 计数自证

三组数全部由 `t222-extract.mjs` 数出，对应命令标在报告 A.2／A.3，可复跑；骨架 JSON 与报告附录 B 逐字相同（`JSON.parse` 验过）。
