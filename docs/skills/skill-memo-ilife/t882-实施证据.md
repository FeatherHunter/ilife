# #882 实施证据：「对象」那一格的取值形状（备忘改分类-批量）

改动四件：`src/render/receipt.ts`（族定义地，按类型分两读）／`src/memo/receipt.ts`（批量支填 `N 条`；回执整份由调用方给）／`src/memo/run.ts`（结论句只在写侧拼一次，半角等号改中文条数）／`test/receipt-882.test.mjs`（11 项）。

## 〇 声明与对账（协议 §2.3 第 4 条／§2.4.2）

本件声称跑过的运行，逐条抄自锁日志（`runId` 与 cmd 与日志条目一对一）：

```text
GATE-RUN runId=03ecf2e7-626a-441f-adc1-989267592618 cmd="pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/gate.ps1"
GATE-RUN runId=43f06aa0-09f5-46d5-99c1-d77aae2a5efb cmd="pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/gate2.ps1"
GATE-RUN runId=bd3f8cef-4d01-462c-9eae-64e6808d9883 cmd="pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/commit.ps1"
GATE-RUN runId=11fc5b0b-93a0-4a62-b7ff-4e596c90b4d1 cmd="pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/commit2.ps1"
GATE-RUN runId=617606f0-8207-4be8-8e4d-256bc21400fe cmd="pwsh -NoProfile -File docs/skills/skill-memo-ilife/t882-门窗口.ps1 -Out packages/skill-memo-ilife/.scratch/t882/墙-终 -Baseline packages/skill-memo-ilife/.scratch/t882/墙-改后"
```

对账源：同目录 `t882-gate-runs.log`（从 gitignored 的 `.scratch/locks/gate-runs.log` 按票号导出、受版本控制）。
复核：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-memo-ilife/t882-实施证据.md --log docs/skills/skill-memo-ilife/t882-gate-runs.log --ticket 882`。

## 一 三个窗口的读数（都在持锁状态下跑）

| 窗口 | runId | 内容 | 读数 |
|---|---|---|---|
| 一 | `03ecf2e7…` | 指纹＋编译／新用例／全包／行数门／重铺墙（`.scratch/t882/墙-改后`）＋与改前墙对账／机审／变异 | `src-sha=0ed86822b2508ca2`／`dist-sha=733cbbb7ded6d6c3` 一致；新用例 7／7；全包 242／243（唯一红＝基线那条）；行数门 106/106；墙 34/34 且「对象」行 **不变=18 改=1**；机审 34/34 六列 0；变异 3 绿／4 红 → 字节还原 → 7／7 绿 |
| 二 | `43f06aa0…` | 同窗口一，跳过墙／机审（该批只动测试件） | 指纹与窗口一**逐字相同** ⇒ 产物未变、墙上读数继续成立；新用例 9／9；行数门 106/106；变异 3 绿／6 红 → 还原 9／9 绿 |
| 四 | `617606f0…` | 入仓脚本 `t882-门窗口.ps1` 全阶段（评审返修后） | `src-sha=82c59a2a27895862`／`dist-sha=ac4d4e59da0777ab` 一致；新用例 **11／11**；全包 **249／250**（唯一红＝基线那条）；行数门 106/106；墙 34/34 且与 `墙-改后` 对账 **对象行 不变=34 改=0 ／ 载荷 33 不变、1 页差异**；机审 34/34 六列 0；变异 5 绿／6 红 → 字节还原（identical=True）→ 11／11 绿；GATE-RESULT: PASS |

窗口三（`ca22257b…`，不入上面的声明）是返修前的同一套阶段：它当场抓到**用例自身**的抽取 bug（调用面白名单那一版把整行字面量都算了进来）——按 §2.4.3「跑了却失败不得充作门禁证据」，只记在这里，不计入对账。

那 1 页载荷差异是**夹具非确定性**，与本票无关：向导页「笔记清单」里 `#1 买牛奶两盒（改后）` 与 `#2 周报要写` 换了位（两条 `updated_at` 同秒，`collectBatchItems` 的 `ORDER BY updated_at DESC` 没有 tiebreaker；同页其余 1602 字节逐字相同）。已记入票面「已知范围外」。

## 二 屏上门（改后墙真渲染整页截图）

| 档 | 「处理结果」第一行 | lead |
|---|---|---|
| 390 | `对象` ／ `批量改分类 1 条` | `改分类完成：更新 1 条，跳过 0 条` |
| 1280 | `对象` ／ `批量改分类 1 条` | `改分类完成：更新 1 条，跳过 0 条` |

两档整页文本里都不再出现 `#`。截图：`.scratch/t882/屏-390-改后.png`、`屏-1280-改后.png`（`.scratch/` 不入仓）。

## 三 复跑办法（工作目录＝仓根；脚本已入仓）

```powershell
node tooling/run-locked.mjs --ticket 882 -- pwsh -NoProfile -File docs/skills/skill-memo-ilife/t882-门窗口.ps1 -Out packages/skill-memo-ilife/.scratch/t882/墙-终 -Baseline packages/skill-memo-ilife/.scratch/t882/墙-改后
```

对账件单独用：`node docs/skills/skill-memo-ilife/t882-对账.mjs <基线墙目录> <候选墙目录> [--payload] [--max-object-changes N] [--max-payload-changes N]`。

**注意（本票自己踩的坑）**：`test/receipt-882.test.mjs` 一律 import `dist`，单跑它**验不到「改了 src 忘了重编译」**。要证这件事，须把本包 `test/cmd-registry-855.test.mjs` 一起跑（它的 ⓪ 就是「dist 不许比 src 旧」的新鲜度守门），或直接用 `t540-指纹绑定.mjs`（本门窗口的 STAGE-1／末段各一次）。Windows 上脚本请用 `pwsh`（7.x）；`powershell` 5.1 会按 GBK 读中文而解析失败。

## 四 独立评审（`/code-review` 两轴，各一个 subagent 分开跑）与逐条处置

**Standards 轴**（subagent `3004e06b`）：硬 3 条 ＋ 坏味道 3 条。

| 发现 | 处置 |
|---|---|
| 【硬】缺 `.changeset/882-*.md`（`docs/agents/changeset-政策.md:31`「每票一件」） | 已补 `.changeset/882-批量回执对象格.md` |
| 【硬】缺对账导出件（§2.3 第 4 条）＋复跑脚本未入仓（§5.1） | 已补：`t882-gate-runs.log`（导出）＋ `t882-门窗口.ps1`、`t882-对账.mjs`（入仓） |
| 【文档同步】证据件读数与现状不符（7／7 vs 9 条；243／242 vs 248／247） | 本件改成三窗口读数（本节一） |
| 坏味道 · Primitive Obsession（`typeof` 顶替领域概念；同格另流通 `'—'`／`'未新建'`） | **不修**（判断题）：改它要动 9 个调用点与族契约，而族口径正等 §四 那张票一次定；清单已写进用例注释 |
| 坏味道 · Speculative Generality（`entityId === ''` 分支没人传，且与「不静默吞掉」自相矛盾） | **已修**：删掉那一支 |
| 坏味道 · Mysterious Name（`entityRefOf` 名不对、只用 `r.entityId`；注释反例已过期） | **已修**：改名 `objectRowTailOf(entityId)` 并只收值；注释改成改后形状（`#1 条`） |

**Spec 轴**（subagent `10a4bc80`）：3 条。

| 发现 | 处置 |
|---|---|
| §六.4 的「18 格仍带 `#`」仓内只锁了 2 格，其余靠一次性墙对账、不成门 | 已补两条用例：**19 格逐格**（族里那条规则在 19 格的定义形态上逐格成立）＋**调用面白名单**（扫全包 `entityId` 取值位的字符串字面量，只许那三处，新冒一处即红）；对账件与门脚本入仓 |
| §六.1 写「`#` 只跟记录号」而落成是「按类型」，`心愿批次 #N` 这处计数站点票面没列 | 订正：落成口径**就是按类型**（数字＝记录号）；**数字＝条数的两处**明知故留在范围外，见下节（票面也补全了名单） |
| 两处兜底的字面被改且被用例锁死 ⇒ 等于替未开的票定措辞 | 已改：那两条断言只锁**形状**（标签前缀 ＋ 无 `#`），`未新建`／`—` 的措辞不锁 |

## 五 已知范围外（不夹带，另开票）

1. **计数冒充记录号，现有两处**：`src/wish/run.ts`（`entityId: items.length` → `对象：心愿清单 #4`）、`src/memo/run.ts`（`entityId: r.receipt.updated` → `对象：心愿批次 #N`）——票面 §四 原只列了前者。
2. 两处兜底措辞（`未新建`／`—`）与回执族 h1 不按唤醒词。
3. **本票门窗口翻出来的新发现**：`collectBatchItems()` 的 `ORDER BY updated_at DESC` 没有 tiebreaker，`updated_at` 同秒的两条两次跑批顺序会翻（见 §一）；既是夹具不稳定，也可能是产品上真会看到的顺序抖动。

## 六 调试期运行不进本件

返修期的靶向跑（新用例单独跑、窗口三那次缺陷窗口）**不声明、也不当门禁证据**；本件的门禁读数全部来自 §〇 声明的五次运行。
