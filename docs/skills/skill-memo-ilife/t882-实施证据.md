# #882 实施证据：「对象」那一格的取值形状（备忘改分类-批量）

改动六件源码／用例：`src/render/receipt.ts`（族定义地，按类型分两读）／`src/memo/receipt.ts`（批量支填 `N 条`；回执整份由调用方给）／`src/memo/run.ts`（结论句只在写侧拼一次；心愿批次那格改 `N 条`）／`src/memo/batch.ts`（收集排序补 `id DESC` 作 tiebreaker）／`src/wish/run.ts`（心愿清单那格改 `N 个`）／`test/receipt-882.test.mjs`（14 项）。

## 〇 声明与对账（协议 §2.3 第 4 条／§2.4.2）

本件声称跑过的运行，逐条抄自锁日志（`runId` 与 cmd 与日志条目一对一）：

```text
GATE-RUN runId=03ecf2e7-626a-441f-adc1-989267592618 cmd="pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/gate.ps1"
GATE-RUN runId=43f06aa0-09f5-46d5-99c1-d77aae2a5efb cmd="pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/gate2.ps1"
GATE-RUN runId=bd3f8cef-4d01-462c-9eae-64e6808d9883 cmd="pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/commit.ps1"
GATE-RUN runId=11fc5b0b-93a0-4a62-b7ff-4e596c90b4d1 cmd="pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/commit2.ps1"
GATE-RUN runId=617606f0-8207-4be8-8e4d-256bc21400fe cmd="pwsh -NoProfile -File docs/skills/skill-memo-ilife/t882-门窗口.ps1 -Out packages/skill-memo-ilife/.scratch/t882/墙-终 -Baseline packages/skill-memo-ilife/.scratch/t882/墙-改后"
GATE-RUN runId=7891bdfd-a44c-4f98-a1ac-ca65eef729f9 cmd="pwsh -NoProfile -File docs/skills/skill-memo-ilife/t882-门窗口.ps1 -Out packages/skill-memo-ilife/.scratch/t882/墙-终2 -Baseline packages/skill-memo-ilife/.scratch/t882/墙-终"
```

对账源：同目录 `t882-gate-runs.log`（从 gitignored 的 `.scratch/locks/gate-runs.log` 按票号导出、受版本控制）。
复核：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-memo-ilife/t882-实施证据.md --log docs/skills/skill-memo-ilife/t882-gate-runs.log --ticket 882`。

## 一 四个窗口的读数（都在持锁状态下跑）

| 窗口 | runId | 内容 | 读数 |
|---|---|---|---|
| 一 | `03ecf2e7…` | 指纹＋编译／新用例／全包／行数门／重铺墙（`墙-改后`）＋与改前墙对账／机审／变异 | `src-sha=0ed86822b2508ca2`；新用例 7／7；全包 242／243（唯一红＝基线那条）；行数门 106/106；「对象」行 **不变=18 改=1**；机审 34/34 六列 0；变异 3 绿／4 红 → 字节还原 → 7／7 绿 |
| 二 | `43f06aa0…` | 同窗口一，跳过墙／机审（该批只动测试件） | 指纹与窗口一**逐字相同** ⇒ 产物未变、墙上读数继续成立；用例 9／9；行数门 106/106；变异 3 绿／6 红 → 还原 9／9 绿 |
| 四 | `617606f0…` | 入仓脚本 `t882-门窗口.ps1` 全阶段（评审返修后） | `src-sha=82c59a2a27895862`；新用例 **11／11**；全包 249／250；行数门 106/106；墙 34/34 且与 `墙-改后` 对账 对象行 改=0；机审六列 0；变异 5 绿／6 红 → 还原 11／11 绿；**PASS** |
| 五 | `7891bdfd…` | 同上（「条数不当记录号」两处 ＋ 排序 tiebreaker 之后） | `src-sha=b0eb81a3ca5154cb`；新用例 **14／14**；全包 **253／254**（唯一红＝基线那条）；行数门 106/106；墙 34/34 且与 `墙-终` 对账 **对象行 不变=33 改=1**（`心愿排期.html`：`#4` → `4 个`）；机审六列 0；变异 6 绿／8 红 → 字节还原 → 14／14 绿；**PASS** |

窗口三（`ca22257b…`）是返修前的同一套阶段，它当场抓出**用例自身**的抽取 bug（调用面白名单那一版把整行字面量都算了进来）——按 §2.4.3 不计入对账，只记一笔。

### 窗口五那 5 页载荷差异，逐页点名定性（都不是本票的回执改动）

| 页 | 差异 | 定性 |
|---|---|---|
| `心愿排期.html` | `对象：心愿清单 #4` → `4 个` | **本票要改的**（条数不当记录号） |
| `备忘改分类-批量-向导.html` | 清单里同秒并列的两条换了位（`#2` 排到 `#1` 前） | **本票要改的**（`id DESC` tiebreaker 生效） |
| `完成心愿.html`／`按时间搜备忘.html`／`查心愿.html` | 只差 `due` 的**日期**（`2026-10-21` ↔ `2026-10-22`、`2026-09-19` ↔ `2026-09-20`） | **夹具日期漂移**：墙的夹具按「今天 ＋ N 天」种排期日期，两批墙跨了本地零点各跑一次 ⇒ 与本票无关 |

（这也是载荷级对账的两个已知噪声源：按「今天」相对的夹具 ＋ 同秒并列的顺序。）

## 二 屏上门（改后墙真渲染整页截图）

| 档 | 「处理结果」第一行 | lead |
|---|---|---|
| 390 | `对象` ／ `批量改分类 1 条` | `改分类完成：更新 1 条，跳过 0 条` |
| 1280 | `对象` ／ `批量改分类 1 条` | `改分类完成：更新 1 条，跳过 0 条` |

两档整页文本里都不再出现 `#`。截图：`.scratch/t882/屏-390-改后.png`、`屏-1280-改后.png`（`.scratch/` 不入仓）。

## 三 复跑办法（工作目录＝仓根；脚本已入仓）

```powershell
node tooling/run-locked.mjs --ticket 882 -- pwsh -NoProfile -File docs/skills/skill-memo-ilife/t882-门窗口.ps1 -Out packages/skill-memo-ilife/.scratch/t882/墙-终2 -Baseline packages/skill-memo-ilife/.scratch/t882/墙-终
```

对账件单独用：`node docs/skills/skill-memo-ilife/t882-对账.mjs <基线墙目录> <候选墙目录> [--payload] [--max-object-changes N] [--max-payload-changes N]`。

**注意（本票自己踩的坑）**：`test/receipt-882.test.mjs` 一律 import `dist`，单跑它**验不到「改了 src 忘了重编译」**。要证这件事，须把本包 `test/cmd-registry-855.test.mjs` 一起跑（它的 ⓪ 就是「dist 不许比 src 旧」），或直接用 `docs/agents/t540-指纹绑定.mjs`（本门窗口 STAGE-1／末段各一次）。Windows 上脚本请用 `pwsh`（7.x）。

## 四 独立评审（`/code-review` 两轴）与逐条处置

**Standards 轴**：硬 3 条 —— 缺 `.changeset/882-*.md` → **已补**；缺对账导出件 ＋ 复跑脚本未入仓 → **已补**；证据件读数与现状不符 → **已改**（本件一）。坏味道 3 条 —— `entityId === ''` 投机分支 → **已删**；`entityRefOf` 名不对／只用 `r.entityId`／注释反例过期 → **已修**（`objectRowTailOf(entityId)`）；Primitive Obsession → **不修**（判断题：要动 9 个调用点与族契约，而族口径正等下一张票一次定；名单已写进用例注释）。

**Spec 轴**：3 条 —— §六.4 的「18 格」仓内只锁 2 格、不成门 → **已补**两条用例（19 格逐格 ＋ 调用面白名单）；§六.1 措辞与落成不符、`心愿批次` 没列 → **已订正**并补全（两处计数格这一轮直接修掉）；两处兜底**字面**被锁死 → **已改**成只锁形状。

## 五 本票收口时一并修掉的三条（不开新票）

1. `心愿清单`／`心愿批次` 两处**条数当记录号** → 改字符串形态（`N 个`／`N 条`），用例两条真出口锁住。
2. `collectBatchItems()` 排序**同秒并列不稳** → 补 `id DESC`，用例一条（同一份库跑两次 id 序逐字相同）锁住。
3. 两处兜底文案（`未新建`／`—`）**保持原样**：现状已是人话，本轮只去掉了那个 `#`。

**唯一留着的**：回执族 19 格的页 h1 是模板兜底「备忘录回执」（载荷 `data.title` 为空），不按唤醒词出页题。**判不修**：① 本仓没有任何口径或判据要求回执页 h1 必须是唤醒词（`t834-逐格缺陷清单` 对同类问题只判「可改可不改」）；② 真改它要动 `pageEnvelope` 的出口字段，全墙 34 页页题一起变，属视觉终审面，不该夹在有文案缺陷的本票里。要改另说（改动 1 行 ＋ 重铺全墙）。

## 六 调试期运行不进本件

返修期的靶向跑（新用例单独跑、窗口三那次缺陷窗口、行数台账 `--sync` 的前置窗口）**不声明、也不当门禁证据**；本件的门禁读数全部来自 §〇 声明的六次运行。
