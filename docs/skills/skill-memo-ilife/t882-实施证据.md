# #882 实施证据：「对象」那一格的取值形状（备忘改分类-批量）

GATE-RUN runId=03ecf2e7-626a-441f-adc1-989267592618 cmd="pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/gate.ps1"

（这一行抄自 `.scratch/locks/gate-runs.log` 里同标识的条目，按 `docs/subagent-concurrency-protocol.md` §2.4.2 声明与对账；该次运行 exit=0、waitedMs=20021。）

## 一 门窗口（一个持锁窗口内跑完七阶段）

包作用域：`packages/skill-memo-ilife`；窗口内一律直调底层命令，不再嵌套加锁包装器（§2.4 第 5 条）。

| 阶段 | 命令 | 读数 |
|---|---|---|
| ① 指纹＋编译 | `node docs/agents/t540-指纹绑定.mjs --scope packages/skill-memo-ilife` | `RESULT: src-n=100 dist-n=404 src-sha=0ed86822b2508ca2 dist-sha=733cbbb7ded6d6c3 一致（dist 与 src 指纹一致）` |
| ② 新用例 | `node --test packages/skill-memo-ilife/test/receipt-882.test.mjs` | tests 7／pass 7／fail 0 |
| ③ 全包用例 | 包目录内 `node --test ../../test/scaffold.test.mjs test/*.test.mjs` | tests 243／pass 242／fail 1；红的唯一一条是 `test/cli-help-229.test.mjs:85`「独占写与递补那三个名字不再出现在本包源码里」（点 `src/cli/health/probe.ts`），**与冻结基线同一条**（改动前基线：228／227／1，同一条红） |
| ④ 行数门 | `node packages/skill-memo-ilife/scripts/check-warning-line.mjs` | `RESULT: 106/106 PASS：行数台账齐全、与实况一致、越线件只许变短（超线 0 件在册）`；本票三件 LF：`src/render/receipt.ts` 160／`src/memo/receipt.ts` 87／`src/memo/run.ts` 343（告警线 350，余 7 行） |
| ⑤ 重铺墙＋「对象」行对账 | `t834-gen-batch.mjs --out …/.scratch/t882/墙-改后` ＋ `diffrows.mjs 墙 墙-改后` | 34/34 格真跑成功；**不变=18 改=1**：`备忘改分类-批量.html` 由 `对象：批量改分类 #更新 1 条` → `对象：批量改分类 1 条` |
| ⑥ 机审 | `node docs/skills/skill-memo-ilife/t869-机审.mjs --dir …/.scratch/t882/墙-改后` | `RESULT: 34/34 PASS ①0 ②0 ③0 ④0 ⑤0 ⑥0 缺件— 名单=页群目录` |
| ⑦ 变异自证 | 族侧那一行改回旧写法 `return ' #' + String(r.entityId);` → 重编 → 跑新用例 | `MUTATE-APPLIED changed=True`；`pass 3／fail 4`；`MUTATE-RED exit=1`（期望非 0） |
| ⑦′ 还原 | 逐文件字节还原 → 重编 → 跑新用例 → 复读指纹 | `MUTATE-RESTORED identical=True`；`pass 7／fail 0`；还原后指纹与开窗逐字一致 |

## 二 屏上门（改后墙，Chrome 无网真渲染整页截图）

| 档 | 「处理结果」第一行 | lead |
|---|---|---|
| 390 | `对象` ／ `批量改分类 1 条` | `改分类完成：更新 1 条，跳过 0 条` |
| 1280 | `对象` ／ `批量改分类 1 条` | `改分类完成：更新 1 条，跳过 0 条` |

两档整页文本里都不再出现 `#`。截图：`packages/skill-memo-ilife/.scratch/t882/屏-390-改后.png`、`屏-1280-改后.png`（`.scratch/` 不入仓）。

## 三 复跑办法（工作目录＝仓根）

```powershell
node tooling/run-locked.mjs --ticket 882 -- pwsh -NoProfile -File packages/skill-memo-ilife/.scratch/t882/gate.ps1
```

复跑脚本与两批墙都住 `.scratch/t882/`（不入仓），结论以本件与票面正文为准；用例 `test/receipt-882.test.mjs` 已入仓，随包测试一起跑。

## 四 只对账声明过的那一次运行

调试期的靶向跑（新用例单独跑、旧墙重铺）**不写进本件、也不当门禁证据**；本件的门禁读数全部来自上面那一个窗口。
