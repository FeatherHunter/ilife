## 关票决议（2026-09-12）

**结论：说明面成文且与真跑逐条对上，验收通过。**

### 交付物

- `packages/skill-memo-ilife/SKILL.md` 正文：`## 快速开始` 加 1 行 ＋ `<!-- HELP-AUTO-END -->` 之后新增 **`## HELP 交付（说「备忘录 HELP」走这里，不分大小写）`（7 条）**：①缺省＝交付物（路径通式 ＋ `delivery.path` 绝对路径 ＋ `delivery.bytes`）＋**完成标准**（文件存在 ＋ 大小＝bytes ＋ exit 0）；②要速查表才加参数；③要现找才加参数（`q` 不落盘、`q`/`mode` 互斥）；④`--html` 语义；⑤边界（不开库不建库）；⑥**「『联动速查』块是构建期注入的索引」与「HELP 文件是缺省交付物」两句同场**；⑦入口只认 `备忘录 HELP` 一条。`git diff --stat` = 17 insertions / 1 deletion；**LF 保持**（63 → 75）。
- `packages/plugin-memo-ilife/test/skills-provider.test.mjs`：原条件断言 `:106` **翻成无条件正向**（现 `:95-101`），并补一条**同伴断言**（`备忘录_HELP_<YYYYMMDD_HHMMSS>.html`），删掉已无调用点的兜底函数。

### 真跑逐条核对（`SKILLS_DB_PATH=D:\2Study\StudyNotes\.db`）

`exit=0`；回执 `delivery.path` 给**绝对路径**；产物 `备忘录_HELP_20260912_133947.html` **130 885 B ＝ `delivery.bytes`**；**跑完 `.db\memo` 仍不存在**。三支口径亦真跑：`mode:"lookup"` → `备忘录_速查表_…`（total 28／6476 B）；`q` → 命中 1 条、**回执无 `delivery`、零落盘**；`q`+`mode` 与坏 `mode` → `ERR 2`（exit 2）；同秒三连跑真复现 `_2`／`_3`（**从 `_2` 起**，合裁决 3）；`--html` 覆盖写不递补、多级父目录自动建。

### 两点值得记账的验证质量

1. **它证明了自己的正向断言单独是空断言**：删掉新增的 HELP 交付节后，`含 'memo.help.lookup'` **仍为 True**——因为**被 `HELP-AUTO-*` 注入块的「相关场景：…」白捡**（那块本来就列了这条命令）。⇒ 它**主动补了一条同伴断言**（`备忘录_HELP_<YYYYMMDD_HHMMSS>.html`），删节后该条转 **False**，**真挡得住**。这是本图里少见的「自己证伪自己的断言强度」。
2. 两件旧账查清：① **`scripts/build-help.mjs` 注入不保检出换行**（`:29` 写死 `'\n'`、无 eol 探测；对照记账 `:34-37` 有）——用**未改一字的脚本**在 `%TEMP%` 副本上造 CRLF 复现「CRLF=63/bareLF=0 → CRLF=30/bareLF=33」；四家同族对照：记账／`base-combos` **保**，居家／卡路里／大厨／备忘录**不保**；**本机 `core.autocrlf=false` 且 `.gitattributes` 无 `*.md` 规则 ⇒ 当前不触发**，本票只查未修。②**「速查块」与「HELP 交付」同源两物**的关系已写进 `SKILL.md:69`。

### 裁掉它上报的一处矛盾（本席此前两条口径打架）

它指出：票面交接项 2 要求本票把 frontmatter 里那句无空格的「备忘录HELP」纠成带空格形，**但用户明确「frontmatter 归票 12、已改好、不许再动」**，且现测试与 frontmatter **逐字一致（绿）**。它按用户口径**未动**，请本席收口。
**裁决**：**维持不动**。理由：①`SKILL.md` 的 frontmatter 由**票 12**（`#232`，已关 100%）处置，该处**已同时声明了规范入口**「触发词：备忘录 HELP（不分大小写）」且**有断言焊死**；②开头那个无空格形是**名称引用**，测试断言的是「技能表返回的描述 ＝ frontmatter 实测描述」⇒ **两者一致、不会漂**；③为一个外观层面的一致性去重开已关票的产物，收益不抵风险。**本席先前的交接项 2 就此作废**，改为记账：这是一处**已知的、有意保留的表述**。

### 边界与纪律

只构建本包；**未**跑仓根 `tsc -b`／`pnpm -r build`；**一次都没碰暂存区**；`packages/base-render/**` 与只读件未碰；未重启／未杀 GUI；仓库根 `.scratch-*` = 0。HEAD 未动（`3da2559`），**未 commit**。
