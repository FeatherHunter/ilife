## 关票决议（2026-09-12）——**维护者肉眼终审「过」，地图目的地达成**

### 关票凭据（维护者原话，逐字）

> **「重启了 通过命令拿到了 HELP 页面 符合我的要求」**

### 本次验收运行的实测证据（编排会话复核）

维护者这次运行产出：

```
备忘录_HELP_20260912_173247.html    130825 B    2026-09-12 17:32:47
```

编排会话逐项复核该产物：

| 项 | 结果 |
|---|---|
| 字节数 | **130 825**，与编排会话此前验证过的 `…134456`／`…140336` **逐字节同尺寸** |
| 模板指纹 | `help-data` ×2 ／ `<title>` ×1 ／ `@media` ×6 —— 与**已被维护者验收过的卡位里 HELP** 逐项同形 |
| 硬规矩：命令不上页面 | `memo.help.lookup` 0 ／ `memo-cmd-read` 0 ／ `--html` 0 |
| 硬规矩：不标缺失 | `无唤醒词` 0 |
| 实现在场检查 | `原子操作` 0 ／ `级联` 0 ／ `全文索引` 0（18 处黑话清理成果保住） |
| 边界：不开库不建库 | 跑完 `<db>\memo` **仍不存在** |

### 目的地两条口径（地图 Destination 原文）

1. **明确拿到 help HTML 文件（落盘＋可打开）** —— ✅ 落 `<SKILLS_DB_PATH>/memo_html/备忘录_HELP_<YYYYMMDD_HHMMSS>.html`，回执顶层 `delivery.path` 给绝对路径；**扁平落盘**（无 `help/` 层）。
2. **由现成通用 help 模板渲染** —— ✅ 走 `base-paint/help-shell` 的 `renderHelpShellHtml`；指纹与卡位里 HELP 逐项同形。
3. **技能侧与插件侧都要跑通** —— ✅ 技能侧：`~/.agents/skills/skill-memo-ilife`（Junction → 仓库）使 DSH 认到该技能；插件侧：`dsh-memo-ilife` 技能提供方 ＋ 两个断链点（frontmatter／`files`）已修。
4. **维护者肉眼终审「过」** —— ✅ 见上。

> **过程中最关键的一次纠正**（详见裁决正本**裁决 25**）：先前"插件提供方那一路已够、`~/.agents/skills` 先不建"的判断，被维护者的实机症状推翻——他在 DSH 里说「备忘录 HELP」得到「**备忘录这块我这儿没挂技能**」，而列出的恰是 `skill-bill`／`skill-calorie`／`skill-chef`；编排会话实测该目录里**正是这三个、`skill-memo-ilife` 不存在**（一比一对应）⇒ **DSH 认技能的入口就是这个目录**。建 Junction 后**技能目录立刻更新、无需重载**。

### 从票 12（`#232`）接走的 T1–T5

| 项 | 状态 |
|---|---|
| T1 重启后核技能表真可见 | ✅ **本票完成**——维护者已重启并通过命令拿到页面 |
| T2 核日志无 `failed to apply loader entry dsh-memo-ilife` | ⬜ 未做（不影响验收结论；如需归档可补） |
| T3 定 `~/.agents/skills` 要不要建 | ✅ **已建 Junction**（裁决 25） |
| T4 核 `inject` 三连是否全需（含历史遗留的 `webServer`） | ⬜ 未做（同上） |
| T5 `test/skills-provider.test.mjs:106` 那条断言在说明面落地后翻正向 | ✅ 已由 `#231` 翻正（并补了不被注入块白捡的同伴断言） |

### 本票完成判据的措辞（复审员 H 指出后已订正）

原措辞「与老骨架**条数、名称、顺序逐字**」对 `title` 已不成立（按裁决 21 D5／裁决 23 授权有 7 处换说法）。**已订正为**：**条数／顺序逐字**（30 场景／8 域／13 二级组／64 字段／12 别名）＋ **名称／可见文案在声明的换说法之后逐字**。维护者本次验收通过，即按订正后口径通过。
