# t867 · 证据：读数链的逐条读数（正例／反例／判分对照）

> 票：[#867](https://github.com/FeatherHunter/ilife/issues/867)（Part of [#820](https://github.com/FeatherHunter/ilife/issues/820) 任务清单序 25）。
> 本件是**读数留痕**：每一条都给出命令与当刻读数，可复跑。形状口径在 `docs/base/base-render/t867-读数链契约.md`；
> 人核四列的口径与出处写法在 `docs/skills/skill-memo-ilife/t867-人核档.md`。
> 命令里的 `<读数目录>` 是本票现场用的落点，共两处：会话取数现场（仓根临时目录）的 `t867` 子目录，与系统临时目录 `t867-验收`。
> **落点只是「跑在哪」的记录，不是依赖** —— §二 第 3 跑把落点放在仓外照样四件齐（§七 有读数）。

## §一 交付物（五件 ＋ 一件夹具）

| 件 | 路径 | 是什么 |
|---|---|---|
| 装配器 | `docs/skills/skill-memo-ilife/t867-facts.mjs` | 页群目录 ＋ 人核档 → `facts.json`；三件 reader 缺则产、在则校 |
| DOM 列探针 | `docs/skills/skill-memo-ilife/t867-dom-probe.mjs` | 吃 `--dir`，出 DOM 六列 ＋ 两列机器候选（不再写死任何目录） |
| 人核档 | `docs/skills/skill-memo-ilife/t867-人核档.md` | 四列（＋一个可选位）的取值口径与逐页出处 |
| 形状契约 | `docs/base/base-render/t867-读数链契约.md` | 四件读数的字段表、判分怎么读、装配器的自检清单 |
| 证据 | `docs/skills/skill-memo-ilife/t867-证据.md` | 本件 |
| 夹具页群 | `docs/skills/skill-memo-ilife/t867-夹具-页群/`（三件真产物 ＋ `来源.md`） | 让正例在干净克隆上就能重跑；逐字节同源，sha256 见该目录 `来源.md` |
| 按域配置（夹具页群那份） | `docs/skills/skill-memo-ilife/t867-按域配置-夹具页群.json` | 公共层判分引擎的实例名单：逐条给 `{file, key}`，`key` ＝文件名主体（§五 用得上） |

三件既有 reader 一个字节没动（`packages/skill-calorie/scripts/audit-separators.mjs`、`packages/skill-calorie/scripts/measure-responsive.mjs`、`docs/skills/skill-calorie/t516-判据-版式.mjs`）；`packages/**` 全未触碰。

## §二 正例：一条命令产出 `facts.json`

命令形态（`<票号>` 与本票实际用的一致）：

```powershell
node tooling/run-locked.mjs --ticket <票号> -- node docs/skills/skill-memo-ilife/t867-facts.mjs --dir docs/skills/skill-memo-ilife/t867-夹具-页群 --human docs/skills/skill-memo-ilife/t867-人核档.md --json <读数目录>/facts.json
```

| # | 场景 | `<读数目录>` | 读数 |
|---|---|---|---|
| 1 | **现产**：读数目录是空目录（等价干净克隆） | 会话取数现场的 `t867\正例` | 三件 `produced`；末行 `RESULT: 四件读数齐（sep.json＋resp.json＋fmt.json＋facts.json） 页键 3（页群产物 3 件）` ＋ `PASS`，**exit 0** |
| 2 | **票面原文那一跑**（三件已在盘上） | 会话取数现场的 `t867` | 三件 `reused`；同一摘要行 ＋ `PASS`，**exit 0** |
| 3 | **落点在仓外**（证明不依赖任何取数现场） | 系统临时目录 `t867-验收` | 三件 `produced`；`FACTS-WROTE …\Temp\t867-验收\facts.json`，**exit 0** |

逐页读数（三次一致）：

```
PAGE 同步报告             DOM六列: tables=0 imgTags=0 tdDataLabel=0 tocEl=0 aspectRatio=0 scrollMargin=0   人核四列: dupFacts=3 english=2 d1=0 d2=0
PAGE 备忘改分类-批量-向导   DOM六列: tables=0 imgTags=0 tdDataLabel=0 tocEl=0 aspectRatio=0 scrollMargin=0   人核四列: dupFacts=2 english=4 d1=0 d2=0
PAGE 搜备忘               DOM六列: tables=0 imgTags=0 tdDataLabel=0 tocEl=0 aspectRatio=0 scrollMargin=1   人核四列: dupFacts=0 english=0 d1=0 d2=0
SUMMARY 三件 reader：sep.json=produced|reused resp.json=… fmt.json=…
```

三件 reader 自己的读数（同一次跑里由装配器按固定命令形态调用；这三行是「四件读数齐」背后的原始面）：

- `sep.json`：`RESULT: 2/3` ＋ `FAIL`（**exit 1 是正常读数**，不是失败）——那 1 处是 `同步报告` 的 `R1`：`L342` 的「点复制数据保存本次同步结果 · 点复制日志用于反馈问题」。
- `resp.json`：`OVERFLOW-ZERO pages=3 cells=9 failed=0 scopeOutFailed=0 label=t867`（**exit 0**）。
- `fmt.json`：`RESULT: 3/3 件读数完成（四档=390/768/1440）`（**exit 0**）；390 档 `touchSmall` 三者合计 10 处（`同步报告` 5 ＋ `备忘改分类-批量-向导` 5 ＋ `搜备忘` 0），最小字号＝11／11／12。

## §三 票面三条反例（逐条点名）

| # | 变异（都在会话现场的副本上做，不动仓内件） | 命令差异 | 读数（exit 1） |
|---|---|---|---|
| 1 | 人核档里删掉 `搜备忘` 的 `english` 那一列 | `--human <现场>/反例1/人核档.md --readings <现场>/正例` | `FAIL: 缺列 english（页 搜备忘）` |
| 2 | 页群目录里删掉 `同步报告_20260921_080952.html` | `--dir <现场>/反例2/页群 --readings <现场>/正例` | `FAIL: 缺产物 同步报告（人核档页单里有，页群目录里没有对应的 .html）`；另三条 `FAIL: sep.json／resp.json／fmt.json 里多出一页（页群里没有它）：同步报告_20260921_080952.html` |
| 3 | 临时副本里把 `sep.json` 一处 `node.hits[].tags` 改成 `["R1","R9"]` | `--readings <现场>/反例3` | `FAIL: sep.json 同步报告_20260921_080952.html 的 node.hits[1].tags 取到不在闭集里的值「R9」（契约 §一：R1–R7）` |

（三条各只差一个参数，其余与 §二 命令逐字相同；`<现场>` ＝ 会话取数现场。）

## §四 自查三条反例（改坏必红；本票的负向证据）

| # | 变异 | 读数（exit 1） |
|---|---|---|
| 4 | `同步报告` 的 `english` 判「改判」但删掉理由 | `FAIL: 列 english 判「改判」但没写理由（页 同步报告）` |
| 5 | `备忘改分类-批量-向导` 的 `english` 静态出处引 `#999` | `FAIL: 列 english 的出处引 #999，但它不在机器候选里（页 备忘改分类-批量-向导；候选节点号 5/14/17/18）` |
| 6 | 同页 `english` 取值改成 3（候选 4）却仍写「采纳候选」 | `FAIL: 列 english 判「采纳候选」但取值 3 ≠ 机器候选 4（页 备忘改分类-批量-向导）；不同就写「改判」并给理由` |

还原一致：六条反例逐个跑完后，§二 第 2 跑（正例）逐字回绿（`PASS`，exit 0）。

## §五 判分对照：这份 `facts.json` 判分引擎吃得动

### 5.1 公共层引擎（[#868](https://github.com/FeatherHunter/ilife/issues/868) 已落的那一份，`packages/base-render/scripts/判分.mjs`）

```powershell
node tooling/run-locked.mjs --ticket <票号> -- node packages/base-render/scripts/判分.mjs --dir <读数目录> --config docs/skills/skill-memo-ilife/t867-按域配置-夹具页群.json --json <读数目录>/score-公共层引擎.json
```

读数：三页页分 **60／69／96**、均分 75、过线页 1/3，`RECONCILE 逐页逐维（含硬扣分与总分）最大绝对差 = 0 ⇒ 一致（差 0）（比对 21 项）`，**exit 0**；
引擎自己报的冻结值与判据数值取自公共层：`可点控件命中下限 44px；正文类字号下限 12px；仓内断点集合 400／640／820／1001／1200`。

**接口读数一条（写给接线的人）**：引擎按**按域配置给的键**配对 `facts.json` 的页（`instances[].key`，找不到就按文件名兜底）；本链的页键是**文件名主体**，
所以不配 `instances` 直接跑会红：`判分中止（退出码 1）：缺件：facts.json 的 pages 里没有 同步报告_20260921_080952.html（按 文件名 … 配对）—— 缺件报警，不静默取 0 分`。
34 格整批那份名单来自 `docs/skills/skill-memo-ilife/t856-manifest.json` 与清单生成器，归 [#834](https://github.com/FeatherHunter/ilife/issues/834) 收口时补。

### 5.2 过渡复制件（备忘录域 `t827-判分.mjs`，两域分账时的对照）

把四件读数与产物摆成引擎要的目录形状（四件读数在目录顶层，产物放 `pages/` 子目录），用备忘录域的**过渡复制件**跑一遍：

```powershell
node docs/skills/skill-memo-ilife/t827-判分.mjs --dir <对照目录> --json <对照目录>/score.json
```

同一份读数、两台引擎（公共层引擎与过渡复制件）**逐页逐维读数相同**：60／69／96，差 0 —— 这也是「同一读数目录两域分差 0」那条结构保证的一次现场印证。

| 页 | D1/15 | D2/20 | D3/25 | D4/15 | D5/25 | 逐维合计 | 硬扣分 | **页分** | 每维≥80% |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 同步报告 | 15 | 20 | 17 | 12 | 16 | 80 | −20 | **60** | 否 |
| 备忘改分类-批量-向导 | 15 | 20 | 21 | 15 | 16 | 87 | −18 | **69** | 否 |
| 搜备忘 | 15 | 20 | 25 | 15 | 21 | 96 | −0 | **96** | **是** |
| 均分 | 15 | 20 | 21 | 14 | 17.67 | — | −12.67 | **75** | 1/3 页 |

- `RECONCILE 逐页逐维（含硬扣分与总分）最大绝对差 = 0 ⇒ 一致（差 0）（比对 21 项）`，**exit 0**。
- 两向都走到了：改前状态的报告页与预检确认页判红（60／69），search 域那件**过线页判绿**（96，每维 ≥80%）。
- 硬扣分逐条与 §二 的人核四列对得上：`同步报告` −20 ＝ H1 2＋H3 6＋H6 10＋H7 2；`备忘改分类-批量-向导` −18 ＝ H3 4＋H6 10＋H7 4。D4 扣 3 的正是 `d4cut`（1440 档只多留空）。

## §六 硬缺口：静态面 ≠ 载荷渲染面（本票翻出来的一条，读数可复现）

**现象**：备忘录的产物页把内容放在页内 `<script id="payload" type="application/json">` 里、由页内脚本渲染；四件读数里靠静态读的那部分（`sep.json` 的可见文本、`facts.json` 的 DOM 六列与两列机器候选）先剥掉 `<script>` 整段，于是**看不到脚本渲染出来的文字**。

**复现（本仓现场，可重跑）**：

```powershell
# ① 静态面：这一页在渲染后有点号并列、有半角逗号、有重复串，静态候选却是 0
node docs/skills/skill-memo-ilife/t867-dom-probe.mjs --dir docs/skills/skill-memo-ilife/t867-夹具-页群 --quiet
#    实测：同步报告 候选 dupFacts=0 english=0（sep.json 也只有 L342 那一处 R1）
# ② 渲染面：同一页的标题、四块指标卡明细、错误明细都由脚本渲染（行号即出处）
#    L418／L424 渲染 '同步完成 · 1 个错误'（点号并列）；L419 与 L458 渲染的串用半角逗号；
#    L374 渲染出来的两个复制按钮与静态 L344／L345 那两个同名
```

**这条缺口值多少**：`同步报告` 的 H1 少算 1 处（渲染出来的 `·` 没进 `sep.json`）、H7 少算 2 处、H3 少算 3 处 —— 本票把它**写在人核档的取值里**（`出处=渲染后 …`，判「改判」并写理由），但**机器读数面本身没治**：三件 reader 的语义按票面「一行语义不动」，本票不改它们。

**建议去向**（本票不自行开票，按 `docs/agents/编排纪律.md`「遗留当场开票」的三问先摆事实）：能复现（上面两条命令）；有归属（这份链的读面归 [#869](https://github.com/FeatherHunter/ilife/issues/869) 六列机审与 [#868](https://github.com/FeatherHunter/ilife/issues/868) 引擎的读数目录，但「静态 vs 渲染」是四件共同的前提，落在谁身上要由地图定）；是已承诺的目标吗（八张域票与 [#834](https://github.com/FeatherHunter/ilife/issues/834) 收口都要用这两门判据，读面缺一半会直接影响判分）。

## §七 票面断言：新件里不出现取数现场的目录名字面（点号 ＋ `scratch` 六个字母）

```
0  docs/skills/skill-memo-ilife/t867-facts.mjs
0  docs/skills/skill-memo-ilife/t867-dom-probe.mjs
0  docs/skills/skill-memo-ilife/t867-人核档.md
0  docs/skills/skill-memo-ilife/t867-夹具-页群/来源.md
0  docs/base/base-render/t867-读数链契约.md
0  docs/skills/skill-memo-ilife/t867-证据.md
```

六件**全是 0 处**（本件的命令写成 `<读数目录>`，两处实际落点在件头说明）。这条断言要守的是「干净克隆可重跑」：§二 第 3 跑的落点在仓外（系统临时目录），四件照齐、exit 0；夹具页群与三件 reader 全在仓内，正例不读任何取数现场。

## §八 夹具的逐字节同源读数

```powershell
Get-ChildItem docs/skills/skill-memo-ilife/t867-夹具-页群/*.html | ForEach-Object { (Get-FileHash $_.FullName -Algorithm SHA256).Hash.ToLower() + '  ' + $_.Name }
```

| 文件 | 字节数 | sha256（与来源件逐字节相同） |
|---|---:|---|
| `搜备忘_20260921_192606.html` | 24561 | `afe76d6cd1254f5138d5d77975ad54b19c5733b381c04eeffffd820a913c7594` |
| `备忘改分类-批量-向导_20260921_175428.html` | 23659 | `edcaa158f3727b6a0f328d6a8add5a71ba7bc0d1fd52577f945be4d7d93babad` |
| `同步报告_20260921_080952.html` | 29166 | `4273d714a88c70db98372d9765b6c2fdce489a0153b4da993236774a6c0d6237` |

## §九 独立复核席位的复跑与它翻出来的东西（作者自评不算复核）

复核由**另一席位**独立做（只信提交号与自己的复跑，不采信本件）：完整报告在会话现场 `t867-复核/复核-独立.md`（359 行，逐条带复现命令与当刻读数原文），复核用的现场在 `t867-复核/` 下。

**复核自己复现的票面验收面**：正例 exit 0（三件 `produced`、页键 3、`PASS`）；三条票面反例逐条 exit 1 且点名；判分 exit 0、60／69／96、`RECONCILE` 差 0（21 项）；三件夹具的 sha256 与 `来源.md` 登记值一致、且与原始件逐字节相同；断言 0 处。它自设 **25 条变异**，24 条按契约该红的都红了（页键撞车／人核档多页／`d1`·`d4cut` 扣分无理由／坏出处／写「渲染后」却判「采纳候选」／`L` 超行／三档缺失／读数多页／计数器错／`overflow`·`ok`·`touchSmall` 矛盾／跨列候选／`--no-readers`／用法错三态）。

**它翻出来的东西与处置（本件第二笔修复批次）**：

| # | 它报的 | 我复核后判定 | 处置（读数） |
|---|---|---|---|
| 1（缺陷） | 契约 §六#9 写「同一页同一列写两次 → exit 1」，实测 **exit 2** | 成立 —— 解析器当时抛 `Abort` 被映射成 2，与契约不一致 | 改为记进 `bugs` 走 exit 1：实测 `FAIL: 人核档里同一页同一列写了两次：english（页 搜备忘）`，exit 1；同时补「同一页出现两次」同款点名 |
| 2（可疑） | 红的一跑**不作废**同目录旧 `facts.json`：下游会静默拿旧读数出分 | 成立 —— 这是收口批处理上的真风险（漏转发退出码就静默出分） | 红的一跑就地作废落点并点名：实测 `STALE-DROPPED ………/facts.json（上一跑留下的 facts.json 与本跑不一致，已作废；下游只认 exit 0 的那一跑）`，作废后文件不在 |
| 3（可疑） | 人核档里 `english = 2` 的理由**点错了块**（把 `L489`／`L496`／`L507`／`L514`／`L520` 说成折叠的错误明细，其实它们在四块指标明细的条件分支里、本页没渲染） | 成立 —— 数值站得住、理由不站得住（`makeDetail()` 在 `L543`–`L549`，真折叠块是 `L525`–`L531`） | 人核档理由改正为两类分别写清（折叠态一处、条件未渲染一处）；数值不变 |
| 4（低危） | 契约 §四 的 `judge` 子字段「9 处缺」 | 成立但**是文档没写清**：那几位本来就按列出现 | 契约 §四 补一句「`judge.<列>` 的字段按列出现」 |
| 5（低危） | `--widths` 收窄时装配器照绿（下游引擎才拦） | 成立 —— 装配器不该放过少了判据档的输入 | 改成用法错 exit 2：实测 `--widths 少了判据档 1440（四件读数按三档 390／768／1440 出；少了档下游判分引擎吃不了）` |
| 6（低危） | 页键撞车不点两个文件；同一个开关写两次首见优先、后写静默失效 | 成立 | 撞车点两个文件 ＋ **早报**（不再白跑两趟浏览器）：实测 `页键撞车（文件名主体重复）：同步报告（同步报告_20260921_080952.html 与 同步报告_20260922_000000.html 撞同一个键）`，exit 1；重复开关改成用法错：`同一个开关写了两次：--dir（首见并不优先，直接算用法错）`，exit 2 |

修复批次之后，票面验收面**逐条回绿**：正例 exit 0（页键 3＝产物 3 件）、三条票面反例 exit 1 且点名、六条自查反例 exit 1 且点名、断言 0 处、引擎 60／69／96 与差 0。

**复核明确列为「未证」的（本票也不假装证了）**：三件 reader 的 exit 2 分支（没破坏现场）；R3／R5／R6／R7 四条规则在这三页上没有真样本；H5／D4 的破格分支未触发；`d1`／`d2` 该不该扣属人核判断、机器核不了（含「1440 档正文列约 936px」那个实测值）；渲染面是否还有第三处处数（没做逐处穷举，只按视觉模型看得到的那几处记）；`--widths` 收窄算不算违规（已按「少档即用法错」处置）；#834 的批处理是否转发退出码（不在本票写集里）。

