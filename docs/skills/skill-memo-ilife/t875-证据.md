# t875 · 备忘录失败文案零内部命令名（#832／#829 记账的家族债）

> 票：[#875](https://github.com/FeatherHunter/ilife/issues/875)（Parent [#820](https://github.com/FeatherHunter/ilife/issues/820)；堵住 [#820](https://github.com/FeatherHunter/ilife/issues/820) 的一格）。
> 判据口径：`docs/skills/skill-memo-ilife/t849-视觉基准.md` §1 硬扣分 H2／H7 ＋ `docs/skills/skill-memo-ilife/t869-机审.mjs` 的 ⑥ 列（英文裸词／半角标点）。
> 债的记账：`docs/skills/skill-memo-ilife/t869-机审读数.md` §二 ⑥ 列「内部命令名」一行；现场读数 `docs/skills/skill-memo-ilife/t832-sync-证据.md` §六.1（改前 ⑥ 命中 1 处）。

---

## 一 结论

飞书四门（存在／登录／授权／超时）不过时抛出的 7 条失败文案已改成面向用户的中文：
**零内部命令名与内部标识符**（`lark-cli`／`auth status`／`openId`／`scope`）、**零 ASCII 字母与零半角标点**，
四门各自卡在哪一步仍逐门点名。真产物旁证：不给远端挡板时 `备忘录同步` 的报告页副标题不再出现 `lark-cli`，
机审 ⑥ 列由改前的 **1 处** 归零（`RESULT: 1/1 PASS ①0 ②0 ③0 ④0 ⑤0 ⑥0`）。

---

## 二 逐条文案对照（改前 → 改后）

抛出点 `packages/skill-memo-ilife/src/sync/feishu.ts`（行号＝改前／改后同一批行，本票一字未增删行）：

| 行 | 改前（内部名上屏） | 改后（面向用户） | 卡在哪一步 |
|---|---|---|---|
| `:133` | `lark-cli 不可用：<CLI 路径>` | `飞书命令行工具不可用：程序无法启动` | 工具起不来（ENOENT） |
| `:134` | `lark-cli 超时：<命令原文>` | `飞书命令行工具超时：命令没在时限内返回` | 超时 |
| `:148` | `lark-cli auth status 失败（未登录？）` | `飞书登录状态查询失败：可能还没登录，先登录一次` | 没登录（查询失败那一支） |
| `:151` | `lark-cli auth status 非 JSON` | `飞书登录状态读不出内容：先按安装指引重新登录一次` | 没登录（读不出内容那一支） |
| `:154` | `lark-cli 未登录（无 openId，先 auth login）` | `飞书未登录：登录状态里没有你的身份，先登录一次` | 没登录（确凿未登录） |
| `:169` | `lark-cli 未找到：缺失阻断取数` | `飞书命令行工具未找到：这一步要它，先按安装指引装好` | 没装 |
| `:172` | `缺 scope 授权：<scope>` | `飞书授权不足：这一步要的权限还没给，重新授权时请一并勾上` | 授权不足 |
| `src/sync/auth.ts:11`（见 §七 越界报备） | `lark-cli 未找到：缺失阻断取数` | 与 `:169` 同一句 | 没装 |

**含义不许丢**（四门仍各读得出）：没装（`:169`／`auth.ts:11`）、没登录（`:148`／`:151`／`:154` 三支）、
授权不足（`:172`）、超时（`:134`）；另留一条「装好了但起不来」（`:133`）。

**两个动态片断按票面撤掉**：`:133` 的 `<CLI 路径>` 与 `:134` 的 `<命令原文>` 都是内部名本身
（路径里就带 `lark-cli`，命令原文里就有 `auth status`），留下任一条都过不了 ⑥ 列 ⇒ 一并撤掉。

**不动的东西**：`LARK_INSTALL_PROMPT`（`src/sync/feishu.ts:22-57`）一字未改 —— 那份「复制给 AI 的安装指引」
本来就该带命令原文，票面明写不在此列且按载荷位不计入判据（改后真产物里 `lark-cli` 的 15 处**全在它里面**，见 §六）。

---

## 三 改动面

| 件 | 改了什么 |
|---|---|
| `packages/skill-memo-ilife/src/sync/feishu.ts` | §二 的 7 条文案（抛出点本体） |
| `packages/skill-memo-ilife/src/sync/auth.ts` | 同族同名那一句（`memo.auth` 走这条；见 §七 越界报备） |
| `packages/skill-memo-ilife/test/t760-设置页收窄.test.mjs:336` | 断言指到新文案的等价串（`/lark-cli 未找到/` → `/飞书命令行工具未找到/`） |
| `packages/skill-memo-ilife/test/wizard-pages-665.test.mjs:141` | 同上（写法与它那一条逐字同形，仍带「点名缺席」的失败文案） |
| `packages/skill-memo-ilife/test/t875-sync-wording.test.mjs` | 本票新增用例（7 条） |

两条既有断言**没放宽**：仍是「点名缺席」这一件事的逐字断言，只是把等价串换成新文案；
两条各自还留着 `官网行逐字`／`lark.prompt` 两条邻居断言（改后一并绿，见 §四）。

`wish/gate.ts` 与 `wish/reconcile.ts` **一字未改**：`gate.why` 收的就是 `e.message`，
`reconcile` 拼的也是它，抛出点改完这两处自动跟着干净（读数见 §四／§六）。

行数台账：`node packages/skill-memo-ilife/scripts/check-warning-line.mjs` →
`RESULT: 106/106 PASS：行数台账齐全、与实况一致、越线件只许变短（超线 0 件在册）`（`feishu.ts` 仍 221 LF、`auth.ts` 仍 19 LF）。

---

## 四 读数

### 4.1 新增用例（`test/t875-sync-wording.test.mjs`，7 条）

```
✔ #875 远端四门不过：回执文案零内部命令名
  ✔ ① 存在门·没装：message 与 errors 零内部名，且点名卡在哪一步
  ✔ ① 登录门·查询失败：message 与 errors 零内部名，且点名卡在哪一步
  ✔ ① 登录门·读不出内容：message 与 errors 零内部名，且点名卡在哪一步
  ✔ ① 登录门·没登录：message 与 errors 零内部名，且点名卡在哪一步
  ✔ ① 授权门·权限不足：message 与 errors 零内部名，且点名卡在哪一步
  ✔ ② 心愿类合成写也走同一条闸门文案：memo.create 回执零内部名
  ✔ ③ 工具不可用与超时两条文案也零内部名（回执路径覆盖不到这两支，直调取数层）
ℹ tests 7 / pass 7 / fail 0        RESULT: ticket=875 runId=f2475b9c-f53b-4391-9756-637a66865dab exit=0
```

判的三样（逐条都是外部读数）：① 零内部名与内部标识符（`lark-cli`／`auth status`／`openId`／`scope`／
`auth login`／`auth check`）；② 零 ASCII 字母、零半角标点（判据集照 `t869-机审.mjs` 的 `HALF`）；
③ 每一门各自点名卡在哪一步。布景：存在门复用 `t832-sync-domain.test.mjs` 的 `envNoLark`；
登录门与授权门用本文件自带的替身命令行工具（按 mode 摆 `status-fail`／`not-json`／`no-openid`／`no-scope` 四态）；
超时与「起不来」两支回执路径覆盖不到（`larkReady` 不抛超时那一支），改直调 `runLark` 并把时限压到 400 毫秒。

### 4.2 两条既有断言（票面点名的）

```
✔ 直调探测命令缺席时 stderr 带官网行与 lark.prompt 指引        ← test/t760-设置页收窄.test.mjs:336
✔ #665 W6 飞书缺席即大声失败＋带安装指引（#760）              ← test/wizard-pages-665.test.mjs:141
ℹ tests 24 / pass 24 / fail 0      RESULT: ticket=875 runId=5a675514-8f64-4c26-91fa-faa8ee4e8154 exit=0
```

### 4.3 回归（本包全量）

```
ℹ tests 270 / suites 54 / pass 269 / fail 1
RESULT: ticket=875 runId=afb89a70-f03e-4fcc-9410-f5252b8a6a8d exit=1
✖ #240 · 自持件已删（防这笔债再长回来）→「独占写与递补那三个名字不再出现在本包源码里」
```

**这 1 条红不是本票引入**（如实记）：`packages/skill-memo-ilife/src/cli/health/probe.ts:51` 里有
`writeFileSync(probe, 'probe', { encoding: 'utf8', flag: 'wx' })`，踩中 `#240` 的禁用式
（`flag: 'wx'`）。该件是 **HEAD 已提交、本票一字未碰**（`git show HEAD:<路径>` 逐字同形；
`git status --short -- <路径>` 空），出处在 #855 拆分那一笔（`4b034d72`）；见 §七 遗留出口。

同一支子集（`test/t760-设置页收窄` ＋ `test/wizard-pages-665` ＋ `test/t832-sync-domain` ＋ `test/t875-sync-wording`
＋ `test/fetch` ＋ `test/wish-sync-661` ＋ `test/wish-pages-829` ＋ `test/sentinel-666`）交付态读数：
`tests 69 / pass 69 / fail 0`；`RESULT: ticket=875 runId=d747c251-5040-4770-8aa2-c3503cd609fc exit=0`

### 4.4 GATE-RUN 行（`.scratch/locks/gate-runs.log`，逐字）

```
RUN ticket=875 runId=f2475b9c-f53b-4391-9756-637a66865dab cmd="node .scratch/t875/run-in-pkg.mjs --test test/t875-sync-wording.test.mjs" waitedMs=0 exit=0
RUN ticket=875 runId=5a675514-8f64-4c26-91fa-faa8ee4e8154 cmd="node .scratch/t875/run-in-pkg.mjs --test test/t760-设置页收窄.test.mjs test/wizard-pages-665.test.mjs" waitedMs=0 exit=0
RUN ticket=875 runId=d747c251-5040-4770-8aa2-c3503cd609fc cmd="node .scratch/t875/run-in-pkg.mjs --test test/t760-设置页收窄.test.mjs test/wizard-pages-665.test.mjs test/t832-sync-domain.test.mjs test/t875-sync-wording.test.mjs test/fetch.test.mjs test/wish-sync-661.test.mjs test/wish-pages-829.test.mjs test/sentinel-666.test.mjs" waitedMs=0 exit=0
RUN ticket=875 runId=afb89a70-f03e-4fcc-9410-f5252b8a6a8d cmd="node .scratch/t875/run-in-pkg.mjs --test" waitedMs=10010 exit=1
RUN ticket=875 runId=3c930403-9674-4f05-ab71-9962f23be453 cmd="node .scratch/t875/gen-sync-page.mjs" waitedMs=30029 exit=0
RUN ticket=875 runId=782e9629-89c3-42b0-a51b-aa2855ac22fe cmd="node .scratch/t875/run-in-pkg.mjs --test test/t875-sync-wording.test.mjs" waitedMs=10013 exit=1   ← 变异态（改坏那一轮）
```

（全量那一条 `exit=1` 就是 §4.3 那条既有红；其余 `exit=1` 是变异态，见 §五；交付态各条均 `exit=0`。）

### 4.5 提交件

`fix(875): 飞书四门失败文案去内部命令名（7 条改面向用户中文、两条断言换等价串、定义级用例）＋ t875 证据件`
—— `42103561`，6 件（`src/sync/feishu.ts`／`src/sync/auth.ts`／两条既有测试／新用例／本件）。

---

## 五 变异自证（改坏必红／还原必绿）

把 `src/sync/feishu.ts:169` 那一句**改回带内部名的旧写法** → 重编译 → 跑新用例：

| 轮 | 读数 |
|---|---|
| 变异态 | `✖ ① 存在门·没装` ＋ `✖ ② 心愿类合成写…`；`ℹ tests 7 / pass 5 / fail 2`；`RESULT: ticket=875 runId=782e9629-89c3-42b0-a51b-aa2855ac22fe exit=1` |
| 还原态 | `✔` 7 条全绿；`ℹ tests 7 / pass 7 / fail 0`；`RESULT: ticket=875 runId=f2475b9c-f53b-4391-9756-637a66865dab exit=0` |

两行原文（变异态点名那一处）：

```
AssertionError [ERR_ASSERTION]: 存在门·没装 的 message 里出现内部名「lark-cli」：对账没跑成：远端不可用（lark-cli 未找到：缺失阻断取数）
AssertionError [ERR_ASSERTION]: 合成写回执 message 里出现内部名「lark-cli」：已记一条：1；远端没成（lark-cli 未找到：缺失阻断取数）
```

还原判据＝**逐字节**：`packages/skill-memo-ilife/src/sync/feishu.ts` 的 sha256
改前与还原后同为 `20F845C21D87C4B1D087D780A13C77E9679FD0C53B1E37290CF566B4337E55CB`。

---

## 六 真产物旁证（验收第二条）

跑法：不给远端挡板（本机没装飞书命令行工具那一支），真跑唯一出口 `memo.sync` 收那一页
（`.scratch/t875/gen-sync-page.mjs`，形状逐字照 `.scratch/t832/gen-sync-page.mjs`）。

```
exit = 4    delivery.bytes = 124121    remote = 'unavailable'
message = 对账没跑成：远端不可用（飞书命令行工具未找到：这一步要它，先按安装指引装好）
errors[0] = 远端不可用：飞书命令行工具未找到：这一步要它，先按安装指引装好
RESULT: ticket=875 runId=3c930403-9674-4f05-ab71-9962f23be453 exit=0
```

机审（`docs/skills/skill-memo-ilife/t869-机审.mjs --dir .scratch/t875/pages`）：

```
FILE 备忘录同步.html  ①0 ②0 ③0 ④0 ⑤0 ⑥0
⑥ 英文裸词或半角标点：0 件
  合计：④ 0 句；⑤ 0 处（其中共用件债·复制区说明行 0 处）；⑤b 标题写法位 0 处；⑤c 符号顶替（旁证）0 处；⑥ 英文裸词 0 处／半角标点 0 处
RESULT: 1/1 PASS ①0 ②0 ③0 ④0 ⑤0 ⑥0 缺件— 名单=页群目录
```

**与改前对照**（`t832-sync-证据.md` §六.1 同一支）：改前 `RESULT: 0/1 FAIL … ⑥1`，改后 `⑥0`。

**豁免区如实记**：这一页里 `lark-cli` 仍出现 **15 处**，**全在 `larkSetup.prompt`（复制给 AI 的安装指引）之内**
（`.scratch/t875/read-page.mjs`：`larkSetup 区之前 0 处／区之内＋之后 15 处`；
副标题那一句里 `false`）。票面明写这份指引不在此列、且按载荷位不计入判据 —— 15 处里没有一处是「文案面上屏」。

---

## 七 越界报备与遗留出口

### 7.1 越界报备：`src/sync/auth.ts:11`（同包、同域，但不在票面点名的件里）

票面点名的两条断言（`t760:336`／`wizard-pages-665:141`）断言的是 `memo.auth --params '{"step":"status"}'`
的 stderr，而这条命令走的是 `src/cli/health`→`src/sync/auth.ts` 的 `authStatus()`，
那一句 `'lark-cli 未找到：缺失阻断取数'` 与 `feishu.ts:169` **逐字同名**（票面把它记在 `:169` 那一行上）。
不连改这一处，「把断言指到新文案的等价串」这条要求落不了地（断言会停在旧串上）。
故本票把它**同一句一起改**：同一件事只用一句文案。这是本票唯一一处越出票面点名清单的改动，如实报备。

### 7.2 遗留出口（本票不改，留给各自的票）

1. **`packages/skill-memo-ilife/src/cli/health/items.ts:198-207`（体检面「飞书 CLI」一条）**：
   `message`／`action` 里仍有 `lark-cli`（`找不到 lark-cli：…`）、`lark-cli auth check --scope task`。
   住体检面、不在本票点名的 7 条之列 ⇒ 不夹带。同族先例：`t869-机审读数.md` ⑥ 列把「`CLI`／`fts5`／首次使用页」也记在别家的账上。
2. **`packages/skill-memo-ilife/src/sync/sentinel.ts:164`**：`cleanup` 那句直接给用户一条
   `lark-cli task tasks delete --task-guid … --yes`。它是**给用户照着抄的清理命令**（与安装指引同类），
   按 `t869-机审读数.md` §3.1 的载荷位口径不进判据 ⇒ 本票不动；若后续判它属内部名上屏，是那一件的账。
3. **既有红：`#240 · 自持件已删`**（`src/cli/health/probe.ts:51` 命中 `flag: 'wx'`）：见 §4.3，非本票引入。
4. **`packages/skill-schedule/src/fetch/feishu.ts:104-140`** 有同一族文案（作息管家侧）。
   本票边界明写不许碰 `packages/skill-schedule/**`（那边另有票在飞）⇒ 只记账，不碰。

---

## 八 可重跑清单

```sh
cd D:\ilife        # 以下一律仓根
# 0) 编译（改了 src 才要）
node tooling/run-locked.mjs --ticket 875 -- node node_modules/typescript/bin/tsc -b packages/skill-memo-ilife

# 1) 新用例（7 条）＋ 票面点名的两条断言 ＋ 同族回归
node tooling/run-locked.mjs --ticket 875 -- node .scratch/t875/run-in-pkg.mjs --test test/t875-sync-wording.test.mjs
node tooling/run-locked.mjs --ticket 875 -- node .scratch/t875/run-in-pkg.mjs --test test/t760-设置页收窄.test.mjs test/wizard-pages-665.test.mjs
node tooling/run-locked.mjs --ticket 875 -- node .scratch/t875/run-in-pkg.mjs --test test/t760-设置页收窄.test.mjs test/wizard-pages-665.test.mjs test/t832-sync-domain.test.mjs test/t875-sync-wording.test.mjs test/fetch.test.mjs test/wish-sync-661.test.mjs test/wish-pages-829.test.mjs test/sentinel-666.test.mjs

# 2) 本包全量
node tooling/run-locked.mjs --ticket 875 -- node .scratch/t875/run-in-pkg.mjs --test

# 3) 真产物（不给远端挡板那一支）＋ 机审 ＋ 页内 lark-cli 处数
node tooling/run-locked.mjs --ticket 875 -- node .scratch/t875/gen-sync-page.mjs
node docs/skills/skill-memo-ilife/t869-机审.mjs --dir .scratch/t875/pages --json .scratch/t875/gate.json
node .scratch/t875/read-page.mjs

# 4) 行数台账
node packages/skill-memo-ilife/scripts/check-warning-line.mjs
```

⚠️ 本包测试**必须把 cwd 钉在包目录**（出口依赖包内 `node_modules` 的 junction 依赖 `base-paint`）——
`run-locked.mjs` 强制 cwd＝仓根，故用 `.scratch/t875/run-in-pkg.mjs` 换 cwd 再跑（同 `.scratch/t832/run-in-pkg.mjs`）。
