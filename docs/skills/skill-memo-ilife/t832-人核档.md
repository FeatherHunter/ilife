# t832 · 人核档（sync 域 1 页的 `facts.json` 四列取值）

票：[#832](https://github.com/FeatherHunter/ilife/issues/832)（Part of [#820](https://github.com/FeatherHunter/ilife/issues/820)）。
口径与写法照 `docs/skills/skill-memo-ilife/t867-人核档.md` §一／§二（同族先例）与
`docs/skills/skill-memo-ilife/t849-视觉基准.md` §1／§2（D1／D2 判据、手机端数值表与禁令）。
页键＝册子主体（`src/help/booklet.ts` 的 seq 29，族「报告」）。

**页群**：`.scratch/t832/pages/` 的 1 件真产物（`备忘录同步.html`，由 `.scratch/t832/gen-sync-page.mjs` 真跑 `memo.sync` 落盘）。
**复核手段**：① 机器候选＝`docs/skills/skill-memo-ilife/t867-dom-probe.mjs`（静态可见文本，本页两列候选均 0）；
② 渲染后＝Chrome `--dump-dom`（`.scratch/t832/rendered-dom.html`）＋ `.scratch/t832/scan-rendered.mjs`（同机审 ⑥ 判据扫渲染后文案面）；
③ 视觉模型＝390／1440 两档整页截图（`.scratch/t832/pages/备忘录同步.html` 的 fullPage 截图）；
④ DOM／CSS 读数＝`.scratch/t832/fmt.json`（三档 `touchSmall=0`、`minFontPxNoSvg=12`、`toc=1`）与 `t869-机审.mjs`（六列全 0）。

**同形说明（如实标注）**：本域只有 1 页，不存在族内同形外推；四列逐条取在本页的实读上。

---

## 逐页取值

### 备忘录同步

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0 ｜ 出处=静态 `.wrap{max-width:480px}`（L48）、`font-variant-numeric:tabular-nums`（L101／L133／L179）＋ 渲染后三档实测 ｜ 理由=四条判据逐条判：① 内容列收得住 —— 480px 有明确边界，1440 档两侧余量对称（收得住，只是偏窄；窄这一条落 `d4cut`，不落 D1）；② 数值列等宽数字 —— 四块指标卡的数（`.kpi-num`，L133）与状态卡 meta 行（L101）都带 `tabular-nums`；③ 缺值统一形状 —— 计数为 0 的卡照同一套形状渲染、只换灰阶与色条（`kpi-num.zero`），不是随机留白；④ 色只用令牌 —— 本票做了令牌收债：原先散在规则里的 7 个字面色值（状态卡四档底色、超小屏图标底、徽章底、桌面底色）已搬进 `:root` 令牌定义地（L32–L33），规则里只剩 `var()`，全页规则零字面色值
- d2 = 0 ｜ 出处=渲染后 整页两档（390／1440） ｜ 理由=四条判据逐条判：① 主角元素有视觉存在感 —— 首屏结论卡的图标＋「同步完成」22px／700，第一眼落在它，体量压过正文 13–14px；② 同级标题一致 —— 四块指标卡标签同 13px／500、三条明细摘要同 14.5px／600；③ 无一片灰小字 —— 最长的浅灰说明段是「数据与日志」的 40 字提示与明细卡的 24 字注，均未达 50 字阈值；④ 间距成 4／8 倍数 —— 卡片内距 16／14、栅格 gap 10、区块间距 14／10 皆为偶数档，视觉模型两档复核「节奏一致」
- d4cut = 3 ｜ 出处=静态 `.wrap{max-width:480px}`（L48）＋ 渲染后 1440 档两侧余量实测 ｜ 理由=三档零溢出、都算「收得住」，但 1440 档正文列固定 480px（占视口 33%，两侧各留约 480px 空白）⇒ 属基准 §4.2 ③「1440 档只多留空」的观感债，按人核位记 3（同 `t867-人核档.md` 里同族那一页的落法：同一份 `.wrap{max-width:480px}` 底座，同一判据、同一分数）

---

## 未取自本档的两列

- **H1／H2／H4**（分隔符串／内部标识符／符号顶替）：本页三条都由机器读数直接判 —— 分隔符门节点级与行级 0 处（`packages/base-render/test/separator-probe.mjs`）、`sep.json` 的 R7 标识符 0 处、机审 ⑤ 与 ⑤c 均 0 处。**如实记一处已收的债**：本票改前，页内脚本运行时拼出的文案里有半角逗号、`·` 并列与裸英文（`due`／`task`／`memo_id`），静态链看不见（`t867-读数链契约.md` §八 的已知缺口）——本票按机审 ⑥ 同款判据写了 `.scratch/t832/scan-template-text.mjs`（扫模板）与 `.scratch/t832/scan-rendered.mjs`（扫渲染后 DOM），两跑均 **0 处**；故 `english` 仍取静态候选 0，不需要走「改判」那一条路。
- **字号／触摸目标／跨宽溢出**：按 `t867-人核档.md` §一 第 1 条，机器能读的不进人核位 —— 字号 12px（`fmt.json` 的 `minFontPxNoSvg`）、三档 `touchSmall=0`、`OVERFLOW-ZERO` 三项都不在这里重复计价。
