## Question

把老骨架搬进仓库，成为 HELP 的唯一事实源（照 `#146`／`#188` 的做法）。

- **数据源**：老 `references/scenarios.yaml`（＋票 2 认定的第二事实源，若有），逐字搬，不改写。
- **形状**：三层 `groups[].subgroups[].scenes[]`，字段名逐字 `id／title／wake_word／status／prompt_template／types`；二级组 `id` 照记账通式 `<域id>_<序数>`（居家已实测该通式）。
- **生成物纪律**：若走「机器生成 ＋ `--check` 字节一致」的路（照 `#146`），生成器与产物都入仓并加摘要锁；**禁手改生成物**。
- **命名**：文件与公开接口名取自 HELP 下一级（子功能），目录名取自 HELP 一级分组——具体形状由票 5 报用户点头后定。
- **必报五步**：第一、二步已由票 5 做过那道门；交付时报第五步「交付对账」（第一步每一行都有着落、偏差为零）。

**并入票 3（`#223`）的实测**（逐字出处见 `t223-template-contract.md`）：

- 老 `groups[].subgroups[].id` 是 `memo_0`／`memo_1`…（**0 起**，与记账／居家 1 起**不同**）——沿用哪一套归票 6 裁，本票照裁完的口径落。
- 老 `editable_fields` 是 **76 条／29 场景**，且**不能逐字搬**（1 条布尔脏数据、22 条 ASCII label 回落、12 条是 CLI 开关 `html`）——进不进新 HELP 归票 6。
- `types` 的原子只有 **4 个**（回执 30／采集 20／查看 10／向导 4），**没有「选择」**；4 个全在模板配色表里（`:1698-1709`），模板零改动。
- 老载荷只有 **7 键**（无 `meta_blocks`）；老 `公共组件/injector.py:127` 的必填仅 `skill_name`／`title`／`groups`。

完成判据：仓内资产与老骨架逐字对得上（条数、名称、顺序），并与票 2 的对账表双向对齐；`--check` 类门为绿。

**并入票 2（`#222`）／票 1（`#221`）的实测**：

- **「照搬老骨架」的原料是两处**：老 `references/scenarios.yaml` ＋ 老 `SKILL.md` 的三张表（`:1076-1174` HELP 分支／`:147-172` 口语→CLI 反向表／`:226-292` HTML 生成对照表）——`:300` 载有 yaml 里没有的别名（`完成打卡`／`初始化`／`新手`）。第二事实源**判为算**。
- **一场景一唤醒词、别名进 `aliases`**：票 2 特别提醒——4 条情绪孪生（记／查／改／删情绪日记）**不要各占一行场景**。
- ⚠️ **HELP 自身的唤醒词不进场景目录**（用户 V1 定案）：「HELP HTML 中不包括 HELP 的唤醒词场景。用户说 备忘录HELP 就能拿到」⇒ 老 30 场景里没有 HELP 自己那条，资产里也不许新加；`aliases` 里同样不放 HELP 触发短语。
- ⚠️ **prompt 里不许出现命令硬编码**（用户 U6 备注立规，2026-09-12）：HELP 的 `prompt_template` **只出现唤醒词**，`memo.*` 这类命令名不许进页面——命令只住在 `SKILL.md` 的内部流程里。**本票要先查老 prompt 里有没有硬编码命令**：若有，逐条换成唤醒词说法，并在对账里记下改了哪几条。
- ⚠️ **`editable_fields` 进资产（清洗后）**（用户 V7 定案）：照老 76 条／29 场景，但**三类缺陷要清掉**——1 条布尔脏数据剔除、22 条补正确中文名、12 条 CLI 开关（`html`）不列。
- ⚠️ **不标缺失**（用户 U1／U2／U3 备注立规）：资产里**不要**为「新表暂无唤醒词」的老场景写缺失标记——HELP 呈现的是**最终完整体**，不是现状快照。
- ⚠️ **老侧还有「第三类」**（复核票 `#234` 实测）：**有唤醒词、有场景卡、有 CLI，却不在老 `SKILL.md` 的两张表里**——Init 类（首次使用）。⇒ **只按两表建别名表会漏掉它**；资产源头必须是「yaml ＋ 三张表 ＋ **21 条子命令**」三处合起来（子命令全集见 `t234-cli-inventory.md`；其中 `due`／`init-report` 两条**表里没有**）。
- **生成器纪律**：若照账单走生成器路，备忘录要**按 YAML 重写读取段与形状断言**（`packages/skill-bill/scripts/gen-wake-assets.mjs:29-30`／`:99-118`），产物 `wake-assets.ts` **禁手改**。
- 老 30 场景里**12 条无逐字落点**：哪些进 HELP、哪些留登记位（`status`）、哪些不列——照票 6 的裁决落。
- `editable_fields`：老备忘录场景多这一个字段（老 `memo_render.py:562-567`），共享模板**认它**（`help-template.html:1669`）——进不进由票 6 裁，进的话本票要落进资产。

## 进度：0%

下一步：等票 5（结构设计）＋票 6（内容与取值裁决）关票；票 2 已关。
