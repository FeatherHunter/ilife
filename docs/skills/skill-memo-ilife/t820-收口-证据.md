# #820 · 备忘录 34 页 HTML 视觉与文案优化 · 收口证据

**收口批次**：`packages/skill-memo-ilife/.scratch/t820-重测/验收-r11/`（墙、34 份产物、清单同目录 —— 视觉验收墙 §3.2 要求）
**源码锚点**：`a2af5cbf`（本轮全套优化的最后一次提交；收口时 `git status --porcelain -- packages/skill-memo-ilife` 为空，读数与产物同锚点）
**生成器（入仓）**：`docs/skills/skill-memo-ilife/t856-gen-wall.mjs`（墙＋总索引）、`t834-gen-batch.mjs`（34 格真跑）、`t834-发批.mjs`、`t856-gen-linkage.mjs`、`t856-gen-audit-list.mjs`、`t869-机审.mjs`、`t834-收口读数.mjs`
**形制**：`docs/agents/视觉验收墙.md`

---

## 一 生成器自检（正例 exit 0 ＋ 反例 exit 1 点名，§4 判据）

正例：

```
> node docs/skills/skill-memo-ilife/t856-gen-wall.mjs --check packages/skill-memo-ilife/.scratch/t820-重测/验收-r11
34 格；链接 174 条（手机墙-390.html 69 ＋ 桌面墙-1280.html 69 ＋ 总索引.html 36）；缺失 0 -> 可发
[exit 0]
```

反例（临时目录里删掉 `看提醒.html`，**不动交付批**；跑完即清理）：

```
> node docs/skills/skill-memo-ilife/t856-gen-wall.mjs --check <临时副本>
33 格；链接 174 条（手机墙-390.html 69 ＋ 桌面墙-1280.html 69 ＋ 总索引.html 36）；缺失 2 -> 不可发
  清单点名却没有文件：16 看提醒 -> 看提醒.html
  页上引用却落不到：看提醒.html
[exit 1]
```

两面对读：反例能红、且**逐条点名是哪一格哪一份**，说明这条自检不是恒绿装饰。

---

## 二 机器读数（本批实测）

| 门 | 命令 | 读数 |
|---|---|---|
| 五维尺 | `node docs/skills/skill-memo-ilife/t834-收口读数.mjs` | `均分=97.41 最低=96 最高=100`；`每维≥80% 的页=34/34`；`passPages=34`；`硬扣=0`；`RECONCILE 差=0`（238 项对账） |
| 五维逐维 | 同上 | d1 15/15、d2 20/20、d3 25/25、d4 15/15、d5 22.41/25 |
| 机审六列 | `t869-机审.mjs` | `34/34 PASS ①0 ②0 ③0 ④0 ⑤0 ⑥0 缺件 0` |
| 版式门（渲染面） | `t835-计算样式面.mjs` | 34 页 × 2 档：横向溢出 0、触摸区 <44px 0、最小字号 12px |
| 分隔符门 | `t834-收口读数.mjs`（sep） | `；`／`·`／≥3 段并列：节点级与行级全 0 |
| 屏上文字（渲染面） | `t835-屏上字符.mjs` | `;`／`|`／`·` 命中 **0** |
| 剪字与贴边（真 390／1280） | `node docs/skills/skill-memo-ilife/t820-剪字与贴边.mjs --dir <批目录> --widths 390,1280 --json 读数-r11/剪与贴边.json` | 34 页 × 2 档：**真剪字 0**（唯一命中的 `label.sr-only` 是无障碍标签、按设计裁到 1px）；**贴卡右缘 2 页**：`首次使用`／`首次使用-向导` 390 档，值列 `padding-right:0`、文字右缘距卡右缘 **1px** |
| 包内套件 | `node --test test/*.test.mjs` | 275/275 绿 |
| 清单完整性 | 本票复核 | rows=34，34 份产物全在；两张墙各 34 `<iframe>`、死链 0；总索引含「有意不出产物及其原因」一节 |

**22 页拿 96 分不是硬扣分**：是 D5 的 `toc` 腿各扣 4 分（`判分.mjs` 的 `D5W.toc`）。逐页点名（`读数-r11/facts.json`）：

- `tocEl=0` 的 22 页＝**19 张短回执页**（记／改／删备忘、记／改／删心愿、完成心愿、心愿排期、记／改／删打卡、记／改／删情绪、记提醒、设提醒、备忘改分类、备忘改子分类、备忘改分类-批量）＋ **3 张短向导页**（备忘改分类-批量-向导、完成心愿-向导、心愿排期-向导）。这些页没有页内目录，D5 得 21/25。
- `tocEl=1` 的 12 页＝9 张列表页（搜备忘、按时间搜备忘、查备忘、看备忘、查心愿、查打卡、查情绪、看提醒、查已提醒备忘）＋ 1 张报告页（备忘录同步）＋ 2 张首次使用页，D5 拿满 25。

族形状归公共层票，本票不改族（同族先例：`t831-mood-证据.md:84`、`t835-视觉终审.md:35`）。

### 新增实测：两处「贴边」（据实登记，本批不改）

`首次使用`／`首次使用-向导` 的「待办指引」表在 **390 档**下，值列文字右缘距卡片右缘 **1px**（该格 `padding-right:0`）；宽档 1280 同表 `padding-right:14px`，不出现。

- 改前（`验收-r3`）那一版更重：末字被边框**切掉**。本轮 `INIT_TABLE_WRAP_FIX` 补换行后，实测 **剪字 0**，字完整；剩下的是「没有右净空」这一档观感。
- 病根在**公共层** `ilife-block-data-table` 的窄档（行卡化那一段），不在备忘录这 34 页里；动它要动公共层、影响全仓 141 处调用点 ⇒ **本批不改**，留用户决定是否开票。
- 复核命令：`node docs/skills/skill-memo-ilife/t820-剪字与贴边.mjs --dir <批目录> --widths 390,1280 --json <落点>`

**量宽度的坑（本轮实测发现，写给下一批）**：本机 Chrome 把窗口宽**钳在 526px 下限**——`--window-size=390` 得到的 `window.innerWidth=526`，任何靠 `--window-size` 量的「390 档」都是假的。批内各门都走 `Emulation.setDeviceMetricsOverride`（`t835-计算样式面.mjs:196`、`t835-双档截图.mjs:113`），读数才是真 390；`t820-剪字与贴边.mjs` 同为 CDP 口径。

---

## 三 交付件

| 件 | 路径 |
|---|---|
| 手机墙 390 宽 × 3 列 | `packages/skill-memo-ilife/.scratch/t820-重测/验收-r11/手机墙-390.html` |
| 桌面墙 1280 宽 × 1 列 | `…/验收-r11/桌面墙-1280.html` |
| 总索引（按页族分组 ＋ 有意不出） | `…/验收-r11/总索引.html` |
| 链路总表（34 行七列，绝对路径点直达） | `…/验收-r11/链路总表.html` |
| 交付说明（读数 ＋ 改了什么 ＋ 未改的） | `…/验收-r11/交付说明.md` |
| 逐格缺陷清单 | `…/验收-r11/逐格缺陷清单-视觉审查.md` |
| 文案与设计审查（7,684 字逐页读） | `…/验收-r11/文案与设计审查.md` |

墙与产物**同目录**，iframe 走相对路径——**从该目录内打开**（双击即可，不起服务；无头浏览器长截图只画得出前 3 格 iframe，真浏览器不受影响）。

---

## 四 视觉验收墙 §8 收口自检清单

- [x] 本图动过 HTML／UI → 命中触发条件，本图带墙
- [x] ① 机检全过、② 机审六列 0 命中，才进墙
- [x] 双端各一张墙（390／1280），格数（34）与清单条目数（34）相等
- [x] 生成器自检正例 exit 0 ＋ 反例 exit 1 点名，都走过（§一）
- [x] 生成器未加 `loading="lazy"`（实测 0 处）；清单不含签名
- [x] 每格标签有编号、点开整页的链接、一句「这一格该确认什么：…」（实测 34/34）
- [x] 总索引页有「有意不出产物及其原因」一节（本批 34 格全部出产物 → 记「无有意不出」）
- [x] 逐格缺陷成清单（见交付目录两份审查；本轮 39 条，高 4／中 22／低 13，另 4 项标「未证」不当作缺陷）
- [x] 墙 ＋ 逐格缺陷清单 ＋ 自检退出码，三样一起交

---

## 五 负责人签字

> 「没有墙不签字，有墙也不等于签了字。」（§8）

- 看的哪两页：☐ `手机墙-390.html`（390 宽）　☐ `桌面墙-1280.html`（1280 宽）
- 签字：＿＿＿＿＿＿＿＿
- 日期：＿＿＿＿＿＿＿＿
- 结论（通过／打回，打回请逐格写「哪一格·什么症状」）：＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿
