# #855 开工前读数与实施规格（`src` 按 8 域重排）

**本件是什么**：本票的阶段闸门读数 ＋ 必报五步第一步（影响清单）与第二步（结构设计）的落盘件；
件级映射由脚本 `.scratch/t855/map-files.mjs` 从盘上实况生成（不手抄，可重跑）。

**此刻结论一句话**：**规格可实施，但此刻不能动工** —— 同工作区有 6 席域票正在**实时编辑**本票独占的框架位（`src/cli/cmd_read.ts` 等），
现树编译红（证据见 §五）。按 `docs/subagent-concurrency-protocol.md` §1／§2 与 `docs/agents/命令登记纪律.md` §二·形状二，
本票属「新增能力 ＋ 框架级变更」，**须先抢 `gate.lock` 并广播**、由编排者开串行窗口。

## 一 阶段闸门：本票现状

| 项 | 读数 |
|---|---|
| 认领 | 已认领（assignee＝FeatherHunter，认领即本席第一次写操作） |
| 标签 | `ready-for-agent`（**无** `needs-triage`）⇒ 不走 needs-triage 分支，诊断与实施同票 |
| 阻塞关系 | `Part of #820`；输入三票 #822（册子）／#823（本规格决议）／#824（视觉基准）**已全部关闭** |
| 进度区 | 本票正文原**无** `## 进度` 段、**无**评论、**无**实施记录 ⇒ 未动工（进度 0%） |
| 交付物（票面 Solution ＋ Implementation Decisions） | ① 8 个能力目录（每域最小三件）② 六个共用位按 ADR-0003 三类准入收敛 ③ 命令事实单一来源翻转（各域 `commands.ts` ＋ `routes.ts`）④ 三个超线件拆分 ⑤ 五道机器门分三段开 ⑥ 搬迁 6 批（每批编译绿 ＋ 产物字节不变） |
| 验收标准 | 票面 Solution ＋ User Stories 25 条；硬判据＝编译绿／产物字节不变／行数门与棘轮在位／无未登记命令 |

## 二 现状读数（实测，2026-09-21 13:02 本地）

- **件数**：`src/**/*.ts` 59 件 ＋ 包内 `scripts/*.mjs` 2 件 ＝ **61 件**。
- **超线（>350 告警线）3 件**：`src/cli/cmd_read.ts` ＝ **854**（本席开工前 5 分钟内由 779 → 854，仍在长）、`src/health.ts` ＝ 618、`scripts/gen-help-assets.mjs` ＝ 497。
- **工种名目录 5 个**（不合铁律四）：`fetch/`、`policy/`、`render/`、`cli/`、`help/`；**唯一达标的按域能力目录＝ `src/wish/`**（10 件）。
- **`t823-结构规格-草案.md` 那张 57 件表已陈化**：`src/help/booklet.ts`（#848）、`src/render/receipt.ts`／`templates/receipt.html`（#831 在途）、`src/init/*`（#833 在途）都是后加的，故本件的件级映射**重新从盘上生成**（61 件）。

## 三 目标树与六个共用位

```
src/
  index.ts                 包门（只许收窄）
  config.ts                ★ 边界：本图不碰
  memo/                    备忘域（记／改／删／批量／统计）
  search/                  查找域（搜／查／按时间搜／看备忘）
  remind/                  提醒域（读四视图 ＋ 写一条）
  wish/                    心愿域（已是达标样板）
  checkin/                 打卡域（3 场景，词面属本域）
  mood/                    情绪域（3 场景，词面属本域）
  sync/                    同步域（对账 ＋ 飞书链 ＋ 授权只读诊断）
  init/                    初始化域（首次使用，只渲染）
  shared/                  共用位（信封／路径／错误／批量／页资产／回执族／命令形状）
  render/                  框架位（html／templates／页族共用件）
  cli/                     框架位（readArgs／delivery／registry／health／config）
  help/                    HELP 渲染与资产（scenes/ 8 片不动）
  db/                      库连接只读层（禁 DDL）
  triggers/                唤醒词冻结表（只读）＋ routes.generated.ts（生成物）＋ 生成器
```

**每域最小三件**：`index.ts`（唯一一道门，只转出跨域真要用的）／`commands.ts`（**恰好一个声明数组**）／`routes.ts`（词→键，`order` 是顺序权威）；有页面的域另加自家页面件与装配件。
**共用与机器面按 ADR-0003 三类准入收进六位**：两家在用的放 `shared/`；出入口与派生件放 `cli/`／`render/`／`help/`／`triggers/`；三条都说不出的目录不许存在。
**单一来源翻转**：手写只两处（各域 `commands.ts` ＋ 各域 `routes.ts`）；`cli/registry.ts` 与 `triggers/routes.generated.ts` 均为**生成物**，人不手改；生成期同键两处声明直接抛，运行期查表再拦一道。

## 四 件级映射（61 件，机器生成）

件数: 61（src 59 ＋ scripts 2）
未映射: `src/init/diagnosis.ts`, `src/init/index.ts`, `src/init/page.ts`（#833 在途新建件，本票开工时按 init 域归位）, `scripts/build-help.mjs`（构建件，位置不动）
超线(>350): `src/cli/cmd_read.ts`=854／`src/health.ts`=618／`scripts/gen-help-assets.mjs`=497

| 现位置 | LF | 目标位置 | 理由 |
|---|---|---|---|
| `src/cli/cmd_read.ts` | 854 | src/cli/{readArgs,delivery,main}.ts ＋ 各域 commands.ts／run | 框架位拆三角色；域逻辑搬回各域 |
| `src/cli/config.ts` | 113 | src/cli/config.ts | 框架位：页外先拦的配置键 |
| `src/cli/health.ts` | 36 | src/cli/health/index.ts（聚合） | 框架位：体检聚合＋页外先拦 |
| `src/config.ts` | 154 | src/config.ts | ★ 边界：本图不碰（与 #760 的分界） |
| `src/fetch/auth.ts` | 19 | src/sync/auth.ts | 授权只读诊断属同步域 |
| `src/fetch/batch.ts` | 62 | src/memo/batch.ts | 批量改分类是备忘域的写面（域内取数） |
| `src/fetch/db.ts` | 333 | src/db/readonly.ts | 库连接属框架位（禁 DDL） |
| `src/fetch/errors.ts` | 25 | src/shared/errors.ts | 错误类型全域在用 |
| `src/fetch/feishu.ts` | 221 | src/sync/feishu.ts | 飞书链属同步域 |
| `src/fetch/index.ts` | 44 | （撤，改为各域门） | 工种名目录的转发门，各域有自己的门之后多余 |
| `src/fetch/paths.ts` | 100 | src/shared/paths.ts | 落点算式两域以上在用 |
| `src/fetch/reminders.ts` | 302 | src/remind/store.ts | 提醒行的存取属提醒域 |
| `src/fetch/sentinel.ts` | 175 | src/sync/sentinel.ts | 任务域自检属同步域 |
| `src/fetch/taskWrite.ts` | 75 | src/wish/taskWrite.ts | 飞书任务写属心愿域的远端侧 |
| `src/fetch/tasks.ts` | 76 | src/wish/tasks.ts | 飞书任务读属心愿域的远端侧 |
| `src/health.ts` | 618 | src/`<域>`/health.ts（按体检项归域）＋ src/cli/health/*.ts | 超线 618；体检项按域归属 |
| `src/help/booklet.ts` | 92 | src/help/booklet.ts | 册子唯一定义地（#848） |
| `src/help/helpFile.ts` | 220 | src/help/helpFile.ts | HELP 渲染与资产 |
| `src/help/index.ts` | 4 | src/help/index.ts | HELP 门 |
| `src/help/lookup.ts` | 56 | src/help/lookup.ts | HELP 速查 |
| `src/help/manifest.ts` | 50 | src/help/manifest.ts | 落点名与目录名的唯一定义地 |
| `src/help/sceneData.ts` | 64 | src/help/sceneData.ts | 场景资产组装件（生成器输入） |
| `src/help/scenes/*.ts`（8 片） | 71／44／123／73／90／131／43／103 | src/help/scenes/*.ts（8 片不动） | HELP 官方源：8 域的冻结词表与页型声明 |
| `src/index.ts` | 5 | src/index.ts | 包门（导出面按 #703 口径收窄） |
| `src/init/*.ts`（3 件，在途） | 133／40／242 | src/init/*（归位后不动内容） | #833 在途新建的 init 域件 |
| `src/policy/category.ts` | 32 | src/memo/category.ts | 顶层分类口径属备忘域 |
| `src/policy/crud.ts` | 35 | src/memo/crud.ts | 增删改参数校验属备忘域 |
| `src/policy/index.ts` | 17 | （撤，改为各域门） | 同上 |
| `src/policy/media.ts` | 90 | src/memo/media.ts | 附件路径口径属备忘域 |
| `src/policy/reminder.ts` | 90 | src/remind/policy.ts | 提醒时间／重复规则口径属提醒域 |
| `src/policy/wakewords.ts` | 73 | src/triggers/wakewords.ts（只读冻结）＋ 各域 routes.ts（声明） | 唤醒词表不再手写第二份；路由记录走生成物 |
| `src/policy/wish.ts` | 14 | src/wish/policy.ts | 心愿 op 口径属心愿域 |
| `src/render/envelope.ts` | 52 | src/shared/envelope.ts | 信封形状全域在用 |
| `src/render/errors.ts` | 11 | src/render/errors.ts | 渲染框架位 |
| `src/render/html.ts` | 50 | src/render/html.ts | 渲染框架位 |
| `src/render/index.ts` | 23 | src/render/index.ts | 渲染框架位的门 |
| `src/render/pageAssets.ts` | 74 | src/shared/pageAssets.ts | 页资产窄兼容对（自持 CSS 去向归 #824） |
| `src/render/pages.ts` | 261 | 按族拆：shared/queryPage.ts｜shared/wizardPage.ts｜shared/reportPage.ts | 同一件事一处定义：三形状各一份共用件，各域只给参数 |
| `src/render/receipt.ts` | 107 | src/shared/receipt.ts | 通用回执族共用件（19 格） |
| `src/render/templates.ts` | 27 | src/render/templates.ts | 渲染框架位 |
| `src/wish/*.ts`（10 件） | 78／61／269／23／12／17／153／20／63／134 | src/wish/*（原样保留） | 唯一达标的样板域 |
| `scripts/gen-help-assets.mjs` | 497 | 瘦身 ＋ scripts/help-assets.data.mjs | 超线 497；声明表抽成数据件 |
| `scripts/build-help.mjs` | 38 | scripts/build-help.mjs | 构建件，位置不动 |

**未映射项＝0**（`src/init/*` 三件与 `scripts/build-help.mjs` 已在上一句逐条交代归属）。

## 五 命令键 → 域（12 键 ＋ 1 交付键）

| 键 | 域 | 覆盖场景 |
|---|---|---|
| `memo.create` | memo | 记备忘／记提醒／记心愿／记打卡／记情绪 |
| `memo.update` | memo | 改备忘／改分类／改子分类／完成心愿／改心愿／改打卡／改情绪 |
| `memo.remove` | memo | 删备忘／删心愿／删打卡／删情绪（真删）＋ 废弃提醒 |
| `memo.batch` | memo | 批量改分类（向导） |
| `memo.stats` | memo | 统计（无唤醒词；处置归 #842） |
| `memo.search` | search | 搜备忘／查备忘／按时间搜备忘／查打卡／查情绪 |
| `memo.detail` | search | 看备忘（单条详情） |
| `memo.wish` | wish | 查心愿／心愿排期（列表 ＋ 两条向导） |
| `memo.remind` | remind | 看提醒／查已提醒备忘（四视图） |
| `memo.reminder` | remind | 设提醒（#850 新开，只 INSERT） |
| `memo.sync` | sync | 备忘录同步（对账报告） |
| `memo.auth` | sync | 飞书授权只读诊断（`status`／`diag`，无唤醒词） |
| `memo.init` | init | 首次使用（#850 新开，只渲染） |
| `memo.help.lookup` | help（框架位） | HELP 交付（开库前分派，不进各域门） |

**词面归属**：域票票面已把 30 个场景按 HELP 一级分组分到 8 个域；`记打卡`／`删打卡`／`改打卡`／`查打卡` 四个词住在 `src/checkin/routes.ts`，
`记情绪`／`删情绪`／`改情绪`／`查情绪` 住在 `src/mood/routes.ts`，它们指向的键（`memo.create`／`update`／`remove`／`search`）仍由键的属主域声明 ——
**「词住场景域、键住属主域」** 是本规格对卡路里「词随键走」的一处**刻意的不同**，理由是 8 张域票的写集互斥必须逐个域成立（照 `t820-图重设计-第一性.md` 的独占写集判据）。

## 六 五道机器门与搬迁 6 批

| 门 | 落点 | 段 |
|---|---|---|
| 命令登记生成器 | `scripts/gen-cli.mjs`（读各域 `commands.ts` → 出 `cli/registry.ts`／`cli/keys.ts`） | 段一 |
| 生成物一致性 | `pnpm gen`／`pnpm gen:check`（仓根接链，含陈旧守卫） | 段一 |
| 行数台账 | `scripts/check-warning-line.mjs` ＋ 本包 `AGENTS.md` 的台账块（350 ＋ LF） | 段二（超线拆完再冻结读数） |
| 棘轮 | `test/cmd-registry-855.test.mjs`（键集与定义地单源、老路容器缺席、分派层只许变短） | 段二 |
| 命令自治自证 | `docs/skills/skill-memo-ilife/t855-验收-命令自治.mjs`（`UNACCOUNTED` 必须为空） | 段三 |

搬迁 6 批（每批硬判据：**编译绿 ＋ 产物字节不变**，编译与重导走 `run-locked.mjs` 串行）：
**0** 空骨架 ＋ 生成器空跑 → **1** `wish/` 端到端（含分派器最小拆）→ **2** 框架全拆（`cmd_read.ts`／`health.ts`／`gen-help-assets.mjs`）→
**3** 大域各单批（`memo`／`search`／`remind`）→ **4** 小域合批（`checkin`＋`mood` 一批、`sync`＋`init` 一批）→ **5** 收尾（撤转发门、收窄包门、补台账）。

## 七 此刻为什么不能动工（并发阻塞，附证据）

**实测**（2026-09-21 13:02 本地）：

1. **同工作区 6 席域票在飞**：`.scratch/` 下 `wf826`／`memo-t827`／`tickets828`／`t829`／`t830`／`memo-831`／`t832`／`t833` 均为今日 12:5x–13:0x 的活动目录；
   `gate-runs.log` 末段显示 **ticket=832 于 05:01Z 正在编译 `packages/skill-memo-ilife`**、ticket=843 于 05:01Z 跑作息包测试。
2. **本票独占的框架位正被实时编辑**：`src/cli/cmd_read.ts`、`src/policy/wakewords.ts`、`src/render/**`、`test/wizard-pages-665.test.mjs`、`SKILL.md` 今日 12:58–13:01 均有未提交改动；
   `cmd_read.ts` 的 LF **在本席开工前 5 分钟内由 779 涨到 854**。
3. **现树编译红**（本席持锁实测，`runId=0666d3a4-3d08-4b5d-89ed-c71abc8ad768`，`GATE-RUN` 已落 `gate-runs.log`，`waitedMs=10001 exit=1`）：
   `packages/skill-memo-ilife/src/cli/cmd_read.ts(250,1): error TS1128: Declaration or statement expected.` —— 是某席**写了一半**的在途态，不是历史债。
4. **域票新建件已出现**：`src/init/{index,page,diagnosis}.ts`、`src/render/receipt.ts`、`templates/receipt.html`、`test/t832-sync-domain.test.mjs` 均为未跟踪新件。

**据此的判定**：本票的实施动作（`.git mv` 全树 ＋ 改写 `cmd_read.ts`／`policy/wakewords.ts`）会**同时**命中 §1 禁止的「改动他人路径」与 §3 禁止的破坏性 git 操作，
且 `docs/agents/命令登记纪律.md` §二·形状二把「新增能力 ＋ 框架级变更」明定为**先抢 `gate.lock` 并广播**的情形。
⇒ 按 `subagent-concurrency-protocol.md` §2.1 第 5 条（编译因缺件失败 → 停手上报编排者并保留现场），**本席停手上报，不自行改动他席在途件**。

## 八 待编排者裁定（不猜）

1. **串行窗口**：为 #855 开一段「本包 `src/**` 无其他写者」的窗口（各域席暂停并提交／回退其在途改动），本席持 `gate.lock` 按 6 批施工；或
2. **改派顺序**：先让在飞的域席（#831／#832／#833…）把手上的源提交到可编译态，本席再从批次 0 起做；或
3. **收窄本票**：只落**加法面**（`shared/commandSpec.ts`、`triggers/routeSpec.ts`、生成器与五道门、台账块），**不搬现有件**，把搬迁留给后续窗口。

**现场日志**（保留，供恢复用）：`.scratch/t855/baseline-compile.log`（编译原文）、`.scratch/t855/map.md`（件级映射读数）。

## 九 段一实施记录（2026-09-21，加法面，已入仓）

**提交 `e24bc6c0`**（7 件、＋532 行；只碰本票独占位与新增件，他席在途的 `cmd_read.ts`／`policy/`／`render/`／`init/` 一律未动）：

| 件 | 是什么 |
|---|---|
| `packages/skill-memo-ilife/src/shared/commandSpec.ts` | 命令声明的唯一定义地（`read`／`write`／`pre-open` 三判别式；写命令不写 `shape`） |
| `packages/skill-memo-ilife/src/triggers/routeSpec.ts` | 路由声明的唯一定义地（`order` 为顺序权威；冻结词表仍住 `help/scenes/*.ts`，不另立第二份） |
| `packages/skill-memo-ilife/scripts/gen-cli.mjs` | 生成器：源码文本 → `src/cli/keys.ts`／`src/cli/registry.ts`／`src/triggers/routes.generated.ts`，配 `--check` 逐件比对与逐条守卫 |
| `src/cli/keys.ts`／`src/cli/registry.ts`／`src/triggers/routes.generated.ts` | 三件生成物（空骨架态：域 0／命令 0／路由 0），随生成器一起入仓 |
| `package.json` | 仓根 `gen`／`gen:check` 接上 `skill-memo-ilife`（第六家） |

**机器读数**（`GATE-RUN` 逐条，runId 抄自 `.scratch/locks/gate-runs.log`）：

| runId | 命令 | 读数 |
|---|---|---|
| `30f734ae-c7f4-42af-8aa9-34ddf8b67fb5` | `pnpm gen:check` | `RESULT: 6/6`；memo 三件逐件 `GEN-CHECK ok` |
| `be862890-ea04-4383-a651-80d4b4b96adb` | `node .scratch/t855/verify-gates.mjs` | 门自证 `RESULT: 3/3`（正例绿／篡改 `keys.ts` 一行即红且点名／逐字还原复绿） |
| `dc3721b2-4259-4c80-a2c7-ed47d8e7556b` | `tsc -p .scratch/t855/tsconfig.check.json` | 新增件隔离类型检查 exit 0 |
| `4046ee00-5aef-4584-81b9-5116e38ed30e` | `tsc -b packages/skill-memo-ilife` | 整包编译 exit 0（他席在途件修好之后；本批未引入红） |
| `ef22d480-d1ba-4a00-83fe-98359fb25d56` | `node docs/skills/skill-memo-ilife/t855-产物基线.mjs --write` | 取基线：14 条用例（2451 ms） |
| `9c73c95d-d0ca-4f40-83a1-963542b5a2db` | `node docs/skills/skill-memo-ilife/t855-产物基线.mjs --check` | 确定性自证 `RESULT: 14/14`（复跑逐条一致） |

## 十 产物字节基线（6 批搬迁共用的硬判据）

**件**：`docs/skills/skill-memo-ilife/t855-产物基线.mjs`（可重跑）＋ `t855-产物基线.json`（基线）。

**判据三样**：出口退出码 ／ 产物**落点主体**（去掉同秒后缀）／ 产物正文的**归一化 sha256**。
归一化只动两类易变值（页面时间戳与当次临时根路径），其余逐字节比对——把「时间戳漂移」与「内容漂移」分开，免得每批都假红。

**口径**：唯一出口真跑（`dist/cli/cmd_read.js`），一条命令一个**全新临时库**（种子定死 `created_at`），家目录指临时目录、PATH 钉死飞书缺席；绝不碰活库与真家目录。

**迁移前基线（14 条）**：真出产物的 **5** 条 —— `memo.create` 17 013B／`memo.batch`（收集向导）22 069B／`memo.init` 80 017B／`memo.help.lookup`（全量）116 643B／`memo.help.lookup`（速查表）4 567B；
其余 9 条今天只回 envelope（`memo.search`／`memo.detail`／`memo.update`／`memo.stats`／`memo.remind`／`memo.reminder`／`memo.wish`／`memo.remove` 与一条时间区间查询）——**这批「零产物」正是 8 张域票要补的接线**，基线把它们的退出码与回执键固下来，免得搬迁把行为改掉。

**用法**：每一批搬迁之后跑 `--check`；`DIFF` 行逐条点名三面（退出码／落点／正文哈希），`RESULT: n/m` 是机器读数。

**基线不跨批冻结（实测教训，2026-09-21 13:25）**：复跑当场报
`DIFF 首次使用（初始化渲染）：落点 "〈临时根〉/memo_html/初始化报告_〈时间戳〉.html" → "…/首次使用_〈时间戳〉.html"` ——
这是 **#833 席按册子主体把 init 落点改名**（已提交），不是本席改的。⇒ 两件事同时成立：
① 本件**有鉴别力**（他席改行为它当场点名，不是橡皮章）；② 故口径定死为「**每一批搬迁的起点各取一次基线**」，
不跨批拿旧读数当判据。重取后 14/14 复绿。

## 十一 段二（超线件拆分）第一件：`src/health.ts` 618 → 17

**分件**（`cli/health` 只留聚合与页外先拦，实现按角色分开）：

| 件 | LF | 是什么 |
|---|---|---|
| `src/cli/health/configRead.ts` | 208 | 只读配置解析（文件不在时不落默认件；受限子集口径与配置件同一套） |
| `src/cli/health/probe.ts` | 185 | 机器面探针（目录在否／能不能写、模板件数、库表数、飞书 CLI 三档） |
| `src/cli/health/items.ts` | 240 | 九条体检项与报告组装（按域归位的下一步写在该件件头） |
| `src/cli/health/index.ts` | 17 | 聚合转出（报告构造＋报告类型＋表数门槛＋只读解析） |
| `src/cli/health.ts` | 37 | **页外先拦**（`memo.config.check` 在库目录预检之前拦下），import 改指 `./health/index.js` |
| `src/health.ts` | **17** | **跨包兼容口**：`dist/health.js` 仍导出 `buildMemoHealthReport` 等原名（面板侧按这条老路取报告，包门只许收窄） |

**读数**：`tsc -b packages/skill-memo-ilife` exit 0（runId `bc84e121-6dc7-49f6-b35d-f4d49c165f62`）；
`memo.config.check` 真跑 exit 0、`shape=detail`、**9 条体检项**逐条在场；产物基线 14/14（runId `de18d8bc-9518-4dbd-8346-641d196adbdc`）。
**超线件 3 → 2**（余 `src/cli/cmd_read.ts` 与 `scripts/gen-help-assets.mjs`）。

**归因（本批未碰他席在途件）**：本批只动 `src/health.ts`／`src/cli/health.ts`／新增 `src/cli/health/**`；
包内测试全跑 176 例、红 14 例**全部与体检无关**，逐条归因如下（都是他票遗留，不是本批引入）：
卡路里两例（`#81 唤醒词路由层`／`calorie triggers parity`）；`15 对 key×shape` 红在 `未知 registry key：memo.init`
（combos 注册表未补 `#850` 新键）；`memo 十键取数全通`／`读键全链路`／`#659 T7` 三例红在 **`lark.cliPath` 已随 #760 退役**
而三个老测试仍按那条已删的配置键注入假 lark（远端侧进不去 ⇒ `memo.sync` exit 4）。

## 十二 段二第二件：`scripts/gen-help-assets.mjs` 497 → 304（声明表抽数据件）

**三件分工**（一件一件事）：

| 件 | LF | 是什么 |
|---|---|---|
| `scripts/help-assets.data.mjs` | 125 | 三枚摘要锁 ＋ 五张声明表（`PROMPT_EDITS`／`TEXT_EDITS`／三类禁令词／`DROP_FIELDS`＋`LABEL_FIX`＋`BOOL_FIELD`／`ALIASES`）；纯字面量、零 import |
| `scripts/help-assets.render.mjs` | 99 | 三个渲染件（`header` 文件头注释／`renderDomain` 域文件／`renderSceneData` 组装件）；事实源文件名、版本、两枚摘要、两个落点由 `ctx` 传进来 |
| `scripts/gen-help-assets.mjs` | **304** | 读事实源／两地交叉复核／清洗／形状断言／跑 main |

**切法**：**按行切片 ＋ 逐条边界断言**（脚本 `.scratch/t855/split-help-assets.mjs`，可重跑）——
两处边界与当刻文件不符时**当场停、不落盘**（§2.5 第 1 条：断言是前置条件，不是事后报表），修准后再切。

**判据（产物字节不变）**：`node scripts/gen-help-assets.mjs --check` 需仓外两处事实源在盘（本机在）——
实测 **exit 0**、`OK：9 个文件与生成结果字节一致`（runId `d6c847ea-d634-42f6-9b32-d2cd72c2f71d`），
且三枚摘要锁（老 30 条 `0aa8c228…`／清洗后 `444f645…`）、形状读数（域 8／二级组 13／场景 30／字段 64／别名 12）
与清洗计数（34 处等）逐条与切分前一致。

**超线件 3 → 1**（余 `src/cli/cmd_read.ts`，其拆法依赖串行窗口）。

## 十三 搬迁切片 3：提醒域端到端（读四视图＋写一条）

**五件分工**（一件一件事）：

| 件 | LF | 是什么 |
|---|---|---|
| `src/remind/run.ts` | 123 | 运行件：`memo.remind` 四视图（到期／已完成／有效／已废弃）＋ `memo.reminder` 只 INSERT（`note_id` 可选、给了校验存在、不给即独立提醒）；写参数四件解析随命令一起搬 |
| `src/remind/commands.ts` | 27 | 命令声明：一读（`memo.remind` list）一写（`memo.reminder` 写命令不写 `shape`） |
| `src/remind/routes.ts` | 30 | 路由声明：提醒类 3 词（orders 8–10） |
| `src/remind/index.ts` | 9 | 能力门：只转出 `REMIND_COMMANDS` |
| `src/shared/exit.ts` | 11 | 失败约定 `fail` 的唯一定义地（第二个用法出现 ⇒ 提到共用位） |

`src/cli/cmd_read.ts` 删两条 case 留一行注记、搬走写参数四件解析、清掉随搬无用的 import。

**读数**：`scripts/gen-cli.mjs` 写盘 exit 0（runId `c847ead6-e7c0-46de-9f52-1056eaaff3fe`，域 3 个、命令 5 条、路由 11 条）；
`tsc -b` exit 0（runId `d3ee5c46-2ced-4109-bf4f-40db072b32cd`）；
`pnpm gen:check` exit 0、`RESULT: 6/6`（runId `f28d2e39-91df-4aba-bab3-76e38d6a6eee`）；
**产物字节基线 `--check` `RESULT: 14/14`**（runId `c6fe8753-9e95-477d-bcad-e7b518cb4591`）；
`cmd-850-four-questions` 16/16（runId `83572a70-3559-4ac3-ad6e-09f6b5dc1f09`）、
`policy.test.mjs` 7/7（runId `b3f6cbc5-62b9-4d90-96ec-bbc97e92025c`）、
`cli-help-230.test.mjs` 6/6（runId `79176d4d-80b4-4368-8c33-666f0eaa5c14`）。

提交 `3cb65dbd`（8 件、＋191／−78）。

## 十四 搬迁切片 4：备忘域端到端（五命令）＋ 打卡／情绪／心愿词面补齐

**八件分工**：

| 件 | LF | 是什么 |
|---|---|---|
| `src/memo/run.ts` | 335 | 运行件：create／update／remove／batch／stats 五命令逐字搬运 ＋ 七件辅助（`asIds`／`noteIdOfMessage`／`receiptOptsOf`／`buildReceipt`／`delete*Of` 三件） |
| `src/memo/commands.ts` | 48 | 命令声明：四写一读（`memo.stats` 读、无唤醒词） |
| `src/memo/routes.ts` | 60 | 路由声明：备忘类 6 场景词（orders 11–16） |
| `src/memo/index.ts` | 8 | 能力门：只转出 `MEMO_COMMANDS` |
| `src/wish/routes.ts` | ＋4 行 | 心愿类后 4 词（orders 17–20；键属备忘域，词随场景住本件） |
| `src/checkin/routes.ts` | 32 | 打卡类 3 词（orders 21–23）；本域无自己的键，词面就是全部交付 |
| `src/mood/routes.ts` | 32 | 情绪类 3 词（orders 24–26）；同上 |
| `src/shared/commandSpec.ts` | 改 1 处 | `CommandOut.data` 放宽到 `unknown`（与出口旧形同） |

`src/cli/cmd_read.ts` 删五条 case＋七件辅助（469 行）；清掉随搬无用的 import 整行。
过程里两道守卫生效：同词同件两格（备忘改分类）促生成器把「同词两处」收窄为「整组同迁、只拦跨件」；
字面量解析器放开**裸标识符**（`run:` 引用是不透明值）。

**读数**：写盘 exit 0（runId `9f267df6-9f75-4450-8c3a-69e032519924`，**域 6 个、命令 10 条、路由 27 条**）；
`tsc -b` exit 0（runId `0dac52d8-2f48-4c7d-afc4-018ed8a72c17`）；
`pnpm gen:check` exit 0、`RESULT: 6/6`（runId `5189792f-92b4-4872-970d-7ba5fb6bd0da`）；
**产物字节基线 `--check` `RESULT: 14/14`**（runId `283083e8-7e93-4ffb-b6dd-c1910f933000`）；
包内全量 176 例 162 绿／红 14 例与片前逐条一致（runId `2c723280-46f3-4691-b7bb-a4e0488f7976`）；
反例 3/3（runId `ea089187-62fb-4d66-8f41-524e1ce328d7`，撤 `memo/commands.ts` 即红并点名）。

提交 `dc89c2a8`。





