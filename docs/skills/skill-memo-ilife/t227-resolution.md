## 关票决议（2026-09-12）

**结论：内容资产逐字忠实（零未声明偏差），页面可见黑话已清尽；报告有一处数字滞后，已在此订正。**

### 两份对抗式复审的评分

| 复审 | 评分 | 结论 |
|---|---|---|
| **H**（逐字忠实打假） | — | **资产：零未声明偏差。** 它用**自写解析器**从老实物载荷独立提取 30 条、**自己重写了一遍** `memo_render.py:551-560` 的转换层（先按 `category` 分域、再按 `subfunction` 收组），**逐条复现载荷序** ⇒ **裁决 22 独立证实**。它独立复算 **41 处**偏差、全部可归因到声明的类别；`editable_fields` 用「label===name」独立扫出 21 条、减 12 条 `html` 正好剩那 9 条；三把摘要锁**真 fail-closed**（`%TEMP%` 副本改 1 字符／置 `FILL_` 均抛错）；变异自证它用**备份还原**（非生成器重写）在 `sync.ts` 重做、三方哈希一致。**它明确写：「我一条未声明偏差都没找到。」** |
| **I**（用户硬规矩＋仓规） | **82/100**（三条硬规矩 **100**／仓规合规 68／生成器纪律 95／报告诚实度 64） | 三条硬规矩**机器可验通过且有门**；裁决 16＝**49**（自己 import `dist` 数的）；**三把摘要锁它用自己的实现独立复算全部对上**；`--check` 跑前后 mtime+sha 9/9 不变；变异测试在**仓外副本**重做通过；D2 的 12 处真落且逐条声明。**它打出的实质缺口＝18 格用户可见实现细节字面**，本票已按它的逐条改法清尽。 |

### 最终清洗量（**以此为准**；报告 §3.1 那行的数字已滞后，见下）

生成器 `--check` 现行输出：

```
形状：域 8／二级组 13（兜底 4）／场景 30／字段 64（含字段场景 27）／别名 12
原子：{"采集":20,"回执":30,"向导":4,"查看":10}
清洗：prompt 改动 34 处（CLI 9 ／ DB 12 ／ IMPL 13）／title 3 ／label 1 ／hint 9／剔除字段 12 条／牵动场景 19 个
摘要锁：老 30 条 0aa8c228…；清洗后 30 条 7c72fb92…
```

**编排会话实测复核**：18 个黑话词在产物上 **全部 0 处**；`待开发` **0 处**；`memo.`／`--html`／`无唤醒词` **0 处**；与已被维护者终审过的卡路里产物**模板指纹逐项同形**。

### ⚠️ 已知勘误（本席记录，不再回改）

- 报告 `t227-assets-report.md:82` 写「prompt 改动 **24 处（CLI 9／DB 12／IMPL 3）**／牵动场景 16」——**那是清理 18 格之前的数**。**正确值是 34 处（CLI 9／DB 12／IMPL 13）／牵动场景 19**（见上）。
- 报告 `:207` 写「IMPL 类 **3 处**」——**正确值是 13 处**（D5 的 4 处 title/hint ＋ D7 的 3 片段 ＋ 复审员 I 点出的 18 格中的 6 格实现词，去重后 13）。
- **教训（复审员 H 的「必须改」第 1 条，本席采纳为规矩）**：「改生成器声明表 → 重跑 → 产物变更」**必须同批更新报告并重贴原始输出**，否则 `--check` 绿了也不算交付。本票的两次报告滞后（36→41、24→34）都是同一个根因。

### 验收判据的措辞已订正（复审员 H 的「必须改」第 2 条）

原判据「与老骨架**条数、名称、顺序逐字**对得上」**对 `title` 已不成立**（按裁决 21 D5／裁决 23 授权有换说法）。**已订正并写入 `#233` 票面**：

> **条数／顺序逐字**（30 场景／8 域／13 二级组／64 字段／12 别名）＋ **名称／可见文案在「声明五／六」那批换说法之后逐字**（其余 23 个 `title`／多数 `prompt` 逐字未动）。

否则 `#233` 的肉眼终审会照旧判红。

### 边界与纪律

只改 `src/help/scenes/*.ts`／`sceneData.ts`／`scripts/gen-help-assets.mjs`／本票报告与票面；**未碰**别人家的 `test/render.test.mjs`／`docs/memo-migration-split.md`；**未跑仓根 `tsc -b`／`pnpm -r build`**；**未重启／未杀 GUI**；**未动暂存区**（本图新规：暂存与提交由编排会话独占）；仓库根 `.scratch-*` **已两次清空、末态为 0**；HEAD 未动（`3da2559`），**未 commit**。
