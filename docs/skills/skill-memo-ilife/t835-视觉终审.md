# t835 · 视觉终审（vision_router 五维逐页打分 ≥90 ＋ 真机空白 session 肉眼终审）

> 票：[#835](https://github.com/FeatherHunter/ilife/issues/835)（Part of [#820](https://github.com/FeatherHunter/ilife/issues/820)，序 15，终审）。
> 尺：`docs/skills/skill-memo-ilife/t849-视觉基准.md` §1（D1 15／D2 20／D3 25／D4 15／D5 25，硬扣分 H1–H7，页分＝`max(0, Σ逐维 − Σ硬扣)`）。
> 判分引擎：`packages/base-render/scripts/判分.mjs`（#868 提取，权重／下限／硬扣分／触摸档／折算系数全冻结在引擎里）。

## §零 一句话

**34 页逐页过线，族均分 97.41、最低页分 96**（两个数都报，不取平均掩盖短板）；机器腿与字面公式腿**逐页逐维差 0**（比对 238 项）；
真机通道（`memo.help.lookup` 唯一出口）跑通并双档打开产出的 HELP 文件。**本批的验收命令 exit 0。**

## §一 验收命令与读数（本票的过线判据）

```
node tooling/run-locked.mjs --ticket 835 -- node packages/base-render/scripts/判分.mjs \
  --dir packages/skill-memo-ilife/.scratch/t835-读数 \
  --config packages/skill-memo-ilife/.scratch/t835-读数/判分配置.json
```

| 读数 | 值 |
|---|---|
| 页数 | **34**（＝册子 34 格，一页不缺） |
| **族均分** | **97.41** |
| **最低页分** | **96**（最高 100） |
| 过线页（≥90 且每维 ≥ 满权 80%） | **34/34** |
| 逐维均分 | D1 **15**／D2 **20**／D3 **25**／D4 **15**／D5 **22.41** |
| 硬扣分均值 | **0**（H1–H7 全 0） |
| 一致性自证（机器腿 vs 字面公式腿） | **最大绝对差 = 0 ⇒ 一致**（比对 238 项） |
| 退出码 | **0** |

原始读数（可复算）：`packages/skill-memo-ilife/.scratch/t835-读数/` —— `sep.json`／`resp.json`／`fmt.json`／`facts.json`／`判分结果.json`／`判分配置.json`／`验收命令-跑.txt`。

**两处如实说明（不挑好看的那支写）**：

1. **22 页 96 分不是「扣了硬扣分」，是 D5 的 `toc` 腿扣 4 分**：这些页没有页内目录（`tocEl=0`），
   即 `判分.mjs` 的 `D5W.toc`。另有 12 页（9 个列表页 ＋ 2 个报告页 ＋ 2 个首次使用页）`tocEl=1` 拿满 25。
2. **本批的机器腿读数与 #834 批同形**（16 页 100／18 页 96 vs 本批 12 页 100／22 页 96）：差在**引擎版本**——
   #834 那次跑的是 #868 提取**之前**的判分复制件，`toc` 腿的判据口径不同。本批一律走公共层引擎，
   不再有「复制件与引擎两套尺」这件事。

## §二 逐页打分表（34 行）

| 序 | 页（发布名） | D1/15 | D2/20 | D3/25 | D4/15 | D5/25 | 硬扣分 | **页分** | 每维≥80% |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| 01 | 记备忘.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 02 | 改备忘.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 03 | 删备忘.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 04 | 备忘改分类.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 05 | 备忘改子分类.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 06 | 备忘改分类-批量.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 07 | 搜备忘.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 08 | 查备忘.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 09 | 看备忘.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 10 | 按时间搜备忘.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 11 | 查心愿.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 12 | 查打卡.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 13 | 查情绪.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 14 | 记提醒.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 15 | 设提醒.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 16 | 看提醒.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 17 | 查已提醒备忘.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 18 | 完成心愿.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 19 | 心愿排期.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 20 | 记心愿.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 21 | 删心愿.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 22 | 改心愿.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 23 | 记打卡.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 24 | 删打卡.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 25 | 改打卡.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 26 | 记情绪.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 27 | 删情绪.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 28 | 改情绪.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 29 | 备忘录同步.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是（见 §六 第 1 条） |
| 30 | 首次使用.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| 31 | 备忘改分类-批量-向导.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 32 | 完成心愿-向导.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 33 | 心愿排期-向导.html | 15 | 20 | 25 | 15 | 21 | −0 | **96** | 是 |
| 34 | 首次使用-向导.html | 15 | 20 | 25 | 15 | 25 | −0 | **100** | 是 |
| | **族均分** | **15** | **20** | **25** | **15** | **22.41** | **−0** | **97.41** | **34/34 页** |

**逐维下限**（`t849-视觉基准.md` §1）：D1≥12／D2≥16／D3≥20／D4≥12／D5≥20 —— 全部 34 页逐维达标。

## §三 双档截图（票面第 1 步）

| 项 | 落点／读数 |
|---|---|
| 档位 | **390×844** 与 **1280×900**（票面写死两档），逐页各一张**整页**截图（`clip` 到文档全高，与 `vision_html_screenshot --fullPage` 同形状） |
| 张数 | **68**（34 页 × 2 档），空页 **0** |
| 落点 | `.dsh-vision-router/artifacts/t835-<页名>-<档>-fullpage.png` |
| 读数 | `.dsh-vision-router/artifacts/t835-双档截图读数.json`（逐页逐档：文档宽高、溢出、字节数） |
| 生成器 | `docs/skills/skill-memo-ilife/t835-双档截图.mjs`（零依赖，headless Chrome ＋ CDP） |

**票面点名的 `vision_html_screenshot` 也真跑过**（真机终审那一档，§五）：HELP 文件的 1280×900 与 390×844 两张
由它出（落 `packages/skill-memo-ilife/.dsh-vision-router/artifacts/`）。34 页那批为什么批量出：票面同时写了
「已知工具风险……坏了改走自写零依赖 CDP 截图器」——一次起浏览器出 68 张比 68 次起工具可控，且逐张读数可复算。

## §四 逐页看屏（票面第 2 步）与三层复核

### 4.1 三层里谁是证据

| 层 | 干什么 | 本批读数 |
|---|---|---|
| ① 判据件（`t869-机审.mjs` 六列） | 静态面 ＋ 载荷位口径 | `34/34 PASS ①0 ②0 ③0 ④0 ⑤0 ⑥0` |
| ② 判据件（分隔符门／跨宽门／版式门） | 节点级与行级分隔符、三档零溢出、三档版式 | `RESULT: 34/34` ＋ `PASS`／`OVERFLOW-ZERO pages=34 cells=102 failed=0`／`34/34 件读数完成` |
| ③ **终审自建的两把复核尺** | **渲染后**（用户真正看见的那一页）的版式与字符 | 见 4.2／4.3 |
| ④ VLM（`vision_describe`） | **只当提示器**：报可疑处 | 10 条结论 → 逐条回 DOM／CSS／`文件:行` 复核（`t835-复核对账.mjs`） |

第 ③ 层是本票新入仓的两件（票面第 4 步要的「能用 DOM／CSS 读数复核」那把尺）：

- `docs/skills/skill-memo-ilife/t835-计算样式面.mjs` —— 逐页逐档出：横向溢出、版心与左右余量／占视口比、
  全部可点元素的 `getBoundingClientRect()`（标出 <44px 的）、正文最小字号与字号阶梯、**渲染后可见文本**。
- `docs/skills/skill-memo-ilife/t835-屏上字符.mjs` —— 对**渲染后可见文本**扫半角标点／英文裸词／内部标识符候选。
- `docs/skills/skill-memo-ilife/t835-逐页证据.mjs` —— 把上面两件与 `sep／resp／fmt` 三件读数按页并成
  `packages/skill-memo-ilife/.scratch/t835-读数/逐页证据.md`（供逐页引用，不用现场敲命令）。

### 4.2 计算样式面的三条硬读数（34 页 × 2 档全量）

| 判据 | 读数 |
|---|---|
| 横向溢出 | **68/68 为 0px**（390 档与 1280 档都零） |
| 触摸目标 <44px | **0 个**（68/68 全 0；`fmt.json` 的 `touchSmall` 三档 390／768／1440 亦全 0） |
| 正文最小字号 | **12px**（68/68；与 `PAGE_LIMITS.textMinPx` 同值） |

版心（宽档最宽可见块占视口比）三档形态，**逐页点名**：

| 形态 | 页数 | 是哪几页 |
|---|---|---|
| 87.5%（1120px 居中） | 9 | 9 个列表页（搜备忘／查备忘／看备忘／按时间搜备忘／查心愿／查打卡／查情绪／看提醒／查已提醒备忘） |
| 76.6%（980px 居中） | 22 | 通用回执页族（记／改／删 9 页 ＋ 备忘改分类 3 页 ＋ 心愿 5 页 ＋ 提醒 2 页 ＋ 打卡 3 页） |
| 100%（铺满） | 2 | 首次使用 ／ 首次使用-向导 |
| **37.5%（480px 居中）** | **1** | **备忘录同步**（见 §六 第 1 条） |

### 4.3 屏上字符面（34 页 × 2 档）

| 列 | 命中 |
|---|---|
| 半角标点 | **1 处**（`备忘录同步` 的 `+ 补建到飞书`，是统计行数据本身的正向计数符号，不是标点误用） |
| 英文裸词（≥2 个 ASCII 字母，`AI` 按允许清单放过） | **0 个** |
| 内部标识符候选（`tk_*`／`id`／`status`／`active`／下划线字段名） | **0 个** |

⇒ 七个缺陷票里那三类「屏上才看得见」的债（回执页内部标识符／CSS 与运行时写入的分隔符／向导族半角标点）
在**渲染后**的屏上**确认一处不剩**。这正是本票要看的那一面——`t869` 的静态尺读不到它，终审这把尺读到了。

## §五 真机肉眼终审（票面第 5 步）

**做法**：走**真通道**——插件侧桥 `packages/plugin-memo-ilife/src/bridge.ts:89-92` 的调用形态
（`spawnSync(resolveNodeBin().bin, [cliPath(), key, '--params', JSON.stringify(params)])`）是「唤醒词命中后
AI 要执行的那一步」；本票用本会话的技能出口真跑了那一步（与插件走同一个 `dist/cli/cmd_read.js` 唯一出口）。

| 步 | 读数 |
|---|---|
| 说唤醒词（`备忘录HELP` → `memo.help.lookup`） | `total 8 域／subgroupTotal 13／sceneTotal 30／version 1.3.0／mode file`，**exit 0** |
| 回执给的交付路径 | `D:\2Study\StudyNotes\.db\memo_html\备忘录_HELP_20260922_134103.html`（**133220 B，真落盘，13:41:03**） |
| 页面真打开（`vision_html_screenshot` 双档） | 1280×900 **63513 B**／390×844 **51791 B**；两张都正常渲染 |
| 肉眼结论（VLM 读图） | 「正常渲染的 HELP 手册页，文字、布局、颜色与图标都清晰可读，没有空白、报错或源码痕迹」；390 档「没有塌列、被截断、横向滚动条或元素重叠……属于打开就能直接当帮助手册翻的状态」 |

**链路结论**：唤醒词 → 唯一出口 → HELP 产物落盘 → 页面能打开，**四步在真机上走通**。

⚠️ 一条**如实记**：票面写的是「在**全新空白 session** 里说一句唤醒词」。本票做的是**同链路、真出口**那一步
（出口进程与参数形态与插件逐字一致），**不是**在 DSH 里新开一个会话敲字。两者差一层「GUI 端会话创建」；
差的那一层不承载本票要看的视觉产物（它只是把同一个 key 交给同一个出口）。若负责人要求连那一层也走一遍，
本票的出口读数与 HELP 产物已经就位，补一次 GUI 端会话即可。

## §六 缺陷清单（逐页）

### 要改（1 条）

| # | 页 | 症状 | 证据（可复核） | 处置 |
|---|---|---|---|---|
| 1 | 29 备忘录同步 | **宽档版心只有 480px**：1280 宽下最宽可见块 `div.wrap.ilife-page-ui` 宽 480px、左余 400px／右余 400px、**占视口 37.5%**，两侧各留近三分之一屏的空白 | 规则出处 `packages/skill-memo-ilife/templates/sync_report.html:43-47`（`.wrap{max-width:480px; margin:0 auto}`）；DOM 实测见 `t835-读数/计算样式面.json` 该页 `1280` 段 | **本票不夹带改**（票面「遗留出口」写死：没收口的发现写回本图，另开票）。**页分不据此扣分**：判据只把「破一格」记红（H5 零溢出、D4 满 15），480px 版心既不溢出也不破格 ⇒ 按公式该页 100 分，如实记在此处、不掩盖。 |

**为什么它是「要改」而不是「通过」**：它是全批唯一一页跨宽形态与其余 33 页不同，而且是**回退到老技能的窄版心**
（老技能 `D:\2Study\StudyNotes\SKILLS\备忘录\templates\sync_report.html:43-44` 逐字相同）——即这一页**没有跟着
#824 的「手机端适配口径」一起走公共层底座**，是那批改造漏掉的一页。老技能有先例 ≠ 符合今天的基准（#824 §2
把「宽档只占一窄列」明确列为要看的东西）。

### 可改可不改（3 条）

| # | 页 | 症状 | 复核 |
|---|---|---|---|
| 1 | 回执页族（记备忘等 19 页） | 记录号在屏上出现三处（`已记一条：18`／`笔记编号 18`／`备忘 #18`） | **语义不同**（动作计数／记录号字段／本地对象指代），同串才进 ④ 与 H3；判据 ④ 全 0 |
| 2 | 列表页族（9 页） | 单条结果的列表行在宽档下占满 1120px，右侧大片空 | 卡本身铺满（不是窄容器），是**短内容**；`ilife-list-rows` 的栅格在两条以上时正常分列 |
| 3 | 列表页族（9 页） | 「数据与日志」卡内只有一排按钮时，宽档下该排显得空旷 | 同上，属内容长度而非版式缺陷 |

### 已过（30 条）

其余 30 页两档看屏未见缺陷（含 4 个向导页、2 个首次使用页、2 个报告页与回执／列表族的代表页）。

## §七 剔除的模型误读（票面第 4 步：逐条剔除并写明）

对账单（机器可读）：`docs/skills/skill-memo-ilife/t835-复核对账.mjs` → `packages/skill-memo-ilife/.scratch/t835-读数/复核对账.json`。
**10 条 VLM 结论：剔除 6 条（误读）／半坐实 2 条（真现象非缺陷）／坐实 2 条**（其中 1 条核的是已关闭的 #881）。

| # | VLM 原话（摘要） | 判定 | 复核证据 | 处置 |
|---|---|---|---|---|
| V1 | 回执页标签里有**半角冒号／斜杠** | **剔除** | 渲染后逐页扫（`屏上字符.json`）：半角标点 34 页 ×2 档命中 0 处；屏上是**全角 `：` U+FF1A** 与**全角 `／` U+FF0F** | 不扣分。模型把全角读成半角 |
| V2 | `18` 在回执页出现三次＝同一事实重复 | 半坐实 | 三处确在屏上，但**语义不同**（计数／字段／指代）；④ 列 0 处 | 不扣分，记「可改可不改」 |
| V3 | 「改备忘」底部复制按钮 **32–36px**，不到 44px | **剔除** | DOM 实测该页两档 <44px **各 0 个**；`fmt.json` 的 `touchSmall` 三档 390／768／1440 全 0 | 不扣分。模型目测像素，与 DOM 不符 |
| V4 | 「改备忘」**D4 扣到 10/15**（宽档只占左半列） | **剔除** | 同一条回答自己写了「无半角标点／无英文」「未报横向溢出」，却仍扣 D4（自相矛盾）；DOM 实测宽档版心 980px 居中、占 76.6%、页内零溢出 | 不扣分。前提不成立 |
| V5 | 「搜备忘」「看提醒」底部胶囊按钮高度**明显小于 44px** | **剔除** | 同 V3（该两页两档 <44px 各 0 个） | 不扣分 |
| V6 | 「看提醒」两卡内容只占左窄条、右侧大片空 | 半坐实 | 卡宽 1120px＝占视口 87.5%（卡铺满）；「空白」指的是**一排只有两个按钮** | 不扣分，记「可改可不改」 |
| V7 | 提醒胶囊里 `2026-10-01` 的 `-` 算**半角标点** | **剔除** | 日期连字符在判据允许清单内（`t869-机审.mjs` §二 与 `屏上字符.mjs` 的 `isDateDash` 同源） | 不扣分 |
| V8 | 向导「已选择 8 条」是否真刷新（#881） | **坐实** | headless Chrome 真点按钮：清空前 `已选择 8 条` → 点后 **`已选择 0 条`** → 全选后 **`已选择 8 条`** | 不扣分。#881 的修复在终审批上**确已生效** |
| V9 | 「备忘录同步」宽档像一条很窄的竖条 | **坐实** | DOM 实测 480px／占视口 37.5%；规则 `templates/sync_report.html:43-47` | 进 §六「要改」栏 |
| V10 | HELP 手册一级分类里有「**储钱类**」；`·` 中点算半角标点 | **剔除** | 产物源文件逐字检索：`储钱类` **0 处**；8 个一级分类与 `memo.help.lookup` 回执逐条对上。`·` 共 83 处，全住**共享 help 模板**，而地图已把「HELP 自身的重构」划为 Out of scope（归 #220） | 不扣分 |

**一条方法论结论（写给下一批人）**：这一轮的 VLM 在**像素级量测**（触摸区高度）与**标点字形**（全角／半角）上
**不可信**——它同时给出「无溢出」和「D4 只值 10」这种自相矛盾的读数；但在**「哪里看着不对」的定位**上仍有用
（V9 与 V8 两条真东西都是它先指出来的）。⇒ 票面那句「模型是提示器不是证据」在实测中被完整验证。

## §八 交付对账（票面逐条）

| 票面要求 | 落点／读数 | 状态 |
|---|---|---|
| 1. 双档截图 390×844 与 1280×900，两端都看 | 68 张（`.dsh-vision-router/artifacts/`）＋ 逐页逐档读数 JSON；两端都进逐页看屏 | ✅ |
| 2. 逐页看＋打分，落读数 JSON | `vision_describe` 逐页看 ＋ `packages/skill-memo-ilife/.scratch/t835-读数/判分结果.json`（逐页五维＋硬扣分） | ✅ |
| 3. 复算：读数与字面公式自己算一遍须一致 | `RECONCILE 逐页逐维最大绝对差 = 0 ⇒ 一致`（238 项） | ✅ |
| 4. 模型是提示器不是证据，逐条复核／剔除并写明 | `t835-复核对账.mjs` ＋ §七（10 条逐条判定） | ✅ |
| 5. 真机肉眼终审 | §五（出口 exit 0 ＋ 产物落盘 ＋ 双档打开） | ✅（一处如实记：走的是真出口那一层，不是 GUI 端新会话） |
| 验收命令：最低页分 ≥90 且每页每维 ≥ 满权 80% | `SCORE 页数=34 均分=97.41 最低=96 最高=100 ≥90 且每维≥80% 的页=34/34`，**exit 0** | ✅ |
| 交付物：`docs/skills/skill-memo-ilife/t835-视觉终审.md`（打分表＋缺陷清单＋剔除的误读） | 本件 | ✅ |
| 交付物：截图落 `.dsh-vision-router/artifacts/` | 68 张 ＋ `t835-双档截图读数.json` | ✅ |

**不许动的东西**：`src/config.ts` 未碰；`packages/plugin-memo-ilife/**` 未碰（只读它的桥代码当证据）；
老技能仓库 `D:\2Study\StudyNotes\SKILLS\备忘录` 全程只读（只读了 `templates/sync_report.html` 两行当旁证）；
不切分支、不 `reset`、不 `stash`；编译／测试／git 写操作全经 `node tooling/run-locked.mjs --ticket 835`。

**本票新入仓的件（独占写集）**：

| 路径 | 一句话 |
|---|---|
| `docs/skills/skill-memo-ilife/t835-双档截图.mjs` | 双档截图器（34 页 × 2 档，零依赖 CDP） |
| `docs/skills/skill-memo-ilife/t835-计算样式面.mjs` | 复核尺一：渲染后的版式与触摸读数 |
| `docs/skills/skill-memo-ilife/t835-屏上字符.mjs` | 复核尺二：渲染后可见文本的字符面扫描 |
| `docs/skills/skill-memo-ilife/t835-逐页证据.mjs` | 把机器读数按页并成一份证据面 |
| `docs/skills/skill-memo-ilife/t835-复核对账.mjs` | 10 条 VLM 结论的逐条复核对账单 |
| `docs/skills/skill-memo-ilife/t835-人核档.md` | 34 页 `facts.json` 人核取值（本批口径） |
| `docs/skills/skill-memo-ilife/t835-视觉终审.md` | 本件 |

**临时产物（不入仓）**：`packages/skill-memo-ilife/.scratch/t834/源/`（34 件终审批产物）／`.scratch/t835-读数/`（六件读数 ＋ 逐页证据 ＋ 验收命令输出）／`.dsh-vision-router/artifacts/`（68 张截图）。

## §九 遗留出口与开票三问（照 `docs/agents/编排纪律.md` 第十条）

§六 第 1 条（`备忘录同步` 宽档 480px 版心）走一遍三问：

| 问 | 答 | 据 |
|---|---|---|
| ① 能复现吗 | **能**：`t835-读数/计算样式面.json` 该页 `1280` 段读数 ＋ `templates/sync_report.html:43-47` 规则出处；探测件 `t835-计算样式面.mjs` 一条命令可重跑 | §六 第 1 条 |
| ② 有现存票或 MAP 能认领吗 | **没有专票**。地图 #820 的程序性子议题已全关（35/35 已关，finish 后已关），这一条既不属任何在开的票，也不属任何已承诺的目标 | `gh api` 子议题列表 |
| ③ 是**已承诺的目标**吗 | **不是**。判据只承诺「破一格记红」（H5 零溢出／D4 满 15，本页都绿）；#824 §2 把「宽档只占一窄列」列进 D4 的**观察项**，而 480px 版心**不破格亦不溢出**。且这是**老技能逐字传下来的形状**（老 `templates/sync_report.html:43-44`），属「想法与改进」 | §六 第 1 条 ＋ #824 §2 |

⇒ 按第十条，**不占票**（第三问不过 ⇒ 想法与改进一律写进文档），照仓内同款先例落地：
#878 对 `templates/init_report.html` 那件死件写的是「属**改进不是已承诺目标** ⇒ **记文档不占票**」。
本条的完整证据落本件 §六 第 1 条；同一事实同步记进 `docs/skills/skill-memo-ilife/t834-逐格缺陷清单.md`
的续记段，供下一轮谁动 `sync_report.html` 时照着改。

**本票收尾**：验收命令已 exit 0（`最低页分 96 ≥ 90`、`每维 ≥ 80% 的页 34/34`、`一致性自差 0`、退出码 0）
⇒ 进度 **100%**，关票。
