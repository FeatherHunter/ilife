# #848 册子冻结实施证据（34 格＋4 族＋1 共用件＋主体命名＋扁平目录）

## 落了什么（What，本票范围）

- 唯一定义地：`packages/skill-memo-ilife/src/help/booklet.ts`（92 LF）
  - 34 行：seq 1–30 结果页（`t822-册子冻结.md` §五原序）＋ 31–34 过程页
  - 4 族：通用回执 19／列表查询 9／报告 2／向导 4（19＋9＋2＋4＝34）
  - 1 共用件：复制区按钮排加说明行（只定名与复用范围，实现归 #849／#851）
- 公开接口：`src/help/index.ts` 转出（包根与 `package.json` 未动）
- 定义级自检：`test/booklet-848.test.mjs`（5 例全绿，不碰墙文件与产物字节）

## 对抗式更正一处

`t822-册子冻结.md` §五第 6 行把批量结果页主体误写作 `备忘改分类`
（与单条结果同名，会撞）。按同文 §二「单条／批量」后缀规则＋本票用户故事 14
（撞名按构造不可能），本件落实为 `备忘改分类-批量`；过程页为
`备忘改分类-批量-向导`。34 主体实测两两不同。

## 没碰什么（写集互斥）

- `cmd_read.ts` 旧主体名（心愿排期向导／心愿完成向导／同步报告／批量改分类）
  本次未改：改名涉 4 条落盘行为＋ `wizard-pages-665` 锁，归 8 张域票各填各的。
- 墙前缀匹配 5 行小改归 #825；样式实现归 #849／#851；8 域端到端与终审出本票范围。
- 时间戳与目录只引用不重写：时间戳归 `base-paint/save-html`；
  目录归 `src/help/manifest.ts`（`memo_html` 扁平，既有 216 件原地不动）。

## 机器证据（持锁）

- 编译：`node tooling/run-locked.mjs --ticket 848 -- node node_modules/typescript/bin/tsc -b packages/skill-memo-ilife`
  runId `3a9f4e70-7768-40f1-9630-100a46466d2e` exit 0
- 自检：`node tooling/run-locked.mjs --ticket 848 -- node --test packages/skill-memo-ilife/test/booklet-848.test.mjs`
  runId `37d4e7e2-0079-4a7a-9349-32879b088712` tests 5／pass 5／fail 0
- 回归抽查（229＋665＋render）：runId `7b1e7f86-9b5e-4831-a735-0671f0616594`
  tests 17／pass 16／fail 1；唯一红是 `src/health.ts` 命中
  `nextExclusiveCandidate|writeFileExclusiveWithRetry|flag:'wx'`（本票未碰该件，
  既有债务，归 #823 拆分；`t823-结构规格-草案.md` 已排其拆法）。

## 下游怎么读

- 域票：`bookletFileStem(sceneId[, kind])` 取主体，只填参数不另起布局
  （通用回执只参数化标题与字段）。
- #825：消费行字段（seq／family／wake／file／check）；file 存主体，
  实例 `主体_时间戳.html`，墙按主体前缀匹配（改动落 #825）。
- #849／#851：按族＋共用件实现样式；渲染路径 pending 该票。

## 行数门

`booklet.ts` 92 LF／`index.ts` 4 LF／测试 82 LF（测试不管辖），350 线未触发。
