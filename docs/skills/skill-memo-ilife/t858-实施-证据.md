# #858 · 实施证据（路由表清理四问：飞书授权词／废弃提醒／别名总表／统计命令）

**票**：[规格：路由表清理四问实施](https://github.com/FeatherHunter/ilife/issues/858)（序 39，**不挡 #820 收口**）
**口径**：[#842](https://github.com/FeatherHunter/ilife/issues/842) 四条全 A → 落盘件 `t837b-路由清理口径.md`
**开工前状态**：`ready-for-agent`、无 `needs-triage`、无 `## 进度` 段；认领＝`--add-assignee @me`
**独立复核**：另派一席（工作区只读复核 ＋ 两处变异复现），回执见 §九；本件已按它翻出的 8 条逐条处置。

---

## 一 · 必报五步

### 第一步 · 影响清单（开工前报的，＋ 复核后追加的两件）

| 件 | 一句话 | 理由 |
|---|---|---|
| `src/help/lookup.ts` | 速查上游换成生成路由表 ＋ 场景资产；一场景一行 | #842 Q③ |
| `src/triggers/wakewords.ts` | **删件**（31 词手写表） | 它是速查的第二上游、与记录面必然漂移；`cmd-registry-855` 台账自记「归并后本行该撤」 |
| `src/remind/routes.ts`／`src/memo/routes.ts`／`src/mood/routes.ts` | 撤 `废弃提醒` 行 ＋ `order` 重排 | #842 Q② |
| `src/init/routes.ts`／`src/wish/routes.ts` | 接三条别名（`初始化`／`新手`／`完成打卡`） | **复核翻出**：`t855-验收-命令自治.mjs` 的 `PENDING_WORDS` 挂着它们 ＋ 票面用户故事 2 |
| `src/memo/commands.ts`／`src/memo/run.ts`／`src/render/envelope.ts` | `memo.stats` 四处同步删 | #842 Q④ |
| `src/remind/run.ts` | 看提醒页复制区补已废弃语义与调用形 | #858 用户故事 14 |
| `src/shared/commandSpec.ts` | 注释里的死例子（原拿 `memo.stats` 举例「`wakeWord` 可缺」） | 复核 F4 |
| `src/cli/cmd_read.ts`／`src/cli/config.ts`／`src/cli/health.ts` | 删件后指着它的话（三处注释／一行说明） | 就地摆正 |
| `scripts/help-assets.data.mjs` | 看提醒状态说明 ＋ 摘要锁回填 | HELP 交付面 |
| `scripts/wizard-publish.sh` | 说明句里的能力面与契约键 | 复核 F3 |
| 生成物三件 ＋ `SKILL.md` | 随 `pnpm gen`／`build-help.mjs` 重导 | 派生件 |
| 包内用例 9 改 ＋ 1 退役 ＋ 1 新增 | 见 §五对账 | 判据 |
| 仓根 `test/memo-e2e.test.mjs`／`test/combos-p8.test.mjs`／`test/memo-split.test.mjs` ＋ `docs/memo-migration-split.md`／`tooling/check-publish.mjs` | 撤掉指着被退役键的那几行 | 「没有外部调用断裂」 |
| 历史探针 4 件（`t827-probe-search.mjs`／`t830-probe-checkin.mjs`／`t855-验收-命令自治.mjs`／`t855-产物基线.mjs`） | 路由件路径改指 ＋ 在册表按实况刷新 | 复核 F2、F1 |
| `packages/skill-memo-ilife/AGENTS.md` | 行数台账（`--sync` 派生） | 派生面 |

### 第二步 · 结构设计

- **能力目录**：不动形状。八域、六共用位、三根散件仍是闭集（`cmd-registry-855` ② 当场绿）。
- **删的不是目录，是一层中转**：`src/triggers/wakewords.ts` 删件后，`src/triggers/` 只剩
  `routes.generated.ts`（生成物）／`routeSpec.ts`（形状）／`routing.ts`（逻辑）——**声明 → 记录 → 消费**
  三段，人只碰声明那一段。
- **速查的筛选判据住一处**：`src/help/lookup.ts` 的 `buildHelpLookup()`。它读**两处已存在的权威**
  （`WAKE_ROUTES` 生成物 ＋ `MEMO_HELP_GROUPS` 场景资产），不新立第三份清单。
- **三条别名接线落在「词随场景住」的域**：`初始化`／`新手` 进 `src/init/routes.ts`（场景 `memo_init_setup`）、
  `完成打卡` 进 `src/wish/routes.ts`（场景 `memo_complete_wish`）——不新开命令、不新开场景。
- **共用件与门**：`lookup.ts` 属 `help` 能力内部；跨目录引用仍只走 `../triggers/routes.generated.js`
  （共用位）与 `../render/index.js`（既有门）。域间零直引由 #855 那道门守。

### 第三、四步 · 写代码与超线

- 常量与类型仍只有一处：键约束是生成的 `MemoKey`；路由记录是 `WAKE_ROUTES`。
- **超线报警：无**。`node packages/skill-memo-ilife/scripts/check-warning-line.mjs` →
  `RESULT: 105/105 PASS：行数台账齐全、与实况一致、越线件只许变短（超线 0 件在册）`（已 `--sync`）。

### 第五步 · 交付对账（实际碰到的件 ↔ 第一步清单）

**逐行对得上的**：
`src/help/lookup.ts`｜`src/triggers/wakewords.ts`（删）｜`src/remind/routes.ts`｜`src/memo/routes.ts`｜
`src/mood/routes.ts`｜`src/init/routes.ts`｜`src/wish/routes.ts`｜`src/memo/commands.ts`｜`src/memo/run.ts`｜
`src/render/envelope.ts`｜`src/remind/run.ts`｜`src/shared/commandSpec.ts`｜`src/cli/cmd_read.ts`｜
`src/cli/config.ts`｜`src/cli/health.ts`｜`scripts/help-assets.data.mjs`｜`scripts/wizard-publish.sh`｜
生成物三件（`src/cli/keys.ts`／`src/cli/registry.ts`／`src/triggers/routes.generated.ts`）｜`SKILL.md`｜
`AGENTS.md`｜`docs/skills/skill-memo-ilife/t837b-路由清理口径.md`｜`docs/skills/skill-memo-ilife/t858-实施-证据.md`｜
历史探针 4 件｜`docs/memo-migration-split.md`｜`test/memo-e2e.test.mjs`｜`test/combos-p8.test.mjs`｜
`test/memo-split.test.mjs`｜`tooling/check-publish.mjs`。

**包内用例 9 改 1 退役 1 新增**（开工清单里只点了 4 改，其余 5 件属**偏差**，逐条给理由）：

| 件 | 为什么非改不可 |
|---|---|
| `test/skill.test.mjs` | 速查断言换上游（原读被删的手写表） |
| `test/policy.test.mjs` | `废弃提醒` 那行改成「须抛无命中」＋导入路径改指 `routing.js` |
| `test/render.test.mjs` | 夹具与条数（14 → 13） |
| `test/cmd-registry-855.test.mjs` | 台账撤掉 `triggers/wakewords.ts` 那行（它自记「归并后该撤」） |
| `test/t827-search-domain.test.mjs` | 旧版给「`查情绪` 缺行」留的白名单，本票补上行后撤掉；导入路径改指 |
| `test/t832-sync-domain.test.mjs` | 导入路径改指（引用被删的模块） |
| `test/cmd-850-four-questions.test.mjs` | 命令面条数 14 → 13 ＋ 导入路径改指 |
| `test/cli-help-229.test.mjs`／`test/cli-help-230.test.mjs` | `q` 支的语料原用别名 `查提醒`（已撤行），改用主名 `看提醒` |
| `test/route-table-parity-855.test.mjs` | **退役**：它的对象就是那张被删的手写表（同义词回归已由新探针 ① 覆盖） |
| `test/route-cleanup-858.test.mjs` | **新增**：本票七面判据（唯一出口黑盒） |

**另有 8 个场景文件**（`src/help/scenes/*.ts`）只随**摘要锁头注**变了一行（`清洗后 30 条 sha256`），
正文只有 `remind.ts` 那一句语义说明真改——它们本就是生成物，重导即全部重写。

**偏差合计**：开工清单没点到的 5 件包内用例 ＋ 1 件源码注释（`src/shared/commandSpec.ts`）＋
1 件脚本说明句（`wizard-publish.sh`）＋ 2 件历史探针 —— 复核 F1–F5 翻出后补齐，逐条理由见上表与 §九。

---

## 二 · 读数（改动前 ↔ 改动后）

> ⚠️ **开工前的环境读数先要清一笔**：`packages/skill-memo-ilife/dist/cli/cmd_read.js` 被**别的测试缝的
> fixture 残留覆盖**（`packages/skill-calorie/test/helpers/land-inferred-stub.mjs` 的「崩溃残留」那条：
> 它把 `land-fixture.mjs` 暂放到 `skill-schedule`／`skill-memo-ilife` 两个 `dist/cli/`，文件头写明
> 「进程崩溃没走到还原 → 下次跑兄弟包测试前重编一次」）。**未重建时包内套件满屏红**。
> 按该件医嘱 `node node_modules/typescript/bin/tsc -b --force packages/skill-memo-ilife` 重建后回到
> **273/273 绿**——下表全部读数都在重建之后取。

| 面 | 改动前 | 改动后 | 判读 |
|---|---|---|---|
| 包内套件 `test/*.test.mjs` | **273/273 绿** | **274/274 绿** | 273 − 6（退役 `route-table-parity-855`）＋ 7（新探针） |
| 本票新探针 | — | **7/7 绿** | 七个面逐条（见 §三） |
| `docs/skills/skill-memo-ilife/t855-验收-命令自治.mjs` | **FAIL**（在册待办 4 条 ＋ 未账 1 条：`memo.batch` 有词面却不在速查面） | **PASS**（在册待办 0 条／`UNACCOUNTED: 空`／13 键全落桶 A 11＋B 1＋C 1） | 这张在册表点名的四条待办正是本票的活（复核 F1） |
| 仓根 `memo-e2e`／`combos-p8`／`memo-split`／`combos-42`／`combos-help-80` | 24 过 / 4 红 | 24 过 / 4 红 | **红名有变**：`memo-e2e` 由「整份读不进来」（`dist/index.js` 不导出 `routeWakeword`）变成「一条用例红」（skilllink 里 `memo.sync` 合成写的远端侧没成）。两条都是**先于本票**的腐坏/漂移，本票只是把其中一条从「隐藏」变成「看得见」 |
| 插件侧套件 | 87/89 | 87/89 | 同两条红（速查条数写死 28、版本行漂移），与技能侧改动无关 |
| `gen:check`（memo 三件） | 绿 | **绿**（`命令 12 条、路由 42 条`） | 生成物＝生成器输出 |
| `check-combos`／`check-boundaries`／`skill-call-form --check`／`gen-help-assets --check` | 绿 | 绿（OK／PASS／`6/6`／`9 件字节一致`） | 未被本票碰坏 |
| `snapshot:html:check` | exit 1（89 件 base-paint 标记） | exit 1（同一条） | 既有红；本票另加读数见 §五 |
| `tooling/test/skill-html-snapshot.test.mjs` | 7/10 | 7/10 | 三条红全在别的技能（chef 键片段 12≠8、home 标记、快照过期） |
| 行数台账 | 105 件在册、0 超线 | **105 件在册、0 超线** | 删件少一行 |

---

## 三 · 探针七个面（`packages/skill-memo-ilife/test/route-cleanup-858.test.mjs`）

| # | 判据 | 读数 |
|---|---|---|
| ① | 速查块＝一场景一行、行数＝场景数（30） | 30＝30；每场景恰一行；`备忘改分类` 两行（单条／批量，命令不同）；词集 ⊂ 场景主名；**每个有词面的键都有行**（含 `memo.batch`） |
| ② | 别名不在速查块、但 HELP 资产在、**条条可路由** | 12 条别名无一在块里；条条在资产 `aliases`；**12 条条条**路由回原键（含新接的 `初始化`／`新手`／`完成打卡`） |
| ③ | 废弃提醒无命中，废弃支可用且笔记保留 | `POLICY_NO_MATCH` ＋ 按键 exit 0／笔记保留／有效视图无它／已废弃视图有它 |
| ④ | 统计命令按键报未知键、速查无此行 | 形状表无键、无 `stat` 形、`SKILL.md` 无 `memo.stats`（**静态面先读**，见 §四 M3）＋ 真出口 exit 3 |
| ⑤ | 授权短语无命中、两诊断档可用、三支指去安装指引 | `POLICY_NO_MATCH`；`status`／`diag(dryRun)` exit 0；`init`／`qr`／`poll` exit 2 且报文带 `lark.prompt` |
| ⑥ | 说明句已改 | `SKILL.md` 有「只住 HELP 资产的 `aliases`」，无「住上一节索引块」 |
| ⑦ | 生成链自检绿 | `gen-cli.mjs --check` exit 0 |

**「看提醒页的状态说明」落在哪一层**（只读探针 `.scratch/t858/page-probe.mjs`，真出口跑出来的那一页）：

```
产物：…/memo_html/看提醒_20260922_184206.html（115042 字节）
  结果页复制区那句（#858 新加：语义＋调用形）：出现 1 次／在 <script> 里（页内载荷）
  结果页复制区里的命令形：出现 1 次／在 <script> 里（页内载荷）
  HELP 交付面的语义句（不在结果页）：出现 0 次／不在静态 HTML 里
页面第一个 script 标：<script id="payload" type="application/json">
```

判读：**AI 侧读得到**（页内载荷 → 渲染后上屏），**静态机审读不到**——与
`docs/base/base-render/t867-读数链契约.md` §八「静态面 ≠ 载荷渲染面」同一条已知缺口，
故本票**不给机审/五维尺添新红**。HELP 交付面那一句另住 HELP 页（`src/help/scenes/remind.ts` 的
`hint:status`），载荷检查当场读到「HELP 载荷含语义句 ✓／场景数=30」。

---

## 四 · 变异自证（改坏必红／还原必绿）

`.scratch/t858/mutate-858.mjs`（每处改坏 → 重导＋编译 → 跑探针 → **逐字节还原**（比 sha256）→ 再跑）：

| 变异 | 改的件 | 红在哪 | 还原后 |
|---|---|---|---|
| M1 速查的两道筛选**同时**失效（主名过滤 ＋ 一场景一行） | `src/help/lookup.ts` | `fail=2`：① 与 ②（块内 42 行 ≠ 30、别名混进块里） | `fail=0`，sha256 逐字节一致 ✓ |
| M2 把 `废弃提醒` 路由记录加回（order 42） | `src/remind/routes.ts` | `fail=1`：③（短语不再抛无命中） | `fail=0`，sha256 逐字节一致 ✓ |
| M3 把 `'memo.stats': 'stat'` 加回形状表 | `src/render/envelope.ts` | `fail=1`：④（**形状表那条静态断言先红**） | `fail=0`，sha256 逐字节一致 ✓ |

**两条如实写下的口径说明**：

1. **M1 为什么要同时打掉两道筛选**：现值下「主名过滤」与「一场景一行」**各自都够用**（任一单独失效，
   结果仍 30 行）——因为每条别名记录都排在它服务场景的主名记录之后。两道都留着是**防未来**：
   有人把别名声明排到主名之前时，靠主名过滤仍不出错。故这条变异证明的是「① 有牙」，不是「哪一道有牙」。
2. **④ 的读序**：独立复核指出旧序会让形状表那条断言「执行不到」（先撞真出口的报文断言）——
   已改成**静态面先读**，M3 现在准确红在形状表那一条；证据里的归因随之订正。

另两处**降级面**的机器读数：`gen-cli.mjs --check` 在还原态 0、在「拿掉筛选后重导」态 1（生成物 ≠
生成器输出）；`SKILL.md` 的注入块由 `build-help.mjs` 从 `buildHelpLookup()` 现算，`skill.test.mjs`
的「互联区新鲜」逐字比对（归一化后），手改即红。

---

## 五 · 快照门的本票读数（为什么没刷新）

`tooling/skill-html-snapshot.mjs --check` 在**影响面断言**处短路（89 件含 base-paint 命名空间标记，
**先于本票的既有红**），拿不到 changed/added/removed 三格。故另写只读探针
`.scratch/t858/snapshot-memo-delta.mjs`（只调它导出的 `collectArtifacts/buildSnapshot/readSnapshot/compare`）：

```
memo 侧 added  ：[]
memo 侧 removed：["memo/frag/memo.stats"]
memo 侧 changed：["memo/keys","memo/shape/list-empty","memo/tpl/change_category","memo/tpl/init_report",
                  "memo/tpl/memo_query","memo/tpl/receipt","memo/tpl/sync_report","memo/tpl/wish_complete","memo/tpl/wish_plan"]
全仓 added/removed/changed 条数：0／1／45
markers：89 件（既有红：base-paint 命名空间标记）
writeSnapshotChecked（临时落点）：written=false／markers=89／临时快照落盘=false
```

判读：**本票只贡献 1 条 removed（`memo/frag/memo.stats`）＋ `memo/keys` 少一键**；其余 44 条 changed
与 89 件 markers 都是 base-paint 迁移那条线的既有红。刷新快照这条路由今天**物理上走不通**：
`--write` 是先校验后落盘，标记命中即一字节不写（上面第三行读数）。

---

## 六 · 遗留出口（只登记去处，不占票）

1. **`packages/base-combos/**`（P8 combos 公共层登记）**：`combos.yaml` 里 `memo.stats` 仍在册
   （combos 段 ＋ channels 段），且这份登记与技能现状另有漂移——`test/combos-p8.test.mjs` 的
   「15 对 key×shape 与实现侧一致」红在 `未知 registry key：memo.init`、「present.ts == 代码生成输出」
   红在 `calorie.workout.xunji-key-set` 一族。**属公共层别席写集，本票不动**。
2. **`packages/plugin-memo-ilife/**`**（地图口径明写本图不碰）：`test/skills-provider.test.mjs:122`
   速查条数写死 28（今天 30）；`scripts/wizard-publish.sh:351` 的说明句仍写「契约键 memo.stats 打通」。
   技能侧那份发布脚本本票已改。
3. **`test/memo-e2e.test.mjs` 的 `memo.sync`**：经 skilllink 时远端合成写没成（那条用例的假 lark 挡板
   没有任务域能力）——先于本票的腐坏；本票只修好它读不进来的那一行导入。
4. **两件 #855 时代探针**：`t855-产物基线.mjs`（搬迁基线快照，今天 `0/13`，本票只撤掉退役那条用例，
   其余 DIFF 是先于本票的漂移）；`t855-验收-命令自治.mjs` 已按实况更新（今天 PASS）。
5. **别席在途件会因本票过期**（未提交、处理正确、不在本票写集）：`docs/agents/交付面-缺省落点现状与
   对齐成本.html` 把 `memo.stats` 当活口径写（引 `memo/run.ts:338-344`）——那条线的席收口时会踩到。

---

## 七 · 跑过的门（逐条可复跑）

| 门 | 命令 | 读数 |
|---|---|---|
| 包内套件 | `node tooling/run-locked.mjs --ticket 858 -- pwsh -NoProfile -Command "Set-Location packages/skill-memo-ilife; node --test test/*.test.mjs"` | 274/274，exit 0 |
| 本票探针 | 同上，`node --test test/route-cleanup-858.test.mjs` | 7/7，exit 0 |
| 命令自治探针 | `node docs/skills/skill-memo-ilife/t855-验收-命令自治.mjs` | `RESULT: PASS（13 键全部落桶：A 11 ＋ B 1 ＋ C 1）` |
| 生成物门 | `node packages/skill-memo-ilife/scripts/gen-cli.mjs --check` | exit 0（命令 12／路由 42） |
| 行数门 | `node packages/skill-memo-ilife/scripts/check-warning-line.mjs` | 105/105 PASS，0 超线 |
| 说明面同源 | `node tooling/skill-call-form.mjs --check` | `6/6 PASS` |
| 边界门 | `node tooling/check-boundaries.mjs` | `boundaries: PASS` |
| combos 门 | `node tooling/check-combos.mjs` | OK（150 注册＋15 对＋30 场景） |
| HELP 资产生成门 | `node packages/skill-memo-ilife/scripts/gen-help-assets.mjs --check` | exit 0（9 件字节一致） |
| 插件侧套件 | `node tooling/run-locked.mjs --ticket 858 -- node --test "packages/plugin-memo-ilife/test/*.test.mjs"` | 87/89（同基线两条红） |
| 快照门 | `node tooling/skill-html-snapshot.mjs --check` | exit 1（既有红，见 §五） |

---

## 八 · 外部调用面的补证

**① 发布门 G3 的契约键改指能不能跑通**（`tooling/check-publish.mjs` 的 CONTRACT_KEY 由
`memo.stats` 改指 `memo.help.lookup`）。按 G3 那一跑的同一布景（临时家目录 ＋ `~/.ilife/data/memo.db`）：

```
memo.help.lookup → exit=0 key=memo.help.lookup shape=list
memo.stats       → exit=3  ERR 3: 未知联动 key：memo.stats
```

**② 三条别名接线后的路由读数**（复核 F1 的正面复算）：

```
routeWakeword('初始化')   → memo.init      （场景 memo_init_setup）
routeWakeword('新手')     → memo.init
routeWakeword('完成打卡') → memo.update {done:true}（场景 memo_complete_wish）
```

**③ 那两处既有红的定位读数**（本票不修，只点名）：

```
test/combos-p8.test.mjs → a) memo 十键取数全通：memo.sync 经 skilllink exit 4「合成写没达成」
                          b) 15 对 key×shape：Error: 未知 registry key：memo.init
                          c) present.ts == 代码生成输出：差在 calorie.workout.xunji-key-set／xunji-key-clear
test/memo-e2e.test.mjs  → 读键全链路：同上 a) 那条
```

---

## 九 · 独立复核回执的处置（8 条逐条）

| # | 复核说的 | 处置 | 现场读数 |
|---|---|---|---|
| F1【中】 | `初始化`／`新手`／`完成打卡` 三条别名按词无命中，而新写说明句声称「仍能路由」；`t855-验收-命令自治.mjs` 的在册待办挂着它们 | **接线**（`init/routes.ts` ＋ `wish/routes.ts`）＋ 探针 ② 改成「12 条条条可路由」（原循环对无词面的词空转＝静默豁免） | 三条现在各有词面、路由回原场景；`t855` 探针由 FAIL 转 **PASS** |
| F2【中低】 | `t855-产物基线.mjs` 是活判据（6 个 `run.ts` 件头引用），其中「统计」那条 DIFF 是本票造成，未登记 | 撤掉它那条退役用例 ＋ 登记进 §六.4 | `0/13`（其余 12 条 DIFF 是先于本票的漂移） |
| F3【低】 | 技能侧 `wizard-publish.sh` 说明句仍写「契约键 memo.stats」，未登记 | 改指 `memo.help.lookup` ＋ 能力面去「统计」；插件侧那份登记进 §六.2 | 全文再无 `memo.stats`（技能侧） |
| F4【低】 | `src/shared/commandSpec.ts:46` 拿 `memo.stats` 当「`wakeWord` 可缺」的例子 | 例子换成 `memo.auth`（同样是「有键无词」的真例） | 活源码里无退役键残留 |
| F5【低】 | 证据件 §五对账漏点名 6 件实际碰过的件；「多碰 5 件」实为 9 个用例件 | 本件 §五重写为**逐件对账表 ＋ 偏差表** | 偏差逐条有名有姓 |
| F6【低】 | ④ 的形状表断言在变异下执行不到（旧序先撞真出口报文）；归因不准；⑤ 断内部键名；⑥ 是散文逐字匹配 | ④ 改成**静态面先读**、M3 已复算并订正归因；⑤⑥ 保留（⑤ 断的是「报错须指去安装指引」这条票面判据，⑥ 断的是票面点名要改的那两句） | 见 §四表 |
| F7【低】 | 插件侧两条红复现确认 | 与 §六.2 一致，不影响本票判定 | 87/89 |
| F8【低】 | 「看提醒」已废弃支复制区写「（调用形同左）」——载荷是一整串文本，没有「左」 | 该支**照写全调用形** | 页内载荷两支都含 `memo.remove --params '{"mode":"abandon","id":…}'` |
