# t827 · 人核档（查找域 7 页的 `facts.json` 四列取值）

票：[#827](https://github.com/FeatherHunter/ilife/issues/827)（Part of [#820](https://github.com/FeatherHunter/ilife/issues/820)）。
口径与写法照 `docs/skills/skill-memo-ilife/t867-人核档.md` §一／§二（同族先例）与
`docs/skills/skill-memo-ilife/t849-视觉基准.md` §1／§4.1（D1／D2 判据、硬扣分）。
页键＝册子主体（`src/help/booklet.ts` 的 `seq 7–13`，族「列表查询」）。

**页群**：`.scratch/t827/pages/` 的 7 件真产物。
**复核手段**：① 机器候选＝`docs/skills/skill-memo-ilife/t867-dom-probe.mjs`（静态可见文本，7 页候选均为 0）；
② 视觉模型＝390 档整页截图三轮（改前 → 改后 → 复核），判 D1／D2 并出具体元素名；
③ DOM／CSS 读数＝`.scratch/t827/fmt.json`（三档 `touchSmall=0`、`minFontPx=12`）与
`docs/skills/skill-memo-ilife/t869-机审.mjs`（六列全 0）。

**同形说明（如实标注）**：本族 7 页由**同一份模板 ＋ 同一份族样式**渲染，页间只差数据行；
故 D1／D2 由视觉模型在**代表页两张**（`按时间搜备忘` 6 行、`看备忘` 单条）上判，**逐页同值**；
`english`／`dupFacts` 走机器候选（7 页逐页各跑）。

---

## 逐页取值

### 搜备忘

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0 ｜ 出处=静态 `.wrap{max-width:1120px}` ＋ 渲染后 390／1440 两档实测 ｜ 理由=内容列 1120px 收得住（在公共层桌面版心之内）；同排卡片等高；本页无成列数值（每卡一个计数）⇒ `tabular-nums` 无对齐对象，不扣；无缺值留白；色只用令牌（本批已把 `badge.warn`／`badge.ok` 的字面色值换成 `--soft`／`--blue2`／`--ok`）
- d2 = 0 ｜ 出处=渲染后 整页两档 ｜ 理由=主角元素是页题（38px 对正文 13–14px，体量差明显）；面板小标题本批改为 15px／800（视觉模型复核「层级已拉开」）；卡片同列对齐；最长浅灰说明段约 40 字、未达「一片灰小字」阈值；区块间距节奏一致

### 查备忘

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0 ｜ 出处=渲染后 整页两档（同形外推：本族同一模板与族样式） ｜ 理由=与 `搜备忘` 同形；本页数据行更少，无新增扣分对象
- d2 = 0 ｜ 出处=渲染后 整页两档（同形外推） ｜ 理由=同形；页题与面板小标题层级同 `搜备忘`

### 看备忘

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0 ｜ 出处=渲染后 单条详情版（视觉模型代表页之一） ｜ 理由=单条也走同一张卡；内容列与徽章同族；无成列数值；无缺值留白
- d2 = 0 ｜ 出处=渲染后 整页两档 ｜ 理由=主角元素是页题与那一条笔记卡；无灰小字段；间距节奏同族

### 按时间搜备忘

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0 ｜ 出处=渲染后 6 行版（视觉模型代表页之一，第三轮复核） ｜ 理由=本页有三处「排期」强调徽章与六条卡；强调徽章本批改为 `background:var(--soft);color:var(--blue2);border:1px solid var(--blue)`，视觉模型第三轮复核「全屏徽章全部归入蓝／灰蓝色板，无暖色或杂色」⇒ 色只用令牌成立；其余同族
- d2 = 0 ｜ 出处=渲染后 整页两档 ｜ 理由=六条卡单列（本批补 `#list{grid-template-columns:1fr}`，390 档由双列改单列，视觉模型复核「单列、宽松不挤」）；小标题层级已拉开；间距节奏一致

### 查心愿

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0 ｜ 出处=渲染后 整页两档（同形外推：本族同一模板与族样式） ｜ 理由=与 `按时间搜备忘` 同形（有排期徽章那一路）：本批换令牌后同判
- d2 = 0 ｜ 出处=渲染后 整页两档（同形外推） ｜ 理由=同形

### 查打卡

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0 ｜ 出处=渲染后 整页两档（同形外推：本族同一模板与族样式） ｜ 理由=同形；无排期徽章
- d2 = 0 ｜ 出处=渲染后 整页两档（同形外推） ｜ 理由=同形

### 查情绪

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0 ｜ 出处=渲染后 整页两档（同形外推：本族同一模板与族样式） ｜ 理由=同形；无排期徽章
- d2 = 0 ｜ 出处=渲染后 整页两档（同形外推） ｜ 理由=同形

---

## `d4cut`（可选位）

**不写（按 0）**：三档零溢出；1440 档正文列 1120px（视口 78%），两侧余量对称、不属 §4.2 ③「只多留空」的观感债；
`#list` 在 1440 档双列、内容铺满 ── 不是「留空」形状。

## 未取自本档的两列

- **H2／H4**（内部标识符／符号顶替）：静态分隔符探针只发 R1–R3，本族页面又是 JS 渲染正文，
  静态链取不到这两类；已由 `t869-机审.mjs` 的③⑤两列按备忘录域口径判 0（六列全 0）；
  卡内「第 N 条」是本批按人眼复核把人话化的结果（原为 `#N`）。
- **运行时渲染文本**：本档的 english／dupFacts 是**静态候选**；运行时那一面（`生成时间：…`、
  卡片徽章行）本批已逐处人眼复核收过（见 `t827-search-证据.md` §3.4／§五.2）。
