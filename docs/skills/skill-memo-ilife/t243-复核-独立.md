# t243 独立复核（MAP #638 编排者席）

- 票：#243（备忘录 HELP 渲染后 DOM 回归锁）。实施席自证见同目录 `t243-DOM回归锁.md`。
- 判定：**PASS**（独立复跑一致 ＋ 自设新探针成立）。
- 复核窗口：`node tooling/run-locked.mjs --ticket 243 -- pwsh -File .scratch/map638/t243-review.ps1`
  ⇒ `GATE-RUN runId=9102e768-1cb9-472e-ae82-aea9c74b0817 exit=0`（窗口内直调，按协议 §2.4.5 不二次抢锁）。

## 一、独立复跑（不采信实施席结论）

| 项 | 命令 | 读数 |
|---|---|---|
| 基线载荷锁 | `node --test packages/skill-memo-ilife/test/help-file-228.test.mjs` | 15/15，exit 0 |
| 新 DOM 锁 | `node --test packages/skill-memo-ilife/test/help-dom-243.test.mjs` | 4/4，exit 0 |

提交核对：`de051a19` 仅两件（新建 `help-dom-243.test.mjs` 597 行；`help-file-228.test.mjs` 改 1 行＝改名恒真断言），与实施席自述一致；src 模板／wake 表／别包零改动可验。

## 二、自设新探针（专打「脚本自我满足」盲区）

实施席的变异旋钮 `T243_MUTATE=1` 只改**运行时读键**（`st.title`／`st.desc`）；它覆盖不到「渲染层别的轴断了」。故另设一条不同轴的探针：

- 做法：`.scratch/map638/t243-build-probe.ps1` 生成探针副本（相对导入改绝对 `file://`），加旋钮 `T243_PROBE_INPUT=1` ⇒ 把产物里的 `<input` 全替换为 `<inputx`（打掉 ④ 依赖的输入框标记），**载荷段一字不动**。
- 读数：
  - 探针副本无旋钮 ⇒ 4/4 绿（证明副本自身不是坏样本）。
  - 探针副本 `T243_PROBE_INPUT=1` ⇒ **3/4，红在 ④**（`input[data-p]` 数不符），exit 1。
  - 同一变异下**载荷锁仍 15/15 绿**（exit 0）⇒ 票面论点（载荷级锁抓不到渲染层断，含其「HTML 命中」恒真断言）由独立探针复现。

## 三、归属与遗留

- 本票无 S1；判据只紧不松（既有 15 条一条未放宽）。
- 遗留（不属本票、不开票）：判据要求的路由依赖（清单 129 键＝实况，与票面 99 旧数无关）本票未涉。
- 证据：本文件＋`t243-DOM回归锁.md`（两件已入版本库）；跑测日志 `.scratch/map638/t243rev-*.log`（未入仓，随窗口留存）。
