# #855 对抗式复核报告（席位：复核席 · 只读）

## 元信息（先说清楚复核对象是哪一刻的树）

| 项 | 值 |
|---|---|
| 复核对象 | 票 #855「`src` 按 8 域 ＋ 6 共用位重排 ＋ 命令登记纪律成门」 |
| 报告落盘时刻 | 2026-09-21 16:35（本地） |
| 实测时的 HEAD | `1df7b2af`（`docs(855): §二十 补产物面复验读数`） |
| 任务点名的提交 | `e1c4f245` —— **不是现在的 HEAD**。复核期间作者又落了 `ae8f609a`／`4441ed40`／`15986e3b`／`1df7b2af` 四个提交，其中 `ae8f609a` 改了 4 个 `src` 件（都只是注释里的表名对齐）、`15986e3b` 改了门与仓根测试。**凡「在 `e1c4f245` 不成立、后来被修掉」的项，本报告单独标注**，不当成现在的红。 |
| **我对原仓的写操作** | **零**。全部写操作只落在 `.scratch/rev855/`（`.gitignore:5` 已忽略 `.scratch/`）。实测 `git status --porcelain` 里没有任何 `packages/skill-memo-ilife/**` 或 `docs/skills/skill-memo-ilife/**` 的条目是我造成的；复制件全部在 `.scratch/rev855/{copy,root}` 下，变异只打复制件。 |
| 跑过的命令与读数 | 见文末「附录 · 命令台账」 |

**一句话结论**：**四道门今天都真绿、变异四题都真红，行为零改的读数全部复现**；但**「13 条命令的事实只在各域 `commands.ts`」这条纪律今天有 4 处仍在别处的第二定义（其中 2 处零门守、1 处是仓外并行登记表且已漂）**，且**门读 `dist/`、没有任何陈旧守卫**——改 `src` 不编译即门全绿。六条判据：**结构 黄／单源 黄／门 绿（附三个已证门洞）／行为零改 绿／诚实性 黄／口径漂移 黄偏绿**。

---

## 一 · 结构：`src/` 下是不是只有 8 域 ＋ 6 共用位

### 结论：**黄**（现状逐件对得上；声称漏了第 3 个散件；「只有这些」这条没有机器门）

**逐件列路径**（`Get-ChildItem -Directory`，14 个目录，与声称逐一相符）：

| 类 | 目录 | 件形状 |
|---|---|---|
| 域 | `src/wish/` | `index.ts` ＋ `commands.ts` ＋ `routes.ts` ＋ 13 件域内实现 ✓ |
| 域 | `src/memo/` | `index.ts` ＋ `commands.ts` ＋ `routes.ts` ＋ 5 件 ✓ |
| 域 | `src/search/` | `index.ts` ＋ `commands.ts` ＋ `routes.ts` ＋ `run.ts` ✓ |
| 域 | `src/remind/` | `index.ts` ＋ `commands.ts` ＋ `routes.ts` ＋ 3 件 ✓ |
| 域 | `src/sync/` | `index.ts` ＋ `commands.ts` ＋ `routes.ts` ＋ 4 件 ✓ |
| 域 | `src/init/` | `index.ts` ＋ `commands.ts` ＋ `routes.ts` ＋ 3 件 ✓ |
| 域（词面域） | `src/checkin/` | **只有 `routes.ts`** |
| 域（词面域） | `src/mood/` | **只有 `routes.ts`** |
| 共用位 | `src/shared/` `src/render/` `src/cli/` `src/help/` `src/db/` `src/triggers/` | 6 个 ✓（`help/scenes/` 8 片是 HELP 官方源，属 `help/` 内部） |
| 根散件 | `src/index.ts`（包门）／`src/config.ts`（★边界）／**`src/health.ts`（17 LF，跨包兼容口）** | 3 件 |

- 词面域只有 `routes.ts`：**这是票面允许的形状**（规格 §五「词住场景域、键住属主域」；`test/cmd-registry-855.test.mjs:81-89` 的判据就是「没有 `commands.ts` 的域只能是有词面的词面域」）。**绿**。
- **声称漏项**：任务书说的「8 域 ＋ 6 共用位（＋ `index.ts`／`config.ts`）」少算了 `src/health.ts`。它不是域、也不是六个共用位之一。它有正当理由（见第五节的核实：`packages/plugin-manager/test/health-706.test.mjs:91` 真按 `dist/health.js` 取 `buildMemoHealthReport`），但**它确实使「`src/` 第一层只剩下域与共用位」这句话不成立**。
- **最关键的一条**：**「`src/` 第一层只有域与共用位」今天没有机器门。** 我做的变异 ⑦c（新建 `src/utils/index.ts` ＋同步 `dist/utils/index.js` ＋台账 `--sync` 补一行）**五道门全绿**（详见第三节）。也就是说 `docs/agents/structure.md` 的铁律四（不许工种名目录）在这一层目前只靠人看。

**证据**
```
$ node mutate3.mjs  →  目录  checkin index.ts=- commands.ts=- routes.ts=Y
                       目录  mood    index.ts=- commands.ts=- routes.ts=Y
                       散件  config.ts / health.ts / index.ts
$ node mutate4.mjs  →  绿 棘轮（9 条）｜绿 差分回归｜绿 台账门（100/100）｜绿 生成器门｜绿 自治探针
```

---

## 二 · 命令事实单源：13 条命令的键／形状／标题／示例

### 结论：**黄**（主链单源确实立住了；但「只在各域 `commands.ts`」有 4 处例外，其中 2 处零门守）

**立住的那半边（实测，绿）**
- 13 条 `key:` 只写在 6 个域的 `commands.ts` 里（memo 5／remind 2／search 2／sync 2／wish 1／init 1）；`MEMO_CLI_KEYS`／`REGISTRY_KEYS`／`MEMO_DECLARED_SHAPES`／`MEMO_KEY_TITLES`／`MEMO_DOMAIN_KEYS` 全是 `scripts/gen-cli.mjs` 的派生件（`src/cli/keys.ts:1`、`src/cli/registry.ts:1` 头注「本文件由 … 生成，勿手改」）。
- `src/cli/registry.ts:15-36` 只有 `build(SOURCES)`，**零手写键**；`src/cli/cmd_read.ts` 无 `case 'memo.…'`（测试 `test/cmd-registry-855.test.mjs:140` 的 `!/case 'memo\./` 守着，我变异⑥验过它有牙）。
- 两张形状表：`MEMO_DECLARED_SHAPES`（生成物）↔ `MEMO_KEY_SHAPES`（`src/render/envelope.ts:5-25` 手写 14 行）逐键一致，由 `test/cmd-registry-855.test.mjs:108-116` 守，且多出来的键被钉死为 `['memo.help.lookup']`。**这条断言有牙**——把 `dist/render/envelope.js` 的 `'memo.search': 'list'` 改成 `'detail'` 即红（见第三节变异⑦B）。

**找到的第二定义（逐条 file:line）**

| # | 位置 | 是什么 | 有没有门 |
|---|---|---|---|
| 1 | `src/render/envelope.ts:5-25` | 13 条命令的形状**手写第二份**（`'memo.search': 'list'` 逐字在场） | **有门**（测试③逐键一致）——属「受纪律约束的共用位」，认下 |
| 2 | `src/triggers/wakewords.ts:4-7` | 手写 `export type MemoKey = 'memo.search' \| …`（13 键），与生成物 `src/cli/keys.ts:40-52` 同值同形 | **无门** |
| 3 | `src/triggers/wakewords.ts:9` | `export interface WakeRoute { key; params }` 与 `src/triggers/routeSpec.ts:18` 的 `WakeRoute { wakeWord; scene; key; cli; needs?; preset? }` **同名不同形**，两份同住 `triggers/` | **无门**（零消费者，`routing.ts:14` 取的是 `routeSpec.js` 那份） |
| 4 | `src/help/lookup.ts:25-39` | `DESCS` 手写 **12 个键** → 一句话（`memo.stats`／`memo.auth` 无词故不在） | **无门**（变异⑩：往里写 `'memo.search': 'list'`，五道门全绿） |
| 5 | `packages/base-combos/combos.yaml:683-742` | memo 12 条键的**并行登记表**（`key:`／`skill: memo`／`cmd: skill-memo-ilife`／`note:`），仓外件 | **无门，且已经漂**：`memo.init`／`memo.reminder` 在里面 0 命中（`grep memo\.init` = 0），仓根 `test/combos-p8.test.mjs` 当场红：`Error [RegistryError]: 未知 registry key：memo.init` |
| 6 | `src/cli/config.ts:20-25`、`src/cli/health.ts:16` | 另 4 条命令键 `memo.config.read/write/reset/check` 住框架位 `cli/`，**不在** 13 条登记表里 | 探针的键宇宙也不含它们（见第六节） |

**证据（原文）**
```
$ git ls-files packages/base-combos/combos.yaml
packages/base-combos/combos.yaml                 （在仓、手维护；base-combos/package.json:4「combos.yaml 唯一真相源」）
$ (Select-String packages/base-combos/combos.yaml -Pattern 'memo\.init|memo\.reminder').Count
0
$ node --test test/combos-p8.test.mjs            → exit 1
✖ 15 对 key×shape 与实现侧一致（memo MEMO_KEY_SHAPES + registry）
  Error [RegistryError]: 未知 registry key：memo.init（对不上即 fail，不返空）
```

---

## 三 · 门真会咬人（变异测试）

### 结论：**绿**（任务点名的四题**全部真红**）；**另附 3 个我已证实的门洞**（都不是这四题）

**基准（复制件的原样态，绿）**
```
$ node --test test/cmd-registry-855.test.mjs        → pass 8（今为 9，见下）
$ node --test test/route-table-parity-855.test.mjs  → pass 6
```

**任务点名的四题（全部红 ✓）**

| 变异 | 结果 | 红在哪条断言 |
|---|---|---|
| ① 复制件 `src/cli/cmd_read.ts` 加一行 `case 'memo.search':` | **红 exit=1** | 两条同时红：`④ 旧形状只许变短：分派件与生成器的挂号值`（318→319 LF 越天花板）＋ `④ 老路容器与转发门不许再出现`（`!/case 'memo\./`） |
| ② 复制件 `src/remind/commands.ts` 删掉 `memo.remind` 声明（不重生成） | **红 exit=1** | `① 键集单源：域声明 ≡ MEMO_CLI_KEYS ≡ REGISTRY_KEYS` ＋ `① 生成物不许自己长命令` |
| ③ 复制件 `src/triggers/wakewords.ts` 的 `WAKE_TABLE` 把 `看提醒` 的键改成 `memo.stats`（`dist` 同步改） | **红 exit=1** | `route-table-parity-855 ① 手写表每一词满槽位同结果`（旧表得 `memo.stats`、新表得 `memo.remind`） |
| ④ 复制件新建 `src/fetch/index.ts` | **红 exit=1** | `④ 老路容器与转发门不许再出现`（`src/fetch` 里还有源件） |

> 说明：③ 只改 `src` 不改 `dist` 不会红——该测试 `import { WAKE_TABLE } from '../dist/triggers/wakewords.js'`（见下面的门洞 D）。

**补充变异（我自己加的，用来找洞）**

| 变异 | 结果 | 说明 |
|---|---|---|
| ⑤ 出口加一行 `if (key === 'memo.search') {…}`（**加了行**） | 红 | 但红的是**行数天花板**，不是「认键」——见 ⑨ |
| ⑥ 只在注释里写上 `case 'memo.x':`（**长度中性**） | **红** | `!/case 'memo\./` 这条正则自己有牙 ✓ |
| ⑦ 只加 `src/fetch/index.ts`（不编译、不补台账） | 红 | `④ dist 顶层目录名必须 == src 顶层目录名`（**这条门是 `15986e3b` 才加的**；`e1c4f245` 时不存在）＋台账门「台账漏报」 |
| ⑦b 加 `src/utils/helper.ts`（无 `index.ts`、无 `routes.ts`） | 红 | `② 没有 commands.ts 的域只能是有词面的词面域` |
| **⑦c 加 `src/utils/index.ts` ＋ `dist/utils/index.js` ＋ 台账 `--sync` 补行**（＝一次正常编译后的开发动作） | **全绿** ❗ | 棘轮 9/9、差分 6/6、台账门 **100/100 PASS**、生成器门 3/3、自治探针 PASS。**一个「既不是域、也不是共用位」的工种名目录可以完全混过五道门** |
| ⑧ `src/sync/routes.ts` 把 `key: 'memo.sync'` 改成不存在的 `memo.ghost` | 棘轮／台账／探针**绿**；**生成器门红** | `Error: src/sync/routes.ts · "备忘录同步"（order=27）：路由指向未知键：memo.ghost` → 兜住了，不算洞 |
| **⑨ 出口长度中性写手写键分支**（把 `if (key === 'memo.help.lookup')` 那行整条换成 `if (key === 'memo.search')`，LF 不变） | **全绿** ❗ | 分派件里出现命令键字面量＋分支，五道门都不响。⇒ `src/cli/cmd_read.ts:177` 那句「往本件加 `case`、加键字面量即红」**只对了前半句** |
| **⑩ `src/help/lookup.ts` 里写第二份 `'memo.search': 'list'`** | **全绿** ❗ | 域外件里的键→形状第二表，没有任何门看它 |
| **⑦A 改 `src/render/envelope.ts` 的形状一格（不编译）** | **全绿** ❗ | 测试③读的是 `dist/render/index.js` |
| **⑦B 同一处 `src` ＋ `dist` 同步改（＝编译过）** | **红** ❗ | `✖ ③ 两张形状表逐键一致` → 断言本身有牙，**缺的是「dist 是否新鲜」这道守卫** |

**门洞 D（最要紧的一条口径缺陷）**：两件 855 测试都从 `dist/` 取事实（`test/cmd-registry-855.test.mjs:22-24`），而**本包没有任何陈旧守卫**——`scripts/gen-cli.mjs` 读的是**源码文本**（这一点是优点，生成器门不受影响），但测试侧的 `dist` 新鲜度**只写在测试头注的一句「先 `pnpm build`」里，没有人守**。对照 `docs/agents/命令登记纪律.md` §二·形状二明写的那套「陈旧守卫 `GEN-STALE FAIL` ＋ `dist/.gen-inputs.json` 内容印记」，memo 这家**没有实现**。后果：`src` 改完不重编译，「门绿」是假绿。

---

## 四 · 行为零改（四道门的实跑读数）

### 结论：**绿**（四条全绿，与声称逐字相符）

| # | 命令（原文） | 读数 | exit |
|---|---|---|---|
| 1 | `node tooling/run-locked.mjs --ticket 855 -- node node_modules/typescript/bin/tsc -b packages/skill-memo-ilife` | `RESULT: ticket=855 runId=b8244d17-f563-4a7c-b9fa-fdedef0cfa25 waitedMs=0 exit=0` | **0** |
| 2 | `node packages/skill-memo-ilife/scripts/gen-cli.mjs --check` | 3 行 `GEN-CHECK ok`（`src/cli/keys.ts`／`src/cli/registry.ts`／`src/triggers/routes.generated.ts`，逐行「域 8 个、命令 13 条、路由 40 条」）＝ **3/3** | **0** |
| 3 | `node docs/skills/skill-memo-ilife/t855-产物基线.mjs --check` | 14 行 `ok`（含 `记一条 17013B`／`批量改分类 22069B`／`首次使用 80145B`／`HELP 全量 116643B`／`速查表 4567B`）＋ `RESULT: 14/14（2214 ms）` | **0** |
| 4 | `node docs/skills/skill-memo-ilife/t855-验收-命令自治.mjs` | `UNACCOUNTED: 空` ＋ `RESULT: PASS（14 键全部落桶：A 11 ＋ B 2 ＋ C 1）`；头部 `命令 13 条／词 39 个／场景 30 张` | **0** |
| 5 | `node packages/skill-memo-ilife/scripts/check-warning-line.mjs` | `读数：件 99／超线（>350）0` ＋ `RESULT: 99/99 PASS` | **0** |
| 6 | `node --test packages/skill-memo-ilife/test/cmd-registry-855.test.mjs packages/skill-memo-ilife/test/route-table-parity-855.test.mjs` | `ℹ tests 15 / pass 15 / fail 0`（棘轮 9 ＋ 差分 6） | **0** |

**runId**：只有走 `run-locked` 的那一条有 runId（`b8244d17-…`）；另两条也是我实测的（更早一次 `7487a06b-0dac-45d7-a909-e673aff283dc` exit 0、`ba05de18-3237-480d-ba43-ae8bc5428581` exit 0，两次都在树变动前）。其余脚本不经锁，无 runId。

**与声称的偏差**：声称「棘轮 8/8」——现在文件里是 **9 条**（`15986e3b` 补了 dist↔src 目录名那条）；这不是漂，是后续提交加的。其余逐条相符。

---

## 五 · 诚实性：过时的话

### 结论：**黄**（有一批真过时的路径与数字；对的那几处我也列出来）

**B1 · 指向已不存在的路径（present tense，不是「原…」的历史句）**

| file:line | 原话（摘） | 实况 |
|---|---|---|
| `src/cli/config.ts:3` | 「为什么不进 `src/policy/wakewords.ts` 那张唤醒词表」 | `src/policy/` 已撤；表在 `src/triggers/wakewords.ts` |
| `src/cli/config.ts:78` | 「算式唯一定义地＝`src/fetch/paths.ts`」 | 实为 `src/shared/paths.ts`（同件 `:15` 的 import 就是 `../shared/paths.js`） |
| `src/cli/config.ts:80` | 「判据唯一定义地＝`src/fetch/feishu.ts` 的 `larkTierInfo`」 | 实为 `src/sync/feishu.ts`（同件 `:17` 的 import 是 `../sync/feishu.js`） |
| `src/cli/health.ts:3` | 「不进 `src/policy/wakewords.ts` 那张唤醒词表」 | 同上，`src/policy/` 已撤 |
| `src/shared/paths.ts:8-9` | 「`src/policy/media.ts` 的附件目录调 `mediaDirOf`，三处不许走散；照记账样板 #749 的 `src/fetch/paths.ts`」 | `src/policy/media.ts` → `src/memo/media.ts`；`src/fetch/paths.ts` 就是本件自己（现名 `shared/paths.ts`） |
| `src/shared/paths.ts:66` | 「「必须已存在」的判定归 `src/policy/media.ts` 的 `resolveMediaDir()`」 | → `src/memo/media.ts` |
| `src/sync/auth.ts:3` | 「…由 `src/fetch/feishu.ts`…」 | → `src/sync/feishu.ts` |
| `src/wish/policy.ts:1` | 「实现见 fetch/feishu」 | → `src/sync/feishu.ts` |
| `src/db/readonly.ts:49` | 「库目录从哪来见 `./paths.js`」 | **`src/db/paths.ts` 不存在**（`src/db/` 只有 `readonly.ts`）→ `src/shared/paths.ts` |
| `src/db/readonly.ts:261` | 「判定逻辑住 `reminders.ts`，此处只做行存取」 | **`src/db/reminders.ts` 不存在** → `src/remind/store.ts` |
| `src/init/diagnosis.ts:6` | 「谁在用：`src/cli/cmd_read.ts` 的 `dispatchInit`（唯一出口）」 | **`dispatchInit` 已被 #855 删除**（全 `src` 只剩此处与本域 `run.ts` 的注释提它）；实际调用方是 `src/init/run.ts:24` 的 `runInit`，经 `src/cli/registry.ts` 的 `REGISTRY` 走到 |
| `src/sync/feishu.ts:74` | 「`src/health.ts` 里那份只读副本只用前 5 档」 | `src/health.ts` 现 0 处提 `lark-cli`（它只剩 6 行 re-export）；那份 5 档副本在 **`src/cli/health/probe.ts:118`** |

> 其中 `src/config.ts:13/15/33` 的 `src/fetch/db.ts`／`src/policy/media.ts`／`src/fetch/paths.ts` 里，前两处是「原…／改造前是…」的历史句（可接受），但 `:33` 的「唯一定义地（`src/fetch/paths.ts` 的 `mediaDirOf`…）」是现在时 → 过时。**注**：票面 §三 明写 `src/config.ts` 是「★ 边界：本图不碰」，所以这一处算「本票有意不碰带来的陈旧」，不是偷偷漏改。

**B2 · 用户看得见的过时话（被「行为零改」冻住）**
- `src/init/run.ts:47`：复制区载荷里 `call_chain: 'memo.init --params → dispatchInit → src/init 两页装配 → base-paint 文档壳'` —— `dispatchInit` 这个函数在 #855 里已删，但它是**产物字节**的一部分，改它会撞 `t855-产物基线` 的 14/14，于是留着。这是「结构改了、产物里的话没改」的典型，值得记账。

**B3 · 数不到的数**
- `src/help/lookup.ts:43`：「全量速查表（**28 短语：16 显式 + 12 子唤醒词**）」——**实测 31**：
  ```
  node -e "…buildHelpLookup()…"  →  buildHelpLookup 词数=31；WAKE_TABLE 长度=31；唯一 phrase 数=31；唯一 key 数=11
  ```
  19 条显式行（`src/triggers/wakewords.ts:18-38`）＋ 12 条子词（`WAKE_TOPS`，`src/memo/category.ts:9-14`）。
  （任务书把「插件侧硬编码 28」划给 #858——这里是**技能侧同一句话**，我按「本报告要查的过时说法」列上。）
- `test/cmd-registry-855.test.mjs:10`：「生成器 `scripts/gen-cli.mjs` **≤ 380 LF**」——同件 `:122` 的实守值是 **293**。**门件自己的头注与实现差 87 行**；照头注读会以为门比实际松。

**B4 · 我核过、并且是对的（列出来免得只挑坏的）**
- `src/render/envelope.ts:1`「14 联动 key×shape 映射（10 条 ＋ help.lookup ＋ auth ＋ init ＋ reminder）」：实际 14 条 ✓（我逐行数过）。
- `src/help/sceneData.ts:21`「本文件给 2 个导出」：`MEMO_HELP_GROUPS` ＋ `buildHelpSceneIndex` = 2 ✓（`MEMO_HELP_VERSION` 确实没导出）。
- `src/health.ts:3` 的兼容口理由**成立**：全仓真有一个消费者 `packages/plugin-manager/test/health-706.test.mjs:91` 惰性 `import(join(REPO,'packages','skill-memo-ilife','dist','health.js'))` 取 `buildMemoHealthReport`。（只是「面板侧 `dsh-life-pack` 的 health 面就是这条老路」这句指得偏：面板面走 `health-fetch.ts` 的 RPC → spawn `memo.config.check`，不是这条 `dist/health.js`。）
- `src/cli/health.ts:15`／`src/cli/config.ts:20`「插件侧镜像同值，见 `packages/plugin-memo-ilife/src/bridge.ts`」：实核对 `bridge.ts:128-132` 四条 key 逐字一致 ✓。
- `src/help/sceneData.ts:3` 的「机器生成，禁手改」与 `src/triggers/routes.generated.ts:1`／`src/cli/keys.ts:1`／`src/cli/registry.ts:1` 的生成物头注：与 `scripts/gen-cli.mjs` 的 3 个落点逐条相符 ✓。

---

## 六 · 口径漂移：三桶与在册待办有没有把该修的写成「在册」

### 结论：**黄偏绿**（口径出处基本都点得到，探针确有牙；只有一处出处指得不实 ＋ 一处口径大于事实）

逐条判：

| 条目 | 判 | 出处（点到行） |
|---|---|---|
| `memo.auth` 无唤醒词 | **口径本来就该这样** | 票面 §五「`memo.auth`｜sync｜飞书授权只读诊断（`status`／`diag`，**无唤醒词**）」；探针 `EXEMPT['memo.auth'].from` 点的 `src/help/lookup.ts:40-41`（#760 注：飞书授权词随授权三支退役）与 `src/sync/commands.ts:5`（「`memo.auth`（读，`receipt` 形，无唤醒词）」）**逐条在场** ✓ |
| `memo.stats` 无唤醒词 | **口径对，出处指得不实** | 票面 §五「`memo.stats`｜memo｜统计（**无唤醒词**；处置归 #842）」✓；探针点的 `src/memo/commands.ts:6` 在场（「无唤醒词——命令面处置在 #842」）✓；但探针另写的「老骨架 30 场景里 0 条**统计**内容（**票 6 裁「不展」**）」在两处 `from` 里**查不到**——`src/triggers/wakewords.ts:55` 只说清理归 #858。（「0 条统计场景」这个事实我核了：8 片场景卡里 `统计` 只出现 1 次，是 `src/help/scenes/sync.ts:37` 的 prompt 里「回执 11 项统计」，不是场景。） |
| 4 条在册待办 | **不算敷衍** | 实为 **1 条「词面有、场景无」＋ 3 条「场景别名、词面无」**（不是任务书写的「4 个未接词面别名」）：`废弃提醒` 在 `src/triggers/routes.generated.ts:43` 有记录、HELP 30 场景无出处；`完成打卡`（`src/help/scenes/wish.ts:43` 的 `aliases`）、`初始化`／`新手`（`src/help/scenes/init.ts:39`）三条别名未接词面。四条**全挂 #858**，而 `gh issue view 858` ＝ **OPEN**、标题「规格：路由表清理四问实施（飞书授权词／废弃提醒／别名总表／统计命令）」；其裁定件 #842（**CLOSED**，标题「裁定：路由表清理四问…」）。⇒ 出处成立；且本票是**结构票**，接一条词＝行为变更（会动 HELP/速查面），推迟是对的。 |
| `memo.help.lookup` 框架位 | **口径本来就该这样** | 票面 §五最后一行「`memo.help.lookup`｜help（框架位）｜HELP 交付（**开库前分派，不进各域门**）」。探针把它单列桶 C 并断言「它不许进域声明或登记表」（`t855-验收-命令自治.mjs:117-121`），实测 `REGISTRY` 里确实没有它 ✓ |

**探针不是橡皮章（实测它有牙）**——在独立沙箱（`.scratch/rev855/root/`）里做两个变异：
```
绿 基线（原样）                    → UNACCOUNTED: 空 ⏎ RESULT: PASS
红 a 清空 PENDING_WORDS（表空着）   → UNACCOUNTED（1）✖ 词「废弃提醒」（→ memo.remove）在词面里、却在 HELP 场景面查不到出处，也不在 PENDING_WORDS 在册表里 ⏎ RESULT: FAIL
红 c 新加一条无词命令（memo.stats 改名）→ UNACCOUNTED（2）✖ 键 … 声明在 `memo/commands.ts`、但没有可跑的登记表条目 ＋ ✖ 键 memo.stats 有一面读数、却没有任何域声明它 ⏎ RESULT: FAIL
```
⇒ `PENDING_WORDS` 是**承重**的，不是装饰。

**唯一「口径大于事实」的地方**：探针头注 `t855-验收-命令自治.mjs:7` 说「凡是「在某一面出现过、却没被三桶任何一个收下」的键或词，一律计未账」，但它的键宇宙只有五个来源（各域声明／登记表／词面／速查／`FRAMEWORK`，`:110`）。**4 条 `memo.config.*` 命令键**（`src/cli/config.ts:22-24`、`src/cli/health.ts:16`）既不在那五面、也不在 13 条登记表，于是**根本进不了这把尺子**。这不是「漏账」（它们按设计走框架位），是**探针的那句自述范围写大了**。

**未验证**：探针在「词面少一个场景词」上的鉴别力——它读的是 `dist/triggers/routes.generated.js`，我改 `src/<域>/routes.ts` 的源文本不生效；要真验得先重生成，本席在收口哨声前没跑。同理，`test/route-table-parity-855.test.mjs` 对「同一词被拆到两个件」那条生成期守卫（`scripts/gen-cli.mjs:230`）也没做变异。

---

## 最该先修的三件事（按严重度）

### 1. `src/triggers/wakewords.ts:4-10` 的两份「同名单源」——直接违反票面要守的「单一定义地」，且零门
- **问题**：`MemoKey` 在这个件里手写了一遍 13 键联合（生成物 `src/cli/keys.ts:40-52` 已有同一份）；`WakeRoute` 在这里是 `{key, params}`，在 `src/triggers/routeSpec.ts:18` 是另一个形状——**同名不同形、同住一个共用位**。今天两份的值恰好一致，所以看不出来；`commands.ts` 里改一个键、这里不跟着改，`tsc` 也不会红（`routing.ts` 用的是生成物那份）。
- **改法**：`wakewords.ts:4-7` 的 `MemoKey` 整段删掉，改成 `import type { MemoKey } from '../cli/keys.js';`（`routing.ts:12` 已经正是这么取的）；`wakewords.ts:9` 的 `WakeRoute` 删掉（零消费者），`WakeEntry` 留着并让它 `key: MemoKey`。改完 `gen:check` 与两件测试都应保持绿。

### 2. `src/cli/cmd_read.ts:177` 的假承诺 ＋ 「域外件里的键字面量」没有门
- **问题**：那句注释写「往本件加 `case`、加键字面量即红」。实测：加 `case` 会红（变异⑥），**加一个长度中性的键字面量分支不会红**（变异⑨，五道门全绿）；`src/help/lookup.ts` 里另起一张键→形状表也不红（变异⑩）。
- **改法**：在 `test/cmd-registry-855.test.mjs` 末尾再加一条 ④，把「键字面量只许出现在在册位置」做成断言：扫 `src/**/*.ts`（剔除生成器三件与各域自己的 `commands.ts`／`routes.ts`），命中 `/'(memo\.[a-z0-9.]+)'/` 时，该键必须落在**白名单**里（`memo.help.lookup` 在 `cmd_read.ts`、`memo.config.*` 在 `cli/config.ts`／`cli/health.ts`、13 键在各自域件）；否则红并点名 file:line。顺带把 `:177` 那句话改成「加 `case` 即红；键字面量由 ④ 那条白名单门守」，别留超出事实的承诺。

### 3. `src/` 第一层加一条**闭集**门（今天只做 OR 判断，⑦c 全绿）
- **问题**：`test/cmd-registry-855.test.mjs:81-89` 对「没有 `commands.ts` 的目录」只要求「有 `index.ts` 或 `routes.ts`」。于是 `src/utils/index.ts`（工种名目录、铁律四的违反样子）只要**编译过一次**、台账 `--sync` 补一行，就五道门全绿（变异⑦c 实测）。
- **改法**：把 `:82` 那张手写的 `framework` 表换成闭集断言，一行就够：
  ```js
  const EXPECT_TOP = ['checkin','cli','db','help','init','memo','mood','remind','render','search','shared','sync','triggers','wish'];
  assert.deepEqual(readdirSync(SRCDIR,{withFileTypes:true}).filter(e=>e.isDirectory()).map(e=>e.name).sort(), EXPECT_TOP,
    'src 第一层只能有 8 个能力域 ＋ 6 个共用位；多出来的是没归位的目录');
  ```
  （`src` 根下的散件另用一条同样的闭集：`['config.ts','health.ts','index.ts']`——顺带把第一节那个「第 3 个散件」在册。）

### 附：第 4 件（够不上前三，但很便宜）
`test/cmd-registry-855.test.mjs:10` 的「`gen-cli.mjs` ≤ 380 LF」改成 **293**（与 `:122` 对齐）；同时把两件 855 测试的「读 `dist/`」补一道新鲜度守卫（哪怕只是比对 `dist/**` 的 mtime ≤ `src/**` 的 mtime，或照 `命令登记纪律.md` §二·形状二补 `dist/.gen-inputs.json` 那套内容印记）——**第七节的门洞 D 就是这条**。

---

## 附录 · 命令台账（我实际跑过的原文与退出码）

| 命令 | exit | 备注 |
|---|---|---|
| `git log --oneline -1` | 0 | `1df7b2af`（复核收尾时） |
| `node tooling/run-locked.mjs --ticket 855 -- node node_modules/typescript/bin/tsc -b packages/skill-memo-ilife` | 0 | `runId=b8244d17-f563-4a7c-b9fa-fdedef0cfa25`（另两次：`7487a06b-…`、`ba05de18-…`，均 exit 0） |
| `node packages/skill-memo-ilife/scripts/gen-cli.mjs --check` | 0 | 3/3 `GEN-CHECK ok`（域 8／命令 13／路由 40） |
| `node packages/skill-memo-ilife/scripts/check-warning-line.mjs` | 0 | `RESULT: 99/99 PASS`（超线 0） |
| `node docs/skills/skill-memo-ilife/t855-产物基线.mjs --check` | 0 | `RESULT: 14/14（2214 ms）` |
| `node docs/skills/skill-memo-ilife/t855-验收-命令自治.mjs` | 0 | `UNACCOUNTED: 空`／`RESULT: PASS` |
| `node --test packages/skill-memo-ilife/test/cmd-registry-855.test.mjs packages/skill-memo-ilife/test/route-table-parity-855.test.mjs` | 0 | `tests 15 / pass 15 / fail 0` |
| `node .scratch/rev855/mutate.mjs` | 0（脚本本身） | 变异 ①–⑧ |
| `node .scratch/rev855/mutate2.mjs` | 0 | 变异 ⑨⑩（长度中性） |
| `node .scratch/rev855/mutate3.mjs` | 0 | 变异 ⑦⑦b⑧ ＋ 目录形状盘点 |
| `node .scratch/rev855/mutate4.mjs` | 0 | 变异 ⑦c（五道门全绿） |
| `node .scratch/rev855/mutate5.mjs` | 0 | 变异 ⑥⑦⑧ |
| `node .scratch/rev855/mutate6.mjs` | 0 | 变异 ⑦A／⑦B（dist 新鲜度实验） |
| `node .scratch/rev855/probe-teeth.mjs` | 0 | 探针鉴别力（清空 PENDING_WORDS／新加无词命令） |
| `node --test test/combos-p8.test.mjs`（仓根） | **1** | `未知 registry key：memo.init`（归 #850；见第二节第 5 项） |
| `gh issue view 858` / `gh issue view 842` | 0 | 858 OPEN（别名总表等四条的实施）／842 CLOSED（同题裁定） |

**我没跑到的（写明为什么）**
1. 探针／差分门在「`src` 声明改了但没重生成」这一路的鉴别力——同上（读 `dist`，要重生成才生效）——**未验证**。
2. `test/route-table-parity-855.test.mjs` 对「同一词跨件重复」的生成期守卫（`scripts/gen-cli.mjs:230`）的变异——本席在收口哨声前没跑——**未验证**。
3. 包内全量测试套（`pnpm --filter skill-memo-ilife test`）——作者 §十九 自报「172 条 1 红（`cli-help-229.test.mjs:85`，起于 #706 `a42c9d4e`）」，任务书已划为票外；我**未复跑**该套——**未验证**。

**我改动过原仓任何文件吗**：**没有。** 全部写操作在 `D:\ilife\.scratch\rev855\`（`copy/`、`root/`、6 个 `.mjs`、本报告）；`.scratch/` 在 `.gitignore:5` 里。仓内 `packages/skill-memo-ilife/**` 与 `docs/skills/skill-memo-ilife/**` 在我的 `git status --porcelain` 里没有任何条目。
