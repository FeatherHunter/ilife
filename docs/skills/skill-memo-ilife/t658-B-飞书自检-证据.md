# t658-B · 备忘侧「飞书自检 sentinel」移植证据（#666）

- 票：[#666 备忘侧：飞书自检 sentinel ＋ 授权引导（D-03 三处改造）](https://github.com/FeatherHunter/ilife/issues/666)（`wayfinder:task`，Part of 地图 #658，依据规格 #657，用户故事 20／21／22／23）。
- 接缝：技能统一出口（spawn `packages/skill-memo-ilife/dist/cli/cmd_read.js`）；两个注入点：`SKILLS_DB_PATH`（临时库）＋ `LARK_CLI_PATH`（挡板 `tooling/contract-lark-stub.mjs`，未动）。
- 老实现（仓外只读）：`D:\2Study\StudyNotes\SKILLS\备忘录\script\feishu_sync.py:62`（前缀）／`:831-921`（`_sentinel_task`＋`_sentinel_calendar`＋`run_sentinel_write_test`）／`:948`（常驻入口 `check`）／`:883-889`（差集不过不跑）。
- 范围裁定（见 §三）：授权引导三步已由 #665 落地（`src/fetch/auth.ts`＋`memo.auth`，t658-C §三 W5／W6），本票只落**自检任务半场**；日程域归作息侧 `op=check`（D-20，#660 证据 R9），本侧不重复建。

## 一、判定

**PASS**。验收三条逐条有机器读数（本节）；老行为对照除第四节登记的偏离外逐条对上；两轮「改坏必红／还原必绿」全绿（§二）。

### 1.1 票面验收（`sentinel-666.test.mjs`，6 用例全绿）

| # | 命令 | 读数 |
|---|---|---|
| D1 | `memo.auth step:diag` | 挡板留痕 `task +create／+update／+complete／tasks delete` 逐条 argv；`--summary` 前缀 `[备忘录测试]`＋`任务权限验证`（老逐字）、改题后缀 `(已更新)`（老逐字）、`--assignee ou_stub_user`（老取登录身份）；回执 `left/leftId/cleanup` 全 null、`remote:'synced'`、exit 0；本地 0 行（自检不开库）；远端 0 残留 |
| D2 | `memo.auth step:diag dryRun:true` | 零任务调用；回执 `dryRun:true`、`remote:'not-applicable'`、exit 0 |
| D3 | `memo.auth`（缺省 status） | 零 `+create`、零任务调用（D-03 改造①：默认路径不跑） |
| D4 | 挡板 `mode:unavailable` 下 diag | exit 4、`remote:'unavailable'`、零任务调用（闸门在任何写之前拦住） |
| D5 | 挡板 `createFails` 下 diag | exit 4，`steps[0]=task_create:fail` 且仅一步（建失败短路，老同形），无残留 |
| D6 | 内联包装板（仅 `task tasks delete` 恒败，其余透传真挡板）下 diag | exit 4；`left/leftId/cleanup/message` 四格同点名 guid 与清法；步骤 `create/update/complete:true、delete:false`；远端留一条且为完成态（老终态语义保留） |

### 1.2 相关套件（最终验证轮，全绿）

- 新用例 `sentinel-666` 6／6；备忘包全量 84／84（含 #661 十一例、#665 六例、HELP 五件）；跨影响四门（`combos-p8`／`skilllink`／`synthetic-write-659`／`memo-e2e`）28／28。
- 门禁留痕 runId：构建 `7f52ebb1`（exit 0）；新用例 `fcfdbdf3`（exit 0，6／6）；包全量 `5612798e`（exit 0，84／84）；变异 `d2931b4e`（exit 0，`MUTATION RESULT: PASS`）；跨影响 `4d0f839d`（exit 0，28／28）。
- 首跑记录：新用例首跑 5／6（D6 断言漏了"删失败后的在不在复核"那次 `+get-related-tasks`，实现未动，补断言后 6／6，runId `2e858f5a` exit 1 留档）。

## 二、变异自证（两轮，改坏必红／还原必绿）

电池：`docs/skills/skill-memo-ilife/t666-变异电池.mjs`（经排队锁跑）；读数：`docs/skills/skill-memo-ilife/t666-变异读数.txt`。`MUTATION RESULT: PASS`。

| 轮 | 改坏成什么 | 红在哪条读数 | 还原 |
|---|---|---|---|
| MUT-1 | `status` 分支体改调自检（模拟"自检挂到默认路径"，票面明令禁止） | D3（零 `+create` 真断言） | 绿，产物无残留 |
| MUT-2 | 删尝试改成空操作（模拟"只标完成不清"，D-03 改造③的反面） | D1（删干净＋exit 0） | 绿，产物无残留 |

**两条实操教训（写进电池头注释）：**
1. 变异锚点必须选"改了真能走到"的分支：前两版把 `diag` 守卫改恒真／改或 `status`，都被它前面的 `status` 早返回遮蔽，改坏恒绿——锚点落在早返回之后的分支等于没改。教训：下刀前先走一遍分支序。
2. 断言须把实现的有意行为一次写全：D6 首版漏了删失败后的复核调用，红的是断言不是实现——"实现行为清单"先行，断言逐条对清单。

## 三、老行为对照

| 老行为（代码为准） | 新侧当刻 | 读数落在哪 |
|---|---|---|
| 前缀逐字 `[备忘录测试]`（`:62`） | `SENTINEL_PREFIX` 同值，D1 断言 `--summary` 前缀 | D1 |
| 标题 `任务权限验证 HHMMSS`（`:838`）／改题后缀 `(已更新)`（`:851`） | 同形（`sentinel.ts`），逐字 | D1 |
| 指派＝登录身份（`:839` `_get_user_open_id`） | `larkReady('task')` 的 `openId`，`--assignee` 断言 `ou_stub_user` | D1 |
| 建失败短路（`:848-849`） | 同形（无 guid 即返 exit 4，无残留） | D5 |
| 任务域 complete 为终态（`:858-864`，无删除） | complete 照打；其后加删尝试（D-31），删不掉落回完成态残留 | D1／D6 |
| 差集不过不跑（`:883-889`） | 闸门（四门）关即 `remote:'unavailable'` exit 4，零任务调用 | D4 |
| 日程域 create→update→delete（`:868-908`） | **不归本侧**：作息侧 `op=check` 已覆盖（D-20，#660 证据 R9 `deleteFails→exit=4`）；本包无日历写能力，不建第二定义地 | #660 R9（引用） |
| 授权引导 `feishu_auth_helper.py` | #665 已落地（`auth.ts`＋`memo.auth` init/qr/poll/status），本票复用闸门语义，不重复搬 | W5／W6（t658-C） |

## 四、有意偏离清单（D-31／D-32 · 正本＝裁定件第四节）

- **D-31 任务域补删尝试**：老自检任务域无删除（当时 lark-cli 短路无 `+delete`，`:834` 自认），完成即终态留真任务；D-03 原文"任务域保留 `complete` 终态"写在 #661 补删除能力之前。新侧 complete 照打之后，用 #661 的原生 `task tasks delete --task-guid <guid> --yes` 先尝试删除，删不掉才落回完成态残留＋四格点名＋exit 4。为什么：D-03 改造②③"代价要可见、能删的删干净"在任务域的落地；删失败非 0 与作息侧 `op=check` 同口径。读数：D1（删干净 exit 0）／D6（残留 exit 4）／MUT-2。
- **D-32 新建自带检说明**：老 `+create` 只带 `--summary`＋`--assignee`，无 `--description`。新侧多带一句话描述（自检来历＋跑完即删＋残留清法指向）。为什么：飞书侧可反查"这是自检对象"（用户故事 21）；描述故意不带「原备忘 #N」归属正则，对账仍判 `skippedNoMark`，不会被认领（`reconcile.ts:99-101` 双向校验）。读数：D1（描述断言）。

## 五、不隐瞒

- 本票改动面 0 红：包全量 84／84、跨影响 28／28；全仓其余红若有，均在本票改动面之外（本票4个源码文件＋2个测试文件＋3个证据文件之外未动一字节，见 §六）。
- 附带发现（不动，属他席在途）：`packages/base-combos/src/present.ts` 在本轮门禁运行时被 `combos-p8` 自愈重写，多出 `calorie.workout.xunji-backfill`／`xunji-push` 两键（卡路里训记侧在途工作，`combos.yaml` 已先行）。本票既未改 `combos.yaml` 也未提交该文件，原样留给其作者。
- 变异电池写法迭代两轮（runId `91601fa5`／`0d3490de`，均为 `MUTATION RESULT: FAIL(1)`）：MUT-1 锚点落在早返回遮蔽区致改坏恒绿，见 §二教训 1；第三轮 `d2931b4e` PASS 为准。
- 测试安全：远端闸门一律挡板（`LARK_CLI_PATH` 指向接缝临时目录，D6 包装板同目录内联）；活库从未打开；自检命令默认关闭，误触须显式 `step:'diag'`。

## 六、交付物路径

- `packages/skill-memo-ilife/src/fetch/sentinel.ts`（新建，自检本体，174 行＜350 线）
- `packages/skill-memo-ilife/src/fetch/index.ts`（加导出 3 行）、`src/wish/ensure.ts`（`local` 并集加 `'checked'` 一值，沿作息侧 `LOCAL_STATES` 同名）、`src/cli/cmd_read.ts`（`memo.auth` 加 `diag` 分支约 8 行＋注释 1 行）
- `packages/skill-memo-ilife/test/sentinel-666.test.mjs`（新建，D1～D6）
- 证据：本件＋`t666-变异电池.mjs`＋`t666-变异读数.txt`；正本：裁定件第四节 D-31／D-32 两行
- 超线报警：`cmd_read.ts` 542→549 行（本包线 350，**已超线，需要根据规则进行重构。**——超因在前，#665 起十二键分发集中此文件；本次增量 7 行，拆文件属另票量级，这次先不拆）

## 七、遗留出口

- D-31／D-32 待负责人签（签收方式沿 D-21～D-27 前例：票面确认即入正本；本票先行入仓，签收后补记）。
- HELP `memo.auth` 场景（含 `diag` 步骤说明）仍由 HELP 管线票承接（t658-C §七遗留）；速查表未动，诊断命令有意不做广告（D-03 改造①）。
- 本票关闭后，地图 #658 前沿清零（作息／备忘写侧全部落地），可走关图条件复核。
