## Question

票 2（`#222`）的对账表与票 3（`#223`）的取值表把要用户拍板的条目都摆齐了：**甲组 8 条内容题（U1–U8）＋ 乙组 8 条取值题**。逐条给本席建议，用户逐条答；裁完资产票、渲染票、出口票才有准数。

**约束**（用户 Q2 补充原话）：「我们要把HELP HTML这个官方源给做完善。以后MAP才有参照」——HELP 是**官方源**，宁全勿缺；凡「留登记位 vs 不列」的取舍，一律向「留位、可查」倾斜。

**甲组 · 内容（票 2 的对账表；逐字出处见 `t222-content-reconcile.md`）**

先说分量：新表把老核心动词整体换掉了——`记备忘`／`改备忘`／`删备忘`／`备忘改分类`／`备忘录同步`／`记情绪`／`删情绪`／`改情绪`／`首次使用` 用新表口径复算**全部 `POLICY_NO_MATCH`**；`memo.sync` 有命令、有形状、有实现，却**没有任何唤醒词指向它**。老 29 个唯一唤醒词里**完全不可路由 10 个**，新仓 `test/` 只覆盖 1/10。

1. **U1** `记备忘`／`改备忘`／`删备忘` 无落点 → 补唤还是改名？（HELP 是官方源 ⇒ 「要补哪些唤」应当能从 HELP 直接读出来）
2. **U2** `memo.sync` 有实现无唤醒词 → 用老词 `备忘录同步` 还是新造？
3. **U3** 老 `备忘改分类` 单条＋批量共用一个词，新表拆出 `批量改分类`，**单条改分类在新表无词可用**。
4. **U4** 情绪族 5 词改名（`…情绪` → `…情绪日记`）→ 老词收进 `aliases` 还是作废？
5. **U5** `memo.stats` 有形状而**老骨架 0 条内容** → HELP 展不展？
   ✅ **复核票（`#234`）已给出事实，不反转**：老技能**没有** `stats` 子命令、**没有**任何统计类场景卡（八处读遍的证据链见 `t234-cli-inventory.md`；全域「统计」只出现在 `sync-from-feishu` 的 11 个对账字段里，唯一那处 `### 统计` 是图例计数表标题）。⇒ 按「老骨架为准」本不该展；但 HELP 是官方源，「展不展」仍请你裁。
6. **U6** 新词 `废弃提醒` 无场景；老「删 X」4 条语义归 `memo.remove` 还是 `memo.update`（新表 `删心愿` 的命令就是 `memo.update`）。
   ✅ **复核票（`#234`）的事实**：老「删 X」是**独立子命令** `delete`（`:1571`／`:1704`／`:323`）——老 21 个 parser 里**根本没有 `remove`**，`update` 也没有删除／废弃能力；另有两个不同的「废弃」：`dismiss`（提醒，`:1657`）与 `complete-wish`（心愿＝删心愿＋建打卡，`:1579`）。本席建议：老 4 条「删 X」场景（`scenarios.yaml:89/424/469/510`）**整体归 `memo.remove`**。
7. **U7** 老 HELP 触发短语实为 **9 条**（1 字面 ＋ 8 变体，`manual`／`guide` 同行）——确认口径（地图 Notes 已按 9 条改）。
8. **U8** 4 处「分类下只有一个子功能 → 走『基础』兜底」（sync／checkin／mood／init）要不要真建二级组。

**乙组 · 模板取值（票 3 的取值表；逐字出处见 `t223-template-contract.md`）**

9. **`meta_blocks[1]`（HELP 唤醒词块）该不该有**：新 `WAKE_TABLE` 里没有 `memo.help.lookup`，**无源可派生**；且老 `SKILL.md:1141` 明写「不展示 HELP 唤醒词自身」⇒ 可能本就不该有。
10. **`title` 要不要自带技能名**：照老取 `'使用手册'` ⇒ hero 大字行里没有技能名，与 `#145` 引述的「技能名应在大字行」相悖。
11. **`version` 是否仍取 `1.3.0`**：新表与老 yaml 不是同代资产，这个数在新仓无出处。
12. **初始化口径要重定义**：老家「DB 文件存在＝已初始化」在本机实测**判错**——`D:\.db\memo.db` 是 0 字节空壳、真正的库目录 `D:\.db\memo` 不存在。新库是**目录**不是 `.db` 文件。建议口径＝**「memo 库目录存在」**。
13. **`HELP_INITIALIZED` 逃生阀**要不要保留（记账没有这个）。
14. **`contact` 补不补** `url`／邮箱（老家只有两项，无 `url`、无 `copy_all`）。
15. **`editable_fields` 进不进新 HELP**（票 2 从内容侧、票 3 从取值侧都点到）：老 **76 条／29 场景**，带三类缺陷——1 条布尔脏数据、22 条 label 回落成 ASCII 键、12 条是 CLI 开关 `html` ⇒ **不能逐字搬**；但共享模板**认这个字段**（`help-template.html:1669`，账单没有）。票 3 倾向**不进**。
16. **二级组 `id` 序数从 0 还是 1 起**：老 `subgroups[].id` 是 `memo_0`／`memo_1`…（**0 起**），与记账／居家（1 起）不同。

产出：逐条裁决写回票面，并落一份决策卡（`docs/skills/skill-memo-ilife/` 下本票号前缀，形式照 `docs/skills/skill-home/t196-decisions.html`）。

**全部裁决（2026-09-12，16/16；决策卡定稿在 `t226-decisions.html`）**

| 题 | 定案 | 用户原话／要点 |
|---|---|---|
| U1 | A | 「不标出 无唤醒词可以路由。我们开发help html就是那个最终功能全部实现的完整体！」 |
| U2 | A | 「不额外标记 无唤醒词。我们肯定是要将新技能开发成和老技能一样支持这些功能。之前开发的新技能因为AI疏忽遗漏了大量功能。」 |
| U3 | A | 同 U2（不额外标记无唤醒词） |
| U4 | A | 照老：老词为主名、新词进 `aliases`；4 条情绪孪生**不各占一行场景** |
| U5 | A | 「都按老的来！」⇒ 老骨架没有统计内容，`memo.stats` **不展**，只在口径区留一行 |
| U6 | A | 「HELP HTML的prompt中不应该出现具体的命令硬编码，只有唤醒词，唤醒词在SKILL.md中内部流程才涉及到命令」⇒ 4 条「删 X」整体归 `memo.remove` ＋ 立下「命令不上页面」的规矩 |
| U7 | **更正项，非决策** | 「U7 你说的这9条是什么？……完全没看懂」——**是票 2 发现地图把 9 条数成了 8 条**（第 ⑨ 条 `guide` 与 `manual` 同格被漏算）。按 **9 条**更正即可，无需裁；沾边的只有「新技能 `SKILL.md` 将来把 9 种说法写全」（按 U2 口径＝写全） |
| U8 | A | 照老写「基础」，保持 13 个二级组 |
| V1 | A | 「HELP HTML中不包括HELP的唤醒词场景。用户说 备忘录HELP就能拿到」⇒ 不加「你可以这样喊我」那块，也**不把 HELP 自身唤醒词当场景卡** |
| V2 | A | 照老取 `'使用手册'` |
| V3 | A | 取 `1.3.0` |
| V4 | A | 改判「memo 库目录存在」 |
| V5 | A | 不要 `HELP_INITIALIZED` 逃生阀 |
| V6 | **B** | contact 补上可点的链接（**非推荐项**，按用户裁）——归票 8 落 |
| V7 | A | 「使用这个参数，挺好的，既然 HELP HTML 有这个说明还很优秀的功能」⇒ `editable_fields` **进**，按清洗后的 76 条落 |
| V8 | A | 二级组 id 从 1 起 |

**三条从备注／定案里长出来的硬规矩（已进地图 Notes 与票 7／票 8）**

1. **不标缺失**：HELP 页面上**不许**出现「当前无唤醒词可路由」这类标记——HELP HTML 就是**最终功能全部实现的完整体**，是目标态，不是现状快照。
2. **命令不出现在页面上**：HELP 的 prompt 里**只出现唤醒词**，不出现命令硬编码；命令只住在 `SKILL.md` 的内部流程里。
3. **HELP 自身的唤醒词不上页面**：不渲染「怎么喊我」那一块，也不把 HELP 自己的唤醒词当成一条场景卡。

**产出**：决策卡 `docs/skills/skill-memo-ilife/t226-decisions.html`。细节出处：票 2 `t222-content-reconcile.md`、票 3 `t223-template-contract.md`、票 14 `t234-cli-inventory.md`。

## 进度：100%

**本票完成（2026-09-12）**：16 条全部定案，决策卡定稿；三条硬规矩已并进票 7／票 8 与地图 Notes。下一步：无（关票）——票 7／票 8 的开工前置由此解除。
