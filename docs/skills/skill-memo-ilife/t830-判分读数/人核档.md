# t830 · 人核档（`facts.json` 四列人核取值的口径与出处）

> 票：[#830](https://github.com/FeatherHunter/ilife/issues/830)（Part of [#820](https://github.com/FeatherHunter/ilife/issues/820)）。
> 格式与出处写法照 `docs/skills/skill-memo-ilife/t867-人核档.md`；页群＝本域 3 格产物（`记打卡`／`改打卡`／`删打卡`），
> 由临时库真跑落盘、逐字节拷贝而来（驱动器 `.scratch/t830/gen-checkin-pages.mjs`，可重跑）。
> 两档真截图（390×844 与 1440×900，各 3 张）落 `.dsh-vision-router/artifacts/`（`add.html-*`／`update.html-*`／`remove.html-*`），
> 即「页群目录里 ASCII 名副本」的渲染结果——文件名只为截图方便，内容与产物逐字节相同。

## §一 `english` 与 `dupFacts`：采纳机器候选，三页各 0 处

三页的静态候选都是 0 处（装配器输出），逐页判「采纳候选」。**复核过为什么是 0**：

- 可见文本面没有允许清单之外的 ASCII 字母与半角标点（六列机审 ⑥ 列同判 0 处，逐件读数见 `t830-机审读数.json`）；
- 没有同一串文字在一页出现两次（六列机审 ④ 列同判 0 句）。

## §二 逐页取值

### 记打卡

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0｜ 出处=渲染后 390×844 与 1440×900 两档真截图（`add.html-*`）｜ 理由=结构纪律逐条过：页壳与区块走公共层（本页样式块 0、内联样式仅 4 处公共层写法、本页字面色值 0——六列机审 ③ 列同判），栅格与间距由族模板统一给；本页无表格列、无缺值占位、无数字列，故无 `tabular-nums`／缺值两条的适用位。
- d2 = 0｜ 出处=渲染后 同两张截图｜ 理由=信息层级清楚：页题「备忘录回执」→ 一句话结论「已记一条：4」→ 分类与子分类徽章 → 元信息行（时间 ＋ 唤醒词）→「这次的结果」三条事实 →「处理结果」键值四行 →「数据与日志」动作区；同类事实相邻、无同事实两处。

### 改打卡

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0｜ 出处=渲染后 390×844 与 1440×900 两档真截图（`update.html-*`）｜ 理由=同「记打卡」（同一份族模板，本页样式块 0、本页字面色值 0）。
- d2 = 0｜ 出处=渲染后 同两张截图｜ 理由=同「记打卡」；结论行换成「已更新：1」，事实条与处理结果同构，改前改后不换版式。

### 删打卡

- english = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- dupFacts = 0 ｜ 判=采纳候选 ｜ 出处=静态 —（候选 0 处）
- d1 = 0｜ 出处=渲染后 390×844 与 1440×900 两档真截图（`remove.html-*`）｜ 理由=同「记打卡」；无子分类时徽章只留「打卡」一枚，不留空槽（缺值不占位）。
- d2 = 0｜ 出处=渲染后 同两张截图｜ 理由=同「记打卡」；结论行「已删除：2」，被删对象的事实条仍在（删前那一行是权威），页上不出现悬空引用。

## §三 已知边界（如实声明）

- `d4cut` 未写（可选列，引擎缺省 0）：本票未跑通可复现的视觉模型判分链路，与 `t827`／`t828`／`t829` 同一条缺口（读数链契约 §六 已登记）。
- 两档截图为 **headless Chrome 固定视口**（390×844／1440×900），只截可视区、不整页长图；本域 3 页可视区内的版式与层级已全覆盖，长图不改变结论。
