# t658-A · 备忘侧「心愿排期」移植 · 证据（#661）

- 票：[#661 备忘侧：到期日（排期日期）＋ 心愿飞书层 5 操作 ＋ 反向对账](https://github.com/FeatherHunter/ilife/issues/661)（`wayfinder:task`，Part of 地图 #658；规格 #657）
- 判据与接缝（唯一依据）：`docs/agents/合成写判据.md`（#659 出件，一个接缝 ＋ 两个注入点 ＋ 四条读数模板 ＋ 偏离收口机制）
- 老不变量清单（判据主体，以老代码为准）：`docs/skills/skill-memo-ilife/t659-不变量清单.md`
- 老实现（仓外只读）：`D:\2Study\StudyNotes\SKILLS\备忘录\script\memo_cli.py`、`script\feishu_sync.py`
- 接缝：技能统一出口（spawn `packages/skill-memo-ilife/dist/cli/cmd_read.js`）；两个注入点：`SKILLS_DB_PATH`／`LARK_CLI_PATH`
- 取证日：2026-09-17

## 一 · 判定

**PASS**。本条接缝上的四条读数全绿；老不变量 M-01～M-13 除下节登记的有意偏离外逐条对上；八个偏离行各有一组「改坏必红／还原必绿」机器读数。

## 二 · 机器证据

### 2.1 四条读数（规格 #657「Testing Decisions」）

命令：`node --test packages/skill-memo-ilife/test/wish-sync-661.test.mjs`（11／11 绿，运行标识 `a0e70e1e-7335-44d0-9ce4-81d818addd26`）

```
#661 四条读数：idempotent=pass({"本地位数":"1→1","远端重复建":0}) | receipt=pass({"三格":{"本地侧":"created","远端侧":"created","远端标识":"tk_1"},"填了几格":3}) | degrade=pass({"本地照样写":true,"回执标明远端没成":true,"退出码":4}) | ownership=pass({"远端建了几个":1,"锚":"原备忘 #mmu4s4rj3v5","对象":"飞书任务"})
```

（归属锚里的 id 每次跑都不同，形如 `原备忘 #<本地 id>`。）

### 2.2 变异自证（八轮，改坏必红／还原必绿各一行）

命令：`node tooling/run-locked.mjs --ticket 661 -- node .scratch/t661/mutation-round.mjs`（窗口运行标识 `a1be2c1e-e6a8-4351-8d00-e3e6951391b4`，exit 0）
可复跑脚本入仓：`docs/skills/skill-memo-ilife/t661-变异电池.mjs`；机器读数入仓：`docs/skills/skill-memo-ilife/t661-变异读数.txt`

| 轮 | 改坏成什么（＝老行为／坏行为） | 红在哪条读数 | 还原 |
|---|---|---|---|
| MUT-A | 建前不查重（老样子） | 四条读数·幂等：`本地位数:1→1，远端重复建:1` | sha 一致 ＋ 该读数回绿 |
| MUT-B | 删心愿只标完成（老 D-06／D-17） | T6：`远端要收到删除（老实现只发 +complete）` | 同上 |
| MUT-C | 查重键用未截断全文（老 D-15） | T10：`长标题心愿也要查得中` | 同上 |
| MUT-D | 只在有排期日期时查重（老 D-16） | T9：远端复用断言 | 同上 |
| MUT-E | 对账步 2 把心愿转打卡（老 D-18） | T7：`远端完成 → 本地只标完成，不转打卡` | 同上 |
| MUT-F | 远端不可用时退出码写 0（老 D-19 口径） | T7：`远端不可用：本地照落，但这一趟没达成` | 同上 |
| MUT-H | 本地侧建前不判重（老 D-20） | 四条读数·幂等：`本地位数:1→2，远端重复建:0` | 同上 |
| MUT-G | 手改 HELP 产物一个字节 | `gen-help-assets.mjs --check`：`与生成结果不一致（禁手改）` | 同上 |

**一条实操教训（写进电池脚本头注释）**：Windows 的 `copyFileSync` **保留源文件 mtime**，还原后源码比 `dist` 还旧 ⇒ `tsc -b` 判「已是最新」不重编，`dist` 里留着改坏那一版（会出假绿、也会出假红）。故还原后必须把源码 mtime 拨到当下，并**断言产物里真有／真没有那段行为**——只看用例红绿不够。

### 2.3 门禁运行留痕（协议 §2.3 第 4 条）

导出件：`docs/skills/skill-memo-ilife/t658-A-心愿排期移植-门禁留痕.log`（本票全部 `START`／`RUN` 条目）。门禁证据只领 exit＝0 的条目：

```
GATE-RUN runId=a0e70e1e-7335-44d0-9ce4-81d818addd26 cmd=node --test packages/skill-memo-ilife/test/wish-sync-661.test.mjs
GATE-RUN runId=f18d2b11-cd41-4e41-a1e8-24bfec65a25e cmd=node --test packages/skill-memo-ilife/test/{cli,policy,render,fetch,skill}.test.mjs test/{memo-e2e,combos-p8,combos-42,synthetic-write-659,memo-split}.test.mjs packages/plugin-memo-ilife/test/{smoke,skills-provider}.test.mjs
GATE-RUN runId=bd500138-7d7c-4e0a-8e3c-9890588d68e3 cmd=node --test packages/skill-memo-ilife/test/{help-file-228,cli-help-229,cli-help-230,help-dom-243,help-reuse-245}.test.mjs
GATE-RUN runId=259e5f46-9461-4dcb-b5fc-5fab3c413d0c cmd=node tooling/skill-html-snapshot.mjs --check
GATE-RUN runId=70c3635e-5024-495d-bcb8-69a20a72e3f6 cmd=pnpm gen:check
GATE-RUN runId=31a4479f-508a-4748-99de-e82761d31d00 cmd=pnpm boundaries
GATE-RUN runId=b70bc02b-a131-4aed-b537-8050861a0ef4 cmd=pnpm doctor
GATE-RUN runId=a1be2c1e-e6a8-4351-8d00-e3e6951391b4 cmd=node .scratch/t661/mutation-round.mjs（八轮变异窗口）
```

摘要行：新用例 `tests 11／pass 11／fail 0`；既有相关套件 `tests 64／suites 11／pass 64／fail 0`；HELP 五件绿；快照门 `OK: 5 技能 HTML 快照 == 实际（186 件产物）`；`GEN-CHECK PASS：键 129…`；`OK: 未迁移技能源码／模板不 import base-*`；`doctor` exit 0。

### 2.4 全量套件与基线（不隐瞒）

`pnpm test`（运行标识 `ae6aacd9-57cc-4e63-8459-bd602157f682`，**exit 1**）：`tests 3253／pass 3170／fail 83`。83 红**逐件按文件分组**全部落在 `packages/skill-calorie/**` 与 `test/calorie-*.test.mjs`（`help-center-88` 10、`sport-homogeneity-109` 8、`calorie-routing-81` 7、`help-center-106` 6…），与仓里开着的 #88／#40／#86／#654 一族同源；**无一件引用本票改动面**（按失败文件的 `skill-memo|contract-lark|contract-seam` 引用检索，命中 0）。备忘／插件备忘／`test/memo-*`／`test/combos-*`／`test/synthetic-write-659` 全绿。

## 三 · 老不变量 ⇄ 新侧读数（M-01～M-13）

| 编号 | 不变量（老） | 新侧当刻 | 读数落在哪 |
|---|---|---|---|
| M-01 | 记一条心愿＝一条命令同时做本地 ＋ 飞书任务 | 是（合成写） | T1（本地落 ＋ 远端建，一次调用） |
| M-02 | 到期日字段名 `due`，仅心愿生效、非心愿静默置空 | 是 | T11（非心愿 due＝null 且零远端调用） |
| M-03 | 本地侧建前不查重 | **偏离 D-20**：心愿这条合成写本地也判重 | MUT-H／T2 幂等读数 |
| M-04 | 远端侧建前查重只在有到期日时执行 | **偏离 D-16**：一律查重 | MUT-D／T9 |
| M-05 | 归属标记逐字 `原备忘 #{memo_id}`，写进描述；反查正则只扫描述 | 是（正则按本仓 id 字符集放宽） | T1（`--description` 逐字 ＋ 四条读数·归属） |
| M-06 | 标识回写 `feishu_task_guid`，且只在拿到标识时写 | 是（**取不到标识即抛**，不写空） | T1（本地行 `feishuTaskGuid` ＝ 回执 `remoteId`） |
| M-07 | 五个同步操作齐备 | 是（建／改题／标完成／改期／清期） | T4（五条 argv 各一条断言） |
| M-08 | 删心愿：远端只标完成、没有删除路径 | **偏离 D-17**：两侧都确保没有（`task +delete`） | MUT-B／T6 |
| M-09 | 反向对账三步 | 是（三步 ＋ 11 项统计） | T7（步 1 补建／步 2 完成／步 3 改期） |
| M-10 | `create_task` 没有幂等键 | 照实：仍无幂等键，防重靠 M-04 那条自建查重（口径见 D-15／D-16） | T3／T9／T10（复用而非重复建） |
| M-11 | 远端不可用时本地照落，回执记 `synced:false`，**退出码 0** | **偏离 D-19**：本地照落 ＋ `remote:'unavailable'` 分格 ＋ 退出码非 0 | T2·降级／T7 首段／MUT-F |
| M-12 | 飞书自检 sentinel | **未落**（票面五项不含它；老缺口表把 D-03 挂在 #661） | 见第七节 |
| M-13 | 远端拉取没有分片（全量 ＋ 逐条补 due） | 照实：本侧同样全量 ＋ 逐条 `tasks get`（分片是老作息侧的事，备忘侧口径不变） | T7 步 3 |

## 四 · 有意偏离清单（追加行 D-15～D-21 · **待签**）

清单正本＝裁定件 `docs/skills/skill-calorie/t593-A-跨技能调用-裁定.md` 第四节那张表；本席**未改那份未入仓的正本**（它现在在盘上是未跟踪状态、不属本票交付路径），按收口机制 §五.2 在**本票证据件里登记并标「待签」**，请本图负责人签字后并回正本。

| 编号 | 老行为 | 新行为 | 为什么 | 出处 | 两行机器读数 |
|---|---|---|---|---|---|
| D-15 | 查重键用**未截断全文**，写入用 `[:200]` 截断标题（长正文必然比不中 ⇒ 重复建） | 查重键与写入键同为 `taskTitle()` 的 200 字截断 | 同一件事一个口径（铁律二） | 老 `feishu_sync.py:313` vs `:327`；新 `src/fetch/tasks.ts` 的 `searchTasks`／`src/fetch/taskWrite.ts` 的 `taskTitle` | 改坏必红＝MUT-C（`--query` 用全文 → T10 红）；还原必绿＝T10 |
| D-16 | 建前查重**整段锁在 `if due_iso:`** 里，无到期日的心愿完全不查重 | 一律查重（无到期日按标题查） | 契约：两侧各按自然键判重，无例外 | 老 `feishu_sync.py:312`；新 `src/wish/taskSync.ts` | 改坏必红＝MUT-D；还原必绿＝T9 |
| D-17 | 删心愿：本地先硬删，远端**只标完成**（任务永远留着；远端失败不可回滚、无补偿入口） | 删心愿＝**两侧都确保没有**：先删远端、删干净才删本地；远端删失败先复核「是不是已经没了」（幂等），仍在则本地不删并如实报 | 用户故事 13；也顺手堵上「先删本地再动远端」那条不可回滚的缝 | 老 `memo_cli.py:407`→`:414-417`、`feishu_sync.py:834`；新 `src/wish/taskRemove.ts`／`ensure.ts` 的 `removeWish` | 改坏必红＝MUT-B；还原必绿＝T6 |
| D-18 | 对账步 2 走本地 `complete-wish`：**删掉心愿 ＋ 生成打卡记录** | 对账只把心愿**标完成**（不动分类、不生成打卡） | 转打卡是完成向导的事，不属于对账链；对账只表达「远端完成了」 | 老 `feishu_sync.py:648-655`；新 `src/wish/reconcile.ts` | 改坏必红＝MUT-E；还原必绿＝T7（含 `D-18` 断言） |
| D-19 | 远端不可用／没达成时：回执只有一句 `synced:false`，**退出码仍 0**（对账不可用直接返回全 0 静默） | 回执 `remote` 一格分 `unavailable`（连不上）／`failed`（调失败），退出码反映「这一趟算不算达成」（非 0） | 契约 A3／A6 ＋ 用户故事 6（上层不能误以为办完了） | 老 `memo_cli.py:74`／`:176`／`:184`、`feishu_sync.py:579-584`；新 `src/wish/ensure.ts`／`src/cli/cmd_read.ts` 的收尾退出码 | 改坏必红＝MUT-F；还原必绿＝T2·降级（退出码 4）／T8 |
| D-20 | 本地侧建前不查重（直插 INSERT，无 SELECT／无 UNIQUE） | 心愿这条合成写**本地也判重**（自然键＝分类＋标题＋排期日期）；非心愿分类保持原样（不把「记两条一样的备忘」也变成幂等） | 契约：两侧各按自然键判重，责任全在被调方 | 老 `memo_cli.py:140`／`:143`、`init.sql:68`；新 `src/wish/ensure.ts` 的 `localWishRow` | 改坏必红＝MUT-H；还原必绿＝T2·幂等（`1→1`） |
| D-21 | HELP 广告：删心愿「自动标完成」／对账「生成同步报告页(11 项统计)」 | 文案改「会一并删除」／「并回执 11 项统计。」（对账三步与 11 项统计已落到运行面） | 票面第 5 条：HELP 与实装对齐；改的是生成链的声明表，产物由生成器重出 | 老 `references/scenarios.yaml` 的 prompt（经 `gen-help-assets.mjs` 声明 2／声明 5 清洗）；新 `scripts/gen-help-assets.mjs` | 改坏必红＝MUT-G（手改产物一字节 → `--check` 红）；还原必绿＝`--check` OK ＋ HELP 五件绿（`bd500138…`） |

## 五 · HELP 三处广告与运行面的对齐结果

| 广告（生成物 `src/help/scenes/*.ts`） | 运行面 | 结论 |
|---|---|---|
| 「AI 设置本地排期日期,并与飞书任务同步。」（`memo_wish_schedule`） | `memo.update` 改期／清期 → 远端 `+update --due`／`+update --data {"due":null}` | 对齐（T4） |
| 「AI 创建心愿笔记,自动建飞书任务并建立关联。」（`memo_add_wish`） | `memo.create`（心愿）→ 本地落 ＋ 远端建 ＋ 标识回写 | 对齐（T1／T2） |
| 「AI 执行 3 步对账…并回执 11 项统计。」（`memo_sync_feishu`） | `memo.sync` 三步 ＋ 回执 11 项统计 | 对齐（T7／T8）；**同步报告页本身不在本条交付面**（见第七节） |
| 「…若有飞书任务,会一并删除。」（`memo_delete_wish`，本次改写） | `memo.remove` → 远端 `task +delete` | 对齐（T6） |

改法与旧值：`gen-help-assets.mjs` 声明 2／声明 5 两行改写；摘要锁 `清洗后 30 条 sha256` 由 `7c72fb92…5827` 改为 `ada2433d…86a4`（`--check` 绿；`t227-assets-report.md:83` 那行是 t227 当时的读数，作为历史记录保留、不回头改）。

## 六 · 必报五步 · 第五步交付对账

| 第一步声明的面 | 实际碰到的 | 偏差 |
|---|---|---|
| `src/wish/**`（新增） | `due.ts`／`mark.ts`／`gate.ts`／`taskSync.ts`／`taskRemove.ts`／`ensure.ts`／`reconcile.ts`／`index.ts` | 无 |
| `src/fetch/**` | 新增 `tasks.ts`／`taskWrite.ts`；改 `db.ts`（due／feishuTaskGuid）／`errors.ts`（`LARK_TASK_FAILED`）／`index.ts` | 无（比声明多一个 `errors.ts` 的一档错误码，理由：要与「连不上」分开记） |
| `src/cli/cmd_read.ts` | 分发层改 `{data, exit}` ＋ 六键接线 ＋ 收尾退出码 | 无 |
| `test/**` | 新增 `packages/skill-memo-ilife/test/wish-sync-661.test.mjs`；改 `test/memo-e2e.test.mjs`／`test/combos-p8.test.mjs` 的假 lark 挡板（补任务域应答） | 无 |
| 生成链 | `scripts/gen-help-assets.mjs`（两行声明 ＋ 摘要锁）＋ 重生成 9 件 | 无 |
| `tooling/contract-lark-stub.mjs` | 加一条 `task +delete`（D-06 要的删除在老实现里没有形状可照） | **跨出本包交付路径的唯一改动**，第一步已点名 |
| 证据件 | `t658-A-心愿排期移植-证据.md`（本件）＋`门禁留痕.log`＋`t661-变异电池.mjs`＋`t661-变异读数.txt` | 无 |

## 七 · 未做项与下一手缺什么

1. **飞书自检 sentinel ＋ 授权引导（D-03）未落**：#659 的缺口表把它挂在 `#661`，但本条票面的五项不含它 ⇒ 按票面交付，另开票承接（老行为与三处改造见裁定件 D-03）。
2. **页面交付不在本条**：`memo.sync` 只回执 11 项统计，出「同步报告页」要接 `templates/sync_report.html`（以及排期／完成两张向导页）——那是备忘侧「向导与提醒域」那一支的事，另开票。
3. **偏离行待签**：D-15～D-21 七行登记在本件第四节，**尚未并回裁定件正本**（那份文件在盘上是未跟踪状态，不属本票交付路径）；请本图负责人签字后并入。
4. **全量套件 83 红不是本条引入**：逐件落在 `skill-calorie`，与 #88／#40／#86／#654 一族同源；本票改动面内 0 红（见 2.4）。
5. **本票未动 `packages/base-combos/**`、未改 `tooling/skilllink.mjs` 对外行为、未推真实飞书**（全部读数经 `tooling/contract-lark-stub.mjs` 挡板）。
