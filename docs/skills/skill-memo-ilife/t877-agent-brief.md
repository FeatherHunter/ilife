> *This was generated by AI during triage.*

## Triage Notes

**Category**: `bug` · **State**: `ready-for-agent`（诊断完毕，实施落点是一条改一个类名的活）

### 诊断结论

根因**已定**（不是候选）：新仓把页面壳从老公共层的 `hm-*` 类名切到 `base-render` 的 `ilife-*` 类名，`memo_query.html` 里那条栅格规则漏改。

- 产出侧：`src/render/memoPageAssets.ts:149-157`（`window.emptyState`）吐 `class="ilife-empty"`，本件 `:52` 自陈「空态卡 = 公共层 `ilife-empty-*` 三件」；
- 消费侧：`templates/memo_query.html:21` 的规则写成 `#list .hm-empty{grid-column:1/-1}` ⇒ 选择器对不上 ⇒ 规则空转 ⇒ 空卡落进两列栅格的第一列。

**判定它是新仓缺陷而非老技能遗留**：老公共层 `SKILLS\公共组件\assets\base.js:608-616` 的 `emptyState()` 吐的正是 `hm-empty`（样式住 `base.css:231-244`），老模板那两条规则与老公共层同名同字，老技能下本来是活的。

### 现象实测（1280 宽真渲染，整页 1280×1277）

| 件 | 横向 | 宽度 |
|---|---|---|
| 同页全宽面板（页头／筛选／快照／日志） | x=111 → 1169 | 1058 |
| 空状态卡「没有匹配结果」 | x=111 → 629 | 518 |

518 ＋ 10（`gap`）＋ 518 ＋ 11 ＝ 1058 ⇒ 逐像素吻合「两列栅格第一列」。390 档因 `memo_query.html:31` 的 `@media(max-width:820px){#list{grid-template-columns:1fr}}` 收单列，物理上看不见。

### 影响范围

列表查询页族 **9 格全带同一条空转规则**（搜备忘／查备忘／看备忘／按时间搜备忘／查心愿／查打卡／查情绪／看提醒／查已提醒备忘），但**只有 payload 为空、真走空分支的格看得见**（本墙 = 17 「查已提醒备忘」）。

同源死码 1 处（无可见症状，不夹带）：`templates/change_category.html:32` 的 `#noteList .hm-empty{grid-column:1/-1}`——该页空分支是 `list.innerHTML='<div class="panel">'+window.emptyState({…})+'</div>'`，分栏项是外层 `.panel` 不是空卡，故规则空转但症状被掩盖。

### 「有附件」占位横线：判**不是缺陷**

老契约逐字要求它这么显示：`SKILLS/备忘录/.scratch/templates-hardening/spec.md:75` =「B4 memo_query KPI "有附件" → 后端如未返字段,降级显示 "—" 而非 0(不隐瞒契约破缺)」。`看提醒`／`查已提醒备忘` 的行结构是 `MemoReminder`（`src/db/readonly.ts:32-42`，reminders 表本就没有 media 列）⇒ 不返该字段 ⇒ 按契约显示 `—`。**建议从本票剔除**，另判是否做规格变更（见 #820）。

---

## Agent Brief

**任务**：让列表查询页族的空状态卡在桌面档跨满两列。

**Why**：空卡只占左半列，右半屏整片空白；同页其它面板都满宽，视觉上像渲染坏掉。判据抓不到（零溢出、`toc=1`、机审六列全 0 都是绿的），只有双端真渲染看得见。

**改什么**

1. **必改一处**：`packages/skill-memo-ilife/templates/memo_query.html:21` —— 把 `#list .hm-empty{grid-column:1/-1}` 的类名改成 `ilife-empty`。
   - 方向必须选「改模板」而不是「改渲染件」：`ilife-empty` 是**公共层冻结契约的类名**（`base-render` 的 `renderEmptyState` ＋ `buildStyleSheet()` 的 `emptyState` 区，见 `docs/visual-spec-blocks.md:140-145`），`memoPageAssets.ts:52` 已自陈空态卡走公共层那一条；改渲染件等于自造第二处类名定义。
2. **顺手收同源死码（可选，无可见行为差）**：同一处 `templates/change_category.html:32` 的 `#noteList .hm-empty{grid-column:1/-1}` 改类名或直接删。本仓已无 `hm-` 类名体系（全仓 `hm-*` 只剩这两条规则 ＋ 一组 `hm-actions` 布局容器名）。

**怎么验（不靠肉眼）**

```powershell
# 1 重铺墙
node docs/skills/skill-memo-ilife/t856-gen-wall.mjs --stage <源> <批目录>
# 2 1280 档截 查已提醒备忘.html 整页，量空态卡宽 ≈ 版心宽：518 → 1058±1，左右边缘与同页其它面板对齐
# 3 族页三档零溢出
node packages/skill-calorie/scripts/measure-responsive.mjs --dir <页群> --widths 390,768,1440   # 须 OVERFLOW-ZERO
# 4 机审六列
node docs/skills/skill-memo-ilife/t869-机审.mjs --dir <页群目录>                                  # 须六列全 0
```

**别碰**：`src/config.ts`、`packages/plugin-memo-ilife/**`、老技能仓库（仓外只读）；不切分支、不 `reset`、不 `stash`。**别顺手改「有附件」那格**（老契约要求 `—`，见上）。

**收尾**：t834 的遗留发现须落回地图 [#820](https://github.com/FeatherHunter/ilife/issues/820)；`.scratch/t834/验收/` 不受版本管理，结论要另落盘，不能只留在 `.scratch`。

**提交前**：跑 `node tooling/run-locked.mjs --ticket 877 -- node node_modules/typescript/bin/tsc -b packages/skill-memo-ilife`；只提交自己声明的路径（`git add <路径>` ＋ `git diff --cached --name-only` 复核）。

---

## 进度：0%

下一步：按「改什么」第 1 条改模板类名 → 重跑四条验收命令 → 回读 1280 档实测卡宽回填本段。95% 时的待确认项：1280 档空态卡实测宽度是否 ≈ 版心宽（本票是纯视觉缺陷，该项未确认前不得 `close`）。
