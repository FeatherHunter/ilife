# 票 4（`#224`）决策草案 · 对抗式复审（复审员 A）

> 被审对象：`docs/skills/skill-memo-ilife/t224-decision-draft.md`（248 行，六节）
> 复审方式：**只读**。未改 `packages/**`、未改任何 issue、未 commit、未 `git add`。本文件是本次唯一新建的文件。
> 成文：2026-09-12。凡本文与草案不一致处，均给出可核的文件:行号或命令输出。

---

## 1. 裁决

**推荐：甲（备忘录自持一份最小管线，照 `#143` 裁决 (b)／`helpPaths.ts` 那一小块自抄），附三条硬条件；同时另开一张「备忘录迁入共用件」的票挂在 `#237` 之后。**

一句话理由：**乙把本图目的地押在另一张地图的队尾票上，而那张票自己还要等一次「新建目录层级」的用户点头，并且它的 `_N` 口径与本图老实物不同（行为漂移）；丙在本图的并发现实下必然造出「只有 1 个消费者」的共用件，比自持更不合仓规——甲是本图唯一既能在本会话内完工、又不违背用户原话语境的路。**

三条硬条件（缺一条即不算达标）：

1. **只抄通式那一小块**：独占创建（`wx`）＋ `EEXIST` 递补 ＋ `resolve` 绝对路径 ＋ 本地时间戳。**不搬**卡路里的命令名→中文段落映射／动态段／三态回退（`t224-body.md:7` 已写死这条边界）。
2. **`_N` 从 `_2` 起，与本图老实物一致**（见 §3 Q1「一处否决乙的技术理由」）。
3. **留可见欠债指针**：在自持件头注释里写明「此处是第 4 份同逻辑；共用件＝`#237` `saveHtmlFile`；备忘录迁入见本图新票」，并在本图另开一张迁移票、挂 `blocked_by = #237`。**理由**：`#237` 的覆盖面只写三家（`#237` 票面第四节），`#208:145` 又把备忘明确划出该图范围——**没有任何地方记录过「备忘录也要迁」**，不挂这张票，这份欠债就永远没人接。

---

## 2. 评分

### 2.1 草案的「决策质量」：**58 / 100**

**做对的（不减分）**

- 引证纪律好：几乎每条断言都带「文件:行号」，第五节 A／B／C 分三源（代码／老技能／磁盘）。
- **主动披露矛盾**（第六节）：`check-boundaries.mjs` 行号从 `:37` 漂到 `:41`、`#208` 那句「同形」理由自相矛盾、`_N` 起步数两套口径——这三条都是真的，我逐条复核属实（见 §3 Q1、Q5）。
- 第二节第 7 条三条硬规矩确实是用户原话，引用准确（对照 `map-220-body.md:158-163` 的采访问区）。

**扣分项（共 −42）**

| # | 扣分 | 说明 | 可核 |
|---|---|---|---|
| 1 | **−14** | **选择性引用用户裁定**：把「用户裁乙」当成无范围限定的通则，**完全没引 `#208:145` 那句范围排除**，也没引 `#208:30` 决议文里「**三家**（私家大厨／饼干记账／卡路里）」这个限定词。这是本草案最关键的事实遗漏。 | `#208` 正文第 30／145 行（§3 Q1 逐字） |
| 2 | **−10** | **把选项描述得比实际更好**：表格里乙的「证据」只写「`#208` Q7 已裁乙」，**没写乙的行为后果**——`#237` 票面第三节要统一改成 `_1` 起步，而本图老实物是 `_2` 起（`t224-decision-draft.md:234` 自己承认了这一点，却把它放在第六节「事实不清」，没并进第一节的选项代价）。一个会改产物名的选项，代价栏里一个字没提。 | 草案 `:21` vs `:234` |
| 3 | **−8** | **代价栏写轻了**：「选乙但不排票……`#237` 今天还没写第一步影响清单」（`:35`）。**真相更重**：`#237` 的下一步不是「写影响清单」，而是「**写影响清单报用户点头**」——`#237` 票面末尾逐字「新建目录层级必须先点头再动手」（`structure.md:88`）。草案把一个**人工审批闸门**降级成了一次文书动作。 | `#237` 票面「进度：0%」段 |
| 4 | **−6** | **「用户原话」与「地图作者转述」混用**：第二节表头写「用户原话（逐字）」，但第 2 条引的是 `map-220-body.md:83` 的 Notes 转述，第 1／3 条引的是 `## Destination`（地图作者写的目的地陈述）。用户对 Q3 的**真正原话只有一个字符 `Q3=A`**（`map-220-body.md:159`）。 | 见 §3 Q5 |
| 5 | **−4** | **把「管线归属」定性成必须用户裁的题**：草案第三节把它列为一页纸问 1。但 `t225-structure-design.md:101` 已实测「**这一条的成本差异只影响 A5 一个文件的落点与件数**」。为一个只影响一个文件落点的选择去卡住本图 frontier，不成比例。 | `t225-structure-design.md:101` |

### 2.2 被推荐方案（乙）的「帮助达成目的地」程度：**32 / 100**

判据＝地图目的地第一句：「在 DSH 真机对 AI 说『备忘录 help』→ **明确拿到 help HTML 文件**」（`map-220-body.md:3`）。问：**选乙，这句话能不能更早成真？**

- **乙对「拿到文件」零贡献**（−40）：备忘录要落的目录、文件名主体、内容、模板渲染，全部与本选项无关。乙只改「那 200 字节的写盘代码住在哪」。
- **乙把新的人工审批串进来**（−20）：`#237` 未开工、无 assignee、零评论（§3 Q2），且开工前要用户点头。
- **乙改产物名**（−8）：`_2` → `_1`（`t224-decision-draft.md:234`）。
- **加分项（+?，不给分）**：乙在「仓库长期整洁」这把尺子上确实更好，铁律一确实站在它那边。**但铁律一不是本图的目的地判据**——`map-220-body.md:5` 定的判定口径是「①明确拿到 help HTML 文件 ②用现成通用 help 模板渲染 ＋ 维护者肉眼终审」，架构规则是**过程基准**，不是完工判据。

> 合起来：**乙是「用本图的完工风险，去买别人图纸的整洁」。** 这个交换在仓库层面可能划算，但它必须由负责人明说「本图可以延后」，不能在草案里默认成立。

---

## 3. Q1–Q5 逐条作答

### Q1. 乙 vs 自持：哪一条真正帮助达成目的地？

**尺子一 · 用户原话效力（乙占优，但优势比草案说的小得多）**

用户对同一道题的原话确实存在，逐字是：

> `saveHelpFile -- 这个名字不好。叫 saveHtmlFile 感觉更好。我们用乙（建共用件，推荐）`
> —— `#208` 正文第 242 行（`gh api repos/FeatherHunter/ilife/issues/208 --jq .body` 实测第 242 行）

**但同一份正文对这句话的适用范围写了两处限定，草案一处都没引：**

> **Q7 已裁（2026-09-12）**：维护者取 **乙**——「命名与落盘」收成**共用位**，**三家（私家大厨／饼干记账／卡路里）**一起改成走它
> —— `#208` 正文第 30 行（**注意：「三家」是决议文自己写的限定词**）

> 其余 5 个技能（卡路里／饼干记账／居家管家／作息／**备忘**）的 HELP 交付：**本图只做私家大厨**。（票 13 是**例外**……）
> —— `#208` 正文第 145 行（「Out of scope」段）

**⇒ 「语境限定」能不能推翻「同类问题照办」？我的判定：能，但只推翻一半。**

- **不能推翻的一半**：用户在这道题上的**判断倾向**是一贯的（他要共用件）。这条倾向对备忘录同样有效，我采纳它——所以我在裁决里加了「另开迁移票挂 `#237` 之后」，而不是判「备忘录永远自持」。
- **能推翻的一半**：草案把这条倾向用成了「**本图现在就得照办**」。但 `#208:145` 把备忘**逐字点名划出去**了，`#208:30` 又由决议文自己把范围锁死在「三家」。**在一张已经把备忘排除的图里，用它的裁决去强制备忘录买单，是拿别人的范围裁自己的事。** 而且用户对「备忘的管线归属」**从没被问过**——本图采访区（`map-220-body.md:154-164`）六问里没有这一问，Q3 的取值是 `A`，全文没有这一问的题干。

**尺子二 · 本图完工风险（甲压倒性占优）**

目的地的判定口径是「拿到文件 ＋ 通用模板渲染 ＋ 维护者肉眼终审」（`map-220-body.md:5`）。乙对这三条**贡献为零**，却把「出口票 `#229`」挂到 `#237` 之后（见 Q2）。而且本图 9 号票是**下游咽喉**：`#229` 一卡，票 10（`#230` 用例）、票 11（`#231` 说明面）跟着卡，票 13（`#233` 真机终审）更在后面——`t229` 的「阻塞全部已解除」是三张 frontier 票的前提（`map-220-body.md:107`）。

**合判：甲。** 乙的正当性来自一个**已被本图排除的裁决**；乙的代价是**本图全部下游票**。

**一处否决乙的技术理由（草案自己找到、但用错了地方）**

草案第六节第 3 条发现：老备忘录 `_N` **从 `_2` 起**（`memo_render.py:133-140` ＋ 实物 `备忘录_HELP_20260810_093619_2.html`），而 `#237` 票面第三节建议共用件统一**从 `_1` 起**（照老家大厨 `align_08.py`）。草案把它归到「事实不清」，并建议「跟共用件走 `_1`」。

**我实测复核，并且把它升级为否决理由**：

- 我独立核了仓内三家现役实现，**全部是 `_2` 起**：
  - `packages/skill-bill/src/output.ts:26-27`：「无 `_N` 则 `_2`」；`:38` `return join(dir, stem + '_2' + '.html');`
  - `packages/skill-calorie/src/output.ts:143-144`：「无 `_N` 则 `_2`」；`:155` 同形
  - `packages/skill-bill/src/render/helpPaths.ts:13`：「同秒递补**从 `_2` 起**」
- 也就是说：`#237` 的 `_1` 不是「统一既有口径」，而是**要改掉三家现役行为**（这正是 `#237` 票面第五节为什么要求「先落行为快照，再迁移，再比对」）。
- **推论**：`#237` 的完成判据里含「三家行为逐字节不变」＋「统一 `_1`」两条互相拉扯的要求，这是一张**规格还没收敛**的票。让本图等它，等的是一个**会反过来改本图老实物口径**的东西——这不是「同一个东西只有一个定义地」，这是把一个未定案的规格引进来。

**⇒ 这一条足以单独否掉乙。**

### Q2. 若判乙，`#229` 等 `#237` 的现实代价是多少？

**先纠正一个事实。** 我的任务书说 `#237`「无阻塞边」。**这条不准确**，我实测如下：

```
gh api repos/FeatherHunter/ilife/issues/237 --jq '...blocked_by...'   → blocked_by=0  blocking=0  assignee=null
gh api repos/FeatherHunter/ilife/issues/215/dependencies/blocked_by    → 含 #237
```

`#215`（大厨票 7）的阻塞边里有 `#237`——这一点与 `#208:114` 的计划表一致（票 7 被 票 4 ＋ 票 6 ＋ 票 12`#236` ＋ 票 13`#237` 阻塞）。

**但这不改变结论，反而更糟**：`#237` 是**大厨那张图的咽喉**（票 7 等它），而它自己又要等用户点头才能开工。**备忘录去排这张队，等于同时排在「大厨票 7」后面。**

**`#237` 之前还排着什么（逐项带状态）**

| 序 | 项 | 状态（实测） | 为什么挡在 `#237` 前面 |
|---|---|---|---|
| 1 | **用户点头**（新包名＋新建目录层级） | **未发生** | `#237` 票面末段逐字「下一步：写第一步『影响清单』（含新包名提议）报用户点头——**新建目录层级必须先点头再动手**」；`structure.md:88` 是这条的来源 |
| 2 | 大厨闸门票 `#236` | `OPEN`，**无 assignee**，零评论，blocks 3 张（票 5／6／7） | `#208:34` 明写「**等你裁的三项**」（chip 路由／三项取值／体检归属）。`#208:32` 复述「`#236` 仍在等用户点头」 |
| 3 | 大厨票 5 `#213`／票 6 `#214` | 均 `OPEN`，均无 assignee，均被 `#236` 阻塞 | `#236` 是它们**共同的前置**；`#208:123` 的 frontier 就是这两张 |
| 4 | 大厨票 7 `#215` | `OPEN`，无 assignee，被 `#212`（已关）／`#214`／`#236`／`#237` 阻塞 | 与前 3 项构成一条串行链 |
| 5 | `#237` 自己的完成判据 | **进度 0%**，8 个验收项 | 见下 |

**`#237` 的完成判据（票面「完成的样子」段，逐条）**：新包落地 ／ **三家都改成走它，行为逐字节不变（迁移前先落快照）** ／ 三家本地不再各留一份 ／ **四道门全绿**（boundaries／`snapshot:html:check`／`snapshot:check`／typecheck）／ CLI 级真 spawn 锁（名字通式／同秒递补／`reuse` 两支／`onExists` 四态／退出码）。

**⇒ 本图目的地会不会因此拖出本会话？会，而且不是「可能」，是「大概率」。**

理由三条：

1. `#237` 现在 **0%**，它的第 1 步是**等人**。本图剩余票（`#227`／`#228`／`#229`／`#230`／`#231`／`#233`）全部在 `#229` 之后或与 `#229` 并行，`#229` 一挂就是全挂。
2. 对照本图自己的速度：本图一天内推进到 **5/14**（`map-220-body.md:9-11`）。`#237` 要「新建包 ＋ 定包名过用户 ＋ 迁三家 ＋ 逐字节快照不变 ＋ 四门 ＋ 一套新用例」——按本仓的节奏，这是**跨会话量级**的活，不是一轮能收的。
3. `#229` 现在原生 `blocked_by = {#224, #228}`，**其中没有 `#237`**（实测 `total_blocked_by=1`，即只算未关的 `#224`）。要走乙就必须**新增**这条边——也就是说，草案推荐的方案要**主动给自己加一道锁**。

### Q3. 有没有第三条路？三选一 ＋ 对比表

**先给对比表。表内每一格的依据都写在格子后面。**

| 判据 | **甲 · 自持最小管线** | **乙 · 等 `#237` 共用件** | **丙 · 本图先做共用件** |
|---|---|---|---|
| **是否合仓规** | **部分违反**：铁律一（`structure.md:23`「把对方那份抄一遍放进自己目录，不算走了接口」）。**但抄的是 calorie／bill 的**既成实现**，不是某个已宣告的共用件 | **合规**：走别人对外那一个门（`structure.md:13`） | **更差**：①`structure.md:30` 铁律二「同一个东西……只有一个定义地」——`#237` 一落地就有**两个** `saveHtmlFile` 定义地；②`structure.md:67`「共用件是从第二个用法里长出来的，**不是预先设计的**」——本图自建共用件正是「预先设计」 |
| **是否有 ≥2 消费者** | 不适用（不是共用件；住备忘录自己目录，`structure.md:67` 末句「写不出来的，留在那个能力的目录里」） | **是**：`#237` 票面要三家（大厨／记账／卡路里）；备忘录加入即第 4 家 | **否**（现实下：只有备忘录 1 家）。见下面的「第二家实测」 |
| **本图完工时间** | **最快**：不动 `#229` 的阻塞边，`#228` 一关即可开工 | **最慢**：`#229` 新增 `blocked_by #237`；`#237` 0% ＋ 第 1 步等人 ＋ 8 项判据 | **不可估**：要先等用户点头建包（`structure.md:88`），再迁第二家 |
| **并发撞车风险** | **最低**：只动 `packages/skill-memo-ilife/**`（本图自己的包）；且 `base-paint` **只消费不修改** | **中**：不改别人代码，但读一个会被别人持续改的接口——`#237` 落地后本图要再改一次 | **最高**：必须动第二家的包。见下面的实测 |
| **违背哪条用户原话** | 未违背「缺省＝HELP 文件」（`map-220-body.md:3`）；未违背入口只认 1 条（`:41`）；**只违背「用乙」的倾向**，而该倾向的适用范围是三家（`#208:30`／`:145`） | **不违背**；但**违背用户对 `#208:145` 的范围划定**——把备忘强行并进一张自己已声明「只做私家大厨」的图 | **违背** `#208:145`（把备忘塞进大厨图的范围）＋ `structure.md:88`（新建目录层级未先点头） |

**约束 2 实测复核：在这种并发状态下，丙的「迁移第二家」做不做得了？**

我按 `git -C D:\ilife status --porcelain` ＋ 各文件 mtime 自己核了一遍。**先纠正任务书里的一处说法**：

```
git -C D:\ilife status --porcelain   →  packages/skill-calorie/** 一条都没有（clean）
git -C D:\ilife diff --stat -- packages/skill-calorie   →  （空）
mtime  packages\skill-calorie\src\output.ts       2026-09-10 10:34:07
mtime  packages\skill-calorie\package.json        2026-09-10 10:19:17
```

**任务书说的「`packages/skill-calorie/**` 工作树里带别的会话未提交的改动」不成立**——`skill-calorie` 是**干净**的。有未提交改动的是另外两个：

```
 M packages\plugin-chef\package.json          2026-09-12 11:25
 M packages\plugin-chef\src\client.ts         2026-09-12 11:25
 M packages\skill-chef\SKILL.md               2026-09-12 11:25
 M packages\skill-chef\package.json           2026-09-12 11:25
?? packages\skill-chef\scripts\add-frontmatter-218.mjs
 M packages\skill-schedule\package.json
 M packages\skill-schedule\src\render\html.ts
 M packages\skill-schedule\src\render\index.ts
?? packages\skill-schedule\src\help\helpFile.ts       2026-09-12 13:08
?? packages\skill-schedule\src\shared\              （templateFill.ts）2026-09-12 12:30
?? packages\skill-schedule\scripts\gen-help-assets.mjs
?? packages\skill-schedule\src\help\scenes\
 M pnpm-lock.yaml                             2026-09-12 13:06
 M tooling\check-boundaries.mjs               2026-09-12 11:42
```

**逐家判定「能不能当第二家」：**

| 候选第二家 | 有没有现成的落盘管线可迁？ | 并发状态 | 判断 |
|---|---|---|---|
| **大厨** `skill-chef` | **没有**。`packages/skill-chef/` 下**无 `output.ts`、无 `helpPaths.ts`**（实测 `Get-ChildItem -Filter output.ts` 全仓只有 `skill-bill`／`skill-calorie` 两份）。它的出口管线还没写——票 7 `#215` 正是干这个的活 | `skill-chef/SKILL.md` ＋ `package.json` 未提交（11:25）；`skill-chef/src/cli/cmd_read.ts` mtime 是 09-07，**没被碰** | **迁不了**：没有可迁的对象。而且票 7 正被 `#236`＋`#237` 双重阻塞 |
| **记账** `skill-bill` | **有**：`src/output.ts`（三块可整块照抄）＋ `src/render/helpPaths.ts`（55 行） | **clean**（`git status` 无一条）。mtime 09-11 15:35 | **技术上可迁**——但这是**已完工且维护者肉眼终审「过」**的地图产物（`map-220-body.md:5` 的验收口径） |
| **卡路里** `skill-calorie` | **有**：`src/output.ts`（`:159-229`）＋ `src/render/helpPaths.ts` | **clean**（见上）。但 `docs/skills/skill-calorie/` 下有**两张未跟踪新文档**（`t179-copy-component-design.md`／`t179-ui-issues.md`），HEAD 最新提交 `3da2559 feat(skill-calorie): 场景 07 页面装配…（#179）` ⇒ **该技能有在跑的活** | **技术上可迁**——同上，也是已终审「过」的产物；且该技能有并发工作线 |
| **居家** `skill-home` | 需另查 | 未见未提交改动 | 未深查（超本次范围）；`#183` 图在跑 |
| **作息** `skill-schedule` | **有**：`src/help/helpFile.ts`（216 行，**未跟踪、12:30 刚写**）＋ 计划中的 `help/helpPaths.ts` ＋ `help/output.ts`（其 `helpFile.ts:3` 逐字写「落点通式与写盘全在交付管线（#203：`help/helpPaths.ts` ＋ `help/output.ts`）」） | **未提交，正在写** | **绝不能碰**：另一路会话正在同一个功能上施工，撞车即双输 |

**⇒ 结论：丙的「迁第二家」这一步，在本图的并发现实下做不了。**

- 唯一「本来就越界」的候选是**记账**（clean、现成管线），但迁它是**改一张已终审过的地图的产物**——超出本图 `Out of scope`（`map-220-body.md:130`「其余技能的 HELP 交付：本图只做备忘录」），必须另立票、另走一次必报五步、还要复跑 `snapshot:html:check`。
- 大厨（`#237` 自己点名的迁移对象）**根本没有可迁的文件**。
- 作息正在被另一个会话写，碰它等于制造 `#208:43` 记过的那类事故。

**⇒ 丙 = 造一个只有 1 个消费者的共用件 ⇒ 应被否决。** 约束 1 的推理成立，我采纳。

**为什么草案的推荐（乙）和我说的「另开迁移票」是两回事**

草案的推荐是「**本图等共用件**」；我的推荐是「**本图不等，但承认共用件，并把迁入记成一张挂账的票**」。差别在于：**本图的完工不依赖 `#237` 何时落地。** 这一条在 `t225-structure-design.md:101` 已经算过账：「**这一条的成本差异只影响 A5 一个文件的落点与件数**，不影响 A1～A4 与 A6」。

### Q4. 若走乙或丙，`#229` 的阻塞边怎么挂才对？（**只给方案，未执行**）

**先报一个必须先知道的事实**：`#229` 现在原生 `blocked_by = {#224（未关）, #228（未关）}`，**没有 `#237`**。走乙就必须**新增**这条边。

**实测拿到的数据库 id（执行时才用，本次未调用任何 POST）**：

```
#224 id=5430893649
#228 id=5430894220
#229 id=5430894331
#237 id=5431194298
```

**方案 A（推荐：不挂）** —— 走甲的默认。`#229` 保持现状，不新增任何边。这是保持本图关键路径最短的唯一选择。

**方案 B（若负责人明说「本图可以延后」＝选乙）**

```powershell
# 唯一一条：把 #237 挂成 #229 的前置（blocker = #237）
gh api --method POST repos/FeatherHunter/ilife/issues/229/dependencies/blocked_by -F issue_id=5431194298
```

- **不要**再挂 `#236`：`#236` 在 `#237` 的上游，挂它是冗余边（`#237` 自己会先解）。
- **不要**把「共用件三层」拆成 `#236`＋`#237` 两条：`#229` 只需要「`saveHtmlFile` 可用」这一个里程碑。
- 挂完后**必须复验**：`gh api repos/FeatherHunter/ilife/issues/229/dependencies/blocked_by --jq '.[].number'` 应含 `237`。

**方案 C（走丙：本图自建共用件）** —— 这一条**不该有阻塞边**，而该有**两张票**：

1. **新建一张本图票**「共用件 `saveHtmlFile` 落地」，挂 `blocked_by = #229`（出口票得先知道接口形状）；
2. **新建一张本图迁移票**「备忘录迁入共用件」，挂 `blocked_by = #237`；
3. `#229` **保持现状不挂**（自建包不再等别人）。

> ⚠️ 但如上判定，丙的**第二家消费者**无解 ⇒ 方案 C 不可采。

### Q5. 草案有没有把「已经定案」与「还需裁」分错？**分错了，而且方向一致地偏。**

**关键事实**：地图的采访问区里，用户对 Q1–Q6 的**真正原话只有取值本身**：

```
Q1=A
Q2=A 之前AI重构为新技能偷工减料少了很多底层的唤醒词、命令，在完成HELP HTML后会有MAP进行开发。我们要把HELP HTML这个官方源给做完善。以后MAP才有参照
Q3=A
Q4=A
Q5=A
Q6=A 没说不允许消费base
默认三条：照办
```
—— `map-220-body.md:156-163`（verbatim 区，一字未改）

**⇒ `Q3=A` 的题干不在图上。** 因此草案第二节里凡是标注「（Q3=A）」的引文，**都不是用户原话，而是地图作者对 Q3 取值的转述**。逐条核如下：

| 草案第二节条目 | 草案 claim | 我的核验 | 判定 |
|---|---|---|---|
| **第 1 条 落盘目录** | 用户原话逐字「产物落 `<SKILLS_DB_PATH>/memo_html/备忘录_HELP_<YYYYMMDD_HHMMSS>.html`，回执给绝对路径」 | `map-220-body.md:3` **逐字确实有这句**，但它是「## Destination」段——**地图作者写的目的地陈述**。用户对它的认可记录在 `:5`「判定口径（2026-09-12 与用户一轮对齐定案）」。**用户原话里没有这句。** | **标错来源**（应写「目的地陈述，用户已认可」），但**结论成立**：目的地是判定基准，`t233-body.md:5` 的验收也逐字要求落 `memo_html/` |
| **第 2 条 `help/` 那一层** | 「交付 HTML 落老目录 `<SKILLS_DB_PATH>/memo_html/`」（Q3=A） | `map-220-body.md:83` 是 Notes 里一行，括号写「（用户 Q3=A）」。**用户原话＝`Q3=A` 一个字符，无题干、无引文。** 草案把「带 Q3=A 标签的转述」写进了「用户原话（逐字）」表头下 | **原话支撑不成立**，须点名。**但结论仍成立**，靠三条独立证据（草案 `:230` 自己列的：Destination 扁平 ＋ 老 `memo_render.py:114-121` 只建一层 ＋ 磁盘实测四家兄弟目录扁平）。**这正是草案自己第六节第 1 条承认的「事实不清」那一块** |
| **第 3 条 文件名主体** | 「同上第 1 条原话逐字（目的地是唯一口径来源）」 | 文件名主体 `备忘录_HELP` 在 `:3` 的 Destination 里确实写了；**时间戳通式 `[_N]` 部分来自老 `memo_render.py:602` 与老实物，不是用户说的** | **一半成立**：主体 ✅；通式与 `_N` 起数＝**仓内实测**，应单列 |
| **第 4 条 缺省口径** | 用户原话「在 DSH 真机对 AI 说『备忘录 help』→ 明确拿到 help HTML 文件（落盘＋可打开）」 | `map-220-body.md:3` 第一句，逐字属实 | **✅ 成立**（这是全文最扎实的一条） |
| **第 5 条 不写固定名镜像** | Notes `:83`（`#131`／`#143` 已裁） | `map-220-body.md:83` 确有；且 `#208` Out of scope 段同款语句也有**用户原话**（`#208` Out of scope 第 1 行） | **✅ 成立**（但依据是「别人图的用户原话 ＋ 已关票」，应照实写，别挂「用户原话」） |

**⇒ 点名：**

- **第 2 条** 的「用户原话」claim **不成立**（依据是 Notes 转述，用户原话仅 `Q3=A`）。
- **第 1／3 条** 的「逐字」claim **来源标错**（是地图作者的目的地陈述，不是用户原话；`_N` 起数更是老代码实测）。
- **第 4 条** 成立，但**它证明的是「目的地」**，不是「缺省行为的机制」——「缺省＝文件而速查走显式参数」这个**机制**是草案从 `#139`／`#144` 先例推来的（草案 `:47` 自己也这么写）。
- **结论**：草案第二节的表头「**用户原话（逐字）**」对它 5 条里的 **4 条**不准确。**这不改变落盘目录／`help/` 层／文件名主体的结论**（结论有独立证据），但**改变了「谁还能反对它」**——一旦贴上「用户已定案」，后面任何人再问就是违纪；而实际上 `#229` 票面 `:17` 明写这条**归票 4 裁**。

**另外三处我需要纠正草案的「已定案」表述：**

1. 草案 `:6` 一句话总览说「**五件事**已被用户原话 ＋ 磁盘实测定案」。按上面核验，**只有第 4 条（缺省＝HELP 文件）有用户原话支撑**；其余是「目的地陈述 ＋ 老代码 ＋ 磁盘实测」。**表述应改成「已定案（依据分三类）」**，别一律挂「用户原话」。
2. 草案 `:86` 说这三条「都不需要再问负责人」。**我同意结论**，但建议把 `#229` 票面 `:17` 那句「归票 4 裁」**显式关掉**并写清依据，否则下游会以为这道题还开着。
3. 草案 `:234` 建议「走乙就跟共用件走 `_1`」。**我反对**——见 Q1 末尾，`_1` 会改掉本图老实物口径，而 `#225:6` 已定「行数口径 LF」，本图对「与老实物并排」有硬要求（`t233-body.md:5`）。

---

## 4. 我推翻成功了吗？

**部分成功。**

**成功的部分（推翻「乙」为核心推荐）**

1. **驳倒了草案最重要的一条论据**：草案理由 1「用户在同一轮的判法没有只为一家改的理由」——`#208:145` 逐字把**备忘**划出该图范围，`#208:30` 由决议文自己写「**三家**」。「同类问题照办」在**本图被逐字排除**的情况下不适用。
2. **发现草案遗漏的硬事实**：`#237` 的下一步是「**报用户点头**」（新建目录层级，`structure.md:88`），不是草案写的「写第一步影响清单」。这把乙的等待从「一张文书」升级为「一道人工审批＋ 0% 的 8 项判据」。
3. **发现 `#237` 是另一个咽喉**：`#215`（大厨票 7）的 `blocked_by` 含 `#237`（实测）。备忘录排 `#237` 的队，等于同时排大厨票 7 的队。
4. **升级了草案自己发现的 `_N` 问题**：草案把它当「事实不清」，我实测三家现役实现**全部 `_2` 起**（bill `output.ts:26-27`／`:38`；calorie `output.ts:143-144`／`:155`；bill `helpPaths.ts:13`），证明 `#237` 的 `_1` 是**要改三家行为**，即该票规格未收敛。**这一条单独足以否掉乙。**
5. **推翻了任务书的一处事实**：`packages/skill-calorie/**` 工作树是**干净的**（`git status` 无一条、`git diff --stat` 空、mtime 09-10）。真正带未提交改动的是 `plugin-chef/**`／`skill-chef/**`／`skill-schedule/**`／`pnpm-lock.yaml`／`tooling/check-boundaries.mjs`。
6. **推翻了约束 3 的一半**：`packages/skill-memo-ilife/package.json:32` 的 `test` 确实是 `node --test ../../test/scaffold.test.mjs`（盖不到包内 5 个用例）——**但仓根 `package.json` 的 `test` 脚本已显式含 `"packages/skill-memo-ilife/test/*.test.mjs"`**（我实测读取），所以 `pnpm test` 一层是覆盖到的，「CI 完全盖不到」这个说法不成立，**只是 `pnpm --filter skill-memo-ilife test` 盖不到**。`SKILL.md` frontmatter＋`files` 已修，我复核属实（`package.json:16-20` 含 `"SKILL.md"`；`SKILL.md:1-4` 有 `---`／`name`／`description`）。
7. **发现草案与在跑的票 5 已经分叉**：`t225-structure-design.md:98-101`（`#225` 结构设计正本）已经把 A5 `src/help/manifest.ts` 设计成「**逻辑全在别处**」并明确「本图**不自持抄件**」，同时承认「若裁甲，这一条的成本差异**只影响 A5 一个文件的落点与件数**」。**草案把一个只影响一个文件的选择，升级成了阻塞 frontier 的用户裁决题。**

**没成功的部分（我推翻不了、且草案是对的）**

1. **铁律一确实站在乙那边**。`structure.md:23`「把对方那份抄一遍放进自己目录，不算走了接口——抄的那份迟早和原份走散」是真的、且直接命中甲。甲**确实**留了记录在案的债，我没有豁免它，而是把它变成**挂账的迁移票**。
2. **`structure.md:67`「共用件是从第二个用法里长出来的」也站在乙那边**——落盘管线的消费者在代码里实测 **2 家**（bill `output.ts`／calorie `output.ts`，我实测全仓只有这两份 `output.ts`），第 3 家（大厨）由 `#237` 承接。备忘录是第 4 家，**不是**「第二个用法」。这条**恰恰说明共用件该由 `#237` 建，而不是由本图建，也不是本图必须马上消费**。
3. **草案第六节的三条「事实不清」我逐条复核，全部属实**（`check-boundaries.mjs` 行号漂移实测 `SKILLS_BASE_FROZEN = ['skill-chef','skill-home','skill-memo-ilife']` 在当前 `:41` 行附近，`skill-schedule` 已移出；`#208` 那句「同形」理由与五个目录里四个无 `help/` 的事实相反；`_N` 两套口径）。**这三条披露是有价值的。**
4. **草案的出口方向修正（第四节）完全正确**，我复核属实：`packages/skill-memo-ilife/src/cli/cmd_read.ts:129` 在分派前无条件开库（草案引的行号对），修法照 `skill-bill/src/cli/cmd_read.ts:493-495`。**这一节我全盘采纳，不打折。**

---

## 5. 若判丙

**我的判定是「丙不可行」，所以本节不是推荐，而是「若负责人强行判丙，必须同时满足的两个前置」——按任务书要求照给。**

**前置一 · 包名与落点候选（2 个以内）**

| 候选 | 目录／包名 | 理由 | 风险 |
|---|---|---|---|
| **①** | `packages/base-html-out/` → 包名 `base-html-out` | 与仓内既有「目录名 ≠ 包名」惯例并列（实测：目录 `base-render`／包名 `base-paint`）；`base-` 系是仓内**公共层**的现有家族，不占用技能名 | ① 铁律四（`structure.md:44-49`）要求名字**取自 HELP 的现成说法**，「落盘／输出 HTML」在备忘 HELP 的 8 个域里**没有现成说法**（域＝`memo`／`search`／`remind`／`wish`／`checkin`／`mood`／`sync`／`init`，`t225-structure-design.md:39-48`）⇒ 名字**必然是自创**，正是铁律四点名的样子；② `structure.md:88` 新建目录层级**要用户点头** |
| **②** | `packages/base-file-out/` → 包名 `base-file-out` | 同上，且 `file` 比 `html` 更贴「只做落盘、不管内容」 | 同 ① 的两条；③ **与 `#237` 的既定范围重叠**——`#237` 票面第六节已声明「包名由本票第一步连提议一起报给用户」⇒ 本图另立一个包＝**同一件事两个定义地**（铁律二） |

**② 的危险高于 ①**，而**① 和 ② 都过不了铁律四与「不可预先设计共用件」两道**——所以我**不建议判丙**。

**前置二 · 迁移三家的最小安全顺序（考虑并发会话）**

如果强行做，顺序只能是：

1. **先不迁任何家**，先把新包建出来、把 `saveHtmlFile` 的接口与用例做出来（这一步是本图可控的）；
2. **`#237` 的属主明确接受这个包**（否则第 3 步一落地就有两个定义地）——**这一步是人工协商，本会话无权代替**；
3. 第二家消费者**只能从记账或卡路里里选一个**（大厨没有可迁文件；作息正在被另一会话写）。两者都 clean，但都是**已终审「过」的地图产物**，迁移须另立票、走必报五步、复跑 `snapshot:html:check` ＋ `snapshot:check`（`#237` 票面第五节同款要求）；
4. 三家（大厨／记账／卡路里）按 `#237` 票面第四节的表**由 `#237` 自己迁**，本图只负责第 3 步选中的那一家；
5. **永不碰** `packages/skill-chef/**`（11:25 有未提交改动）、`packages/skill-schedule/**`（12:30／13:08 正在施工）、`packages/plugin-chef/**`（11:25 有未提交改动）。

**⇒ 这条链的第 2 步就卡死了：`#237` 的包名权在别人手里。所以丙不可行，我判「否」。**

---

## 6. 给编排会话的落地清单（按优先级，8 条）

1. **把 `#224` 的推荐从乙改成甲**（自持最小管线），并把「本图另开一张迁移票挂 `#237` 之后」写成同一条决定。**不要**给 `#229` 挂 `#237` 的阻塞边。
2. **给自持件写死 `_N` 从 `_2` 起**，并在草案第六节第 3 条把「建议跟共用件走 `_1`」**改掉**——`_1` 会与本图老实物（`备忘录_HELP_20260810_093619_2.html`）及 `#233` 的「与老实物并排」验收冲突。
3. **修正草案第二节的表头与来源标注**：把「用户原话（逐字）」改成「已定案（依 Destination／老代码实测／既有裁决）」，并在第 2 条（`help/` 那一层）**明确写出「本条无用户原话支撑，依据是三条独立证据」**（草案 `:230` 已有这三条）。这一步是防「假定案」堵住后续正当异议。
4. **把 `#229` 票面 `:17` 那句「落盘目录要不要加一层 `help/`——归票 4」显式关掉**（结论：不加，扁平），否则下游以为这道题还开着。
5. **另开一张本图新票「备忘录迁入共用件 `saveHtmlFile`」，挂 `blocked_by = #237`**，票面写清「迁入前提＝`#237` 落地且包名已获用户点头」。**理由**：`#237` 票面第四节只列三家，`#208:145` 又把备忘划出，**没有任何现存票记录过备忘录要迁**——不挂这张，第 4 份代码的欠债永远没人接。
6. **把 `#224` 第三节的一页纸压缩成一条**：删掉「问 1 管线归属」（按草案 `t225:101` 它只影响一个文件的落点，不值得占负责人一次裁决），**只留「问 2 速查支名字与参数名」**——那一条才是真正需要用户给名字的（`备忘录_速查表`／`mode:"lookup"`／`q`／`--html`），且 `t225:151` 的 `LOOKUP_FILE_STEM` 正等它。
7. **出口方向修正（草案第四节）原样保留并优先落**：`packages/skill-memo-ilife/src/cli/cmd_read.ts:129` 无条件开库这条是本图真正的硬骨头，归 `#229`、兼记 `#228`，与管线归属无关，**不被本次裁决影响**。
8. **`#224` 关票后立刻解锁 `#225`**：`t225-structure-design.md:15` 明写「票 7（`#227`）与票 8（`#228`）都被本票挡着」——`#224` 一关，本图 frontier 就只剩用户点头那一道门。

---

## 附 · 本次复审用到的复核命令（幂等，可重跑）

```powershell
# 1) #237 状态（OPEN／无 assignee／零评论／下一步＝报用户点头）
gh issue view 237 --repo FeatherHunter/ilife --json number,state,assignees,comments,body

# 2) #208 的「三家」限定与「备忘被划出」
(gh api repos/FeatherHunter/ilife/issues/208 --jq .body) -split "`n" | Select-String '三家|其余 5 个技能|Q7 已裁'

# 3) 原生边：确认 #229 没有 #237，而 #215 有
gh api repos/FeatherHunter/ilife/issues/229 --jq '.issue_dependencies_summary'
gh api repos/FeatherHunter/ilife/issues/215/dependencies/blocked_by --jq '.[].number'

# 4) 工作树并发真相（注意：skill-calorie 是干净的）
git -C D:\ilife status --porcelain
git -C D:\ilife diff --stat -- packages/skill-calorie

# 5) 全仓只有两份 output.ts；三家的 _N 都是 _2 起
Get-ChildItem D:\ilife\packages -Recurse -File -Filter output.ts
Select-String -Path D:\ilife\packages\skill-bill\src\output.ts,
  D:\ilife\packages\skill-calorie\src\output.ts -Pattern "stem \+ '_2'"

# 6) 仓根 test 脚本已含 memo 包内用例
(Get-Content D:\ilife\package.json -Raw) -match 'packages/skill-memo-ilife/test'
```

**未查到的（如实报）**：`skill-home`（居家）是否已有可当第二家的落盘管线、以及其工作树并发状态——本次未深查（超出 Q3 所需；大厨／记账／卡路里／作息四家的判定已足以否决丙）。
