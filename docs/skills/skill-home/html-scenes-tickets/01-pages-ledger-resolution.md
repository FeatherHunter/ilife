## 结论

册子已产出：`docs/skills/skill-home/pages-ledger.md`（49 行页族表）。

老技能 `references/scenarios.yaml` 引用的 **49 个模板全部存在**（49/49，无缺失），即 **49 个页族**；本图实做 **46 族**（49 − `link` 域 3 族，`link` 已裁不做）。

**页面类型分布**：混合 27／查看 11／采集＋回执 6／选择＋回执 3／向导 2。
**按域页族数**：items 19、space 4、outfit 5、stats 4、express 4、receipt 4、family 2、setup 4（＋link 3，不做）。

## 这张票改掉的地图事实（三条，均已落进地图与票面）

1. **本期是 8 个域、70 条场景、46 个页族**，不是「9 个域」——`link` 已裁不做（用户 Q7）。地图 Destination 与票 20 的墙体口径按此订正：**8 对墙**（手机＋桌面），`link` 无产物不出墙。
2. **产物命名会互相覆盖**：老实现是「模板 → 一个中文名」的 1:1（`scripts/render/__init__.py:130-147`），`物品/receipt.html` → `操作回执_<戳>.html` 承担 4 条场景，`物品/add_form.html` → `录物品_<戳>.html` 承担 4 条。这条**直接堵住票 4（落盘）与票 7（总览／墙清单）**，已当场开票 **#836**（命名裁决），并给这两张票加了原生阻塞边。
3. **一处口径订正**：「开始使用四模板无渲染调用点」应改为「`scripts/` 无调用点；`first_use_wizard.html` 全库唯一调用点在 `tests/test_开始使用.py:622`；另外 3 张在任何地方都没有调用点」。票 19 正文已同步。

## 其余实测（册子 §三 有证据行号，供各域票引用）

- 老技能票据凭证域**写类 11 条只打 JSON 不落 HTML**（核实成立）；且 `SM6-13`／`SM6-14` 根本没有自己的子命令，只是 `cert add` 的 `--scene` 取值。
- 家庭协作域**只有它**把 HTML 做成 opt-in（`scripts/home_manager/home_manager.py:712-723`／`:776-787`），其余域 CLI 全部直通自动落盘。
- 老 `templates/` 另有 **18 个 legacy 平铺件**不被 yaml 引用，但其中 17 个仍挂在 `TEMPLATE_TO_COMMAND_CN` 上——老实现是「根目录平铺旧件 ＋ 域子目录新件」两套命名长期并存，legacy 不等于死件。
- 「73 条场景都有实现」不成立：`SM6-4` 在老 `scripts/` 无任何入口。

## 自检读数（本席复跑，与册子自报一致）

- 页族表行数：**49**（口径要求 ≥40）；
- yaml 引用模板去重 **49**，盘上缺 **0**；
- 编码：无 BOM、无不带签名的 LF（CR 字节 0）。
