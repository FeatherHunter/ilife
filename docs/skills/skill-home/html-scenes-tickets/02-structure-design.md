## Question

70 条场景的页面要在 11 张票里并行做出来，先得把**契约**一次裁定：形状、产物命名、页族归属、域内写集怎么切、共用位归谁。这五条是同一件事的五半——**写页面前裁不完，11 个执行席就会各按自己的理解写，最后谁也没法合并**。本票吸收原「产物命名裁决」票（#836 已并入，见地图正文）。

## 目标

产出契约文档，**先报用户点头再动**：

① **形状**：8 个能力目录名（取自 HELP 一级分组：`items`／`space`／`outfit`／`stats`／`express`／`receipt`／`family`／`setup`；`link` 只留登记位、不建目录）＋每个能力的标准件（照卡路里：`index.ts` 能力门／`commands.ts`／`routes.ts`／`pages/<页族>.ts`）。
② **产物命名**（给票 4 与票 7 的硬约束）：老实现是「模板 → 一个中文名」的 1:1（`scripts/render/__init__.py:130-147`），`物品/receipt.html` 与 `物品/add_form.html` 各承担 4 条场景 ⇒ 照它落盘**会互相覆盖**。三选一，各给利弊与至少三个真实例子：(a) `<命令中文名>_<场景 id>_<戳>.html`；(b) `<唤醒词>_<戳>.html`（推荐——与老规范形态最像、人一眼看出是哪个场景；SM3-4「带物品/归物品」按老技能综合名取 `出行清单`）；(c) `<命令中文名>_<唤醒词>_<戳>.html`。
③ **页族归属表**：46 个页族 ↔ 70 条场景的对应（票 1 册子 `pages-ledger.md` 表一已给出族与场景，本票定「一族一个装配件」的落法）。
④ **域内写集**：同域多张票（items 三张、receipt 两张）靠**页族文件**天然不相交——页族落 `templates/<域>/<族>.html` 与 `src/<域>/pages/<族>.ts`，一票只写自己那几族。
⑤ **共用位所有权**：`src/render/**` 共用件、`package.json`、`SKILL.md`、派生件归票 3（唯一写者）；域票一律不碰。
⑥ **锁上限**：跑 `tooling/run-locked.mjs` 一律带 `--max-wait-ms 600000`（协议规定最多 10 分钟、不无限等；实测默认值 0＝无限等，现场最长等待 23.2 分钟）。
⑦ **页族解析规则**：今天 `src/render/templates.ts:33-58` 是「一个命令 key → 一个模板」的 1:1 映射，而本图要的是 **46 个页族**——一个 key 底下挂多条场景（例如 `home.item.update` 同时覆盖 移物品／数量变更／状态变更／标物品／改物品／合并物品／撤销操作／物品关联／管照片），**一个模板表达不了**。契约必须定死：**由 `(key, preset/场景) → 页族` 两层解析**，并给出 46 个页族名与命令 key 的对应总表。（**哪些唤醒词落到哪一族**由票 22「唤醒词层规格」裁，本票只引它的结论，不重复裁。）
⑧ **页族的界，按信息结构划，不按老模板文件划**：老结构是历史产物——`物品/receipt.html` 一条模板同时承担 移物品／数量变更／状态变更／标物品 四条**信息结构并不相同**的故事；反过来也有两条老模板几乎一模一样的。契约要按「**信息结构相同才同族**」给出族表，**允许合并与拆分**，并逐条写明理由（沿用老模板名只作为「信息结构对齐源」的可查锚点）。这条同时决定 46 这个数字能不能变。
⑨ **每条场景的「必需块」清单**：契约要为每族给出该族页面的**必需块**（字段／操作／空态／状态词，取自票 1 册子表一）——这张清单是**页面内容的结构判据**的来源（判据件见票 6、骨架登记见票 8）。没有它，域票的验收只能证明「文件在、不丑」，证不了「该显示的都显示了」。

## 验收命令

`(Select-String -Path docs\skills\skill-home\scene-pages-contract.md -Pattern '^## ').Count` —— 读数 **≥6**，且本票评论里**有用户点头的原话**（HITL 票，无点头不算过）。

## 不许动的东西

本票只出契约，不写代码：不改任何 `src/`／`templates/`；不碰生产库与生产产物目录（`D:\2Study\StudyNotes\.db`）。

## 交付物路径

`docs/skills/skill-home/scene-pages-contract.md`（契约 ＋ 给下游票的硬约束）。

## 遗留出口

契约里给票 3／4／7／8 与 11 张域票的硬约束逐条链接；裁出「本图不做」的写进地图 Out of scope。
