## Question

老技能的归档命名是「模板 → 一个中文名」的 1:1 映射（`scripts/render/__init__.py:130-147` 的 `_auto_output_path()` 查 `TEMPLATE_TO_COMMAND_CN`），**不分场景**：`物品/receipt.html` → `操作回执_<戳>.html`，而它承担 3-2 移物品／3-3 数量变更／3-4 状态变更／3-8 标物品 四条场景；`物品/add_form.html` → `录物品_<戳>.html`，1-2 拍物品／1-3 批量录入／1-4 补录／3-1 改物品 四条全落成「录物品_*」。本图要的是「**70 条场景各有自己的真产物**」＋一张链路总览页（一行一场景、点绝对路径就能打开）——照老规范直接落盘会**互相覆盖**，链路行会指向同一份文件。

## 目标

裁一条产物命名规则，同时满足：① 70 条场景一份产物、彼此不覆盖；② 文件名一眼看出是哪个场景；③ 与老规范尽量同形（落点仍是 `<根>/home_manager_html/`，形态仍是 `<中文名>_<YYYYMMDD_HHMMSS>.html`）；④ 走共用件 `base-paint/save-html`，居家自持只留落点值；⑤ 链路总览页与墙的清单由**同一份规则**算出（不许两处各算一次，仓规点名过这个坑）。候选（各写清利弊与至少 3 个真实例子）：
(a) `<命令中文名>_<场景 id>_<戳>.html`，如 `查物品_2-1_20260920_101010.html`
(b) `<唤醒词>_<戳>.html`，如 `移物品_20260920_101010.html`
(c) `<命令中文名>_<唤醒词>_<戳>.html`
产出：一条裁决 ＋ 给票 4（落盘）与票 7（总览／墙清单）的硬约束。

## 验收命令

`(Select-String -Path docs\skills\skill-home\scene-page-naming.md -Pattern '^## ').Count` —— 读数 **≥3**，且本票评论里**有用户点头的原话**（HITL 票，无点头不算过）。

## 不许动的东西

本票只出裁决，不写代码；不改 `scenarios.yaml`；不碰生产库与生产产物目录（`D:\2Study\StudyNotes\.db`）。

## 交付物路径

`docs/skills/skill-home/scene-page-naming.md`（裁决 ＋ 给下游票的硬约束）。

## 遗留出口

若裁决与 #183 票 4 已冻的落点值冲突，回写票 4 并当场补票；若发现 HELP 交付支（`居家管家_HELP_<戳>.html`）也要跟着改，回写票 2。
