本票**退役**，内容并入 [契约冻结 #799](https://github.com/FeatherHunter/ilife/issues/799)。

理由（第一性原理 + 两席对抗式审查）：命名规则与形状、页族归属、写集划分是**同一件要在写页面前裁完的事**；拆成两张票只是多造一个人工等待点（审查 A 把它记为饥饿窗口的来源之一）。三条候选（`<命令中文名>_<场景 id>_<戳>`／`<唤醒词>_<戳>`／`<命令中文名>_<唤醒词>_<戳>`）与「老实现按模板 1:1 会互相覆盖」的实测证据，已逐字搬进 #799 票面的第 ② 条。

本票原先承担的「集成与共用位派生」职责也**不再需要**：v3 让域票不新增命令（91 条唤醒词今天已在表里），派生件在票 9–19 期间不会变，派生件与共用位的单写者归票 3（机器落地）。
