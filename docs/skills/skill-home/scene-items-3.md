# 物品管理域（三）对账：照片、盘点与历史 8 条（#808）

链路：唤醒词 → 命令（真 spawn）→ 信封 → 本族 `renderFamilyPage` → 产物落盘 →
`manifest.json` → 双墙（手机 390／桌面 1280）→ 墙自检。
种子：票 5 仓内种子库（幂等补齐后拷入临时家，不碰生产库）；照片 8 件、盘点记录 3 条、
物品历史 65 事件（创建 62＋关联 3）。

## 场景×产物对照（8 行）

| 场景 | 唤醒词 | 命令＋预设 | 页族 | 产物（`.scratch/808/`，戳为运行时刻） |
|---|---|---|---|---|
| 5-1 | 查看照片 | `home.item.detail` view=photos | photos | `查看照片_5-1_<戳>.html` |
| 5-2 | 管照片 | `home.item.update` op=photo | photos | `管照片_5-2_<戳>.html` |
| 5-3 | 照片墙 | `home.item.search` wall=true | photo_wall | `照片墙_5-3_<戳>.html` |
| 6-1 | 盘点 | `home.inventory.round` op=round | inventory_round | `盘点_6-1_<戳>.html` |
| 6-2 | 差异处理 | `home.inventory.round` op=resolve | inventory_diff | `差异处理_6-2_<戳>.html` |
| 6-3 | 盘点记录 | `home.inventory.records` | inventory_records | `盘点记录_6-3_<戳>.html` |
| 6-4 | 搬家盘点 | `home.inventory.round` op=move | move_checklist | `搬家盘点_6-4_<戳>.html` |
| 7-1 | 历史 | `home.item.detail` view=history | history | `历史_7-1_<戳>.html` |

版式三张各表其形：5-3 网格墙（分组＋照片网格＋补拍引导）、6-2 分组处理
（缺／多／异／待确认四组＋动作集＋批量行）、6-4 二态标记清单
（分组＋二态＋统一确认），未压成通用列表。

## 验收读数（2026-09-21 现场）

- ① `node tooling/run-locked.mjs --ticket 808 --max-wait-ms 600000 -- node --test packages/skill-home/test/items-3.test.mjs` → **10/10**（8 场景＋2 机审前置）。
- ② `node docs/skills/skill-home/gen-scene-wall.mjs --check .scratch/808 物品管理-3-手机墙.html` → **8 格可发**（桌面墙同）。
- ③ 分隔符：**8 份产物 0 命中**；整目录 8/11，另 3 份（双墙＋总索引）是生成器自带问题，见 #866。
- ④ 结构块：**8 份产物块块在位**（22～33 块／份，族清单逐页断言）；整目录 8/11，另 3 份缺默认块是生成器壳的问题，见 #866。
- 脚手架三方对账（附录↔页模块↔登记表）：本域 7 族在 `scaffold.test.mjs` 逐族装配中全绿。

## 每格该确认什么（与清单 `check` 同字）

1. 查看照片：主图与类型选择及查看态动作齐全。
2. 管照片：管理回执与三处照片动作可用。
3. 照片墙：网格分组与补拍引导完整。
4. 盘点：三态判定与修正入口齐备。
5. 差异处理：四组动作与批量确认可用。
6. 盘点记录：记录卡与复查入口齐备。
7. 搬家盘点：二态标记与统一确认可用。
8. 历史：时间线与轨迹筛选可用。

## 视觉复核记录（本票内）

- 手机墙整墙渲染：8 格标题与确认语齐，首格内容正常；其余 7 格 iframe 在无头
  渲染下空白（产物单页直渲正常，系无头子帧加载假象，真机点标题开整页不受影响）。
- 照片墙单页直渲（390 宽）：分组＋网格＋类型 chips＋补拍引导＋动作齐，触摸键
  44px，820 断点在位；据此修掉两处视觉冗余（模板与内容双 `cmd` 行并存、
  机器拼接标题如“分组分类位置”改自然词）。
- 桌面墙同源（1280 宽单列缩显示），墙自检双墙可发。

## 机审写法（后人照做）

- 含拉丁字符的必需块（`物品（名称/ID）`、`当前mode`、`差异分组missing/extra/diff/pending`、
  `时间线（N条）`、`事件条目（类型/摘要/diff展开）`、`历史盘点（N）`、
  `补充态：还有N件无照片→去补拍`、`点图复制详情prompt`）只进 `data-need` 属性，
  可见文案用中文意译——英文裸词行零容忍，属性对机审不可见、对块门可见。
- 可见文案禁 `badge|item-head|stat` 子串类名（版式位），超 6 字可见行逐行唯一
  （重复句），回执里的英文原词（`all`／`location`／`mode`／照片文件名）只进数据原文。
- 类名禁用词也含 `status|state`（含 `stat` 子串），本域用 `st` 系以外的中性名。

## 已知缺口（遗留出口，不在本票私自放宽）

1. 收据型四族（6-1／6-2／6-4／5-2管理态）与记录族（6-3）的信封偏薄：
   差异明细行、记录发生时间与缺多异计数、核对清单条目、照片二进制均不在信封里，
   本页按回执如实呈现任务台（标识＋动作＋话术组装），未伪造明细。
   要“老页同款信息密度”需命令侧增补场景载荷（动 `src/items/` 命令实现，与 806／807
   共用文件，需跨票协调），建议另补票；照片类型落点另有 #857。
2. 历史时间线无发生时刻（命令侧拼串时 dropped `created_at`），本页以序号代替时刻。
3. 墙与索引三份文件的机审口径冲突＋脚手架 `--check` 全局 61 项漂移：已补票 #866，
   待票 6／票 7／票 2 侧裁决；#808 因此停 95% 待确认，未关闭。
