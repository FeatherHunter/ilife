# #890 执行证据：居家场景页收口后的剩余项

判定：**部分完成（不 close）**。页面侧零星 8 项里 7 项已落、共用件 3 项全部有当刻读数、取数层值一组已落；
剩 seq 24 一处未落（写集被在途票占用，见末节）与六维复评未跑。

## 一 · 提交（每笔只含本票声明的路径）

| sha | 内容 |
|---|---|
| `eb9e181a` | 页面侧零星与共用件：46 窄档 `.x-row`／43 说明句／43–46 复制路径走公共层共享运行时＋删 `xCopy` 与两颗空壳／27 折叠区四格字段位／15 删重复行／22 与 7 位置值剥复合串（新立 `render` 的 `locationPath`）／1·2·3·4·11 状态候选改真控件／`SHARED_CSS` 补 `.fam-head` |
| `199d4925` | 取数层值：票据凭证域五类命令补齐（`listPurchases`／`listWarranties`／`listCerts` 加厚、`maskCertNumber` 立唯一规则、六类写回执逐字段回显、`buildReceipt`／`buildTicketList` 可选新参） |

## 二 · 门禁运行（对账源：`.scratch/locks/gate-runs.log` 的 `ticket=890` 条目）

产物目录 `.scratch/t890/raw-post4`（70/70 真命令链落盘）。

GATE-RUN runId=0a3c4c14-06d6-44c4-aa87-6c32a54e5817 cmd="node node_modules/typescript/bin/tsc -b packages/skill-home"
GATE-RUN runId=c29356fd-bb32-4167-9f2b-b55194e4000c cmd="node packages/skill-home/scripts/audit-page-blocks.mjs --dir .scratch/t890/raw-post4 --blocks packages/skill-home/scripts/page-blocks.json --manifest .scratch/t890/raw-post4/manifest.json"
GATE-RUN runId=b9dc7f58-f289-49d3-a17b-498f2b677d27 cmd="node packages/skill-home/scripts/audit-separators.mjs --dir .scratch/t890/raw-post4 --manifest .scratch/t890/raw-post4/manifest.json"
GATE-RUN runId=3a77d955-77af-4018-8c98-dbd9eacb10c1 cmd="node packages/skill-home/scripts/audit-responsive.mjs --dir .scratch/t890/raw-post4 --widths 390,1280 --json .scratch/t890/resp-post4.json"
GATE-RUN runId=4c2edda2-c952-4792-b80b-e2965655f980 cmd="node --test <包内 29 个 test 件，含 naming-discipline 以外全部> test/scaffold.test.mjs"

读数：`tsc` exit 0／结构块 **70/70**／分隔符 **70/70**／双端与触摸 **70/70 cells=140**／包内套件 **344/344**。

两个生成器（不经加锁包装器，是纯生成器不是门）：
`gen-scene-wall.mjs --stage` → `70 格；链接 354 条；缺失 0 -> 可发`；
`gen-chain-page.mjs` → `70 行；链接 70 条；缺失 0 -> 可发`。

## 三 · 逐条读数要点

- **46**：媒体块 `.x-row` 规则＝`flex-direction:row;align-items:center`（改前 `column;stretch`）。
- **22／7**：可见位置格＝`客厅/阳台柜`；`×1[在家]` 只剩在 `data-*` 载荷与「数据原文」里。
- **7（「在家」拆两行）**：复现不出来——390 档 `.kv.rest` 第 4 列 `display:none`（实测 w=0／h=0）。
- **27**：「记录N缺— 多— 异— 待确认—」可见命中 0 处。
- **15**：可见区「合并记录」0 处；头卡「已合并到 63」与「变更后 → 并入数量」各承载一半。
- **1·2·3·4·11**：真浏览器读数 12 颗 `<button>`、最窄短边 **44px**、带 `data-action-id`（改前 12 颗 33–34px 的 `<span>`、零监听）。
- **43–46**：页内 `xCopy` 与 `xCopyData`／`xCopyLog` 全部为 0 处；真点击读数＝未勾选 `data-t=""`＋拦截提示、勾选后 `data-t` 按当刻选择重写、共享运行时 toast 真出现（headless 里剪贴板两通道都被拒，走的是它自己的失败反馈分支）、页面报错 0。
- **`.fam-head`**：带标记的 26 页**全部**有规则（改前 4／26）；算后样式 12px／800／域色。
- **30–33 底栏**：复现不出来——390 档那颗按钮实测 44px 高、单行（30＝179×44；31／32／33＝85×44）。
- **65／66 复制区**：复现不出来——可见区 `data-t` 10／8 个，「数据快照」可见 0 次。
- **取数层**：47「—」70→30；48 28→12；49 1→0；50 回显查询物品；51／53–56／58 回执逐字段；52 七格全有值；57 八列全有值；61 四组（改前一组「其他」）。

## 四 · 未做项与下一手

1. **seq 24「去补拍」入口未落**：改法已定并已跑过全绿，但写集 `packages/skill-home/src/items/pages/photo_wall.ts`
   在本票收尾时被 **#859 的在途命名扫批**占用（当刻 `packages/skill-home` 下 67 个件未提交，
   该文件的 `PAGE_META.scenarios` → `rows` 就是它手笔）。按并发纪律 §1／§3.2.1 已**逐字撤回**、
   未带进提交。撤回前的原文与复现读数见 `.scratch/t890/t24-pending-patch.md`；#859 落批后照抄回填即可。
2. **六维复评未跑**（票面验收口径「整批综合分 ≥90/100」）：本轮只跑四门机检，未跑 §四之十四 那套独立评分席，故该条**未确认**。
3. **未接的门**：`test/naming-discipline.test.mjs` 是 #859 在途新建件，其提交自述「当前读数 FAIL 827 处，收口进行中」，且不在本包 `test` 串内；它点名的行与文件非本票改动。

## 五 · 并发现场交代

本票开工时 #865 正占用 `src/fetch/domains.ts`（本票取数层的必经件），故先做页面侧与共用件、
后做取数层；#865 于 2026-09-23T00:31Z 关票并提交（`630217b3`）后取数层才动，
两边没有落到同一个件上（地图 #912 Notes 里那句「开工前裁一次文件边界」因此现场不再需要裁）。
收尾时 #859 又开始全域命名扫批，占用面覆盖到本票两处写集（`photo_wall.ts` 已撤回，见上）。
