# #695 · skill-home 分包证据：路径类取值改读配置文件、删掉全部环境变量读取

- 票：GitHub issue #695（口径出处＝#675 解决评论）；分包范围＝**只动 `packages/skill-home`**。
- 时间：2026-09-18 22:26（+08:00）。
- 代码提交 sha：`06f5b34c7a19803efbbf5c1e747c36338db35587`（2026-09-18T22:26:59+08:00，14 件，+496／−99）。
- 形状样板：`packages/skill-chef/src/config.ts`、`src/cli/config.ts`、`src/fetch/paths.ts`、`src/help/manifest.ts`、`src/cli/cmd_read.ts`（逐行照抄结构、只改名字）。

## 一、改动件清单

源码（`packages/skill-home/src/`）：

| 件 | 改动 |
|---|---|
| `config.ts` | **新增**：`HOME_CONFIG_STEM = 'home'`、`HOME_CONFIG_DEFAULTS`（`db.dir=''`／`db.name='home.db'`／`html.dir='home_manager_html'`／`files.help='居家管家_HELP'`／`files.lookup='居家管家_速查表'`／`backup.dir='backups'`）、`loadHomeConfig`／`saveHomeConfig`／`resetHomeConfig`／`splitDirSegments` |
| `cli/config.ts` | **新增**：三个配置 key 的唯一定义地（`home.config.read`／`write`／`reset`）＋ `isConfigKey` ＋ envelope（`version/skill/shape/key/data`，读 `detail`、写与重置 `receipt`）＋ `mergeValues` ＋ 人话报错 |
| `fetch/paths.ts` | `resolveDbDir()` 读配置 `db.dir`（空串＝数据目录）；新增 `dbFilename()`／`DEFAULT_DB_FILENAME`；`resolveDbPath()` 保持 `mkdirSync` 语义；**删掉** `assertWritablePath` 与 `HOME_FORCE_PROD` |
| `fetch/index.ts` | 导出面同步：`resolveDbDir/resolveDbPath/dbFilename/DEFAULT_DB_FILENAME`（`assertWritablePath`／`DB_FILENAME` 出局） |
| `fetch/backup.ts` | `BACKUP_DIR_NAME` 常量换成 `backupDirName()`（读配置 `backup.dir`，空串＝`backups`）＋ `DEFAULT_BACKUP_DIR_NAME`；`resolveBackupDir()` 仍是 `join(resolveDbDir(), 那个值)`；zip 条目名与清单 `db_filename` 改 `dbFilename()` |
| `help/manifest.ts` | 三个落点值改函数：`helpDirName()`／`helpFileStem()`／`lookupFileStem()`；`HELP_HTML_DIR_NAME` 消失（默认值另立 `DEFAULT_HELP_DIR_NAME`）；家是**一段**目录，段串经 `splitDirSegments` 归一 |
| `help/helpFile.ts` | 只改注释：`dbPath` 的说法由「调用方从 `SKILLS_DB_PATH` 解析」改成「按配置项 `db.dir`（空串＝数据目录）＋库文件名解析」 |
| `cli/cmd_read.ts` | `preflight()` 只留 node 版本检查；三个配置 key 在**预检与形状表之前**拦下并直接打印 envelope；错误归类新增配置件那一档 `/(配置文件\|配置项\|测试缺隔离)/` → exit 1；`DB_FILENAME` 全部调用点改 `dbFilename()`；件头写死 `SKILLS_DB_PATH` 的两段注释改成「配置项 `db.dir`」说法 |

测试（`packages/skill-home/test/`）：

| 件 | 改动 |
|---|---|
| `cli.test.mjs` | `SKILLS_DB_PATH` → `ILIFE_CONFIG_DIR`（数据目录＝`<它>/data`）；「缺 DB 1」那条换成「测试缺隔离 ⇒ exit 1 ＋ stderr 含『测试缺隔离』」 |
| `fetch.test.mjs` | 删掉 `assertWritablePath` 与它两条断言；改成配置面断言：默认值表逐项对照 ＋ 落点＝`<配置目录>/data/home.db` ＋ 首次读落 `home.yaml` ＋ **缺 `ILIFE_CONFIG_DIR` 子进程响亮失败** |
| `backup.test.mjs` | 同上迁移；「库目录缺失」那条改成新配置目录（数据目录下无库） |
| `help-delivery-190.test.mjs` | 同上迁移；产物落点由 `<dir>` 改成 `<dir>/data`（落点值逐字仍写死）；⑤b 先建 `data/` 再占位；⑥ 「不建库」改判配置目录／数据目录／产物目录三处 `.db` 数＝0 |
| `config-695.test.mjs` | **新增**：四条（默认值对照／三个配置 key 真出口／配置真进执行路径／缺隔离响亮失败） |

文档：`packages/skill-home/AGENTS.md` 行数台账（`cmd_read.ts` 818→**876**，超 526；`db.ts` 434 不变）。

## 二、验收机器读数

### 1. 编译 exit 0

```
node tooling/run-locked.mjs --ticket 695 -- node node_modules/typescript/bin/tsc -b packages/skill-home
→ FINAL build exit=0
（log 尾：RESULT: ticket=695 runId=… exit=0，见 .scratch/t695-home/final-build.log）
```

### 2. 测试全绿

```
node tooling/run-locked.mjs --ticket 695 -- node --test packages/skill-home/test/*.test.mjs
→ FINAL test exit=0
  ℹ tests 62
  ℹ suites 7
  ℹ pass 62
  ℹ fail 0
  ℹ cancelled 0 / skipped 0 / todo 0
（log：.scratch/t695-home/final-test.log）
```

### 3. 零命中：`packages/skill-home/src` 下 `process.env` 命中 0

```
grep 工具：pattern=process\.env，path=packages/skill-home/src → No matches found
Select-String -Path src\**\*.ts -Pattern 'process\.env' → hits=0，files=（空）
```

### 4. 落点不变（默认配置；`ILIFE_CONFIG_DIR` 指向临时目录）

脚本 `.scratch/t695-home/acceptance-695.mjs`（读数 `.scratch/t695-home/landing.log`）：

```
写命令 home.item.add  exit=0
库路径  C:\Users\辰辰洋洋\AppData\Local\Temp\home695-landing-yEki4w\data\home.db  exists=true
读命令 home.help.lookup  exit=0
HELP 路径  C:\Users\辰辰洋洋\AppData\Local\Temp\home695-landing-yEki4w\data\home_manager_html\居家管家_HELP_20260918_222512.html
名字通式=true  exists=true
```

两条实际路径＝`<数据目录>/home.db` 与 `<数据目录>/home_manager_html/居家管家_HELP_<YYYYMMDD_HHMMSS>.html`，与改造前逐字相同。

### 5. 变异自证两行

- **红（改坏默认值表）**：`src/config.ts` 的 `db.name` 由 `home.db` 改成 `home_x.db` → 重编译 `exit 0` → 测试 **`ℹ pass 57 / ℹ fail 5`，退出码 `1`**（红的用例：`config-695` ①②③、`fetch.test.mjs` 的配置面那条、`backup.test.mjs` 的「zip 两件」那条）——log `.scratch/t695-home/mut-test.log`。
- **绿（逐字回改，未用 `git checkout`／`git restore`）**：改回 `home.db` → 重编译 `exit 0` → 测试 **`ℹ pass 62 / ℹ fail 0`，退出码 `0`**——log `.scratch/t695-home/revert-test.log`。
- **还原后与原文逐字节相同**：`src/config.ts` SHA256 变异前＝`0057D3E0BA60B96A7B2F63BA7DE310B8D4E11DF902951264A22523BEB9D9C250`，回改后＝`0057D3E0BA60B96A7B2F63BA7DE310B8D4E11DF902951264A22523BEB9D9C250`（同哈希同长度 4705 字节）。

## 三、未做项（本分包范围外，留给下一手）

1. `packages/plugin-home-ilife/test/smoke.test.mjs` 仍设 `SKILLS_DB_PATH` 并 spawn 技能 CLI——该包在本次禁写清单内（设置页／插件那个分包），需由持该包的席位改成 `ILIFE_CONFIG_DIR`。
2. `packages/skill-home/SKILL.md` 第 23／24／33／153／160 行仍写 `SKILLS_DB_PATH`（说明面，本分包未列入改动件）；若要一刀切口径，另立改文档小票。
3. 本包 `package.json` 的 `test` 串仍只跑 3 个测试件（不含新件 `config-695.test.mjs`）；收口跑的是 `node --test packages/skill-home/test/*.test.mjs` 全量（CI 的仓根 glob 也是全量），是否改 `test` 串由该包席位裁。
