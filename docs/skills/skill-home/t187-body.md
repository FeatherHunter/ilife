## Question

两件事一起定，定完票 7 照它做：

**① 命名与落盘的管线归属**：

- 照 #143 的裁决 (b)：居家**自持一份最小管线**（只抄通式那一小块，不搬卡路里的命令名→中文段落映射／动态段／三态）；
- 还是按新架构规则的共用件条款收成共用位——`docs/agents/structure.md`：**共用件是从第二个用法里长出来的**，并要写得出哪两个能力在用。今天的现实是卡路里／记账／居家已经是第三、四处在做同一件事。

要把新规则摆到桌面：铁律二（同一个东西只有一个定义地）与铁律五（接口小、里面厚）对这件事怎么说，以及 #147 当时的理由（`base-paint` 是被 #96 主动冻结的渲染包、落盘是 IO、技能间零依赖不可破）今天还成不成立。

**② 缺省出口口径**：

- 照 #139／#144 的判法：**缺省＝HELP 文件**（对 AI 说「居家管家 帮助」就落盘＋回执绝对路径），**速查走显式参数**；
- 还是保留现在 `home.help.lookup` 回一份列表的缺省行为。

要带上桌的事实：老目录 `<SKILLS_DB_PATH>/home_manager_html/`、老通式 `居家管家_HELP_<YYYYMMDD_HHMMSS>.html`、新表 3 条 HELP 唤醒词（居家管家 帮助／居家管家帮助／居家管家能做什么）、票 1 的照抄清单。

产出：决定＋理由，写回票面（含落盘目录、文件名主体、速查支产物名与显式参数名），并决定 `combo` 侧要不要同步登记。

## 进度：100%

下一步：无阻塞——四条裁决已定并经**两席独立对抗审查**（76／100、72／100，均「整改后通过」），全部必改项已落，另有 1 条审查意见**经一手证据驳回**（见下）。

**裁决全文**：`docs/skills/skill-home/t187-decision.md`；取证：`t187-facts.md`（共用件 5 家使用方、组合表 111 条）。

**四条裁决一句话**：

1. **命名与落盘的管线归属**：居家**自持只留「落点值」**（目录名 `home_manager_html` ＋ 文件名主体 `居家管家_HELP`），命名通式／独占写／递补／复用窗口／绝对路径回执**全部走共用件** `base-paint/save-html` 的 `saveHtmlFile`（`#237` 已收拢，账单／卡路里／大厨／作息／备忘**五家**都在用）。
2. **缺省出口口径**：**缺省＝HELP 文件**（回执追加 `delivery{mode,path,bytes}`）；**三支冻结**——缺省／`mode:"lookup"`（产物 `居家管家_速查表_<stamp>.html`，与 HELP 分名）／`q` 支保持不动，**三支出参一律仍回 `{items,total}`**，不得挪走；`--html` 支保持原样、**不许顺手砍**。
3. **combo 侧要登记**（原先裁「不登记」被取证推翻）：`packages/base-combos/combos.yaml` 今天 111 条，卡路里与备忘的 help 命令都在表里，备忘线有先例裁过「必须登记」；**落地归票 7**，成本不是「改一行 yaml」——还要同批重跑 `gen-present.mjs` 与 `build-help.mjs`，否则组合门直接红。
4. **新 HELP 件全部收进 `src/help/`**（取代设计稿的三处分散），落点件定名 `manifest.ts`（照大厨／作息最新先例）。

**被驳回的审查意见（附反证）**：审查要求「递补编号照老家从 `_1` 起步」。查居家的老家源码 `D:\2Study\StudyNotes\SKILLS\居家管家\scripts\render\__init__.py:130-147`，docstring 逐字写**「冲突直接覆盖」**——**老家没有 `_N` 递补机制**；大厨契约引的证据是私家大厨自己的老家脚本。故维持共用件缺省（`succession`，`_2` 起），不为边角情形给共用件开特例。

**给下游票的硬约束**（并入票 12 的七条之后，见决策文件 §12–§15）：票 7 开工前把「摘 `check-boundaries.mjs:55` 名单＋同批补 `base-paint` 依赖」写进第一步影响清单；`manifest.ts` 导出面按铁律五收小；包内告警线 **350**（生成物单列不计）落 `packages/skill-home/AGENTS.md`；**票 6 必须过滤 `deprecated` 分组**（`link` 以 `deprecated: true` 留在资产里作登记位），页面主数按实际列出派生 **70**、口径区写一行「骨架 73 条，联动 3 条已停用不列」。
