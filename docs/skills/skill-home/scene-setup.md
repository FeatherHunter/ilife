# 开始使用域对账（地图 #797 票 19／issue #816）

4 条场景各出一份真页面。老页面只作信息结构来源，UI 走新仓共用件与本页样式。

## 真链与产物

| 场景 | 唤醒词 | 真链 | 产物（`.scratch/816/`） |
| --- | --- | --- | --- |
| SM8-1 首次使用 | 首次使用 | `home.care.write {"kind":"init"}` | `首次使用_SM8-1_20260921_163539.html` |
| SM8-2 查异常 | 查异常 | `home.care.query {"kind":"lint"}` | `查异常_SM8-2_20260921_163539.html` |
| SM8-3 备份导出 | 备份导出 | `home.care.write {"kind":"backup"}`（另验 `backup-list` 查询同族） | `备份导出_SM8-3_20260921_163539.html` |
| SM8-4 导入恢复 | 导入恢复 | `home.care.write {"kind":"import-preview"}`＋`{"kind":"import",confirm:true}` 两步 | `导入恢复_SM8-4_20260921_163539.html` |

种子：2 件物品＋1 位家人＋1 份备份（舞台脚本 `.scratch/816/stage.mjs`，隔离家目录，不碰生产库）。
页族文件：`src/setup/pages/<first_use_wizard／health_report／backup_receipt／import_restore>.ts`（各 145–196 行，告警线 350 内）；模板壳 4 件未动；`test/setup.test.mjs` 5 例。

## 老实现偏差（遗留出口，供票 2 裁是否回写 HELP 内容资产）

1. 四模板在老 `scripts/` 无渲染调用点（`first_use_wizard.html` 唯一调用在老测试，其余三张零调用）——新页按用户裁决（Q10）做成真页面，本票已做。
2. SM8-2 老页称 8 检查项，新链只给 3 项（无标签／无照片／单级位置）：页按真值画几项画几项，不凑 8 项。
3. SM8-4 老页有逐条冲突名单＋跳过／覆盖下拉，新实现是整库覆盖（恢复前自动自备份）：冲突区如实写整库覆盖，不虚构名单；两种历史口径只作说明文字。
4. SM8-1 老页是可交互 6 步向导，新链是一次性回执：页按回执推导步骤状态（已初始化即全绿），环境值取进程实测，不虚构分步回执。
5. 票面验收③④的字面目录写法会把票 7 生成的墙与索引也扫进去：墙文字（`setup · 向导型`、命令键、文件名）与墙骨架不走产物合同——票 7 自带先例（`.scratch/804-demo` 同样墙红产物绿）。本票判据只对 4 份产物，读数见下节。

## 验收读数（2026-09-21，戳 20260921_163539）

- ① 自家测试：`node tooling/run-locked.mjs --ticket 816 --max-wait-ms 600000 -- node --test packages/skill-home/test/setup.test.mjs` —— 5/5 exit 0。
- ② 双墙自检：`node docs/skills/skill-home/gen-scene-wall.mjs --check .scratch/816 开始使用-手机墙.html`（桌面墙同）—— 4 格，链接 13 条，缺失 0，exit 0。
- ③ 分隔符（4 份产物）：版式位 0，英文 0，重复 0 —— 4/4 exit 0。
- ④ 结构块（4 份产物）：24/24、22/22、23/23、25/25 —— 4/4 exit 0。
- 双端实测（票 6 口径，额外）：390／1280 两档溢出 0，最窄可点 44px —— 4/4 exit 0。

## 视觉复核记录

- 手机页（首次使用，390 宽）：纵向堆叠正常，无错乱重叠溢出；按钮完整。采纳一条：完成态主按钮换成“开始录入第一批”（已落地，其余按钮顺序不变）。
- 备份页（390 宽）：标题卡片表格按钮齐全，无截断。
- 桌面墙 3、4 格在墙截图里发白：单页直截正常＋真浏览器双端实测全过，判定为墙截图时序（折叠下 iframe 未加载），非产物缺陷。
- 未采纳（契约锁死）：环境信息卡片下沉、按钮收折叠、说明区移抽屉——必需块必须在场，密度由合同定，不在本票砍。
