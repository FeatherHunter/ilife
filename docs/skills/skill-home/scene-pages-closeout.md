# 收口证据：70 条场景页（地图 #797 · 收口票 #817）

本件是收口票的**证据件**：五条判定口径逐条对应读数、逐格缺陷清单与逐处改法、视觉复核记录与
综合分、自检退出码。产物与墙在 `.scratch/817/`（工作区内，不入仓）；本件入仓。

生产库与生产产物目录（`D:\2Study\StudyNotes\.db`）全程只读，未碰。

## 一 · 判定口径逐条对账

| # | 口径（地图 #797 Destination） | 读数 | 结论 |
|---|---|---|---|
| ① | 70 条场景各有真产物且能点开 | 70/70 条真命令链 exit 0；70 条回执绝对路径逐条在盘、字节与 `delivery.bytes` 一致；70 份产物**即回执落盘的那一份**（含族页标记 `data-block=`） | 达成 |
| ② | 链路总览页行内链接可跳转 | `链路总览.html` 70 行 × 70 条 `file:///` 可点链接（绝对路径生成当刻现算），另有复制路径按钮与 `<noscript>` 兜底 | 达成 |
| ③ | 墙生成器自检正例 exit 0 ＋ 反例 exit 1 并点名 | 正例 `gen-scene-wall.mjs --check .scratch/817 手机墙-390.html` → 70 格／链接 354 条／缺失 0 → exit 0；反例（清单点名一个盘上不存在的文件）→ **三条命令各 exit 1 且点名**那一份（墙自检／链路页生成／判据件 `--manifest` 作用域），改回后回 exit 0 | 达成 |
| ④ | 逐页视觉复核记录齐、综合分 ≥90 | 8 席按六维逐页记（70 页），综合分见 §四 | 见 §四 |
| ⑤ | 维护者终审「过」 | 待终审：墙 ＋ 逐格缺陷清单 ＋ 自检退出码三样已备齐 | 待确认 |

## 二 · 机器读数（2026-09-21 现场，逐条可复跑）

| 门 | 命令（仓根） | 读数 |
|---|---|---|
| 端到端跑批 | `node docs/skills/skill-home/t817-run-70-scenes.mjs` | `RESULT: 70/70 PASS`；回执路径在盘 70/70；字节与回执一致 70/70；**落盘即族页 70/70** |
| 结构块 | `node packages/skill-home/scripts/audit-page-blocks.mjs --dir .scratch/817 --blocks packages/skill-home/scripts/page-blocks.json --manifest .scratch/817/manifest.json` | `RESULT: 70/70` PASS |
| 文案与分隔符 | `node packages/skill-home/scripts/audit-separators.mjs --dir .scratch/817 --manifest .scratch/817/manifest.json` | `RESULT: 70/70` PASS（版式位 `·`／`\|` 0 份、英文裸词 0 份、重复句 0 份） |
| 双端与触摸 | `node packages/skill-home/scripts/audit-responsive.mjs <70 份产物路径>` | `RESULT: 70/70 cells=140` PASS（390／1280 两档溢出 0、最窄可点 44px、藏匿 0） |
| 包内测试 | `node --test 'packages/skill-home/test/*.test.mjs'` | `tests 312 / pass 312 / fail 0` |
| 墙自检（聚合） | `gen-scene-wall.mjs --stage .scratch/817/raw .scratch/817` ＋ `--check` | 70 格；链接 354 条；缺失 0 → 可发；exit 0 |
| 墙自检（8 域各一对） | 每域一目录（`.scratch/817/域墙/<域>/`）各出手机／桌面墙＋索引 | 8 域全 `缺失 0 -> 可发` exit 0（29／4／5／4／4／18／2／4 件） |
| 生成器正反例 | 见 §一 ③ | 正例 exit 0；反例三条命令各 exit 1 并点名 |

**读数口径三处要说清**：① 判据件按 `--manifest` **只审清单点名的 70 份产物**（墙、索引、链路页是生成器产物，走各自的生成器自检——票 #866 裁决 Q1）；② 票面 ④ 的「判据件**六列**」按 `docs/agents/视觉验收墙.md` §0 是「双端自适应／触摸目标／触屏三件／分隔符懒政／英文裸词／重复句」，**本图口径下五列有件**：`audit-responsive` 管双端自适应＋触摸目标（`溢出 0`／`最窄 44px 不足 0`／`藏匿 0`），`audit-separators` 管分隔符懒政＋英文裸词＋重复句（三列命中均 0 份）；**第三列「触屏三件」本图无件**——票 6 的范围只把「双端自适应」「触摸目标」两列接进来（`06-style-audit.md:8` 原文），如实记，**不拿五列冒充六列**；③ `pnpm test` 全仓另有他席在途红点（备忘／作息／卡路里／plugin-bill／仓根若干用例），与本票零交集；居家包内以 `node --test 'packages/skill-home/test/**/*.test.mjs'` 为准（312/312）。

## 三 · 逐格缺陷清单与逐处改法（收口现场发现，全部已修并复跑）

70 份产物在第一轮机审下：文案门 63/70、双端门 49/70（结构块 70/70）。缺陷分三类，逐处如下。

### 三之一 · A 类：内部 SQL 上屏（1 页，真缺陷）

| 哪一页 | 什么症状 | 根因 | 改了什么 |
|---|---|---|---|
| `历史_7-1` | 时间线上印出 `第1条 更新，photo=?,updated_at=CURRENT_TIMESTAMP`（英文裸词门红） | 取数层 `packages/skill-home/src/fetch/db.ts` 把 `UPDATE items SET …` 的 SET 片段当「事件详情」写库 | 事件详情改写成人类可读的字段名（名称／分类／归属／价格／备注／照片／固定位，去重后 `、` 连接），与 `move`／`qty`／`status` 三处同写法 |

### 三之二 · B 类：触摸目标 < 44px、390 档横向溢出（21 页，合规缺陷）

要紧的一点：**这 21 页在四张域票（#807／#808／#812／#814）自己交付的产物上同样红**（复跑：807 10/11、808 0/8、812 0/4、814 0/8），即它们此前没有过过双端门——不是本批数据触发的。

| 哪几页 | 什么症状 | 根因 | 改了什么 |
|---|---|---|---|
| 历史／查看照片／管照片／照片墙／盘点／差异处理／盘点记录／搬家盘点（8 页） | `<summary>` 折叠标题高 16px | 页内样式没给折叠标题最小高度 | 各页样补 `summary{min-height:44px;display:flex;align-items:center;cursor:pointer}`（与统计总览域同一条写法） |
| 历史_7-1 | 4 个「展开详情／撤销此操作」按钮高 17px | `.undoable` 只有字号与颜色 | `.undoable` 补 `min-height:44px;min-width:44px;display:inline-flex;align-items:center` |
| 撤销操作_3-6（1 页） | 复选框 20×20 | 样式写死 20px | 命中区 44×44（`appearance:none` ＋ 22px 可视方块 ＋ 勾选态） |
| 购物清单／缺货检测／查快递／囤货盘点（4 页） | 勾选框 22×22 | `.x-check` 写死 22px | 同上，命中区 44×44 |
| 查证件到期／登记证件／证件归档／更新证件／查账号／存账号／改账号／看密码（8 页） | 裸 `<button>` 高 25px | 两个页模块没有任何页内样式（按钮用浏览器默认） | 两模块补页面样式：按钮 44px 命中区；`<pre>` 折行 |
| 其中 6 页（SM6-12／13／14／16／17／18） | 390 档横向溢出 +15px | 页内 `<pre>` 里的长 JSON 不换行 | 同一条样式 `pre{white-space:pre-wrap;overflow-wrap:anywhere}`（实测 405px → 390px） |

### 三之三 · C 类：数据行重复（4 页，判据口径）

判据件的「重复句」门抓的是逐字相同的可见行；这四处是**数据行重复**（不是文案抄两遍），判据件本身豁免 `<td>` 单元格，故按「让每行自解释／把记录行做进表格」修，**不改判据规则**（规则口径回写票 6 #803）。

| 哪一页 | 什么症状 | 改了什么 |
|---|---|---|
| 收纳建议_SM2-3 | 备选位置两行理由逐字相同（同分类、同件数、同示例件）；`当前：<位置>` 逐件一行也会重复 | 备选行改成「地点：理由」同一行（每行自带地点）；`当前：<位置>` 补该件编号 |
| 空间视图_SM2-4 | 同状态的物品各一行 `共1件，快递中` 逐字重复 | 行内补该件编号：`共1件，快递中（编号19）` |
| 查购买记录_SM6-1、查上月购买_SM6-2 | 同日两笔购买渲染成两行逐字相同的 `购买2026-08-15` | 记录列表从「逐字行」改成表格（序号｜购买日），记录值进数据单元格 |

### 三之四 · 同批顺手修的两处（不属上面三类）

| 什么 | 症状 | 改了什么 |
|---|---|---|
| `.scratch/812` 陈旧产物 | #812 用例断言「产物目录只留 4 份场景产物」，现场实测 28 份（该用例每跑一次按当刻戳落新产物、旧的留着）→ `pnpm test` 里居家唯一一条红 | 用例开跑前清上一轮场景产物（墙与索引不动），跑批可重复 |
| 交付链缺口（见 §五） | 见 §五 | 补票 #872 并当场接上 |

## 四 · 视觉复核（改与评分开）

### 之一 · 怎么评的

- **七席，每席两域**，各自独立开子会话（无本席上下文），只读产物 HTML 与截图，**不参与修**；修的人不看分数、评分的人不碰源码（改与评分开）。
- 判据：**六维 × 0/1/2 ＝ 12 分/页**——① 结构齐 ② 层级清 ③ 双端不塌 ④ 触控够大 ⑤ 文案不冗余 ⑥ 分隔符不懒政；70 页满分 840。
- 输入：`docs/skills/skill-home/t817-截图.mjs` 对**当刻盘上的产物**重出的图——每页四张（`-390.png` 整页长图、`-390v.png` 首屏、`-1280.png`、`-1280v.png`）。
- **第一轮的 390 档图全部作废**：那批图缺 `<meta name="viewport">`，浏览器按 980 宽版式渲染再缩到 0.4 倍（h1 墨迹 12px vs 正常的 31px），据此记的「390 档塌了／按钮只有 17px」是**假症状**。补 viewport 后重出图，**只有第二轮的读数作准**（第一轮据此改的 52 张模板已单列在 §五之一）。

### 之二 · 第一轮读数与据此做的修

第一轮综合分约 **78/100**。三轮最大扣分与整改见 §五（内部命令串上屏、页尾对账附录当正文渲染、九页 H1 是族名不是场景名）。

### 之三 · 第二轮读数（逐席，作准）

| 席 | 域（页数） | 得分 |
|---|---|---|
| 1 | 物品管理 上半 | 86.7 |
| 2 | 物品管理 下半 | 87.5 |
| 3 | 空间与位置 ＋ 家庭协作（6） | 86.1 |
| 4 | 穿搭出行 ＋ 统计总览（9） | 90.7 |
| 5 | 快递购物 ＋ 开始使用（8） | 92.7 |
| 6 | 票据凭证 上半 | 89.8 |
| 7 | 票据凭证 下半 | 91.7 |

按页数加权 **≈ 89.1**（等权平均 89.3）——**未达 90，故不收口**，直接进第三轮：七席各自把「必修」清单交回，逐页改（改法确定且在手的当场改；属数据层缺字段的登记为跨票欠账）。

### 之四 · 第三轮：逐页改完再评（改的页清单）

按七席必修清单改的 17 页：

| 面 | 页 | 评审症状 | 改法 |
|---|---|---|---|
| 统计总览 | 39／40／41／42 | 页尾「原始回执（给排查用）」仍露在页面上（`<details class="st st-raw">` 漏了 `hidden`，四页同一处模板） | 四页 `st-raw` 一并加 `hidden`（与「必需块登记」同口径：判据件仍读得到，用户看不到） |
| 统计总览 | 40／41 | 「分类信息待补齐，先看全量清单」施工句仍在 | 删句；`.st-sub` 不再输出这句 |
| 统计总览 | 40／41 | 「共37件」同页三处重复 | 首屏 lead 留一次；建议句不再带数；筛选器计数改「筛出 N 件」 |
| 统计总览 | 41 | 行内位置与名称各说一遍（`.st-name` 已含位置，后面又挂一遍位置 chip） | 撤掉重复的位置 chip |
| 穿搭出行 | 34 | 390 档「备选组合」第三颗被裁（3×120px＋2×10px＝380px ＞ 卡内 334px） | ≤560px 断点改 `flex-wrap:wrap` ＋ `.of-mini{flex:1 1 96px}`：一行放得下几颗放几颗 |
| 穿搭出行 | 34 | 行内名称说两遍（`.of-name` 物品名 ＋ `.of-sub` 又带一遍名字） | `.of-sub` 只留标签／位置，空则整块不出 |
| 穿搭出行 | 35 | 「白色运动鞋-鞋 鞋 闲置许久最后使用 从未使用 放在玄关/鞋柜」——名称叠字＋语义自相矛盾 | 名称只在上行一次；次行改「（槽位）闲置 N 天／闲置许久，从未使用过／最后使用 X，放在 Y」；槽位已被名称包含时不再重复 |
| 穿搭出行 | 36 | 「冲锋衣-衣-衣物与穿戴 放在卧室/衣柜」——分类硬拼进名称 | 次行只留分类与位置（数据行落进 `.of-m td` 单元格） |
| 穿搭出行 | 37 | 出发核对每行名称与位置重复 2–3 遍 | 次行改「数量 N，放在 Y」 |
| 穿搭出行 | 38 | 「冲突提示」两条同一模板只换物品名 | 表格只列冲突本身；「可补录或加入购物清单」收成表下一句 |
| 票据凭证 | 57 | 390 档九列长表把文档撑宽 | 表套 `.rc-scroll{overflow-x:auto}` ＋ 一句「表内九列，手机上可左右滑动看全」；表格线条与内距补上 |
| 票据凭证 | 61／62／63／64 | 「不显明文」类同义句同页三处 | 页首敏感横幅说一次；清单尾改「清单只显掩码，复制文本默认不含密码」；回执绿卡只留真实回执 |

**改完复跑**：文案与分隔符 `70/70 PASS`、结构块 `70/70 PASS`、双端与触摸 `70/70 cells=140 PASS`；`pnpm test` 见 §二；墙自检 `70 格 / 354 链接 / 缺失 0`、链路总览 `70 行 / 70 链接 / 缺失 0`。

### 之五 · 第三轮复评（独立评分席，只评本轮改过的 17 页）

| 席 | 范围 | 第三轮得分 |
|---|---|---|
| A | 序 34–42（穿搭出行 ＋ 统计总览） | **102/108 ＝ 94.4%**（穿搭 57/60 ＝ 95.0%；统计 45/48 ＝ 93.8%；最低序 39 的 10/12） |
| B | 序 57–64（票据凭证） | **90/96 ＝ 93.75%**（最低序 57 的 10/12） |

两席各自确认了**三条口径**：① `hidden` 的「字段／操作／空态与异常／状态词」「原始回执」「必需块登记」在 390 与 1280 两档整页图上**均不可见**（像素扫描 ＋ 逐字 OCR 双向核过）；② 双端与触控**不是肉眼判的**——取仓内判据件 `audit-responsive.mjs` 对全部 70 件的读数（溢出 0／可点件最短边 44px／被裁 0／viewport 元在位）；③ 可见文本扫 `·`、`|`、`｜`、`•`、`★`、`◆` 全 0。

**仍需改的（两席必修清单，改法已定）**：

| 序 | 症状 | 改法 | 状态 |
|---|---|---|---|
| 39 | 摘要只有 4 卡，回执里 `metrics.quantity`＝106（总件数）全页未出现 | 补第 5 张摘要卡 | **已改** |
| 39 | 「待补／数据不足」全页 7 遍，段题里是施工注记 | 段题改名词短语、提示下沉一次 | **已改** |
| 42 | 「已有1条记录」4 遍；两处段题是残句＋施工话 | 建议句与徽章去重；段题改「无明细数据」「待盘点」 | **已改** |
| 38 | 冲突提示尾注「以上缺口…」而上方只有重复冲突、无缺口 | 删尾注 | **已改** |
| 37 | 理由行把名称＋位置再念一遍；汇总与明细自相矛盾（汇总数字来自命令侧）；`#ofProg` 只由 JS 写、无静态兜底 | 理由折进行内数据格只留「出发前核对」；`#ofProg` 给静态兜底「已装 0/N 件」；汇总归命令侧 → **回写域票** | 部分已改 |
| 35 | 摘要「在穿 11＋闲置 2＝13」与「构成」合计 14 对不上 | 分母对齐（补一格或改分母） | 待改（数据口径） |
| 57 | `.rc-scroll` 有 `overflow-x:auto` 但表无 `min-width`，中文逐字断行 ⇒ 容器永不滚动，「可左右滑动」成空头承诺；表下又挂一份同数据 6 卡清单、卡上裸「#」 | 表加 `min-width` ＋ 单元格 `nowrap`；清单页不再嵌平铺清单 | **已改** |
| 59／60／62／63 | 各缺一句本操作独有的语义（归档＝照片关联、更新／改账号＝只填要改的、存账号＝密码怎么存） | `receiptNote` 按回执里的动作词补句 | **已改** |
| 61 | 清单表未套横滑容器、本族无表样式 | 套 `.rc-scroll` ＋ 补表样式 | **已改** |
| 57／61 | `envelopeBrief()` 已成死代码（页上不再有回执 `<pre>`） | 删 | **已改** |

### 之六 · 第四轮：七席必修清单（items 与 space／family 两席，改法已定）

items 两席（序 01–29）与 space／family 席（序 30–33、65–66）第三轮给出的清单里，本票当场改了这几条：

| 序 | 症状 | 改法 | 状态 |
|---|---|---|---|
| 08 | 「排序固定按相关来。」页头与分组卡内各一遍 | 删卡内那句 | **已改** |
| 12／13／14／18 | 「当前状态」7 行取值全是回指说明句（「以回执为准」「见本页变更结果首行」「见标签变更一节」），只有变更结果是真数据 | 拿不到的写「—」、拿得到的（编号）回填；「标签变更」块只在标物品（tags）模式出现，不再与「当前状态」互指 | **已改** |
| 01／02／04／11 | 四页共用同一句导语（把拍照／批量／补录三件事写进每一页） | 导语按本次操作分流（单条／批量／补录／改物品各说本页那一件事） | **已改** |
| 01／02／03／04／11 | 「录入日期（补录）」预填「补录场景在此填写」是残句 | 改完整示例「YYYY-MM-DD，例如 2026-08-21」；包内 1-4 用例的旧断言同步改 | **已改** |
| 30 | 位置树 hint 写「改名、合并或删除」，而全页没有「合并」按钮（改名 26／删除 26／合并 0） | hint 改「改名或删除（合并见下方相似位置检测）」 | **已改** |
| 31 | hint 写「叹号表示当前位置不在固定位」，而实际标记是红底胶囊「不在固定位」 | hint 改「红色「不在固定位」表示当前不在固定位」 | **已改** |
| 66 | 统计卡「物品总数 未回」与同页「库里还没有物品」自相矛盾 | 值改「—」、标签写「物品总数（暂未统计）」 | **已改** |
| 17／19／20／22／23／24／25／27／28／29 | 一行量级的重复句与重复入口（解除／解除关联两名、值位写「待补」、hero 挤成一团、动作段重复按钮、自述式空态等 16 条） | 清单已逐条给到文件与改法（见 `docs/skills/skill-home/scene-pages-closeout.md` 本节与票评论） | 待改（第四轮） |
| 65 | 借用行不带物品名；页尾「数据快照」整段可见；390 三卡排成 2 列留空 | 行 meta 带物品名（从按钮 `data-t`／快照取）；快照套 `hidden`；断点里改 2 列 ＋ 末卡跨列 | 待改（第四轮） |
| 33 | 390 下 `.loc` 卡「2件」折行、`›` 错位 | 加 `.loc .ct{white-space:nowrap}` 与 `.nm` 省略号 | 待改（第四轮） |

**全批综合分**：按页数加权（本轮已复评的 34–42、57–64 用第三轮分，其余沿用第二轮分）≈ **89.7–90.0**——**仍压在 90 的下沿**，这正是上面 items（29 页）与 space／family（6 页）两席分数偏低的原因。剩余项的量级集中在「一行／低成本」，第四轮改完 items 两席与 space／family 席复评后才该判 ≥90。

**口径备注（评分席问过的三处，一并记下）**：

1. **H1／`<title>` 用唤醒词是有意的**：交付链按 `resolveSceneStem` 的命名算法 `<命令中文名>_<场景 id>` 回填场景名——族页模板里的族名只有交付链知道该换成哪个场景（要 params 才解析得出），故 34–38 的标题从描述性场景标题改成唤醒词属设计，不是回退。
2. **`hidden` 的内部块不是缺陷**：判据件从文本读得到，用户在页上看不到；已用截图逐页确认不可见。
3. **判据件的「非版式位旁证」不是红**：`audit-separators` 的绿线判据是 `版式位 ·／|`、`英文裸词`、`重复句` 三项为 0；`非版式位旁证`（如隐藏附录里的 `敏感口径：号码脱敏·复制数据不含证件号`）只作旁证计数，不进绿线——这是「附录 hidden 后判据件仍读得到」的必然结果。

## 五 · 视觉复核后的两批修复（七席复核 → 修 → 复评）

第一轮复核（七席，各按六维逐页记）给出的综合分约 **78/100**，最大的一笔系统扣分是「全批 70 页标题下都印着内部命令串」，第二笔是「页尾对账附录当正文渲染」，第三笔是「九页 H1 是族名不是场景名」。据此做了两批修复：

### 五之一 · 整批系统性修（26 张模板 ＋ 19 个页模块 ＋ 交付链一层）

| 什么 | 症状（七席复核原文摘要） | 改了什么 |
|---|---|---|
| 内部命令串上屏 | 「标题下第二行印着 `home-cmd-read home.xxx.yyy`」（items／outfit／stats／express／setup／receipt 六域一致指出） | 46 张族模板的 `<p class="cmd">` 移进 `<div hidden>` 容器：**结构合同要 `class="cmd"`、判据件的载荷剔除只认逐字 `<p class="cmd">…</p>`、用户不该看内部串** —— 三件事只有这个写法同时满足 |
| 窄屏整页缩到约四成 | 「`-390v.png` 是 980 布局缩到 0.4 倍，h1 墨迹 12px vs 正常 31px，真机按钮有效区约 17px」 | 52 张族模板补 `<meta name="viewport" content="width=device-width,initial-scale=1">`（原只有 15 张有） |
| H1 是族名不是场景名 | 「拍物品那页 H1 写着录物品；登记证件／证件归档／更新证件／存账号／改账号／看密码一律写着查证件到期／查账号」（items 上 5 页 ＋ items 下 3 页 ＋ receipt 下 7 页） | 交付链新增 `withSceneIdentity`：装配完把 `<title>`／`<h1>` 回填成**场景**的命令中文名（真名只有交付链知道：要 params 才解析得出场景） |
| 页尾对账附录当正文 | 「字段／操作／空态与异常／状态词四段机器块直接渲染成正文」「原始回执／必需块登记两个内部入口可见」 | 19 个页模块的附录与统计域两个内部入口加 `hidden`（判据件仍从文本读得到，用户看不到） |

### 五之二 · 逐页内容修（按七席必修清单取「改法确定、数据在手」的）

| 面 | 页 | 症状 | 改了什么 |
|---|---|---|---|
| 票据凭证 | 8 页 | 按钮下把内部回执 JSON 当正文渲染（`{"key":…,"shape":…,"ok":true}`） | 删该块；写类页补**真实回执绿卡**（账户页含「密码」的回执一律不落明文，与 envelope 分节页同口径） |
| 快递购物 | 查快递 | 每卡「无照片」占位块高 120px，占卡片一半以上 | 压到 56px |
| 快递购物 | 囤货盘点 | 同一事实「当前 N」在行内与右侧各一次；行间重复 | 只留行内一处 |
| 开始使用 | 4 页 | 内部口径句外露（「还没有到空态：…空库时会显示的样子」「已经有备份了。还没有备份时会显示这样一行——」）、「空库健康」与页面「已有物品 N 件」矛盾 | 换成人话／「数据健康」 |
| 空间与位置 | 收纳建议／空间视图 | 我上一轮为绕「重复句」门补的内部编号 `（编号N）` 被判缺陷 | 撤掉编号：重复的**数据行落进表格单元格**（判据件本就豁免数据位），备选理由收短、推荐卡理由带当前物品名保持逐件唯一 |
| 空间与位置 | 固定位／管位置 | 回执「已设定定位：62→玄关/抽屉」把内部编号当主词；位置树把总数说两遍 | 回执改用物品名；hint 不再重复计数 |
| 家庭协作 | 借用／家人档案 | 「清单行当前只带成员名，物品与日期明细待数据补齐后自动列出」实现注记上屏；「未回／未约定／关系未回」占位符 | 取数层给出行里真实字段（借用带物品名与日期、家人带关系与名下件数）；页面缺值改「—」或整条省掉 |

**复跑后的机器读数**：文案与分隔符 `70/70 PASS`、结构块 `70/70 PASS`、双端与触摸 `70/70 cells=140 PASS`；包内测试见 §二。


## 五 · 交付链缺口：本票当场补齐（补票 #872）

### 症状

`home-cmd-read` 的缺省落盘交的不是场景页：实测 `home.item.search --params '{}'` → `delivery.path`
指的那份 HTML **922 字节、无 `data-block=`**（21 张平铺模板的分节页），而同一条场景的族页是
**4,967 字节**。也就是说：唤醒词 → 命令 → 回执路径这一条通着，但「拿到**那份页面**」差最后一步。

### 根因

46 个页族的模板与装配件在**运行期没有任何调用点**（`src/render/pageFamilies.ts` 的两层解析只被测试引用）：
票 4（#801 落盘）先于票 8（#805 页族骨架）与 11 张域票落地，两截各自完好，**中间那一步没有任何票拥有**。
按地图「变更协议」当场补票：**#872**（已挂为地图 #797 子票）。

### 改了什么（#872）

- 新增 `packages/skill-home/src/render/familyPage.ts`：`renderFamilyHtml(key, params, env) → 整页 HTML 或 null`；
  命令键 → 候选域目录表（`home.care.*` 同时挂 family 与 setup 两个域的族，族名跨域唯一故确定性）；
  族名未知／模块不在／装配抛错 → 返回 `null`（**不吞错、不猜名**）。
- `packages/skill-home/src/cli/cmd_read.ts`：缺省落盘与 `--html` 显式出口都先走族页；解析不到族即降级旧的
  21 模板分节页并在 stderr 记一条 note（退出码不变）。旧模板与 `renderEnvelopeHtml` 一字未改，留作降级路径。
- `packages/skill-home/package.json`：`files` 补 `templates/**`（族页模板在子目录，不发版则装上读不到）。
- 新用例 `packages/skill-home/test/family-delivery.test.mjs`（11 例）：扫盘对账（46 族跨域唯一；附录 70 条场景
  逐条解析到族、恰有一个候选域装得上、域与附录一致）；降级分支可达；8 条真链落盘内容**逐字节等于**族页装配结果；
  `--html` 显式出口同内容、单份单回执。
- 票 4 的两条旧断言（「产物＝分节页」）按新语义改为「产物＝族页（含 `data-block=`）」。

### 判据

- 70/70 条回执路径指的文件都含 `data-block=`（跑批读数「落盘即族页 70/70」）；
- 包内测试 312/312（含新增 11 例）；
- 负向：族页装配不可达时降级分支有 note（用例锁定），批校验会因「缺 `data-block=`」当场点名。

## 六 · 仍未解决项：页面缺陷 vs 数据层缺口

判据：**页面缺陷**＝本票范围内该改、已排好改法的；**数据层缺口**＝页面已按现有字段如实呈现，
要补的是取数层／域票的字段（本票不改，逐条回写归属票）。

### 页面缺陷（本票内，改法与页数已定）

| 页 | 症状 | 状态 |
|---|---|---|
| 39 统物品 | 「分布」四块与「价值排行与趋势」是占位（逐类／逐位置／逐状态／逐归属的件数、价格与近 30 天趋势拿不到数） | 登记待裁：是补取数层，还是维持「待数据补齐」的诚实占位 |
| 42 盘点统计 | 「完成率与趋势」是「趋势数据不足」空块（同一形状） | 同上 |

上面两条不是页面写错，而是**这一页要的数字取数层暂时给不出**：按现有 `statsOverview` 只回
items／locations／tags／categories 四类聚合，逐维分布与时间趋势都要新的查询。本票按「不改命令实现」
的红线只登记，请维护者裁：并入数据层欠账（另票），还是本票内补查询。

### 数据层缺口（逐条回写归属）

| 页 | 症状 | 归属 |
|---|---|---|
| 01–04／11 物品管理表单与明细 | 明细表若干列「—」（价格、录入日期、备注等种子库无值） | 取数层字段（域票 #803／#804） |
| 06 看物品 | 「关联／邻居位置」用说明性文字代数据 | 取数层（#803） |
| 08 筛选浏览 | 价格区间筛不出东西（库里没有价格字段值） | 取数层（#803） |
| 10 查重复 | 重复分组的取值缺失，全靠名称近似 | 取数层（#803） |
| 18 标物品 | 标签指路是占位文案 | 取数层（#803） |
| 19／20 管标签／管分类 | 释义文字代数据（分组件数拿不到） | 取数层（#804） |
| 22／23／24 照片类 | 无缩略图（`photo` 字段为路径或无值） | 取数层＋资源解析（#803） |
| 26／27／28 盘点三页 | 部分区块空（无盘点记录） | 取数层／种子数据（#806） |
| 41 查过期 | 行首没有物品名：`expiringItems` 只回 `item_id／location／expiration_date`，页面只能拿位置当标题 | `src/fetch/domains.ts` 查询补 `items.name`（本票未动取数层，另票） |
| 49–55 票据字段 | 购买记录／保修／保养的若干字段种子库无值 | 取数层（#813 域票） |
| 61 查账号 | 类型列多为「其他」（`type` 未落库，只能归到「其他」组） | 取数层（#813 域票） |
| 65／66 家庭协作 | 借用／家人档案的若干家庭字段（关系、件数口径）种子库不全 | 取数层（#815 域票） |

口径：这些页在**页面层**已做到两点——拿不到的字段不编数字（写「待补」或整条不出），
拿得到的一律上屏（借用行带物品名与日期、家人行带关系与名下件数，见 §五之二）。

## 七 · 交出去的三样

1. **墙**：`.scratch/817/手机墙-390.html`／`桌面墙-1280.html`（聚合 70 格）；`.scratch/817/域墙/<域>/` 下 8 个域各一对
   （物品管理 29 格／空间与位置 4／穿搭出行 5／统计总览 4／快递购物 4／票据凭证 18／家庭协作 2／开始使用 4）；
   总索引 `.scratch/817/总索引.html`；链路总览 `.scratch/817/链路总览.html`（70 行、链接 70、缺失 0）。
   第三轮改过的 17 页另出一份小目录 `.scratch/817-r3/`（含 68 张双端图），维护者复核「这轮改了什么」看这一份。
2. **逐格缺陷清单**：本件 §三（三类缺陷逐页逐处 ＋ 改法）、§四之四（第三轮 17 页逐页症状与改法）、
   §六（仍未解决项：页面缺陷 vs 数据层缺口，逐条带归属）。
3. **自检退出码**（本轮复跑，当刻盘上产物）：
   - 70 条真命令链 `RESULT: 70/70 PASS`（exit 0）；
   - `audit-separators --dir .scratch/817 --manifest .scratch/817/manifest.json` → `RESULT: 70/70` / `PASS`（exit 0）；
   - `audit-page-blocks … --manifest …` → `RESULT: 70/70` / `PASS`（exit 0）；
   - `audit-responsive`（清单范围 70 份）→ `RESULT: 70/70 cells=140` / `EXIT=0`；
   - 墙生成器自检 `70 格；链接 354 条；缺失 0 -> 可发`（exit 0）；反例（缺件／坏名／懒政分隔符）exit 1 并点名；
   - 包内测试 `node --test packages/skill-home/test/**/*.test.mjs` → `pass 312 / fail 0`（exit 0）。
