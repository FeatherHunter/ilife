## Question

照 #148 的做法给这条出口上锁——**只经真 spawn 的 CLI 级用例**，至少覆盖：

- 文件名通式（主体 ＋ 时间戳 ＋ 递补）逐字；
- 产物与通用 help 模板的前后缀逐字；
- 并发同秒独占递补（连跑 6 次，名字不互相覆盖）；
- 两支互不串（缺省 HELP 支与显式速查支不互相污染）；
- 退出码矩阵。

实现已在票 7 落在，所以红→绿用**变异自证**（改一处实现点，看对应用例红；还原后全绿），并踩实「还原后要 `tsc -b --force` 干净重建」，否则陈旧 `dist` 会假红。

## 来自上游票的硬约束（开工前必读）

<!-- 2026-09-12 由编排方写入；依据＝票 7 #190 的交付（`t190-impl-notes.md`）＋两席对抗审查（`review-t190-A.md` 通过 86/100、`review-t190-B.md` 整改后通过 77/100） -->

**为什么本票必须存在（审查 B 席 ①）：本图今天「交付面零锁」** —— `packages/skill-home/test/**` 里**零处**引用 `delivery`／`居家管家_HELP`／`.db`。实测：把 `src/cli/cmd_read.ts` 的交付段（约 `:791-798`）删掉、或改掉 `src/help/manifest.ts` 的落点值，**现有用例照样全绿**。本票的第一职责就是把这个洞堵上。

**判据（写进你的用例，可变异自证）**：**删掉交付段或改掉落点值 ⇒ 必须至少一条用例变红。** 兄弟三家（账单／卡路里／大厨）都有专件锁交付，居家缺这一件。

**覆盖面（票面原有要求，逐条都要有）**

1. **文件名通式逐字**：主体 `居家管家_HELP` ＋ `YYYYMMDD_HHMMSS` ＋ `.html`，父目录 `home_manager_html`；递补同秒出 `_2`（共用件口径）。
2. **产物与通用 help 模板的前后缀逐字**（页面出自公共层 `base-paint/help-shell`）。
3. **同秒并发独占递补**：连跑 6 次，名字互不覆盖。
4. **两支互不串**：缺省 HELP 支与显式速查支不互相污染（跑一支时另一支的产物不得出现）。
5. **退出码矩阵**。

**票 7 已实测出的行为，请逐条固化成断言**（这些是「已经对的」，锁住防回退）：

- `q` 支：**无 `delivery`**、载荷命中对应命令（语义与改造前一字不变）。
- `{"mode":"lookup","q":…}`（互斥）、非法 `mode`、**坏 `reuseHours`**、**非字符串 `q`** ⇒ **exit 2**（三支一致，不因走哪支两副面孔）。
- 写失败（落点被同名文件占位）⇒ **exit 5 且 stdout 空**（不降级）。
- 复用窗口内再跑 ⇒ **回同一路径、不新建**；`{"reuseHours":0}` ⇒ 落新件。
- **产物目录 `.db` 数＝0**（「看帮助不许把库建出来」——审查 A 自己 `readdirSync` 数过，是本图最硬的判据之一）。
- `delivery` 在顶层**只追加**（既有五键序不变）。
- `--html <路径>` 支**仍出分节页**（所有 key 通用的出口，不许被顺手砍）。

**变异自证（票面原有）＋一条纪律**：改一处实现点 → 对应用例红；还原后 **`tsc -b --force` 干净重建**再复跑，否则陈旧 `dist` 会假红／假绿（审查 A 的探针全程用的是新鲜构建，这条别省）。

**归票订正**：票 12 设计稿曾把 `test/help-delivery-*`／`help-exit-*` 归票 7；**以地图计划为准，交付锁归本票（票 8）**——票 7 已关，其范围止于接线与出口。

**不属本票**：`SKILL.md` 说明面与 `build-help.mjs`（票 9）；`--html` 与「缺省交付」的双产物语义对齐（编排方已裁「本图不动 `--html`」，另立票候选）。

## 进度：100%

下一步：无阻塞——交付锁已上并接线。新增 `packages/skill-home/test/help-delivery-190.test.mjs`（**316 行／11 用例**，只经真 spawn＋临时 `SKILLS_DB_PATH`；落点三值在用例里**写死逐字**，不从 `dist` 取——否则改实现点会同时改期望值，锁成同义反复）。包级 `test` 串已含它（＋资产锁＋仓根 scaffold）→ **26/26 绿**；包内 7 个用例文件合计 **48/48 绿**。

**锁有真牙（三重变异证明）**：① 注释掉交付段（`cmd_read.ts:797-805`）→ **10/11 红**；② 改落点值一字 → **5 红**；③ 改落点**目录名**一字 → 6 红，按审查建议加固后 **8 红**（原本「缺席式空绿」的 ④⑥ 现在也会红）。三次变异均按字节复原并 `tsc -b --force` 重建后全绿。

**审查**：`review-t191-A.md` **通过 95/100**（覆盖 40/40、强度 31/35、纪律 24/25；它自己独立取点做了一次变异）。唯一强度缺口（④⑥ 缺席式断言）已改成 fail-closed：**先断言产物目录存在，再判缺席**，并有变异证明「这次会红」。

**接线**：仓根 `package.json` 的 test glob 含本包用例、CI 先 `pnpm build` 后 `pnpm test` ⇒ 跑在新 `dist` 上。
