## 决议

**关票**。五条全部拿到用户答复（2026-09-11）。

### 用户答复原话

```
A A A A
```
```
联动域那 3 条怎么落： A 我们以后在 combos相关地方设计。
```

### 五条落法（下游照做）

| 序 | 争点 | 裁 | 落法 |
|---|---|---|---|
| Q1 | 推位置／找位置挂哪张卡 | **A** | 留在「空间与位置 › 位置管理 › 管位置」卡上（跟新命令 `home.location.query` 走）；场景总数不变 |
| Q2 | 看标签要不要独立卡片 | **A** | 不独立；仍作「管标签」卡的附属词，场景主数保持 **73** |
| Q3 | 3 条带 `(HTML)` 的词进不进 HELP | **A** | 不进清单；只在口径区写一行说明（实现上 `src/help/lookup.ts` 与 `scripts/build-help.mjs` 要支持排除） |
| Q4 | HELP 主数写哪个 | **A** | 主数用 **73 场景**；21 条命令写口径区 |
| Q5 | 联动 3 条怎么落 | **A（＋补充）** | 留登记位（`status: "deprecated"`）、prompt 不迁、**HELP 不列**、**不建域目录**；用户原话：「我们以后在 combos 相关地方设计」——联动将来的设计**出本图** |

### 本票引出的出行范围一条

「联动功能」那 3 条的将来设计（联动总览／记到卡路里／记到记账）→ 走 **combos 相关票**，不在本图目的地内。

### 与 Q4 的算术待办（不属本票，已挂票 6）

联动 3 条不列 ⇒ 页面实际列出的卡片是 70 张，而主数写 73。这条算术已挂票 6（`#189`）报用户点头时一并定；本席建议：副标题计数按**实际列出**派生，口径区写一行「骨架 73 条，联动 3 条已停用不列」。
