## Question

照 #151 的做法做真机端到端＋肉眼终审：

- 生产目录真跑：`SKILLS_DB_PATH=D:\2Study\StudyNotes\.db` 裸跑 `home-cmd-read home.help.lookup` → 拿到绝对路径 ＋ 产物文件，与老的四份 `居家管家_HELP_*.html` 并排落在同一目录；
- DSH 真机技能表可见 `skill-home`（名＋介绍，可按需读全文）；
- 两侧视口取图（手机 375px ＋ 桌面），确认通用 help 模板的观感与卡路里／记账同款；
- **维护者肉眼终审「过」**——这一条不达成不关票。

证据落 `docs/skills/skill-home/t<本票>-evidence/`（跑的命令、产物、截图、结论各一份）。

## 上游交接（开工前必读）

<!-- 2026-09-12 由编排方写入 -->

**票 7 #190／票 8 #191／票 9 #192 的产物**：出口三支（缺省＝HELP 文件／`mode:"lookup"`＝速查表分名／`q` 不落盘）、交付面 11 条真 spawn 锁、`SKILL.md` 说明面（含「HELP 交付」节与「公共安装器运行时」节）。命令 `home-cmd-read home.help.lookup` 在真机 `.bin` 上可达（`~/node_modules/.bin/` 与 profile 的 `.bin` 都有）。

**编排方已完成**的证据（`t194-evidence/01-production-run.md`，21:03）：

- **生产目录真跑**：`SKILLS_DB_PATH=D:\2Study\StudyNotes\.db` ＋ `home-cmd-read home.help.lookup` → exit 0，产物 `…\home_manager_html\居家管家_HELP_20260912_210318.html`（**132317 B**，sha256 `E618A369…1DA5`），与 6 份老 HELP（69–75 KB）**并排**，目录 18→19 件
- **看帮助不建库**：该目录 `.db` 本次调用**动过 0 个**；
- **可打开＋内容全在**：零外部引用、`<meta charset="UTF-8">`＋移动视口声明、8 分组／**27** 二级组／**70 条场景标题与唤醒词全部落页（缺 0）**、停用域未出现。（原文写「30 二级组」——21:2x 复核实测**真值 27**，见下节「订正」）

## 本会话复验（2026-09-12 21:25–21:35；全档 `t194-evidence/03-fresh-verification.md` ＋ 可复跑脚本 `03-verify-artifact.mjs`）

四条，全部由本会话亲手跑：

1. **新会话＝插件侧真机成立**：本会话技能目录里 `skill-home` 的**名字／介绍已是票 9 改后的新文本**（「缺省落一份 HELP 文件并回执绝对路径…」），且「可按需读全文」实测可用（取到 `packages/skill-home/SKILL.md` 全文，含「HELP 交付」节）⇒「插件侧也要跑通」这条**不需要重启 DSH 就已成立**（重启也不会坏，只是再跑一遍）。
2. **生产目录再跑两条**：缺省 → exit 0，回执 `…\居家管家_HELP_20260912_210318.html`（**复用窗口命中**：24h 内回同一份、不新建不改写，目录 19→19 不涨）；`--params '{"reuseHours":0}'` → exit 0，新产物 `…\居家管家_HELP_20260912_212642.html`（132317 B），目录 19→20。两条都回**绝对路径**、`bytes` ＝ 实际大小；跑前跑后 `.db` 计数 17→17、**动过 0 个**。
3. **产物结构校验 PASS**：8 分组／**27** 二级组／70 场景／零外部引用／charset＋viewport 齐／无 BOM／停用联动 3 组名未落页／口径区有「骨架 73 条，联动 3 条已停用不列」。
4. **两张视口图补了**（复核辅助，非判据）：本机 Chrome 无头＋CDP 取图（**不需要** playwright 浏览器二进制、零下载），桌面 1440×2014 整页、手机 375×1750 单列，随交付一起给维护者；图**未入仓**（要留档再入 `t194-evidence/`）。

**订正一处旧证据**：证据 ① 的「30 二级组全部落页」与实测不符，真值 **27 组**＝骨架 30 − 联动域那 3 组（联动总览／食品联动／价格联动，每组各 1 条停用场景；票 13 已裁「联动 3 条不列、不建域目录」⇒ 3 个容器随之不落页，30 − 3 ＝ 27）。场景数不受影响（73 − 3 ＝ 70，副标题写的就是 70）。脚本期望值已改成 27。

## 进度：100%

**维护者肉眼终审「过」**（2026-09-12，原话：「认可 HTML样式符合我的要求 验证通过」）⇒ 终点两条判据（①**明确拿到 help HTML 文件**：落盘＋可打开 ②页面由**现成通用 help 模板**渲染）＋肉眼终审「过」**全部达成，本票关票**。

- 交付产物：`D:\2Study\StudyNotes\.db\home_manager_html\居家管家_HELP_20260912_212642.html`（**132317 B**；8 分组／27 二级组／70 场景／零外部引用）
- 技能侧：`home-cmd-read home.help.lookup` 缺省＝落文件＋回执绝对路径（本会话两条真跑见 `t194-evidence/03-fresh-verification.md`）
- 插件侧：DSH 真机技能表可见 `skill-home`（名＋介绍，全文可按需读）
- 复跑校验：`docs/skills/skill-home/t194-evidence/03-verify-artifact.mjs`
- **订正留档**：证据 ① 的「30 二级组」与实测不符，真值 **27**（＝骨架 30 − 联动域 3 组）
