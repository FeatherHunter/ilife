# 家庭协作域对账（#815，本域两页的事实记录）

本域 2 场景、2 页族（`link` 已裁不计）：SM7-1 借用管理 → `family_borrow`，
SM7-2 家人档案 → `family_members`。老模板在老技能 `templates/` 根（全域唯一
没有子目录的一域），新位置按契约归位到 `templates/family/` 与 `src/family/pages/`。

## 一、场景与页族

| 场景 | 唤醒词 | 页族 | 主命令 | 验收产物名前缀 |
|---|---|---|---|---|
| SM7-1 借用管理（借出/借入/归还/催还） | 借用 | family_borrow | home.care.query | 借用_SM7-1_ |
| SM7-2 家人档案（成员/物品归属标记） | 家人档案 | family_members | home.care.query | 家人档案_SM7-2_ |

四写操作路由（#800 已登记，本票只消费不改表）：借出 → care.write
op=borrow，借入 → care.write op=borrow，归还 → care.write op=return，
催还 → care.query kind=borrow。双锁用例见 `test/family.test.mjs` 末两节。

## 二、信息结构对齐（老模板 → 新页）

SM7-1（老 `family_borrow.html` 158 行）：借出区与借入区分区开列（未压成
一张通用列表）；超期横幅原文保留（有超期报件数、无超期报平安，均带
超期提醒前缀）；记录行含对象、借出日、约定归还日、状态、已借天数；
登记表单含方向下拉、家人下拉、外部联系人自由输入、库外物品名自由输入
（借入形态）、借出日期、约定归还日；每行未归还记录带确认归还与复制催还文案。

SM7-2（老 `family_members.html` 130 行）：成员列表卡（首字头像、称呼、
关系、归属件数、移除成员）；物品归属勾选清单（成员下拉、勾选、确认标记归属）；
物品总数；添加成员表单（称呼、关系、备注、确认添加）。

两页各带数据快照区（真 envelope 原样）与复制数据、复制日志出口，
必需块原文收进文末折叠对照区（机器对账用，人看折叠）。

## 三、每格该确认什么（上墙用）

- SM7-1：借出区与借入区是否分开；超期提醒是否在；四操作形态（借出登记、
借入登记、确认归还、复制催还文案）是否齐。未决（册子遗留）：借入的库外
物品名是否落进物品台账——老实现只记借用表不进台账，新页维持现状，
待维护者裁。
- SM7-2：成员列表与归属勾选清单是否在；添加与移除形态是否齐。未决
（册子遗留）：成员关系字段无枚举（老 `family_ops.py:28` 自由文本），
新页维持自由输入，待维护者裁。

## 四、取数缺口（诚实声明，回写票 3）

`home.care.query` 两分支当前只回人名行（借用回 `借用+成员名`，
家人回成员名），无方向、日期、约定归还日、关系、归属数字段；
`borrow_records` 表也只有四列（item_id、member、action、date），
无方向、约定归还日、归还时间列。后果三条：借入分区在真链下恒空
（页内留诚实空态）；超期恒零（横幅报平安并明示口径）；记录行物品与
日期列标待补齐。页面按行自带字段自动分区富化，取数补齐后无需改页。
本票不私自扩表扩键（写集纪律），请票 3 评估补齐。

另：包内 `test` 串未登记本件（`package.json` 的 test 显式文件表归票 3，
本票不碰），验收时本件单跑 `node --test test/family.test.mjs`，
请票 3 把它收进串。

## 五、遗留出口（老表与新表口径）

票面写的老表 95 对新表 91 是 #800 之前的数。现行结论：新路由表
125 行（71 主词、48 变体、3 入口、3 待复裁），四分类与三向对账由
`test/wake-family-gates.test.mjs` 锁定；本域四写操作词全部可达且
逐行双锁（本票测试末节），与老场景标题对齐，无口径差。无需回写票 2，
此处销账。

## 六、验收证据（持锁命令见 issue 评论，结论摘要）

- 包内回归：`pnpm --filter skill-home test` 全绿（68 项，含唤醒词对账锁）；
本件 `node --test test/family.test.mjs` 12 项全绿。
- 墙自检：`gen-scene-wall.mjs --check .scratch/815 家庭协作-手机墙.html`
与桌面墙各 exit 0。
- 分隔符：`audit-separators.mjs --dir .scratch/815` 全绿（版式位、英文、
重复句三门零命中；节点报告仅剩命令原文 boilerplate 与一条必需块斜线，
均为载荷或契约原文，非懒政）。
- 结构块：`audit-page-blocks.mjs --dir .scratch/815 --blocks
…/page-blocks.json` 全绿（两份产物必需块齐全）。

注：票面验收命令的目录式写法（分隔符与块审计直接跟目录）与脚本签名
不一致（脚本要 `--dir`，块审计还要 `--blocks`），实跑用显式旗标，
请票 2 以显式为准更新票面措辞。
