# t794 证据（技能侧：居家设置页收窄 ＋ 主密钥改从文件读）

票：[#794](https://github.com/FeatherHunter/ilife/issues/794)，定稿 [#793](https://github.com/FeatherHunter/ilife/issues/793)，样板 [#749](https://github.com/FeatherHunter/ilife/issues/749)。

## 一、交付了什么（逐文件一句话）

| 件 | 改了什么 |
|---|---|
| `src/config.ts` | 默认值表删 `files` 组、加 `key.file`（默认 `.master.key`）；`HOME_CONFIG_RETIRED` 填两退休键；写盘口径（`writableDefaults`＋`withoutBlankEditableDir`，照记账样板）：写落绝对路径、读认空串 |
| `src/fetch/paths.ts` | 落点算式收成纯函数一族（`dbDirOf`／`dbFileOf`／`htmlDirOf`／`keyFileOf`／`backupDirOf`）＋ `resolvedHomePaths()` 五格；`backup.dir` 改绝对优先；`key.file` 绝对优先否则数据目录下 |
| `src/fetch/masterKey.ts`（新建） | `resolveKeyFile` 定位＋`loadMasterKey` 读口令（trim、去BOM、≥8位；缺席报文带绝对路径）＋退场报文与判据；不自动建文件 |
| `src/fetch/errors.ts` | `HomeFetchError` 码表加 `HOME_MASTER_KEY_MISSING`／`HOME_MASTER_KEY_UNREADABLE`（新文件要新码，无处可借） |
| `src/fetch/backup.ts` | `resolveBackupDir` 委托 `paths.ts`（删本地第二份算式与 `backupDirName`；`DEFAULT_BACKUP_DIR_NAME` 改具名转出） |
| `src/fetch/index.ts` | 转出 `resolveKeyFile`／`keyFileOf`／`resolvedHomePaths`／`HomeResolvedPaths`／`loadMasterKey`／`retiredParamMessage`／`hasParamMasterKey` |
| `src/cli/config.ts` | `home.config.read` 回执 `data` 新增 `resolved` 五格 |
| `src/cli/cmd_read.ts` | 账号三命令改从文件读密钥；`master_key`／`masterKey` 参数给即 exit 2 指文件；备份回执加「本备份不含主密钥文件」一句 |
| `src/health.ts` | 落点改调 `paths.ts` 纯函数（与回执同源）；④⑥ action 改指配置文件；⑧ 改按 `key.file` 判在／不在＋老位置复制指引；`masterKeyCandidates` 改名 `legacyKeyCandidates`（只读提示） |
| `src/help/manifest.ts` | `helpFileStem`／`lookupFileStem` 回常量（`HELP_FILE_STEM`／`LOOKUP_FILE_STEM`），不再读配置 |
| `src/help/scenarios.yaml`＋重跑生成器 | 存账号／改账号／看密码三处 prompt 退掉「主密钥: ___」行；生成物与摘要锁同步 |
| `test/t794-设置页收窄.test.mjs`（新建，19条） | 票面 9 组判据（回执扩组／读认空串／写绝对／退休键过渡／key.file三态／密钥真跑三态／backup口径＋zip不含密钥／体检同源＋⑧两态） |
| `test/config-695.test.mjs`、`test/fetch.test.mjs`、`test/cli.test.mjs`、`test/help-assets.test.mjs` | 旧断言跟新形状走（默认值表／账号改走文件／摘要锁重算；改前红、改后绿，见三） |

## 二、机器读数（门禁全在 `run-locked` 下跑，标识见 `t794-门禁运行记录.md`）

- 编译：`tsc -b packages/skill-home` exit 0（`GATE-RUN runId=173593aa-3344-430b-a614-56818d383395`；还原后复编 `runId=b7433957-4e87-421a-aa59-2838896abc85` exit 0）。
- 新用例：`tests 19／pass 19／fail 0`（`runId=5b820f34-427b-4e0a-801c-62e9fde8b5b6` exit 0）。
- 全量：`tests 81／pass 81／fail 0`（`runId=4a106d1b-371d-451f-8c04-30c2d3251fac` exit 0，含回归的 config-695／fetch／cli／backup 24 条 `runId=bf1f706e-a7c3-4cc7-8cbb-f048446a0aa8`）。
- 帮助资产：`gen:help-assets` exit 0（`runId=22828aa8-9aa9-44ef-af9f-91505256f683`，43678字节，9域／30组／73场景）；`gen:help-assets:check` exit 0（`runId=553c644d-2020-45d8-9ec6-240b39bfae0b`）；help-assets＋help-delivery-190 全绿（合跑 `runId=96ac4e33-e050-490c-9206-0be90d1480a1`）。
- 变异自证（源码级）：`paths.ts` 的 `keyFile` 置空 ⇒ `tests 19／pass 14／fail 5`（`runId=ae8b2fd7-40ae-441f-9be6-e60c970bce2f` exit 1）；还原 ⇒ `19／19／0`（`runId=5b820f34`），且源码无 `MUT-794` 残留。
- 合并树重绑（#754 把本票 `config.ts`／`cmd_read.ts` 两件随其提交先行入仓后，按指纹绑定重编重跑）：
  两包重编 exit 0（`runId=bb45dd83-c1a6-4485-992a-c77e32be4665`）；技能t794＋插件t794＋696＋跨包锁合跑
  `tests 72／pass 72／fail 0`（`runId=2b8955d8-2ec9-4992-9691-eb4198c23003` exit 0）。
- 真出口抽查（沙盒）：删 `home.yaml` 再读 ⇒ 落盘 `db.dir` 为绝对路径；改回 `""` ⇒ 生效默认且读不写回（用例①② machine-readable，同上）。

## 三、旧用例的三处改动（改前红、改后绿，如实记）

1. `config-695` 的 DEFAULTS 与写合并期望跟新表走；`cli.test` 的账号两行改走密钥文件（＋参数退场 exit 2）；`fetch.test` 的 DEFAULTS 跟新表走。
2. `help-assets.test.mjs` 的 `YAML_SHA256`／`YAML_BYTES`／`ASSET_SHA256` 按重跑结果更新（46123字节／`3fa35b0b…`／`dbb186be…`）——锁的设计就是"改事实源即更新锁"。
3. 中间红：首次合跑 `runId=3df112df` exit 1（摘要锁旧值）→ 更新锁；`runId=52ec6151` exit 1（跨包锁 hint 违禁词，属插件侧，修 hint 后绿，见插件证据）。

## 四、结构纪律五步对账

- 第一步清单 vs 实际：一致，偏差三处如实记——① `src/fetch/errors.ts` 加两码（清单未单列：新文件要新码）；② `src/fetch/backup.ts` 删 `backupDirName`（清单写了"删本地第二份算式"，即它）；③ `packages/plugin-home-ilife/src/index.ts` 加两类型转出（插件侧清单漏写，已补）。新增目录：无（`masterKey.ts` 落既有 `fetch/` 能力目录）。偏差清零。
- 第四步超线报警：已超线，需要根据规则进行重构。`src/cli/cmd_read.ts`（既有超线件，归票7 #190，本次在其上加约15行账号门）、`src/health.ts`（约553行，体检三摊活同处一件：子集解析／探针／报告装配，拆法＝解析器另立 `healthConfig.ts`、探针另立 `healthProbe.ts`，本次先不拆：判据正靠它对齐，窗口内大搬会把读数变成合成变更）、`src/fetch/db.ts`（434行，归属另立票）。超线是报警不是拦路。
- 第五步：本节即对账；第一步每一行都有着落。

## 五、老库 accounts 对照读数（验收第 5 条：只读，不伪造）

- 老库 `D:\2Study\StudyNotes\.db\home.db` 在：19 张表，`accounts` 5 条（平台 `zlife`／`oa.czdsoft.com.cn HCL`／`公司人事系统`／`minimax-59.9`／`minimax-39.9`，密文长 100／100／100／140／140）。
- 结论：**未能对照**。原因两条：① 拿不到口令，无法解密比对；② 结构证据显示老密文与新实现**不是同一族**——老 5 条无冒号、混合字符集，新实现是 `iv32hex:cipherhex`（AES-256-CBC，`deriveKey＝scryptSync(口令,'home-salt',32)`），新 `decryptPassword` 遇到老行会报"账号密文损坏"。**#793"老库照样解得开、无迁移"的前提与现场实测不符**，转票建议见收尾评论（本票不搬库、不另开票）。
- 方法件：`.scratch/t794-oldhome-probe.mjs`／`probe2`／`probe3`（只读 `readOnly:true`，未打印任何密文内容）。

## 六、票面字面偏差（一处，如实记）

- 验收第 4 条写"用临时 `ILIFE_CONFIG_DIR` 沙盒"——该变量已删（#754／#763 方向）。本票一律用临时家目录沙盒（`USERPROFILE`／`HOME` 注入，`requireIsolatedHome` 当场自证），覆盖同一件事（别动真配置）。

## 七、未做项

- 发版与装机（#755）；真机点屏（归维护者）；主密钥文件权限 ACL／600（票面遗留）；体检⑧"老位置复制指引"分支无自动化用例（在沙盒里造老位置＝往真实包目录写文件，不做；逻辑经 code review，判据分支 simple，风险记此）；`SKILL.md` 说明面若仍写 `files.help`／`ILIFE_CONFIG_DIR`（沿记账 #749 的先例：不在本票交付面，建议由 #762／#754 收尾或另开说明面票）。
