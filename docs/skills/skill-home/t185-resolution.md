## 决议

**关票**（第二次派单）。产物三件（全在 `docs/skills/skill-home/`）：

- `t185-content-reconcile.md` —— 七节报告（逐条对账／20 条归属／`(HTML)` 去留／link 落法／中英名对照／骨架 JSON／10 条待裁）
- `t185-skeleton.json` —— 机器可读骨架（`JSON.parse` 通过，与报告里的副本逐字相同）
- `t185-extract.mjs` —— 只读抽取脚本，六模式（默认／`join`／`routes`／`lines`／`skeleton`／`names`）；原始输出存 `t185-evidence/`

### 三组计数（脚本数出，不是抄票面）

1. 老 **73** 场景里 **3 条无落点**（全是 link 域：联动总览／记到卡路里／记到记账），其余 70 条逐字命中新表同名词。
2. 新表 **91** 条里**补进 20 条**：3 条 HELP 自身词 ＋ 3 条 `(HTML)` 兼容词 ＋ 14 条功能词。
3. 域计数：items 29／space 4／outfit 5／stats 4／express 4／receipt 18／family 2／setup 4／link 3；子功能 30 个。

### 一条关键发现（改了「老骨架」这条口径的原料）

老家**不只有** `scenarios.yaml`——老 `SKILL.md` 的「## 路由表」（117-321 行）有 95 行／91 个不重复唤醒词。新表 91 条 ＝ 老 91 − 3 废弃 ＋ 3 新（居家管家帮助／居家管家能做什么／改购物清单）。所以「新表多出的 20 条」里 **17 条老家早有出处**（老路由表的「功能／加载文件」两列直接指归域），**只 3 条是真新增**。→ 票 5 落资产时，事实源要用**老 yaml ＋ 老路由表**两份，不只 yaml。

### 三条本票给出的判断（下游照做，除非用户另裁）

1. **3 条 `(HTML)` 不进 HELP 清单**，改在口径块写一行：依据是老 `features/search.md:71,102`「一律默认输出 HTML、不保留 `-html` 后缀词」，且新表里这三词与主词同命令同 shape。实现落点 `src/help/lookup.ts:43-51` 与 `scripts/build-help.mjs:16` 要支持排除。
2. **link 3 条**：骨架留登记位（`status: "deprecated"`）、prompt 不迁、HELP 不列、不建域目录。
3. **HELP 主数用 73 场景**，21 条命令写口径区（三组数并存：91 词／73 场景／21 命令）。

### 待裁的十条没丢

第七节 10 条里，真正要**用户**拍板的四条（推位置／找位置归域、看标签要不要独立卡片、`(HTML)` 去留、HELP 主数）＋ link 登记位，已收成新票 **内容骨架裁决**（门票面见地图 `#183` 的「计划（任务清单）」第 13 行）；`care` 跨两域（U3）归**结构设计**票；其余（子功能英文名大小写、组副标题、老 `status` 字段语义等）归下游实施票，票面已列。

### 中英名对照表（票 5／票 6 要用的）

域级九行见报告第五节 5A；子功能 30 行见 5B。新仓命令前缀与老域名**有四处不一致**（space↔location、outfit→trip、express↔shopping、receipt↔ticket、family/setup 共用 care 前缀）——这条与铁律四的张力，正是结构设计票要裁的。
