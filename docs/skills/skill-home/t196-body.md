## Question

票 2（`#185`）的对账表摆出来五条「老骨架」与「新表」对不齐的地方。这几条是**要用户拍板**的（不是实施细节），裁完票 5（资产）与票 6（渲染）才有准数。逐条给本席的建议，用户逐条答。

1. **`推位置`／`找位置` 挂哪个域**：暂挂 `space`／「位置管理」（与新命令 `home.location.query` 一致）；但老家把这两个词记作**录入流程的子步骤**（`features/add.md` Step 2.5／2.6，老路由表 147／148 行），没有独立场景、没有模板。另一条路是挂 `items`／「录入」（老出处一致、新命令不一致）。
2. **`看标签` 要不要独立卡片**：现行挂到老 `4-1 管标签`（老 prompt 的菜单里本来就有「查看」），场景数保持 73；要独立卡片就得新增老骨架没有的场景（73 → 74），与「老骨架为准」相抵。
3. **3 条带 `(HTML)` 的唤醒词进不进 HELP**：报告建议**不进** HELP 清单，只在口径区写一行（依据：老 `features/search.md:71,102`「一律默认输出 HTML、不保留 `-html` 后缀词」；新表里它们与主词同命令同 shape）。
4. **HELP 的「主数」写哪个**：三组数并存——91 条唤醒词（新表）／73 场景（老骨架）／21 条命令。报告建议主数用 **73 场景**，21 条命令写口径区。
5. **link 域那 3 条（联动总览／记到卡路里／记到记账）**：报告给的落法是**留登记位**（`status: "deprecated"`）、prompt 不迁、HELP 不列、不建域目录。

## 进度：0%

下一步：一轮 grilling 把上面五条逐条定死（写在对话正文里问），定完关票；票 5 与票 6 照它落。
