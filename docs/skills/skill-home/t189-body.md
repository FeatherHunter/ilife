## Question

把内容资产接进通用 help 模板，照 #145 的做法：**零 IO 出全页 HTML**（渲染字节数留证），并把三个小口径定死：

- `version` 取什么（照记账：技能数据世代，不是 npm 包版本）；
- 初始化状态怎么判（照记账：只读页**不建库**，读失败照显）；
- 显隐走模板自带的开关（记账走 `hidden`，不删字段——payload 形状不随状态变）。

**本票同时要报新架构规则的落点**（用户 Q8=(a)）：

- 新增件住哪个能力目录（目录名取自 HELP 一级分组、写成英文名）、每个文件的公开接口给什么（导出几个、各一句）；共用件要写得出哪两个能力在用；
- 被碰到的旧件就地摆正；
- **第一步「影响清单」与第二步「结构设计」先报用户点头**再动手；交付时报第五步「交付对账」；超告警线当场报「已超线，需要根据规则进行重构。」。

## 进度：0%

下一步：等票 1（照抄清单）＋票 3（模板契约）关票。
