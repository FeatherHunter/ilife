## 交接：本图只剩两件（票 11）

### 一、已完成（证据①，`t194-evidence/01-production-run.md`）

- **生产目录真跑**：`SKILLS_DB_PATH=D:\2Study\StudyNotes\.db` ＋ `home-cmd-read home.help.lookup` → exit 0
- 产物：`…\home_manager_html\居家管家_HELP_20260912_210318.html`（**132317 B**，sha256 `E618A369…1DA5`），与 6 份老 HELP（69–75 KB）**并排**，目录 18 → 19 件
- **看帮助不建库**：该目录 21 个 `.db`，本次调用**动过 0 个**
- **可打开＋内容全在**：零外部引用／`<meta charset="UTF-8">`／移动视口声明／8 分组·30 二级组·**70 条场景标题与唤醒词全部落页（缺 0）**／停用域未出现

### 二、待办 1：插件侧真机（**需重启 DSH**，时机由维护者定）

现状：`dsh-home-ilife` 装机已就位（profile 依赖 `link:`、bundles 在册、两条 Junction 均在；提供方用例 9/9、smoke 7/7、客户端产物门 21/21），但**插件半要下次启动才生效**——今天这个 GUI 进程起于装机之前。

重启后的核验（**三步**）：

1. **技能表可见**：让 AI 或自己在 DSH 里看技能目录，应出现居家的 HELP 技能，名与介绍取自 `packages/skill-home/SKILL.md` 的 frontmatter（现为「缺省落一份 HELP 文件并回执绝对路径（老骨架 9 域／30 组／73 场景）」），并能按需读全文。
2. **命令仍可达**：任意目录跑一次 `home-cmd-read home.help.lookup`（`SKILLS_DB_PATH` 已设时）→ 应 exit 0 并回 `delivery.path`。
3. **落一份新产物**（可选）：确认重启后仍能落盘（会与既有产物同目录）。

⚠️ 重启会**中断当前 GUI 会话**（编排方的会话随之中止），故这一步必须在维护者点头后做；重启后如要接着推这张票，请在新会话里让 AI 读本文件与 `map-183-body.md` 即可续上。

### 三、待办 2：维护者肉眼终审「过」（**这一条不达成不关票**）

产物已交维护者（同目录绝对路径见上）。按地图判定口径（用户 2026-09-11 定案），只要求：**①明确拿到文件（落盘＋可打开）②用现成通用 help 模板渲染**，**不含**与老 HELP 逐字比对 UI。故看的时候请按「这份页面对不对、能不能用」来判，不必拿老样式逐条对。

**不建议截图留档**（本机 python playwright 只有库、没有浏览器二进制，`playwright install` 需一次性下载 ~100MB+）；若维护者明确要留两张视口图，再下载后补。

### 四、收尾动作（签「过」之后）

- 票 11 → 写回票面（100%）＋结论评论 ＋ 关票；
- 地图 `#183` → 进度 100%、Decisions so far 追加票 11 的 gist、**Out of scope** 里那条「取图／视觉比对」按裁定留档；
- 本图全部改动仍未提交（22 处，清单见编排方清点）；提交由维护者决定。
