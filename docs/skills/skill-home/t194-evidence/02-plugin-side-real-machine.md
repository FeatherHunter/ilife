# 票 #194 证据 ②：DSH 真机技能表可见 `skill-home`

- 时刻：2026-09-12（编排方核）
- 目的：核地图终点判据里的「**插件侧（`dsh-home-ilife` ＋ DSH 真机装机）也要跑通**」

## 一、结论

**在正在运行的这个 DSH 里已经成立**——不需要重启。

## 二、证据链（三条互相印证）

1. **进程晚于装机**：当前占 43120 的进程＝`DSH Desktop` pid **11968**，**启动于 2026-09-12 18:03:24**；而票 10 的插件装机时刻是 **2026-09-12 17:46:37** ⇒ 进程启动时插件已在盘上，**启动即加载**。
2. **技能表里看得到**：编排方（本会话）接到的**技能目录**里就有 `skill-home`，其条目＝`SKILL.md` frontmatter 的名字／介绍／触发词（「居家管家 帮助、居家管家帮助、居家管家能做什么、查物品、看物品、录物品、改物品、盘物品、查位置、查快递、查保修、查证件、看统计、初始化」）——**名＋介绍可查**这一半成立。
3. **「按需读全文」同一份源**：本会话目录里带的是**改动前**的描述（「出一份居家管家速查列表」），而 `SKILL.md` 的 `description` 是票 9 在 **21:06** 才改的 ⇒ 目录是在**会话开始时从包里的 `SKILL.md` 读出来的**，不是硬编码。也就是说：读的就是 `packages/skill-home/SKILL.md` 这一份，改它即改技能表（**下一个会话**会自动显示新描述）。

**旁证（装机面仍完好）**：profile 依赖 `dsh-home-ilife: link:D:/ilife/packages/plugin-home-ilife` 在册、`dsh.profile.bundles` 含它、两条 Junction 均在（`profiles/web/node_modules/dsh-home-ilife → 包`、`~/node_modules/skill-home → 包`）；插件产物新鲜且合法（`dist/index.js` 2609 B、`dist/client.js` **3541 B 且首行 `window.__ModuleLoader__.load({`**，不是那份会让整页脚本解析失败的 837 B 裸 ESM）。

## 三、剩下要做的

- **维护者肉眼终审「过」**——唯一未达成的判据（票面明写「这一条不达成不关票」）。
- 可选（非必需）：开一个**新会话**，确认技能表显示的是票 9 改后的新描述（「缺省落一份 HELP 文件并回执绝对路径…」）；这不需要重启 DSH，只需新开会话。
