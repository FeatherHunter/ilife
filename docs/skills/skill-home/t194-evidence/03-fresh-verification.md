# 票 #194 证据 ③：新会话复验（技能表全文可读 ＋ 生产目录再跑 ＋ 产物结构校验）

- 时刻：2026-09-12 21:25–21:35（本地）
- 执行者：编排方（本会话亲手跑，非采信实施者自述）
- 目的：把票 11 剩下两件里**能由 AI 达成的那半**做实，并把最后一条（维护者肉眼终审）缩到「只看一个文件」

## 一、插件侧真机（证据 ② 的补强：不是在旧进程里猜，是在**新会话**里证）

- 本会话（＝票 10 装机之后新开的会话）技能目录里 `skill-home` 的**名字与介绍**＝票 9（21:06）改后的新文本，逐字：
  「居家管家HELP」→home.help.lookup 缺省落一份 HELP 文件并回执绝对路径（老骨架 9 域／30 组／73 场景）；唯一出口 home-cmd-read。触发词：居家管家 帮助、居家管家帮助、居家管家能做什么、查物品…
- 「**可按需读全文**」实测成立：本会话用技能加载工具取到 `packages/skill-home/SKILL.md` 全文，含「## HELP 交付」节（缺省＝落文件＋绝对路径、完成判据＝文件存在且大小＝`delivery.bytes`）。
- 结论：地图终点判据里「**插件侧（`dsh-home-ilife` ＋ DSH 真机装机）也要跑通**」**在新会话里成立，不需要重启 DSH**（重启也不会坏，只是再跑一遍同一件事）。

## 二、生产目录再跑两条（`SKILLS_DB_PATH=D:\2Study\StudyNotes\.db`）

| 命令 | exit | 回执 `delivery` | 该目录件数 |
|---|---|---|---|
| `home-cmd-read home.help.lookup`（缺省） | 0 | `…\home_manager_html\居家管家_HELP_20260912_210318.html`（132317 B）——**复用窗口命中**（24h 内回同一份，不新建不改写） | 19 → 19 |
| `home-cmd-read home.help.lookup --params '{"reuseHours":0}'` | 0 | `…\home_manager_html\居家管家_HELP_20260912_212642.html`（132317 B） | 19 → 20 |

- 两条都回**绝对路径**；`bytes` ＝ 实际文件大小（132317，两处一致）
- 跑前跑后该生产目录 `.db` 计数 **17 → 17**，本次调用**动过 0 个**（「看帮助不建库」在生产里再证一次）
- 现检出构建（`dist` 21:07:42）产出的这份，与上一棒 21:03 那份**结构逐项一致**（只差页面里那行「更新于」戳）

## 三、产物结构校验（可复跑脚本）

- 脚本：`docs/skills/skill-home/t194-evidence/03-verify-artifact.mjs`（只读；`node <脚本> <产物绝对路径>`；PASS exit 0）
- 对 `…居家管家_HELP_20260912_212642.html`：**PASS**
  - 8 个在列分组（`link` 停用域不入组）
  - **27 个二级组**、**70 条场景**（按内嵌 JSON 载荷**结构计数**，不靠正则）
  - 零外部引用（`<link>`／`<script src>`／`<img src>`）、`<meta charset="UTF-8">`＋移动视口声明、无 BOM
  - 停用联动 3 组名（联动总览／食品联动／价格联动）**未落页**；口径区有「骨架 73 条，联动 3 条已停用不列」
  - 首次使用横幅＝`hidden:true`（有库）；对**空目录**产物的同脚本对照＝`false`（判法＝判库且不建库，该行只报不判）

### ⚠️ 订正一条旧证据（照实记录）

证据 ①（`01-production-run.md`）里「在列二级组（应为 30）｜30 个名字全部落页」**与实测不符**。按载荷结构数：**二级组真值＝27**，不是 30。

推导：老骨架 30 组 ＝ 8 个在列域的 **27 组** ＋ 联动域的 **3 组**（联动总览／食品联动／价格联动，每组各 1 条停用场景）。票 13 已裁「联动 3 条不列、不建域目录」⇒ 那 **3 个容器随域一起不落页**，页面就该是 8 域／27 组／70 场景（30 − 3 ＝ 27；73 − 3 ＝ 70，实测逐项对上）。副标题只写场景数（70），不受影响。

后续引用票 11 结论时**以 27 为准**；校验脚本的期望值已改成 27。

## 四、两张视口图（复核辅助，非判据）

- 地图判定口径（用户 2026-09-11 定案）**不含**视觉比对，票 11 原列的两侧取图也已按裁定不做；这次**顺手补上**，只为让肉眼终审不必先去找文件。
- 取图方式：本机 **Chrome 无头 ＋ CDP**（node 自带 WebSocket 直连，**不需要** playwright 浏览器二进制、零下载）。图落 `%TEMP%\t194-shots\`（**未入仓**；要留档再决定是否入 `t194-evidence/`）：
  - `桌面视口-1440.png`（1440×2014，整页）
  - `移动视口-375.png`（375×1750，单列）
- 图里可见：标题「居家管家 · 使用手册(HELP)」＋副标题「8 功能域 · 70 场景 · 版本 2.0 · 更新于 2026-09-12 21:26」；功能域标签页**恰 8 个**（物品管理／空间与位置／穿搭出行／统计总览／快递购物／票据凭证／家庭协作／开始使用）＋「关于」；当前页＝物品管理，7 个二级组、29 条场景卡。
- **本次没做的**：与卡路里／饼干记账产物的逐张视觉比对（同一套通用 help 模板渲染、样式同源——那是票 3 的取证结论，这里不重复比对）。

## 五、只剩一条

- **维护者肉眼终审「过」**——产物已交（绝对路径 ＋ 两张图）。**这一条不达成不关票。**
