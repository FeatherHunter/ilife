## Question

用户 Q8=(a) 定了「本次只让新增／改动的 HELP 相关件合规」，而 `docs/agents/structure.md` 的必报五步要求**第一步「影响清单」与第二步「结构设计」先报用户点头**再动手。本票就是那道门：在任何人写第一行代码之前，把形状报清、拿到点头。

要定的形状（把已关两张票的事实摆上桌）：

- 票 1 `#184` 建议把内容资产、渲染接线、命名通式、落盘点都放进 `packages/skill-home/src/help/`（`home.help.lookup` 的出口已住 `src/help/`），并指出**不要照抄** bill 的 `src/triggers/`（工种名）。
- 票 3 `#186` 给了资产的准确形状（三层 `groups`／二级组 id 通式 `<域id>_<序数>`／`types` 按 `+` 切开）与接线要喂的键。
- 票 2 `#185` 会给那 9 个域与 30 个子功能的中英名对照表。

要回答（逐条）：

1. 新增哪几个目录、哪几个文件（内容资产／生成器／渲染接线／命名通式／落盘点），每个文件对外给什么（导出几个、各一句）；有没有共用件、写得出哪两个能力在用。
2. HELP 交付算不算一个「能力」——`src/help/` 这种技能级落点违不违反铁律四（卡路里侧先例：`help-lookup` 是技能级查找入口，铁律四管不到它，见 `docs/skills/skill-calorie/t179-180-structure-design.md` 第五节）。
3. 被碰到的旧件（`src/cli/cmd_read.ts` 等）怎么就地摆正；`cmd_read.ts` 已超告警线，本票要不要顺手抽件（抽的话影响面多大）。
4. 每件归哪张票（票 5 资产／票 6 渲染／票 7 出口），以及「包内 `test` 脚本盖不到新用例」（票 1 查出来的）要不要同批修。

产出：一份形状报告（落 `docs/skills/skill-home/`，含第一步影响清单＋第二步结构设计），并**拿到用户点头**才算关票——本票关票是票 5 与票 6 开工的前置。

## 进度：0%

下一步：等票 2（对账表，要它的中英名）关票，然后写形状报告报用户点头。
