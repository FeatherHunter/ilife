# t407 重评打分 · 手机端回执页（390 宽 · 16 页，返工后复评）

## 一、出图口径（含真 390 自证＋页高）

- 图源：`D:\ilife\docs\skills\skill-bill\` 下**当前**产物 16 份（`t407-代表-记支出-回执页.html` ＋ `t407-页-<词>-回执页.html` 15 份），重出，未用旧图。
- 量法（守红线）：`--window-size=390` 一律不用。做法＝外层页固定 390 宽，目标页放进 `width:390px` 的 iframe，用真 Chrome（CDP，`Emulation.setDeviceMetricsOverride` width=390）截**外层**。外层页与量法脚本：`.scratch\t407-rescore\mobile-receipt\wrap\`、`measure.mjs`。
- **真 390 自证**（逐页读数，非推断）：每页 `内层VW=390`、`外层DOCW=390`、`内层scrollWidth=390`（16/16，无横向溢出）。图宽 390（16/16），图高＝页高＋30px 量尺条。
- 页高读数（px）：记支出 1730／记收入 1782／记报销 2071／拍账单 1829／批量录入 1782／记退款 1922／报销到账 1939／记借出 1978／记借入 1960／记收回 1956／记偿还 1956／记分期 3176／记一笔 2037／改记录 1343／撤销 1615／恢复 1649。
- 逐页 `vision_describe` 1 张，问题照原文未改写；成功 16／失败 0／未评 0。结论以 `read_image` 亲看 16 张 ＋ 产物原文（抽文本落 `.scratch\t407-rescore\mobile-receipt\text\`）为准。
- 图顶部 30px 深色量尺条（`外层宽390 内层VW=390 页高=…`）**不是产物内容**，3 页的模型意见受它干扰，已在下表剔除。

## 二、逐页分数表（16行，含「上版分→本版分」）

| 页 | 层级 | 留白对齐 | 符号串 | 内部话 | 溢出 | 上版→本版 |
| --- | --- | --- | --- | --- | --- | --- |
| 记支出（代表） | 好，金额与状态两级焦点 | 卡片网格齐，徽标行略挤 | 无 | 无 | 无 | 78 → 84 ▲6 |
| 记收入 | 好 | 齐，底部收尾略拖 | 无 | 无（上版顶部量数串已去） | 无 | 82 → 90 ▲8 |
| 记报销 | 好，黑底打标块抢焦点 | 两列已不裂；块内行距偏紧 | 无 | 无（仅 #待报销 业务标签） | 无 | 67 → 81 ▲14 |
| 拍账单 | 好 | 齐 | 有（标题「·」） | 无 | 无 | 92 → 88 ▼4 |
| 批量录入 | 好 | 齐，尾句偏小 | 无 | 无 | 无 | 92 → 92 ＝ |
| 记退款 | 好 | 黑块左右两栏不等高 | 有（标题「·」＋两句「；」，见 §三） | 无（仅 #退款） | 无 | 89 → 86 ▼3 |
| 报销到账 | 好 | 黑块左栏三行偏重 | 有（同上两句「；」） | 无（仅 #报销到账） | 无 | 90 → 81 ▼9 |
| 记借出 | 好 | 标签卡提示三行，两卡不等高 | 无 | 无（字段齐、复制区单组） | 无 | 88 → 83 ▼5 |
| 记借入 | 好 | 标签卡五行最高，落差最大 | 无 | 无 | 无 | 78 → 82 ▲4 |
| 记收回 | 好 | 齐，黑块密度高 | 有（两句「；」） | 无 | 无 | 62 → 88 ▲26 |
| 记偿还 | 好 | 黑块四行，行宽偏长 | 有（两句「；」） | 无（#偿还 是业务标签） | 无 | 92 → 83 ▼9 |
| 记分期 | 好，金额最大 | 齐 | 有（两句「；」＋「分摊预览…；」） | 无 | 无 | 88 → 90 ▲2 |
| 记一笔 | 金额权重弱于同类页 | 齐，日期行窄折行 | 有（末句「；」＋标题「·」） | 无 | 无 | 78 → 79 ▲1 |
| 改记录 | 好，焦点集中 | 齐，纵向最短 | 无 | 无 | 无 | 95 → 88 ▼7 |
| 撤销 | 好 | 齐；「↩︎撤销这一笔」图标与首字相贴 | 无 | 无 | 无（双组按钮为已披露例外） | 92 → 86 ▼6 |
| 恢复 | 好 | 齐 | 有（两句「；」） | 无 | 无 | 90 → 87 ▼3 |

## 三、涨跌归纳

① 上轮被判编造的句子，这次产物里**一句都没有**：`MySQL／UTF8／建表／user_id／created_at／INNR…DOCW…VW=` 在 16 份产物可见文字里 0 命中（全仓文本抽检）；`bill.*` 上屏 0/16；记收入顶部量数串、#收回 标签串、"哪一项／记成什么"列名表**均已不在**。
② 符号串剩 7 页／16：记退款、报销到账、记收回、记偿还、记分期、记一笔、恢复；其余 9 页的「；」只在四页同源的「要撤销这一笔，点下面那颗「复制数据」…；撤销之后记录还在…」上（该句 15/16 页在，记支出也在内）。正文已无「·」串长句，「·」只剩面包屑「记账 · 写入域」与标题；批量的别字引导句、撤销页那句已改干净。
③ 区外内部话剩 0 页／16：模型点出的记借出／记一笔／拍账单「内部字段名」，回产物核对全是**用户面字段标签与回执表头**（记录编号／分类／金额／时间／账户／账本／币种／备注），非库列名、非型名；记收回的 #收回/#未还 是业务标签，不是数据库标识符。
④ 撤销页双组按钮**仍在**（DOM 实测：该页 4 枚复制按钮＝两组「复制数据／复制日志」，其余 15 页各 2 枚＝单组；上版 15 页的重复组已消）。按披露口径注记，不计入否决。
⑤ 涨最多三页：记收回 62→88（▲26，造假标签与量数串清掉）、记报销 67→81（▲14，两列断裂已修）、记收入 82→90（▲8，顶部量数串清掉）。跌的页：报销到账 90→81（▼9）、记偿还 92→83（▼9）、改记录 95→88（▼7）、撤销 92→86（▼6）、记借出 88→83（▼5）、拍账单 92→88（▼4）、记退款 89→86（▼3）——跌的都是**同一处**：黑底说明块的左右两栏不等高／行距偏紧把长页拖重，非新缺陷。

## 四、小计与结论

- 本版平均分：**86.5**（16 页合计 1384）；上版 84.6；**净涨 ▲1.9**。口径：视觉层级 20／留白对齐 20／无符号串 15／无内部话（复制区外）15／双端适配（本端）15／对比度与按钮 15；一票否决三项 0 触发（无切屏、无看不清的低对比、无两组都可点的重复按钮）。
- 过／不过：**不过**（判据 ≥90 过，本版最高 92、平均 86.5）。仍未过 90 的 13 页与最小修法：
  - 记支出 84：徽标行「记支出　支出 金额取负数」用全角空格并排，改一个逗号或删冗余段。
  - 记借出 83／记借入 82：标签卡的提示句压到 3～5 行，与邻卡差 3 行；提示句截到一行，两卡即等高。
  - 记偿还 83／记退款 86／报销到账 81／记收回 88：黑底块左右两栏不等高、行距 1.2 偏紧；统一行距 1.5、两栏等高。
  - 记报销 81：黑底打标块占首屏近三分之一，说明压成两行即可。
  - 记一笔 79：金额字号与同类页齐平，摘要行字号提一级；日期行给足宽度不折行。
  - 撤销 86：按钮内「↩︎」与首字相贴，补右侧内距。
  - 恢复 87／记分期 90：长页收尾两句说明移到「对账信息」折叠里。
  - 改记录 88／拍账单 88／批量录入 92：仅纵向节奏，无需改动版式。
- 模型记分口径差异：模型 16 页均值 86.4（与我 86.5 近同），但**记借出 52 分不可用**——它把量尺条与用户面字段标签当成硬伤；已按亲看结果改正。
