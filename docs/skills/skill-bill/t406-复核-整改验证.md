# t406 整改 · 窄面验证（12 条逐条 ＋ 端到端）

被验对象：提交 `17e8bde`（＋`c54a7d3`），自称 12 条整改全部完成、`node --test` 70 通过／0 失败。
验证依据：`docs\skills\skill-bill\t406-整改-复核项.md`（68 行）的自称，逐条回**源码与实测**核。
本件只读：未改任何源码／文档，仓内只新建本件；机器读数与驱动落 `.scratch\t406-recheck\`（仓外，`.gitignore` 忽略）。

## 一、12 条逐条核验

| 条目 | 整改方自称 | 我核到的（`文件:行号`／命令读数） | 三态 |
| --- | --- | --- | --- |
| A1 · 概念唯一 | 缺槽位那句文案收成 `collect.ts` 一处；`ADD_FIELDS` 改由槽位表派生 | ① 文案：全包搜「缺必需槽位」共 12 处命中——生产代码 6 处：`src\record\collect.ts:4`（头注释）、`:78`（函数签名 `missingSlotMessage`）、**`:79`（唯一拼接处）**、`:95`（调用点），`src\record\write.ts:24`（import）、`:71`（调用点）；另 6 处全在测试件（`test\record-write.test.mjs:140／145／208／210`、`test\cli.test.mjs:70／74`）当断言文本。**拼接地 1 处成立**。② `ADD_FIELDS`：全包 2 处命中，`src\record\write.ts:30` 定义（`RECORD_SLOTS['bill.record.add'].map((s) => s.name)`，即由槽位表派生）＋`:125` 使用；槽位表 `RECORD_SLOTS` 定义地只 `src\record\collect.ts:34` 一处。**不再两处定义成立** | 成立 |
| A2 · 接口粒度 | `writeParts.ts` 对外 8→5，`esc` 收回内部，三常量另立 `pageIdentity.ts` | 自己数本目录 export：`src\shared\writeParts.ts` 对外恰 **5** 件——`BillReceipt:28`／`RECEIPT_FORMAT:42`／`totalChanges:50`／`commandLine:57`／`writeSection:65`；`esc` 定义在 `:45`（`function esc`，无 `export`），文件头 `:2-3` 写明只在本件内部用。新件 `src\shared\pageIdentity.ts` 对外 **3** 件（`DOC_VERSION:15`／`DOC_SKILL:18`／`DOC_TITLE:21`），件名与三个名字都念的是「页面标识」这件活，**无工种名**（不含 render／页面装配那类词）。两枚取值与 HELP 资产一致（技能名 `bill`、标题「饼干记账」＝`src\render\helpFile.ts:26` 的技能名）；`DOC_VERSION='0.1.0'` 是 envelope 版本，与 HELP 手册版本 `HELP_FILE_VERSION='2.0'`（`helpFile.ts:29`）不同口径，不是照抄。备注：「饼干记账」这个字面量在 `helpFile.ts:26／27` 另有一枚（HELP 手册名那件事实），与新件的页标题同值不同事 | 成立 |
| A3 · 类型面 | `write.ts` 的 `as unknown as` 与 `patchRecord` 两个转型全去掉 | 全包搜 `as unknown as`：**`src\record\write.ts` 零命中**；全包还剩 2 处，都在 `src\fetch\db.ts:83`／`:159`（既有件、不在本票被改清单里，复核两轴也没点它）。搜 `patchRecord`：全包零命中。改后走的是 `src\record\write.ts:63-64` 的 `preValue(pre: BillRow, column: string)`（`pre[column as keyof BillRow]`，是受限的字段名转型，不是 `as unknown as`），`:164` 用它比「值是否与改前一致」 | 成立 |
| A4 · 用词 | 先 grep 全部命中逐处改，共 19 处；未清清单另列 | 拿 `docs\agents\wording.md` 禁用表搜本票 16 个改动文件：**源码 11 件零命中**（`cmd_read.ts`／`registry.ts`／`record\{write,collect,receipt}.ts`／`shared\{writeParts,pageIdentity,commandSpec,copyArea,docPage,receiptParts}.ts` 搜「底座／壳／渲染层／落地／写键／读键／键」全部 0）；三份改写文档（`t406-实施证据.md`／`t406-验收-真跑配方与判据.md`／`packages\skill-bill\AGENTS.md`）**零命中**。**还剩 1 处**：`packages\skill-bill\test\record-write.test.mjs:85` 的断言名里那句「形状事实不写在**渲染层**」（引文）——该件在本票改动清单里（23 行改动），但这一行**不是本次新增行**（`git show 17e8bde -- packages/skill-bill/test/record-write.test.mjs` 的增删行里没有它）。整改记录自身另有 11 处命中，全在它自标的「引文表」左列（`t406-整改-复核项.md:32-47`）＋`:49` 的未清清单说明，属原话引文，不另计 | 部分成立 |
| A5 · 包内告警线落点 | 新建 `packages\skill-bill\AGENTS.md`：350＋LF 口径＋「暂定…待维护者确认」，超线件挂号 986／558 | 件存在（63 行）：`:7` 「**告警线＝350 行。数法：LF 口径，只数 `\n`。**」；`:13` 逐字「暂定：本数照兄弟件同数取 350，**待维护者确认**。」；`:9-11` 范围与超线处置（必报五步第四步）；`:23`／`:24`／`:63` 超线件两件逐个挂号带超因与拆法去向。我用节点口径自数（`readFileSync(f,'utf8').split('\n').length - 1`）：`src\triggers\wake-assets.ts` **986**、`src\cli\cmd_read.ts` **558**，与它报的数**逐个相符**；另抽 9 件（`write.ts` 171／`collect.ts` 141／`writeParts.ts` 73／`pageIdentity.ts` 21／`registry.ts` 41／`copyArea.ts` 116／`commandSpec.ts` 46／`receiptParts.ts` 45／`docPage.ts` 54）与该件表内数字**9／9 相符** | 成立 |
| A6 · 第二消费者＋过渡形态＋裁定 | 五件头注释点明 `src/query/`；`registry.ts` 写明过渡形态；三件共用件不搬回 `record/` 的裁定另记 | ① 头注释：实际是**六件**（作者报五件，多写了新件）都点明第二个消费者——`shared\writeParts.ts:9`／`receiptParts.ts:6`／`pageIdentity.ts:8`／`copyArea.ts:6`／`commandSpec.ts:6`／`docPage.ts:6`，各带「随兄弟图 #403 的查询域一起到位」。② `src\cli\registry.ts:10-16` 逐条写明过渡形态：`:8` 本文件手写、`:11` 「没有生成器、`pnpm gen:check` 管不到它、也没有棘轮」、`:12-13` 今天的去重只靠 `build()` 运行期抛错、去重与机器拦交给「生成器链」那张票。③ 裁定：复核件 `t406-复核-结构纪律对账.md:159` 的原建议是「三件移进 `src\record\`」；整改件 `t406-整改-复核项.md:53` 记「**维护者已裁定退掉**」＋维护者原话的理由（第二个用法确定且即将到来，此刻来回搬是白 churn），并把「等查询域到位再复核落点」写成交棒项；`t406-实施证据.md:317` 另记「维护者把结构轴重裁成 A1–A6（写法与审查报告原建议不同的地方，一律按维护者裁定）」。**裁定被如实记录，未被写成自己的自由裁量** | 成立 |
| B1 · 体积门对准交付整页 | 新增 `gatedHtml()`，四条交付路都过它；原先判在别处的两句删掉 | ① 结构：`src\cli\cmd_read.ts:535` `const gatedHtml = (html: string): string => { assertHtmlSize(html); return html; };`，两个交付分支各用一次——`:538` `gatedHtml(help.deliver.html ?? sectionHtml())`（HELP 整页／速查表分节页两条）与 `:541` `gatedHtml(sectionHtml())`（迁移过的整页／未迁移套模板整页两条），四条路都从这一处过。`assertHtmlSize` 全包调用点**只此一处**（另一处在 `test\render.test.mjs:35`，测试直接调）；原先判在别处的两句确已删。② 我自己做变异（动手改 `src\render\html.ts:5` 的 `256 * 1024` → `512`，改完还原）：红腿四条全红——迁移过的写命令整页 `EXIT=5`「HTML 超体积：63315 > 512」、未迁移命令套模板整页 `EXIT=5`「1142 > 512」、HELP 缺省整页 `EXIT=5`「130735 > 512」、HELP 速查表分节页 `EXIT=5`「14923 > 512」；还原后四条全 `EXIT=0`（`delivery.bytes` 63315／1379／130735／14923） | 成立（独立复现） |
| B2 · 补 `data-shape` | `writeSection` 补回标记，取值＝本次 envelope 的形状，由调用方传同一枚 | 结构：`shared\writeParts.ts:65-72` 的 `writeSection` 收 `shape` 字段并写进 `:72` 的 `data-shape="…"`，**没有写死**；两个调用方传同一枚 envelope——`src\record\collect.ts:139`（`shape: envelope.shape`）与 `src\record\receipt.ts:95`（同）。真跑（我自己三条）：回执页 `data-shape="receipt"`、采集页 `data-shape="receipt"`，值都与本次 stdout envelope 的 `shape` 字段同源；两页四枚标记齐（`data-skill="bill"`／`data-slot="ilife:bill:receipt"`／`"ilife:bill:collect"`／`data-shape`／`data-key`） | 成立 |
| B3 · 补记边界事实 | §三.6.1 补「建库建表但不写行」（库 20480 B、行数 0） | `t406-实施证据.md:177-182`：库文件 `biscuit_accountant.db` **20480 B**、`bills` 表 **10 列**、`SELECT COUNT(*) FROM bills` ＝ **0** 且连跑两次仍 0；`:179-180` 另把 `total_changes()` 那个 0 的射程说清（另起只读连接的读数，证「不写行」的是行数两次为 0）；`:182` 口径改成「本票的语义改变是『**不写账单行**』，不是『不碰库文件』」。我自己复跑（`.scratch\t406-recheck\db2`）：空目录 → 缺槽位 `--params '{}'` 第一次 `EXIT=0` → 库 20480 B、`bills` 10 列、行数 **0**；第二次 `EXIT=0` → 行数仍 **0** | 成立 |
| B4 · 字节读数改可复现口径 | 单次字节数换成量级；老路径上界那句更正 | `t406-实施证据.md:139` 写明单次字节数不可复现（换库目录差 1776–1860 B）、`:152` 改「三处字节数已改成量级」并给出结构断言六条（整页收尾／首行 doctype／含 `<section`／四枚标记／复制区三枚／采集页缺槽位明示）、`:154` 量级判据（新页 ≈60–64 KB、老路径 ≈0.9–1.3 KB）、`:156` 明确更正「962–1011 B 上界**不成立**」（`bill.analysis.overview` **1235 B** 越界 224 B），`:372`／`:374` 又给整改方自己的老路径读数 **1241 B**。**不再写死单次字节数当判据**；任务口径里的「约 1235–1241 B」两个读数都在件里可查（1235 复核者／1241 整改方），件里没有把新的硬上界再写一句——这正合 B4 的方向（口径改成区间＋结构） | 成立 |
| B5 · 对齐判据件 | 验收件第三节定点更正三处，未重写全篇 | `t406-验收-真跑配方与判据.md:94` 口径更正：单次字节数不作可复现判据、老路径「962–1011 B 上界」更正为「没有这样的上界」（`bill.analysis.overview` 1235 B 越界）、判据以结构为主量级为辅；`:96` 写明新页也带 `data-shape="receipt"`、四枚标记、断言钉在 `test\record-write.test.mjs`；`:100`／`:119` 点明 `delivery.bytes` 与文件实际大小一致那条仍可复现、括号里字节数只当参考。与 B2／B4 已对齐 | 成立 |
| B6 · 记一条已探明的边界 | §三.6.2 记 argv 上界（≥120 KB `ENAMETOOLONG`）＋逃生通道 | `t406-实施证据.md:187-196`：params 90043 B 照跑（退出码 0）、120043 B 时 `spawnSync` 报 `ENAMETOOLONG`、stderr 空、命令根本没启动，上界落在 90 KB 与 120 KB 之间；`:191` 另记真跑里交付整页最大 243180 B（近 237 KB，仍在 256 KB 门下）；`:193-195` 三条逃生通道（分次 `bill.record.update` 补、进程内驱动绕开 argv、命令行留在 90 KB 以内），并注明不改代码 | 成立 |

小计：**成立 11 条，部分成立 1 条（A4），不成立 0 条**。

## 二、端到端复跑读数

环境：cwd `D:\ilife`，`SKILLS_DB_PATH=D:\ilife\.scratch\t406-recheck\db`，`BILL_FORCE_PROD=1`，node v24 系。
命令与读数全落 `.scratch\t406-recheck\`（`run-log.txt`／`tsc.txt`／`test.txt`／`run1-3.*`／`rows-proof-log.txt`／`mut2-log.txt`）。

**① 构建与测试（两条命令各跑通一次）**

| 命令 | 退出码 | 读数 |
| --- | --- | --- |
| `npx tsc -b packages/skill-bill` | **0** | 零诊断（`tsc.txt` 零字节） |
| `node --test "packages/skill-bill/test/*.test.mjs"` | **0** | `tests 70`／`suites 14`／`pass 70`／`fail 0`／`cancelled 0`／`skipped 0`／`todo 0`，`duration_ms 5501` |

与整改方自称的「70 通过／0 失败」**逐个相符**。

**② 三条真跑**

| 真跑 | 命令（cwd `D:\ilife`，`--html` 落 `.scratch\t406-recheck\out\`） | 退出码 | 产物 | 量级 | `data-shape` 等 | 复制区 | 缺槽位明示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 有槽位 `add` | `node packages/skill-bill/dist/cli/cmd_read.js bill.record.add --params '{"category":"餐饮/外卖/午餐","amount":-35,"note":"午饭","account":"微信","ledger":"日常","currency":"CNY"}'` | **0** | `out\add-receipt.html` | 63417 B ＝ **61.9 KB**（与 `delivery.bytes` 一致） | `data-skill="bill"`／`data-slot="ilife:bill:receipt"`／`data-shape="receipt"`／`data-key="bill.record.add"` | 有 | —（已写库） |
| `update` | 同上换 `bill.record.update --params '{"id":1,"category":"餐饮/外卖/晚餐","amount":-42.5,"note":"改成晚饭"}'` | **0** | `out\update-receipt.html` | 62629 B ＝ **61.2 KB** | 四枚齐，`data-slot="ilife:bill:receipt"`、`data-shape="receipt"` | 有 | — |
| 缺槽位 `add` | `bill.record.add --params '{}'` | **0** | `out\collect-page.html` | 64108 B ＝ **62.6 KB** | 四枚齐，`data-slot="ilife:bill:collect"`、`data-shape="receipt"` | 有 | 有：「缺必需槽位：category、amount（已出采集页，补齐后重跑同一条命令）」 |

量级口径照 B4：新装配的整页 **≈61–63 KB**（六十 KB 量级），不把上面某次的精确字节当判据；`delivery.bytes` 与文件实际字节数一致这一条**可复现**（两处逐条相符）。

**③ 缺槽位不写行（自证）**

- 独立只读连接数行（`.scratch\t406-recheck\rows.mjs`）：主库序列 add（写 1 行）→ update → 缺槽位 add 之后，`bills` 行数仍 **1**；专库 `db2` 更干净：空目录 → 缺槽位 `--params '{}'` 跑第一次 → `{"exists":true,"tables":["bills","sqlite_sequence"],"billsRows":0,"billsColumns":10}`，库文件 **20480 B**；**连跑第二次仍 0 行**。与 B3 的记账一致：**建库建表、不写账单行**。

**④ 回执整页能打开（无头 Chrome 实渲染）**

- `out\add-receipt.html`（63417 B）与 `out\collect-page.html`（64108 B）两份整页都在无头 Chrome 里渲染出图（1200×900，PNG 44738 B／52195 B，落 `.dsh-vision-router\artifacts\`）；看图复核：回执页不是空白页，有标题「记一笔 · 回执」、状态徽标「写入成功」、三块统计卡（已改动／影响行数 1 行／写入字段 7 项）、写入字段表、底部「复制数据 ▾」「复制日志」两枚按钮；采集页有「补齐槽位」标题、副标题「缺必需槽位：category、amount（已出采集页，补齐后重跑同一条命令）」、徽标「待补槽位 · 未写库」、采集表单。**判据不在「文件存在」而在「页真能看出内容」**。

**⑤ 体积门变异（B1 的独立复现，读数）**

- 红腿：`src\render\html.ts:5` 的 `256 * 1024` 改 `512` ＋ `tsc -b --force`（产物实测 `BILL_HTML_MAX_BYTES = 512`）→ 四条交付路**全红**（退出码 5，读数见上表 B1 行）。
- 绿腿：还原 ＋ `tsc -b --force`（产物实测 `256 * 1024`）→ 四条交付路**全绿**（退出码 0）。
- **一条给后来人的坑**：第一轮我漏了 `--force`，而 `Copy-Item` 保留原 mtime，增量 `tsc -b` 判定「源文件没变」、没有重编——产物里的门限仍是 512，绿腿假绿。**还原源码不等于还原产物**；做同类变异的人还原后必须 `--force` 重编，并用产物内容自证。

**⑥ 工作树自证（只读纪律）**

- `git diff --stat -- packages/skill-bill` → 空；`git status --porcelain -- packages/skill-bill` → 空；`src\render\html.ts` 的 SHA256 ＝ 变异前备份的 SHA256（`AC6B472965FF212062C8B8BAE408AF0B9B3F8380EE2A8292259F962F4CCDB78F`）。除本件外我未新增／改动任何文件；仓内其余 `M`／`??` 条目属其他在途工作者（`skill-calorie`／`base-render` 等），与本次验证无关。无 commit／amend／rebase／reset／push／checkout。

## 三、不成立或部分成立的条目

**A4 · 用词（部分成立）**

- **差了哪一点**：本票 16 个改动文件里还剩 **1 处**禁用表命中——`packages\skill-bill\test\record-write.test.mjs:85` 的断言名「注册表一能力一行；迁移过的命令的形状从它派生（形状事实不写在**渲染层**）」（引文，禁用词只作引文出现）。这一行不是本次新增行（本票对该件的 23 行增删里没有它），但该件在本票改动清单里；整改件「未清、且本次不打算清的命中」那段只挂了 `test\cli.test.mjs:37／55／65／76／88` 与 `src\render\*` 若干，**漏记这 1 处**。源码 11 件与三份改写文档是零命中，整改的 19 处改法本身核得到。
- **最小修法**：把那 4 个字改成「页面交付那一层」（只动 `it(...)` 的断言名，不动断言体）；不愿改就把该行挂进「未清清单」并写清理由（旧文、非本次新增、属整包按域重排那张票）。
- **会不会影响本票验收**：**不影响**。测试件不在本票验收判据里（判据是两条命令各跑通一次＋回执整页能打开＋五步报齐），该行是旧文；且这不是产物或机器拦的问题——`node --test` 70／0 实测不变。

## 四、结论

**放行。** 12 条里 11 条成立、1 条部分成立（A4 只差「未清清单漏记 1 处旧命中」这一点，属记账面，最小修法是一句话），端到端全绿：`tsc` 退出码 0、`node --test` 70 通过／0 失败、三条真跑全退出码 0，体积门的四条交付路我做了独立变异（红→绿都对），缺槽位那条连跑两次库内 0 行、库文件 20480 B。

**本票验收在整改后仍成立**：两条命令各跑通一次（`bill.record.add`／`bill.record.update` 实测各退出码 0）、回执整页能打开（61.9 KB／61.2 KB 两份整页在无头 Chrome 里渲染出内容，`delivery.bytes` 与文件实际大小逐条一致）、五步报齐的落点在位（`t406-整改-复核项.md` 的 12 条逐条对账表 ＋ `packages\skill-bill\AGENTS.md` 的超线件挂号即必报第四步）；五步全文未在本窄面内逐条对账。
