# t729 · 门禁运行记录导出（对账源）

- 票：[#729 analysis 域端到端搬迁](https://github.com/FeatherHunter/ilife/issues/729)
- 性质：按 `docs/subagent-concurrency-protocol.md` §2.3 第 4 条，把**本票声明过的**加锁运行条目
  从 `.scratch/locks/gate-runs.log`（未受版本控制）抄到受版本控制的路径，作证据对账源。
- 抄录范围：`ticket=729` 的条目中，**证据里引用过的那几次**（干活时的调试红不在此列；
  §2.4 第 3 条：没有声明的运行不按裸跑追究）。
- 读法：每行 `GATE-RUN runId=<运行标识> cmd=<命令> exit=<退出码>`——标识与尾列可在运行记录里一对一核。

```
GATE-RUN runId=982b32fe-2a0a-4b8f-8202-7fb4d2ec6f62 cmd="node node_modules/typescript/bin/tsc -b packages/skill-bill" exit=0
GATE-RUN runId=ce737966-ee39-4121-8414-09b9d159dbd4 cmd="node --test packages/skill-bill/test/*.test.mjs" exit=0
GATE-RUN runId=a1cfe266-28db-4317-bdbc-db649dd883f9 cmd="node --test packages/skill-bill/test/t729-分析域.test.mjs" exit=0
GATE-RUN runId=d9a3bf7c-f49a-4cca-b864-87630fcb35ed cmd="node docs/skills/skill-bill/t729-探针-真出口.mjs" exit=0
GATE-RUN runId=73b40855-2d6a-4b82-bc3b-eb754e76bf10 cmd="node packages/skill-bill/scripts/gen-cli.mjs --stamp" exit=0
GATE-RUN runId=6470121f-335a-4007-8c9d-b2bb0ecd3e43 cmd="node packages/skill-bill/scripts/gen-cli.mjs --check" exit=0
GATE-RUN runId=60ea9412-f42e-40ad-a4e0-a48c414ac297 cmd="node .scratch/t729/图判据-读数-图族.mjs" exit=0
```

**未持锁的三道门**（`check-ratchet-tight.mjs`／`check-scene-shape.mjs`／`check-warning-line.mjs`）：
它们**只读**（量文件、读台账、比对），按 §2「不需要持锁」那一档直接在当刻工作区跑；
读数在 `t729-证据.md` §二逐条给（棘轮 9/9、形状门 54/54、告警线门 13/13）。

**本票是共享工作区里的三票之一**（#729 分析／#730 目标／#731 开始使用，同树并发）。
上面这些运行都在**三域都落地之后**那一窗里跑；`src/` 漂移与编译指纹的绑定按 §2.6 办——
本票所有以 `dist` 为前提的读数都取「先 `tsc -b` 再跑」的同一窗（编译 runId 见上），
窗口内没有别的席位在改 `src/analysis/`（该目录只有本票的件）。
