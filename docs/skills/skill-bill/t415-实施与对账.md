# t415 详情页 · 实施与对账（结构纪律五步 ＋ 真跑读数 ＋ 变异自证）

> 票 #415「饼干记账查询详情页：1词 record.detail（查账单详情）」。本件是**实施证据**：
> 事前影响清单与事后对账逐行对得上（structure.md 第五步是唯一机械验收点），读数全部是当刻盘上跑出来的。
> 认领：`gh issue edit 415 --add-assignee=@me` 已落（assignee `FeatherHunter`，复核见 `gh issue view 415 --json assignees`）。
> 研究输入：`.scratch/t415/research-415416417.md`（§1 详情专属差表：字段全＋软删可见＋复制区＋阻断＋最长匹配）＋
> `.scratch/t403/research-components.md`（清单一复用公共组件／清单二双能力才进 shared／清单三不动 base）。
> 前票已提交（`git log --oneline -15` 当刻含 `f93f21b 证据(414)／c119c90 测试(414)／01d56fb 功能(414)`，只读确认，不改其文件）。

## 一 必报五步

### 第一步 · 影响清单（动手前，逐行一个能力目录）

**新增／改动（本票声明路径，仅三类）**

| 件 | 一句话 | 为什么 |
|---|---|---|
| `packages/skill-bill/src/query/detail.ts` | 新建详情装配件 `queryDetailDoc`（金额卡＋状态卡＋字段表十行＋复制区＋页壳，127 LF） | 票内核：详情专属版式；七列表格放不下币种／两时间／软删态，KPI 四格对单条互斥——确需新块，放查询域本地 |
| `packages/skill-bill/src/query/read.ts` | `viewRecordDetail` 一支改含删行取数＋载荷加法式＋调新装配件；`today/range/search` 三支一字不动 | 票内核：软删态可见（已撤销不报无此号）＋字段全（10 列）＋复制区详情口径；必需改动仅此一支 |
| `packages/skill-bill/test/t415-record-detail.test.mjs` | 本票判据（9 条：整页 2／软删三态 2／红线 3／路由 2） | 一票一判据；2 笔种子＋整段开标签计数 |
| `docs/skills/skill-bill/t415-实施与对账.md` | 本件（实施证据，含五步＋真跑＋变异＋GATE-RUN） | 证据落归属件目录 |
| `docs/skills/skill-bill/t415-锁记录导出.log` | 锁目录中本票条目导出（§六对账源） | 运行记录在未受控目录，导出件当对账源 |
| `.scratch/t415/*` | 窗口五件（`window-1/-3/-4/-c1/-c2/-c3.ps1` 照抄 t411/t414 模板）＋`mutate.mjs`（照抄 t411 模板改锚点）＋日志 | 整窗交锁＋变异自证，永不入库（`.gitignore:5:.scratch/`） |

**不动（逐条有去处）**

| 件 | 为什么不动 |
|---|---|
| `viewRecordToday／viewRecordRange／viewRecordSearch` 三支 | 票面「不动其它三页」原话；t412（13 条）／t413（17 条）／t414（12 条）／t411（16 条）复跑全绿即回归 |
| `packages/skill-bill/src/query/commands.ts` | 声明是契约（`bill.record.detail` 形状 detail 不变）；含删行与字段表是处理体入参与装配，不进声明 |
| `packages/skill-bill/src/query/list.ts` | 通用列表装配是三页的共用底（`COLUMNS` 七列＋`kpiCards` 四格＋`listEnvelope/detailEnvelope` 成对）；本票不改它，只**引用** `detailEnvelope`（`item: Record` 天然容加法式两列） |
| `packages/skill-bill/src/shared/*`、路由（`policy/wakewords.ts`） | 装配与路由沿用；本票无新词、无新形状，`WAKE_TABLE` 一字不动（最长匹配只回归不断言改） |
| `packages/skill-bill/src/fetch/*`、`policy/*`、`render/*`、`base-paint` | 取数只引用（`fetchAll includeDeleted` 经 `fetch/index.ts` 公开接口，不重写 SQL；`needId` 经 `policy/index.ts`）；`toBillItem／BillItem` 本体不动（载荷旧 8 字段一字不动）；`base-paint/blocks` 只调用不新增组件 |

### 第二步 · 结构设计（目录树／每件职责／导出面／共用件＋沉淀判断）

```
packages/skill-bill/src/query/detail.ts（新建 127 LF，在线内）
  QueryDetailInput { key, params, wakeWord, window, row, envelope, source, actionAt }: 8 字段（铁律五≤8）
    key／params＝命令原文取数；wakeWord 恒查账单详情；window＝编号·时刻·已撤销后缀；
    row＝含删行 BillRow；envelope＝调用方拼好的 detailEnvelope；source／actionAt＝日志 3/5 段
  queryDetailDoc(input): string            对外 1 件（＋类型 1 件，≤5；不出 src/query/index.ts 门，域内互用）
    金额卡＋状态卡（renderKpiGrid 两格）＋ 字段表（renderDataTable 字段/值两列十行）＋ 复制区（copyArea/copyLog）＋ 页壳（pageShell）
packages/skill-bill/src/query/read.ts（改后 313 LF，在线内）
  viewRecordDetail(params, db): ViewOut    改：fetchAll includeDeleted 找 id→真无才 BILL_RECORD_NOT_FOUND；
                                           item＝{...toBillItem, created_at, deleted_at}（加法式）；window 加已撤销后缀；调 queryDetailDoc
```

- 每个文件对外给的东西 ≤5（铁律五）：`detail.ts` 对外 1 函数＋1 类型（域内用，不进 `index.ts` 门）；`read.ts` 对外仍是四处理体，新增零导出（`detailCards／detailRows／textOrDash` 全在 `detail.ts` 域内）。
- 依赖方向：`query → fetch／policy／render／shared／base-paint` 不变，不反向。`fetchAll` 经 `fetch/index.ts` 公开接口（铁律一：用接口不抄 SQL）；`needId` 经 `policy/index.ts`；`toBillItem` 经 `render/views.js` 同一份（铁律二：行投影唯一定义）。
- **沉淀判断（本票结论，复用什么／新建落哪／为什么）**：
  - 复用 `base-paint/blocks`：`renderKpiGrid`（金额卡＋状态卡两格，`status: ok/danger` 徽走冻结 `STATUS_KINDS`）＋`renderDataTable`（字段／值两列，`data-label` 窄屏行卡化自带）——清单一「直接用，不自造」。
  - 复用 `shared` 四件（两能力在用，指名）：`copyArea／copyLog／actionStamp`（`shared/copyArea.ts`，写域＋查询域）＋`pageShell`（`shared/pageShell.ts`，`domain:'query'`）＋`commandLine`（`shared/writeParts.ts`）＋`detailEnvelope／DOC_*／sceneKeyOf`（`query/list.ts`＋`shared/pageIdentity.ts`，形状与载荷成对）——只引用，不改其一句。
  - 新建落查询域本地（`src/query/detail.ts`）：字段表列定义（`DETAIL_COLUMNS` 字段／值）＋十行装配（`detailRows`）＋两卡装配（`detailCards`）＋占位 `—` 口径——今日唯一消费者是详情页（写入域无「单条十列竖表」场景），单能力不进 `shared`（`structure.md` 共用件：从第二个用法里长出来，不是预先设计；`t403` 清单一并裁定过滤芯片／聚合卡同理本地）。
  - 不进 `shared` 的点名：本票**零新增 shared 导出**（`shared/*` 一字不动）；待第二能力（写入域或分析域）引用同一竖表之日，再从查询域本地提升（届时改件头「谁在用」并补断言，`t403 §2.2` 门槛）。
  - KPI 取舍判据：舍四格（笔数恒 1 是废话，支出／收入／净额对单条互斥其二为零）→ 改两格（金额卡说清支出／收入／零金额，状态卡说清正常／已撤销＋徽）。块序＝金额卡＋状态卡＋字段表＋复制区（`t411` 骨架四块中学 §4 留／舍／改三档）。

### 第三步 · 写代码（改了什么，一行一因）

1. 进口行（`read.ts`）：撤 `getById`（软删行它永远读不到），增 `queryDetailDoc`（`./detail.js` 域内件）；`fetchAll／BillFetchError／needId／toBillItem／detailEnvelope` 沿用（同一扇门，不另开）。
2. `viewRecordDetail` 取数：`fetchAll(db, { includeDeleted: true }).find(id)`→无才 `BILL_RECORD_NOT_FOUND '无此账单：'+id`（与 `db.ts:128` 同字，exit 4；`needId` 坏 id 仍 exit 2）。用 `fetchAll` 公开接口即含删行，不动 `db.ts`（本票声明外），列表三页默认过滤一字不动。
3. 载荷加法式：`{ ...toBillItem(row), created_at, deleted_at }`（旧 8 字段逐字同值同类型，新 2 列只增不改；`QueryDetailData.item: Record` 天然容下，无需改 `list.ts` 类型即契约兼容）。
4. 窗口与复制详情口径：`window＝记录编号 N · time（＋已撤销后缀）`；`copyLog.detail＝详情 1 笔 · 记录编号 N · 正常／已撤销`（旧「查到 1 笔」是列表话术）；`command＝commandLine(key, params)` 可照抄重跑；`source＝DB_FILENAME＋查询结果只读不改库`；`actionAt＝actionStamp()` 唯一取法。
5. 新件 `detail.ts`：`deleted＝deleted_at 非空即真` 唯一判地；两卡（金额两位小数与载荷逐字一致，状态徽 `ok/danger`）；十行（编号／时间／分类／金额／账户／账本／币种／备注／创建时间／删除时间，空即 `—`，币种缺省也是读数）；`slot/page:'list'＋shape:'detail'`（`writeParts SECTION_CLASS` 无 detail 槽，沿用 list 槽，靠 shape 选形，与旧详情页同标记）。

### 第四步 · 超线报警

本包告警线 **350 行（LF 口径，只数 `\n`，`packages/skill-bill/AGENTS.md`）**：

- `packages/skill-bill/src/query/detail.ts` **LF 127**：**在线内，不超线**。判据：`readFileSync(f,'utf8').split('\n').length-1`（实测 `LF=127`）。
- `packages/skill-bill/src/query/read.ts` **LF 313**（改前 311，`+24/-13`，净 `+2` 行注释换版式）：**在线内，不超线**（同口径实测 `LF=313`）。
- 未碰件读数沿用 #411 后复测（`list.ts 182／commands.ts 53`，均在线内）；挂号两件（`wake-assets.ts 986`、`cmd_read.ts 485`）本票未碰、不扩大。
- 测试文件（`test/*.mjs`）、模板、构建产物不进管辖计数（`structure.md` 管辖节）。

### 第五步 · 交付对账（逐行对第一步）

- 第一步「源码两件」→ `detail.ts＋read.ts` 已在 `822710d` 落成（`2 files changed, 142 insertions(+), 13 deletions(-)`），路径一致。
- 第一步「判据」→ `t415-record-detail.test.mjs` 已在 `f41e22e` 落成（新建 176 行），路径一致。
- 第一步「本件＋锁导出＋scratch」→ 本件与 `t415-锁记录导出.log` 待 c3 落成；`.scratch/t415/*` 永不入库（ignore），与第一步一致。
- 未碰件（`today/range/search` 三支／`commands.ts／list.ts／shared／路由／fetch／policy／render` 本体）：`git diff f41e22e -- <未碰件>` 干净（多席他票的在途件与本票无关，本票提交一律 `-- <自己路径>` 限定）。**偏差为零**。

## 二 真跑读数（全部经加锁包装器）

### 窗口一（`e4ef733b`）：编译 → 靶向（t415 新锁＋t411/t412/t413/t414 回归）→ 全量

| 命令 | 退出码 | 摘要行 |
|---|---|---|
| `node node_modules/typescript/bin/tsc -b packages/skill-bill` | 0 | 零诊断（窗口一＋窗口三末段＋窗口四三处一致） |
| `node --test test/t415-record-detail.test.mjs` | 0 | `tests 9 / pass 9 / fail 0`（整页 2／软删三态 2／红线 3／路由 2） |
| `node --test test/t411-query-skeleton.test.mjs` | 0 | `tests 16 / pass 16 / fail 0`（旧锁不漂移，详情单条页仍 shape detail） |
| `node --test test/t412-record-today.test.mjs` | 0 | `tests 13 / pass 13 / fail 0`（查账单别名同页＋详情不被吞） |
| `node --test test/t413-record-range.test.mjs` | 0 | `tests 17 / pass 17 / fail 0`（周月锚点＋分类三级＋空串阻断） |
| `node --test test/t414-record-search.test.mjs` | 0 | `tests 12 / pass 12 / fail 0`（五词＋精确回归＋空态） |
| `node --test`（包内全量，窗口一） | 0 | `tests 245 / pass 245 / fail 0`（236 基线＋9 本票） |

种子（临时库，2 笔，分类一律合法 L1）：`餐饮/外卖/午餐 -35.5 t415午饭 #工作餐 支付宝／生活`；
`工资/基本工资 +8000.25 t415工资 银行卡／工资`（币种皆人民币）。id 经 `search q` 回查锁定（`ID1／ID2`），不写死自增。

存在 id 整页（`ID1` 支出笔，断言即真跑，加锁真出口 `dist/cli/cmd_read.js` spawn）：
`shape=detail`＋`item.id＝ID1`＋`amount -35.5（数）`＋`created_at 串`＋`deleted_at` 在场＋
H1 `查账单详情`（`<h1 class="ilife-block-page-shell-title">查账单详情</h1>` 整段）＋
副标题 `记录编号 ID1 · 2026-09-06 12:00:00`＋KPI 两格（`<div class="ilife-block ilife-block-kpi-card">` 整段计数 `2`）＋
字段表一张（`<table class="ilife-block-data-table-table">` 计数 `1`）＋`<tr>` 计数 `11`（表头 1＋字段 10）＋
十标签（编号／时间／分类／金额／账户／账本／币种／备注／创建时间／删除时间）＋
`-35.50`（两位小数＝载荷数值）＋`支付宝`＋`人民币`＋`t415午饭`＋正常徽（`<span class="ilife-status-badge ilife-status-badge-ok">`）＋
无 danger 徽＋复制区（复制数据／复制日志／`bill-cmd-read bill.record.detail`／`--params`／`&quot;id&quot;` 转义与否皆可）＋
页标记（`data-key="record.detail"／data-shape="detail"／data-page` 恰一枚）＋复制日志详情口径（`详情 1 笔 · 记录编号 ID1 · 正常`，非列表话术）。
收入笔（`ID2`）：`8000.25` 文案＝载荷数值＋金额卡 `收入`＋`银行卡`。

软删三态（同 `ID1` 三次）：撤销（`bill.record.update {"op":"undo","id"}` exit 0）后详情仍 exit 0＋
danger 徽（`<span class="ilife-status-badge ilife-status-badge-danger">`）＋`已撤销`＋删除时间原值（载荷 `deleted_at` 非空且页内逐字）＋
副标题后缀 `已撤销`＋复制日志 `详情 1 笔 · 记录编号 ID1 · 已撤销`＋仍两格＋仍 11 行；
恢复（`{"op":"restore","id"}` exit 0）后 `deleted_at null`＋ok 徽＋无 danger 徽。

红线（exit 码＋stdout 空＋stderr 语义）：`{}` 缺 id→exit 2；`0／-1／1.5／"1"／null` 坏 id→exit 2；
`99999` 真无此号→exit 4（`无此账单`），三者 `stdout` 皆空（不冒充正常）。

路由回归（`routeWakeword` 经 `dist/policy`）：`查账单→bill.record.today`／`查账单详情→bill.record.detail`／
`帮我查账单详情里的那笔（含两词）→detail`（最长匹配，长者胜；短词不反吞）。

## 三 变异自证（改坏必红 → 从 HEAD 取回必绿）

工具 `.scratch/t415/mutate.mjs`（照抄 `.scratch/t411/mutate.mjs` 改锚点为两件）：锚点命中数 ≠1 即拒绝落盘；还原一律 `git show HEAD:<件>` 逐字节取回。

| 变异 | 改的是什么 | 变异后 | 还原后 |
|---|---|---|---|
| M1 | 含删行撤掉（`fetchAll(db, { includeDeleted: true })`→`fetchAll(db)`，已撤销退回默认过滤） | 靶向 **9 跑／8 过／1 红**，红在软删三态撤销后：实得 exit 4（`无此账单：1`），期望 exit 0 整页 | dirty dist 重编后 **9/9 绿**（窗口三末段） |
| M2 | 字段表少一行（`币种` 行删掉，字段不再全） | 靶向 **9 跑／7 过／2 红**，红在整页与撤销后：实得 `<tr>` 10，期望 11（币种标签亦无） | 同上 **9/9 绿** |

```
WINDOW3-SUMMARY pre=0 a1=0 tm1=0 tt1=1 r1=0 a2=0 tm2=0 tt2=1 r2=0 post=0 all=0
WINDOW4-SUMMARY tsc=0 t415=0 all=0（tests 245 / pass 245 / fail 0）
```

- `pre=0／post=0`：变异前后源码与 HEAD 逐字节一致（`DIFF-CLEAN`）。
- `tt1=1／tt2=1` 即变红（非 0＝已变红）；`all=0` 即还原＋重编后复绿。回滚＝源码取回＋重编，两步缺一即假（t413 第四节第 2 条同教训，本窗已把重编织进窗口三末段）。

## 四 走偏与整改（如实记，别当没发生）

1. **复制命令 id 断言裸双引号假红**：初版断言 `text.includes('"id":'+ID)` 得 0（HTML 里双引号已转义为 `&quot;`，`buildLogText→renderCopyBlock` 经五字符表）。整改：改断言为 `--params` 必在＋`&quot;id&quot;` 与 `"id":ID` 二选一（转义与否皆可），复跑 9/9 绿。教训：复制区文本断言先看转义表，不数裸串（与 t411 M1 裸子串命中 CSS 同族）。
2. **首跑窗口一 1 红即全量 1 红**：`cb323862` 那次 `WINDOW1 tsc=0 t415=1 … all=1`（红的即上条），包装器 exit 仍 0（锁只保序不保测试结果）。整改：修测试后重跑 `e4ef733b` 全绿（`t415=0 … all=0`）。首跑行仍在锁日志里（非门禁证据，见第六节）。
3. **暂存区他席在途件**：c1／c2 窗口 `git diff --cached --name-only` 均只含自己路径（c1 两件／c2 一件），未卷入他席件（t414 第四节第 4 条同律，本票提交一律 `-- <自己路径>` 限定）。
4. **提交信息无 BOM**：三笔提交信息文件一律经 `write` 工具落盘（无 BOM），`git commit -F` 引用；提交前一律 `git diff --cached --name-only` 复核（见各 `window-c*.ps1` 日志）。

## 五 边界与不做（本票范围外，逐条有去处）

- **列表三页的 KPI／列／窗口一字不动**：`list.ts COLUMNS／kpiCards／listEnvelope` 沿用；详情两卡与竖表是另一套，不反写回列表（#415 不动其它三页）。
- **`getById` 本体不动**：`fetch/db.ts:126-129` 仍是默认过滤（列表与写入域在用）；详情含删行走 `fetchAll includeDeleted` 公开接口，属查询侧另一支，不改取数层（改了即扩大到三页）。
- **转账单条金额卡**：本票种子无转账；`detailCards` 金额符号照库（支出负／收入正），转账分类不另算（KPI 转账除外是列表合计口径，单条无需）。
- **SKILL/HELP 说明面接线**：是 #416；本票 `SKILL.md` 一字不动。
- **样张 HTML**：本票无样张件；整页由 9 条判据逐条落盘（`--html` 产物在临时目录，块级读数见第二节）。

## 六 GATE-RUN 逐行声明（锁记录导出）

锁目录 `.scratch/locks`（`gate-runs.log`），本票 6 次持锁门禁运行已落盘（12 行＝6 START＋6 RUN，全部 `exit=0`；导出件 `t415-锁记录导出.log`；本件提交 c3 的两行在锁日志中，提交后以 `Select-String gate-runs.log -Pattern <runId>` 复核）：

- `cb323862`（窗口一第 1 跑：编译／t415 靶向／四回归／全量）→ `exit=0`，`WINDOW1-SUMMARY tsc=0 t415=1 t411=0 t412=0 t413=0 t414=0 all=1`（t415 复制 id 转义红，见第四节第 1 条；非门禁证据）。
- `e4ef733b`（窗口一第 2 跑，同上）→ `exit=0`，`WINDOW1-SUMMARY tsc=0 t415=0 t411=0 t412=0 t413=0 t414=0 all=0`（tests 245 / pass 245 / fail 0）。
- `d8c05634`（提交 c1：源码两件）→ `822710d`。
- `5b11a3fa`（提交 c2：测试）→ `f41e22e`。
- `239c2992`（窗口三：变异 M1／M2 红→还原→diff 干净→靶向复绿）→ 包装器 `exit=0`，`WINDOW3-SUMMARY pre=0 a1=0 tm1=0 tt1=1 r1=0 a2=0 tm2=0 tt2=1 r2=0 post=0 all=0`。
- `85d879e7`（窗口四：重编＋靶向＋全量复绿）→ `exit=0`，`WINDOW4-SUMMARY tsc=0 t415=0 all=0`（tests 245 / pass 245 / fail 0）。
- 本件提交 c3 的两行（START／RUN）在锁日志中，提交后可查。

提交（逐小步持锁，已落两笔）：`822710d 功能(415)：viewRecordDetail 专属版式＋含删行可见＋字段表十行`／`f41e22e 测试(415)：t415-record-detail 整页＋软删三态＋红线＋路由回归`（各 `git diff --cached --name-only` 只含自己路径，见各窗口日志）。

## 七 交棒（下一手要什么）

- 读盘入口：`packages/skill-bill/src/query/detail.ts`（`queryDetailDoc／DETAIL_COLUMNS／detailCards／detailRows`，127 LF）＋`packages/skill-bill/src/query/read.ts`（`viewRecordDetail` 含删行支，313 LF）；判据 `packages/skill-bill/test/t415-record-detail.test.mjs`；变异工具 `.scratch/t415/mutate.mjs`。
- 下一票（#416 接线）读的是 `WAKE_TABLE` 与 `SKILL.md`，别动 `commands.ts` 形状（形状是契约）与本票含删行（`includeDeleted` 是详情唯一判地）。
- 沿革债（非本票引入，不扩大）：`src/policy/record.ts:64-68`「查询域与三个分析命令共用」注释现已半旧（查询侧周月窗已搬出），随分析侧对齐那张后票一起改（t413 第七节同记）；`src/cli/cmd_read.ts 485` 与 `wake-assets.ts 986` 超线挂号未动。
