PASS
# t410 独立复跑（非作者复核，不采信结论）

## 一、结论与口径
判 PASS。被审：`docs/skills/skill-bill/t410-锁-证据.md`（69 行，三验收读数＋GATE-RUN 四行）与 `packages/skill-bill/test/t410-lock.test.mjs`（182 行，13 录入＋3 修正＋2 降级＝18 条）。被审提交 `80e7cbd`（测试）/`7bc0872`（文档），复核时均在主干祖先链上（`merge-base --is-ancestor` 均为 exit 0；复核中主干又进两笔 565 提交，HEAD 至 `23a2b29`，不影响祖先关系）。口径：结论全部自己重跑；红线不改源码，临时变异逐字节还原；编译／测试／探针／git 写一律 `node tooling/run-locked.mjs --ticket 410 -- …` 排队。

## 二、锁与全量重跑读数
编译 `tsc -b packages/skill-bill` 持锁一次绿（runId `1b02b4f3` waitedMs 30028 exit 0，被 565B 活锁挡 30s 后拿到锁）。锁测试重跑 18/18 绿（runId `4735bfc1` exit 0；复核后还原再跑 `3b7fd78b` 仍 18/0）。全量 `node --test packages/skill-bill/test/*.test.mjs` 重跑 178/178 绿（runId `d2009f41` exit 0，19 个测试文件），与证据“基线 160→本轮 178（＋18 无跌）”一致：本轮全量含 t410 的 18 条，无失败新增。证据 GATE-RUN 四行与 `.scratch/locks/gate-runs.log` 一对一（runId 短号 `408a6860`/`8d397767`/`2e1743a3`/`c30d27ea`），复核另起八行 ticket=410 留痕（见第三节）。

## 三、自设探针与受控变异
新探针（t410 只钉成功分支，缺槽位分支无锁）：`bill.record.add` 缺 category／缺 amount／amount=0 视同没给、`bill.record.update` 缺 id，四案均断言 exit 0＋`ok:false`＋message 含“缺必需槽位/已出采集页”＋落盘为 collect 页（`data-page="collect"`、`data-slot="ilife:bill:collect"`、“补齐槽位”、“这一页先不写库”）＋库行数不变（铺底 1 笔，终态仍 1）。首跑 `a682aa47` exit 1 系探针脚本自误（仓库根多退一级致 bin 路径错＋`SKILLS_DB_PATH` 误传文件而非目录），修正后 `eaaa74e4` exit 0，4/4 绿。受控变异：photo 降级串“准”改“坏”，锁测试红 pass16/fail2（runId `7e90837f` exit 1，两处失败恰为拍账单两条，与证据一致），还原后绿 18/0。变异经由 dist 单串落地：src 变异后整包重编被他席未入库新件 `src/query/list.ts` 类型错阻塞（runId `01f8d2c6` exit 1，非产品故障），故 src 当即还原、dist 单串改回，两处 photo 好串各 1、坏串 0。

## 四、还原与提交
源 `src/record/write.ts` 临时变异已还原（复核前后 photo 好串 1、坏串 0；终态 `git diff` 该件仅剩他席在途的 actionStamp 改动，无 photo 行，本席未碰）；dist（忽略件）单串改回好串；探针脚本 `.scratch/t410-probe-missing.mjs` 试完已删。提交前 `git diff --cached --name-only` 仅本件一件，持锁提交本件。
