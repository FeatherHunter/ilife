# t731 证据 · setup（开始使用）域端到端搬迁（6 场景／3 片页型）

- 票：[setup 域：端到端搬迁（6 场景／3 片）](https://github.com/FeatherHunter/ilife/issues/731)（父图 [#681](https://github.com/FeatherHunter/ilife/issues/681)）
- 差异表（本票产物之一）：`docs/skills/skill-bill/t731-差异表.md`（46 行差异 ＋ 8 条留白）
- 真出口探针（可复跑）：`docs/skills/skill-bill/t731-探针-真出口.mjs`（19 条）
- 靶向测试（可复跑）：`packages/skill-bill/test/t731-setup-域.test.mjs`（33 条）
- 老侧勘察摘要（只读取证，入 `.scratch/`，不入库）：`.scratch/t731/old-setup.md`（690 行；本件与差异表引老侧 `文件:行号` 一律照它）

**写集**（本票声明的路径，逐条对得上）：

`packages/skill-bill/src/setup/`（新建 16 件：`commands`／`index`／`params`／`backups`／`status`／`importer`／`steps`／`pageParts`／`blocked`／`template-{wizard,receipt,list}`／`scene`／`scene-{init,init-status,backup-create,backup-list,restore,import}`／`run`／`restore`／`import`）＋
`src/fetch/db.ts`（`BillDb.repaired`：把 DDL 自愈补了什么报出来）＋
`src/shared/params.ts`／`src/account/params.ts`（`isGiven`／`textOf`／`numberOf` 三件从账户域上浮共用位，本域是第二个用法）＋
`src/cli/cmd_read.ts`（删本域一个 `case` 与已不用的 import）＋
`src/render/envelope.ts`（过渡表删一行）＋
`src/shared/category.ts`（「谁在用」那一行跟着落点改）＋
`scripts/gen-cli.mjs`（生成物件头的条数读数）＋
`scripts/ratchet-frozen-686.mjs`（棘轮冻结值同窗下调）＋
`test/{t731-setup-域.test.mjs 新建, record-write, t721-域声明, t689-scene-shape}`（两处硬编码 ＋ 新建靶向测试）＋
`packages/skill-bill/AGENTS.md`（台阶账 `--sync`）＋
`docs/skills/skill-bill/{t731-差异表.md, t731-证据.md, t731-探针-真出口.mjs, t731-门禁运行记录.md}`。

**一个字节都没动**：公共层（`packages/base-render/**`）／其他域的源码与处理体／域声明（`src/*/declaration.ts`）／插件包。

## 复跑（每条读数都可复算；按仓规一律经加锁包装器）

```text
node tooling/run-locked.mjs --ticket 731 --child-timeout-ms 1800000 -- node .scratch/t731/gate-window.mjs   # 一个窗口跑完：编译→生成→全部门禁→本票两件
node --test packages/skill-bill/test/t731-setup-域.test.mjs        # 本域靶向测试
node docs/skills/skill-bill/t731-探针-真出口.mjs                    # 真出口探针（19 条）
node packages/skill-bill/scripts/gen-cli.mjs --check               # 生成物门
node packages/skill-bill/scripts/check-ratchet-tight.mjs           # 棘轮收紧守卫
node packages/skill-bill/scripts/check-scene-shape.mjs             # 场景形状门
node packages/skill-bill/scripts/check-warning-line.mjs            # 告警线门
```

窗口驱动的两段（写阶段／读阶段）与「读阶段前后各算一次 `src`／`dist` 指纹」写在 `.scratch/t731/gate-window.mjs` 的件头；
本票声明过的运行条目导出在 `docs/skills/skill-bill/t731-门禁运行记录.md`（协议 §2.3 第 4 条的对账源）。

## 一 判据逐条

### 判据 ① 5 条唤醒词／6 个场景逐条真跑（exit 0、产物落盘、整页、字段正确，含「备份」一词两场景各自出对页）

真出口探针（spawn `dist/cli/cmd_read.js`，`--html` 读回盘上产物）实测 **19/19 通过**，六条场景逐条：`exit 0`、`delivery.path` 是绝对路径、`文件字节 = delivery.bytes`、整页（`<!doctype html>` ＋ `<section>`）。
「备份」一词两场景各出对页那一条**单列一条断言**：`op=backup-create` 出结果回执页（含「备份成功」），`op=backup-list` 出记录列表页（含「查看备份」，且**不含**「备份成功」）。

| # | 唤醒词 | op | 页型 | 读数（探针内逐条） |
|---|---|---|---|---|
| 1 | 初始化 | `init` | 向导 | `ok:true`；四步全 done（含「共 4 步」与「第 4 步」）；完成卡＋记第一笔口令；库落盘；幂等复跑照样 `ok:true` |
| 2 | 初始化状态 | `init-status` | 记录列表／状态 | `receipt.version='v2.0'`；三重判定三项齐；页内导航；「现有记录 0 条」（零记录不吞） |
| 3 | 备份 | `backup-create` | 结果回执 | `receipt.backup` 的文件真在备份目录里、字节数与回执一致；结论句；页内导航 |
| 4 | 备份 | `backup-list` | 记录列表 | `receipt.count≥1`；列表写出文件名；不串回执页内容 |
| 5 | 恢复备份 | `restore` | 向导 | `receipt.selected` 点名选中的那一份；覆盖警告三句齐；步骤条「共 4 步／第 3 步」 |
| 6 | 导入 | `import` | 向导 | `receipt.new_rows=2`／`duplicates=0`；「本次将新增 2 行」「不覆盖」「自动备份一次」「前 2 行」都在 |

### 判据 ② 恢复三件套（`#688` §二 D1 的可判形式）

| 件 | 可判形式 | 读数 |
|---|---|---|
| ① 恢复前**无条件**自动备份现状 | 恢复那一趟之后备份目录份数 +1，且页面点名那一份 | 探针：`n0 → n0+1`，页含「现状已备份：<文件名>」 |
| ② 显式覆盖警告 | 产物含「恢复将覆盖当前数据」 | 探针：命中 |
| ③ 警告里写「怎么回去」 | 产物含「恢复前会自动备份现状」＋「回到现在的样子」 | 探针：两句都命中；恢复完成态里「把现状备份的名字」写进口令与提示 |
| ④ 恢复后验证库可读，失败点名现状备份 | `verifyRestored` 真开一条连接读 `SELECT 1 FROM bills LIMIT 1` | 探针：恢复后 `records()` 回到导入前的 0 条（库确实换回来了）；失败那一路（源备份读不动）**先验后动**，页上点名，且**没有动现在的数据** |

**一处与老侧不同**（差异表 §二）：老侧是「先覆盖、后校验」（`backup.py:128-131` 在 `:139-142` 之前）；新侧把**源侧校验挪到覆盖之前**。

### 判据 ③ 导入四件（`#688` §二 D2）

| 件 | 可判形式 | 读数 |
|---|---|---|
| ① 明示「本次将新增 N 行、不覆盖已有记录」 | 产物含「本次将新增 2 行」「不覆盖」 | 探针：命中；`receipt.new_rows=2` |
| ② 导入前提示或自动备份一次 | 确认那一趟之后备份目录多一份、且载荷点名 | 探针：`receipt.backup` 在备份目录里（导入三笔那一趟同样成立） |
| ③ 重复导入检测 | 同一份 CSV 再导一次：`new_rows=0`、`duplicates=2` | 探针：两条读数都对上；页含「库里已经有了」 |
| ④ 页面级结果卡 | 产物含「导入完成：成功 N 行」，失败行逐条列原因 | 探针：好行 1＋坏行 2 的那份 CSV 得到 `imported=1`／`bad=2`，页含「导入完成：成功 1 行」与「读不出来」 |

### 判据 ④ 选中项与详情同源（`#688` §二 D5）

探针两条断言：切换备份跑两次（`name=A` 与 `name=B`），产物里「备份名称」后第一个文件名与复制口令里那一份**逐字相同**，且两次不同（`A=biscuit_…_2.db`／`B=biscuit_….db`）。
结构上只有一处真源：`src/setup/template-wizard.ts` 的 `RestoreWizardInput.selected`（详情卡、步骤说明、口令三处都从它派生）。

### 判据 ⑤ 步骤条按状态算（D6／D7）

- 编号与总数**都由数组算**（`src/setup/steps.ts` 的 `numbered()`，`共 N 步` 由 `steps.length` 派生），三支向导各一条探针断言（`共 4 步`／`第 4 步`／`第 3 步`）。
- 假绿那一路（D7）：`envChecksOf([])` 只出一句「没测到就是没测到，不当成过」，**不含**「全部通过」四字；靶向测试单列一条断言把这两种情形分开。
- 靶向测试另钉：`initSteps` 四项事实全过⇒全 done、一项都没检测到⇒第 1 步 `blocked`；`restoreSteps` 确认前停在第 2 步、确认＋现状备份＋验库逐格推进；`importSteps` 三档读数都进说明。

### 判据 ⑥ 与 T6 逐条对账（`t687-parity-审计清单.md`）

| T6 的项 | 老侧 | 新仓落点 | 判定 |
|---|---|---|---|
| §2 第 66 行「初始化」 | 1 场景 | `src/setup/scene-init.ts`（`sceneId=setup_init_wizard`）＋ `bill.setup.run` op=init | **对上了**：场景件一件、路由回同键（探针路由层 5/5） |
| §2 第 67 行「初始化状态」 | 1 场景 | `scene-init-status.ts`（`setup_init_status`）→ 记录列表／状态页 | 对上了 |
| §2 第 68 行「备份」 | **2 场景** | `scene-backup-create.ts` ＋ `scene-backup-list.ts`（同一词两支，各出一片页型） | 对上了（探针单列一条「一词两场景各出对页」） |
| §2 第 69 行「恢复备份」 | 1 场景 | `scene-restore.ts`（`setup_restore`）→ 向导页 | 对上了 |
| §2 第 70 行「导入」 | 1 场景 | `scene-import.ts`（`setup_import`）→ 向导页 | 对上了 |
| §3 第 15 行 `bill.setup.run`「可用（弱）· 域页缺口；HTML 1,091 B／16 行空壳页；无专测」 | 旧读数 | 当刻：整页产物（探针逐条给 `delivery.bytes`）＋ 靶向测试 33 条＋ 探针 19 条 | **对上了（缺口补掉）**：空壳那 1,091 B／16 行由本票的三片页型替代 |
| §4 第 16 行 `开始使用/init_wizard.html` | 无落点 | 向导页型（`template-wizard.ts` 的 init 那一支） | 对上（补） |
| §4 第 17 行 `开始使用/init_status.html` | 无落点 | 记录列表／状态页型（`template-list.ts` 的 status 那一支）＋ 迁移块独立成块（C11） | 对上（补） |
| §4 第 18 行 `开始使用/backup.html` | 无落点 | 结果回执页型（`template-receipt.ts`） | 对上（补） |
| §4 第 19 行 `开始使用/backup_list.html` | 无落点 | 记录列表页型（`template-list.ts` 的 list 那一支）＋ 空态引导 | 对上（补） |
| §4 第 20 行 `开始使用/restore.html` | 无落点 | 向导页型（`template-wizard.ts` 的 restore 那一支）＋ D1 三件套 ＋ D5 同源 | 对上（补） |
| §4 第 21 行 `开始使用/import.html` | 无落点 | 向导页型（`template-wizard.ts` 的 import 那一支）＋ D2 四件 | 对上（补） |

**一处读数口径说明**：T6 那些「第 66–70 行」「第 16–21 行」是**表内行号**（票面同样这么引），在文件里分别落在 `:123-127` 与 `:215-220`；本域件头与本节照票面引表内行号。

### 判据 ⑦ 与老侧逐条对照出一张差异表

`docs/skills/skill-bill/t731-差异表.md`：六个主题节共 **46 行差异**（四列：老行为（出处）／新行为（落点）／为什么改／谁批的）＋ **8 条留白**。表里另记了**三处任务书与实测不合**的订正（老侧「先关连接再拷」无出处、老侧同一秒冲突其实是给目录名加 `_2`／`_3` 而非静默覆盖、新侧「失败非零退出码」只对坏参数成立）。

### 判据 ⑧ 路径从零走完（域声明 → 命令 → 页面 → 测试）

| 走过的段 | 落点 | 当刻状态 |
|---|---|---|
| 域声明 | `src/setup/declaration.ts`（#721 已落，5 条词／6 场景，本票一字未动） | 词表／HELP／路由都由它派生；探针路由层 5/5 |
| 命令 | `src/setup/commands.ts`（一条）→ `src/setup/index.ts`（域门）→ `pnpm gen` 重生成 `src/cli/registry.ts` | 生成物门 `GEN-CHECK PASS：命令 14 条；能力 6 个（account／analysis／goal／query／setup／write）` |
| 分派 | `src/cli/cmd_read.ts` 删掉本域那一个 `case`（六种 op 那一段 57 行） | 分派层 LF 455 → 280（冻结值同窗下调）；棘轮 `RESULT: 9/9` |
| 页面 | 三片页型 ＋ 六件场景件 ＋ 域内共件（`pageParts`／`steps`／`blocked`） | 场景形状门 `RESULT: 54/54`（扫描面＝全包 `src/*/scene-*.ts`） |
| 测试 | `test/t731-setup-域.test.mjs`（33 条）＋ 真出口探针（19 条） | 两条都绿（见下面「当刻门禁读数」） |

**本域第一次落「多步向导」这条形状，走完留下的口径注释**：步骤状态怎么表达写在 `src/setup/pageParts.ts` 件头——公共层没有步骤条这一件，故本域按「共用件从第二个用法里长出来」先落**域内两枚**：徽章列（`renderChips`）承载**进度**（`共 N 步` ＋ 每步 `第 N 步 · 步骤名 · 状态`），小表（`renderDataTable`）承载**每一步的现状说明**；两枚都只把 `steps.ts` 算好的 `WizardStep[]` 摊开，**不含任何算式**。将来第二个域也要步骤条时再立公共层票。

### 判据 ⑨ 接线五件同窗办

| 件 | 落点 | 读数 |
|---|---|---|
| 删 `cmd_read.ts` 里本域 `case` | 六种 op 那一段连同 `fetch/paths` 备份两件与 fs 三件 import 一并撤 | 该件 `git status` 只剩同窗其他域也动过的行；分派层键集实测 `['bill.help.lookup','bill.link.submit']` |
| 过渡表删一行 | `src/render/envelope.ts` 的 `TRANSITIONAL_KEY_SHAPES` 去掉 `bill.setup.run` | 实测过渡表键集同上（两处同集） |
| `pnpm gen` 重生成注册表 | `src/cli/registry.ts` | `GEN-CHECK PASS：命令 14 条；能力 6 个` |
| 棘轮冻结值同窗下调 | `scripts/ratchet-frozen-686.mjs`：`legacyKeys`／`dispatchKeys` 各减到 2 条、`registryKeyCount` 8 → 14、`lineCaps` 照实测（`cmd_read.ts` 280／`envelope.ts` 56） | `check-ratchet-tight` `RESULT: 9/9` |
| 台账 `--sync` | `packages/skill-bill/AGENTS.md`（`cmd_read.ts` 当场实测 455 → 280） | 告警线门 `RESULT: 13/13` |
| 两处测试硬编码 | `test/record-write.test.mjs`（注册表键集 8 → 14，并补上新键名）／`test/t721-域声明.test.mjs`（8 → 14）／`test/t689-scene-shape.test.mjs`（19/19 → 54/54，照当刻扫描面） | 全量测试里这三件都过 |

### 判据 ⑩ 门禁

当刻读数见下节；**全量测试那一条当刻不绿，红的不是本票**（见下节）。

## 二 当刻门禁读数（读数窗口绑定）

本票的读数在一个**加锁窗口**里取（协议 §2：外层包装器持锁、窗内一律直调）。窗口驱动分写阶段与读阶段，读阶段前后各算一次 `src`／`dist` 指纹：

- **写阶段**：`t540 指纹绑定编译`（`RESULT: src-n=194 dist-n=793 … 一致`）→ 重打生成器内容印记 → 重生成注册表 → **再编一次**（`registry.ts` 是住在 `src/` 里的生成物；只生成不重编，下一段读到的全是旧注册表——本驱动第一版踩过这个坑）→ 重生成 SKILL.md 命令块 → 台账同步。
- **读阶段**：生成物门 → 全量测试 → 棘轮收紧守卫 → 场景形状门 → 告警线门 → 本票靶向测试 → 真出口探针。

| 步 | 读数 |
|---|---|
| 生成物门 | `GEN-CHECK PASS：命令 14 条；能力 6 个（account／analysis／goal／query／setup／write）` |
| 棘轮收紧守卫 | `RESULT: 9/9` |
| 场景形状门 | `RESULT: 54/54` |
| 告警线门 | `RESULT: 13/13` |
| 本票靶向测试 | **绿**（33 条；`node --test packages/skill-bill/test/t731-setup-域.test.mjs`） |
| 真出口探针 | `RESULT: 19/19` |
| 全量测试 | **12/13 件绿**；那 1 件红的是**其他席在途**的件（`test/t729-分析域.test.mjs` 自身 `ReferenceError: billEnv is not defined`／`test/t721-判据与摘要锁.test.mjs` 命中的是 `src/analysis/scene-reimburse.ts:47` 的唤醒词字面量），**与本票写集无关**；本票自己的三件（靶向测试／探针／本域源码面）都绿 |

**读数窗口口径**：窗口里出现过别的席位写 `src/` 的情形（`READING-WINDOW 漂移`），那几轮读数按协议 §2.6 **一律作废、不计入本件**；计入本件的是**指纹一致**的那一轮。

## 三 老 6 张模板 → 新落点（信息等价的逐张对照）

老侧块的逐块清单照 `.scratch/t731/old-setup.md` §1.3／§2.3／§3.3／§4.3／§5.3／§6.3；新落点照当刻产物。

| 老模板 | 老侧信息块（处） | 新落点 | 判定 |
|---|---|---|---|
| `开始使用/init_wizard.html`（初始化，向导） | chips＋完成卡＋步骤列表 4 条＋环境检测格＋底部按钮与复制 prompt（4 处数据块） | 徽章列（唤醒词＋`4 步零决策`＋状态）＋结论句＋步骤条两件（共 4 步／逐步现状）＋环境检测表＋复制口令＋复制区＋来源脚注 | **对上了**（4/4；环境检测项由老侧 6 项落成当刻真有事实的 2 项，逐条记在差异表 §三） |
| `开始使用/init_status.html`（初始化状态，查看） | 英雄区＋检查清单 3 项＋迁移块＋引导按钮（4 处） | 读数卡三格（数据存在／结构版本／就绪）＋检查小表＋迁移块（独立成块）＋未就绪引导口令＋复制区＋来源脚注 | **对上了**（4/4；迁移块改报「本次自动补了什么」，见差异表 §三） |
| `开始使用/backup.html`（一键备份，回执） | 成功卡＋备份路径＋文件清单（3 处） | 读数四格（备份文件／时间／大小／内容）＋明细表（路径等六行）＋结论句＋复制区＋来源脚注 | **对上了**（3/3；「内容」改照实报，见差异表 §一第 5 行） |
| `开始使用/backup_list.html`（查看备份，查看） | 空态（`emptyState`：文案＋引导）／列表卡（名称／时间／大小／文件 chips）＋再备份一次（3 处） | 读数三格（份数／最新一份／合计大小）＋备份清单表（文件／时间／大小／内容）＋空态块＋引导口令＋复制区＋来源脚注 | **对上了**（3/3；老侧「目录」那一层随备份形状改成文件，见差异表 §一） |
| `开始使用/restore.html`（从备份恢复，向导） | 三步列表＋覆盖警告＋备份选择器＋详情卡四行＋确认按钮与复制 prompt（5 处） | 步骤条两件（共 4 步）＋覆盖警告（含「怎么回去」）＋备份清单表（选中行带标记）＋详情卡四行（与口令同源）＋复制口令＋复制区＋来源脚注 | **对上了**（5/5；D5 同源与 D6 步骤号那两条缺陷当场修掉） |
| `开始使用/import.html`（导入CSV，向导） | 文件卡＋列映射卡 7 格＋预览卡＋确认按钮与复制 prompt（4 处） | 步骤条两件（共 4 步）＋文件卡（一行口径）＋列映射表（字段／认到第几列／首行样例）＋预览表（前 8 行）＋**四件**（新增行数／自动备份／重复检测／结果卡）＋复制口令＋复制区＋来源脚注 | **对上了**（4/4 ＋ 四件全补；老侧「收支方向」那一列随金额符号定方向而撤，见差异表 §四） |

## 四 留白（如实记）

1. **页指纹账本（判据甲的账本）仍只覆盖写入域 32 页**，开始使用域的页不在其中——与 #691 记的同一条留白；扩账本属横向决定，留给下一张页面票。
2. **全量测试那一件红不是本票的**（本文件第二节末行点名），等两席在途落地后应复跑一次；本件不作「全量绿」的声明。
3. **差异表的新侧行号锚点是勘察当刻（19:09–19:26）的读数**；那之后本域又动过三件（`run.ts` 拆出 `restore.ts`／`import.ts`／`blocked.ts`、`params.ts` 撤掉未使用的 `OP_LABEL`），故**以每格的引文片段为准**，行号只作定位（差异表 §六 留白 7 已记同一句）。
4. **老侧 `references/` 与 `tests/` 那两摊没进本票**：老侧 25 个测试件怎么当判据仍是 #681 的雾区（不属本票）。
5. **标点口径（页内全角 vs 老侧半角）**：差异表记了一行但「谁批的」写**未批**——`docs/agents/wording.md` 里查不到标点明文，本票按现仓既有页面（写入域／查询域／账户域）的全角口径写。
