# t407 复核 · 三轴整改验证

验证对象：提交 `db56b63` ＋ `8a18f8a`（自称 10 条改动全部完成、`node --test` 78 通过／0 失败）。
验证方式：只读源码与实测，不采信整改件自述。两提交都是当前 `HEAD`（`fd5c922`）的祖先；
`git status --porcelain -- packages/skill-bill docs/skills/skill-bill` 显示本包源码零改动（只有未入库的新件），
故下面读到的就是入库那一版。整改件＝`docs\skills\skill-bill\t407-整改-三轴复核项.md`（65 行，整读）。

## 一、10 条逐条核验

| 条目 | 整改方自称 | 我核到的 | 三态 |
|---|---|---|---|
| C1 口径单一定义地 | 四处口径各自收到一处定义地（缺省值／符号即方向／时间缺省／两位小数） | ① 缺省值：`prefillNote.ts:20／34–36` 确已改引 `policy/category.ts:11` 的 `DEFAULTS`（原来那份账本「生活」／币种「人民币」已删）；但包内另有 4 处硬写同一对值：`fetch/db.ts:28／29`（DDL 默认）、`fetch/db.ts:75／76`（取值回落）、`cli/cmd_read.ts:465`（导入兜底 `cells[4] \|\| '生活'` ＋ `currency: '人民币'`）→「只剩一处」按包内实读不成立。② 符号即方向：定义地确是一处（`summaryRow.ts:34` 的 `DIRECTION` 表 ＋ `:40 directionOf` ＋ `:51 moneyDirection`）；`directionOf` 有 2 处外部调用（`blockedSlots.ts:54`、`typeBadge.ts:53`），而 `moneyDirection` 全包只被 `summaryRow.ts:72` 引一次、**就在本件内**，外部调用方仍为 0 且仍挂着 `export`（原缺陷未动）。③ 时间缺省：真源 `policy/category.ts:15 DEFAULT_TIME_SUFFIX` ＋ `:18 defaultTimeOn`，取值点 `category.ts:76／111`、`prefillNote.ts:66／67`、`summaryRow.ts:80` 都引它；只剩 `collect.ts:55` 的表单提示字面量没并（整改件已自报并给理由）。④ 两位小数：`money2` 定义 `summaryRow.ts:45`，原来的三处（摘要行／重复检测／阻断条）已全走它；包内另有 5 处 `toFixed(2)`（`write.ts:69／170`、`cmd_read.ts:318／326／384`，整改件已自报为各说各的账目文本） | 部分成立 |
| C2 `errorReceipt` | 改名 `errorReceipt.ts`、收回对外导出、件头写「整页替换归后票」的债务 | 件名已是 `src/shared/errorReceipt.ts`；`failureReceipt` 在包内 0 命中、`dist/shared/` 也无旧件；外部 import 只有同目录 `blockedSlots.ts:27`（共用位内部件），`src/index.ts`／`render/index.ts` 都没再导出它——对外导出确实收回了；件头 `:13–16` 写明整页替换这条债归后票。附一处措辞自相矛盾：件头 `:6` 写「`errorReceipt` 不 export」，而 `:39` 正是 `export function errorReceipt`（本意应是「不出本共用位」） | 成立 |
| C3 第二消费者 ＋ 虚报改正 | 四件头补真实调用方；证据件那句虚报改成实话 | 四件头逐条对源码：`blockedSlots.ts:3–7` 写 `collect.ts`（`:190 blockedItems`／`:231 blockedBar`）＋ `write.ts`（`:160`／`:182` 当闸门）真；`duplicateNote.ts:3–8` 写 `collect.ts:229` ＋ `receipt.ts:91` 真；`prefillNote.ts:3–9` 写只 `collect.ts:230` 真；`emptyNote.ts:3–8` 写只 `collect.ts` 真（但行号写 `:211`，实调 `:214`）。证据件 `:29–33` 已把虚报改成实话（四件当时没写 `src/query` 那条）——但该句件数对不上：称「七件里只有 `pageShell`（原 `pageFrame`）／`typeBadge`／`summaryRow`／`errorReceipt` 四件写了」，实读 `a1fc236` 那一版只有 3 件写了（`pageFrame`／`summaryRow`／`failureReceipt`），`typeBadge` 是本次拆件才有的件头（4＋4 大于 7）。同类残留还有一处：`summaryRow.ts:3–7` 写「真引用本件的是上面两个调用点，再无第三处」，实读有 5 个件 import 本件（`collect.ts:35`／`receipt.ts:27`／`blockedSlots.ts:26`／`typeBadge.ts:16`／`duplicateNote.ts:22`），证据件 `:25–26` 同样只记两处 | 部分成立 |
| C4 用词 | 源码那处 5 处全删、证据件 5 处逐处改写 | 本次改动文件（`db56b63` 的 20 件）逐行扫禁用表：改动过的源码 14 件 **0 命中**（「键」作命令讲也是 0 命中）；还剩 8 行命中——证据件新增行 3 处（`:225` 的 `落地`（作实施讲）＋ `:353`／`:354` 用词改正表左列印出禁用原词当引文）、既有行 5 处（两页产物里嵌入的 base 注文 4 处 `壳`、`test/record-write.test.mjs:85` 断言名里的 `渲染层`；这 5 行都不是本次新增行） | 部分成立 |
| C5 命名与拆件 | 拆成 `pageShell.ts` ＋ `typeBadge.ts`；「错误回执」统一 | 拆件真：`pageShell.ts` 60 行 ＋ `typeBadge.ts` 62 行已建，`pageFrame.ts` 已删（`git show --stat db56b63` 记 −95 行），两页各两处 import 同步（`collect.ts:32／37`、`receipt.ts:25／29`）；`pageFrame` 包内 0 命中。「错误回执」8 处照此写，但「失败回执」仍有 1 处 `src/output.ts:20`（「exit 5 ＋ 结构化失败回执」，该件不在本次改动清单里）——自称「本包 grep：旧名 0 命中」不实 | 部分成立 |
| C6 方向判定收窄 | 只服务录入路径；`update` 那支不传方向 | 源码对：`write.ts:160`（`bill.record.add`）传 `kind` 判方向，`write.ts:182`（`bill.record.update`）传 `kind: ''` 不判；`blockedSlots.ts:19–21` 件头与 `:51` 注释写明「只服务录入路径」。实测（自建库）：`bill.record.update --params '{"op":"undo","id":1,"kind":"expense","amount":35}'` 退出码 0、`data.ok=true`、「已撤销：1（软删，恢复走 restore）」；`restore` 同（退出码 0、`data.ok=true`、「已恢复：1」）——带方向不符的 `amount:35` 不再被拦 | 成立 |
| C7 证据件补交代 | 补「改前后果＋影响面」两句 | 证据件 `:68–74` 有两句：「改前照写会留一笔方向相反的金额（记支出写成收入）…`validateAmount` 只校数字不校方向」＋「影响面＝判定挂在 `write.ts:158`／`:183`，收窄后只管 `bill.record.add` 录入路径」。句在，与源码一致（`:158`／`:183` 指那一带的语句，实际调用在 `:160`／`:182`，差 2 行以内） | 成立 |
| C8 `data-page` | `data-shape` 不动；另加两枚 `data-page` | 源码 `writeParts.ts:75` 同一段里同时输出 `data-page` 与 `data-shape`（`:63–64` 写明两者分工）。两份入库产物：采集页 `data-page="collect"` ×1 ＋ `data-shape="receipt"` ×1；回执页 `data-page="receipt"` ×1 ＋ `data-shape="receipt"` ×1（各整页只有一枚 `data-page`）。我自建库真跑的两份产物同读数 | 成立 |
| C9 重复检测口径 | 分类未给＝不出条 | 判定处 `duplicateNote.ts:48–54` 三条门槛（金额可解析／分类非空／日期成立）＋ `:60–61` 同日同额同分类。两次真跑对比：分类未给（同日同额）→ 采集页 `重复检测提示条` 命中 **0**；分类已给同日同额 → 回执页命中 **1** | 成立 |
| C10 机器读数 | 证据件 §3.2 补可复跑读数 | 证据件 `:162–190` 有读数表：`<!DOCTYPE html>` 命中、单块 `<style>` 34219 字符与首行、规则 313／变量 13／`var(--…)` 176／`@media` 13、内联脚本 21908 字符、外部请求 0／0、起标记与类名 token 计数；另有无头渲染页高 1930 px／1497 px；读数文件 `.scratch\t407-fix\out\readings.json` 在 | 成立 |

**计数**：成立 6（C2／C6／C7／C8／C9／C10），部分成立 4（C1／C3／C4／C5），不成立 0。

## 二、端到端复跑读数

全部用**自建临时库**（`SKILLS_DB_PATH=D:\ilife\.scratch\t407-recheck\db`、`BILL_FORCE_PROD=1`），
命令输出落 `.scratch\t407-recheck\`，只读末尾 25 行。

| 判据 | 命令 | 读数 |
|---|---|---|
| 类型检查（禁增量假绿） | `npx tsc -b packages/skill-bill --force` | **退出码 0，零诊断**（`.scratch\t407-recheck\tsc.txt` 只有 `EXIT=0`） |
| 测试 | `node --test "packages/skill-bill/test/*.test.mjs"` | **tests 78／suites 15／pass 78／fail 0**，**退出码 0**（`.scratch\t407-recheck\test.txt`） |

真跑（`.scratch\t407-recheck\recheck-run.mjs`，读数 `.scratch\t407-recheck\out\readings.json`／`readings.txt`）：

| # | 命令 | 退出码 | `data.ok` | 当日行数（前→后） | 产物字节 | 关键标记 |
|---|---|---|---|---|---|---|
| 种子 | `bill.record.add --params '{"category":"餐饮/外卖/午餐","amount":-12.5,"time":"2026-09-14 12:00:00","account":"支付宝","ledger":"生活","note":"午饭"}'` | 0 | true | 0 → 1 | — | 记录编号 1 |
| ① | `bill.record.add --params '{"kind":"expense","amount":-12.5,"time":"2026-09-14 12:30:00"}' --html .scratch\t407-recheck\out\collect.html` | **0** | **false** | **1 → 1** | **68323** | `data-page="collect"` ×1／`data-shape="receipt"` ×1／缺项阻断条 1／重复检测提示条 **0**／复制数据 2＋复制日志 1／`<!DOCTYPE html>` 有 |
| ② | `bill.record.add --params '{"category":"餐饮/外卖/午餐","amount":-12.5,"time":"2026-09-14 12:40:00","account":"支付宝","ledger":"生活","note":"午饭","kind":"expense"}' --html .scratch\t407-recheck\out\receipt.html` | **0** | **true** | **1 → 2** | **67289** | `data-page="receipt"` ×1／`data-shape="receipt"` ×1／缺项阻断条 0／重复检测提示条 **1**／复制数据 4＋复制日志 2 |
| ③ | `bill.record.update --params '{"op":"undo","id":1,"kind":"expense","amount":35}' --html .scratch\t407-recheck\out\undo.html` | **0** | **true** | 2 → 1 | 66149 | `data-page="receipt"` ×1／缺项阻断条 0 |
| ③附 | 同一条换 `"op":"restore"` --html …\restore.html | 0 | true | 1 → 2 | 66012 | `data-page="receipt"` ×1 |

三条真跑产物量级与整改件自报的 68323／67289 逐字节相同；两枚 `data-page` 与 `data-shape` 都在，
复制区（复制数据／复制日志）与阻断条标记都按页出现。

**`dist` 残留风险核验（整改方自报的那条）**：`dist\shared\` 下 `pageShell.js`／`errorReceipt.js`／`typeBadge.js`
各 4 件（`.js`／`.js.map`／`.d.ts`／`.d.ts.map`），`pageFrame`／`failureReceipt` 文件名命中 **0**；
`dist\shared\writeParts.js` 里 `data-page` 命中 2 处；本轮 `tsc -b --force` 全量重建后，
CLI 真跑产物里出现的是新件的标记（`data-page`、`⛔ 先补齐`、眉标 `记账 · 写入域`）。
**结论：残留风险不成立**——旧件已不在，缓存也没有把它带回来；那个自称「另有缓存入口未查」的顾虑，实测落空。

## 三、不成立或部分成立的条目

### C1（部分成立）

- **差了哪一点**：① 缺省值仍有 4 处硬写——`fetch/db.ts:28／29`（DDL `DEFAULT '生活'`／`'人民币'`，改口径层不会带着它改）、
  `fetch/db.ts:75／76`（取值回落）、`cli/cmd_read.ts:465`（导入兜底）；② `moneyDirection` 全包只有 `summaryRow.ts:72`
  一处引用、就在本件内，外部调用方仍是 0，`export` 依旧挂着（A 轴原话点的就是这个）；④ 两位小数另有 5 处
  `toFixed(2)`（`write.ts:69／170`、`cmd_read.ts:318／326／384`）。
- **最小修法**：① DDL 与取值回落改引 `DEFAULTS.ledger`／`DEFAULTS.currency`（DDL 那一处若不便引，就在行上写明
  「与 `DEFAULTS` 同值，改一处要改两处」），`cmd_read.ts:465` 的兜底同样引 `DEFAULTS`；② 去掉 `moneyDirection` 的
  `export`（它只在同一件里用），或把 `summaryRow.ts:72` 的方向格换成 `directionOf` 口径后删掉它；④ 留后票，
  在件头写下「另 5 处是各说各的账目文本」。
- **会不会影响 15 张铺开**：不影响。四条口径的真源已各在一处（`summaryRow.ts`／`policy/category.ts`），
  15 张页照引即可；上面三处是存量件的硬写与一个多余导出，不动任何页面的取值。

### C3（部分成立）

- **差了哪一点**：(a) 证据件 `:29–30` 那句「七件里只有 `pageShell`／`typeBadge`／`summaryRow`／`errorReceipt` 四件写了
  `src/query`」件数对不上——`a1fc236` 那一版只有 3 件写了（`pageFrame`／`summaryRow`／`failureReceipt`），
  `typeBadge` 是本次拆件才有的件头；(b) `summaryRow.ts:3–7` 仍写「真引用本件的是上面两个调用点，再无第三处」，
  实读 5 个件 import 本件，证据件 `:25–26` 同记两处；(c) `emptyNote.ts:4` 写 `collect.ts:211`，实调 `collect.ts:214`。
- **最小修法**：(a) 那半句改成「七件里当时只有 `pageFrame`／`summaryRow`／`failureReceipt` 三件写了 `src/query`」；
  (b) `summaryRow.ts` 件头把「两处调用点」与「另三处引它的口径（`blockedSlots.ts:26`／`typeBadge.ts:16`／
  `duplicateNote.ts:22`）」分开列，删掉「再无第三处」；(c) 行号改 `:214`。
- **会不会影响 15 张铺开**：不影响取值。影响的是「谁引了哪一份口径」的可追性——后手照件头找口径时，
  `summaryRow` 件头现在少报了三个引用方，容易把那一份口径当成只服务两页。

### C4（部分成立）

- **差了哪一点**：本次改动文件里还剩 8 行命中禁用表：证据件新增行 3 处（`:225` 的 `落地`（作实施讲）；
  `:353`／`:354` 的用词改正表左列把禁用原词当引文印了出来）＋既有行 5 处（两页产物里嵌入的 base 注文 4 处 `壳`、
  `test/record-write.test.mjs:85` 断言名里的 `渲染层`）。
- **最小修法**：`:225` 改「最后改上去、也入库的那一次真跑」；`:353`／`:354` 左列不再逐字印原词（改「本件原 `:29`／`:177`
  那两处，原词见首轮提交」）；`test:85` 的断言名去掉 `渲染层` 三字。两页产物里那 4 处在 base 注文里，属 base 件自己的面。
- **会不会影响 15 张铺开**：不影响。改法都是一句话替换；两页产物那 4 处本票既改不动，也不该在本票改。

### C5（部分成立）

- **差了哪一点**：「失败回执」包内仍有 1 处 `src/output.ts:20`（「exit 5 ＋ 结构化失败回执」），
  与自称「本包 grep：旧名 0 命中」不符（该件不在本次改动清单里）。件名与调用点本身已同步，这一条只差那句自称。
- **最小修法**：`output.ts:20` 改「结构化错误回执」；或把自称那句改成「改名与调用点已同步；另在未改的
  `output.ts:20` 有一处旧措辞，归后票」。
- **会不会影响 15 张铺开**：不影响。

## 四、结论

1. **放行**：10 条里 6 条成立、4 条部分成立、**0 条不成立**；四条部分成立没有一条动取值口径或产物结构，
   端到端三处真跑全绿（退出码 0、`data-page` 真在、量级与自称逐字节相同），`tsc -b --force` 退出码 0、
   `node --test` 78 通过／0 失败。
2. **本票（页面积木 ＋ 缺口块 ＋ 代表页）经整改后仍成立，可以据此铺其余 15 张**——铺之前带三件小事：
   每张页给一枚自己的 `data-page`（照 `writeParts.ts:75` 那一处加，别动 `data-shape` 契约）；
   重复检测只按「已给分类 ＋ 同日 ＋ 同额」出条，别在页里写第二份判定；方向判定只挂 `bill.record.add`，
   `bill.record.update` 的改字段／撤销／恢复三支不判。
