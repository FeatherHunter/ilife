# t417 HTML 打磨 · 查询页双端＋文案（终审 100/100 PASS）

> 用户要求：自适应桌面＋手机（对标 HELP）、代码层 UI 审美、无冗余文案、`；|·` 分隔改 UI 设计、vision 逐轮审查、终审 ≥90。
> 范围：只动查询能力目录（`src/query/` 三件）＋判据两件；`base-paint` 是外部版本包只读、共用件与他票分支一字不动。

## 一 改动（5 处，行内 `·` 不许进版式位沿 t407 用户语言整改同口径）

1. 结果胶囊行（`renderChips`，已有共用先例）：列表 `共 N 笔`、详情 `记录编号 id`（＋已撤销）；副标题只留窗口说明（日期／时刻）。
2. 笔数卡去单位（`2 笔`→`2`，label 已是笔数）；支出／收入 caption `这一段的…`→`本页…合计`（哪一页都成立）。
3. 正常态去第三遍（值正常＋ok 徽已两遍，删 `记录有效`；已撤销保留撤销时间）。
4. 空态 hint 去 `；`（两句直说；冻结子串 `换个日子再查一次`／`换个关键词`逐字保留）。
5. 详情表题 `账单详情`→`字段明细`（H1 已是查账单详情）。

不改（冻结或机器文本）：复制日志 `｜·`（载荷原文，t415:107 冻结，只活在按钮 data 属性里，不上屏）；
KPI 四格／两格计数、徽章、H1、空态四句；金额 `-35.50` 半角负号（t415:90 冻结，与载荷同字）；
空态保留 KPI（t412:118／t414:191 冻结）；负数着色无 base 层支持，记 base 候选不动。

## 二 读数（持锁，ticket 417）

- `tsc -b` 0；靶向 60/60；全量 **274/274**（271＋UI 锁 3 条）。
- 变异（沿用 t417 程序锁 M1，窗 `31cf57eb` 必红；本轮纯呈现改动，判据新增 `UI 打磨锁` 3 条：胶囊在／副标题无 ·／笔数无单位）。
- 行数 LF：list 187、detail 133、read 315（告警线 350，在线内，无超线）。

```
GATE-RUN runId=4a5b5a24.../b1eaa08a.../06610a48.../56856faf.../96e9ea7a.../d51cc57c...（编＋靶向＋全量＋重造，见 .scratch/html-polish/*.log）
```

## 三 vision 三轮

1. 基线（9 图：桌面 6＋手机 3）：副标题 `·`、笔数单位、表题复述、hint `；` 四处实锤。
2. 第一轮后复审：5 修全落定；追出 `这一段`、`记录有效` 两处（第二轮改完）。
3. 终审（4 图）：布局 20＋文案 25＋卡表 20＋空态 10＋移动 15＋操作 10＝**100/100 PASS**。
