# t407 重评 · 手机端采集页（390 宽 · 16 页 · 返工后新产物）

## 一、出图口径（含真 390 自证＋页高）

- 图取自**当前**产物（`docs\skills\skill-bill\` 下 16 份 `t407-*采集页.html`，时间戳均 2026-09-15 17:27），未用旧图。落盘 `.scratch\t407-rescore\mobile-collect\`：16 张 `-390-full.png`（1:1 全页）＋每页 3 张 `-seg1..3.png`（竖切 2 倍放大，供逐段细看）。
- 量法：**不用 `--window-size=390`**。外层页放 `width:390px` 的 iframe 载目标页，iframe 高锁到内页文档高，截外层；外层窗口 500×页高，不与 390 沾边。
- 真 390 自证：CDP 量内页 `window.innerWidth` 与 `documentElement.clientWidth`，16/16 全 **390**；`documentElement.scrollWidth = body.scrollWidth = 390`，16/16 无横向溢出（391／511 兜底问题在本批不成立）。
- 页高（截图像素高，与文档高逐页一致）：代表-记支出 2467／记收入 2647／记报销 3164／拍账单 3477／批量录入 2425／记退款 3366／报销到账 3626／记借出 3153／记借入 3170／记收回 3455／记偿还 3474／记分期 3658／记一笔 2673／改记录 2074／撤销 2118／恢复 1759。
- 判定口径：量表六项满分＝层级 20／留白对齐 20／无符号串 15／无内部话（复制区外）15／本端适配 15／对比度与按钮 15。符号串专指用「·」「|」「；」把多个事项串成一句的长句（标签分隔、顿号不算）。内部话专指命令名（`bill.*`／`bill-cmd-read`）、库列名（`category`／`amount`／`source_id`／`kind`）、数据库词（库／落库／库里／记录编号 2）。
- 复核口径：vision 抄出的任何内部标识符逐条回产物 HTML／当页可见文本 grep；产物里没有的一律记「模型编造」丢弃，不进分数理由。结论以 `read_image` 看到＋产物原文为准。

## 二、逐页分数表（16 行 · 上版→本版）

| 页 | 上版→本版 | 扣分（本版实例，已核产物原文） | 判 |
| --- | --- | --- | --- |
| 代表-记支出 | 52→60 | 口令原文块整条 `bill-cmd-read bill.record.add --params '{…}'` 上屏，说明句再抄一遍；「库里／落库默认／记录编号 2」进填空注解；末尾复述块与上面同义重复 | 及格未达标 |
| 记收入 | 62→68 | 口令原文块只剩命令名，句子断在「照这条说：bill.record.add。」；缺项在卡片矩阵与「还缺什么」块各说一遍 | 及格未达标 |
| 记报销 | 68→70 | 「库里／落库」「记录编号 2」进填空注解；「照这条说：bill.record.add。」断句；三个复制按钮与主按钮同款分不出主次 | 及格未达标 |
| 拍账单 | 92→50 | 首屏后叠两张「要素／参数名／现在／这一步谁办」四行表（`amount`／`category`／`time`）＋「缺一项就先不写库」表逐项重复；三块元信息挤在下半屏；口令原文 `bill-cmd-read` 整条上屏 | 不合格 |
| 批量录入 | 78→78 | 可见正文 `分类（category：没给）、金额（amount：没给）`；口令原文块 `bill-cmd-read` 整条；「币种同理由缺省『人民币』兜」是代码注释腔 | 及格未达标 |
| 记退款 | 78→73 | 可见正文 `分类（category：没给）、金额（amount：没给）、原支出编号（source_id：没给）`；口令原文整条；「两笔成对，可核」「拿不准就别选」两段重复讲同一件事 | 及格未达标 |
| 报销到账 | 48→78 | 空态卡文案重复半句：「这一格要的是**这一格要的是**打了 #待报销…」，并接「」。，列表里一条都没有。」；可见正文带 `category：没给／amount：没给／source_id：没给` | 及格未达标 |
| 记借出 | 72→78 | 「补齐了，说一遍试试」贴右缘偏挤；口令原文整条上屏；可见正文带 `category／amount／who` 三个参数名 | 及格未达标 |
| 记借入 | 58→70 | 上版「未给」卡堆密已缓解；可见正文带 `category／amount／who` 三个参数名；口令原文整条；说明行与标签行间距偏紧 | 及格未达标 |
| 记收回 | 92→82 | 「借出原记录」候选为空（0 条即出空态）；可见正文带 `category／amount／source_id`；口令原文整条；首屏三处讲同一件事 | 及格未达标 |
| 记偿还 | 78→76 | 「这一格要的是**这一格要的是**还没还回去的那笔借入…」。，列表里一条都没有。」重复半句；可见正文带 `category／amount／source_id` 三个参数名；口令原文整条 | 及格未达标 |
| 记分期 | 85→72 | 深色提示条 `.ilife-toast` 被写在参数卡内部，溢出成半条压在「首期日」输入框一侧；可见正文带 `category／amount`；口令原文整条 | 及格未达标 |
| 记一笔 | 82→70 | 口令原文块只剩命令名「bill.record.add。」断句；「缺省值（库里还没有可用的账户，留空＝落库默认）」两处；表单标签与格值间空行过多显散 | 及格未达标 |
| 改记录 | 73→72 | 口令原文整条上屏；候选表列名照抄数据库叫法「是哪一笔」「为什么是它」；「库里共 2 条未撤销的记录」是方言 | 及格未达标 |
| 撤销 | 72→78 | 空态说明里「库里共 2 条未撤销的记录」；候选表「记录编号／是哪一笔／为什么是它」；口令原文整条；同行双按钮（复制给助手／复制数据／复制日志）并列无分组 | 及格未达标 |
| 恢复 | 92→90 | 用词与间距干净、层级清楚；仅口令原文块整条上屏（`bill-cmd-read bill.record.update --params '{"op":"restore"…}'`）一处内部话 | 未过 90 |

## 三、涨跌归纳

- 上一轮被判编造的句子，这次产物里**仍不见踪影**：`期借借`、`MySQL`、`UTF8`、`user_id`、`created_at`、`amount_display`、`建表`、`bill.record.add --preset`、`bill:record:add`、`bill.repaid_bill`、`库共 2 条`，16 份源码 grep 全 0 命中，确认上轮那几处是模型编造，本轮不再采信。
- 符号串（「；」串多事项的长句）剩 **12/16 页**：代表-记支出、记报销、拍账单、批量录入、记退款、报销到账、记借出、记借入、记收回、记偿还、记分期、记一笔。集中形如「三级分类，如 餐饮/外卖/午餐；要选到最细那一级」「缺必需槽位：分类、金额（已出采集页，补齐之后跟助手说一遍）；本型另需：借出记录编号」。干净的 4 页：记收入、改记录、撤销、恢复。
- 区外内部话剩 **16/16 页**：命令名或库列名上屏是普遍现象——多为「口令原文」块整条 `bill-cmd-read bill.record.add --params …`（15 页）与可见正文里的 `category／amount／source_id／who`（11 页）。两类残留最多的是：代表-记支出／拍账单／报销到账／记偿还；相对最轻的是恢复（仅口令原文块一处）。
- 本批新暴露、上版没记的两类硬伤：① 空态卡模板替换串了，「这一格要的是」自我重复（报销到账、记偿还）；② 「口令原文」块只剩命令名，句子断成「照这条说：bill.record.add。」（记收入、记报销、记一笔）。
- 一票否决 0 页命中：无内容被切屏、无对比度低到看不清、无阻断按钮失效或重复两组可点，本批不触发否决。
- vision 质量注记：本轮 16 页均一次成功；模型仍会改写原句（记借出把「#借出」写成「#借入」），其抄出的标识符已逐条 grep，编造处见下节。

**本轮模型编造清单（已 grep 产物核否，未计入分数理由）**：① `bill.record.add --preset`（记退款）；② `bill:record:add`（记收回）；③ `bill.repaid_bill`（记一笔）；④ `bill-cmd=read bill.record.add --params…`（记借入）；⑤ `MySQL／UTF8／user_id／created_at／amount_display／建表／库共 2 条`（记收回、改记录）；⑥ `期借借`（记借入）；⑦ 记一笔「认不得罪名的时候，这一页也照安报，不清楚奉一型」；⑧ 记偿还「你欠别支出|已记写成欠款」「照估销哪一笔」「拿不到一笔顶替」等改写句；⑨ 拍账单把「分类要选到最细那一级（如 餐饮/外卖/午餐）」抄成带括号长句。

## 四、小计与结论

- 本版平均分 **72.8**（16 页均有分有理由）；上版 73.9；净涨跌 **−1.1**。判定：**不过**（≥90 才算过，本批 0 页过线）。
- 涨最多三页：报销到账 48→78、记借入 58→70、记借出 72→78。跌三页（均为新证据推倒上版宽松分）：拍账单 92→50、记分期 85→72、记一笔 82→70。
- 仍未过 90 的页与最小修法：恢复 90 分差一点——只删「口令原文」块里的 `bill-cmd-read … --params …`，改一句「补齐后照这句跟助手说一遍」纯中文引语；记收回 82 的最小修法是补一条借出候选（写死一条示例记录即可），免空态；记借出／报销到账／撤销 78 的最小修法是同一条——把 `category／amount／source_id／who` 从可见正文删掉，只在口令原文块留中文占位；拍账单 50 的最小修法是删掉下半屏第二、三张要素表和「缺一项就先不写库」整表，只留一张。
