# t432 生成器链证据（饼干记账命令登记机器拦）

## 一 · 目标与红线

- 给写入域命令登记补机器拦：`src/cli/registry.ts` 由生成器派生，`pnpm gen:check` 守真。
- 只纳已迁移的两条（`bill.record.add`／`bill.record.update`，能力 `record`）；其余 14 条留 `src/render/envelope.ts` 过渡表，行为与产物不动。
- 只在 `packages/skill-bill` 包内：不碰 `base-render` 与其他技能包；`combos.yaml` 属跨技能登记禁区，本生成器不派生它（见生成器头注释）；`wake-assets` 不手改；不放宽门。

## 二 · 改动件（四处＋日志）

- 新增 `packages/skill-bill/scripts/gen-cli.mjs`（287 行，告警线 350 以内，无需触发第四步）。
- 改 `packages/skill-bill/package.json`：`build` 加 `--stamp`，新增 `gen`／`gen:check`。
- 生成物 `packages/skill-bill/src/cli/registry.ts`：由手写过渡形态改为生成物（一能力一行，语义与旧文件同形，进出口 `REGISTRY`／`REGISTRY_KEYS` 不变）。
- 内容印记 `packages/skill-bill/dist/.gen-inputs.json`（构建产物，不入仓）：声明源 1 件（`record/commands.ts` 的 `{src, dist}` 配对）。
- 日志 `.scratch/t432/*.log`（本节 GATE-RUN 即对账源）。

## 三 · 门与验收（机器读数）

- 绿基线：`GEN-CHECK ok packages\skill-bill\src\cli\registry.ts sha256=308a24eb…`＋`GEN-CHECK PASS：命令 2 条；能力 1 个（record）`。
- 验收 1（手改必红）：手加 `// HAND-EDIT-PROBE-t432` 后 `gen:check` 出 `GEN-CHECK FAIL`（第 36 行起：盘上 `// HAND-EDIT-PROBE-t432`／生成空），exit 1；跑 `pnpm gen` 恢复后同上绿行，exit 0。
- 验收 2（加命令触面）：新命令只改 `src/<能力>/commands.ts`（恰好一个声明数组，且 `index.ts` 再导出）加它那个子功能文件；`registry.ts` 由 `pnpm gen` 派生，`git status` 无需手跟共用位。
- 验收 3（确定性）：两次 `pnpm gen` 的 `registry.ts` SHA256 均为 `308A24EB…21DB48`，逐字节相同。
- `GEN-STALE`：删 `dist/record/commands.js` 后报 `GEN-STALE FAIL`（缺该产物），`tsc -b --force`＋重打印记后回绿，重建产物与探前逐字节相同。
- `GEN-PAIR`：改 `dist/record/commands.js` 一字节后报 `GEN-PAIR FAIL`（编译产物与印记对不上），重建＋重打印记后回绿。
- 回归：`node --test packages/skill-bill/test/*.test.mjs`＝153 通过／0 失败（含 16 键全票与过渡表 16 条形状不断言缩水）。

## 四 · 持锁对账（GATE-RUN）

- `GATE-RUN runId=t432-build-tsc2` 编译 exit 0；`runId=t432-stamp` 印记 exit 0；`runId=t432-gen1` 派生 exit 0。
- `runId=t432-check-green` 绿 exit 0；`runId=t432-check-red` 红 exit 1；`runId=t432-gen-restore` 恢复 exit 0（waitedMs=15010，排队一轮 15 秒，符合锁口径）；`runId=t432-check-restore` 绿 exit 0。
- `runId=t432-gen-a`／`t432-gen-b` 双派生 exit 0；`runId=t432-stale-probe` 红 exit 1；`runId=t432-force-build`＋`t432-restamp2`＋`t432-final-check` 绿 exit 0。
- `runId=t432-pair-probe` 红 exit 1；`runId=t432-pair-fix-build`＋`t432-pair-fix-stamp`＋`t432-pair-fix-check` 绿 exit 0。
- `runId=t432-bill-test` 全量 153/0 exit 0。详见 `.scratch/t432/*.log` 尾部标记行与 `gate-runs.log` 对应条目。

## 五 · 遗留出口（下一票输入，本票不动）

- 若卡路里与饼干两套同构生成器需共用，把「扫描 `src/*/commands.ts`→写注册表＋内容印记＋双门」提升到公共层；本票保持两份同构独立，不做跨包改动。
- 后续搬迁每搬一条：删过渡表对应行并在能力声明里补 `example`，本生成器自动纳入，无需改门。
