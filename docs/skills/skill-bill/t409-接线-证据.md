# 409 接线证据：写入 16 词唤醒词→工作流程（程序可验）

> 票：#409（写入 16 词唤醒词→工作流程接线：SKILL.md＋HELP HTML 两处可查、程序可验；不做真出口断言）。
> 结论前置：16 行五段全对（`RESULT 16/16`），无命令可执行词 0 条，最长匹配回归绿；
> HELP 卡片 CLI 格仍为 `scene.id` 原文（base 冻结口径，回补归 #106 系，遗留 432），本票只给映射不断言卡片改字。

## 一 · 影响清单（第一步申报 ↔ 交付对账）

| # | 路径 | 动作 | 一句话理由 |
|---|---|---|---|
| 1 | `packages/skill-bill/src/help/writeWire.ts` | 新增 | 16 行接线派生（四处事实读拼，无第二份词表），help 能力内新增 |
| 2 | `packages/skill-bill/src/help/index.ts` | 改 1 行 | 转出 `WRITE_WORDS`／`buildWriteWire`（门对外 5 件，铁律五达标） |
| 3 | `packages/skill-bill/test/t409-wire.test.mjs` | 新增 | 7 断言：五段＋HELP 写卡 1:1＋SKILL 块＋落点互核＋最长匹配＋反例 |
| 4 | `docs/skills/skill-bill/t409-reconcile.mjs` | 新增 | 对账脚本（正例 `RESULT 16/16` exit 0；反例点名 exit 非 0） |
| 5 | `docs/skills/skill-bill/t409-接线-证据.md` | 新增 | 本证据 |
| 6 | `docs/skills/skill-bill/t409-GATE-RUN.log` | 新增 | 门禁运行导出（§2.3 对账源，见二） |
| 7 | `.scratch/t409/impl/*` | 日志 | 等待量／运行明细（未入仓，备查） |

未碰（申报即边界）：`base-render` 全件、`src/record`、`src/shared`、`src/cli/cmd_read.ts`、
旧测试、`SKILL.md` 字节（`build-help` 复核无改写）、`wake-assets` 载荷（`LEGACY_DIGEST` 未动）。
偏差：零（逐行对得上）。

结构纪律：能力门 `src/help/index.ts` 对外 5 件（`buildHelpLookup`／`lookupWake`／`HelpHit`／
`WRITE_WORDS`／`buildWriteWire`）；`writeWire.ts` 对外 2 件（类型 `WriteWireRow` 不出门，不计数）；
`help→policy／render（门）／triggers（沿 helpFile.ts 先例直引）`向下引用，无环；
超线报警：两件均远低于 350 行（`writeWire.ts` 约 110 行），无超线。

## 二 · 门禁对账（GATE-RUN，一对一）

导出件 `docs/skills/skill-bill/t409-GATE-RUN.log` 为对账源（§2.3；等待量见 `.scratch/t409/impl/gate-wait.log`，
15 秒重试、10 分钟上限，本次最长等待约 285 秒后取到锁，无超时抢回、无危险 git 命令）。

| GATE-RUN | 命令 | 退出码 |
|---|---|---:|
| `runId=81859fde-85ef-40f1-9403-53528129f5d1` | `npx tsc -b packages/skill-bill [fix-shapeOf]` | 0 |
| `runId=8a750d8b-ea71-48c8-a4d1-ab14252bce5b` | `node packages/skill-bill/scripts/build-help.mjs [SKILL-same=True]` | 0 |
| `runId=9d8bf619-a6aa-4906-a9c1-cefeec279527` | `node --test packages/skill-bill/test/*.test.mjs [fix-shapeOf]`（145/145） | 0 |
| `runId=72c72816-7724-4e53-8e35-4cecfd6980de` | `node docs/skills/skill-bill/t409-reconcile.mjs [final-dist]`（RESULT 16/16） | 0 |
| `runId=8c9becb8-7a60-42bc-b70f-e67000cfb33d` | `... --check-phrase [final-dist]`（反例点名 exit 2） | 2（预期） |
| `runId=576bea38-b2c2-4f03-b129-be8549bee650` | 变异轮（改坏一行→t409 测试变红，fail=2） | 见五 |
| `runId=9ca21f37-8ddf-4f87-9e59-10bac08f3345` | 还原轮（还原一致→t409 测试 pass=7 fail=0） | 见五 |

过程注：中间两轮构建曾红（`TS2367` 形状比较、`TS2552` 改名漏两处，见 `.scratch/t409/impl/build.log`／
`build2.log`），当场修好后重跑为绿；红轮不计入门禁证据（只认 exit 0 条目）。

## 三 · 对账读数（RESULT）

- 正例：`node docs/skills/skill-bill/t409-reconcile.mjs` → `RESULT 16/16`，exit 0
 （`runId=72c72816-7724-4e53-8e35-4cecfd6980de`，明细 `.scratch/t409/impl/reconcile-final.log`）。
- 反例：`... --check-phrase 同步记账到云端` → `PHRASE-NO-WIRE：同步记账到云端`，exit 2（点名，
 `runId=8c9becb8-7a60-42bc-b70f-e67000cfb33d`）；正探针 `--check-phrase 记支出` → `PHRASE-OK` exit 0。
- 16 行明细见 `.scratch/t409/impl/reconcile-positive.log`（`OK … ｜ HELP[write_*]` 16 行）。
- 关键难行：`记偿还 → bill.record.add {"kind":"repay"} ｜ HELP[write_payback]`
 （路由 kind `repay` 与 HELP 场景 id `payback` 异名，映射点名，无猜测）。
- 最长匹配：`记报销→reimburse`／`报销到账→reimburse-done`／`记报销报销到账→reimburse-done`。

## 四 · 测试红绿

- 新增 `t409-wire.test.mjs`：7 断言（绿，`runId=9ca21f37` 轮 `pass=7 fail=0`）。
- 存量：`wake-assets`／`policy`／`skill` 及 `skill-bill` 全量 145/145（绿，
 `runId=9d8bf619`，`pass 145 fail 0`，见 `.scratch/t409/impl/test-full-bill3.log`）。
- 构建：`npx tsc -b packages/skill-bill` exit 0（`runId=81859fde`）；
 `node scripts/build-help.mjs` exit 0 且 `SKILL.md` 前后 SHA256 一致（`runId=8a750d8b`），手写词未动。

## 五 · 变异自证（源码级，一行改坏变红，还原变绿）

- 变异位：`src/help/writeWire.ts` 写域判定一行
 （`if (g.id !== 'write')` → `if (g.id !== 'write-mut')`，持锁改坏→重建→跑 `t409-wire`）。
- 红读数：`ℹ fail 2`（五段＋HELP 1:1 两断言红，`runId=576bea38`，见 `mut-red.log`）。
- 绿读数（还原一致）：源文件 SHA256 与变异前一致（`RESTORE-SAME=True`），重建 exit 0，
 `ℹ pass 7 fail 0`（`runId=9ca21f37`，见 `mut-green.log`）。

## 六 · 未做与遗留（下一手）

- 未做（票面不做）：真出口断言（16 词 `exit 0` 真跑）→ 遗留 **410**（锁票，程序钉死＋用户肉眼终审）。
- 未做（本次不动）：HELP 卡片 CLI 格可执行化（`scene.id` 原文→`bill-cmd-read …`，须动 base-render
  `cliText`／`Scene` 契约，归 #106 系）→ 遗留 **432**（后票先抢 `gate.lock` 并广播，见命令登记纪律 §二）。
- 单源链仍为“双源＋对账缝合”（C 调查结论）：路由源 `WAKE_TABLE` vs 内容源仓外实物，
  本票只加 16 词直连缝合，不断生成器链（G1–G8 原样留后票，见 C-调查 §3）。
