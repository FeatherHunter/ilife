# t686 机器面证据（生成器链／两条棘轮／收紧守卫／行数门）

正本＝issue [#686](https://github.com/FeatherHunter/ilife/issues/686)；本件记**机器读数**，判断与结论在票面。

## 一 · 交付物与提交

| 产出 | 件 | 提交 |
|---|---|---|
| 1 生成器链（含内容印记 `--stamp`，接进 build 与 CI） | `package.json`（仓根 `gen`／`gen:check`／`build`）、`packages/skill-bill/package.json`、`packages/skill-bill/scripts/gen-wake-assets.mjs`、`packages/skill-bill/src/triggers/wake-assets.ts`（生成物随生成器头注释重生成，逐字节只差那一行） | `edcc1276` |
| 2 两条棘轮 | `packages/skill-bill/test/cmd-registry-686.test.mjs`、`test/legacy-ratchet-686.test.mjs`、`scripts/ratchet-frozen-686.mjs`（冻结值与量法的唯一定义地） | `edcc1276` |
| 3 行数门（机器扫 ＋ 对账台账 ＋ 生成物按生成器声明自动剔除） | `packages/skill-bill/scripts/check-warning-line.mjs`、`test/t686-告警线门.test.mjs`、`packages/skill-bill/AGENTS.md`（手抄行数表换成机器台账） | `57880028`＋`fa4e122b` |
| 4 收紧守卫（卡路里没有的那条：冻结值不许比实况松） | `packages/skill-bill/scripts/check-ratchet-tight.mjs`、`test/t686-收紧守卫.test.mjs` | `edcc1276` |
| 判据自证件（可复跑） | `docs/skills/skill-bill/t686-变异探针.mjs`、`docs/skills/skill-bill/t686-加命令探针.mjs` | `edcc1276`（加命令探针后改，见 §6） |

## 二 · 票面四条判据的机器读数

### 判据 ① 加一条命令的 `git status` 里，除声明与生成物之外没有手写件

`node docs/skills/skill-bill/t686-加命令探针.mjs`（持锁、真树、两种加法、探针自建件全复位）＝`RESULT: 4/4`：

- **A 往已有能力（`query`）加一条命令**：`git status -uall` 新增项 ＝ `["packages/skill-bill/src/query/commands.ts"]`——**只有声明件**；
  生成物 `src/cli/registry.ts` 该次**一字节未变**（sha256 `113648fa…` 前后相等），生成器自报 `gen 完成：命令 7 条；能力 2 个`。
- **B 新开一个能力目录**（`src/probe/`）：新增项恰为三件 ＝ 两份手写声明（`src/probe/commands.ts`、`src/probe/index.ts`）
  ＋ 一件生成物（`src/cli/registry.ts`，sha256 `bc5b5775…` 由生成器派生：多一行 import 与一行 `SOURCES`）。
- 复位读数：两种加法各自「重建 `tsc -b` ＋ 重打内容印记 `--stamp` ＋ 重跑生成器」后，声明与生成物 sha256 逐字节回位、
  `git status` 新增项为空、探针自建目录（含 `dist/probe/`）删净。

### 判据 ② 手改任一生成物 ⇒ `gen:check` 红（门的鉴别力自证一次）

`node docs/skills/skill-bill/t686-变异探针.mjs`（持锁）＝`RESULT: 6/6`，两件生成物各三条读数：

| 生成物 | 手改 ⇒ 红 | 重新生成 ⇒ 逐字节回位 | 复位后回绿 |
|---|---|---|---|
| `src/cli/registry.ts`（`scripts/gen-cli.mjs`） | exit=1｜`GEN-CHECK FAIL：命令 6 条；能力 2 个（query／record）` | 探前/复位后 sha256 均 `113648fa3c1434c6…` | `GEN-CHECK PASS` |
| `src/triggers/wake-assets.ts`（`scripts/gen-wake-assets.mjs`） | exit=1｜`DRIFT：…与生成结果不一致（禁止手改；重跑不带 --check 即覆盖）` | 前后 sha256 均 `7bdb61108711c93b…` | `OK：…与生成结果字节一致` |

### 判据 ③ 棘轮的冻结值必须随搬迁同步下调 ＋ 收紧断言「实况 ≤ 冻结值 且 冻结值不高于实况 + 0」

- 收紧守卫真包：`RESULT: 9/9`＋`PASS: 棘轮与实况逐项恒等`；判据自证 `SELFTEST 5/5`（恒等放行；冻结值留多余键／实况多出／行数上涨／注册表少一条，四种松法各自被点名）。
- 夹具树（`--root`，同一棵树改一处即红）：分派层多一条 `case` ⇒ 红并点名那条键；过渡表少一条而冻结值留着 ⇒ 红并点名「冻结值仍有 1 条」（卡路里 #294 的病）；行数上涨 ⇒ 红并点名 `lineCaps`；**计数不变而键被换了**（注册表里一条键被替掉）⇒ 红并点名 `unionIsTotal`（判据不退化成数数）；缺 `dist/` 量不到实况 ⇒ 红且不是静默放行。
- 棘轮两个测试件：`RESULT 21/21`。其中「同窗纪律机器自证」四例：只删 `case`／只删过渡表行／两处都删却没下调冻结值 ⇒ 三种都红；两处都删 ＋ 冻结值与上限同窗下调 ⇒ 绿。

### 判据 ④ 行数门跑得过、且生成物自动剔除（不靠人手维护剔除名单）

`node packages/skill-bill/scripts/check-warning-line.mjs`（无参）＝`RESULT: 12/12`＋`PASS: 告警线台账齐全且与实况一致`：

```
GENERATED-SKIP src/cli/registry.ts LF=37（由 scripts/gen-cli.mjs 生成，不在扫描面内）
GENERATED-SKIP src/triggers/wake-assets.ts LF=986（由 scripts/gen-wake-assets.mjs 生成，不在扫描面内）
OVER scripts/check-warning-line.mjs LF=363 已超线，需要根据规则进行重构。（本次先不拆，见台账）
OVER src/cli/cmd_read.ts LF=501 已超线，需要根据规则进行重构。（本次先不拆，见台账）
OVER src/health.ts LF=486 已超线，需要根据规则进行重构。（本次先不拆，见台账）
AGENTS.md：…（台账 3 行；扫描面 90 件，超线 3 件，剔出生成物 2 件；挂号台账命中 1/1）
```

- 剔除名单**从生成器自己的输出声明推**（`const OUT = join(SRC_DIR, …)`／`targets` 里的 `path: join(SRC_DIR, …)`），没有第二份手写名单；生成物印章「勿手改」而生成器声明认不出时必红（判据自证）。
- 五种红（漏报／台账陈化／台账件不在扫描面内／台账缺行或挂号值被改写／生成物判据自证）逐条现场读数在
  `docs/skills/skill-bill/t686-告警线门-证据.md` §4；门的自测 `test/t686-告警线门.test.mjs`＝10/10（含 `--sync --dry` 不改文件、`--sync` 回绿且块外逐字节不动）。
- 台账 3 行的「当场实测」列与盘上实况逐行相符（501／486／363，节点口径 `split('\n').length - 1`）；挂号值 `src/cli/cmd_read.ts｜558` 是历史事实、门只核它没被改写。
  **`src/triggers/wake-assets.ts` 由「历史挂号件」转为「生成物自动剔除」**：它的 986 是 t406 当期的历史读数（那时确实超线），
  现在它的行数由生成器决定、重跑 `pnpm gen` 就变，再挂号只会逼无关的票来同步台账——**这不是删账**，台账说明里写明了。

### 落地门（CI 口径）

- `pnpm gen:check`（仓根，CI 真跑的那一步）＝**exit 0**，逐件输出含
  `GEN-CHECK PASS：命令 6 条；能力 2 个（query／record）` ＋ wake-assets `OK：…与生成结果字节一致`——**饼干这一支从此在 CI 里真跑**。
- `pnpm build`（仓根）在 `tsc -b` 之后写两家的内容印记：`…skill-calorie/scripts/gen-cli.mjs --stamp && …skill-bill/scripts/gen-cli.mjs --stamp`。
- 包内全量 `node --test packages/skill-bill/test/*.test.mjs`＝**311/311 通过**（含本票两个棘轮件、收紧守卫件、告警线门件）。
- 行数门与收紧守卫在 CI 里经 `pnpm test` 真跑（两个测试件都会 spawn 真门、无参跑真台账）。

## 三 · 冻结值（照票面口径：形状照抄、数字自己现场测）

`packages/skill-bill/scripts/ratchet-frozen-686.mjs` 的 `FROZEN` 是**唯一定义地**：

| 项 | 冻结值 | 实况 | 出处 |
|---|---|---|---|
| 分派层按键分派字面量集 | 10 条 | 10 条 | `src/cli/cmd_read.ts` 的 `switch (key)` 十个 `case`（＋三处 `key === '…'` 前置守卫同键） |
| 过渡表键集 | 10 条 | 10 条 | `src/render/envelope.ts` 的 `TRANSITIONAL_KEY_SHAPES` |
| 注册表键数 | 6 | 6 | 生成物 `src/cli/registry.ts` 的 `REGISTRY_KEYS` |
| 键总数 | 16 | 16 | 口径层 `WAKE_TABLE` 的键集（手写，独立事实源） |
| 行数上限 `src/cli/cmd_read.ts` | 501 | 501 | 节点口径 LF |
| 行数上限 `src/render/envelope.ts` | 64 | 64 | 同 |

「搬一条」要动三处（同窗）：删能力目录外那两处（分派层 `case` ＋ 过渡表形状行）＋ 下调冻结值（`registryKeyCount` ＋1，键集与行数各按实况改）。

## 四 · 持锁对账（本票声明过的运行）

导出的锁记录正本：`docs/skills/skill-bill/t686-锁记录导出.log`（照协议 §2.3 第 4 条抄录本票条目）。

```
GATE-RUN runId=cdbb1210-6618-4ce9-b4b3-8ce2328907c1 cmd=cmd /c "node packages/skill-bill/scripts/gen-cli.mjs --check && node packages/skill-bill/scripts/gen-wake-assets.mjs --check"
GATE-RUN runId=f3dd2da7-c527-429f-a437-f7ffc6d45d04 cmd=node packages/skill-bill/scripts/gen-wake-assets.mjs
GATE-RUN runId=feb2acac-6dd4-41bf-9124-f706284e25d4 cmd=node docs/skills/skill-bill/t686-变异探针.mjs
GATE-RUN runId=332080d1-7f95-4815-96c9-ec9fe3d381a1 cmd=node docs/skills/skill-bill/t686-加命令探针.mjs
GATE-RUN runId=b59936d1-875d-4de2-8cf8-d79c7ef9cca6 cmd=node --test packages/skill-bill/test/cmd-registry-686.test.mjs packages/skill-bill/test/legacy-ratchet-686.test.mjs packages/skill-bill/test/t686-收紧守卫.test.mjs
GATE-RUN runId=3a42d0da-ffea-4703-b554-b4ad880617a2 cmd=pnpm gen:check
GATE-RUN runId=275691dc-cb9a-4e2d-ba5f-b3fb346ae817 cmd=node packages/skill-bill/scripts/check-ratchet-tight.mjs
GATE-RUN runId=a09af052-c416-4216-ac84-a9f864c445cb cmd=node packages/skill-bill/scripts/check-ratchet-tight.mjs --selftest
GATE-RUN runId=057cfc83-c39c-4228-b1f1-d50bc45fb50a cmd=node packages/skill-bill/scripts/check-warning-line.mjs
GATE-RUN runId=309b6212-fbac-4d2a-aec9-40ce22ce4832 cmd=node --test packages/skill-bill/test/*.test.mjs（逐件列名）
```

日志件：`.scratch/t686/*.log`（含 `ratchet-tests-3.log`／`mutation-probe.log`／`add-command-probe.log`／`root-gen-check.log`／`bill-tests-2.log`／`tight-final.log`）。
行数门那一支的持锁条目见 `docs/skills/skill-bill/t686-告警线门-证据.md` §6（runId=036e11ea…／a7ef2d3e…）。

**本清单是首轮（实施当刻）**；复核席两轮（`rev686-01…14`）与修订后的条目见导出件全文（同一个 `t686-锁记录导出.log`，末次导出含全部）。

## 五 · 未做项与下一手

1. **三件超线件一件未拆**（`src/cli/cmd_read.ts` 501／`src/health.ts` 486／`scripts/check-warning-line.mjs` 363）：拆法逐件写在
   `packages/skill-bill/AGENTS.md` 台账「结论」列，待归属票认领；本票只立门与台账，不动任何件源码（`wake-assets.ts` 只随生成器头注释重生成）。
2. **搬迁票要同窗动的共用位**（受纪律约束，不是漏网）：① 能力目录两份声明；② 删分派层 `case`；③ 删过渡表形状行；
   ④ 下调 `scripts/ratchet-frozen-686.mjs` 的冻结值；⑤ 更新既有对账面 `test/record-write.test.mjs:87-91`（那里硬写「6 条＋六个键」，属**范围外发现**：它是 #411 留下的手写对账面，本票不改别人的测试件，记账转给搬迁票）。
3. **冻结口径是恒等**（不是 `<=`）：任何改动 `src/cli/cmd_read.ts` 行数的票都必须同窗改 `FROZEN.lineCaps`——这是设计行为
   （卡路里那两条棘轮正是被 `<=` 喂松的），不是误报；修法就一句：改 `scripts/ratchet-frozen-686.mjs`。
4. **行数门会对扫描面任何改动转红**（改超线件行数／新增删除扫描面内的件即红）：修法＝`node packages/skill-bill/scripts/check-warning-line.mjs --sync`。
5. 本票**未 push**（按当轮口径由编排者统一收口）。

## 六 · 独立复核席（另起一席，实施席不得自审）

复核报告正本＝`docs/skills/skill-bill/t686-复核-独立复跑.md`（两轮，判定均 **PASS**；日志 `.scratch/t686/review-*.log`）。

- 第一轮：逐条复跑四条判据（`rev686-01…10`），另自造三条探针——仓根链上的生成物漂移（改 `wake-assets.ts` ⇒ `DRIFT` ⇒ 重生成回绿）、
  行数门的**真·超线件**（新建 360 行件 ⇒ `RED 漏报`）与**台账陈化**（`health.ts` +1 行 ⇒ `RED 台账陈化：486/487`）、
  生成物剔除判据的两条反例（输出声明改成认不出的写法 ⇒ 该件掉回扫描面；两台都改 ⇒ 另红「判据自证」＋「印记已认领」）。
  缺陷：本票引入 **0**；本票范围两处低危（见下）。
- 两处低危发现的处置：①**「仓外事实源缺席时 `--check` 打 SKIP 放行」这条机器前提没进包内台账** ⇒ 已写进
  `packages/skill-bill/AGENTS.md`「文件行数告警线」一节（`ecf19f66`）；②守卫红条原文与票面引文的**字面差异**
  （票面写「冻结值比实况松」，实现原文是「实况已降 N 而冻结值没下调：棘轮的牙齿松了」）⇒ 语义等价，不改实现，
  按实际原文判定（复核席亦据此判）。
- 复核席自查之后，实施席又自查出一处**缺口并同窗堵上**（`ecf19f66`）：分派层量法原先只盯 `src/cli/cmd_read.ts` 一件——
  把 `case` 挪进同目录姊妹件即可绕开判据。现改成 **`src/cli` 全目录**（照卡路里 `cmd-registry-294`），并新增一条夹具自证。
  同一处还修掉一个**静默吞错**：`cliDispatchKeysOf` 里原来的 `try/catch` 会把「`readdirSync` 未导入」的 `ReferenceError`
  吞成「实况 0 条」（实测当场把守卫判假红）——现改成直接抛，读不到就报 `FAIL: 量不到实况`。
- 第二轮（新修订 `ecf19f66`）：`rev686-11` 守卫 `9/9`＋`SELFTEST 5/5`＋行数门 `12/12`＋棘轮三件 `22/22`＋包内全量 `312/312`；
  `rev686-12` 新探针（姊妹件里放一条按键分派）⇒ 守卫点名 `bill.zz.probe`、删件回绿；`rev686-13` 同场对读「旧口径 10 条（不含探针键）／
  新口径 11 条（含）」——洞真在、真被堵；`rev686-14` 缺 `src/cli` 的夹具 ⇒ `RESULT: 0/1` ＋`FAIL: 量不到实况（ENOENT）`，不静默当绿。
  判定仍 **PASS**、本修订引入缺陷 **0**；它动过的件逐字节回位（`ratchet-frozen-686.mjs` sha `0a1cf46f…617a` 改前＝还原后）。
