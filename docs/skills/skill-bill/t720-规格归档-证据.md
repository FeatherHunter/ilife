# #720 规格归档与收口 · 证据

- 票：[#720](https://github.com/FeatherHunter/ilife/issues/720)（[规格] 目标 src/ 形状：7 个域目录与共用位）· 母图：[#681](https://github.com/FeatherHunter/ilife/issues/681) · 决议来源：[#683](https://github.com/FeatherHunter/ilife/issues/683)（2026-09-18 已关）
- 性质：**本票零源码改动**。执行 ＝ 把规格的交付物落进版本控制 ＋ 逐条复核规格里的读数与依据（对抗式）。
- 执行当刻：2026-09-19，工作区 `D:\ilife`，branch `master`；commit `05eb0bef8a177f942d65252c2c3d2f143ca3198a`。

## 一 · 交付物与入仓读数

| 交付物 | 路径 | 机器读数 |
|---|---|---|
| 逐件归属表（86 件） | `docs/skills/skill-bill/t683-目标src形状.md` | blob `9b8de313d4e5d7c8899d128fb5e1b671d3490a54`，首次入仓（`git ls-tree HEAD` 可验） |
| 规则级记录（三类准入） | `docs/adr/0003-一级目录三类准入.md` | blob `d04ae4def34b000578c3c9e968e754291633b3a3` |
| 过程留档（讨论页） | `docs/skills/skill-bill/t683-待裁五问-第二轮.html` | blob `71b6d9685b2b4920202cc9a3b61764ad7866281f` |
| 过程留档（讨论页） | `docs/skills/skill-bill/t683-待裁六问.html` | blob `4feaaee2551d52056bc534c918667fb628c66dce` |
| 规格正文（tracker 条目） | [#720](https://github.com/FeatherHunter/ilife/issues/720) 票面 | 本次补 `## 进度：100%` 段 ＋ 改两处陈化读数（见 §四） |

入仓纪律：先声明四条路径 → 持锁 `git add` ＋ 暂存区复核（`STAGED-COUNT=4`，逐条按 pathspec 问 git 自己）→ `git commit -F`（限路径）。四条文件经核：无 BOM、全 LF、无字面 `\n`。
零源码改动：`git status --porcelain -- packages/skill-bill/src` 为空（`src/` 与 `test/` 一字未动）。

## 二 · 读数复核（逐目录逐件，与归属表逐值比对；2026-09-19 复测）

| 一级目录 | 件 | LF | 归属表 | 判定 |
|---|---|---|---|---|
| `<root>` | 4 | 624 | 624（74／486／4／60） | 相同 |
| `cli` | 4 | 658 | 658（501／84／36／37） | 相同 |
| `fetch` | 4 | 321 | 321（250／44／23／4） | 相同 |
| `help` | 3 | 172 | 172（55／2／115） | 相同 |
| `policy` | 7 | 483 | 483（125／91／43／37／33／142／12） | 相同 |
| `query` | 5 | 727 | 727（53／148／22／189／315） | 相同 |
| `record` | 25 | 5,104 | 5,104 | 相同 |
| `render` | 8 | 548 | 548（64／11／185／20／70／15／58／125） | 相同 |
| `shared` | 25 | 2,645 | 2,645 | 相同 |
| `triggers` | 1 | 986 | 986 | 相同 |
| **合计** | **86** | **12,268** | 86 件／12,268 LF | **相同** |

读数口径与归属表同（LF：只数 `\n`）。与票面「问题」节、归属表 §三 标题的 86 件／12,268 LF 逐字相同 ⇒ **规格的读数没有陈化**。

## 三 · 逐条抽核（归属表与规格里带依据的断言）

| 断言 | 实测 | 判定 |
|---|---|---|
| `shared/` 留 6 件 553 LF（`commandSpec` 74／`copyArea` 159／`pageIdentity` 36／`pageShell` 65／`writeParts` 89／`docPage` 130）（§3.8） | 逐件相同，合计 553 | 成立 |
| `shared/` 下沉 19 件 2,092 LF（§3.8） | 19 件逐件相同，合计 2,092；553＋2,092＝2,645 ＝ `shared/` 实况 | 成立 |
| `render/views.ts` 12 个符号的行位与去向（§3.9） | `BillItem:8`／`toBillItem:13`／`isTransfer:20`（文件级私有）／`calcKpi:25`／`buildRecordReceipt:54`／`buildOverview:59`／`buildCompare:76`／`buildTrend:88`／`buildGoalQuery:111`／`buildAccountQuery:115`／`HelpItem:120`／`buildHelpItems:122` | 成立 |
| 5 件件头注释过期（`blockedSlots`／`duplicateNote`／`emptyNote`／`prefillNote`／`receiptParts` 写「第二个消费者 `src/query/`」而 query 侧零 import）（§七） | 这 5 件的 **72 条 import 边全在 `record/**`**；`summaryRow` 确有 query 消费者（不属过期）——与 §八 复核表的「收窄到 5 件」逐条一致 | 成立 |
| `shared/docPage.ts` 7 处 `ilife-query-kv-*` 类名在 89–96 行（§六 ③b） | 恰 7 处，行位 89–96 | 成立 |
| `shared/pageShell.ts:22-24` 的「域名→眉标」表（待改成域自报 `eyebrow`）（§六 ③b） | 表在 22–25 行，两键 `write`／`query` | 成立（行位 22 起） |
| `shared/writeParts.ts` 三字符 `esc` **不得**与五字符 `escapeHtml` 互换的红线（§五 红线） | 红线注释在 18–20 行逐字在位 | 成立 |
| 「两处就地摆正」保字节不变：公共层已导出五字符转义，实体表同构（§五／§八） | `packages/base-render/src/contract.ts:38` 导出 `escapeHtml`，实体源 `spec/controls.ts:18` 的 `ESCAPE_HTML_ENTITIES` ＝ `&amp;`／`&lt;`／`&gt;`／`&quot;`／`&#39;`；`src/render/html.ts:7` 自家副本同五字符 | 成立 |
| 包门 `src/index.ts` 是四行 `export *`（含 `policy/index.js`），删 `policy/` 须重指按「只许收窄」办（§六／§八） | 恰 4 行：`fetch`／`policy`／`render`／`help` | 成立 |
| 唤醒词表的真 import 边（§六「既有边两处」） | `src/triggers/wake-assets.ts:25` ← `../policy/wakewords.js`；`src/help/lookup.ts:2` 与 `src/help/writeWire.ts:17` ← `policy/index.js` | 成立 |
| 本包告警线台账未陈化（`packages/skill-bill/AGENTS.md`） | 台账「当场实测」两件超线件 `src/cli/cmd_read.ts`＝501、`src/health.ts`＝486，与实测逐值相同 | 成立 |

## 四 · 本票发现的两处不一致（已处置）

1. **票面件数陈化**：票面「84 件」两处（用户故事 9、「依据与指针」）与实况矛盾 —— `#683` 的独立席复核把漏掉的两件（`src/health.ts`、`src/cli/health.ts`）补进后，归属表标题已改 **86 件**，票面没跟上。本次按实测改 86 件（读数见 §二）。
2. **风险表那条对策已被实况取代**：风险表写「归属表里的判断项（表内带标记的 7 处）未经人眼复核 → **#683 保持开启**，抽查通过才达 100%」。实况是 `#683` 已于 2026-09-18 15:18 关闭，收口评论记「本席对抗式自查 ＋ 独立席只读复核，两批合并后改掉 3 处真错、2 处判据漏洞、3 处漏记耦合面」；表内 7 处 `判过` 已按复核处置（归属表 §八 对照表逐条留痕），不是悬置项。

## 五 · 持锁运行记录（对账导出，协议 §2.3 第 4 条）

```
START ticket=720 runId=598a33cf-868a-4135-a4f2-450f6a474717 cmd="pwsh -NoProfile -File .scratch/t720/commit1.ps1" waitedMs=10005 pid=34860 at=2026-09-19T02:02:31.540Z
RUN ticket=720 runId=598a33cf-868a-4135-a4f2-450f6a474717 cmd="pwsh -NoProfile -File .scratch/t720/commit1.ps1" waitedMs=10005 exit=0 pid=34860 at=2026-09-19T02:02:35.546Z
```

GATE-RUN runId=598a33cf-868a-4135-a4f2-450f6a474717 cmd="pwsh -NoProfile -File .scratch/t720/commit1.ps1"

等待量 10,005 ms＝当刻另一席（`ticket=717`）持锁，本票按「活进程一律不抢回」等它释放后取得。

## 六 · 未做项与下一手归属

- **不改 `src/`**：搬迁动作（目录改名 `record/`→`write/`、`shared/` 19 件下沉、`policy/` 拆散与删除、`render/views.ts` 拆散、两处就地摆正）归 **#689**；域声明形状与事实源归 **#684**（规格单 #721）；页面装配深模块与 7 域页型清单归 **#685**；机器守卫（白名单以本件归属表为来源）归 **#686**。
- **本票不跑编译与测试**：改动面只有四条文档路径，`packages/skill-bill/src`／`test` 零改动 ⇒ 没有可跑的行为门禁，也就不造「跑了却无对象」的假读数。
- 当刻在途（非本票路径，未碰）：`packages/skill-bill/scripts/ratchet-frozen-686.mjs`（已改未提交）、`docs/skills/skill-bill/t686-复核-独立复跑.md`、`packages/skill-bill/docs/`（皆属 #686 窗口）。
