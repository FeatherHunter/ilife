# t407 复核 · 页面积木与代表页 · 独立复跑

被审对象：提交 `a1fc2364a7ff86b8b60699951c367e57023edac3`（19 文件，+5522 / −103）。
复核方式：只读源码；不信被审方自述与其在 `.scratch\t407-kit\` 下的脚本与读数；所有读数由本次复核自建现场取得，落盘在 `.scratch\t407-verify\`。
被审证据件 `docs/skills/skill-bill/t407-冻结-页面积木与代表页-证据.md`（199 行）只用于登记「它自称什么」，不作判据。

## 一、复跑环境与命令原文

| 项 | 值 |
|---|---|
| 工作目录 | `D:\ilife`（`pwd` 实测） |
| 提交 | `a1fc2364a7ff86b8b60699951c367e57023edac3`，`git log` 顺位第一（最新） |
| node | `v24.19.0` |
| 本机 shell | pwsh 5.1 起（`pwsh -NoProfile -File`） |
| 我的临时库 | `D:\ilife\.scratch\t407-verify\db`（绝对路径，`BILL_FORCE_PROD=1`） |
| 我的产物目录 | `D:\ilife\.scratch\t407-verify\out\` |
| 读数落盘 | 全部在 `D:\ilife\.scratch\t407-verify\`（见各节路径） |

命令入口（原文）：`node packages\skill-bill\dist\cli\cmd_read.js`。
库文件名由 env 推得：`SKILLS_DB_PATH` 是**目录**，库落在 `<SKILLS_DB_PATH>\biscuit_accountant.db`（`src/fetch/paths.ts` 的 `DB_FILENAME`）。

**1.1 工作树与提交一致**

```
git diff --stat HEAD -- packages/skill-bill docs/skills/skill-bill/t407-代表-记支出-采集页.html docs/skills/skill-bill/t407-代表-记支出-回执页.html
```

读数：**空**（落盘 `.scratch\t407-verify\diff-stat.txt`）。核实前 `git status` 里 `packages/skill-bill` 零改动（仓里其余在途件属别的窗口，与本票无关）。

**1.2 `git show --stat` 与证据件「影响清单」对账**

`git show --stat a1fc236` 读数 19 文件、+5522 / −103（落盘 `.scratch\t407-verify\show-stat.txt`），与证据件第 6 行「19 文件」一致。逐件对账：

| 证据件自称 | `git show --stat` 实测 | 对账 |
|---|---|---|
| 新增 7 件（`pageFrame`／`summaryRow`／`blockedSlots`／`duplicateNote`／`prefillNote`／`emptyNote`／`failureReceipt`） | 7 件全在，LF 95／63／111／86／82／37／52 | 合 |
| 改动 9 件（`collect.ts`／`receipt.ts`／`write.ts`／`copyArea.ts`／`pageIdentity.ts`／`docPage.ts`／`fetch/db.ts`／`fetch/index.ts`／测试件） | 9 件全在 | 合 |
| 3 份文档（两产物 ＋ 证据件） | 3 份全在 | 合 |
| 未碰 base／其它包／仓根／CI／老技能 | 19 文件全落在 `packages/skill-bill/src`、`packages/skill-bill/test`、`docs/skills/skill-bill` 三处 | 合 |

**1.3 编译与测试命令原文**

```
npx tsc -b packages/skill-bill
node --test "packages/skill-bill/test/*.test.mjs"
node --test packages/skill-bill/test/record-write.test.mjs
```

**1.4 真跑两条（自建配方）**

```
$env:SKILLS_DB_PATH='D:\ilife\.scratch\t407-verify\db'; $env:BILL_FORCE_PROD='1'
# 缺槽位 -> 采集页
node packages\skill-bill\dist\cli\cmd_read.js bill.record.add --params '{"kind":"expense"}' --html D:\ilife\.scratch\t407-verify\out\collect.html
# 补全槽位 -> 回执页
node packages\skill-bill\dist\cli\cmd_read.js bill.record.add --params '{"kind":"expense","category":"餐饮/外卖/午餐","amount":-12.5,"time":"2026-09-14 12:40:00","account":"支付宝","ledger":"生活","note":"午饭"}' --html D:\ilife\.scratch\t407-verify\out\receipt.html
```

两条之前先铺底一笔（种子，证据件第 1 次同款）：`{"category":"餐饮/外卖/午餐","amount":-12.5,"time":"2026-09-14 12:00:00","account":"支付宝","ledger":"生活","note":"午饭"}`。
脚本落盘 `.scratch\t407-verify\runs.ps1`，输出 `.scratch\t407-verify\runs.txt`。

**1.5 只读连接读数工具（自建）**

`.scratch\t407-verify\dbstat.mjs`——`node:sqlite` 的 `DatabaseSync(file, {readOnly:true})`，报 `SELECT COUNT(*) FROM bills` 与 `SELECT total_changes()`。与 CLI 进程分离，不复用被审方脚本。

## 二、判据复跑结果（逐条）

**2.1 类型检查**

| 次 | 命令 | 退出码 | 诊断 |
|---|---|---|---|
| 1 | `npx tsc -b packages/skill-bill` | **0** | 零输出（落盘 `.scratch\t407-verify\tsc-1.txt`） |
| 2 | `npx tsc -b packages/skill-bill --force`（变异还原后） | **0** | 零输出（落盘 `.scratch\t407-verify\tsc-force.txt`） |

与自称「退出码 0、零诊断」一致。

**2.2 全量测试连跑三次**

| 次 | 命令 | 退出码 | tests | suites | pass | fail |
|---|---|---|---|---|---|---|
| 1 | `node --test "packages/skill-bill/test/*.test.mjs"` | 0 | **76** | 15 | **76** | **0** |
| 2 | 同上 | 0 | **76** | 15 | **76** | **0** |
| 3 | 同上 | 0 | **76** | 15 | **76** | **0** |

落盘 `.scratch\t407-verify\test-full-1.txt`／`-2`／`-3`。三次同读数，可重复。

**2.3 `record-write` 单件连跑三次**

| 次 | 命令 | 退出码 | tests | suites | pass | fail |
|---|---|---|---|---|---|---|
| 1 | `node --test packages/skill-bill/test/record-write.test.mjs` | 0 | **19** | 4 | **19** | **0** |
| 2 | 同上 | 0 | **19** | 4 | **19** | **0** |
| 3 | 同上 | 0 | **19** | 4 | **19** | **0** |

落盘 `.scratch\t407-verify\test-rw-1.txt`／`-2`／`-3`。（19／76 里含本票新增 6 条；「基线 57→70→76」是沿革自述，我不追溯历史提交，只核本次 76／0。）

**2.4 真跑两条 ＋ 三条补充**

| # | 场景 | 退出码 | stdout 首 80 字符 | 产物绝对路径 | 字节 |
|---|---|---|---|---|---|
| 种子 | 铺底一笔 | 0 | `{"version":"0.1.0","skill":"bill","shape":"receipt","key":"bill.record.add","dat` | 无 `--html` | — |
| 1 | 缺槽位 → 采集页 | **0** | 同上（`data.ok=false`） | `D:\ilife\.scratch\t407-verify\out\collect.html` | **69171** |
| 2 | 补全槽位 → 回执页 | **0** | 同上（`data.ok=true`） | `D:\ilife\.scratch\t407-verify\out\receipt.html` | **67269** |
| 3 | 方向不符（`amount:35`） | **0** | 同上（`data.ok=false`） | `D:\ilife\.scratch\t407-verify\out\bad-direction.html` | **68736** |

逐条载荷（`runs.ps1` 落盘 `.scratch\t407-verify\run*.out.txt`，读取件 4 行／件）：

- 第 1 次 `message`：`缺必需槽位：category（已出采集页，补齐后重跑同一条命令）`，`data.ok=false`，`data.receipt` 缺（采集页没有回执事实）。
- 第 2 次 `message`：`已记录：餐饮/外卖/午餐 -12.50（expense）（id=2，账单回执可复制 prompt）`，`recordId=2`，`data.ok=true`。
- 第 3 次 `message`：`写库已阻断：amount（方向不符：记支出要负数，给的是 35.00）（已出采集页，改好后重跑同一条命令）`，`data.ok=false`。

**三个字节数与被审自称逐字相同**（自称 69171／67269／68736）。库落在 `D:\ilife\.scratch\t407-verify\db\biscuit_accountant.db`（真跑首轮由 CLI 自建，stderr 印 `NOTE: 记账 DB 已初始化：…`）。

**2.5 产物断言（自建判据，脚本 `.scratch\t407-verify\asserts.mjs`，读数 `.scratch\t407-verify\asserts.txt`）**

| 断言 | 采集页 69171 | 回执页 67269 | 方向不符页 68736 |
|---|---|---|---|
| `<!DOCTYPE html>` | **有** | **有** | **有** |
| `<section` | 有（5 处） | 有（5 处） | 有（5 处） |
| `data-shape` | 有（`data-shape="receipt"`） | 有 | 有 |
| 复制区（`ilife-block-copy-block`） | **有** | **有** | **有** |
| 缺项阻断条 | **有**（`⛔ 先补齐（1 项）`） | 无（不适用） | **有**（`⛔ 先补齐（1 项）`） |
| 预填标注 | **有** | 无（不适用） | **有** |
| **`bill.bill` 零命中** | **0** | **0** | **0** |
| `data-t` 里残留占位符（`_{3,}` 或 `{{`／`}}`） | **0 处** | **0 处** | **0 处** |
| `data-t` 里尖括号占位符（`<分类>` 一类） | **0 处** | **0 处** | **0 处** |

`data-t` 实际条数：两份各 **5** 位（复制 prompt／复制数据／复制日志／退出口撤销指令等），与证据件「`data-t` 5 位」一致。
页面结构计数（按 `class` 属性逐 token 精确计，脚本 `.scratch\t407-verify\classcount.mjs`）：采集页 `ilife-block-kpi-card` **5**、`ilife-copy-btn` **4**、`<select>` **3**、`<section>` **5**、置灰阻断按钮 **1**；回执页 `ilife-block-kpi-card` **8**、`<details>` **1**、`ilife-copy-btn` **4**、危险色退出口 **1**——与证据件第 119 行逐条相同。

**2.6 提交入库的产物是不是同一份真跑产物**

自称「CLI 真跑产出后整份拷入（`Copy-Item`，非重写）」。我拿自己的真跑产物与入库件做逐字节比对（脚本 `.scratch\t407-verify\bytediff.mjs`）：

| 对 | 字节（我／入库） | 差异处数 | 差异内容 |
|---|---|---|---|
| 采集页 | 69171 ／ 69171 | 2 | 只在 `actionAt`（我 `12:58:57`／入库 `12:5_:_21`，两名宽度相同故字节数相同） |
| 回执页 | 67269 ／ 67269 | 4 | 同上，两处 `actionAt`（写入明细表 ＋ 复制日志） |

落盘 `.scratch\t407-verify\bytediff-collect.txt`／`bytediff-receipt.txt`。**除运行时刻外逐字节相同**，且入库件 sha256 前 24 位＝`DB2607EE8BAECADE6A1CE62B`（采集）／`61E8BF043B39AD17263E2666`（回执），与证据件第 128／129 行逐字相同。这一条同时排掉「产物是手写或事后改过」的怀疑。

**2.7 变异探测（见第五节前的「自证」）**

自建一处判定改坏（`src/shared/blockedSlots.ts` 的 `DIRECTION` 表把 `expense`／`income` 的 `want` 对调）⇒ `tsc -b` 退出码 0、`node --test "packages/skill-bill/test/*.test.mjs"` ＝ tests **76**／pass **72**／fail **4**、退出码 **1**（**红**）。还原后 `tsc -b --force` 退出码 0、全量连跑三次均 76／76／0（**绿**）。落盘 `.scratch\t407-verify\mut-tsc.txt`／`mut-full.txt`／`test-post-1..3.txt`。

**结论：判据有鉴别力——把判定改坏，测试真红，不是恒绿。**

## 三、边界探测结果

脚本 `.scratch\t407-verify\probes.ps1`／`probes2.ps1`，原始输出 `.scratch\t407-verify\probes.txt`／`probes2.txt`。
所有行数读数来自独立只读连接（`.scratch\t407-verify\dbstat.mjs`），不复用被审方脚本。

**3.1 采集页不写库**

| 项 | 读数 |
|---|---|
| 命令 | `node packages\skill-bill\dist\cli\cmd_read.js bill.record.add --params '{"kind":"expense","amount":-77.7,"time":"2026-09-14 20:00:00"}' --html D:\ilife\.scratch\t407-verify\out\noWrite.html` |
| 退出码 | **0** |
| 载荷 | `data.ok=false`，`message=缺必需槽位：category（已出采集页，补齐后重跑同一条命令）`，`delivery.bytes=68374` |
| `bills` 行数（前→后） | **4 → 4** |
| 产物 | 含缺项阻断条（`⛔ 先补齐（1 项）`）与预填标注 |

**通过：采集页不写库。**

**3.2 方向不符（记支出给正数）**

| 项 | 读数 |
|---|---|
| 命令 | `node packages\skill-bill\dist\cli\cmd_read.js bill.record.add --params '{"kind":"expense","category":"餐饮/外卖/午餐","amount":35,"time":"2026-09-14 14:00:00"}'` |
| 退出码 | **0**（不是非 0；靠载荷说事） |
| 载荷 | `data.ok=false`，`message=写库已阻断：amount（方向不符：记支出要负数，给的是 35.00）（已出采集页，改好后重跑同一条命令）` |
| `bills` 行数（前→后） | **3 → 3**（**库里没有被写**） |
| 带 `--html` 同款 | 退出码 0、`data.ok=false`、产物 68736 字节、页上缺项阻断条 `⛔ 先补齐（1 项）`、`缺的槽位=amount`／`为什么挡住=方向不符：记支出要负数，给的是 35.00` |

**通过：方向不符阻断且不写库。**

**3.3 重复检测出现条件**

| 场景 | 命令要点 | 退出码 | `bills` 行数 | 「重复检测提示条」 | 条上记录编号 |
|---|---|---|---|---|---|
| 同日同额同分类，先写一笔 | `{"category":"出行/打车","amount":-8.8,"time":"2026-09-14 18:00:00"}` | 0 | 4 → 5 | — | — |
| 同日同额同分类，再出采集页 | `{"kind":"expense","category":"出行/打车","amount":-8.8,"time":"2026-09-14 18:30:00"}` | 0 | 5 → 6 | **出现** | `记录编号 5` |
| 同额同日、**不同分类** | `{"kind":"expense","category":"餐饮/外卖/午餐","amount":-77.7,"time":"2026-09-14 19:00:00"}` | 0 | 6 → 7 | **不出现** | — |
| 同日同额同分类、**换一天** | `{"kind":"expense","category":"出行/打车","amount":-8.8,"time":"2026-09-15 10:00:00"}` | 0 | 7 → 8 | **不出现** | — |
| 分类未给（同日同额比） | `{"kind":"expense","amount":-12.5,"time":"2026-09-14 12:30:00"}` | 0 | 1 → 1 | **出现** | `记录编号 1`（注明「按同日同额比」） |

**通过：三条判据（同日／同额／同分类）各自都有「出现／不出现」两面读数，不是只测一面。**
判定出处（`.scratch\t407-verify` 外，只读）：`packages\skill-bill\src\shared\duplicateNote.ts:55-60`——日期取前十位逐字比、金额 `toFixed(2)` 比、分类给空即跳过分类这一条。

**3.4 未知命令**

| 项 | 读数 |
|---|---|
| 命令 | `node packages\skill-bill\dist\cli\cmd_read.js bill.record.nope --params '{}'` |
| 退出码 | **3** |
| stderr 首行 | `ERR 3: 未知联动 key：bill.record.nope` |
| stdout | 空；`bills` 4 → 4 不变 |

（「未知联动 key」是 CLI 既有文案；本复核只记读数，不评该文案。）

**3.5 `--params` 坏 JSON**

| 项 | 读数 |
|---|---|
| 命令 | `node packages\skill-bill\dist\cli\cmd_read.js bill.record.add --params '{"kind":"expense"'` |
| 退出码 | **2** |
| stderr 首行 | `ERR 2: --params 须为 JSON` |
| stdout | 空；`bills` 4 → 4 不变 |

**3.6 `bill.record.update` 给不存在的 id**

| 项 | 读数 |
|---|---|
| 命令 | `node packages\skill-bill\dist\cli\cmd_read.js bill.record.update --params '{"id":99999,"note":"x"}'` |
| 退出码 | **4** |
| stderr 首行 | `[skill-bill] BILL_FORCE_PROD=1: 写入 …`（首行是守卫提示）；含 `ERR` 的那行须看第 2 行 |
| stdout | 空；`bills` 4 → 4 不变 |

**3.7 补充一条（不在指定六类里，顺手探到）**

| 项 | 读数 |
|---|---|
| 命令 | 去掉 `SKILLS_DB_PATH` 后跑 `bill.record.add --params '{"kind":"expense"}'` |
| 退出码 | **1** |
| stderr 首行 | `ERR 1: SKILLS_DB_PATH 未设置（无默认值，必设）` |
| `bills` | 4 → 4 不变 |

**3.8 我自己踩到的两处（属我方探法，不记被审方账上）**

1. 首版探法 A 把七个槽位给全了，于是**走了写库那一支**（`data.ok=true`、行数 2 → 3）——采集页根本没出，不是被审件的缺陷。改成缺 `category` 后读数才是 3.1 那张表。
2. 首版探法 C3 我写了 `交通/打车`，`交通` 不在 `EXPENSE_L1` 里（`packages\skill-bill\src\policy\category.ts:5`），CLI 以退出码 **2**／`ERR 2: 口径失败：未知 L1 分类：交通` 正确拒绝——是我的输入错，不是缺陷。改成 `餐饮/外卖/午餐` 后得到 3.3 第三行的读数。

## 四、自称与实测的差异

**4.1 逐字同读数、无差异的（先销账，免得读者以为漏核）**

| 自称 | 读数出处 | 我的实测 | 判 |
|---|---|---|---|
| `tsc -b` 退出码 0、零诊断 | 证据件第 97 行 | 0／零输出（`tsc-1.txt`） | 同 |
| tests 76／suites 15／pass 76／fail 0 | 证据件第 98 行 | 三次同读数（`test-full-1..3.txt`） | 同 |
| 采集页 69171 字节、回执页 67269 字节 | 证据件第 119、184 行 | 69171／67269 | 同 |
| 产物 sha256 前 24 位 `DB2607EE8BAECADE6A1CE62B`／`61E8BF043B39AD17263E2666` | 证据件第 128／129 行 | 逐字相同 | 同 |
| 采集页不写库（第 2／3／5 次行数不变） | 证据件第 120 行 | 库状态 4 → 4（3.1）；首轮 1 → 1；方向不符 3 → 3 | 同 |
| 方向不符阻断且不写库 | 证据件第 113、197 行 | 3 → 3，`data.ok=false`，`⛔ 先补齐（1 项）` | 同 |
| `bill.bill` 零命中 | 证据件第 43 行 | 二份产物均 **0** | 同 |
| 采集页 `bills（写库回执）` 0 次、`只读：本页不写库` 命中；回执页反之 | 证据件第 43 行 | 0／1、1／0（见本节末命令） | 同 |
| 缺项阻断条那枚按钮点不动（不给 `data-t`） | 证据件第 76 行 | 采集页与方向不符页 `data-t` 共 5 位，**其中无一条含占位符**（3 份产物各 0 处） | 同 |
| 复制 prompt 原先有标签没按钮、本票补上 | 证据件第 177 行 | 无头 Chrome 真图里点得到「复制 prompt」，两份产物各 4 枚 `ilife-copy-btn` | 同 |
| 两处口径错（双前缀、采集页来源句） | 证据件第 40／41 行 | 见上两行 | 同 |
| 影响清单 19 文件 | 证据件第 6 行 | `git show --stat` 19 文件（`show-stat.txt`） | 同 |

**4.2 差异一：变异自证 B 的读数口径与我复现的不是同一种读数（轻）**

- 自称（证据件第 140 行）：变异 B 把 `blockedBar` 里那条含占位符的写库指令补上 `actionId` ＋ `copyText`，跑 `--test-name-pattern` 两条 ⇒ `tests 2 / pass 1 / fail 1`，报红原文是「含占位符的写库指令不得可复制，却出现在 data-t：…」。
- 我的实测：改的是另一处（`blockedSlots.ts` 的 `DIRECTION` 表把 `expense`／`income` 的 `want` 对调），全量 ⇒ `tests 76 / pass 72 / fail 4`、退出码 1。
- 差异：**读数不是同一行**（它 2／1／1 是挑读两条，我 76／72／4 是全量）。数值不同属正常，**结论方向一致（都真红）**。
- 最小复现：`node --test "packages/skill-bill/test/*.test.mjs"`（改坏 `blockedSlots.ts:37-40` 的 `DIRECTION` 后）。
- 判：**不扣分**。自称那一行我没复现其数值，只复现了「改坏判定 ⇒ 测试变红」这一性质；已在 4.4 标明哪一条未独立复现。

**4.3 差异二：我自己的计数口径与被审不同（属复核方口径说明，不算被审差）**

- 证据件第 119 行称回执页 `kpi-card` 8 格。我按 `class` 里的**子串**计得 41，按 **token 精确相等**计得 **8**（`.scratch\t407-verify\classcount.mjs`）。同理 `data-table` 我按子串计得 40、按 token 计得 2（与自称 2 合）。
- 差异：**计数口径不同**，不是数值冲突。以 token 精确计数后逐条与被审相同：采集页 `kpi-card` 5、`select` 3、置灰按钮 1；回执页 `kpi-card` 8、`details` 1、危险色退出口 1、`copy-btn` 4。
- 最小复现：`node .scratch\t407-verify\classcount.mjs .scratch\t407-verify\out\receipt.html`。
- 判：**不扣分**，记在此处只为让后手复核不再踩同一个计数坑。

**4.4 差异三：一条自称我没独立复现（已在文中标明）**

- 自称：变异 B 那一行的红／绿两条读数（证据件第 140 行右两格）、以及「A／B 两次变异的原始输出在 `.scratch\t407-kit\mut-a.txt`／`mut-a2.txt`／`mut-b.txt`」（第 142 行）。
- 我的实测：我**没有**复现变异 B 与变异 A 的原始文件（按规矩不复用被审方脚本，也不读它的临时件当判据）；我自建的是变异 C（`DIRECTION` 表）。
- 差异：**未独立复现**，非冲突。
- 最小复现（若后手要核）：对 `src/shared/duplicateNote.ts:57` 把 `!== day` 改成 `=== day`，跑 `node --test packages/skill-bill/test/record-write.test.mjs`，然后 `Copy-Item` 还原并 `npx tsc -b packages/skill-bill --force`。
- 判：**不扣分**（任务允许「只核两行读数并标未独立复现」），但因为是两行读数里的一半，在前述「可复现性」维度按「复核方自建变异只覆盖一处」记一分轻扣。

**4.5 差异四：被审自己记在遗留里的半拉子，我实测确认它确实还是半拉子（不扣分，但必须点名）**

- 自称（证据件第 175 行）：`failureReceipt` 只接了一半；JSON 异常／缺 id／参数不合法仍走 stderr ＋ 非 0 退出码，整页替换是后票的事。
- 我的实测：未知命令**退出码 3**、坏 JSON **退出码 2**、不存在的 id **退出码 4**，三者 stdout 均为空（无整页产物），与我读到的 `src/record/write.ts` 只把 `blockedBar` 接在缺槽位那条分支上相符。
- 差异：**无**——自称与实测一致，且它主动把它记成了遗留而不是成果。
- 判：**不扣分**。这是它如实自报的边界，不是它隐瞒的缺口。

**4.6 差异五：行为实测与工单口径冲突、被审方漏报（记一处轻扣）**

- 工单／证据件均要求「新产物 69／67KB 对比老侧 83.7KB，判新装配是不是真把 base 样式与 helpers 内联进去了」。
- 我实测：新产物确是把 base 样式与 helpers 内联进去了——单块 `<style>` 34,219 字符，开头第一行逐字为 `/* base-paint 共享样式资产 · ilife-base · v0.1.0 */`，含 `:root` 变量 12 条、`var(--…)` 引用 176 处、`@media` 13 处、规则 313 条；内联脚本 21,908 字符；`<link href>` 与 `<script src>` 均为 **0**（无外部请求）。
- 但「老侧 83.7KB 比新侧大 14KB」这个前提本身要打折：老样页是**整页内联脚本 53,353 字符**（`.scratch\t407-verify\csscheck.json`）的另一种页，两边的 CSS／脚本量根本不是同一套账，**不能拿总字节数推断新旧装配的样式覆盖**。要判样式内联只能看 `<style>` 头那一行与规则数（上面那组读数）。
- 差异：证据件第 185 行/对账表把「产物能打开、不是空壳」记成「0 偏差」，但**没有给出任何样式内联的机器读数**（没有 `<style>` 头、没有规则数、没有外部请求数），只用一句「产物由 CLI 真跑产出后整份拷入」代过；对照工单把它列为一条要判的事，**属漏报读数**（结论对，判据缺）。
- 最小复现：`Get-Content .scratch\t407-verify\out\collect.html -Raw` 取 `<style>` 段看首行；或 `node .scratch\t407-verify\csscheck.mjs`。
- 判：**轻扣**（证据可追溯维度），结论方向无误。

**4.7 差异六：`data-shape` 在采集页上写的也是 `receipt`（轻／须后手注意）**

- 工单断言只要求「含 `data-shape`」，两份产物**都含**（合）。
- 但实测取值：采集页 `data-slot="ilife:bill:collect"`、`data-shape="receipt"`、`data-key="bill.record.add"`；回执页 `data-slot="ilife:bill:receipt"`、`data-shape="receipt"`。**采集页同时挂着「collect 槽位」和「receipt 形状」**，靠 `data-shape` 单独选页会选错。
- 自称：工单没把 `data-shape` 的取值列为判据，证据件也没就它说什么（第 144 行测试原文对采集页正是断言 `data-shape="receipt"`，说明这是**有意沿用**，不是笔误）。
- 差异：**自称与实测一致，但取值本身有歧义**；因为下游 15 张页要照同一套标记铺开，这条歧义会跟着复制。
- 最小复现：`[regex]::Matches((Get-Content .scratch\t407-verify\out\collect.html -Raw),'data-shape="[^"]*"')`。
- 判：**不扣本票的分**（工单只要求「含」），列为**须后手裁定**的观察项。

**4.8 差异七：采集页的重复检测条在「常见金额」上很容易误报（观察，不扣分）**

- 实测：分类未给时按「同日同额」比（`duplicateNote.ts:59`），于是 `-12.5` 这种常见金额在当天已有任一同额记录时就报「疑似重复」（我那次报的是「记录编号 1」）。同一页同时出「重复检测提示条（黄条：提示，不阻断）」，条内逐字写明「按同日同额比」，不阻断写库。
- 自称：证据件第 70、121 行都写明这条口径，没掩饰。
- 差异：**无**；但采集页正是「还没填分类」的那一步，误报率天然偏高，值得在后续 15 张页上留意。
- 最小复现：库里先有一笔同日 `-12.5`，再跑 `bill.record.add --params '{"kind":"expense","amount":-12.5,"time":"2026-09-14 12:30:00"}' --html out.html`。
- 判：**不扣分**（工单没要求这一条，且页上有明文依据）。

## 五、评分与结论

**5.1 产物真实性（无头 Chrome 真图）**

把三份产物各自放进无头 Chrome 整页截图，真图上我看到的块名逐条记下（截图落在 `.dsh-vision-router\artifacts\`）：

| 产物 | 页面高度 | 真图上看到的块 |
|---|---|---|
| `collect.html` 69171 | 2081 px | 页头（`记账 · 写入域` ＋ 标题「补齐槽位」＋ 副标题）→ 类型徽章（`记支出 · 支出（金额取负数） · bill.record.add · 补槽位 · 未写库（已阻断）`，红档）→ 结论摘要行五格（金额 -12.50／分类 未给／账户 未给／账本 未给／时间 2026-09-14 12:30:00）＋ 一行口径 → 一句「写库：未发生」→ **重复检测提示条**（黄条，`记录编号 1 · 2026-09-14 12:00:00 · 餐饮/外卖/午餐 · -12.50 · 账户 支付宝`）→ **预填标注**（账户 支付宝／账本 生活／币种 人民币，来源「来自记录编号 1（最近一笔）」）→ **缺项阻断条（写库已阻断）** 红回执 ＋ 明示表（`category｜分类｜没给`）＋ 不可复制的写库指令（`--params '{"kind": "expense", "amount":-12.5, "time":…, "category":"<分类>"}'`）＋ 置灰按钮 `⛔ 先补齐（1 项）` → 采集表单（分类／账户／账本三枚选择器 ＋ 金额／时间／币种／备注四枚输入）→ **复制 prompt** 区（按钮点得到）→ 复制数据／复制日志两枚 |
| `receipt.html` 67269 | 1497 px | 页头（`记账 · 写入域` ＋「记一笔 · 回执」＋ `已记录：餐饮/外卖/午餐 -12.50（expense）（id=2，账单回执可复制 prompt）`）→ 类型徽章（`… · 写库成功`，绿档）→ **八格 KPI 卡**（金额／分类／账户／账本／时间／状态／影响行数 1 行／写入字段 7 项）→ 重复检测提示条（`记录编号 1 … 账户 支付宝（同账户）`）→ 本次写入的字段与值表（id／category／amount／time／account／ledger／currency／note 八行）→ **对账信息折叠区**（`<details>`）→ **退出口**（红色 `撤销这一笔（危险色出口）` ＋提示「撤销指令挂在同区那颗『复制数据』上」）→ 复制数据／复制日志 |
| `bad-direction.html` 68736 | 1930 px | 页头标题「补齐槽位」→ 类型徽章（红档 `补槽位 · 未写库（已阻断）`）→ 摘要行（金额 **+35.00**，副标注「收入（取正数）」，与「记支出」自相矛盾，正是被拦下的那个输入）→ 预填标注（来源「来自记录编号 2」）→ **缺项阻断条**：`缺的槽位=amount／是什么=金额／为什么挡住=方向不符：记支出要负数，给的是 35.00` ＋ 置灰 `⛔ 先补齐（1 项）` |

**结论：三份都真渲染出内容，不是空壳；页面靠内联样式排版（截图里卡片有圆角、阴影、状态色），不是无样式的死文本。**

**5.2 新装配是不是真把 base 样式与 helpers 内联进去了**

| 读数 | 采集页 | 回执页 |
|---|---|---|
| `<style>` 块数 | 1 | 1 |
| `<style>` 字符数 | 34,219 | 34,219 |
| 首行 | `/* base-paint 共享样式资产 · ilife-base · v0.1.0 */` | 同 |
| `:root` 变量 | 12 条（`--fg`／`--fg2`／`--bg`／`--blue`…） | 同 |
| `var(--…)` 引用／`@media`／规则数 | 176／13／313 | 176／13／313 |
| 内联 `<script>` 字符数 | 21,908 | 21,908 |
| `<link href>`／`<script src>`（外部请求） | **0／0** | **0／0** |
| base 类名规则命中（`ilife-copy-btn`／`ilife-action-btn`／`ilife-block-kpi-card`／`ilife-block-data-table`／`ilife-status-badge`／`ilife-block-param-form`／`ilife-block-disclosure`／`ilife-block-copy-block`） | 8／3／1／4／1／1／2／2 | 同 |

落盘 `.scratch\t407-verify\csscheck.json`。**是**：base 的样式表与点击／复制 helpers 都真写进了单份文件，页面不依赖任何外部请求。
（对照见 4.6：老样页另有 53,353 字符内联脚本、CSS 只 19,627 字符，「老 83.7KB 比新 69KB 大」这条差不能用来看样式覆盖。）

**5.3 变异还原自证（三条）**

自建变异：`packages\skill-bill\src\shared\blockedSlots.ts:37-40` 的 `DIRECTION` 表把 `expense`／`income` 的 `want` 对调（判定取反）。备份 `.scratch\t407-verify\blockedSlots.ts.bak`。

| 自证 | 命令 | 读数 |
|---|---|---|
| 1 | `git diff --stat -- packages/skill-bill` | **空**（`.scratch\t407-verify\selfprove-diffstat.txt`） |
| 2 | `git status --porcelain -- packages/skill-bill` | **空**（`.scratch\t407-verify\selfprove-status.txt`） |
| 3 | 被测件 sha256 与备份对照 | 现在 `67ED14E26869E16530EFA0F817D34A860E5CA5EC5D2BCAABD2D8C98932829A92` ＝ 备份 `67ED14E…A92`，**一致** |

**踩过的坑我避开了**：还原后不是直接再跑 `tsc -b`（增量编译可能因 mtime 不重编、拿变异期的 `dist` 给你**假绿**），而是先 `npx tsc -b packages/skill-bill --force`（退出码 0），再连跑三次全量：**76／76／0 各一次**。变异期读数：`tsc` 0、tests 76／pass 72／**fail 4**、退出码 **1**（红）。
另：我那个 `--force` 会话之后又跑了一次采集页，产物 68,880 字节、与首跑 69,171 字节不相等——原因查清是**库在探测器阶段长大了**（重复检测条列出命中条目），不是还原没干净；还原后产物里 `bill.bill` 仍 0 命中、阻断条与预填标注仍在。

**5.4 评分（五维等权各 20）**

| 维度 | 分 | 判据与扣分理由 |
|---|---|---|
| 可复现性 | **19 / 20** | 全量测试三次同读数（76／15／76／0）、`record-write` 三次同读数（19／4／19／0）；两条真跑字节数与被审逐字相同（69171／67269），连产物 sha256 前 24 位都对上；入库件与我自跑件逐字节相同（只差运行时刻）。**扣 1**：两行变异读数我只自建复现了一处（`DIRECTION` 表），另一处（`duplicateNote.ts:57`）标了「未独立复现」（4.4）。 |
| 断言强度 | **19 / 20** | 判据能证伪：变异后 `fail 4`、退出码 1，不是恒绿；方向不符那条断言真拦住了写库（`bills` 3 → 3），不是「出了提示但还是写进去」；采集页「`data-t` 里 0 处占位符」这条断言核到点子上（含占位符的写库指令真拷不走）。**扣 1**：产物断言多数是字符串包含判定，易被同名文案误命中，我另建 token 精确计数才敢下结论（4.3）。 |
| 边界覆盖 | **20 / 20** | 工单列的六类**逐类都探到**：不写库（4 → 4）／方向不符（阻断且 3 → 3）／重复检测（出现、不同分类不出现、换一天不出现、分类未给按同额比四个状态）／未知命令（退出码 3）／坏 JSON（退出码 2）／不存在的 id（退出码 4）；另顺手补探「缺 `SKILLS_DB_PATH`」（退出码 1）。每条都留了命令原文、退出码与只读连接行数。 |
| 产物真实性 | **20 / 20** | 三份都过无头 Chrome 真渲染并读出块名（5.1）；369 条 base 规则级内联、外部请求 0／0（5.2）；入库件与我自跑件逐字节同（2.6），排除事后重写。 |
| 证据可追溯 | **16.5 / 20** | 自称的读数逐条可查（行号对得上，哈希前 24 位对上），我自己的 17 份读数全部落在 `.scratch\t407-verify\` 且本文逐条给了路径。**扣 2**：「产物能打开、样式已内联」这一条被审件只给结论、**没留任何机器读数**（无 `<style>` 头、无规则数、无外部请求数，见 4.6）；**扣 1.5**：`data-shape` 在采集页写成 `receipt` 的歧义（4.7）与重复检测在「分类未给」时误报偏高（4.8）两条观察，被审件都没有落成读数或待裁项，后手 15 张页会照抄。 |

**总分：94.5 / 100**

**5.5 结论：放行（附两条后手须看的观察）**

四条判据（两份产物标记齐／采集页不写库／重复检测条出现条件／缺项阻断条 ＋ 写库被拦）在我自建现场**逐条复现通过**；编译与测试三次同读数；产物经无头 Chrome 确认是真页面且内联了 base 样式与 helpers；两处口径错（双前缀、采集页来源句）实测已修好。**没有触发任何一条「一律标严重」的情形**——产物打得开、判据改坏会变红（不是恒绿）、自称与实测的核心读数逐字相同。

放行时要带上两条后手注意（都不是本票的错，但会往下传）：

1. **`data-shape` 在采集页写的也是 `receipt`**（4.7）——照 `data-shape` 单独选页会选错，`data-slot` 才是可靠的区分位。
2. **「分类未给」时重复检测按「同日同额」比**（4.8）——采集页正是分类还没填的那一步，常见金额容易报「疑似重复」；页上写明依据、也不阻断，但 15 张页铺开前建议先裁一次口径。





