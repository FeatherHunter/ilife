# t407 复核——语言形状整改验证

> 只读核验（变异探测一处临时改坏已还原）。证据件 `docs/skills/skill-bill/t407-整改-语言与形状-证据.md`（88 行）逐条复核；提交 `934a9ae`、`dd16720`、`d59c4be`、`42dc01d`、`d501331`（5 连，未 push，`f13ceed` 之下）。命令输出一律落 `.scratch/t407-verify4/` 只读末尾 25 行。

## 一、逐条核验

1. 机审真绿——成立。`node docs/skills/skill-bill/t407-v8-style-audit.mjs --dir docs/skills/skill-bill`：EXIT 0，全绿；设计位 `·`／`|` 0 处；标题写法 55 处；英文裸词 0 份；重复句 0 份；32／32 齐（`.scratch/t407-verify4/audit.txt:44,66`）。标题／眉头保留如证据所写：`packages/skill-bill/src/shared/pageShell.ts` 眉头 `记账 · 写入域`，`src/record/collectBody.ts:165`／`receiptBody.ts:109` 页标题 `唤醒词 · 补齐槽位／回执`；抽 `t407-代表-记支出-采集页.html:1788` 眉头与标题一致。旁证 `同分类近邻 · 金额与本笔不符` 4 处在表格单元格（非版式位），与证据一致。
2. 映射单一定义地——成立。对外 5 件全在 `packages/skill-bill/src/shared/userWording.ts`：`statusNoteOf:84`／`wakeWordOf:91`／`fieldLabelOf:97`／`badgeTextOf:111`／`nextStepOf:122`，`FIELD_LABELS:28`／`WAKE_WORDS:47`／`STATUS_NOTES` 只此一处定义。抽 4 份产物（代表采集／回执、退款采集、撤销回执）grep：`bill.record.*` 残留只在可重跑位（`pre` 只读块／`data-t` 复制 prompt／日志 `bill.record.add（receipt）`），徽章与正文无；`category/deleted_at/expense/refund/restore` 去载荷预块后可见残留 0（`category` 仅 `name="category"` 表单属性，`restore` 仅 `data-action-id="ilife-exit-restore"` 属性，见 `.scratch/t407-verify4/residue-out.txt`）。`data-t` 复制载荷里 32 产物内 `【bill` 0 处、`记账数据` 32 处（全 `dir` 另有 2 处在 `t407-对照乙-记退款-新仓基础全面开发.html:1743,1752`，非 32 名单，见第三节）。
3. 徽章无 `·` 串——成立。`packages/skill-bill/src/shared/typeBadge.ts:59-67` 四枚独立形状（`renderChips` 唤醒词＋口径／`renderStatusBadge` 状态／`renderCaliberLine` 下一步），代码 `' · '` 拼接 0 处（残留 `·` 仅注释引改前旧句 `:9`）。抽 4 份产物徽章区（例 `t407-代表-记支出-采集页.html:1788`）：`<span class="ilife-block-chip">记支出　支出 金额取负数</span><span class="ilife-status-badge …">还没写库</span><p class="ilife-block-caliber">…`，三枚独立，无 `·` 拼接。测试钉 `packages/skill-bill/test/user-wording.test.mjs:100-121`（形状三枚＋徽章内无 `·`＋命令名不上屏）。
4. 疑似错字 `退出囗`——成立（同意证据“无需改”）。`packages/skill-bill/src/shared/**`＋`src/record/**` grep `囗` 0 命中；32 产物 grep `囗` 0 命中；`退出` 命中均为 `退出口／退出码` 之 `口`。
5. 五条裁定——成立。① 命令去屏：`typeBadge.ts` 无 `input.key` 引用（仅注释 `:9,:16` 交代去掉），测试钉 `user-wording.test.mjs:119-121,131`。② 复制载荷：`copyArea.ts:103-121`（`formatsOf`，`title` 缺省 `记账数据`，`text` 显式 `title`，`json/csv` 不走头行），32 产物复量见第 2 条。③ 金额 0：`summaryRow.ts:44-58`（`money2(0)→'未给'`，`moneyDirection(0)→'还没给'`），`slots.ts` 金额 0 视同缺项（grep 命中 `missingSlots` 相关行），值栏抽查 `t407-代表-记支出-采集页.html:1788` 金额缺项写 `未给`。④ 页头统一：`receiptBody.ts:38-59`（`PAGE_WAKE_WORDS`＋`wakeWordOf` 同源，型认不出按操作）与 `collectBody.ts:98`（`wakeWordOf`）同源；`add` 恒写记一笔（`WAKE_WORDS plain→记一笔`）。⑤ 退款不动：`summaryRow.ts:34-37`（`DIRECTION` 仅 `expense/income` 两条），`refund` 不判方向。
6. 真 390——成立。390 宽 iframe 当视口（外层 1200，`iframe width=390`，未用 `--window-size=390`；脚本 `.scratch/t407-verify4/iframe-measure.mjs` 经 CDP `Emulation.setDeviceMetricsOverride 1200×1400`）：`t407-代表-记支出-采集页.html` ｛vw 390，doc 390，overflow 0｝；`t407-页-记退款-采集页.html` ｛vw 390，doc 390，overflow 0｝（`.scratch/t407-verify4/iframe-out.txt`）。细查各 2 处 `CAPTION.ilife-block-data-table-caption L17 R393`（超 3px）均落在 `DIV.ilife-block` 横滚祖先内，`bodySW` 390，页级超宽元素 0。
7. 提交范围——成立。`git show --stat` 5 笔：`934a9ae` 19 件、`dd16720` 20 件、`d59c4be` 10 件、`42dc01d` 12 件、`d501331` 13 件；五笔并集非 `packages/skill-bill/*` 且非 `docs/skills/skill-bill/t407*` 者 0（`.scratch/t407-verify4/outliers.txt` 空）。只含 skill-bill 包内与本任务 t407 产物＋证据件，无别窗文件卷入。
8. 读数——成立（两次一致）。`npx tsc -b packages/skill-bill --force` EXIT 0（空输出，`.scratch/t407-verify4/tsc1.txt`，还原后重编亦 0）。`node --test "packages/skill-bill/test/*.test.mjs"` 连跑两次均为 tests 127、pass 127、fail 0（`.scratch/t407-verify4/test1.txt`、`test2.txt`，还原后 `test-after.txt` 同）。

变异探测 A——必红成立、落点部分成立（见第三节）。把 `userWording.ts:30 category:'分类'→'category'`：`user-wording.test.mjs` 单跑 11／11 绿（不断言 `category`）；全包 `node --test` 127 例中 126 过 1 红（`record-write.test.mjs:282`，`actual:'缺必需槽位：category、金额…'` ≠ `expected:'缺必需槽位：分类、金额…'`，`.scratch/t407-verify4/mut-full.txt`）。还原后绿：全包 127／127，`--force` 重编 tsc 0，机审 EXIT 0。三自证：`git diff --stat -- packages/skill-bill` 空（0 字节）；`git status --porcelain -- packages/skill-bill` 空（0 字节）；`sha256(userWording.ts)` 改前＝备份＝还原后 `2F8141861AE935DB2FC247693D9551AC37597FAD5223A406A337063DE3A741CB`，与证据件 `2f8141…3a741cb` 前后缀一致。B（徽章退回 `·` 串）本窗未复演。

## 二、读数

- 机审：EXIT 0；设计位 0；标题写法 55；英文裸词 0 份；重复句 0 份；32／32 齐（`audit.txt:44,66`）。
- 对外映射：5（`userWording.ts:84,91,97,111,122`）；覆盖 6 类落点逐条见一、5。
- 载荷：32 内 `【bill` 0、`记账数据` 32；`0.00` 子串 32 处皆正常金额（如 `200.00`），裸 `0.00` 仅 1 处合计行（见第三节）；`退出囗` 0。
- 真 390（iframe 390）：两份产物均 doc 390 ＝ vw 390，页级超宽 0。
- tsc：`--force` EXIT 0（改前、还原后各 1 次）。
- 测试：127／0 连跑两次一致；变异态全包 126／1；还原后 127／0。
- 提交：5 连 `934a9ae dd16720 d59c4be 42dc01d d501331`，范围外文件 0，未 push。
- 待裁定 5 条：如实列证据件第七节，未硬编（`op undo/restore` 字面值、重跑命令原文与 JSON 键、日志 `scene` 段与库文件名、格式名 `text/json/csv`、单元格数据标点均保留现状）。

## 三、不成立或部分成立

1. 变异 A 落点部分成立。证据件五、59 写“`user-wording.test.mjs` TEST:1（`actual:'category' expected:'分类'`）必红”；实测该件单跑 11／11 绿，红的是全包 `record-write.test.mjs:282`（126／1）。“改坏必红”成立，“在哪一档红”不成立。建议：证据订正落点，并在 `user-wording.test.mjs` 给 `fieldLabelOf('category')==='分类'` 补一条直断言，否则映射单测护不住这一格。
2. `0.00` 读数部分成立。证据件五、57 写“静态复查 `0.00` 0”；实测 `0.00` 子串 32 产物内 32 处（均为正常金额 `200.00／5000.00` 等的子串），裸 `0.00` 1 处在 `t407-页-批量录入-采集页.html` 合计行“合计 0.00（解析不出数的格没算进合计）”，非金额值栏。值栏裁定 3 本身成立（缺项写 `未给`），只是证据把“值栏无裸零”写成了“全文无子串”，建议改口径为“值栏裸 `0.00` 0”。
3. `【bill` 口径部分成立。证据件“`【bill` 0 处”限定 32 产物成立；全 `dir` 另有 2 处在 `t407-对照乙-记退款-新仓基础全面开发.html:1743,1752`（对照文件，非 32 名单，机审亦不计入）。建议证据加一句“32 名单外对照文件 2 处未动”免歧义。
4. 无完全不成立项。另注：核验中途曾见工作区 `category:'category'` 与 HEAD（`'分类'`）不一致 1 行，复查时已为 `'分类'` 且三自证全空、多窗在途 100＋件，判为并发窗口瞬态，当前已干净。

## 四、结论

- 结论：须小整改后放行（代码根因成立，证据三处写法订正＋`user-wording` 补 `category` 直断言；补完即放行）。
- W2（桌面版式＋收尾）可以开工（本窗未动版式代码，真 390 页级无溢出，W2 不依赖证据文字）。
