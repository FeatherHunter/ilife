# t728 差异表：写入域 32 页视觉整改

逐条记「改了哪一层哪一件／为什么／谁批的」。**谁批的**一栏只写两种出处：票面「裁定」条目（D1～D5，诊断席自决 ＋ 对抗式审查）或用户 2026-09-19 追加的验收要求第 1～6 条。

改动面（`git diff --stat`，40 件源码 ＋ 7 件测试／账本）：**373 插入 ／ 141 删除**。

---

## 一、公共层 `packages/base-render`（连带 6 个技能包的页面外观）

| 件 | 改了什么 | 为什么 | 谁批的 |
|---|---|---|---|
| `src/blocks.ts` · `renderPageShell` ／ 新增 `eyebrowHtml`／`splitSegments`／`eyebrowSegmentClass` | 眉标带并列分隔符时**拆段出 `<span>`**（首段品牌位、其余域位），分隔交版式 | 眉标 `记账 · 写入域` 那 32 处 `·` 是可见文本里的并列符——用户逐字要求 5 点名的「拿符号简化 UI」。拆段之后字符不进产物，段间那条细竖线由 CSS 出（与口径行 `renderCaliberLine` **同一机制、同一处定义**） | D3「分隔符判据换用公共层已有的门」＋用户要求 5 |
| `src/blocks.ts` · `PARALLEL_SEP` 常量 | 把「公共层唯一并列分隔符」提到 B-01 区开头，`CALIBER_SEP` 改为别名 | 眉标与口径行要认同一个分隔符；两处各写一个 `｜` 字面量就是同一口径的第二份定义 | D3「一条口径只许一处实现」 |
| `src/blocks.ts` · `renderChipRow`（新增） | 徽章行的容器件：`renderChips` 那串裸 `<span>` 收进一个 `<div>`，可选 `tailHtml` 透传状态徽章 | 两个理由：① `pageUi` 在 ≥1001px 把正文变成栅格，**裸 `<span>` 也是栅格项**，一行徽章会被摊成一行一枚；② 「并列标签要对齐成一行」这件事必须有载体才谈得上间距与换行 | D1 ①（零连带档：形状件补齐） |
| `src/blocks.ts` · `pageShell` 区 CSS | ① 眉标改面包屑（品牌位 `--fg2`／域位 `--blue2`／段间 hairline）；② 页头三级外边距 `6px` → `8px`；③ 副题加一条 `border-bottom`，无副题的页由标题收尾（`:has` 二选一）；④ 徽章行 `gap` 与 30px 行盒；⑤ `.block-chip-row` 系列 | ① 改前眉标与正文徽章**同规格**（12px／600／`--blue2`），「这是页头」与「这是页里一枚标签」分不出来（诊断 R3）；② `6px` 破 4px 间距网格（诊断 R4）；③ 长表单页看不出页头在哪结束；④ 视觉第 1 轮实测：胶囊（2px 8px 内距）与状态徽章（6px 14px 内距）**文字基线不齐**；⑤ 徽章行需要一个块级载体 | D1 ②（本包六域连带：页头层级／眉标与徽章规格分开）＋用户要求 3 |
| `src/pageUi.ts` · ⑪ 桌面档（新增） | ≥1001px 参数表单两列并排，说明句与预览位跨两列 | 单列表单在 390 档每格约 350px、到 1280 档被拉到版心宽 880px，读到的是「一列长条」而不是「一张要填的表」（诊断 R5） | D1 ② ＋ 用户要求 1 |
| `src/pageUi.ts` · ⑧ 段追加一条 | ≥1001px 页头三级也收进 880 正文列并居中 | ⑧ 只收正文那层，页头是它的兄弟节点；不写这条则标题贴版心最左、正文居中（实测标题 x≈26、正文 x≈202） | D1 ② ＋ 用户要求 1 |

**未动**：`--blue:#007aff`（判据 3）；`STYLE_FORBIDDEN_TOKENS = ['--r-xl','--pink']` 与深色区禁入；`BLOCK_STYLE_SECTIONS` 那 12 项闭集。

---

## 二、本包页装配 `packages/skill-bill/src/shared`

| 件 | 改了什么 | 为什么 | 谁批的 |
|---|---|---|---|
| `shared/docPage.ts` | ① 打开公共层页面级配方 `pageUi: true` ＋ 把 `pageUiCss()` 拼进 `extraCss`；② `DESKTOP_CSS` 从 4 条收窄到 1 条（撤掉 `max-width:1120px` 与卡片网格两条，只留表格宽度收口）；③ `TOAST_CSS` 的 `10px` 圆角收到闭集里的 `999px`；④ `KV_CSS` 的字面色值 `rgba(210,210,215,.6)` 换成 token `var(--line)` | ① 手机端配方是一枚 opt-in 开关，本包没传 ⇒ `viewport-fit=cover` 与 `ilife-page-ui` 命中 0（诊断 R1，且是已登记欠账）；② 改前桌面端**只有一条宽度补丁、没有版式**（诊断 R5）；③④ 判据 1 改「样式随它的选择器」后要求「零字面色值 ＋ 圆角取闭集」 | D1 ①／D2（抄能力不抄资产）／D3 判据 1 新口径 |
| `shared/pageIdentity.ts` · 新增 `docTitleOf` | 文档标题的**唯一拼法** `<这一页是什么> - <技能名>`；`DOC_TITLE` 注释同步改 | 32 页的 `<title>` 是 `饼干记账·X`（`·` 是并列符，t508 门判红）。文档标题里的「页名 ＋ 站点名」不是并列内容，它是标题写法本身 | D3「文档标题 → 按标题写法单列」＋用户要求 5 |
| `shared/sourceLine.ts` | 来源脚注从「先 `join(' · ')` 拼成一整串」改成**三段并列**交给口径行拆段；窗口起止同值时只说一次；没窗口时改说「时间不限」（改前逐页印 `— → —`） | 改前先拼串再交给口径行，而口径行只认并列分隔符 ⇒ 版式永不激活、那行 `·` 串原样上屏（诊断 R2，32 处）；空窗口印 `— → —` 是实打实的冗余 | D3 三类处置的第一类「真并列 → 出形状」＋用户要求 4／5 |
| `shared/copyArea.ts` · `EXIT_NOTE` | 两句之间 `；` 改句号 | 分号在这里不是并列，是把两件事挤进一句的排版手法 | 用户要求 5 |

---

## 三、写入域 `packages/skill-bill/src/write`

| 件 | 改了什么 | 为什么 | 谁批的 |
|---|---|---|---|
| `typeBadge.ts` | ① 徽章行第一枚从**唤醒词**改成**页型**（`pageKind` 新入参，空串不出）；② 两枚徽章与状态徽章收进 `renderChipRow` | H1 恒写着唤醒词，徽章行再抄一遍就是同一件事印两遍（实测采集页 5 次、回执页 6 次，诊断 R6） | D1 ①「H1 与徽章去重」＋用户要求 4 |
| `pageParts.ts` | `EYEBROW = '记账 · 写入域'` → `'记账｜写入域'`（其余五个域同改，共 6 件） | 分隔符归公共层；域侧只说「这几段是并列的」 | D3 ＋用户要求 5 |
| `collectFrame.ts` | ① `collectProgress` 只说「补上会怎样」，不再复述唤醒词与项数；② 缺项标签改走 `renderChipRow`；③ 去掉已不用的 `wakeWordOfKind` 依赖 | 副标题与进度行改前说同一件事（缺 2 项 × 2），进度行还把 H1 的唤醒词抄第二遍 | 用户要求 4 |
| `blockedSlots.ts` | ① 错误标题从 `还缺 3 项，补齐再记：分类、金额、向谁借`（句尾顿号串）改成 `还缺 3 项，补齐再记`——缺哪几项由紧下面那张表与页顶的缺项徽章承担；② 口径行 `；` 改句号 | 用户要求 5 逐字点名的写法（≥3 件并列塞进一句）+ 要求 4 | D3 第一类 ＋用户要求 4／5 |
| `recentPicks.ts` · 新增 `optionLabelOf` | 三级分类路径的**显示写法**：`借贷/借出` → `借贷 › 借出`（层级字形）；`optionsFor` 据此落 `{value,label}` 对象项 | 库里存的是层级值（事实，一字不改），但原样上屏就是把「这是三层」交给一个斜线排版 | D3「层级值 → 换成层级字形或由版式出分隔」＋用户要求 5 |
| `candidatePick.ts` | 候选行摘要过 `optionLabelOf` | 同上（那一行同样印着 `借贷/借出`） | 同上 |
| `summaryRow.ts` | KPI 卡「分类」值过 `optionLabelOf` | 同上 | 同上 |
| 5 张模板 `template-{expense,flow,batch,installment,update}.ts` | ① `docTitle` 改成派生（`docTitleOf(spec.word + ' 采集页'／' 回执'／' 补齐槽位')`），`docTitle` 字段从 3 个 spec 接口与 11 个场景件删掉；② 回执页 H1 只写唤醒词（撤 ` · 回执`），页型落徽章；③ 采集页补 `pageKind`；④ 回执读数卡「写进去的项」说明从 `分类、金额、…`（7 段顿号串）改成「逐项见下面的明细表」 | ① 唤醒词的字面量不许在非声明件里再写一遍（`test/t721-判据与摘要锁.test.mjs` 钉着这条）；② H1 与徽章去重；④ 卡片值位已写「7 项」，说明位再列一遍 7 个名字是同一件事印两遍 | 用户要求 4／5 ＋ D3 第一类 |
| 11 个 `scene-*.ts` | 删 `docTitle:` 一行 | 同上（唤醒词字面量） | 同上 |
| `slots.ts`／`scene-photo.ts`／`outsideScan.ts` | ① `自由文本；…`／`不填＝改字段；…` 等 4 处 `；` 改句号；② 分类提示从 `三级分类，如 餐饮/外卖/午餐。要选到最细那一级` 改成 `要选到最细那一级，如「午餐」` | ① 用户要求 5；② 视觉第 1 轮实测：原提示比控件宽，390 档被截成「…选到「午  ▾」 | 用户要求 3／5 |
| `duplicateNote.ts`／`installmentPreview.ts`／`scene-reimburse.ts`／`outsideScan.ts`／`photoEscape.ts`／`userWording.ts` | 各 1～2 处 `；` 改句号、`、` 串改人话 | 同上 | 用户要求 5 |
| `template-batch.ts` | 副标题与回执卡说明去冗（`缺 N 项，详见下表。` → `缺的就是下面这几项。`；`共 N 项，详见下表。` → `逐项见下面的明细表`） | 副标题与进度行、卡值位与卡说明位各说两遍 | 用户要求 4 |

---

## 四、测试与账本（判据变了，守卫必须跟着落到新形状上）

| 件 | 改了什么 | 为什么 |
|---|---|---|
| `test/t410-lock.test.mjs` | 4 处 H1 断言从 `'X · 回执'` 改成 `'>X</h1>'` ＋ `'>回执</span>'` 两条 | 锁的是「路由到哪一页」，不是某一版的标题写法；新断言同时钉 H1 与页型徽章，**强度不降** |
| `test/desktop-copy.test.mjs` | D1 那组断言换成新口径：配方接线两处证据（`viewport-fit=cover`／`ilife-page-ui`）、宽屏正文列 880、表单两列、**外加一条反向断言**「1120 那一条不许回来」 | 旧断言钉的正是诊断 R5 那条「只有宽度补丁」 |
| `test/user-wording.test.mjs` | 形状组断言改「页型胶囊 ＋ 口径胶囊 ＋ 状态徽章 ＋ 下一步动作」；`·` 那一条改成按**承载文本的标签**取（容器不是文本） | 同上；顺带把「唤醒词不许再进徽章行」写成负向断言 |
| `test/record-write.test.mjs`／`t730-目标域.test.mjs`／`shared-judgments.test.mjs` | 三处钉旧写法的断言改成新写法（H1 形状／来源脚注三段并列＋反向断言／层级字形＋反向断言） | 同上 |
| `test/t689-页面指纹.json` | 按 `--write --declare-layout-change 728` 重录 32 页 | 判据 6 |

---

## 五、没有改的（逐条说清为什么不改）

| 件 | 为什么不改 |
|---|---|
| `packages/base-render/assets/help-template.html` 与其生成器 | 本票只把 HELP 页的**移动端能力**落在公共层配方上（裁定 D2）；那份 109,884 字节的烤制资产是另一张票 |
| 复制载荷与页面事实 | 「不在本票内」段：页面事实与复制载荷文本不变。目录业务值一律不动（分类路径的 `value` 仍是 `借贷/借出`，只改给人看的标签） |
| 其它 5 个技能包的页面 | 本票只担保「不倒退」（判据 10），读数见证据件 |
| 本包机审 `t407-v8-style-audit.mjs` 的分隔符位置白名单 | 判据 9 已把那一列并到公共层 t508 门；留第二份定义就是同一口径两处实现。**本票不再往那份白名单里加 `caliber`**（那是判据 5 的旧口径，已被 9 取代） |
