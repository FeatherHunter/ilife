# 11 记报销 采集页 · 逐页视觉审计

- 席位：11／32（写入域 32 页视觉整改）
- 产物：`D:\ilife\.scratch\t410-wall\t410-页-记报销-采集页.html`（2817 行；正文块序 `content[]` 住 `packages/skill-bill/src/write/template-expense.ts:161-215`，差异声明住 `src/write/scene-reimburse.ts`）
- 本阶段只出本报告：**没改任何源码、产物、测试**

---

## 1 两端截图（都真的看过，看细节用 vision_crop 放大）

| 用途 | 路径 | 尺寸 |
|---|---|---|
| 手机首屏 | `D:\ilife\.dsh-vision-router\artifacts\D--ilife-.scratch-t410-wall-t410-57acc759-shot-390x844.png` | 390×844 |
| 桌面首屏 | `D:\ilife\.dsh-vision-router\artifacts\D--ilife-.scratch-t410-wall-t410-57acc759-shot-1280x900.png` | 1280×900 |
| 手机全页（折叠态＝交付态） | `D:\ilife\.dsh-vision-router\artifacts\D--ilife-.scratch-t410-wall-t410-57acc759-shot-390x900-fullpage.png` | 390×1957 |
| 手机全页（展开态） | `D:\ilife\.dsh-vision-router\artifacts\D--ilife-.scratch-t410-audit-tmp-6f214827-shot-390x844-fullpage.png` | 390×2599 |
| 桌面全页（展开态） | `D:\ilife\.dsh-vision-router\artifacts\D--ilife-.scratch-t410-audit-tmp-6f214827-shot-1280x900-fullpage.png` | 1280×2170 |

字号、间距、颜色一律取自产物 CSS（`blocksCss()`／`pageUiCss()` 已内联，产物第 1102 行起），不靠猜。

产物里只有一个 `<details>`（摘要「还缺什么，以及补齐后照抄的那条」）**默认折叠**，折着的那半页也是用户看得到的一页。为了真的看到它，我把产物复制一份、只把 `<details class="ilife-block ilife-block-disclosure">` 改成 `<details open …>` 出了一张 **临时审阅副本**（原文件一字节没动、副本已在收尾时删除）。要复现，按下面这段重做一份到同一路径即可（截图存放目录名由该路径派生，逐字相同）：

```
$p='D:\ilife\.scratch\t410-wall\t410-页-记报销-采集页.html'
$d='D:\ilife\.scratch\t410-audit-tmp-11\inspect-details-open.html'
New-Item -ItemType Directory -Force -Path (Split-Path $d) | Out-Null
$t2 = (Get-Content -Raw -Encoding UTF8 $p) -replace '<details class="ilife-block ilife-block-disclosure">','<details open class="ilife-block ilife-block-disclosure">'
[System.IO.File]::WriteAllText($d, $t2, (New-Object System.Text.UTF8Encoding($false)))
```

### 两端第一眼看到的东西（逐字）

- 390 档首屏（844px）：眉标「记账 写入域」→ H1「记报销」→ 副标题「记报销还差 2 项」→ 两行胶囊（采集页／分类按三级挂靠／还没写库，下一行裸的 分类／金额）→ 蓝色空壳结论条「第 1 段 先看这一笔缺什么」→「提示」块（垫付打标）→ 四行「预填标注」表（每行三行标签）→ 折叠条「还缺什么，以及补齐后照抄的那条」→ 小标题「没有可选的历史账户」。**首屏里一个要填的格都没有**（第一个格「分类」的标签在 y≈1122）。
- 1280 档首屏（900px）：同一串，只是表格摊成真三列、空态白卡横铺 860px。首屏结尾停在空态卡中间，**首屏内 0 个输入控件**。

---

## 2 缺陷清单（每条：在哪／证据／严重度／归属）

### D1 窄屏三列表把表头逐行印满 18 次（共用底 §5 第 1 类的加重版）

- **在哪**：① 「预填标注」表（4 行 × 3 列，390 档 y≈390–660）② 折叠区「还缺什么」表（2 行 × 3 列）
- **证据**：产物 `data-label=` 共 **18** 处（表 1＝12、表 2＝6），`≤640` 档 `td::before{content:attr(data-label)}` 逐格印一遍（产物第 1618–1624 行）。390 档首行「预填的格／顶上去的值／来源」占 y≈418–489（**71px**），4 行事实吃掉 ≈280px；单是「预填的格」这四个字在一屏里印 4 次。共用底 §5 第 1 条记的是「8 行 × 2 ＝ 16 次」，本页是 **4 行 × 3 ＋ 2 行 × 3 ＝ 18 次**，同一根病、同一档位。
- **严重度**：高（维护者已判「非常的丑陋」的同一类）
- **归属**：全族共用（公共层）——规则本体在 `packages/base-render/src/blocks.ts` 的 `blocksCss()` data-table 区 `≤640` 段；本页只是受害者

### D2 「账户」那一行自己说两遍同一句话

- **在哪**：预填标注表第 1 行（账户）
- **证据**：`顶上去的值` 格＝`（留空＝落库默认）`，`来源` 格＝`缺省值（库里还没有可用的账户，留空＝落库默认）`；`留空＝落库默认` 在同一行出现 **2 次**。成因在 `src/write/prefillNote.ts`：`:91` 把空值替换成那句「（留空＝落库默认）」，`:62` 的来源句又把它接一遍。而这一格**根本没有值**——它被摆在「顶上去的值」这一列里，是名不副实的：
- **严重度**：中
- **归属**：本族共用（`packages/skill-bill/src/write/prefillNote.ts`；凡 `prefill: 'note'` 的采集页都中）

### D3 「账户」格的提示被剪掉半句，且句首是空值加悬空逗号（共用底 §5 第 3 类）

- **在哪**：表单 7 格里的「账户」input（390 档 y≈1350 标签 / y≈1385 空框内）
- **证据**：产物 `placeholder="已经替你填上 ，缺省值（库里还没有可用的账户，留空＝落库默认）"`。390 档输入框可视宽 358px − 左右内距 24px ＝ **334px**，13px 字号下这句话 ≈30 字 ⇒ 渲染到「留空＝落」就被右缘剪断（crop 0,1320,390,1400 实测最后一个可见字是「落」）。同一句开头是「已经替你填上 ，」——**值为空串**，所以 UI 上说的「已经替你填上」后面什么都没有，只剩一个悬空逗号。这句话同时又是 D1 那行「来源」的原文：用户在同一屏里读到同一句两次。
- **严重度**：高（维护者已截图取证的同一类）
- **归属**：本族共用（`prefillNote.ts` 的 `prefillHint()` 只出 placeholder 一条通路 ＋ 公共层 `renderParamForm` 的提示位）

### D4 时间／币种／账本三格的「来源」是死文本，页上永远看不见

- **在哪**：表单「时间」input（value 已填）、「币种」input（value 已填）、「账本」select
- **证据**：产物里 `已经替你填上` 共 4 处：3 处在 `placeholder`（时间／账户／币种），1 处在 `<option value="" disabled>已经替你填上 生活，来自记录编号 114（最近一笔）</option>`。前两个 input **带 value** ⇒ placeholder 规范上不渲染（实测 390 档「时间」格里只有 `2026-09-19 12:00:00`）；select 折叠时显示的是 selected 项 `生活` ⇒ 那行文字只在展开下拉时可见。而「预填标注」表的标题恰好写着「每个被顶上的格都写清来源」——**表那一侧说了四格，格这一侧三格是空的**。
- **严重度**：中
- **归属**：本族共用（`prefillHint()` 的载体只有一个 placeholder 位）

### D5 同一页对「库里有没有数据」自相矛盾

- **在哪**：页尾来源脚注（`.ilife-block-caliber`）对 预填标注表第 2、3 行
- **证据**：产物 `共 0 条` **1** 处；`来自记录编号 114（最近一笔）` **4** 处（表 2 处 ＋ 账本 option ＋ 币种 placeholder）。页脚说「共 0 条」，上面两行说「来自记录编号 114」——390 档这两个读数相距 ≈1000px、同屏不同屏都会读到。
- **严重度**：中
- **归属**：本页专有（`template-expense.ts:214` 的 `collectSourceNote(facts.time)` 是查询域的「行数」口径，被搬到采集页与预填来源并置）

### D6 「第 1 段 先看这一笔缺什么」是一条 690px 的空壳结论

- **在哪**：页头与「提示」块之间那张蓝左边框卡（390 档 y≈205–255；1280 档 y≈232–278，卡宽 878px）
- **证据**：这行由 `src/write/collectFrame.ts:36` 的 `renderConclusionBar('第 ' + no + ' 段 ' + title)` 产出——**形状是「结论条」，内容是「段号＋段名」**。1280 档实测：文字止于 x≈393，卡右缘 x≈1080 ⇒ **约 690px 空条**。产物里 `第 1 段` 只出现 **1 次**：本页 `section2`／`section3` 都是空串（`scene-reimburse.ts:47,52`），页上根本没有第 2 段 ⇒ 序数悬空，且这句话里没有任何一笔的事实。
- **严重度**：中
- **归属**：本族共用（`src/write/collectFrame.ts` 的 `collectSectionTitle`；五张模板的采集页都在用）

### D7 阻断条那枚「置灰」按钮既没置灰、又只占半行

- **在哪**：折叠区内 `.ilife-action-row` 里唯一那枚 `⛔ 先补齐（2 项）`（展开态 390 档 y≈1185；1280 档 y≈1170–1200）
- **证据**：产物 `.ilife-action-btn-ghost`（第 262–266 行：`border-color: rgba(0,122,255,.38); background: var(--card); color: var(--blue2)`）与 `.ilife-copy-btn-ghost`（第 301–305 行）**逐值相同** ⇒ 这枚「阻断」按钮与旁边真能用的「复制日志」长得一模一样；而本仓真有的置灰配方 `.ilife-copy-btn[disabled]`（第 317–324 行：`background: var(--soft); color: var(--fg3); border-color: var(--line)`）**没被用上**。`blockedSlots.ts:13` 明写这枚按钮「点了既不复制也不写库」是设计如此（不写 `data-t` 是「真阻断」三处之一），可外观没有承担这个信息。第二处：`.ilife-action-row` 是 `repeat(2, minmax(0,1fr))` 两列网格（第 220–224 行），单枚按钮只占左半——390 档 ≈157px，1280 档 **427px** 宽带一枚 90px 宽的内容。
- **严重度**：中
- **归属**：本族共用（样式住公共层 `blocks.ts` action-bar 区）＋ 产物第 1981 行那条「单钮独占整行」的补丁**只认退出口一个 id**（`:has([data-action-id="ilife-exit-undo"])`），本页这种单钮不享受

### D8 「补齐了，说一遍试试」是一枚全宽蓝色主按钮，点了什么都不会发生

- **在哪**：折叠区错误回执 `.ilife-error-actions` 里那枚（展开态 390 档 y≈905；1280 档 y≈605，宽 506px 居中）
- **证据**：产物里这个 `<button type="button" class="ilife-copy-btn ilife-copy-btn-primary ilife-copy-btn-wide">补齐了，说一遍试试</button>` **既无 `data-action-id` 也无 `data-t`**。helpers 的点击委派先找 `[data-action-id]`（找不到就 `return`），再读 `data-t`（为空也 `return`）⇒ 点它零动作。这是公共层固定渲染的：`packages/base-render/src/controls.ts:1467-1470` 把 `retryPrompt` 渲染成 `copy-btn-primary copy-btn-wide`，而 `controls.ts:42-43` 明写「该按钮**不带** `ACTION_ID_ATTR`……绑定归调用方」——本页没有调用方绑定（全族用 `errorReceipt` 的页都这样）。**页上视觉权重最重的一枚按钮是死的**，它把旁边真能用的「复制日志」「复制数据」全比下去了。
- **严重度**：高
- **归属**：全族共用（公共层 `packages/base-render/src/controls.ts` 的 `renderErrorReceipt`）——调用点在本包 `src/write/errorReceipt.ts:49`

### D9 两个档的首屏都看不到任何要填的格

- **在哪**：全页块序
- **证据**：390×844 首屏止于小标题「没有可选的历史账户」（y≈830），**第一个要填的格「分类」在 y≈1122**（crop 0,1044,390,1140 实测「分类 *」位于 y≈1122），首屏内唯一可点件是折叠条摘要（y≈782）；1280×900 首屏内**0 个输入控件**。也就是说：一页 7 格要填的采集页，把「预填标注表 ＋ 一句提示 ＋ 折叠条 ＋ 一张空态卡」排在表单之前，用户得先滚过 ≈278px（手机）／整屏（桌面）的说明才够到第一格。
- **严重度**：高（对用户第 1、3 条）
- **归属**：本页专有块序（`template-expense.ts:161-215` 这个 `content[]` 数组；记报销这一支由 `scene-reimburse.ts` 的 `prefill:'note'`／`emptyAccount`／`section1WhenBlockedOnly` 打开）

### D10 空态卡：48px 上下内距 ＋ 标题与正文复读 ＋ 两档都空

- **在哪**：「没有可选的历史账户」块（390 档 y≈852–1048（**≈196px 高**）；1280 档 y≈1176–1313（≈137px 高、宽 860px））
- **证据**：`<h2>没有可选的历史账户</h2>` 与卡内 18px 粗体「库里还没有带账户的记录，账户这一格没有候选可以挑。」说的是同一件事（「没有可选」／「没有候选可以挑」）；`.ilife-empty{padding:48px 20px}`（产物第 459–466 行）且本页**没有** `.ilife-empty-icon` 元素 ⇒ 上半 48px 是纯空白；13px 提示行用 `--fg3`(#86868b) 压在 `--card`(#fff) 上（共用底 §5 第 5 类「灰阶过浅」在本页的落点）。
- **严重度**：中
- **归属**：全族共用（`.ilife-empty` 住公共层 `blocks.ts` empty-state 区）；「标题＋正文复读」是本页专有文案（`scene-reimburse.ts:43-46`）

### D11 「还差什么／差的是分类和金额」在这一页说了 8 遍，`2 项` 可见 4 次

- **在哪**：副标题（y≈91）／第 2 行胶囊（分类 金额）／结论条「先看这一笔缺什么」／错误回执标题「❌ 还缺 2 项，补齐再记」／「还缺什么」表两行／按钮「⛔ 先补齐（2 项）」／口令正文 pre「这一笔还差 2 项：分类（没给）、金额（没给）」／表单两格的必填星
- **证据**：390 档可见「2 项」4 处——副标题 y≈91、错误标题（展开态 y≈880）、按钮（展开态 y≈1185）、pre 正文 y≈1660；产物 `还差 2 项` 3 处（2 处可见 ＋ 1 处在 `data-t`）、`还缺 2 项` 1 处、`先补齐（2 项）` 1 处。同一事实由三个件各说一遍：`collectMissingTags`（缺项胶囊）、`blockedBar`（错误回执＋表＋按钮）、采集页副标题。
- **严重度**：中
- **归属**：本族共用（`src/write/collectFrame.ts` ＋ `src/write/blockedSlots.ts` ＋ 各场景件 `subtitle`）

### D12 「还缺什么」表有一列是纯复读：`分类 | 分类 | 这一项没给`

- **在哪**：折叠区「还缺什么」表（2 行 × 3 列）
- **证据**：产物逐字节：`<td … data-label="还缺哪一项">分类</td><td … data-label="是什么">分类</td><td … data-label="为什么写不进去">这一项没给</td>`。成因：`blockedSlots.ts:124-127` 把 `slot: fieldLabelOf(i.name)` 与 `label: i.label` 都填成同一个字段中文名 ⇒ **3 列只装了 2 件事实**，而 390 档每行要为此多印一行标签。
- **严重度**：中
- **归属**：本族共用（`packages/skill-bill/src/write/blockedSlots.ts`；凡出缺项阻断条的页都中）

### D13 分类选择器与它自己的承诺相反：要三级、只给一级，还混进收入类目

- **在哪**：表单「分类」select（390 档 y≈1140）与顶部胶囊「分类按三级挂靠」、折叠区「口令原文」
- **证据**：产物 select 的首项文案是「要选到最细那一级，如「午餐」」，而 10 个 option 是 `餐饮／退款／工资／分期／借贷 › 偿还／借贷 › 收回／借贷 › 借入／借贷 › 借出／其他收入／出行`——**没有一个是二级或三级**，`午餐` 在整份产物里只出现 **1 次**（就是那句提示）、不在候选里；同一页的「口令原文」又要求 `<支出侧三级分类，如 出行/打车/机场往返>`。另外「记报销」是支出型（`scene-reimburse.ts:4`），选择器里却混着 4 个收入类目（工资／其他收入／借入／收回）。
- **严重度**：高（要用户做一件当前控件做不到的事，采到的分类必错）
- **归属**：本页专有文案（`scene-reimburse.ts:29,33` 的 `replaces.category` 与提示句）＋ 候选来源住 `src/shared/category.ts`

### 分隔符门自查（不改，只报读数）

可见文本 `；` **0**、`|` **0**、`·` **0**；`·` 1 处与 `｜` 1 处都只落在「复制日志」的 `data-t` 载荷里（`2026-09-19 14:06:48 · 版本 0.1.0`、`bill-cmd-read … ｜ 没写库（采集页）`）。可见面不违规，我不动门；载荷那两处受硬约束 4「复制载荷文本不变」限制，本阶段不动，见 §4。

---

## 3 设计方案：这一页该长成什么样

**一句话**：这一页现在是一篇「说明文」，应该改回一张**待补齐的单据**——第一屏就摆出要填的格，把「为什么这么顶」「照哪句说」压到单据背面。

### 3.1 块序（改一处，`template-expense.ts` 的 `content[]`，161–215 行）

| 位 | 现状（行号） | 目标 | 为什么更好看 |
|---|---|---|---|
| 1 | `typeBadge` 一行三枚胶囊（162） | 留 | 已收成一行（`typeBadge.ts:69`），是这一页最干净的一块 |
| 2 | `collectMissingTags` 裸胶囊行（170） | **改成缺项卡**（见 3.2） | 两行同形胶囊 → 一行状态、一块待填 |
| 3 | `collectSectionTitle 第 1 段…`（173） | 删段号；标题挪到缺项卡卡头（见 3.3） | 去掉 690px 空条与悬空序数 |
| 4 | 打标提示块（181） | 降到字段卡**之后** | 它是「这笔记什么」的说明，不是「先做什么」 |
| 5 | `prefillNote` 表（188） | **改形状 + 降到折叠条内**（见 3.4） | 来源是「证明」，不是首屏信息 |
| 6 | `collectBlockedFold`（189） | 留（折叠）＋表瘦一列（见 3.6） | 阻断内容是「为什么」，折叠正确 |
| 7 | `empties` 空态卡（190） | **降级成「账户」格下的一行内注**（见 3.5） | 少 196px 白卡 |
| 8 | **`fieldCardOf` 字段卡（192）** | **提到第 2 位（缺项卡之后）** | 首屏从「读」变成「填」——治 D9 |
| 9 | `promptCopyArea`（199） | 留，升主次（见 3.8） | |
| 10 | `copyArea`（201）／页脚（214） | 留 | |

落地：`template-expense.ts` 的 `content[]` 数组重排（一处改，五张模板的采集页跟着改），并给 `spec` 加一条 `collectOrder: 'form-first'`（本族开关），以便与兄弟页分批。

### 3.2 缺项不再用裸胶囊（`collectFrame.ts:26-30` 的 `collectMissingTags`）

- 改哪一条：把 `renderChipRow({items})` 换成**格位形状**——每枚＝字段中文名 ＋ 一个空槽轮廓（虚线框 ＋ 红色必填点）。公共层 `blocks.ts` chip-row 区加一档 `slot` 变体（选择器由公共层产出，故规则住公共层）；本包不写本地样式。
- 为什么：390 档那两枚裸的「分类」「金额」现在指代不明（与上面三枚状态胶囊同形同重），用户看不出它们是「还缺的两项」。做成格位之后，缺什么**一眼是形状**——正对用户第 5 条（该用 UI 设计的时候别用文字）。

### 3.3 「第 N 段」不再借结论条（`collectFrame.ts:33-37`）

- 改哪一条：`collectSectionTitle` 不再走 `renderConclusionBar`；段号只在 `section2`／`section3` 有内容时才印（本页只有一个段 ⇒ 不印「第 1 段」）。标题改为挂在缺项卡卡头的一句：`还缺 2 项 · 补齐就能记`——**不写 `·`**，改成两枚小胶囊（`还缺 2 项` ＋ 徽标）或直接一句「还缺 2 项，补齐就能记」。
- 为什么：1280 档那条 878px 宽、右边 690px 空白的卡片消失；页上不再出现一个指不到第 2 段的序数（用户第 4 条：文字不许冗余、不许不合理）。

### 3.4 预填标注：三列表 → 一行一格的「来源行」（`prefillNote.ts`）

- 改哪一条：`prefillNote()` 的 `renderDataTable` 换成 `renderListRows`（公共层已有 `.ilife-block-list-rows`，三列 `左 44px / 中 1fr / 右 auto`）：左＝格名（账户／账本／币种／时间），中＝值（账户那格给灰字「留空」），右＝**来源胶囊**两型：`库内 #114`（`--soft` 底）／`缺省值`（白底细线框）。空值那格从表里拿掉，并进 3.5。
- 为什么：治 D1（390 档 12 行标签 → 0）＋ D2（同一行说两遍 → 一遍）；「来源」从第三列的句尾变成两型可一眼分类的徽章，正是用户第 5 条要的「形状代替句子」。桌面档仍是四行、但信息密度提高（不再有 700px 空列）。

### 3.5 空态降级成「账户」格的内注（`template-expense.ts:151-158` ＋ `scene-reimburse.ts:43-46`）

- 改哪一条：`empties` 那一块不再独立成块；`pageParts.fieldCardOf` 的 slots 给 `account` 一格加 `hint`（复用 `marks` 已有的那个提示位），文案合成一句：`库里还没有可用的账户，留空即落默认；想选就先给一笔带账户的记录（例如支付宝）`。块级 `.ilife-empty` 只留给「整页无候选」。
- 为什么：治 D10（196px/137px 空卡 ＋ 标题复读）＋ D4（来源在表单那一侧不可见）。同一件事从 3 处（表行／空态卡／placeholder）收到 1 处，且落在它要解释的那一格下面。

### 3.6 阻断条瘦身 ＋ 真置灰 ＋ 单钮独占整行（`blockedSlots.ts:118-138`）

- 改哪一条（三个小改）：
  1. 表列 `还缺哪一项 / 是什么 / 为什么写不进去` → **两列**（`还缺哪一项 / 为什么写不进去`），删掉与第一列逐字相同的 `是什么`（治 D12）。
  2. `renderActionBar` 的那枚按钮加 `disabled`（`ButtonInput` 补一个 `disabled` 位），落到本仓既有的 `.ilife-copy-btn[disabled]` 同款置灰；它**仍然不写 `data-t`**（`blockedSlots.ts:13` 的「真阻断」不动）。
  3. 公共层 `.ilife-action-row` 的「单钮独占整行」从退出口那一个 id 推广为通用规则（`> :only-child { grid-column: 1/-1 }`），替换产物第 1981 行那条 `:has([data-action-id="ilife-exit-undo"])` 的专用补丁。
- 为什么：治 D7。阻断是**结论**，不该长得像「可点的复制按钮」；置灰是这一页唯一能一眼看懂「现在不能写」的形状。

### 3.7 错误回执那枚按钮：要么真动作，要么别做成按钮（公共层 `controls.ts:1461-1488`）

- 改哪一条：`renderErrorReceipt` 的 `retryPrompt` 只在调用方给了 `actionId` 时渲染成按钮；没给就渲染成一行提示文字（复用 `.ilife-block-feedback-block-note` 的正文类）。本页这一处二选一：把「口令原文」的 `data-t` 绑到它（那才是「照这句说一遍」的真动作），或按上面的口径降级成提示行。
- 为什么：治 D8。页上最重的蓝实心按钮必须真能动作；不能动作的那一句就回到它本来的身份——一句话。

### 3.8 复制区主次（`pageParts.promptCopyArea` ＋ `copyArea.ts`）

- 改哪一条：出口那枚升 `primary` 并独占整行；文案 `复制给助手：这一句可以直接复制` → `复制口令给助手`（后缀那句是实现自指，删）；`复制数据 ▾`／`复制日志` 保持 ghost 并排。
- 为什么：三枚同形同重时，最该点的反而不显眼（用户第 3、4 条）。出口是这一页的目的地，给它唯一的实心蓝。

### 3.9 两个档的字面收口（公共层，各一条）

- 空态（`blocks.ts` empty-state 区）：`.ilife-empty` 的 `padding:48px 20px` → ≤640 档 `28px 20px`、≥641 档 `32px 20px`；`empty-hint` 的 `--fg3` 抬到 `--fg2`（共用底 §5 第 5 类，本页 hint 正是这一档）。
- 表单：≥1001 档两列按语义配对（分类｜金额、时间｜账本、币种｜备注），`param-form-description` 跨两列不变（`pageUiCss()` ⑪ 已经做了栅格，只调配对与顺序）。
- 为什么：宽屏不空荡、窄屏不浪费（用户第 1 条）；两档都不靠「把字变小」压高度。

**这一页改完的目标长相（390 档，从上到下）**：眉标 → H1「记报销」→ 副标题「还差 2 项，补齐就能记」（不再复述唤醒词）→ 一行三枚状态胶囊 → **缺项卡**（两枚格位：分类 ○／金额 ○）→ **字段卡 7 格**（必填两格带红星；账户格下一行小注）→ 打标提示（轻）→ 折叠条「这笔是怎么顶上的」（里面是 3.4 的来源行）→ 折叠条「还缺什么」（里面是 3.6 的两列表 ＋ 置灰按钮）→ 口令 pre ＋ **一枚实心蓝的「复制口令给助手」** → `复制数据 ▾`｜`复制日志` → 页脚口径行。

---

## 4 跨界提示（哪些兄弟页会跟着变）

- **D1／D3／D4（预填标注形状与提示）**：`prefillNote.ts` 的调用点只有 `collect.ts` 一处，映射到 8 张采集页——**记报销／记支出／记收入／记一笔／批量录入／记分期／改记录**（`prefill: 'note'` 那几支）。改形状这 8 页一起变；改文案（`prefillHint`）全部一起变。
- **D7／D12（缺项阻断条）**：凡出 `blockedBar` 的采集页一起变——记支出／记收入／记一笔／批量录入／记分期／改记录／记报销，以及流程族「报销到账／记退款／记借出／记借入／记收回／记偿还／撤销／恢复」里被阻断的那几支。
- **D8（错误回执那枚死按钮）**：公共层 `renderErrorReceipt`，**全族共用**——所有用 `errorReceipt` 的页（含查询域「整页替换」那一支的后票）。
- **D6（段标题借结论条）**：五张模板（expense／flow／batch／installment／update）的采集页全中。
- **D10／3.9 空态**：公共层 `.ilife-empty`，全族所有空态页。
- **D13（分类候选）**：本页（记报销）专有文案，但同一选择器在记支出／记收入／记一笔也走同一条路；改候选要动 `src/shared/category.ts`，会连带动这三页。
- 载荷里那两个符号（`·`／`｜`，只在「复制日志」的 `data-t`）**不属于视觉面**，受硬约束 4 保护，本阶段不碰；要不要在统一改动集里改载荷排版，请编排者与维护者定。

---

## 5 不许做的（自检）

- 事实一条不少：上面每一条都只改**形状与表达**，`共 0 条`／`记录编号 114`／`2026-09-19 12:00:00`／`#待报销`／`bill.record.add` 等值一个字不动；D13 是**报缺陷**，不是在本阶段改数据。
- 不把 `；`／`·`／`|` 用回来：3.3、3.6 里出现的「两枚小胶囊」就是替掉标点排版的形状法。
- 不写页面本地样式：上面所有改动要么落公共层（`blocks.ts`／`pageUi.ts`／`controls.ts`），要么落本包件（`collectFrame.ts`／`prefillNote.ts`／`blockedSlots.ts`／`template-expense.ts`／`scene-reimburse.ts`）；新断点只用仓内既有集合（400／640／820／1001／1200）。
- 不弄回已修的四类：来源脚注的 `·` 串、眉标 `·`、H1 与徽章重复、层级值 `/`——本页现在这四处都是干净的（层级值已在 option 文案里走 `›`），改版时逐条守住。

---

## 6 打分

- `score_before`：**58／100**。理由：一页 7 格要填的采集页，两个档的首屏都够不到第一格（D9）；页上视觉权重最重的蓝主按钮是死的（D8）；提示半句被剪断（D3）；窄屏 18 行标签复读（D1）；196px 白卡空态（D10）；同一件事说 8 遍（D11）。剩下的分给「状态胶囊一行」「提示块」「桌面两列表单」这三处确实做对了的地方。
- `score_after_proposal`：**90／100**。达到 90 的前提是公共层那三条（D1 的窄屏表格形状、D8 的 retryPrompt 落法、3.6 的单钮独占整行）一并收进统一改动集；只做本页块序与文案，能到 80 上下，卡在 D1 与 D8。
