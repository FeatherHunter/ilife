# t729 · 分析域端到端搬迁 —— 证据

- 票：[analysis 域：端到端搬迁（25 场景／5 个形态族）](https://github.com/FeatherHunter/ilife/issues/729)（父图 [#681](https://github.com/FeatherHunter/ilife/issues/681)）
- 交付件：`packages/skill-bill/src/analysis/`（**40 件／5,083 LF**：25 件场景声明 ＋ 5 件形态族模板 ＋
  `scene.ts` 契约与落点表／`agg.ts` 聚合／`cards.ts` 卡形状／`pageParts.ts` 页内共件／`read.ts` 处理体／
  `commands.ts` 三条命令声明／`index.ts` 域门 ＋ 既有的 `declaration.ts`／`params.ts`／`views.ts`）
  ＋ `packages/skill-bill/test/t729-分析域.test.mjs`（靶向测试，11 例）
  ＋ `packages/skill-bill/test/helpers/bill-seed.mjs`（合成库夹具，39 条）
  ＋ `docs/skills/skill-bill/t729-探针-真出口.mjs`（真出口探针，28 条）
  ＋ `docs/skills/skill-bill/t729-差异表.md`（差异表，21 行）
  ＋ `docs/skills/skill-bill/t729-门禁运行记录.md`（加锁运行条目导出）
- 对照基准：`688-融合基准.md` §二 C4/C5/C6/C8/C9 ＋ §四 裁定 1／2／3／4／5／7／8／11／12 ＋ §五 5.2 的 ⑦ 列；
  `t687-parity-审计清单.md` §2 第 31–55 行（25 条词）／§3 第 7–9 行（三条键）／§4 第 7 行（老模板落点）。

## 一、作废与取代（口径）

- **加锁运行**：本票所有编译／测试／探针一律经 `node tooling/run-locked.mjs --ticket 729 -- <命令>`；
  声明过的运行条目导出见 `docs/skills/skill-bill/t729-门禁运行记录.md`。
- **共享工作区**：本票与 #730（目标）／#731（开始使用）**同树并发**，三个域都改编排件的三处
  （`src/cli/cmd_read.ts` 的按键分派、`src/render/envelope.ts` 的过渡表、`scripts/ratchet-frozen-686.mjs`
  的冻结值）。本票只删**本域**那三个 `case` 与三行形状，别人的行一字未碰；那三处收口读数以
  **三域都落地后**当刻实况为准（本票动的是 `test/t686-收紧守卫.test.mjs` 的 `REGISTRY_KEYS`
  与 `test/legacy-ratchet-686.test.mjs` 的样本下标两处硬编码，见判据 7）。

## 二、判据逐条读数

| 票面判据 | 读数 |
|---|---|
| **1 · 25 条唤醒词逐条真跑**（exit 0／产物落盘／字节如实／整页／读数与老侧对得上） | 探针 **28/28**（25 场景 ＋ 空窗 ＋ 空库 ＋ 命令面）；产物 73,357 ~ 85,055 B，逐页「文件字节＝`delivery.bytes`」、`delivery.path` 与 `--html` 同路径、整页（doctype＋`<section>`）。读数逐条与夹具手算值相同：看月度 2026-05 支出 **5640.20**／收入 **13759.00**／净额 **8118.80**／**14 笔**；看年度 2026 支出 15522.70／收入 66359.00；看周报 本周 585.00 vs 上周 52.00；看借贷 借出未还 2000.00（张三）／借入未还 1500.00（李四）；看报销 760.00／760.00；看分期「手机」总额 1374.00·每期 458.00·已还 3 期·剩余 9 期；看退款 499.00／1 次；看高频 L1「餐饮」10 笔 845.50 |
| **2 · 图表四条**（裁定 3 的可判形式） | 四条齐（图族另跑一份判据脚本，runId `60ea9412…`）：① **纵轴刻度**各图 4 条读数——折线 `0.00／5095.93／10191.85／15287.78`、笔数柱 `0 笔／2 笔／4 笔／6 笔`、金额柱 `0.00／144.00／288.00／432.00`；② **零值不出柱身**——分布「10 元以下」0 笔时柱身只 4 根、横轴无该档，明细里仍如实写「0 笔 · 0.0%」；③ **点数不足出说明句**——趋势窗口 1 个月时产物里 `svg` 为 0、无折线路径，图卡出说明句、口径句写「不画半截线」；④ **跨空档同色虚线 ＋ 口径句点名**——趋势 14 个月窗口（2025-06~12 夹在中间）出 2 段同色虚线桥（`dasharray 5 4`）、点只落在有记录的 8 个月上，口径句逐字点名那 7 个空月 |
| **3 · 裁定 1／2／4／5／7／8／11／12 逐条自证 ＋ grep 0 命中** | 探针对**每一页**跑共同判据：可见文本里 `bill.`／`.py`／`scripts/`／`undefined`／`NaN` **各 0 命中**（裁定 1／4）；**恰好 1 个**页内导航块（`<nav aria-label="页内导航">`）＋ 来源脚注（`数据来源…· 共 N 条`）＋ 口径说明行（裁定 2）；复制区在（裁定 5，日志第 4 段＝本次命令原文，由共用件 `copyArea` 给）；`--r-xl`／`--pink`／`[data-theme`／`prefers-color-scheme: dark` **各 0 命中**、页脚无按钮（裁定 11／12）；`check-scene-shape.mjs` 判据乙 54/54（裁定 8 的「一命令一页、块序住模板件」）。缺值口径：`money`／`textOrDash` 一律 `—`，空数组给空态句 |
| **4 · 与 T6 逐条对账** | **§2 的 25 条词**：探针命令面逐条路由 **25/25**（看月度／看年度／…／看退款 → 三条命令，与 t687 §2 第 31–55 行的键归属逐条相同）。**§3 三条键**：`bill.analysis.overview`＝`stat`、`bill.analysis.compare`／`trend`＝`analysis`（与过渡表原值逐字相同）；四处一致矩阵由「能力声明 ✗／生成物 ✗」补成 **✓**——`src/analysis/commands.ts` 有声明、`src/cli/registry.ts` 生成物纳 14 键（`GEN-CHECK PASS：命令 14 条；能力 6 个`）、分派层不再有本域 `case`（`MEASURE dispatchKeys=2`）、SKILL 速查不变。**§4 第 7 行那张模板**：老 `分析/analysis_view.html` 无落点 ⇒ 本票把它的 25 个渲染器落成 **25 件场景 ＋ 5 片页型**（片数＝t685 §2.3 的归并结果，也是 #724 §四落点清单里的「分析 5」） |
| **5 · 差异表**（四列 ＋ 行号位） | `docs/skills/skill-bill/t729-差异表.md`：**21 行**（口径类 6 行／页面类 12 行／留白 3 行），每行给「老行为（含老侧文件:行号）／新行为／为什么改／谁批的」；其中 8 行有具名出处（#688 裁定 1／2／3／4／6／8、C4／C5／C6／C8／C9／C10、#691 与 #411 的既有口径），其余 13 行为本票判定（逐条写明是版式改还是口径改） |
| **6 · 路径从零走完 ＋ 横向卡口当场定死** | 域声明（#721 落）→ 命令（`commands.ts` 三条）→ 页面（5 片页型＋25 场景）→ 测试（靶向 11 例 ＋ 探针 28 条）。**过程中的三处横向卡口当场定死并写进口径注释**：① **场景件契约**（`scene.ts`：`AnalysisScene` 按形态族分支、`SceneResult` 十一字段、五族页面类型）——25 件场景与 5 件模板都按它写；② **卡形状收进 `cards.ts`**（条卡／列表卡／小表卡／事实卡／图卡／对比双卡被两个以上的族用到，按「共用件从第二个用法里长出来」收口）；③ **窗口读法收进 `agg.ts`**（`readWindow`／`windowWithRecords`——六个场景件各写过一份同形局部件，实施中发现后当场收成一处，改完复跑探针 28/28 ＋ 靶向 11/11 仍绿）。另有两处写进 `t729-差异表.md`（看周报的对比事实并进结论句＋事实卡；看洞察不引入 AI 解读分段） |
| **7 · 接线五件同窗办** | ① `cmd_read.ts` 三个 `case` 删净（该件 455 → **280 LF**，正落在冻结值上）；② `envelope.ts` 过渡表删三行（62 → **56 LF**，`TRANSITIONAL_KEY_SHAPES` 只剩 help／link 两条）；③ `pnpm gen` 重生成注册表（`GEN-CHECK PASS：命令 14 条；能力 6 个`）；④ 棘轮冻结值同窗对齐（`legacyKeys`／`dispatchKeys`＝2、`registryKeyCount`＝14、`lineCaps` 280／56；`check-ratchet-tight.mjs` **9/9 PASS**）＋ 两处硬编码改判（`test/t686-收紧守卫.test.mjs` 的 `REGISTRY_KEYS` 提到 14 条；`test/legacy-ratchet-686.test.mjs` 的自证样本下标从写死的 3 改成按当刻清单取头一条——只剩两条未搬迁键时它会越界取到 `undefined`，三条自证会一起变成假绿）；`test/t721-域声明.test.mjs` 的 14 键硬编码由邻票同窗更新；⑤ `AGENTS.md` 台账与实况一致（`check-warning-line.mjs` **13/13 PASS**，本域新增 40 件**没有一件越过 350 LF**：最大者 `scene.ts` 265） |
| **8 · 门禁全绿 ＋ 两份件入仓** | 全量测试 **403/403 PASS**（`packages/skill-bill/test/*.test.mjs`）；`gen:check` PASS；棘轮 9/9 PASS；场景形状门 **54/54** PASS；告警线门 **13/13** PASS；靶向测试 `test/t729-分析域.test.mjs` **11/11** PASS；真出口探针 `docs/skills/skill-bill/t729-探针-真出口.mjs` **28/28** PASS（runId 见 `t729-门禁运行记录.md`） |

## 三、必报五步（结构纪律）

1. **影响清单**：`src/analysis/` 新增 37 件／改 1 件（`index.ts` 收窄门面）；`src/cli/cmd_read.ts` 删本域三个 `case`
   ＋ `src/render/envelope.ts` 删本域三行（**两处是与 #730／#731 同窗共用的编排件**，只动本域行）；
   `test/` 新增 2 件／改 2 件（棘轮与收紧守卫的硬编码）；`docs/skills/skill-bill/` 新增 4 件。
2. **结构设计**：域内一层到底（场景件按形态族分五片、模板件＝块序唯一住所、卡形状与窗口读法住域内共用件），
   域门只转出 `ANALYSIS_COMMANDS` 与 `calcCategories` 两件。
3. **写代码**：跨域引用只经对方的门（`../shared/`／`../fetch/`／`../triggers/`／`base-paint`）。
4. **超线报警**：本票新增件**无一超线**（最大 265 LF）；`src/analysis/declaration.ts`（375 LF）是 #721 起就挂号的数据件，
   台账结论「本次先不拆」不动。
5. **交付对账**：与第一步逐行对得上——实际只碰了上面四个目录；**偏差一处**：为修 `t729` 的接线，
   `test/legacy-ratchet-686.test.mjs` 与 `test/t686-收紧守卫.test.mjs` 两件不在最初清单里，
   它们是票面判据 7 点名的「两处硬编码」，属同窗必改件（已在此补记）。

## 四、跑法（可复跑）

```bash
node tooling/run-locked.mjs --ticket 729 -- node node_modules/typescript/bin/tsc -b packages/skill-bill
node tooling/run-locked.mjs --ticket 729 -- node docs/skills/skill-bill/t729-探针-真出口.mjs
node tooling/run-locked.mjs --ticket 729 -- node --test packages/skill-bill/test/t729-分析域.test.mjs
node tooling/run-locked.mjs --ticket 729 -- node --test "packages/skill-bill/test/*.test.mjs"
node packages/skill-bill/scripts/gen-cli.mjs --check
node packages/skill-bill/scripts/check-ratchet-tight.mjs
node packages/skill-bill/scripts/check-scene-shape.mjs
node packages/skill-bill/scripts/check-warning-line.mjs
```

## 五、没做的与下一手

1. **本地只读下钻**（老侧点分类看明细／点月份展开，`analysis_view.html:421-443`／`:666-692`）：照 #688 §二 C7
   **登记不实施**（结果型 HTML 是静态文件，要联动得加脚本）。
2. **视觉面**：本票只保证「信息等价」与 25 条机器判据；好不好看由用户肉眼看（#681 偏好 4，视觉不做脚本化判定）。
3. **页指纹账本（判据甲）** 仍只覆盖写入域 32 页，本域页不在其中——与 #691 同一条留白：扩账本属横向决定。
4. **单元级净化**：`readWindow`／`windowWithRecords` 已上提共用，但图族与表族里仍有几处同形局部小件
   （空档点名句、月份参数读取）；本次按「不扩大改动面」留着，下一张动本域的票若再长出第三份即收口。
