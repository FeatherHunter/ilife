# t403 查询域视觉验收墙 · 18 格（17 词＋1 空态）——终审：逐页 90 线全过

> MAP：[#403](https://github.com/FeatherHunter/ilife/issues/403)（查询域 17 词）；程序锁 [#417](https://github.com/FeatherHunter/ilife/issues/417)（t417 19/19＋全量 271/271）。
> 本件收**视觉验收**：产物 18 格真跑落盘 → 双墙＋总索引（自检正反两面）→ 机审全绿 → vision_router 双端审查＋打分
> → D1／D2 两处缺陷修掉（P1／P2，t418 钉住）→ 复量逐页 ≥90。
> **结论：过。最低格 93，均值约 94。**

## 一、Q1：17 词 HTML 完成情况

**代码层 17/17 完成**（四条命令＋两装配件＋路由）：

| 命令（`src/query/commands.ts`） | 唤醒词（5／6／5／1） | 装配件 | 页型 |
|---|---|---|---|
| `bill.record.today` | 查今天／查昨天／查某天／查最近／查账单（别名，同页） | `queryListDoc`（`list.ts`） | 结果型·列表页 |
| `bill.record.range` | 查周／查月／查区间／查分类／查账户／查账本 | `queryListDoc` | 结果型·列表页 |
| `bill.record.search` | 搜备注／查标签／查欠款／查待报销／查分期 | `queryListDoc` | 结果型·列表页 |
| `bill.record.detail` | 查账单详情 | `queryDetailDoc`（`detail.ts`） | 结果型·详情页 |

**遗漏＝0（无缺词）**。 madre 情况说明三点：① 查询域**没有过程型采集页**（`read.ts:16-17` 写明"查询缺参就是缺参，阻断即错误回执"；过程型是写入域 t407 的概念）；② 阻断分支（空查询 exit 2／空区间 exit 4／无此号 exit 4）**有意不出静态产物**，由 t417 逐条断言；③ 入仓样张此前只有 4 份（t411×2＋t412×2），本件补齐到 18 格。

## 二、Q2：产物绝对路径（18 格，种子 8 笔，2026-09-16 生成）

产物目录：`D:\ilife\.scratch\t403-query-wall\`（墙页与产物同目录，iframe 相对路径可看）。

| 序 | prompt → 唤醒词 → 命令 | HTML 绝对路径 |
|---|---|---|
| 1 | 查今天的账 → 查今天 → `bill.record.today {}` | `D:\ilife\.scratch\t403-query-wall\w01-查今天.html` |
| 2 | 查昨天花了多少钱 → 查昨天 → `bill.record.today {"date":"yesterday"}` | `D:\ilife\.scratch\t403-query-wall\w02-查昨天.html` |
| 3 | 查 2026-09-06 的账 → 查某天 → `bill.record.today {"date":"2026-09-06"}` | `D:\ilife\.scratch\t403-query-wall\w03-查某天.html` |
| 4 | 查最近 3 笔 → 查最近 → `bill.record.today {"recent":true,"limit":3}` | `D:\ilife\.scratch\t403-query-wall\w04-查最近.html` |
| 5 | 查账单 → 查账单（别名，同查今天页） → `bill.record.today {}` | `D:\ilife\.scratch\t403-query-wall\w05-查账单.html` |
| 6 | 本周的账 → 查周 → `bill.record.range {"range":"week"}` | `D:\ilife\.scratch\t403-query-wall\w06-查周.html` |
| 7 | 本月汇总 → 查月 → `bill.record.range {"range":"month"}` | `D:\ilife\.scratch\t403-query-wall\w07-查月.html` |
| 8 | 9-03 到 9-06 → 查区间 → `bill.record.range {"start":"2026-09-03","end":"2026-09-06"}` | `D:\ilife\.scratch\t403-query-wall\w08-查区间.html` |
| 9 | 餐饮花了多少 → 查分类 → `bill.record.range {"category":"餐饮"}` | `D:\ilife\.scratch\t403-query-wall\w09-查分类.html` |
| 10 | 支付宝的账 → 查账户 → `bill.record.range {"account":"支付宝"}` | `D:\ilife\.scratch\t403-query-wall\w10-查账户.html` |
| 11 | 旅行账本 → 查账本 → `bill.record.range {"ledger":"旅行"}` | `D:\ilife\.scratch\t403-query-wall\w11-查账本.html` |
| 12 | 搜备注午饭 → 搜备注 → `bill.record.search {"q":"午饭"}` | `D:\ilife\.scratch\t403-query-wall\w12-搜备注.html` |
| 13 | 查标签旅行计划 → 查标签 → `bill.record.search {"kind":"tag","tag":"旅行计划"}` | `D:\ilife\.scratch\t403-query-wall\w13-查标签.html` |
| 14 | 还欠着哪些 → 查欠款 → `bill.record.search {"kind":"debt"}` | `D:\ilife\.scratch\t403-query-wall\w14-查欠款.html` |
| 15 | 待报销 → 查待报销 → `bill.record.search {"kind":"reimburse"}` | `D:\ilife\.scratch\t403-query-wall\w15-查待报销.html` |
| 16 | 分期 → 查分期 → `bill.record.search {"kind":"installment"}` | `D:\ilife\.scratch\t403-query-wall\w16-查分期.html` |
| 17 | 编号 1 → 查账单详情 → `bill.record.detail {"id":1}`（id 由搜备注取） | `D:\ilife\.scratch\t403-query-wall\w17-查账单详情.html` |
| 18 | 附带空态 → 查某天（2020-01-01 零行） | `D:\ilife\.scratch\t403-query-wall\w00-空态-查某天无记录.html` |

复跑：`node .scratch/t403-query-wall/gen.mjs`（DB 落系统临时目录，HTML 落产物目录；周月窗用相对锚点天然命中，区间用 09-03~09-06 固定窗）。

## 三、Q3＋Q4：链路总览与双墙（可点即开）

- 链路总览（prompt→唤醒词→命令→绝对路径，18 行，绝对路径列 file:// 可点）：
  `D:\ilife\docs\skills\skill-bill\t403-链路总览.html`（同内容另有一份在产物目录随墙走）。
- 手机墙 390×3列：`D:\ilife\.scratch\t403-query-wall\手机墙-390.html`；桌面墙 1280×1列整体缩：
  `D:\ilife\.scratch\t403-query-wall\桌面墙-1280.html`；总索引：
  `D:\ilife\.scratch\t403-query-wall\总索引.html`（含"有意不出"一节）。
- 生成器（入仓，可重出）：`D:\ilife\docs\skills\skill-bill\t403-验收墙.mjs`；
  清单：`D:\ilife\docs\skills\skill-bill\t403-manifest.json`。
- 自检：正例 `18格；链接94条；缺失0 -> 可发`（exit 0）；反例改清单首行为不存在名 → `exit 1 且点名`；改回重跑回绿。

## 四、机审读数（t403-v8-style-audit，18 格批量，exit 0 全绿）

- 分隔符懒政 0 处；英文裸词 0 行；重复句 0 句；清单缺件 0。
- `·` 仅标题写法位 18 处（眉标"记账·查询域"，允许保留）；`；`／`;` 0 处。
- 本页样式块 0 份、内联 style 0 份（外观全来自共享样式表）；共享样式表 18/18 逐字节同一份；
  共享层 820／640 断点、44px、flex-wrap、auto-fit 塌列、窄屏内距全有。
- 判据脚本：`.scratch/t403-query-wall/t403-v8-style-audit.mjs`（照 t407 同名件改名单，逻辑逐字同）。

## 五、vision_router 审查（7 截图人眼＋4 模型打分）——终审复量

截图：`.dsh-vision-router/artifacts/` 下 `*t403-query-wal*shot-*.png`（修前 5 张＋修后 4 张：w01 桌面、w17 手机／桌面、w07 桌面／手机）。
模型第二意见（修后 4 次）：桌面列表"no major defects, well-balanced"；手机详情"10-row key-value list, no overflow, no repeated labels"。
模型误读 4 处（以源码／像素为准，已剔除）："账本"→版本、"午饭"→午餐、"共 2 笔"→并 2 笔、详情评语未给分只给描述——结论以人眼像素为准，模型只当提示器（仓规 §7 同口径）。

### 缺陷清单（逐格，2 项，均已修）

- **D1 · 桌面表窄（修前 18/18 格）**：数据表卡片宽约 KPI 行的 55%，右约 40% 空；密集页被迫折行。根因：`base-paint` 的 `.ilife-block-data-table` 锁 `max-width:680px`。
  修法 **P1**（已落地）：`src/shared/docPage.ts` 的 `DESKTOP_CSS` 加桌面独占规则（1200px＋表卡 `max-width:none`，手机档不动；base 包不动）。
- **D2 · 手机详情字段值转置冗余（修前 w17 手机）**：字段／值两列表在卡片模式下逐行重复表头（20 行）。根因：`renderDataTable` 的 `data-label` 恒取列头。
  修法 **P2**（已落地）：`src/query/detail.ts` 增同一数据源派生的 `<dl class="ilife-query-kv-list">`（转义同单元格五字符），显隐由共享 KV_CSS 按 640 断点切换（桌面只见表、移动只见列表、打印走表）。

### 打分（量表：层级20／留白对齐20／无符号串15／无内部话15／本端适配15／对比按钮15）

| 页 | 桌面 | 手机 | 备注 |
|---|---|---|---|
| 查今天（列表代表，余 15 列表页同形） | 95 | 94 | P1 后表与 KPI 同宽，无折行 |
| 查月（密集 8 行） | 94 | 93 | P1 后单行无折行；手机 8 行卡干净无溢出 |
| 查账单详情 | 94 | 93 | P1＋P2；手机 10 行键值零重复标签 |
| 空态 | 95 | 95 | 无缺陷 |
| 18 格 | 最低 93 | 均值约 94 | **逐页 90 线全过 → 过** |

## 六、落地记录（P1／P2 已在本窗落地，不是交接）

- 碰的目录（结构纪律第一步清单）：`packages/skill-bill/src/shared/docPage.ts`（P1 表全宽＋P2 KV 显隐，共用位两域在用，落点唯一）；
  `packages/skill-bill/src/query/detail.ts`（P2 键值列表，不新增导出）；`packages/skill-bill/test/t418-query-visual.test.mjs`（新，4 条钉住两处修法）。
- 编译：`tsc -b packages/skill-bill` 绿；测试：包内全量 **280/280**（t415／t417 原判据全绿＋t418 新 4 条；经 `run-locked --ticket 417` 排队）。
- 超线检查：docPage.ts、detail.ts 均在 350 线内，无报警。
- 产物重出：`gen.mjs` → 18 格（读数同 §二）→ 墙生成器（18 格缺失 0）→ 链路总览（18 行）；机审复跑全绿；截图复量见 §五。

## 七、备注

- 本窗开工时 `src/query/` 有他席已提交的打磨（`2323f3f`，工作区干净后才动手），无覆盖冲突。
- #417 剩用户肉眼终审（空白 session 亲手跑 17 词）：本墙 18 格＋双墙即肉眼材料，验完在 #417 评论收口再关图。
