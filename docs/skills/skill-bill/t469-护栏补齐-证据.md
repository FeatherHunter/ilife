# t469 护栏补齐证据（#402 之下；断言只钉现有行为）

- 票面：`.scratch/bill-t-469-body-r1.md`（4 条漏钉判定＋场景件 15 处小 `textOf` 三样口径）。
- 旧护栏：`packages/skill-bill/test/shared-judgments.test.mjs`（26 条，零改）。
- 新件：`packages/skill-bill/test/t469-guard-additions.test.mjs`（12 条，只加）。
- 红线执行：页面可见行为一行未改；终态 `git diff -- packages/skill-bill/src` 无输出。
- 用词：命令／页面／候选／口径；未用禁用黑话。

## 一、四条判定逐条断言（共 12 条）

1. 只读回显九行与缺省标题（`src/shared/recordPicker.ts` 的 `snapshotRows`／`snapshotTable`，2 条）：
   九行按序断言（记录编号／分类／金额／时间／账户／账本／币种／备注／撤销标记，中文列名走 `fieldLabelOf`），
   金额逐字 `-12.50`（`toFixed(2)`）、撤销两态（`没撤销过`／`已撤销（时刻）`）；
   缺省标题逐字 `这一条记录（记录编号 7　只读回显）`（全角空格），自定义标题原样用且压过缺省。
2. 候选预选与自定义提示（`pickerBlock` 的 `selectedId`／`hint`，4 条）：
   不给＝不预选（全表＋`候选未预选（不给默认选中）`＋缺省提示，不拿第一条兜底）；
   给中＝预选（`候选已认准 #N`、表收起、编号在表单值里、条数口径仍在）；
   自定义提示两态压过缺省；对不上的编号按未预选算。
3. 同刻按编号倒序（`byTimeDesc`，2 条）：
   同刻两笔（`t469same1`／`t469same2`，同 `2026-09-10 12:00:00`）编号大的在先；
   异刻仍按时间倒序（同刻规则不压时间规则）。
4. `textOf` 三样口径保留（4 条，见第二节后半）：
   乙（数字也转）／甲（只认字符串）／丙（缺省未设置）各钉现有面，
   另加甲乙对照（同一输入 `12` 在乙得 `"12"`、在甲判空：硬并必改行为的当面证据）。

## 二、机器读数

- 基线冻结（先冻后加）：构建 `7a9cb738` exit=0；全量 `d7a1a80e` exit=0，tests 138 / suites 32 / pass 138 / fail 0。
- 新旧同绿：`6f98cc57` exit=0，新 12＋旧 26＝tests 38 / pass 38 / fail 0。
- 变异 M1（金额 `toFixed(2)→(1)`）：红 `8c03e815` exit=1（12 条中 1 条红：金额两位）；绿 `cb8c8fe7` exit=0（12/12）。
- 变异 M2（预选直写 `null`，忽略传入）：红 `a13ae3e5` exit=1（12 条中 2 条红：认准与自定义已认准半句）；绿 `cb8c8fe7` exit=0（12/12）。
- 变异 M3（同刻 `b.id-a.id→a.id-b.id`）：红 `66fdee5b` exit=1（12 条中 1 条红：同刻倒序；异刻仍绿）；绿见 M4 红跑同刻 2/2 绿＋终轮 `37c936a1`。
- 变异 M4（乙口径去掉数字分支）：红 `8f4b9813` exit=1（38 条中 3 条红：旧护栏数字串＋新件乙口径＋甲乙对照）；
  绿 `37c936a1` exit=0，全量 tests 150 / suites 36 / pass 150 / fail 0。
- 全量只涨不跌：138→150（+12 通过，+4 套件，失败新增 0）。旧件零改。
- 中间 `7be2c7d3` exit=1 为并发构建污染（快照变异残留与预选还原撞车，同时段 ticket=546 持有锁），
  已还原后由 `cb8c8fe7` 覆盖，不充绿证。如实记录，不隐瞒。
- 复核：`check-gate-audit --ticket 469 --since 2026-09-15T14:44:00Z --until 2026-09-15T15:00:30Z --allow-nonzero`
  → matched=18/18 scoped=18 undeclared=0 PASS（runId=54435db3 exit=0，窗口外，不入声明）。
- 锁排队：15 秒轮询、10 分钟上限内全部取得；
  `7a9cb738` 等 105068ms（ticket=523）、`d7a1a80e` 等 60022ms（ticket=546）、
  `8c03e815` 等 15016ms（ticket=500）、`0e92920a` 等 60031ms（ticket=546）、
  `02418432` 等 30012ms（ticket=546）、`d6158be8` 等 120065ms（ticket=560）、
  `8f4b9813` 等 150081ms（ticket=560）、`cb8c8fe7` 等 15003ms（ticket=560）。全部落盘。

GATE-RUN runId=7a9cb738-09c0-4c47-a98d-b8a7807548b3 cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=d7a1a80e-33a3-4dcd-98a6-824f79b8b49e cmd=node --test packages/skill-bill/test/*.test.mjs
GATE-RUN runId=6f98cc57-d157-4a7a-a425-561f3a2765dc cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs packages/skill-bill/test/shared-judgments.test.mjs
GATE-RUN runId=8c03e815-7199-48d1-81ee-52807eca8048 cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs
GATE-RUN runId=a13ae3e5-35f0-4ab5-8979-22cb9544cbbf cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs
GATE-RUN runId=66fdee5b-e338-46f4-9117-8bf06a5d720b cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs
GATE-RUN runId=8f4b9813-53a4-40c5-85cb-a2d4f8346a23 cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs packages/skill-bill/test/shared-judgments.test.mjs
GATE-RUN runId=cb8c8fe7-33e7-4a2e-81cd-2f9062c322f9 cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs
GATE-RUN runId=37c936a1-0b97-40c9-9ce0-f826994e035c cmd=node --test packages/skill-bill/test/*.test.mjs
GATE-RUN runId=e4482130-7255-4fda-ac24-c1d2a95d7764 cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=08f6b1af-1e77-4c9a-9750-5d870a0b8931 cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs
GATE-RUN runId=557a5bce-7c20-4bbd-8550-cc9fa7a6962e cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=0e92920a-26bf-415d-b0c0-d13e519a7f31 cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=7be2c7d3-1e43-4e90-9b4e-a64a9fc4354a cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs
GATE-RUN runId=8f2e3964-0028-4fff-a15e-983cae8edb1b cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=02418432-ecf6-4de9-b7bb-643ef4ee7871 cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=d6158be8-ea9d-4e8c-b65e-5350fa8cd07b cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=27b9652b-e7d0-4367-8cd5-e4726f66c3aa cmd=npx tsc -b packages/skill-bill --force
GATE-RELAX flag=--allow-nonzero reason=变异红跑 exit=1 为预期断言红（改坏必红），非门禁失败；绿跑全部 exit=0。

## 三、`textOf` 三样口径：保留（不统一）＋逐处理由

- 结论：保留差异，不统一。任一方向硬并都改页面可见行为，触红线；故 `src` 零改，只加表征断言。
- 当面证据：新件甲乙对照用例（同一 `12` 两面不同）＋ M4 红跑（乙改甲后旧护栏同红）。

| 口径 | 处数 | 位置 | 现有面 | 保留理由（逐处） |
|---|---|---|---|---|
| 甲·只认字符串 | 2 | `src/shared/prefillNote.ts:40`（预填判定）、`src/record/collect.ts:20`（分派判落点） | 字符串剪空白，其余一律空 | 判的是有没有在这一格说话：`kind`／`op` 与预填四格都是字符串槽；数字若算给了会误路由（`sceneFor` 落错件）或跳过预填（该顶的不顶）。改乙＝分派与预填行为变。 |
| 乙·数字也转 | 12 | `src/shared/recentPicks.ts:25`（三枚选择器候选）＋ 11 件场景件（`scene-batch.ts:38`、`scene-borrow.ts:52`、`scene-collect.ts:48`、`scene-installment.ts:46`、`scene-lend.ts:52`、`scene-reimburse-done.ts:47`、`scene-refund.ts:48`、`scene-repay.ts:48`、`scene-restore.ts:44`、`scene-undo.ts:45`、`scene-update.ts:40`） | 字符串剪空白、数字写十进制串、其余空 | 处理的是值转回显：金额／编号／总额／期数常以数字形态进（JSON 数字、库内数值）；候选、摘要、表格都要把数字印出来。改甲＝数字金额从候选与回显里消失。11 件场景件字面同形是各能力自有，未走共用位；收拢即改 11 张页，另开票才议。另：`collectBody.ts:32` 直接引乙（非第 16 处定义）；`scene-expense/income/plain/photo/reimburse` 走通用装配体间接受乙。 |
| 丙·缺省未设置 | 1 | `src/shared/diffTable.ts:36`（比对） | `undefined`／`null`／空白串同面 `未设置`，其余 `String(v)`（值本身不剪空白） | 算的是改了没改：没给与空串必须同面，否则没动那一格会被算成一次改动；数字与数字串同面（`12` 与 `"12"` 不算改）；值保留首尾空格（`"12"` 与 `" 12 "` 算改）。改乙＝空串与未给分家，误报改动；改甲＝数字变空串，漏报误报齐出。 |

- 改前改后产物差异读数：本票未统一，故无产物差异；替代读数＝ M4 红跑（乙改甲后 3 条红）＋甲乙对照绿（两面并存）。
- 15 处 census 取法：`rg "function textOf" packages/skill-bill/src`（3 共用＋1 分派＋11 场景＝15，与票面数一致）。

## 四、路径与对账

- 新增（本票全部）：`packages/skill-bill/test/t469-guard-additions.test.mjs`；
  `docs/skills/skill-bill/t469-护栏补齐-证据.md`（本件）；
  `.scratch/t469/baseline.log`、`mutations.log`、`full-compare.log`（过程日志，未入仓）。
- 未碰：`base` 页面绘制包、他技能包、`src/cli`、政策、页面模板、触发器、旧测试（含 `shared-judgments`）；
  `src/shared` 与 `src/record` 终态零改（变异全部还原并重建，`git diff -- packages/skill-bill/src` 无输出）。
- 遗留出口：补断言时未发现第 5 处无护栏判定（`pickerBlock` 空态／读不通两态旧件已钉；`snapshot`／预选／同刻／三口径本件钉完；`diffRowsFor` 只比给了的字段旧件 `diffTable` 侧已覆盖）。故无滚入项。
- 结构：只加测试与文档，未改源码形状，不触发重构报警；超线件（`wake-assets`、`cmd_read`）本票未碰。

## 五、未做与下一手

- 未做：`textOf` 统一（有意不做，理由见上）；页面可见行为零改；全仓测试未跑（只跑 `packages/skill-bill` 全量）。
- 下一手：若要统一口径另开票，先定三面语义（ presence／display／compare 三个名字，不共用一个 `textOf` 名），
  再逐页给出产物差异读数；本件断言届时即统一与否的红绿判据。
- 复核法：先 `npx tsc -b packages/skill-bill --force`，再 `node --test packages/skill-bill/test/*.test.mjs`（持锁见 §二），
  读数须为 tests 150 / pass 150 / fail 0；任一 §一判定改坏须红。
