# #686 第 3 件 · 行数门（机器扫 ＋ 对账台账 ＋ 生成物自动剔除）证据

**判定：PASS**（真门禁无参 `exit 0`／`RESULT: 12/12`／`PASS: 告警线台账齐全且与实况一致`；
靶向测试 `pass 10 / fail 0`；包内全量 `pass 311 / fail 0`；同步器 `--sync --dry` 计划空 → 老化 → `--sync` 回绿，
`AGENTS.md` sha256 前后一致）

日期：2026-09-19。票：#686「机器面照卡路里装齐（生成物／棘轮／行数门）」的第 3 件产出。
参照物：`packages/skill-calorie/scripts/check-warning-line.mjs`（356 行，形状照抄）＋
`packages/skill-calorie/test/t445-告警线门.test.mjs`（298 行）＋ `docs/skills/skill-calorie/t445-告警线门证据.md`。

## 1 现场实测（先测再写，不采信任何预备值）

- **告警线＝350 行，数法＝LF 口径**（`packages/skill-bill/AGENTS.md`「文件行数告警线」一节原有，本票只补「生成物不算」那条判据）。
- **扫描面＝本包 `src/**/*.ts` ＋ `scripts/**/*.mjs`，共 90 件**（交办实测的 89 件 ＋ 本件新建的
  `scripts/check-warning-line.mjs`）。
- **超线件 3 件**（不是交办预估的 2 件——**第 3 件就是这道门自己的脚本**，见 §2）；
  **生成物自动剔出 2 件**：

| 件 | LF | 凭谁的输出声明 | 为什么剔 |
|---|---|---|---|
| `src/triggers/wake-assets.ts` | 986 | `scripts/gen-wake-assets.mjs` 第 31 行 `const OUT = join(SRC_DIR, 'triggers', 'wake-assets.ts');` | `pnpm gen` 重生成，件头自述「禁止手工改词」 |
| `src/cli/registry.ts` | 37 | `scripts/gen-cli.mjs` 第 274 行 `targets` 数组里 `path: join(SRC_DIR, 'cli', 'registry.ts')` | `pnpm gen` 重生成，件头带「勿手改」印记 |

  判据读的是**生成器自己的输出声明**（`const <名> = join(SRC_DIR, …)` 或 `targets` 里的 `path: join(SRC_DIR, …)`），
  **仓里没有第二份手写的剔除名单**；认不出的写法（变量、模板串、拼接）一律放弃——宁可漏剔，不可误剔。

- **冻结挂号值**：`src/cli/cmd_read.ts｜558`（来源＝本包 `AGENTS.md` 里 t406 那张表的逐字读数，
  按「登记原文不改」逐字搬进脚本 `REQUIRED`）。`src/health.ts` 从未挂号 ⇒ 挂号值 `—`。
  `src/triggers/wake-assets.ts` 由「历史挂号件」（986）变成「生成物自动剔除」⇒ **它的冻结行不再存在**，
  缘由写在 `AGENTS.md`「本包现状」一节，**不是删账**。

## 2 交办预估与实测的差异（两条，都当场记下）

1. **超线件是 3 件不是 2 件**：`scripts/check-warning-line.mjs` 建件当刻 363 行（> 350），
   按本包纪律它**必须**挂号——本门第一次跑就把自己照出来了（`OVER scripts/check-warning-line.mjs LF=363`），
   那是这道门对**自己**的一次读数，不是漏检。台账里它有一行，结论列写了超因与拆法（按判据族切姊妹件），
   并注明「本次先不拆」的理由（本票正在收敛这道门，同窗口搬件会把红→绿变成两次编辑的合成）。参照物
   卡路里那件 356 行同样挂号在它自己的台账里。
2. **`wake-assets.ts` 的读数没进台账**：它是生成物，不进扫描面、也不进「挂号值」这一列；
   历史读数 986 只在 `AGENTS.md`「本包现状」里提一句（不带逐件行的正文数字，免得第二种陈化面）。

## 3 绿基线机器读数（原文，`.scratch/t686/告警线门-10-收尾门禁.log`）

```
GENERATED-SKIP src/cli/registry.ts LF=37（由 scripts/gen-cli.mjs 生成，不在扫描面内）
GENERATED-SKIP src/triggers/wake-assets.ts LF=986（由 scripts/gen-wake-assets.mjs 生成，不在扫描面内）
OVER scripts/check-warning-line.mjs LF=363 已超线，需要根据规则进行重构。（本次先不拆，见台账）
OVER src/cli/cmd_read.ts LF=501 已超线，需要根据规则进行重构。（本次先不拆，见台账）
OVER src/health.ts LF=486 已超线，需要根据规则进行重构。（本次先不拆，见台账）
SCAN-ROOT: D:\ilife\packages\skill-bill
LEDGER: D:\ilife\packages\skill-bill\AGENTS.md
AGENTS.md：D:\ilife\packages\skill-bill\AGENTS.md（台账 3 行；扫描面 90 件，超线 3 件，剔出生成物 2 件；挂号台账命中 1/1）
RESULT: 12/12
PASS: 告警线台账齐全且与实况一致
```

改前基线（同一脚本、台账标记还没写进 `AGENTS.md` 时）＝ `exit 1`／`RESULT: 4/9`，逐条红条：
`RED 台账解析源缺标记` ＋ 三条 `RED 漏报（台账没有）` ＋ 一条 `RED 台账缺行：src/cli/cmd_read.ts`。

## 4 五种红各一条现场读数（探针夹具，`.scratch/t686/告警线门-13-五种红.log`）

探针＝`.scratch/t686/告警线门-五种红探针.mjs`（草稿，`.scratch/` 不入仓）：在
`mkdtempSync` 独占夹具上把每种红当场造出来，抄门自己打的那行原文，跑完即删（`CLEANUP … 删除后存在=false`）。
同一棵树上不改坏 → `GREEN-CONTROL exit=0 :: RESULT: 10/10`。

```
RED-①漏报 HOLDS exit=1 :: RED 漏报（台账没有）：src/surprise.ts LF=360；修法：node packages/skill-bill/scripts/check-warning-line.mjs --sync
RED-②陈化 HOLDS exit=1 :: RED 台账陈化：src/over.ts 台账=999 实况=360；修法：…
RED-③台账件不在面内 HOLDS exit=1 :: RED 台账件在扫描面内不成立：src/moved-away.ts（不在盘上、或已判为生成物）；修法：…
RED-④a台账缺行 HOLDS exit=1 :: RED 台账缺行：src/cli/cmd_read.ts；修法：…
RED-④b挂号值被改写 HOLDS exit=1 :: RED 挂号值被改写：src/cli/cmd_read.ts（台账 500 ≠ 登记原文冻结值 558）；挂号值是历史事实不回改，该改的是「当场实测」列；修法：…
RED-⑤a生成物判据自证 HOLDS exit=1 :: RED 生成物判据自证：本包有生成器（scripts/gen-x.mjs）却一条输出声明都抽不到——判据会静默失效，请把生成器的输出写成 `const OUT = join(SRC_DIR, …)` 或 `targets` 里的 `path: join(…)` 形式
RED-⑤b生成物印记已认领 HOLDS exit=1 :: RED 生成物印记已认领：src/triggers/big.generated.ts 件头带「勿手改」印记，却不在生成器声明的输出清单里：把生成器的输出声明写成 `const OUT = join(SRC_DIR, …)` 形式，或把它正式划出扫描面
PROBE-DONE readings=7 miss=0
```

**缩面失明的负向对照**（测试 `T4`）：同一棵探针树、同一个未挂号的 360 行件，
「只查 `REQUIRED` 行存在性」的缩面口径 `exit 0`／零红条，扩面真门在同一棵树上 `exit 1` 并点名该件。
**没有关掉扫描面的开关**（测试 `T10`）：传 `--no-scan`／`--skip-generated`／`--ignore-over-line`
一类未知开关，退出码与输出与无参**一字不差**。

## 5 同步器 dry → write 三条读数（真包真台账；本轮做了「老化 → 一次 `--sync` 回绿」的往返）

| 步 | 命令 | 机器读数 | 退出码 |
|---|---|---|---|
| 基线 dry | `… check-warning-line.mjs --sync --dry` | `SYNC-PLAN mode=dry 行=3 改=0 增=0 删=0` ＋ `SYNC-DRY ok（台账已与实况一致，未落盘）` | 0 |
| 老化后 dry | 同上（台账把本脚本那行「当场实测」故意写成 `999`） | `SYNC-PLAN mode=dry 行=3 改=1 增=0 删=0` ＋ `SYNC-CHANGE scripts/check-warning-line.mjs 台账=999 实况=363` ＋ `SYNC-DRY 计划非空（未落盘）：改=1 增=0 删=0；台账尚未与实况一致，别只看退出码` | 1 |
| 老化后 write | `… check-warning-line.mjs --sync` | `SYNC-PLAN mode=write 行=3 改=1 增=0 删=0` ＋ 同一条 `SYNC-CHANGE` ＋ `SYNC-WRITE D:\ilife\packages\skill-bill\AGENTS.md` ＋ `SYNC-VERIFY ok（回读逐字节等于计划；台账 3 行）` ＋ `RESULT: 12/12` | 0 |

- **dry 不改文件**：老化态 dry 之后 `AGENTS.md` sha256 ＝ `9F4367F1…`（仍是老化件），与 dry 前逐字节相同。
- **write 只改台账块、且无损**：`--sync` 之后 sha256 ＝ `BA29BEB3206D544B2ABFA6DCD6DDE5461940920CAD09D305914EAE0308461C16`，
  **与老化前完全一致**——同步器只把「当场实测」列改回实况，`挂号值`（`558`）与整段「结论」（超因／拆法）
  逐字保留，块外一个字未动（脚本内另断言块外前后缀逐字节相同）。
- 无参门禁在 write 后回 `exit 0`／`RESULT: 12/12`／`PASS`。

## 6 测试与运行读数

- 靶向：`node --test packages/skill-bill/test/t686-告警线门.test.mjs` → **`tests 10 / pass 10 / fail 0`**，`exit 0`。
  十例＝`T1` 夹具自身绿／`T2` 漏报必红＋删除还原必绿／`T3` 陈化必红＋还原必绿／`T4` 缩面失明负向对照／
  `T5` **真包无参门禁绿读数**（读真台账，`RESULT: n/n`）／`T6` 生成物按生成器声明剔除且不挂号不算漏报／
  `T7` 生成物判据自证必红＋生成物掉回扫描面被点名＋印记已认领／`T8` 同步器 `--dry` 不改文件 ＋
  `--sync` 回绿且只动台账块、冻结挂号值不被改写／`T9` 真包 `--sync --dry` 零改动且不动文件／
  `T10` 不许有关掉扫描面的开关。
  夹具一律 `mkdtempSync` 独占目录（前缀 `t686-gate-`）＋ 路径守卫 ＋ 用完即删，不碰真库、不碰任何源码件。
- 包内全量：`node --test "packages/skill-bill/test/*.test.mjs"` → **`tests 311 / pass 311 / fail 0`**，`exit 0`。
- **`T5`／`T9` 的红＝全仓当刻状态**（测试件头已写明）：本仓多席共用一个工作区，他席在飞的超线件、
  或任何扫描面内的件被改过行数／新增／删除，都会让这两例（以及包内全量）转红。**那是这道门的设计行为，
  不是缺陷**；修法就一条仓内命令：`node packages/skill-bill/scripts/check-warning-line.mjs --sync`。
  本轮实测到的一次：老化窗口里他席跑包内全量时本门转红（见 §7 的报备条目）。

## 7 交付物与对账

- 新建：`packages/skill-bill/scripts/check-warning-line.mjs`（363 行，判据 ①～⑧ ＋ 两条自证 ＋ `--sync`）；
  `packages/skill-bill/test/t686-告警线门.test.mjs`（10 例）；`docs/skills/skill-bill/t686-告警线门-证据.md`（本件）。
- 改：`packages/skill-bill/AGENTS.md`——「文件行数告警线」一节补「生成物不算」的判据；
  两张**人手抄的行数表**（「本包现状（2026-09-15 t406 整改实测）」「#411 查询骨架后复测」）删除，
  换成 `<!-- warning-line-ledger:begin/end -->` 之间的**四列机器对账台账**（`件`／`挂号值`／`当场实测`／`结论`）；
  另补「台账两种口径」「台账」「本包现状」「检查脚本」四节。**表外正文不再出现会陈化的逐件行数**。
- 未碰：本包任何源码件（`src/**` 一件未动）、`packages/skill-bill/package.json`（票主加脚本）、
  `scripts/gen-cli.mjs`、`scripts/gen-wake-assets.mjs`、`scripts/ratchet-*.mjs`、`test/cmd-registry-686.test.mjs`、
  `test/legacy-ratchet-686.test.mjs`、`test/t686-收紧守卫.test.mjs`（都是票主的件）；未切分支、未 push。
- 草稿（`.scratch/` 被忽略，不入仓）：`.scratch/t686/告警线门-五种红探针.mjs` 与 `告警线门-*.log`。

GATE-RUN runId=a39051bf-4bdc-4f7b-9f01-a3deb002e1a8 cmd=node packages/skill-bill/scripts/check-warning-line.mjs
GATE-RUN runId=dd58bd64-0422-4b6e-a67e-0483deb745b7 cmd=node packages/skill-bill/scripts/check-warning-line.mjs
GATE-RUN runId=365f2398-2030-44fd-87e7-c3037fbb4731 cmd=node packages/skill-bill/scripts/check-warning-line.mjs --sync --dry
GATE-RUN runId=be57ced3-2707-4d92-ba25-ce19cc646bce cmd=node --test packages/skill-bill/test/t686-告警线门.test.mjs
GATE-RUN runId=3d6d5da6-cac9-4250-a4d9-c08eae969163 cmd=node packages/skill-bill/scripts/check-warning-line.mjs
GATE-RUN runId=8716982e-9a5f-4775-b2c6-93d69ec87e80 cmd=node packages/skill-bill/scripts/check-warning-line.mjs --sync --dry
GATE-RUN runId=4046088f-dc54-4b97-a1b2-ec09160305fb cmd=node packages/skill-bill/scripts/check-warning-line.mjs --sync
GATE-RUN runId=377af7f3-a1a3-45c8-993e-3fb910f00385 cmd=node packages/skill-bill/scripts/check-warning-line.mjs
GATE-RUN runId=e17957f2-8635-470d-8e68-d7996f02ce6d cmd=node --test packages/skill-bill/test/t686-告警线门.test.mjs
GATE-RUN runId=dc6c3042-cab3-4215-8d82-1dca9206a775 cmd=node packages/skill-bill/scripts/check-warning-line.mjs
GATE-RUN runId=05e787d1-9f67-42a9-9407-916c9a01674c cmd=node --test packages/skill-bill/test/*.test.mjs
GATE-RUN runId=80dbb78b-20ea-4d79-9b7e-1c56f3bf0b4f cmd=node .scratch/t686/告警线门-五种红探针.mjs

- 逐条对应：`a39051bf`＝改前基线（`exit 1`，台账标记还没有）；`dd58bd64`＝台账落地后第一次无参绿；
  `365f2398`＝真包 `--sync --dry` 计划空；`be57ced3`／`e17957f2`＝靶向测试两跑（都 `exit 0`）；
  `3d6d5da6`＝老化后无参红；`8716982e`＝老化后 dry（计划非空、未落盘）；`4046088f`＝落盘并回绿；
  `377af7f3`／`dc6c3042`＝回绿复跑两次；`05e787d1`＝包内全量；`80dbb78b`＝五种红探针。
- **窗口外、未声明**（本件不覆盖，逐条报备）：`9f0dcb1e-0155-4fec-8521-5fd53ec0ffb9`（本席第一次包内全量，
  `--test <目录>` 写法被 Windows `cmd` 吃掉尾斜杠 → `Cannot find module …\test`，`exit 1`；改用 glob 后
  `05e787d1` 全绿，脚本本身没问题）；`718f7dfb…`／`ecbd8f87…`／`8087413d…` 等属**他席**（票主）在本票上的
  运行条目，不属本件。

## 8 未做项与下一手缺什么

1. **超线件的拆法不在本件**：3 件只挂号、一件未拆（`cmd_read.ts` 的拆法＝按域拆分派，属「读命令搬迁」那张后票；
   `health.ts` 的拆法＝解析器／探针／报告装配切三件姊妹件；`check-warning-line.mjs` 的拆法＝按判据族切姊妹件）。
2. **本门是「活快照」门**：`当场实测` 列对的是当刻 LF，任何人改扫描面内的件而不同步台账，本门必红
   ——这是设计行为；修法＝仓内一条命令 `node packages/skill-bill/scripts/check-warning-line.mjs --sync`。
   `T5`／`T9` 与包内全量会一起红，别误当缺陷。**生成物不再触发本门**（重跑 `pnpm gen` 改
   `wake-assets.ts`／`registry.ts` 都不会逼无关的票来同步台账）。
3. **本门与本包别的机器面尚未接线**：`pnpm test`（包内 `test/*.test.mjs`）已含本件测试；
   `package.json` 的 `check:warning-line` 脚本由票主加，本件未动该文件；是否把本门接进仓根 CI／
   `pnpm build` 链由票主决定（不在本件写集）。
