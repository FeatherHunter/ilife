PASS
# t410 墙复核（非作者独立重跑，不采信结论）

## 一、结论与口径
判 PASS。被审：生成器 `docs/skills/skill-bill/t410-验收墙.mjs`（239 行，`64f871e`）＋清单 `.scratch/t410-wall/manifest.json`（rows32＋pages32）＋B回执 `t410-锁-证据.md`（16词锁表＋GATE-RUN四行）＋C回执 `.scratch/t410-wall/A-清单.md`（32页铺底口径）与 `run.log`（`RESULT 32/32`）。B／C结论一律自己重跑，不采信。红线：不改源码与墙产物，临时改名逐字节还原（size＋hash双证）；编译／测试／探针／git 写一律 `node tooling/run-locked.mjs --ticket 410 -- …` 排队。

## 二、正反自检重跑读数
正例持锁重跑 `t410-验收墙.mjs --check .scratch/t410-wall`：`32格；链接164条（手机墙65＋桌面墙65＋总索引34）；缺失0 -> 可发`，exit 0（runId `133fd912`）。反例复现原探针 `.scratch/t410-wall-neg.mjs`（清单首行改故意缺件名）：exit 1 且点名 `__故意缺件-`，改回 exit 0（runId `dadf214a`）。清单自核：rows＝pages＝32 且文件集合逐字一致，seq 1–32 连续，rowCount 32，无 BOM，notShipped 1 条（他域 61 词归他图），命名 single-source（rows[].file＝pages[].file）。

## 三、抽查三页与自设新探针
抽查 3 页（回执各一支：普通／降级／修正）：`t410-页-记支出-回执页.html`（73966B，doctype1＋section1，含 `-12.50` 与 `餐饮`）、`t410-页-拍账单-回执页.html`（74300B，含 `三要素以外部识别为准` 与 `餐饮 -30.00`）、`t410-页-改记录-回执页.html`（72713B，含 `改过` 与 `已修改`）；三页 file 与 manifest／A-清单逐字一致，且手机墙／桌面墙／总索引三处引用全有（node 直查 has＝1）。新探针（与原 manifest 改法不同，改盘上文件名）：临时改名 `t410-页-记分期-回执页.html` → `--check` exit 1，`缺失2`（dropped＋dead 同点一名）→ 还原后 size 74818 一致 → exit 0 缺失 0（runId `511437aa`）。manifest SHA256 前后均为 `746AC7F9…9D4D0`，无 `.bak` 残留。

## 四、还原与提交
改名页已还原（size 一致，无残留）；manifest hash 不变；生成器与 32 件产物零改；临时脚本（spot×2＋myprobe）试完已删。新增唯一入仓件为本件；提交前 `git diff --cached --name-only` 仅本件一件，持锁提交。
