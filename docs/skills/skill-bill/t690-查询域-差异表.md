# t690 查询域 · 差异表（老行为／新行为／为什么改／谁批的）

- 票：[query 域：parity 优化](https://github.com/FeatherHunter/ilife/issues/690)（父图 [#681](https://github.com/FeatherHunter/ilife/issues/681)）
- 口径出处：地图 #681 Notes 偏好 2「真相源＝老技能的行为，**允许改，但每次改动逐条留痕**」；
  四列＝`老行为／新行为／为什么改／谁批的`。
- **读法**（与同域姊妹件 `t689-差异表.md` 同形，**这一对表就地定形**）：本表只记**本票改掉的**东西；
  「老行为」＝老技能（`D:\2Study\StudyNotes\SKILLS\饼干记账`）与**本票动手之前**的新仓产物；
  「新行为」＝本票之后的产物。两域表的表头都是「四列事实 ＋ 一个行号位」——
  地图 #681 的 `## Not yet specified` 里那条「**差异表的形状与住处**（每域一张，但表头与落点未定）」
  由这一对表结清：**形状**＝`老行为／新行为／为什么改／谁批的` 四列，允许一个行号位（各域自取 `#` 或 `#＋块`）；
  **住处**＝`docs/skills/skill-bill/t<票号>-差异表.md`（同 `t687`／`t688` 两件的落点）。
- 对照物：老侧 `templates/query_view.html`（**行号口径 549／LF 口径 548**，§9.4 第 3 条）＋
  `scripts/bill_inject.py`；新仓 `src/query/{declaration,commands,read,list,detail,pageParts}.ts`。
- 逐块三档表（留／舍／改）**先有** `t411-查询域-老模板逐块清单.md:77-104`；本件是**复核后**的那一份，
  与 t411 判得不一样的行，理由写在「为什么改」一列里。

## 一 逐块差异

| # | 块 | 老行为 | 新行为 | 为什么改 | 谁批的 |
|---|---|---|---|---|---|
| 1 | KPI 行（老 6 处：summary／list 族／tag／debt／reimburse／installment） | 4 格：笔数／支出／收入／净额（各型出几格不同） | 4 格，口径 `calcKpi`（转账不计进收支） | 不改（留） | t411 判「留」；#690 复核维持 |
| 2 | 明细列表 | `recordsList`（`:522-534`）4 字段：时间／分类／备注／金额 | `renderDataTable` 7 列：时间／分类／金额／账户／账本／备注／编号 | **改**：语义表格（`table/thead/th/td`）换掉自绘 grid，拿可访问性与窄屏行卡化；「编号」列是新仓补的读链入口（老页没有 detail 分支） | t411 判「改」；#690 复核维持 |
| 3 | **分类聚合卡**（老 `categoryBar` 前 8 类） | `:261-266`／`:294-299`／`:509-520`：分类名＋金额／笔数／占比／均额＋按最大值归一的宽度条，`slice(0,8)` | `renderDistributionRows`（公共层占比条）：`名称 ｜ 条 ｜ 金额 · 笔数 · 均额`，前 8 类 | **改**：四件事一件不少，条形版式换公共层件（老侧这条是自绘 CSS，属「设计联邦」那一类） | t411 判「改」；#690 实施 |
| 4 | **过滤条件芯片**（老 `:280`／`:287` 的 `data.filter`） | 一排键值对芯片（如「分类: 餐饮」） | **不出这一排**：筛选条件住**副标题**（`分类＝餐饮（全部时间）`／`备注里有「午饭」的记录`／`标签 旅行（精确匹配）`…） | **复核 t411 的「留」**：t411 留它的理由是「让用户核对 AI 解析出的筛选条件」——**那件事新仓已经做了**，落点是副标题（`t413-record-range.test.mjs:176,198` 与 `t414-record-search.test.mjs:90,111,132,143,152` 逐条钉着）。信息等价已成立，再出一排芯片就是同一件事说两遍（`688-融合基准.md` 裁定 6 同款毛病） | #690 复核改判 |
| 5 | **占比芯片**（老 `category_pct`，`:287`） | 查分类页有一枚「占比 X% 的同期支出」 | 查分类页结果胶囊多一枚：**「占全部支出的 19%」** | 不改（照搬）：老侧这枚是**新仓原来没有的**，补进胶囊行 | t411 判「留」；#690 实施 |
| 6 | **记录列表 200 条显示上限** | `:524` `slice(0,200)` ＋ `:533` 追一行「仅显示前 200 条 · 共 N 条」 | 条数**按当刻产物的字节算**（上限仍是 200）：先试上限，放不下就二分找「放得下的最大条数」，另出折叠区「还有 N 条没显示」＋口径行 | **改数，理由是硬门**：交付路对写下去的那一串跑 `assertHtmlSize`（`packages/skill-bill/src/render/html.ts:46`，上限 256 KB），超了 **exit 5、整页不出**。而本页体积由**复制载荷**主导（复制区按 #247 给三种格式，同一份数据落三处，实测每条约 1.6 KB）⇒ 老侧那个 200 在本仓画不出来。实测：205 条的窗按 200 条画 → 404,980 B（撞门）；改成按体积定条数后 → 表 91 行／238,918 B（过）；100 条 200 字长备注 → 38 行／236,026 B（过） | #690 判定（题面「这条口径要重新拍」点名本票）；读数见证据件 |
| 7 | 自绘样式层（`:7-109`）／共享样式位／payload 注入位 | 每页一份自绘 CSS，绑死 `max-width:920px` | 全走公共层样式表（18/18 逐字节同一份） | 舍：纯渲染机制 | t411 判「舍」 |
| 8 | 主路由 `init` ＋ type 分发（`:147-201`）／启动 | 客户端按 `data.type` 查 routers 表当场拼 DOM | 无：Node 侧渲染成静态页，一条命令一页 | 舍：客户端路由；且老侧那 13 个 type 键里有 4 个在标准管线**不可达**（缺陷 D15） | t411 判「舍」；`688` §一 §二·C4 |
| 9 | 对比卡 `compareCard`／`change-badge`（`:325-344`）／图表挂钩 `charts.donut`（`:369-374`）／monthly／overview／stats 分支 | 老页有（17 词里**无人用**，属分析域） | 不出 | 舍：属分析域，查询骨架不承担 | t411 判「舍」 |
| 10 | 页脚（`:212-219`） | `generated_at` ＋ `type` 代码 ＋ **源脚本路径字面**「`scripts/record_bill.py {type} --json`」 | **来源脚注**一行：`数据来源 · 记账库（只读） · 起 → 止 · 共 N 条` | **改**：老侧把脚本路径印上屏，与实际分域路由对不上（缺陷 D6），对用户也无意义；新侧照 `688` §四 裁定 1 写**人话来源**。条数与窗口都留 | `688` §四 裁定 1／裁定 2；#690 实施（`src/shared/sourceLine.ts` 新建） |
| 11 | 错误态 `renderError`（`:221-232`，四入口） | 带原始 payload 的回执可复制 | 新仓既有失败面（`render/errors.ts` ＋ `renderFeedbackBlock`），不引老 `error_receipt.html` | 留能力、换实现 | t411 判「留」；`688` §二 A8／A10 |
| 12 | 动作区（`:234-247`，复制数据／日志） | 公共层注入两通道，页面本地再放一颗「📋 确认复制」 | `copyArea` 三格式菜单 ＋ 双按钮，无同名标题 | 改：老侧两套复制入口并存、语义重叠 | t411 判「改」；`688` §二 · 裁定 5 |
| 13 | 空态（老 8 处 `emptyState`） | `window.emptyState`，各支一句 | `renderEmptyBlock`，句＋**引导句**（说清下一步说什么词） | 改：老侧空态只说「没有」，不说下一步 | t411 判「留」；`688` §二 A9；#690 补引导句 |
| 14 | 离线提示条（`:173-175`） | 客户端探缓存后出黄条 | 不出 | 舍：静态页没有客户端缓存这一成因 | t411 判「改」；#690 复核改判「舍」（无内容可承载） |
| 15 | 全页布局（`:23` 写死 `max-width:920px`、`:102` 640px 断点） | 每件自写断点与画布 | 公共层 `pageUi`／`docPage` 补丁（桌面 1200px 档表铺满版心、窄屏替 `dl` 键值列表） | 改：断点与画布单源住公共层，页面本地不写断点 | t461／t403-D1／D2 |

## 二 新侧新增（老侧没有的块，来自 `688-融合基准.md` §五 骨架表）

| # | 块 | 出处 | 为什么加 | 谁批的 |
|---|---|---|---|---|
| 16 | **结论句一行**（§五 第 3 行，⑤ 类恒出） | `renderConclusionBar` | 老侧只有读数卡（四个数），没有一句「这批账主要是什么」的人话判定 | `688` §四 裁定 2／§五 第 3 行；#690 实施 |
| 17 | **页内导航**（§五 第 4 行，⑤ 类恒出） | `renderTocBlock` | 老侧只有「header ＋ 离线条 ＋ 页脚」这一薄恒显层（`query_view.html:113-119` 实测只有 4 个挂载点），内容一长就失去定位 | `688` §四 裁定 2；#690 实施 |
| 18 | **口径说明行**（§五 第 24 行） | `renderCaliberLine` | 笔数／占比／均额三处口径原来没有一处交代 | `688` §四 裁定 2；#690 实施 |
| 19 | **来源脚注**（§五 第 26 行，恒出） | `src/shared/sourceLine.ts`（**新建共用位件**） | 照 `688` §八 A5：新建共用位件，落第一张用它的页面票 | `688` §八 A5；#690 是第一个用法 |
| 20 | **长列表明示截断 ＋ 折叠说明**（§五 第 21 行，⑤ 类恒出） | `renderDisclosure` ＋ 口径行 | 老侧是 `slice(0,200)`＋一行提示（属**优点**：不静默截断），本条继承其意图、换实现 | `688` §五 第 21 行；#690 实施 |

## 三 判过而**不动**的（老侧缺陷，不照抄）

| # | 项 | 老侧怎么做 | 新侧怎么做 | 出处 |
|---|---|---|---|---|
| 21 | 老页脚把源脚本路径印成字面（`query_view.html:217` 恒印 `scripts/record_bill.py`） | 印字面 | 不印（改人话来源） | `688` §一 §二 A5（缺陷 D6／D16） |
| 22 | `query_view.html` 四个渲染器在标准管线不可达（`:307-322`／`:325-344`／`:378-390`／`:393-402`） | 死代码 | 不搬 | `688` §六 第 2 行（缺陷 D15） |
| 23 | `breakdown` 的域归属三处不一致（CLI 在分析、模板回落查询页、页脚却印查询脚本） | 三处各说各的 | 新侧 `type` 与页 1:1，不靠人维持一致 | `688` §二 · C4 |
| 24 | 老侧全件零可访问性（`role=`／`aria-`／`alt=`／`<label for=` 全 0 命中，可点元素是裸 `<div onclick>`） | 裸 div | 公共层自成语义（页内导航是 `<nav aria-label="页内导航">`，表是 `<table>/<th>/<td>`） | `688` §二 · D9 |

## 四 复核 T6 清单交本票的那一条

| # | 项 | 判定 | 为什么 |
|---|---|---|---|
| 25 | `bill.record.detail` 的**页型标记**存疑（T6 `t687-parity-审计清单.md` §3.2 注 1：形状是 `detail`，`--html` 出的却是 `data-page="list"`／`data-slot="ilife:bill:list"`；`pageShell.ts:36` 的 `slot/page` 只有 `collect`／`receipt`／`list` 三值） | **不补 `detail` 这一档** | ① 逐页标识已经够用且各命令互不撞：4 条命令的 `data-key` 各不相同（`record.today`／`record.range`／`record.search`／`record.detail`），`data-shape` 在详情页就是 `detail`（`test/t417-query-lock.test.mjs:148` 逐条钉着）。② `data-page`／`data-slot` 答的是「这一页用**哪套槽位**装配」，不是「这是哪条命令」——详情页与列表页共用 `list` 槽位（两页的页壳与复制区同一套），这正是 `t685-按域页型表.md` §2.2 登记的两张页型共用一套页框的意思。③ 补一档要动**共用位** `src/shared/pageShell.ts` 与 `src/shared/writeParts.ts` 的槽位闭集，而它换不到任何新信息。⇒ 判「不补」，理由如上；T6 那句「交 #690 判」到此结清 |

## 五 一处**没对上**，据实登记（不许写成对上）

| # | 项 | 读数 | 为什么 | 处置 |
|---|---|---|---|---|
| 26 | 判据③「**机审六列 0 命中**」 | 五列 0（样式块 0／色值数 0／设计位 `·`、`|` 0／英文裸词 0／重复句 0）；**第六列「内联 style」不是 0**：16 张列表页各 1–6 处，详情页与空态页 0 处 | 那 1–6 处**全部**是公共层 `renderDistributionRows` 的填充宽度（`<span class="…-dist-row-fill" style="width:N%">`）——它是**数据宽度**，不是页面自造的版式；而占比条正是 `688-融合基准.md` §五 第 13 行对⑤类页点名要的块（`renderDistributionRows:463`）。⇒ **判据③这一条与 T7 §五 第 13 行互相矛盾**，本票按「做 parity」取块，并如实报读数 | **待维护者裁**：认这条读数为「数据宽度、非页面自造」而判通过；或要我把占比条拆掉换成小表（那样第六列回 0，但 §五 第 13 行的占比条就没了）。见票面「进度与待确认」 |
