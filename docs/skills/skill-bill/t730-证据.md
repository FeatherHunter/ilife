# t730 目标域 · 实施证据

- 票：[goal 域：端到端搬迁（4 场景／2 片）](https://github.com/FeatherHunter/ilife/issues/730)（父图 [#681](https://github.com/FeatherHunter/ilife/issues/681)）
- 强制对照基准＝`docs/skills/skill-bill/688-融合基准.md`（T7）；形状规格＝`packages/skill-bill/docs/t685-按域页型表.md`（T4）＋
  `packages/skill-bill/docs/t685-接口与判据.md`；parity 审计＝`docs/skills/skill-bill/t687-parity-审计清单.md`（T6）。
- 可复跑件：`docs/skills/skill-bill/t730-探针-真出口.mjs`（真出口 29 步）· `packages/skill-bill/test/t730-目标域.test.mjs`（靶向 12 条）。
- 差异留痕：`docs/skills/skill-bill/t730-差异表.md`（24 行，四列 ＋ 行号位）。

## 一 票面判据逐条

| 判据 | 结论 | 读数与出处 |
|---|---|---|
| 1 · 4 条唤醒词逐条真跑（exit 0／产物落盘「文件字节＝`delivery.bytes`」／整页／字段正确） | **对上了** | 探针 ①–④ 共 16 步全绿（`RESULT: 29/29`）：`设定预算`（采集／回执／冲突／覆盖四态）· `设定目标`（采集／回执两态）· `看预算`（整页／✓无／夹取／三态／过去月／空表）· `看目标`（整页／已达成／空表）；靶向测试 `t730-目标域.test.mjs` 12/12 同判。落盘字段逐条断言：`goals.json` 的 `budgets[0]`＝`{month:'2026-09',category:'',amount:2500,created_at:'YYYY-MM-DD HH:MM:SS'}`、`savings[0]`＝`{name:'换手机',amount:10000,deadline:'2026-12-31'}` |
| 2 · 进度条与三态（双端夹取 0 ≤ pct ≤ 100；三态各自跑得出来，阈值 ±0.01；超支项 0 出「✓ 无」） | **对上了** | 夹取：超支那条产物 `class="ilife-block-kpi-card-bar-fill …" style="width:100%"` ＋ 上屏「进度 100.0%」（不是 150.0%），「超出 500.00 元」照写；口径函数自证 `clampPct(-20)=0`／`clampPct(180)=100`／区间内不动。三态（今天钉 2026-09-15、当月 30 天、已过 15 天、实际 1500）：预算 2500 ⇒「预计超 500.00 元」／3500 ⇒「预计省 500.00 元」／3000 ⇒「预计持平」，三态逐条命中；另测过去月「预测＝实际」（2026-08：月底预计 800.00 元）与未来月不给预测。「✓ 无」：超支项 0 时产物里是 `✓ 无` 而不是 `0` |
| 3 · T7 §四 裁定 1／2／4／6／7／9／11／12 逐条自证 ＋ 可见文本 grep 内部标识 0 命中 | **对上了** | 见 §四 逐条（探针 ⑤ 的 8 步就是这 8 条的可判形式；裁定 1 在 5 张页上 0 命中，裁定 11 本域只触达「不出页脚按钮组」那一半，理由见该行） |
| 4 · 与 T6 逐条对账（§2 的 4 条词／§3 两条键／§4 四张模板） | **对上了** | 见 §三：4 条词逐字在位（探针路由层 4/4 ＋ `test/wake-assets.test.mjs`）；两条键由「可用（弱）· 域页缺口」升为**可用**（四处一致 ✓✓✓✓）；4 张模板全部销账（改前 4 张全「无落点」） |
| 5 · 与老侧逐条对照出一张差异表（四列 ＋ 行号位；「两套下界口径取双端」写明出处） | **对上了** | `docs/skills/skill-bill/t730-差异表.md`：§一 页型层 4 行 ·§二 事实与口径层 12 行 ·§三 判过不动 6 行 ·§四 留白 3 行 ·§五 T4 逐块补 2 行；第 9 行「同域两套下界口径取双端」写明两处出处（`budget_view.html:209`／`:228` 只有上界、`saving_view.html:208` 双端）与裁定出处（T7 §二 C13） |
| 6 · 路径从零走完（域声明 → 命令 → 页面 → 测试）＋「读写两类命令同域」的结论写进口径注释 | **对上了** | 域声明（#721 已落，本票零改）→ 命令（`commands.ts` 两条）→ 页面（两片页型 6 张页）→ 测试（靶向 12 条 ＋ 真出口探针 29 步）。结论落在 `src/goal/index.ts` 的门注释：**一个域里同时放写入口与读入口不需要第二个入口函数**——命令声明里的 `run` 就是唯一入口（出口按注册表直接调 `spec.run`），与 `src/account/index.ts`（纯写、也不再有第二入口）／`src/query/index.ts`（纯读、入口服务那一窗按域写死的分派）两处口径注释成对照 |
| 7 · 接线五件同窗办 | **对上了**（逐件读数见 §五·接线） | `cmd_read.ts` 删本域两个 `case`（共 59 行）· `envelope.ts` 过渡表删两行（8→2）· `pnpm gen` 重生成注册表（14 条／6 能力）· 棘轮冻结值同窗下调（`legacyKeys` 8→2、`dispatchKeys` 8→2、`registryKeyCount` 8→14、`lineCaps` 455→280 与 62→56）· 测试硬编码三处（`record-write`／`t686-收紧守卫`／`t721-域声明`，均 8→14）＋ `cli.test.mjs` 那条 goal 断言（冲突 exit 2 → 阻断页 exit 0）· `AGENTS.md` 台账 `--sync` |
| 8 · 门禁全绿 ＋ 本域靶向测试 ＋ 真出口探针各一份入仓 | **对上了** | 见 §五（全量测试／`gen:check`／棘轮／场景形状门／告警线门 ＋ 页指纹门）；两份判据件已入仓：`packages/skill-bill/test/t730-目标域.test.mjs`、`docs/skills/skill-bill/t730-探针-真出口.mjs` |

## 二 老侧 4 张模板逐块 → 新侧落点（一条信息一行）

老侧出处一律给 `templates/目标/<件>:行号`；新侧给件与块位。

| 老侧块（出处） | 新侧落点 |
|---|---|
| `budget_form.html:110-118` 标题 ＋ 三枚 chips（⚡唤醒词／● 填写确认／🕒 生成时间） | 徽章列（`badgeOf`：唤醒词 ＋ 口径 ＋ 状态徽章）；生成时间不上屏（裁定 1：内部标识与时间戳不进正文，窗口与条数走来源脚注） |
| `budget_form.html:55`＋`:129-134` 覆盖冲突提示条（「已存在 X 月预算（分类 金额 元），确认后将覆盖」） | 采集页「同月同类预算已存在」块（`renderFeedbackBlock` 浅色静态形态）＋ 阻断折叠里的那一项（含原来那条的金额与「要覆盖就把确认覆盖给上」的出路） |
| `budget_form.html:58-68` 三格：金额（¥ 前缀 ＋ 数字）／月份／分类 ＋ 每格 hint | 字段卡三格（`renderParamForm`，`GOAL_WRITE_SLOTS['set-budget']` 的 name／label／hint／required）＋ 口径说明行 |
| `budget_form.html:137-146` `buildPrompt`（三格 ＋ `__如:3000__` 占位） | 复制指令块（`promptOf` 的 `____`／`____（选填）` 占位写法，同义） |
| `budget_form.html:74` 「📋 确认复制」单按钮 ＋ `:153-161` actionBar（复制数据／日志两通道） | 复制区（`copyArea`：三格式菜单 ＋ 双按钮）；老侧那两颗并存的复制入口合成一处（裁定 5） |
| `budget_form.html:78` payload ＋ shared helpers 注入 | 新侧页面内置 envelope（`envelopeOf`）＋ `assembleDocPage`（无运行期注入） |
| `saving_form.html:55-63` 三格：目标／金额／截止日期（hint「不填 = 无截止日期」） | 字段卡三格（`GOAL_WRITE_SLOTS['set-saving']`）＋ 口径说明行「没写截止日就算到今天」 |
| `saving_form.html` 无「已有条目」块（同族 budget_form 有） | 采集页「已有条目」只读表两件都出（差异表第 8 条） |
| `budget_view.html:142-149` header（eyebrow／h1／subtitle） | 页壳：眉标「记账 · 目标域」＋ 标题＝唤醒词 ＋ 副标题＝「2026-09 的预算执行（当月支出实时合计）」 |
| `budget_view.html:151-156` footer（生成时间／类型 `budget`／数据载体） | 来源脚注（「数据来源 · 预算表与账本（只读） · 起 → 止 · 共 N 条」）；类型代码与脚本路径不上屏（裁定 1） |
| `budget_view.html:193-196` KPI 4 卡：预算总额／实际支出／剩余／超支项 | 读数行 4 格，同数同序（`scene-budget.ts` 的 `budgetKpi`）；「预算总额」那格带完成度条（§五 5.2 第 5 行） |
| `budget_view.html:196` 超支项 0 → `✓ 无` | 同：`value: over_count > 0 ? String(over_count) : '✓ 无'`（探针 ② 断言） |
| `budget_view.html:223` 状态徽章（预算内／接近上限／已超支） | 读数卡的状态徽章（`BUDGET_STATUS_META` 三档同字） |
| `budget_view.html:224` 已用 X / Y 元 · N 笔 | 卡内 `detail`：「预算 X 元 · 已用 Y 元 · N 笔」 |
| `budget_view.html:226` 进度条（宽度 `Math.min(pct,100)` ＋ 状态色） | 卡内进度条（`bar.pct = clampPct(pct)`，**双端**；色由公共层 `kpiBarClass` 分档） |
| `budget_view.html:228-229` 进度 X% ＋ 剩余／超出 | 卡内 `detail`：「进度 X% · 剩余／超出 N 元」（百分比同过 `clampPct`） |
| `budget_view.html:210-211` 超支／接近上限警示句 | 状态徽章 ＋ 「超出 N 元」那一格（同一件事不重复说两遍） |
| `budget_view.html:214-219` 月底预测（日均 ＋ 月底预计 ＋ 超／省／持平，阈值 ±0.01） | 卡内 `detail` 尾段（`monthEndHintOf`，三态阈值逐字照 `:216-217`） |
| `budget_view.html:203` 空态（🎯 ＋「X 还没有设置预算」＋「说『设定预算』即可开始」） | 空态块 ＋ 引导句（同义，唤醒词由域声明投影算出） |
| `budget_view.html:97-98` `fmtMoney`（千分位）／`fmtPct`（一位小数） | 公共层数值格式（两位小数；千分位不照抄——`688` §二 C10 的单源纪律） |
| `saving_view.html:142-148` header／`:150-155` footer | 同 budget 那两行（来源脚注写「目标表与账本（只读）」） |
| `saving_view.html:192-195` KPI 4 卡：目标数／已完成／累计已存／目标总额 | 读数行 4 格，同数同序（`scene-saving.ts` 的 `savingKpi`）；「累计已存」那格带完成度条 |
| `saving_view.html:220` 状态徽章（已达成／进行中／进度落后／暂无进度） | 读数卡状态徽章（`SAVING_STATUS_META` 四档同字） |
| `saving_view.html:221` 已存 X / Y 元 | 卡内 `detail`：「目标 Y 元 · 进度 X% · 还差／已超出 N 元」 |
| `saving_view.html:223` 进度条（宽度双端） | 卡内进度条（`clampPct`，同口径） |
| `saving_view.html:225` 进度 X% ＋ 月均净存 N 元 | 卡内 `detail`：「进度 X% · 月均净存 N 元」 |
| `saving_view.html:209-211` 预计达成日／已达成／暂无预计 ＋ 截止日期 | 卡内 `detail`：「预计 YYYY-MM 达成」／「目标已达成」／「暂无预计（先存一笔吧）」＋「截止 X」／「无截止日期」 |
| `saving_view.html:213-216` 达标所需月存（有截止日且未达成） | 卡内 `detail` 尾段：「截止前达标还需每月 N 元」 |
| `saving_view.html:202` 空态（🐷 ＋「还没有储蓄目标」＋「说『设定目标』即可开始」） | 空态块 ＋ 引导句 |
| `budget_view.html:131`／`saving_view.html:130` 离线横幅 | **舍**（静态页没有客户端缓存这个成因） |
| `budget_view.html:134`＋`:163`／`saving_view.html:133`＋`:162` 双转义（用户看到 `&lt;`） | **不照抄**（转义只由公共层一处做） |
| `budget_view.html:159-169`／`saving_view.html:158-168` `renderError`（errorReceipt 控件） | 新仓整页失败面（`renderFeedbackBlock` 的 error 位），老侧那套不引入（`688` §二 A10） |
| `saving_view.html` 的 `files` 空数组渲染成空白行 | **不引入**（新侧没有这一块；`688` §二 A7 点名过的老侧缺陷） |

## 三 与 T6 逐条对账

### §2 词层（第 56–59 行，本域 4 条词）

| T6 行 | 项 | 结论 | 读数 |
|---|---|---|---|
| 56 | 「设定预算」 | **对上了** | 老 1 场景（`scenes/goal.yaml:10`）逐字在位；探针路由层 `routeWakeword('设定预算')→bill.goal.write` 命中；域声明 `goal/declaration.ts:16-28` 零改（本票不动词层） |
| 57 | 「看预算」 | **对上了** | 同上（`scenes/goal.yaml:26`）→ `bill.goal.query` 命中 |
| 58 | 「设定目标」 | **对上了** | 同上（`scenes/goal.yaml:42`）→ `bill.goal.write` 命中 |
| 59 | 「看目标」 | **对上了** | 同上（`scenes/goal.yaml:58`）→ `bill.goal.query` 命中 |

词层本票**零改动**（T6 §2 的处置建议就是「不补」，本票只做搬迁）：`test/wake-assets.test.mjs` 与
`test/t721-判据与摘要锁.test.mjs` 的三条摘要锁（74 条内容锁／老 71 条锁／77 条路由锁）全绿。

### §3 键层（第 10–11 行，本域两条键）

| T6 行 | 项 | 改前（T6 实测） | 改后（本票） |
|---|---|---|---|
| 10 | `bill.goal.write` | 可用（弱）· 域页缺口；HTML **992 B／16 行**（16 行空壳页）；专测：**无**（仅 `cli.test.mjs:89-91` 冒烟，含冲突 exit 2）；四处一致 ✗／✓／✓／✗ | **可用**；HTML **76,027 B／2554 行**（写库回执整页）＋ 采集页 75,239 B／2562 行；专测：`test/t730-目标域.test.mjs`（12 条）；四处一致 **✓／✓／✓／✓**（能力声明 `src/goal/commands.ts:19-27`／SKILL 速查／分派走注册表／生成物 `src/cli/registry.ts`） |
| 11 | `bill.goal.query` | 可用（弱）· 域页缺口；HTML **1,100 B／16 行**；专测：**无**（仅 `cli.test.mjs:92` 冒烟）；四处一致 ✗／✓／✓／✗ | **可用**；HTML **78,629 B／2597 行**（预算执行）／**78,599 B／2591 行**（目标进度）；专测：同上；四处一致 **✓／✓／✓／✓** |

两处附带销账：T6 §3.1 的「四处一致矩阵」本域两行由 `—` 变 `✓`；§3 注记里那条「分派层加 `case` 却不登记」
的缺口本域不再产生（两条 `case` 已从 `cmd_read.ts` 删掉，形状由注册表派生）。

### §4 页面层（第 8–11 行，本域 4 张模板）

| T6 行 | 老模板 | 改前 | 改后 |
|---|---|---|---|
| 8 | `目标/budget_form.html`（8981 B／165 行） | **无落点**（`goal_write` 空壳页 992 B） | **有落点**：采集页（缺项／冲突）＋ 回执页（`scene-set-budget.ts` → `template-form.ts`）；`bill.goal.write` 两份产物各一整页 |
| 9 | `目标/budget_view.html`（12467 B／252 行） | **无落点**（`goal_query` 空壳页 1,100 B，两种 op 共用） | **有落点**：`scene-budget.ts` → `template-progress.ts`（读数行／进度卡／占比条／口径行／脚注） |
| 10 | `目标/saving_form.html`（8198 B／151 行） | **无落点** | **有落点**：`scene-set-saving.ts` → `template-form.ts`（同一片页型的第二件） |
| 11 | `目标/saving_view.html`（11998 B／248 行） | **无落点** | **有落点**：`scene-saving.ts` → `template-progress.ts` |

§4.1 的「B · 目标域 4 张」这一类**销账**（4 张全部由「无落点」变「有落点」）。

## 四 T7 §四 裁定逐条自证（判据 3）

| 裁定 | 可判形式（原件的原话） | 结论 | 读数 |
|---|---|---|---|
| 1 · 页面最大一个字写内容，眉标只写「命令名 · 人话归属」 | 跑本域全部读命令 → 产物可见文本里**不出现** `scripts/`、`.py`、`.json`、`bill.`、`_` 形态的内部标识符 | **对上了** | 探针「裁定 1」步：5 张页（进度 2 ＋ 回执 2 ＋ 空态 1）在剔掉复制载荷区与两处「口令原文」`<pre>` 之后，`/bill\./`、`/\.py\b/`、`/scripts\//`、`/goals\.json/`、`/undefined/`、`/NaN/` **全 0 命中**；眉标为「记账 · 目标域」 |
| 2 · 结果型页三条恒出：页内导航 ＋ 口径说明行 ＋ 来源脚注 | 每张产物各含**一个**页内导航块（`ilife-block-toc`）＋ **一行**来源脚注（含窗口与条数）；口径类页面另含一行口径说明 | **对上了** | 探针「裁定 2」步：4 张结果型页各 `<nav aria-label="页内导航">` **恰 1 个**；4 张都有 `ilife-block-caliber` 口径行；4 张的来源脚注都匹配 `数据来源 · … · 起 → 止 · 共 N 条` |
| 4 · 缺值一律 `—`，空窗出空态句与引导句 | 产物仍是完整文档、含空态句与来源脚注；有记录但某字段缺时该处读数等于 `—`；**全产物 grep 不到字面 `undefined` 与 `NaN`** | **对上了** | 探针「裁定 4」步：5 张页 0 命中；空态两支（本月无预算／无目标）各出完整页（空态句 ＋ 引导句 ＋ 脚注 ＋ 口径行）。缺值口径住 `pageParts.ts` 的 `textOrDash`／`money`（空串与缺值一律 `—`；`deadline` 缺时写「不限」） |
| 6 · 过程型采集页不画「同形空卡」 | 把采集页的所有可缺字段清空跑 → 产物里**不出现**值位为「未给」／`—`／`0` 的读数卡网格；缺项标签与字段卡**都在** | **对上了** | 探针「裁定 6」步：`budget-collect.html` 正文里没有 `ilife-block-kpi-card`、没有「未给」；`ilife-block-param-form` 与 `ilife-block-disclosure` 都在 |
| 7 · 一个形状只许一处定义（页面本地不许自造第七种） | 产物 grep `<style` → **命中 1**；grep 颜色字面（`#` 三位／六位、`rgb(`、`hsl(`）→ 在页面装配件里 **0 命中**；grep `@media` → 在页面装配件里 **0 命中** | **对上了** | 探针「裁定 7」步：产物 `<style` 恰 1 处；`src/goal/` 七件页面装配件（pageParts／两模板件／四场景件）去掉注释后 `#…`／`rgb(`／`hsl(`／`@media` 全 0 命中 |
| 9 · 缺项阻断：缺什么就不给复制 | 把一条必填清空跑该命令 → 产物里**不出现**复制指令块，出现缺项阻断条且阻断条点名缺哪一项 | **对上了**（判法＝本图先例 `record-write.test.mjs:289`） | 探针「裁定 9」步：缺项那页的折叠里那条口令是**占位符形态**（`&lt;金额&gt;`），且**任何 `data-t`（复制数据／复制日志两条通道）都不含占位符形态的指令**；折叠里那条口令**没有复制按钮**（`renderPreBlock` 不给 `actionId`）；阻断条逐项点名（`test/t730-目标域.test.mjs` 断言「还差 1 项／还差 2 项」与三条「为什么进不去」）。**「填上再跑 → 复制指令块出现」那一半**：填齐后那次调用**直接写库出回执页**（老侧「采集 → 复制给助手 → AI 再跑」两段合成一次调用，裁定 8），故那一半落在**回执页的复制区**（复制数据／复制日志恒出）与采集页「复制给助手」那一句上——与 #689 对 `record.add` 的同一处判法相同 |
| 11 · 老侧做法与用户自家原则相撞时按原则 | 产物里出现**复制区**（含撤销出口）；产物里**不出现**独立的页脚按钮组 | **对上了**（本域只触达后半条，理由如下） | 探针「裁定 11」步：4 张结果型页都 `<footer>` 内无按钮、也没有 `undoExit`／「撤销这一笔」。**前半条（撤销出口）本域不适用**：裁定 11 的触发实例是「回执页报告一笔**账单记录**」（老 `联动/receipt.html:79`），而目标域的写回执报告的是**预算／目标本身**——老侧 4 张模板里一处退出口都没有（`budget_form`／`saving_form`／`budget_view`／`saving_view` 全篇 grep `撤销` 0 命中），本域没有与之相撞的实例，故不新造退出口；若将来出现新实例，按裁定 11 末句追写进 `688` §二 |
| 12 · 老侧三件已确权的自家原则一并生效 | 产物 grep `--r-xl`／`--pink`／`[data-theme`／`prefers-color-scheme: dark` → **全 0 命中**；墙生成器跑正例 `exit 0`＋反例 `exit 1` 并点名；响应式同时给 `overflow` 与 `clippedCount` | **对上了** | 探针「裁定 12」步：5 张页 4 个禁入 token **0 命中**，主色 `#007aff` 在样式段里（未被改写）。**视觉不做脚本化判定**：本票不新增视觉判定，好不好看由用户肉眼看（`t166:116`）。**墙与响应式两条**属「照抄墙脚本时才生效」的护栏（本票不新增墙脚本）；本域页面的响应式走公共层 `pageUi`，页面本地 0 断点（裁定 7 那一步已量） |

## 五 门禁读数

见 `docs/skills/skill-bill/t730-门禁运行记录.md`（每条带 `GATE-RUN runId=` 声明，可对账）。

**一窗跑全绿的那一次**（`GATE-RUN runId=9f795937-4278-4abb-afd7-b6434d1efdfd`，三票都落地后的安静态）：

| 步骤 | 读数 |
|---|---|
| `tsc -b packages/skill-bill` | exit 0 |
| `gen-cli.mjs --check` | `GEN-CHECK PASS：命令 14 条；能力 6 个（account／analysis／goal／query／setup／write）` |
| `check-ratchet-tight.mjs` | `RESULT: 9/9 ｜ PASS: 棘轮与实况逐项恒等` |
| `check-scene-shape.mjs` | `RESULT: 54/54 ｜ PASS: 场景件只声明差异值…`（本域 4 件场景件在扫描面内） |
| `check-warning-line.mjs` | `RESULT: 13/13 ｜ PASS: 告警线台账齐全且与实况一致`（台账 `--sync` 后） |
| `gen-page-fingerprints.mjs --check` | `RESULT: 32/32 ｜ PASS: 32 张页指纹与账本一致` |
| `node --test packages/skill-bill/test/*.test.mjs` | **403/403 pass ／ 0 fail**（本域靶向 12 条在其中） |
| `docs/skills/skill-bill/t730-探针-真出口.mjs` | `RESULT: 29/29`（`runId=14173caf…`） |

**接线五件的逐件读数**：`cmd_read.ts` 删两个 `case`（455 → **280** 行）· `envelope.ts` 过渡表删两行（8 → **2** 条；62 → **56** 行）·
注册表重生成（8 → **14** 条键，含本域两条）· 棘轮冻结值（`legacyKeys` 8→**2**、`dispatchKeys` 8→**2**、
`registryKeyCount` 8→**14**、`lineCaps.cmd_read.ts` 455→**280**、`lineCaps.envelope.ts` 62→**56**）·
测试硬编码三处 8→**14** ＋ `cli.test.mjs` 的 goal 断言改判 · `AGENTS.md` 台账 `--sync`（13/13）。

**一处如实记**：仓根全量（`node --test "test/*.test.mjs" "packages/*/test/*.test.mjs"`）当刻 3801 条里 **61 条红**，
**逐条落在别的包**（`skill-calorie` 10＋条 · `base-render` 1 条 `#79 三包版本 lockstep` · `plugin-memo-ilife` 1 条 ·
`plugin-bill-ilife` 2 条）。归因读数：三票提交碰到的路径只有 `packages/skill-bill`（94 件）与 `docs/skills`（12 件）
（`git diff --name-only e6125c81..HEAD`），那三个包的文件最后一次改动在 16:45 之前（本票窗口是 19:1x–19:4x）；
`plugin-bill-ilife` 那两条是 #726 退役 `SKILLS_DB_PATH` 之后的老断言（测试仍传旧环境变量，子进程读到 `NODE_TEST_CONTEXT`
即按 `CONFIG_TEST_ISOLATION_MISSING` 响亮失败），且该包在 #681 的 `## Out of scope` 里明写「有別席在途、另有归属」。
**本票的「全量测试」按本图先例＝**skill-bill 包内全量**（403/403），仓根那 61 条红不作为本票的门禁判据、也不由本票处置。

## 六 留白与下一步

1. **判据甲（页指纹账本）仍只覆盖写入域 32 页**：本域 6 张页不在账本里（与 #691 同一留白）。本票以靶向测试 ＋ 真出口探针顶上；扩账本是横向决定，留给下一张页面票。
2. **接线五件里的共用位是三票同窗收敛的**：#730 动手时 #729（分析域）与 #731（开始使用域）在同一工作区在飞，且都持有同几个共用位（`cmd_read.ts`／`envelope.ts`／`registry.ts`／棘轮／测试硬编码／`AGENTS.md` 台账）。本票自己动手的是：`cmd_read.ts` 删本域两个 `case`、`envelope.ts` 删本域两行、`cli.test.mjs` 的 goal 断言、本域四件场景件进场景形状门扫描面；其余三件（注册表重生成、棘轮冻结值、测试硬编码与台账同步）由三票**共同收敛到当刻终态**——终态读数就是上面那一组，逐条与当刻盘上一致（`gen:check`／棘轮／告警线门三道门当场复核）。
