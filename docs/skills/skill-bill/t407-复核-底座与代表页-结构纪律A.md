# t407 复核 · 底座与代表页 · 结构纪律 A（窄面）

被审：`a1fc236`（19 文件＝7 新件 ＋ 8 改件 ＋ 1 测试 ＋ 2 代表页 HTML ＋ 1 证据件）。本次只读复核，未改任何在途件；本件是本次唯一新建文件。
口径：`docs/agents/structure.md:17–58`（五铁律）／`docs/agents/wording.md`（67 行）／施工图 `docs/skills/skill-bill/t407-页面块清单-16词.md` 第一节。
行数口径：节点法（把文件按 LF 换行切分，段数减一）；本包管辖＝`src/**/*.ts`（`test/*.mjs` 不在内，`packages/skill-bill/AGENTS.md`）。

## 一、逐条判定

### 铁律一 · 能力自治（调用方 ＋ 第二个消费者）

| 件 | 实际调用方（grep 数出） | 件头「谁在用」 | 第二个消费者 | 判 |
|---|---|---|---|---|
| `pageFrame.ts` | `record/collect.ts:251`、`record/receipt.ts:112` | `:4–5` 属实 | `src/query/`（`:6`）✓ | 过 |
| `summaryRow.ts` | `collect.ts:224`、`receipt.ts:81` | `:4–5` 属实 | `src/query/`（`:6`）✓ | 过（`moneyDirection` 无外部调用方） |
| `blockedSlots.ts` | `collect.ts:187／188／228`、`write.ts:157／178／98` | `:4–5` 属实 | 写的是 `record/receipt.ts`＋「以后 15 张页」（`:6`）✗ | 缺第二个消费者 |
| `duplicateNote.ts` | `collect.ts:226`、`receipt.ts:90` | `:4–5` 属实 | 记收入／记报销／记一笔（`:6`）✗ | 缺 |
| `prefillNote.ts` | 只有 `collect.ts:193／227／139` | `:6` 自认「本件不出」 | 记收入／拍账单…（`:7`）✗ | 缺（且名义调用点 ② 不调用） |
| `emptyNote.ts` | 只有 `collect.ts:211` | `:5` 自认「还没有空手面」 | 撤销／恢复、改记录（`:6`）✗ | 缺（同上） |
| `failureReceipt.ts` | 只有同共用位 `shared/blockedSlots.ts:89`，**能力目录 0 调用方** | `:5` 自认「本票还没有落点」 | `src/query/`（`:6`）✓ | 半过 |

七件都住 `src/shared/`（本包共用位），调用方 100% 落在 `record/` 一处能力里——按上一轮裁定「留共用位并点明第二个消费者」办，**4/7 没点明**。另：`summaryRow.ts:17` 走 `../policy/category.js` 深路径（`policy/index.ts:1` 已公开 `l1Of`），`prefillNote.ts:16`／`duplicateNote.ts:17` 同法走 `../fetch/db.js`；同包先例在（`render/views.ts:4`、`cli/cmd_read.ts:36`、`write.ts:19–21` 反而走门），记既有习惯，扣轻分。

### 铁律二 · 概念唯一

| 被判定的事 | 定义地 | 别处又写一遍 | 判 |
|---|---|---|---|
| 缺项阻断判定 | `blockedSlots.ts:50`（`blockedItems`） | 无。`collect.ts:81` 的 `missingSlots` 只做「在不在」探针（`collect.ts:10` 写明分工）；槽位名单唯一在 `collect.ts:48`（`RECORD_SLOTS`），`write.ts:30／36／155／177` 引用它 | 过 |
| 重复检测判定 | `duplicateNote.ts:47`（`findDuplicates`） | 无第二处 | 过 |
| 预填规则 | `prefillNote.ts:39`（`prefillOf`） | **三格缺省值又抄一份**：`prefillNote.ts:27–31`（账本「生活」／币种「人民币」）与 `policy/category.ts:11` 的 `DEFAULTS` 逐字重合，而 `collect.ts:23` 本来就在引 `DEFAULTS` | 违纪 |
| 符号即方向 | `summaryRow.ts:35` 自称唯一定义地 | `pageFrame.ts:27–28`、`blockedSlots.ts:37–40`、`collect.ts:51` 各写一遍；`moneyDirection` 无外部调用方，阻断条没引它 | 违纪（宣称不实） |
| 时间缺省 `12:00:00` | 真源 `policy/category.ts:67` | `prefillNote.ts:59` 自己拼串，`summaryRow.ts:55`／`collect.ts:52` 又写文本 | 三处重写 |
| 金额两位小数文本 | `summaryRow.ts:29–31` | `duplicateNote.ts:43`、`blockedSlots.ts:55` 各写一份（`write.ts:67／167` 沿用旧写法） | 三处各写 |
| 空态／失败回执是不是一件事 | 不是一件拆两件：`emptyNote.ts:27` 包 `renderEmptyBlock`（锁 `next` 必填），`failureReceipt.ts:37` 包 `renderFeedbackBlock`（锁 `message` 必填）；施工图 `:18／:19` 也是两行 | 拆分成立 | 过 |

### 铁律四 · 名字取自 HELP

| 件 | 可指认的出处说法 | 判 |
|---|---|---|
| `pageFrame.ts` | 施工图 `:9`「页壳与页头」＋`:10`「类型徽章」（两行） | 能指认，但一件管两块，件名只说第一块 |
| `summaryRow.ts` | 施工图 `:11`「结论摘要行」 | 过 |
| `blockedSlots.ts` | 施工图第三节「缺项阻断条」 | 过 |
| `duplicateNote.ts` | 施工图第三节「重复检测提示条」 | 过 |
| `prefillNote.ts` | 施工图第三节「预填标注」 | 过 |
| `emptyNote.ts` | 施工图 `:18`「空态」 | 过 |
| `failureReceipt.ts` | 施工图 `:19` 写的是「**错误**回执」 | 件名与件头写「**失败**回执」（`failureReceipt.ts:1`、证据件 `:61`），说法漂移 |

无 `utils`／`common`／`core`／`misc`／`types` 工种名 ✓。HELP 侧：`src/triggers/wake-assets.ts` 里「阻断／预填／重复检测／失败回执／空态／摘要行」命中 **0**——HELP 是唤醒词面，这批块名的正源是施工图第一节／第三节，上一轮已裁定接受。

### 铁律五 · 接口小、里面厚

| 件 | 出本目录的名字（raw 导出 → 外部真引用） | 对外名数 | 字段最多 | 判 |
|---|---|---|---|---|
| `pageFrame.ts` | raw 5 → 真引用 2（`PAGE_EYEBROW` `:23`、`TypeBadgeInput` `:45`、`PageFrameInput` `:68` **零外部引用**） | 2 | 7 | 过（自报「3 个值到顶」把零引用导出算了进去） |
| `summaryRow.ts` | raw 4 → 3（`moneyDirection` `:35` 零外部引用，`:9` 说它被阻断条／状态卡引，实际只有本件 `:47` 用） | 3 | 5 | 过＋宣称不实 |
| `blockedSlots.ts` | raw 4 → 4 | 4 | 3 | 过 |
| `duplicateNote.ts` | raw 4 → 3（`DuplicateHit` `:31` 零外部引用） | 3 | 6 | 过 |
| `prefillNote.ts` | raw 4 → 4 | 4 | 4 | 过 |
| `emptyNote.ts` | raw 1 → 1 | 1 | 4 | 过 |
| `failureReceipt.ts` | raw 1 → 1，唯一消费者是同共用位的 `blockedSlots.ts:89` | 1 | 6 | 薄层风险 |

七件对外名都 ≤5 ✓、类型字段 ≤8 ✓、都真干活 ✓。转发层判据：`failureReceipt` 删掉后把两个缺省（`message` 必填、`retryPrompt` 缺省）并进 `blockedSlots.ts`，`record/` 侧一处不少——它自己 `:12–15` 也承认整页替换的落点「本票没有接线」。

## 二、超线实测与用词命中

超线（LF，节点口径；本包告警线 350）：

| 件 | LF | 判 |
|---|---|---|
| `test/record-write.test.mjs` | 334 | **不在管辖**（`test/*.mjs` 不算） |
| `src/record/collect.ts` | **260** | 管辖内最大，未超线 |
| `src/fetch/db.ts` 250 ／ `src/record/write.ts` 213 ／ `src/shared/copyArea.ts` 144 ／ `src/record/receipt.ts` 121 ／ 其余 10 件 ≤111 | — | 全部未超线 |

16 件逐件数过：**超线 0 件**；管辖内最大 `collect.ts` 260，与被审自报「16 件全部 <350、最大 collect.ts 260」一致 ✓。

用词（`wording.md` 禁用表逐词 grep 新增 7 件 ＋ 证据件）：

| 词 | 命中 | 位置 |
|---|---|---|
| 页壳 | 5（源码） | `pageFrame.ts:1／4／10／67／85` |
| 页壳 | 2（证据） | 证据件 `:12／:55` |
| 底座 | 1（证据） | 证据件 `:196`「对着同一套底座铺开」 |
| 空壳 | 2（证据，连带） | 证据件 `:29／:177`，说的是按钮有标签无动作，非「help 模板」义 |

硬命中 8（源码 5 ＋ 证据 3），连带 2，合计 10。「页壳与页头」是照抄施工图行头（`t407-页面块清单-16词.md:9`），但仓规明禁「壳」。「键」作命令讲：**0 命中** ✓（`pageFrame.ts:48` 用「命令名」，`key` 只作标识符）。

## 三、缺口与薄弱处

1. 第二个消费者 4/7 未点明（第一节），且证据件 `:20` 写「七个件头各写了…第二个消费者：`src/query/`」——与源码不符（`blockedSlots.ts:6`／`duplicateNote.ts:6`／`prefillNote.ts:7`／`emptyNote.ts:6` 都不是这句）。
2. `failureReceipt.ts` 本票 0 个能力目录调用方；`prefillNote.ts:6`／`emptyNote.ts:5` 的「两个调用点」里各有一个自认不调用。件头「谁在用」是后来人唯一的指路牌，写成名义调用点等于路牌指错。
3. 零外部引用的导出五个：`PAGE_EYEBROW`／`TypeBadgeInput`／`PageFrameInput`／`DuplicateHit`／`moneyDirection`——要么接上，要么收进件内。
4. 缺省值／时间缺省／两位小数三处口径重写（第一节铁律二表后四行），其中账本／币种与 `policy/category.ts:11` 逐字重合，是最该先并的一处。
5. 代表页两张 HTML 每张 2167 行，本票用 `git show --stat` 只核了体量与落点，未逐行看页面内容——页面面的复核不在本窄面内。

## 四、评分与结论

| 维 | 分 | 扣分理由（`文件:行号`） |
|---|---|---|
| 能力自治 | 14/20 | 4/7 未点明第二个消费者（`blockedSlots.ts:6`／`duplicateNote.ts:6`／`prefillNote.ts:7`／`emptyNote.ts:6`）；`failureReceipt.ts` 能力目录 0 调用方；`summaryRow.ts:17`／`prefillNote.ts:16`／`duplicateNote.ts:17` 走深路径不用门（同包先例在，扣轻） |
| 概念唯一 | 12/20 | 缺省值两份（`prefillNote.ts:27–31` vs `policy/category.ts:11`）；符号即方向四处（`pageFrame.ts:27–28`／`blockedSlots.ts:37–40`／`summaryRow.ts:35`／`collect.ts:51`）；`12:00:00` 三处（`prefillNote.ts:59`／`summaryRow.ts:55`／`collect.ts:52`）；两位小数三处（`summaryRow.ts:31`／`duplicateNote.ts:43`／`blockedSlots.ts:55`）。三件判定本身各只有一处定义 ✓ |
| 命名与 HELP 一致 | 15/20 | 7/7 指得出施工图说法、无工种名 ✓；「失败回执」与施工图「错误回执」不一致（`failureReceipt.ts:1` vs 施工图 `:19`）；`pageFrame.ts` 一件管「页壳与页头＋类型徽章」两块；HELP 侧零出处（前轮已裁定以施工图为准） |
| 接口粒度 | 13/20 | 对外名 ≤4 ✓、字段 ≤8 ✓；零引用导出 5 个（`pageFrame.ts:23／45／68`、`summaryRow.ts:35`、`duplicateNote.ts:31`）；`failureReceipt.ts` 目前是同共用位里的薄层（`:12–15` 自认未接线）；`summaryRow.ts:9` 宣称不实 |
| 用词与超线 | 14/20 | 超线 0 件、自报与实测一致 ✓；用词硬命中 8（`pageFrame.ts:1／4／10／67／85`、证据件 `:12／:55／:196`）＋ 连带 2（证据件 `:29／:177`）；证据件 `:20` 一处不实 |

总分 **68 / 100**。结论：**须整改**（不涉目录重排，五处小改）：

1. 四件件尾各补一行「第二个消费者：`src/query/`（随兄弟图 #403）」，或写明本件为什么只服务 `record/`；证据件 `:20` 同步改成实话。
2. `prefillNote.ts:27–31` 的账本／币种缺省改成引 `policy` 的 `DEFAULTS`（`collect.ts:23` 已有先例）；`summaryRow.ts:55`／`collect.ts:52`／`prefillNote.ts:59` 的时间缺省并成一句，出处指 `policy/category.ts:67`。
3. 方向那句话二选一：让 `blockedSlots.ts:37–40` 与 `pageFrame.ts:27–28` 引 `moneyDirection`，或删掉 `moneyDirection` 并把 `summaryRow.ts:9` 的「唯一定义地」收回去。
4. 用词：`pageFrame.ts` 五处「页壳」改「共享页面模板／页头」；证据件 `:196`「底座」改「已经能用的实现」，`:12／:55` 照施工图行头改写法或用引号标为施工图原文。
5. `failureReceipt.ts:3–6`／`emptyNote.ts:3–6` 的「两个调用点」按实际写成「本票一个调用点 ＋ 后票落点」，别写名义调用点。
