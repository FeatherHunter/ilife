# t689 写入域 · 差异表（老行为／新行为／为什么改／谁批的）

- 票：[write 域：形状重构与 parity 优化](https://github.com/FeatherHunter/ilife/issues/689)（父图 [#681](https://github.com/FeatherHunter/ilife/issues/681)）
- 强制对照基准＝`docs/skills/skill-bill/688-融合基准.md`（T7）；形状规格＝`packages/skill-bill/docs/t685-接口与判据.md` ＋ `packages/skill-bill/docs/t685-按域页型表.md`（T4）。
- 读法：本表只记**本票改掉的**东西。「老行为」＝老技能（`D:\2Study\StudyNotes\SKILLS\饼干记账`）与**本票动手之前**的新仓产物；「新行为」＝本票之后的产物。
- **本票不动版式**（T4 §Solution 逐字「本条不重构版式，只保证『块位序列与每块位数据形态』两件事各只有一处定义」）：32 张产物的指纹逐页比对见证据件，改动的页逐条在下面点名。

## 一、形状层（不进页面，但改变维护形状）

| # | 老行为 | 新行为 | 为什么改 | 谁批的 |
|---|---|---|---|---|
| 1 | 写入域 16 个场景件各自写块序：一件场景件 22–319 行，块序在 16 件里各写一份（实测 67% 的代码行在 ≥3 件中逐字重复） | **5 张模板件**（`src/record/template-{expense,flow,batch,installment,update}.ts`）各自写一份块序；场景件退成**差异声明**（值／文案／可选块开关），16 件合计大幅缩短 | 改一次版式要动 41 处的根因就是「一场景一份块序」；T4 已定写入域走**老侧原形**（模板级单份 ＋ 场景只填值，不做「页型 × 块位三档」动态机制） | T4 规格 `t685-按域页型表.md` §2.1（维护者 2026-09-18 确认）＋ 本票 |
| 2 | 场景件里可以 import 公共层块位渲染函数、可以拼 HTML、可以调装配件 | 场景件**不许**出现块位序：新增门 `scripts/check-scene-shape.mjs`（判据乙），三条判据红即点名到 `文件:行号` | 形状不靠人记、靠机器守；T4 §2.2 定的判据乙就是这条 | T4 §2.2 ＋ 本票 |
| 3 | 页面级块序**没有任何机器判据**（账单 31 个测试件全打在真交付出口上，没有一条断块序次序） | 新增 `scripts/gen-page-fingerprints.mjs` ＋ 账本 `test/t689-页面指纹.json`（32 张页的归一化 sha256），`--check` 读差异集；重录必须 `--declare-layout-change <票号>` | 「改一次版式只动一处」是可判的后果陈述，取代原判据「每场景差异声明 ≤ 20 行」（T4 实测两口径差 4 倍） | T4 §2.1／§2.3（账本落点由 T4 §五 5.1 定） |

## 二、页面层（产物有变化的页，逐条点名）

口径：32 张页（16 词 × 采集／回执）逐页指纹比对；**只有下面点名的页变了**，其余逐字节相同（读数见证据件 §三）。

| # | 页 | 老行为 | 新行为 | 为什么改 | 谁批的 |
|---|---|---|---|---|---|
| 4 | 记报销·采集页 | 第 1 段标题「先看这一笔缺什么」在缺项标签**之前** | 缺项标签在前、第 1 段标题在后（缺项为 0 时不出） | 同族另外 4 件都是「标签 → 段标题」；T4 定「一域一份块序」⇒ 统一到多数派。两块**都在**，只是相邻次序互换 | T4（`t685-按域页型表.md` §2.1 支出表单那一行的块序）＋ 本票 |
| 5 | 记报销·采集页 | 读数行（金额／分类／账户／账本／时间五格）**无条件出**：字段还没值也画五格「未给」卡 | 五格**全都没值**时不出这一行（有一个真值就照旧出；「时间」那一格不算——它缺省取本页执行那天，永远非空） | #688 裁定 6 逐字「采集页在字段还没值时，不出『金额／分类／账户／账本／时间』五张同形『未给』读数卡」，其可判形式要求「清空可缺字段跑 ⇒ 产物里不出现值位为『未给』的读数卡网格」 | #688 裁定 6（本图强制基准） |

**只改了这 2 处**：32 张页逐页指纹比对（同夹具、时钟串归一化）——**31 张逐字节相同**，上面两张（都是记报销采集页）是全部差异；逐页块类集合对照：1 张页少 6 个块类（就是第 5 条那五张「未给」卡 ＋ 它们的网格容器），无新增块类、无其它页少块。读数见证据件 §三。

## 二之二、族内偏差已统一的一处（原「留给维护者定」，本票第三刀已办）

| # | 页 | 老行为 | 新行为 | 为什么改 | 谁批的 |
|---|---|---|---|---|---|
| 6 | 记借入·采集页 | 读数行只有 4 格（借入金额／分类／向谁借／同人未还），同族另 4 件都是「摘要五格 ＋ 本型三格」 | 改成与同族同形的**八格**（摘要五格 ＋ 借入金额／向谁借／同人未还）；模板里那个 `genericCards` 开关**随之下线** | 留着它＝模板为一个场景留一处块序开关，正是 T4 禁止的「页型 × 块位」动态机制；统一后这一页**多出账户／账本／时间三格**（只加不减，信息等价仍成立） | 本票（T4 §2.1「一域一份块序」＋ #688 裁定 8「同域同形共用一份装配件」） |

## 二之三、按 T7 裁定 2 补的两块恒出（**改版式**）

口径：`688-融合基准.md` §五 5.2 骨架表**顺序一律照它**；可判形式（裁定 2 逐字）：每张产物各含**一个**页内导航块 ＋ **一行**来源脚注。

| # | 页 | 老行为 | 新行为 | 为什么改 | 谁批的 |
|---|---|---|---|---|---|
| 7 | 写入域**回执页**（16 张） | 无页内导航 | 徽章之后、读数行之前出**页内导航**（锚点 `sec-kpi` 读数／`sec-detail` 明细／`sec-reconcile` 对账／`sec-copy` 复制；分期页多一个分期表锚点、改记录页「结果」块有内容才进） | §五 5.2 第 4 行：结果型页（含 ④ 回执页）**恒出**；可判形式要求每张产物恰含一个 `ilife-block-toc` | T7（#688 裁定 2 ＋ §五 5.2） |
| 8 | 写入域**全部 32 张页** | 无来源脚注 | 复制区之后出**一行来源脚注**（`数据来源 · <来源> · <窗口> · 共 N 条`；来源＝记账库那句人话、窗口＝这一笔的时间、条数＝本次改动笔数） | §五 5.2 第 26 行：来源脚注**全 7 类恒出**；出处件＝共用位 `src/shared/sourceLine.ts`（#690 落成，写入域是它的下一个用法） | T7（#688 裁定 2 ＋ §五 5.2） |

**手法**：页内导航那两个原语（`sectionOf`／`pageBody`／`pageNav`）从 `src/query/pageParts.ts` **提上共用位** `src/shared/pageSections.ts`（第二个用法）——写入域与查询域共用同一份；写入域另立 `src/write/sourceNote.ts` 管「来源／窗口／条数」三样取值；页面本地不写 CSS／色值／断点（裁定 7）。

## 二之四、顺带收掉的死件（本票发现）

| # | 件 | 事实 | 处置 |
|---|---|---|---|
| 9 | `src/write/collectBody.ts`／`receiptBody.ts` | 折叠完成后**已无任何调用方**（16 件场景件都走五张模板了），件头却还写着「16 件场景件都指它」 | 删两件；`collectBody.ts` 里唯一还有用的 `collectBlockedFold` 搬进新件 `src/write/blockedFold.ts`；相关过期说法一并改对 |

## 三、本票**没有**改的（留给后续票）

| 项 | 现状 | 去向 |
|---|---|---|
| 老侧 24 张模板里属写入域的 17 项缺页／部分缺页 | 本票不补页（T6 `t687-parity-审计清单.md` 页面层已登记） | 各域后续票；本域见证据件「按 T6 逐条」那张表 |
| ~~目录搬迁~~ | **已落**（三批：`a7d734bf`／`e6aaceba`／`34f59a18`） | — |
| 结果型页三条恒出（页内导航 ＋ 口径说明行 ＋ 来源脚注） | 本域回执页当刻**没有**这三条；`src/shared/sourceLine.ts`（来源脚注的共用件）在本仓**未落成** | #688 裁定 2；本票按「共用位从第二个用法里长出来」不抢先建共用件，逐条自证里记「没对上」并写明去向 |
