# t731 开始使用域 · 差异表（老行为／新行为／为什么改／谁批的）

- 票：**#731 开始使用（setup）域端到端搬迁**。老侧＝`D:\2Study\StudyNotes\SKILLS\饼干记账` 的「开始使用」域
  （`scripts/setup/cli.py` 709 行 · `scripts/setup/render.py` 375 行 · `scripts/backup.py` 172 行 ·
  `scripts/db.py` 的建库与写库两个函数 · `templates/开始使用/` 六张模板）；
  新侧＝`packages/skill-bill/src/setup/` 全部件 ＋ `src/fetch/{db,paths}.ts` 里被本域读的那两处
  （落点解析与开库自愈）。
- **老侧引文一律出自只读勘察摘要**`.scratch/t731/old-setup.md`（该摘要逐条带 `文件:行号`，本表照它引；
  摘要里的行号是**老仓的行号**）。凡本表写「摘要 §n」即指该文件的节。
- **新侧引文出自当刻盘上版本，读于 2026-09-19 19:09–19:26**。读的过程中本域被同席改过
  （`src/setup/run.ts` 420 → 419 → 184 → 185 LF；`src/setup/template-wizard.ts` 325 → 358 → 360 → 300 LF；
  19:21 新出现 `src/setup/restore.ts` 与 `src/setup/import.ts`，19:23 新出现 `src/setup/blocked.ts`），
  本域全部件当刻**未提交**。
  故格子里的行号是**读取当刻**的锚点，认锚请以同一格引的那段文本为准（差异见 §六 留白 7）。
- 强制对照基准＝`docs/skills/skill-bill/688-融合基准.md`（§二 D1–D7／§五 5.2／§四 裁定）；
  形状规格＝`packages/skill-bill/docs/t685-按域页型表.md` §2.7。
- 姊妹件：`docs/skills/skill-bill/t691-差异表.md`（账户域）、`t690-查询域-差异表.md`。

## 怎么读这张表

四列固定：`老行为（出处）`／`新行为（落点）`／`为什么改`／`谁批的`。

- 「老行为」写的是**老侧代码真正做的事**（不是它页面上宣称的事）；只要两处不一致，两处都引。
- 「新行为」只写**当刻盘上**的落点，并给 `文件:行号`；本域代码正在被同席改，见上一节那句。
- 「为什么改」分三种：① **老侧缺陷不照抄**（引 `#688` §二 的编号或本表出处）；② **照抄／原样继承**
  （引老侧那一行）；③ 本票自己的口径（写「本票」）。
- 「谁批的」给判过这件事的那一条：`#688` §二 的 D 编号、§四 的裁定编号、`t685` §2.7 的分片行、
  或「本票」；**没判过的**写「未批」并列进 §六 留白。

## 一 一份备份的形状（老＝一个目录；新＝一个文件）

| # | 老行为（出处） | 新行为（落点） | 为什么改 | 谁批的 |
|---|---|---|---|---|
| 1 | 一次备份＝**新建一个以 `%Y%m%d_%H%M%S` 命名的时间戳目录**，再把库与 `goals.json` 按原名拷进去（摘要 §3.1 的步骤表引 `backup.py:63-69`、`:73-84`）；列表读的是「目录名 ＋ 目录内文件大小之和」（摘要 §4.2 引 `cli.py:349-364`） | 一份备份＝**一个文件** `<backup.stem><YYYYMMDD_HHMMSS>.db`，同目录一个同名影子件 `<同名>.goals.json`（`src/fetch/paths.ts:70-73`；`src/setup/backups.ts:107-126`、`:69-72`） | 落点口径按 #726 的配置文件（`backup.dir`／`backup.stem`）；一份备份要有一个单一读数（名字／字节数／时刻），老侧目录口径下「大小」只能把两份文件的字节数并成一个数 | #726 配置口径 ＋ 本票 |
| 2 | 备份目录里有什么就按原名拷什么；`goals.json` 不在只记一句「不存在,跳过」，不改口（摘要 §3.1 引 `backup.py:73-84`） | 影子件名由库文件名派生（`x.db` → `x.goals.json`，`src/setup/backups.ts:69-72`、`:119-122`）；在不在照实报（`hasGoals`，`:30-34`、`:57-64`） | 同第 1 行：名字载体从目录变成文件之后，影子件必须跟着改名，否则同一目录里两份备份的影子件会撞名 | 本票 |
| 3 | `create_backup` 的步骤表里**没有任何 checkpoint／关连接动作**，直接 `shutil.copy2` 拷主库文件（摘要 §3.1 的 `backup.py:61 → 63-68 → 69 → 73-84 → 86-88`） | 拷贝前先 `PRAGMA wal_checkpoint(TRUNCATE)`（`src/setup/backups.ts:117`）再 `copyFileSync`（`:118`） | 新侧开着 WAL（`src/fetch/db.ts:62`）且库句柄归出口持有、处理体不能自己收连接（`src/setup/restore.ts:8-9` 那段硬约束）；checkpoint 达成同一目的——把 WAL 里还没回写的事务落进主库文件 | 本票 |
| 4 | 同一秒内连点两次，得到 `…_2`、`_3` 后缀的时间戳目录（`backup.py:63-68`，摘要 §3.1 幂等栏） | 同名文件已存在就换 `_2`／`_3`（`src/setup/backups.ts:129-137`，由 `:115` 调用） | **照抄／原样继承**「同一秒不覆盖前一份」；只有作用的粒度随形状从目录名落到文件名 | 照抄（`backup.py:63-68`）＋ 本票 |
| 5 | 回执里「这一份里有什么」是**硬编码**「数据库 + 目标(goals.json)」，影子件实际不在也不改口（`cli.py:328`，摘要 §3.2） | 照实报「库 ＋ 目标」／「只有库」（`src/setup/template-receipt.ts:54-60`；读数来自 `src/setup/backups.ts:57-64`） | 老侧那一格与它要报的事实脱钩——同一份数据里就有影子件在不在 | 本票 ＋ `#688` §四 裁定 4（读数照实报） |
| 6 | 报的是 **CLI 完成时刻** `datetime.now()`，不是备份本身的名字或 mtime（`cli.py:326`，摘要 §3.1③） | 报备份文件自己的时刻：文件名里的 `YYYYMMDD_HHMMSS`，解不出退回文件 mtime（`src/setup/backups.ts:44-48`、`:61`）；回执用这一格（`src/setup/template-receipt.ts:52`） | 一份备份的身份就是它的文件名（第 1 行），时刻与身份同源 | 本票 |
| 7 | 每行的「大小」＝该目录下所有文件的 `st_size` 之和（`cli.py:350-354`）；`files` 只有文件名、没有单个大小（摘要 §4.2 引 `cli.py:353`／`:363`） | 每行＝一份 `.db` 的字节数，影子件的字节数单独一格（`src/setup/backups.ts:58-65`；页面 `src/setup/template-list.ts:62-67`、`:69-76`、`:79-91`） | 同第 1 行：并成一个数之后，看的人分不出库有多大 | 本票 |
| 8 | `>=1024` 就写成 `(n/1024).toFixed(1) KB`，再大也没有 MB 档（摘要 §3.3 引 `backup.html:115`；§4.3 引 `backup_list.html:124`） | `B`／`KB`／`MB` 三档（`src/setup/backups.ts:169-174`） | 老侧没有 MB 档，几十兆的备份会显示成几千 KB | 本票 |
| 9 | 备份目录不存在／为空时**不报错**，打印 `⚠ 暂无备份`／`⚠ 备份目录为空` 并返回空表（`backup.py:96`／`:110`，摘要 §4.1） | 目录不存在即返回空表（`src/setup/backups.ts:76-85`）；页面出空态句＋「怎么来第一份」的引导（`src/setup/template-list.ts:97-103`、`:120-122`） | **照抄／原样继承**「没有备份不是错误」；空态句照老侧那一句的意思（`backup_list.html:117` 的 `emptyState`） | 照抄（`backup.py:96`／`:110`）＋ `#688` §四 裁定 4（空态句与引导句） |

## 二 恢复：顺序与选中项

| # | 老行为（出处） | 新行为（落点） | 为什么改 | 谁批的 |
|---|---|---|---|---|
| 10 | **先覆盖**（`backup.py:128-131`）**再验库**（`:139-142`）；校验失败 `sys.exit(1)` 并点名现状备份（`:144-146`）——损坏的备份会先把现状覆盖掉才报错（摘要 §5.1⑤⑥、§8.5 末句） | 确认之后**先验那一份备份**读得动（`src/setup/restore.ts:72`），不通过就出阻断页、一点现状都不动（`:41-47`，阻断页整页在 `src/setup/blocked.ts:24-73`）；验库留在覆盖之后（`src/setup/restore.ts:113`） | 老侧的顺序让「验」这一步拦不住损失；摘要 §8.5 末句点名「端口应把校验挪到覆盖之前」 | 本票（照摘要 §8.5 的处置） |
| 11 | 真正覆盖之前**无条件**给现状造一份备份，没有开关可跳过：`# 1. 恢复前自动备份现状` ＋ `safety = create_backup()`（`backup.py:122-124`） | 同（`src/setup/restore.ts:91-92`），并把这一份的名字写进回执与口令（`:114-117`、`:128-129`；页面 `src/setup/template-wizard.ts:146-149`） | **照抄／原样继承** | `#688` §二 D1「原样继承」 |
| 12 | 显式覆盖警告（原文：`⚠️ 恢复将覆盖当前数据` ＋「恢复前会自动备份现状,若恢复后不满意可再用「恢复备份」回到现状备份」）＋ 步骤里再预告一次（`restore.html:159`、`:137`） | 警告改由结论条出，后面跟一行口径写清「不用你记着」与「拿刚刚那一份回去」（`src/setup/template-wizard.ts:119-121`） | **照抄／原样继承**（D1 第二、三件：显式警告 ＋ 警告里写「怎么回去」） | `#688` §二 D1 |
| 13 | 恢复后验库：`SELECT 1 FROM bills LIMIT 1`（`backup.py:140-142`）；失败把现状备份路径编进错误串（`:145`） | `verifyRestored` 新开一条连接跑同一句 SQL、验完即关（`src/setup/backups.ts:155-166`）；失败在页与 message 里点名现状备份（`src/setup/restore.ts:113-117`） | **照抄／原样继承**（同一句 SQL ＋「失败路径必须留回滚点」那一条） | `#688` §二 D1（第三件） |
| 14 | `sel` 只在页面加载时取一次（`restore.html:125`）：详情卡四行读它（`:141-145`）、步骤 1 的 detail 也读它（`:135`）；点另一份备份只改 `curName` 与高亮（`:167-169`），复制用新选的那一个（`:174`）⇒ **详情＝旧项、高亮与口令＝新项** | 「选中的那一份」只有一处 `RestoreWizardInput.selected`（`src/setup/template-wizard.ts:69-79`）；清单行的「选中的」标记、详情卡四行、口令三处都从它派生（同件 `:127-145`；口令由 `src/setup/restore.ts:64` 交给场景件 `src/setup/scene-restore.ts:31-34`） | **老侧缺陷不照抄**：D5 的可判形式＝切换选项后，产物里「详情」与「确认文本」点名的必须是同一项 | `#688` §二 D5（老侧最高危缺陷） |
| 15 | 点名的备份不在目录里时，向导页把 `selected` 退回 `backups[0]`（`render.py:281-282`，摘要 §5.2）——页面照样给你看最新那份；真跑恢复时才由 `backup.py:118-120` 报「备份不存在」并 `sys.exit(1)` | 出阻断页、点名「备份目录里没有这一份」，不退最新的一份（`src/setup/restore.ts:40-47`） | 不猜：老侧那处让「确认页指的那一份」与「你点的那一份」不是同一份，与 D5 同族 | `#688` §四 裁定 9（缺项阻断）同判法 ＋ 本票 |
| 16 | 三段文字与步骤号全是字面：第 1 步圆圈画 `✓`、`:136` 写死 `2`、`:138` 写死 `3`（`restore.html:133-139`），总数在页面上**没有任何一处**；初始化那边定义了会自增编号的 `stepIcon`（`init_wizard.html:131`）却从未被调用（摘要 §7b、§7e-3） | 编号＝数组下标（`src/setup/steps.ts:33-36` 的 `numbered`），总数＝数组长度；页面摊成徽章列（`共 N 步` ＋ 每步 `第 N 步 · 步骤名 · 状态`）＋ 步骤说明小表（`src/setup/pageParts.ts:83-90`、`:94-111`） | **老侧缺陷不照抄** | `#688` §二 D6 |
| 17 | 恢复向导三步，状态徽章写死 `已完成`／`待确认`／`待执行`（`restore.html:134-138`）；「恢复前会自动备份现状」塞在第 2 步的说明里（`:137`） | 四步：`选一份备份`／`确认要覆盖`／`覆盖前先备份现状`／`恢复 + 验证`；状态四档 `done／current／todo／blocked` 按事实算（`src/setup/steps.ts:94-125`，四档的中文名 `:26-31`） | D1 的三件套要能在页面上逐件对上——「无条件现状备份」在老侧只是说明句，不是独立一步 | `#688` §二 D1 ＋ D6（本票的落法） |

## 三 初始化与初始化状态

| # | 老行为（出处） | 新行为（落点） | 为什么改 | 谁批的 |
|---|---|---|---|---|
| 18 | 「只读验证」那一步的 detail 是**硬编码** `SELECT 1 ✓ · bills 表 ✓ · 现有记录 {n} 条`（`cli.py:262`），而同一段代码实际只跑了 `PRAGMA table_info` 与 `SELECT COUNT(*)`（`cli.py:252-253`）——从未执行 `SELECT 1`，也没校验 `bills` 表在不在（摘要 §1.2 末「注意（不要照抄）」） | 真跑 `SELECT 1` 与 `SELECT COUNT(*)`（`src/setup/run.ts:60-66` 的 `verifyReadable`），步骤说明照真结果写（`src/setup/steps.ts:71-77`） | 老侧那句是假读数（摘要 §7e-5） | 本票（照摘要 §1.2 的「应改成真跑真报」） |
| 19 | 环境检测六项：操作系统／Python／PyYAML／数据目录／HTML 目录／SKILL 目录（`cli.py:79-153`，摘要 §1.2 的六项清单） | 两项：`运行环境`（node 版本 ≥22.13）／`数据目录`（真写探测）（`src/setup/run.ts:78-81`） | 老侧六项里四项是老侧那套 Python 宿主的事实（Python 版本／PyYAML／HTML 产物目录／`SKILL.md`），TS 侧没有对应物；留下的两项与出口预检同一条线 | 本票 |
| 20 | `env.checks` 为空时：就绪分支回落渲染**绿色** `✓ 全部通过`（`init_wizard.html:155`，同一行表达式里 `envItems \|\| '<div class="v ok">✓ 全部通过</div>'`），未就绪分支回落成一个空 `.env-grid`（`:163`）——同一份数据两种相反表现 | 一项都没取到只出一句「这一页一项环境事实都没取到（不是「全部通过」——没测到就是没测到）」（`src/setup/pageParts.ts:116-119`）；步骤条那一格走 `blocked` 而不是 `done`（`src/setup/steps.ts:53-56`） | **老侧缺陷不照抄**（假绿）：新侧「没数据」只能有一个说法 | `#688` §二 D7 |
| 21 | 四条步骤名里只有前两条来自 CLI（`cli.get("steps")`，`render.py:147`），后两条在 `render.py:154-157`（未 ready）／`161-165`（ready）写死；ready 分支还用 `steps[:2] + [...]` 截断（`render.py:160`）；页面上的「4」只出现在写死文案里（副标题 `render.py:170`、chip `init_wizard.html:127`） | 四步全由事实算（`src/setup/steps.ts:52-79`），总数由 `steps.length` 出（`src/setup/pageParts.ts:86-88`） | 老侧同一技能里「数据驱动」与「字面」两套做法并存（D6）；`steps[:2]` 那一处还会静默少渲染（摘要 §7e-4） | `#688` §二 D6 ＋ 本票 |
| 22 | 「数据存在」这一重**可判为假**：库文件不存在时 detail 是 `数据库文件不存在(未初始化)`，`ready` 被这一重挡住（`cli.py:161-164`／`:209`，摘要 §2.2） | 恒为真：`src/setup/status.ts:76-79` 写死 `ok: true`，`:97` 写死 `dbExists: true`——出口在调处理体之前就已经开了库（`src/fetch/db.ts:52-58` 打开即建文件） | 新侧「还没初始化」这件事改由**表在不在**承担（`tableExists`／版本 `—`，`src/setup/status.ts:68-73`），不再由「文件在不在」承担 | 本票（记下这处口径差异） |
| 23 | 迁移提示指的是「运行某个迁移脚本」并**印出脚本路径** `python3 scripts/migrations/add_bills_check_constraints.py`（`cli.py:186-188`，同句在 `:197-199` 复述，摘要 §2.1／§2.4） | 不印脚本路径（`#688` 裁定 1），改报「本次打开时自动补了什么」：`repaired`（`src/fetch/db.ts:24-29`、`:60-71`；`src/setup/status.ts:37`／`:104`；页面 `src/setup/template-list.ts:179-188`） | 裁定 1（脚本路径一律不上屏）；新侧库句柄打开时就把 v1.0 补成 v2.0，「要你手工跑脚本」这件事不存在了，能报的只有「补了什么」 | `#688` §四 裁定 1 |
| 24 | 迁移提示只在两种情形出：「缺 `deleted_at` 列」或「版本不达标」（`cli.py:186-188`／`:197-199`，摘要 §2.1） | 迁移块在 `repaired` 非空**或**结构真的还不是当前版本时出（`src/setup/template-list.ts:179-188`）；而**新建库**也会往 `repaired` 里推 `建表`（`src/fetch/db.ts:64-67`）⇒ 刚建的库上「初始化状态」会出 `⚠️ 需要迁移` | 记差异（触发条件比老侧宽）；当刻被同域测试点名为红：`packages/skill-bill/test/t731-setup-域.test.mjs` 的「当前版本的库：三重全过、没有迁移块」实测 `repaired` 得 `['建表']`、期望 `[]` | **未批**（见 §六 留白 2） |
| 25 | 零记录时照实显示「现有记录 0 条」（`init_status.html:145`；`#688` 裁定 4 把它列为正面例子） | 照实写 0（`src/setup/status.ts:59-63` 的 `countOf`、`:29-30`；`src/setup/run.ts:96-97`／`:117-118`；来源脚注条数 `src/setup/template-list.ts:216`） | **照抄／原样继承**（0 是真实读数，不吞） | `#688` §四 裁定 4（正面例子） |
| 26 | `init --check` 的 help 原文写「只读环境检测(向导步骤 1-2,不建库)」（`cli.py:655`），实际仍 `mkdir` 数据目录与 HTML 目录并写删 `.write_probe`（`cli.py:114-118`、`:128-133`，摘要 §7e-6） | 可写探测照旧真写一个 `.write_probe` 再删（`src/setup/run.ts:41-51`），但**只在 `op=init` 这一支出页时跑**（`:78`）；「只看状态」那一支不跑探测（`op=init-status`，`:113-128`） | **照抄／原样继承**老侧「可写才报可写」的真探测；把「环境检测」与「只看状态」分给两片页型是 `t685` §2.7 的分片（老侧两件事挂同一次调用） | 照抄（`cli.py:111-125`）＋ `packages/skill-bill/docs/t685-按域页型表.md:115`（向导片）＋ 本票 |

## 四 导入（D2 四件 ＋ 事务 ＋ 三档读数）

| # | 老行为（出处） | 新行为（落点） | 为什么改 | 谁批的 |
|---|---|---|---|---|
| 27 | 页面只有文件总行数（`import.html:158`／`:161`）；确认文案只有文件路径与列映射、**没有行数**（`:172-174`）；CLI 侧 `build_import_payload` 没有「预计写入行数」字段（`cli.py:548-558`），写库前也没有计数确认（`cli.py:587-645`）（摘要 §6.6(i)） | `planImport` 先算出三档，页面写「本次将新增 N 行，不覆盖已有记录」（数据 `src/setup/importer.ts:209-241`；页面 `src/setup/template-wizard.ts:191-195`；载荷 `src/setup/import.ts:68-71`） | **老侧缺陷不照抄**（D2 第一件：导入前明示会写多少、不覆盖什么） | `#688` §二 D2 |
| 28 | `cmd_import` 全函数（`cli.py:562-645`）**没有任何 backup 引用**；`create_backup` 的唯一调用点在 `cmd_backup_create`（`cli.py:313`），导入路径 0 次（摘要 §6.6(ii)） | 确认那一路先 `createBackup`，再写库（`src/setup/import.ts:91-95`），备份文件名进载荷与页面（`:99-103`；页面 `src/setup/template-wizard.ts:197`） | **老侧缺陷不照抄**（D2 第二件：写前自动备份一次） | `#688` §二 D2 |
| 29 | `重复`／`已存在`／`dedup` 在 `scripts/setup/cli.py` 里各 **0 命中**；写库是纯 `INSERT INTO bills (...) VALUES (...)`，无唯一约束、无先查后插（`db.py:85-89`，摘要 §6.6(iii)）⇒ 同一份 CSV 导两次＝两批新记录 | 以「时刻｜金额｜分类｜备注」四件签名（`src/setup/importer.ts:110-112`）比库内已有记录（`:210`）与本次已排过的行（`:233`），重号的进 `duplicates`、不写库 | **老侧缺陷不照抄**（D2 第三件：重复导入检测） | `#688` §二 D2 |
| 30 | `import.html` 里 `imported`／`failed`／`failed_count` 各 **0 命中**；页面的数据源是 `import --preview`（`render.py:313`），而预览分支在写循环之前就 `return`（`cli.py:573-585`）⇒ 页面载荷里根本没有结果字段（摘要 §6.6(iv)、§7d） | 结果卡出结论句（成功 N 行／失败 N 行）＋ 读不出来的行逐条表（`src/setup/template-wizard.ts:199-209`），读数来自 `applyImport` 的返回（`src/setup/importer.ts:243-267`） | **老侧缺陷不照抄**（D2 第四件：页面级结果卡） | `#688` §二 D2 |
| 31 | 逐行 `insert_record`，每次调用内部都 `init_db()`（`db.py:82`）并按行 `commit()`（`db.py:89`）⇒ 中途失败已写的行不回退；库原本不存在时还会**静默建库**（摘要 §6.1 关键条、§7e-8） | 整批一个 `BEGIN`／`COMMIT`（`src/setup/importer.ts:248`、`:261`），异常 `ROLLBACK` 并整批当失败抛出（`:262-265`） | **老侧缺陷不照抄**（摘要 §7e-8 点名「导入非事务」） | 本票 |
| 32 | `total = len(body)`（`cli.py:632`），而全空行在 `cli.py:597-598` 被 `continue` 跳过 ⇒「成功 N 行 / 共 M 行」的分母偏大（摘要 §7e-9） | 三档分开报：将新增／库里已有／读不出来（`src/setup/importer.ts:209-241`；页面 `src/setup/template-wizard.ts:191-195`）；空行照跳且**不计入任何一档**（`src/setup/importer.ts:217`） | **老侧缺陷不照抄**（一个数里混着三种行） | 本票 |
| 33 | 认七列，含「收支方向」（`cli.py:557` 的 `columns`，与页面 `FIELD_LABELS` 同集同序，摘要 §6.2／§6.4）；方向列含「收」取正、否则取负（`cli.py:614-617`） | 认六列，**没有方向格**（`src/setup/importer.ts:28-32`、`:44-45`）；金额那一格经 `validateAmount` 校验后原样带符号进库（`src/setup/importer.ts:224`；`src/shared/category.ts:48-56`「带符号浮点，支出负/收入正，零阻断」） | 金额的正负本来就在金额那一格；老侧再收一格方向＝同一件事两处输入 | 本票 |
| 34 | `--mapping` 只在显式给出时生效，非法片静默丢弃（`cli.py:532-533`、`:537-539`）；越界列在 `_col` 里返回 `""`（`:601-605`）⇒ 表现为运行时错误（`日期无法识别: ''`）而不是参数错误（摘要 §7e-10） | 认不得那一片就抛 `POLICY_BAD_INPUT` 并给出写法（`src/setup/importer.ts:176-195`），越界列号不静默 | 不猜（老侧把参数错误拖到运行时才报） | 本票 |
| 35 | 预览取 body 前 8 行（`cli.py:545-546`），页面标题写「前 min(8,N) 行预览」（`import.html:161`）；必填标记是前端硬编码 `date/amount/category`（`import.html:136`，摘要 §9） | 同 8 行（`src/setup/importer.ts:62-63`；页面 `src/setup/template-wizard.ts:180-187`、`:212`）；必填三列由数据件出（`src/setup/importer.ts:44-45`，页面 `src/setup/template-wizard.ts:175` 渲染成「（必填）」） | **照抄／原样继承**——D2 明写「确认门形态可继承老侧的好那一半：把机器猜的每一列都写明猜了哪一列、哪些必填」 | `#688` §二 D2 |
| 36 | `scenes/setup.yaml:82` 标了 `type: 向导`，但页里没有步骤模型：只有两张静态区块标题卡（`列映射` `import.html:159`／`前 N 行预览` `import.html:161`）与写死的 chip `● 列映射向导`（`import.html:121`）（摘要 §6.5） | 四步：`读文件`／`列映射`／`确认将新增几行`／`导入并出结果`（`src/setup/steps.ts:141-175`） | 「向导」这一片页型在本域只有一份写法（D6 的按状态算），老侧那一页只是标了类型 | `#688` §二 D6 ＋ `packages/skill-bill/docs/t685-按域页型表.md:115` |
| 37 | 导入没有 `--yes` 之类的闸门（`cli.py:671-679`，摘要 §7d）；页面点「📥 确认导入」只是把一句话复制给 AI（`import.html:176-178`），真正写库是 AI 另外跑的一次调用 | `confirm` 只认 `true`（`src/setup/params.ts:92-95`）；不给就只出向导页（`src/setup/import.ts:73-89`、`src/setup/restore.ts:49-68`），给了才动数据 | 老侧「页面」与「真正写库的那次调用」之间没有可判的联系；一命令一页（裁定 8） | 本票 ＋ `#688` §二 D2（导入这一支的「写前明示」） |

## 五 页型与出口的形状

| # | 老行为（出处） | 新行为（落点） | 为什么改 | 谁批的 |
|---|---|---|---|---|
| 38 | 六个场景的页面**都不写库**，页面只复制一句话交给 AI（摘要 §7e-11 逐页行号：`restore.html:173-175`／`init_wizard.html:157-159`、`165-167`／`init_status.html:149-153`／`backup_list.html:119-121`、`132-134`／`import.html:176-178`）；初始化向导页的数据源还是只读的 `init --check`（`render.py:75`） | 一条命令一次调用内做完：`src/setup/run.ts:166-185` 一处按 op 分流，初始化／一键备份／查看备份／初始化状态四条当次就干活；要动数据的两条由 `confirm` 分两次调用（`src/setup/restore.ts`／`src/setup/import.ts` 各一件） | 一命令一页：页与库不再是两次互不相干的动作 | `#688` §四 裁定 8 |
| 39 | 六张模板六个形状（`backup.html` 与 `backup_list.html` 共用一个唤醒词的两个场景；摘要 §0 的场景表、§7e-11） | 三片页型盖六个场景：向导 3／结果回执 1／记录列表 2（`src/setup/params.ts:39-47` 的 `PAGE_OF_OP`；六件落点表 `src/setup/scene.ts:51-53`） | `t685` §2.7 的三行分片；同形场景共用一份装配件 | `packages/skill-bill/docs/t685-按域页型表.md:115-117` ＋ `#688` §四 裁定 8 |
| 40 | 六张模板各带自绘 `<style>` 与页面级 JS（例：`restore.html:133-139` 的步骤串、`init_wizard.html:131` 的 `stepIcon`、`import.html:129-140` 的映射下拉与 `buildPrompt()`） | 页面只 import 公共层区块（`src/setup/template-wizard.ts:24`／`template-receipt.ts:22`／`template-list.ts:22`）；产物 `<style` 恰一处、禁入 token 0 命中（`docs/skills/skill-bill/t731-探针-真出口.mjs:245-253` 的断言，当刻实测 3 张页过） | **老侧缺陷不照抄**：一个形状只许一处定义（老侧 24 件各一份色板） | `#688` §四 裁定 7 |
| 41 | 徽章列三枚：`⚡ 唤醒词`／`● 4 步零决策`／`🕒 {generated_at}`（`init_wizard.html:125-128`，摘要 §1.3） | 页面名＋口径两枚 chip ＋ 一枚状态徽章（`src/setup/pageParts.ts:63-79`）；生成时刻只落在复制日志那一格（`src/setup/pageParts.ts:177-199` 的 `actionAt`） | 页面第一眼要说「这一页是什么、现在到哪儿了」；唤醒词属口令那一格、生成时刻属日志那一格 | 本票 |
| 42 | 数据／日志两通道由公共组件 `actionBar` 注入 `#actionbar-zone`（摘要 §1.3 第 8 项、§2.3 第 7 项），页面本地另有 `window.copyText(...)` 的按钮（`import.html:176-178`） | 复制区走共用件、恒双按钮三格式（`src/setup/pageParts.ts:177-199`）；过程型三页各两颗按钮（探针 `:238-244`，当刻实测过） | **裁定 5**：过程型页复制区恒双按钮、无同名标题、日志第 4 段写命令原文；老侧两套复制入口并存 | `#688` §四 裁定 5 |
| 43 | 六张模板没有独立 `<footer>`、也没有页内导航；页脚语义由 `.actions` 与 Base `actionBar` 承担（摘要 §12 末条） | 结果型三页恒出页内导航＋来源脚注（`src/setup/template-list.ts:104-129`、`:195-216`；`src/setup/template-receipt.ts:84-104`）；向导三页只出脚注、**不出**导航（`src/setup/template-wizard.ts:217-254`） | 裁定 2（结果型页三条恒出）；过程型不出导航是 `#688` §五 5.2 第 4 行那一处唯一按页类的例外 | `#688` §四 裁定 2 |
| 44 | 复制信封由 `render.py` **另手写一套键名**，与 CLI 返回的键对不上：`render.py:209` 取 `schema_version`、`:211` 取 `exists`、`:212` 取 `schema_version`、`:327` 取 `rows`——四个键在 CLI 的返回里都不存在（真实键是 `version`／`db_exists`／`total_rows`）⇒ 信封里「存在」恒为「否」、schema 恒空或 `(缺失)`、导入恒「0 行」（摘要 §7e-1／2、§11 的四行表） | 页内信封与出口载荷由**同一个对象**摊开：`src/setup/run.ts:14-17` 的口径，`:98-102`／`:120-124`／`:137-141`／`:154-160` 各处一处造 `receipt`、两处用；没有第二份手写键名 | 老侧四处是同一类笔误（两处数据源各自手写键名，改一处忘另一处） | 本票 |
| 45 | 命令体异常被 `except (ValueError, RuntimeError)`／`except Exception` 吃掉（`cli.py:692-703`），`main()` 里没有任何 `sys.exit`（`cli.py:650-705`）⇒ 失败时**退出码仍是 0**，失败只体现在文本／JSON（摘要 §0 末段、§7e-7） | 业务失败也是 exit 0，但载荷里 `ok:false` ＋ message 点名（`src/setup/run.ts:171-177`）；缺项那一路出的是一整页阻断页（`src/setup/blocked.ts:24-73` 的 `blockedWizardDoc`）；非零码只给出口自己的坏参数档（`src/cli/cmd_read.ts:46` 的 `fail(2)`；当刻实跑 `--params` 不成 JSON 得 `ERR 2`，本域各种失败实跑都是 exit 0） | 记差异：老侧失败没有可判的结构（摘要 §7e-7 要的「失败有非零退出码」在本域**未实现**）；本域取的是「阻断页 ＋ `ok:false`」判法 | 本票（与摘要 §7e-7 的建议不一致，见 §六 留白 5） |
| 46 | `scenes/setup.yaml` 六条场景、五条唤醒词；「备份」这一条词挂着两个场景：一键备份（`setup.yaml:42-50`）与查看备份（`setup.yaml:51-59`）（摘要 §0 的场景表；`t687-parity-审计清单.md:125` 记「老 **2 场景**」、`:245` 记「70 唯一词、`备份` 一词两场景」） | 域声明五条词条、六条场景，同一条词的两个场景各一件（`src/setup/declaration.ts:43-63`；二级组 `:94-98`） | **照抄／原样继承**老侧那条形状（一词两场景各出对页） | 照抄（摘要 §0）＋ 本票 |

## 六 留白

1. **页指纹账本只覆盖写入域 32 页**：`packages/skill-bill/test/t689-页面指纹.json` 当刻 `pages` 恰 **32** 个键，
   逐个是写入域 16 个场景的「采集／回执」——本域六张页**不在账本里**，本域改版式不会让那道门变红
   （与 `docs/skills/skill-bill/t691-差异表.md` 记的同一条留白；扩账本是横向决定，留给下一张页面票）。
   另一道页面门（判据乙 `packages/skill-bill/scripts/check-scene-shape.mjs`）的扫描面**已纳本域六件场景件**
   （`packages/skill-bill/AGENTS.md` 的「页面两道门」一节记扫描面＝全包 `src/*/scene-*.ts`）——
   本域的盲区只在页指纹那一道。
2. **同域测试当刻 4 条红**（读数见 §七），其中两条要本域收口：① 新建库上 `repaired` 得 `['建表']`，
   「补过什么」是否该把「建库」排除；② `envChecksOf` 空表那一句里仍含「全部通过」四字（否定句的一部分），
   而同域测试按子串判 ⇒ 红。另两条是测试自身的口径问题（色值正则把注释里的票号当色值；行数断言把
   一版 419 LF 的 `run.ts` 报成超线）。本表不判这四条谁对。
3. **两处新侧注释与摘要不合**（本表照实记、未改件）：① `src/setup/backups.ts:9` 写「老侧 `copyFileSync`
   静默覆盖同一份」——摘要读到的是老侧给目录名追加 `_2`／`_3`（`backup.py:63-68`），没有静默覆盖；②
   `src/setup/backups.ts:69` 写影子件名是「老侧同一算式 `name.replace('.db', '.goals.json')`」——
   摘要里老侧是把备份目录里的文件按原名拷回（`backup.py:73-84`、`:128-131`），没有这条改名算式。
4. **一处任务书说法在摘要里找不到出处**：老侧「先关掉连接再拷」——摘要 §3.1 的步骤表里
   `create_backup` 只有 `mkdir → 目录命名 → 拷库 → 拷影子件 → 打印`，没有关连接／checkpoint 动作；
   `backup.py:72` 是取库路径、`:73-77` 是 `shutil.copy2`。本表未采信（§一 第 3 行按摘要写）。
5. **失败面与摘要 §7e-7 的建议不一致**：§7e-7 记「端口应让失败有非零退出码」，本域当刻只做到「坏参数
   exit 2」（`src/cli/cmd_read.ts:46`），业务失败仍是 exit 0 ＋ `ok:false`（§五 第 45 行）。
6. **标点那一处没有判过的出处**：本仓页内中文文案的标点规矩当刻在 `docs/agents/wording.md` 里查不到
   明文（该文件「标点」「全角」「半角」三词 0 命中）；同一批件里全角与半角两种写法并存。
7. **本域全部件当刻未提交，且读的过程中被同席改过**：`git status` 对本域逐件 `??`；
   `src/setup/run.ts` 420 → 419 → 184 → 185 LF、`src/setup/template-wizard.ts` 325 → 358 → 360 → 300 LF；
   19:21 新出现 `src/setup/restore.ts`（133 → 134 LF）／`src/setup/import.ts`（115 → 116 LF），
   19:23 新出现 `src/setup/blocked.ts`（73 LF，缺项阻断页从模板件搬了出来）。故本表的行号是
   **读取当刻（19:09–19:26）**的锚点，认锚请以格子里引的那段文本为准。
8. **新侧件头引的 `t687` 行号对不上**：`src/setup/scene.ts:8-10` 与 `scene-backup-create.ts:4-5` 引
   「`t687-parity-审计清单.md` §2 第 66-70 行」那处；当刻 `t687-parity-审计清单.md:66-70` 读到的是写入域「记收回／记偿还／
   记分期／改记录／撤销」五行，「备份」的词条在 `:125`（§2 第 68 行）。

## 七 复跑

读数一律经仓规的锁排队。三条命令如下（`<dist 版本>`＝当刻 `packages/skill-bill/dist/`，
本表的读数取自 2026-09-19 19:18:20 那一次构建之后、同席开始重构之前的那一份；
两条门都从 `dist/` 取件——测试侧见 `packages/skill-bill/test/t731-setup-域.test.mjs:15-24` 的
`await import('../dist/setup/…')`）。

```powershell
# ① 同域测试（33 条）
node tooling/run-locked.mjs --ticket 731 -- node packages/skill-bill/test/t731-setup-域.test.mjs
```

当刻读数：`tests 33 · suites 8 · pass 29 · fail 4`（exit 1）。四条红逐条：

- `当前版本的库：三重全过、没有迁移块`——刚建的库上 `repaired` 实测 `['建表']`、期望 `[]`（见 §六 留白 2）。
- `环境检测那一格：空表只出「没测到就是没测到」，不出假绿`——受测那句里含「全部通过」四字（否定句），断言按子串判。
- `页面装配件里不写色值／断点／自绘样式段`——正则 `#[0-9a-fA-F]{3,6}` 把注释里的票号（`#731`／`#688`）当色值。
- `本域各件行数都在包内告警线（350 LF）以内`——那一版 `run.ts=419`（当刻已拆成 184 ＋ 两件）。

```powershell
# ② 真出口探针（19 条 ＋ 路由层）
node tooling/run-locked.mjs --ticket 731 -- node docs/skills/skill-bill/t731-探针-真出口.mjs
```

当刻读数：`RESULT: 7/19`（exit 1）。失败分三类：① 临时配置基座的落点——探针按 `<cfg>/biscuit_accountant.db`
与 `<cfg>/backups` 找，实况是 `<cfg>/data/…`（`src/fetch/paths.ts:45-55` 的 `dataDir` 口径）；
② 那一版出口 JSON 的 `data` 只有 `ok`／`message`（探针读 `data.version`／`new_rows`／`imported`／
`selected`／`safety` 等十一处）；③ 路由层 `routeWakeword('导入', {})` 因域声明的 `needs: ['file']`
不返回 `bill.setup.run`。**最终绿读数：未取（等门禁安静窗口）**——那是「读的过程中同席正在改」的中间态。

```powershell
# ③ 出口载荷与退出码（临时基座，不改工作区）
node -e "const{spawnSync}=require('node:child_process');const env={...process.env,ILIFE_CONFIG_DIR:'<临时目录>'};for(const p of [{op:'init-status'},{op:'import'},{op:'restore',name:'不存在的备份.db'}]){const r=spawnSync(process.execPath,['packages/skill-bill/dist/cli/cmd_read.js','bill.setup.run','--params',JSON.stringify(p)],{encoding:'utf8',env});console.log(JSON.stringify(p),'exit='+r.status,(r.stdout||'').trim().split(/\r?\n/).pop());}"
```

当刻读数（同一版 dist）：三条都 `exit=0`；`init-status` 得 `ok=true`（`就绪：10 列齐（含 deleted_at）…`）；
`import` 与 `restore`（名字不存在那一支）各得 `ok=false`，message 点名缺哪一项（§五 第 45 行的读数出处）。

**未取（等门禁安静窗口）**：本域五道门禁的绿读数（`pnpm build`／`pnpm gen:check`／
`scripts/check-warning-line.mjs`／`scripts/check-scene-shape.mjs`／`scripts/gen-page-fingerprints.mjs --check`）
——本域各件当刻未提交、同席在改同一批件，取到的只会是中间态，故本表不记；由本域收口票在安静窗口重取。
