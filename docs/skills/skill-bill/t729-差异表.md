# t729 · 分析域搬迁差异表（老行为／新行为／为什么改／谁批的）

- 票：[analysis 域：端到端搬迁（25 场景／5 个形态族）](https://github.com/FeatherHunter/ilife/issues/729)（父图 [#681](https://github.com/FeatherHunter/ilife/issues/681)）
- 口径来源：`#681` 偏好 2「老技能的行为是**默认真相**；允许改，但**每次改动逐条留痕**（老行为／新行为／为什么改／谁批的）」。
- 老侧对照物：`D:\2Study\StudyNotes\SKILLS\饼干记账\scripts\analysis\cli.py`（1,126 行，25 个 `cmd_*`）＋
  `scripts/analyze.py`（218 行）＋ `scripts/insights.py`（184 行）＋ `templates\分析\analysis_view.html`（1,166 行，25 个渲染器）。
- 新侧落点：`packages/skill-bill/src/analysis/`（`scene-*.ts` 25 件 ＋ `template-*.ts` 5 件 ＋ `agg.ts`／`cards.ts`／
  `pageParts.ts`／`read.ts`／`commands.ts`／`scene.ts`／`index.ts`）。
- 读法：**只列「新侧与老侧不一样」的地方**；一样的地方（25 个场景都搬到了）不占行。行号＝老侧文件物理行号。

## 一、口径类（读数会不一样的）

| # | 老行为（出处） | 新行为 | 为什么改 | 谁批的 |
|---|---|---|---|---|
| 1 | `_calc_kpi`（`cli.py:77-83`）把**转账**也计进收支：`#转账` 那两笔按金额符号分别进 expense／income，也进 `count`；`_agg_expense_by`（`:108-130`）同理 | 一律走 `src/shared/kpi.ts` 的 `calcKpi`／`isTransfer`：**转账不入收支**（支出／收入／笔数／分类聚合／趋势／排行都剔）；余额类读数另计 | 账户域 #691 已定死的口径（转账是同一笔钱换口袋，不算收入也不算支出），本仓只许一处定义（`shared/kpi.ts` 是唯一判地） | #691（地图 #681「两条已定口径」） |
| 2 | `_tag_match`（`cli.py:794-799`）自己一份正则：分隔符集 `[\s,，。.．;；:：!！?？)）]` | 走共用位 `src/fetch/db.ts:148` 的 `tagMatch`：分隔符集更宽（多 `、·\|/()（）[]{}「」『』` 等） | 同一件事（`#tag` 整词匹配）全仓只许一处定义；查询域的查标签早已走它 | #411（查询域搬迁已落该件） |
| 3 | `_month_series`（`cli.py:133-163`）的序列终点＝**库里最新一条记录所在的月**，靠 `fetch_all` 的 `time DESC` 取 `records[0]` | 同一条口径，但**从记录集合里取 `time` 最大值**（`agg.ts` 的 `lastTimeOf`） | 老侧那个写法依赖取数层的排序方向；新侧 `fetchAll` 是升序（`ORDER BY time ASC`），照抄会把终点算成最早那个月——静默算错比不一样更坏 | 实现层，无口径变化 |
| 4 | 窗口内零记录：老 CLI 照打印空结果；**搬迁前新仓**的 `cmd_read.ts` 三条 case 直接抛 `BILL_EMPTY_RANGE`（exit 4） | **窗口为空**照 #688 裁定 4 出**完整页**（标题＋空态句＋「怎么记第一条」的引导句＋来源脚注）；**库为空**（一条记录都没有）仍 `BILL_EMPTY_RANGE`／exit 4 | 裁定 4 的空窗澄清：空窗 ≠ 空库；照出完整页比报错更像话，空库的 exit 4 是既有设计行为、明文不变 | #688 §四 裁定 4（行 216 的澄清） |
| 5 | 金额格式化 `fmtMoney`（`analysis_view.html:153`）带**千分位**＋固定两位 | `pageParts.ts` 的 `money()`＝两位小数，**无千分位** | 本仓数值格式由公共层读数卡与表格承载（#688 §二 C10「格式单源」）；页面本地不另造一套格式 | #689／#690（写入域／查询域已按同一口径落） |
| 6 | 缺字段直出：`analysis_view.html:604`／`:614`／`:621` 在缺值处算出 `NaN` 并写进 `style="width:NaN%"`（C10 记的老侧缺陷 D12 之一） | 缺值一律 `—`（`textOrDash`／`money` 的缺值分支）；条长用 `round1` 归一，越界由公共层夹取 | 裁定 4：可见文本缺值写 `—`，**不许渲染 `undefined`／`NaN`**；老侧那条是明确的反面 | #688 §四 裁定 4 |

## 二、页面类（版式与块的差别，事实不丢）

| # | 老行为（出处） | 新行为 | 为什么改 | 谁批的 |
|---|---|---|---|---|
| 7 | **一张模板扛 25 个视图**：`analysis_view.html:200-226` 一张 25 条 `type` 分发表，`$('#app').innerHTML` 全量替换 | **一命令一页**：25 件场景件各出一页（`scene-*.ts`），块序收进 **5 个形态族**模板（`template-bars`／`charts`／`tables`／`compare`／`insight`） | #688 §二 C4＋裁定 8：老侧 `type` 与「页」本来就是 1:1（38 张互斥视图压在 2 张模板上），按 type 一条一页搬即可，不必发明新粒度 | #724 §四 落点清单（分析域 5 片）＋ #688 裁定 8 |
| 8 | 每页页脚恒印 `scripts/analysis/cli.py <type> --json`（`analysis_view.html:249-256`）；页头 `h1` 由脚本改写 | 来源脚注写**人话**（`数据来源 · 记账库（只读） · 起 → 止 · 共 N 条`，经 `src/shared/sourceLine.ts`）；页面上不出现脚本路径、命令名、库文件名 | 裁定 1：参数名、常量名、英文内部标识符、脚本路径一律不上屏；老侧那行与真实分域路由也对不上（C4 记的缺陷） | #688 §四 裁定 1＋§二 A5 |
| 9 | 每张卡各写自己的空态；`analysis_view.html:831-842` 周几分布卡**没有** `length` 守卫（空数据留白卡），同一函数的时段分布却有守卫 | 每张卡都带 `emptyText`（条卡／列表卡／小表卡／图卡），整页另出**联合空态**（`empty.text` ＋ `empty.hint`） | #688 §三 第 7 条逐字点名本域：每表／每列表／每图各传 `emptyText`，不许留白卡；裁定 4 要求空窗页仍完整 | #688 §三 第 7 条＋裁定 4 |
| 10 | 四图自绘／挂 `charts.js`：环形（`:412-417`／`:517-528`）、折线（`:1122-1143`）、柱（`:712-717`）、直方（`:791-796`） | 一律走公共层 `renderChartBlock`（`bar`／`line`／`donut`），技能侧**零 SVG 副本、零样式副本** | #688 §二 C5／C6：老侧注释自己就说「样式全部由公共组件决定」；本域是唯一有图的域，落点就在这里 | #688 §二 C5／C6 |
| 11 | 图表四条是**反面**：逐月条不跳过 `expense === 0` 的月（`:320`）、逐周几不跳过 `count === 0`（`:833`）、`categoryBar` 对 `total === 0` 仍出「0.00 元 · 0 笔」＋零宽条（`:1088-1098`） | **裁定 3 四条**：纵轴给刻度（`yTicks`）、零值不出柱身、点数不足出说明句、**跨空档用同色虚线并把「哪几个月没有记录」写进口径句** | 裁定 3 逐字点名老侧在同位置是反面教材 | #688 §四 裁定 3 |
| 12 | **本地只读下钻**：点分类看明细（`:421-443`）、点月份展开该月汇总（`:666-692`），用裸 `<div onclick>`（无 `role`／`tabindex`／键盘事件） | **不实施**：结果型 HTML 是静态文件，下钻要脚本；本票只**登记**（与卡路里 `t425` 移植项 3／4 同类） | #688 §二 C7 明文「先登记不实施」；若将来实施也不许照抄裸 `onclick` | #688 §二 C7 |
| 13 | AI 解读按 `|` 切段（`parseAiNote` `:851-861`）＋分段卡（`:874-879`），未知标题降级「AI 解读」 | **不引入分段机制**：本域页面由本地 CLI 渲染，没有 AI 解读文本可切；「这批数说明什么」那段由**结论句**承载 | #688 §二 C8 要的是「格式对就分段、格式不对也不丢文本」；本侧根本没有那段文本，引入 `renderDetailSection` 只是加一层没有输入的空机制 | 本票判定（差异登记于此） |
| 14 | 看周报的**对比双卡 ＋ 变更徽标**（`:347-378`） | 归入「读数＋条」族：对比事实并进**结论句**（「比上周 ↑533.00 元（1025.0%）」）＋一张**事实卡**「本周 vs 上周」八行 | #688 §五 5.2 的 ⑦ 列没有「对比卡」这一块（那是 ④ 对比类的形状）；本族槽位固定，事实一条不少地换了个摆法 | 本票判定（信息等价，#681 偏好 3） |
| 15 | 看账户／看账本／看结构**没有**总读数卡（`:446-529`） | 照旧没有（三页 `kpis: []`），读数在各分组卡内 | 裁定 6 的反面同向：没有可报的数就不出同形「全 0」卡；老侧本来就是分组读数 | #688 §四 裁定 6（同向口径） |
| 16 | 长列表最多显示 200 条（`recordsList` `:1102-1111`），多出部分只写一行说明 | 本域各页的明细卡按场景给条数上限（大额 TOP N／高频 TOP N／退款明细／借贷对象列表），没有「静默截断」 | 交付体积门（256 KB）下本域没有接近阈值的页（最大页约 80 KB）；老侧那个 200 条上限是通用件口径，本域用不到 | 本票判定（读数见证据件） |
| 17 | `limit` 默认 5（搬迁前新仓 `cmd_read.ts` 那一支）／10（老 CLI `--limit` 默认 10） | 看大额／看高频默认 **10**（照老 CLI 的默认值），给了 `limit` 必须 1~50 整数（照搬迁前 cmd_read 的校验） | 老 CLI 是用户真在用的那条入口，它的默认值才算「老行为」；校验保留搬迁前那一支的口径 | 本票判定（差异登记于此） |
| 18 | 看总览支持 `--month`（老 CLI `cli.py:213-237`）；搬迁前新仓 `cmd_read.ts` 也认 `params.month` | 看总览只认 `start`/`end`（缺则本月）——**服务端默认本月**与 `month=本月` 等价；看分类／看账户／看账本／看结构／看分布仍认 `month` | 各场景按自己的参数表取参（老侧 25 个命令本来就各有各的参数集）；看总览那条唤醒词问的是「开始日期／结束日期」 | 本票判定（差异登记于此） |

## 三、留白（本票没做、也不是本票范围内的）

| # | 事项 | 去处 |
|---|---|---|
| 19 | 老侧「本地只读下钻」（C7）与 25 个页面级渲染器的**逐块视觉打磨** | 下钻：登记不实施（见第 12 行）；视觉：本票只保证「信息等价」与 25 条判据，好不好看由用户肉眼看（#681 偏好 4） |
| 20 | 老侧 25 个测试件（7,438 行）当判据 | 地图 #681 的「Not yet specified」第 1 条（怎么写判据是**决定**，不是本票的活） |
| 21 | 页指纹账本（判据甲）只覆盖写入域 32 页，本域页不在其中 | 与 #691 同一条留白：扩账本属横向决定，留给下一张页面票（#691 收口时已如实记） |
