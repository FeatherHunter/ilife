PASS

# t469 独立复跑复核（非作者，只信提交号与自己读数）

## 一、判定

被审：`70d2124`（测试）＋`ad76493`（文档），两笔皆为 HEAD 祖先，未被改写。`git diff HEAD -- packages/skill-bill/src` 空，`git status --short -- packages/skill-bill` 空。评分（5 维均分，S1 一票否决，均分低于 85 即 FAIL）：契约一致 90／证据真实 92／新旧对照与变异强度 88／工程红线 100／文档同步 90，均分 92.0，S1 计 0，故 PASS。扣分唯一去向见 §四 D1（S3）。

## 二、复跑

窗口 `2026-09-15T15:06:09Z` 至 `2026-09-15T15:31:58Z`，全部经 `tooling/run-locked.mjs --ticket 469` 持锁，15 秒轮询、未超 10 分钟上限。摘要：构建 3/3 exit 0；新旧同跑 38/38（含终轮还原后复绿）；全量 150/36/150/0；自设探针绿→红→绿；变异树上旧护栏 38/38 存活（证未覆盖，见 D1）。

GATE-RUN runId=3f149efd-88c4-45f6-9bca-3875655bbdd4 cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=580790c9-7298-420f-8e5e-b1fb53440b36 cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs packages/skill-bill/test/shared-judgments.test.mjs
GATE-RUN runId=8764e57d-33a1-455c-ad57-2e009e912a6f cmd=node --test packages/skill-bill/test/*.test.mjs
GATE-RUN runId=62a3f228-648f-4da6-8d11-f1dc9a067ef2 cmd=node .scratch/t469/review-probe.mjs
GATE-RUN runId=545deeb5-cf73-4c3f-b6f7-ca2f22cf8791 cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=83a55aae-378f-4614-9fda-f4bc2445ab77 cmd=node .scratch/t469/review-probe.mjs
GATE-RUN runId=65bbac2c-2b84-4d3e-bf9e-106d8a97b1a2 cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs packages/skill-bill/test/shared-judgments.test.mjs
GATE-RUN runId=9a2d19d2-9d71-4927-b697-98ccc9260776 cmd=npx tsc -b packages/skill-bill --force
GATE-RUN runId=c00ba5ed-3f05-4f8e-901d-d04995a1a4e8 cmd=node .scratch/t469/review-probe.mjs
GATE-RUN runId=c08ecac7-05aa-463f-aad9-2c4bb923c1ab cmd=node --test packages/skill-bill/test/t469-guard-additions.test.mjs packages/skill-bill/test/shared-judgments.test.mjs
GATE-RELAX flag=--allow-nonzero reason=变异红跑 83a55aae exit 1 为预期断言红（去 given 过滤后探针必红），非门禁失败；其余 9 条 exit 0。
GATE-RELAX flag=--allow-undeclared reason=共享日志窗口内含他票 RUN，反向对账按票号隔离外仍须放宽，复核只认领本票 10 条。

读数：`3f149efd` 构建 exit 0（等 ticket=495 约 30s）；`580790c9` 新 12＋旧 26＝tests 38 pass 38 fail 0（等约 801s）；`8764e57d` 全量 tests 150 suites 36 pass 150 fail 0；`62a3f228` 探针 GREEN 3/3；`545deeb5` 变异后构建 exit 0；`83a55aae` 探针 RED（P-R1 得 2 行，`amount/-12.5/未设置/true` 泄漏，exit 1）；`65bbac2c` 变异树上新旧仍 38/38（存活）；`9a2d19d2` 还原后构建 exit 0（源码哈希回 `66c51379`）；`c00ba5ed` 探针回绿 3/3；`c08ecac7` 终轮新旧 38/38。对账：`node tooling/check-gate-audit.mjs --evidence docs/skills/skill-bill/t469-复核-独立复跑.md --ticket 469 --since 2026-09-15T15:06:09Z --until 2026-09-15T15:31:58Z --allow-nonzero --allow-undeclared` → matched=10/10 scoped=10 undeclared=0 gate-audit PASS。

## 三、路径

新增（本票）：`packages/skill-bill/test/t469-guard-additions.test.mjs`（187 行，12 条）；`docs/skills/skill-bill/t469-护栏补齐-证据.md`（96 行）。复核新增（本次唯一入仓件）：`docs/skills/skill-bill/t469-复核-独立复跑.md`（本件）。过程（未入仓）：`.scratch/t469/review-probe.mjs`（自设探针，P-R1／R1b／R2）、`.scratch/t469/review.log`（本窗 10 条 RUN 原文与红绿输出）。未碰：`packages/skill-bill/src` 终态零改（含变异还原＋重建）；`base` 绘制包、他技能包、`src/cli`、政策、模板、旧测试零改；工作区他票脏件（skill-calorie 验收墙等）未碰。红线：未装包，临时变异仅 `recordPicker.ts:245` 一行且已还原（三重证：`git diff` 空＋哈希回 `66c51379`＋还原后构建＋探针回绿）。

## 四、缺陷与未做

D1（S3，本票范围，证据过断）：证据 §四称“未发现第 5 处无护栏判定，`diffRowsFor` 只比给了的字段旧件 `diffTable` 侧已覆盖”。实测：去 `given` 过滤后新旧 38/38 仍绿（`65bbac2c`），自设探针才红（`83a55aae`），证 `diffRowsFor` 过滤无护栏。代码行为当前正确（还原后探针绿），故非 S1；但“无第 5 处”不成立，扣 §一中新旧对照 12 分与文档 10 分。范围外：未发现 S1／S2； skill-calorie 脏件与他票锁争用属范围外，未计分。未做：全仓测试未跑（只跑 skill-bill 全量，与被审口径一致）；M1～M4 未逐条重跑（以新旧同绿＋全量＋自设变异覆盖“改坏必红／还原即绿”机制，未采信其具体数字）；`textOf` 统一有意不做（同意保留理由）。下一手：另开票补 `diffRowsFor` 过滤断言（以本件探针 P-R1／R1b 为用例，含 `amount:0` 视同给了），并订正证据 §四该句；`textOf` 统一另议（presence／display／compare 分名后逐页给产物差异）。
