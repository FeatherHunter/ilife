# t730 目标域 · 门禁运行记录（运行条目导出件）

- 票：[goal 域：端到端搬迁（4 场景／2 片）](https://github.com/FeatherHunter/ilife/issues/730)
- 本件按 `docs/subagent-concurrency-protocol.md` §2.3 第 4 条导出：把**本票声明过的**加锁运行条目
  从（未受版本控制的）`.scratch/locks/gate-runs.log` 抄到受版本控制的路径，供第三方对账。
- 声明口径照 §2.4 第 2／3 条：只声明**退出码为 0**的运行，每条给 `GATE-RUN runId=<运行标识> cmd=<命令>`；
  标识从运行记录的对应行逐字抄录。**当刻红的运行不声明**（红读数不是门禁证据），其现场另记在 §二。

## 一 声明的运行（`GATE-RUN` 行）

```
GATE-RUN runId=d3c5f571-2964-4a59-9edd-165f56ef050a cmd=node node_modules/typescript/bin/tsc -b packages/skill-bill
GATE-RUN runId=5019fcc5-812b-4f25-9c59-58d9cdd957ef cmd=node docs/skills/skill-bill/t730-探针-真出口.mjs
GATE-RUN runId=8fcbc513-1546-4986-afbc-894343fb2279 cmd=node --test packages/skill-bill/test/t730-目标域.test.mjs
GATE-RUN runId=1280ba79-01c9-41e9-94c7-64abfd5ed941 cmd=node .scratch/t730/gates.mjs
GATE-RUN runId=14173caf-d1e3-4a31-aa20-c88fdb8cee2e cmd=node docs/skills/skill-bill/t730-探针-真出口.mjs
GATE-RUN runId=5e78bc95-8a53-4581-9831-11e57c8b619d cmd=node packages/skill-bill/scripts/check-warning-line.mjs --sync
GATE-RUN runId=622a0245-6a0d-45d0-ba66-f44d108337d3 cmd=node packages/skill-bill/scripts/check-warning-line.mjs
GATE-RUN runId=6973c235-ea53-4dd9-8481-3e100201b7ee cmd=node .scratch/t730/gates.mjs --with-full-test
GATE-RUN runId=f30a2160-8a60-401a-aa91-0169e1939e0f cmd=git commit -F .scratch/t730/commit-msg.txt -- <本票声明的路径>
```

逐条读数（摘要行；明细落 `.scratch/t730/gate-*.log`）：

| runId | 命令 | 退出码 | 摘要行 |
|---|---|---|---|
| `d3c5f571…` | `tsc -b packages/skill-bill`（删掉两个 `case` ＋ 过渡表两行之后） | 0 | 无输出（编译通过） |
| `5019fcc5…` | `t730-探针-真出口.mjs`（28 步版，夹取那条还在修） | 0 | `RESULT: 28/28` |
| `8fcbc513…` | `node --test packages/skill-bill/test/t730-目标域.test.mjs` | 0 | `tests 12 ｜ pass 12 ｜ fail 0` |
| `1280ba79…` | `.scratch/t730/gates.mjs`（一窗跑：编译＋五道门＋包内测试） | 0 | 窗口内：tsc 0／`gen:check` PASS（命令 14 条／能力 6 个）／棘轮 `RESULT: 9/9`／场景形状门 `RESULT: 54/54`／页指纹门 `RESULT: 32/32`；告警线门当刻红（台账未同步，见 §二） |
| `14173caf…` | `t730-探针-真出口.mjs`（29 步版，含读数那一步） | 0 | `RESULT: 29/29` |
| `5e78bc95…` | `check-warning-line.mjs --sync` | 0 | `SYNC-VERIFY ok（台账 4 行）` |
| `622a0245…` | `check-warning-line.mjs`（无参门禁） | 0 | `RESULT: 13/13 ｜ PASS: 告警线台账齐全且与实况一致` |
| `6973c235…` | `.scratch/t730/gates.mjs --with-full-test` | 0 | 窗口内：tsc 0／`gen:check` PASS／棘轮 9/9／场景形状门 54/54／**告警线门 13/13 PASS**／页指纹门 32/32；包内测试与全量测试当刻红（见 §二） |
| `f30a2160…` | `git commit`（本票声明的路径，21 件） | 0 | `ca125a0d feat(730): goal 域端到端搬迁（4 场景／2 片）…` |

## 二 当刻红、**不计入证据**的读数（连同原因一起留档）

| 窗口 | 红在哪 | 原因（逐条点名） |
|---|---|---|
| `6973c235…` 的 `pkg-tests` / `full-tests` 两步 | 包内测试 9 条红、全量测试另有若干红 | **全部落在在飞的两张兄弟票上**：`t721-判据与摘要锁` 报 `analysis/scene-reimburse.ts:47` 有唤醒词字面量（#729 的件）· `t729-分析域` 的 8 条读数／图表／空窗断言（#729 的测试，跑的是 #729 当刻未完成的实现）。**本域 12 条靶向测试与 29 步探针在同一次窗口里全绿**（见 §一）。这两步的红**不作为本票的门禁证据**；本票的全量读数以两票落地后的安静态复跑为准（见 §三） |
| `1280ba79…` 的 `warning-line` 步 | `RED 台账陈化：src/cli/cmd_read.ts 台账=455 实况=280` ＋ `RED 漏报：src/setup/template-wizard.ts LF=361` | 台账是**三票共用的**一件（#729／#730／#731 都改 `src/**` 的行数）；当窗后即由本票 `--sync` 收口（见上表 `5e78bc95…`／`622a0245…`，13/13 PASS） |

## 三 复跑法

```powershell
cd D:\ilife
node tooling/run-locked.mjs --ticket 730 -- node node_modules/typescript/bin/tsc -b packages/skill-bill
node tooling/run-locked.mjs --ticket 730 -- node packages/skill-bill/scripts/gen-cli.mjs --check
node tooling/run-locked.mjs --ticket 730 -- node packages/skill-bill/scripts/check-ratchet-tight.mjs
node tooling/run-locked.mjs --ticket 730 -- node packages/skill-bill/scripts/check-scene-shape.mjs
node tooling/run-locked.mjs --ticket 730 -- node packages/skill-bill/scripts/check-warning-line.mjs
node tooling/run-locked.mjs --ticket 730 -- node packages/skill-bill/scripts/gen-page-fingerprints.mjs --check
node tooling/run-locked.mjs --ticket 730 -- node --test "packages/skill-bill/test/*.test.mjs"
node tooling/run-locked.mjs --ticket 730 -- node docs/skills/skill-bill/t730-探针-真出口.mjs
```

（`.scratch/t730/gates.mjs` 是把上面那一串按一窗跑完的脚本，末行打每步的 `STEP <名> exit=<码>`；
它住 `.scratch/`——施工件、不入库；要复跑照上面这一串单条跑即可。）
