## 一、逐条核验

1. D1 真在：部分成立。代码成立：`packages/skill-bill/src/shared/docPage.ts:52` 为 `repeat(auto-fit, minmax(220px, 1fr))`，同文件 `50` 行限定 `@media (min-width:1200px)`，`46` 行注明手机端 `≤640px` 不动；HEAD 同位含 220px 且无 MUTANT（head-check：HEAD_doc_has220=1，HEAD_doc_hasMut=0）。产物源码成立：记退款-回执等 3 份均 has220=1、hasAutoFit=1，记退款-回执内 `2026-09-14 12:30:00` 单行串出现 3 次。视觉第二排铺满与日期单行未在本验独立渲染复核，故只到部分成立。
2. D2 真在：成立。`packages/skill-bill/src/shared/copyArea.ts:145-150` 的 `undoExit` 只留红标记按钮与去向说明，无 `dataText`/`dataActionId`（HEAD 切片复核：undoHasDataText=0，undoHasDataAction=0）；复制载荷仍走 `buildDataText`（同文件 `39-40`、`102-118`）。官方探针复核 `FILES=32 复制数据=33 复制日志=33 data-t=145`，16 回执中 15 页数据=1 日志=1、撤销-回执数据=2 日志=2 例外，采集页无 ODD，即单组后态；data-t 存在，载荷不断。
3. D3：成立。本包 `src/shared/*.ts` 内 `#[0-9a-fA-F]{3,6}` 只命中注释里的 `#403` 系列与 `copyArea.ts:141` 的 `base #336` 史实说明，无色值字面新增。证据件八-3（`docs/skills/skill-bill/t407-整改-桌面版式与收尾-证据.md:166-175`）写清色值全在 base tokens、复用前窗 5.69:1/5.07:1/3.55:1/3.41:1 读数，并记为 base 层票据（已有 #533 同类，沿七-2请裁）。
4. D4：成立。映射走 `packages/skill-bill/src/shared/userWording.ts:28-30`（`category: '分类'`），`fieldLabelOf` 在 src 下 84 命中（含 `receiptBody.ts` 3 处）。产物侧探针 `英文td=0`、机审 `英文裸词 0 份`，抽查记退款-回执 bareTd=0。错字 `囗`：src 的 `.ts` 0 命中、test 的 `.mjs` 0 命中、32 产物 `退出囗=0`（probe-count），审计亦 `退出囗` 无异常。
5. 直断言在：成立，且为本次最要紧一条。`packages/skill-bill/test/user-wording.test.mjs:28-30` 存在 `W1 补钉：category 单独直断言`（`fieldLabelOf('category')==='分类'`）。本验改坏映射一次（`category: '分类'→'category'`）→ 单跑 `tests 12 pass 10 fail 2` 必红，含 `W1 补钉 actual:'category' expected:'分类'`；还原后单跑 `pass 12 fail 0`。
6. docPage 变异行确已清除：成立。工作区 `docPage.ts` 的 MUTANT 单文件 0 命中，`src` 递归 0 命中、`test` 0 命中（grep-count HITS=0，还原后复查亦 0）；`220px` 在 `docPage.ts:52` 在位，HEAD 与产物均含 220px。
7. 读数：成立。`npx tsc -b packages/skill-bill --force` 变异前 EXIT=0、还原后重编 EXIT=0。`node --test "packages/skill-bill/test/*.test.mjs"` 变异前连跑两次均为 `tests 132 pass 132 fail 0`，还原后复跑仍 `132/132/0`；`user-wording` 单跑还原后 `12/12`。
8. 提交范围：成立。6 笔 `git show --stat`：`4eea838`、`e29da4a`、`117be02`、`3c6f8a1`、`b4fecf0` 各只改证据件 1 个文件（分别为 50/8/11/12/47 行增）；`2ad2552` 只改 32 产物（48 增 48 删）。路径一律在 `docs/skills/skill-bill/` 下，无别窗文件。

## 二、读数

- tsc：变异前 EXIT=0；变异态重编无输出后单测变红；还原后 `--force` 重编 EXIT=0。
- 全包测试：变异前两次 `tests 132 pass 132 fail 0 duration约4316ms/4289ms`；还原后 `tests 132 pass 132 fail 0 duration约4332ms`。
- 单测：变异态 `tests 12 pass 10 fail 2`（W1 补钉红）；还原后 `tests 12 pass 12 fail 0`。
- 探针：`FILES=32 复制数据=33 复制日志=33 data-t=145`，`英文td=0 退出囗=0 【bill=0 记账数据=32`，ODD 16 均为回执数据=1 日志=1（撤销 2/2 例外），即 D2 去重后态。
- 机审：还原后 `读数：全绿；共 32 份产物，缺 0 份`，共享样式 32/32 同一份，本页样式块 0、内联 0，既有提示位 `本页缺触屏三件 32 份` 改前改后同形。
- 变异哈希：改前 `2f8141861ae935db2fc247693d9551ac37597fad5223a406a337063de3a741cb`（与证据件八-7同值），变异态 `919897d7c5049a7e29df51a1b7044788c1e845625134c3394dce6d6a72e364f7`，还原后回到改前值。
- 三自证：MUTANT 残留 src/test 0 命中；`git diff -- packages/skill-bill/src packages/skill-bill/test` 空；三绿（tsc EXIT 0、全包 132/132、机审全绿）跑在还原后哈希态上。

## 三、不成立或部分成立

- 第 1 条记部分成立：缺的是本验未独立渲染桌面 1280 第二排铺满与日期单行视觉，代码与产物源码串证据成立，视觉以证据件自看结论为准，本验未重复截图。
- 真 390 的 `DOCW=375 VW=390` 与两列不断裂、桌面 4 份与手机 2 份自看过程未在本验重复，仅复核代码约束（补丁只在 1200px 以上生效、`desktop-copy` 的 640 档绿、机审 640 断点有）与产物含 220px，不记为不成立。
- 其余 7 条成立；未发现须整改的实质缺陷。撤销-回执 2/2 与置灰日志残留属证据件七-1已披露例外，不记为新的不成立。

## 四、结论

**放行。** 8 条中 7 条成立、1 条（D1视觉铺满）代码与源码串成立而独立渲染未重复故记部分成立，变异 M1 红转绿与三自证齐、tsc 与两次 132/132 及机审绿均复核一致，无须整改项。
