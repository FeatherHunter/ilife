# t407 复核 · 十六页收口 · 独立复跑（证据轴）

被审对象：提交 **`5930127`**（53 件＝22 源码／测试 ＋ 1 证据件 ＋ 30 产物）＋ `41d6dbc`。
复核方立场：**只信提交号与自己跑出来的读数**，被审方自述（`t407-恢复-稳定化证据.md`）与它的 `.scratch\` 脚本一律只当待验断言。
只读约束履行情况：未改任何源码／文档，未做任何 git 写操作；唯一一次源码触碰＝第五节「变异探测」的一行，已还原并三重自证（见第三节第 6 条）。
被审方证据件提到的 `src\policy\lookup.ts` 在盘上是 `src\help\lookup.ts`（`src/policy/` 下无 `lookup.ts`），下文按盘上路径记。

## 一、复跑环境与命令原文

| 项 | 值 |
|---|---|
| 工作目录 | `D:\ilife` |
| HEAD | `d26d6ec`（`5930127` 之上还有 `41d6dbc`、`d26d6ec` 两个后提交） |
| node | `v24.19.0` |
| 命令入口 | `node packages\skill-bill\dist\cli\cmd_read.js` |
| 临时库（采集页不写库那组） | `.scratch\t407-verify2\db`（绝对路径 `D:\ilife\.scratch\t407-verify2\db`，`BILL_FORCE_PROD=1`） |
| 落盘 | `.scratch\t407-verify2\out\`（自己写的驱动），**未覆盖 `docs\skills\skill-bill\` 下任何入库产物** |

命令原文（逐条，均为我自己跑的）：

```powershell
# 1) 工作树与提交一致
git rev-parse --short HEAD
git show --stat 5930127
git diff --stat HEAD -- packages/skill-bill docs/skills/skill-bill/t407-页-记收入-采集页.html docs/skills/skill-bill/t407-页-记收入-回执页.html
git diff --stat HEAD -- docs/skills/skill-bill/t407-页-记分期-采集页.html docs/skills/skill-bill/t407-页-批量录入-回执页.html
git diff --stat HEAD -- docs/skills/skill-bill/
# 2) 编译与测试（测试连跑三次）
npx tsc -b packages/skill-bill --force
npx tsc -b --force
node --test "packages/skill-bill/test/*.test.mjs"
# 3) 自己写的 16 词 × 2 支驱动（采集页只给 kind/op；回执页补全槽位）
node .scratch\t407-verify2\run30.mjs r1
node .scratch\t407-verify2\run30.mjs r2
node .scratch\t407-verify2\run30fresh.mjs          # 洁净库同口径复跑 30 页
# 4) 边界（每条一个独立空库，落在 .scratch\t407-verify2\b-<case>\db）
node .scratch\t407-verify2\bounds.mjs collectonly
node .scratch\t407-verify2\bounds.mjs wakewords
node .scratch\t407-verify2\bounds.mjs softdelete
node .scratch\t407-verify2\bounds.mjs unknownkey
node .scratch\t407-verify2\bounds.mjs badjson
node .scratch\t407-verify2\bounds.mjs missingid
# 5) 产物断言 / 场景特有块 / 渲染
node .scratch\t407-verify2\check_artifacts.mjs
node .scratch\t407-verify2\gate2.mjs               # 缺一项必需槽位态：表单控件与块
node .scratch\t407-verify2\inst_probe.mjs          # 分摊预览表：12 期 × 300.00 vs 总价 3600
node .scratch\t407-verify2\domcheck.mjs            # 自己看无头 Chrome 渲染出的 DOM
```

工具口径两条（都踩过、都记下）：pwsh 5.1 会给内联 `--params '{"k":"v"}'` 吃掉内层双引号（`.scratch\t407-verify2\env.txt` 里第一次取证因此报 `ERR 2: --params 须为 JSON`），驱动一律走 `.mjs` 直传 argv；命令输出一律 `| Out-File -Encoding utf8 <路径>`，只读末尾行。

## 二、判据复跑结果（逐条）

### 1. 工作树与提交一致 —— 通过

| 命令 | 读数 |
|---|---|
| `git diff --stat HEAD -- packages/skill-bill docs/…/t407-页-记收入-采集页.html docs/…/t407-页-记收入-回执页.html` | **空** |
| `git diff --stat HEAD -- docs/…/t407-页-记分期-采集页.html docs/…/t407-页-批量录入-回执页.html` | **空** |
| `git diff --stat HEAD -- docs/skills/skill-bill/` | **空**（0 行） |

`git show --stat 5930127` 与证据件「影响清单」对账：**53 件完全对上**（22 源码／测试件 ＋ 1 证据件 ＋ 30 产物），文件清单逐名一致，未出现清单外的件。段「未 push」与盘上状态一致（HEAD 已在 `5930127` 之上两个提交，`5930127` 本身是 53 件的完整提交）。

### 2. 编译与测试 —— 通过

| 项 | 实测 |
|---|---|
| `npx tsc -b packages/skill-bill --force` | **退出码 0，诊断 0 条**（输出文件 0 行） |
| `npx tsc -b --force`（仓根全量） | **退出码 0** |
| `node --test "packages/skill-bill/test/*.test.mjs"` 第一次 | 退出码 0，**tests 90／pass 90／fail 0** |
| 第二次 | 退出码 0，**tests 90／pass 90／fail 0** |
| 第三次 | 退出码 0，**tests 90／pass 90／fail 0** |
| 编译产物是否新鲜 | 对 `dist\cli\cmd_read.js` 作 `tsc --force` 前后 sha256 对比：`B3A5F4C9…92A0` **两次相同**（盘上 dist 就是当前源码的产物） |

`dist\cli\cmd_read.js` 里中文注释读出来是乱码（末行 `node 浣庝簬 22.13` 等），这是**运行体源码被 GBK 落盘的既有痕迹**；`tsc --force` 后哈希不变，且 30 份产物中文全部正常，故判定为注释面的既有现象、非本票产物缺陷（未计入扣分）。

### 3. 16 词 × 2 支真跑（32 次命令）—— 通过

我自己写的驱动，16 条词逐条跑「采集页（只给 `kind`／`op`）」＋「回执页（补全槽位）」：

| 轮 | `kind`／`op` 表来源 | 采集页退出码 | 回执页退出码 |
|---|---|---|---|
| 第 1 轮（我自己给的槽位） | `t407-场景落点清单.md` ＋ `wakewords.ts` 的 `preset` 逐字 | 16/16 **exit 0** | 12/16 exit 0（4 条是我驱动自身的参数错：`id` 给成字符串、分类 `数码/手机/分期` 不在候选里；改对后全绿） |
| 第 2 轮（同上，修正驱动） | 同上 | 16/16 **exit 0** | 13/16 exit 0（余 3 条同上仍是 id 取值问题，已在 `receipts.mjs` 单独跑通） |
| 洁净库轮（`run30fresh.mjs`） | 同上 | 16/16 **exit 0** | 15/16 exit 0（唯一失败＝空库无 1 号记录，`ERR 2`，属参数不成立） |

补跑 `receipts.mjs`（真实 id）：`改记录`／`撤销`／`恢复` 三张回执页 **exit 0**，`记支出` 回执页 exit 0。合计 **30 份产物口径的成功次数＝30／30**。（命令总数 32＋32＋32＋4，其中因我驱动参数错而未过者已在上面逐条记明。）

字节范围（洁净库同口径，30 份）：**最小 67,564（恢复-采集页）／最大 75,267（拍账单-采集页）**；有槽位态的拍账单-采集页 75,016。

### 4. 产物断言 —— 通过（我自己写的一套）

对仓内 30 份入库产物逐份核：`<!DOCTYPE html>`、`<section`、`data-page="collect|receipt"`、`data-shape`、复制区 —— **30 份全中，STRUCT_BAD＝0**；`bill.bill` 双前缀 **0 命中**；`锟`／`U+FFFD` **0 命中**。

场景特有块抽查（我自己抽的原文）：

| 词 | 断言 | 实测 |
|---|---|---|
| 记退款 | 候选单选 ＋ 流程三段式 | 采集页有 `候选` 单选区（`退的是哪一笔：从候选里挑一行…`）＋ **三段式三枚状态徽标**：「第 1 段 · 原记账：还没认准是哪一笔／第 2 段 · 退款（这一步）：未定／第 3 段 · 结果：还差 3 项」。回执页无三段（三段只在采集页），与清单「采集页出三段式」一致 |
| 记分期 | 分摊预览表且**合计＝总价** | 采集页在缺 `total`／`periods`／`start_date` 时报阻断条（这是入库产物那 71,573 字节的状态）；补上这三个槽位后预览表出来：**12 行 × 每期 300.00，日期按月递推 2026-09-20 → 2027-08-20**，我独立算 12 × 300 = 3600 ＝ 给的总价 **3600**，**相符** |
| 批量录入 | 逐行可编辑表 | 缺金额态：`本批现阶段单笔化：一次只落一笔，表里一屏 1 行、逐行可编辑` ＋ **7 个逐格输入控件**（`batch-1-category`／`-amount`／`-time`／`-account`／`-ledger`／`-currency`／`-note`）＋ 合计行（`有 1 行缺必需格…合计 0.00`）＋ 缺项那行不给复制指令 |
| 拍账单 | 识别外置文案 ＋ 三要素逃生通道 | `识别口径：图片识别在本仓之外办——本仓不装识别引擎，也不做上传控件；页上只给三要素文字填空，缺哪样报哪样`＋独立「识别分工」卡（收图在 AI 侧／读图在本仓之外）＋**三要素文字填空表单**（`<input name="amount">`、`<select>` 分类、`<input name="time">`，三格齐）＋缺一项不给复制按钮 |

另核被审方自报的三条标记（正面复核）：拍账单-采集页命中「本仓不装识别引擎」「张账单图片」；记一笔-采集页命中「按金额符号判支出／收入」「认不得的」；撤销-采集页命中「要撤销的那条记录」——**全部命中，且「缺槽位」0 命中**。

如实附一条观察（不构成判据失守，但值得登记）：`data-page` 在采集页／回执页上分别是 `collect`／`receipt`（分得开），而 **`data-shape` 在四类页上都写 `receipt`**（含两张采集页），页面上也**没有 `data-region` 属性**。即「这一张是采集页还是回执页」只由 `data-page` 一处表达，`data-shape` 不承担这个区分。

`WAKE_TABLE` 实数：**77 条短语，77 个互不重复**（与 `policy.test.mjs` 的「唤醒词 77 全量」一致）。

### 5. 被审方「体检」与「收口后」两个读数不矛盾

证据件第 19 行记「90 条用例，通过 88，失败 2」，同一件第 279 行、任务书自报都记「90 通过／0 失败」。我按**提交态**复跑三次全是 **90／90／0**：前一个读数是它动手**之前**的体检、后一个是收口后的终态，二者不矛盾；两条失败用例它改在「用例过时」那一侧（`test/policy.test.mjs`）与「产物过时」那一侧（重注 `SKILL.md`），属如实登记。

## 三、边界探测结果

每条的库都是**独立新建的空库**，读数逐条如下。

### 1. 采集页不写库 —— 成立（但会建库文件）

```
{"step":"before","counts":{"dbExists":false}}                       ← 跑之前：库文件不存在
{"step":"collectOnlyCodes","codes":"记一笔:0,记支出:0,…,恢复:0"}      ← 16 条采集页全部 exit 0
{"step":"afterCollectOnly","counts":{"dbExists":true,"tables":["bills","sqlite_sequence"],"bills":0}}
```

判据：`bills` 行数 **0 → 0**，**不写库成立**。附一条如实记：库**文件**仍被创建（`NOTE: 记账 DB 已初始化`，只建表不插行），与「采集页不写库」不冲突。

### 2. 破口修复实证：只说唤醒词文本 —— 成立

扒 `dist/policy/wakewords.js` 的 `routeWakeword` 直接调：

```
撤销   → {"key":"bill.record.update","params":{"op":"undo"}}      ← 不再抛「缺槽位 id」
恢复   → {"key":"bill.record.update","params":{"op":"restore"}}   ← 不再抛
改记录 → {"key":"bill.record.update","params":{}}                  ← 不再抛
查某天 → THROW: 缺槽位 date：查某天                                 ← 对照：真必需槽位仍然拦
查区间 → THROW: 缺槽位 start：查区间                                ← 对照：仍然拦
```

三条修正词不再在路由层报错、两条真必需槽位的词**仍然拦**——破口补在正确的一侧，且没把 `needs` 机制整体放空（有对照）。采集页实证：`bill.record.update --params '{}'`／`--params '{"op":"undo"}'`／`--params '{"op":"restore"}'` 三次 **exit 0** 并落出采集页（envelope `data.ok=false`、`缺必需槽位：id（已出采集页…）`）。

### 3. 软删口径 —— 成立

```
add     → code 0；查库 [{"id":1,"deleted_at":null}]
撤销    → code 0；查库 [{"id":1,"deleted_at":"2026-09-14 15:39:17"}]   ← 打标 now，非空
恢复    → code 0；查库 [{"id":1,"deleted_at":null}]                    ← 置回 NULL
```

且回执正文同口径：撤销 `writtenFields:["deleted_at"]`、`已撤销：1（软删，恢复走 restore）`；恢复 `已恢复：1`。

### 4. 未知键 / 坏 JSON / 不存在的 id —— 全部有明确退出码

| 探测 | 命令 | 退出码 | stderr 原文 |
|---|---|---|---|
| 未知键 | `cmd_read.js bill.record.bogus --params '{…}'` | **3** | `ERR 3: 未知联动 key：bill.record.bogus` |
| 坏 JSON | `cmd_read.js bill.record.add --params '{kind:expense}'` | **2** | `ERR 2: --params 须为 JSON` |
| id 不存在 | `bill.record.update --params '{"op":"undo","id":999999}'` | **4** | `ERR 4: 取数失败：无此账单：999999` |
| id 非正整数 | `bill.record.update --params '{"op":"undo","id":"abc"}'` | **2** | `ERR 2: 口径失败：须给正整数 id` |

### 5. 变异探测（唯一允许碰源码的动作）—— 变异存活，已还原

我挑的判定：`src/shared/recordPicker.ts` 第 82 行 `keeps()`——「撤销候选＝未删记录／恢复候选＝已软删记录」这条**有注释、有文档口径**的运行期规则。把它改坏成 `return !isDeleted(row);`（撤销候选错纳软删行）：

| 步骤 | 读数 |
|---|---|
| 变异态 `npx tsc -b packages/skill-bill --force` | 退出码 0，诊断 0 条 |
| 变异态 `node --test "packages/skill-bill/test/*.test.mjs"` | 退出码 0，**tests 90／pass 90／fail 0**（已排除「跑的是旧产物」：`--force` 后 `dist/shared/recordPicker.js` 内即变异后的 `return !isDeleted(row);`，而测试全部 `import '../dist/*'`） |
| 还原后 `tsc --force` ＋ `node --test` | 退出码 0；**tests 90／pass 90／fail 0** |

**结论：这一处判定被改坏后，tsc 与 90 条测试全绿——该规则在本包零测试护栏。** 与证据件第五节自报的两处变异（改 `wakewords.ts`、改 `sceneFor` 落点，两处都必红）不矛盾：它挑的是**有测试守门的两处**，我挑的这处**没有守门**。

三条还原自证（我自己在**还原之后**复核，非引用被审方或子代理的读数）：

```
(Get-FileHash packages\skill-bill\src\shared\recordPicker.ts -Algorithm SHA256) = 37DF28F5488A6A8B4A14CEDEAC12A906CCC61DFADB03C7BCB159DBA1A09C02F5
（＝变异前基线；备份件 .scratch\t407-verify2\recordPicker.backup.ts 同哈希）
git diff --stat -- packages/skill-bill      → 空
git status --porcelain -- packages/skill-bill → 空
还原后 dist/shared/recordPicker.js 内即为 mode === 'restore' ? isDeleted(row) : !isDeleted(row);
```

### 6. 事故面独立复核（重点）—— 未见残留乱码，返工侧无可判差异

① **残留乱码**：对 `src/help/lookup.ts`、`src/record/scene-income.ts`、`src/record/scene-reimburse.ts` 逐件搜 `锟`／`U+FFFD`／可疑串（`鐨`／`鈥`／`Ã` 等），**0 命中**；扩到 `packages/skill-bill/src/**/*.ts` 全包搜 `锟`／`U+FFFD`，**仍 0 命中**。

② **与 `git show 5930127^:<路径>` 对照**：`help/lookup.ts` 的差异**正好是证据件第三节自己登记的三处**（`exampleParams` 入参加 `carries`、注释「未知键」改「未认的名字」、`needs` 与 `carries` 一起进示例），`git diff --stat` 记为 `7 +-`，与其自述「只剩那三处」相符。另外两件在 `5930127^` 里是 22／20 行上下、两格还指通用装配体 `collectBody`／`receiptBody` 的**占位件**，本票是「从占位件长成本场景件」（`scene-income.ts` 净 +172 行；它与 `-24` 的差值＝216 行文件，其自报「198 行」对不上，见第四节第 4 条）。
三件行数对账：`scene-income.ts` **191**、`scene-reimburse.ts` **237**、`help/lookup.ts` **50**。前两件与「新增 196／247，删除 24」自洽（191＋24−195≈…简算：原 ~19／14 行占位 ＋ 新增 172／233），**未能复现它所报的 198／247 两个数**；因每个差值的**行数与盘上一致**，判为它按含空行／含 BOM 另一种数法，不判缺陷，只登记。

③ **返工是否真的复原**：原始未损坏稿不在 git 里（损坏与返工都发生在打提交之前），**结构性证据不足以判「逐字复原」**。可判的只有：现盘上三件**没有乱码残留**，且 `lookup.ts` 的差异面与它自报的三处**逐处对上**。

④ **四张页内部自洽**（记收入／记报销 × 采集页／回执页）：DOM 里 `锟`／`U+FFFD` **0**；正文读得通、无半截句子（抽查段如「这一条记在收入侧：金额是正数、分类一级取自收入名单；本页的字段与值都取自库内那一行，不是拿参数顶的」「写库：未发生——这一页只采集、不碰库；补齐后重跑同一条命令才会写」）；加 `<section>` 开闭配平（5／5、4／4）。**未见内部不自洽**。

## 四、自称与实测的差异

| # | 自称（出处） | 实测 | 差在哪 / 最小复现命令 |
|---|---|---|---|
| 1 | 「30 份产物…字节 67,460–75,049」并给出 30 个具体字节数（证据件第三节表） | 仓内 30 份**逐份字节与它的表逐字相符**（我实测 67,460 改记录-回执页 ～ 75,049 拍账单-采集页） | **不算差异**：一律相符。差异在「能不能复现这个字节」——见第 2 条 |
| 2 | 「跑法：`SKILLS_DB_PATH` 指向临时库…采集页只给 `kind`／`op`」（第三节）→ 读起来是「洁净库即可复现」 | 洁净库 + `kind`／`op` 复跑：**10／30 份字节完全相同**，13／30 差 ≤800 字节，**7／30 差 443–8,608 字节**（撤销-采集页 −8,608、改记录-采集页 −8,553、记退款-采集页 −2,047、恢复-采集页 −1,752、拍账单-回执页 −932、记一笔-回执页 +938、记收入-回执页 +755） | 差异来源已查明＝**页面按库内候选渲染**：`撤销`／`恢复`／`改记录`／`记退款` 的采集页要列候选（我的库里有 28 条、它当时库里 0～1 条），`记收入`／`记报销` 的账户候选与「库里还没有带账户的记录」空态随库变。最小复现：`node .scratch\t407-verify2\run30fresh.mjs` → `node .scratch\t407-verify2\segdiff3.mjs`（段差只出现在候选行与账户候选提示上）。**判定：可复现性属「同库态可复现」，冻结产物对库态敏感、未在证据件里写明** —— 按用户口径「不算缺陷，但必须写明」，记 0 分并在此写明 |
| 3 | 「采集页不写库」 | 成立，但**库文件仍会被创建**（`bills` 0 → 0） | 最小复现：`node .scratch\t407-verify2\bounds.mjs collectonly` |
| 4 | 证据件记 `scene-income.ts`(198)／`scene-reimburse.ts`(247) 行 | 盘上 LF 口径 **191／237** | 每件差 7／10 行；`help/lookup.ts` 自报 55、实为 50（同型差 5）。最小复现：`(Get-Content packages\skill-bill\src\record\scene-income.ts | Measure-Object -Line).Lines`。判为**数法口径未写明**（含空行／BOM／计数工具差异），不判缺陷 |
| 5 | 自报两处变异「必红」（甲 `wakewords.ts`、乙 `sceneFor`） | **未独立复现**（我只核了它落盘的两行读数；我自己另挑的一处变异**存活**） | 按用户允许的第二条路径办：明确标「未独立复现」。存活的变异见第三节第 5 条 |
| 6 | 证据件写「`tsc` 退出码 0、0 诊断」／「90 通过／0 失败」 | 复跑一致（三次全绿；仓根 `tsc -b --force` 也 0） | **不算差异** |
| 7 | 「对账偏差 0；行数超线 0 件；形状超线 1 件（`photoEscape.ts` 对外 12 名 > 5）」 | 行数：本票件未见超 350（抽查 `scene-reimburse.ts` 237 等）。形状：**只核了它自报的那个数**，未重数 12 名 | 弱证据，不判缺陷 |

**自称与实测不符条数：6 条**（第 2、3、4、5 条为实质差异，第 1、6 条为「相符登记」，第 7 条为「弱证据登记」）。逐条差异无一项推翻它「30 份产物真实存在且可用」这一核心断言。

## 五、评分与结论

| 维度 | 分 | 理由 |
|---|---|---|
| 可复现性 | **18** | `tsc` 退出码 0／0 诊断（仓包与仓根都验）、测试三次 **90／90／0**、`dist` 与源码同哈希；30 份产物**全部真跑出来、exit 0**，字节范围实测 67,564–75,267 ≈ 它自报的 67,460–75,049。扣 2 分：7／30 份冻结产物在洁净库下不可复现（差 443–8,608 字节），差异源是**页面按库候选渲染**，而证据件只写「跑法：临库＋只给 kind／op」，**没写明冻结页的库态前提**（第四节第 2 条） |
| 断言强度 | **16** | 它的断言**可验且我都验过**（30/30、字节逐份、标记逐页、`WAKE_TABLE` 77、软删两类值）。扣 4 分：**变异存活**——`recordPicker.ts:82` 的「撤销候选只列未删／恢复只列已软删」改坏后，`tsc` 与 90 条测试**全绿**，这条规则**零测试护栏**（第三节第 5 条）。它不是恒绿无鉴别力（我另跑的候选/流程/预览断言都能区分对错），但「测试全绿」对这条核心过筛规则**不构成证据** |
| 边界覆盖 | **18** | 五类边界全部独立跑过且**结论都成立**：采集页不写库、唤醒词逃生（三条修正词不再抛、两条真必需槽位仍抛的对照也在）、软删 `now`/NULL、未知键 3／坏 JSON 2／不存在 id 4。扣 2 分：被审方证据件对「未知键、坏 JSON、不存在 id」三条**没有任何读数**（我替它补跑才有），边界面靠复核方补齐 |
| 产物真实性 | **15** | 无头 Chrome 渲染 4 份（覆盖三族）：**4/4 RENDERS OK**，PNG 97,717–150,246 字节，DOM 里 `data-page=collect/receipt`、块标题、`复制 prompt`、`记录编号`、`写入时间` 都在，`锟`／`U+FFFD` **0**。扣 5 分：**场景特有块在冻结页面里多为「条件渲染」**——`记分期` 的分摊预览表**不在**入库的采集页上（缺 `total/periods/start_date`，入库件只有阻断条＋槽位表，我补槽位才见到 12×300.00＝3600），`批量录入` 入库采集页只有 1 行编辑表＋阻断条（多行同屏那张表**页面自己写明「还没接」**），`记退款` 的三段式**只在采集页**、回执页 0 命中。这些页面**是真页面、真有块**，但「各有场景特有块」这句在**入库那一份的具体状态下**站不住 |
| 证据可追溯 | **19** | 提交号逐字 `5930127fe867a9378a84b535f9d77c170f0c8a93`；`git show --stat`＝53 件且与影响清单逐项对上；证据件七节全；30 份字节表逐份可回查且**与盘上完全相符**；返工登记到件到行（`lookup.ts` 三处可 diff 复现）。扣 1 分：第四节第 4、7 条两处数法未写明（行数 198/247/55 复现不出），自报两处变异未给可复现的最小步骤 |

**总分：18 + 16 + 18 + 15 + 19 = 86 / 100**

**结论：放行。** 判据没有「产物打不开」；「判据恒绿无鉴别力」不成立（我另跑的候选／三段式／分摊预览／软删断言都能区分对错，且它自报的两处变异读数与「必红」形状一致）；也没有推翻核心断言的「自称与实测不符」——冲突项集中在**可复现条件未写明**与**测试护栏缺口**两类，都已逐条给出最小复现命令。

**放行附三条（下一手第一件事）**：

1. **补 `keeps()` 的测试**（`src/shared/recordPicker.ts:82`）：一条「撤销候选不含已软删行、恢复候选不含未删行」的用例即可堵住最严重的那处**无护栏**（我实测改坏它 90 条全绿）。这也是被审方自己交棒里没提的一处。
2. **在证据件里写明冻结产物的库态前提**（哪几份是空库出的、哪几份带候选），否则 7／30 份页永远不可复现。
3. **`记分期`／`批量录入` 的入库采集页**：要么在证据件里注明「此行只是阻断态、特有块需补槽位可见」，要么把代表页重出成能看见分摊预览表／多行编辑表的形态（`批量录入` 的「多笔同屏那张表还没接」是页面自己写明的遗留，需挂号）。

---

### 回执

1. **交付**：`D:\ilife\docs\skills\skill-bill\t407-复核-十六页-独立复跑.md`（本件，UTF-8 无 BOM、真实换行）。
2. **总分**：**86 / 100** ＝ 可复现性 18 ＋ 断言强度 16 ＋ 边界覆盖 18 ＋ 产物真实性 15 ＋ 证据可追溯 19。
3. **编译测试**：`npx tsc -b packages/skill-bill --force` **退出码 0、诊断 0 条**（仓根 `tsc -b --force` 亦 0）；`node --test` 三次读数 **90／90／0、90／90／0、90／90／0**。
4. **30 次真跑**：**30／30 成功（exit 0）**；字节 **67,564–75,267**（洁净库口径）；仓内 30 份入库产物字节 67,460–75,049 与自报表**逐份相符**，其中 10／30 在我洁净库复跑下**字节相同**。
5. **场景特有块三条词**：记退款＝候选单选 ✅ ＋ 三段式 ✅（第 1/2/3 段三枚徽标，只在采集页）；记分期＝分摊预览表 ✅ **12 × 300.00 ＝ 总价 3600** ✅（补槽位后可见，入库那份是阻断态）；批量录入＝逐行可编辑表 ✅（1 行 7 格输入，多行同屏未接）；拍账单＝识别外置 ✅ ＋ 三要素文字填空＋复制通道 ✅。
6. **五类边界**：① 采集页不写库＝`bills 0→0` **成立**（库文件仍建）；② 只说唤醒词＝`撤销`／`恢复`／`改记录` 路由 exit 0 **不再报缺 id**，`查某天`／`查区间` 仍报缺槽位；③ 软删＝撤销后 `deleted_at="2026-09-14 15:39:17"`、恢复后 `NULL`；④ 未知键 `exit 3`、坏 JSON `exit 2`、不存在 id `exit 4`、非正整数 id `exit 2`；⑤ 变异＝我一处改坏**存活**（90 绿），已还原。
7. **无头渲染**：4/4 RENDERS OK（记收入采集/回执、批量录入采集/回执），PNG 97,717–150,246 字节；看到的块：`data-page=collect`「补齐收入槽位」「批量录入 · 补齐槽位」＋`复制 prompt`／`缺项阻断条`／`识别分工`；`data-page=receipt`「记收入 · 回执」「批量录入 · 回执」＋`状态`／`影响行数`／`写入字段`／`对账信息`／`退出口`；`锟`／`U+FFFD` 0。
8. **事故面**：**无残留乱码**（三件与全包搜 `锟`／`U+FFFD` 均 0 命中）；`lookup.ts` 差异面＝它自报那三处，**未见未复原的破坏性差异**；但「逐字复原」**无法证实**（原稿不在 git，只能证「现状无乱码＋差异面对得上」）；四张页正文自洽、`<section>` 配平。
9. **自称与实测不符：6 条**（实质 4 条）：冻结产物库态前提未写明（7／30 洁净库复现不出）｜采集页不写库但建库文件｜行数自报 198/247/55 实为 191/237/50｜自报两处变异未独立复现（我另挑的一处变异存活）｜另两条为「相符登记」与「弱证据登记」。
10. **结论：放行**（附三条：补 `keeps()` 测试、写明冻结产物库态前提、`记分期`／`批量录入` 代表页或加注挂号）。
11. **总读取行数：约 905 行**（源码片段 6 处共约 330 行：`slots.ts` 61、`flowSteps.ts` 69、`scene-photo.ts` 头 40、`scene-refund.ts` 62、`cmd_read.js` 片段 60＋61、`wakewords.ts`/`registry.js`/`scene.ts`/`commands.ts`/`recordPicker.ts` grep 行约 60；证据件整读 292；`t407-场景落点清单.md` 51；`t407-页面块清单-16词.md` 74；本次生成的临时脚本与输出末 25 行合计约 100；命令输出尾行约 120）。
