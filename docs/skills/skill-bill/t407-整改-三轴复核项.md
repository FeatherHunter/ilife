# t407 整改 · 三轴复核项对账

整改对象：提交 `a1fc236`（页面积木 7 件 ＋ 缺口块 ＋ 代表页两张）。整改执行：本窗口（本轮唯一写者，只改
`packages\skill-bill\` 与 `docs\skills\skill-bill\` 下本单要改的三份件）。

三轴来源：`t407-复核-…-结构纪律A.md`（68 分，须整改 5 条）／`…-结构纪律B.md`（88 分，须整改 2 条）／
`…-独立复跑.md`（94.5 分，放行；它的两条后手观察 4.7 `data-shape` 与 4.8 重复检测口径由维护者裁定并入本单，
第四轴两条）。本件每行给：轴／条目／严重度／改在哪／改后摘要／状态。

## 逐条对账

| 轴 | 条目 | 严重度 | 改在哪 | 改后摘要 | 状态 |
|---|---|---|---|---|---|
| A | C1-1 缺省值两份（`prefillNote.ts:27–31` vs `policy/category.ts:11`） | 硬（违纪） | `src/shared/prefillNote.ts` | 删掉本件那份账本「生活」／币种「人民币」，改引 `policy` 的 `DEFAULTS.account`／`.ledger`／`.currency`（`collect.ts` 早有先例） | 完成 |
| A | C1-2「符号即方向」四处（`pageFrame.ts:27-28`／`blockedSlots.ts:37-40`／`summaryRow.ts:35`／`collect.ts:51`），且 `moneyDirection` 零外部调用方 | 硬（宣称不实） | `src/shared/summaryRow.ts`（定义地）；`src/shared/typeBadge.ts`／`src/shared/blockedSlots.ts`／`src/shared/summaryRow.ts`（引用点） | 收成一处：`DIRECTION` 表 ＋ `directionOf`（判定取值）＋ `moneyDirection`（那句话）；徽章那句、阻断条的方向判定与文案、摘要行方向格都真引它——`moneyDirection`／`directionOf` 现有 3 处真引用 | 完成 |
| A | C1-3 时间缺省 `12:00:00` 三处 | 硬 | `src/policy/category.ts`（真源）；`prefillNote.ts`／`summaryRow.ts`／`collect.ts`（引用点） | 口径层加 `DEFAULT_TIME_SUFFIX` ＋ `defaultTimeOn(date)`；`validateTime`／`validateRecord`／预填值／摘要行那句都引它 | 完成 |
| A | C1-4 金额两位小数三处 | 中 | `src/shared/summaryRow.ts`（定义地）；`duplicateNote.ts`／`blockedSlots.ts`（引用点） | 收成 `money2` 一处；重复检测的比对与列示、阻断条的方向文案都引它（正数带 `+`，故方向文案由 `35.00` 变 `+35.00`，断言已同步） | 完成 |
| A | C2 `failureReceipt` 去留（能力目录 0 调用方） | 中（薄层） | `src/shared/errorReceipt.ts`（原 `failureReceipt.ts`） | 定为共用位内部件：件名与件头统一「错误回执」，整页替换那条债写在件头；改记录页头那句引用同步 | 完成 |
| A | C2 附 零外部引用或调用点不实的导出 | 中 | `src/shared/typeBadge.ts`／`pageShell.ts`／`prefillNote.ts`／`emptyNote.ts`／`summaryRow.ts`／`duplicateNote.ts` | `PAGE_EYEBROW` 不再 export（收进件内）；`TypeBadgeInput`／`PageShellInput`／`DuplicateHit` 保留（各自是公开函数的入参／返回类型，按实际留）；`prefillNote`／`emptyNote` 自认的「两个调用点」按实际改成「一处调用 ＋ 后票落点」 | 完成 |
| A | C3 四件没写第二个消费者（`blockedSlots.ts:6`／`duplicateNote.ts:6`／`prefillNote.ts:7`／`emptyNote.ts:6`） | 硬 | 四件件头 | 四件各补「第二个消费者：`src/query/`（随兄弟图 #403 的查询域一起到位）」，并各自写明本票真调用点（`blockedSlots` 两处／`duplicateNote` 两处／`prefillNote` 一处／`emptyNote` 一处）；`blockedSlots.ts` 原来写的 `receipt.ts` 是假的，删 | 完成 |
| A | C3 附 证据件 `:20` 虚报「七个件头各写了第二个消费者」 | 硬（虚报比缺陷严重） | `t407-冻结-页面积木与代表页-证据.md` §1.1 末 | 改成实话：只有四件写了、另四件当时没写、本次补齐；`prefillNote`／`emptyNote` 的名义调用点也逐条改正 | 完成 |
| A | C4 用词 10 处（源码 5 ＋ 证据件 5） | 硬 | 源码 `pageFrame.ts`（随拆分重写为 `pageShell.ts`／`typeBadge.ts`）；证据件 5 处 | 源码那一个词 5 处全删（改「页面模板与页头」）；证据件 5 处逐处改写（2 处同词、1 处判断语、2 处连带）；改前原文按「位置」列可查，正文不重印 | 完成 |
| A | C5-1「失败回执」vs 施工图「错误回执」 | 中 | `src/shared/errorReceipt.ts` ＋ 唯一调用方 `blockedSlots.ts` ＋ 证据件 | 统一「错误回执」；文件改名 `shared/errorReceipt.ts`，导入点同步（本包 grep：旧名 0 命中） | 完成 |
| A | C5-2 `pageFrame` 一件管两块（页面模板与页头 ＋ 类型徽章） | 中 | `src/shared/pageShell.ts` ＋ `src/shared/typeBadge.ts` | 拆成两件：对外名字数不变（各 2），两页各两处 import 同步；`pageFrame.ts` 删除 | 完成 |
| B | C6 方向判定管宽了（`write.ts:157`／`:178` 两支共用一个判定，撤销／恢复带 `kind`＋`amount` 也被拦） | 硬 | `src/record/write.ts:183`；`src/shared/blockedSlots.ts:36–39` | 收窄到录入路径：`writeRecordAdd` 传 `kind` 判方向，`writeRecordUpdate` 传空串不判；`blockedItems` 件头与注释写明「方向判定只服务录入路径」 | 完成 |
| B | C7 补「改前『照写』的后果 ＋ 影响面」 | 硬 | 证据件 §1.3 第 8 条后面那两句 | 补两句：(a) 照写会留一笔方向相反的金额（支出写成收入），按符号汇总的统计／预算读错，而 `validateAmount` 只校数字不校方向；(b) 影响面＝判定原挂在 `write.ts:158`／`:183`，收窄后**只管 `bill.record.add` 录入路径** | 完成 |
| B | C7 附 `collect.ts:17` 头注释「九块」与实读十块不一致 | 轻 | `src/record/collect.ts` 件头与 §2.3 | 件头按实读改成十块并列名；证据件 §2.3 同步写「共十块」 | 完成 |
| 裁定 | C8 `data-shape` 在采集页也写 `receipt`（原独立复跑 4.7 的观察，经维护者裁定成整改项；下游靠它选页会选错） | 中（维护者裁定） | `src/shared/writeParts.ts`；`pageShell.ts` 调用点；两页传值；测试 | `data-shape` 不动（envelope 契约）；另加 `data-page="collect"`／`data-page="receipt"` 作选页标记；测试逐条钉住（含「整页只有一枚 `data-page`」「回执页不得带 collect」） | 完成 |
| 裁定 | C9 分类未给时重复检测按「同日同额」比（原独立复跑 4.8 的观察，经维护者裁定成整改项），采集页天然易误报 | 中（维护者裁定） | `src/shared/duplicateNote.ts` 判定处；测试 | 分类未给＝不出条；只在「已给分类 ＋ 同一天 ＋ 同金额」三条齐时出条；加断言钉住「分类未给不出条」（同日同额也不报） | 完成 |
| 独立轴 | C10「产物能打开、样式已内联」只给结论没留机器读数 | 轻 | 证据件 §3.2（新增机器读数表） | 补：`<!DOCTYPE html>` 命中、单块 `<style>` 34219 字符与首行、规则 313／变量 13／`var(--…)` 176／`@media` 13、内联脚本 21908 字符、外部请求 0／0、真 DOM 起标记与 base 类名 token 计数；读数落 `.scratch\t407-fix\out\readings.json` | 完成 |

**偏差 ＝ 0**（上表 16 行对三轴 10 条 ＋ 两条裁定 ＋ 一条独立轴观察，逐条有着落）。

## 未改与理由

1. **三份复核件正文一个字没动**（`t407-复核-…-结构纪律A.md`／`…-结构纪律B.md`／`…-独立复跑.md`）：它们是复核方的件，
   改了就成自己给自己写评语；本单只交对账件（本件）与证据件。**维护者裁定「不要去改它」的那条也照办。**
2. **施工图 `t407-页面块清单-16词.md` 没动**：它第一节写的「错误回执」与本票件名对齐的那一半已按它改（件名改了），
   但它第一节第 1 行的块名里带着`docs/agents/wording.md`禁用的那个词，属施工图自己的行头；改它会把施工图与三份复核件的引用对不上。
   已记在证据件 §7 的遗留里（本件不重印那个词）。
3. **首轮提交 `a1fc236` 的信息不改**：红线禁 `amend`／`rebase`／`push`／`reset`，那一个字留在 git 历史里；本次提交信息
   逐字用维护者给的那句，不带那个字。**未改的理由是红线，不是没看见。**
4. **`toFixed(2)` 的其余出现点没动**：`record/write.ts:67／167`（回执明细与摘要句）与 `src/cli/cmd_read.ts:318／326／384`
   （预算／目标／转账的旧文案）。口径收单针对的是「同一句话在多处各写」，这两处是各说各的账目文本；`write.ts` 那两处
   跟库内真值直连（改`money2`会把「未给」那种措辞带进库内取值的场合），本次不动，留作后票。
5. **`collect.ts:52` 的槽位提示「缺省＝今天 12:00:00」没并**：那是表单字段的一句提示，不是取值逻辑；真源已由
   `DEFAULT_TIME_SUFFIX` 一处定义。并它得让槽位表反查口径层，得不偿失，本次不改。
6. **`failureReceipt`／`pageFrame` 两个旧路径名在 `dist/` 里留过残留**：`tsc -b` 增量不清理已删源文件的旧产物
   （`dist/shared/` 下两件各 4 个文件仍在），本轮已手工删净（现 0 件）；`dist` 在 `.gitignore`
   里、不在管辖内，删它们不动任何入库件。后手换件名后**顺手看一眼 `dist/<目录>/` 有没有旧名残留**，别让 CLI 取到旧产物。
7. **`src/query/` 那个第二消费者没建**：它是兄弟图 #403 的活，本票只按包内惯例在件头写清去向，不代建目录。

## 交棒

1. **`data-page` 是全仓选页的读法**：两张页的 `data-shape` 同为 `receipt`（契约），下游选页一律读 `data-page`；
   后手 15 张页铺开时给每张页一个自己的 `data-page` 值（本票先用 `collect`／`receipt` 两枚）。
2. **重复检测口径已收窄**：只在「已给分类 ＋ 同日 ＋ 同额」出条；**分类未给一律不出**（宁可漏提示，不误报）。
   15 张页若想要「分类未给也提示」，得先回来改这条裁定，别在页里自己写第二份判定。
3. **方向判定只服务录入路径**：`bill.record.update` 的改字段／撤销／恢复三支不判方向；若后手要给它加方向校验，
   得单立一处口径（例如「撤销的是哪一型、要什么符号」），不许把它挂回 `blockedItems` 的录入口径上。
4. **四条口径的单一定义地**（后手照引，别抄）：`summaryRow.ts` 的 `money2`／`moneyDirection`／`directionOf`、
   `policy/category.ts` 的 `DEFAULTS`／`DEFAULT_TIME_SUFFIX`／`defaultTimeOn`。
5. **本票没动的两件既有超线件**（`src/triggers/wake-assets.ts` 986、`src/cli/cmd_read.ts` 558）仍挂号，拆法归后票。
6. **整改后读数**：`tsc -b` 退出码 0／零诊断；`node --test "packages/skill-bill/test/*.test.mjs"` ＝ 78 通过／0 失败
   （整改前 76／0）；采集页 68323 字节／回执页 67289 字节（真跑重出，逐字节拷入）；两条变异各真红真绿（证据件 §4.2）。
