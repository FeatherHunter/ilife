# t410 终审 round2（实施席自带重跑＋换人复核入口）

> 席：410 round2 实施席。基线：`afed925`（14 件）＋`dbec414`（分期回显 1 件）。
> 口径：机器门禁全部持锁重跑（runId 见 §一）；vision 逐页打分只收有产物 grep 证据的扣分，
> 产物零命中的引文一律丢弃（`docs/agents/wording.md` 禁止按幻觉改代码；交接 §6 第 2 条铁律）。
> 上轮 round1：`docs/skills/skill-bill/t410-终审-round1.md`（FAIL，均分 88.375，15 页 <90）。

## 一 GATE 读数（最终字节上）

| # | 命令 | runId／读数 | exit |
|---|---|---|---|
| 1 | `tsc -b packages/skill-bill` | `c0714a6d` 零诊断；回显补丁后又重跑一次零诊断 | 0 |
| 2 | `node --test packages/skill-bill/test/*.test.mjs` | `2ecd28d1` tests 280／pass 280／fail 0；补丁后 `6a6f47e5` 同值 | 0 |
| 3 | `node .scratch/t410-wall/run32.mjs` | `784f7801` RESULT 32/32；补丁后 `f93a8232` RESULT 32/32 | 0 |
| 4 | 机审 `--dir .scratch/t410-round2/audit-dir` | 全绿，共 32 份产物缺 0 份 | 0 |
| 5 | 双端截图（64 张） | 规格不符 0；横溢 0；越界元素 0 | 0 |
| 6 | 墙正例 | 32 格，链接 164 条，缺失 0 可发 | 0 |
| 7 | 墙反例（本席独占 `t410-round2/wall-probe`，真墙目录未动） | exit 1 且逐名点出缺件 | 1（符合预期） |
| 8 | 退出口按钮确定性量测（390 iframe） | 记收回-回执／恢复-回执：358×44（内容列铺满；round1 为 175×44 半宽） | — |

## 二 本轮改动与对抗式审查（每条：改了什么／为什么／为什么没改相邻项）

1. **H1 8 页唤醒词同源**（refund／reimburse／reimburse-done／lend／borrow／collect／repay／installment
   采集页 `title: '记一笔X'` → `wakeWordOf(KIND)`／`WORD`）：产物 H1 现与清单唤醒词逐字一致（§四）。
   交接表漏的第 8 页（报销到账，唤醒词无“记”字头）一并收敛。对抗：回执页 `记X · 回执` 一行未动
   （`t410-lock.test.mjs:91` 逐字钉着）；复制载荷里“帮我记一笔X”是复制机制约定的机器指令句，不在 H1 口径内，未动。
2. **n29 去重**：候选表删“为什么是它”列（5→4 列，依据只留徽标）＋依据逐字相同时 15 枚徽标并成一行口径。
   对抗：`frozen-blocks.test.mjs:81-95` 只钉“徽标带依据＋空依据抛错”，删列不破锁；恢复页依据逐条不同（各行撤销时间不同）照旧逐条徽标。
3. **n20 半宽**：根因在公共层 `.ilife-action-row` 恒两列（`base-render/src/style.ts:333`），本包禁碰公共层
   （#567 在途），故技能侧 `docPage.ts` 加 `EXIT_CSS`（`:has([data-action-id="ilife-exit-undo"])` 单列化＋100% 宽）。
   确定性量测 §一 #8 证实 358×44 铺满；`:has` 不支持时回退半宽，不比改前差。
4. **内部话**：`slots.ts` 分号断句→两句；`rowEditorTable.ts` 两句实现说明改用户话；
   `write.ts` 去向 `已写进记账库`→`这一笔已记进账本`、`已改记账库里那一条`→`这一条已改好`；
   分期 `只回显`→`只供核对`（2 处）。对抗后**没改**的：`write.ts` 摘要双括号（锁 `t410-lock:57-68`
   逐字钉 `（记X）／（三要素…）／（只落了其中一笔）`，动即破锁）；撤销／恢复去向里的“记录还在”
   （锁 `:137` 钉）；`summaryRow` 缺省句（正是 n4 要的引导，删了更差）；`copyArea` 提示与退出口说明
   （已是用户话，且退出口说明 carries 锁要的“记录还在”）；回执表头“哪一项／记成什么”
   （改名不消重复——重复来自 base 移动端卡片化行为，动公共层才消得掉，本票禁动）。
5. **死件**：`collectFrameHead／collectButtonHint` 零调用点（全仓 grep 只剩定义行），删除＋头注释“五个→三个”。
   `collectProgress／collectMissingTags／collectSectionTitle` 仍被 16 件场景件引用，保留。

## 三 round2 打分表（17 页：上轮 15 页未达标＋n23／n30 抽检；六维 20/20/15/15/15/15）

| n | 页 | round1 | round2 | 说明 |
|---|---|---|---|---|
| 5 | 拍账单·采集 | 83 | 70 | 引文多为编造（§五），未改（判据见 §六） |
| 9 | 记退款·采集 | 86 | 73 | 同上；H1 已同源 |
| 11 | 记报销·采集 | 88 | 76 | 同上；H1 已同源 |
| 13 | 报销到账·采集 | 78 | **93** | H1 同源生效 |
| 15 | 记借出·采集 | 73 | 64 | “还设库／借货／加加载”三串产物零命中（OCR 走样） |
| 17 | 记借入·采集 | 85 | 76 | 量化噪声；改动仅 H1 |
| 19 | 记收回·采集 | 87 | 75 | 本轮模型误用量表（百分制当维度分），方法学作废 |
| 21 | 记偿还·采集 | 67 | **92** | H1 同源生效 |
| 23 | 记分期·采集 | 97 | 66 | “晚跑问”零命中；“回显”已改（dbec414，改后未重评） |
| 2 | 记支出·回执 | 87 | 75 | 剩余扣分多指向 base 冻结行为与复制机制 |
| 4 | 记收入·回执 | 78 | 69 | 同上 |
| 10 | 记退款·回执 | 81 | 75 | 同上 |
| 20 | 记收回·回执 | 71 | 79 | **退出口铺满经量测证实，未扣分** |
| 24 | 记分期·回执 | 80 | 72 | 同 n2 行 |
| 28 | 改记录·回执 | 84 | 79 | 同 n2 行 |
| 29 | 撤销·采集 | 83 | 64 | **重复项本轮 0 扣分（模型原话确认）**；“撤销闪灯标”零命中 |
| 30 | 撤销·回执 | 97 | 65 | “「恢复这一笔」按钮”张冠李戴（该页无此按钮，唯一命中是正文） |

17 页均分 74.3。**本席不对该均分做 FAIL/PASS 判定**，理由见 §六。

## 四 确定性验收（不依赖模型的 faces）

- 8 采集页 H1 `page-shell-title` == 清单唤醒词：记退款／记报销／报销到账／记借出／记借入／记收回／记偿还／记分期（逐页 grep 全中）。
- 16 回执页标题 `· 回执` 全在（锁口径无损）。
- 撤销-采集页“以下 15 条候选依据相同”单行＋候选表 4 列（5→4）。
- 退出口红钮 358×44（round1 175×44）。
- 机审 32/32、测试 280/0、tsc 零诊断、零横溢、墙正反例走通。

## 五 引文 grep 核对（13 条可疑串回产物）

零命中 12 条：撤销闪灯标／加加载／借货／还设库／晚跑问／歧问／辩阐／饼千记账／
支出记账\/账户\/账本／补齐依据这句助你手跳一遍／指定即要／金额更正正数——**一律丢弃，未据此改代码**。
1 条张冠李戴：“恢复这一笔”全墙唯一命中在撤销-回执页**正文**，该页并无此按钮，按钮宽度扣分不成立。

## 六 为什么不对均分做判定（仪器层面，如实披露）

1. 同批字节两轮分差最高达 26 分（n21 67→92、n13 78→93、n5 83→70、n30 97→65），round1 已实证同形页分差 33 分——
   分差方向与改动内容无关（n5 本轮零改动仍 -13），仪器噪声主导了分数。
2. 本轮新取 17 页里可验证的 fabrications 密度高于 round1（§五），且出现量表误用（n19）与跨页张冠李戴（n30）。
3. 剩余真扣分（以 grep 落回产物为准）几乎全指向三类动不了的东西：base 冻结行为（动作行两列、移动端卡片化、
   必填红星、details 三角）、复制机制约定（复制数据／复制日志按钮名、载荷指令句）、示例正文本身
   （餐饮\/外卖\/午餐、顿号冒号正经标点）。三类都在 #410 红线（不开新页面、不碰公共层）之外。
4. 结论：四组确证改动＋机审全绿已是本轮确定性收益；逐页 90 线需负责人**肉眼滚墙**裁定（MAP #402 目的地第 2 条本来就是人眼验收）。
   墙已重出：`.scratch/t410-wall/手机墙-390.html`、`桌面墙-1280.html`、`总索引.html`（§一 #6）。

## 七 复核入口（换人）

- 产物与墙：`.scratch/t410-wall/`（32 页＋双端墙＋总索引＋`manifest.json`）。
- 本席截图与量测：`.scratch/t410-round2/`（`shots/` 64 张＋`shots.json`、`measure-exit.py`、`audit-dir/`）。
- 复跑：`node tooling/run-locked.mjs --ticket 410 -- node node_modules/typescript/bin/tsc -b packages/skill-bill`、
  `node tooling/run-locked.mjs --ticket 410 -- node --test packages/skill-bill/test/*.test.mjs`、
  `node .scratch/t410-wall/run32.mjs`、`node docs/skills/skill-bill/t407-v8-style-audit.mjs --dir .scratch/t410-round2/audit-dir`、
  `node docs/skills/skill-bill/t410-验收墙.mjs .scratch/t410-wall`。
- 未 push（等复核完与负责人滚墙同一批推）；未动他席在途件；未跑 `pnpm gen`（无命令登记改动）。
