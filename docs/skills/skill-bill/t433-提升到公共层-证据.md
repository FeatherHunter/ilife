# T433 · 回执页共用件提升到公共层：诊断证据（未实施）

- 票：[#433 饼干记账＋卡路里：回执页共用件提升到公共层（base-paint，合并已落地的两份）](https://github.com/FeatherHunter/ilife/issues/433)（图 [#634](https://github.com/FeatherHunter/ilife/issues/634)）
- 本文件成文时点：2026-09-17 00:2x～00:4x（Asia/Shanghai），仓库 `master`，工作树**不干净**（他席在途）。
- 本文件只记**读到的现状与机器读数**；实施一行未做。计数口径一律 **LF（只数 `\n`）**，与各包 `AGENTS.md` 的告警线口径同。

## 一 · 开工硬条件复测（票面「已认领？＋ 已认领前核查」）

票面前一轮核查（2026-09-16，[票面评论](https://github.com/FeatherHunter/ilife/issues/433#issuecomment-5693858469)）给的两条阻塞，**今天仍成立**：

| 硬条件 | 当刻读数 | 判 |
|---|---|---|
| 卡路里套件全绿 | `node --test "packages/skill-calorie/test/*.test.mjs"` → `ℹ tests 1746 / ℹ pass 1658 / ℹ fail 88`，exit 1 | **未满足** |
| 工作树干净 | `git status --porcelain`：`packages/skill-calorie/src/photo/*` 与 `packages/skill-calorie/test/photo-gif-page-352.test.mjs` 等**多处未提交修改**（他席在途） | **未满足** |

机器读数（本次两跑的运行标识与命令，均经加锁包装器）：

```
GATE-RUN runId=t433-baseline-calorie cmd=node --test "packages/skill-calorie/test/*.test.mjs"
GATE-RUN runId=t433-movegate-before cmd=node --test packages/skill-calorie/test/copy-component-179.test.mjs packages/skill-calorie/test/profile-doc-179.test.mjs packages/skill-calorie/test/exercise-receipt-264.test.mjs packages/skill-calorie/test/weight-receipt-337.test.mjs
```

- 全量：`RESULT: ... exit=1`，摘要 `tests 1746 / pass 1658 / fail 88`。
- 与本票直接相关的靶向四件（搬迁判据的落点）：`tests 42 / pass 37 / fail 5`，红在
  `copy-component-179.test.mjs` 的 4 条（`#247 ④`／`#239 日志六段`／`#239 弹提示`／`#247 场景 07 五张页`）与 `#365 身体细节四条写命令已是完整文档`。
- 红因**不是本票造成的**：`copyArea.ts:53` 的 `MENU_HINTS = ['', '', '']`（注释自述作者 2026-09-14 裁定删掉用途说明）与测试里「缺老仓原样的用途提示」的断言正面冲突；该冲突在 `HEAD` 与工作树两处同形（`copyArea.ts` 无未提交修改）。
- 与他席的现场冲突（并发纪律）：`647` 在跑 `packages/skill-calorie/test/t275-…`，`.scratch/locks/owner.json` 当刻属于它；本席读数一律经 `run-locked` 排队取。

**结论**：票面「恢复条件：卡路里套件回绿且工作树干净」**未满足** ⇒ 验收面「两边套件全绿＋产物逐字节不变」的绿↔绿对拍结构上做不出来。

## 二 · 差量复测（对比 t406 清单，2026-09-14 版）

件行数（LF，当刻实测）：

| 件 | 卡路里 | 饼干 | t406 清单记的数 | 差 |
|---|---|---|---|---|
| `shared/copyArea.ts` | **202** | 159 | 卡 187／饼 116 | 卡 ＋15／饼 ＋43 |
| `shared/docPage.ts` | **161** | **130**（另有 `shared/pageShell.ts` 65 行做域侧外壳） | 卡 113／饼 53 | 卡 ＋48／饼 ＋77 |
| `shared/writeParts.ts` | 113 | **89** | 卡 113／饼 73 | 饼 ＋16 |
| `shared/receiptParts.ts` | 43 | **46** | 卡 43／饼 45 | 饼 ＋1 |
| `shared/commandSpec.ts` | 66 | 62 | — | — |

**两侧已经分叉到「同名不同物」的四处**（逐条给位置，都与产物字面有关）：

1. **整页壳**：卡路里 `docPage.ts:101-111` 走 `<!doctype html>`（小写）＋ `charts` 槽＋ `pageUi` 视口配方；饼干 `docPage.ts:33-39` 走 `<!DOCTYPE html>`（大写）＋ 无槽，且**另带四段技能补丁 CSS**（`DESKTOP_CSS`／`TOAST_CSS`／`KV_CSS`／`EXIT_CSS`，`docPage.ts:48-115`）。合并＝把**大小写不同的 doctype** 做成入参，否则必改一方的产物字节。
2. **复制区 prompt 位**：卡路里 `copyArea.ts:109-115`＝`renderPreBlock` ＋ 包内 `copyActionHtml`（`render/copy.ts:38`）→ `renderActionBar({copyData})` 的 ghost 按钮；饼干 `copyArea.ts:80-89`＝`renderPreBlock({actionId, copyText, copyLabel})` 自带的按钮。**两套构造**，合并要选一套，选哪套都改另一方的产物字节。
3. **菜单用途提示**：卡路里 `copyArea.ts:53` ＝ `['', '', '']`（空串，不渲染）；饼干 `copyArea.ts:41` ＝ 三行中文（`纯文本 粘贴给助手或自己看`／`JSON 结构化存档`／`CSV 表格导入`）。**同一个位的两种相反做法**。
4. **对账信息表**：卡路里 `receiptParts.ts:31-42` 三行（表头「项／值」，含 `回执格式 v1（写库回执）`，`recordId === null` 写「未设置」）；饼干 `receiptParts.ts:35-45` 两行（表头「哪一项／记成什么」，无格式行，空值写「还没有」）。**函数体不同物**。
5. **复制日志六段**：卡路里 `copyArea.ts:177-185` 第 3 段＝`DB_FILENAME ｜ source`、第 2 段「本页由本地 CLI 渲染，无 AI 链」；饼干 `copyArea.ts:133-141` 第 3 段＝只有调用方给的 `source`（缺省还回落到空态句）、第 2 段「本页由本地渲染，无助手链」。

真正的**同物**只剩：`totalChanges(db)`（卡 `writeParts.ts:38-41` ＝ 饼 `writeParts.ts:51-54`，逐字同）、`esc`（三字符，两侧同形）、`COPY_EMPTY_TEXT`（两侧同串）、`LOG_EXCEPTION`（两侧同串）、`statusCard` 的**对象字面形状**（卡 `receiptParts.ts:21-27`＝饼 `receiptParts.ts:19-29`，取数不同）。

## 三 · 「第二个使用者」逐件重判（按结构标准口径）

| 件（对外名） | 第二个使用者是谁 | 当刻结论 |
|---|---|---|
| `assembleDocPage` | 有：卡路里 30 余个 `*Docs.ts`；饼干 `record/`＋`query/` 两域走 `shared/pageShell.ts` | 有，但**两侧已非同物**（见 §二.1）：可合并的只有「拼页骨架」，差异（doctype 大小写／槽／四段补丁 CSS）要参数化，参数量与两侧差异项等量 |
| `copyArea` | 有：卡路里 30 余个调用点；饼干写入域＋查询域 | 有，但 prompt 位是**两套构造**（§二.2）；卡路里侧那一枚还牵着包内 `render/copy.ts` 的冻结缺省 |
| `copyLog` | 有：同上 | 有，但第 2／3 段两侧字面不同（§二.5） |
| `statusCard`／`reconcileDisclosure` | 有：卡路里 `goal/receipt.ts`／`exercise/receipt.ts`／`diet/receipt.ts`／`workout/receipt.ts`；饼干 `record/receipt.ts`／`record/collect.ts` | 有，但 `reconcileDisclosure` 两侧**函数体不同物**（§二.4）；`statusCard` 只剩对象字面形状同 |
| `writeParts`（机制：`R`／`out`／`receiptHtml`／`provided`／`definedKeys`／`cliNames`／删除措辞） | **无**：饼干侧对应物是 `writeSection`（`<section>` 段装配，含 `data-page`／`data-shape`／`data-key` 四个机器标记）与 `BillReceipt` 形状，与卡路里的 `R`／`out`／`receiptHtml` 不是同一件事 | 按票面「写不出第二个用法的留在原处」⇒ **不搬** |
| `commandSpec.ts` | 无（两侧各有一份，服务各自的命令面） | 同上 ⇒ **不搬** |
| `F`／`COL_CLI` 字段表 | 无（卡路里专有字段事实） | ⇒ **不搬** |
| `DB_FILENAME` | 无（两侧各自取值） | ⇒ **不搬** |

## 四 · 结论（不实施的理由）

1. **前置条件未满足**：卡路里套件 88 红、工作树有他席在途件；票面「恢复条件」原样写在这里，未满足不得开工。
2. **「搬家不是改样式」这条口径对当刻两侧已不成立**：§二 的五处差异全部落在**产物字面**上（doctype 大小写、按钮构造、提示串、表头与行数、日志段）。要把它们合到一处，必须新增一组入参把差异逐项抬出去；那是**形状统一**（本票「不许动」与「遗留出口」明确排除的活），不是搬家。
3. **票面「遗留出口」正好覆盖本现状**：「若某件在提升中暴露第三种差异……当场拆成两张票：一张只搬共同部分、一张记差异，不在本票里做形状统一」。当刻暴露的不是「第三种差异」，而是**同名两物的系统性分叉**。

## 五 · 下一步的可选路径（待负责人定夺，本席不自行选）

- **甲（等绿）**：等卡路里套件回绿＋工作树干净后重开，按原口径做「共同切片搬家」（`totalChanges`／`esc`／`statusCard` 形状／`copyLog` 六段填法这些真正的同物），差异件按遗留出口拆票。
- **乙（先拆票）**：当刻就把本票按「只搬共同部分／记差异」拆成两张子票（子票不依赖绿基线也能各自定范围），本票转成「只做共同切片」。
- **丙（带留痕的例外）**：接受「他席 88 红与本次搬迁零交集」这一论证，用**独立产物字节对拍**（不经测试断言）替代绿↔绿，并在证据里逐条列出与红项的交集为零。此路径要负责人书面点头（票面「验收」第 1 条与第 3 条都被放宽）。
