# t728 证据件：写入域 32 页视觉整改

「改了哪一层哪一件／为什么／谁批的」逐条见同目录 `t728-差异表.md`；本件记**读数**：机器判据、页指纹差异集、视觉评审判读、以及按判据 1～10 的自查。

跑法一律经并发闸门：`node tooling/run-locked.mjs --ticket 728 -- <命令>`。命令里带 `./` 前缀的写在锁内。

---

## 0 一句话

改前：产物功能齐、机器面全绿，唯独**视觉被判「不好看」**，而公共层 t508 分隔符门扫出 **32/32 页判红、节点级 161 处**。
改后：**t508 门 32/32 页 exit 0（节点级 0 处、行级 0 行）**；页面级移动端配方接线两处证据 32/32；本包全量 **407/407**；页指纹按本票号重录。

---

## 1 机器读数（改前 → 改后）

| # | 判据 | 命令 | 改前 | 改后 |
|---|---|---|---|---|
| 1 | 公共层分隔符门（判据 9 指定） | `node packages/base-render/test/separator-probe.mjs <每页>` 逐页跑 32 次 | **32/32 判红；节点级 161 处、行级 97 行** | **32/32 exit 0；节点级 0 处、行级 0 行** |
| 2 | 真出口锁 16 词 | `node --test packages/skill-bill/test/t410-lock.test.mjs` | tests 18／pass 18 | **tests 18／pass 18／fail 0** |
| 3 | 页指纹账本（判据甲） | `node packages/skill-bill/scripts/gen-page-fingerprints.mjs --write --declare-layout-change 728` | — | **RESULT 32/32**；`重录声明（票 728）·差异集 32 张`（＝本票声明的覆盖页集合） |
| 4 | 机审十一列 | `node docs/skills/skill-bill/t407-v8-style-audit.mjs --dir .scratch/t410-wall` | exit 0，分隔符懒政 0 | **exit 0，「读数：全绿」**；分隔符懒政 0 份／英文裸词 0／重复句 0 |
| 5 | 本页 0 CSS | 逐份量（`.scratch/t728-metrics.mjs`） | `<style>` 各 1、`style="` 各 0、`<link>` 各 0 | **`<style>` 恰 1 份：32/32；内联 `style="`：0；`<link>`：0；共享样式表 53,536 字符、32 页同一份** |
| 6 | 页面级配方接线（判据 9 的量测） | 同上 | `viewport-fit=cover` 命中 **0**、`ilife-page-ui` 命中 **0** | **两处各 32/32 页命中** |
| 7 | 墙自检（正例） | `node docs/skills/skill-bill/t410-验收墙.mjs .scratch/t410-wall` | 32 格；链接 164 条；缺失 0 → exit 0 | **32 格；链接 164 条；缺失 0 → 可发，exit 0** |
| 8 | 墙自检（反例，必跑） | 复制产物目录，把 `manifest.json` 的 `rows[3].file` 改成盘上不存在的名字再跑 `--check` | exit 1 且点名 | **`31格；…；缺失1 -> 不可发`，exit 1，逐名点名 `清单点名却没有文件：4 记收入 -> t410-页-这一份不存在.html`** |
| 9 | 本包全量 | `node --test "packages/skill-bill/test/*.test.mjs"` | 407/407 | **tests 407／pass 407／fail 0** |
| 10 | 公共层全量 | `node --test "packages/base-render/test/*.test.mjs"` | 762 里 1 红（`#79 三包版本 lockstep`） | **tests 762／pass 761／fail 1 —— 同一条**（`skill-calorie` 的 `base-link-core` 例外值漂移，实得 `undefined`／期望 `^0.1.0`；该件与 `packages/skill-calorie/package.json` 本票一行未碰，是 `0a55bd50 fix(727)` 那一版抬版本留下的） |

**反例复核（不是假绿灯）**：反例第一次没红——因为 `manifest.json` 里 `pages` 与 `rows` 是两张表，重出脚本读 `pages`、墙读 `rows`，改错表即自检照样绿。改 `rows` 之后复跑才红（上表第 8 行）。这正是 `docs/agents/视觉验收墙.md` §6.4 点名的那个坑，本次走了一遍。

---

## 2 判据 10：未列本票的域「不倒退」

公共层改动连带 6 个技能包。读数分两层。

**（a）双端截图 ＋ 量测**（代表页，`packages/skill-calorie`，用仓库外独占 tmp 库出页）：

| 页面 | 命令 | 双端截图 | 读数 |
|---|---|---|---|
| 卡路里 · 看体重 | `node packages/skill-calorie/dist/cli/cmd_read.js calorie.view.weight --params '{}' --html .scratch/t728-regress/calorie_view_weight.html` | 390（`.dsh-vision-router/artifacts/D--ilife-.scratch-t728-regress-c-282ba863-shot-390x844.png`）／1280（同目录 `-shot-1280x900.png`） | 版心与正文列对齐、无横向溢出、内容未被裁、字号不低于既有下限；页头那条新分隔线在卡路里页上同样成立 |
| 卡路里 · 看运动 | `.scratch/t728-regress/calorie_view_exercise.html`（同命令换键） | — | 同上（本页只出产物，未逐格截图） |

结论：**没有出现 D1 可证伪条件里那三种倒退**（横向溢出／内容被裁／字号低于既有下限）。

**（b）别包自己的门**（比肉眼量测更硬的一层）：

| 包 | 读数 | 归因 |
|---|---|---|
| `skill-chef`＋`skill-home`＋`skill-memo-ilife`＋`skill-schedule` 四包合跑 | tests 323／pass 322／fail 1 | 那 1 条是 `skill-memo-ilife` 的 `#240 自持件已删`（红在 `src/health.ts`，本票一行未碰）→ **既有红** |
| `skill-calorie` | tests 1855／fail 40 | **既有红 ＋ 字节冻结类两掺**，见下 |

**卡路里那 40 条怎么归因（如实记，不装作没有）**：

- 仓根基线本来就有红：`docs/skills/skill-bill/t730-门禁运行记录.md:47` 逐字记着「3801 条里 61 条红…逐条落在 `skill-calorie`（10＋条）／`base-render`（`#79 三包版本 lockstep`）／`plugin-memo-ilife`（1 条）／`plugin-bill-ilife`（2 条）」。
- 本轮实测，其中**与本票无关**的一大类：路由与词表（`唤醒词错键：看今日运动`、`场景表条数 == 写键数` 46≠52、`#97 场景表覆盖全部写键`、`#255 真：目标 29 词`）、单源扫描（`运动域标签表登记必须只有一处` 实得 4 件、`整份四值色表须只在一处` 实得 2 件、`#440 src 里旧词零命中`）、取数口径（`trendAvg` 668≠477 等 `data.metrics` 冻结值）。
- **确实由本票引起的一类**：几处「产物与改前逐字节相同／结构 sha 与改动前同值」的**字节冻结**断言（`#448 判据①②③`、`#254`、`#290`、`#365` 等）。它们的留档产物是更早的一版（对照留档里还写着 `.ilife-action-btn { min-height: 40px }`、`.ilife-toast { font-size: 12.5px }`，而那两处早已被 #533 改成 44px／13px ⇒ 这些断言在**本票开工前就已经红**）。
- 判据 10 的判据是**视觉不倒退**，不是「别包的字节冻结不被打破」——改公共层必然改掉每页 `<style>` 的字节，这一点 `#681` 的「设计联邦」整改本身就要求。故本票按 D5 办：**担保不倒退，卡路里自己的字节账本由其归属票重录**；不把它的重录拉进本票写集。

---

## 3 视觉评审（判据 7／9 第三条：三轮多数决）

规则先写死（照票面裁定 D4）：三族各 1 页 × 两档（390×844、1280×900）× 三轮；三轮中位数 ≥90 且至少两轮 ≥90 才算过；每条扣分必须能回产物核对，核对不上的丢弃；三轮读数全部公开。

**评审装置**（可重跑）：`.scratch/t410-wall/gen-评审页.mjs` 出两张评审板 —— `t728-评审板-手机.html`（三页原尺寸 390）与 `t728-评审板-桌面.html`（同三页 1280，整体缩 62%）。

**三轮实际读数（全部公开，不许只报过的那一轮）**：

| 轮次 | 提示词 | 返回 | 分数 |
|---|---|---|---|
| 第 1 轮 | 五维＋总分，要求 JSON | 详细版式判读 ＋ 文字总评「综合评分约 **84/100**」 | 84 |
| 第 2 轮 | 收紧成「只输出 JSON、不要别的话」 | 仍返回版式判读；文字小结「structure solid, hierarchy clear, typography consistent, responsive adapts well, **mobile density needs minor tuning**」，**未给数** | 无数 |
| 第 3 轮 | 同上 | 同上（仍不肯只吐 JSON） | 无数 |

**判定：本票不声称「视觉 ≥90」**。理由是这台仪器在本轮**没有给出可用的数值读数**（三轮里只有一轮给了数、另两轮连数都不出）。票面 D4 已经预订了这种情形：「三轮极差 >10 分即认为仪器噪声仍主导，本票放弃按分数判定，改按**缺陷清单归零 ＋ 维护者滚墙**收口，并把这一条写进证据件（不是悄悄降低标准）」。本轮的实际情形比那一条更硬——不是极差大，而是**仪器不出数**，故照同一条刹车处置：**改按缺陷清单收口**。

（另外：这台仪器的 OCR 大量读错（把「把缺的格逐格补齐」读成「把缺的格怪怪补齐」、「按库里的改动算」读成「按家里的改动量」），与票面「vision 打分是提示器不是证据」的口径一致，只当线索用。）

**逐条扣分回产物核对（判据 D4：核对不上的丢弃）**：

| 视觉席说的 | 回产物核对 | 处置 |
|---|---|---|
| 「分类那一格的提示被控件截断，390 档读成『…选到「午  ▾」』」 | **核对得上**：`slots.ts` 的提示 `三级分类，要选到最细那一级，如「餐饮」下面选到「午餐」` 比 390 档控件宽 | **已改**：提示收成 `要选到最细那一级，如「午餐」`（`slots.ts`／`scene-photo.ts`／`outsideScan.ts` 三处）；改后同一位置读全 |
| 「徽章行里元素竖直基线不齐」（第 1 轮，另一次调用） | **核对得上**：量到胶囊内距 2px 8px／字号 12，状态徽章 6px 14px／字号 13，`align-items:center` 只居中盒子 | **已改**：徽章行统一 30px 行盒、上下内距归零；改后裁图复核，三者同一条基线 |
| 「桌面端标题贴在版心最左、正文居中，左右不对称」 | **核对得上**：裁图量到标题 x≈26、正文 x≈202（`pageUi` ⑧ 只收正文那层） | **已改**：⑧ 段补一条，页头三级同样收进 880 并居中 |
| 「桌面端属性卡高度参差」 | **核对不上**：产物是栅格，同行卡片默认等高；裁图逐卡量上下沿一致 | **丢弃**（照 D4：核对不上一律丢弃） |
| 「手机版字段挤／密度需微调」 | 半个核对得上：390 档单列表单每格约 350px、44px 高，属配方既定档位，没有量到低于既有下限的字号 | 保留为提示，未据此改版式 |

**结论**：视觉这一关按票面判据 4 归**维护者肉眼判**（判据只在这一处终止）。双端墙、逐格缺陷清单、生成器自检退出码三样见下节。

---

## 4 交付三样（判据 8）

1. **双端墙**：`.scratch/t410-wall/手机墙-390.html`（32 格 × 390）、`.scratch/t410-wall/桌面墙-1280.html`（32 格 × 1280）、`.scratch/t410-wall/总索引.html`（按页族分组，末尾列「有意不出产物及其原因」）。形制照 `docs/agents/视觉验收墙.md`：每格一件真产物、不加 `loading="lazy"`、清单不带 BOM、`dropped` 与 `dead` 一起判。
2. **逐格缺陷清单**：见 §3 的核对表（两处已改、一处丢弃并写明理由、一处保留为提示）。看过的格＝三族 × 两档（记支出采集页／记借出回执页／批量录入采集页）＋ 卡路里代表页两档。
3. **生成器自检退出码**：正例 `exit 0`（32 格／链接 164 条／缺失 0）；反例 `exit 1` 且逐名点名（见 §1 第 8 行）。

---

## 5 按判据逐条自查

| 判据 | 自查 |
|---|---|
| 1 页面本地 0 CSS（新口径：样式随它的选择器） | 32/32 页 `<style>` 恰 1 份、内联 `style="` 0、`<link>` 0；本包补丁只留 1 段（表格宽度收口，选择器由公共层产出、只在 1200px 以上生效、零字面色值、断点取仓内既有值 `1200`）；另外三段里的字面色值与越界圆角已清掉 |
| 2 已锁机器判据仍绿 | t410 真出口锁 18/18（4 处 H1 断言改成新形状，**并加负向断言**，不放松）；指纹账本按 `--declare-layout-change 728` 重录，差异集＝声明的 32 页；机审十一列全绿 |
| 3 视觉硬约束不动 | `--blue:#007aff` 未动；未引入 `--r-xl`／`--pink`；深色区未动（唯一深色件 B-12 toast 位于公共层） |
| 4 视觉由维护者肉眼判 | 墙 ＋ 缺陷清单 ＋ 退出码三样备齐待滚墙；本件不代替法官 |
| 5 判据补洞 | 按判据 9 办：分隔符那一条**换用公共层 t508 门**，本包机审那份位置白名单**未扩**（不在仓里留第二份定义） |
| 6 产物重出与指纹重录 | `node .scratch/t410-wall/run32.mjs` → `RESULT 32/32`；指纹重录 32/32、差异集 32 张 |
| 7 视觉复评 ≥90 | **未声称达成**，三轮读数公开、判定与刹车理由见 §3；最终签字归维护者滚墙 |
| 8 交付三样一起交 | 见 §4 |
| 9 判据 1／5／7 按裁定换口径 | ① 判据 1 已办（见第 1 行）；② 判据 5 已办（见第 5 行）；③ 判据 7 三轮多数决已办、结论见 §3 |
| 10 未列本票的域不倒退 | 见 §2（卡路里代表页双端截图量测无倒退；四包合跑仅 1 条既有红） |

---

## 6 遗留出口（本票没做的，逐条说清）

1. **卡路里那批「产物与改前逐字节相同」的字节冻结断言**：改公共层必然改掉每页 `<style>` 字节 ⇒ 它们的留档必须由卡路里自己的票重录。本票按 D5 只担保不倒退，不把它拉进写集。**这一条与 D1 的实测预期不符**（D1 当时记「其快照账本只覆盖触发词元数据，页面字节不在内」——实测卡路里确有页面字节冻结门），如实记在这里。
2. **`t407-v8-style-audit.mjs` 的位置白名单**：判据 9 已把分隔符那一列并到 t508 门；那份白名单里的分隔符相关行**本票未动**（删它＝动判据脚本的判据面）。按票面「遗留出口」那一条，并轨的收尾归后续票。
3. **HELP 页自己缺 `viewport-fit=cover`**：本票未动（那是烤制资产 ＋ 生成器，另一张票）。
4. **其它 5 个域的「变好看」**：本票只担保不倒退。
5. **`RADIUS_LG = 20` 死常量**：本票未删（属跨包那档的「同一语义多名值」，本票只做了证据最强的那两处：字面色值、越界圆角）。留待跨域票。
