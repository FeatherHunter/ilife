# t691 证据 · account 域端到端搬迁（样板二）

- 票：[account 域：端到端搬迁（样板二）](https://github.com/FeatherHunter/ilife/issues/691)（父图 [#681](https://github.com/FeatherHunter/ilife/issues/681)）
- 差异表（本票产物之一）：`docs/skills/skill-bill/t691-差异表.md`
- 写集：`packages/skill-bill/src/account/`（新建 `commands/accounts/pageParts/scene/scene-{add,update,transfer}/template-{form,update,summary}/write/read.ts`，
  改写 `params.ts`／`index.ts`，删除 `query.ts`）＋ `src/cli/cmd_read.ts`（删两个 `case`、`runRegistered` 改调 `spec.run`、收窄 import）＋
  `src/render/envelope.ts`（过渡表删两行）＋ `src/shared/commandSpec.ts`（`receipt` 那一格的口径）＋
  `scripts/gen-cli.mjs`（生成物件头的读数）＋ `scripts/ratchet-frozen-686.mjs`（冻结值同窗下调）＋
  `test/{t691-账户域.test.mjs（新建）,cli,t721-域声明,t686-收紧守卫,t689-scene-shape,record-write}` ＋
  `packages/skill-bill/AGENTS.md`（台账 `--sync`）＋ 本件 ＋ 差异表 ＋ 探针 ＋ 运行记录导出。
  **一个字都没动**公共层（`packages/base-render/**`）、写入域与查询域的处理体、8 份域声明（`src/*/declaration.ts`）。
- 复跑（每条读数都可复算；持锁按仓规 `node tooling/run-locked.mjs --ticket 691 -- <命令>`）：

```text
node node_modules/typescript/bin/tsc -b packages/skill-bill          # 编译
node packages/skill-bill/scripts/gen-cli.mjs --stamp                 # 重打生成器内容印记
node packages/skill-bill/scripts/gen-cli.mjs                         # 重生成 src/cli/registry.ts（8 条／3 能力）
node --test packages/skill-bill/test/*.test.mjs                      # 全量测试（337/337）
node packages/skill-bill/scripts/gen-cli.mjs --check                 # 生成物对账
node packages/skill-bill/scripts/check-ratchet-tight.mjs             # 棘轮（9/9）
node packages/skill-bill/scripts/check-scene-shape.mjs               # 场景形状门（19/19）
node packages/skill-bill/scripts/check-warning-line.mjs              # 行数告警线门（14/14）
node --test packages/skill-bill/test/t691-账户域.test.mjs            # 本域靶向测试（10/10）
node docs/skills/skill-bill/t691-探针-真出口.mjs                      # 真出口探针（13/13）
```

门禁运行的一对一留痕（`runId`）抄在 `docs/skills/skill-bill/t691-门禁运行记录.md`。

## 一 票面四条判据逐条

### 判据 ①：4 条唤醒词逐条真跑（exit 0、产物落盘、字段正确）

真出口（spawn `dist/cli/cmd_read.js` ＋ `--html` 读回落盘页）实测，全部 `exit 0`、`delivery.path` 绝对路径、
`文件字节 = delivery.bytes`、整页（`<!doctype html>` ＋ `<section>`）：

| # | 唤醒词 | 命令与参数 | 读数（探针 runId `32945a92`） |
|---|---|---|---|
| 1 | 新增账户 | `bill.account.write {"op":"add","name":"招行卡","type":"银行卡"}` | 回执页 74,934 B 级产物；`data.ok=true`；`receipt.affectedRows=1`；`goals.json.accounts[0] = {name:招行卡, type:银行卡, disabled:false, created_at:YYYY-MM-DD HH:MM:SS}` |
| 2 | 改账户 | `bill.account.write {"op":"update","name":"招行卡","new-name":"招行工资卡"}` | `receipt.renamedRows=1`（级联那一笔转入）；回执页含「改了什么」块与改前／改后两值；账户表改名后余额仍 500 |
| 3 | 账户转账 | `bill.account.write {"op":"transfer","amount":500,"from":"支付宝","to":"招行卡"}` | `receipt.affectedRows=2`；回执页含「两笔分录」块 |
| 4 | 看账户汇总 | `bill.account.query` | `data.total=2`（登记 1 ＋ 只在流水里出现过 1）；汇总页含读数行／账户卡区／占比条／流水表／页内导航／来源脚注 |

四条词还逐条验了**路由**：`新增账户／改账户／账户转账 → bill.account.write`、`看账户汇总 → bill.account.query`，
且 `projectWakeWord` 算出的代表词路由回同一条命令（`dist/triggers/wakeTable.js` 直读）。

### 判据 ②：转账落两笔（转出负数 ＋ 转入正数）、账本＝转账、不入收支统计

| 项 | 读数 | 怎么取的 |
|---|---|---|
| 落两笔 | `receipt.affectedRows = 2`；两行的编号都在回执页明细表里 | 真出口回执载荷 ＋ 页面 |
| 转出负数／转入正数 | 分类 `转账/转出`（负数）与 `转账/转入`（正数），账本 `转账`，备注 `#转账 转出至X`／`#转账 转入自Y` | `accounts.ts` 的 `writeTransfer`（备注逐字回老侧 `cli.py:218-219`） |
| 两个账户一增一减 | `totals.balance = 0`（支付宝 −500 ＋ 招行工资卡 ＋500） | `bill.account.query` 载荷 |
| 不入收支统计 | `totals.income = 0`、`totals.expense = 0`；`bill.record.range` 的 `kpi.count = 0` | 两条命令的载荷（口径唯一判地＝`src/shared/kpi.ts` 的 `isTransfer`） |

### 判据 ③：与老侧逐条对照出一张差异表

`docs/skills/skill-bill/t691-差异表.md`：17 条逐条给「老行为／新行为／为什么改／谁批的」，
分五节（页型层 4 条／事实与口径层 8 条／判过而不动 4 条／T4 页型表更正 1 条／本票没做的 3 项）。
老侧 4 张模板的**信息块逐条落点表**见本件 §二。

### 判据 ④：路径从零走完（域声明 → 命令 → 页面 → 测试，过程中不需要再决定任何横向事情）

| 走过的段 | 落点 | 当刻状态 |
|---|---|---|
| 域声明 | `src/account/declaration.ts`（#721 已落，4 词条／1 二级组，本票一字未动） | 词表／HELP／路由都由它派生（2721 判据里 4 条词逐条路由成立） |
| 命令 | `src/account/commands.ts`（新建，两条）→ `src/account/index.ts`（域门）→ `pnpm gen` 重生成 `src/cli/registry.ts` | `GEN-CHECK PASS：命令 8 条；能力 3 个（account／query／write）`（runId `e5954c7e`） |
| 分派 | `src/cli/cmd_read.ts` 的 `runRegistered` 改调 `spec.run(params, handle)`（**不再按域写死入口**） | 第三个域进来时外壳这一支一行不动（`cmd_read.ts` LF 501 → 455，冻结值同窗下调） |
| 页面 | `src/account/template-{form,update,summary}.ts` 三片页型 ＋ `pageParts.ts` 域内共件 ＋ 三件场景件 | 场景形状门 `RESULT: 19/19`（16 件写入域 ＋ 3 件账户域，runId `b5490159`） |
| 测试 | `test/t691-账户域.test.mjs`（10 例）＋ `docs/skills/skill-bill/t691-探针-真出口.mjs`（13 例） | 10/10（runId `30fa744a`）／13/13（runId `32945a92`） |

**过程中真撞到的横向不确定项只有两处，都在本票当场定死并留痕**（不是留给下一票才发现）：

1. `WriteOut['data'].receipt` 那一格的形状：写入域写的是 `BillReceipt`，账户域的回执事实不是那一套
   （没有「记录编号」这个概念，转账是两笔）。处置＝**共用位不收窄成某一家**（那一格形状由各域自己定，
   出口只把 `data` 交 envelope 形状守卫，而 `receipt` 形只校验 `ok`／`message`），口径写进
   `src/shared/commandSpec.ts` 的注释——下一个域进来直接用，不必再问一次。
2. 出口分派按域写死两条入口（`spec.kind === 'write' ? runRecordWrite : runQueryRead`）：改成调
   **声明里本来就有的 `run`**。这一改让「加一个域」不再需要动外壳。

**没有解决的横向项（如实记）**：判据甲（页指纹账本）只覆盖写入域 32 页，账户页不在账本里
（差异表 §五 第 1 行）；扩账本会动到写入域的账本文件与它的测试，属横向决定，留给下一张页面票。

## 二 信息等价对照表（老 4 张模板 × 老侧信息块 → 新落点）

老侧块清单由子代理逐块勘察（模板行号为准），新落点由本票产物逐条核。

| 老模板 | 老侧信息块（处） | 新落点 | 判定 |
|---|---|---|---|
| `账户/account_form.html`（新增账户，采集） | ①场景 chips ②账户名输入（预填）③重名警示 ④类型输入 ⑤已有账户建议行／空态 ⑥确认复制文本 ＋ 复制数据／日志 | 徽章列（①）／字段卡两格（②④）／阻断表点名重名（③）／已有账户只读表 ＋ 空态（⑤）／复制指令块 ＋ 复制区双按钮三格式（⑥） | **对上了**（6/6） |
| `账户/transfer_confirm.html`（账户转账，采集） | ①场景 chips ②转账口径说明条 ③金额输入 ④从账户输入 ⑤从账户建议行 ⑥到账户输入 ⑦到账户建议行 ⑧时间输入 | 徽章列（①）／口径说明行（②）／**操作预览三段**（新增，信息等价加强）／字段卡四格（③④⑥⑧，从／到两格各带下拉候选＝老侧两排建议按钮的同一件事，⑤⑦） | **对上了**（8/8） |
| `账户/confirm.html`（改账户，选择） | ①场景 chips ②原账户三行键值 ③表头（字段／原值／新值）④diff 行 ⑤历史保留 tip ⑥确认复制 ＋ 复制数据／日志 | 徽章列（①）／确认页只读回显（②）／变更预览（③④，改前 → 改后）／口径行「执行后历史流水保留……」（⑤）／复制指令块 ＋ 复制区（⑥）／**回执页结果块＝改前改后对照**（写库那一次的同一件事） | **对上了**（6/6） |
| `账户/account_view.html`（看账户汇总，查看） | ①header（标题／副标题）②离线横幅 ③KPI×5 ④停用账户口径注记 ⑤账户卡区标题＋计数 ⑥账户卡×N（类型 chip ＋ 停用／未登记徽章 ＋ 余额 ＋ 六行读数）⑦流水区标题＋计数 ⑧流水行×N ⑨复制数据／日志 ⑩footer ⑪空态 ⑫错误态 | 页头（①）／**舍**离线横幅（静态页没有缓存成因，差异表 §三 第 16 行）／读数行 5 格（③）／停用注记口径行（④）／各账户余额卡（⑤⑥，含类型、状态徽章、余额、收入／支出／转入／转出／笔数／最近六读数）／最近流水表 12 笔＋截断口径行（⑦⑧）／复制区（⑨）／来源脚注（⑩改人话）／空态 ＋ 引导句（⑪）／失败面走公共层 `renderFeedbackBlock`（⑫，`688` §二 A8／A10） | **对上了**（10 块在位 ＋ 2 处有意改，逐条有据） |

老侧载荷逐键（`cards` 11 键／`totals` 12 键／`flows` 6 键）在新侧 `src/account/accounts.ts` 的
`AccountCard`／`AccountTotals`／`AccountFlow` 三个形状里逐键在位；出口载荷 `data` ＝
`{items, total, totals, flows, flow_count, records}`（`items`／`total` 与搬迁前同名同形，其余为加法）。

## 三 T7 强制基准逐条自证（可判形式照原件的原话）

| T7 条 | 本票读数 | 判定 |
|---|---|---|
| 裁定 1（标识符不上屏） | 6 张产物可见文本（剔掉复制载荷区 `data-t` 后）grep `bill.`／`.py`／`scripts/`／`undefined`／`NaN` **全 0 命中** | **对上了** |
| 裁定 2（结果型三条恒出） | 回执页与汇总页各含**一个** `<nav aria-label="页内导航">` ＋ **一行**来源脚注（`数据来源 · … · 起 → 止 · 共 N 条`）；汇总页另含口径说明行 | **对上了** |
| 裁定 4（缺值一律 `—`，空窗出完整页） | 汇总页空库仍是整页（标题 ＋ 空态句 ＋ 引导句 ＋ 页内导航 ＋ 复制区 ＋ 来源脚注，exit 0）；可见文本无 `undefined`／`NaN` | **对上了** |
| 裁定 6（采集页不画同形空卡） | 采集页没有「金额／账户／时间」那一排空读数卡（本域采集页根本不设读数行）；账户表为空时出空态句 ＋ 引导句 | **对上了** |
| 裁定 7（一个形状一处定义，页面本地不写 CSS／色值／断点） | 三张模板件只调公共层区块；产物里样式段仍只有公共层那一份（`<style>` 计数 1）；页面件内无 `#` 色值、无 `@media` | **对上了** |
| 裁定 9（缺项阻断） | 缺必需槽位／值不对／账户表冲突三类的产物里**都没有可复制的写库指令**（口令只给看不给复制），出现缺项阻断折叠区并逐项点名；补齐后再跑 ⇒ 复制指令块出现、写库成功 | **对上了** |
| 裁定 11（老侧与用户原则相撞时按原则） | 本域无触发实例（账户域没有页脚按钮那一类）；**未追写 §二**（本条要求一经触发必须追写，本票未触发） | 不适用 |
| 裁定 12（三条已确权原则） | `--r-xl`／`--pink`／`[data-theme`／`prefers-color-scheme: dark` 在 6 张产物里 **全 0 命中**；视觉不做脚本化判定（好不好看由用户看）；墙生成器的两条坑本票不涉（本域不做墙） | 前两条**对上了**；第三条不适用 |
| §五 5.2 骨架表（顺序一律照它） | 采集页 ①：徽章列 →（结论条 ○）→ 缺项标签／阻断（●）→ 口径行（○）→ 操作预览（○）→ 已有账户表／空态（●）→ 字段卡（●）→ 复制指令块（●）→ 复制区（●）→ 来源脚注（●）；回执页 ④：徽章列 → 页内导航（●）→ 读数行（●）→ 结果块（○）→ 口径行（○）→ 明细表（●）→ 对账（●）→ 复制区（●）→ 来源脚注（●）；汇总页 ⑥：徽章列 → 结论句（●）→ 页内导航（●）→ 读数行（●）→ 账户卡（●）→ 占比条（○）→ 停用注记（○）→ 流水表（●）→ 截断口径（○）→ 口径行（●）→ 对账（●）→ 复制区（●）→ 来源脚注（●） | **对上了**；⑥ 两行（第 14 行图表／第 15 行进度条）本域无对应事实，差异表 §一 第 4 行写明 |
| §五 5.1 页面类 | account 有 ①②⑥ 三类，本票各落一片：① 账户表单（新增账户／账户转账）·② 改账户确认 ·⑥ 账户汇总 | **对上了** |

## 四 机器读数（门禁与运行标识）

| 门 | 命令 | 读数 | runId |
|---|---|---|---|
| 编译 | `tsc -b packages/skill-bill` | exit 0 | `bad7859f`／`26e92921`…`bdd6da9a`（收敛过程） |
| 生成器 | `gen-cli.mjs --check` | `GEN-CHECK PASS：命令 8 条；能力 3 个（account／query／write）` | `e5954c7e` |
| 棘轮 | `check-ratchet-tight.mjs` | `RESULT: 9/9`／`PASS: 棘轮与实况逐项恒等` | `d021205e` |
| 场景形状门 | `check-scene-shape.mjs` | `RESULT: 19/19`／`PASS: 场景件只声明差异值…` | `b5490159` |
| 行数告警线门 | `check-warning-line.mjs` | `RESULT: 14/14`（台账 `--sync` 后） | `b3dd2f7a`（sync）／`d06eeebb` 段的全量测试 |
| 全量测试 | `node --test packages/skill-bill/test/*.test.mjs` | `tests 337 / pass 337 / fail 0` | `d06eeebb` |
| 本域靶向 | `node --test …/t691-账户域.test.mjs` | `tests 10 / pass 10 / fail 0` | `30fa744a` |
| 真出口探针 | `node docs/skills/skill-bill/t691-探针-真出口.mjs` | `RESULT: 13/13` | `32945a92` |

同窗改掉的冻结值（棘轮「搬一条、下调一格」）：

| 冻结值 | 改前 | 改后 | 依据 |
|---|---|---|---|
| `legacyKeys`／`dispatchKeys` | 10 条（含两条账户键） | 8 条 | 两条账户命令搬进能力目录 |
| `registryKeyCount` | 6 | 8 | 同上 |
| `lineCaps['src/cli/cmd_read.ts']` | 501 | 455 | 删掉两个 `case`（34 行）＋ 改 `runRegistered`（净 −46） |
| `lineCaps['src/render/envelope.ts']` | 64 | 62 | 过渡表删两行 |
| `test/t721-域声明` 与 `test/t686-收紧守卫` 的硬编码 | 6 | 8 | 同上（两处测试的期望值） |
| `AGENTS.md` 台账「当场实测」列 | 501 | 455 | `--sync` 同步 |

## 五 未做项与下一手缺什么

1. **判据甲（页指纹账本）不覆盖账户页**：账本 `test/t689-页面指纹.json` 是写入域 32 页的账，
   本票的 6 张产物不在其中。本票用「真出口探针 ＋ 靶向测试」顶上；**扩账本属横向决定**
   （会同时改写入域的账本、它的生成器夹具与测试），留给下一张页面票或机器面票。
2. **老侧遗留的页型差异**：`688` §一 说账户域有 4 张模板，本票落成 3 片页型 ＋ 6 张产物
   （3 个采集／确认页 ＋ 3 个回执页）——「一张老页 ≠ 一张新页」照裁定 8 办，逐条记在差异表 §一。
3. **`link`／`setup`／`goal`／`analysis` 四域**仍走过渡表（8 条键）；本票把「加一个域」的两处横向卡口
   （入口泛化、`receipt` 那格的形状）解掉之后，下一域应当只剩域内工作。
