# t407 链路总览对账证据

件名：`D:\ilife\docs\skills\skill-bill\t407-链路总览.html`（本页逐条核它印出来的每一行、每一个链接）。
口径：唤醒词／命令／`kind`／`op` 逐字抄 `packages\skill-bill\src\policy\wakewords.ts` 的 `WAKE_TABLE` 与
`docs\skills\skill-bill\t407-场景落点清单.md`；文件名与字节数按盘上实物读，不猜。**只读源码，未改 `packages/` 下任何文件。**

## 一、逐条对账（16 行）

| 唤醒词 | 命令 | `kind`／`op` | 采集页文件名 | 回执页文件名 | 两文件存在性与字节数（实测） | href 实测可开 |
|---|---|---|---|---|---|---|
| 记支出 | `bill.record.add` | `kind=expense` | `t407-代表-记支出-采集页.html` | `t407-代表-记支出-回执页.html` | True 68323 ／ True 67289 | href 在页内，脚本逐条 Test-Path 均 True |
| 记收入 | `bill.record.add` | `kind=income` | `t407-页-记收入-采集页.html` | `t407-页-记收入-回执页.html` | True 71794 ／ True 69573 | 同上，均 True |
| 拍账单 | `bill.record.add` | `kind=photo` | `t407-页-拍账单-采集页.html` | `t407-页-拍账单-回执页.html` | True 75049 ／ True 69068 | href 在页内，脚本逐条 Test-Path 均 True |
| 批量录入 | `bill.record.add` | `kind=batch` | `t407-页-批量录入-采集页.html` | `t407-页-批量录入-回执页.html` | True 70858 ／ True 68892 | href 在页内，脚本逐条 Test-Path 均 True |
| 记退款 | `bill.record.add` | `kind=refund` | `t407-页-记退款-采集页.html` | `t407-页-记退款-回执页.html` | True 74787 ／ True 69390 | href 在页内，脚本逐条 Test-Path 均 True |
| 记报销 | `bill.record.add` | `kind=reimburse` | `t407-页-记报销-采集页.html` | `t407-页-记报销-回执页.html` | True 73805 ／ True 70745 | href 在页内，脚本逐条 Test-Path 均 True |
| 报销到账 | `bill.record.add` | `kind=reimburse-done` | `t407-页-报销到账-采集页.html` | `t407-页-报销到账-回执页.html` | True 74489 ／ True 69467 | href 在页内，脚本逐条 Test-Path 均 True |
| 记借出 | `bill.record.add` | `kind=lend` | `t407-页-记借出-采集页.html` | `t407-页-记借出-回执页.html` | True 73420 ／ True 69368 | href 在页内，脚本逐条 Test-Path 均 True |
| 记借入 | `bill.record.add` | `kind=borrow` | `t407-页-记借入-采集页.html` | `t407-页-记借入-回执页.html` | True 73424 ／ True 69379 | href 在页内，脚本逐条 Test-Path 均 True |
| 记收回 | `bill.record.add` | `kind=collect` | `t407-页-记收回-采集页.html` | `t407-页-记收回-回执页.html` | True 74204 ／ True 69416 | href 在页内，脚本逐条 Test-Path 均 True |
| 记偿还 | `bill.record.add` | `kind=repay` | `t407-页-记偿还-采集页.html` | `t407-页-记偿还-回执页.html` | True 74218 ／ True 69406 | href 在页内，脚本逐条 Test-Path 均 True |
| 记分期 | `bill.record.add` | `kind=installment` | `t407-页-记分期-采集页.html` | `t407-页-记分期-回执页.html` | True 71786 ／ True 69506 | 同上，均 True |
| 记一笔 | `bill.record.add` | 不给 `kind`（通用词） | `t407-页-记一笔-采集页.html` | `t407-页-记一笔-回执页.html` | True 71925 ／ True 69757 | href 在页内，脚本逐条 Test-Path 均 True |
| 改记录 | `bill.record.update` | 不给 `op`（缺省＝改字段） | `t407-页-改记录-采集页.html` | `t407-页-改记录-回执页.html` | True 69918 ／ True 67460 | href 在页内，脚本逐条 Test-Path 均 True |
| 撤销 | `bill.record.update` | `op=undo` | `t407-页-撤销-采集页.html` | `t407-页-撤销-回执页.html` | True 70096 ／ True 68444 | href 在页内，脚本逐条 Test-Path 均 True |
| 恢复 | `bill.record.update` | `op=restore` | `t407-页-恢复-采集页.html` | `t407-页-恢复-回执页.html` | True 67860 ／ True 68301 | href 在页内，脚本逐条 Test-Path 均 True |

16 行齐；32 个文件名全为盘上实数（`all_exist=True`），字节数即 `Get-Item <绝对路径>).Length` 的读数。

## 二、怎么核的（命令与读数）

| 判据 | 命令（PowerShell，摘） | 读数 |
|---|---|---|
| 32 个 href 逐个实测存在 | `$hrefs=[regex]::Matches($t,'href="([^"]+)"')…; Test-Path -LiteralPath (Join-Path $dir $h)` | 总 32 ／ 唯一 32 ／ 缺失 0 ／ 全部存在 True |
| `href` 数量＝32 | `([regex]::Matches($t,'href="')).Count` | 32 |
| 表体 16 行 × 6 列 | 拆 `<tbody>` 再拆 `<tr>`／`<td` | `tbody_rows=16`、`cells_total=96`、`rows_not_6_cells=0` |
| ② 唤醒词列逐字对齐 `WAKE_TABLE` | 取每行 `class="wake"` 文本与 16 条期望表比 | `wake_col_equals_expected=True` |
| 含 `<!DOCTYPE html>`／内联 `<style>` | `$t.Contains('<!DOCTYPE html>')`／`<style>` | `True` ／ `True` |
| 外部请求 0 | `(src\|link\|@import)\s*[=:]\s*["']?https?` 计数 | 0（页里没有任何外链、没有外站字体与图片） |
| 无字面 `\n` | `([regex]::Matches($t,'\\n')).Count` | 0（真实换行，无转义字符残留） |
| 33 个文件的存在性读数（另取一次） | `Test-Path` ＋ `(Get-Item …).Length` 逐对走 16 轮 | `checked_pairs=16`、`all_exist=True`（`False` 命中 0） |
| 量级读数 | 行数／字节数 | HTML 281 行、23008 字节；表体 16 行 × 6 列＝96 格；`href` 32（16 行 × 2 列） |
| 本票没改源码 | `git status --porcelain`（改动只应落在本页与证据件） | `packages/skill-bill/` 下 0 条改动；本票新增只有这两件（其余 M／?? 行是入场前就在工作区里的邻件状态，不是本票动的） |
| href 可否真开 | 页与产物同目录，`href` 一律相对文件名；逐个 `Test-Path` 落同一目录的实数文件（上表读数），路径拼接即真路径 | 32／32 命中 |
| 采集页「不写库」／回执页「写库」是否页页印着 | `Get-ChildItem 't407-*采集页.html'` 逐件 `-match '不写库'`；`t407-*回执页.html`（排掉 `对照`／`老样`）逐件 `-match '写库成功\|写库回执'` | 采集 16／16 印「不写库」；回执 16／16 印「写库成功／写库回执」；两列 bad 均为空 |
| 采集页「缺哪些槽」抽读三例 | 逐件取页内 `缺必需槽位：[^<]*` 那一句 | 记退款：`缺必需槽位：category、amount…本型另需：原支出编号`；撤销：`缺必需槽位：id…`；记支出：`缺必需槽位：category…` |

## 三、没能确认的事

1. **没在浏览器里真点开这 32 个链接**：只做了「`href` 值 ＋ 同目录 `Test-Path` 命中」的核对（等价于双击打开的前提），没有用浏览器逐张打开比对内容。渲染截图看过一次整页版式，未逐图目视核对页内文案。
2. **没逐页抽满 16 张采集页的缺槽句**：只抽了三例（记退款／撤销／记支出）与必需格定义（`src\record\slots.ts`：`category`／`amount` 必给；`update` 那支 `id` 必给）核对，其余 13 张未逐页抽读。
3. **没有真跑命令做端到端复核**：本票只读源码，未执行 `bill-cmd-read`（那会写库、落产物，超出本票范围）。「采集页不写库、只有回执那一步写库」这一条取自 `src\record\write.ts` 的装配口径（阻断即不进写库那步）＋上面那张 32 件页内文案读数，不是本票真跑出来的读数。
4. **「报销到账／记收回／记偿还」的候选单选取数**：`t407-场景落点清单.md` 的遗留一节写「撤销／恢复列候选记录的查询命令本仓还没有」，`candidatePick` 冻好了但取数要等 #403 查询域；据此我在第 ⑥ 列写的「挑一条候选」是页面设计口径，未确认候选列表当前真能取到数据。
5. **记分期出页与唤醒词的关系**：`记分期` 那条词的 `types` 在 `wake-assets.ts` 里记的是「向导」（其余写场景记「采集」），本票按落点清单「采集页＋回执页同一对」写第 ④⑤ 列；「向导」与「采集页」的措辞差别未再追。
6. **`wake-assets.ts` 的行数**：任务书写该件约 4747 行，实测本工作区该件为 986 行 × 38556 字节（`Get-Content … \| Measure-Object -Line` ＝ 962，`read` 工具报 986 行）；两者不一致，未确认是版本差异还是口径差异。
