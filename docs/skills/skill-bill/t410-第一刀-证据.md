# t410 第一刀证据（确证两节＋已裁定两节）

> 作者：410 第一刀（唯一作者）。范围：只做接手须知第 4 条待办的确证项与已裁定项——
> 一黑底说明块（确证）、二拍账单参数名列（确证）、三 scene-lend 对称句（改）、
> 四内部话判据口径（改）。不动：三采集大跌页架构（scene-photo／installment／plain）、
> 符号串形状化、版心 1120（`DESKTOP_CSS`）。
> 声明路径：`packages/skill-bill/src/record/scene-lend.ts`、
> `packages/skill-bill/src/record/scene-borrow.ts`、
> `docs/skills/skill-bill/t407-v8-style-audit.mjs`、
> `packages/skill-bill/test/t410-lend-borrow-caliber.test.mjs`、
> `docs/skills/skill-bill/t410-第一刀-证据.md`（本件）、`.scratch/t410/`（工作日志，不入仓）。

## §0 改前基线（2026-09-16 UTC）

- tsc：`node node_modules/typescript/bin/tsc -b packages/skill-bill` EXIT 0。
- 测试：`node --test packages/skill-bill/test/*.test.mjs` → pass 274／fail 0。
- 机审：`node docs/skills/skill-bill/t407-v8-style-audit.mjs --dir docs/skills/skill-bill` → 全绿 32/32，EXIT 0。

GATE-RUN runId=80fe1246-08a5-4114-9b07-57c4dfb14c3d cmd="node node_modules/typescript/bin/tsc -b packages/skill-bill"
GATE-RUN runId=694c823e-b1c1-475a-9f98-65a6e29ef426 cmd="node --test packages/skill-bill/test/*.test.mjs"
GATE-RUN runId=efecf8aa-c0be-4eb3-8cee-016073b06be3 cmd="node docs/skills/skill-bill/t407-v8-style-audit.mjs --dir docs/skills/skill-bill"

## §1 节一确证：黑底说明块两栏等高＋行距 1.5（已落地，无需改码）

- 源码落点 `packages/skill-bill/src/shared/docPage.ts:64-71`（`TOAST_CSS`，第三轮返工已入仓）：
  `.ilife-toast{line-height:1.5}`、标题行 `nowrap＋stretch＋margin-bottom:6px`、
  标题 `flex:1 1 auto＋line-height:1.5`、胶囊 `inline-flex居中`、说明行 `line-height:1.6`。
- 产物：`.scratch/t410-wall/` 32/32 页含 `t407-r3 B` 标记；
  `docs/skills/skill-bill/t407-页-记借出-回执页.html:1790` 同标记（出自同一 `sharedCssText` 路径）。
- 旁证：桌面基线回执页共性“层级留白稳定 18”；手机基线黑块记为“紧凑／争抢”（权重问题），
  两份基线均未报两栏不等高。结论：确证通过，本节零改码。

## §2 节二确证：拍账单「参数名」整列已删（已落地，无需改码）

- 源码落点 `packages/skill-bill/src/shared/outsideScan.ts:73-92`（`escapeCard`，第三轮返工已入仓）：
  表列只剩 `要素／现在／这一步谁办` 三列；`参数名` 字面在源码中仅 1 处，
  落在第 70 行注释（记述删列原由），非渲染输出。
- 产物：`docs/skills/skill-bill/t407-页-拍账单-采集页.html` 与
  `.scratch/t410-wall/t410-页-拍账单-采集页.html` 中 `参数名` 均为 0 命中；
  后者 `口令原文` 块仍在（pre 载荷未动，符合“该页 pre 口令原文块不动”）。
- 结论：全批区外库列名 32/32 满足，确证通过，本节零改码。

## §3 节三改码：scene-lend／borrow 口径句对称去内部话（待裁定 4）

- 改动（各 1 行，其余不动）：
  `scene-lend.ts:176` 与 `scene-borrow.ts:197` 的 `借贷走标签流转：` → `标签流转：`；
  借出／借入 specifics（`#借给（对象）／还回来` ↔ `#向（对象）借／还回去`）原样保留，
  两句仍为镜像对称（与 toast `标签流转：` 同口径）。
- 护栏：新建 `packages/skill-bill/test/t410-lend-borrow-caliber.test.mjs`（2 条，只增不改旧）。
- 变异自证（lend 单字回退）：重编后护栏 pass 1／fail 1（红）；还原后重编全绿。
  全量：改前 274/0 → 改后 280/0（＋2 本票；另＋4 系并发席未入仓件
  `t418-query-visual.test.mjs` 在基线后落盘，非本票引入，本票未碰它）。
- 墙重出：`run32.mjs` RESULT 32/32；新句仅两采集页同行 1835（n15／n17），全墙旧串 0。
- 重截图自看（`.scratch/t410/shots/`，基线 PNG 未动）：桌面 2/2、手机 2/2（cw=sw=390，
  页高 3152／2866 与手机基线一致，无位移）；亲眼：新句上屏、对称、无溢出，黑底块与版心无变化。

GATE-RUN runId=5ea1c6fa-dfab-4424-a47d-42f16b746353 cmd="node node_modules/typescript/bin/tsc -b packages/skill-bill"
GATE-RUN runId=b1de9bbc-66a3-4ee4-b8be-9088ff173a53 cmd="node --test packages/skill-bill/test/t410-lend-borrow-caliber.test.mjs"
GATE-RUN runId=6a992024-67cd-4871-81e2-289d37e251c1 cmd="node node_modules/typescript/bin/tsc -b packages/skill-bill"
GATE-RUN runId=b8a45aeb-98f6-4fe3-839b-9b775575c604 cmd="node --test packages/skill-bill/test/t410-lend-borrow-caliber.test.mjs"
GATE-RUN runId=eca9ee40-2bbb-4822-bfbb-85f284a8be6c cmd="node node_modules/typescript/bin/tsc -b packages/skill-bill"
GATE-RUN runId=93db6b79-24f8-4463-b517-4ef2e6e154b0 cmd="node --test packages/skill-bill/test/*.test.mjs"
GATE-RUN runId=d8314d36-a073-4be4-bfcd-a1b27267b941 cmd="node .scratch/t410-wall/run32.mjs"
GATE-RUN runId=1b3b043b-843a-404c-81a0-686281f1390c cmd="node .scratch/t410/shot4.cjs"
GATE-RUN runId=9c45ab0c-04bb-4fb8-952a-de0aa65749a5 cmd="python .scratch/t410/shot4-mobile.py"
