# t407 整改（一）——用户语言映射＋徽章形状·证据

> 收口窗唯一写者：只改 `packages/skill-bill/src/shared/**`、`src/record/**`、`test/**`＋32 产物＋本件。
> 红线：禁 amend/rebase/reset/push/checkout/restore；只 `git add <显式路径>`＋`git commit -- <显式路径>`。
> 产物一律 CLI 真跑后整份拷入，不手打整页 HTML。

## 一、体检（先体检再动手）

- `npx tsc -b packages/skill-bill --force` → EXIT 0（空输出）。
- `node --test "packages/skill-bill/test/*.test.mjs"` → 125 例 125 过 0 红（改前残留态）。
- `node docs/skills/skill-bill/t407-v8-style-audit.mjs --dir docs/skills/skill-bill` → EXIT 0，全绿：
  设计位 `·`／`|` 0 处；标题写法位 55 处（眉头／页标题，允许保留）；英文裸词 0 份；重复句 0 份；32／32 齐。
  表头行（本页断点／≥44px 给的是本页读数，公共层读数见共享层）：32 行懒政 `·` 全 0、英文全 0。
  共享层：head 样式 40101 字符；820／640 断点有；44px 有；flex-wrap／auto-fit／窄屏内距有；色值 27；32／32 同一份；本页 body 后样式块 0、内联 0。
- 归因：三命令在残留态即绿，红→绿不在本窗发生（前窗已把版式位与裸词清零，本窗只做口径精确化＋32 重出）。
  别包／别窗在途改动（`skill-calorie` 约 100＋件、`base-render` 等）一律不碰；`git status` 另见 100＋件在途，本窗只动允许路径。

## 二、P0 用户语言映射（单一定义地）

落点：`packages/skill-bill/src/shared/userWording.ts`（残留为准收完）。

- 对外名字 5（≤5）：`wakeWordOf`／`statusNoteOf`／`fieldLabelOf`／`nextStepOf`／`badgeTextOf`。
  件头已更正为“对外五件”，第二个用法保持两处指名：① `src/record/` 16 件场景件；② 同层共用件（`typeBadge`／`summaryRow`／`blockedSlots`）。
- 覆盖类数 6：
  ① 命令名：`bill.*` 不进用户可见处（徽章／正文不印，只活在复制载荷 `commandLine` 与日志 `command` 可重跑位；载荷头行 `【bill · record.add】` 按裁定 2 改 `title:'记账数据'`，见 `copyArea.ts formatsOf`）。
  ② 库列名 15（`fieldLabelOf`）：`id→记录编号／category→分类／amount→金额／time→时间／account→账户／ledger→账本／currency→币种／note→备注／source_id→原记录编号／who→往来对象／total→总额／periods→期数／start_date→首期日／deleted_at→撤销标记／op→这一步做什么`。字段表／缺口清单／回执明细一律走它（`slots.ts slot()`＋各场景 `fieldLabelOf`＋`write.ts rowDetail`）。
  ③ 内建型名 13（`wakeWordOf`，含新增 `plain→记一笔`）：`expense→记支出／income→记收入／photo→拍账单／batch→批量录入／refund→记退款／reimburse→记报销／reimburse-done→报销到账／lend→记借出／borrow→记借入／collect→记收回／repay→记偿还／installment→记分期／plain→记一笔`；认不得与空串落 `记一笔`。
  ④ `deleted_at＝now` →「已撤销，记录还在，随时可恢复」；置 NULL →「已恢复」（`STATUS_NOTES` 两键已按上级口径逐字改；`write.ts` 撤销／恢复摘要与明细改走 `statusNoteOf`，摘要保留记录编号：`已撤销，记录还在，随时可恢复（记录编号 N）`／`已恢复（记录编号 N）`）。
  ⑤ L1／L2／L3 → 人话：槽位提示与摘要行不再出现层级名（`slots.ts` 改“三级分类…”；`summaryRow.ts` 改“分类要选到最细那一级”）；`scene-plain.ts` 残留 `（L3 即名目…）` 改“（最细一级即名目…）”。
  ⑥「本仓尚未定额的型」→「分类按三级挂靠」（人话，不隐藏；`typeBadge.ts caliberOf` 同句）；「三形态之一：缺记录编号 · 先挑一条」→「先挑一条记录」。
- 映射只定义一次：`FIELD_LABELS`／`WAKE_WORDS`／`STATUS_NOTES` 只在 `userWording.ts`；`slots.ts`／`blockedSlots.ts`／`typeBadge.ts`／`write.ts`／各场景只引用。

## 三、P1 徽章与状态行结构化槽位

- 原句 `记支出 · 支出（金额取负数）· bill.record.add · 待补槽位 · 未写库（已阻断）` 已拆四枚独立形状（`typeBadge.ts`，残留态，本窗未改形状只改文案来源）：
  ① 唤醒词＋口径胶囊（`renderChips`，`wakeWordOf(kind)＋全角空格＋caliberOf(kind)`）；② 状态徽章（`renderStatusBadge`，`statusNoteOf(state)`，空串则无文本）；③ 下一步动作（`renderCaliberLine(nextStepOf(...))`）；命令身份不上屏（裁定 1）。
  方向口径取值走 `summaryRow.ts directionOf`（唯一定义地），本件不另写。
- 同理：提示行副行（`duplicateNote` 一句话说全）／阻断条标题（`blockedBar`：`errorReceipt 还缺什么`＋缺项表＋只读口令＋置灰按钮＋口径行）／卡片副行（`summaryCards` 五格＋`renderCaliberLine` 口径）一律形状化，无 `·` 串拼接。
- 页标题与眉头各留一个分隔符（标题写法）：眉头 `记账 · 写入域`（`pageShell.ts` 32 份同一句）；页标题 `唤醒词 · 补齐槽位／回执`（`collectBody.ts`／`receiptBody.ts`／各场景 `title: WAKE＋' · '＋…`）；机审 `TITLE_POS` 另计 55 处，不进懒政。
- 全部走 base 现成组件：`renderChips`／`renderStatusBadge`／`renderCaliberLine`／`renderKpiGrid`／`renderDataTable`／`renderPreBlock`／`renderCopyBlock`／`renderActionBar`，未新增 base 组件。

## 四、必办小事与已裁定

- （6）字段表库列名走映射：`write.ts rowDetail`／`recordPicker.ts snapshotTable`／`scene-undo/restore` 明细／`receiptBody.ts` 写进去的项／各场景 `writtenFields.map(fieldLabelOf)` 全走 `fieldLabelOf`；`slots.ts` 中文名由 `fieldLabelOf` 派生（`slot()`），两处不各写一份。
- （7）`退出囗` 疑似错字：核 `src/shared/**`＋`src/record/**`（`退出口／退出码` 全为 `口`，`囗` 0 命中）与 32 产物（`退出囗` 0 命中），源码无错字，无需改。
- 已裁定 1 徽章命令名去掉：`typeBadge` 不再拼 `input.key`，测试钉 `!includes('bill.record.add'/'update')`。
- 已裁定 2 复制载荷 `【bill · record.add】` 算上屏一并改：`copyArea.ts formatsOf` 给 `text` 显式 `title:'记账数据'`，头行不再拼命令名；`json/csv` 不走头行。32 产物复量 `【bill` 0 处、`记账数据` 32 处（utf8 口径）。
- 已裁定 3 金额 `0.00`＋「零（本仓不记零）」→ 值 `未给`、说明 `还没给`：`summaryRow.ts money2(0)→'未给'`（`moneyDirection(0)` 本已 `还没给`）；`slots.ts missingSlots` 金额 `0／"0"` 视同缺项；`record-write.test.mjs` 旧断言 `金额 0 算给了` 已按裁定改。
- 已裁定 4 代表页页头与徽章不一致是笔误，统一：`receiptBody.ts PAGE_WAKE_WORDS`＋`collectBody.ts wakeWordOf` 同源（型认得出按型，否则按操作），`add 恒写记一笔` 已修。
- 已裁定 5 「退款」进收入名目不改：`summaryRow.ts DIRECTION` 仍仅 `expense/income` 两条，`refund` 不判方向（`caliberOf` 落“分类按三级挂靠”），业务口径未动。

## 五、判据自跑读数

1. 32 产物重出（文件名不变）：`$env:TEMP/t407-gen32.mjs` 按既有口径真跑（`node <bin> <key> --params <json> --html docs/...`，`SKILLS_DB_PATH` 新建临时库，采集／回执口径与既有产物逐份同参：采集缺必需槽位、回执全参；`改记录/撤销/恢复` 用新建库 id 2／3 链）→ `ALL32_OK`；`git status` 32 份 `M`，文件名与机审名单一致，缺 0 份。CLI 文本直接落盘，未手打整页 HTML。
2. 机审由红转绿：改前残留态已绿（见第一节），本窗重出后仍绿：`EXIT 0，全绿；设计位 0 处；标题写法 55 处（眉头 32＋页标题 23，允许保留）；英文裸词 0 份；重复句 0 份；32／32 齐`。非版式位旁证 `同分类近邻 · 金额与本笔不符` 4 处（表格单元格数据，前窗 6→本窗 4，新库重复更少，不进懒政）。
3. `node --test` 只涨不跌：改前 125／125 过；改后 127／127 过（`user-wording.test.mjs` 新增金额 0 与载荷头行两条；`record-write.test.mjs` 两条按裁定改期望，未删用例）。
4. 真 390 视口复量仍不溢出：本窗未动任何版式代码（无 CSS／无新增固定宽／无结构改，只改文案与 `title`）；机审共享层 32／32 同一份（820／640／44px／flex-wrap／auto-fit／窄屏内距全有；本页 body 后样式块 0、内联 0）；墙约束（390 单列、卡片自带横滚、`minmax(0,1fr)`／`overflow-x:auto`）未动；静态 `utf8` 复查新产物 `【bill` 0、`记账数据` 32、`0.00` 0、`退出囗` 0。量法按假缺陷警告走 390 宽 iframe（不用 `--window-size=390`）。
5. 变异自证两条（护栏 `test/user-wording.test.mjs` 头注释即设计）：
   - A 改坏映射：`userWording.ts category:'分类'→'category'` → `tsc 0`＋`user-wording.test.mjs TEST:1`（`actual:'category' expected:'分类'`）必红；还原→ `TEST:0` 绿。
   - B 改坏徽章：`typeBadge.ts` 芯片退回 `' · '＋' · bill.record.add'` 串 → `TEST:1`（形状无 `·`／命令名不上屏两条挂）必红；还原→绿。
   还原后 `--force` 重编（防增量假绿）→ `tsc 0`＋全包 `127／127`＋机审 `EXIT 0`。
   三条还原自证：① `sha256(userWording.ts)=2f8141…3a741cb` 变异前后一致；② `sha256(typeBadge.ts)=54f4209a…e29cc9142` 一致；③ `typeBadge.ts` 现文件 `' · '` 代码拼接 0 处（残留 `·` 仅注释引改前旧句与映射键内部串），`git status` 两件仍为预期态（`M typeBadge`／`?? userWording`），无变异残留文件。

## 六、交付与提交

- 源码改动（LF 口径，只数 `\n`；包内告警线 350，`test/*.mjs` 不计）：
  新增 2：`src/shared/userWording.ts` 136、`test/user-wording.test.mjs` 133（本窗按裁定收完口径精确化＋2 新用例）。
  本窗改 7：`src/shared/typeBadge.ts` 68（头注释三→四件 1 行）、`src/shared/copyArea.ts` 156（载荷头行 `title`）、`src/shared/summaryRow.ts` 95（`money2(0)`＋注释）、`src/record/slots.ts` 79（金额 0 视同缺项）、`src/record/write.ts` 225（撤销恢复口径＋走映射，最大件）、`src/record/scene-plain.ts` 212（L3→人话 1 处）、`test/record-write.test.mjs` 366（2 处按裁定改期望，不计超线）。
  前窗残留改（本窗未碰，随同提交）：`src/record/` 13 件（`collectBody` 173／`receiptBody` 117／`scene-batch/borrow/collect/income/installment/lend/photo/refund/reimburse-done/reimburse/repay/restore/undo/update`）、`src/shared/` 12 件（`blockedSlots` 136 等）、`test/` 2 件（`cli/frozen-blocks/shared-judgments`）。
  超线件 0（src 最大 `write.ts` 225＜350）。
- 32 产物：文件名不变（机审名单 2 代表＋15×2），CLI 真跑整份落盘（`t407-gen32.mjs ALL32_OK`），`M` 32／32。
- 本件：本件（`t407-整改-语言与形状-证据.md`，六节＋七＋交棒）。
- 提交：`fix(407): 写入域页面根因整改（一）——用户语言映射单一定义地＋徽章与状态行结构化槽位`，`git add`＋`git commit --` 一律显式路径，不 `push`。
- 对账偏差 0：机审 32／32 齐且全绿；测试 127／127；HELP 资产（`#146` 6 条）全过；退款业务口径未动；`退出囗` 0 命中无需改。

## 七、待裁定

映射不出的不硬编，列此处（均保留现状，机审已剔除或允许清单覆盖）：

1. `op` 槽位值 `undo／restore`（`slots.ts` 提示 `不填＝改字段；undo＝撤销／restore＝恢复`）：用户真要传的字面值，藏了就跑不起来；是否允许命令参数值保留英文，请裁定。
2. 可重跑命令原文 `bill-cmd-read bill.record.* --params '{JSON}'`（阻断条只读块／复制 prompt／日志第 4 段；JSON 键 `category/amount/time/...`）：过程证据＋重跑口令，改了即不可跑；裁定 2 只改头行 `title`，此处未动。
3. 日志 `scene` 段 `bill.record.add（receipt）` 与来源段库文件名：envelope 派生的过程证据（`buildLogText` 恒派生，无 `title` 覆盖位）；是否算上屏，请裁定。
4. 数据格式名 `text／json／csv（JSON／CSV）`（复制菜单提示）：格式本来的样子，非内部标识。
5. 表格单元格与选择器回显内的数据标点（分类 `/`、金额 `+`、候选 `同分类近邻 · …` 在单元格）：数据与回显，非版式（机审 `NON_DESIGN_POS` 另列旁证，不进懒政）。

## 交棒

本窗收完，无需交棒。后窗若动文案：只改 `userWording.ts` 一处＋跑 `t407-gen32.mjs` 重出 32＋机审＋`--force` 重编；勿碰 `skill-calorie/base-render` 在途件。
