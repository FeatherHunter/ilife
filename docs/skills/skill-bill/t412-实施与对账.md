# t412 时间页01 · 实施与对账（结构纪律五步 ＋ 真跑读数 ＋ 变异自证）

> 票 #412「饼干记账查询时间页01：5 词 record.today（今天／昨天／某天／最近／查账单）」。本件是**实施证据**：
> 事前影响清单与事后对账逐行对得上（structure.md 第五步是唯一机械验收点），读数全部是当刻盘上跑出来的。
> 认领：票已有人认领（assignee FeatherHunter），按实施要求跳过 `gh issue edit --add-assignee`。
> 研究输入：`.scratch/t412/research-412.md`（只读结论，GAP-T1 标题缺口＋GAP-E1/E2 空态缺口）。
> Q1/Q2 已按推荐落定：يوم行即 KPI 行（`src/query/list.ts` 四格，`calcKpi` 口径不动）；查账单别名同页（无参标题查今天，不引入口标记）。

## 一 必报五步

### 第一步 · 影响清单（动手前，逐行一个能力目录）

**新增／改动（本票声明路径，仅四类）**

| 件 | 一句话 | 为什么 |
|---|---|---|
| `packages/skill-bill/src/query/read.ts` | today 分支标题判四态＋空态拆四句（新增 `todayWakeWord`／`todayEmpty` 两个小函数，`viewRecordToday` 日期支改调两处） | 票内核：标题按参判＋空态四句＋回归锁定；必需小函数仅此两处 |
| `packages/skill-bill/test/t412-record-today.test.mjs` | 本票判据（13 条：标题 5／空态 4／回归 4） | 一票一判据，判据先行；计数一律整段开标签 |
| `docs/skills/skill-bill/t412-样张-查今天.html` | 样张：无参查今天（有数） | 5 词真跑的落盘证据之一 |
| `docs/skills/skill-bill/t412-样张-查昨天空.html` | 样张：昨天空态（空态一条） | 内核点名的昨天无账空文案落盘 |
| `docs/skills/skill-bill/t412-实施与对账.md` | 本件（实施证据，含五步＋真跑＋变异＋GATE-RUN） | 证据落归属件目录 |
| `.scratch/t412/*` | 窗口四件（`window-1/-2/-3/-c1/-c2/-c3.ps1` 照抄 t411 模板）＋`mutate.mjs`（照抄 t411 模板改锚点）＋日志 | 整窗交锁＋变异自证，永不入库（`.gitignore:5:.scratch/`） |

**不动（逐条有去处）**

| 件 | 为什么不动 |
|---|---|
| `packages/skill-bill/src/query/commands.ts` | 声明是契约（代表词查今天仍是真唤醒词，t411 锁不断）；标题四态在处理体分支，与声明形状无关 |
| `packages/skill-bill/src/shared/*` | 装配（KPI 行＋数据表＋空态＋复制区）沿用 `list.ts`，本票只换入参 |
| `packages/skill-bill/src/policy/wakewords.ts`（路由） | 最长匹配＋`查某天 needs:[date]` 沿用，只做回归锁（详情不被吞／缺 date 抛） |
| `range／search／detail` 处理体 | 票面「不做」原话；detail 只做互不吞词回归 |

### 第二步 · 结构设计（目录树／每件职责／导出面／共用件）

```
packages/skill-bill/src/query/read.ts（改后 253 LF，在线内）
  todayWakeWord(params): string                 新增，对外不出目录（域内自用）
    yesterday→查昨天／显式 date→查某天／无参→查今天（含查账单别名）
  todayEmpty(params): {emptyText,emptyHint}     新增，对外不出目录（域内自用）
    无参→今天还没有记录／yesterday→昨天还没有记录／显式→这一天没有记录／recent 支沿用库里还没有记录
  viewRecordToday(params, db): ViewOut          改：日期支改调上面两处；recent 支一字不动
```

- 每个文件对外给的东西 ≤5（铁律五）：本票对外仍是 `viewRecordToday` 等四处理体，新增两函数不出 `src/query/`（域内互用，不进 `index.ts` 门）。
- 共用件被哪两个能力用：本票**未新增共用件引用**（`listOut`／`listToday`／`resolveQueryDate`／`yesterdayStr` 沿用 #411 落点）。
- 依赖方向：`query → policy／fetch` 不变；`DEFAULT_TIME_SUFFIX 12:00:00` 仍是 `policy/category.ts` 唯一定义地，本票只回归（补时可查到）。

### 第三步 · 写代码（改了什么，一行一因）

1. `todayWakeWord`：把 `params.date==='yesterday' ? 查昨天 : 查今天` 的两态判扩成三态（加显式 date→查某天）；`''／null／undefined` 归无参（与 `resolveQueryDate` 的缺参口径同向）。
2. `todayEmpty`：把三合一通用句拆成三句——今天 `今天还没有记录`（老措辞，票面原话）／昨天 `昨天还没有记录`（内核点名）／显式 `这一天没有记录`（沿用 t411 通用句，hint 摘自指：不说「查某天」，说换个日子再查一次）；recent 支不动（`库里还没有记录`）。
3. `viewRecordToday` 日期支：`wakeWord: todayWakeWord(params)`＋`empty: todayEmpty(params)`；取数（`yesterdayStr／resolveQueryDate／listToday`）与 `window: date+' 这一天'`、`extra:{date}` 一字不动。
4. 判据 `t412-record-today.test.mjs`：标题 H1 一律断 `<h1 class="ilife-block-page-shell-title">…</h1>` 整段；KPI 数 `<div class="ilife-block ilife-block-kpi-card">`；空态数 `<section class="ilife-block ilife-block-empty-block">`；有数数 `<table class="ilife-block-data-table-table">`（禁裸子串，CSS 误命中已在注释写明）。

### 第四步 · 超线报警

本包告警线 **350 行（LF 口径，只数 `\n`，`packages/skill-bill/AGENTS.md`）**：

- `packages/skill-bill/src/query/read.ts` **LF 253**（改前 236，`+20/-3`）：**在线内，不超线**。判据：`readFileSync(f,'utf8').split('\n').length-1`。
- `packages/skill-bill/src/query/list.ts`、`commands.ts`、`policy/wakewords.ts` 本票一字未动（读数沿用 #411 后复测：182／53／133，均在线内）。
- 超线件仍是挂号两件（`wake-assets.ts 986`、`cmd_read.ts 485`），本票未碰、不扩大。
- 测试文件（`test/*.mjs`）、模板（`templates/*.html`）、`SKILL.md`、构建产物不进管辖计数（`structure.md` 管辖节）。

### 第五步 · 交付对账（逐行对第一步）

- 第一步「改动」→ `read.ts` 已在 `42c7f40` 落成（`git show --stat` 一件，`+20/-3`），路径一致。
- 第一步「判据」→ `t412-record-today.test.mjs` 已在 `a708374` 落成（新建 181 行），路径一致。
- 第一步「样张两张」→ 待 `c3` 落成（`t412-样张-查今天.html 71 465 B`／`t412-样张-查昨天空.html 69 244 B`，见第二节）。
- 第一步「本件＋scratch」→ 本件待 `c4` 落成；`.scratch/t412/*` 永不入库（ignore），与第一步一致。
- 未碰件（`commands.ts`／`shared`／路由／`range/search/detail`）：`git status --porcelain -- <未碰件>` 干净，与第一步一致。**偏差为零**（除多席他票的 `skill-calorie` 两件 M，本票未动）。

## 二 真跑读数（全部经加锁包装器）

### 窗口一：编译 → 靶向（t412 新锁＋t411 回归）→ 全量

| 命令 | 退出码 | 摘要行 |
|---|---|---|
| `node node_modules/typescript/bin/tsc -b packages/skill-bill` | 0 | 零诊断 |
| `node --test test/t412-record-today.test.mjs` | 0 | `tests 13 / pass 13 / fail 0`（标题 5／空态 4／回归 4） |
| `node --test test/t411-query-skeleton.test.mjs` | 0 | `tests 16 / pass 16 / fail 0`（显式空 `这一天没有记录` 不漂移） |
| `node --test`（包内全量） | 0 | `tests 207 / pass 207 / fail 0`（194 基线＋13 本票） |

### 窗口二：5 词逐条真跑（含空态一条）＋两张样张

种子（临时库）：今天 1 笔＋`2026-09-06` 2 笔＋`2026-09-01` 1 笔；昨天不种（留空态）。

| 词（入参） | 退出码 | 载荷 | 页标题 H1 | 产物／字节 |
|---|---|---|---|---|
| 查今天 `{}` | 0 | `total=1` | `查今天` | `w2-today.html 71 465 B` → `t412-样张-查今天.html` |
| 查昨天 `{"date":"yesterday"}`（**空态一条**） | 0 | `total=0` | `查昨天` | `w2-yesterday-empty.html 69 244 B` → `t412-样张-查昨天空.html`（`昨天还没有记录`＋空态块 1＋KPI 4） |
| 查某天 `{"date":"2026-09-06"}` | 0 | `total=2` | `查某天` | `w2-someday.html 73 271 B`（临时） |
| 查最近 `{"recent":true,"limit":2}` | 0 | `total=2` | `查最近`（窗口 `最近 2 笔`） | `w2-recent.html 73 264 B`（临时） |
| 查账单 `{}`（别名） | 0 | `total=1` | `查今天`（别名同页） | `w2-bill-alias.html 71 465 B`（临时，与查今天逐字节同内容） |

样张块级读数（真数产物）：查今天 `KPI 4（<div class="ilife-block ilife-block-kpi-card">）`、`TABLE 1`、`TH 7`；查昨天空 `空态块 1（<section class="ilife-block ilife-block-empty-block">）`、`KPI 4`、`TABLE 0`。

## 三 变异自证（受控变异：改坏必红 → 从 HEAD 取回必绿）

工具 `.scratch/t412/mutate.mjs`（照抄 `.scratch/t411/mutate.mjs` 改锚点）：锚点命中数 ≠1 即拒绝落盘；还原一律 `git show HEAD:<件>` 逐字节取回；末了 `git diff --stat` 判干净。

| 变异 | 改的是什么 | 变异后 | 还原后 |
|---|---|---|---|
| M1 | `todayWakeWord` 显式支退化为 `查今天`（GAP-T1 回退） | `tsc=0`，靶向 **13 跑／12 过／1 红**，红在「显式 date→查某天（GAP-T1）」`H1 应为查某天` | `tsc=0`，**13/13 绿** |
| M2 | `todayEmpty` 昨天支退化为 `这一天没有记录`（GAP-E1 回退） | `tsc=0`，靶向 **13 跑／12 过／1 红**，红在「昨天空：昨天还没有记录」`昨天空文案` | `tsc=0`，**13/13 绿** |

```
MUTATION m1 apply=0 tscMut=0 testMut=1 restore=0 tscRes=0 testRes=0
MUTATION m2 apply=0 tscMut=0 testMut=1 restore=0 tscRes=0 testRes=0
DIFF-CLEAN 与 HEAD 逐字节一致（POSTCHECK-DIFF-EXIT=0）
ALL-EXIT=0（全量复绿：tests 207 / pass 207 / fail 0）
```

首轮窗口（`6f76ccc0`）因工作区未提交而 `PRECHECK=1／BACK 仍红／M2 锚点 0 次`——那是「未提交即变异」的当面演示，已如实记在 `.scratch/t412/window-3.log` 前半；上表是提交后重跑（`90f937a4`）的干净读数。

## 四 走偏与整改（如实记，别当没发生）

1. **变异窗首跑未先提交**：`6f76ccc0` 的 `restore` 从 HEAD 取回的是旧码（无本票改动），故 `BACK 仍红`＋`M2 锚点 0 次`＋`ALL=1`。整改：按「逐小步持锁提交」先落 `42c7f40／a708374`，再重跑得上表干净红绿。**这条是「变异须在干净 HEAD 上跑」的实例**。
2. **样张校验曾拼错引号**：`.scratch/t412/probe-samples.cjs` 落盘前的一次性 `node -e` 因 pwsh 引号转义报 `SyntaxError`（exit 1）。整改：改用 `write` 落探针文件再跑，读数见第二节。非编译／测试／git／变异，不经锁。
3. **提交信息无 BOM**：三笔提交信息文件一律经 `write` 工具落盘（无 BOM），`git commit -F` 引用；提交前一律 `git diff --cached --name-only` 复核只含自己路径（见各 `window-c*.ps1` 日志）。

## 五 边界与不做（本票范围外，逐条有去处）

- **KPI 行格数／分类聚合卡／过滤芯片／200 条上限**：仍是 #411 骨架四块（KPI 四格＋数据表＋空态＋复制区）；按词分别定是 #413～#415 各自页的事。
- **查账单详情专属版式**：本票只锁互不吞词（`查账单→today` vs `查账单详情→detail`），版式是 #415。
- **SKILL/HELP 说明面接线**：是 #416；本票 `SKILL.md:51-55` 五行调用表一字不动（无参两行同调用即别名证据）。
- **17 词肉眼终审**：在 #417；本票两张样张可在侧栏直接打开。

## 六 GATE-RUN 逐行声明（锁记录导出）

锁目录 `.scratch/locks`（`gate-runs.log`），本票 7 次持锁已落盘（14 行＝7 START＋7 RUN，全部 `exit=0`；本件提交 c4 的两行在锁日志中，提交后可查）：

```
START ticket=412 runId=4135697e-64e2-4ca7-ab28-c0c74c950290 cmd="pwsh -File .scratch\\t412\\window-1.ps1" waitedMs=30027 pid=47648 at=2026-09-16T01:43:52.673Z
RUN ticket=412 runId=4135697e-64e2-4ca7-ab28-c0c74c950290 cmd="pwsh -File .scratch\\t412\\window-1.ps1" waitedMs=30027 exit=0 pid=47648 at=2026-09-16T01:44:10.153Z
START ticket=412 runId=fd6f359d-3852-40ca-867c-43541282e9b6 cmd="pwsh -File .scratch\\t412\\window-2.ps1" waitedMs=0 pid=44484 at=2026-09-16T01:44:15.970Z
RUN ticket=412 runId=fd6f359d-3852-40ca-867c-43541282e9b6 cmd="pwsh -File .scratch\\t412\\window-2.ps1" waitedMs=0 exit=0 pid=44484 at=2026-09-16T01:44:20.860Z
START ticket=412 runId=6f76ccc0-0346-4503-a1ff-8e61aa3930f6 cmd="pwsh -File .scratch\\t412\\window-3.ps1" waitedMs=0 pid=72612 at=2026-09-16T01:44:46.419Z
RUN ticket=412 runId=6f76ccc0-0346-4503-a1ff-8e61aa3930f6 cmd="pwsh -File .scratch\\t412\\window-3.ps1" waitedMs=0 exit=0 pid=72612 at=2026-09-16T01:45:11.698Z
START ticket=412 runId=94e53d7c-6270-47b1-aa78-da3f70301ccb cmd="pwsh -File .scratch\\t412\\window-c1.ps1" waitedMs=0 pid=58512 at=2026-09-16T01:46:04.447Z
RUN ticket=412 runId=94e53d7c-6270-47b1-aa78-da3f70301ccb cmd="pwsh -File .scratch\\t412\\window-c1.ps1" waitedMs=0 exit=0 pid=58512 at=2026-09-16T01:46:09.821Z
START ticket=412 runId=ce0f536e-dde8-4f51-bcb8-96603371314f cmd="pwsh -File .scratch\\t412\\window-c2.ps1" waitedMs=0 pid=45948 at=2026-09-16T01:46:12.878Z
RUN ticket=412 runId=ce0f536e-dde8-4f51-bcb8-96603371314f cmd="pwsh -File .scratch\\t412\\window-c2.ps1" waitedMs=0 exit=0 pid=45948 at=2026-09-16T01:46:17.748Z
START ticket=412 runId=90f937a4-32b8-45f3-a034-42e46fd42b58 cmd="pwsh -File .scratch\\t412\\window-3.ps1" waitedMs=0 pid=32092 at=2026-09-16T01:46:21.287Z
RUN ticket=412 runId=90f937a4-32b8-45f3-a034-42e46fd42b58 cmd="pwsh -File .scratch\\t412\\window-3.ps1" waitedMs=0 exit=0 pid=32092 at=2026-09-16T01:46:49.334Z
START ticket=412 runId=1063af94-cfc4-4a19-ba74-54dbbc89ca0a cmd="pwsh -File .scratch\\t412\\window-c3.ps1" waitedMs=0 pid=68372 at=2026-09-16T01:47:52.701Z
RUN ticket=412 runId=1063af94-cfc4-4a19-ba74-54dbbc89ca0a cmd="pwsh -File .scratch\\t412\\window-c3.ps1" waitedMs=0 exit=0 pid=68372 at=2026-09-16T01:47:55.378Z
```

- `4135697e`（窗口一：编译／t412 靶向／t411 回归／全量）→ `exit=0`，`WINDOW1-SUMMARY tsc=0 t412=0 t411=0 all=0`。
- `fd6f359d`（窗口二：5 词真跑＋两张样张）→ `exit=0`，`WINDOW2-SUMMARY bad=0`（`ACCEPT 5 行`见 `.scratch/t412/window-2.log`）。
- `6f76ccc0`（变异首跑：未提交演示）→ `exit=0`（包装器成功；窗内 `PRECHECK=1／ALL=1` 如实落盘）。
- `94e53d7c`（提交 c1：源码）→ `42c7f40`；`ce0f536e`（提交 c2：测试）→ `a708374`。
- `90f937a4`（变异重跑：干净红绿）→ `exit=0`，`WINDOW3-SUMMARY preClean=0 postClean=0 all=0`。
- `1063af94`（提交 c3：样张两张）→ `07cb152`。
- 本件提交 c4 的两行（START／RUN）在锁日志中，提交后以 `Select-String gate-runs.log -Pattern <runId>` 复核。

提交（逐小步持锁，已落三笔）：`42c7f40 功能(412)：today 分支标题按参判＋空态拆四句`／`a708374 测试(412)：t412-record-today 五词四态＋空态四句＋回归四锁`／`07cb152 证据(412)：5 词样张两张`（各 `git diff --cached --name-only` 只含自己路径，见各窗口日志）。

## 七 交棒（下一手要什么）

- 读盘入口：`packages/skill-bill/src/query/read.ts:84-129`（`todayWakeWord／todayEmpty／viewRecordToday`）；判据 `packages/skill-bill/test/t412-record-today.test.mjs`；样张两件在 `docs/skills/skill-bill/t412-样张-*`。
- 下一票（#413 起）改的是**区间／搜索／详情各自的页**：在 `list.ts` 通用版式上按词加块，改 `read.ts` 里那一支的入参即可，别动 `commands.ts` 形状（形状是契约）。
- 沿革债（非本票引入，不扩大）：`src/render/templates.ts:32-35` 四条查询 key→老模板映射仍在（t411 第六节第 3 条已记，随「读命令搬迁」整条线处理）。
