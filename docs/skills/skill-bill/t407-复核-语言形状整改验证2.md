# t407 复核-语言形状整改验证2

> 只读验证：核 W1 根因整改是否真成立＋端到端仍绿。证据件 `docs\skills\skill-bill\t407-整改-语言与形状-证据.md`；提交 `934a9ae`/`dd16720`/`d59c4be`（+同信息两笔 `42dc01d`/`d501331`）。

## 一、逐条核验

1. 机审真绿：成立。`node docs\skills\skill-bill\t407-v8-style-audit.mjs --dir docs\skills\skill-bill` 自跑 EXIT 0；设计位 `·`/`|` 0 处；标题写法 55 处（允许保留）；英文裸词 0 份；重复句 0 份；32/32 齐。眉头 `记账 · 写入域`、页标题 `唤醒词 · 补齐槽位/回执` 均在 TITLE_POS，与证据件 `t407-整改-语言与形状-证据.md:11-12,55` 一致；非版式位旁证 `同分类近邻 · 金额与本笔不符` 4 处（`t407-页-记退款-采集页.html` 表格单元格，不进懒政）。
2. 映射单一定义地：成立。对外 5 件只数出目录：`packages/skill-bill/src/shared/userWording.ts:84(statusNoteOf)`、`91(wakeWordOf)`、`97(fieldLabelOf)`、`111(badgeTextOf)`、`122(nextStepOf)`；`FIELD_LABELS:28`、`WAKE_WORDS:47`、`STATUS_NOTES:66` 只在此定义一次（`HELP_WAKE_WORDS`/`WAKE_TABLE` 系另一套口径，不混）。抽 4 份（代表-记支出-采集页、页-记退款-采集页、页-撤销-回执页、页-恢复-回执页）徽章区 `bill.record.` 0、正文裸 `category/amount/deleted_at` 与 `expense/refund/restore` 0（仅存于允许位：复制 prompt/日志重跑口令与 JSON 键，见待裁定 2/3）；`data-t` 载荷 `【bill` 在 32 产物 0 处、`记账数据` 32 处。
3. 徽章无·串：成立。抽上 4 份，`badge-check.mjs` 实测 chip 全角空格连接（`记支出　支出 金额取负数`、`记退款　分类按三级挂靠`、`记一笔　方向按金额符号判`），`badgeZoneHasDot=false`、`badgeZoneHasBill=false`；落点 `packages/skill-bill/src/shared/typeBadge.ts:63-66` 走 `renderChips`/`renderStatusBadge`/`renderCaliberLine` 独立形状，未拼 `input.key`。
4. 疑似错字：成立。`packages/skill-bill` 全包 `囗` 0 命中；32 产物 `退出囗` 0 命中；证据件 `t407-整改-语言与形状-证据.md:45` “0 无需改”复核属实。
5. 五条裁定：均成立。①命令去屏 `typeBadge.ts:16,63-66`＋`test/user-wording.test.mjs:119-120` 钉 `!includes('bill.record.add')`；②复制载荷 `copyArea.ts:112-115` 缺省 `title:'记账数据'`，32 产物复量见第 2 条；③未给 `summaryRow.ts:46-48(money2(0)→'未给')`＋`slots.ts:60-68,73-75(0/"0"视同缺项)`；④笔误统一 `receiptBody.ts:38-43(PAGE_WAKE_WORDS add恒写记一笔)`＋`58-59(wakeWordOf同源)`；⑤退款不动 `summaryRow.ts:34-37(DIRECTION仅expense/income)`，`refund` 不判方向。
6. 真 390：成立。390 宽 iframe（不用 `--window-size=390`），两份产物（代表-记支出-采集页、页-记退款-回执页）900 高均 `VW=390 DOC=375 SCROLL=375 OVER=0`（15px 为纵向滚动条占位）；加高到 5000 高消纵滚后 `VW=390 INNER=390 DOC=390 SCROLL=390 OVER=0`，即 `doc=vw` 且超宽元素 0。
7. 提交范围：成立。`934a9ae` 19 件仅 `packages/skill-bill/src/record/*`；`dd16720` 20 件仅 `src/shared/*`＋`test/*`；`d59c4be` 10 件为 6 产物＋证据＋2 测试；`42dc01d` 12 件、`d501331` 13 件均为 `docs/skills/skill-bill/t407-页-*` 产物；五笔路径前缀仅 `packages/skill-bill/` 与 `docs/skills/skill-bill/`，无别窗文件卷入（当前 `??` 在途件均未进这五笔）。
8. 读数：成立。`npx tsc -b packages/skill-bill --force` EXIT 0；`node --test "packages/skill-bill/test/*.test.mjs"` 两次均为 127/127 过、0 红（详见第二节）。

## 二、读数

- 机审：`EXIT 0，全绿；设计位 0；标题写法 55；英文裸词 0；重复句 0；32/32 齐`（`.scratch\t407-verify4\audit.txt` 首尾 25 行）。
- 载荷与错字（32 产物 utf8 口径）：`【bill` 0；`记账数据` 32；`>0.00<` 严格 0（裸 `0.00` 子串 35 均为 `120.00`/`5000.00` 等尾串，非零金额格）；`退出囗` 0；`未给` 28。
- 待裁定留痕（未硬编旁证）：`bill-cmd-read bill.record` 68 行；日志 `bill.record.*（receipt）` 32 行；`同分类近邻` 1 行内 4 处；`slots.ts:49` 仍保留 `undo/restore` 字面值。
- tsc：`npx tsc -b packages/skill-bill --force` EXIT 0（空输出），变异前/还原后各 1 次均为 0。
- 测试：`node --test "packages/skill-bill/test/*.test.mjs"` 第一遍 `tests 127 pass 127 fail 0` EXIT 0；第二遍 `tests 127 pass 127 fail 0` EXIT 0。
- 真 390：A（代表-记支出-采集页）`VW=390 DOC=375 SCROLL=375 OVER=0`；B（页-记退款-回执页）同值；A 加高 `VW=390 INNER=390 DOC=390 SCROLL=390 OVER=0`。
- 变异：改坏 `userWording.ts:30 category:'分类'→'category'` 后 `tsc EXIT 0`＋`user-wording.test.mjs 11/10/1`（`actual:'category' expected:'分类'`，与证据件一致）；还原后 `tsc 0`＋全包 `127/127`＋机审 `EXIT 0`。三自证：`git diff --stat -- packages/skill-bill` 空（0 字节）；`git status --porcelain -- packages/skill-bill` 空（0 字节）；`sha256(userWording.ts)=2F8141861AE935DB2FC247693D9551AC37597FAD5223A406A337063DE3A741CB` 与备份一致（`fc` 无差异）。

## 三、不成立或部分成立

无不成立，无部分成立。仅两处口径澄清（不扣分）：

- 证据件“`0.00` 0”指严格零金额格 `>0.00<` 为 0；裸子串 `0.00` 在 32 产物 35 处均为正常金额尾串（如 `120.00`），已逐一排除。
- 真 390 常规高度 `DOC=375` 系纵向滚动条占 15px（`SCROLL=DOC` 且 `OVER=0`）；加高消纵滚后 `DOC=390` 与视口严格相等，已自证非溢出。

## 四、结论

结论：放行。

W2（桌面版式＋收尾）可以开工。
