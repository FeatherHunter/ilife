PASS

## 一 · 机器证据（只认提交号与自跑，不采信被审结论）

被审提交 `19249d6`（功能）＋`66036d9`（证据），`merge-base --is-ancestor` 两笔 exit 均为 0，复核开工收工 HEAD 均为 `66036d9`。编译入口 `node node_modules/typescript/bin/tsc -b packages/skill-bill`。全部持锁（`node tooling/run-locked.mjs --ticket 432`，runId 见 `.scratch/locks/gate-runs.log`）：`r432-v-build` exit 0；`r432-v-stamp` exit 0；`r432-v-gen-a`／`r432-v-gen-b` exit 0，产物 sha256 均为 `308a24eb0050a459d02f09edaf88e50cdbda5ee984a61c140399fe774c21db48`（与证据 `308A24EB…21DB48` 同值，逐字节相同）；`r432-v-check-green` exit 0（`GEN-CHECK PASS：命令 2 条；能力 1 个（record）`）。手改探针 `r432-v-handred` exit 0：内 `HAND-RED-EXIT=1`（第 37 行起：盘上 `// HAND-EDIT-PROBE-t432`／生成空）、`GEN-RESTORE-EXIT=0`、`RESTORE-CHECK-EXIT=0`、`BYTE-EQUAL-ORIG=1`。双门 `r432-v-stale2`／`r432-v-pair` exit 0：`STALE-RED-EXIT=1`／`PAIR-RED-EXIT=1`，`--force` 重建＋重打印记后回绿，`DIST-BYTE-EQUAL=1`。自设新探针 `r432-v-stampmiss` exit 0：删印记后 `GEN-STAMP WARN`＋`GEN-CHECK PASS`（`STAMP-MISSING-EXIT=0`），恢复后 `STAMP-BYTE-EQUAL=1`。命令面：注册表 2 键、过渡表 14 键、envelope 16 键（`SURFACE-OK=1`）；真跑 `r432-v-surface` exit 0（缺槽位出采集页 77721 B，库隔离在仓外临时目录）。回归 `r432-v-billtest` exit 0（`tests 153／pass 153／fail 0`）。排队实测：`r432-v-check-green waitedMs=10015`（等 ticket 481）、`r432-v-stale2` 前身等 20023、`r432-v-pair waitedMs=90069`、`r432-v-stampmiss waitedMs=40036`（等 ticket 543），活锁一律未抢，符合锁口径。

## 二 · 逐条路径与复跑结论

改动件（`19249d6`，3 件）：`scripts/gen-cli.mjs`（LF 297，告警线 350 以内）、`package.json`（`build` 加 `--stamp`、新增 `gen`／`gen:check`）、`src/cli/registry.ts`（手写改生成物，进出口 `REGISTRY`／`REGISTRY_KEYS` 不变）。范围外未动：`combos.yaml`、`base-render`、过渡表 14 行、`wake-assets` 均不在该提交内（`git show --stat` 无第四件）。证据件 `t432-生成器链-证据.md`（`66036d9`）所称三验收逐条复现成立。红线：复核未改任何源码，临时变异（手改行、删 dist、改 dist 一字节、删印记）均在同一持锁内还原且字节核等，收工 `git status --short packages/skill-bill docs/skills/skill-bill/t432-生成器链-证据.md` 无输出。复核驱动落 `.scratch/t432-verify/`（`hand-probe.mjs`、`stale-probe.mjs`、`pair-probe.mjs`、`stamp-missing-probe.mjs`、`surface.mjs`、`surface-run.mjs`，均 gitignored）。观察两则（S3，不阻断）：①证据写生成器“287 行”，实测 LF 297（提交插入行数亦 297），数字笔误，告警结论不变；②删 dist 后 plain `tsc -b` 因增量判定跳过重建、门持续红，须 `--force` 才回绿——与证据医嘱一致，门行为正确，`r432-v-stale` 首跑即因此 exit 1，改用 `--force` 后 `r432-v-stale2` exit 0，如实披露。

## 三 · 缺陷判定

S1／S2 无。S3 两则见第二节（行数笔误；`--force` 口径建议写进证据医嘱，当前证据已写，无需改）。双声明同键、`index.ts` 未再导出、字段缺失等分支有生成期 `merge`／`loadCapability` 抛错代码在位，本次未逐条变异（已有双门＋手改＋印记缺失三类红证，判别力已足）。

## 四 · 未做项与下一手

未做：未跑卡路里侧同构生成器对照（范围外）；未逐条变异全部声明校验分支（见第三节理由）。下一手：本复核判 PASS，被审两笔可照常 downstream；遗留出口（提升公共层、逐条搬迁删过渡表行）属后票输入，本票不动。复核者停止，等编排者指派。
