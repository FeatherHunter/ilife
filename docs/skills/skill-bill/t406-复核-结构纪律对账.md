# t406 复核 · 结构纪律与命令登记对账（对抗式，只读）

被审对象：`9b85ccd`（＋`72ac657` 的 4 行证据补记）。复核方式：整读新增九件源码 ＋ `git show --numstat` 复跑读数 ＋ 与 `packages/skill-calorie` 同名件逐行对照。本次复核未改任何源码、未提交。

## 一、五条铁律逐条判定

### 铁律一 · 能力自治 —— 部分成立

成立的：

- `src/record/` 五件都只属记账写入域一个能力：`record/write.ts:16-19`／`record/collect.ts:16-17`／`record/receipt.ts:15-17` 的跨目录引用一律经 `../fetch/index.js`／`../policy/index.js`／`base-paint`，没有一件伸手进别能力的内部件；出口 `src/cli/cmd_read.ts:33-34` 也只取 `runRecordWrite` 与 `REGISTRY` 两个名字。
- 不是「照抄一份自家逻辑」：两条命令的处理体确实从 `cmd_read.ts` 搬走并删净（见第二节）。

不成立的（三处）：

1. **共用位直接伸手进别能力的内部件**。`src/shared/commandSpec.ts:16` `import type { BillDb } from '../fetch/db.js'`、`src/record/index.ts:9` 同一路径；而 `fetch/index.ts:4` 已经把 `BillDb` 转出，`policy/index.ts:3` 也已经把 `RecordOp` 转出。`src/shared/writeParts.ts:19` `import type { RecordOp } from '../policy/record.js'` 同理。`src/cli/cmd_read.ts:36`（改动前就在）同形——新件把这条老毛病照搬了。判据出处：`docs/agents/structure.md:23`「把对方那份抄一遍放进自己目录，不算走了接口」同段的「互相引用内部件」。
2. **同名公式在本包内又写一份**。`src/shared/writeParts.ts:41-43` 的 `esc` 与 `packages/skill-calorie/src/shared/writeParts.ts:29-31` 逐字节相同；`writeParts.ts:46-49` 的 `totalChanges`（`SELECT total_changes()`）与 calorie 同件 `:38-41` 逐字节相同。饼干侧文件头 `writeParts.ts:10-15` 只交代「转义口径不得互换」，**没有一句承认这两条是从别包抄来的、也没有说清抄的债务由谁收**。
3. **共用位里只坐着一个能力**。以「两个调用点」代替「两个能力」的自证写在 `shared/receiptParts.ts:3-5`、`shared/docPage.ts:3-5`、`shared/copyArea.ts:3-5`、`shared/writeParts.ts:4-7`。全仓引用面实测（`Select-String` 全 `src`）：`docPage`／`copyArea`／`receiptParts` 的调用方只有 `record/collect.ts` 与 `record/receipt.ts` 两件，都在 `record/` 里面；**唯一真被第二个能力用的是 `commandSpec`**（`record/*` ＋ `cli/registry.ts` ＋ `cli/cmd_read.ts`，`structure.md:67`「共用件是从第二个用法里长出来的」）。

### 铁律二 · 概念唯一 —— 部分成立

成立的：

- `BILL_KEY_SHAPES` 只有一处定义（`src/render/envelope.ts:41`），且其迁移段由 `REGISTRY` 运行期派生（`:29-38`）；两条命令的形状事实真住 `src/record/commands.ts:17-28` 一处。
- 过渡表写清了它是债务：`envelope.ts:7-9`「过渡表是**债务**：14 条尚未搬进能力目录的命令……待生成器链那票把它替换成」。
- 闸门是双向的：`envelope.ts:33-35` 在「过渡表里还留着同一条命令且形状对不上」时抛 `BILL_SHAPE_MISMATCH`；`test/record-write.test.mjs:88-91` 反过来钉 `BILL_KEY_SHAPES` 不缩水且逐条等于注册表形状。

不成立的（三处）：

1. **同一条断言文本写两遍**。`src/record/write.ts:63` 与 `src/record/collect.ts:88` 各拼一次 `'缺必需槽位：' + … + '（已出采集页，补齐后重跑同一条命令）'`；前者进 envelope 载荷，后者进页内文案。改一处漏一处即同一件事两个说法。
2. **同一条「加命令接受哪些参数」的事实两处**：`src/record/write.ts:29` 的 `ADD_FIELDS`（七个字段）与 `src/record/collect.ts:34-42` 的槽位表（七个字段）各写一遍，且后者混进了非字段的 `op`（`:45`），两份名单迟早走散。
3. **过渡表仍是同一个事实的第二个定义地**。14 条未迁移命令的形状在各能力声明里还有一份权威源，`envelope.ts:10-25` 又手抄一份。债务标注合格，但「一个东西只有一个定义地」在这 14 条上现在不成立。

### 铁律三 · 改动可预告 —— 部分成立

**逐行对账结果：偏差 0。** 复跑 `git show --numstat 9b85ccd` 与证据第一节两张表逐行核对：

| 证据件那一行 | 复跑读数 | 偏差 |
| --- | --- | --- |
| 新增十一件（`shared` 五 ＋ `record` 五 ＋ `cli/registry.ts`）＋ `test/record-write.test.mjs` | 与 `--numstat` 逐件逐数相同（45／72／44／53／115／34／23／164／134／96／35／207） | 0 |
| 改 `cmd_read.ts` ＋26／−28（558→556） | `git show` 实测 PLUS=26 MINUS=28；node 口径 556 行 | 0 |
| 改 `envelope.ts` ＋22／−3 | 实测 PLUS=22 MINUS=3（`9b85ccd^` node 口径 49 → 68） | 0 |
| 改 `test/cli.test.mjs` ＋5／−1（126→130） | 实测 PLUS=5 MINUS=1；node 口径 130 行 | 0 |
| 「计划外新增／改动：无」 | `git status --short packages/skill-bill` 现在为空；无一件清单外文件 | 0 |

- 类型上没有 `any`；`unknown` 也都写得出形状（`Record<string, unknown>` 一类）。
- 不成立一处：**`src/record/write.ts:153` 新引入 `getById(db, id) as unknown as Record<string, unknown>`**。`fetch/db.ts:112` 的 `getById` 收 `BillDb` 返 `BillRow`（十个字段写得出），这里先转 `unknown` 再转宽字典，只为让 `:157` 的 `pre[f]` 用变的字符串取字段——正是条文点名的「说不出形状的 `unknown`」；同文件 `:154`／`:156` 的 `patch as Partial<BillRow>`、`patchRecord` 是同一手法。搬迁前的 `cmd_read.ts` 老写法没有这两个转型（见第一节 diff `-r = updateBill(handle, id, patch as Partial<BillRow>)` 只有一处），新写法把类型面放大了。

### 铁律四 · 名字取自 HELP —— 成立（一处分不清）

成立的：

- `record` 与 HELP 的 `bill.record.*` 一级分组同名；`collect`／`receipt` 对应 `packages/skill-bill/src/help/lookup.ts:22-24` 的两句话（记一笔＝须 category+amount；改/撤销/恢复＝须 id），用户念得出。
- 公开接口名都能在 HELP 或既有形状里指出说法：`RECORD_COMMANDS`／`runRecordWrite`（命令与入口）、`RECORD_SLOTS`／`missingSlots`（槽位）、`recordCollectDoc`／`recordReceiptDoc`（两张页）、`statusCard`／`reconcileDisclosure`。
- 全包 `src/` 下无 `utils`／`helpers`／`common`／`misc`／`core`／`types`／`base` 这类工种名（新增件逐件核过）；`shared/` 是本仓既有的固定名，不算自创。

一处分不清：`src/cli/registry.ts:15-17` 的 `SOURCES` 与 `record/commands.ts:9` 的说法不同——`commands.ts` 说「本票只这两条：13 条 `record.add` 的字段矩阵与 8 条特殊收支的确认页是后票的事」，`lookup.ts:23` 的 HELP 说记一笔「含借贷/分期/退款/报销」。用户按 HELP 找「报销」「分期」会落到还没搬的 13 条字段矩阵上，名字对得上、范围对不上。

### 铁律五 · 接口小、里面厚 —— 部分成立

逐件数「对外给的本能力目录名字」（同目录内互用不算，零引用者单列）：

| 件 | 对外给几个 | 结论 |
| --- | --- | --- |
| `record/index.ts:14,19` | 2（`RECORD_COMMANDS`、`runRecordWrite`） | ≤5 成立 |
| `shared/commandSpec.ts:21,29,32,45` | 4 | ≤5 成立 |
| `shared/writeParts.ts:24,38,41,46,53,60,70,71,72` | 8（`BillReceipt`／`RECEIPT_FORMAT`／`esc`／`totalChanges`／`commandLine`／`writeSection`／`DOC_VERSION`／`DOC_SKILL`／`DOC_TITLE`） | **超 5** |
| `shared/receiptParts.ts:18,23,32` | 3 | ≤5 成立 |
| `shared/docPage.ts:41` | 1 | ≤5 成立 |
| `shared/copyArea.ts:61,72,107` | 3 | ≤5 成立 |
| `record/collect.ts:24,33,63,86` | 4 | ≤5 成立 |
| `record/receipt.ts:49` | 1 | ≤5 成立 |
| `record/write.ts:104,125` | 2 | ≤5 成立 |
| `cli/registry.ts:33,35` | 3（`SOURCES` 是模块私有的，见 `:15`） | ≤5 成立，但见下方「重算别家口径」 |

「里面厚」成立：`copyArea.ts:92-104` 的三格式经 `buildDataText`、`collect.ts:94-126` 的六块页面装配、`receipt.ts:58-88` 的三格加明细加折叠区，都是真活；没有「删掉外面说不出少了什么」的空层（三件共用件的身体分别包 `base-paint` 的三组现成件，删掉它们调用方要各自重写一遍）。

不成立三处：

1. **`writeParts.ts` 对外 8 个 > 5**（`structure.md:53`）。它同时出「回执事实类型 ＋ 影响行数口径 ＋ 转义 ＋ 命令原文 ＋ 页壳 ＋ 三个页标识」，页标识三常量与转义本可另立一件。
2. **`cli/registry.ts` 在重算别家的口径**：`:20-31` 的 `build` 自己遍历各能力声明、自己查重（`structure.md:55-58` 的「不许重算别家的口径」；`docs/agents/命令登记纪律.md:105-112` 把这类汇总位的机器拦截交给生成器与棘轮）。`REGISTRY_KEYS`（`:35`）生产代码零引用，只有 `test/record-write.test.mjs` 用。
3. **`record/index.ts:16`** 自己又建一张 `BY_KEY` 查表（`RECORD_COMMANDS` 已经在 `commands.ts` 里），与注册表的查表职责重叠了一层。

## 二、命令登记纪律四条判据

**先说结论：「一个命令恰住一处」对本票两条命令成立，对剩下 14 条不成立；四条判据里第 2／3／4 条今天无法跑（没有生成器、没有棘轮、没有探针）。**

**判据 1 · 加一条命令，除声明与生成物之外还有没有手写文件？—— 部分成立，且与本纪律的形状一冲突。**

- 成立的一侧：两条写命令确实一条一处。复跑 `git show 9b85ccd -- packages/skill-bill/src/cli/cmd_read.ts`：`case 'bill.record.add'`（五支）与 `case 'bill.record.update'`（十一支）**整段删除，没有留空壳、没有留 `default` 兜底**；diff 里两条命令名在 `cmd_read.ts` 只剩两处旁注（`:167-168` 的守卫注释、`:520` 的主路注释），零 `case`（当前 `:174` 起是 `switch`）。运行期入口改由 `cmd_read.ts:148-159` 的 `runRegistered` ＋ `:522` 的 `REGISTRY[key] !== undefined` 查表，命中即走 `src/record/index.ts:19`，命令名不属本域即抛（`test/record-write.test.mjs:94-97` 两条断言钉住）。
- 一处弱残留：`cmd_read.ts:411`／`:414`（这两行未改）在 `bill.link.submit` 的文案里逐字写 `bill.record.add` 作「主操作请先走」的提示串。它不改口径、不是第二个处理入口，但那句文案若与命令名走散就没人拦——证据件没提这一处。
- 不成立的一侧：**加一条命令要手动改的文件不止「它的声明 ＋ 它那个子功能文件」**。按 `commands.ts:10` 自己的说法是「加一条命令＝只改这个文件＋它那个子功能文件；`src/cli/registry.ts` 与 `src/cli/cmd_read.ts` 一行不动」，但 `registry.ts:15-17` 的 `SOURCES` 是手写数组、`registry.ts:8-10` 自认「本文件**手写**（卡路里那份由它的 `scripts/gen-cli.mjs` 生成）：饼干还没有生成器链，那是后票」。本纪律 `docs/agents/命令登记纪律.md:28-36` 的形状一要求这一处由生成器派生，判据 1 今天在饼干上答「有手写文件」。
- 严重项（见第五节）。

**判据 2 · 手改一件生成物，`pnpm gen:check` 红不红？—— 跑不了。** `packages/skill-bill` 没有生成器链：本票证据第三节第 5 条逐字写「`pnpm gen`／`pnpm gen:check` **没跑**（不在本票范围，生成器链是后票）」。这一台上没有生成物可手改，判据 2 今天既非红也非绿，是「没有这道门」。

**判据 3 · 搬完一条，自己场景分片与棘轮上限是不是都变小了？—— 跑不了。** 本包没有 `src/cli/legacy/` 场景分片、也没有两个棘轮文件（它们住 `packages/skill-calorie/test/`：`docs/agents/命令登记纪律.md:102-103`）。搬迁确实发生了，但「只许变短」今天只体现在 `cmd_read.ts` 的 558→556 与 `envelope.ts` 过渡表删两行上，没有机械拦截。

**判据 4 · 加／删一条命令之后，独立验收探针 `UNACCOUNTED` 是不是空的？—— 跑不了。** 探针 `docs/skills/skill-calorie/t293-验收-命令自治.mjs` 是卡路里件的，饼干侧没有对应探针，证据件也没提这一条。

**「一个命令恰住一处」复核读数**：`cmd_read.ts` 的两处 `case` 删干净（上面已给行号与支数）；`+26／−28` 的构成复跑核对过——删 28 行＝两个 `case` 全段（含 `import` 里顺带删掉的 `updateBill`／`undoBill`／`restoreBill`／`validateAddInput`／`validateUpdateInput`／`parseRecordOp` 六名），加 26 行＝`runRegistered` 一件（13 行）＋ 主路两处查表（3 行）＋ 守卫改写与注释（若干）；证据件 `:32` 对这段的描述（「删掉两个 `case`……新增 `runRegistered`……顺带清掉迁走后再没人用的五个 import」）与实际删除相符（实际清了六名 import，证据写「五个 import」是按行数口径数的两行声明，差一名，不影响判定）。

14 条未迁移命令仍住 `cmd_read.ts` 的 switch（`:174` 起）＋ `envelope.ts` 过渡表两处，这是债务（`envelope.ts:7-9` 认了）。

## 三、必报五步与超线报警

五步在证据件里的落点是：影响清单＝第一节（`:6-47`，含「有意未动」）、结构设计＝第二节（`:48-106`）、写代码＝第三节真跑读数 ＋ 第四节变异自证（`:108-240`）、超线报警＝第五节（`:242-252`）、交付对账＝第六节（`:254-278`）。**五步齐；第一步在动手之前（取证件的「基线」写在 `:4`，且 `72ac657` 的补记只动读数一节），第四、五步在交付时；顺序符合「报给用户看过再动」的形式要求。**

- **第一步 · 影响清单**（`:6-34`）：两张表逐行都在，每个文件一句话与理由都在。判据（`structure.md:80`「每行指向一个能力目录」）**不成立**：`cli/registry.ts`（`:24`）、`cli/cmd_read.ts`（`:32`）、`render/envelope.ts`（`:33`）三行指向的是工种目录而不是能力目录；`shared/` 五行（`:14-18`）也没写「被哪两个能力用」（这一栏到第二步 `:91-99` 才补）。
- **第二步 · 结构设计**（`:48-99`）：目录树、逐件职责、逐件对外给几个、共用件两个调用点都报齐，且自报「≤5」。三处不符实：`writeParts.ts` 实为 8 个（见第一节末）、共用件的两个「用法」有三件落在同一个能力 `record/` 里（`:96-99`）、第二节 `:103` 只点 `RECORD_SLOTS` 为「必备槽位」的唯一定义地，没交代页面文案那条同句两写（`write.ts:63`／`collect.ts:88`）。另：本票新建了 `src/record/` 与 `src/shared/` 两个目录层级，`structure.md:88` 要求「新建目录层级……等用户点头再动」，证据件没有记这一步的点头。
- **第三步 · 写代码**：跨能力引用走公开接口——大部分成立，三处破口已列（`fetch/db.js`、`policy/record.js` 深路径）；常量与类型只写一处——部分成立（第二节已列三处双写）；就地摆正——成立且做了：`test/cli.test.mjs` 那条旧断言（缺 `id` 原期望 `exit 2`）随新规矩改成 `exit 0` ＋ `ok:false` ＋ `缺必需槽位：id`（diff `:15-20`）。
- **第四步 · 超线报警**:**正面报了**。`:246-252` 给出一句话「已超线，需要根据规则进行重构。」＋ 超因（一个文件背五件事、长期欠账）＋ 先不拆的理由（等 16 条命令全搬完再拆 `dispatch-*.ts`），符合 `structure.md:97-101`。
- **第五步 · 交付对账**（`:254-278`）：16 行逐行 ＋ 一行「计划外」＋「偏差合计：0」。**复跑核对：偏差真为 0**（第一节表）。

**超线报警的两处口径问题**：

1. `packages/skill-bill` 没有 `AGENTS.md`，本包告警线数字**未定义**（`structure.md:70` 要求写在各包自己的地方，兄弟件是 `packages/skill-calorie/AGENTS.md:9` 的 350 行）。实施者按兄弟件 350 行自报（证据 `:244` 写明口径来源），**判恰当**：数字来源可追、与兄弟件同数，是过渡期唯一可用的口径。
2. 但证据件没写明「本包这一条规矩今天缺、数字是借来的」，`:45` 只写「本包 `AGENTS.md`、告警线｜未建、未动」。按纪律，缺的是「本包自己的数字」这件事本身，不写就等于把借来的 350 当成自家规矩。`cmd_read.ts` 556 行超线它**正面报了**，没有回避。

## 四、用词纪律命中项

按 `docs/agents/wording.md` 的禁用表逐词 `grep` 新增与改动件（命令行 `Select-String`，命中含中文注释与证据件正文）：

| 位置 | 命中词 | 逐字 | 判 |
| --- | --- | --- | --- |
| `src/shared/writeParts.ts:1` | 底座 | 「写命令的回执**底座**」 | 违（`wording.md:37` 底座→「已经有用的实现」） |
| `src/record/write.ts:14` | 键 | 「create **键**＝该键写入列全集」 | 违（`wording.md:19` 键→命令） |
| `src/render/envelope.ts:1,2,44,45` | 键 | 「16 联动 key×shape 映射」／`billShapeFor`／`BILL_KEY_SHAPES` 旁注 | 违（本行未改，但它是本票在读的形状源；`:2` 是本票新加的行，逐字「迁移过的命令……形状从 `src/cli/registry.ts` 运行期派生」，未出现「键」） |
| `docs/skills/skill-bill/t406-实施证据.md:15` | 底座 | 「写命令的回执底座」 | 违 |
| `docs/skills/skill-bill/t406-实施证据.md:69` | 底座 | 「writeParts.ts　回执底座」 | 违 |
| `docs/skills/skill-bill/t406-实施证据.md:86` | 底座 | 「回执底座五件」 | 违 |
| `docs/skills/skill-bill/t406-实施证据.md:101` | 落地 | 「都在代码里**落地**」 | 违（`wording.md:51` 落地→实施） |
| `docs/skills/skill-bill/t406-实施证据.md:258` | 落地 | 表头「实际**落地**」 | 违 |
| `docs/skills/skill-bill/t406-实施证据.md:244` | 口径 | 「兄弟件 350 行」借数，不属禁用词 | 过 |
| `test/cli.test.mjs:37,55,65,76,88` | 键／闭环 | 「16 键全票」「闭环」 | 违（旧文，本票未改这几行；`:37` 是本次改动所在 describe 的标题） |

未见「打通」「沉淀」「赋能」「抓手」「颗粒度」「白名单／黑名单」「升格」「收敛」。本复核件只在第四节引用被审件的原话，正文不用这些词。

## 五、评分与结论

### 总分

**总分 72／100。**

| 维度 | 得分 | 扣分理由（逐条带依据） |
| --- | --- | --- |
| 能力自治度 | 9／20 | `esc`／`totalChanges` 与 `packages/skill-calorie/src/shared/writeParts.ts:29-31／38-41` 逐字节重复（`skill-bill/src/shared/writeParts.ts:41-49`）且文件头不认这份债务 −5；`shared/commandSpec.ts:16`／`record/index.ts:9`／`shared/writeParts.ts:19` 深路径伸手进 `fetch/db.js`／`policy/record.js`（两件 index 已转出同名类型）−3；`shared/docPage.ts`／`shared/copyArea.ts`／`shared/receiptParts.ts` 唯一调用方都在 `record/` 内，按 `structure.md:67` 该留在能力目录 −3 |
| 概念唯一度 | 14／20 | 同一句断言文本两写（`record/write.ts:63` 对 `record/collect.ts:88`）−2；加命令字段名单两处（`write.ts:29` 对 `collect.ts:34-42`）−2；过渡表 14 条形状仍是第二定义地（`envelope.ts:10-25`）；债务标注与双向闸门给它留了分 −2 |
| 改动可预告度 | 16／20 | 逐行对账偏差 0、无 `any`、`--stat` 与清单逐行相符；扣 `record/write.ts:153` 新增 `as unknown as Record<string, unknown>`（`fetch/db.ts:112` 的 `getById` 收 `BillDb` 返 `BillRow`，形状本来说得出）−3；影响清单三行指向工种目录、`shared/` 五行未写第二个能力（证据 `:14-33`）−1 |
| 命名与接口粒度 | 16／20 | 目录与文件名念得出用户的话、无工种名、五处接口 ≤5；扣 `shared/writeParts.ts` 对外 8 个 > 5 −2；`cli/registry.ts:20-31` 在重算别家口径、`:35` 零生产引用 −1；`record/index.ts:14,16` 第二处转出与自建查表（`key` 不一个定义地）−1 |
| 纪律文本符合度 | 17／20 | 命令登记形状一不符、判据 2／3／4 今天跑不了（本包无生成器、无棘轮、无探针）−1；必报五步齐但影响清单判据未过、共用件第二用法有两件不实（证据 `:96-99`）、新建两层目录没记「等用户点头」（`structure.md:88`）−1；用词命中九处（本票新增注释与证据件正文，见第四节）−1 |

### 严重问题（会让地图目的地达不成）—— 1 条

**严重 · `src/cli/registry.ts` 是本纪律点名的共用位，却是人手写、无生成器、无 `pnpm gen:check`、无棘轮，且把「重算各家口径」的逻辑自己写了一遍。** 依据：`registry.ts:8-10` 自认手写、`:15-17` 手列 `SOURCES`、`:20-31` 自建查表与重名抛错；`docs/agents/命令登记纪律.md:43-58` 要求共用位由生成器写、手改无效、`gen:check` 红；`:105-112` 把「往分派层加 `case` 即红」交给棘轮。本票的两条命令确实只声明一处，但**这条不变式今天没有任何机器拦**——下一条命令的施工者可以绕过 `commands.ts` 直接往这张表或 switch 里加一行而不被拦。这不是本票做错了搬迁，而是「搬完 14 条」这个目的地所依赖的那道闸没建。

### 结论：**须整改**（先改这五条，各给最小修法）

1. **共用位只剩一个能力用**：`shared/docPage.ts`／`shared/copyArea.ts`／`shared/receiptParts.ts` 三件移进 `src/record/`（最小修法：只改三件自身的相对 import 与 `record/*.ts` 的 import 路径，`shared/` 只留 `commandSpec.ts` 与 `writeParts.ts`；等第二个能力真用时再移出来，符合 `structure.md:67`）。
2. **两处双写**：把 `record/write.ts:63` 与 `record/collect.ts:88` 的同一句断言文本收成 `collect.ts` 一处导出、`write.ts` 引用它；把 `write.ts:29` 的 `ADD_FIELDS` 与 `collect.ts:34-42` 的字段名单合成一份（最小修法：`RECORD_SLOTS` 增一个 `writesColumn` 标记，`ADD_FIELDS` 由它派生）。
3. **`writeParts.ts` 对外 8 个**：把三个页标识常量（`:70-72`）与 `esc`（`:41-43`）拆成 `shared/pageIds.ts`／并入 `docPage.ts`，使每件 ≤5（最小修法：只动这几行与三处 import）。
4. **类型面**：`record/write.ts:153` 改回 `const pre = getById(db, id);`，`:157` 的比对照 `fetch/db.ts:7-17` 的 `BillRow` 写死字段（最小修法：用 `patch` 的完备键集，别用变长字符串索引）。
5. **用词与告警线**：本票新增注释与证据件里的「底座」（`shared/writeParts.ts:1`、证据 `:15／:69／:86`）、「键」（`record/write.ts:14`）、「落地」（证据 `:101／:258`）按 `wording.md:19／37／51` 改写；在本包 `AGENTS.md` 里写下自己的告警线数字（可按兄弟件 350 行起步，但落点必须在 `packages/skill-bill/AGENTS.md`），并在证据件里补一句「本包告警线未定义、本次借兄弟件口径」。

### 未列入整改但该记账的两条

- `cmd_read.ts:522` 的 `REGISTRY[key] !== undefined` 与 `:151` `runRegistered(): WriteOut` 把「谁在注册表里」当成「谁是写命令」。今天注册表只有两条写命令，成立；读命令进注册表那一票若忘记改这里，会拿 `WriteOut` 的形状去接读命令的产物（`writeOut` 为对象即走 `:528` 分支，`html` 可能缺失）。
- `copyArea.ts:30-31` 的 `MENU_HINTS` 与 `base-paint` 的 `COPY_FORMATS` 顺序对齐只靠一行注释，没有断言；顺序若变，三项提示会对错档。
