# t406 现状 · 16 词页面覆盖表

本件回答一件事：写入域 16 条唤醒词，今天各自的内部流程 HTML 到了什么程度——是每条都有自己的定制页，还是大家共用同一张通用页。

- 唤醒词与 preset 参数源：`packages/skill-bill/src/policy/wakewords.ts` 的 `WAKE_TABLE`。
- 页面装配源：`packages/skill-bill/src/record/`（`commands.ts`／`index.ts`／`write.ts`／`collect.ts`／`receipt.ts`）。
- 老侧对照源：`docs/skills/skill-bill/t406-写入域-老模板逐块清单.md` 第一节、第二节。
- 实测环境：临时库 `D:\ilife\.scratch\t406-cover\db`（`SKILLS_DB_PATH` 指此，`BILL_FORCE_PROD=1`），产物落 `D:\ilife\.scratch\t406-cover\out\`。逐条真跑 32 次（16 条 × 只给唤醒词／补全槽位各一次），另加 1 次基准写入（供改记录／撤销／恢复取 `id`）。产物清单与原始读数在 `.scratch\t406-cover\cover.tsv`。

## 一、代码面：几条命令、几张页

**16 条唤醒词 → 2 条命令。** `WAKE_TABLE` 里这 16 条只指向两个命令名：

| 命令 | 条数 | 唤醒词 | preset 参数 |
| --- | --- | --- | --- |
| `bill.record.add` | 13 | 记支出、记收入、拍账单、批量录入、记退款、记报销、报销到账、记借出、记借入、记收回、记偿还、记分期、记一笔 | 前 12 条把场景写进 `kind`（`expense`／`income`／`photo`／`batch`／`refund`／`reimburse`／`reimburse-done`／`lend`／`borrow`／`collect`／`repay`／`installment`）；`记一笔` 无 preset |
| `bill.record.update` | 3 | 改记录、撤销、恢复 | `撤销` 带 `op: 'undo'`，`恢复` 带 `op: 'restore'`；`改记录` 无 preset，且这三条都带 `needs: ['id']` |

**2 条命令 → 2 个页面装配函数（合计 2 张页）。**

| 页 | 装配函数 | 定义处 | 谁在出它 |
| --- | --- | --- | --- |
| 过程型采集页（1 张） | `recordCollectDoc` | `src/record/collect.ts` | 两条命令缺必需槽位时，都出这一张 |
| 结果型回执页（1 张） | `recordReceiptDoc` | `src/record/receipt.ts` | 两条命令写库成功后，都出这一张 |

回执页的页标题由回执事实的 `op` 派生（`receipt.ts` 的 `PAGE_TITLES`），一共四种：`记一笔 · 回执`／`改记录 · 回执`／`撤销 · 回执`／`恢复 · 回执`。四种标题走的是同一个装配函数、同一套块。

**分岔口径：按「必需槽位齐不齐」分岔，不按 `kind` 分岔。**

- 槽位表 `RECORD_SLOTS`（唯一定义地在 `collect.ts`）：`bill.record.add` 一张（7 槽，`category`／`amount` 必需，`time`／`account`／`ledger`／`currency`／`note` 可缺），`bill.record.update` 一张（9 槽，`id` 必需）。13 条 `record.add` 唤醒词**共用第一张**，没有第二张。
- `writeRecordAdd`（`write.ts:112`）：缺必需槽位 → 采集页；齐全 → 回执页。`kind` 在这条路上只改回执页那一句 summary 的尾缀，共四支：`photo` →「（拍账单图片识别以外置为准）」、`batch` →「（批量逐笔校验其一）」、其余任何 `kind` →「（refund）」这种原文回显、无 `kind` → 空。**它不选页。**
- `writeRecordUpdate`（`write.ts:133`）：缺 `id` → 采集页；有 `id` → 按 `op` 分三支（`undo`／`restore`／`update`）。三支决定的是 summary 文案、明细表两行、写入字段（撤销／恢复写 `deleted_at`），**页面装配函数仍是同一个**。
- 出口分派只有一处：`src/cli/cmd_read.ts:157` 的 `runRecordWrite(key, params, handle)`，没有为任何一条唤醒词开专用分支。

一句话：**16 条唤醒词 ＝ 2 条命令 ＝ 2 个页面装配函数 ＝ 1 张采集页 ＋ 1 张回执页。**

## 二、16 条逐条实测表

「只给唤醒词」＝照该词的 preset 参数直呼它那条命令（模拟用户只说了唤醒词）；「补全槽位」＝按实现补上必需槽位再跑一次，参数写在括注里。产物量级一列是 `采集页 ／ 回执页` 两个文件的 KB 数，退出码一列同序。

| 唤醒词 | preset | 只给唤醒词时出的页 | 补全槽位后出的页 | 产物量级 | 退出码 |
| --- | --- | --- | --- | --- | --- |
| 记支出 | `{"kind":"expense"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=餐饮 amount=-12.5） | 63.0KB ／ 62.1KB | 0 ／ 0 |
| 记收入 | `{"kind":"income"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=工资 amount=8000） | 63.0KB ／ 62.1KB | 0 ／ 0 |
| 拍账单 | `{"kind":"photo"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=餐饮 amount=-30） | 63.0KB ／ 62.2KB | 0 ／ 0 |
| 批量录入 | `{"kind":"batch"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=餐饮 amount=-10） | 63.0KB ／ 62.2KB | 0 ／ 0 |
| 记退款 | `{"kind":"refund"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=退款 amount=20） | 63.0KB ／ 62.1KB | 0 ／ 0 |
| 记报销 | `{"kind":"reimburse"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=出行 amount=-100） | 63.0KB ／ 62.1KB | 0 ／ 0 |
| 报销到账 | `{"kind":"reimburse-done"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=其他收入 amount=100） | 63.0KB ／ 62.2KB | 0 ／ 0 |
| 记借出 | `{"kind":"lend"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=借贷/借出 amount=-500） | 63.0KB ／ 62.1KB | 0 ／ 0 |
| 记借入 | `{"kind":"borrow"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=借贷/借入 amount=500） | 63.0KB ／ 62.1KB | 0 ／ 0 |
| 记收回 | `{"kind":"collect"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=借贷/收回 amount=500） | 63.0KB ／ 62.2KB | 0 ／ 0 |
| 记偿还 | `{"kind":"repay"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=借贷/偿还 amount=-500） | 63.0KB ／ 62.1KB | 0 ／ 0 |
| 记分期 | `{"kind":"installment"}` | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=分期 amount=-1200） | 63.0KB ／ 62.1KB | 0 ／ 0 |
| 记一笔 | （无 preset） | 采集页（补齐槽位） | 回执页（记一笔 · 回执）（补 category=餐饮 amount=-12.5） | 62.9KB ／ 62.0KB | 0 ／ 0 |
| 改记录 | （无 preset，路由要求 `id`） | 采集页（补齐槽位） | 回执页（改记录 · 回执）（补 id=1 note=改过） | 63.0KB ／ 61.0KB | 0 ／ 0 |
| 撤销 | `{"op":"undo"}` | 采集页（补齐槽位） | 回执页（撤销 · 回执）（补 id=1 op=undo） | 63.0KB ／ 61.2KB | 0 ／ 0 |
| 恢复 | `{"op":"restore"}` | 采集页（补齐槽位） | 回执页（恢复 · 回执）（补 id=1 op=restore） | 63.0KB ／ 61.1KB | 0 ／ 0 |

**注 1（只给唤醒词有两层口径，上表取 CLI 层）。** 上表「只给唤醒词时出的页」是照 preset 参数直呼命令名跑出来的。真·只把唤醒词文本交给路由（`routeWakeword(词, {})`）时：13 条 `record.add` 与上表一致，出采集页；`改记录`／`撤销`／`恢复` 三条在**路由层就抛 `POLICY_MISSING_SLOT`**（缺槽位 `id`），出不了页——`WAKE_TABLE` 给这三条写了 `needs: ['id']`。绕开路由、按命令名直呼时，这三条照出采集页（63.0KB，退出码 0）。

**注 2（补全槽位时实测到的一次失败）。** 分类给成不在合法 L1 里的词（例如 `记报销` 给「交通」），退出码 2、不出页，stderr 报「未知 L1 分类：交通」。合法 L1 共 19 个＝支出 10（餐饮／居家／穿着／出行／玩乐／学习／健康／社交／宠物／其他）＋收入 6（工资／奖金／兼职／投资／其他收入／退款）＋特殊 3（借贷／分期／转账）。换成合法分类（`记报销` → 出行）后即退出码 0、出回执页——上表 16 行都是换过之后的读数。

**注 3（量级的大头不是页面内容）。** 62～63KB 里绝大部分是整页内联的样式与脚本壳，页面自身那几块内容不到 1KB；因此 16 条的产物量级几乎一样，只差在复制区里回显的参数串长短。

**注 4（页里出现的特征块，16 条一样）。** 采集页：状态徽标（`ilife-status-badge-warn`「待补槽位 · 未写库」）＋三格状态卡（待补槽位／已给槽位／写库）＋缺槽位明示表（`ilife-block-data-table`）＋采集表单（`ilife-block-param-form`，7 槽或 9 槽）＋复制 prompt 区（`ilife-block-pre-block`）＋复制区（`ilife-copy-menu`）。回执页：状态徽标（`ilife-status-badge-ok`「写库成功」）＋三格状态卡（状态／影响行数／写入字段）＋写入明细表（两列「项／值」）＋页尾对账折叠区（`ilife-block-disclosure`）＋复制区。两类页的类名一律 `ilife-*`。

## 三、场景定制块的有无

老侧依据逐字取 `docs/skills/skill-bill/t406-写入域-老模板逐块清单.md` 第一节、第二节。证据一列给产物文件名与命中的字符串；写「未命中」即该块的名字或类名在产物里一次都没出现。

| 唤醒词 | 该场景应有的特征块（老侧依据） | 实测有或无 | 证据（产物文件名 ＋ 命中的字符串或「未命中」） |
| --- | --- | --- | --- |
| 记支出 | 老侧 `expense_form.html` 字段卡：金额／分类／账户／账本／时间／备注／币种 ＋ 分类选择器 `smartSelect` | 有字段卡、无选择器 | `w1-jizhichu__wake.html` 命中 `ilife-block-param-form` 表单（7 个字段：category／amount／time／account／ledger／currency／note）；`smartSelect` 未命中 |
| 记收入 | 同 记支出（老侧这两条同一张页） | 有字段卡、无选择器 | `w2-jishouru__wake.html` 同上；`smartSelect` 未命中 |
| 拍账单 | 图片识别入口：老侧 `expense_form.html` 的 `#prefillBox` ＋「🖼 已收到 N 张账单图片」预填条 | 无 | `w3-paizhangdan__wake.html`／`w3-paizhangdan__full.html`：`prefillBox`、`已收到` 均未命中；`kind=photo` 的唯一痕迹是回执页 summary 尾缀「（拍账单图片识别以外置为准）」一句，不是块 |
| 批量录入 | 逐行可编辑表：老侧 `batch_confirm.html` 的 `table`／`th`／`.row-num`，逐行输入、编辑即重算、底部合计 | 无 | `w4-piliangluru__full.html`：`row-num` 未命中；页面只有通用回执那五块。`逐行` 全仓产物里唯一一处命中落在内联脚本注释（`copyArea` 的口径注释），不是块；`kind=batch` 的痕迹只有 summary 尾缀「（批量逐笔校验其一）」 |
| 记退款 | 候选单选与流程三段式：老侧 `flow_confirm.html` 的候选卡 `#candCard`／`class="cand"`／`name="cand"`／`未找到原记录`、操作预览卡「将执行以下操作」、超支警示 `#warnBox`、字段卡 `fAmount`／`fReason`、chips「单步确认」「填写确认」 | 无 | `w5-jituikuan__wake.html`／`w5-jituikuan__full.html`：`class="cand"`、`name="cand"`、`未找到原记录`、`将执行以下操作`、`warnBox`、`单步确认`、`填写确认`、`退款原因`、`id="fReason"`、`id="fAmount"` **全部未命中**；采集页表单那 7 个字段名是 `category/amount/time/account/ledger/currency/note`，没有 `fReason` |
| 记报销 | 同 记退款（老侧 `flow_confirm.html` 的 `reimburse` 型） | 无 | `w6-jibaoxiao__full.html`：上列标识全部未命中；`kind=reimburse` 的痕迹只有 summary 尾缀「（reimburse）」 |
| 报销到账 | 同 记退款（老侧 `flow_confirm.html` 的 `reimburse_done` 型） | 无 | `w7-baoxiaodaozhang__full.html`：上列标识全部未命中；只有 summary 尾缀「（reimburse-done）」 |
| 记借出 | 同 记退款 ＋ 期限字段 `#fDeadline`（老侧借贷两型动态追加） | 无 | `w8-jijiechu__full.html`：`id="fDeadline"` 等上列标识全部未命中；只有 summary 尾缀「（lend）」。分类口径侧另有支持（`借贷` 是合法 L1），但页面上没有对应块 |
| 记借入 | 同 记借出 | 无 | `w9-jijieru__full.html`：上列标识全部未命中；只有 summary 尾缀「（borrow）」 |
| 记收回 | 同 记退款 | 无 | `w10-jishouhui__full.html`：上列标识全部未命中；只有 summary 尾缀「（collect）」 |
| 记偿还 | 同 记借出 | 无 | `w11-jichanghuan__full.html`：上列标识全部未命中；只有 summary 尾缀「（repay）」 |
| 记分期 | 分摊预览：老侧 `installment_confirm.html` 的「分摊预览」标题 ＋ `#countHint` ＋ 期数表（期数／日期／金额）＋ `fold-hint` 折叠 | 无 | `w12-jifenqi__wake.html`／`w12-jifenqi__full.html`：`分摊预览`、`countHint`、`fold-hint`、`期数` **全部未命中**；`installment` 只出现在采集页复制 prompt 的命令行里（回显参数）与回执页 summary 尾缀「（installment）」 |
| 记一笔 | 老侧 `expense_form.html` 字段卡（通用款，同 记支出） | 有字段卡、无选择器 | `w13-jiyibi__wake.html`／`w13-jiyibi__full.html`：采集页 7 槽表单在；`smartSelect` 未命中；它是唯一一条回执 summary 不带场景尾缀的（无 `kind`） |
| 改记录 | diff 表：老侧 `update_confirm.html` 的「字段／原值／新值」三列 ＋ `#diff`、原记录块 `#orig`、`.old`／`.new` | 无 | `w14-gaijilu__full.html`：`id="diff"`、`原值`、`新值`、`class="old"`、`class="new"`、`id="orig"` **全部未命中**；页面是通用回执，明细表两列叫「项／值」，本次两行是 `id=1`、`note=改过` |
| 撤销 | 老侧 `receipt.html` 的「↩ 撤销这笔」入口（老侧无撤销专用模板） | 无专用块 | `w15-chexiao__full.html`：只有通用回执页，标题「撤销 · 回执」，明细表两行 `op=undo（软删）`、`id=1`；老侧那个撤销入口的名字未命中 |
| 恢复 | 老侧无对应模板 | 无专用块 | `w16-huifu__full.html`：只有通用回执页，标题「恢复 · 回执」，明细表两行 `op=restore`、`id=1` |

**全量检索结论。** 在 32 个产物里对老侧的类名与标识统一检索——`class="cand"`、`name="cand"`、`smartSelect`、`id="diff"`、`class="old"`、`class="new"`、`id="orig"`、`prefillBox`、`countHint`、`fold-hint`、`row-num`、`class="chip"`、`class="op"`、`warnBox`——**零命中**。

**16 条唤醒词在产物里能看到的场景差异，只有一处。** 回执页 summary 的那句尾缀：`photo` 与 `batch` 各一句中文（「拍账单图片识别以外置为准」「批量逐笔校验其一」），其余 10 条 `kind` 把原文回显进用户可见的句子里（「（refund）」「（lend）」「（installment）」等）。这是一句话，不是块；采集页这一侧连这句话都没有。

## 四、结论

16 条中拿到场景定制页的有 **0** 条，只有通用页的有 **16** 条。

未定制的这 16 条今天都跑得通、都有页可看：只给唤醒词时 13 条 `record.add` 出采集页（63.0KB、退出码 0），`改记录`／`撤销`／`恢复` 这 3 条按命令名直呼同样出采集页（63.0KB、退出码 0，真·只给唤醒词文本时它们会先在路由层被缺 `id` 拦下），补全槽位后 16 条都出回执页（61.0～62.2KB、退出码 0）；唯一实测到的不出页情形是补槽位时给了不在 19 个合法 L1 内的分类（退出码 2、stderr 报「未知 L1 分类」）。
