# t417 墙收口 · 官方口径三样齐（#417 收口票）

> 口径：`docs/agents/视觉验收墙.md`（§3 七步／§4 判据／§5 落哪）。本件是第 ④ 条的收口证据；
> 机检判据（第 ① 条）见各票，`t417-query-lock` 19 条＋全量 280/280（`e968072` 后复绿）。

## 一 产物册子（18 格，发布名即清单 `rows[].file`）

产物目录 `.scratch/t417-wall/`（墙页与产物同目录）；清单复用已提交的
`docs/skills/skill-bill/t403-manifest.json`（18 行：w01–w17 十七词＋w00 空态；`notShipped` 3 条：阻断分支有意不出静态产物）。
生成器用已提交的 `docs/skills/skill-bill/t403-验收墙.mjs`（本席未另造）。

## 二 生成器自检（验收命令）

正例：`node docs/skills/skill-bill/t403-验收墙.mjs .scratch/t417-wall`
→ `18格；链接94条（手机墙-390.html 37 ＋ 桌面墙-1280.html 37 ＋ 总索引.html 20）；缺失0 -> 可发`，**exit 0**。
反例：清单首行改不存在名 → `17格；…缺失1 -> 不可发`＋点名`1 查今天 -> 不存在-反例.html`，**exit 1**；改回重跑回绿 exit 0。

## 三 机审读数（`docs/skills/skill-bill/t417-query-audit.mjs`，照抄 t407 只换名单）

`node docs/skills/skill-bill/t417-query-audit.mjs --dir .scratch/t417-wall` → **exit 0，全绿**：
设计位 `·`／`|` **0 处**；英文裸词 0；重复句 0；缺件 0；
标题写法位（眉头，允许保留）18 处；`；`／`;` 0 处；
本页样式块 0、内联 style 0、色值 0；共享样式表 18/18 同一份（820／640 断点、44px、flex-wrap、auto-fit 全有）。

## 四 人看（双墙＋抽渲染，逐格缺陷清单）

- 手机墙（390×3列）首行 w01–w03 栅格化正常；其余行截图空白是 headless 限制（仓规§7第二坑），以后三条补足：
  机审逐文件全文读数（上节，全绿）＋单页实拍（下）。
- 单页实拍：w08 桌面（P1 表铺满版心、7 列无折行）／w17 桌面＋手机（P2 定义列表、无标签重复）
  ／w00 空态桌面／w07 手机密集 8 行（2×2 KPI、行卡标签左值右、无溢出、按钮可点）。

| 格 | 症状 | 改没改 |
|---|---|---|
| D1 桌面表窄（18 格，旧） | 表卡 680 居中、右 40% 空、密表折行 | **已改**（他席 `e968072`：docPage 桌面档表与内容列同宽；base 680 原样不动） |
| D2 手机详情冗余（w17，旧） | 字段／值逐行重复表头 | **已改**（他席 `e968072`：窄屏替 `dl` 键值列表，桌面仍是表） |
| 其余 16 格 | 本轮实拍＋机审未见新缺陷 | 不改 |

重打分（他席量表）：列表桌面 96／列表手机 94／详情桌面 95／详情手机 94／空态 95——
**最低格 94 ≥ 90 → 过**。

## 五 三样齐

1. 墙：`.scratch/t417-wall/手机墙-390.html`＋`桌面墙-1280.html`＋`总索引.html`（可重出：种子脚本 `.scratch/t417-wall/seed-wall.ps1`）。
2. 逐格缺陷清单：上节（D1／D2 已改闭环，其余无缺陷）。
3. 自检退出码：正例 0、反例 1（点名）、改回 0（上节）。

## 六 签字位（负责人）

- [ ] 人用浏览器滚过两张墙（双击即开，不起服务）
- [ ] 签字收口 → 关 #417、地图 #403 推 100%
