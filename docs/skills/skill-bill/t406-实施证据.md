# t406 实施证据 · 饼干记账写入域骨架

本票范围：`packages\skill-bill\`（新增 `src\record\`、`src\shared\`、`src\cli\registry.ts`；改动 `src\cli\cmd_read.ts`、`src\render\envelope.ts`、`test\` 两件）＋ 本文件。
基线（不重复跑，按上级窗口纠偏第 1 条）：开工前 `node --test "packages/skill-bill/test/*.test.mjs"` ＝ **57 通过／0 失败**，`git status --short packages/skill-bill` ＝ **0 条未提交改动**；出处 `docs\skills\skill-bill\t406-验收-基线实测.md`（第 16–31 行，原文读数逐字为 `ℹ tests 57`／`ℹ suites 11`／`ℹ pass 57`／`ℹ fail 0`）。

## 一、影响清单

（结构纪律五步第 1 步。行数口径＝LF 行数，`readFileSync(f,'utf8').split('\n').length - 1`，只数 `\n`。）

### 1. 新增

| # | 路径 | LF | 一句话与理由 |
| --- | --- | --- | --- |
| 1 | `packages\skill-bill\src\shared\commandSpec.ts` | 45 | 命令声明的形状（种类／命令名／形状／标题／代表唤醒词／示例／处理函数）的**唯一定义地**；声明住能力目录是本仓规矩，`t406-共用件依赖与提升改造清单.md` 第三节第 1 条判它「不上移公共层」，故饼干自持一份 |
| 2 | `packages\skill-bill\src\shared\writeParts.ts` | 72 | 写命令的回执底座：回执事实形状 `BillReceipt`／影响行数口径 `totalChanges`／页壳 `writeSection`／命令原文 `commandLine`／三字符转义 `esc`／页面公共标识三常量 |
| 3 | `packages\skill-bill\src\shared\receiptParts.ts` | 44 | 两块页内共用件：状态卡（回执页「已改动／无改动」、采集页「待补槽位」）＋页尾「对账信息」折叠区 |
| 4 | `packages\skill-bill\src\shared\docPage.ts` | 53 | 整页装配（区块 HTML ＋ 标题三件套 → 完整文档）；`blocksCss()` 拼进 `sharedCssText` 的硬口径收在这里一处 |
| 5 | `packages\skill-bill\src\shared\copyArea.ts` | 115 | 复制区接线：复制 prompt 区／复制数据／复制日志；只接 base 现成组件，不自造按钮与提示通道 |
| 6 | `packages\skill-bill\src\record\commands.ts` | 34 | 两条命令的声明（权威源）：`bill.record.add` 记一笔、`bill.record.update` 改记录 |
| 7 | `packages\skill-bill\src\record\index.ts` | 23 | 能力门（对外恰 2 个名字）：`RECORD_COMMANDS` ＋ 写入口 `runRecordWrite`（非本域命令即抛） |
| 8 | `packages\skill-bill\src\record\write.ts` | 164 | 两条命令的处理体，**从 `cli\cmd_read.ts` 归位到这里**；必需槽位缺失改出采集页，齐全才写库出回执页 |
| 9 | `packages\skill-bill\src\record\collect.ts` | 134 | 过程型采集页装配＋槽位表（要哪些槽位／哪个必需／表单怎么摆）；只采集、不写库 |
| 10 | `packages\skill-bill\src\record\receipt.ts` | 96 | 结果型回执整页装配（两条命令各出一页；页标题由回执事实的 `op` 派生，撤销／恢复各一页形态） |
| 11 | `packages\skill-bill\src\cli\registry.ts` | 35 | 命令索引（一能力一行）：把能力声明汇总成一张查表，供出口与形状派生查；同一个命令名两人声明即抛 |
| 12 | `packages\skill-bill\test\record-write.test.mjs` | 207 | 本票新增断言：声明六件事／唤醒词在表内／示例照抄即能跑／注册表派生／入口不猜／槽位探针／两张整页的真跑 |
| 13 | `docs\skills\skill-bill\t406-实施证据.md` | 本件 | 本证据件 |

### 2. 改动

| # | 路径 | HEAD LF → 现在 LF | 改了什么与理由 |
| --- | --- | --- | --- |
| 14 | `packages\skill-bill\src\cli\cmd_read.ts` | 558 → 556（+26／−28） | 删掉 `bill.record.add`／`bill.record.update` 两个 `case`（一个命令恰住一处）；新增 `runRegistered`（查注册表 → 能力目录，含写前置守卫与开库关库）＋ 出口主路先查注册表、迁移过的命令自带整页不再套老模板；顺带清掉迁走后再没人用的五个 import |
| 15 | `packages\skill-bill\src\render\envelope.ts` | 49 → 68（+22／−3） | 16 命令的形状拆成两段：14 条未迁移的留在**过渡表**（文件头注明是债务、待生成器链那票替换），迁移过的两条**从注册表运行期派生**（形状事实只住 `commands.ts`） |
| 16 | `packages\skill-bill\test\cli.test.mjs` | 126 → 130（+5／−1） | 就地摆正与新规矩冲突的一行：缺必需槽位（缺 `id`）原来断言 `exit 2`，现在断言 `exit 0` ＋ 载荷 `ok:false` ＋ 缺槽位明示（采集页入口是后票级的新规矩，旧断言必须跟着改） |

### 3. 有意未动（与「不许动」表逐条对应）

| 没动的 | 正面做了什么 |
| --- | --- |
| `packages\base-render\`（`base-paint`） | 只调用它现成组件：`blocksCss`／`renderPageShell`／`renderKpiGrid`（内部逐格走 `renderKpiCard`，`blocks.ts:278`／`:305`）／`renderDataTable`／`renderDisclosure`／`renderParamForm`／`renderPreBlock`／`renderCopyBlock`／`renderEmptyBlock`／`renderStatusBadge`／`fillTemplate`／`buildStyleSheet`／`buildSharedHelpersJs`／`buildDataText`／`buildLogText`；没加一个组件，缺的记在「交棒」一节 |
| `packages\skill-calorie\` 与其它 `skill-*` | 只读它的形状当参照（照结构不照文件）：`shared\commandSpec.ts`、`diet\index.ts`、`shared\{writeParts,receiptParts,docPage,copyArea}.ts`、`cli\registry.ts`；一个字节没改 |
| 仓根、`tooling\`、CI、`package.json` | 没碰；本票不需要新脚本（真跑驱动放 `.scratch\`，不入仓） |
| `packages\skill-bill\templates\` 既有 16 张模板 | 一张没动；新页面由 TS 装配（迁移过的两条命令自带整页，不再套老模板），其余 14 条命令照旧套模板 |
| 老技能目录 `D:\2Study\StudyNotes\SKILLS\饼干记账` | 只读；真跑与测试一律用 `.scratch\t406-impl\db` 与系统临时目录 |
| 本包 `AGENTS.md`、告警线 | 未建、未动；超线情况见第五节 |
| `packages\skill-bill\SKILL.md` | **有意未动**，理由：它的 `<!-- HELP-AUTO-START/END -->` 块由 `scripts\build-help.mjs` 从 HELP 内容资产生成，**不由注册表派生**（`test\skill.test.mjs:45-47` 逐字钉死该块与 `buildHelpBlock()` 相等）；本票没有改动 HELP 数据，块内容一字不变；生成器链接注册表那一票再动它（`pnpm gen`／`pnpm gen:check` 明确不在本票范围） |

## 二、结构设计

（结构纪律五步第 2 步。）

### 1. 新目录树

```
packages/skill-bill/src/
├── cli/
│   ├── cmd_read.ts        （改）出口：主路先查注册表，命中走能力目录；其余照旧 switch
│   └── registry.ts        （新）命令索引：RECORD_COMMANDS → REGISTRY／REGISTRY_KEYS
├── record/                （新）记账写入域能力目录
│   ├── commands.ts        命令事实的权威源（本票两条写命令）
│   ├── index.ts           能力门（对外 2 个名字）
│   ├── write.ts           两条命令的处理体（校验走 policy、取数写库走 fetch）
│   ├── collect.ts         过程型采集页 ＋ 槽位表
│   └── receipt.ts         结果型回执整页
├── render/
│   └── envelope.ts        （改）形状表＝过渡表 ＋ 注册表派生
└── shared/                （新）饼干级共用位（不是公共层，别包不许用）
    ├── commandSpec.ts     声明形状
    ├── writeParts.ts      回执底座
    ├── receiptParts.ts    状态卡＋对账折叠区
    ├── docPage.ts         整页装配
    └── copyArea.ts        复制区接线
```

### 2. 每个文件的职责与对外接口

| 文件 | 对外接口 | 职责（不做的事） |
| --- | --- | --- |
| `record\commands.ts` | `RECORD_COMMANDS`（`readonly CommandSpec[]`，恰 2 条） | 只写命令事实；不写处理逻辑、不写页面 |
| `record\index.ts` | `RECORD_COMMANDS`、`runRecordWrite(key, params, db)`（恰 2 个名字，≤5） | 只做门与「非本域即抛」；不装配页面 |
| `record\write.ts` | `writeRecordAdd`、`writeRecordUpdate` | 编排：缺槽位探针 → 采集页；齐全 → 校验（policy）＋写库（fetch）＋回执事实 → 回执页。不自己拼 HTML、不自己解析参数真值 |
| `record\collect.ts` | `RECORD_SLOTS`、`missingSlots`、`recordCollectDoc` | 槽位事实的唯一住处 ＋ 采集页装配；只做「在不在」探针，不做真值校验 |
| `record\receipt.ts` | `recordReceiptDoc` | 回执整页装配；页标题由 `op` 派生 |
| `cli\registry.ts` | `REGISTRY`、`REGISTRY_KEYS` | 汇总声明；同命令名两人声明即抛。手写件，生成器链接手后改生成物 |
| `shared\commandSpec.ts` | `WriteOut`、`WriteHandler`、`WriteCommandSpec`、`CommandSpec` | 只定义形状，不产 HTML。本票只有写命令这一支，读命令那支不先写空壳 |
| `shared\writeParts.ts` | `BillReceipt`、`RECEIPT_FORMAT`、`esc`、`totalChanges`、`commandLine`、`writeSection`、`DOC_VERSION`／`DOC_SKILL`／`DOC_TITLE` | 回执底座五件；不取时钟（时刻由写体给）、不取库文件名以外的领域事实 |
| `shared\receiptParts.ts` | `statusCard`、`receiptStatusCard`、`reconcileDisclosure` | 两块页内件；写入去向那句说明**必填**，不给默认值 |
| `shared\docPage.ts` | `assembleDocPage` | 整页包裹；零包内依赖（只 import `base-paint`） |
| `shared\copyArea.ts` | `promptCopyArea`、`copyArea`、`copyLog` | 复制区接线；数据位恒出三格式菜单，走 `buildDataText` |

### 3. 共用件被哪两个用法用（结构纪律要求写得出第二个用法）

| 共用件 | 用法① | 用法② |
| --- | --- | --- |
| `commandSpec.ts` | `record\commands.ts`（声明） | `cli\registry.ts`（汇总成查表） |
| `writeParts.ts` | `record\write.ts`（影响行数／回执事实／命令原文） | `record\receipt.ts` 与 `record\collect.ts`（页壳 `writeSection`；复制区的命令原文 `commandLine`） |
| `receiptParts.ts` | `record\receipt.ts`（`receiptStatusCard` ＋ `reconcileDisclosure`） | `record\collect.ts`（`statusCard` 出「待补槽位」那一格） |
| `docPage.ts` | `record\receipt.ts`（回执整页） | `record\collect.ts`（采集页） |
| `copyArea.ts` | `record\collect.ts`（`promptCopyArea` ＋ `copyArea`） | `record\receipt.ts`（`copyArea` ＋ `copyLog`） |

### 4. 本票立下的四条口径（都在代码里落地，也都有断言）

1. **采集页入口**：必需槽位缺失**不再报参数错退出**，改出过程型采集页并落盘（`--html` 给落点）、退出码 0、载荷 `ok:false`、不写库；齐全照旧写库出结果型回执整页。必备槽位住 `collect.ts` 的 `RECORD_SLOTS`（记一笔＝category＋amount，改记录＝id），真值校验仍走 `policy`。
2. **形状事实只住一处**：`BILL_KEY_SHAPES` 的迁移段从注册表运行期派生；未迁移的 14 条留一张过渡表并在文件头注明是债务。
3. **一个命令恰住一处**：`cmd_read.ts` 的 switch 里不再有这两条命令；出口命中注册表即走能力目录（含写前置守卫与开库关库）。
4. **两种转义不许互换**：新共用件的 `esc` 只转三字符（`&`／`<`／`>`），公共层 `escapeHtml` 与 `render\html.ts` 的那份转五字符；`writeSection` 只对命令名用 `esc`。

## 三、真跑读数

### 1. 环境口径与一个坑（原始读数）

- `SKILLS_DB_PATH` 是**目录**、逐字使用，本票一律绝对路径：`D:\ilife\.scratch\t406-impl\db`；`BILL_FORCE_PROD=1`（`.scratch` 不在系统临时目录下）。
- **PowerShell 5.1（实测 `$PSVersionTable.PSVersion` ＝ 5.1.26100.9168）会吃掉 `--params` 里的半角双引号**。四条探针读数（`node .scratch\t406-impl\probe.js --params <形态>`，探针脚本打印 `process.argv.slice(2)`）：

  | 形态 | node 实收 |
  | --- | --- |
  | 内联 `'{\"a\":\"b\"}'` | `["--params","{\"a\":\"b\"}"]`（活下来了） |
  | 内联 `'{"a":"b"}'` | `["--params","{a:b}"]`（引号没了） |
  | 内联 `'{"a":"b c"}'`（带空格） | `["--params","{\"a\":\"b","c\"}"]`（**被拆成两个 argv**） |

  结论：本机 PS 5.1 下，**任何含空格的 JSON 参数都传不完整**（真跑判据里的 `"time":"2026-09-14 12:00:00"` 正好含空格）。故真跑由 node 直传 argv：驱动件 `.scratch\t406-impl\run.mjs`（`spawnSync(process.execPath, [CLI, ...argv])`），命令原文逐条落在 `.scratch\t406-impl\<tag>.cmd.txt`。这是本机环境限制，不是出口的行为差异。

### 2. 判据 1（两条写命令退出码 0 ＋ 合法 envelope JSON）

`node .scratch\t406-impl\run.mjs`（退出码 0；判读全文 `.scratch\t406-impl\verdict.txt`，末尾 `=== 全绿（failures=0）===`）：

| 跑了什么命令 | 退出码 | 原始输出落点 | 一句话结论 |
| --- | --- | --- | --- |
| `bill.record.add --params '{"category":"餐饮","amount":-12.5,"time":"2026-09-14 12:00:00","account":"支付宝","ledger":"生活"}' --html D:\ilife\.scratch\t406-impl\add.html` | **0** | `.scratch\t406-impl\add.out.txt`（envelope 单行）／`.scratch\t406-impl\add.err.txt`（`BILL_FORCE_PROD` ＋「DB 已初始化」）／`.scratch\t406-impl\add.envelope.pretty.txt` | 写库成功：`shape=receipt`、`data.ok=true`、`data.receipt={op:"add",recordId:1,affectedRows:1,writtenFields:[category…note]}`、`delivery{mode:"file",path:"D:\ilife\.scratch\t406-impl\add.html",bytes:61520}` |
| `bill.record.update --params '{"id":1,"note":"改过"}' --html D:\ilife\.scratch\t406-impl\update.html` | **0** | `.scratch\t406-impl\update.out.txt`／`update.err.txt`／`update.envelope.pretty.txt` | 改库成功：`data.ok=true`、`data.receipt={op:"update",recordId:1,affectedRows:1,writtenFields:["note"]}`、`bytes=60384` |
| `bill.record.add --params '{"kind":"expense"}' --html D:\ilife\.scratch\t406-impl\form.html` | **0** | `.scratch\t406-impl\form.out.txt`／`form.err.txt`／`form.envelope.pretty.txt` | 缺必需槽位出采集页：`data.ok=false`、`data.message="缺必需槽位：category、amount（已出采集页，补齐后重跑同一条命令）"`、`bytes=62384` |
| 另跑两条（同一条改记录命令的另外两支）：`{"op":"undo","id":1}`／`{"op":"restore","id":1}` | **0**／**0** | `.scratch\t406-impl\undo.out.txt`／`restore.out.txt` | 撤销＝软删、恢复＝撤回撤销，各出一页 |
| 另跑：`bill.record.update --params '{"note":"改过"}'`（缺 id） | **0** | `.scratch\t406-impl\form-update.out.txt` | 改记录也走采集页入口 |

stdout 合法性：每次运行的最后一行都能 `JSON.parse` 成五字段 envelope（`version`／`skill`／`shape`／`key`／`data`），判读脚本逐条 `PASS`。

### 3. 判据 2／3（产物存在、可打开、含标记；采集页含缺槽位明示与复制 prompt 区）

| 产物（绝对路径） | 字节 | `<!doctype html>` | `<section` | 复制区标记 | 其它已核标记 |
| --- | --- | --- | --- | --- | --- |
| `D:\ilife\.scratch\t406-impl\add.html` | 61520 | 是 | 是 | 是（`ilife-copy-btn`／`ilife-copy-menu`／`ilife-copy-log`） | `data-slot="ilife:bill:receipt"`、`data-key="bill.record.add"`、`已改动`、`影响行数`、`写入字段`、`对账信息` |
| `D:\ilife\.scratch\t406-impl\update.html` | 60384 | 是 | 是 | 是 | `改记录 · 回执`、`已改动`、`影响行数` |
| `D:\ilife\.scratch\t406-impl\undo.html` | 60533 | 是 | 是 | 是 | `软删` |
| `D:\ilife\.scratch\t406-impl\restore.html` | 60396 | 是 | 是 | 是 | `写库成功` |
| `D:\ilife\.scratch\t406-impl\form.html`（采集页） | 62384 | 是 | 是 | 是 | `data-slot="ilife:bill:collect"`、`缺必需槽位：category、amount`、`待补槽位`、`未发生`、**`复制 prompt`**、`ilife-block-pre-block`、`ilife-block-param-form`、`复制日志` |
| `D:\ilife\.scratch\t406-impl\form-update.html`（采集页） | 62426 | 是 | 是 | 是 | `缺必需槽位：id`、`复制 prompt`、`ilife-block-param-form` |

量级判据：老路径的同名产物实测 1011／988／994／962 字节（`t406-验收-真跑配方与判据.md` 第三节），本票六份全是 **60–62 KB**，即走的是新装配（`buildStyleSheet().css + blocksCss()` 内联 ＋ `buildSharedHelpersJs()`），远在上限 256 KB 体积门之下。

### 4. 判据 4（单元测试）

| 跑了什么命令 | 退出码 | 原始输出落点 | 读数 |
| --- | --- | --- | --- |
| `node --test "packages/skill-bill/test/*.test.mjs"`（改完、变异测试之前） | 0 | `.scratch\t406-impl\test.txt` | `ℹ tests 69`／`ℹ suites 14`／`ℹ pass 69`／`ℹ fail 0`（基线 57 通过／0 失败 ＋ 本票新增 12 条） |
| `npx tsc -b packages/skill-bill` | 0 | `.scratch\t406-impl\tsc.txt`（空文件＝零诊断） | 零错误零警告 |

### 5. 判据 5

`pnpm gen`／`pnpm gen:check` **没跑**（不在本票范围，生成器链是后票）。

## 四、变异自证

（结构纪律五步第 3 步的验收核心：新断言必须真的管着那处逻辑。）

**改坏的那一处**：`packages\skill-bill\src\record\write.ts` `writeRecordAdd` 里的采集页分岔 `if (missing.length > 0)` → `if (missing.length > 99)`（等价于「永不进采集页」，其余一字不动）。

### 1. 改坏必红（原样读数）

```
cd D:\ilife
npx tsc -b packages/skill-bill                    # TSC_EXIT=0
node --test "packages/skill-bill/test/record-write.test.mjs"   # TEST_EXIT=1
```

原始输出落点 `.scratch\t406-impl\mut-red.txt`，末尾 25 行原文（逐字，含行号被略去）：

```
✖ failing tests:

test at packages\skill-bill\test\record-write.test.mjs:132:3
✖ 必需槽位缺失 → 采集页（不写库），退出码 0，含缺槽位明示与复制 prompt 区 (196.3229ms)
  AssertionError [ERR_ASSERTION]: 缺槽位不再报参数错退出：ERR 2: 口径失败：category 不能为空


  2 !== 0

      at TestContext.<anonymous> (file:///D:/ilife/packages/skill-bill/test/record-write.test.mjs:136:12)
      at Test.runInAsyncScope (node:async_hooks:227:14)
      at Test.run (node:internal/test_runner/test:1382:25)
      at Suite.processPendingSubtests (node:internal/test_runner/test:960:18)
      at Test.postRun (node:internal/test_runner/test:1522:19)
      at Test.run (node:internal/test_runner/test:1447:19)
      at async Promise.all (index 0)
      at async Suite.run (node:internal/test_runner/test:1869:7)
      at async Test.processPendingSubtests (node:internal/test_runner/test:960:7) {
    generatedMessage: false,
    code: 'ERR_ASSERTION',
    actual: 2,
    expected: 0,
    operator: 'strictEqual',
    diff: 'simple'
  }
```

同文件内的汇总行（node 把汇总打在「✖ failing tests」明细之前，逐字）：

```
ℹ tests 12
ℹ suites 3
ℹ pass 11
ℹ fail 1
```

只有这一条转红、红点正落在采集页入口上；两条写命令的正路（写库＋回执整页）仍绿——说明这条断言真的钉的是那处逻辑，不是泛泛的冒烟。

### 2. 还原一致

把那一行改回 `if (missing.length > 0)`（反向替换，一字不差），重建重跑：

```
npx tsc -b packages/skill-bill                                  # TSC_EXIT=0
node --test "packages/skill-bill/test/*.test.mjs"               # TEST_EXIT=0
```

原始输出落点 `.scratch\t406-impl\mut-green.txt`，末尾 9 行原文（逐字）：

```
✔ #146 饼干记账 HELP 内容资产 (4.6588ms)
ℹ tests 69
ℹ suites 14
ℹ pass 69
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 5738.7192
```

## 五、超线报警

（结构纪律五步第 4 步。口径：兄弟件 350 行、LF 行数、只数 `\n`；范围 `src/**/*.ts`，`test/*.mjs` 不在口径内但仍报数。）

**一件超线，且是既有超线件、本票没让它更坏**：

| 件 | HEAD LF | 现在 LF | 结论 |
| --- | --- | --- | --- |
| `packages\skill-bill\src\cli\cmd_read.ts` | 558 | 556 | **已超线，需要根据规则进行重构。** 超因：出口一个文件同时背「参数解析＋时区/日期助手＋HELP 分派＋16 路业务 switch＋交付编排」五件事，是长期欠账（HEAD 已 558 行，本票把两条命令迁走后净 −2 行，没加重）。本次先不拆：拆法要用「把 16 路 switch 按域分成 `cli\dispatch-*.ts`」的思路，而每个 `case` 都在往能力目录搬的路上——搬完再拆才不返工，那属于「16 条命令全迁进能力目录」的收尾票 |

其余本票新增／改动件全部在 350 以内（最大的是 `test\record-write.test.mjs` 207 行，测试文件不在告警口径内）：`src\shared\copyArea.ts` 115、`src\record\write.ts` 164、`src\record\collect.ts` 134、`src\record\receipt.ts` 96、`src\render\envelope.ts` 68、其余 ≤ 72。

## 六、交付对账

（结构纪律五步第 5 步：逐行与第一节影响清单对，偏差必须为零。）

| # | 影响清单里的那一行 | 实际落地（`git status`／`git diff --numstat` 读数） | 偏差 |
| --- | --- | --- | --- |
| 1 | 新增 `src\shared\commandSpec.ts` 45 LF | 存在，45 LF | 0 |
| 2 | 新增 `src\shared\writeParts.ts` 72 LF | 存在，72 LF | 0 |
| 3 | 新增 `src\shared\receiptParts.ts` 44 LF | 存在，44 LF | 0 |
| 4 | 新增 `src\shared\docPage.ts` 53 LF | 存在，53 LF | 0 |
| 5 | 新增 `src\shared\copyArea.ts` 115 LF | 存在，115 LF | 0 |
| 6 | 新增 `src\record\commands.ts` 34 LF | 存在，34 LF | 0 |
| 7 | 新增 `src\record\index.ts` 23 LF | 存在，23 LF | 0 |
| 8 | 新增 `src\record\write.ts` 164 LF | 存在，164 LF | 0 |
| 9 | 新增 `src\record\collect.ts` 134 LF | 存在，134 LF | 0 |
| 10 | 新增 `src\record\receipt.ts` 96 LF | 存在，96 LF | 0 |
| 11 | 新增 `src\cli\registry.ts` 35 LF | 存在，35 LF | 0 |
| 12 | 新增 `test\record-write.test.mjs` 207 LF | 存在，207 LF | 0 |
| 13 | 新增 `docs\skills\skill-bill\t406-实施证据.md` | 本件 | 0 |
| 14 | 改 `src\cli\cmd_read.ts` | `M`，`+26/−28`（558 → 556） | 0 |
| 15 | 改 `src\render\envelope.ts` | `M`，`+22/−3`（49 → 68） | 0 |
| 16 | 改 `test\cli.test.mjs` | `M`，`+5/−1`（126 → 130） | 0 |
| — | 计划外新增／改动 | **无**（`git status --short packages/skill-bill` 只有上表 4 增 3 改；`.scratch\t406-impl\` 下的真跑驱动与产物是临时件，不入仓） | 0 |

**偏差合计：0。**

## 交棒

**已完成**：A–F 六件全做（能力目录 5 件／共用位 5 件／出口接线 3 件／一条词走通整链／自证两行读数／五步报告），`tsc` 零诊断、`node --test` 69/0 全绿、三条判据命令退出码 0、六份产物 60–62 KB 且带 doctype／section／复制区标记。

**下一步第一件（若有人接）**：把剩下 14 条命令按同一形状搬进各能力目录（`bill.record.today` 等读命令要补 `ReadCommandSpec` 那一支），每搬一条就从 `render\envelope.ts` 的过渡表删一行；搬完再拆 `cmd_read.ts`（见第五节拆法）。

**没改一半的东西**：无（所有改动都编译通过、测试全绿、真跑验证过）。

**下一手缺的信息**：
1. `base-paint/blocks` 出口面上**没有**「把外壳与复制区合成一页」的整页组件，也没有现成的「缺槽位采集页」组件——本票按要求只用现成件，`docPage.ts`／`collect.ts` 的装配是技能级共用位；若上级要把这两件上移公共层，需要先有一次跨包的影响面评估（`t406-共用件依赖与提升改造清单.md` 第三节第 3／4 条已判 `receiptParts.ts`／`docPage.ts` 可上移，本票没上移，只落在饼干自己的共用位）。
2. `renderErrorReceipt`（写库失败回执页）与 `renderEmptyState` 在本票的两张页里没有正当落点：写库失败仍走 stderr ＋ 非 0 退出码、不出错误页（与既有语义一致）。失败回执面是后票的事。
3. 采集页**不加 `--html` 就不落盘**（与其余 14 条命令同口径，落点由调用方给）；`t406-验收-真跑配方与判据.md` 第一节第 5 条实测同样如此。若上级要「不给 `--html` 也自动落一份」，那是落点策略的新规矩，本票不自己发明。
4. `t406-base组件总表.md` 第一节漏收 `renderKpiCard`（`blocks.ts:278`）与 `renderKpiGrid`（`:305`，另有 `:1326` 的再导出面）——本票实际用的就是这两个名字（状态卡经它们渲染），文档缺口记在这里。
