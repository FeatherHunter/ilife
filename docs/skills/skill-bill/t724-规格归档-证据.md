# #724 规格归档与复核 · 证据

- 票：[规格 · 页面装配骨架与按域页型表](https://github.com/FeatherHunter/ilife/issues/724) · 母图：[#681](https://github.com/FeatherHunter/ilife/issues/681) · 来源：[#685](https://github.com/FeatherHunter/ilife/issues/685)（2026-09-19 10:33 收口）
- 性质：**本票零源码改动**——规格自己写明「本票产出 ＝ 规格，不落码」（票面 `## 归属与纪律` 第一条）。执行 ＝ 把规格的交付物落进版本控制 ＋ 逐条复核规格里的读数与依据（对抗式）；形状照 [#720 的证据件](t720-规格归档-证据.md)。
- 执行当刻：2026-09-19 11:20–11:40，工作区 `D:\ilife`，branch `master`，起手 HEAD ＝ `a3474c60`。
- **在途提示**：本席开工时 #689（write 域）与 #690（query 域）两席正在改 `packages/skill-bill/src/{record,query}` 与包内 `AGENTS.md`（未提交）。故下文一切「域读数」一律取 `git show <提交>:<路径>` 的**冻结读数**，不取工作区。

## 一 · 交付物与入仓读数

| 交付物 | 路径 | 机器读数 |
|---|---|---|
| 规格件（接口与判据） | `docs/skills/skill-bill/t685-接口与判据.md` | 首次入仓 `268f28f7`，blob `59b3cb43…`，8,090 字节／LF 101 |
| 规格件（按域页型表） | `docs/skills/skill-bill/t685-按域页型表.md` | 首次入仓 `268f28f7`，blob `49343a51…`，17,513 字节／LF 156 |
| 过程留档（讨论页） | `docs/skills/skill-bill/t685-讨论决策页.html` | 首次入仓 `268f28f7`，blob `4029dd38…`，27,803 字节／LF 279 |
| 复核脚本（可复跑） | `docs/skills/skill-bill/t724-读数复核.mjs` | 只读；无参跑，`RESULT` 行见 §三 |
| 规格正文（tracker 条目） | [#724](https://github.com/FeatherHunter/ilife/issues/724) 票面 | 本次补 `## 复核读数（2026-09-19 执行）` 与 `## 进度：95%` 两节 |

- 提交：`268f28f7`（`docs(724): 页面装配规格入仓 —— 两件规格件与讨论页落到归属件目录`）——`git diff --cached --name-only` 复核 **STAGED-COUNT=3**、提交后 **COMMIT-FILES=3**、声明路径下 `WORKTREE-CLEAN-OF-DECLARED=true`。
- 三条文件经核：**无 BOM、全 LF**、与源件**逐字节相同**（同一脚本 `node .scratch/t724/archive.mjs` → `RESULT: 3/3 PASS`）；入仓身份的复跑判据见 `t724-读数复核.mjs` §⑤（三条全 OK）。
- **零源码改动**：本票只碰 `docs/skills/skill-bill/`；`packages/skill-bill/src`／`test`、卡片路里与公共层一字未动（改动面逐条见 §二）。

## 二 · 一处地址更正（本票唯一的「搬家」）

#685 窗口在 2026-09-19 10:02–10:25 把两件规格件与讨论页写在 `packages/skill-bill/docs/`（**未跟踪**：`git log --all -- 'packages/skill-bill/docs/*'` 为空）；而三处已出版的说法指向归属件目录：

- 票面「来源」行：`docs/skills/skill-bill/t685-接口与判据.md` 与 `…/t685-按域页型表.md`；
- 地图 #681 的 #724 条目：同两条路径（讨论页那一处写的是 `packages/skill-bill/docs/`）；
- 两件规格件各自的「姊妹件」行：互相写 `docs/skills/skill-bill/…`。

按 `docs/agents/doc-homes.md` 的落点表（技能的工作文档住 `docs/skills/<件名>/`），把三件**逐字节同搬**到位并提交。**原处的工作副本原样留下、未删未改**——在飞席 #689 的包内 `AGENTS.md` 正文（该席 2026-09-19 11:1x 写入的「页面两道门」一节）正引用 `packages/skill-bill/docs/t685-接口与判据.md` §二，本席不碰别人的写集；该副本的退场与那处引用的改址，归 #689 窗口（`packages/skill-bill/AGENTS.md` 在它的写集里）。本席留痕：三对文件 sha256 逐条相同（`130b481c…`／`d294c38d…`／`b60e4b44…`），副本内容与入仓版当刻一致，不存在两套说法。

## 三 · 逐条复核（对抗式；读数取冻结提交）

复跑：`node docs/skills/skill-bill/t724-读数复核.mjs` ⇒ `RESULT: 14/15（另 SKIP 9 条，只登记不判定） FAIL`——**唯一红条就是 §3.2 第 1 条**（卡路里调用点：规格 110、复测 114）。这条红是本席**故意留着**的：把判据改成对得上＝放宽，不许。

### 3.1 成立（逐值相同）

| 断言（出处） | 复测读数 | 判定 |
|---|---|---|
| 公共层块位闭集 19 行／20 个函数行号（`t685-按域页型表.md` §一） | `packages/base-render/src/blocks.ts` 逐名比对 **20/20 全对**（218／254／286／308／463／432／500／532／579／623／679／765／810／856／903／956／1012／1099／1128／1220） | 成立 |
| 卡路里 `src/shared/docPage.ts` 161 行、入参 11 字段 | LF 161；`DocPageInput` 逐字段数 11（`docTitle`／`title`／`eyebrow`／`subtitle`／`content`／`charts?`／`metaLeft?`／`badge?`／`summary?`／`printable?`／`pageUi?`） | 成立 |
| 账单 `src/shared/docPage.ts` 130 行、入参 5 字段、1 处调用点 | LF 130；5 字段（`docTitle`／`title`／`eyebrow`／`subtitle`／`content`）；`assembleDocPage(` 全包命中 2 ＝ 定义 1 ＋ `shared/pageShell.ts` 调用 1 | 成立 |
| 账单页面出口 **34 处调用点**（票面「不动」节） | HEAD `pageShell(` 命中 35 ＝ 定义 1 ＋ **调用 34**；逐件：`query/list.ts` 1、`query/detail.ts` 1、`record/collectBody.ts` 1、`record/receiptBody.ts` 1、15 件 `record/scene-*` 各 2（30） | 成立 |
| query 域 5 件 **727 行**（成文当刻） | 提交 `9ec05336`：`read` 315 · `list` 189 · `detail` 148 · `commands` 53 · `index` 22 ⇒ 727 | 成立（当刻） |
| 「账单测试件**没有一条断块序次序**」 | HEAD `test/` 34 件里，提到任一公共层块位函数名（20 个名字）的件 **0 个** | 成立 |
| 写入域「16 件里只有 1 件用通用装配体，另 15 件各写自己的采集页与回执页函数」 | HEAD 16 个 `scene-*.ts`：`scene-expense.ts`（21 LF）走通用 `collectBody`／`receiptBody`；其余 15 件各写 `collect*`／`receipt*`（抽样 `scene-income.ts`：`collectIncome` :96、`receiptIncome` :168） | 成立 |
| 老侧写入模板锚点（`t685-按域页型表.md` §2.1） | `写入/expense_form.html`：页壳与标题 `:46-50` ✔／`.prefill` `:54` ✔／`.dup-warn` `:56` ✔／表单区 `:58-76` ✔／`.actions` `:80-82` ＋ `#actionbar-zone` `:83` ✔；`写入/flow_confirm.html:190-196` 候选卡（`name="cand"` @192）✔（抽样 7 条 0 不符） | 成立 |
| 老侧分析模板分发表 25 个 type 键（讨论正文 `:199-232`） | `templates/分析/analysis_view.html` `:199-232` 逐字在位，`routers` 恰 25 键 | 成立 |

### 3.2 对不上（四处，逐条给读数与处置）

1. **卡路里「110 处调用点」→ 复测 114。** 口径：逐行扫 `packages/skill-calorie/src` 的 `assembleDocPage(`，全部命中 115、扣定义体 1 ＝ 114；只算非注释行 112、只算非注释且对象字面量调用 111。三个采样提交（`9ec05336` 成文当刻／`3efe9363`／HEAD）同值 ⇒ **110 在盘上复现不出来**（差 4）。处置：**登记**，票面按实况补一行；该数只出现在「不动多少处」的陈述里，不参与任何接口或判据决定。
2. **「账单 31 个测试件」→ 成文当刻 28。** `packages/skill-bill/test/` 件数轨迹：09-18 20:10 起 28 ⇒ 09-19 09:51（`edcc1276`，即 #686 的提交）**31** ⇒ 09:53（`57880028`）32 ⇒ 10:59（`bdbd3904`，#721）34；HEAD 34。讨论页正文写「已回读并核实：…31 个测试件」——31 只在 09-19 09:51–09:53 之间成立，不在成文当刻。断块序那条断言（§3.1 倒数第 4 行）不受影响。处置：登记，票面改按当刻实况写。
3. **分析域「6 个形态族／25 个渲染器」的清单有两处缺口。** ① `§2.3` 的「读数＋条」一行：行内**场景 13 个**、渲染器名只列 **11 个**——漏了 `renderOverview`（看总览，`:330`）与 `renderWeek`（看周报，`:347`），而「读数＋条 13 个」这个数数的正是含这两支的 13。② 第 6 族「读数＋占比图（环形）＋明细」点的 `renderBreakdown(347-375)` **不在分析模板里**：它是查询模板 `templates/query_view.html` 的同名渲染器（该文件 `:346-374`，KPI(3) → donut 卡 ＋ 占比明细卡），分析模板里 `breakdown` 字样 0 命中 ⇒ 分析域自己的 25 个渲染器落 **5 个形态族**（13／4／3／4／1），第 6 族的唯一实例在查询域。处置：写进票面「待确认」——它正是「七域落点清单」里分析那一格（规格自己写明该清单「尚待维护者点头」）。
4. **老侧分析模板行数自相矛盾。** 讨论正文一处写「1 张模板（1,166 行）」，另一处写「分析 1（1,098 行）」；盘上 **LF ＝ 1,166** ⇒ 1,098 对不上。查询模板同理：盘上 LF **548**，讨论正文写 503（#688 已按 548／549 记过一笔、登记不修）。处置：登记；票面「对照物」行按实况写。

## 四 · 读数漂移（成文当刻 → HEAD；全部由 2026-09-19 的 #721 两次提交带来）

| 读数 | `9ec05336`（成文当刻） | HEAD（`a3474c60`） | 漂移原因 |
|---|---|---|---|
| query 五件合计 | 727（`read` 315 · `commands` 53） | **720**（`read` 311 · `commands` 50） | #721 撤命令声明的代表词 |
| `record/` | 25 件／5,104 LF | **26 件／5,343 LF** | #721 新建 `record/declaration.ts`（246 LF） |
| `test/` 件数 | 28 | **34** | #686 两件 ＋ #721 两件 |
| 卡路里 `assembleDocPage(` 调用点 | 114 | 114 | 未变（#721 未碰卡路里装配口） |
| 账单 `pageShell(` 调用点 | 34 | 34 | 未变 |

⇒ 规格里的「727 行／43 行每场景」与「≈5,100 行／319 行每场景」**在成文当刻逐值成立**，此后被 #721 的域声明改动带偏；在飞席 #689 正在把 write 域改回模板级单份，`record/` 的读数还会再动。票面照「地址随实况、历史不改」办：保留成文当刻读数，本节记录漂移。

## 五 · 持锁运行记录（对账导出，协议 §2.3 第 4 条）

```
START ticket=724 runId=7b15a3b3-7ea0-42b5-81b0-a89a77ade76f cmd="node .scratch/t724/commit1.mjs" waitedMs=0 pid=39376 at=2026-09-19T03:23:31.088Z
RUN ticket=724 runId=7b15a3b3-7ea0-42b5-81b0-a89a77ade76f cmd="node .scratch/t724/commit1.mjs" waitedMs=0 exit=0 pid=39376 at=2026-09-19T03:23:31.349Z
```

GATE-RUN runId=7b15a3b3-7ea0-42b5-81b0-a89a77ade76f cmd="node .scratch/t724/commit1.mjs"

本导出涵盖截至提交 `268f28f7` 的声明运行（逐行抄自 `<lock-dir>/gate-runs.log`）。两件只读工作（`node .scratch/t724/archive.mjs` 落自己声明路径下的文档、`node docs/skills/skill-bill/t724-读数复核.mjs` 纯读）按协议 §2「不需要持锁」一栏办，不列门禁条目；本件与其后的提交同法记于同一份运行记录，需者按 `ticket=724` ＋ `runId` 复核（本席收尾时把本件入仓那一笔追加在 §七）。

## 六 · 未做项与下一手

- **不落生产者码**（规格自己写明）：公共层骨架件（`docShell.ts`／`renderDocShell`）归公共层票——**当刻地图 #681 上还没有这张票**（建议由维护者立）；判据甲／乙两道门已由 #689 窗口落在 `packages/skill-bill/scripts/{check-scene-shape,gen-page-fingerprints}.mjs`（见包内 `AGENTS.md` 新节「页面两道门」）；各域页型表与场景归各域票（#689／#690／#691）。
- **待维护者点头**：七域落点清单（写入 5 模板／查询 2／分析 6／目标 2／账户 3／开始使用 3／联动 0）。本次复核把分析那一格的两处缺口写成票面「待确认」（§3.2 第 3 条）。
- **本票未关闭**：按维护者的进度契约，待确认未落不得 close——票面写 `## 进度：95%` ＋ 逐条写明待确认什么。
- 本席未碰（属在途席）：`packages/skill-bill/src/**`、`packages/skill-bill/test/**`、`packages/skill-bill/AGENTS.md`、`packages/skill-bill/docs/**`（#685 窗口留下的工作副本）。
