# t730 目标域 · 差异表（老行为／新行为／为什么改／谁批的）

- 票：[goal 域：端到端搬迁（4 场景／2 片）](https://github.com/FeatherHunter/ilife/issues/730)（父图 [#681](https://github.com/FeatherHunter/ilife/issues/681)）
- 强制对照基准＝`docs/skills/skill-bill/688-融合基准.md`（T7）；形状规格＝`packages/skill-bill/docs/t685-接口与判据.md` ＋
  `packages/skill-bill/docs/t685-按域页型表.md`（T4）。
- 读法（与姊妹件 `t689-差异表.md`／`t690-查询域-差异表.md`／`t691-差异表.md` 同形，**这一族表就地定形**）：本表只记
  **本票改掉的**东西；「老行为」＝老技能（`D:\2Study\StudyNotes\SKILLS\饼干记账`）**与搬迁前的新仓产物**；
  「新行为」＝本票之后的产物。四列 ＋ 一个行号位（本域取 `#`／`#＋块`）。
- 对照物：老侧 `scenes/goal.yaml`（4 场景）· `scripts/goal/cli.py`（387 行，四支子命令）·
  `scripts/goal/render.py`（281 行，`GOAL_META:48-61` ＋ `MODE_TEMPLATE:64-69` 四件一张表）·
  4 张模板（`目标/budget_form.html` 165 · `saving_form.html` 151 · `budget_view.html` 252 · `saving_view.html` 248）；
  新仓 `src/goal/{declaration,params,goalData,pageParts,scene,template-form,template-progress,write,read,commands,index}.ts`
  ＋ 四件场景件 `scene-set-{budget,saving}.ts`／`scene-{budget,saving}.ts`。
- **本票改的是「块位与落点」，不改事实**：老侧那 4 张模板承载的每一条信息（金额／月份／分类／覆盖冲突／
  预算总额／实际支出／剩余／超支项／进度百分比／月底预测三态／目标期／已存／月均净存／预计达成日／达标所需月存）
  在新侧都有落点，逐条对照见 `docs/skills/skill-bill/t730-证据.md` §二。

## 一 页型层（4 个老场景 → 2 片页型）

| # | 片 | 老行为 | 新行为 | 为什么改 | 谁批的 |
|---|---|---|---|---|---|
| 1 | 设定表单片 · 采集页 ＋ 回执页（盖设定预算） | `目标/budget_form.html`：三格（金额／月份／分类）＋ 一条冲突提示条（`:55`／`:129-134`）＋ 单颗「📋 确认复制」＋ 一份自绘 `<style>` | 徽章列 → 结论条（缺项才出）→ 覆盖冲突提示（可见，同月同类已存在才出）→ 缺项标签与阻断折叠 → 口径行 → 本月已有预算只读表／空态 → 字段卡 → 复制指令块 → 复制区 → 来源脚注；齐了那次直接写库出**回执页**（读数行／结果块［覆盖时］／明细表／对账折叠／复制区／来源脚注） | 块位换公共层件（裁定 7）；页内脚本不做（老侧「确认复制」那颗按钮在新侧由复制区双按钮 ＋ 复制指令块承担，裁定 5）；缺项与冲突收进同一条阻断路径（裁定 9）；结果型三条恒出（裁定 2） | T4 §2.4 ＋ T7 裁定 5／7／9 |
| 2 | 设定表单片 · 采集页 ＋ 回执页（盖设定目标） | `目标/saving_form.html`：三格（目标／金额／截止日期）＋ 单颗确认复制；**没有**「已有条目」这一块 | 同一骨架（同一片页型的第二件）；「已有条目」读目标表（目标／金额／截止日期） | 同第 1 行；「已有条目」取同族齐全的那一套（见 §二 第 8 条） | T4 §2.4 ＋ T7 裁定 5／7／9 |
| 3 | 进度视图片 · 预算执行（盖看预算） | `目标/budget_view.html` 8 块：header／离线横幅 ＋ KPI×4（`:193-196`）＋ 预算明细卡（每条一截进度条 ＋ 状态徽章 ＋ 已用/预算 ＋ 笔数 ＋ 进度 ＋ 剩余或超出 ＋ 月底预测，`:207-235`）＋ 空态（`:203`）＋ 复制 ＋ footer | 11 块：徽章列（结果胶囊）→ 结论句 → 页内导航 → 读数行×4 → **各条预算进度卡**（卡内进度条 ＋ 状态徽章 ＋ 预算／已用／笔数／进度／剩余或超出／月底预测三态）→ 占比条 → 口径说明行 → 对账折叠 → 复制区 → 来源脚注 | 结果型页三条恒出（页内导航＋口径说明行＋来源脚注）照裁定 2；占比条照 §五 5.2 第 13 行（⑥ 类）；离线横幅**舍**（静态页没有「客户端缓存」这个成因，同 #690／#691）；footer 的脚本路径**不照抄**（裁定 1） | T7 裁定 1／2 ＋ §五 第 13 行 |
| 4 | 进度视图片 · 目标进度（盖看目标） | `目标/saving_view.html` 8 块：KPI×4（`:192-195`）＋ 目标卡（进度条 ＋ 状态徽章 ＋ 已存/目标 ＋ 进度 ＋ 月均净存 ＋ 预计达成日 ＋ 截止日期 ＋ 达标所需月存，`:206-231`）＋ 空态（`:202`）＋ 复制 ＋ footer | 同一片页型的第二件（同 11 块骨架），内容换成目标自己的读数 | 同第 3 行 | T7 裁定 1／2 |

## 二 事实与口径层

| # | 项 | 老行为 | 新行为 | 为什么改 | 谁批的 |
|---|---|---|---|---|---|
| 5 | 同月同类预算撞车 | 老侧 `cli.py:143-147`：`emit_ok`（`status:ok` ＋ `data.conflict:true`）＋ 一句「确认覆盖请加 --force」，**退出码 0 但不写库**；搬迁前的过渡实现改成 `throw POLICY_CONFLICT`（**exit 2**，一句错误串） | **阻断页**：exit 0、`ok:false`、不写库；页上可见地写清「已存在哪一条、原来的金额」，并给出路（`确认覆盖`） | 两边的实质相同（都要用户确认），新侧把它收进与缺项同一条路径（裁定 9）——用户不必去 stdout 里读那句提示；同时把过渡实现那**两处**与老侧都不一致的行为（错误串 ＋ 退出码 2）一次摆正 | T7 裁定 9 ＋ 偏好 2 |
| 6 | 覆盖的落库形状 | 老侧 `cli.py:149-153`：**删旧 ＋ 加新**（新的一条拿新编号） | 逐字同形（`goalData.appendBudget`） | **回老侧真源**：搬迁前的过渡实现是就地改 `amount`／`created_at`（编号不变，也不是删旧加新）；回执页上「覆盖了原来的 X 元」这一格随它才对得上 | 偏好 2 |
| 7 | 目标进度「已存」 | 老侧 `cli.py:275-278`：目标期内**全部记录**的金额累计（收入正 ＋ 支出负） | 逐字同形（不做转账排除） | **回老侧真源**：搬迁前的过渡实现排除了转账（`ledger !== '转账' && !category.startsWith('转账/')`）。实测两者**等价**（转账两笔同一时刻落库、一增一减净额相抵），但排除那一条是第二份口径，且与老侧不同 ⇒ 删掉 | 偏好 2（口径只留一处） |
| 8 | 采集页「已有条目」只读表 | `budget_form.html` 有冲突提示、`saving_form.html` **没有**这一块（同族两套） | 两件都出：设定预算列当月已有预算（分类／金额／记下的时间），设定目标列已有目标（目标／金额／截止日期） | 老侧同族两套做法取齐全的那一套（`688` §四 裁定 6「同域两套做法」的同一判法）；代价是一张只读小表，换来「再设一个之前先看见已经有哪些」 | 本票（T4 §2.4 的块位形态） |
| 9 | 进度百分比 | **同域两套下界口径**：`budget_view.html:209` 只有上界 `Math.min(pct,100)`、`:228` 显示那一处连下界都没有（负 pct 会画负宽度 ＋ 印负百分比）；`saving_view.html:208` 是双端 `Math.min(Math.max(pct,0),100)` | **取双端**：条长与上屏的百分数同过 `clampPct`（0 ≤ pct ≤ 100），夹取之后仍由「超出 X 元」那一行说清超了多少 | `688` §二 C13 点名判过的那处老侧缺陷（**老侧缺陷不照抄**，且同域两套取严的那一套）；公共层读数卡的 `bar.pct` 本来就是 0–100 闭区间，夹取必须在进条之前完成 ⇒ 口径住 `pageParts.clampPct` 一处 | T7 §二 C13 |
| 10 | 分类空值的说法 | 老侧 CLI 写「总」（`cli.py:158` 的 `category or '总'`）、页面写「总预算」（`cli.py:193` 的 `category_cn`）——**同域两套** | 「全月总预算」（一处定义，回执／页／日志都读它） | 仓规 `用词纪律`：同一件事一处说法；老侧那两套取规范的那一套并补全 | 本票 |
| 11 | 回执文案 | 老侧 `cli.py:158`／`:245`：`已设定{month}预算 {分类} {金额} 元` / `已设定储蓄目标「{name}」{金额} 元` | 「已设定预算：2026-09 全月总预算 2500.00 元」／「已设定目标「换手机」10000.00 元（截止 2026-12-31）」 | 事实一字不差（月份／分类／金额／目标名／截止日全在），说法按规范词摆正；覆盖那一次另加「（覆盖了原来的 2500.00 元）」 | 本票 |
| 12 | 缺项与坏值 | 老侧全是 `raise ValueError`（`cli.py:133-137`／`:167-168`／`:226-236`）：页上看不出缺哪一格，退出码 1 | **阻断页**：exit 0、`ok:false`、不写库、逐项点名（缺项／金额非正数／月份形态／截止日非真实日期）；折叠里那条口令只给看不给复制 | 裁定 9 的同一处置（与 #689／#691 同形） | T7 裁定 9 |
| 13 | 「改了目标表里几处」 | 老侧无此读数 | 设定预算／设定目标都报 **1 处**（**自报**） | 目标表住 `goals.json`（不在 SQLite），**没有 `total_changes()` 可读**——与账户域的账户表那一半同一处置与同一理由（口径写死在 `write.ts` 的 `finish` 一处） | 本票 ＋ #691 先例 |
| 14 | 空表时的读数卡 | `budget_view.html:191-196`＋`:203` 与 `saving_view.html:190-195`＋`:202` 在空态时**先画 4 个全 0 读数卡再挂空态** | 空表时**不出读数卡**，只出空态句 ＋ 引导句（页仍是完整的：标题／结论句／口径行／复制区／来源脚注都在） | `688` §二 C13 点名的那处老侧缺陷（零值照画）；同技能 `账户/account_view.html:188-191` 反而提前 return ⇒ 取不画的那一套（裁定 6 同判法） | T7 裁定 6 ＋ §二 C13 |
| 15 | 来源脚注 | 老侧 footer（`budget_view.html:151-156`／`saving_view.html:150-155`）：生成时间 ＋ 类型代码 ＋ **数据载体**（`goals.json(预算) + bills 聚合(实际支出)`） | 「数据来源 · 预算表与账本（只读） · 2026-09-01 → 2026-09-30 · 共 N 条」 | 老侧那句载体说明是**长板**（写清数据从哪来），新侧以「预算表与账本」保留这层信息；但「库文件名 ＋ 脚本路径」不上屏（裁定 1），窗口与条数是新侧恒出的那一份（裁定 2） | T7 裁定 1／2 |
| 16 | 出口载荷的键名 | 老侧 `cmd_budget` 回 `{month, budgets, totals, count}`、`cmd_saving` 回 `{savings, count, done_count}`；搬迁前的过渡实现回 `{items, total, op}` | `{items, total, op, month, count, budgets, totals, records}`（预算那一支）／`{items, total, op, count, done_count, savings, records}`（目标那一支） | `list` 形要求 `items` 必填（一件事实一处：公共层形状守卫用的就是它）；老侧那几个键名一并留着，下游读哪一个都对得上 | 本票（`base-link-core` 的 `list` 形） |

## 三 判过而**不动**的（老侧机制不照抄）

| # | 项 | 老侧怎么做 | 新侧怎么做 | 出处 |
|---|---|---|---|---|
| 17 | 自绘样式层 | 4 件各一份 `<style>`，写死 `max-width:920px`（`budget_view.html:23` 那一类） | 全走公共层样式资产（每份产物同一段 head 样式，`<style` 恰 1 处） | T7 裁定 7；`688` §二·D9 |
| 18 | 客户端 JS | 注入 payload 后当场拼 DOM：预填、`buildPrompt`、错误分支、Base 预览弹层 | 无：Node 侧渲染静态页，一命令一页 | `688` §五 5.1；T4 §2.2 |
| 19 | 离线横幅 | `budget_view.html:131`／`saving_view.html:130` 客户端探缓存后出黄条 | 不出 | 静态页没有「客户端缓存」这个成因（同 #690 第 14 行、#691 第 16 行） |
| 20 | 双转义 | `budget_view.html:134`＋`:163`／`saving_view.html:133`＋`:162`：先 `escapeHTML` 再交公共层 `esc()`，用户看到 `&lt;` | 不出（转义只由公共层一处做） | `688` §二 A10 的「老侧缺陷不照抄」 |
| 21 | 复制载荷的截断 | `render.py:166`／`:179` 的 `[:15]`：envelope 的 sections 只列前 15 行（页面仍画全部） | 复制载荷给全量（页面画什么就复制什么） | `688` 裁定 5（复制载荷是给助手照抄的那一份，不静默截断）＋ A9 |
| 22 | 错误回执 | 老侧 `error_envelope` ＋ 模板里的错误分支 | 走新仓整页失败面（`renderFeedbackBlock`），老侧那套不引入 | `688` §二 A10 |

## 四 本票**没有**做的（留给后续票）

| 项 | 现状 | 去向 |
|---|---|---|
| 判据甲（页指纹账本）只覆盖写入域 32 页 | 目标域的 4 张页**不在账本里**：本票新增的目标页不会让那道门变红（与 #691 同一留白） | 本票以 `test/t730-目标域.test.mjs` 的页断言 ＋ `docs/skills/skill-bill/t730-探针-真出口.mjs` 顶上；扩账本是**横向决定**，留给下一张页面票 |
| 旧模板死件 | `templates/goal_write.html`／`goal_query.html` 两条命令搬进注册表后不再走 `templateFor` 那条路（与写入域 16 条、查询域 4 条、账户域 2 条搬迁后同一状态） | **判「不删」**：与兄弟域同一处置（留着作老侧复刻对照物），清算归后续票 |
| 老侧 `saving --name X` 的按名筛选 | 新侧读命令也认 `name`（子串匹配，照 `cli.py:323-324`），但页上没有任何提示 | 不补：老侧页上也没有；`看目标` 那条唤醒词的 `prompt_template` 里没有这一格 |

## 五 T4 §2.4 的两行按老侧逐块搬（T4 §六 第 3 条授权的「真实施须逐块对照老模板」）

`packages/skill-bill/docs/t685-按域页型表.md:92-97` 那两行是**依老侧 4 张模板静态读出的初始表单**；
本票按它逐块搬时补两处：

| # | 项 | T4 读到的是 | 本票怎么落 | 为什么 |
|---|---|---|---|---|
| 23 | 片数 | 「设定表单 ｜ 采集 ×2」与「进度视图 ｜ 查看 ×2」两行 | 片数 **2**（不变）；**一行盖两件**——4 个场景各一件场景件，两件共用一份装配件 | `688` 裁定 8「同域同形共用一份装配件」；两行的块位序列逐字相同，差别只在块内取值 |
| 24 | 「块位数据形态」列 | 设定表单＝「金额／周期／备注」；进度视图＝「读数＝已用／剩余／进度百分比」 | 设定表单＝金额 ＋ 月份（周期）＋ 分类 ＋ 已有条目表；进度视图＝读数（预算总额／实际支出／剩余／超支项 ｜ 目标数／已完成／累计已存／目标总额）＋ 每条进度卡 ＋ 占比条 | 老侧 4 张模板里没有「备注」这一格（`budget_form.html`／`saving_form.html` 都是三格）；那两行是初始表单，真实施按老模板逐块搬是 T4 §六 第 3 条明写的授权 |
