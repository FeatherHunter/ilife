# t407 终审打分 · 手机端采集页（390 宽 · 16 页）

## 一、出图口径（沿用盘上图＋抽检验）

- 沿用盘上图：`.scratch\t407-final\mobile\` 内 `390-*-采集页.png` 16/16 齐（盘上名省略 `t407-` 前缀），未重渲。
- 抽检 2 张 `read_image`：代表-记支出-采集页（390x2484）、记退款-采集页（390x3144），均为真全页长图（INNER=390 DOCW=390 VW=390），非空白非错误页。
- 前轮结论沿用：上一轮第①②条（横向溢出／三列网格放不下）是量法伪影、已作废；第④⑤条有效。
- 本轮改动（据整改证据件）：桌面补丁只在 1200px 以上生效，手机端 CSS 未动；复制区去重后采集页底部为单组。

## 二、逐页分数表（16行）

| 页 | ①层级 | ②留白对齐 | ③符号串（照抄） | ④内部话（照抄） | ⑤溢出 | ⑥分·理由 |
| --- | --- | --- | --- | --- | --- | --- |
| 代表-记支出 | 重点可辨，标题＋红标＋负金额形成警示 | 标题与左上标签过近显挤，底部按钮贴边留白不均 | 有：“请务必落实数据第一遍：这一页先不写库、补齐之后跟踪助手说一遍才合情。” | 有：`bill=cmd:read bill_record.add --params {"kind":"expense","amount":-12.5,"time":"2026-09-14 12:30:00","category":"分类"}`、账单、账户、分类、人民币 | 无溢出，排版完整在视口内 | 52·命令原文大块上屏成噪声，挤掉补齐行动权重 |
| 记收入 | 勉强，多张“未给”卡稀释重点，蓝行动按钮被夹不突出 | 边距统一，预填注解换行后与下卡偏紧，备注与下区留白偏大略断裂 | 有：“支出记账户，收入记收入——金额同号同方向；给成分数会被归在运一页，不进写库那一步。”、“分类 · 金额 · 时间 · 账户 · 账本” | 有：`bill.add_record`、`bill_record.add_params`、`kind`、`category`、`amount`、账本、账户、币种 | 无溢出，全在 390 内换行 | 62·“未给”卡重复占首屏一半，行动点被挤到中段 |
| 记报销 | 重点清楚，“缺 2 项”主任务一眼可扫到 | 对齐良好无截断，概览卡内三行竖堆稍紧，深底提示缩进贴左偏密 | 有：“找出来还有问题：收入记证过数；分类要没到格给那一级。”（另多段疑似误读乱码） | 有：`bill_record.add`、`'kind'`、`'reimburse'`、`'category'`、实体类型（数实生型） | 无溢出，代码块与底部按钮完整在屏内 | 68·深底提示区多段机器乱码占位文案，拉低可信度 |
| 拍账单 | 一眼抓住“还缺什么→补齐入口” | 卡片 2×2 居中对齐良好，无挤断裂错位截断 | 无 | 有：`bill=cmd read bill.record.add`、账户、分类（补齐回填示例行内） | 无溢出 | 92·补齐示例命令与说明混排，用户阅读吃力 |
| 批量录入 | 可，“还差 2 项”徽章＋大蓝按钮第一屏明显 | 2 列网格间距均匀，标签栅格稳定，深色条与正文左缘对齐 | 有：“支出记负数，收入记正数；分类要选到最细那一级。”、“缺必需槽位：分类、金额（已出采集页，补齐之后跟助手说一遍）” | 有：`bill_cmd=read bill.record add --params '{"kind":"batch","category":"<分类>","amount":"<金额>"}'`、`category`、`amount` | 无溢出，全在屏内 | 78·错误态与命令块紧挨却无显式关联，新手需脑补补齐回路 |
| 记退款 | 合格，标题→步骤条→红色缺口块→三段小标题共 4 级清楚 | 边距干净，候选表 label/value 全左贴边、“金额”重影需二次扫描 | 有：“支出的金额记成负数，收入记成正数；分类要选到最细那一级。”、“该给哪一笔；从候选里挑一行；候选只列金额为正的记录” | 有：`bill.record.add --preset`、`category`、`amount`、`source_id`、`refund` | 无溢出，最宽段 358px 内换行 | 78·候选表 label/value 未分列对齐致重影，另有两句语义重复占说明预算 |
| 报销到账 | 尚可，说明→录入→校验→示例节奏清楚，两条提示块字号近正文不先被看到 | 边距一致，长说明段贴卡片下沿无分隔，2×2 网格两行卡高轻微错位，底部按钮与错误块紧贴 | 无（最接近两处是连写句，非标点串多项） | 有：`bill_record_add`、`kind`、`category`、`amount`、`source_id`、待报销记录编号、报销回款 | 无溢出，超长命令串近右缘留白偏紧 | 48·伪命令示例把内部字段名直接摆给用户，术语密度过高 |
| 记借出 | 可，标题＋蓝签＋红“还缺 3 项”＋蓝按钮 1 秒抓到重点 | 行距偏紧，长句行高贴底，命令块挤进说明区致重量失衡 | 有：“支出的金额记成负数，收入记正数；分类要到最细那一级。”、“借贷走标签流程：这一笔写「#借出 #借给（对象）#未还」” | 有：`bill_record_add`、`kind`、`lend`、`category`、`amount`、账户 | 无溢出，标签词贴近右缘行宽体感偏满 | 72·行高过紧、长说明缺分段与项目符号，需逐行细读 |
| 记借入 | 有递进但首屏 8 张同质“未给”卡堆密，信息冗余 | 边线对齐好无截断，第 1 段正文与红标签行距略紧 | 有：“支出的金额记成收款，收入记成正数；分类要选到最细那一级”、“还差 3 项：补齐了，再说一遍『记借入』” | 有：`bill.record add --params`、`kind`、`borrow`、`category`、`amount`、`who`、“期借借”乱码 | 无溢出，长命令块已换行在屏内 | 58·同形“未给”卡塞满首屏且含截断乱码，金额被淹没信噪比低 |
| 记收回 | 一目了然，标题／状态签／步骤编号色块对比强秒抓重点 | 两列等高对齐好，标签左值右整齐，无挤断裂错位截断 | 无（均为短句或独立条目） | 有：`bill:record:add`、`amount`、`amount_display`、`datetime`、`user_id`、`created_at`、`status`、MySQL、UTF8、建表 | 无溢出，整页完整渲染 | 92·“求助”红框说明句稍长字号偏小，首屏易被忽略 |
| 记偿还 | 清楚，标题＋用途说明一眼到位，按钮卡片分层 | 卡片等宽，表单左对齐与按钮一致，无截字 | 无（用顿号或换行，非“·\|；”串） | 有：`record.add`、`category_id`、`source_id`、`amount`（接口手册片段行内） | 无溢出，均贴合 390 宽 | 78·井号标题被原样显示成字面字符，显得粗糙 |
| 记分期 | 突出，标题＋金额＋期数＋首期日一眼可见，预览卡排列清楚 | 留白充裕对齐一致，底部第 12 期日期行贴底边有轻微截断风险 | 有：“每期 = 总价 ÷ 期数，四舍五入到分；尾数对齐到最后一期”、“分热购会与金额，再将每期金额与日期逐期累推；尾数对齐到最后一期”（部分字疑误读，照抄） | 无（未见 bill.*、列名、类型名上屏） | 无横向溢出，均在屏内 | 85·底卡日期行贴底疑裁切，说明文案偏技术腔门槛略高 |
| 记一笔 | 清楚，标题＋进度提示最显眼，行动与提示成二级焦点逐级下沉 | 留白合理模块匀，底部示例标题与示例块间距略紧轻微断裂 | 有：“记一笔·补齐槽位”、“金额/分类/补齐之后的顺序·一级。”、“写者·还没发生——这一项先不用填”（部分字疑误读，照抄） | 有：`bill.repaid_bill`、`"note"`、`"amount"`、`["category"]` | 无溢出，全在可视范围 | 82·错误说明区与按钮示例区间呼吸感不足，局部拥挤 |
| 改记录 | 清楚，标题＋红缺口提示最显眼，蓝主按钮梯度明确 | 卡片字段左对齐金额右对齐统一，呼吸感好无错位 | 有：“支出的金额记成负数，收入记成正数；分类要选到最细那一级。”、“库共 2 条未撤销的记录，本页列表前 2 条（按时间倒序）。” | 有：`bill.cmd=read bill.record.update --params '{"id":"记录编号"}'`、记录编号、候选 #1 #2 | 无溢出，代码块长文本均未越界 | 73·裸命令行直接上屏无弱化，与轻量引导语境割裂 |
| 撤销 | 警示→行动层级清楚一眼可找，但橙候选签比主蓝按钮抢眼略倒挂 | vision 未答②（答复从①直接跳到③） | 无 | 有：`bill.cmd=read bill.record.update --params {"op":"undo","id":"记录编号"}`、record、update、params、id、op、undo | 无溢出，底部两组按钮完整在屏内 | 72·候选签与主按钮主次倒挂，命令原文面向用户 |
| 恢复 | 清楚，状态条＋标题＋副标三层递进，蓝按钮最强焦点权重合理 | 四卡两两并排时间独占一行整齐，说明命令按钮贴齐无断裂错位截断 | 无（仅口语连接句，非分隔符串） | 有：`bill.cmd=read bill.record.update --params '{"op":"restore","id":"{{记录编号}}"}'`、记录编号、op、restore、id | 无溢出，全在 390 内 | 92·“缺 1 项”红框与底部说明信息重复略冗余 |

## 三、横向归纳

- 溢出 0/16：vision 逐页一致判无横向溢出、无内容切屏，前轮第①②条作废在本轮得到印证。
- 符号串 10/16 页剩余：“支出的金额记成负数，收入记成正数；分类要选到最细那一级。”在多页重复出现；干净 6 页（拍账单、报销到账、记收回、记偿还、撤销、恢复）。
- 内部话 15/16 页剩余：命令原文或列名上屏（bill.record.add／update 系列＋kind／category／amount／source_id／id／op），仅记分期干净；报销到账、代表-记支出两页最重。
- 层级留白通病：红阻断块＋蓝主按钮双重心抢视线；候选表 label/value 未分列对齐；底部按钮与错误块贴边偏紧；对比度与按钮均无“字看不清”报告。
- 一票否决 0 页命中：无切屏、无看不清、无阻断按钮失效，本批判不触发否决。
- vision 质量注记：记报销③多段疑误读乱码、记收回④出现 MySQL／UTF8 等可疑词、撤销缺答②，均已照抄并在表内标注，未用文字猜补。

## 四、小计

- 16 页平均分 73.9，未评 0 页（vision 调用成功 16、失败 0、未评 0，均一次成功无重试）。
- 最低两页：报销到账 48、代表-记支出 52；最高三页：拍账单／记收回／恢复均为 92。
