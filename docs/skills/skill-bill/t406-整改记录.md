# T406 勘察件按对抗审查整改 · 逐条留痕

整改面：两份审查报告点名的地方。被审件四份：
- `docs/skills/skill-bill/t406-写入域-老入口对照与共用件候选.md`（对照件）
- `docs/skills/skill-bill/t406-写入域-老模板逐块清单.md`（清单件）
- `docs/skills/skill-bill/t406-base组件总表.md`（总表件）
- `docs/skills/skill-bill/t406-共用件依赖与提升改造清单.md`（改造清单件）

审查报告：`t406-勘察件-对抗审查-老侧.md`（下称「老侧报告」）、`t406-勘察件-对抗审查-新仓.md`（下称「新仓报告」）。

凭据口径：本记录只写改了什么、改后原文摘要。每条改动都在被改件末尾的 `## 修订记录` 里留一行。

## 逐条对账

| 审查件 | 条目编号 | 严重度 | 改在哪个文件哪一行 | 改后原文摘要 | 状态 |
|---|---|---|---|---|---|
| 老侧报告 | 第三节第 1 条（第五节须改 1） | 严重 | `t406-写入域-老模板逐块清单.md` 总表第 10、12、13 行（「页面类型」列） | `flow_confirm.html`／`installment_confirm.html`／`update_confirm.html` 三行由「两者」改判「过程型确认页」，格内写明依据：`:72`／`:74` 两枚可编辑输入框、`:64`／`:62` 只有确认按钮、`:105-111` 等只读回显，无任何已落库结果 | 已改 |
| 老侧报告 | 第三节第 2 条（第五节须改 2） | 严重 | 同件 总表第 14 行（「服务的写入场景」列） | 由「买东西（联动记账已完成）」改为「买东西联动／吃饭联动**两型共用**」，补 `scenes/link.yaml:10`／`:26`、`scripts/link/cli.py:452-457`、`scripts/link/cli.py:263 build_receipt_payload` | 已改 |
| 老侧报告 | 第三节第 12 条 | 轻微（两份共有） | 同件 总表之后新增一段 | 新增「页面类型判据（本件与兄弟件同用）」：报告已发生的事＝结果型；收集或确认尚未发生的事、只输出一段交回 AI 的命令文本＝过程型；并单列一句「只读回显不等于结果型」 | 已改 |
| 老侧报告 | 第三节第 5 条（第五节建议 5） | 中等 | 同件 正文「六个模板的所有字段…」＋ 第四节已知问题 6 | 两处「一律没有 `name` 属性」收窄为「**数据字段**没有 `name` 属性」，写明唯一例外 `flow_confirm.html:192` 的候选单选件 `name="cand"`（选关联原记录，不是数据字段） | 已改 |
| 老侧报告 | 第三节第 6 条（第五节建议 6） | 中等 | 同件 第六节没能确认 7 | 「这六个文件」改为「`templates\写入` 5 份 ＋ `templates\联动` 3 份（`meal_confirm.html`／`purchase_confirm.html`／`receipt.html`），两个目录共 8 份」 | 已改 |
| 老侧报告 | 第三节第 3 条（第五节须改 3） | 严重 | `t406-写入域-老入口对照与共用件候选.md` 第二节「结果型（回执）」一段末句 | 改为「写入域**内部**没有独立回执页装配函数（`render_write.py` 5 个装配函数 `:149`／`:232`／`:290`／`:466`／`:514` 都产采集页与确认页）；**跨域**看结果型回执页还在联动域有一份：`scripts/link/cli.py:263 build_receipt_payload` ＋ `templates/联动/receipt.html`（`references/路由表.md:70-71` 写「HTML 表单+回执」；`scripts/link/cli.py:452-457` 一份页服务 `purchase`／`meal` 两条）」；同段开头补一句判据「只读回显不等于结果型」 | 已改 |
| 老侧报告 | 第三节第 4 条（第五节须改 4） | 严重 | 同件 第三节共用件表 `envelope`／`inject_base`／`write_html` 三行的「被哪几处用」列 | 域外行号由五个导入行换成真实调用点：`envelope`→`account/render.py:104/144/202/259`、`link/cli.py:253/355`、`setup/render.py:181/206/233/259/295/325`、`goal/render.py:138/159/173`、`bill_inject.py:350`；`inject_base`→`account/render.py:82`、`link/cli.py:379`、`setup/render.py:127`、`goal/render.py:191`、`bill_inject.py:371`；`write_html`→`account/render.py:85`、`link/cli.py:381`、`setup/render.py:130`、`goal/render.py:192`、`bill_inject.py:372` | 已改 |
| 老侧报告 | 第三节第 4 条（同一列的同型错，第二节打分处点到） | 轻微 | 同件 共用件表 `error_envelope` 行 | 四处改为 `account/render.py:275`、`setup/render.py:358`、`goal/render.py:148`、`bill_inject.py:312`，并注明上一版引的两处是守卫行、两处是导入行 | 已改 |
| 老侧报告 | 第三节第 4 条（附带核对口令，兼第三节第 11 条「可重跑」） | 轻微 | 同件 共用件表之后 | 新增「域外行号口径（核对用）」：写明五个导入行行号、判据（同一行号被三件共用＝导入行），并给可重跑口令 `grep -rn -e "inject_base(" -e "write_html(" -e "envelope(" .` | 已改 |
| 老侧报告 | 第三节第 7 条（第五节建议 7） | 中等 | 同件 第六节没能确认 4 | 由「是否还有第二条件入口没确认」改为「`scripts/record_bill.py` 已核：`cmd_add:75`／`cmd_update:105` 与 `write/cli.py:28`／`:57` 同职责，不产 HTML、不调 `envelope`／`inject_base`／`write_html`（该文件零命中），故页面装配面不受影响、第一节「老 Python 入口」列需补一行；`scripts/bill_inject.py` 仍未读（它调 `:350`／`:371`／`:372`）」 | 已改 |
| 新仓报告 | 第三节第 1 条（第五节须改 1） | 严重 | `t406-共用件依赖与提升改造清单.md` §四 口径句 | 「只有 `packages/skill-calorie/src/**` 命中，包外零命中」改为「已按件名逐路径检索，**包外已有命中**：`packages/skill-bill/src/shared/docPage.ts`（53 行）与 `packages/skill-bill/src/shared/copyArea.ts`」，并写明检索式可留但必须附命中路径 | 已改 |
| 新仓报告 | 第三节第 1 条（同条） | 严重 | 同件 §四 表 `docPage.ts` 行 | 「第二个使用者」由「无」改为「**有**——同名同职责的平行实现」；结论倾向写明关键约束：**提升到公共层时要「并把这两份合并进去」，不是新造第三份** | 已改 |
| 新仓报告 | 第三节第 1 条（同条） | 严重 | 同件 §四 表 `copyArea.ts` 行 | 「第二个使用者」由「无」改为「**有**——`packages/skill-bill/src/shared/copyArea.ts`（`promptCopyArea:61`／`copyArea:72`／`copyLog:107`）」；结论倾向改为「按两份实现的差量重算」 | 已改 |
| 新仓报告 | 第三节第 3 条（第五节须改 2） | 中 | 同件 §二 `copyActionHtml` 行 | 「是不是卡路里专有」由「**是**」改判「**否（画像错）**」，依据 `base-render/src/spec/help.ts:249-250` 与冻结面 `base-render/src/spec/index.ts:168`（`render/copy.ts:18-24` 就是从 `base-paint` 取的）；解耦难易列删去「缺省改成必填或由公共层给冻结缺省」这条空改动 | 已改 |
| 新仓报告 | 第三节第 3 条（同条） | 中 | 同件 §三.5「新增 base 导出」行 ＋「要加的参数」第 2 项 ＋ §四 `copyActionHtml` 行 | 三处同步：`action` 的缺省**不必新加**（本来就取公共层冻结值），保留 `action?` 覆盖位 | 已改 |
| 新仓报告 | 第三节第 1 条（附带）/ 老侧报告第三节第 2 条的连带面 | 中 | 同件 §五 没能确认 5 | 补上老侧 `templates\联动\receipt.html` 一份页服务两个场景（`scenes/link.yaml:10`／`:26`；`scripts/link/cli.py:452-457`，装配入口 `:263 build_receipt_payload`） | 已改 |
| 新仓报告 | 第三节第 2 条（第五节须改 3） | 中 | `t406-base组件总表.md` §1.3 表 | 补收 `renderKpiCard`（`blocks.ts:278`）／`renderKpiGrid`（`:305`）／`renderTocBlock`（`:242`）／`renderCaliberLine`（`:260`）／`renderListRows`（`:439`）／`renderDetailSection`（`:527`）六行；KPI 两行写明「`KpiCardInput` 的唯一渲染器」与「回执页状态卡必需件」，另四行标注「只核到声明行与签名，块体未读」 | 已改 |
| 新仓报告 | 第三节第 2 条（同条） | 中 | 同件 §1.5（新增一节） | 补上区块层再导出面 `blocks.ts:1326`（`ACTION_ID_ATTR`／`COPY_ACTION_IDS`／`DEFAULT_DATA_ATTR`／`TOAST_DEFAULTS`）与 `:1327-1334`（六个类型），并注明取值与字段形状未读 | 已改 |
| 新仓报告 | 第三节第 2 条（同条） | 中 | 同件 §四 没能确认 5 | 由「只由 grep 见到声明行，故未进第一节」＋「若回执页要明细段或行列表需补读」改为直陈「已补进第一节；回执页要明细段／行列表即取 `renderListRows`／`renderDetailSection`」 | 已改 |
| 新仓报告 | 第三节第 4 条（第五节随改 4） | 中 | 同件 §2.3 行数列 ＋ §四 第 6 条 ＋ §四 第 7 条 | `spec/*.ts` 行数由 49／255／283／227／246 改为实测 63／362／390／273／281；新增「行数口径」一句写明上一版五个数字全错、第 6 条曾把其中四个又抄一遍；第 7 条点明「同一处不一致只修了一半」 | 已改 |
| 新仓报告 | 第三节第 6 条（第五节随改 4） | 低 | 同件 §三「明显用不上的」 | `chartsCss` 行号 `src/charts.ts:1749` 改为 `src/charts.ts:1752` | 已改 |
| 新仓报告 | 第三节第 5 条（第五节随改 5） | 低 | 改造清单件 §三.2 | 五字符表引用由 `blocks.ts:52`–`:59` ／ `blocks.ts:51` 改为 `spec/controls.ts:14`（`ESCAPE_HTML_CHARS`）＋ `contract.ts:32-33`（注释），与同件第五节第 3 条统一 | 已改 |
| 新仓报告 | 第三节第 7 条（第五节随改 6） | 低 | 改造清单件 §二 `CrudReceipt` 收据形状行 | 「十二个字段」改为「所引 `:43`–`:54` 段内**自有字段 10 个**，另 `extends M5Fields`（`receipt.ts:82`）」 | 已改 |
| 新仓报告 | 第三节第 8 条（第五节随改 6） | 低 | 改造清单件 §一 第 2 件头部、第 4 件头部 | 自报条数由「（3 条）」改「（4 条）」、「（5 条）」改「（6 条）」，与各自正文列出的条数对齐 | 已改 |
| 老侧报告 | 第三节第 8 条 | 轻微 | 对照件 第一节「记一笔」行引文 | `subs: 修改` 改为 `subs: 修正`（`scenes/write.yaml:211` 的实际组名是「修正」） | 已改 |
| 老侧报告 | 第三节第 10 条 | 轻微 | 清单件 第四节已知问题 1 | 「缺 `--purple`」由只点 `flow_confirm.html:9` 一份改为四份（`flow_confirm`／`batch_confirm`／`installment_confirm`／`update_confirm` 的 `:9`） | 已改 |

## 未改

两份审查报告「必须先改」共 7 条（老侧 4 ＋ 新仓 3），**全部已改**；「建议同批改／随改」的老侧 3 条、新仓 4 条也全部已改；另附两处轻微项（老侧第三节第 8、10 条）一并改了。下面是不在本轮改动面内的条目与原因。

| 审查件 | 条目 | 为什么没改 |
|---|---|---|
| 老侧报告 | 第三节第 9 条（对照件已知问题 7 的例证列举不全：`:123-127`／`:99`／`:132` 同样只 `print` 不置退出码，`:83` 也走 `sys.exit(1)`） | 审查报告自己写明「结论（退出码不统一）成立」，缺的只是三处例证与一处 `sys.exit(1)` 的补记；补例证要另读对照件第四节已知问题 7 那一段正文，属本轮改面之外的正文补充，故留作收口项（见「交棒」）。 |
| 老侧报告 | 第三节第 11 条（两份件都没给可重跑的核对口令） | 只在对照件共用件表的域外调用点那一处补了口令——那一处正是本轮改的地方；给两份件全篇逐条配核对口令不属本轮改动面。 |
| 新仓报告 | 第三节「薄弱处」（§四 检索结论「只报结论、不贴命中项」的写法） | 已随第 1 条一并改掉写法：§四 口径句现在逐条附上两条命中路径。其余各节的证据写法未逐条加命中列表。 |
| 老侧报告 | 第一节第 41 条（拍账单预填条的命令侧取数 `photo_meta`）／新仓报告同类「无法核实」项 | 这些是「没能确认」，不是错——两件里都如实写着；按纪律**不许删，也不许改写成实测**，故原样保留。 |

## 交棒

1. **对照件第一节「老 Python 入口」列还差一行。** `scripts/record_bill.py` 已核为一条与 `scripts/write/cli.py` 同职责的平行入口（`cmd_add:75`／`cmd_update:105`，不产 HTML、不调信封），但第一节那张表的「老 Python 入口」列本轮没补（改面只到第六节没能确认 4）。下一轮要用那张表数入口时，先补这一行。
2. **`scripts/bill_inject.py`（24.2K）仍未读。** 它确实调 `envelope`（`:350`）＋ `inject_base`（`:371`）＋ `write_html`（`:372`），是一条独立的展示与落盘入口；对照件第六节没能确认 4 已如实保留这一条。
3. **`packages/skill-bill/src/shared/` 下两份平行实现按「合并」执行。** `docPage.ts`（53 行）与 `copyArea.ts` 已在饼干包内；提升到公共层时把这两份合并进去，不是新造第三份（见改造清单件 §四 `docPage.ts`／`copyArea.ts` 两行）。
4. **核对口令**（对照件共用件表的域外调用点，在老技能 `scripts` 目录下跑）：`grep -rn -e "inject_base(" -e "write_html(" -e "envelope(" .`——带左括号的才是调用点。
5. **另有三处需要再读一段正文才能收口的条目**：对照件已知问题 7 的例证补全（老侧报告第三节第 9 条）、两份件全篇的核对口令（老侧报告第三节第 11 条）、拍账单元数据的命令侧取数（老侧报告第一节第 41 条）。
