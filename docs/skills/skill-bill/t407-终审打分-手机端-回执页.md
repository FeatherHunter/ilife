# t407 终审打分 · 手机端回执页（390 宽 · 16 页）

## 一、出图口径

- 图源 `.scratch\t407-final\mobile\` 内 16 张 `390-*-回执页.png`，32 张中回执页全齐，无补渲。
- 抽检 2 张（记退款-回执、撤销-回执）`read_image` 确认为真全页：390 宽整页，顶部状态字样 `DOCW=390 VW=390`。
- 上一轮①②条（横向溢出／三列网格放不下）是量法伪影、已作废；本轮以真机渲染为准。
- 本轮改动（据整改证据）：桌面补丁只在 1200px 以上生效，手机端 CSS 未动；复制区去重后 15 页单组，撤销-回执仍两组（已披露例外）。
- 每页 `vision_describe` 1 张，问题照原文未改写。

## 二、逐页分数表（16行）

| 页 | 层级 | 留白对齐 | 符号串 | 内部话 | 溢出 | 分数·理由 |
| --- | --- | --- | --- | --- | --- | --- |
| 报销到账-回执 | 重点清晰 | 对齐工整，无截断 | 无 | 无 | 无 | 90·下半段表格层级偏弱 |
| 撤销-回执 | 重点明确 | 整体良好 | 无 | 无 | 无 | 92·恢复钮与复制钮距离偏近 |
| 代表-记支出-回执 | 主区与撤销钮两级焦点，状态字偏弱 | 卡片网格对齐良好 | 无 | 无 | 无 | 78·状态浅灰小字弱于徽标，权重倒置 |
| 改记录-回执 | 层级合理，撤销钮最强焦点 | 对称整齐，无挤压断裂 | 有：“这一笔已经记下了，不用再做什么；要反悔就点下面的「撤销这一笔」。” | 无 | 无 | 95·顶部状态色带使主标题略下沉 |
| 恢复-回执 | 主次分明 | 均匀整齐 | 有：“这一笔已经记下了，不用再做什么；要反悔就点下面的『撤销这一笔』。”等3句 | 无（“记录编号”等仅业务文案） | 无 | 90·分号串联过密，可读性略弱 |
| 记报销-回执 | 重点一眼可见 | 说明条内一条错列断裂 | 有：“这一笔记已经记下了，不用再做什么；要反悔就点下面的「撤销这一笔」。” | 无（仅用户标签 #待报销） | 无 | 67·说明条错列换行，破坏连贯性 |
| 记偿还-回执 | 一眼锁定金额与状态 | 留白舒适，对齐良好 | 无 | 无 | 无 | 92·底部说明字号略小行宽偏长 |
| 记分期-回执 | 清晰，金额最大字号突出 | 整体良好，副标题换行略有断行感 | 有：“这笔已经记下了，不用再做什么；要反悔就点下面的「撤销这一笔」。” | 无 | 无 | 88·副标题括号换行孤悬 |
| 记借出-回执 | 首屏一眼看到重点 | 网格稳定，无错位 | 无「·｜；」（仅「/」混排与长复句） | 有字段名上屏：“记录编号、借贷标签、这次记了几笔、币种” | 无 | 88·内部字段名直接暴露，运维味重 |
| 记借入-回执 | 焦点梯度清晰 | 顶部标题区留白偏紧 | 有：“这一笔已经记下了，不用再做什么；要反悔就点下面的「撤销这一笔」。” | 无（仅业务词） | 无 | 78·顶部三行紧贴，缺少呼吸感 |
| 记收回-回执 | 清楚，金额与状态字号最大 | 均匀贴齐，无错位截断 | 有：“这一笔记记下了，不用再做什麼；要反悔就点下面的「撤销这一笔」。”等 | 有：“#收回”“#借出记录编号”“#未还”“记记编号”“查次数” | 无（顶部有调试串残留） | 62·数据库标签原样上屏＋符号堆砌 |
| 记收入-回执 | 重点突出，层级清晰 | 宽松整齐，底部留白略多 | 无（仅卡片标签点列） | 有：顶部调试串“INNR#=390 DOCW=390 VW=390” | 无 | 82·顶部调试信息上屏 |
| 记退款-回执 | 三段递进清晰 | 边缘基线一致，无截断 | 无 | 无（仅标签 #退款） | 无 | 89·元信息行权重落差大，稀释首屏 |
| 记一笔-回执 | 重点清晰，辅助说明不抢戏 | 克制整齐，编号处换行略紧 | 无 | 有：“记录编号”“分类”“金额”“时间”“账户”“账本”“币种”“备注”列名上屏 | 无 | 78·字段表重复卡片信息，占用纵向空间 |
| 拍账单-回执 | 一眼可见 | 贴边对齐，表行留白偏紧 | 无 | 无 | 无 | 92·分类卡窄宽换行略碎 |
| 批量录入-回执 | 颜色突出重点，一眼能找到 | 均匀等宽对齐 | 有：“这一笔已经记下了，不用再做什么；要反向就点下面的「撤销这一笔」” | 无（仅业务词） | 无 | 92·标题区三彩签稍挤，次级注释字号偏小 |

## 三、横向归纳

- 溢出 0/16 页：一票否决零触发，上一轮①②条作废确认，两列卡片无断裂。
- 符号串 7/16 页残留：多为“这一笔已经记下了…；要反悔就点下面的「撤销这一笔」”引导句。
- 内部话 4/16 页：记借出字段名直露、记收回 #标签串、记一笔字段表列名、记收入顶部水印；`bill.*` 上屏 0/16。
- 顶部绿字量数水印（DOCW/VW）是截图水印非页面内容， vision 计入处已按非产物注记。
- 撤销页两组复制按钮如披露所示；字段表与卡片信息重复（记一笔等）是主要纵向浪费。

## 四、小计

- 16 页平均分：84.6；未评数：0（vision 成功 16、失败 0）。
- 撤销页例外是否如披露所示：是，仍两组复制按钮，其余 15 页单组。
