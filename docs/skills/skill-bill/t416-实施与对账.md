# t416 查询接线 · 实施与对账（结构纪律五步 ＋ 双向对账 ＋ 变异自证）

> 票 #416「饼干记账查询接线：17词唤醒词→工作流程（SKILL.md＋HELP）」。本件是**实施证据**：
> 事前影响清单与事后对账逐行对得上（structure.md 第五步是唯一机械验收点），读数全部是当刻盘上跑出来的。
> 认领：`gh issue edit 416 --add-assignee "@me"` 已落（assignee `FeatherHunter`）。
> 研究输入：`.scratch/t415/research-415416417.md`（§2 17词五段＋双向对账脚本形状＋难行预告）＋
> `gh issue view 416` 全文（验收：17词五段对齐双向对账绿；不做真出口断言）。
> 前票已提交（`git log --oneline` 当刻含 `5492265 证据(415)／f41e22e 测试(415)／822710d 功能(415)`＋
> `f93f21b 证据(414)／c119c90 测试(414)／01d56fb 功能(414)`＋`af8be91 证据(412)` 等，只读确认，不改其文件）。

## 一 必报五步

### 第一步 · 影响清单（动手前，逐行一个能力目录）

**新增（本票声明路径，仅两类；src 零改动）**

| 件 | 一句话 | 为什么 |
|---|---|---|
| `packages/skill-bill/test/t416-query-wiring.test.mjs` | 本票判据（7条：17词表／五段全对／HELP查询卡1:1／SKILL块17行／反向对账／最长匹配／反例） | 一票一判据；静态可执行判定，不 spawn（票面不做真出口断言） |
| `docs/skills/skill-bill/t416-reconcile-query.mjs` | 对账脚本（正例`RESULT 17/17` exit0；`--check-phrase`正反探针） | 双向对账的第二实现，与测试同口径互核；只读dist |
| `docs/skills/skill-bill/t416-实施与对账.md` | 本件（实施证据，含五步＋真跑＋变异＋GATE-RUN） | 证据落归属件目录 |
| `docs/skills/skill-bill/t416-锁记录导出.log` | 锁目录中本票条目导出（§六对账源） | 运行记录在未受控目录，导出件当对账源 |
| `.scratch/t416/*` | 窗口五件（`window-1/-3/-4/-c1/-c2/-c3.ps1`照抄t411模板）＋`mutate.mjs`（照抄t411模板改锚点为本票两件）＋日志 | 整窗交锁＋变异自证，永不入库（`.gitignore:5:.scratch/`） |

**不动（逐条有去处）**

| 件 | 为什么不动 |
|---|---|
| `packages/skill-bill/src/policy/wakewords.ts`（`WAKE_TABLE:52-71`） | 17词路由源是唯一上游；本票只读不改（`routeWakeword`最长匹配沿用，只做回归锁） |
| `packages/skill-bill/src/help/lookup.ts`（`buildHelpLookup:43-51`） | SKILL行与CLI的同一函数；本票只引用（`exampleParams`查询分支已能出示例），不动 |
| `packages/skill-bill/src/triggers/wake-assets.ts`（`query`域17卡） | HELP卡载荷是内容源；本票只找`g.id==='query'`下恰一张，不改一字（`LEGACY_DIGEST`未动） |
| `packages/skill-bill/src/query/*`四页实现 | 票面外＋派单“不动四页实现”；t412（13条）／t413（17条）／t414（12条）／t415（9条）复跑全绿即回归 |
| `packages/skill-bill/SKILL.md`（`HELP-AUTO`块） | 禁手改；本票跑`node scripts/build-help.mjs`须逐字节无变化（`SKILL-SAME=True`两窗一致） |
| `packages/skill-bill/src/help/writeWire.ts`／`index.ts` | 写入16词链不断；本票不新建`queryWire.ts`（派单只写三类路径），接线逻辑住测试＋对账脚本双实现（见第二步沉淀判断） |

### 第二步 · 结构设计（目录树／每件职责／导出面／共用件＋沉淀判断）

```
packages/skill-bill/test/t416-query-wiring.test.mjs（新增 258 LF，测试不管辖计数）
  CANON 17（SKILL 51-67表序）＋EXPECT_KEY 17＋EXPECT_SCENE 17＋FULL_CTX 9槽
  findQueryScene(phrase): query域内wake_word全等找，恰1张才返（禁裸子串）
  executableReason(key,cli): 静态JSON解析，不spawn
    today 无参即今天／date非空／recent true（limit缺省10，在1~200才过）
    range week/month 或 start+end同给 或 category/account/ledger单条件
    search q非空 或 kind=tag+tag非空 或 kind=debt/reimburse/installment
    detail id正整数
  buildQueryRows(): 路由→比entry.key→找SKILL行→比BILL_KEY_SHAPES→断CLI前缀→找HELP卡→断可执行；任一步失败写逐字reason
docs/skills/skill-bill/t416-reconcile-query.mjs（新增 203 LF，文档不管辖计数）
  同上CANON／EXPECT／FULL_CTX／findQueryScene／executableReason／buildQueryWire双实现
  正例17行OK＋RESULT 17/17；--check-phrase反例探针（PHRASE-OK exit0／PHRASE-NO-WIRE exit2）
```

- 每个文件对外给的东西≤5（铁律五）：本票**零新增src导出**（`src/help/index.ts`仍2件，`src/query`门仍四处理体）；测试与脚本是判据与工具，不进能力门，不计数。
- 依赖方向：测试／脚本→`policy（门）／render（门）／triggers（沿helpFile.ts先例直引）`向下引用，无环；常量与类型只一处（`WAKE_TABLE`／`BILL_KEY_SHAPES`／`WAKE_GROUPS`全引用，不抄第二份词表）。
- **沉淀判断（本票结论，为什么不新建`src/help/queryWire.ts`）**：
  - 派单只写三类路径（`test/t416-*`＋`docs/.../t416-*`＋`.scratch/t416/*`），`src/help/queryWire.ts`不在声明内；新建即越界，宁可双实现互核。
  - 查询接线逻辑今日唯一消费者是判据与对账（测试7断言＋脚本正反例），单用法不进`help`能力目录（`structure.md`共用件：从第二个用法里长出来，不是预先设计）。
  - 待第二能力（分析域接线或锁票#417）引用同一17词五段之日，再从测试／脚本提升为`src/help/queryWire.ts`（届时补`index.ts`门转出＋`WRITE_WORDS`旁`QUERY_WORDS`，对外≤5，学t409§1）。

### 第三步 · 写代码（改了什么，一行一因）

1. `CANON`：照`WAKE_TABLE:52-71`表序（5 today＋6 range＋5 search＋1 detail），与`SKILL.md:51-67`行序逐行对（顺序差即红，集合一致不够）。
2. `EXPECT_KEY／EXPECT_SCENE`：难行点名（`查账单→today／query_bills`别名无独立场景id，`查账单详情→detail／query_bill_detail`；路由kind与HELP id异名处无猜测，映射逐字）。
3. `FULL_CTX`：9槽全集（id／date／start／end／category／account／ledger／q／tag），缺槽词也能路由过；只做路由，不spawn（票面不做真出口断言）。
4. `findQueryScene`：只在`g.id==='query'`下找，`wake_word===phrase`全等（禁`includes`裸子串，查CSS误命中同族）；命中恰1张，多张／零张即reason点名（含全资产同词计数）。
5. `executableReason`：静态`JSON.parse` CLI的`--params`（today无参即过，其余无参即缺槽）；today／range／search／detail按分支验必需槽位（见第二步）；形状另断（today／range／search＝list，detail＝detail，与`commands.ts`四条一致）。
6. 7断言：17词表派生＋五段全对（逐词reason空）＋HELP卡1:1（`wake_word` 1:1＋`prompt_template`含唤醒词＋`唤醒词`三字）＋SKILL块17行（反引号整段``cli``）＋反向对账（HELP 17卡逐张回查）＋最长匹配（查账单／详情互不吞＋看分类对比／报销到账既有回归）＋反例（无命中抛`无命中`）。

### 第四步 · 超线报警

本包告警线 **350行（LF口径，只数`\n`，`packages/skill-bill/AGENTS.md`）**：

- `packages/skill-bill/test/t416-query-wiring.test.mjs` **LF 258**：测试文件不管辖计数（`structure.md`管辖节），仅报读数，不报警。
- `docs/skills/skill-bill/t416-reconcile-query.mjs` **LF 203**：文档不管辖计数，仅报读数，不报警。
- 未碰件读数沿用#411后复测＋t415实测（`wakewords.ts 142／lookup.ts 55／writeWire.ts 115／commands.ts 53`，均在线内）；挂号两件（`wake-assets.ts 986`、`cmd_read.ts 485`）本票未碰、不扩大。
- 结论：**无超线**（本票零新增src，`已超线，需要根据规则进行重构。`一句不触发）。

### 第五步 · 交付对账（逐行对第一步）

- 第一步“判据”→`t416-query-wiring.test.mjs`已在`2e9a309`落成（新建258行），路径一致。
- 第一步“对账脚本”→`t416-reconcile-query.mjs`已在`a5028e7`落成（新建203行），路径一致。
- 第一步“本件＋锁导出＋scratch”→本件与`t416-锁记录导出.log`待c3落成；`.scratch/t416/*`永不入库（ignore），与第一步一致。
- 未碰件（`WAKE_TABLE／lookup／wake-assets／query四页／SKILL.md字节`）：`git diff 2e9a309 -- <未碰件>`干净（多席他票在途件与本票无关，本票提交一律`-- <自己路径>`限定）。**偏差为零**。

## 二 真跑读数（全部经加锁包装器）

### 窗口一（`0e04d469`）：编译→HELP注入→重生成→门对账→靶向→全量→对账正反例

| 命令 | 退出码 | 摘要行 |
|---|---|---|
| `node node_modules/typescript/bin/tsc -b packages/skill-bill` | 0 | 零诊断（窗口一＋窗口四两处一致） |
| `node scripts/build-help.mjs` | 0 | `SKILL-SAME=True`（前后SHA256一致，手写词未动） |
| `node scripts/gen-cli.mjs` | 0 | `命令6条；能力2个（query／record）` |
| `node scripts/gen-cli.mjs --check` | 0 | `GEN-CHECK PASS：命令6条` |
| `node --test test/t416-query-wiring.test.mjs` | 0 | `tests 7 / pass 7 / fail 0`（17词表／五段／HELP1:1／SKILL块／反向／最长匹配／反例） |
| `node --test`（包内全量，窗口一） | 0 | `tests 252 / pass 252 / fail 0`（245基线＋7本票） |
| `node docs/skills/skill-bill/t416-reconcile-query.mjs` | 0 | `RESULT 17/17`（17行`OK 词→key ｜SKILL[shape] cli｜HELP[query_*]`） |
| `... --check-phrase 查标签` | 0 | `PHRASE-OK：查标签`（正探针） |
| `... --check-phrase 同步记账到云端` | 2（预期） | `PHRASE-NO-WIRE：同步记账到云端`（反例点名） |

17行明细见`.scratch/t416/window-1.log`（`OK 查今天→today ｜HELP[query_today]`…`OK 查账单详情→detail ｜HELP[query_bill_detail]`）；
关键难行：`查账单 → bill.record.today {} ｜SKILL[list] bill-cmd-read bill.record.today ｜HELP[query_bills]`（别名无参同页）与
`查账单详情 → bill.record.detail {"id":1} ｜SKILL[detail] …｜HELP[query_bill_detail]`（5字长者胜，映射点名，无猜测）。

### 窗口四（`99698a1e`）：还原后复绿（重编＋靶向＋全量＋对账＋HELP同字节＋gen:check）

| 命令 | 退出码 | 摘要行 |
|---|---|---|
| `tsc -b` | 0 | 零诊断 |
| `build-help` | 0 | `SKILL-SAME=True` |
| `gen-cli --check` | 0 | `PASS：命令6条` |
| `t416靶向` | 0 | `tests 7 / pass 7 / fail 0` |
| `全量` | 0 | `tests 252 / pass 252 / fail 0` |
| `reconcile` | 0 | `RESULT 17/17` |

等待量：窗口四`waitedMs=40029`（`WAIT-OWNER-ALIVE pid=42552 ticket=444`，活进程一律不抢回，等它释放；15秒重试、10分钟上限，本次约40秒后取到锁，无超时抢回、无危险git命令）。

## 三 变异自证（改坏必红 → 从HEAD取回必绿）

工具`.scratch/t416/mutate.mjs`（照抄`.scratch/t411/mutate.mjs`改锚点为本票两件）：锚点命中数≠1即拒绝落盘；还原因`git show HEAD:<件>`逐字节取回。
路径所有权说明：本票声明外无源码改动，故变异位落在“本票自己的判据实现”上（M1证测试的HELP断言有效，M2证对账脚本的形状断言有效；不动他人src）。

| 变异 | 改的是什么 | 变异后 | 还原后 |
|---|---|---|---|
| M1 | 测试侧HELP判定改坏（`if (g.id !== 'query')`→`'query-mut'`，查询域永无命中） | 靶向**7跑／4过／3红**，红在五段＋HELP1:1＋反向三断言：实得`HELP 查询卡缺位或不唯一（全资产同词1张）：查今天` | dirty dist无关（测试改动不进dist）→从HEAD取回后**7/7绿**（窗口三`testRes=0`） |
| M2 | 对账侧形状期望改坏（`detail?'detail':'list'`→`detail?'list':'list'`，detail被压成list） | 对账**RESULT 16/17 exit1**，红在`NG 查账单详情→detail ｜REASON：形状对不上：detail≠detail（期望list）`；测试侧未动仍绿（`testMut=0`） | 同上**RESULT 17/17**（`reconRes=0`） |

```
MUTATION m1 apply=0 testMut=1 reconMut=0 restore=0 testRes=0 reconRes=0
MUTATION m2 apply=0 testMut=0 reconMut=1 restore=0 testRes=0 reconRes=0
WINDOW3-SUMMARY preClean=0 postClean=0 all=0
```

- `pre=0／post=0`：变异前后两件与HEAD逐字节一致（`DIFF-CLEAN`）。
- `testMut=1／reconMut=1`即变红（非0＝已变红）；`all=0`即还原后全量复绿（`tests 252/pass 252/fail 0`）。回滚＝取回＋（测试改动不进dist，无需重编；若动src则重编，本窗无此步）。

## 四 走偏与整改（如实记，别当没发生）

1. **暂存区他席在途件**：c1／c2窗口`git diff --cached --name-only`均见他席件（c1见`docs/agents/t575-替换-证据.md`，c2见该件＋`docs/skills/skill-calorie/t443-复核.md`），均为他人已暂存未提交的在途件。本票提交一律`git commit -F … -- <自己路径>`限定，实得`1 file changed`（c1测试／c2脚本各一件），未卷入他席件（t415第四节第3条同律）。
2. **窗口四等锁40秒**：`WAIT-OWNER-ALIVE ticket=444`，按协议活进程一律不抢回，等它释放后取锁（`waitedMs=40029`）；未改轮询间隔，未抢回，不记违规。
3. **查区间路由params与SKILL示例end差一天**：对账明细`查区间→range {"start":"2026-09-01","end":"2026-09-30"}`（FULL_CTX） vs `SKILL … '{"start":"2026-09-01","end":"2026-09-01"}'`（示例）。两者皆满足“start+end同给”分支，`executableReason`只验非空不同值比对，不逐值比对；属预期差（路由补槽≠示例槽），非漂移。
4. **提交信息无BOM**：三笔提交信息文件一律经`write`工具落盘（无BOM），`git commit -F`引用；提交前一律`git diff --cached --name-only`复核（见各`window-c*.ps1`日志）。

## 五 边界与不做（本票范围外，逐条有去处）

- **真出口断言**：17词`exit 0`真跑是#417（锁票，程序钉死＋用户肉眼终审）；本票只静态验示例带齐槽位，不spawn（票面“不做真出口断言”原话）。
- **四页实现**：today／range／search／detail的处理体、字段表、KPI、复制区一字不动；t412／t413／t414／t415复跑全绿即回归（窗口一全量252含四票判据）。
- **SKILL／HELP生成链**：`HELP-AUTO`块禁手改；改后全链重跑加`gen:check`绿（窗口一`SKILL-SAME=True`＋`GEN-CHECK PASS`，窗口四复核一致）。
- **HELP卡片CLI格可执行化**：卡片仍显示场景文案（base冻结口径，回补归#106系，遗留432）；本票只给映射不断言卡片改字（学t409§六）。
- **单源链仍为“双源＋对账缝合”**：路由源`WAKE_TABLE` vs内容源仓外实物，本票只加17词直连缝合（测试＋脚本双实现），不断生成器链（G1–G8留后票，见研究§2.3）。

## 六 GATE-RUN逐行声明（锁记录导出）

锁目录`.scratch/locks`（`gate-runs.log`），本票5次持锁门禁运行已落盘（10行＝5 START＋5 RUN，全部`exit=0`；导出件`t416-锁记录导出.log`；本件提交c3的两行在锁日志中，提交后以`Select-String gate-runs.log -Pattern <runId>`复核）：

- `0e04d469-6fb9-4194-a879-ab393d8f7b35`（窗口一：编译／HELP／重生成／门对账／靶向7/7／全量252／对账17/17＋双探针）→`exit=0`，`WINDOW1-SUMMARY tsc=0 help=0 skillSame=True gen=0 gencheck=0 t416=0 all=0 rec=0 probeOk=0 probeNo=2`。
- `24f60005-f758-4f2b-ad8d-4c0cd7670657`（提交c1：测试一件）→`2e9a309`。
- `c4372aec-87ed-4051-b876-98649b6b0146`（提交c2：对账脚本一件）→`a5028e7`。
- `cdaee025-a974-40fc-b505-469e849c9da5`（窗口三：变异M1／M2红→还原→diff干净→全量复绿）→包装器`exit=0`，`WINDOW3-SUMMARY preClean=0 postClean=0 all=0`。
- `99698a1e-8f60-4f39-af12-38acca5f4daa`（窗口四：重编＋靶向＋全量复绿）→`exit=0`，`WINDOW4-SUMMARY tsc=0 t416=0 all=0 rec=0 skillSame=True gencheck=0`（`waitedMs=40029`，见第二节）。
- 本件提交c3的两行（START／RUN）在锁日志中，提交后可查。

提交（逐小步持锁，已落两笔）：`2e9a309 测试(416)：t416-query-wiring 17词五段＋反向＋最长匹配＋反例`／`a5028e7 脚本(416)：t416-reconcile-query 双向对账17/17＋探针`（各`git diff --cached --name-only`见各窗口日志，提交均`-- <自己路径>`限定，实得各`1 file changed`）。

## 七 交棒（下一手要什么）

- 读盘入口：判据`packages/skill-bill/test/t416-query-wiring.test.mjs`（`CANON／EXPECT_KEY／EXPECT_SCENE／FULL_CTX／findQueryScene／executableReason／buildQueryRows`）；对账`docs/skills/skill-bill/t416-reconcile-query.mjs`（同口径第二实现）；变异工具`.scratch/t416/mutate.mjs`。
- 下一票（#417锁）读的是17词真出口（`dist/cli/cmd_read.js` spawn＋`--html`落盘＋`delivery.path／bytes`＋字段＋空态／阻断同步断言）；别动`commands.ts`形状（形状是契约）与本票静态判定（`executableReason`是静态可执行，不是真跑）。
- 沿革债（非本票引入，不扩大）：`src/cli/cmd_read.ts 485`与`wake-assets.ts 986`超线挂号未动；`scripts/build-help.mjs:17`相关场景行“16联动”仍写16（实77词，待生成器链票）；`SKILL.md:109`同句待同票。
