# t407 · 整改验证（第二轮）—— 核 `65bae46`（源码轴）与 `263899f`（文档轴）

验证人：复核窗口（只读）。本件只新建，不改任何源码／文档，不提交。
被验对象：`65bae46`（14 件）＋ `263899f`（1 件，＋76／−20）。整改账本＝`docs/skills/skill-bill/t407-整改-十六页复核项.md`（239 行）。

---

## 一、F1 逐条核验

### 1. S1 拆件到位 —— **成立**

| 件 | LF 行数（`readFileSync(f,'utf8').split('\n').length-1`） | 对外给的名字（本目录之外真被引用的） |
|---|---|---|
| `src/shared/photoEscape.ts` | **179** | **3**：`valuesOf`（`:100`）／`fieldCardOf`（`:148`）／`blockedPromptOf`（`:165`） |
| `src/shared/outsideScan.ts` | **137** | **5**：`PhotoScale`（`:32`）／`ESCAPE_FIELDS`（`:40`）／`imageNote`（`:51`）／`escapeCard`（`:70`）／`escapePrompt`（`:113`） |

自己数的依据（不是抄自称）：`Select-String '^export'` 两件分别命中 3／5 条；另一侧从调用点反查——
`src/record/scene-income.ts:31`、`scene-reimburse.ts:30`、`scene-plain.ts:32`、`scene-photo.ts:35` 四处 `import` 的
`photoEscape` 名字正好是那 3 个；`scene-photo.ts:36-37` 引的 `outsideScan` 名字正好是那 5 个（含 `import type { PhotoScale }`）。
两侧都 ≤ 5，与自称「3 名／179 行」「5 名／137 行」逐字相符。

### 2. S2 取数唯一 —— **部分成立**（见第三节）

`grep` 全包（`packages/skill-bill/src/**` ＋ `test/*.mjs`）数定义地：

| 名字 | 定义地 | 结论 |
|---|---|---|
| `pickOf` | `src/shared/recentPicks.ts:43`（真定义）；`src/shared/photoEscape.ts:58`、`src/record/collectBody.ts:44` 各一行转发 | 转发成立（两处都读原文确认是 `return pickValues(...)` 一行，转发时分别用 `categorySideOf(kind)`／`kind === 'expense' ? EXPENSE_L1 : ALL_L1` 定退哪一侧名单） |
| `optionsFor` | `src/shared/recentPicks.ts:58` **唯一** | 成立 |
| `distinctOf` | `src/shared/recentPicks.ts:31` **唯一**（收在件内，不外给） | 成立 |
| `PICK_LIMIT` | `recentPicks.ts:22`＝12；**另有** `src/shared/recordPicker.ts:43`＝20 | 字面两处，但两个不同概念（候选列表一页几条／选择器候选取几个），整改记录第二节补充读数与第七节第 4 条**已自报** |
| `textOf` | `recentPicks.ts:25`（给出去的那个）＋ `src/record/scene-*.ts` **15 处**（另有 `src/record/collect.ts:20`）＋ `src/shared/diffTable.ts:35`、`src/shared/prefillNote.ts:40`，共 **19 处** | 「只剩一处定义」按全包字面不成立；但这 15 处的边界正是整改记录第七节第 2 条自报的口径（三样口径、硬并会改行为），不是隐瞒 |

即：**「采集页取数这一族只剩一处定义 ＋ 两侧各一行转发」成立；「这组取数只剩一处定义」这句按全包字面不成立**（`textOf` 19 处、`PICK_LIMIT` 2 处），但两处边界都在件内自报过。

### 3. `src/shared/` 跨目录引用数 —— **成立（0）**

`Select-String -Path "packages\skill-bill\src\shared\*.ts" -Pattern "from '\.\./record/"` → **0 行命中**，与自称 0 相符。

### 4. E1 护栏真的在（本单核心）＋ 我的独立变异 —— **成立**

护栏落点读原文确认（`test/shared-judgments.test.mjs:69-90`）：

- `:69` `describe('t407 E1 · 撤销／恢复候选的过筛（recordPicker.ts 的 keeps）')`；
- `:70` 「撤销候选只列未删：软删那笔一个都不出现、条数按未撤销数报」——断言 `!html.includes('zzgone')`（`:76`）、
  `'库里共 3 条未撤销的记录'`（`:77`）、`'按时间倒序'`（`:78`），且缺省与 `op:'undo'` 两态各跑一遍（`:71`）；
- `:82` 「恢复候选只列已软删：没打标的那三笔一个都不出现、条数按已打标数报」——断言 `zzgone` 在、三笔未删都不在、条数 1。

被钉的判定／件原文：`src/shared/recordPicker.ts:81-83` `keeps(mode,row) = mode === 'restore' ? isDeleted(row) : !isDeleted(row)`。

**我自己改坏它（本次唯一允许碰源码的动作，已还原并自证）**：把 `:82` 的那一半改成 `: true`：

```
【变异态】 tsc exit=0（输出 0 行，零诊断）  node --test exit=1  tests 116 / pass 115 / fail 1
          红着的用例：撤销候选只列未删：软删那笔一个都不出现、条数按未撤销数报
【还原态】 tsc exit=0（--force 全量重建）   node --test exit=0  tests 116 / pass 116 / fail 0
```

读数落 `.scratch\t407-verify3\tsc-mut.txt`／`test-mut.txt`／`tsc-restore.txt`／`test-restore.txt`。
**结论：这条护栏这次真的长出来了**——改坏必红，且红的是**断言不是类型**（同一个变异态 `tsc` 零诊断）。
附带独立印证：变前 `recordPicker.ts` 的 sha256 为 `37df28f5488a6a8b4a14cedeac12a906ccc61dfadb03c7bcb159dba1a09c02f5`，
与整改记录自报的前 8 位 `37df28f5…` 一致。

### 5. E3 两页加注 —— **成立**

| 页 | 装配代码里的加注 | 产物里的加注 | 字节数 |
|---|---|---|---|
| 记分期采集页 | `src/record/scene-installment.ts:148`「补齐总价／期数／首期日后，本页显示分摊预览（每期金额与日期）。」 | `docs/skills/skill-bill/t407-页-记分期-采集页.html` 命中该句**逐字 1 处** | **71786**（自称 71786） |
| 批量录入采集页 | `src/record/scene-batch.ts:128`「多行待接：这一页会把一屏里的多行都铺出来给你改，但「一屏多行一次落库」这一段还没接，…（重跑时只带那一行要的值）。」 | `docs/skills/skill-bill/t407-页-批量录入-采集页.html` 命中「「一屏多行一次落库」这一段还没接」**逐字 1 处** | **70858**（自称 70858） |

批量录入那句是把「一屏能铺多行」与「一次只落一笔」的差别写成读者看得懂的话，不写只有本行读得懂的参数名——核过，属实。

### 6. 对账（影响清单 ↔ `git show --stat 65bae46`）—— **成立，偏差 0**

`git show --name-only 65bae46` 得 **14 件**；第一节影响清单第 1–13、15 行也是 **14 件**。逐名比对（脚本核）：
`missing from stat: []`、`extra in stat: []`，件名与「新增／改」全部对上。清单第 14 行（`t407-恢复-稳定化证据.md`）不在本提交，
归另一窗口——与 `263899f` 只动那一件的事实互证。**偏差＝0，成立。**

### 7. 读数 —— **成立**

```
npx tsc -b packages/skill-bill --force            → 退出码 0，输出 0 行（零诊断）
node --test "packages/skill-bill/test/*.test.mjs" → 第一次 exit 0  tests 116 / pass 116 / fail 0
                                                    第二次 exit 0  tests 116 / pass 116 / fail 0
```

与自称「tsc 退出码 0（零诊断）＋ 116 通过／0 失败」逐字相符。测试件本身 `it(` 计 26 条、`describe(` 计 5 组，与「26 条用例、五件」相符。

---

## 二、F2 逐条核验

### 1. 五处改正逐处核 —— **五处全部到位**

| # | 改正项 | 落点（`docs/skills/skill-bill/t407-恢复-稳定化证据.md`） | 判定 |
|---|---|---|---|
| ① | 影响清单去掉 `SKILL.md`、改成实话 | `:277` 清单已无 `SKILL.md`、场景件数由「16 个」改「15 个」；`:280-286` 订正段：SKILL.md 一字未改（依据 `git diff 5930127^ 5930127 -- packages/skill-bill/SKILL.md` 为空；处置是重跑 `scripts/build-help.mjs` 后与 HEAD 逐字相同 ⇒ 无净改动）；另 `:316` 交棒处再述一次 | **成立**（我复核：该 diff 0 行；`5930127` 的 22 个包内件里有 0 个 `SKILL.md`） |
| ② | 场景件件数改「16 件在盘／本票改动 15 件」 | `:283-285`：逐件核 `git show --stat 5930127`，16 件在盘、改动 15 件，没动的 2 件＝`scene-expense.ts` 与落点表 `scene.ts`；并写明与复核件的「14」差 1、以逐件核为准 | **头条数字成立**（见第 3 条）；收尾那句「没动的 2 件」不自洽，计入第三节 |
| ③ | 删掉不可复算的绝对段数（原值留痕） | `:68` 改为「首轮有几段对不上，按差集逐条改回去，最终四张页对上」；`:70-79` 原为一段代码块（四张页 `oldSeg`／`newSeg`＝199／221／168／180、首轮有差 0／2／0／3、末行 `BAD=0`）已删，但在**同一处**逐字留痕，并写明为什么删（复核件按同一条跑法复算得 251／286／203／225，与上一组不成比例 ⇒ 不能独立复算），改立两条当场可核的结论：坏字 0（`git grep -I -e <U+FFFD> / -e 锟` 于 `5930127 -- packages/skill-bill` 两条都无输出）＋ 四张基准页 blob 同哈希 ⇒ 相对差 0 | **成立**（原值留痕，删得有据） |
| ④ | 行数两组口径并列 | `:232-236` 订正写明数法（本节与第三节表同一条＝LF 含空行；空行不计口径逐件低 1～19 行；两组都不算错，只是口径不同）＋ 复核件点名的三件并列 `scene-income.ts` 198／191、`scene-reimburse.ts` 247／237、`src/help/lookup.ts` 55／50；`:258` 表内三行各自加注「（空行不计＝…）」；`:85`／`:101` 两张表头加「含空行口径」 | **成立**（我在 `5930127` 上复算三件：198／191、247／237、55／50，三组逐字相符） |
| ⑤ | `src/policy/lookup.ts` 的订正说明 | `:52-55`：路径逐处核过，本件 5 处引用全是 `src/help/lookup.ts`（第六节那处简写 `help/lookup.ts`），件内**没有** `src/policy/lookup.ts` 这一串，并给出证据命令与「`src/policy/` 下是 `wakewords.ts`／`category.ts` 等七个件」 | **成立**（我复核：`git rev-parse 5930127:packages/skill-bill/src/policy/lookup.ts` → exit 128，`fatal: path … does not exist`；件内该串只在订正语境出现 3 回＝`:53`／`:54`／`:341`，没有一处拿它当有效路径用） |

另：`:334-347` 新增第九节「两轴复核与本件改正」，交代五处改正并把第八节留给源码轴的 `t407-整改-十六页复核项.md`（节号从七跳到九，属实）。件为 348 行、无 BOM。

### 2. 有没有动历史读数 —— **没动**（逐处判）

看的是 `263899f` 的**完整 diff**（191 行文件，含 96 行改动），逐处判：

| 历史读数 | 判 |
|---|---|
| **真跑读数**：第四节「30 份产物读数」表（成功份数 30／30、30 份全部 exit 0、全部 ≥ 50KB 及字节表）、页内自述文字 | **原样保留**——整份 diff 没有任何一条 hunk 落在这些行上 |
| **变异读数**：第五节「变异自证」（变异甲／变异乙）`:199-229` | **原样保留**——无 hunk 触及 |
| **提交号**：第七节交棒 `:308` 的 `5930127fe867a9378a84b535f9d77c170f0c8a93`（及 `:286` 的统计口径） | **原样保留**——无 hunk 触及 |
| 唯一被删的历史读数：第二节那组绝对段数（199／221／168／180、0／2／0／3、`BAD=0`） | **删了但在原处逐字留痕**，并说明删除理由；不存在「删得看不出改过什么」——同一个 `@@` 块里就写着原值 |
| 附带的措辞弱化：「最终四张页**全部 0 差**」→「最终四张页对上」 | 同一处的订正说明了新判据（四张基准页 blob 与 `5930127` 逐份同哈希 ⇒ 相对差 0），属**有据弱化**，不是抹数 |

`:347` 那句「真跑读数、变异读数、提交号、30 份产物的字节表与页内自述文字一律保留原样」——**核过，属实**。

### 3. 它报的那处分歧：场景件改动数 —— **我的结论＝15 件**

依据（自己数，不看自称）：

1. `git show --name-only 5930127 -- packages/skill-bill/src/record` → **15 个 `scene-*.ts`**：`scene-batch`／`scene-borrow`／`scene-collect`／`scene-income`／`scene-installment`／`scene-lend`／`scene-photo`／`scene-plain`／`scene-refund`／`scene-reimburse-done`／`scene-reimburse`／`scene-repay`／`scene-restore`／`scene-undo`／`scene-update`。
2. `git show --numstat 5930127 -- …/src/record`：这 15 件每件都是 9～14 行净删、198～268 行净增，**没有一件是空改**（不可能把其中一件数掉）。
3. 盘上 `scene-*.ts` 共 **16 件**，唯一没被本票动的是 **`scene-expense.ts`**（22 行，两格指通用装配体）；`scene.ts` 是落点表（108 行），**不是 `scene-*.ts` 计数里的一件**，本票也没动它；`scene.ts` 的 import 恰好 16 个场景件模块，与「16 件在盘」对得上。
4. 复核件自己写的算式是「16 件在盘 − `scene-expense.ts` 未碰 ＝ 14 件」（`t407-复核-十六页-结构纪律.md:13`／`:50`／`:60`）——**这一步减错了**，16−1＝15。F2 引述复核件写「14」属实，F2 改成 15 **站得住**。

**结论：15 件。**（F2 对，结构轴复核件的 14 错。）
小瑕疵：F2 那段收尾写「没动的 2 件＝`scene-expense.ts` …与落点表 `scene.ts`」，与自己上一句「16 件在盘、本票改动 15 件」不自洽（未动的**场景件**只有 1 件；`scene.ts` 不是场景件）。不影响头条数字，计入第三节。

### 4. 库态前提是否标明是反推 —— **标明了**

- `:147` 小节标题即写「库态前提（订正：原节没写，F2-3）」；
- `:153` 表头逐字为「页内自述读数（逐字）｜**反推出的库态**」；
- 正文明说「当时那个库的运行期读数（总行数、软删行数、候选条数）**本件没留**——本件全程没记过库文件读数」，
  四行都是「页内自述 → 反推」成对写法（撤销／改记录页 2 条候选 ⇒ 产出时库里 2 条记录、0 条软删；恢复页空态与撤销页互证；记收入／记报销「还没有带账户的记录」⇒ 那 2 条都没有账户字段）。
- 另写明洁净库复现的预期（7／30 份会有数百至数千字节差）与**哪两处不能归因**（拍账单-回执页、记一笔-回执页）。

⇒ **不是伪装实测**，反推与实测分得清。

---

## 三、不成立或部分成立的条目

| 条目 | 判定 | 具体 |
|---|---|---|
| **F1-2（S2 取数唯一）** | **部分成立** | 「采集页取数这一族只剩一处定义 ＋ 两侧各一行转发」成立；但「`pickOf`／`PICK_LIMIT`／`textOf`／`distinct`／`optionsFor` 这组取数**只剩一处定义**」按全包字面不成立：`textOf` 全包 **19 处**定义（`recentPicks.ts:25` ＋ `scene-*.ts` 15 处 ＋ `src/record/collect.ts:20` ＋ `shared/diffTable.ts:35` ＋ `shared/prefillNote.ts:40`），`PICK_LIMIT` **2 处**（`recentPicks.ts:22`＝12、`recordPicker.ts:43`＝20）。两处边界整改记录**都已自报**（第七节第 2 条、第二节补充读数与第七节第 4 条），属口径写宽，不属隐瞒；若要求字面为真，可把该句收成「采集页取数这一族」。 |
| **F2-1 第②处的收尾句** | **部分成立（低危）** | 「16 件在盘、本票改动 15 件」正确（我已独立核出 15）；但紧接的「没动的 2 件＝`scene-expense.ts` 与落点表 `scene.ts`」与自己上一句不自洽——未动的场景件只有 1 件，`scene.ts`（108 行落点表）不在 16 件场景件之内。改法＝把「没动的 2 件」改成「场景件里没动的 1 件＋没碰的落点表 `scene.ts`」。 |

其余 F1 第 1、3、4、5、6、7 条与 F2 第 1–4 条（含五处改正、历史读数未被改、库态前提标明反推）**全部成立**。

**变异探测的还原自证（三条，均空／一致）**：

| 自证 | 读数 |
|---|---|
| `git diff --stat -- packages/skill-bill` | **空**（输出 0 行） |
| `git status --porcelain -- packages/skill-bill` | **空**（输出 0 行） |
| 改过文件 sha256 与备份一致 | 变前＝还原后＝备份＝`37df28f5488a6a8b4a14cedeac12a906ccc61dfadb03c7bcb159dba1a09c02f5`（`src/shared/recordPicker.ts`） |

还原后按 `--force` 全量重建再跑：`tsc` 退出码 0、测试 116／0。

---

## 四、结论

**放行。**（F1 无严重项、无「不成立」项；F2 五处改正到位、历史读数未被篡改。）
本轮（16 张定制页 ＋ 两轴复核整改）**成立**，可据以回写 #407／#408 并收口（第三节那两处口径瑕疵属写宽/解释句，够不上再来一轮整改）。

**场景件改动数：我核出的是 15 件**（`5930127` 里改动的 `scene-*.ts`；16 件在盘，唯一未动 `scene-expense.ts`）。
