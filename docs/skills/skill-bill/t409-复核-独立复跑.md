PASS · 96分（契约28＋证据24＋对照20＋红线15＋文档9，无S1，均分≥85）——409 独立复核（非作者独立重跑）

> 复核提交：`b8b2241`（feat/src）＋`c9eae02`（test/对账脚本）＋`bd7c818`／`f22dd39`（docs/证据＋门禁导出）；
> HEAD `e7f7e7d` 未碰 bill 件，提交内容即当刻内容（复核前后 `git status` 干净）。
> 本席重跑：build＋三套测试＋五段对账正反例＋自设探针 8/8＋新变异（去 write 守卫）试完还原重编重签。

## 一 · 复跑读数（GATE-RUN 一对一，摘要行即判据）

| RUN（本席） | 命令 | 退出码 | 摘要 |
|---|---|---:|---|
| `runId=t409R-build1` | `npx tsc -b packages/skill-bill` | 0 | 空日志干净构建 |
| `runId=t409R-help1` | `node packages/skill-bill/scripts/build-help.mjs` | 0 | `SKILL-same=True` |
| `runId=t409R-t409` | `node --test …/test/t409-wire.test.mjs`（套A） | 0 | `pass 7 fail 0` |
| `runId=t409R-stock3` | `node --test wake-assets＋policy＋skill`（套B） | 0 | `tests 15 pass 15` |
| `runId=t409R-full`／`t409R-full2`（还原后） | `node --test …/test/*.test.mjs`（套C） | 0 | `tests 145 pass 145 fail 0` |
| `runId=t409R-recpos` | `node docs/skills/skill-bill/t409-reconcile.mjs` | 0 | `RESULT 16/16` |
| `runId=t409R-recneg` | `… --check-phrase 同步记账到云端` | 2（预期） | `PHRASE-NO-WIRE：同步记账到云端` |
| `runId=t409R-recprobe` | `… --check-phrase 记支出` | 0 | `PHRASE-OK：记支出` |
| `runId=t409R-probe` | `node .scratch/t409/review/t409-review-probe.mjs`（自设） | 0 | `PROBE-RESULT 8/8` |
| `runId=t409R-mut` | 去 write 守卫变异＋重建＋t409 单测 | 0 | `pass 7 fail 0`（存活，见四 S3-1） |
| `runId=t409R-restore` | 还原＋重建＋重签＋t409＋对账 | 0 | `RESTORE-SAME=True` 重建0 重签0（`SKILL-same=True`）`pass 7 fail 0`＋`RESULT 16/16` |

等待量（`.scratch/t409/review/gate-wait.log`）：首轮等 15 秒获锁未果（253F2 持锁变异中，活锁不抢），
次轮等约 165 秒获锁；中途一次竞态落空即重排，无超时抢回、无危险 git 命令。
对账源导出件：`docs/skills/skill-bill/t409-复核-GATE-RUN.log`（§2.3）。

## 二 · 逐条路径（改动／证据／日志）

- 改动件（本票，复核确认与提交一致）：`packages/skill-bill/src/help/writeWire.ts`（新增 115 行，LF 口径远低于 350 线，
  门 `help/index.ts` 对外 5 件达标）／`src/help/index.ts`（＋1 行转出）／`test/t409-wire.test.mjs`（7 断言）／
  `docs/skills/skill-bill/t409-reconcile.mjs`（只读 dist）／`t409-接线-证据.md`＋`t409-GATE-RUN.log`。
- 复核证据件（本次入仓）：本件＋`t409-复核-GATE-RUN.log`（RUN 导出）＋`t409-复核-探针.mjs`（可复跑脚本，P1–P4）。
- 日志件（备查未入仓，`.scratch/t409/review/`）：`gate-wait.log`／`gate-runs-review.log`／`build.log`／
  `build-help.log`／`test-t409.log`／`test-stock3.log`／`test-full.log`／`test-full-after.log`／
  `reconcile-pos.log`／`reconcile-neg.log`／`reconcile-probe-pos.log`／`review-probe.log`／
  `mut-build.log`／`mut-red.log`／`restore-build.log`／`restore-resign.log`／`restore-green.log`／
  `REVIEW-SUMMARY.log`。红线：只写复核自有路径，他人路径未碰，未装包；源码仅一次临时变异且已还原。

## 三 · 缺陷（归属标注）＋未做与下一手

- **S3-1（本票范围）**：`findWriteScene` 的 write 域守卫无测试覆盖——去守卫变异存活（`t409R-mut` 仍 7/7 绿）。
  功能无损（探针 P3：16 词在全资产里精确匹配各恰一张，守卫当前恒真），转后票：把本复核探针 P2/P3 转正为单测。
- **S3-2（本票范围）**：实施 RUN 行未进共享锁日志（共享 `gate-runs.log` 无 `ticket=409` 行，只有票内导出件）。
  本席全量复跑结论与导出件一致，无分歧不阻断；转后票：统一经共享锁日志留痕。
- **S3-3（本票范围）**：feat（新增 src 115 行）无 changeset 版本记账；转后票：确认是否补 patch。
- **范围外发现（不判 FAIL，转票措辞）**：`lookupWake` 用 `includes` 无最长排序，与 `routeWakeword` 全局最长不一致
  （本票 16 词链走后者，不受影响）；HELP 卡 CLI 格仍 `scene.id` 原文（遗留 432 已知）；真出口断言未做（遗留 410 已知）。
- **未做**：实施方变异红轮（`write-mut`→fail 2）备查未重跑（本席用正交变异覆盖牙齿）；未碰 base-render／旧测试／SKILL 字节（票面边界，复核确认干净）。
- **下一手缺什么**：410 真出口断言／432 卡片 CLI 可执行化／本探针转正，三者各需一票，互不前置。
