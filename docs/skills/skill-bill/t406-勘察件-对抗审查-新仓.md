# T406 勘察件对抗审查（独立复核 · 新仓）

审查对象：
- `docs/skills/skill-bill/t406-base组件总表.md`（145 行）
- `docs/skills/skill-bill/t406-共用件依赖与提升改造清单.md`（179 行）

审查口径：只信源头（`packages/base-render/`、`packages/skill-calorie/`），被审件的自述一律回源重核。本次不修改任何已有文件，只新建本件。

## 一、抽样与重核表

### A. `t406-base组件总表.md`（抽样 22 条）

| # | 断言摘要 | 它给的行号 | 我去哪核的 | 源头实际是什么 | 三态 |
|---|---|---|---|---|---|
| A1 | `blocksCss` 由区块层导出，签名为 `(input?: BlocksCssInput): string` | `src/blocks.ts:1312` | `blocks.ts:1312` 直接读 | `export function blocksCss(input?: BlocksCssInput): string {`，与件里逐字一致 | 相符 |
| A2 | `BLOCK_STYLE_SECTIONS` ＋ `BlockStyleSection` 配套 | `blocks.ts:145`／`:160` | `blocks.ts` 按名字定位 | `:145 export const BLOCK_STYLE_SECTIONS = [`；`:160 export type BlockStyleSection = (typeof BLOCK_STYLE_SECTIONS)[number];` | 相符 |
| A3 | `renderPageShell` 从 `base-paint/blocks` 出 | `blocks.ts:207` | `blocks.ts:207` | `export function renderPageShell(input: PageShellInput): string {`，行号精确 | 相符 |
| A4 | `renderDataTable` 从 `base-paint/blocks` 出 | `blocks.ts:341` | `blocks.ts:341` | `export function renderDataTable(input: DataTableInput): string {`，行号精确 | 相符 |
| A5 | `renderCopyBlock` 从 `base-paint/blocks` 出 | `blocks.ts:740` | `blocks.ts:740` | `export function renderCopyBlock(input: CopyBlockInput): string {`，行号精确 | 相符 |
| A6 | `renderFeedbackBlock` 从 `base-paint/blocks` 出 | `blocks.ts:780` | `blocks.ts:780` | `export function renderFeedbackBlock(input: FeedbackBlockInput): string {`，行号精确 | 相符 |
| A7 | `renderStatusBadge` 由包根出，且在 `controls.ts` 实现 | `controls.ts:1405`；导入路径 `base-paint` | `controls.ts` ＋ `src/index.ts` | `:1405 export const renderStatusBadge: RenderStatusBadge = (input) => {`；`index.ts:26` 确在包根导出清单内 | 相符 |
| A8 | `buildDataText`／`buildLogText` 由包根出 | `text.ts:434`／`:470` | `text.ts` | `:434 export function buildDataText(...)`；`:470 export function buildLogText(...)`；`index.ts:32` 二者同出口 | 相符 |
| A9 | `fillTemplate` 来源行号 | `template.ts:218` | `template.ts:218` | `export function fillTemplate(input: FillTemplateInput): FillTemplateOutput {` | 相符 |
| A10 | `escapeHtml` 来源行号 | `contract.ts:38` | `contract.ts:38` | `export function escapeHtml(s: string): string {` | 相符 |
| A11 | 包名 `base-paint`，对外五个开口 | `package.json:2`／`:8-14` | `package.json` 整读（30 行） | `:2 "name": "base-paint"`；`:8-14` 恰五条：`.`→`dist/index.js`、`./blocks`→`dist/blocks.js`、`./help-shell`→`dist/helpShell.js`、`./save-html`→`dist/output/saveHtml.js`、`./package.json` | 相符 |
| A12 | **硬口径**：`blocksCss()` 必须拼进 `TemplateAssets.sharedCssText` 再交 `fillTemplate`，不得走 `StyleSheetInput.extraCss` | `blocks.ts:1309-1311`（注释） | `blocks.ts:1280-1334` 读注释原文 | 注释逐字为：「区块样式资产唯一产出者（用法：调用方把返回值拼进 `TemplateAssets.sharedCssText` 再交 `fillTemplate` 包裹注入；不得走 `StyleSheetInput.extraCss`——其语义被契约限死为技能作用域 token 覆盖块，不得塞共享页面模板的样式）」 | 相符（**不是**过度解读：该句是源头注释原文，件里只是转述） |
| A13 | 收据页用 envelope `shape='receipt'` | `src/spec/index.ts:61` | `spec/index.ts:61` | `STRICT_ENVELOPE_SHAPES` 签名 `readonly ['list','detail','stat','receipt','analysis','fallback']`，含 `receipt`，恰六形状 | 相符 |
| A14 | 复制文本投影 `receipt→ok+message` | `spec/index.ts:146-147` | `spec/index.ts:147` ＋ `spec/text.ts:120-126` | `DATA_TEXT_PROJECTIONS` 的 `receipt` 条：`{ header: TEXT_HEADER_TEMPLATE, body: 'ok', tail: 'message', csvSections: ['status','message'] }` | 相符 |
| A15 | 冻结面共 130 条、`kind:'type'` 42 条、`runtime` 88 条；§3.1/3.2/3.3/3.4/3.5 各 25/8/44/24/27 条 | `spec/index.ts:44-188` | 对 `spec/index.ts` 逐项计数（`name: '` / `kind: '` / `section: '`） | 实测 130／42／88，分段 25／8／44／24／27；加 §5 一条、§7 一条恰好 130 | 相符（六个数字全中） |
| A16 | `StyleSheetInput`／`StyleSheetOutput` 字段形状 | `spec/index.ts:77-78` | 同 | `:77 { prefix?: string; extraCss?: string }`；`:78 { css: string; tokens: readonly CssVarName[]; prefix: string; version: string }`，与件里逐字一致 | 相符 |
| A17 | `BASE_PAINT_CONTRACT_VERSION` 值 `'0.1.0'` | `spec/index.ts:26` | `spec/index.ts:24-27` | `:26 export const BASE_PAINT_CONTRACT_VERSION = '0.1.0' as const;` | 相符 |
| A18 | 自报「实测行数」修正简报：`src/index.ts` 40 行、`src/blocks.ts` 1334 行、`src/spec/index.ts` 188 行、`src/spec/text.ts` 153 行 | 第四节（未能确认的事）第 7 条 | `[System.IO.File]::ReadAllText` 按 LF 计数 | `index.ts` 40、`blocks.ts` 1334（LF 1335 减尾部空）、`spec/index.ts` 188（LF 189 减尾部空）、`spec/text.ts` 153 —— 四项全对；简报给的 1300 行确为旧数 | 相符 |
| A19 | `src/spec/*.ts` 子文件行数：`style.ts` 49／`template.ts` 255／`controls.ts` 283／`charts.ts` 227／`help.ts` 246 | `§2.3` 行数列；第四节第 6 条重复其中四个 | 同法实测 | **63／362／390／273／281**。五个数字全错，且第四节第 6 条把其中四个又抄了一遍 | **不符** |
| A20 | `chartsCss` 声明行 | `src/charts.ts:1749` | `charts.ts` 定位 | 实际在 `:1752 export function chartsCss(prefix: string): string {`，偏 3 行 | **不符** |
| A21 | 区块层另有六个导出只由 grep 见到声明行（`renderTocBlock`／`renderCaliberLine`／`renderKpiCard`／`renderKpiGrid`／`renderListRows`／`renderDetailSection`） | `blocks.ts:242`／`:260`／`:278`／`:305`／`:439`／`:527`；`renderCaliberLine` 签名 `(text: string): string`、`renderKpiGrid` 签名 `(cards: readonly KpiCardInput[]): string` | `blocks.ts` 逐个定位 | 六处行号全中：`:242`、`:260`（`export function renderCaliberLine(text: string): string {`）、`:278`、`:305`（`export function renderKpiGrid(cards: readonly KpiCardInput[]): string {`）、`:439`、`:527` | 相符 |
| A22 | `TemplateError` 类不在包根导出面，调用方按 `name`／`code` 判定 | `src/index.ts:12-13` | `src/index.ts` 整读（40 行） | 注释在 `:12-13` 逐字写明，导出清单内无 `TemplateError` | 相符 |

合计：抽样 22 条，相符 20、不符 2、无法核实 0。

### B. `t406-共用件依赖与提升改造清单.md`（抽样 29 条）

| # | 断言摘要 | 它给的行号 | 我去哪核的 | 源头实际是什么 | 三态 |
|---|---|---|---|---|---|
| B1 | `commandSpec.ts` 对外 8 个名字，逐个给行号 | `:17`/`:20`/`:31`/`:36`/`:37`/`:40`/`:55`/`:66` | 对该件 `^export` 全取 | 恰 8 个，且八个行号逐个精确（`DeliveryKind`17／`ViewOut`20／`WriteOut`31／`ViewHandler`36／`WriteHandler`37／`ReadCommandSpec`40／`WriteCommandSpec`55／`CommandSpec`66） | 相符 |
| B2 | `writeParts.ts` 对外 17 个名字，逐个给行号 | `:19`…`:106` | 同法 | 恰 17 个，行号逐个精确（`SOFT_EXCLUDED_INNER`19／`SOFT_EXCLUDED`20／`HARD_INNER`21／`HARD_WORDING`22／`deleteStatus`25／`esc`29／`totalChanges`38／`F`45／`COL_CLI`56／`cliNames`67／`provided`70／`definedKeys`75／`M5Patch`80／`receiptHtml`83／`out`91／`commandLine`101／`R`106） | 相符 |
| B3 | `receiptParts.ts` 对外 2 个名字 | `:21`／`:31` | 同法 | 恰 2 个（`statusCard`21／`reconcileDisclosure`31），无第三个导出 | 相符 |
| B4 | `docPage.ts` 对外 2 个名字（`DocPageInput` 不导出） | `:77`／`:107`／`:20` | 同法 | 恰 2 个（`assembleDocPage`77／`metricsOf`107）；`:20` 处 `DocPageInput` 确为未导出 | 相符 |
| B5 | `copyArea.ts` 对外 5 个名字 | `:101`/`:116`/`:162`/`:167`/`:178` | 同法 | 恰 5 个，行号逐个精确 | 相符 |
| B6 | `commandSpec.ts` 从 base 拿 2 条，来源行号 `:12`／`:13` | `commandSpec.ts:12`／`:13` | `commandSpec.ts:10-15` 读 import 段 | `:12 import type { EnvelopeShape } from 'base-link-core';`；`:13 import type { HtmlLanding } from 'base-paint/save-html';`；`:14` 另 `CrudReceipt` from `'../render/receipt.js'` | 相符 |
| B7 | `writeParts.ts:87` 写死 `data-skill="calorie"` ＋ `data-slot="ilife:calorie:receipt"` | `writeParts.ts:87` | 该件精确检索 | `:87 return '<section class="ilife-page" data-skill="calorie" data-slot="ilife:calorie:receipt"><h1>' + esc(scene) + '</h1>' +`，逐字一致 | 相符 |
| B8 | `writeParts.ts:102` 写死 `'calorie-cmd-read ' + key` | `writeParts.ts:102` | 同上 | `:102 return 'calorie-cmd-read ' + key + " --params '" + JSON.stringify(params) + "'";` | 相符 |
| B9 | `render/copy.ts:35` 的 `CALORIE_COPY_ACTION`；`/copy.ts:38` `copyActionHtml` 两行转 `renderActionBar` | `render/copy.ts:35`／`:38`／`:39` | 该件检索 | `:35 export const CALORIE_COPY_ACTION: { readonly actionId: string; readonly label: string } = HELP_COPY_ACTIONS.prompt;`；`:38 export function copyActionHtml(text, action = CALORIE_COPY_ACTION): string {`；`:18-24` 的 `HELP_COPY_ACTIONS` **import 自 `'base-paint'`** | 行号相符；但见 B26 |
| B10 | `copyArea.ts:170` 取 `DB_FILENAME`；`paths.ts:10` 值为 `'calorie_data.db'` | `copyArea.ts:170`／`paths.ts:10` | 两处检索 | `paths.ts:10 export const DB_FILENAME = 'calorie_data.db';`；`copyArea.ts:170 dataStructure: DB_FILENAME + (…)` | 相符 |
| B11 | `docPage.ts:86` 缺省 badge `'卡路里 · 分析'` | `docPage.ts:86` | 该件检索 | `:86 const badge = typeof input.badge === 'string' && input.badge !== '' ? input.badge : '卡路里 · 分析';` | 相符 |
| B12 | `esc` 三字符（`&`/`<`/`>`）与公共层 `escapeHtml` 五字符不可互换 | `writeParts.ts:29`／`blocks.ts:52`–`:59`、`blocks.ts:51` | `writeParts.ts:29-31` ＋ `contract.ts:28-40` ＋ 全仓检索 `ESCAPE_HTML_CHARS` | 实质结论**成立**：`esc` 只 `.replace(/&/).replace(/</).replace(/>/)` 三字符；`escapeHtml`（`contract.ts:38`）走冻结五字符表。但**件里给的公共层位置错**：`blocks.ts:52-59` 是一段 `import type { ChartKind, CopyButtonInput, …, EscapeHtmlChar, Scene, SceneTypeBadge, … }` 类型清单，`blocks.ts:51` 是 `ActionBarInput,`——该处并无「五字符表」。五字符表实在 `spec/controls.ts:14`（`ESCAPE_HTML_CHARS`），注释在 `contract.ts:32-33`。同件第五节第 3 条又自引 `contract.ts:38`／`:32`（那才是对的），前后两处自相矛盾 | **不符**（引用位置错，实质结论对） |
| B13 | `profile-doc-179.test.mjs:279` 钉住回执片段起始属性 | `:279` | 该测试件检索 | `:279 assert.ok(r.file.startsWith('<section class="ilife-page" data-skill="calorie" data-slot="ilife:calorie:receipt">'),` | 相符 |
| B14 | `copy-component-179.test.mjs:73`／`:117` 钉住空态句 | `:73`／`:117` | 该测试件检索 | `:73 assert.ok(html.includes('本页没有可复制的数据'), '缺缺省空态句');`；`:117 assert.ok(noData.includes('本页没有可复制的数据'), '没给数据就该说一句');` | 相符 |
| B15 | `CrudReceipt` 收据形状「十二个字段」 | `receipt.ts:43`–`:54` | `receipt.ts:43-55` 读接口体 | 所引行段内自有字段 **10 个**（`scene`／`action`／`op`／`recordId`／`summary`／`items`／`tagDiff`／`distance`／`noChange`／`meta`），另 `extends M5Fields`；连 `M5Fields` 一起数才够十二 | **不符**（计数与所引行段不符；不影响「无卡路里分支」的结论） |
| B16 | `entityType` 是自由字符串（形状无领域分支） | `receipt.ts:38` | 同上 | `:38 entityType: string;` | 相符 |
| B17 | `buildCrudReceipt:167`／`withM5:137`／`M5IdSource:80`／`M5_CONTRACT:70='1'`／`M5_AFFECTED_SOURCE:72='sqlite:total_changes'`／`M5Input:92`／`M5Fields:82`／`receipt.ts:126-128` 写入 | 各 | `receipt.ts` 逐个定位 | 全部精确命中，无一偏行 | 相符 |
| B18 | `F` 是七个表、占 `writeParts.ts:45`–`:53` | `writeParts.ts:45`–`:53` | `writeParts.ts:44-54` 读 | `:45 export const F = {` 起、`:53 } as const;` 止；七张表 `diet`／`water`／`weight`／`weightBatch`／`exercise`／`photo`／`product` | 相符 |
| B19 | `copyArea.ts` 的 `COPY_EMPTY_TEXT`（:41）／`MENU_HINTS`（:45）／`COPY_TITLE_DUP_OF_BUTTON`（:97）／「不自己取时钟」（:77-78） | 各 | `copyArea.ts` 读 `:36-49`、`:95-109` | `:41 const COPY_EMPTY_TEXT = '本页没有可复制的数据';`；`:45 const MENU_HINTS = ['粘贴给 AI / 自己看', '结构化存档', '表格导入'];`；`:97 const COPY_TITLE_DUP_OF_BUTTON = '复制数据';`；`:76-77` 注释「**必填，本件不自己取时钟**」 | 相符 |
| B20 | `copyArea.ts` 的 base 依赖行号：`renderCopyBlock:129`／`renderEmptyBlock:137`／`renderFeedbackBlock:179`／`renderPreBlock:104-105`／`buildDataText:154-156`／`buildLogText:132`／`copyActionHtml:106` | 各 | 该件检索 | 七处全部精确命中（`:154`/`:155`/`:156` 恰为 text／json／csv 三格式各算一份） | 相符 |
| B21 | `docPage.ts` 对包内依赖数为 0，只 import `base-paint`；从 base 拿到 6 个名字且在既有出口 | `docPage.ts:16`–`:17`；`index.ts:4`／`:14`／`:19`／`:37` | `docPage.ts:14-19` ＋ `src/index.ts` 整读 | import 段只有两条：`:16 …from 'base-paint/blocks'`、`:17 …from 'base-paint'`，包内依赖确为 0；`index.ts:4` 出 `buildStyleSheet`、`:14` 出 `fillTemplate`、`:19` 出 `buildSharedHelpersJs`、`:37` 出 `buildChartsHelpersJs`，四个出口行号全中 | 相符 |
| B22 | 该件头部称 `docPage.ts` 从 base 拿到「（5 条）」，正文列 6 条；同件第 3 条自己写「六个名字」 | `docPage.ts:59`（头部）／`:143`（正文） | 该件对读 | 正文 `:61-66` 实列 6 个名字（`blocksCss`／`renderPageShell`／`buildStyleSheet`／`fillTemplate`／`buildSharedHelpersJs`／`buildChartsHelpersJs`），`:143` 亦称「六个名字」。头部「（5 条）」为错误计数 | **不符**（自相矛盾，少计一条） |
| B23 | `writeParts.ts` 从卡路里本包拿到「（3 条）」 | `writeParts.ts:30`（头部）；正文 `:32-35` | 该件对读 | 正文实列 4 条（`buildCrudReceipt`／`CrudReceipt`／`M5IdSource`／`WriteOut`）。四处都是不同的对外名字，应记 4 条 | **不符**（少计一条） |
| B24 | **§四口径**：「全仓检索 `shared/(writeParts|receiptParts|docPage|copyArea|commandSpec)` 只有 `packages/skill-calorie/src/**` 命中，包外零命中」；据此 `docPage.ts`、`copyArea.ts` 的「第二个使用者＝无」 | `§四` `:158`；表 `:166`／`:167` | 在 `packages/skill-bill/src` 检索同名共用件 | **包外有命中**：`packages/skill-bill/src/shared/docPage.ts`（53 行，`:7` 自述「沿卡路里同件的 `docPage.ts`」，`:9-10` **逐字引用本总表第 1.1 节硬口径**，`:11` 自述「零包内依赖」）与 `packages/skill-bill/src/shared/copyArea.ts`（导出 `promptCopyArea:61`／`copyArea:72`／`copyLog:107`，`:18` **引用本改造清单第二节 `DB_FILENAME` 行**，`:49` 已把库文件名改成调用方给）。两份文件都命中该检索式 | **不符（严重）** |
| B25 | 饼干 `buildRecordReceipt` 只回两个字段，尚无回执页 | `skill-bill/src/render/views.ts:71` | 该件检索 | `:71 export function buildRecordReceipt(message: string): { ok: boolean; message: string } {` —— 与该断言一致 | 相符 |
| B26 | `copyActionHtml` 判为「**是**卡路里专有」，理由是缺省文案「复制指令」是本包说法，直接复用会把卡路里的说法印到饼干页上 | `render/copy.ts:35` | `spec/help.ts:249`／`spec/index.ts:168`／`render/copy.ts:18-24` | 事实相反：「复制指令」是**公共层冻结缺省**——`spec/help.ts:249-250 HELP_COPY_ACTIONS.prompt = { actionId: 'ilife-help-copy-prompt', label: '复制指令' }`，且已在冻结面登记（`spec/index.ts:168`，ticket `#78`）；`render/copy.ts:18-24` 正是 `import { HELP_COPY_ACTIONS } from 'base-paint'`。故 `CALORIE_COPY_ACTION` 只是公共层冻结值的一个别名，「本包说法」不成立；据此提出的「缺省改成必填或由公共层给冻结缺省」在取值上是空改动 | **不符**（画像判错，改造建议随之失真） |
| B27 | 饼干 `html.ts:57`／`:58` 手写 CSS 与 helpers、`:60` 自写 `fillTemplate` | `skill-bill/src/render/html.ts:57`／`:58`／`:60` | 该件检索 | `:58 export const SHARED_HELPERS = '<script>function copyItem(id){…}</script>';`、`:60 export function fillTemplate(template: string, contentHtml: string): string {` —— 与断言一致 | 相符 |
| B28 | 饼干 `DB_FILENAME` 在同名不同值件 | `skill-bill/src/fetch/paths.ts:7` | 该件检索 | `:7 export const DB_FILENAME = 'biscuit_accountant.db';` | 相符 |
| B29 | 饼干命令事实另有住处（`BillKey`） | `skill-bill/src/policy/wakewords.ts:5` | 该件检索 | `:5 export type BillKey =` | 相符 |

合计：抽样 29 条，相符 23、不符 6、无法核实 0。

## 二、两份各自评分

四个维度等权各 25 分。

### A. `t406-base组件总表.md`：**76 / 100**

| 维度 | 得分 | 扣分理由与依据 |
|---|---|---|
| 事实准确 | 18 / 25 | 抽样 22 条中 2 条不符。①`§2.3` 的「行数」列 5 个数字全错（`spec/style.ts` 49→实 63、`spec/template.ts` 255→实 362、`spec/controls.ts` 283→实 390、`spec/charts.ts` 227→实 273、`spec/help.ts` 246→实 281），且第四节第 6 条把这 4 个错数又抄了一遍（A19）；②`chartsCss` 报 `charts.ts:1749`，实在 `:1752`（A20）。撑住分数的是主体全对：30 个组件里抽 8 个名字的导出门与签名全中（A1–A10）、冻结面 130/42/88 与分段 25/8/44/24/27 六个数字全中（A15）、`package.json` 五个开口与包名全中（A11）、自报的四个实测行数全中（A18）。**硬口径一条经核为源头注释原文，判为不过度解读**，这是本件最有价值的部分。 |
| 证据完备 | 23 / 25 | 几乎每条结论都带 `文件:行号`，抽查的 20 条相符项里行号普遍精确到行（A3–A6、A17、A21 六处全中）。扣 2 分：`§2.3` 与第四节第 6 条的五个行数没有回源实测就写成了事实；`chartsCss` 的 `:1749` 亦无实测痕迹。「未读」的四处子文件如实标注，没有把没读的当读了，这一点是加分项。 |
| 覆盖完整 | 17 / 25 | 有**明显漏收，且漏的恰是写入域页面要用的**。第一节 30 件不收区块层六个导出——其中 `renderKpiCard`（`blocks.ts:278`）／`renderKpiGrid`（`blocks.ts:305`）是 `KpiCardInput` 数据对象**唯一的渲染器**，而收据页的状态卡（`receiptParts.ts:21` 的 `statusCard` 返回 `KpiCardInput`）没有它就出不了 HTML；`renderDetailSection`（`:527`）／`renderListRows`（`:439`）是回执页「明细段／行列表」的现成件；`renderTocBlock`（`:242`）／`renderCaliberLine`（`:260`）同理。这六件只在第四节第 5 条以「只由 grep 见到声明行」一笔带过，且只提示了后两个需要补读。另漏收区块层自身的再导出面 `blocks.ts:1326`（`ACTION_ID_ATTR`／`COPY_ACTION_IDS`／`DEFAULT_DATA_ATTR`／`TOAST_DEFAULTS`）与 `:1327-1334`（六个类型），凡走 `base-paint/blocks` 的页面作者都受它影响。收得对的部分：把图表层与 HELP 壳明确排除并给理由（第三节），边界划得干净。 |
| 结论可执行 | 18 / 25 | 主体可执行：硬口径给了「必须／不得」的判定，签名表逐条给了输入输出形状，导入路径按 `package.json` 真实开口写，下游照抄即可装配。扣分依据是**它自己留了一个未闭合的必读**：第四节第 5 条写「其中后两个若回执页要『明细段』或『行列表』，需补读 `src/blocks.ts:426-572`」——而收据页本来就要明细段与行列表，等于把一件必做之事写成条件句；并且完全没有提示 KPI 卡一族是状态卡的必需件。下游只读第一节就动手，会以为公共层没有 KPI 卡渲染器而自造 HTML。 |

### B. `t406-共用件依赖与提升改造清单.md`：**71 / 100**

| 维度 | 得分 | 扣分理由与依据 |
|---|---|---|
| 事实准确 | 16 / 25 | 抽样 29 条中 6 条不符，其中 1 条严重。严重项：§四的自证口径「全仓检索…包外零命中」被证伪，`packages/skill-bill/src/shared/docPage.ts` 与 `packages/skill-bill/src/shared/copyArea.ts` 都命中该检索式（B24）。其余：公共层五字符表位置引成 `blocks.ts:52-59`／`:51`（实为类型 import 清单与 `ActionBarInput`，真位置 `spec/controls.ts:14`＋`contract.ts:32-33`），且与同件第五节第 3 条自引的 `contract.ts:38`／`:32` 互相矛盾（B12）；`CrudReceipt` 记「十二个字段」而所引 `:43-54` 段内自有字段只 10 个（B15）；`copyActionHtml` 被判「是卡路里专有」，而缺省文案「复制指令」实为公共层冻结值 `HELP_COPY_ACTIONS.prompt`（`spec/help.ts:250`，冻结面 `spec/index.ts:168`），件里说「是本包说法」不成立（B26）；两处自报条数与自己正文对不上（「5 条」实列 6、「3 条」实列 4，B22／B23）。撑住分数的是抽查里最硬的部分全对：五件对外名字个数与行号全中（B1–B5）、五处卡路里专有耦合的行号**五处全中**（B7–B11）、三处产物断言全中（B13／B14）、`receipt.ts` 七个名字行号全中（B17）、`copyArea.ts` 七处 base 依赖行号全中（B20）。 |
| 证据完备 | 22 / 25 | 结论条条带 `文件:行号`，抽查相符项的精确度很高（导出 34 个名字的行号无一偏）。扣 3 分：三处不符本身就是证据缺陷——B12 引到了不相干的类型 import 清单，B24 的检索式结论没有落到任何一条可复核的命中列表上（只写「只有…命中」，没贴命中项），B26 的画像没有回源看 `HELP_COPY_ACTIONS` 的定义地。第五节对未确认项如实列了 5 条，态度是好的。 |
| 覆盖完整 | 17 / 25 | 五件都收齐了，维度也齐（对外面／依赖面／专有耦合／改造清单／第二个用法）。扣分在第 5 节之外的那一维——§四整节的存在意义就是回答「第二个用法在哪」，而它据以判定的检索漏掉了仓内已经存在的两份同名共用件，导致 `docPage.ts`、`copyArea.ts` 两行的「第二个使用者＝无」结论错误；`copyActionHtml` 一行亦因没看定义地而画像错。**它漏掉的恰恰是「第二个用法」这一维唯一要找的东西。** |
| 结论可执行 | 16 / 25 | 单件的可执行度其实很高：每件都给了「新增 base 导出」「要加的参数」「会不会改到卡路里现有页面产物」三问的明确答复，改造口径细到「保留三字符版本」「缺省值保持不变即可」，并配了断言级依据。扣分依据：两行的结论方向被 B24 带偏——照它做，会在公共层新造一份整页装配件与一份复制区装配件，而不去合并 `skill-bill/src/shared/` 下已有的两份平行实现，产物是「三份装配件并存」；B26 给出的「缺省改成必填或由公共层给冻结缺省」在取值上是空改动，会让下游白加一个参数。 |

### C. 「对外名字个数」逐件对账表

口径：对该件全文取 `^export` 全部命中，逐个数出对外名字个数（含类型、常量、函数、类型别名）。

| 件 | 件里报的数 | 我数的数 | 对账 | 依据 |
|---|---|---|---|---|
| `commandSpec.ts` | 8 | 8 | 一致 | 恰 8 个 `export`，八个行号与件里给的逐个相同 |
| `writeParts.ts` | 17 | 17 | 一致 | 恰 17 个 `export`，十七个行号与件里给的逐个相同 |
| `receiptParts.ts` | 2 | 2 | 一致 | 恰 2 个；件里另标注两个形参「不构成对外名字」，判断正确 |
| `docPage.ts` | 2 | 2 | 一致 | 恰 2 个；`DocPageInput` 确为未导出，件里判断正确 |
| `copyArea.ts` | 5 | 5 | 一致 | 恰 5 个 `export`，五个行号与件里给的逐个相同 |

**五件五中，此维零扣分。**

## 三、发现的错误与薄弱处

按严重度排序。「会让下游做错决定」的标**严重**。

### 严重

**1. 〔严重〕`t406-共用件依赖与提升改造清单.md` §四的自证口径被证伪，`docPage.ts`／`copyArea.ts` 两行的「第二个使用者＝无」结论错误**（抽样 B24）

- 件里写的证据是「全仓检索 `shared/(writeParts|receiptParts|docPage|copyArea|commandSpec)` 只有 `packages/skill-calorie/src/**` 命中，包外零命中」（`:158`）。
- 源头实际：
  - `packages/skill-bill/src/shared/docPage.ts` 存在（53 行）。`:7` 逐字写「包裹约定（沿卡路里同件的 `docPage.ts`，不新增）」；`:9-10` 逐字引用兄弟件第 1.1 节 `blocksCss` 行的硬口径；`:11` 自述「零包内依赖（只 import `base-paint`）」；`:13-14` 的 import 与卡路里同件同构；`:52` 调 `fillTemplate`。
  - `packages/skill-bill/src/shared/copyArea.ts` 存在，导出 `promptCopyArea`（`:61`）／`copyArea`（`:72`）／`copyLog`（`:107`）——与卡路里同件的三个对外名字逐个同名；`:18` 直接引用本改造清单第二节的 `DB_FILENAME` 行；`:49` 已经把库文件名写成调用方给（正是件里 §三.5 建议的改造）。
  - 两份文件都命中件里自报的那个检索式，所以「只有卡路里命中」这句在源头站不住。
- 影响判断：§四整节的存在意义就是回答「第二个用法在哪」，判定口径又明写「按仓内已经存在的第二个调用点判定」。按它自己的口径，这两行应当答「有」，而且是「同名同职责的平行实现」，不是「无」。

### 中

**2. `t406-base组件总表.md` 第一节漏收写入域页面必需的区块件，KPI 卡一族尤其**（抽样 A21、§二覆盖维度）

- 第一节 30 件不含 `renderKpiCard`（`blocks.ts:278`）／`renderKpiGrid`（`blocks.ts:305`）／`renderDetailSection`（`:527`）／`renderListRows`（`:439`）／`renderTocBlock`（`:242`）／`renderCaliberLine`（`:260`）。
- 其中 KPI 卡一族不是可选项：`receiptParts.ts:21` 的 `statusCard` 返回类型就是 `KpiCardInput`（`blocks.ts:268`），而 `KpiCardInput` 的**唯一**渲染出口是 `renderKpiCard`。第一节不收它，等于给「收据页形状」留了一个出不了 HTML 的数据对象。
- 这六件只在第四节第 5 条以「只由 grep 见到声明行」出现，且只提示后两个「若回执页要明细段或行列表需补读」——把必做之事写成了条件句。
- 另：区块层自身的再导出面 `blocks.ts:1326`（`ACTION_ID_ATTR`／`COPY_ACTION_IDS`／`DEFAULT_DATA_ATTR`／`TOAST_DEFAULTS`）与 `:1327-1334`（六个类型）全文未提。

**3. `t406-共用件依赖与提升改造清单.md` 把 `copyActionHtml` 判成「卡路里专有」，画像判错导致改造建议失真**（抽样 B26）

- 件里写「缺省文案落在本包常量 `CALORIE_COPY_ACTION`……饼干记账要的文案不同，直接复用会把卡路里的说法印到饼干页上」。
- 源头实际：「复制指令」是公共层冻结缺省——`base-render/src/spec/help.ts:249-250` 的 `HELP_COPY_ACTIONS.prompt = { actionId: 'ilife-help-copy-prompt', label: '复制指令' }`，已在冻结面登记（`spec/index.ts:168`，ticket `#78`）；`render/copy.ts:18-24` 正是 `import { HELP_COPY_ACTIONS } from 'base-paint'`。`CALORIE_COPY_ACTION` 只是公共层冻结值的一个本包别名。
- 因此「把缺省动作改成公共层冻结值」在取值上是空改动；「或直接设必填」则是让每个调用方去传一个本来就有冻结缺省的值。

**4. `t406-base组件总表.md` §2.3 的五个子文件行数全错，且第四节第 6 条把其中四个又抄了一遍**（抽样 A19）

- 报 49／255／283／227／246，实测 63／362／390／273／281。
- 第四节第 7 条已经察觉到「文件体量与简报口径不一致」并修正了四件，却没有回头核 `spec/*.ts` 这一列——同一处不一致只修了一半。

### 低

**5. `t406-共用件依赖与提升改造清单.md` 公共层五字符表的位置引错，并与同件第五节自引相矛盾**（抽样 B12）

- §三.2 引 `base-render/src/blocks.ts:52`–`:59` 与 `blocks.ts:51` 为「冻结五字符表」；源头那两处是 `import type { ChartKind, CopyButtonInput, …, EscapeHtmlChar, Scene, SceneTypeBadge }` 类型清单与 `ActionBarInput,`。真位置是 `spec/controls.ts:14`（`ESCAPE_HTML_CHARS`）与 `contract.ts:32-33`（注释）。
- 同件第五节第 3 条自引 `contract.ts:38`／`:32` 是对的。同一事实两处引两样，读者若只看 §三.2 会找错地方。
- 实质结论（`esc` 三字符与 `escapeHtml` 五字符不可互换）经核**成立**：`writeParts.ts:30` 只转 `&`／`<`／`>`；`contract.ts:38` 走冻结五字符表。

**6. `t406-base组件总表.md` `chartsCss` 行号偏 3 行**（抽样 A20）

- 报 `src/charts.ts:1749`，实在 `:1752`。

**7. `t406-共用件依赖与提升改造清单.md` 「十二个字段」与所引行段不符**（抽样 B15）

- 所引 `receipt.ts:43`–`:54` 段内自有字段 10 个，另 `extends M5Fields`。不影响「形状无领域分支」的结论。

**8. `t406-共用件依赖与提升改造清单.md` 两处自报条数与正文对不上**（抽样 B22、B23）

- `docPage.ts` 头部写「从 base 拿到什么（5 条）」，正文列 6 条，同件 `:143` 又称「六个名字」。
- `writeParts.ts` 头部写「从卡路里本包拿到什么（3 条）」，正文列 4 条。

### 薄弱处（非错误，但会削弱可复核性）

- 两份都如实标了未读范围，这是优点：总表第四节第 1、2、4、6 条标出 `controls.ts` 实现体与 `spec/` 四个子文件未读；改造清单第五节第 2 条承认「没有跑测试，也没有跑产物对拍」。
- 但改造清单 §四的检索结论是「没有贴命中列表」的写法——只报结论、不报命中项，恰恰是这次出事的地方。若当初把命中路径逐条列出，两份同名件当场就会浮出来。

## 四、对下游的影响

只有一条严重错误，进本节。

**错误 1（§四「包外零命中」被证伪）**

- 影响哪张下游票：T406 的「共用件提升到公共层」实施票，具体是其中 `docPage.ts` 与 `copyArea.ts` 两个子任务；连带影响 `copyActionHtml` 子任务。
- 会做出什么错东西：照本件 §三.4／§三.5 执行，会在公共层新造一份整页装配件与一份复制区装配件，而 `packages/skill-bill/src/shared/docPage.ts`（53 行）与 `packages/skill-bill/src/shared/copyArea.ts` 不并入、不删除、不改名。产物会是「公共层一份 ＋ 卡路里包内一份 ＋ 饼干包内一份」三份职责重叠的装配件，而且饼干那份已经先按本件的建议做对了（库文件名由调用方给），本件却在 §四 说它「无」。此后任何一次改装配口径，都要同时改三处。
- 冒烟口径也会被带偏：本件 `:130`／`:133`／`:143`／`:153` 的「会不会改到卡路里现有页面产物」只对卡路里做断言级核对，饼干那两份平行实现不在核对范围内；合并不做，改动的真实影响面就无法只靠卡路里那批断言钉住。
- 最小修法：把 §四口径那一句改掉，改为「已按件名逐路径检索，命中两份包外平行实现：`packages/skill-bill/src/shared/docPage.ts`、`packages/skill-bill/src/shared/copyArea.ts`」；两行「第二个使用者」由「无」改成「有——同名同职责的平行实现，见上两路径」；`docPage.ts` 一行把「饼干侧若要做写入域页面，复用它是省掉一份 `fillTemplate` 拼装的唯一办法」改成「复用它是省掉第三份 `fillTemplate` 拼装的办法，前置是把饼干现有 `src/shared/docPage.ts` 并入」；`copyArea.ts` 一行的结论倾向需重判——既然饼干已有同件，就地留包内与上移之间的取舍要按两份实现的差量重算。检索式本身可保留，但必须附命中路径列表。

## 五、结论

### 总评分

**73.5 / 100**（两份平均）

| 件 | 总分 | 事实准确 | 证据完备 | 覆盖完整 | 结论可执行 |
|---|---|---|---|---|---|
| `t406-base组件总表.md` | 76 | 18 | 23 | 17 | 18 |
| `t406-共用件依赖与提升改造清单.md` | 71 | 16 | 22 | 17 | 16 |

两份的可信度分布不一样，值得分开说。总表的问题在「广度」：抽样的事实大部分精确（组件导出门、签名、`package.json` 五个开口、冻结面六个计数全对），硬口径一条经核为源头注释原文，判为**不过度解读**；它失分在漏收写入域页面必需的区块件与一张全错的子文件行数表。改造清单的问题在「收口」：抽查里最硬的部分——五件对外名字个数（五件五中）、五处卡路里专有耦合的行号（五处全中）、三处产物断言、`receipt.ts` 与 `copyArea.ts` 的行级引用——无一偏行，精度相当高；但它把「第二个用法在哪」这一整节建立在一个没有贴出命中项的检索结论上，而这个结论是错的，且错在这一节唯一要找的东西上。

### 结论：**须整改**

放行会直接把错误结论带到实施票上——公共层会多出一份与饼干包内已有件职责重叠的装配件。下列三条必须先改，改完可放行。

**必须先改（阻塞放行）**

1. `t406-共用件依赖与提升改造清单.md` §四口径句（`:158`）＋ 表中 `docPage.ts`（`:166`）、`copyArea.ts`（`:167`）两行：把「包外零命中」改成已命中的事实，补上 `packages/skill-bill/src/shared/docPage.ts` 与 `packages/skill-bill/src/shared/copyArea.ts` 两条路径，并把两行的「第二个使用者＝无」改判；`copyArea.ts` 一行的结论倾向按两份实现的差量重算。
2. `t406-共用件依赖与提升改造清单.md` 第二节 `copyActionHtml` 行（`:101`）与 §四该行（`:169`）：把「是不是卡路里专有」由「是」改为「否（缺省值是公共层冻结缺省）」并给出依据 `base-render/src/spec/help.ts:249-250` 与冻结面 `spec/index.ts:168`；相应删去或改写「缺省改成必填或由公共层给冻结缺省」这条改造建议。
3. `t406-base组件总表.md` 第一节：把 `renderKpiCard`（`blocks.ts:278`）／`renderKpiGrid`（`:305`）／`renderDetailSection`（`:527`）／`renderListRows`（`:439`）／`renderTocBlock`（`:242`）／`renderCaliberLine`（`:260`）补进表内并补读签名，同时补上区块层再导出面 `blocks.ts:1326` 与 `:1327-1334`；第四节第 5 条由「若回执页要明细段或行列表」改为「收据页必需」的直陈，并点明 `KpiCardInput` 的唯一渲染器是 KPI 卡一族。

**随改（不阻塞放行，但应在同一批修正）**

4. `t406-base组件总表.md` §2.3 行数列五处（49／255／283／227／246 → 63／362／390／273／281）及第四节第 6 条的重复；`chartsCss` 行号 `:1749` → `:1752`。
5. `t406-共用件依赖与提升改造清单.md` §三.2 的五字符表引用位置，统一到 `spec/controls.ts:14` ＋ `contract.ts:32-33`（与同件第五节第 3 条一致）。
6. `t406-共用件依赖与提升改造清单.md` 三处计数：`CrudReceipt`「十二个字段」（`:97`）、`docPage.ts`「（5 条）」（`:59`）、`writeParts.ts`「（3 条）」（`:30`）。

两份都没有发现一处在答案方向上有害的编造：所有不符都是「位置引错、计数写错、结论边界划错」，没有凭空造出源头不存在的导出或断言。这一点决定了整改是收口工作，而不是重做。
