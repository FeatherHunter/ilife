# t407 手机墙 · 补件证据

只读复核：本次未改 `docs\skills\skill-bill\` 下任何已有件，也未动 `packages\`，无 git 写操作；临时件全在 `.scratch\t407-wall\`。
复跑命令（产物改道，不覆盖已入盘那版）：`node docs\skills\skill-bill\t407-手机墙.mjs D:\ilife\docs\skills\skill-bill ..\..\..\.scratch\t407-wall\t407-手机墙-重跑.html`

## 一、生成器自检读数

- 退出码 **0**；自打印总读数：`手机墙 …：格 32 · iframe 32 · href 32 · 目标 32 个，缺失 0 · loading="lazy" 命中 0 · 外部 http 引用 0`。
- 逐字节比对：已入盘 `t407-手机墙.html` 与复跑产物同为 SHA256 `00F5270D…A8D08AE9`、12909 字节 ⇒ **生成器可复现，零差异**。
- 重跑前后，已入盘那版的哈希与 mtime（12:28:45）都没变 ⇒ 复跑没有改到已入盘件。

## 二、32 格逐格清单与三族对账

| 格号 | 唤醒词 | 页类型 | 文件名 | 目标存在 |
|---|---|---|---|---|
| 1 | 记支出 | 过程型采集页 | `t407-代表-记支出-采集页.html` | 有 |
| 2 | 记支出 | 结果型回执页 | `t407-代表-记支出-回执页.html` | 有 |
| 3 | 记收入 | 过程型采集页 | `t407-页-记收入-采集页.html` | 有 |
| 4 | 记收入 | 结果型回执页 | `t407-页-记收入-回执页.html` | 有 |
| 5 | 记报销 | 过程型采集页 | `t407-页-记报销-采集页.html` | 有 |
| 6 | 记报销 | 结果型回执页 | `t407-页-记报销-回执页.html` | 有 |
| 7 | 拍账单 | 过程型采集页 | `t407-页-拍账单-采集页.html` | 有 |
| 8 | 拍账单 | 结果型回执页 | `t407-页-拍账单-回执页.html` | 有 |
| 9 | 记一笔 | 过程型采集页 | `t407-页-记一笔-采集页.html` | 有 |
| 10 | 记一笔 | 结果型回执页 | `t407-页-记一笔-回执页.html` | 有 |
| 11 | 记退款 | 过程型采集页 | `t407-页-记退款-采集页.html` | 有 |
| 12 | 记退款 | 结果型回执页 | `t407-页-记退款-回执页.html` | 有 |
| 13 | 报销到账 | 过程型采集页 | `t407-页-报销到账-采集页.html` | 有 |
| 14 | 报销到账 | 结果型回执页 | `t407-页-报销到账-回执页.html` | 有 |
| 15 | 记借出 | 过程型采集页 | `t407-页-记借出-采集页.html` | 有 |
| 16 | 记借出 | 结果型回执页 | `t407-页-记借出-回执页.html` | 有 |
| 17 | 记借入 | 过程型采集页 | `t407-页-记借入-采集页.html` | 有 |
| 18 | 记借入 | 结果型回执页 | `t407-页-记借入-回执页.html` | 有 |
| 19 | 记收回 | 过程型采集页 | `t407-页-记收回-采集页.html` | 有 |
| 20 | 记收回 | 结果型回执页 | `t407-页-记收回-回执页.html` | 有 |
| 21 | 记偿还 | 过程型采集页 | `t407-页-记偿还-采集页.html` | 有 |
| 22 | 记偿还 | 结果型回执页 | `t407-页-记偿还-回执页.html` | 有 |
| 23 | 记分期 | 过程型采集页 | `t407-页-记分期-采集页.html` | 有 |
| 24 | 记分期 | 结果型回执页 | `t407-页-记分期-回执页.html` | 有 |
| 25 | 批量录入 | 过程型采集页 | `t407-页-批量录入-采集页.html` | 有 |
| 26 | 批量录入 | 结果型回执页 | `t407-页-批量录入-回执页.html` | 有 |
| 27 | 改记录 | 过程型采集页 | `t407-页-改记录-采集页.html` | 有 |
| 28 | 改记录 | 结果型回执页 | `t407-页-改记录-回执页.html` | 有 |
| 29 | 撤销 | 过程型采集页 | `t407-页-撤销-采集页.html` | 有 |
| 30 | 撤销 | 结果型回执页 | `t407-页-撤销-回执页.html` | 有 |
| 31 | 恢复 | 过程型采集页 | `t407-页-恢复-采集页.html` | 有 |
| 32 | 恢复 | 结果型回执页 | `t407-页-恢复-回执页.html` | 有 |

三族对账（与三处格头自述及应有数都对得上）：记账族 10 格＝应有 10（记支出／记收入／记报销／拍账单／记一笔 ×2）；特殊收支族 14 格＝应有 14（记退款／报销到账／记借出／记借入／记收回／记偿还／记分期 ×2）；批量与修正族 8 格＝应有 8（批量录入／改记录／撤销／恢复 ×2）；合计 32。
混入 0：`t407-老样-*` 13 份、`t407-对照甲/乙-*` 3 份、`t407-链路总览.html` 在一个没进（全页 64 处 href／src 去重后 32 个目标，逐一核过）；16 条唤醒词各 2 格，不多不少；重复格 0（按文件名查一次、按「族＋格头」再查一次）。

## 三、总索引与「有意不出产物的」

- 卡 32 张、href 33 条（32 张卡 ＋ 导语里那条指向手机墙的链接），缺失 0；没链接的卡 0；卡的 href、卡上显示的文本、墙的 32 个目标，三方同一个集合。
- 「有意不出产物的」4 条，依据逐条落回 `t407-页面块清单-16词.md`：
  - 1 拍账单不做图片识别类页面 → 第 28 行「本仓不装 OCR」＋第 51 行「图片识别入口…老侧也没有…无上传控件」＋第 67 行「拍账单没有图片入口」。**有依据**。
  - 2 撤销／恢复没有更早的页可继承 → 第 40、41 行老侧对应栏都写「老侧无页」。**有依据**。
  - 3 批量录入没有「多行一次落库」→ 第 29 行「现阶段单笔化」＋第 50 行「本批先单笔化」。**有依据，留一句保留**：清单第 81 行自己写「是否已获维护者同意，没有读到出处」，索引把这条当成既定口径写死了。
  - 4 采集页上没有撤销出口 → 第 71 行「确认页没有撤销出口…撤销出口只放在回执页」。**有依据**。
- 编的、引不出依据的：**0 条**。

## 四、对抗自查与没能确认的事

**坑一（最坑）：九张截图不是「按行分块」，是九张都从页面顶端起、高度递增的截段。** 证据：把九张的原始像素逐行比对，除 `07` 外两两前 1105 行完全相同（首位差都落在 y=1105～1107，`07` 落在 5323）；另做一次整页截图（1560 宽）量得页面高 **12919**，而九张里最高的 `08` 只有 **12400** ⇒ 末尾 519 像素（第 12 行那一对框的下半段）**没有一张截到**。最小修法：补截 12400～12919 这一段，或只留一张 12919 高的整页图、丢掉其余八张。
**坑二：文件名与内容对不上，照名字当真会上当。** `08-墙尾-恢复两格.png` 实际是（近）整页图；`06-批量与修正第1行.png` 的底段读到的是特殊收支族的 报销到账／记借出 三格（见 `.scratch\t407-wall\z06-尾段.png`），里头没有一格批量与修正族。最小修法：按实测改名，写成「页首起 H 行」，别用「第N行」。
**坑三（墙自己的）：390 宽的屏上，墙自己宽 410，横向溢出 20 像素。** `figure{overflow:hidden}` 与 `.grid{grid-template-columns:1fr}`（墙第 18、25 行）让单列轨道被框的最小宽度 390 撑住，再叠上 `.wrap` 左右各 20 的内边距 ＝ 410 > 390。实测：headless 渲染 390×900，图边框横段自 x=32 一直伸到画面右缘被切断，最右列还有 261 行白像素 —— 框的右缘被推到屏外。最小修法：末条媒体查询改成 `.grid{grid-template-columns:minmax(0,1fr)}`，并把 `figure{overflow:hidden}` 改成 `overflow-x:auto`。
查过但没问题的：① 每格 href＝格头文件名＝iframe src，三处同一个值（不一致 0）；② iframe 一律宽 390 高 980；③ 总索引没有「有卡没链接」；④ `loading="lazy"` 与外部 http 引用都是 0；⑤ 16 条唤醒词 × 2 类页一一对上；⑥ 格头与框内内容逐格对过，没有配错格 —— 只有 记支出 两格的框内大标题写「记一笔 · 回执」／「补齐槽位」（徽章仍写 记支出），那是页面自己的口径，不是墙配错。
没能确认的事：① 九张之间的差异只落在框内（自 y≈1105 起），成因没查，所以「每张截到第几行」是按高度算出来的，不是逐格数出来的；② `.scratch\t407-wall\` 里的 `check.mjs`／`gen-index.mjs` 是上一窗留下的临时件，本次只读未改，也没拿它们当依据；③ 真机（手机浏览器）上的横向溢出没实机验，只有 headless Chrome 的读数。

## 五、三处修复记录（R1–R3）

本节**只追加**：上面一至四节的读数一字未改（历史读数照旧）。临时量具在两处：`.scratch\t407-wall\cdp-wall.mjs`（CDP 驱动本机 Chrome 量几何／截图）、`.scratch\t407-wall\px-scan.mjs`（用 DSH 自带 sharp 扫 PNG 像素）。

### R1 · 墙自身横向溢出 20 像素

**量法**：本机 Chrome 走 CDP，把版面钉成**真 390 宽**（`--headless=new` ＋ `Emulation.setDeviceMetricsOverride` 宽 390 高 900、`--hide-scrollbars`），量 `document.documentElement.scrollWidth`、卡片（figure）与框（iframe）的右缘。

```
node .scratch\t407-wall\cdp-wall.mjs geo   docs\skills\skill-bill\t407-手机墙.html 390 900
node .scratch\t407-wall\cdp-wall.mjs wheel docs\skills\skill-bill\t407-手机墙.html 390 900
```
（`wheel` 是真手势：把指针放到第 1 张卡片的格头上横滚 `deltaX=120` 三下，再看 32 张卡片的 `scrollLeft`。）

改前那版留了备份：`.scratch\t407-wall\前缀版本-改前-手机墙.html`，SHA256 `00F5270D7A85A18252761D4872958A4DCA9DAF49D96D53BF722082B1A8D08AE9`，与修复前的已入盘那版逐字节相同（同哈希、同 12909 字节）。

| 读数（390 视口） | 改前 | 改后 |
|---|---|---|
| innerWidth ／ clientWidth | 390 ／ 390 | 390 ／ 390 |
| `documentElement.scrollWidth` | 390 | **390 ≤ 390（无横向溢出）** |
| `body.scrollWidth` | 390 | 390 |
| `.grid` 轨道（三族同） | `1fr` ＝ 350px，右缘 370 | `minmax(0,1fr)` ＝ 350px，右缘 370 |
| 卡片右缘（figMaxRight） | 370（卡片在屏内） | 370（卡片在屏内） |
| 框右缘（iframe） | **411**（框 390 宽、自 x=21 起） | 411（同一位置，但落在卡片自己的滚动面里） |
| figure `overflow-x/y` | `hidden/hidden` | **`auto/auto`** |
| figure clientWidth ／ scrollWidth | 348 ／ 390 ⇒ **右边 42 像素被裁、且滚不出来** | 348 ／ 390 ⇒ 右边 42 像素**可滚** |
| 真手势后的 `scrollLeft`（32 格） | 0（滚动的格 0 个） | **42**（随手势滚动的格 1 个，滚到界） |
| 最右列 x=389（390×900 截图，900 行） | — | 全是页面底色 `245,245,247`，非底色像素 **0** |

改前那版的要害：卡片只有 350 宽，框 390 宽，`overflow:hidden` 把每格框右边 **42 像素**裁掉、还没路可看（真手势滚不动）；框右缘 x=411 已经越过屏右缘 21 像素。改后：页自己横不出条（`scrollWidth` 390 ＝ `clientWidth` 390），卡片右缘 370 在屏内，框那多出来的 42 像素改由卡片自带的横向滚动看全（真手势能滚到 `scrollLeft=42`，此时框右缘 369 ≤ 卡片右缘 370）。

**改法**（照自查给的最小改法，两处，都在 `t407-手机墙.mjs` 里）：
1. 末条媒体查询：`@media(max-width:900px){.grid{grid-template-columns:1fr}}` → `grid-template-columns:minmax(0,1fr)`；
2. `figure{background:#fff;border:1px solid #d2d2d7;border-radius:12px;overflow:hidden}` → 末项改 `overflow-x:auto`。
另在生成器头部加了一条「别改回去」的版面约束说明（改回 `1fr`／`overflow:hidden` 就把每格框右边 42 像素裁掉且滚不出来），并在改完重跑生成器重出墙（结构与逐字节读数见下）。两张对照截图：`.scratch\t407-wall\r1-before-390x900.png`（改前，用备份那版量）、`.scratch\t407-wall\r1-after-390x900.png`（改后）。

**另记一条量法上的坑（上一窗的 410／261 读数从哪来）**：上一窗写的是「墙宽 410、最右列 261 行白像素」。用它那条命令原样复现：

```
cd .scratch\t407-wall
& "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless=new --hide-scrollbars --window-size=390,900 --screenshot=raw-new-390.png "file:///D:/ilife/docs/skills/skill-bill/t407-%E6%89%8B%E6%9C%BA%E5%A2%99.html"
node px-scan.mjs raw-new-390.png 1    ⇒ 图 390×900，最右列 x=389：白 261 行
```
261 与上一窗数出来的**一模一样**，但成因不是页面溢出：**本机 Chrome 不给比 500 窄的窗口**——`--window-size=390,900` 落下来 `window.innerWidth` 是 **500**（同一条命令量出的卡片右缘 480、框右缘 411），也就是它跑的是 500 宽的版面、只截了左边 390 ⇒ 最右列那 261 行白像素是**截图裁出来的**，不是页撑出来的。所以「390 宽无横向溢出」这条判据我按真 390 视口给读数（上表），这条坑一并记在这里。

### R2 · 九张截图改成按行分块

**量法**（同一把量具，先量总高与每行的 y 起止）：

```
node .scratch\t407-wall\cdp-wall.mjs geo  docs\skills\skill-bill\t407-手机墙.html 1180 1000
node .scratch\t407-wall\rows-table.mjs %TEMP%\r2-geo.txt      （把上面那份 JSON 摘成每行的 y 起止）
```
**截法**（按行逐张截，落 `.scratch\t407-wall\shots\`）：

```
node .scratch\t407-wall\cdp-wall.mjs rows docs\skills\skill-bill\t407-手机墙.html 1180 1000 D:\ilife\.scratch\t407-wall\shots
```

**为什么选 1180 宽**：这一档落在页内 2 列那条断点里，32 格两列铺开正好 **16 行**，每行恰好是一条唤醒词的两格（左采集页、右回执页），文件名才可能把行里的格写清楚。这就是「真行」的来处：先把 32 个 `figure` 的 y 起止实测出来，再在相邻两行之间的空白缝里对半切；首块**从 y=0 起**（连带页首两段导语与记账族格头），末块**到 y=页高止**（含页尾留白）。

**总高与分块读数**：
- 墙总高 **17116**（1180 宽）；`scrollWidth` 1180 ＝ `clientWidth` 1180 ⇒ 宽向无溢出；`figure` 32 张。
- 行 **16 行**；块覆盖：首块 top **0**、末块 bottom **17116**、相邻块严丝合缝（缺口 **0** 处）、16 块高度合计 **17116** ＝ 页高 ⇒ **0 → 页高无缺口**。
- 截图 **16 张**，与 16 行一一对上（张数 ＝ 行数）。

| # | 文件（`.scratch\t407-wall\shots\`） | 块 y 区间（高） | 行内两格 | 我 `read_image` 看到的 |
|---|---|---|---|---|
| 01 | `row-01-页首-记账族-记支出两格.png` | 0→1258（1258） | 记支出 采集页＋回执页 | 页首两段导语＋「格 32 · 框 32 · 目标 32 个」＋「记账族 10 格」格头；框内大标题「补齐槽位」与「记一笔 · 回执」，两格格头都写 `记支出`。回执页框内标题写「记一笔 · 回执」而徽章写 `记支出`，与第四节那条已知项是同一回事，不是配错格 |
| 02 | `row-02-记账族-记收入两格.png` | 1258→2307（1049） | 记收入 ×2 | 「补齐收入槽位」／「记收入 · 回执」，金额 +5000.00、方向「收入（取正数）」 |
| 03 | `row-03-记账族-记报销两格.png` | 2307→3356（1049） | 记报销 ×2 | 「补齐报销槽位」／「记报销 · 回执」；回执页有「打标说明条」讲 #待报销 怎么流转 |
| 04 | `row-04-记账族-拍账单两格.png` | 3356→4405（1049） | 拍账单 ×2 | 「拍账单 · 补齐三要素」／「拍账单 · 回执」；采集页那块写「已收到 0 张账单图片」，没有上传控件 |
| 05 | `row-05-记账族-记一笔两格.png` | 4405→5472（1067） | 记一笔 ×2 | 「记一笔 · 补充槽位」／「记一笔 · 回执」，方向按金额符号判 |
| 06 | `row-06-特殊收支族-记退款两格.png` | 5472→6539（1067） | 记退款 ×2 | 块首带「特殊收支族 14 格」格头（跟本族第一行走，与文件名里的族名对得上）；框内「记一笔退款」／「记退款 · 回执」＋ #退款 标签流转 |
| 07 | `row-07-特殊收支族-报销到账两格.png` | 6539→7588（1049） | 报销到账 ×2 | 「记一笔报销到账」／「报销到账 · 回执」＋ #报销到账 |
| 08 | `row-08-特殊收支族-记借出两格.png` | 7588→8637（1049） | 记借出 ×2 | 「记一笔借出」／「记借出 · 回执」＋ #借出＋#未还 |
| 09 | `row-09-特殊收支族-记借入两格.png` | 8637→9686（1049） | 记借入 ×2 | 「记一笔借入」／「记借入 · 回执」＋ #借入＋#未还 |
| 10 | `row-10-特殊收支族-记收回两格.png` | 9686→10735（1049） | 记收回 ×2 | 「记一笔收回」／「记收回 · 回执」＋ #收回 |
| 11 | `row-11-特殊收支族-记偿还两格.png` | 10735→11784（1049） | 记偿还 ×2 | 「记一笔偿还」／「记偿还 · 回执」＋ #偿还 |
| 12 | `row-12-特殊收支族-记分期两格.png` | 11784→12851（1067） | 记分期 ×2 | 「记一笔分期」／「记分期 · 回执」，回执页有分期角标 |
| 13 | `row-13-批量与修正族-批量录入两格.png` | 12851→13918（1067） | 批量录入 ×2 | 块首带「批量与修正族 8 格」格头；框内「批量录入 · 补齐槽位」／「批量录入 · 回执」，页上写明现阶段单笔化 |
| 14 | `row-14-批量与修正族-改记录两格.png` | 13918→14967（1049） | 改记录 ×2 | 「改记录 · 挑一条记录」／「改记录 · 回执」 |
| 15 | `row-15-批量与修正族-撤销两格.png` | 14967→16016（1049） | 撤销 ×2 | 「撤销 · 挑一条记录」／「撤销 · 回执」，回执页带 `deleted_at` 的 diff 表 |
| 16 | `row-16-批量与修正族-恢复两格.png` | 16016→17116（1100） | 恢复 ×2 | 「恢复 · 挑一条记录」／「恢复 · 回执」；这一块一直截到页尾（含页尾留白），是全场最后一块 |

**每张都自己看过**：上表末列逐张来自 `read_image` 的结论，16 张的格头文件名、框图里的页面内容、文件名写的族与唤醒词，三处一致（族的格头只在每族第一行那块出现，与文件名写的族名同族）。尺寸：**121823～177696 字节**，1180×1049～1180×1258，1.24～1.48 兆像素；**没有一张大到推不出去**——最大那张 `row-01`（177696 字节）用 `vision_present` 推成功（读数：width 1180／height 1258／bytes 177696／safePresentation true），**没有一张需要再切两段**。

**与旧九张的关系**：旧九张（1500／1900／2960／3740／4500／5450／6460／7260／12400 高）都从页顶起、高度递增，最高的那张只到 12400，页高 12919（旧口径 1560 宽、12 行）⇒ 末尾 519 像素没截到；旧九张里 `08-墙尾-恢复两格.png`／`06-批量与修正第1行.png` 两个名字与内容不符。这一节新 16 张（1180 宽、16 行）才是按行的那套，落在 `.scratch\t407-wall\shots\`；旧九张仍在 `.scratch\t407-wall\` 根下（不在 `shots\` 里），**别再引用旧那套的名字**。

### R3 · 总索引里一处写死了的口径

**改哪**：`t407-全部样张.html`「有意不出产物的」第 3 条（件内第 244 行）。

**改前**（原文照抄）：
> 3. 批量录入没有「多行一次落库」的页　逐行可编辑表本批先单笔化：一次只落一笔，页上明示；多行一次落库未接。依据：`t407-页面块清单-16词.md` 第二节批量录入行「现阶段单笔化」、第三节「逐行可编辑表」行。

事实与依据都是真的，毛病在末尾这半句被当成**已定**的口径写出去，可清单自己写着这句还没核。

**改后**（原文照抄，`保留一句` 之后是新加的；前面的事实与依据一字未动）：
> 3. 批量录入没有「多行一次落库」的页　逐行可编辑表本批先单笔化：一次只落一笔，页上明示；多行一次落库未接。依据：`t407-页面块清单-16词.md` 第二节批量录入行「现阶段单笔化」、第三节「逐行可编辑表」行。
> 保留一句：本条的维护者同意与否，在页面块清单里没有读到出处——清单第五节第 2 条自己就写着「是否已获维护者同意，没有读到出处」；所以这一条只算本批现有的写法，不算已定的口径。

**这句保留的出处**（我读到的原文）：`t407-页面块清单-16词.md` 第 81 行（第五节「没法确认的事」第 2 条）：「逐行可编辑表也没有现成件（`renderDataTable` 只出只读表格）；批量录入「现阶段单笔化」是否已获维护者同意，没有读到出处。」保留句里那句引文逐字来自这一行——没删事实，也没编出处。

**件内 grep 得到**（工具读数）：

```
grep "本条的维护者同意与否" docs\skills\skill-bill\t407-全部样张.html
→ docs\skills\skill-bill\t407-全部样张.html:244: … 保留一句：本条的维护者同意与否，在页面块清单里没有读到出处——清单第五节第 2 条自己就写着「是否已获维护者同意，没有读到出处」；所以这一条只算本批现有的写法，不算已定的口径。
```
件内只命中这一处。另按真宽度 1200 整页截图复核过：四条都还在（第 1、2、4 条一字未动），第 3 条那句保留排在自己的「依据」之后（整页截图页高 **2434**，见 `.dsh-vision-router\artifacts\` 下那张 `…-191dd1d2-shot-1200x720-fullpage.png`，我裁了下半段自己看过）。

### 收尾复核（判据 4／5／6 的读数）

**判据 4 · 结构读数没回退**（数已入盘那版 `t407-手机墙.html`）：`iframe src` **32** · `href` **32** · 目标去重 **32** · 缺失 **0** · `loading="lazy"` 命中 **0** · 外部 http 引用 **0**；生成器自打印同一串读数。另：`grid-template-columns:minmax(0,1fr)` 命中 1、`overflow-x:auto` 命中 1、老的 `grid-template-columns:1fr}` 命中 0。
总索引也没回退：`href` 33 条（32 张卡 ＋ 导语里指向手机墙那条），去重 33，缺目标 **0**；卡 **32** 张；「有意不出产物的」仍 **4** 条。

**判据 5 · 生成器复跑逐字节可复现**（产物改道 .scratch，不覆盖已入盘那版）：

```
node docs\skills\skill-bill\t407-手机墙.mjs D:\ilife\docs\skills\skill-bill ..\..\..\.scratch\t407-wall\t407-手机墙-复跑2.html
```
- 复跑产物：SHA256 `9B03896E9D10797524745BD0388D324799DAFED5102D0284DFB31F87EC7E294B`、12909 字节；
- 已入盘 `t407-手机墙.html`：同一个哈希、同 12909 字节 ⇒ **逐字节相同**；
- 复跑前后已入盘那版的哈希没变 ⇒ 复跑没改到入盘件。

**判据 6 · 四件体检**（`.scratch\t407-wall\final-check.mjs`，按字节断 BOM、按 `TextDecoder(fatal)` 断 UTF-8、按回车符与换行符断换行、按「反斜杠加 n」断字面转义）：

| 件 | 字节 | 行数 | BOM | UTF-8 | 换行 | 字面转义（反斜杠加 n） |
|---|---|---|---|---|---|---|
| `t407-手机墙.mjs` | 9174 | 158 | 无 | 通过 | 全 LF（CRLF 0／单独 CR 0） | 8 处，全在第 98～106 行拼 HTML 的 JS 代码里（JS 的换行转义），**不是正文** |
| `t407-手机墙.html` | 12919 | 177 | 无 | 通过 | 全 LF | 0 |
| `t407-全部样张.html` | 14505 | 252 | 无 | 通过 | 全 LF | 0 |
| `t407-手机墙-证据.md` | 23257 | 206 | 无 | 通过 | 全 LF | 0 |

替换符（U+FFFD）0、NUL 0。四件都没 BOM、都是真实换行。（证据件那一行是本节全部落盘后重跑的终读数；本节只是追加，前文亦一字未动。）
