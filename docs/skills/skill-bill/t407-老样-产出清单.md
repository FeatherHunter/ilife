# t407 老样 产出清单（老写入域 13 场景 · 老渲染器原样产出）

- 渲染器：`D:\2Study\StudyNotes\SKILLS\饼干记账\scripts\render_write.py`（727 行，未改一字，全程只读）
- 公共资产：`D:\2Study\StudyNotes\SKILLS\公共组件\`（`assets\base.css` 17589 B、`assets\base.js` 46892 B，经 `injector.inject()` 内联）
- 库：运行前设 `$env:SKILLS_DB_PATH=D:\ilife\.scratch\t407-old\db`，读的是老库副本（1277952 B），老库与老技能目录全程未写
- 命令前缀统一 `python D:\2Study\StudyNotes\SKILLS\饼干记账\scripts\render_write.py`，`--out` 一律先落 `D:\ilife\.scratch\t407-old\out\`，再原样拷进本目录

## 一、逐场景产出表

| form_type | 老唤醒词 | 渲染命令原文 | 退出码 | 产物路径 | 字节数 | 服务的模板文件 |
| --- | --- | --- | --- | --- | --- | --- |
| expense | 记支出 | `render_write.py expense --amount 38.5 --category-hint 午饭 --note 公司楼下食堂 牛肉面 --account 支付宝 --ledger 生活 --time "2026-09-07 12:20:00" --out .scratch\t407-old\out\t407-老样-expense.html` | 0 | `docs\skills\skill-bill\t407-老样-expense.html` | 83722 | `templates\写入\expense_form.html` |
| income | 记收入 | `render_write.py income --amount 8000 --category-hint 工资 --note 九月工资到账 --account 工资卡 --ledger 生活 --time "2026-09-05 09:00:00" --out ...` | 0 | `docs\skills\skill-bill\t407-老样-income.html` | 83791 | `templates\写入\expense_form.html` |
| photo | 拍账单 | `render_write.py photo --amount 92.8 --category-hint 外卖 --note 美团外卖截图识别 --photo-note "识别自美团外卖截图：刀板香5菜2饭2汤套餐 尾号8741" --images 1 --ledger 生活 --out ...` | 0 | `docs\skills\skill-bill\t407-老样-photo.html` | 83777 | `templates\写入\expense_form.html` |
| reimburse | 记报销 | `render_write.py reimburse --amount 88 --category-hint 客户午餐 --note "客户午餐 #待报销" --account 支付宝 --ledger 生活 --time "2026-05-11 12:00:00" --out ...` | 0 | `docs\skills\skill-bill\t407-老样-reimburse.html` | 83748 | `templates\写入\expense_form.html` |
| batch | 批量录入 | `render_write.py batch --items "[{\"amount\":\"35\",\"category\":\"餐饮/外卖/午餐\",\"note\":\"公司楼下麻辣烫\"},{\"amount\":\"12.5\",\"category\":\"出行/公共交通/公交\",\"note\":\"早高峰通勤\"},{\"amount\":\"199\",\"category\":\"居家/日用品\",\"note\":\"抽纸囤货\"},{\"category\":\"餐饮/零食\",\"note\":\"下午茶待补金额\"}]" --ledger 生活 --out ...` | 0 | `docs\skills\skill-bill\t407-老样-batch.html` | 78245 | `templates\写入\batch_confirm.html` |
| refund | 记退款 | `render_write.py refund --amount 33.4 --search-hint LeThai --reason "商家漏送一份，原路退款" --candidates "[{\"id\":7134,...},{\"id\":7136,...}]" --out ...` | 0 | `docs\skills\skill-bill\t407-老样-refund.html` | 82723 | `templates\写入\flow_confirm.html` |
| reimburse_done | 报销到账 | `render_write.py reimburse_done --amount 88 --search-hint 待报销 --candidates "[{\"id\":6989,...}]" --out ...` | 0 | `docs\skills\skill-bill\t407-老样-reimburse_done.html` | 82302 | `templates\写入\flow_confirm.html` |
| lend | 记借出 | `render_write.py lend --amount 500 --search-hint 小王 --reason 2026-10-08 --out ...` | 0 | `docs\skills\skill-bill\t407-老样-lend.html` | 81941 | `templates\写入\flow_confirm.html` |
| borrow | 记借入 | `render_write.py borrow --amount 1000 --search-hint 小李 --reason 2026-10-01 --out ...` | 0 | `docs\skills\skill-bill\t407-老样-borrow.html` | 81955 | `templates\写入\flow_confirm.html` |
| collect | 记收回 | `render_write.py collect --amount 500 --search-hint 小明 --candidates "[{\"id\":6987,...}]" --out ...` | 0 | `docs\skills\skill-bill\t407-老样-collect.html` | 82260 | `templates\写入\flow_confirm.html` |
| payback | 记偿还 | `render_write.py payback --amount 300 --search-hint 小红 --candidates "[{\"id\":6988,...}]" --out ...` | 0 | `docs\skills\skill-bill\t407-老样-payback.html` | 82258 | `templates\写入\flow_confirm.html` |
| installment | 记分期 | `render_write.py installment --name "MacBook Air M4 14寸" --total 8999 --periods 12 --start-date 2026-09-10 --account 花呗 --ledger 生活 --out ...` | 0 | `docs\skills\skill-bill\t407-老样-installment.html` | 75775 | `templates\写入\installment_confirm.html` |
| update | 改记录 | `render_write.py update --id 7134 --amount 29.9 --note "LeThai乐泰披萨 芒果糯米饭/咸蛋黄嫩鸡披萨一人食（金额更正）" --out ...` | 0 | `docs\skills\skill-bill\t407-老样-update.html` | 74428 | `templates\写入\update_confirm.html` |

命令行原文里 `...` = `D:\ilife\.scratch\t407-old\out\t407-老样-<form_type>.html`；带 `\"` 的 JSON 参数是 PowerShell 数组传参、无 shell 二次解析，完整原文见 `.scratch\t407-old\log\cmds.txt`。

## 二、公共资产注入自证

取两段唯一特征串，逐产物 grep（唯一性已先在资产文件上核对：css 段在 `base.css` 命中 1 次，js 段在 `base.js` 命中 1 次）：

- base.css 段一：`Base Skill token A 组 + 控件样式 v1.11（唯一真相源 · 跨技能 · 领域无关）` → 13/13 命中
- base.css 段二：`--shadow: 0 1px 2px rgba(0,0,0,.04), 0 12px 36px rgba(0,0,0,.06);`（`:root` token A 组里的一行）→ 13/13 命中
- base.css 段三：`smartSelect 选择器组件（v1.11 · #312 · 复用优先·新建其次）` → 13/13 命中
- base.js 段一：`Base Skill 控件库 v1.2（唯一真相源 · 跨技能 · 领域无关）` → 13/13 命中
- base.js 段二：`_SS_SOURCES = ['inferred','recommended_new','existing','history','custom','empty']` → 13/13 命中
- base.js 段三：`function smartSelect(inputEl, config){` → 13/13 命中

结构与注入落地（同一轮逐产物统计）：

- `<!DOCTYPE html>`：13/13 命中；`<section` 标签数：每个产物 1 处
- 老样式 class（`class="hm-…"`／`hero`／`card`／`badge`／`remind`）：每个产物 38–40 处
- 模板占位符残留 `<!--INJECT-DATA-->`／`<!--SHARED-HELPERS-->`／`<!--SHARED-CSS-->`／`<!--CHARTS-HELPERS-->`：13 个产物全部 0 处，说明占位符是被真替换掉的
- 每产物 payload 里的 `scene_id` 与其 form_type 一一对上（write_expense／write_income／write_bill_photo／write_reimburse／write_batch／write_refund／write_reimburse_done／write_lend／write_borrow／write_collect／write_payback／write_installment／write_update）：13/13
- 文件头 3 字节为 `EF BB BF`（渲染器 `_base_render.write_html()` 的 utf-8-sig 契约）

## 三、没能确认的事

- 13 个场景全部跑通，退出码 0，没有跑不动的场景；字节区间 74428–83791。
- 参数上做的两处挪动：① `--out` 全部指向 `.scratch`，老技能目录只读；② `$env:SKILLS_DB_PATH` 由 `D:\2Study\StudyNotes\.db` 改指 `.scratch\t407-old\db`（老库副本），因此这几个场景读到的历史/dev 数据与老机一致，但**老库本身未写一个字节**（运行后 mtime 仍是 2026/9/6 21:08:50）。
- 老库只读的判断依据：`scripts\db.py` 的 `_find_db_path()` 两层查找里环境变量 `SKILLS_DB_PATH` 优先于 `D:/.db`；更关键的是 `fetch_all()` 内部先调 `init_db()`，而 `init_db()` 会执行 `CREATE TABLE IF NOT EXISTS`／`CREATE INDEX IF NOT EXISTS`——也就是**光读历史也会对库发建表语句**，所以必须换库路径，不能靠「只是读」侥幸。
- `update` 用到的 id 7134 是库副本里的真实记录（`餐饮/外卖/午餐 2026-09-06 12:35:27 -33.4`），不是造出来的；`refund` / `reimburse_done` / `collect` / `payback` 的候选走 `--candidates` 显式传入真实 id（7134/7136/6989/6987/6988），脚本兜底匹配那一支没走。`batch` 第 4 条故意缺 amount，用来让 `missing_count` 出数。
- 四场景共用同一张模板：`expense`／`income`／`photo`／`reimburse` 都渲染 `expense_form.html`，其 `<title>` 静态写死「饼干记账 · 记一笔支出」，不随 form_type 变；场景身份只活在注入 payload 的 `meta.wake_word`／`command_cn` 里。要看样式差异得看这个。
- `templates\联动\receipt.html`（180 行）在 `render_write.py` 里检索不到引用，13 个 form_type 没有一条路径服务它，本清单不覆盖它。
- 只做了结构与特征串核对，没有在真机浏览器里逐个打开点按钮；`js1` 之外的 base.js 行为（复制数据/复制日志/smartSelect 交互）未逐个实测。
