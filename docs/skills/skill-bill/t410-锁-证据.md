# t410 机器锁证据（本轮只做机器锁；视觉重排与用户亲手端到端不动）

口径源：`.scratch/bill-t-410-body-r6.md` Question/内核/验收三节。以唤醒词为起点，16词逐条真出口断言：落盘/绝对路径/可打开/字节如实+字段正确；photo/batch降级语义同步断言。不开新页面。红线：只钉现有行为，页行为有误另开票不改。

## 一、16词锁表（补槽值沿t406覆盖表合法L1组合）

| 词 | 路由 | 补槽 | 摘要降级串 |
|---|---|---|---|
| 记支出 | bill.record.add/kind=expense | 餐饮/-12.5 | （记支出） |
| 记收入 | bill.record.add/kind=income | 工资/8000 | （记收入） |
| 拍账单 | bill.record.add/kind=photo | 餐饮/-30 | （三要素以外部识别为准） |
| 批量录入 | bill.record.add/kind=batch | 餐饮/-10 | （只落了其中一笔） |
| 记退款 | bill.record.add/kind=refund | 退款/20 | （记退款） |
| 记报销 | bill.record.add/kind=reimburse | 出行/-100 | （记报销） |
| 报销到账 | bill.record.add/kind=reimburse-done | 其他收入/100 | （报销到账） |
| 记借出 | bill.record.add/kind=lend | 借贷/借出/-500 | （记借出） |
| 记借入 | bill.record.add/kind=borrow | 借贷/借入/500 | （记借入） |
| 记收回 | bill.record.add/kind=collect | 借贷/收回/500 | （记收回） |
| 记偿还 | bill.record.add/kind=repay | 借贷/偿还/-500 | （记偿还） |
| 记分期 | bill.record.add/kind=installment | 分期/-1200 | （记分期） |
| 记一笔 | bill.record.add/无kind | 餐饮/-12.5 | 无尾缀（仅记录编号一组括号） |
| 改记录 | bill.record.update | id+note=改过 | 已修改（改了备注） |
| 撤销 | bill.record.update/op=undo | id | 已撤销，记录还在 |
| 恢复 | bill.record.update/op=restore | id（先undo再restore） | 已恢复（记录编号） |

逐条断言五件：显式`--html`落盘存在；`delivery.path`绝对且等于显式落点；读出含doctype+section可打开；`stat.size=delivery.bytes`；回执字段（add: op=add/recordId>0/affectedRows=1/7项；update系各对op与writtenFields）+页标题`词 · 回执`+分类与两位小数金额+回执三件（对账信息/复制数据/复制日志，data-page单枚）。

降级同步：photo摘要+页内同说外置（本仓之外/三要素）；batch摘要+页内同说单笔（一次只落）；两者仍affectedRows=1/7项（单笔化）。

## 二、三验收机器读数（持锁跑，15秒轮询/10分钟上限）

- 验收1新锁全绿：18/18（13录入+3修正+2降级同步），exit 0。
- 验收2全量只涨不跌：基线160/0 → 本轮178/0（+18，无跌）。
- 验收3变异改坏必红还原绿：改`src/record/write.ts` photo串为坏串→锁红pass16/fail2；还原重编→绿18/0，文件逐字节还原。

GATE-RUN（边跑边补，与`.scratch/locks/gate-runs.log`一对一）：

GATE-RUN runId=408a6860-5d44-40a5-ae1c-4b88d85daa37 cmd="node --test packages/skill-bill/test/*.test.mjs"
GATE-RUN runId=8d397767-b068-4804-9b89-aa479d759272 cmd="node --test packages/skill-bill/test/t410-lock.test.mjs"
GATE-RUN runId=2e1743a3-2255-4935-adc5-42d690bb835e cmd="node --test packages/skill-bill/test/*.test.mjs"
GATE-RUN runId=c30d27ea-cf32-439f-8e29-a4f91f484f5a cmd="node .scratch/t410/mutate-driver.mjs"

| 运行 | runId短 | exit | 摘要 |
|---|---|---|---|
| 基线全量 | 408a6860 | 0 | tests 160/pass 160/fail 0 |
| 新锁 | 8d397767 | 0 | tests 18/pass 18/fail 0（waitedMs 90030，排队后拿到锁） |
| 全量 | 2e1743a3 | 0 | tests 178/pass 178/fail 0 |
| 变异 | c30d27ea | 0 | MUTATE-RED pass16/fail2；RESTORE-GREEN pass18/fail0；clean1 |

日志（本票独占目录，落盘+只读尾5行）：`.scratch/t410/baseline.log`、`.scratch/t410/newlock2.log`、`.scratch/t410/full.log`、`.scratch/t410/mutate.log`、`.scratch/t410/probe.log`（`.scratch`忽略不入库，备查）。

## 三、改动与未做

- 新增：`packages/skill-bill/test/t410-lock.test.mjs`（18条锁）；证据本件。
- 未碰：源码/页面/旧测试/他包（变异临时改`src/record/write.ts`已逐字节还原+重编，`git diff`该件干净）。
- 未做（本轮不动）：视觉重排三节后续、用户亲手端到端16词（待用户亲验收口，非代理代签）。

## 四、对账导出（本窗口410条目原文）

```
START ticket=410 runId=408a6860-5d44-40a5-ae1c-4b88d85daa37 cmd="node --test packages/skill-bill/test/*.test.mjs" waitedMs=0 pid=52128
RUN ticket=410 runId=408a6860-5d44-40a5-ae1c-4b88d85daa37 cmd="node --test packages/skill-bill/test/*.test.mjs" waitedMs=0 exit=0 pid=52128
START ticket=410 runId=8d397767-b068-4804-9b89-aa479d759272 cmd="node --test packages/skill-bill/test/t410-lock.test.mjs" waitedMs=90030 pid=36164
RUN ticket=410 runId=8d397767-b068-4804-9b89-aa479d759272 cmd="node --test packages/skill-bill/test/t410-lock.test.mjs" waitedMs=90030 exit=0 pid=36164
START ticket=410 runId=2e1743a3-2255-4935-adc5-42d690bb835e cmd="node --test packages/skill-bill/test/*.test.mjs" waitedMs=0 pid=71300
RUN ticket=410 runId=2e1743a3-2255-4935-adc5-42d690bb835e cmd="node --test packages/skill-bill/test/*.test.mjs" waitedMs=0 exit=0 pid=71300
START ticket=410 runId=c30d27ea-cf32-439f-8e29-a4f91f484f5a cmd="node .scratch/t410/mutate-driver.mjs" waitedMs=1 pid=73284
RUN ticket=410 runId=c30d27ea-cf32-439f-8e29-a4f91f484f5a cmd="node .scratch/t410/mutate-driver.mjs" waitedMs=1 exit=0 pid=73284
```
