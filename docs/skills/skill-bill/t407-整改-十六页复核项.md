# t407 · 十六页收口 · 按两轴复核整改（本窗＝三次中断后的残留收尾）

被改对象：提交 `5930127`（53 件）＋ `41d6dbc`。整改指令出处＝两轴复核件：

- `docs/skills/skill-bill/t407-复核-十六页-结构纪律.md`（**82 分，须整改（轻），0 严重**）；
- `docs/skills/skill-bill/t407-复核-十六页-独立复跑.md`（**86 分，放行＋附三条**）。

本件是这次整改的账。上一窗做到一半中断，盘上留的是**未验证残留**（S1 拆件、S2 取数提共用位已落盘但没人验过；
E1 补护栏、E3 两页加注重出、任何提交都没做）。本窗接手后**先验后改**，逐节落盘，读数全部实测。

**本窗边界**：`t407-恢复-稳定化证据.md` 归另一窗口，本窗一字未碰（第一节影响清单第 14 行的归属见第四节）。

---

## 一、必报五步

### 第一步 · 影响清单（动手前的清单，事后逐行对，见第四节「逐条对账」）

只碰两处：`packages/skill-bill/`（源码与测试）与 `docs/skills/skill-bill/`（产物与整改记录）。
不碰其它 `packages/`、`packages/base-render/`、仓根、CI、老技能目录。

| # | 件 | 新增／改 | 碰它的理由 |
|---|---|---|---|
| 1 | `packages/skill-bill/src/shared/recentPicks.ts` | 新增 | **S2**：取数那一半（值转字符串／按最近去重取前十二／与本次已给值合并）的唯一落点 |
| 2 | `packages/skill-bill/src/shared/outsideScan.ts` | 新增 | **S1 第二刀**：拍账单「图片识别外置」通道五样另立件，把 `photoEscape.ts` 的对外面收下来 |
| 3 | `packages/skill-bill/src/shared/photoEscape.ts` | 改 | **S1 第一刀 ＋ S2**：取数转发到 `recentPicks.ts`；识别五样交出；表单名字并成三个粗入口 |
| 4 | `packages/skill-bill/src/record/collectBody.ts` | 改 | **S2**：删本地那份同形同名的取数，`pickOf` 留一行转发 |
| 5 | `packages/skill-bill/src/record/scene-income.ts` | 改 | **S1**：改 import（粗入口改名）＋ 件头描述改指新落点 |
| 6 | `packages/skill-bill/src/record/scene-reimburse.ts` | 改 | 同上（同一套粗入口） |
| 7 | `packages/skill-bill/src/record/scene-plain.ts` | 改 | 同上（同一套粗入口） |
| 8 | `packages/skill-bill/src/record/scene-photo.ts` | 改 | **S1**：识别五样的 import 改指 `outsideScan.ts`；件头两处指路同步 |
| 9 | `packages/skill-bill/src/record/scene-installment.ts` | 改 | **E3**：记分期采集页加注「补齐总价／期数／首期日后，本页显示分摊预览（每期金额与日期）」 |
| 10 | `packages/skill-bill/src/record/scene-batch.ts` | 改 | **E3**：批量录入采集页把「多行待接」写成读者看得懂的话 |
| 11 | `packages/skill-bill/test/shared-judgments.test.mjs` | 新增 | **E1**：新共用件与取数共用位里没有断言管着的判定，逐条补到「改坏必红」 |
| 12 | `docs/skills/skill-bill/t407-页-记分期-采集页.html` | 改 | **E3**：照第 9 条重出的整页，CLI 真跑后整份拷入（不手打 HTML） |
| 13 | `docs/skills/skill-bill/t407-页-批量录入-采集页.html` | 改 | **E3**：照第 10 条重出的整页，同上 |
| 14 | `docs/skills/skill-bill/t407-恢复-稳定化证据.md` | 改 | **S3／S5／E2／E4**：**归另一窗口**（它正在改这一件），本窗一字未碰 |
| 15 | `docs/skills/skill-bill/t407-整改-十六页复核项.md` | 新增 | 本件：整改记录与逐条对账 |

**一行一个能力目录**：上表 1–11 落 `packages/skill-bill/`（一个能力包，包内 `src/shared/`、`src/record/`、`test/`
三层都在这一个能力目录里），12–13、15 落 `docs/skills/skill-bill/`（本包自己的文档位）。没有新建能力目录、
没有新建生成器链。

### 第二步 · 结构设计

新增两件，各一句话职责与对外面：

| 件 | 职责 | 对外给的名字（只数真被本目录之外引用的） | 用它的地方 |
|---|---|---|---|
| `src/shared/recentPicks.ts` | 从近期记录里取三枚选择器的候选值（剪空白、按最近在先去重、上限十二、与本次已给值合并） | **3 个**：`textOf`／`pickOf`／`optionsFor`（`PICK_LIMIT` 与 `distinctOf` 收在件内） | ① `src/shared/photoEscape.ts`；② `src/record/collectBody.ts` |
| `src/shared/outsideScan.ts` | 拍账单「图片识别外置」那一条通道（三要素表、已收图片数、识别分工卡、可复制的那段话） | **5 个**：`PhotoScale`／`ESCAPE_FIELDS`／`imageNote`／`escapeCard`／`escapePrompt` | 唯一调用点 `src/record/scene-photo.ts` |

两个件都**零跨目录引用**：只吃普通数据入参，可引 `base-paint`／`base-paint/blocks`／`src/policy/category.ts`／
`src/fetch/db.ts` 与同目录共用件；不引 `src/record/` 下任何一件。

### 第三步～第五步

第三步（结构轴读数）＝第二节；第四步（复跑轴与护栏）＝第三节；第五步（逐条对账与自证）＝第四节、第五节。

---

## 二、S1／S2 · 结构轴点名的整改

### 第一步读数（改前，本窗实测）

```
cd D:\ilife
npx tsc -b packages/skill-bill --force            → 退出码 0，零诊断
node --test "packages/skill-bill/test/*.test.mjs" → exit 0，tests 90 / pass 90 / fail 0
```

读数落在 `.scratch\t407-fix3\tsc-before.txt`（0 行输出）与 `.scratch\t407-fix3\test-before.txt`。
**归因**：别的包、别的窗口在途件这一轮没有抛诊断，整棵树编得过、测试全绿——所以「未验证残留」这一条
在读数上成立：残留没有把树弄红，只是**没有任何断言管着它**（这正是 E1 的来由）。

### S1 · 拆 `photoEscape.ts`（对外 12 名 > 5）

- 读数（LF 口径，`readFileSync(f,'utf8').split('\n').length - 1`）：`photoEscape.ts` **179 行**（拆前 308 行）；
  新件 `outsideScan.ts` **137 行**。
- 对外名字：`photoEscape.ts` **3 名**（`valuesOf`／`fieldCardOf`／`blockedPromptOf`，拆前 12 名）；
  `outsideScan.ts` **5 名**（`PhotoScale`／`ESCAPE_FIELDS`／`imageNote`／`escapeCard`／`escapePrompt`）。
  两件都在件头逐名写了「谁在用」，各调用点后头写明它取哪几个名字。
- 两刀：① 拍账单「图片识别外置」五样搬去 `outsideScan.ts`（唯一调用点 `scene-photo.ts`）；
  ② 表单那六个名字并成三个粗入口（`pickOf`＋`probeOf`＋`factsOf` 合成 `valuesOf`；`commandsOf`＋`promptOf`
  合成 `blockedPromptOf`）。

### S2 · 概念唯一（取数那一半提共用位）

- 判据①：`photoEscape.ts` 与 `outsideScan.ts` 各自对外名字 3／5，都在 5 以内。
- 判据②：`pickOf`／`PICK_LIMIT`／`textOf`／`distinct`／`optionsFor` 这组取数**只剩一处定义**，在
  `src/shared/recentPicks.ts`（66 行）：`PICK_LIMIT = 12` 与 `distinctOf` 收在件内，`textOf`／`pickOf`／
  `optionsFor` 是本件给出去的三名。两侧各留**一行转发**：`photoEscape.ts` 的件内 `pickOf`（转发时用
  `categorySideOf` 定分类退哪一侧）、`collectBody.ts` 的件内 `pickOf`（转发时按 `kind` 选名单）。
- 判据③：`src/shared/` 下 `from '../record/…'` 命中 **0**（`src/shared/` 与 `src/record/` 之间不互相引）。
- 判据④：两件件头都写了谁在用（见 S1 读数），本次另把三个场景件里指错落点的描述改正
  （`scene-income.ts`／`scene-reimburse.ts` 原先指 `photoEscape.ts` 的件内 `categorySideOf`、
  `scene-plain.ts` 原先指已不外给的 `pickOf`、`scene-photo.ts` 原先把识别五样指在 `photoEscape.ts`）。
- 补充读数（供复核者判边界）：`recordPicker.ts` 里另有一个同名常量 `PICK_LIMIT = 20`，它管的是「候选记录
  列表一页最多列几条」，不是选择器候选取几个——两处不是一个概念，本单不并（见第七节第 4 条）。

---

## 三、E1／E3 · 独立复跑轴点名的整改

### E1 · 补护栏（本单最高价值项）

新增 `packages/skill-bill/test/shared-judgments.test.mjs`（335 行，**26 条用例**），专钉五件里
「改坏必红」的判定：`recordPicker.ts`（7 处）、`recentPicks.ts`（6 处）、`flowSteps.ts`（4 处）、
`installmentPreview.ts`（5 处）、`outsideScan.ts`（4 处）。

**自查另发现 29 处无护栏**，逐条补到有断言管着：

| 件 | 原先没有断言管着的判定 | 补法 |
|---|---|---|
| `recordPicker.ts` | ① 撤销／改记录候选只列未删；② 恢复候选只列已软删；③ `op` 落哪一档（缺省与不认识的值落「改记录」）；④ 按编号读一条认得软删行、`deleted` 一并给全；⑤ 编号不存在给 `null`；⑥ 读不通与「库里没有」不许混成一句；⑦ 候选摘要「分类 · 账户 · 备注」与按时间倒序 | 端到端真跑三态（缺省／`op=undo`／`op=restore`）落整页，断言软删那笔在不在、条数与顺序；再直调 `pickerBlock`／`readRowById` 钉空态、读不通态与预选 |
| `recentPicks.ts` | ① 值的字符串形态（数字写十进制串／剪空白／其余空串）；② 按最近在先去重与次序；③ 上限十二；④ 分类退调用方给的 L1 名单、账本退缺省「生活」、账户无兜底；⑤ 选项＝候选＋已给值（不在候选里并到队首）；⑥ 端到端候选项不重复 | 直调 `textOf`／`pickOf`／`optionsFor` 逐条断言；端到端跑通用采集页，数同一分类在 `<option>` 里只出现一次 |
| `flowSteps.ts` | ① 空数组＝空串；② 段状态（缺省「未定」走 warn／`done` 走 ok）；③ 自定义 `state` 压过缺省；④ 段内次序「徽标 → 说明 → 表单 → 成品」；⑤ 段间按给的次序；⑥ 只剩徽标也出得来 | 直调 `flowSteps`，断言徽标类名、文案与各段的先后下标 |
| `installmentPreview.ts` | ① 尾差对齐末期（故合计逐分等于总价）；② 日期每月同日、该月没有就回退月末、跨年同样；③ 先校验再算（总额／期数／首期日七种坏输入各自抛错）；④ 超 24 期折起来、24 期整好不折、折叠区写明哪几期；⑤ 口径行写明尾差去向与合计 | 直调 `installmentShares`／`installmentPreview` |
| `outsideScan.ts` | ① 三要素表只有一处定义（金额／分类／时间）；② 已收图片数两态（0 张给另一条路、给了图说清收图与读图各在哪）；③ 明示卡两态与「缺一样就不写库」；④ 可复制那段话两态（缺项留尖括号占位符、齐全照抄真值） | 直调 `ESCAPE_FIELDS`／`imageNote`／`escapeCard`／`escapePrompt` |

**本窗自挑的第二处变异**（复核者没挑过的）：`recentPicks.ts` 的 `distinctOf`——「候选按最近在先去重」那条。
选它的理由：它是 S2 这次提取出来的口径，提取之前没人管、提取之后原先也没人管；它一坏，选择器里同一个值
会重复出现，而 90 条旧测试全绿。读数见第五节。

### E3 · 两张页加注重出

- **记分期采集页**：入库那份是阻断态（缺 `category`／`amount`，本型另缺总额／期数／首期日——分摊预览看不到）。
  加注写在装配代码里（`src/record/scene-installment.ts` 的 `collectPage`）：预览算不出来那一支多出一行口径
  「补齐总价／期数／首期日后，本页显示分摊预览（每期金额与日期）。」；解析失败那句「分摊没算出来：……」并进
  同一行，两态都不改既有块数。
- **批量录入采集页**：加注写在装配代码里（`src/record/scene-batch.ts` 的 `collectOf`）：「本批现阶段单笔化：
  一次只落一笔，表里一屏 N 行、逐行可编辑。多行待接：这一页会把一屏里的多行都铺出来给你改，但「一屏多行
  一次落库」这一段还没接，眼下一次只落一笔——要落多笔，就按行一笔一笔重跑同一条命令（重跑时只带那一行
  要的值）。」——把「一屏能铺多行」与「一次只落一笔」的差别写清，不写只有本行读得懂的参数名。
- 重出方式：`node packages/skill-bill/dist/cli/cmd_read.js bill.record.add --params <JSON> --html <scratch 落点>`
  各跑一次（`SKILLS_DB_PATH` 指向 `.scratch\t407-fix3\db`、`BILL_FORCE_PROD=1`），产物**整份拷入** docs，
  拷完核对 sha256 与真跑产物一致（**没有手打一个字节的 HTML**）。读数：
  - `t407-页-记分期-采集页.html`＝**71786 字节**，加注在内=true，`data.ok=false`（阻断态）；
  - `t407-页-批量录入-采集页.html`＝**70858 字节**，加注在内=true，`data.ok=false`（阻断态）。
  - 库态前提：跑前该目录下**没有库文件**（首次运行即建表），故候选走 L1 名单那一支。

---

## 逐条对账

| 条目 | 严重度 | 改在哪 | 改后摘要 | 状态 |
|---|---|---|---|---|
| **S1** 拆 `photoEscape.ts`（对外 12 名 > 5） | 形状超线（轻） | `src/shared/photoEscape.ts`、`src/shared/outsideScan.ts`（新）、四个场景件 | 对外 12 名 → **3 名**；识别五样另立件（**5 名**）；两件件头写清谁在用 | **完** |
| **S2** 概念唯一（取数五样两处定义） | 扣 6（本轴最大） | `src/shared/recentPicks.ts`（新）、`photoEscape.ts`、`src/record/collectBody.ts` | 取数只剩**一处定义**在 `recentPicks.ts`；两侧各留一行转发；`src/shared/` 跨目录引用 **0** | **完** |
| **S3** 证据件两处不实（`SKILL.md`、16 个场景件） | 扣 4 | `docs/skills/skill-bill/t407-恢复-稳定化证据.md` | 归**另一窗口**（它正在改这一件）；本窗一字未碰 | 另窗 |
| **S4** 真对账（覆盖第一步影响清单） | 扣 4 的同一处 | 本件第四节与第一节影响清单 | 清单 15 行、本窗实改 **14 件**（第 14 行属另一窗口），逐行对上，**偏差 0** | **完** |
| **S5** 事故面中文段计数不可复算 | 扣 1 | 同 S3 那一件 | 归另一窗口 | 另窗 |
| **E1** 补护栏（变异存活、零护栏） | 扣 4（放行附三条第 1 条） | `test/shared-judgments.test.mjs`（新） | 26 条用例钉住五件 **29 处**判定；两组变异「改坏必红、还原必绿」；测试数 90 → **116** | **完** |
| **E2** 冻结产物的库态前提未写明 | 扣 2（第 2 条） | 同 S3 那一件 | 归另一窗口；本窗另在本件第三节写明自己重出那两张的库态前提 | 另窗 |
| **E3** 两张页加注 | 扣 5（第 3 条） | `scene-installment.ts`、`scene-batch.ts`、两张产物 | 两处加注写在装配代码里；两页 CLI 真跑重出、整份拷入（71786／70858 字节） | **完** |
| **E4** 更正两处自称数字（行数、路径） | 扣 1 | 同 S3 那一件 | 归另一窗口 | 另窗 |

**交付对账（覆盖第一步影响清单，逐行核）**：本窗实际改 **14 件**，与清单 1–13、15 行**逐字对上**
（件名、新增／改都对得上）；清单第 14 行点名的那一件归另一窗口，本窗一字未碰、也不在对账范围内。
**偏差＝0**。上一窗被判的两处偏差（清单点名 `SKILL.md` 而该件一字未改、清单说 16 个场景件实为 14 件）
都落在另一窗口的那件证据里，本窗不转述、不背账，只如实列本窗实际动过的件名。

---

## 变异自证

跑法：备份 → 改坏 → `npx tsc -b packages/skill-bill --force` → `node --test "packages/skill-bill/test/*.test.mjs"`
→ 还原备份 → 同一套再跑一遍。读数落 `.scratch\t407-fix3\mut-A.txt`／`mut-B.txt`。

### 第一组 · 复核者挑的那一处（`recordPicker.ts` 的 `keeps`：「撤销候选只列未删」）

改成 `return mode === 'restore' ? isDeleted(row) : true;`

```
【变异态】 tsc=0（零诊断） test exit=1 pass=115 fail=1
          红着的用例：撤销候选只列未删：软删那笔一个都不出现、条数按未撤销数报
【还原态】 tsc=0（零诊断） test exit=0 pass=116 fail=0（红着的用例：无）
```

**这条过去是绿的**（复核者实测：改坏后 `tsc` 与 90 条测试全绿）——本单的核心修复就在这一行读数上。

### 第二组 · 本窗自挑（`recentPicks.ts` 的 `distinctOf`：「候选按最近在先去重」）

改成 `if (v !== '') out.push(v);`

```
【变异态】 tsc=0（零诊断） test exit=1 pass=113 fail=3
          红着的用例：候选按最近在先去重：同一个值只留第一次出现的那一个，次序按库里的先后
                     ／分类一条历史都没有才退到调用方给的 L1 名单；账本退到缺省「生活」；账户没有兜底
                     ／端到端：采集页的分类候选不重复（同一分类在选项里只出现一次）
【还原态】 tsc=0（零诊断） test exit=0 pass=116 fail=0（红着的用例：无）
```

两组都是**「变异态只有测试红、诊断不红」**：证明抓住它们的是断言，不是类型。

### 还原三自证

| 自证 | 读数 |
|---|---|
| 改过的件 sha256 与备份一致 | `recordPicker.ts` 变前＝还原后＝`37df28f5…`；`recentPicks.ts` 变前＝还原后＝`52c20f75…`（备份文件已删，比对在同一次运行内完成） |
| `git diff --stat -- packages/skill-bill` | 空（提交后实测，见第七节） |
| `git status --porcelain -- packages/skill-bill` | 空（同上） |

**还原后重跑不开增量**：两组自证的「还原态」都是 `--force` 全量重建之后再跑的（本仓踩过增量编译假绿的坑）。

---

## 未改与理由

1. **`recordPicker.ts` 里剩下没钉的判定**：`snapshotRows`／`snapshotTable` 的九行回显与缺省标题、
   `pickerBlock` 的「预选编号」与自定义提示两句、`byTimeDesc` 的「同一时刻按编号倒序」那一支。
   理由：前两条要改库态或造样本才看得出差别，第三条要有两条同刻的样本；三条都不进任何一条复核判据，
   本次把「凡进判据的判定逐条钉住」办完，余下这四条留作下一张护栏票。
2. **各场景件自带的那个小 `textOf`（`src/record/scene-*.ts` 里 15 处）没有并进 `recentPicks.ts`**：
   它们的口径其实有**三样**（只认字符串、数字也转十进制串、缺省给「未设置」），硬并成一处会改行为；
   本单只并「采集页取数」这一族，别处不动。这一条是判据②的边界，写在这里备查。
3. **记分期回执页那句「这次写库没带总额／期数／首期日，摊不出来」保留**：E3 点名的是采集页，回执页那句话
   已经说清了自己的情形，不改。
4. **`recordPicker.ts` 的 `PICK_LIMIT = 20` 与 `recentPicks.ts` 的 `PICK_LIMIT = 12` 并存**：前者管「候选
   记录列表一页最多列几条」、后者管「选择器候选取几个」，不是同一个概念；合并会把两处不相干的数字绑在
   一起，本单不并。
5. **禁用词命中不在本单的件上**：本单新增／改动的件与文档**零命中**（逐字查过）。命中只在两处：
   ① 既有的 `src/render/*` 与 `src/triggers/wake-assets.ts`（本单不动，也不是本单的件）；
   ② `packages/base-render/src/controls.ts:932` 那段注释——它被内联进每一张页产物，**30 张页都带**，
   重出的这两张也各带 2 处。那一处住在**派单明令不许碰的目录**里（改它等于动别的窗口的件），故**报不修**，
   留给碰 `base-render` 的那张票。
6. **`docs/skills/skill-bill/` 下其它件、其它 `packages/skill-*`、仓根、CI、老技能目录**：派单明令不动。

---

## 交棒

1. **本窗交出的读数**：改前 `tsc` 退出码 0 ＋ 测试 90 通过／0 失败；改后 `tsc` 退出码 0（零诊断）＋
   **116 通过／0 失败**（只涨不跌）；两组变异自证＋三条还原自证（第五节）。
2. **归另一窗口的四条**：S3／S5／E2／E4 都落在 `t407-恢复-稳定化证据.md`；它要引本窗读数时，用本件
   「逐条对账」那张表与「变异自证」那两组读数，别转抄本窗没测过的数。
3. **留给下一张护栏票的**：本件第七节第 1 条那四条判定，以及第 2 条那三样口径要不要收敛。
4. **留给碰 `base-render` 那张票的**：第七节第 5 条那处禁用词（页产物内联带出，30 张都带）。
5. **提交**：本窗一次提交，显式路径、不动工作树里别的在途改动；**不 push**。
