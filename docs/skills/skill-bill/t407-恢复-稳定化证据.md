# t407 恢复 · 崩溃残留稳定化证据

**本件是收口件**：三个并发窗口在铺写入域 15 张定制页时因网络中断同时阵亡，盘上留下未提交残留
（13 个场景件已改、4 个新共用件、`wakewords.ts` 已改，无验证、无产物、无提交）。
本件把这份残留稳定到「编译通过 ＋ 测试全绿 ＋ 15 条词各出两支页」。

写者：这一窗唯一的写者，对 `packages/skill-bill/` 与 `docs/skills/skill-bill/` 下本任务点名的件有写权。
提交纪律：全程只有 `git add <显式路径>` ＋ `git commit -- <显式路径>`，未用 amend／rebase／reset／push／checkout／restore，未 `git add -A`。

---

## 一、体检结论

体检在动手之前跑，落在 `tsc -b packages/skill-bill --force` 与 `node --test "packages/skill-bill/test/*.test.mjs"` 两条命令上。

| 项 | 读数 |
|---|---|
| `npx tsc -b packages/skill-bill --force` | **退出码 0，诊断 0 条**（全输出只有一行 `EXIT=0`） |
| `node --test "packages/skill-bill/test/*.test.mjs"` | **90 条用例，通过 88，失败 2** |

**两条失败用例（逐字）**：

1. `policy.test.mjs:30` → 「唤醒词 77 全量 + 最长匹配 + 缺槽位」，断言 `assert.throws(() => routeWakeword('改记录'), /缺槽位 id/)`，报 `Missing expected exception`。
2. `skill.test.mjs:45` → 「互联区新鲜（构建期注入可复现）」，报 `SKILL.md` 里那段注入块的「改记录／撤销／恢复」三行示例与当场重算出来的不一致。

**归因**：两条**都在本包内**，都由这一票残留自身引起，没有出现别的包（`packages/skill-calorie`／`base-render` 等）或别的窗口在途件的诊断，故不需要分开报，也没有去改别人的件。

- 第 1 条的成因：残留把 `wakewords.ts` 里「改记录／撤销／恢复」三条的 `needs: ['id']` 去掉了（施工图第二节「撤销」「恢复」两行的缺项阻断栏把「缺 id 就在路由层报错」点名成要修的缺陷），而 `policy.test.mjs` 还停在旧口径上——**用例过时**，不是产品错。
- 第 2 条的成因：`SKILL.md` 的互联系表是构建期注入物（`scripts/build-help.mjs`），残留改了 `wakewords.ts` 却没重跑注入，**产物停在旧输入上**。

两条的处置见第二节。

---

## 二、回退登记

**回退 0 件。** 没有哪个场景件被判成「补不完、改不动」，因此没有对任何场景件动用「回退到拆件后占位形态」这一条，
`git checkout -- packages/skill-bill/src/record/scene-*.ts` 一次都没跑。

两处失败不是靠改产品迁就旧用例，也不是靠回退绕过，而是各自修在正确那一侧：

| 失败 | 修在哪 | 修法 |
|---|---|---|
| 用例过时（第 1 条） | `test/policy.test.mjs` | 把「只说『改记录』必须在路由层报缺槽位」那条旧断言换成新口径的三条判定（三条词各自仍路由到 `bill.record.update`），并留一段注释写明为什么改、缺陷出处在哪一行 |
| 产物过时（第 2 条） | `packages/skill-bill/SKILL.md` | 跑包内既有的 `node scripts/build-help.mjs` 重注互联系表（**没建生成器链、没跑 `pnpm gen`**） |

### 编码事故与返工登记（如实记）

动手过程中，有一次「批量改三处措辞」用了 PowerShell 的 `Get-Content -Raw` ＋ `Set-Content -Encoding utf8` 管道，
那台机器的默认解码页把这 3 个件的 UTF-8 中文读错再写回，**盘上这 3 个件的正文被打坏**：
`src/help/lookup.ts`（41 处坏点）、`src/record/scene-income.ts`（65 处）、`src/record/scene-reimburse.ts`（102 处）。
发现后逐件判定来源并返工，**没有一件是靠猜的**：

| 件 | 返工来源 | 校验 |
|---|---|---|
| `src/help/lookup.ts` | `git show HEAD:…` 取原件，再逐字重放本票那三处改动 | `git diff` 只剩那三处（一处分类型签名、一处注释＋循环、一处措辞） |
| `src/record/scene-income.ts` | 本窗在事故前用读工具读过整件，按那一份逐行重建 | 见下「四段对账」，旧新两版**逐段 0 差** |
| `src/record/scene-reimburse.ts` | 按坏点上下文重修（事故前未整读，故注释由本窗按原意重写） | 同上，四段对账最终**逐段 0 差** |

「四段对账」的判据：拿事故前**已经落盘**的那 4 张页（`t407-页-记收入-采集页.html`／`-回执页.html`、
`t407-页-记报销-采集页.html`／`-回执页.html`）当基准，重建后按同一次跑法重跑，把两份页里所有中文段落
（长度 ≥2 的连续中文段）取成集合逐段比。首轮结果 0／2／0／3 段有差，按差集逐条改回去，最终四张页**全部 0 差**：

```
t407-页-记收入-采集页.html  exit=0  oldSeg=199 newSeg=199  旧有新无=0 新有旧无=0
t407-页-记报销-采集页.html  exit=0  oldSeg=221 newSeg=221  旧有新无=0 新有旧无=0
t407-页-记收入-回执页.html  exit=0  oldSeg=168 newSeg=168  旧有新无=0 新有旧无=0
t407-页-记报销-回执页.html  exit=0  oldSeg=180 newSeg=180  旧有新无=0 新有旧无=0
BAD=0
```

事故的教训照实写进第七节交棒：本仓改中文源文件只走写入工具，不走 PowerShell 的读→写管道。

---

## 三、场景件完成度逐件表

判据是具体的：缺哪个函数、引用了不存在的名字、两格是不是指向本件自己的装配体、件头那一行的块有没有落齐。

### 15 个场景件

| 唤醒词 | 件（LF 行数） | 体检判定 | 本票动作 |
|---|---|---|---|
| 记支出 | `scene-expense.ts`（22） | **完整可用**（代表页那一件：两格指通用装配体 `collectBody`／`receiptBody`，代表页就是从它出的） | 不动 |
| 记收入 | `scene-income.ts`（198） | **完整可用**（两格各有本件装配体 `collectIncome`／`receiptIncome`；收入侧方向、收入侧分类候选、收入语术三处场景差异齐） | 事故返工重建（见第二节），四段对账 0 差 |
| 拍账单 | `scene-photo.ts` | **根本没做**（残留期未动：22 行占位件，两格还指通用装配体，图片入口、三要素填空、已收图片数一概没有） | **新做**（229 行） |
| 批量录入 | `scene-batch.ts`（232） | 完整可用（`rowEditorTable` 逐行可编辑表已接） | 不动 |
| 记退款 | `scene-refund.ts`（271） | 完整可用（流程三段式 `flowSteps` ＋ 候选单选 `candidatePick`） | 不动 |
| 记报销 | `scene-reimburse.ts`（247） | **完整可用**（打标提示条 `tagNote` 与来源提示条 `sourceNote` 各占一块，正是施工图第四节第 9 条要修的缺陷） | 事故返工重建（见第二节），四段对账 0 差 |
| 报销到账 | `scene-reimburse-done.ts`（280） | 完整可用（流程三段式 ＋ 候选单选 ＋ 打标说明条） | 不动 |
| 记借出 | `scene-lend.ts`（273） | 完整可用（期限字段只在借贷两型渲染，施工图第四节第 11 条已修） | 不动 |
| 记借入 | `scene-borrow.ts`（272） | 完整可用（同上） | 不动 |
| 记收回 | `scene-collect.ts`（281） | 完整可用（消标说明 ＋ 候选单选） | 不动 |
| 记偿还 | `scene-repay.ts`（281） | 完整可用（同上） | 不动 |
| 记分期 | `scene-installment.ts`（235） | 完整可用（分摊预览表 `installmentPreview` ＋ 期数角标） | 不动 |
| 记一笔 | `scene-plain.ts` | **根本没做**（残留期未动：23 行占位件，两格还指通用装配体） | **新做**（207 行） |
| 改记录 | `scene-update.ts`（245） | 完整可用（三形态齐：缺编号列候选／带编号出原记录＋diff 表／那条已打标撤销） | 只改一处：软删判定改吃 `readRowById` 新给的 `deleted`（见下） |
| 撤销 | `scene-undo.ts`（262） | 完整可用（缺编号时列候选，不再在路由层报错） | 不动 |
| 恢复 | `scene-restore.ts`（252） | 完整可用（候选只列已打标撤销的那些） | 只改一处：同上 |

**本票新做的两件，逐块照施工图第二节那一行**：

- 拍账单（`scene-photo.ts`）：页头与页面外框、类型徽章（图片识别确认）、结论摘要行、口径行、**已收图片数说明**
  （`imageNote`，报「已收到 N 张账单图片」并且不装上图控件）、**识别外置分工卡**（`escapeCard`：三要素现在到了哪一步 ＋
  每一步在谁那里办）、重复检测提示条、预填标注、**缺项阻断条**（三要素缺一不出可跑的写库指令）、
  **文字三要素填空**（金额／分类／时间，名字取自 `ESCAPE_FIELDS` 这一处定义）、复制指令块（那段话由 `escapePrompt` 出）、
  动作区、错误回执（阻断条内）。`params.images`／`params.imageWhere` 两个页面级入参写在件头。
- 记一笔（`scene-plain.ts`）：页头与页面外框、类型徽章（按金额符号判支出／收入）、结论摘要行、口径行（写清「给什么符号就记什么方向」）、
  重复检测提示条、预填标注、缺项阻断条、字段卡（三级分类＋心法）、空态、复制指令块、动作区、错误回执（阻断条内），
  另加一条口径行照实报「认不得的 `kind` 也落这一件」（落点判定的兜底那一支）。

### 4 个新共用件 ＋ `wakewords.ts`

| 件（LF 行数） | 判定 | 依据 |
|---|---|---|
| `src/shared/flowSteps.ts`（69） | **完整可用** | 对外 1 个名字（`flowSteps`）真被引用；6 条流程型词共用；只引 `base-paint`／`base-paint/blocks` |
| `src/shared/installmentPreview.ts`（170） | **完整可用** | 对外 2 个名字（`installmentPreview`／`installmentShares`）；只引 `base-paint/blocks` |
| `src/shared/recordPicker.ts`（231） | **半写 → 本票补完** | 原样对外 11 个名字（含 1 个没人引的空函数、4 个只在本件内用的名字）。补完：删掉没人引的 `pickLabelOf`；把 `PickMode`／`CandidateRead`／`readCandidates`／`snapshotRows` 收回件内；把「一条记录是否已打标撤销」这一处判定并进 `readRowById` 的返回值（`deleted`），对外只剩 **5 个名字** |
| `src/shared/photoEscape.ts`（308） | **完整可用，但对外名字超线（报警，见第六节）** | 原样对外 17 个名字，含 5 个彻底没人引的（`collectCaliber` 等）。补完：删掉没人引的 `collectCaliber` 与和 `escapeCard` 重复的 `escapeFacts`；把 `textOf`／`formFieldsOf` 收回件内；件头把「四个调用点各取哪几个名字」逐条写明。**仍对外 12 个名字，超 5，未拆，理由与拆法见第六、七节** |
| `src/policy/wakewords.ts`（142） | **半写 → 本票补完** | 残留只「删掉 `needs: ['id']`」，副作用是连「上下文里给了 `id` 就带进命令」和「HELP 速查表那一格示例」一起丢了（`SKILL.md` 因此与源码对不上）。补完：把「必需槽位」与「不拦、给了就随命令带下去」分成两格（`needs`／`carries`），三条修正词落 `carries: ['id']`——路由层不再报错，示例与带值照旧。`SKILL.md` 重注后与 HEAD **一字不差** |

**契约判定（4 个新共用件）**：

- **跨目录引用：0 处**。`src/shared/` 里 `from '../record/…'` 一次都没有；四个件都只吃普通数据入参，
  页面入参（`CollectInput`／`ReceiptInput`）由各场景件自己解成普通数据再传进来。
- **只用 base 现成组件**：四件的 import 只落在 `base-paint`／`base-paint/blocks`（外加上层允许的 `../policy/category.js`、
  `../fetch/db.js` 与同目录共用件——这与已冻的 `duplicateNote.ts`／`prefillNote.ts`／`summaryRow.ts` 同形状）。
- **件头写清谁在用**：四个件都写得出第二个用法（`flowSteps` 6 条词、`installmentPreview` 1 条 ＋ 同族备查、
  `recordPicker` 3 条词、`photoEscape` 4 个场景件，逐条指名）。

---

## 四、30 份产物读数

落点 `docs/skills/skill-bill/`，命名 `t407-页-<唤醒词>-采集页.html`／`-回执页.html`。
跑法：`SKILLS_DB_PATH` 指向临时库，`node packages/skill-bill/dist/cli/cmd_read.js <命令名> --params <JSON> --html <落点>`；
采集页只给 `kind`／`op`（**不给必需槽位**），回执页把槽位补全后同一支命令重跑。`kind`／`op` 取值逐字照 `t407-场景落点清单.md`。

| 读数 | 值 |
|---|---|
| 成功份数 | **30／30** |
| 退出码分布 | `采集页:0` 15 份、`回执页:0` 15 份（**30 份全部 exit 0**） |
| 字节数 | 最小 **67,460**（改记录-回执页），最大 **75,049**（拍账单-采集页）；**30 份全部 ≥ 50KB** |
| 逐页认出「这一页真是那一件出的」 | 30 页各查本件独有标记，**缺 0 处**（例：拍账单-采集页命中「本仓不装识别引擎」与「张账单图片」，记一笔-采集页命中「按金额符号判支出／收入」与「认不得的」，撤销-采集页命中「要撤销的那条记录」） |

逐份字节数（采集页在前、回执页在后）：

| 唤醒词 | 采集页 | 回执页 |
|---|---|---|
| 记收入 | 71,794 | 69,573 |
| 记报销 | 73,805 | 70,745 |
| 记一笔 | 71,925 | 69,757 |
| 拍账单 | 75,049 | 69,068 |
| 记退款 | 74,787 | 69,390 |
| 报销到账 | 74,489 | 69,467 |
| 记借出 | 73,420 | 69,368 |
| 记借入 | 73,424 | 69,379 |
| 记收回 | 74,204 | 69,416 |
| 记偿还 | 74,218 | 69,406 |
| 记分期 | 71,573 | 69,506 |
| 批量录入 | 70,554 | 68,892 |
| 改记录 | 69,918 | 67,460 |
| 撤销 | 70,096 | 68,444 |
| 恢复 | 67,860 | 68,301 |

产物一律由真跑后整份拷入，**没有手打整页 HTML**。

---

## 五、变异自证

挑两处判定各改坏 ⇒ 必红；还原 ⇒ 必绿。每轮都跑 `npx tsc -b packages/skill-bill --force` 再跑测试
（增量编译会因 mtime 不重编给出假绿，本仓踩过）。

**变异甲：把「改记录／撤销／恢复」缺编号不该在路由层报错这一条改坏**
`src/policy/wakewords.ts` 给「撤销」那一行加回 `needs: ['id']`（正是残留改掉的那一处）。

- 改坏后：`tsc` 退出码 0、诊断 0（这一处是行为不是类型，理当不报）；`node --test` **89 通过／1 失败**，
  失败用例 = `policy.test.mjs:30`「唤醒词 77 全量 + 最长匹配 + 缺槽位」。**红**。
- 还原后：`tsc` 退出码 0；`node --test` **90 通过／0 失败**。**绿**。

**变异乙：把「一条词一件」的落点判定改坏**
`src/record/scene.ts` 的 `sceneFor` 里，把「命令名是 `bill.record.update` → 改记录那一件」这条判定提到 `op` 判定之前
（撤销／恢复会被误落到改记录那一件）。

- 改坏后：`tsc` 退出码 0、诊断 0；`node --test` **88 通过／2 失败**，
  失败用例 = `frozen-blocks.test.mjs:31`「16 条词各落一件：落点两两不同、顺序与清单一致、每件场景件在盘上」
  ＋ `frozen-blocks.test.mjs:58`「认不得的 `kind` 落通用词那一件；不带 `kind` 的『记一笔』也落它」。**红**。
- 还原后：`tsc` 退出码 0；`node --test` **90 通过／0 失败**。**绿**。

**三条还原自证**（还原之后、提交之后各验一次）：

1. `git diff --stat -- packages/skill-bill` → **空**。
2. `git status --porcelain -- packages/skill-bill` → **空**。
3. 改过文件 sha256 与备份一致：`src/policy/wakewords.ts`＝`E7889FF820446CE45AB326BF77B2511C6151127AC739CEFD20238C35819E3180`、
   `src/record/scene.ts`＝`3D0A795E51F59065003FA039FE00257BCA103FAF08E015F2895B409AA6CF03F7`、
   `src/record/scene-plain.ts`＝`7A42FFB74ECD48806EAF0E723AD8B4163F817ABFD151D2F8431171F002A5BB92`——**三件逐字一致**。

---

## 六、超线报警与五步对账

### 行数超线（本包告警线 350 行，LF 口径）

**本票新增／改动的件，逐件 LF 行数与是否超线**：

| 件 | LF | 结论 |
|---|---|---|
| `src/shared/photoEscape.ts` | 308 | 在 350 以内 |
| `src/record/scene-repay.ts` | 281 | 在 350 以内 |
| `src/record/scene-collect.ts` | 281 | 在 350 以内 |
| `src/record/scene-reimburse-done.ts` | 280 | 在 350 以内 |
| `src/record/scene-lend.ts` | 273 | 在 350 以内 |
| `src/record/scene-borrow.ts` | 272 | 在 350 以内 |
| `src/record/scene-refund.ts` | 271 | 在 350 以内 |
| `src/record/scene-undo.ts` | 262 | 在 350 以内 |
| `src/record/scene-restore.ts` | 252 | 在 350 以内 |
| `src/record/scene-reimburse.ts` | 247 | 在 350 以内 |
| `src/record/scene-update.ts` | 245 | 在 350 以内 |
| `src/record/scene-installment.ts` | 235 | 在 350 以内 |
| `src/record/scene-batch.ts` | 232 | 在 350 以内 |
| `src/shared/recordPicker.ts` | 231 | 在 350 以内 |
| `src/record/scene-photo.ts` | 229 | 在 350 以内（本票新做） |
| `src/record/scene-plain.ts` | 207 | 在 350 以内（本票新做） |
| `src/record/scene-income.ts` | 198 | 在 350 以内 |
| `src/policy/wakewords.ts` | 142 | 在 350 以内 |
| `src/help/lookup.ts` | 55 | 在 350 以内 |
| `src/record/scene-expense.ts` | 22 | 在 350 以内 |

**本票超线件：0 件。** 包内既有的两件超线件不是本票的件、本票也没碰：
`src/triggers/wake-assets.ts`（971，生成物，拆法待生成器链那张票）、`src/cli/cmd_read.ts`（526，既有超线件，拆法＝按域拆分派，属读命令搬迁那张后票）。

### 形状超线报警：`photoEscape.ts` 对外名字 12 个（照结构标准不多于 5 个）

- 读数：盘上 `export` 13 个；**真被 `src/record/` 下引用的 12 个**（`FieldSlot`／`pickOf`／`fieldCardOf`／
  `commandsOf`／`promptOf`／`probeOf`／`factsOf`／`PhotoScale`／`imageNote`／`escapeCard`／`escapePrompt`／`ESCAPE_FIELDS`）。
- 超因：这一件是「四个场景件共用的采集页积木」＋「只服务拍账单一件的外部识别通道」**两件事同住**，粗入口还没抽。
- **本次没当场拆，理由**：拆它要同时改四个场景件（其中记收入／记报销是别的族的交付件），
  在本票预算内动手就会威胁「30 份产物」这条硬要求；先把已经在盘上的正确件保住，把拆法登记给下一手。
- 拆法（下一手第一件事）：① 表单六个名字并成两个粗入口（字段卡把候选取值吃进去；prompt 那段话与写库指令原文合成一段）；
  ② 外部识别那五样（`PhotoScale`／`imageNote`／`escapeCard`／`escapePrompt`／`ESCAPE_FIELDS`）搬去 `src/record/scene-photo.ts` 自用，
  顺手把件名与内容对齐。两刀下去落到 5 个名字以内。

### 必报五步

1. **影响清单**：本票只碰 `packages/skill-bill/`（16 个场景件、4 个共用件、`wakewords.ts`、`help/lookup.ts`、`test/policy.test.mjs`、`SKILL.md`）
   与 `docs/skills/skill-bill/` 下本票自己的产物与证据件。未碰 `packages/base-render/`、`packages/skill-calorie/`、
   其它 `packages/skill-*`、仓根、CI、老技能目录；`docs/skills/skill-bill/` 下原有件一律未改。
2. **结构设计**：写入域按「一条词一件」已成形状——分派位 `record/scene.ts` 的落点表 16 行、一件一组装体；
   共用件住 `src/shared/` 且**零跨目录引用**（只吃普通数据入参）；本票新增的两个场景件照同一形状落位，
   没有新造目录层级、没有新造生成器链。
3. **写代码**：本票新增 `scene-photo.ts`／`scene-plain.ts` 两个场景件，补完 `recordPicker.ts`／`photoEscape.ts`／`wakewords.ts` 三件，
   改 `scene-restore.ts`／`scene-update.ts` 各一处（软删判定改吃 `readRowById` 的 `deleted`），重建 `help/lookup.ts`／`scene-income.ts`／`scene-reimburse.ts`（见第二节）。
4. **超线报警**：行数 0 件超线；形状超线 1 件（`photoEscape.ts` 对外 12 个名字 > 5），已在上一小节报警并给出拆法。
5. **交付对账**：任务书列的交付物逐条对：

| 任务书要求的交付物 | 实数 | 偏差 |
|---|---|---|
| `packages/skill-bill/` 的源码（稳定化后的状态） | 编译通过、测试全绿、16 个场景件两格齐、4 个共用件在盘 | 0 |
| 30 份产物（15 条词 × 采集页／回执页） | **30 份，全部 exit 0，全部 ≥ 50KB** | 0 |
| `docs/skills/skill-bill/t407-恢复-稳定化证据.md`（七节） | 本件，七节齐 | 0 |
| 一条提交，信息逐字为任务书给的那一句，未 push | 见第七节提交号 | 0 |

**对账偏差：0。**

---

## 七、交棒

### 本票做完的
- 15 条唤醒词的场景件全部到位：13 件残留**完整可用**（其中 2 件经事故返工重建、逐段对账 0 差），
  2 件残留期**根本没做**的（拍账单 `scene-photo.ts`、记一笔 `scene-plain.ts`）**照施工图做出来了**。
- 4 个新共用件全部核过契约：**零跨目录引用**、只用 base 现成组件、件头写得出第二个用法；
  `recordPicker.ts` 从 11 个对外名字收到 **5 个**；`photoEscape.ts` 拆掉了没人引的名字（超线未拆，已报警）。
- `wakewords.ts` 的破口补齐：路由层不再报「缺槽位 id」，同时把「给了 `id` 就带进命令」与 HELP 速查表示例一起找回来，
  `SKILL.md` 重注后与 HEAD 一字不差。
- 编译通过（`tsc -b --force` 退出码 0、诊断 0）、测试全绿（90 通过／0 失败）、30 份产物全出。

### 下一手第一件事
**拆 `src/shared/photoEscape.ts`**（形状超线，第六节给了两刀拆法）：表单六名并成两个粗入口，
外部识别那五样搬进 `scene-photo.ts` 自用。拆完跑 `npx tsc -b packages/skill-bill --force` ＋
`node --test "packages/skill-bill/test/*.test.mjs"`，再重跑 30 份产物对账（第三节那张表就是基准）。

### 半点没确认的事 / 下一手缺什么
- **`photoEscape.ts` 那 12 个对外名字没拆**——已在盘上正确、只是没做形状收口，这是本票唯一一处自知未收的线。
- **记报销件头的注释是本窗按原意重写的**（事故前没整读过它）：正文与四张页逐段 0 差，但注释措辞与原稿不保证逐字相同。
  记收入件的正文与注释都按事故前读过的原稿重建，可放心引用。
- 拍账单的 `params.images`／`params.imageWhere` 两个页面级入参**只写在件头，没进命令注册表**；
  这两个名字的最终口径没在仓内找到出处（老侧同样只有一行「已收到 N 张账单图片」），要维护者定。
- 本机 PowerShell 的默认解码页会弄坏 UTF-8 中文源文件：**改中文源件只走写入工具，不要走 PowerShell 的读→写管道。**
