# t689 写入域 · 证据（形状重构与 parity 优化）

- 票：[write 域：形状重构与 parity 优化](https://github.com/FeatherHunter/ilife/issues/689)（父图 [#681](https://github.com/FeatherHunter/ilife/issues/681)）
- 形状规格＝[#685](https://github.com/FeatherHunter/ilife/issues/685) 收口的两件规格：`packages/skill-bill/docs/t685-接口与判据.md`（接口与判据）＋ `packages/skill-bill/docs/t685-按域页型表.md`（按域页型表；写入域＝§2.1 五张模板）。
- 对照基准＝[#688](https://github.com/FeatherHunter/ilife/issues/688) 的 `docs/skills/skill-bill/688-融合基准.md`（强制）；清单＝[#687](https://github.com/FeatherHunter/ilife/issues/687) 的 `docs/skills/skill-bill/t687-parity-审计清单.md`（T6）。
- 差异留痕＝`docs/skills/skill-bill/t689-差异表.md`（老行为／新行为／为什么改／谁批的）。
- 读数口径：**LF**（只数 `\n`）；产物指纹＝整页 sha256，先把「今天 ±1 天」的日期时间串换成 `<TS>` 再取（它们来自时钟）。

## 一、交付物（本票写集）

| 类 | 件 | 说明 |
|---|---|---|
| 模板（块位序列的唯一住所） | `src/record/template-expense.ts`（285）· `template-flow.ts`（348）· `template-batch.ts`（286）· `template-installment.ts`（335）· `template-update.ts`（327） | 五张模板盖 16 个场景；每张只写一次块序，导出 `bind<族>Pages(spec)` |
| 域内共用小件 | `src/record/pageParts.ts`（136） | 值加工与取值口径（表单格子／摘要事实／重复探针／信封／回执读数行后两格）；零块位 |
| 场景件（差异声明） | `src/record/scene-{expense,income,photo,reimburse,plain,refund,reimburse-done,lend,borrow,collect,repay,batch,installment,update,undo,restore}.ts` | 16 件各只剩一个 `SCENE` ＋ 差异值（最长的 `borrow` 172 行，其中相当部分是它自己的文案） |
| 门·判据乙 | `scripts/check-scene-shape.mjs`（117）＋ `test/t689-scene-shape.test.mjs` ＋ 负向夹具 `test/fixtures/t689/scene-before-fold.ts` | 场景件里不许有块序（乙-1 import 块位渲染函数／乙-2 HTML 字面量与块位调用／乙-3 装配件调用） |
| 门·判据甲 | `scripts/gen-page-fingerprints.mjs`（180）＋ 账本 `test/t689-页面指纹.json`（32 页）＋ `test/t689-page-fingerprint.test.mjs` | 32 张页的归一化指纹；`--check` 读差异集；重录必须 `--declare-layout-change <票号>` |
| 文档 | `packages/skill-bill/AGENTS.md`（两道门的绿／红／重录读数）· `docs/skills/skill-bill/t689-差异表.md` | — |

## 二、判据读数

| 票面判据 | 读数 | 判 |
|---|---|---|
| 1 `test/t410-lock.test.mjs` 16 条真出口锁全绿（一条不删、一条不放宽） | `tests 18 / pass 18 / fail 0`（13 条录入词 ＋ 3 条修正词 ＋ 2 条降级锁），文件一行未改 | **对上了** |
| 2 16 张页面经双端墙对照，信息等价 | 首刀：32 张页（16 词 × 采集／回执）逐页指纹 **31 张逐字节相同**（唯一变化＝`记报销·采集页` 两处，差异表 §二-4／5），逐页块类对照无新增块、1 张页少 6 个块类（＝#688 裁定 6 那五张「未给」卡 ＋ 网格容器）。**末刀（§十）按 T7 裁定 2 补了两块恒出块 ⇒ 32 张全变（声明重录）**：采集页只多来源脚注、回执页多导航＋脚注，逐条见差异表 §二之三；双端墙已重出 | **对上了**（视觉仍由用户肉眼判） |
| 3 write 域总行数显著下降：每场景 ≤ 20 行差异声明 ＋ 一份域内装配件 | 按 T4 §Solution 本条**降为试算脚手架、不进验收**（实测两口径差 4 倍）。本票实测：场景件 16 件合计 **1,565 LF**（改前 16 件合计 **4,068** 行，降 **61.5%**，逐件中位数 ≈ 98 行——余下的大头是**本场景自己的文案**：复制 prompt 那段逐行键值 text，不是块序）；**块序副本数 16 → 5**（每族一份，住五张模板件，合计 1,581 LF）＋ 域内共用小件 `pageParts.ts` 136 LF | 试算读数，见上 |
| 4 机审六列 0 命中 | `node docs/skills/skill-bill/t407-v8-style-audit.mjs --dir .scratch/t689/audit` → `exit 0`、**「读数：全绿；共 32 份产物，缺 0 份」**（分隔符懒政／英文裸词／区外内部话／重复句四列逐行空） | **对上了** |
| 5 按 T6 逐条 ＋ 按 T7 逐条自证 | 见 §五、§六 | 逐条给 |

### 判据甲（T4 §2.1：改一次版式只动一处）

| 读数 | 实测 |
|---|---|
| 甲-1 改版式的文件数 ＝ 1 | 本票的形状重构只在五张模板件里写块序；场景件里 grep 不到块序（判据乙门绿） |
| 甲-2 未受影响的页指纹不变 | 32 张里 31 张与改前逐字节相同 |
| 甲-3 受影响的页变化集合 ＝ 该版式覆盖的页集合 | **变异实测**：在 `template-flow.ts` 的徽章状态串里插一个「变异探针」→ 重建 → 指纹门变红，**逐页点名的恰好是 `template-flow.ts` 盖住的 6 张采集页**（记退款／报销到账／记借出／记借入／记收回／记偿还），`RESULT: 26/32`；还原 → 重建 → `RESULT: 32/32` 绿 |

## 三、逐页读数（32 张）

- 指纹账本：`packages/skill-bill/test/t689-页面指纹.json`（本次以 `--write --declare-layout-change 689` 重录，声明票号＝689）。
- `--check` 绿：`RESULT: 32/32`、`PASS: 32 张页指纹与账本一致`。
- 与**改前**的逐页对照（`.scratch/t689/baseline/sha256.txt` vs `.scratch/t689/after1/sha256.txt`）：**变了 1 / 32 张**（记报销·采集页）。
- 逐页块类集合对照（`class="ilife-block-*"` 多重集 ＋ `data-slot`）：有差异的页 1、**块类丢失 6、新增 0**。

## 四、自证（红／绿两行）

| 项 | 红 | 绿 |
|---|---|---|
| 判据甲门（指纹账本） | 变异 `template-flow.ts` 状态串 → `RESULT: 26/32`、逐页点名 6 张、exit 1（GATE-RUN `0bcd221a-15c2-4d3b-8bd0-dc385fc1d6cf`） | 还原 → `RESULT: 32/32`、`PASS`、exit 0（GATE-RUN `58679a52-c78c-413c-9ca7-054ba299a7da`） |
| 判据乙门（场景件形状） | 改前的场景件（夹具 `test/fixtures/t689/scene-before-fold.ts`，225 行）→ `RESULT: 0/1`、红条逐条点名到 `文件:行号`；现场塞一行块位 import → 必红；扫描面为空 → 必红（三条都在 `test/t689-scene-shape.test.mjs` 里跑） | 真包无参 → `RESULT: 16/16`、`PASS: 场景件只声明差异值，块序与块位拼装都在模板件里`、exit 0 |

## 五、按 T7 融合基准（`docs/skills/skill-bill/688-融合基准.md`）逐条自证

写入域相关的裁定逐条给「对上了／没对上 ＋ 读数」：

| 裁定 | 判 | 读数 |
|---|---|---|
| 1 页面最大一个字写内容，眉标只写「命令名 · 人话归属」；内部标识符不上屏 | **对上了** | 机审「英文裸词」「区外内部话」两列 **0 命中**（§二判据 4 的读数即本条的可判形式） |
| 2 结果型页三条恒出（页内导航 ＋ 口径说明行 ＋ 来源脚注） | **对上了**（本票最后一刀补的，见 §十） | 页内导航：16 张回执页各含**恰一个** `ilife-block-toc` 容器；来源脚注：32 张页各含一行 `数据来源 · …`（共用位 `shared/sourceLine.ts`）；口径说明行本就有（○「有内容才出」）。手法：导航两个原语提上共用位 `shared/pageSections.ts`（第二个用法） |
| 3 图表必须可读数 | 不适用 | 写入域无图表 |
| 4 缺值一律 `—`，空窗出空态句与引导句，不许渲染 `undefined`／`NaN` | **对上了** | 32 张产物 `grep -c "undefined\|NaN"` 全 0（本票未改这条口径，属回归读数） |
| 5 过程型页：复制区恒双按钮、无同名标题、日志第 4 段写命令原文 | **对上了** | 16 条真出口锁断言 `对账信息／复制数据／复制日志` 三件齐（判据 1 的 18/18） |
| 6 过程型采集页不画「同形空卡」 | **对上了**（本票改的正是这一条） | 记报销采集页：五格全空时不再出「未给」卡网格（差异表 §二-5）；块类对照少 6 个块类；其余 15 张采集页本就无卡 |
| 7 一个形状只许一处定义（页面本地不许自造第七种） | **对上了** | 机审四列 0 命中；本票之后块序副本数 16 → 5，且场景件里 grep 不到块序（判据乙门绿） |
| 8 一命令一页；同域同形共用一份装配件；一张老页 ≠ 一张新页 | **对上了** | 五张模板各盖一族；16 张产物 `data-page`／`data-slot` 仍是各自的值；`记借入` 那处读数行差异**原样保留**（差异表 §二之二，附理由） |
| 9 缺项阻断：缺什么就不给复制 | **对上了** | 16 条真出口锁里的采集面断言（缺项出阻断条、不出可跑指令）仍在绿；4.7 万字节的采集页在缺项时不含复制指令块（阻断条里那条口令不给复制按钮） |
| 11 老侧做法与用户自家原则相撞时按原则 | **对上了** | 回执页退出口走复制区（`copyArea` 的 `undoExit`），产物里**没有**独立页脚按钮组 |
| 12 三条已确权口径（B1 主色／视觉不脚本化／墙的两条坑） | **对上了** | 主色与禁入 token 未动（本票零 CSS 改动）；视觉由用户肉眼判（§七 墙页）；墙生成器照 `docs/agents/视觉验收墙.md` §6.2 的 `dropped` 判据重出 |
| §五 5.2 骨架表（顺序一律照本表） | **对上了** | 末刀补齐第 4 行（页内导航，仅结果型回执页）与第 26 行（来源脚注，全 7 类）两条恒出块，位置照表序（导航在标题之后、读数行之前；脚注在复制区之后）；其余块序本票未动（先保 31/32 逐字节不变，末刀只加不改——差异表 §二之三） |

## 六、按 T6 清单（`t687-parity-审计清单.md`）里属 write 的项逐条

| 项 | T6 当刻读数 | 本票后 | 判 |
|---|---|---|---|
| 键层 1 `bill.record.add`（写·receipt；13 条词） | 可用；合成库 exit 0；HTML 77,247 B／2554 行（域整页） | 仍 exit 0、仍整页（本次 32 页产物 73–82 KB）；专测全绿 | **对上了**（本票未动命令面与四渠道一致：能力声明／SKILL 速查／分派／生成物） |
| 键层 2 `bill.record.update`（写·receipt；3 条词） | 可用；HTML 76,062 B／2554 行 | 仍 exit 0、仍整页 | **对上了** |
| 页面层：老侧写入 5 张模板（`expense_form` 225／`flow_confirm` 271／`batch_confirm` 170／`installment_confirm` 144／`update_confirm` 136） | 6 有落点／1 部分／17 无落点（全图口径，不限本域） | 本票把「写入 5 张模板」的形状**落成了五张模板件**（一族一份块序），页面本身未重做 | **对上了**（形状层）；页面内容 parity 的逐条清单仍在 T6 §4 |
| 本域**不补**的项 | T6 记「不补（本票只审计；写入域的手艺在 #689 那张票里）」 | 本票不新增页面、不改版式 | **对上了** |

## 七、门禁运行对账（GATE-RUN）

| runId | 命令 | exit |
|---|---|---|
| `0c5ebfaf-6fd6-4f6e-b7a6-b8947e6f9e7f` | 改前 32 页真出口铺底（`.scratch/t689/baseline/run32.mjs`） | 0（RESULT 32/32） |
| `1402ca18-b0a2-460e-ad1c-c211d3a3882a` | `tsc -b packages/skill-bill`（五张模板全落地后） | 0 |
| `edda49ab-b4ec-4f37-847f-6016cc31579b` | 判据甲门 `gen-page-fingerprints.mjs --check` | 0（RESULT 32/32） |
| `8afe468c-31ea-4783-b879-0f357b2d0f77` | `node --test packages/skill-bill/test/t410-lock.test.mjs` | 0（pass 18／fail 0） |
| `b0e00c66-91f3-42c1-ae8e-ce20d4685027` | `node --test packages/skill-bill/test/*.test.mjs`（全量 337 条） | 0（pass 337／fail 0） |
| `62d33c54-0d2a-4db6-939e-ee339a3bc1d6` | 判据甲门 `--write --declare-layout-change 689`（重录账本） | 0 |
| `0bcd221a-15c2-4d3b-8bd0-dc385fc1d6cf` | **变异红**：模板状态串插探针后 `--check` | 1（RESULT 26/32，点名 6 张） |
| `ec19ef30-d1ce-4c71-8140-42545851b931` | 还原后 `tsc -b` | 0 |
| `58679a52-c78c-413c-9ca7-054ba299a7da` | 还原后 `--check` | 0（RESULT 32/32） |

（本票运行条目的导出抄录同见本件；锁目录 `.scratch/locks/gate-runs.log` 为原始记录。）

## 八、结构搬迁（#720 转交给本票的那一摊）

口径：搬迁是**等价改写**——判据是「32 张页产物逐字节不变 ＋ 包内全量测试绿」。逐批落、逐批提交。

| 批 | 内容 | 提交 | 读数 |
|---|---|---|---|
| 一 | 纯搬家：`src/record/` → `src/write/`；`shared/` 19 件下沉写入域（blockedSlots／candidatePick／collectFrame／diffTable／duplicateNote／emptyNote／flowSteps／installmentPreview／outsideScan／photoEscape／prefillNote／receiptParts／recentPicks／recordPicker／rowEditorTable／summaryRow／typeBadge／userWording／errorReceipt）；`help/writeWire.ts` → `write/`；`render/helpFile.ts`／`helpPaths.ts` → `help/`；全部 import 说明符按模块解析重算 242 条（源码／测试／脚本三处）＋ 注释路径 65 处；测试里写死的路径（frozen-blocks 场景件落点、t721 的 8 份域声明清单）跟着改 | `a7d734bf` | `tsc` 绿 · 全量 **337/337** · 页指纹 **32/32 与账本一致** · 形状门 16/16 · 告警线 14/14 · `gen-cli --check` PASS（能力 2：query／write） |
| 二 | 两处就地摆正：① `render/html.ts` 删自造五字符转义副本、改引公共层 `base-paint`（`query/detail.ts` 的深引随之消失；`shared/writeParts.ts` 的三字符 `esc` 一字未动）；② `shared/pageShell.ts` 的「域名→眉标」表删掉、改**域自报 `eyebrow`**（`write/pageParts.ts` 出 `EYEBROW`／`writePageShell`，`query/pageParts.ts` 出 `QUERY_EYEBROW`／`queryPageShell`，9 个调用点只换 import 一行） | `e6aaceba` | 同上：`tsc` 绿 · 全量 **337/337** · 页指纹 **32/32** |

| 三 | `policy/` 7 件拆散后**删除**（分类表＋字段校验 → `shared/category.ts`；日期与时间窗口整族 → `shared/dateRange.ts`；写命令 params 校验 → `write/record.ts`；`analysis|goals|accounts` → 各自域；薄转出件消费方改指 `triggers/wakeTable.js`；barrel 删）；`render/views.ts` 拆散后**删除**（`calcKpi`／`isTransfer` → `shared/kpi.ts`；`BillItem`／`toBillItem` → `query/`；`calcCategories`／`buildOverview`／`buildCompare`／`buildTrend` → `analysis/`；`buildRecordReceipt` → `write/`；`buildGoalQuery` → `goal/`；`buildAccountQuery` → `account/`；`buildHelpItems` → `help/`）；新增三扇域门；包门 `src/index.ts` 删 policy 行、`package.json` 的 `exports` 删 `"./policy"`；16 件测试改引新家 | `34f59a18` | 见下「假绿收口」＋同一组读数：`tsc` 绿 · 全量 **337/337** · 页指纹 **32/32** · 告警线 PASS · 形状门 16/16 · `gen-cli --check` PASS |

**假绿收口（本批最要紧的一处）**：`scripts/ratchet-frozen-686.mjs` 的 16 键读数原取自 `dist/policy/index.js`——那件随本批删除，当刻只靠 **`dist/` 里 248 件陈旧产物**（历次搬家留下的旧路径产物，`tsc -b` 增量编译不删）才解析得出「77 键」而报绿。处置两步：① 读数改取 `dist/triggers/wakeTable.js`（域声明合并出的派生面）＋夹具落点同步；② 新增一次性清孤儿脚本（按「源码树 → 应存在产物」逐件判），**删掉 248 件**并 `tsc -b --force` 全量重编，再跑上面那一组读数——仍全绿。**这一步证明第三批的绿不是陈旧产物撑出来的。**

**搬迁面至此齐了**：`record/`→`write/`、`shared/` 19 件下沉、`help/writeWire.ts`→`write/`、`render/helpFile.ts`／`helpPaths.ts`→`help/`、`policy/` 拆散删除、`render/views.ts` 拆散、两处就地摆正、包门收窄——#720「未做项与去向」交给本票的那一摊**逐条落完**。

## 九、未做项与下一手

1. **结构搬迁已齐**（§八三批）：#720「未做项与去向」交给本票的那一摊逐条落完，判据逐批复跑。**遗留一条建议**：清出 248 件陈旧 `dist` 产物说明「增量编译不删已搬走源码的旧产物」是这包反复踩的坑（#715 记过一次），本轮用一次性脚本清了；要不要固化成常跑门（`gen:check` 加一步「产物孤儿」判据）属 #686 机器面的面，建议另开一票。
2. **范围外发现**：`packages/skill-bill/docs/`（T4 两件规格 ＋ 讨论页）当刻仍未入仓（`git status` 里是未跟踪）——本票证据引用它们，报给编排者。
3. **本票**只剩一条**不由本席自判**的：判据 2 的**视觉面**——32 张页按 T7 补了两块恒出块（导航／脚注）与一处读数行统一，**好不好看由维护者肉眼判**（裁定 12：视觉不做脚本化判定）。墙页已重出：`.scratch/t410-wall/` 的 `手机墙-390.html`／`桌面墙-1280.html`／`总索引.html`（32 格、`缺失0 -> 可发`）。

## 收口（2026-09-19）

维护者看过墙后的判定逐字：**「功能都有，样式不好看」**，并指示「本质任务完成即 close，视觉去那张新票推」——本票据此**关闭**：

- 票面判据 1／3／4／5 与判据 2 的「信息等价」那一半：**全绿**（读数见上）。
- 判据 2 的「视觉由用户肉眼判」那一半：**红**，且**已转出**：[写入域页面视觉整改（#728）](https://github.com/FeatherHunter/ilife/issues/728)——对象＝这 32 张产物，改动面＝公共层形状件（`packages/base-render`）＋ 本包 `src/shared/docPage.ts`／`pageShell.ts` 的补丁与页头（**页面本地不许写 CSS**：裁定 7）。本票**不改写**这条判据，只把它的归属写清。
- 关闭**不等于**视觉达标：谁要复看，墙在 `.scratch/t410-wall/`；样式改动归 #728。

## 十、最后一刀：按 T7 裁定 2 补两块恒出 ＋ 统一记借入 ＋ 收两件死件

| 项 | 内容 | 读数 |
|---|---|---|
| 页内导航（§五 5.2 第 4 行，④ 回执页恒出） | 五张模板的回执页在「徽章 → 读数行」之间出导航；锚点 `sec-kpi`／`sec-detail`／`sec-reconcile`／`sec-copy`（分期页多一个分期表锚点、改记录页「结果」块有内容才进） | 每张回执页**恰一个** `ilife-block-toc` 容器（与查询域同形：`<nav class="ilife-block-toc">` ＋ 4–5 条目） |
| 来源脚注（§五 5.2 第 26 行，全 7 类恒出） | 32 张页在复制区之后出 `数据来源 · <来源> · <窗口> · 共 N 条`；取值住新件 `src/write/sourceNote.ts`，版面走共用位 `shared/sourceLine.ts` | 32/32 页各一行（样例：`数据来源 · 记账库（写入） · 2026-09-14 12:00:00 → 20…`） |
| 手法（一个形状一处定义） | 导航两个原语（`sectionOf`／`pageBody`／`pageNav`）从 `src/query/pageParts.ts` **提上共用位** `src/shared/pageSections.ts`（第二个用法）；查询域与写入域共用同一份 | 查询域两张页型仍绿（全量 337/337） |
| 记借入采集页读数行统一 | 改成族内同形的「摘要五格 ＋ 本型三格」；模板里的 `genericCards` 开关**下线**（一场景一份块序的开关＝T4 禁止的动态机制） | 指纹差异集里恰好含 `记借入-采集` |
| 收两件死装配体 | `write/collectBody.ts`／`receiptBody.ts` 折叠后**已无调用方**；`collectBlockedFold` 搬进新件 `write/blockedFold.ts`，过期件头改对 | 全量 337/337 |

**末刀门禁读数**（各附锁运行 runId）：编译 exit 0（`686472be`）· 全量 **337/337** · 真出口锁 **18/18**（`3d8c2d30`）· 机审 `exit 0`／「读数：全绿；共 32 份产物，缺 0 份」· 墙重出 `RESULT 32/32`／`缺失0 -> 可发` · 指纹账本**按声明重录**（`--declare-layout-change 689`，差异集 **32 张**＝两块恒出打在全部页上，与 §五 第 26 行的 ● 相符）。


