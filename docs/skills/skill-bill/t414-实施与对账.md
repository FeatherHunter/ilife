# t414 搜索页 · 实施与对账（结构纪律五步 ＋ 真跑读数 ＋ 变异自证）

> 票 #414「饼干记账查询条件页：5 词 record.search（搜备注／标签／欠款／待报销／分期）」。本件是**实施证据**：
> 事前影响清单与事后对账逐行对得上（structure.md 第五步是唯一机械验收点），读数全部是当刻盘上跑出来的。
> 认领：`gh issue edit 414 --add-assignee=@me` 已落（assignee `FeatherHunter`，复核见 `gh issue view 414 --json assignees`）。
> 研究输入：`.scratch/t414/research-414.md`（只读结论：G1 五词真跑＋G2 旅行双向回归＋G3 三支流转口径三问＋G4 空态复制区）。
> #413 已提交（`04c79e5／375cfc6／9ce2879`，只读其提交确认，不改其文件）。

## 一 必报五步

### 第一步 · 影响清单（动手前，逐行一个能力目录）

**新增／改动（本票声明路径，仅三类）**

| 件 | 一句话 | 为什么 |
|---|---|---|
| `packages/skill-bill/src/query/read.ts` | `viewRecordSearch` 三支改精确口径＋3 个域内小函数（`isDebtRow／isReimburseRow／isInstallmentRow`）；`today/range/detail` 三支一字不动 | 票内核：欠款／待报销／分期走 `#tag` 流转口径（未还／待报销／分期中）；必需小函数仅此三处 |
| `packages/skill-bill/test/t414-record-search.test.mjs` | 本票判据（12 条：搜备注 2／查标签 2／三支 3／红线 4／空结果 1） | 一票一判据；16 笔种子＋整段开标签计数 |
| `docs/skills/skill-bill/t414-实施与对账.md` | 本件（实施证据，含五步＋真跑＋变异＋GATE-RUN） | 证据落归属件目录 |
| `docs/skills/skill-bill/t414-锁记录导出.log` | 锁目录中本票条目导出（§六对账源） | 运行记录在未受控目录，导出件当对账源 |
| `.scratch/t414/*` | 窗口六件（`window-1/-3/-4/-c1/-c2/-c3.ps1` 照抄 t411 模板）＋`mutate.mjs`（照抄 t411 模板改锚点）＋日志 | 整窗交锁＋变异自证，永不入库（`.gitignore:5:.scratch/`） |

**不动（逐条有去处）**

| 件 | 为什么不动 |
|---|---|
| `viewRecordToday／viewRecordRange／viewRecordDetail` 三支 | 票面「不动 today／range／detail」原话；t412（13 条）／t413（17 条）／t411（16 条）复跑全绿即回归 |
| `packages/skill-bill/src/query/commands.ts` | 声明是契约（`bill.record.search` 形状 list 不变）；口径是处理体入参，不进声明 |
| `packages/skill-bill/src/shared/*`、路由（`policy/wakewords.ts`） | 装配与路由沿用；本票无新词、无新形状 |
| `packages/skill-bill/src/fetch/*`、`policy/*`、`render/*` | 口径只引用（`tagMatch／listByTag／searchKeyword` 经 `fetch/index.ts` 公开接口，不重写比对）；`tagMatch` 本体不动 |

### 第二步 · 结构设计（目录树／每件职责／导出面／共用件）

```
packages/skill-bill/src/query/read.ts（改后 311 LF，在线内）
  isDebtRow(r): boolean                       新增，对外不出目录（域内自用）
    （借贷分类或 #未还精确）且排除 #已还精确（收回/偿还后分类仍借贷，换的只是 tag）
  isReimburseRow(r): boolean                  新增，域内自用
    #待报销精确且排除 #已报销精确（到账消 #待报销补 #已报销，残留双标也不算）
  isInstallmentRow(r): boolean                新增，域内自用
    分期分类或 #分期/#分期中精确（票面“分期中”口径；写侧记分期不落 #tag，分类是主口径）
  viewRecordSearch(params, db): ViewOut       改：三支改调上之三判；tag/q 支仅修剪入参（trim 后再查与回填）
```

- 每个文件对外给的东西 ≤5（铁律五）：本票对外仍是四处理体，新增三函数不出 `src/query/`（域内互用，不进 `index.ts` 门）。
- 共用件被哪两个能力用：本票**未新增共用件引用**（`tagMatch` 经 `fetch/index.ts` 公开接口，与 `listByTag／searchKeyword` 同一扇门；铁律一：用接口不抄比对）。
- 依赖方向：`query → fetch` 不变。`tagMatch` 仍是取数层那一份唯一定义（铁律二：精确匹配只此一处，查询侧只调不用第二份）。

### 第三步 · 写代码（改了什么，一行一因）

1. 进口行：增 `tagMatch`（与 `listByTag／searchKeyword` 同门进，不动 `fetch` 本体）。
2. `isDebtRow`：`(借贷/ 或 tagMatch 未还) 且非 tagMatch 已还`。旧 `includes('#未还')` 两处漏：① 已还记录分类仍 `借贷/` 被多算；② 子串 `#未还款` 被误算。新判两处都堵（d2／d4 对照锁定）。
3. `isReimburseRow`：`tagMatch 待报销 且非 tagMatch 已报销`。旧 `includes('#待报销')` 把子串 `#待报销单` 误算（r3 对照锁定）；已报销排除是双标残留兜底（r2 对照）。
4. `isInstallmentRow`：`分期/ 或 tagMatch 分期 或 tagMatch 分期中`。旧 `includes('#分期')` 两处漏：① 票面“分期中”无分支（i3 对照锁定）；② 子串 `#分期付款` 被误算（i4 对照锁定）。写侧记分期不落 `#tag` 故分类分支保留（主口径）。
5. `tag／q` 支：`trim` 后再查与回填（`tag:‹标签›／search:‹词›` 回填稳定，首尾空格不再进 `kind`）。

### 第四步 · 超线报警

本包告警线 **350 行（LF 口径，只数 `\n`，`packages/skill-bill/AGENTS.md`）**：

- `packages/skill-bill/src/query/read.ts` **LF 311**（改前 291，`+28/-8`）：**在线内，不超线**。判据：`readFileSync(f,'utf8').split('\n').length-1`（实测 `LF=311`）。
- 未碰件读数沿用 #411 后复测（`list.ts 182／commands.ts 53`，均在线内）；挂号两件（`wake-assets.ts 986`、`cmd_read.ts 485`）本票未碰、不扩大。
- 测试文件（`test/*.mjs`）、模板、构建产物不进管辖计数（`structure.md` 管辖节）。

### 第五步 · 交付对账（逐行对第一步）

- 第一步「源码」→ `read.ts` 已在 `01d56fb` 落成（`+28/-8`），路径一致。
- 第一步「判据」→ `t414-record-search.test.mjs` 已在 `c119c90` 落成（新建 204 行），路径一致。
- 第一步「本件＋锁导出＋scratch」→ 本件与 `t414-锁记录导出.log` 待 c3 落成；`.scratch/t414/*` 永不入库（ignore），与第一步一致。
- 未碰件（`today/range/detail` 三支／`commands.ts`／`shared`／路由／`fetch／policy／render` 本体）：`git diff c119c90 -- <未碰件>` 干净（多席他票的在途件与本票无关，本票提交一律 `-- <自己路径>` 限定）。**偏差为零**。

## 二 真跑读数（全部经加锁包装器）

### 窗口一（`ade8e872` 靶向绿＋`603a165c` 全量绿；另两次红见第四节）

| 命令 | 退出码 | 摘要行 |
|---|---|---|
| `node node_modules/typescript/bin/tsc -b packages/skill-bill` | 0 | 零诊断（三次窗口一＋窗口三＋窗口四一致） |
| `node --test test/t414-record-search.test.mjs` | 0 | `tests 12 / pass 12 / fail 0`（搜备注 2／查标签 2／三支 3／红线 4／空结果 1） |
| `node --test test/t411-query-skeleton.test.mjs` | 0 | `tests 16 / pass 16 / fail 0`（旧锁不漂移） |
| `node --test`（包内全量，窗口四） | 0 | `tests 236 / pass 236 / fail 0`（224 基线＋12 本票） |

种子（临时库，16 笔，分类一律合法 L1）：搜备注 `t414午饭 #工作餐／t414午饭 加班`；查标签 `#旅行计划 订金／#旅行 门票／旅行计划讨论（无#对照）`；
查欠款 `借小明 #未还（正例）／借小红 #已还（已还对照）／普通午饭（负例）／#未还款（子串对照）`；
查待报销 `出差打车 #待报销（正例）／出差住宿 #已报销（对照）／#待报销单（子串对照）`；
查分期 `分期/手机（分类）／耳机 #分期／手表 #分期中／电脑 #分期付款（子串对照）`。

五词逐条真跑（窗口一内，断言即真跑，加锁真出口 `dist/cli/cmd_read.js` spawn）：搜备注（`q:t414午饭` 2 笔／双 token AND 1 笔，`kind=search:t414午饭`）／
查标签（`tag:旅行` 1 笔仅 `#旅行 门票`／`tag:旅行计划` 1 笔仅 `#旅行计划 订金`，互不误伤，无#不命中）／
查欠款（`kind=debt` 1 笔仅 `#未还`）／查待报销（`kind=reimburse` 1 笔仅 `#待报销`）／查分期（`kind=installment` 3 笔：分类＋`#分期`＋`#分期中`）。
每词 exit 0＋`shape=list`＋H1 唤醒词（`搜备注／查标签／查欠款／查待报销／查分期` 整段开标签）＋KPI 四格（整段开标签计数）＋数据表一张。

红线（exit 2 且 stdout 空）：`{}` 无 q 无 kind／`kind:tag` 缺 tag／`q` 空串与全空格／`tag` 空串，各 exit 2。
空结果（`tag:不存在的标签`）：exit 0＋`total=0`＋`kind=tag:不存在的标签`＋空态句 `没有找到符合条件的记录`＋空态块一枚
（`<section class="ilife-block ilife-block-empty-block">` 整段计数）＋KPI 四格＋零表＋`复制数据／复制日志／bill-cmd-read bill.record.search／data-key="record.search"／data-shape="list"`＋
stdout envelope 级（`<2000B`）＋页内无种子串（`t414午饭／#旅行` 皆无，全量未倒出）。

## 三 变异自证（改坏必红 → 从 HEAD 取回必绿）

工具 `.scratch/t414/mutate.mjs`（照抄 `.scratch/t411/mutate.mjs` 改锚点）：锚点命中数 ≠1 即拒绝落盘；还原一律 `git show HEAD:<件>` 逐字节取回。

| 变异 | 改的是什么 | 变异后 | 还原后 |
|---|---|---|---|
| M1 | 查欠款已还排除撤掉（`&& !tagMatch(#已还)` 删除，已还混入欠款） | 靶向 **12 跑／11 过／1 红**，红在查欠款：实得 total 2（d2 混入），期望 1 | dirty dist 重编后 **12/12 绿**（窗口三末段） |
| M2 | 查分期分期中分支删掉（`或 tagMatch(#分期中)` 删除，票面口径丢失） | 靶向 **12 跑／11 过／1 红**，红在查分期：实得 total 2（i3 掉出），期望 3 | 同上 **12/12 绿** |

```
WINDOW3-SUMMARY pre=0 a1=0 tm1=0 tt1=1 r1=0 a2=0 tm2=0 tt2=1 r2=0 post=0 all=0
WINDOW4-SUMMARY tsc=0 t414=0 all=0（tests 236 / pass 236 / fail 0）
```

- `pre=0／post=0`：变异前后源码与 HEAD 逐字节一致（`DIFF-CLEAN`）。
- `tt1=1／tt2=1` 即变红（非 0＝已变红）；`all=0` 即还原＋重编后复绿。回滚＝源码取回＋重编，两步缺一即假（t413 第四节第 2 条同教训，本窗已把重编织进窗口三末段）。

## 四 走偏与整改（如实记，别当没发生）

1. **空态块标签写错（div vs section）**：初版断言数 `<div …empty-block>` 得 0。整改：对 `base-render` 一查，空态块是 `<section class="ilife-block ilife-block-empty-block">`（t412 同断），改后过。教训：新断言先对公共层实现取一手 mw。
2. **空页字节阈值拍脑袋（20000）**：空页 HTML 实得 67393B（页壳＋KPI＋复制区本就大），阈值错。整改：改断言为 stdout envelope 级（`<2000B`）＋页内无种子串（`t414午饭／#旅行` 皆无），与 t411-P4 同口径（stdout 188B  envelope 级）。
3. **全量 4 红（help-exit-148）昙花一现**：窗口一第三跑全量 236 跑出 232／4 红，全红在 HELP 壳层（本票未碰）。前两跑 HELP 全绿；当时锁日志满屏 `WAIT-OWNER-ALIVE`（他席在途），且暂存区有他席 `base-render` HELP 件。整改：窗口四重编＋全量重跑，**236/236 绿**。结论：他席并发扰动，非本票回归。
4. **暂存区有他席在途件**：c1／c2 窗口 `git diff --cached --name-only` 均带出他席数十件（`skill-calorie／base-render` 等）。本票提交一律 `-- <自己路径>` 限定，c1／c2 各 `1 file changed`，未卷入他席件。
5. **提交信息无 BOM**：三笔提交信息文件一律经 `write` 工具落盘（无 BOM），`git commit -F` 引用；提交前一律 `git diff --cached --name-only` 复核（见各 `window-c*.ps1` 日志）。

## 五 边界与不做（本票范围外，逐条有去处）

- **未知 kind（如 `kind:foo`）**：无 q 即 exit 2（走搜备注缺 q 支，不返全量）；带 q 则按搜备注跑。本票不新增“未知 kind 阻断”，留待后票需要时再锁。
- **搜备注大小写／半全角**：取数层 `searchKeyword` 已 `toLowerCase`，本票不另锁。
- **分期已结清对照（如 `#已分期`）**：写侧无此 tag（`scene-installment.ts` 全件无 `#分期` 字样），本票不设排除；有朝一日写侧落此标再锁。
- **SKILL/HELP 说明面接线**：是 #416；本票 `SKILL.md` 一字不动。
- **样张 HTML**：本票无样张件；五词真跑由 12 条判据逐条落盘（`--html` 产物在临时目录，块级读数见第二节）。

## 六 GATE-RUN 逐行声明（锁记录导出）

锁目录 `.scratch/locks`（`gate-runs.log`），本票 7 次持锁门禁运行已落盘（14 行＝7 START＋7 RUN，全部 `exit=0`；导出件 `t414-锁记录导出.log`；本件提交 c3 落后共 8 次 16 行，提交后以 `Select-String gate-runs.log -Pattern <runId>` 复核）：

- `730d0bdd`（窗口一第 1 跑：编译／t414 靶向／t411 回归／全量）→ `exit=0`，`WINDOW1-SUMMARY tsc=0 t414=1 t411=0 all=1`（t414 空态块标签红，见第四节第 1 条）。
- `479accc8`（窗口一第 2 跑，同上）→ `exit=0`，`WINDOW1-SUMMARY tsc=0 t414=1 t411=0 all=1`（t414 空页阈值红，见第四节第 2 条）。
- `ade8e872`（窗口一第 3 跑，同上）→ `exit=0`，`WINDOW1-SUMMARY tsc=0 t414=0 t411=0 all=1`（靶向 12/12＋回归 16/16 绿；全量 4 红系 HELP 并发扰动，见第四节第 3 条）。
- `671bd831`（提交 c1：源码）→ `01d56fb`。
- `8a63ad46`（提交 c2：测试）→ `c119c90`。
- `b7437904`（窗口三：变异 M1／M2 红→还原→diff 干净→靶向复绿）→ 包装器 `exit=0`，`WINDOW3-SUMMARY pre=0 a1=0 tm1=0 tt1=1 r1=0 a2=0 tm2=0 tt2=1 r2=0 post=0 all=0`。
- `603a165c`（窗口四：重编＋靶向＋全量复绿）→ `exit=0`，`WINDOW4-SUMMARY tsc=0 t414=0 all=0`（tests 236 / pass 236 / fail 0）。
- 本件提交 c3 的两行（START／RUN）在锁日志中，提交后可查。

提交（逐小步持锁，已落两笔）：`01d56fb 功能(414)：viewRecordSearch 三支按#tag精确口径＋已还/已报销排除＋分期中分支`／`c119c90 测试(414)：t414-record-search 五词真跑＋精确回归＋三支口径＋空态`（各 `git diff --cached --name-only` 含他席在途但提交 `-- <自己路径>` 各 `1 file changed`，见各窗口日志）。

## 七 交棒（下一手要什么）

- 读盘入口：`packages/skill-bill/src/query/read.ts`（`isDebtRow／isReimburseRow／isInstallmentRow／viewRecordSearch`，311 LF）；判据 `packages/skill-bill/test/t414-record-search.test.mjs`；变异工具 `.scratch/t414/mutate.mjs`。
- 下一票（#415 详情页）改的是 `viewRecordDetail` 那一支的入参（专属版式），别动 `commands.ts` 形状（形状是契约）与本票三判（`is*Row` 各是唯一判地）。
- 沿革债（非本票引入，不扩大）：`src/policy/record.ts:64-68`「查询域与三个分析命令共用」注释现已半旧（查询侧周月窗已搬出），随分析侧对齐那张后票一起改（t413 第七节同记）。
