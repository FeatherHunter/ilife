# t407 对抗式复核 · 写入域 15 张定制页收口（5930127 ＋ 41d6dbc）

被审：`5930127`（53 件＝22 源码/测试 ＋ 1 证据件 ＋ 30 产物）＋ `41d6dbc`（证据件补提交号）。
口径：只读复核，未改任何被审件；`git show --stat 5930127` 实数 53 件、**21 件源码/测试**（非 22，见 §三.4）。
「对外给的名字」只数个件自己的 `export`，见 `docs/agents/structure.md:57`。

## 一、五条铁律逐条判定

| 铁律 | 判定 | 证据 |
|---|---|---|
| 一 能力自治 | **成立** | `src/shared/` 下 `from '../record/…'` **0 处**（同目录 `./` 引用不算）。新共用件依赖只落 `base-paint`／`base-paint/blocks`／`../fetch/*`／`../policy/*` ＋同目录共用件：`photoEscape.ts:39-47`、`recordPicker.ts:28-37`、`flowSteps.ts:21-23`、`installmentPreview.ts:20`。调用方与第二用法都写得出：`flowSteps` 6 件、`recordPicker` 3 件、`photoEscape` 4 件（`scene-income`／`scene-photo`／`scene-plain`／`scene-reimburse`）、`installmentPreview` 仅 `scene-installment.ts` 1 件（第二个用法是「同族备查」，未指名）。 |
| 二 概念唯一 | **部分成立** | 常量侧干净：四件都引 `../policy/category.js` 的 `DEFAULTS`／L1 名单，无第二份（`photoEscape.ts:42`）。**但同一件事实两处定义**：`photoEscape.ts:55` 的 `PICK_LIMIT` ＋ `:61` `textOf` ＋ `:85` `pickOf`＋`distinct`＋`optionsFor` 与 `collectBody.ts:41`／`:44`／`:61`／`:50`／`:73` **逐行同形同名**（上游只多一层 `categorySideOf`）。`photoEscape` 件头自认这是抄的，理由写的是「`src/shared/` 与 `src/record/` 不互相引」（`photoEscape.ts:51-53`）。`wakewords.ts:19` 的 `carries` 与 `needs` 不是同一事实：`:44-50` 三条修正词只有 `carries`，`:54-71` 查询词只有 `needs`，两格在 `:135` 合成入参；`lookup.ts:7-16` 同法取示例——两处均按「必需／不拦」分格，成立。 |
| 三 改动可预告 | **部分成立** | 证据件 `:246-248` 的影响清单与 `git show --stat` 主体对得上（53 件：`packages/skill-bill/` 21 件源码/测试 ＋ `docs/skills/skill-bill/` 31 件产物/证据，`42d6dbc` 无包外污染）。**偏差 2 处，非 0**：① 清单点名 `SKILL.md`（`:246`），但 `git diff 5930127^ 5930127 -- packages/skill-bill/SKILL.md` **为空**，该件未在本次提交里出现；② 清单点名 `16 个场景件`，实际改动 14 件场景件（`scene-expense.ts` 父子同 blob ＝ 未碰，全包唯一点 `collectBody`／`receiptBody` 的通用件）。类型侧干净：改动件内 **0 处 `any`**；`unknown` 只出现在真收窄的入参位（`k: unknown` 等 78 处，如 `recordPicker.ts:71`／`:216`／`:225`、`photoEscape.ts:55`／`:61`），形状都有窄化，不是糊类型。 |
| 四 名字取自 HELP | **部分成立** | 四个件的对外名都能在施工图指出来：`flowSteps`→第三节「流程三段式」；`installmentPreview`→第三节「分摊预览表」＋第二节记分期那一行；`recordPicker`／`photoEscape`→第三节「候选单选」与「图片识别入口」；`ESCAPE_FIELDS` 逐字出自施工图第四节第 7 条。**两处不成立**：① `src/shared/` 这四个文件名在 HELP 里指不出——「把文件名念给用户听」（`structure.md:48`）只能指到施工图的自造块名（「分摊预览」「三要素」不是 HELP 说法）；② `wakewords.ts:15` `carries` 与 `lookup.ts:7` `carries` 是新自造词，未见出处。件内公开接口名本身可念、不带工种名（无 `utils`／`helpers`／`common`／`base`）。 |
| 五 接口小、里面厚 | **三件成立、一件超线** | 逐件数（只数出本目录）：`flowSteps.ts` **4**、`installmentPreview.ts` **4**、`recordPicker.ts` **5**（`pickModeOf`／`pickerBlock`／`readRowById`／`snapshotTable`／`diffRowsFor` 全部被 `scene-update`／`scene-undo`／`scene-restore` 引用）—— 三件 ≤5 成立；`photoEscape.ts` 盘上 `export` **13** 个（`photoEscape.ts:78-289`）、`src/record/` 下 13 个名字都出现（注释引用也算），**对外面 12 名 > 5 真超线**（证据件 `:235-236` 读数一致）。拆法见 §五。 |

## 二、30 页形状一致性抽检

抽 6 份（记账族 `记收入`／`记一笔`、特殊收支族 `记退款`／`报销到账`、批量与修正族 `批量录入`／`撤销`；另 24 份只取字形读数）：

| 抽检项 | 读数 | 结论 |
|---|---|---|
| 同一套页面积木 | 30/30 份 `<style>` 各 **1 块且 sha 全同**（`89CA32D0E8E4`，36404 字符）；30 份 `data-page=` 共 30 次（每份 1）、`data-shape=` 只在 15 份回执页出现；`ilife-block-page-shell"`＝1 每份；`ilife-block-copy-block"`＋`ilife-block-pre-block"`＋`ilife-action-bar"` 三件齐（采集页 preBlock＝2） | **一致** |
| 自造骨架 | 无：每份只有 1 个页壳容器，无第二套整页结构 | **无** |
| 写死颜色 | 6 份各 42 处 `: #hex`，但样式块 30 份同一份 sha ⇒ 全部出自 base 注入的 `blocksCss`／`buildStyleSheet`；采集页／回执页**页内 0 处新增** | **无** |
| 新造 CSS 变量 | 6 份变量名集合全同，13 个（`--bg` `--blue` `--blue2` `--card` `--fg` `--fg2` `--fg3` `--ilife-charts-dot` `--line` `--ok` `--params` `--shadow` `--soft`）；逐名回查 base 均有出处（`--bg` 见 `packages/base-render/src/spec/style.ts:16`） | **无** |

## 三、必报五步、超线报警与用词

1. **六节**齐：证据件 `## 一`～`## 七`（`:12` `:35` `:76` `:133` `:170` `:201` `:268`），比任务书多出的 `第二节回退登记` 与 `第五节变异自证` 是加料不是缺口。
2. **对账偏差**：**不为 0**。本次对账表只对「任务书列的交付物」（`:255-264`），没对第一步自己的影响清单；第一／第五步互对后偏差 2 处（§一.三）。
3. **超线报警正面报**：成立。行数报警在 `:230-231` 正面点名两件既有超线件（`wake-assets.ts` 971／`cmd_read.ts` 526）；形状报警在 `:233-242` 正面报「`photoEscape` 对外 12 名 > 5」并给拆法。**但漏报** `t407-场景落点清单.md:35` 列的 3 件共用件里 `flowSteps` 正压 5 名线（`structure.md:53` 是「不多于五个」，压线不违规，记为观察项）。
4. **节点口径自数**（`readFileSync(f,'utf8').split('\n').length-1`）：最大件 `src/shared/photoEscape.ts` **308**；21 件逐个与证据件 `:207-228` 的 LF 表比对 **0 处不符**；**超线名额（>350）＝ 0 件**（全部 ≤308）。证据件自称「22 个源码/测试件」与实数 21 差 1（`git show --stat`）。
5. **用词纪律**：拿 `wording.md` 禁用表 grep 21 件改动源码 ＋ 证据件（「键」作命令讲／底座／壳／渲染层／闭环／打通／沉淀／赋能／抓手／颗粒度／落地作实施讲），**剩余命中 0 处**（证据件 `:119` 用「名字」不用「键」，`:51` 是命令名原文不触发）。

## 四、事故影响面独立核验

| 核验 | 我的读数 | 结论 |
|---|---|---|
| ① 残留乱码 | `src/help/lookup.ts`（55 行）、`src/record/scene-income.ts`（198 行）、`src/record/scene-reimburse.ts`（247 行）逐行查 `锟`／`\ufffd`／`鍩`／`湪`／`銆`／`绋`／`鑺` ＝ **0 处**；另「?」连串 0 处 | **无残留乱码** |
| ② 未复原差异 | 三个文件与 `5930127^` 的 `git show` 基准对照：`lookup.ts` 实数只 55 行，改动面与证据件 `:56` 自述的三处（类型签名／注释＋循环／措辞）一致；`scene-income`／`scene-reimburse` 无整段回退痕迹。**但证据件自认记报销件头注释是「按原意重写」（`:288`），本复核无事故前原件可比 ⇒ 注释措辞无法判定逐字复原**，正文与页面同源可判 | **无功能性未复原差异；注释侧无法判定** |
| ③ 4 张基准页 | 4 份在 `5930127^` **都不存在**（本次新增），工作树与提交 blob **逐份 hash 相同**（`git hash-object` ＝ `git rev-parse 5930127:…`）；证据件 `:64-70` 引用的页名与提交号在 git 内**确实存在**。**但读数复算不出**：我用「长度 ≥2 连续中文段取集合」同法复算，`记收入-采集页` distinct=251（证据 199）／`记报销-采集页` 286（221）／`记收入-回执页` 203（168）／`记报销-回执页` 225（180），四份**各多 52／65／35／45，增量不成比例** ⇒ 该跑法未被记录，绝对读数按现描述不可独立复算；**相对读数可复算**：工作树＝提交 ⇒ 重跑同页必得同一集合，两侧差 0 在逻辑上成立 | **逻辑成立、绝对读数不可复算** |

## 五、评分与结论

| 维度 | 分 | 扣分理由（`文件:行号`） |
|---|---|---|
| 能力自治 | **18** | 跨目录引用 0 处、依赖只落 base（`photoEscape.ts:39-47`）；扣 2：`installmentPreview` 写不出**第二个用法**（`installmentPreview.ts` 只被 `scene-installment.ts` 引），按 `structure.md:67`「写不出来的留原目录」，它本该留在场景目录里备查 |
| 概念唯一 | **14** | 扣 6：`photoEscape.ts:55`＋`:61`＋`:85` 与 `collectBody.ts:41`＋`:44`＋`:61` 同一套 `pickOf`／`PICK_LIMIT`／`textOf`／`distinct`／`optionsFor` 两处定义（`photoEscape.ts:51-53` 自认） |
| 改动可预告（含对账） | **16** | 扣 4：影响清单 2 处偏差（`SKILL.md` 未提交、16 件场景件实为 14 件），见证据件 `:246` 与 `git show --stat`；「偏差 0」（`:264`）只覆盖第五步自设的四个交付物，没覆盖第一步清单 |
| 接口粒度与命名 | **16** | 扣 4：`photoEscape.ts:78-289` 对外 12 名 > 5（真超线）；`wakewords.ts:15` `carries` 与 `lookup.ts:7` 为新自造词；四个共用件名在 HELP 里指不出（`structure.md:48` 判据）。**拆法**：① 表单五个（`FieldSlot`／`fieldCardOf`／`commandsOf`／`promptOf`／`factsOf`）并成两个粗入口（字段卡吃候选取值；prompt 与写库指令原文合成一段）；② 外部识别五样（`PhotoScale`／`imageNote`／`escapeCard`／`escapePrompt`／`ESCAPE_FIELDS`）搬出——**不建议搬进 `scene-photo.ts` 自用**，该件已 229 行，宜另立一个只服务拍账单的共用件，重定向 `scene-photo` 的 import。两刀后各 ≤5 |
| 页面一致性与用词合规 | **18** | 扣 2：抽检口径为 30 份字形读数 ＋ 6 份逐项，未逐份比对块清单（`t407-页面块清单-16词.md:26-41` 每词一行）；另 15 份采集页 `details=0`、回执页均 1，与「对账折叠区只在回执侧」一致但清单未写明 |

**总分 82／100**（五维等权）。

**结论：须整改（轻）**，不阻塞 15 张页的可用性——30 份产物在盘上形状一致、事故三件无残留乱码。必须先改：

1. **`photoEscape.ts` 对外 12 名超线**（`photoEscape.ts:78-289`）——按 §五① ② 两刀拆，拆完 `npx tsc -b packages/skill-bill --force` ＋重跑 30 份产物；最小修法＝只做第 ② 刀（识别五样另立件）即可落到 7 名，再并 ① 的字段卡入口收进 5 名以内。
2. **`pickOf` 两处定义**（`photoEscape.ts:85` vs `collectBody.ts:61`）——共用位与能力目录不互引，最小修法＝把取数那一半（`distinct`／`textOf`／`PICK_LIMIT`）提到共用位，两侧各留一行转发；别在 `collectBody.ts` 删而不同步 `scene-expense.ts`。
3. **`SKILL.md` 归属**（证据件 `:246` vs 空 diff）——最小修法＝在证据件第一节把 `SKILL.md` 一行改成「已与 HEAD 一字不差，本票未再改它」，并把「16 个场景件」改成「14 件已改／16 件在盘」。

**会让 15 张页交付不成立的问题（严重）：0 条。** 承载性事实全部独立复核成立：`src/shared/` 跨目录引用 0、30/30 份页样式同一份 sha、21 件 LF 与证据表 0 不符、超线 0 件、事故三件 0 处乱码。

> 本复核未复跑 `tsc`／`node --test`／30 份产物（按任务书只需要核读数可复算）；「测试 90／90」「产物 30/30 exit 0」按被审方自述采信，本条是本件唯一的采信项。
