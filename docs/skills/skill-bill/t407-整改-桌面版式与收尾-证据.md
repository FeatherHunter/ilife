# t407 整改（二）——桌面版式用满宽与卡片换行＋复制区去重＋对比度·证据

> 本窗唯一写者：`packages/skill-bill/src/shared/**`＋`test/**`＋32 产物＋本件。
> 红线：禁 amend/rebase/reset/push/checkout/restore；只 `git add <显式路径>`＋`git commit -- <显式路径>`。
> 产物一律 CLI 真跑 `--html` 直接落盘，不手打整页 HTML。不碰 `base-render`／其它包／仓根／CI。

## 一、体检与基线（动手前）

- `npx tsc -b packages/skill-bill --force` → EXIT 0。
- `node --test "packages/skill-bill/test/*.test.mjs"` → 127/127 过 0 红。
- `node docs/skills/skill-bill/t407-v8-style-audit.mjs --dir docs/skills/skill-bill` → EXIT 0，全绿 32/32 齐。
- 产物基线计数（`probe-count.mjs`）：`复制数据=48 复制日志=48`（16 回执页每页 2/2，16 采集页每页 1/1），`data-t=160`；
  英文 `<td>` 列名 0、`退出囗` 0、`【bill` 0、`记账数据` 32。

### 一-补、本窗复测（2026-09-15 收口窗第一步，未改码，先体检）

- `npx tsc -b packages/skill-bill --force` → EXIT 0（`.scratch-t407-tsc.txt` 空输出）。
- `node --test "packages/skill-bill/test/*.test.mjs"` → 132/132 过 0 红 EXIT 0（即前窗改后态，无回退）。
- `node docs/skills/skill-bill/t407-v8-style-audit.mjs --dir docs/skills/skill-bill` → EXIT 0，
  读数“全绿；共 32 份产物，缺 0 份”；附带一行“本页缺触屏三件：32 份”为机审既有提示位（改前改后同形，非本窗引入）。
- `node .scratch/t407d2/probe-count.mjs docs/skills/skill-bill` → EXIT 0：
  `FILES=32 复制数据=33 复制日志=33 data-t=145`，`英文td=0 退出囗=0 【bill=0 记账数据=32`；
  ODD 16 行全是“回执 数据=1 日志=1”（撤销-回执 数据=2 日志=2 例外），即 D2 去重后态（见七-1）。
- `node .scratch/t407d2/probe-contrast.mjs` → 5.69:1／5.07:1／3.55:1／3.41:1（与四节一致，色值在 base，本包未动）。
- 归因：三读数均为前窗已改 staged 态（`docPage 69／copyArea 150`）的复测，无新改；
  仓内另有别窗在途件（`skill-calorie` 未暂存改动、`AGENTS.md` 已暂存等）与本窗读数无关，一律不碰。

## 二、改动清单（结构对账）

影响清单（事前）→交付（事后），偏差 1 处（见 5）：

- `src/shared/docPage.ts`（LF 69）：D1。`sharedCssText` 末尾追加桌面补丁（`@media (min-width:1200px)` 内
  `.ilife-block-page-shell{max-width:1120px}`＋卡片网格 `repeat(auto-fit,minmax(220px,1fr))`）。
  落在这里的理由：公共层不许动；本页 body 后不加样式块（机审“本页 0”列须保持 0）；32 份走同一代码路径，
  head 样式仍 32/32 同一份。`≤640px` 两列档一行不动。
- `src/shared/copyArea.ts`（LF 150）：D2。`undoExit` 去掉 `dataText/dataActionId`（删 `EXIT_COPY_ACTION` 与
  `commandLine` 引用），退出口只留红标记按钮＋去向说明（说明末追加记录编号，仍是用户说法）＋无 `data-t`；
  撤销指令走复制区那颗「复制数据」（载荷里仍带可重跑命令）。
- `test/desktop-copy.test.mjs`（新增，不计行数）：D1 两条（补丁在位／640 档仍在）＋D2 两条（退出口无复制位
  无 `data-t`／复制区载荷仍在）。
- `test/user-wording.test.mjs`（＋1 条）：W1 补钉 `fieldLabelOf('category')==='分类'` 单独直断言（派单追加）。
- `test/record-write.test.mjs`（改 1 处期望）：旧断言“撤销口令须有可复制位”按 D2 去重令改为三条
  （无 `ilife-exit-undo-copy`／复制数据·日志各仅 1 组）。
- 超线：无（最大改动件 `copyArea.ts` 150＜350）。
- 偏差：`src/record/collectBody.ts`／`receiptBody.ts` 最终未动——D1 纯 CSS 即达判据，D2 落点在共享
  `undoExit`（14 场景＋`receiptBody` 共用，见三）；不动是对账偏差 0 的一部分（少碰即少险）。

## 三、32 产物重出（文件名不变，CLI 真跑）

- 脚本 `.scratch/t407d2/gen32.mjs`（配方写头注释）：全新临时库＋种子 2 行
  （id1 支出·午饭·支付宝/生活，id2 收入·9月工资·空账户/生活）→16 采集（只给 kind/op，缺槽位）
  →16 回执（全参，机审名单顺序）。链：改记录改 id2；撤销 undo id3（首个回执行）；恢复 restore id3。
  参数原文逐份取自现产物日志 `data-t`（逐份同参），`SKILLS_DB_PATH` 指临时库＋`BILL_FORCE_PROD=1`。
- 读数：`ALL32_OK files=32`（32 次 exit 0；采集 `ok:false`×16、回执 `ok:true`×16；67–77KB 落盘 docs）。
- 库态声明（沿复核口径“同库态可复现”）：候选/预填/重复提示行随种子库态属正常差异，非缺陷。

### 三-补、本窗重出（2026-09-15 收口窗，码未动，CLI 真跑整份拷入）

- 同配方 `node .scratch/t407d2/gen32.mjs` 直跑 → `ALL32_OK files=32` EXIT 0
  （采集 `ok:false`×16、回执 `ok:true`×16；本轮 HTML 71–79KB，如记退款-回执 73252B、记分期-采集 78957B）。
- 重出后复核：`probe-count` → `FILES=32 复制数据=33 复制日志=33 data-t=145`，
  `英文td=0 退出囗=0 【bill=0 记账数据=32`（与一-补一致）；
  机审 `--dir docs/skills/skill-bill` → EXIT 0“全绿；共 32 份产物，缺 0 份”。

## 四、视觉复量（1280 桌面×4＋真 390×2，均真机渲染截图，图在 `.scratch/t407d2/shots/`）

- 记退款-回执：改前（`before-jituikuan 135247B`）首排 6 窄卡、日期折 3 行（`2026-09-/14/12:30:00`），
  第二排 2 卡占左 1/3、右 2/3 全空，底部两组＋置灰日志 → 改后（`after-jituikuan 125203B`，read_image 结论）
  首排 4 卡＋第二排 4 卡铺满整行宽，日期 `2026-09-14 12:30:00` 单行，底部只剩「复制数据▼／复制日志」一组，
  明细表项名全中文（记录编号/分类/金额/时间/账户/账本/币种/备注）。
- 记分期-采集：改后（`after-jifenqi 164237B`）首排 4 卡铺满，第二排时间卡独占一行左卡（5 卡 4+1，
  非回执页，不属 D1 第二排判据，见七-3）；底部单组。
- 批量录入-采集：改后（`after-piliang 154437B`）同上 4+1，底部单组。
- 撤销-回执：改后（`after-chexiao 106352B`）4+4 铺满、日期单行；底部仍两组（`scene-undo` 自建退出口，
  本窗无权改，见七-1；第二组置灰日志即 D3 所见的低对比位）。
- 手机真 390（390 宽 iframe 法，不用 `--window-size=390`）：记退款-回执
  `DOCW=390 VW=390 GRIDS=2`（`m-jituikuan 137123B`，read_image：两列卡不断裂、无溢出、无横滚），
  批量录入-采集 `DOCW=390 VW=390 GRIDS=2`（`m-piliang 158098B`，同）。手机端 CSS 未动，无回退。
  （截图内底部 `ERR…documentElement` 一行是 wrapper 自检竞态字样，非产物内容；量数以 `--dump-dom` 读数为准。）

### 四-补、本窗复量（2026-09-15 收口窗，重出后产物，`vision_html_screenshot` 真渲染＋`read_image` 自看）

- 桌面 1280（`width=1280 fullPage`，4 份）：
  ① 记退款-回执：首排 4 卡＋第二排 4 卡铺满整行宽，第二排无 2/3 空；
  日期 `2026-09-14 12:30:00` 单行；底部只剩「复制数据▼／复制日志」一组（退出口红钮是标记非复制组）；
  明细表项名全中文；蓝字白底可读。② 记分期-采集：首排 4 卡铺满，第二排时间卡独占左（5 卡 4+1，
  非回执判据，沿七-3 可接受）；底部单组。③ 批量录入-采集：同 4+1，底部单组，无日期折行。
  ④ 撤销-回执：4+4 铺满、日期 `2026-09-14 12:40:00` 单行；底部仍两组——退出口自建组（七-1 例外，
  `scene-undo.ts` 不在本窗写者范围）＋复制区组；该页残留置灰日志即 D3 低对比位，随七-1 消除。
- 手机真 390（产物直视口 `width=390`，2 份）：记退款-回执两列卡不断裂、无溢出、无横滚（视口 390 即 `doc≈vw`）；
  批量录入-采集同。390 下日期 `2026-09-14／12:30:00` 折两行属窄卡正常换行（卡约 172 宽），非 D1 所治的桌面折三行。
  iframe-wrapper 法在本渲染器下取不到子帧（`file://` 子帧被禁，底部署 `ERR…documentElement`，与前窗注明的竞态同类），
  故本窗以直视口视觉＋代码证据为准：桌面补丁仅 `@media (min-width:1200px)` 内生效，`≤640px` 两列档一行未动
  （`desktop-copy` 测试“640 档断点仍在”绿），手机端无回退。
- D3 对比度（色值全在 base tokens，本包无可控色值，未硬调）：现产物复制按钮 `--blue2 #0a63ce` 压
  `--card #ffffff`＝**5.69:1**；菜单用途提示 `--fg2` 压白＝**5.07:1**；撤销红钮白字压 `#ff3b30`＝**3.55:1**
  （大字钮，AA 大字档过）；改前置灰位 `--fg3` 压 `--soft`＝3.41:1 已随 D2 重复组消除（31/32 页；
  撤销页残留位随七-1 消除）。
- D4：① 明细表英文列名 32 产物 0 命中（`write.ts rowDetail` 早走 `fieldLabelOf`，屏上已是中文，无需改）；
  ② `囗` 源码（`shared/**`＋`record/**`）0 命中、32 产物 `退出囗` 0 命中，无需改。

## 五、判据自跑读数

1. `tsc -b packages/skill-bill --force`：改前 0 → 改后 EXIT 0。
2. `node --test`：127/0 → **132/0**（＋W1 补钉 1＋D1/D2 护栏 4；`record-write` 1 处按 D2 令改期望，未删用例）。
3. 机审 `--dir docs/skills/skill-bill`：重出后仍 EXIT 0，全绿 32/32 齐（设计位 `·` 0、英文 0 份、重复句 0 份）。
4. 复制按钮计数：改前 48/48 → 改后 `复制数据=33 复制日志=33`（31 页单组；撤销-回执 2/2 例外见七-1），
   `data-t` 160→145（去 16 个退出口直拷，菜单三格式＋日志载荷-shape-不动）。
5. 变异自证两条（还原后 `--force` 重编防假绿）：
   - M1 改坏映射（`category:'分类'→'category'`）→ `user-wording` 单跑 fail 2（含“W1 补钉”直断言）必红；
     还原→单跑 12/12 绿。
   - M2 改坏 D2（`undoExit` 加回 `dataText`）→ `desktop-copy`＋`record-write` fail 2 必红；还原→绿。
   - 还原后 `tsc --force` EXIT 0＋全包 132/132＋机审 EXIT 0。
   三条还原自证：① `MUTANT` 残留 grep（src＋test）0 命中（`copyArea` 注释内 `ilife-exit-undo-copy`
   仅史实 Pool，不产按钮）；② `git diff` 仅本票 4 件意向 hunk（`userWording.ts` 经 M1 还原后无 diff，
   即零残留）；③ 上述三绿读数即跑在还原后哈希态上（`docPage` 69／`copyArea` 150 行态）。

## 六、交付与提交

- 源码：`src/shared/docPage.ts`、`src/shared/copyArea.ts`；测试：`test/desktop-copy.test.mjs`（新）、
  `test/user-wording.test.mjs`、`test/record-write.test.mjs`；32 产物（`M` 32/32，文件名不变）；本件。
- 提交（显式路径逐个列，不 `push`）信息：
  `fix(407): 写入域页面根因整改（二）——桌面版式用满宽与卡片换行＋复制区去重＋对比度`。
- 对账偏差：机审 32 齐绿；测试 132 全绿（含 HELP #146 在内套件）；退款等业务口径未动；
  偏差仅二稿列的两处（collectBody/receiptBody 未动＝少改；撤销页待主窗＝未改），余为 0。

## 七、待裁定

1. **撤销-回执仍两组**（本窗唯一未闭环）：`src/record/scene-undo.ts:177-181` 自建退出口
   （`title:'想反悔（把这一笔找回来）'`＋恢复红钮＋`dataText: commandLine(KEY,{op:'restore',id})`＋
   base #336 置灰日志），不在本窗写者范围。提案（主窗一行）：去掉该 `renderCopyBlock` 的
   `dataText/dataActionId`（及 `RESTORE_COPY_ACTION` 常量），红钮与说明保留——注意该页 exit＝恢复向，
   不可直调撤销向的共享 `undoExit`。改后该页计数应为 数据1/日志1（现 2/2/5）。
2. D3 红钮 3.55:1 仅满足 AA 大字档、菜单 hover 底 `--soft` 未实测（菜单默认闭合），是否需要 base 层票据请裁。
3. 采集页 5 卡网格桌面呈 4+1（第二排单卡在左）：D1 判据只点回执页第二排，本窗判为可接受；若要 5 卡亦铺满，
   需把 min 降到 ~200 或拆网格，请裁（手机端不受影响，断点外）。
4. W1 另三条（证据变异落点订正／`0.00` 收窄／`【bill` 名单外说明）不在本窗，已由派单方另行安排。

## 交棒

后窗重出 32：`node .scratch/t407d2/gen32.mjs`（配方见三；产物直落 docs）＋机审＋`--force` 重编。
勿碰 `skill-calorie/base-render` 在途件；`scene-undo.ts` 动前先看七-1。

## 八、本窗（第三次）收口复核（2026-09-15 第三窗，唯一写者）

基线（未改码先体检）：`tsc --force` EXIT 0；`node --test` 131/132（仅 `desktop-copy` D1 条红，
`minmax(220px,1fr) 不见了`，工作区 `docPage.ts:52` 被改成 `minmax(100px,1fr)`＋`MUTANT-M2` 注释残留，
HEAD 本是 220px 正确态）；HEAD 已含 D1/D2/直断言（`copyArea.ts` 150 行去重态、`user-wording` 补钉均在）。

### 八-1、D1（桌面内容列与卡片网格）

- 处置：删掉工作区变异行，恢复 `minmax(220px, 1fr)`（与 HEAD 同，即 `@media (min-width:1200px)` 内
  `.ilife-block-page-shell{max-width:1120px}`＋卡片网格 `repeat(auto-fit,minmax(220px,1fr))`）；
  `git diff -- docPage.ts` 已空，`≤640px` 档一行未动。
- 复测：`tsc --force` EXIT 0；`node --test` **132/132** 0 红。
- 手机约束：补丁仅 1200px 以上生效，`desktop-copy`“640 档仍在”绿；真 390 复量见八-6。
