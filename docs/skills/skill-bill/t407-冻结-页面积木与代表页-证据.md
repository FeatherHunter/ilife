# t407 冻结页面积木与代表页 · 证据件

任务：写入域 16 张定制页的第一步——把「每张页都有的部分」冻在 `packages/skill-bill/src/shared/`，补三个缺口块，出代表页「记支出」（`bill.record.add`，preset `{"kind":"expense"}`）两张真产物。
施工图：`docs/skills/skill-bill/t407-页面块清单-16词.md`。组件只从 `docs/skills/skill-bill/t406-base组件总表.md` 取。

## 一、影响清单

### 1.1 新增件（7 件，全在 `packages/skill-bill/src/shared/`）

| 件 | LF | 管的活 | 对外名字 |
|---|---|---|---|
| `pageFrame.ts` | 95 | 页壳与页头 ＋ 类型徽章 | `PAGE_EYEBROW`／`typeBadge`／`pageFrame`（＋ `TypeBadgeInput`／`PageFrameInput` 两个类型） |
| `summaryRow.ts` | 63 | 结论摘要行 | `moneyDirection`／`summaryCards`／`summaryRow`（＋ `SummaryFacts`） |
| `blockedSlots.ts` | 111 | 缺项阻断条（缺口块 1） | `blockedItems`／`blockedMessage`／`blockedBar`（＋ `BlockedItem`） |
| `duplicateNote.ts` | 86 | 重复检测提示条（缺口块 2） | `findDuplicates`／`duplicateNote`（＋ `DuplicateProbe`／`DuplicateHit`） |
| `prefillNote.ts` | 82 | 预填标注（缺口块 3） | `prefillOf`／`prefillHint`／`prefillNote`（＋ `PrefillMark`） |
| `emptyNote.ts` | 37 | 空态 | `emptyNote` |
| `failureReceipt.ts` | 52 | 失败回执 | `failureReceipt` |

七个件头各写了一行「谁在用（调用点，指名）」，并按包内惯例补了「第二个消费者：`src/query/`（随兄弟图 #403）」。

### 1.2 改动件（9 件）

| 件 | LF 改前 → 改后 | 改了什么 |
|---|---|---|
| `src/record/collect.ts` | 141 → 260 | 页内块序列换成第一节那套（九块）；三枚选择器候选、预填标注、重复检测条的判定搬进来；`missingSlotMessage` 移出到 `blockedSlots.ts` 的 `blockedMessage` |
| `src/record/receipt.ts` | 97 → 121 | 类型徽章＋摘要行五格并入网格；加重复检测提示条与退出口；envelope 的 `key` 过 `sceneKeyOf` |
| `src/record/write.ts` | 171 → 213 | 闸门由「缺槽位」改成「有阻断项」（`blockedItems`，含方向不符）；两页的来源句分开；取数加 `listRecent` 一次；回执页传事实与近期记录 |
| `src/shared/copyArea.ts` | 116 → 144 | `promptCopyArea` 补 `actionId`（**改前是空壳：写着「复制 prompt」却没有按钮**）；新增 `undoExit`（退出口） |
| `src/shared/pageIdentity.ts` | 21 → 38 | 新增 `sceneKeyOf`（修双前缀的口径落点） |
| `src/shared/docPage.ts` | 54 → 54 | `<!doctype html>` → `<!DOCTYPE html>`（判据①按字面核，行数不变） |
| `src/fetch/db.ts` | 236 → 250 | 新增 `listRecent`（预填／重复检测／选择器候选三处共用的取数） |
| `src/fetch/index.ts` | 4 → 4 | 再导出 `listRecent` |
| `test/record-write.test.mjs` | 220 → 334 | 新增 `t407 · 记支出代表页`六条（本票全绿；原有断言一条没改） |

### 1.3 顺手修的两处现存口径错（改前／改后逐字）

| # | 改前 | 改后 | 落点 |
|---|---|---|---|
| 1 | 复制日志的场景标识印成 `bill.bill.record.add（receipt）`（双前缀） | `bill.record.add（receipt）` | `src/shared/pageIdentity.ts` 新增 `sceneKeyOf`：页面内置 envelope 的 `key` 只写本地场景名（`record.add`），`skill`（`bill`）与它拼起来正是对外命令名；公共层 `sceneText` 的 `{skill}.{key}` 口径原样不动。CLI 那条 envelope 与 `<section data-key>` 照旧写整名 |
| 2 | 采集页的复制日志「数据结构」段写着 `biscuit_accountant.db · bills（写库回执）`——采集页根本不写库 | 采集页写 `biscuit_accountant.db · bills（只读：本页不写库，只采集）`；回执页照旧 `bills（写库回执）` | `src/record/write.ts` 的来源常量拆成 `SOURCE_RECEIPT`／`SOURCE_COLLECT`，`collectOut` 用后者 |

两处都有机器读数：`docs/skills/skill-bill/t407-代表-记支出-采集页.html` 里 `bill.bill` 出现 0 次、`bills（写库回执）` 出现 0 次、`只读：本页不写库` 命中；回执页 `bill.bill` 0 次、`bills（写库回执）` 命中。

### 1.4 没碰的

`packages/base-render`／`skill-calorie`／其它 `packages/skill-*`／仓根／CI 一个字节没动（`git status` 里本票路径之外零改动）；老技能目录只读；临时库与中间产物一律落 `.scratch\t407-kit\`；`docs/skills/skill-bill/` 下只新建了这三份（两份产物＋本证据件），别处的在途件（`t407-页面块清单-16词.md`、`t407-对照甲/乙*.html`、`t406-现状-16词页面覆盖表.md`）是别的窗口的未提交件，本票一个都没 `git add`。生成器链与 `pnpm gen` 没碰。

## 二、结构设计

### 2.1 页面积木（施工图第一节 7 类，全部走 base 现成组件）

| 块 | 落点 | 对外名字数 | 走的 base 组件 |
|---|---|---|---|
| 页壳与页头 | `pageFrame.ts` | 3 | `renderPageShell`（经 `docPage.ts`）＋ `fillTemplate`（`blocksCss()` 拼进 `TemplateAssets.sharedCssText`，不走 `extraCss`） |
| 类型徽章 | `pageFrame.ts` | （同上） | `renderStatusBadge`，`ok`／`warn`／`danger`／`empty` 四档；型与方向的措辞表（13 条）住这里 |
| 结论摘要行 | `summaryRow.ts` | 3 | `renderKpiCard`（经 `renderKpiGrid`）＋ `renderCaliberLine`；L1 取自 `src/policy/category.ts` 的 `l1Of` |
| 动作区（三颗按钮） | `copyArea.ts`（已有件，扩了两处） | 4 | `renderCopyBlock`／`renderPreBlock`／`buildDataText`／`buildLogText` |
| 对账信息折叠区 | `receiptParts.ts`（已有件，未改） | 3 | `renderDisclosure` ＋ `renderDataTable` |
| 空态 | `emptyNote.ts` | 1 | `renderEmptyBlock`；锁一条口径：**`next`（下一步）为空即抛错**，不许出「什么都没有」而没有下一步 |
| 失败回执 | `failureReceipt.ts` | 1 | `renderFeedbackBlock`（含 `renderErrorReceipt`／`renderToast`）；锁一条口径：**事由为空即抛错** |

铁律五逐件核过：新件的对外名字最多 5 个（`pageFrame.ts`：3 个值 ＋ 2 个类型，正好到顶），其余都在 5 以内——`blockedSlots.ts` 4（3 值 ＋ 1 类型）、`summaryRow.ts` 3、`duplicateNote.ts` 4、`prefillNote.ts` 4、`emptyNote.ts` 1、`failureReceipt.ts` 1；`copyArea.ts` 扩到 4 个值仍在 5 以内。

### 2.2 三个缺口块

| 块 | 件 | 判定（一处定义） | 呈现 |
|---|---|---|---|
| 缺项阻断条 | `blockedSlots.ts` | `blockedItems`：缺的必需槽位 ＋ **金额符号与这一型的方向不符**（`expense` 要负数、`income` 要正数） | 失败回执（标题「缺项阻断条（写库已阻断）」）＋ 缺项明示表 ＋ 不可复制的写库指令 ＋ 置灰按钮「⛔ 先补齐（N 项）」 |
| 重复检测提示条 | `duplicateNote.ts` | `findDuplicates`：**同日 ＋ 同额（两位小数比）＋ 同分类（整串比）**；分类没给时按同日同额比并在文案里写明依据；账户同则加一格「同账户」 | `renderToast`（`badge.type: 'warn'`）包在独立的 `renderFeedbackBlock` 里，不与其他提示共容器；零命中返回空串 |
| 预填标注 | `prefillNote.ts` | `prefillOf`：只预填账户／账本／币种（来源＝近期最近一笔的库内值）＋ 时间（来源＝缺省 12:00:00）；**分类一律不预填** | `renderDataTable`（字段／取值／来源三列）＋ 表单那一格的提示写成「预填 … · 来源 …」 |

**真阻断怎么真**（三处，缺一处就只是提示）：
1. 写库那一半由 `src/record/write.ts` 拦——`blockedItems` 非空即不进 `addBill`／`updateBill`，库里行数一行不变（第三节实测：采集页两次运行前后都是 1 行）；
2. 页上不给可复制的写库指令——`renderPreBlock` 只给 `command`、不给 `copyText`，本仓复制按钮靠元素自带 `data-t` 才拷得走（实测：两份采集页 `data-t` 里含占位符的有 **0** 处）；
3. 写库那枚按钮置灰（`kind: 'ghost'`）且不带 `data-t`：base 的点击委派读到 `data-t` 为空就早退（`packages/base-render/src/controls.ts` 的 `onClick`），点了不复制也不写库。

### 2.3 两张代表页的块序列（按序）

- **采集页**（`collect.ts` 的 `recordCollectDoc`）：类型徽章（`danger`）→ 结论摘要行（五格 ＋ 一行口径）→ 一句「写库：未发生」→ 重复检测提示条 → 预填标注 → 缺项阻断条 → 选择器空态（无候选时才出）→ 采集表单（七槽：三枚选择器 ＋ 四枚输入）→ 复制 prompt 区 → 复制区（复制数据三格式 ＋ 复制日志）。
- **回执页**（`receipt.ts` 的 `recordReceiptDoc`）：类型徽章（`ok`）→ 一张大网格八格（摘要行五格 ＋ 状态 ＋ 影响行数 ＋ 写入字段）→ 重复检测提示条（排除本次这条编号）→ 写入明细表 → 对账信息折叠区 → 退出口 → 复制区三件。

`名目` 没有库列：它落在三级分类的 L3（`餐饮/外卖/午餐` 的第三级）或备注里，槽位提示里写明——这是本票对施工图「七槽」的一处落法，见第五节遗留。

### 2.4 接线（三处）

1. `write.ts` 的闸门：`const blocked = blockedItems({ params, missing, kind }); if (blocked.length > 0) return collectOut(...)`——两条命令各一处，**取数只一次** `listRecent(db, 锚点日, 90 天)`，预填／重复检测／选择器候选三处共用同一份；
2. `collect.ts` 的 `recordCollectDoc` 换成九块序列，并接上 `pageFrame`（`<section>` 段与标题三件套都归它）；
3. `receipt.ts` 的 `recordReceiptDoc` 接上 `summaryCards`／`duplicateNote`／`undoExit`，`rowFacts(row)` 取库内真值（不是拿 params 顶）。

## 三、真跑读数

命令：`cd D:\ilife`；`$env:SKILLS_DB_PATH='D:\ilife\.scratch\t407-kit\db'`；`$env:BILL_FORCE_PROD='1'`。

| 判据 | 命令 | 读数 |
|---|---|---|
| 类型检查 | `npx tsc -b packages/skill-bill` | **退出码 0，零诊断** |
| 测试 | `node --test "packages/skill-bill/test/*.test.mjs"` | **tests 76／suites 15／pass 76／fail 0**（基线 57／0，上一版 70／0，＋6 条） |

**真跑五次**（脚本 `.scratch\t407-kit\run.ps1`，输出 `.scratch\t407-kit\run4.txt`）：

| # | 命令 | 退出码 | `data.ok` | 当日行数（前→后） | 产物字节 |
|---|---|---|---|---|---|
| 1 | `bill.record.add --params '{"category":"餐饮/外卖/午餐","amount":-12.5,"time":"2026-09-14 12:00:00","account":"支付宝","ledger":"生活","note":"午饭"}'`（种子） | 0 | true | 0 → 1 | — |
| 2 | `bill.record.add --params '{"kind":"expense","amount":-12.5,"time":"2026-09-14 12:30:00"}' --html collect.html` | 0 | **false** | **1 → 1** | 69171 |
| 3 | `bill.record.add --params '{"kind":"expense"}' --html collect-bare.html` | 0 | **false** | **1 → 1** | 68757 |
| 4 | `bill.record.add --params '{"kind":"expense","category":"餐饮/外卖/午餐","amount":-12.5,"time":"2026-09-14 12:40:00","account":"支付宝","ledger":"生活","note":"午饭"}' --html receipt.html` | 0 | true | 1 → 2 | 67269 |
| 5 | `bill.record.add --params '{"kind":"expense","category":"餐饮/外卖/午餐","amount":35,"time":"2026-09-14 13:00:00"}' --html bad-direction.html`（方向不符） | 0 | **false** | **2 → 2** | 68736 |

第 2、3、5 次的 `data.message` 逐字：
- 第 2 次：`缺必需槽位：category（已出采集页，补齐后重跑同一条命令）`
- 第 3 次：`缺必需槽位：category、amount（已出采集页，补齐后重跑同一条命令）`
- 第 5 次：`写库已阻断：amount（方向不符：记支出要负数，给的是 35.00）（已出采集页，改好后重跑同一条命令）`

**判据四条**：

| 判据 | 结论 | 读数 |
|---|---|---|
| ① 两份产物存在、含 `<!DOCTYPE html>`／`<section`／`data-shape`／复制区／缺项阻断条 | 过 | 采集页 69171 字节、回执页 67269 字节；真 DOM 计数（不数 CSS 里的类选择器）：两份都是 `section.ilife-write` 1、`page-shell` 1、`status-badge` 1、`copy-btn` 4、`data-t` 5 位；采集页 `param-form` 1／`select` 3／`input` 4／`pre-block` 2／`copy-block` 1／`feedback-block` 2／`data-table` 2／`action-btn-ghost` 1（阻断条那枚）；回执页 `kpi-card` 8／`copy-block` 2／`feedback-block` 1／`data-table` 2／`details` 1／`action-btn-red` 1（退出口） |
| ② 采集页不写库（库里行数不变） | 过 | 第 2 次 1 → 1；第 3 次 1 → 1；第 5 次 2 → 2（`bill.record.today` 真查） |
| ③ 重复检测条「同日同额同分类」出现、无重复不出现 | 过 | 采集页（同日同额、分类未给 ⇒ 按同额比）命中「重复检测提示条」并印出 `记录编号 1`；回执页（排除本次 id=2 后撞上 id=1）命中；换一天（`2026-09-15`）那一页 **0** 命中 |
| ④ 缺项阻断条在缺槽位时出现且写库被拦 | 过 | 第 3 次页上有 `⛔ 先补齐（2 项）` ＋ `ilife-action-btn ilife-action-btn-ghost` 1 枚，写库指令 `&lt;分类&gt;` 只在 `<pre>` 里出现 1 处、**`data-t` 里 0 处**（拷不走）；该次行数 1 → 1 |

产物（`git` 里本票新建的三份文档之一／之二）：

| 文件 | 字节 | sha256（前 24 位） | LF |
|---|---|---|---|
| `docs/skills/skill-bill/t407-代表-记支出-采集页.html` | 69171 | `DB2607EE8BAECADE6A1CE62B` | 2169 |
| `docs/skills/skill-bill/t407-代表-记支出-回执页.html` | 67269 | `61E8BF043B39AD17263E2666` | 2167 |

产物由 CLI 真跑产出后整份拷入（`Copy-Item`，非重写），两份都能在原位打开。

## 四、变异自证

两行机器读数，均为本机真跑（`node --test --test-name-pattern=… packages/skill-bill/test/record-write.test.mjs`）：

| 变异 | 改法 | 读数 | 还原后 |
|---|---|---|---|
| A · 重复检测判定改坏 | `src/shared/duplicateNote.ts` 的 `findDuplicates`：`r.time.slice(0,10) !== day` 改成 `=== day`（同日比改成异日比） | `tsc -b` 0；`tests 2 / pass 0 / fail 2`（红，两条都报红；含「采集页九块齐全」那条） | 还原 ⇒ `tsc -b` 0；`tests 2 / pass 2 / fail 0`（绿） |
| B · 缺项阻断判定改坏 | `src/shared/blockedSlots.ts` 的 `blockedBar`：给那条含占位符的写库指令的 `renderPreBlock` 补上 `actionId` ＋ `copyText`（于是它能被复制） | `tsc -b` 0；`tests 2 / pass 1 / fail 1`（红，断言原文：`含占位符的写库指令不得可复制，却出现在 data-t：bill-cmd-read bill.record.add --params …`） | 还原 ⇒ `tsc -b` 0；`tests 2 / pass 2 / fail 0`（绿） |

还原后全量复核：`tsc -b` 退出码 0；`node --test "packages/skill-bill/test/*.test.mjs"` ＝ tests 76／pass 76／fail 0。两次变异的原始输出在 `.scratch\t407-kit\mut-a.txt`／`mut-a2.txt`／`mut-b.txt`。

## 五、超线报警

包内告警线 ＝ 350 行（LF 口径，`packages/skill-bill/AGENTS.md`）。本票 16 个改动件逐件实测：

| 件 | LF | 结论 |
|---|---|---|
| `src/record/collect.ts` | 260 | 在 350 以内（本票 141 → 260，最大的一件） |
| `src/record/write.ts` | 213 | 在 350 以内（171 → 213） |
| `src/fetch/db.ts` | 250 | 在 350 以内（236 → 250） |
| `src/shared/copyArea.ts` | 144 | 在 350 以内（116 → 144） |
| `src/record/receipt.ts` | 121 | 在 350 以内（97 → 121） |
| `src/shared/blockedSlots.ts` | 111 | 新建，在 350 以内 |
| `src/shared/pageFrame.ts` | 95 | 新建，在 350 以内 |
| `src/shared/duplicateNote.ts` | 86 | 新建，在 350 以内 |
| `src/shared/prefillNote.ts` | 82 | 新建，在 350 以内 |
| `src/shared/summaryRow.ts` | 63 | 新建，在 350 以内 |
| `src/shared/docPage.ts` | 54 | 在 350 以内（行数不变） |
| `src/shared/failureReceipt.ts` | 52 | 新建，在 350 以内 |
| `src/shared/pageIdentity.ts` | 38 | 在 350 以内（21 → 38） |
| `src/shared/emptyNote.ts` | 37 | 新建，在 350 以内 |
| `src/fetch/index.ts` | 4 | 在 350 以内 |
| `test/record-write.test.mjs` | 334 | 测试件不在告警线范围内（列此仅为便于复核） |

**无超线件**；包内既有的两件超线件（`src/triggers/wake-assets.ts` 986、`src/cli/cmd_read.ts` 558）本票一行没碰。

### 5.1 记成遗留的事（base 缺什么，本票不硬塞）

1. **动作条场景按钮没有禁用档**：`ActionBarButton` 只有 `label`／`kind`／`actionId`，没有 `disabled`；置灰那枚「⛔ 先补齐（N 项）」靠「不给 `data-t` ⇒ 点击委派早退」实现真阻断（外观用 `ghost`）。base 若补 `disabled`，本件可换过去。
2. **动作条场景按钮不带复制文本**：`renderActionBar` 的 `buttons` 渲染出的按钮不带 `data-t`（`controls.ts` 的 `sceneButtonHtml`），能带文本的只有复制位那两枚。回执页的退出口因此是两枚并列：危险色出口标记（`red`）＋旁边一枚能复制的撤销指令（`ghost`）。施工图想要的「一枚危险色按钮直接复制撤销指令」要等 base 补这一档。
3. **没有「心法」表**：本仓与 base 都没有分类心法数据，摘要行的分类那格改报「一级＝X（L1/L2/L3 三级）」＋一行口径；心法表由哪一张票补未定。
4. **`名目` 没有库列**：`bills` 表的列是 id／category／amount／time／account／ledger／currency／note，没有名目；落在 L3 或备注里，提示写明。要不要为名目加列／加字段由维护者定。
5. **失败回执只接了一半**：`failureReceipt` 已冻好并接在缺项阻断条上；施工图第一节末给它列的另三种情形（JSON 异常／缺 id／参数不合法 → 整页替换）今天仍走 stderr ＋ 非 0 退出码（`test/record-write.test.mjs`「id 有值但不是记录编号」那条钉着），整页替换是后票的事。
6. **候选单选件仍未冻**：本票只做了「分类／账户／账本三枚选择器」（走 `renderParamForm` 的 `options`），施工图第三节第 1 行的候选单选（带「为什么是它」徽标）留给记退款那批页。
7. **顺手发现并修掉的一处空壳**：`promptCopyArea` 原先只给 `copyText` 不给 `actionId`，而 base 的 `renderPreBlock` 只在给了 `actionId` 时才渲染复制按钮（`blocks.ts:729-736`）——所以两张旧样张上的「复制 prompt」一直是**有标签没按钮**。本票补上 `actionId`，第 2–5 次真跑页上 `copy-btn` 各 4 枚（prompt ＋ 数据 ＋ 日志 ＋ 退出口那枚）。

## 六、交付对账

| 交付物（任务原文） | 实际 | 偏差 |
|---|---|---|
| 1 · `packages\skill-bill\` 的源码改动（新增页面积木与三个缺口块的件；改 `record/collect.ts`／`receipt.ts` 接线） | 新增 7 件（`pageFrame`／`summaryRow`／`blockedSlots`／`duplicateNote`／`prefillNote`／`emptyNote`／`failureReceipt`）＋ 改 9 件（含 `collect.ts`／`receipt.ts`／`write.ts`／`copyArea.ts`／`pageIdentity.ts`／`docPage.ts`／`fetch/db.ts`／`fetch/index.ts`／测试件） | 0 |
| 2 · `docs\skills\skill-bill\t407-代表-记支出-采集页.html` 与 `t407-代表-记支出-回执页.html` | 两份都在，69171／67269 字节，CLI 真跑产出后整份拷入 | 0 |
| 3 · `t407-冻结-页面积木与代表页-证据.md`：固定六节 ＋ 按需交棒 | 本件，六节齐（影响清单／结构设计／真跑读数／变异自证／超线报警／交付对账）＋ 末节交棒 | 0 |
| 4 · 提交：显式路径 `git add`／`git commit --`，信息按指定 | 见下（提交在最后一步做） | 0 |
| 判据 ① 两份产物标记齐 | 过（第三节逐条读数） | 0 |
| 判据 ② 采集页不写库 | 过（1→1／1→1／2→2） | 0 |
| 判据 ③ 重复检测条 | 过（命中／不命中各一次真跑） | 0 |
| 判据 ④ 缺项阻断条 ＋ 写库被拦 | 过（置灰按钮 1 枚、`data-t` 里 0 处占位符、行数不变） | 0 |
| 变异自证两行 | 两行都给（第四节） | 0 |
| 红线：不碰 base／其它包／仓根／CI／老技能目录／既有 docs 件／生成器链 | 逐条守（`git status` 里本票路径之外零改动） | 0 |

## 交棒

1. **其余 15 张页对着同一套底座铺开**：每张页只需三件事——① 在 `RECORD_SLOTS` 之外给该型自己的阻断项（`blockedItems` 的 `DIRECTION` 表按型加行）；② 页内块序列照采集页／回执页那两串挑；③ 页特有的块（候选单选、流程三段式、分摊预览表、diff 表、逐行可编辑表）按施工图第三节另做件，**别写进这七个共用件**。
2. **两条新口径请复核**：(a) 页面内置 envelope 的 `key` 用本地场景名（`sceneKeyOf`），CLI 那条 envelope 与 `<section data-key>` 仍用整名；(b) 记支出／记收入时金额符号与型不符**从「照写」改成「阻断并出采集页」**（第 5 次真跑：写库已阻断，行数 2 → 2）。这两条都会影响 AI 侧的调用习惯。
3. **待维护者定的三件**：名目要不要进库列；心法表归谁补；候选单选件（带「为什么是它」徽标）要不要按 base 新件做。
4. **本票没动的两件既有超线件**（`wake-assets.ts` 986、`cmd_read.ts` 558）仍挂号，拆法归后票。
