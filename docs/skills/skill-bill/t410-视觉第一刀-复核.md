PASS
# t410视觉第一刀复核（非作者独立重跑，不采信结论）

## 一、结论与口径
判PASS。被审`t410-第一刀-证据.md`（§0基线＋§1§2确证＋§3借贷口径＋§4机审R4）与两笔提交`06350f6`（节三）/`e075fa6`（节四），均在主干祖先链上（HEAD`60475e9`，`merge-base --is-ancestor`均为exit 0）。口径：结论全部自己重跑；不改源码、无源码变异，新探针走仓库外临时目录试完即删；编译／测试／墙／截图／探针／git写一律持锁排队（被583/584/585/289活锁各挡30~120s，活进程一律等）。

## 二、重跑读数与改动页打分
持锁九跑：tsc`9c2bafc6` exit 0；护栏`66f5f09b` 2/2；机审正例`32502179`全绿32/32（R4两类0处）；反例`d9610f86` exit 1缺32；全量`c8c744fa` 280/280（与证据274→280一致）；墙`8668795c` RESULT 32/32；桌面`57a72d1b` 2/2；手机`df7894bb` 2/2（cw=sw=390，页高3152/2866与本票基线一致）；新探针`9b47ad95`见下节。九组START/RUN在`.scratch/locks/gate-runs.log`一对一。§1§2成立：`docPage.ts:70-77`黑块行距1.5＋stretch＋6px在位；`outsideScan.ts:73-92`三列在位，`参数名`墙与t407拍账单采集页均0命中、`口令原文`仍在。§3：全墙旧串0，新句仅n15/n17同行1835各1。亲看：新句单行上屏、借出借入对称、无溢出，黑底块与版心1120无变化。对照（同量表）：桌面92→92、92→92；手机记借出78→78、记借入70→70（口令原文整条与可见参数名残留未动，一字之改不加减分）。

## 三、自设新探针（未覆盖页一页试完还原）
P1取作者未探的n23记分期采集，注入`bill.record.update`＋`user_id`（作者用记借出＋bill-cmd-read／source_id）：32份全拷仓库外临时目录、单页注入、机审应红。内层机审exit 1且点名该页命令名1处／库列名1处（兼英文裸词1份），`PROBE-RED 1/1 CLEAN 1`，临时目录已删。另抽查同页墙产物：旧串0、新借贷句0，78499B，doctype与section齐，证未覆盖页未被波及。源码零碰，无残留（探针脚本在仓库外TEMP，仓内零新增除本件）。

## 四、路径与未做
被审改动：`record/scene-lend.ts:176`、`record/scene-borrow.ts:197`、`test/t410-lend-borrow-caliber.test.mjs`、`t407-v8-style-audit.mjs`（332行内）、`t410-第一刀-证据.md`。本件：`docs/skills/skill-bill/t410-视觉第一刀-复核.md`。日志：`gate-runs.log`九组、`t410-wall/run.log`（32/32）、`.scratch/t410/shots/`四图（基线目录未动）。未做：他包全量未跑；作者历史GATE-RUN未逐条重跑（以关键链为准）；无真机。下一手：三采集大跌页架构／符号串形状化／版心1120不在本刀，留后票；手机口令原文与可见参数名残留另开票。
