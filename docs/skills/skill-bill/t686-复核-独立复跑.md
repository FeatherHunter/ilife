# #686 独立复核席 · 对抗式复跑（生成物／棘轮／行数门）

**判定：PASS。** 票面四条判据逐条由复核席自跑，读数与实施席证据件一致；另加三条本席自造探针（仓根生成链上的
生成物漂移、行数门的真·超线件与台账陈化、生成物剔除判据的两条反例），全部拿到预期红／绿。

日期：2026-09-19。票：[#686](https://github.com/FeatherHunter/ilife/issues/686)。复核席写路径只有本件与
`.scratch/t686/review-*.log`；一切编译／测试／git 写操作经
`node tooling/run-locked.mjs --ticket 686 --run-id rev686-0N -- <命令>`，改坏与还原都在**同一次持锁窗口内**完成，
逐件核 sha256。持锁条目原文见 `.scratch/locks/gate-runs.log`（`RUN ticket=686 runId=rev686-01…10 exit=0`）。

## 一 · 复核基线与并发说明

- 探前（`rev686` 会话开始时）`packages/skill-bill` 工作区**干净**，逐件 sha256 记在
  `.scratch/t686/review-baseline-sha256.log`。
- 复核期间他席并发提交，HEAD 从 `fa4e122b` 移到 `8a1348d3`；本票相关件的 sha256 **全程未变**
  （收尾对账见 §四）。
- 探针驱动件本身也落在我声明的 `.scratch/t686/review-0N-driver.log`（Node 可按任意扩展名执行，
  驱动内 `require`）；输出件 `review-0N-*.log`。本件只引摘要行，全文在日志。

## 二 · 逐条机器读数

### 复跑 1 · `pnpm gen:check`（仓根，持锁）— runId `rev686-01`，exit 0

日志 `.scratch/t686/review-01-gen-check.log`。bill 段与 wake-assets 段原文：

```
GEN-CHECK ok packages\skill-bill\src\cli\registry.ts sha256=113648fa…0525
GEN-CHECK PASS：命令 6 条；能力 2 个（query／record）
老实物：71 场景／7 域／20 二级组／70 唯一唤醒词
OK：D:\ilife\packages\skill-bill\src\triggers\wake-assets.ts 与生成结果字节一致
```

仓外事实源**在盘**，wake-assets 那一支是**真比对**（不是 SKIP 放行）。同一命令里卡路里段（属别席，不计本票账）
原文 `GEN-CHECK PASS：键 134（写 52 ＋ 读 82）；能力 10 个（analysis／body／diet／exercise／goal／home／photo／profile／weight／workout）声明 134 条（一条命令一个定义地）`
——未被我方改动，口径未见放宽。

### 复跑 2 · 包内全量测试（持锁）— `rev686-02`，exit 0

`node --test packages/skill-bill/test/*.test.mjs`（日志 `.scratch/t686/review-02-pkg-tests.log`）：
`tests 311 / suites 72 / pass 311 / fail 0 / duration_ms 6996.3365`，无 `not ok`。与实施席报的 311/311 相符。

### 复跑 3 · 判据 2 独立复跑（我自己的变异，不用实施席探针）— `rev686-03`，exit 0（驱动自身）

我手改 `packages/skill-bill/src/cli/registry.ts` **一个字符**（`命令索引` → `命令索引X`）后
`node packages/skill-bill/scripts/gen-cli.mjs --check`：

```
GEN-CHECK FAIL：命令 6 条；能力 2 个（query／record）
  packages\skill-bill\src\cli\registry.ts 与生成器输出不一致（第 3 行起）：
  盘上： * 命令索X引（一能力一行）…
  生成： * 命令索引（一能力一行）…
```

exit 1，且**指出了第一处差异行**。照生成器 `node packages/skill-bill/scripts/gen-cli.mjs` 重生成后
sha256 逐字节回位（改前 `113648fa…0525` = 还原后 `113648fa…0525`），`--check` 回 `GEN-CHECK PASS`，
`git diff --stat -- packages/skill-bill/src/cli/registry.ts` 输出为空。日志 `.scratch/t686/review-03-gen-check-drift.log`。

**本席新探针（实施席脚本没跑的那一条整根链）** `rev686-09`（`.scratch/t686/review-09-root-chain.log`）：
手改第二件生成物 `src/triggers/wake-assets.ts`（标题行加一个字符）后跑**仓根** `pnpm gen:check` ⇒ exit 1，
原文 `DRIFT：D:\ilife\packages\skill-bill\src\triggers\wake-assets.ts 与生成结果不一致（禁止手改；重跑不带 --check 即覆盖）`；
`node packages/skill-bill/scripts/gen-wake-assets.mjs` 重生成 ⇒ 逐字节回位（`7bdb6110…3089` 前后相等）⇒ 仓根链回绿。
同一驱动还测了 SKIP 路径：事实源指向不存在的路径时 `WAKE-ASSETS SKIP：事实源不在本机（…）…` 且 **exit 0**（见 §五 缺陷 1）。

### 复跑 4 · 判据 3 收紧守卫（我自改松一格）— `rev686-04`，exit 0

`node packages/skill-bill/scripts/check-ratchet-tight.mjs`（`.scratch/t686/review-04-tight-guard.log`）：

```
MEASURE dispatchKeys=10 legacyKeys=10 registryKeys=6 wakeKeys=16；行数 src/cli/cmd_read.ts=501 src/render/envelope.ts=64
RESULT: 9/9
PASS: 棘轮与实况逐项恒等（实况 ≤ 冻结值 且 冻结值 ≤ 实况）
```

把 `scripts/ratchet-frozen-686.mjs` 的 `FROZEN.registryKeyCount` 由 6 改成 **7**（冻结值比实况松）后，守卫当场红并点名：

```
TIGHT FAIL registryKeyCount（生成物注册表键数）：冻结=7 实况=6（实况已降 1 而冻结值没下调：棘轮的牙齿松了）
RESULT: 8/9
```

改回后回 9/9（`f7707232…6634` 逐字节回位）。另在 `rev686-10` 单独跑了判据鉴别力自证：
`SELFTEST 5/5`（恒等放行；冻结值留多余键／分派层多出／行数上涨／注册表少一条，四种松法各自被点名）。
判据 3 的「搬一条必须同时删键、下调上限」由三个恒等断言同时扛：`dispatchKeys`／`legacyKeys` 集合相等 ＋
`registryKeyCount`／`totalKeyCount` 计数相等 ＋ `lineCaps` 行数相等——搬迁会同时踩三处，任一处漏下调即红。

### 复跑 5 · 判据 4 独立探针（本席新造，实施席的测试件用的是夹具）— `rev686-05`，exit 0

日志 `.scratch/t686/review-05-warning-line.log`：

- **真·超线件**：新建 `packages/skill-bill/src/probe-over.ts`（360 个 LF，未入台账）⇒ 门 exit 1、
  `RESULT: 12/13`、`RED 漏报（台账没有）：src/probe-over.ts LF=360；修法：node packages/skill-bill/scripts/check-warning-line.mjs --sync`。
  删掉该件 ⇒ exit 0、`RESULT: 12/12`、`PASS: 告警线台账齐全且与实况一致`。
- **边界（已在台账的件改一行）**：给 `src/health.ts` 末尾加一行注释（486 → 487）⇒ exit 1、`RESULT: 11/12`、
  `RED 台账陈化：src/health.ts 台账=486 实况=487；修法：…`。还原后逐字节回位（`2d6a9a49…1816` 前后相等）⇒ 12/12。
- 末了 `git status --porcelain -uall -- packages/skill-bill` 输出为空：两段探针都没留痕。

### 复跑 6 · 生成物剔除不靠人手名单 ＋ 两条反例 — `rev686-06`，exit 0

日志 `.scratch/t686/review-06-generated-skip.log`。剔除判据读的是**生成器自己的输出声明**
（`scripts/gen-*.mjs` 里名字带 `OUT`／`TARGET(S)`／`DST`／`DEST`／`GEN…` 段的 `const <名> = join(SRC_DIR, …)`
与 `targets` 里的 `path: join(SRC_DIR, …)`）。我另用 grep 复核：`check-warning-line.mjs` 里
`wake-assets`／`registry` 只出现在注释与那句 `REQUIRED`（`src/cli/cmd_read.ts`，那是台账冻结行，不是剔除名单），
**没有第二份手写的剔除清单**。真包门读数：

```
GENERATED-SKIP src/cli/registry.ts LF=37（由 scripts/gen-cli.mjs 生成，不在扫描面内）
GENERATED-SKIP src/triggers/wake-assets.ts LF=986（由 scripts/gen-wake-assets.mjs 生成，不在扫描面内）
OVER scripts/check-warning-line.mjs LF=363 …／OVER src/cli/cmd_read.ts LF=501 …／OVER src/health.ts LF=486 …
RESULT: 12/12
```

**反例一（判据认不出的输出声明）**：把 `gen-wake-assets.mjs` 的声明改成
`const OUT = join(HERE, '..', 'src', 'triggers', 'wake-assets.ts');` ⇒ 该件**掉回扫描面**：
`OVER src/triggers/wake-assets.ts LF=986` ＋ `RED 漏报（台账没有）：src/triggers/wake-assets.ts LF=986`，exit 1、`RESULT: 12/13`。
**反例二（两台生成器都改）**：`declaredCount=0` ⇒ `RESULT: 11/14`，同时打出

```
RED 生成物判据自证：本包有生成器（scripts/gen-cli.mjs／scripts/gen-wake-assets.mjs）却一条输出声明都抽不到——判据会静默失效…
RED 生成物印记已认领：src/cli/registry.ts 件头带「勿手改」印记，却不在生成器声明的输出清单里…
RED 漏报（台账没有）：src/triggers/wake-assets.ts LF=986
```

两条反例改回后，两件生成器 sha256 逐字节回位（`6c791e0f…cd5b`／`4b4a23a0…b3d`），门回 `12/12`、exit 0。

### 复跑 7 · 判据 1（加一条命令的 `git status`）— `rev686-07`，exit 0

跑实施席的 `docs/skills/skill-bill/t686-加命令探针.mjs`（`.scratch/t686/review-07-add-command.log`），
并**自己在探前／探后核 sha256 与 `git status`**（不采信它自报）：

- A 往已有能力（`query`）加一条命令：新增项 `["packages/skill-bill/src/query/commands.ts"]`——只有声明件；
  生成物 `src/cli/registry.ts` 该次 sha256 不变（`113648fa…0525`）。
- B 新开一个能力目录（`src/probe/`）：新增项恰为 `src/probe/commands.ts`／`src/probe/index.ts`（两件手写声明）
  ＋ `src/cli/registry.ts`（生成物）。
- 探针 `RESULT: 4/4`；我自己比对：四件（`query/commands.ts`／`registry.ts`／`cmd_read.ts`／`wake-assets.ts`）
  sha256 逐字节回位，`src/probe`／`dist/probe` 均已删净，`git status --porcelain -uall -- packages/skill-bill` 为空。

### 复跑 8 · 台账对账（本席自己解析、自己数 LF，不调门的代码）— `rev686-08`

日志 `.scratch/t686/review-08-ledger-crosscheck.log`：

```
台账行数=3
ROW src/cli/cmd_read.ts 挂号值=558 台账实测=501 复核席实测=501 相符=true
ROW src/health.ts 挂号值=— 台账实测=486 复核席实测=486 相符=true
ROW scripts/check-warning-line.mjs 挂号值=— 台账实测=363 复核席实测=363 相符=true
扫描面件数=92；>350 四件：wake-assets.ts 986（生成物，剔除）／cmd_read.ts 501／health.ts 486／check-warning-line.mjs 363
扫描面最大五行：986 / 501 / 486 / 363 / src/record/scene-borrow.ts=319
```

（我这份数是**原始面** 92 件；门的同一口径读数写作「扫描面 90 件 … 剔出生成物 2 件」，90 ＋ 2 ＝ 92，两处不矛盾。）
三行逐行相符；原始面除生成物外**没有第四个超线件**；扫描面内离告警线最近的非挂号件是 `src/record/scene-borrow.ts` 319。
表外（`AGENTS.md` 正文与 `docs/skills/skill-bill/*.md`）逐条扫「N 行」字样：`AGENTS.md` 里只有告警线数字 350
与台账三行自身，**没有第二张会陈化的逐件行数表**（旧的「本包现状（2026-09-15）」「#411 查询骨架后复测」两张手抄表已删）。

### 复跑 9 · 收口读数 — `rev686-10`

`.scratch/t686/review-10-final.log`：守卫 `SELFTEST 5/5`；无参门禁
`AGENTS.md：…（台账 3 行；扫描面 90 件，超线 3 件，剔出生成物 2 件；挂号台账命中 1/1）` ＋ `RESULT: 12/12` ＋ `PASS`；
`--sync --dry` ⇒ `SYNC-PLAN mode=dry 行=3 改=0 增=0 删=0` ＋ `SYNC-DRY ok（台账已与实况一致，未落盘）`、exit 0
（另一条独立代码路径同样证明台账与实况一致）。

## 三 · 我动过并已还原的路径（sha256 改前／还原后）

| 件 | 改前 sha256 | 还原后 sha256 | 探针 |
|---|---|---|---|
| `packages/skill-bill/src/cli/registry.ts` | `113648fa…0525` | `113648fa…0525` | 一个字符（`rev686-03`） |
| `packages/skill-bill/scripts/ratchet-frozen-686.mjs` | `f7707232…6634` | `f7707232…6634` | `registryKeyCount` 6→7（`rev686-04`） |
| `packages/skill-bill/src/health.ts` | `2d6a9a49…1816` | `2d6a9a49…1816` | 末尾加一行（`rev686-05`） |
| `packages/skill-bill/scripts/gen-wake-assets.mjs` | `6c791e0f…cd5b` | `6c791e0f…cd5b` | 输出声明改认不出的写法（`rev686-06`） |
| `packages/skill-bill/scripts/gen-cli.mjs` | `4b4a23a0…b3d` | `4b4a23a0…b3d` | `targets` 改认不出的写法（`rev686-06`） |
| `packages/skill-bill/src/triggers/wake-assets.ts` | `7bdb6110…3089` | `7bdb6110…3089` | 标题行加一个字符（`rev686-09`） |
| `packages/skill-bill/src/query/commands.ts` | `3b0adbca…2011` | `3b0adbca…2011` | 由加命令探针临时改，本席探前／探后自核（`rev686-07`） |
| `packages/skill-bill/src/probe-over.ts` | （新建，360 LF） | **已删除**（`exists=False`） | `rev686-05` |
| `packages/skill-bill/src/probe/`、`dist/probe/` | （加命令探针 B 段临时新建） | **已删净** | `rev686-07`（探针自删，本席复核） |

收尾（HEAD `8a1348d3`）：上表除已删件外**逐件与探前基线相同**，
`git status --porcelain -uall -- packages/skill-bill` 输出为空；`packages/skill-bill/AGENTS.md`（`ba29beb3…1c16`）
与实施席证据件记的 `BA29BEB3…C16` 一致。本席**未改实施席任何件**，也未提交任何件。

## 四 · 缺陷清单

- **本票引入**：无。
- **本票范围（低危）· 判据 2 的机器前提没写进本包台账**：`gen-wake-assets.mjs --check` 在仓外事实源缺席时
  打 `WAKE-ASSETS SKIP` 并 **exit 0**（`rev686-09` A 段原文；明打，不静默）。本机事实源在盘，故判据 2 在这里成立；
  但「无事实源的机器上手改 `wake-assets.ts` 不会被 `gen:check` 抓住」这一前提只写在生成器头注释与生成物头注释里，
  `packages/skill-bill/AGENTS.md` 未记（该件它只记了「生成物自动剔除」）。建议把这一句补进台账「文件行数告警线」一节；
  是否补由票主定，不影响本票判定。
- **本票范围（低危）· 票面引文与输出原文字面不同**：票面写「冻结值比实况松」那一句，守卫实际的原文是
  `（实况已降 1 而冻结值没下调：棘轮的牙齿松了）`（`rev686-04`）。语义等价，本席按**实际原文**判定为红且点名到位；
  该字面「冻结值比实况松」只出现在 `test/t686-收紧守卫.test.mjs` 的断言消息里。
- **范围外发现**：`test/record-write.test.mjs:87-91` 仍手写「6 条 ＋ 六个键」的对账面（搬迁时会陈化）——实施席在
  `t686-机器面-证据.md` §五.2 已自己记账转给搬迁票，本席未复跑该条（不在本票写集）。
- **范围外发现（极低危）**：台账「结论」列里嵌了跨包活读数（兄弟包 `check-warning-line.mjs`「356 行」）与历史读数
  （986）。它们不在机器核对列上，只会在人读时陈化；本包表外确无逐件行数表（§二 复跑 8 实测）。

## 五 · 未做项

1. 未跑仓根全量 `pnpm test`（跨包；票面只要求包内 `packages/skill-bill/test/*.test.mjs`，已 311/311）。
2. 未跑 `pnpm build`（会重写 `dist/` 与内容印记，属别席窗口）；判据 2／3 用到的 `dist/` 是当刻盘上产物，
   门与守卫都会自证新鲜度（`GEN-STALE`／`GEN-PAIR`／缺 `dist` 即红）。
3. 未复跑别席的卡路里那一支判据（只读它在 `gen:check` 里的绿行，不计本票账）。
4. 未把本件提交入仓（复核席不提交、不 push；提交与否由编排者收口）。

## 六 · 复核补记（新修订 `ecf19f66`）

**判定：PASS。** 新修订的四条复跑全过；本席另加三条自造探针，专打这次新扩的 `src/cli` 全目录面、
「旧口径会失明」这句话、以及「吞错改抛」那句声明——三条都拿到预期红／绿。

修订范围（`ecf19f66`，4 件）：`scripts/ratchet-frozen-686.mjs`（分派层量法由「只盯 `src/cli/cmd_read.ts`」扩成
`src/cli` 全目录，新增导出 `cliDispatchKeysOf(root)`；`try/catch` 静默吞错改为直接抛；`totalKeyCount` 上涨提示改写）、
`test/cmd-registry-686.test.mjs`（新增夹具自证一例 ＋ 改走新入口）、`test/legacy-ratchet-686.test.mjs`（改走新入口）、
`AGENTS.md`（＋1 行：生成器链与它的机器前提）。新基线 sha256 记在 `.scratch/t686/review-baseline2-sha256.log`。

### 复跑读数（持锁）

| runId | 命令 | 退出码 | 摘要行原文 |
|---|---|---|---|
| `rev686-11` | 收紧守卫真包 | 0 | `MEASURE dispatchKeys=10 legacyKeys=10 registryKeys=6 wakeKeys=16；行数 src/cli/cmd_read.ts=501 src/render/envelope.ts=64`／`RESULT: 9/9`／`PASS: 棘轮与实况逐项恒等（实况 ≤ 冻结值 且 冻结值 ≤ 实况）` |
| `rev686-11` | 守卫 `--selftest` | 0 | `SELFTEST 5/5` |
| `rev686-11` | 行数门（无参） | 0 | `RESULT: 12/12`／`PASS: 告警线台账齐全且与实况一致`（`AGENTS.md` 正文多一行，台账三行未动；两条 `GENERATED-SKIP`、三条 `OVER` 原样） |
| `rev686-11` | 棘轮三件测试 | 0 | `tests 22 / suites 0 / pass 22 / fail 0` |
| `rev686-13` | 包内全量测试 | 0 | `tests 312 / pass 312 / fail 0`（与提交信息自报的 312/312 相符） |

日志：`.scratch/t686/review-11-revised-checks.log`、`.scratch/t686/review-13-old-vs-new.log`。

### 新探针 A · 姊妹件里的按键分派（打新扩的面）— `rev686-12`

临时新建 `packages/skill-bill/src/cli/probe-dispatch.ts`
（内容 `export function f(key: string) { return key === 'bill.zz.probe'; }`），守卫当场红，两条判据同时点名：

```
MEASURE dispatchKeys=11 legacyKeys=10 registryKeys=6 wakeKeys=16；行数 src/cli/cmd_read.ts=501 src/render/envelope.ts=64
TIGHT FAIL dispatchKeys（分派层按键分派字面量集）：冻结=10 实况=11；实况多出 1 条（bill.zz.probe）——棘轮只许随搬迁变短…
TIGHT FAIL dispatchEqualsLegacy（未搬迁的键恰住两处：分派层 case ＋ 过渡表形状行）：两处不同集（分派层 11／过渡表 10）…
RESULT: 7/9
```

删掉探针件 ⇒ `dispatchKeys=10`／`RESULT: 9/9`／`PASS`；`src/cli` 目录回到
`["cmd_read.ts","config.ts","health.ts","registry.ts"]`，`git status --short -- packages/skill-bill/src/cli` 为空（无痕）。

### 新探针 B · 把「旧口径会失明」变成机器读数 — `rev686-13`

同一个姊妹件在场时，同一份扫描函数的两条入口对照：

```
[A] 旧口径（只读 cmd_read.ts）条数=10 含 bill.zz.probe=false
[A] 姊妹件在场时：旧口径仍 10 条、含 bill.zz.probe=false
[A] 姊妹件在场时：新口径 11 条、含 bill.zz.probe=true
[A] 删掉后：新口径 10 条、含 bill.zz.probe=false；探针件 exists=false
```

⇒ 「把 `case` 挪进同目录姊妹件就绕开判据」这个洞**确实存在过**，也确实被这次修订堵上（不是纸面声明）。

### 新探针 C · 打「吞错改抛」那句声明 — `rev686-14`

```
[①] 不存在的根：抛=true 错误码=ENOENT 消息=ENOENT: no such file or directory, scandir '…\review-no-such-root\src'
[②] 无 src/cli 的夹具根 exit=1 ／ RESULT: 0/1 ／ FAIL: 量不到实况（ENOENT: … scandir '…\t686-nocli-…\src\cli'）
```

⇒ 读不到就抛，不再把「量不到」静默当「实况 0 条」；守卫在上游把它转成 `量不到实况` 的红条 ＋ `RESULT: 0/1`，不静默当绿。

### 冻结值松一格复跑（在**新修订件**上）— `rev686-12` B 段

改前 sha256 `0a1cf46f…617a`；`FROZEN.registryKeyCount` 6→7 ⇒ exit 1：

```
TIGHT FAIL registryKeyCount（生成物注册表键数）：冻结=7 实况=6（实况已降 1 而冻结值没下调：棘轮的牙齿松了）
RESULT: 8/9
```

还原后 sha256 `0a1cf46f…617a`（**逐字节回位**）⇒ 回 `RESULT: 9/9`／`PASS`。

### 我动过并已还原的路径（新修订）

| 件 | 改前 sha256 | 还原后 sha256 | 探针 |
|---|---|---|---|
| `packages/skill-bill/scripts/ratchet-frozen-686.mjs` | `0a1cf46f…617a` | `0a1cf46f…617a` | `registryKeyCount` 6→7（`rev686-12`） |
| `packages/skill-bill/src/cli/probe-dispatch.ts` | （新建，1 LF） | **已删除**（`exists=false`） | `rev686-12`／`rev686-13` |
| 其余修订件：`AGENTS.md` `f2b22fdd…ec07`、`test/cmd-registry-686.test.mjs` `36ab19fb…524d`、`test/legacy-ratchet-686.test.mjs` `5f147b2f…7e10`、`test/t686-收紧守卫.test.mjs` `8a71694d…c0f2`、`scripts/check-ratchet-tight.mjs` `813d6bfc…3c0d`、`src/cli/cmd_read.ts` `8863af16…18ea`、`src/render/envelope.ts` `1ae96b79…7982` | 见 `review-baseline2-sha256.log` | 逐件与基线相同（本席未动） | — |

收尾：`git status --short -- packages/skill-bill` 只剩**他席在飞件** `?? packages/skill-bill/docs/`
（内容 `t685-讨论决策页.html`，不属本票、不属本席；本席未动、未删）。

### 修订后缺陷清单

- 上一轮 (a)「判据 2 的机器前提没进台账」：**已处置，关闭**。`AGENTS.md`「文件行数告警线」一节新增一条，逐字写明
  「事实源不在盘的机器上 `--check` 会**明打一行 `WAKE-ASSETS SKIP` 后放行（exit 0）**，不静默当绿；但那种机器上
  手改 `wake-assets.ts` 不会被 `gen:check` 抓住」——与我在 `rev686-09` A 段拿到的读数一致
  （`WAKE-ASSETS SKIP：事实源不在本机…`，exit 0），写法与实测不矛盾。
- 上一轮 (b)「守卫原文与票面引文措辞不同」：**未处置、维持为措辞差异**（本轮 `rev686-12` 原文仍是
  「实况已降 1 而冻结值没下调：棘轮的牙齿松了」）。不影响判定，不入缺陷。
- **本修订引入：0**。三条修订各有机器自证（新探针 A／B／C），新夹具例也在 `rev686-11` 的 22/22 里真跑了。
- 范围外（沿用上一轮）：`test/record-write.test.mjs:87-91` 手写对账面；台账结论列嵌跨包活读数 356。
- 未做（沿用上一轮）：仓根 `pnpm test`／`pnpm build` 未跑；卡路里那一支未复跑；本件未提交。
