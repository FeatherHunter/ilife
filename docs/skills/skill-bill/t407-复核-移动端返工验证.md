# t407 复核 · 移动端返工窄面验证

> 复核对象：提交 `684976c`（R4）／`cdbae91`（R2）／`f225394`（R3）／`aa1ce63`（32 产物＋证据件）／`78c1679`（证据件补提交清单）
> 被验证据件：`docs/skills/skill-bill/t407-整改-移动端返工-证据.md`（225 行，整读）
> 复核纪律：只读（唯一例外＝第五节变异探测，已还原并自证）；本件为唯一新建交付物，临时件在 `.scratch\t407-verify6\`
> 读数出处：所有命令读数落 `.scratch\t407-verify6\*.txt`；命中判据件原文引用

## 一、逐条核验

### 1. 四判据（自跑）— **成立**

| 判据 | 命令 | 我的读数 | 判定 |
| --- | --- | --- | --- |
| 编译 | `npx tsc -b packages/skill-bill --force` | EXIT 0，诊断 0 行（`tsc.txt`） | 成立 |
| 测试 | `node --test "packages/skill-bill/test/*.test.mjs"`（连跑两次） | 第一次 EXIT 0：`tests 137 / suites 32 / pass 137 / fail 0`；第二次 EXIT 0：同读数（`test1.txt`／`test2.txt`） | 成立 |
| 机审 | `node docs\skills\skill-bill\t407-v8-style-audit.mjs --dir docs/skills/skill-bill` | EXIT 0；`有分隔符懒政：0 份`／`有英文裸词：0 份`／`有重复句：0 份`／`读数：全绿；共 32 份产物，缺 0 份` | 成立 |
| 合计数 | 同上末行 | ``设计位 `·`／`|` 0 处；标题写法位 55 处；`；`／`;` 534 处（载荷位 431）`` — 与被验方自称**逐字同值** | 成立 |

`mobile-rework.test.mjs` 护栏 5 条在册（`describe`／`it` 各 5），与自称「新建护栏 5 条」一致。

### 2. R4 区外 0 — **成立**

- 自写探针（摘 `<pre>…</pre>` 与 `data-t="…"` 两个载荷位）跑 32 份点名产物：`FILES=32 bill.全部=91 载荷区内=91 区外=0 data-t=145`（`r4b.txt`）。
- 被验方判据件 `.scratch\t407e\r4.mjs` 我自己重跑：`FILES=32 bill.全部=91 载荷区内=91 区外=0 → R4_OK 复制区外零命中`（`r4-run.txt`）——与证据件所贴读数同值。
- 脚本口径差异（不影响结论）：证据件第 144 行说 r4.mjs 摘两个载荷位；实测脚本第 9 行是 `data-t="[\s\S]*?"`（跨行非贪婪），不是按 `"` 收尾，比字面口径更宽，32 页读数仍为 0。
- 附带发现（**不属被验方**）：全目录另有 23 处区外 `bill.`，落在 `t407-链路总览.html`（17）与 `t407-对照乙-记退款-新仓基础全面开发.html`（6），两份不在 32 份点名产物内（`r4.txt`）。

### 3. R2 落点（抽 2 处）— **成立**

| 抽核处 | 产物原文 | 判定 |
| --- | --- | --- |
| 记一笔-回执表列名 `项/值`→`哪一项/记成什么` | `t407-页-记一笔-回执页.html:1793`：`<th scope="col" …>哪一项</th><th …>记成什么</th>`，且行内 `data-label="哪一项"`／`data-label="记成什么"`；`>项<`／`>值<` 均 0 命中（`probe2.txt`） | 成立 |
| 撤销-回执 diff 表列名 `字段/原值/新值`→`改了哪一项/改前/改后` | `t407-页-撤销-回执页.html:1793`：表头集合＝`改了哪一项 | 改前 | 改后 | 哪一项 | 记成什么`；`原值`／`新值`／`deleted_at` 均 0 命中；行名已映成「撤销标记」（`probe3.txt`） | 成立 |
| 附带（记借出-回执） | `记了几笔` 命中 1（旧串「这次记了几笔」0 命中）；「借贷标签流转：这一笔打 #借出 与 #未还」在可见文案里命中 1（toast 标题，`data-t` 为 −1 即不在载荷区）——`标签流转` 这半截改了，`借贷标签流转` 整串仍在（`probe2.txt`／`probe3.txt` A） | 记录备查 |

### 4. R3 剩 0 — **成立（带一处口径偏松）**

- 重跑被验方判据件 `.scratch\t407e\r3.mjs`：命中行 87 行，**`；`≥2 的串 0 处**、**`｜`／`|` 0 处**；命中行逐行按位置分：`<title>` 32 处、眉头 `记账 · 写入域` 32 处、H1 页标题 23 处（9 页的 H1 本就不带 `·`，如「记一笔借入」「补齐收入槽位」），合计 87（`r3-run.txt`＋`probe7.txt`）。
- 抽 4 份产物（记借出-回执／记收回-回执／记退款-回执／撤销-回执）非载荷区：`；`≥2 的串 0 处，`·` 仅 2 处（眉头＋H1），`|` 0 处。
- 口径偏松：证据件写「只剩页标题与眉头各一个 `·`」，但探针口径下每份还含 `<title>` 里那一个 `·`（32 处）。证据件第 135–136 行自己已声明 `<title>` 是浏览器标签页文案、不在页内，所以**没漏改**，只是「剩处」这句话的数法与探针读数差 32。
- `；` 总数：重放机审那套可见文本区位置扫描（剔 `<style>`／`<script>`／`data-t`，按标签栈判 class）得 `` `；`／`;` 合计=534，载荷位=431 ⇒ 非载荷 103 ``，与证据件第 164 行「595 → 534（非载荷 164 → 103）」的**改后值逐字同值**。

### 5. `data-key` 语义改动（重点）— **判得出：不影响下游（低风险，附一条语义走散备查）**

**① 产物现值（抽 3 页＋全 32 页）**：`data-key="record.add"`（26 页）／`data-key="record.update"`（6 页）＝32 页整（`r4b.txt` 逐份）。抽核 3 页原文：

- `t407-代表-记支出-采集页.html:1793`：`<section class="ilife-write" data-skill="bill" data-slot="ilife:bill:collect" data-page="collect" data-shape="receipt" data-key="record.add">`
- `t407-页-改记录-回执页.html:1793`：同形，`data-page="receipt"`、`data-key="record.update"`
- `t407-页-记收回-采集页.html:1793`：同形，`data-key="record.add"`

**② 谁在消费 `data-key`（全包 grep）**：

| 文件 | 命中 | 性质 |
| --- | --- | --- |
| `packages\skill-bill\src\shared\writeParts.ts:77` | 写方，`esc(sceneKeyOf(input.key))` | 唯一写方 |
| `packages\skill-bill\src\render\html.ts:27` | 写方，`data-key="' + escapeHtml(env.key) + '"`（**仍是整名** `bill.record.*`） | 另一条路径（老 envelope 模板页），不写场景名 |
| `packages\skill-bill\test\record-write.test.mjs:128,145` | 断言 `text.includes('data-key="record.add"')` | 只读断言 |
| `docs\skills\skill-bill\t407-手机墙.mjs` | **0 命中**（它按 `FAMILIES` 硬编码的 16 唤醒词拼文件名，第 66–80 行；自检只做 `existsSync`，第 85–91 行） | 不读 `data-key` |
| `docs\skills\skill-bill\t407-全部样张.html` | **0 命中**（251 行，逐条 `<a href="文件名">` 导航） | 不读 `data-key` |
| 探针 `.scratch\t407e\r3.mjs`／`r4.mjs`、`.scratch\t407d2\gen32.mjs` | **0 命中**（都不按 `data-key` 选页） | 不读 `data-key` |
| 其余全仓 `.mjs` | 只有别的技能自己的 `data-key` 断言（`skill-home`／`skill-schedule` 的 `render.test.mjs` 等），与 bill 无关 | 无关 |

**③ 影响判定：不影响下游。** 理由两条，都落在源码上：

1. 真正的「选页」标记是 **`data-page`**（`collect`／`receipt`），源码里写死这条分工——`writeParts.ts:64`「`data-page`（**选页用这一枚**：`collect`／`receipt`；两者共用 `shape=receipt`，靠 shape 选页会选错）」，`pageShell.ts:34`「`<section>` 上**选页用的那一枚**：`collect`／`receipt`；与 `slot` 同值，下游选页读它」。`data-key` 在整仓只被当**场景锚**用，没有任何一处按它过滤／分组／选页。
2. `data-key` 是全仓唯一的「只写不读」标记（除本包两条测试断言外无消费者）；手机墙与全部样张都按文件名清单工作，不看页内属性。故 `bill.` 前缀去掉后**不会静默失效**。

**备查（不算返工缺陷，属留痕）**：`render/html.ts:27` 那条老路径仍写整名，同一包里两条路径的 `data-key` 口径不一致；若日后有人拿 `data-key` 做跨页匹配，会碰上「新页 `record.add`／老页 `bill.record.add`」两种取值。现状无消费者，故不构成必须整改项。

### 6. 对账 5 条偏差 — **成立（其中 1 条另有归因偏差）**

| 偏差 | 是否如披露 | 我的读数 |
| --- | --- | --- |
| ⑤ `data-key` 取值改了 | 成立 | 见本节第 5 条：取值确为场景名，测试两处断言同步改了（`record-write.test.mjs:128,145`），`pageIdentity.ts:32` 头注释已改为「同写场景名」；`.scratch\t407d2\probe-extract.mjs` 里 `data-key="bill\.record\.\w+"` 正则仍在（临时探针，非交付物） |
| ② 报告 10 处抄句经核无此句 | 成立 | 抽 2 条到产物 grep：`期借借`＝0、`MySQL`／`UTF8`／`建表`／`amount_display`／`user_id`／`created_at`／`记记编号`／`查次数` 全 0（`probe8.txt`）；另抽 `请务必落实数据第一遍`／`库共 2 条未撤销的记录`／`分热购会与金额`／`写者·还没发生——这一项不用填`／`找出来还有问题`／`分类 · 金额 · 时间 · 账户 · 账本`／`bill=cmd:read`／`bill_record.add` 亦全 0 |
| ③ 记借入-采集首屏「未给」卡密度未动 | 成立 | 产物里 kpi-card 卡共 53 张，其中值「未给」5 张；被验方换算法（连容器）得 7 张；两数都在，结论「密度未动」不受影响 |
| ④ 撤销-回执两组复制按钮 | 成立 | 与披露同一条（`scene-undo.ts` 自建退出口），本窗未动 |
| ① 名单外同类 1 处一并改形状 | 成立但归因错 | 见 `## 三`：改动本身在产物里确认存在，但被点名的页不对 |
| ⑥ 体检读数跑在 `--force` 重编后的 dist 上 | 成立 | 我的 tsc 也是 `--force`；`tsc.txt` 诊断 0 行，非旧 dist 假绿 |
| 提交清单四笔 | 成立 | 见下第 7 条 |

### 7. 提交范围 — **成立**

五笔 `git show --name-only` 逐笔过滤非 `packages/skill-bill/`／`docs/skills/skill-bill/` 路径，**越域文件 0**：

| 提交 | 文件数 | 越域 | 内容 |
| --- | --- | --- | --- |
| `684976c` | 4 | 0 | `src/shared/{outsideScan,pageIdentity,writeParts}.ts`＋`test/record-write.test.mjs` |
| `cdbae91` | 12 | 0 | 11 件 `src/**`＋新建 `test/mobile-rework.test.mjs` |
| `f225394` | 14 | 0 | `src/shared/*`＋`src/record/*` |
| `aa1ce63` | 33 | 0 | 32 份产物＋证据件 |
| `78c1679` | 1 | 0 | 证据件补提交清单（9 行） |

仓根另有大量在途件（`skill-calorie` 的 26 件改动、各种 `.t499-*`／`probe.mjs`／`fieldLabel.ts` 等未跟踪件），**均不在这五笔提交内**，与证据件「别窗在途、本窗未碰」的自述一致。`packages/skill-bill` 工作区在复核起点即为干净态（`git status --porcelain -- packages/skill-bill` 空）。

## 二、读数

| 项 | 读数 | 出处 |
| --- | --- | --- |
| `tsc -b --force` | EXIT 0，诊断 0 行 | `tsc.txt` |
| 测试第一次 | EXIT 0：`tests 137 / suites 32 / pass 137 / fail 0 / cancelled 0 / skipped 0 / todo 0`，`duration_ms 4500.9` | `test1.txt` |
| 测试第二次 | EXIT 0：同上，`duration_ms 4448.0` | `test2.txt` |
| 机审 | EXIT 0：懒政 0 份／英文裸词 0 份／重复句 0 份／32 份缺 0 份；设计位 `·`／`|` 0 处，标题写法位 55 处，`；` 534（载荷 431） | `audit.txt` |
| R4 | `FILES=32 bill.全部=91 载荷区内=91 区外=0 data-t=145` | `r4b.txt`／`r4-run.txt` |
| R3 | 命中 87 行：`；`≥2＝0 处、`｜`／`|`＝0 处；`·` 分布 `<title>` 32＋眉头 32＋H1 23 | `r3-run.txt`／`probe7.txt` |
| R2 | 记一笔-回执／撤销-回执／改记录-回执 表头＝`哪一项｜记成什么`／`改了哪一项｜改前｜改后`；旧列名与 `原值`／`新值`／`deleted_at` 全 0 | `probe2.txt`／`probe3.txt` |
| 抄句 | 13 个被点名串在 32 份产物里全部 0 命中 | `probe.txt` |
| 变异（dropped `sceneKeyOf`） | tsc EXIT 0；测试 EXIT 1：`tests 137 / pass 135 / fail 2`，红名＝`必需槽位齐全 → 写库 + 回执整页落盘`、`必需槽位缺失 → 采集页（不写库）…复制 prompt 区` | `tsc-mut.txt`／`test-mut.txt` |
| 还原后 | `tsc --force` EXIT 0；测试 EXIT 0：`tests 137 / pass 137 / fail 0` | `tsc-restore.txt`／`test3.txt` |

## 三、不成立或部分成立

一条「部分成立」，不构成返工缺陷（改动的字节在，只是落在别的页）：

1. **证据件第 110 行「报销到账-采集（48）」那一行的落点写错了页**。该行记「`src/shared/outsideScan.ts`「在哪办」格 `本仓（bill.record.add）`→`这台手机上的记账技能`；同页 `data-key` 去 `bill.` 前缀」。实测：全 32 份产物里 `这台手机上的记账技能` **只命中 `t407-页-拍账单-采集页.html:1793`**（`probe8.txt`＋grep 复核），报销到账-采集页**没有**这张「在哪办」表、也没有这句。源码侧也对得上——`outsideScan.ts:105` 的 `{ step: '落库', who: '这台手机上的记账技能', … }` 属拍账单的「图片识别外置」表，该表只落拍账单-采集页。
   - 影响面：**R4 计数不受影响**。32 页区外 `bill.` 仍为 0（自跑 0／判据件 R4_OK 0）；那一处可见文案确实改掉了，只是改的是拍账单-采集页。报销到账-采集页的区外 `bill.` 归零靠的是 `data-key` 去前缀（该页确为 `record.add`），与 33→0 的算式自洽。
   - 结论：**成立（33→0 计数与改动事实都成立）／部分成立（落点归属错一处）**。

余下 6 条与证据件逐节吻合，未发现不成立项。另记两处「措辞比读数紧一点但不影响结论」：

- R3 的「只剩页标题与眉头各一个 `·`」——探针口径下每份还有 `<title>` 那一个，证据件第 135–136 行已自行声明 `<title>` 不在页内，数法差 32，不是漏改。
- R2「记借出-回执」行把「本窗补 `借贷标签流转：`→`标签流转：`」写成已完成口径，但产物可见文案里 `借贷标签流转：这一笔打 #借出 与 #未还` **仍原样在**（toast 标题，`probe2.txt` 第 41 行）。即该处是**改了一半**（`＋`→`、` 那半已改），证据件与产物对不上。

## 四、结论

**结论一：放行** —— 四判据自跑全绿（tsc EXIT 0／测试两次 137‑0／机审 EXIT 0 全绿／R4 区外 32 页 0 命中）；R2 抽两处列名落点在产物里真改了；R3 非载荷区 `；`≥2 与 `·`／`|` 为 0（只剩眉头／页标题／`<title>` 的标题写法位）；29→0 的 R4 算式与五笔提交范围（越域 0）都对得上；变异探测必红（`fail 2`，红名与证据件逐字一致）、还原后三自证齐（`git diff --stat` 空、`git status --porcelain` 空、`writeParts.ts` sha256 `84208baf…96ccc` 与备份一致）。唯一「部分成立」是证据件一处落点**归属写错页**（记成报销到账-采集，实为拍账单-采集），改动事实与 R4 计数均不受影响，属留痕性修订、不拦放行；另有「记借出-回执 `借贷标签流转：` 只改了半截」建议随下次文档修订一并记明。

**结论二：双端墙可以开工** —— 本轮改动全部落在页面文案与页内标记，`data-key` 从整名改场景名的语义变动**判定为不影响下游**：真正的选页标记是 `data-page`（源码写死这条分工），`data-key` 除本包两条测试断言外全仓无消费者，`t407-手机墙.mjs` 与 `t407-全部样张.html` 都按文件名清单选页分组，**不会静默失效**；32 份产物同名前缀与文件名一个没动，手机墙的 `existsSync` 自检仍逐条可过。
