# PASS —— t411 复核 · 独立复跑

#411「查询骨架」交付与票面验收一致：7 项机器门复跑全绿、红线四条读数与期望逐条相符；另有 **1 处对账偏差**、**2 项未验证**（浏览器渲染面、变异自证），都不足以翻判定。

本文每条结论的形式为「命令 ＋ 加锁运行标识 runId ＋ 退出码 ＋ 摘要行」。跑法一律 `node tooling/run-locked.mjs --ticket 411V -- pwsh -NoProfile -Command "<一条命令>"`，禁止裸跑编译／测试；长输出落 `.scratch/t411V/`。复核用临时库：`%TEMP%\t411V-db`（验收）、`%TEMP%\t411V-probe-db`（探针）；产物落 `%TEMP%\t411V-out`。

## 第一节 判定

| 票面验收项 | 复跑读数 | 判 |
|---|---|---|
| 查今天跑通 | exit 0、`data.total=2`、整页 73271 B | 达标 |
| 查区间跑通 | exit 0、`data.total=3`、整页 75107 B | 达标 |
| 列表整页能打开 | 产物含 `<!DOCTYPE html`×1、`data-slot="ilife:bill:list"`×1、`<table`×1、`复制数据`×3、`复制日志`×1 | 结构面达标；**渲染面未验证** |
| 红线：空查询不返全量 | exit 2／2、stdout 0 字节 | 达标 |
| 红线：坏输入阻断不冒充正常 | exit 4／4、stdout 0 字节 | 达标 |
| 结构纪律五步 | `t411-实施与对账.md` 五节齐；超线件已正面报警并给拆法 | 达标（第三步偏差一处，见第四节） |

## 第二节 机器证据

**提交与祖先（读盘）**

- `git merge-base --is-ancestor <sha> HEAD` → `58d6b25`／`9d1a7c8`／`cc970a2` 三笔 **exit=0/0/0**（都是 HEAD 祖先）。
- `git show --stat` 三笔只碰 `packages/skill-bill/` 下的件：`58d6b25` 19 件、`9d1a7c8` 2 件、`cc970a2` 2 件。
- **交付不止三笔**：另有两笔文档提交 `405db91`（实施与对账 ＋ 老模板逐块清单 ＋ 两张样张 ＋ AGENTS.md 台账）与 `b989d41`（证据件补两段读数），同属 #411，`--stat` 只碰 `docs/` 与 `packages/skill-bill/AGENTS.md`。
- HEAD 复核开始时是 `0ecb495`，复核过程中他席推进到 `b989d41`；`git diff --stat 0ecb495..HEAD -- packages/skill-bill/src packages/skill-bill/scripts packages/skill-bill/test` **为空**（源码零变化），故本轮复跑对当前 HEAD 同样成立。`git status --porcelain -- packages/skill-bill/` 空（工作区干净）。复核起点时两张样张还是未跟踪件，随后被 `405db91` 收进仓。

**编译／生成门／测试**

| 命令 | runId | 退出码 | 摘要行 |
|---|---|---|---|
| `node node_modules/typescript/bin/tsc -b packages/skill-bill` | `20f30b53` | 0 | `TSC_EXIT=0`，`tsc.log` 0 字节（零诊断） |
| `node packages/skill-bill/scripts/gen-cli.mjs --check` | `d23fae5f` | 0 | `GEN-CHECK ok …registry.ts sha256=113648fa…`／`GEN-CHECK PASS：命令 6 条；能力 2 个（query／record）` |
| `cd packages/skill-bill && node --test` | `2728b1cc` | 0 | `tests 194 / suites 45 / pass 194 / fail 0 / cancelled 0 / duration_ms 4748.98` |
| `node --test test/t411-query-skeleton.test.mjs` | `873a0624` | 0 | `tests 16 / pass 16 / fail 0` |

**验收两条（唯一出口真跑，非测试内路）**。样本自种 4 笔（与 `test/cli.test.mjs` 的 `before()` 同数据），加锁运行 `124edba4`，`SEED_EXITS=0,0,0,0`。

| 命令 | runId | 退出码 | 摘要行 |
|---|---|---|---|
| `bill.record.today --params '{"date":"2026-09-06"}' --html …\a.html` | `8a9c93fc` | 0 | `SHAPE=list`、`DATA_TOTAL=2`、`BYTES=73271`、`DELIVERY_BYTES=73271`；stderr 空 |
| `bill.record.range --params '{"start":"2026-09-01","end":"2026-09-30"}' --html …\b.html` | `677dff33` | 0 | `SHAPE=list`、`DATA_TOTAL=3`、`BYTES=75107`；stderr 空 |

**红线四条**（stdout 一律 0 字节 ＝ 不吐载荷）

| 命令 | runId | 退出码 | stderr 摘要 |
|---|---|---|---|
| `bill.record.search --params '{}'` | `979f723a` | **2** | `ERR 2: 口径失败：搜备注须给 q` |
| `bill.record.range --params '{}'` | `459923ed` | **2** | `ERR 2: 口径失败：缺槽位 start/end（或 range=week/month，或 category/account/ledger 条件）` |
| `bill.record.range --params '{"start":"2020-01-01","end":"2020-01-02"}'` | `c243091b` | **4** | `ERR 4: 取数失败：区间无记录：2020-01-01~2020-01-02（缺失阻断，不返空统计）` |
| `bill.record.detail --params '{"id":99999}'` | `62fae1ae` | **4** | `ERR 4: 取数失败：无此账单：99999` |

**与样张对差（可复现性）**：我这次重出的 `a.html` 与仓里 `t411-样张-查今天.html` 逐字符比 —— 长度都是 71063 字符、**相异 3 个字符**，差异只在时刻串（`2026-09-15 19:29:31` vs `…19:21:24`）；两份字节数同为 73271／75107。即样张是这两条命令的真产物，且除时间戳外可逐字节重出。

## 第三节 逐条路径

**改动件（`58d6b25`，19 件，全在 `packages/skill-bill/` 下）**：新增 `src/query/{commands,index,read,list}.ts`（53／22／236／182 LF）；改动 `src/cli/{cmd_read.ts(485),registry.ts(37)}`、`src/shared/{commandSpec(74),writeParts(89),pageShell(65),copyArea(159),docPage,pageIdentity}`、`src/policy/{index,record(91)}`、`src/render/{views(125),index,envelope(64)}`、`src/record/write.ts`、`scripts/gen-cli.mjs(302)`。测试件：`test/t411-query-skeleton.test.mjs`（`9d1a7c8`）、`test/record-write.test.mjs` 注册表条数 2→6（`9d1a7c8`）；`cc970a2` 再改 `src/query/list.ts` ＋ 测试断言。

**证据件**：`docs/skills/skill-bill/t411-实施与对账.md`（五步 ＋ 真跑表 ＋ 变异两行 ＋ 走偏三条）、`t411-查询域-老模板逐块清单.md`、`t411-样张-查今天.html`、`t411-样张-查区间.html`、`packages/skill-bill/AGENTS.md` 的「#411 查询骨架后复测」节。

**行数台账独立复核**：我用 LF 口径（只数 `\n`）逐件重量，与 AGENTS.md 那一节的 13 个读数**逐个相符**（485／53／22／236／182／302／159／74／65／89／125／91／37）。超线仍两件：`cmd_read.ts` 485（本票净减 73）、`wake-assets.ts` 986（本票未动），两件都带超因与拆法去向。

**交付形状（structure.md 五条铁律 ＋ 必报五步）**

- **铁律一 能力自治**：grep 逐行核 `src/query/*.ts` 的引用面 —— 只有 `../fetch`、`../policy`、`../render/views.js`、`../shared/*` 与本域件；**无 `../record/` 引用、无抄份**。`views.ts` 的 `calcKpi`／`toBillItem` 是两消费者共用（查询四条 ＋ 出口的 `account.query`），头注释记为搬迁债务。达标。
- **铁律二 概念唯一**：命令事实唯一源 `query/commands.ts`；形状不再写死 —— `render/envelope.ts` 的过渡表已删四条查询命令、改由 `REGISTRY` 运行期派生，且对「两处形状不一致」会抛。**残留一处**：`src/render/templates.ts:32-35` 仍留着四条查询 key→老模板映射（迁移路径已不可达，属 t406 写入域迁移时同形的沿革债，非本票引入）。
- **铁律三 改动可预告**：偏差一处，见第四节第 1 条。
- **铁律四 名字取自 HELP**：目录与四条命令名（查今天／查区间／搜备注／查账单详情）都能在 `src/policy/wakewords.ts` 的 `WAKE_TABLE` 里指出来；测试逐条核代表唤醒词。达标。
- **铁律五 接口小**：能力门 `src/query/index.ts` 对外 2 件（`QUERY_COMMANDS`／`runQueryRead`）；`read.ts`／`list.ts` 不出目录。达标。
- **一个命令恰住一处**：`cmd_read.ts` 的 switch 里已无任何 `case 'bill.record.*'`（grep 只剩 10 条未迁移命令），四条查询命令只经 `REGISTRY`。达标。

**日志件（`.scratch/t411V/`）**：`tsc.log`、`gen-check.log`、`test-full.log`、`test-t411.log`、`today.stdout.json`、`today.stderr.txt`、`range.stdout.json`、`range.stderr.txt`、`redline.log`、`probe-p1.json`、`probe-p2.json`、`probe-p3.json`。加锁审计行在 `.scratch/locks/gate-runs.log`（ticket=411V，含上表各 runId 与退出码）。

## 第四节 未做项与下一手缺什么

1. **对账偏差一处（本票范围内）**：`58d6b25` 动了 `src/record/write.ts`（`nowStamp()` 改为调 `shared/copyArea.ts` 的 `actionStamp()`，±6 行），但 `t411-实施与对账.md` 第一步「改动」清单没有这一行，第五步却写「未预告而碰到的件：**0**」。该件在第二步的散文里被提到过，不改变结论，但第五步那句读数是错的 —— 下一手把它补进清单或改成「偏差一处」。
2. **未验证①「能打开」的渲染面**：我只核了产物结构与 DOCTYPE，没复跑浏览器；实施方自称无头 Chrome `--dump-dom` 两条 exit 0，那是自报，**未验证**。视觉那一眼仍留给用户（#417）。
3. **未验证②变异自证 M1／M2**：工具 `.scratch/t411/mutate.mjs` 属他席窗口，我未复跑，其红绿读数**未验证**；我自设的探针只覆盖标签匹配与金额文案，不替代变异。
4. **未跑写路径**：本轮只跑 `gen-cli --check`（生成物门），未跑 `pnpm gen`／`pnpm build` 的写路径（按纪律不改生成物）。
5. **下一手缺什么**：①把 `src/record/write.ts` 补进第一步清单；②`render/templates.ts` 的四条查询 key→老模板映射与 `envelope.ts` 的清理不同步，建议随「读命令搬迁」整条线记一笔；③17 词说明面接线（SKILL.md／HELP）属 #416，本票未碰。

## 第五节 自设探针（命令与读数）

探针样本是我自己造的（**未照抄** `t411-query-skeleton.test.mjs` 的参数表；唤醒词从 `src/policy/wakewords.ts` 的 `WAKE_TABLE` 读）；临时库 `%TEMP%\t411V-probe-db`，种 4 笔，加锁运行 `5e4b05e2`，`PROBE_SEED_EXITS=0,0,0,0`：`#旅行计划 订金` −35.5（只靠备注命中、金额为负）／`#旅行 门票` −58／`9月工资 到账` +8000.25／`旅行计划讨论` −12（带词不带 `#`，是对照组）。

| 探针 | 命令 | runId | 退出码 | 读数 |
|---|---|---|---|---|
| P1 标签精确匹配 | `bill.record.search --params '{"kind":"tag","tag":"旅行计划"}'` | `ddfdd43a` | 0 | `TOTAL=1`，只命中 id=1 `#旅行计划 订金`（金额 −35.5，仅备注命中）；**未误伤** `#旅行 门票`，也未命中不带 `#` 的 `旅行计划讨论` |
| P2 前缀不误命中 | `… '{"kind":"tag","tag":"旅行"}'` | `d218a0ea` | 0 | `TOTAL=1`，只命中 id=2 `#旅行 门票` ⇒ `#旅行计划` **不**被 `旅行` 前缀命中（整词比对生效） |
| P3 页面金额 vs 载荷 | `bill.record.today --params '{"date":"2026-09-06"}' --html …\p3.html` | `5b13fec6` | 0 | 载荷 `-12/-35.5/-58` → 页面文本 `-12.00/-35.50/-58.00`，三条 `INPAGE=True`；KPI `expense=105.5`→`105.50`、`net=-105.5`→`-105.50` 都在页；实取片段：`<td class="…-cell-right" data-label="金额">-35.50</td>` ⇒ 符号与两位小数逐字一致 |
| P4 空命中不返全量 | `… '{"kind":"tag","tag":"不存在的标签"}'` | `061376ab` | 0 | `TOTAL=0`，stdout 188 B（只有 envelope，**没有把 4 笔全量倒出来**） |

结论：被审用例覆盖不到的两处（标签整词匹配的不误伤面、页面与载荷的金额文案一致性）本次独立探针都通过。
