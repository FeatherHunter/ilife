# t417 程序锁 · 17 条真出口（#417 的程序部分；肉眼终审待用户）

> 本件只钉程序锁：17 个唤醒词逐条真出口 exit 0＋落盘＋绝对路径＋字段正确，另附空态 1 条与阻断 5 条。
> 用户肉眼终审（空白 session 亲手跑 17 词）仍待做，本票不关。判据 `packages/skill-bill/test/t417-query-lock.test.mjs`（19 条）。

## 一 覆盖（词→调用，SKILL.md:51-67）

today 五词：查今天（无参）／查昨天／查某天（2026-09-06＝2 笔）／查最近（limit 3）／查账单（别名，同页同数）；
range 六词：查周／查月／查区间（09-03~09-06 固定窗 5 笔，与今天无关）／查分类／查账户／查账本（旅行＝1 笔）；
search 五词：搜备注／查标签／查欠款／查待报销／查分期（各 1 笔）；detail：查账单详情（由搜备注取 id）。
种子日期用相对日期（今天／昨天），周月窗天然命中，不写死（#413 G4）。

## 二 读数（持锁）

| 运行 | runId | 结果 |
|---|---|---|
| t417 靶向 | 99446fc2 | 19/19 exit 0（首跑区间计数按错 8 vs 5，当场收窄固定窗后绿，走偏如实记） |
| 包内全量 | 49f4fe4c | 271/271（252＋19） exit 0 |
| 变异 M1（查最近窗文改 LATEST） | 31cf57eb | tsc 0，t417 红 1 条（最近 3 笔），判据有鉴别力 |
| 还原重编复绿 | 63b20364 | tsc 0，t417 19/19；`git diff` 本票两路径零差异 |

```
GATE-RUN runId=99446fc2-0304-4e5a-affb-9a5e9a8d4d14 cmd=node --test test/t417-query-lock.test.mjs
GATE-RUN runId=49f4fe4c-d09e-436f-b114-0cbcdf674bac cmd=node --test（包内全量）
GATE-RUN runId=31cf57eb-77ba-4c8a-aabd-3fc63afcb2a7 cmd=变异窗（tsc＋t417，红）
GATE-RUN runId=63b20364-4b7f-488a-a446-4e20fb1a55c6 cmd=还原重编复绿窗
```

## 三 交棒

- #417 剩用户肉眼终审：在空白 session 亲手跑 17 词验标题／KPI 格数／芯片／聚合／手机版式，验完在票里评论收口再关。
- 样张：`docs/skills/skill-bill/t412-样张-查今天.html`／`t412-样张-查昨天空.html`（t411 两张同目录）。
