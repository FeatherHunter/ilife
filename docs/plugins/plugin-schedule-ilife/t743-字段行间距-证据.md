# #743 追加证据 · dsh-schedule-ilife：设置页样式表逐项同形（字段行间距那一处 3 : 3）

判定：**PASS**（本包读数全绿；形状与判据逐段照抄样板件，本件只记本包不同之处）

- 形状来源：`packages/plugin-bill-ilife`（同票样板，现象、根因、甲案取值表、机器读数、变异自证与结构纪律
  全票记在 `docs/plugins/plugin-bill-ilife/t743-字段行间距-证据.md`，本件不重抄）。
- 本包改了什么：`src/client.ts` 的样式表 `S`（本包是甲案那一支，只动取值形态与缺陷那一处，可见版式未变）：
  `input` 补 `boxSizing: 'border-box'`、`input`／`btnPick`／`btn`／`btnPrimary` 的 `background`／`color`／
  边框色取别名那一族（`btnPick` 与居家归到同一条）、`btnPick` 的 `cursor`／`whiteSpace` 两条调成六家同序、
  `okText` 的色标换成宿主真有的 `var(--dsw-alias-state-success-primary, #12805c)`、
  样式表上方注释加「六家逐项同形」一行。
- 本包机器读数：与样板件同一批门禁运行（runId 见样板件与
  `docs/plugins/plugin-bill-ilife/t743-字段行间距-门禁运行记录.md`）。
- 本包行数（LF 口径，超线报警见样板件第四步）：`src/client.ts` 490 → 494 行。
