# #736 证据 · dsh-schedule-ilife：设置页目录行接系统文件夹选择器

判定：**PASS**（本地可判的判据全绿；「真机上点开系统对话框」那一跳按票面 Agent Brief 由维护者点屏）

## 改了什么

| 件 | 改了什么 |
| --- | --- |
| `src/settings.ts` | `ConfigControl` 加第四档 `'directory'`；**只**把 `db.dir` 改档（`db.name`／`html.dir`／`files.help`／`lark.cliPath` 四行一字未动，控件档已由 H 组锁死） |
| `src/dsh-ctx.ts` | 照样板末尾两格新增 `REMOTE_DIRECTORY_PICKER`（服务名）与 `DirectoryPickerFace`（本包只用 `pick`） |
| `src/client.ts` | 软依赖取命名空间（`ctx.get`，**没写进 inject**）＋ 目录行的「选择文件夹…」按钮 ＋ 三态归一（选中／取消／供不了）；`Row` 改为**导出**供用例直调 |
| `test/config-surface-696.test.mjs` | 新增 H 组七条（档位不含糊：目录键集＝`['db.dir']`／缺席不画按钮／按钮链路／取消不动／被拒给人话／归一三态／`inject` 未变）；共用加载件用 `test/helpers/client-bundle.mjs`（仓根那一份，**没复制第六份**） |

形状不变式逐条落地：目录档只发给 `db.dir`；拿不到命名空间 ⇒ `resolveDirectoryPicker` 回 `null` ⇒ 不画按钮、文本框照旧；
点按钮 ⇒ 唤一次 `pick()` ⇒ 绝对路径回填该行（`null`／空白串不动值；抛错只出人话 ⇒ 页面收起入口）；
`inject` 保持 `['slots','connection']`（写进去＝硬依赖，提供方缺席整包被停靠）。

## 机器读数（门禁运行，逐条对账）

GATE-RUN runId=6a90b63b-02ee-4992-94f5-1580df8a0006 cmd=node tooling/run-locked.mjs --ticket 736 -- node node_modules/typescript/bin/tsc -b packages/plugin-schedule-ilife
GATE-RUN runId=1228c94b-7a11-4c63-bede-585728dcb59f cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-schedule-ilife run build:client
GATE-RUN runId=760a12cd-47af-4962-adb9-32ecffd1b7e6 cmd=node tooling/run-locked.mjs --ticket 736 -- node --test packages/plugin-schedule-ilife/test/config-surface-696.test.mjs
GATE-RUN runId=52ff34a9-aaf1-422c-8939-d46d9b2bf002 cmd=node tooling/run-locked.mjs --ticket 736 -- node tooling/check-boundaries.mjs

四条全 exit=0。前三条分别是宿主侧编译、client 束重建（`dist/client.js` 23.67 kB）、本包套件；
第三条是**还原之后**那次读数：`tests 35／pass 35／fail 0`，其中 `H 目录行与系统文件夹选择器入口（#736）` 一组七条全绿；
第四条边界门 `boundaries: PASS`。

## 变异自证（红／还原一致两行）

变异点：`src/client.ts` 里 `resolveDirectoryPicker` 的
`return isDirectoryPicker(raw) ? raw : null;` 改成
`return isDirectoryPicker(raw) ? raw : ({ pick: async () => null } as DirectoryPickerFace);`
（编辑器改写，**没用** PS 的 `Set-Content`／`Out-File`）。

- 红：变异前 `client.ts` SHA256＝`fb8b060e1640da0fd96cd8feaf61a7feebb35036890e5bd92dd9c4bd67d5d444`
  ⇒ 变异后＝`c692e5369b95b61108116e1fd75cf8a303822dfdcea899cb2f43daf4d21d60e1`；
  重建 GATE-RUN runId=0c3ee546-f2c1-4513-953c-aa7d8447c745（exit=0——这是**语义**破坏，不是语法破坏）；
  套件 GATE-RUN runId=10f3c1a8-e842-443b-b377-a480043baa84 ⇒ **exit=1，pass 34／fail 1**；
  红在哪一条：`命名空间拿不到 ⇒ 没有入口（软依赖；守卫拒绝也当没有）`（`AssertionError: 拿不到就不给入口：() => undefined`）。
- 还原一致：编辑器改回原样 ⇒ SHA256＝`fb8b060e1640da0fd96cd8feaf61a7feebb35036890e5bd92dd9c4bd67d5d444`，与变异前**逐字节相同**（`MATCH=yes`）；
  重建 GATE-RUN runId=1b953bfd-2648-4b19-9377-906d352d2638（exit=0）＋ 套件 GATE-RUN runId=9efb42b5-9bc6-45c8-8f07-ab5518c30049 ⇒ **exit=0，pass 35／fail 0**。

上一轮同形变异（同样两条命令、未夹哈希）读数一致，留档备查：红＝runId=c6ded55f-fb02-4939-9415-bb833f020b10（build，exit=0）＋
runId=480cb69e-a1d5-488b-a44b-5670d71f3050（test，exit=1，fail 1）；还原＝runId=aa0bdf07-dfa4-41af-84d2-0504f1c13b35（build，exit=0）＋
runId=7abcc7e2-2a8e-4ece-891c-1e8b6417ea90（test，exit=0，fail 0）。

## 未做项（如实记）

- **真机点屏那一跳没做**：需要人在爱生活面板点一次、看操作系统文件夹对话框弹不弹、选完值是否落进「数据目录」那一行。
  判据与失败处置写在 #736 的 Agent Brief「外部确认出口」一节（本席无 GUI 点击能力）。
- 第一次 client 束重建曾误用**未加锁**的裸 `pnpm --filter dsh-schedule-ilife run build:client`（无 `run-locked` 包装）；
  该次读数作废，有效读数只有上面那条 runId=1228c94b… 的加锁运行（其余全部经包装器）。
- 本仓是共享工作区：本席只碰上面四个件 ＋ 本证据件，未做任何 git 写操作（提交由本票作者统一办）；
  `dist/` 是构建产物（未跟踪），不在交付清单里。


## 超线报警（必报五步·第四步）

**已超线，需要根据规则进行重构。** src/client.ts 本票改前 357 行 → 现在 470 行（**LF 口径**；本仓插件包沿用的兄弟口径 350）。更正：本条先前那对数字量的是「不含空行」的计数，已作废；全票统一按 LF 口径，见 docs/plugins/plugin-bill-ilife/t736-证据.md 的订正。）
超因、为什么不这次拆、以及拆法，见 docs/plugins/plugin-bill-ilife/t736-证据.md 的「超线报警」一节（六家同形，全票在那里记一次）。
