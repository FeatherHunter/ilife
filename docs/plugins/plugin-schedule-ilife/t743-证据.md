# #743 证据 · dsh-schedule-ilife：设置页四处整改（选择文件夹 · 数据目录预填 · 文案精简 · 头部两行间距）

判定：**PASS**（本包读数全绿；形状与判据逐段照抄样板件，本件只记本包不同之处）

- 形状来源：`packages/plugin-bill-ilife`（同票样板，判定、根因、变异自证与结构纪律全票记在
  `docs/plugins/plugin-bill-ilife/t743-证据.md`，本件不重抄）。
- 本包改了什么：`src/settings.ts`（行文案换短句 ＋ `db.dir` 标 `prefillFrom: 'dataDir'`）、
  `src/dsh-ctx.ts`（补 `DirectoryPickerAnswer`，`pick` 的返回类型改成信封）、
  `src/client.ts`（`readPickAnswer` 解信封 ＋ `toDraft(values, prefill)` 预填 ＋ `info` 去 margin ＋
  页脚与报错人话换短句 ＋ 等对话框期间一行提示）、`test/config-surface-*.test.mjs`（H 组按平台信封订正 ＋ 预填用例）。
- 本包机器读数（同一批门禁运行，runId 见样板件）：
  - `tsc -b packages/plugin-schedule-ilife` exit 0
  - `pnpm --filter dsh-schedule-ilife run build:client` exit 0
  - 本包套件并入六家那一轮：`tests 215／pass 215／fail 0`
  - `boundaries: PASS`
- 本包行数（LF 口径，见样板件第四步超线报警）：`src/client.ts` 与 `src/settings.ts` 的读数按样板件的表。
