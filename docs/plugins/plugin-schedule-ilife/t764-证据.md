# #764 证据 · 作息管家设置页收窄 · **插件侧**（页面从 5 行可改表单收成「1 可改 ＋ 2 只读」＋ 飞书 CLI 状态行）

判定：**PASS**（页面判据全绿：新增 13 条组件级用例 13／13、整页读数 6／6 PASS、插件套件 70／70、
跨包锁 5／5、变异自证「打掉只读标记必红／还原必绿」跑齐）。

## 一、交付了什么（逐件一句）

| 件 | 改了什么 |
| --- | --- |
| `src/settings.ts` | 行表 5 行 → 3 行（可改 1：数据目录；只读 2：库文件名 `resolveFrom: dbFile`／HELP 产物目录 `resolveFrom: htmlDir`）；加 `readonly`／`resolveFrom` 两字段；高级组已空（空组不渲染） |
| `src/client.ts` | `Row` 支持只读（控件 `disabled`、不接 `onChange`，目录行按钮保留＋不可点）；`toDraft` 只读行显示 `resolved` 格（缺回执则空串）；`fromDraft` 跳过只读行；`dirty` 只算可改行；页头数据目录改用 `resolved.dbDir`；控件文案去省略号；新增飞书 CLI 状态区（三档显示＋复制 prompt＋官网行＋重新检测） |
| `src/contract.ts` | 回执形状加 `ResolvedPaths`（`dbDir`／`dbFile`／`htmlDir`）＋可选字段 `resolved`（旧技能缺席 ⇒ 只读行空串） |
| `src/index.ts` | 转出 `ResolvedField`／`ResolvedPaths` 两个类型（对外面只加类型，值面一字未动） |
| `test/t764-设置页收窄.test.mjs`（新） | 13 条：行表形状·只读值只来自回执·只读不进保存·只读渲染·文案零省略号·状态行（判据挑取／官网逐字／复制三态）·端到端（整面 resolved＋体检 lark 项） |
| `test/config-surface-696.test.mjs` | 行数 5→3、高级组空、退休清单断言、删 `files.help`／`lark.cliPath` 旧断言（旧断言跟着口径走，#749 同例） |
| `docs/plugins/plugin-schedule-ilife/t764-设置页读数.mjs`（新） | 整页读数脚本：真产物＋真技能回执（临时家目录＋掐 PATH ⇒ 红档确定）渲染整页，量验收第 2、4 条（那台 t696 渲染台只 mock 三端点，吃不到 `config.check`，故另写一份） |

技能侧一行没动之外的东西：`slot.ts`／`bridge.ts`／`dsh-ctx.ts`／`skill-provider.ts` 一行没动；
`inject` 仍是 `['slots','connection']`；`config.check` 端点是 #706 既有注册，本票只消费它。

## 二、页面形状（改造前 → 改造后）

| 行（键） | 改造前 | 改造后 | 显示什么 |
| --- | --- | --- | --- |
| 数据目录（`db.dir`） | 可改·目录行 | **可改**（不变） | 生效绝对路径（空值＝按默认落点，面板按回执预填） |
| 库文件名（`db.name`） | 可改·文本框 | **只读** | 回执 `resolved.dbFile`（库文件绝对路径） |
| HELP 产物目录（`html.dir`，已改名） | 可改·文本框 | **只读** | 回执 `resolved.htmlDir` |
| 产物文件名主体（`files.help`，高级） | 可改·文本框 | **删行**（键已退休） | — |
| 飞书 CLI 路径（`lark.cliPath`，高级） | 可改·文本框 | **删行**（键已退休） | — |
| 飞书 CLI 状态行（新增，非配置项） | 无 | 三档显示＋复制 prompt＋官网行 | 体检 `lark.cli` 那一项（判据由技能侧出） |

本家无只读目录行（唯一的目录行就是可改的数据目录）：「只读目录行按钮保留＋不可点」那条形状
以合成只读目录行在组件级证明（D 组第二条），页面上无此形态——如实记，不凑数。

## 三、整页读数（`t764-设置页读数.mjs`，真产物＋真回执，exit 0）

```
PASS 四块齐：数据目录／库文件名／HELP 产物目录／飞书 CLI 状态行
PASS 行形态：[可改, 只读, 只读]（框里是生效绝对路径）
PASS 按钮齐：选择文件夹／复制 prompt／重新检测／保存／重置为默认／重新读取
PASS 状态行红档（PATH 已掐掉真 CLI，确定性红）
PASS 官网行逐字＋可点击
PASS 全页文案零省略号
CALLS ["config.get","config.check"]
TEXT 作息管家 · 配置 配置文件 C:\…\.ilife\schedule.yaml 数据目录 C:\…\.ilife\data … 数据目录 库文件与产物的根目录。留空＝用默认目录。 选择文件夹 库文件名 数据目录下的库文件。只读，要改请编辑配置文件。 HELP 产物目录 HELP 页面的存放目录。只读，要改请编辑配置文件。 飞书 CLI 没找到飞书 CLI：面板「飞书 CLI」那一行有安装指引（复制 prompt 照做）。 装好并登录后，在面板上点重新检测。 复制 prompt 重新检测 飞书CLI官网为：https://www.feishu.cn/feishu-cli 保存 重置为默认 重新读取
```

## 四、机器读数

GATE-RUN runId=f7c1ecdf-24ec-46e7-a23e-3763df81bd63 cmd=node node_modules/typescript/bin/tsc -b packages/plugin-schedule-ilife packages/skill-schedule
GATE-RUN runId=86c85656-e70e-4579-a0e7-be23690ade49 cmd=pnpm --filter dsh-schedule-ilife run build:client
GATE-RUN runId=796339db-fc0a-40b6-8bdc-5240db1a8fdf cmd=node --test packages/plugin-schedule-ilife/test/t764-设置页收窄.test.mjs
GATE-RUN runId=d4f70527-003c-49ba-9915-ccd97abbfc54 cmd=node --test packages/plugin-schedule-ilife/test/*.test.mjs
GATE-RUN runId=c4c0a8e0-3ccf-49f0-a5be-545f8656d81e cmd=node --test test/panel-copy-743.test.mjs
GATE-RUN runId=02e23c2e-b84e-47ba-8584-ef93d200951f cmd=node docs/plugins/plugin-schedule-ilife/t764-设置页读数.mjs
GATE-RUN runId=725373e5-5347-4a65-8592-adb6413b45c9 cmd=node .scratch/t764/mutation-plugin.mjs

| runId（尾段） | 命令 | exit | 摘要 |
| --- | --- | --- | --- |
| `f7c1ecdf` | `tsc -b` 两包 | 0 | 宿主侧编译通过 |
| `86c85656` | `build:client`（tsdown） | 0 | client 束重建（`dist/client.js` 73.44 kB） |
| `796339db` | 插件侧 t764 单件 | 0 | `tests 13／pass 13／fail 0` |
| `d4f70527` | 插件套件整批 | 0 | `tests 70／pass 70／fail 0` |
| `c4c0a8e0` | 跨包锁 `panel-copy-743` | 0 | `tests 5／pass 5／fail 0`（样式表未加新项，既有五条全绿） |
| `02e23c2e` | 整页读数 | 0 | 6／6 PASS（上节） |
| `725373e5` | 变异自证（见下） | 0 | 红 `pass=11 fail=2` → 还原后绿 `pass=13 fail=0` |

本机黄档读数（插件 G 组真 CLI，验收第 4 条「本机当前应是黄」的实证）：
`status=yellow message=找到了 C:/Users/辰辰洋洋/AppData/Roaming/npm/lark-cli，但还没登录／日历读不到。`

## 五、变异自证（打掉一处 ⇒ 必红；还原 ⇒ 必绿）

**产物级变异**（理由同技能侧：并行席在途，源码级有被代提交带走的风险；`.scratch/t764/mutation-plugin.mjs`）。

| 变异 | 红读数 | 还原读数 |
| --- | --- | --- |
| 把「库文件名」那一行的 `readonly` 打掉（`dist/settings.js`） | `exit=1 tests=13 pass=11 fail=2` | 逐字节拷回 ＋ `tsc -b` ⇒ `exit=0 tests=13 pass=13 fail=0` |

## 六、结构纪律

- 样式表零新增（跨包锁第 ⑤ 条）：状态区复用既有样式项（`label`／`hint`／`info`／`muted`／`okText`／`error`／`bar`／`btn`／`rows`），黄档用 `muted`——六家同形面一字未动，锁 5／5 为证。
- 行数：`src/client.ts` 565 → 735（＋170，状态区＋只读分支）；本包无行数告警线数字（无 `AGENTS.md`），
  如实记、不拆分（状态区是票面要的新功能，不是既有膨胀）。
- 交付对账：`git diff --stat -- packages/plugin-schedule-ilife` 实得件与第一节清单逐行一致，偏差为零。

## 七、未做项

1. **发版与装机**：`dsh-schedule-ilife` 要抬版本、发布、装进 profile；那是 #755。
2. **真机点屏**：同技能侧；面板行为已用真产物量过，真机那一跳归维护者。
3. **两段填的面板侧拆分**：#761 连带第 6 条的 8 秒／30 秒两段，在技能侧落成同一组超时数字
   （体检快段 8 秒／慢段 30 秒，见技能侧证据）；面板侧是单次 `config.check` 取数＋「重新检测」按钮，
   未另开快慢两个端点——端点要加就动 `bridge`＋宿主注册，超出本票交付物路径，留待维护者定夺。
