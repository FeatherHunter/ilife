# #744 证据 · 作息管家：目录行改接应用内文件夹浏览器

判定：**PASS**（客户端类型检查 exit 0、束重建 exit 0、本包套件 37 条全绿；真机点屏那一跳列在未做项）

形状来源：packages/plugin-chef（本票第一家接通，本家照它的四处逐字复制）。
共用件与全局判据见 docs/plugins/plugin-manager/744-证据.md（本件只记本家读数，不重复）。

## 改了什么（逐件一句）

| 件 | 改了什么 |
| --- | --- |
| packages/plugin-schedule-ilife/src/client.ts | 四处：① 加两条共用件 import（dsh-life-pack/directory-browser 与 …-ui）；② eadPickAnswer／pickerModeOf 改经共用件同名转出（本地那五段重复实现删掉，只留 pickDirectory 五行）；③ Row 的目录按钮改三态（
ative「选择文件夹…」／rowse「浏览…」／
one 不画）；④ 面板加应用内浏览那条路（createDirectoryRowBrowser ＋ DirectoryBrowserFromRow 弹层），登记点改成带状态的槽包装 |
| packages/plugin-schedule-ilife/test/config-surface-696.test.mjs | 目录行断言改吃新入口形状（rowser{mode,onOpen}）；按钮文案断言由「选择文件夹」改「浏览」（只有 browse 那一档时的正确写法） |
| packages/plugin-schedule-ilife/src/settings.ts | **一字未动**（control:'directory' 那一档是 #736 定的） |

## 机器读数

| 命令 | exit | 摘要 |
| --- | --- | --- |
| pnpm --filter dsh-schedule-ilife exec tsc -p tsconfig.client.json | 0 | 客户端类型检查通过 |
| pnpm --filter dsh-schedule-ilife run build:client | 0 | 束重建通过 |
| 
ode --test packages/plugin-schedule-ilife/test/config-surface-696.test.mjs | 0 | **pass 37 / fail 0** |
| 束文案核对 | — | 「选择文件夹／浏览／上一级／新建文件夹」四条齐（真产物读数） |

## 未做项

真机点一次本家设置页的目录行按钮：看「浏览…」开出的自绘浏览器能否进目录、新建文件夹、选定后回填该行且保存后仍在。本席无 GUI 点击能力，归维护者。
