# t696 证据：#696 其余四家的设置页 · 作息件

**票**：[#696 其余四家的设置页：照卡路里样板铺开（分级呈现）](https://github.com/FeatherHunter/ilife/issues/696)（wayfinder:task，属图 [#671 爱生活设置面](https://github.com/FeatherHunter/ilife/issues/671)）

**本件范围**：`packages/plugin-schedule-ilife`（件名逐字；四家同票，另三家的证据件各落自家目录）。提交 `ae287c63`（实施）＋ `bfd78cac`（client connection 声明）。

口径出处：`#675` 解决评论（配置存与生效）、`#692` 产出（清单权威）、`#676`／`#677` 两个样板件；技能侧配置面由 `#695` 落地（`skill-schedule/src/config.ts` 的 `SCHEDULE_CONFIG_DEFAULTS` 与 `src/cli/config.ts` 的 `CONFIG_KEYS`），本件只镜像、不另造。

## 一、交付物（逐文件一句）

| 文件 | 动作 | 一句话 |
| --- | --- | --- |
| `packages/plugin-schedule-ilife/src/contract.ts` | 新建 | 本包**第一份端口**：通道 `/ilife-schedule-ilife` ＋ 三个配置端点 ＋ 载荷校验 ＋ `ConfigSurfaceReply`；零 node |
| `src/settings.ts` | 改 | 只读行表换成 **5 行真配置行表**（常用 3 ＋ 高级 2），键＝`SCHEDULE_CONFIG_DEFAULTS` 的投影；零 node |
| `src/bridge.ts` | 改 | 三个配置 key 的镜像 ＋ 三个薄转调；`spawn` 补 `resolveNodeBin()`／`env`／20 秒超时（原先不传 `env`、不给 `ELECTRON_RUN_AS_NODE`） |
| `src/index.ts` | 改 | `inject` 加 `connection`／`webServer`，注册 `/api/ilife-schedule-ilife` 路由并按端点分发 |
| `src/dsh-ctx.ts` | 改 | 补 connection 两侧 type-only 镜像，出处写进件头 |
| `src/client.ts` | 改 | 静态只读行 → **真配置页**（三态／分级／保存／重置／人话报错） |
| `test/config-surface-696.test.mjs` | 新建 | 七组判据 25 条用例（含端到端经真 CLI 的读／写／重置） |
| `tsconfig.client.json` | 改 | include 补 `src/contract.ts`（与大厨同集） |
| `package.json` | 改 | `dsh.client.inject` 补 `@deepseek-ai/dsh-client-connection`（照 #676 卡路里样板） |

**没碰**：`packages/skill-*`（#695 有别的席位在途；只 spawn 现成的 `dist/cli/cmd_read.js`）、别的 `packages/plugin-*`、根 `test/`、`pnpm-lock.yaml`。

## 二、机器读数（只列实际跑过的，全部持锁）

| # | 命令 | exit | 摘要 |
| --- | --- | --- | --- |
| 1 | `tsc -b packages/plugin-schedule-ilife` | 0 | 宿主侧编译通过 |
| 2 | `tsc -p packages/plugin-schedule-ilife/tsconfig.client.json` | 0 | browser 产物侧类型门通过 |
| 3 | `tsdown`（包目录内） | 0 | `dist/client.js` 19.19 kB（含通道路由串与「配置读取中」） |
| 4 | `node --test …/test/config-surface-696.test.mjs` | 0 | **tests 25 / pass 25 / fail 0** |
| 5 | `node --test …/test/{config-surface-696,smoke,skills-provider}.test.mjs` | 0 | **tests 43 / pass 43 / fail 0** |
| 6 | `node tooling/check-boundaries.mjs` | 0 | `boundaries: PASS` |
| 7 | `node --test test/plugin-p10-boundaries.test.mjs` | 0 | **tests 13 / pass 13 / fail 0** |
| 8 | 渲染台 `--panels plugin-schedule-ilife --check`（我方复跑，见第四节） | 0 | **4 页全绿** |
| 9 | 变异自证（`settings.ts:53` 的 `db.name`→`db.names`，重建后跑本票用例） | — | `MUTANT-RED fail=4 exit=1` ／ `RESTORED-GREEN fail=0 exit=0` |

## 三、真机三连读数（票面验收第 1 条）

见「四家汇总」件（`docs/plugins/plugin-chef/t696-四家汇总-证据.md`）。

## 四、渲染台读数（我方复跑，独立于实施席）

```
PAGE plugin-schedule-ilife 行数=5 常用=3 宽=360/720
PASS plugin-schedule-ilife-default-360.html  行=5 overflowX=0 调用=["config.get","config.save","config.get","config.reset","config.get"] 改='db.dir'→"渲染台探针值"
PASS plugin-schedule-ilife-expanded-360.html 同上
PASS plugin-schedule-ilife-default-720.html  同上
PASS plugin-schedule-ilife-expanded-720.html 同上
设置页渲染台自检：4 页全绿（exit 0）
```

判据同大厨件那张表（行数＝5、常用＝3、高级默认收起、无横向溢出、三按钮在位、改一项后值进 `config.save` 载荷、点重置送出 `config.reset`、正文与按钮无干活入口）。

## 五、逐条对验收

| 票面验收 | 读数 |
| --- | --- |
| 保存后重启宿主仍在（三次读数） | 汇总件第三节 |
| 「重置为默认」＋重置前 `.bak` | 用例 E 组；渲染台 `resetDispatched` |
| 页里没有干活入口 | 用例 F 组（源码级）＋ 渲染台 `noWorkEntry` |
| `vision_router` 逐页打分 ≥90 | 汇总件第四节 |
| 两条边界门不红 | 读数 #6、#7 |

## 六、结构纪律（必报五步）

- **第一步影响清单**由编排者按「照大厨样板逐家铺」的写集下发；**实际碰到**的路径与声明逐行一致（8 件），偏差为零。
- **第四步超线报警**：**已超线，需要根据规则进行重构。** `src/client.ts` **354 LF**（本仓插件包沿用的兄弟口径 350）。超因与拆法同居家件：整份是设置页模板（样式表 ＋ 行控件 ＋ 三态组件），本票口径要求「照大厨那份逐段同形」；拆法＝`configRpc`／`humanizeConfigFailure`／`toDraft`／`fromDraft` 四个纯函数切 `src/config-rpc.ts`（约 70 LF）。**本次先不拆**：四家同形件已各落一份，统一拆等本图收尾裁定是否提取公共层（见 `docs/plugins/plugin-chef/t696-四家同形清单.md`）。
- 一处**刻意的同形偏差**：页签注册保留本包原有的 `order: SLOT_ORDER`（大厨 client 未传、记账传了），符合「保持原注册参数不变」。

## 七、遗留

1. 四家 `client.ts` 的同形版式是否提到公共层——攒到本图收尾统一裁，清单见 `docs/plugins/plugin-chef/t696-四家同形清单.md`。
2. 技能侧 `schedule.config.check` 一类「配置体检」端点在 `#706` 在途，不属本票范围。
