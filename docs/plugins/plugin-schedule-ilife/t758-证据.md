# #758 证据短条 · 作息管家（3 行：可改 1＋只读 2）＋飞书 CLI 状态行

完整读数在样板件 `docs/plugins/plugin-bill-ilife/t758-证据.md`；本家那一行：

```
PASS plugin-schedule-ilife 行=3 可改=[db.dir] db.dir=<dataDir> db.name=<resolved.dbFile> html.dir=<resolved.htmlDir>
```

定稿 #761：5 行收成 3 行（可改 1：数据目录；只读 2）＋「飞书 CLI」状态行。
本家无只读目录行，跨包锁第 ⑦ 条断言名单为空。
