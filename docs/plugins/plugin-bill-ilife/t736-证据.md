# #736 证据 · dsh-bill-ilife：设置页目录行接系统文件夹选择器

判定：**PASS**（本地可判的判据全绿；「真机上点开系统对话框」那一跳按票面 Agent Brief 由维护者点屏）

## 改了什么

| 件 | 改了什么 |
| --- | --- |
| `src/settings.ts` | `ConfigControl` 加第四档 `'directory'`；`db.dir`／`backup.dir` 两行改档（其余六行一字未动） |
| `src/dsh-ctx.ts` | 新增 `REMOTE_DIRECTORY_PICKER`（服务名）与 `DirectoryPickerFace`（本包只用 `pick`） |
| `src/client.ts` | 软依赖取命名空间（`ctx.get`，**没写进 inject**）＋ 目录行的「选择文件夹…」按钮 ＋ 三态归一（选中／取消／供不了）；`Row` 改为导出供用例直调 |
| `test/config-surface-677.test.mjs` | 新增 H 组七条（档位／缺席不画按钮／按钮链路／取消不动／被拒给人话／归一三态／inject 未变） |
| `test/helpers/client-bundle.mjs` | **新建共用件**：真产物 `dist/client.js` 的 loader 加载器（六家共用，避免六份拷贝） |
| `docs/agents/dsh-client-contract.md` | 新增 §13：目录选择器的出处、软依赖口径与三条禁令 |

## 机器读数（门禁运行，逐条对账）

GATE-RUN runId=2d8f7d4c-bdc1-4b72-a812-f8a4e182fe1e cmd=node tooling/run-locked.mjs --ticket 736 -- node node_modules/typescript/bin/tsc -b packages/plugin-bill-ilife
GATE-RUN runId=28302d66-af4d-4882-a598-7b18a6726ccc cmd=node tooling/run-locked.mjs --ticket 736 -- node node_modules/typescript/bin/tsc -p packages/plugin-bill-ilife/tsconfig.client.json
GATE-RUN runId=b6dd4e84-7900-4676-a5b5-0725f2db5c39 cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-bill-ilife run build:client
GATE-RUN runId=374baa6e-a2b3-4122-bf75-92fc38c7825e cmd=node tooling/run-locked.mjs --ticket 736 -- node --test packages/plugin-bill-ilife/test/config-surface-677.test.mjs

四条全 exit=0。最后一条是**还原之后**那次套件读数：`pass 31／fail 0`（其中 H 组七条全绿）；
前三条分别是宿主侧编译、客户端程序类型检查、client 束重建（`dist/client.js` 26.21 kB）。

## 变异自证（红／还原一致两行）

- 变异：`src/client.ts` 里 `resolveDirectoryPicker` 的
  `return isDirectoryPicker(raw) ? raw : null;` 改成
  `return isDirectoryPicker(raw) ? raw : ({ pick: async () => null } as DirectoryPickerFace);`
  - 重建（runId=6d6d5bff-5c7f-40ef-bb7a-e0f90f1e3924，exit=0——这是**语义**破坏，不是语法破坏）
  - 套件：**runId=04516dcb-0d57-40b4-9448-8507d8a6e582，exit=1，fail 1**
  - 红在哪一条：`命名空间拿不到 ⇒ 没有入口（软依赖；守卫拒绝也当没有）`（断言「拿不到就不给入口：() => undefined」）
- 还原：编辑器改回原样，SHA256 与变异前**逐字节相同**
  （`9faa8a109e2e235fea4bb97b77ae67f28f3422951523cdef272f437d79156126`）；
  重建（runId=b6dd4e84…）＋ 套件（runId=374baa6e…）⇒ **exit=0，pass 31／fail 0**。

## 未做项（如实记）

- **真机点屏那一跳没做**：需要人在爱生活面板点一次、看操作系统文件夹对话框弹不弹、选完值是否落进那一行。
  判据与失败处置写在 #736 的 Agent Brief「外部确认出口」一节（本席无 GUI 点击能力）。
- 本机 PowerShell 是 **5.1**：`Set-Content`／`Out-File -Encoding utf8` 会写 BOM。第一轮变异曾因此把
  `src/client.ts` 写出 BOM、重建 exit 1（假红、且套件读到旧产物仍绿）。已改用编辑器改写并**重做**变异，
  上面给的是重做后的读数；那条弯路留在这里当路标：**改源码一律用编辑器，不用 PS 的写作 cmdlet**。
