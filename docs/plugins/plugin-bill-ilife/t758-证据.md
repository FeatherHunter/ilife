# #758 证据 · 六家同形面收口：跨包锁 ＋ 逐家端到端读数

判定：**PASS**（跨包锁 8／8 绿：既有 5 条一字未动＋新增 3 条；六家端到端读数 6／6 绿；
变异自证「把只读改回可改必红／还原必绿」跑齐）。

本件是六家的收口样板件：完整读数在此；其余五家各留一张短条
（`docs/plugins/plugin-<件名>/t758-证据.md`）指回这里。

## 一、交付了什么（逐件一句）

| 件 | 改了什么 |
| --- | --- |
| `test/panel-copy-743.test.mjs` | **只加断言**：新增 ⑥⑦⑧ 三条（见第二节）；既有 ①–⑤ 一字未动；文件头注释「锁五件事」改成「锁八件事」 |
| `docs/plugins/plugin-bill-ilife/t758-六家端到端读数.mjs`（新） | 六家读数脚本：临时家目录 → 真技能 `*.config.read` → 真桥 `readConfigSurface` → 真 `toDraft` → 真 `Row`，按定稿表逐行验（一家一行 PASS） |
| `docs/plugins/plugin-<六家>/t758-证据.md` | 本证据件（样板在本件）＋五张短条 |
| `docs/plugins/plugin-bill-ilife/t758-门禁运行记录.md` | 门禁对账导出件（§2.3 第 4 条） |

技能侧一行未动；六家 `src/settings.ts`／`src/client.ts` 一行未动；锁里既有断言一字未动。

## 二、跨包锁新增的三条（定稿出处逐条点名）

- ⑥ 每家可改的行只有定稿允许的那几行：记账 `db.dir`（#747）／卡路里 `db.dir`、`photos.dir`、`xunji.key`（#748）／备忘 `db.dir`、`media.dir`（#759）／作息 `db.dir`（#761）／居家 `db.dir`（#793）／大厨 `db.dir`（#795）；行数同步钉死 6／11／4／3／5／4；可改之外每一行都须是只读（没有第三态）。
- ⑦ 只读目录行的浏览按钮存在且不可点击：记账 `backup.dir`／卡路里 `xunji.stateDir`／居家 `backup.dir`各画一枚 disabled 按钮＋disabled 输入框＋不接 `onChange`；备忘／作息／大厨无只读目录行，名单断言为空（加一行只读目录不补按钮即红）。
- ⑧ 控件文案不含省略号：六份 `client.ts` 无 `…`；六家行标题与 hint 无 `…` 与 `...`；目录行两档按钮文案逐字是「选择文件夹」／「浏览」。

## 三、六家端到端读数（真链路，一家一行）

```
PASS plugin-bill-ilife 行=6 可改=[db.dir] db.dir=<dataDir> db.name=<resolved.dbFile> db.goals=<resolved.goalsFile> html.dir=<resolved.htmlDir> backup.dir=<resolved.backupDir> backup.stem=<resolved.backupSample>
PASS plugin-calorie 行=11 可改=[db.dir、photos.dir、xunji.key] db.dir=<dataDir> db.name=<resolved.dbFile> html.dir=<resolved.htmlDir> photos.dir=<resolved.photosDir> xunji.key="" photos.gifs=<resolved.gifsDir> xunji.stateDir=<resolved.stateDir> xunji.catalog=<resolved.catalog> xunji.backfillDays="1" land.xunjiSeconds="300" land.landSeconds="60"
PASS plugin-memo-ilife 行=4 可改=[db.dir、media.dir] db.dir=<dataDir> db.name=<resolved.dbFile> html.dir=<resolved.htmlDir> media.dir=<resolved.mediaDir>
PASS plugin-schedule-ilife 行=3 可改=[db.dir] db.dir=<dataDir> db.name=<resolved.dbFile> html.dir=<resolved.htmlDir>
PASS plugin-home-ilife 行=5 可改=[db.dir] db.dir=<dataDir> db.name=<resolved.dbFile> html.dir=<resolved.htmlDir> backup.dir=<resolved.backupDir> key.file=<resolved.keyFile>
PASS plugin-chef 行=4 可改=[db.dir] db.dir=<dataDir> db.name=<resolved.dbFile> html.dir=<resolved.htmlDir> html.sceneDir=<resolved.sceneDir>
六家端到端读数：6 家全绿（exit 0）
```

每行读的是：真技能回执的 `dataDir` 为绝对路径；只读行显示值逐字等于回执 `resolved` 格（路径格逐格验绝对路径；`backupSample` 验示例名；数字类三行显示生效数字）；可改目录行显示生效绝对路径；`Row` 真渲染的输入框与按钮 disabled 态与定稿一致。

## 四、机器读数（门禁运行，逐条对账）

GATE-RUN runId=1a7d1b15-8131-4281-9b1c-500ba70e686d cmd=node --test test/panel-copy-743.test.mjs
GATE-RUN runId=821710c3-0d8a-4911-a760-bf877bade389 cmd=node docs/plugins/plugin-bill-ilife/t758-六家端到端读数.mjs
GATE-RUN runId=8e264a63-14fc-4ba6-9cc7-8801236e9adb cmd=node docs/plugins/plugin-chef/t696-设置页渲染台.mjs --check
GATE-RUN runId=14a9c1e9-9408-4b09-a96a-5d117ea2dd2f cmd=node --test test/panel-copy-743.test.mjs
GATE-RUN runId=cc5eb14b-0fb5-46ed-98ec-defad4fb5de2 cmd=node --test test/panel-copy-743.test.mjs

| runId（尾段） | 命令 | exit | 摘要 |
| --- | --- | --- | --- |
| `1a7d1b15` | 跨包锁验收跑 | 0 | `tests 8／pass 8／fail 0`（①–⑧全绿） |
| `821710c3` | 六家端到端读数 | 0 | 6 家 PASS（见第三节） |
| `8e264a63` | t696 渲染台 `--check`（六家 × 两态 × 两宽＝24 页） | 1 | 18 页 PASS；6 页 FAIL 挂在同一条旧判据（见第五节，非产品缺陷） |
| `14a9c1e9` | 变异红（见第五节） | 1 | `pass 6／fail 2`，红在 ⑥⑦ |
| `cc5eb14b` | 变异还原后重跑 | 0 | `tests 8／pass 8／fail 0` |

对账导出件：`docs/plugins/plugin-bill-ilife/t758-门禁运行记录.md`。

## 五、变异自证与渲染台旧判据说明

**变异自证**（产物级，`dist/` 在忽略清单里，复原即拷回）：把记账 `dist/settings.js` 里
`backup.dir` 的 `readonly: true` 改成 `false`（＝把一行从只读改回可改）→ 锁 `fail 2`，
红在 ⑥（可改名单多出一行）与 ⑦（只读目录名单少了一行）；逐字节拷回 → 锁 8／8 绿。
红绿由只读标记造成，不是写死读数。

**t696 渲染台 6 页 FAIL 的说明**（如实记，非本票引起，不在本票写集，故不动它）：
FAIL 全挂在 `advancedCollapsedByDefault` 这一条旧判据上——它要求默认收起页有含输入框的
收起态 `<details>`。收窄后备忘／作息／大厨三家的高级组**按定稿就是空的**
（备忘 #760「高级组清空」／作息 #764「高级组已空，空组不渲染」／大厨 #796「高级组是空的」），
client 不渲染空组，判据的 `details !== null` 一支即红（3 家 × 两宽＝6 页）。
这 6 页其余 11 项检查（行数／调用序列／改后保存进载荷／重置／零横向溢出等）全绿，
6 家的 expanded 页 12 页全绿，记账／卡路里／居家的 default 页 6 页全绿。
产品行为符合定稿；是 rig 的判据没跟进「高级组可空」的新形状——留待后人裁定 rig 是否跟进。

## 六、未做项

1. **真机点屏**：本席无界面点击能力；面板行为已用真产物量过（跨包锁 8 条＋六家端到端读数＋渲染台 18 页 PASS），真机那一跳按 #736／#743 的先例归维护者。
2. **发版与装机**：六个插件的抬版本、发布、装进 profile 是 #755（本图末段收口），不在本票。
