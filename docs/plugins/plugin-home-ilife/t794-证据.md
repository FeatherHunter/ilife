# t794 证据（插件侧：居家设置页收窄成 1 可改 ＋ 4 只读）

票：[#794](https://github.com/FeatherHunter/ilife/issues/794)，样板 [#749](https://github.com/FeatherHunter/ilife/issues/749)。

## 一、交付了什么

| 件 | 改了什么 |
|---|---|
| `src/settings.ts` | 行表 6→5：可改 1（数据目录，`prefillFrom: dataDir`）＋ 只读 4（库文件名／HTML产物目录／备份目录／主密钥文件，各标 `resolveFrom`）；HTML标题改「HTML 产物目录」；删 `files.*` 两行；新增 `ResolvedField`／`readonly`／`resolveFrom` |
| `src/contract.ts` | 新增 `ResolvedPaths`（`dbDir/dbFile/htmlDir/backupDir/keyFile`，与技能侧 `HomeResolvedPaths` 逐字同形）＋回执可选 `resolved`（旧技能缺席即空串展示） |
| `src/client.ts` | 只读行显示 `resolveFrom` 那一格；只读行不进保存（`fromDraft` 跳过）；只读控件 `disabled` 且不接 `onChange`；只读目录行浏览按钮保留但不可点；头部数据目录显示 `resolved.dbDir`；去省略号 4 处（选择文件夹／浏览／配置读取中／处理中／正在读取） |
| `src/index.ts` | 类型转出加 `ResolvedField`／`ResolvedPaths` |
| `test/t794-设置页收窄.test.mjs`（新建，14条） | 行表形状／只读值对账／旧技能空串／无路径拼接／不进保存／只读渲染三态／零省略号 |
| `test/config-surface-696.test.mjs` | 旧断言跟新形状走（5行／常用3／key.file默认／写合并期望；改前红、改后绿） |

## 二、机器读数（`run-locked`，标识见技能侧 `t794-门禁运行记录.md`）

- 编译：`tsc -b packages/plugin-home-ilife` exit 0（`runId=89e81f8f-ec5b-4c56-bd7b-14e2937626a5`；hint 修复后复编 `runId=87c4e55e-c333-4c10-b4bd-fadc2cb5acbe`；变异还原后复编 `runId=5740cbaf-9219-4baa-bcda-518c1f6955e5`）。
- 客户端产物：`build:client`（tsdown）exit 0（`runId=b525a057-68af-4763-bb68-5b73a31dda98`，`dist/client.js` 64.10 kB）。
- 新用例＋回归：`tests 48／pass 48／fail 0`（t794 14 条 ＋ 696 全组，`runId=f6103af7-4ea7-4590-b94b-217a6909f703` exit 0；另有合跑 `runId=65ae220a` 与跨包合跑 `runId=96ac4e33` 全绿）。
- 跨包锁：`test/panel-copy-743.test.mjs` 通过（合跑 `runId=96ac4e33` exit 0；五条全绿，含样式表逐项同形——本票未改样式表）。
- 变异自证（源码级）：`db.name` 去掉 `readonly` ⇒ `tests 14／pass 10／fail 4`（`runId=41d4fd22-ef50-49f6-8448-4a93095c9c5c` exit 1）；还原 ⇒ `48／48／0`，源码无 `MUT-794` 残留。
- 中间红：`runId=52ec6151` exit 1——`key.file` 的 hint 带违禁词「落点」，改「位置」后绿（锁②的原文指认见该次输出）。

## 三、未做项

- 发版与装机（#755）；真机点屏（归维护者；面板行为已用真产物量过：渲染束组件级用例 14 条 ＋ 真 CLI 回执对账）。
