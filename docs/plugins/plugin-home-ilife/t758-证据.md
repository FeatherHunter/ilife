# #758 证据短条 · 居家管家（5 行：可改 1＋只读 4）

完整读数在样板件 `docs/plugins/plugin-bill-ilife/t758-证据.md`；本家那一行：

```
PASS plugin-home-ilife 行=5 可改=[db.dir] db.dir=<dataDir> db.name=<resolved.dbFile> html.dir=<resolved.htmlDir> backup.dir=<resolved.backupDir> key.file=<resolved.keyFile>
```

定稿 #793：6 行收成 5 行（可改 1：数据目录；只读 4，含主密钥文件行）。
只读目录行 `backup.dir` 的浏览按钮保留且不可点击（跨包锁第 ⑦ 条）。
