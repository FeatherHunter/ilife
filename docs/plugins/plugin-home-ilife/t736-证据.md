# #736 证据 · dsh-home-ilife：设置页目录行接系统文件夹选择器

判定：**PASS**（本地可判判据全绿：宿主编译 exit 0、客户端束重重建 exit 0、本包套件 pass 32／fail 0
（含新 H 组七条）、边界门 PASS；变异自证红／还原一致两行见下。
「真机点开系统文件夹对话框」那一跳需要人点屏，本席无 GUI 点击能力，列在未做项）

## 改了什么

| 件 | 改了什么 |
| --- | --- |
| `src/settings.ts` | `ConfigControl` 加第四档 `'directory'`；只有 `db.dir`／`backup.dir` 两行改档。`db.name`／`html.dir`／`files.help`／`files.lookup` 四行一字未动 |
| `src/dsh-ctx.ts` | 末尾新增 `REMOTE_DIRECTORY_PICKER`（服务名）与 `DirectoryPickerFace`（本包只用 `pick` 一格） |
| `src/client.ts` | 软依赖取命名空间（`ctx.get`，**没写进 inject**）＋ 目录行的「选择文件夹…」按钮 ＋ 三态归一（选中／取消／供不了）＋ 被拒即收起入口；`Row` 改为**导出**供用例直调 |
| `test/config-surface-696.test.mjs` | 新增 H 组七条（档位键集／缺席不画按钮／按钮链路／取消不动／被拒给人话／归一三态／inject 未变）；共用加载件用仓根 `../../../test/helpers/client-bundle.mjs`，未复制第六份 |

未动：`inject` 仍是 `['slots','connection']`；`test/helpers/client-bundle.mjs`、`docs/agents/dsh-client-contract.md`、
`package.json` 与其它包一律未碰。

## 机器读数（门禁运行，逐条对账；runId 抄自 `.scratch/locks/gate-runs.log`）

GATE-RUN runId=6e7e284c-ab5a-43ff-83c0-51c3c7bbf650 cmd=node tooling/run-locked.mjs --ticket 736 -- node node_modules/typescript/bin/tsc -b packages/plugin-home-ilife
GATE-RUN runId=71b48b83-442f-436c-8d93-af9dc473d2de cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-home-ilife run build:client
GATE-RUN runId=6af51ba0-f7a5-45c7-a771-eccd97c1f802 cmd=node tooling/run-locked.mjs --ticket 736 -- node --test packages/plugin-home-ilife/test/config-surface-696.test.mjs
GATE-RUN runId=58a07c4a-5373-4a7e-a1e1-9d9602569f64 cmd=node tooling/run-locked.mjs --ticket 736 -- node tooling/check-boundaries.mjs

四条全 **exit=0**：
- 宿主编译（`tsc -b`，exit 0）；
- client 束重建 `dist/client.js` 23.73 kB（`tsc -b` 不产它，故必须单跑）；
- 本包套件 **pass 32／fail 0**（H 组七条全绿）；
- 边界门 `boundaries: PASS`。

## 变异自证（红／还原一致两行）

- 变异：`src/client.ts` 的 `resolveDirectoryPicker` 里
  `return isDirectoryPicker(raw) ? raw : null;` 改成
  `return isDirectoryPicker(raw) ? raw : ({ pick: async () => null } as DirectoryPickerFace);`
  - 重建：GATE-RUN runId=4544b144-6a45-4bb3-9dba-7fe194be9353 cmd=... pnpm --filter dsh-home-ilife run build:client ⇒ **exit=0**（语义破坏，不是语法破坏）
  - 套件：GATE-RUN runId=9267d3df-6951-440f-817f-6973226f03a2 cmd=... node --test …config-surface-696.test.mjs ⇒ **exit=1，pass 31／fail 1**
  - 红在哪一条：`H 目录行与系统文件夹选择器入口（#736）` › `命名空间拿不到 ⇒ 没有入口（软依赖；守卫拒绝也当没有）`（断到「拿不到就不给入口：() => undefined」）
- 还原：用**编辑器**把那一行改回原样
  - `RESTORED_CLIENT_SHA256=13acbc116d98c4640b61acf8b956a122ac45d95711d25ea03c529ea06d11c2f7`，
    与变异前的基线 `BASELINE_CLIENT_SHA256=13acbc116d98c4640b61acf8b956a122ac45d95711d25ea03c529ea06d11c2f7` **逐字节相同**
    （21458 字节，首三字节 47,42,42＝`/**`，无 BOM）
  - 重建：GATE-RUN runId=74b95552-02ad-433c-8c97-c7e2371f762b cmd=... pnpm --filter dsh-home-ilife run build:client ⇒ **exit=0**
  - 套件：GATE-RUN runId=55c20997-83f7-49c1-90a3-c765728b762b cmd=... node --test …config-surface-696.test.mjs ⇒ **exit=0，pass 32／fail 0**

日志：`.scratch/t736/logs/home-host-build.log`、`home-client-build.log`、`home-test.log`、`home-mut-build.log`、
`home-mut-test.log`、`home-restore-build.log`、`home-restore-test.log`、`home-boundaries.log`。

## 未做项（如实记）

- **真机点屏那一跳没做**：需要人在爱生活面板点一次「选择文件夹…」，看操作系统文件夹对话框弹不弹、选完的绝对路径是否落进那一行；
  判据与失败处置见 #736 的 Agent Brief「外部确认出口」一节（本席无 GUI 点击能力）。
- 收工前的变异改写与还原一律用编辑器工具；本机 PowerShell 的 `Set-Content`／`Out-File -Encoding utf8` 会写 BOM
  （#736 记账那席已因此假红一轮），故全程未用写作 cmdlet 碰源码。


## 超线报警（必报五步·第四步）

**已超线，需要根据规则进行重构。** src/client.ts 本票改前 357 行 → 现在 469 行（**LF 口径**；本仓插件包沿用的兄弟口径 350）。更正：本条先前那对数字量的是「不含空行」的计数，已作废；全票统一按 LF 口径，见 docs/plugins/plugin-bill-ilife/t736-证据.md 的订正。）
超因、为什么不这次拆、以及拆法，见 docs/plugins/plugin-bill-ilife/t736-证据.md 的「超线报警」一节（六家同形，全票在那里记一次）。
