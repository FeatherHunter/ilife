# #736 证据 · dsh-chef：设置页目录行接系统文件夹选择器

判定：**PASS**（本地可判的判据全绿：宿主编译 exit 0、客户端束重建 exit 0、本包套件 32/32（含新 H 组七条）、
变异自证「红 → 还原一致」、边界门 PASS。「真机上点开系统对话框」那一跳要人点屏，见「未做项」）

形状来源：`packages/plugin-bill-ilife`（同票样板，已验过）。本件逐段照抄，只换本包的键、组件名与 props 名。
本包的目录行恰一行：`db.dir`。

## 改了什么（逐件一句）

| 件 | 改了什么 |
| --- | --- |
| `packages/plugin-chef/src/settings.ts` | `ConfigControl` 加第四档 `'directory'`；**只**把 `db.dir` 改档（`db.name`／`html.dir`／`files.help`／`files.lookup` 四行一字未动） |
| `packages/plugin-chef/src/dsh-ctx.ts` | 末尾补两格：`REMOTE_DIRECTORY_PICKER`（服务名）＋ `DirectoryPickerFace`（本包只用 `pick`） |
| `packages/plugin-chef/src/client.ts` | 软依赖取命名空间（`ctx.get` 现取，**没写进 `inject`**）＋ `PickOutcome`／`isDirectoryPicker`／`resolveDirectoryPicker`／`pickDirectory`／`createBrowseHandler` ＋ 目录行的「选择文件夹…」按钮（`Row` 改为**导出**，并按本包命名传 `onBrowse` 进 `ChefConfig`） |
| `packages/plugin-chef/test/config-surface-696.test.mjs` | 新增 H 组七条（档位键集／缺席不画按钮／按钮链路／取消不动／被拒给人话／归一三态／`inject` 未变）；共用加载件走 `test/helpers/client-bundle.mjs`（不复制第六份） |
| `docs/plugins/plugin-chef/t736-证据.md` | 本证据件 |

`inject` 保持 `['slots','connection']` 不变（写进去＝硬依赖，提供方缺席时整包被停靠，见 cookbook §13）。

## 机器读数（门禁运行，逐条对账）

GATE-RUN runId=e73f0d63-d7b2-4c59-a45e-8d26d9109109 cmd=node tooling/run-locked.mjs --ticket 736 -- node node_modules/typescript/bin/tsc -b packages/plugin-chef
GATE-RUN runId=58f4682b-f7e8-487c-beb5-3bc416f6d832 cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-chef run build:client
GATE-RUN runId=47a8c32d-b523-4447-9c19-0ae50b80c0fb cmd=node tooling/run-locked.mjs --ticket 736 -- node --test packages/plugin-chef/test/config-surface-696.test.mjs
GATE-RUN runId=0f8da4a2-6f16-41d2-9b45-7a5ef0a39bb8 cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-chef run build:client
- 变异态套件（**读数，不是门禁声明**：这一次 exit=1，按协议 §2.4.3「只领 exit=0 的条目」故不写成 GATE-RUN）：runId=e2dab56b-cbc3-4d6b-9750-59a19352517b，pass 31／fail 1，红在「命名空间拿不到 ⇒ 没有入口」
GATE-RUN runId=8d966e9b-c456-458f-8260-a40771548eb3 cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-chef run build:client
GATE-RUN runId=ed66a7ec-6c85-459e-91cb-a11571f5bf3d cmd=node tooling/run-locked.mjs --ticket 736 -- node --test packages/plugin-chef/test/config-surface-696.test.mjs
GATE-RUN runId=ad3552e8-9746-4d2d-9253-bc5f1d126a44 cmd=node tooling/run-locked.mjs --ticket 736 -- node tooling/check-boundaries.mjs

| runId（尾段） | 命令 | exit | 摘要 |
| --- | --- | --- | --- |
| `e73f0d63` | `tsc -b packages/plugin-chef` | 0 | 宿主侧编译通过 |
| `58f4682b` | `pnpm --filter dsh-chef run build:client` | 0 | client 束重建（`dist/client.js` 23.64 kB） |
| `47a8c32d` | `node --test …config-surface-696.test.mjs` | 0 | **tests 32／pass 32／fail 0** |
| `ad3552e8` | `node tooling/check-boundaries.mjs` | 0 | `boundaries: PASS` |

套件条数对账：改前 25 条（t696 读数），改后 32 条＝＋H 组 7 条，其余六组一条未动、一条未红。

## 变异自证（红／还原一致两行）

- 变异：`src/client.ts` 里 `resolveDirectoryPicker` 的
  `return isDirectoryPicker(raw) ? raw : null;` 改成
  `return isDirectoryPicker(raw) ? raw : ({ pick: async () => null } as DirectoryPickerFace);`
  - 重建：runId=`0f8da4a2…`，**exit=0**（这是语义破坏、不是语法破坏）
  - 套件：runId=`e2dab56b…`，**exit=1，tests 32／pass 31／fail 1**
  - 红在哪一条：`H 目录行与系统文件夹选择器入口（#736）` › `命名空间拿不到 ⇒ 没有入口（软依赖；守卫拒绝也当没有）`
- 还原：用编辑器改回原样 ⇒ 重建 runId=`8d966e9b…` exit=0；套件 runId=`ed66a7ec…`
  **exit=0，tests 32／pass 32／fail 0**
- 还原一致性自查（`[System.Security.Cryptography.SHA256]::Create()`）：改前与还原后
  逐字节相同 `f26e4e998773bd7ea69f92b47426331bf57878ce3d831448a51bc9acf2b1da5d`，
  文件无 BOM（`EF BB BF` 判 False）。变异与还原全程只用编辑器工具，未用 `Set-Content`／`Out-File`。

## 结构纪律（必报五步）

- **第一步影响清单**（开工前已报）：`packages/plugin-chef/src/settings.ts`／`src/client.ts`／`src/dsh-ctx.ts`、
  `packages/plugin-chef/test/config-surface-696.test.mjs`、`docs/plugins/plugin-chef/t736-证据.md`——每行一个文件，零跨件。
- **第五步交付对账**：`git status --porcelain -- packages/plugin-chef docs/plugins/plugin-chef` 实得
  4 个改动件（三个 src ＋ 一个 test）＋ 本证据件，与清单逐行一致，**偏差为零**（`dist/` 产物不入库）。
- **第四步超线报警**：**已超线，需要根据规则进行重构。** `src/client.ts` 现 **472** 行（本仓插件包沿用的
  350 行告警线；本票改前 356 行，本身已在线外）。超的原因是本票按样板往这一页加「行控件 ＋ 选择器装配 ＋
  容器接线」三块。拆法（t696 证据已备，同一条）：把「行控件 ＋ 草稿换算 ＋ 报错人话」切到 `src/config-form.ts`，
  页面只留三态与版式。**这次先不拆**：本票写集与形状被票面冻结（六家逐段同形，拆法要六家一齐拆才不散形），
  单拆本包会把六家的同形面拆散；留待本图收尾统一裁。

## 未做项（如实记）

1. **真机点屏那一跳没做**：要在爱生活面板真点一次「选择文件夹…」，看操作系统对话框弹不弹、选完是否回填该行。
   本席无 GUI 点击能力，按票面归维护者。
2. **`docs/plugins/plugin-chef/t696-设置页渲染台.mjs` 没跑**：它不在本票写集内，且它会往
   `docs/plugins/plugin-chef/` 写 HTML 报告件——本票红线只许碰声明的路径，故不跑（该台的读数属 #696 的账）。
3. **兄弟用例里一条既有红（非本件）**：runId=`515dbf24…`（`node --test packages/plugin-chef/test/smoke.test.mjs
   packages/plugin-chef/test/skills-provider.test.mjs`，exit=1，tests 15／pass 14／fail 1）——红在
   `smoke.test.mjs:26` 的「单品声明总管同版本线」：`plugin-chef/package.json` 写 `dsh-life-pack: ^0.2.6`，
   而 `plugin-manager/package.json` 已是 `0.2.7`。这是版本线漂移（`package.json` 不属本票写集、本席一字未碰，
   且本件只改 `src`／`test`，改不回这条读数）；`skills-provider` 那部分全绿。归属：发版窗口那条线。
4. **真机 `ctx.get('remote.directoryPicker')` 的实际可达性未验**：本席只验了「拿不到就不出入口」这一半；
   「拿得到就用它」由 H 组用注入的假命名空间验过，剩下的最后一跳同上第 1 条。


## 超线报警（必报五步·第四步）

**已超线，需要根据规则进行重构。** src/client.ts 本票改前 356 行 → 现在 472 行（**LF 口径**；本仓插件包沿用的兄弟口径 350）。更正：本条先前那对数字量的是「不含空行」的计数，已作废；全票统一按 LF 口径，见 docs/plugins/plugin-bill-ilife/t736-证据.md 的订正。）
超因、为什么不这次拆、以及拆法，见 docs/plugins/plugin-bill-ilife/t736-证据.md 的「超线报警」一节（六家同形，全票在那里记一次）。
