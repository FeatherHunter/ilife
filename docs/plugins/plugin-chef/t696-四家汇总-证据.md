# t696 四家汇总证据：#696 其余四家的设置页

**票**：[#696 其余四家的设置页：照卡路里样板铺开（分级呈现）](https://github.com/FeatherHunter/ilife/issues/696)（wayfinder:task，属图 [#671 爱生活设置面](https://github.com/FeatherHunter/ilife/issues/671)）

本件收**四家一齐跑**的读数（门禁／真机三连／渲染台截图与逐页打分）；逐家的交付物与逐条验收各在自家证据件：`docs/plugins/plugin-chef/t696-证据.md`、`docs/plugins/plugin-home-ilife/t696-证据.md`、`docs/plugins/plugin-memo-ilife/t696-证据.md`、`docs/plugins/plugin-schedule-ilife/t696-证据.md`。

## 一、交付一览（四家 × 提交）

| 件 | 实施提交 | 附笔 | 行表 |
| --- | --- | --- | --- |
| `plugin-chef` | `e7eae10a` | `d29ff1cc`（client connection 声明） | 5 行（常用 3 ＋ 高级 2） |
| `plugin-home-ilife` | `e95e0078` | `317fc132`（同上） | 6 行（4 ＋ 2） |
| `plugin-schedule-ilife` | `ae287c63` | `bfd78cac`（同上） | 5 行（3 ＋ 2） |
| `plugin-memo-ilife` | `fd63691c`（另四件被别席 `f8957dd8` 连带提交） | — | 8 行（4 ＋ 4） |

收口附笔：`e3cda0e1`（大厨／居家／作息三家补 `SlotsRegisterOptions.channel` 镜像，解 #706 联动后的客户端编译红）。

四家合计 **24 行**，与用户 2026-09-18 批准的清单逐家吻合（5／6／8／5），键逐字等于技能侧 `*_CONFIG_DEFAULTS`（由各家用例 lockstep 断言）。

## 二、门禁（四家一齐，持锁）

| 门 | 命令 | 读数 |
| --- | --- | --- |
| 边界门 | `node tooling/check-boundaries.mjs` | `boundaries: PASS`，exit 0 |
| P10 冻结边 | `node --test test/plugin-p10-boundaries.test.mjs` | **tests 13 / pass 13 / fail 0**，exit 0 |
| 四家配置面用例 | `node --test packages/plugin-{chef,home-ilife,memo-ilife,schedule-ilife}/test/config-surface-696.test.mjs` | **tests 109 / pass 109 / fail 0**，exit 0 |
| 安装与客户端产物 | `node --test test/plugin-p10-install.test.mjs test/client-bundle-48.test.mjs` | **tests 26 / pass 26 / fail 0**，exit 0 |
| 四家客户端类型门 | `tsc -p packages/plugin-<件名>/tsconfig.client.json`（四家） | 四家 exit 0（补 `channel` 镜像之后；见第六节） |

## 三、真机三连读数（票面验收第 1 条：保存后重启宿主仍然在）

探针：`docs/plugins/plugin-chef/t696-读数.mjs`。走的就是面板按按钮那条路——插件宿主侧 `dist/bridge.js` → 技能 CLI 的三个配置 key → 盘上 YAML；「新进程再读」＝另起一个全新 node 进程再读一次（宿主重启后要面对的局面）。

```
PASS plugin-chef           改前="chef_data.db"     改后落盘=true 新进程再读="t696-chef-probe.db"     还原="chef_data.db"     文件=C:\Users\辰辰洋洋\.ilife\chef.yaml
PASS plugin-home-ilife     改前="home.db"          改后落盘=true 新进程再读="t696-home-probe.db"     还原="home.db"          文件=C:\Users\辰辰洋洋\.ilife\home.yaml
PASS plugin-memo-ilife     改前="memo.db"          改后落盘=true 新进程再读="t696-memo-probe.db"     还原="memo.db"          文件=C:\Users\辰辰洋洋\.ilife\memo.yaml
PASS plugin-schedule-ilife 改前="schedule_data.db" 改后落盘=true 新进程再读="t696-schedule-probe.db" 还原="schedule_data.db" 文件=C:\Users\辰辰洋洋\.ilife\schedule.yaml
MODE=真机（默认落点 ~/.ilife）
RESULT: PASS 4/4
```

**探针跑完把改过的那一项还原**，四份配置文件除「首次读自动落一份默认」外不留痕。

## 四、渲染台 ＋ 逐页打分（票面验收第 4 条：`vision_router` 逐页打分 ≥90）

渲染台：`docs/plugins/plugin-chef/t696-设置页渲染台.mjs --check`（物化四家真 `dist/client.js` → 渲真组件 → **真调**输入框 `onChange` 与按钮 `onClick`）。四家自检**全绿**（各家两态 × 两宽）：

```
PASS plugin-chef-default-360.html / -expanded-360 / -default-720 / -expanded-720          行=5 常用=3 overflowX=0
PASS plugin-home-ilife-default-360.html / -expanded-360 / -default-720 / -expanded-720    行=6 常用=4 overflowX=0
PASS plugin-schedule-ilife-default-360.html / -expanded-360 / -default-720 / -expanded-720 行=5 常用=3 overflowX=0
改='db.dir'→"渲染台探针值"（值真的进了 config.save 载荷）；调用序列 = config.get, config.save, config.get, config.reset, config.get
```

**逐页打分（vision_router，量表 100：层级 20／留白 20／无符号串 20／分组 15／可控性 15／适配 10）**：

| 页面 | 截图（`.dsh-vision-router/artifacts/`） | 层级 | 留白 | 无符号串 | 分组 | 可控性 | 适配 | 总分 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 大厨 · 配置 | `D--ilife-.scratch-t696-plugin-ch-8fd91cd5-shot-900x900-fullpage.png` | 18 | 17 | 18 | 13 | 15 | 10 | **91** |
| 居家 · 配置 | `D--ilife-.scratch-t696-plugin-ho-0e07c2d5-shot-900x900-fullpage.png` | 18 | 17 | 18 | 13 | 15 | 10 | **91** |
| 作息 · 配置 | `D--ilife-.scratch-t696-plugin-sc-0ce250f0-shot-900x900-fullpage.png` | 18 | 17 | 18 | 13 | 15 | 10 | **91** |
| 备忘 · 配置 | `D--ilife-.scratch-t696-plugin-me-67fa576f-shot-900x900-fullpage.png` | 18 | 17 | 18 | 13 | 14 | 10 | **90** |

扣分点（四张一致，已记账）：高级分组的小标题字重／间距／折叠控件感与上方同级项略有落差（备忘页另扣「输入框无占位提示」1 分）——**本票不再改**，理由见第六节（同批文件正被 #706 席叠改，且统一整改要等本图收尾的提取裁定）。

**边界（本台替代不了什么）**：它用 stub loader 物化真产物、跑真组件、真调回调，但用 hooks 替身渲成静态 DOM，**不过 DSH 的槽位渲染、不跑 React 自己的调和与事件系统、不点链接**。「四家页签在爱生活面板上点得到」由三处合成：总管页签条六格（`MANAGER_TABS`，边界门断言）＋ 四家各注册进 `ilife.config-tab` 槽（各家用例断言 `name`／`id`／`order`）＋ 渲染台渲出本家页面本身；**总管面板里点开本家页签的整链**属总管件（#678／#679／#706）的读数面。

**门脚本的负向对照（新增门必须有「改坏必红」）**：把大厨 `src/client.ts` 的按钮标签 `'重新读取'` 改成 `'重新读取X'` → 重建（`tsc -b` ＋ tsdown） → 跑渲染台：

```
MUTANT-RED      FAIL:threeButtons × 4 页，exit=1
RESTORED-GREEN  16 页全绿，exit=0（`src/client.ts` sha256 还原为 C9F4D4BA…D62E，与原值逐字节一致；重编后产物同步）
```

**这条对照抓出一条假绿**：`threeButtons` 原先用子串匹配（`b.indexOf('重新读取') >= 0`），标签被改一个字照样命中 ⇒ 已改成**逐字相等**（`pageButtons.includes(t)`），改后对照才红。这正是「新门必须自证」要咬的东西。

## 五、逐条对票面验收

| 票面验收 | 读数 |
| --- | --- |
| 四家页签点得到、改得动、**保存后重启宿主仍在**（改前／改后／重启后三次读数） | 第三节（真机四段读数）＋ 第四节（渲染台真调 `onChange`／`onClick`，值进 `config.save` 载荷） |
| 每页有「重置为默认」，点了能回默认，且**重置前自动留一份 `.bak`** | 各家用例 E 组：`resetConfigToDefaults()` 回执带 `backupPath` 且该文件在盘上；渲染台 `resetDispatched` |
| 页里没有任何干活入口 | 各家用例 F 组（源码级：设置页段不含取数调用与干活 key）＋ 渲染台 `noWorkEntry`（正文与按钮都不含干活词，脚注那句「只配置，不干活」除外） |
| `vision_router` 逐页打分 ≥90 | 第四节（四页；三家 91 分，备忘见备忘件） |
| 边界断言不红：`check-boundaries.mjs`、`test/plugin-p10-boundaries.test.mjs` | 第二节（`boundaries: PASS`；13/13） |

## 六、跨席交叠与归属（写清楚，免得对账时看着像偏差）

1. **#695（技能侧）**：四家的技能侧配置面由它落地（`src/config.ts` 的默认值表 ＋ `src/cli/config.ts` 的三个 key ＋ `cmd_read.ts` 预检前拦截）。本票**一个技能侧文件都没碰**，只 spawn 现成的 `dist/cli/cmd_read.js`。开工前已在 #695 留边界评论（`issues/695#issuecomment-5731501321`）。技能侧同期把四家插件的 `test/smoke.test.mjs` 测试隔离迁到 `ILIFE_CONFIG_DIR`（提交 `e4808516`）——与本票不冲突，本票用例全绿。
2. **#706（配置体检）**：在四家页面上叠加「配置体检」块（提交 `a42c9d4e`／`f8957dd8`），并在六个插件 `client.ts` 里加 `channel: RPC_CHANNEL`。两处连带后果：
   - `f8957dd8` **把本票备忘席尚未提交的四件改动一并卷进了它的提交**（混合提交）——内容仍是本票的配置页改动，只是提交信息挂在 #706 上；本票**不改写历史**，在备忘件里如实记一笔。
   - 它加 `channel` 时**没同步六个包的 `dsh-ctx.ts` 镜像** ⇒ 六个包里五个的 `tsc -p tsconfig.client.json` 当场红（`TS2353`）。备忘家的那格由备忘席在自己写集内补；大厨／居家／作息三家由本席补（提交 `e3cda0e1`）；**卡路里与记账两家（`plugin-calorie/src/client.ts:613`、`plugin-bill-ilife/src/client.ts:400`）当刻仍红，不在本票写集**——已在本票正文与 #706 留言点名，交它处置。
3. **#707／#712**：居家的「备份目录」行接 #707 落地的取值口；备忘的「附件目录」行按 #712 的真目录语义写。两者都只经配置 key 读写，不碰那两票的实现文件。
4. 四家 `src/client.ts` 均已超 350 线（342／354／354，备忘 566——末者含 #706 叠加的体检块）——第四步超线报警与拆法写在各家证据件；**统一拆等本图收尾裁定是否提取公共层**，清单见 `docs/plugins/plugin-chef/t696-四家同形清单.md`。

## 七、遗留

1. 四家设置页的同形版式是否提到公共层（端口件／行表骨架／配置页组件三个候选）——本图收尾统一裁，见 `docs/plugins/plugin-chef/t696-四家同形清单.md`。
2. 高级折叠组的识别度（四张截图一致的扣分点）——留给下一次动这批文件的票一并做，避免与 #706 的在途叠加互撞。
3. **卡路里与记账两家的客户端编译红**（第六节第 2 条）——属 #706 的写集，交它处置；在它修好之前，`pnpm test` 里这两包的 client 类型门是红的。
4. **备忘 `test/skills-provider.test.mjs:122` 的 28／29 冻结红**（非本票引入，自 2026-09-17 起）：技能侧 `buildHelpLookup()` 现 29 条速查唤醒词，插件这份冻结写在 28。修法一行，属备忘录面的归属票。
5. 内容宽 360／720 两档已量（`overflowX=0`）；更窄档（机制页）不在本票验收面内。
