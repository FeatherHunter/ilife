# #744 证据 · 私家大厨：目录行改接应用内文件夹浏览器（本票的参考实现）

判定：**PASS**（客户端类型检查 exit 0、束重建 exit 0、本包套件 36 条全绿；真机点屏那一跳列在未做项）

形状来源：**本家就是形状来源** —— 其余五家照本家的四处逐字复制。
共用件与全局判据见 `docs/plugins/plugin-manager/744-证据.md`（本件只记本家读数）。

## 改了什么（逐件一句）

| 件 | 改了什么 |
| --- | --- |
| `packages/plugin-chef/src/client.ts` | 四处：① 加两条共用件 import；② `readPickAnswer`／`pickerModeOf` 改经共用件同名转出（本地那五段重复实现删掉，`pickDirectory` 一并收口）；③ `Row` 的目录按钮改三态；④ 面板加应用内浏览那条路（`createDirectoryRowBrowser` ＋ `DirectoryBrowserFromRow`），登记点改成带状态的槽包装 |
| `packages/plugin-chef/test/config-surface-696.test.mjs` | 改两条既有断言（`Row` 改吃 `browser` 入口）＋ 新增两条（三态判定；束里带着浏览器界面与文案） |
| `packages/plugin-chef/src/settings.ts` | **一字未动** |

## 机器读数

| 命令 | exit | 摘要 |
| --- | --- | --- |
| `pnpm --filter dsh-chef exec tsc -p tsconfig.client.json` | 0 | 客户端类型检查通过 |
| `node node_modules/typescript/bin/tsc -b packages/plugin-chef` | 0 | 宿主侧编译通过 |
| `pnpm --filter dsh-chef run build:client` | 0 | 束重建通过（`dist/client.js` 51.41 kB） |
| `node --test packages/plugin-chef/test/config-surface-696.test.mjs` | 0 | **tests 36 / pass 36 / fail 0**（原 34 ＋ 新 2） |

## 未做项

真机点一次大厨设置页的「数据目录」行按钮：看「浏览…」开出的自绘浏览器能否进目录、新建文件夹、选定后回填该行且保存后仍在。本席无 GUI 点击能力，归维护者（#736／#743 两票的同一处出口）。
