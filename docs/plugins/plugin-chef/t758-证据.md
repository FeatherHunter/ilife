# #758 证据短条 · 私家大厨（4 行：可改 1＋只读 3）

完整读数在样板件 `docs/plugins/plugin-bill-ilife/t758-证据.md`；本家那一行：

```
PASS plugin-chef 行=4 可改=[db.dir] db.dir=<dataDir> db.name=<resolved.dbFile> html.dir=<resolved.htmlDir> html.sceneDir=<resolved.sceneDir>
```

定稿 #795：5 行收成 3 行＋ #766 落点后顺带的第 4 行（场景产物目录），共 4 行
（可改 1：数据目录；只读 3）。本家无只读目录行，跨包锁第 ⑦ 条断言名单为空。
