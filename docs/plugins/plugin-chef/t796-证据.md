# #796 证据 · 私家大厨设置页收窄（照记账 #749 样板）· **插件侧**

判定：**PASS**（插件侧判据全绿：行表 4 行＝可改 1 ＋ 只读 3；只读值逐行等于回执 `resolved` 格；
只读不进保存；只读渲染 disabled 且目录按钮保留不可点；`client.ts` 零省略号；包级全量 71／71；
跨包锁 `panel-copy-743` 5／5；#766 落点后第 4 行已顺带，同复绿）。

## 一、交付了什么（逐件一句，为什么）

| 件 | 改了什么 | 为什么 |
|---|---|---|
| `src/settings.ts` | 5 行→4 行：`db.dir` 可改（`directory`＋`prefillFrom`）＋ `db.name`／`html.dir`／`html.sceneDir` 只读（`resolveFrom: dbFile／htmlDir／sceneDir`）；`html.dir` 标题改「HELP 产物目录」，`html.sceneDir` 标题「场景产物目录」（两行父子目录、标题区分开，#795）；删 `files.*` 两行；加 `readonly`／`resolveFrom`／`ResolvedField` | 票面验收 2（#795 定稿 ＋ #766 落点后遗留出口第 4 行） |
| `src/client.ts` | `toDraft` 只读行显示 `resolved` 格（缺席即空串）／`fromDraft` 跳只读行／`Row` 只读 `disabled`＋不接 `onChange`＋只读目录按钮保留但不可点；按钮与三处文案去省略号；脏值只看可改行；头部数据目录优先 `resolved.dbDir` | 票面验收 2（与记账样板逐字同形） |
| `src/contract.ts` | `ResolvedPaths` 四格（`dbDir`／`dbFile`／`htmlDir`／`sceneDir`）＋ `ConfigSurfaceReply.resolved?` 可选 | 与技能回执同形；旧技能缺席时只读行显空串 |
| `test/t796-设置页收窄.test.mjs`（新） | 6 组 16 条：行表形状／回执对账／不进保存／只读渲染／零省略号／数字甲档合成 | 票面验收 5（插件侧） |
| `test/config-surface-696.test.mjs` | 3 键对齐＋常量断言；D 组 3 行；E 组去 `files.*` 断言 | 旧锁随新口径走 |

行文案（跨包锁 ②：≤2 句／≤40 字／零开发期口径词，全过）：`db.name`＝「数据目录下的库文件。只读，要改请
编辑配置文件。」、`html.dir`＝「HELP 与速查表的存放目录。只读，要改请编辑配置文件。」。

## 二、机器读数（门禁运行，逐条与 `.scratch/locks/gate-runs.log` 对账；导出件见 `t796-门禁运行记录.md`）

GATE-RUN runId=b3dfc3a2-94e3-4367-b2f7-05e4baace5ae cmd=node node_modules/typescript/bin/tsc -b packages/plugin-chef
GATE-RUN runId=271bfacd-6a5d-4ff4-aaf1-f02843cb06b6 cmd=pnpm --filter dsh-chef run build:client
GATE-RUN runId=5daa63fa-89df-4e5f-bebf-0bb03b80f6eb cmd=node --test packages/plugin-chef/test/t796-设置页收窄.test.mjs packages/plugin-chef/test/config-surface-696.test.mjs
GATE-RUN runId=a62c48ee-bd23-4b48-9066-3b54dd8309f1 cmd=node --test packages/plugin-chef/test/*.test.mjs
GATE-RUN runId=9808bf8c-d385-4b59-bf16-34601dda35e0 cmd=node --test test/panel-copy-743.test.mjs

| runId（首段） | 命令 | exit | 摘要 |
|---|---|---|---|
| `b3dfc3a2` | `tsc -b packages/plugin-chef`（3 行态） | 0 | 编译过 |
| `271bfacd` | `build:client`（tsdown，3 行态束） | 0 | 组件级用例读的就是这一束 |
| `5daa63fa` | 插件侧 t796＋696（3 行态） | 0 | `tests 51／pass 51／fail 0` |
| `a62c48ee` | 插件侧包级全量（3 行态） | 0 | `tests 71／pass 71／fail 0` |
| `787ec852` | `tsc -b` 双包（#766 落点吸收后） | 0 | 编译过 |
| `32a81feb` | `build:client`（终态束，4 行） | 0 | 组件级用例读的就是这一束 |
| `d7cd391f` | 插件侧包级全量（终态） | 0 | `tests 71／pass 71／fail 0` |
| `aa0cad83` | 跨包锁 `panel-copy-743`（终态） | 0 | `tests 5／pass 5／fail 0`（六家同形面未破） |

页面读数（票面验收 2 按遗留出口更新为四块：#766 落点后第 4 行为「场景产物目录」只读，显示
`resolved.sceneDir`；原验收“没有第四行”已被遗留出口取代）：数据目录（可改，框里是回执生效绝对路径）／
库文件名（只读，`resolved.dbFile`）／HELP 产物目录（只读，`resolved.htmlDir`）／场景产物目录
（只读，`resolved.sceneDir`）；控件文案零省略号；本家三行只读都是文本档，只读目录行“按钮保留但不可点”
由 `Row` 统一承接（合成项钉住，与样板同形）。

## 三、 bundle 新鲜度实证（附带）

`f2f3f83f`（exit=1）那次跑的是**未重建的旧 `dist/client.js`**（只读新形态 6 条全红）——证明组件级用例
对束的新鲜度敏感，不是自满足；重建（`271bfacd`）后 `5daa63fa` 全绿。教训已记：改 `client.ts` 后必须
`build:client` 再跑组件级用例。

## 四、未做项（如实记）

1. 发版与装机归 #755；真机点屏归维护者（同技能侧 §六）。
2. 六家同形面收口（跨包锁 ＋ 逐家端到端读数）归 #758；本票只保证本家不破锁。
