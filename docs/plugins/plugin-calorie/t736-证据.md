# #736 证据 · dsh-calorie：设置页目录行接系统文件夹选择器

判定：**PASS**（本地可判的判据全绿；「真机上点开系统对话框」那一跳按票面由维护者点屏）

## 改了什么

| 件 | 改了什么 |
| --- | --- |
| `src/settings.ts` | `ConfigControl` 加第四档 `'directory'`；`db.dir`／`photos.dir`／`xunji.stateDir` 三行改档（其余八行一字未动） |
| `src/dsh-ctx.ts` | 照样板新增 `REMOTE_DIRECTORY_PICKER`（服务名）与 `DirectoryPickerFace`（本包只用 `pick`）；文件头那句「type-only，零运行时」按事实校正（本文件由此含一枚运行期字符串常量） |
| `src/client.ts` | 软依赖取命名空间（`ctx.get`，**没写进 inject**）＋ 目录行的「选择文件夹…」按钮 ＋ 三态归一（选中／取消／供不了）；`Row` 改为导出供用例直调 |
| `test/config-surface-676.test.mjs` | 新增 H 组七条（档位键集／缺席不画按钮／按钮链路／取消不动／被拒给人话／归一三态／inject 未变）；真产物加载改走共用件 `test/helpers/client-bundle.mjs`（不为本包再抄一份加载器） |

目录行集合（H 组第一条锁死，排序后逐字）：`['db.dir', 'photos.dir', 'xunji.stateDir']`。

## 机器读数（门禁运行，逐条对账）

GATE-RUN runId=60f6d6e9-7212-4222-a232-b2539d3d8d0d cmd=node tooling/run-locked.mjs --ticket 736 -- node node_modules/typescript/bin/tsc -b packages/plugin-calorie
GATE-RUN runId=cf6bf0b7-f92c-456b-a5fa-5fa969b6dad9 cmd=node tooling/run-locked.mjs --ticket 736 -- node node_modules/typescript/bin/tsc -p packages/plugin-calorie/tsconfig.client.json
GATE-RUN runId=29a94133-311c-4aea-871e-3d1c8e78e75e cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-calorie run build:client
GATE-RUN runId=253ab1b3-ef15-40e7-bb23-dee2ce9ee7de cmd=node tooling/run-locked.mjs --ticket 736 -- node --test packages/plugin-calorie/test/config-surface-676.test.mjs
GATE-RUN runId=178171f1-6102-4bd3-b1c2-580fbf90fee7 cmd=node tooling/run-locked.mjs --ticket 736 -- node tooling/check-boundaries.mjs

五条全 exit=0。第二条是客户端程序类型检查（`tsconfig.json` 的 `exclude` 把 `src/client.ts` 挡在宿主编译外，故单跑一次 `tsconfig.client.json` 补上类型面）。
最后一条的套件读数：`tests 26／pass 26／fail 0`（其中 H 组七条全绿）；client 束重建后 `dist/client.js` 36690 字节，
含 `remote.directoryPicker`（1 处）与「选择文件夹…」「这台部署用不了系统文件夹对话框…」两条新文案。
边界门 `boundaries: PASS`。

## 变异自证（红／还原一致两行）

- 变异：`src/client.ts` 里 `resolveDirectoryPicker` 的
  `return isDirectoryPicker(raw) ? raw : null;` 改成
  `return isDirectoryPicker(raw) ? raw : ({ pick: async () => null } as DirectoryPickerFace);`
  - 重建：**runId=eb633a17-6383-4f60-92d2-7b0d235c9a54，exit=0**（这是**语义**破坏，不是语法破坏）
  - 套件：**runId=a26bfabb-9c2b-48e9-9e80-1a87deaa78f7，exit=1，pass 25／fail 1**
  - 红在哪一条：`命名空间拿不到 ⇒ 没有入口（软依赖；守卫拒绝也当没有）`（断言「拿不到就不给入口：() => undefined」）
- 还原：编辑器改回原样，`[System.Security.Cryptography.SHA256]::Create()` 自查与变异前**逐字节相同**
  （`e6130d26289d23345257a2cbe6abbff8c67c43e7de10b7cf11b6f0d6efa8353c`）；
  重建 **runId=48f4c745-e1ee-474a-a5f1-5b12fb350db5，exit=0** ＋ 套件
  **runId=56348e2f-e5a0-42bb-9f6e-a0d7ba771d09，exit=0，pass 26／fail 0**。

两次改写都走**编辑器工具**（未用 `Set-Content`／`Out-File`：本机 PowerShell 5.1 会写 BOM 把源码写坏）。

## 未做项（如实记）

- **真机点屏那一跳没做**：需要人在爱生活面板点一次、看操作系统文件夹对话框弹不弹、选完值是否落进那一行。
  本席无 GUI 点击能力，判据与失败处置见 #736 Agent Brief「外部确认出口」一节。
- **一处旁证（本包既有、与 #736 无关）**：`test/smoke.test.mjs` 在 `node --test` 里跑时固定红 2 条
  （`#48 envelope 契约` 那两条），真因是技能侧 #675 隔离护栏——`node --test` 的子进程继承
  `NODE_TEST_CONTEXT`，技能 CLI 便要求 `ILIFE_CONFIG_DIR`，而 `smoke.test.mjs` 没设它，出口以
  `CONFIG_TEST_ISOLATION_MISSING` 响亮报错（skill-calorie 源码原文即写「测试跑在 `node --test` 里却没设它就直接响亮报错」）。
  读数：GATE-RUN runId=b796c177-1877-4dd4-b159-2a32f132c197（`version-dynamic-130` ＋ `smoke` 合并跑，
  pass 20／fail 2）与 runId=37971e57-8465-4d9e-b5c4-94a0834d570c（`smoke` 单跑，pass 8／fail 2）皆 exit=1；
  补上 `ILIFE_CONFIG_DIR` 后同一命令 **runId=1955dafd-e12c-4518-bb53-7042f66317ef，exit=0，pass 10／fail 0**。
  该文件不在本票可改清单内（红线只放本包 `test/config-surface-*.test.mjs`），故留原样、只记读数；
  `version-dynamic-130.test.mjs` 在同一轮里全绿（含 client 束纯度门与 `inject` 未变）。


## 超线报警（必报五步·第四步）

**已超线，需要根据规则进行重构。** `src/client.ts` 本票改前 **654** 行 → 现在 **765** 行（涨 +58；
口径＝本仓插件包沿用的兄弟数字 **350**）。本包**改前就已超线**，本票只是继续往上加。

超因：这一件同时住五样活——样式表、行控件（含目录选择器的按钮与三态归一）、草稿与报错人话、三态容器、槽注册；
本票按「照样板逐段复刻」的口径往同一件里加了三段（目录选择五件、`Row` 的按钮与两格样式、容器接线与取用器）。
为什么这次先不拆、以及拆法，见 `docs/plugins/plugin-bill-ilife/t736-证据.md` 的「超线报警」一节
（六家同形，全票在那里记一次；本节数字按本包实测，与那张总表里本包那一行**不一致**——总表当时按样板块大小估的 765 行，
本包实测 667 行，已在本席回执里点名请作者订正）。

## 交付对账（必报五步·第五步）

本包实际碰到的路径：`packages/plugin-calorie/src/settings.ts`、`src/client.ts`、`src/dsh-ctx.ts`、
`test/config-surface-676.test.mjs`、`docs/plugins/plugin-calorie/t736-证据.md`——与票面分配逐行一致，**偏差 0**。
（票级的补账表在 `docs/plugins/plugin-bill-ilife/t736-证据.md` 的「交付对账」一节。）

> **口径订正（2026-09-20）：** 本节原写「改前 609 → 667（涨 +58）」，那是**不含空行**的计数；全票统一改用 **LF 口径**后为「654 → 765（涨 +111）」。六家 LF 涨量 111～116 行，见 docs/plugins/plugin-bill-ilife/t736-证据.md 的订正。
