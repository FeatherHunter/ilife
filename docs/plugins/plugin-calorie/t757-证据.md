# #757 证据 · 卡路里设置页收窄（照记账样板铺开）· **插件侧**（页面从 13 行可改表单收成「3 可改 ＋ 8 只读」）

判定：**PASS**（页面判据全绿：新增 17 条组件级用例 17／17、插件套件 85／85 全绿、
两道跨包锁绿、变异自证「打掉只读标记必红／还原必绿」跑齐）。

## 一、交付了什么（逐件一句）

| 件 | 改了什么 |
| --- | --- |
| `src/settings.ts` | 行表加三个字段：`readonly`（只读行不参与保存）与 `resolveFrom`（只读路径行的显示值来自回执 `resolved` 组哪一格）＋ `prefillResolved`（可改照片目录空着时显示 `resolved.photosDir`）；`db.name`／`html.dir`／`photos.gifs`／`xunji.stateDir`／`xunji.catalog`／`xunji.backfillDays`／`land.xunjiSeconds`／`land.landSeconds` 八行转只读；删 `land.scheduleCli`／`land.memoCli` 两行（13→11）；八行 hint 改成「只读，要改请编辑配置文件」（数字三行甲档同式）；照片目录 hint 改新口径。**落点说明**：本件内容已在主干（提交 `7776217d` feat 754 同批带上，`git diff HEAD` 为空逐字节一致）——多席共享工作区的一次交叉暂存，本票提交不再重复收录，归属仍是本票（见 §四对账） |
| `src/client.ts` | `Row` 支持只读：控件 `disabled`、不接 `onChange`，目录行的浏览按钮**保留但不可点击**；`toDraft` 让只读路径行显示 `resolved` 那一格（缺回执则空串，面板不拼路径）、可改照片目录空着时显示 `resolved.photosDir`、数字只读行显示生效数字；`fromDraft` 跳过只读行；`dirty` 只算可改 3 行；页头「数据目录」改用 `resolved.dbDir`（生效值）；控件文案去省略号（`选择文件夹`／`浏览`／`卡路里加载中`／`配置读取中`／`处理中`／`正在读取`）；`SurfacePrefill` 加 `resolved` 组 |
| `src/contract.ts` | 回执形状加 `ResolvedPaths`（七格）＋ 可选字段 `resolved`（旧技能缺席 ⇒ 路径类只读行显示空串，不炸） |
| `src/index.ts` | 转出加 `ResolvedField` 与 `ResolvedPaths`（类型面，运行时出口不变） |
| `test/t757-设置页收窄.test.mjs`（新） | 17 条：行表形状（可改 3／只读 8／目录档三行／删键两行）· 只读值只来自回执（含与配置文件的逐行对账、照片预填、旧技能空串、面板零拼接）· 只读行不进保存（只收 `db`／`photos`／`xunji` 三组的可改格，`land` 不进）· 只读渲染（控件 disabled、按钮不可点、可改两行照旧）· 文案零省略号 · **只读数字行的甲档形态（本家真行 `backfillDays`）** ＋ 脏值只看可改行 |
| `test/health-706.test.mjs`（顺手） | F 组件头改新口径一句（断言一行未改，7／7 绿） |

技能侧一行未动之外的东西：`slot.ts`／`bridge.ts`／`dsh-ctx.ts`／`skill-provider.ts`／`skill-tool.ts` 一行没动；
`inject` 仍是 `['slots','connection']`；`bridge.ts` 的三个配置 key 镜像值未动（技能侧 `CONFIG_KEYS` 未改名）。

## 二、页面形状（改造前 → 改造后）

| 行（键） | 改造前 | 改造后 | 显示什么 |
| --- | --- | --- | --- |
| 数据目录（`db.dir`，常用） | 可改·目录行 | **可改**（不变） | 生效绝对路径（空值＝按默认落点，面板按回执预填） |
| 库文件名（`db.name`，常用） | 可改·文本框 | **只读** | 回执 `resolved.dbFile`（库文件绝对路径） |
| HTML 产物目录名（`html.dir`，常用） | 可改·文本框 | **只读** | `resolved.htmlDir` |
| 照片目录（`photos.dir`，常用） | 可改·目录行 | **可改**（不变，hint 改新口径） | 生效绝对路径（空值＝按默认落点 `…/photos`，面板按 `resolved.photosDir` 预填） |
| 训记 KEY（`xunji.key`，常用） | 可改·文本框 | **可改**（不变，控件形态不动） | 配置值本身 |
| 照片 GIF 子目录（`photos.gifs`，高级） | 可改·文本框 | **只读**（浏览按钮不画——文本行） | `resolved.gifsDir` |
| 训记状态文件目录（`xunji.stateDir`，高级） | 可改·目录行 | **只读**（浏览按钮保留＋不可点） | `resolved.stateDir` |
| 训记动作库路径（`xunji.catalog`，高级） | 可改·文本框 | **只读** | `resolved.catalog`（空＝包内预置那份的绝对路径） |
| 回写默认天数（`xunji.backfillDays`，高级） | 可改·数字框 | **只读**（甲档：`disabled` 的 `number` 框） | 生效数字（默认 1） |
| 训记调用限时（`land.xunjiSeconds`，高级） | 可改·数字框 | **只读**（甲档） | 生效数字（默认 300） |
| 落地调用限时（`land.landSeconds`，高级） | 可改·数字框 | **只读**（甲档） | 生效数字（默认 60） |
| 跨技能出口 · 作息（`land.scheduleCli`） | 可改·文本框 | **删行**（键已退休） | —— |
| 跨技能出口 · 备忘（`land.memoCli`） | 可改·文本框 | **删行**（键已退休） | —— |

行数 13 → 11（#748 定稿：可改 3＋只读 8）；`xunji.cli` 页外键照旧不上页。

## 三、只读数字行的形态（#749 补注二要的样板裁定：**甲**，本家真行）

- **裁定＝甲**（照只读文本行画：`disabled` 的 `number` 输入框，值就是生效数字）——记账 #749 已钉，本票用真行
  `xunji.backfillDays`（另两行 `land.xunjiSeconds`／`land.landSeconds` 同形）把形态落成读数：
  `input.type='number'`、`input.disabled=true`、`input.value='1'`（生效数字）、无 `onChange`、不画按钮。
- 脏值只看可改行：只读 8 行显示的是技能算好的绝对路径／生效数字，拿它们当「改动」会让打开面板就变「未保存」
  ——`dirty` 只算 `EDITABLE` 三行（读数见 t757 ⑥ 第二条的前提断言与 ③ 那条）。

## 四、机器读数

| 命令 | exit | 摘要 |
| --- | --- | --- |
| `tsc -b packages/skill-calorie packages/plugin-calorie` | 0 | 宿主侧两包编译通过 |
| `pnpm --filter dsh-calorie run build:client` | 0 | client 束重建（`dist/client.js` 77.87 kB） |
| 插件侧 t757 单件 | 0 | `tests 17／pass 17／fail 0` |
| 插件套件 `test/*.test.mjs` | 0 | `tests 85／pass 85／fail 0` |
| 设置页渲染台 `--check` | 1（见下） | 卡路里四页全绿（行=11）；另 6 红在别家默认页（`advancedCollapsedByDefault`，备忘／大厨／作息三家，与本票无关，见下） |
| 跨包锁 `test/panel-copy-743.test.mjs` | 0 | 5 条全绿（③ 只查 `prefillFrom`，本票新加的 `prefillResolved` 不破锁；② hint 逐条 ≤40 字、无「落点」等词） |
| 变异自证（见下） | 0 | 红 `pass=14 fail=3` → 还原后绿 `pass=17 fail=0` |

渲染台卡路里四页（`--check` 里本家那四页）读数：

```
PASS plugin-calorie-default-360.html  行=11 overflowX=0 改='db.dir'→"渲染台探针值"
PASS plugin-calorie-expanded-360.html 行=11 overflowX=0 改='db.dir'→"渲染台探针值"
PASS plugin-calorie-default-720.html  行=11 overflowX=0 改='db.dir'→"渲染台探针值"
PASS plugin-calorie-expanded-720.html 行=11 overflowX=0 改='db.dir'→"渲染台探针值"
```

两处关键读数：`行=11`（只读行照样画出来，页面上仍是 11 行）、
`改='db.dir'→"渲染台探针值"`（渲染台挑的第一个「能改的框」正是数据目录——只读那 8 行不接 `onChange`，
故不再被当成可改行；#743 起「打开面板改一项再保存」那条链照常跑通）。

另 6 红（备忘／大厨／作息三家的 `default-*.html`，`advancedCollapsedByDefault`）与本票无关：
它们是别家页面的高级组默认收起断言（那三家常用行即全部，高级组无行可展），本票一行没碰那三家。
卡路里四页（`default`＋`expanded` × 360／720）**全绿**。

## 五、变异自证（打掉一处 ⇒ 必红；还原 ⇒ 必绿）

**产物级变异**（不是源码级），原因与技能侧同一件：工作区多席按锁排队提交，
源码级变异有被扫描提交进主干的风险；`dist/**` 在忽略清单里、复原路径就是重编。

| 变异 | 红读数 | 还原读数 |
| --- | --- | --- |
| 把 `xunji.stateDir` 的 `readonly` 去掉（`dist/settings.js` 行表） | `exit=1 tests=17 pass=14 fail=3`（①可改 3 变 4／①只读 8 缺 1／④按钮可点） | 删产物强制重编（增量 `tsc -b` 会跳过无源码变更的产物，须删 `dist/settings.*` 后 `--force`）⇒ `exit=0 tests=17 pass=17 fail=0` |

## 六、未做项（如实记）

1. **发版与装机**：见技能侧证据件 §六第 1 条（#755）。
2. **真机点屏**：见技能侧证据件 §六第 2 条（归维护者）。
