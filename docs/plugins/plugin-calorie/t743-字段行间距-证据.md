# #743 追加证据 · dsh-calorie：设置页样式表逐项同形（字段行间距那一处 3 : 3）

判定：**PASS**（本包读数全绿；形状与判据逐段照抄样板件，本件只记本包不同之处）

- 形状来源：`packages/plugin-bill-ilife`（同票样板，现象、根因、甲案取值表、机器读数、变异自证与结构纪律
  全票记在 `docs/plugins/plugin-bill-ilife/t743-字段行间距-证据.md`，本件不重抄）。
- 本包改了什么：`src/client.ts` 的样式表 `S` 逐项对齐到甲案（本包属「首行标题离分隔线 18px」那一支，改得最多）：
  `row`（`marginTop: 10` → `marginBottom: 10`）、`hint`（三级灰＋`margin` → 二级灰＋`marginBottom: 4`）、
  `info`（改单行写法，语义不变）、`bar`（`marginTop: 14` → 12，去掉 `alignItems`）、`btn`／`btnPrimary`
  （`5px 14px`／圆角 7 → `4px 12px`／圆角 6）、`error`／`okText`（补 `marginTop: 8`；`error` 补 `whiteSpace: 'pre-wrap'`）、
  `adv`＋`advSummary` → `advanced`＋`summary`（去掉上分隔线与 `paddingTop`）、样式表上方注释加「六家逐项同形」一行。
- 本包机器读数：与样板件同一批门禁运行（runId 见样板件与
  `docs/plugins/plugin-bill-ilife/t743-字段行间距-门禁运行记录.md`）。
- 本包行数（LF 口径，超线报警见样板件第四步）：`src/client.ts` 792 → 787 行。
