## 本席现状（第二席接手 #676 后的第一段）

### 一、已落地并给出读数的（技能侧配置面）

按用户 2026-09-18 的三条裁决（走 B／本席继续／分级照推荐）开工，先落了配置面三件：

| 件 | 动作 | 一句话 |
|---|---|---|
| `packages/skill-calorie/src/config.ts` | 新建 | 卡路里的默认值表（键＝设置页的行）＋ 读／写／重置三个薄转调，内部调 `base-link-core` 的 `loadConfig`／`saveConfig`／`resetConfig`（不复制解析器） |
| `packages/skill-calorie/src/cli/config.ts` | 新建 | 三个配置 key（`calorie.config.read`／`.write`／`.reset`）的唯一定义地；**不是唤醒词命令**，不进 `registry.ts`／`keys.ts`、不进 HELP 与唤醒词计数 |
| `packages/skill-calorie/src/cli/cmd_read.ts` | 改 | 在**预检与分派层之前**拦下配置 key（读写配置不该要求库目录已配），输出仍是唯一出口那条规矩：stdout 一行 envelope |
| `packages/skill-calorie/package.json` | 改 | `base-link-core` 从 devDependencies 提到 dependencies（`^0.3.0`，与其余五家同档） |

**真机读数（`ILIFE_CONFIG_DIR` 指向临时目录，`node dist/cli/cmd_read.js …`）**：

- 读（文件不存在）：`created:true`，落一份默认，键 5 组 13 项；
- 重置：`{path, backupPath}`，`.bak` 实测存在；
- **错项人话**：把 `db.dir` 改成 `db.dirx` 后 → `ERR 1: 不认识的配置项「db.dirx」：默认值表里没有它（第 2 行），文件：<临时目录>\calorie.yaml`（**带行号与文件名**，正是票面「缺项与错项给人话指引」要的那一句）。

编译：`tsc -b packages/skill-calorie` 绿（首跑那次红是别席正在重建 `base-link-core` 的 `dist`）。

### 二、卡点：同一张票上现在有**两个席位在写同一批文件**

本席开工期间，另一席正在 `packages/plugin-calorie/src` 里按**另一条路**（插件宿主直接 `import base-link-core`）落码，已落盘的有：
`src/bridge.ts:14`（`import { ConfigError, loadConfig, resetConfig, saveConfig } from 'base-link-core'`）、
`src/config-defaults.ts:12`（`import { configPaths } from 'base-link-core'`）、`tsconfig.json` 加 `references: ../base-link-core`、`package.json` 加 `base-link-core` 依赖、`contract.ts` 加三个配置端点、`settings.ts` 重写成 14 行。

**这条路与本票验收第 4 条冲突**：`test/plugin-p10-boundaries.test.mjs:52` 断言单品源码不许 `import base-` 包（正则 `/from\s+['"]base-/`），而验收要求这条门不红。用户 2026-09-18 的裁决一已选**走 B**（配置读写经技能 CLI 配置面，不装依赖、不碰边界门）——本席按 B 落的配置面就是 B 的那一半。

**本席已停手**：两边同时写 `plugin-calorie/src` 会互相覆盖。请编排者裁：由哪一席收口本票的插件侧（若要 B，另一席那三处 `base-link-core` 连线与三个配置端点需撤回）。

### 三、下一步（按 B 的路线）

1. 插件侧四件照 B 落：`settings.ts`（11 项 13 行、常用／高级分级）、`contract.ts`（三个配置 key）、`client.ts`（真能配的页：行＋输入＋保存＋重置＋人话错误，`CalorieWork` 一行不动）、`index.ts` 导出面；
2. 技能侧路径定位改读配置（12 项）＋ 删掉全部环境变量读取；
3. 测试迁移：130 个技能测试件（`SKILLS_DB_PATH` → `ILIFE_CONFIG_DIR`）、77 个用 `CALORIE_TODAY` 的测试件改走 `--require test/freeze-clock.cjs` 钉时钟（本席已核：该件存在且正是这个手法）、17 个 `CALORIE_PHOTOS_DIR`、3 个挡板变量、2 个 `CALORIE_FORCE_PROD`；
4. 两条边界门 ＋ 真机读数三连 ＋ 变异自证。

**一条写集说明**：票面「交付物路径」只列了 `plugin-calorie/src` 四件，但单品不许 import base- 包（上引门），页面读写配置只能经技能 CLI，故技能侧必增一个配置面（上面第一节那三件）——这是票面漏写的一处，实施后对账会点名。
