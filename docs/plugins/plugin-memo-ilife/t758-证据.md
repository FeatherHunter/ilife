# #758 证据短条 · 备忘录（4 行：可改 2＋只读 2）＋飞书 CLI 状态行

完整读数在样板件 `docs/plugins/plugin-bill-ilife/t758-证据.md`；本家那一行：

```
PASS plugin-memo-ilife 行=4 可改=[db.dir、media.dir] db.dir=<dataDir> db.name=<resolved.dbFile> html.dir=<resolved.htmlDir> media.dir=<resolved.mediaDir>
```

定稿 #759：8 行收成 4 行（可改 2：数据目录／附件目录；只读 2）＋「飞书 CLI」状态行。
本家无只读目录行（两项只读都是文本行），跨包锁第 ⑦ 条断言名单为空。
