# #736 证据 · dsh-memo-ilife：设置页目录行接系统文件夹选择器

判定：**PASS**（本地可判的判据全绿；「真机上点开系统对话框」那一跳需人在爱生活面板点屏，见末节未做项）

## 本包的目录行（恰三行）

`db.dir`（常用）／`media.dir`（常用）／`lark.qrDir`（高级）。其余五行
（`db.name`／`html.dir`／`files.help`／`files.lookup`／`lark.cliPath`）**一律未动**，档位仍是 `text`。

## 改了什么

| 件 | 改了什么 |
| --- | --- |
| `src/settings.ts` | `ConfigControl` 加第四档 `'directory'`（注明是字符串档的页面形态）；`db.dir`／`media.dir`／`lark.qrDir` 三行改档，其余行与全部 hint 一字未动 |
| `src/dsh-ctx.ts` | 照样板末尾抄两格：`REMOTE_DIRECTORY_PICKER`（服务名）与 `DirectoryPickerFace`（本包只用 `pick`） |
| `src/client.ts` | 照样板抄：`PickOutcome`／`isDirectoryPicker`／`resolveDirectoryPicker`／`pickDirectory`／`createBrowseHandler`；`Row` 改为**导出**＋目录档按钮（`S.pickRow`／`S.btnPick`）；`MemoConfig` 加 `pickerGone` 与 `onBrowse`，`apply` 加 `getPicker`（`ctx.get` 现取，**没写进 inject**） |
| `test/config-surface-696.test.mjs` | 新增 H 组七条；`client-bundle.mjs` 走仓根共用件（没复制第六份）；D 组那条「控件都是 text」的断言按本票改成「字符串档（`text`／`directory`）」——不改它，三行目录档会与它自相矛盾 |
| `docs/plugins/plugin-memo-ilife/t736-证据.md` | 本文件 |

`inject` 保持 `['slots', 'connection']` 不变（H 组末条锁死）。

## 机器读数（门禁运行，runId 抄自 `.scratch/locks/gate-runs.log`）

GATE-RUN runId=47027f1f-09ab-47bb-b09b-1e7126150db8 cmd=node tooling/run-locked.mjs --ticket 736 -- node node_modules/typescript/bin/tsc -b packages/plugin-memo-ilife
GATE-RUN runId=8ee2e483-e70d-45b6-872e-806c9f3eb3d5 cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-memo-ilife run build:client
GATE-RUN runId=8ba546c9-62b3-4a41-9b28-dce5db70e3ee cmd=node tooling/run-locked.mjs --ticket 736 -- node --test packages/plugin-memo-ilife/test/config-surface-696.test.mjs
GATE-RUN runId=408ed8cf-0ee7-4430-a107-3daecc429b0a cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-memo-ilife run build:client
- 变异态套件（**读数，不是门禁声明**：exit=1）：runId=c82166a9-bc32-4354-a3c1-0af77b819ff4，pass 37／fail 1，红在「命名空间拿不到 ⇒ 没有入口」
GATE-RUN runId=5b497173-387c-4c30-a8df-be7d1dd81776 cmd=node tooling/run-locked.mjs --ticket 736 -- pnpm --filter dsh-memo-ilife run build:client
GATE-RUN runId=5afc7b0a-778b-4422-937b-7b243fe08c7e cmd=node tooling/run-locked.mjs --ticket 736 -- node --test packages/plugin-memo-ilife/test/config-surface-696.test.mjs
GATE-RUN runId=57160221-1ae7-4d84-a3c5-e1904b91a551 cmd=node tooling/run-locked.mjs --ticket 736 -- node tooling/check-boundaries.mjs

- 宿主侧编译 exit=0；client 束重建 exit=0（`tsc -b` 不产 client 束，束由 `tsdown` 出）。
- 本包套件（还原态，读的是真产物 `dist/client.js`）：**`tests 38／pass 38／fail 0`**，exit=0。
- 边界门：`boundaries: PASS`，exit=0。
- 日志留档：`.scratch/t736/logs/memo-{tsc,build-client,test,build-client-mut,test-mut,build-client-restore,test-restore,boundaries}.log`。

## 变异自证（红／还原一致两行）

- **红**：把 `src/client.ts` 里 `resolveDirectoryPicker` 的
  `return isDirectoryPicker(raw) ? raw : null;` 改成
  `return isDirectoryPicker(raw) ? raw : ({ pick: async () => null } as DirectoryPickerFace);`
  ⇒ 重建 runId=408ed8cf… exit=0（语义破坏，非语法破坏）⇒ 套件 runId=c82166a9… **`pass 37／fail 1`，exit=1**；
  红在 `命名空间拿不到 ⇒ 没有入口（软依赖；守卫拒绝也当没有）`（断言「拿不到就不给入口：() => undefined」，actual 是那个假对象、expected `null`），连带 `H 目录行与系统文件夹选择器入口（#736）` 整组标红。
- **还原一致**：编辑器改回原样 ⇒ 重建 runId=5b497173… exit=0 ⇒ 套件 runId=5afc7b0a… **`pass 38／fail 0`，exit=0**；
  `src/client.ts` 的 SHA256 与变异前逐字节相同：
  `310cefc7b9fd4fa1056b2f938cd135692be9d320ebbb01024d6a891678acb539`（`[System.Security.Cryptography.SHA256]::Create()` 自查，equals-pre=True）。

## 形状不变式对账

1. 目录档只发给那三行：H 组首条断言排序后的键集 `['db.dir','lark.qrDir','media.dir']`。
2. 拿不到命名空间 ⇒ `resolveDirectoryPicker(...)` 回 `null` ⇒ 不画按钮：H 组第二条（五种缺席形态＋守卫抛错都回 null）；
   第三条断言 `onBrowse` 缺席时按钮数 0、文本框仍是 1 枚。
3. 点按钮 ⇒ 唤一次 `pick()` ⇒ 回填该行；取消／空白串一字不动；`pick()` 抛错只出人话、不写值：H 组第四、五条
   （`createBrowseHandler` 的 `onUnavailable` 由容器接到 `setError` ＋ `setPickerGone(true)`，入口收起，不留死按钮）。
4. `inject` 不变：H 组末条 `deepEqual(CLIENT.exports.inject, ['slots','connection'])`。

## 未做项（如实记）

- **真机点屏那一跳没做**：需要人在爱生活面板点一次「选择文件夹…」，看操作系统对话框弹不弹、选完值是否落进那一行
  （本席无 GUI 点击能力）。本席只能给到「组件级 ＋ 真 client 束」这一层读数。
- 未跑本包另外两个测试件（`smoke.test.mjs`／`skills-provider.test.mjs`）：票面判据只点名 `config-surface-*.test.mjs`，
  且它们与本次改动无交集。
- 未提交（本票作者统一提交）；工作区只改了上表四个件，未碰 `package.json` 与任何别人路径。


## 超线报警（必报五步·第四步）

**已超线，需要根据规则进行重构。** src/client.ts 本票改前 566 行 → 现在 677 行（**LF 口径**；本仓插件包沿用的兄弟口径 350）。更正：本条先前那对数字量的是「不含空行」的计数，已作废；全票统一按 LF 口径，见 docs/plugins/plugin-bill-ilife/t736-证据.md 的订正。）
超因、为什么不这次拆、以及拆法，见 docs/plugins/plugin-bill-ilife/t736-证据.md 的「超线报警」一节（六家同形，全票在那里记一次）。
