# t696 证据：#696 其余四家的设置页 · 备忘件

**票**：[#696 其余四家的设置页：照卡路里样板铺开（分级呈现）](https://github.com/FeatherHunter/ilife/issues/696)（wayfinder:task，属图 [#671 爱生活设置面](https://github.com/FeatherHunter/ilife/issues/671)）

**本件范围**：`packages/plugin-memo-ilife`（件名逐字；四家同票，另三家的证据件各落自家目录）。提交 `fd63691c`（本席三件）＋ **`f8957dd8`（别席 #706 连带提交了本票的 `src/{contract,bridge,index,client}.ts` 四处改动）**。

口径出处：`#675` 解决评论、`#692` 产出（清单权威）、`#676`／`#677` 样板；技能侧配置面由 `#695` 落地（`skill-memo-ilife/src/config.ts` 的 `MEMO_CONFIG_DEFAULTS` 与 `src/cli/config.ts` 的 `CONFIG_KEYS`）。**本包与另三家不同**：它**早有** RPC 通道（`/ilife-memo`）与取数端点，本票只在既有通道上**加三个配置端点**，取数端点一行未动。

## 一、交付物（逐文件一句）

| 文件 | 动作 | 一句话 |
| --- | --- | --- |
| `packages/plugin-memo-ilife/src/settings.ts` | 改 | `SETTING_ROWS` 换成 **8 行真配置行表**（常用 4 ＋ 高级 4），键＝`MEMO_CONFIG_DEFAULTS` 的投影；零 node |
| `src/contract.ts` | 改 | 既有取数端点（`RPC_ENDPOINT_READ`／`ReadPayload`／`parseReadPayload`／`DEFAULT_READ_KEY`）一行未动，加 `config.get`／`config.save`／`config.reset` ＋ `SavePayload`／`parseSavePayload`／`ConfigSurfaceReply`；零 node |
| `src/bridge.ts` | 改 | 三个配置 key 的镜像 ＋ 三个薄转调；既有 spawn（`ELECTRON_RUN_AS_NODE`／env／超时）未重写 |
| `src/index.ts` | 改 | 端点分发加三个配置分支；既有路由注册一行未动 |
| `src/dsh-ctx.ts` | 改 | 补具名 `HostConnectionFace`（出处照大厨那份）＋ `SlotsRegisterOptions.channel`（见第六节偏差 A） |
| `src/client.ts` | 改 | `MemoConfig` 换真配置页（三态／分级／保存／重置／人话报错）；功能页、版本行、既有取数逻辑未动 |
| `test/config-surface-696.test.mjs` | 新建 | 七组判据 31 条用例（含端到端经真 CLI 的读／写／重置） |

**没碰**：`packages/skill-*`（只 spawn 现成的 `dist/cli/cmd_read.js`）、别的 `packages/plugin-*`、根 `test/`、`pnpm-lock.yaml`。

## 二、机器读数（只列实际跑过的，全部持锁）

| # | 命令 | exit | 摘要 |
| --- | --- | --- | --- |
| 1 | `tsc -b packages/plugin-memo-ilife` | 0 | 宿主侧编译通过 |
| 2 | `tsc -p packages/plugin-memo-ilife/tsconfig.client.json` | 0 | browser 产物侧类型门通过（跑之前红，见第六节偏差 A） |
| 3 | `tsdown`（包目录内） | 0 | `dist/client.js` 28.24 kB |
| 4 | `node --test …/test/config-surface-696.test.mjs`（我方复跑） | 0 | **tests 31 / pass 31 / fail 0** |
| 5 | `node --test …/test/{config-surface-696,smoke}.test.mjs`（提交后复跑） | 0 | **tests 39 / pass 39 / fail 0** |
| 6 | `node tooling/check-boundaries.mjs` | 0 | `boundaries: PASS` |
| 7 | `node --test test/plugin-p10-boundaries.test.mjs` | 0 | **tests 13 / pass 13 / fail 0** |
| 8 | 渲染台 `--panels plugin-memo-ilife --check`（我方复跑，见第四节） | 0 | **4 页全绿** |
| 9 | 变异自证（`settings.ts` 的 `db.name`→`db.names`，重建后跑本票用例） | — | `MUTANT-RED fail=5 exit=1` ／ `RESTORED-GREEN fail=0 exit=0`（还原后 `src` 与 `dist` 的 sha256 与变异前同值） |

## 三、真机三连读数（票面验收第 1 条）

```
PASS plugin-memo-ilife 改前="memo.db" 改后落盘=true 新进程再读="t696-memo-probe.db" 还原="memo.db" 文件=C:\Users\辰辰洋洋\.ilife\memo.yaml
MODE=真机（默认落点 ~/.ilife）  RESULT: PASS 1/1
```

## 四、渲染台读数（我方复跑，独立于实施席）

```
PAGE plugin-memo-ilife 行数=8 常用=4 宽=360/720
PASS plugin-memo-ilife-default-360.html / -expanded-360 / -default-720 / -expanded-720
     行=8 overflowX=0 改='db.dir'→"渲染台探针值" 调用=["config.get","config.save","config.get","config.reset","config.get"]
设置页渲染台自检：4 页全绿（exit 0）
```

## 五、逐条对验收

| 票面验收 | 读数 |
| --- | --- |
| 保存后重启宿主仍在（三次读数） | 第三节 |
| 「重置为默认」＋重置前 `.bak` | 用例 E 组；渲染台 `resetDispatched` |
| 页里没有干活入口 | 用例 F 组（源码级）＋ 渲染台 `noWorkEntry` |
| `vision_router` 逐页打分 ≥90 | 汇总件第四节：**备忘 · 配置 90**（扣分点：高级折叠的控件感偏弱／输入框无占位提示） |
| 两条边界门不红 | 读数 #6、#7 |

## 六、跨席交叠与偏差（如实记，不美化）

- **偏差 A（已处置）**：别席 `f8957dd8`（#706）在 `client.ts` 里写 `channel: RPC_CHANNEL`，六个包的 `dsh-ctx.ts` 镜像都没补那一格 ⇒ 本包 `tsc -p tsconfig.client.json` 当场红（`TS2353`，整棵树 client 侧编译断）。本席在**自己声明的** `dsh-ctx.ts` 里按 `packages/plugin-manager/src/dsh-ctx.ts:19-21` 补齐 `readonly channel?: string`，命令 2 转绿；另五包的同类红由父席处置（大厨／居家／作息三家由父席补，见提交 `e3cda0e1`；卡路里／记账两家的登记在汇总件）。
- **偏差 B（遗留，非本票引入）**：`packages/plugin-memo-ilife/test/skills-provider.test.mjs:122` 冻结 28 条速查唤醒词，技能侧现为 29（`391bd567` 2026-09-17 的 #665 加了 `{phrase:'飞书授权', key:'memo.auth'}`，这份冻结自 09-12 起没跟上）⇒ 该用例自 09-17 起红。修法一行（28→29），不在本票写集，未动；已登记在汇总件「遗留」。
- **偏差 C（跨席，提交归属）**：本票在 `src/{contract,bridge,index,client}.ts` 的四件改动被别席 `f8957dd8` 的提交连带带上（混合提交）。本票**不改写历史**，自己的提交只补它没带上的三件。
- **第四步超线报警**：**已超线，需要根据规则进行重构。** `src/client.ts` LF＝**566**（本仓插件包沿用的兄弟口径 350）。超因：这一件现在同时住着「技能功能页 ＋ 版本行 ＋ 既有取数逻辑」与本票新增的配置页三态。拆法：把配置页（`MemoConfig` ＋ 四个配置 RPC 包装 ＋ `Row`）切 `src/config-page.ts`，`client.ts` 只留槽注册与功能页；**本次先不拆**——四家同形件已各落一份，统一拆等本图收尾裁定是否提取公共层（见 `docs/plugins/plugin-chef/t696-四家同形清单.md`），且该件当刻正被 #706 在途叠改。其余件均在线内：settings 140／dsh-ctx 119／index 127／contract 95／bridge 154。

## 七、遗留

1. 四家 `client.ts` 的同形版式是否提到公共层——本图收尾统一裁，清单见 `docs/plugins/plugin-chef/t696-四家同形清单.md`。
2. 本包 `test/skills-provider.test.mjs` 的 28／29 冻结红（偏差 B）——属备忘录技能面，待其归属票认领。
3. 页签槽 options 的 `channel` 目前只作**镜像格**（总管侧读用），本包未加任何运行时分支。
