# 票 #706 证据：配置体检 ＋ 健康总览

日期：2026-09-18（本席机器读数）· 票：<https://github.com/FeatherHunter/ilife/issues/706> · map：<https://github.com/FeatherHunter/ilife/issues/671>

**判据来源**：检查表 `docs/research/check-table-671-life-panel-20260917.html`（27 条候选：六家通用 5 条 ＋ 各家特有 22 条；作息「第二份库」已由编者撤回）、`docs/research/t698-old-skills-check-survey.md`（老技能的写探针做法、表数门槛、飞书三档）、`docs/research/t692-six-skill-paths-survey.md`（六家路径类取值现在住哪）。

## 一、形状：为什么判据住技能侧

票面「开工前的形状裁定」第一条。一句话的理由：**每条检查项的有效值只有技能源码算得出来**（例：`resolveDbDir()` 里「`db.dir` 空串＝按默认落点」，落点是 `configPaths(stem).dataDir`；库文件名、产物目录名、包内预置件的位置同样只在技能里）。插件包对技能的全部能力是 spawn `cmd_read` 与读 stdout 信封，它看不到这些算式；判据若落在插件层，就得把六家的算式各抄一份进六个插件包，`#695` 一落地就集体走散。

于是三层分工：

| 层 | 干什么 | 判据/文案归谁 |
| --- | --- | --- |
| 技能侧（六家各一件 `src/health.ts` ＋ `src/cli/health.ts`） | 真的读盘、算有效值、出判据 | **判据与文案的唯一真相** |
| 插件包（六家各一件 `config.check` 端点） | 把技能那份报告原样交回面板 | 一个字都不重写 |
| 总管（`src/health-*.ts`） | 一次取数、总览一行、各家一张表 | 零特例：不认任何一家，只认报告形状 |

报告形状的面板侧镜像住 `packages/plugin-manager/src/health-contract.ts`；技能侧那份住 `packages/skill-calorie/src/health.ts`（其余五家同形）。

## 二、交付物

| 路径 | 动作 | 一句话 |
| --- | --- | --- |
| `packages/plugin-manager/src/health-contract.ts` | 新建 | 报告形状（`HealthItem`／`HealthReport`）＋ 三档聚合（`worstStatus`／`countByStatus`）＋ 形状守卫 `isHealthReport`；三档语义写在件头 |
| `packages/plugin-manager/src/health-fetch.ts` | 新建 | 取数：按各家的通道调 `config.check`，一家一次、25 s 有界超时（技能侧 spawn 上限 20 s） |
| `packages/plugin-manager/src/health-panel.ts` | 新建 | **一份快照**：总览那行与各家那张表都从它读（验收第三条因此是结构性质，不靠自觉）；按需取数、无轮询 |
| `packages/plugin-manager/src/health-view.ts` | 新建 | 两个画法：顶部总览（六盏灯 ＋「体检一次」）与各家那张表（每条一行：灯／标题／档／一句话／去哪修） |
| `packages/plugin-manager/src/client.ts` | 改 | 三处：账本投影多读 `channel` 一格、总览挂在页签条之前、每家页签面板里接那张表 |
| `packages/plugin-manager/src/dsh-ctx.ts` | 改 | 页签槽 options 与账本条目镜像多一格 `channel`（各家注册时写进来，总管源码里不出现任何一家的通道名） |
| `packages/skill-calorie/src/health.ts` | 新建 | 卡路里 12 条判据（通用 5 ＋ 自家 7）＋ 只读解析（不落默认配置、不建目录） |
| `packages/skill-calorie/src/cli/health.ts` | 新建 | `calorie.config.check` 的 CLI 包封（envelope 一行） |
| `packages/skill-calorie/src/cli/cmd_read.ts` | 改 | 在配置 key 拦截旁边加体检那两行（进预检之前拦下） |
| `packages/plugin-calorie/src/{contract,bridge,index,client}.ts` | 改 | `config.check` 端点常量 ＋ 透传函数 ＋ 端点分发 ＋ 页签槽交出 `channel` |
| `packages/plugin-calorie/test/health-706.test.mjs` | 新建 | 卡路里真故障三条 ＋ 只报不改 ＋ 不泄露训记 KEY（真 spawn 技能 CLI） |
| `packages/plugin-manager/test/health-706.test.mjs` | 新建 | 面板侧：形状守卫、取数、总览灯、一份数据的结构断言、六家全量对齐门 |

（其余五家技能侧与插件侧同形，逐件清单见第八节。）

## 三、机器读数

### ① 卡路里真故障三条 ＋ 只报不改 ＋ 不泄 KEY

```
node --test packages/plugin-calorie/test/health-706.test.mjs
✔ 12 条齐、id 与检查表逐条对齐，每条都带标题／结论／一句话／去哪修
✔ 训记 KEY 那一条只报「配没配」，任何一段文本里都不许出现值本身
✔ 配置文件不存在时跑体检：不落默认配置、不建数据目录
✔ 库文件被改名 → 该条红，且报文里给出那个路径
✔ 数据目录写不进去 → 该条红
✔ 坏行 → 红，且报文指出错在第 4 行
✔ 库在、目录在、照片目录配了 → 12 条里没有红
ℹ tests 7  ℹ pass 7  ℹ fail 0
```

三条真故障分别对应票面验收的 ①②③：库文件改名 → `db.file` 红；数据目录被同名文件占位（写探针起不来，与「只读」同一档结论，跨平台可造）→ `db.dir` 红；配置文件第 4 行坏 → `config.file` 红，报文里带「配置件第 4 行」。

### ② 面板侧

```
node --test packages/plugin-manager/test/health-706.test.mjs
ℹ tests 12  ℹ pass 12  ℹ fail 0
```

（逐条见第四节。）

### ③ 边界门

```
node tooling/check-boundaries.mjs           → boundaries: PASS
node --test test/plugin-p10-boundaries.test.mjs   → tests 13  pass 13  fail 0
node --test test/client-bundle-48.test.mjs        → tests 21  pass 21  fail 0
```

## 四、面板侧十二条判据逐条

| 组 | 判据 | 怎么判的 |
| --- | --- | --- |
| A | 最严重那一档说了算；空报告回 `null`（不冒充绿） | `worstStatus` 单测 |
| A | 三档计数 | `countByStatus` 单测 |
| A | 形状守卫认得出合法报告、认不出缺字段／错档位 | `isHealthReport` 四条正反例 |
| B | 六家各调**自己那条通道**、端点都是 `config.check`、载荷空对象 | 注入的 call 面记录实参，逐条比对 |
| B | 一家失败不拖别家 | 一家回 `ok:false`，另一家照常有报告 |
| B | 形状认不出的回执照样报「形状认不出」 | 不返空冒充正常 |
| B | 宿主连接缺席 → 整表报错 | 不装作没这回事 |
| C | 每家用最严重那一档点灯；没报告的那盏显 `—` | `lightsOf` 三条页签的读数比对 |
| D | **产物里只有一处取数口**（`config.check` 出现次数＝1） | 扫 `dist/client.js` 文本 |
| D | 通道进了账本投影 | 扫产物文本 |
| E | 六家合起来 52 条＝通用 5×6 ＋ 各家特有 22（逐条 id 比对） | 期望值**独立转写**一份（不从各家源码生成） |
| E | 六家通用那 5 条每家都齐 | 同上 |

## 五、自证：改坏必须红、改回必须绿

| 动作 | 读数 |
| --- | --- |
| 改坏：`packages/skill-calorie/src/health.ts` 里 `db.file` 那条的标题改一个字 | 待补 |
| 还原：SHA256 回到改前 | 待补 |

## 六、门禁与必报五步

| 门 | 读数 |
| --- | --- |
| `node tooling/check-boundaries.mjs` | `boundaries: PASS` |
| `node --test test/plugin-p10-boundaries.test.mjs` | 13/13 绿 |
| `node --test test/client-bundle-48.test.mjs` | 21/21 绿 |

**必报五步第四步（超线报警）**：待交付时逐件核（本席新增件均在 350 以内；`packages/plugin-manager/src/client.ts` 与 `packages/skill-calorie/src/cli/cmd_read.ts` 本票各只加一处挂载／两行拦截，超线状况见各自包的台账）。

## 七、边界：本件替代不了什么

- 面板侧用例跑的是**注入的 call 面**与**真产物**文本，不过 DSH 的槽位渲染、不跑 React 自己的调和；真机的最后一次确认走 `vision_router` 截图打分（见第九节）。
- 技能侧用例 spawn 的是**真 CLI**（`dist/cli/cmd_read.js`），但用的是临时 `ILIFE_CONFIG_DIR`；「保存后重启宿主仍生效」那条不属本票（那是 #676 的验收）。

## 八、其余五家（同形铺开）

六家技能侧各有 `src/health.ts`（判据）＋ `src/cli/health.ts`（CLI 包封）＋ `cli/cmd_read.ts` 里那两行拦截；六家插件包各有 `config.check` 端点透传 ＋ 页签槽 `channel`。

| 家 | 条数 | 逐条 id | 全绿态读数 |
| --- | --- | --- | --- |
| 卡路里 | 12 | 通用 5 ＋ `photos.dir`／`photos.gifs`／`xunji.key`／`xunji.cli`／`xunji.stateDir`／`land.cli`／`xunji.catalog` | 见第四节（含三种真故障） |
| 记账 | 7 | 通用 5 ＋ `goals.file`／`backup.dir` | 7 绿 0 黄 0 红 |
| 大厨 | 7 | 通用 5 ＋ `templates.dir`／`scenarios.file` | 7 绿 0 黄 0 红 |
| 居家 | 9 | 通用 5 ＋ `paths.split`／`seed.file`／`key.file`／`templates.dir` | 8 绿 1 黄 0 红（黄＝`paths.split`，默认布局即产物与库分离，判据如此） |
| 备忘 | 9 | 通用 5 ＋ `media.dir`／`lark.cli`／`templates.dir`／`scenarios.file` | 8 绿 1 黄 0 红（黄＝`lark.cli` 未登录） |
| 作息 | 8 | 通用 5 ＋ `lark.cli`／`whitelist.file`／`templates.dir` | 7 绿 1 黄 0 红（黄＝`lark.cli`） |

**合计 52 条＝六家通用 5×6 ＋ 各家特有 22**（检查表「二、各家特有」那张表的 22 条；作息「第二份库」已由编者撤回，不进报告）。`packages/plugin-manager/test/health-706.test.mjs` 的 E 组就是这条对齐门：期望值独立转写一份，六家逐条 id 比对，条数与「特有 22」一起数。

**一处按新仓事实改过判据的地方（复核时抓住的）**：五家最初把老仓路径当新仓事实——`references/scenarios.yaml`、`references/seed_categories.yaml`、`category_whitelist.yaml`、兄弟目录 `公共组件/` 在新仓**都不存在**，那几条会报一片假红。新仓的对应事实是：`<包根>/src/help/sceneData.ts`（大厨／备忘，生成器读的那件）、`<包根>/src/policy/category.ts`（居家／作息）、包内 `<包根>/templates`（六家都在，件数 6/16/8/21/6/8）。改后判据档位：在＝绿，不在＝黄（生成器输入缺失，功能照常）或红（模板目录缺失，页面装载不了）。用户可见文本里老仓文件名零命中，只留在注释里当出处。

**插件侧 20 件**（`contract`／`bridge`／`index`／`client` 四件/家，除卡路里那家已随第一个提交落地）：端点常量 `RPC_ENDPOINT_CONFIG_CHECK`、体检 key `CONFIG_CHECK_KEY`、透传函数 `readConfigHealth()`、端点分发一行、页签槽 `channel`。厂家自造的字符串只有那一处（技能侧那条只读命令名），六家各写一份、值相同。

## 九、渲染台与截图

`docs/plugins/plugin-manager/t679-面板渲染台.mjs` 已扩出体检那两块，两宽全绿：

```
node docs/plugins/plugin-manager/t679-面板渲染台.mjs --check
PASS panel-360.html  checks={…,"healthOverview":true,"healthSameData":null} tabs=6 overflowX=0
PASS panel-720.html  同上
```

新增判据两条：`healthOverview`（总览框在、那个按钮在、灯 0 盏或 6 盏、在盒内）与 `healthSameData`（取到报告后六盏灯各有档位文字、各家那表有行；没取数的变体记 `null`＝不适用，不算它绿）。渲染台另开了一道页内口子 `window.__T679_SETTLE__()`（替身没有真事件环，取数完成后调它一次即把灯与表落到有读数的形态；渲染台自己不用它，出图页用）。

### 截图打分：**未过**（最新实测 78–82，票面要求 ≥90）

出图步骤：`.scratch/t706/render-health.mjs` 用**真的判据渲染件**（`packages/plugin-manager/dist/health-view.js` 的 `HealthOverview` 与 `HealthTable`）＋ 一份真形状的报告数据（六家：多数绿、那几处该黄的黄），渲成 `.scratch/t706/health-view.html` → `vision_html_screenshot` → `vision_describe` 打分。

为什么不走出图页点击那一条：渲染台的 hooks 替身没有真事件环，取数那一步不落定（下面「根因」那段）。本件绕开的是**事件环**，不是判据与渲染件——组件与数据都是生产那两份。

| 轮次 | 分数 | 评审原话要点 |
| --- | --- | --- |
| 改前（灯 8px、计数用灰字、没有整行底色） | 78 | 六盏灯的对比与可读性弱；说明文字啰嗦；出图当刻是「还没体检」态 |
| 灯改成 10px ＋ 按档位给边框与底色 ＋ 计数加粗 ＋ 说明句缩短 | 64 | 出图页没落定取数，拍到的是空态（灯全是 `—`），「一屏看红黄」无从评 |
| 体检表红黄两档**整行上底**、档位词上档位色、绿行保持淡 | 78–82 | 已接近成品：六盏灯整齐、黄绿一眼可辨、家名 ＋ 计数清晰；扣分点是黄条底色只铺到左半行、右半区留白不均 |

**根因（下一手要治的那个）**：渲染台页面里点那个按钮，取数不落定——`run()` 排进队列的 `then` 没人跑（诊断位 `__T706_RUN__` 空），所以渲染台那两宽里的体检变体拍到的都是空态。渲染台本体已开了一道 `window.__T679_SETTLE__()` 的口子（取数后调它即重渲），出图页也试了排空队列，仍未走通；**这就是唯一没过的验收项**。

下一手三件：① 把出图改成真事件驱动（让渲染台替身按真事件环跑 `then`，或给 `health-panel.ts` 的取数留一个可注入的接缝，让出图页直接注入假答复）；② 顺手治黄条底色不满行这条版式问题；③ 重出图重打分到 ≥90。



## 十、一处提交范围的教训（本席自己犯的）

第三个提交 `f8957dd8`（六家插件包透传，20 件）里，`plugin-memo-ilife` 的 `bridge.ts`／`client.ts`／`contract.ts`／`index.ts` 四件**同时带进了 #696 那一席在途未提交的**内容——本席只加了体检那几行，但那一席的改动当时就在工作树里同一批文件上。协议要求「提交只含自己的件」；正确做法是在 `git add` 前对每个文件先看一眼 `git diff` 是不是只有自己那几行。已在票面如实记下：那四件里 #696 的部分（`config.get`／`config.save`／`config.reset` 三个端点的完整接线）现在随本席的提交入库，若那一席还要继续改，以 HEAD 为基线继续即可；后续本席不再碰 `plugin-memo-ilife` 的这四件。

