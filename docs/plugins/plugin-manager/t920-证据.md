# #920 证据 · 配置面板照 v3.1 落地（阶段二：形状票）

判定：**PASS（机器判据全绿 ＋ 真机已换形）**。与 v3.1 有 **5 处已知不同**，逐条写在 §5，全是「照真源或照锁」二者必居其一的结果，没有一处是我自己配的。

**外观真源**：`docs/agents/配置区域视觉模型-v3.1.html`（票面：「视觉真源只有一件」；阶段一已入仓）。
**本票只改共用件一处 ＋ 包内判据**；六家源码、技能侧、失败支形状、对外出口名：一个字节没动。

---

## 1 改了什么

| 文件 | 一句话 |
| --- | --- |
| `packages/plugin-manager/src/config-panel-view.ts` | 样式表 `S` 从 24 项扩到 45 项、逐项照 v3.1 取值；`Row` 换成 v3.1 的两套行模板（只读行 `.ic-row`／可改行 `.ic-master`）；`PanelBody` 加卡片标题行的状态徽标、头部两行改成 `.ic-file` 那种带 `<b>` 前缀的写法、高级组换成 `.ic-group` 那一套、底栏照 `.ic-bar`（三键在左、那半句话靠 `margin-left:auto` 顶到行尾） |
| `packages/plugin-manager/test/config-panel-920.test.mjs`（新增，24 条） | 形状判据：只读行／可改行／高级组／底栏／七档徽标／样式表取值锚点／几何（不按字符数估宽） |
| `packages/plugin-manager/test/config-panel-908.test.mjs` | 3 条与新形状冲突的断言**改判**（理由写在文件里，一条没删） |
| `test/panel-copy-743.test.mjs` | 跨包锁 ⑦ 里 1 条与新形状冲突的断言**改判**（同上） |
| 七个包的 `dist/client.js` | 重建（`dist` 是 gitignore 的，不进提交）；实测六家束里都带上了新面板码 |

---

## 2 机器读数（三条，逐条 exit 0）

```
GATE-RUN runId=caae924b-bc18-4ebc-88d8-7362ccfce72e cmd=node tooling/run-locked.mjs --ticket 920 -- pnpm -C packages/plugin-manager run test
  ℹ tests 209 / pass 209 / fail 0   exit=0

GATE-RUN runId=c3121d37-3b08-4461-a746-eeffd65e5b73 cmd=node tooling/run-locked.mjs --ticket 920 -- node --test test/panel-copy-743.test.mjs test/panel-type-739.test.mjs
  ℹ tests 14 / pass 14 / fail 0     exit=0

GATE-RUN runId=d18b3d32-5150-42df-8062-7873340deb54 cmd=node tooling/run-locked.mjs --ticket 920 -- node --test test/plugin-p10-boundaries.test.mjs
  ℹ tests 17 / pass 17 / fail 0     exit=0
```

七包重建（`pnpm -C packages/<包> run build:client`）七次 exit=0；产物面读数：六家 `dist/client.js` 里都出现新面板的字面（`没有未保存的改动`／`可改`），旧记号（`已改动`）**零命中**。

---

## 3 形状对照（主判据）：v3.1 ↔ 实现，逐项写「同／不同＋差在哪」

比对件：真机双端（`.scratch/t920/shots/dsh-bill-ilife-top.png`／`-bottom.png`）＋ 渲图台同几何双端（`.scratch/t920/bench-564.html`／`window-top.html`／`window-bottom.html`，卡片宽 564＝真机内容列实测值）＋ 七档状态墙（`.scratch/t920/states.html`）。**不许写「基本一致」，逐项如下：**

| # | 票面要的 | v3.1 长什么样 | 实现 | 判 |
| --- | --- | --- | --- | --- |
| ① | 只读行＝标签—值**同行** | `.ic-row` 三列栅格 | 真机读数 `sameRowValues=3/5`（其余 2 行走让位那一档，见 ④） | **同** |
| ①a | 值**右对齐** | **v3.1 全件零 `text-align`，值是左对齐**（渲图确认） | 左对齐（**不自加 `text-align`**） | **同**（票面文字「右对齐」与真源不符，按真源；见 §5-1） |
| ② | 每行一枚「复制」 | `actionsHTML()` 每行一枚 | 真机读数：5 条只读行各恰一枚、`readonlyExtraButtons=0`；行尾那枚就是它 | **同** |
| ③ | 可改行按钮字面恰「浏览文件夹」＋「复制」 | `masterActionsHTML()` 两个字面 | 真机读数 `browseButtons=2`／`readonlyBrowseButtons=0`；包内判据逐字断言两枚 | **同** |
| ④ | 长值整行让位、零省略号 | `.is-wide`：值落第二行、按钮留第一行行尾 | 真机读数：`HELP 产物目录名` 与 `备份文件名前缀` 两行 `sameRow=false`（值独占一行、复制仍在第一行行尾，见截图）；`clippedValues=0`；面板全文 `…` 0 处、`...` 0 处 | **同** |
| ⑤ | 「可改」徽标 | `.ic-chip`（`--ic-accent` 描边小胶囊） | 在位（截图与判据都在） | **同** |
| ⑥ | 高级组收起 | `<details>` 默认收起 | 真机读数 `advanced.open=false`，`summary="›高级不常改。留空＝用默认值。"` | **同** |
| ⑥a | 展开箭头会转 90° | `.ic-group[open] > .ic-ghead::before{transform:rotate(90deg)}` | 静态「›」（内联样式给不出伪元素与 `[open]` 选择器） | **不同**：少了那一转，字形与位置同；见 §5-3 |
| ⑦ | 底栏三键贴下沿吸住 | `.ic-bar` 本身不吸底；`bottom:6px` 那条吸底口径在 `.ic-attnbar.is-sticky` | 真机读数 `position=sticky`／`bottom=0px`／`paddingTop=12px`／三键字面齐 | **不同（一处取值）**：吸底取 `0` 而不是 `6px`——票面写的是「贴下沿」，取 0 才与下沿齐平；另见 §5-4 的容器边界说明 |
| ⑦a | 三键在左、那半句话在右 | `.ic-bar .ic-note{margin-left:auto}`，说明是**最后一个**孩子 | 真机：左起 `保存／重置为默认／重新读取`，行尾 `没有未保存的改动`（渲图台第一版把顺序写反了，已修并立判据） | **同** |
| ⑧ | 状态徽标那七档 | `SKILL_STATES`／`badgeHTML()` | ①就绪→不画；②有改动→琥珀实底「N 项未保存」；③写入中→琥珀「正在写入」；④已保存→绿描边；⑤值非法回落→琥珀「配置里有值用不了」；⑥写入失败→红实底「写入失败」；⑦读取失败→**那一档把整面换成「一整屏错误＋恰两枚按钮」** | **同**（逐档截图见 `states.html`；③的转圈动画见 §5-3，④的字面见 §5-5） |
| ⑨ | 样式表照抄、不许自己重新设计 | v3.1 的 CSS（`:32-205`） | `S` 45 项平铺；判据把 29 处可判取值逐个锚住（`padding:14px`／`border-radius:12px`／`11px 12px`／`7px 0`／`4px 10px`／`--ic-kw:118px`／`padding-top:12px`…），另有 13 条主题别名逐一点名 | **同**（字号一档见 §5-2） |

---

## 4 像素读数

`vision_pixel_diff`。两张图都渲在**同一几何**里（同一卡片宽、同一左缘），基准字号取 `#739` 那对锚点隐含的 12.5px（`13.5px↔1.08em` ⇒ 1px＝0.08em），这样比的是形状而不是「宿主字号差多少倍」。

| 比的是什么 | 差异比 | 最差区块 | 读法 |
| --- | --- | --- | --- |
| 全卡 550×440（按卡片左上角对齐） | **22.12 %** | `x69..345,y110..165`（54–64 %）＝母版行那一带 | 见下面三条解释 |
| 母版行那一块 550×130（按**行顶**对齐） | **11.23 %** | `x69..138,y102..119`（51 %） | 去掉位移后的纯差 |
| （对照档）基准字号取 14px 时 | 25.47 % / 27.19 % | 同 | 比 12.5px 档各高 3–16 个百分点：**字号档是差异里最大的一块**（见 §5-2） |

**差异集中在哪儿（三条，逐条可对上叠图）**：

1. **整块 17px 竖向位移**——本件头部比 v3.1 多一行「数据目录」；叠图上「配置文件 …」那一行出现两次、相距 17px，下面每一行都跟着错位。这是**信息量的差**（真机一直有这两行，是别票定的），不是外观配错。
2. **只读行不缩进**——v3.1 的只读行住在 `.ic-under`（`margin-left:8px＋padding-left:13px`＋一条左边线）里，本件是平铺的：叠图上标签出现两次、横向差 21px。**这一层共用件做不出来**：`.ic-under` 的语义是「这一行跟着上面那个可改行」，静态跟随表住在六家各自的 `client.ts` 里，共用件只拿得到 `followKeysOf(dirtyKeys)` 这个**当场算**的钩子，拿不到静态映射（票面「六家同形面那三件不是一处改的东西照旧各自住各家」）。
3. **母版行下面那格空槽**——v3.1 演示件留了一格慢面骨架（`.ic-meta` 那条灰块），母图已裁「不实现，原型保留」；本件没有它，母版行矮一截。

剩下的都是**亚像素级**：卡片底色／圆角／母版行盒子／按钮／下边线在叠图上重合，没有一片成块的配色差。

---

## 5 与 v3.1 的 5 处已知不同（逐条给理由）

1. **值列左对齐**（票面文字写的是「右对齐」）。v3.1 全件**零 `text-align`**，渲图里值就是左对齐、行尾才是那枚「复制」。票面同一条又说「视觉真源只有一件…逐项照搬」，故按真源取左对齐，**不自加一条真源里没有的规则**（那是「重新设计」）。—— 父席已确认这一条按我的判读走。
2. **字号一律 em（真机上比 v3.1 的 px 大 12 %）**。跨包锁 `#739` 钉死「零 `font:` 简写／零字族／字号一律相对单位」且逐项钉死 7 个值，票面又把「跨包锁的判据文字」列进不许动。故沿用锁里那两对锚点换算（**1px＝0.08em**），一个取值不丢。**真机实测宿主基准字号＝14px**（`baseFont=14px`，标题渲成 15.12px、值 12.32px），而锚点隐含的基准是 12.5px ⇒ 真机上每一档都比 v3.1 的 px 大 12 %。**这一格是「照锁」与「照 v3.1 的 px」唯一的正面冲突**，修法两条（改锁那 7 个锚点／或在共用件里钉基准字号）都会动别的票的判据，本票不动，如实记账。
3. **三样「内联样式画不出来」的**用等价形态顶上：分组箭头 `::before` → 真字符「›」；`[open]` 的 90° 旋转 → 不转；`@keyframes ic-sp` → 静态环。形状与位置同，只少了那点动。父席已同意。
4. **吸底取 `bottom:0` 而不是 `6px`**。v3.1 唯一的吸底口径是 `.ic-attnbar.is-sticky{bottom:6px}`（那是它那条琥珀告警条的），`.ic-bar` 本身不吸底；票面要的是「底栏三键贴下沿吸住」，故取 0 与下沿齐平。另有一条**真机事实**要记：真机里面板卡片只是设置窗滚动体里的**一块**（后面还有「体检」「作者其他插件」），`position:sticky` 被容器边界夹住 ⇒ 记账这一家卡片比可视区矮，底栏停在卡片下沿（`rectBottom=599`，滚动体可视下沿 900）。真要看「吸住」那一档，得卡片比可视区高（`states`／`window-bottom` 两处渲图里有：底栏钉在下沿、内容从它后面滚过去）。
5. **④那一档的字面**：v3.1 写「已保存 · 刚刚」，「刚刚」是演示里的时刻；本件取档名「已保存」。⑤那一档（值非法回落）本件**有档位、没有生产者**——它要「技能侧交出『这个值用不了』这件事实」（#915 第二步），在那之前恒不亮，与票面「遗留出口」一字不差。

---

## 6 真机读数（scaffold 宿主 43911，`dsh-bill-ilife`）

```
cardWidth 564（＝票面实测的内容列宽）
headText  "饼干记账 · 配置"        badge null（就绪态不画徽标）
只读行 5 条：readonlyWithInput=0 · 每行恰一枚复制 · readonlyBrowseButtons=0 · readonlyExtraButtons=0
            clippedValues=0 · sameRowValues=3（另 2 行走整行让位）
advanced  open=false · summary="›高级不常改。留空＝用默认值。"
bar       position=sticky · bottom=0px · paddingTop=12px · [保存,重置为默认,重新读取] · note=没有未保存的改动
ellipsis  …=0 · ...=0
baseFont  14px（宿主）· titleFont 15.12px · valueFont 12.32px
valueFontFamily  "JetBrains Mono", "Fira Code", "Cascadia…
```

最后一行是一条**顺带捞到的关键事实**：真机继承下来的字族**本身就是等宽栈**（宿主设置面用的就是等宽那一路）⇒ 值列虽然照 `#739 ①` 没写 `fontFamily`，真机上渲出来**仍然是等宽**。v3.1 要的那一点在真机上并不缺；`#739 ①` 那条锁值不值得开例外（值列一处允许等宽），因此**没有实测依据支持**了——如实登记，供父席与维护者裁。

---

## 7 变异自证（红／还原两行）

改法：把只读行那一支换回**旧形状**（标签／说明／整行输入框三行块、值装在一格 disabled 输入框里），其余一字不动。

```
GATE-RUN runId=77caaf23-dd1e-4507-b77e-082f17a790fa  tsc -b packages/plugin-manager        exit=0
GATE-RUN runId=0b158d00-56eb-44f8-be9b-003da36ebe95  pnpm -C packages/plugin-manager run test
        → ℹ tests 209 / pass 202 / fail 7   exit=1      ← 必红
        红的 7 条：#920①「就三件…」「那一行说明不再画在只读行里」「跟着更新的那一行…」「行的容器是换行弹性盒…」
                   ＋ #908「只读目录行…也没有可改控件」「只读行（非目录）同样没有可改控件」「脏行带记号…」
GATE-RUN runId=2e9dffb2-cfaf-44f3-8520-098d4bff1bc3  node --test test/panel-copy-743.test.mjs test/panel-type-739.test.mjs
        → ℹ tests 14 / pass 13 / fail 1   exit=1       ← 必红（跨包锁 ⑦ 点名「只读行不该画输入框」）
```

```
GATE-RUN runId=b6e4720a-9038-4754-ad80-b42e1e79363c  tsc -b packages/plugin-manager        exit=0
GATE-RUN runId=b0fcc000-63eb-4dd5-9e31-3723e2a77254  pnpm -C packages/plugin-manager run test
        → ℹ tests 209 / pass 209 / fail 0   exit=0     ← 必绿
GATE-RUN runId=ce27d734-10e4-4508-86e7-6bc438ac95ff  node --test test/panel-copy-743.test.mjs test/panel-type-739.test.mjs
        → ℹ tests 14 / pass 14 / fail 0     exit=0     ← 必绿
```

还原是**逐字节**的：变异前 `sha256=8A7E42BD68EF…`，注入后 `677E6D2E…`，还原后**回到** `8A7E42BD68EF…`（脚本 `.scratch/t920/mutate.mjs`，注入／还原都打印前后哈希）。

---

## 8 改判了哪几条既有断言（一条没删，逐条给理由）

| 文件 | 原断言 | 改判成 | 为什么 |
| --- | --- | --- | --- |
| `config-panel-908` | 「只读目录行…控件改不动」＝有一格 disabled 输入框、不接 onChange | 零 `input` ＋ 值原样上屏 | 老断言咬的是**老形状**的记号（disabled 输入框）。v3.1 的只读行压根没有可改控件——同一件性质由「零 input」给出，比 disabled 更硬。判据条数不减（一条拆成两条） |
| `config-panel-908` | 「只读行（非目录）同样改不动」同上 | 同上 ＋ 零目录入口 | 同上 |
| `config-panel-908` | 「脏行带记号」＝行里出现「已改动」三个字 | 输入框的注意力描边（2px ＋ 光晕）＋ 标题行徽标的「N 项未保存」 | v3.1 的脏记号**不是一行字**（`.ic-master input.is-dirty` 与 `badgeHTML()`）。判据条数不减（一条拆成两条） |
| `test/panel-copy-743`（跨包锁 ⑦） | 只读目录行「应有一格输入框 ＋ disabled ＋ 不接 onChange」 | 零 `input` ＋ 值原样上屏 ＋ 恰一枚「复制」 | 同上；锁的主判据（**零目录入口**、可改行恰一枚）原样留着。头部注释与用例里都写明了改判理由 |

---

## 9 范围外／没做的（如实记）

- **`.ic-under` 分组缩进**：共用件拿不到静态跟随表（见 §4-2），本票不做；要做得多一个钩子，而「不许给出口加名字」＋「六家源码不动」两条都不许。
- **慢面那两格**（`.ic-sub`／`.ic-meta`）：母图已裁「不实现，原型保留」，照旧不做。
- **⑤ 值非法回落要真亮**：等 #915 第二步（技能侧交事实）；档位与判据都在，生产者不在。
- **控件皮肤（药丸按钮／输入框形状）**：v4 单裁，本票保持 v3.1 的皮肤。
- **只截了记账一家**的真机图（票面就要记账一家）；六家产物重建成，别的家未逐家上真机。
- 真机截图走的是 `.scratch/t920/capture920.mjs`（照 `.scratch/t911/capture3.mjs` 的浏览器底座）；两处探针脚本与渲图台都只活在 `.scratch/`，不入仓。
