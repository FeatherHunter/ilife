# 票 #738 证据：面板页签名补全成产品名 ＋ 删掉那行说「总开关」的静态文本

日期：2026-09-20（本席机器读数）· 票：<https://github.com/FeatherHunter/ilife/issues/738> · 提交：`<见回执>`

## 一、判定

**仓内判据全绿**：本票自己的门 11/11（`packages/plugin-manager/test/tabs-738.test.mjs`），边界门里本票那条
「导航表的页签名与各家契约件逐家一致（#738）」绿；面板用例 75/75；全仓 `tsc -b` exit 0；五包指纹绑定一致；
四个变异按设计红、还原逐字节一致后再跑回绿。

**一条范围外的既有红**：边界门总 exit=1 ——「单品→总管单向（总管零单品依赖）」红在
`plugin-calorie 总管硬依赖口径`：`^0.2.6` ≠ `^0.2.7`（总管包已 0.2.7，六单品仍写 `^0.2.6`）。本票**一个
`package.json` 都没碰**，这条是定版留下的版本线偏斜，与 #736／#737 证据里记的是同一处，归发版窗口。

**装机点屏不在本席**：票面验收的「屏上逐字印产品名」在仓内已由**真产物渲面板**读到（见第五节），
装机看屏要等下一窗发版装上（四家包要动，票面已写明发版不属本单）。

## 二、票面两件与改法

| 件 | 改法 |
| --- | --- |
| 页签名补全 | 四家自己的 `SLOT_TITLE`：作息→**作息管家**、居家→**居家管家**、大厨→**私家大厨**、记账→**饼干记账**（备忘录／卡路里本来已是产品名，不动）；总管导航表 `MANAGER_TABS.title` 四行同值跟上 |
| 名字为什么有两处产地 | 各家常量是主（页签注册交出去的 label、侧边栏页签名、本家设置页标题都从它出）；总管导航表那格是**镜像**（页签还没注册时兜底，更新列表那一行的行首也用它，`update-targets.ts` 原样取 `title`）。只改一边，屏上就一半长名一半短名 |
| 去掉那行静态文本 | `packages/plugin-manager/src/client.ts` 的 meta 区里那行（说有个「缺省启用、只读的开关」）**原地删除**，不留空行残影；同段的「爱生活」标题与版本行照旧。全仓没有任何代码读它或写它，设置页的行表里也没有「启用」这一项 |

顺带补了一行**清单外**的件（见第九节交付对账）：`docs/plugins/plugin-manager/t679-面板渲染台.mjs` 里那份
假账本的页签名（短线四行），它是面板渲染台的输入，不跟上就会渲染出与真装机不一致的页。

## 三、修复前读数（票面现象的复现）

`.scratch/t738/复现-页签名.mjs --标签 修复前`（读的是**产物**，改源码未重编时就是这个读数）：

```
注册页签名  plugin-schedule-ilife  作息     期望=作息管家 BAD
注册页签名  plugin-home-ilife      居家     期望=居家管家 BAD
注册页签名  plugin-chef            大厨     期望=私家大厨 BAD
注册页签名  plugin-bill-ilife      记账     期望=饼干记账 BAD
面板页签（账本空）      ["备忘录","卡路里","作息","居家","大厨","记账"]
面板页签（账本有行）    ["备忘录","卡路里","作息","居家","大厨","记账"]
屏上还有那行静态文本    BAD（还在）
短名字面量残留  四家 BAD（client.js, slot.js）
RESULT: FAIL 不符项=10
```

## 四、修复后读数

`.scratch/t738/复现-页签名.mjs --标签 修复后`（runId=`7b9f13be-fd38-4bf0-926c-4c2af01e703e`，exit=0）：

```
注册页签名  plugin-schedule-ilife  作息管家   期望=作息管家 OK
注册页签名  plugin-home-ilife      居家管家   期望=居家管家 OK
注册页签名  plugin-chef            私家大厨   期望=私家大厨 OK
注册页签名  plugin-bill-ilife      饼干记账   期望=饼干记账 OK
面板页签（账本空）      ["备忘录","卡路里","作息管家","居家管家","私家大厨","饼干记账"]
面板页签（账本有行）    ["备忘录","卡路里","作息管家","居家管家","私家大厨","饼干记账"]
屏上还有那行静态文本    OK（已消失）
总设置区没被误伤        OK（标题与版本行都在）
短名字面量残留  四家 OK（无）
RESULT: PASS 不符项=0
```

## 五、判据（`packages/plugin-manager/test/tabs-738.test.mjs`，11 条）

| 组 | 条目 | 咬什么 |
| --- | --- | --- |
| ① 页签名与设置页标题（1 条） | 逐家：`ilife.config-tab` 那次注册的 label ＝ 产品名，且**真产物渲一次那一卡**的页头含「<产品名> · 配置」 | 拿真产物跑 `apply` 再渲一次，不是搜字符串。硬编码的短名（如把页头写成 `'大厨 · 配置'`）当场红 |
| ② 屏上页签条（2 条） | 真产物渲面板读页签按钮上的字：账本为空（取导航表镜像）与账本有行（取各家 label）**两条产地都走**，都是六个产品名 | 只改一处的半长半短在这里露出来 |
| ③ 更新列表行首（1 条） | `UPDATE_TARGETS` 六行行首 ＝ 六个产品名（总管自己那行仍是「爱生活」） | 那一行取的是导航表那格，漂开即红 |
| ④ 那行静态文本（4 条） | 屏上搜不到 ＋ 七家插件包 src/dist 搜不到「总开关」 ＋ packages 全树 src/dist 搜不到那句冻结原文 ＋ **那段没被误伤**（「爱生活」标题与版本行仍在屏上） | 删多删少都红：删多了「保留标题与版本行」那条红 |
| ⑤ 短名残留（1 条） | 四家改名包，短名不再以**字符串字面量**出现在 src/dist 的任何件里 | 同一个常量驱动的别的面留了硬编码短名即红 |

边界门新增那条（**跨包对齐**，照 #735 通道那条的写法，真 import 两边产物来比）：

- `GATE-RUN runId=b6f86895-8981-4d3a-94be-2416a7af4344 cmd="node --test test/plugin-p10-boundaries.test.mjs"` → 该条绿（15 条里 14 绿 1 红，红的是上面那条版本线偏斜）

渲染替身的取数口径：桩传输口一律回 `bad-request` ⇒ 设置页渲在「拿不到配置」的降级态；页头那行**两支都画**
（成功支与失败支各有一处 `${SLOT_TITLE} · 配置`），所以这份屏上读数不是空页。

## 六、变异自证（红／还原两行，`runId=91537ba2-5acd-4291-b19a-2f25cadd4968`，exit=0）

全程在**同一个加锁窗口**内做，释放锁前逐字节复原（协议 §5）；门只取本票自己的判据
（`tabs-738.test.mjs` 全件 ＋ 边界门里 `--test-name-pattern "#738"` 那条）。

| # | 变异 | 期望红 | 实测 |
| --- | --- | --- | --- |
| M1 | 总管镜像表那格改回短名（只改一边） | ②镜像那条 ＋ ③更新列表那条 ＋ 边界门对齐那条 | exit=1，三条全红 ✓ |
| M2 | 各家常量改回短名（只动作息那家） | ①页签名那条 ＋ ②账本那条 ＋ ⑤字面量那条 ＋ 边界门对齐那条 | exit=1，四条全红 ✓ |
| M3 | 把那行静态文本加回面板 | ④三条全红 | exit=1，三条全红 ✓ |
| M4 | 设置页标题硬编码 `'大厨 · 配置'`（两处页头都换） | ①页头那条 | exit=1，该条红 ✓ |
| 收尾 | 四个变异各自：还原 → 重编 → 再跑 | 逐字节一致 ＋ 全绿 | sha256 逐条相同；两门 exit=0/0 ✓ |

M1／M2 就是「只改一处产地」那条路：镜像与各家常量谁漂开，都当场红。

## 七、对抗式自查

| 轴 | 问 | 读数／处置 |
| --- | --- | --- |
| 边界 | 短名是产品名的子串（作息 ⊂ 作息管家） | 判据一律咬**逐字相等**（label ＝ 产品名）与**字符串字面量**（`'作息'` 这种带引号的形态），不做「不含子串」的软断言——否则产品名自己就把自己判红 |
| 边界 | 删那行会不会把整段删掉 | ④单独一条咬「标题与版本行仍在屏上」；M3 反向证明它咬得住 |
| 边界 | 装机上的老产物 | 装机现在那份产物没有本票代码，屏上仍是短名；下一窗发版装上即变（票面已写明发版不属本单代码面） |
| 异常分支 | 账本里没有某家（没装／产物没注册页签槽） | ②第一条走的就是这条路：页签字取总管镜像，仍是产品名（不会退回空白） |
| 异常分支 | 账本里有行但通道那格是空串（#735 的装机实况） | 渲染替身给的就是 `channel: ''`：灯显缺席态、页签字照印产品名 |
| 回归 | 设置页标题、页签描述子、侧边栏页签名是不是也跟了 | ①（label ＋ 页头屏上文本）＋ 边界门（镜像 ≡ 各家 `SLOT_TITLE`）；`slotDescriptor().title` 与 `registerSingle` 的 title 都取同一个常量（源码侧一处定义） |
| 回归 | 别的票在用的渲染替身 | `version-737.test.mjs` 里的那份本地替身提成了共用件 `test/helpers/panel-render.mjs`，该门 8 条改写后仍全绿（75/75 那轮里） |
| 回归 | 面板其余文案有没有被带歪 | 面板用例 75/75 绿（#679 头部/底部、#706 体检表、#732 页签灯、#737 版本行都在内） |
| 并发 | 改的件有没有别人在写 | 本席动手前量过现场：`plugin-*/src/dsh-ctx.ts`／`index.ts`／新增 `skill-tool.ts` 是 #734 在飞（本席一个没碰）；四家 `slot.ts` 的 diff 里**只有本票那行**（见第十节） |
| 我会漏在哪 | — | ① 装机点屏要发版，本席看不到；② 「总开关」这四个字仓里还剩一处**别的概念**（`packages/skill-calorie/src/xunji/run.ts:64` 的拉取限频开关，注释里的话），本票边界不动技能包，如实记在这里；③ 全量 `pnpm test` 没跑（第十一节）。三条都写明 |

## 八、范围外读数（如实记，不是本票的病）

1. **版本线偏斜**（既有红）：`plugin-manager` 0.2.7 对六单品 `^0.2.6` ⇒ 边界门 1 红（`runId=b6f86895-8981-4d3a-94be-2416a7af4344`）、装门 1 红。本票未碰任何 `package.json`；#736／#737 证据已把它记在同一处，归发版窗口。
2. **插件面 387 条 9 红**（`runId=6874c24b-3121-4f69-9275-d97873787196`）：3 条版本线偏斜；2 条 `SkillBridgeError`（记账／卡路里的 `readViaCli`：一条是「测试缺隔离：没设 `ILIFE_CONFIG_DIR`」、一条是 CLI 起崩）；1 条 `skill-bill` 的 `./policy` 面不在包 `exports` 里（#703 收窄公开面的连带）；1 条备忘录「速查表唤醒词条数变了 29 ≠ 28」；其余同族。**没有一条读本票碰过的件**。
3. **仓根门 154 条 19 红**（`runId=0b6aab8e-0991-4cf5-88a2-beffba438f26`）：全是唤醒词路由层／calorie parity／skilllink 18 键／场景 05 读写那几族（用例名里自带「他席在途」字样）＋ 上面那 2 条版本线偏斜。本票那条 #738 对齐断言**绿**。
4. **陈旧文档**（只登记，不动）：`docs/p10-scaffold.md:35` 还写着「`SETTINGS_OWNER` ＋ `SETTING_ROWS`，总开关缺省启用」——今天源码里是 `CONFIG_ITEMS`，也没有「启用」这一项。该文件属 `docs/` 根的历史归档（落点表里归 `docs/agents/`，且 `test/plugin-p10-install.test.mjs:61` 与多份文档按现路径引用它），随它的下一次改动搬位并改口径，不在本票范围。
5. **`packages/skill-calorie/src/xunji/run.ts:64`** 注释里的「总开关」是**另一个概念**（拉取限频的开关），本票边界不动技能包。

## 九、交付对账（必报五步·第五步）

| 第一步声明的路径 | 实际 | 碰它的理由 |
| --- | --- | --- |
| `packages/plugin-schedule-ilife/src/slot.ts` | 改了 1 行 | 页签名的产地：作息 → 作息管家 |
| `packages/plugin-home-ilife/src/slot.ts` | 改了 1 行 | 居家 → 居家管家 |
| `packages/plugin-chef/src/slot.ts` | 改了 1 行 | 大厨 → 私家大厨 |
| `packages/plugin-bill-ilife/src/slot.ts` | 改了 1 行 | 记账 → 饼干记账 |
| `packages/plugin-manager/src/nav.ts` | ＋9／−4 | 镜像四行同值 ＋ 给 `title` 字段补出处注释（两处必须同值、判据在哪） |
| `packages/plugin-manager/src/client.ts` | ＋2／−1 | 删掉那行静态文本（原位留一条说清「为什么删」的注释） |
| `test/plugin-p10-boundaries.test.mjs` | ＋23 | 新增跨包对齐断言（照 #735 通道那条） |
| `packages/plugin-manager/test/tabs-738.test.mjs` | 新建 142 行 | 本票自证回路（11 条） |
| `docs/plugins/plugin-manager/t679-面板渲染台.mjs` | ＋6／−5 | **清单外补一行**：渲染台那份假账本的页签名跟上，否则渲染台画出的页与真装机不一致 |
| `packages/plugin-manager/test/version-737.test.mjs` | ＋6／−110 | **清单外补一行**：本票要用同一份渲染替身，把它从 #737 的本地函数提成共用件（该门 8 条未改判据） |
| `test/helpers/panel-render.mjs` | 新建 207 行 | 上面那件共用件（两个用法：#737 屏上版本行、#738 页签条与设置页标题） |
| `.changeset/t738-页签名补全.md`、`docs/plugins/plugin-manager/t738-证据.md`、`t738-门禁运行记录.md` | 新建 | 记账与证据 |

**偏差 2 行，两行都在上面点名**（都是「让别人已经在用的东西跟上本票的新事实」，不是顺手扩大范围）；
其余逐行对上。

## 十、超线报警（必报五步·第四步）

**已超线，需要根据规则进行重构。**

| 件 | 本票改前 | 现在 | 本票净增 |
| --- | --- | --- | --- |
| `packages/plugin-manager/src/client.ts` | 491（HEAD） | **492** | ＋1 |

口径＝**LF**；参照线取兄弟包沿用的 **350**（先例 `docs/base/base-link-core/t694-证据.md:155`，
#737 证据第十一节同口径）。**超因**：这一件同时住五样活——样式表、页外壳（头部入口与底部引流卡）、
体检两块视图、更新面板接线、槽注册与页签投影。**本票只动 ＋2／−1 行**（删一行文案 ＋ 补注释）。

**这次先不拆**（两条理由）：① 拆它是一次 400+ 行文件的四分之一搬家，与「改两行文案」不相干，
混进本票会让「页签名」这条判据与「结构搬迁」互相污染；② 拆法要单独验证。
**拆法**：沿用 #679 第四节——把 `PanelActions`／`MorePluginsCard`／`ExternalIcon` 与它们那几条样式
搬去 `src/panel-shell.ts`，`client.ts` 只留样式表、页签条与投影容器。属「改进」，暂无票认领 ⇒ 记在这里，不占票。

## 十一、未做项与下一手

1. **装机点屏**（票面验收的屏上那一跳）：要等下一窗把四家单品包与总管包发出去、装上再看；本席无 GUI 点击能力。
2. **全量 `pnpm test` 没跑**：本票改动面只在插件侧与仓根门，故按判据范围跑了「七家插件包用例（387 条）＋ 仓根门（154 条）＋ 全仓 `tsc -b`」；全量套件的技能侧当刻本就有他席在途的红（见第八节），跑它只会把别人的红混进来。技能包本票**一个字没碰**。
3. **版本线偏斜**（第八节第 1 条）：归发版窗口，本票不动。

## 十二、机器读数与复跑命令

**本票声明的运行（一对一可对账，均 exit=0）**：

- GATE-RUN runId=8f3841f3-8cd1-4675-949b-e7c8a09f0135 cmd="node .scratch/t738/重建-产品.mjs"
- GATE-RUN runId=7b9f13be-fd38-4bf0-926c-4c2af01e703e cmd="node .scratch/t738/跑.mjs --日志 .scratch/t738/读数-修复后.log node .scratch/t738/复现-页签名.mjs --标签 修复后"
- GATE-RUN runId=b993917e-2431-43b5-961d-605ea2acd87a cmd="node .scratch/t738/跑.mjs --日志 .scratch/t738/全仓编译.log node node_modules/typescript/bin/tsc -b"
- GATE-RUN runId=91537ba2-5acd-4291-b19a-2f25cadd4968 cmd="node .scratch/t738/跑.mjs --日志 .scratch/t738/变异电池.log node .scratch/t738/变异电池-738.mjs"
- GATE-RUN runId=53c34d0b-4b86-499d-bbc5-b343ca579b9b cmd="node .scratch/t738/重建-产品.mjs"
- GATE-RUN runId=02c084c6-30da-4a99-b6d2-2ff24e0e3396 cmd="node .scratch/t738/指纹绑定.mjs"
- GATE-RUN runId=12f3ce80-71a6-4612-9457-7593ca2f1442 cmd="node .scratch/t738/跑.mjs --日志 .scratch/t738/收口-面板用例.log node --test packages/plugin-manager/test/*.test.mjs"

**记在册的红读数（不作门禁证据）**：

- runId=`e45eae42-769e-4433-a568-144abb2684e6`：第一次编译调用多传了一个 `--`，被 cmd 当成命令名 ⇒ exit 1（装配失误，非代码红）
- runId=`e62ab6f6-303f-4dd8-8ea7-79b081fb9776`：边界门＋装门，2 红＝版本线偏斜（第八节第 1 条）
- runId=`76e28835-7690-4201-997c-bae0d34529b1`：变异电池第一版，判据写成「只要 exit≠0 就算设计红」，把上面那条既有红当成了设计红（M4 实测没红也没抓住）⇒ 改判据后重跑得 `91537ba2…`
- runId=`6874c24b-3121-4f69-9275-d97873787196`：插件面 387 条 9 红（第八节第 2 条）
- runId=`0b6aab8e-0991-4cf5-88a2-beffba438f26`：仓根门 154 条 19 红（第八节第 3 条）
- runId=`b6f86895-8981-4d3a-94be-2416a7af4344`：收口边界门 1 红＝版本线偏斜（本票那条绿）

**五包指纹绑定**（`runId=02c084c6-30da-4a99-b6d2-2ff24e0e3396`，逐包 src 窗口内无漂移）：

```
packages/plugin-schedule-ilife  src-sha=f842cd1cde046ea5 dist-sha=28cbc5f5f1dbe49e 一致
packages/plugin-home-ilife      src-sha=bd43843379cc4a51 dist-sha=e5562d628998db8b 一致
packages/plugin-chef            src-sha=6e9e4f580fb507db dist-sha=db2a8e6d510d93cb 一致
packages/plugin-bill-ilife      src-sha=7bc2dee916ad25d9 dist-sha=0a19f197312fb72c 一致
packages/plugin-manager         src-sha=efdd356a32df7121 dist-sha=8f5c666948876bd5 一致
```

**复跑**：

```powershell
cd D:\ilife
node tooling/run-locked.mjs --ticket 738 -- node .scratch/t738/重建-产品.mjs
node tooling/run-locked.mjs --ticket 738 -- node .scratch/t738/指纹绑定.mjs
node tooling/run-locked.mjs --ticket 738 -- node .scratch/t738/变异电池-738.mjs
node tooling/run-locked.mjs --ticket 738 -- node --test "packages/plugin-manager/test/*.test.mjs"
node tooling/run-locked.mjs --ticket 738 -- node --test test/plugin-p10-boundaries.test.mjs
node tooling/check-gate-audit.mjs --evidence docs/plugins/plugin-manager/t738-证据.md --log .scratch/locks/gate-runs.log --ticket 738
```

（读数脚本住 `.scratch/t738/`：`复现-页签名.mjs`／`跑.mjs`（输出落盘＋末 5 行）／`重建-产品.mjs`／
`变异电池-738.mjs`／`指纹绑定.mjs`；明细日志同在 `.scratch/t738/*.log`。）
