# #740 更新区：状态 → 动作与全表文案（证据）

**票**：[#740 爱生活面板·更新区：每个状态都要给得出下一步（重启提示／重试安装／重新检查），不许「一报错就不管了」](https://github.com/FeatherHunter/ilife/issues/740)（`bug`；本席 2026-09-20 认领）。
**运行对账源**：[`t740-运行记录.md`](t740-运行记录.md)（锁目录导出的 `ticket=740` 全部条目）。
**维护者当日裁定**：「报错是允许的，但是这个报错得是实际的、可以解决的报错，而不是这种一报错就不管了」「从第一性原理出发，提供一个合理方案。全部由你决策」。

## 一、必报五步

### 第一步 · 影响清单

| 文件 | 一句话 | 碰它的理由 |
| --- | --- | --- |
| `packages/plugin-manager/src/update-view.ts` | 状态 → 结论与动作的判据（重启按事实判、拦截态各带出路、装完作废其余） | 这套判据的唯一产地 |
| `packages/plugin-manager/src/update-panel.ts` | 画卡与动作：接上「重试安装」「重新检查」，装成一家后作废其余行 | 面板唯一画卡的地方 |
| `packages/plugin-manager/src/update-contract.ts` | 全表人话重写（三段式、清自造词与口语） | 文案一处定义 |
| `packages/plugin-manager/test/update-678.test.mjs` | 四态各自的文案与动作、作废只作废别家、原因表「必须给得出下一步」 | 更新用例集中在这件 |
| `docs/plugins/plugin-manager/t740-证据.md`／`t740-运行记录.md` | 本件与运行记录导出 | 归属件目录、协议 §2.3 第 4 条 |
| `.changeset/t740-更新区状态与文案.md` | 变更记录 | `docs/agents/changeset-政策.md` |

只碰 `plugin-manager` 一个包；不碰六家单品、不碰技能包、不碰 DSH 上游。测试与证据不属源码管辖面。

### 第二步 · 结构设计

没有新目录、不碰第二个能力。`update-view.ts` 新增四个对外名（`restartPendingOf`／`restartLine`／`staleVerdict`／`markStaleOthers`）与一个类型（`VerdictAction`），它本来就是「纯视图判据」的产地；`update-panel.ts` 不新增对外名；`update-contract.ts` 只换字、不动形状。

### 第三步 · 写代码

见「二、改了什么」。

### 第四步 · 超线报警

**本包未写数字告警线**（`docs/agents/structure.md:74` 要求各包自己定；`packages/plugin-manager` 里搜不到这个数字）⇒ 无数值触发、不报警，也不据此宣称达标。改后行数备查：`update-panel.ts` 556／`update-client.ts` 152／`update-view.ts` 145／`update-contract.ts` 100。

### 第五步 · 交付对账（逐行对第一步）

实际碰到的受版本控制件与第一步逐行一致，**偏差为零**：三个源码件、一个测试件、两件证据、一个 changeset。`dist/**` 是构建产物（不在管辖面），已按当刻源码重出（host ＋ client 两半）。

## 二、改了什么

1. **待重启改按事实判**（`update-view.ts` 的 `restartPendingOf`）：判据是「磁盘上的版本 ≠ 正在跑的版本」，与原因码无关。真机那次正是因为按原因码判，把「该重启」漏成了「上次安装被打断」。
2. **拦截态一律带出路**（`actionForBlocked`）：`installation-changed` → 按钮「重新检查」（就地重查这家、重绑指纹）；`recovery-required` → 按钮「重试安装」（更新包语义就是重跑一次收尾）；其余四种面板代劳不了 ⇒ 不给按钮，改摊那条可复制命令，并在命令上方写清**它是干什么的**。
3. **装成一家 ⇒ 其余各家的旧快照作废**（`markStaleOthers`）：七家共用同一份使用范围清单，刚那次安装改写了它，别家再拿旧快照去装必然被守卫拦下。作废只作废别家（装成那家保持原样），原对象不改写（纯函数）。
4. **「没东西可装」时不说拦截**：已经是最新的那一家不再挂「安装清单变了」＋一条重装命令（真机上那条纯噪声）。
5. **全表文案重写**（`update-contract.ts` ＋ `update-view.ts` ＋ `update-panel.ts`）：每条按「事实 → 后果 → 动作」三段写，动作必须是用户做得到、且面板上真有的那一步。清掉的自造词与口语：半截任务、电话没接上、宿主半、回执异常、还出现就重装、查宿主日志、没跑成、对不上、不要重试旧编号、一次查七家。重启动作统一写成**「重启 DSH（退出后重新打开）」**。

## 三、机器读数

门禁证据（退出码 0 的那些）：

```
GATE-RUN runId=97c3b81c-eaa9-4638-8b4b-c8d204d140e5 cmd=node node_modules/typescript/bin/tsc -b packages/plugin-manager    exit=0
GATE-RUN runId=666bcd55-4ac9-4592-ab5e-a1d47a2d0a17 cmd=pnpm --filter dsh-life-pack run build:client                       exit=0（dist/client.js 89.05 kB）
GATE-RUN runId=7105c560-0da5-49bd-94da-160425e6f3d7 cmd=node --test packages/plugin-manager/test/update-678.test.mjs      exit=0（28/28）
GATE-RUN runId=bea6e71e-0f28-462d-af70-9d02323faced cmd=node --test packages/plugin-manager/test/*.test.mjs                exit=0（80/80）
GATE-RUN runId=3ab44338-fcc8-42f6-ba9c-71753b938327 cmd=node --test test/client-bundle-48.test.mjs                         exit=0（21/21）
GATE-RUN runId=71ea412a-44ac-48af-a8ed-dbcba0d7f37a cmd=node tooling/check-boundaries.mjs                                 exit=0（boundaries: PASS）
GATE-RUN runId=3434b8c7-1742-4c46-a99b-8893d54dcad9 cmd=node .scratch/t740/mutation-battery.mjs                             exit=0
```

变异自证（三条，全部「改坏就红、还原就绿、字节一致」）：

| 变异 | 改法 | 读数 |
| --- | --- | --- |
| `MUT-E` | 把「待重启」判据改回按原因码 | MUTATED build=0 test=1 ⇒ RED-OK；RESTORED build=0 test=0 ⇒ GREEN-OK；SAME-BYTES=true |
| `MUT-F` | 把拦截态的下一步全去掉（退回「只给一句话」） | 同上 RED-OK／GREEN-OK／SAME-BYTES=true |
| `MUT-G` | 装成一家后不作废别家的读数 | 同上 RED-OK／GREEN-OK／SAME-BYTES=true |

窗口末尾 `FINAL: host-build=0 client-build=0`（产物与还原后的源码一致，不留混合态）。

**声明的红（逐条点名，都已修或与本单无关）**：

- `runId=e1f36a1b-…`（`node --test …/update-678.test.mjs` exit=1）：本席新加的 import 撞上 `dist/update-panel.js` 陈旧（见「四」），把纯函数搬到 `update-view.ts` 后复绿。
- `runId=70d2d314-…`（同一命令 exit=1）：三条断言压着旧文案（`手工命令`／`未知原因`／`同时只装一个`），改成压**判据**（每条报错必须给得出下一步、自造词不许回来）后复绿。

## 四、范围外发现（记账，不阻塞本单）

1. **`packages/plugin-manager/dist/update-panel.js` 是一份陈旧且在 ESM 下打不开的产物**（mtime 09-18 21:21）：宿主工程的编译面不含 `update-panel.ts`（它是浏览器侧件），那份老产物里有一句 `export { MANAGER_ACTIONS };`，`import` 它直接 SyntaxError。今天没有消费者（运行时走 `dist/client.js` 与 `dist/index.js`），但它会误导下一个想 import 它的人（本席就是先踩了这一步）。删不删属构建产物面，留维护者定。
2. 本包仍未定数字告警线（`structure.md:74`）。

## 五、未做项 / 下一手缺什么

- **真机读数未做**（要装机 ＋ 重启宿主）：四种态各看一眼——① 已是最新不再挂黄条；② 装了没重启先说「重启 DSH（退出后重新打开）」；③ 磁盘与运行一致、但上一次作业留了记录时给「重试安装」；④ 快照过期给「重新检查」，且装成一家之后其余各家自动进「重新检查」态。那一步按维护者的节奏走，本单不据此关票。

## 六、对抗式审查（维护者要求「先红队自家代码，争取一次性验收到位」）

**攻击面**：① 判据依赖的**事实**在面板拿到的快照里真的有吗；② 我给的**动作**真的做得成吗（不是空转按钮）；③ 判据的**覆盖盲区**（纯函数全绿、界面却没画出来）；④ 换一层（宿主侧读数）会不会把面板的按钮变成谎话。

**逮到并已修的三条（第一条会让这次装机白跑）**：

| # | 发现 | 证据 | 修法 |
| --- | --- | --- | --- |
| A1 | `installation-changed` 在本进程里是**一次绑定、永久拦**：宿主把「本进程第一次读到的安装态」记下，之后任何变化一律拦 ⇒ 我新加的「重新检查」点了还是同一句（空转），「装完一家再装下一家」仍要重启宿主 | 读原 `update-env.ts` 的绑定三段；更新包自己的守卫是**按凭证**的（`service.js:309`／`:325`：装之前重读，`env.installationKey !== checked.installationKey` 才抛） | 删掉进程级绑定，只如实投影安装态；TOCTOU 交给更新包按凭证那道。新增判据（真夹具读两次环境 ＋ 变异 `MUT-I` 看守） |
| A3 | **有新版、却没凭证时会说「已是最新」**（假话）：`canInstall` 为假且 `blockedReason` 为空（凭证过期／没签发）时，旧判据落进 `up-to-date` | 读更新包 `service.js:218-220`（`canInstall` 要求 `fresh` ＋ 指纹相等 ＋ 最新版严格高于运行版） | 新增一支：「有新版本 X（正在运行 Y）。点「重新检查」，再点「装上更新」」＋`recheck` 按钮；变异 `MUT-H` 看守 |
| B2 | 「装成一家就作废别家」在**作业失败／中断**时也触发（那时安装态没动，白逼用户重查） | 自查 | 只在 `installedVersion` **真的变了**时才作废 |

**核实过、结论「不必改」的两条**：

- **`recovery-required` 的「重试安装」会不会空转**：更新包的 `install` 要凭证，凭证只在可装态签发（`service.js:256`）⇒ 在「磁盘 ≠ 运行中」那一态确实装不了。但事实判据 `restartPendingOf` **先于**拦截分支，所以那一态给的是「重启 DSH」；「重试安装」只在「磁盘 == 运行中、上一次作业留了记录」时出现——那时 `canInstall` 可以为真，重试就是上游说的「重新点一次安装」。两支各自成立。
- **待重启那句写「新版 X」**：手工降级时「新版」不实 ⇒ 改成「磁盘上装的是 X；正在运行的是 Y。重启 DSH（退出后重新打开）后生效。」（只换措辞，判据不变）

**已知盲区（如实记账）**：

- 面板 hook 的接线（`act(target, action)` 传对动作、作废真的画出来）**没有渲染级判据**；只有纯函数判据 ＋ 一条「四个按钮名确实进了浏览器束」的交付面判据。真正的接线由真机那一步兜。
- 体检那半边（#735）：六家并发 spawn 的**总耗时**没在真机量过（技能侧 spawn 上限 20 s、面板超时 25 s）。真机首跑若撞上限，读数会写「体检超时（25s）：技能没在时限内回话。」，那时再谈放宽。

**这一轮的读数**（与上面同一张运行记录件）：`node --test …/update-678.test.mjs` **31 pass / 0 fail**；本包用例 **83 pass / 0 fail**；客户端束门 **21 pass / 0 fail**；`check-boundaries` PASS；变异 `MUT-E`／`F`／`G`／`H`／`I` 五条全部 RED-OK ＋ GREEN-OK ＋ `SAME-BYTES=true`。

## 七、对抗式审查第二轮（维护者：「反复让你进行对抗式审查之后，我才会决定发布新版本」）

**这一轮换的攻击面**：① 六家宿主半是不是**都**接了那个端点；② 真实载体这一跳能不能**离线取证**；③ 判据的**时限口径**对不对；④ 文案里有没有写**我们并不知道的原因**。

**逮到并已修的三条**：

| # | 发现 | 证据 | 修法 |
| --- | --- | --- | --- |
| R2-4 | **体检的整批时限按「一家」算，而真实上界是「六家之和」**：各单品的宿主半用 `spawnSync`（阻塞同一个宿主线程），六通电话是一个接一个干完的 ⇒ 旧口径 25 s（＝单家 20 s ＋ 余量）在真机上会误报超时，靠后的几家会写成「体检超时（25s）…」，看着像半坏 | 各单品 `src/bridge.ts` 的 `SPAWN_TIMEOUT_MS = 20_000` ＋ `spawnSync` | 整批时限改 **60 s** 并写清为什么；新增**跨包判据**（`test/plugin-p10-boundaries.test.mjs`：`HEALTH_TIMEOUT_MS >= 3 × SPAWN_TIMEOUT_MS`）；变异 `MUT-J` 看守 |
| R2-2 | `recovery-required` 那句教人「重启 DSH」，而这一条**只在「磁盘版本 == 正在运行」（或磁盘那份读不到）时才会印出来**——那时重启什么也改变不了 | 判据顺序（事实分支先截走「磁盘 ≠ 运行中」）＋ 更新包 `service.js:211` | 改成「上次安装没有收尾。点「重试安装」再跑一次；仍出现这一条，就用下面这条命令重装。」；判据断言它**不含「重启」** |
| R2-1 | `installation-changed` 那句断言「刚装过别的插件」——真因也可能是清单被手工改过／别的工具装的 ⇒ 写了我们并不知道的原因 | 指纹内容＝使用范围清单 ＋ 锁文件 ＋ 那家自己的清单 | 改成「这台机器的安装清单变了（在别处装过、或清单被手工改过）」，不再单点一个原因 |

**写成程序时自己踩的一处（记账）**：第一版把 `packages/plugin-*/src/bridge.ts` 写进块注释，里面的 `*/` 把注释提前收尾 ⇒ 编译报 TS1005；改成「各单品的 `src/bridge.ts`」后复绿。不是产品缺陷，但值得记：注释里不写通配路径。

**这一轮的读数**：本包用例 **83/83**；边界门 **17/17**（本单三条门全 ✔，且那条**既有**的版本范围红已被别席修掉）；`check-boundaries` PASS；客户端束门 **21/21**；变异 **六条（E–J）** 全部 RED-OK ＋ GREEN-OK ＋ `SAME-BYTES=true`。

**静态面确认（六家都接得住这一跳）**：六家单品的宿主半分派里都有 `if (endpoint === RPC_ENDPOINT_CONFIG_CHECK) return ok(readConfigHealth());`（`plugin-calorie`／`plugin-schedule-ilife`／`plugin-bill-ilife`／`plugin-memo-ilife`／`plugin-home-ilife`／`plugin-chef` 逐家读到）。

**仍未证的一处（如实记账）**：真实载体上「面板 → `/api/<通道>` → 单品宿主半 → 技能 CLI」这一跳**没有被离线证过**。本席向正在跑的 DSH 载体 POST 了一次只读的 `config.check`：`/api/ilife-calorie` 回 **HTTP 401**；对照实验 `/api/ilife-not-a-real-channel-740` **也回 401** ⇒ 那 401 是一刀切的鉴权门，**证明不了路由存不存在**（写死结论就成假证据了）。它目前由三段静态事实支撑：① URL 由载体自己的代码拼（`` `${第一段}/${第二段}` ``）；② 六家宿主半都要求信封 `method === RPC_CHANNEL.slice(1)`；③ 六家都接了 `config.check` 分支。**真机那一次点击是它唯一的验收。**

## 八、对抗式审查第三轮（维护者：「反复让你进行对抗式审查之后，我才会决定发布新版本」）

**这一轮换的攻击面**：① **朗读表**——把每个状态下屏上真会印的那句话、那颗按钮、那个命令块逐条念出来（不再只看纯函数绿不绿）；② 状态**组合矩阵**（八种拦截原因 × 有没有新版 × 磁盘与运行一不一致）；③ **混版**（新面板 ＋ 老宿主 / 老面板 ＋ 新宿主）；④ 从没被**执行过**的分支（失败的行、缺席即装）。

### 8.1 朗读表（本轮的主产物，`.scratch/t740/readout.mjs` 直接读产物里的判据件生成）

| 状态 | 屏上那句话 | 按钮 | 命令块 |
| --- | --- | --- | --- |
| 未安装 · 还没查过 | 未安装。点「重新检查」查最新版本，再点「装上」。 | 重新检查 | —— |
| 未安装 · 查到最新版 | 未安装；最新版本 0.3.0。点「装上」安装。 | 装上 | —— |
| 已装 · 还没查过 | 还没查过最新版本。点「重新检查」。 | 重新检查 | —— |
| 已装 · 有新版可装 | 有新版本 0.3.0（正在运行 0.2.7）。点「装上更新」。 | 装上更新 | —— |
| 已装 · 已是最新 | 已是最新版本 0.2.7。 | （无） | —— |
| 已装 · 有新版但没凭证 | 有新版本 0.3.0（正在运行 0.2.7）。点「重新检查」，再点「装上更新」。 | 重新检查 | —— |
| 装了没重启（两种原因码） | 磁盘上装的是 0.3.0；正在运行的是 0.2.7。重启 DSH（退出后重新打开）后生效。／横幅：⚠️ 待重启 DSH（退出后重新打开）：卡路里 | （无） | —— |
| 磁盘与运行一致 · 上一次作业没收尾 | 上次安装没有收尾。点「重试安装」再跑一次；还出现这一条，就用下面这条命令重装。 | 重试安装 | **出** |
| 别处装过 · 安装清单变了 | 这台机器的安装清单变了（在别处装过、或清单被手工改过），这一家现在不能装。点「重新检查」重新读一次，再点「装上更新」。 | 重新检查 | —— |
| 认不出安装位置 | 认不出这台机器上的安装位置（使用范围的目录不存在，或名字不合法）。把使用范围目录建好，再点「重新检查」。 | 重新检查 | —— |
| 按版本号装的 · 源码装 | 这一份不是从 npm 发行包装的（源码装），面板不能更新它。要更新，照下面这条命令重装，装完再点「重新检查」。 | 重新检查 | **出** |
| 磁盘上那一份装得不完整 | 磁盘上这一份装得不完整（包名或版本对不上、入口文件缺失）。照下面这条命令重装，装完再点「重新检查」。 | 重新检查 | **出** |
| 清单里的版本与磁盘对不上 | 使用范围清单里写的版本与磁盘上的实际版本对不上。打开使用范围的 `package.json`，把这一行改成磁盘上的版本号，再点「重新检查」。 | 重新检查 | —— |
| Node 版本不够 | 新版本要求 Node 22 或更高，当前运行的是更低版本。升级 Node、重启 DSH 后，再点「检查更新」。 | （无） | —— |
| 已被本面板装过一家（读数作废） | 刚在本面板装过别的插件，共用的安装清单变了，这一家要先重读一次。点「重新检查」，再点「装上更新」。 | 重新检查 | —— |
| **失败的行**（八种过程错误码） | 装不上：＋ 该码的人话 | 重新检查 | 出 |

读表读出来的规矩（本轮就按它改）：**这句话点名的第一颗按钮，必须在那一行上真有一颗、真够得着**；写着「下面这条命令」的那几行，命令块必须真出来；只有发生在**重启之后**的下一步才许写题头的「检查更新」。

### 8.2 逮到并已修的六条

| # | 发现 | 证据 | 修法 |
| --- | --- | --- | --- |
| R3-1 | 两张「还没查过」的卡（未安装·未查／已装·未查）不给按钮，句子却写着「点「检查更新」」——那颗按钮住在**面板题头**，而结果浮层一开就把它盖住：「点「检查更新」」正是开浮层那一下 ⇒ 用户在浮层里**无路可走** | `update-panel.ts:345-359`（题头按钮）＋`:464-508`（`PANEL_STYLE.overlay` 浮层，关法只有 ×／点遮罩／Esc） | 两态改给 `action: 'recheck'`（画在那一行上），句子改写成「点「重新检查」」（变异 `MUT-K` 看守） |
| R3-6 | 同一类病的**另外四态**：`unknown-profile`／`registry-conflict`／`invalid-installation`／`source-install` 一个动作都不给，而各自那句都写着「再点「检查更新」」 | 朗读表（改前逐行念出「（无）／再点「检查更新」」） | `RECHECK_REASONS` 四态全给「重新检查」（它们是「条件在别处，改完回来重读一次」）；`incompatible-node` 与 `pending-restart` 保持无按钮——它们要的是升级 Node ＋ 重启，那句「检查更新」发生在重启之后，那时浮层早关了（变异 `MUT-F` 看守） |
| R3-6b | **失败的行**（查不到最新／检查过期／装失败……）既没有快照也没有结论，原先**一颗按钮都不画**，屏上却写着「再点「重新检查」」 | `update-panel.ts` 旧渲染：按钮由 `verdict?.action` 决定，失败时结论为 `null` | 新增纯函数 `cardActionOf(verdict, phase)`：失败的行一律补「重新检查」（结果行与设置页那张缺席卡共用同一份判据，变异 `MUT-N` 看守） |
| R3-7 | `recovery-required` 那句写着「就用下面这条命令重装」，而命令块只在「拦截态且没按钮」时才画 ⇒ **那句话下面什么都没有**（屏上给了一个不存在的东西） | 旧渲染 `showManual` 条件 vs `REASON_TEXT['recovery-required']` | `Verdict` 新增 `manualHint`；面板据此画命令块（三态：源码装／装得不完整／上次没收尾）＋文案判据「写着「下面这条命令」就必须 `manualHint`」（变异 `MUT-O` 看守） |
| R3-4 | 重启那句话**一屏两遍**：横幅一遍、卡片一遍 | 朗读表（待重启那两行） | 横幅退成一行总账「⚠️ 待重启 DSH（退出后重新打开）：卡路里、备忘录」——**横幅答「谁要重启」，卡片答「为什么、怎么做」**（变异 `MUT-L` 看守） |
| R3-5 | 作废态的下一步是空话「再操作这一家」（照着念也不知道点哪颗） | `staleVerdict()` | 改成「点「重新检查」，再点「装上更新」」（变异 `MUT-M` 看守） |

**顺手修掉的一处既有红（R3-8）**：`tsc -p packages/plugin-manager/tsconfig.client.json` 从 #678 起一直是 exit 2（两处 `TS7016`：`import type … from 'dsh-plugin-update'` 找不到声明件）。根因不是包缺声明件——`src/dsh-plugin-update.d.ts` 就是本仓手写的那个声明件，**但客户端工程的 `include` 清单里没列它**（宿主工程的 `include: ["src"]` 把它带进去了，所以只有客户端这道门红）。把它加进 `include` 后转绿（runId `25d04d73…` 红 → `54bf3be5…` 绿）。

### 8.3 核实过、结论「不必改」的两条

- **R3-2 缺席即装那条路不是盲区**：它真机走过（`t678-真机验收证据-0.2.6.md` 第 39–52 行，含 pin→caret 那条记账），本轮只补了「未安装·还没查过」那一格的动作。
- **R3-3 混版**：删掉的那条「本进程第一次读到的样子」的绑定住在**宿主半**（`update-env.ts`）⇒ 面板这一版把「重新检查」接好了，但**宿主进程里跑的若还是旧版宿主半，那一次重读仍会读到旧指纹**。所以装机后必须重启 DSH 一次，面板那套「重新检查」才真正生效——这条写进交付说明，不让用户以为面板能替宿主热更。老面板 ＋ 新宿主是协议安全的：电话名来自更新包自己的表，面板只把通道名转给载体。

### 8.4 这一轮的读数

```
GATE-RUN runId=2b356b80-5bcd-4f65-8ef7-a390a7ac4f0c cmd=node node_modules/typescript/bin/tsc -b packages/plugin-manager                exit=0
GATE-RUN runId=9c341685-acd1-4b41-aceb-03fac4af474f cmd=node node_modules/typescript/bin/tsc -b packages/plugin-manager --force          exit=0（全量重编，排除增量假绿）
GATE-RUN runId=54bf3be5-6c55-494e-b2c8-7e3048839005 cmd=node node_modules/typescript/bin/tsc -p packages/plugin-manager/tsconfig.client.json exit=0（这一轮修好的既有红）
GATE-RUN runId=88179ffa-8fe3-4e07-84a0-a181bf60adde cmd=pnpm --filter dsh-life-pack run build:client                                     exit=0（dist/client.js 93.66 kB）
GATE-RUN runId=4d982f11-91aa-497c-894b-4a7ec7ad69eb cmd=node --test packages/plugin-manager/test/*.test.mjs                              exit=0（88/88，含本轮新增 3 条判据）
GATE-RUN runId=90ff1a52-7371-4e29-af91-92e781084470 cmd=node --test --test-reporter=tap packages/plugin-manager/test/*.test.mjs test/plugin-p10-boundaries.test.mjs test/client-bundle-48.test.mjs exit=0（126/126）
GATE-RUN runId=290dd486-775b-439d-9cf6-6d1c8f38b46a cmd=node tooling/check-boundaries.mjs                                               exit=0（boundaries: PASS）
GATE-RUN runId=d19b76ed-2e03-4ee4-9ee3-58f21264748d cmd=node .scratch/t740/mutation-battery.mjs                                         exit=0
```

**声明的红（逐条点名，都已修或属既有）**：`274a9f9b…`／`99d97e6f…`／`67130dab…`（本包用例 exit=1）＝三条旧断言压着这一轮改掉的文案，改成压判据后复绿；`25d04d73…`（客户端类型门 exit=2）＝R3-8 那条既有红，本轮修好；`7ceb802c…`／`569b4ad4…`＝第二轮记账里那次「块注释里写通配路径」的编译红。

**变异自证（**十一条**，方法：改坏 → `tsc -b` → 跑用例 → 还原 → 再跑；三态全中才算数）**：

| 变异 | 改法 | 读数 |
| --- | --- | --- |
| `MUT-E` | 把「待重启」判据改回按原因码（真机那次就是这么漏掉重启提示的） | MUTATED build=0 test=1 ⇒ **RED-OK**；RESTORED build=0 test=0 ⇒ **GREEN-OK**；`SAME-BYTES=true` |
| `MUT-F` | 把拦截态的下一步全去掉（退回「只给一句话」） | 同上三态全中 |
| `MUT-G` | 装成一家后不作废别家的读数 | 同上三态全中 |
| `MUT-H` | 把「有新版但没凭证」那一支去掉（退回说「已是最新」） | 同上三态全中 |
| `MUT-I` | 把那条「本进程第一次读到的样子」的绑定重新装回去 | 同上三态全中 |
| `MUT-J` | 把体检整批时限改回 25 s（按一家算的旧口径） | 同上三态全中 |
| `MUT-K` | 两张「还没查过」的卡退回不给按钮（浮层一开就无路可走） | 同上三态全中 |
| `MUT-L` | 让待重启横幅把那句话重复第二遍 | 同上三态全中 |
| `MUT-M` | 把作废态的下一步退回空话「再操作这一家」 | 同上三态全中 |
| `MUT-N` | 把失败的行那颗「重新检查」去掉 | 同上三态全中 |
| `MUT-O` | 让「下面这条命令」那句话不再要求画命令块 | 同上三态全中 |

窗口末尾 `FINAL: host-build=0 client-build=0`（产物与还原后的源码一致，不留混合态）。

### 8.5 仍未做 / 仍未见底的三处（如实记账）

- **真机读数仍未做**（要装机 ＋ 重启宿主）：四态各看一眼（见「五」）＋ 本轮新增的四态各看一眼（认不出安装位置／源码装／装得不完整／清单版本对不上：都要有**一颗够得着的按钮**）。
- **渲染级判据仍缺**：本轮的按钮判据仍是「纯函数 ＋ 产物面」（四个按钮名确实进了浏览器束）；「浮层里到底画没画出来」由真机兜。`#679` 那份渲染台可以补这一步，属另一件。
- **真实载体那一跳**（面板 → `/api/<通道>` → 单品宿主半 → 技能 CLI）仍未离线证过，理由与第二轮同（一刀切 401 证明不了路由）。

## 九、第四轮：维护者真机截图（`dsh-life-pack@0.3.1` 装出去之后逮到的第一条）

**维护者报的原话**：「我点了卡路里插件的更新安装，点了之后其他的都变成黄色不让点了。我认为所有插件都能直接安装，然后提示需要重启，而不是所有的没有更新的插件也不让安装了。你需要调查！」

**截图读数**（两次独立的视觉调用互相印证）：卡路里那一行是**对的**——「磁盘上装的是 0.3.0；正在运行的是 0.2.7。重启 DSH（退出后重新打开）后生效。」（第一轮那条事实判据生效）；作息管家、居家管家两行全黄，写着「刚在本面板装过别的插件，共用的安装清单变了，这一家要先重读一次。点「重新检查」，再点「装上更新」。」；**全图只有两颗按钮，都是「重新检查」**（`vision_detect`：卡2 y≈448-500、卡3 y≈843-894），「装上更新」一条也找不着。

### 9.1 根因：第一轮那条「作废别家读数」的**前提是错的**

- 第一轮我写下的理由：「七家共用同一份使用范围清单，刚那次安装已经改写了它，**旧快照再拿去装必然被守卫拦下**」。
- 真正的守卫（更新包 `service.js:309`／`:325`）拦的是**旧凭证**：`env.installationKey !== checked.installationKey` → `installation-changed`。它比的是「凭证里记的安装态」与「提交那一刻读到的安装态」。
- 而面板提交安装走的这条路上，**凭证是提交前那一刻现查现签的**：`update-client.ts:127` 先 `checkTarget`（＝`updateCheck`，新凭证落在**当时**的安装态上），`:136` 才拿这个 `checkId` 提交。**旧快照从头到尾没有参与提交**。
- ⇒ 「作废别家」挡掉的是**一次本来会成功的点击**，代价是用户必须多点一次「重新检查」（而 `重新检查` 做的事与 `装上更新` 提交前那次重查**完全一样**）。它不是保守，是纯粹的摩擦——而且是**我引入的**（0.3.0 里没有这条）。

### 9.2 改法（把整条作废路删掉，不是给它换句话）

| 件 | 改动 |
| --- | --- |
| `update-view.ts` | **删除** `staleVerdict()` 与 `markStaleOthers()`（连判据带文案）；新增纯函数 `showManualOf(verdict, phase)`——「这一行要不要摊手工命令」收成一处、可单测 |
| `update-panel.ts` | `UpdateRowState` 去掉 `stale`；状态源去掉 `staleOthers` 回调与那一串 `stale: false`；画卡改成 `verdictOf(target, snapshot)`（不再按「过期」换结论）；命令块判据改用 `showManualOf`，**待重启那张卡不再挂手工重装命令**（同一张截图里逮到的第二条：那条命令点下去等于把刚装好的那版再装一遍） |
| `update-client.ts` | 不加代码，只在 `updateInstalled` 上写明**为什么这里必须先查一次**——它就是「装了别家，这一家照样点得动」的依据 |

### 9.3 判据与枪

| 判据 | 内容 |
| --- | --- |
| 提交顺序（新） | 用假传输口记调用序列：`装上更新` 的两次调用**必须**是 `['check','install']`（先查一次拿新凭证，再提交）——**这就是别家照样点得动的证明**；查新版失败／没凭证时不许硬提交，照实回原因码 |
| 命令块（新） | `showManualOf`：待重启与 `incompatible-node` **不摊**命令；`manualHint` 命中（源码装／装得不完整／上次没收尾）与失败的行**要摊** |
| 产物面（改） | 四个按钮名照旧；新增**反向断言**：产物里不许再出现「刚在本面板装过别的插件」那句挡人的话 |
| 变异 `MUT-P` | 把「提交前先查一次」偷换成查 `status` 电话 ⇒ 提交顺序判据变红 |
| 变异 `MUT-Q` | 把「待重启不摊命令」改回去 ⇒ 命令块判据变红 |
| 变异 `MUT-G`／`MUT-M` | **随整条作废路删除**（`markStaleOthers`／`staleVerdict` 已不存在，留着只会 GUARD-FAIL）；这一轮新加的两条枪见上 |

### 9.4 这一轮的读数

```
GATE-RUN runId=64c4be59-3d76-4272-bb14-450fe82750aa cmd=node node_modules/typescript/bin/tsc -b packages/plugin-manager              exit=0
GATE-RUN runId=bac9ee80-a94d-4d1e-ac39-59e35fb32997 cmd=node node_modules/typescript/bin/tsc -p packages/plugin-manager/tsconfig.client.json exit=0
GATE-RUN runId=b442cfd1-3d3a-4577-bdc4-8e59cad91e95 cmd=pnpm --filter dsh-life-pack run build:client                                   exit=0（dist/client.js 92.46 kB）
GATE-RUN runId=（见 §9.4 同批）cmd=node --test --test-reporter=tap packages/plugin-manager/test/*.test.mjs test/plugin-p10-boundaries.test.mjs test/client-bundle-48.test.mjs exit=0（127/127）
GATE-RUN cmd=node tooling/check-boundaries.mjs                                                                                       exit=0（boundaries: PASS）
GATE-RUN cmd=node .scratch/t740/mutation-battery.mjs                                                                                 exit=0（十一枪全中：E／F／H／I／J／K／L／N／O／P／Q）
```

### 9.5 残留一处（如实记账，不阻塞）

更新包的 `service.js:237` 有个 **2 秒的重查窗口**：同一个目标距上次检查不到 `RECHECK_WINDOW_MS`（2000 ms）时，重查会回**缓存的那一次**（凭证仍绑在旧安装态上）⇒ 那种点击会以 `installation-changed` 收场，面板照旧给出「点「重新检查」」＋那颗按钮。**真机上触发不到**：装完一家要跑一次 pnpm 安装（秒级以上），用户再点第二家时，这一家的上次检查早就出了那个窗口。真触发到也只是多点一次「重新检查」，不会卡死。

### 9.6 与已发布版本的关系

`dsh-life-pack@0.3.1` **带的是出问题的那个行为**（作废别家）；9.2 的改动**不在 0.3.1 里**，要随下一个补丁版（建议 `0.3.2`）出去。发不发由维护者定。



