# #734 发版走查单（六家单品插件 → 0.3.0；总管押后到那一席提交后）

## 0 一句话

六个单品插件的**定版与三道门已完成**（`0.3.0`），发布只需维护者按 §3 逐包执行（OTP 只在人手上，不进聊天）。**总管 `dsh-life-pack` 本轮不发**：它的工作区正被另一席改着（未提交），按规矩不能从那棵状态发版——见 §4。

## 1 定版读数（本仓已落盘，未提交前在工作区）

| 包 | 版本 | 依赖 |
| --- | --- | --- |
| `dsh-calorie` | `0.2.7` → **`0.3.0`** | `dsh-life-pack: ^0.2.6`（不动） |
| `dsh-bill-ilife` | `0.2.2` → **`0.3.0`** | 同上 |
| `dsh-chef` | `0.2.1` → **`0.3.0`** | 同上 |
| `dsh-home-ilife` | `0.2.1` → **`0.3.0`** | 同上 |
| `dsh-memo-ilife` | `0.2.1` → **`0.3.0`** | 同上 |
| `dsh-schedule-ilife` | `0.2.1` → **`0.3.0`** | 同上 |

连带改写（发版会红的写死断言）：`plugin-calorie/test/smoke.test.mjs:34`、`plugin-memo-ilife/test/smoke.test.mjs:34`、`plugin-schedule-ilife/test/smoke.test.mjs:31` 里的版本线正则 `/^\^0\.2\./` → `/^\^0\.3\./`。bill／chef／home 三家的同类断言是**动态比对**（读总管 `package.json` 的版本线），无需改。

本次对外行为：六家各新增一个 agent 工具（`run_calorie_command`／`run_bill_command`／`run_chef_command`／`run_home_command`／`run_memo_command`／`run_schedule_command`）。

## 2 三道门（已跑，全绿）

| 门 | 命令 | 读数 |
| --- | --- | --- |
| `--pre` | `node tooling/run-locked.mjs --ticket 734 -- node tooling/check-publish.mjs --pre --only <六个>` | **PASS**（runId=10b00e79-0524-400c-b21b-f6188d2825b1，exit=0） |
| `--tarball` | 同上换 `--tarball` | **PASS**（runId=9ed3a4a0-3cdb-46a7-b6dc-a4bf08bbd216） |
| `--fresh-tmp` | 同上换 `--fresh-tmp` | **PASS**（runId=822cf39f-45de-4870-ab0f-4a12fa9a0e81） |

构建（门的前置）：`node tooling/run-locked.mjs --ticket 734 -- pnpm --filter dsh-calorie --filter dsh-bill-ilife --filter dsh-chef --filter dsh-home-ilife --filter dsh-memo-ilife --filter dsh-schedule-ilife run build` → exit=0（六包 `Done`）。

## 3 发布顺序与命令（**维护者执行**；`npm publish` 需要 TTY，否则 `EOTP`）

复用仓内成熟入口：`tooling/publish-chain.mjs --plan` 打印顺序，发布即逐包 `npm publish`。

```bash
# 0. 先看顺序（可选）
node tooling/publish-chain.mjs --plan --only dsh-calorie,dsh-bill-ilife,dsh-chef,dsh-home-ilife,dsh-memo-ilife,dsh-schedule-ilife

# 1. 六个单品（顺序无关，互不依赖；各自目录里发）
cd D:\ilife\packages\plugin-calorie          && npm publish --access public --registry=https://registry.npmjs.org/
cd D:\ilife\packages\plugin-bill-ilife       && npm publish --access public --registry=https://registry.npmjs.org/
cd D:\ilife\packages\plugin-chef             && npm publish --access public --registry=https://registry.npmjs.org/
cd D:\ilife\packages\plugin-home-ilife       && npm publish --access public --registry=https://registry.npmjs.org/
cd D:\ilife\packages\plugin-memo-ilife       && npm publish --access public --registry=https://registry.npmjs.org/
cd D:\ilife\packages\plugin-schedule-ilife   && npm publish --access public --registry=https://registry.npmjs.org/
```

发完后的复核门（联网读 registry）：

```bash
node tooling/check-publish.mjs --post --only dsh-calorie,dsh-bill-ilife,dsh-chef,dsh-home-ilife,dsh-memo-ilife,dsh-schedule-ilife
```

## 4 总管为什么押后，以及押后怎么办

- `packages/plugin-manager` 当前工作区里有**另一席未提交的在途件**：`src/client.ts`、`src/health-fetch.ts`、`src/health-panel.ts`、`src/nav.ts`、`src/update-contract.ts`、`test/health-706.test.mjs`（还有 `test/plugin-p10-boundaries.test.mjs`）。从这棵状态打包＝把别席在途的代码发进 registry，违反仓内并发纪律。
- **不能只把总管改到 0.3.0 单独发**：总管一旦 0.3.0，六个单品的 `dsh-life-pack: ^0.2.6`（`>=0.2.6 <0.3.0`）就把新版排除了 ⇒ 必须**总管先行、六个单品的范围一起提到 `^0.3.0`**才算真统一（`tooling/check-publish.mjs:126` 的「同版本线」断言也会盯着这条）。
- 押后方案（推荐）：**等那一席提交**后另开一窗：① 总管 `0.2.7 → 0.3.0`；② 六个单品 `dsh-life-pack: ^0.2.6 → ^0.3.0`（版本号不必再动，仍 0.3.0，但要**重跑三道门**再发这六个）；③ 顺序＝总管先、六个后。
- 备选方案（现在就把六个发出去，总管不动）：可行（`^0.2.6` 仍满足 0.2.7），代价是日后总管上 0.3.0 时这六个要**再发一次**。

## 5 风险与遗留

- `dsh-client-contract.md` 缺 §14（工具注册面的出处）——卡路里那份镜像注释引了它；chef 已在其镜像注释里直接写真出处。补章不属本票。
- 桥里 `SKILL_CLI_REL = 'dist/cli/cmd_read.js'` 字面路径与工具的「按 `bin` 声明解析」两个口径待合并（`docs/agents/技能调用契约.md` §四已登记）。
- 装机与真机验收（重启宿主后说一次「卡路里 help」）仍待维护者执行——那是本票的切片 2，与发版并行即可。
