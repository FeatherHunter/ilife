# #699 地图收口调查：为什么 100%（12/12）却没关

调查日期：2026-09-19　调查当刻 HEAD：`84c6e19d`　调查者：wayfinder 会话
调查对象：[#699 [wayfinder] 卡路里包结构整改：九条候选逐条裁定并做出来](https://github.com/FeatherHunter/ilife/issues/699)
状态：**本图已于 2026-09-19T03:48:01Z 关闭（`COMPLETED`）**，收尾评论载 [issuecomment-5739123115](https://github.com/FeatherHunter/ilife/issues/699#issuecomment-5739123115)。

> **⚠️ 本文件是调查记录，含一处被后续复核推翻的结论，读时先看这条。**
>
> 调查分两轮。**第二轮（同一会话，晚于本文件初稿）推翻了初稿的核心建议**：初稿按地图自己的框架往下推，建议「补票、先别关」；第二轮回头查仓规，发现**判据 ③ 本身与 `docs/adr/0003-一级目录三类准入.md` 冲突**——ADR-0003 已把 `render/` 裁作第三类**页面交付面**（准入＝「出整页必经 **或** ≥2 个能力域在用」），并在「被否的选项」里具名否掉了「把 `render/` 记作共用位」，而判据 ③ 用的正是那个被否的判据。同时实测 `src/render/` 的 24 件**全部在页面装配路径上**，故「第六批」前提不成立。
>
> ⇒ **第二节的读数全部成立**（判据 ③ 按原文确实未达成、24 件确实还在）；**第八、九节的建议已被推翻**，保留在文末并标注，不再照办。最终处置见第十一节。
>
> **教训（本文件最有价值的部分）**：初稿的错不在读数，在**没有回头质疑目标本身**——把地图写下的到岸判据当成既定事实，于是在一个被否掉的判据上推演出四张施工票。

**结论一句话：12/12 是「子议题全关」这个计数的真话，不是「地图走完」这个判断的真话。地图的 Destination 只达成 2/3，第 3 条按本图自己量出来的规格只走了约四成。所以「100%」是计数与目标脱钩造成的假象，地图当前的状态是「未完成且剩下的是已量清、已排好批次、只差开票的活」。**

---

## 一 · 调查方法（可复跑）

| 动作 | 命令 / 位置 |
| --- | --- |
| 子议题清点 | `gh api repos/FeatherHunter/ilife/issues/699/sub_issues?per_page=100` |
| 全部 issue 面 | `gh issue list --state all --limit 400 --json number,title,state,labels` |
| 每票解决评论 | `gh issue view <n> --comments` |
| `src/render/` 归属分档 | `.scratch/t699/verify2.mjs`（本席一次性脚本） |
| 导入图与测试面直取 | `.scratch/t699/graph.mjs` |
| 台账门 | `node packages/skill-calorie/scripts/check-warning-line.mjs` → `RESULT: 93/93` ＋ `PASS`，exit 0 |
| 结构门 | `node packages/skill-calorie/scripts/check-one-path.mjs` → 两条 `PASS`，exit 0 |
| 未挂图的相关票 | 按标题／标签搜「卡路里｜calorie｜skill-calorie｜render/｜结构」，逐票核 label 与 `Part of` |

---

## 二 · 结论一：按原文判据，第 3 条只走了约四成 —— ⚠️ **判据本身后被改判**

> **更正**：本节全部**读数**成立（判据 ③ 按原文写法确实未达成、24 件确实还在）。但第二轮复核认定**判据 ③ 的原文写法与 ADR-0003 冲突**（见开头「⚠️」段），判据已重写 ⇒ 本节那个「未达成」的**判定**随之作废。读本节请只取读数。

地图的 Destination 自己写死三条达成判据「三条都为真才算到岸」。逐条对当刻树复核：

### 判据 ①：页面内重复出现的排版件只剩一份实现——**成立**

- 三类真实现各 1 处：`src/shared/pageStrips.ts:83`（窗口条）／`:99`（事实条）／`src/shared/copyBlock.ts:35`（复制区）。
- 65 处调用点一行未动，三族各留一行薄转出（#719 解决评论与正文 §实测读数一致）。
- 来源脚注那一类按「不做」处置并登记理由（真重复约 1 行，合并只多一层壳，撞铁律五）。
- `src/shared/` 当刻 21 件，四类形状件具名在内。

### 判据 ②：十个能力门只剩「自家命令声明」这一件真承诺——**部分成立**

成立的一半：

- **19 个 `run*` 全删，实测 0 命中**（`src/{analysis,body,diet,exercise,goal,home,photo,profile,weight,workout}/index.ts` 无一个 `export function run*`）。
- 包门 `src/index.ts` **26 LF**，注释即口径「63 个值名」，402 → 63 成立。
- `exports` 由 8 键收成 3 键。

不成立的一半（**这一半在地图上是未裁的雾，不是已决的事**）：

- 十道门当刻仍有 **14 个非声明对外名**，与 #703 范围外读数逐个对得上（脚本按 `export {} from` 逐个取，14/14 全部命中，无一个失配）：

  | 门 | 全部对外名 | 非声明 | 名字 |
  | --- | --- | --- | --- |
  | `body` | 2 | 1 | `bodyReceiptDoc` |
  | `diet` | 3 | 2 | `buildMealDistributionView`／`MealDistributionView`(型) |
  | `exercise` | 2 | 1 | `exerciseBackfillBridge` |
  | `profile` | 2 | 1 | `profileReceiptDoc` |
  | `weight` | 6 | **5** | `weightReceiptDoc`／`getWeightGoalInfo`／`weightTrend`／`WeightGoalInfo`(型)／`WeightTrend`(型) |
  | `workout` | 5 | 4 | `getPlan`／`PlanConfigRow`(型)／`PlanMovement`(型)／`PlanSessionRow`(型) |
  | `analysis`／`goal`／`home`／`photo` | 1 | 0 | —（只剩自家命令声明，已达判据） |

- #703 建议「在 #704 之后另开票」——**那张票没有开**。
- 其中 `src/weight/index.ts` 当刻 **6 件**，门自己注释写明「**已越铁律五「一个文件不多于五个」**」。这是一条**当前在册、无票承接的违规**。
- **增量发现**：这些门余名字**不是「没有调用方」**——门自己的注释逐条写明：`getWeightGoalInfo` 供 `goal/goalExtraPlate.ts`、`weightTrend` 供 `analysis/dashboard.ts`、`getPlan` 供训记取数口、`exerciseBackfillBridge` 是训记回写桥。⇒ 它们不是 #705 那种「删掉即可」的孤儿；**要做的是改走别处或裁定「跨能力取数该不该走门」**，这与 `Not yet specified` 第 1 条（生产自己要用的「声明面」要不要开门）是同一件事。

### 判据 ③：`src/render/` 只剩跨能力共件（写得出哪两个能力在用）——**未达成**

| 读数 | Destination 写的历史值 | #704 当时 | **当刻实测** |
| --- | --- | --- | --- |
| `src/render/` 件数 | 37 件 | 24 件（#716 收窗） | **24 件** |
| `src/render/` LF | 10,579 LF | 7,429 LF（#716 收窗） | **7,431 LF** |
| 「稳过」判据的件（Destination 点名的 `errors`／`receipt`／`copy`） | 3 件 | — | **3 件仍在，且仍是引用面最广的三件** |

**⇒ 只走了四成（24 ÷ 37 件；LF 7,431 ÷ 10,579 ≈ 70%）。并且「稳稳过判据的那三件之外一律要处置」这条没有落实：当刻 24 件里只有 3 件有「就是它留下的」这个结论，剩下 21 件在本节口径下无归属结论。**

**一条必须点出来的口径冲突（不要替用户裁，但要报）**：Destination 把判据写成「**真共件必须过得了「写得出哪两个能力在用」这一条**」。按当刻树逐件实测，**能过这一条的远不止三件——是 15 件**（下表）。也就是说，**这条判据既放不进三件，也挡不住那 15 件**：`dietDocs`(diet+home+analysis)／`sportDocs`(exercise+home)／`planPlate`(exercise+home+workout) 这些明明住在 `render/` 的**册页件**，按字面判据全都「写得出两个能力在用」。⇒ `src/render/` 到底收成「过得了这条判据的件」还是收成「`errors`／`receipt`／`copy` 这三件」，**是两件不同的事，地图没定**。这是本调查最需要人来拍板的一处。

> **第二轮复核推翻上面这段的框架（保留原文以留痕）**：问题不是「地图没定收成哪种」，而是**判据 ③ 本身就是错的**。`docs/adr/0003-一级目录三类准入.md` 已把 `render/` 裁作第三类**页面交付面**，准入＝「**出整页必经 或 ≥2 个能力域在用**」，并在「被否的选项」里**具名否掉**了「把 `render/` 记作共用位」——原判据用的正是那个被否的判据（共用位的准入规则）。⇒ 判据 ③ 已按 ADR-0003 重写，问题不存在了。**下面这些读数仍然成立**，只是结论从「要裁一个新判据」变成「照已决的 ADR 办」。

**分档明细（当刻实测。判据＝该 `render/` 件被哪些**能力目录**经 `from '…render/<件>.js'` 引用；`cli`／`render` 自身／`shared`／包根件另计，不计入「能力」）：**

| 档 | 件数 | 明细 |
| --- | --- | --- |
| **真共件（≥2 个能力域）** | **15** | `errors`(9 域·14LF)／`receipt`(9 域·243)／`copy`(5 域·46)／`analysisPlate`(analysis,diet·169)／`diet`(diet,home·117)／`dietDocs`(analysis,diet,home·350)／`exercisePort`(exercise,workout·544)／`html`(analysis,workout·586)／`insightPlate`(analysis,diet,workout·298)／`pageChromeCss`(analysis,workout·45)／`planPlate`(exercise,home,workout·337)／`sportDocs`(exercise,home·466)／`trendDocs`(analysis,goal,workout·528)／`trendMiscPort`(analysis,workout·416)／`trendMiscPortDocs`(analysis,workout·618) |
| **单能力自用** | **1** | `index`(仅 `xunji` 经它转出·116) —— **barrel 本体**，正是 #704 排在「最后一批」要退役的那件 |
| **零能力域引用** | **8** | `envelope`(仅 `cli`·208)／`planWizardCss`(124)／`planWizardDocs`(184)／`trendPredictDocs`(1190)／`workoutMovementTable`(158)／`workoutPlanCss`(231)／`workoutPlanDocs`(282)／`workoutPlanLook`(161) |

**第三档那 8 件要拆开读，别当成同一类：**

- `envelope`(208)：唯一消费者是 `cli`——**页面交付面**，第三类机器面本身。
- 其余 **7 件**（`planWizardCss`／`planWizardDocs`／`trendPredictDocs`／`workoutMovementTable`／`workoutPlanCss`／`workoutPlanDocs`／`workoutPlanLook`，合计 **2,329 LF**）：**只被 `render/` 内部件引用**（`html.ts` 引 `workoutPlanDocs`／`planWizardDocs`；`trendDocs.ts:528` 薄转出 `trendPredictDocs`；`workoutPlanDocs` 引 `workoutPlanLook`／`workoutPlanCss`／`workoutMovementTable`）。⇒ 是**「域属页面件住在交付面里」**这一档。

**关键读数（第二轮实测，结论的支点）**：沿 `dist` 从三个真交付出口（`cli/cmd_read`／`cli/delivery`／`output`）做导入可达性分析，**24 件 24/24 全部在页面装配路径上**。⇒ 按 ADR-0003 的「出整页必经」，`render/` 的现状**全部满足，是合法的，不是违规**。「必经」唯一的歧义是读法：**宽读成「经装配层可达」⇒ 24 件全过；窄读成「就是装配层本身」⇒ 只剩 `errors`／`receipt`／`copy`／`envelope`／`html`／各 plate**。这个读法层没定 ⇒ 本图**不改动它们**，登记待裁（已写进地图 `## Not yet specified`）。

**barrel 仍健在**：`src/render/index.ts` **116 LF、52 条 `export … from`、转出 187 个名字**（Destination 写「193 个导出槽、131 值 ＋ 62 型」，当刻实测 187，差额未逐个定位）。真消费者只有 `src/xunji/planSource.ts:25` 一处（另外 `triggers/routing.ts`／`weight/plate.ts`／`render/diet.ts`／`render/trendDocs.ts`／`render/trendPredictDocs.ts` 五处只是在**注释里**提它，不是真引用——**核这条时最容易把注释当引用，本席逐行核过**）。#704 把它排在「最后一批」。

**#704 自己量清但从未落实的剩余批次**（#704 解决评论 §七白纸黑字，`#716` 与 `#719` 正文各自复述过一遍），第二轮复核后的处置：

- **第五批**：计划向导与计划页簇六件 1,140 LF，全是 `render/` 内部件、外面经 `html.ts`（当刻 586 LF，同时是 analysis／workout／cli 的入口）⇒ **并入「域属页面件住在交付面里」那条待裁**，不另开施工票；
- **第六批**：跨能力件各自归位十一件 5,058 LF ⇒ **撤销**（前提不成立：24/24 在装配路径上，照 ADR-0003 本来就该留在 `render/`）；
- **最后一批**：真共件与 barrel 收口——`errors`／`receipt`／`copy` 的落点照 ADR-0003 就在 `render/`；`render/index.ts`（116 LF／187 个导出名）的退役归「`render/index.ts` 怎么退役」那条待裁。

**净缺口（修正后）**：`render/` 24 件里 **15 件 ≥2 域在用、1 件是 barrel 本体、8 件零自定义能力域**；按 ADR-0003 **没有一件是违规**。唯一待裁的是那 7 件（2,329 LF）的落点读法。

---

## 三 · 结论二：12/12 与地图正文对不上，正文还在说「未完成」

地图的 `## 任务清单` **只列 11 项**，但 sub-issue 是 **12 个**——**第 12 个（`#708`）在正文里一个字都没有**。逐项核对：

| 清单项 | 票 | 正文框 | 实况 | 对账 |
| --- | --- | --- | --- | --- |
| 页内重复小件合并 | #700 | `[x]` | 已关，出规格 | 对 |
| 同一件事只留一处定义 | #701 | `[ ]` **未勾** | **已关**，出规格 | **正文陈旧** |
| 测试面改成打在接口上 | #702 | `[x]` | 已关 | 对 |
| 包对外公开接口缩到一页 | #703 | `[ ]` **未勾** | **已关**（2026-09-18T14:02:54Z） | **正文陈旧** |
| 页面件搬回（#704 总） | #704 | `[x]` | 已关 | 对 |
| 页面件搬回（第三批） | #714 | `[x]` | 已关 | 对 |
| 页面件搬回（第二批） | #715 | `[ ]` **未勾** | **已关** | **正文陈旧** |
| 页面件搬回（第四批） | #716 | `[ ]` **未勾** | **已关**（2026-09-19T01:47:46Z） | **正文陈旧** |
| 没有调用方的四处 | #705 | `[x]` | 已关 | 对 |
| 规格：口径唯一 | #717 | `[ ]` **未勾** | **已关**（2026-09-19T02:56:31Z） | **正文陈旧** |
| 规格：四类形状合并 | #719 | `[ ]` **未勾** | **已关**（2026-09-19T02:59:57Z） | **正文陈旧** |
| **（正文未列）** 规格：清掉没有消费者的四处 | **#708** | **无此行** | 已关 | **正文漏列** |

⇒ **6 项勾错、1 项整行漏列**。任何只读地图正文的人，看到的是一张「六件未完成」的图——与 12/12 完全相反。**这正是「数字说完成、正文说没完成」的成因。**

---

## 四 · 结论三：逐票核关／开状态——12/12 是真关闭，但多数带着「写进评论的未完成项」

**没有一个误开或漏关**：12 个 sub-issue 全部 `state=CLOSED` ＋ `stateReason=COMPLETED`，且每个都有实质解决评论与提交号。

但**「票关了」≠「票面的事做完了」**。逐票账：

| 票 | 交付 | 票面写清但**当刻仍未做**的 |
| --- | --- | --- |
| #700 | 七问裁定 | 实施全在 #719（已做） |
| #701 | 五处口径裁定 | 实施全在 #717（已做） |
| #702 | 两道机器门 ＋ 测试面口径 | ① 「算式走能力门」**改归属为源侧加门**，与 #703「包门只许收窄」冲突 → **须那侧裁定，未裁**；② 深路径天花板 24/286 那条**未处置** |
| #703 | 19 个 `run*` 全删、`doc` 位、402→63、exports 8→3 | ① 门余 **14 个非声明对外名**，票面建议「另开票」→ **未开**；② `render/index.ts` 整体退役归本线 → **未做** |
| #704 | 第一批六件迁 `src/workout/` | **第五／六／最后三批从未开票**；`html.ts` 处置未裁 |
| #705 | 四处清账（生产零变化） | 另开 #708 落地，已关 |
| #708 | 71 页逐字节相同 | `src/shared/contentSuffix.ts` 的「第二个用法」判据**仍未达成**（唯一消费者是包根 `output.ts`） |
| #714 | 复盘簇两件迁 `src/workout/` | — |
| #715 | `sportPortDocs` 迁 `src/exercise/` | — |
| #716 | 照片族四件迁 `src/photo/`；`render/` 28 件→24 件 | ① `helpShell.ts` 兼容垫片**未退役**（其存在理由已随本窗作废）；② `dist` 孤儿产物 **28 件**；③ `t440` 既有红转入措辞 |
| #717 | 六处口径归位 ＋ 防复发门 | ① 谓词字面收口**已做完**（收口读数 29 处 ＋ 正本 2 处）；② 「逐字抄件合并」判「无对象」；③ 速率阈值判「各有其用不合并」——**②③ 两项按「不做」登记并附了理由** |
| #719 | 三类形状各 1 处、34 文件 65 调用点不动 | ① **来源脚注那一整类未做**（判假重复）；② 窗口②「65 处改指共件」未做（判「非搬迁债务」）——**两项都有具名理由** |

**读法**：#716／#717／#719 的未做项是**当代决议**（有理由、可接受）；**#702／#703 的未做项是悬空的**——票面明说「须裁定」「另开票」，票一关就没人接了。

---

## 五 · 结论四：`Not yet specified` **10 条**里没有一条毕业，其中 2 条已经作废

逐条核当刻状态：

| # | fog 条目 | 当刻状态 | 处置 |
| --- | --- | --- | --- |
| 1 | **生产自己要用的「声明面」要不要开门** | **仍冲突**：#702 第 1 条要「算式走能力门」，#703 要「包门只许收窄」，两句话正面撞。地图自己写「得先裁」。挡着判据 ② | **真 fog，需一张裁定票** |
| 2 | **350 行台账那批等票认领的拆件** | 门当刻读数 **扫描面 344 件、超线 41 件**；台账里「拆法写好、零票承接」的约二十件。原口径「等第一条搬迁票做完后再定」——**搬迁票已做完四批了** | **条件已成熟，需裁定并入或另立** |
| 3 | **形状共件若落在公共层**（`base-render`） | `#700` 已裁「落包内共用位」，本条自认「还缺一张票」——**那张票未开**；`base-paint` 的 `renderFactStrip` 仍并存 | **需一张票** |
| 4 | **卡路里页面产物没有常驻快照门** | 仍在用一次性重出器 ＋ 逐页 sha256。#704 已给出必须的前提（`receipt.ts:155` 的 `nowStamp()` 无注入口） | **需裁定** |
| 5 | **`render/index.ts`（193 导出槽）与 6 条公开子路径怎么退役** | **`render/index.ts` 当刻 116 LF、仍健在**；6 条子路径已由 #703 收成 3 键 | **半条未做，需并入上面第 1 条或另开** |
| 6 | **存活谓词改完要不要一道门** | **作废**：#717 批⑤已立「定义地闭集账」（`check-one-path.mjs` 扩），门当刻 `PASS: 定义地闭集账 6 项全在台账内`，且谓词字面已收口 | **可删** |
| 7 | **共用位的「第二个用法」仍无机械判据** | `contentSuffix.ts` 仍未过判据 | **需并入或裁定** |
| 8 | **又一处「源码常量名上屏」待随搬迁窗收口** | #708 已勘误到 `renderDietHtml:84`；#714～#716 搬迁窗已关 | **须复核是否真收口** |
| 9 | **多人共用一棵树时「包级测试全绿」不可归因** | 各票已改走可归因判据（提交范围法／只取不由施工者控制的读数） | **可作废** |
| 10 | **搬迁剩下的三批（第五／六／最后）** | **一条都没动，一张票都没开**（其余批次已由 #714／#715／#716 吃掉） | **本调查的核心缺口** |

---

## 六 · 结论五：未建立 sub-issue 关系的相关 issue——查完了，没有漏网的

- 全仓 issue 面扫过（400 条上限）：与「卡路里包结构整改」直接相关而**未挂 #699** 的只有 **#724 [规格] 页面装配骨架与按域页型表**。
- **#724 不属本图**：它是 `Part of #681`（饼干记账本体图），`ready-for-agent`，由 #685 讨论票收敛而来，详细规格件住 `docs/skills/skill-bill/`。它对本包的关系是**明确声明为 Out of Scope**：「卡路里侧 110 处调用点与其页型表（本规格只让它改走同一份机制）」。**两条线不交叉，不必并图。**
- `#723`（四家单品页签槽注册）是宿主面缺陷，不属本图。
- 其余 open 票（#691／#690／#689／#678）分属 #681 与 #671 两张别的地图。
- **没有发现「提及 #699 却未挂 sub-issue」的票。**

---

## 七 · 结论六：其他可能情况——逐条排查后的判定

| 假设 | 判定 |
| --- | --- |
| 已真实完成、只是忘了 close？ | **否**。Destination 第 3 条未达成，第 2 条半达成，且 #704 §七的三批从未开票 |
| 计数与实际不符？ | **是，两层不符**：① 正文列 11 项、实有 12 个 sub-issue（漏列 #708）；② 6 个勾陈旧。但 **12/12 这个数字本身是真的** |
| 有 sub-issue 被移出或漏计？ | **否**。`gh api .../sub_issues` 返回恰好 12 条，与全部已关闭票一致 |
| 「已关但实际没做完」？ | **是**：#702／#703／#704／#716 各带**无着落**的未做项；#717／#719 各带**有具名理由**的「按不做处置」；#708 带一项仍未达成的判据（`contentSuffix.ts`）。逐条见第四节表 |
| 进度段与关票状态对不上？ | **是，#705 一张**：票 `CLOSED`、正文 `95%`（其余 11 张都写 `100%`）。处置见第十节 |
| 「已完成却漏标 CLOSED」？ | **否**。12 个都是 `COMPLETED` |
| 有人工遗漏的收尾动作？ | **是，两处**：① 地图正文的任务清单从未随收口同步（6 个勾陈旧 ＋ 漏列 #708）；② **#705 票已 `CLOSED` 而正文停在 `95%`**——进度段没跟上关票动作 |

---

## 八 · 这些缺口是怎么来的（根因，不是事故）—— ⚠️ **本节结论已部分更正**

> **更正**：根因分析本身成立（分批票把批次定义推给解决评论，没人负责把最后几批变成票）。但本节末尾那句「这张地图要是现在 close，那几批会正式变成孤儿」**已被推翻**——那几批活的前提（第六批）本身不成立，第五批与最后批并入了「域属页面件住在交付面里」那条待裁。**根因留作教训，不构成「不许 close」的理由。** 真正的教训见第十一节。

补查了建图当刻的草案区（`.scratch/wayfinder-s2/`，**11 个文件：地图正文 ＋ 6 张票面 ＋ 建图脚本 ＋ 27 行修正对照表**，时间戳 2026-09-18 19:22–19:24），根因清楚了：

1. **建图只切了 6 张票**（`t1 页内形状共件`／`t2 口径唯一`／`t3 测试面`／`t4 门与公开面`／`t5 页面件搬回`／`t6 四处出清`），全部进图。
2. **搬迁那批的边界与顺序，原票面明确写成「在解决时才定、不作为预置」**——`t5-页面件搬回.md:5`「本票只**立判据 ＋ 裁清关系 ＋ 搬第一批**，其余批次在解决时毕业成新票」、`:42`「其余能力的批次：在本票解决评论里给出**分批顺序与每批边界**，毕业成新票，**不预先切进本图**」。
3. **#704 照办了**：解决评论 §七给出了完整分批表（第二～最后一批），并把能立即做的三批毕业成 #714／#715／#716——**这三张都做完了**。
4. **但「其余批次毕业后要有人接着开票」这一步没有机制**：`t5` 只授权「毕业成新票」，没说「谁在第几批做完后开下一张」。四批做完之后，剩下的第五／六／最后批**既不在图里、也没有触发者**，就停在那里。
5. **`t5:44` 还把 `render/index.ts` 整体退役指给了 #703**（「交给『包对外的公开接口缩到一页』那张票」）——#703 关票时把它列进「范围外读数」建议另开票，**同样没人接**。

⇒ 这不是「有人忘了勾选框」，而是**「分批票」这种切法的结构性漏口**：把批次定义推给解决评论，就没人负责把最后几批变成有编号的票。**这张地图要是现在 close，那几批会正式变成孤儿。**

---

## 九 · 建议 —— ⚠️ **本节已被第二轮复核推翻，保留以留痕，不照办**

> **推翻理由**：本节四张票全部建立在「地图判据 ③ 是对的、活没干完」这个前提上。第二轮查仓规后，判据 ③ 被判定为**与 ADR-0003 冲突的错判据**，第 2／3 张票随之失去对象（第六批前提不成立、第五批并进一条待裁），第 1 张根本不是票（是修正会话），只剩第 4 张可按「孤儿名字清算」保留。**最终处置见第十一节。**

### 方案 A（初稿推荐，已废）：先补票、后收图

理由：Destination 第 3 条是**本图自己写死的到岸条件**，第 2 条还剩一条在册违规（`weight/index.ts` 6 件越铁律五）。现在 close 等于把「已量清、已排好批次」的活重新打回雾里。

要补的票（按依赖排序，共 4 张）：

1. **正文索引修复**（无依赖，可立刻做）：把地图 `## 任务清单` 补成 12 项（补 #708 一行）、6 个勾按实况改对。
2. **第五批 ＋ 最后一批**（要先裁 `html.ts` 的处置）：计划向导与计划页族六件 1,140 LF ＋ barrel 收口。**这一张是解开 `src/render/` 的关键**。
3. **第六批**（跨能力件十一件 5,058 LF）：原被 #702 挡，#702 已关 ⇒ **阻塞已解除但要核 #702 改口径后的实际约束**。
4. **门余与声明面**：14 个非声明对外名 ＋ `weight/index.ts` 6 件越铁律五 ＋ 「算式走能力门 vs 包门只许收窄」的正面冲突 —— 一张裁定票。

同时把 `Not yet specified` 的 **10 条**按第五节表逐条毕业或删除：**第 6／9 条可删**（已被实际做掉或本来就无对象）；**第 1／2／3／4／5／7／8 条**各需一张票或并入上面 4 张；**第 10 条**就是上面第 2／3 两张票要吃掉的东西。

**并补一条机制（照第八节的根因）**：「分批票」把批次定义推给解决评论，就得**同时指定「谁在上一批收窗时开下一批的票」**；否则批次走完、图也关掉之后，剩下的批次正式变孤儿。本图已经踩了一次。

### 方案 B：现在就 close 地图

代价：必须**先**把第 3 条改写成「实际达成的样子」（`src/render/` 收成真共件 ＋ 一个 barrel），并把三批未开票的活**转成 Out of scope 或另立新图**。好处是地图不留欠条。

### 方案 C：折中

先把可独立收的口（`#716` §七的 `helpShell` 垫片退役、`dist` 孤儿 28 件、`render/index.ts` 退役）合成一张「收口票」，跑完再决定 close。

---

## 十 · 进度契约：本调查发现的「进度不符」须据实标注

**12 张票的正文 `## 进度` 段逐个核过，11 张是 `100%`、1 张是 `95%`：**

| 票 | 正文进度 | 与「已关」是否一致 |
| --- | --- | --- |
| #700／#701／#702／#703／#704／#708／#714／#715／#716／#717／#719 | `100%` | 一致 |
| **#705** | **`95%`** | **不一致**：票已 `CLOSED`，正文却停在 95% |

**#705 那 95% 的处置**：它的交付物是「出决议 ＋ 开规格单」，决议已由用户逐条拍板、规格单 `#708`（已关，2026-09-18 落地）承接并完成。⇒ 按进度契约应据实标成 **`100%` ＋ 一行说明「实施由 #708 落地，提交 cb2d0e2d ＋ 9f6a7cd3」**，而不是改回未动工。**其余 11 张无需回改。**

需要动的是**地图本体**（#699）：

- 地图 `## 任务清单`：补 #708 一行 ＋ 6 个勾按实况改对（**当时显示为「六件未动工」是失真的**）——**已做**；
- 地图整体进度：初稿说「按方案 A 补票后仍应标注未完成」——**该结论已随方案 A 一起作废**；实际按第十一节处置（改判据 ③ → 到岸 → close）；
- **`weight/index.ts` 的 6 件对外名是一条在册违规**（越铁律五「一个文件不多于五个」）——**已在收尾评论点名**，并写进地图 `## Not yet specified`。

---

## 十一 · 最终处置（2026-09-19，第二轮复核后实际执行的）

**判据 ③ 改判 → 三条判据复核 → 关闭本图。** 未按第八、九节办。

### 做了什么

| 动作 | 结果 |
| --- | --- |
| 判据 ③ 重写 | 改为以 ADR-0003 第三类「外壳与机器面」为准（出整页必经 或 ≥2 个能力域在用），并在 Destination 里写明**改判理由与被否选项的出处** |
| 三条判据复核 | ①成立／②主体成立（`run*` 0 命中、包门恰 63 值名 0 类型、exports 8→3）／③改判后成立 |
| 第六批撤销 | 见上（24/24 在装配路径上，前提不成立） |
| 地图正文 | 任务清单补成 **12 行**、6 个陈旧勾改对；`Not yet specified` 逐条核过（删 2 作废、撤 1、毕业 1、新立 1）；新增 `## 已撤销 / 已作废` 留痕段；**`## Notes` 与 `## Out of scope` 逐字节未动**（构建脚本 19 条断言核过） |
| 收尾评论 ＋ close | [issuecomment-5739123115](https://github.com/FeatherHunter/ilife/issues/699#issuecomment-5739123115)；`#699` → `CLOSED`／`COMPLETED`（2026-09-19T03:48:01Z） |

### 四道门当刻全绿

告警线 `RESULT: 93/93` ＋ `PASS`；`check-one-path` 两条 `PASS` ＋ `--selftest RESULT: 5/5`／`SELFTEST: PASS`；`check-page-assert PASS`；`gen-cli --check PASS：键 134（写 52 ＋ 读 82）；能力 10 个声明 134 条`。

### 已知债（已写进地图 `## Not yet specified`，不阻塞关闭）

门余 **14 个非声明对外名**（**11 个是孤儿**、3 个真有跨能力消费者）／`src/weight/index.ts` 6 件**越铁律五**／`src/photo/helpShell.ts` 垫片理由已失效但生产仍在用／`dist/render/` 28 件孤儿产物（`dist` 被忽略）／台账里 `scripts/build-help.mjs` **重复一行**而门仍绿／`render/` 里 7 件（2,329 LF）**域属页面件住在交付面里**待裁读法。

### 本文件的两条自我更正

1. **不要在别人的判据上推演**。初稿把地图写下的到岸判据当成既定事实，于是在一条**与 ADR 冲突**的判据上推出四张施工票。正确顺序是**先质疑目标，再谈施工**。
2. **「100%」这类汇总数要拆开看**。它数的是子议题，不是目标达成度；正文的复选框又长期没人同步。**两边都不可信，只有逐条对当刻树复核才算数。**

---

## 十二 · 复跑读数留档

```
# 告警线门（当刻）
SCAN-ROOT: D:\ilife\packages\skill-calorie
AGENTS.md：台账 45 行；扫描面 344 件，超线 41 件，剔出生成物 3 件；挂号台账命中 2/2
RESULT: 93/93
PASS: 告警线台账齐全且与实况一致          [exit=0]

# 结构门（当刻）
PASS: 测试面每个符号只有一条 dist 路径（包门与薄转出声明按源码派生豁免）
PASS: 定义地闭集账 6 项全在台账内（#717 批⑤）   [exit=0]

node packages/skill-calorie/scripts/check-one-path.mjs --selftest
RESULT: 5/5
SELFTEST: PASS                                  [exit=0]

node packages/skill-calorie/scripts/check-page-assert.mjs
PASS: spawn 真交付出口并断成功的测试件都断到了信封   [exit=0]

# 子议题清点
gh api repos/FeatherHunter/ilife/issues/699/sub_issues?per_page=100  → 12 条，全 CLOSED/COMPLETED
```

**顺手抓到两处与本图无关、但会影响后续票据读数的小毛病**（只登记，未修）：

1. **台账有一行重复**：`packages/skill-calorie/AGENTS.md` 的告警线台账里 `scripts/build-help.mjs` **占了两行**（同名、同挂号值 `—`、同实测 752、同结论「超因与拆法待补」）。台账数据行当刻 **45 行**，但门报 `RESULT: 93/93` 仍是绿——说明这道门**不查件名唯一性**。后续任何票若指望「台账一行一件」，先知道这件事。
2. **门读数 93 与台账 45 行不是同一把尺**：`check-warning-line.mjs` 报的 `n/n` 是**判据条数**，台账行数要另数。

**本席对本次调查的处置边界**（照技能规矩与用户指令）：**只读**——未 `git add`／未 `git commit`、未 close 任何 issue、未改地图正文、未改任何源码或台账。产出只有本文件与 `.scratch/t699/` 下的一次性脚本与原始读数。

一次性脚本与原始读数：`.scratch/t699/{verify-tree,verify2,graph,scan}.mjs` 与同名 `.txt`（`.scratch/` 不入仓）。
