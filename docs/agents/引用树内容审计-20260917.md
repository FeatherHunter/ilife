# 引用树内容审计 · 归档（2026-09-17）

**这份管什么**：把「从 `AGENTS.md` 起画 md 引用树 → 逐层判存在必要与命名 → 按 `writing-for-agents` 审内容 → 按工作令修复」这一轮的事实、台账、冻结基线、遗留，收在一处。事实截至提交 `c22fde10`。

**边界先说清（别当它覆盖全仓）**：

- 审计面**约 182 件**（按引用树 264 件减未覆盖 83 件计；其中可从落盘件复算的**去重 177 件**，见附录 B）。**L10–L12 的 53 件从未审过**（关票留痕为主），另有约 25 件各层漏网。
- 修复面 **37 件**（审计判 trim／restructure 的那批里，本轮做掉的部分）。**审计结论只是意见，不是已验证的真值**——第一轮就产出过一条假事实（见 §四.1）。
- 8 份文档按冻结基线**故意未动**；11 件 restructure **一件未动**（须连测试一起改，另开票）。

---

## 一、交付件

| 件 | 落点 | 说明 |
| --- | --- | --- |
| 引用树页面 | `docs/agents/引用树-20260917.html` | 从 `AGENTS.md` BFS 的**修复前快照**（2026-09-16 扫描）：265 件可达 / 13 层（L0–L12）/ 仓内引用 1066 / 缺失引用 303 / 外链 4。四视图：主干树、分层总览（点卡片看上下游链路）、明细表、缺失与外部。重跑口径见附录 A |
| 修复台账 | 本件 §三 | 4 次提交、37 件，逐件一句话 |

**提交清单**（全部经 `node tooling/run-locked.mjs --ticket md-audit` 排队提交）：

| 提交 | 范围 | 件数 |
| --- | --- | --- |
| `2c604793` | 入口与纪律面：`AGENTS.md` 指针 7 处、`CONTEXT.md`、`docs/agents/structure.md`、`命令登记纪律.md`、`wording.md`、受限三件（`docs/p10-scaffold.md`、`docs/visual-spec-help.md`、`docs/visual-spec-blocks.md`） | 8 |
| `3c256099` | 技能面与 free 组 | 15 |
| `299b2ef9` | 返修波 R2 通过件（含行号等量三件） | 8 |
| `c22fde10` | 主管亲手收口（含一处被推翻的假事实订正） | 6 |

---

## 二、三件事的结论（一句话各一）

1. **引用树**（问题一）：`AGENTS.md` 直接点名 10 件（L1），`docs/agents/doc-homes.md` 一次带出 20+ 去向是最大分叉口（L2 25 件），`docs/base-paint-contract.md` 再带 19 件进 research／skill-calorie 深水区，一路到 L12。**回指最多的三个**：`packages/skill-bill/SKILL.md` ×125、`docs/agents/structure.md` ×52、`AGENTS.md` ×38。
2. **存在必要与命名**（问题二）：**255 件**可达文件判 **keep 126 / archive 129**；重灾是关票留痕（各技能 `t*-resolution`、`t*-review-*`、`impl-notes`）与 16 份按 `doc-homes` 该搬家的 `docs/` 根存量件。
3. **内容质量**（问题三，`writing-for-agents` 标尺）：跨层七个通病——指针病（触发词沉句中／多分支共用一个触发器／指针复述正文身份）、`t202` 式 stale 句簇、版本号钉死沉积、快照复述、否定无正面、引导词未收敛、臃肿大件（冻结正文＋执行指引＋过程台账三混）。

---

## 三、修复台账（37 件）

**入口与纪律面**（`2c604793`）

- `AGENTS.md`：7 处指针（触发词前置、删身份复述、补分支清单、术语统一为「共用位」、补管辖例外）。**并发纪律红线整段保留**——主管否决「压成纯指针」，独立复核判否决成立。
- `docs/agents/structure.md`：依赖单向与就地摆正交复述合并、铁律五判据拆句、来源节压一行（125→123 行）。
- `docs/agents/命令登记纪律.md`：两处与 §四 重复的括注缩为指针、「（同 §四）」指代改正、纯禁令补正面落点（170→169 行）。
- `docs/agents/wording.md`：「打通」复本删（正本留通用表）、说明列收敛为一说、底座／基座并一行、尾注剪动员语（67→66 行）。
- `CONTEXT.md`：右侧槽条正面在前；删与「不列作准」自相矛盾的 10 分组枚举。

**技能面**（`3c256099` / `299b2ef9` / `c22fde10`）

- `docs/skills/skill-calorie/t293-验收-命令自治.md`：P1=FAIL 旧快照标历史；硬编码行数与 `ci.yml` 行号改指针式。
- `docs/skills/skill-calorie/t291-三件裁定.md`：甲乙待选改已决态（决议乙 ＋ 链证据件）。
- `docs/skills/skill-calorie/core-approach.md`：矛盾计数加「现行拆解见 t163」指针；两处否定转正面。
- `docs/skills/skill-calorie/t341-页面形状.md`：基线十页 sha 列值删（值以脚本为准）；过期票面行删。
- `docs/skills/skill-calorie/t169-设计定稿.md`：「不许动」缩为「见 §四」；读法句并进现有行行尾——**总行数守 249**，5 处外部行锚复位。
- `docs/skills/skill-calorie/t283-候选与快照.md`：六处复述收敛为文首现状注 ＋ 指针；GATE-RUN 三行并一表。
- `docs/skills/skill-calorie/t474-过程型-文本.md`：§七 行数指针取错源已改正；§三 GATE-RUN 缩单行。
- `docs/skills/skill-calorie/t524-改前读数.md`：6.1 与 §7 的 vision 理由去重；旧口径节折为存档指针。
- `docs/skills/skill-calorie/t518-W4-收尾-证据.md`：逐字节四种说法收敛为「逐字节同（DIFF=none）」；§二 首补术语行。
- `docs/skills/skill-calorie/t518-复核-证据.md`：J5 补录格标「W4 时点旧数；当刻值见 §十四·六」。
- `docs/skills/skill-calorie/t294-登记与分派接缝.md`：括注收窄到棘轮件真正冻结的两个行数；条数「三条」→「四条」；**老路 bullet 按棘轮件自述改写**（原断言「未登记注册表 + 照旧落原 switch」已被 #320 证伪）。
- `docs/skills/skill-calorie/t401-融合设计.md`：`eyebrow`→眉标 全件归零；段序「第 3 段」→「第 4 段」（正本 `spec/text.ts:25` 的 `LOG_SECTIONS`）。
- `docs/skills/skill-calorie/t425-融合基准.md`：**订正一处被推翻的假事实**（见 §四.1）；裁定 5 补指回裁定 4 的空窗／空库两态。
- `docs/skills/skill-calorie/t425-样张-今日饮食.html`：同一假事实的第二处落点同步订正。
- `docs/skills/skill-calorie/t164-页面清单与公共层.md` ＋ `docs/skills/skill-calorie/06-目标管理-工作流程.md`：一图一目录与守卫句缩指针；票图逐边补证据；端到端边由 `#255→#257` 改为 **`#256→#257`**（`#257` 正文自述阻塞于「锁 · 真出口用例」）；06 补分支读法清单。
- `docs/skills/skill-chef/t2-body.md`、`t7-body.md`；`docs/skills/skill-home/t184-bill-recipe.md`、`t188-facts-badge-types.md`；`docs/skills/skill-memo-ilife/map-220-body.md`、`t220-orchestrator-decisions.md`、`packages/skill-memo-ilife/AGENTS.md`：过期状态行更新、重复句删、术语收敛（静默退色／_2 起／老骨架）、落盘口径改正面、词表去重。

**free 组**（`3c256099` / `299b2ef9` / `c22fde10`）

- `docs/env.md`：五处 `TBD` 按实测回填 ＋ 哨兵命名规则。
- `docs/public-installer-47.md`、`docs/skill-landing-r2.md`：版号按盘上实测同步（0.1.x → 现行）、`#50` 勾选、`--only` 现状态。
- `docs/calorie-dual-path-acceptance.md`：版本钉死改读盘查询式；空库同文禁令四处去重（**探针四串保活**）。
- `docs/calorie-architecture.md`：`:58` 指空的指针改真实出处；**总行数守 99**（源码写死了 `:60`／`:54` 引用串）。
- `docs/research/benchmark-visual-spec.md`：KPI 标签取舍按实测改写（12px/600，非 13px/500）并改正适用面；**总行数守 969**（`visual-spec-help.md` 有二十余处行锚）。
- `docs/p10-scaffold.md`：`workspace:*` 改正为实测口径；Deferred 四条逐条补现状（**六条安装命令串逐字保活**）。
- `docs/visual-spec-help.md` / `docs/visual-spec-blocks.md`：H-17／H-04／H-16 与 B-11 等订正、复述表折叠（**20 条 H 与 12 条 B 标题形态与编号保活**）。

---

## 四、复核逮到的问题（方法学留痕）

| # | 问题 | 谁逮到 | 处置 |
| --- | --- | --- | --- |
| 1 | **审计自己写了假事实**：称「`gh issue view 428` 返回不存在；样张引 #428」 | 独立复核席（`gh` 直读 ＋ `git log -S` 三路读数） | 推翻：样张原文引的是 **#427**（真实存在、已关），**#428 也存在**（另一张公共层票，判据硬化）。两处落点已订正并注「2026-09-17 重测」 |
| 2 | **把拆改题做成追加题**：`命令登记纪律.md` L46 双括注拼接 | 试点波复核席 | 缩成一个括注 |
| 3 | **脆锚**：「照 65 行的医嘱重建」写死行号 | 试点波复核席 | 改无行号锚 |
| 4 | **事实错误**：`visual-spec-help` H-16 把「页面侧 1ms 轮询」标成 CDP 采样（照它用会假红） | 试点波复核席 | 按 `t121-copied-runtime.md` 主口径更正 |
| 5 | **数字与盘上相反**：KPI 标签 13px/500 | R2 复核席 | 按 `blocks.ts:1675-1679` 实测改写 |
| 6 | **指针指空**：`calorie-architecture.md:58` 指向的件里 0 处 `#111/#113/促进` | R2 复核席 | 改真实出处 |
| 7 | **追认已证伪的断言**：t294 老路 bullet 只加「历史」标记 | G1 复核席 | 棘轮件自述「老路活口＝空集」，改写为「#294 当刻 / #320 起现状」两段 |
| 8 | **插空行打歪外部锚**：t169 全篇 +2，打歪 5 处行锚 | R2 复核席 | 并进现有行行尾，总行数回 249、锚复位 |

**由此得一条口径**：审计件给出的「事实」只能当**线索**，落笔前必须读盘复测；本轮 8 次纠正里有 3 次是审计自身出错。**「审计结论 = 真值」不成立。**

---

## 五、遗留（另开票）

**A. 需要另开票的收口项**

1. `docs/skills/skill-calorie/t164-页面清单与公共层.md:92`「`#288` 在图里没有连线」与同件票图冲突（图里 #288 有连线）；`:100` 的「三档」标注与 `06-目标管理-工作流程.md:11` 的实际三档不符。
2. `docs/research/t94-freeze-evidence.md:316-320` 记「`benchmark-visual-spec.md:13` 那句按裁定保持原样」——该句已删，记录成假。
3. `docs/skills/skill-home/t188-facts-badge-types.md` 对 `packages/base-render/assets/help-template.html` 的行锚整体 **+21**（HEAD 即存在，非本轮引入）。
4. 裁定 8 的「页脚按钮手机端纵向均分」若要提公共层票，须先复核 `#654`（已撤回 ≤640 单列、改「底部并排两颗」）。
5. `t294` 那件**连续三席返回 null**（工具侧问题）；最终由主管机器核对（棘轮件 L211-217 ＋ 两键声明的实读）。
6. `t425` 的审计条目「改②③④」经主管判定为**过度概括**（L79 是柱身、L119 是空态句、裁定 5 是跨空档虚线，不是同一概念），记 declined——不是没做，是裁定不做。

**B. 本轮未动的功能面**

7. **11 件 restructure**：`docs/base-paint-contract.md`（§8 台账下沉）、`t295-返修证据`、`t185-content-reconcile`、`docs/research/t71-help-dissect`、`docs/research/t72-shared-layer-gap`、`docs/research/t92-contract-freeze-brief`、`t236-structure-design`、`docs/skills/skill-chef/t2-content-reconcile`、`docs/skills/skill-chef/map-chef-body`、`packages/skill-calorie/SKILL.md`、`t295-命令汇总位改生成物`。共同点：**要新建/搬文件或改行锚，须连测试或生成器一起改**。
8. **8 份 frozen 未动**（见 §六）。
9. **别席在途**：`packages/skill-calorie/**` 全程未碰（当时有 7 件已修改未提交）。

**C. 审计面缺口**

10. **L10–L12 的 53 件从未审**；另有约 25 件各层漏网。补审优先看四个在途件：`docs/skills/skill-schedule/t199-structure-verdict.md`、`docs/skills/skill-calorie/t484-响应式.md`、`t493-基线重落.md`、`t499-基线再落.md`；其余关票留痕按 archive 处置。

---

## 六、冻结基线（改这些文档前必读）

改动前先查「谁真读盘、读出什么断言」。三档：**frozen**＝改形即红；**constrained**＝只咬形状；**free**＝无真读盘消费者。

| 文档 | 档 | 门禁 | 必须原样存活的东西 |
| --- | --- | --- | --- |
| `docs/base-paint-contract.md` | frozen | 5 处 | 8 对 `FROZEN-SURFACE-TABLE-START/END` 标记；区内**恰 148 行**且与 `packages/base-render/src/spec/index.ts:47-216` 的 `SPEC_FROZEN_SURFACE` 六列逐字相等；`## 2.` 表恰 26 行／`§3.4` 段序表恰 6 行／`§3.1.2⑤` 次序表恰 8 行；```css `:root {` 块与 `buildStyleSheet().css` **逐字节相同**；`AC-1…AC-17`、FX 落点字面 27＋18 条；全文无 BOM、不得出现字面 `\n` |
| `docs/{bill,home,memo,schedule}-migration-split.md` | frozen | 各 4–5 处 | 围栏标记逐字（```bill-keys／```home-keys／```memo-keys／```schedule-keys）；块内**恰 16／21／11／8 行**、每行 `key | shape` 两列且 key 过 `parseRegistryKey` 原样回环、shape ∈ 六形状白名单；全文须含 `定时`／`出 scope`／`combos.yaml`（bill／home）、`写走 receipt`（bill／home）、`reminder_scheduler`（memo）、`lark-cli`（schedule） |
| `docs/research/t81-exec-smoke.md` | frozen | 1 处 | `test/calorie-routing-81.test.mjs:41` 真读盘，逐条 `exec ⟺ exit 0` 不变量 |
| `docs/research/t118-template-inventory.md` | frozen | 2 处 | `tooling/classify-templates.mjs --inventory` 缺省比对本件，行数／逐条不得压并 |
| `packages/base-combos/HELP.md` | frozen | 1 处 | `test/combos-p8.test.mjs:123/153` 断言块＝生成器输出逐字；改法须先改生成器 |
| `docs/p10-scaffold.md` | constrained | 2 处 | 六条 `dsh plugin add dsh-life-pack <包名>` 命令串逐字（单空格、不折行、不加引号） |
| `docs/visual-spec-help.md` | constrained | 1 处 | 20 条 `### H-NN ` 标题形态与编号 1..20、首 H-01 末 H-20 |
| `docs/visual-spec-blocks.md` | constrained | 1 处 | 12 条 `### B-NN ` 标题形态与编号 1..12、首 B-01 末 B-12 |
| `docs/calorie-dual-path-acceptance.md` | 探针标记 | 1 处 | 必含 `运行时读盘 loader` 与 `#107`；**不得**出现 `只被测试读取的死资产`／`HTML 一律以代码形式提供`（`docs/research/t95-rework-mutate.mjs:145-150`） |
| `docs/calorie-architecture.md` | 行号等量 | 软引用 | **总行数必须不变**（源码与测试写死 `:60`／`:54`） |
| `docs/research/benchmark-visual-spec.md` | 行号等量 | 软引用 | **总行数必须不变**（`visual-spec-help.md` 二十余处 `:行` 锚） |
| 其余 8 份（`docs/env.md`、`docs/public-installer-47.md`、`docs/skill-landing-r2.md`、`docs/research/t81-route-evidence.md`、`docs/research/t88-impl-governance-fix.md`、`docs/research/benchmark-visual-spec.md` 内容面、`docs/skills/skill-calorie/t293-验收-命令自治.md`、`CONTEXT.md`） | free | — | 无真读盘消费者（注释级提及不计） |

---

## 附录 A：引用树重跑口径

**扫描**：全仓 `*.md`，排除 `node_modules`／`.git`／`.scratch`／`.dsh-vision-router`／`dist`／`build`／`.tmp-*`／`.changeset`。

**三种抓法**：① 标准链接 `(…​.md)`（可带 `#锚`）；② 行内代码 `` `…​.md` ``；③ 裸路径 `docs|packages|tooling|scripts|fixtures|test|tools/….md` 与 `CONTEXT|AGENTS|SKILL|README.md`。

**归一化**：相对路径按「引用文件所在目录 ＋ 仓根」双候选解析，大小写不敏感；`.md` 结尾才算；`#`／`?` 截断。

**定层**：从 `AGENTS.md` 做 BFS，**同一文件只在首次发现的最浅层出现一次**；后续再被点到记为交叉引用（不减层）。父＝首次发现者。

**四视图**：主干树（层层展开、`[in/out]` 与交叉引用数）、分层总览（点卡片给上游链与下游集）、明细表（可按被引用数排序）、缺失与外部（303＋4）。

**本机草稿**（未入仓，本机 `.scratch/` 留存）：`md-refs-scan.mjs`（扫描）、`md-graph-enrich.mjs`（取标题＋定层）、`md-build-html.mjs`（建页）、`fix-freeze-summary.md`、`content-audit-rollup.json`（182 件审计结论）。

## 附录 B：审计面读数

| 批次 | 范围 | 件数 | 结果 |
| --- | --- | --- | --- |
| 引用树 | `AGENTS.md` BFS 可达 | 265 | 13 层；仓内 1066／缺失 303／外链 4 |
| 存在必要＋命名 | 全可达件（分 31 批） | 255 | keep 126／archive 129 |
| 内容质量（`writing-for-agents`） | L1–L12 保留件（分 5 波） | 见右 | 从落盘件可复算的**去重 177 件**：trim 62／keep-as-is 23／archive 82／restructure 10；L1 的 10 件在会话内返回、未落盘（trim 5／keep-as-is 5），不计入该 177。**口述过的「182 件／33・70・11・68」是按 182 反推的估计，不作为读数** |
| 冻结基线 | 拟改的 40 件 | 40 | frozen 8／constrained 3／free 8（余为未取回分档） |
| 修复 | 审计判 trim／restructure 的目标 | 37 | 4 次提交；每件经独立席复跑 |
