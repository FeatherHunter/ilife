# #575 替换证据（实施席）

## 一、分类（调查席清单缺失，按票面目标／边界现分类）

- `gh issue view 575` 无评论、无 `t575` 文档、无 `.scratch/t575`，调查席改类清单不存在。
- 按票面「以后还会跑的脚本统一换显式入口；历史证据与对账日志一律不改；AGENTS.md 描述行不碰」分类：
- **改类**＝`packages/` 下 git 跟踪、可复跑脚本 37 件（test 头注释 36＋生成器 1）。
- 其中 3 件开工即被他席在途改动占用（`git status` 为 M），本次一律不碰，见 §三。

## 二、改类 34 件逐行（只换命令头，参数与包名不变）

改法统一：`npx tsc` → `node node_modules/typescript/bin/tsc`，其余一字不动。

| # | 件 | 行 | 改前 | 改后 |
|---|---|---|---|---|
| 1 | `packages/skill-bill/test/user-wording.test.mjs` | 8 | `` `npx tsc -b packages/skill-bill --force` `` | `` `node node_modules/typescript/bin/tsc -b packages/skill-bill --force` `` |
| 2 | `packages/skill-bill/test/t469-guard-additions.test.mjs` | 4 | 同上 | 同上 |
| 3 | `packages/skill-bill/test/desktop-copy.test.mjs` | 3 | 同上 | 同上 |
| 4 | `packages/skill-bill/test/shared-judgments.test.mjs` | 11 | 同上 | 同上 |
| 5 | `packages/base-render/test/help-shell-136.test.mjs` | 4 | `` `npx tsc -b packages/base-render` `` | `` `node node_modules/typescript/bin/tsc -b packages/base-render` `` |
| 6 | `packages/base-render/scripts/gen-help-shell.cjs` | 213 | `T.push(' * 运行：先 \`npx tsc -b packages/base-render\`，');` | `T.push(' * 运行：先 \`node node_modules/typescript/bin/tsc -b packages/base-render\`，');` |
| 7 | `packages/skill-calorie/test/analysis-shape-520.test.mjs` | 11 | `先 pnpm build（或 npx tsc -b），再` | `先 pnpm build（或 node node_modules/typescript/bin/tsc -b），再` |
| 8 | `packages/skill-calorie/test/diet-list-t271.test.mjs` | 11 | `` `npx tsc -b packages/base-render packages/skill-calorie` `` | `` `node node_modules/typescript/bin/tsc -b packages/base-render packages/skill-calorie` `` |
| 9 | `packages/skill-calorie/test/goal-weight-deadline-548.test.mjs` | 19 | `` `npx tsc -b packages/skill-calorie` `` | `` `node node_modules/typescript/bin/tsc -b packages/skill-calorie` `` |
| 10 | `packages/skill-calorie/test/goal-receipt-253.test.mjs` | 20 | 同上（含「用例吃 dist」后缀不动） | 同上 |
| 11 | `packages/skill-calorie/test/help-delivery-139.test.mjs` | 16 | 同上 | 同上 |
| 12 | `packages/skill-calorie/test/help-shell-134.test.mjs` | 11 | 同上 | 同上 |
| 13 | `packages/skill-calorie/test/profile-guard-175.test.mjs` | 12 | `先 npx tsc -b，再 node --test …` | `先 node node_modules/typescript/bin/tsc -b，再 node --test …` |
| 14 | `packages/skill-calorie/test/t270-回执四块.test.mjs` | 13 | 同 9 | 同 9 |
| 15 | `packages/skill-calorie/test/t271-总览接线与两态.test.mjs` | 17 | 同 9 | 同 9 |
| 16 | `packages/skill-calorie/test/t272-空窗两态.test.mjs` | 17 | 同 9 | 同 9 |
| 17 | `packages/skill-calorie/test/t274-食品库页.test.mjs` | 24 | 同 8（含多席并发注不动） | 同 8 |
| 18 | `packages/skill-calorie/test/t277-预检确认页.test.mjs` | 14 | 同 9 | 同 9 |
| 19 | `packages/skill-calorie/test/t359-seven-point.test.mjs` | 10 | `` `npx tsc -b` ``（包内编译） | `` `node node_modules/typescript/bin/tsc -b` `` |
| 20 | `packages/skill-calorie/test/t360-measure-trend.test.mjs` | 17 | 同 9（含不走 build 注不动） | 同 9 |
| 21 | `packages/skill-calorie/test/t361-measure-full-table.test.mjs` | 28 | 同 9 | 同 9 |
| 22 | `packages/skill-calorie/test/t362-窗口不截断.test.mjs` | 18 | 同 9 | 同 9 |
| 23 | `packages/skill-calorie/test/t363-补记查冲突.test.mjs` | 19 | 同 9 | 同 9 |
| 24 | `packages/skill-calorie/test/t364-删除取快照.test.mjs` | 28 | 同 9 | 同 9 |
| 25 | `packages/skill-calorie/test/t365-七条写词整页.test.mjs` | 28 | 同 9 | 同 9 |
| 26 | `packages/skill-calorie/test/t366-复制执行闭环.test.mjs` | 24 | 同 9 | 同 9 |
| 27 | `packages/skill-calorie/test/t398-source-group.test.mjs` | 16 | 同 9 | 同 9 |
| 28 | `packages/skill-calorie/test/t440-部位名单一来源.test.mjs` | 18 | 同 9 | 同 9 |
| 29 | `packages/skill-calorie/test/t444-对比来源校验.test.mjs` | 16 | 同 9 | 同 9 |
| 30 | `packages/skill-calorie/test/t456-围度键集对账.test.mjs` | 13 | 同 9 | 同 9 |
| 31 | `packages/skill-calorie/test/t464-备注列口径.test.mjs` | 20 | 同 9 | 同 9 |
| 32 | `packages/skill-calorie/test/t496-文案统一.test.mjs` | 18 | 同 9 | 同 9 |
| 33 | `packages/skill-calorie/test/t509-清尾.test.mjs` | 18 | 同 9 | 同 9 |
| 34 | `packages/skill-calorie/test/t511-清尾二.test.mjs` | 16 | 同 9 | 同 9 |

- `.ps1`：跟踪态 `packages/` 下无含 `npx tsc` 的 `.ps1`（仅 `docs/…/t269-复核-脚本/windowA-B.ps1` 含，属历史复核脚本，见 §三），故无 `.ps1` 改动。

## 三、不改清单与理由

| 类 | 例 | 理由 |
|---|---|---|
| 历史证据 `.md`／对账 `.log`（约 370 处） | `docs/skills/skill-calorie/t480-文本审查-history.md` 等 | 票面红线：历史证据日志一字不动 |
| `AGENTS.md` 描述行 | `AGENTS.md:41` | 票面边界明示不碰 |
| `docs/` 票据级 `.mjs` 跑脚本 12 件 | `t269-mutation-battery.mjs`、`t271-接线-run.mjs`、`t273/274/275/277-真跑.mjs` 等 | 一次性票据脚本，非以后复跑口；动即改写历史取证链 |
| `docs/` 复核 `.ps1` 2 件 | `t269-复核-脚本/windowA.ps1`、`windowB.ps1` | 同上 |
| 他席在途 3 件 | `t272-排行榜页.test.mjs:16`、`t273-review-meal.test.mjs:17`、`t275-营养饮水总览页.test.mjs:35` | 开工即 M，`git add` 会连带暂存他人 hunk；留给下一手 |
| `.scratch/` 全树 | — | 草稿区，不属改类；且禁删他人目录 |

## 四、机器验证

- 改类 `grep npx tsc`：`packages/` 仅剩 §三 3 件在途＝预期残留；已改 34 件零残留（`git diff` `+` 行无 `npx`）。
- 解析：`node --check` 34/34 通过（含 `.cjs`）。
- 持锁编译：`GATE-RUN runId=ee8a76ab-bcca-4f1d-8e49-8678f7f8c3bf cmd="node node_modules/typescript/bin/tsc -b packages/base-render" exit=0`。
- 靶向用例：`GATE-RUN runId=e4f171e6-e295-482d-87c1-a4d0c2093420 cmd="node --test packages/base-render/test/help-shell-136.test.mjs" exit=0 tests 5/pass 5/fail 0`。
- 变异自证（`packages/base-render/src/helpShell.ts:48` `help-data`→`help-datX` 一字）：
  - 红：`GATE-RUN runId=1b0eb940-aee7-41ad-b2c9-948447d0fdcf exit=1 tests 5/pass 4/fail 1`（① 断言 `datX` 差）。
  - 还原：sha256 `4BB3EAAC…15CB8` 与改前逐字相同，`git diff` 该件为空；`--force` 重建后 `GATE-RUN runId=fefbce06-6b61-4534-ba75-140c5f16b7a6 exit=0 tests 5/pass 5/fail 0`。
- 行数纪律：改动全是注释行同行替换，零行数变化；`test/*.mjs` 本就不在两包告警线扫描面内。
