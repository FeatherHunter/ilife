# 删掉位置覆盖变量读取点 —— 证据（票 #754）

> 上游：#746（归零口径）→ #753（读数）→ #756（裁决：家目录通道）→ #763（已关：隔离落地，守卫换判据）→ 本票（删读取点）。
> 作业计划：`.scratch/t754-plan.md`；开工前订正（#763 落地实况）住 #754 的评论（2026-09-21）。
> 对抗审查：只读审查席（`c413b49a`）结论 `RETAIN-LIST-RISK`——保留项三处缺口已并入执行（见 §7）。

## 1. 删了什么（行为面）

- `packages/base-link-core/src/config/dirs.ts`：删 `CONFIG_DIR_ENV` 常量与 `resolveConfigDir()` 里的覆盖分支；只剩 `join(homedir(), '.ilife')` ＋ 生产守卫（跑在测试运行器里却要落到真实家目录即抛 `CONFIG_TEST_ISOLATION_MISSING`，判据取自账号，不读我们定义的变量）。
- `tooling/skilllink.mjs`：`configDirPath()` 只回 `join(homedir(), '.ilife')`；删 `configDirWhere()` 与调用点；doctor 报文去掉位置覆盖岔路。
- `tooling/check-publish.mjs`：生成探针改家目录注入（`USERPROFILE`／`HOME` 指一次性目录）；进程内赋值同改；注释同步。
- `CONFIG_TEST_ISOLATION_MISSING` 那道门**不删**（订正：#763 已换判据留下；它是“忘了隔离 ⇒ 零写 ＋ 响亮”唯一与入口无关的落点）。

## 2. 零命中（读取点清零）

- `packages/*/src` ＋ `tooling`（除下述两处来历注释）：`ILIFE_CONFIG_DIR` **零命中**（代码 token 全清：无 `process.env` 读取、无 env 赋值、无 `delete`、无 `CONFIG_DIR_ENV`）。
- `docs/**` 可跑探针（`.mjs`）：**零命中**（18 件同批换家目录注入；另修三处写盘路径遗漏：t691 两处、t726、t729-EMPTY、t714、t718、t716-regen 检查路、memo 研究探针；注释四处同步）。
- 剩余命中**只剩注释与历史文档**（见 §3 点名）。`packages/*/test` 与 `test/` 无代码 token（`delete env.ILIFE_CONFIG_DIR` 八处已删：六家插件 `config-surface`＋备忘／作息 `config-695`＋卡路里 helper；`t749` 留的是“缺席断言” enforcement test，必须留）。

## 3. 保留项（逐条点名 ＋ 理由）

- `.changeset/763-test-home-injection.md`、`t694-config.md`、`t722-…md`：冻结变更史，回改即篡改。
- 历史证据件（`t694-证据`、`t762-证据`、`t742`、`744-证据`、`t676`、`t734`、`t736`、`t696`、`t706`、`t727`、`t738`、`t671`、`t718`、`t722`、`t695-*`、`t726`、`t731-差异表`、`t702`、`t708`、`t716-搬家`、`map-*-body`、`new-memo-current-state`、`real-products-probe.md`（说明面那份）、调查件《测试缝与0环境变量》全件、research 两 HTML）：旧通道实测记录，“为什么换判据”的线索，不回改。
- `dirs.ts` 里 #763 注释（原判据挂位置覆盖变量）：保留——后来人查守卫来历的唯一线索。
- `test/helpers/home-test-base.mjs:4`、`real-home-snapshot.mjs:4` 件头：保留（已改“要删”→“已删（#754）”）；`real-home-snapshot.mjs:8` 已按审查意见改写（守卫换判据 ＋ 变量已删）。
- `tooling/check-publish.mjs:220`、`tooling/check-real-home-untouched.mjs:6`：来历注释（旧布景已退役），删反而丢信息——审查指出的清单缺口，本次补入。
- 六家 skill helper 迁移件头、`bill/cli.test.mjs:130`（已改“#763 退役”→“#754 删除”）、`calorie/publish-tarball-smoke.test.mjs:69`（述 registry 旧护栏，历史事实）、`calorie/helper:120`（已去掉退役变量半句）、`bill/t749`（缺席断言）、`t731-差异表:165`（历史命令示例）：来历说明，留。
- 本票转交行（见 §7，混窗未提交）：行文本仍含变量名，属“待 carriers 落地即清”的注释，非读取点。

## 4. 测试读数（排队，均经 `run-locked --ticket 754`）

- 编译：`tsc -b packages/base-link-core` exit 0（`runId=e4e1092c`）；`tsc -b` 五家（bill／chef／home／memo-ilife／schedule）exit 0（`runId=c320174c`）；卡路里除外（见 §6）。
- 指纹：`t540-指纹绑定 --scope packages/base-link-core` → `src-sha=4f4e3f0f682e381a dist-sha=a395347d7d34b408` 一致，`tsc-exit=0`（`runId=self:65eababe`，自测窗口非门禁证据，仅供认口）。
- 守卫与隔离：`node --test test/config-694.test.mjs packages/plugin-bill-ilife/test/config-surface-677.test.mjs` → `tests 58 / pass 58 / fail 0`（`runId=b4289ba0`）。A1（配置目录＝当刻家目录下 `.ilife`）、A4（要落真实家目录 → 响亮失败零写盘）即新判据。
- 记账收窄与出口：`node --test packages/skill-bill/test/t749-设置页收窄.test.mjs packages/skill-bill/test/cli.test.mjs` → `tests 17 / pass 17 / fail 0`（`runId=1874044e`）。其中 t749-⑤“体检不再提变量”绿（本票 health 文案 fixes 的端到端印证）。

## 5. 反向读数（不设任何我们定义的变量）

- `node packages/skill-bill/dist/cli/cmd_read.js bill.config.read`（继承 OS 环境，不设任何技能变量）→ `path=C:\Users\辰辰洋洋\.ilife\bill.yaml`，`dataDir=…\.ilife\data`，`created:false`（既有真实配置在位，零写盘）。落在 `os.homedir()/.ilife`，与口径一致。

## 6. 卡路里编译破窗（范围外发现，S3 ＋ 转票措辞）

- `tsc -b` 六家联编时卡路里红：`landRunner.ts` 读 `land.scheduleCli`／`land.memoCli`，而 `CALORIE_CONFIG_DEFAULTS.land` 已无此二键（#757 在途：删键 ＋ `writableDefaults`，工作区未提交）。**非本票引入**（本票对该包只动注释 6 行；破窗来自 #757 行为半边）。本票验证跳过卡路里编译，其余五家绿。待 #757 落地后复跑。

## 7. 混窗转交（本票已改、未提交，待 carriers 落地）

工作区同一窗口有 #757／#796／#760／#764／#794 五家实施席在途（行为半边：`writableDefaults` ＋ 退休键 ＋ 记忆位改绑）。下列件含两家 hunks，本票**不提交**（整件提交会裹走别席行为半边，违一票一作者），行文本转交 carriers 落地时同收（均为注释／文案，无行为）：

- 卡路里（carrier #757）：`src/config.ts`（头注释 1 行 ＋ 缓存注释 1 行）、`src/health.ts`（注释 1 行 ＋ 文案 1 行）、`src/paths.ts`（注释 2 行 ＋ 守 essay 文案 1 行）。
- 大厨（carrier #796）：`src/config.ts`（头 1 行）、`src/fetch/paths.ts`（守卫 1 行 ＋ 库目录 1 行）、`src/health.ts`（注释 1 行 ＋ 文案 1 行）、`plugin-chef/src/settings.ts`（1 行）。
- 备忘（carrier #760）：`src/config.ts`（头 1 行）、`src/fetch/paths.ts`（守卫 2 行 ＋ 库目录 1 行）。
- 作息（carrier #764）：`src/config.ts`（头 1 行）、`src/fetch/paths.ts`（守卫 1 行 ＋ 库目录 1 行）、`src/health.ts`（注释 1 行 ＋ 文案 1 行）、`plugin-schedule-ilife/src/settings.ts`（1 行）。
- 居家（carrier #794）：`src/fetch/paths.ts`（守卫 1 行 ＋ 库目录 1 行）。
- `docs/agents/t763-家目录注入-证据.md`（§7.1 未提交半边属 #763 收尾）：本票不碰；本件即 #754 证据（计划“追加一节”因混窗改为单立本件，归属更清）。

## 8. 结构纪律（必报五步）

- 第一步影响清单：能力目录 `base-link-core/src/config`（删读取分支与常量）1 件；其余全在不管清单（测试／探针／文档／tooling 门禁链）。用户已 blanket 授权（“细节你定”），事前清单即 `.scratch/t754-plan.md` ＋ #754 订正评论。
- 第二步结构设计：零新增目录、零新增对外名（删 `CONFIG_DIR_ENV` 转出；`resolveConfigDir` 签名不变，判据换 inward 私有函数不出门）。
- 第三步写代码：常量与类型一处（删即是减）；跨能力引用走公开接口（技能侧调 `configPaths` 不变）。
- 第四步超线报警：本票 src 改动全是删行与同行改写字面，不增行；`health.ts` 五件本就超线在册（台账挂号，拆法待收口票），本票不动行数，不触发新报警。
- 第五步交付对账：见本节 §7 转交表——已提交件与计划清单逐行对上；混窗 11 件偏差已点名（carrier ＋ 行号），偏差≠遗漏，待 carriers 落地即零。
