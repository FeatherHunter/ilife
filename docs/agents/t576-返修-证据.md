# #576 返修证据（实施席）

## 一、步骤0量现场

- `git status --porcelain` 起始未见以下四件（`-- <四件>` 查无输出，exit 0），判定无在途未提交改动，整单未 BLOCKED：
  - `packages/skill-calorie/test/t368-索引卡片.test.mjs`
  - `packages/skill-calorie/test/t279-真出口用例.test.mjs`
  - `packages/skill-calorie/test/calorie-c43.test.mjs`
  - `docs/skills/skill-calorie/t575-替换-证据.md`
- 当时工作区另有他席在途（exercise 四件 M＋零散 ??），一律未碰。

## 二、三件事（写集内）

1. D1 `t368-索引卡片.test.mjs:12`：`node_modules/.bin/tsc` → `node node_modules/typescript/bin/tsc`，尾参 `-b packages/skill-calorie` 不动。
2. D2 `t279-真出口用例.test.mjs:21`：`node D:\ilife\node_modules\typescript\bin\tsc` → `node node_modules/typescript/bin/tsc`，尾参 `-b packages/base-render packages/skill-calorie --force` 不动。
3. `calorie-c43.test.mjs:71`：`WAKE_TABLE.length` 3→6，注释写明来源 commit `c6ff7e2`（#291 加三行目标别名：看目标推荐／看目标配置／看目标状态；饮食三行形状不变）。
4. `git mv docs/skills/skill-calorie/t575-替换-证据.md docs/agents/t575-替换-证据.md`，内容一字不动（`git diff --cached --stat` 该 rename 为 `0` 行变更）。

旧串残留 0／新串各 1（`Select-String` 计数）：t368 旧 0、新 1；t279 旧 0、新 1。

## 三、持锁重编＋四件全绿（GATE-RUN）

- 重编 `node node_modules/typescript/bin/tsc -b packages/skill-calorie`：runId `ac364be2-53a7-42bb-82b6-f52474c5ded8` exit 0。
- c43：runId `e884c12c-1f1f-4a2f-9cf5-b19bdbfecd06` exit 0，tests 9 pass 9 fail 0。
- t291：runId `f294b512-79e9-4b2d-a5c0-b4f50c7ca194` exit 0，tests 6 pass 6 fail 0（① WAKE_TABLE 6 行；④ SoT 436＋饮食回归）。
- t368：runId `49ede6ba-f893-4dd9-9aa5-a557bb8cc57a` exit 0，tests 12 pass 12 fail 0（`RESULT: 11/11`；② styleBytes=56197 external=0 pathOk=1；③ 13/13）。
- t279：runId `86abfda4-296f-45d5-9d37-76b20956deaa` exit 0，tests 99 pass 99 fail 0（①83 条字节 69175–168774；③空窗例外仍仅“看营养分析” exit 4）。
- git mv：runId `a5cfd299-36ff-41b1-8422-c4cb3c4ab63f` exit 0。

## 四、88／106 只记账不代改

- 88：runId `a919f389-b4ae-4418-a864-ac7c4a727058` exit 1，tests 25 pass 24 fail 1；唯一红 `A2 148 !== 130`（base-render 新增契约面）。
- 106：runId `373f0d86-bd10-45df-bd43-9998b6c89fef` exit 1，tests 10 pass 8 fail 2；红 `392 !== 384`（唯一 CLI 条数）＋ `148 !== 130`（同上冻结面）。
- 本票 diff 未碰 `TRIGGERS`／`Scene`／base-render（仅三件测试注释＋一行断言＋一次 git mv），当刻 `WAKE_TABLE=6 TRIGGERS=436` 与 t291 锁一致；两处红与并行席新增有关，按票面只记账。

## 五、变异自证（持锁期内还原干净）

- c43 功能变异：暂改 `WAKE_TABLE.length` 6→3 → runId `c4a33695-dc1c-4e17-95ed-7a3a33b2cb76` exit 1，tests 9 pass 8 fail 1（`6 !== 3`，C2 红）；逐字节改回 6 → runId `259d4e80-154e-49f7-a013-5da62de69235` exit 0，tests 9 pass 9 fail 0。红绿两行一致，还原后 `git diff` 即 §二形状。
- D1／D2：改动为头注运行指令注释，不进执行流；自证口径为新旧串计数（旧各 0、新各 1）＋ §三四件绿；编译入口新形已由持锁重编验证（exit 0）。

## 六、提交说明

- 他席暂存 `docs/skills/skill-calorie/t576-证据.md`（staged）在本席开工后出现，非本席写集；提交用 `git add <声明路径>`＋`git commit -m … -- <路径>` 点名本席五件，排除它，一律不 `reset`／`restore`／`add -A`。
- 本件即证据件 `docs/agents/t576-返修-证据.md`。
