# #742 · 技能侧说明面收口 · 证据

票：[技能侧说明面收口：唯一出口口径单一来源＋门＋六家统一 0.3.0（#734 的技能侧那半）](https://github.com/FeatherHunter/ilife/issues/742)

**一句话**：六份 `SKILL.md` 的「唯一出口怎么跑」不再是六人各写一段——**正文只住 `docs/agents/技能调用契约.md` §七一处**，由 `tooling/skill-call-form.mjs` 按各包 `bin` 声明渲染进六份的标记块，并配一条门（挂 `pnpm gen:check`）；卡路里／居家那两套「钉死版号的 npm 装法 ＋ 仓内相对路径」整节清掉。

## 一、交付物

| 件 | 干什么 |
|---|---|
| `docs/agents/技能调用契约.md` | §一.2 指向唯一来源；§二之二 补第 4 级（npm 兜底）与 **Node 边界**；§五 补门；**§七＝口径正文的唯一来源区**（`CALL-FORM` 标记之间） |
| `tooling/skill-call-form.mjs` | 生成器 ＋ 门：无参＝写；`--check`＝同源＋块外零旧写法；`--selftest`＝变异自证 |
| `packages/skill-*/SKILL.md`（6） | 各加 `<!-- CALL-FORM-START/END -->` 标记块（位置：卡路里／居家＝原「公共安装器运行时」处；其余四家＝「快速开始」之后）；卡路里第 28 行的仓内相对路径改成按声明说 |
| `package.json`（根） | `gen`／`gen:check` 各接上 `node tooling/skill-call-form.mjs [--check]` |
| `test/skills-export-47.test.mjs` | 旧断言（`## 公共安装器运行时` ＋ `NPM_PIN=@0.2.4`）换成「CALL-FORM 块在 ＋ 块里含本包命令名／入口／技能基目录 ＋ 不许出现 `npm install -g` ＋ 口径里不许钉版本号」 |
| `docs/public-installer-47.md` | 「版本钉死登记」一节退役（历史登记保留不回改）；说明「装源目录无编译产物」正是第 3 级兜底存在的原因 |
| 六技能 `package.json`／六插件 `package.json` | 技能统一 `0.3.0`；单品插件 `0.3.1` ＋ 精确 pin 技能 `0.3.0`；`dsh-life-pack: ^0.3.2` 不动 |
| 12 份 `AGENTS.md`／12 份旧单包向导 | 版本串同步（技能 `0.3.0`／插件 `0.3.1`），旧串回读清零 |

## 二、口径正文（唯一来源，逐字）

```
## 唯一出口：怎么跑

入口＝本技能包 `package.json` 里 `bin` 声明的那条：`{entry}`。
`<技能基目录>`＝加载本技能时给出的 `Base directory for this skill: <路径>` 那一行。

1. 命令名解析得到时：`{cmd} <key> [--params '<json>']`。
2. 解析不到时（`not recognized`／`command not found`）＝ PATH 上没有这条命令，下面这行照样跑得起来：
   `node <技能基目录>/{entry} <key> [--params '<json>']`
3. 本目录里没有编译产物时：`npx -p {pkg} {cmd} <key> [--params '<json>']`（上面第 2 行就够，不必再取一份）。

换走法的信号只有一个：命令名解析不到。其余报错照 stderr 的报文原样交给用户。
```

渲染值现算：`{cmd}`＝`bin` 里以 `-cmd-read` 收尾的键名；`{entry}`＝它的值去掉 `./`（六家今天都是 `dist/cli/cmd_read.js`）；`{pkg}`＝包名。

## 三、机器读数（全部经 `run-locked --ticket 742`）

| 判据 | 读数 |
|---|---|
| 生成器写 | `WROTE`×6 ＋ `PASS: 已把唯一来源写进 6 份 SKILL.md` |
| 门 `--check` | `RESULT: 6/6` ＋ `PASS: 六份说明面与唯一来源逐字同源，块外无旧写法` |
| 变异自证 `--selftest` | `PASS 写 → 应绿／写完 --check → 应绿／拿掉一块 → 应红／写回 → 应绿／块外注入旧写法 → 应红`，5/5 |
| 根 `gen:check` 链 | 卡路里 `GEN-CHECK PASS`（键 134）＋ 记账 `GEN-CHECK PASS`（命令 14）＋ 新门 `6/6` |
| `test/skills-export-47.test.mjs` | tests 5／pass 5／fail 0 |
| `check-publish --pre`（12 包） | PASS（`精确 pin skill-* 0.3.0`、`dsh-life-pack ^0.3.2 同版本线` 逐条 OK） |
| `check-publish --tarball`（12 包） | PASS（六个技能 tarball 均含 `dist/cli/cmd_read.js`，六个插件均含 `dist/index.js`＋`cordis.patch.yml`） |
| 卡路里说明面示例门 `check-examples` | 本次改动版与 **HEAD 版各跑一次：同为 `99/134`（红 35）**，红全是种子库数据类（如 `ERR 4: 无训练计划`），与本次改动无关。⚠️ 记录一处：首跑读到 `78/134`，同文件再跑回 `99/134` ⇒ 该门对种子库状态敏感，读数以对照两次同值为准 |
| 块外旧写法 | 六份 `SKILL.md` 块外零命中 `packages/skill-`／`npm install -g`／`npx -p`／`~/.agents/skills`（门判据③） |

## 四、定版与发版顺序

- 六技能 `0.3.0` → 六单品 pin 精确 `0.3.0` → 单品插件 `0.3.1`；总管 `dsh-life-pack@0.3.2` 不动（零单品依赖）。
- 发布顺序：**先六个技能，后六个单品插件**（插件精确 pin 技能版本，反了会装到旧技能）。统一入口 `tooling/wizard-publish.ps1`（AI 在侧边栏终端驱动、送回车、转授权链接；人只批准 2FA）。

## 五、本票没做（留给发布窗与后续票）

1. **发版后两条真机判据**（本票验收判据 4／5）：装机副本真跑 `node <技能基目录>/dist/cli/cmd_read.js <key>`（六家各一次，`exit 0` ＋ envelope 含绝对路径）、会话里抽验一句「卡路里 HELP」（回复无安装建议、无仓内路径）——要等 registry 上有 `0.3.0` 之后才能做。
2. **插件桥那处 `dist/cli/cmd_read.js` 字面路径**：入口已按 `bin` 声明解析，桥那处属 #734 登记的待收件，不在本票写集。
3. **旧单包向导未退役**：本票只把它们的版本变量同步成真（`V_SKILL`／`V_PLUGIN`），统一发布入口仍是 `tooling/wizard-publish.ps1`；要不要删那 12 份脚本另开票。
4. 一次性定版脚本住 `.scratch/t742-bump*.mjs`（不进仓）。
