# 整图验收方案 · 对抗式审查（第 1 席）——`t-help-acceptance-plan.md` 逐条攻击

- 归属：地图 **#63**《卡路里·本体图（1/3）parity 极致重做》→ `## Not yet specified` 最后一条「整图验收」。
- 被审对象：commit **`4d33dfe`**（未 push）＝ `docs/research/t-help-acceptance-plan.md`（503 行）＋ `docs/research/t-help-acceptance-collect.mjs`（134 行）。
- 本席身份：对抗式审查席（**不是**复核席）。价值＝**攻击**，不是复述。
- 纪律：本席零 `packages/**`／`tooling/**`／既有 `docs/**` 改动；只新增本文件；判据复跑一律经 `node tooling/run-locked.mjs --ticket 63 -- <cmd>`；`git commit --only`，**不 push**。
- 证据：本席自跑的每一条都带 `GATE-RUN runId=…`（§11）；被审文档的数字凡本席复跑过的一律给「实测值」。

---

## 0. Verdict

> **FAIL（方案方向正确，交付不合格）**
> 五维：契约一致 **17/30**｜证据真实可复现 **13/25**｜parity **13/20**｜工程红线 **9/15**｜文档同步 **6/10** ＝ **58/100**。

三条**必须**先修的硬伤（任意一条不修，方案 B 落地即错）：

1. **终局判据 A1 的期望输出是算术假等式**：方案写 `exec 341 ＝ 56＋22＋26＋222`，**56+22+26+222 = 326 ≠ 341**（本席复跑原文见 §3-5）。差 15 条正是「家族未派生但路由层为 exec」的 15 个词。照抄这条判据，A1 **永远绿不了**。
2. **「已覆盖」格引用了一个不存在的脚本**：§4.6 的 A 级已覆盖证据写 `#75 buildStyleSheet（t75-style-evidence.mjs）`——**该文件不存在**（`docs/research/t75-style-evidence.mjs` 无；同名只有 `.md`），而 `t105-check-rulers.mjs --strict` 自己把 **H-02／H-05／H-12／H-16 的 computed 面判为 `BLOCKED`**、H-10 圆角 token 亦 `BLOCKED`。这一格**不是「已覆盖」**。
3. **阻断项归给了一张已关闭的票**：缺口 §5-4「运行时入口」归属 **#64**，而 #64 已于 **2026-09-09T14:32:58Z（COMPLETED）关闭**，早于本方案提交（15:44Z）**72 分钟**；本席实测安装副本的 `description` **仍无「卡路里HELP」**。`RESULT-63` 的条件是「缺口台账无『阻断』项」——该阻断项目前**没有未关闭的归属**。

---

## 1. 被审范围与逐条主张的攻击结论

| # | 被审主张 | 结论 | 一句话理由 |
|---|---|---|---|
| 1 | 推荐方案 B（受跟踪编排脚本＋受跟踪报告＋`.scratch` 原始产物；不重跑 #89b 而是消费其产物） | **部分成立** | 形态正确；但「不重跑浏览器」是**设计选择而非成本所迫**（两套件复跑 11.7s 全绿，§6），且消费接口**未定**（§5） |
| 2 | 覆盖矩阵 36 格：已覆盖 22／部分 8／无覆盖 4 | **数字不成立** | 逐行点票为 **已覆盖 19／部分 8／无覆盖 5 ＋ 已裁定 4**；小计表与行标签互不一致（§4、S1-3） |
| 3 | 三条主线终局判据 A1–A4／B1–B8／C1–C6 的计数与现状（①1/4、②4/8、③4/6） | **部分成立** | ①1/4 与 ③4/6 复跑相符；②4/8 **低估**（B4 一条 0.27s 的命令即绿，实测 `RESULT: 44/44 fails=0`） |
| 4 | 缺口 16 条分类（阻断 7／可接受／可登记）＋「静态层已绿 `RESULT: 27/27`」 | **静态层成立；分类有两处误判** | 27/27 复跑相符（runId `0966ecb1`）；但 #64 归属失效、白名单掩盖真红的**处置方向**对而**执行主体**越权（§9） |
| 5 | 需维护者拍板 3 点（判定口径／证据形态／豁免＋重基线） | **成立但缺一件** | 缺「**外部证据接口**」拍板点：#89b 的产物路径/格式由本方案单方发明（§5） |

---

## 2. 矩阵抽查（10 格，含 4 格「已覆盖」、3 格「部分」、3 格「无覆盖」）

约定：**本席自己复跑**声称的判据命令，不采信被审文档的自述。

| # | 矩阵格（§） | 声称 | 本席复跑／核验 | 判定 |
|---|---|---|---|---|
| 1 | 4.1 SoT 全量 436 | 已覆盖 | `cmd-read-t11.test.mjs:196` = `assert.ok(TRIGGERS.length >= 436)`；CSV 436 数据行（collect A1 PASS，runId `0966ecb1`） | **相符**（注意是 `>=` 非 `===`，弱断言） |
| 2 | 4.4 注册表 99 | 已覆盖 | 5 处断言逐行核对：`cmd-read-t11:79-80`（99）、`cmd-write-40:72`（99）、`render-t41:70-71`（99）、`skill-t11:52`（99）、`db-readonly-93:287`（64）；collect C4/C5/C6 PASS | **相符** |
| 3 | 4.2 需移植 18 | 已覆盖 | `exercise-port-111`／`nutrition-port-112`／`trend-misc-port-113` 三个受跟踪测试在库，测试名逐条对应 6＋4＋8＝18 | **相符** |
| 4 | 4.5 10／54／436 | 已覆盖 | `help-center-88.test.mjs:44-48`（10／54／436）与 `:196-200`（436 卡／54 子功能／1308 按钮）逐字核对；本席实跑 45/45（runId `9ab5d208`） | **相符** |
| 5 | 4.1 逐条入桶 436 | 部分 | 实跑 `t81-route-evidence.mjs` **exit=1，19 项核对失败**（runId `9b6c4cac`／`d7a09bac`，两次逐字一致） | **相符**（但红的构成与方案自述不符，S2-6） |
| 6 | 4.1 命中即渲染 436 | 部分（抽样 5 词） | 实跑 `t83-evidence.mjs` `RESULT: 21/21 PASS`（runId `6846984e`），`⑤-81 联动` 逐词列出**恰 5 个词** | **相符** |
| 7 | 4.6 静态可判（A 级）H-01…H-11、H-13…H-15、H-17、H-18 | **已覆盖** | 实跑 `t105-check-rulers.mjs --strict` exit=0：可判 43（PASS 25／OLD-DEVIATION 18／DEFECT 0），但末行点名 `H-02/H-05/H-12/H-16 「渲染后 computed 值」类判据 … BLOCKED`，另有 H-10 圆角 token `BLOCKED`；所引 `t75-style-evidence.mjs` **不存在** | **不符**（S1-2） |
| 8 | 4.7 B-02／B-03／B-05／B-06／B-08 | 无覆盖 | `visual-spec-blocks.md:244` 逐字 = `BLOCKED（需 #75／#104 产出后判）`；尺子输出同口径 | **相符** |
| 9 | 4.2 旧模板本体 73 逐字 | 无覆盖 | 仓内无旧 73 模板（`git ls-files` 只有 `fixtures/help-instances` 2 件旧 HTML＋新版 `packages/skill-calorie/templates/*.html`）；旧树 `D:\2Study\StudyNotes\SKILLS\卡路里` 确实存在但**不在仓** | **结论相符，引用路径错**（S3-1：`fixtures/README.md:31-52` 不存在，实为 `fixtures/help-instances/README.md:31-52`） |
| 10 | 4.3 helper／部分映射 2 | 无覆盖 | `git grep render_goal_common` 仅命中台账与本文；`render_review.py` 除台账/路由证据文本外**无任何测试引用** | **相符**（注：`render_review.py` 的**已映射子集** `calorie.view.diet-review` 另有覆盖，与「无覆盖」并置需注明口径） |

**抽查结果：10 格中 1 格分类不符（第 7 格，S1-2），另 2 格引用对象不存在/路径错（第 7、9 格）。**

---

## 3. 可证伪性逐条（A／B／C）

判据分三类伪造路径：**① 自数自**（判据数自己的断言）；**② 比 sha 不知内容**；**③ 抽样冒充全量**。

### 3.1 主线①

| 判据 | 可伪造点 | 加固建议 |
|---|---|---|
| **A1 逐条入桶** | ① 「19 项红全属判据漂移」是**人判**，机器只报红不报因；冻结期望表 `expectRoutes` 的**族派生规则就在同一脚本内**（`t81-route-evidence.mjs:270-286`），改派生即绿。③ 漂移明细只打印前 5 条（`:315` 的 `drift.slice(0,5)`），**30 处漂移不可见** | 期望桶位改钉在 E1/E2 上（CSV 增一列 bucket，或独立 fixture），脚本只读不派生；`drift` 全量落盘 |
| **A2 exec 实跑** | ② 快照 `.md` 由 `t81-exec-smoke.mjs` 自己生成，测试再读它——**自数自**。但该测试**另有独立复核**：`test/calorie-routing-81.test.mjs:304-311` 逐行重算 `c[6]==='0'` 与 cli 逐字，比汇总行强（方案未提这点） | 汇总行必须由逐行重算得出（现状已满足，写进判据）；`复制昨日运动` 的日期依赖必须消除，否则判据随墙钟变色 |
| **A3 命中即渲染** | ③ **5/436 抽样**冒充「命中即渲染」全覆盖；样本选择无种子、无分层 | 至少分层抽样＋固定种子；或断言「全 436 词逐词 file 态」（成本已测：t83 3.0s，全量另计） |
| **A4 门面 69 词** | ① 判据＝脚本自己的 `RESULT: 69/69`；期望列表在脚本内 | 从 `SKILL.md` 正文独立解析触发词再比 |
| **静态层 A10** | ① **只在整文件里搜「卡路里HELP」**（`collect.mjs:77` `read(SKILL.md).includes(...)`），不校验 `description` 行 → description 被截断/改写后**仍绿**；而 `description` 余量只有 4 字符（缺口 §5-16） | 解析 `description` 行并断言包含唤醒词；与 B1 的长度断言合并（S2-1） |
| **静态层「交叉来源」** | ② 方案 §3.1 称「同一数字同时钉在 `routing.ts` 实测值与 `help-center-106.test.mjs:66-67` 的断言文本上」——**代码里没有读该测试文件**，只出现在 `src` 说明字符串里（`collect.mjs:71-72`）。若某票把测试里的 341 改成 342，采集器**照样 PASS 并继续引用 `:66`** | 采集器解析被引断言的字面值，或在报告改口径为「双处登记，未互校」（S2-2） |

### 3.2 主线②

| 判据 | 可伪造点 | 加固建议 |
|---|---|---|
| **B1／B2／B3** | 硬编码 341／95／436 在测试内——**改一处即红**，是合格的绊线，不是伪造点 | 保持；把 341/95 的单一来源钉到路由层导出 |
| **B4** | 期望输出写成「436/436 卡 prompt 逐字」，实跑输出是 `RESULT: 44/44 fails=0`（其中「F3 对账：436 条 prompt_template 逐字相等」是**一条**断言内部遍历 436）。按方案的「期望输出」栏复跑，读者会以为判据没跑起来 | 期望输出改写为 `RESULT: 44/44 fails=0`，并在备注写清哪条断言覆盖 436（S3-5） |
| **B5 真实浏览器** | 环境缺件已有 `ABORT exit=2` 语义（`t121-browser-evidence.mjs:57`），**不静默变绿**；时序类判据已有页面侧 1ms 轮询＋`MutationObserver` 双计时（A6／A10），**驱动侧采样**这条伪造路已被堵 | 保持；把「必须页面侧计时」写进编排层的 lint |
| **B6 视觉锁** | ② 「20 条逐条有结论」是**人写的勾选表**（E3）；可把尺子的 PASS 行＋两份浏览器报告拼成「20/20」而不绑定任何单条证据 | 20 条每条必须指名 `脚本＋断言 id`；`BLOCKED` 条目单列并进缺口 |
| **B7 入口可达** | 同 A10：整文件包含即绿；`description` 截断后三要素仍可见（方案 §5-16 自陈），但**判据不会红** | 与 A10 合并为「description 行」判据 |
| **B8 运行时** | 方案已判「本图不可判定」——正确，不伪造 | 需新归属（见 S1-4） |

### 3.3 主线③

| 判据 | 可伪造点 | 加固建议 |
|---|---|---|
| **C1 冻结面 130** | 强判据（实现状态枚举） | 保持 |
| **C2／C3／C4 四门** | ① **exit 码会被 PowerShell 整体退出码污染**——方案 §2.0-2 已正确指出「看内层 `RUN … exit=<n>`，不看 pwsh 退出码」 | 编排层只读内层 exit |
| **C5 失败集新增 0** | ② **白名单可扩**：新增一条白名单即「新增 0」。方案自己抓到了最典型的实例（缺口 §5-5c：`FX-81-5 不变量` 现在**就是红的**且在 `test-failset.txt:16`） | 白名单**只许缩小**；每条白名单必须带 `票号＋签名＋复跑次数`；新增白名单条目需 `GATE-RELAX` 式声明 |
| **C6 #79 收口** | ① 「逐条有结论」＝票面人写 | 同 B6 |
| **编排层「抖动＝不计入」** | ① **越权**：`t88-delta-flake-ruling.md` §5.2／§6／§8 明定「本判据**只能**由编排者按三步判定逐条裁；session 不得自行援引第 1 条豁免」「session 只登记事实形态，不得自行主张豁免」。方案 §2.1／§2.3 让编排脚本**自动**把 NTSTATUS 崩溃判为「抖动、不计入」 | 编排层只输出**事实形态**（签名／单独复跑次数／是否基线红），豁免字段留空并标 `pending-ruling`（S2-3） |
| **编排层 3 处变异抽查** | ① **未实现**（编排脚本「待建」）。方案 §6 以「变异抽查」作为方案 B「难伪造」的论据，但该论据目前**0% 落地**；唯一落地的采集器恰好带着 A10 这个洞 | 变异抽查先落地再写进证据强度论证 |

---

## 4. 覆盖矩阵数字复核（主张 2）

**逐行点票（本席按 §4.1–§4.7 每行的「状态」列数，不采信小计表）：**

| 类别 | 行数 | 已覆盖 | 部分 | 无覆盖 | 已裁定 | 方案小计 |
|---|---:|---:|---:|---:|---:|---|
| 唤醒词 436 | 7 | 4 | 3 | 0 | 0 | 4/3/0 ✓ |
| 模板 73 | 7 | **4** | 1 | **2** | 0 | 5/1/1 ✗ |
| 渲染脚本 67 | 5 | 2 | 1 | 2 | 0 | 2/1/2 ✓ |
| 键 99 | 5 | 3 | 2 | 0 | 0 | 3/2/0 ✓ |
| 分组／子功能／场景 | 4 | 4 | 0 | 0 | 0 | 4/0/0 ✓ |
| B1 20 条 | 4 | **1** | 1 | 0 | **2** | 2/1/0（1 已裁定）✗ |
| 内容页 12 区块 | 4 | **1** | 0 | 1 | **2** | 2/0/1（1 已裁定）✗ |
| **合计** | **36** | **19** | **8** | **5** | **4** | **22/8/4** ✗ |

- 方案把小计表凑成 34＋2 已裁定＝36，却在**模板 73**、**B1**、**内容页**三行各多算 1 格「已覆盖」（其中两格实为「已裁定」，一格实为「无覆盖」）。
- ⇒ 主张「已覆盖 22／部分 8／无覆盖 4」**不成立**；正确的 36 格点票是 **19／8／5 ＋ 4 格已裁定**。
- 方案 §0 的无覆盖清单只列 3 项（旧模板本体／两个 py／B-02…B-08），矩阵里实际有 **5 格**无覆盖（另有模板「明确不做 3」与脚本「明确不做 3」两格）——同一份文档里**两套口径**。

---

## 5. 循环依赖判定

**结论：不存在严格循环，但存在一条「可避免的单向依赖」＋一处「接口未定」风险。**

1. **#89b 不依赖本方案**：`gh issue view 89` 全文 258 字，前置列为 #104／#75／#88／#108–#110／#111–#113／#86／#93——本席逐个查证，**全部 CLOSED**（`gh issue view` 实测）。#89 本身 OPEN、进度 0%、**0 条评论**。⇒ #89b 随时可开工，不等人。
2. **本方案依赖 #89b**：`RESULT-L2` 的 B5／B6 与 §4.6／§4.7 的无覆盖格（H-12／H-20／12 区块 computed）都要 #89b 的产物。这是**线性依赖**，不是环。
3. **真正的问题不是环，是接口**：方案 §6-2 写「把 `docs/research/t89-evidence/` 的 `RESULT` 行纳入 `RESULT-L2`」——本席实测**该目录不存在**（`Test-Path` False），#89 票面与评论里**没有任何**该路径/格式的约定。方案单方定义了别人的交付形态；若 #89b 按自己的形态交付，编排层返工一轮。⇒ **协调风险，非死锁**。
4. **会不会「两边都不动」？** 不会（#89 前置全关，且 #89 的探针**正在跑**：本席在 `gate-runs.log` 里看到 `ticket=89 runId=b4f5eb0d／17b9d767／bdd92ea0` 三跑，前两跑 exit=1、第三跑 exit=0）。但**方案的「不重跑」决策会让验收主动等待一张 0% 的票**——见 §6。
5. **最小解耦方案**（改设计不改代码）：
   - **自有档**：编排层直接复跑 `t121-browser-evidence.mjs` ＋ `t88-browser-evidence-b.mjs`（受跟踪 E2，实测 **11.7s** 全绿），把 H-16／H-19 与「卡级复制／搜索／回顶／无 JS」四组交互纳入 `RESULT-L2`；
   - **外部档**：#89b 只承担 **H-12（断点）／H-20（焦点可见＋动效可关）／12 区块 computed** 三项（本席核验：这三项在 t121/t88-b 里**零命中**）；
   - **接口**：编排层以 `--expect-external <dir>` 显式声明，缺件即 `RESULT: ABORT exit=2`（沿用方案自己的「不静默变绿」）；并把该接口写进 **#89 票面**（改票，不改代码）。

---

## 6. 「不重跑浏览器套件」的证据强度代价

**结论：代价不必要地高，而且省不下机时。**

- **可复用性（本席实测）**：`t121-browser-evidence.mjs` **22/22 PASS，5.3s**（runId `0fb07604`）；`t88-browser-evidence-b.mjs` **29/29 fails=0，6.2s**（runId `dd1b50f3`）。两者**都受跟踪**、都经持锁、都在本机 Chrome 上真跑（CDP 真手势）。⇒ 「不重跑」把**现成的 E2**（可复跑、末行机读）换成**E3**（他人报告/未跟踪产物），代价是证据等级下降，而机时收益≈0。
- **覆盖面（B1 第 12/16/19/20 条）**：
  - **H-16**：`t121` 覆盖（A1–A10 卡级复制、B1–B4 Sheet、C1–C4 通用、D1 失败态；含页面侧 1ms 轮询＋`MutationObserver` 双计时）→ **已有 E2**；
  - **H-19**：`t88-b` B20–B24 覆盖（`#backTop` 存在／初始隐藏／`scrollY>400` 显形／可信点击回顶／再隐藏）→ **已有 E2**；
  - **H-12（断点）／H-20（焦点可见＋`prefers-reduced-motion`）**：本席在两张套件里 grep `640|400px|focus-visible|reduced-motion|setDeviceMetrics|emulateMedia` —— **零命中**；唯一采样断点的是**未受跟踪**的 `docs/research/t89-probe-browser.mjs:259-265`（`for (const w of [1440, 640, 400])` → `Emulation.setDeviceMetricsOverride`，判据名 `P4e · H-12 断点采样`）。⇒ 这两条**必须**由 #89b 产出，且其探针**入库前不可复跑**（方案 §3.3-5 自陈）。
- **判定**：「消费 #89b 产物」对 **H-12／H-20** 是**必须**，对 **H-16／H-19 及全部交互组**是**不必要**。方案把两件事合并成一个决策，导致可用 E2 被降级、同时把验收押在一张 0% 票上。

---

## 7. 与既有裁定自洽性

| 裁定 | 方案的处理 | 本席判定 |
|---|---|---|
| **R35**（与冻结契约冲突处以契约为准；旧版差异＝改进项） | §1.1／§2.2／§4.6／§5-8 全部按 R35 落法；18 处 `OLD-DEVIATION` 判改进项 | **自洽** ✓（本席复跑 `t105 --strict`：`OLD-DEVIATION 18／DEFECT 0`，与 `visual-spec-help.md:331,338` 一致） |
| **delta 判据**（失败集新增 0） | §2.3／§5-12 采用「新增 0，不看 exit 码」 | **口径自洽** ✓；但「抖动＝不计入」的**执行主体越权**（S2-3），与 `t88-delta-flake-ruling.md:75,104` 冲突 ✗ |
| **delta 三步判定 / B 类 flake** | §2.1 抖动行：`0xC0000005`／`0xC000001D`、stderr 空、单独复跑绿 → 「B 类 flake，不计入」 | **签名引用正确**（`:62-63`），但 §5.2-3「基线本就红者一律判 C 类」意味着**基线红名单里的条目永远不能主张 B 类**——方案未把这条写进判据 ✗ |
| **`t88-final.md` L-01…L-19** | §2.2 引 `:166` L-16 说明体积；§1.1 用「已裁定偏离」承接 | **引用错位**：L-16 记 **1,010,979 B**，方案写 **1,264,822 B**（本席实测 `t83-evidence` = 1,264,822，属 #106 回补后的值）——两数不同源，须注明（S3-4） |
| **#64 关闭** | §5-4 归属 #64 | **冲突** ✗（S1-4） |
| **#89b 前置** | §5-1 归 #89 | **相符** ✓（前置全关，但接口未定，§5） |
| **#128 并发写同名产物** | §2.1 失败表第 5 行：验收脚本必须串行＋显式 `--output` | **相符且必要** ✓（本席实测：同一条 `t83-evidence` 在锁外跑出现过 1 次未复现的 `exit=1`，随后锁外复跑 21/21——与「并发假红」形态一致，**该次未留痕，不作为定责证据**） |

---

## 8. 成本与可行性（含采集器「只读」判定）

### 8.1 `t-help-acceptance-collect.mjs` 是否真「只读」——**是**

- 代码判定：import 仅 `node:crypto`（`createHash`）／`node:fs`（`existsSync`、`readFileSync`）／`node:path`／`node:url`；全文**零 `spawn`／零 `writeFileSync`／零 `mkdirSync`／零网络**；只 `import()` 四个 `dist/*.js` 并读 8 个仓内文件。
- 行为判定：本席持锁复跑 `RESULT: 27/27 PASS`（runId `0966ecb1`，**锁内 0.1s**），`git status` 无新增改动。
- 唯一保留：它 `import()` `dist/` 模块（会执行其顶层代码）——若将来某个 dist 模块有写副作用，采集器就不再只读。建议在脚本头把这条写成显式前提。

### 8.2 机时估计——**高估约一个数量级**

| 命令 | 方案声称 | 本席实测（来源） |
|---|---|---|
| `pnpm build` | 1–3 min | **1.2s**（`gate-runs.log` `8ec55c1a`／`53445b2c`，同树） |
| 四门（boundaries／snapshot:check／publish:pre） | 2–4 min | **各 0.8s**（`fa9f09fc`／`38a23ac9`／`1bfee536`） |
| `snapshot:html:check` | ≈1 min | **0.8s**（`ac79e72a`） |
| 45 测试 | 4.4 s | **4.4s** ✓（runId `9ab5d208`，`duration_ms 4383`） |
| `t83-evidence` | 3.0 s | **3.0s** ✓（runId `6846984e`） |
| `t81-exec-smoke` | **5–15 min** | **58s**（`gate-runs.log` `9bba58f1` 15:39:52.544→15:40:50.612；`ac6cf688` 58.2s） |
| 浏览器实证 | 1–3 min | **11.7s**（`0fb07604` 5.3s＋`dd1b50f3` 6.2s） |
| `t81-route-evidence` | 未列 | 20.6s（`9b6c4cac`） |
| `t105`／`t82`／`t88-probe-a`／采集器 | 未列／`<1 s` | 0.06s／0.06s／0.27s／0.1s |
| **合计（不含 install）** | **≈15–25 min** | **≈102 s ≈ 1.7 min** |

⇒ 方案 §3.2／§6 的机时列**与仓内自有的 `gate-runs.log` 相矛盾**（同一份日志正是方案其他数字的来源）。**4–8 人时**（写编排层＋报告模板＋重基线）本席认为**可信**；**15–25 min 机时不可信**（应为 2–3 min 量级）。

### 8.3 新克隆可复跑性——方案 §3.3 自陈基本属实

- 受跟踪数写 **240**，`git ls-tree -r 4d33dfe docs/research` 实为 **243**（S3-2）。
- 未跟踪的证据脚本至少 3 个：`t89-probe-browser.mjs`、`t89-probe-help-interactive.mjs`、`t-help-parity-*.mjs`（平行工作）——**方案只列了 1 个**（`t89-probe-browser.mjs`）。

---

## 9. 缺口 16 条分类复核（重点：#64 与白名单）

| 缺口 | 方案分类 | 本席复核 | 判定 |
|---|---|---|---|
| §5-1 #89b（B1 20 条新版侧） | 阻断 | 前置全关、#89 在飞；本席另证 H-16／H-19 已可自跑 | **维持阻断**，但**范围应缩小**（只 H-12／H-20／12 区块） |
| §5-2 #79 收口 | 阻断 | #79 OPEN ✓ | 维持 |
| §5-3 #83 第二席 | 阻断 | #83 OPEN ✓ | 维持 |
| §5-4 运行时入口（#64） | **阻断／可接受**，归属 #64 | **#64 已 COMPLETED 关闭（14:32:58Z）**；实测安装副本 description **仍无「卡路里HELP」**（3 处存在、第 4 处 `.dsh-module-fallback` 不存在；值 59–61 字符，非方案所称 62） | **升级：归属失效，须新票或改判 #64 口径（S1-4）** |
| §5-5 判据漂移（77／34／43） | 阻断 | 复跑 19 项红 ✓；但「×16」应为 **×15**，且 15 项**不是硬编码断言**（S2-6） | 维持阻断，修描述 |
| §5-5b 日期依赖 | 阻断 | 复跑证实：`routing.ts:300` `copyFrom:"yesterday"`＋`t81-seed.mjs:70-78` 只种 2026-09-01…07；快照 `t81-exec-smoke.md:18` 非零=1、`:538-539` 逐条登记 | 维持阻断 |
| §5-5c 白名单掩盖真红 | 阻断 | 复跑 `test/calorie-routing-81.test.mjs` 8 tests／7 pass／**1 fail**，唯一红=`FX-81-5 不变量：exec ⟺ 实跑 exit 0`（actual 1／expected 0），该名在 `test-failset.txt:16` | **维持阻断，且是最重要的一条**；补一条：该测试读的是**同一脚本生成的快照**，5b 修好后它会自动回绿——应写进 5b 的验收 |
| §5-6 旧模板本体 | 可接受 | 仓内确无旧 73 模板；引用路径错（S3-1） | 维持 |
| §5-7 95 non-exec＋3 不做 | 可接受 | 口径＝「有回执／有登记即通过」合理 | 维持 |
| §5-8 18 处 OLD-DEVIATION | 可接受 | 与 R35 自洽 ✓ | 维持 |
| §5-9 #128 并发 | 可登记 | 串行＋显式 `--output` 可规避 ✓ | 维持 |
| §5-10 #127 | 可登记 | 不属 parity 判据面 | 维持 |
| §5-11 真机非空数据 | 可接受 | `docs/calorie-architecture.md:83` 口径一致 | 维持 |
| §5-12 canonical exit 1 | 可接受 | 判据＝新增 0 ✓；但需补「基线红名单条目永远不能主张 B 类」 | 维持（补口径） |
| §5-13 体积 | 可登记 | 引用错位（S3-4） | 维持 |
| §5-14 `fallback` 无 CLI 出口 | 可登记 | 未复核（本席未跑 t83 ⑥ 全形） | 维持 |
| §5-15 `changeset:status` | 可登记 | 环境红 ✓（`t88-final.md` §6.2） | 维持 |
| §5-16 description 余量 4 | 可登记 | 实测 499 ✓；**但 A10 的整文件搜索让「截断后仍绿」成立** | **升级为 S2-1 的组成部分** |

---

## 10. 缺陷清单（S1／S2／S3 ＋ 归属）

### S1 · 交付缺陷（不修则方案落地即错）

| id | 缺陷 | 归属 |
|---|---|---|
| **S1-1** | A1 期望输出 `exec 341 ＝ 56＋22＋26＋222` 是**假等式**（实为 326，差 15）；方案 §2.4 把失败断言的输出当成「当前权威口径」照抄 | 方案 §1.3／§2.1／§2.4 作者 |
| **S1-2** | §4.6「静态可判（A 级）→ 已覆盖」不成立：尺子自判 H-02／H-05／H-12／H-16 的 computed 面与 H-10 圆角 token 为 `BLOCKED`；所引 `t75-style-evidence.mjs` **不存在**；`t75-style-evidence.md` R7 明写「B1 的 H-06／H-09／H-17／H-19 **不在本票验收面**」 | §4.6 作者＋§5-1 需补 H-02／H-05／H-10 |
| **S1-3** | 覆盖矩阵小计与行不自洽：22／8／4 应为 **19／8／5＋4 格已裁定**；模板 73 小计 5/1/1 ≠ 行 4/1/2；B1 与内容页各多算 1 格 | §4.8 作者 |
| **S1-4** | 阻断缺口「运行时入口」归 **#64**，而 #64 已于 **2026-09-09T14:32:58Z COMPLETED 关闭**（早于本方案提交 72 min），安装副本实测仍无唤醒词 ⇒ 阻断项**无未关闭归属**，`RESULT-63` 的「无阻断项」条件不可达 | §5-4＋需新票（或改判 #64 收口口径） |

### S2 · 实质缺陷（削弱证据强度／越权／成本失真）

| id | 缺陷 | 归属 |
|---|---|---|
| **S2-1** | 采集器 A10 在**整文件**搜「卡路里HELP」，不校验 `description` 行；与缺口 §5-16（余量 4 字符）叠加 → 截断后静态层仍绿 | `collect.mjs:77` |
| **S2-2** | §3.1 声称静态层「已具备交叉来源」，代码里**没有**读 `help-center-106.test.mjs`（仅出现在 `src` 字符串）→ 伪造路径：改测试里的 341，采集器照样 PASS | `collect.mjs:71-72`＋§3.1 |
| **S2-3** | 编排层**自动**把 NTSTATUS 崩溃判为「抖动／不计入」，与 `t88-delta-flake-ruling.md:75,104` 的「只能由编排者裁／session 只登记事实形态」冲突 | 方案 §2.1／§2.3／§3.1 |
| **S2-4** | 「不重跑浏览器套件」把可用 E2 降级为 E3，且依赖**未定接口**（`docs/research/t89-evidence/` 不存在、#89 票面 0 约定、探针未受跟踪）；实测两张套件复跑 11.7s 全绿 | 方案 §6-2／§7-1 |
| **S2-5** | 机时高估约 10×：exec-smoke 声称 5–15 min，实测 **58s**；四门声称 2–4 min，实测各 0.8s；浏览器声称 1–3 min，实测 11.7s；合计实测 ≈1.7 min vs 声称 15–25 min | §3.2／§6 |
| **S2-6** | 19 项红的构成与自述不符：实测＝漂移 30 处（1 项）＋「家族未派生但路由层为 exec」**15 项**＋3 项常量项；方案写「×16」并把这 15 项归入「硬编码断言」签名（它们源于**族派生模型未重基线**，不是硬编码常量） | §2.1／§5-5 |

### S3 · 可登记

| id | 缺陷 |
|---|---|
| **S3-1** | `fixtures/README.md:31-52` 不存在，实为 `fixtures/help-instances/README.md:31-52`（§3.3-6、§5-6 两处引用） |
| **S3-2** | `docs/research` 受跟踪数写 240，`4d33dfe` 实为 243 |
| **S3-3** | §5-5「77／34／43／**361**」与 §2.1 的「341」混用；脚本 `:460` 是自洽断言（`sum === EXEC_ROUTES.length`），无硬编码 361 |
| **S3-4** | 体积引 `t88-final.md:166` L-16（记 **1,010,979 B**），方案用 **1,264,822 B**（#106 回补后；本席实测同值）——须注明来源差异 |
| **S3-5** | B4 期望输出写「436/436」，实跑为 `RESULT: 44/44 fails=0`；B4 现状「未复跑」不实（0.27s 即绿）→ `RESULT-L2` 现状应为 **5/8** |
| **S3-6** | 「62 字符旧版 description」实测 59–61 字符；「四处已安装副本」现仅 3 处存在 |
| **S3-7** | §附索引把 `t82-verify-triggers.mjs`／`t105-check-rulers.mjs` 写成无锁命令，与 §2.0-1「读 dist 一律持锁」自相矛盾 |
| **S3-8** | §3.3-5 只列 1 个未跟踪证据脚本，实为 ≥3 个（`t89-probe-browser.mjs`／`t89-probe-help-interactive.mjs`／`t-help-parity-*.mjs`） |

### 本席核实为**真**的部分（避免误伤）

`RESULT: 27/27 PASS`（`0966ecb1`）｜`t81-route-evidence` 19 项红、两次逐字一致（`9b6c4cac`／`d7a09bac`）｜45/45（`9ab5d208`）｜8 tests／7 pass／1 fail＝`FX-81-5`（`1d09eb27`）｜`t83-evidence` 21/21＋5 词抽样（`6846984e`）｜`t82` 69/69｜`t88-probe-impl-a` 44/44｜`t121` 22/22｜`t88-b` 29/29｜`t105` `DEFECT 0`＋`OLD-DEVIATION 18`＋`BLOCKED` 逐字｜`test-failset.txt:13-16` 三条命中｜`t81-seed.mjs:70-78` 绝对日期｜`routing.ts:300` `copyFrom:"yesterday"`｜`present.ts` 109｜130／99／64／35｜`ci.yml` 117 行无浏览器｜`fixtures/help-instances` 4 件**受跟踪**｜采集器**只读**｜`SKILL.md` 首 3 字节 `2D 2D 2D`。

---

## 11. 五维评分与 verdict

| 维度 | 满分 | 得分 | 依据 |
|---|---:|---:|---|
| **契约一致** | 30 | **17** | R35 全自洽（＋）／delta「新增 0」口径自洽（＋）／但「抖动自动豁免」越权（−）／#64 已关闭仍作归属（−）／#89b 接口单方发明（−）／A1 假等式（−） |
| **证据真实可复现** | 25 | **13** | 27/27＋GATE-RUN 对账格式合规（＋）／主要行号引用准确（＋）／但 A1 口径取自失败断言且算错、B4 期望输出不符、B6 依赖不存在目录、A 级引用不存在脚本、两处路径不存在、体积与 L-16 混用（−） |
| **parity** | 20 | **13** | 三层口径（能力/契约/像素）有据且正确、无覆盖项确无覆盖（＋）／矩阵小计不自洽、A 级把 computed 面当已覆盖、缺口清单漏 H-02/H-05/H-10（−） |
| **工程红线** | 15 | **9** | 零 `packages/**`／`tooling/**` 改动、持锁＋GATE-RELAX 声明、采集器真只读（＋）／承诺的 3 处变异抽查 0% 落地、串行/显式 `--output` 未落地、静态层自带 A10 洞（−） |
| **文档同步** | 10 | **6** | 多数行号引用准确（＋）／2 处引用对象不存在、受跟踪数 240≠243、×16≠×15、361/341 混用、62 字符、B4 期望输出（−） |
| **合计** | **100** | **58** | |

> ## **verdict: FAIL**

### 最小整改清单（7 条，改文档即可，不涉代码）

1. **改 S1-1**：把 A1 期望输出改为 `exec 341 ＝ 直连 56 ＋ override 22 ＋ 孪生 26 ＋ 参数化 222 ＋ **新族派生 15**`（或直接删掉等式，改为「四分类合计 ＋ 未分类 = EXEC_ROUTES.length，未分类必须为 0」）。§2.4 的「权威口径」段落同步改写。
2. **改 S1-2**：§4.6 A 级拆成两行——「冻结常量侧（尺子 PASS）」与「渲染后 computed 侧（`BLOCKED`，归 #89b）」；删除不存在的 `t75-style-evidence.mjs` 引用，改引 `t75-style-evidence.md`（并注明 R7 的排除面）；§5-1 补 H-02／H-05／H-10。
3. **改 S1-3**：§4.8 小计按行重算为 **19／8／5＋4 已裁定**；§0 的无覆盖清单同步为 5 格。
4. **改 S1-4**：运行时入口缺口**改归属**（新票或 #64 重开/改判），并注明「#64 已 COMPLETED」与「实测 3 处安装副本仍为旧 description」。
5. **改 S2-4**：§6-2／§7-1 改为**自有档＋外部档**（§5-5 的最小解耦），并把 `--expect-external`＋`ABORT exit=2` 写进 §3.1 的编排层规格；同时把该接口写进 #89 票面。
6. **改 S2-3**：§2.1／§2.3／§3.1 明确「编排层只登记事实形态，豁免判定归编排者（`t88-delta-flake-ruling.md:75,104`）」，输出字段加 `pending-ruling`。
7. **改 S2-1／S2-2／S2-5／S2-6**：采集器 A10 改判 `description` 行；删除或落实「交叉来源」；机时表按实测改写（合计 ≈2–3 min，exec-smoke 58s）；「×16」改「×15」并改判据签名描述。

---

## 12. 本席证据（GATE-RUN ＋ 对账）

```
GATE-RUN runId=0966ecb1-6bfb-4432-9149-be3031692d47 cmd=node docs/research/t-help-acceptance-collect.mjs            exit=0 (锁内 0.1s)
GATE-RUN runId=9b6c4cac-0242-40aa-a9cd-ad0da3d104be cmd=node docs/research/t81-route-evidence.mjs                    exit=1 (20.6s)
GATE-RUN runId=d7a09bac-72ee-4071-9726-3b70cea12cc4 cmd=node docs/research/t81-route-evidence.mjs                    exit=1 (20.7s)
GATE-RUN runId=9ab5d208-7b96-4d36-999c-02dac9e00be5 cmd=node --test packages/skill-calorie/test/help-center-88.test.mjs packages/skill-calorie/test/help-center-91.test.mjs packages/skill-calorie/test/help-center-106.test.mjs packages/skill-calorie/test/cmd-read-t11.test.mjs  exit=0 (4.4s, 45/45)
GATE-RUN runId=1d09eb27-3d9a-4aa0-8bc5-7ddd7e9bae66 cmd=node --test test/calorie-routing-81.test.mjs                 exit=1 (8/7/1, FX-81-5)
GATE-RUN runId=6846984e-3fbc-411f-8e7d-f7059024293c cmd=node docs/research/t83-evidence.mjs                          exit=0 (3.0s, 21/21)
GATE-RUN runId=0fb07604-18a4-40f9-8264-7eb0042547a0 cmd=node docs/research/t121-browser-evidence.mjs                  exit=0 (5.3s, 22/22)
GATE-RUN runId=dd1b50f3-ad6c-4b20-8286-cfbd5c36c9a2 cmd=node docs/research/t88-browser-evidence-b.mjs                 exit=0 (6.2s, 29/29)
GATE-RELAX flag=--allow-nonzero  reason=上表 3 条 exit≠0 是被观测对象本身（判据漂移／日期依赖／白名单真红），非本席门禁失败
GATE-RELAX flag=--allow-undeclared reason=窗口内另有 ticket=83／ticket=89 的运行，非本席声明面
```

**对账实测**：

```
node tooling/check-gate-audit.mjs --evidence docs/research/t-help-acceptance-plan-review.md \
  --ticket 63 --since 2026-09-09T15:48:30.000Z --until 2026-09-09T15:53:30.000Z \
  --allow-nonzero --allow-undeclared
→ RESULT: matched=8/8 auditEntries=774 scoped=32 undeclared=24 → PASS
```

**锁外探针（声明：只读、零写盘、仅用于机时估计与只读性核验，不作为判据证据）**：`t105 --strict`（0.06s）、`t82`（0.06s）、`t88-probe-impl-a`（0.27s）、采集器计时（0.07s）、`t81-route-evidence` 计时（21.3s）、`t83` 计时（1.38s，其中一次 exit=1 未复现、无留痕，不作为定责证据）。

**未做（诚实声明）**：未跑 `pnpm test` 全量、未跑 `t81-exec-smoke.mjs`（纪律禁止）；未跑 `pnpm build`／四门（改用 `gate-runs.log` 内他席持锁记录推算机时）；未读旧树 `D:\2Study\StudyNotes\SKILLS\卡路里` 内容（只 `Test-Path` 存在性）；未读 `.个人笔记不允许参考`；未验证 §5-14（`fallback` 形）与 §5-13 的体积归因细节。
