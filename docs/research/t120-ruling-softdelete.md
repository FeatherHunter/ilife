# #120 裁定（编排者 · 2026-09-09）：软删口径收敛的边界与串行约束

> 触发：`#120` session 上报三个冲突（A 文案自相矛盾／B 既有测试断言修复前行为／C build 被他人 WIP 打断）。
> 依据：`gh issue view 120`（验收条「口径一致且文案与之一致」）＋ `docs/subagent-concurrency-protocol.md` §1 路径所有权／§5／§6.1。

## 冲突 A：软删回执文案（编排者事实更正 ＋ 授权）

**事实更正**：我此前引用的「已删除（软删除，可恢复）」**不准确**。实测 `packages/skill-calorie/src/cli/write.ts:120` 为：

```
SOFT_STILL_COUNTED = '（软删除：行保留，仍计入历史统计；暂无恢复入口）'
```

（#101 返修时定稿。）该文案与 #120 的「统一排除」口径**直接矛盾**。

**裁定**：**维持方向 1（统一排除）**，文案必须一并改。

- **授权**：`packages/skill-calorie/src/cli/write.ts` 的**软删文案常量 ＋ 紧邻注释**由 #120 修改，**仅此两处**，最小 diff；体脂／围度／下架食品同款措辞一并统一。
- **串行约束（硬）**：`write.ts` 当前由 **#97 session 在飞**（WIP，含 `TS2322`）。#120 **只能在** `git status --short -- packages/skill-calorie/src/cli/write.ts` 为空**且** `git diff --stat -- <该文件>` 为空 的窗口内改。
- **拿不到窗口时**：#120 **不得强改**，把「文案统一」记为**具名待办**（写明为何阻塞、依赖 #97 的哪次 commit）后停报；由编排者在 #97 落地后安排。
- 文案方向示例（须与「排除」一致）：`（已删除：不计入统计；行保留可恢复）`。

## 冲突 B：既有测试与历史证据断言修复前行为

**裁定**：

1. **授权 #120 更新测试** `packages/skill-calorie/test/cmd-write-40-persist.test.mjs:640-655`：把「删前=删后」改为「软删后逐面排除」；测试名／注释须标注「**#120 口径收敛，supersedes #101 的『仍计入』口径**」。#120 的修复**必然**让原断言变红，这属**真 delta（A 类）**，必须由本票同步更新，不得改白名单。
2. **禁止改写历史证据** `docs/research/t101-softdelete-still-counted.mjs`（它是 #101 当时的事实快照）。改为：
   - 新增 `docs/research/t120-supersedes-t101-softdelete.md` 说明取代关系（谁取代谁、依据、日期）；
   - 先查该脚本是否被 `package.json` scripts 或 CI glob 调用：**若被调用**，只更新**调用点**并留痕，脚本本体不动；**若未被调用**，保持原样。

## 冲突 C：build 被他人 WIP 打断（非本票引入）

**裁定**：确认非 #120 引入（报错仅在他票路径 `write.ts:327,451` 的 `TS2322`）。处置：
- #120 等待并重试，**门禁结论必须登记当轮并发上下文**（他人 `git status --short`／`git diff --stat` 快照）。
- 已同步 #97 session 优先修 `TS2322`。

## 新增跨票纪律（即时生效）

**任何 session 在报门禁 exit 码或 delta 之前，必须先确认工作区没有他人未提交的受跟踪改动**：

- `git status --short` 中若出现 ` M <他票路径>` → 门禁结论必须标注「并发窗口：含 X 票 WIP」；
- **不得**在他人 WIP 窗口内据此判定 `pnpm test` delta（新增项可能来自他人半成品）；
- 拿不到干净窗口时，**如实登记「门禁未取得（他票 WIP）」**，不得用带污染的结论冒充通过。
