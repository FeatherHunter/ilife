# t123 AFK 复核证据（2026-09-09，执行员，HEAD a8156fa）

标注：隔离仿真／仓外临时目录，合成数据仅证链路，不得替代真机 HITL-0 证据。

## 版本元组

- `npm view <pkg> version --prefer-online`：base-paint / skill-calorie / dsh-calorie = 0.2.0 / 0.2.0 / 0.2.0；dsh-life-pack = 0.2.0。
- 工作区：packages/base-render / packages/skill-calorie / packages/plugin-calorie = 0.2.0 / 0.2.0 / 0.2.0。
- 结论：版本元组一致。

## 依赖链（注册表实测）

- skill-calorie@0.2.0 deps：{"base-paint":"^0.2.0"}。
- dsh-calorie@0.2.0 deps：{"dsh-life-pack":"^0.2.0","skill-calorie":"^0.2.0"}。

## 仓外试装＋5 导出

- `npm install --prefer-online base-paint@0.2.0 skill-calorie@0.2.0`（隔离目录）：added 2 packages，exit 0。
- 发布产物 base-paint 具名导出 5/5：ACTION_ID_ATTR，DEFAULT_DATA_ATTR，HELP_COPY_ACTIONS，buildSharedHelpersJs，renderActionBar。

## tarball 清单（npm pack 实测）

- skill-calorie-0.2.0.tgz：含 package/SKILL.md；templates/*.html 6 件（diet，exercise，goal，help，home，photo-gallery）。
- dsh-calorie-0.2.0.tgz（共 32 件）：含 package/dist/client.js，package/dist/slot.js。

## 版本串抽查

- SKILL.md 无 @0.1.1 残留；三处 range 均为 ^0.2.0（base-link-core ^0.1.0 属范围外，未动）。

## 冒烟 1 envelope（发布产物 dist/cli/cmd_read.js，argv 直调，date=2026-09-09）

- 空隔离库：exit=4，ERR4 无今日数据 → 按判定三分记缺证据（非缺陷）。
- 合成种子库（calorie.diet.add exit=0 记一餐 AFK合成验证餐 250kcal）再跑 view.home：exit=0，shape=stat，data.metrics.entryCount=1，data.output=calorie_html/今日总览_20260909_164600.html，文件存在（2488 字节，非空）。
- 口径记录：output 在 data.output 下（非顶层字段）；envelope 契约 version 仍为 0.1.0（勿动项）。

## 冒烟 2 loadTemplate（安装态 dist/render/templates.js）

- home / diet / exercise / goal / photo-gallery / help：6/6 可读（1677-1810 字节）。

## 原始日志

- 工作副本 `.tmp-t123logs/`：envelope-20260909.meta.json（含 exit），envelope-seeded-20260909.txt，loadtemplate-20260909.txt，隔离库 `db-empty/`（含落盘 HTML）。
- 注：`envelope-20260909.txt` 0 字节是 exit=4 空库的符合预期形态（stdout 为空，全靠 meta.json 的 exit + `.err` 判定）；`envelope-20260909.err` 为 UTF-8 编码（用字节头验证过，非乱码，控制台按 GBK 显示才似 mojibake），不影响判定。
- 日志命名约定：`install-<YYYYMMDD>.log`（仓外试装完整 stdout/stderr）、`export-<YYYYMMDD>.txt`（5 具名导出检查）、`pack-list-<包>-<YYYYMMDD>.txt`（tarball 文件清单）、`envelope[-seeded]-<YYYYMMDD>.*`（冒烟 envelope）。

## 整改补记（2026-09-09，整改员，三条重跑均通）

- 仓外隔离试装（仓外临时目录 `$env:TEMP/t123-zg-20260909`，`npm install --prefer-online base-paint@0.2.0 skill-calorie@0.2.0`）：exit 0，`added 2 packages`，完整 stdout/stderr 见 `.tmp-t123logs/install-20260909.log`。
- 5 具名导出检查（发布产物 `base-paint`）：`ACTION_ID_ATTR` / `DEFAULT_DATA_ATTR` / `HELP_COPY_ACTIONS` / `buildSharedHelpersJs` / `renderActionBar` = 5/5 OK，见 `.tmp-t123logs/export-20260909.txt`。
- tarball 清单（`npm pack` 实测，输出全文见 `.tmp-t123logs/pack-skill-20260909.txt`）：`skill-calorie-0.2.0.tgz` 共 400 件（含 `package/SKILL.md` + `templates/*.html` 6 件），见 `.tmp-t123logs/pack-list-skill-20260909.txt`；`dsh-calorie-0.2.0.tgz` 共 32 件（含 `package/dist/client.js` + `package/dist/slot.js`），见 `.tmp-t123logs/pack-list-dsh-20260909.txt`。
- 本次为重跑的最小命令新日志（非伪造旧日志）；旧 `.tmp-t123logs/` 的 smoke 输出（envelope/loadtemplate）仍有效，继续引用。

## 给 #122 的锚点（两行）

- 腿 1.1（仓外试装＋安装日志口径）：见本文件“仓外试装＋5 导出”节与 `.tmp-t123logs/install-20260909.log`。
- 腿 2.3（版本元组一致口径）：见本文件“版本元组”节（注册表三包 0.2.0＋`dsh-life-pack` 0.2.0，工作区三包同为 0.2.0）。
