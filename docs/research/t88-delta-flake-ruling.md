# 编排者裁定：`pnpm test` 失败集 delta 的「环境抖动」分类口径（跨票统一）

> 生效：2026-09-09。适用：#63 图内所有票（#88／#97／#120／治理返修…）。
> 依据：`docs/research/t88-baseline/BASELINE.md`（冻结基线，四门 exit 0；失败集判据＝**具名测试名集合新增 0**）＋ `docs/subagent-concurrency-protocol.md` §5／§6.1。
> 触发事实：`#88` 实施 B 段 canonical `pnpm test` 5 轮中 2 轮各多 1 条新失败，签名同为 `AssertionError: exit=3221225477`（＝`0xC0000005` 访问违例，来自 `spawnSync` 起的 node CLI 子进程，`output-naming-87.test.mjs:58`／`cmd-read-t11.test.mjs:69`），而这两个文件**单独连跑各 6/6 全绿**。

## 1. 判据（不变）

`pnpm test` 的 delta 判定**仍以具名测试名集合为准**（不以 exit 码为准），基准＝`docs/research/t88-baseline/test-failset.txt`（34 条并集白名单）。**冻结白名单不得修改**（`git diff 93e27f9 -- docs/research/t88-baseline/test-failset.txt` 必须为 0 行）。

## 2. 新增项必须分类（本次补充）

对每一轮出现的「白名单外新增」，必须按**失败签名**分类，并在证据里逐条留痕：

| 类别 | 判据 | 处置 |
|---|---|---|
| **A. 内容断言不符** | `AssertionError` 携带期望/实际值差异（`expected … actual …`） | **真 delta** → 必须修；`新增≠0` 成立，本票不得关闭 |
| **B. 进程级崩溃／环境抖动** | 签名含 `exit=3221225477`／`0xC0000005`（访问违例）、`SIGSEGV`、`spawn` 子进程被信号杀死，**且**该文件**单独复跑 ≥2 次全绿**（附命令＋输出） | 如实登记（签名＋单独复跑证据＋当时并发上下文），**不计入「新增」** |
| **C. 不确定** | 签名不属于 A 也不属于 B，或单独复跑仍红 | 按**真 delta** 处理（保守优先） |

**升级规则**：同一测试名在**连续 ≥3 轮**中都出现 B 类崩溃（非偶发）→ 升级为真 delta 处理，并另开 flake 票。
**留痕要求**：证据文件必须写清「轮次／新增测试名／失败签名／单独复跑结果／当轮并发上下文」，缺任一项不得主张 B 类。

## 3. 并发上下文（本次成因）

当时同时有 **4 个 session** 在跑（#88 实施 B／治理返修／#120／#97）。全量测试经锁串行，但**锁只保证互斥、不降低残余负载**；Windows 上高负载时 `spawnSync` 子进程易出现 `0xC0000005`。

**纪律（对所有 session）**：
1. 优先跑**靶向**测试；全量 `pnpm test` 只在需要 delta 判定时跑，且经持锁包装器。
2. 全量测试**不得与其他 session 的全量测试重叠**（锁已保证），也**不得与 headless 浏览器实证同时跑**（本票 B 段已出现该组合）。
3. 报告 delta 时**必须附当轮并发上下文**（同时有几个 session 在跑、是否有浏览器实证）。

## 4. 本次裁决

- **接受 (a) 分类处置**：`#88` 轮 1／2 的两条新增按 **B 类（环境抖动）** 处理，但必须满足 §2 的留痕要求（签名＋单独复跑 ≥2 次全绿＋并发上下文）。
- **不接受**任何形式的「改白名单」或「静默忽略」。
- `#88` 收尾时须以**多轮取并集**的方式给出结论，并在证据里列出**每一轮**的新增项与分类结果。

## 5. 修订 1（2026-09-09 · 签名表泛化 ＋ 反滥用守卫）

**起因**：`#88` 实施 B 第 8 轮出现 `AssertionError: exit=3221225501`（＝`0xC000001D`，`STATUS_ILLEGAL_INSTRUCTION`），与 §2-B 列举的 `3221225477`／`0xC0000005` **同族**（`spawn` 子进程以 NTSTATUS 异常终止），但不在字面名单内。

**5.1 签名表泛化**（替代 §2-B 的签名判据）：

> **B 类判据**：失败由 `spawn`／`spawnSync` 起的子进程**以 NTSTATUS 异常退出**（`exit=32212xxxxx`，如 `0xC0000005`／`0xC000001D` 等）**或**被信号杀死，**且** stderr **无断言差异信息**，**且** 该文件**单独复跑 ≥2 次全绿**（附命令与输出）。

**5.2 反滥用守卫（本次新增，优先级高于签名泛化）**：满足下列任一条，**一律判 C 类（真 delta）**，不得主张 B 类：

1. 崩溃测试所属文件**落在本票声明的路径所有权内**，或其被测对象正是本票改动的代码；
2. stderr 含断言差异信息（expected/actual）、或失败为 `AssertionError` 之外的**业务语义错误**；
3. 该测试名在**冻结基线**两轮 canonical 日志中**不是通过**（即基线里本就红）；
4. 同一测试名在**连续 ≥3 轮**中出现 B 类崩溃（非偶发）；
5. 无法提供「单独复跑 ≥2 次全绿」的命令与输出。

**5.3 本次具体裁决**：`#88` 轮 8 的 `落库 · 体重 log/update/batch/remove：三删法各自真删`（`cmd-write-40-persist.test.mjs:80`）满足 5.1，且 **不触发** 5.2 任一条（该文件不在 #88 路径所有权内、stderr 空、基线两轮均 ✔、单独复跑 4/4 全绿）→ 记 **B 类**。

**5.4 并集结论口径**：`#88` 实施 B 段 8 轮 canonical 的并集＝**新增 3 条全为 B 类、真 delta 0**；收尾证据须逐轮列出「轮次／新增测试名／签名／单独复跑结果／并发上下文」。

## 6. 修订 2（2026-09-09 · 跨票 WIP 污染的分类）

**起因**：治理返修 session 终轮 3 条新增，签名是 **A 类（`AssertionError` 带 expected/actual）**，但实测：① 三条的测试文件**都属他票在途 WIP**（`m5-receipt-97.test.mjs`@`11be0c7`、`softdelete-120.test.mjs`@`aaf494d`）；② **同一棵树的前一轮这 3 条全绿**；③ 单独复跑：`#120` 两条 **2×全绿**，`#97` 一条 **2×仍红（35/40，比全量更红）**。

**裁定（三步判定，按序）**：

1. **归属过滤**：新增测试名所属文件若**不在本票声明的路径所有权内**，且失败可归因于他票在途 WIP → **不计入本票 delta**；记为 S3 并**转票**给该文件 owner。
2. **同树对照**：若同一棵树（同 HEAD ＋ 同工作区快照）的**前一轮该条为绿**，则后续轮变红属**环境／顺序污染**，不作真 delta。
3. **单独复跑定责**：若该条**单独复跑仍红** → 它是**该文件 owner 的真实缺陷（S2）**，须由该票修完方可关闭；但**仍不计入本票 delta**。

**本次具体裁定**：

- 治理返修终轮 3 条 → **均不计入 #88 delta**（三步判定全部落在「他票 WIP ＋ 同树前轮绿」）。
- `#120` 两条（`softdelete-120.test.mjs`）→ **顺序／环境污染**，转 #120 复核（单独 2×全绿）。
- `#97` 一条（`m5-receipt-97.test.mjs`，单独复跑 35/40 仍红）→ **#97 的真实缺陷（S2）**：测试存在**夹具顺序依赖**，须由 #97 修完方可关闭；**不得**改白名单掩盖。

**纪律**：本判据**只能**由编排者按三步判定逐条裁；session 不得自行援引第 1 条豁免。

## 7. 修订 3（2026-09-09 · 更正修订 2 对 #97 的判定：陈旧 `dist` 假红）

**起因**：蓝队审查（`docs/research/t88-review-impl-b-blue.md`，commit `d1e2c36`）用证据链指出修订 2 §6 对 `#97` 的判定**证据不足**；编排者复核后**确认蓝队正确**。

**事实**：

- `#97` 的「单独复跑 2×仍红（35/40）」两轮（`13:20:15Z`／`13:20:31Z`）发生在**源码已还原但 `dist/` 仍是变异产物**的窗口——测试走 `dist/`，故读到的是**陈旧变异构建**，属**假红**。
- 干净重建后：`.scratch/locks/gate-runs.log:202-203` 记 `runId=2f278cc0-ef8f-4ab4-b8dd-7287b0908ce1`、`cmd="node --test packages/skill-calorie/test/m5-receipt-97.test.mjs"`、**`exit=0`**；`#97` 另报干净重建后**单独复跑 4 次全绿**（`2f278cc0`／`0d5f315a`／`b938348d`／`3b536393`，含 `--test-concurrency=8` 顺序压力），均 40/40 exit 0。

**裁定（更正）**：

1. **撤销**修订 2 §6 对 `#97` 的「真实缺陷（S2）」判定；`m5-receipt-97.test.mjs` **不构成 #97 的关闭前置**。
2. 该条按 §6 第 1／2 步处置：**不计入任何票的 delta**，记为**环境／构建陈旧污染（S3）**。
3. **判据补充（新增第 4 步）**：单独复跑**必须先做干净重建**（`tsc -b --force`／`pnpm build`），否则读的是 `dist/` 陈旧产物，结论无效。**未重建的「单独复跑仍红」不足以定责**。
4. **纪律**：变异自证必须**每个变异后立即还原 ＋ 重建 dist ＋ 自证**，不得留到批末（断电／并发会把变异留在工作区，并让后续复跑读到陈旧 `dist`）。
