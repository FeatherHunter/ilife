# #897 证据：图表图例 11.5px→12px

## 改前（2026-09-26 读码＋产物）
- `packages/base-render/src/components/charts/style.ts:104`：`charts-legend{font-size:11.5px}`（窄屏 `@media(max-width:720px)` 覆盖；基规则 `:55` 为 12px）。
- `packages/base-render/dist/components/charts/style.js:99`：同值 11.5px。
- `chartsCss('ilife-')`：`HAS_11_5:YES`（复现命令见下）。
- 判据：`PAGE_LIMITS.textMinPx=12`（`dist/pageUi.js` 实测 `{"touchMinPx":44,"textMinPx":12}`）；判分 `D5W.font=5`，`minFont>=12?0:5`。
- 影响：含图例页 390 档 `minFontPxNoSvg=11.5`，5 页（区间汇总 21 天／复盘区间 61 天通用档／复盘区间 7 天按本周档／复盘本周／周视图）各扣 5 分（页分 95／96，仍过线）。

复现：
```powershell
node -e "import('./packages/base-render/dist/charts.js').then(m=>{const css=m.chartsCss('ilife-');console.log('HAS_11_5:'+(css.includes('charts-legend{font-size:11.5px}')?'YES':'NO'))})"
# 改前输出 HAS_11_5:YES
```

## 改后（一处）
- `src/components/charts/style.ts:104` 改为 `charts-legend{font-size:12px}`（附 `#897` 注释说明与桌面基规则值对齐；修一处全域跟走）。
- 编译：`node tooling/run-locked.mjs --ticket 897 -- node node_modules/typescript/bin/tsc -b packages/base-render` exit 0。
- `dist` 验证：`style.js:102` 为 12px；`chartsCss` 输出 `LEGEND_11_5:NO_FIXED`、`LEGEND_12:YES`。
- 未碰：其他组件的 11.5px（`blocks.ts:2839`、`ledger-rows`、`entry-rows` 等）与各域源码（按票面“禁各域抄一份／不塞 if”）。

## 测试
- 新增 `packages/base-render/test/t897-legend-min-font.test.mjs`：缺省／自定义前缀两处图例字号均 ≥`textMinPx`、无 11.5px 残留＋变异自证（改回 11.5 即红）。
- 单文件：3/3 绿；相关三件（`charts`＋`style`＋`t897`）：123/123 绿。
- 全量（2026-09-26，工作区有多席在途改动，失败均为无关项）：
  - `base-render` 全量：3805/3808，3 失败全为 `CDP 未就绪（headless Chrome 起不来）`（`confirm-strip`／`editableValue`／`skeleton` 的无头 Chrome 用例），`t897` 在全量内通过。
  - `skill-schedule` 全量：264/264 绿（直接受影响的 5 页所在域）。
  - `skill-memo-ilife`：286/287，1 失败为联动表 13 vs 15（`skill.test.mjs:49`，命令联动数，与图例无关）。
  - `skill-home`：355/357，2 失败为联动/shape（`render` 21 键），与图例无关。
  - `skill-calorie`：1870/1919，49 失败全为域内票据（`#514/#566/#93/#448` 等），抽检无 `charts-legend` 命中；唯一 `11.5px` 命中为 `data-table td::before`（他票范围）。
  - `gen-help-shell.cjs --check`：OK。
- 跨域回归结论：无新增图例失败；他域失败均为在途改动／环境项，未见 `charts-legend` 相关红。

## 对抗自查
- [x] 边界：≤720 与 ≥721 两档图例均为 12px，无断点跳变；自定义前缀同规则（测试覆盖）。
- [x] 异常分支：`legend=false`／空名序列不出图例块时，CSS 仍在但无 HTML，`minFont` 取他元素，不变差。
- [x] 回归影响：SVG 内文字（9.5／10 等）被 `minFontPxNoSvg` 排除，不受影响；桌面档本就 12px，无变化。
- [x] 并发与旧数据：只写本票两路径（`src/.../style.ts`＋`t897-...test.mjs`＋本证据件）；他席脏文件未碰；旧产物含旧 CSS，需随各域下次发版自然带到（本票不 BACKPORT，按票面口径）。

## 待收口验收（需真机／产物重出，非本包单元可代）
- [ ] 作息 5 页用各域当前产物重出＋`t516` 390 档 `minFontPxNoSvg ≥ 12`，`判分.mjs` 字号扣分归 0（改前 11.5／扣 5）。
- [ ] 卡路里／备忘录／居家含图例页重测只升不降。
