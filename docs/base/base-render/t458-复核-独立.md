# #458 独立复核回执（复核席，与实施无交集）

- 复核对象：实施提交 `0076deff`（`spec/charts.ts` 类型＋`charts.ts` 桥接＋新测试＋证据件）。
- 复核方式：票体与证据件只当读物，不采信结论；全部机器证据本席独立复跑。
- 票面状态：`gh issue view 458` 取回时评论数为 0，无票面新裁定可跟进。

## 判定：PASS

## 机器证据（经 `node tooling/run-locked.mjs --ticket 458 -- …`）

| # | 命令 | runId | exit |
|---|------|-------|------|
| ① | `node node_modules/typescript/bin/tsc -b packages/base-render` | `81f3dd26-3210-41bc-9054-93d92e4ad932` | 0 |
| ② | `node --test packages/base-render/test/charts-gap-458.test.mjs` | `681b5fa9-c692-447d-a586-165d2d6b31ab` | 0（5/5 过） |
| ③ | `node --test packages/base-render/test/charts.test.mjs packages/base-render/test/style.test.mjs packages/base-render/test/contract-signatures.test.mjs` | `5719c0ea-311d-48b9-87c9-84bbc3f0afcd` | 0（168/168 过，含 `B.connectNulls`、`B.fillBetween` 逐字节断言） |
| ④ | 自设探针 `node .scratch/t458r/probe-458r.mjs` | `26411f34-e812-4f71-a16f-f13bd3ca77fa` | 0（12/12 过；等锁约 10s，未抢占他席） |

## 自设探针（实施测试未覆盖处，本席新增）

- P1 显式优先与非法回落：`break` 压住 `connectNulls:true`（＝缺省逐字节）；`dashed` 压住 `connectNulls:true`（仍出桥接）；`connect` 压住 `connectNulls:false`；非法 `gapStyle:'zzz'` 回落旧口径（单给＝缺省，配 `connectNulls:true`＝直连）。
- P2 均线：`avgLine:3`＋`dashed`＋空档 → 桥接仍恰 1 条（均线不自带桥接），均线保持 `stroke-dasharray="6 5"` 灰虚线。
- P3 双空档：桥接 `d` 含 2 个 `M`，实线 3 个 `M`。
- P4 零≠空：`[0,null,0]`＋`dashed` → 点 `data-i=0,2`（零值画点、空档不画点），桥接端点落在实测零值点坐标上，无补 0 造点。
- P5 `smooth`／`step`×`dashed`：桥接几何与折线档逐字一致（桥接只用实测点坐标）。
- 探针脚本与逐行输出：`.scratch/t458r/probe-458r.mjs`、`.scratch/t458r/probe-result.txt`。

## 范围核对（引入／范围／范围外）

- 提交文件恰 4 个（`git show 0076deff --name-only`）：`src/spec/charts.ts`、`src/charts.ts`、`test/charts-gap-458.test.mjs`、证据件。`contract.ts`、`spec/index.ts`、`style.ts`、`blocks.ts`、`skill-calorie` 全系零触碰——与证据件 §7 一致。
- “契约三处”独立核过：`contract.ts` 内 `chart|gapStyle|connectNulls` 零命中（渲染契约确无图表字段）；`spec/index.ts` 冻结面登记到方法级签名（`ChartsApi` 八字段形状），加可选字段不改变任一签名串；`contract-signatures` 全绿佐证。未改两处属有据成立，不记缺陷。
- 范围外（本票未做、证据件已显式声明）：26＋2 处调用点零迁移；图下口径行文案未定；调用点归属票未填。均为票体“遗留出口”事项，不属实施提交缺陷，转编排者另切票。

## 缺陷清单

- 阻塞缺陷：无。
- 观察项（非缺陷）：新测文件名 `charts-gap-458.test.mjs` 与票体占位名 `t<票号>-gap-style.test.mjs` 字面不同，属占位符实例化，不影响验收命令语义；`dashed` 整线虚线序列叠 `gapStyle:'dashed'` 时桥接纹样（5 4）与整线纹样（6 5）并存，票体无此组合口径，属范围外，不拦 PASS。

## 路径

- 实施：`packages/base-render/src/spec/charts.ts`、`packages/base-render/src/charts.ts`、`packages/base-render/test/charts-gap-458.test.mjs`、`docs/base/base-render/t458-折线跨空档虚线桥接-证据.md`
- 复核：本件 `docs/base/base-render/t458-复核-独立.md`、`.scratch/t458r/probe-458r.mjs`、`.scratch/t458r/probe-result.txt`
- 本席未 comment、未 close 票，未改源码与他人证据，未提交 git。复核止于此，等指派。
