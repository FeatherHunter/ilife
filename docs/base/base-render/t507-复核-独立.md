# #507 独立复核回执（复核席 · 与实施无交集 · 只交回执）

> 对象：提交 `4aa74f77`（blocks.ts 三处＋help-template.html＋helpShell 派生＋136 测试＋mobile-base-507 测试＋证据 `t507-移动端底座-证据.md`）。
> 本席不采信任何结论，所有读数独立复跑。

## 判定：PASS（改动面）＋ 2 项范围遗留转裁定 ＋ 1 项证据结论订正

五处改动本身与票面一致、机器全绿、无票引入缺陷、无范围外改动。但票面第 ① 条按票面**未闭环**（见 R-1），第 ② 条未做已如实声明（见 R-2），均需编排者／用户裁定，不挡改动面 PASS。

## 机器证据（经 `node tooling/run-locked.mjs --ticket 507 --`）

| 项 | runId | exit | 读数 |
|---|---|---|---|
| 编译 `tsc -b packages/base-render` | `b8f80e66` | 0 | 绿 |
| 新测试 `mobile-base-507.test.mjs` | `3be7cd1e` | 0 | 7/7 绿 |
| 全包 `packages/base-render/test/*.test.mjs` | `cfcaa2ca` | 0 | **724/724 绿**（与证据件一致） |
| 派生 `gen:help-shell:check` | `72a84911` | 0 | 一致（前后缀哈希与 #136 断言逐字对上） |
| 自设探针 `.scratch/t507r/probe-r1.mjs` | `0dc9369d` | 0 | **31/31 绿**（下文） |
| 全仓 `pnpm gen:check` | `0c0ec99a` | 1 | **与本票无关**：红在 `skill-calorie/scripts/gen-routes.mjs:148`（`order 不连续`，他席在途脏树；本票写集外） |

## 自设探针 R1（`.scratch/t507r/probe-r1.mjs`，31/31 绿）

- R1 斑马纹窄屏安全：偶行规则恰 1 条、吃既有 `--bg`、列头无染色、无新 token。
- R2 KPI 帽确定性：`repeat(3,…)` 恰 1、`auto-fit` 基座恰 1、`1024px` 恰 1；`pageUi.ts` 另有 `repeat(4)`；拼接序 `blocksCss()`→`pageUiCss()`（`docPage.ts:126-127`）同权重后胜 ⇒ **启用页 ≥1024 走 repeat(4)，未启用页走三列帽，确定无抖动**（证据 §5 口径成立）。
- R3 减动效：母版归零段覆盖 `.mini/.tab/.copy-btn/.sl-item/.sheet/toast（祖先类）`＋`animation:none`；默认态 `transition` 仍在（只加档、未改默认）；区块小三角归零在位。
- R4 触摸目标未动确认：26／28／30／22／20／30＋ghost40px 七处逐件仍在 ⇒ 实施确未碰触摸目标，范围纪律成立。
- R5 dvh 双高顺序、`R6` 等宽短栈清零长栈 ≥5、M1 双变异负控（纯字串：帽改坏／斑马纹删除均可被检出）。

## 缺陷清单

**票引入：0。** 五处改动逐行核对票面：③归零档（模板 `:1348-1361`＋blocks `:2206-2210`）／④`100vh→100vh+100dvh`（`:160`，兜底在前）／⑤两处短栈→长栈（`:141／:152`）／⑥偶行斑马纹（blocks `:1846-1852`，落既有区、无新类名）／⑦KPI 三列帽（blocks `:1655-1663`，基座不动）。冻结面：11 token 未增、12 区未增、签名面未触发（区块件不在 `SPEC_FROZEN_SURFACE`）。

**范围（需裁定）：2。**

- R-1（① viewport，**证据结论需订正**）：票面要 15 处补 `cover`，实施为 0 处，证据 §1 写“已绿（#525 覆盖）”。独立复核不成立于 HELP 母版：`help-template.html:5`、`helpShell.ts:42`、`help.ts:638` 三处 viewport **仍无 `cover`**，而 `help-template.html` 内 4 处 `env(safe-area-inset-bottom,0)`（`:117／:133／:154／:272`）在 iOS 仍取 0。`#525` 的 `PAGE_UI_VIEWPORT` 只在 `docPage` 的 `pageUi:true` 结果页替换串（`docPage.ts:103／126-127`），HELP 速查台不走 `pageUi`。⇒ ① 按票面未闭环。请裁定：(a) 接受 HELP 页安全区继续取 0，或 (b) 另开一票补 HELP 母版 `cover`；证据“已绿”请订正为“母版未动”。
- R-2（② 触摸目标，同意转另票）：七处小目标未动（R4 实证），票面分级未做；实施未谎称 44px，已如实声明。ghost40px 另有 `#329` 降权重注释在文件内，档位的确需用户裁定后再做 —— 不挡本票。

**范围外：0。** 未见抢做 `#427` 窄屏档／`#429` 字号／跨件等宽常量；`blocks.ts` 第三栈未碰。

**观察：2。** O-1 pageUi 覆盖确定性成立（见 R2）。O-2 `blocks.ts` 超 350 行线：实施已按包规当场报警，本次仅既有区内 ＋15 行不拆件 —— 接受，拆件另票体量。

## 写集

本件 `docs/base/base-render/t507-复核-独立.md` ＋ `.scratch/t507r/probe-r1.mjs`；未碰源码、未碰他人证据、无危险 git、无 push。复核完毕，等指派。
