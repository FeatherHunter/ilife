# #427 真机量框附录（量测席，只量测不改码）

> 源码与他人证据一字未动。本件只记机器读数＋截图路径；肉眼结论留空待用户。
> `.gitignore` 第 5 行：`.scratch/` 永不入库——截图只记绝对路径，不入仓；入仓仅本附录件。

## 0. 量测方法（可重跑）

- 探针页：真实产物 `.scratch/t375/products/看今日主页.html` 原样复制，DOM 逐字节不动，仅整段 `<style>` 换成当前 `dist` 新鲜 CSS（`buildStyleSheet().css`＋`blocksCss()`）。
  旧样式 40129 字 → 新鲜 43269 字；新鲜 CSS 含 `@media (min-width: 641px)` 且全文件无 `@media (min-width: 821px)` 残留。
  （该产物内嵌的是改前 CSS——仍为 821 档；直接量它只能读到旧数 520／780，故必须换新鲜 CSS 再量。）
- 探针页：`D:\ilife\.scratch\t427-shots\probe-427.html`（`.scratch`，不入仓）
- 量框器：`.scratch/t401-samples-review/shot.mjs` 的复制件（口径同源，落本席目录，不越界写他票草稿）
- 浏览器：本机 Chrome 152 真视口（CDP `Emulation.setDeviceMetricsOverride`；不用 `--window-size`，会被 Windows 最小宽度顶回 526）
- 编译：`node node_modules/typescript/bin/tsc -b packages/base-render` exit 0（两次：`runId=e0984df4-f4af-4a19-83f0-0471ffe1fc5b`／`runId=0c2473cb-9c7b-4c37-bd66-3ff47b9be495`，均为 exit 0）
- 探针生成：`runId=5b135b37-4037-4e92-9614-01ffbe517abf` exit 0（首次 `runId=8a34767a-f276-4c4e-8e2e-31f2f2590148` exit 1：生成脚本在 Windows 下直传盘符路径给 ESM import，改用 `pathToFileURL` 后通过；量测口径无影响）

## 1. measure 820（`runId=df36f35b-2c15-4bb6-bcc2-5627001c9a07`，exit 0）

```
VW 820 scrollW 820
shell null
kpiGrid {"x":20,"w":780,"right":800,"h":198} cols=186px 186px 186px 186px
kpiCard1 {"x":20,"w":186,"right":206,"h":198}
tableWrap {"x":20,"w":780,"right":800,"h":391} inner=778
actionBar {"x":20,"w":780,"right":800,"h":44}
ghostRow {"x":20,"w":780,"right":800,"h":44} cols=386px 386px
  btn0 {"x":20,"w":386,"right":406,"h":44}
  btn1 {"x":414,"w":386,"right":800,"h":44}
chartSvg {"x":35,"w":750,"right":785,"h":336}
nav {"x":20,"w":780,"right":800,"h":44}
overflowing(0):
```

机器结论：`.ilife-action-bar` w=780，与页身内容列同宽（kpiGrid／tableWrap／nav 同为 x=20 w=780 right=800）——**820 两宽相等**；scrollW 820＝VW 820。

## 2. measure 390（`runId=3108c987-e113-42ae-9d7b-e635db6fcfd8`，exit 0）

```
VW 390 scrollW 390
shell null
kpiGrid {"x":16,"w":358,"right":374,"h":673} cols=358px
kpiCard1 {"x":16,"w":358,"right":374,"h":159}
tableWrap {"x":16,"w":358,"right":374,"h":705} inner=356
actionBar {"x":16,"w":358,"right":374,"h":96}
ghostRow {"x":16,"w":358,"right":374,"h":96} cols=358px
  btn0 {"x":16,"w":358,"right":374,"h":44}
  btn1 {"x":16,"w":358,"right":374,"h":44}
chartSvg {"x":31,"w":328,"right":359,"h":147}
nav {"x":16,"w":358,"right":374,"h":44}
overflowing(0):
```

机器结论：ghost `cols=358px` 单列，两颗按钮各占整行上下排列（h=96＝44＋8＋44）；KPI `cols=358px` 单列；
`scrollWidth 390＝clientWidth 390` 且 `overflowing(0)`——**390 两处单列、无横向溢出**。

## 3. 截图（三张，均 `.scratch` 只记绝对路径，不入仓）

| 视口 | 绝对路径 | 大小 | 文档宽／高（shot 日志） | runId |
|---|---|---|---|---|
| 820×1200 | `D:\ilife\.scratch\t427-shots\probe-820.png` | 65401 B | 820／1569 | `6d48b632-b87d-4481-bc44-25e2958d0a40` |
| 390×844 | `D:\ilife\.scratch\t427-shots\probe-390.png` | 34628 B | 390／2181 | `a1a6e70d-0847-4c58-8a12-b6c0f19d82a8` |
| 1920×1080 | `D:\ilife\.scratch\t427-shots\probe-1920.png` | 65890 B | 1920／1782 | `f33fa4bc-6325-4947-aa71-536da5a11bf8` |

备注：`shot.mjs` 的 `shot` 分支按执行目录写出，三图曾落在仓根，已持锁移入 `.scratch/t427-shots/`（`runId=61027c95-12ff-46e0-800e-a96c441fc29e`），仓根无残留。三图仅做“文件有效可读”确认。

## 4. 肉眼结论（待用户，量测席留空）

- 820 右侧不空：＿（待用户）
- 390 无横向溢出、无折行叠字：＿（待用户）
- 1920 对照与修正前无差：＿（待用户）
