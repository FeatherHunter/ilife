# t397 独立复核（只复核，不改实现，不关票）

- 复核者：MAP#158 #397 独立复核席；对象为作者三提交 `015e8887`（实现）/`b68f1a9a`（测试）/`f06fa712`（证据+changeset），未 push。
- 基准：`docs/skills/skill-calorie/t395-融合基准.md` §四裁定 1/3 与 §五-1；票面 `gh issue view 397`；作者证据 `docs/base/base-render/t397-只读与步进约束-证据.md`（只读）。
- 现场：自建 `.scratch/t397-review/` 独占目录（路径守卫：root 内、非禁区、未读/未拷作者 `.scratch/t397/byte-probe.mjs`，只看了测试文件后自写探针）；长命令落盘该目录（`build.log`/`blocks.log`/`byte.log`/`newprobe.log`/`mut-*.log`/`retest-*.log`/`basedir.log`），正文只引尾 5 行。
- 锁：全部闸门经 `node tooling/run-locked.mjs --ticket 397 --poll-ms 15000 --max-wait-ms 600000` 持锁跑；`gate-runs.log` 以 `t397-review-*` runId 对账。拥塞时 waitedMs 达 240011（见 §三），未报超时锁丢失。

## 一、提交范围（逐个 `git show --name-only`）

- `015e8887 feat(397): 参数表单加只读与步进约束` → 仅 `packages/base-render/src/blocks.ts`（`--stat` 74+/2-，1 file）。
- `b68f1a9a feat(397): 只读与步进约束5条新单测` → 仅 `packages/base-render/test/blocks.test.mjs`（44+，1 file，纯追加）。
- `f06fa712 feat(397): 证据与对账导出及changeset` → 仅 `.changeset/t397-param-constraints.md` + `docs/base/base-render/t397-只读与步进约束-证据.md` + `docs/base/base-render/t397-gate-runs.md`（3 files）。
- 三提交均无 `routes.generated.ts`，无 `packages/skill-calorie/**` 改动；中文信息无乱码（`git log --oneline` 三行中文标点正常）。
- 边界：复核写件时 `git diff HEAD -- packages/base-render/src/blocks.ts` 见工作区另有他席 `#421` 约 279 行未提交 hunk（占比条/分布条/徽章/变更行，`pageLevelPart/reqPct` 等），与本票已入库 74 行 hunk 分属不同段；复核未动该文件（除 §四变异临时改后已按回滚点复原）。

## 二、靶向门（持锁重跑，不采信作者结论）

- `t397-review-blocks`：`node --test packages/base-render/test/blocks.test.mjs` exit=0，`pass 46/fail 0`（`blocks.log:86-87`），与作者 `46/0` 一致。`it` 计数 46（含新增 5）。
- 自写最小探针 `t397-review-byte`（`byte-probe-review.mjs`，23 断言）：readonly 落裸 `readonly` 且非 `readonly="`、缺省无；`step="0.1"/min="0"/max="100"` + `type="number"`；`options` 出 `<select` 且三候选 `home_caliper/gym/hospital` 齐、`gym selected`、零 `<script`；`step:'abc'` 与 `min:'10',max:'5'` 抛 `code=bad-input`；旧调用无 `readonly/step=/min=/max=/<select/type="number"`，含 `placeholder="H"`/`data-required="1"`、无 `<script`，且手算旧 input 串 `<input class="ilife-block-param-form-input" name="n" value="v" placeholder="H" data-required="1" required />` 逐字节 `includes` + 两次调用 `===`。读数 `BYTE-PROBE-REVIEW: pass=23 fail=0` exit=0。
- 结构抽查：旧五字段语义产物不变（上条）；既有断言 `325-338` 与 `015e8887^` 对比逐行 `EQUAL`；`assertNoInlineHandler` 仍在 `renderParamForm` 入口与逐字段校验，无 `onclick/oninput/<script` 新增（`options` 逐项 `esc`）；新增对外字段恰 5 个（`readonly/step/min/max/options`），符合“接口对外不超过五个”。

## 三、GATE-RUN（对账源 `.scratch/locks/gate-runs.log`）

- `t397-review-build` `tsc -b packages/base-render` exit=0 waitedMs=0。
- `t397-review-blocks` `blocks.test.mjs` exit=0（46/0）waitedMs=0。
- `t397-review-byte` 自写 byte 探针 exit=0（23/0）waitedMs=0。
- `t397-review-newprobe` 自写新探针 exit=0（13/0）waitedMs=15002（拥塞一次）。
- `t397-review-mut-build` 变异态重编 exit=0；`t397-review-mut-test2` 变异态新探针 exit=1（必红，见 §四）；`t397-review-restore-build` 还原重编 exit=0 waitedMs=90006；`t397-review-retest` 新探针复绿 exit=0（13/0）；`t397-review-retest-blocks` 复绿 exit=0（46/0）。
- `t397-review-charts` `charts.test.mjs` exit=0（88/0）waitedMs=210011。
- `t397-review-wiz86` `wizard-86.test.mjs` exit=1（0/1，加载期 `SyntaxError`，见 §五）waitedMs=165010。
- `t397-review-basedir2` base-render 9 套件 exit=0（465/0）waitedMs=240011。作者 `t397-basedir2` 曾记 462/2（2 红在 charts）；本轮重跑已全绿，差值系他席 `#424` 在途件收敛 + 本轮 `tsc -b` 刷新 dist 所致（见 §五）。

## 四、自设新变异/探针（作者未覆盖）

- 新探针 `new-probe-review.mjs`（13 断言，作者未覆盖）：R1-1 `readonly+options` 同场→`select` 标签落 `disabled`（`select` 无 `readonly`，取 `selTag` 精确判，不被 hint 占位 `disabled` 误导）+ hint 化作 `<option value="" disabled selected>`；R1-2 `step=0/-0.5/'0'` 必拦、`min==max` 放行；R1-3 `options+step/min` 互斥必拦；R1-4 候选 `<a>&"` 转义（无 `<a>`、有 `&lt;a&gt;`）。
- 变异：持锁期间将 `blocks.ts:668` `(readonly ? ' disabled' : '')` 改为 `(readonly ? '' : '')`（回滚点：`HEAD=c84544eb…` + `blocks.ts SHA256=FE86B747…`，记 `.scratch/t397-review/rollback.txt`；释放前已复原，复原后 hash 一致、`git diff HEAD -- blocks.ts` 仅剩他席 #421 段）。
- 两行机器读数：`MUT-VARIANT-RED: runId=t397-review-mut-test2 exit=1 AssertionError R1-1 select 标签落 disabled`；`MUT-RESTORE-GREEN: runId=t397-review-retest exit=0 NEW-PROBE-REVIEW: pass=13 fail=0` + `runId=t397-review-retest-blocks exit=0 pass 46/fail 0`。

## 五、归属核验（不假绿）

- charts：本票三提交未碰 `charts.ts/charts.test.mjs`（§一）；工作区该两文件 `diff --stat` 127+/21- 属他席 `#424`（viewBox 580×260、抽稀、均线共存）。作者期 2 红（`A.height/width`、`B.avgLine`）为 `#424` 在途 + stale dist；本轮持锁单跑 88/0、9 套件 465/0，红点已收敛，归因不变。
- 卡路里 wizard：`wizard-86.test.mjs` 9 项（`test(` 计数 9）本轮持锁跑 exit=1，但败因是加载期 `SyntaxError: Identifier 'weighed' has already been declared at dist/analysis/multiTrendPage.js:360`，源文件为未入库新件 `packages/skill-calorie/src/analysis/multiTrendPage.ts`（`??`，两处 `const weighed` :230/:369），与本票声明路径无关；本票未碰技能侧，故“不受本票破坏”成立，但“9 项全绿”在本工作区不可复现（范围外阻塞，见 §六 S3）。
- 未跑：全仓 `pnpm test`（多席在途）与 `snapshot:html:check`（同作者口径，留 #366 联调）。

## 六、缺陷清单 + 下一手

- 本票引入：无（靶向 46/0、自写 23+13 全绿、变异两行符合预期、结构四项全合）。
- 本票范围：无欠账；遗留出口照作者 §四（皮褶 `0.1/0/100`、围度逐部位、体脂率 `0.01/0/60`、只读 + 文案、`source/sex` options），归 #366。
- 范围外发现（只 S3 + 转票措辞，不挡本票）：S3-1 `multiTrendPage.ts` 未入库新件重复声明致 `wizard-86` 无法加载，建议转票“体成分多趋势页新件去重（`weighed` :230/:369 二选一）+ 重跑 wizard-86 9 项”；S3-2 工作区 `#421` 大 hunk 与 `#424` 图表在途件并存，建议编排者统一收口全仓门。本复核不关票。
