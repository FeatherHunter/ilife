# t441 独立复核（复核席，与实施无交集）

判定：**FAIL（不通过）**——四绿可复现、注释零逻辑改动属实，但 §2 迁移提示的“显式传色 3 处”少计 1 处，同数已写进源码注释，不可按原样关票。

写集：本件 `docs/base/base-render/t441-复核-独立.md` ＋ `.scratch/t441r/` 下探针与计数脚本。
未碰源码、未碰他席路径与他票证据件（含 `t441-S3小修.md` 原件、`t431-四件判据补硬.md` 原件），未做切分支／reset／stash／clean／restore／amend／rebase／push。
编译与测试一律经 `node tooling/run-locked.mjs --ticket 441 -- <命令>`。
取字节口径：复核摘要一律用 Node `execFileSync` 取原始字节，不用 PowerShell 管道转写字节（与 `page-finish-420.test.mjs:176-182` 同口径）。

## 1. 独立复跑四绿（逐条经 run-locked，终态）

| 命令 | runId | exit | 读数 |
| --- | --- | --- | --- |
| `node node_modules/typescript/bin/tsc -b packages/base-render` | `17d10333-4843-4e26-bc06-4967484e8b2b` | 0 | 无输出（绿） |
| `node --test packages/base-render/test/page-viz-421.test.mjs` | `44fb48ca-1a49-442a-b14b-8bddda1dbd46` | 0 | tests 27 pass 27 fail 0 |
| `node --test packages/base-render/test/page-finish-420.test.mjs` | `1a20c305-96a7-4e08-aeb6-0a3d2e50a6bb` | 0 | tests 21 pass 21 fail 0 |
| `node --test packages/base-render/test/blocks.test.mjs` | `1cd9bcb2-ba08-47fe-8f54-b0faba2db49b` | 0 | tests 50 pass 50 fail 0 |

审计行在 `.scratch/locks/gate-runs.log`（`RUN ticket=441 runId=… exit=0` 逐条可查；本席另有探针与计数行，见 §3 与读数行）。

结论：实施回执声称的每次绿均可独立复现，计数一致（27／21／50）。

## 2. 五条逐条核验

① 空态注释四处：属实。`61e7b888` 的 `blocks.ts` 差分只动注释（`+10／-5`，见 `git show 61e7b888 -- packages/base-render/src/blocks.ts`），
落点 `251-253／376-383（迁移提示，见②）／461-463／499／531-533`。
实现侧非数组即拒四行仍在：`258`（Toc `items`）、`467`（分布 `rows`）、`504`（徽章 `items`）、`536`（变更 `rows`），
空数组返空串四行仍在：`259／468／505／537`。注释“缺失（`undefined`）→ `bad-input`（非数组即拒）”与实现一致（`undefined` 落 `!Array.isArray` 分支）。

② 允许清单迁移提示：**计数不实（见缺陷 D1）**，色值形态结论不受影响。
`nutritionPortDocs.ts:219-221` 三色 `#5856d6／#34c759／#ff9500` 经 `339` 行 `m.color` 传入、`sportPortDocs.ts:609／775` 经 `colorOf()` 取
`categoryColors.ts:22-27` 四色 `#5856d6／#0071e3／#34c759／#ff9500`，以上均为 `#rrggbb`、属允许清单，核对无误；
`homeViewParts.ts:101`、`body/wizardDocs.ts:118-126`、`render/trendPredictDocs.ts:97`、
`body/compareCompositionDoc.ts:45-51`、`body/compareMeasureDoc.ts:31-40`、`render/sportDocs.ts:278-284` 均不传 `color`，走缺省冻结 token，核对无误；
排除项核对无误：`weight/volatilityDoc.ts:257` 的 `#ff3b30` 进图表 `markLine`（图表口径，不进 `optColor`），
`photo/receipt.ts:56-59` 的三色只消费 `.badge`（`294／337／352` 行），`.color` 当刻无消费方。
但遗漏第 4 处（D1）：`diet/reviewDocs.ts:339-341` 经 `MEAL_COLORS`（`:304` 四色 `#ff9500／#0071e3／#5856d6／#34c759`）显式传色，
经 `diet/todayDocs.ts:242` 由 `buildMealDistributionBlock` 进入生产（两处 blame 均早于本票：`68332d52 2026-09-15` 与 `0957bc8f 2026-09-15`），
回执“3 处”与源码注释“3 处（`blocks.ts:380-381`）”均少计 1 处。

③ 二选一结论：属实且在写集内。“只作回归基线、不作权威溯源、不补断言”记于 `t441-S3小修.md §3`；
`61e7b888` 写集仅两件（`docs/base/base-render/t441-S3小修.md +106`、`packages/base-render/src/blocks.ts +10／-5`），判据件与夹具零改动。

④ `t431` §0 矛盾句：描述属实，处置合规。原件 §0 第 3 行写面 `+67／-8` 与第 6 行“本席不动实现方向”并读易误解、
第 10 行“实现一行未改”的主语是本席（收官席）而非本票；纠正句“本席（收官席）未动实现方向；本票实现改动见 §2”只记于本票 `§4`，
`61e7b888 --name-only` 证实 `t431-四件判据补硬.md` 原件一字未动，请所属席落字的要求合理。

⑤ 取字节口径：属实。`page-finish-420.test.mjs:176-181` 以 `execFileSync('git', ['show', …])` 返回字节直接做 `sha256`；
口径句“不用 PowerShell 管道加 `Out-File utf8NoBOM` 的转写字节”与 #428 坑一致。本席本件取字节均用 Node 原始字节。

结构第四步：`packages/base-render/src/blocks.ts` 当刻 LF＝2309（原始字节计数，runId `c214ab80-1d9e-45bb-a221-e72156889285` exit 0；
HEAD 版同值，父版 `61e7b888^` 为 2304，净 +5 与 `+10／-5` 对得上）。已超线（告警线 350），需要根据规则进行重构。
超因与拆法沿用 `docs/base/base-render/行数告警线评估.md`，本票只动注释、本席只复核，均先不拆。回执记“2306”与实测 2309／2304 对不上（见 D2），“超线”结论不受影响。

## 3. 新探针 R1（其脚本覆盖不到，5/5 绿）

探针：`.scratch/t441r/probe-r1.mjs`，经 run-locked：runId `17ac394a-83dd-479c-a772-aeb0ae4499d1` exit 0，`R1 ALL GREEN 5/5`。

| 探针 | 覆盖缺口 | 结果 |
| --- | --- | --- |
| R1a `renderTocBlock({items:undefined}) → bad-input` | “空态口径写死”只钉分布／徽章／变更三件，未钉 Toc 显式 `undefined`（`page-finish-420` 只钉 `{}` 缺项与 `'x'` 非数组） | 绿（抛 `bad-input`） |
| R1b `renderTocBlock({items:null}) → bad-input` | 同上，`null` 路径亦无点名断言 | 绿 |
| R1c `renderMiniBar({pct:42,color:null}) → bad-input` | 色值断言只钉 `''` 与 `7`，未钉 `null` | 绿 |
| R1d `renderMiniBar({pct:42,color:'#FF9500'})` 逐字透传 | 透传断言只钉小写 `#ff0000`，实现正则含 `A-F` 但无大写用例 | 绿（产物含 `#FF9500`） |
| R1e `MEAL_COLORS` 四色经 `renderDistributionRows` 逐字透传 | 第 4 处生产色值（D1）在本票审计外，无对应回归断言 | 绿（四色均透传，属允许清单） |

未做源码变异：复核写集禁改源码，故以只读探针代替改坏／改回两行；R1a–R1d 均为现有断言未点名的路径，R1e 为本次审计遗漏点的允许性自证。

## 4. 缺陷清单

D1（票引入，证据与注释计数错误，建议返修后重关）：`t441-S3小修.md §2`“显式传色生产调用 3 处”与
`packages/base-render/src/blocks.ts:380-381` 迁移提示“显式传色 3 处”均少计 1 处。
实为 4 处：第 4 处 `packages/skill-calorie/src/diet/reviewDocs.ts:339-341`（色表 `:304`，调用 `diet/todayDocs.ts:242`）。
功能影响：无——第 4 处四色全为 `#rrggbb`（R1e 自证透传），“均属允许形态、无须改动”的定性成立，只需把两处“3”改为“4”并补第 4 处行号。
返修写集建议：改 `blocks.ts` 该注释两行数字＋补行号（源码席），在 `t441-S3小修.md §2` 补审计第 4 行（所属席），复核席不代笔。

D2（票引入，轻，台账数字瑕疵）：`t441-S3小修.md §0` 记 `blocks.ts`“当刻 2306 行”，
Node 原始字节实测 HEAD＝2309、父版＝2304（runId `627cd3dc-35e7-4ae9-8db1-ff0d97f24e7a` exit 0），2306 与两者皆不合。
“已超线先不拆”的结论不受影响，改法为落字时按实测改数字即可。

范围外（ pre-existing，非本票引入，不拦关票）：Toc 显式 `undefined／null` 无点名断言（`page-viz-421:433-440` 只钉三件，`page-finish-420:256-260` 只钉缺项与非数组）。
实现行为正确（R1a／R1b 绿），本票注释把该口径写全是对的；是否补断言属判据件所属席的范围，请另开票认领，本席探针已留 `.scratch/t441r/probe-r1.mjs` 可直接转写。

## 5. 路径

- 复核件：`docs/base/base-render/t441-复核-独立.md`（本件）
- 探针：`.scratch/t441r/probe-r1.mjs`（R1a–R1e，绿）
- 计数脚本：`.scratch/t441r/count-lf.cjs`、`.scratch/t441r/count-head.cjs`、`.scratch/t441r/show-gate-log.cjs`
- 被核件（只读，未改）：`packages/base-render/src/blocks.ts`、`docs/base/base-render/t441-S3小修.md`、
  `docs/base/base-render/t431-四件判据补硬.md`（§0 第 3／6／10 行）、`packages/base-render/test/page-viz-421.test.mjs`（`:433-440`）、
  `packages/base-render/test/page-finish-420.test.mjs`（`:176-181`、` :231-261`）
