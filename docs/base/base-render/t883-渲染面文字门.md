# t883 · 渲染面文字门实施证据

> 票：[#883](https://github.com/FeatherHunter/ilife/issues/883)（Part of [#820](https://github.com/FeatherHunter/ilife/issues/820)，`#883 ← #878`）。
> 落点：`packages/base-render/scripts/check-rendered-text.mjs`（公共层，占串行窗口）。
> 本件不改任何产物代码、不碰 `src/**`、不改 t869 与分隔符门（只读复用 `visibleText`）。

## §一 一句话

同一套 ⑤⑥ 规则跑两个面：静态面（与 t869 同语义）与渲染面（真浏览器跑完页内脚本后的 DOM＋伪元素 content），逐页差表，差项逐条点名，0／1／2。只对差项判红（静态既有债归原门）。

## §二 口径（与静态面逐字对齐，只补面不扩语义）

- ⑤ 分隔符懒政：`[·|｜]` 落版式位／正文位即红；标题写法位照打；数据位／载荷位豁免。出处 t869 SEP_CHARS 与位置四分。
- ⑥ 英文裸词与半角标点：ALLOW 四条（日期／时间／`#N`／AI）＋ASCII＋HALF 半角集（含小数豁免）。出处 t869。
- 位置名册初值取 t869（备忘录域重写）；渲染面新增装饰位 `status-icon`（放过但照打，理由在位上）。出处 #878 评论＋Q2 裁定。
- 渲染取串：隐藏子树剪除（`display:none`／`visibility`／`hidden`，单遍可见性表）、伪元素 `::before`／`::after` 的 content（`attr()` 就地解析、`\B7 ` 形转义解码、无类名宿主向上找 6 步）。
- 静态取串：样式脚本 → `data-t` 载荷 → 注释剥离后分段，归属＝包住该偏移的最内带 class 元素（标签栈纪律； naive"最近开标签"会在已闭合元素上串位，已修，见 §五）。
- CDP 驱动与 `check-two-col-align.mjs` 同形（第二处用法；第三处出现即抽 `scripts/cdp-page.mjs`）。

## §三 验收三跑（实测命令与读数）

### 正例（新墙，34 件当前产物，exit 0）

#880 落地后重铺的新墙（`packages/skill-memo-ilife/.scratch/t834/源`，34/34 真跑）：

```
node packages/base-render/scripts/check-rendered-text.mjs --dir packages/skill-memo-ilife/.scratch/t834/源
→ RESULT: 34/34 CLEAN ｜静态⑤0⑥0 → 渲染⑤0⑥0 差0 读数失败0（exit 0）
```

占位覆盖加上后重跑同一命令 → 同样 `34/34 CLEAN … 差0`（exit 0）：新墙占位已全角化，无误伤。

### 正例对照（旧墙，38 件修前产物：门必须红，否则门就是摆设）

旧墙（`.scratch/t834/验收`，#880 修前）全批 → `36/38 CLEAN … 差6`（exit 1）。
差 6 处逐条（`batch.json` 的 `diffs`）：

| 页 | 串 | 列 |
|---|---|---|
| 备忘改分类-批量-向导.html（31 格） | `从「备忘」改到「目标分类」(生成时间 …)` | ⑥半角 |
| 完成心愿-向导.html（32 格） | `没有飞书任务,只在本机完成` | ⑥半角 |
| 完成心愿-向导.html（32 格） | `打卡内容(覆盖)` ×4 | ⑥半角 |

归因铁证：#880 正文逐字点名了同串同码位（31 格 U+0028／U+0029、32 格半角逗号 U+002C、
半角括号 ×4），且当时 #880 未关——故 6 处是"已知的真命中"，符合回归条款。
#880 落地（`6c93f2ea` 5 处全角化）后旧墙即历史，对应新墙读数见上一节。

- 三处 #878 回归位（29／32／33 格）渲染 `·` 计数全 0 且逐页点名在表内。
- 名单外 4 件（墙／索引／链路）静态命中是其本来样子，渲染差 0。
- 结论：门行为正确——旧墙红（真债）、新墙绿（修后），不为 exit 0 放水（放水即重造假绿）。

### 变异（五盲点各一处，逐条点名 exit 1）

```
--dir packages/skill-memo-ilife/.scratch/883-triage/mut
→ RESULT: 0/5 CLEAN ｜静态⑤0⑥0 → 渲染⑤3⑥2 差5 读数失败0（exit 1）
  ✗ mut-css.html ::before 正文位<dot>「·」［⑤］
  ✗ mut-js.html #1 版式位<small>「生成时间 10:04 · 场景 同步」［⑤］
  ✗ mut-half.html #1 版式位<hint>「显示全部 (50 条)」［⑥半角］
  ✗ mut-truth.html #2 版式位<small>「已展开全部 · 滚到底可看完」［⑤］
  ✗ mut-attr.html placeholder 正文位<filter>「如:今天完成了 / 2026-07-03 已打卡」［⑥半角］
```

```
--dir packages/skill-memo-ilife/.scratch/883-triage/mut
→ RESULT: 0/4 CLEAN ｜静态⑤0⑥0 → 渲染⑤3⑥1 差4 读数失败0（exit 1）
  ✗ mut-css.html ::before 正文位<dot>「·」［⑤］
  ✗ mut-js.html #1 版式位<small>「生成时间 10:04 · 场景 同步」［⑤］
  ✗ mut-half.html #1 版式位<hint>「显示全部 (50 条)」［⑥半角］
  ✗ mut-truth.html #2 版式位<small>「已展开全部 · 滚到底可看完」［⑤］
```

静态面四件全 0（盲点复现）→ 渲染面四件全红（补齐实证）。其中 `mut-truth.html`
是 Q2 真债原串（`memo_query` 的运行时提示，bar 可见态）：静态 0 → 渲染红，
且证明隐藏剪除不误伤可见的运行时文本。

### 还原对照

`记备忘.html` 单页 → `1/1 CLEAN … 差0`（exit 0）。

### 回归（既有读数不变）

- t869 同批目录：`34/34 PASS ①0 ②0 ③0 ④0 ⑤0 ⑥0 缺件0`（与 #834 逐字相同）。
- 分隔符门同批目录：34 件产物全绿；命中的 4 件是名单外文件（墙／索引／链路，t869 本来就不审），改动前即如此（本件未碰该门）。
- 包内相关三件：`check-rendered-text-883`＋`separator-guard`＋`two-col-batch-884` → 35／35 绿。
- 全量套件：`node --test packages/base-render/test/*.test.mjs` → 803／804（唯一红 `copy-dual-336`「成功回执双位」1 项，单跑复现 7／8；复制双按钮逻辑，与本件零交集——HEAD 既有红，另票处置，本件不碰）。

## §四 单元测试（无浏览器可跑，19 项）

`packages/base-render/test/check-rendered-text-883.test.mjs`：位置四分 5、单串判据 3、CSS 解码 2、取串装配 2、差集 3（含同文异列不互吞）＋占位规则 1、读盘入口 1、用法错 2。19／19 绿。

## §五 修掉的两个实现缺陷（TDD 循环内）

1. 归属串位：naive"最近带 class 开标签"把已闭合 `span.status-icon` 的类名算到后面兄弟 `h1` 上 → 按 t869 标签栈纪律重写 `ownerAt`（开闭配对），旧用例即正控。
2. `readFileSync` 漏 import：单元测试不走 `main()` 没爆 → 把静态读盘抽成 `auditStaticFile()` 出口并加用例（main 与测试共用同一入口）。

## §六 交回

- 本票 historically 抓到的 6 处旧墙差已由 #880 修完（`6c93f2ea` 5 处全角化），新墙 34/34 回绿即互证；#880 自身 95% 待确认是它自己的事，本票不跟。
- 口径外字符另开票，不夹带（Q1 裁定）。
- 交互态局限（v1 已知边界）：门量默认渲染面；要点开才上屏的状态要交互驱动，另开票。
- `copy-dual-336` 的 1 项红是 HEAD 既有（复制双按钮逻辑，零交集），另票处置。
- 跨技能复用本门需按域重写名册（现名册是备忘录域）；第二个使用者出现时再抽共用，现在不预设计。

## §八 对抗式审查结论（维护者授权自裁，关票前最后一轮）

- Q①（正例读法）被事实消解：旧墙差 6（#880 修前真债）＋新墙 34/34 exit 0（#880 修后），门两面证对，无需"接受读法"，直接看数。
- Q②（评审 3 反驳）成立，维持（§七）。
- 自裁新增工作（本轮已做）：#880 全文逐条对账（5 处半角债各有归宿：3 类进本门差项、占位 2 处逼出本门占位覆盖、2 处 `·` 归 #878 已修）；占位覆盖已入件＋变异（`mut-attr`）；全量 805/806（唯一红既有）。
- 关票判据：单元 19／19、变异 0/5 全点名、新墙 34/34 exit 0、t869 逐字不变、包全量无新增红。五项齐 → 可关。
- `copy-dual-336` 的 1 项红是 HEAD 既有（复制双按钮逻辑，零交集），另票处置。
- 待确认（95%）：§三正例的"全批 exit 1 差 6 全归 #880"读法＋干净 corpus 36/36 exit 0；确认后 100% 关票。

## §七 双轴评审收口（implement 规则，提交 a2d996de 后）

- Standards 轴：零 hard 违规；两处改名已采纳（`st`／`rd` → `statRes`／`rendRes`、`staticOnly` → `staticOnlyCount`）；其余 smells 皆 judgement call（口径复写已标出处、CDP 同形第三处抽取、测试复用 export 非包门扩张）。
- Spec 轴 5 项处置：
  1. 正例 exit 0 → 已消解：新墙 34/34 exit 0（§三）；旧墙差 6 是 #880 修前真债（§八）。
  2. 新增越界 → 反驳成立：6 处与 #880 正文同串同码位，属"已知的真命中"，非越界。
  3. 真债未演示 → 已补 `mut-truth.html`（原串可见态，静态 0 → 渲染红）；默认隐藏态属交互态局限（见 §六）。
  4. 复用落空 → 反驳成立：口径抽取自 t834（同形），驱动不用 `--dump-dom` 是因为串行化 DOM 天然无伪元素文本（面①）；`visibleText` 实复用。件头已写明。
  5. 名单手抄 → 反驳成立：票面要的是"任一页群"，无名单需求；位置名册入件是票面＋Q2 原话要求。
  另修 2 处：差键含列（同文异列不互吞）＋⑥含标题写法位的件头澄清＋CDP 孤儿兜底（自查）。
