# #547 表格 caption 窄屏溢出 · 证据

## 根因（390 档真渲，修前）

- `看今日主页.html` 表格容器 `scrollWidth 376 ＞ clientWidth 356`（溢出 20px）；表本体 356、行 356、`td` 已 flex 卡化，均正常。
- 撑开的是 `<caption>`：窄屏段 `display:block;width:100%`（`blocks.ts` 数据表窄屏段）＋内距左右各 10px；结果页无全局 `border-box`（`pageUi.ts:173` 注释），`width:100%` 只管内容宽，实占 356＋20。
- 旧墙对照：主页表 @390 为 X，饮食页两表 @390 双 X；导航轨（358＝358）无溢出，已排除。

## 改动（一处声明）

`packages/base-render/src/blocks.ts` 窄屏 caption 规则加 `box-sizing:border-box`（＋4 行：3 行注释＋1 行声明）。
不新增规则（`ui-fix-154` 守卫按选择器计条）、不新增断点、桌面档（≥641）不在本段。

## 改后读数（`.scratch/measure-9.mjs`，9 页 × 双档）

390 档：9 页 `docX` 全 false，7 张表容器全 ok（含饮食长 caption 两表、运动两表、体重、目标、主页两表；`week`／`month`／`budget` 三档无表）。
1000 档列宽与修前逐值相同（同内容比）：主页 `116.13／281.94／281.94`、运动逐日 `100.25／579.75`、
体重 `100.25／380.95／198.8`、饮食第一表与运动类型表亦同。饮食第二表新旧列宽不同是取数侧在途改动所致
（桌面段本票一字未动，不可能出自本票，见遗留出口）。

## 变异两行

- 拿掉该声明重建：`table-caption-box-547` 2/3 红（点名缺 `box-sizing`）。
- 还原重建：3/3 绿。

## 相关测试

`table-caption-box-547`＋`table-mobile-t541`＋`table-width-512`＋`rowcard-caliber-t154r3`＋
`ui-fix-154`＋`chart-top-table-measure-512`：74/74 绿。

## 复跑

```
npx tsc -b packages/base-render packages/skill-calorie
node --test packages/base-render/test/table-caption-box-547.test.mjs
node .scratch/measure-9.mjs   # 需 Chrome，读数口径见文件头
```
