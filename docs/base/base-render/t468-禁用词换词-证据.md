# t468 禁用词换词证据（base-render）

结论：`packages/base-render/src` 手写面禁用词清零（壳 41→5、白名单 8→1、渲染层/底座/基座/落地/收敛全归零）；
余量均为保留类（§2），逐条有理由；skill-bill render／triggers 同批换词经生成器链路落地；
消费方产物改前改后各一行（§3），双套件全绿（§4），变异红绿各一行（§5）。

## 1. 影响清单与交付对账（结构纪律第一步／第五步）

只换文案字面量，不改行为与结构；未新增目录与导出，行数零增减。

| 文件 | 一句话 |
|---|---|
| `src/blocks.ts` | 空壳→空块、页壳/页面壳→页面模板、基座→基础、落地→实施、收敛→合并（注释） |
| `src/charts.ts` | 落地→实施、底座→机制（t507 文件名引用改泛指，见 R6）、收敛→统一、旧壳/老壳→旧help模板/老help模板（注释） |
| `src/controls.ts` | 落地→实施、白名单→允许清单、HELP 壳→HELP模板、真实壳→真实help模板、冻结壳→冻结help模板（含内联产物注释 932） |
| `src/help.ts` | 壳→help模板/HELP模板、白名单→允许清单（含运行时 `不在 enum` 串，测试正则仍命中） |
| `src/index.ts` | HELP 壳→HELP模板、内置壳模板→内置help模板（注释） |
| `src/injector.ts` | 落地→实施（注释） |
| `src/font.ts` | 基座→基础（注释） |
| `src/style.ts` | 基座→基础（含运行时 `extraCss` 串，测试不断言该串）、落地→实施（注释） |
| `src/output/saveHtml.ts` | 相同的壳→删名保留义（注释） |
| `src/pageShapes.ts` | 空壳→空块（注释） |
| `src/pageUi.ts` | 页壳/壳→页面模板（含产物 CSS 注释串，仅注释） |
| `src/spec/*` | 壳→help模板/HELP模板、白名单→允许清单、渲染层→页面交付、落地→实施、收敛→统一（注释） |
| skill-bill `src/render/*` | 渲染层→页面交付（4 件首行）、壳→help模板（helpFile/html 注释） |
| skill-bill `scripts/gen-wake-assets.mjs` | 头注逐字落地→逐字实施、技能自带壳→技能自带help模板（生成器事实面） |
| skill-bill `src/triggers/wake-assets.ts` | 机器重跑，仅头注 2 行变（diff 2＋−，见 §6 留痕） |

交付对账：清单 15＋5＋2 件逐行有着落，偏差为零；`src/helpShell.ts` 与测试文件零触碰。

## 2. 逐词读数与保留理由

改前基线见 `.scratch/t468/grep-base-render.txt`，改后见 `.scratch/t468/grep-after.txt`。

| 词 | 改前 | 改后 | 去向 |
|---|---|---|---|
| 茎／写键／读键／补写键／99键／95键／非执行词／执行桶／缺键／闭不了环／打通／渲染轨／执行层轨／分轨／三段分置／渲染活／升格／独占交互／同构合并／携带执行／真出口／真机三件套／装机／黑名单／赋能／抓手／闭环／颗粒度／沉淀 | 0 | 0 | 本就无命中 |
| 壳 | 41 | 5 | 36 处换词；余 5 见 R3 |
| 渲染层 | 1 | 0 | →页面交付 |
| 底座 | 2 | 0 | →机制／泛指 |
| 基座 | 20 | 0 | →基础 |
| 落地 | 7 | 0 | →实施 |
| 白名单 | 8 | 1 | 7 处→允许清单；余 1 见 R3 |
| 收敛 | 6 | 0 | →合并／统一 |
| 向导 | 1 | 1 | 见 R3 |
| 键 | 58 | 58 | 见 R1 |
| 对齐 | 49 | 49 | 见 R2 |

- R1（键）：本包 58 处均为对象字段名／映射成员名（键值／键名／键集／键序／丢键／删键／保键）、`一键复制`（Q11／#88 既有契约措辞）、`关键词`／`关键字`（标准词）。禁用表“键→命令”针对命令别称，不含上述语义，故保留。
- R2（对齐）：49 处均为契约逐字一致（逐条对齐契约／逐字对齐形状）与 CSS／布局对齐（左对齐／右对齐／列对齐／对齐轴），非“商量一致”语义，故保留。
- R3（`src/helpShell.ts` 6 处：壳 5、白名单 1、向导 1、对齐 2 含于 R2）：该文件由 `scripts/gen-help-shell.cjs` 全量生成，字节锁 `test/help-shell-136.test.mjs` 校验 drift，手改必红。改词须走 `assets/help-template.html` 源＋重跑 gen＋像素门，已超出本票最小换词范围，建议另开一票（源内 壳 2／白名单 3／向导 1 处徽章配色表键，见 `.scratch/t468` 资产扫描）。
- R4（wake-assets payload 向导 8 处）：老实物逐字（事实源仓外、`assertShape` 老词表、`TYPE_DEFAULT` 徽章配色表联动），改即与实物断链；头注生成器侧 2 处已换词重跑。payload 改词建议另开一票（模板配色表＋老实物口径联动）。
- R5（测试侧历史措辞）：`help.test.mjs` 等测试名／断言信息含白名单／基座，属旧测试，依禁令不碰；断言逻辑不受本票影响（`不在 enum` 正则仍命中、`ui-fix-154` 计数口径不变）。
- R6（t507 文件名）：`charts.ts:417` 原逐字引用 `t507-视觉底座-证据.md`，已改为同目录 t507 证据泛指；旧证据文件名本身不动（历史文件，改名另议）。

## 3. 消费方产物改前改后（skill-bill 真实链路 `assembleDocPage→buildSharedHelpersJs→fillTemplate`）

探针 `.scratch/t468/consumer-probe.mjs`，产物 `.scratch/t468/t468-consumer-sample.html`。

- 改前：`（#88 S5／B 段红队 S3-a：真实壳恒有 <pre>，故 436/436 恒有按钮，不静默丢卡）→ 复用既有委派。 */`（`HAS-OLD=true`）
- 改后：`（#88 S5／B 段红队 S3-a：真实help模板恒有 <pre>，故 436/436 恒有按钮，不静默丢卡）→ 复用既有委派。 */`（`HAS-OLD=false,HAS-NEW=true`）

结构不变：产物 diff 仅此注释行一词之差；行为零改（注释文本，不参与执行）。

## 4. 测试读数（消费方全绿，结构不变）

- `tsc -b packages/base-render packages/skill-bill` exit 0。
- skill-bill：tests 153／pass 153／fail 0（日志 `.scratch/t468/bill-test.log` 尾）。
- base-render：tests 665／pass 665／fail 0（日志 `.scratch/t468/base-test.log` 尾）。

## 5. 变异（改回禁用词红，还原绿）

- 变异红：`packages/base-render/src/controls.ts:932` 改回 `真实壳恒有` 后 grep 命中 1 行（上文 §3 改前行原文）。
- 还原绿：恢复后同口径 grep 命中 0 行；该文件 diff 保持 8＋−（本票 8 处换词，无残留）。

## 6. 他包同类命中（只记证据，不改；建议逐包开票）

扫描 `packages/*/src`（`壳／渲染层／渲染活／白名单／黑名单／向导／底座`，明细 `.scratch/t468/grep-other-pkgs.txt`）：

| 包 | 读数 | 开票建议 |
|---|---|---|
| skill-calorie | 壳 75、渲染层 19、底座 19、白名单 11、向导 63 | 建议开一票（量最大，含产物面复核） |
| skill-chef | 渲染层 5、向导 6、壳 1 | 建议开一票 |
| skill-home | 壳 1、向导 4、渲染层 9 | 建议开一票 |
| skill-memo-ilife | 壳 3、向导 11、渲染层 5 | 建议开一票 |
| skill-schedule | 壳 3、渲染层 6、白名单 6、向导 1 | 建议开一票 |
| base-combos／base-link-core／各 plugin-* | 0 命中 | 无需开票 |

## 7. GATE-RUN 对账（`.scratch/locks/gate-runs.log`，ticket=468）

- `b6bb3757` gen-wake-assets exit 0（waitedMs 0）
- `d1d31163` tsc 调用形式误写（`node_modules/.bin/tsc` 直调）exit 1 → 当即改用 `node node_modules/typescript/bin/tsc` 重跑
- `1299f962` tsc 双包构建 exit 0
- `ca34fae9` 消费探针重跑 exit 0
- `f63bfa60` skill-bill 全套件 exit 0
- `26a053f8` base-render 全套件 exit 0
- 抢锁等待均为 0–1ms，无 15 秒重试触发；加锁包装器全程持锁，finally 释放。

## 8. 超线与红线自查

- 行数零增减：全部同行换词；wake 重跑 diff 仅头注 2 行。skill-bill `wake-assets.ts` 986 行超线为既有挂号（包 AGENTS 已载超因与拆法去向），本票未加重。
- 红线：只换文案字面量，未改行为结构；未放宽禁用门（保留项均有语义级理由，非过门借口）；未碰他技能包、旧测试、生成物手改禁区。
- 未做：`.changeset` 未落（允许路径未含，待编排者确认是否补）；helpShell／assets 链与 wake payload 向导改词、他包换词均为另开票事项（见 R3／R4／§6）。
