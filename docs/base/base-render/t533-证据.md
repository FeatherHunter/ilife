# T533 共享触屏两件补齐 · 证据

归属件：**base-render**（公共层包）。票号：**533**。
改动面：`packages/base-render/src/style.ts`（7 处可点基座各补两条声明）、`packages/base-render/src/blocks.ts`（壳内一条 `button,summary` 规则补两条声明）。
口径正本：票面 `.scratch/bill-t-533-body-r3.md` ＋ `docs/agents/视觉验收墙.md` 手机端四手法（820 断点＋触摸目标 44px＋触屏三件＋窄屏塌列）＋ `docs/skills/skill-bill/t407-机审读数.md` 第 51 行 ＋ HELP `button.copy` 三行与卡路里 `pageChromeCss.ts:29-32`。
红线：只补声明与结论，不改版式文案，不为过门放宽；禁碰技能包与旧测试（旧测试一条未改）。

## 一、改前改后两行读数

探针：`buildStyleSheet().css` 与 `buildStyleSheet().css+blocksCss()` 的 `touch-action`／`-webkit-tap-highlight-color` 计数（持锁 `tooling/run-locked.mjs --ticket 533`，日志 `.scratch/t533/before-build.json`／`after-build.json`）。

| 读数 | 改前 | 改后 |
|---|---|---|
| `buildStyleSheet` 字符 | 25020 | 25538（＋518，只加声明） |
| `buildStyleSheet` 内 `touch-action` | 0 | 7（`toast-act`／`toast-close`／`action-btn`／`copy-btn`／`copy-menu-item`／`help-shell-btn`／`help-shell-card-copy` 各 1） |
| `buildStyleSheet` 内 `-webkit-tap-highlight-color` | 0 | 7（同上各 1） |
| 组合（`build+blocks`）字符 | 40129 | 40789（＋660） |
| 组合内 `touch-action` | 0 | 8（上 7 ＋壳内 `button,summary` 1） |
| 组合内 `-webkit-tap-highlight-color` | 0 | 8（同上） |
| 组合内 `-webkit-touch-callout` | 0 | 0（第三件未补，见第四节交裁） |
| 组合内 `min-height:44px` | 在（改前 7，改后仍在） | 在（未动，触摸目标不断） |

落点明细（`src/style.ts`）：`toast-act:256`、`toast-close:270`、`action-btn:341`、`copy-btn:381`、`copy-menu-item:527`、`help-shell-btn:979`、`help-shell-card-copy:1138` 各加 `transparent`＋`manipulation` 两行；`src/blocks.ts:1425` 后加壳内 `button,summary` 一条（照 `pageChromeCss` 同选择器逐字）。

## 二、消费方页触摸核（按口径核）

无真机与无头浏览器可用，按设计口径核（票面允许“或按设计给出的手感口径核”）：
- 壳规则体实测为 `-webkit-tap-highlight-color: transparent; touch-action: manipulation;`（`.scratch/t533/touch-check.json`），与 HELP `button.copy:1175-1176` 及 `pageChromeCss:31-32` 逐字同值。
- `transparent`＝点击无系统蓝高亮方块；`manipulation`＝去双击缩放延迟且保留 `pan-x/pan-y`，拖动不误触发页面滚动（与 HELP `.tab-bar:117` 的 `touch-action:pan-x` 同向，不打架）。
- 覆盖：壳内一切 `button,summary`（采集页 15 件／回执页 7 件可点件全在壳内，机审 `tapTargets` 列）；壳外仅剩 `toast` 两钮，已在 `style.ts` 同值覆盖。
- 未改版式：只加两条声明，未动选择器权重之外的任何 `display/padding/font`，未改文案（`git diff` 仅 7＋14 行声明）。

## 三、32 份差异结论（漂移，非分档）

- 历史：`t407-机审读数.md:51` 记“32 份不是逐字节同一份（30 页 116 条 `font-size`、代表页 109 条，差一条 `.ilife-block-page-shell-body` 间距开关）”。
- 现状复跑：32 份名单（`t407-v8-style-audit.mjs:35-41`）的 head `<style>` 逐字节同一份（`uniqStyle=1`，各 40818 字符，各 131 条 `font-size`，日志 `.scratch/t533/diff-32.json`）；机审亦报“共享样式表逐字节同一份的页：32／32”。
- 结论：**历史版本漂移，现已统一，无有意分档**。依据为 `docs/skills/skill-bill/t407-复核-桌面收尾验证.md:18`（“共享样式 32/32 同一份”）与本票复跑一致；本票未改 32 份 frozen 页（禁碰技能包），无需再统一。

## 四、第三件与口径交裁（两行）

- HELP `button.copy:1175-1177` 三行是 `tap-highlight`＋`touch-action`＋`-webkit-touch-callout:none`；卡路里 `pageChromeCss:10/29-32` 的“三件”是“44px＋上两条”；两处机审（`t351/t407`）把两条检查叫“三件”。三处口径互相打架，第三件是谁没写清。
- 交裁两行：第三件是否为 `-webkit-touch-callout:none`（HELP 按钮第三行）？若是，是否要随本票同值补到上 8 条？
- 本票未自定：组合内 `callout` 保持 0；若裁定要补，落点同本票 8 条，不新增类名与版式。

## 五、测试与持锁

- `packages/base-render/test/*.test.mjs`：665／665 通过（含 `style 29`、`blocks 46`）。
- `packages/skill-bill/test/*.test.mjs`（消费方）：153／153 通过，产物结构不变（HTML 产出器一行未动，机审“本页样式块 0、内联 0”保持）。
- `npx tsc -b` 持锁通过（`dist` 忽略不提交）。
- 全程经 `node tooling/run-locked.mjs --ticket 533 --poll-ms 15000 --max-wait-ms 600000`（15 秒轮询、10 分钟上限；遇 486 持锁等 15／90 秒后接管，未抢活锁）。
