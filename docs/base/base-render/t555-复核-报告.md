# #555 独立复核报告（复核席，与实施无交集）

判定：PASS（无 S1；S2 共 1 条，方法瑕疵但结论已由独立探针补证，不阻断关闭；S3 共 1 条，不阻断关闭）。

被复核提交：`66909b25bcad3d79c3b3d7cb995115b14d262780`（`feat(555)`，4 件，与回执声称一致）。
复核时刻工作区说明：树上有他席在途未提交改动（`packages/plugin-schedule-ilife/test/smoke.test.mjs`、`packages/skill-calorie/src/triggers/scene-10-analysis.ts` 等）与一批 `.tmp*` 草稿，复核未碰其中任何一件；`packages/base-render/src/text.ts` 的 blob 哈希在 `66909b25` 与 `HEAD` 同为 `7fa16ee072e64f9fd3c12f343739c33e72aeaa10`，`git diff 66909b25..HEAD -- packages/base-render/src/text.ts` 为空，复核基线未漂移。当前 `HEAD` 为 `422c583f`（其后两笔 `feat(508)` 只碰 `t508` 文档），“`git show HEAD` 仅含本票 4 件”一语取被复核提交 `66909b25` 为准验证。

## 0. 缺陷标本票引入／范围／范围外（对票面，不采信回执结论）

- 引入：`#555` 为公共层契约矛盾执行票（父票 `#636`），题为“`buildLogText` 场景标识把技能名拼两遍，而冻结测试把双前缀钉成期望值”。`owner` 2026-09-16 裁定走“Q4 甲方案”（`key` 已以 `skill + '.'` 开头不再补技能名）。`#550` 是卡路里侧漏斗归一缺陷票（四个场景 102 份修对，不动公共层），与本票勿混。
- 范围（本票应做）：`packages/base-render/src/text.ts:296-304` 的 `sceneText` 甲幂等；`packages/base-render/test/text.test.mjs` 那 5 处期望随契约改（`:135`／`:405`／`:412`／`:430`／`:885`，末处因新增断言行号后移一位）＋ 1 条三段负向断言；代表抽样改前／改后对照证明只有场景标识行变；乙／丙在仓里写死为已废。
- 范围外（本票不做）：`packages/skill-calorie/**`（归 `#550`）；模板与生成物（`packages/base-render/assets/help-template.html:198/206` 与 `src/helpShell.ts:45` 的 `docs/component-contract.md` 指针，改模板必重生并触字节锁，属扩面）；他席在途件；存量墙重跑（按各场景收口票换代，本票仅代表抽样）。`git show --stat 66909b25` 仅含回执声称的 4 件，未见范围外文件。

## 1. 机器证据（独立复跑，非采信）

声称的 5 个 `GATE-RUN` 在 `.scratch/locks/gate-runs.log` 中一对一找到，`ticket` 与 `cmd` 逐字对上，`exit` 与回执一致（行号为复核抄录时刻值）：

- 声称 `runId=0bc04183-1268-49f7-84c4-bec5f992737e`（`pwsh -File .scratch/t555/do-green.ps1`）：日志 20165–20166 行 `START／RUN` 对，`exit=0`。对应日志件 `.scratch/t555/green.log:117` 有 `MARK-GREEN-DONE build=0 test=0 exit=0`，尾部 `tests 84／pass 84／fail 0`。
- 声称 `runId=260908d7-c334-4ef0-b5ef-bddaac7f41f7`（第一次变异，已作废）：日志 20179–20180 行，`exit=1`。作废理由（增量编译未重发产物致绿段假红）与回执一致，不作判据。
- 声称 `runId=93949a96-d59c-4dfb-a8d9-67622b25cc2b`（`do-rebuild.ps1`）：日志 20201–20202 行，`waitedMs=10000 exit=0`（等锁 10 秒，符合活锁不抢回协议）。对应 `.scratch/t555/rebuild.log` 有 `MARK-REBUILD-DONE build=0 test=0 exit=0`。
- 声称 `runId=7c7f5e29-8582-4ea1-a98f-367546777433`（`do-mutate2.ps1`，`--force` 重做）：日志 20211–20212 行，`exit=0`。对应 `.scratch/t555/mutate2.log:298/414-415` 有 `MUT-RED build=0 testExit=1 failDetected=1`、`MUT-GREEN build=0 testExit=0 hashMatch=1`、`MARK-MUTATE-DONE ok=1`，首行 `ORIG-HASH` 与当前工作区 `text.ts` 的 `Get-FileHash` 值一致（`A0D13B…DD80F`），复原可信。
- 声称 `runId=421d2a50-06e1-4e06-bffe-1b28ab843fc0`（`do-diff.ps1`）：日志 20217–20218 行，`exit=0`。对应 `.scratch/t555/diff.log` 为 `DIFF-SUMMARY total=15 onlySceneChanged=15`＋`MARK-DIFF-DONE ok=1`。但 csv 分支的证明力见 D1。

复核独立复跑（本次新标识，一律经 `node tooling/run-locked.mjs --ticket 555 --`，均为 `exit=0`，落盘见 `t555-复核-gate-runs.md`）：

- `runId=7a0583d4-72d8-41b9-8499-da33a7f1c7fb`：`node node_modules/typescript/bin/tsc -b packages/base-render`，`exit=0`。
- `runId=7911e48b-8fef-4415-a7d2-707ebef4532b`：`node packages/base-render/test/text.test.mjs`，`tests 84／pass 84／fail 0`，`exit=0`（输出存 `.scratch/t555r/test.log`）。
- `runId=e80e0c49-3b30-4c30-8616-da9e38b90b90`：`node node_modules/typescript/bin/tsc -b packages/base-render --force`，`exit=0`。
- `runId=b0b57bd8-677f-4c29-959d-305b79f56f5b`：`--force` 后重跑 `text.test.mjs`，`tests 84／pass 84／fail 0`，`exit=0`（输出存 `.scratch/t555r/test-after-force.log`）。
- `runId=987b7e5e-f184-4839-a995-7e39ef210147`：`node .scratch/t555/do-diff.mjs` 原样重跑，`DIFF-SUMMARY total=15 onlySceneChanged=15`＋`MARK-DIFF-DONE ok=1`，`exit=0`（输出存 `.scratch/t555r/diff-rerun.log`；证明力限制见 D1）。
- `runId=898d6f9d-e664-4bb0-9725-f3b4a03ed67d`：新探针 `node .scratch/t555r/probe-edge.mjs`，`PROBE-SUMMARY pass=25 fail=0`＋`MARK-PROBE-DONE ok=1`，`exit=0`（输出存 `.scratch/t555r/probe.log`）。

其余核对：`git log -1 --format='%H %s' 66909b25` 中文信息无损；`git diff --cached --name-only` 为空；`git status --short` 无本票路径的在途改动；编译入口与测试入口与证据 §9 声明一致。

## 2. 新探针／变异（被审脚本覆盖不到，只读 dist，不碰源码）

探针件：`.scratch/t555r/probe-edge.mjs`（25 项全绿，见上）。专打两处盲区：

- P1 真实 csv 解析：被审 `do-diff.mjs` 的 csv 分支恒真（`hasOld／hasNew` 算出不用，`sameExceptScene=true` 写死，`newS` 为构造值而非从输出解析；见 D1）。本探针逐 `format` 真实解析场景值（`text` 取第 2 行、`json` 取 `scene` 键、`csv` 按 RFC4180 简解析取 `section=scene` 行），`E1（已带前缀）／E2（不带前缀）／E3（异技能前缀）× text／json／csv` 共 9 项逐字等于契约形状，且 `E1` 文本面无 `skill-a.skill-a.` 子串。
- P2 边缘＋内存变异（不写源文件）：`N1` 多级后缀（`calorie.view.plan.detail` 仍一次剥离）、`N2` 部分前缀无点（`skill-aX.key` 必须不剥，得 `skill-a.skill-aX.key`）、`N3` 键恰等于技能名（得 `skill-a.skill-a`）、`N4` 尾点、`N5` 大小写敏感、`N6` 前导空格、`N7` 空技能、`N8` 空键、`N9` 已双前缀（单步幂等，不循环剥）共 9 文本面＋ 3 处 `json／csv` 抽查，全部与源码语义一致。内存变异三组：`M-old`（旧双拼）与 `M-direct`（丙·直接印 `key`）均被契约值捕获；`M-naive`（弱修复 `key.indexOf(skill)===0`，缺 `+'.'`）能穿过被审三段断言（已带／不带／异技能三组全绿）却在 `N2` 现形（变异得 `skill-aX.key`，契约为 `skill-a.skill-aX.key`），证明被审负向断言有网眼（见 D2）。当前源码用的是 `skill + '.'` 比对，不受该变异影响。

## 3. 缺陷清单（均标注归属与分级）

- D1［本票范围，S2］：`do-diff.mjs:43-48` 的 csv 分支未实质验证（`hasOld／hasNew`  dead code，`sameExceptScene` 恒 `true`），而证据 §8 写“`csv` 口径含场景标识单元格比对”、§10 称“三口径中 `csv` 口径含场景标识单元格比对”，措辞与实现不符。后果：声称的 15 项中 5 项 csv 的“`onlyScene=1`”是自赋分，不是测得的。独立补证：本复核 P1 用真实 csv 解析对 `E1／E2／E3／N2` 的 `scene` 单元格逐字比对通过，故结论（csv 面亦为单前缀）成立，不阻断关闭。下一手：后票修该脚本（复用 `.scratch/t555r/probe-edge.mjs` 的 `sceneOf(csv)` 解析），或以本探针为准归档，不必重跑全量墙。
- D2［本票范围，S3］：新增负向断言三段（已带／不带／异技能）未覆盖“同技能部分前缀无点”（`skill-a` ＋ `skill-aX.key`）。弱修复 `indexOf(skill)===0` 可穿过现有三段（见 P2 的 `M-naive` 证明），当前源码因用 `skill + '.'` 比对而不受影响。下一手：后票把 `N2`（含 `json／csv` 各一）补进 `text.test.mjs`，行数代价约 8 行。
- 禁区与过程：`git show --stat 66909b25` 仅含 4 件（`src/text.ts`、`test/text.test.mjs`、`t555-证据.md`、`t555-gate-runs.md`），无 `skill-calorie`、无模板与生成物、无他票文件；helpShell 指引现状复核属实（模板第 198／206 行与 `helpShell.ts:45` 内嵌注释指向不存在的 `docs/component-contract.md`，`Test-Path` 为 `False`；正身为 `src/spec/**`（如 `spec/index.ts:135` 的 `LOG_SECTION_TITLES` 冻结签名）＋ `docs/base-paint-contract.md:569/607`），本票只在证据 §7 给订正方案而不改模板，符合“只到判据绿为止，不扩面”，正确。乙／丙已废记账（证据 §3）与票面裁定一致。四处说法对照（证据 §2）逐条读后无误。

## 4. 路径

- 改动件（被复核，`66909b25`）：`packages/base-render/src/text.ts`、`packages/base-render/test/text.test.mjs`、`docs/base/base-render/t555-证据.md`、`docs/base/base-render/t555-gate-runs.md`。
- 复核证据件（本次新增，未提交）：`docs/base/base-render/t555-复核-报告.md`（本件）、`docs/base/base-render/t555-复核-gate-runs.md`（运行条目导出，可作对账源）。
- 草稿（本次新增，未提交）：`.scratch/t555r/probe-edge.mjs`、`.scratch/t555r/probe.log`、`.scratch/t555r/test.log`、`.scratch/t555r/test-after-force.log`、`.scratch/t555r/rebuild.log`、`.scratch/t555r/diff-rerun.log`、`.scratch/t555r/issue555.log`、`.scratch/t555r/issue555-body.log`。
- 复核禁区遵守：未改源码、未改他人证据、未做危险 git 操作（仅只读 `git show／diff／status／hash-object` 与持锁读命令）。

## 5. 未做项与下一手

- 未做项：无（复核只到“判据绿”，不拆件；`text.ts` 现 497 行超 350 线的事由与拆法已在证据 §4 第四步记账，复核不另起拆件）。
- 下一手：编排者收票关闭；D1 的脚本修复与 D2 的 `N2` 入仓可并入一后票（只碰测试与草稿脚本，不碰 `sceneText` 语义）；存量墙按各场景收口票重跑，本票不替任何场景重出墙。
