# #242 独立复核回执（636MAP 复核席，与实施无交集）

**判定：PASS**（票面两处错位已按"以模板为准改 schema"口径消除；门禁全绿；未扩面）

实施提交：`46b86eb1`（`src/spec/help.ts`＋`src/help.ts`＋`test/help-t242.test.mjs`＋`docs/base/base-render/t242-证据.md`）。复核只读结论、不采信结论，以下均为本席独立复跑。

## 一、机器证据（均经 `node tooling/run-locked.mjs --ticket 242 --`）

| 门禁 | runId | exit |
|---|---|---|
| `node node_modules/typescript/bin/tsc -b packages/base-render` | `f244706d-2421-4a00-a1ee-83e2750e79a8` | 0 |
| 新测试 `node --test packages/base-render/test/help-t242.test.mjs` | `5885de0a-9b36-4ca4-bd95-3435b41ca2be` | 0（8/8 全绿） |
| help 回归 `node --test packages/base-render/test/help.test.mjs` | `d23860a7-26ea-4033-bc9b-e3c8517c3d34` | 0（48/48 全绿；等锁 ticket=427 持锁者约 20s，属正常排队） |
| `node --test packages/base-render/test/contract-signatures.test.mjs` | `e0d08bc5-cbff-4135-a203-a00041c53704` | 0（48/48 全绿） |
| `node packages/base-render/scripts/gen-help-shell.cjs --check` | `748bbeed-decc-446f-8c12-71a79a258bfa` | 0（`check OK`，模板未动） |
| 本席新探针 `node .scratch/t242r/probe-t242r.mjs` | `0bdea894-b981-4feb-9b20-a7657b5465be` | 0（11/11 全绿；首轮 R2–R4 系本席断言粗判 `includes('<a')` 误报——壳内嵌模板 JS 本就含 `<a class="a-link" href=` 字面（assets:1820），收紧为 B 路自产类断言后全绿，源码无问题） |

## 二、自设新探针（`.scratch/t242r/probe-t242r.mjs`，11 项）

- R1 steps 混合形态 `[string, {title,desc}, {title}]` 过校验且三者皆有文案（实施测试只覆盖纯串／纯对象，混合为新增）。
- R2 `url:false`＋http 值：过校验但 B 路仍 `<span>`，与模板 `:1819` 真值判同口径。
- R3 `url:true`＋非 http 值：过校验但仍 `<span>`，不链化。
- R4 `url:true`＋`javascript:alert(1)`：过校验但不得产出 B 路 `<a>`（XSS 守栏与模板 `indexOf('http')===0` 一致），原文转义留存。
- R5 范围外四键仍红：`hidden`／`closable`／`copy_all:true`／`aliases` 仍 `schema-invalid`（证实施未扩面，与证据 §六一致）。
- R6 模板三处判字逐字仍在（1790 `INIT_BANNER && !INIT_BANNER.hidden`＋`closable === false`；1791 `esc(st.title)`／`esc(st.desc)`；1819 `it.url && …indexOf('http')===0`）；B 路对象步渲染含标题与说明。
- R7 非法对象步仍红：缺 `title`、带多余键各一例，皆 `schema-invalid`。

## 三、缺陷清单（标本票引入／范围／范围外）

1. 标本① `contact.items[].url`：票面缺陷，修后 schema 收 `boolean|string` 可选（生产四家 `true` 旗标与票面字符串两 side 皆绿），B 路 `url` 真值＋值 `http` 开头即 `<a>`，与模板同判。——已消除。
2. 标本② `init_banner.steps`：票面缺陷，修后项为 `oneOf(string, {title, desc?} 闭集对象)`，纯串／纯对象／混合皆过；非法（数字步、数字 url、缺 title、多余键）仍红。——已消除。
3. 范围外（有意未动，由他票定）：`hidden`／`closable`、`copy_all` 布尔形、`aliases` 仍走 A 路绕行（R5 实测仍红，非漏修）。
4. 观察项（不判 FAIL，建议后续票）：A 路模板对字符串步仍渲空格（读 `st.title` 得空），B 路现已正确渲染字符串；本票"模板逐字不动"口径下该残余 A/B 差有意保留，对象形（生产实际形态）两路已一致。

## 四、写集与纪律

- 本席写集：本件＋`.scratch/t242r/probe-t242r.mjs`；源码、他人证据、工作区他席文件一律未碰（`packages/base-render/src|test` 零 diff；`git status` 仅见他席在途文件，原样保留）。
- 未 comment、未 close 票。复核完毕，停止等指派。
