# T564 helpShell独立页5处补touch-action · 证据

归属件：**base-render**（公共层包）。票号：**564**（#533复核探针D1另票）。
改动面：`packages/base-render/src/style.ts`的helpShell区5处各加一行`touch-action: manipulation;`（+5/-0）。
口径正本：票面＋`docs/base/base-render/t533-证据.md`＋`docs/base/base-render/t533-复核-独立复跑.md`第四段D1＋壳规则`src/blocks.ts:1432-1436`同值。
红线：只加声明，不改版式文案；照#533同值抄；禁碰技能包源码与旧测试（旧测试一条未改）。

## 一、改前改后两行读数

探针：`.scratch/t564/probe.mjs`读`buildStyleSheet().css`与`build+blocksCss()`组合（持锁`tooling/run-locked.mjs --ticket 564 --poll-ms 15000 --max-wait-ms 600000`，日志`.scratch/t564/before-build.json`／`after-build.json`）。

| 读数 | 改前 | 改后 |
|---|---|---|
| `buildStyleSheet`字符 | 25538（＝#533改后基线） | 25688（＋150，只加5行声明） |
| `buildStyleSheet`内`touch-action` | 7 | 12（＋5） |
| `buildStyleSheet`内`-webkit-tap-highlight-color` | 7 | 7（不动，本票只补touch-action） |
| 组合（`build+blocks`）字符 | 40789 | 40939（＋150） |
| 组合内`touch-action` | 8 | 13（＋5） |
| 组合内`-webkit-tap-highlight-color` | 8 | 8（不动） |
| 组合内`-webkit-touch-callout` | 0 | 0（第三件不自定，随#533交裁） |

改前`UNCOVERED`（`cursor:pointer`缺`touch-action`）6项：5处helpShell＋`disclosure-summary`；改后仅剩`disclosure-summary`1项（`.scratch/t564/touch-check.json`）。

## 二、落点明细与排除项

5处（独立HELP页，无壳祖先，壳元素规则兜不住）：`help-shell-tab:822`、`help-shell-sheet-summary:945`、`help-shell-subgroup-summary:1037`、`help-shell-tab-search-clear:1136`、`help-shell-btn-backtop:1224`，各在`cursor: pointer;`后加`touch-action: manipulation;`一行（`git diff`仅5行新增，零删除）。

排除项：`block-disclosure-summary`系`summary`（有`::-webkit-details-marker`为证），壳内一律被`.ilife-block-page-shell summary`元素选择器兜住，不计（复核D1原口径）。

同值核：壳规则体为`touch-action: manipulation;`（`blocks.ts:1435`），5处逐字同值；`tap-highlight`独立页由HELP前缀`*{\-webkit-tap-highlight-color:transparent}`全局覆盖一半，本票按票面不补。

## 三、消费方测试与结构不变

- `packages/base-render/test/*.test.mjs`：665／665通过（`runId=t564-base-test`）。
- `packages/skill-bill/test/*.test.mjs`（消费方）：153／153通过（`runId=t564-bill-test`）。
- 结构不变：`help.ts`／`controls.ts`／`blocks.ts`／`helpShell.ts`零diff；`style.ts`仅+5/-0；HTML产出器一行未动。
- `npx tsc -b packages/base-render`持锁通过（`runId=t564-build`，曾等544持锁90秒后接管，未抢活锁；`dist`忽略不提交）。

## 四、变异自证

只动gitignored的`dist/style.js`（源码零动，`try/finally`还原，`.scratch/t564/probe-mutant.mjs`，`runId=t564-mutant`）：

- 去`help-shell-tab`一处声明：探针红（`buildTouch 11/12`、`combTouch 12/13`、`uncovered 2`）。
- 还原：探针绿（`buildTouch 12`、`sha 5f41f5ab0255`相等，`T564-PROBE ALL-GREEN`）。
- 结论：判别力成立且已还原（`.scratch/t564/mutant.json`）。

## 五、持锁与结构记账

- 全程经`node tooling/run-locked.mjs --ticket 564 --poll-ms 15000 --max-wait-ms 600000`（15秒轮询、10分钟上限；`gate-runs.log`见`t564-before-build`／`t564-build`／`t564-after-build`／`t564-probe-green`／`t564-mutant`／`t564-base-test`／`t564-bill-test`）。
- 结构纪律：第一步影响清单仅一行（`packages/base-render/src/style.ts` helpShell区5处补声明，因独立页无壳祖先）；无新增目录与文件职责（第二步不适用）；第三步跨能力引用零新增；第四步超线报警：告警线本包未定数字（沿#541记账），`style.ts` 1369→1374行，本票就地不拆；第五步交付对账：实际仅碰清单一行，偏差零（`.scratch/`草稿不计交付）。
