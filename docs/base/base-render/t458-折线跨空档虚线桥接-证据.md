# #458 折线跨空档虚线桥接 · 证据

- 目标：折线新增 `gapStyle` 开关，`'dashed'` 档跨空档逐段虚线桥接（实线只连两侧都有记录的相邻点，跨空档段另出一条同色虚线路径）。
- 判据（票内验收命令）：
  - `node node_modules/typescript/bin/tsc -b packages/base-render` 全绿；
  - `node --test packages/base-render/test/charts.test.mjs` 全绿（含 `B.connectNulls` 与 `B.fillBetween` 逐字节断言）；
  - 新增 `packages/base-render/test/charts-gap-458.test.mjs` 全绿：缺省/`'break'` 与今天逐字节相同；`'connect'` 等价 `connectNulls:true`；`'dashed'` 实线只连实测相邻点＋桥接段另出虚线路径（同色、`stroke-dasharray="5 4"`），首末空档不出桥接；三档点只画实测点、不补 0。
- 写集（只许这四类）：`src/spec/charts.ts`（类型层）＋`src/charts.ts`（桥接段）＋`test/charts*.test.mjs`（本票新测）＋`docs/base/base-render/t458-*`（本件）。
- 不许动：`contract.ts`（46 行渲染契约无图表字段，签名串不变不动）／`spec/index.ts` 冻结面登记（签名串不变不动）／`blocks.ts`／`style.ts`／`skill-calorie` 全系（含 `home/goalProgressDocs.ts:317`、`home/homeViewParts.ts:118` 归 #401）／他票文件；不补 0；均值线灰虚线不动；不 push。

## 1. 收窄核验（对抗结论复核）

- ① 类型层只动 `spec/charts.ts`：`gapStyle` 为 `LineChartOptions` 可选字段，`ChartsApi`/`ChartOutput`/`ChartItem` 签名串不变 → `contract.ts`（渲染契约，无图表字段）与 `spec/index.ts`（`SPEC_FROZEN_SURFACE`）不动。`contract-signatures` 测试为证。
- ② 调用点 28 处（`kind: 'line'` 在 `skill-calorie/src` 内 28 命中＋`charts.ts:1286` 自身返回 1 命中＝29）：其中 `home/goalProgressDocs.ts:317` 与 `home/homeViewParts.ts:118` 归 #401，本票不碰；其余 26 处本票不迁移，全部保持缺省（等价 `'break'`，逐字节不变）。迁移口径由编排者另切票。
- ③ 既有 `dashed` 系整线虚线（`charts.test.mjs:319`：单条路径整线 `stroke-dasharray="6 5"`），无逐段桥接等价实现 → 本票新增能力非重复。

## 2. 影响清单（第一步）

- `packages/base-render/src/spec/charts.ts`：`LineChartOptions` 加 `gapStyle?: 'break' | 'connect' | 'dashed'`（类型层，同一能力内）。
- `packages/base-render/src/charts.ts`：折线绘制位（`polyPath/smoothPath/stepPath` 调用处一带）`'dashed'` 档另出桥接虚线路径（同一能力内）。
- `packages/base-render/test/charts-gap-458.test.mjs`：新增（测试，不进结构管辖）。
- `docs/base/base-render/t458-折线跨空档虚线桥接-证据.md`：本件（文档，不进结构管辖）。

## 3. 结构设计（第二步）

- 无新增目录／文件职责变化：`gapStyle` 与 `connectNulls` 同属折线能力缺口语义，概念唯一（`spec/charts.ts` 一处定义，`charts.ts` 引用）。
- 解析口径：`gapStyle` 显式给定时优先；未给时回落 `connectNulls`（`true`→`'connect'`，否则→`'break'`），缺省产出逐字节不变。
- 绘制口径：`'break'`＝今天（分段 `M`，`connect=false`）；`'connect'`＝今天的 `connectNulls:true`；`'dashed'`＝实线走 `break`＋桥接段另出一条 `…-charts-line-bridge` 路径（跨空档两端点直连 `M…L…`，`stroke` 取本序列同色，`stroke-dasharray="5 4"`）。均线序列（`avg:true`）不参与桥接；面积填充走 `break`；点只画实测点。
- 超线报警（第四步）：`charts.ts`（2154 行）与 `spec/charts.ts`（282 行）改前已超本包 350 行告警线。「已超线，需要根据规则进行重构。」——超因：图表层八族单文件实现；本次只改折线绘制位一行附近＋类型一行，不拆件（拆件另票）。

## 4. 变异红绿（源码改必两行）

- 红：新测先行，用占位实现跑红（桥接路径缺失）。
- 绿：实现后跑绿；同步确认既有 `charts.test.mjs` 全绿。

## 5. GATE-RUN（经 `node tooling/run-locked.mjs --ticket 458 -- …`）

- 编译：`node node_modules/typescript/bin/tsc -b packages/base-render` → exit 0（实现后重跑亦 0）。
- 新测试（红）：实现前 `node --test packages/base-render/test/charts-gap-458.test.mjs` → 5 中 3 过 2 红（`connect 等价`与`dashed 桥接`两行按预期红）。
- 新测试（绿）：实现后同命令 → 5/5 过。
- 既有：`node --test packages/base-render/test/charts.test.mjs packages/base-render/test/style.test.mjs packages/base-render/test/contract-signatures.test.mjs` → 168/168 过（含 `B.connectNulls` 与 `B.fillBetween` 逐字节断言，冻结面清单与文档投影一致，签名串不变故 `spec/index.ts`／`contract.ts` 未动）。

## 6. 产出对账（第五步）

- [x] `src/spec/charts.ts`（`LineChartOptions` 加 `gapStyle`）
- [x] `src/charts.ts`（`bridgePath`＋解析＋绘制位桥接段）
- [x] `test/charts-gap-458.test.mjs`（5 条，红绿两行已验）
- [x] 本证据件

偏差：零（第一步四行逐行有着落；工作区他席文件如 `src/blocks.ts` 一律未碰）。

## 7. 未做项（回执用）

- `skill-calorie` 26＋2 处调用点零迁移（含 #401 两处）；`spec/index.ts`／`contract.ts` 未动（签名串不变）；样式文件未动（桥接样式走内联属性）。
