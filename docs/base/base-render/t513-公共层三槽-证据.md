# #513 公共层三槽证据（实施完成）

- 票：#513「公共层：三处加槽」；包：`packages/base-render`
- 写集（只此三类，已对账）：`src/blocks.ts`（＋62／−7）、`test/blocks.test.mjs`（新 5 断言）、本证据件
- owner 约束：只动公共层母版与区块，不动技能代码 ⇒ ② 走 **renderKpiGrid 加 title** 路线，不碰 `homeDocs.ts`、不碰 triggers
- 串行约束：开工时 `blocks.ts` 在 `b3e02e85` 之后树上无未提交改动；收工 diff 仅本票 62＋／7−（`git diff HEAD --stat` 实测）

## 结构纪律对账（必报五步）

- 第一步影响清单：见骨架，三行全部着落（源码一处、测试一处、证据一件），偏差零。
- 第二步结构设计：无新目录新文件（测试断言进既有 `blocks.test.mjs`）；公开接口只加可选位（`DataTableRow.marker`、`renderKpiGrid(cards, opts?)`、`KpiGridOptions`），无新导出函数。
- 第三步写代码：纯本包，无跨能力引用；复用冻结 token（`--soft`／`--blue2`）与既有 `RADIUS_PILL`，无新常量。
- 第四步超线报警：「已超线，需要根据规则进行重构。」`blocks.ts` 2351→2406 行，超本包 350 行告警线（`packages/base-render/AGENTS.md`）。超因是历史沉淀（12 区块实现＋样式同文件），非本票引入；拆件碰全包导入面，超本票写集，这次先不拆（记账由维护者另排）。
- 第五步交付对账：本节即对账——第一步三行逐行有着落，偏差为零。

## 槽① 表格行标记 `rows[i].marker`

- 形状：`DataTableRow = Readonly<Record<string, unknown> & { marker?: unknown }>`；非空串 ⇒ 首格前插 `<span class="ilife-block-data-table-row-marker">`（转义）；缺省／空串 ⇒ 零渲染；非字符串非 undefined ⇒ `bad-input`（与 `cellText` 同严格）。
- 列键碰撞：全仓 `grep` 无 `key: 'marker'` 列（141 处调用点零影响，见实施记录）。
- 缺省逐字节一致：改前（TEMP worktree 编译 HEAD 版）vs 改后 14 组矩阵（空表／caption／cellHtml 透传与回退／转义／空串 marker／四卡 grid 含 bar＋badge／空 opts）——14 组逐字节相等，sha256 同为 `54f7300ffa6e0ede23eeb495dfe7d5f8e1cce307173aefb3f66c2ea973b03461`（`BYTE-IDENTICAL=PASS`）。
- 窄屏不横溢：夹具页 512 档行标记 `right=266.83 ≤ 表容器界 494`，全页横向溢出=false（`PASS`）。

## 槽② `renderKpiGrid(cards, opts?: KpiGridOptions)`

- 路线裁决（作者裁）：选「grid 加 title」，不选「统一产出器」。理由：① owner 禁动技能代码，统一产出器要收 141＋117 两族标题槽并迁页面，超写集；② 117 处旧调用全不动（第二参缺省即今日产物，14 组差分已证）；③ 产出与 `chartBlock`／`copyBlock` 同形（`h2`＋转义＋缺省零渲染）。
- 产出形：有 title → `<h2 class="ilife-block-kpi-card-title">`＋原 grid 串（与页面当刻手写形一致，页面票后迁可逐字节对）；无 title → 返回原 grid 串（逐字节同，不包 section，不新类，不新增样式区）。
- 槽级证明：夹具页 grid 标题与 chart 标题 computed 同为 `15px／700`（`PASS`）；单测钉死三族标题档全等。
- 表 caption 双重身份（表名兼段标题）不顺手改，留遗留出口。

## 槽③ 徽章贴底

- 改动（kpiCard 区内两条）：卡片容器加 `display:flex; flex-direction:column`；徽章 `margin-top:8px` → `margin-top:auto; padding-top:8px`（`auto` 吃余量贴底；`padding-top:8px` 保住 8px 最小间距，最高卡徽章位置不动）。
- 几何读数（样板页真机 CDP，四档）：512（2×2 两行）徽章 bottom `[488.88,488.88]／[698.86,698.86]`；820／1000／1440（一行四卡）`[508.08×4]`——最大差全部 `0px ≤ 1px`（`PASS`）。

## 负向自证（改坏必红×3，还原复绿）

- ① `ci === 0` → `ci === -1`（标记永不进首格）：`blocks.test.mjs` 58／59，红的恰是 marker 断言；还原后该断言复绿（在②的红跑中同步见证）。
- ② 标题分支丢 `+ grid`（标题后无 grid）：58／59，红的恰是 grid 标题断言；还原后复绿（在③的红跑中同步见证）。
- ③ 徽章退回 `margin-top: 8px`（去 `auto`＋`padding-top`）：58／59，红的恰是三槽 CSS 断言；还原后终检 109／109 全绿。
- 注：② 的第一版变异（`|| true`）撞类型收窄导致编译红，已换成等效的单行输出变异，要的是测试红。

## GATE-RUN（锁内，`node tooling/run-locked.mjs --ticket 513 -- …`）

- `node node_modules/typescript/bin/tsc -b packages/base-render`：exit 0
- `node --test blocks.test.mjs style.test.mjs ui-fix-154.test.mjs`：109／109，fail 0，exit 0
- `pnpm gen:check`：PASS（129 键）
- `node packages/skill-calorie/scripts/check-warning-line.mjs`：红，但与本票无关——扫描面是 skill-calorie，树上他席未提交改动（`scene-10-analysis.ts`、`volatilityDoc.ts`）致台账行数不符；本票写集不含技能件，未跑 `--sync`（跑了会动他席文件，纪律不许）。

## 产物判据（机器三条）

- 同级段标题同字号同字重：样板页仍红（caption 12px／600＠512、13px／600＠820＋ vs 三 h2 15px／700）——预期内：页面未迁（`homeDocs.ts` 不动），槽级已绿，归一待页面票。实测逐字在 `.scratch/t513/读数.log`。
- 表格行标记窄屏不横溢：`PASS`（上文）。
- 同排卡徽章底对齐：`PASS`（上文，四档 0px）。
- 样板重出：`.scratch/t513/看今日主页.html`（真 CLI，SEED_TODAY=2026-09-07，82393 bytes，三槽验戳 4／4）；夹具 `.scratch/t513/夹具.html`；实拍 `实拍-512／820／1000／1440.png`；量具 `.scratch/t513/量徽章.mjs`、出片 `.scratch/t513/出片.mjs`、夹具脚本 `做夹具.mjs`。

## 未做项（交编排者）

1. 样板页四标题归一（判据 3-① 样板侧）待页面票迁 `homeDocs.ts`（grid 标题槽＋表 caption／段标题分槽）；本票只保证槽可用。
2. 用户肉眼终审（判据第 4 段）：四档实拍已落 `.scratch/t513/`，请编排者送审。
3. `check-warning-line` 外部红：待他席落定后自愈（或由编排者协调重跑），本票不碰。
4. 遗留出口（票面原样，不占新票）：行标记其它形态待页面票定；表 caption 双重身份记账；其它页同型债由 #508 立规后逐页认领；窄屏标记若挤位改形态（当刻 512 界内有余量）。
