# #513 独立复核回执（复核席，与实施无交集）

- 判定：**PASS**——三槽（行标记／grid 标题位／徽章贴底）可用，缺省零渲染成立；两处红（样板页标题档、check-warning-line）均确系范围外。
- 复核位：实施提交 `d89dc3da`（blocks.ts ＋62／−7、blocks.test.mjs 新断言、证据件）；复核未碰源码、未碰他人证据与他席脏件（`scene-10-analysis.ts`＋3 行、`smoke.test.mjs` 在途）。

## 机器证据（锁内 `node tooling/run-locked.mjs --ticket 513 -- …`）

| 门 | 结果 | runId／exit |
|---|---|---|
| build `tsc -b packages/base-render` | 绿 | `5a61b03a` exit 0 |
| 三套单测 blocks／style／ui-fix-154 | **109／109** fail 0 | `0bb1d908` exit 0 |
| `pnpm gen:check` | PASS，129 键（写47＋读82） | `3661efb5` exit 0 |
| `check-warning-line.mjs`（无参） | 红 **87／91**，4 条全是台账陈化（见下） | `7d19c2ed` exit 1 |
| 自设新探针 `.scratch/t513r/probe-513.mjs` | **28／28** | `0a892151` exit 0 |

## 自设新探针（实施证据未覆盖，4 组）

- P1 行标记越界：超长 marker（40 字）徽标恰 1、落首格内、表结构仍 3td／2tr；marker 转义前插与 `cellHtml` 受信透传共存（透传不被二次转义）。（6 项）
- P2 列键 `marker` 碰撞：列含 `marker` 键时徽标与单元格各产出一次（`<span>M1</span>M1`，互不吞）；空串 marker 零渲染但该列空值格标签仍在。（4 项）
- P3 标题槽边界＋caption 身份：`{}`／`{title:''}` 与缺省逐字节等；`opts null／[]`→bad-input；`title:数字` 静默零渲染＝与 `renderChartBlock` 同口径（parity，非缺陷）；caption 走 `<caption>`、grid title 走 `<h2>`，互不串台。（7 项）
- P4 贴底非等高卡：卡片纵向 flex 列且无 `align-items:center` 收窄；徽章 `margin-top:auto＋padding-top:8px`；网格 `grid-auto-rows:1fr` 仍在；单卡徽章仍产出；两卡（有／无 detail）徽章各一；合成旧串（`margin-top:8px`）通不过本探针＝改坏必红成立。（8 项）
- F0 冻结集：样式区仍 12 项、无 `--r-xl／--pink`、行徽标只走 `--soft／--blue2`。（3 项）
- 变异说明：写集禁改源码，故未做源码级变异；以合成旧串灵敏度等效自证。探针途中两次红均为探针侧 bug（import 路径误指 index；badge 误写 `badge:{text}` 正形为 `status/statusText`），修正后 28／28，与实现无关。

## 缺陷清单（引入／范围／范围外）

- 票引入缺陷：**无**。三槽缺省逐字节等（P1–P3＋109 测试），非法入参沿 `badInput` 旧口径，转义沿 `esc` 旧口径。
- 范围内观察（非缺陷）：`title`/marker 的数字与空白容忍走 `optText` 旧口径，与 chartBlock／copy 族一致；`opts` 多余非 on 字段静默接受，亦与同族一致。
- 范围外① 样板页标题样板侧红：页面未迁——`homeViewParts.ts:153,213,229`（caption＋手写 h2“标题由本页自产”）、`goalProgressDocs.ts:305,357,368`（caption＋手写 h2＋裸 `renderKpiGrid`）、`homeDocs.ts:270-273`（裸调 grid）。`d89dc3da` 写集无页面件，归一待页面票，确属页面票。
- 范围外② `check-warning-line` 红：4 条台账陈化——`build-help.mjs` 744→743、`sportDocs.ts` 434→466、`planEditorRuntime.ts` 425→426（×2 行悬记）。`d89dc3da` 写集无 skill-calorie 件；在途 `scene-10-analysis.ts`（LF184，未超线）非红因。证据件归因文件名部分陈化但“他席”结论成立，本票禁跑 `--sync`（会动他席文件）。
- 范围外③ `blocks.ts` 2406 行超 350 线：历史沉淀，证据件已报第四步（报警非拦路），本票 ＋62 非引入。

## 路径

- 实施：`packages/base-render/src/blocks.ts`（grid 槽 616–637、行槽 651–654／707–741、贴底 1652–1706／行徽标 1793–1808）、`packages/base-render/test/blocks.test.mjs`（513 断言 171–184／249–265／608–640）、`docs/base/base-render/t513-公共层三槽-证据.md`
- 复核：本件＋`.scratch/t513r/probe-513.mjs`（gitignored，不随仓）

复核止于此，停止等指派。
