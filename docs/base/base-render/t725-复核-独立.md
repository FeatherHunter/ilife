# t725 · 文档骨架件（docShell 抽取）—— 独立复核

- 票：[公共层] 文档骨架件：docShell 抽取，两侧 docPage 缩成薄页头件（[#725](https://github.com/FeatherHunter/ilife/issues/725)）
- 本件性质：**独立复核席证据**（对抗式；实施者不兼任自己票的审查者）
- 被审冻结点：`59e0a82a`（公共层新件 ＋ 子路径出口 ＋ 夹具测试 ＋ 卡路里侧改走）／`0390c9f8`（账单侧改走 ＋ 骨架件加 doctype 写法位）。两笔均已提交、**未推**（`git branch -r --contains` 两笔皆空）。
- 被审件：`packages/base-render/src/docShell.ts`、`packages/base-render/test/doc-shell-725.test.mjs`、`packages/skill-calorie/src/shared/docPage.ts`、`packages/skill-bill/src/shared/docPage.ts`、`packages/base-render/package.json`
- 规格正本：`docs/skills/skill-bill/t685-接口与判据.md` §一／§二
- 本席读到的读数**全部自跑**；实施者证据件（`t725-文档骨架件-证据.md`／`t725-gate-runs.md`／`t725-equivalence.mjs`）只当线索与对账源，数字一律不采信。
- 工作区纪律：一切会动工作区或产物的命令都经 `node tooling/run-locked.mjs --ticket 725 -- …` 排队；未用 `git stash／reset／checkout -- .／clean`，未切分支，未提交。临时脚本全在 `.scratch/t725-review/`。

## 一、判定

**PASS**（四条判据全部对上；S1 计 0）。

扣分去向（均落在**证据件**，不在被审件）：证据真实 82（§三 行 12 的「全绿」与自己的运行导出件对不上，两条 exit=1 未登记）、文档同步 88（§四「唯一解法」这一步没写出来，读者复算不出来）。契约一致 92／新旧对照与变异强度 95／工程红线 100，均分 91.4。

## 二、机器证据（本席跑过的每一条命令）

全部经 `tooling/run-locked.mjs --ticket 725 --` 排队。除注明外均在仓根跑。

| # | 命令 | 退出码 | 摘要（机器读数原文尾行） |
|---|---|---|---|
| 1 | `node node_modules/typescript/bin/tsc -b packages/base-render packages/skill-calorie packages/skill-bill --force` | 0 | 无诊断输出；`RESULT: … exit=0` |
| 2 | `node docs/base/base-render/t725-equivalence.mjs --side calorie --old 9b0b9be8` | 0 | `EQUIV: 相同=26244 不同=0 共=26244` ／ `RESULT: PASS 穷举逐字节相同` |
| 3 | `node docs/base/base-render/t725-equivalence.mjs --side bill --old d78bb116` | 0 | `EQUIV: 相同=24 不同=0 共=24` ／ `RESULT: PASS 穷举逐字节相同` |
| 4 | `node packages/skill-bill/scripts/gen-page-fingerprints.mjs --check` | 0 | `渲染 32/32 张` ／ `RESULT: 32/32` ／ `PASS: 32 张页指纹与账本一致` |
| 5 | `node --test packages/base-render/test/doc-shell-725.test.mjs` | 0 | `tests 11 / pass 11 / fail 0` |
| 6 | `node .scratch/t725-review/probeA-boundary.mjs` | 0 | `PROBE-A 合计=46 反例=0` |
| 7 | `node .scratch/t725-review/probeB-reverse.mjs` | 0 | 变异：`EQUIV-mut exit=1`（`DIFF #3 … old=ce4062897ddc8b84 new=039116fe55d25c10`）、`FP-mut exit=1`（`RED 页指纹变了：恢复-回执`／`RESULT: 0/32`）；还原：`RESTORE sha 相同=true`、`EQUIV-restored exit=0`（24/24）、`FP-restored exit=0`（32/32） |
| 8 | `node .scratch/t725-review/probeC-delete-audit.mjs` | 0 | 删件态：`BUILD-away exit=2`（两侧各一条 `TS2307: Cannot find module 'base-paint/docShell'`）、`FIXTURE-away exit=1`、`EQUIV-away exit=1`；还原态：`BUILD-restored exit=0`、`FIXTURE-restored exit=0`；`RESTORE src sha 相同=true`、`RESTORE dist 逐件 sha 相同=true` |
| 9 | `node .scratch/t725-review/probeD-forcing-audit.mjs` | 0 | 见 §四 与 §五 |
| 10 | `node --test packages/skill-bill/test/*.test.mjs` | 0 | `tests 337 / pass 337 / fail 0` |
| 11 | `node --test packages/base-render/test/*.test.mjs` | 1 | `tests 762 / pass 761 / fail 1`——唯一红是 `技能面 base-link-core runtime range…`／`#79 base-* 三包版本 lockstep`，**范围外** |
| 12 | `node --test packages/skill-calorie/test/*.test.mjs` | 1 | `tests 1855 / pass 1820 / fail 35`（基线未复跑，见 §六） |
| 13 | 存量门逐条（只读类） | — | bill：`check-warning-line` 0／`check-scene-shape` 0／`gen-cli --check` 0／`check-ratchet-tight` 0／`ratchet-frozen-686` 0；calorie：`check-warning-line` 0／`check-page-assert` 0／`check-one-path` 0／`gen-cli --check` 0；仓根：`check-boundaries` 0／`skill-html-snapshot --check` 0 |
| 14 | `node packages/skill-bill/scripts/check-page-assert.mjs` | **1** | `Error: Cannot find module '…\packages\skill-bill\scripts\check-page-assert.mjs'`——skill-bill **没有**这道门（见 §五 D2） |
| 15 | `node packages/skill-calorie/scripts/check-ratchet-tight.mjs` | **1** | 同上，skill-calorie **没有**这道门（见 §五 D2） |

只读核对（未动工作区）：

- 被审五件终态与 `HEAD` 逐字节相同（`git hash-object` 对 `git rev-parse HEAD:<路径>`，五对全等）。
- `packages/skill-calorie/src/HEAD` 相对 `9b0b9be8` 只有 `src/shared/docPage.ts` 一件变化；`packages/skill-bill/src` 相对 `d78bb116` 只有 `src/shared/docPage.ts` 一件变化。
- `packages/skill-bill/src/shared/pageShell.ts` 在 `d78bb116`、`HEAD`、工作区三处的 blob 哈希同为 `f795e09ff4f9d0a544f34d9bbae3b64cfc76cb9e`。
- `9b0b9be8`／`d78bb116` 都是 `HEAD` 的祖先（`git merge-base --is-ancestor` 两处 True）。

## 三、四条判据对账

| 判据 | 结论 | 读数 |
|---|---|---|
| 1 · 产物逐字节相同（两侧） | **对上了** | 卡路里穷举 26244/26244 逐字节相同（命令 2）；账单穷举 24/24（命令 3）；账单真出口 32 张页指纹 32/32（命令 4）；骨架件夹具 11/11（命令 5）；自造边界探针 46 例 0 反例（命令 6） |
| 2 · 调用点一行不碰 | **对上了** | 卡路里 `assembleDocPage(` 调用点：`9b0b9be8` 与 `HEAD` 都是 115 行命中减去 1 行定义＝**114 处**；且 `9b0b9be8..HEAD` 在 `packages/skill-calorie/src` 下只有 `docPage.ts` 一件变化 ⇒ 114 处所在行一字未动。账单 `pageShell.ts` blob 哈希改前／HEAD／工作区三处相同 ⇒ 它与其 34 处 `pageShell(` 调用点一字未动 |
| 3 · 删件判据（铁律五） | **对上了** | 见 §四 |
| 4 · 公共层零领域词 | **对上了** | 见 §四 |

## 四、自造探针（实施者脚本盖不到的面）

### 探针 A · 边界入参、槽位字面量、断标签串（`.scratch/t725-review/probeA-boundary.mjs`）

两段。**A1 页头件层**：把改前那一份 `docPage.ts`（`git show <ref>:…` 落成包内临时副本，跑完即删）与当刻 `dist` 那一份对照，12 例卡路里 ＋ 6 例账单，逐例比整串：

- 非布尔真值：`charts: 1`／`charts: []`／`charts: 'false'`、`pageUi: 'yes'`／`{}`／`'true'`、`printable: 0`／`[]`／`'yes'`——两侧产物逐字节相同。
- 非字符串给字符串位：`metaLeft: 123`／`badge: null`／`summary: 0`／`eyebrow: 42`／`subtitle: 0`／`title: undefined`——相同（其中 `title: undefined` 两侧**同一条** `BlocksError: renderPageShell: input.title 必须是非空字符串`）。
- 槽位字面量嵌进正文与标题（`<!--SHARED-CSS-->`／`<!--CONTENT-->`／`<!--SHARED-HELPERS-->`／`<!--CHARTS-HELPERS-->` 四件同串）、断标签串（`</style>`／`</head>`／`</body>`／`</title><meta …>`）、冻结五字符表（`&<>"'`）、`String.replace` 的替换模式字符（`$&`／`$'`／反引号／`$$`）——相同（例：卡路里 A线带断标签串 sha `39f956a46dcef074` 67144 字节，B线 `ba7bf5e22b288063` 68430 字节，账单断标签串 `52b9e7d50e918ee7` 69589 字节）。

**A2 骨架件层**（28 例）：

- `extraCss` 给空串／`null`／`undefined`／`0`／`false`——产物与空串**同一串**（sha `379baabef6b66ecc`），且空 `extraCss` 时段尾恰在 `blocksCss()` 之后（不多出空段）。
- 敌意 `extraCss`（含 `$&` 家族、`</style><script>evil()</script>`、NUL）**逐字进样式槽**，不转义、不改写；正文与 `<title>` 里的 `$&` 家族原样落盘（`fillTemplate` 用 `indexOf` 拼接、不走 `String.replace`，此处得到机器确认）。
- 正文里的四个槽位字面量**原样保留**、不触发二次填充。
- `charts`／`pageUi` 给 `1`／`'yes'`／`[]`／`{}`／`'false'`——与不给**同串**（只认真真值）。
- `doctypeCase` 给 `{}`／`0`／`'UPPER'`／`'lower'`／`' upper'`／`true`／`[]`——一律落回小写；`'upper'` 与缺省**只差那一处**。
- 缺 `docTitle` 时字面 `undefined` 进 `<title>`（与改前同形，未新增护栏）。

结论：探针 A 46 例，**0 反例**。

### 探针 B · 反向验证（把实施者那份穷举脚本变成能变红的）

在 `packages/skill-bill/src/shared/docPage.ts` 里把 `TOAST_CSS` 从 `extraCss` 的四段里去掉（少拼一段补丁样式），强编后：

```
BUILD-mut exit=0
EQUIV-mut exit=1 :: DIFF #3 … old=ce4062897ddc8b84(69059) new=039116fe55d25c10(68556) | RESULT: FAIL
FP-mut exit=1 :: RED 页指纹变了：恢复-回执 | RESULT: 0/32
RESTORE sha 相同=true sha=54a7df4b4bc961b7
BUILD-restored exit=0
EQUIV-restored exit=0 :: EQUIV: 相同=24 不同=0 共=24 | RESULT: PASS
FP-restored exit=0 :: RESULT: 32/32 | PASS: 32 张页指纹与账本一致
```

「改坏红／还原绿」两态四行读数齐。还原后强编（`--force`），避开证据件 §三 自己记下的那口坑（`Copy-Item` 带回旧 mtime ⇒ 增量判定认为无需重编 ⇒ 假绿）。

### 探针 C · 判据 3 删件审计（`.scratch/t725-review/probeC-delete-audit.mjs`）

**① 挪走**：把 `packages/base-render/src/docShell.ts` 连同它的四件 dist 产物（`docShell.js`／`.js.map`／`.d.ts`／`.d.ts.map`）一起挪走。读数：

```
BUILD-away exit=2 :: packages/skill-calorie/src/shared/docPage.ts(20,32): error TS2307: Cannot find module 'base-paint/docShell'
                     packages/skill-bill/src/shared/docPage.ts(18,32): error TS2307: Cannot find module 'base-paint/docShell'
FIXTURE-away exit=1（夹具测试整件 'test failed'）
EQUIV-away   exit=1（卡路里等价脚本塌）
RESTORE src sha 相同=true / RESTORE dist 逐件 sha 相同=true
BUILD-restored exit=0 / FIXTURE-restored exit=0
```

两侧**各自**一条 `TS2307` 点名到自家 `docPage.ts` 的 `renderDocShell` 引入行——删掉骨架件，两侧都出不了页。这不是「一层只做转发的包装」。

**② 重复度度量**（回答「是不是真的得各自再写一遍」）：拿**改前**两侧各自的 `docPage.ts`，同一组入参各出一份整页文档：

```
DUP 两侧改前整页字节=67193 / 69138
DUP 前缀（doctype 小写化后）逐字节相同=true 前缀字节=162
DUP 后缀（</style> 起）逐字节相同=true 后缀字节=23742
DUP 前缀差异（doctype 小写化前）＝["<!doctype html>\n","<!DOCTYPE html>\n"]
```

即：改前两侧**独立写了两遍**的文档壳，除 doctype 大小写外逐字节相同（162 字节的文档头 ＋ 23742 字节的正文之后那一段）；两者唯一的实义差异是账单那四段补丁样式（69138−67193＝1945 字节，属账单自己的口径，本票未动）。骨架件持住的正是这段被写了两遍的常量与资产拼接口径。

**本席判断**：判据 3 成立，骨架件不白占一层。依据＝① 删掉它两侧的编译、夹具测试与等价脚本当场塌；② 它持的正是改前两侧逐字节重复的那一段（含 doctype 两写法、head 三槽位序、版面根、资产拼接公式、图表资源位）。

### 判据 4 · 公共层零领域词（独立扫描）

不用实施者夹具里那份从 `packages/` 派生的技能名清单，改由**本席自列 55 词**扫描 `docShell.ts`：26 个技能目录名（含去 `skill-`／`plugin-` 前缀形）＋ 29 个中文领域词与英文域名词（账单／记账／卡路里／饮食／热量／体重／身材照／菜谱／备忘录／作息／物品／快递／保修；`bill`／`calorie`／`chef`／`memo`／`schedule`／`home`／`diet`／`weight`／`photo`／`gif`／`exercise`／`workout`／`profile`／`ledger`／`account`／`category`／`budget`／`goal`／`review`／`receipt`／`insight`／`ranking`／`analysis`／`domain` 等）。

结果：**零命中**。件里出现的是骨架自己的词（`docTitle`／`bodyHtml`／`extraCss`／`charts`／`pageUi`／`doctypeCase`／槽位标记／`pageUi.ts` 的两个常量）。判据 4 对上了。

## 五、缺陷与登记项

### D1（**S2 · 本票范围 · 证据件**）证据 §三 行 12 的「全绿」与自己的运行导出件对不上

证据件 §三 行 12 写「两包门禁（warning-line／scene-shape／page-assert／one-path／gen-cli／ratchet）＋ 仓根 boundaries／snapshot-html：全绿」。实测：

- `packages/skill-bill/scripts/` 里**没有** `check-page-assert.mjs`（该门只有卡路里包有）；实施者自己的导出件 `t725-gate-runs.md` 第 86 行记着这条命令 `exit=1`。
- `packages/skill-calorie/scripts/` 里**没有** `check-ratchet-tight.mjs`（ratchet 两道门只有账单包有）；导出件第 110 行记着这条命令 `exit=1`。
- 本席复跑这两条路径，退出码同为 **1**（`MODULE_NOT_FOUND`）。

这两条 exit=1 在证据件 §六「范围外发现」里也没有登记。⇒ 行 12 那句话不能与它自己的对账源（协议 §2.3.4 的导出件）对上。

**注意**：这不是产物层面的假绿——两包**实际存在**的门本席逐条复跑全绿（见 §二 命令 13），四条判据也不依赖这两条不存在的路径。**最小修法**：行 12 改成「各包按自己有的那几道门全绿」，并把两条 `MODULE_NOT_FOUND` 按「门的名字在另一包、调用姿势错」登记进 §六。

### D2（**S3 · 本票范围 · 证据件**）§四「唯一解法」这一步没写出来，读者复算不出来

§四 称两位（`pageUi`／`doctypeCase`）是「判据 1 ＋ 判据 3 ＋ 铁律二 三条同时成立的**唯一**解法」。本席去找第三种解法（探针 D）：

- **窄口径反例成立**：骨架件不吃这两位，改由**调用方对自己的产物做字符串改写**（＝提示里说的「两侧各自保留一小段」），14 例里 **13 例**与骨架件内建那两位**逐字节相同**（素面／补丁样式嵌基线串／正文嵌字面量／带图表位／标题带五字符表／空正文，各两处；唯一不成立的是「补丁样式里嵌了版面根字面量」——`extraCss` 落在 `<style>` 里、位次在真正的版面根之前，改写会打错位置）。
- **但这条解法不满足 §四 自己列的三条**：它要调用方把骨架件的基线字面量抄一遍——`width=device-width,initial-scale=1`（骨架件里现 1 次）、`wrap ilife-page`（现 1 次）、重写目标 `<!DOCTYPE html>`；正是铁律二「同一个常量在两个文件里各写一份」的形状。另外 needle 不在时改写**静默原样返回**（无护栏，注入式失效）。

⇒ 结论不变：骨架件吃这两位是**对的**；不足只在措辞与论证的完备性——「唯一」这一步的依据（铁律二排除改写解）没有落成文字，读者从件里复算不出来。**最小修法**：§四 补一句「改写解法要把骨架件的三个基线字面量抄到调用方，落在铁律二禁止的形状上」。

### D3（**S3 · 本票范围 · 规格与门的口径**）「对外只这一个名字」在类型面上不成立

规格 §一 写「对外名 `renderDocShell`：一个文件对外只这一个名字」；夹具测试断的是**运行时**键（`Object.keys(mod)` 恰 `['renderDocShell']`）。当刻 `docShell.d.ts` 的类型出口是三个：`DocShellDoctypeCase`／`DocShellInput`／`renderDocShell`。

**判断**：不构成违规——铁律五的数字是「一个文件对外不多于五个」，同包先例 `pageUi.ts` 也是「一个 interface ＋ 一个函数」并列导出。但规格那句话与门的口径要对齐：要么把规格改成「运行时对外只这一个名字」，要么给夹具加一条 `.d.ts` 出口面断言。

### 登记项（**范围外**，只记不处置）

- **R1**：公共层既有件 `pageUi.ts`／`style.ts`／`charts.ts`／`controls.ts` 的**注释**里含技能名与领域词（`卡路里`／`skill-calorie`／`记账`／`weight` 等，逐行核对全是注释里的出处指针，非代码与出口）。本票判据 4 只管 `docShell.ts`（已零命中），这些是既存事实，非本票引入。
- **R2**：卡路里侧「改前留档」类断言当刻已因他票改动而红。实测 `#448 判据①：改前留档在场 → 三件样本逐字节相同` 的差异是 `<title>卡路里 运动回执</title>` 对 `<title>卡路里·运动回执</title>`、`.ilife-toast` 字号 `13px` 对 `12.5px`——doctype 两侧都是小写、与骨架件无关。⇒ 该门当刻**不是**本票判据 1 的有效读数；实施者改用穷举等价与真出口取数，方向正确。
- **R3**：`packages/base-render/test/base-version-lockstep.test.mjs` 一族当刻红（`skill-calorie` 的 `base-link-core` 已住 `dependencies`，断言仍按 `devDependencies` 找）——陈化，非本票引入。
- **R4**：本票给 `base-paint` 加了新子路径 `./docShell`；两个技能此后按 `base-paint/docShell` 取件 ⇒ 发布这两个技能之前 `base-paint` 必须先发一版带该子路径的。这条实施者已在证据 §六 登记，本席复核其成立（属发版线）。

## 六、未做项与下一手

**未做**（说清缺什么）：

1. **卡路里真出口 87 张页「改前／改后同 sha」未复跑**——它要一份改前基线，而本席不许切分支／还原工作区。替代读数：账单侧同口径的真出口门本席跑成 32/32（逐字节比账本），卡路里侧由 26244 例穷举等价盖住 `docPage` 这一整条变换轴。
2. **卡路里套件「改前 37 红」基线未复跑** ⇒ 实施者那句「新增红 0」**未独立复核**。本席当刻读数是 `fail 35`（与它写的 36 不同，共享工作区在他那趟之后又落了别票的改动）。替代读数：本席把 35 条失败的断言消息逐条看过，**没有一条**提及骨架件、`docShell` 或 doctype 大小写；产物轴又已被穷举等价与账单指纹门证明逐字节不变。
3. 证据 §三 行 12 里若干门（scene-shape／one-path／gen-cli／ratchet）的**逐条**复跑本席做了（§二 命令 13，全绿），但 `gen-sot-snapshot --check`／`check-examples`／`audit-separators` 三条手工门未复跑（实施者已登记为手工门缺夹具，本席未追加读数）。

**下一手**：

1. 收口前把 D1／D2 两句按上面的最小修法订正（只改实施者证据件，不动被审件）；D3 二选一（改规格措辞或给夹具加 `.d.ts` 出口面断言）。
2. R4 的发版顺序归发版线，发布前确认 `base-paint` 新版已带 `./docShell`。
3. 本席未改任何被审件：五件终态与 `HEAD` 逐字节相同（§二 只读核对）；临时变异与挪件全部还原（`RESTORE sha 相同=true` 两处）。工作区里 `packages/skill-bill/docs/`（未跟踪）是别席的件，本席未碰。
