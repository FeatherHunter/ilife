# #430 独立复核报告（复核席，与实施无交集）

判定：PASS（无 S1、无 S2；S3 共 3 条，不阻断关闭）。

被复核提交：`4ab168ea566290b27c6de34627fcf1945362ae77`（`feat(430)`，2 件，与回执声称一致）。
复核时刻工作区说明：树上有他席在途未提交改动（`blocks.ts`、`text.ts` 等），复核未碰其中任何一件；下文凡涉及行数处同时给出本次复量值与被复核提交时刻值，漂移部分单独点名。

## 1. 机器证据（独立复跑，非采信）

被复核回执声称的两门，留痕与复跑如下：

- 声称 GATE-RUN `runId=fc501603-2f1c-46d5-8414-44e96ff0e49f`，命令 `node node_modules/typescript/bin/tsc -b packages/base-render`：在 `.scratch/locks/gate-runs.log` 第 20185–20186 行找到 START／RUN 对，`exit=0`，票号与命令逐字对上。独立复跑（本次新标识）`runId=7a6277d5-3aca-4f3d-a953-ca8acab1c5cc`，`exit=0`（落盘见 `t430-复核-gate-runs.md`）。
- 声称 GATE-RUN `runId=939a730a-9ba5-48a8-aaa9-b0096ff06e3e`，命令 `node --test packages/base-render/test/blocks.test.mjs`：在同日志第 20189–20190 行找到 START／RUN 对，`exit=0`，票号与命令逐字对上。独立复跑（本次新标识）`runId=d5278cca-3f89-4809-aff9-eac73fb3d41f`，`tests 50／pass 50／fail 0`，`exit=0`。
- 复跑时树上 `blocks.ts` 已被他席改动（复量 2309，比被复核值 2304 多 5），两门仍绿，说明回执“结论不变”成立。

## 2. 行数复量（LF 口径，只数 `\n`，`split('\n').length - 1`）

- 被复核提交树 `blocks.ts` 复量 2304，与回执“当刻 2304”逐字一致；`charts 2154／controls 1492／style 1383／help 687／pageShapes 452／template 430／helpShell 83` 在本次复量中逐字一致。
- 本次工作区复量与回执的差异仅两处：`blocks.ts 2309（＋5）`、`text.ts 497（回执 492，＋5）`，均为他席在途改动所致漂移；超线与否的结论 15 件全部不变（8 超 7 内，`helpShell` 线内）。
- 15 件均不含 `\r`（LF 纯净），数法有效；超线分类逐件核对无误。

## 3. 新探针（被审脚本覆盖不到）

- P1 出口清单探针（只读）：`src/blocks.ts` 中 `export function render*／blocksCss` 实测 21 个（`render*` 20 个＋`blocksCss`），`BLOCK_STYLE_SECTIONS` 命中 12 处。回执写“约 19 个 `render*`”，以“约”字兜底不算错，但精确数应为 20（见 D1）。
- P2 宽门探针（持锁）：回执门禁只跑了 `blocks.test.mjs`（50 项）；复核加跑 `charts＋controls＋style＋contract-signatures` 四件合并一次运行，新标识 `runId=c7cbe4fb-0f48-4935-89dd-4a546d0080ed`，`tests 232／pass 232／fail 0`，`exit=0`。该探针专打“窄门自我满足”盲区，通过。
- 未改源码故复核亦未做源码变异；P2 以宽门探针代替，满足“至少 1 个新探针”。

## 4. 缺陷清单（均标注归属与分级）

- D1［本票范围，S3］：评估 §3.1“对外出口约 19 个 `render*`”与实测 20 个 `render*`（＋`blocksCss` 共 21）差 1。有“约”字兜底，不证伪结论；建议拆件票按 20 个为准。下一手：拆件票直接采用 P1 清单。
- D2［本票范围，S3］：`AGENTS.md` 范围行只写“生成物（`dist/`、`.tsbuildinfo`）与页面模板不算”，未点名 `src/helpShell.ts` 系生成物；评估正文明确其“不计入手写面”。按字面读 `AGENTS.md` 会把该件计入，而该件当刻 83 行线内，计与不计结论不变。建议后续小修时在 `AGENTS.md` 补半句点名该件。
- D3［范围外发现，S3，转票措辞］：`docs/agents/structure.md` 第 70 行“超线即触发第五步”与其“第四步·超线报警”标题互相不一致；本票取第四步正确（与标题一致）。本条不阻断 #430，记账跟进，建议另开一票修该行。
- 禁区与过程：被复核提交 `git show --stat` 仅含回执声称的两件；未见他票文件，未见危险操作痕迹。用词抽查两件未见禁止清单内措辞。

## 5. 路径

- 改动件（被复核）：`docs/base/base-render/行数告警线评估.md`、`packages/base-render/AGENTS.md`。
- 复核证据件（本次新增）：`docs/base/base-render/t430-复核-报告.md`（本件）、`docs/base/base-render/t430-复核-gate-runs.md`（运行条目导出，可作对账源）。
- 草稿：`.scratch/t430r/探针-原始记录.md`（LF 全表、出口清单、测试尾行原文）。

## 6. 未做项与下一手

- 未做项无（复核只到“判据绿”，不拆件）。
- 下一手：拆件另开票（判据产物逐字节相同照 #332 口径），关票由编排者收；D1 的 20 个出口清单与 D2 的 `helpShell` 点名可直接带入拆件票。
