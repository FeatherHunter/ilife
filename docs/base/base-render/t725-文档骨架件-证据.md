# t725 · 文档骨架件（docShell 抽取）—— 证据

- 票：[公共层] 文档骨架件：docShell 抽取，两侧 docPage 缩成薄页头件（[#725](https://github.com/FeatherHunter/ilife/issues/725)）
- 父图：[wayfinder 饼干记账本体图](https://github.com/FeatherHunter/ilife/issues/681)（`wayfinder:task`）
- 规格：#724 收口件 `docs/skills/skill-bill/t685-接口与判据.md` §一／§二（接口纸样与两道门）
- 本件性质：执行票证据（公共层新件 ＋ **两侧**改走它）
- 本件状态：**两侧已落、读数已取**；收口前还需**独立复核席**两轮对抗式复核（实施者不得兼任自己票的审查者）

## 零、一句话判定

公共层新件 `packages/base-render/src/docShell.ts` 落地，两侧 `docPage.ts` 改走它：**两侧产物逐字节相同**（卡路里穷举 26244 例、账单穷举 24 例、账单真出口 32 张页指纹门、卡路里真出口 87 张页、两包套件零新增红），调用点一行未碰，公共层零领域词。

## 一、交付物（改前 → 改后）

| 件 | 改前 | 改后 | 说明 |
|---|---|---|---|
| `packages/base-render/src/docShell.ts` | 无 | **新件**，对外只 `renderDocShell` 一个名字 | 骨架：doctype／head 三槽位序／版面根／资产拼接／图表资源位 |
| `packages/base-render/package.json` | 无 `./docShell` | `"./docShell": "./dist/docShell.js"` | 子路径出口（照 `./blocks`／`./help-shell` 先例；**主入口 `src/index.ts` 一行未动**——那是冻结契约面） |
| `packages/base-render/test/doc-shell-725.test.mjs` | 无 | **新件**，11 例 | 夹具驱动，**不依赖任何技能**（票面产出第 3 条） |
| `packages/skill-calorie/src/shared/docPage.ts` | 161 行，自持整页模板 | **149 行**，改走 `renderDocShell` | 11 字段与 A／B 双路一行未动；`BLINE_CSS` 去掉首字符那个换行（改由骨架件补） |
| `packages/skill-bill/src/shared/docPage.ts` | 130 行，自持整页模板 | **113 行**，改走 `renderDocShell` | 5 字段一行未动；四段补丁样式走 `extraCss`；`pageShell.ts` 与其调用点一行未碰 |

## 二、判据逐条自证

### 判据 1 · 产物逐字节相同

| 读数 | 命令 | 结果 |
|---|---|---|
| 卡路里·穷举等价 | `node docs/base/base-render/t725-equivalence.mjs --side calorie --old 9b0b9be8` | **26244/26244 逐字节相同** |
| 账单·穷举等价 | `node docs/base/base-render/t725-equivalence.mjs --side bill --old d78bb116` | **24/24 逐字节相同** |
| 账单·真出口指纹门 | `node packages/skill-bill/scripts/gen-page-fingerprints.mjs --check` | **32/32 绿**（账本一字未改；改前基线同为 32/32） |
| 卡路里·真出口 87 张页 | `node packages/skill-calorie/test/t380-真出口.mjs --all` 改前／改后各一遍 | **87/87 同 sha 同 bytes** |
| 卡路里·套件 | `node --test packages/skill-calorie/test/*.test.mjs`（改前／改后各一遍） | 改前 37 个失败用例／改后 36，**新增红 0** |
| 账单·套件 | `node --test packages/skill-bill/test/*.test.mjs` | **exit 0，零红** |

穷举口径写在那份脚本的件头：入参取**全部字段的组合**（卡路里 9 轴 → 26244 例，含 `charts`／`pageUi`／`printable`／A线／B线／眉标筛除／doctype 大小写；账单 4 轴 → 24 例），逐例比整页 sha256。改前那一份从版本库取（`git show <ref>:…`）落成包内临时副本，跑完即删。

### 判据 2 · 调用点一行不碰

- 卡路里 `assembleDocPage(` 调用点：改前 114 处 → 改后 **114 处**（`Get-ChildItem packages/skill-calorie/src -Filter *.ts | Select-String "assembleDocPage\("`）。
- 账单：`src/shared/pageShell.ts` 未改，`pageShell(` 调用点未改（`git diff --name-only` 账单侧只出现 `src/shared/docPage.ts` 一件）。
- 两侧 `git diff --stat` 都只有各自那一件 `docPage.ts`（见 §三 提交清单）。

### 判据 3 · 删件判据（铁律五）

删掉骨架件，调用方要各自再写一遍：doctype 起／`</html>` 止、head 的 charset→viewport→title 序、版面根、样式槽与两个 helpers 槽的位次、资产拼接（`style + '\n' + blocksCss + ['\n' + extraCss]`）、图表资源位、doctype 写法表。新测试把这八件逐条钉住。

### 判据 4 · 公共层零领域词

- 件内**零技能名**：新测试把「技能名清单**从 `packages/` 目录派生**（`skill-*`／`plugin-*` 去前缀）再在件正文里找」当断言——手写清单会过期，派生清单随目录增减自动跟上。
- 出处指针（规格件路径、两侧调用件）**故意不写进件里**，落在本证据件：件头只留机制口径（`pageUi`／`charts`／`extraCss`／`doctypeCase` 都是公共层自己的词）。
- 骨架件用到的页面级常量（`PAGE_UI_VIEWPORT`／`PAGE_UI_CLASS`）取值仍只由 `base-render/src/pageUi.ts` 定义。

## 三、机器读数与提交

运行标识与命令的逐条对应见 `t725-gate-runs.md`（协议 §2.3.4 导出件：`.scratch/locks/gate-runs.log` 里 `ticket=725` 全部条目逐字抄录）。本证据件里声称跑过的每次运行，都能在其中找到一对一对应条目。

| # | 读数 | 退出码 | 摘要 |
|---|---|---|---|
| 1 | 编译（三包 `--force`） | 0 | — |
| 2 | 骨架件夹具测试 | 0 | `pass 11 / fail 0` |
| 3 | **变异自证·改坏**（去掉图表资源位 ＋ 强编） | **1** | `pass 8 / fail 1` |
| 4 | **变异自证·还原**（＋强编） | **0** | `pass 9 / fail 0`；件 sha 与备份逐字节相同（`7e904a29c2b2a594`） |
| 5 | 卡路里穷举等价 | 0 | 26244/26244 |
| 6 | 账单穷举等价 | 0 | 24/24 |
| 7 | 账单指纹门（改前／改后） | 0 / 0 | 32/32 ／ 32/32 |
| 8 | 账单套件 | 0 | 零红 |
| 9 | 卡路里真出口 87 页（改前／改后） | 1 / 1 | 逐页 sha 87/87 相同；该件自身 2 条红改前改后一字不差（见 §六） |
| 10 | 卡路里套件（改前／改后） | 1 / 1 | 37 / 36 个失败用例名，**新增红 0** |
| 11 | base-render 包门 | 1 | 唯一红＝`base-version-lockstep.test.mjs:110` 的**陈化**断言（见 §六） |
| 12 | 各包**自己有的那几道门**：账单 `check-warning-line`／`check-scene-shape`／`gen-cli --check`／`check-ratchet-tight`／`ratchet-frozen-686`；卡路里 `check-warning-line`／`check-page-assert`／`check-one-path`／`gen-cli --check`；仓根 `check-boundaries`／`skill-html-snapshot --check` | 全 0 | 逐条命令与 exit 码见 `t725-gate-runs.md`。**订正（独立复核席 D1）**：本席曾按「两包各有一道 page-assert／ratchet」的对称假设跑过两条**根本不存在**的脚本（账单 `check-page-assert.mjs`、卡路里 `check-ratchet-tight.mjs`，两次都是 `MODULE_NOT_FOUND` exit=1），上一版这一行写成「两包门禁全绿」与自己的对账源对不上——已按实况登记进 §六 |
| 13 | 卡路里真出口 87 张页·**最终构建** | 1 | 与改前基线逐页比 **87/87 同 sha**（含 `doctypeCase` 位加入之后复跑） |

**一个坑，如实记**（下次省一趟）：用 `Copy-Item` 还原源码会**连旧 mtime 一起带回来** ⇒ `tsc -b` 的增量判定「无需重编」，于是跑出来的还是**改坏版**的 dist —— 那次「还原绿」一度是假的。还原后必须 `--force` 重编；第 3／4 行的读数是**强编之后**取的。

## 四、与规格纸样的两处偏差（已按判据裁，可一句话反转）

规格 §一 的入参纸样是「4 字段（`docTitle`／`bodyHtml`／`extraCss`／`charts?`）、零可选开关」。把两侧骨架**逐字节摊开**后（探针：两侧 `dist` 骨架件逐个入参打印文档头），有两处骨架**必须**知道、纸样装不下：

1. **`pageUi`**（卡路里 11 字段之一）：它改 `viewport` 串与版面根类，两处都在骨架里；票面明写「卡路里**保留全部 11 个字段**」，40+ 个调用点在传 `pageUi: true` ⇒ 骨架件不吃这一位就没法「保留全部字段」。**这一位是票面自己推出来的**。
2. **`doctypeCase`**（`'lower'` 缺省／`'upper'`）：两侧现状写法逐字不同（一侧 `<!doctype html>`、一侧 `<!DOCTYPE html>`），而两侧都被自家断言钉死（卡路里 ~40 条「第 0 字节」硬断言；账单 3 条），两侧也都真跑在真出口上。

**本票的裁法（按判据本身）**：判据 1 是「两侧产物 sha256 不变」，判据 3 要求骨架件持有 doctype，铁律二要求常量的定义地唯一 —— 三条同时成立只有一种解法：**两种写法都住骨架件（一张 `DOCTYPE_HTML5` 表），调用方只给名字、不给字面量**。于是两侧字节全不变、判据 1 全绿，而「统一写法」这件事**没被顺手做掉**（它要动产物，是另一件事；真要做，就是一句 `doctypeCase` 的反转 ＋ 账单 32 张页的指纹账重录 ＋ 3 条账单断言改成大小写不敏感）。**反转成本＝一次 `--write --declare-layout-change` ＋ 3 行断言**。

**为什么没有第三种解法（独立复核席 D2 补齐的那一步）**：复核席去找过反例，找到一条窄口径能成立的——「调用方对自己拼好的整页做字符串改写」（14 例里成 13 例）。它被排除的理由有两条，都落在票面判据或铁律上：① 它要把骨架件的三个基线字面量（`width=device-width,initial-scale=1`／`wrap ilife-page`／doctype）**抄到调用方**，而这三处骨架件里各写一次 ⇒ 同一常量两个定义地，正落**铁律二**禁止的形状；② needle 不在时它**静默原样返回**（拿不到「找不到就当场炸」这条失败语义）。故两位是必要的。

## 五、独立复核（对抗式）与缺陷处置

**复核席判定：PASS**（S1 计 0；四条判据全部对上）。复核证据件 `t725-复核-独立.md`（复核席自己写、自己复跑；临时脚本在 `.scratch/t725-review/`）。它的独立读数：编译 0；穷举等价 26244/26244（卡路里）与 24/24（账单）；账单指纹门 32/32；夹具 11/11；自造探针 4 支（边界入参 46 例 0 反例／反向变异「改坏必红、还原必绿」／挪走骨架件两侧编译与测试全塌／逼出性审计）；存量门逐条复跑全绿；账单套件 337/337。

它给出的三条缺陷**全部落在本证据件、被审件零缺陷**，处置如下：

| 缺陷 | 级别 | 处置 |
|---|---|---|
| D1 证据 §三 行 12 与自己的对账源对不上（两条门在另一包、调用姿势错） | **S2** | 已订正：行 12 改写＋两条 `MODULE_NOT_FOUND` 登记进 §六 |
| D2 §四「唯一解法」缺依据 | S3 | 已补：把复核席找到的「字符串改写」反例与它的两条排除理由（铁律二／静默失效）写进 §四 |
| D3 规格「对外只这一个名字」是**运行时**口径，类型面出口 3 个 | S3 | 已处置：夹具测试把类型面出口**逐字点名**（`DocShellDoctypeCase`／`DocShellInput`，改动即变红），并按实况写进 §四 |

**复核席未独立复跑的两条读数**（它的工作区不许切分支／还原 ⇒ 拿不到「改前」基线）：卡路里真出口 87 张页「同 sha」、卡路里套件「新增红 0」。两条都是**实施者自跑**，为让第三方可复核，已把逐页 sha 与两份红名单落成 `t725-改前改后读数.md`（含确定性口径与诚实标注）。复核席对这两条给出的是**旁证**：它当刻的套件读数为 `fail 35`，且逐条看过 35 条断言消息，**无一条**提到骨架件／`docShell`／doctype 大小写。

## 六、范围外发现（只登记，不处置）

三条卡路里侧的手工门当刻红，**三条都与本票无关**（本票只动 `src/shared/docPage.ts` 一件，不碰声明面／取数面／示例数据）：

- `scripts/gen-sot-snapshot.mjs --check`：`快照漏了 1 条 entry_sha：goal_set_exercise…` —— 声明面与快照的对齐问题（该门读的是声明／路由面）。
- `scripts/check-examples.mjs`：若干条 `ERR 4 取数失败（缺失阻断）` —— 该门要一份铺底库与照片目录，属**手工门缺夹具**。
- `scripts/audit-separators.mjs`：要一个文件参数（本席按无参调用，是**调用姿势**问题，不是门红）。
- **两条「门在另一包」的调用姿势错**（独立复核席 D1）：账单 `scripts/check-page-assert.mjs` 与卡路里 `scripts/check-ratchet-tight.mjs` **都不存在**，本席按对称假设跑过，两次 `MODULE_NOT_FOUND` exit=1 —— 它们的运行条目就在 `t725-gate-runs.md` 里（证据与对账源对不上正是 D1 的由来，已订正）。
- **卡路里「改前留档」类断言当刻红**（复核席 R2）：差异是 `<title>卡路里 运动回执</title>` 对 `卡路里·运动回执`、字号 13px 对 12.5px —— 别的票改了这些文案与字号；**doctype 两侧都是小写**，与本票无关。
- `t380-真出口.mjs --all` 在**改前**就有 2 条红：`TOTAL 期望 169 实际 170`（写死的路由条数陈化）、`看整体趋势 :: 输出未消费 group=weight_calorie`——一道手工门在空转，值勤席位值得认领。
- `packages/base-render/test/base-version-lockstep.test.mjs:110` 断言 `skill-calorie` 的 `base-link-core` **devDependencies** 恰为 `^0.1.0`，而当刻它已住 `dependencies`（`^0.3.0`）⇒ 陈化红，非本票引入。
- **公共层既有件的注释含技能名／领域词**（复核席 R1）：`pageUi.ts`／`style.ts`／`charts.ts`／`controls.ts` 的注释里有（判据 4 只管 `docShell.ts`，本件已零命中）——另一件事，归收口票。
- **发版顺序**（归发版线）：本票给 `base-paint` 加了新子路径 `./docShell`，而两个技能从此按 `base-paint/docShell` 取件 ⇒ **发布这两个技能之前，`base-paint` 必须先发一版带该子路径的**（先例与判据见 `tooling/check-registry-exports.mjs` 件头：工作区有、registry 没有 ⇒ 第三方装完直接 `ERR_PACKAGE_PATH_NOT_EXPORTED`）。
