# t512 独立复核回执（复核席，与实施无交集）

复核对象：#512「图件轴标签字号断点跳变」，实施提交 `f5d27605`（`charts.ts` 留白区＋读数脚本＋`t512-轴标签字号-证据.md`）。
复核动作：票面＋三件既有证据（图表字号与刻度／表格列宽／最后一公里）已读；结论一字不采信，全部独立复跑。
判定：**PASS**（附 2 项范围外遗留观察，均已有编号出口，不 blocking）。

## 一、机器证据（全部经 `node tooling/run-locked.mjs --ticket 512 --`）

| 门 | 命令 | runId | exit | 结果 |
|---|---|---|---|---|
| ①编译 | `node node_modules/typescript/bin/tsc -b packages/base-render` | `32a7cd37-8f85-4e18-9de1-8dfb6d417ec4` | 0 | 绿 |
| ①测试 | `node --test packages/base-render/test/charts.test.mjs` | `2d610ee6-d78a-4e83-a117-3a21842ad081` | 0 | **pass 90／fail 0**（用例 90、命中 75：票面下限 88／74 只增不减 ✓） |
| 冻结token | `node --test packages/base-render/test/style.test.mjs` | `48311113-8d28-4f18-92a9-63713303c892` | 0 | pass 30／fail 0（11 token 闭集无增减 ✓） |
| ②四档 | `node docs/base/base-render/t512-轴标签-读数.mjs 512 820 1000 1440` | `0b056fe5-ddb1-465c-b89f-d8fbb6f07626` | 0 | 15.05≤16.03≤17.32＝17.32，四档≥15 ✓ 单调不降 ✓，与证据 §三逐值一致 |
| ③390 | `node docs/base/base-render/t512-轴标签-读数.mjs 390` | `f80c6256-8a18-4b56-8862-9436cd46ff1e` | 0 | 9 条标签 minGap=4.69≥0，左右沿 inside=true，与证据 §四逐值一致 |

冻结面抽查：`CHART_BREAKPOINTS` 签名仍含 `mobileMaxPx: 720`（`spec/index.ts:163`，一字未动）；
`f5d27605 --stat` 仅三件（`charts.ts`＋读数脚本＋证据件），`skill-*/src` 零命中；
`preserveAspectRatio="xMidYMid meet"` 在折线主路仍在（#507 五件未回改）。

## 二、自设新探针（≥1，机器证据落 `.scratch/t512r/`）

- **N1 dist行为级** `probe-n1-dist.mjs`（runId `5844b699-33bd-4e97-960b-a1e25915799f` exit 0，9/9 绿）：
  ① bar-xlabel 仍两条 9.5px、721–875／≥876 两档媒体查询内零非折线族字号规则 → **确未顺手改他族**；
  ② `minRight:` 全仓仅 renderLine 一处传入，`insetsFor` 缺省 `?? 0` → 他族右留白恒为 `padX`（逐字节同改前）；
  ③ `line labels:'none'` 可渲染且零 xlabel（`minRight=0` 分支不崩），bar 九项标签齐（他族不传路径正常）。
- **N2 CDP观察** `probe-n2-cdp.mjs`（runId `bb9937f1-5c8b-4db3-b342-1e5d43334677` exit 0，读数 `读数-n2.json`＋出片 `shots/n2-*.png`）：
  ① line-721 实渲 **13.92px**／line-875 **17.21px** → 证据“已知余量”14.0／17.2 推算诚实（见 §三）；
  ② bar-390 **9.74px**／bar-512 **13.36px** → 与证据 §九表格逐值一致，**同病仍在＝确未顺手改**。

## 三、缺陷清单（引入／范围／范围外）

1. **本票范围内缺陷：无。**断点跳变已治：四档单调不降且≥15px（CDP 实测），390 档末条顶出已收（改前顶出 6.17px 的口径本席未复测改前态，采信其红→绿变异两行＋本席绿态复现）。
2. **范围外遗留观察 O1（已知余量，非缺陷）：**721–875 档内仍有锯齿，档下沿 721 实渲 13.92px＜15px。证据与 `charts.ts:436-438` 已如实披露；票面判据只覆盖四个采样档，且“按容器宽度分档”已在票面遗留出口另开票。本席 N2 独立坐实披露数字，**不判 FAIL**。
3. **范围外遗留观察 O2（同族同病）：**柱／组合／散点窄档仍＜15px（N2：bar-390 9.74px）。实施只登记（§九）未顺手改，符合票面“另开票”纪律；N1＋N2 双重坐实未偷改。**需另开票，不 blocking 本票。**
4. **⑤肉眼终审留收口：妥当。**四档＋390 出片五张齐（`.scratch/t512-wavec/shots/shot-{512,820,1000,1440,390}.png` 本席已见），票面⑤本就规定以用户原话为准，复核席不做视觉判断；留收口统一收符合票面。
5. **过程披露：**本席复跑原读数脚本按其硬编码 OUT 复写了 `.scratch/t512-wavec/` 内读数 JSON 与出片时间戳，内容与证据数字一致，证据结论不受影响；本席新增文件只在 `.scratch/t512r/`（探针×2＋样张×2＋读数＋出片）与本件，源码与他人证据一字未碰。工作区另有他席在途脏文件（`scene-10-analysis.ts` 等），与本票无关，未碰。
6. **结构纪律记账：**本件落 `docs/base/base-render/`（归属件目录 ✓）；`charts.ts` 约 2202 行超 350 行告警线 —— **已超线，需要根据规则进行重构**：超因八族同住一文件（历史形状），拆法按族立能力目录，另开票量级，本票不动。实施证据 §十已同口径记账。

## 四、回执（1200词内）

PASS。build／charts.test(90/0)／style(30/0)／四档(15.05≤16.03≤17.32＝17.32)／390(minGap 4.69, inside) 全绿，runId 见 §一；N1 九项全绿坐实“他族未动、缺省安全、none 路径不崩”，N2 坐实“721 沿 13.92 披露诚实、柱族窄档仍红＝未顺手改”。范围外 O1／O2 均有出口，不回改、不另判。复核完毕，停止等指派。
