# t418 复核回执（636MAP 独立复核席）

- 票：#418 KPI 卡补完成度条位；实施提交 `b3e02e85`（`blocks.ts` +42、`blocks.test.mjs` +25、证据 `docs/base/base-render/t418-条位-证据.md`）；复核不采信实施者结论，全部独立复跑。
- 判定：**PASS**（票面硬判据全绿，自设探针 51 项全绿，无 FAIL 级缺陷）。

## 机器证据（持锁独立复跑）

- build：`node tooling/run-locked.mjs --ticket 418 -- node node_modules/typescript/bin/tsc -b packages/base-render`，runId=`4e31e083-7381-4fb2-ac03-38bcb49a77a5`，exit 0。
- blocks.test：同锁 `-- node --test packages/base-render/test/blocks.test.mjs`，runId=`b3b68bf5-99cf-46db-92cd-589816bb5b5f`，52/52，exit 0（含 #418 两用例）。
- style.test：同锁 `-- node --test packages/base-render/test/style.test.mjs`，runId=`50336e08-be93-4f2a-bff3-21fe1c18706b`，30/30，exit 0（T2 的 11 键集同序仍绿，`blocks.test.mjs:101` 的 var() 闭集合规仍绿）。
- 自设探针：`.scratch/t418r/probe-r1.mjs`，runId=`d057fc4b-4ee1-4927-b5e4-e776e54dd7b3`，51/51，exit 0。

## 自设探针（4 组新覆盖，均过）

- P1 三件顺序：detail+bar+badge 俱在时 `detail<bar<badge`（实测 216<264<404）；无 detail 时 bar 仍在 badge 前且不产 detail 槽。
- P2 非对象 bar：`null`／字符串／数字／数组一律 `bad-input`；`{pct:undefined}`／缺 pct 拒收。
- P3 小数边界＋变异杀伤：89.9=mid／90=high、59.9=low／60=mid、90.5=high、宽 `89.9%` 原样；`Inf`／`-Inf`／100.5／`"92"`／null／bool 拒收；坏实现 90→91（90 落 mid）、60→61（60 落 low）均可被本探针杀死。
- P4 CSS 三档＋闭集：`bar`／`bar-fill`／`bar-high|mid|low` 五规则俱在，三色 `#34c759`／`#ff9500`／`#ff3b30` 齐备，槽底 `var(--line)`；全 CSS 的 `var()` 皆在冻结集内；冻结 token 恰 11 个且键序未动；bar 产物无 `ilife-bar"`／`ilife-kpi"` 等 6 类闭集外类名。
- P0 零变：最简与六槽满配（无 bar）均无 `kpi-card-bar`，detail／badge 仍在。

## 核验（票面不许动的东西）

- 零变：bar 只在 `bar!==undefined` 时插入（`blocks.ts:594-604`，detail 后 badge 前），无 bar 路径形状不变；`skill-calorie/src` 不在 `b3e02e85` 写集内，一行未动。
- 11 冻结 token：`spec/style.ts:12-24` 未动，探针 P4 逐键同序确认。
- 闭集外类名：只产 `blockPart('kpiCard',…)` 族（`bar`／`bar-fill`／`bar-high|mid|low`），判档函数唯一（`kpiBarClass` 定义 572-576、调用仅 602），色映射唯一（样式区 1684-1692 各一处）；`style.test` T9-T11 家族绿。
- 色单源甲路成立：三色逐值等于 `CHART_PALETTE[1..3]`（`spec/charts.ts:246-248`）且高档 `#34c759` 与 `--ok` 同值；未新增 token。

## 缺陷清单（引入／范围／范围外）

- 引入·轻微①：证据件 §4 提交 sha 仍“待填”，补 `b3e02e85` 即可，不拦 PASS。
- 引入·轻微②：`blocks.ts:1671-1672` 注释“与状态徽章底色同值”不准——实测徽章底为粉彩 `#e6f7ec`／`#fff5e0`／`#fff0ee`（`style.ts:632-643`），条色实为饱和三色（同 `CHART_PALETTE[1..3]`）。功能无影响，建议后票改注释为“同 CHART_PALETTE[1..3]，高档兼与 --ok 同值”。
- 票面文字·非实施缺陷：甲路“每个字面量在 base-render 内只出现一次”按包级字面不成立（三色散见 palette／spec-index／style／helpShell），但按“条区三档各一处＋不新增 token”的本意成立；建议后票澄清为区级口径。
- 范围外·存量：`blocks.ts` 2351 行超 350 告警线，证据 §5 已报（存量挂号 #430、拆法归 #235），本次 +42 非引入，不行动。
- 范围内硬判据：零缺陷（两界、封顶、越界／非数拒收、build＋双测＋var 闭集、变异杀伤）。

## 写集与纪律

- 写集：本件＋`.scratch/t418r/probe-r1.mjs`；未改源码与他人证据，未 comment／close，未切分支／reset／stash／clean，无危险 git。
- 超线声明：本复核件与探针不计入 `packages/` 行数口径；源码超线已由实施证据 §5 申报。
