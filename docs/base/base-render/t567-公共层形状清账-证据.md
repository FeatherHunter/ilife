# #567 公共层形状清账 · 证据件（实施席）

归属：`docs/base/base-render/`（doc-homes：公共层包；本票改动主体在 `packages/base-render/`）。
票面：`gh issue view 567 --comments`（D1–D7／J4J5／状态列＋两条缺口追加评论）。
基准：`docs/skills/skill-calorie/t516-场景10-视觉整改基准.md` §5.2／§5.3／§5.4。
派单：`.scratch/t161-plan/dispatch-567.md`。另收 #572 转入 S3-3（`t572-复核-证据.md:98`：表头 `th{text-transform:uppercase}` 把 `饮水（ml）` 渲染成 `饮水（ML）`）。

## §0 影响清单（结构纪律第一步）——已报／已确认

确认源：dispatch-567（编排者 2026-09-16）＋本席派工指令（含 572-S3-3 转入、GATE-RUN 记账、800 字四段式回执）。
“已确认”指派单明示范围即授权；终审若推翻逐条回滚（见 §8 未做项）。

| # | 文件（能力归属） | 改什么（一句） | 谁在用（grep 实证） |
|---|---|---|---|
| 1 | `packages/base-render/src/blocks.ts`（公共层区块） | D1 表卡去 680 上限吃满内容列；D2 首列 `44px`→`minmax(44px,auto)`；D6 toc-a 触摸 44px；`renderDataTable` 加 `cellHtml`；th 去 `uppercase`（572-S3-3） | skill-calorie（render／analysis／home／diet 全族表）＋ skill-bill（`shared/*` 8 件＋`record/*` 16 件＋`query/*`，`shared/docPage.ts:103` 拼 `blocksCss()`）；home／chef／memo／schedule 零 `renderDataTable` 调用 |
| 2 | `packages/base-render/src/charts.ts`（公共层图表） | 柱状 `valueThin` 抽稀＋`yTicks` 刻度＋`showValues:'edge'语义修正`；线 `showValues:'last'` 只标末值＋`labelEvery` 稀疏标签；默认行为不变 | 仅 skill-calorie（diet 营养柱／trend 缺口线／report `lineOf`／sport 分布柱）；bill／home／chef／memo／schedule 零 charts 调用；现存唯一 `showValues:'edge'`+bar 已是注释里的改前记录（reportDocScoreSeries.ts:9），无活调用 |
| 3 | `packages/base-render/src/style.ts`（公共层样式 controls 区） | 字号按 §5.2 收档（10/10.5→11，11.5→12，12.5→13，14→15，17/20→18，26/32/40→28；22 KPI 主数字与 charts 段不动） | 一切 embed `buildStyleSheet()` 的页（31 页＋bill docPage）；ACTION_BAR 常量冻结值不动 |
| 4 | `packages/base-render/src/spec/charts.ts`（冻结类型面） | 只追可选键（`valueThin?`／`labelEvery?`／`yTicks?`(bar)／`showValues:'last'`），不动既有运行时出口；契约 doc §6.5 选项表同步＋changeset（`spec/index.ts` 纪律：引用类型形状变更视同签名变更） | 同 #2 |
| 5 | `packages/base-render/test/*` | 改 3 件（blocks:188 去大写断言；chart-top-table-measure-512 §②上限改吃满；page-viz-421 CHANGE_ROW 14→15）；增 `shape-567.test.mjs`＋`style.test.mjs` G-2 下限守卫 | 本票写集内 |
| 6 | `packages/skill-calorie/src/render/trendPredictDocs.ts`**只缺口段** | D4 参数说明去重（与口径行只留一处）；D5 堆叠条值槽改占比（绝对数只留 KPI detail，385 冻结）；**LF 中性**（±0 行，不碰 AGENTS 台账——AGENTS.md 在途） | 缺口页 21／22；预测族 9 页、报告族 8 页一行不动 |
| 7 | `packages/skill-calorie/test/analysis-shape-567.test.mjs`（新）＋`.scratch/t567/probe-shape.mjs` | 页级断言（D1/D2/D6/D4/D5/cellHtml 合成表） | 新件 |
| 8 | `.changeset/t567-公共层形状清账.md`、`docs/visual-spec-blocks.md:73,76` 去大写同步 | 记账与测量规同步 | — |

段界声明（dispatch “不许碰 trendPredictDocs.ts（预测族）” vs “只 buildDeficitDoc 段”）：
`buildDeficitDoc` 经 #518-W1 搬家物理上住 `trendPredictDocs.ts:136–290`（`trendDocs.ts:517` 只剩薄转出）。
本票以**段**为界：只动缺口独占行（param description、deficitBurnMix 值槽），`pageSection`／`signed` 等两族共件、预测族 9 页零改动。
`renderDataTable` 缺口页**暂不接线 cellHtml**：`analysis-deficit-385.test.mjs:158` 的 `CELL_RE` 要求单元格无标签（冻结 analysis-* 不许动），接线即红；能力先行＋合成表自证，接线待 385 解冻（§8）。

不碰：在途 `packages/skill-calorie/AGENTS.md`、`src/render/sportDocs.ts`、`docs/skills/skill-bill/t410-*`、`docs/skills/skill-calorie/t460-*`、`test/goal-result-290.test.mjs`；`reportDoc*`／`reportPlate.ts`；别席一切件。不 push、不 close。

## §1 探针（改前必红 → 改后必绿）

- `.scratch/t567/probe-shape.mjs`（静态：读 `base-render/dist`＋`t387/html/21-查热量缺口.html`＋合成输入；无浏览器）。
- ① D1 表卡无宽度上限 ② D2 首列非固定 44px ③ D6 toc-a≥44px ④ J4 页段（基准量法 chartsAt 切分）≤6 种且 ≥11px
  （单列偏差：票面“≤5”与基准 §5.2“五档＋下限”＝6 值矛盾，以基准件为准）
  ⑤ J5 断点集合恰为底盘＋页面三档（改前已绿见下，守卫防新增）⑥ D4 口径只一处（最长公共子串＜8 字）＋D5（堆叠条值槽 `%`、无 `公斤`）。
- 图表开关各一条（thin／yTicks／last／every），默认行为另有守卫（改前改后同绿）。
- 改前读数：见 `.scratch/t567/probe-before.log`（逐条 FAIL 点名）。

## §2 改动明细（diff 实况，按 §0 逐行对账）

（落盘时填：每件 ±行、关键行号、LF 台账影响：base-render 无台账；trendPredictDocs LF ±0。）

## §3 机器门（逐条 GATE-RUN，只领 exit=0）

| runId | cmd | 结果 |
|---|---|---|
| — | — | （落盘时填；长输出进 `.scratch/t567/*.log`，回执只看尾 5 行） |

- tsc：`node node_modules/typescript/bin/tsc -b packages/base-render packages/skill-calorie`
- 公共层门：`packages/base-render/test/*` 全绿＋他技能靶向（bill `frozen-blocks`＋`t418-query-visual`；memo `help-file-228` 烟囱——bill 直接消费 blocksCss，余三技能零 blocks 调用故抽 bill＋memo）
- audit-separators 31/31 零；measure-responsive 93 格 0（390,768,1440）；卡路里靶向；check-warning-line PASS（只读跑，不 `--sync`，AGENTS.md 在途）；gen-cli GEN-CHECK PASS
- 重出 31 页＋逐页 sha256（`.scratch/t567/sha-after.txt`；形状命中数只许增／持平，对 `.scratch/t567/sha-before.txt`）

## §4 变异自证（两行：改坏必红 → 逐文件还原必绿）

（落盘时填：每类一处，公共层负向证据含开关三项。）

## §5 代表页双档实拍读图（不用 dsh visionreview）

- `vision_html_screenshot` 直出 21（1440／390）＋24（S3-3 `ml`/`KG` 档），`read_image` 读图：三档无溢出佐证、触摸 44px 目检、间距 4/8 抽点。
- 手机参照 `fixtures/help-instances/卡路里_HELP_20260731_201530.html`（触摸／间距手法出处）。

## §6 用户 HTML 要求逐条（与票面冲突以票面为准并单列）

（落盘时填：三档无溢出／触摸 44px／手机参照／间距 4-8／零冗余 wording／符号拼串改形状。）

## §7 交付对账（第五步：逐行对 §0，偏差为零）

（收尾填。）

## §8 未做项与下一手

- cellHtml 缺口页接线（待 385 解冻）／D3 参数区／D7 登记／J5 页面 820-vs-821 双写（pageChrome 在禁区外，现状互补已收敛）／26px 页标题与 16px 小节标题（禁区文件，探针口径外）。
