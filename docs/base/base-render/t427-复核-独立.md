# #427 独立复核回执（复核席，与实施 f0b66563 无交集）

> 只交回执，不 comment、不 close 票。结论均来自本席锁内复跑，不采信实施结论。

**判定：PASS（有条件）**——源码改动与测试机器证据成立；遗留范围外收口项 1 条（真机量框＋三截图缺席），须编排者关票前补，方可关票。

## 1. 独立复跑（`node tooling/run-locked.mjs --ticket 427 -- …`，ticket=427）

| # | 命令 | runId | exit | 读数 |
|---|---|---|---|---|
| 编译 | `node node_modules/typescript/bin/tsc -b packages/base-render` | `807252a1-5a61-4ff6-96c3-7c13213e0a16` | 0 | 零报错 |
| 新测试 | `node --test packages/base-render/test/page-finish-427.test.mjs` | `1a121fef-71f8-49da-9112-21348eb1f58f` | 0 | pass 9／fail 0 |
| 存量 | `node --test …/style.test.mjs …/blocks.test.mjs` | `aa251ac0-6394-405f-a108-0d41300775fb` | 0 | pass 89／fail 0（11冻结变量＋闭集类名未红） |
| 关联 | `node --test …/mobile-base-507.test.mjs …/ui-fix-154.test.mjs …/copy-format-menu-247.test.mjs` | `a206fa4a-098b-400d-8eda-8330c3f08198` | 0 | pass 44／fail 0（507之1024三列帽／154计数／247基座均绿） |

复跑顺序先编译后测试，读的均为本次编译后的 `dist`，与实施声称一致（9／89／44 三数逐一对上）。

## 2. 自设新探针（与实施变异 1／2 不重叠）

`.scratch/t427r/probe-427r.mjs`，锁内 `runId=16e87e28-4918-42dc-a656-1e31f46f8528` exit 0，pass 4／fail 0：

- **R1·821 桌面档零变**：641 档唯一且含动作条放开（基座 520→放开 none），`action-bar` 上下文无 821 残留，≥821 与改前同为 `none`。
- **R2·640 单列无溢出**：ghost 窄列与 KPI 窄列各恰 1 条 `minmax(0, 1fr)`，不含 `auto`／`150px` 下限（0 下限防长值卡撑破）。
- **R3·44 值一致性**：`ACTION_BAR_DEFAULTS.minHeightPx===44`＋`src/spec/controls.ts:308` 源行 44＋CSS 内 `min-height: 44px`≥2 处（桌面与窄屏托底一致）。
- **R4·nowrap 未动**：`blocks.ts` 桌面行首列 `nowrap` 与窄屏 `th normal` 双在，#427 未碰表格换行（对抗结论“#457 另解、不做”落实）。

## 3. 缺陷清单（引入／范围／范围外）

- **票引入：无**。三处改动（`style.ts:330` 641 放开、`style.ts:357-361` ghost 单列、`blocks.ts:1765-1769` KPI 单列）逐行核对 diff，断点均取既有 640 档、无新类名新 token；`style.ts:553-555` 仅注释 40→44，实现仍读冻结常量，无数值改动。
- **范围（票面三缺口）：无残留**。缺口 1（641–820 铺满）／缺口 2（ghost 单列）／缺口 3（KPI 单列）＋44 对齐，新测试 9 条逐条对判据，复跑全绿。
- **范围外收口项（1 条，关票前必补）**：**真机量框与截图缺席**。CSS 级“单列在位”≠“390 无横向溢出”，“641 档在位”≠“820 两宽像素相等”；`scrollWidth` 需 CDP 调试端口、三截图（820／390／1920 对照）＋用户肉眼均未出。鉴于断点／0 下限／142 合跑已把残余风险压到“子项内容溢出”一角，**可接受为编排者收口项，但不可接受为“已验证”**：关票前须补 `shot.mjs measure … 820／390`＋三张截图＋1920 对照，判据照票面（820 两宽相等；390 两处单列且无横向溢出）。

## 4. 局限与备注

- `gh issue view 427` 正文拉取失败（`api.github.com/graphql: EOF`），仅见一条公共层 backlog 评论；票面三缺口以证据件 §0–§2 转述为准核对，未能逐字对票面原文。
- `style.ts`／`blocks.ts` 超 350 行告警线一事，证据 §0 已按包规矩报警并说明本次不拆（既有区内约 10 行），复核认可。
- 写集仅本件＋`.scratch/t427r/`；源码、他人证据一字未动；无 push。

## 5. 路径

- 实施提交 `f0b66563`；证据 `docs/base/base-render/t427-adapt.md`；源码 `packages/base-render/src/style.ts:320-361,553-555`、`packages/base-render/src/blocks.ts:1753-1769`；测试 `packages/base-render/test/page-finish-427.test.mjs`；本复核件 `docs/base/base-render/t427-复核-独立.md`；新探针 `.scratch/t427r/probe-427r.mjs`。
