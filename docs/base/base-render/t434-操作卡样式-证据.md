# t434 操作卡样式（公共层 pageShell 区）证据

## 0. 结论

- `blocksCss()` 的 `pageShell` 区新增操作卡头部基规则 1 条＋四色档 4 条＋子件 5 条（含时刻键 1 条），`BLOCK_STYLE_SECTIONS` 仍 12 项。
- 结论条裁定：**保留**既有 `#507` 形态，不照样张 `one-line` 改。
- 运动族 `sportUi.ts` 的 4 条操作卡页级覆盖已迁回公共层并删除，`sportUi` 内零 `.ilife-block-op-head` 规则（只剩指向注释）。
- 无两类名的 3 个样本页产物逐字节不变，`blocksCss` 对既有规则零删改（只增 22 行）。

## 1. 改动清单（与开工影响清单对账，偏差零）

| 件 | 改动 |
| --- | --- |
| `packages/base-render/src/blocks.ts` | `pageShell` 区结论条规则之后加操作卡 10 条规则（`+81` 行含注释），其余一字不动 |
| `packages/base-render/test/blocks.test.mjs` | 尾部加 `#434` 断言 4 条（`+37` 行），既有断言不动 |
| `packages/skill-calorie/src/exercise/sportUi.ts` | 删操作卡页级覆盖 4 条及配套注释，加 3 行指向注释（净 `-11` 行）；其余覆盖（变更行／数据表／导航／分布条）一字不动 |
| 本证据件 | 新建 |

## 2. 色值与形态口径

- 基规则：卡片＋`border-left: 4px solid`（缺省中性 `var(--line)`），形态照老实物 `crud_receipt` 的 `id-card` 左色条（样张 `t156-样张-写后回执.html:153-160` 随 op 切）；四色档只改 `border-left-color`。
- ok 取冻结 token `var(--ok)`（值 `#34c759`，与样张 `opColors` 同值）；冻结表无 warn／danger 灯位，warn／danger 取同仓状态徽章同源 D-5 常量（`src/style.ts` statusBadge 区 `#a25b00`／`#a83228`，不新增 token 名）；empty 取冻结 `var(--fg2)`。
- 样张的 `#0071e3` 在 H-01 禁色表内（`test/style.test.mjs:241`），一律不用；全部 `var()` 引用均在 11 个冻结 token 内（既有「只读冻结 token」断言仍绿）。
- 内距取同族卡片档 14px（样张 18px 20px 是 throwaway 密度不照搬）；图标 40px 中性盒（样张 12px 圆角按 D-5 收进闭集取 14，色档由左条＋徽章承担）；标题 18px／700 取 `detail-section-title` 档（#567 J4）；时刻行 12px 取 `--fg2`（#179 对比度，`--fg3` 不用）。
- 时刻键 `::before`（`sportUi.ts` 迁回）：`actionAt` 即写入时间（`body/receipt.ts:162` 同值同词），CSS 内容只进渲染层；第二消费族出现时提到调用方文案面并删此条（过渡债务）。

## 3. 结论条裁定

选了**保留**既有形态，不照样张 `one-line` 改，一句话理由：既有左 3px 主色边＋强调字形态是 `#507` 审查裁定、用于与状态徽章软底形态区分，照回样张会丢掉该区分并改动全仓既有结论条外观。

## 4. 判据红绿两行

- 红：`GATE-RUN runId=bd7ea07e-6b65-4050-9254-6b4e85248384` exit=1，`tests 50 pass 48 fail 2`（红在两条操作卡新断言；12 项与结论条两条当时即绿）。
- 绿：`GATE-RUN runId=f524980b-d593-478f-9b26-5f7db7469f1c` exit=0，`tests 50 pass 50 fail 0`；同区五件 `runId=25be34d7-3892-43e2-af50-b34e71fd6646` exit=0，`tests 120 pass 120 fail 0`。
- 编译：`runId=6cf6be92-8b70-431a-8d6e-caee40f43ebe`（`tsc -b packages/base-render`）与 `runId=d74b9be6-49b4-468f-abcb-68df042865b8`（`tsc -b packages/skill-calorie`）均为 exit=0。

## 5. 抽样（不带两类名的既有页面产物逐字节不变）

探针 `.scratch/t434/snapshot.mjs`（样本含 KPI 卡／数据表／页内导航／口径行／徽章，逐一校验不含两类名串）：

- `GATE-RUN runId=0cd1f203-defc-453f-bad6-22d4cc619ad5` exit=0，`RESULT: ticket=434 sampling=PASS`。
- 三页哈希改前改后逐字同：`s1=8deb9d9698c1`、`s2=de74f05e8229`、`s3=ab11ef53c9a3`。
- `blocksCss` 全文比对：`cssRemoved=0 cssAdded=22`（只增操作卡行，既有规则一行未动；结论条规则零改动）。

## 6. 变异（必红还原）

- 变异（ok 档 `var(--ok)` 改 `var(--blue)`）：编译 `runId=b6d4e79e-2173-422e-9239-be9934053db4` exit=0，测试 `GATE-RUN runId=7501b646-f252-47fa-a45a-1a2aca5b6f0e` exit=1，`tests 50 pass 49 fail 1`，红在「四色档色值口径」。
- 还原（逐字改回）：编译 `runId=c3086bdc-a742-4b1c-8317-32739dcb1954` exit=0，测试 `GATE-RUN runId=2273a2d7-17d3-49bd-b897-d344c3986142` exit=0，`tests 50 pass 50 fail 0`。

## 7. 消费侧一红归因（非本票）

`GATE-RUN runId=19d01d9b-b951-4194-9a15-7964b60abfe1` exit=1，`tests 42 pass 41 fail 1`：唯一红是 `fusion-shared-422`「类别色单源」（四值同现命中 `diet/reviewDocs.ts` 与 `categoryColors.ts` 两件）；`HEAD` 的 `reviewDocs.ts:304`（`MEAL_COLORS` 餐别色表）已含四个同值，本票三件改动与该件零交集（`git status` 证该件干净），属既有债务，转下一手由归属票认领。本票消费面断言（四态头／结论条／字段标签／来源脚注）全部为绿。

## 8. GATE-RUN 对账（`.scratch/locks/gate-runs.log`，`ticket=434`，短 id／exit／命令）

- `1dd96f1a`／0／基线编译 base-render；`1152ce89`／0／改前快照；`bd7ea07e`／1／判据跑红。
- `6cf6be92`／0／实施后编译；`f524980b`／0／判据跑绿；`7c05fd43`／0／抽样。
- `25be34d7`／0／同区五件；`d74b9be6`／0／技能层编译；`19d01d9b`／1／消费侧（归因见 §7）。
- `b6d4e79e`／0／变异态编译；`7501b646`／1／变异红；`c3086bdc`／0／还原编译；`2273a2d7`／0／还原绿；`0cd1f203`／0／终验抽样。

## 9. 用词与交付

- 用词守 `docs/agents/wording.md`：命令／页面交付／实施／提取到公共层；「形态照齐」指版面照老实物，不作商量一致解。
- 开工 `git status` 记他席在途件（`.changeset`、`goalWeightDoc.ts` 等十余件）一字未碰；提交只含 §1 四件（`.scratch` 与 `dist` 均忽略不入库）；中文信息 `feat(434)`；不 push。
