# #507 移动端底座余量 · 证据（WaveB 实施席）

> 范围：只动公共层母版与区块（`packages/base-render`），不动技能代码、不动命令声明与路由。
> 前置已落地（本票不重做）：数值列等宽栈（`blocks.ts` 数值单元格 `SF Mono`＋`tnum`，#507 视觉底座）、
> `viewport-fit=cover` 的 `pageUi:35` 按需启用口径（#525）、结果页触摸区 44px（`pageUi.ts` ⑤）。
> 关票由编排者收，本件只交证据与回执。

## 0. 影响清单（结构纪律第一步；事前＝事后，偏差零）

| # | 文件 | 一句话 | 理由 |
|---|---|---|---|
| 1 | `packages/base-render/src/blocks.ts`（源码，管辖内） | KPI 网格加 ≥1024 三列帽；数据表加偶行斑马纹 | 票面第 7 条、第 6 条余量；落既有区，不新开区、不新类名 |
| 2 | `packages/base-render/assets/help-template.html`（页面模板，不管辖） | 减动效归零档；`phone` 高 `100vh`  fallback＋`100dvh`；等宽栈同文件统一 | 票面第 3、4、5 条余量 |
| 3 | `packages/base-render/test/mobile-base-507.test.mjs`（测试，不管辖） | 六条新判据 | 覆盖本票全部改动点 |
| 4 | 本证据件（文档，不管辖） | 逐条读数、变异两行、未做项 | 票面交付物 |

## 1. 逐项核验（开工前现状；读数均为本机实测）

| 票面条 | 结论 | 实测依据 |
|---|---|---|
| ① viewport cover | 已绿，不做 | 生产代码缺 cover 15 处（与票面一致）；但 #525 已给按需启用口径（`pageUi.ts:35`），母版不动 |
| ② 触摸目标 | 未做（见 §5） | 小目标读数仍在：26／28／30／22／20／30＋ghost40px(`:1222`)；结果页侧 `pageUi.ts` ⑤ 已 44px |
| ③ 减动效 | 真缺 → 本票补 | `help-template.html` 与 `blocks.ts` 的 `prefers-reduced-motion` 命中双 0 |
| ④ `100vh` | 真缺 → 本票补 | `:159`（≤500px 档 `.phone`）`height:100vh`，无 `dvh`；`:12` 的 `.stage` 是 `min-height`，票面未点名，不碰 |
| ⑤ 等宽栈 | 真缺一半 → 只做同文件内统一 | `:141`／`:152` 短栈 vs `:271`／`:1273`／`:1442` 长栈；跨件统一（`blocks.ts` 第三栈 `H-06` 钉死 4 处）不碰 |
| ⑥ 表格 | 斑马纹真缺 → 补；数值等宽已绿不重做 | `nth-child` 命中 0；数值单元格等宽栈＋`tnum` 在位（另有 `table-width-512` 钉住） |
| ⑦ KPI 列数 | 真缺 → 本票补 | 基座仍 `repeat(auto-fit, minmax(150px, 1fr))`，无 1024 档 |

## 2. 改动明细（文件:行号以提交后为准）

- ③减动效：`assets/help-template.html:1348-1361` 新增归零段（覆盖 10 处动效：summary 三角×2／mini／tab／copy-btn／sl-item／sheet-mask／sheet／toast（祖先类提权重）／hmFade／hmPop；默认态零改）；
  `src/blocks.ts:2206-2210` 折叠头小三角归零（本区唯一 transition；终态 rotate 照走）。
- ④dvh：`assets/help-template.html:160` 改为 `height:100vh;height:100dvh`（vh 兜底在前；`:12` 的 stage min-height 票面未点名，不碰）。
- ⑤等宽：`assets/help-template.html:141`／`:152` 短栈→长栈（与 `:271`／`:1273`／`:1442` 同形；跨件统一不碰，`blocks.ts` 第三栈另有 H-06 钉死）。
- ⑥斑马纹：`src/blocks.ts:1846-1852` 偶行 `td` 取 `var(--bg)`（既有 token；列头不动；窄屏卡化下偶数卡整格同底）。
- ⑦KPI 帽：`src/blocks.ts:1655-1663` 加 `@media (min-width: 1024px)` 祖先类三列帽（基座 auto-fit 一字不动；窄屏 2×2／pageUi 400 单列各管各档；
  pageUi 启用页 ≥1001 档另有 repeat(4) 在后覆盖，确定性成立，见 §5）。
- 派生：`src/helpShell.ts`＋`test/help-shell-136.test.mjs` 由 `scripts/gen-help-shell.cjs` 重生成（仅哈希 5 行差集，手未碰）。

## 3. 测试

- 新：`packages/base-render/test/mobile-base-507.test.mjs`（7 条；改前 7 红→改后 7 绿，红绿读数见锁内运行）。
- 全量：`tsc -b packages/base-render` exit 0；`node --test packages/base-render/test/*.test.mjs` **724/724 绿**
  （含 regen 后的 #136④；途中 #136④ 因模板改动红过一次，按票面纪律走 `gen-help-shell` 重生成后归绿，未手改派生件）。
- 超线报警：`blocks.ts` 本改前已 2406 行，超本包 350 行告警线——「已超线，需要根据规则进行重构。」
  超因：12 区块样式区＋多票注释同件沉积；本次只在既有区内加两条规则（约 15 行），不拆件（拆件是另票体量，且 `blocksCss` 唯一产出者口径在多票进行中，拆会撞写窗）。

## 4. 变异自证两行（机器读数）

- 变异 1（KPI 帽改坏：`repeat(3,`→`repeat(auto-fit,`）：新测试 6 绿 1 红（红的恰为⑦）；改回后 7 绿。
- 变异 2（斑马纹删 `background-color` 一行）：新测试 6 绿 1 红（红的恰为⑥）；改回后 7 绿。

## 5. 未做项（转回执）

- ① viewport 母版 15 处：#525 按需启用口径已覆盖，不代母版。
- ② HELP 母版小触摸目标：三档目标值票面未裁定（ghost 40px 另有 #329 降权重依据在文件内注释），结果页侧已 44px；建议另开一票请用户裁定档位后再做。
- ⑥数值列等宽栈、`blocks.ts` 等宽第三栈：已落地／另票钉死，不碰。
- 已知交互（非缺陷）：pageUi 启用页 ≥1001 档读数卡走 `pageUi.ts` ⑧ 的 repeat(4)（后出现同权重覆盖本票 1024 三列帽）；
  未启用页走本票三列帽。两档各有归属票（#525／#507），本票不碰 pageUi。
