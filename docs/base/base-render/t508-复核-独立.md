# t508 独立复核（复核席，与实施无交集，不采信任何结论）

判定行：**PASS** —— 实施回执声称的每次绿全部独立复跑命中（runId 与 exit 一对一见 §二），
探针同源逐字成立（8 函数体逐字节相同＋同页 JSON 命中逐字相同，见 §三），
三笔提交只含自己 4 件、466 行、`src/**` 一行未动（见 §四），
自设新探针 9 检全过、收窄未变放宽（见 §五）；缺陷清单为空，观察项 1 条、披露 2 条（见 §六）。

复核席位：独立复核席。票号：**508**。归属件：**base-render**。
本席只写 `docs/base/base-render/t508-复核-*` 与 `.scratch/t508r/`，未碰源码、未碰他人证据、未用危险 git。

## 一、复核动作声明

- 不采信实施结论：其 `t508-分隔符立规.md`、`t508-gate-runs.md` 只当“待验声称”读，
  每条逐字用自己的 `run-locked` 运行重验（`--ticket 508 --run-id t508r-*`，见 §二）。
- 全程经 `node tooling/run-locked.mjs` 持锁；凡 exit 非零属预期者（红线 exit 1）均在命令层面预期一致，
  包装器 `RESULT exit=` 与命令退出码一致即记账。

## 二、独立复跑对照（声称 → 复核 runId → exit）

| 声称（实施） | 复核 runId | 复核命令 | 复核 exit | 对读 |
|---|---|---|---|---|
| t508-build1 tsc exit0 | `t508r-build1` | `node node_modules/typescript/bin/tsc -b packages/base-render` | 0 | tests 无输出，exit 0 一致 |
| t508-guard2 guard10/10 exit0 | `t508r-guard1` | `node --test packages/base-render/test/separator-guard.test.mjs` | 0 | tests 10 / pass 10 / fail 0，与声称一致 |
| t508-probe-today exit1 | `t508r-probe-today` | `node packages/base-render/test/separator-probe.mjs .scratch/t401c/看今日主页.html` | 1 | 行级 3 行／节点级 8 处，与声称一致 |
| t508-probe-week exit1 | `t508r-probe-week` | `node packages/base-render/test/separator-probe.mjs .scratch/t401c/看本周主页.html` | 1 | 行级 3 行／节点级 8 处，与声称一致 |
| t508-probe-month exit1 | `t508r-probe-month` | `node packages/base-render/test/separator-probe.mjs .scratch/t401c/看本月主页.html` | 1 | 行级 3 行／节点级 8 处（节点序号 #127／#137，窗口近 30 天），与声称一致 |
| t508-probe-orig exit1 | `t508r-probe-orig` | `node .scratch/sep-audit/probe.mjs .scratch/t401c/看今日主页.html` | 1 | 行级 3 行／节点级 8 处，与入仓件同页读数一致 |
| MUT-RED node1 exit1 | `t508r-mut-red` | `node .scratch/t508r/mut.mjs red`（同核异形，见 §六披露 D2） | 1 | `MUT-RED node=1 exit=1`，与声称一致 |
| MUT-GREEN node0 exit0 | `t508r-mut-green` | `node .scratch/t508r/mut.mjs green`（同核异形，见 §六披露 D2） | 0 | `MUT-GREEN node=0 exit=0`，与声称一致 |
| t508-regress1 93/93 exit0 | `t508r-regress1` | `node --test packages/base-render/test/blocks.test.mjs packages/base-render/test/page-finish-420.test.mjs packages/base-render/test/rowcard-caliber-t154r3.test.mjs` | 0 | tests 93 / pass 93 / fail 0，与声称一致 |

机读对账导出见 `docs/base/base-render/t508-复核-gate-runs.md`（抄自 `.scratch/locks/gate-runs.log` 的 `t508r-*` 行）。

## 三、同源核对（“逐字搬”是否成立）

- 函数体逐字节比对（`.scratch/t508r/parity.mjs`，`t508r-parity exit=0`）：
  `decodeEntities`、`blank`、`visibleText`、`lineOf`、`ownerOf`、`parallelRun`、`clip`、`judge`
  八项全部 SAME（长度逐项相等：370/51/288/74/251/638/318/214）；
  常量 `MAXSEG = 40`、9 元 `PARALLEL` 集、`SENTINEL` 全部 SAME。
- 同页 JSON 命中比对（`.scratch/t508r/json-parity.mjs`，`t508r-jsonparity2 exit=0`）：
  同一页 `.scratch/t401c/看今日主页.html`，来源与入仓件
  `origLine=3 origNode=8 totalLines=2334` ＝ `wareLine=3 wareNode=8 totalLines=2334`，
  行级／节点级命中数组逐字相同（`JSON-HITS IDENTICAL`；仅 `path` 标签不同，属输入形不同）。
- 结论：“来源逐字搬、判据一字未动”成立；入仓新增仅为 plumbing
  （`auditHtml` 字符串版核心、`exitCodeFor`、`import` 主入口守卫），判据面无放宽无收窄。

## 四、提交范围核对（禁令与“src 一行未改”）

- `git show --name-only`：
  `fc5db19b` 含且仅含 `t508-gate-runs.md`、`t508-分隔符立规.md`、
  `test/separator-guard.test.mjs`、`test/separator-probe.mjs`（4 件）；
  `1488606f`、`422c583f` 各只碰 `t508-分隔符立规.md` 一件。三笔均无他席路径。
- `git show --stat fc5db19b`：4 files changed, 466 insertions(+)，“466 行”成立（纯新增）。
- `packages/base-render/src` 在工作区干净（`git status --short` 无该目录条目），
  其最近改动属 555／441／434（`git log --oneline -- packages/base-render/src` 首条为 555），
  “src 一行未改”成立。
- `422c583f` 信息“补第二笔 sha”与其只碰立规文档一件自洽（补记提交清单）。

## 五、自设新探针（§5.1：其脚本覆盖不到的 9 检）

脚本 `.scratch/t508r/new-probes.mjs`，经 `t508r-newprobes exit=0` 全过。
设计原则：专挑其守卫没断言的边——实体解码路、剥除三件（注释／script／style）、
R3 双边界（2 段反例、超长段反例）、`PARALLEL` 集内未测成员（半角 `;`）、跨行属性。

| 编号 | 探针（输入） | 期望（按口径） | 实测 |
|---|---|---|---|
| N1 | `A&middot;B` 作正文 | R1 命中 exit 1 | PASS（node=1 exit=1 tags=R1） |
| N2 | `<!-- A·B；C 苹果、香蕉、橙子 -->`＋干净正文 | 注释剥除，零命中 exit 0 | PASS |
| N3 | `<script>var s="A·B；C";…苹果、香蕉、橙子、葡萄…</script>`＋干净正文 | 整段剥除，零命中 exit 0 | PASS |
| N4 | `苹果、香蕉`（仅 2 段） | R3 需 ≥3 段，零命中 exit 0 | PASS |
| N5 | 三段各 41 字以 `、` 连接 | 每段 >MAXSEG(40)，R3 不命中 exit 0 | PASS |
| N6 | `apple;banana;cherry;date`（半角 `;`） | `;`∈PARALLEL，R3 命中 exit 1 | PASS（tags=R3） |
| N7 | 跨行 `data-t="A·B\n第二行；C\n…"`＋干净正文 | 全串级剥标签，零命中 exit 0 | PASS |
| N8 | `<style>.x::after{content:"A·B；C"}</style>`＋干净正文 | 整段剥除，零命中 exit 0 | PASS |
| N10 | `A&#183;B` 作正文 | 数字实体解码后 R1 命中 exit 1 | PASS |

收窄／放宽判定：N1／N6／N10 证“该抓的能抓”（无收窄）；N2–N5／N7–N8 证“不该抓的不抓”（无放宽）。
另有观察项 N9（单引号 class 归属为空）记 §六 O1，不计缺陷。

## 六、缺陷清单（缺陷标本：票引入／范围／范围外逐条定性）

**缺陷：无（0 条）。**

| 编号 | 标本 | 定性 |
|---|---|---|
| O1（观察，非缺陷） | 单引号 `class='…'` 内节点的 `owner` 为空（`ownerOf` 正则只认双引号）。命中本身不受影响（N9：hit=true）。 | 票引入：否（判据继承自来源，两件同形）；范围：范围外（真实产物用双引号，家底三页归属全部可对到件；单引号仅影响归属备注，不影响门禁红绿） |
| D1（披露） | `t508r-jsonparity exit=1` 为复核席 harness 写法瑕疵（`execFileSync` 把探针“有命中 exit 1”当异常抛出），与被复核件无关；修正后 `t508r-jsonparity2 exit=0`（`JSON-HITS IDENTICAL`）。 | 票引入：否；范围：复核席自有，不入 508 缺陷 |
| D2（披露） | 复核的 MUT 红绿未用实施原形的 `node --input-type=module -e` 内联串（pwsh 引号转义易失真），改用 `.scratch/t508r/mut.mjs` 同核脚本（同一 `auditHtml`＋`exitCodeFor`＋同一红绿文案）。读数与声称逐字一致（node=1/exit=1，node=0/exit=0）。 | 票引入：否；范围：复核手段差异，结论等价 |

## 七、路径

- 复核主件：`docs/base/base-render/t508-复核-独立.md`（本件）
- 复核对账导出：`docs/base/base-render/t508-复核-gate-runs.md`
- 复核工作区：`.scratch/t508r/new-probes.mjs`、`.scratch/t508r/mut.mjs`、
  `.scratch/t508r/parity.mjs`、`.scratch/t508r/json-parity.mjs`
- 被复核件（只读，未改）：`packages/base-render/test/separator-probe.mjs`、
  `packages/base-render/test/separator-guard.test.mjs`、
  `docs/base/base-render/t508-分隔符立规.md`、`docs/base/base-render/t508-gate-runs.md`

## 八、范围（票引入／范围／范围外）

- 票引入（508 立规票内）：探针入仓同源性、双守卫 10/10、家底三页 24 处登记、门禁退出码口径——全部复核通过。
- 范围（本复核席已做）：§二每次绿独立复跑、§三同源逐字节核对、§四提交范围与 src 未动核对、§五自设 9 检新探针。
- 范围外（本席未做，不背书）：逐页改页（归各场景票，508 本就是“只列清单不施工”）；
  家底样本之外页面的残留量；`renderCaliberLine` 已实施形状（17123173）的正确性（本席只核对“508 未动它”，不复核 154r3 本体）；
  关票由编排者收。
