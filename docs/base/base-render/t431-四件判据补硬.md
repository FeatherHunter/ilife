# t431 · #421 四件判据补硬（S2-1／S2-2 色值口径改写·甲法、S3-6 色值允许清单、S2-3 样式鉴别力、S3-4 a11y、S3-5 空态口径）

写面（`git diff --numstat`）：`packages/base-render/src/blocks.ts` **+67／−8**、`packages/base-render/test/page-viz-421.test.mjs` **+158／−16**
（22 条既有判据未删，现 27 条；只读公共层，未碰别家能力目录与生成物）。

## 0. 判定：上一席实现**保留**（本席不动实现方向，只补收尾与机器读数）

- **保留**根据：票面五条整改逐条有断言（§2）；四条变异改坏必红、改回必绿（§4）；甲法落法合规——票面要求「二选一并写明理由」，
  判据名「缺省填充色走样式段冻结 token」与 `src/blocks.ts`「色值口径（#431 定稿）」注释、本件三处说法同口径。
  本席**未动**：11 个冻结 token、12 区闭集、既有 22 条判据、任何断言口径；**实现一行未改**。
- 本席处置的两处**过程残留**（不是实现缺陷）：
  1. 中断的第二席把 `src/blocks.ts` 留在变异 A 状态（sha256 `CF4928FC…`＝删掉 `'  width: 72px;',` 那一条）。
     本席按另存件 `DBA73FB9…` 逐字节写回；**未用 `git checkout --`、未从提交取回**。
  2. 第二席变异「改回」步没真正重编译：`Copy-Item` 写回**保留原 mtime**，`tsc -b` 见输入比 buildinfo 旧即跳过，
     dist 留在变异版 → 「改回必绿」读数无效。本席改成「写回字节＋触新 mtime」，四行读数见 §4。
- **前席两轮日志不得当证据用**：第一轮 `.scratch/t431/mutate-log.txt` 漏了编译步（测试读 `dist`，改坏也全绿 27/27）；
  第二轮 `mutate2-log.txt` 只到「变异 A 已落」即被中断。四行由本席重跑（runId `c59a6b71…`）。

## 1. 三行量现场（只读 `dist` 探针 `.scratch/t431/probe-seat3.mjs`，读数落 `.scratch/t431/probe-final.txt`）

① **13 类规则实测声明数** 7／4／6／4／5／4／4／10／6／3／2／4／3；每类**色值字面量声明 0**（缺省色与线色全走冻结 token）。
② **四件缺省产物**：色值字面量均 `[]`；a11y 读数——迷你条 `role="img"`＋`aria-label="42%"`、变更行 `aria-hidden="true"`、
   分布条行与徽章 `[]`（零读数）。
③ **两处口径**：空态 `[] → 空串`（三件）／`undefined → bad-input`（三件）／`renderMiniBar()` 缺 input → `bad-input`；
   色值收拒 **9 收／9 拒**（收 `#f00`／`#ff0000`／`rgb(1,2,3)`／`rgba(1,2,3,.5)`／`var(--blue)`／`tomato`／`--blue`／`Blue`／`rgb(0 0 0)`；
   拒 `;` 注入／`expression(`／`url(`／`--nope`／`var(--nope)`／`var(--not-frozen)`／`tomato/*`／`7`／空串）。

## 2. 五条整改各自的断言位置（`packages/base-render/test/page-viz-421.test.mjs`）

| 整改 | 断言位置 | 断言内容 |
| --- | --- | --- |
| S2-1／S2-2 色值口径改写（甲法） | 144、343 | 缺省填充只有 `width` 一条内联声明、缺省产物零色值字面量；样式段两条 `-fill` 规则 `background`＝`var(--blue)`，且 `--blue` 在 11 键闭集内 |
| 甲法配套「逐字透传」 | 166 | 六种写法逐字进内联 `background`，只多一条声明、不加第三条 |
| S3-6 色值允许清单 | 180 | 8 个清单外样例一律 `bad-input`；同一把尺子守分布条行 |
| S2-3 样式鉴别力 | 377 | 13 类声明数下限（实测−2，三条窄类取 1）＋**17 条关键属性快照**（宽高／栏宽／字号／缺省色）＋13 类规则体零色值字面量 |
| S3-4 a11y | 413 | 迷你条 `role="img"`＋`aria-label="<pct>%"`（夹取同口径）、变更行箭头三态 `aria-hidden="true"`、分布条行与徽章零读数 |
| S3-5 空态口径 | 433 | 空数组 → 空串；`undefined` → `bad-input`；迷你条无空态分支 |

## 3. 门禁表（收尾一遍，逐条 `node tooling/run-locked.mjs --ticket 431 -- <命令>`）

| 命令 | runId | exit | 摘要 |
| --- | --- | --- | --- |
| `pnpm build` | `1a54f38a-0444-4bd8-956f-5c62aeff6465` | 0 | 全绿（2.3 s） |
| `node --test …/page-viz-421.test.mjs` | `3b27dcd4-a586-4702-8cc1-9e5ff31693d9` | 0 | `tests 27 pass 27 fail 0` |
| `node --test …/blocks.test.mjs` | `45c407ad-40d5-4a92-9fcf-8917cbe7eeda` | 0 | `tests 46 pass 46 fail 0` |
| `node --test …/style.test.mjs` | `221419fc-1f28-4364-a0fd-9962c063ea3a` | 0 | `tests 29 pass 29 fail 0` |
| `node --test …/page-finish-420.test.mjs` | `075a67fd-4a2c-4f44-9e71-a44ff1faff62` | 0 | `tests 20 pass 20 fail 0` |

同五条在本席第一遍（runId 首八位 `2fc56d67`／`099730c2`／`2f346429`／`3eb7ee76`／`2b191c09`）也是全绿，退出码同为 0。
**一条过程读数**：修 dist 的 `fix-dist.ps1` 内层那次 `pnpm build` 当刻 exit=2（错行不在 `packages/base-render/`——同期
`pnpm -C packages/base-render exec tsc -b` exit=0；他席在途件），到收尾门禁再跑已 exit=0。

## 4. 变异自证四行（`.scratch/t431/mutate5.ps1`，持锁 runId `c59a6b71-4240-4d9e-9be3-383eb95bccf5`；日志 `mutate5-log.txt`）

每行口径：**改坏＝变异 src → 定向编译 `pnpm -C packages/base-render exec tsc -b` → 跑件**；**改回＝写回另存字节（sha256 同值）＋触新 mtime → 编译 → 跑件**。

| 变异 | 改坏读数 | 改回读数 |
| --- | --- | --- |
| ① 删关键属性快照一条声明（`'  width: 72px;',`） | exit=1，`tests 27 pass 26 fail 1`，红在「四件样式鉴别力…关键属性快照」；dist 探针 `width: 72px` 命中 **0** | exit=0，`pass 27 fail 0`；dist 探针命中 **1**，dist sha256 回到绿建 `BE8F1806…` |
| ② 允许清单放宽到收任意串 | exit=1，`tests 27 pass 26 fail 1`，红在「清单外色值一律 bad-input」；dist 探针「不在色值允许清单里」命中 **0** | exit=0，`pass 27 fail 0`；探针命中 **1**，dist 回 `BE8F1806…` |
| ③ 删一条 a11y 属性（箭位 `aria-hidden="true"`） | exit=1，`tests 27 pass 26 fail 1`，红在「a11y 逐条…」；dist 探针 `aria-hidden="true"` 命中 **2** | exit=0，`pass 27 fail 0`；探针命中 **3**，dist 回 `BE8F1806…` |
| ④ 缺省色换硬编码字面量（`background: var(--blue)`→`#007aff`，命中 2 处） | exit=1，`tests 27 pass 25 fail 2`，红在「缺省填充色走样式段冻结 token」＋「四件样式鉴别力」；dist 探针 `background: #007aff` 命中 **2** | exit=0，`pass 27 fail 0`；探针命中 **0**，dist 回 `BE8F1806…` |

四次写回的 sha256 同值读数**全部为真**：`DBA73FB9FD75C11E3F9DEA571E994A08631CEB6F61047323EB596779A48F1FB1`（＝另存件 `blocks.ts.seat5green`；收尾再验一次同值）。

## 5. 逐字节对照（甲法：四件产物与样式段**零差异**）

前席取样件 `.scratch/t431/ab-before.json`／`ab-after.json` 两件同 sha256（`CD51987C…`）；本席用同一支取样器
（`.scratch/t431/ab-snapshot.mjs`）在**本席绿建**上重取 `.scratch/t431/ab4-now.json` 逐条对账：

- `blocksCss()` sha256 两侧同值 `c794e19b3be90c72b1769a2c5294f6ab1cbf3dfb7f9f381b41da1d1487beb198`（长度 10603）→ **样式段零变**；
- 8 件产物（4 件缺省调用＋4 件给色调用）sha256 **逐条同值，差异 0 条** → **结构零变**。

所以甲法在本票的改动面就是**入参校验＋注释**：缺省色本来已是冻结 token（`HEAD` 版 `blocks.ts` 里 `background: var(--blue);` 已出现 2 次，
`isFrozenToken`／`CSS_NAMED_COLORS` 出现 0 次），故票面「只有样式段该值变」在甲法下退化为「零差异」——产物与样式段都不动。

## 6. GATE-RUN 对账（窗口 `--since 2026-09-14T12:23:00Z --until 2026-09-14T12:30:00Z`）

窗口内 `ticket=431` 的 `RUN` 条目共 **13 条，逐条都在下面声明，无一条未声明；13 条全 exit=0**，故**不需要 `GATE-RELAX`**。
窗口外的条目属前席与别席（前席两条 `exit=1` 是先红读数，见其 11:55 记录）；**本件自身的 `git add`／`git commit`／`git push`
与对账自检运行也在窗口之外（自指面），按惯例不列入此表**。

```
GATE-RUN runId=2fc56d67-bfe6-4e14-a539-b675c54b2ef5 cmd="pnpm build"
GATE-RUN runId=099730c2-b367-4d5f-8e0d-e244b8ce72bd cmd="node --test packages/base-render/test/page-viz-421.test.mjs"
GATE-RUN runId=2f346429-5ad2-4feb-8449-1944b8e85e15 cmd="node --test packages/base-render/test/blocks.test.mjs"
GATE-RUN runId=3eb7ee76-e80d-4354-b14a-02f039af53ce cmd="node --test packages/base-render/test/style.test.mjs"
GATE-RUN runId=2b191c09-d922-4732-9625-c7d87a357e15 cmd="node --test packages/base-render/test/page-finish-420.test.mjs"
GATE-RUN runId=1078f185-2492-4f73-9e14-efc7638d0693 cmd="pwsh -File .scratch/t431/mutate4.ps1"
GATE-RUN runId=6dd13eb5-a588-4ca3-a587-6d5a1bdc4bbf cmd="pwsh -File .scratch/t431/fix-dist.ps1"
GATE-RUN runId=c59a6b71-4240-4d9e-9be3-383eb95bccf5 cmd="pwsh -File .scratch/t431/mutate5.ps1"
GATE-RUN runId=1a54f38a-0444-4bd8-956f-5c62aeff6465 cmd="pnpm build"
GATE-RUN runId=3b27dcd4-a586-4702-8cc1-9e5ff31693d9 cmd="node --test packages/base-render/test/page-viz-421.test.mjs"
GATE-RUN runId=45c407ad-40d5-4a92-9fcf-8917cbe7eeda cmd="node --test packages/base-render/test/blocks.test.mjs"
GATE-RUN runId=221419fc-1f28-4364-a0fd-9962c063ea3a cmd="node --test packages/base-render/test/style.test.mjs"
GATE-RUN runId=075a67fd-4a2c-4f44-9e71-a44ff1faff62 cmd="node --test packages/base-render/test/page-finish-420.test.mjs"
```

## 7. 未做项与遗留

- 票面遗留出口 S3-7（`blocksCss()` 全表另有既有色值字面量）**不在本票**，未动。
- 本件未新增前端展示页；四件的视觉面改动为零（§5）。
