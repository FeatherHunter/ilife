# #242 证据骨架（实施席 636MAP WaveD）

> 票面：`base-render` 共享层 help 模板与 schema 两处对不上。
> 修法：以模板为准改 schema（模板是用户肉眼看到的东西），模板逐字不动。
> 写集：`src/spec/help.ts`＋`src/help.ts`＋测试＋`docs/base/base-render/t242-*`；他票文件一律不碰。

## 一、影响清单（结构纪律第一步）

- `packages/base-render/src/spec/help.ts`：加两处类型与 schema（`contact.items[].url` 可选、`init_banner.steps` 兼容对象形），理由是机读权威须收下模板已读的键。
- `packages/base-render/src/help.ts`：渲染侧对齐模板（对象形 steps 可渲染、带 `url` 的联系人可点），理由是 B 路 HTML 与 A 路模板观感须一致。
- `packages/base-render/test/help-t242.test.mjs`（新建）：两处错位的红绿变异锁，理由是源码改必有测试见证。
- `docs/base/base-render/t242-*`：本证据与门禁记录，理由是件归属目录。

## 二、核验锚点（改前实测，2026-09-16）

- `src/spec/help.ts:68-74`：`SceneInitBanner.steps` 仍 `readonly string[]`。
- `src/spec/help.ts:76-84`：`SceneContactItem` 仍 `{label,value}`、`copy_all` 仍 `string`。
- `src/spec/help.ts:198-226`：schema 仍闭集（steps 仅 `string` 项、contact 仅 `label/value`、`copy_all` 仅 `string`）。
- `src/help.ts:340-362`：`validateSceneData` 仍 fail-closed（首个命中即抛）。
- `assets/help-template.html:1790`：`INIT_BANNER && !INIT_BANNER.hidden`＋`closable === false` 判逐字在。
- `assets/help-template.html:1791`：`INIT_BANNER.steps.map` 读 `st.title`／`st.desc` 逐字在。
- `assets/help-template.html:1819`：`if (it.url && String(it.value).indexOf('http') === 0)` 逐字在。
- 生产形态：`skill-bill/schedule/home/chef` 四家 `url?: true`（布尔旗标）＋`copy_all: true`（布尔），走 A 路；备忘录 `HELP_INIT_STEPS` 为 `{title,desc}[]` 六步。
- 包名：`packages/base-render/package.json` 名确为 `base-paint`，`gen:help-shell` 链为 `assets→helpShell` 有效。

## 三、修法口径（不扩面）

只消票面两处，不碰 `hidden`／`closable`／`copy_all` 布尔／`aliases`（仍走 A 路绕行，由他票定）：

1. `contact.items[]` 收下可选 `url`（布尔或字符串皆收：生产四家用 `true` 旗标，票面附言写字符串，模板只判真值，两 side 都须绿）。
2. `init_banner.steps` 项改 `oneOf`（字符串或 `{title, desc?}` 对象；纯串数组与纯对象数组与混合数组皆可过，模板的对象形不再非法）。

模板三处判字（1790／1791／1819）逐字不动；生成物 `src/helpShell.ts` 不手改。

## 四、判据（绿为止）

- 变异红：改前 `steps=[{title,desc}]` 报 `schema-invalid`、`contact={label,value,url:true}` 报 `schema-invalid`（两行皆红）。
- 变异绿：改后两者皆过校验且渲染正确（对象步有文案、带标联系人成 `<a>`）；旧形（纯串步、无标联系人）仍绿。
- 门禁：`tsc -b packages/base-render` 全绿；包内 `help` 相关测试全绿；`gen:help-shell:check` 仍绿（模板未动）。

## 五、门禁记录（GATE-RUN，均经 `node tooling/run-locked.mjs --ticket 242 --`）

- 基线编译：`node node_modules/typescript/bin/tsc -b packages/base-render` → exit 0（runId `e74214c9`）。
- 变异红：`node --test packages/base-render/test/help-t242.test.mjs` → exit 1，6 中 4 过 2 红：
  `url:true` 报 `schema-invalid /contact/items/0/url 多余字段：url`；
  `{title,desc}[]` 报 `schema-invalid /init_banner/steps/0 类型不符：期望 string，实际 object`（runId `db0b5117`）。
- 修后编译：`tsc -b packages/base-render` → exit 0（runId `22476031`）。
- 变异绿：同新测试 6/6 全绿（runId `6ef8cc4f`）；补强后 8/8 全绿。
- 回归：`help.test.mjs` 48/48 全绿（runId `7bf65842`）；`contract-signatures.test.mjs` 48/48 全绿（runId `90e6fbf6`）。
- 模板门：`node packages/base-render/scripts/gen-help-shell.cjs --check` → `check OK` exit 0（runId `ccfd6950`，模板逐字未动）。
- 终轮：`node --test help-t242 + help.test` 56/56 全绿（runId `4da577a6`）。

## 六、未做项

- `init_banner.hidden`／`closable`、`contact.copy_all` 布尔形、`aliases` 一律未动（仍走 A 路绕行，由他票定）。
- `assets/help-template.html` 三处判字（1790／1791／1819）逐字未动；`src/helpShell.ts` 未手改。
- `blocks.ts`／`charts.ts`／`style.ts`、技能包（`skill-calorie` 全系含内）一律未碰。
- 不设 `subtitle` 渲染落点、不改 `copy_all` 语义、不碰落盘与发布。
