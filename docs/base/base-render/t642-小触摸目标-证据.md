# #642 HELP 小触摸目标 · 证据（实施席）

> 范围：只动公共层 HELP 母版**第一段 `<style>`**（壳自有段）＋它的两件机器生成物＋判据件；**第二段
> （页面侧控件库烘焙段）逐字节未动**（本件 §6 以 sha256 冻结）。
> 口径：**命中区 ≥44×44、视觉尺寸一字不动**（沿 #525 裁定——触摸区 ≥44 是全宽口径、可点控件最小高取 44）。
> 工作区留未提交状态，本席全程零 git 写操作；关票由编排者收。

## 0. 影响清单（五件，多一件没有）

| # | 文件 | 一句话 | 写者 |
|---|---|---|---|
| 1 | `packages/base-render/assets/help-template.html`（母版＝唯一真相源） | 第一段末尾追加命中区层（3 行注释＋3 行规则），`+6/-0` | 本席手工 |
| 2 | `packages/base-render/src/helpShell.ts`（**机器生成物**） | `HELP_SHELL_PREFIX` 字面量随源重出（1 行） | `gen-help-shell.cjs` |
| 3 | `packages/base-render/test/help-shell-136.test.mjs`（**机器生成物**） | PREFIX 哈希＋夹具哈希重出（2 行，SUFFIX 哈希未变） | `gen-help-shell.cjs` |
| 4 | `packages/base-render/test/mobile-base-507.test.mjs` | 顶部「未覆盖」条目收口＋文末 #642 五条判据 | 本席手工 |
| 5 | 本证据件 | 判定表／读数／变异两行／门线对账 | 本席手工 |

生成物由生成器重出（手未碰）：`src/helpShell.ts` `2 +-`、`test/help-shell-136.test.mjs` `4 +-`（仅哈希字面量）。

## 1. 改动明细（母版行号为改后）

```diff
@@ -163,6 +163,12 @@
     .sheet{left:0;right:0;bottom:0;border-radius:22px 22px 0 0}
   }
+  /* ===== #642 小触摸目标：命中区扩到 ≥44×44（视觉尺寸一字不动）=====
+     覆盖层挂在按钮自己的 ::after 上（绝对定位不参与布局，不吃独立点击事件）——命中的仍是按钮本体，
+     故 [data-c] 点击委派与 .mini 卡片点击口径不变；三处图标键本已 position:absolute，只补 ::after，不再重复给 relative。 */
+  .mini .copy-btn,.init-banner .copy-btn,.copy-btn.a-copy{position:relative}
+  .mini .copy-btn::after,.init-banner .copy-btn::after,.copy-btn.a-copy::after{content:"";position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);width:max(100%,44px);height:max(100%,44px)}
+  .ib-close::after,.s-clear::after,.sheet .s-close::after{content:"";position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);width:max(100%,44px);height:max(100%,44px)}
 </style>
```

- 三个 copy 类补 `position:relative`（原来无定位）；三处图标键**本已** `position:absolute`（`L34/L73/L140`），
  只补 `::after`，不叠 `relative`（判据 ③ 逐条钉住）。
- 覆盖层 `max(100%,44px)` 双向：本体大于 44 时取本体（不缩水），小于 44 时**只为命中面扩到 44**，视觉尺寸零改。
- 伪元素不吃独立点击事件，命中的仍是按钮本体 ⇒ `screen` 上的 `e.target.closest('[data-c]')` 委派、
  `.mini` 卡片点击、`.s-clear` 显示切换口径全部不变。
- 几何注记（源码级推演，非像素门）：① `.mini{overflow:hidden}` 会裁覆盖层——卡片复制按钮高 26px 居底行、
  下方还有 9px 卡内 padding，居中 ±9px 恰落在卡内，命中面不缩水；② `.ib-close` 覆盖层上缘越出横幅 3px，
  横幅无 `overflow:hidden`，视觉无痕（`.init-banner .copy-btn` 的 `margin-right:26px` 与关闭键不重叠）。

## 2. 13 条 `min-height` 超线（0<x<44px）逐条判定表

判定口径：超线＝ `0 < min-height < 44px`（`min-height:0` 是 flex 折叠复位、`.subgroup .sg-body`／`.sheet .s-scroll`
两处，不是命中尺寸声明，不入扫描面）。扫描面＝母版两个 `<style>` 块，逐规则取 `min-height` 声明行。

| # | 段 | 改前行 | 改后行 | 选择器 | min-height | HELP 会渲染的可点控件？ | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 第一段 | L33 | L33 | `.init-banner .copy-btn` | 30px | 是（首屏横幅复制按钮） | **命中层覆盖**（真控件①） |
| 2 | 第一段 | L55 | L55 | `.about-row .a-copy` | 28px | 是（关于行「一键复制」，元素类 `copy-btn a-copy`） | **命中层覆盖**（真控件②） |
| 3 | 第一段 | L84 | L84 | `.copy-btn` | 36px | 基类，HELP 四处实例全被更具体规则遮蔽 | 不改（单改零效果，见下注） |
| 4 | 第一段 | L109 | L109 | `.mini .m-top` | 18px | 否（chip 视觉容器；点击目标是父级 `.mini`） | 不改（非控件） |
| 5 | 第一段 | L114 | L114 | `.mini .copy-btn` | 26px | 是（场景卡复制按钮） | **命中层覆盖**（真控件③） |
| 6 | 第二段 | L1223 | L1229 | `button.copy.ghost` | 40px | 否（页面侧控件库，HELP 不渲染） | 一律不碰 |
| 7 | 第二段 | L1324 | L1330 | `.sl-widget-input` | 40px | 否（同上） | 一律不碰 |
| 8 | 第二段 | L1538 | L1544 | `.ss-search` | 40px | 否（同上） | 一律不碰 |
| 9 | 第二段 | L1588 | L1594 | `.ss-more` | 36px | 否（同上） | 一律不碰 |
| 10 | 第二段 | L1606 | L1612 | `.ss-new input` | 40px | 否（同上） | 一律不碰 |
| 11 | 第二段 | L1619 | L1625 | `.ss-new button` | 40px | 否（同上） | 一律不碰 |
| 12 | 第二段 | L1634 | L1640 | `.ss-empty` | 36px | 否（同上） | 一律不碰 |
| 13 | 第二段 | L1656 | L1662 | `.ss-chip` | 38px | 否（同上） | 一律不碰 |

- 计数对账：**13 ＝ 第一段 5 ＋ 第二段 8**（判据 ⑤ 把这条口径钉死在测试里）。
- 注（第 3 行为何不改）：`.copy-btn` 基类在 HELP 的四处实例分别被 `.mini .copy-btn`(26)／`.init-banner .copy-btn`(30)／
  `.about-row .a-copy`(28)／`.sheet .s-actions .copy-btn`(48) 覆盖，基类 36px 对 HELP 无生效面，**单改它零效果**。
- 第 6–13 行（第二段）的实例化者 `formPrompt`／`selectList`／`smartSelect` 在 HELP 里零调用 ⇒ HELP 页不渲染它们；
  该段是结果页 SoT，本票**一字未碰**（§6）。本席复核：这三个名字在母版里只出现在定义与注释处（`:550/554/616/630/858/902`），
  `.ss-*`／`.sl-*`／`.fp-*` 类名只出现在控件库自己的串里（`:516–1108`），渲染主流程（`GROUPS.forEach`／about 段，`:1790` 起）不产出它们。

## 3. 六个真控件读数（改动前视觉 ／ 改后命中区）

| 控件 | 元素出处 | 改动前视觉 | 改后命中区 | 覆盖层规则 |
|---|---|---|---|---|
| 首屏横幅复制 | `help-template.html:1796` 模板串 | `min-height:30px` | `max(本体,44px) × max(本体,44px)` ≥44×44 | `.init-banner .copy-btn::after` |
| 关于行复制 | `:1833` 模板串（`class="copy-btn a-copy"`） | `min-height:28px` | 同上 | `.copy-btn.a-copy::after` |
| 场景卡复制 | `:1811` 模板串 | `min-height:26px` | 同上 | `.mini .copy-btn::after` |
| 横幅关闭键 | `:1796` 模板串 | `22×22`（width×height） | 同上 | `.ib-close::after` |
| 搜索清空键 | `:1799` 模板串 | `20×20` | 同上 | `.s-clear::after` |
| 弹层关闭键 | `:183` 静态壳 | `30×30` | 同上 | `.sheet .s-close::after` |

读数由只读探针逐条比对「源文本改动前基准」（从现文件删掉本席插入的 6 行重建，并用 sha256 校核为改动前原文）得出。

## 4. 判据（`test/mobile-base-507.test.mjs` 文末 #642 组，5 条）

| 判据 | 断言内容 |
|---|---|
| ① | 第一段超线条**恰为既知 5 条**（三条真控件 `=30/28/26` ＋ 两条钉住的非命中项 `=36/18`）；除两条非命中项外全部落三条控件白名单 |
| ② | 第一段 `::after` 选择器集合**恰为六条命中目标**；每条 `width`／`height` 双向 ≥44px；命中层不得夹带 `min-height`；须为 `position:absolute` |
| ③ | `position:relative` **只给三个 copy 类**（图标键本已 absolute，重复给即红）；三处图标键 `22×22`／`20×20`／`30×30` 与 `position:absolute` 一字不动 |
| ④ | 第二段（第二个 `<style>` 块）sha256＝改动前取值；第二段超线条仍是页面侧 8 条 |
| ⑤ | 计数对账：第一段 5 ＋ 第二段 8 ＝ 13 |

顶部「未覆盖（另有归属）」的**「触摸目标分级（等用户裁定档位）」条目按本票收口**（改为记录 #642 口径），
viewport／数值列等宽栈／blocks 第三栈三条仍挂他人票号，不动。

## 5. 机器生成物：红→绿对（源改后未跑 gen 必红）

| 步骤 | 命令 | exit | 关键行 |
|---|---|---|---|
| 基线（树干净，改动前） | `gen-help-shell.cjs --check` | 0 | `help-template check OK: prefix=e01d19e6…613ea7…`／`suffix=b2304de5…` |
| **源已改、未跑 gen** | `gen-help-shell.cjs --check` | **1** | `DRIFT base-render\src\helpShell.ts committed=f0e52280…`／`DRIFT base-render\test\help-shell-136.test.mjs committed=2e318eac…` |
| 跑 gen | `gen-help-shell.cjs` | 0 | `wrote base-render\src\helpShell.ts 118952 bytes; chunks: prefix=1 suffix=6` |
| 跑 gen 后 | `gen-help-shell.cjs --check` | 0 | `help-template check OK: prefix=596e8368…0f39f74`／`suffix=b2304de5…` |

**SUFFIX sha256 两行（本票「第二段未动」的机器读数）**：

```
SUFFIX sha256 改前 = b2304de599e5b2f48aaf56d6742628d78cf76184523dcd40c35df4e7e40697b5   （基线 check 输出）
SUFFIX sha256 改后 = b2304de599e5b2f48aaf56d6742628d78cf76184523dcd40c35df4e7e40697b5   （gen 输出；两值相等）
```

源文本侧同口径复算：`suffix_sha256 改前=改后=b2304de5…`；`template_sha256 改前=537c05be…` → `改后=eefbebc7…`。

## 6. 第二段逐字节冻结（sha 取值方式说明）

- 取值方式：对母版里**第二个 `<style>…</style>` 块**（含两端标签、源文 CRLF 原样、utf8）取 sha256；
  该段即页面侧控件库烘焙段（改后 L1154–1664）。
- `seg2_style_sha256 改前 = 01aeb17f5890ecbd063f55d6da468d4fd52412206d1856abc8bc6da7c0894629`
- `seg2_style_sha256 改后 = 01aeb17f5890ecbd063f55d6da468d4fd52412206d1856abc8bc6da7c0894629`（相等）
- 佐证：SUFFIX（help-data 配对闭标签至文末，含第二段＋运行时）sha256 亦逐字节相等（§5 两行）。
- 该常量已写进判据 ④（`SEG2_STYLE_SHA256`），后续任何人碰第二段即红。

## 7. 变异自证两行（机器读数）

```
MUTATION: fail=1 pass=11 exit=1   （把三处图标键覆盖层的 height:max(100%,44px) 改成 30px；只动这一处）
   红件恰为 ②：AssertionError: .ib-close::after 命中区高 30px < 44px（#525 裁定：触摸区 ≥44）
RESTORE: pass=12 fail=0 exit=0    （逐文件取回本席改过的母版副本；模板 sha256 回 eefbebc7…＝备份值）
```

## 8. 门线实测（收尾四道，全部经 `tooling/run-locked.mjs --ticket 642`）

| # | 命令 | exit | 实测输出（一行） |
|---|---|---|---|
| 1 | `node node_modules/typescript/bin/tsc -b packages/base-render` | 0 | 无诊断输出（tsc 静默即绿） |
| 2 | `node --test packages/base-render/test/mobile-base-507.test.mjs packages/base-render/test/help-shell-136.test.mjs` | 0 | `ℹ tests 17 / ℹ pass 17 / ℹ fail 0` |
| 3 | `node packages/base-render/scripts/gen-help-shell.cjs --check` | 0 | `help-template check OK: prefix=596e83686928d44a7ca9b6556018b548c253dcf64db058242d64478030f39f74 suffix=b2304de599e5b2f48aaf56d6742628d78cf76184523dcd40c35df4e7e40697b5` |
| 4 | `node --test "packages/base-render/test/*.test.mjs"`（回归兜底） | 0 | `ℹ tests 746 / ℹ pass 746 / ℹ fail 0` |

- 第 4 道命令形说明：票面给的 `node --test packages/base-render/test/`（目录形）在本机 Node v24.19.0 下被按
  入口模块解析 → `Error: Cannot find module 'D:\ilife\packages\base-render\test'`（`tests 1 / pass 0 / fail 1`，
  同样留痕；无斜杠形同错），故改用 glob 形跑满全目录 `*.test.mjs`。两读都留痕，最终以 glob 形 746/746 为准。

## 9. GATE-RUN 对账行

```
GATE-RUN: ticket=642 在 .scratch/locks/gate-runs.log 内共 21 条 RUN 行（START 同数），全部出自本席 runId；
  其中 exit=0 17 条、exit=1 4 条——4 条红灯＝「源已改未跑 gen 的 check」（成对红→绿的红端）、
  「变异态测试」（MUTATION 红端）、「目录形回归两读」（命令形不支持，见 §8 说明）。
  收尾四道门（全部在最后一次落笔之后重跑）与日志逐条对齐：
  tsc(exit=0) / 两件测试(exit=0, pass 17 fail 0) / gen:help-shell:check(exit=0) / glob 回归(exit=0, 746/746/0)。
```

## 10. 边界（本票没做的事，白纸黑字）

- 第二段（页面侧控件库烘焙段）**一字未改**：8 条超线项留给结果页自己的票（HELP 不渲染其实例化者）。
- `.copy-btn` 基类与 `.mini .m-top` **不改**（前者被遮蔽、后者非控件）——两条以固定名单钉进判据 ①，多一条即红。
- 未动 `skill-*/` 下任何件、未动他席在途改动（skill-calorie／plugin-calorie 等）、未做任何 git 写操作。
- 未跑 `pnpm build`／`npx tsc`；编译入口写死 `node node_modules/typescript/bin/tsc -b packages/base-render`。
