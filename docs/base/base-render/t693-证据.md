# t693 证据：HELP 复制提示落不到视口底部

票面：[#693](https://github.com/FeatherHunter/ilife/issues/693)（`bug` ＋ `ready-for-agent`，Agent Brief 在同票评论里）。
一句话结论：**修好了**——改动面是共享 HELP 模板里的一行（运行时给提示栈补上定位样式所用的类名），
加上重跑的生成物与一条新回归用例；六条新判据全绿，变异自证红／绿两态分明，六家消费面 40 例全绿。

## 一、现象与复现

用户报告：HELP 页面点复制按钮，底部不出提示，只能隐约看到一点黑边。

复现件用**真实渲染出口** `base-paint/help-shell` 出页，再在页尾追加一段触发脚本（load 时点一次页面自带的
复制按钮 `.copy-btn[data-c]`，走的是页内全局委派 → `doCopy` → `showToast` 这条真路径；之后每 3.5s 再点一次，
好让截图时提示仍在屏上）。生成器 `t693-repro.mjs` 随本件入仓，可复跑。

| 态 | 短页（24 场景） | 长页（96 场景，滚到底） |
| --- | --- | --- |
| 修前 | 视口内**看不到任何提示**：`t693-复现-短页-修前.png` | 提示卡落在文档流末尾，被固定底栏盖住，**只露一点黑边**：`t693-复现-长页-修前.png` |
| 修后 | 悬浮在视口底部、居中、完整可见：`t693-复现-短页-修后.png` | 同样悬浮，压过固定底栏、不被遮挡：`t693-复现-长页-修后.png` |

截图口径：headless Chrome，视口 1280×800。

### 真产品页复核（不是夹具页）

另用**真实技能**出页复核一次：`packages/skill-memo-ilife` 的 HELP（30 场景、真实文案与初始化横幅），
由技能自己的 dist 出口渲染、`base-paint/help-shell` 走工作区包解析（即本票当刻产物），页尾同样追加一次复制按钮点击。
读数：提示悬浮在视口底部、居中、压过固定底栏 —— `t693-复现-真产品页-修后.png`。

## 二、根因

提示栈的定位样式按**类名** `.hm-toast-stack` 下发：

```
.hm-toast-stack{position:fixed;left:50%;bottom:calc(24px + env(safe-area-inset-bottom,0));
                transform:translateX(-50%);z-index:9999;…}
```

而运行时创建栈元素时只写了 `id`：

```
stack = document.createElement('div');
stack.id = 'hm-toast-stack';          ← 修前：只有 id，选择器对不上
stack.className = 'hm-toast-stack';   ← 修后：补上定位样式所用的类名（id 保留做兼容）
```

两者对不上 ⇒ 那条 `position:fixed` 从未生效 ⇒ 栈按普通块级元素排在文档末尾：短页在首屏之外，
长页滚到底被固定底栏（`.tab-bar`，`z-index:600`）压住，只剩深色卡上沿一条黑边。这与用户描述逐条对上。

同源的副产物（一并被这一行修好）：`@media (prefers-reduced-motion: reduce)` 里那条
`.hm-toast-stack .hm-toast{transition:none}` 也一直没命中过（祖先类不存在），修后按设计生效——
减动效档下提示不再跑入场过渡，但**仍然即时可见**（可见性由 `.show` 类切换，不靠过渡）。

## 三、改动清单

| 文件 | 改了什么 |
| --- | --- |
| `packages/base-render/assets/help-template.html` | 模板源（唯一真相源）：`getStack()` 里给栈元素补 `className = 'hm-toast-stack'`，并写一句为什么（一行 ＋ 一行注释） |
| `packages/base-render/src/helpShell.ts` | 生成物，重跑 `gen:help-shell` 派生，未手改 |
| `packages/base-render/test/help-shell-136.test.mjs` | 生成的哈希锁（前后缀与夹具哈希），同上重跑派生 |
| `packages/base-render/test/help-toast-stack-693.test.mjs` | 新增：六条回归判据 |
| `docs/base/base-render/t693-证据.md`、`t693-gate-runs.md`、`t693-repro.mjs`、`t693-mut.mjs`、四张复现截图 | 本件与可复跑脚本 |
| `.changeset/693-help-toast-stack.md` | `base-paint` patch 记账 |

生成物哈希：前缀 `596e83686928d44a7ca9b6556018b548c253dcf64db058242d64478030f39f74`（未变）、
后缀 `b2304de599e5b2f48aaf56d6742628d78cf76184523dcd40c35df4e7e40697b5 → 6f1bf697223e512153c087e41a5c78d886d0e0a138312d00a3edaebc86f38b3f`（随模板改）。

## 四、判据

### 4.1 新回归用例（`node --test packages/base-render/test/help-toast-stack-693.test.mjs`，6/6 绿）

用例读的是**产出物本身**（`HELP_SHELL_SUFFIX`，即各技能 HELP 落盘文件里那段运行时），不是复制品；用最小 DOM 桩
把它跑起来，再拿它自带的 CSS 串去比对：

1. 栈元素必须匹配定位选择器——CSS 按类名下发，运行时就得给同一个类名（反向自证：只带 id 的同种元素必须不匹配；并锁 id 仍在）；
2. 定位规则本体仍锁在视口底部（`position:fixed`／底部锚点 `calc(24px + env(safe-area-inset-bottom,0))`／居中变换／`z-index > 600` 压得过固定底栏）；
3. 提示卡挂在栈下：多条提示共用一个栈，不许各挂 body；
4. 栈契约不变：桌面容量 5、窄视口（≤820px）3、单条寿命 4500ms；
5. 类名只有一条真相：注入样式那条规则的类名与运行时给栈元素的类名必须同源；
6. 警告态（「请先填写…」那条）与成功态落在同一个悬浮栈。

### 4.2 消费面（六家技能 HELP 落盘文件的模板运行时）

```
node --test packages/skill-memo-ilife/test/help-dom-243.test.mjs packages/skill-schedule/test/help-file-202.test.mjs \
  packages/skill-home/test/help-delivery-190.test.mjs packages/skill-chef/test/help-delivery-216.test.mjs \
  packages/skill-bill/test/help-exit-148.test.mjs
```

读数为 40/40 绿（其中备忘录／作息两件是把模板运行时真跑起来数 DOM 的用例）。

### 4.3 生成管线不漂移

`node packages/base-render/scripts/gen-help-shell.cjs --check` → exit 0，`help-template check OK: prefix=596e8368… suffix=6f1bf697…`。

### 4.4 本票门禁窗口（连续一段，逐条声明；对账命令见 §4.7）

门禁＝**一段连续窗口** `2026-09-18T10:37:07.594Z → 2026-09-18T10:37:14.465Z` 里的全部运行，七条逐一走加锁包装器，
窗口内**没有**任何未声明的运行条目。跑法：`pwsh -NoProfile -File .scratch/t693/gate-window.ps1`（窗口日志 `.scratch/t693/gate-window.log`）。

```
GATE-RUN runId=180d8759-c908-4e3c-96e1-784d69041256 cmd="node node_modules/typescript/bin/tsc -b packages/base-render"
GATE-RUN runId=9d8d163e-7aa4-4a29-b427-1a56676767ec cmd="node docs/base/base-render/t693-mut.mjs"
GATE-RUN runId=556741cf-6b76-40f1-a27c-52c26be14212 cmd="node packages/base-render/scripts/gen-help-shell.cjs --check"
GATE-RUN runId=0c4fc890-fc46-431f-a3ef-5668931e9d5f cmd="node --test packages/base-render/test/help-toast-stack-693.test.mjs"
GATE-RUN runId=d356c887-deab-45ad-895b-863128cd106d cmd="node --test packages/base-render/test/help-shell-136.test.mjs packages/base-render/test/mobile-base-507.test.mjs"
GATE-RUN runId=7ee6a73f-505a-4880-b769-38c83a002155 cmd="node --test packages/skill-memo-ilife/test/help-dom-243.test.mjs packages/skill-schedule/test/help-file-202.test.mjs packages/skill-home/test/help-delivery-190.test.mjs packages/skill-chef/test/help-delivery-216.test.mjs packages/skill-bill/test/help-exit-148.test.mjs"
GATE-RUN runId=4449c352-43a6-4836-97de-5ad3d263b090 cmd="node docs/agents/t540-指纹绑定.mjs --scope packages/base-render"
```

逐条读数（全部 exit 0）：编译无诊断；变异自证 `MUTATION red: fail=3` / `RESTORE green: pass=6 模板逐字还原=true`；
`check OK: prefix=596e8368… suffix=6f1bf697…`；693 用例 `tests 6 / pass 6 / fail 0`；
136＋507 `tests 17 / pass 17 / fail 0`；六家消费面 `tests 40 / pass 40 / fail 0`；指纹绑定见 §4.6。

### 4.6 指纹绑定（协议 §2.6：以 `dist` 为前提的读数须与编译指纹同证）

本票的核心判据都以 `dist/helpShell.js` 为前提（693 用例第 21 行直接从 `../dist/helpShell.js` 取那段运行时）。
故补一条指纹绑定读数（runId `4449c352-43a6-4836-97de-5ad3d263b090`，与门禁窗口同段）：

```
node docs/agents/t540-指纹绑定.mjs --scope packages/base-render
RESULT: src-n=23 dist-n=92 src-sha=49a58fe7af2f015c dist-sha=09470b6c8e36730a 一致（dist 与 src 指纹一致）
selftest-dist-变动件=0 tsc-exit=0
```

即当刻 `dist` 与 `src` 同证、窗口内没有别人动本包 `src`。独立复核席位另跑一次（runId `c55d292f-e632-4f17-92bf-4b2be8c33677`）
得到**同一对指纹**（`src-sha=49a58fe7af2f015c`／`dist-sha=09470b6c8e36730a`），两证相合。

### 4.7 门禁对账（`tooling/check-gate-audit.mjs`，协议 §2.4）

```
node tooling/check-gate-audit.mjs --evidence docs/base/base-render/t693-证据.md --ticket 693 \
  --since 2026-09-18T10:37:07.594Z --until 2026-09-18T10:37:14.465Z
```

读数见 §六 末（该命令自身在窗口之外，故不需要也不应该被声明）。

### 4.5 全仓全量一轮（对账用，不作为本票门禁口径）

`node tooling/run-locked.mjs --ticket 693 -- pnpm test` → **3405 例、3352 绿、53 红**。
53 条红全部落在别家在途面（calorie 各场景多票、备忘录打包与烟囱、插件烟囱、模板 A8 等），
**无一条涉及 HELP 模板或提示栈**；本票六条新判据在全量里同样是绿（日志里逐条为 `✔ #693 ①…⑥`）。
本仓是多席共享工作区，当刻树上有他人未提交改动，故这 53 红**不作本票口径**——本票口径是 4.1–4.3。

## 五、变异自证（源码级：把修复从模板源上拿掉，再走一遍生成管线）

跑法：`node tooling/run-locked.mjs --ticket 693 -- node docs/base/base-render/t693-mut.mjs`（窗口内直调，不嵌套抢锁）。

```
MUTATION red: test exit=1 fail=3 失败用例行=7
RESTORE  green: test exit=0 pass=6 fail=0 模板逐字还原=true
RESULT: PASS mutate(gen=0,tsc=0,tallTop=0,tallBottom=0) restore(gen=0,tsc=0)
```

红的三条正是「栈元素匹配定位选择器」那一类（①／⑤／⑥），结构化与契约类三条保持绿——变异打的是根因，不是把整套用例打红。
还原＝把模板源逐字写回（脚本 `finally` 里做），随后重跑生成管线与编译器；`模板逐字还原=true` 即逐字节相同。

## 六、过程运行（非门禁声明，逐条披露）

本票的实施是迭代着做的：先复现、再反复调试用例、再试变异脚本，最后才在**一段连续窗口**里冻结门禁（§4.4）。
下面这些运行**都不在门禁窗口内**，也**不作为门禁证据**（协议 §2.4-3④ 只认领 `exit=0` 的条目），在此逐条披露以便复核者查漏：

```
过程运行 runId=3117c72a-c131-4149-9f9d-649acb637611 exit=1  调试红：用例首版缺 esc 桩
过程运行 runId=8d4cdca4-313a-4a51-b57b-89a066cee26e exit=1  调试红：文本节点缺 classList
过程运行 runId=5ca3ed85-82a4-48fb-8540-7ca7e70ae372 exit=1  草稿变异窗首跑（脚本自身机读字段写错，见下）
过程运行 runId=a6e0d3a6-2cb1-4a3c-8b25-a074e8fe772e exit=0  修前基线编译
过程运行 runId=c8990c55-2235-46ab-9243-222a560b5d76 exit=0  修后重跑生成管线
过程运行 runId=7ade8ac5-90f7-4667-8d9d-6e47d4eabc81 exit=0  修后编译
过程运行 runId=64e24c41-bd11-4259-a136-d6b09c26008d exit=0  用例首绿
过程运行 runId=bc721654-6fb2-4350-8231-b2bb60614e32 exit=0  草稿变异窗（机读字段修正后）
过程运行 runId=a26795fe-46e7-4bf0-834c-99db83f71bce exit=0  草稿变异窗（加长页复现件）
过程运行 runId=a0bdfa2f-4d13-4a86-b3d0-860cad2587c7 exit=0  用例六条定型后复跑
过程运行 runId=304f77ac-3d60-46ad-9a29-2704bbf497b0 exit=1  base-render 全包 752 例：751 绿 1 红（范围外，见 §七.1）
过程运行 runId=9e321d79-761e-4ac5-beef-a0198e865bfd exit=0  草稿变异窗（六条用例版）
过程运行 runId=044aefe1-420f-4cb3-8b86-6a5d5cef21c0 exit=0  生成管线不漂移
过程运行 runId=6b617377-a5a0-4351-a6f6-a7c1c61cf300 exit=0  入仓版变异脚本首跑（docs/base/base-render/t693-mut.mjs）
过程运行 runId=d6e96e86-cbd3-472e-a4f6-df20971a4337 exit=0  六家消费面 40 例
过程运行 runId=a2dfdfe7-243c-4965-9bdb-d0c4968b6493 exit=0  指纹绑定（并入门禁窗口前的一次）
```

关于 `5ca3ed85`（唯一的 exit=1 变异窗）：那是我自己草稿脚本的机读字段写错（把摘要计数取成了不存在的字段），
脚本**当次就把模板逐字还原并重跑生成管线**；随后 `bc721654` 起为修正版，入仓版 `t693-mut.mjs` 是同一件事的定稿，
门禁窗口里的 `9d8d163e` 即入仓版实跑。非 0 的还有两条调试红与一条范围外红，逐条性质已在上表写明。

### 门禁对账读数（§4.7 那条命令）

```
node tooling/check-gate-audit.mjs --evidence docs/base/base-render/t693-证据.md --ticket 693 \
  --since 2026-09-18T10:37:07.594Z --until 2026-09-18T10:37:14.465Z
runId=2bce452f-4a5b-49e2-9880-ac9ad8d29929 exit=0
RESULT: matched=7/7 auditEntries=11592 scoped=7 undeclared=0
gate-audit: PASS
```

（无任何 `--allow-*` 放宽，故不需要 `GATE-RELAX`。）

窗口边界**止于门禁窗口末条运行**（`10:37:14.465Z`），对账命令本身在窗口之外——它既不是被对账的对象，也不需要被声明。

独立复核席位另用它自己的窗口跑过一次对账（runId `8d31646a-1f3c-4c85-91db-dba550f7b857`，窗口 `10:13–10:21`）：
当时 `matched=8/8` 但 `undeclared=7`（即上表那 7 条：窗口按**全程**界定、而声明只覆盖了其中 8 条）⇒ exit=1。
本节的处置是**把门禁口径定成一段连续窗口**（§4.4）并把全程运行逐条披露，而不是放宽任何开关；
复核报告 `t693-复核-独立.md` §二 缺陷 1 指的就是这件事。

## 七、范围外发现（不属本票，未动手）

1. `packages/base-render/test/template.test.mjs` 的 A8 子例「6 个数据页（memo）：先按迁移动作去包裹，再填完无残留」
   在当刻 HEAD 上红。实测归因：备忘录六页模板已由 **#665**（提交 `391bd567`）迁成裸标记形状，
   而该用例仍按迁移前形状断言「迁移前必抛 `marker-conflict`」，于是 `fillTemplate` 正常返回、
   `catchError` 里的 `assert.fail` 反被自己捕获，报成「必须是 TemplateError，实为 AssertionError」。
   判据随迁移过期，属 #665 的收尾面；本票未碰该文件（该用例不读 HELP 模板，与本票改动无关）。
2. `packages/plugin-bill-ilife` 下有他人未提交改动（`git status` 的 ` M` 五项），本票一律未碰、未提交。

## 八、到「用户手上真能用上」还差什么（出本票范围，但影响验收）

本票修的是**生成侧**：提示栈的类名随模板进 `base-paint` 的产物（`dist/helpShell.js`）。要让技能真出修好的 HELP，还差两件：

1. **发版**：`base-paint` 现版 0.3.2，各技能依赖写的是 `^0.3.0`（补丁位可升）。带本修复的补丁版发出并装进 `.dsh` 的 profile 之后，技能才拿得到修好的壳。仓内现成机制：`.changeset/693-help-toast-stack.md`（`base-paint: patch`）＋ 发版线的 wizard。
2. **重出 HELP 文件**：HELP 产物有**复用窗口**（缺省 24 小时、`--params` 的 `reuseHours` 可改，`0`＝每次都落新的）。
   24 小时内的旧文件会被原样复用——**已落盘的旧 HELP 不会自己变好**，要么等窗口过期，要么显式 `reuseHours: 0` 重出一份。

实测当刻装机态（只读核对）：

```
C:\Users\辰辰洋洋\.dsh\profiles\web\node_modules\base-paint\dist\helpShell.js
  含 stack.id：True      含 stack.className='hm-toast-stack'：False      版本：0.3.2
工作区 packages/base-render/dist/helpShell.js 的 sha256 与装机版不同（88 件里的这一件未同步）
```

⇒ **真机验收要么用本仓新出的 HELP 文件，要么先走发版线让装机版升到带修复的补丁版**（两者任选其一，见票面评论的待确认事项）。

## 九、未做项与下一手

- **真机（手机浏览器）人工确认**：本轮只在 1280×800 的 headless Chrome 与 Node 侧取证；真机手感（安全区、触摸）留待维护者过目。
- **全仓全量**：已跑一轮（读数见 4.5，3405 例 / 53 红均在别家在途面）；合并点安静态复核时如需要，按协议 §5.1 另起一轮，以那一轮的安静态为准。
- **独立复核**：复核者席位在途，报告落 `docs/base/base-render/t693-复核-独立.md`。
- 关闭口径：修复完成但未验收 ⇒ 票面进度记 95%·待确认，**未确认不 close**。
