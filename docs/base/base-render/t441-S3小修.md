# t441 · S3 小修五条（注释与证据口径）——证据件

写集：`packages/base-render/src/blocks.ts` 仅注释行、`docs/base/base-render/t441-S3小修.md` 本件。
实现语义零改动；他席路径（含 `packages/skill-calorie/triggers/` 全系、他票证据件）一律未碰。
用词照 `docs/agents/wording.md`（允许清单／命令／页面交付，不用黑话）。

## 0. 影响清单（结构纪律第一步）与交付对账（第五步）

- 改 `packages/base-render/src/blocks.ts`：只加注释字句（①四处空态注释、②允许清单迁移提示），逻辑一字不动。
- 新建本件 `docs/base/base-render/t441-S3小修.md`：记五条改前／改后读数、审计数、二选一结论、矛盾句纠正、取字节口径。
- 不碰：`t431-四件判据补硬.md`、`t428-判据硬化.md`、`t420-page-finish.md` 原件（④⑤的纠正句记于本件 §4 §5，原件由所属席落字）；
  不碰判据件与夹具（③选回归基线，故无断言改动）；不碰 `packages/skill-calorie/` 任何件（②只读审计）。
- 交付对账：本节三行与实际改动逐行对得上，偏差为零。
- 结构第四步：`packages/base-render/src/blocks.ts` 当刻 2309 行（复核 D2 实测 HEAD＝2309，超线结论不变），已超线，需要根据规则进行重构。
  超因：12 区块＋页面级四件＋打印段同住一件。拆法见 `docs/base/base-render/行数告警线评估.md`，本次先不拆：
  本票只动注释字句，结构重排另开票，待收口票认领。

## 1. ① 空态注释四处（缺失 `undefined` → `bad-input` 字样）

判据与证据已锚住该口径（`test/page-viz-421.test.mjs:433-440` 空数组 → 空串、`undefined` → `bad-input` 全绿），只差实现侧注释。
本席只加注释字句，逻辑一字不动。改前为派单当刻行号（252／457／494／526），改后为本席落字后行号。

- 第 1 处 `renderTocBlock`：改前 252 行 `空列表＝不出这一块（与「没内容不留空块」同口径，返回空串）；非数组／项缺 id／text → bad-input。`；
  改后 251–253 行 `空列表＝不出这一块（与「没内容不留空块」同口径，返回空串）；缺失（undefined）→ bad-input（非数组即拒）；项缺 id／text → bad-input。`。
  对应实现 `blocks.ts:257` 非数组即拒（`undefined` 落此分支）。
- 第 2 处 `renderDistributionRows`：改前 457 行 `rows: [] ＝ 空串（与「没内容不留空块」同口径）；行内校验逐条 fail-fast。`；
  改后 461–463 行 `rows: [] ＝ 空串（与「没内容不留空块」同口径）；缺失（undefined）→ bad-input（非数组即拒）；行内校验逐条 fail-fast。`。
  对应实现 `blocks.ts:466` 非数组即拒。
- 第 3 处 `renderChips`：改前 494 行 `项即件（不另加容器类）；items: [] ＝ 空串。`；
  改后 499 行 `项即件（不另加容器类）；items: [] ＝ 空串；缺失（undefined）→ bad-input（非数组即拒）。`。
  对应实现 `blocks.ts:503` 非数组即拒。
- 第 4 处 `renderChangeRows`：改前 526 行 `rows: [] ＝ 空串。`；
  改后 531–533 行 `rows: [] ＝ 空串；缺失（undefined）→ bad-input（非数组即拒）。`。
  对应实现 `blocks.ts:535` 非数组即拒。

## 2. ② 允许清单迁移提示（重审计生产调用点后写，不抄原句）

落点：`packages/base-render/src/blocks.ts:376-383` `optColor` 注释尾新增迁移提示 4 行（只加注释）。
审计方法：全仓检索 `renderMiniBar／renderDistributionRows／renderChips／renderChangeRows` 生产调用（`packages/skill-calorie/src/**`），
逐处看是否传 `color`，再看色值形态是否在允许清单内；测试与产物目录不计入生产调用。

审计读数（当刻盘上，如实记）：

- 显式传色生产调用 **4 处**（复核 D1 补第 4 处）：
  1. `packages/skill-calorie/src/diet/nutritionPortDocs.ts:339`（`renderDistributionRows`，经 `m.color` 传入 3 个值
     `#5856d6／#34c759／#ff9500`，定义见同件 219–221）；
  2. `packages/skill-calorie/src/render/sportPortDocs.ts:609`（`renderDistributionRows`，经 `colorOf()` 取类别色表）；
  3. `packages/skill-calorie/src/render/sportPortDocs.ts:775`（同上，第二处）。
  4. `packages/skill-calorie/src/diet/reviewDocs.ts:339-341`（`renderDistributionRows`，经 `MEAL_COLORS`（同件 `:304` 四色
     `#ff9500／#0071e3／#5856d6／#34c759`）显式传色，由 `packages/skill-calorie/src/diet/todayDocs.ts:242` 经 `buildMealDistributionBlock` 进入生产）。
  前三处之外的说明：后两处（2／3）色值单源自 `packages/skill-calorie/src/exercise/categoryColors.ts:22-27` 的 4 个值
  `#5856d6／#0071e3／#34c759／#ff9500`。以上 4 处的值形态均为 `#rrggbb`，属允许清单内，无须改动。
- 其余生产调用均不传 `color`，走缺省冻结 token `var(--blue)`（如 `home/homeViewParts.ts:101`、`body/wizardDocs.ts:126`、
  各 `renderChips／renderChangeRows` 调用），不受清单收紧影响。
- 非计数项（已排除并说明理由）：`weight/volatilityDoc.ts:257` 的 `#ff3b30` 进图表 `markLine`（图表口径，不进 `optColor`）；
  `photo/receipt.ts:57-59` 的三个色值只取 `.badge` 字串用，`.color` 字段当刻无消费方（不进四件入参）；
  `test/**` 与各页内样式串里的色值不是四件入参，不计入。

迁移句（本席现写，不抄票面原句）：见 `optColor` 注释尾，含义为显式传色 4 处均属允许形态、无须改动，
其余走缺省色，新增写法先对清单、清单外改走冻结 token 或具名色。

## 3. ③ 基线夹具二选一结论（回归基线或摘要绑定，落注释）

结论（一句）：**本夹具只作回归基线、不作权威溯源，权威出处仍以仓库历史中的 `dc16ba1^` 为准，本票不补摘要绑定断言。**

理由：补摘要绑定须动判据件与夹具并带变异两行，已超出本席写集（注释行＋本件）；
夹具现有自证（`page-finish-420.test.mjs:194-228` 四条：基线提交对照、字节摘要对照、真产物形态、禁硬编码字面量）
已够支撑回归用途；`t428-review-报告.md:60-64` 记的 D1／D2 缺口（整体换指提交即全绿、`html` 与 blob 无摘要绑定）
在本选法下转为已知约束，不当作缺陷改。落点：本节即注释落点，判据件与夹具零改动。

## 4. ④ `t431-四件判据补硬.md` §0 矛盾句纠正

改前（原件当刻行号）：§0 标题行后第 6 行 `判定：上一席实现保留（本席不动实现方向，只补收尾与机器读数）` 与第 10 行
`实现一行未改`，易读成整票零实现改动；但同件第 3 行写面为 `packages/base-render/src/blocks.ts +67／-8`，
两处并读即矛盾。

纠正句（本席现写，记于此件，原件未动）：**本席（收官席）未动实现方向；本票实现改动见 §2。**

写集说明：按本席写集（注释行＋本件），`t431-四件判据补硬.md` 原件一字未动；
上句为该件 §0 第 10 行的替换文案，请该件所属席落字。本席在此只做读数与纠正句交付，不扩面。

## 5. ⑤ 取字节口径复核提示

口径句：**复核摘要一律用与判据件同口径的取字节方式（Node `execFileSync` 取原始字节）；不用 PowerShell 管道加 `Out-File utf8NoBOM` 的转写字节。**

依据：`page-finish-420.test.mjs:176-182` 以 `execFileSync('git', ['show', ...])` 的返回字节直接做 `sha256`；
同一份 `git show <rev>:<path>` 字节若经 PowerShell 管道加 `Out-File utf8NoBOM` 落盘，编码与换行被翻译，
所得摘要与夹具记录值不同（#428 审查席实测坑）。本件复核提示即本节；
相关证据件（`t428-判据硬化.md`）的复核提示由所属席补字，本席写集受限故记于此，原件未动。

## 6. 门禁与变异两行

门禁（逐条 `node tooling/run-locked.mjs --ticket 441 -- <命令>`，终态）：

| 命令 | runId | exit | 摘要 |
| --- | --- | --- | --- |
| `node node_modules/typescript/bin/tsc -b packages/base-render` | `79e01c0e-7189-4396-b1b7-85398bd56cba` | 0 | 终态重编绿 |
| `node --test packages/base-render/test/page-viz-421.test.mjs` | `066f4baa-e595-4256-82af-479fb16176e4` | 0 | `tests 27 pass 27 fail 0` |
| `node --test packages/base-render/test/page-finish-420.test.mjs` | `b526b4c4-4fba-403e-b2ff-716d79bcd0e6` | 0 | `tests 21 pass 21 fail 0` |
| `node --test packages/base-render/test/blocks.test.mjs` | `bb352333-8b42-4027-be65-d72724b99e5b` | 0 | `tests 50 pass 50 fail 0` |

变异两行（注释改动的逻辑敏感度自证；临时改 `renderChips` 空分支 `return ''` → `return 'x'`，编译后跑件，复原后再编再跑）：

| 变异 | 改坏读数 | 改回读数 |
| --- | --- | --- |
| 空分支改写（`if (block.items.length === 0) return 'x';`） | runId `0b4a5a4b-79c2-4a66-855e-dbc3a030eca2` exit=1，`tests 27 pass 25 fail 2`，首红 `空态口径写死：空数组 → 空串；缺失（undefined）→ bad-input`（`'x' !== ''`，徽章 `[] → 空串`） | runId `066f4baa-e595-4256-82af-479fb16176e4` exit=0，`tests 27 pass 27 fail 0` |

复核取字节口径：本件复核摘要取字节用 Node `execFileSync` 原始字节口径（见 §5），不用 PowerShell 管道转写字节。
