# t410-F3 证据：分隔符形状化＋D清单（认领全部位，切写面后零改码转交）

> 作者：410-整改 F3（唯一作者）。派单：认领 D 清单全部位；`;｜·` 改独立形状，`:／#／//` 间隔号统一；
> 改 `base-render/src/style.ts` 与 `blocks.ts` 及 skill-bill render 件时与 F1／F2 切写面，跨界停手转交；持锁；
> 输出本件；回执四段。
> 声明路径（本窗唯一写路径）：`docs/base/base-render/t410-F3-证据.md`（本件）。工作日志
> `.scratch/t410-F3/`（`tsc.log`／`help-test.log`，忽略件不入仓，备查）。
> D 清单正本：`.scratch/t410-visual/D-分隔符.md`（82 行，7 符 verdict＋26 组落点，只读审计件）；
> 形状化词典出口见该件第 5 行（徽章／缺项标签／进度／分段标题／图标＋−）。

## §0 影响清单（事前声明，事后对账见 §7）

| # | 目录 | 文件 | 一句话与理由 |
|---|---|---|---|
| 1 | `docs/base/base-render/` | `t410-F3-证据.md`（本件，新建） | F3 交付物归属件目录（`doc-homes`：公共层包 `docs/base/<件名>/`），唯一写路径 |
| 2 | `packages/base-render/src/` | `style.ts`、`blocks.ts`（**只读，未改**） | 派单点名的 base 侧写面；实测他席 #567 在途未提交改动中（§2），跨界停手 |
| 3 | `packages/skill-bill/src/render/` | `helpFile.ts`、`views.ts` 等（**只读，未改**） | 派单点名的 bill render 写面；逐处被测试／老实物逐字钉死或在 D 清单 scope 外（§3），停手 |

- 事前即声明三行：一行只给一个能力（第 2 行是共用层 base-render，第 3 行是 skill-bill 的页面交付能力）。
- 共用件判据：第 2 行是 F1／F2 与本席都要读的形状层，落点争议见 §2。

## §1 结构设计（目录树／职责／公开接口）

- 本窗不新增目录、不新增源码文件、不改任何公开接口（`src/<能力>/index.ts` 零动，`SPEC_FROZEN_SURFACE` 零动）。
- 唯一交付物是本证据件：职责＝D 清单认领＋切写面裁定＋间隔号统一审计＋转交清单＋持锁门禁读数。
- 对外给的东西：0 个（文档件，无导出）。

## §2 base 侧切写面裁定（`style.ts`／`blocks.ts`：跨界停手）

- 现场（只读实测，2026-09-16 UTC）：工作树中 `packages/base-render/src/blocks.ts`、
> `style.ts` 均有他席**未提交改动**（`git diff --stat` 实测；内容抽查：`style.ts` 为 #567 J4
> 字号档 `12.5px→13px` 等 6 处、`blocks.ts` 为 #567 `cellHtml` 受信透传＋页标题 `32px→28px` 等）。
> 另 `charts.ts`／`pageShapes.ts`／`pageUi.ts`／`spec/charts.ts` 亦在途（同一次改动面）。
- 裁定：按派单「与 F1F2 切写面，跨界停手转交」，本窗**不碰** `style.ts`／`blocks.ts` 一字。
> 理由：① 路径所有权硬约束——在途未提交改动 чужой 写面，任何顺手改都会静默损坏他人工作；
> ② 同文件再改必与 #567 的 hunks 交织，提交时无法做到「暂存区只含自己的件」。
- D 清单落在 base 侧的形状资产（只读备查，不动）：
> - `src/pageShapes.ts:1-16`：用户裁定第 5 条「一个内容要用 `；`／`·` 分割就代表该处需要 UI 设计」＋
>   事实条／图片容器／时间轴条三件（替掉 `A · B · C` 串）——正是 `;｜·` 改独立形状的 base 侧词典；
> - `src/blocks.ts:272` `CALIBER_SEP='｜'`（全角竖线不再进产物文本，分隔走版式，t154-r3）；
> - `src/blocks.ts:456／524` 分布条／变更行注释内的 `｜` 仅注释记述分隔语义，非产物；
> - `src/help.ts:411` lead `N 场景 · 点场景卡看指令全文,一键复制指令`（半角 `,` 混用）与
>   `src/help.ts:611-614`、`src/helpShell.ts:58-69` 的 ` · ` 标题拼法——`help.ts` 不在派单点名写面内，
>   且 lead 被 `test/help.test.mjs:337-341`（W14）逐字钉死（截断即红），一并转交（§6 H-1）。

## §3 bill render 侧逐处核对（只读，未改；每处给不改的理由）

| # | 位置 | 可见串 | D 归属 | 裁定 |
|---|---|---|---|---|
| R-1 | `src/render/helpFile.ts:27` `HELP_FILE_TITLE='饼干记账 · 使用手册(HELP)'` | ` · `（已带空格统一） | D-·-05（HELP 标题照 D-·-02 改主标题＋版本徽章） | **转交**：模块头 `23-27` 行声明老实物逐字＋`test/help-file-145.test.mjs:34` 逐字断言；改常量即破 parity，转 F1／F2（§6 H-2） |
| R-2 | `src/render/helpFile.ts:104-105` `deriveSummaryLine`（`功能域 · N 场景 · 版本 · 更新于`） | ` · `（三处全带空格统一） | D-·-05 备查 | **合规记账**：间隔号已统一，无改动 |
| R-3 | `src/render/helpFile.ts:112` `HELP_WAKE_WORDS.join(' / ')` | 半角 ` / ` | D-/-组备查（`views:97,104` 同注：不在 32 页内） | **转交**：`test/help-file-145.test.mjs:48` 与 `test/help-exit-148.test.mjs:104` 双重逐字钉死（含字面 `饼干记账 HELP / 饼干记账帮助 / …`）；改形状须同步改模板渲染＋双测试，超 F3 窄面（§6 H-3） |
| R-4 | `src/render/views.ts:97,104` `.join('、')`（趋势／主要支出摘要） | `、` | D-、-07（明记「不在 32 页内，记账备查，接入时同改标签组」） | **转交**：分析域摘要形状（`html.ts:37` analysis 只做转义＋`<br>`，无徽章位）；改 `、` 即改摘要形状＋渲染形状两层，超窄面（§6 H-4） |
| R-5 | `src/render/helpFile.ts:49-50` 联系 URL `https://…` 内 `//` | `//` | 间隔号统一审计 | **合规记账**：功能性 URL，非分隔符懒政，不动 |
| R-6 | `src/render/helpFile.ts:126` `第一次用饼干记账?` 内半角 `?` | `?` | 非 D 清单 7 符、非派单 `;\|·:/ # //` 范围 | ** scope 外**：且模块头 `126-128` 声明文案照老实样；记账备查，不动 |

## §4 `:/ # //` 间隔号统一审计（本窗写面内，零命中即合规）

- `:#`（半角冒号＋`#`）：本窗写面（`style.ts`／`blocks.ts`／`src/render/*`）可见串**零命中**。
> 对照：审计 `审计-符号与文案.md §二1` 的 8 处 `:#`（`#报销到账 原记录:#<…>` 等，每页正文＋`data-t` 各 1）
> 落点全在 `src/record/scene-*.ts` 口径预块（F1／F2 写面），转交（§6 H-5）。
- `//`（半角双斜杠）：除 R-5 功能性 URL 外**零命中**。
- `/`（半角单斜杠）：写面内仅 R-3 一处（被双测试钉死，转交）；其余 `/` 均为代码注释／路径／正则，非可见串。
- `·` 间隔号空格：写面内可见 `·`（R-1／R-2／`TEXT_HEADER_TEMPLATE='【{skill} · {key}】'`／help lead）**全部为带空格 ` · `**，
> 紧贴 `·`（`记支出·回执` 式）仅出现在 `src/record/`＋`src/shared/` 的标题三件套（D-·-02），转交（§6 H-6）。
- `;`／`|` 半角：写面内可见串**零命中**（D-；-00／D-｜-00 合规记账一致）；`|` 仅 TS 联合类型，`;` 仅语句结束。

## §5 门禁（持锁跑，15 秒轮询语义由包装器承担）

- `tsc -b packages/base-render`：EXIT 0（他席在途改动下仍绿；本窗零改码，无新增失败）。
- `node --test packages/base-render/test/help.test.mjs`：pass 48／fail 0（§2 引用的 W14 逐字断言在内，全绿）。

GATE-RUN runId=1b1ca6e1-6f25-454c-b4af-1b4925938b68 cmd="node node_modules/typescript/bin/tsc -b packages/base-render"
GATE-RUN runId=de7d6538-28d2-496a-9e83-422be9750613 cmd="node --test packages/base-render/test/help.test.mjs"

| 运行 | runId 短 | exit | 摘要 |
|---|---|---|---|
| base tsc | 1b1ca6e1 | 0 | 零诊断（waitedMs 10011，被他席持锁挡一轮后拿到锁，活进程未抢回） |
| help 单测 | de7d6538 | 0 | pass 48／fail 0 |

- 日志（本窗独占目录，落盘＋只读尾行）：`.scratch/t410-F3/tsc.log`、`.scratch/t410-F3/help-test.log`
> （`.scratch` 忽略不入库，备查；长输出未读进上下文，只读尾 5–6 行与 `pass/fail` 行）。
- 变异自证：本窗源码零改动，无变异面；以「复跑即绿」代替红绿对（tsc＋48 条逐字断言全绿即形状无回退）。
- 超线报警：本窗**未改任何** `packages/` 源码件——**无超线触发**（第四步无报警）。
> 只读 citation 的件行数不计入本窗（未写即不触发）。

## §6 转交清单（F1／F2 接，按文件切写面；每条带形状处方，D 词典同源）

| # | D 组 | 落点（源码根） | 影响页（页内搜复算键） | 形状处方（D 词典） |
|---|---|---|---|---|
| H-1 | D-·-02 标题三件套＋间隔号 `,` | `src/record/collectBody.ts:164-165`／`receiptBody.ts:108-109`＋各场景同式（borrow:225,283 等，D-·-02 行）；base 侧 `packages/base-render/src/help.ts:411` 的半角 `,`（W14 钉死，改须同步 `test/help.test.mjs:338`） | 32/32 H1＋`<title>`（搜 ` · 补齐`／` · 回执`／` · 挑一条`） | H1 只留唤醒词，页型进页型徽章；base lead 的 `,` 改 `，`（与测试同改） |
| H-2 | D-·-01 眉标＋D-·-05 HELP 标题 | `packages/skill-bill/src/shared/pageShell.ts:23-24`（`记账 · 写入域`）；`src/render/helpFile.ts:27`（HELP 标题） | 32/32 眉标（搜 `记账 · 写入域`）；HELP 页 | 眉标改双徽章 `[记账][写入域]`；HELP 标题改主标题＋版本徽章（先解 `help-file-145` 逐字锁＋老实物 parity） |
| H-3 | D-·-03 候选标签／D-·-04 依据 | `scene-borrow.ts:70`／`scene-lend.ts:70`／`scene-collect.ts:73`／`scene-repay.ts:73`；`scene-collect.ts:76,135`／`scene-repay.ts:76,135` | 借贷族 4 采集候选表 `是哪一笔` 列（搜 `是哪一笔`）；收回／偿还 `为什么是它` 列 | 分类与备注各占一列（或徽章副行）；依据后半句独立成第二枚状态徽章；`#N · 分类 · 时间` 拆三格 |
| H-4 | D-；-01～08（全角 `；` 8 组） | `record/slots.ts:39,45,49`；`scene-borrow.ts:108`（lend/collect/repay/reimburse 同式）；副标题 `；本型另需：`（borrow:155 等）；prompt 連写（borrow:170-171 等）；`shared/outsideScan.ts:120-124`；`shared/blockedSlots.ts:77-80`；`shared/candidatePick.ts:126`；`shared/userWording.ts:136-137` | 16 采集副标题＋prompt 预块＋口径行（搜 `本型另需`／`这一笔还差`／`写库：还没发生`／`这一页先不写库`） | 分段标题＋缺项标签＋进度（D-；-01～08 各自处方；`envelope.message` 机读串保持原样） |
| H-5 | D-｜-01＋`:#` 8 处＋D-/-01～03 | `shared/copyArea.ts:137`（`｜`）；`:#` 在 4 采集口径预块（报销到账／偿还／收回／退款 L17／L22）；分类路径 `scene-borrow.ts:39` 等 6 处；举例 `slots.ts:39` 等；要素連写 `outsideScan.ts:63,104,125` | 32 页 `data-t` 日志段（搜 `｜`）；4 采集指导预块（搜 `原记录:#`）；借贷族分类格（搜 `借贷/`） | 日志内分段标题（命令原文／过程各一段）；`:#` 改全角 `：`；分类改三级面包屑徽章组；举例改级联示例；三要素各一枚徽章 |
| H-6 | D-、-01～07／D-—-01～02／D-（）-01～03 | `shared/blockedSlots.ts:77,80,113`；`record/collectBody.ts:79`；`shared/photoEscape.ts:178`；`outsideScan.ts:92,124`；各场景 `extra`；`record/receiptBody.ts:81,100`；`scene-lend.ts:233`；破折号 9 采集（`scene-update.ts:153` 等＋`collectBody.ts:137`）；`installmentPreview.ts:53,65,76`；括号四型（prompt 占位／说明补充／状态补充） | 16 采集副标题＋阻断条＋回执写入明细（搜 `缺必需槽位`／`写进去`／`（已出采集页`／`（对象）`） | 缺项标签＋进度（副标题只留计数）；破折号句拆两行口径；占位符改缺项标签形态；计数类改计数徽章 |
| H-7 | D-·-05 日志时间戳＋render 分析摘要 | `shared/copyArea.ts:138`（` · 版本 `）；`src/render/views.ts:97,104`（`、`）；`src/render/helpFile.ts:112`（` / `，见 R-3） | `data-t` 日志段（搜 `版本`）；分析域摘要（32 页外） | 日志内分段标题；分析摘要接入标签组时同改；` / ` 改徽章组时同步双测试 |

- 以上 H-1～H-7 即 D 清单 26 组**全部位**的归属（D-；-00／D-｜-00／D-·-00／D-/-00／D-（）-04 五个合规记账组除外，
> 共 26 实施组＋5 合规组＝31 组，与 D 清单 §总览＋各节一一对应）。F3 在本窗写面内可实施组为 **0**（§2–§4），
> 故全部转交；F1／F2 按文件切面认领后，本件即 F3 的交棒单。

## §7 交付对账（与 §0 逐行对）

- §0 第 1 行：`docs/base/base-render/t410-F3-证据.md`（本件）——已落盘，待提交（§8）。
- §0 第 2 行：`style.ts`、`blocks.ts`——只读核验（diff 现场＋行号引用），**零改动**，偏差说明见 §2（在途占用，停手）。
- §0 第 3 行：render 件——只读核对 6 处（R-1～R-6），**零改动**，偏差说明见 §3（测试钉死／scope 外／已合规）。
- 偏差合计：源码改动 0 件（计划内「改」转为「确证＋转交」，派单允许的跨界停手路径）；证据件 1 件新增。
- 对账导出（本窗口 410 条目原文，抄自 `.scratch/locks/gate-runs.log:17689-17694`）：

```
START ticket=410 runId=1b1ca6e1-6f25-454c-b4af-1b4925938b68 cmd="node node_modules/typescript/bin/tsc -b packages/base-render" waitedMs=10011 pid=63268 at=2026-09-16T04:36:31.807Z
RUN ticket=410 runId=1b1ca6e1-6f25-454c-b4af-1b4925938b68 cmd="node node_modules/typescript/bin/tsc -b packages/base-render" waitedMs=10011 exit=0 pid=63268 at=2026-09-16T04:36:31.912Z
START ticket=410 runId=de7d6538-28d2-496a-9e83-422be9750613 cmd="node --test packages/base-render/test/help.test.mjs" waitedMs=0 pid=38848 at=2026-09-16T04:36:34.751Z
RUN ticket=410 runId=de7d6538-28d2-496a-9e83-422be9750613 cmd="node --test packages/base-render/test/help.test.mjs" waitedMs=0 exit=0 pid=38848 at=2026-09-16T04:36:34.918Z
```

## §8 提交记录（持锁，限定路径；本节在提交后补）

- （待补：commit sha＋提交信息清单）
