# base-render 组件目录架构（迁移台账）

件名 `base-render`（包名 `base-paint`）。**本文只记两处扩展与迁移台账**，规矩正文不在这里重复抄。

## 一、规矩正文的住处（唯一事实源，别在本文再写一份）

组件层怎么组织，正文住 **`src/components/index.ts` 的件头**：

```
src/components/
  index.ts          ← 组件层总出口（新增一个组件只在这里加一行）
  README.md         ← 这一层自己的规矩
  shared/           ← 组件之间共用的小件（转义／入参校验）
  <组件名>/          ← 一个组件一个目录，目录内自足
    index.ts · attrs.ts · render.ts · style.ts · runtime.ts · README.md
```

同一条规矩在两处各写一份就会走散——**要改规矩，改那个件头**，本文只跟着改「扩展」与台账。

## 二、两处扩展（本文的裁定）

### 扩展一：布局与出口是两件事，不要绑在一起

`src/components/index.ts` 现在写着「本层**不进**冻结面、**不从根导出**」。那是把「件住哪儿」与
「名字从哪条路径出去」绑成了一件事，后果是：**凡进冻结面的组件就只能散落在 `src/` 根目录**——正是扎堆的成因。

本件裁定：

- **目录只回答「件住哪儿」；冻结面只回答「哪个名字从哪条路径出去、签名钉不钉死」。** 两条轴各自独立。
- 冻结面的名字仍从**原路径**出（根出口、`base-paint/blocks` 等五条子路径），由组件目录的 `index.ts`
  **薄转出**；`src/spec/**` 的签名**一字不动**。
- 因此组件目录既可以是冻结的（`page-nav`／`blocks` 那三族），也可以是不进冻结面的（`editable-value`）；
  区别只在出口，不在住处。

### 扩展二：族的集合按变化频率分族，族内一件一文件

`blocks.ts`（2856 行）／`charts.ts`（2230 行）这种「一族 N 个组件」，不必拆成 N 个目录
（目录数会爆炸、读者迷路）。做法：**按变化频率分族目录，族内一个组件一个文件**。
切口不要现编——`行数告警线评估.md` §3.2／§4 已经定过（页框／数据展示／输入说明；直角坐标／圆形与微型；
静态呈现／运行时），照它执行。

## 三、迁移台账（现状 → 目标）

现状读数为 2026-09-24 当刻实测（LF 口径 `split('\n').length - 1`）：

| 批 | 现状件 | LF | 目标住处 | 状态 |
|---|---|---|---|---|
| ① | ~~`src/pageNav.ts`＋`pageNavCss.ts`~~ → `src/components/page-nav/{index,style}.ts` | 149／87 | `src/components/page-nav/` | **已完成**（产物哈希前后相同：`pageNavCss` `412b3341…`） |
| ① | ~~`src/pageBars.ts`＋`pageBarsCss.ts`~~ → `src/components/page-bars/{index,style}.ts` | 241／231 | `src/components/page-bars/` | **已完成**（产物哈希前后相同：`pageBarsCss` `bf7ff8b5…`；`pageShapeCss` `3b784ab6…`） |
| ② | ~~`src/pageShapes.ts`~~ → `src/components/page/{index,fact-strip,media,timeline,style,shared}.ts` | 488 | `src/components/page/` | **已完成**（族内一件一文件：事实条 119／媒体 240／时间轴 98／样式 30／共用 31／出口 12；`pageShapeCss` `3b784ab6…` 前后相同） |
| ② | ~~`src/pageUi.ts`＋`pageUiToast.ts`~~ → `src/components/page-ui/{index,toast}.ts` | 412／48 | `src/components/page-ui/` | **已完成**（`pageUiCss` `e5eea522…` 前后相同；**旧路径 `src/pageUi.ts` 留 10 行薄转出**——`dist/pageUi.js` 还有三个消费方见下） |
| ③ | `src/blocks.ts` | 2856 | `src/components/block-{page-frame,data-display,form-feedback}/`，本件留薄转出 | 待开工 |
| ④ | ~~`src/charts.ts`~~ → `src/components/charts/{index,shared,options,coords,svg,line,bar,donut,progress,combo,sparkline,gauge,scatter,dispatch,style,helpers}.ts` | 2230 | `src/components/charts/` | **已完成**（族内一件一文件，16 件；八种图表产物 ＋ `chartsCss` ＋ helpers JS ＋ 冻结面十条读数全同；旧路径留薄转出） |
| ⑤ | ~~`src/controls.ts`~~ → `src/components/controls/{index,shared,toast,copy,bind,helpers-meta,helpers,action-bar,status}.ts` | 1518 | `src/components/controls/` | **已完成**（族内一件一文件，9 件；五个渲染器产物 ＋ helpers JS ＋ **出口逐名 13 个**全同；旧路径留薄转出） |
| ⑤ | `src/style.ts` | 1409 | `src/components/style/` | 待开工（下一轮；件内是「常量 ＋ 12 区 CSS 大对象 ＋ 组装与校验」三块） |
| ⑥ | `src/help.ts` | 708 | `src/components/help/`（含 `helpShell` 与模板资产） | 待开工 |
| ⑥ | `src/text.ts`／`template.ts` | 497／430 | 同族一件一目录 | 待开工 |

线内件（`docShell` 99／`index` 100／`contract`／`injector`／`ui`／`spec/**`）**平铺不动**——
目录化是为了「一次改动通常只碰一个目录」，不是为了把每个小件都塞进目录。

**薄转出（兼容垫片）登记**：批次②之后 `src/pageUi.ts` 只剩 10 行转出。为什么留着——`dist/pageUi.js`
这个**产物路径**还有三个消费方按它取件：`src/docShell.ts`（并行席位在途，不敢碰）、`scripts/判分.mjs`、
`test/two-col-inset-919.test.mjs`／`test/two-col-align-879.test.mjs`。等那三处引用迁到组件目录后即可删本件。
批次④之后 `src/charts.ts` 同理只剩 8 行：`dist/charts.js` 的消费方是 `src/blocks.ts`／`src/docShell.ts`
（两件都在并行席位的在途写集里）、`src/style.ts`（其 import 行被 `test/style.test.mjs` 钉着）与 5 个测试文件。

**第四步声明（超线件）**：批次④之后 `src/components/charts/line.ts` **698 LF**，仍在 350 行告警线之上——
**已超线，需要根据规则进行重构。** 超因：`renderLine`（`line.ts` 现存正文的主体）是**一个连续函数里的
一整条装配链**（量程判定 → 刻度与标签 → 路径与区域 → 标记线与标记点 → 触摸位），搬迁批次只能原样搬，
切开属于**改写**而不是搬迁。拆法＝把 8 个路径构造器（`bridgePath`／`polyPath`／`smoothPath`／`stepPath`／
`areaPath`／`betweenPath` ＋ `Pt` 类型）另立 `path.ts`（约 130 行），`renderLine` 再按
「量程与刻度／序列与标记／组装」切三段；**本批先不拆**（改写须另开票，并在「产物逐字节相同」判据下做）。

批次⑤（controls 半）之后 `src/components/controls/helpers.ts` **633 LF** 同样在线之上——
**已超线，需要根据规则进行重构。** 超因：`buildSharedHelpersJs()` 是**一个函数产出一整段运行时 JS 文本**
（约 600 行字符串模板：复制三格式菜单／toast 栈／HELP 速查台的搜索与回到顶部），段与段之间靠这个函数
里的局部变量串联，切开要重排函数体。拆法＝按「复制菜单／toast 栈／HELP 速查台」把文本切成三个并列的
产出函数、由一个组装函数拼回（顺序与分隔符逐字不动）；**本批先不拆**（同上：属改写，须另开票＋逐字节判据）。

## 四、每批的判据（照仓规）

1. **产物逐字节相同**：搬迁前后同一批入口的产物（页面 HTML／CSS 文本／运行时 JS 文本）逐字节相同。
   `blocksCss()`／`pageShapeCss()` 这类**组装函数**尤其要盯：文本拼接顺序是产物的一部分。
2. **全量测试绿**：`node node_modules/typescript/bin/tsc -b packages/base-render/tsconfig.json` ＋
   `node --test "packages/base-render/test/*.test.mjs"`（他席在途造成的既有红照实记账，不算本批的）。
3. **出口与签名不动**：五条子路径（`.`／`blocks`／`docShell`／`help-shell`／`save-html`）与
   `SPEC_FROZEN_SURFACE` 逐条不变；消费方（6 个技能包 214 处 `base-paint/blocks` import）零改动。
4. **一批一个提交**，提交信息写明批次号与判据读数。

## 五、共享工作区：与并行席位的关系

本包由多个席位并行改（实测同一份工作区里同时有：`src/components/editable-value/**`＋`shared/**`＋
`src/components/index.ts` 的组件层抽取在途；另一席在 `help*`／`docShell.ts` 一带）。

- 分席按**目录**切：谁改哪个组件就住哪个目录，不再两人抢同一个文件——这正是本次目录化的目的。
- 动 `src/components/index.ts` 时**只加一行、不重排**，避免与另一席的同一文件冲突。
- 搬迁期间若目标目录正在被人改，**让位**、先做别的批次，不抢同一段代码。
