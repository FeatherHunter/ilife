﻿﻿﻿﻿Part of #161

> 被基准票阻塞（本票开工前 `docs/skills/skill-calorie/t516-场景10-视觉整改基准.md` 必须已在盘上且已关票）。
> **形状标杆（开工先读，别凭空设计）**：提交 `71cd5e5`（票 #467「看今日目标进度」整页重做，2026-09-15 12:24）＋ 证据件 `docs/skills/skill-calorie/t467-重做-证据.md`（345 行，读它的 §一「照的哪六条标准」与 §三「三组读数」）。本仓已有成文的六条标准、三组读数体例、四档取值、变异自证做法——**一律照它，不另造尺**：
> ① 完整文档（`<!doctype html>`／charset／视口 meta／全内联／单文件／零外部引用）；② 四档自适应（**390／512／820／1440**，横向溢出 0 **且逐元素越界数 0**；KPI 每行卡数**按卡的实际位置数**，不信 `gridTemplateColumns` 字符串切分）；③ 可见文本零内部标识符（零 snake_case／零裸英文枚举／零命令键／零票号）；④ 分隔符＝设计债（探针 `0／0／0`）；⑤ 形状化——**全部取 `packages/base-render/` 现成件**（`renderKpiCard`／`renderTocBlock`／`renderConclusionBar`／`renderCaliberLine`／`renderDataTable`／`renderChartBlock`／`renderListRows`／空态件）；**页面本地一行色值、一个字号都不许写**；⑥ 口径句走口径行，页上无整段纯文本墙。另加 `t425-融合基准.md` 裁定 3 的三条恒出：页内导航 ＋ 口径说明行 ＋ 来源脚注。
> **缺的形状加到 `packages/base-render/`，不在页面里就地写死**；加了就要带「改坏必红」的负向证据。
> **收尾两道包内门**：`node packages/skill-calorie/scripts/check-warning-line.mjs`（`RESULT: n/n` ＋ `PASS: 告警线台账齐全且与实况一致`）与 `node packages/skill-calorie/scripts/gen-cli.mjs --check`（`GEN-CHECK PASS`）。



> **本轮最重要的一条落点结论（编排者实测）**：场景10 的 31 页是**全仓唯一没接上公共层形状的一族**。31 页 DOM 逐类点数：`ilife-block-conclusion`＝**0**／`ilife-block-toc`＝**0**／`ilife-block-caliber`＝**0**／`ilife-block-disclosure`＝**0**／`ilife-block-chip`＝**0**／`ilife-empty`＝**0**／`ilife-status-badge`＝**7**。而同仓别的族都接了：`homeDocs.ts:323`／`goalProgressDocs.ts:336-337`／`libraryDocs.ts:187`／`rankingDocs.ts:200`／`sourceStatsDocs.ts:88`／`receipt.ts:313`／`records.ts:261` 都在用 `renderTocBlock`／`renderConclusionBar`。**`trendDocs.ts` 与 `reportDoc*.ts` 里这两个产出器的命中数＝0。** 成因＝这一族由 #383–#385 建成，那时 #401／#507 的形状词汇还没定稿，之后从未回填。
> ⇒ **本票的主形态是「接上现成件」，不是新建形状**：`renderConclusionBar`／`renderTocBlock`／`renderCaliberLine`／`renderChips`／`renderDisclosure`／状态徽章／空态件／`renderListRows`／`renderDataTable`／`renderChartBlock` 一律**优先调用**。新建形状是**例外**，每新建一个都要在证据件里具名清单并说明为什么现成件不行。
> ⇒ **要求⑤的判据不是「分隔符为 0」**：把 `；`／`·` 删掉却不换形状＝删信息，不算整改。判据是「分隔符为 0 **且**公共层形状类的命中达到骨架要求」。
> ⇒ **不要再去修公共层已修过的东西**（实测已绿）：图早已 `preserveAspectRatio="xMidYMid meet"`（非等比拉伸是旧账）；数值列早已 `min-width:5.5em ＋ color:var(--fg) ＋ tabular-nums`；列头早已 `--fg3`／11.5px；结论条产出器 `blocks.ts:285` 已建；段标题三族已同名同值（`blocks.ts:1668` 起）。

## 目标

按基准件先做出**共享形状**，再用**一页样板**把全套判据跑绿，给后面两票立可照抄的样板。样板页＝`calorie.view.deficit`（查热量缺口／看热量缺口，`buildDeficitDoc`）。

两件事：

1. **共享形状**：页面壳断点与安全区、字号阶梯、窄屏表格行为、卡片栅格、同族区块标题同值、结论条形状、图表非等比拉伸、数据表主次层级，以及要求⑤点名的形状化产出器（键值行／徽章列／公式条／进度条）。
2. **样板页重排**：`buildDeficitDoc`（`packages/skill-calorie/src/render/trendDocs.ts:340`）按新形状重排，并按基准的文案规则清一遍可见文本。

**落点判定（本票必须给出结论并落盘）**：共享形状**落 `packages/base-render/src/{style,blocks,charts}.ts`**——这是本仓六条标准第 ⑤ 条的明文（「页面本地一行色值、一个字号都不许写」，版面单源住 `packages/base-render/`），不是可选项。

判定顺序：① 先找公共层现成件（`renderKpiCard`／`renderTocBlock`／`renderConclusionBar`／`renderCaliberLine`／`renderDataTable`／`renderChartBlock`／`renderListRows`／空态件／状态徽章）——能用就**只调用、不改它**；② 现成件缺的形状，**加到公共层**并带「改坏必红」负向证据；③ 只有在「落公共层会让别的技能的门变红、且改造成本高于收益」时才退到卡路里包内共享件，**且必须在基准件里具名记账**：退让的是哪一条形状、为什么、代价是什么。

**判「能不能落公共层」的依据**：跑 `packages/base-render/test/*` 与另外五个技能的靶向测试；变红的**具体是哪几条断言、属于哪件**要写进证据。不许用「影响面大」当理由——影响面大要先量出来。

## 验收命令

- **主判据：按 `docs/skills/skill-calorie/t516-场景10-视觉整改基准.md` 的全尺逐条跑，逐条绿**（这是本票真正的验收；改前这些判据是红的）。
- **回归判据（改前就已绿，只许保持不许变红）**：`node packages/skill-calorie/scripts/measure-responsive.mjs --dir <样板页目录> --json <路径>` —— 样板页 390／768／1440 三档逐格 0。已知：整改前 31 页 × 3 档 = 93 格**全部**为 0，所以「零溢出」不是整改目标，只是不许退化的底线。
- `node packages/skill-calorie/scripts/audit-separators.mjs --dir <样板页目录> --json <路径>`：样板页**节点级读数全零**。
- 靶向测试全绿：`analysis-deficit-385`／`analysis-accept-386` ＋ 本票新增断言件；**若落公共层**，`packages/base-render/test/*` 与其它技能靶向测试也必须全绿（这是「能不能落公共层」的判定依据）。
- **变异自证两行**：改坏一处（例：把结论条形状换回行内魔法值／把卡片栅格改回 `auto-fit`）→ 相关测试必红；逐文件还原 → 必绿。
- **vision 审查**：样板页 1440／390 两档截图交视觉审查席，五维总分 ≥ 基准件给的线。截图与审查结论落盘。
- 全部命令经加锁包装器跑；证据逐条 `GATE-RUN runId=`。

## 不许动的东西

- **不改字段与取值口径**：`analysis-*.test.mjs` 冻结的六列表头（日期／摄入／消耗／缺口／目标／状态）与各 KPI 标签、数值口径一律不动，只改呈现。
- **不自造第二份样式表**：色值只取 `packages/base-render/src/spec/style.ts` 的 11 个冻结 token；圆角只取闭集 `{8,14,20,999,50%}`；不新增断点值（照 HELP 那三层）。
- **不动后续两票的地盘**：`trendDocs.ts` 只许动 `buildDeficitDoc` 与其上方的共用小件（`fmt`／`unitOf`／`deltaHuman`／`humanText`／`techNoteHtml`），其余 9 个 `build*Doc` 一行不碰；`src/analysis/reportDoc*.ts`／`reportPlate.ts` 一行不碰。
- 禁 `git add -A`／`git add .`／`stash`／`reset --hard`／`clean`／`checkout -- .`／`switch`／`amend`／`rebase`／`force`。

## 交付物路径

- 改动件：`packages/base-render/src/{style,blocks,charts}.ts`（若落公共层）与／或卡路里包内共享件；`packages/skill-calorie/src/render/trendDocs.ts`（只 `buildDeficitDoc` 段）
- 新测试：`packages/skill-calorie/test/analysis-shape-520.test.mjs`
- 证据：`docs/skills/skill-calorie/t517-W1-共享形状与样板-证据.md`
- 样板产物与截图：`.scratch/t517/样板/`
- 台账同步：改了超线件必须跑 `node packages/skill-calorie/scripts/check-warning-line.mjs --sync`（先 `--dry` 演练），并让本包 AGENTS.md 的台账与实况一致

## 遗留出口

- `src/render/trendDocs.ts` 已 1019 LF 超线，本票改动只会更长 ⇒ 收尾按本包 AGENTS.md 的必报五步报一句并给拆法；**拆法照台账已写好的那条**：预测／诊断薄页切姊妹件 `trendPredictDocs.ts`。实际搬家与预测族重排合并进下一票，本票不搬。
- 若共享形状必须落公共层、而影响面超出本票窗口，就地收尾：把已绿的共享件与样板页提交，剩下的公共层改动**当场开一张具名票**，并把编号回写本票。






## 样板页现状与改法要点（编排者已读源码，直接照此，不必重新发现）

样板页 `calorie.view.deficit`（`21-查热量缺口.html`／`22-看热量缺口.html`，**两页字节完全相同＝同一产物两个别名**）的装配件是 `packages/skill-calorie/src/render/trendDocs.ts:340 buildDeficitDoc`。它的内容是一个 7 项的 `parts` 数组，逐项现状如下（行号为当刻源码）：

| # | 现状 | 行 | 改法要点 |
|---|---|---|---|
| 1 | `noteBits = techNoteHtml([...])` —— 三条口径（缺口算式、`KCAL_PER_KG=7700`、图读法）**被塞进 HTML 注释，读者看不见** | `:345-349` | **必须改成可见的「口径说明行」**（`renderCaliberLine`，`blocks.ts:271`）——这是 `t425-融合基准.md` 裁定 3 的「三条恒出」之一，当刻**整页没有**。注释留源码里可以，但不能替代口径行 |
| 2 | `renderParamForm({fields:[开始,结束], description:'缺口就是当天消耗减掉当天吃的：…'})` | `:352-355` | 保留件；宽屏可考虑与 KPI 并排（见下「宽屏余量」） |
| 3 | `renderKpiGrid([日均摄入/日均消耗/日均缺口/理论减重])` | `:356-361` | 保留件；`日均消耗` 的 detail 现写 `'日常消耗 ' + tdee + ' ＋ 运动 ' + avgExerciseBurn + ' 卡'` —— **「加法分解」要换形状**（堆叠条／两行键值），别用 `＋` 串 |
| 4 | `renderChartBlock({kind:'line', title:'每日摄入与消耗', …})` | `:367-386` | 保留件；图题已合规（画法已移到 `markLine` 标签与图例名） |
| 5 | `renderDataTable({columns:[日期,摄入,消耗,缺口,目标,状态], caption:'缺口明细（共 N 天），其中 X 天是工作日，Y 天是周末'})` | `:391-410` | ① **`状态` 列现把判定词当**单元格文本**印**：`s.deficit >= targetDef ? '✓ 达标' : s.deficit > 0 ? '⚠ 偏低' : '✗ 超量'`（`:405`）⇒ 该换成**状态徽章**（`renderStatusBadge`，闭集 `STATUS_KINDS`，`spec/controls.ts:289-298`）。② `目标` 列 7 行全是同一个 `+300`、`状态` 列 7 行全同一个词 ⇒ 零信息量的两列占 1/3 宽度，**该上浮成表注或徽章**。③ caption 里「共 N 天 ＋ X 工作日 ＋ Y 周末」三件事串一行 ⇒ 形状化（徽章列／键值） |
| 6 | `renderListRows({items:[合计摄入,合计消耗,合计缺口]})` | `:413-417` | 保留件；右槽已是整句，合规 |
| 7 | `dataCopyArea('复制数据', {envelope:{...}})` | `:418-…` | 保留件（含「复制日志」） |

**页内导航（`renderTocBlock`）的落点**：按上面 7 项，可锚的区块有 6 个——参数（开始／结束）、概览（4 张 KPI 卡）、每日摄入与消耗（图）、缺口明细（表）、合计（3 行）、数据与日志（复制区）。**每个区块必须先有 `id`，导航每项 `href="#x"` 都要有同 `id` 目标**（这条已写成判据，见票面「形状标杆」段）。

**结论条（`renderConclusionBar`）的素材**：当刻整页没有结论条（`ilife-block-conclusion` 命中 0）。页里现成的结论素材是 4 张 KPI 卡与合计三行——一条人话结论句要自己从这些数里写出来（例如把「日均缺口 ＋ 理论减重 ＋ 周缺口」串成一句「这段时间平均每天有 N 卡缺口，折算一周约 N 卡，按 7700 卡≈1 公斤估算，预计掉 N 公斤」）。**结论句必须是页里已有的数，不许新算。**

**宽屏余量**：当刻内容列固定 `max-width:960px`，1440 档两侧各空约 240px。这一步的选择（加宽列／页面壳侧两列栅格／只让表与图吃掉余量）由你在本票裁定并写明理由；`.ilife-help-shell-grid` 那个既有类的取舍也在这里一并裁（见票面「形状标杆」段）。

