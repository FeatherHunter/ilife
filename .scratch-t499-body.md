对比页（`calorie.photo.compare`）自己的超预算横幅仍是系统口吻的老句：形如「超预算横幅：已嵌入 1 张，还有 1 张未嵌入（单页上限 1 MiB）·替代操作：改查单张详情分看，或换小图后重跑」。同域其它页已在 #472 换成「照片较多，本页只显示 N 张；还有 M 张没显示」＋公共提示块（`notice()`）的人话口径，并不再暴露 1 MiB／字节数。两位独立审查席都点名了这一处走散。

## 目标

让这一处横幅与「看身材照」页（#472）同口径：

- 说人话，去掉「超预算横幅：」这种开发前缀与「单页上限 1 MiB」这类内部单位，页面不再出现 `1 MiB`／字节数；
- 超限不许静默：让位时必须仍写明**哪张没显示**与**为什么**，并给出**替代操作**（这是 #438／#472 定下的口径）。

## 判据

- `packages/skill-calorie/src/photo/compareDoc.ts` 的横幅字串换成与 #472 同族的人话，且保留「哪张没显示／为什么」与「替代操作」；
- `packages/skill-calorie/test/photo-shape-read-281.test.mjs` 里钉这句话的断言同步收紧（不许放宽成永真）；负向仍要能咬住——把新句改回旧句必红；
- 变异两行机器读数：新句改回旧句 → 编译 → 测试必红 → 逐字节还原（sha256 一致）→ 必绿（还原后 `tsc -b --force` 再构建，不靠 mtime）；
- 回归全绿：`photo-shape-read-281`／`photo-shape-341`／`photo-budget-438`／`photo-gif-page-352`／`photo-picker-283`／`photo-receipt-docs-282`／`wizard-86`／`photo-responsive-484`／`photo-helpdoc-488`（红项须精确归因）；
- 受影响样张重出且三档溢出为 0：`09-04-对比两张照片（跨标签·间隔48天）.html`（真库只读副本）、`09-05-生成身材照GIF（结果页·内嵌GIF）.html`（顺带与最新 dist 对齐）。

## 不许动

- `PHOTO_LIST_PAGE_MAX_BYTES` 的值；
- 命令声明（`src/photo/commands.ts` 及汇总位）；
- `src/render/html.ts`（只读）；
- 本轮其它页已定文案（`galleryDoc.ts`／`viewerDoc.ts`／`receipt.ts` 等一律不碰）；
- 老技能目录只读，且不读其 `.个人笔记不允许参考/`；
- **不跑** `gen-photo-baseline.mjs --write`。

## 遗留出口

十页基线须由收口票重落：本票改可见文本会让 `calorie.photo.compare` 那页 sha 变；`#493` 刚重落过（钉死渲染「今天」、双轮 `BASELINE-OK 10/10`），故本票落地后要再落一次。接续关系：本票 → 收口票重落十页基线（含 `calorie.photo.compare`）。

证据件：`docs/skills/skill-calorie/t499-对比横幅.md`。
