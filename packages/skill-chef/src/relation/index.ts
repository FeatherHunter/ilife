/** 派生能力门：能力对外只经这里（结构纪律铁律一、铁律五）。
 *
 * 对外两件：命令声明表（暂空）与路由声明表（全 tbd）。处理函数与取数实现不在这里，
 * 本票只立声明；外边要用派生能力的东西，只经这两个名字。
 */

export { RELATION_COMMANDS } from './commands.js';
export { RELATION_ROUTES } from './routes.js';
