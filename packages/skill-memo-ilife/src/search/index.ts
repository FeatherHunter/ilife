/** 查找域 · **能力门**（票 #855）：本域对外的唯一一道门。
 *
 * 门规（照 `docs/agents/structure.md` 铁律五与 `命令登记纪律.md` 形状一）：
 *   - 只转出**跨目录真要用**的名字。今天只有命令声明 `SEARCH_COMMANDS` 一个——生成的
 *     `src/cli/registry.ts` 从**门**取数组（不深引域内实现件）；
 *   - 域内互相用的东西（`run.ts` 里的两个处理函数）**不出这个门**——门里怎么实现，外面不看；
 *   - 别的域要用查找域的取数结果，走这道门；同域内部件之间直接深引，不经门。
 */
export { SEARCH_COMMANDS } from './commands.js';
