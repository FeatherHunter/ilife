/** 减重速率的两套阈值（#717 批④ 第 3 件）：**登记在册、各自具名**——不是一处，但每一处都追得到出处。
 *
 * 谁在用（写得出哪两个能力在用）：
 *   ① 分析能力 `analysis/`——`simulate.ts` 的「目标可达性」与 `simulate2.ts` 的「缺口↔速率合理性」；
 *   ② 页面层（`render/trendPredictDocs.ts` 的 `看热量缺口` 页口径行印的就是 `DEFICIT_RATE_TOLERANCE`）。
 *
 * 本件**零依赖**：不 import 任何件，也不出现任何一个能力目录的名字。
 *
 * **为什么是两处而不是一处**（#717 实施时据实判定，登记在此）：
 *   · `TARGET_RATE`（0.5–1.0 kg/周）——**目标速率**：定目标时「每周掉多少算合理」，老家 `simulate.py` 的口径；
 *   · `DEFICIT_RATE_TOLERANCE`（0.3–1.2 kg/周）——**缺口折算的容差带**：由已知日均缺口反推的速率落在
 *     「大致合理」的范围内即可，比目标速率宽松（它回答的是「这个缺口水平靠不靠谱」，不是「该定多少」）。
 *   两者合并会**改用户可见阈值**（`看热量缺口` 页的判定会从 0.3–1.2 收紧到 0.5–1.0），
 *   属内容裁定而非施工——规格 §三 的三裁不含此项。故本件的交付是：**把两处都搬进共用位、各自具名、
 *   写清各自的用处**，让「阈值到底有几套、各是什么」这一问只有一处答案，而不是散在算式里的裸数字。
 */

/** 目标速率（kg/周）：定目标时的合理区间（老家 `simulate.py` 口径）。 */
export const TARGET_RATE: readonly [number, number] = [0.5, 1.0];

/** 由缺口折算的速率的**容差带**（kg/周）：`看热量缺口` 页判「缺口水平靠不靠谱」用它，比目标速率宽松。 */
export const DEFICIT_RATE_TOLERANCE: readonly [number, number] = [0.3, 1.2];
