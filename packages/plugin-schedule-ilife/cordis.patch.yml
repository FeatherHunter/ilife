# dsh-schedule-ilife bundle 层：单命令双包由 dsh plugin add 装配（包声明 dsh.bundle.patch），
# 无需手改任何 profile 文件；dsh plugin remove 时自动移除。
- insert:
    - id: dsh-schedule-ilife
      name: 'dsh-schedule-ilife'
      config: {}
